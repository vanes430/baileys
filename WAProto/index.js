/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars*/
"use strict";

var $protobuf = require("protobufjs/minimal");

// Common aliases
var $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;

// Exported root namespace
var $root = $protobuf.roots["default"] || ($protobuf.roots["default"] = {});

$root.proto = (function() {

    /**
     * Namespace proto.
     * @exports proto
     * @namespace
     */
    var proto = {};

    proto.ADVDeviceIdentity = (function() {

        /**
         * Properties of a ADVDeviceIdentity.
         * @memberof proto
         * @interface IADVDeviceIdentity
         * @property {number|null} [rawId] ADVDeviceIdentity rawId
         * @property {number|Long|null} [timestamp] ADVDeviceIdentity timestamp
         * @property {number|null} [keyIndex] ADVDeviceIdentity keyIndex
         * @property {proto.ADVEncryptionType|null} [accountType] ADVDeviceIdentity accountType
         * @property {proto.ADVEncryptionType|null} [deviceType] ADVDeviceIdentity deviceType
         */

        /**
         * Constructs a new ADVDeviceIdentity.
         * @memberof proto
         * @classdesc Represents a ADVDeviceIdentity.
         * @implements IADVDeviceIdentity
         * @constructor
         * @param {proto.IADVDeviceIdentity=} [properties] Properties to set
         */
        function ADVDeviceIdentity(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ADVDeviceIdentity rawId.
         * @member {number|null|undefined} rawId
         * @memberof proto.ADVDeviceIdentity
         * @instance
         */
        ADVDeviceIdentity.prototype.rawId = null;

        /**
         * ADVDeviceIdentity timestamp.
         * @member {number|Long|null|undefined} timestamp
         * @memberof proto.ADVDeviceIdentity
         * @instance
         */
        ADVDeviceIdentity.prototype.timestamp = null;

        /**
         * ADVDeviceIdentity keyIndex.
         * @member {number|null|undefined} keyIndex
         * @memberof proto.ADVDeviceIdentity
         * @instance
         */
        ADVDeviceIdentity.prototype.keyIndex = null;

        /**
         * ADVDeviceIdentity accountType.
         * @member {proto.ADVEncryptionType|null|undefined} accountType
         * @memberof proto.ADVDeviceIdentity
         * @instance
         */
        ADVDeviceIdentity.prototype.accountType = null;

        /**
         * ADVDeviceIdentity deviceType.
         * @member {proto.ADVEncryptionType|null|undefined} deviceType
         * @memberof proto.ADVDeviceIdentity
         * @instance
         */
        ADVDeviceIdentity.prototype.deviceType = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVDeviceIdentity.prototype, "_rawId", {
            get: $util.oneOfGetter($oneOfFields = ["rawId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVDeviceIdentity.prototype, "_timestamp", {
            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVDeviceIdentity.prototype, "_keyIndex", {
            get: $util.oneOfGetter($oneOfFields = ["keyIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVDeviceIdentity.prototype, "_accountType", {
            get: $util.oneOfGetter($oneOfFields = ["accountType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVDeviceIdentity.prototype, "_deviceType", {
            get: $util.oneOfGetter($oneOfFields = ["deviceType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ADVDeviceIdentity instance using the specified properties.
         * @function create
         * @memberof proto.ADVDeviceIdentity
         * @static
         * @param {proto.IADVDeviceIdentity=} [properties] Properties to set
         * @returns {proto.ADVDeviceIdentity} ADVDeviceIdentity instance
         */
        ADVDeviceIdentity.create = function create(properties) {
            return new ADVDeviceIdentity(properties);
        };

        /**
         * Encodes the specified ADVDeviceIdentity message. Does not implicitly {@link proto.ADVDeviceIdentity.verify|verify} messages.
         * @function encode
         * @memberof proto.ADVDeviceIdentity
         * @static
         * @param {proto.IADVDeviceIdentity} message ADVDeviceIdentity message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ADVDeviceIdentity.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.rawId != null && Object.hasOwnProperty.call(message, "rawId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.rawId);
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.timestamp);
            if (message.keyIndex != null && Object.hasOwnProperty.call(message, "keyIndex"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.keyIndex);
            if (message.accountType != null && Object.hasOwnProperty.call(message, "accountType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.accountType);
            if (message.deviceType != null && Object.hasOwnProperty.call(message, "deviceType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.deviceType);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ADVDeviceIdentity message, length delimited. Does not implicitly {@link proto.ADVDeviceIdentity.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ADVDeviceIdentity
         * @static
         * @param {proto.IADVDeviceIdentity} message ADVDeviceIdentity message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ADVDeviceIdentity.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ADVDeviceIdentity message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ADVDeviceIdentity
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ADVDeviceIdentity} ADVDeviceIdentity
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ADVDeviceIdentity.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.rawId = reader.uint32();
                        message._rawId = "rawId";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.timestamp = reader.uint64();
                        message._timestamp = "timestamp";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.keyIndex = reader.uint32();
                        message._keyIndex = "keyIndex";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.accountType = reader.int32();
                        message._accountType = "accountType";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.deviceType = reader.int32();
                        message._deviceType = "deviceType";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a ADVDeviceIdentity message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ADVDeviceIdentity
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ADVDeviceIdentity} ADVDeviceIdentity
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ADVDeviceIdentity.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ADVDeviceIdentity message.
         * @function verify
         * @memberof proto.ADVDeviceIdentity
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ADVDeviceIdentity.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.rawId != null && Object.hasOwnProperty.call(message, "rawId")) {
                properties._rawId = 1;
                if (!$util.isInteger(message.rawId))
                    return "rawId: integer expected";
            }
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp")) {
                properties._timestamp = 1;
                if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                    return "timestamp: integer|Long expected";
            }
            if (message.keyIndex != null && Object.hasOwnProperty.call(message, "keyIndex")) {
                properties._keyIndex = 1;
                if (!$util.isInteger(message.keyIndex))
                    return "keyIndex: integer expected";
            }
            if (message.accountType != null && Object.hasOwnProperty.call(message, "accountType")) {
                properties._accountType = 1;
                if (typeof message.accountType !== "number" || (message.accountType | 0) !== message.accountType)
                    return "accountType: enum value expected";
            }
            if (message.deviceType != null && Object.hasOwnProperty.call(message, "deviceType")) {
                properties._deviceType = 1;
                if (typeof message.deviceType !== "number" || (message.deviceType | 0) !== message.deviceType)
                    return "deviceType: enum value expected";
            }
            return null;
        };

        /**
         * Creates a ADVDeviceIdentity message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ADVDeviceIdentity
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ADVDeviceIdentity} ADVDeviceIdentity
         */
        ADVDeviceIdentity.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.ADVDeviceIdentity: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.rawId != null)
                message.rawId = object.rawId >>> 0;
            if (object.timestamp != null)
                if ($util.Long)
                    message.timestamp = $util.Long.fromValue(object.timestamp, true);
                else if (typeof object.timestamp === "string")
                    message.timestamp = parseInt(object.timestamp, 10);
                else if (typeof object.timestamp === "number")
                    message.timestamp = object.timestamp;
                else if (typeof object.timestamp === "object")
                    message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber(true);
            if (object.keyIndex != null)
                message.keyIndex = object.keyIndex >>> 0;
            switch (object.accountType) {
            case "E2EE":
            case 0:
                message.accountType = 0;
                break;
            case "HOSTED":
            case 1:
                message.accountType = 1;
                break;
            case "NON_E2EE":
            case 2:
                message.accountType = 2;
                break;
            default:
                if (typeof object.accountType === "number" && (object.accountType | 0) === object.accountType)
                    message.accountType = object.accountType;
            }
            switch (object.deviceType) {
            case "E2EE":
            case 0:
                message.deviceType = 0;
                break;
            case "HOSTED":
            case 1:
                message.deviceType = 1;
                break;
            case "NON_E2EE":
            case 2:
                message.deviceType = 2;
                break;
            default:
                if (typeof object.deviceType === "number" && (object.deviceType | 0) === object.deviceType)
                    message.deviceType = object.deviceType;
            }
            return message;
        };

        /**
         * Creates a plain object from a ADVDeviceIdentity message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ADVDeviceIdentity
         * @static
         * @param {proto.ADVDeviceIdentity} message ADVDeviceIdentity
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ADVDeviceIdentity.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.rawId != null && Object.hasOwnProperty.call(message, "rawId"))
                object.rawId = message.rawId;
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.timestamp = typeof message.timestamp === "number" ? BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, true).toBigInt();
                else if (typeof message.timestamp === "number")
                    object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                else
                    object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber(true) : message.timestamp;
            if (message.keyIndex != null && Object.hasOwnProperty.call(message, "keyIndex"))
                object.keyIndex = message.keyIndex;
            if (message.accountType != null && Object.hasOwnProperty.call(message, "accountType"))
                object.accountType = options.enums === String ? $root.proto.ADVEncryptionType[message.accountType] === undefined ? message.accountType : $root.proto.ADVEncryptionType[message.accountType] : message.accountType;
            if (message.deviceType != null && Object.hasOwnProperty.call(message, "deviceType"))
                object.deviceType = options.enums === String ? $root.proto.ADVEncryptionType[message.deviceType] === undefined ? message.deviceType : $root.proto.ADVEncryptionType[message.deviceType] : message.deviceType;
            return object;
        };

        /**
         * Converts this ADVDeviceIdentity to JSON.
         * @function toJSON
         * @memberof proto.ADVDeviceIdentity
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ADVDeviceIdentity.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ADVDeviceIdentity
         * @function getTypeUrl
         * @memberof proto.ADVDeviceIdentity
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ADVDeviceIdentity.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.ADVDeviceIdentity";
        };

        return ADVDeviceIdentity;
    })();

    /**
     * ADVEncryptionType enum.
     * @name proto.ADVEncryptionType
     * @enum {number}
     * @property {number} E2EE=0 E2EE value
     * @property {number} HOSTED=1 HOSTED value
     * @property {number} NON_E2EE=2 NON_E2EE value
     */
    proto.ADVEncryptionType = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "E2EE"] = 0;
        values[valuesById[1] = "HOSTED"] = 1;
        values[valuesById[2] = "NON_E2EE"] = 2;
        return values;
    })();

    proto.ADVKeyIndexList = (function() {

        /**
         * Properties of a ADVKeyIndexList.
         * @memberof proto
         * @interface IADVKeyIndexList
         * @property {number|null} [rawId] ADVKeyIndexList rawId
         * @property {number|Long|null} [timestamp] ADVKeyIndexList timestamp
         * @property {number|null} [currentIndex] ADVKeyIndexList currentIndex
         * @property {Array.<number>|null} [validIndexes] ADVKeyIndexList validIndexes
         * @property {proto.ADVEncryptionType|null} [accountType] ADVKeyIndexList accountType
         */

        /**
         * Constructs a new ADVKeyIndexList.
         * @memberof proto
         * @classdesc Represents a ADVKeyIndexList.
         * @implements IADVKeyIndexList
         * @constructor
         * @param {proto.IADVKeyIndexList=} [properties] Properties to set
         */
        function ADVKeyIndexList(properties) {
            this.validIndexes = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ADVKeyIndexList rawId.
         * @member {number|null|undefined} rawId
         * @memberof proto.ADVKeyIndexList
         * @instance
         */
        ADVKeyIndexList.prototype.rawId = null;

        /**
         * ADVKeyIndexList timestamp.
         * @member {number|Long|null|undefined} timestamp
         * @memberof proto.ADVKeyIndexList
         * @instance
         */
        ADVKeyIndexList.prototype.timestamp = null;

        /**
         * ADVKeyIndexList currentIndex.
         * @member {number|null|undefined} currentIndex
         * @memberof proto.ADVKeyIndexList
         * @instance
         */
        ADVKeyIndexList.prototype.currentIndex = null;

        /**
         * ADVKeyIndexList validIndexes.
         * @member {Array.<number>} validIndexes
         * @memberof proto.ADVKeyIndexList
         * @instance
         */
        ADVKeyIndexList.prototype.validIndexes = $util.emptyArray;

        /**
         * ADVKeyIndexList accountType.
         * @member {proto.ADVEncryptionType|null|undefined} accountType
         * @memberof proto.ADVKeyIndexList
         * @instance
         */
        ADVKeyIndexList.prototype.accountType = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVKeyIndexList.prototype, "_rawId", {
            get: $util.oneOfGetter($oneOfFields = ["rawId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVKeyIndexList.prototype, "_timestamp", {
            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVKeyIndexList.prototype, "_currentIndex", {
            get: $util.oneOfGetter($oneOfFields = ["currentIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVKeyIndexList.prototype, "_accountType", {
            get: $util.oneOfGetter($oneOfFields = ["accountType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ADVKeyIndexList instance using the specified properties.
         * @function create
         * @memberof proto.ADVKeyIndexList
         * @static
         * @param {proto.IADVKeyIndexList=} [properties] Properties to set
         * @returns {proto.ADVKeyIndexList} ADVKeyIndexList instance
         */
        ADVKeyIndexList.create = function create(properties) {
            return new ADVKeyIndexList(properties);
        };

        /**
         * Encodes the specified ADVKeyIndexList message. Does not implicitly {@link proto.ADVKeyIndexList.verify|verify} messages.
         * @function encode
         * @memberof proto.ADVKeyIndexList
         * @static
         * @param {proto.IADVKeyIndexList} message ADVKeyIndexList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ADVKeyIndexList.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.rawId != null && Object.hasOwnProperty.call(message, "rawId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.rawId);
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.timestamp);
            if (message.currentIndex != null && Object.hasOwnProperty.call(message, "currentIndex"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.currentIndex);
            if (message.validIndexes != null && message.validIndexes.length) {
                writer.uint32(/* id 4, wireType 2 =*/34).fork();
                for (var i = 0; i < message.validIndexes.length; ++i)
                    writer.uint32(message.validIndexes[i]);
                writer.ldelim();
            }
            if (message.accountType != null && Object.hasOwnProperty.call(message, "accountType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.accountType);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ADVKeyIndexList message, length delimited. Does not implicitly {@link proto.ADVKeyIndexList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ADVKeyIndexList
         * @static
         * @param {proto.IADVKeyIndexList} message ADVKeyIndexList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ADVKeyIndexList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ADVKeyIndexList message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ADVKeyIndexList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ADVKeyIndexList} ADVKeyIndexList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ADVKeyIndexList.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.rawId = reader.uint32();
                        message._rawId = "rawId";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.timestamp = reader.uint64();
                        message._timestamp = "timestamp";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.currentIndex = reader.uint32();
                        message._currentIndex = "currentIndex";
                        continue;
                    }
                case 4: {
                        if (u === 2) {
                            if (!(message.validIndexes && message.validIndexes.length))
                                message.validIndexes = [];
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.validIndexes.push(reader.uint32());
                            continue;
                        }
                        if (u !== 0)
                            break;
                        if (!(message.validIndexes && message.validIndexes.length))
                            message.validIndexes = [];
                        message.validIndexes.push(reader.uint32());
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.accountType = reader.int32();
                        message._accountType = "accountType";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a ADVKeyIndexList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ADVKeyIndexList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ADVKeyIndexList} ADVKeyIndexList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ADVKeyIndexList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ADVKeyIndexList message.
         * @function verify
         * @memberof proto.ADVKeyIndexList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ADVKeyIndexList.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.rawId != null && Object.hasOwnProperty.call(message, "rawId")) {
                properties._rawId = 1;
                if (!$util.isInteger(message.rawId))
                    return "rawId: integer expected";
            }
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp")) {
                properties._timestamp = 1;
                if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                    return "timestamp: integer|Long expected";
            }
            if (message.currentIndex != null && Object.hasOwnProperty.call(message, "currentIndex")) {
                properties._currentIndex = 1;
                if (!$util.isInteger(message.currentIndex))
                    return "currentIndex: integer expected";
            }
            if (message.validIndexes != null && Object.hasOwnProperty.call(message, "validIndexes")) {
                if (!Array.isArray(message.validIndexes))
                    return "validIndexes: array expected";
                for (var i = 0; i < message.validIndexes.length; ++i)
                    if (!$util.isInteger(message.validIndexes[i]))
                        return "validIndexes: integer[] expected";
            }
            if (message.accountType != null && Object.hasOwnProperty.call(message, "accountType")) {
                properties._accountType = 1;
                if (typeof message.accountType !== "number" || (message.accountType | 0) !== message.accountType)
                    return "accountType: enum value expected";
            }
            return null;
        };

        /**
         * Creates a ADVKeyIndexList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ADVKeyIndexList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ADVKeyIndexList} ADVKeyIndexList
         */
        ADVKeyIndexList.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.ADVKeyIndexList: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.rawId != null)
                message.rawId = object.rawId >>> 0;
            if (object.timestamp != null)
                if ($util.Long)
                    message.timestamp = $util.Long.fromValue(object.timestamp, true);
                else if (typeof object.timestamp === "string")
                    message.timestamp = parseInt(object.timestamp, 10);
                else if (typeof object.timestamp === "number")
                    message.timestamp = object.timestamp;
                else if (typeof object.timestamp === "object")
                    message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber(true);
            if (object.currentIndex != null)
                message.currentIndex = object.currentIndex >>> 0;
            if (object.validIndexes) {
                if (!Array.isArray(object.validIndexes))
                    throw TypeError(".proto.ADVKeyIndexList.validIndexes: array expected");
                message.validIndexes = Array(object.validIndexes.length);
                for (var i = 0; i < object.validIndexes.length; ++i)
                    message.validIndexes[i] = object.validIndexes[i] >>> 0;
            }
            switch (object.accountType) {
            case "E2EE":
            case 0:
                message.accountType = 0;
                break;
            case "HOSTED":
            case 1:
                message.accountType = 1;
                break;
            case "NON_E2EE":
            case 2:
                message.accountType = 2;
                break;
            default:
                if (typeof object.accountType === "number" && (object.accountType | 0) === object.accountType)
                    message.accountType = object.accountType;
            }
            return message;
        };

        /**
         * Creates a plain object from a ADVKeyIndexList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ADVKeyIndexList
         * @static
         * @param {proto.ADVKeyIndexList} message ADVKeyIndexList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ADVKeyIndexList.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.validIndexes = [];
            if (message.rawId != null && Object.hasOwnProperty.call(message, "rawId"))
                object.rawId = message.rawId;
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.timestamp = typeof message.timestamp === "number" ? BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, true).toBigInt();
                else if (typeof message.timestamp === "number")
                    object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                else
                    object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber(true) : message.timestamp;
            if (message.currentIndex != null && Object.hasOwnProperty.call(message, "currentIndex"))
                object.currentIndex = message.currentIndex;
            if (message.validIndexes && message.validIndexes.length) {
                object.validIndexes = Array(message.validIndexes.length);
                for (var j = 0; j < message.validIndexes.length; ++j)
                    object.validIndexes[j] = message.validIndexes[j];
            }
            if (message.accountType != null && Object.hasOwnProperty.call(message, "accountType"))
                object.accountType = options.enums === String ? $root.proto.ADVEncryptionType[message.accountType] === undefined ? message.accountType : $root.proto.ADVEncryptionType[message.accountType] : message.accountType;
            return object;
        };

        /**
         * Converts this ADVKeyIndexList to JSON.
         * @function toJSON
         * @memberof proto.ADVKeyIndexList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ADVKeyIndexList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ADVKeyIndexList
         * @function getTypeUrl
         * @memberof proto.ADVKeyIndexList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ADVKeyIndexList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.ADVKeyIndexList";
        };

        return ADVKeyIndexList;
    })();

    proto.ADVSignedDeviceIdentity = (function() {

        /**
         * Properties of a ADVSignedDeviceIdentity.
         * @memberof proto
         * @interface IADVSignedDeviceIdentity
         * @property {Uint8Array|null} [details] ADVSignedDeviceIdentity details
         * @property {Uint8Array|null} [accountSignatureKey] ADVSignedDeviceIdentity accountSignatureKey
         * @property {Uint8Array|null} [accountSignature] ADVSignedDeviceIdentity accountSignature
         * @property {Uint8Array|null} [deviceSignature] ADVSignedDeviceIdentity deviceSignature
         */

        /**
         * Constructs a new ADVSignedDeviceIdentity.
         * @memberof proto
         * @classdesc Represents a ADVSignedDeviceIdentity.
         * @implements IADVSignedDeviceIdentity
         * @constructor
         * @param {proto.IADVSignedDeviceIdentity=} [properties] Properties to set
         */
        function ADVSignedDeviceIdentity(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ADVSignedDeviceIdentity details.
         * @member {Uint8Array|null|undefined} details
         * @memberof proto.ADVSignedDeviceIdentity
         * @instance
         */
        ADVSignedDeviceIdentity.prototype.details = null;

        /**
         * ADVSignedDeviceIdentity accountSignatureKey.
         * @member {Uint8Array|null|undefined} accountSignatureKey
         * @memberof proto.ADVSignedDeviceIdentity
         * @instance
         */
        ADVSignedDeviceIdentity.prototype.accountSignatureKey = null;

        /**
         * ADVSignedDeviceIdentity accountSignature.
         * @member {Uint8Array|null|undefined} accountSignature
         * @memberof proto.ADVSignedDeviceIdentity
         * @instance
         */
        ADVSignedDeviceIdentity.prototype.accountSignature = null;

        /**
         * ADVSignedDeviceIdentity deviceSignature.
         * @member {Uint8Array|null|undefined} deviceSignature
         * @memberof proto.ADVSignedDeviceIdentity
         * @instance
         */
        ADVSignedDeviceIdentity.prototype.deviceSignature = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVSignedDeviceIdentity.prototype, "_details", {
            get: $util.oneOfGetter($oneOfFields = ["details"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVSignedDeviceIdentity.prototype, "_accountSignatureKey", {
            get: $util.oneOfGetter($oneOfFields = ["accountSignatureKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVSignedDeviceIdentity.prototype, "_accountSignature", {
            get: $util.oneOfGetter($oneOfFields = ["accountSignature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVSignedDeviceIdentity.prototype, "_deviceSignature", {
            get: $util.oneOfGetter($oneOfFields = ["deviceSignature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ADVSignedDeviceIdentity instance using the specified properties.
         * @function create
         * @memberof proto.ADVSignedDeviceIdentity
         * @static
         * @param {proto.IADVSignedDeviceIdentity=} [properties] Properties to set
         * @returns {proto.ADVSignedDeviceIdentity} ADVSignedDeviceIdentity instance
         */
        ADVSignedDeviceIdentity.create = function create(properties) {
            return new ADVSignedDeviceIdentity(properties);
        };

        /**
         * Encodes the specified ADVSignedDeviceIdentity message. Does not implicitly {@link proto.ADVSignedDeviceIdentity.verify|verify} messages.
         * @function encode
         * @memberof proto.ADVSignedDeviceIdentity
         * @static
         * @param {proto.IADVSignedDeviceIdentity} message ADVSignedDeviceIdentity message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ADVSignedDeviceIdentity.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.details != null && Object.hasOwnProperty.call(message, "details"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.details);
            if (message.accountSignatureKey != null && Object.hasOwnProperty.call(message, "accountSignatureKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.accountSignatureKey);
            if (message.accountSignature != null && Object.hasOwnProperty.call(message, "accountSignature"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.accountSignature);
            if (message.deviceSignature != null && Object.hasOwnProperty.call(message, "deviceSignature"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.deviceSignature);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ADVSignedDeviceIdentity message, length delimited. Does not implicitly {@link proto.ADVSignedDeviceIdentity.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ADVSignedDeviceIdentity
         * @static
         * @param {proto.IADVSignedDeviceIdentity} message ADVSignedDeviceIdentity message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ADVSignedDeviceIdentity.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ADVSignedDeviceIdentity message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ADVSignedDeviceIdentity
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ADVSignedDeviceIdentity} ADVSignedDeviceIdentity
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ADVSignedDeviceIdentity.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.details = reader.bytes();
                        message._details = "details";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.accountSignatureKey = reader.bytes();
                        message._accountSignatureKey = "accountSignatureKey";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.accountSignature = reader.bytes();
                        message._accountSignature = "accountSignature";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.deviceSignature = reader.bytes();
                        message._deviceSignature = "deviceSignature";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a ADVSignedDeviceIdentity message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ADVSignedDeviceIdentity
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ADVSignedDeviceIdentity} ADVSignedDeviceIdentity
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ADVSignedDeviceIdentity.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ADVSignedDeviceIdentity message.
         * @function verify
         * @memberof proto.ADVSignedDeviceIdentity
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ADVSignedDeviceIdentity.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.details != null && Object.hasOwnProperty.call(message, "details")) {
                properties._details = 1;
                if (!(message.details && typeof message.details.length === "number" || $util.isString(message.details)))
                    return "details: buffer expected";
            }
            if (message.accountSignatureKey != null && Object.hasOwnProperty.call(message, "accountSignatureKey")) {
                properties._accountSignatureKey = 1;
                if (!(message.accountSignatureKey && typeof message.accountSignatureKey.length === "number" || $util.isString(message.accountSignatureKey)))
                    return "accountSignatureKey: buffer expected";
            }
            if (message.accountSignature != null && Object.hasOwnProperty.call(message, "accountSignature")) {
                properties._accountSignature = 1;
                if (!(message.accountSignature && typeof message.accountSignature.length === "number" || $util.isString(message.accountSignature)))
                    return "accountSignature: buffer expected";
            }
            if (message.deviceSignature != null && Object.hasOwnProperty.call(message, "deviceSignature")) {
                properties._deviceSignature = 1;
                if (!(message.deviceSignature && typeof message.deviceSignature.length === "number" || $util.isString(message.deviceSignature)))
                    return "deviceSignature: buffer expected";
            }
            return null;
        };

        /**
         * Creates a ADVSignedDeviceIdentity message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ADVSignedDeviceIdentity
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ADVSignedDeviceIdentity} ADVSignedDeviceIdentity
         */
        ADVSignedDeviceIdentity.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.ADVSignedDeviceIdentity: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.details != null)
                if (typeof object.details === "string")
                    $util.base64.decode(object.details, message.details = $util.newBuffer($util.base64.length(object.details)), 0);
                else if (object.details.length >= 0)
                    message.details = object.details;
            if (object.accountSignatureKey != null)
                if (typeof object.accountSignatureKey === "string")
                    $util.base64.decode(object.accountSignatureKey, message.accountSignatureKey = $util.newBuffer($util.base64.length(object.accountSignatureKey)), 0);
                else if (object.accountSignatureKey.length >= 0)
                    message.accountSignatureKey = object.accountSignatureKey;
            if (object.accountSignature != null)
                if (typeof object.accountSignature === "string")
                    $util.base64.decode(object.accountSignature, message.accountSignature = $util.newBuffer($util.base64.length(object.accountSignature)), 0);
                else if (object.accountSignature.length >= 0)
                    message.accountSignature = object.accountSignature;
            if (object.deviceSignature != null)
                if (typeof object.deviceSignature === "string")
                    $util.base64.decode(object.deviceSignature, message.deviceSignature = $util.newBuffer($util.base64.length(object.deviceSignature)), 0);
                else if (object.deviceSignature.length >= 0)
                    message.deviceSignature = object.deviceSignature;
            return message;
        };

        /**
         * Creates a plain object from a ADVSignedDeviceIdentity message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ADVSignedDeviceIdentity
         * @static
         * @param {proto.ADVSignedDeviceIdentity} message ADVSignedDeviceIdentity
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ADVSignedDeviceIdentity.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.details != null && Object.hasOwnProperty.call(message, "details"))
                object.details = options.bytes === String ? $util.base64.encode(message.details, 0, message.details.length) : options.bytes === Array ? Array.prototype.slice.call(message.details) : message.details;
            if (message.accountSignatureKey != null && Object.hasOwnProperty.call(message, "accountSignatureKey"))
                object.accountSignatureKey = options.bytes === String ? $util.base64.encode(message.accountSignatureKey, 0, message.accountSignatureKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.accountSignatureKey) : message.accountSignatureKey;
            if (message.accountSignature != null && Object.hasOwnProperty.call(message, "accountSignature"))
                object.accountSignature = options.bytes === String ? $util.base64.encode(message.accountSignature, 0, message.accountSignature.length) : options.bytes === Array ? Array.prototype.slice.call(message.accountSignature) : message.accountSignature;
            if (message.deviceSignature != null && Object.hasOwnProperty.call(message, "deviceSignature"))
                object.deviceSignature = options.bytes === String ? $util.base64.encode(message.deviceSignature, 0, message.deviceSignature.length) : options.bytes === Array ? Array.prototype.slice.call(message.deviceSignature) : message.deviceSignature;
            return object;
        };

        /**
         * Converts this ADVSignedDeviceIdentity to JSON.
         * @function toJSON
         * @memberof proto.ADVSignedDeviceIdentity
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ADVSignedDeviceIdentity.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ADVSignedDeviceIdentity
         * @function getTypeUrl
         * @memberof proto.ADVSignedDeviceIdentity
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ADVSignedDeviceIdentity.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.ADVSignedDeviceIdentity";
        };

        return ADVSignedDeviceIdentity;
    })();

    proto.ADVSignedDeviceIdentityHMAC = (function() {

        /**
         * Properties of a ADVSignedDeviceIdentityHMAC.
         * @memberof proto
         * @interface IADVSignedDeviceIdentityHMAC
         * @property {Uint8Array|null} [details] ADVSignedDeviceIdentityHMAC details
         * @property {Uint8Array|null} [hmac] ADVSignedDeviceIdentityHMAC hmac
         * @property {proto.ADVEncryptionType|null} [accountType] ADVSignedDeviceIdentityHMAC accountType
         */

        /**
         * Constructs a new ADVSignedDeviceIdentityHMAC.
         * @memberof proto
         * @classdesc Represents a ADVSignedDeviceIdentityHMAC.
         * @implements IADVSignedDeviceIdentityHMAC
         * @constructor
         * @param {proto.IADVSignedDeviceIdentityHMAC=} [properties] Properties to set
         */
        function ADVSignedDeviceIdentityHMAC(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ADVSignedDeviceIdentityHMAC details.
         * @member {Uint8Array|null|undefined} details
         * @memberof proto.ADVSignedDeviceIdentityHMAC
         * @instance
         */
        ADVSignedDeviceIdentityHMAC.prototype.details = null;

        /**
         * ADVSignedDeviceIdentityHMAC hmac.
         * @member {Uint8Array|null|undefined} hmac
         * @memberof proto.ADVSignedDeviceIdentityHMAC
         * @instance
         */
        ADVSignedDeviceIdentityHMAC.prototype.hmac = null;

        /**
         * ADVSignedDeviceIdentityHMAC accountType.
         * @member {proto.ADVEncryptionType|null|undefined} accountType
         * @memberof proto.ADVSignedDeviceIdentityHMAC
         * @instance
         */
        ADVSignedDeviceIdentityHMAC.prototype.accountType = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVSignedDeviceIdentityHMAC.prototype, "_details", {
            get: $util.oneOfGetter($oneOfFields = ["details"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVSignedDeviceIdentityHMAC.prototype, "_hmac", {
            get: $util.oneOfGetter($oneOfFields = ["hmac"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVSignedDeviceIdentityHMAC.prototype, "_accountType", {
            get: $util.oneOfGetter($oneOfFields = ["accountType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ADVSignedDeviceIdentityHMAC instance using the specified properties.
         * @function create
         * @memberof proto.ADVSignedDeviceIdentityHMAC
         * @static
         * @param {proto.IADVSignedDeviceIdentityHMAC=} [properties] Properties to set
         * @returns {proto.ADVSignedDeviceIdentityHMAC} ADVSignedDeviceIdentityHMAC instance
         */
        ADVSignedDeviceIdentityHMAC.create = function create(properties) {
            return new ADVSignedDeviceIdentityHMAC(properties);
        };

        /**
         * Encodes the specified ADVSignedDeviceIdentityHMAC message. Does not implicitly {@link proto.ADVSignedDeviceIdentityHMAC.verify|verify} messages.
         * @function encode
         * @memberof proto.ADVSignedDeviceIdentityHMAC
         * @static
         * @param {proto.IADVSignedDeviceIdentityHMAC} message ADVSignedDeviceIdentityHMAC message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ADVSignedDeviceIdentityHMAC.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.details != null && Object.hasOwnProperty.call(message, "details"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.details);
            if (message.hmac != null && Object.hasOwnProperty.call(message, "hmac"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.hmac);
            if (message.accountType != null && Object.hasOwnProperty.call(message, "accountType"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.accountType);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ADVSignedDeviceIdentityHMAC message, length delimited. Does not implicitly {@link proto.ADVSignedDeviceIdentityHMAC.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ADVSignedDeviceIdentityHMAC
         * @static
         * @param {proto.IADVSignedDeviceIdentityHMAC} message ADVSignedDeviceIdentityHMAC message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ADVSignedDeviceIdentityHMAC.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ADVSignedDeviceIdentityHMAC message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ADVSignedDeviceIdentityHMAC
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ADVSignedDeviceIdentityHMAC} ADVSignedDeviceIdentityHMAC
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ADVSignedDeviceIdentityHMAC.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.details = reader.bytes();
                        message._details = "details";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.hmac = reader.bytes();
                        message._hmac = "hmac";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.accountType = reader.int32();
                        message._accountType = "accountType";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a ADVSignedDeviceIdentityHMAC message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ADVSignedDeviceIdentityHMAC
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ADVSignedDeviceIdentityHMAC} ADVSignedDeviceIdentityHMAC
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ADVSignedDeviceIdentityHMAC.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ADVSignedDeviceIdentityHMAC message.
         * @function verify
         * @memberof proto.ADVSignedDeviceIdentityHMAC
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ADVSignedDeviceIdentityHMAC.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.details != null && Object.hasOwnProperty.call(message, "details")) {
                properties._details = 1;
                if (!(message.details && typeof message.details.length === "number" || $util.isString(message.details)))
                    return "details: buffer expected";
            }
            if (message.hmac != null && Object.hasOwnProperty.call(message, "hmac")) {
                properties._hmac = 1;
                if (!(message.hmac && typeof message.hmac.length === "number" || $util.isString(message.hmac)))
                    return "hmac: buffer expected";
            }
            if (message.accountType != null && Object.hasOwnProperty.call(message, "accountType")) {
                properties._accountType = 1;
                if (typeof message.accountType !== "number" || (message.accountType | 0) !== message.accountType)
                    return "accountType: enum value expected";
            }
            return null;
        };

        /**
         * Creates a ADVSignedDeviceIdentityHMAC message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ADVSignedDeviceIdentityHMAC
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ADVSignedDeviceIdentityHMAC} ADVSignedDeviceIdentityHMAC
         */
        ADVSignedDeviceIdentityHMAC.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.ADVSignedDeviceIdentityHMAC: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.details != null)
                if (typeof object.details === "string")
                    $util.base64.decode(object.details, message.details = $util.newBuffer($util.base64.length(object.details)), 0);
                else if (object.details.length >= 0)
                    message.details = object.details;
            if (object.hmac != null)
                if (typeof object.hmac === "string")
                    $util.base64.decode(object.hmac, message.hmac = $util.newBuffer($util.base64.length(object.hmac)), 0);
                else if (object.hmac.length >= 0)
                    message.hmac = object.hmac;
            switch (object.accountType) {
            case "E2EE":
            case 0:
                message.accountType = 0;
                break;
            case "HOSTED":
            case 1:
                message.accountType = 1;
                break;
            case "NON_E2EE":
            case 2:
                message.accountType = 2;
                break;
            default:
                if (typeof object.accountType === "number" && (object.accountType | 0) === object.accountType)
                    message.accountType = object.accountType;
            }
            return message;
        };

        /**
         * Creates a plain object from a ADVSignedDeviceIdentityHMAC message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ADVSignedDeviceIdentityHMAC
         * @static
         * @param {proto.ADVSignedDeviceIdentityHMAC} message ADVSignedDeviceIdentityHMAC
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ADVSignedDeviceIdentityHMAC.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.details != null && Object.hasOwnProperty.call(message, "details"))
                object.details = options.bytes === String ? $util.base64.encode(message.details, 0, message.details.length) : options.bytes === Array ? Array.prototype.slice.call(message.details) : message.details;
            if (message.hmac != null && Object.hasOwnProperty.call(message, "hmac"))
                object.hmac = options.bytes === String ? $util.base64.encode(message.hmac, 0, message.hmac.length) : options.bytes === Array ? Array.prototype.slice.call(message.hmac) : message.hmac;
            if (message.accountType != null && Object.hasOwnProperty.call(message, "accountType"))
                object.accountType = options.enums === String ? $root.proto.ADVEncryptionType[message.accountType] === undefined ? message.accountType : $root.proto.ADVEncryptionType[message.accountType] : message.accountType;
            return object;
        };

        /**
         * Converts this ADVSignedDeviceIdentityHMAC to JSON.
         * @function toJSON
         * @memberof proto.ADVSignedDeviceIdentityHMAC
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ADVSignedDeviceIdentityHMAC.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ADVSignedDeviceIdentityHMAC
         * @function getTypeUrl
         * @memberof proto.ADVSignedDeviceIdentityHMAC
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ADVSignedDeviceIdentityHMAC.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.ADVSignedDeviceIdentityHMAC";
        };

        return ADVSignedDeviceIdentityHMAC;
    })();

    proto.ADVSignedKeyIndexList = (function() {

        /**
         * Properties of a ADVSignedKeyIndexList.
         * @memberof proto
         * @interface IADVSignedKeyIndexList
         * @property {Uint8Array|null} [details] ADVSignedKeyIndexList details
         * @property {Uint8Array|null} [accountSignature] ADVSignedKeyIndexList accountSignature
         * @property {Uint8Array|null} [accountSignatureKey] ADVSignedKeyIndexList accountSignatureKey
         */

        /**
         * Constructs a new ADVSignedKeyIndexList.
         * @memberof proto
         * @classdesc Represents a ADVSignedKeyIndexList.
         * @implements IADVSignedKeyIndexList
         * @constructor
         * @param {proto.IADVSignedKeyIndexList=} [properties] Properties to set
         */
        function ADVSignedKeyIndexList(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ADVSignedKeyIndexList details.
         * @member {Uint8Array|null|undefined} details
         * @memberof proto.ADVSignedKeyIndexList
         * @instance
         */
        ADVSignedKeyIndexList.prototype.details = null;

        /**
         * ADVSignedKeyIndexList accountSignature.
         * @member {Uint8Array|null|undefined} accountSignature
         * @memberof proto.ADVSignedKeyIndexList
         * @instance
         */
        ADVSignedKeyIndexList.prototype.accountSignature = null;

        /**
         * ADVSignedKeyIndexList accountSignatureKey.
         * @member {Uint8Array|null|undefined} accountSignatureKey
         * @memberof proto.ADVSignedKeyIndexList
         * @instance
         */
        ADVSignedKeyIndexList.prototype.accountSignatureKey = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVSignedKeyIndexList.prototype, "_details", {
            get: $util.oneOfGetter($oneOfFields = ["details"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVSignedKeyIndexList.prototype, "_accountSignature", {
            get: $util.oneOfGetter($oneOfFields = ["accountSignature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVSignedKeyIndexList.prototype, "_accountSignatureKey", {
            get: $util.oneOfGetter($oneOfFields = ["accountSignatureKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ADVSignedKeyIndexList instance using the specified properties.
         * @function create
         * @memberof proto.ADVSignedKeyIndexList
         * @static
         * @param {proto.IADVSignedKeyIndexList=} [properties] Properties to set
         * @returns {proto.ADVSignedKeyIndexList} ADVSignedKeyIndexList instance
         */
        ADVSignedKeyIndexList.create = function create(properties) {
            return new ADVSignedKeyIndexList(properties);
        };

        /**
         * Encodes the specified ADVSignedKeyIndexList message. Does not implicitly {@link proto.ADVSignedKeyIndexList.verify|verify} messages.
         * @function encode
         * @memberof proto.ADVSignedKeyIndexList
         * @static
         * @param {proto.IADVSignedKeyIndexList} message ADVSignedKeyIndexList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ADVSignedKeyIndexList.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.details != null && Object.hasOwnProperty.call(message, "details"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.details);
            if (message.accountSignature != null && Object.hasOwnProperty.call(message, "accountSignature"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.accountSignature);
            if (message.accountSignatureKey != null && Object.hasOwnProperty.call(message, "accountSignatureKey"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.accountSignatureKey);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ADVSignedKeyIndexList message, length delimited. Does not implicitly {@link proto.ADVSignedKeyIndexList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ADVSignedKeyIndexList
         * @static
         * @param {proto.IADVSignedKeyIndexList} message ADVSignedKeyIndexList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ADVSignedKeyIndexList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ADVSignedKeyIndexList message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ADVSignedKeyIndexList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ADVSignedKeyIndexList} ADVSignedKeyIndexList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ADVSignedKeyIndexList.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.details = reader.bytes();
                        message._details = "details";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.accountSignature = reader.bytes();
                        message._accountSignature = "accountSignature";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.accountSignatureKey = reader.bytes();
                        message._accountSignatureKey = "accountSignatureKey";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a ADVSignedKeyIndexList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ADVSignedKeyIndexList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ADVSignedKeyIndexList} ADVSignedKeyIndexList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ADVSignedKeyIndexList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ADVSignedKeyIndexList message.
         * @function verify
         * @memberof proto.ADVSignedKeyIndexList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ADVSignedKeyIndexList.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.details != null && Object.hasOwnProperty.call(message, "details")) {
                properties._details = 1;
                if (!(message.details && typeof message.details.length === "number" || $util.isString(message.details)))
                    return "details: buffer expected";
            }
            if (message.accountSignature != null && Object.hasOwnProperty.call(message, "accountSignature")) {
                properties._accountSignature = 1;
                if (!(message.accountSignature && typeof message.accountSignature.length === "number" || $util.isString(message.accountSignature)))
                    return "accountSignature: buffer expected";
            }
            if (message.accountSignatureKey != null && Object.hasOwnProperty.call(message, "accountSignatureKey")) {
                properties._accountSignatureKey = 1;
                if (!(message.accountSignatureKey && typeof message.accountSignatureKey.length === "number" || $util.isString(message.accountSignatureKey)))
                    return "accountSignatureKey: buffer expected";
            }
            return null;
        };

        /**
         * Creates a ADVSignedKeyIndexList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ADVSignedKeyIndexList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ADVSignedKeyIndexList} ADVSignedKeyIndexList
         */
        ADVSignedKeyIndexList.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.ADVSignedKeyIndexList: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.details != null)
                if (typeof object.details === "string")
                    $util.base64.decode(object.details, message.details = $util.newBuffer($util.base64.length(object.details)), 0);
                else if (object.details.length >= 0)
                    message.details = object.details;
            if (object.accountSignature != null)
                if (typeof object.accountSignature === "string")
                    $util.base64.decode(object.accountSignature, message.accountSignature = $util.newBuffer($util.base64.length(object.accountSignature)), 0);
                else if (object.accountSignature.length >= 0)
                    message.accountSignature = object.accountSignature;
            if (object.accountSignatureKey != null)
                if (typeof object.accountSignatureKey === "string")
                    $util.base64.decode(object.accountSignatureKey, message.accountSignatureKey = $util.newBuffer($util.base64.length(object.accountSignatureKey)), 0);
                else if (object.accountSignatureKey.length >= 0)
                    message.accountSignatureKey = object.accountSignatureKey;
            return message;
        };

        /**
         * Creates a plain object from a ADVSignedKeyIndexList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ADVSignedKeyIndexList
         * @static
         * @param {proto.ADVSignedKeyIndexList} message ADVSignedKeyIndexList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ADVSignedKeyIndexList.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.details != null && Object.hasOwnProperty.call(message, "details"))
                object.details = options.bytes === String ? $util.base64.encode(message.details, 0, message.details.length) : options.bytes === Array ? Array.prototype.slice.call(message.details) : message.details;
            if (message.accountSignature != null && Object.hasOwnProperty.call(message, "accountSignature"))
                object.accountSignature = options.bytes === String ? $util.base64.encode(message.accountSignature, 0, message.accountSignature.length) : options.bytes === Array ? Array.prototype.slice.call(message.accountSignature) : message.accountSignature;
            if (message.accountSignatureKey != null && Object.hasOwnProperty.call(message, "accountSignatureKey"))
                object.accountSignatureKey = options.bytes === String ? $util.base64.encode(message.accountSignatureKey, 0, message.accountSignatureKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.accountSignatureKey) : message.accountSignatureKey;
            return object;
        };

        /**
         * Converts this ADVSignedKeyIndexList to JSON.
         * @function toJSON
         * @memberof proto.ADVSignedKeyIndexList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ADVSignedKeyIndexList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ADVSignedKeyIndexList
         * @function getTypeUrl
         * @memberof proto.ADVSignedKeyIndexList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ADVSignedKeyIndexList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.ADVSignedKeyIndexList";
        };

        return ADVSignedKeyIndexList;
    })();

    proto.AIHomeState = (function() {

        /**
         * Properties of a AIHomeState.
         * @memberof proto
         * @interface IAIHomeState
         * @property {number|Long|null} [lastFetchTime] AIHomeState lastFetchTime
         * @property {Array.<proto.AIHomeState.IAIHomeOption>|null} [capabilityOptions] AIHomeState capabilityOptions
         * @property {Array.<proto.AIHomeState.IAIHomeOption>|null} [conversationOptions] AIHomeState conversationOptions
         */

        /**
         * Constructs a new AIHomeState.
         * @memberof proto
         * @classdesc Represents a AIHomeState.
         * @implements IAIHomeState
         * @constructor
         * @param {proto.IAIHomeState=} [properties] Properties to set
         */
        function AIHomeState(properties) {
            this.capabilityOptions = [];
            this.conversationOptions = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AIHomeState lastFetchTime.
         * @member {number|Long|null|undefined} lastFetchTime
         * @memberof proto.AIHomeState
         * @instance
         */
        AIHomeState.prototype.lastFetchTime = null;

        /**
         * AIHomeState capabilityOptions.
         * @member {Array.<proto.AIHomeState.IAIHomeOption>} capabilityOptions
         * @memberof proto.AIHomeState
         * @instance
         */
        AIHomeState.prototype.capabilityOptions = $util.emptyArray;

        /**
         * AIHomeState conversationOptions.
         * @member {Array.<proto.AIHomeState.IAIHomeOption>} conversationOptions
         * @memberof proto.AIHomeState
         * @instance
         */
        AIHomeState.prototype.conversationOptions = $util.emptyArray;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIHomeState.prototype, "_lastFetchTime", {
            get: $util.oneOfGetter($oneOfFields = ["lastFetchTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AIHomeState instance using the specified properties.
         * @function create
         * @memberof proto.AIHomeState
         * @static
         * @param {proto.IAIHomeState=} [properties] Properties to set
         * @returns {proto.AIHomeState} AIHomeState instance
         */
        AIHomeState.create = function create(properties) {
            return new AIHomeState(properties);
        };

        /**
         * Encodes the specified AIHomeState message. Does not implicitly {@link proto.AIHomeState.verify|verify} messages.
         * @function encode
         * @memberof proto.AIHomeState
         * @static
         * @param {proto.IAIHomeState} message AIHomeState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIHomeState.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.lastFetchTime != null && Object.hasOwnProperty.call(message, "lastFetchTime"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.lastFetchTime);
            if (message.capabilityOptions != null && message.capabilityOptions.length)
                for (var i = 0; i < message.capabilityOptions.length; ++i)
                    $root.proto.AIHomeState.AIHomeOption.encode(message.capabilityOptions[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.conversationOptions != null && message.conversationOptions.length)
                for (var i = 0; i < message.conversationOptions.length; ++i)
                    $root.proto.AIHomeState.AIHomeOption.encode(message.conversationOptions[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AIHomeState message, length delimited. Does not implicitly {@link proto.AIHomeState.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AIHomeState
         * @static
         * @param {proto.IAIHomeState} message AIHomeState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIHomeState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AIHomeState message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AIHomeState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AIHomeState} AIHomeState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIHomeState.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.lastFetchTime = reader.int64();
                        message._lastFetchTime = "lastFetchTime";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(message.capabilityOptions && message.capabilityOptions.length))
                            message.capabilityOptions = [];
                        message.capabilityOptions.push($root.proto.AIHomeState.AIHomeOption.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        if (!(message.conversationOptions && message.conversationOptions.length))
                            message.conversationOptions = [];
                        message.conversationOptions.push($root.proto.AIHomeState.AIHomeOption.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a AIHomeState message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AIHomeState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AIHomeState} AIHomeState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIHomeState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AIHomeState message.
         * @function verify
         * @memberof proto.AIHomeState
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AIHomeState.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.lastFetchTime != null && Object.hasOwnProperty.call(message, "lastFetchTime")) {
                properties._lastFetchTime = 1;
                if (!$util.isInteger(message.lastFetchTime) && !(message.lastFetchTime && $util.isInteger(message.lastFetchTime.low) && $util.isInteger(message.lastFetchTime.high)))
                    return "lastFetchTime: integer|Long expected";
            }
            if (message.capabilityOptions != null && Object.hasOwnProperty.call(message, "capabilityOptions")) {
                if (!Array.isArray(message.capabilityOptions))
                    return "capabilityOptions: array expected";
                for (var i = 0; i < message.capabilityOptions.length; ++i) {
                    var error = $root.proto.AIHomeState.AIHomeOption.verify(message.capabilityOptions[i], q + 1);
                    if (error)
                        return "capabilityOptions." + error;
                }
            }
            if (message.conversationOptions != null && Object.hasOwnProperty.call(message, "conversationOptions")) {
                if (!Array.isArray(message.conversationOptions))
                    return "conversationOptions: array expected";
                for (var i = 0; i < message.conversationOptions.length; ++i) {
                    var error = $root.proto.AIHomeState.AIHomeOption.verify(message.conversationOptions[i], q + 1);
                    if (error)
                        return "conversationOptions." + error;
                }
            }
            return null;
        };

        /**
         * Creates a AIHomeState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AIHomeState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AIHomeState} AIHomeState
         */
        AIHomeState.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AIHomeState: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.lastFetchTime != null)
                if ($util.Long)
                    message.lastFetchTime = $util.Long.fromValue(object.lastFetchTime, false);
                else if (typeof object.lastFetchTime === "string")
                    message.lastFetchTime = parseInt(object.lastFetchTime, 10);
                else if (typeof object.lastFetchTime === "number")
                    message.lastFetchTime = object.lastFetchTime;
                else if (typeof object.lastFetchTime === "object")
                    message.lastFetchTime = new $util.LongBits(object.lastFetchTime.low >>> 0, object.lastFetchTime.high >>> 0).toNumber();
            if (object.capabilityOptions) {
                if (!Array.isArray(object.capabilityOptions))
                    throw TypeError(".proto.AIHomeState.capabilityOptions: array expected");
                message.capabilityOptions = Array(object.capabilityOptions.length);
                for (var i = 0; i < object.capabilityOptions.length; ++i) {
                    if (!$util.isObject(object.capabilityOptions[i]))
                        throw TypeError(".proto.AIHomeState.capabilityOptions: object expected");
                    message.capabilityOptions[i] = $root.proto.AIHomeState.AIHomeOption.fromObject(object.capabilityOptions[i], q + 1);
                }
            }
            if (object.conversationOptions) {
                if (!Array.isArray(object.conversationOptions))
                    throw TypeError(".proto.AIHomeState.conversationOptions: array expected");
                message.conversationOptions = Array(object.conversationOptions.length);
                for (var i = 0; i < object.conversationOptions.length; ++i) {
                    if (!$util.isObject(object.conversationOptions[i]))
                        throw TypeError(".proto.AIHomeState.conversationOptions: object expected");
                    message.conversationOptions[i] = $root.proto.AIHomeState.AIHomeOption.fromObject(object.conversationOptions[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a AIHomeState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AIHomeState
         * @static
         * @param {proto.AIHomeState} message AIHomeState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AIHomeState.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults) {
                object.capabilityOptions = [];
                object.conversationOptions = [];
            }
            if (message.lastFetchTime != null && Object.hasOwnProperty.call(message, "lastFetchTime"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastFetchTime = typeof message.lastFetchTime === "number" ? BigInt(message.lastFetchTime) : $util.Long.fromBits(message.lastFetchTime.low >>> 0, message.lastFetchTime.high >>> 0, false).toBigInt();
                else if (typeof message.lastFetchTime === "number")
                    object.lastFetchTime = options.longs === String ? String(message.lastFetchTime) : message.lastFetchTime;
                else
                    object.lastFetchTime = options.longs === String ? $util.Long.prototype.toString.call(message.lastFetchTime) : options.longs === Number ? new $util.LongBits(message.lastFetchTime.low >>> 0, message.lastFetchTime.high >>> 0).toNumber() : message.lastFetchTime;
            if (message.capabilityOptions && message.capabilityOptions.length) {
                object.capabilityOptions = Array(message.capabilityOptions.length);
                for (var j = 0; j < message.capabilityOptions.length; ++j)
                    object.capabilityOptions[j] = $root.proto.AIHomeState.AIHomeOption.toObject(message.capabilityOptions[j], options, q + 1);
            }
            if (message.conversationOptions && message.conversationOptions.length) {
                object.conversationOptions = Array(message.conversationOptions.length);
                for (var j = 0; j < message.conversationOptions.length; ++j)
                    object.conversationOptions[j] = $root.proto.AIHomeState.AIHomeOption.toObject(message.conversationOptions[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this AIHomeState to JSON.
         * @function toJSON
         * @memberof proto.AIHomeState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AIHomeState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AIHomeState
         * @function getTypeUrl
         * @memberof proto.AIHomeState
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AIHomeState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AIHomeState";
        };

        AIHomeState.AIHomeOption = (function() {

            /**
             * Properties of a AIHomeOption.
             * @memberof proto.AIHomeState
             * @interface IAIHomeOption
             * @property {proto.AIHomeState.AIHomeOption.AIHomeActionType|null} [type] AIHomeOption type
             * @property {string|null} [title] AIHomeOption title
             * @property {string|null} [promptText] AIHomeOption promptText
             * @property {string|null} [sessionId] AIHomeOption sessionId
             * @property {string|null} [imageWdsIdentifier] AIHomeOption imageWdsIdentifier
             * @property {string|null} [imageTintColor] AIHomeOption imageTintColor
             * @property {string|null} [imageBackgroundColor] AIHomeOption imageBackgroundColor
             * @property {string|null} [cardTypeId] AIHomeOption cardTypeId
             */

            /**
             * Constructs a new AIHomeOption.
             * @memberof proto.AIHomeState
             * @classdesc Represents a AIHomeOption.
             * @implements IAIHomeOption
             * @constructor
             * @param {proto.AIHomeState.IAIHomeOption=} [properties] Properties to set
             */
            function AIHomeOption(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AIHomeOption type.
             * @member {proto.AIHomeState.AIHomeOption.AIHomeActionType|null|undefined} type
             * @memberof proto.AIHomeState.AIHomeOption
             * @instance
             */
            AIHomeOption.prototype.type = null;

            /**
             * AIHomeOption title.
             * @member {string|null|undefined} title
             * @memberof proto.AIHomeState.AIHomeOption
             * @instance
             */
            AIHomeOption.prototype.title = null;

            /**
             * AIHomeOption promptText.
             * @member {string|null|undefined} promptText
             * @memberof proto.AIHomeState.AIHomeOption
             * @instance
             */
            AIHomeOption.prototype.promptText = null;

            /**
             * AIHomeOption sessionId.
             * @member {string|null|undefined} sessionId
             * @memberof proto.AIHomeState.AIHomeOption
             * @instance
             */
            AIHomeOption.prototype.sessionId = null;

            /**
             * AIHomeOption imageWdsIdentifier.
             * @member {string|null|undefined} imageWdsIdentifier
             * @memberof proto.AIHomeState.AIHomeOption
             * @instance
             */
            AIHomeOption.prototype.imageWdsIdentifier = null;

            /**
             * AIHomeOption imageTintColor.
             * @member {string|null|undefined} imageTintColor
             * @memberof proto.AIHomeState.AIHomeOption
             * @instance
             */
            AIHomeOption.prototype.imageTintColor = null;

            /**
             * AIHomeOption imageBackgroundColor.
             * @member {string|null|undefined} imageBackgroundColor
             * @memberof proto.AIHomeState.AIHomeOption
             * @instance
             */
            AIHomeOption.prototype.imageBackgroundColor = null;

            /**
             * AIHomeOption cardTypeId.
             * @member {string|null|undefined} cardTypeId
             * @memberof proto.AIHomeState.AIHomeOption
             * @instance
             */
            AIHomeOption.prototype.cardTypeId = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIHomeOption.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIHomeOption.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIHomeOption.prototype, "_promptText", {
                get: $util.oneOfGetter($oneOfFields = ["promptText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIHomeOption.prototype, "_sessionId", {
                get: $util.oneOfGetter($oneOfFields = ["sessionId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIHomeOption.prototype, "_imageWdsIdentifier", {
                get: $util.oneOfGetter($oneOfFields = ["imageWdsIdentifier"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIHomeOption.prototype, "_imageTintColor", {
                get: $util.oneOfGetter($oneOfFields = ["imageTintColor"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIHomeOption.prototype, "_imageBackgroundColor", {
                get: $util.oneOfGetter($oneOfFields = ["imageBackgroundColor"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIHomeOption.prototype, "_cardTypeId", {
                get: $util.oneOfGetter($oneOfFields = ["cardTypeId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AIHomeOption instance using the specified properties.
             * @function create
             * @memberof proto.AIHomeState.AIHomeOption
             * @static
             * @param {proto.AIHomeState.IAIHomeOption=} [properties] Properties to set
             * @returns {proto.AIHomeState.AIHomeOption} AIHomeOption instance
             */
            AIHomeOption.create = function create(properties) {
                return new AIHomeOption(properties);
            };

            /**
             * Encodes the specified AIHomeOption message. Does not implicitly {@link proto.AIHomeState.AIHomeOption.verify|verify} messages.
             * @function encode
             * @memberof proto.AIHomeState.AIHomeOption
             * @static
             * @param {proto.AIHomeState.IAIHomeOption} message AIHomeOption message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AIHomeOption.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.title);
                if (message.promptText != null && Object.hasOwnProperty.call(message, "promptText"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.promptText);
                if (message.sessionId != null && Object.hasOwnProperty.call(message, "sessionId"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.sessionId);
                if (message.imageWdsIdentifier != null && Object.hasOwnProperty.call(message, "imageWdsIdentifier"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.imageWdsIdentifier);
                if (message.imageTintColor != null && Object.hasOwnProperty.call(message, "imageTintColor"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.imageTintColor);
                if (message.imageBackgroundColor != null && Object.hasOwnProperty.call(message, "imageBackgroundColor"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.imageBackgroundColor);
                if (message.cardTypeId != null && Object.hasOwnProperty.call(message, "cardTypeId"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.cardTypeId);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AIHomeOption message, length delimited. Does not implicitly {@link proto.AIHomeState.AIHomeOption.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.AIHomeState.AIHomeOption
             * @static
             * @param {proto.AIHomeState.IAIHomeOption} message AIHomeOption message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AIHomeOption.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a AIHomeOption message from the specified reader or buffer.
             * @function decode
             * @memberof proto.AIHomeState.AIHomeOption
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.AIHomeState.AIHomeOption} AIHomeOption
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AIHomeOption.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.type = reader.int32();
                            message._type = "type";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.title = reader.stringVerify();
                            message._title = "title";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.promptText = reader.stringVerify();
                            message._promptText = "promptText";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.sessionId = reader.stringVerify();
                            message._sessionId = "sessionId";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.imageWdsIdentifier = reader.stringVerify();
                            message._imageWdsIdentifier = "imageWdsIdentifier";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.imageTintColor = reader.stringVerify();
                            message._imageTintColor = "imageTintColor";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.imageBackgroundColor = reader.stringVerify();
                            message._imageBackgroundColor = "imageBackgroundColor";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.cardTypeId = reader.stringVerify();
                            message._cardTypeId = "cardTypeId";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a AIHomeOption message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.AIHomeState.AIHomeOption
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.AIHomeState.AIHomeOption} AIHomeOption
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AIHomeOption.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a AIHomeOption message.
             * @function verify
             * @memberof proto.AIHomeState.AIHomeOption
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AIHomeOption.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                    properties._type = 1;
                    if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                        return "type: enum value expected";
                }
                if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                    properties._title = 1;
                    if (!$util.isString(message.title))
                        return "title: string expected";
                }
                if (message.promptText != null && Object.hasOwnProperty.call(message, "promptText")) {
                    properties._promptText = 1;
                    if (!$util.isString(message.promptText))
                        return "promptText: string expected";
                }
                if (message.sessionId != null && Object.hasOwnProperty.call(message, "sessionId")) {
                    properties._sessionId = 1;
                    if (!$util.isString(message.sessionId))
                        return "sessionId: string expected";
                }
                if (message.imageWdsIdentifier != null && Object.hasOwnProperty.call(message, "imageWdsIdentifier")) {
                    properties._imageWdsIdentifier = 1;
                    if (!$util.isString(message.imageWdsIdentifier))
                        return "imageWdsIdentifier: string expected";
                }
                if (message.imageTintColor != null && Object.hasOwnProperty.call(message, "imageTintColor")) {
                    properties._imageTintColor = 1;
                    if (!$util.isString(message.imageTintColor))
                        return "imageTintColor: string expected";
                }
                if (message.imageBackgroundColor != null && Object.hasOwnProperty.call(message, "imageBackgroundColor")) {
                    properties._imageBackgroundColor = 1;
                    if (!$util.isString(message.imageBackgroundColor))
                        return "imageBackgroundColor: string expected";
                }
                if (message.cardTypeId != null && Object.hasOwnProperty.call(message, "cardTypeId")) {
                    properties._cardTypeId = 1;
                    if (!$util.isString(message.cardTypeId))
                        return "cardTypeId: string expected";
                }
                return null;
            };

            /**
             * Creates a AIHomeOption message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.AIHomeState.AIHomeOption
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.AIHomeState.AIHomeOption} AIHomeOption
             */
            AIHomeOption.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.AIHomeState.AIHomeOption: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.type) {
                case "PROMPT":
                case 0:
                    message.type = 0;
                    break;
                case "CREATE_IMAGE":
                case 1:
                    message.type = 1;
                    break;
                case "ANIMATE_PHOTO":
                case 2:
                    message.type = 2;
                    break;
                case "ANALYZE_FILE":
                case 3:
                    message.type = 3;
                    break;
                case "COLLABORATE":
                case 4:
                    message.type = 4;
                    break;
                case "OPEN_GREETING_CARD":
                case 5:
                    message.type = 5;
                    break;
                default:
                    if (typeof object.type === "number" && (object.type | 0) === object.type)
                        message.type = object.type;
                }
                if (object.title != null)
                    message.title = String(object.title);
                if (object.promptText != null)
                    message.promptText = String(object.promptText);
                if (object.sessionId != null)
                    message.sessionId = String(object.sessionId);
                if (object.imageWdsIdentifier != null)
                    message.imageWdsIdentifier = String(object.imageWdsIdentifier);
                if (object.imageTintColor != null)
                    message.imageTintColor = String(object.imageTintColor);
                if (object.imageBackgroundColor != null)
                    message.imageBackgroundColor = String(object.imageBackgroundColor);
                if (object.cardTypeId != null)
                    message.cardTypeId = String(object.cardTypeId);
                return message;
            };

            /**
             * Creates a plain object from a AIHomeOption message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.AIHomeState.AIHomeOption
             * @static
             * @param {proto.AIHomeState.AIHomeOption} message AIHomeOption
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AIHomeOption.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    object.type = options.enums === String ? $root.proto.AIHomeState.AIHomeOption.AIHomeActionType[message.type] === undefined ? message.type : $root.proto.AIHomeState.AIHomeOption.AIHomeActionType[message.type] : message.type;
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    object.title = message.title;
                if (message.promptText != null && Object.hasOwnProperty.call(message, "promptText"))
                    object.promptText = message.promptText;
                if (message.sessionId != null && Object.hasOwnProperty.call(message, "sessionId"))
                    object.sessionId = message.sessionId;
                if (message.imageWdsIdentifier != null && Object.hasOwnProperty.call(message, "imageWdsIdentifier"))
                    object.imageWdsIdentifier = message.imageWdsIdentifier;
                if (message.imageTintColor != null && Object.hasOwnProperty.call(message, "imageTintColor"))
                    object.imageTintColor = message.imageTintColor;
                if (message.imageBackgroundColor != null && Object.hasOwnProperty.call(message, "imageBackgroundColor"))
                    object.imageBackgroundColor = message.imageBackgroundColor;
                if (message.cardTypeId != null && Object.hasOwnProperty.call(message, "cardTypeId"))
                    object.cardTypeId = message.cardTypeId;
                return object;
            };

            /**
             * Converts this AIHomeOption to JSON.
             * @function toJSON
             * @memberof proto.AIHomeState.AIHomeOption
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AIHomeOption.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AIHomeOption
             * @function getTypeUrl
             * @memberof proto.AIHomeState.AIHomeOption
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AIHomeOption.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.AIHomeState.AIHomeOption";
            };

            /**
             * AIHomeActionType enum.
             * @name proto.AIHomeState.AIHomeOption.AIHomeActionType
             * @enum {number}
             * @property {number} PROMPT=0 PROMPT value
             * @property {number} CREATE_IMAGE=1 CREATE_IMAGE value
             * @property {number} ANIMATE_PHOTO=2 ANIMATE_PHOTO value
             * @property {number} ANALYZE_FILE=3 ANALYZE_FILE value
             * @property {number} COLLABORATE=4 COLLABORATE value
             * @property {number} OPEN_GREETING_CARD=5 OPEN_GREETING_CARD value
             */
            AIHomeOption.AIHomeActionType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "PROMPT"] = 0;
                values[valuesById[1] = "CREATE_IMAGE"] = 1;
                values[valuesById[2] = "ANIMATE_PHOTO"] = 2;
                values[valuesById[3] = "ANALYZE_FILE"] = 3;
                values[valuesById[4] = "COLLABORATE"] = 4;
                values[valuesById[5] = "OPEN_GREETING_CARD"] = 5;
                return values;
            })();

            return AIHomeOption;
        })();

        return AIHomeState;
    })();

    proto.AIMediaCollectionMessage = (function() {

        /**
         * Properties of a AIMediaCollectionMessage.
         * @memberof proto
         * @interface IAIMediaCollectionMessage
         * @property {string|null} [collectionId] AIMediaCollectionMessage collectionId
         * @property {number|null} [expectedMediaCount] AIMediaCollectionMessage expectedMediaCount
         * @property {boolean|null} [hasGlobalCaption] AIMediaCollectionMessage hasGlobalCaption
         */

        /**
         * Constructs a new AIMediaCollectionMessage.
         * @memberof proto
         * @classdesc Represents a AIMediaCollectionMessage.
         * @implements IAIMediaCollectionMessage
         * @constructor
         * @param {proto.IAIMediaCollectionMessage=} [properties] Properties to set
         */
        function AIMediaCollectionMessage(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AIMediaCollectionMessage collectionId.
         * @member {string|null|undefined} collectionId
         * @memberof proto.AIMediaCollectionMessage
         * @instance
         */
        AIMediaCollectionMessage.prototype.collectionId = null;

        /**
         * AIMediaCollectionMessage expectedMediaCount.
         * @member {number|null|undefined} expectedMediaCount
         * @memberof proto.AIMediaCollectionMessage
         * @instance
         */
        AIMediaCollectionMessage.prototype.expectedMediaCount = null;

        /**
         * AIMediaCollectionMessage hasGlobalCaption.
         * @member {boolean|null|undefined} hasGlobalCaption
         * @memberof proto.AIMediaCollectionMessage
         * @instance
         */
        AIMediaCollectionMessage.prototype.hasGlobalCaption = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIMediaCollectionMessage.prototype, "_collectionId", {
            get: $util.oneOfGetter($oneOfFields = ["collectionId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIMediaCollectionMessage.prototype, "_expectedMediaCount", {
            get: $util.oneOfGetter($oneOfFields = ["expectedMediaCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIMediaCollectionMessage.prototype, "_hasGlobalCaption", {
            get: $util.oneOfGetter($oneOfFields = ["hasGlobalCaption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AIMediaCollectionMessage instance using the specified properties.
         * @function create
         * @memberof proto.AIMediaCollectionMessage
         * @static
         * @param {proto.IAIMediaCollectionMessage=} [properties] Properties to set
         * @returns {proto.AIMediaCollectionMessage} AIMediaCollectionMessage instance
         */
        AIMediaCollectionMessage.create = function create(properties) {
            return new AIMediaCollectionMessage(properties);
        };

        /**
         * Encodes the specified AIMediaCollectionMessage message. Does not implicitly {@link proto.AIMediaCollectionMessage.verify|verify} messages.
         * @function encode
         * @memberof proto.AIMediaCollectionMessage
         * @static
         * @param {proto.IAIMediaCollectionMessage} message AIMediaCollectionMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIMediaCollectionMessage.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.collectionId != null && Object.hasOwnProperty.call(message, "collectionId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.collectionId);
            if (message.expectedMediaCount != null && Object.hasOwnProperty.call(message, "expectedMediaCount"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.expectedMediaCount);
            if (message.hasGlobalCaption != null && Object.hasOwnProperty.call(message, "hasGlobalCaption"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.hasGlobalCaption);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AIMediaCollectionMessage message, length delimited. Does not implicitly {@link proto.AIMediaCollectionMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AIMediaCollectionMessage
         * @static
         * @param {proto.IAIMediaCollectionMessage} message AIMediaCollectionMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIMediaCollectionMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AIMediaCollectionMessage message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AIMediaCollectionMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AIMediaCollectionMessage} AIMediaCollectionMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIMediaCollectionMessage.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.collectionId = reader.stringVerify();
                        message._collectionId = "collectionId";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.expectedMediaCount = reader.uint32();
                        message._expectedMediaCount = "expectedMediaCount";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.hasGlobalCaption = reader.bool();
                        message._hasGlobalCaption = "hasGlobalCaption";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a AIMediaCollectionMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AIMediaCollectionMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AIMediaCollectionMessage} AIMediaCollectionMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIMediaCollectionMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AIMediaCollectionMessage message.
         * @function verify
         * @memberof proto.AIMediaCollectionMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AIMediaCollectionMessage.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.collectionId != null && Object.hasOwnProperty.call(message, "collectionId")) {
                properties._collectionId = 1;
                if (!$util.isString(message.collectionId))
                    return "collectionId: string expected";
            }
            if (message.expectedMediaCount != null && Object.hasOwnProperty.call(message, "expectedMediaCount")) {
                properties._expectedMediaCount = 1;
                if (!$util.isInteger(message.expectedMediaCount))
                    return "expectedMediaCount: integer expected";
            }
            if (message.hasGlobalCaption != null && Object.hasOwnProperty.call(message, "hasGlobalCaption")) {
                properties._hasGlobalCaption = 1;
                if (typeof message.hasGlobalCaption !== "boolean")
                    return "hasGlobalCaption: boolean expected";
            }
            return null;
        };

        /**
         * Creates a AIMediaCollectionMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AIMediaCollectionMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AIMediaCollectionMessage} AIMediaCollectionMessage
         */
        AIMediaCollectionMessage.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AIMediaCollectionMessage: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.collectionId != null)
                message.collectionId = String(object.collectionId);
            if (object.expectedMediaCount != null)
                message.expectedMediaCount = object.expectedMediaCount >>> 0;
            if (object.hasGlobalCaption != null)
                message.hasGlobalCaption = Boolean(object.hasGlobalCaption);
            return message;
        };

        /**
         * Creates a plain object from a AIMediaCollectionMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AIMediaCollectionMessage
         * @static
         * @param {proto.AIMediaCollectionMessage} message AIMediaCollectionMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AIMediaCollectionMessage.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.collectionId != null && Object.hasOwnProperty.call(message, "collectionId"))
                object.collectionId = message.collectionId;
            if (message.expectedMediaCount != null && Object.hasOwnProperty.call(message, "expectedMediaCount"))
                object.expectedMediaCount = message.expectedMediaCount;
            if (message.hasGlobalCaption != null && Object.hasOwnProperty.call(message, "hasGlobalCaption"))
                object.hasGlobalCaption = message.hasGlobalCaption;
            return object;
        };

        /**
         * Converts this AIMediaCollectionMessage to JSON.
         * @function toJSON
         * @memberof proto.AIMediaCollectionMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AIMediaCollectionMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AIMediaCollectionMessage
         * @function getTypeUrl
         * @memberof proto.AIMediaCollectionMessage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AIMediaCollectionMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AIMediaCollectionMessage";
        };

        return AIMediaCollectionMessage;
    })();

    proto.AIMediaCollectionMetadata = (function() {

        /**
         * Properties of a AIMediaCollectionMetadata.
         * @memberof proto
         * @interface IAIMediaCollectionMetadata
         * @property {string|null} [collectionId] AIMediaCollectionMetadata collectionId
         * @property {number|null} [uploadOrderIndex] AIMediaCollectionMetadata uploadOrderIndex
         */

        /**
         * Constructs a new AIMediaCollectionMetadata.
         * @memberof proto
         * @classdesc Represents a AIMediaCollectionMetadata.
         * @implements IAIMediaCollectionMetadata
         * @constructor
         * @param {proto.IAIMediaCollectionMetadata=} [properties] Properties to set
         */
        function AIMediaCollectionMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AIMediaCollectionMetadata collectionId.
         * @member {string|null|undefined} collectionId
         * @memberof proto.AIMediaCollectionMetadata
         * @instance
         */
        AIMediaCollectionMetadata.prototype.collectionId = null;

        /**
         * AIMediaCollectionMetadata uploadOrderIndex.
         * @member {number|null|undefined} uploadOrderIndex
         * @memberof proto.AIMediaCollectionMetadata
         * @instance
         */
        AIMediaCollectionMetadata.prototype.uploadOrderIndex = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIMediaCollectionMetadata.prototype, "_collectionId", {
            get: $util.oneOfGetter($oneOfFields = ["collectionId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIMediaCollectionMetadata.prototype, "_uploadOrderIndex", {
            get: $util.oneOfGetter($oneOfFields = ["uploadOrderIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AIMediaCollectionMetadata instance using the specified properties.
         * @function create
         * @memberof proto.AIMediaCollectionMetadata
         * @static
         * @param {proto.IAIMediaCollectionMetadata=} [properties] Properties to set
         * @returns {proto.AIMediaCollectionMetadata} AIMediaCollectionMetadata instance
         */
        AIMediaCollectionMetadata.create = function create(properties) {
            return new AIMediaCollectionMetadata(properties);
        };

        /**
         * Encodes the specified AIMediaCollectionMetadata message. Does not implicitly {@link proto.AIMediaCollectionMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.AIMediaCollectionMetadata
         * @static
         * @param {proto.IAIMediaCollectionMetadata} message AIMediaCollectionMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIMediaCollectionMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.collectionId != null && Object.hasOwnProperty.call(message, "collectionId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.collectionId);
            if (message.uploadOrderIndex != null && Object.hasOwnProperty.call(message, "uploadOrderIndex"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.uploadOrderIndex);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AIMediaCollectionMetadata message, length delimited. Does not implicitly {@link proto.AIMediaCollectionMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AIMediaCollectionMetadata
         * @static
         * @param {proto.IAIMediaCollectionMetadata} message AIMediaCollectionMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIMediaCollectionMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AIMediaCollectionMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AIMediaCollectionMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AIMediaCollectionMetadata} AIMediaCollectionMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIMediaCollectionMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.collectionId = reader.stringVerify();
                        message._collectionId = "collectionId";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.uploadOrderIndex = reader.uint32();
                        message._uploadOrderIndex = "uploadOrderIndex";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a AIMediaCollectionMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AIMediaCollectionMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AIMediaCollectionMetadata} AIMediaCollectionMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIMediaCollectionMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AIMediaCollectionMetadata message.
         * @function verify
         * @memberof proto.AIMediaCollectionMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AIMediaCollectionMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.collectionId != null && Object.hasOwnProperty.call(message, "collectionId")) {
                properties._collectionId = 1;
                if (!$util.isString(message.collectionId))
                    return "collectionId: string expected";
            }
            if (message.uploadOrderIndex != null && Object.hasOwnProperty.call(message, "uploadOrderIndex")) {
                properties._uploadOrderIndex = 1;
                if (!$util.isInteger(message.uploadOrderIndex))
                    return "uploadOrderIndex: integer expected";
            }
            return null;
        };

        /**
         * Creates a AIMediaCollectionMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AIMediaCollectionMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AIMediaCollectionMetadata} AIMediaCollectionMetadata
         */
        AIMediaCollectionMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AIMediaCollectionMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.collectionId != null)
                message.collectionId = String(object.collectionId);
            if (object.uploadOrderIndex != null)
                message.uploadOrderIndex = object.uploadOrderIndex >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a AIMediaCollectionMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AIMediaCollectionMetadata
         * @static
         * @param {proto.AIMediaCollectionMetadata} message AIMediaCollectionMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AIMediaCollectionMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.collectionId != null && Object.hasOwnProperty.call(message, "collectionId"))
                object.collectionId = message.collectionId;
            if (message.uploadOrderIndex != null && Object.hasOwnProperty.call(message, "uploadOrderIndex"))
                object.uploadOrderIndex = message.uploadOrderIndex;
            return object;
        };

        /**
         * Converts this AIMediaCollectionMetadata to JSON.
         * @function toJSON
         * @memberof proto.AIMediaCollectionMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AIMediaCollectionMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AIMediaCollectionMetadata
         * @function getTypeUrl
         * @memberof proto.AIMediaCollectionMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AIMediaCollectionMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AIMediaCollectionMetadata";
        };

        return AIMediaCollectionMetadata;
    })();

    proto.AIMetadataOperation = (function() {

        /**
         * Properties of a AIMetadataOperation.
         * @memberof proto
         * @interface IAIMetadataOperation
         * @property {proto.IHatchMetadataSync|null} [hatchMetadataSync] AIMetadataOperation hatchMetadataSync
         */

        /**
         * Constructs a new AIMetadataOperation.
         * @memberof proto
         * @classdesc Represents a AIMetadataOperation.
         * @implements IAIMetadataOperation
         * @constructor
         * @param {proto.IAIMetadataOperation=} [properties] Properties to set
         */
        function AIMetadataOperation(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AIMetadataOperation hatchMetadataSync.
         * @member {proto.IHatchMetadataSync|null|undefined} hatchMetadataSync
         * @memberof proto.AIMetadataOperation
         * @instance
         */
        AIMetadataOperation.prototype.hatchMetadataSync = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIMetadataOperation.prototype, "_hatchMetadataSync", {
            get: $util.oneOfGetter($oneOfFields = ["hatchMetadataSync"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AIMetadataOperation instance using the specified properties.
         * @function create
         * @memberof proto.AIMetadataOperation
         * @static
         * @param {proto.IAIMetadataOperation=} [properties] Properties to set
         * @returns {proto.AIMetadataOperation} AIMetadataOperation instance
         */
        AIMetadataOperation.create = function create(properties) {
            return new AIMetadataOperation(properties);
        };

        /**
         * Encodes the specified AIMetadataOperation message. Does not implicitly {@link proto.AIMetadataOperation.verify|verify} messages.
         * @function encode
         * @memberof proto.AIMetadataOperation
         * @static
         * @param {proto.IAIMetadataOperation} message AIMetadataOperation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIMetadataOperation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.hatchMetadataSync != null && Object.hasOwnProperty.call(message, "hatchMetadataSync"))
                $root.proto.HatchMetadataSync.encode(message.hatchMetadataSync, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AIMetadataOperation message, length delimited. Does not implicitly {@link proto.AIMetadataOperation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AIMetadataOperation
         * @static
         * @param {proto.IAIMetadataOperation} message AIMetadataOperation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIMetadataOperation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AIMetadataOperation message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AIMetadataOperation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AIMetadataOperation} AIMetadataOperation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIMetadataOperation.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.hatchMetadataSync = $root.proto.HatchMetadataSync.decode(reader, reader.uint32(), undefined, q + 1, message.hatchMetadataSync);
                        message._hatchMetadataSync = "hatchMetadataSync";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a AIMetadataOperation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AIMetadataOperation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AIMetadataOperation} AIMetadataOperation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIMetadataOperation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AIMetadataOperation message.
         * @function verify
         * @memberof proto.AIMetadataOperation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AIMetadataOperation.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.hatchMetadataSync != null && Object.hasOwnProperty.call(message, "hatchMetadataSync")) {
                properties._hatchMetadataSync = 1;
                {
                    var error = $root.proto.HatchMetadataSync.verify(message.hatchMetadataSync, q + 1);
                    if (error)
                        return "hatchMetadataSync." + error;
                }
            }
            return null;
        };

        /**
         * Creates a AIMetadataOperation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AIMetadataOperation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AIMetadataOperation} AIMetadataOperation
         */
        AIMetadataOperation.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AIMetadataOperation: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.hatchMetadataSync != null) {
                if (!$util.isObject(object.hatchMetadataSync))
                    throw TypeError(".proto.AIMetadataOperation.hatchMetadataSync: object expected");
                message.hatchMetadataSync = $root.proto.HatchMetadataSync.fromObject(object.hatchMetadataSync, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a AIMetadataOperation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AIMetadataOperation
         * @static
         * @param {proto.AIMetadataOperation} message AIMetadataOperation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AIMetadataOperation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.hatchMetadataSync != null && Object.hasOwnProperty.call(message, "hatchMetadataSync"))
                object.hatchMetadataSync = $root.proto.HatchMetadataSync.toObject(message.hatchMetadataSync, options, q + 1);
            return object;
        };

        /**
         * Converts this AIMetadataOperation to JSON.
         * @function toJSON
         * @memberof proto.AIMetadataOperation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AIMetadataOperation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AIMetadataOperation
         * @function getTypeUrl
         * @memberof proto.AIMetadataOperation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AIMetadataOperation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AIMetadataOperation";
        };

        return AIMetadataOperation;
    })();

    proto.AIQueryFanout = (function() {

        /**
         * Properties of a AIQueryFanout.
         * @memberof proto
         * @interface IAIQueryFanout
         * @property {proto.IMessageKey|null} [messageKey] AIQueryFanout messageKey
         * @property {proto.IMessage|null} [message] AIQueryFanout message
         * @property {number|Long|null} [timestamp] AIQueryFanout timestamp
         */

        /**
         * Constructs a new AIQueryFanout.
         * @memberof proto
         * @classdesc Represents a AIQueryFanout.
         * @implements IAIQueryFanout
         * @constructor
         * @param {proto.IAIQueryFanout=} [properties] Properties to set
         */
        function AIQueryFanout(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AIQueryFanout messageKey.
         * @member {proto.IMessageKey|null|undefined} messageKey
         * @memberof proto.AIQueryFanout
         * @instance
         */
        AIQueryFanout.prototype.messageKey = null;

        /**
         * AIQueryFanout message.
         * @member {proto.IMessage|null|undefined} message
         * @memberof proto.AIQueryFanout
         * @instance
         */
        AIQueryFanout.prototype.message = null;

        /**
         * AIQueryFanout timestamp.
         * @member {number|Long|null|undefined} timestamp
         * @memberof proto.AIQueryFanout
         * @instance
         */
        AIQueryFanout.prototype.timestamp = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIQueryFanout.prototype, "_messageKey", {
            get: $util.oneOfGetter($oneOfFields = ["messageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIQueryFanout.prototype, "_message", {
            get: $util.oneOfGetter($oneOfFields = ["message"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIQueryFanout.prototype, "_timestamp", {
            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AIQueryFanout instance using the specified properties.
         * @function create
         * @memberof proto.AIQueryFanout
         * @static
         * @param {proto.IAIQueryFanout=} [properties] Properties to set
         * @returns {proto.AIQueryFanout} AIQueryFanout instance
         */
        AIQueryFanout.create = function create(properties) {
            return new AIQueryFanout(properties);
        };

        /**
         * Encodes the specified AIQueryFanout message. Does not implicitly {@link proto.AIQueryFanout.verify|verify} messages.
         * @function encode
         * @memberof proto.AIQueryFanout
         * @static
         * @param {proto.IAIQueryFanout} message AIQueryFanout message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIQueryFanout.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.messageKey != null && Object.hasOwnProperty.call(message, "messageKey"))
                $root.proto.MessageKey.encode(message.messageKey, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                $root.proto.Message.encode(message.message, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.timestamp);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AIQueryFanout message, length delimited. Does not implicitly {@link proto.AIQueryFanout.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AIQueryFanout
         * @static
         * @param {proto.IAIQueryFanout} message AIQueryFanout message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIQueryFanout.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AIQueryFanout message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AIQueryFanout
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AIQueryFanout} AIQueryFanout
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIQueryFanout.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.messageKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.messageKey);
                        message._messageKey = "messageKey";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.message = $root.proto.Message.decode(reader, reader.uint32(), undefined, q + 1, message.message);
                        message._message = "message";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.timestamp = reader.int64();
                        message._timestamp = "timestamp";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a AIQueryFanout message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AIQueryFanout
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AIQueryFanout} AIQueryFanout
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIQueryFanout.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AIQueryFanout message.
         * @function verify
         * @memberof proto.AIQueryFanout
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AIQueryFanout.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.messageKey != null && Object.hasOwnProperty.call(message, "messageKey")) {
                properties._messageKey = 1;
                {
                    var error = $root.proto.MessageKey.verify(message.messageKey, q + 1);
                    if (error)
                        return "messageKey." + error;
                }
            }
            if (message.message != null && Object.hasOwnProperty.call(message, "message")) {
                properties._message = 1;
                {
                    var error = $root.proto.Message.verify(message.message, q + 1);
                    if (error)
                        return "message." + error;
                }
            }
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp")) {
                properties._timestamp = 1;
                if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                    return "timestamp: integer|Long expected";
            }
            return null;
        };

        /**
         * Creates a AIQueryFanout message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AIQueryFanout
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AIQueryFanout} AIQueryFanout
         */
        AIQueryFanout.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AIQueryFanout: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.messageKey != null) {
                if (!$util.isObject(object.messageKey))
                    throw TypeError(".proto.AIQueryFanout.messageKey: object expected");
                message.messageKey = $root.proto.MessageKey.fromObject(object.messageKey, q + 1);
            }
            if (object.message != null) {
                if (!$util.isObject(object.message))
                    throw TypeError(".proto.AIQueryFanout.message: object expected");
                message.message = $root.proto.Message.fromObject(object.message, q + 1);
            }
            if (object.timestamp != null)
                if ($util.Long)
                    message.timestamp = $util.Long.fromValue(object.timestamp, false);
                else if (typeof object.timestamp === "string")
                    message.timestamp = parseInt(object.timestamp, 10);
                else if (typeof object.timestamp === "number")
                    message.timestamp = object.timestamp;
                else if (typeof object.timestamp === "object")
                    message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a AIQueryFanout message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AIQueryFanout
         * @static
         * @param {proto.AIQueryFanout} message AIQueryFanout
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AIQueryFanout.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.messageKey != null && Object.hasOwnProperty.call(message, "messageKey"))
                object.messageKey = $root.proto.MessageKey.toObject(message.messageKey, options, q + 1);
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                object.message = $root.proto.Message.toObject(message.message, options, q + 1);
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.timestamp = typeof message.timestamp === "number" ? BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
                else if (typeof message.timestamp === "number")
                    object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                else
                    object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
            return object;
        };

        /**
         * Converts this AIQueryFanout to JSON.
         * @function toJSON
         * @memberof proto.AIQueryFanout
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AIQueryFanout.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AIQueryFanout
         * @function getTypeUrl
         * @memberof proto.AIQueryFanout
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AIQueryFanout.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AIQueryFanout";
        };

        return AIQueryFanout;
    })();

    proto.AIRegenerateMetadata = (function() {

        /**
         * Properties of a AIRegenerateMetadata.
         * @memberof proto
         * @interface IAIRegenerateMetadata
         * @property {proto.IMessageKey|null} [messageKey] AIRegenerateMetadata messageKey
         * @property {number|Long|null} [responseTimestampMs] AIRegenerateMetadata responseTimestampMs
         */

        /**
         * Constructs a new AIRegenerateMetadata.
         * @memberof proto
         * @classdesc Represents a AIRegenerateMetadata.
         * @implements IAIRegenerateMetadata
         * @constructor
         * @param {proto.IAIRegenerateMetadata=} [properties] Properties to set
         */
        function AIRegenerateMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AIRegenerateMetadata messageKey.
         * @member {proto.IMessageKey|null|undefined} messageKey
         * @memberof proto.AIRegenerateMetadata
         * @instance
         */
        AIRegenerateMetadata.prototype.messageKey = null;

        /**
         * AIRegenerateMetadata responseTimestampMs.
         * @member {number|Long|null|undefined} responseTimestampMs
         * @memberof proto.AIRegenerateMetadata
         * @instance
         */
        AIRegenerateMetadata.prototype.responseTimestampMs = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRegenerateMetadata.prototype, "_messageKey", {
            get: $util.oneOfGetter($oneOfFields = ["messageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRegenerateMetadata.prototype, "_responseTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["responseTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AIRegenerateMetadata instance using the specified properties.
         * @function create
         * @memberof proto.AIRegenerateMetadata
         * @static
         * @param {proto.IAIRegenerateMetadata=} [properties] Properties to set
         * @returns {proto.AIRegenerateMetadata} AIRegenerateMetadata instance
         */
        AIRegenerateMetadata.create = function create(properties) {
            return new AIRegenerateMetadata(properties);
        };

        /**
         * Encodes the specified AIRegenerateMetadata message. Does not implicitly {@link proto.AIRegenerateMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.AIRegenerateMetadata
         * @static
         * @param {proto.IAIRegenerateMetadata} message AIRegenerateMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRegenerateMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.messageKey != null && Object.hasOwnProperty.call(message, "messageKey"))
                $root.proto.MessageKey.encode(message.messageKey, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.responseTimestampMs != null && Object.hasOwnProperty.call(message, "responseTimestampMs"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.responseTimestampMs);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AIRegenerateMetadata message, length delimited. Does not implicitly {@link proto.AIRegenerateMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AIRegenerateMetadata
         * @static
         * @param {proto.IAIRegenerateMetadata} message AIRegenerateMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRegenerateMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AIRegenerateMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AIRegenerateMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AIRegenerateMetadata} AIRegenerateMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRegenerateMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.messageKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.messageKey);
                        message._messageKey = "messageKey";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.responseTimestampMs = reader.int64();
                        message._responseTimestampMs = "responseTimestampMs";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a AIRegenerateMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AIRegenerateMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AIRegenerateMetadata} AIRegenerateMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRegenerateMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AIRegenerateMetadata message.
         * @function verify
         * @memberof proto.AIRegenerateMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AIRegenerateMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.messageKey != null && Object.hasOwnProperty.call(message, "messageKey")) {
                properties._messageKey = 1;
                {
                    var error = $root.proto.MessageKey.verify(message.messageKey, q + 1);
                    if (error)
                        return "messageKey." + error;
                }
            }
            if (message.responseTimestampMs != null && Object.hasOwnProperty.call(message, "responseTimestampMs")) {
                properties._responseTimestampMs = 1;
                if (!$util.isInteger(message.responseTimestampMs) && !(message.responseTimestampMs && $util.isInteger(message.responseTimestampMs.low) && $util.isInteger(message.responseTimestampMs.high)))
                    return "responseTimestampMs: integer|Long expected";
            }
            return null;
        };

        /**
         * Creates a AIRegenerateMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AIRegenerateMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AIRegenerateMetadata} AIRegenerateMetadata
         */
        AIRegenerateMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AIRegenerateMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.messageKey != null) {
                if (!$util.isObject(object.messageKey))
                    throw TypeError(".proto.AIRegenerateMetadata.messageKey: object expected");
                message.messageKey = $root.proto.MessageKey.fromObject(object.messageKey, q + 1);
            }
            if (object.responseTimestampMs != null)
                if ($util.Long)
                    message.responseTimestampMs = $util.Long.fromValue(object.responseTimestampMs, false);
                else if (typeof object.responseTimestampMs === "string")
                    message.responseTimestampMs = parseInt(object.responseTimestampMs, 10);
                else if (typeof object.responseTimestampMs === "number")
                    message.responseTimestampMs = object.responseTimestampMs;
                else if (typeof object.responseTimestampMs === "object")
                    message.responseTimestampMs = new $util.LongBits(object.responseTimestampMs.low >>> 0, object.responseTimestampMs.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a AIRegenerateMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AIRegenerateMetadata
         * @static
         * @param {proto.AIRegenerateMetadata} message AIRegenerateMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AIRegenerateMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.messageKey != null && Object.hasOwnProperty.call(message, "messageKey"))
                object.messageKey = $root.proto.MessageKey.toObject(message.messageKey, options, q + 1);
            if (message.responseTimestampMs != null && Object.hasOwnProperty.call(message, "responseTimestampMs"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.responseTimestampMs = typeof message.responseTimestampMs === "number" ? BigInt(message.responseTimestampMs) : $util.Long.fromBits(message.responseTimestampMs.low >>> 0, message.responseTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof message.responseTimestampMs === "number")
                    object.responseTimestampMs = options.longs === String ? String(message.responseTimestampMs) : message.responseTimestampMs;
                else
                    object.responseTimestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.responseTimestampMs) : options.longs === Number ? new $util.LongBits(message.responseTimestampMs.low >>> 0, message.responseTimestampMs.high >>> 0).toNumber() : message.responseTimestampMs;
            return object;
        };

        /**
         * Converts this AIRegenerateMetadata to JSON.
         * @function toJSON
         * @memberof proto.AIRegenerateMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AIRegenerateMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AIRegenerateMetadata
         * @function getTypeUrl
         * @memberof proto.AIRegenerateMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AIRegenerateMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AIRegenerateMetadata";
        };

        return AIRegenerateMetadata;
    })();

    proto.AIRichResponseCodeMetadata = (function() {

        /**
         * Properties of a AIRichResponseCodeMetadata.
         * @memberof proto
         * @interface IAIRichResponseCodeMetadata
         * @property {string|null} [codeLanguage] AIRichResponseCodeMetadata codeLanguage
         * @property {Array.<proto.AIRichResponseCodeMetadata.IAIRichResponseCodeBlock>|null} [codeBlocks] AIRichResponseCodeMetadata codeBlocks
         */

        /**
         * Constructs a new AIRichResponseCodeMetadata.
         * @memberof proto
         * @classdesc Represents a AIRichResponseCodeMetadata.
         * @implements IAIRichResponseCodeMetadata
         * @constructor
         * @param {proto.IAIRichResponseCodeMetadata=} [properties] Properties to set
         */
        function AIRichResponseCodeMetadata(properties) {
            this.codeBlocks = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AIRichResponseCodeMetadata codeLanguage.
         * @member {string|null|undefined} codeLanguage
         * @memberof proto.AIRichResponseCodeMetadata
         * @instance
         */
        AIRichResponseCodeMetadata.prototype.codeLanguage = null;

        /**
         * AIRichResponseCodeMetadata codeBlocks.
         * @member {Array.<proto.AIRichResponseCodeMetadata.IAIRichResponseCodeBlock>} codeBlocks
         * @memberof proto.AIRichResponseCodeMetadata
         * @instance
         */
        AIRichResponseCodeMetadata.prototype.codeBlocks = $util.emptyArray;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseCodeMetadata.prototype, "_codeLanguage", {
            get: $util.oneOfGetter($oneOfFields = ["codeLanguage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AIRichResponseCodeMetadata instance using the specified properties.
         * @function create
         * @memberof proto.AIRichResponseCodeMetadata
         * @static
         * @param {proto.IAIRichResponseCodeMetadata=} [properties] Properties to set
         * @returns {proto.AIRichResponseCodeMetadata} AIRichResponseCodeMetadata instance
         */
        AIRichResponseCodeMetadata.create = function create(properties) {
            return new AIRichResponseCodeMetadata(properties);
        };

        /**
         * Encodes the specified AIRichResponseCodeMetadata message. Does not implicitly {@link proto.AIRichResponseCodeMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.AIRichResponseCodeMetadata
         * @static
         * @param {proto.IAIRichResponseCodeMetadata} message AIRichResponseCodeMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseCodeMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.codeLanguage != null && Object.hasOwnProperty.call(message, "codeLanguage"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.codeLanguage);
            if (message.codeBlocks != null && message.codeBlocks.length)
                for (var i = 0; i < message.codeBlocks.length; ++i)
                    $root.proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock.encode(message.codeBlocks[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AIRichResponseCodeMetadata message, length delimited. Does not implicitly {@link proto.AIRichResponseCodeMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AIRichResponseCodeMetadata
         * @static
         * @param {proto.IAIRichResponseCodeMetadata} message AIRichResponseCodeMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseCodeMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AIRichResponseCodeMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AIRichResponseCodeMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AIRichResponseCodeMetadata} AIRichResponseCodeMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseCodeMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.codeLanguage = reader.stringVerify();
                        message._codeLanguage = "codeLanguage";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(message.codeBlocks && message.codeBlocks.length))
                            message.codeBlocks = [];
                        message.codeBlocks.push($root.proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a AIRichResponseCodeMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AIRichResponseCodeMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AIRichResponseCodeMetadata} AIRichResponseCodeMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseCodeMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AIRichResponseCodeMetadata message.
         * @function verify
         * @memberof proto.AIRichResponseCodeMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AIRichResponseCodeMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.codeLanguage != null && Object.hasOwnProperty.call(message, "codeLanguage")) {
                properties._codeLanguage = 1;
                if (!$util.isString(message.codeLanguage))
                    return "codeLanguage: string expected";
            }
            if (message.codeBlocks != null && Object.hasOwnProperty.call(message, "codeBlocks")) {
                if (!Array.isArray(message.codeBlocks))
                    return "codeBlocks: array expected";
                for (var i = 0; i < message.codeBlocks.length; ++i) {
                    var error = $root.proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock.verify(message.codeBlocks[i], q + 1);
                    if (error)
                        return "codeBlocks." + error;
                }
            }
            return null;
        };

        /**
         * Creates a AIRichResponseCodeMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AIRichResponseCodeMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AIRichResponseCodeMetadata} AIRichResponseCodeMetadata
         */
        AIRichResponseCodeMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AIRichResponseCodeMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.codeLanguage != null)
                message.codeLanguage = String(object.codeLanguage);
            if (object.codeBlocks) {
                if (!Array.isArray(object.codeBlocks))
                    throw TypeError(".proto.AIRichResponseCodeMetadata.codeBlocks: array expected");
                message.codeBlocks = Array(object.codeBlocks.length);
                for (var i = 0; i < object.codeBlocks.length; ++i) {
                    if (!$util.isObject(object.codeBlocks[i]))
                        throw TypeError(".proto.AIRichResponseCodeMetadata.codeBlocks: object expected");
                    message.codeBlocks[i] = $root.proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock.fromObject(object.codeBlocks[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a AIRichResponseCodeMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AIRichResponseCodeMetadata
         * @static
         * @param {proto.AIRichResponseCodeMetadata} message AIRichResponseCodeMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AIRichResponseCodeMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.codeBlocks = [];
            if (message.codeLanguage != null && Object.hasOwnProperty.call(message, "codeLanguage"))
                object.codeLanguage = message.codeLanguage;
            if (message.codeBlocks && message.codeBlocks.length) {
                object.codeBlocks = Array(message.codeBlocks.length);
                for (var j = 0; j < message.codeBlocks.length; ++j)
                    object.codeBlocks[j] = $root.proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock.toObject(message.codeBlocks[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this AIRichResponseCodeMetadata to JSON.
         * @function toJSON
         * @memberof proto.AIRichResponseCodeMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AIRichResponseCodeMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AIRichResponseCodeMetadata
         * @function getTypeUrl
         * @memberof proto.AIRichResponseCodeMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AIRichResponseCodeMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AIRichResponseCodeMetadata";
        };

        AIRichResponseCodeMetadata.AIRichResponseCodeBlock = (function() {

            /**
             * Properties of a AIRichResponseCodeBlock.
             * @memberof proto.AIRichResponseCodeMetadata
             * @interface IAIRichResponseCodeBlock
             * @property {proto.AIRichResponseCodeMetadata.AIRichResponseCodeHighlightType|null} [highlightType] AIRichResponseCodeBlock highlightType
             * @property {string|null} [codeContent] AIRichResponseCodeBlock codeContent
             */

            /**
             * Constructs a new AIRichResponseCodeBlock.
             * @memberof proto.AIRichResponseCodeMetadata
             * @classdesc Represents a AIRichResponseCodeBlock.
             * @implements IAIRichResponseCodeBlock
             * @constructor
             * @param {proto.AIRichResponseCodeMetadata.IAIRichResponseCodeBlock=} [properties] Properties to set
             */
            function AIRichResponseCodeBlock(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AIRichResponseCodeBlock highlightType.
             * @member {proto.AIRichResponseCodeMetadata.AIRichResponseCodeHighlightType|null|undefined} highlightType
             * @memberof proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock
             * @instance
             */
            AIRichResponseCodeBlock.prototype.highlightType = null;

            /**
             * AIRichResponseCodeBlock codeContent.
             * @member {string|null|undefined} codeContent
             * @memberof proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock
             * @instance
             */
            AIRichResponseCodeBlock.prototype.codeContent = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseCodeBlock.prototype, "_highlightType", {
                get: $util.oneOfGetter($oneOfFields = ["highlightType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseCodeBlock.prototype, "_codeContent", {
                get: $util.oneOfGetter($oneOfFields = ["codeContent"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AIRichResponseCodeBlock instance using the specified properties.
             * @function create
             * @memberof proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock
             * @static
             * @param {proto.AIRichResponseCodeMetadata.IAIRichResponseCodeBlock=} [properties] Properties to set
             * @returns {proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock} AIRichResponseCodeBlock instance
             */
            AIRichResponseCodeBlock.create = function create(properties) {
                return new AIRichResponseCodeBlock(properties);
            };

            /**
             * Encodes the specified AIRichResponseCodeBlock message. Does not implicitly {@link proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock.verify|verify} messages.
             * @function encode
             * @memberof proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock
             * @static
             * @param {proto.AIRichResponseCodeMetadata.IAIRichResponseCodeBlock} message AIRichResponseCodeBlock message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AIRichResponseCodeBlock.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.highlightType != null && Object.hasOwnProperty.call(message, "highlightType"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.highlightType);
                if (message.codeContent != null && Object.hasOwnProperty.call(message, "codeContent"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.codeContent);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AIRichResponseCodeBlock message, length delimited. Does not implicitly {@link proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock
             * @static
             * @param {proto.AIRichResponseCodeMetadata.IAIRichResponseCodeBlock} message AIRichResponseCodeBlock message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AIRichResponseCodeBlock.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a AIRichResponseCodeBlock message from the specified reader or buffer.
             * @function decode
             * @memberof proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock} AIRichResponseCodeBlock
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AIRichResponseCodeBlock.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.highlightType = reader.int32();
                            message._highlightType = "highlightType";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.codeContent = reader.stringVerify();
                            message._codeContent = "codeContent";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a AIRichResponseCodeBlock message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock} AIRichResponseCodeBlock
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AIRichResponseCodeBlock.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a AIRichResponseCodeBlock message.
             * @function verify
             * @memberof proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AIRichResponseCodeBlock.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.highlightType != null && Object.hasOwnProperty.call(message, "highlightType")) {
                    properties._highlightType = 1;
                    if (typeof message.highlightType !== "number" || (message.highlightType | 0) !== message.highlightType)
                        return "highlightType: enum value expected";
                }
                if (message.codeContent != null && Object.hasOwnProperty.call(message, "codeContent")) {
                    properties._codeContent = 1;
                    if (!$util.isString(message.codeContent))
                        return "codeContent: string expected";
                }
                return null;
            };

            /**
             * Creates a AIRichResponseCodeBlock message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock} AIRichResponseCodeBlock
             */
            AIRichResponseCodeBlock.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.highlightType) {
                case "AI_RICH_RESPONSE_CODE_HIGHLIGHT_DEFAULT":
                case 0:
                    message.highlightType = 0;
                    break;
                case "AI_RICH_RESPONSE_CODE_HIGHLIGHT_KEYWORD":
                case 1:
                    message.highlightType = 1;
                    break;
                case "AI_RICH_RESPONSE_CODE_HIGHLIGHT_METHOD":
                case 2:
                    message.highlightType = 2;
                    break;
                case "AI_RICH_RESPONSE_CODE_HIGHLIGHT_STRING":
                case 3:
                    message.highlightType = 3;
                    break;
                case "AI_RICH_RESPONSE_CODE_HIGHLIGHT_NUMBER":
                case 4:
                    message.highlightType = 4;
                    break;
                case "AI_RICH_RESPONSE_CODE_HIGHLIGHT_COMMENT":
                case 5:
                    message.highlightType = 5;
                    break;
                default:
                    if (typeof object.highlightType === "number" && (object.highlightType | 0) === object.highlightType)
                        message.highlightType = object.highlightType;
                }
                if (object.codeContent != null)
                    message.codeContent = String(object.codeContent);
                return message;
            };

            /**
             * Creates a plain object from a AIRichResponseCodeBlock message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock
             * @static
             * @param {proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock} message AIRichResponseCodeBlock
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AIRichResponseCodeBlock.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.highlightType != null && Object.hasOwnProperty.call(message, "highlightType"))
                    object.highlightType = options.enums === String ? $root.proto.AIRichResponseCodeMetadata.AIRichResponseCodeHighlightType[message.highlightType] === undefined ? message.highlightType : $root.proto.AIRichResponseCodeMetadata.AIRichResponseCodeHighlightType[message.highlightType] : message.highlightType;
                if (message.codeContent != null && Object.hasOwnProperty.call(message, "codeContent"))
                    object.codeContent = message.codeContent;
                return object;
            };

            /**
             * Converts this AIRichResponseCodeBlock to JSON.
             * @function toJSON
             * @memberof proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AIRichResponseCodeBlock.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AIRichResponseCodeBlock
             * @function getTypeUrl
             * @memberof proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AIRichResponseCodeBlock.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock";
            };

            return AIRichResponseCodeBlock;
        })();

        /**
         * AIRichResponseCodeHighlightType enum.
         * @name proto.AIRichResponseCodeMetadata.AIRichResponseCodeHighlightType
         * @enum {number}
         * @property {number} AI_RICH_RESPONSE_CODE_HIGHLIGHT_DEFAULT=0 AI_RICH_RESPONSE_CODE_HIGHLIGHT_DEFAULT value
         * @property {number} AI_RICH_RESPONSE_CODE_HIGHLIGHT_KEYWORD=1 AI_RICH_RESPONSE_CODE_HIGHLIGHT_KEYWORD value
         * @property {number} AI_RICH_RESPONSE_CODE_HIGHLIGHT_METHOD=2 AI_RICH_RESPONSE_CODE_HIGHLIGHT_METHOD value
         * @property {number} AI_RICH_RESPONSE_CODE_HIGHLIGHT_STRING=3 AI_RICH_RESPONSE_CODE_HIGHLIGHT_STRING value
         * @property {number} AI_RICH_RESPONSE_CODE_HIGHLIGHT_NUMBER=4 AI_RICH_RESPONSE_CODE_HIGHLIGHT_NUMBER value
         * @property {number} AI_RICH_RESPONSE_CODE_HIGHLIGHT_COMMENT=5 AI_RICH_RESPONSE_CODE_HIGHLIGHT_COMMENT value
         */
        AIRichResponseCodeMetadata.AIRichResponseCodeHighlightType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "AI_RICH_RESPONSE_CODE_HIGHLIGHT_DEFAULT"] = 0;
            values[valuesById[1] = "AI_RICH_RESPONSE_CODE_HIGHLIGHT_KEYWORD"] = 1;
            values[valuesById[2] = "AI_RICH_RESPONSE_CODE_HIGHLIGHT_METHOD"] = 2;
            values[valuesById[3] = "AI_RICH_RESPONSE_CODE_HIGHLIGHT_STRING"] = 3;
            values[valuesById[4] = "AI_RICH_RESPONSE_CODE_HIGHLIGHT_NUMBER"] = 4;
            values[valuesById[5] = "AI_RICH_RESPONSE_CODE_HIGHLIGHT_COMMENT"] = 5;
            return values;
        })();

        return AIRichResponseCodeMetadata;
    })();

    proto.AIRichResponseContentItemsMetadata = (function() {

        /**
         * Properties of a AIRichResponseContentItemsMetadata.
         * @memberof proto
         * @interface IAIRichResponseContentItemsMetadata
         * @property {Array.<proto.AIRichResponseContentItemsMetadata.IAIRichResponseContentItemMetadata>|null} [itemsMetadata] AIRichResponseContentItemsMetadata itemsMetadata
         * @property {proto.AIRichResponseContentItemsMetadata.ContentType|null} [contentType] AIRichResponseContentItemsMetadata contentType
         */

        /**
         * Constructs a new AIRichResponseContentItemsMetadata.
         * @memberof proto
         * @classdesc Represents a AIRichResponseContentItemsMetadata.
         * @implements IAIRichResponseContentItemsMetadata
         * @constructor
         * @param {proto.IAIRichResponseContentItemsMetadata=} [properties] Properties to set
         */
        function AIRichResponseContentItemsMetadata(properties) {
            this.itemsMetadata = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AIRichResponseContentItemsMetadata itemsMetadata.
         * @member {Array.<proto.AIRichResponseContentItemsMetadata.IAIRichResponseContentItemMetadata>} itemsMetadata
         * @memberof proto.AIRichResponseContentItemsMetadata
         * @instance
         */
        AIRichResponseContentItemsMetadata.prototype.itemsMetadata = $util.emptyArray;

        /**
         * AIRichResponseContentItemsMetadata contentType.
         * @member {proto.AIRichResponseContentItemsMetadata.ContentType|null|undefined} contentType
         * @memberof proto.AIRichResponseContentItemsMetadata
         * @instance
         */
        AIRichResponseContentItemsMetadata.prototype.contentType = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseContentItemsMetadata.prototype, "_contentType", {
            get: $util.oneOfGetter($oneOfFields = ["contentType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AIRichResponseContentItemsMetadata instance using the specified properties.
         * @function create
         * @memberof proto.AIRichResponseContentItemsMetadata
         * @static
         * @param {proto.IAIRichResponseContentItemsMetadata=} [properties] Properties to set
         * @returns {proto.AIRichResponseContentItemsMetadata} AIRichResponseContentItemsMetadata instance
         */
        AIRichResponseContentItemsMetadata.create = function create(properties) {
            return new AIRichResponseContentItemsMetadata(properties);
        };

        /**
         * Encodes the specified AIRichResponseContentItemsMetadata message. Does not implicitly {@link proto.AIRichResponseContentItemsMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.AIRichResponseContentItemsMetadata
         * @static
         * @param {proto.IAIRichResponseContentItemsMetadata} message AIRichResponseContentItemsMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseContentItemsMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.itemsMetadata != null && message.itemsMetadata.length)
                for (var i = 0; i < message.itemsMetadata.length; ++i)
                    $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata.encode(message.itemsMetadata[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.contentType != null && Object.hasOwnProperty.call(message, "contentType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.contentType);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AIRichResponseContentItemsMetadata message, length delimited. Does not implicitly {@link proto.AIRichResponseContentItemsMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AIRichResponseContentItemsMetadata
         * @static
         * @param {proto.IAIRichResponseContentItemsMetadata} message AIRichResponseContentItemsMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseContentItemsMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AIRichResponseContentItemsMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AIRichResponseContentItemsMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AIRichResponseContentItemsMetadata} AIRichResponseContentItemsMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseContentItemsMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(message.itemsMetadata && message.itemsMetadata.length))
                            message.itemsMetadata = [];
                        message.itemsMetadata.push($root.proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.contentType = reader.int32();
                        message._contentType = "contentType";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a AIRichResponseContentItemsMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AIRichResponseContentItemsMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AIRichResponseContentItemsMetadata} AIRichResponseContentItemsMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseContentItemsMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AIRichResponseContentItemsMetadata message.
         * @function verify
         * @memberof proto.AIRichResponseContentItemsMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AIRichResponseContentItemsMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.itemsMetadata != null && Object.hasOwnProperty.call(message, "itemsMetadata")) {
                if (!Array.isArray(message.itemsMetadata))
                    return "itemsMetadata: array expected";
                for (var i = 0; i < message.itemsMetadata.length; ++i) {
                    var error = $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata.verify(message.itemsMetadata[i], q + 1);
                    if (error)
                        return "itemsMetadata." + error;
                }
            }
            if (message.contentType != null && Object.hasOwnProperty.call(message, "contentType")) {
                properties._contentType = 1;
                if (typeof message.contentType !== "number" || (message.contentType | 0) !== message.contentType)
                    return "contentType: enum value expected";
            }
            return null;
        };

        /**
         * Creates a AIRichResponseContentItemsMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AIRichResponseContentItemsMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AIRichResponseContentItemsMetadata} AIRichResponseContentItemsMetadata
         */
        AIRichResponseContentItemsMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AIRichResponseContentItemsMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.itemsMetadata) {
                if (!Array.isArray(object.itemsMetadata))
                    throw TypeError(".proto.AIRichResponseContentItemsMetadata.itemsMetadata: array expected");
                message.itemsMetadata = Array(object.itemsMetadata.length);
                for (var i = 0; i < object.itemsMetadata.length; ++i) {
                    if (!$util.isObject(object.itemsMetadata[i]))
                        throw TypeError(".proto.AIRichResponseContentItemsMetadata.itemsMetadata: object expected");
                    message.itemsMetadata[i] = $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata.fromObject(object.itemsMetadata[i], q + 1);
                }
            }
            switch (object.contentType) {
            case "DEFAULT":
            case 0:
                message.contentType = 0;
                break;
            case "CAROUSEL":
            case 1:
                message.contentType = 1;
                break;
            default:
                if (typeof object.contentType === "number" && (object.contentType | 0) === object.contentType)
                    message.contentType = object.contentType;
            }
            return message;
        };

        /**
         * Creates a plain object from a AIRichResponseContentItemsMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AIRichResponseContentItemsMetadata
         * @static
         * @param {proto.AIRichResponseContentItemsMetadata} message AIRichResponseContentItemsMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AIRichResponseContentItemsMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.itemsMetadata = [];
            if (message.itemsMetadata && message.itemsMetadata.length) {
                object.itemsMetadata = Array(message.itemsMetadata.length);
                for (var j = 0; j < message.itemsMetadata.length; ++j)
                    object.itemsMetadata[j] = $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata.toObject(message.itemsMetadata[j], options, q + 1);
            }
            if (message.contentType != null && Object.hasOwnProperty.call(message, "contentType"))
                object.contentType = options.enums === String ? $root.proto.AIRichResponseContentItemsMetadata.ContentType[message.contentType] === undefined ? message.contentType : $root.proto.AIRichResponseContentItemsMetadata.ContentType[message.contentType] : message.contentType;
            return object;
        };

        /**
         * Converts this AIRichResponseContentItemsMetadata to JSON.
         * @function toJSON
         * @memberof proto.AIRichResponseContentItemsMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AIRichResponseContentItemsMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AIRichResponseContentItemsMetadata
         * @function getTypeUrl
         * @memberof proto.AIRichResponseContentItemsMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AIRichResponseContentItemsMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AIRichResponseContentItemsMetadata";
        };

        AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata = (function() {

            /**
             * Properties of a AIRichResponseContentItemMetadata.
             * @memberof proto.AIRichResponseContentItemsMetadata
             * @interface IAIRichResponseContentItemMetadata
             * @property {proto.AIRichResponseContentItemsMetadata.IAIRichResponseReelItem|null} [reelItem] AIRichResponseContentItemMetadata reelItem
             */

            /**
             * Constructs a new AIRichResponseContentItemMetadata.
             * @memberof proto.AIRichResponseContentItemsMetadata
             * @classdesc Represents a AIRichResponseContentItemMetadata.
             * @implements IAIRichResponseContentItemMetadata
             * @constructor
             * @param {proto.AIRichResponseContentItemsMetadata.IAIRichResponseContentItemMetadata=} [properties] Properties to set
             */
            function AIRichResponseContentItemMetadata(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AIRichResponseContentItemMetadata reelItem.
             * @member {proto.AIRichResponseContentItemsMetadata.IAIRichResponseReelItem|null|undefined} reelItem
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata
             * @instance
             */
            AIRichResponseContentItemMetadata.prototype.reelItem = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            /**
             * AIRichResponseContentItemMetadata aIRichResponseContentItem.
             * @member {"reelItem"|undefined} aIRichResponseContentItem
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata
             * @instance
             */
            Object.defineProperty(AIRichResponseContentItemMetadata.prototype, "aIRichResponseContentItem", {
                get: $util.oneOfGetter($oneOfFields = ["reelItem"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AIRichResponseContentItemMetadata instance using the specified properties.
             * @function create
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata
             * @static
             * @param {proto.AIRichResponseContentItemsMetadata.IAIRichResponseContentItemMetadata=} [properties] Properties to set
             * @returns {proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata} AIRichResponseContentItemMetadata instance
             */
            AIRichResponseContentItemMetadata.create = function create(properties) {
                return new AIRichResponseContentItemMetadata(properties);
            };

            /**
             * Encodes the specified AIRichResponseContentItemMetadata message. Does not implicitly {@link proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata.verify|verify} messages.
             * @function encode
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata
             * @static
             * @param {proto.AIRichResponseContentItemsMetadata.IAIRichResponseContentItemMetadata} message AIRichResponseContentItemMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AIRichResponseContentItemMetadata.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.reelItem != null && Object.hasOwnProperty.call(message, "reelItem"))
                    $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem.encode(message.reelItem, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AIRichResponseContentItemMetadata message, length delimited. Does not implicitly {@link proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata
             * @static
             * @param {proto.AIRichResponseContentItemsMetadata.IAIRichResponseContentItemMetadata} message AIRichResponseContentItemMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AIRichResponseContentItemMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a AIRichResponseContentItemMetadata message from the specified reader or buffer.
             * @function decode
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata} AIRichResponseContentItemMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AIRichResponseContentItemMetadata.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.reelItem = $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem.decode(reader, reader.uint32(), undefined, q + 1, message.reelItem);
                            message.aIRichResponseContentItem = "reelItem";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a AIRichResponseContentItemMetadata message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata} AIRichResponseContentItemMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AIRichResponseContentItemMetadata.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a AIRichResponseContentItemMetadata message.
             * @function verify
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AIRichResponseContentItemMetadata.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.reelItem != null && Object.hasOwnProperty.call(message, "reelItem")) {
                    properties.aIRichResponseContentItem = 1;
                    {
                        var error = $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem.verify(message.reelItem, q + 1);
                        if (error)
                            return "reelItem." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a AIRichResponseContentItemMetadata message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata} AIRichResponseContentItemMetadata
             */
            AIRichResponseContentItemMetadata.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.reelItem != null) {
                    if (!$util.isObject(object.reelItem))
                        throw TypeError(".proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata.reelItem: object expected");
                    message.reelItem = $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem.fromObject(object.reelItem, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a AIRichResponseContentItemMetadata message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata
             * @static
             * @param {proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata} message AIRichResponseContentItemMetadata
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AIRichResponseContentItemMetadata.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.reelItem != null && Object.hasOwnProperty.call(message, "reelItem")) {
                    object.reelItem = $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem.toObject(message.reelItem, options, q + 1);
                    if (options.oneofs)
                        object.aIRichResponseContentItem = "reelItem";
                }
                return object;
            };

            /**
             * Converts this AIRichResponseContentItemMetadata to JSON.
             * @function toJSON
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AIRichResponseContentItemMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AIRichResponseContentItemMetadata
             * @function getTypeUrl
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AIRichResponseContentItemMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata";
            };

            return AIRichResponseContentItemMetadata;
        })();

        AIRichResponseContentItemsMetadata.AIRichResponseReelItem = (function() {

            /**
             * Properties of a AIRichResponseReelItem.
             * @memberof proto.AIRichResponseContentItemsMetadata
             * @interface IAIRichResponseReelItem
             * @property {string|null} [title] AIRichResponseReelItem title
             * @property {string|null} [profileIconUrl] AIRichResponseReelItem profileIconUrl
             * @property {string|null} [thumbnailUrl] AIRichResponseReelItem thumbnailUrl
             * @property {string|null} [videoUrl] AIRichResponseReelItem videoUrl
             */

            /**
             * Constructs a new AIRichResponseReelItem.
             * @memberof proto.AIRichResponseContentItemsMetadata
             * @classdesc Represents a AIRichResponseReelItem.
             * @implements IAIRichResponseReelItem
             * @constructor
             * @param {proto.AIRichResponseContentItemsMetadata.IAIRichResponseReelItem=} [properties] Properties to set
             */
            function AIRichResponseReelItem(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AIRichResponseReelItem title.
             * @member {string|null|undefined} title
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem
             * @instance
             */
            AIRichResponseReelItem.prototype.title = null;

            /**
             * AIRichResponseReelItem profileIconUrl.
             * @member {string|null|undefined} profileIconUrl
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem
             * @instance
             */
            AIRichResponseReelItem.prototype.profileIconUrl = null;

            /**
             * AIRichResponseReelItem thumbnailUrl.
             * @member {string|null|undefined} thumbnailUrl
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem
             * @instance
             */
            AIRichResponseReelItem.prototype.thumbnailUrl = null;

            /**
             * AIRichResponseReelItem videoUrl.
             * @member {string|null|undefined} videoUrl
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem
             * @instance
             */
            AIRichResponseReelItem.prototype.videoUrl = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseReelItem.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseReelItem.prototype, "_profileIconUrl", {
                get: $util.oneOfGetter($oneOfFields = ["profileIconUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseReelItem.prototype, "_thumbnailUrl", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseReelItem.prototype, "_videoUrl", {
                get: $util.oneOfGetter($oneOfFields = ["videoUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AIRichResponseReelItem instance using the specified properties.
             * @function create
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem
             * @static
             * @param {proto.AIRichResponseContentItemsMetadata.IAIRichResponseReelItem=} [properties] Properties to set
             * @returns {proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem} AIRichResponseReelItem instance
             */
            AIRichResponseReelItem.create = function create(properties) {
                return new AIRichResponseReelItem(properties);
            };

            /**
             * Encodes the specified AIRichResponseReelItem message. Does not implicitly {@link proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem.verify|verify} messages.
             * @function encode
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem
             * @static
             * @param {proto.AIRichResponseContentItemsMetadata.IAIRichResponseReelItem} message AIRichResponseReelItem message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AIRichResponseReelItem.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.title);
                if (message.profileIconUrl != null && Object.hasOwnProperty.call(message, "profileIconUrl"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.profileIconUrl);
                if (message.thumbnailUrl != null && Object.hasOwnProperty.call(message, "thumbnailUrl"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.thumbnailUrl);
                if (message.videoUrl != null && Object.hasOwnProperty.call(message, "videoUrl"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.videoUrl);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AIRichResponseReelItem message, length delimited. Does not implicitly {@link proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem
             * @static
             * @param {proto.AIRichResponseContentItemsMetadata.IAIRichResponseReelItem} message AIRichResponseReelItem message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AIRichResponseReelItem.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a AIRichResponseReelItem message from the specified reader or buffer.
             * @function decode
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem} AIRichResponseReelItem
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AIRichResponseReelItem.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.title = reader.stringVerify();
                            message._title = "title";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.profileIconUrl = reader.stringVerify();
                            message._profileIconUrl = "profileIconUrl";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.thumbnailUrl = reader.stringVerify();
                            message._thumbnailUrl = "thumbnailUrl";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.videoUrl = reader.stringVerify();
                            message._videoUrl = "videoUrl";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a AIRichResponseReelItem message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem} AIRichResponseReelItem
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AIRichResponseReelItem.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a AIRichResponseReelItem message.
             * @function verify
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AIRichResponseReelItem.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                    properties._title = 1;
                    if (!$util.isString(message.title))
                        return "title: string expected";
                }
                if (message.profileIconUrl != null && Object.hasOwnProperty.call(message, "profileIconUrl")) {
                    properties._profileIconUrl = 1;
                    if (!$util.isString(message.profileIconUrl))
                        return "profileIconUrl: string expected";
                }
                if (message.thumbnailUrl != null && Object.hasOwnProperty.call(message, "thumbnailUrl")) {
                    properties._thumbnailUrl = 1;
                    if (!$util.isString(message.thumbnailUrl))
                        return "thumbnailUrl: string expected";
                }
                if (message.videoUrl != null && Object.hasOwnProperty.call(message, "videoUrl")) {
                    properties._videoUrl = 1;
                    if (!$util.isString(message.videoUrl))
                        return "videoUrl: string expected";
                }
                return null;
            };

            /**
             * Creates a AIRichResponseReelItem message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem} AIRichResponseReelItem
             */
            AIRichResponseReelItem.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.title != null)
                    message.title = String(object.title);
                if (object.profileIconUrl != null)
                    message.profileIconUrl = String(object.profileIconUrl);
                if (object.thumbnailUrl != null)
                    message.thumbnailUrl = String(object.thumbnailUrl);
                if (object.videoUrl != null)
                    message.videoUrl = String(object.videoUrl);
                return message;
            };

            /**
             * Creates a plain object from a AIRichResponseReelItem message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem
             * @static
             * @param {proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem} message AIRichResponseReelItem
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AIRichResponseReelItem.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    object.title = message.title;
                if (message.profileIconUrl != null && Object.hasOwnProperty.call(message, "profileIconUrl"))
                    object.profileIconUrl = message.profileIconUrl;
                if (message.thumbnailUrl != null && Object.hasOwnProperty.call(message, "thumbnailUrl"))
                    object.thumbnailUrl = message.thumbnailUrl;
                if (message.videoUrl != null && Object.hasOwnProperty.call(message, "videoUrl"))
                    object.videoUrl = message.videoUrl;
                return object;
            };

            /**
             * Converts this AIRichResponseReelItem to JSON.
             * @function toJSON
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AIRichResponseReelItem.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AIRichResponseReelItem
             * @function getTypeUrl
             * @memberof proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AIRichResponseReelItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem";
            };

            return AIRichResponseReelItem;
        })();

        /**
         * ContentType enum.
         * @name proto.AIRichResponseContentItemsMetadata.ContentType
         * @enum {number}
         * @property {number} DEFAULT=0 DEFAULT value
         * @property {number} CAROUSEL=1 CAROUSEL value
         */
        AIRichResponseContentItemsMetadata.ContentType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "DEFAULT"] = 0;
            values[valuesById[1] = "CAROUSEL"] = 1;
            return values;
        })();

        return AIRichResponseContentItemsMetadata;
    })();

    proto.AIRichResponseDynamicMetadata = (function() {

        /**
         * Properties of a AIRichResponseDynamicMetadata.
         * @memberof proto
         * @interface IAIRichResponseDynamicMetadata
         * @property {proto.AIRichResponseDynamicMetadata.AIRichResponseDynamicMetadataType|null} [type] AIRichResponseDynamicMetadata type
         * @property {number|Long|null} [version] AIRichResponseDynamicMetadata version
         * @property {string|null} [url] AIRichResponseDynamicMetadata url
         * @property {number|null} [loopCount] AIRichResponseDynamicMetadata loopCount
         */

        /**
         * Constructs a new AIRichResponseDynamicMetadata.
         * @memberof proto
         * @classdesc Represents a AIRichResponseDynamicMetadata.
         * @implements IAIRichResponseDynamicMetadata
         * @constructor
         * @param {proto.IAIRichResponseDynamicMetadata=} [properties] Properties to set
         */
        function AIRichResponseDynamicMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AIRichResponseDynamicMetadata type.
         * @member {proto.AIRichResponseDynamicMetadata.AIRichResponseDynamicMetadataType|null|undefined} type
         * @memberof proto.AIRichResponseDynamicMetadata
         * @instance
         */
        AIRichResponseDynamicMetadata.prototype.type = null;

        /**
         * AIRichResponseDynamicMetadata version.
         * @member {number|Long|null|undefined} version
         * @memberof proto.AIRichResponseDynamicMetadata
         * @instance
         */
        AIRichResponseDynamicMetadata.prototype.version = null;

        /**
         * AIRichResponseDynamicMetadata url.
         * @member {string|null|undefined} url
         * @memberof proto.AIRichResponseDynamicMetadata
         * @instance
         */
        AIRichResponseDynamicMetadata.prototype.url = null;

        /**
         * AIRichResponseDynamicMetadata loopCount.
         * @member {number|null|undefined} loopCount
         * @memberof proto.AIRichResponseDynamicMetadata
         * @instance
         */
        AIRichResponseDynamicMetadata.prototype.loopCount = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseDynamicMetadata.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseDynamicMetadata.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseDynamicMetadata.prototype, "_url", {
            get: $util.oneOfGetter($oneOfFields = ["url"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseDynamicMetadata.prototype, "_loopCount", {
            get: $util.oneOfGetter($oneOfFields = ["loopCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AIRichResponseDynamicMetadata instance using the specified properties.
         * @function create
         * @memberof proto.AIRichResponseDynamicMetadata
         * @static
         * @param {proto.IAIRichResponseDynamicMetadata=} [properties] Properties to set
         * @returns {proto.AIRichResponseDynamicMetadata} AIRichResponseDynamicMetadata instance
         */
        AIRichResponseDynamicMetadata.create = function create(properties) {
            return new AIRichResponseDynamicMetadata(properties);
        };

        /**
         * Encodes the specified AIRichResponseDynamicMetadata message. Does not implicitly {@link proto.AIRichResponseDynamicMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.AIRichResponseDynamicMetadata
         * @static
         * @param {proto.IAIRichResponseDynamicMetadata} message AIRichResponseDynamicMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseDynamicMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.version);
            if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.url);
            if (message.loopCount != null && Object.hasOwnProperty.call(message, "loopCount"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.loopCount);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AIRichResponseDynamicMetadata message, length delimited. Does not implicitly {@link proto.AIRichResponseDynamicMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AIRichResponseDynamicMetadata
         * @static
         * @param {proto.IAIRichResponseDynamicMetadata} message AIRichResponseDynamicMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseDynamicMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AIRichResponseDynamicMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AIRichResponseDynamicMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AIRichResponseDynamicMetadata} AIRichResponseDynamicMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseDynamicMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.type = reader.int32();
                        message._type = "type";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.version = reader.uint64();
                        message._version = "version";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.url = reader.stringVerify();
                        message._url = "url";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.loopCount = reader.uint32();
                        message._loopCount = "loopCount";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a AIRichResponseDynamicMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AIRichResponseDynamicMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AIRichResponseDynamicMetadata} AIRichResponseDynamicMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseDynamicMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AIRichResponseDynamicMetadata message.
         * @function verify
         * @memberof proto.AIRichResponseDynamicMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AIRichResponseDynamicMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                properties._type = 1;
                if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                    return "type: enum value expected";
            }
            if (message.version != null && Object.hasOwnProperty.call(message, "version")) {
                properties._version = 1;
                if (!$util.isInteger(message.version) && !(message.version && $util.isInteger(message.version.low) && $util.isInteger(message.version.high)))
                    return "version: integer|Long expected";
            }
            if (message.url != null && Object.hasOwnProperty.call(message, "url")) {
                properties._url = 1;
                if (!$util.isString(message.url))
                    return "url: string expected";
            }
            if (message.loopCount != null && Object.hasOwnProperty.call(message, "loopCount")) {
                properties._loopCount = 1;
                if (!$util.isInteger(message.loopCount))
                    return "loopCount: integer expected";
            }
            return null;
        };

        /**
         * Creates a AIRichResponseDynamicMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AIRichResponseDynamicMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AIRichResponseDynamicMetadata} AIRichResponseDynamicMetadata
         */
        AIRichResponseDynamicMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AIRichResponseDynamicMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.type) {
            case "AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_UNKNOWN":
            case 0:
                message.type = 0;
                break;
            case "AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_IMAGE":
            case 1:
                message.type = 1;
                break;
            case "AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_GIF":
            case 2:
                message.type = 2;
                break;
            default:
                if (typeof object.type === "number" && (object.type | 0) === object.type)
                    message.type = object.type;
            }
            if (object.version != null)
                if ($util.Long)
                    message.version = $util.Long.fromValue(object.version, true);
                else if (typeof object.version === "string")
                    message.version = parseInt(object.version, 10);
                else if (typeof object.version === "number")
                    message.version = object.version;
                else if (typeof object.version === "object")
                    message.version = new $util.LongBits(object.version.low >>> 0, object.version.high >>> 0).toNumber(true);
            if (object.url != null)
                message.url = String(object.url);
            if (object.loopCount != null)
                message.loopCount = object.loopCount >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a AIRichResponseDynamicMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AIRichResponseDynamicMetadata
         * @static
         * @param {proto.AIRichResponseDynamicMetadata} message AIRichResponseDynamicMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AIRichResponseDynamicMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                object.type = options.enums === String ? $root.proto.AIRichResponseDynamicMetadata.AIRichResponseDynamicMetadataType[message.type] === undefined ? message.type : $root.proto.AIRichResponseDynamicMetadata.AIRichResponseDynamicMetadataType[message.type] : message.type;
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.version = typeof message.version === "number" ? BigInt(message.version) : $util.Long.fromBits(message.version.low >>> 0, message.version.high >>> 0, true).toBigInt();
                else if (typeof message.version === "number")
                    object.version = options.longs === String ? String(message.version) : message.version;
                else
                    object.version = options.longs === String ? $util.Long.prototype.toString.call(message.version) : options.longs === Number ? new $util.LongBits(message.version.low >>> 0, message.version.high >>> 0).toNumber(true) : message.version;
            if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                object.url = message.url;
            if (message.loopCount != null && Object.hasOwnProperty.call(message, "loopCount"))
                object.loopCount = message.loopCount;
            return object;
        };

        /**
         * Converts this AIRichResponseDynamicMetadata to JSON.
         * @function toJSON
         * @memberof proto.AIRichResponseDynamicMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AIRichResponseDynamicMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AIRichResponseDynamicMetadata
         * @function getTypeUrl
         * @memberof proto.AIRichResponseDynamicMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AIRichResponseDynamicMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AIRichResponseDynamicMetadata";
        };

        /**
         * AIRichResponseDynamicMetadataType enum.
         * @name proto.AIRichResponseDynamicMetadata.AIRichResponseDynamicMetadataType
         * @enum {number}
         * @property {number} AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_UNKNOWN=0 AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_UNKNOWN value
         * @property {number} AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_IMAGE=1 AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_IMAGE value
         * @property {number} AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_GIF=2 AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_GIF value
         */
        AIRichResponseDynamicMetadata.AIRichResponseDynamicMetadataType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_UNKNOWN"] = 0;
            values[valuesById[1] = "AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_IMAGE"] = 1;
            values[valuesById[2] = "AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_GIF"] = 2;
            return values;
        })();

        return AIRichResponseDynamicMetadata;
    })();

    proto.AIRichResponseGridImageMetadata = (function() {

        /**
         * Properties of a AIRichResponseGridImageMetadata.
         * @memberof proto
         * @interface IAIRichResponseGridImageMetadata
         * @property {proto.IAIRichResponseImageURL|null} [gridImageUrl] AIRichResponseGridImageMetadata gridImageUrl
         * @property {Array.<proto.IAIRichResponseImageURL>|null} [imageUrls] AIRichResponseGridImageMetadata imageUrls
         */

        /**
         * Constructs a new AIRichResponseGridImageMetadata.
         * @memberof proto
         * @classdesc Represents a AIRichResponseGridImageMetadata.
         * @implements IAIRichResponseGridImageMetadata
         * @constructor
         * @param {proto.IAIRichResponseGridImageMetadata=} [properties] Properties to set
         */
        function AIRichResponseGridImageMetadata(properties) {
            this.imageUrls = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AIRichResponseGridImageMetadata gridImageUrl.
         * @member {proto.IAIRichResponseImageURL|null|undefined} gridImageUrl
         * @memberof proto.AIRichResponseGridImageMetadata
         * @instance
         */
        AIRichResponseGridImageMetadata.prototype.gridImageUrl = null;

        /**
         * AIRichResponseGridImageMetadata imageUrls.
         * @member {Array.<proto.IAIRichResponseImageURL>} imageUrls
         * @memberof proto.AIRichResponseGridImageMetadata
         * @instance
         */
        AIRichResponseGridImageMetadata.prototype.imageUrls = $util.emptyArray;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseGridImageMetadata.prototype, "_gridImageUrl", {
            get: $util.oneOfGetter($oneOfFields = ["gridImageUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AIRichResponseGridImageMetadata instance using the specified properties.
         * @function create
         * @memberof proto.AIRichResponseGridImageMetadata
         * @static
         * @param {proto.IAIRichResponseGridImageMetadata=} [properties] Properties to set
         * @returns {proto.AIRichResponseGridImageMetadata} AIRichResponseGridImageMetadata instance
         */
        AIRichResponseGridImageMetadata.create = function create(properties) {
            return new AIRichResponseGridImageMetadata(properties);
        };

        /**
         * Encodes the specified AIRichResponseGridImageMetadata message. Does not implicitly {@link proto.AIRichResponseGridImageMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.AIRichResponseGridImageMetadata
         * @static
         * @param {proto.IAIRichResponseGridImageMetadata} message AIRichResponseGridImageMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseGridImageMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.gridImageUrl != null && Object.hasOwnProperty.call(message, "gridImageUrl"))
                $root.proto.AIRichResponseImageURL.encode(message.gridImageUrl, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.imageUrls != null && message.imageUrls.length)
                for (var i = 0; i < message.imageUrls.length; ++i)
                    $root.proto.AIRichResponseImageURL.encode(message.imageUrls[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AIRichResponseGridImageMetadata message, length delimited. Does not implicitly {@link proto.AIRichResponseGridImageMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AIRichResponseGridImageMetadata
         * @static
         * @param {proto.IAIRichResponseGridImageMetadata} message AIRichResponseGridImageMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseGridImageMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AIRichResponseGridImageMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AIRichResponseGridImageMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AIRichResponseGridImageMetadata} AIRichResponseGridImageMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseGridImageMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.gridImageUrl = $root.proto.AIRichResponseImageURL.decode(reader, reader.uint32(), undefined, q + 1, message.gridImageUrl);
                        message._gridImageUrl = "gridImageUrl";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(message.imageUrls && message.imageUrls.length))
                            message.imageUrls = [];
                        message.imageUrls.push($root.proto.AIRichResponseImageURL.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a AIRichResponseGridImageMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AIRichResponseGridImageMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AIRichResponseGridImageMetadata} AIRichResponseGridImageMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseGridImageMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AIRichResponseGridImageMetadata message.
         * @function verify
         * @memberof proto.AIRichResponseGridImageMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AIRichResponseGridImageMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.gridImageUrl != null && Object.hasOwnProperty.call(message, "gridImageUrl")) {
                properties._gridImageUrl = 1;
                {
                    var error = $root.proto.AIRichResponseImageURL.verify(message.gridImageUrl, q + 1);
                    if (error)
                        return "gridImageUrl." + error;
                }
            }
            if (message.imageUrls != null && Object.hasOwnProperty.call(message, "imageUrls")) {
                if (!Array.isArray(message.imageUrls))
                    return "imageUrls: array expected";
                for (var i = 0; i < message.imageUrls.length; ++i) {
                    var error = $root.proto.AIRichResponseImageURL.verify(message.imageUrls[i], q + 1);
                    if (error)
                        return "imageUrls." + error;
                }
            }
            return null;
        };

        /**
         * Creates a AIRichResponseGridImageMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AIRichResponseGridImageMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AIRichResponseGridImageMetadata} AIRichResponseGridImageMetadata
         */
        AIRichResponseGridImageMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AIRichResponseGridImageMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.gridImageUrl != null) {
                if (!$util.isObject(object.gridImageUrl))
                    throw TypeError(".proto.AIRichResponseGridImageMetadata.gridImageUrl: object expected");
                message.gridImageUrl = $root.proto.AIRichResponseImageURL.fromObject(object.gridImageUrl, q + 1);
            }
            if (object.imageUrls) {
                if (!Array.isArray(object.imageUrls))
                    throw TypeError(".proto.AIRichResponseGridImageMetadata.imageUrls: array expected");
                message.imageUrls = Array(object.imageUrls.length);
                for (var i = 0; i < object.imageUrls.length; ++i) {
                    if (!$util.isObject(object.imageUrls[i]))
                        throw TypeError(".proto.AIRichResponseGridImageMetadata.imageUrls: object expected");
                    message.imageUrls[i] = $root.proto.AIRichResponseImageURL.fromObject(object.imageUrls[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a AIRichResponseGridImageMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AIRichResponseGridImageMetadata
         * @static
         * @param {proto.AIRichResponseGridImageMetadata} message AIRichResponseGridImageMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AIRichResponseGridImageMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.imageUrls = [];
            if (message.gridImageUrl != null && Object.hasOwnProperty.call(message, "gridImageUrl"))
                object.gridImageUrl = $root.proto.AIRichResponseImageURL.toObject(message.gridImageUrl, options, q + 1);
            if (message.imageUrls && message.imageUrls.length) {
                object.imageUrls = Array(message.imageUrls.length);
                for (var j = 0; j < message.imageUrls.length; ++j)
                    object.imageUrls[j] = $root.proto.AIRichResponseImageURL.toObject(message.imageUrls[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this AIRichResponseGridImageMetadata to JSON.
         * @function toJSON
         * @memberof proto.AIRichResponseGridImageMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AIRichResponseGridImageMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AIRichResponseGridImageMetadata
         * @function getTypeUrl
         * @memberof proto.AIRichResponseGridImageMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AIRichResponseGridImageMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AIRichResponseGridImageMetadata";
        };

        return AIRichResponseGridImageMetadata;
    })();

    proto.AIRichResponseImageURL = (function() {

        /**
         * Properties of a AIRichResponseImageURL.
         * @memberof proto
         * @interface IAIRichResponseImageURL
         * @property {string|null} [imagePreviewUrl] AIRichResponseImageURL imagePreviewUrl
         * @property {string|null} [imageHighResUrl] AIRichResponseImageURL imageHighResUrl
         * @property {string|null} [sourceUrl] AIRichResponseImageURL sourceUrl
         */

        /**
         * Constructs a new AIRichResponseImageURL.
         * @memberof proto
         * @classdesc Represents a AIRichResponseImageURL.
         * @implements IAIRichResponseImageURL
         * @constructor
         * @param {proto.IAIRichResponseImageURL=} [properties] Properties to set
         */
        function AIRichResponseImageURL(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AIRichResponseImageURL imagePreviewUrl.
         * @member {string|null|undefined} imagePreviewUrl
         * @memberof proto.AIRichResponseImageURL
         * @instance
         */
        AIRichResponseImageURL.prototype.imagePreviewUrl = null;

        /**
         * AIRichResponseImageURL imageHighResUrl.
         * @member {string|null|undefined} imageHighResUrl
         * @memberof proto.AIRichResponseImageURL
         * @instance
         */
        AIRichResponseImageURL.prototype.imageHighResUrl = null;

        /**
         * AIRichResponseImageURL sourceUrl.
         * @member {string|null|undefined} sourceUrl
         * @memberof proto.AIRichResponseImageURL
         * @instance
         */
        AIRichResponseImageURL.prototype.sourceUrl = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseImageURL.prototype, "_imagePreviewUrl", {
            get: $util.oneOfGetter($oneOfFields = ["imagePreviewUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseImageURL.prototype, "_imageHighResUrl", {
            get: $util.oneOfGetter($oneOfFields = ["imageHighResUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseImageURL.prototype, "_sourceUrl", {
            get: $util.oneOfGetter($oneOfFields = ["sourceUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AIRichResponseImageURL instance using the specified properties.
         * @function create
         * @memberof proto.AIRichResponseImageURL
         * @static
         * @param {proto.IAIRichResponseImageURL=} [properties] Properties to set
         * @returns {proto.AIRichResponseImageURL} AIRichResponseImageURL instance
         */
        AIRichResponseImageURL.create = function create(properties) {
            return new AIRichResponseImageURL(properties);
        };

        /**
         * Encodes the specified AIRichResponseImageURL message. Does not implicitly {@link proto.AIRichResponseImageURL.verify|verify} messages.
         * @function encode
         * @memberof proto.AIRichResponseImageURL
         * @static
         * @param {proto.IAIRichResponseImageURL} message AIRichResponseImageURL message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseImageURL.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.imagePreviewUrl != null && Object.hasOwnProperty.call(message, "imagePreviewUrl"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.imagePreviewUrl);
            if (message.imageHighResUrl != null && Object.hasOwnProperty.call(message, "imageHighResUrl"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.imageHighResUrl);
            if (message.sourceUrl != null && Object.hasOwnProperty.call(message, "sourceUrl"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.sourceUrl);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AIRichResponseImageURL message, length delimited. Does not implicitly {@link proto.AIRichResponseImageURL.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AIRichResponseImageURL
         * @static
         * @param {proto.IAIRichResponseImageURL} message AIRichResponseImageURL message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseImageURL.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AIRichResponseImageURL message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AIRichResponseImageURL
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AIRichResponseImageURL} AIRichResponseImageURL
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseImageURL.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.imagePreviewUrl = reader.stringVerify();
                        message._imagePreviewUrl = "imagePreviewUrl";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.imageHighResUrl = reader.stringVerify();
                        message._imageHighResUrl = "imageHighResUrl";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.sourceUrl = reader.stringVerify();
                        message._sourceUrl = "sourceUrl";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a AIRichResponseImageURL message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AIRichResponseImageURL
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AIRichResponseImageURL} AIRichResponseImageURL
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseImageURL.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AIRichResponseImageURL message.
         * @function verify
         * @memberof proto.AIRichResponseImageURL
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AIRichResponseImageURL.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.imagePreviewUrl != null && Object.hasOwnProperty.call(message, "imagePreviewUrl")) {
                properties._imagePreviewUrl = 1;
                if (!$util.isString(message.imagePreviewUrl))
                    return "imagePreviewUrl: string expected";
            }
            if (message.imageHighResUrl != null && Object.hasOwnProperty.call(message, "imageHighResUrl")) {
                properties._imageHighResUrl = 1;
                if (!$util.isString(message.imageHighResUrl))
                    return "imageHighResUrl: string expected";
            }
            if (message.sourceUrl != null && Object.hasOwnProperty.call(message, "sourceUrl")) {
                properties._sourceUrl = 1;
                if (!$util.isString(message.sourceUrl))
                    return "sourceUrl: string expected";
            }
            return null;
        };

        /**
         * Creates a AIRichResponseImageURL message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AIRichResponseImageURL
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AIRichResponseImageURL} AIRichResponseImageURL
         */
        AIRichResponseImageURL.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AIRichResponseImageURL: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.imagePreviewUrl != null)
                message.imagePreviewUrl = String(object.imagePreviewUrl);
            if (object.imageHighResUrl != null)
                message.imageHighResUrl = String(object.imageHighResUrl);
            if (object.sourceUrl != null)
                message.sourceUrl = String(object.sourceUrl);
            return message;
        };

        /**
         * Creates a plain object from a AIRichResponseImageURL message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AIRichResponseImageURL
         * @static
         * @param {proto.AIRichResponseImageURL} message AIRichResponseImageURL
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AIRichResponseImageURL.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.imagePreviewUrl != null && Object.hasOwnProperty.call(message, "imagePreviewUrl"))
                object.imagePreviewUrl = message.imagePreviewUrl;
            if (message.imageHighResUrl != null && Object.hasOwnProperty.call(message, "imageHighResUrl"))
                object.imageHighResUrl = message.imageHighResUrl;
            if (message.sourceUrl != null && Object.hasOwnProperty.call(message, "sourceUrl"))
                object.sourceUrl = message.sourceUrl;
            return object;
        };

        /**
         * Converts this AIRichResponseImageURL to JSON.
         * @function toJSON
         * @memberof proto.AIRichResponseImageURL
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AIRichResponseImageURL.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AIRichResponseImageURL
         * @function getTypeUrl
         * @memberof proto.AIRichResponseImageURL
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AIRichResponseImageURL.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AIRichResponseImageURL";
        };

        return AIRichResponseImageURL;
    })();

    proto.AIRichResponseInlineImageMetadata = (function() {

        /**
         * Properties of a AIRichResponseInlineImageMetadata.
         * @memberof proto
         * @interface IAIRichResponseInlineImageMetadata
         * @property {proto.IAIRichResponseImageURL|null} [imageUrl] AIRichResponseInlineImageMetadata imageUrl
         * @property {string|null} [imageText] AIRichResponseInlineImageMetadata imageText
         * @property {proto.AIRichResponseInlineImageMetadata.AIRichResponseImageAlignment|null} [alignment] AIRichResponseInlineImageMetadata alignment
         * @property {string|null} [tapLinkUrl] AIRichResponseInlineImageMetadata tapLinkUrl
         */

        /**
         * Constructs a new AIRichResponseInlineImageMetadata.
         * @memberof proto
         * @classdesc Represents a AIRichResponseInlineImageMetadata.
         * @implements IAIRichResponseInlineImageMetadata
         * @constructor
         * @param {proto.IAIRichResponseInlineImageMetadata=} [properties] Properties to set
         */
        function AIRichResponseInlineImageMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AIRichResponseInlineImageMetadata imageUrl.
         * @member {proto.IAIRichResponseImageURL|null|undefined} imageUrl
         * @memberof proto.AIRichResponseInlineImageMetadata
         * @instance
         */
        AIRichResponseInlineImageMetadata.prototype.imageUrl = null;

        /**
         * AIRichResponseInlineImageMetadata imageText.
         * @member {string|null|undefined} imageText
         * @memberof proto.AIRichResponseInlineImageMetadata
         * @instance
         */
        AIRichResponseInlineImageMetadata.prototype.imageText = null;

        /**
         * AIRichResponseInlineImageMetadata alignment.
         * @member {proto.AIRichResponseInlineImageMetadata.AIRichResponseImageAlignment|null|undefined} alignment
         * @memberof proto.AIRichResponseInlineImageMetadata
         * @instance
         */
        AIRichResponseInlineImageMetadata.prototype.alignment = null;

        /**
         * AIRichResponseInlineImageMetadata tapLinkUrl.
         * @member {string|null|undefined} tapLinkUrl
         * @memberof proto.AIRichResponseInlineImageMetadata
         * @instance
         */
        AIRichResponseInlineImageMetadata.prototype.tapLinkUrl = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseInlineImageMetadata.prototype, "_imageUrl", {
            get: $util.oneOfGetter($oneOfFields = ["imageUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseInlineImageMetadata.prototype, "_imageText", {
            get: $util.oneOfGetter($oneOfFields = ["imageText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseInlineImageMetadata.prototype, "_alignment", {
            get: $util.oneOfGetter($oneOfFields = ["alignment"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseInlineImageMetadata.prototype, "_tapLinkUrl", {
            get: $util.oneOfGetter($oneOfFields = ["tapLinkUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AIRichResponseInlineImageMetadata instance using the specified properties.
         * @function create
         * @memberof proto.AIRichResponseInlineImageMetadata
         * @static
         * @param {proto.IAIRichResponseInlineImageMetadata=} [properties] Properties to set
         * @returns {proto.AIRichResponseInlineImageMetadata} AIRichResponseInlineImageMetadata instance
         */
        AIRichResponseInlineImageMetadata.create = function create(properties) {
            return new AIRichResponseInlineImageMetadata(properties);
        };

        /**
         * Encodes the specified AIRichResponseInlineImageMetadata message. Does not implicitly {@link proto.AIRichResponseInlineImageMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.AIRichResponseInlineImageMetadata
         * @static
         * @param {proto.IAIRichResponseInlineImageMetadata} message AIRichResponseInlineImageMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseInlineImageMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.imageUrl != null && Object.hasOwnProperty.call(message, "imageUrl"))
                $root.proto.AIRichResponseImageURL.encode(message.imageUrl, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.imageText != null && Object.hasOwnProperty.call(message, "imageText"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.imageText);
            if (message.alignment != null && Object.hasOwnProperty.call(message, "alignment"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.alignment);
            if (message.tapLinkUrl != null && Object.hasOwnProperty.call(message, "tapLinkUrl"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.tapLinkUrl);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AIRichResponseInlineImageMetadata message, length delimited. Does not implicitly {@link proto.AIRichResponseInlineImageMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AIRichResponseInlineImageMetadata
         * @static
         * @param {proto.IAIRichResponseInlineImageMetadata} message AIRichResponseInlineImageMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseInlineImageMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AIRichResponseInlineImageMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AIRichResponseInlineImageMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AIRichResponseInlineImageMetadata} AIRichResponseInlineImageMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseInlineImageMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.imageUrl = $root.proto.AIRichResponseImageURL.decode(reader, reader.uint32(), undefined, q + 1, message.imageUrl);
                        message._imageUrl = "imageUrl";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.imageText = reader.stringVerify();
                        message._imageText = "imageText";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.alignment = reader.int32();
                        message._alignment = "alignment";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.tapLinkUrl = reader.stringVerify();
                        message._tapLinkUrl = "tapLinkUrl";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a AIRichResponseInlineImageMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AIRichResponseInlineImageMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AIRichResponseInlineImageMetadata} AIRichResponseInlineImageMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseInlineImageMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AIRichResponseInlineImageMetadata message.
         * @function verify
         * @memberof proto.AIRichResponseInlineImageMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AIRichResponseInlineImageMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.imageUrl != null && Object.hasOwnProperty.call(message, "imageUrl")) {
                properties._imageUrl = 1;
                {
                    var error = $root.proto.AIRichResponseImageURL.verify(message.imageUrl, q + 1);
                    if (error)
                        return "imageUrl." + error;
                }
            }
            if (message.imageText != null && Object.hasOwnProperty.call(message, "imageText")) {
                properties._imageText = 1;
                if (!$util.isString(message.imageText))
                    return "imageText: string expected";
            }
            if (message.alignment != null && Object.hasOwnProperty.call(message, "alignment")) {
                properties._alignment = 1;
                if (typeof message.alignment !== "number" || (message.alignment | 0) !== message.alignment)
                    return "alignment: enum value expected";
            }
            if (message.tapLinkUrl != null && Object.hasOwnProperty.call(message, "tapLinkUrl")) {
                properties._tapLinkUrl = 1;
                if (!$util.isString(message.tapLinkUrl))
                    return "tapLinkUrl: string expected";
            }
            return null;
        };

        /**
         * Creates a AIRichResponseInlineImageMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AIRichResponseInlineImageMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AIRichResponseInlineImageMetadata} AIRichResponseInlineImageMetadata
         */
        AIRichResponseInlineImageMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AIRichResponseInlineImageMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.imageUrl != null) {
                if (!$util.isObject(object.imageUrl))
                    throw TypeError(".proto.AIRichResponseInlineImageMetadata.imageUrl: object expected");
                message.imageUrl = $root.proto.AIRichResponseImageURL.fromObject(object.imageUrl, q + 1);
            }
            if (object.imageText != null)
                message.imageText = String(object.imageText);
            switch (object.alignment) {
            case "AI_RICH_RESPONSE_IMAGE_LAYOUT_LEADING_ALIGNED":
            case 0:
                message.alignment = 0;
                break;
            case "AI_RICH_RESPONSE_IMAGE_LAYOUT_TRAILING_ALIGNED":
            case 1:
                message.alignment = 1;
                break;
            case "AI_RICH_RESPONSE_IMAGE_LAYOUT_CENTER_ALIGNED":
            case 2:
                message.alignment = 2;
                break;
            default:
                if (typeof object.alignment === "number" && (object.alignment | 0) === object.alignment)
                    message.alignment = object.alignment;
            }
            if (object.tapLinkUrl != null)
                message.tapLinkUrl = String(object.tapLinkUrl);
            return message;
        };

        /**
         * Creates a plain object from a AIRichResponseInlineImageMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AIRichResponseInlineImageMetadata
         * @static
         * @param {proto.AIRichResponseInlineImageMetadata} message AIRichResponseInlineImageMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AIRichResponseInlineImageMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.imageUrl != null && Object.hasOwnProperty.call(message, "imageUrl"))
                object.imageUrl = $root.proto.AIRichResponseImageURL.toObject(message.imageUrl, options, q + 1);
            if (message.imageText != null && Object.hasOwnProperty.call(message, "imageText"))
                object.imageText = message.imageText;
            if (message.alignment != null && Object.hasOwnProperty.call(message, "alignment"))
                object.alignment = options.enums === String ? $root.proto.AIRichResponseInlineImageMetadata.AIRichResponseImageAlignment[message.alignment] === undefined ? message.alignment : $root.proto.AIRichResponseInlineImageMetadata.AIRichResponseImageAlignment[message.alignment] : message.alignment;
            if (message.tapLinkUrl != null && Object.hasOwnProperty.call(message, "tapLinkUrl"))
                object.tapLinkUrl = message.tapLinkUrl;
            return object;
        };

        /**
         * Converts this AIRichResponseInlineImageMetadata to JSON.
         * @function toJSON
         * @memberof proto.AIRichResponseInlineImageMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AIRichResponseInlineImageMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AIRichResponseInlineImageMetadata
         * @function getTypeUrl
         * @memberof proto.AIRichResponseInlineImageMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AIRichResponseInlineImageMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AIRichResponseInlineImageMetadata";
        };

        /**
         * AIRichResponseImageAlignment enum.
         * @name proto.AIRichResponseInlineImageMetadata.AIRichResponseImageAlignment
         * @enum {number}
         * @property {number} AI_RICH_RESPONSE_IMAGE_LAYOUT_LEADING_ALIGNED=0 AI_RICH_RESPONSE_IMAGE_LAYOUT_LEADING_ALIGNED value
         * @property {number} AI_RICH_RESPONSE_IMAGE_LAYOUT_TRAILING_ALIGNED=1 AI_RICH_RESPONSE_IMAGE_LAYOUT_TRAILING_ALIGNED value
         * @property {number} AI_RICH_RESPONSE_IMAGE_LAYOUT_CENTER_ALIGNED=2 AI_RICH_RESPONSE_IMAGE_LAYOUT_CENTER_ALIGNED value
         */
        AIRichResponseInlineImageMetadata.AIRichResponseImageAlignment = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "AI_RICH_RESPONSE_IMAGE_LAYOUT_LEADING_ALIGNED"] = 0;
            values[valuesById[1] = "AI_RICH_RESPONSE_IMAGE_LAYOUT_TRAILING_ALIGNED"] = 1;
            values[valuesById[2] = "AI_RICH_RESPONSE_IMAGE_LAYOUT_CENTER_ALIGNED"] = 2;
            return values;
        })();

        return AIRichResponseInlineImageMetadata;
    })();

    proto.AIRichResponseLatexMetadata = (function() {

        /**
         * Properties of a AIRichResponseLatexMetadata.
         * @memberof proto
         * @interface IAIRichResponseLatexMetadata
         * @property {string|null} [text] AIRichResponseLatexMetadata text
         * @property {Array.<proto.AIRichResponseLatexMetadata.IAIRichResponseLatexExpression>|null} [expressions] AIRichResponseLatexMetadata expressions
         */

        /**
         * Constructs a new AIRichResponseLatexMetadata.
         * @memberof proto
         * @classdesc Represents a AIRichResponseLatexMetadata.
         * @implements IAIRichResponseLatexMetadata
         * @constructor
         * @param {proto.IAIRichResponseLatexMetadata=} [properties] Properties to set
         */
        function AIRichResponseLatexMetadata(properties) {
            this.expressions = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AIRichResponseLatexMetadata text.
         * @member {string|null|undefined} text
         * @memberof proto.AIRichResponseLatexMetadata
         * @instance
         */
        AIRichResponseLatexMetadata.prototype.text = null;

        /**
         * AIRichResponseLatexMetadata expressions.
         * @member {Array.<proto.AIRichResponseLatexMetadata.IAIRichResponseLatexExpression>} expressions
         * @memberof proto.AIRichResponseLatexMetadata
         * @instance
         */
        AIRichResponseLatexMetadata.prototype.expressions = $util.emptyArray;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseLatexMetadata.prototype, "_text", {
            get: $util.oneOfGetter($oneOfFields = ["text"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AIRichResponseLatexMetadata instance using the specified properties.
         * @function create
         * @memberof proto.AIRichResponseLatexMetadata
         * @static
         * @param {proto.IAIRichResponseLatexMetadata=} [properties] Properties to set
         * @returns {proto.AIRichResponseLatexMetadata} AIRichResponseLatexMetadata instance
         */
        AIRichResponseLatexMetadata.create = function create(properties) {
            return new AIRichResponseLatexMetadata(properties);
        };

        /**
         * Encodes the specified AIRichResponseLatexMetadata message. Does not implicitly {@link proto.AIRichResponseLatexMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.AIRichResponseLatexMetadata
         * @static
         * @param {proto.IAIRichResponseLatexMetadata} message AIRichResponseLatexMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseLatexMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.text);
            if (message.expressions != null && message.expressions.length)
                for (var i = 0; i < message.expressions.length; ++i)
                    $root.proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression.encode(message.expressions[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AIRichResponseLatexMetadata message, length delimited. Does not implicitly {@link proto.AIRichResponseLatexMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AIRichResponseLatexMetadata
         * @static
         * @param {proto.IAIRichResponseLatexMetadata} message AIRichResponseLatexMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseLatexMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AIRichResponseLatexMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AIRichResponseLatexMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AIRichResponseLatexMetadata} AIRichResponseLatexMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseLatexMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.text = reader.stringVerify();
                        message._text = "text";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(message.expressions && message.expressions.length))
                            message.expressions = [];
                        message.expressions.push($root.proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a AIRichResponseLatexMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AIRichResponseLatexMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AIRichResponseLatexMetadata} AIRichResponseLatexMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseLatexMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AIRichResponseLatexMetadata message.
         * @function verify
         * @memberof proto.AIRichResponseLatexMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AIRichResponseLatexMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.text != null && Object.hasOwnProperty.call(message, "text")) {
                properties._text = 1;
                if (!$util.isString(message.text))
                    return "text: string expected";
            }
            if (message.expressions != null && Object.hasOwnProperty.call(message, "expressions")) {
                if (!Array.isArray(message.expressions))
                    return "expressions: array expected";
                for (var i = 0; i < message.expressions.length; ++i) {
                    var error = $root.proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression.verify(message.expressions[i], q + 1);
                    if (error)
                        return "expressions." + error;
                }
            }
            return null;
        };

        /**
         * Creates a AIRichResponseLatexMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AIRichResponseLatexMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AIRichResponseLatexMetadata} AIRichResponseLatexMetadata
         */
        AIRichResponseLatexMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AIRichResponseLatexMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.text != null)
                message.text = String(object.text);
            if (object.expressions) {
                if (!Array.isArray(object.expressions))
                    throw TypeError(".proto.AIRichResponseLatexMetadata.expressions: array expected");
                message.expressions = Array(object.expressions.length);
                for (var i = 0; i < object.expressions.length; ++i) {
                    if (!$util.isObject(object.expressions[i]))
                        throw TypeError(".proto.AIRichResponseLatexMetadata.expressions: object expected");
                    message.expressions[i] = $root.proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression.fromObject(object.expressions[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a AIRichResponseLatexMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AIRichResponseLatexMetadata
         * @static
         * @param {proto.AIRichResponseLatexMetadata} message AIRichResponseLatexMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AIRichResponseLatexMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.expressions = [];
            if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                object.text = message.text;
            if (message.expressions && message.expressions.length) {
                object.expressions = Array(message.expressions.length);
                for (var j = 0; j < message.expressions.length; ++j)
                    object.expressions[j] = $root.proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression.toObject(message.expressions[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this AIRichResponseLatexMetadata to JSON.
         * @function toJSON
         * @memberof proto.AIRichResponseLatexMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AIRichResponseLatexMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AIRichResponseLatexMetadata
         * @function getTypeUrl
         * @memberof proto.AIRichResponseLatexMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AIRichResponseLatexMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AIRichResponseLatexMetadata";
        };

        AIRichResponseLatexMetadata.AIRichResponseLatexExpression = (function() {

            /**
             * Properties of a AIRichResponseLatexExpression.
             * @memberof proto.AIRichResponseLatexMetadata
             * @interface IAIRichResponseLatexExpression
             * @property {string|null} [latexExpression] AIRichResponseLatexExpression latexExpression
             * @property {string|null} [url] AIRichResponseLatexExpression url
             * @property {number|null} [width] AIRichResponseLatexExpression width
             * @property {number|null} [height] AIRichResponseLatexExpression height
             * @property {number|null} [fontHeight] AIRichResponseLatexExpression fontHeight
             * @property {number|null} [imageTopPadding] AIRichResponseLatexExpression imageTopPadding
             * @property {number|null} [imageLeadingPadding] AIRichResponseLatexExpression imageLeadingPadding
             * @property {number|null} [imageBottomPadding] AIRichResponseLatexExpression imageBottomPadding
             * @property {number|null} [imageTrailingPadding] AIRichResponseLatexExpression imageTrailingPadding
             */

            /**
             * Constructs a new AIRichResponseLatexExpression.
             * @memberof proto.AIRichResponseLatexMetadata
             * @classdesc Represents a AIRichResponseLatexExpression.
             * @implements IAIRichResponseLatexExpression
             * @constructor
             * @param {proto.AIRichResponseLatexMetadata.IAIRichResponseLatexExpression=} [properties] Properties to set
             */
            function AIRichResponseLatexExpression(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AIRichResponseLatexExpression latexExpression.
             * @member {string|null|undefined} latexExpression
             * @memberof proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression
             * @instance
             */
            AIRichResponseLatexExpression.prototype.latexExpression = null;

            /**
             * AIRichResponseLatexExpression url.
             * @member {string|null|undefined} url
             * @memberof proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression
             * @instance
             */
            AIRichResponseLatexExpression.prototype.url = null;

            /**
             * AIRichResponseLatexExpression width.
             * @member {number|null|undefined} width
             * @memberof proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression
             * @instance
             */
            AIRichResponseLatexExpression.prototype.width = null;

            /**
             * AIRichResponseLatexExpression height.
             * @member {number|null|undefined} height
             * @memberof proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression
             * @instance
             */
            AIRichResponseLatexExpression.prototype.height = null;

            /**
             * AIRichResponseLatexExpression fontHeight.
             * @member {number|null|undefined} fontHeight
             * @memberof proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression
             * @instance
             */
            AIRichResponseLatexExpression.prototype.fontHeight = null;

            /**
             * AIRichResponseLatexExpression imageTopPadding.
             * @member {number|null|undefined} imageTopPadding
             * @memberof proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression
             * @instance
             */
            AIRichResponseLatexExpression.prototype.imageTopPadding = null;

            /**
             * AIRichResponseLatexExpression imageLeadingPadding.
             * @member {number|null|undefined} imageLeadingPadding
             * @memberof proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression
             * @instance
             */
            AIRichResponseLatexExpression.prototype.imageLeadingPadding = null;

            /**
             * AIRichResponseLatexExpression imageBottomPadding.
             * @member {number|null|undefined} imageBottomPadding
             * @memberof proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression
             * @instance
             */
            AIRichResponseLatexExpression.prototype.imageBottomPadding = null;

            /**
             * AIRichResponseLatexExpression imageTrailingPadding.
             * @member {number|null|undefined} imageTrailingPadding
             * @memberof proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression
             * @instance
             */
            AIRichResponseLatexExpression.prototype.imageTrailingPadding = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseLatexExpression.prototype, "_latexExpression", {
                get: $util.oneOfGetter($oneOfFields = ["latexExpression"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseLatexExpression.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseLatexExpression.prototype, "_width", {
                get: $util.oneOfGetter($oneOfFields = ["width"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseLatexExpression.prototype, "_height", {
                get: $util.oneOfGetter($oneOfFields = ["height"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseLatexExpression.prototype, "_fontHeight", {
                get: $util.oneOfGetter($oneOfFields = ["fontHeight"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseLatexExpression.prototype, "_imageTopPadding", {
                get: $util.oneOfGetter($oneOfFields = ["imageTopPadding"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseLatexExpression.prototype, "_imageLeadingPadding", {
                get: $util.oneOfGetter($oneOfFields = ["imageLeadingPadding"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseLatexExpression.prototype, "_imageBottomPadding", {
                get: $util.oneOfGetter($oneOfFields = ["imageBottomPadding"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseLatexExpression.prototype, "_imageTrailingPadding", {
                get: $util.oneOfGetter($oneOfFields = ["imageTrailingPadding"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AIRichResponseLatexExpression instance using the specified properties.
             * @function create
             * @memberof proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression
             * @static
             * @param {proto.AIRichResponseLatexMetadata.IAIRichResponseLatexExpression=} [properties] Properties to set
             * @returns {proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression} AIRichResponseLatexExpression instance
             */
            AIRichResponseLatexExpression.create = function create(properties) {
                return new AIRichResponseLatexExpression(properties);
            };

            /**
             * Encodes the specified AIRichResponseLatexExpression message. Does not implicitly {@link proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression.verify|verify} messages.
             * @function encode
             * @memberof proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression
             * @static
             * @param {proto.AIRichResponseLatexMetadata.IAIRichResponseLatexExpression} message AIRichResponseLatexExpression message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AIRichResponseLatexExpression.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.latexExpression != null && Object.hasOwnProperty.call(message, "latexExpression"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.latexExpression);
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.url);
                if (message.width != null && Object.hasOwnProperty.call(message, "width"))
                    writer.uint32(/* id 3, wireType 1 =*/25).double(message.width);
                if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                    writer.uint32(/* id 4, wireType 1 =*/33).double(message.height);
                if (message.fontHeight != null && Object.hasOwnProperty.call(message, "fontHeight"))
                    writer.uint32(/* id 5, wireType 1 =*/41).double(message.fontHeight);
                if (message.imageTopPadding != null && Object.hasOwnProperty.call(message, "imageTopPadding"))
                    writer.uint32(/* id 6, wireType 1 =*/49).double(message.imageTopPadding);
                if (message.imageLeadingPadding != null && Object.hasOwnProperty.call(message, "imageLeadingPadding"))
                    writer.uint32(/* id 7, wireType 1 =*/57).double(message.imageLeadingPadding);
                if (message.imageBottomPadding != null && Object.hasOwnProperty.call(message, "imageBottomPadding"))
                    writer.uint32(/* id 8, wireType 1 =*/65).double(message.imageBottomPadding);
                if (message.imageTrailingPadding != null && Object.hasOwnProperty.call(message, "imageTrailingPadding"))
                    writer.uint32(/* id 9, wireType 1 =*/73).double(message.imageTrailingPadding);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AIRichResponseLatexExpression message, length delimited. Does not implicitly {@link proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression
             * @static
             * @param {proto.AIRichResponseLatexMetadata.IAIRichResponseLatexExpression} message AIRichResponseLatexExpression message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AIRichResponseLatexExpression.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a AIRichResponseLatexExpression message from the specified reader or buffer.
             * @function decode
             * @memberof proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression} AIRichResponseLatexExpression
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AIRichResponseLatexExpression.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.latexExpression = reader.stringVerify();
                            message._latexExpression = "latexExpression";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.url = reader.stringVerify();
                            message._url = "url";
                            continue;
                        }
                    case 3: {
                            if (u !== 1)
                                break;
                            message.width = reader.double();
                            message._width = "width";
                            continue;
                        }
                    case 4: {
                            if (u !== 1)
                                break;
                            message.height = reader.double();
                            message._height = "height";
                            continue;
                        }
                    case 5: {
                            if (u !== 1)
                                break;
                            message.fontHeight = reader.double();
                            message._fontHeight = "fontHeight";
                            continue;
                        }
                    case 6: {
                            if (u !== 1)
                                break;
                            message.imageTopPadding = reader.double();
                            message._imageTopPadding = "imageTopPadding";
                            continue;
                        }
                    case 7: {
                            if (u !== 1)
                                break;
                            message.imageLeadingPadding = reader.double();
                            message._imageLeadingPadding = "imageLeadingPadding";
                            continue;
                        }
                    case 8: {
                            if (u !== 1)
                                break;
                            message.imageBottomPadding = reader.double();
                            message._imageBottomPadding = "imageBottomPadding";
                            continue;
                        }
                    case 9: {
                            if (u !== 1)
                                break;
                            message.imageTrailingPadding = reader.double();
                            message._imageTrailingPadding = "imageTrailingPadding";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a AIRichResponseLatexExpression message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression} AIRichResponseLatexExpression
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AIRichResponseLatexExpression.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a AIRichResponseLatexExpression message.
             * @function verify
             * @memberof proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AIRichResponseLatexExpression.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.latexExpression != null && Object.hasOwnProperty.call(message, "latexExpression")) {
                    properties._latexExpression = 1;
                    if (!$util.isString(message.latexExpression))
                        return "latexExpression: string expected";
                }
                if (message.url != null && Object.hasOwnProperty.call(message, "url")) {
                    properties._url = 1;
                    if (!$util.isString(message.url))
                        return "url: string expected";
                }
                if (message.width != null && Object.hasOwnProperty.call(message, "width")) {
                    properties._width = 1;
                    if (typeof message.width !== "number")
                        return "width: number expected";
                }
                if (message.height != null && Object.hasOwnProperty.call(message, "height")) {
                    properties._height = 1;
                    if (typeof message.height !== "number")
                        return "height: number expected";
                }
                if (message.fontHeight != null && Object.hasOwnProperty.call(message, "fontHeight")) {
                    properties._fontHeight = 1;
                    if (typeof message.fontHeight !== "number")
                        return "fontHeight: number expected";
                }
                if (message.imageTopPadding != null && Object.hasOwnProperty.call(message, "imageTopPadding")) {
                    properties._imageTopPadding = 1;
                    if (typeof message.imageTopPadding !== "number")
                        return "imageTopPadding: number expected";
                }
                if (message.imageLeadingPadding != null && Object.hasOwnProperty.call(message, "imageLeadingPadding")) {
                    properties._imageLeadingPadding = 1;
                    if (typeof message.imageLeadingPadding !== "number")
                        return "imageLeadingPadding: number expected";
                }
                if (message.imageBottomPadding != null && Object.hasOwnProperty.call(message, "imageBottomPadding")) {
                    properties._imageBottomPadding = 1;
                    if (typeof message.imageBottomPadding !== "number")
                        return "imageBottomPadding: number expected";
                }
                if (message.imageTrailingPadding != null && Object.hasOwnProperty.call(message, "imageTrailingPadding")) {
                    properties._imageTrailingPadding = 1;
                    if (typeof message.imageTrailingPadding !== "number")
                        return "imageTrailingPadding: number expected";
                }
                return null;
            };

            /**
             * Creates a AIRichResponseLatexExpression message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression} AIRichResponseLatexExpression
             */
            AIRichResponseLatexExpression.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.latexExpression != null)
                    message.latexExpression = String(object.latexExpression);
                if (object.url != null)
                    message.url = String(object.url);
                if (object.width != null)
                    message.width = Number(object.width);
                if (object.height != null)
                    message.height = Number(object.height);
                if (object.fontHeight != null)
                    message.fontHeight = Number(object.fontHeight);
                if (object.imageTopPadding != null)
                    message.imageTopPadding = Number(object.imageTopPadding);
                if (object.imageLeadingPadding != null)
                    message.imageLeadingPadding = Number(object.imageLeadingPadding);
                if (object.imageBottomPadding != null)
                    message.imageBottomPadding = Number(object.imageBottomPadding);
                if (object.imageTrailingPadding != null)
                    message.imageTrailingPadding = Number(object.imageTrailingPadding);
                return message;
            };

            /**
             * Creates a plain object from a AIRichResponseLatexExpression message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression
             * @static
             * @param {proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression} message AIRichResponseLatexExpression
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AIRichResponseLatexExpression.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.latexExpression != null && Object.hasOwnProperty.call(message, "latexExpression"))
                    object.latexExpression = message.latexExpression;
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    object.url = message.url;
                if (message.width != null && Object.hasOwnProperty.call(message, "width"))
                    object.width = options.json && !isFinite(message.width) ? String(message.width) : message.width;
                if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                    object.height = options.json && !isFinite(message.height) ? String(message.height) : message.height;
                if (message.fontHeight != null && Object.hasOwnProperty.call(message, "fontHeight"))
                    object.fontHeight = options.json && !isFinite(message.fontHeight) ? String(message.fontHeight) : message.fontHeight;
                if (message.imageTopPadding != null && Object.hasOwnProperty.call(message, "imageTopPadding"))
                    object.imageTopPadding = options.json && !isFinite(message.imageTopPadding) ? String(message.imageTopPadding) : message.imageTopPadding;
                if (message.imageLeadingPadding != null && Object.hasOwnProperty.call(message, "imageLeadingPadding"))
                    object.imageLeadingPadding = options.json && !isFinite(message.imageLeadingPadding) ? String(message.imageLeadingPadding) : message.imageLeadingPadding;
                if (message.imageBottomPadding != null && Object.hasOwnProperty.call(message, "imageBottomPadding"))
                    object.imageBottomPadding = options.json && !isFinite(message.imageBottomPadding) ? String(message.imageBottomPadding) : message.imageBottomPadding;
                if (message.imageTrailingPadding != null && Object.hasOwnProperty.call(message, "imageTrailingPadding"))
                    object.imageTrailingPadding = options.json && !isFinite(message.imageTrailingPadding) ? String(message.imageTrailingPadding) : message.imageTrailingPadding;
                return object;
            };

            /**
             * Converts this AIRichResponseLatexExpression to JSON.
             * @function toJSON
             * @memberof proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AIRichResponseLatexExpression.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AIRichResponseLatexExpression
             * @function getTypeUrl
             * @memberof proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AIRichResponseLatexExpression.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression";
            };

            return AIRichResponseLatexExpression;
        })();

        return AIRichResponseLatexMetadata;
    })();

    proto.AIRichResponseMapMetadata = (function() {

        /**
         * Properties of a AIRichResponseMapMetadata.
         * @memberof proto
         * @interface IAIRichResponseMapMetadata
         * @property {number|null} [centerLatitude] AIRichResponseMapMetadata centerLatitude
         * @property {number|null} [centerLongitude] AIRichResponseMapMetadata centerLongitude
         * @property {number|null} [latitudeDelta] AIRichResponseMapMetadata latitudeDelta
         * @property {number|null} [longitudeDelta] AIRichResponseMapMetadata longitudeDelta
         * @property {Array.<proto.AIRichResponseMapMetadata.IAIRichResponseMapAnnotation>|null} [annotations] AIRichResponseMapMetadata annotations
         * @property {boolean|null} [showInfoList] AIRichResponseMapMetadata showInfoList
         */

        /**
         * Constructs a new AIRichResponseMapMetadata.
         * @memberof proto
         * @classdesc Represents a AIRichResponseMapMetadata.
         * @implements IAIRichResponseMapMetadata
         * @constructor
         * @param {proto.IAIRichResponseMapMetadata=} [properties] Properties to set
         */
        function AIRichResponseMapMetadata(properties) {
            this.annotations = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AIRichResponseMapMetadata centerLatitude.
         * @member {number|null|undefined} centerLatitude
         * @memberof proto.AIRichResponseMapMetadata
         * @instance
         */
        AIRichResponseMapMetadata.prototype.centerLatitude = null;

        /**
         * AIRichResponseMapMetadata centerLongitude.
         * @member {number|null|undefined} centerLongitude
         * @memberof proto.AIRichResponseMapMetadata
         * @instance
         */
        AIRichResponseMapMetadata.prototype.centerLongitude = null;

        /**
         * AIRichResponseMapMetadata latitudeDelta.
         * @member {number|null|undefined} latitudeDelta
         * @memberof proto.AIRichResponseMapMetadata
         * @instance
         */
        AIRichResponseMapMetadata.prototype.latitudeDelta = null;

        /**
         * AIRichResponseMapMetadata longitudeDelta.
         * @member {number|null|undefined} longitudeDelta
         * @memberof proto.AIRichResponseMapMetadata
         * @instance
         */
        AIRichResponseMapMetadata.prototype.longitudeDelta = null;

        /**
         * AIRichResponseMapMetadata annotations.
         * @member {Array.<proto.AIRichResponseMapMetadata.IAIRichResponseMapAnnotation>} annotations
         * @memberof proto.AIRichResponseMapMetadata
         * @instance
         */
        AIRichResponseMapMetadata.prototype.annotations = $util.emptyArray;

        /**
         * AIRichResponseMapMetadata showInfoList.
         * @member {boolean|null|undefined} showInfoList
         * @memberof proto.AIRichResponseMapMetadata
         * @instance
         */
        AIRichResponseMapMetadata.prototype.showInfoList = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseMapMetadata.prototype, "_centerLatitude", {
            get: $util.oneOfGetter($oneOfFields = ["centerLatitude"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseMapMetadata.prototype, "_centerLongitude", {
            get: $util.oneOfGetter($oneOfFields = ["centerLongitude"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseMapMetadata.prototype, "_latitudeDelta", {
            get: $util.oneOfGetter($oneOfFields = ["latitudeDelta"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseMapMetadata.prototype, "_longitudeDelta", {
            get: $util.oneOfGetter($oneOfFields = ["longitudeDelta"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseMapMetadata.prototype, "_showInfoList", {
            get: $util.oneOfGetter($oneOfFields = ["showInfoList"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AIRichResponseMapMetadata instance using the specified properties.
         * @function create
         * @memberof proto.AIRichResponseMapMetadata
         * @static
         * @param {proto.IAIRichResponseMapMetadata=} [properties] Properties to set
         * @returns {proto.AIRichResponseMapMetadata} AIRichResponseMapMetadata instance
         */
        AIRichResponseMapMetadata.create = function create(properties) {
            return new AIRichResponseMapMetadata(properties);
        };

        /**
         * Encodes the specified AIRichResponseMapMetadata message. Does not implicitly {@link proto.AIRichResponseMapMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.AIRichResponseMapMetadata
         * @static
         * @param {proto.IAIRichResponseMapMetadata} message AIRichResponseMapMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseMapMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.centerLatitude != null && Object.hasOwnProperty.call(message, "centerLatitude"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.centerLatitude);
            if (message.centerLongitude != null && Object.hasOwnProperty.call(message, "centerLongitude"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.centerLongitude);
            if (message.latitudeDelta != null && Object.hasOwnProperty.call(message, "latitudeDelta"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.latitudeDelta);
            if (message.longitudeDelta != null && Object.hasOwnProperty.call(message, "longitudeDelta"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.longitudeDelta);
            if (message.annotations != null && message.annotations.length)
                for (var i = 0; i < message.annotations.length; ++i)
                    $root.proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation.encode(message.annotations[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
            if (message.showInfoList != null && Object.hasOwnProperty.call(message, "showInfoList"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.showInfoList);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AIRichResponseMapMetadata message, length delimited. Does not implicitly {@link proto.AIRichResponseMapMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AIRichResponseMapMetadata
         * @static
         * @param {proto.IAIRichResponseMapMetadata} message AIRichResponseMapMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseMapMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AIRichResponseMapMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AIRichResponseMapMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AIRichResponseMapMetadata} AIRichResponseMapMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseMapMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 1)
                            break;
                        message.centerLatitude = reader.double();
                        message._centerLatitude = "centerLatitude";
                        continue;
                    }
                case 2: {
                        if (u !== 1)
                            break;
                        message.centerLongitude = reader.double();
                        message._centerLongitude = "centerLongitude";
                        continue;
                    }
                case 3: {
                        if (u !== 1)
                            break;
                        message.latitudeDelta = reader.double();
                        message._latitudeDelta = "latitudeDelta";
                        continue;
                    }
                case 4: {
                        if (u !== 1)
                            break;
                        message.longitudeDelta = reader.double();
                        message._longitudeDelta = "longitudeDelta";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        if (!(message.annotations && message.annotations.length))
                            message.annotations = [];
                        message.annotations.push($root.proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        message.showInfoList = reader.bool();
                        message._showInfoList = "showInfoList";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a AIRichResponseMapMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AIRichResponseMapMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AIRichResponseMapMetadata} AIRichResponseMapMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseMapMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AIRichResponseMapMetadata message.
         * @function verify
         * @memberof proto.AIRichResponseMapMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AIRichResponseMapMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.centerLatitude != null && Object.hasOwnProperty.call(message, "centerLatitude")) {
                properties._centerLatitude = 1;
                if (typeof message.centerLatitude !== "number")
                    return "centerLatitude: number expected";
            }
            if (message.centerLongitude != null && Object.hasOwnProperty.call(message, "centerLongitude")) {
                properties._centerLongitude = 1;
                if (typeof message.centerLongitude !== "number")
                    return "centerLongitude: number expected";
            }
            if (message.latitudeDelta != null && Object.hasOwnProperty.call(message, "latitudeDelta")) {
                properties._latitudeDelta = 1;
                if (typeof message.latitudeDelta !== "number")
                    return "latitudeDelta: number expected";
            }
            if (message.longitudeDelta != null && Object.hasOwnProperty.call(message, "longitudeDelta")) {
                properties._longitudeDelta = 1;
                if (typeof message.longitudeDelta !== "number")
                    return "longitudeDelta: number expected";
            }
            if (message.annotations != null && Object.hasOwnProperty.call(message, "annotations")) {
                if (!Array.isArray(message.annotations))
                    return "annotations: array expected";
                for (var i = 0; i < message.annotations.length; ++i) {
                    var error = $root.proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation.verify(message.annotations[i], q + 1);
                    if (error)
                        return "annotations." + error;
                }
            }
            if (message.showInfoList != null && Object.hasOwnProperty.call(message, "showInfoList")) {
                properties._showInfoList = 1;
                if (typeof message.showInfoList !== "boolean")
                    return "showInfoList: boolean expected";
            }
            return null;
        };

        /**
         * Creates a AIRichResponseMapMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AIRichResponseMapMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AIRichResponseMapMetadata} AIRichResponseMapMetadata
         */
        AIRichResponseMapMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AIRichResponseMapMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.centerLatitude != null)
                message.centerLatitude = Number(object.centerLatitude);
            if (object.centerLongitude != null)
                message.centerLongitude = Number(object.centerLongitude);
            if (object.latitudeDelta != null)
                message.latitudeDelta = Number(object.latitudeDelta);
            if (object.longitudeDelta != null)
                message.longitudeDelta = Number(object.longitudeDelta);
            if (object.annotations) {
                if (!Array.isArray(object.annotations))
                    throw TypeError(".proto.AIRichResponseMapMetadata.annotations: array expected");
                message.annotations = Array(object.annotations.length);
                for (var i = 0; i < object.annotations.length; ++i) {
                    if (!$util.isObject(object.annotations[i]))
                        throw TypeError(".proto.AIRichResponseMapMetadata.annotations: object expected");
                    message.annotations[i] = $root.proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation.fromObject(object.annotations[i], q + 1);
                }
            }
            if (object.showInfoList != null)
                message.showInfoList = Boolean(object.showInfoList);
            return message;
        };

        /**
         * Creates a plain object from a AIRichResponseMapMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AIRichResponseMapMetadata
         * @static
         * @param {proto.AIRichResponseMapMetadata} message AIRichResponseMapMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AIRichResponseMapMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.annotations = [];
            if (message.centerLatitude != null && Object.hasOwnProperty.call(message, "centerLatitude"))
                object.centerLatitude = options.json && !isFinite(message.centerLatitude) ? String(message.centerLatitude) : message.centerLatitude;
            if (message.centerLongitude != null && Object.hasOwnProperty.call(message, "centerLongitude"))
                object.centerLongitude = options.json && !isFinite(message.centerLongitude) ? String(message.centerLongitude) : message.centerLongitude;
            if (message.latitudeDelta != null && Object.hasOwnProperty.call(message, "latitudeDelta"))
                object.latitudeDelta = options.json && !isFinite(message.latitudeDelta) ? String(message.latitudeDelta) : message.latitudeDelta;
            if (message.longitudeDelta != null && Object.hasOwnProperty.call(message, "longitudeDelta"))
                object.longitudeDelta = options.json && !isFinite(message.longitudeDelta) ? String(message.longitudeDelta) : message.longitudeDelta;
            if (message.annotations && message.annotations.length) {
                object.annotations = Array(message.annotations.length);
                for (var j = 0; j < message.annotations.length; ++j)
                    object.annotations[j] = $root.proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation.toObject(message.annotations[j], options, q + 1);
            }
            if (message.showInfoList != null && Object.hasOwnProperty.call(message, "showInfoList"))
                object.showInfoList = message.showInfoList;
            return object;
        };

        /**
         * Converts this AIRichResponseMapMetadata to JSON.
         * @function toJSON
         * @memberof proto.AIRichResponseMapMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AIRichResponseMapMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AIRichResponseMapMetadata
         * @function getTypeUrl
         * @memberof proto.AIRichResponseMapMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AIRichResponseMapMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AIRichResponseMapMetadata";
        };

        AIRichResponseMapMetadata.AIRichResponseMapAnnotation = (function() {

            /**
             * Properties of a AIRichResponseMapAnnotation.
             * @memberof proto.AIRichResponseMapMetadata
             * @interface IAIRichResponseMapAnnotation
             * @property {number|null} [annotationNumber] AIRichResponseMapAnnotation annotationNumber
             * @property {number|null} [latitude] AIRichResponseMapAnnotation latitude
             * @property {number|null} [longitude] AIRichResponseMapAnnotation longitude
             * @property {string|null} [title] AIRichResponseMapAnnotation title
             * @property {string|null} [body] AIRichResponseMapAnnotation body
             */

            /**
             * Constructs a new AIRichResponseMapAnnotation.
             * @memberof proto.AIRichResponseMapMetadata
             * @classdesc Represents a AIRichResponseMapAnnotation.
             * @implements IAIRichResponseMapAnnotation
             * @constructor
             * @param {proto.AIRichResponseMapMetadata.IAIRichResponseMapAnnotation=} [properties] Properties to set
             */
            function AIRichResponseMapAnnotation(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AIRichResponseMapAnnotation annotationNumber.
             * @member {number|null|undefined} annotationNumber
             * @memberof proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation
             * @instance
             */
            AIRichResponseMapAnnotation.prototype.annotationNumber = null;

            /**
             * AIRichResponseMapAnnotation latitude.
             * @member {number|null|undefined} latitude
             * @memberof proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation
             * @instance
             */
            AIRichResponseMapAnnotation.prototype.latitude = null;

            /**
             * AIRichResponseMapAnnotation longitude.
             * @member {number|null|undefined} longitude
             * @memberof proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation
             * @instance
             */
            AIRichResponseMapAnnotation.prototype.longitude = null;

            /**
             * AIRichResponseMapAnnotation title.
             * @member {string|null|undefined} title
             * @memberof proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation
             * @instance
             */
            AIRichResponseMapAnnotation.prototype.title = null;

            /**
             * AIRichResponseMapAnnotation body.
             * @member {string|null|undefined} body
             * @memberof proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation
             * @instance
             */
            AIRichResponseMapAnnotation.prototype.body = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseMapAnnotation.prototype, "_annotationNumber", {
                get: $util.oneOfGetter($oneOfFields = ["annotationNumber"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseMapAnnotation.prototype, "_latitude", {
                get: $util.oneOfGetter($oneOfFields = ["latitude"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseMapAnnotation.prototype, "_longitude", {
                get: $util.oneOfGetter($oneOfFields = ["longitude"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseMapAnnotation.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseMapAnnotation.prototype, "_body", {
                get: $util.oneOfGetter($oneOfFields = ["body"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AIRichResponseMapAnnotation instance using the specified properties.
             * @function create
             * @memberof proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation
             * @static
             * @param {proto.AIRichResponseMapMetadata.IAIRichResponseMapAnnotation=} [properties] Properties to set
             * @returns {proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation} AIRichResponseMapAnnotation instance
             */
            AIRichResponseMapAnnotation.create = function create(properties) {
                return new AIRichResponseMapAnnotation(properties);
            };

            /**
             * Encodes the specified AIRichResponseMapAnnotation message. Does not implicitly {@link proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation.verify|verify} messages.
             * @function encode
             * @memberof proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation
             * @static
             * @param {proto.AIRichResponseMapMetadata.IAIRichResponseMapAnnotation} message AIRichResponseMapAnnotation message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AIRichResponseMapAnnotation.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.annotationNumber != null && Object.hasOwnProperty.call(message, "annotationNumber"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.annotationNumber);
                if (message.latitude != null && Object.hasOwnProperty.call(message, "latitude"))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.latitude);
                if (message.longitude != null && Object.hasOwnProperty.call(message, "longitude"))
                    writer.uint32(/* id 3, wireType 1 =*/25).double(message.longitude);
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.title);
                if (message.body != null && Object.hasOwnProperty.call(message, "body"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.body);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AIRichResponseMapAnnotation message, length delimited. Does not implicitly {@link proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation
             * @static
             * @param {proto.AIRichResponseMapMetadata.IAIRichResponseMapAnnotation} message AIRichResponseMapAnnotation message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AIRichResponseMapAnnotation.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a AIRichResponseMapAnnotation message from the specified reader or buffer.
             * @function decode
             * @memberof proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation} AIRichResponseMapAnnotation
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AIRichResponseMapAnnotation.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.annotationNumber = reader.uint32();
                            message._annotationNumber = "annotationNumber";
                            continue;
                        }
                    case 2: {
                            if (u !== 1)
                                break;
                            message.latitude = reader.double();
                            message._latitude = "latitude";
                            continue;
                        }
                    case 3: {
                            if (u !== 1)
                                break;
                            message.longitude = reader.double();
                            message._longitude = "longitude";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.title = reader.stringVerify();
                            message._title = "title";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.body = reader.stringVerify();
                            message._body = "body";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a AIRichResponseMapAnnotation message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation} AIRichResponseMapAnnotation
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AIRichResponseMapAnnotation.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a AIRichResponseMapAnnotation message.
             * @function verify
             * @memberof proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AIRichResponseMapAnnotation.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.annotationNumber != null && Object.hasOwnProperty.call(message, "annotationNumber")) {
                    properties._annotationNumber = 1;
                    if (!$util.isInteger(message.annotationNumber))
                        return "annotationNumber: integer expected";
                }
                if (message.latitude != null && Object.hasOwnProperty.call(message, "latitude")) {
                    properties._latitude = 1;
                    if (typeof message.latitude !== "number")
                        return "latitude: number expected";
                }
                if (message.longitude != null && Object.hasOwnProperty.call(message, "longitude")) {
                    properties._longitude = 1;
                    if (typeof message.longitude !== "number")
                        return "longitude: number expected";
                }
                if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                    properties._title = 1;
                    if (!$util.isString(message.title))
                        return "title: string expected";
                }
                if (message.body != null && Object.hasOwnProperty.call(message, "body")) {
                    properties._body = 1;
                    if (!$util.isString(message.body))
                        return "body: string expected";
                }
                return null;
            };

            /**
             * Creates a AIRichResponseMapAnnotation message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation} AIRichResponseMapAnnotation
             */
            AIRichResponseMapAnnotation.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.annotationNumber != null)
                    message.annotationNumber = object.annotationNumber >>> 0;
                if (object.latitude != null)
                    message.latitude = Number(object.latitude);
                if (object.longitude != null)
                    message.longitude = Number(object.longitude);
                if (object.title != null)
                    message.title = String(object.title);
                if (object.body != null)
                    message.body = String(object.body);
                return message;
            };

            /**
             * Creates a plain object from a AIRichResponseMapAnnotation message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation
             * @static
             * @param {proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation} message AIRichResponseMapAnnotation
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AIRichResponseMapAnnotation.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.annotationNumber != null && Object.hasOwnProperty.call(message, "annotationNumber"))
                    object.annotationNumber = message.annotationNumber;
                if (message.latitude != null && Object.hasOwnProperty.call(message, "latitude"))
                    object.latitude = options.json && !isFinite(message.latitude) ? String(message.latitude) : message.latitude;
                if (message.longitude != null && Object.hasOwnProperty.call(message, "longitude"))
                    object.longitude = options.json && !isFinite(message.longitude) ? String(message.longitude) : message.longitude;
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    object.title = message.title;
                if (message.body != null && Object.hasOwnProperty.call(message, "body"))
                    object.body = message.body;
                return object;
            };

            /**
             * Converts this AIRichResponseMapAnnotation to JSON.
             * @function toJSON
             * @memberof proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AIRichResponseMapAnnotation.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AIRichResponseMapAnnotation
             * @function getTypeUrl
             * @memberof proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AIRichResponseMapAnnotation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation";
            };

            return AIRichResponseMapAnnotation;
        })();

        return AIRichResponseMapMetadata;
    })();

    proto.AIRichResponseMessage = (function() {

        /**
         * Properties of a AIRichResponseMessage.
         * @memberof proto
         * @interface IAIRichResponseMessage
         * @property {proto.AIRichResponseMessageType|null} [messageType] AIRichResponseMessage messageType
         * @property {Array.<proto.IAIRichResponseSubMessage>|null} [submessages] AIRichResponseMessage submessages
         * @property {proto.IAIRichResponseUnifiedResponse|null} [unifiedResponse] AIRichResponseMessage unifiedResponse
         * @property {proto.IContextInfo|null} [contextInfo] AIRichResponseMessage contextInfo
         */

        /**
         * Constructs a new AIRichResponseMessage.
         * @memberof proto
         * @classdesc Represents a AIRichResponseMessage.
         * @implements IAIRichResponseMessage
         * @constructor
         * @param {proto.IAIRichResponseMessage=} [properties] Properties to set
         */
        function AIRichResponseMessage(properties) {
            this.submessages = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AIRichResponseMessage messageType.
         * @member {proto.AIRichResponseMessageType|null|undefined} messageType
         * @memberof proto.AIRichResponseMessage
         * @instance
         */
        AIRichResponseMessage.prototype.messageType = null;

        /**
         * AIRichResponseMessage submessages.
         * @member {Array.<proto.IAIRichResponseSubMessage>} submessages
         * @memberof proto.AIRichResponseMessage
         * @instance
         */
        AIRichResponseMessage.prototype.submessages = $util.emptyArray;

        /**
         * AIRichResponseMessage unifiedResponse.
         * @member {proto.IAIRichResponseUnifiedResponse|null|undefined} unifiedResponse
         * @memberof proto.AIRichResponseMessage
         * @instance
         */
        AIRichResponseMessage.prototype.unifiedResponse = null;

        /**
         * AIRichResponseMessage contextInfo.
         * @member {proto.IContextInfo|null|undefined} contextInfo
         * @memberof proto.AIRichResponseMessage
         * @instance
         */
        AIRichResponseMessage.prototype.contextInfo = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseMessage.prototype, "_messageType", {
            get: $util.oneOfGetter($oneOfFields = ["messageType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseMessage.prototype, "_unifiedResponse", {
            get: $util.oneOfGetter($oneOfFields = ["unifiedResponse"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AIRichResponseMessage instance using the specified properties.
         * @function create
         * @memberof proto.AIRichResponseMessage
         * @static
         * @param {proto.IAIRichResponseMessage=} [properties] Properties to set
         * @returns {proto.AIRichResponseMessage} AIRichResponseMessage instance
         */
        AIRichResponseMessage.create = function create(properties) {
            return new AIRichResponseMessage(properties);
        };

        /**
         * Encodes the specified AIRichResponseMessage message. Does not implicitly {@link proto.AIRichResponseMessage.verify|verify} messages.
         * @function encode
         * @memberof proto.AIRichResponseMessage
         * @static
         * @param {proto.IAIRichResponseMessage} message AIRichResponseMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseMessage.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.messageType != null && Object.hasOwnProperty.call(message, "messageType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.messageType);
            if (message.submessages != null && message.submessages.length)
                for (var i = 0; i < message.submessages.length; ++i)
                    $root.proto.AIRichResponseSubMessage.encode(message.submessages[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.unifiedResponse != null && Object.hasOwnProperty.call(message, "unifiedResponse"))
                $root.proto.AIRichResponseUnifiedResponse.encode(message.unifiedResponse, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AIRichResponseMessage message, length delimited. Does not implicitly {@link proto.AIRichResponseMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AIRichResponseMessage
         * @static
         * @param {proto.IAIRichResponseMessage} message AIRichResponseMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AIRichResponseMessage message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AIRichResponseMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AIRichResponseMessage} AIRichResponseMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseMessage.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.messageType = reader.int32();
                        message._messageType = "messageType";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(message.submessages && message.submessages.length))
                            message.submessages = [];
                        message.submessages.push($root.proto.AIRichResponseSubMessage.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.unifiedResponse = $root.proto.AIRichResponseUnifiedResponse.decode(reader, reader.uint32(), undefined, q + 1, message.unifiedResponse);
                        message._unifiedResponse = "unifiedResponse";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                        message._contextInfo = "contextInfo";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a AIRichResponseMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AIRichResponseMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AIRichResponseMessage} AIRichResponseMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AIRichResponseMessage message.
         * @function verify
         * @memberof proto.AIRichResponseMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AIRichResponseMessage.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.messageType != null && Object.hasOwnProperty.call(message, "messageType")) {
                properties._messageType = 1;
                if (typeof message.messageType !== "number" || (message.messageType | 0) !== message.messageType)
                    return "messageType: enum value expected";
            }
            if (message.submessages != null && Object.hasOwnProperty.call(message, "submessages")) {
                if (!Array.isArray(message.submessages))
                    return "submessages: array expected";
                for (var i = 0; i < message.submessages.length; ++i) {
                    var error = $root.proto.AIRichResponseSubMessage.verify(message.submessages[i], q + 1);
                    if (error)
                        return "submessages." + error;
                }
            }
            if (message.unifiedResponse != null && Object.hasOwnProperty.call(message, "unifiedResponse")) {
                properties._unifiedResponse = 1;
                {
                    var error = $root.proto.AIRichResponseUnifiedResponse.verify(message.unifiedResponse, q + 1);
                    if (error)
                        return "unifiedResponse." + error;
                }
            }
            if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                properties._contextInfo = 1;
                {
                    var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                    if (error)
                        return "contextInfo." + error;
                }
            }
            return null;
        };

        /**
         * Creates a AIRichResponseMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AIRichResponseMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AIRichResponseMessage} AIRichResponseMessage
         */
        AIRichResponseMessage.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AIRichResponseMessage: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.messageType) {
            case "AI_RICH_RESPONSE_TYPE_UNKNOWN":
            case 0:
                message.messageType = 0;
                break;
            case "AI_RICH_RESPONSE_TYPE_STANDARD":
            case 1:
                message.messageType = 1;
                break;
            default:
                if (typeof object.messageType === "number" && (object.messageType | 0) === object.messageType)
                    message.messageType = object.messageType;
            }
            if (object.submessages) {
                if (!Array.isArray(object.submessages))
                    throw TypeError(".proto.AIRichResponseMessage.submessages: array expected");
                message.submessages = Array(object.submessages.length);
                for (var i = 0; i < object.submessages.length; ++i) {
                    if (!$util.isObject(object.submessages[i]))
                        throw TypeError(".proto.AIRichResponseMessage.submessages: object expected");
                    message.submessages[i] = $root.proto.AIRichResponseSubMessage.fromObject(object.submessages[i], q + 1);
                }
            }
            if (object.unifiedResponse != null) {
                if (!$util.isObject(object.unifiedResponse))
                    throw TypeError(".proto.AIRichResponseMessage.unifiedResponse: object expected");
                message.unifiedResponse = $root.proto.AIRichResponseUnifiedResponse.fromObject(object.unifiedResponse, q + 1);
            }
            if (object.contextInfo != null) {
                if (!$util.isObject(object.contextInfo))
                    throw TypeError(".proto.AIRichResponseMessage.contextInfo: object expected");
                message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a AIRichResponseMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AIRichResponseMessage
         * @static
         * @param {proto.AIRichResponseMessage} message AIRichResponseMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AIRichResponseMessage.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.submessages = [];
            if (message.messageType != null && Object.hasOwnProperty.call(message, "messageType"))
                object.messageType = options.enums === String ? $root.proto.AIRichResponseMessageType[message.messageType] === undefined ? message.messageType : $root.proto.AIRichResponseMessageType[message.messageType] : message.messageType;
            if (message.submessages && message.submessages.length) {
                object.submessages = Array(message.submessages.length);
                for (var j = 0; j < message.submessages.length; ++j)
                    object.submessages[j] = $root.proto.AIRichResponseSubMessage.toObject(message.submessages[j], options, q + 1);
            }
            if (message.unifiedResponse != null && Object.hasOwnProperty.call(message, "unifiedResponse"))
                object.unifiedResponse = $root.proto.AIRichResponseUnifiedResponse.toObject(message.unifiedResponse, options, q + 1);
            if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
            return object;
        };

        /**
         * Converts this AIRichResponseMessage to JSON.
         * @function toJSON
         * @memberof proto.AIRichResponseMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AIRichResponseMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AIRichResponseMessage
         * @function getTypeUrl
         * @memberof proto.AIRichResponseMessage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AIRichResponseMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AIRichResponseMessage";
        };

        return AIRichResponseMessage;
    })();

    /**
     * AIRichResponseMessageType enum.
     * @name proto.AIRichResponseMessageType
     * @enum {number}
     * @property {number} AI_RICH_RESPONSE_TYPE_UNKNOWN=0 AI_RICH_RESPONSE_TYPE_UNKNOWN value
     * @property {number} AI_RICH_RESPONSE_TYPE_STANDARD=1 AI_RICH_RESPONSE_TYPE_STANDARD value
     */
    proto.AIRichResponseMessageType = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "AI_RICH_RESPONSE_TYPE_UNKNOWN"] = 0;
        values[valuesById[1] = "AI_RICH_RESPONSE_TYPE_STANDARD"] = 1;
        return values;
    })();

    proto.AIRichResponseSubMessage = (function() {

        /**
         * Properties of a AIRichResponseSubMessage.
         * @memberof proto
         * @interface IAIRichResponseSubMessage
         * @property {proto.AIRichResponseSubMessageType|null} [messageType] AIRichResponseSubMessage messageType
         * @property {proto.IAIRichResponseGridImageMetadata|null} [gridImageMetadata] AIRichResponseSubMessage gridImageMetadata
         * @property {string|null} [messageText] AIRichResponseSubMessage messageText
         * @property {proto.IAIRichResponseInlineImageMetadata|null} [imageMetadata] AIRichResponseSubMessage imageMetadata
         * @property {proto.IAIRichResponseCodeMetadata|null} [codeMetadata] AIRichResponseSubMessage codeMetadata
         * @property {proto.IAIRichResponseTableMetadata|null} [tableMetadata] AIRichResponseSubMessage tableMetadata
         * @property {proto.IAIRichResponseDynamicMetadata|null} [dynamicMetadata] AIRichResponseSubMessage dynamicMetadata
         * @property {proto.IAIRichResponseLatexMetadata|null} [latexMetadata] AIRichResponseSubMessage latexMetadata
         * @property {proto.IAIRichResponseMapMetadata|null} [mapMetadata] AIRichResponseSubMessage mapMetadata
         * @property {proto.IAIRichResponseContentItemsMetadata|null} [contentItemsMetadata] AIRichResponseSubMessage contentItemsMetadata
         */

        /**
         * Constructs a new AIRichResponseSubMessage.
         * @memberof proto
         * @classdesc Represents a AIRichResponseSubMessage.
         * @implements IAIRichResponseSubMessage
         * @constructor
         * @param {proto.IAIRichResponseSubMessage=} [properties] Properties to set
         */
        function AIRichResponseSubMessage(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AIRichResponseSubMessage messageType.
         * @member {proto.AIRichResponseSubMessageType|null|undefined} messageType
         * @memberof proto.AIRichResponseSubMessage
         * @instance
         */
        AIRichResponseSubMessage.prototype.messageType = null;

        /**
         * AIRichResponseSubMessage gridImageMetadata.
         * @member {proto.IAIRichResponseGridImageMetadata|null|undefined} gridImageMetadata
         * @memberof proto.AIRichResponseSubMessage
         * @instance
         */
        AIRichResponseSubMessage.prototype.gridImageMetadata = null;

        /**
         * AIRichResponseSubMessage messageText.
         * @member {string|null|undefined} messageText
         * @memberof proto.AIRichResponseSubMessage
         * @instance
         */
        AIRichResponseSubMessage.prototype.messageText = null;

        /**
         * AIRichResponseSubMessage imageMetadata.
         * @member {proto.IAIRichResponseInlineImageMetadata|null|undefined} imageMetadata
         * @memberof proto.AIRichResponseSubMessage
         * @instance
         */
        AIRichResponseSubMessage.prototype.imageMetadata = null;

        /**
         * AIRichResponseSubMessage codeMetadata.
         * @member {proto.IAIRichResponseCodeMetadata|null|undefined} codeMetadata
         * @memberof proto.AIRichResponseSubMessage
         * @instance
         */
        AIRichResponseSubMessage.prototype.codeMetadata = null;

        /**
         * AIRichResponseSubMessage tableMetadata.
         * @member {proto.IAIRichResponseTableMetadata|null|undefined} tableMetadata
         * @memberof proto.AIRichResponseSubMessage
         * @instance
         */
        AIRichResponseSubMessage.prototype.tableMetadata = null;

        /**
         * AIRichResponseSubMessage dynamicMetadata.
         * @member {proto.IAIRichResponseDynamicMetadata|null|undefined} dynamicMetadata
         * @memberof proto.AIRichResponseSubMessage
         * @instance
         */
        AIRichResponseSubMessage.prototype.dynamicMetadata = null;

        /**
         * AIRichResponseSubMessage latexMetadata.
         * @member {proto.IAIRichResponseLatexMetadata|null|undefined} latexMetadata
         * @memberof proto.AIRichResponseSubMessage
         * @instance
         */
        AIRichResponseSubMessage.prototype.latexMetadata = null;

        /**
         * AIRichResponseSubMessage mapMetadata.
         * @member {proto.IAIRichResponseMapMetadata|null|undefined} mapMetadata
         * @memberof proto.AIRichResponseSubMessage
         * @instance
         */
        AIRichResponseSubMessage.prototype.mapMetadata = null;

        /**
         * AIRichResponseSubMessage contentItemsMetadata.
         * @member {proto.IAIRichResponseContentItemsMetadata|null|undefined} contentItemsMetadata
         * @memberof proto.AIRichResponseSubMessage
         * @instance
         */
        AIRichResponseSubMessage.prototype.contentItemsMetadata = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseSubMessage.prototype, "_messageType", {
            get: $util.oneOfGetter($oneOfFields = ["messageType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseSubMessage.prototype, "_gridImageMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["gridImageMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseSubMessage.prototype, "_messageText", {
            get: $util.oneOfGetter($oneOfFields = ["messageText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseSubMessage.prototype, "_imageMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["imageMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseSubMessage.prototype, "_codeMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["codeMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseSubMessage.prototype, "_tableMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["tableMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseSubMessage.prototype, "_dynamicMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["dynamicMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseSubMessage.prototype, "_latexMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["latexMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseSubMessage.prototype, "_mapMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["mapMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseSubMessage.prototype, "_contentItemsMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["contentItemsMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AIRichResponseSubMessage instance using the specified properties.
         * @function create
         * @memberof proto.AIRichResponseSubMessage
         * @static
         * @param {proto.IAIRichResponseSubMessage=} [properties] Properties to set
         * @returns {proto.AIRichResponseSubMessage} AIRichResponseSubMessage instance
         */
        AIRichResponseSubMessage.create = function create(properties) {
            return new AIRichResponseSubMessage(properties);
        };

        /**
         * Encodes the specified AIRichResponseSubMessage message. Does not implicitly {@link proto.AIRichResponseSubMessage.verify|verify} messages.
         * @function encode
         * @memberof proto.AIRichResponseSubMessage
         * @static
         * @param {proto.IAIRichResponseSubMessage} message AIRichResponseSubMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseSubMessage.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.messageType != null && Object.hasOwnProperty.call(message, "messageType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.messageType);
            if (message.gridImageMetadata != null && Object.hasOwnProperty.call(message, "gridImageMetadata"))
                $root.proto.AIRichResponseGridImageMetadata.encode(message.gridImageMetadata, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.messageText != null && Object.hasOwnProperty.call(message, "messageText"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.messageText);
            if (message.imageMetadata != null && Object.hasOwnProperty.call(message, "imageMetadata"))
                $root.proto.AIRichResponseInlineImageMetadata.encode(message.imageMetadata, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.codeMetadata != null && Object.hasOwnProperty.call(message, "codeMetadata"))
                $root.proto.AIRichResponseCodeMetadata.encode(message.codeMetadata, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
            if (message.tableMetadata != null && Object.hasOwnProperty.call(message, "tableMetadata"))
                $root.proto.AIRichResponseTableMetadata.encode(message.tableMetadata, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
            if (message.dynamicMetadata != null && Object.hasOwnProperty.call(message, "dynamicMetadata"))
                $root.proto.AIRichResponseDynamicMetadata.encode(message.dynamicMetadata, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
            if (message.latexMetadata != null && Object.hasOwnProperty.call(message, "latexMetadata"))
                $root.proto.AIRichResponseLatexMetadata.encode(message.latexMetadata, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
            if (message.mapMetadata != null && Object.hasOwnProperty.call(message, "mapMetadata"))
                $root.proto.AIRichResponseMapMetadata.encode(message.mapMetadata, writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
            if (message.contentItemsMetadata != null && Object.hasOwnProperty.call(message, "contentItemsMetadata"))
                $root.proto.AIRichResponseContentItemsMetadata.encode(message.contentItemsMetadata, writer.uint32(/* id 10, wireType 2 =*/82).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AIRichResponseSubMessage message, length delimited. Does not implicitly {@link proto.AIRichResponseSubMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AIRichResponseSubMessage
         * @static
         * @param {proto.IAIRichResponseSubMessage} message AIRichResponseSubMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseSubMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AIRichResponseSubMessage message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AIRichResponseSubMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AIRichResponseSubMessage} AIRichResponseSubMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseSubMessage.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.messageType = reader.int32();
                        message._messageType = "messageType";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.gridImageMetadata = $root.proto.AIRichResponseGridImageMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.gridImageMetadata);
                        message._gridImageMetadata = "gridImageMetadata";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.messageText = reader.stringVerify();
                        message._messageText = "messageText";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.imageMetadata = $root.proto.AIRichResponseInlineImageMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.imageMetadata);
                        message._imageMetadata = "imageMetadata";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.codeMetadata = $root.proto.AIRichResponseCodeMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.codeMetadata);
                        message._codeMetadata = "codeMetadata";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        message.tableMetadata = $root.proto.AIRichResponseTableMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.tableMetadata);
                        message._tableMetadata = "tableMetadata";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        message.dynamicMetadata = $root.proto.AIRichResponseDynamicMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.dynamicMetadata);
                        message._dynamicMetadata = "dynamicMetadata";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        message.latexMetadata = $root.proto.AIRichResponseLatexMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.latexMetadata);
                        message._latexMetadata = "latexMetadata";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        message.mapMetadata = $root.proto.AIRichResponseMapMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.mapMetadata);
                        message._mapMetadata = "mapMetadata";
                        continue;
                    }
                case 10: {
                        if (u !== 2)
                            break;
                        message.contentItemsMetadata = $root.proto.AIRichResponseContentItemsMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.contentItemsMetadata);
                        message._contentItemsMetadata = "contentItemsMetadata";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a AIRichResponseSubMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AIRichResponseSubMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AIRichResponseSubMessage} AIRichResponseSubMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseSubMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AIRichResponseSubMessage message.
         * @function verify
         * @memberof proto.AIRichResponseSubMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AIRichResponseSubMessage.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.messageType != null && Object.hasOwnProperty.call(message, "messageType")) {
                properties._messageType = 1;
                if (typeof message.messageType !== "number" || (message.messageType | 0) !== message.messageType)
                    return "messageType: enum value expected";
            }
            if (message.gridImageMetadata != null && Object.hasOwnProperty.call(message, "gridImageMetadata")) {
                properties._gridImageMetadata = 1;
                {
                    var error = $root.proto.AIRichResponseGridImageMetadata.verify(message.gridImageMetadata, q + 1);
                    if (error)
                        return "gridImageMetadata." + error;
                }
            }
            if (message.messageText != null && Object.hasOwnProperty.call(message, "messageText")) {
                properties._messageText = 1;
                if (!$util.isString(message.messageText))
                    return "messageText: string expected";
            }
            if (message.imageMetadata != null && Object.hasOwnProperty.call(message, "imageMetadata")) {
                properties._imageMetadata = 1;
                {
                    var error = $root.proto.AIRichResponseInlineImageMetadata.verify(message.imageMetadata, q + 1);
                    if (error)
                        return "imageMetadata." + error;
                }
            }
            if (message.codeMetadata != null && Object.hasOwnProperty.call(message, "codeMetadata")) {
                properties._codeMetadata = 1;
                {
                    var error = $root.proto.AIRichResponseCodeMetadata.verify(message.codeMetadata, q + 1);
                    if (error)
                        return "codeMetadata." + error;
                }
            }
            if (message.tableMetadata != null && Object.hasOwnProperty.call(message, "tableMetadata")) {
                properties._tableMetadata = 1;
                {
                    var error = $root.proto.AIRichResponseTableMetadata.verify(message.tableMetadata, q + 1);
                    if (error)
                        return "tableMetadata." + error;
                }
            }
            if (message.dynamicMetadata != null && Object.hasOwnProperty.call(message, "dynamicMetadata")) {
                properties._dynamicMetadata = 1;
                {
                    var error = $root.proto.AIRichResponseDynamicMetadata.verify(message.dynamicMetadata, q + 1);
                    if (error)
                        return "dynamicMetadata." + error;
                }
            }
            if (message.latexMetadata != null && Object.hasOwnProperty.call(message, "latexMetadata")) {
                properties._latexMetadata = 1;
                {
                    var error = $root.proto.AIRichResponseLatexMetadata.verify(message.latexMetadata, q + 1);
                    if (error)
                        return "latexMetadata." + error;
                }
            }
            if (message.mapMetadata != null && Object.hasOwnProperty.call(message, "mapMetadata")) {
                properties._mapMetadata = 1;
                {
                    var error = $root.proto.AIRichResponseMapMetadata.verify(message.mapMetadata, q + 1);
                    if (error)
                        return "mapMetadata." + error;
                }
            }
            if (message.contentItemsMetadata != null && Object.hasOwnProperty.call(message, "contentItemsMetadata")) {
                properties._contentItemsMetadata = 1;
                {
                    var error = $root.proto.AIRichResponseContentItemsMetadata.verify(message.contentItemsMetadata, q + 1);
                    if (error)
                        return "contentItemsMetadata." + error;
                }
            }
            return null;
        };

        /**
         * Creates a AIRichResponseSubMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AIRichResponseSubMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AIRichResponseSubMessage} AIRichResponseSubMessage
         */
        AIRichResponseSubMessage.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AIRichResponseSubMessage: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.messageType) {
            case "AI_RICH_RESPONSE_UNKNOWN":
            case 0:
                message.messageType = 0;
                break;
            case "AI_RICH_RESPONSE_GRID_IMAGE":
            case 1:
                message.messageType = 1;
                break;
            case "AI_RICH_RESPONSE_TEXT":
            case 2:
                message.messageType = 2;
                break;
            case "AI_RICH_RESPONSE_INLINE_IMAGE":
            case 3:
                message.messageType = 3;
                break;
            case "AI_RICH_RESPONSE_TABLE":
            case 4:
                message.messageType = 4;
                break;
            case "AI_RICH_RESPONSE_CODE":
            case 5:
                message.messageType = 5;
                break;
            case "AI_RICH_RESPONSE_DYNAMIC":
            case 6:
                message.messageType = 6;
                break;
            case "AI_RICH_RESPONSE_MAP":
            case 7:
                message.messageType = 7;
                break;
            case "AI_RICH_RESPONSE_LATEX":
            case 8:
                message.messageType = 8;
                break;
            case "AI_RICH_RESPONSE_CONTENT_ITEMS":
            case 9:
                message.messageType = 9;
                break;
            default:
                if (typeof object.messageType === "number" && (object.messageType | 0) === object.messageType)
                    message.messageType = object.messageType;
            }
            if (object.gridImageMetadata != null) {
                if (!$util.isObject(object.gridImageMetadata))
                    throw TypeError(".proto.AIRichResponseSubMessage.gridImageMetadata: object expected");
                message.gridImageMetadata = $root.proto.AIRichResponseGridImageMetadata.fromObject(object.gridImageMetadata, q + 1);
            }
            if (object.messageText != null)
                message.messageText = String(object.messageText);
            if (object.imageMetadata != null) {
                if (!$util.isObject(object.imageMetadata))
                    throw TypeError(".proto.AIRichResponseSubMessage.imageMetadata: object expected");
                message.imageMetadata = $root.proto.AIRichResponseInlineImageMetadata.fromObject(object.imageMetadata, q + 1);
            }
            if (object.codeMetadata != null) {
                if (!$util.isObject(object.codeMetadata))
                    throw TypeError(".proto.AIRichResponseSubMessage.codeMetadata: object expected");
                message.codeMetadata = $root.proto.AIRichResponseCodeMetadata.fromObject(object.codeMetadata, q + 1);
            }
            if (object.tableMetadata != null) {
                if (!$util.isObject(object.tableMetadata))
                    throw TypeError(".proto.AIRichResponseSubMessage.tableMetadata: object expected");
                message.tableMetadata = $root.proto.AIRichResponseTableMetadata.fromObject(object.tableMetadata, q + 1);
            }
            if (object.dynamicMetadata != null) {
                if (!$util.isObject(object.dynamicMetadata))
                    throw TypeError(".proto.AIRichResponseSubMessage.dynamicMetadata: object expected");
                message.dynamicMetadata = $root.proto.AIRichResponseDynamicMetadata.fromObject(object.dynamicMetadata, q + 1);
            }
            if (object.latexMetadata != null) {
                if (!$util.isObject(object.latexMetadata))
                    throw TypeError(".proto.AIRichResponseSubMessage.latexMetadata: object expected");
                message.latexMetadata = $root.proto.AIRichResponseLatexMetadata.fromObject(object.latexMetadata, q + 1);
            }
            if (object.mapMetadata != null) {
                if (!$util.isObject(object.mapMetadata))
                    throw TypeError(".proto.AIRichResponseSubMessage.mapMetadata: object expected");
                message.mapMetadata = $root.proto.AIRichResponseMapMetadata.fromObject(object.mapMetadata, q + 1);
            }
            if (object.contentItemsMetadata != null) {
                if (!$util.isObject(object.contentItemsMetadata))
                    throw TypeError(".proto.AIRichResponseSubMessage.contentItemsMetadata: object expected");
                message.contentItemsMetadata = $root.proto.AIRichResponseContentItemsMetadata.fromObject(object.contentItemsMetadata, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a AIRichResponseSubMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AIRichResponseSubMessage
         * @static
         * @param {proto.AIRichResponseSubMessage} message AIRichResponseSubMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AIRichResponseSubMessage.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.messageType != null && Object.hasOwnProperty.call(message, "messageType"))
                object.messageType = options.enums === String ? $root.proto.AIRichResponseSubMessageType[message.messageType] === undefined ? message.messageType : $root.proto.AIRichResponseSubMessageType[message.messageType] : message.messageType;
            if (message.gridImageMetadata != null && Object.hasOwnProperty.call(message, "gridImageMetadata"))
                object.gridImageMetadata = $root.proto.AIRichResponseGridImageMetadata.toObject(message.gridImageMetadata, options, q + 1);
            if (message.messageText != null && Object.hasOwnProperty.call(message, "messageText"))
                object.messageText = message.messageText;
            if (message.imageMetadata != null && Object.hasOwnProperty.call(message, "imageMetadata"))
                object.imageMetadata = $root.proto.AIRichResponseInlineImageMetadata.toObject(message.imageMetadata, options, q + 1);
            if (message.codeMetadata != null && Object.hasOwnProperty.call(message, "codeMetadata"))
                object.codeMetadata = $root.proto.AIRichResponseCodeMetadata.toObject(message.codeMetadata, options, q + 1);
            if (message.tableMetadata != null && Object.hasOwnProperty.call(message, "tableMetadata"))
                object.tableMetadata = $root.proto.AIRichResponseTableMetadata.toObject(message.tableMetadata, options, q + 1);
            if (message.dynamicMetadata != null && Object.hasOwnProperty.call(message, "dynamicMetadata"))
                object.dynamicMetadata = $root.proto.AIRichResponseDynamicMetadata.toObject(message.dynamicMetadata, options, q + 1);
            if (message.latexMetadata != null && Object.hasOwnProperty.call(message, "latexMetadata"))
                object.latexMetadata = $root.proto.AIRichResponseLatexMetadata.toObject(message.latexMetadata, options, q + 1);
            if (message.mapMetadata != null && Object.hasOwnProperty.call(message, "mapMetadata"))
                object.mapMetadata = $root.proto.AIRichResponseMapMetadata.toObject(message.mapMetadata, options, q + 1);
            if (message.contentItemsMetadata != null && Object.hasOwnProperty.call(message, "contentItemsMetadata"))
                object.contentItemsMetadata = $root.proto.AIRichResponseContentItemsMetadata.toObject(message.contentItemsMetadata, options, q + 1);
            return object;
        };

        /**
         * Converts this AIRichResponseSubMessage to JSON.
         * @function toJSON
         * @memberof proto.AIRichResponseSubMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AIRichResponseSubMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AIRichResponseSubMessage
         * @function getTypeUrl
         * @memberof proto.AIRichResponseSubMessage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AIRichResponseSubMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AIRichResponseSubMessage";
        };

        return AIRichResponseSubMessage;
    })();

    /**
     * AIRichResponseSubMessageType enum.
     * @name proto.AIRichResponseSubMessageType
     * @enum {number}
     * @property {number} AI_RICH_RESPONSE_UNKNOWN=0 AI_RICH_RESPONSE_UNKNOWN value
     * @property {number} AI_RICH_RESPONSE_GRID_IMAGE=1 AI_RICH_RESPONSE_GRID_IMAGE value
     * @property {number} AI_RICH_RESPONSE_TEXT=2 AI_RICH_RESPONSE_TEXT value
     * @property {number} AI_RICH_RESPONSE_INLINE_IMAGE=3 AI_RICH_RESPONSE_INLINE_IMAGE value
     * @property {number} AI_RICH_RESPONSE_TABLE=4 AI_RICH_RESPONSE_TABLE value
     * @property {number} AI_RICH_RESPONSE_CODE=5 AI_RICH_RESPONSE_CODE value
     * @property {number} AI_RICH_RESPONSE_DYNAMIC=6 AI_RICH_RESPONSE_DYNAMIC value
     * @property {number} AI_RICH_RESPONSE_MAP=7 AI_RICH_RESPONSE_MAP value
     * @property {number} AI_RICH_RESPONSE_LATEX=8 AI_RICH_RESPONSE_LATEX value
     * @property {number} AI_RICH_RESPONSE_CONTENT_ITEMS=9 AI_RICH_RESPONSE_CONTENT_ITEMS value
     */
    proto.AIRichResponseSubMessageType = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "AI_RICH_RESPONSE_UNKNOWN"] = 0;
        values[valuesById[1] = "AI_RICH_RESPONSE_GRID_IMAGE"] = 1;
        values[valuesById[2] = "AI_RICH_RESPONSE_TEXT"] = 2;
        values[valuesById[3] = "AI_RICH_RESPONSE_INLINE_IMAGE"] = 3;
        values[valuesById[4] = "AI_RICH_RESPONSE_TABLE"] = 4;
        values[valuesById[5] = "AI_RICH_RESPONSE_CODE"] = 5;
        values[valuesById[6] = "AI_RICH_RESPONSE_DYNAMIC"] = 6;
        values[valuesById[7] = "AI_RICH_RESPONSE_MAP"] = 7;
        values[valuesById[8] = "AI_RICH_RESPONSE_LATEX"] = 8;
        values[valuesById[9] = "AI_RICH_RESPONSE_CONTENT_ITEMS"] = 9;
        return values;
    })();

    proto.AIRichResponseTableMetadata = (function() {

        /**
         * Properties of a AIRichResponseTableMetadata.
         * @memberof proto
         * @interface IAIRichResponseTableMetadata
         * @property {Array.<proto.AIRichResponseTableMetadata.IAIRichResponseTableRow>|null} [rows] AIRichResponseTableMetadata rows
         * @property {string|null} [title] AIRichResponseTableMetadata title
         */

        /**
         * Constructs a new AIRichResponseTableMetadata.
         * @memberof proto
         * @classdesc Represents a AIRichResponseTableMetadata.
         * @implements IAIRichResponseTableMetadata
         * @constructor
         * @param {proto.IAIRichResponseTableMetadata=} [properties] Properties to set
         */
        function AIRichResponseTableMetadata(properties) {
            this.rows = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AIRichResponseTableMetadata rows.
         * @member {Array.<proto.AIRichResponseTableMetadata.IAIRichResponseTableRow>} rows
         * @memberof proto.AIRichResponseTableMetadata
         * @instance
         */
        AIRichResponseTableMetadata.prototype.rows = $util.emptyArray;

        /**
         * AIRichResponseTableMetadata title.
         * @member {string|null|undefined} title
         * @memberof proto.AIRichResponseTableMetadata
         * @instance
         */
        AIRichResponseTableMetadata.prototype.title = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseTableMetadata.prototype, "_title", {
            get: $util.oneOfGetter($oneOfFields = ["title"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AIRichResponseTableMetadata instance using the specified properties.
         * @function create
         * @memberof proto.AIRichResponseTableMetadata
         * @static
         * @param {proto.IAIRichResponseTableMetadata=} [properties] Properties to set
         * @returns {proto.AIRichResponseTableMetadata} AIRichResponseTableMetadata instance
         */
        AIRichResponseTableMetadata.create = function create(properties) {
            return new AIRichResponseTableMetadata(properties);
        };

        /**
         * Encodes the specified AIRichResponseTableMetadata message. Does not implicitly {@link proto.AIRichResponseTableMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.AIRichResponseTableMetadata
         * @static
         * @param {proto.IAIRichResponseTableMetadata} message AIRichResponseTableMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseTableMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.rows != null && message.rows.length)
                for (var i = 0; i < message.rows.length; ++i)
                    $root.proto.AIRichResponseTableMetadata.AIRichResponseTableRow.encode(message.rows[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.title);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AIRichResponseTableMetadata message, length delimited. Does not implicitly {@link proto.AIRichResponseTableMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AIRichResponseTableMetadata
         * @static
         * @param {proto.IAIRichResponseTableMetadata} message AIRichResponseTableMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseTableMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AIRichResponseTableMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AIRichResponseTableMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AIRichResponseTableMetadata} AIRichResponseTableMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseTableMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(message.rows && message.rows.length))
                            message.rows = [];
                        message.rows.push($root.proto.AIRichResponseTableMetadata.AIRichResponseTableRow.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.title = reader.stringVerify();
                        message._title = "title";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a AIRichResponseTableMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AIRichResponseTableMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AIRichResponseTableMetadata} AIRichResponseTableMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseTableMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AIRichResponseTableMetadata message.
         * @function verify
         * @memberof proto.AIRichResponseTableMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AIRichResponseTableMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.rows != null && Object.hasOwnProperty.call(message, "rows")) {
                if (!Array.isArray(message.rows))
                    return "rows: array expected";
                for (var i = 0; i < message.rows.length; ++i) {
                    var error = $root.proto.AIRichResponseTableMetadata.AIRichResponseTableRow.verify(message.rows[i], q + 1);
                    if (error)
                        return "rows." + error;
                }
            }
            if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                properties._title = 1;
                if (!$util.isString(message.title))
                    return "title: string expected";
            }
            return null;
        };

        /**
         * Creates a AIRichResponseTableMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AIRichResponseTableMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AIRichResponseTableMetadata} AIRichResponseTableMetadata
         */
        AIRichResponseTableMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AIRichResponseTableMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.rows) {
                if (!Array.isArray(object.rows))
                    throw TypeError(".proto.AIRichResponseTableMetadata.rows: array expected");
                message.rows = Array(object.rows.length);
                for (var i = 0; i < object.rows.length; ++i) {
                    if (!$util.isObject(object.rows[i]))
                        throw TypeError(".proto.AIRichResponseTableMetadata.rows: object expected");
                    message.rows[i] = $root.proto.AIRichResponseTableMetadata.AIRichResponseTableRow.fromObject(object.rows[i], q + 1);
                }
            }
            if (object.title != null)
                message.title = String(object.title);
            return message;
        };

        /**
         * Creates a plain object from a AIRichResponseTableMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AIRichResponseTableMetadata
         * @static
         * @param {proto.AIRichResponseTableMetadata} message AIRichResponseTableMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AIRichResponseTableMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.rows = [];
            if (message.rows && message.rows.length) {
                object.rows = Array(message.rows.length);
                for (var j = 0; j < message.rows.length; ++j)
                    object.rows[j] = $root.proto.AIRichResponseTableMetadata.AIRichResponseTableRow.toObject(message.rows[j], options, q + 1);
            }
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                object.title = message.title;
            return object;
        };

        /**
         * Converts this AIRichResponseTableMetadata to JSON.
         * @function toJSON
         * @memberof proto.AIRichResponseTableMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AIRichResponseTableMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AIRichResponseTableMetadata
         * @function getTypeUrl
         * @memberof proto.AIRichResponseTableMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AIRichResponseTableMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AIRichResponseTableMetadata";
        };

        AIRichResponseTableMetadata.AIRichResponseTableRow = (function() {

            /**
             * Properties of a AIRichResponseTableRow.
             * @memberof proto.AIRichResponseTableMetadata
             * @interface IAIRichResponseTableRow
             * @property {Array.<string>|null} [items] AIRichResponseTableRow items
             * @property {boolean|null} [isHeading] AIRichResponseTableRow isHeading
             */

            /**
             * Constructs a new AIRichResponseTableRow.
             * @memberof proto.AIRichResponseTableMetadata
             * @classdesc Represents a AIRichResponseTableRow.
             * @implements IAIRichResponseTableRow
             * @constructor
             * @param {proto.AIRichResponseTableMetadata.IAIRichResponseTableRow=} [properties] Properties to set
             */
            function AIRichResponseTableRow(properties) {
                this.items = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AIRichResponseTableRow items.
             * @member {Array.<string>} items
             * @memberof proto.AIRichResponseTableMetadata.AIRichResponseTableRow
             * @instance
             */
            AIRichResponseTableRow.prototype.items = $util.emptyArray;

            /**
             * AIRichResponseTableRow isHeading.
             * @member {boolean|null|undefined} isHeading
             * @memberof proto.AIRichResponseTableMetadata.AIRichResponseTableRow
             * @instance
             */
            AIRichResponseTableRow.prototype.isHeading = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseTableRow.prototype, "_isHeading", {
                get: $util.oneOfGetter($oneOfFields = ["isHeading"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AIRichResponseTableRow instance using the specified properties.
             * @function create
             * @memberof proto.AIRichResponseTableMetadata.AIRichResponseTableRow
             * @static
             * @param {proto.AIRichResponseTableMetadata.IAIRichResponseTableRow=} [properties] Properties to set
             * @returns {proto.AIRichResponseTableMetadata.AIRichResponseTableRow} AIRichResponseTableRow instance
             */
            AIRichResponseTableRow.create = function create(properties) {
                return new AIRichResponseTableRow(properties);
            };

            /**
             * Encodes the specified AIRichResponseTableRow message. Does not implicitly {@link proto.AIRichResponseTableMetadata.AIRichResponseTableRow.verify|verify} messages.
             * @function encode
             * @memberof proto.AIRichResponseTableMetadata.AIRichResponseTableRow
             * @static
             * @param {proto.AIRichResponseTableMetadata.IAIRichResponseTableRow} message AIRichResponseTableRow message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AIRichResponseTableRow.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.items != null && message.items.length)
                    for (var i = 0; i < message.items.length; ++i)
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.items[i]);
                if (message.isHeading != null && Object.hasOwnProperty.call(message, "isHeading"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.isHeading);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AIRichResponseTableRow message, length delimited. Does not implicitly {@link proto.AIRichResponseTableMetadata.AIRichResponseTableRow.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.AIRichResponseTableMetadata.AIRichResponseTableRow
             * @static
             * @param {proto.AIRichResponseTableMetadata.IAIRichResponseTableRow} message AIRichResponseTableRow message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AIRichResponseTableRow.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a AIRichResponseTableRow message from the specified reader or buffer.
             * @function decode
             * @memberof proto.AIRichResponseTableMetadata.AIRichResponseTableRow
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.AIRichResponseTableMetadata.AIRichResponseTableRow} AIRichResponseTableRow
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AIRichResponseTableRow.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(message.items && message.items.length))
                                message.items = [];
                            message.items.push(reader.stringVerify());
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.isHeading = reader.bool();
                            message._isHeading = "isHeading";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a AIRichResponseTableRow message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.AIRichResponseTableMetadata.AIRichResponseTableRow
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.AIRichResponseTableMetadata.AIRichResponseTableRow} AIRichResponseTableRow
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AIRichResponseTableRow.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a AIRichResponseTableRow message.
             * @function verify
             * @memberof proto.AIRichResponseTableMetadata.AIRichResponseTableRow
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AIRichResponseTableRow.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.items != null && Object.hasOwnProperty.call(message, "items")) {
                    if (!Array.isArray(message.items))
                        return "items: array expected";
                    for (var i = 0; i < message.items.length; ++i)
                        if (!$util.isString(message.items[i]))
                            return "items: string[] expected";
                }
                if (message.isHeading != null && Object.hasOwnProperty.call(message, "isHeading")) {
                    properties._isHeading = 1;
                    if (typeof message.isHeading !== "boolean")
                        return "isHeading: boolean expected";
                }
                return null;
            };

            /**
             * Creates a AIRichResponseTableRow message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.AIRichResponseTableMetadata.AIRichResponseTableRow
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.AIRichResponseTableMetadata.AIRichResponseTableRow} AIRichResponseTableRow
             */
            AIRichResponseTableRow.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.AIRichResponseTableMetadata.AIRichResponseTableRow: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.items) {
                    if (!Array.isArray(object.items))
                        throw TypeError(".proto.AIRichResponseTableMetadata.AIRichResponseTableRow.items: array expected");
                    message.items = Array(object.items.length);
                    for (var i = 0; i < object.items.length; ++i)
                        message.items[i] = String(object.items[i]);
                }
                if (object.isHeading != null)
                    message.isHeading = Boolean(object.isHeading);
                return message;
            };

            /**
             * Creates a plain object from a AIRichResponseTableRow message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.AIRichResponseTableMetadata.AIRichResponseTableRow
             * @static
             * @param {proto.AIRichResponseTableMetadata.AIRichResponseTableRow} message AIRichResponseTableRow
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AIRichResponseTableRow.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.items = [];
                if (message.items && message.items.length) {
                    object.items = Array(message.items.length);
                    for (var j = 0; j < message.items.length; ++j)
                        object.items[j] = message.items[j];
                }
                if (message.isHeading != null && Object.hasOwnProperty.call(message, "isHeading"))
                    object.isHeading = message.isHeading;
                return object;
            };

            /**
             * Converts this AIRichResponseTableRow to JSON.
             * @function toJSON
             * @memberof proto.AIRichResponseTableMetadata.AIRichResponseTableRow
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AIRichResponseTableRow.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AIRichResponseTableRow
             * @function getTypeUrl
             * @memberof proto.AIRichResponseTableMetadata.AIRichResponseTableRow
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AIRichResponseTableRow.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.AIRichResponseTableMetadata.AIRichResponseTableRow";
            };

            return AIRichResponseTableRow;
        })();

        return AIRichResponseTableMetadata;
    })();

    proto.AIRichResponseUnifiedResponse = (function() {

        /**
         * Properties of a AIRichResponseUnifiedResponse.
         * @memberof proto
         * @interface IAIRichResponseUnifiedResponse
         * @property {Uint8Array|null} [data] AIRichResponseUnifiedResponse data
         */

        /**
         * Constructs a new AIRichResponseUnifiedResponse.
         * @memberof proto
         * @classdesc Represents a AIRichResponseUnifiedResponse.
         * @implements IAIRichResponseUnifiedResponse
         * @constructor
         * @param {proto.IAIRichResponseUnifiedResponse=} [properties] Properties to set
         */
        function AIRichResponseUnifiedResponse(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AIRichResponseUnifiedResponse data.
         * @member {Uint8Array|null|undefined} data
         * @memberof proto.AIRichResponseUnifiedResponse
         * @instance
         */
        AIRichResponseUnifiedResponse.prototype.data = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseUnifiedResponse.prototype, "_data", {
            get: $util.oneOfGetter($oneOfFields = ["data"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AIRichResponseUnifiedResponse instance using the specified properties.
         * @function create
         * @memberof proto.AIRichResponseUnifiedResponse
         * @static
         * @param {proto.IAIRichResponseUnifiedResponse=} [properties] Properties to set
         * @returns {proto.AIRichResponseUnifiedResponse} AIRichResponseUnifiedResponse instance
         */
        AIRichResponseUnifiedResponse.create = function create(properties) {
            return new AIRichResponseUnifiedResponse(properties);
        };

        /**
         * Encodes the specified AIRichResponseUnifiedResponse message. Does not implicitly {@link proto.AIRichResponseUnifiedResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.AIRichResponseUnifiedResponse
         * @static
         * @param {proto.IAIRichResponseUnifiedResponse} message AIRichResponseUnifiedResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseUnifiedResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.data);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AIRichResponseUnifiedResponse message, length delimited. Does not implicitly {@link proto.AIRichResponseUnifiedResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AIRichResponseUnifiedResponse
         * @static
         * @param {proto.IAIRichResponseUnifiedResponse} message AIRichResponseUnifiedResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIRichResponseUnifiedResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AIRichResponseUnifiedResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AIRichResponseUnifiedResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AIRichResponseUnifiedResponse} AIRichResponseUnifiedResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseUnifiedResponse.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.data = reader.bytes();
                        message._data = "data";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a AIRichResponseUnifiedResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AIRichResponseUnifiedResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AIRichResponseUnifiedResponse} AIRichResponseUnifiedResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIRichResponseUnifiedResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AIRichResponseUnifiedResponse message.
         * @function verify
         * @memberof proto.AIRichResponseUnifiedResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AIRichResponseUnifiedResponse.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.data != null && Object.hasOwnProperty.call(message, "data")) {
                properties._data = 1;
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            }
            return null;
        };

        /**
         * Creates a AIRichResponseUnifiedResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AIRichResponseUnifiedResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AIRichResponseUnifiedResponse} AIRichResponseUnifiedResponse
         */
        AIRichResponseUnifiedResponse.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AIRichResponseUnifiedResponse: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.data != null)
                if (typeof object.data === "string")
                    $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                else if (object.data.length >= 0)
                    message.data = object.data;
            return message;
        };

        /**
         * Creates a plain object from a AIRichResponseUnifiedResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AIRichResponseUnifiedResponse
         * @static
         * @param {proto.AIRichResponseUnifiedResponse} message AIRichResponseUnifiedResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AIRichResponseUnifiedResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                object.data = options.bytes === String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === Array ? Array.prototype.slice.call(message.data) : message.data;
            return object;
        };

        /**
         * Converts this AIRichResponseUnifiedResponse to JSON.
         * @function toJSON
         * @memberof proto.AIRichResponseUnifiedResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AIRichResponseUnifiedResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AIRichResponseUnifiedResponse
         * @function getTypeUrl
         * @memberof proto.AIRichResponseUnifiedResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AIRichResponseUnifiedResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AIRichResponseUnifiedResponse";
        };

        return AIRichResponseUnifiedResponse;
    })();

    /**
     * AISubscriptionRequestType enum.
     * @name proto.AISubscriptionRequestType
     * @enum {number}
     * @property {number} UNSPECIFIED=0 UNSPECIFIED value
     * @property {number} THINK_HARD=1 THINK_HARD value
     * @property {number} IMAGE_GEN=2 IMAGE_GEN value
     * @property {number} VIDEO_GEN=3 VIDEO_GEN value
     */
    proto.AISubscriptionRequestType = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "UNSPECIFIED"] = 0;
        values[valuesById[1] = "THINK_HARD"] = 1;
        values[valuesById[2] = "IMAGE_GEN"] = 2;
        values[valuesById[3] = "VIDEO_GEN"] = 3;
        return values;
    })();

    proto.AISubscriptionUpsellMetadata = (function() {

        /**
         * Properties of a AISubscriptionUpsellMetadata.
         * @memberof proto
         * @interface IAISubscriptionUpsellMetadata
         * @property {proto.AISubscriptionRequestType|null} [requestType] AISubscriptionUpsellMetadata requestType
         */

        /**
         * Constructs a new AISubscriptionUpsellMetadata.
         * @memberof proto
         * @classdesc Represents a AISubscriptionUpsellMetadata.
         * @implements IAISubscriptionUpsellMetadata
         * @constructor
         * @param {proto.IAISubscriptionUpsellMetadata=} [properties] Properties to set
         */
        function AISubscriptionUpsellMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AISubscriptionUpsellMetadata requestType.
         * @member {proto.AISubscriptionRequestType|null|undefined} requestType
         * @memberof proto.AISubscriptionUpsellMetadata
         * @instance
         */
        AISubscriptionUpsellMetadata.prototype.requestType = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AISubscriptionUpsellMetadata.prototype, "_requestType", {
            get: $util.oneOfGetter($oneOfFields = ["requestType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AISubscriptionUpsellMetadata instance using the specified properties.
         * @function create
         * @memberof proto.AISubscriptionUpsellMetadata
         * @static
         * @param {proto.IAISubscriptionUpsellMetadata=} [properties] Properties to set
         * @returns {proto.AISubscriptionUpsellMetadata} AISubscriptionUpsellMetadata instance
         */
        AISubscriptionUpsellMetadata.create = function create(properties) {
            return new AISubscriptionUpsellMetadata(properties);
        };

        /**
         * Encodes the specified AISubscriptionUpsellMetadata message. Does not implicitly {@link proto.AISubscriptionUpsellMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.AISubscriptionUpsellMetadata
         * @static
         * @param {proto.IAISubscriptionUpsellMetadata} message AISubscriptionUpsellMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AISubscriptionUpsellMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.requestType != null && Object.hasOwnProperty.call(message, "requestType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.requestType);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AISubscriptionUpsellMetadata message, length delimited. Does not implicitly {@link proto.AISubscriptionUpsellMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AISubscriptionUpsellMetadata
         * @static
         * @param {proto.IAISubscriptionUpsellMetadata} message AISubscriptionUpsellMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AISubscriptionUpsellMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AISubscriptionUpsellMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AISubscriptionUpsellMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AISubscriptionUpsellMetadata} AISubscriptionUpsellMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AISubscriptionUpsellMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.requestType = reader.int32();
                        message._requestType = "requestType";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a AISubscriptionUpsellMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AISubscriptionUpsellMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AISubscriptionUpsellMetadata} AISubscriptionUpsellMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AISubscriptionUpsellMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AISubscriptionUpsellMetadata message.
         * @function verify
         * @memberof proto.AISubscriptionUpsellMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AISubscriptionUpsellMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.requestType != null && Object.hasOwnProperty.call(message, "requestType")) {
                properties._requestType = 1;
                if (typeof message.requestType !== "number" || (message.requestType | 0) !== message.requestType)
                    return "requestType: enum value expected";
            }
            return null;
        };

        /**
         * Creates a AISubscriptionUpsellMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AISubscriptionUpsellMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AISubscriptionUpsellMetadata} AISubscriptionUpsellMetadata
         */
        AISubscriptionUpsellMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AISubscriptionUpsellMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.requestType) {
            case "UNSPECIFIED":
            case 0:
                message.requestType = 0;
                break;
            case "THINK_HARD":
            case 1:
                message.requestType = 1;
                break;
            case "IMAGE_GEN":
            case 2:
                message.requestType = 2;
                break;
            case "VIDEO_GEN":
            case 3:
                message.requestType = 3;
                break;
            default:
                if (typeof object.requestType === "number" && (object.requestType | 0) === object.requestType)
                    message.requestType = object.requestType;
            }
            return message;
        };

        /**
         * Creates a plain object from a AISubscriptionUpsellMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AISubscriptionUpsellMetadata
         * @static
         * @param {proto.AISubscriptionUpsellMetadata} message AISubscriptionUpsellMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AISubscriptionUpsellMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.requestType != null && Object.hasOwnProperty.call(message, "requestType"))
                object.requestType = options.enums === String ? $root.proto.AISubscriptionRequestType[message.requestType] === undefined ? message.requestType : $root.proto.AISubscriptionRequestType[message.requestType] : message.requestType;
            return object;
        };

        /**
         * Converts this AISubscriptionUpsellMetadata to JSON.
         * @function toJSON
         * @memberof proto.AISubscriptionUpsellMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AISubscriptionUpsellMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AISubscriptionUpsellMetadata
         * @function getTypeUrl
         * @memberof proto.AISubscriptionUpsellMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AISubscriptionUpsellMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AISubscriptionUpsellMetadata";
        };

        return AISubscriptionUpsellMetadata;
    })();

    proto.AIThreadInfo = (function() {

        /**
         * Properties of a AIThreadInfo.
         * @memberof proto
         * @interface IAIThreadInfo
         * @property {proto.AIThreadInfo.IAIThreadServerInfo|null} [serverInfo] AIThreadInfo serverInfo
         * @property {proto.AIThreadInfo.IAIThreadClientInfo|null} [clientInfo] AIThreadInfo clientInfo
         */

        /**
         * Constructs a new AIThreadInfo.
         * @memberof proto
         * @classdesc Represents a AIThreadInfo.
         * @implements IAIThreadInfo
         * @constructor
         * @param {proto.IAIThreadInfo=} [properties] Properties to set
         */
        function AIThreadInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AIThreadInfo serverInfo.
         * @member {proto.AIThreadInfo.IAIThreadServerInfo|null|undefined} serverInfo
         * @memberof proto.AIThreadInfo
         * @instance
         */
        AIThreadInfo.prototype.serverInfo = null;

        /**
         * AIThreadInfo clientInfo.
         * @member {proto.AIThreadInfo.IAIThreadClientInfo|null|undefined} clientInfo
         * @memberof proto.AIThreadInfo
         * @instance
         */
        AIThreadInfo.prototype.clientInfo = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIThreadInfo.prototype, "_serverInfo", {
            get: $util.oneOfGetter($oneOfFields = ["serverInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIThreadInfo.prototype, "_clientInfo", {
            get: $util.oneOfGetter($oneOfFields = ["clientInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AIThreadInfo instance using the specified properties.
         * @function create
         * @memberof proto.AIThreadInfo
         * @static
         * @param {proto.IAIThreadInfo=} [properties] Properties to set
         * @returns {proto.AIThreadInfo} AIThreadInfo instance
         */
        AIThreadInfo.create = function create(properties) {
            return new AIThreadInfo(properties);
        };

        /**
         * Encodes the specified AIThreadInfo message. Does not implicitly {@link proto.AIThreadInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.AIThreadInfo
         * @static
         * @param {proto.IAIThreadInfo} message AIThreadInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIThreadInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.serverInfo != null && Object.hasOwnProperty.call(message, "serverInfo"))
                $root.proto.AIThreadInfo.AIThreadServerInfo.encode(message.serverInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
                $root.proto.AIThreadInfo.AIThreadClientInfo.encode(message.clientInfo, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AIThreadInfo message, length delimited. Does not implicitly {@link proto.AIThreadInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AIThreadInfo
         * @static
         * @param {proto.IAIThreadInfo} message AIThreadInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AIThreadInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a AIThreadInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AIThreadInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AIThreadInfo} AIThreadInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIThreadInfo.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.serverInfo = $root.proto.AIThreadInfo.AIThreadServerInfo.decode(reader, reader.uint32(), undefined, q + 1, message.serverInfo);
                        message._serverInfo = "serverInfo";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.clientInfo = $root.proto.AIThreadInfo.AIThreadClientInfo.decode(reader, reader.uint32(), undefined, q + 1, message.clientInfo);
                        message._clientInfo = "clientInfo";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a AIThreadInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AIThreadInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AIThreadInfo} AIThreadInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AIThreadInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a AIThreadInfo message.
         * @function verify
         * @memberof proto.AIThreadInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AIThreadInfo.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.serverInfo != null && Object.hasOwnProperty.call(message, "serverInfo")) {
                properties._serverInfo = 1;
                {
                    var error = $root.proto.AIThreadInfo.AIThreadServerInfo.verify(message.serverInfo, q + 1);
                    if (error)
                        return "serverInfo." + error;
                }
            }
            if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo")) {
                properties._clientInfo = 1;
                {
                    var error = $root.proto.AIThreadInfo.AIThreadClientInfo.verify(message.clientInfo, q + 1);
                    if (error)
                        return "clientInfo." + error;
                }
            }
            return null;
        };

        /**
         * Creates a AIThreadInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AIThreadInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AIThreadInfo} AIThreadInfo
         */
        AIThreadInfo.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AIThreadInfo: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.serverInfo != null) {
                if (!$util.isObject(object.serverInfo))
                    throw TypeError(".proto.AIThreadInfo.serverInfo: object expected");
                message.serverInfo = $root.proto.AIThreadInfo.AIThreadServerInfo.fromObject(object.serverInfo, q + 1);
            }
            if (object.clientInfo != null) {
                if (!$util.isObject(object.clientInfo))
                    throw TypeError(".proto.AIThreadInfo.clientInfo: object expected");
                message.clientInfo = $root.proto.AIThreadInfo.AIThreadClientInfo.fromObject(object.clientInfo, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a AIThreadInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AIThreadInfo
         * @static
         * @param {proto.AIThreadInfo} message AIThreadInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AIThreadInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.serverInfo != null && Object.hasOwnProperty.call(message, "serverInfo"))
                object.serverInfo = $root.proto.AIThreadInfo.AIThreadServerInfo.toObject(message.serverInfo, options, q + 1);
            if (message.clientInfo != null && Object.hasOwnProperty.call(message, "clientInfo"))
                object.clientInfo = $root.proto.AIThreadInfo.AIThreadClientInfo.toObject(message.clientInfo, options, q + 1);
            return object;
        };

        /**
         * Converts this AIThreadInfo to JSON.
         * @function toJSON
         * @memberof proto.AIThreadInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AIThreadInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AIThreadInfo
         * @function getTypeUrl
         * @memberof proto.AIThreadInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AIThreadInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AIThreadInfo";
        };

        AIThreadInfo.AIThreadClientInfo = (function() {

            /**
             * Properties of a AIThreadClientInfo.
             * @memberof proto.AIThreadInfo
             * @interface IAIThreadClientInfo
             * @property {proto.AIThreadInfo.AIThreadClientInfo.AIThreadType|null} [type] AIThreadClientInfo type
             * @property {string|null} [sourceChatJid] AIThreadClientInfo sourceChatJid
             */

            /**
             * Constructs a new AIThreadClientInfo.
             * @memberof proto.AIThreadInfo
             * @classdesc Represents a AIThreadClientInfo.
             * @implements IAIThreadClientInfo
             * @constructor
             * @param {proto.AIThreadInfo.IAIThreadClientInfo=} [properties] Properties to set
             */
            function AIThreadClientInfo(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AIThreadClientInfo type.
             * @member {proto.AIThreadInfo.AIThreadClientInfo.AIThreadType|null|undefined} type
             * @memberof proto.AIThreadInfo.AIThreadClientInfo
             * @instance
             */
            AIThreadClientInfo.prototype.type = null;

            /**
             * AIThreadClientInfo sourceChatJid.
             * @member {string|null|undefined} sourceChatJid
             * @memberof proto.AIThreadInfo.AIThreadClientInfo
             * @instance
             */
            AIThreadClientInfo.prototype.sourceChatJid = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIThreadClientInfo.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIThreadClientInfo.prototype, "_sourceChatJid", {
                get: $util.oneOfGetter($oneOfFields = ["sourceChatJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AIThreadClientInfo instance using the specified properties.
             * @function create
             * @memberof proto.AIThreadInfo.AIThreadClientInfo
             * @static
             * @param {proto.AIThreadInfo.IAIThreadClientInfo=} [properties] Properties to set
             * @returns {proto.AIThreadInfo.AIThreadClientInfo} AIThreadClientInfo instance
             */
            AIThreadClientInfo.create = function create(properties) {
                return new AIThreadClientInfo(properties);
            };

            /**
             * Encodes the specified AIThreadClientInfo message. Does not implicitly {@link proto.AIThreadInfo.AIThreadClientInfo.verify|verify} messages.
             * @function encode
             * @memberof proto.AIThreadInfo.AIThreadClientInfo
             * @static
             * @param {proto.AIThreadInfo.IAIThreadClientInfo} message AIThreadClientInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AIThreadClientInfo.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
                if (message.sourceChatJid != null && Object.hasOwnProperty.call(message, "sourceChatJid"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.sourceChatJid);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AIThreadClientInfo message, length delimited. Does not implicitly {@link proto.AIThreadInfo.AIThreadClientInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.AIThreadInfo.AIThreadClientInfo
             * @static
             * @param {proto.AIThreadInfo.IAIThreadClientInfo} message AIThreadClientInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AIThreadClientInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a AIThreadClientInfo message from the specified reader or buffer.
             * @function decode
             * @memberof proto.AIThreadInfo.AIThreadClientInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.AIThreadInfo.AIThreadClientInfo} AIThreadClientInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AIThreadClientInfo.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.type = reader.int32();
                            message._type = "type";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.sourceChatJid = reader.stringVerify();
                            message._sourceChatJid = "sourceChatJid";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a AIThreadClientInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.AIThreadInfo.AIThreadClientInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.AIThreadInfo.AIThreadClientInfo} AIThreadClientInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AIThreadClientInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a AIThreadClientInfo message.
             * @function verify
             * @memberof proto.AIThreadInfo.AIThreadClientInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AIThreadClientInfo.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                    properties._type = 1;
                    if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                        return "type: enum value expected";
                }
                if (message.sourceChatJid != null && Object.hasOwnProperty.call(message, "sourceChatJid")) {
                    properties._sourceChatJid = 1;
                    if (!$util.isString(message.sourceChatJid))
                        return "sourceChatJid: string expected";
                }
                return null;
            };

            /**
             * Creates a AIThreadClientInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.AIThreadInfo.AIThreadClientInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.AIThreadInfo.AIThreadClientInfo} AIThreadClientInfo
             */
            AIThreadClientInfo.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.AIThreadInfo.AIThreadClientInfo: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.type) {
                case "UNKNOWN":
                case 0:
                    message.type = 0;
                    break;
                case "DEFAULT":
                case 1:
                    message.type = 1;
                    break;
                case "INCOGNITO":
                case 2:
                    message.type = 2;
                    break;
                case "SIDE_CHAT":
                case 3:
                    message.type = 3;
                    break;
                default:
                    if (typeof object.type === "number" && (object.type | 0) === object.type)
                        message.type = object.type;
                }
                if (object.sourceChatJid != null)
                    message.sourceChatJid = String(object.sourceChatJid);
                return message;
            };

            /**
             * Creates a plain object from a AIThreadClientInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.AIThreadInfo.AIThreadClientInfo
             * @static
             * @param {proto.AIThreadInfo.AIThreadClientInfo} message AIThreadClientInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AIThreadClientInfo.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    object.type = options.enums === String ? $root.proto.AIThreadInfo.AIThreadClientInfo.AIThreadType[message.type] === undefined ? message.type : $root.proto.AIThreadInfo.AIThreadClientInfo.AIThreadType[message.type] : message.type;
                if (message.sourceChatJid != null && Object.hasOwnProperty.call(message, "sourceChatJid"))
                    object.sourceChatJid = message.sourceChatJid;
                return object;
            };

            /**
             * Converts this AIThreadClientInfo to JSON.
             * @function toJSON
             * @memberof proto.AIThreadInfo.AIThreadClientInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AIThreadClientInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AIThreadClientInfo
             * @function getTypeUrl
             * @memberof proto.AIThreadInfo.AIThreadClientInfo
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AIThreadClientInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.AIThreadInfo.AIThreadClientInfo";
            };

            /**
             * AIThreadType enum.
             * @name proto.AIThreadInfo.AIThreadClientInfo.AIThreadType
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} DEFAULT=1 DEFAULT value
             * @property {number} INCOGNITO=2 INCOGNITO value
             * @property {number} SIDE_CHAT=3 SIDE_CHAT value
             */
            AIThreadClientInfo.AIThreadType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "DEFAULT"] = 1;
                values[valuesById[2] = "INCOGNITO"] = 2;
                values[valuesById[3] = "SIDE_CHAT"] = 3;
                return values;
            })();

            return AIThreadClientInfo;
        })();

        AIThreadInfo.AIThreadServerInfo = (function() {

            /**
             * Properties of a AIThreadServerInfo.
             * @memberof proto.AIThreadInfo
             * @interface IAIThreadServerInfo
             * @property {string|null} [title] AIThreadServerInfo title
             */

            /**
             * Constructs a new AIThreadServerInfo.
             * @memberof proto.AIThreadInfo
             * @classdesc Represents a AIThreadServerInfo.
             * @implements IAIThreadServerInfo
             * @constructor
             * @param {proto.AIThreadInfo.IAIThreadServerInfo=} [properties] Properties to set
             */
            function AIThreadServerInfo(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AIThreadServerInfo title.
             * @member {string|null|undefined} title
             * @memberof proto.AIThreadInfo.AIThreadServerInfo
             * @instance
             */
            AIThreadServerInfo.prototype.title = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIThreadServerInfo.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AIThreadServerInfo instance using the specified properties.
             * @function create
             * @memberof proto.AIThreadInfo.AIThreadServerInfo
             * @static
             * @param {proto.AIThreadInfo.IAIThreadServerInfo=} [properties] Properties to set
             * @returns {proto.AIThreadInfo.AIThreadServerInfo} AIThreadServerInfo instance
             */
            AIThreadServerInfo.create = function create(properties) {
                return new AIThreadServerInfo(properties);
            };

            /**
             * Encodes the specified AIThreadServerInfo message. Does not implicitly {@link proto.AIThreadInfo.AIThreadServerInfo.verify|verify} messages.
             * @function encode
             * @memberof proto.AIThreadInfo.AIThreadServerInfo
             * @static
             * @param {proto.AIThreadInfo.IAIThreadServerInfo} message AIThreadServerInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AIThreadServerInfo.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.title);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AIThreadServerInfo message, length delimited. Does not implicitly {@link proto.AIThreadInfo.AIThreadServerInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.AIThreadInfo.AIThreadServerInfo
             * @static
             * @param {proto.AIThreadInfo.IAIThreadServerInfo} message AIThreadServerInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AIThreadServerInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a AIThreadServerInfo message from the specified reader or buffer.
             * @function decode
             * @memberof proto.AIThreadInfo.AIThreadServerInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.AIThreadInfo.AIThreadServerInfo} AIThreadServerInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AIThreadServerInfo.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.title = reader.stringVerify();
                            message._title = "title";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a AIThreadServerInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.AIThreadInfo.AIThreadServerInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.AIThreadInfo.AIThreadServerInfo} AIThreadServerInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AIThreadServerInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a AIThreadServerInfo message.
             * @function verify
             * @memberof proto.AIThreadInfo.AIThreadServerInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AIThreadServerInfo.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                    properties._title = 1;
                    if (!$util.isString(message.title))
                        return "title: string expected";
                }
                return null;
            };

            /**
             * Creates a AIThreadServerInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.AIThreadInfo.AIThreadServerInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.AIThreadInfo.AIThreadServerInfo} AIThreadServerInfo
             */
            AIThreadServerInfo.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.AIThreadInfo.AIThreadServerInfo: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.title != null)
                    message.title = String(object.title);
                return message;
            };

            /**
             * Creates a plain object from a AIThreadServerInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.AIThreadInfo.AIThreadServerInfo
             * @static
             * @param {proto.AIThreadInfo.AIThreadServerInfo} message AIThreadServerInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AIThreadServerInfo.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    object.title = message.title;
                return object;
            };

            /**
             * Converts this AIThreadServerInfo to JSON.
             * @function toJSON
             * @memberof proto.AIThreadInfo.AIThreadServerInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AIThreadServerInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AIThreadServerInfo
             * @function getTypeUrl
             * @memberof proto.AIThreadInfo.AIThreadServerInfo
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AIThreadServerInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.AIThreadInfo.AIThreadServerInfo";
            };

            return AIThreadServerInfo;
        })();

        return AIThreadInfo;
    })();

    proto.Account = (function() {

        /**
         * Properties of an Account.
         * @memberof proto
         * @interface IAccount
         * @property {string|null} [lid] Account lid
         * @property {string|null} [username] Account username
         * @property {string|null} [countryCode] Account countryCode
         * @property {boolean|null} [isUsernameDeleted] Account isUsernameDeleted
         */

        /**
         * Constructs a new Account.
         * @memberof proto
         * @classdesc Represents an Account.
         * @implements IAccount
         * @constructor
         * @param {proto.IAccount=} [properties] Properties to set
         */
        function Account(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Account lid.
         * @member {string|null|undefined} lid
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.lid = null;

        /**
         * Account username.
         * @member {string|null|undefined} username
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.username = null;

        /**
         * Account countryCode.
         * @member {string|null|undefined} countryCode
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.countryCode = null;

        /**
         * Account isUsernameDeleted.
         * @member {boolean|null|undefined} isUsernameDeleted
         * @memberof proto.Account
         * @instance
         */
        Account.prototype.isUsernameDeleted = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Account.prototype, "_lid", {
            get: $util.oneOfGetter($oneOfFields = ["lid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Account.prototype, "_username", {
            get: $util.oneOfGetter($oneOfFields = ["username"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Account.prototype, "_countryCode", {
            get: $util.oneOfGetter($oneOfFields = ["countryCode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Account.prototype, "_isUsernameDeleted", {
            get: $util.oneOfGetter($oneOfFields = ["isUsernameDeleted"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new Account instance using the specified properties.
         * @function create
         * @memberof proto.Account
         * @static
         * @param {proto.IAccount=} [properties] Properties to set
         * @returns {proto.Account} Account instance
         */
        Account.create = function create(properties) {
            return new Account(properties);
        };

        /**
         * Encodes the specified Account message. Does not implicitly {@link proto.Account.verify|verify} messages.
         * @function encode
         * @memberof proto.Account
         * @static
         * @param {proto.IAccount} message Account message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Account.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.lid != null && Object.hasOwnProperty.call(message, "lid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.lid);
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.username);
            if (message.countryCode != null && Object.hasOwnProperty.call(message, "countryCode"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.countryCode);
            if (message.isUsernameDeleted != null && Object.hasOwnProperty.call(message, "isUsernameDeleted"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.isUsernameDeleted);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Account message, length delimited. Does not implicitly {@link proto.Account.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Account
         * @static
         * @param {proto.IAccount} message Account message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Account.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an Account message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Account
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Account} Account
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Account.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.lid = reader.stringVerify();
                        message._lid = "lid";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.username = reader.stringVerify();
                        message._username = "username";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.countryCode = reader.stringVerify();
                        message._countryCode = "countryCode";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.isUsernameDeleted = reader.bool();
                        message._isUsernameDeleted = "isUsernameDeleted";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes an Account message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Account
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Account} Account
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Account.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Account message.
         * @function verify
         * @memberof proto.Account
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Account.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.lid != null && Object.hasOwnProperty.call(message, "lid")) {
                properties._lid = 1;
                if (!$util.isString(message.lid))
                    return "lid: string expected";
            }
            if (message.username != null && Object.hasOwnProperty.call(message, "username")) {
                properties._username = 1;
                if (!$util.isString(message.username))
                    return "username: string expected";
            }
            if (message.countryCode != null && Object.hasOwnProperty.call(message, "countryCode")) {
                properties._countryCode = 1;
                if (!$util.isString(message.countryCode))
                    return "countryCode: string expected";
            }
            if (message.isUsernameDeleted != null && Object.hasOwnProperty.call(message, "isUsernameDeleted")) {
                properties._isUsernameDeleted = 1;
                if (typeof message.isUsernameDeleted !== "boolean")
                    return "isUsernameDeleted: boolean expected";
            }
            return null;
        };

        /**
         * Creates an Account message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Account
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Account} Account
         */
        Account.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.Account: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.lid != null)
                message.lid = String(object.lid);
            if (object.username != null)
                message.username = String(object.username);
            if (object.countryCode != null)
                message.countryCode = String(object.countryCode);
            if (object.isUsernameDeleted != null)
                message.isUsernameDeleted = Boolean(object.isUsernameDeleted);
            return message;
        };

        /**
         * Creates a plain object from an Account message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Account
         * @static
         * @param {proto.Account} message Account
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Account.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.lid != null && Object.hasOwnProperty.call(message, "lid"))
                object.lid = message.lid;
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.countryCode != null && Object.hasOwnProperty.call(message, "countryCode"))
                object.countryCode = message.countryCode;
            if (message.isUsernameDeleted != null && Object.hasOwnProperty.call(message, "isUsernameDeleted"))
                object.isUsernameDeleted = message.isUsernameDeleted;
            return object;
        };

        /**
         * Converts this Account to JSON.
         * @function toJSON
         * @memberof proto.Account
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Account.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Account
         * @function getTypeUrl
         * @memberof proto.Account
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Account.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.Account";
        };

        return Account;
    })();

    proto.ActionLink = (function() {

        /**
         * Properties of an ActionLink.
         * @memberof proto
         * @interface IActionLink
         * @property {string|null} [url] ActionLink url
         * @property {string|null} [buttonTitle] ActionLink buttonTitle
         */

        /**
         * Constructs a new ActionLink.
         * @memberof proto
         * @classdesc Represents an ActionLink.
         * @implements IActionLink
         * @constructor
         * @param {proto.IActionLink=} [properties] Properties to set
         */
        function ActionLink(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ActionLink url.
         * @member {string|null|undefined} url
         * @memberof proto.ActionLink
         * @instance
         */
        ActionLink.prototype.url = null;

        /**
         * ActionLink buttonTitle.
         * @member {string|null|undefined} buttonTitle
         * @memberof proto.ActionLink
         * @instance
         */
        ActionLink.prototype.buttonTitle = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ActionLink.prototype, "_url", {
            get: $util.oneOfGetter($oneOfFields = ["url"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ActionLink.prototype, "_buttonTitle", {
            get: $util.oneOfGetter($oneOfFields = ["buttonTitle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ActionLink instance using the specified properties.
         * @function create
         * @memberof proto.ActionLink
         * @static
         * @param {proto.IActionLink=} [properties] Properties to set
         * @returns {proto.ActionLink} ActionLink instance
         */
        ActionLink.create = function create(properties) {
            return new ActionLink(properties);
        };

        /**
         * Encodes the specified ActionLink message. Does not implicitly {@link proto.ActionLink.verify|verify} messages.
         * @function encode
         * @memberof proto.ActionLink
         * @static
         * @param {proto.IActionLink} message ActionLink message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ActionLink.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.url);
            if (message.buttonTitle != null && Object.hasOwnProperty.call(message, "buttonTitle"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.buttonTitle);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ActionLink message, length delimited. Does not implicitly {@link proto.ActionLink.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ActionLink
         * @static
         * @param {proto.IActionLink} message ActionLink message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ActionLink.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ActionLink message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ActionLink
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ActionLink} ActionLink
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ActionLink.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.url = reader.stringVerify();
                        message._url = "url";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.buttonTitle = reader.stringVerify();
                        message._buttonTitle = "buttonTitle";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes an ActionLink message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ActionLink
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ActionLink} ActionLink
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ActionLink.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ActionLink message.
         * @function verify
         * @memberof proto.ActionLink
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ActionLink.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.url != null && Object.hasOwnProperty.call(message, "url")) {
                properties._url = 1;
                if (!$util.isString(message.url))
                    return "url: string expected";
            }
            if (message.buttonTitle != null && Object.hasOwnProperty.call(message, "buttonTitle")) {
                properties._buttonTitle = 1;
                if (!$util.isString(message.buttonTitle))
                    return "buttonTitle: string expected";
            }
            return null;
        };

        /**
         * Creates an ActionLink message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ActionLink
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ActionLink} ActionLink
         */
        ActionLink.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.ActionLink: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.url != null)
                message.url = String(object.url);
            if (object.buttonTitle != null)
                message.buttonTitle = String(object.buttonTitle);
            return message;
        };

        /**
         * Creates a plain object from an ActionLink message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ActionLink
         * @static
         * @param {proto.ActionLink} message ActionLink
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ActionLink.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                object.url = message.url;
            if (message.buttonTitle != null && Object.hasOwnProperty.call(message, "buttonTitle"))
                object.buttonTitle = message.buttonTitle;
            return object;
        };

        /**
         * Converts this ActionLink to JSON.
         * @function toJSON
         * @memberof proto.ActionLink
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ActionLink.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ActionLink
         * @function getTypeUrl
         * @memberof proto.ActionLink
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ActionLink.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.ActionLink";
        };

        return ActionLink;
    })();

    proto.AutoDownloadSettings = (function() {

        /**
         * Properties of an AutoDownloadSettings.
         * @memberof proto
         * @interface IAutoDownloadSettings
         * @property {boolean|null} [downloadImages] AutoDownloadSettings downloadImages
         * @property {boolean|null} [downloadAudio] AutoDownloadSettings downloadAudio
         * @property {boolean|null} [downloadVideo] AutoDownloadSettings downloadVideo
         * @property {boolean|null} [downloadDocuments] AutoDownloadSettings downloadDocuments
         */

        /**
         * Constructs a new AutoDownloadSettings.
         * @memberof proto
         * @classdesc Represents an AutoDownloadSettings.
         * @implements IAutoDownloadSettings
         * @constructor
         * @param {proto.IAutoDownloadSettings=} [properties] Properties to set
         */
        function AutoDownloadSettings(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AutoDownloadSettings downloadImages.
         * @member {boolean|null|undefined} downloadImages
         * @memberof proto.AutoDownloadSettings
         * @instance
         */
        AutoDownloadSettings.prototype.downloadImages = null;

        /**
         * AutoDownloadSettings downloadAudio.
         * @member {boolean|null|undefined} downloadAudio
         * @memberof proto.AutoDownloadSettings
         * @instance
         */
        AutoDownloadSettings.prototype.downloadAudio = null;

        /**
         * AutoDownloadSettings downloadVideo.
         * @member {boolean|null|undefined} downloadVideo
         * @memberof proto.AutoDownloadSettings
         * @instance
         */
        AutoDownloadSettings.prototype.downloadVideo = null;

        /**
         * AutoDownloadSettings downloadDocuments.
         * @member {boolean|null|undefined} downloadDocuments
         * @memberof proto.AutoDownloadSettings
         * @instance
         */
        AutoDownloadSettings.prototype.downloadDocuments = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AutoDownloadSettings.prototype, "_downloadImages", {
            get: $util.oneOfGetter($oneOfFields = ["downloadImages"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AutoDownloadSettings.prototype, "_downloadAudio", {
            get: $util.oneOfGetter($oneOfFields = ["downloadAudio"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AutoDownloadSettings.prototype, "_downloadVideo", {
            get: $util.oneOfGetter($oneOfFields = ["downloadVideo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AutoDownloadSettings.prototype, "_downloadDocuments", {
            get: $util.oneOfGetter($oneOfFields = ["downloadDocuments"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AutoDownloadSettings instance using the specified properties.
         * @function create
         * @memberof proto.AutoDownloadSettings
         * @static
         * @param {proto.IAutoDownloadSettings=} [properties] Properties to set
         * @returns {proto.AutoDownloadSettings} AutoDownloadSettings instance
         */
        AutoDownloadSettings.create = function create(properties) {
            return new AutoDownloadSettings(properties);
        };

        /**
         * Encodes the specified AutoDownloadSettings message. Does not implicitly {@link proto.AutoDownloadSettings.verify|verify} messages.
         * @function encode
         * @memberof proto.AutoDownloadSettings
         * @static
         * @param {proto.IAutoDownloadSettings} message AutoDownloadSettings message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AutoDownloadSettings.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.downloadImages != null && Object.hasOwnProperty.call(message, "downloadImages"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.downloadImages);
            if (message.downloadAudio != null && Object.hasOwnProperty.call(message, "downloadAudio"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.downloadAudio);
            if (message.downloadVideo != null && Object.hasOwnProperty.call(message, "downloadVideo"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.downloadVideo);
            if (message.downloadDocuments != null && Object.hasOwnProperty.call(message, "downloadDocuments"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.downloadDocuments);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AutoDownloadSettings message, length delimited. Does not implicitly {@link proto.AutoDownloadSettings.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AutoDownloadSettings
         * @static
         * @param {proto.IAutoDownloadSettings} message AutoDownloadSettings message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AutoDownloadSettings.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an AutoDownloadSettings message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AutoDownloadSettings
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AutoDownloadSettings} AutoDownloadSettings
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AutoDownloadSettings.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.downloadImages = reader.bool();
                        message._downloadImages = "downloadImages";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.downloadAudio = reader.bool();
                        message._downloadAudio = "downloadAudio";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.downloadVideo = reader.bool();
                        message._downloadVideo = "downloadVideo";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.downloadDocuments = reader.bool();
                        message._downloadDocuments = "downloadDocuments";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes an AutoDownloadSettings message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AutoDownloadSettings
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AutoDownloadSettings} AutoDownloadSettings
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AutoDownloadSettings.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AutoDownloadSettings message.
         * @function verify
         * @memberof proto.AutoDownloadSettings
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AutoDownloadSettings.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.downloadImages != null && Object.hasOwnProperty.call(message, "downloadImages")) {
                properties._downloadImages = 1;
                if (typeof message.downloadImages !== "boolean")
                    return "downloadImages: boolean expected";
            }
            if (message.downloadAudio != null && Object.hasOwnProperty.call(message, "downloadAudio")) {
                properties._downloadAudio = 1;
                if (typeof message.downloadAudio !== "boolean")
                    return "downloadAudio: boolean expected";
            }
            if (message.downloadVideo != null && Object.hasOwnProperty.call(message, "downloadVideo")) {
                properties._downloadVideo = 1;
                if (typeof message.downloadVideo !== "boolean")
                    return "downloadVideo: boolean expected";
            }
            if (message.downloadDocuments != null && Object.hasOwnProperty.call(message, "downloadDocuments")) {
                properties._downloadDocuments = 1;
                if (typeof message.downloadDocuments !== "boolean")
                    return "downloadDocuments: boolean expected";
            }
            return null;
        };

        /**
         * Creates an AutoDownloadSettings message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AutoDownloadSettings
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AutoDownloadSettings} AutoDownloadSettings
         */
        AutoDownloadSettings.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AutoDownloadSettings: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.downloadImages != null)
                message.downloadImages = Boolean(object.downloadImages);
            if (object.downloadAudio != null)
                message.downloadAudio = Boolean(object.downloadAudio);
            if (object.downloadVideo != null)
                message.downloadVideo = Boolean(object.downloadVideo);
            if (object.downloadDocuments != null)
                message.downloadDocuments = Boolean(object.downloadDocuments);
            return message;
        };

        /**
         * Creates a plain object from an AutoDownloadSettings message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AutoDownloadSettings
         * @static
         * @param {proto.AutoDownloadSettings} message AutoDownloadSettings
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AutoDownloadSettings.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.downloadImages != null && Object.hasOwnProperty.call(message, "downloadImages"))
                object.downloadImages = message.downloadImages;
            if (message.downloadAudio != null && Object.hasOwnProperty.call(message, "downloadAudio"))
                object.downloadAudio = message.downloadAudio;
            if (message.downloadVideo != null && Object.hasOwnProperty.call(message, "downloadVideo"))
                object.downloadVideo = message.downloadVideo;
            if (message.downloadDocuments != null && Object.hasOwnProperty.call(message, "downloadDocuments"))
                object.downloadDocuments = message.downloadDocuments;
            return object;
        };

        /**
         * Converts this AutoDownloadSettings to JSON.
         * @function toJSON
         * @memberof proto.AutoDownloadSettings
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AutoDownloadSettings.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AutoDownloadSettings
         * @function getTypeUrl
         * @memberof proto.AutoDownloadSettings
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AutoDownloadSettings.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AutoDownloadSettings";
        };

        return AutoDownloadSettings;
    })();

    proto.AvatarUserSettings = (function() {

        /**
         * Properties of an AvatarUserSettings.
         * @memberof proto
         * @interface IAvatarUserSettings
         * @property {string|null} [fbid] AvatarUserSettings fbid
         * @property {string|null} [password] AvatarUserSettings password
         */

        /**
         * Constructs a new AvatarUserSettings.
         * @memberof proto
         * @classdesc Represents an AvatarUserSettings.
         * @implements IAvatarUserSettings
         * @constructor
         * @param {proto.IAvatarUserSettings=} [properties] Properties to set
         */
        function AvatarUserSettings(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AvatarUserSettings fbid.
         * @member {string|null|undefined} fbid
         * @memberof proto.AvatarUserSettings
         * @instance
         */
        AvatarUserSettings.prototype.fbid = null;

        /**
         * AvatarUserSettings password.
         * @member {string|null|undefined} password
         * @memberof proto.AvatarUserSettings
         * @instance
         */
        AvatarUserSettings.prototype.password = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AvatarUserSettings.prototype, "_fbid", {
            get: $util.oneOfGetter($oneOfFields = ["fbid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AvatarUserSettings.prototype, "_password", {
            get: $util.oneOfGetter($oneOfFields = ["password"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new AvatarUserSettings instance using the specified properties.
         * @function create
         * @memberof proto.AvatarUserSettings
         * @static
         * @param {proto.IAvatarUserSettings=} [properties] Properties to set
         * @returns {proto.AvatarUserSettings} AvatarUserSettings instance
         */
        AvatarUserSettings.create = function create(properties) {
            return new AvatarUserSettings(properties);
        };

        /**
         * Encodes the specified AvatarUserSettings message. Does not implicitly {@link proto.AvatarUserSettings.verify|verify} messages.
         * @function encode
         * @memberof proto.AvatarUserSettings
         * @static
         * @param {proto.IAvatarUserSettings} message AvatarUserSettings message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AvatarUserSettings.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.fbid != null && Object.hasOwnProperty.call(message, "fbid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.fbid);
            if (message.password != null && Object.hasOwnProperty.call(message, "password"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.password);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified AvatarUserSettings message, length delimited. Does not implicitly {@link proto.AvatarUserSettings.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.AvatarUserSettings
         * @static
         * @param {proto.IAvatarUserSettings} message AvatarUserSettings message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AvatarUserSettings.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an AvatarUserSettings message from the specified reader or buffer.
         * @function decode
         * @memberof proto.AvatarUserSettings
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.AvatarUserSettings} AvatarUserSettings
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AvatarUserSettings.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.fbid = reader.stringVerify();
                        message._fbid = "fbid";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.password = reader.stringVerify();
                        message._password = "password";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes an AvatarUserSettings message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.AvatarUserSettings
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.AvatarUserSettings} AvatarUserSettings
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AvatarUserSettings.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AvatarUserSettings message.
         * @function verify
         * @memberof proto.AvatarUserSettings
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AvatarUserSettings.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.fbid != null && Object.hasOwnProperty.call(message, "fbid")) {
                properties._fbid = 1;
                if (!$util.isString(message.fbid))
                    return "fbid: string expected";
            }
            if (message.password != null && Object.hasOwnProperty.call(message, "password")) {
                properties._password = 1;
                if (!$util.isString(message.password))
                    return "password: string expected";
            }
            return null;
        };

        /**
         * Creates an AvatarUserSettings message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.AvatarUserSettings
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.AvatarUserSettings} AvatarUserSettings
         */
        AvatarUserSettings.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.AvatarUserSettings: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.fbid != null)
                message.fbid = String(object.fbid);
            if (object.password != null)
                message.password = String(object.password);
            return message;
        };

        /**
         * Creates a plain object from an AvatarUserSettings message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.AvatarUserSettings
         * @static
         * @param {proto.AvatarUserSettings} message AvatarUserSettings
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AvatarUserSettings.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.fbid != null && Object.hasOwnProperty.call(message, "fbid"))
                object.fbid = message.fbid;
            if (message.password != null && Object.hasOwnProperty.call(message, "password"))
                object.password = message.password;
            return object;
        };

        /**
         * Converts this AvatarUserSettings to JSON.
         * @function toJSON
         * @memberof proto.AvatarUserSettings
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AvatarUserSettings.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AvatarUserSettings
         * @function getTypeUrl
         * @memberof proto.AvatarUserSettings
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AvatarUserSettings.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.AvatarUserSettings";
        };

        return AvatarUserSettings;
    })();

    proto.BizAccountLinkInfo = (function() {

        /**
         * Properties of a BizAccountLinkInfo.
         * @memberof proto
         * @interface IBizAccountLinkInfo
         * @property {number|Long|null} [whatsappBizAcctFbid] BizAccountLinkInfo whatsappBizAcctFbid
         * @property {string|null} [whatsappAcctNumber] BizAccountLinkInfo whatsappAcctNumber
         * @property {number|Long|null} [issueTime] BizAccountLinkInfo issueTime
         * @property {proto.BizAccountLinkInfo.HostStorageType|null} [hostStorage] BizAccountLinkInfo hostStorage
         * @property {proto.BizAccountLinkInfo.AccountType|null} [accountType] BizAccountLinkInfo accountType
         */

        /**
         * Constructs a new BizAccountLinkInfo.
         * @memberof proto
         * @classdesc Represents a BizAccountLinkInfo.
         * @implements IBizAccountLinkInfo
         * @constructor
         * @param {proto.IBizAccountLinkInfo=} [properties] Properties to set
         */
        function BizAccountLinkInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BizAccountLinkInfo whatsappBizAcctFbid.
         * @member {number|Long|null|undefined} whatsappBizAcctFbid
         * @memberof proto.BizAccountLinkInfo
         * @instance
         */
        BizAccountLinkInfo.prototype.whatsappBizAcctFbid = null;

        /**
         * BizAccountLinkInfo whatsappAcctNumber.
         * @member {string|null|undefined} whatsappAcctNumber
         * @memberof proto.BizAccountLinkInfo
         * @instance
         */
        BizAccountLinkInfo.prototype.whatsappAcctNumber = null;

        /**
         * BizAccountLinkInfo issueTime.
         * @member {number|Long|null|undefined} issueTime
         * @memberof proto.BizAccountLinkInfo
         * @instance
         */
        BizAccountLinkInfo.prototype.issueTime = null;

        /**
         * BizAccountLinkInfo hostStorage.
         * @member {proto.BizAccountLinkInfo.HostStorageType|null|undefined} hostStorage
         * @memberof proto.BizAccountLinkInfo
         * @instance
         */
        BizAccountLinkInfo.prototype.hostStorage = null;

        /**
         * BizAccountLinkInfo accountType.
         * @member {proto.BizAccountLinkInfo.AccountType|null|undefined} accountType
         * @memberof proto.BizAccountLinkInfo
         * @instance
         */
        BizAccountLinkInfo.prototype.accountType = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BizAccountLinkInfo.prototype, "_whatsappBizAcctFbid", {
            get: $util.oneOfGetter($oneOfFields = ["whatsappBizAcctFbid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BizAccountLinkInfo.prototype, "_whatsappAcctNumber", {
            get: $util.oneOfGetter($oneOfFields = ["whatsappAcctNumber"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BizAccountLinkInfo.prototype, "_issueTime", {
            get: $util.oneOfGetter($oneOfFields = ["issueTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BizAccountLinkInfo.prototype, "_hostStorage", {
            get: $util.oneOfGetter($oneOfFields = ["hostStorage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BizAccountLinkInfo.prototype, "_accountType", {
            get: $util.oneOfGetter($oneOfFields = ["accountType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BizAccountLinkInfo instance using the specified properties.
         * @function create
         * @memberof proto.BizAccountLinkInfo
         * @static
         * @param {proto.IBizAccountLinkInfo=} [properties] Properties to set
         * @returns {proto.BizAccountLinkInfo} BizAccountLinkInfo instance
         */
        BizAccountLinkInfo.create = function create(properties) {
            return new BizAccountLinkInfo(properties);
        };

        /**
         * Encodes the specified BizAccountLinkInfo message. Does not implicitly {@link proto.BizAccountLinkInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.BizAccountLinkInfo
         * @static
         * @param {proto.IBizAccountLinkInfo} message BizAccountLinkInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BizAccountLinkInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.whatsappBizAcctFbid != null && Object.hasOwnProperty.call(message, "whatsappBizAcctFbid"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.whatsappBizAcctFbid);
            if (message.whatsappAcctNumber != null && Object.hasOwnProperty.call(message, "whatsappAcctNumber"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.whatsappAcctNumber);
            if (message.issueTime != null && Object.hasOwnProperty.call(message, "issueTime"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.issueTime);
            if (message.hostStorage != null && Object.hasOwnProperty.call(message, "hostStorage"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.hostStorage);
            if (message.accountType != null && Object.hasOwnProperty.call(message, "accountType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.accountType);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BizAccountLinkInfo message, length delimited. Does not implicitly {@link proto.BizAccountLinkInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BizAccountLinkInfo
         * @static
         * @param {proto.IBizAccountLinkInfo} message BizAccountLinkInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BizAccountLinkInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BizAccountLinkInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BizAccountLinkInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BizAccountLinkInfo} BizAccountLinkInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BizAccountLinkInfo.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.whatsappBizAcctFbid = reader.uint64();
                        message._whatsappBizAcctFbid = "whatsappBizAcctFbid";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.whatsappAcctNumber = reader.stringVerify();
                        message._whatsappAcctNumber = "whatsappAcctNumber";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.issueTime = reader.uint64();
                        message._issueTime = "issueTime";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.hostStorage = reader.int32();
                        message._hostStorage = "hostStorage";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.accountType = reader.int32();
                        message._accountType = "accountType";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BizAccountLinkInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BizAccountLinkInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BizAccountLinkInfo} BizAccountLinkInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BizAccountLinkInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BizAccountLinkInfo message.
         * @function verify
         * @memberof proto.BizAccountLinkInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BizAccountLinkInfo.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.whatsappBizAcctFbid != null && Object.hasOwnProperty.call(message, "whatsappBizAcctFbid")) {
                properties._whatsappBizAcctFbid = 1;
                if (!$util.isInteger(message.whatsappBizAcctFbid) && !(message.whatsappBizAcctFbid && $util.isInteger(message.whatsappBizAcctFbid.low) && $util.isInteger(message.whatsappBizAcctFbid.high)))
                    return "whatsappBizAcctFbid: integer|Long expected";
            }
            if (message.whatsappAcctNumber != null && Object.hasOwnProperty.call(message, "whatsappAcctNumber")) {
                properties._whatsappAcctNumber = 1;
                if (!$util.isString(message.whatsappAcctNumber))
                    return "whatsappAcctNumber: string expected";
            }
            if (message.issueTime != null && Object.hasOwnProperty.call(message, "issueTime")) {
                properties._issueTime = 1;
                if (!$util.isInteger(message.issueTime) && !(message.issueTime && $util.isInteger(message.issueTime.low) && $util.isInteger(message.issueTime.high)))
                    return "issueTime: integer|Long expected";
            }
            if (message.hostStorage != null && Object.hasOwnProperty.call(message, "hostStorage")) {
                properties._hostStorage = 1;
                if (typeof message.hostStorage !== "number" || (message.hostStorage | 0) !== message.hostStorage)
                    return "hostStorage: enum value expected";
            }
            if (message.accountType != null && Object.hasOwnProperty.call(message, "accountType")) {
                properties._accountType = 1;
                if (typeof message.accountType !== "number" || (message.accountType | 0) !== message.accountType)
                    return "accountType: enum value expected";
            }
            return null;
        };

        /**
         * Creates a BizAccountLinkInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BizAccountLinkInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BizAccountLinkInfo} BizAccountLinkInfo
         */
        BizAccountLinkInfo.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BizAccountLinkInfo: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.whatsappBizAcctFbid != null)
                if ($util.Long)
                    message.whatsappBizAcctFbid = $util.Long.fromValue(object.whatsappBizAcctFbid, true);
                else if (typeof object.whatsappBizAcctFbid === "string")
                    message.whatsappBizAcctFbid = parseInt(object.whatsappBizAcctFbid, 10);
                else if (typeof object.whatsappBizAcctFbid === "number")
                    message.whatsappBizAcctFbid = object.whatsappBizAcctFbid;
                else if (typeof object.whatsappBizAcctFbid === "object")
                    message.whatsappBizAcctFbid = new $util.LongBits(object.whatsappBizAcctFbid.low >>> 0, object.whatsappBizAcctFbid.high >>> 0).toNumber(true);
            if (object.whatsappAcctNumber != null)
                message.whatsappAcctNumber = String(object.whatsappAcctNumber);
            if (object.issueTime != null)
                if ($util.Long)
                    message.issueTime = $util.Long.fromValue(object.issueTime, true);
                else if (typeof object.issueTime === "string")
                    message.issueTime = parseInt(object.issueTime, 10);
                else if (typeof object.issueTime === "number")
                    message.issueTime = object.issueTime;
                else if (typeof object.issueTime === "object")
                    message.issueTime = new $util.LongBits(object.issueTime.low >>> 0, object.issueTime.high >>> 0).toNumber(true);
            switch (object.hostStorage) {
            case "ON_PREMISE":
            case 0:
                message.hostStorage = 0;
                break;
            case "FACEBOOK":
            case 1:
                message.hostStorage = 1;
                break;
            default:
                if (typeof object.hostStorage === "number" && (object.hostStorage | 0) === object.hostStorage)
                    message.hostStorage = object.hostStorage;
            }
            switch (object.accountType) {
            case "ENTERPRISE":
            case 0:
                message.accountType = 0;
                break;
            default:
                if (typeof object.accountType === "number" && (object.accountType | 0) === object.accountType)
                    message.accountType = object.accountType;
            }
            return message;
        };

        /**
         * Creates a plain object from a BizAccountLinkInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BizAccountLinkInfo
         * @static
         * @param {proto.BizAccountLinkInfo} message BizAccountLinkInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BizAccountLinkInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.whatsappBizAcctFbid != null && Object.hasOwnProperty.call(message, "whatsappBizAcctFbid"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.whatsappBizAcctFbid = typeof message.whatsappBizAcctFbid === "number" ? BigInt(message.whatsappBizAcctFbid) : $util.Long.fromBits(message.whatsappBizAcctFbid.low >>> 0, message.whatsappBizAcctFbid.high >>> 0, true).toBigInt();
                else if (typeof message.whatsappBizAcctFbid === "number")
                    object.whatsappBizAcctFbid = options.longs === String ? String(message.whatsappBizAcctFbid) : message.whatsappBizAcctFbid;
                else
                    object.whatsappBizAcctFbid = options.longs === String ? $util.Long.prototype.toString.call(message.whatsappBizAcctFbid) : options.longs === Number ? new $util.LongBits(message.whatsappBizAcctFbid.low >>> 0, message.whatsappBizAcctFbid.high >>> 0).toNumber(true) : message.whatsappBizAcctFbid;
            if (message.whatsappAcctNumber != null && Object.hasOwnProperty.call(message, "whatsappAcctNumber"))
                object.whatsappAcctNumber = message.whatsappAcctNumber;
            if (message.issueTime != null && Object.hasOwnProperty.call(message, "issueTime"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.issueTime = typeof message.issueTime === "number" ? BigInt(message.issueTime) : $util.Long.fromBits(message.issueTime.low >>> 0, message.issueTime.high >>> 0, true).toBigInt();
                else if (typeof message.issueTime === "number")
                    object.issueTime = options.longs === String ? String(message.issueTime) : message.issueTime;
                else
                    object.issueTime = options.longs === String ? $util.Long.prototype.toString.call(message.issueTime) : options.longs === Number ? new $util.LongBits(message.issueTime.low >>> 0, message.issueTime.high >>> 0).toNumber(true) : message.issueTime;
            if (message.hostStorage != null && Object.hasOwnProperty.call(message, "hostStorage"))
                object.hostStorage = options.enums === String ? $root.proto.BizAccountLinkInfo.HostStorageType[message.hostStorage] === undefined ? message.hostStorage : $root.proto.BizAccountLinkInfo.HostStorageType[message.hostStorage] : message.hostStorage;
            if (message.accountType != null && Object.hasOwnProperty.call(message, "accountType"))
                object.accountType = options.enums === String ? $root.proto.BizAccountLinkInfo.AccountType[message.accountType] === undefined ? message.accountType : $root.proto.BizAccountLinkInfo.AccountType[message.accountType] : message.accountType;
            return object;
        };

        /**
         * Converts this BizAccountLinkInfo to JSON.
         * @function toJSON
         * @memberof proto.BizAccountLinkInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BizAccountLinkInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BizAccountLinkInfo
         * @function getTypeUrl
         * @memberof proto.BizAccountLinkInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BizAccountLinkInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BizAccountLinkInfo";
        };

        /**
         * AccountType enum.
         * @name proto.BizAccountLinkInfo.AccountType
         * @enum {number}
         * @property {number} ENTERPRISE=0 ENTERPRISE value
         */
        BizAccountLinkInfo.AccountType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "ENTERPRISE"] = 0;
            return values;
        })();

        /**
         * HostStorageType enum.
         * @name proto.BizAccountLinkInfo.HostStorageType
         * @enum {number}
         * @property {number} ON_PREMISE=0 ON_PREMISE value
         * @property {number} FACEBOOK=1 FACEBOOK value
         */
        BizAccountLinkInfo.HostStorageType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "ON_PREMISE"] = 0;
            values[valuesById[1] = "FACEBOOK"] = 1;
            return values;
        })();

        return BizAccountLinkInfo;
    })();

    proto.BizAccountPayload = (function() {

        /**
         * Properties of a BizAccountPayload.
         * @memberof proto
         * @interface IBizAccountPayload
         * @property {proto.IVerifiedNameCertificate|null} [vnameCert] BizAccountPayload vnameCert
         * @property {Uint8Array|null} [bizAcctLinkInfo] BizAccountPayload bizAcctLinkInfo
         */

        /**
         * Constructs a new BizAccountPayload.
         * @memberof proto
         * @classdesc Represents a BizAccountPayload.
         * @implements IBizAccountPayload
         * @constructor
         * @param {proto.IBizAccountPayload=} [properties] Properties to set
         */
        function BizAccountPayload(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BizAccountPayload vnameCert.
         * @member {proto.IVerifiedNameCertificate|null|undefined} vnameCert
         * @memberof proto.BizAccountPayload
         * @instance
         */
        BizAccountPayload.prototype.vnameCert = null;

        /**
         * BizAccountPayload bizAcctLinkInfo.
         * @member {Uint8Array|null|undefined} bizAcctLinkInfo
         * @memberof proto.BizAccountPayload
         * @instance
         */
        BizAccountPayload.prototype.bizAcctLinkInfo = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BizAccountPayload.prototype, "_vnameCert", {
            get: $util.oneOfGetter($oneOfFields = ["vnameCert"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BizAccountPayload.prototype, "_bizAcctLinkInfo", {
            get: $util.oneOfGetter($oneOfFields = ["bizAcctLinkInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BizAccountPayload instance using the specified properties.
         * @function create
         * @memberof proto.BizAccountPayload
         * @static
         * @param {proto.IBizAccountPayload=} [properties] Properties to set
         * @returns {proto.BizAccountPayload} BizAccountPayload instance
         */
        BizAccountPayload.create = function create(properties) {
            return new BizAccountPayload(properties);
        };

        /**
         * Encodes the specified BizAccountPayload message. Does not implicitly {@link proto.BizAccountPayload.verify|verify} messages.
         * @function encode
         * @memberof proto.BizAccountPayload
         * @static
         * @param {proto.IBizAccountPayload} message BizAccountPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BizAccountPayload.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.vnameCert != null && Object.hasOwnProperty.call(message, "vnameCert"))
                $root.proto.VerifiedNameCertificate.encode(message.vnameCert, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.bizAcctLinkInfo != null && Object.hasOwnProperty.call(message, "bizAcctLinkInfo"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.bizAcctLinkInfo);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BizAccountPayload message, length delimited. Does not implicitly {@link proto.BizAccountPayload.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BizAccountPayload
         * @static
         * @param {proto.IBizAccountPayload} message BizAccountPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BizAccountPayload.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BizAccountPayload message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BizAccountPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BizAccountPayload} BizAccountPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BizAccountPayload.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.vnameCert = $root.proto.VerifiedNameCertificate.decode(reader, reader.uint32(), undefined, q + 1, message.vnameCert);
                        message._vnameCert = "vnameCert";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.bizAcctLinkInfo = reader.bytes();
                        message._bizAcctLinkInfo = "bizAcctLinkInfo";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BizAccountPayload message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BizAccountPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BizAccountPayload} BizAccountPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BizAccountPayload.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BizAccountPayload message.
         * @function verify
         * @memberof proto.BizAccountPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BizAccountPayload.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.vnameCert != null && Object.hasOwnProperty.call(message, "vnameCert")) {
                properties._vnameCert = 1;
                {
                    var error = $root.proto.VerifiedNameCertificate.verify(message.vnameCert, q + 1);
                    if (error)
                        return "vnameCert." + error;
                }
            }
            if (message.bizAcctLinkInfo != null && Object.hasOwnProperty.call(message, "bizAcctLinkInfo")) {
                properties._bizAcctLinkInfo = 1;
                if (!(message.bizAcctLinkInfo && typeof message.bizAcctLinkInfo.length === "number" || $util.isString(message.bizAcctLinkInfo)))
                    return "bizAcctLinkInfo: buffer expected";
            }
            return null;
        };

        /**
         * Creates a BizAccountPayload message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BizAccountPayload
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BizAccountPayload} BizAccountPayload
         */
        BizAccountPayload.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BizAccountPayload: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.vnameCert != null) {
                if (!$util.isObject(object.vnameCert))
                    throw TypeError(".proto.BizAccountPayload.vnameCert: object expected");
                message.vnameCert = $root.proto.VerifiedNameCertificate.fromObject(object.vnameCert, q + 1);
            }
            if (object.bizAcctLinkInfo != null)
                if (typeof object.bizAcctLinkInfo === "string")
                    $util.base64.decode(object.bizAcctLinkInfo, message.bizAcctLinkInfo = $util.newBuffer($util.base64.length(object.bizAcctLinkInfo)), 0);
                else if (object.bizAcctLinkInfo.length >= 0)
                    message.bizAcctLinkInfo = object.bizAcctLinkInfo;
            return message;
        };

        /**
         * Creates a plain object from a BizAccountPayload message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BizAccountPayload
         * @static
         * @param {proto.BizAccountPayload} message BizAccountPayload
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BizAccountPayload.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.vnameCert != null && Object.hasOwnProperty.call(message, "vnameCert"))
                object.vnameCert = $root.proto.VerifiedNameCertificate.toObject(message.vnameCert, options, q + 1);
            if (message.bizAcctLinkInfo != null && Object.hasOwnProperty.call(message, "bizAcctLinkInfo"))
                object.bizAcctLinkInfo = options.bytes === String ? $util.base64.encode(message.bizAcctLinkInfo, 0, message.bizAcctLinkInfo.length) : options.bytes === Array ? Array.prototype.slice.call(message.bizAcctLinkInfo) : message.bizAcctLinkInfo;
            return object;
        };

        /**
         * Converts this BizAccountPayload to JSON.
         * @function toJSON
         * @memberof proto.BizAccountPayload
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BizAccountPayload.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BizAccountPayload
         * @function getTypeUrl
         * @memberof proto.BizAccountPayload
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BizAccountPayload.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BizAccountPayload";
        };

        return BizAccountPayload;
    })();

    proto.BizIdentityInfo = (function() {

        /**
         * Properties of a BizIdentityInfo.
         * @memberof proto
         * @interface IBizIdentityInfo
         * @property {proto.BizIdentityInfo.VerifiedLevelValue|null} [vlevel] BizIdentityInfo vlevel
         * @property {proto.IVerifiedNameCertificate|null} [vnameCert] BizIdentityInfo vnameCert
         * @property {boolean|null} [signed] BizIdentityInfo signed
         * @property {boolean|null} [revoked] BizIdentityInfo revoked
         * @property {proto.BizIdentityInfo.HostStorageType|null} [hostStorage] BizIdentityInfo hostStorage
         * @property {proto.BizIdentityInfo.ActualActorsType|null} [actualActors] BizIdentityInfo actualActors
         * @property {number|Long|null} [privacyModeTs] BizIdentityInfo privacyModeTs
         * @property {number|Long|null} [featureControls] BizIdentityInfo featureControls
         */

        /**
         * Constructs a new BizIdentityInfo.
         * @memberof proto
         * @classdesc Represents a BizIdentityInfo.
         * @implements IBizIdentityInfo
         * @constructor
         * @param {proto.IBizIdentityInfo=} [properties] Properties to set
         */
        function BizIdentityInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BizIdentityInfo vlevel.
         * @member {proto.BizIdentityInfo.VerifiedLevelValue|null|undefined} vlevel
         * @memberof proto.BizIdentityInfo
         * @instance
         */
        BizIdentityInfo.prototype.vlevel = null;

        /**
         * BizIdentityInfo vnameCert.
         * @member {proto.IVerifiedNameCertificate|null|undefined} vnameCert
         * @memberof proto.BizIdentityInfo
         * @instance
         */
        BizIdentityInfo.prototype.vnameCert = null;

        /**
         * BizIdentityInfo signed.
         * @member {boolean|null|undefined} signed
         * @memberof proto.BizIdentityInfo
         * @instance
         */
        BizIdentityInfo.prototype.signed = null;

        /**
         * BizIdentityInfo revoked.
         * @member {boolean|null|undefined} revoked
         * @memberof proto.BizIdentityInfo
         * @instance
         */
        BizIdentityInfo.prototype.revoked = null;

        /**
         * BizIdentityInfo hostStorage.
         * @member {proto.BizIdentityInfo.HostStorageType|null|undefined} hostStorage
         * @memberof proto.BizIdentityInfo
         * @instance
         */
        BizIdentityInfo.prototype.hostStorage = null;

        /**
         * BizIdentityInfo actualActors.
         * @member {proto.BizIdentityInfo.ActualActorsType|null|undefined} actualActors
         * @memberof proto.BizIdentityInfo
         * @instance
         */
        BizIdentityInfo.prototype.actualActors = null;

        /**
         * BizIdentityInfo privacyModeTs.
         * @member {number|Long|null|undefined} privacyModeTs
         * @memberof proto.BizIdentityInfo
         * @instance
         */
        BizIdentityInfo.prototype.privacyModeTs = null;

        /**
         * BizIdentityInfo featureControls.
         * @member {number|Long|null|undefined} featureControls
         * @memberof proto.BizIdentityInfo
         * @instance
         */
        BizIdentityInfo.prototype.featureControls = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BizIdentityInfo.prototype, "_vlevel", {
            get: $util.oneOfGetter($oneOfFields = ["vlevel"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BizIdentityInfo.prototype, "_vnameCert", {
            get: $util.oneOfGetter($oneOfFields = ["vnameCert"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BizIdentityInfo.prototype, "_signed", {
            get: $util.oneOfGetter($oneOfFields = ["signed"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BizIdentityInfo.prototype, "_revoked", {
            get: $util.oneOfGetter($oneOfFields = ["revoked"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BizIdentityInfo.prototype, "_hostStorage", {
            get: $util.oneOfGetter($oneOfFields = ["hostStorage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BizIdentityInfo.prototype, "_actualActors", {
            get: $util.oneOfGetter($oneOfFields = ["actualActors"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BizIdentityInfo.prototype, "_privacyModeTs", {
            get: $util.oneOfGetter($oneOfFields = ["privacyModeTs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BizIdentityInfo.prototype, "_featureControls", {
            get: $util.oneOfGetter($oneOfFields = ["featureControls"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BizIdentityInfo instance using the specified properties.
         * @function create
         * @memberof proto.BizIdentityInfo
         * @static
         * @param {proto.IBizIdentityInfo=} [properties] Properties to set
         * @returns {proto.BizIdentityInfo} BizIdentityInfo instance
         */
        BizIdentityInfo.create = function create(properties) {
            return new BizIdentityInfo(properties);
        };

        /**
         * Encodes the specified BizIdentityInfo message. Does not implicitly {@link proto.BizIdentityInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.BizIdentityInfo
         * @static
         * @param {proto.IBizIdentityInfo} message BizIdentityInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BizIdentityInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.vlevel != null && Object.hasOwnProperty.call(message, "vlevel"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.vlevel);
            if (message.vnameCert != null && Object.hasOwnProperty.call(message, "vnameCert"))
                $root.proto.VerifiedNameCertificate.encode(message.vnameCert, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.signed != null && Object.hasOwnProperty.call(message, "signed"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.signed);
            if (message.revoked != null && Object.hasOwnProperty.call(message, "revoked"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.revoked);
            if (message.hostStorage != null && Object.hasOwnProperty.call(message, "hostStorage"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.hostStorage);
            if (message.actualActors != null && Object.hasOwnProperty.call(message, "actualActors"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.actualActors);
            if (message.privacyModeTs != null && Object.hasOwnProperty.call(message, "privacyModeTs"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.privacyModeTs);
            if (message.featureControls != null && Object.hasOwnProperty.call(message, "featureControls"))
                writer.uint32(/* id 8, wireType 0 =*/64).uint64(message.featureControls);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BizIdentityInfo message, length delimited. Does not implicitly {@link proto.BizIdentityInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BizIdentityInfo
         * @static
         * @param {proto.IBizIdentityInfo} message BizIdentityInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BizIdentityInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BizIdentityInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BizIdentityInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BizIdentityInfo} BizIdentityInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BizIdentityInfo.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.vlevel = reader.int32();
                        message._vlevel = "vlevel";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.vnameCert = $root.proto.VerifiedNameCertificate.decode(reader, reader.uint32(), undefined, q + 1, message.vnameCert);
                        message._vnameCert = "vnameCert";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.signed = reader.bool();
                        message._signed = "signed";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.revoked = reader.bool();
                        message._revoked = "revoked";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.hostStorage = reader.int32();
                        message._hostStorage = "hostStorage";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        message.actualActors = reader.int32();
                        message._actualActors = "actualActors";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        message.privacyModeTs = reader.uint64();
                        message._privacyModeTs = "privacyModeTs";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        message.featureControls = reader.uint64();
                        message._featureControls = "featureControls";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BizIdentityInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BizIdentityInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BizIdentityInfo} BizIdentityInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BizIdentityInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BizIdentityInfo message.
         * @function verify
         * @memberof proto.BizIdentityInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BizIdentityInfo.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.vlevel != null && Object.hasOwnProperty.call(message, "vlevel")) {
                properties._vlevel = 1;
                if (typeof message.vlevel !== "number" || (message.vlevel | 0) !== message.vlevel)
                    return "vlevel: enum value expected";
            }
            if (message.vnameCert != null && Object.hasOwnProperty.call(message, "vnameCert")) {
                properties._vnameCert = 1;
                {
                    var error = $root.proto.VerifiedNameCertificate.verify(message.vnameCert, q + 1);
                    if (error)
                        return "vnameCert." + error;
                }
            }
            if (message.signed != null && Object.hasOwnProperty.call(message, "signed")) {
                properties._signed = 1;
                if (typeof message.signed !== "boolean")
                    return "signed: boolean expected";
            }
            if (message.revoked != null && Object.hasOwnProperty.call(message, "revoked")) {
                properties._revoked = 1;
                if (typeof message.revoked !== "boolean")
                    return "revoked: boolean expected";
            }
            if (message.hostStorage != null && Object.hasOwnProperty.call(message, "hostStorage")) {
                properties._hostStorage = 1;
                if (typeof message.hostStorage !== "number" || (message.hostStorage | 0) !== message.hostStorage)
                    return "hostStorage: enum value expected";
            }
            if (message.actualActors != null && Object.hasOwnProperty.call(message, "actualActors")) {
                properties._actualActors = 1;
                if (typeof message.actualActors !== "number" || (message.actualActors | 0) !== message.actualActors)
                    return "actualActors: enum value expected";
            }
            if (message.privacyModeTs != null && Object.hasOwnProperty.call(message, "privacyModeTs")) {
                properties._privacyModeTs = 1;
                if (!$util.isInteger(message.privacyModeTs) && !(message.privacyModeTs && $util.isInteger(message.privacyModeTs.low) && $util.isInteger(message.privacyModeTs.high)))
                    return "privacyModeTs: integer|Long expected";
            }
            if (message.featureControls != null && Object.hasOwnProperty.call(message, "featureControls")) {
                properties._featureControls = 1;
                if (!$util.isInteger(message.featureControls) && !(message.featureControls && $util.isInteger(message.featureControls.low) && $util.isInteger(message.featureControls.high)))
                    return "featureControls: integer|Long expected";
            }
            return null;
        };

        /**
         * Creates a BizIdentityInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BizIdentityInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BizIdentityInfo} BizIdentityInfo
         */
        BizIdentityInfo.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BizIdentityInfo: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.vlevel) {
            case "UNKNOWN":
            case 0:
                message.vlevel = 0;
                break;
            case "LOW":
            case 1:
                message.vlevel = 1;
                break;
            case "HIGH":
            case 2:
                message.vlevel = 2;
                break;
            default:
                if (typeof object.vlevel === "number" && (object.vlevel | 0) === object.vlevel)
                    message.vlevel = object.vlevel;
            }
            if (object.vnameCert != null) {
                if (!$util.isObject(object.vnameCert))
                    throw TypeError(".proto.BizIdentityInfo.vnameCert: object expected");
                message.vnameCert = $root.proto.VerifiedNameCertificate.fromObject(object.vnameCert, q + 1);
            }
            if (object.signed != null)
                message.signed = Boolean(object.signed);
            if (object.revoked != null)
                message.revoked = Boolean(object.revoked);
            switch (object.hostStorage) {
            case "ON_PREMISE":
            case 0:
                message.hostStorage = 0;
                break;
            case "FACEBOOK":
            case 1:
                message.hostStorage = 1;
                break;
            default:
                if (typeof object.hostStorage === "number" && (object.hostStorage | 0) === object.hostStorage)
                    message.hostStorage = object.hostStorage;
            }
            switch (object.actualActors) {
            case "SELF":
            case 0:
                message.actualActors = 0;
                break;
            case "BSP":
            case 1:
                message.actualActors = 1;
                break;
            default:
                if (typeof object.actualActors === "number" && (object.actualActors | 0) === object.actualActors)
                    message.actualActors = object.actualActors;
            }
            if (object.privacyModeTs != null)
                if ($util.Long)
                    message.privacyModeTs = $util.Long.fromValue(object.privacyModeTs, true);
                else if (typeof object.privacyModeTs === "string")
                    message.privacyModeTs = parseInt(object.privacyModeTs, 10);
                else if (typeof object.privacyModeTs === "number")
                    message.privacyModeTs = object.privacyModeTs;
                else if (typeof object.privacyModeTs === "object")
                    message.privacyModeTs = new $util.LongBits(object.privacyModeTs.low >>> 0, object.privacyModeTs.high >>> 0).toNumber(true);
            if (object.featureControls != null)
                if ($util.Long)
                    message.featureControls = $util.Long.fromValue(object.featureControls, true);
                else if (typeof object.featureControls === "string")
                    message.featureControls = parseInt(object.featureControls, 10);
                else if (typeof object.featureControls === "number")
                    message.featureControls = object.featureControls;
                else if (typeof object.featureControls === "object")
                    message.featureControls = new $util.LongBits(object.featureControls.low >>> 0, object.featureControls.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a BizIdentityInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BizIdentityInfo
         * @static
         * @param {proto.BizIdentityInfo} message BizIdentityInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BizIdentityInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.vlevel != null && Object.hasOwnProperty.call(message, "vlevel"))
                object.vlevel = options.enums === String ? $root.proto.BizIdentityInfo.VerifiedLevelValue[message.vlevel] === undefined ? message.vlevel : $root.proto.BizIdentityInfo.VerifiedLevelValue[message.vlevel] : message.vlevel;
            if (message.vnameCert != null && Object.hasOwnProperty.call(message, "vnameCert"))
                object.vnameCert = $root.proto.VerifiedNameCertificate.toObject(message.vnameCert, options, q + 1);
            if (message.signed != null && Object.hasOwnProperty.call(message, "signed"))
                object.signed = message.signed;
            if (message.revoked != null && Object.hasOwnProperty.call(message, "revoked"))
                object.revoked = message.revoked;
            if (message.hostStorage != null && Object.hasOwnProperty.call(message, "hostStorage"))
                object.hostStorage = options.enums === String ? $root.proto.BizIdentityInfo.HostStorageType[message.hostStorage] === undefined ? message.hostStorage : $root.proto.BizIdentityInfo.HostStorageType[message.hostStorage] : message.hostStorage;
            if (message.actualActors != null && Object.hasOwnProperty.call(message, "actualActors"))
                object.actualActors = options.enums === String ? $root.proto.BizIdentityInfo.ActualActorsType[message.actualActors] === undefined ? message.actualActors : $root.proto.BizIdentityInfo.ActualActorsType[message.actualActors] : message.actualActors;
            if (message.privacyModeTs != null && Object.hasOwnProperty.call(message, "privacyModeTs"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.privacyModeTs = typeof message.privacyModeTs === "number" ? BigInt(message.privacyModeTs) : $util.Long.fromBits(message.privacyModeTs.low >>> 0, message.privacyModeTs.high >>> 0, true).toBigInt();
                else if (typeof message.privacyModeTs === "number")
                    object.privacyModeTs = options.longs === String ? String(message.privacyModeTs) : message.privacyModeTs;
                else
                    object.privacyModeTs = options.longs === String ? $util.Long.prototype.toString.call(message.privacyModeTs) : options.longs === Number ? new $util.LongBits(message.privacyModeTs.low >>> 0, message.privacyModeTs.high >>> 0).toNumber(true) : message.privacyModeTs;
            if (message.featureControls != null && Object.hasOwnProperty.call(message, "featureControls"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.featureControls = typeof message.featureControls === "number" ? BigInt(message.featureControls) : $util.Long.fromBits(message.featureControls.low >>> 0, message.featureControls.high >>> 0, true).toBigInt();
                else if (typeof message.featureControls === "number")
                    object.featureControls = options.longs === String ? String(message.featureControls) : message.featureControls;
                else
                    object.featureControls = options.longs === String ? $util.Long.prototype.toString.call(message.featureControls) : options.longs === Number ? new $util.LongBits(message.featureControls.low >>> 0, message.featureControls.high >>> 0).toNumber(true) : message.featureControls;
            return object;
        };

        /**
         * Converts this BizIdentityInfo to JSON.
         * @function toJSON
         * @memberof proto.BizIdentityInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BizIdentityInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BizIdentityInfo
         * @function getTypeUrl
         * @memberof proto.BizIdentityInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BizIdentityInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BizIdentityInfo";
        };

        /**
         * ActualActorsType enum.
         * @name proto.BizIdentityInfo.ActualActorsType
         * @enum {number}
         * @property {number} SELF=0 SELF value
         * @property {number} BSP=1 BSP value
         */
        BizIdentityInfo.ActualActorsType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "SELF"] = 0;
            values[valuesById[1] = "BSP"] = 1;
            return values;
        })();

        /**
         * HostStorageType enum.
         * @name proto.BizIdentityInfo.HostStorageType
         * @enum {number}
         * @property {number} ON_PREMISE=0 ON_PREMISE value
         * @property {number} FACEBOOK=1 FACEBOOK value
         */
        BizIdentityInfo.HostStorageType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "ON_PREMISE"] = 0;
            values[valuesById[1] = "FACEBOOK"] = 1;
            return values;
        })();

        /**
         * VerifiedLevelValue enum.
         * @name proto.BizIdentityInfo.VerifiedLevelValue
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} LOW=1 LOW value
         * @property {number} HIGH=2 HIGH value
         */
        BizIdentityInfo.VerifiedLevelValue = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "LOW"] = 1;
            values[valuesById[2] = "HIGH"] = 2;
            return values;
        })();

        return BizIdentityInfo;
    })();

    proto.BotAgeCollectionMetadata = (function() {

        /**
         * Properties of a BotAgeCollectionMetadata.
         * @memberof proto
         * @interface IBotAgeCollectionMetadata
         * @property {boolean|null} [ageCollectionEligible] BotAgeCollectionMetadata ageCollectionEligible
         * @property {boolean|null} [shouldTriggerAgeCollectionOnClient] BotAgeCollectionMetadata shouldTriggerAgeCollectionOnClient
         * @property {proto.BotAgeCollectionMetadata.AgeCollectionType|null} [ageCollectionType] BotAgeCollectionMetadata ageCollectionType
         */

        /**
         * Constructs a new BotAgeCollectionMetadata.
         * @memberof proto
         * @classdesc Represents a BotAgeCollectionMetadata.
         * @implements IBotAgeCollectionMetadata
         * @constructor
         * @param {proto.IBotAgeCollectionMetadata=} [properties] Properties to set
         */
        function BotAgeCollectionMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotAgeCollectionMetadata ageCollectionEligible.
         * @member {boolean|null|undefined} ageCollectionEligible
         * @memberof proto.BotAgeCollectionMetadata
         * @instance
         */
        BotAgeCollectionMetadata.prototype.ageCollectionEligible = null;

        /**
         * BotAgeCollectionMetadata shouldTriggerAgeCollectionOnClient.
         * @member {boolean|null|undefined} shouldTriggerAgeCollectionOnClient
         * @memberof proto.BotAgeCollectionMetadata
         * @instance
         */
        BotAgeCollectionMetadata.prototype.shouldTriggerAgeCollectionOnClient = null;

        /**
         * BotAgeCollectionMetadata ageCollectionType.
         * @member {proto.BotAgeCollectionMetadata.AgeCollectionType|null|undefined} ageCollectionType
         * @memberof proto.BotAgeCollectionMetadata
         * @instance
         */
        BotAgeCollectionMetadata.prototype.ageCollectionType = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotAgeCollectionMetadata.prototype, "_ageCollectionEligible", {
            get: $util.oneOfGetter($oneOfFields = ["ageCollectionEligible"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotAgeCollectionMetadata.prototype, "_shouldTriggerAgeCollectionOnClient", {
            get: $util.oneOfGetter($oneOfFields = ["shouldTriggerAgeCollectionOnClient"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotAgeCollectionMetadata.prototype, "_ageCollectionType", {
            get: $util.oneOfGetter($oneOfFields = ["ageCollectionType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotAgeCollectionMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotAgeCollectionMetadata
         * @static
         * @param {proto.IBotAgeCollectionMetadata=} [properties] Properties to set
         * @returns {proto.BotAgeCollectionMetadata} BotAgeCollectionMetadata instance
         */
        BotAgeCollectionMetadata.create = function create(properties) {
            return new BotAgeCollectionMetadata(properties);
        };

        /**
         * Encodes the specified BotAgeCollectionMetadata message. Does not implicitly {@link proto.BotAgeCollectionMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotAgeCollectionMetadata
         * @static
         * @param {proto.IBotAgeCollectionMetadata} message BotAgeCollectionMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotAgeCollectionMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ageCollectionEligible != null && Object.hasOwnProperty.call(message, "ageCollectionEligible"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.ageCollectionEligible);
            if (message.shouldTriggerAgeCollectionOnClient != null && Object.hasOwnProperty.call(message, "shouldTriggerAgeCollectionOnClient"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.shouldTriggerAgeCollectionOnClient);
            if (message.ageCollectionType != null && Object.hasOwnProperty.call(message, "ageCollectionType"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.ageCollectionType);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotAgeCollectionMetadata message, length delimited. Does not implicitly {@link proto.BotAgeCollectionMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotAgeCollectionMetadata
         * @static
         * @param {proto.IBotAgeCollectionMetadata} message BotAgeCollectionMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotAgeCollectionMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotAgeCollectionMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotAgeCollectionMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotAgeCollectionMetadata} BotAgeCollectionMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotAgeCollectionMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.ageCollectionEligible = reader.bool();
                        message._ageCollectionEligible = "ageCollectionEligible";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.shouldTriggerAgeCollectionOnClient = reader.bool();
                        message._shouldTriggerAgeCollectionOnClient = "shouldTriggerAgeCollectionOnClient";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.ageCollectionType = reader.int32();
                        message._ageCollectionType = "ageCollectionType";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotAgeCollectionMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotAgeCollectionMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotAgeCollectionMetadata} BotAgeCollectionMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotAgeCollectionMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotAgeCollectionMetadata message.
         * @function verify
         * @memberof proto.BotAgeCollectionMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotAgeCollectionMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.ageCollectionEligible != null && Object.hasOwnProperty.call(message, "ageCollectionEligible")) {
                properties._ageCollectionEligible = 1;
                if (typeof message.ageCollectionEligible !== "boolean")
                    return "ageCollectionEligible: boolean expected";
            }
            if (message.shouldTriggerAgeCollectionOnClient != null && Object.hasOwnProperty.call(message, "shouldTriggerAgeCollectionOnClient")) {
                properties._shouldTriggerAgeCollectionOnClient = 1;
                if (typeof message.shouldTriggerAgeCollectionOnClient !== "boolean")
                    return "shouldTriggerAgeCollectionOnClient: boolean expected";
            }
            if (message.ageCollectionType != null && Object.hasOwnProperty.call(message, "ageCollectionType")) {
                properties._ageCollectionType = 1;
                if (typeof message.ageCollectionType !== "number" || (message.ageCollectionType | 0) !== message.ageCollectionType)
                    return "ageCollectionType: enum value expected";
            }
            return null;
        };

        /**
         * Creates a BotAgeCollectionMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotAgeCollectionMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotAgeCollectionMetadata} BotAgeCollectionMetadata
         */
        BotAgeCollectionMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotAgeCollectionMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.ageCollectionEligible != null)
                message.ageCollectionEligible = Boolean(object.ageCollectionEligible);
            if (object.shouldTriggerAgeCollectionOnClient != null)
                message.shouldTriggerAgeCollectionOnClient = Boolean(object.shouldTriggerAgeCollectionOnClient);
            switch (object.ageCollectionType) {
            case "O18_BINARY":
            case 0:
                message.ageCollectionType = 0;
                break;
            case "WAFFLE":
            case 1:
                message.ageCollectionType = 1;
                break;
            default:
                if (typeof object.ageCollectionType === "number" && (object.ageCollectionType | 0) === object.ageCollectionType)
                    message.ageCollectionType = object.ageCollectionType;
            }
            return message;
        };

        /**
         * Creates a plain object from a BotAgeCollectionMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotAgeCollectionMetadata
         * @static
         * @param {proto.BotAgeCollectionMetadata} message BotAgeCollectionMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotAgeCollectionMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.ageCollectionEligible != null && Object.hasOwnProperty.call(message, "ageCollectionEligible"))
                object.ageCollectionEligible = message.ageCollectionEligible;
            if (message.shouldTriggerAgeCollectionOnClient != null && Object.hasOwnProperty.call(message, "shouldTriggerAgeCollectionOnClient"))
                object.shouldTriggerAgeCollectionOnClient = message.shouldTriggerAgeCollectionOnClient;
            if (message.ageCollectionType != null && Object.hasOwnProperty.call(message, "ageCollectionType"))
                object.ageCollectionType = options.enums === String ? $root.proto.BotAgeCollectionMetadata.AgeCollectionType[message.ageCollectionType] === undefined ? message.ageCollectionType : $root.proto.BotAgeCollectionMetadata.AgeCollectionType[message.ageCollectionType] : message.ageCollectionType;
            return object;
        };

        /**
         * Converts this BotAgeCollectionMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotAgeCollectionMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotAgeCollectionMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotAgeCollectionMetadata
         * @function getTypeUrl
         * @memberof proto.BotAgeCollectionMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotAgeCollectionMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotAgeCollectionMetadata";
        };

        /**
         * AgeCollectionType enum.
         * @name proto.BotAgeCollectionMetadata.AgeCollectionType
         * @enum {number}
         * @property {number} O18_BINARY=0 O18_BINARY value
         * @property {number} WAFFLE=1 WAFFLE value
         */
        BotAgeCollectionMetadata.AgeCollectionType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "O18_BINARY"] = 0;
            values[valuesById[1] = "WAFFLE"] = 1;
            return values;
        })();

        return BotAgeCollectionMetadata;
    })();

    proto.BotAgentDeepLinkMetadata = (function() {

        /**
         * Properties of a BotAgentDeepLinkMetadata.
         * @memberof proto
         * @interface IBotAgentDeepLinkMetadata
         * @property {string|null} [token] BotAgentDeepLinkMetadata token
         */

        /**
         * Constructs a new BotAgentDeepLinkMetadata.
         * @memberof proto
         * @classdesc Represents a BotAgentDeepLinkMetadata.
         * @implements IBotAgentDeepLinkMetadata
         * @constructor
         * @param {proto.IBotAgentDeepLinkMetadata=} [properties] Properties to set
         */
        function BotAgentDeepLinkMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotAgentDeepLinkMetadata token.
         * @member {string|null|undefined} token
         * @memberof proto.BotAgentDeepLinkMetadata
         * @instance
         */
        BotAgentDeepLinkMetadata.prototype.token = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotAgentDeepLinkMetadata.prototype, "_token", {
            get: $util.oneOfGetter($oneOfFields = ["token"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotAgentDeepLinkMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotAgentDeepLinkMetadata
         * @static
         * @param {proto.IBotAgentDeepLinkMetadata=} [properties] Properties to set
         * @returns {proto.BotAgentDeepLinkMetadata} BotAgentDeepLinkMetadata instance
         */
        BotAgentDeepLinkMetadata.create = function create(properties) {
            return new BotAgentDeepLinkMetadata(properties);
        };

        /**
         * Encodes the specified BotAgentDeepLinkMetadata message. Does not implicitly {@link proto.BotAgentDeepLinkMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotAgentDeepLinkMetadata
         * @static
         * @param {proto.IBotAgentDeepLinkMetadata} message BotAgentDeepLinkMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotAgentDeepLinkMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.token);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotAgentDeepLinkMetadata message, length delimited. Does not implicitly {@link proto.BotAgentDeepLinkMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotAgentDeepLinkMetadata
         * @static
         * @param {proto.IBotAgentDeepLinkMetadata} message BotAgentDeepLinkMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotAgentDeepLinkMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotAgentDeepLinkMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotAgentDeepLinkMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotAgentDeepLinkMetadata} BotAgentDeepLinkMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotAgentDeepLinkMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.token = reader.stringVerify();
                        message._token = "token";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotAgentDeepLinkMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotAgentDeepLinkMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotAgentDeepLinkMetadata} BotAgentDeepLinkMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotAgentDeepLinkMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotAgentDeepLinkMetadata message.
         * @function verify
         * @memberof proto.BotAgentDeepLinkMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotAgentDeepLinkMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.token != null && Object.hasOwnProperty.call(message, "token")) {
                properties._token = 1;
                if (!$util.isString(message.token))
                    return "token: string expected";
            }
            return null;
        };

        /**
         * Creates a BotAgentDeepLinkMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotAgentDeepLinkMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotAgentDeepLinkMetadata} BotAgentDeepLinkMetadata
         */
        BotAgentDeepLinkMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotAgentDeepLinkMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.token != null)
                message.token = String(object.token);
            return message;
        };

        /**
         * Creates a plain object from a BotAgentDeepLinkMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotAgentDeepLinkMetadata
         * @static
         * @param {proto.BotAgentDeepLinkMetadata} message BotAgentDeepLinkMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotAgentDeepLinkMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                object.token = message.token;
            return object;
        };

        /**
         * Converts this BotAgentDeepLinkMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotAgentDeepLinkMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotAgentDeepLinkMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotAgentDeepLinkMetadata
         * @function getTypeUrl
         * @memberof proto.BotAgentDeepLinkMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotAgentDeepLinkMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotAgentDeepLinkMetadata";
        };

        return BotAgentDeepLinkMetadata;
    })();

    proto.BotAgentMetadata = (function() {

        /**
         * Properties of a BotAgentMetadata.
         * @memberof proto
         * @interface IBotAgentMetadata
         * @property {proto.IBotAgentDeepLinkMetadata|null} [deepLinkMetadata] BotAgentMetadata deepLinkMetadata
         */

        /**
         * Constructs a new BotAgentMetadata.
         * @memberof proto
         * @classdesc Represents a BotAgentMetadata.
         * @implements IBotAgentMetadata
         * @constructor
         * @param {proto.IBotAgentMetadata=} [properties] Properties to set
         */
        function BotAgentMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotAgentMetadata deepLinkMetadata.
         * @member {proto.IBotAgentDeepLinkMetadata|null|undefined} deepLinkMetadata
         * @memberof proto.BotAgentMetadata
         * @instance
         */
        BotAgentMetadata.prototype.deepLinkMetadata = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotAgentMetadata.prototype, "_deepLinkMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["deepLinkMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotAgentMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotAgentMetadata
         * @static
         * @param {proto.IBotAgentMetadata=} [properties] Properties to set
         * @returns {proto.BotAgentMetadata} BotAgentMetadata instance
         */
        BotAgentMetadata.create = function create(properties) {
            return new BotAgentMetadata(properties);
        };

        /**
         * Encodes the specified BotAgentMetadata message. Does not implicitly {@link proto.BotAgentMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotAgentMetadata
         * @static
         * @param {proto.IBotAgentMetadata} message BotAgentMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotAgentMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.deepLinkMetadata != null && Object.hasOwnProperty.call(message, "deepLinkMetadata"))
                $root.proto.BotAgentDeepLinkMetadata.encode(message.deepLinkMetadata, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotAgentMetadata message, length delimited. Does not implicitly {@link proto.BotAgentMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotAgentMetadata
         * @static
         * @param {proto.IBotAgentMetadata} message BotAgentMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotAgentMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotAgentMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotAgentMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotAgentMetadata} BotAgentMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotAgentMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.deepLinkMetadata = $root.proto.BotAgentDeepLinkMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.deepLinkMetadata);
                        message._deepLinkMetadata = "deepLinkMetadata";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotAgentMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotAgentMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotAgentMetadata} BotAgentMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotAgentMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotAgentMetadata message.
         * @function verify
         * @memberof proto.BotAgentMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotAgentMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.deepLinkMetadata != null && Object.hasOwnProperty.call(message, "deepLinkMetadata")) {
                properties._deepLinkMetadata = 1;
                {
                    var error = $root.proto.BotAgentDeepLinkMetadata.verify(message.deepLinkMetadata, q + 1);
                    if (error)
                        return "deepLinkMetadata." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BotAgentMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotAgentMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotAgentMetadata} BotAgentMetadata
         */
        BotAgentMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotAgentMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.deepLinkMetadata != null) {
                if (!$util.isObject(object.deepLinkMetadata))
                    throw TypeError(".proto.BotAgentMetadata.deepLinkMetadata: object expected");
                message.deepLinkMetadata = $root.proto.BotAgentDeepLinkMetadata.fromObject(object.deepLinkMetadata, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a BotAgentMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotAgentMetadata
         * @static
         * @param {proto.BotAgentMetadata} message BotAgentMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotAgentMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.deepLinkMetadata != null && Object.hasOwnProperty.call(message, "deepLinkMetadata"))
                object.deepLinkMetadata = $root.proto.BotAgentDeepLinkMetadata.toObject(message.deepLinkMetadata, options, q + 1);
            return object;
        };

        /**
         * Converts this BotAgentMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotAgentMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotAgentMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotAgentMetadata
         * @function getTypeUrl
         * @memberof proto.BotAgentMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotAgentMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotAgentMetadata";
        };

        return BotAgentMetadata;
    })();

    proto.BotCapabilityMetadata = (function() {

        /**
         * Properties of a BotCapabilityMetadata.
         * @memberof proto
         * @interface IBotCapabilityMetadata
         * @property {Array.<proto.BotCapabilityMetadata.BotCapabilityType>|null} [capabilities] BotCapabilityMetadata capabilities
         */

        /**
         * Constructs a new BotCapabilityMetadata.
         * @memberof proto
         * @classdesc Represents a BotCapabilityMetadata.
         * @implements IBotCapabilityMetadata
         * @constructor
         * @param {proto.IBotCapabilityMetadata=} [properties] Properties to set
         */
        function BotCapabilityMetadata(properties) {
            this.capabilities = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotCapabilityMetadata capabilities.
         * @member {Array.<proto.BotCapabilityMetadata.BotCapabilityType>} capabilities
         * @memberof proto.BotCapabilityMetadata
         * @instance
         */
        BotCapabilityMetadata.prototype.capabilities = $util.emptyArray;

        /**
         * Creates a new BotCapabilityMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotCapabilityMetadata
         * @static
         * @param {proto.IBotCapabilityMetadata=} [properties] Properties to set
         * @returns {proto.BotCapabilityMetadata} BotCapabilityMetadata instance
         */
        BotCapabilityMetadata.create = function create(properties) {
            return new BotCapabilityMetadata(properties);
        };

        /**
         * Encodes the specified BotCapabilityMetadata message. Does not implicitly {@link proto.BotCapabilityMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotCapabilityMetadata
         * @static
         * @param {proto.IBotCapabilityMetadata} message BotCapabilityMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotCapabilityMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.capabilities != null && message.capabilities.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (var i = 0; i < message.capabilities.length; ++i)
                    writer.int32(message.capabilities[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotCapabilityMetadata message, length delimited. Does not implicitly {@link proto.BotCapabilityMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotCapabilityMetadata
         * @static
         * @param {proto.IBotCapabilityMetadata} message BotCapabilityMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotCapabilityMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotCapabilityMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotCapabilityMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotCapabilityMetadata} BotCapabilityMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotCapabilityMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u === 2) {
                            if (!(message.capabilities && message.capabilities.length))
                                message.capabilities = [];
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.capabilities.push(reader.int32());
                            continue;
                        }
                        if (u !== 0)
                            break;
                        if (!(message.capabilities && message.capabilities.length))
                            message.capabilities = [];
                        message.capabilities.push(reader.int32());
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotCapabilityMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotCapabilityMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotCapabilityMetadata} BotCapabilityMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotCapabilityMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotCapabilityMetadata message.
         * @function verify
         * @memberof proto.BotCapabilityMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotCapabilityMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            if (message.capabilities != null && Object.hasOwnProperty.call(message, "capabilities")) {
                if (!Array.isArray(message.capabilities))
                    return "capabilities: array expected";
                for (var i = 0; i < message.capabilities.length; ++i)
                    if (typeof message.capabilities[i] !== "number" || (message.capabilities[i] | 0) !== message.capabilities[i])
                        return "capabilities: enum value[] expected";
            }
            return null;
        };

        /**
         * Creates a BotCapabilityMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotCapabilityMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotCapabilityMetadata} BotCapabilityMetadata
         */
        BotCapabilityMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotCapabilityMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.capabilities) {
                if (!Array.isArray(object.capabilities))
                    throw TypeError(".proto.BotCapabilityMetadata.capabilities: array expected");
                message.capabilities = [];
                for (var i = 0; i < object.capabilities.length; ++i)
                    switch (object.capabilities[i]) {
                    case "UNKNOWN":
                    case 0:
                        message.capabilities[message.capabilities.length] = 0;
                        break;
                    case "PROGRESS_INDICATOR":
                    case 1:
                        message.capabilities[message.capabilities.length] = 1;
                        break;
                    case "RICH_RESPONSE_HEADING":
                    case 2:
                        message.capabilities[message.capabilities.length] = 2;
                        break;
                    case "RICH_RESPONSE_NESTED_LIST":
                    case 3:
                        message.capabilities[message.capabilities.length] = 3;
                        break;
                    case "AI_MEMORY":
                    case 4:
                        message.capabilities[message.capabilities.length] = 4;
                        break;
                    case "RICH_RESPONSE_THREAD_SURFING":
                    case 5:
                        message.capabilities[message.capabilities.length] = 5;
                        break;
                    case "RICH_RESPONSE_TABLE":
                    case 6:
                        message.capabilities[message.capabilities.length] = 6;
                        break;
                    case "RICH_RESPONSE_CODE":
                    case 7:
                        message.capabilities[message.capabilities.length] = 7;
                        break;
                    case "RICH_RESPONSE_STRUCTURED_RESPONSE":
                    case 8:
                        message.capabilities[message.capabilities.length] = 8;
                        break;
                    case "RICH_RESPONSE_INLINE_IMAGE":
                    case 9:
                        message.capabilities[message.capabilities.length] = 9;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_CONTROL":
                    case 10:
                        message.capabilities[message.capabilities.length] = 10;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_1":
                    case 11:
                        message.capabilities[message.capabilities.length] = 11;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_2":
                    case 12:
                        message.capabilities[message.capabilities.length] = 12;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_3":
                    case 13:
                        message.capabilities[message.capabilities.length] = 13;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_4":
                    case 14:
                        message.capabilities[message.capabilities.length] = 14;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_5":
                    case 15:
                        message.capabilities[message.capabilities.length] = 15;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_6":
                    case 16:
                        message.capabilities[message.capabilities.length] = 16;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_7":
                    case 17:
                        message.capabilities[message.capabilities.length] = 17;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_8":
                    case 18:
                        message.capabilities[message.capabilities.length] = 18;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_9":
                    case 19:
                        message.capabilities[message.capabilities.length] = 19;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_10":
                    case 20:
                        message.capabilities[message.capabilities.length] = 20;
                        break;
                    case "RICH_RESPONSE_SUB_HEADING":
                    case 21:
                        message.capabilities[message.capabilities.length] = 21;
                        break;
                    case "RICH_RESPONSE_GRID_IMAGE":
                    case 22:
                        message.capabilities[message.capabilities.length] = 22;
                        break;
                    case "AI_STUDIO_UGC_MEMORY":
                    case 23:
                        message.capabilities[message.capabilities.length] = 23;
                        break;
                    case "RICH_RESPONSE_LATEX":
                    case 24:
                        message.capabilities[message.capabilities.length] = 24;
                        break;
                    case "RICH_RESPONSE_MAPS":
                    case 25:
                        message.capabilities[message.capabilities.length] = 25;
                        break;
                    case "RICH_RESPONSE_INLINE_REELS":
                    case 26:
                        message.capabilities[message.capabilities.length] = 26;
                        break;
                    case "AGENTIC_PLANNING":
                    case 27:
                        message.capabilities[message.capabilities.length] = 27;
                        break;
                    case "ACCOUNT_LINKING":
                    case 28:
                        message.capabilities[message.capabilities.length] = 28;
                        break;
                    case "STREAMING_DISAGGREGATION":
                    case 29:
                        message.capabilities[message.capabilities.length] = 29;
                        break;
                    case "RICH_RESPONSE_GRID_IMAGE_3P":
                    case 30:
                        message.capabilities[message.capabilities.length] = 30;
                        break;
                    case "RICH_RESPONSE_LATEX_INLINE":
                    case 31:
                        message.capabilities[message.capabilities.length] = 31;
                        break;
                    case "QUERY_PLAN":
                    case 32:
                        message.capabilities[message.capabilities.length] = 32;
                        break;
                    case "PROACTIVE_MESSAGE":
                    case 33:
                        message.capabilities[message.capabilities.length] = 33;
                        break;
                    case "RICH_RESPONSE_UNIFIED_RESPONSE":
                    case 34:
                        message.capabilities[message.capabilities.length] = 34;
                        break;
                    case "PROMOTION_MESSAGE":
                    case 35:
                        message.capabilities[message.capabilities.length] = 35;
                        break;
                    case "SIMPLIFIED_PROFILE_PAGE":
                    case 36:
                        message.capabilities[message.capabilities.length] = 36;
                        break;
                    case "RICH_RESPONSE_SOURCES_IN_MESSAGE":
                    case 37:
                        message.capabilities[message.capabilities.length] = 37;
                        break;
                    case "RICH_RESPONSE_SIDE_BY_SIDE_SURVEY":
                    case 38:
                        message.capabilities[message.capabilities.length] = 38;
                        break;
                    case "RICH_RESPONSE_UNIFIED_TEXT_COMPONENT":
                    case 39:
                        message.capabilities[message.capabilities.length] = 39;
                        break;
                    case "AI_SHARED_MEMORY":
                    case 40:
                        message.capabilities[message.capabilities.length] = 40;
                        break;
                    case "RICH_RESPONSE_UNIFIED_SOURCES":
                    case 41:
                        message.capabilities[message.capabilities.length] = 41;
                        break;
                    case "RICH_RESPONSE_UNIFIED_DOMAIN_CITATIONS":
                    case 42:
                        message.capabilities[message.capabilities.length] = 42;
                        break;
                    case "RICH_RESPONSE_UR_INLINE_REELS_ENABLED":
                    case 43:
                        message.capabilities[message.capabilities.length] = 43;
                        break;
                    case "RICH_RESPONSE_UR_MEDIA_GRID_ENABLED":
                    case 44:
                        message.capabilities[message.capabilities.length] = 44;
                        break;
                    case "RICH_RESPONSE_UR_TIMESTAMP_PLACEHOLDER":
                    case 45:
                        message.capabilities[message.capabilities.length] = 45;
                        break;
                    case "RICH_RESPONSE_IN_APP_SURVEY":
                    case 46:
                        message.capabilities[message.capabilities.length] = 46;
                        break;
                    case "AI_RESPONSE_MODEL_BRANDING":
                    case 47:
                        message.capabilities[message.capabilities.length] = 47;
                        break;
                    case "SESSION_TRANSPARENCY_SYSTEM_MESSAGE":
                    case 48:
                        message.capabilities[message.capabilities.length] = 48;
                        break;
                    case "RICH_RESPONSE_UR_REASONING":
                    case 49:
                        message.capabilities[message.capabilities.length] = 49;
                        break;
                    case "RICH_RESPONSE_UR_ZEITGEIST_CITATIONS":
                    case 50:
                        message.capabilities[message.capabilities.length] = 50;
                        break;
                    case "RICH_RESPONSE_UR_ZEITGEIST_CAROUSEL":
                    case 51:
                        message.capabilities[message.capabilities.length] = 51;
                        break;
                    case "AI_IMAGINE_LOADING_INDICATOR":
                    case 52:
                        message.capabilities[message.capabilities.length] = 52;
                        break;
                    case "RICH_RESPONSE_UR_IMAGINE":
                    case 53:
                        message.capabilities[message.capabilities.length] = 53;
                        break;
                    case "AI_IMAGINE_UR_TO_NATIVE_LOADING_INDICATOR":
                    case 54:
                        message.capabilities[message.capabilities.length] = 54;
                        break;
                    case "RICH_RESPONSE_UR_BLOKS_ENABLED":
                    case 55:
                        message.capabilities[message.capabilities.length] = 55;
                        break;
                    case "RICH_RESPONSE_INLINE_LINKS_ENABLED":
                    case 56:
                        message.capabilities[message.capabilities.length] = 56;
                        break;
                    case "RICH_RESPONSE_UR_IMAGINE_VIDEO":
                    case 57:
                        message.capabilities[message.capabilities.length] = 57;
                        break;
                    case "JSON_PATCH_STREAMING":
                    case 58:
                        message.capabilities[message.capabilities.length] = 58;
                        break;
                    case "AI_TAB_FORCE_CLIPPY":
                    case 59:
                        message.capabilities[message.capabilities.length] = 59;
                        break;
                    case "UNIFIED_RESPONSE_EMBEDDED_SCREENS":
                    case 60:
                        message.capabilities[message.capabilities.length] = 60;
                        break;
                    case "AI_SUBSCRIPTION_ENABLED":
                    case 61:
                        message.capabilities[message.capabilities.length] = 61;
                        break;
                    case "UNIFIED_RESPONSE_AI_CONTENT_SEARCH_ENABLED":
                    case 62:
                        message.capabilities[message.capabilities.length] = 62;
                        break;
                    case "UNIFIED_RESPONSE_MARKDOWN_LINKS_ENABLED":
                    case 63:
                        message.capabilities[message.capabilities.length] = 63;
                        break;
                    case "AI_RICH_RESPONSE_MAPS_V2_ENABLED":
                    case 64:
                        message.capabilities[message.capabilities.length] = 64;
                        break;
                    case "AI_SUBSCRIPTION_METERING_ENABLED":
                    case 65:
                        message.capabilities[message.capabilities.length] = 65;
                        break;
                    default:
                        if (typeof object.capabilities[i] === "number" && (object.capabilities[i] | 0) === object.capabilities[i])
                            message.capabilities[message.capabilities.length] = object.capabilities[i];
                    }
            }
            return message;
        };

        /**
         * Creates a plain object from a BotCapabilityMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotCapabilityMetadata
         * @static
         * @param {proto.BotCapabilityMetadata} message BotCapabilityMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotCapabilityMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.capabilities = [];
            if (message.capabilities && message.capabilities.length) {
                object.capabilities = Array(message.capabilities.length);
                for (var j = 0; j < message.capabilities.length; ++j)
                    object.capabilities[j] = options.enums === String ? $root.proto.BotCapabilityMetadata.BotCapabilityType[message.capabilities[j]] === undefined ? message.capabilities[j] : $root.proto.BotCapabilityMetadata.BotCapabilityType[message.capabilities[j]] : message.capabilities[j];
            }
            return object;
        };

        /**
         * Converts this BotCapabilityMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotCapabilityMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotCapabilityMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotCapabilityMetadata
         * @function getTypeUrl
         * @memberof proto.BotCapabilityMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotCapabilityMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotCapabilityMetadata";
        };

        /**
         * BotCapabilityType enum.
         * @name proto.BotCapabilityMetadata.BotCapabilityType
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} PROGRESS_INDICATOR=1 PROGRESS_INDICATOR value
         * @property {number} RICH_RESPONSE_HEADING=2 RICH_RESPONSE_HEADING value
         * @property {number} RICH_RESPONSE_NESTED_LIST=3 RICH_RESPONSE_NESTED_LIST value
         * @property {number} AI_MEMORY=4 AI_MEMORY value
         * @property {number} RICH_RESPONSE_THREAD_SURFING=5 RICH_RESPONSE_THREAD_SURFING value
         * @property {number} RICH_RESPONSE_TABLE=6 RICH_RESPONSE_TABLE value
         * @property {number} RICH_RESPONSE_CODE=7 RICH_RESPONSE_CODE value
         * @property {number} RICH_RESPONSE_STRUCTURED_RESPONSE=8 RICH_RESPONSE_STRUCTURED_RESPONSE value
         * @property {number} RICH_RESPONSE_INLINE_IMAGE=9 RICH_RESPONSE_INLINE_IMAGE value
         * @property {number} WA_IG_1P_PLUGIN_RANKING_CONTROL=10 WA_IG_1P_PLUGIN_RANKING_CONTROL value
         * @property {number} WA_IG_1P_PLUGIN_RANKING_UPDATE_1=11 WA_IG_1P_PLUGIN_RANKING_UPDATE_1 value
         * @property {number} WA_IG_1P_PLUGIN_RANKING_UPDATE_2=12 WA_IG_1P_PLUGIN_RANKING_UPDATE_2 value
         * @property {number} WA_IG_1P_PLUGIN_RANKING_UPDATE_3=13 WA_IG_1P_PLUGIN_RANKING_UPDATE_3 value
         * @property {number} WA_IG_1P_PLUGIN_RANKING_UPDATE_4=14 WA_IG_1P_PLUGIN_RANKING_UPDATE_4 value
         * @property {number} WA_IG_1P_PLUGIN_RANKING_UPDATE_5=15 WA_IG_1P_PLUGIN_RANKING_UPDATE_5 value
         * @property {number} WA_IG_1P_PLUGIN_RANKING_UPDATE_6=16 WA_IG_1P_PLUGIN_RANKING_UPDATE_6 value
         * @property {number} WA_IG_1P_PLUGIN_RANKING_UPDATE_7=17 WA_IG_1P_PLUGIN_RANKING_UPDATE_7 value
         * @property {number} WA_IG_1P_PLUGIN_RANKING_UPDATE_8=18 WA_IG_1P_PLUGIN_RANKING_UPDATE_8 value
         * @property {number} WA_IG_1P_PLUGIN_RANKING_UPDATE_9=19 WA_IG_1P_PLUGIN_RANKING_UPDATE_9 value
         * @property {number} WA_IG_1P_PLUGIN_RANKING_UPDATE_10=20 WA_IG_1P_PLUGIN_RANKING_UPDATE_10 value
         * @property {number} RICH_RESPONSE_SUB_HEADING=21 RICH_RESPONSE_SUB_HEADING value
         * @property {number} RICH_RESPONSE_GRID_IMAGE=22 RICH_RESPONSE_GRID_IMAGE value
         * @property {number} AI_STUDIO_UGC_MEMORY=23 AI_STUDIO_UGC_MEMORY value
         * @property {number} RICH_RESPONSE_LATEX=24 RICH_RESPONSE_LATEX value
         * @property {number} RICH_RESPONSE_MAPS=25 RICH_RESPONSE_MAPS value
         * @property {number} RICH_RESPONSE_INLINE_REELS=26 RICH_RESPONSE_INLINE_REELS value
         * @property {number} AGENTIC_PLANNING=27 AGENTIC_PLANNING value
         * @property {number} ACCOUNT_LINKING=28 ACCOUNT_LINKING value
         * @property {number} STREAMING_DISAGGREGATION=29 STREAMING_DISAGGREGATION value
         * @property {number} RICH_RESPONSE_GRID_IMAGE_3P=30 RICH_RESPONSE_GRID_IMAGE_3P value
         * @property {number} RICH_RESPONSE_LATEX_INLINE=31 RICH_RESPONSE_LATEX_INLINE value
         * @property {number} QUERY_PLAN=32 QUERY_PLAN value
         * @property {number} PROACTIVE_MESSAGE=33 PROACTIVE_MESSAGE value
         * @property {number} RICH_RESPONSE_UNIFIED_RESPONSE=34 RICH_RESPONSE_UNIFIED_RESPONSE value
         * @property {number} PROMOTION_MESSAGE=35 PROMOTION_MESSAGE value
         * @property {number} SIMPLIFIED_PROFILE_PAGE=36 SIMPLIFIED_PROFILE_PAGE value
         * @property {number} RICH_RESPONSE_SOURCES_IN_MESSAGE=37 RICH_RESPONSE_SOURCES_IN_MESSAGE value
         * @property {number} RICH_RESPONSE_SIDE_BY_SIDE_SURVEY=38 RICH_RESPONSE_SIDE_BY_SIDE_SURVEY value
         * @property {number} RICH_RESPONSE_UNIFIED_TEXT_COMPONENT=39 RICH_RESPONSE_UNIFIED_TEXT_COMPONENT value
         * @property {number} AI_SHARED_MEMORY=40 AI_SHARED_MEMORY value
         * @property {number} RICH_RESPONSE_UNIFIED_SOURCES=41 RICH_RESPONSE_UNIFIED_SOURCES value
         * @property {number} RICH_RESPONSE_UNIFIED_DOMAIN_CITATIONS=42 RICH_RESPONSE_UNIFIED_DOMAIN_CITATIONS value
         * @property {number} RICH_RESPONSE_UR_INLINE_REELS_ENABLED=43 RICH_RESPONSE_UR_INLINE_REELS_ENABLED value
         * @property {number} RICH_RESPONSE_UR_MEDIA_GRID_ENABLED=44 RICH_RESPONSE_UR_MEDIA_GRID_ENABLED value
         * @property {number} RICH_RESPONSE_UR_TIMESTAMP_PLACEHOLDER=45 RICH_RESPONSE_UR_TIMESTAMP_PLACEHOLDER value
         * @property {number} RICH_RESPONSE_IN_APP_SURVEY=46 RICH_RESPONSE_IN_APP_SURVEY value
         * @property {number} AI_RESPONSE_MODEL_BRANDING=47 AI_RESPONSE_MODEL_BRANDING value
         * @property {number} SESSION_TRANSPARENCY_SYSTEM_MESSAGE=48 SESSION_TRANSPARENCY_SYSTEM_MESSAGE value
         * @property {number} RICH_RESPONSE_UR_REASONING=49 RICH_RESPONSE_UR_REASONING value
         * @property {number} RICH_RESPONSE_UR_ZEITGEIST_CITATIONS=50 RICH_RESPONSE_UR_ZEITGEIST_CITATIONS value
         * @property {number} RICH_RESPONSE_UR_ZEITGEIST_CAROUSEL=51 RICH_RESPONSE_UR_ZEITGEIST_CAROUSEL value
         * @property {number} AI_IMAGINE_LOADING_INDICATOR=52 AI_IMAGINE_LOADING_INDICATOR value
         * @property {number} RICH_RESPONSE_UR_IMAGINE=53 RICH_RESPONSE_UR_IMAGINE value
         * @property {number} AI_IMAGINE_UR_TO_NATIVE_LOADING_INDICATOR=54 AI_IMAGINE_UR_TO_NATIVE_LOADING_INDICATOR value
         * @property {number} RICH_RESPONSE_UR_BLOKS_ENABLED=55 RICH_RESPONSE_UR_BLOKS_ENABLED value
         * @property {number} RICH_RESPONSE_INLINE_LINKS_ENABLED=56 RICH_RESPONSE_INLINE_LINKS_ENABLED value
         * @property {number} RICH_RESPONSE_UR_IMAGINE_VIDEO=57 RICH_RESPONSE_UR_IMAGINE_VIDEO value
         * @property {number} JSON_PATCH_STREAMING=58 JSON_PATCH_STREAMING value
         * @property {number} AI_TAB_FORCE_CLIPPY=59 AI_TAB_FORCE_CLIPPY value
         * @property {number} UNIFIED_RESPONSE_EMBEDDED_SCREENS=60 UNIFIED_RESPONSE_EMBEDDED_SCREENS value
         * @property {number} AI_SUBSCRIPTION_ENABLED=61 AI_SUBSCRIPTION_ENABLED value
         * @property {number} UNIFIED_RESPONSE_AI_CONTENT_SEARCH_ENABLED=62 UNIFIED_RESPONSE_AI_CONTENT_SEARCH_ENABLED value
         * @property {number} UNIFIED_RESPONSE_MARKDOWN_LINKS_ENABLED=63 UNIFIED_RESPONSE_MARKDOWN_LINKS_ENABLED value
         * @property {number} AI_RICH_RESPONSE_MAPS_V2_ENABLED=64 AI_RICH_RESPONSE_MAPS_V2_ENABLED value
         * @property {number} AI_SUBSCRIPTION_METERING_ENABLED=65 AI_SUBSCRIPTION_METERING_ENABLED value
         */
        BotCapabilityMetadata.BotCapabilityType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "PROGRESS_INDICATOR"] = 1;
            values[valuesById[2] = "RICH_RESPONSE_HEADING"] = 2;
            values[valuesById[3] = "RICH_RESPONSE_NESTED_LIST"] = 3;
            values[valuesById[4] = "AI_MEMORY"] = 4;
            values[valuesById[5] = "RICH_RESPONSE_THREAD_SURFING"] = 5;
            values[valuesById[6] = "RICH_RESPONSE_TABLE"] = 6;
            values[valuesById[7] = "RICH_RESPONSE_CODE"] = 7;
            values[valuesById[8] = "RICH_RESPONSE_STRUCTURED_RESPONSE"] = 8;
            values[valuesById[9] = "RICH_RESPONSE_INLINE_IMAGE"] = 9;
            values[valuesById[10] = "WA_IG_1P_PLUGIN_RANKING_CONTROL"] = 10;
            values[valuesById[11] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_1"] = 11;
            values[valuesById[12] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_2"] = 12;
            values[valuesById[13] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_3"] = 13;
            values[valuesById[14] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_4"] = 14;
            values[valuesById[15] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_5"] = 15;
            values[valuesById[16] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_6"] = 16;
            values[valuesById[17] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_7"] = 17;
            values[valuesById[18] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_8"] = 18;
            values[valuesById[19] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_9"] = 19;
            values[valuesById[20] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_10"] = 20;
            values[valuesById[21] = "RICH_RESPONSE_SUB_HEADING"] = 21;
            values[valuesById[22] = "RICH_RESPONSE_GRID_IMAGE"] = 22;
            values[valuesById[23] = "AI_STUDIO_UGC_MEMORY"] = 23;
            values[valuesById[24] = "RICH_RESPONSE_LATEX"] = 24;
            values[valuesById[25] = "RICH_RESPONSE_MAPS"] = 25;
            values[valuesById[26] = "RICH_RESPONSE_INLINE_REELS"] = 26;
            values[valuesById[27] = "AGENTIC_PLANNING"] = 27;
            values[valuesById[28] = "ACCOUNT_LINKING"] = 28;
            values[valuesById[29] = "STREAMING_DISAGGREGATION"] = 29;
            values[valuesById[30] = "RICH_RESPONSE_GRID_IMAGE_3P"] = 30;
            values[valuesById[31] = "RICH_RESPONSE_LATEX_INLINE"] = 31;
            values[valuesById[32] = "QUERY_PLAN"] = 32;
            values[valuesById[33] = "PROACTIVE_MESSAGE"] = 33;
            values[valuesById[34] = "RICH_RESPONSE_UNIFIED_RESPONSE"] = 34;
            values[valuesById[35] = "PROMOTION_MESSAGE"] = 35;
            values[valuesById[36] = "SIMPLIFIED_PROFILE_PAGE"] = 36;
            values[valuesById[37] = "RICH_RESPONSE_SOURCES_IN_MESSAGE"] = 37;
            values[valuesById[38] = "RICH_RESPONSE_SIDE_BY_SIDE_SURVEY"] = 38;
            values[valuesById[39] = "RICH_RESPONSE_UNIFIED_TEXT_COMPONENT"] = 39;
            values[valuesById[40] = "AI_SHARED_MEMORY"] = 40;
            values[valuesById[41] = "RICH_RESPONSE_UNIFIED_SOURCES"] = 41;
            values[valuesById[42] = "RICH_RESPONSE_UNIFIED_DOMAIN_CITATIONS"] = 42;
            values[valuesById[43] = "RICH_RESPONSE_UR_INLINE_REELS_ENABLED"] = 43;
            values[valuesById[44] = "RICH_RESPONSE_UR_MEDIA_GRID_ENABLED"] = 44;
            values[valuesById[45] = "RICH_RESPONSE_UR_TIMESTAMP_PLACEHOLDER"] = 45;
            values[valuesById[46] = "RICH_RESPONSE_IN_APP_SURVEY"] = 46;
            values[valuesById[47] = "AI_RESPONSE_MODEL_BRANDING"] = 47;
            values[valuesById[48] = "SESSION_TRANSPARENCY_SYSTEM_MESSAGE"] = 48;
            values[valuesById[49] = "RICH_RESPONSE_UR_REASONING"] = 49;
            values[valuesById[50] = "RICH_RESPONSE_UR_ZEITGEIST_CITATIONS"] = 50;
            values[valuesById[51] = "RICH_RESPONSE_UR_ZEITGEIST_CAROUSEL"] = 51;
            values[valuesById[52] = "AI_IMAGINE_LOADING_INDICATOR"] = 52;
            values[valuesById[53] = "RICH_RESPONSE_UR_IMAGINE"] = 53;
            values[valuesById[54] = "AI_IMAGINE_UR_TO_NATIVE_LOADING_INDICATOR"] = 54;
            values[valuesById[55] = "RICH_RESPONSE_UR_BLOKS_ENABLED"] = 55;
            values[valuesById[56] = "RICH_RESPONSE_INLINE_LINKS_ENABLED"] = 56;
            values[valuesById[57] = "RICH_RESPONSE_UR_IMAGINE_VIDEO"] = 57;
            values[valuesById[58] = "JSON_PATCH_STREAMING"] = 58;
            values[valuesById[59] = "AI_TAB_FORCE_CLIPPY"] = 59;
            values[valuesById[60] = "UNIFIED_RESPONSE_EMBEDDED_SCREENS"] = 60;
            values[valuesById[61] = "AI_SUBSCRIPTION_ENABLED"] = 61;
            values[valuesById[62] = "UNIFIED_RESPONSE_AI_CONTENT_SEARCH_ENABLED"] = 62;
            values[valuesById[63] = "UNIFIED_RESPONSE_MARKDOWN_LINKS_ENABLED"] = 63;
            values[valuesById[64] = "AI_RICH_RESPONSE_MAPS_V2_ENABLED"] = 64;
            values[valuesById[65] = "AI_SUBSCRIPTION_METERING_ENABLED"] = 65;
            return values;
        })();

        return BotCapabilityMetadata;
    })();

    proto.BotCommandMetadata = (function() {

        /**
         * Properties of a BotCommandMetadata.
         * @memberof proto
         * @interface IBotCommandMetadata
         * @property {string|null} [commandName] BotCommandMetadata commandName
         * @property {string|null} [commandDescription] BotCommandMetadata commandDescription
         * @property {string|null} [commandPrompt] BotCommandMetadata commandPrompt
         */

        /**
         * Constructs a new BotCommandMetadata.
         * @memberof proto
         * @classdesc Represents a BotCommandMetadata.
         * @implements IBotCommandMetadata
         * @constructor
         * @param {proto.IBotCommandMetadata=} [properties] Properties to set
         */
        function BotCommandMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotCommandMetadata commandName.
         * @member {string|null|undefined} commandName
         * @memberof proto.BotCommandMetadata
         * @instance
         */
        BotCommandMetadata.prototype.commandName = null;

        /**
         * BotCommandMetadata commandDescription.
         * @member {string|null|undefined} commandDescription
         * @memberof proto.BotCommandMetadata
         * @instance
         */
        BotCommandMetadata.prototype.commandDescription = null;

        /**
         * BotCommandMetadata commandPrompt.
         * @member {string|null|undefined} commandPrompt
         * @memberof proto.BotCommandMetadata
         * @instance
         */
        BotCommandMetadata.prototype.commandPrompt = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotCommandMetadata.prototype, "_commandName", {
            get: $util.oneOfGetter($oneOfFields = ["commandName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotCommandMetadata.prototype, "_commandDescription", {
            get: $util.oneOfGetter($oneOfFields = ["commandDescription"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotCommandMetadata.prototype, "_commandPrompt", {
            get: $util.oneOfGetter($oneOfFields = ["commandPrompt"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotCommandMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotCommandMetadata
         * @static
         * @param {proto.IBotCommandMetadata=} [properties] Properties to set
         * @returns {proto.BotCommandMetadata} BotCommandMetadata instance
         */
        BotCommandMetadata.create = function create(properties) {
            return new BotCommandMetadata(properties);
        };

        /**
         * Encodes the specified BotCommandMetadata message. Does not implicitly {@link proto.BotCommandMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotCommandMetadata
         * @static
         * @param {proto.IBotCommandMetadata} message BotCommandMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotCommandMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.commandName != null && Object.hasOwnProperty.call(message, "commandName"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.commandName);
            if (message.commandDescription != null && Object.hasOwnProperty.call(message, "commandDescription"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.commandDescription);
            if (message.commandPrompt != null && Object.hasOwnProperty.call(message, "commandPrompt"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.commandPrompt);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotCommandMetadata message, length delimited. Does not implicitly {@link proto.BotCommandMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotCommandMetadata
         * @static
         * @param {proto.IBotCommandMetadata} message BotCommandMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotCommandMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotCommandMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotCommandMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotCommandMetadata} BotCommandMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotCommandMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.commandName = reader.stringVerify();
                        message._commandName = "commandName";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.commandDescription = reader.stringVerify();
                        message._commandDescription = "commandDescription";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.commandPrompt = reader.stringVerify();
                        message._commandPrompt = "commandPrompt";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotCommandMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotCommandMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotCommandMetadata} BotCommandMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotCommandMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotCommandMetadata message.
         * @function verify
         * @memberof proto.BotCommandMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotCommandMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.commandName != null && Object.hasOwnProperty.call(message, "commandName")) {
                properties._commandName = 1;
                if (!$util.isString(message.commandName))
                    return "commandName: string expected";
            }
            if (message.commandDescription != null && Object.hasOwnProperty.call(message, "commandDescription")) {
                properties._commandDescription = 1;
                if (!$util.isString(message.commandDescription))
                    return "commandDescription: string expected";
            }
            if (message.commandPrompt != null && Object.hasOwnProperty.call(message, "commandPrompt")) {
                properties._commandPrompt = 1;
                if (!$util.isString(message.commandPrompt))
                    return "commandPrompt: string expected";
            }
            return null;
        };

        /**
         * Creates a BotCommandMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotCommandMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotCommandMetadata} BotCommandMetadata
         */
        BotCommandMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotCommandMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.commandName != null)
                message.commandName = String(object.commandName);
            if (object.commandDescription != null)
                message.commandDescription = String(object.commandDescription);
            if (object.commandPrompt != null)
                message.commandPrompt = String(object.commandPrompt);
            return message;
        };

        /**
         * Creates a plain object from a BotCommandMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotCommandMetadata
         * @static
         * @param {proto.BotCommandMetadata} message BotCommandMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotCommandMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.commandName != null && Object.hasOwnProperty.call(message, "commandName"))
                object.commandName = message.commandName;
            if (message.commandDescription != null && Object.hasOwnProperty.call(message, "commandDescription"))
                object.commandDescription = message.commandDescription;
            if (message.commandPrompt != null && Object.hasOwnProperty.call(message, "commandPrompt"))
                object.commandPrompt = message.commandPrompt;
            return object;
        };

        /**
         * Converts this BotCommandMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotCommandMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotCommandMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotCommandMetadata
         * @function getTypeUrl
         * @memberof proto.BotCommandMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotCommandMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotCommandMetadata";
        };

        return BotCommandMetadata;
    })();

    proto.BotDocumentMessageMetadata = (function() {

        /**
         * Properties of a BotDocumentMessageMetadata.
         * @memberof proto
         * @interface IBotDocumentMessageMetadata
         * @property {proto.BotDocumentMessageMetadata.DocumentPluginType|null} [pluginType] BotDocumentMessageMetadata pluginType
         */

        /**
         * Constructs a new BotDocumentMessageMetadata.
         * @memberof proto
         * @classdesc Represents a BotDocumentMessageMetadata.
         * @implements IBotDocumentMessageMetadata
         * @constructor
         * @param {proto.IBotDocumentMessageMetadata=} [properties] Properties to set
         */
        function BotDocumentMessageMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotDocumentMessageMetadata pluginType.
         * @member {proto.BotDocumentMessageMetadata.DocumentPluginType|null|undefined} pluginType
         * @memberof proto.BotDocumentMessageMetadata
         * @instance
         */
        BotDocumentMessageMetadata.prototype.pluginType = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotDocumentMessageMetadata.prototype, "_pluginType", {
            get: $util.oneOfGetter($oneOfFields = ["pluginType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotDocumentMessageMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotDocumentMessageMetadata
         * @static
         * @param {proto.IBotDocumentMessageMetadata=} [properties] Properties to set
         * @returns {proto.BotDocumentMessageMetadata} BotDocumentMessageMetadata instance
         */
        BotDocumentMessageMetadata.create = function create(properties) {
            return new BotDocumentMessageMetadata(properties);
        };

        /**
         * Encodes the specified BotDocumentMessageMetadata message. Does not implicitly {@link proto.BotDocumentMessageMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotDocumentMessageMetadata
         * @static
         * @param {proto.IBotDocumentMessageMetadata} message BotDocumentMessageMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotDocumentMessageMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.pluginType != null && Object.hasOwnProperty.call(message, "pluginType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.pluginType);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotDocumentMessageMetadata message, length delimited. Does not implicitly {@link proto.BotDocumentMessageMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotDocumentMessageMetadata
         * @static
         * @param {proto.IBotDocumentMessageMetadata} message BotDocumentMessageMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotDocumentMessageMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotDocumentMessageMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotDocumentMessageMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotDocumentMessageMetadata} BotDocumentMessageMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotDocumentMessageMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.pluginType = reader.int32();
                        message._pluginType = "pluginType";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotDocumentMessageMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotDocumentMessageMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotDocumentMessageMetadata} BotDocumentMessageMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotDocumentMessageMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotDocumentMessageMetadata message.
         * @function verify
         * @memberof proto.BotDocumentMessageMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotDocumentMessageMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.pluginType != null && Object.hasOwnProperty.call(message, "pluginType")) {
                properties._pluginType = 1;
                if (typeof message.pluginType !== "number" || (message.pluginType | 0) !== message.pluginType)
                    return "pluginType: enum value expected";
            }
            return null;
        };

        /**
         * Creates a BotDocumentMessageMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotDocumentMessageMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotDocumentMessageMetadata} BotDocumentMessageMetadata
         */
        BotDocumentMessageMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotDocumentMessageMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.pluginType) {
            case "TEXT_EXTRACTION":
            case 0:
                message.pluginType = 0;
                break;
            case "OCR_AND_IMAGES":
            case 1:
                message.pluginType = 1;
                break;
            default:
                if (typeof object.pluginType === "number" && (object.pluginType | 0) === object.pluginType)
                    message.pluginType = object.pluginType;
            }
            return message;
        };

        /**
         * Creates a plain object from a BotDocumentMessageMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotDocumentMessageMetadata
         * @static
         * @param {proto.BotDocumentMessageMetadata} message BotDocumentMessageMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotDocumentMessageMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.pluginType != null && Object.hasOwnProperty.call(message, "pluginType"))
                object.pluginType = options.enums === String ? $root.proto.BotDocumentMessageMetadata.DocumentPluginType[message.pluginType] === undefined ? message.pluginType : $root.proto.BotDocumentMessageMetadata.DocumentPluginType[message.pluginType] : message.pluginType;
            return object;
        };

        /**
         * Converts this BotDocumentMessageMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotDocumentMessageMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotDocumentMessageMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotDocumentMessageMetadata
         * @function getTypeUrl
         * @memberof proto.BotDocumentMessageMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotDocumentMessageMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotDocumentMessageMetadata";
        };

        /**
         * DocumentPluginType enum.
         * @name proto.BotDocumentMessageMetadata.DocumentPluginType
         * @enum {number}
         * @property {number} TEXT_EXTRACTION=0 TEXT_EXTRACTION value
         * @property {number} OCR_AND_IMAGES=1 OCR_AND_IMAGES value
         */
        BotDocumentMessageMetadata.DocumentPluginType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "TEXT_EXTRACTION"] = 0;
            values[valuesById[1] = "OCR_AND_IMAGES"] = 1;
            return values;
        })();

        return BotDocumentMessageMetadata;
    })();

    proto.BotFeedbackMessage = (function() {

        /**
         * Properties of a BotFeedbackMessage.
         * @memberof proto
         * @interface IBotFeedbackMessage
         * @property {proto.IMessageKey|null} [messageKey] BotFeedbackMessage messageKey
         * @property {proto.BotFeedbackMessage.BotFeedbackKind|null} [kind] BotFeedbackMessage kind
         * @property {string|null} [text] BotFeedbackMessage text
         * @property {number|Long|null} [kindNegative] BotFeedbackMessage kindNegative
         * @property {number|Long|null} [kindPositive] BotFeedbackMessage kindPositive
         * @property {proto.BotFeedbackMessage.ReportKind|null} [kindReport] BotFeedbackMessage kindReport
         * @property {proto.BotFeedbackMessage.ISideBySideSurveyMetadata|null} [sideBySideSurveyMetadata] BotFeedbackMessage sideBySideSurveyMetadata
         */

        /**
         * Constructs a new BotFeedbackMessage.
         * @memberof proto
         * @classdesc Represents a BotFeedbackMessage.
         * @implements IBotFeedbackMessage
         * @constructor
         * @param {proto.IBotFeedbackMessage=} [properties] Properties to set
         */
        function BotFeedbackMessage(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotFeedbackMessage messageKey.
         * @member {proto.IMessageKey|null|undefined} messageKey
         * @memberof proto.BotFeedbackMessage
         * @instance
         */
        BotFeedbackMessage.prototype.messageKey = null;

        /**
         * BotFeedbackMessage kind.
         * @member {proto.BotFeedbackMessage.BotFeedbackKind|null|undefined} kind
         * @memberof proto.BotFeedbackMessage
         * @instance
         */
        BotFeedbackMessage.prototype.kind = null;

        /**
         * BotFeedbackMessage text.
         * @member {string|null|undefined} text
         * @memberof proto.BotFeedbackMessage
         * @instance
         */
        BotFeedbackMessage.prototype.text = null;

        /**
         * BotFeedbackMessage kindNegative.
         * @member {number|Long|null|undefined} kindNegative
         * @memberof proto.BotFeedbackMessage
         * @instance
         */
        BotFeedbackMessage.prototype.kindNegative = null;

        /**
         * BotFeedbackMessage kindPositive.
         * @member {number|Long|null|undefined} kindPositive
         * @memberof proto.BotFeedbackMessage
         * @instance
         */
        BotFeedbackMessage.prototype.kindPositive = null;

        /**
         * BotFeedbackMessage kindReport.
         * @member {proto.BotFeedbackMessage.ReportKind|null|undefined} kindReport
         * @memberof proto.BotFeedbackMessage
         * @instance
         */
        BotFeedbackMessage.prototype.kindReport = null;

        /**
         * BotFeedbackMessage sideBySideSurveyMetadata.
         * @member {proto.BotFeedbackMessage.ISideBySideSurveyMetadata|null|undefined} sideBySideSurveyMetadata
         * @memberof proto.BotFeedbackMessage
         * @instance
         */
        BotFeedbackMessage.prototype.sideBySideSurveyMetadata = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotFeedbackMessage.prototype, "_messageKey", {
            get: $util.oneOfGetter($oneOfFields = ["messageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotFeedbackMessage.prototype, "_kind", {
            get: $util.oneOfGetter($oneOfFields = ["kind"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotFeedbackMessage.prototype, "_text", {
            get: $util.oneOfGetter($oneOfFields = ["text"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotFeedbackMessage.prototype, "_kindNegative", {
            get: $util.oneOfGetter($oneOfFields = ["kindNegative"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotFeedbackMessage.prototype, "_kindPositive", {
            get: $util.oneOfGetter($oneOfFields = ["kindPositive"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotFeedbackMessage.prototype, "_kindReport", {
            get: $util.oneOfGetter($oneOfFields = ["kindReport"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotFeedbackMessage.prototype, "_sideBySideSurveyMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["sideBySideSurveyMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotFeedbackMessage instance using the specified properties.
         * @function create
         * @memberof proto.BotFeedbackMessage
         * @static
         * @param {proto.IBotFeedbackMessage=} [properties] Properties to set
         * @returns {proto.BotFeedbackMessage} BotFeedbackMessage instance
         */
        BotFeedbackMessage.create = function create(properties) {
            return new BotFeedbackMessage(properties);
        };

        /**
         * Encodes the specified BotFeedbackMessage message. Does not implicitly {@link proto.BotFeedbackMessage.verify|verify} messages.
         * @function encode
         * @memberof proto.BotFeedbackMessage
         * @static
         * @param {proto.IBotFeedbackMessage} message BotFeedbackMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotFeedbackMessage.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.messageKey != null && Object.hasOwnProperty.call(message, "messageKey"))
                $root.proto.MessageKey.encode(message.messageKey, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.kind != null && Object.hasOwnProperty.call(message, "kind"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.kind);
            if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.text);
            if (message.kindNegative != null && Object.hasOwnProperty.call(message, "kindNegative"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.kindNegative);
            if (message.kindPositive != null && Object.hasOwnProperty.call(message, "kindPositive"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.kindPositive);
            if (message.kindReport != null && Object.hasOwnProperty.call(message, "kindReport"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.kindReport);
            if (message.sideBySideSurveyMetadata != null && Object.hasOwnProperty.call(message, "sideBySideSurveyMetadata"))
                $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.encode(message.sideBySideSurveyMetadata, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotFeedbackMessage message, length delimited. Does not implicitly {@link proto.BotFeedbackMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotFeedbackMessage
         * @static
         * @param {proto.IBotFeedbackMessage} message BotFeedbackMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotFeedbackMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotFeedbackMessage message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotFeedbackMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotFeedbackMessage} BotFeedbackMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotFeedbackMessage.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.messageKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.messageKey);
                        message._messageKey = "messageKey";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.kind = reader.int32();
                        message._kind = "kind";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.text = reader.stringVerify();
                        message._text = "text";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.kindNegative = reader.uint64();
                        message._kindNegative = "kindNegative";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.kindPositive = reader.uint64();
                        message._kindPositive = "kindPositive";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        message.kindReport = reader.int32();
                        message._kindReport = "kindReport";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        message.sideBySideSurveyMetadata = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.sideBySideSurveyMetadata);
                        message._sideBySideSurveyMetadata = "sideBySideSurveyMetadata";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotFeedbackMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotFeedbackMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotFeedbackMessage} BotFeedbackMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotFeedbackMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotFeedbackMessage message.
         * @function verify
         * @memberof proto.BotFeedbackMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotFeedbackMessage.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.messageKey != null && Object.hasOwnProperty.call(message, "messageKey")) {
                properties._messageKey = 1;
                {
                    var error = $root.proto.MessageKey.verify(message.messageKey, q + 1);
                    if (error)
                        return "messageKey." + error;
                }
            }
            if (message.kind != null && Object.hasOwnProperty.call(message, "kind")) {
                properties._kind = 1;
                if (typeof message.kind !== "number" || (message.kind | 0) !== message.kind)
                    return "kind: enum value expected";
            }
            if (message.text != null && Object.hasOwnProperty.call(message, "text")) {
                properties._text = 1;
                if (!$util.isString(message.text))
                    return "text: string expected";
            }
            if (message.kindNegative != null && Object.hasOwnProperty.call(message, "kindNegative")) {
                properties._kindNegative = 1;
                if (!$util.isInteger(message.kindNegative) && !(message.kindNegative && $util.isInteger(message.kindNegative.low) && $util.isInteger(message.kindNegative.high)))
                    return "kindNegative: integer|Long expected";
            }
            if (message.kindPositive != null && Object.hasOwnProperty.call(message, "kindPositive")) {
                properties._kindPositive = 1;
                if (!$util.isInteger(message.kindPositive) && !(message.kindPositive && $util.isInteger(message.kindPositive.low) && $util.isInteger(message.kindPositive.high)))
                    return "kindPositive: integer|Long expected";
            }
            if (message.kindReport != null && Object.hasOwnProperty.call(message, "kindReport")) {
                properties._kindReport = 1;
                if (typeof message.kindReport !== "number" || (message.kindReport | 0) !== message.kindReport)
                    return "kindReport: enum value expected";
            }
            if (message.sideBySideSurveyMetadata != null && Object.hasOwnProperty.call(message, "sideBySideSurveyMetadata")) {
                properties._sideBySideSurveyMetadata = 1;
                {
                    var error = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.verify(message.sideBySideSurveyMetadata, q + 1);
                    if (error)
                        return "sideBySideSurveyMetadata." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BotFeedbackMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotFeedbackMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotFeedbackMessage} BotFeedbackMessage
         */
        BotFeedbackMessage.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotFeedbackMessage: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.messageKey != null) {
                if (!$util.isObject(object.messageKey))
                    throw TypeError(".proto.BotFeedbackMessage.messageKey: object expected");
                message.messageKey = $root.proto.MessageKey.fromObject(object.messageKey, q + 1);
            }
            switch (object.kind) {
            case "BOT_FEEDBACK_POSITIVE":
            case 0:
                message.kind = 0;
                break;
            case "BOT_FEEDBACK_NEGATIVE_GENERIC":
            case 1:
                message.kind = 1;
                break;
            case "BOT_FEEDBACK_NEGATIVE_HELPFUL":
            case 2:
                message.kind = 2;
                break;
            case "BOT_FEEDBACK_NEGATIVE_INTERESTING":
            case 3:
                message.kind = 3;
                break;
            case "BOT_FEEDBACK_NEGATIVE_ACCURATE":
            case 4:
                message.kind = 4;
                break;
            case "BOT_FEEDBACK_NEGATIVE_SAFE":
            case 5:
                message.kind = 5;
                break;
            case "BOT_FEEDBACK_NEGATIVE_OTHER":
            case 6:
                message.kind = 6;
                break;
            case "BOT_FEEDBACK_NEGATIVE_REFUSED":
            case 7:
                message.kind = 7;
                break;
            case "BOT_FEEDBACK_NEGATIVE_NOT_VISUALLY_APPEALING":
            case 8:
                message.kind = 8;
                break;
            case "BOT_FEEDBACK_NEGATIVE_NOT_RELEVANT_TO_TEXT":
            case 9:
                message.kind = 9;
                break;
            case "BOT_FEEDBACK_NEGATIVE_PERSONALIZED":
            case 10:
                message.kind = 10;
                break;
            case "BOT_FEEDBACK_NEGATIVE_CLARITY":
            case 11:
                message.kind = 11;
                break;
            case "BOT_FEEDBACK_NEGATIVE_DOESNT_LOOK_LIKE_THE_PERSON":
            case 12:
                message.kind = 12;
                break;
            case "BOT_FEEDBACK_NEGATIVE_HALLUCINATION_INTERNAL_ONLY":
            case 13:
                message.kind = 13;
                break;
            case "BOT_FEEDBACK_NEGATIVE":
            case 14:
                message.kind = 14;
                break;
            default:
                if (typeof object.kind === "number" && (object.kind | 0) === object.kind)
                    message.kind = object.kind;
            }
            if (object.text != null)
                message.text = String(object.text);
            if (object.kindNegative != null)
                if ($util.Long)
                    message.kindNegative = $util.Long.fromValue(object.kindNegative, true);
                else if (typeof object.kindNegative === "string")
                    message.kindNegative = parseInt(object.kindNegative, 10);
                else if (typeof object.kindNegative === "number")
                    message.kindNegative = object.kindNegative;
                else if (typeof object.kindNegative === "object")
                    message.kindNegative = new $util.LongBits(object.kindNegative.low >>> 0, object.kindNegative.high >>> 0).toNumber(true);
            if (object.kindPositive != null)
                if ($util.Long)
                    message.kindPositive = $util.Long.fromValue(object.kindPositive, true);
                else if (typeof object.kindPositive === "string")
                    message.kindPositive = parseInt(object.kindPositive, 10);
                else if (typeof object.kindPositive === "number")
                    message.kindPositive = object.kindPositive;
                else if (typeof object.kindPositive === "object")
                    message.kindPositive = new $util.LongBits(object.kindPositive.low >>> 0, object.kindPositive.high >>> 0).toNumber(true);
            switch (object.kindReport) {
            case "NONE":
            case 0:
                message.kindReport = 0;
                break;
            case "GENERIC":
            case 1:
                message.kindReport = 1;
                break;
            default:
                if (typeof object.kindReport === "number" && (object.kindReport | 0) === object.kindReport)
                    message.kindReport = object.kindReport;
            }
            if (object.sideBySideSurveyMetadata != null) {
                if (!$util.isObject(object.sideBySideSurveyMetadata))
                    throw TypeError(".proto.BotFeedbackMessage.sideBySideSurveyMetadata: object expected");
                message.sideBySideSurveyMetadata = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.fromObject(object.sideBySideSurveyMetadata, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a BotFeedbackMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotFeedbackMessage
         * @static
         * @param {proto.BotFeedbackMessage} message BotFeedbackMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotFeedbackMessage.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.messageKey != null && Object.hasOwnProperty.call(message, "messageKey"))
                object.messageKey = $root.proto.MessageKey.toObject(message.messageKey, options, q + 1);
            if (message.kind != null && Object.hasOwnProperty.call(message, "kind"))
                object.kind = options.enums === String ? $root.proto.BotFeedbackMessage.BotFeedbackKind[message.kind] === undefined ? message.kind : $root.proto.BotFeedbackMessage.BotFeedbackKind[message.kind] : message.kind;
            if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                object.text = message.text;
            if (message.kindNegative != null && Object.hasOwnProperty.call(message, "kindNegative"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.kindNegative = typeof message.kindNegative === "number" ? BigInt(message.kindNegative) : $util.Long.fromBits(message.kindNegative.low >>> 0, message.kindNegative.high >>> 0, true).toBigInt();
                else if (typeof message.kindNegative === "number")
                    object.kindNegative = options.longs === String ? String(message.kindNegative) : message.kindNegative;
                else
                    object.kindNegative = options.longs === String ? $util.Long.prototype.toString.call(message.kindNegative) : options.longs === Number ? new $util.LongBits(message.kindNegative.low >>> 0, message.kindNegative.high >>> 0).toNumber(true) : message.kindNegative;
            if (message.kindPositive != null && Object.hasOwnProperty.call(message, "kindPositive"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.kindPositive = typeof message.kindPositive === "number" ? BigInt(message.kindPositive) : $util.Long.fromBits(message.kindPositive.low >>> 0, message.kindPositive.high >>> 0, true).toBigInt();
                else if (typeof message.kindPositive === "number")
                    object.kindPositive = options.longs === String ? String(message.kindPositive) : message.kindPositive;
                else
                    object.kindPositive = options.longs === String ? $util.Long.prototype.toString.call(message.kindPositive) : options.longs === Number ? new $util.LongBits(message.kindPositive.low >>> 0, message.kindPositive.high >>> 0).toNumber(true) : message.kindPositive;
            if (message.kindReport != null && Object.hasOwnProperty.call(message, "kindReport"))
                object.kindReport = options.enums === String ? $root.proto.BotFeedbackMessage.ReportKind[message.kindReport] === undefined ? message.kindReport : $root.proto.BotFeedbackMessage.ReportKind[message.kindReport] : message.kindReport;
            if (message.sideBySideSurveyMetadata != null && Object.hasOwnProperty.call(message, "sideBySideSurveyMetadata"))
                object.sideBySideSurveyMetadata = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.toObject(message.sideBySideSurveyMetadata, options, q + 1);
            return object;
        };

        /**
         * Converts this BotFeedbackMessage to JSON.
         * @function toJSON
         * @memberof proto.BotFeedbackMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotFeedbackMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotFeedbackMessage
         * @function getTypeUrl
         * @memberof proto.BotFeedbackMessage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotFeedbackMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotFeedbackMessage";
        };

        /**
         * BotFeedbackKind enum.
         * @name proto.BotFeedbackMessage.BotFeedbackKind
         * @enum {number}
         * @property {number} BOT_FEEDBACK_POSITIVE=0 BOT_FEEDBACK_POSITIVE value
         * @property {number} BOT_FEEDBACK_NEGATIVE_GENERIC=1 BOT_FEEDBACK_NEGATIVE_GENERIC value
         * @property {number} BOT_FEEDBACK_NEGATIVE_HELPFUL=2 BOT_FEEDBACK_NEGATIVE_HELPFUL value
         * @property {number} BOT_FEEDBACK_NEGATIVE_INTERESTING=3 BOT_FEEDBACK_NEGATIVE_INTERESTING value
         * @property {number} BOT_FEEDBACK_NEGATIVE_ACCURATE=4 BOT_FEEDBACK_NEGATIVE_ACCURATE value
         * @property {number} BOT_FEEDBACK_NEGATIVE_SAFE=5 BOT_FEEDBACK_NEGATIVE_SAFE value
         * @property {number} BOT_FEEDBACK_NEGATIVE_OTHER=6 BOT_FEEDBACK_NEGATIVE_OTHER value
         * @property {number} BOT_FEEDBACK_NEGATIVE_REFUSED=7 BOT_FEEDBACK_NEGATIVE_REFUSED value
         * @property {number} BOT_FEEDBACK_NEGATIVE_NOT_VISUALLY_APPEALING=8 BOT_FEEDBACK_NEGATIVE_NOT_VISUALLY_APPEALING value
         * @property {number} BOT_FEEDBACK_NEGATIVE_NOT_RELEVANT_TO_TEXT=9 BOT_FEEDBACK_NEGATIVE_NOT_RELEVANT_TO_TEXT value
         * @property {number} BOT_FEEDBACK_NEGATIVE_PERSONALIZED=10 BOT_FEEDBACK_NEGATIVE_PERSONALIZED value
         * @property {number} BOT_FEEDBACK_NEGATIVE_CLARITY=11 BOT_FEEDBACK_NEGATIVE_CLARITY value
         * @property {number} BOT_FEEDBACK_NEGATIVE_DOESNT_LOOK_LIKE_THE_PERSON=12 BOT_FEEDBACK_NEGATIVE_DOESNT_LOOK_LIKE_THE_PERSON value
         * @property {number} BOT_FEEDBACK_NEGATIVE_HALLUCINATION_INTERNAL_ONLY=13 BOT_FEEDBACK_NEGATIVE_HALLUCINATION_INTERNAL_ONLY value
         * @property {number} BOT_FEEDBACK_NEGATIVE=14 BOT_FEEDBACK_NEGATIVE value
         */
        BotFeedbackMessage.BotFeedbackKind = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "BOT_FEEDBACK_POSITIVE"] = 0;
            values[valuesById[1] = "BOT_FEEDBACK_NEGATIVE_GENERIC"] = 1;
            values[valuesById[2] = "BOT_FEEDBACK_NEGATIVE_HELPFUL"] = 2;
            values[valuesById[3] = "BOT_FEEDBACK_NEGATIVE_INTERESTING"] = 3;
            values[valuesById[4] = "BOT_FEEDBACK_NEGATIVE_ACCURATE"] = 4;
            values[valuesById[5] = "BOT_FEEDBACK_NEGATIVE_SAFE"] = 5;
            values[valuesById[6] = "BOT_FEEDBACK_NEGATIVE_OTHER"] = 6;
            values[valuesById[7] = "BOT_FEEDBACK_NEGATIVE_REFUSED"] = 7;
            values[valuesById[8] = "BOT_FEEDBACK_NEGATIVE_NOT_VISUALLY_APPEALING"] = 8;
            values[valuesById[9] = "BOT_FEEDBACK_NEGATIVE_NOT_RELEVANT_TO_TEXT"] = 9;
            values[valuesById[10] = "BOT_FEEDBACK_NEGATIVE_PERSONALIZED"] = 10;
            values[valuesById[11] = "BOT_FEEDBACK_NEGATIVE_CLARITY"] = 11;
            values[valuesById[12] = "BOT_FEEDBACK_NEGATIVE_DOESNT_LOOK_LIKE_THE_PERSON"] = 12;
            values[valuesById[13] = "BOT_FEEDBACK_NEGATIVE_HALLUCINATION_INTERNAL_ONLY"] = 13;
            values[valuesById[14] = "BOT_FEEDBACK_NEGATIVE"] = 14;
            return values;
        })();

        /**
         * BotFeedbackKindMultipleNegative enum.
         * @name proto.BotFeedbackMessage.BotFeedbackKindMultipleNegative
         * @enum {number}
         * @property {number} BOT_FEEDBACK_MULTIPLE_NEGATIVE_GENERIC=1 BOT_FEEDBACK_MULTIPLE_NEGATIVE_GENERIC value
         * @property {number} BOT_FEEDBACK_MULTIPLE_NEGATIVE_HELPFUL=2 BOT_FEEDBACK_MULTIPLE_NEGATIVE_HELPFUL value
         * @property {number} BOT_FEEDBACK_MULTIPLE_NEGATIVE_INTERESTING=4 BOT_FEEDBACK_MULTIPLE_NEGATIVE_INTERESTING value
         * @property {number} BOT_FEEDBACK_MULTIPLE_NEGATIVE_ACCURATE=8 BOT_FEEDBACK_MULTIPLE_NEGATIVE_ACCURATE value
         * @property {number} BOT_FEEDBACK_MULTIPLE_NEGATIVE_SAFE=16 BOT_FEEDBACK_MULTIPLE_NEGATIVE_SAFE value
         * @property {number} BOT_FEEDBACK_MULTIPLE_NEGATIVE_OTHER=32 BOT_FEEDBACK_MULTIPLE_NEGATIVE_OTHER value
         * @property {number} BOT_FEEDBACK_MULTIPLE_NEGATIVE_REFUSED=64 BOT_FEEDBACK_MULTIPLE_NEGATIVE_REFUSED value
         * @property {number} BOT_FEEDBACK_MULTIPLE_NEGATIVE_NOT_VISUALLY_APPEALING=128 BOT_FEEDBACK_MULTIPLE_NEGATIVE_NOT_VISUALLY_APPEALING value
         * @property {number} BOT_FEEDBACK_MULTIPLE_NEGATIVE_NOT_RELEVANT_TO_TEXT=256 BOT_FEEDBACK_MULTIPLE_NEGATIVE_NOT_RELEVANT_TO_TEXT value
         */
        BotFeedbackMessage.BotFeedbackKindMultipleNegative = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[1] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_GENERIC"] = 1;
            values[valuesById[2] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_HELPFUL"] = 2;
            values[valuesById[4] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_INTERESTING"] = 4;
            values[valuesById[8] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_ACCURATE"] = 8;
            values[valuesById[16] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_SAFE"] = 16;
            values[valuesById[32] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_OTHER"] = 32;
            values[valuesById[64] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_REFUSED"] = 64;
            values[valuesById[128] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_NOT_VISUALLY_APPEALING"] = 128;
            values[valuesById[256] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_NOT_RELEVANT_TO_TEXT"] = 256;
            return values;
        })();

        /**
         * BotFeedbackKindMultiplePositive enum.
         * @name proto.BotFeedbackMessage.BotFeedbackKindMultiplePositive
         * @enum {number}
         * @property {number} BOT_FEEDBACK_MULTIPLE_POSITIVE_GENERIC=1 BOT_FEEDBACK_MULTIPLE_POSITIVE_GENERIC value
         */
        BotFeedbackMessage.BotFeedbackKindMultiplePositive = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[1] = "BOT_FEEDBACK_MULTIPLE_POSITIVE_GENERIC"] = 1;
            return values;
        })();

        /**
         * ReportKind enum.
         * @name proto.BotFeedbackMessage.ReportKind
         * @enum {number}
         * @property {number} NONE=0 NONE value
         * @property {number} GENERIC=1 GENERIC value
         */
        BotFeedbackMessage.ReportKind = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "NONE"] = 0;
            values[valuesById[1] = "GENERIC"] = 1;
            return values;
        })();

        BotFeedbackMessage.SideBySideSurveyMetadata = (function() {

            /**
             * Properties of a SideBySideSurveyMetadata.
             * @memberof proto.BotFeedbackMessage
             * @interface ISideBySideSurveyMetadata
             * @property {string|null} [selectedRequestId] SideBySideSurveyMetadata selectedRequestId
             * @property {number|null} [surveyId] SideBySideSurveyMetadata surveyId
             * @property {string|null} [simonSessionFbid] SideBySideSurveyMetadata simonSessionFbid
             * @property {string|null} [responseOtid] SideBySideSurveyMetadata responseOtid
             * @property {string|null} [responseTimestampMsString] SideBySideSurveyMetadata responseTimestampMsString
             * @property {boolean|null} [isSelectedResponsePrimary] SideBySideSurveyMetadata isSelectedResponsePrimary
             * @property {string|null} [messageIdToEdit] SideBySideSurveyMetadata messageIdToEdit
             * @property {proto.BotFeedbackMessage.SideBySideSurveyMetadata.ISideBySideSurveyAnalyticsData|null} [analyticsData] SideBySideSurveyMetadata analyticsData
             * @property {proto.BotFeedbackMessage.SideBySideSurveyMetadata.ISidebySideSurveyMetaAiAnalyticsData|null} [metaAiAnalyticsData] SideBySideSurveyMetadata metaAiAnalyticsData
             */

            /**
             * Constructs a new SideBySideSurveyMetadata.
             * @memberof proto.BotFeedbackMessage
             * @classdesc Represents a SideBySideSurveyMetadata.
             * @implements ISideBySideSurveyMetadata
             * @constructor
             * @param {proto.BotFeedbackMessage.ISideBySideSurveyMetadata=} [properties] Properties to set
             */
            function SideBySideSurveyMetadata(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * SideBySideSurveyMetadata selectedRequestId.
             * @member {string|null|undefined} selectedRequestId
             * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
             * @instance
             */
            SideBySideSurveyMetadata.prototype.selectedRequestId = null;

            /**
             * SideBySideSurveyMetadata surveyId.
             * @member {number|null|undefined} surveyId
             * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
             * @instance
             */
            SideBySideSurveyMetadata.prototype.surveyId = null;

            /**
             * SideBySideSurveyMetadata simonSessionFbid.
             * @member {string|null|undefined} simonSessionFbid
             * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
             * @instance
             */
            SideBySideSurveyMetadata.prototype.simonSessionFbid = null;

            /**
             * SideBySideSurveyMetadata responseOtid.
             * @member {string|null|undefined} responseOtid
             * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
             * @instance
             */
            SideBySideSurveyMetadata.prototype.responseOtid = null;

            /**
             * SideBySideSurveyMetadata responseTimestampMsString.
             * @member {string|null|undefined} responseTimestampMsString
             * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
             * @instance
             */
            SideBySideSurveyMetadata.prototype.responseTimestampMsString = null;

            /**
             * SideBySideSurveyMetadata isSelectedResponsePrimary.
             * @member {boolean|null|undefined} isSelectedResponsePrimary
             * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
             * @instance
             */
            SideBySideSurveyMetadata.prototype.isSelectedResponsePrimary = null;

            /**
             * SideBySideSurveyMetadata messageIdToEdit.
             * @member {string|null|undefined} messageIdToEdit
             * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
             * @instance
             */
            SideBySideSurveyMetadata.prototype.messageIdToEdit = null;

            /**
             * SideBySideSurveyMetadata analyticsData.
             * @member {proto.BotFeedbackMessage.SideBySideSurveyMetadata.ISideBySideSurveyAnalyticsData|null|undefined} analyticsData
             * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
             * @instance
             */
            SideBySideSurveyMetadata.prototype.analyticsData = null;

            /**
             * SideBySideSurveyMetadata metaAiAnalyticsData.
             * @member {proto.BotFeedbackMessage.SideBySideSurveyMetadata.ISidebySideSurveyMetaAiAnalyticsData|null|undefined} metaAiAnalyticsData
             * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
             * @instance
             */
            SideBySideSurveyMetadata.prototype.metaAiAnalyticsData = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideSurveyMetadata.prototype, "_selectedRequestId", {
                get: $util.oneOfGetter($oneOfFields = ["selectedRequestId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideSurveyMetadata.prototype, "_surveyId", {
                get: $util.oneOfGetter($oneOfFields = ["surveyId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideSurveyMetadata.prototype, "_simonSessionFbid", {
                get: $util.oneOfGetter($oneOfFields = ["simonSessionFbid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideSurveyMetadata.prototype, "_responseOtid", {
                get: $util.oneOfGetter($oneOfFields = ["responseOtid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideSurveyMetadata.prototype, "_responseTimestampMsString", {
                get: $util.oneOfGetter($oneOfFields = ["responseTimestampMsString"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideSurveyMetadata.prototype, "_isSelectedResponsePrimary", {
                get: $util.oneOfGetter($oneOfFields = ["isSelectedResponsePrimary"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideSurveyMetadata.prototype, "_messageIdToEdit", {
                get: $util.oneOfGetter($oneOfFields = ["messageIdToEdit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideSurveyMetadata.prototype, "_analyticsData", {
                get: $util.oneOfGetter($oneOfFields = ["analyticsData"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideSurveyMetadata.prototype, "_metaAiAnalyticsData", {
                get: $util.oneOfGetter($oneOfFields = ["metaAiAnalyticsData"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new SideBySideSurveyMetadata instance using the specified properties.
             * @function create
             * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
             * @static
             * @param {proto.BotFeedbackMessage.ISideBySideSurveyMetadata=} [properties] Properties to set
             * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata} SideBySideSurveyMetadata instance
             */
            SideBySideSurveyMetadata.create = function create(properties) {
                return new SideBySideSurveyMetadata(properties);
            };

            /**
             * Encodes the specified SideBySideSurveyMetadata message. Does not implicitly {@link proto.BotFeedbackMessage.SideBySideSurveyMetadata.verify|verify} messages.
             * @function encode
             * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
             * @static
             * @param {proto.BotFeedbackMessage.ISideBySideSurveyMetadata} message SideBySideSurveyMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SideBySideSurveyMetadata.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.selectedRequestId != null && Object.hasOwnProperty.call(message, "selectedRequestId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.selectedRequestId);
                if (message.surveyId != null && Object.hasOwnProperty.call(message, "surveyId"))
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.surveyId);
                if (message.simonSessionFbid != null && Object.hasOwnProperty.call(message, "simonSessionFbid"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.simonSessionFbid);
                if (message.responseOtid != null && Object.hasOwnProperty.call(message, "responseOtid"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.responseOtid);
                if (message.responseTimestampMsString != null && Object.hasOwnProperty.call(message, "responseTimestampMsString"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.responseTimestampMsString);
                if (message.isSelectedResponsePrimary != null && Object.hasOwnProperty.call(message, "isSelectedResponsePrimary"))
                    writer.uint32(/* id 6, wireType 0 =*/48).bool(message.isSelectedResponsePrimary);
                if (message.messageIdToEdit != null && Object.hasOwnProperty.call(message, "messageIdToEdit"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.messageIdToEdit);
                if (message.analyticsData != null && Object.hasOwnProperty.call(message, "analyticsData"))
                    $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData.encode(message.analyticsData, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
                if (message.metaAiAnalyticsData != null && Object.hasOwnProperty.call(message, "metaAiAnalyticsData"))
                    $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.encode(message.metaAiAnalyticsData, writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SideBySideSurveyMetadata message, length delimited. Does not implicitly {@link proto.BotFeedbackMessage.SideBySideSurveyMetadata.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
             * @static
             * @param {proto.BotFeedbackMessage.ISideBySideSurveyMetadata} message SideBySideSurveyMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SideBySideSurveyMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a SideBySideSurveyMetadata message from the specified reader or buffer.
             * @function decode
             * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata} SideBySideSurveyMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SideBySideSurveyMetadata.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.selectedRequestId = reader.stringVerify();
                            message._selectedRequestId = "selectedRequestId";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.surveyId = reader.uint32();
                            message._surveyId = "surveyId";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.simonSessionFbid = reader.stringVerify();
                            message._simonSessionFbid = "simonSessionFbid";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.responseOtid = reader.stringVerify();
                            message._responseOtid = "responseOtid";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.responseTimestampMsString = reader.stringVerify();
                            message._responseTimestampMsString = "responseTimestampMsString";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.isSelectedResponsePrimary = reader.bool();
                            message._isSelectedResponsePrimary = "isSelectedResponsePrimary";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.messageIdToEdit = reader.stringVerify();
                            message._messageIdToEdit = "messageIdToEdit";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.analyticsData = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData.decode(reader, reader.uint32(), undefined, q + 1, message.analyticsData);
                            message._analyticsData = "analyticsData";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            message.metaAiAnalyticsData = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.decode(reader, reader.uint32(), undefined, q + 1, message.metaAiAnalyticsData);
                            message._metaAiAnalyticsData = "metaAiAnalyticsData";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a SideBySideSurveyMetadata message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata} SideBySideSurveyMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SideBySideSurveyMetadata.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SideBySideSurveyMetadata message.
             * @function verify
             * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SideBySideSurveyMetadata.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.selectedRequestId != null && Object.hasOwnProperty.call(message, "selectedRequestId")) {
                    properties._selectedRequestId = 1;
                    if (!$util.isString(message.selectedRequestId))
                        return "selectedRequestId: string expected";
                }
                if (message.surveyId != null && Object.hasOwnProperty.call(message, "surveyId")) {
                    properties._surveyId = 1;
                    if (!$util.isInteger(message.surveyId))
                        return "surveyId: integer expected";
                }
                if (message.simonSessionFbid != null && Object.hasOwnProperty.call(message, "simonSessionFbid")) {
                    properties._simonSessionFbid = 1;
                    if (!$util.isString(message.simonSessionFbid))
                        return "simonSessionFbid: string expected";
                }
                if (message.responseOtid != null && Object.hasOwnProperty.call(message, "responseOtid")) {
                    properties._responseOtid = 1;
                    if (!$util.isString(message.responseOtid))
                        return "responseOtid: string expected";
                }
                if (message.responseTimestampMsString != null && Object.hasOwnProperty.call(message, "responseTimestampMsString")) {
                    properties._responseTimestampMsString = 1;
                    if (!$util.isString(message.responseTimestampMsString))
                        return "responseTimestampMsString: string expected";
                }
                if (message.isSelectedResponsePrimary != null && Object.hasOwnProperty.call(message, "isSelectedResponsePrimary")) {
                    properties._isSelectedResponsePrimary = 1;
                    if (typeof message.isSelectedResponsePrimary !== "boolean")
                        return "isSelectedResponsePrimary: boolean expected";
                }
                if (message.messageIdToEdit != null && Object.hasOwnProperty.call(message, "messageIdToEdit")) {
                    properties._messageIdToEdit = 1;
                    if (!$util.isString(message.messageIdToEdit))
                        return "messageIdToEdit: string expected";
                }
                if (message.analyticsData != null && Object.hasOwnProperty.call(message, "analyticsData")) {
                    properties._analyticsData = 1;
                    {
                        var error = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData.verify(message.analyticsData, q + 1);
                        if (error)
                            return "analyticsData." + error;
                    }
                }
                if (message.metaAiAnalyticsData != null && Object.hasOwnProperty.call(message, "metaAiAnalyticsData")) {
                    properties._metaAiAnalyticsData = 1;
                    {
                        var error = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.verify(message.metaAiAnalyticsData, q + 1);
                        if (error)
                            return "metaAiAnalyticsData." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a SideBySideSurveyMetadata message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata} SideBySideSurveyMetadata
             */
            SideBySideSurveyMetadata.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.selectedRequestId != null)
                    message.selectedRequestId = String(object.selectedRequestId);
                if (object.surveyId != null)
                    message.surveyId = object.surveyId >>> 0;
                if (object.simonSessionFbid != null)
                    message.simonSessionFbid = String(object.simonSessionFbid);
                if (object.responseOtid != null)
                    message.responseOtid = String(object.responseOtid);
                if (object.responseTimestampMsString != null)
                    message.responseTimestampMsString = String(object.responseTimestampMsString);
                if (object.isSelectedResponsePrimary != null)
                    message.isSelectedResponsePrimary = Boolean(object.isSelectedResponsePrimary);
                if (object.messageIdToEdit != null)
                    message.messageIdToEdit = String(object.messageIdToEdit);
                if (object.analyticsData != null) {
                    if (!$util.isObject(object.analyticsData))
                        throw TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.analyticsData: object expected");
                    message.analyticsData = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData.fromObject(object.analyticsData, q + 1);
                }
                if (object.metaAiAnalyticsData != null) {
                    if (!$util.isObject(object.metaAiAnalyticsData))
                        throw TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.metaAiAnalyticsData: object expected");
                    message.metaAiAnalyticsData = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.fromObject(object.metaAiAnalyticsData, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a SideBySideSurveyMetadata message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
             * @static
             * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata} message SideBySideSurveyMetadata
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SideBySideSurveyMetadata.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.selectedRequestId != null && Object.hasOwnProperty.call(message, "selectedRequestId"))
                    object.selectedRequestId = message.selectedRequestId;
                if (message.surveyId != null && Object.hasOwnProperty.call(message, "surveyId"))
                    object.surveyId = message.surveyId;
                if (message.simonSessionFbid != null && Object.hasOwnProperty.call(message, "simonSessionFbid"))
                    object.simonSessionFbid = message.simonSessionFbid;
                if (message.responseOtid != null && Object.hasOwnProperty.call(message, "responseOtid"))
                    object.responseOtid = message.responseOtid;
                if (message.responseTimestampMsString != null && Object.hasOwnProperty.call(message, "responseTimestampMsString"))
                    object.responseTimestampMsString = message.responseTimestampMsString;
                if (message.isSelectedResponsePrimary != null && Object.hasOwnProperty.call(message, "isSelectedResponsePrimary"))
                    object.isSelectedResponsePrimary = message.isSelectedResponsePrimary;
                if (message.messageIdToEdit != null && Object.hasOwnProperty.call(message, "messageIdToEdit"))
                    object.messageIdToEdit = message.messageIdToEdit;
                if (message.analyticsData != null && Object.hasOwnProperty.call(message, "analyticsData"))
                    object.analyticsData = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData.toObject(message.analyticsData, options, q + 1);
                if (message.metaAiAnalyticsData != null && Object.hasOwnProperty.call(message, "metaAiAnalyticsData"))
                    object.metaAiAnalyticsData = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.toObject(message.metaAiAnalyticsData, options, q + 1);
                return object;
            };

            /**
             * Converts this SideBySideSurveyMetadata to JSON.
             * @function toJSON
             * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SideBySideSurveyMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for SideBySideSurveyMetadata
             * @function getTypeUrl
             * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            SideBySideSurveyMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.BotFeedbackMessage.SideBySideSurveyMetadata";
            };

            SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData = (function() {

                /**
                 * Properties of a SideBySideSurveyAnalyticsData.
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
                 * @interface ISideBySideSurveyAnalyticsData
                 * @property {string|null} [tessaEvent] SideBySideSurveyAnalyticsData tessaEvent
                 * @property {string|null} [tessaSessionFbid] SideBySideSurveyAnalyticsData tessaSessionFbid
                 * @property {string|null} [simonSessionFbid] SideBySideSurveyAnalyticsData simonSessionFbid
                 */

                /**
                 * Constructs a new SideBySideSurveyAnalyticsData.
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
                 * @classdesc Represents a SideBySideSurveyAnalyticsData.
                 * @implements ISideBySideSurveyAnalyticsData
                 * @constructor
                 * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.ISideBySideSurveyAnalyticsData=} [properties] Properties to set
                 */
                function SideBySideSurveyAnalyticsData(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * SideBySideSurveyAnalyticsData tessaEvent.
                 * @member {string|null|undefined} tessaEvent
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData
                 * @instance
                 */
                SideBySideSurveyAnalyticsData.prototype.tessaEvent = null;

                /**
                 * SideBySideSurveyAnalyticsData tessaSessionFbid.
                 * @member {string|null|undefined} tessaSessionFbid
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData
                 * @instance
                 */
                SideBySideSurveyAnalyticsData.prototype.tessaSessionFbid = null;

                /**
                 * SideBySideSurveyAnalyticsData simonSessionFbid.
                 * @member {string|null|undefined} simonSessionFbid
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData
                 * @instance
                 */
                SideBySideSurveyAnalyticsData.prototype.simonSessionFbid = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SideBySideSurveyAnalyticsData.prototype, "_tessaEvent", {
                    get: $util.oneOfGetter($oneOfFields = ["tessaEvent"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SideBySideSurveyAnalyticsData.prototype, "_tessaSessionFbid", {
                    get: $util.oneOfGetter($oneOfFields = ["tessaSessionFbid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SideBySideSurveyAnalyticsData.prototype, "_simonSessionFbid", {
                    get: $util.oneOfGetter($oneOfFields = ["simonSessionFbid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new SideBySideSurveyAnalyticsData instance using the specified properties.
                 * @function create
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData
                 * @static
                 * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.ISideBySideSurveyAnalyticsData=} [properties] Properties to set
                 * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData} SideBySideSurveyAnalyticsData instance
                 */
                SideBySideSurveyAnalyticsData.create = function create(properties) {
                    return new SideBySideSurveyAnalyticsData(properties);
                };

                /**
                 * Encodes the specified SideBySideSurveyAnalyticsData message. Does not implicitly {@link proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData.verify|verify} messages.
                 * @function encode
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData
                 * @static
                 * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.ISideBySideSurveyAnalyticsData} message SideBySideSurveyAnalyticsData message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                SideBySideSurveyAnalyticsData.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.tessaEvent != null && Object.hasOwnProperty.call(message, "tessaEvent"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.tessaEvent);
                    if (message.tessaSessionFbid != null && Object.hasOwnProperty.call(message, "tessaSessionFbid"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.tessaSessionFbid);
                    if (message.simonSessionFbid != null && Object.hasOwnProperty.call(message, "simonSessionFbid"))
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.simonSessionFbid);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified SideBySideSurveyAnalyticsData message, length delimited. Does not implicitly {@link proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData
                 * @static
                 * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.ISideBySideSurveyAnalyticsData} message SideBySideSurveyAnalyticsData message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                SideBySideSurveyAnalyticsData.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a SideBySideSurveyAnalyticsData message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData} SideBySideSurveyAnalyticsData
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                SideBySideSurveyAnalyticsData.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.tessaEvent = reader.stringVerify();
                                message._tessaEvent = "tessaEvent";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.tessaSessionFbid = reader.stringVerify();
                                message._tessaSessionFbid = "tessaSessionFbid";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.simonSessionFbid = reader.stringVerify();
                                message._simonSessionFbid = "simonSessionFbid";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a SideBySideSurveyAnalyticsData message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData} SideBySideSurveyAnalyticsData
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                SideBySideSurveyAnalyticsData.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a SideBySideSurveyAnalyticsData message.
                 * @function verify
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                SideBySideSurveyAnalyticsData.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.tessaEvent != null && Object.hasOwnProperty.call(message, "tessaEvent")) {
                        properties._tessaEvent = 1;
                        if (!$util.isString(message.tessaEvent))
                            return "tessaEvent: string expected";
                    }
                    if (message.tessaSessionFbid != null && Object.hasOwnProperty.call(message, "tessaSessionFbid")) {
                        properties._tessaSessionFbid = 1;
                        if (!$util.isString(message.tessaSessionFbid))
                            return "tessaSessionFbid: string expected";
                    }
                    if (message.simonSessionFbid != null && Object.hasOwnProperty.call(message, "simonSessionFbid")) {
                        properties._simonSessionFbid = 1;
                        if (!$util.isString(message.simonSessionFbid))
                            return "simonSessionFbid: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a SideBySideSurveyAnalyticsData message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData} SideBySideSurveyAnalyticsData
                 */
                SideBySideSurveyAnalyticsData.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.tessaEvent != null)
                        message.tessaEvent = String(object.tessaEvent);
                    if (object.tessaSessionFbid != null)
                        message.tessaSessionFbid = String(object.tessaSessionFbid);
                    if (object.simonSessionFbid != null)
                        message.simonSessionFbid = String(object.simonSessionFbid);
                    return message;
                };

                /**
                 * Creates a plain object from a SideBySideSurveyAnalyticsData message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData
                 * @static
                 * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData} message SideBySideSurveyAnalyticsData
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                SideBySideSurveyAnalyticsData.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.tessaEvent != null && Object.hasOwnProperty.call(message, "tessaEvent"))
                        object.tessaEvent = message.tessaEvent;
                    if (message.tessaSessionFbid != null && Object.hasOwnProperty.call(message, "tessaSessionFbid"))
                        object.tessaSessionFbid = message.tessaSessionFbid;
                    if (message.simonSessionFbid != null && Object.hasOwnProperty.call(message, "simonSessionFbid"))
                        object.simonSessionFbid = message.simonSessionFbid;
                    return object;
                };

                /**
                 * Converts this SideBySideSurveyAnalyticsData to JSON.
                 * @function toJSON
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                SideBySideSurveyAnalyticsData.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for SideBySideSurveyAnalyticsData
                 * @function getTypeUrl
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                SideBySideSurveyAnalyticsData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData";
                };

                return SideBySideSurveyAnalyticsData;
            })();

            SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData = (function() {

                /**
                 * Properties of a SidebySideSurveyMetaAiAnalyticsData.
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
                 * @interface ISidebySideSurveyMetaAiAnalyticsData
                 * @property {number|null} [surveyId] SidebySideSurveyMetaAiAnalyticsData surveyId
                 * @property {string|null} [primaryResponseId] SidebySideSurveyMetaAiAnalyticsData primaryResponseId
                 * @property {string|null} [testArmName] SidebySideSurveyMetaAiAnalyticsData testArmName
                 * @property {string|null} [timestampMsString] SidebySideSurveyMetaAiAnalyticsData timestampMsString
                 * @property {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyCTAImpressionEventData|null} [ctaImpressionEvent] SidebySideSurveyMetaAiAnalyticsData ctaImpressionEvent
                 * @property {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyCTAClickEventData|null} [ctaClickEvent] SidebySideSurveyMetaAiAnalyticsData ctaClickEvent
                 * @property {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyCardImpressionEventData|null} [cardImpressionEvent] SidebySideSurveyMetaAiAnalyticsData cardImpressionEvent
                 * @property {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyResponseEventData|null} [responseEvent] SidebySideSurveyMetaAiAnalyticsData responseEvent
                 * @property {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyAbandonEventData|null} [abandonEvent] SidebySideSurveyMetaAiAnalyticsData abandonEvent
                 */

                /**
                 * Constructs a new SidebySideSurveyMetaAiAnalyticsData.
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata
                 * @classdesc Represents a SidebySideSurveyMetaAiAnalyticsData.
                 * @implements ISidebySideSurveyMetaAiAnalyticsData
                 * @constructor
                 * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.ISidebySideSurveyMetaAiAnalyticsData=} [properties] Properties to set
                 */
                function SidebySideSurveyMetaAiAnalyticsData(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * SidebySideSurveyMetaAiAnalyticsData surveyId.
                 * @member {number|null|undefined} surveyId
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                 * @instance
                 */
                SidebySideSurveyMetaAiAnalyticsData.prototype.surveyId = null;

                /**
                 * SidebySideSurveyMetaAiAnalyticsData primaryResponseId.
                 * @member {string|null|undefined} primaryResponseId
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                 * @instance
                 */
                SidebySideSurveyMetaAiAnalyticsData.prototype.primaryResponseId = null;

                /**
                 * SidebySideSurveyMetaAiAnalyticsData testArmName.
                 * @member {string|null|undefined} testArmName
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                 * @instance
                 */
                SidebySideSurveyMetaAiAnalyticsData.prototype.testArmName = null;

                /**
                 * SidebySideSurveyMetaAiAnalyticsData timestampMsString.
                 * @member {string|null|undefined} timestampMsString
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                 * @instance
                 */
                SidebySideSurveyMetaAiAnalyticsData.prototype.timestampMsString = null;

                /**
                 * SidebySideSurveyMetaAiAnalyticsData ctaImpressionEvent.
                 * @member {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyCTAImpressionEventData|null|undefined} ctaImpressionEvent
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                 * @instance
                 */
                SidebySideSurveyMetaAiAnalyticsData.prototype.ctaImpressionEvent = null;

                /**
                 * SidebySideSurveyMetaAiAnalyticsData ctaClickEvent.
                 * @member {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyCTAClickEventData|null|undefined} ctaClickEvent
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                 * @instance
                 */
                SidebySideSurveyMetaAiAnalyticsData.prototype.ctaClickEvent = null;

                /**
                 * SidebySideSurveyMetaAiAnalyticsData cardImpressionEvent.
                 * @member {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyCardImpressionEventData|null|undefined} cardImpressionEvent
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                 * @instance
                 */
                SidebySideSurveyMetaAiAnalyticsData.prototype.cardImpressionEvent = null;

                /**
                 * SidebySideSurveyMetaAiAnalyticsData responseEvent.
                 * @member {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyResponseEventData|null|undefined} responseEvent
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                 * @instance
                 */
                SidebySideSurveyMetaAiAnalyticsData.prototype.responseEvent = null;

                /**
                 * SidebySideSurveyMetaAiAnalyticsData abandonEvent.
                 * @member {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyAbandonEventData|null|undefined} abandonEvent
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                 * @instance
                 */
                SidebySideSurveyMetaAiAnalyticsData.prototype.abandonEvent = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_surveyId", {
                    get: $util.oneOfGetter($oneOfFields = ["surveyId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_primaryResponseId", {
                    get: $util.oneOfGetter($oneOfFields = ["primaryResponseId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_testArmName", {
                    get: $util.oneOfGetter($oneOfFields = ["testArmName"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_timestampMsString", {
                    get: $util.oneOfGetter($oneOfFields = ["timestampMsString"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_ctaImpressionEvent", {
                    get: $util.oneOfGetter($oneOfFields = ["ctaImpressionEvent"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_ctaClickEvent", {
                    get: $util.oneOfGetter($oneOfFields = ["ctaClickEvent"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_cardImpressionEvent", {
                    get: $util.oneOfGetter($oneOfFields = ["cardImpressionEvent"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_responseEvent", {
                    get: $util.oneOfGetter($oneOfFields = ["responseEvent"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_abandonEvent", {
                    get: $util.oneOfGetter($oneOfFields = ["abandonEvent"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new SidebySideSurveyMetaAiAnalyticsData instance using the specified properties.
                 * @function create
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                 * @static
                 * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.ISidebySideSurveyMetaAiAnalyticsData=} [properties] Properties to set
                 * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData} SidebySideSurveyMetaAiAnalyticsData instance
                 */
                SidebySideSurveyMetaAiAnalyticsData.create = function create(properties) {
                    return new SidebySideSurveyMetaAiAnalyticsData(properties);
                };

                /**
                 * Encodes the specified SidebySideSurveyMetaAiAnalyticsData message. Does not implicitly {@link proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.verify|verify} messages.
                 * @function encode
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                 * @static
                 * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.ISidebySideSurveyMetaAiAnalyticsData} message SidebySideSurveyMetaAiAnalyticsData message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                SidebySideSurveyMetaAiAnalyticsData.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.surveyId != null && Object.hasOwnProperty.call(message, "surveyId"))
                        writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.surveyId);
                    if (message.primaryResponseId != null && Object.hasOwnProperty.call(message, "primaryResponseId"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.primaryResponseId);
                    if (message.testArmName != null && Object.hasOwnProperty.call(message, "testArmName"))
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.testArmName);
                    if (message.timestampMsString != null && Object.hasOwnProperty.call(message, "timestampMsString"))
                        writer.uint32(/* id 4, wireType 2 =*/34).string(message.timestampMsString);
                    if (message.ctaImpressionEvent != null && Object.hasOwnProperty.call(message, "ctaImpressionEvent"))
                        $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData.encode(message.ctaImpressionEvent, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                    if (message.ctaClickEvent != null && Object.hasOwnProperty.call(message, "ctaClickEvent"))
                        $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData.encode(message.ctaClickEvent, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
                    if (message.cardImpressionEvent != null && Object.hasOwnProperty.call(message, "cardImpressionEvent"))
                        $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData.encode(message.cardImpressionEvent, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
                    if (message.responseEvent != null && Object.hasOwnProperty.call(message, "responseEvent"))
                        $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData.encode(message.responseEvent, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
                    if (message.abandonEvent != null && Object.hasOwnProperty.call(message, "abandonEvent"))
                        $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData.encode(message.abandonEvent, writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified SidebySideSurveyMetaAiAnalyticsData message, length delimited. Does not implicitly {@link proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                 * @static
                 * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.ISidebySideSurveyMetaAiAnalyticsData} message SidebySideSurveyMetaAiAnalyticsData message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                SidebySideSurveyMetaAiAnalyticsData.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a SidebySideSurveyMetaAiAnalyticsData message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData} SidebySideSurveyMetaAiAnalyticsData
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                SidebySideSurveyMetaAiAnalyticsData.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                message.surveyId = reader.uint32();
                                message._surveyId = "surveyId";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.primaryResponseId = reader.stringVerify();
                                message._primaryResponseId = "primaryResponseId";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.testArmName = reader.stringVerify();
                                message._testArmName = "testArmName";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                message.timestampMsString = reader.stringVerify();
                                message._timestampMsString = "timestampMsString";
                                continue;
                            }
                        case 5: {
                                if (u !== 2)
                                    break;
                                message.ctaImpressionEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData.decode(reader, reader.uint32(), undefined, q + 1, message.ctaImpressionEvent);
                                message._ctaImpressionEvent = "ctaImpressionEvent";
                                continue;
                            }
                        case 6: {
                                if (u !== 2)
                                    break;
                                message.ctaClickEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData.decode(reader, reader.uint32(), undefined, q + 1, message.ctaClickEvent);
                                message._ctaClickEvent = "ctaClickEvent";
                                continue;
                            }
                        case 7: {
                                if (u !== 2)
                                    break;
                                message.cardImpressionEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData.decode(reader, reader.uint32(), undefined, q + 1, message.cardImpressionEvent);
                                message._cardImpressionEvent = "cardImpressionEvent";
                                continue;
                            }
                        case 8: {
                                if (u !== 2)
                                    break;
                                message.responseEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData.decode(reader, reader.uint32(), undefined, q + 1, message.responseEvent);
                                message._responseEvent = "responseEvent";
                                continue;
                            }
                        case 9: {
                                if (u !== 2)
                                    break;
                                message.abandonEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData.decode(reader, reader.uint32(), undefined, q + 1, message.abandonEvent);
                                message._abandonEvent = "abandonEvent";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a SidebySideSurveyMetaAiAnalyticsData message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData} SidebySideSurveyMetaAiAnalyticsData
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                SidebySideSurveyMetaAiAnalyticsData.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a SidebySideSurveyMetaAiAnalyticsData message.
                 * @function verify
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                SidebySideSurveyMetaAiAnalyticsData.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.surveyId != null && Object.hasOwnProperty.call(message, "surveyId")) {
                        properties._surveyId = 1;
                        if (!$util.isInteger(message.surveyId))
                            return "surveyId: integer expected";
                    }
                    if (message.primaryResponseId != null && Object.hasOwnProperty.call(message, "primaryResponseId")) {
                        properties._primaryResponseId = 1;
                        if (!$util.isString(message.primaryResponseId))
                            return "primaryResponseId: string expected";
                    }
                    if (message.testArmName != null && Object.hasOwnProperty.call(message, "testArmName")) {
                        properties._testArmName = 1;
                        if (!$util.isString(message.testArmName))
                            return "testArmName: string expected";
                    }
                    if (message.timestampMsString != null && Object.hasOwnProperty.call(message, "timestampMsString")) {
                        properties._timestampMsString = 1;
                        if (!$util.isString(message.timestampMsString))
                            return "timestampMsString: string expected";
                    }
                    if (message.ctaImpressionEvent != null && Object.hasOwnProperty.call(message, "ctaImpressionEvent")) {
                        properties._ctaImpressionEvent = 1;
                        {
                            var error = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData.verify(message.ctaImpressionEvent, q + 1);
                            if (error)
                                return "ctaImpressionEvent." + error;
                        }
                    }
                    if (message.ctaClickEvent != null && Object.hasOwnProperty.call(message, "ctaClickEvent")) {
                        properties._ctaClickEvent = 1;
                        {
                            var error = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData.verify(message.ctaClickEvent, q + 1);
                            if (error)
                                return "ctaClickEvent." + error;
                        }
                    }
                    if (message.cardImpressionEvent != null && Object.hasOwnProperty.call(message, "cardImpressionEvent")) {
                        properties._cardImpressionEvent = 1;
                        {
                            var error = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData.verify(message.cardImpressionEvent, q + 1);
                            if (error)
                                return "cardImpressionEvent." + error;
                        }
                    }
                    if (message.responseEvent != null && Object.hasOwnProperty.call(message, "responseEvent")) {
                        properties._responseEvent = 1;
                        {
                            var error = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData.verify(message.responseEvent, q + 1);
                            if (error)
                                return "responseEvent." + error;
                        }
                    }
                    if (message.abandonEvent != null && Object.hasOwnProperty.call(message, "abandonEvent")) {
                        properties._abandonEvent = 1;
                        {
                            var error = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData.verify(message.abandonEvent, q + 1);
                            if (error)
                                return "abandonEvent." + error;
                        }
                    }
                    return null;
                };

                /**
                 * Creates a SidebySideSurveyMetaAiAnalyticsData message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData} SidebySideSurveyMetaAiAnalyticsData
                 */
                SidebySideSurveyMetaAiAnalyticsData.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.surveyId != null)
                        message.surveyId = object.surveyId >>> 0;
                    if (object.primaryResponseId != null)
                        message.primaryResponseId = String(object.primaryResponseId);
                    if (object.testArmName != null)
                        message.testArmName = String(object.testArmName);
                    if (object.timestampMsString != null)
                        message.timestampMsString = String(object.timestampMsString);
                    if (object.ctaImpressionEvent != null) {
                        if (!$util.isObject(object.ctaImpressionEvent))
                            throw TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ctaImpressionEvent: object expected");
                        message.ctaImpressionEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData.fromObject(object.ctaImpressionEvent, q + 1);
                    }
                    if (object.ctaClickEvent != null) {
                        if (!$util.isObject(object.ctaClickEvent))
                            throw TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ctaClickEvent: object expected");
                        message.ctaClickEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData.fromObject(object.ctaClickEvent, q + 1);
                    }
                    if (object.cardImpressionEvent != null) {
                        if (!$util.isObject(object.cardImpressionEvent))
                            throw TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.cardImpressionEvent: object expected");
                        message.cardImpressionEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData.fromObject(object.cardImpressionEvent, q + 1);
                    }
                    if (object.responseEvent != null) {
                        if (!$util.isObject(object.responseEvent))
                            throw TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.responseEvent: object expected");
                        message.responseEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData.fromObject(object.responseEvent, q + 1);
                    }
                    if (object.abandonEvent != null) {
                        if (!$util.isObject(object.abandonEvent))
                            throw TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.abandonEvent: object expected");
                        message.abandonEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData.fromObject(object.abandonEvent, q + 1);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a SidebySideSurveyMetaAiAnalyticsData message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                 * @static
                 * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData} message SidebySideSurveyMetaAiAnalyticsData
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                SidebySideSurveyMetaAiAnalyticsData.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.surveyId != null && Object.hasOwnProperty.call(message, "surveyId"))
                        object.surveyId = message.surveyId;
                    if (message.primaryResponseId != null && Object.hasOwnProperty.call(message, "primaryResponseId"))
                        object.primaryResponseId = message.primaryResponseId;
                    if (message.testArmName != null && Object.hasOwnProperty.call(message, "testArmName"))
                        object.testArmName = message.testArmName;
                    if (message.timestampMsString != null && Object.hasOwnProperty.call(message, "timestampMsString"))
                        object.timestampMsString = message.timestampMsString;
                    if (message.ctaImpressionEvent != null && Object.hasOwnProperty.call(message, "ctaImpressionEvent"))
                        object.ctaImpressionEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData.toObject(message.ctaImpressionEvent, options, q + 1);
                    if (message.ctaClickEvent != null && Object.hasOwnProperty.call(message, "ctaClickEvent"))
                        object.ctaClickEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData.toObject(message.ctaClickEvent, options, q + 1);
                    if (message.cardImpressionEvent != null && Object.hasOwnProperty.call(message, "cardImpressionEvent"))
                        object.cardImpressionEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData.toObject(message.cardImpressionEvent, options, q + 1);
                    if (message.responseEvent != null && Object.hasOwnProperty.call(message, "responseEvent"))
                        object.responseEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData.toObject(message.responseEvent, options, q + 1);
                    if (message.abandonEvent != null && Object.hasOwnProperty.call(message, "abandonEvent"))
                        object.abandonEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData.toObject(message.abandonEvent, options, q + 1);
                    return object;
                };

                /**
                 * Converts this SidebySideSurveyMetaAiAnalyticsData to JSON.
                 * @function toJSON
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                SidebySideSurveyMetaAiAnalyticsData.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for SidebySideSurveyMetaAiAnalyticsData
                 * @function getTypeUrl
                 * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                SidebySideSurveyMetaAiAnalyticsData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData";
                };

                SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData = (function() {

                    /**
                     * Properties of a SideBySideSurveyAbandonEventData.
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                     * @interface ISideBySideSurveyAbandonEventData
                     * @property {string|null} [abandonDwellTimeMsString] SideBySideSurveyAbandonEventData abandonDwellTimeMsString
                     */

                    /**
                     * Constructs a new SideBySideSurveyAbandonEventData.
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                     * @classdesc Represents a SideBySideSurveyAbandonEventData.
                     * @implements ISideBySideSurveyAbandonEventData
                     * @constructor
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyAbandonEventData=} [properties] Properties to set
                     */
                    function SideBySideSurveyAbandonEventData(properties) {
                        if (properties)
                            for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    /**
                     * SideBySideSurveyAbandonEventData abandonDwellTimeMsString.
                     * @member {string|null|undefined} abandonDwellTimeMsString
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData
                     * @instance
                     */
                    SideBySideSurveyAbandonEventData.prototype.abandonDwellTimeMsString = null;

                    // OneOf field names bound to virtual getters and setters
                    var $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(SideBySideSurveyAbandonEventData.prototype, "_abandonDwellTimeMsString", {
                        get: $util.oneOfGetter($oneOfFields = ["abandonDwellTimeMsString"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    /**
                     * Creates a new SideBySideSurveyAbandonEventData instance using the specified properties.
                     * @function create
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData
                     * @static
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyAbandonEventData=} [properties] Properties to set
                     * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData} SideBySideSurveyAbandonEventData instance
                     */
                    SideBySideSurveyAbandonEventData.create = function create(properties) {
                        return new SideBySideSurveyAbandonEventData(properties);
                    };

                    /**
                     * Encodes the specified SideBySideSurveyAbandonEventData message. Does not implicitly {@link proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData.verify|verify} messages.
                     * @function encode
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData
                     * @static
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyAbandonEventData} message SideBySideSurveyAbandonEventData message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    SideBySideSurveyAbandonEventData.encode = function encode(message, writer, q) {
                        if (!writer)
                            writer = $Writer.create();
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        if (message.abandonDwellTimeMsString != null && Object.hasOwnProperty.call(message, "abandonDwellTimeMsString"))
                            writer.uint32(/* id 1, wireType 2 =*/10).string(message.abandonDwellTimeMsString);
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (var i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    /**
                     * Encodes the specified SideBySideSurveyAbandonEventData message, length delimited. Does not implicitly {@link proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData.verify|verify} messages.
                     * @function encodeDelimited
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData
                     * @static
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyAbandonEventData} message SideBySideSurveyAbandonEventData message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    SideBySideSurveyAbandonEventData.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                    };

                    /**
                     * Decodes a SideBySideSurveyAbandonEventData message from the specified reader or buffer.
                     * @function decode
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @param {number} [length] Message length if known beforehand
                     * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData} SideBySideSurveyAbandonEventData
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    SideBySideSurveyAbandonEventData.decode = function decode(reader, length, z, q, g) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (q === undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                        while (reader.pos < end) {
                            var s = reader.pos;
                            var tag = reader.tag();
                            if (tag === z) {
                                z = undefined;
                                break;
                            }
                            var u = tag & 7;
                            switch (tag >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    message.abandonDwellTimeMsString = reader.stringVerify();
                                    message._abandonDwellTimeMsString = "abandonDwellTimeMsString";
                                    continue;
                                }
                            }
                            reader.skipType(u, q, tag);
                            if (!reader.discardUnknown) {
                                $util.makeProp(message, "$unknowns", false);
                                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                            }
                        }
                        if (z !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    /**
                     * Decodes a SideBySideSurveyAbandonEventData message from the specified reader or buffer, length delimited.
                     * @function decodeDelimited
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData} SideBySideSurveyAbandonEventData
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    SideBySideSurveyAbandonEventData.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    /**
                     * Verifies a SideBySideSurveyAbandonEventData message.
                     * @function verify
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData
                     * @static
                     * @param {Object.<string,*>} message Plain object to verify
                     * @returns {string|null} `null` if valid, otherwise the reason why it is not
                     */
                    SideBySideSurveyAbandonEventData.verify = function verify(message, q) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            return "max depth exceeded";
                        var properties = {};
                        if (message.abandonDwellTimeMsString != null && Object.hasOwnProperty.call(message, "abandonDwellTimeMsString")) {
                            properties._abandonDwellTimeMsString = 1;
                            if (!$util.isString(message.abandonDwellTimeMsString))
                                return "abandonDwellTimeMsString: string expected";
                        }
                        return null;
                    };

                    /**
                     * Creates a SideBySideSurveyAbandonEventData message from a plain object. Also converts values to their respective internal types.
                     * @function fromObject
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData
                     * @static
                     * @param {Object.<string,*>} object Plain object
                     * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData} SideBySideSurveyAbandonEventData
                     */
                    SideBySideSurveyAbandonEventData.fromObject = function fromObject(object, q) {
                        if (object instanceof C)
                            return object;
                        if (!$util.isObject(object))
                            throw TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData: object expected");
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var message = new C();
                        if (object.abandonDwellTimeMsString != null)
                            message.abandonDwellTimeMsString = String(object.abandonDwellTimeMsString);
                        return message;
                    };

                    /**
                     * Creates a plain object from a SideBySideSurveyAbandonEventData message. Also converts values to other types if specified.
                     * @function toObject
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData
                     * @static
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData} message SideBySideSurveyAbandonEventData
                     * @param {$protobuf.IConversionOptions} [options] Conversion options
                     * @returns {Object.<string,*>} Plain object
                     */
                    SideBySideSurveyAbandonEventData.toObject = function toObject(message, options, q) {
                        if (!options)
                            options = {};
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var object = {};
                        if (message.abandonDwellTimeMsString != null && Object.hasOwnProperty.call(message, "abandonDwellTimeMsString"))
                            object.abandonDwellTimeMsString = message.abandonDwellTimeMsString;
                        return object;
                    };

                    /**
                     * Converts this SideBySideSurveyAbandonEventData to JSON.
                     * @function toJSON
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData
                     * @instance
                     * @returns {Object.<string,*>} JSON object
                     */
                    SideBySideSurveyAbandonEventData.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    /**
                     * Gets the default type url for SideBySideSurveyAbandonEventData
                     * @function getTypeUrl
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData
                     * @static
                     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                     * @returns {string} The default type url
                     */
                    SideBySideSurveyAbandonEventData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData";
                    };

                    return SideBySideSurveyAbandonEventData;
                })();

                SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData = (function() {

                    /**
                     * Properties of a SideBySideSurveyCTAClickEventData.
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                     * @interface ISideBySideSurveyCTAClickEventData
                     * @property {boolean|null} [isSurveyExpired] SideBySideSurveyCTAClickEventData isSurveyExpired
                     * @property {string|null} [clickDwellTimeMsString] SideBySideSurveyCTAClickEventData clickDwellTimeMsString
                     */

                    /**
                     * Constructs a new SideBySideSurveyCTAClickEventData.
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                     * @classdesc Represents a SideBySideSurveyCTAClickEventData.
                     * @implements ISideBySideSurveyCTAClickEventData
                     * @constructor
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyCTAClickEventData=} [properties] Properties to set
                     */
                    function SideBySideSurveyCTAClickEventData(properties) {
                        if (properties)
                            for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    /**
                     * SideBySideSurveyCTAClickEventData isSurveyExpired.
                     * @member {boolean|null|undefined} isSurveyExpired
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData
                     * @instance
                     */
                    SideBySideSurveyCTAClickEventData.prototype.isSurveyExpired = null;

                    /**
                     * SideBySideSurveyCTAClickEventData clickDwellTimeMsString.
                     * @member {string|null|undefined} clickDwellTimeMsString
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData
                     * @instance
                     */
                    SideBySideSurveyCTAClickEventData.prototype.clickDwellTimeMsString = null;

                    // OneOf field names bound to virtual getters and setters
                    var $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(SideBySideSurveyCTAClickEventData.prototype, "_isSurveyExpired", {
                        get: $util.oneOfGetter($oneOfFields = ["isSurveyExpired"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(SideBySideSurveyCTAClickEventData.prototype, "_clickDwellTimeMsString", {
                        get: $util.oneOfGetter($oneOfFields = ["clickDwellTimeMsString"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    /**
                     * Creates a new SideBySideSurveyCTAClickEventData instance using the specified properties.
                     * @function create
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData
                     * @static
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyCTAClickEventData=} [properties] Properties to set
                     * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData} SideBySideSurveyCTAClickEventData instance
                     */
                    SideBySideSurveyCTAClickEventData.create = function create(properties) {
                        return new SideBySideSurveyCTAClickEventData(properties);
                    };

                    /**
                     * Encodes the specified SideBySideSurveyCTAClickEventData message. Does not implicitly {@link proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData.verify|verify} messages.
                     * @function encode
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData
                     * @static
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyCTAClickEventData} message SideBySideSurveyCTAClickEventData message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    SideBySideSurveyCTAClickEventData.encode = function encode(message, writer, q) {
                        if (!writer)
                            writer = $Writer.create();
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        if (message.isSurveyExpired != null && Object.hasOwnProperty.call(message, "isSurveyExpired"))
                            writer.uint32(/* id 1, wireType 0 =*/8).bool(message.isSurveyExpired);
                        if (message.clickDwellTimeMsString != null && Object.hasOwnProperty.call(message, "clickDwellTimeMsString"))
                            writer.uint32(/* id 2, wireType 2 =*/18).string(message.clickDwellTimeMsString);
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (var i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    /**
                     * Encodes the specified SideBySideSurveyCTAClickEventData message, length delimited. Does not implicitly {@link proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData.verify|verify} messages.
                     * @function encodeDelimited
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData
                     * @static
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyCTAClickEventData} message SideBySideSurveyCTAClickEventData message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    SideBySideSurveyCTAClickEventData.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                    };

                    /**
                     * Decodes a SideBySideSurveyCTAClickEventData message from the specified reader or buffer.
                     * @function decode
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @param {number} [length] Message length if known beforehand
                     * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData} SideBySideSurveyCTAClickEventData
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    SideBySideSurveyCTAClickEventData.decode = function decode(reader, length, z, q, g) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (q === undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                        while (reader.pos < end) {
                            var s = reader.pos;
                            var tag = reader.tag();
                            if (tag === z) {
                                z = undefined;
                                break;
                            }
                            var u = tag & 7;
                            switch (tag >>>= 3) {
                            case 1: {
                                    if (u !== 0)
                                        break;
                                    message.isSurveyExpired = reader.bool();
                                    message._isSurveyExpired = "isSurveyExpired";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 2)
                                        break;
                                    message.clickDwellTimeMsString = reader.stringVerify();
                                    message._clickDwellTimeMsString = "clickDwellTimeMsString";
                                    continue;
                                }
                            }
                            reader.skipType(u, q, tag);
                            if (!reader.discardUnknown) {
                                $util.makeProp(message, "$unknowns", false);
                                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                            }
                        }
                        if (z !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    /**
                     * Decodes a SideBySideSurveyCTAClickEventData message from the specified reader or buffer, length delimited.
                     * @function decodeDelimited
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData} SideBySideSurveyCTAClickEventData
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    SideBySideSurveyCTAClickEventData.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    /**
                     * Verifies a SideBySideSurveyCTAClickEventData message.
                     * @function verify
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData
                     * @static
                     * @param {Object.<string,*>} message Plain object to verify
                     * @returns {string|null} `null` if valid, otherwise the reason why it is not
                     */
                    SideBySideSurveyCTAClickEventData.verify = function verify(message, q) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            return "max depth exceeded";
                        var properties = {};
                        if (message.isSurveyExpired != null && Object.hasOwnProperty.call(message, "isSurveyExpired")) {
                            properties._isSurveyExpired = 1;
                            if (typeof message.isSurveyExpired !== "boolean")
                                return "isSurveyExpired: boolean expected";
                        }
                        if (message.clickDwellTimeMsString != null && Object.hasOwnProperty.call(message, "clickDwellTimeMsString")) {
                            properties._clickDwellTimeMsString = 1;
                            if (!$util.isString(message.clickDwellTimeMsString))
                                return "clickDwellTimeMsString: string expected";
                        }
                        return null;
                    };

                    /**
                     * Creates a SideBySideSurveyCTAClickEventData message from a plain object. Also converts values to their respective internal types.
                     * @function fromObject
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData
                     * @static
                     * @param {Object.<string,*>} object Plain object
                     * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData} SideBySideSurveyCTAClickEventData
                     */
                    SideBySideSurveyCTAClickEventData.fromObject = function fromObject(object, q) {
                        if (object instanceof C)
                            return object;
                        if (!$util.isObject(object))
                            throw TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData: object expected");
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var message = new C();
                        if (object.isSurveyExpired != null)
                            message.isSurveyExpired = Boolean(object.isSurveyExpired);
                        if (object.clickDwellTimeMsString != null)
                            message.clickDwellTimeMsString = String(object.clickDwellTimeMsString);
                        return message;
                    };

                    /**
                     * Creates a plain object from a SideBySideSurveyCTAClickEventData message. Also converts values to other types if specified.
                     * @function toObject
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData
                     * @static
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData} message SideBySideSurveyCTAClickEventData
                     * @param {$protobuf.IConversionOptions} [options] Conversion options
                     * @returns {Object.<string,*>} Plain object
                     */
                    SideBySideSurveyCTAClickEventData.toObject = function toObject(message, options, q) {
                        if (!options)
                            options = {};
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var object = {};
                        if (message.isSurveyExpired != null && Object.hasOwnProperty.call(message, "isSurveyExpired"))
                            object.isSurveyExpired = message.isSurveyExpired;
                        if (message.clickDwellTimeMsString != null && Object.hasOwnProperty.call(message, "clickDwellTimeMsString"))
                            object.clickDwellTimeMsString = message.clickDwellTimeMsString;
                        return object;
                    };

                    /**
                     * Converts this SideBySideSurveyCTAClickEventData to JSON.
                     * @function toJSON
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData
                     * @instance
                     * @returns {Object.<string,*>} JSON object
                     */
                    SideBySideSurveyCTAClickEventData.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    /**
                     * Gets the default type url for SideBySideSurveyCTAClickEventData
                     * @function getTypeUrl
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData
                     * @static
                     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                     * @returns {string} The default type url
                     */
                    SideBySideSurveyCTAClickEventData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData";
                    };

                    return SideBySideSurveyCTAClickEventData;
                })();

                SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData = (function() {

                    /**
                     * Properties of a SideBySideSurveyCTAImpressionEventData.
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                     * @interface ISideBySideSurveyCTAImpressionEventData
                     * @property {boolean|null} [isSurveyExpired] SideBySideSurveyCTAImpressionEventData isSurveyExpired
                     */

                    /**
                     * Constructs a new SideBySideSurveyCTAImpressionEventData.
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                     * @classdesc Represents a SideBySideSurveyCTAImpressionEventData.
                     * @implements ISideBySideSurveyCTAImpressionEventData
                     * @constructor
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyCTAImpressionEventData=} [properties] Properties to set
                     */
                    function SideBySideSurveyCTAImpressionEventData(properties) {
                        if (properties)
                            for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    /**
                     * SideBySideSurveyCTAImpressionEventData isSurveyExpired.
                     * @member {boolean|null|undefined} isSurveyExpired
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData
                     * @instance
                     */
                    SideBySideSurveyCTAImpressionEventData.prototype.isSurveyExpired = null;

                    // OneOf field names bound to virtual getters and setters
                    var $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(SideBySideSurveyCTAImpressionEventData.prototype, "_isSurveyExpired", {
                        get: $util.oneOfGetter($oneOfFields = ["isSurveyExpired"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    /**
                     * Creates a new SideBySideSurveyCTAImpressionEventData instance using the specified properties.
                     * @function create
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData
                     * @static
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyCTAImpressionEventData=} [properties] Properties to set
                     * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData} SideBySideSurveyCTAImpressionEventData instance
                     */
                    SideBySideSurveyCTAImpressionEventData.create = function create(properties) {
                        return new SideBySideSurveyCTAImpressionEventData(properties);
                    };

                    /**
                     * Encodes the specified SideBySideSurveyCTAImpressionEventData message. Does not implicitly {@link proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData.verify|verify} messages.
                     * @function encode
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData
                     * @static
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyCTAImpressionEventData} message SideBySideSurveyCTAImpressionEventData message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    SideBySideSurveyCTAImpressionEventData.encode = function encode(message, writer, q) {
                        if (!writer)
                            writer = $Writer.create();
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        if (message.isSurveyExpired != null && Object.hasOwnProperty.call(message, "isSurveyExpired"))
                            writer.uint32(/* id 1, wireType 0 =*/8).bool(message.isSurveyExpired);
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (var i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    /**
                     * Encodes the specified SideBySideSurveyCTAImpressionEventData message, length delimited. Does not implicitly {@link proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData.verify|verify} messages.
                     * @function encodeDelimited
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData
                     * @static
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyCTAImpressionEventData} message SideBySideSurveyCTAImpressionEventData message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    SideBySideSurveyCTAImpressionEventData.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                    };

                    /**
                     * Decodes a SideBySideSurveyCTAImpressionEventData message from the specified reader or buffer.
                     * @function decode
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @param {number} [length] Message length if known beforehand
                     * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData} SideBySideSurveyCTAImpressionEventData
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    SideBySideSurveyCTAImpressionEventData.decode = function decode(reader, length, z, q, g) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (q === undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                        while (reader.pos < end) {
                            var s = reader.pos;
                            var tag = reader.tag();
                            if (tag === z) {
                                z = undefined;
                                break;
                            }
                            var u = tag & 7;
                            switch (tag >>>= 3) {
                            case 1: {
                                    if (u !== 0)
                                        break;
                                    message.isSurveyExpired = reader.bool();
                                    message._isSurveyExpired = "isSurveyExpired";
                                    continue;
                                }
                            }
                            reader.skipType(u, q, tag);
                            if (!reader.discardUnknown) {
                                $util.makeProp(message, "$unknowns", false);
                                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                            }
                        }
                        if (z !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    /**
                     * Decodes a SideBySideSurveyCTAImpressionEventData message from the specified reader or buffer, length delimited.
                     * @function decodeDelimited
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData} SideBySideSurveyCTAImpressionEventData
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    SideBySideSurveyCTAImpressionEventData.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    /**
                     * Verifies a SideBySideSurveyCTAImpressionEventData message.
                     * @function verify
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData
                     * @static
                     * @param {Object.<string,*>} message Plain object to verify
                     * @returns {string|null} `null` if valid, otherwise the reason why it is not
                     */
                    SideBySideSurveyCTAImpressionEventData.verify = function verify(message, q) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            return "max depth exceeded";
                        var properties = {};
                        if (message.isSurveyExpired != null && Object.hasOwnProperty.call(message, "isSurveyExpired")) {
                            properties._isSurveyExpired = 1;
                            if (typeof message.isSurveyExpired !== "boolean")
                                return "isSurveyExpired: boolean expected";
                        }
                        return null;
                    };

                    /**
                     * Creates a SideBySideSurveyCTAImpressionEventData message from a plain object. Also converts values to their respective internal types.
                     * @function fromObject
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData
                     * @static
                     * @param {Object.<string,*>} object Plain object
                     * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData} SideBySideSurveyCTAImpressionEventData
                     */
                    SideBySideSurveyCTAImpressionEventData.fromObject = function fromObject(object, q) {
                        if (object instanceof C)
                            return object;
                        if (!$util.isObject(object))
                            throw TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData: object expected");
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var message = new C();
                        if (object.isSurveyExpired != null)
                            message.isSurveyExpired = Boolean(object.isSurveyExpired);
                        return message;
                    };

                    /**
                     * Creates a plain object from a SideBySideSurveyCTAImpressionEventData message. Also converts values to other types if specified.
                     * @function toObject
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData
                     * @static
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData} message SideBySideSurveyCTAImpressionEventData
                     * @param {$protobuf.IConversionOptions} [options] Conversion options
                     * @returns {Object.<string,*>} Plain object
                     */
                    SideBySideSurveyCTAImpressionEventData.toObject = function toObject(message, options, q) {
                        if (!options)
                            options = {};
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var object = {};
                        if (message.isSurveyExpired != null && Object.hasOwnProperty.call(message, "isSurveyExpired"))
                            object.isSurveyExpired = message.isSurveyExpired;
                        return object;
                    };

                    /**
                     * Converts this SideBySideSurveyCTAImpressionEventData to JSON.
                     * @function toJSON
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData
                     * @instance
                     * @returns {Object.<string,*>} JSON object
                     */
                    SideBySideSurveyCTAImpressionEventData.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    /**
                     * Gets the default type url for SideBySideSurveyCTAImpressionEventData
                     * @function getTypeUrl
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData
                     * @static
                     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                     * @returns {string} The default type url
                     */
                    SideBySideSurveyCTAImpressionEventData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData";
                    };

                    return SideBySideSurveyCTAImpressionEventData;
                })();

                SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData = (function() {

                    /**
                     * Properties of a SideBySideSurveyCardImpressionEventData.
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                     * @interface ISideBySideSurveyCardImpressionEventData
                     */

                    /**
                     * Constructs a new SideBySideSurveyCardImpressionEventData.
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                     * @classdesc Represents a SideBySideSurveyCardImpressionEventData.
                     * @implements ISideBySideSurveyCardImpressionEventData
                     * @constructor
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyCardImpressionEventData=} [properties] Properties to set
                     */
                    function SideBySideSurveyCardImpressionEventData(properties) {
                        if (properties)
                            for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    /**
                     * Creates a new SideBySideSurveyCardImpressionEventData instance using the specified properties.
                     * @function create
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData
                     * @static
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyCardImpressionEventData=} [properties] Properties to set
                     * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData} SideBySideSurveyCardImpressionEventData instance
                     */
                    SideBySideSurveyCardImpressionEventData.create = function create(properties) {
                        return new SideBySideSurveyCardImpressionEventData(properties);
                    };

                    /**
                     * Encodes the specified SideBySideSurveyCardImpressionEventData message. Does not implicitly {@link proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData.verify|verify} messages.
                     * @function encode
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData
                     * @static
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyCardImpressionEventData} message SideBySideSurveyCardImpressionEventData message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    SideBySideSurveyCardImpressionEventData.encode = function encode(message, writer, q) {
                        if (!writer)
                            writer = $Writer.create();
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (var i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    /**
                     * Encodes the specified SideBySideSurveyCardImpressionEventData message, length delimited. Does not implicitly {@link proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData.verify|verify} messages.
                     * @function encodeDelimited
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData
                     * @static
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyCardImpressionEventData} message SideBySideSurveyCardImpressionEventData message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    SideBySideSurveyCardImpressionEventData.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                    };

                    /**
                     * Decodes a SideBySideSurveyCardImpressionEventData message from the specified reader or buffer.
                     * @function decode
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @param {number} [length] Message length if known beforehand
                     * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData} SideBySideSurveyCardImpressionEventData
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    SideBySideSurveyCardImpressionEventData.decode = function decode(reader, length, z, q, g) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (q === undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                        while (reader.pos < end) {
                            var s = reader.pos;
                            var tag = reader.tag();
                            if (tag === z) {
                                z = undefined;
                                break;
                            }
                            reader.skipType(tag & 7, q, tag);
                            if (!reader.discardUnknown) {
                                $util.makeProp(message, "$unknowns", false);
                                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                            }
                        }
                        if (z !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    /**
                     * Decodes a SideBySideSurveyCardImpressionEventData message from the specified reader or buffer, length delimited.
                     * @function decodeDelimited
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData} SideBySideSurveyCardImpressionEventData
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    SideBySideSurveyCardImpressionEventData.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    /**
                     * Verifies a SideBySideSurveyCardImpressionEventData message.
                     * @function verify
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData
                     * @static
                     * @param {Object.<string,*>} message Plain object to verify
                     * @returns {string|null} `null` if valid, otherwise the reason why it is not
                     */
                    SideBySideSurveyCardImpressionEventData.verify = function verify(message, q) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            return "max depth exceeded";
                        return null;
                    };

                    /**
                     * Creates a SideBySideSurveyCardImpressionEventData message from a plain object. Also converts values to their respective internal types.
                     * @function fromObject
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData
                     * @static
                     * @param {Object.<string,*>} object Plain object
                     * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData} SideBySideSurveyCardImpressionEventData
                     */
                    SideBySideSurveyCardImpressionEventData.fromObject = function fromObject(object, q) {
                        if (object instanceof C)
                            return object;
                        if (!$util.isObject(object))
                            throw TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData: object expected");
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        return new C();
                    };

                    /**
                     * Creates a plain object from a SideBySideSurveyCardImpressionEventData message. Also converts values to other types if specified.
                     * @function toObject
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData
                     * @static
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData} message SideBySideSurveyCardImpressionEventData
                     * @param {$protobuf.IConversionOptions} [options] Conversion options
                     * @returns {Object.<string,*>} Plain object
                     */
                    SideBySideSurveyCardImpressionEventData.toObject = function toObject() {
                        return {};
                    };

                    /**
                     * Converts this SideBySideSurveyCardImpressionEventData to JSON.
                     * @function toJSON
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData
                     * @instance
                     * @returns {Object.<string,*>} JSON object
                     */
                    SideBySideSurveyCardImpressionEventData.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    /**
                     * Gets the default type url for SideBySideSurveyCardImpressionEventData
                     * @function getTypeUrl
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData
                     * @static
                     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                     * @returns {string} The default type url
                     */
                    SideBySideSurveyCardImpressionEventData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData";
                    };

                    return SideBySideSurveyCardImpressionEventData;
                })();

                SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData = (function() {

                    /**
                     * Properties of a SideBySideSurveyResponseEventData.
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                     * @interface ISideBySideSurveyResponseEventData
                     * @property {string|null} [responseDwellTimeMsString] SideBySideSurveyResponseEventData responseDwellTimeMsString
                     * @property {string|null} [selectedResponseId] SideBySideSurveyResponseEventData selectedResponseId
                     */

                    /**
                     * Constructs a new SideBySideSurveyResponseEventData.
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData
                     * @classdesc Represents a SideBySideSurveyResponseEventData.
                     * @implements ISideBySideSurveyResponseEventData
                     * @constructor
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyResponseEventData=} [properties] Properties to set
                     */
                    function SideBySideSurveyResponseEventData(properties) {
                        if (properties)
                            for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    /**
                     * SideBySideSurveyResponseEventData responseDwellTimeMsString.
                     * @member {string|null|undefined} responseDwellTimeMsString
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData
                     * @instance
                     */
                    SideBySideSurveyResponseEventData.prototype.responseDwellTimeMsString = null;

                    /**
                     * SideBySideSurveyResponseEventData selectedResponseId.
                     * @member {string|null|undefined} selectedResponseId
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData
                     * @instance
                     */
                    SideBySideSurveyResponseEventData.prototype.selectedResponseId = null;

                    // OneOf field names bound to virtual getters and setters
                    var $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(SideBySideSurveyResponseEventData.prototype, "_responseDwellTimeMsString", {
                        get: $util.oneOfGetter($oneOfFields = ["responseDwellTimeMsString"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(SideBySideSurveyResponseEventData.prototype, "_selectedResponseId", {
                        get: $util.oneOfGetter($oneOfFields = ["selectedResponseId"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    /**
                     * Creates a new SideBySideSurveyResponseEventData instance using the specified properties.
                     * @function create
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData
                     * @static
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyResponseEventData=} [properties] Properties to set
                     * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData} SideBySideSurveyResponseEventData instance
                     */
                    SideBySideSurveyResponseEventData.create = function create(properties) {
                        return new SideBySideSurveyResponseEventData(properties);
                    };

                    /**
                     * Encodes the specified SideBySideSurveyResponseEventData message. Does not implicitly {@link proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData.verify|verify} messages.
                     * @function encode
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData
                     * @static
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyResponseEventData} message SideBySideSurveyResponseEventData message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    SideBySideSurveyResponseEventData.encode = function encode(message, writer, q) {
                        if (!writer)
                            writer = $Writer.create();
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        if (message.responseDwellTimeMsString != null && Object.hasOwnProperty.call(message, "responseDwellTimeMsString"))
                            writer.uint32(/* id 1, wireType 2 =*/10).string(message.responseDwellTimeMsString);
                        if (message.selectedResponseId != null && Object.hasOwnProperty.call(message, "selectedResponseId"))
                            writer.uint32(/* id 2, wireType 2 =*/18).string(message.selectedResponseId);
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (var i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    /**
                     * Encodes the specified SideBySideSurveyResponseEventData message, length delimited. Does not implicitly {@link proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData.verify|verify} messages.
                     * @function encodeDelimited
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData
                     * @static
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ISideBySideSurveyResponseEventData} message SideBySideSurveyResponseEventData message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    SideBySideSurveyResponseEventData.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                    };

                    /**
                     * Decodes a SideBySideSurveyResponseEventData message from the specified reader or buffer.
                     * @function decode
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @param {number} [length] Message length if known beforehand
                     * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData} SideBySideSurveyResponseEventData
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    SideBySideSurveyResponseEventData.decode = function decode(reader, length, z, q, g) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (q === undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                        while (reader.pos < end) {
                            var s = reader.pos;
                            var tag = reader.tag();
                            if (tag === z) {
                                z = undefined;
                                break;
                            }
                            var u = tag & 7;
                            switch (tag >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    message.responseDwellTimeMsString = reader.stringVerify();
                                    message._responseDwellTimeMsString = "responseDwellTimeMsString";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 2)
                                        break;
                                    message.selectedResponseId = reader.stringVerify();
                                    message._selectedResponseId = "selectedResponseId";
                                    continue;
                                }
                            }
                            reader.skipType(u, q, tag);
                            if (!reader.discardUnknown) {
                                $util.makeProp(message, "$unknowns", false);
                                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                            }
                        }
                        if (z !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    /**
                     * Decodes a SideBySideSurveyResponseEventData message from the specified reader or buffer, length delimited.
                     * @function decodeDelimited
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData} SideBySideSurveyResponseEventData
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    SideBySideSurveyResponseEventData.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    /**
                     * Verifies a SideBySideSurveyResponseEventData message.
                     * @function verify
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData
                     * @static
                     * @param {Object.<string,*>} message Plain object to verify
                     * @returns {string|null} `null` if valid, otherwise the reason why it is not
                     */
                    SideBySideSurveyResponseEventData.verify = function verify(message, q) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            return "max depth exceeded";
                        var properties = {};
                        if (message.responseDwellTimeMsString != null && Object.hasOwnProperty.call(message, "responseDwellTimeMsString")) {
                            properties._responseDwellTimeMsString = 1;
                            if (!$util.isString(message.responseDwellTimeMsString))
                                return "responseDwellTimeMsString: string expected";
                        }
                        if (message.selectedResponseId != null && Object.hasOwnProperty.call(message, "selectedResponseId")) {
                            properties._selectedResponseId = 1;
                            if (!$util.isString(message.selectedResponseId))
                                return "selectedResponseId: string expected";
                        }
                        return null;
                    };

                    /**
                     * Creates a SideBySideSurveyResponseEventData message from a plain object. Also converts values to their respective internal types.
                     * @function fromObject
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData
                     * @static
                     * @param {Object.<string,*>} object Plain object
                     * @returns {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData} SideBySideSurveyResponseEventData
                     */
                    SideBySideSurveyResponseEventData.fromObject = function fromObject(object, q) {
                        if (object instanceof C)
                            return object;
                        if (!$util.isObject(object))
                            throw TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData: object expected");
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var message = new C();
                        if (object.responseDwellTimeMsString != null)
                            message.responseDwellTimeMsString = String(object.responseDwellTimeMsString);
                        if (object.selectedResponseId != null)
                            message.selectedResponseId = String(object.selectedResponseId);
                        return message;
                    };

                    /**
                     * Creates a plain object from a SideBySideSurveyResponseEventData message. Also converts values to other types if specified.
                     * @function toObject
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData
                     * @static
                     * @param {proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData} message SideBySideSurveyResponseEventData
                     * @param {$protobuf.IConversionOptions} [options] Conversion options
                     * @returns {Object.<string,*>} Plain object
                     */
                    SideBySideSurveyResponseEventData.toObject = function toObject(message, options, q) {
                        if (!options)
                            options = {};
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var object = {};
                        if (message.responseDwellTimeMsString != null && Object.hasOwnProperty.call(message, "responseDwellTimeMsString"))
                            object.responseDwellTimeMsString = message.responseDwellTimeMsString;
                        if (message.selectedResponseId != null && Object.hasOwnProperty.call(message, "selectedResponseId"))
                            object.selectedResponseId = message.selectedResponseId;
                        return object;
                    };

                    /**
                     * Converts this SideBySideSurveyResponseEventData to JSON.
                     * @function toJSON
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData
                     * @instance
                     * @returns {Object.<string,*>} JSON object
                     */
                    SideBySideSurveyResponseEventData.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    /**
                     * Gets the default type url for SideBySideSurveyResponseEventData
                     * @function getTypeUrl
                     * @memberof proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData
                     * @static
                     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                     * @returns {string} The default type url
                     */
                    SideBySideSurveyResponseEventData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData";
                    };

                    return SideBySideSurveyResponseEventData;
                })();

                return SidebySideSurveyMetaAiAnalyticsData;
            })();

            return SideBySideSurveyMetadata;
        })();

        return BotFeedbackMessage;
    })();

    proto.BotGroupMetadata = (function() {

        /**
         * Properties of a BotGroupMetadata.
         * @memberof proto
         * @interface IBotGroupMetadata
         * @property {Array.<proto.IBotGroupParticipantMetadata>|null} [participantsMetadata] BotGroupMetadata participantsMetadata
         */

        /**
         * Constructs a new BotGroupMetadata.
         * @memberof proto
         * @classdesc Represents a BotGroupMetadata.
         * @implements IBotGroupMetadata
         * @constructor
         * @param {proto.IBotGroupMetadata=} [properties] Properties to set
         */
        function BotGroupMetadata(properties) {
            this.participantsMetadata = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotGroupMetadata participantsMetadata.
         * @member {Array.<proto.IBotGroupParticipantMetadata>} participantsMetadata
         * @memberof proto.BotGroupMetadata
         * @instance
         */
        BotGroupMetadata.prototype.participantsMetadata = $util.emptyArray;

        /**
         * Creates a new BotGroupMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotGroupMetadata
         * @static
         * @param {proto.IBotGroupMetadata=} [properties] Properties to set
         * @returns {proto.BotGroupMetadata} BotGroupMetadata instance
         */
        BotGroupMetadata.create = function create(properties) {
            return new BotGroupMetadata(properties);
        };

        /**
         * Encodes the specified BotGroupMetadata message. Does not implicitly {@link proto.BotGroupMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotGroupMetadata
         * @static
         * @param {proto.IBotGroupMetadata} message BotGroupMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotGroupMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.participantsMetadata != null && message.participantsMetadata.length)
                for (var i = 0; i < message.participantsMetadata.length; ++i)
                    $root.proto.BotGroupParticipantMetadata.encode(message.participantsMetadata[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotGroupMetadata message, length delimited. Does not implicitly {@link proto.BotGroupMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotGroupMetadata
         * @static
         * @param {proto.IBotGroupMetadata} message BotGroupMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotGroupMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotGroupMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotGroupMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotGroupMetadata} BotGroupMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotGroupMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(message.participantsMetadata && message.participantsMetadata.length))
                            message.participantsMetadata = [];
                        message.participantsMetadata.push($root.proto.BotGroupParticipantMetadata.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotGroupMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotGroupMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotGroupMetadata} BotGroupMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotGroupMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotGroupMetadata message.
         * @function verify
         * @memberof proto.BotGroupMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotGroupMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            if (message.participantsMetadata != null && Object.hasOwnProperty.call(message, "participantsMetadata")) {
                if (!Array.isArray(message.participantsMetadata))
                    return "participantsMetadata: array expected";
                for (var i = 0; i < message.participantsMetadata.length; ++i) {
                    var error = $root.proto.BotGroupParticipantMetadata.verify(message.participantsMetadata[i], q + 1);
                    if (error)
                        return "participantsMetadata." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BotGroupMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotGroupMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotGroupMetadata} BotGroupMetadata
         */
        BotGroupMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotGroupMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.participantsMetadata) {
                if (!Array.isArray(object.participantsMetadata))
                    throw TypeError(".proto.BotGroupMetadata.participantsMetadata: array expected");
                message.participantsMetadata = Array(object.participantsMetadata.length);
                for (var i = 0; i < object.participantsMetadata.length; ++i) {
                    if (!$util.isObject(object.participantsMetadata[i]))
                        throw TypeError(".proto.BotGroupMetadata.participantsMetadata: object expected");
                    message.participantsMetadata[i] = $root.proto.BotGroupParticipantMetadata.fromObject(object.participantsMetadata[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a BotGroupMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotGroupMetadata
         * @static
         * @param {proto.BotGroupMetadata} message BotGroupMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotGroupMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.participantsMetadata = [];
            if (message.participantsMetadata && message.participantsMetadata.length) {
                object.participantsMetadata = Array(message.participantsMetadata.length);
                for (var j = 0; j < message.participantsMetadata.length; ++j)
                    object.participantsMetadata[j] = $root.proto.BotGroupParticipantMetadata.toObject(message.participantsMetadata[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this BotGroupMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotGroupMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotGroupMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotGroupMetadata
         * @function getTypeUrl
         * @memberof proto.BotGroupMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotGroupMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotGroupMetadata";
        };

        return BotGroupMetadata;
    })();

    proto.BotGroupParticipantMetadata = (function() {

        /**
         * Properties of a BotGroupParticipantMetadata.
         * @memberof proto
         * @interface IBotGroupParticipantMetadata
         * @property {string|null} [botFbid] BotGroupParticipantMetadata botFbid
         */

        /**
         * Constructs a new BotGroupParticipantMetadata.
         * @memberof proto
         * @classdesc Represents a BotGroupParticipantMetadata.
         * @implements IBotGroupParticipantMetadata
         * @constructor
         * @param {proto.IBotGroupParticipantMetadata=} [properties] Properties to set
         */
        function BotGroupParticipantMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotGroupParticipantMetadata botFbid.
         * @member {string|null|undefined} botFbid
         * @memberof proto.BotGroupParticipantMetadata
         * @instance
         */
        BotGroupParticipantMetadata.prototype.botFbid = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotGroupParticipantMetadata.prototype, "_botFbid", {
            get: $util.oneOfGetter($oneOfFields = ["botFbid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotGroupParticipantMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotGroupParticipantMetadata
         * @static
         * @param {proto.IBotGroupParticipantMetadata=} [properties] Properties to set
         * @returns {proto.BotGroupParticipantMetadata} BotGroupParticipantMetadata instance
         */
        BotGroupParticipantMetadata.create = function create(properties) {
            return new BotGroupParticipantMetadata(properties);
        };

        /**
         * Encodes the specified BotGroupParticipantMetadata message. Does not implicitly {@link proto.BotGroupParticipantMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotGroupParticipantMetadata
         * @static
         * @param {proto.IBotGroupParticipantMetadata} message BotGroupParticipantMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotGroupParticipantMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.botFbid != null && Object.hasOwnProperty.call(message, "botFbid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.botFbid);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotGroupParticipantMetadata message, length delimited. Does not implicitly {@link proto.BotGroupParticipantMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotGroupParticipantMetadata
         * @static
         * @param {proto.IBotGroupParticipantMetadata} message BotGroupParticipantMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotGroupParticipantMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotGroupParticipantMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotGroupParticipantMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotGroupParticipantMetadata} BotGroupParticipantMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotGroupParticipantMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.botFbid = reader.stringVerify();
                        message._botFbid = "botFbid";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotGroupParticipantMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotGroupParticipantMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotGroupParticipantMetadata} BotGroupParticipantMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotGroupParticipantMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotGroupParticipantMetadata message.
         * @function verify
         * @memberof proto.BotGroupParticipantMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotGroupParticipantMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.botFbid != null && Object.hasOwnProperty.call(message, "botFbid")) {
                properties._botFbid = 1;
                if (!$util.isString(message.botFbid))
                    return "botFbid: string expected";
            }
            return null;
        };

        /**
         * Creates a BotGroupParticipantMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotGroupParticipantMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotGroupParticipantMetadata} BotGroupParticipantMetadata
         */
        BotGroupParticipantMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotGroupParticipantMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.botFbid != null)
                message.botFbid = String(object.botFbid);
            return message;
        };

        /**
         * Creates a plain object from a BotGroupParticipantMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotGroupParticipantMetadata
         * @static
         * @param {proto.BotGroupParticipantMetadata} message BotGroupParticipantMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotGroupParticipantMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.botFbid != null && Object.hasOwnProperty.call(message, "botFbid"))
                object.botFbid = message.botFbid;
            return object;
        };

        /**
         * Converts this BotGroupParticipantMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotGroupParticipantMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotGroupParticipantMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotGroupParticipantMetadata
         * @function getTypeUrl
         * @memberof proto.BotGroupParticipantMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotGroupParticipantMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotGroupParticipantMetadata";
        };

        return BotGroupParticipantMetadata;
    })();

    proto.BotImagineMetadata = (function() {

        /**
         * Properties of a BotImagineMetadata.
         * @memberof proto
         * @interface IBotImagineMetadata
         * @property {proto.BotImagineMetadata.ImagineType|null} [imagineType] BotImagineMetadata imagineType
         * @property {string|null} [shortPrompt] BotImagineMetadata shortPrompt
         */

        /**
         * Constructs a new BotImagineMetadata.
         * @memberof proto
         * @classdesc Represents a BotImagineMetadata.
         * @implements IBotImagineMetadata
         * @constructor
         * @param {proto.IBotImagineMetadata=} [properties] Properties to set
         */
        function BotImagineMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotImagineMetadata imagineType.
         * @member {proto.BotImagineMetadata.ImagineType|null|undefined} imagineType
         * @memberof proto.BotImagineMetadata
         * @instance
         */
        BotImagineMetadata.prototype.imagineType = null;

        /**
         * BotImagineMetadata shortPrompt.
         * @member {string|null|undefined} shortPrompt
         * @memberof proto.BotImagineMetadata
         * @instance
         */
        BotImagineMetadata.prototype.shortPrompt = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotImagineMetadata.prototype, "_imagineType", {
            get: $util.oneOfGetter($oneOfFields = ["imagineType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotImagineMetadata.prototype, "_shortPrompt", {
            get: $util.oneOfGetter($oneOfFields = ["shortPrompt"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotImagineMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotImagineMetadata
         * @static
         * @param {proto.IBotImagineMetadata=} [properties] Properties to set
         * @returns {proto.BotImagineMetadata} BotImagineMetadata instance
         */
        BotImagineMetadata.create = function create(properties) {
            return new BotImagineMetadata(properties);
        };

        /**
         * Encodes the specified BotImagineMetadata message. Does not implicitly {@link proto.BotImagineMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotImagineMetadata
         * @static
         * @param {proto.IBotImagineMetadata} message BotImagineMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotImagineMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.imagineType != null && Object.hasOwnProperty.call(message, "imagineType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.imagineType);
            if (message.shortPrompt != null && Object.hasOwnProperty.call(message, "shortPrompt"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.shortPrompt);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotImagineMetadata message, length delimited. Does not implicitly {@link proto.BotImagineMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotImagineMetadata
         * @static
         * @param {proto.IBotImagineMetadata} message BotImagineMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotImagineMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotImagineMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotImagineMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotImagineMetadata} BotImagineMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotImagineMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.imagineType = reader.int32();
                        message._imagineType = "imagineType";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.shortPrompt = reader.stringVerify();
                        message._shortPrompt = "shortPrompt";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotImagineMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotImagineMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotImagineMetadata} BotImagineMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotImagineMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotImagineMetadata message.
         * @function verify
         * @memberof proto.BotImagineMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotImagineMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.imagineType != null && Object.hasOwnProperty.call(message, "imagineType")) {
                properties._imagineType = 1;
                if (typeof message.imagineType !== "number" || (message.imagineType | 0) !== message.imagineType)
                    return "imagineType: enum value expected";
            }
            if (message.shortPrompt != null && Object.hasOwnProperty.call(message, "shortPrompt")) {
                properties._shortPrompt = 1;
                if (!$util.isString(message.shortPrompt))
                    return "shortPrompt: string expected";
            }
            return null;
        };

        /**
         * Creates a BotImagineMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotImagineMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotImagineMetadata} BotImagineMetadata
         */
        BotImagineMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotImagineMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.imagineType) {
            case "UNKNOWN":
            case 0:
                message.imagineType = 0;
                break;
            case "IMAGINE":
            case 1:
                message.imagineType = 1;
                break;
            case "MEMU":
            case 2:
                message.imagineType = 2;
                break;
            case "FLASH":
            case 3:
                message.imagineType = 3;
                break;
            case "EDIT":
            case 4:
                message.imagineType = 4;
                break;
            default:
                if (typeof object.imagineType === "number" && (object.imagineType | 0) === object.imagineType)
                    message.imagineType = object.imagineType;
            }
            if (object.shortPrompt != null)
                message.shortPrompt = String(object.shortPrompt);
            return message;
        };

        /**
         * Creates a plain object from a BotImagineMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotImagineMetadata
         * @static
         * @param {proto.BotImagineMetadata} message BotImagineMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotImagineMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.imagineType != null && Object.hasOwnProperty.call(message, "imagineType"))
                object.imagineType = options.enums === String ? $root.proto.BotImagineMetadata.ImagineType[message.imagineType] === undefined ? message.imagineType : $root.proto.BotImagineMetadata.ImagineType[message.imagineType] : message.imagineType;
            if (message.shortPrompt != null && Object.hasOwnProperty.call(message, "shortPrompt"))
                object.shortPrompt = message.shortPrompt;
            return object;
        };

        /**
         * Converts this BotImagineMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotImagineMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotImagineMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotImagineMetadata
         * @function getTypeUrl
         * @memberof proto.BotImagineMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotImagineMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotImagineMetadata";
        };

        /**
         * ImagineType enum.
         * @name proto.BotImagineMetadata.ImagineType
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} IMAGINE=1 IMAGINE value
         * @property {number} MEMU=2 MEMU value
         * @property {number} FLASH=3 FLASH value
         * @property {number} EDIT=4 EDIT value
         */
        BotImagineMetadata.ImagineType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "IMAGINE"] = 1;
            values[valuesById[2] = "MEMU"] = 2;
            values[valuesById[3] = "FLASH"] = 3;
            values[valuesById[4] = "EDIT"] = 4;
            return values;
        })();

        return BotImagineMetadata;
    })();

    proto.BotInfrastructureDiagnostics = (function() {

        /**
         * Properties of a BotInfrastructureDiagnostics.
         * @memberof proto
         * @interface IBotInfrastructureDiagnostics
         * @property {proto.BotInfrastructureDiagnostics.BotBackend|null} [botBackend] BotInfrastructureDiagnostics botBackend
         * @property {Array.<string>|null} [toolsUsed] BotInfrastructureDiagnostics toolsUsed
         * @property {boolean|null} [isThinking] BotInfrastructureDiagnostics isThinking
         */

        /**
         * Constructs a new BotInfrastructureDiagnostics.
         * @memberof proto
         * @classdesc Represents a BotInfrastructureDiagnostics.
         * @implements IBotInfrastructureDiagnostics
         * @constructor
         * @param {proto.IBotInfrastructureDiagnostics=} [properties] Properties to set
         */
        function BotInfrastructureDiagnostics(properties) {
            this.toolsUsed = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotInfrastructureDiagnostics botBackend.
         * @member {proto.BotInfrastructureDiagnostics.BotBackend|null|undefined} botBackend
         * @memberof proto.BotInfrastructureDiagnostics
         * @instance
         */
        BotInfrastructureDiagnostics.prototype.botBackend = null;

        /**
         * BotInfrastructureDiagnostics toolsUsed.
         * @member {Array.<string>} toolsUsed
         * @memberof proto.BotInfrastructureDiagnostics
         * @instance
         */
        BotInfrastructureDiagnostics.prototype.toolsUsed = $util.emptyArray;

        /**
         * BotInfrastructureDiagnostics isThinking.
         * @member {boolean|null|undefined} isThinking
         * @memberof proto.BotInfrastructureDiagnostics
         * @instance
         */
        BotInfrastructureDiagnostics.prototype.isThinking = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotInfrastructureDiagnostics.prototype, "_botBackend", {
            get: $util.oneOfGetter($oneOfFields = ["botBackend"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotInfrastructureDiagnostics.prototype, "_isThinking", {
            get: $util.oneOfGetter($oneOfFields = ["isThinking"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotInfrastructureDiagnostics instance using the specified properties.
         * @function create
         * @memberof proto.BotInfrastructureDiagnostics
         * @static
         * @param {proto.IBotInfrastructureDiagnostics=} [properties] Properties to set
         * @returns {proto.BotInfrastructureDiagnostics} BotInfrastructureDiagnostics instance
         */
        BotInfrastructureDiagnostics.create = function create(properties) {
            return new BotInfrastructureDiagnostics(properties);
        };

        /**
         * Encodes the specified BotInfrastructureDiagnostics message. Does not implicitly {@link proto.BotInfrastructureDiagnostics.verify|verify} messages.
         * @function encode
         * @memberof proto.BotInfrastructureDiagnostics
         * @static
         * @param {proto.IBotInfrastructureDiagnostics} message BotInfrastructureDiagnostics message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotInfrastructureDiagnostics.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.botBackend != null && Object.hasOwnProperty.call(message, "botBackend"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.botBackend);
            if (message.toolsUsed != null && message.toolsUsed.length)
                for (var i = 0; i < message.toolsUsed.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.toolsUsed[i]);
            if (message.isThinking != null && Object.hasOwnProperty.call(message, "isThinking"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.isThinking);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotInfrastructureDiagnostics message, length delimited. Does not implicitly {@link proto.BotInfrastructureDiagnostics.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotInfrastructureDiagnostics
         * @static
         * @param {proto.IBotInfrastructureDiagnostics} message BotInfrastructureDiagnostics message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotInfrastructureDiagnostics.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotInfrastructureDiagnostics message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotInfrastructureDiagnostics
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotInfrastructureDiagnostics} BotInfrastructureDiagnostics
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotInfrastructureDiagnostics.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.botBackend = reader.int32();
                        message._botBackend = "botBackend";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(message.toolsUsed && message.toolsUsed.length))
                            message.toolsUsed = [];
                        message.toolsUsed.push(reader.stringVerify());
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.isThinking = reader.bool();
                        message._isThinking = "isThinking";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotInfrastructureDiagnostics message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotInfrastructureDiagnostics
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotInfrastructureDiagnostics} BotInfrastructureDiagnostics
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotInfrastructureDiagnostics.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotInfrastructureDiagnostics message.
         * @function verify
         * @memberof proto.BotInfrastructureDiagnostics
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotInfrastructureDiagnostics.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.botBackend != null && Object.hasOwnProperty.call(message, "botBackend")) {
                properties._botBackend = 1;
                if (typeof message.botBackend !== "number" || (message.botBackend | 0) !== message.botBackend)
                    return "botBackend: enum value expected";
            }
            if (message.toolsUsed != null && Object.hasOwnProperty.call(message, "toolsUsed")) {
                if (!Array.isArray(message.toolsUsed))
                    return "toolsUsed: array expected";
                for (var i = 0; i < message.toolsUsed.length; ++i)
                    if (!$util.isString(message.toolsUsed[i]))
                        return "toolsUsed: string[] expected";
            }
            if (message.isThinking != null && Object.hasOwnProperty.call(message, "isThinking")) {
                properties._isThinking = 1;
                if (typeof message.isThinking !== "boolean")
                    return "isThinking: boolean expected";
            }
            return null;
        };

        /**
         * Creates a BotInfrastructureDiagnostics message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotInfrastructureDiagnostics
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotInfrastructureDiagnostics} BotInfrastructureDiagnostics
         */
        BotInfrastructureDiagnostics.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotInfrastructureDiagnostics: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.botBackend) {
            case "AAPI":
            case 0:
                message.botBackend = 0;
                break;
            case "CLIPPY":
            case 1:
                message.botBackend = 1;
                break;
            default:
                if (typeof object.botBackend === "number" && (object.botBackend | 0) === object.botBackend)
                    message.botBackend = object.botBackend;
            }
            if (object.toolsUsed) {
                if (!Array.isArray(object.toolsUsed))
                    throw TypeError(".proto.BotInfrastructureDiagnostics.toolsUsed: array expected");
                message.toolsUsed = Array(object.toolsUsed.length);
                for (var i = 0; i < object.toolsUsed.length; ++i)
                    message.toolsUsed[i] = String(object.toolsUsed[i]);
            }
            if (object.isThinking != null)
                message.isThinking = Boolean(object.isThinking);
            return message;
        };

        /**
         * Creates a plain object from a BotInfrastructureDiagnostics message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotInfrastructureDiagnostics
         * @static
         * @param {proto.BotInfrastructureDiagnostics} message BotInfrastructureDiagnostics
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotInfrastructureDiagnostics.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.toolsUsed = [];
            if (message.botBackend != null && Object.hasOwnProperty.call(message, "botBackend"))
                object.botBackend = options.enums === String ? $root.proto.BotInfrastructureDiagnostics.BotBackend[message.botBackend] === undefined ? message.botBackend : $root.proto.BotInfrastructureDiagnostics.BotBackend[message.botBackend] : message.botBackend;
            if (message.toolsUsed && message.toolsUsed.length) {
                object.toolsUsed = Array(message.toolsUsed.length);
                for (var j = 0; j < message.toolsUsed.length; ++j)
                    object.toolsUsed[j] = message.toolsUsed[j];
            }
            if (message.isThinking != null && Object.hasOwnProperty.call(message, "isThinking"))
                object.isThinking = message.isThinking;
            return object;
        };

        /**
         * Converts this BotInfrastructureDiagnostics to JSON.
         * @function toJSON
         * @memberof proto.BotInfrastructureDiagnostics
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotInfrastructureDiagnostics.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotInfrastructureDiagnostics
         * @function getTypeUrl
         * @memberof proto.BotInfrastructureDiagnostics
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotInfrastructureDiagnostics.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotInfrastructureDiagnostics";
        };

        /**
         * BotBackend enum.
         * @name proto.BotInfrastructureDiagnostics.BotBackend
         * @enum {number}
         * @property {number} AAPI=0 AAPI value
         * @property {number} CLIPPY=1 CLIPPY value
         */
        BotInfrastructureDiagnostics.BotBackend = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "AAPI"] = 0;
            values[valuesById[1] = "CLIPPY"] = 1;
            return values;
        })();

        return BotInfrastructureDiagnostics;
    })();

    proto.BotLinkedAccount = (function() {

        /**
         * Properties of a BotLinkedAccount.
         * @memberof proto
         * @interface IBotLinkedAccount
         * @property {proto.BotLinkedAccount.BotLinkedAccountType|null} [type] BotLinkedAccount type
         */

        /**
         * Constructs a new BotLinkedAccount.
         * @memberof proto
         * @classdesc Represents a BotLinkedAccount.
         * @implements IBotLinkedAccount
         * @constructor
         * @param {proto.IBotLinkedAccount=} [properties] Properties to set
         */
        function BotLinkedAccount(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotLinkedAccount type.
         * @member {proto.BotLinkedAccount.BotLinkedAccountType|null|undefined} type
         * @memberof proto.BotLinkedAccount
         * @instance
         */
        BotLinkedAccount.prototype.type = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotLinkedAccount.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotLinkedAccount instance using the specified properties.
         * @function create
         * @memberof proto.BotLinkedAccount
         * @static
         * @param {proto.IBotLinkedAccount=} [properties] Properties to set
         * @returns {proto.BotLinkedAccount} BotLinkedAccount instance
         */
        BotLinkedAccount.create = function create(properties) {
            return new BotLinkedAccount(properties);
        };

        /**
         * Encodes the specified BotLinkedAccount message. Does not implicitly {@link proto.BotLinkedAccount.verify|verify} messages.
         * @function encode
         * @memberof proto.BotLinkedAccount
         * @static
         * @param {proto.IBotLinkedAccount} message BotLinkedAccount message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotLinkedAccount.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotLinkedAccount message, length delimited. Does not implicitly {@link proto.BotLinkedAccount.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotLinkedAccount
         * @static
         * @param {proto.IBotLinkedAccount} message BotLinkedAccount message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotLinkedAccount.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotLinkedAccount message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotLinkedAccount
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotLinkedAccount} BotLinkedAccount
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotLinkedAccount.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.type = reader.int32();
                        message._type = "type";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotLinkedAccount message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotLinkedAccount
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotLinkedAccount} BotLinkedAccount
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotLinkedAccount.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotLinkedAccount message.
         * @function verify
         * @memberof proto.BotLinkedAccount
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotLinkedAccount.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                properties._type = 1;
                if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                    return "type: enum value expected";
            }
            return null;
        };

        /**
         * Creates a BotLinkedAccount message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotLinkedAccount
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotLinkedAccount} BotLinkedAccount
         */
        BotLinkedAccount.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotLinkedAccount: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.type) {
            case "BOT_LINKED_ACCOUNT_TYPE_1P":
            case 0:
                message.type = 0;
                break;
            default:
                if (typeof object.type === "number" && (object.type | 0) === object.type)
                    message.type = object.type;
            }
            return message;
        };

        /**
         * Creates a plain object from a BotLinkedAccount message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotLinkedAccount
         * @static
         * @param {proto.BotLinkedAccount} message BotLinkedAccount
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotLinkedAccount.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                object.type = options.enums === String ? $root.proto.BotLinkedAccount.BotLinkedAccountType[message.type] === undefined ? message.type : $root.proto.BotLinkedAccount.BotLinkedAccountType[message.type] : message.type;
            return object;
        };

        /**
         * Converts this BotLinkedAccount to JSON.
         * @function toJSON
         * @memberof proto.BotLinkedAccount
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotLinkedAccount.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotLinkedAccount
         * @function getTypeUrl
         * @memberof proto.BotLinkedAccount
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotLinkedAccount.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotLinkedAccount";
        };

        /**
         * BotLinkedAccountType enum.
         * @name proto.BotLinkedAccount.BotLinkedAccountType
         * @enum {number}
         * @property {number} BOT_LINKED_ACCOUNT_TYPE_1P=0 BOT_LINKED_ACCOUNT_TYPE_1P value
         */
        BotLinkedAccount.BotLinkedAccountType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "BOT_LINKED_ACCOUNT_TYPE_1P"] = 0;
            return values;
        })();

        return BotLinkedAccount;
    })();

    proto.BotLinkedAccountsMetadata = (function() {

        /**
         * Properties of a BotLinkedAccountsMetadata.
         * @memberof proto
         * @interface IBotLinkedAccountsMetadata
         * @property {Array.<proto.IBotLinkedAccount>|null} [accounts] BotLinkedAccountsMetadata accounts
         * @property {Uint8Array|null} [acAuthTokens] BotLinkedAccountsMetadata acAuthTokens
         * @property {number|null} [acErrorCode] BotLinkedAccountsMetadata acErrorCode
         */

        /**
         * Constructs a new BotLinkedAccountsMetadata.
         * @memberof proto
         * @classdesc Represents a BotLinkedAccountsMetadata.
         * @implements IBotLinkedAccountsMetadata
         * @constructor
         * @param {proto.IBotLinkedAccountsMetadata=} [properties] Properties to set
         */
        function BotLinkedAccountsMetadata(properties) {
            this.accounts = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotLinkedAccountsMetadata accounts.
         * @member {Array.<proto.IBotLinkedAccount>} accounts
         * @memberof proto.BotLinkedAccountsMetadata
         * @instance
         */
        BotLinkedAccountsMetadata.prototype.accounts = $util.emptyArray;

        /**
         * BotLinkedAccountsMetadata acAuthTokens.
         * @member {Uint8Array|null|undefined} acAuthTokens
         * @memberof proto.BotLinkedAccountsMetadata
         * @instance
         */
        BotLinkedAccountsMetadata.prototype.acAuthTokens = null;

        /**
         * BotLinkedAccountsMetadata acErrorCode.
         * @member {number|null|undefined} acErrorCode
         * @memberof proto.BotLinkedAccountsMetadata
         * @instance
         */
        BotLinkedAccountsMetadata.prototype.acErrorCode = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotLinkedAccountsMetadata.prototype, "_acAuthTokens", {
            get: $util.oneOfGetter($oneOfFields = ["acAuthTokens"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotLinkedAccountsMetadata.prototype, "_acErrorCode", {
            get: $util.oneOfGetter($oneOfFields = ["acErrorCode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotLinkedAccountsMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotLinkedAccountsMetadata
         * @static
         * @param {proto.IBotLinkedAccountsMetadata=} [properties] Properties to set
         * @returns {proto.BotLinkedAccountsMetadata} BotLinkedAccountsMetadata instance
         */
        BotLinkedAccountsMetadata.create = function create(properties) {
            return new BotLinkedAccountsMetadata(properties);
        };

        /**
         * Encodes the specified BotLinkedAccountsMetadata message. Does not implicitly {@link proto.BotLinkedAccountsMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotLinkedAccountsMetadata
         * @static
         * @param {proto.IBotLinkedAccountsMetadata} message BotLinkedAccountsMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotLinkedAccountsMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.accounts != null && message.accounts.length)
                for (var i = 0; i < message.accounts.length; ++i)
                    $root.proto.BotLinkedAccount.encode(message.accounts[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.acAuthTokens != null && Object.hasOwnProperty.call(message, "acAuthTokens"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.acAuthTokens);
            if (message.acErrorCode != null && Object.hasOwnProperty.call(message, "acErrorCode"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.acErrorCode);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotLinkedAccountsMetadata message, length delimited. Does not implicitly {@link proto.BotLinkedAccountsMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotLinkedAccountsMetadata
         * @static
         * @param {proto.IBotLinkedAccountsMetadata} message BotLinkedAccountsMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotLinkedAccountsMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotLinkedAccountsMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotLinkedAccountsMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotLinkedAccountsMetadata} BotLinkedAccountsMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotLinkedAccountsMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(message.accounts && message.accounts.length))
                            message.accounts = [];
                        message.accounts.push($root.proto.BotLinkedAccount.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.acAuthTokens = reader.bytes();
                        message._acAuthTokens = "acAuthTokens";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.acErrorCode = reader.int32();
                        message._acErrorCode = "acErrorCode";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotLinkedAccountsMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotLinkedAccountsMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotLinkedAccountsMetadata} BotLinkedAccountsMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotLinkedAccountsMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotLinkedAccountsMetadata message.
         * @function verify
         * @memberof proto.BotLinkedAccountsMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotLinkedAccountsMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.accounts != null && Object.hasOwnProperty.call(message, "accounts")) {
                if (!Array.isArray(message.accounts))
                    return "accounts: array expected";
                for (var i = 0; i < message.accounts.length; ++i) {
                    var error = $root.proto.BotLinkedAccount.verify(message.accounts[i], q + 1);
                    if (error)
                        return "accounts." + error;
                }
            }
            if (message.acAuthTokens != null && Object.hasOwnProperty.call(message, "acAuthTokens")) {
                properties._acAuthTokens = 1;
                if (!(message.acAuthTokens && typeof message.acAuthTokens.length === "number" || $util.isString(message.acAuthTokens)))
                    return "acAuthTokens: buffer expected";
            }
            if (message.acErrorCode != null && Object.hasOwnProperty.call(message, "acErrorCode")) {
                properties._acErrorCode = 1;
                if (!$util.isInteger(message.acErrorCode))
                    return "acErrorCode: integer expected";
            }
            return null;
        };

        /**
         * Creates a BotLinkedAccountsMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotLinkedAccountsMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotLinkedAccountsMetadata} BotLinkedAccountsMetadata
         */
        BotLinkedAccountsMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotLinkedAccountsMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.accounts) {
                if (!Array.isArray(object.accounts))
                    throw TypeError(".proto.BotLinkedAccountsMetadata.accounts: array expected");
                message.accounts = Array(object.accounts.length);
                for (var i = 0; i < object.accounts.length; ++i) {
                    if (!$util.isObject(object.accounts[i]))
                        throw TypeError(".proto.BotLinkedAccountsMetadata.accounts: object expected");
                    message.accounts[i] = $root.proto.BotLinkedAccount.fromObject(object.accounts[i], q + 1);
                }
            }
            if (object.acAuthTokens != null)
                if (typeof object.acAuthTokens === "string")
                    $util.base64.decode(object.acAuthTokens, message.acAuthTokens = $util.newBuffer($util.base64.length(object.acAuthTokens)), 0);
                else if (object.acAuthTokens.length >= 0)
                    message.acAuthTokens = object.acAuthTokens;
            if (object.acErrorCode != null)
                message.acErrorCode = object.acErrorCode | 0;
            return message;
        };

        /**
         * Creates a plain object from a BotLinkedAccountsMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotLinkedAccountsMetadata
         * @static
         * @param {proto.BotLinkedAccountsMetadata} message BotLinkedAccountsMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotLinkedAccountsMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.accounts = [];
            if (message.accounts && message.accounts.length) {
                object.accounts = Array(message.accounts.length);
                for (var j = 0; j < message.accounts.length; ++j)
                    object.accounts[j] = $root.proto.BotLinkedAccount.toObject(message.accounts[j], options, q + 1);
            }
            if (message.acAuthTokens != null && Object.hasOwnProperty.call(message, "acAuthTokens"))
                object.acAuthTokens = options.bytes === String ? $util.base64.encode(message.acAuthTokens, 0, message.acAuthTokens.length) : options.bytes === Array ? Array.prototype.slice.call(message.acAuthTokens) : message.acAuthTokens;
            if (message.acErrorCode != null && Object.hasOwnProperty.call(message, "acErrorCode"))
                object.acErrorCode = message.acErrorCode;
            return object;
        };

        /**
         * Converts this BotLinkedAccountsMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotLinkedAccountsMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotLinkedAccountsMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotLinkedAccountsMetadata
         * @function getTypeUrl
         * @memberof proto.BotLinkedAccountsMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotLinkedAccountsMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotLinkedAccountsMetadata";
        };

        return BotLinkedAccountsMetadata;
    })();

    proto.BotMediaMetadata = (function() {

        /**
         * Properties of a BotMediaMetadata.
         * @memberof proto
         * @interface IBotMediaMetadata
         * @property {string|null} [fileSha256] BotMediaMetadata fileSha256
         * @property {string|null} [mediaKey] BotMediaMetadata mediaKey
         * @property {string|null} [fileEncSha256] BotMediaMetadata fileEncSha256
         * @property {string|null} [directPath] BotMediaMetadata directPath
         * @property {number|Long|null} [mediaKeyTimestamp] BotMediaMetadata mediaKeyTimestamp
         * @property {string|null} [mimetype] BotMediaMetadata mimetype
         * @property {proto.BotMediaMetadata.OrientationType|null} [orientationType] BotMediaMetadata orientationType
         */

        /**
         * Constructs a new BotMediaMetadata.
         * @memberof proto
         * @classdesc Represents a BotMediaMetadata.
         * @implements IBotMediaMetadata
         * @constructor
         * @param {proto.IBotMediaMetadata=} [properties] Properties to set
         */
        function BotMediaMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotMediaMetadata fileSha256.
         * @member {string|null|undefined} fileSha256
         * @memberof proto.BotMediaMetadata
         * @instance
         */
        BotMediaMetadata.prototype.fileSha256 = null;

        /**
         * BotMediaMetadata mediaKey.
         * @member {string|null|undefined} mediaKey
         * @memberof proto.BotMediaMetadata
         * @instance
         */
        BotMediaMetadata.prototype.mediaKey = null;

        /**
         * BotMediaMetadata fileEncSha256.
         * @member {string|null|undefined} fileEncSha256
         * @memberof proto.BotMediaMetadata
         * @instance
         */
        BotMediaMetadata.prototype.fileEncSha256 = null;

        /**
         * BotMediaMetadata directPath.
         * @member {string|null|undefined} directPath
         * @memberof proto.BotMediaMetadata
         * @instance
         */
        BotMediaMetadata.prototype.directPath = null;

        /**
         * BotMediaMetadata mediaKeyTimestamp.
         * @member {number|Long|null|undefined} mediaKeyTimestamp
         * @memberof proto.BotMediaMetadata
         * @instance
         */
        BotMediaMetadata.prototype.mediaKeyTimestamp = null;

        /**
         * BotMediaMetadata mimetype.
         * @member {string|null|undefined} mimetype
         * @memberof proto.BotMediaMetadata
         * @instance
         */
        BotMediaMetadata.prototype.mimetype = null;

        /**
         * BotMediaMetadata orientationType.
         * @member {proto.BotMediaMetadata.OrientationType|null|undefined} orientationType
         * @memberof proto.BotMediaMetadata
         * @instance
         */
        BotMediaMetadata.prototype.orientationType = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMediaMetadata.prototype, "_fileSha256", {
            get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMediaMetadata.prototype, "_mediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMediaMetadata.prototype, "_fileEncSha256", {
            get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMediaMetadata.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMediaMetadata.prototype, "_mediaKeyTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMediaMetadata.prototype, "_mimetype", {
            get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMediaMetadata.prototype, "_orientationType", {
            get: $util.oneOfGetter($oneOfFields = ["orientationType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotMediaMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotMediaMetadata
         * @static
         * @param {proto.IBotMediaMetadata=} [properties] Properties to set
         * @returns {proto.BotMediaMetadata} BotMediaMetadata instance
         */
        BotMediaMetadata.create = function create(properties) {
            return new BotMediaMetadata(properties);
        };

        /**
         * Encodes the specified BotMediaMetadata message. Does not implicitly {@link proto.BotMediaMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotMediaMetadata
         * @static
         * @param {proto.IBotMediaMetadata} message BotMediaMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotMediaMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.fileSha256);
            if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.mediaKey);
            if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.fileEncSha256);
            if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.directPath);
            if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.mediaKeyTimestamp);
            if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.mimetype);
            if (message.orientationType != null && Object.hasOwnProperty.call(message, "orientationType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.orientationType);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotMediaMetadata message, length delimited. Does not implicitly {@link proto.BotMediaMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotMediaMetadata
         * @static
         * @param {proto.IBotMediaMetadata} message BotMediaMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotMediaMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotMediaMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotMediaMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotMediaMetadata} BotMediaMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotMediaMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.fileSha256 = reader.stringVerify();
                        message._fileSha256 = "fileSha256";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.mediaKey = reader.stringVerify();
                        message._mediaKey = "mediaKey";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.fileEncSha256 = reader.stringVerify();
                        message._fileEncSha256 = "fileEncSha256";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.directPath = reader.stringVerify();
                        message._directPath = "directPath";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.mediaKeyTimestamp = reader.int64();
                        message._mediaKeyTimestamp = "mediaKeyTimestamp";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        message.mimetype = reader.stringVerify();
                        message._mimetype = "mimetype";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        message.orientationType = reader.int32();
                        message._orientationType = "orientationType";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotMediaMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotMediaMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotMediaMetadata} BotMediaMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotMediaMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotMediaMetadata message.
         * @function verify
         * @memberof proto.BotMediaMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotMediaMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256")) {
                properties._fileSha256 = 1;
                if (!$util.isString(message.fileSha256))
                    return "fileSha256: string expected";
            }
            if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey")) {
                properties._mediaKey = 1;
                if (!$util.isString(message.mediaKey))
                    return "mediaKey: string expected";
            }
            if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256")) {
                properties._fileEncSha256 = 1;
                if (!$util.isString(message.fileEncSha256))
                    return "fileEncSha256: string expected";
            }
            if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath")) {
                properties._directPath = 1;
                if (!$util.isString(message.directPath))
                    return "directPath: string expected";
            }
            if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp")) {
                properties._mediaKeyTimestamp = 1;
                if (!$util.isInteger(message.mediaKeyTimestamp) && !(message.mediaKeyTimestamp && $util.isInteger(message.mediaKeyTimestamp.low) && $util.isInteger(message.mediaKeyTimestamp.high)))
                    return "mediaKeyTimestamp: integer|Long expected";
            }
            if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype")) {
                properties._mimetype = 1;
                if (!$util.isString(message.mimetype))
                    return "mimetype: string expected";
            }
            if (message.orientationType != null && Object.hasOwnProperty.call(message, "orientationType")) {
                properties._orientationType = 1;
                if (typeof message.orientationType !== "number" || (message.orientationType | 0) !== message.orientationType)
                    return "orientationType: enum value expected";
            }
            return null;
        };

        /**
         * Creates a BotMediaMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotMediaMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotMediaMetadata} BotMediaMetadata
         */
        BotMediaMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotMediaMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.fileSha256 != null)
                message.fileSha256 = String(object.fileSha256);
            if (object.mediaKey != null)
                message.mediaKey = String(object.mediaKey);
            if (object.fileEncSha256 != null)
                message.fileEncSha256 = String(object.fileEncSha256);
            if (object.directPath != null)
                message.directPath = String(object.directPath);
            if (object.mediaKeyTimestamp != null)
                if ($util.Long)
                    message.mediaKeyTimestamp = $util.Long.fromValue(object.mediaKeyTimestamp, false);
                else if (typeof object.mediaKeyTimestamp === "string")
                    message.mediaKeyTimestamp = parseInt(object.mediaKeyTimestamp, 10);
                else if (typeof object.mediaKeyTimestamp === "number")
                    message.mediaKeyTimestamp = object.mediaKeyTimestamp;
                else if (typeof object.mediaKeyTimestamp === "object")
                    message.mediaKeyTimestamp = new $util.LongBits(object.mediaKeyTimestamp.low >>> 0, object.mediaKeyTimestamp.high >>> 0).toNumber();
            if (object.mimetype != null)
                message.mimetype = String(object.mimetype);
            switch (object.orientationType) {
            case "CENTER":
            case 1:
                message.orientationType = 1;
                break;
            case "LEFT":
            case 2:
                message.orientationType = 2;
                break;
            case "RIGHT":
            case 3:
                message.orientationType = 3;
                break;
            default:
                if (typeof object.orientationType === "number" && (object.orientationType | 0) === object.orientationType)
                    message.orientationType = object.orientationType;
            }
            return message;
        };

        /**
         * Creates a plain object from a BotMediaMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotMediaMetadata
         * @static
         * @param {proto.BotMediaMetadata} message BotMediaMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotMediaMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                object.fileSha256 = message.fileSha256;
            if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                object.mediaKey = message.mediaKey;
            if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                object.fileEncSha256 = message.fileEncSha256;
            if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                object.directPath = message.directPath;
            if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.mediaKeyTimestamp = typeof message.mediaKeyTimestamp === "number" ? BigInt(message.mediaKeyTimestamp) : $util.Long.fromBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                else if (typeof message.mediaKeyTimestamp === "number")
                    object.mediaKeyTimestamp = options.longs === String ? String(message.mediaKeyTimestamp) : message.mediaKeyTimestamp;
                else
                    object.mediaKeyTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.mediaKeyTimestamp) : options.longs === Number ? new $util.LongBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0).toNumber() : message.mediaKeyTimestamp;
            if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                object.mimetype = message.mimetype;
            if (message.orientationType != null && Object.hasOwnProperty.call(message, "orientationType"))
                object.orientationType = options.enums === String ? $root.proto.BotMediaMetadata.OrientationType[message.orientationType] === undefined ? message.orientationType : $root.proto.BotMediaMetadata.OrientationType[message.orientationType] : message.orientationType;
            return object;
        };

        /**
         * Converts this BotMediaMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotMediaMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotMediaMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotMediaMetadata
         * @function getTypeUrl
         * @memberof proto.BotMediaMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotMediaMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotMediaMetadata";
        };

        /**
         * OrientationType enum.
         * @name proto.BotMediaMetadata.OrientationType
         * @enum {number}
         * @property {number} CENTER=1 CENTER value
         * @property {number} LEFT=2 LEFT value
         * @property {number} RIGHT=3 RIGHT value
         */
        BotMediaMetadata.OrientationType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[1] = "CENTER"] = 1;
            values[valuesById[2] = "LEFT"] = 2;
            values[valuesById[3] = "RIGHT"] = 3;
            return values;
        })();

        return BotMediaMetadata;
    })();

    proto.BotMemoryFact = (function() {

        /**
         * Properties of a BotMemoryFact.
         * @memberof proto
         * @interface IBotMemoryFact
         * @property {string|null} [fact] BotMemoryFact fact
         * @property {string|null} [factId] BotMemoryFact factId
         */

        /**
         * Constructs a new BotMemoryFact.
         * @memberof proto
         * @classdesc Represents a BotMemoryFact.
         * @implements IBotMemoryFact
         * @constructor
         * @param {proto.IBotMemoryFact=} [properties] Properties to set
         */
        function BotMemoryFact(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotMemoryFact fact.
         * @member {string|null|undefined} fact
         * @memberof proto.BotMemoryFact
         * @instance
         */
        BotMemoryFact.prototype.fact = null;

        /**
         * BotMemoryFact factId.
         * @member {string|null|undefined} factId
         * @memberof proto.BotMemoryFact
         * @instance
         */
        BotMemoryFact.prototype.factId = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMemoryFact.prototype, "_fact", {
            get: $util.oneOfGetter($oneOfFields = ["fact"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMemoryFact.prototype, "_factId", {
            get: $util.oneOfGetter($oneOfFields = ["factId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotMemoryFact instance using the specified properties.
         * @function create
         * @memberof proto.BotMemoryFact
         * @static
         * @param {proto.IBotMemoryFact=} [properties] Properties to set
         * @returns {proto.BotMemoryFact} BotMemoryFact instance
         */
        BotMemoryFact.create = function create(properties) {
            return new BotMemoryFact(properties);
        };

        /**
         * Encodes the specified BotMemoryFact message. Does not implicitly {@link proto.BotMemoryFact.verify|verify} messages.
         * @function encode
         * @memberof proto.BotMemoryFact
         * @static
         * @param {proto.IBotMemoryFact} message BotMemoryFact message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotMemoryFact.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.fact != null && Object.hasOwnProperty.call(message, "fact"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.fact);
            if (message.factId != null && Object.hasOwnProperty.call(message, "factId"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.factId);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotMemoryFact message, length delimited. Does not implicitly {@link proto.BotMemoryFact.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotMemoryFact
         * @static
         * @param {proto.IBotMemoryFact} message BotMemoryFact message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotMemoryFact.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotMemoryFact message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotMemoryFact
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotMemoryFact} BotMemoryFact
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotMemoryFact.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.fact = reader.stringVerify();
                        message._fact = "fact";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.factId = reader.stringVerify();
                        message._factId = "factId";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotMemoryFact message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotMemoryFact
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotMemoryFact} BotMemoryFact
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotMemoryFact.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotMemoryFact message.
         * @function verify
         * @memberof proto.BotMemoryFact
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotMemoryFact.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.fact != null && Object.hasOwnProperty.call(message, "fact")) {
                properties._fact = 1;
                if (!$util.isString(message.fact))
                    return "fact: string expected";
            }
            if (message.factId != null && Object.hasOwnProperty.call(message, "factId")) {
                properties._factId = 1;
                if (!$util.isString(message.factId))
                    return "factId: string expected";
            }
            return null;
        };

        /**
         * Creates a BotMemoryFact message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotMemoryFact
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotMemoryFact} BotMemoryFact
         */
        BotMemoryFact.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotMemoryFact: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.fact != null)
                message.fact = String(object.fact);
            if (object.factId != null)
                message.factId = String(object.factId);
            return message;
        };

        /**
         * Creates a plain object from a BotMemoryFact message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotMemoryFact
         * @static
         * @param {proto.BotMemoryFact} message BotMemoryFact
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotMemoryFact.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.fact != null && Object.hasOwnProperty.call(message, "fact"))
                object.fact = message.fact;
            if (message.factId != null && Object.hasOwnProperty.call(message, "factId"))
                object.factId = message.factId;
            return object;
        };

        /**
         * Converts this BotMemoryFact to JSON.
         * @function toJSON
         * @memberof proto.BotMemoryFact
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotMemoryFact.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotMemoryFact
         * @function getTypeUrl
         * @memberof proto.BotMemoryFact
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotMemoryFact.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotMemoryFact";
        };

        return BotMemoryFact;
    })();

    proto.BotMemoryMetadata = (function() {

        /**
         * Properties of a BotMemoryMetadata.
         * @memberof proto
         * @interface IBotMemoryMetadata
         * @property {Array.<proto.IBotMemoryFact>|null} [addedFacts] BotMemoryMetadata addedFacts
         * @property {Array.<proto.IBotMemoryFact>|null} [removedFacts] BotMemoryMetadata removedFacts
         * @property {string|null} [disclaimer] BotMemoryMetadata disclaimer
         */

        /**
         * Constructs a new BotMemoryMetadata.
         * @memberof proto
         * @classdesc Represents a BotMemoryMetadata.
         * @implements IBotMemoryMetadata
         * @constructor
         * @param {proto.IBotMemoryMetadata=} [properties] Properties to set
         */
        function BotMemoryMetadata(properties) {
            this.addedFacts = [];
            this.removedFacts = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotMemoryMetadata addedFacts.
         * @member {Array.<proto.IBotMemoryFact>} addedFacts
         * @memberof proto.BotMemoryMetadata
         * @instance
         */
        BotMemoryMetadata.prototype.addedFacts = $util.emptyArray;

        /**
         * BotMemoryMetadata removedFacts.
         * @member {Array.<proto.IBotMemoryFact>} removedFacts
         * @memberof proto.BotMemoryMetadata
         * @instance
         */
        BotMemoryMetadata.prototype.removedFacts = $util.emptyArray;

        /**
         * BotMemoryMetadata disclaimer.
         * @member {string|null|undefined} disclaimer
         * @memberof proto.BotMemoryMetadata
         * @instance
         */
        BotMemoryMetadata.prototype.disclaimer = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMemoryMetadata.prototype, "_disclaimer", {
            get: $util.oneOfGetter($oneOfFields = ["disclaimer"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotMemoryMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotMemoryMetadata
         * @static
         * @param {proto.IBotMemoryMetadata=} [properties] Properties to set
         * @returns {proto.BotMemoryMetadata} BotMemoryMetadata instance
         */
        BotMemoryMetadata.create = function create(properties) {
            return new BotMemoryMetadata(properties);
        };

        /**
         * Encodes the specified BotMemoryMetadata message. Does not implicitly {@link proto.BotMemoryMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotMemoryMetadata
         * @static
         * @param {proto.IBotMemoryMetadata} message BotMemoryMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotMemoryMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.addedFacts != null && message.addedFacts.length)
                for (var i = 0; i < message.addedFacts.length; ++i)
                    $root.proto.BotMemoryFact.encode(message.addedFacts[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.removedFacts != null && message.removedFacts.length)
                for (var i = 0; i < message.removedFacts.length; ++i)
                    $root.proto.BotMemoryFact.encode(message.removedFacts[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.disclaimer != null && Object.hasOwnProperty.call(message, "disclaimer"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.disclaimer);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotMemoryMetadata message, length delimited. Does not implicitly {@link proto.BotMemoryMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotMemoryMetadata
         * @static
         * @param {proto.IBotMemoryMetadata} message BotMemoryMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotMemoryMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotMemoryMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotMemoryMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotMemoryMetadata} BotMemoryMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotMemoryMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(message.addedFacts && message.addedFacts.length))
                            message.addedFacts = [];
                        message.addedFacts.push($root.proto.BotMemoryFact.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(message.removedFacts && message.removedFacts.length))
                            message.removedFacts = [];
                        message.removedFacts.push($root.proto.BotMemoryFact.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.disclaimer = reader.stringVerify();
                        message._disclaimer = "disclaimer";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotMemoryMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotMemoryMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotMemoryMetadata} BotMemoryMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotMemoryMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotMemoryMetadata message.
         * @function verify
         * @memberof proto.BotMemoryMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotMemoryMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.addedFacts != null && Object.hasOwnProperty.call(message, "addedFacts")) {
                if (!Array.isArray(message.addedFacts))
                    return "addedFacts: array expected";
                for (var i = 0; i < message.addedFacts.length; ++i) {
                    var error = $root.proto.BotMemoryFact.verify(message.addedFacts[i], q + 1);
                    if (error)
                        return "addedFacts." + error;
                }
            }
            if (message.removedFacts != null && Object.hasOwnProperty.call(message, "removedFacts")) {
                if (!Array.isArray(message.removedFacts))
                    return "removedFacts: array expected";
                for (var i = 0; i < message.removedFacts.length; ++i) {
                    var error = $root.proto.BotMemoryFact.verify(message.removedFacts[i], q + 1);
                    if (error)
                        return "removedFacts." + error;
                }
            }
            if (message.disclaimer != null && Object.hasOwnProperty.call(message, "disclaimer")) {
                properties._disclaimer = 1;
                if (!$util.isString(message.disclaimer))
                    return "disclaimer: string expected";
            }
            return null;
        };

        /**
         * Creates a BotMemoryMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotMemoryMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotMemoryMetadata} BotMemoryMetadata
         */
        BotMemoryMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotMemoryMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.addedFacts) {
                if (!Array.isArray(object.addedFacts))
                    throw TypeError(".proto.BotMemoryMetadata.addedFacts: array expected");
                message.addedFacts = Array(object.addedFacts.length);
                for (var i = 0; i < object.addedFacts.length; ++i) {
                    if (!$util.isObject(object.addedFacts[i]))
                        throw TypeError(".proto.BotMemoryMetadata.addedFacts: object expected");
                    message.addedFacts[i] = $root.proto.BotMemoryFact.fromObject(object.addedFacts[i], q + 1);
                }
            }
            if (object.removedFacts) {
                if (!Array.isArray(object.removedFacts))
                    throw TypeError(".proto.BotMemoryMetadata.removedFacts: array expected");
                message.removedFacts = Array(object.removedFacts.length);
                for (var i = 0; i < object.removedFacts.length; ++i) {
                    if (!$util.isObject(object.removedFacts[i]))
                        throw TypeError(".proto.BotMemoryMetadata.removedFacts: object expected");
                    message.removedFacts[i] = $root.proto.BotMemoryFact.fromObject(object.removedFacts[i], q + 1);
                }
            }
            if (object.disclaimer != null)
                message.disclaimer = String(object.disclaimer);
            return message;
        };

        /**
         * Creates a plain object from a BotMemoryMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotMemoryMetadata
         * @static
         * @param {proto.BotMemoryMetadata} message BotMemoryMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotMemoryMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults) {
                object.addedFacts = [];
                object.removedFacts = [];
            }
            if (message.addedFacts && message.addedFacts.length) {
                object.addedFacts = Array(message.addedFacts.length);
                for (var j = 0; j < message.addedFacts.length; ++j)
                    object.addedFacts[j] = $root.proto.BotMemoryFact.toObject(message.addedFacts[j], options, q + 1);
            }
            if (message.removedFacts && message.removedFacts.length) {
                object.removedFacts = Array(message.removedFacts.length);
                for (var j = 0; j < message.removedFacts.length; ++j)
                    object.removedFacts[j] = $root.proto.BotMemoryFact.toObject(message.removedFacts[j], options, q + 1);
            }
            if (message.disclaimer != null && Object.hasOwnProperty.call(message, "disclaimer"))
                object.disclaimer = message.disclaimer;
            return object;
        };

        /**
         * Converts this BotMemoryMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotMemoryMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotMemoryMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotMemoryMetadata
         * @function getTypeUrl
         * @memberof proto.BotMemoryMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotMemoryMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotMemoryMetadata";
        };

        return BotMemoryMetadata;
    })();

    proto.BotMemuMetadata = (function() {

        /**
         * Properties of a BotMemuMetadata.
         * @memberof proto
         * @interface IBotMemuMetadata
         * @property {Array.<proto.IBotMediaMetadata>|null} [faceImages] BotMemuMetadata faceImages
         */

        /**
         * Constructs a new BotMemuMetadata.
         * @memberof proto
         * @classdesc Represents a BotMemuMetadata.
         * @implements IBotMemuMetadata
         * @constructor
         * @param {proto.IBotMemuMetadata=} [properties] Properties to set
         */
        function BotMemuMetadata(properties) {
            this.faceImages = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotMemuMetadata faceImages.
         * @member {Array.<proto.IBotMediaMetadata>} faceImages
         * @memberof proto.BotMemuMetadata
         * @instance
         */
        BotMemuMetadata.prototype.faceImages = $util.emptyArray;

        /**
         * Creates a new BotMemuMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotMemuMetadata
         * @static
         * @param {proto.IBotMemuMetadata=} [properties] Properties to set
         * @returns {proto.BotMemuMetadata} BotMemuMetadata instance
         */
        BotMemuMetadata.create = function create(properties) {
            return new BotMemuMetadata(properties);
        };

        /**
         * Encodes the specified BotMemuMetadata message. Does not implicitly {@link proto.BotMemuMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotMemuMetadata
         * @static
         * @param {proto.IBotMemuMetadata} message BotMemuMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotMemuMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.faceImages != null && message.faceImages.length)
                for (var i = 0; i < message.faceImages.length; ++i)
                    $root.proto.BotMediaMetadata.encode(message.faceImages[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotMemuMetadata message, length delimited. Does not implicitly {@link proto.BotMemuMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotMemuMetadata
         * @static
         * @param {proto.IBotMemuMetadata} message BotMemuMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotMemuMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotMemuMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotMemuMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotMemuMetadata} BotMemuMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotMemuMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(message.faceImages && message.faceImages.length))
                            message.faceImages = [];
                        message.faceImages.push($root.proto.BotMediaMetadata.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotMemuMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotMemuMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotMemuMetadata} BotMemuMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotMemuMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotMemuMetadata message.
         * @function verify
         * @memberof proto.BotMemuMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotMemuMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            if (message.faceImages != null && Object.hasOwnProperty.call(message, "faceImages")) {
                if (!Array.isArray(message.faceImages))
                    return "faceImages: array expected";
                for (var i = 0; i < message.faceImages.length; ++i) {
                    var error = $root.proto.BotMediaMetadata.verify(message.faceImages[i], q + 1);
                    if (error)
                        return "faceImages." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BotMemuMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotMemuMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotMemuMetadata} BotMemuMetadata
         */
        BotMemuMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotMemuMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.faceImages) {
                if (!Array.isArray(object.faceImages))
                    throw TypeError(".proto.BotMemuMetadata.faceImages: array expected");
                message.faceImages = Array(object.faceImages.length);
                for (var i = 0; i < object.faceImages.length; ++i) {
                    if (!$util.isObject(object.faceImages[i]))
                        throw TypeError(".proto.BotMemuMetadata.faceImages: object expected");
                    message.faceImages[i] = $root.proto.BotMediaMetadata.fromObject(object.faceImages[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a BotMemuMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotMemuMetadata
         * @static
         * @param {proto.BotMemuMetadata} message BotMemuMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotMemuMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.faceImages = [];
            if (message.faceImages && message.faceImages.length) {
                object.faceImages = Array(message.faceImages.length);
                for (var j = 0; j < message.faceImages.length; ++j)
                    object.faceImages[j] = $root.proto.BotMediaMetadata.toObject(message.faceImages[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this BotMemuMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotMemuMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotMemuMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotMemuMetadata
         * @function getTypeUrl
         * @memberof proto.BotMemuMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotMemuMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotMemuMetadata";
        };

        return BotMemuMetadata;
    })();

    proto.BotMessageOrigin = (function() {

        /**
         * Properties of a BotMessageOrigin.
         * @memberof proto
         * @interface IBotMessageOrigin
         * @property {proto.BotMessageOrigin.BotMessageOriginType|null} [type] BotMessageOrigin type
         */

        /**
         * Constructs a new BotMessageOrigin.
         * @memberof proto
         * @classdesc Represents a BotMessageOrigin.
         * @implements IBotMessageOrigin
         * @constructor
         * @param {proto.IBotMessageOrigin=} [properties] Properties to set
         */
        function BotMessageOrigin(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotMessageOrigin type.
         * @member {proto.BotMessageOrigin.BotMessageOriginType|null|undefined} type
         * @memberof proto.BotMessageOrigin
         * @instance
         */
        BotMessageOrigin.prototype.type = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMessageOrigin.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotMessageOrigin instance using the specified properties.
         * @function create
         * @memberof proto.BotMessageOrigin
         * @static
         * @param {proto.IBotMessageOrigin=} [properties] Properties to set
         * @returns {proto.BotMessageOrigin} BotMessageOrigin instance
         */
        BotMessageOrigin.create = function create(properties) {
            return new BotMessageOrigin(properties);
        };

        /**
         * Encodes the specified BotMessageOrigin message. Does not implicitly {@link proto.BotMessageOrigin.verify|verify} messages.
         * @function encode
         * @memberof proto.BotMessageOrigin
         * @static
         * @param {proto.IBotMessageOrigin} message BotMessageOrigin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotMessageOrigin.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotMessageOrigin message, length delimited. Does not implicitly {@link proto.BotMessageOrigin.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotMessageOrigin
         * @static
         * @param {proto.IBotMessageOrigin} message BotMessageOrigin message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotMessageOrigin.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotMessageOrigin message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotMessageOrigin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotMessageOrigin} BotMessageOrigin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotMessageOrigin.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.type = reader.int32();
                        message._type = "type";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotMessageOrigin message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotMessageOrigin
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotMessageOrigin} BotMessageOrigin
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotMessageOrigin.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotMessageOrigin message.
         * @function verify
         * @memberof proto.BotMessageOrigin
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotMessageOrigin.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                properties._type = 1;
                if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                    return "type: enum value expected";
            }
            return null;
        };

        /**
         * Creates a BotMessageOrigin message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotMessageOrigin
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotMessageOrigin} BotMessageOrigin
         */
        BotMessageOrigin.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotMessageOrigin: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.type) {
            case "BOT_MESSAGE_ORIGIN_TYPE_AI_INITIATED":
            case 0:
                message.type = 0;
                break;
            default:
                if (typeof object.type === "number" && (object.type | 0) === object.type)
                    message.type = object.type;
            }
            return message;
        };

        /**
         * Creates a plain object from a BotMessageOrigin message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotMessageOrigin
         * @static
         * @param {proto.BotMessageOrigin} message BotMessageOrigin
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotMessageOrigin.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                object.type = options.enums === String ? $root.proto.BotMessageOrigin.BotMessageOriginType[message.type] === undefined ? message.type : $root.proto.BotMessageOrigin.BotMessageOriginType[message.type] : message.type;
            return object;
        };

        /**
         * Converts this BotMessageOrigin to JSON.
         * @function toJSON
         * @memberof proto.BotMessageOrigin
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotMessageOrigin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotMessageOrigin
         * @function getTypeUrl
         * @memberof proto.BotMessageOrigin
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotMessageOrigin.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotMessageOrigin";
        };

        /**
         * BotMessageOriginType enum.
         * @name proto.BotMessageOrigin.BotMessageOriginType
         * @enum {number}
         * @property {number} BOT_MESSAGE_ORIGIN_TYPE_AI_INITIATED=0 BOT_MESSAGE_ORIGIN_TYPE_AI_INITIATED value
         */
        BotMessageOrigin.BotMessageOriginType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "BOT_MESSAGE_ORIGIN_TYPE_AI_INITIATED"] = 0;
            return values;
        })();

        return BotMessageOrigin;
    })();

    proto.BotMessageOriginMetadata = (function() {

        /**
         * Properties of a BotMessageOriginMetadata.
         * @memberof proto
         * @interface IBotMessageOriginMetadata
         * @property {Array.<proto.IBotMessageOrigin>|null} [origins] BotMessageOriginMetadata origins
         */

        /**
         * Constructs a new BotMessageOriginMetadata.
         * @memberof proto
         * @classdesc Represents a BotMessageOriginMetadata.
         * @implements IBotMessageOriginMetadata
         * @constructor
         * @param {proto.IBotMessageOriginMetadata=} [properties] Properties to set
         */
        function BotMessageOriginMetadata(properties) {
            this.origins = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotMessageOriginMetadata origins.
         * @member {Array.<proto.IBotMessageOrigin>} origins
         * @memberof proto.BotMessageOriginMetadata
         * @instance
         */
        BotMessageOriginMetadata.prototype.origins = $util.emptyArray;

        /**
         * Creates a new BotMessageOriginMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotMessageOriginMetadata
         * @static
         * @param {proto.IBotMessageOriginMetadata=} [properties] Properties to set
         * @returns {proto.BotMessageOriginMetadata} BotMessageOriginMetadata instance
         */
        BotMessageOriginMetadata.create = function create(properties) {
            return new BotMessageOriginMetadata(properties);
        };

        /**
         * Encodes the specified BotMessageOriginMetadata message. Does not implicitly {@link proto.BotMessageOriginMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotMessageOriginMetadata
         * @static
         * @param {proto.IBotMessageOriginMetadata} message BotMessageOriginMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotMessageOriginMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.origins != null && message.origins.length)
                for (var i = 0; i < message.origins.length; ++i)
                    $root.proto.BotMessageOrigin.encode(message.origins[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotMessageOriginMetadata message, length delimited. Does not implicitly {@link proto.BotMessageOriginMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotMessageOriginMetadata
         * @static
         * @param {proto.IBotMessageOriginMetadata} message BotMessageOriginMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotMessageOriginMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotMessageOriginMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotMessageOriginMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotMessageOriginMetadata} BotMessageOriginMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotMessageOriginMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(message.origins && message.origins.length))
                            message.origins = [];
                        message.origins.push($root.proto.BotMessageOrigin.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotMessageOriginMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotMessageOriginMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotMessageOriginMetadata} BotMessageOriginMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotMessageOriginMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotMessageOriginMetadata message.
         * @function verify
         * @memberof proto.BotMessageOriginMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotMessageOriginMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            if (message.origins != null && Object.hasOwnProperty.call(message, "origins")) {
                if (!Array.isArray(message.origins))
                    return "origins: array expected";
                for (var i = 0; i < message.origins.length; ++i) {
                    var error = $root.proto.BotMessageOrigin.verify(message.origins[i], q + 1);
                    if (error)
                        return "origins." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BotMessageOriginMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotMessageOriginMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotMessageOriginMetadata} BotMessageOriginMetadata
         */
        BotMessageOriginMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotMessageOriginMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.origins) {
                if (!Array.isArray(object.origins))
                    throw TypeError(".proto.BotMessageOriginMetadata.origins: array expected");
                message.origins = Array(object.origins.length);
                for (var i = 0; i < object.origins.length; ++i) {
                    if (!$util.isObject(object.origins[i]))
                        throw TypeError(".proto.BotMessageOriginMetadata.origins: object expected");
                    message.origins[i] = $root.proto.BotMessageOrigin.fromObject(object.origins[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a BotMessageOriginMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotMessageOriginMetadata
         * @static
         * @param {proto.BotMessageOriginMetadata} message BotMessageOriginMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotMessageOriginMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.origins = [];
            if (message.origins && message.origins.length) {
                object.origins = Array(message.origins.length);
                for (var j = 0; j < message.origins.length; ++j)
                    object.origins[j] = $root.proto.BotMessageOrigin.toObject(message.origins[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this BotMessageOriginMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotMessageOriginMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotMessageOriginMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotMessageOriginMetadata
         * @function getTypeUrl
         * @memberof proto.BotMessageOriginMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotMessageOriginMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotMessageOriginMetadata";
        };

        return BotMessageOriginMetadata;
    })();

    proto.BotMessageSharingInfo = (function() {

        /**
         * Properties of a BotMessageSharingInfo.
         * @memberof proto
         * @interface IBotMessageSharingInfo
         * @property {proto.BotMetricsEntryPoint|null} [botEntryPointOrigin] BotMessageSharingInfo botEntryPointOrigin
         * @property {number|null} [forwardScore] BotMessageSharingInfo forwardScore
         */

        /**
         * Constructs a new BotMessageSharingInfo.
         * @memberof proto
         * @classdesc Represents a BotMessageSharingInfo.
         * @implements IBotMessageSharingInfo
         * @constructor
         * @param {proto.IBotMessageSharingInfo=} [properties] Properties to set
         */
        function BotMessageSharingInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotMessageSharingInfo botEntryPointOrigin.
         * @member {proto.BotMetricsEntryPoint|null|undefined} botEntryPointOrigin
         * @memberof proto.BotMessageSharingInfo
         * @instance
         */
        BotMessageSharingInfo.prototype.botEntryPointOrigin = null;

        /**
         * BotMessageSharingInfo forwardScore.
         * @member {number|null|undefined} forwardScore
         * @memberof proto.BotMessageSharingInfo
         * @instance
         */
        BotMessageSharingInfo.prototype.forwardScore = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMessageSharingInfo.prototype, "_botEntryPointOrigin", {
            get: $util.oneOfGetter($oneOfFields = ["botEntryPointOrigin"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMessageSharingInfo.prototype, "_forwardScore", {
            get: $util.oneOfGetter($oneOfFields = ["forwardScore"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotMessageSharingInfo instance using the specified properties.
         * @function create
         * @memberof proto.BotMessageSharingInfo
         * @static
         * @param {proto.IBotMessageSharingInfo=} [properties] Properties to set
         * @returns {proto.BotMessageSharingInfo} BotMessageSharingInfo instance
         */
        BotMessageSharingInfo.create = function create(properties) {
            return new BotMessageSharingInfo(properties);
        };

        /**
         * Encodes the specified BotMessageSharingInfo message. Does not implicitly {@link proto.BotMessageSharingInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.BotMessageSharingInfo
         * @static
         * @param {proto.IBotMessageSharingInfo} message BotMessageSharingInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotMessageSharingInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.botEntryPointOrigin != null && Object.hasOwnProperty.call(message, "botEntryPointOrigin"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.botEntryPointOrigin);
            if (message.forwardScore != null && Object.hasOwnProperty.call(message, "forwardScore"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.forwardScore);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotMessageSharingInfo message, length delimited. Does not implicitly {@link proto.BotMessageSharingInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotMessageSharingInfo
         * @static
         * @param {proto.IBotMessageSharingInfo} message BotMessageSharingInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotMessageSharingInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotMessageSharingInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotMessageSharingInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotMessageSharingInfo} BotMessageSharingInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotMessageSharingInfo.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.botEntryPointOrigin = reader.int32();
                        message._botEntryPointOrigin = "botEntryPointOrigin";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.forwardScore = reader.uint32();
                        message._forwardScore = "forwardScore";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotMessageSharingInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotMessageSharingInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotMessageSharingInfo} BotMessageSharingInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotMessageSharingInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotMessageSharingInfo message.
         * @function verify
         * @memberof proto.BotMessageSharingInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotMessageSharingInfo.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.botEntryPointOrigin != null && Object.hasOwnProperty.call(message, "botEntryPointOrigin")) {
                properties._botEntryPointOrigin = 1;
                if (typeof message.botEntryPointOrigin !== "number" || (message.botEntryPointOrigin | 0) !== message.botEntryPointOrigin)
                    return "botEntryPointOrigin: enum value expected";
            }
            if (message.forwardScore != null && Object.hasOwnProperty.call(message, "forwardScore")) {
                properties._forwardScore = 1;
                if (!$util.isInteger(message.forwardScore))
                    return "forwardScore: integer expected";
            }
            return null;
        };

        /**
         * Creates a BotMessageSharingInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotMessageSharingInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotMessageSharingInfo} BotMessageSharingInfo
         */
        BotMessageSharingInfo.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotMessageSharingInfo: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.botEntryPointOrigin) {
            case "UNDEFINED_ENTRY_POINT":
            case 0:
                message.botEntryPointOrigin = 0;
                break;
            case "FAVICON":
            case 1:
                message.botEntryPointOrigin = 1;
                break;
            case "CHATLIST":
            case 2:
                message.botEntryPointOrigin = 2;
                break;
            case "AISEARCH_NULL_STATE_PAPER_PLANE":
            case 3:
                message.botEntryPointOrigin = 3;
                break;
            case "AISEARCH_NULL_STATE_SUGGESTION":
            case 4:
                message.botEntryPointOrigin = 4;
                break;
            case "AISEARCH_TYPE_AHEAD_SUGGESTION":
            case 5:
                message.botEntryPointOrigin = 5;
                break;
            case "AISEARCH_TYPE_AHEAD_PAPER_PLANE":
            case 6:
                message.botEntryPointOrigin = 6;
                break;
            case "AISEARCH_TYPE_AHEAD_RESULT_CHATLIST":
            case 7:
                message.botEntryPointOrigin = 7;
                break;
            case "AISEARCH_TYPE_AHEAD_RESULT_MESSAGES":
            case 8:
                message.botEntryPointOrigin = 8;
                break;
            case "AIVOICE_SEARCH_BAR":
            case 9:
                message.botEntryPointOrigin = 9;
                break;
            case "AIVOICE_FAVICON":
            case 10:
                message.botEntryPointOrigin = 10;
                break;
            case "AISTUDIO":
            case 11:
                message.botEntryPointOrigin = 11;
                break;
            case "DEEPLINK":
            case 12:
                message.botEntryPointOrigin = 12;
                break;
            case "NOTIFICATION":
            case 13:
                message.botEntryPointOrigin = 13;
                break;
            case "PROFILE_MESSAGE_BUTTON":
            case 14:
                message.botEntryPointOrigin = 14;
                break;
            case "FORWARD":
            case 15:
                message.botEntryPointOrigin = 15;
                break;
            case "APP_SHORTCUT":
            case 16:
                message.botEntryPointOrigin = 16;
                break;
            case "FF_FAMILY":
            case 17:
                message.botEntryPointOrigin = 17;
                break;
            case "AI_TAB":
            case 18:
                message.botEntryPointOrigin = 18;
                break;
            case "AI_HOME":
            case 19:
                message.botEntryPointOrigin = 19;
                break;
            case "AI_DEEPLINK_IMMERSIVE":
            case 20:
                message.botEntryPointOrigin = 20;
                break;
            case "AI_DEEPLINK":
            case 21:
                message.botEntryPointOrigin = 21;
                break;
            case "META_AI_CHAT_SHORTCUT_AI_STUDIO":
            case 22:
                message.botEntryPointOrigin = 22;
                break;
            case "UGC_CHAT_SHORTCUT_AI_STUDIO":
            case 23:
                message.botEntryPointOrigin = 23;
                break;
            case "NEW_CHAT_AI_STUDIO":
            case 24:
                message.botEntryPointOrigin = 24;
                break;
            case "AIVOICE_FAVICON_CALL_HISTORY":
            case 25:
                message.botEntryPointOrigin = 25;
                break;
            case "ASK_META_AI_CONTEXT_MENU":
            case 26:
                message.botEntryPointOrigin = 26;
                break;
            case "ASK_META_AI_CONTEXT_MENU_1ON1":
            case 27:
                message.botEntryPointOrigin = 27;
                break;
            case "ASK_META_AI_CONTEXT_MENU_GROUP":
            case 28:
                message.botEntryPointOrigin = 28;
                break;
            case "INVOKE_META_AI_1ON1":
            case 29:
                message.botEntryPointOrigin = 29;
                break;
            case "INVOKE_META_AI_GROUP":
            case 30:
                message.botEntryPointOrigin = 30;
                break;
            case "META_AI_FORWARD":
            case 31:
                message.botEntryPointOrigin = 31;
                break;
            case "NEW_CHAT_AI_CONTACT":
            case 32:
                message.botEntryPointOrigin = 32;
                break;
            case "MESSAGE_QUICK_ACTION_1_ON_1_CHAT":
            case 33:
                message.botEntryPointOrigin = 33;
                break;
            case "MESSAGE_QUICK_ACTION_GROUP_CHAT":
            case 34:
                message.botEntryPointOrigin = 34;
                break;
            case "ATTACHMENT_TRAY_1_ON_1_CHAT":
            case 35:
                message.botEntryPointOrigin = 35;
                break;
            case "ATTACHMENT_TRAY_GROUP_CHAT":
            case 36:
                message.botEntryPointOrigin = 36;
                break;
            case "ASK_META_AI_MEDIA_VIEWER_1ON1":
            case 37:
                message.botEntryPointOrigin = 37;
                break;
            case "ASK_META_AI_MEDIA_VIEWER_GROUP":
            case 38:
                message.botEntryPointOrigin = 38;
                break;
            case "MEDIA_PICKER_1_ON_1_CHAT":
            case 39:
                message.botEntryPointOrigin = 39;
                break;
            case "MEDIA_PICKER_GROUP_CHAT":
            case 40:
                message.botEntryPointOrigin = 40;
                break;
            case "ASK_META_AI_NO_SEARCH_RESULTS":
            case 41:
                message.botEntryPointOrigin = 41;
                break;
            case "META_AI_SETTINGS":
            case 45:
                message.botEntryPointOrigin = 45;
                break;
            case "WEB_INTRO_PANEL":
            case 46:
                message.botEntryPointOrigin = 46;
                break;
            case "WEB_NAVIGATION_BAR":
            case 47:
                message.botEntryPointOrigin = 47;
                break;
            case "GROUP_MEMBER":
            case 54:
                message.botEntryPointOrigin = 54;
                break;
            case "CHATLIST_SEARCH":
            case 55:
                message.botEntryPointOrigin = 55;
                break;
            case "NEW_CHAT_LIST":
            case 56:
                message.botEntryPointOrigin = 56;
                break;
            default:
                if (typeof object.botEntryPointOrigin === "number" && (object.botEntryPointOrigin | 0) === object.botEntryPointOrigin)
                    message.botEntryPointOrigin = object.botEntryPointOrigin;
            }
            if (object.forwardScore != null)
                message.forwardScore = object.forwardScore >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a BotMessageSharingInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotMessageSharingInfo
         * @static
         * @param {proto.BotMessageSharingInfo} message BotMessageSharingInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotMessageSharingInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.botEntryPointOrigin != null && Object.hasOwnProperty.call(message, "botEntryPointOrigin"))
                object.botEntryPointOrigin = options.enums === String ? $root.proto.BotMetricsEntryPoint[message.botEntryPointOrigin] === undefined ? message.botEntryPointOrigin : $root.proto.BotMetricsEntryPoint[message.botEntryPointOrigin] : message.botEntryPointOrigin;
            if (message.forwardScore != null && Object.hasOwnProperty.call(message, "forwardScore"))
                object.forwardScore = message.forwardScore;
            return object;
        };

        /**
         * Converts this BotMessageSharingInfo to JSON.
         * @function toJSON
         * @memberof proto.BotMessageSharingInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotMessageSharingInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotMessageSharingInfo
         * @function getTypeUrl
         * @memberof proto.BotMessageSharingInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotMessageSharingInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotMessageSharingInfo";
        };

        return BotMessageSharingInfo;
    })();

    proto.BotMetadata = (function() {

        /**
         * Properties of a BotMetadata.
         * @memberof proto
         * @interface IBotMetadata
         * @property {string|null} [personaId] BotMetadata personaId
         * @property {proto.IBotPluginMetadata|null} [pluginMetadata] BotMetadata pluginMetadata
         * @property {proto.IBotSuggestedPromptMetadata|null} [suggestedPromptMetadata] BotMetadata suggestedPromptMetadata
         * @property {string|null} [invokerJid] BotMetadata invokerJid
         * @property {proto.IBotSessionMetadata|null} [sessionMetadata] BotMetadata sessionMetadata
         * @property {proto.IBotMemuMetadata|null} [memuMetadata] BotMetadata memuMetadata
         * @property {string|null} [timezone] BotMetadata timezone
         * @property {proto.IBotReminderMetadata|null} [reminderMetadata] BotMetadata reminderMetadata
         * @property {proto.IBotModelMetadata|null} [modelMetadata] BotMetadata modelMetadata
         * @property {string|null} [messageDisclaimerText] BotMetadata messageDisclaimerText
         * @property {proto.IBotProgressIndicatorMetadata|null} [progressIndicatorMetadata] BotMetadata progressIndicatorMetadata
         * @property {proto.IBotCapabilityMetadata|null} [capabilityMetadata] BotMetadata capabilityMetadata
         * @property {proto.IBotImagineMetadata|null} [imagineMetadata] BotMetadata imagineMetadata
         * @property {proto.IBotMemoryMetadata|null} [memoryMetadata] BotMetadata memoryMetadata
         * @property {proto.IBotRenderingMetadata|null} [renderingMetadata] BotMetadata renderingMetadata
         * @property {proto.IBotMetricsMetadata|null} [botMetricsMetadata] BotMetadata botMetricsMetadata
         * @property {proto.IBotLinkedAccountsMetadata|null} [botLinkedAccountsMetadata] BotMetadata botLinkedAccountsMetadata
         * @property {proto.IBotSourcesMetadata|null} [richResponseSourcesMetadata] BotMetadata richResponseSourcesMetadata
         * @property {Uint8Array|null} [aiConversationContext] BotMetadata aiConversationContext
         * @property {proto.IBotPromotionMessageMetadata|null} [botPromotionMessageMetadata] BotMetadata botPromotionMessageMetadata
         * @property {proto.IBotModeSelectionMetadata|null} [botModeSelectionMetadata] BotMetadata botModeSelectionMetadata
         * @property {proto.IBotQuotaMetadata|null} [botQuotaMetadata] BotMetadata botQuotaMetadata
         * @property {proto.IBotAgeCollectionMetadata|null} [botAgeCollectionMetadata] BotMetadata botAgeCollectionMetadata
         * @property {string|null} [conversationStarterPromptId] BotMetadata conversationStarterPromptId
         * @property {string|null} [botResponseId] BotMetadata botResponseId
         * @property {proto.IBotSignatureVerificationMetadata|null} [verificationMetadata] BotMetadata verificationMetadata
         * @property {proto.IBotUnifiedResponseMutation|null} [unifiedResponseMutation] BotMetadata unifiedResponseMutation
         * @property {proto.IBotMessageOriginMetadata|null} [botMessageOriginMetadata] BotMetadata botMessageOriginMetadata
         * @property {proto.IInThreadSurveyMetadata|null} [inThreadSurveyMetadata] BotMetadata inThreadSurveyMetadata
         * @property {proto.IAIThreadInfo|null} [botThreadInfo] BotMetadata botThreadInfo
         * @property {proto.IAIRegenerateMetadata|null} [regenerateMetadata] BotMetadata regenerateMetadata
         * @property {proto.ISessionTransparencyMetadata|null} [sessionTransparencyMetadata] BotMetadata sessionTransparencyMetadata
         * @property {proto.IBotDocumentMessageMetadata|null} [botDocumentMessageMetadata] BotMetadata botDocumentMessageMetadata
         * @property {proto.IBotGroupMetadata|null} [botGroupMetadata] BotMetadata botGroupMetadata
         * @property {proto.IBotRenderingConfigMetadata|null} [botRenderingConfigMetadata] BotMetadata botRenderingConfigMetadata
         * @property {proto.IBotInfrastructureDiagnostics|null} [botInfrastructureDiagnostics] BotMetadata botInfrastructureDiagnostics
         * @property {proto.IAIMediaCollectionMetadata|null} [aiMediaCollectionMetadata] BotMetadata aiMediaCollectionMetadata
         * @property {proto.IBotCommandMetadata|null} [commandMetadata] BotMetadata commandMetadata
         * @property {proto.IBotResolvedToolCallMetadata|null} [resolvedToolCallMetadata] BotMetadata resolvedToolCallMetadata
         * @property {proto.IAISubscriptionUpsellMetadata|null} [subscriptionUpsellMetadata] BotMetadata subscriptionUpsellMetadata
         * @property {proto.IBotPttPromptMetadata|null} [pttPromptMetadata] BotMetadata pttPromptMetadata
         * @property {Uint8Array|null} [internalMetadata] BotMetadata internalMetadata
         */

        /**
         * Constructs a new BotMetadata.
         * @memberof proto
         * @classdesc Represents a BotMetadata.
         * @implements IBotMetadata
         * @constructor
         * @param {proto.IBotMetadata=} [properties] Properties to set
         */
        function BotMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotMetadata personaId.
         * @member {string|null|undefined} personaId
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.personaId = null;

        /**
         * BotMetadata pluginMetadata.
         * @member {proto.IBotPluginMetadata|null|undefined} pluginMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.pluginMetadata = null;

        /**
         * BotMetadata suggestedPromptMetadata.
         * @member {proto.IBotSuggestedPromptMetadata|null|undefined} suggestedPromptMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.suggestedPromptMetadata = null;

        /**
         * BotMetadata invokerJid.
         * @member {string|null|undefined} invokerJid
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.invokerJid = null;

        /**
         * BotMetadata sessionMetadata.
         * @member {proto.IBotSessionMetadata|null|undefined} sessionMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.sessionMetadata = null;

        /**
         * BotMetadata memuMetadata.
         * @member {proto.IBotMemuMetadata|null|undefined} memuMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.memuMetadata = null;

        /**
         * BotMetadata timezone.
         * @member {string|null|undefined} timezone
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.timezone = null;

        /**
         * BotMetadata reminderMetadata.
         * @member {proto.IBotReminderMetadata|null|undefined} reminderMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.reminderMetadata = null;

        /**
         * BotMetadata modelMetadata.
         * @member {proto.IBotModelMetadata|null|undefined} modelMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.modelMetadata = null;

        /**
         * BotMetadata messageDisclaimerText.
         * @member {string|null|undefined} messageDisclaimerText
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.messageDisclaimerText = null;

        /**
         * BotMetadata progressIndicatorMetadata.
         * @member {proto.IBotProgressIndicatorMetadata|null|undefined} progressIndicatorMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.progressIndicatorMetadata = null;

        /**
         * BotMetadata capabilityMetadata.
         * @member {proto.IBotCapabilityMetadata|null|undefined} capabilityMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.capabilityMetadata = null;

        /**
         * BotMetadata imagineMetadata.
         * @member {proto.IBotImagineMetadata|null|undefined} imagineMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.imagineMetadata = null;

        /**
         * BotMetadata memoryMetadata.
         * @member {proto.IBotMemoryMetadata|null|undefined} memoryMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.memoryMetadata = null;

        /**
         * BotMetadata renderingMetadata.
         * @member {proto.IBotRenderingMetadata|null|undefined} renderingMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.renderingMetadata = null;

        /**
         * BotMetadata botMetricsMetadata.
         * @member {proto.IBotMetricsMetadata|null|undefined} botMetricsMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.botMetricsMetadata = null;

        /**
         * BotMetadata botLinkedAccountsMetadata.
         * @member {proto.IBotLinkedAccountsMetadata|null|undefined} botLinkedAccountsMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.botLinkedAccountsMetadata = null;

        /**
         * BotMetadata richResponseSourcesMetadata.
         * @member {proto.IBotSourcesMetadata|null|undefined} richResponseSourcesMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.richResponseSourcesMetadata = null;

        /**
         * BotMetadata aiConversationContext.
         * @member {Uint8Array|null|undefined} aiConversationContext
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.aiConversationContext = null;

        /**
         * BotMetadata botPromotionMessageMetadata.
         * @member {proto.IBotPromotionMessageMetadata|null|undefined} botPromotionMessageMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.botPromotionMessageMetadata = null;

        /**
         * BotMetadata botModeSelectionMetadata.
         * @member {proto.IBotModeSelectionMetadata|null|undefined} botModeSelectionMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.botModeSelectionMetadata = null;

        /**
         * BotMetadata botQuotaMetadata.
         * @member {proto.IBotQuotaMetadata|null|undefined} botQuotaMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.botQuotaMetadata = null;

        /**
         * BotMetadata botAgeCollectionMetadata.
         * @member {proto.IBotAgeCollectionMetadata|null|undefined} botAgeCollectionMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.botAgeCollectionMetadata = null;

        /**
         * BotMetadata conversationStarterPromptId.
         * @member {string|null|undefined} conversationStarterPromptId
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.conversationStarterPromptId = null;

        /**
         * BotMetadata botResponseId.
         * @member {string|null|undefined} botResponseId
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.botResponseId = null;

        /**
         * BotMetadata verificationMetadata.
         * @member {proto.IBotSignatureVerificationMetadata|null|undefined} verificationMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.verificationMetadata = null;

        /**
         * BotMetadata unifiedResponseMutation.
         * @member {proto.IBotUnifiedResponseMutation|null|undefined} unifiedResponseMutation
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.unifiedResponseMutation = null;

        /**
         * BotMetadata botMessageOriginMetadata.
         * @member {proto.IBotMessageOriginMetadata|null|undefined} botMessageOriginMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.botMessageOriginMetadata = null;

        /**
         * BotMetadata inThreadSurveyMetadata.
         * @member {proto.IInThreadSurveyMetadata|null|undefined} inThreadSurveyMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.inThreadSurveyMetadata = null;

        /**
         * BotMetadata botThreadInfo.
         * @member {proto.IAIThreadInfo|null|undefined} botThreadInfo
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.botThreadInfo = null;

        /**
         * BotMetadata regenerateMetadata.
         * @member {proto.IAIRegenerateMetadata|null|undefined} regenerateMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.regenerateMetadata = null;

        /**
         * BotMetadata sessionTransparencyMetadata.
         * @member {proto.ISessionTransparencyMetadata|null|undefined} sessionTransparencyMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.sessionTransparencyMetadata = null;

        /**
         * BotMetadata botDocumentMessageMetadata.
         * @member {proto.IBotDocumentMessageMetadata|null|undefined} botDocumentMessageMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.botDocumentMessageMetadata = null;

        /**
         * BotMetadata botGroupMetadata.
         * @member {proto.IBotGroupMetadata|null|undefined} botGroupMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.botGroupMetadata = null;

        /**
         * BotMetadata botRenderingConfigMetadata.
         * @member {proto.IBotRenderingConfigMetadata|null|undefined} botRenderingConfigMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.botRenderingConfigMetadata = null;

        /**
         * BotMetadata botInfrastructureDiagnostics.
         * @member {proto.IBotInfrastructureDiagnostics|null|undefined} botInfrastructureDiagnostics
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.botInfrastructureDiagnostics = null;

        /**
         * BotMetadata aiMediaCollectionMetadata.
         * @member {proto.IAIMediaCollectionMetadata|null|undefined} aiMediaCollectionMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.aiMediaCollectionMetadata = null;

        /**
         * BotMetadata commandMetadata.
         * @member {proto.IBotCommandMetadata|null|undefined} commandMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.commandMetadata = null;

        /**
         * BotMetadata resolvedToolCallMetadata.
         * @member {proto.IBotResolvedToolCallMetadata|null|undefined} resolvedToolCallMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.resolvedToolCallMetadata = null;

        /**
         * BotMetadata subscriptionUpsellMetadata.
         * @member {proto.IAISubscriptionUpsellMetadata|null|undefined} subscriptionUpsellMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.subscriptionUpsellMetadata = null;

        /**
         * BotMetadata pttPromptMetadata.
         * @member {proto.IBotPttPromptMetadata|null|undefined} pttPromptMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.pttPromptMetadata = null;

        /**
         * BotMetadata internalMetadata.
         * @member {Uint8Array|null|undefined} internalMetadata
         * @memberof proto.BotMetadata
         * @instance
         */
        BotMetadata.prototype.internalMetadata = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_personaId", {
            get: $util.oneOfGetter($oneOfFields = ["personaId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_pluginMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["pluginMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_suggestedPromptMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["suggestedPromptMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_invokerJid", {
            get: $util.oneOfGetter($oneOfFields = ["invokerJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_sessionMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["sessionMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_memuMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["memuMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_timezone", {
            get: $util.oneOfGetter($oneOfFields = ["timezone"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_reminderMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["reminderMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_modelMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["modelMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_messageDisclaimerText", {
            get: $util.oneOfGetter($oneOfFields = ["messageDisclaimerText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_progressIndicatorMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["progressIndicatorMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_capabilityMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["capabilityMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_imagineMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["imagineMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_memoryMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["memoryMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_renderingMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["renderingMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botMetricsMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botMetricsMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botLinkedAccountsMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botLinkedAccountsMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_richResponseSourcesMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["richResponseSourcesMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_aiConversationContext", {
            get: $util.oneOfGetter($oneOfFields = ["aiConversationContext"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botPromotionMessageMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botPromotionMessageMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botModeSelectionMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botModeSelectionMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botQuotaMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botQuotaMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botAgeCollectionMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botAgeCollectionMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_conversationStarterPromptId", {
            get: $util.oneOfGetter($oneOfFields = ["conversationStarterPromptId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botResponseId", {
            get: $util.oneOfGetter($oneOfFields = ["botResponseId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_verificationMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["verificationMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_unifiedResponseMutation", {
            get: $util.oneOfGetter($oneOfFields = ["unifiedResponseMutation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botMessageOriginMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botMessageOriginMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_inThreadSurveyMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["inThreadSurveyMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botThreadInfo", {
            get: $util.oneOfGetter($oneOfFields = ["botThreadInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_regenerateMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["regenerateMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_sessionTransparencyMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["sessionTransparencyMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botDocumentMessageMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botDocumentMessageMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botGroupMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botGroupMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botRenderingConfigMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botRenderingConfigMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botInfrastructureDiagnostics", {
            get: $util.oneOfGetter($oneOfFields = ["botInfrastructureDiagnostics"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_aiMediaCollectionMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["aiMediaCollectionMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_commandMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["commandMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_resolvedToolCallMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["resolvedToolCallMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_subscriptionUpsellMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["subscriptionUpsellMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_pttPromptMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["pttPromptMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_internalMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["internalMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotMetadata
         * @static
         * @param {proto.IBotMetadata=} [properties] Properties to set
         * @returns {proto.BotMetadata} BotMetadata instance
         */
        BotMetadata.create = function create(properties) {
            return new BotMetadata(properties);
        };

        /**
         * Encodes the specified BotMetadata message. Does not implicitly {@link proto.BotMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotMetadata
         * @static
         * @param {proto.IBotMetadata} message BotMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.personaId != null && Object.hasOwnProperty.call(message, "personaId"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.personaId);
            if (message.pluginMetadata != null && Object.hasOwnProperty.call(message, "pluginMetadata"))
                $root.proto.BotPluginMetadata.encode(message.pluginMetadata, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.suggestedPromptMetadata != null && Object.hasOwnProperty.call(message, "suggestedPromptMetadata"))
                $root.proto.BotSuggestedPromptMetadata.encode(message.suggestedPromptMetadata, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.invokerJid != null && Object.hasOwnProperty.call(message, "invokerJid"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.invokerJid);
            if (message.sessionMetadata != null && Object.hasOwnProperty.call(message, "sessionMetadata"))
                $root.proto.BotSessionMetadata.encode(message.sessionMetadata, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
            if (message.memuMetadata != null && Object.hasOwnProperty.call(message, "memuMetadata"))
                $root.proto.BotMemuMetadata.encode(message.memuMetadata, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
            if (message.timezone != null && Object.hasOwnProperty.call(message, "timezone"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.timezone);
            if (message.reminderMetadata != null && Object.hasOwnProperty.call(message, "reminderMetadata"))
                $root.proto.BotReminderMetadata.encode(message.reminderMetadata, writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
            if (message.modelMetadata != null && Object.hasOwnProperty.call(message, "modelMetadata"))
                $root.proto.BotModelMetadata.encode(message.modelMetadata, writer.uint32(/* id 10, wireType 2 =*/82).fork(), q + 1).ldelim();
            if (message.messageDisclaimerText != null && Object.hasOwnProperty.call(message, "messageDisclaimerText"))
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.messageDisclaimerText);
            if (message.progressIndicatorMetadata != null && Object.hasOwnProperty.call(message, "progressIndicatorMetadata"))
                $root.proto.BotProgressIndicatorMetadata.encode(message.progressIndicatorMetadata, writer.uint32(/* id 12, wireType 2 =*/98).fork(), q + 1).ldelim();
            if (message.capabilityMetadata != null && Object.hasOwnProperty.call(message, "capabilityMetadata"))
                $root.proto.BotCapabilityMetadata.encode(message.capabilityMetadata, writer.uint32(/* id 13, wireType 2 =*/106).fork(), q + 1).ldelim();
            if (message.imagineMetadata != null && Object.hasOwnProperty.call(message, "imagineMetadata"))
                $root.proto.BotImagineMetadata.encode(message.imagineMetadata, writer.uint32(/* id 14, wireType 2 =*/114).fork(), q + 1).ldelim();
            if (message.memoryMetadata != null && Object.hasOwnProperty.call(message, "memoryMetadata"))
                $root.proto.BotMemoryMetadata.encode(message.memoryMetadata, writer.uint32(/* id 15, wireType 2 =*/122).fork(), q + 1).ldelim();
            if (message.renderingMetadata != null && Object.hasOwnProperty.call(message, "renderingMetadata"))
                $root.proto.BotRenderingMetadata.encode(message.renderingMetadata, writer.uint32(/* id 16, wireType 2 =*/130).fork(), q + 1).ldelim();
            if (message.botMetricsMetadata != null && Object.hasOwnProperty.call(message, "botMetricsMetadata"))
                $root.proto.BotMetricsMetadata.encode(message.botMetricsMetadata, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
            if (message.botLinkedAccountsMetadata != null && Object.hasOwnProperty.call(message, "botLinkedAccountsMetadata"))
                $root.proto.BotLinkedAccountsMetadata.encode(message.botLinkedAccountsMetadata, writer.uint32(/* id 18, wireType 2 =*/146).fork(), q + 1).ldelim();
            if (message.richResponseSourcesMetadata != null && Object.hasOwnProperty.call(message, "richResponseSourcesMetadata"))
                $root.proto.BotSourcesMetadata.encode(message.richResponseSourcesMetadata, writer.uint32(/* id 19, wireType 2 =*/154).fork(), q + 1).ldelim();
            if (message.aiConversationContext != null && Object.hasOwnProperty.call(message, "aiConversationContext"))
                writer.uint32(/* id 20, wireType 2 =*/162).bytes(message.aiConversationContext);
            if (message.botPromotionMessageMetadata != null && Object.hasOwnProperty.call(message, "botPromotionMessageMetadata"))
                $root.proto.BotPromotionMessageMetadata.encode(message.botPromotionMessageMetadata, writer.uint32(/* id 21, wireType 2 =*/170).fork(), q + 1).ldelim();
            if (message.botModeSelectionMetadata != null && Object.hasOwnProperty.call(message, "botModeSelectionMetadata"))
                $root.proto.BotModeSelectionMetadata.encode(message.botModeSelectionMetadata, writer.uint32(/* id 22, wireType 2 =*/178).fork(), q + 1).ldelim();
            if (message.botQuotaMetadata != null && Object.hasOwnProperty.call(message, "botQuotaMetadata"))
                $root.proto.BotQuotaMetadata.encode(message.botQuotaMetadata, writer.uint32(/* id 23, wireType 2 =*/186).fork(), q + 1).ldelim();
            if (message.botAgeCollectionMetadata != null && Object.hasOwnProperty.call(message, "botAgeCollectionMetadata"))
                $root.proto.BotAgeCollectionMetadata.encode(message.botAgeCollectionMetadata, writer.uint32(/* id 24, wireType 2 =*/194).fork(), q + 1).ldelim();
            if (message.conversationStarterPromptId != null && Object.hasOwnProperty.call(message, "conversationStarterPromptId"))
                writer.uint32(/* id 25, wireType 2 =*/202).string(message.conversationStarterPromptId);
            if (message.botResponseId != null && Object.hasOwnProperty.call(message, "botResponseId"))
                writer.uint32(/* id 26, wireType 2 =*/210).string(message.botResponseId);
            if (message.verificationMetadata != null && Object.hasOwnProperty.call(message, "verificationMetadata"))
                $root.proto.BotSignatureVerificationMetadata.encode(message.verificationMetadata, writer.uint32(/* id 27, wireType 2 =*/218).fork(), q + 1).ldelim();
            if (message.unifiedResponseMutation != null && Object.hasOwnProperty.call(message, "unifiedResponseMutation"))
                $root.proto.BotUnifiedResponseMutation.encode(message.unifiedResponseMutation, writer.uint32(/* id 28, wireType 2 =*/226).fork(), q + 1).ldelim();
            if (message.botMessageOriginMetadata != null && Object.hasOwnProperty.call(message, "botMessageOriginMetadata"))
                $root.proto.BotMessageOriginMetadata.encode(message.botMessageOriginMetadata, writer.uint32(/* id 29, wireType 2 =*/234).fork(), q + 1).ldelim();
            if (message.inThreadSurveyMetadata != null && Object.hasOwnProperty.call(message, "inThreadSurveyMetadata"))
                $root.proto.InThreadSurveyMetadata.encode(message.inThreadSurveyMetadata, writer.uint32(/* id 30, wireType 2 =*/242).fork(), q + 1).ldelim();
            if (message.botThreadInfo != null && Object.hasOwnProperty.call(message, "botThreadInfo"))
                $root.proto.AIThreadInfo.encode(message.botThreadInfo, writer.uint32(/* id 31, wireType 2 =*/250).fork(), q + 1).ldelim();
            if (message.regenerateMetadata != null && Object.hasOwnProperty.call(message, "regenerateMetadata"))
                $root.proto.AIRegenerateMetadata.encode(message.regenerateMetadata, writer.uint32(/* id 32, wireType 2 =*/258).fork(), q + 1).ldelim();
            if (message.sessionTransparencyMetadata != null && Object.hasOwnProperty.call(message, "sessionTransparencyMetadata"))
                $root.proto.SessionTransparencyMetadata.encode(message.sessionTransparencyMetadata, writer.uint32(/* id 33, wireType 2 =*/266).fork(), q + 1).ldelim();
            if (message.botDocumentMessageMetadata != null && Object.hasOwnProperty.call(message, "botDocumentMessageMetadata"))
                $root.proto.BotDocumentMessageMetadata.encode(message.botDocumentMessageMetadata, writer.uint32(/* id 34, wireType 2 =*/274).fork(), q + 1).ldelim();
            if (message.botGroupMetadata != null && Object.hasOwnProperty.call(message, "botGroupMetadata"))
                $root.proto.BotGroupMetadata.encode(message.botGroupMetadata, writer.uint32(/* id 35, wireType 2 =*/282).fork(), q + 1).ldelim();
            if (message.botRenderingConfigMetadata != null && Object.hasOwnProperty.call(message, "botRenderingConfigMetadata"))
                $root.proto.BotRenderingConfigMetadata.encode(message.botRenderingConfigMetadata, writer.uint32(/* id 36, wireType 2 =*/290).fork(), q + 1).ldelim();
            if (message.botInfrastructureDiagnostics != null && Object.hasOwnProperty.call(message, "botInfrastructureDiagnostics"))
                $root.proto.BotInfrastructureDiagnostics.encode(message.botInfrastructureDiagnostics, writer.uint32(/* id 37, wireType 2 =*/298).fork(), q + 1).ldelim();
            if (message.aiMediaCollectionMetadata != null && Object.hasOwnProperty.call(message, "aiMediaCollectionMetadata"))
                $root.proto.AIMediaCollectionMetadata.encode(message.aiMediaCollectionMetadata, writer.uint32(/* id 38, wireType 2 =*/306).fork(), q + 1).ldelim();
            if (message.commandMetadata != null && Object.hasOwnProperty.call(message, "commandMetadata"))
                $root.proto.BotCommandMetadata.encode(message.commandMetadata, writer.uint32(/* id 39, wireType 2 =*/314).fork(), q + 1).ldelim();
            if (message.resolvedToolCallMetadata != null && Object.hasOwnProperty.call(message, "resolvedToolCallMetadata"))
                $root.proto.BotResolvedToolCallMetadata.encode(message.resolvedToolCallMetadata, writer.uint32(/* id 40, wireType 2 =*/322).fork(), q + 1).ldelim();
            if (message.subscriptionUpsellMetadata != null && Object.hasOwnProperty.call(message, "subscriptionUpsellMetadata"))
                $root.proto.AISubscriptionUpsellMetadata.encode(message.subscriptionUpsellMetadata, writer.uint32(/* id 41, wireType 2 =*/330).fork(), q + 1).ldelim();
            if (message.pttPromptMetadata != null && Object.hasOwnProperty.call(message, "pttPromptMetadata"))
                $root.proto.BotPttPromptMetadata.encode(message.pttPromptMetadata, writer.uint32(/* id 42, wireType 2 =*/338).fork(), q + 1).ldelim();
            if (message.internalMetadata != null && Object.hasOwnProperty.call(message, "internalMetadata"))
                writer.uint32(/* id 999, wireType 2 =*/7994).bytes(message.internalMetadata);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotMetadata message, length delimited. Does not implicitly {@link proto.BotMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotMetadata
         * @static
         * @param {proto.IBotMetadata} message BotMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotMetadata} BotMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (u !== 2)
                            break;
                        message.personaId = reader.stringVerify();
                        message._personaId = "personaId";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.pluginMetadata = $root.proto.BotPluginMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.pluginMetadata);
                        message._pluginMetadata = "pluginMetadata";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.suggestedPromptMetadata = $root.proto.BotSuggestedPromptMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.suggestedPromptMetadata);
                        message._suggestedPromptMetadata = "suggestedPromptMetadata";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.invokerJid = reader.stringVerify();
                        message._invokerJid = "invokerJid";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        message.sessionMetadata = $root.proto.BotSessionMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.sessionMetadata);
                        message._sessionMetadata = "sessionMetadata";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        message.memuMetadata = $root.proto.BotMemuMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.memuMetadata);
                        message._memuMetadata = "memuMetadata";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        message.timezone = reader.stringVerify();
                        message._timezone = "timezone";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        message.reminderMetadata = $root.proto.BotReminderMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.reminderMetadata);
                        message._reminderMetadata = "reminderMetadata";
                        continue;
                    }
                case 10: {
                        if (u !== 2)
                            break;
                        message.modelMetadata = $root.proto.BotModelMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.modelMetadata);
                        message._modelMetadata = "modelMetadata";
                        continue;
                    }
                case 11: {
                        if (u !== 2)
                            break;
                        message.messageDisclaimerText = reader.stringVerify();
                        message._messageDisclaimerText = "messageDisclaimerText";
                        continue;
                    }
                case 12: {
                        if (u !== 2)
                            break;
                        message.progressIndicatorMetadata = $root.proto.BotProgressIndicatorMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.progressIndicatorMetadata);
                        message._progressIndicatorMetadata = "progressIndicatorMetadata";
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        message.capabilityMetadata = $root.proto.BotCapabilityMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.capabilityMetadata);
                        message._capabilityMetadata = "capabilityMetadata";
                        continue;
                    }
                case 14: {
                        if (u !== 2)
                            break;
                        message.imagineMetadata = $root.proto.BotImagineMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.imagineMetadata);
                        message._imagineMetadata = "imagineMetadata";
                        continue;
                    }
                case 15: {
                        if (u !== 2)
                            break;
                        message.memoryMetadata = $root.proto.BotMemoryMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.memoryMetadata);
                        message._memoryMetadata = "memoryMetadata";
                        continue;
                    }
                case 16: {
                        if (u !== 2)
                            break;
                        message.renderingMetadata = $root.proto.BotRenderingMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.renderingMetadata);
                        message._renderingMetadata = "renderingMetadata";
                        continue;
                    }
                case 17: {
                        if (u !== 2)
                            break;
                        message.botMetricsMetadata = $root.proto.BotMetricsMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.botMetricsMetadata);
                        message._botMetricsMetadata = "botMetricsMetadata";
                        continue;
                    }
                case 18: {
                        if (u !== 2)
                            break;
                        message.botLinkedAccountsMetadata = $root.proto.BotLinkedAccountsMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.botLinkedAccountsMetadata);
                        message._botLinkedAccountsMetadata = "botLinkedAccountsMetadata";
                        continue;
                    }
                case 19: {
                        if (u !== 2)
                            break;
                        message.richResponseSourcesMetadata = $root.proto.BotSourcesMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.richResponseSourcesMetadata);
                        message._richResponseSourcesMetadata = "richResponseSourcesMetadata";
                        continue;
                    }
                case 20: {
                        if (u !== 2)
                            break;
                        message.aiConversationContext = reader.bytes();
                        message._aiConversationContext = "aiConversationContext";
                        continue;
                    }
                case 21: {
                        if (u !== 2)
                            break;
                        message.botPromotionMessageMetadata = $root.proto.BotPromotionMessageMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.botPromotionMessageMetadata);
                        message._botPromotionMessageMetadata = "botPromotionMessageMetadata";
                        continue;
                    }
                case 22: {
                        if (u !== 2)
                            break;
                        message.botModeSelectionMetadata = $root.proto.BotModeSelectionMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.botModeSelectionMetadata);
                        message._botModeSelectionMetadata = "botModeSelectionMetadata";
                        continue;
                    }
                case 23: {
                        if (u !== 2)
                            break;
                        message.botQuotaMetadata = $root.proto.BotQuotaMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.botQuotaMetadata);
                        message._botQuotaMetadata = "botQuotaMetadata";
                        continue;
                    }
                case 24: {
                        if (u !== 2)
                            break;
                        message.botAgeCollectionMetadata = $root.proto.BotAgeCollectionMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.botAgeCollectionMetadata);
                        message._botAgeCollectionMetadata = "botAgeCollectionMetadata";
                        continue;
                    }
                case 25: {
                        if (u !== 2)
                            break;
                        message.conversationStarterPromptId = reader.stringVerify();
                        message._conversationStarterPromptId = "conversationStarterPromptId";
                        continue;
                    }
                case 26: {
                        if (u !== 2)
                            break;
                        message.botResponseId = reader.stringVerify();
                        message._botResponseId = "botResponseId";
                        continue;
                    }
                case 27: {
                        if (u !== 2)
                            break;
                        message.verificationMetadata = $root.proto.BotSignatureVerificationMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.verificationMetadata);
                        message._verificationMetadata = "verificationMetadata";
                        continue;
                    }
                case 28: {
                        if (u !== 2)
                            break;
                        message.unifiedResponseMutation = $root.proto.BotUnifiedResponseMutation.decode(reader, reader.uint32(), undefined, q + 1, message.unifiedResponseMutation);
                        message._unifiedResponseMutation = "unifiedResponseMutation";
                        continue;
                    }
                case 29: {
                        if (u !== 2)
                            break;
                        message.botMessageOriginMetadata = $root.proto.BotMessageOriginMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.botMessageOriginMetadata);
                        message._botMessageOriginMetadata = "botMessageOriginMetadata";
                        continue;
                    }
                case 30: {
                        if (u !== 2)
                            break;
                        message.inThreadSurveyMetadata = $root.proto.InThreadSurveyMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.inThreadSurveyMetadata);
                        message._inThreadSurveyMetadata = "inThreadSurveyMetadata";
                        continue;
                    }
                case 31: {
                        if (u !== 2)
                            break;
                        message.botThreadInfo = $root.proto.AIThreadInfo.decode(reader, reader.uint32(), undefined, q + 1, message.botThreadInfo);
                        message._botThreadInfo = "botThreadInfo";
                        continue;
                    }
                case 32: {
                        if (u !== 2)
                            break;
                        message.regenerateMetadata = $root.proto.AIRegenerateMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.regenerateMetadata);
                        message._regenerateMetadata = "regenerateMetadata";
                        continue;
                    }
                case 33: {
                        if (u !== 2)
                            break;
                        message.sessionTransparencyMetadata = $root.proto.SessionTransparencyMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.sessionTransparencyMetadata);
                        message._sessionTransparencyMetadata = "sessionTransparencyMetadata";
                        continue;
                    }
                case 34: {
                        if (u !== 2)
                            break;
                        message.botDocumentMessageMetadata = $root.proto.BotDocumentMessageMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.botDocumentMessageMetadata);
                        message._botDocumentMessageMetadata = "botDocumentMessageMetadata";
                        continue;
                    }
                case 35: {
                        if (u !== 2)
                            break;
                        message.botGroupMetadata = $root.proto.BotGroupMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.botGroupMetadata);
                        message._botGroupMetadata = "botGroupMetadata";
                        continue;
                    }
                case 36: {
                        if (u !== 2)
                            break;
                        message.botRenderingConfigMetadata = $root.proto.BotRenderingConfigMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.botRenderingConfigMetadata);
                        message._botRenderingConfigMetadata = "botRenderingConfigMetadata";
                        continue;
                    }
                case 37: {
                        if (u !== 2)
                            break;
                        message.botInfrastructureDiagnostics = $root.proto.BotInfrastructureDiagnostics.decode(reader, reader.uint32(), undefined, q + 1, message.botInfrastructureDiagnostics);
                        message._botInfrastructureDiagnostics = "botInfrastructureDiagnostics";
                        continue;
                    }
                case 38: {
                        if (u !== 2)
                            break;
                        message.aiMediaCollectionMetadata = $root.proto.AIMediaCollectionMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.aiMediaCollectionMetadata);
                        message._aiMediaCollectionMetadata = "aiMediaCollectionMetadata";
                        continue;
                    }
                case 39: {
                        if (u !== 2)
                            break;
                        message.commandMetadata = $root.proto.BotCommandMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.commandMetadata);
                        message._commandMetadata = "commandMetadata";
                        continue;
                    }
                case 40: {
                        if (u !== 2)
                            break;
                        message.resolvedToolCallMetadata = $root.proto.BotResolvedToolCallMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.resolvedToolCallMetadata);
                        message._resolvedToolCallMetadata = "resolvedToolCallMetadata";
                        continue;
                    }
                case 41: {
                        if (u !== 2)
                            break;
                        message.subscriptionUpsellMetadata = $root.proto.AISubscriptionUpsellMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.subscriptionUpsellMetadata);
                        message._subscriptionUpsellMetadata = "subscriptionUpsellMetadata";
                        continue;
                    }
                case 42: {
                        if (u !== 2)
                            break;
                        message.pttPromptMetadata = $root.proto.BotPttPromptMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.pttPromptMetadata);
                        message._pttPromptMetadata = "pttPromptMetadata";
                        continue;
                    }
                case 999: {
                        if (u !== 2)
                            break;
                        message.internalMetadata = reader.bytes();
                        message._internalMetadata = "internalMetadata";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotMetadata} BotMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotMetadata message.
         * @function verify
         * @memberof proto.BotMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.personaId != null && Object.hasOwnProperty.call(message, "personaId")) {
                properties._personaId = 1;
                if (!$util.isString(message.personaId))
                    return "personaId: string expected";
            }
            if (message.pluginMetadata != null && Object.hasOwnProperty.call(message, "pluginMetadata")) {
                properties._pluginMetadata = 1;
                {
                    var error = $root.proto.BotPluginMetadata.verify(message.pluginMetadata, q + 1);
                    if (error)
                        return "pluginMetadata." + error;
                }
            }
            if (message.suggestedPromptMetadata != null && Object.hasOwnProperty.call(message, "suggestedPromptMetadata")) {
                properties._suggestedPromptMetadata = 1;
                {
                    var error = $root.proto.BotSuggestedPromptMetadata.verify(message.suggestedPromptMetadata, q + 1);
                    if (error)
                        return "suggestedPromptMetadata." + error;
                }
            }
            if (message.invokerJid != null && Object.hasOwnProperty.call(message, "invokerJid")) {
                properties._invokerJid = 1;
                if (!$util.isString(message.invokerJid))
                    return "invokerJid: string expected";
            }
            if (message.sessionMetadata != null && Object.hasOwnProperty.call(message, "sessionMetadata")) {
                properties._sessionMetadata = 1;
                {
                    var error = $root.proto.BotSessionMetadata.verify(message.sessionMetadata, q + 1);
                    if (error)
                        return "sessionMetadata." + error;
                }
            }
            if (message.memuMetadata != null && Object.hasOwnProperty.call(message, "memuMetadata")) {
                properties._memuMetadata = 1;
                {
                    var error = $root.proto.BotMemuMetadata.verify(message.memuMetadata, q + 1);
                    if (error)
                        return "memuMetadata." + error;
                }
            }
            if (message.timezone != null && Object.hasOwnProperty.call(message, "timezone")) {
                properties._timezone = 1;
                if (!$util.isString(message.timezone))
                    return "timezone: string expected";
            }
            if (message.reminderMetadata != null && Object.hasOwnProperty.call(message, "reminderMetadata")) {
                properties._reminderMetadata = 1;
                {
                    var error = $root.proto.BotReminderMetadata.verify(message.reminderMetadata, q + 1);
                    if (error)
                        return "reminderMetadata." + error;
                }
            }
            if (message.modelMetadata != null && Object.hasOwnProperty.call(message, "modelMetadata")) {
                properties._modelMetadata = 1;
                {
                    var error = $root.proto.BotModelMetadata.verify(message.modelMetadata, q + 1);
                    if (error)
                        return "modelMetadata." + error;
                }
            }
            if (message.messageDisclaimerText != null && Object.hasOwnProperty.call(message, "messageDisclaimerText")) {
                properties._messageDisclaimerText = 1;
                if (!$util.isString(message.messageDisclaimerText))
                    return "messageDisclaimerText: string expected";
            }
            if (message.progressIndicatorMetadata != null && Object.hasOwnProperty.call(message, "progressIndicatorMetadata")) {
                properties._progressIndicatorMetadata = 1;
                {
                    var error = $root.proto.BotProgressIndicatorMetadata.verify(message.progressIndicatorMetadata, q + 1);
                    if (error)
                        return "progressIndicatorMetadata." + error;
                }
            }
            if (message.capabilityMetadata != null && Object.hasOwnProperty.call(message, "capabilityMetadata")) {
                properties._capabilityMetadata = 1;
                {
                    var error = $root.proto.BotCapabilityMetadata.verify(message.capabilityMetadata, q + 1);
                    if (error)
                        return "capabilityMetadata." + error;
                }
            }
            if (message.imagineMetadata != null && Object.hasOwnProperty.call(message, "imagineMetadata")) {
                properties._imagineMetadata = 1;
                {
                    var error = $root.proto.BotImagineMetadata.verify(message.imagineMetadata, q + 1);
                    if (error)
                        return "imagineMetadata." + error;
                }
            }
            if (message.memoryMetadata != null && Object.hasOwnProperty.call(message, "memoryMetadata")) {
                properties._memoryMetadata = 1;
                {
                    var error = $root.proto.BotMemoryMetadata.verify(message.memoryMetadata, q + 1);
                    if (error)
                        return "memoryMetadata." + error;
                }
            }
            if (message.renderingMetadata != null && Object.hasOwnProperty.call(message, "renderingMetadata")) {
                properties._renderingMetadata = 1;
                {
                    var error = $root.proto.BotRenderingMetadata.verify(message.renderingMetadata, q + 1);
                    if (error)
                        return "renderingMetadata." + error;
                }
            }
            if (message.botMetricsMetadata != null && Object.hasOwnProperty.call(message, "botMetricsMetadata")) {
                properties._botMetricsMetadata = 1;
                {
                    var error = $root.proto.BotMetricsMetadata.verify(message.botMetricsMetadata, q + 1);
                    if (error)
                        return "botMetricsMetadata." + error;
                }
            }
            if (message.botLinkedAccountsMetadata != null && Object.hasOwnProperty.call(message, "botLinkedAccountsMetadata")) {
                properties._botLinkedAccountsMetadata = 1;
                {
                    var error = $root.proto.BotLinkedAccountsMetadata.verify(message.botLinkedAccountsMetadata, q + 1);
                    if (error)
                        return "botLinkedAccountsMetadata." + error;
                }
            }
            if (message.richResponseSourcesMetadata != null && Object.hasOwnProperty.call(message, "richResponseSourcesMetadata")) {
                properties._richResponseSourcesMetadata = 1;
                {
                    var error = $root.proto.BotSourcesMetadata.verify(message.richResponseSourcesMetadata, q + 1);
                    if (error)
                        return "richResponseSourcesMetadata." + error;
                }
            }
            if (message.aiConversationContext != null && Object.hasOwnProperty.call(message, "aiConversationContext")) {
                properties._aiConversationContext = 1;
                if (!(message.aiConversationContext && typeof message.aiConversationContext.length === "number" || $util.isString(message.aiConversationContext)))
                    return "aiConversationContext: buffer expected";
            }
            if (message.botPromotionMessageMetadata != null && Object.hasOwnProperty.call(message, "botPromotionMessageMetadata")) {
                properties._botPromotionMessageMetadata = 1;
                {
                    var error = $root.proto.BotPromotionMessageMetadata.verify(message.botPromotionMessageMetadata, q + 1);
                    if (error)
                        return "botPromotionMessageMetadata." + error;
                }
            }
            if (message.botModeSelectionMetadata != null && Object.hasOwnProperty.call(message, "botModeSelectionMetadata")) {
                properties._botModeSelectionMetadata = 1;
                {
                    var error = $root.proto.BotModeSelectionMetadata.verify(message.botModeSelectionMetadata, q + 1);
                    if (error)
                        return "botModeSelectionMetadata." + error;
                }
            }
            if (message.botQuotaMetadata != null && Object.hasOwnProperty.call(message, "botQuotaMetadata")) {
                properties._botQuotaMetadata = 1;
                {
                    var error = $root.proto.BotQuotaMetadata.verify(message.botQuotaMetadata, q + 1);
                    if (error)
                        return "botQuotaMetadata." + error;
                }
            }
            if (message.botAgeCollectionMetadata != null && Object.hasOwnProperty.call(message, "botAgeCollectionMetadata")) {
                properties._botAgeCollectionMetadata = 1;
                {
                    var error = $root.proto.BotAgeCollectionMetadata.verify(message.botAgeCollectionMetadata, q + 1);
                    if (error)
                        return "botAgeCollectionMetadata." + error;
                }
            }
            if (message.conversationStarterPromptId != null && Object.hasOwnProperty.call(message, "conversationStarterPromptId")) {
                properties._conversationStarterPromptId = 1;
                if (!$util.isString(message.conversationStarterPromptId))
                    return "conversationStarterPromptId: string expected";
            }
            if (message.botResponseId != null && Object.hasOwnProperty.call(message, "botResponseId")) {
                properties._botResponseId = 1;
                if (!$util.isString(message.botResponseId))
                    return "botResponseId: string expected";
            }
            if (message.verificationMetadata != null && Object.hasOwnProperty.call(message, "verificationMetadata")) {
                properties._verificationMetadata = 1;
                {
                    var error = $root.proto.BotSignatureVerificationMetadata.verify(message.verificationMetadata, q + 1);
                    if (error)
                        return "verificationMetadata." + error;
                }
            }
            if (message.unifiedResponseMutation != null && Object.hasOwnProperty.call(message, "unifiedResponseMutation")) {
                properties._unifiedResponseMutation = 1;
                {
                    var error = $root.proto.BotUnifiedResponseMutation.verify(message.unifiedResponseMutation, q + 1);
                    if (error)
                        return "unifiedResponseMutation." + error;
                }
            }
            if (message.botMessageOriginMetadata != null && Object.hasOwnProperty.call(message, "botMessageOriginMetadata")) {
                properties._botMessageOriginMetadata = 1;
                {
                    var error = $root.proto.BotMessageOriginMetadata.verify(message.botMessageOriginMetadata, q + 1);
                    if (error)
                        return "botMessageOriginMetadata." + error;
                }
            }
            if (message.inThreadSurveyMetadata != null && Object.hasOwnProperty.call(message, "inThreadSurveyMetadata")) {
                properties._inThreadSurveyMetadata = 1;
                {
                    var error = $root.proto.InThreadSurveyMetadata.verify(message.inThreadSurveyMetadata, q + 1);
                    if (error)
                        return "inThreadSurveyMetadata." + error;
                }
            }
            if (message.botThreadInfo != null && Object.hasOwnProperty.call(message, "botThreadInfo")) {
                properties._botThreadInfo = 1;
                {
                    var error = $root.proto.AIThreadInfo.verify(message.botThreadInfo, q + 1);
                    if (error)
                        return "botThreadInfo." + error;
                }
            }
            if (message.regenerateMetadata != null && Object.hasOwnProperty.call(message, "regenerateMetadata")) {
                properties._regenerateMetadata = 1;
                {
                    var error = $root.proto.AIRegenerateMetadata.verify(message.regenerateMetadata, q + 1);
                    if (error)
                        return "regenerateMetadata." + error;
                }
            }
            if (message.sessionTransparencyMetadata != null && Object.hasOwnProperty.call(message, "sessionTransparencyMetadata")) {
                properties._sessionTransparencyMetadata = 1;
                {
                    var error = $root.proto.SessionTransparencyMetadata.verify(message.sessionTransparencyMetadata, q + 1);
                    if (error)
                        return "sessionTransparencyMetadata." + error;
                }
            }
            if (message.botDocumentMessageMetadata != null && Object.hasOwnProperty.call(message, "botDocumentMessageMetadata")) {
                properties._botDocumentMessageMetadata = 1;
                {
                    var error = $root.proto.BotDocumentMessageMetadata.verify(message.botDocumentMessageMetadata, q + 1);
                    if (error)
                        return "botDocumentMessageMetadata." + error;
                }
            }
            if (message.botGroupMetadata != null && Object.hasOwnProperty.call(message, "botGroupMetadata")) {
                properties._botGroupMetadata = 1;
                {
                    var error = $root.proto.BotGroupMetadata.verify(message.botGroupMetadata, q + 1);
                    if (error)
                        return "botGroupMetadata." + error;
                }
            }
            if (message.botRenderingConfigMetadata != null && Object.hasOwnProperty.call(message, "botRenderingConfigMetadata")) {
                properties._botRenderingConfigMetadata = 1;
                {
                    var error = $root.proto.BotRenderingConfigMetadata.verify(message.botRenderingConfigMetadata, q + 1);
                    if (error)
                        return "botRenderingConfigMetadata." + error;
                }
            }
            if (message.botInfrastructureDiagnostics != null && Object.hasOwnProperty.call(message, "botInfrastructureDiagnostics")) {
                properties._botInfrastructureDiagnostics = 1;
                {
                    var error = $root.proto.BotInfrastructureDiagnostics.verify(message.botInfrastructureDiagnostics, q + 1);
                    if (error)
                        return "botInfrastructureDiagnostics." + error;
                }
            }
            if (message.aiMediaCollectionMetadata != null && Object.hasOwnProperty.call(message, "aiMediaCollectionMetadata")) {
                properties._aiMediaCollectionMetadata = 1;
                {
                    var error = $root.proto.AIMediaCollectionMetadata.verify(message.aiMediaCollectionMetadata, q + 1);
                    if (error)
                        return "aiMediaCollectionMetadata." + error;
                }
            }
            if (message.commandMetadata != null && Object.hasOwnProperty.call(message, "commandMetadata")) {
                properties._commandMetadata = 1;
                {
                    var error = $root.proto.BotCommandMetadata.verify(message.commandMetadata, q + 1);
                    if (error)
                        return "commandMetadata." + error;
                }
            }
            if (message.resolvedToolCallMetadata != null && Object.hasOwnProperty.call(message, "resolvedToolCallMetadata")) {
                properties._resolvedToolCallMetadata = 1;
                {
                    var error = $root.proto.BotResolvedToolCallMetadata.verify(message.resolvedToolCallMetadata, q + 1);
                    if (error)
                        return "resolvedToolCallMetadata." + error;
                }
            }
            if (message.subscriptionUpsellMetadata != null && Object.hasOwnProperty.call(message, "subscriptionUpsellMetadata")) {
                properties._subscriptionUpsellMetadata = 1;
                {
                    var error = $root.proto.AISubscriptionUpsellMetadata.verify(message.subscriptionUpsellMetadata, q + 1);
                    if (error)
                        return "subscriptionUpsellMetadata." + error;
                }
            }
            if (message.pttPromptMetadata != null && Object.hasOwnProperty.call(message, "pttPromptMetadata")) {
                properties._pttPromptMetadata = 1;
                {
                    var error = $root.proto.BotPttPromptMetadata.verify(message.pttPromptMetadata, q + 1);
                    if (error)
                        return "pttPromptMetadata." + error;
                }
            }
            if (message.internalMetadata != null && Object.hasOwnProperty.call(message, "internalMetadata")) {
                properties._internalMetadata = 1;
                if (!(message.internalMetadata && typeof message.internalMetadata.length === "number" || $util.isString(message.internalMetadata)))
                    return "internalMetadata: buffer expected";
            }
            return null;
        };

        /**
         * Creates a BotMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotMetadata} BotMetadata
         */
        BotMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.personaId != null)
                message.personaId = String(object.personaId);
            if (object.pluginMetadata != null) {
                if (!$util.isObject(object.pluginMetadata))
                    throw TypeError(".proto.BotMetadata.pluginMetadata: object expected");
                message.pluginMetadata = $root.proto.BotPluginMetadata.fromObject(object.pluginMetadata, q + 1);
            }
            if (object.suggestedPromptMetadata != null) {
                if (!$util.isObject(object.suggestedPromptMetadata))
                    throw TypeError(".proto.BotMetadata.suggestedPromptMetadata: object expected");
                message.suggestedPromptMetadata = $root.proto.BotSuggestedPromptMetadata.fromObject(object.suggestedPromptMetadata, q + 1);
            }
            if (object.invokerJid != null)
                message.invokerJid = String(object.invokerJid);
            if (object.sessionMetadata != null) {
                if (!$util.isObject(object.sessionMetadata))
                    throw TypeError(".proto.BotMetadata.sessionMetadata: object expected");
                message.sessionMetadata = $root.proto.BotSessionMetadata.fromObject(object.sessionMetadata, q + 1);
            }
            if (object.memuMetadata != null) {
                if (!$util.isObject(object.memuMetadata))
                    throw TypeError(".proto.BotMetadata.memuMetadata: object expected");
                message.memuMetadata = $root.proto.BotMemuMetadata.fromObject(object.memuMetadata, q + 1);
            }
            if (object.timezone != null)
                message.timezone = String(object.timezone);
            if (object.reminderMetadata != null) {
                if (!$util.isObject(object.reminderMetadata))
                    throw TypeError(".proto.BotMetadata.reminderMetadata: object expected");
                message.reminderMetadata = $root.proto.BotReminderMetadata.fromObject(object.reminderMetadata, q + 1);
            }
            if (object.modelMetadata != null) {
                if (!$util.isObject(object.modelMetadata))
                    throw TypeError(".proto.BotMetadata.modelMetadata: object expected");
                message.modelMetadata = $root.proto.BotModelMetadata.fromObject(object.modelMetadata, q + 1);
            }
            if (object.messageDisclaimerText != null)
                message.messageDisclaimerText = String(object.messageDisclaimerText);
            if (object.progressIndicatorMetadata != null) {
                if (!$util.isObject(object.progressIndicatorMetadata))
                    throw TypeError(".proto.BotMetadata.progressIndicatorMetadata: object expected");
                message.progressIndicatorMetadata = $root.proto.BotProgressIndicatorMetadata.fromObject(object.progressIndicatorMetadata, q + 1);
            }
            if (object.capabilityMetadata != null) {
                if (!$util.isObject(object.capabilityMetadata))
                    throw TypeError(".proto.BotMetadata.capabilityMetadata: object expected");
                message.capabilityMetadata = $root.proto.BotCapabilityMetadata.fromObject(object.capabilityMetadata, q + 1);
            }
            if (object.imagineMetadata != null) {
                if (!$util.isObject(object.imagineMetadata))
                    throw TypeError(".proto.BotMetadata.imagineMetadata: object expected");
                message.imagineMetadata = $root.proto.BotImagineMetadata.fromObject(object.imagineMetadata, q + 1);
            }
            if (object.memoryMetadata != null) {
                if (!$util.isObject(object.memoryMetadata))
                    throw TypeError(".proto.BotMetadata.memoryMetadata: object expected");
                message.memoryMetadata = $root.proto.BotMemoryMetadata.fromObject(object.memoryMetadata, q + 1);
            }
            if (object.renderingMetadata != null) {
                if (!$util.isObject(object.renderingMetadata))
                    throw TypeError(".proto.BotMetadata.renderingMetadata: object expected");
                message.renderingMetadata = $root.proto.BotRenderingMetadata.fromObject(object.renderingMetadata, q + 1);
            }
            if (object.botMetricsMetadata != null) {
                if (!$util.isObject(object.botMetricsMetadata))
                    throw TypeError(".proto.BotMetadata.botMetricsMetadata: object expected");
                message.botMetricsMetadata = $root.proto.BotMetricsMetadata.fromObject(object.botMetricsMetadata, q + 1);
            }
            if (object.botLinkedAccountsMetadata != null) {
                if (!$util.isObject(object.botLinkedAccountsMetadata))
                    throw TypeError(".proto.BotMetadata.botLinkedAccountsMetadata: object expected");
                message.botLinkedAccountsMetadata = $root.proto.BotLinkedAccountsMetadata.fromObject(object.botLinkedAccountsMetadata, q + 1);
            }
            if (object.richResponseSourcesMetadata != null) {
                if (!$util.isObject(object.richResponseSourcesMetadata))
                    throw TypeError(".proto.BotMetadata.richResponseSourcesMetadata: object expected");
                message.richResponseSourcesMetadata = $root.proto.BotSourcesMetadata.fromObject(object.richResponseSourcesMetadata, q + 1);
            }
            if (object.aiConversationContext != null)
                if (typeof object.aiConversationContext === "string")
                    $util.base64.decode(object.aiConversationContext, message.aiConversationContext = $util.newBuffer($util.base64.length(object.aiConversationContext)), 0);
                else if (object.aiConversationContext.length >= 0)
                    message.aiConversationContext = object.aiConversationContext;
            if (object.botPromotionMessageMetadata != null) {
                if (!$util.isObject(object.botPromotionMessageMetadata))
                    throw TypeError(".proto.BotMetadata.botPromotionMessageMetadata: object expected");
                message.botPromotionMessageMetadata = $root.proto.BotPromotionMessageMetadata.fromObject(object.botPromotionMessageMetadata, q + 1);
            }
            if (object.botModeSelectionMetadata != null) {
                if (!$util.isObject(object.botModeSelectionMetadata))
                    throw TypeError(".proto.BotMetadata.botModeSelectionMetadata: object expected");
                message.botModeSelectionMetadata = $root.proto.BotModeSelectionMetadata.fromObject(object.botModeSelectionMetadata, q + 1);
            }
            if (object.botQuotaMetadata != null) {
                if (!$util.isObject(object.botQuotaMetadata))
                    throw TypeError(".proto.BotMetadata.botQuotaMetadata: object expected");
                message.botQuotaMetadata = $root.proto.BotQuotaMetadata.fromObject(object.botQuotaMetadata, q + 1);
            }
            if (object.botAgeCollectionMetadata != null) {
                if (!$util.isObject(object.botAgeCollectionMetadata))
                    throw TypeError(".proto.BotMetadata.botAgeCollectionMetadata: object expected");
                message.botAgeCollectionMetadata = $root.proto.BotAgeCollectionMetadata.fromObject(object.botAgeCollectionMetadata, q + 1);
            }
            if (object.conversationStarterPromptId != null)
                message.conversationStarterPromptId = String(object.conversationStarterPromptId);
            if (object.botResponseId != null)
                message.botResponseId = String(object.botResponseId);
            if (object.verificationMetadata != null) {
                if (!$util.isObject(object.verificationMetadata))
                    throw TypeError(".proto.BotMetadata.verificationMetadata: object expected");
                message.verificationMetadata = $root.proto.BotSignatureVerificationMetadata.fromObject(object.verificationMetadata, q + 1);
            }
            if (object.unifiedResponseMutation != null) {
                if (!$util.isObject(object.unifiedResponseMutation))
                    throw TypeError(".proto.BotMetadata.unifiedResponseMutation: object expected");
                message.unifiedResponseMutation = $root.proto.BotUnifiedResponseMutation.fromObject(object.unifiedResponseMutation, q + 1);
            }
            if (object.botMessageOriginMetadata != null) {
                if (!$util.isObject(object.botMessageOriginMetadata))
                    throw TypeError(".proto.BotMetadata.botMessageOriginMetadata: object expected");
                message.botMessageOriginMetadata = $root.proto.BotMessageOriginMetadata.fromObject(object.botMessageOriginMetadata, q + 1);
            }
            if (object.inThreadSurveyMetadata != null) {
                if (!$util.isObject(object.inThreadSurveyMetadata))
                    throw TypeError(".proto.BotMetadata.inThreadSurveyMetadata: object expected");
                message.inThreadSurveyMetadata = $root.proto.InThreadSurveyMetadata.fromObject(object.inThreadSurveyMetadata, q + 1);
            }
            if (object.botThreadInfo != null) {
                if (!$util.isObject(object.botThreadInfo))
                    throw TypeError(".proto.BotMetadata.botThreadInfo: object expected");
                message.botThreadInfo = $root.proto.AIThreadInfo.fromObject(object.botThreadInfo, q + 1);
            }
            if (object.regenerateMetadata != null) {
                if (!$util.isObject(object.regenerateMetadata))
                    throw TypeError(".proto.BotMetadata.regenerateMetadata: object expected");
                message.regenerateMetadata = $root.proto.AIRegenerateMetadata.fromObject(object.regenerateMetadata, q + 1);
            }
            if (object.sessionTransparencyMetadata != null) {
                if (!$util.isObject(object.sessionTransparencyMetadata))
                    throw TypeError(".proto.BotMetadata.sessionTransparencyMetadata: object expected");
                message.sessionTransparencyMetadata = $root.proto.SessionTransparencyMetadata.fromObject(object.sessionTransparencyMetadata, q + 1);
            }
            if (object.botDocumentMessageMetadata != null) {
                if (!$util.isObject(object.botDocumentMessageMetadata))
                    throw TypeError(".proto.BotMetadata.botDocumentMessageMetadata: object expected");
                message.botDocumentMessageMetadata = $root.proto.BotDocumentMessageMetadata.fromObject(object.botDocumentMessageMetadata, q + 1);
            }
            if (object.botGroupMetadata != null) {
                if (!$util.isObject(object.botGroupMetadata))
                    throw TypeError(".proto.BotMetadata.botGroupMetadata: object expected");
                message.botGroupMetadata = $root.proto.BotGroupMetadata.fromObject(object.botGroupMetadata, q + 1);
            }
            if (object.botRenderingConfigMetadata != null) {
                if (!$util.isObject(object.botRenderingConfigMetadata))
                    throw TypeError(".proto.BotMetadata.botRenderingConfigMetadata: object expected");
                message.botRenderingConfigMetadata = $root.proto.BotRenderingConfigMetadata.fromObject(object.botRenderingConfigMetadata, q + 1);
            }
            if (object.botInfrastructureDiagnostics != null) {
                if (!$util.isObject(object.botInfrastructureDiagnostics))
                    throw TypeError(".proto.BotMetadata.botInfrastructureDiagnostics: object expected");
                message.botInfrastructureDiagnostics = $root.proto.BotInfrastructureDiagnostics.fromObject(object.botInfrastructureDiagnostics, q + 1);
            }
            if (object.aiMediaCollectionMetadata != null) {
                if (!$util.isObject(object.aiMediaCollectionMetadata))
                    throw TypeError(".proto.BotMetadata.aiMediaCollectionMetadata: object expected");
                message.aiMediaCollectionMetadata = $root.proto.AIMediaCollectionMetadata.fromObject(object.aiMediaCollectionMetadata, q + 1);
            }
            if (object.commandMetadata != null) {
                if (!$util.isObject(object.commandMetadata))
                    throw TypeError(".proto.BotMetadata.commandMetadata: object expected");
                message.commandMetadata = $root.proto.BotCommandMetadata.fromObject(object.commandMetadata, q + 1);
            }
            if (object.resolvedToolCallMetadata != null) {
                if (!$util.isObject(object.resolvedToolCallMetadata))
                    throw TypeError(".proto.BotMetadata.resolvedToolCallMetadata: object expected");
                message.resolvedToolCallMetadata = $root.proto.BotResolvedToolCallMetadata.fromObject(object.resolvedToolCallMetadata, q + 1);
            }
            if (object.subscriptionUpsellMetadata != null) {
                if (!$util.isObject(object.subscriptionUpsellMetadata))
                    throw TypeError(".proto.BotMetadata.subscriptionUpsellMetadata: object expected");
                message.subscriptionUpsellMetadata = $root.proto.AISubscriptionUpsellMetadata.fromObject(object.subscriptionUpsellMetadata, q + 1);
            }
            if (object.pttPromptMetadata != null) {
                if (!$util.isObject(object.pttPromptMetadata))
                    throw TypeError(".proto.BotMetadata.pttPromptMetadata: object expected");
                message.pttPromptMetadata = $root.proto.BotPttPromptMetadata.fromObject(object.pttPromptMetadata, q + 1);
            }
            if (object.internalMetadata != null)
                if (typeof object.internalMetadata === "string")
                    $util.base64.decode(object.internalMetadata, message.internalMetadata = $util.newBuffer($util.base64.length(object.internalMetadata)), 0);
                else if (object.internalMetadata.length >= 0)
                    message.internalMetadata = object.internalMetadata;
            return message;
        };

        /**
         * Creates a plain object from a BotMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotMetadata
         * @static
         * @param {proto.BotMetadata} message BotMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.personaId != null && Object.hasOwnProperty.call(message, "personaId"))
                object.personaId = message.personaId;
            if (message.pluginMetadata != null && Object.hasOwnProperty.call(message, "pluginMetadata"))
                object.pluginMetadata = $root.proto.BotPluginMetadata.toObject(message.pluginMetadata, options, q + 1);
            if (message.suggestedPromptMetadata != null && Object.hasOwnProperty.call(message, "suggestedPromptMetadata"))
                object.suggestedPromptMetadata = $root.proto.BotSuggestedPromptMetadata.toObject(message.suggestedPromptMetadata, options, q + 1);
            if (message.invokerJid != null && Object.hasOwnProperty.call(message, "invokerJid"))
                object.invokerJid = message.invokerJid;
            if (message.sessionMetadata != null && Object.hasOwnProperty.call(message, "sessionMetadata"))
                object.sessionMetadata = $root.proto.BotSessionMetadata.toObject(message.sessionMetadata, options, q + 1);
            if (message.memuMetadata != null && Object.hasOwnProperty.call(message, "memuMetadata"))
                object.memuMetadata = $root.proto.BotMemuMetadata.toObject(message.memuMetadata, options, q + 1);
            if (message.timezone != null && Object.hasOwnProperty.call(message, "timezone"))
                object.timezone = message.timezone;
            if (message.reminderMetadata != null && Object.hasOwnProperty.call(message, "reminderMetadata"))
                object.reminderMetadata = $root.proto.BotReminderMetadata.toObject(message.reminderMetadata, options, q + 1);
            if (message.modelMetadata != null && Object.hasOwnProperty.call(message, "modelMetadata"))
                object.modelMetadata = $root.proto.BotModelMetadata.toObject(message.modelMetadata, options, q + 1);
            if (message.messageDisclaimerText != null && Object.hasOwnProperty.call(message, "messageDisclaimerText"))
                object.messageDisclaimerText = message.messageDisclaimerText;
            if (message.progressIndicatorMetadata != null && Object.hasOwnProperty.call(message, "progressIndicatorMetadata"))
                object.progressIndicatorMetadata = $root.proto.BotProgressIndicatorMetadata.toObject(message.progressIndicatorMetadata, options, q + 1);
            if (message.capabilityMetadata != null && Object.hasOwnProperty.call(message, "capabilityMetadata"))
                object.capabilityMetadata = $root.proto.BotCapabilityMetadata.toObject(message.capabilityMetadata, options, q + 1);
            if (message.imagineMetadata != null && Object.hasOwnProperty.call(message, "imagineMetadata"))
                object.imagineMetadata = $root.proto.BotImagineMetadata.toObject(message.imagineMetadata, options, q + 1);
            if (message.memoryMetadata != null && Object.hasOwnProperty.call(message, "memoryMetadata"))
                object.memoryMetadata = $root.proto.BotMemoryMetadata.toObject(message.memoryMetadata, options, q + 1);
            if (message.renderingMetadata != null && Object.hasOwnProperty.call(message, "renderingMetadata"))
                object.renderingMetadata = $root.proto.BotRenderingMetadata.toObject(message.renderingMetadata, options, q + 1);
            if (message.botMetricsMetadata != null && Object.hasOwnProperty.call(message, "botMetricsMetadata"))
                object.botMetricsMetadata = $root.proto.BotMetricsMetadata.toObject(message.botMetricsMetadata, options, q + 1);
            if (message.botLinkedAccountsMetadata != null && Object.hasOwnProperty.call(message, "botLinkedAccountsMetadata"))
                object.botLinkedAccountsMetadata = $root.proto.BotLinkedAccountsMetadata.toObject(message.botLinkedAccountsMetadata, options, q + 1);
            if (message.richResponseSourcesMetadata != null && Object.hasOwnProperty.call(message, "richResponseSourcesMetadata"))
                object.richResponseSourcesMetadata = $root.proto.BotSourcesMetadata.toObject(message.richResponseSourcesMetadata, options, q + 1);
            if (message.aiConversationContext != null && Object.hasOwnProperty.call(message, "aiConversationContext"))
                object.aiConversationContext = options.bytes === String ? $util.base64.encode(message.aiConversationContext, 0, message.aiConversationContext.length) : options.bytes === Array ? Array.prototype.slice.call(message.aiConversationContext) : message.aiConversationContext;
            if (message.botPromotionMessageMetadata != null && Object.hasOwnProperty.call(message, "botPromotionMessageMetadata"))
                object.botPromotionMessageMetadata = $root.proto.BotPromotionMessageMetadata.toObject(message.botPromotionMessageMetadata, options, q + 1);
            if (message.botModeSelectionMetadata != null && Object.hasOwnProperty.call(message, "botModeSelectionMetadata"))
                object.botModeSelectionMetadata = $root.proto.BotModeSelectionMetadata.toObject(message.botModeSelectionMetadata, options, q + 1);
            if (message.botQuotaMetadata != null && Object.hasOwnProperty.call(message, "botQuotaMetadata"))
                object.botQuotaMetadata = $root.proto.BotQuotaMetadata.toObject(message.botQuotaMetadata, options, q + 1);
            if (message.botAgeCollectionMetadata != null && Object.hasOwnProperty.call(message, "botAgeCollectionMetadata"))
                object.botAgeCollectionMetadata = $root.proto.BotAgeCollectionMetadata.toObject(message.botAgeCollectionMetadata, options, q + 1);
            if (message.conversationStarterPromptId != null && Object.hasOwnProperty.call(message, "conversationStarterPromptId"))
                object.conversationStarterPromptId = message.conversationStarterPromptId;
            if (message.botResponseId != null && Object.hasOwnProperty.call(message, "botResponseId"))
                object.botResponseId = message.botResponseId;
            if (message.verificationMetadata != null && Object.hasOwnProperty.call(message, "verificationMetadata"))
                object.verificationMetadata = $root.proto.BotSignatureVerificationMetadata.toObject(message.verificationMetadata, options, q + 1);
            if (message.unifiedResponseMutation != null && Object.hasOwnProperty.call(message, "unifiedResponseMutation"))
                object.unifiedResponseMutation = $root.proto.BotUnifiedResponseMutation.toObject(message.unifiedResponseMutation, options, q + 1);
            if (message.botMessageOriginMetadata != null && Object.hasOwnProperty.call(message, "botMessageOriginMetadata"))
                object.botMessageOriginMetadata = $root.proto.BotMessageOriginMetadata.toObject(message.botMessageOriginMetadata, options, q + 1);
            if (message.inThreadSurveyMetadata != null && Object.hasOwnProperty.call(message, "inThreadSurveyMetadata"))
                object.inThreadSurveyMetadata = $root.proto.InThreadSurveyMetadata.toObject(message.inThreadSurveyMetadata, options, q + 1);
            if (message.botThreadInfo != null && Object.hasOwnProperty.call(message, "botThreadInfo"))
                object.botThreadInfo = $root.proto.AIThreadInfo.toObject(message.botThreadInfo, options, q + 1);
            if (message.regenerateMetadata != null && Object.hasOwnProperty.call(message, "regenerateMetadata"))
                object.regenerateMetadata = $root.proto.AIRegenerateMetadata.toObject(message.regenerateMetadata, options, q + 1);
            if (message.sessionTransparencyMetadata != null && Object.hasOwnProperty.call(message, "sessionTransparencyMetadata"))
                object.sessionTransparencyMetadata = $root.proto.SessionTransparencyMetadata.toObject(message.sessionTransparencyMetadata, options, q + 1);
            if (message.botDocumentMessageMetadata != null && Object.hasOwnProperty.call(message, "botDocumentMessageMetadata"))
                object.botDocumentMessageMetadata = $root.proto.BotDocumentMessageMetadata.toObject(message.botDocumentMessageMetadata, options, q + 1);
            if (message.botGroupMetadata != null && Object.hasOwnProperty.call(message, "botGroupMetadata"))
                object.botGroupMetadata = $root.proto.BotGroupMetadata.toObject(message.botGroupMetadata, options, q + 1);
            if (message.botRenderingConfigMetadata != null && Object.hasOwnProperty.call(message, "botRenderingConfigMetadata"))
                object.botRenderingConfigMetadata = $root.proto.BotRenderingConfigMetadata.toObject(message.botRenderingConfigMetadata, options, q + 1);
            if (message.botInfrastructureDiagnostics != null && Object.hasOwnProperty.call(message, "botInfrastructureDiagnostics"))
                object.botInfrastructureDiagnostics = $root.proto.BotInfrastructureDiagnostics.toObject(message.botInfrastructureDiagnostics, options, q + 1);
            if (message.aiMediaCollectionMetadata != null && Object.hasOwnProperty.call(message, "aiMediaCollectionMetadata"))
                object.aiMediaCollectionMetadata = $root.proto.AIMediaCollectionMetadata.toObject(message.aiMediaCollectionMetadata, options, q + 1);
            if (message.commandMetadata != null && Object.hasOwnProperty.call(message, "commandMetadata"))
                object.commandMetadata = $root.proto.BotCommandMetadata.toObject(message.commandMetadata, options, q + 1);
            if (message.resolvedToolCallMetadata != null && Object.hasOwnProperty.call(message, "resolvedToolCallMetadata"))
                object.resolvedToolCallMetadata = $root.proto.BotResolvedToolCallMetadata.toObject(message.resolvedToolCallMetadata, options, q + 1);
            if (message.subscriptionUpsellMetadata != null && Object.hasOwnProperty.call(message, "subscriptionUpsellMetadata"))
                object.subscriptionUpsellMetadata = $root.proto.AISubscriptionUpsellMetadata.toObject(message.subscriptionUpsellMetadata, options, q + 1);
            if (message.pttPromptMetadata != null && Object.hasOwnProperty.call(message, "pttPromptMetadata"))
                object.pttPromptMetadata = $root.proto.BotPttPromptMetadata.toObject(message.pttPromptMetadata, options, q + 1);
            if (message.internalMetadata != null && Object.hasOwnProperty.call(message, "internalMetadata"))
                object.internalMetadata = options.bytes === String ? $util.base64.encode(message.internalMetadata, 0, message.internalMetadata.length) : options.bytes === Array ? Array.prototype.slice.call(message.internalMetadata) : message.internalMetadata;
            return object;
        };

        /**
         * Converts this BotMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotMetadata
         * @function getTypeUrl
         * @memberof proto.BotMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotMetadata";
        };

        return BotMetadata;
    })();

    /**
     * BotMetricsEntryPoint enum.
     * @name proto.BotMetricsEntryPoint
     * @enum {number}
     * @property {number} UNDEFINED_ENTRY_POINT=0 UNDEFINED_ENTRY_POINT value
     * @property {number} FAVICON=1 FAVICON value
     * @property {number} CHATLIST=2 CHATLIST value
     * @property {number} AISEARCH_NULL_STATE_PAPER_PLANE=3 AISEARCH_NULL_STATE_PAPER_PLANE value
     * @property {number} AISEARCH_NULL_STATE_SUGGESTION=4 AISEARCH_NULL_STATE_SUGGESTION value
     * @property {number} AISEARCH_TYPE_AHEAD_SUGGESTION=5 AISEARCH_TYPE_AHEAD_SUGGESTION value
     * @property {number} AISEARCH_TYPE_AHEAD_PAPER_PLANE=6 AISEARCH_TYPE_AHEAD_PAPER_PLANE value
     * @property {number} AISEARCH_TYPE_AHEAD_RESULT_CHATLIST=7 AISEARCH_TYPE_AHEAD_RESULT_CHATLIST value
     * @property {number} AISEARCH_TYPE_AHEAD_RESULT_MESSAGES=8 AISEARCH_TYPE_AHEAD_RESULT_MESSAGES value
     * @property {number} AIVOICE_SEARCH_BAR=9 AIVOICE_SEARCH_BAR value
     * @property {number} AIVOICE_FAVICON=10 AIVOICE_FAVICON value
     * @property {number} AISTUDIO=11 AISTUDIO value
     * @property {number} DEEPLINK=12 DEEPLINK value
     * @property {number} NOTIFICATION=13 NOTIFICATION value
     * @property {number} PROFILE_MESSAGE_BUTTON=14 PROFILE_MESSAGE_BUTTON value
     * @property {number} FORWARD=15 FORWARD value
     * @property {number} APP_SHORTCUT=16 APP_SHORTCUT value
     * @property {number} FF_FAMILY=17 FF_FAMILY value
     * @property {number} AI_TAB=18 AI_TAB value
     * @property {number} AI_HOME=19 AI_HOME value
     * @property {number} AI_DEEPLINK_IMMERSIVE=20 AI_DEEPLINK_IMMERSIVE value
     * @property {number} AI_DEEPLINK=21 AI_DEEPLINK value
     * @property {number} META_AI_CHAT_SHORTCUT_AI_STUDIO=22 META_AI_CHAT_SHORTCUT_AI_STUDIO value
     * @property {number} UGC_CHAT_SHORTCUT_AI_STUDIO=23 UGC_CHAT_SHORTCUT_AI_STUDIO value
     * @property {number} NEW_CHAT_AI_STUDIO=24 NEW_CHAT_AI_STUDIO value
     * @property {number} AIVOICE_FAVICON_CALL_HISTORY=25 AIVOICE_FAVICON_CALL_HISTORY value
     * @property {number} ASK_META_AI_CONTEXT_MENU=26 ASK_META_AI_CONTEXT_MENU value
     * @property {number} ASK_META_AI_CONTEXT_MENU_1ON1=27 ASK_META_AI_CONTEXT_MENU_1ON1 value
     * @property {number} ASK_META_AI_CONTEXT_MENU_GROUP=28 ASK_META_AI_CONTEXT_MENU_GROUP value
     * @property {number} INVOKE_META_AI_1ON1=29 INVOKE_META_AI_1ON1 value
     * @property {number} INVOKE_META_AI_GROUP=30 INVOKE_META_AI_GROUP value
     * @property {number} META_AI_FORWARD=31 META_AI_FORWARD value
     * @property {number} NEW_CHAT_AI_CONTACT=32 NEW_CHAT_AI_CONTACT value
     * @property {number} MESSAGE_QUICK_ACTION_1_ON_1_CHAT=33 MESSAGE_QUICK_ACTION_1_ON_1_CHAT value
     * @property {number} MESSAGE_QUICK_ACTION_GROUP_CHAT=34 MESSAGE_QUICK_ACTION_GROUP_CHAT value
     * @property {number} ATTACHMENT_TRAY_1_ON_1_CHAT=35 ATTACHMENT_TRAY_1_ON_1_CHAT value
     * @property {number} ATTACHMENT_TRAY_GROUP_CHAT=36 ATTACHMENT_TRAY_GROUP_CHAT value
     * @property {number} ASK_META_AI_MEDIA_VIEWER_1ON1=37 ASK_META_AI_MEDIA_VIEWER_1ON1 value
     * @property {number} ASK_META_AI_MEDIA_VIEWER_GROUP=38 ASK_META_AI_MEDIA_VIEWER_GROUP value
     * @property {number} MEDIA_PICKER_1_ON_1_CHAT=39 MEDIA_PICKER_1_ON_1_CHAT value
     * @property {number} MEDIA_PICKER_GROUP_CHAT=40 MEDIA_PICKER_GROUP_CHAT value
     * @property {number} ASK_META_AI_NO_SEARCH_RESULTS=41 ASK_META_AI_NO_SEARCH_RESULTS value
     * @property {number} META_AI_SETTINGS=45 META_AI_SETTINGS value
     * @property {number} WEB_INTRO_PANEL=46 WEB_INTRO_PANEL value
     * @property {number} WEB_NAVIGATION_BAR=47 WEB_NAVIGATION_BAR value
     * @property {number} GROUP_MEMBER=54 GROUP_MEMBER value
     * @property {number} CHATLIST_SEARCH=55 CHATLIST_SEARCH value
     * @property {number} NEW_CHAT_LIST=56 NEW_CHAT_LIST value
     */
    proto.BotMetricsEntryPoint = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "UNDEFINED_ENTRY_POINT"] = 0;
        values[valuesById[1] = "FAVICON"] = 1;
        values[valuesById[2] = "CHATLIST"] = 2;
        values[valuesById[3] = "AISEARCH_NULL_STATE_PAPER_PLANE"] = 3;
        values[valuesById[4] = "AISEARCH_NULL_STATE_SUGGESTION"] = 4;
        values[valuesById[5] = "AISEARCH_TYPE_AHEAD_SUGGESTION"] = 5;
        values[valuesById[6] = "AISEARCH_TYPE_AHEAD_PAPER_PLANE"] = 6;
        values[valuesById[7] = "AISEARCH_TYPE_AHEAD_RESULT_CHATLIST"] = 7;
        values[valuesById[8] = "AISEARCH_TYPE_AHEAD_RESULT_MESSAGES"] = 8;
        values[valuesById[9] = "AIVOICE_SEARCH_BAR"] = 9;
        values[valuesById[10] = "AIVOICE_FAVICON"] = 10;
        values[valuesById[11] = "AISTUDIO"] = 11;
        values[valuesById[12] = "DEEPLINK"] = 12;
        values[valuesById[13] = "NOTIFICATION"] = 13;
        values[valuesById[14] = "PROFILE_MESSAGE_BUTTON"] = 14;
        values[valuesById[15] = "FORWARD"] = 15;
        values[valuesById[16] = "APP_SHORTCUT"] = 16;
        values[valuesById[17] = "FF_FAMILY"] = 17;
        values[valuesById[18] = "AI_TAB"] = 18;
        values[valuesById[19] = "AI_HOME"] = 19;
        values[valuesById[20] = "AI_DEEPLINK_IMMERSIVE"] = 20;
        values[valuesById[21] = "AI_DEEPLINK"] = 21;
        values[valuesById[22] = "META_AI_CHAT_SHORTCUT_AI_STUDIO"] = 22;
        values[valuesById[23] = "UGC_CHAT_SHORTCUT_AI_STUDIO"] = 23;
        values[valuesById[24] = "NEW_CHAT_AI_STUDIO"] = 24;
        values[valuesById[25] = "AIVOICE_FAVICON_CALL_HISTORY"] = 25;
        values[valuesById[26] = "ASK_META_AI_CONTEXT_MENU"] = 26;
        values[valuesById[27] = "ASK_META_AI_CONTEXT_MENU_1ON1"] = 27;
        values[valuesById[28] = "ASK_META_AI_CONTEXT_MENU_GROUP"] = 28;
        values[valuesById[29] = "INVOKE_META_AI_1ON1"] = 29;
        values[valuesById[30] = "INVOKE_META_AI_GROUP"] = 30;
        values[valuesById[31] = "META_AI_FORWARD"] = 31;
        values[valuesById[32] = "NEW_CHAT_AI_CONTACT"] = 32;
        values[valuesById[33] = "MESSAGE_QUICK_ACTION_1_ON_1_CHAT"] = 33;
        values[valuesById[34] = "MESSAGE_QUICK_ACTION_GROUP_CHAT"] = 34;
        values[valuesById[35] = "ATTACHMENT_TRAY_1_ON_1_CHAT"] = 35;
        values[valuesById[36] = "ATTACHMENT_TRAY_GROUP_CHAT"] = 36;
        values[valuesById[37] = "ASK_META_AI_MEDIA_VIEWER_1ON1"] = 37;
        values[valuesById[38] = "ASK_META_AI_MEDIA_VIEWER_GROUP"] = 38;
        values[valuesById[39] = "MEDIA_PICKER_1_ON_1_CHAT"] = 39;
        values[valuesById[40] = "MEDIA_PICKER_GROUP_CHAT"] = 40;
        values[valuesById[41] = "ASK_META_AI_NO_SEARCH_RESULTS"] = 41;
        values[valuesById[45] = "META_AI_SETTINGS"] = 45;
        values[valuesById[46] = "WEB_INTRO_PANEL"] = 46;
        values[valuesById[47] = "WEB_NAVIGATION_BAR"] = 47;
        values[valuesById[54] = "GROUP_MEMBER"] = 54;
        values[valuesById[55] = "CHATLIST_SEARCH"] = 55;
        values[valuesById[56] = "NEW_CHAT_LIST"] = 56;
        return values;
    })();

    proto.BotMetricsMetadata = (function() {

        /**
         * Properties of a BotMetricsMetadata.
         * @memberof proto
         * @interface IBotMetricsMetadata
         * @property {string|null} [destinationId] BotMetricsMetadata destinationId
         * @property {proto.BotMetricsEntryPoint|null} [destinationEntryPoint] BotMetricsMetadata destinationEntryPoint
         * @property {proto.BotMetricsThreadEntryPoint|null} [threadOrigin] BotMetricsMetadata threadOrigin
         */

        /**
         * Constructs a new BotMetricsMetadata.
         * @memberof proto
         * @classdesc Represents a BotMetricsMetadata.
         * @implements IBotMetricsMetadata
         * @constructor
         * @param {proto.IBotMetricsMetadata=} [properties] Properties to set
         */
        function BotMetricsMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotMetricsMetadata destinationId.
         * @member {string|null|undefined} destinationId
         * @memberof proto.BotMetricsMetadata
         * @instance
         */
        BotMetricsMetadata.prototype.destinationId = null;

        /**
         * BotMetricsMetadata destinationEntryPoint.
         * @member {proto.BotMetricsEntryPoint|null|undefined} destinationEntryPoint
         * @memberof proto.BotMetricsMetadata
         * @instance
         */
        BotMetricsMetadata.prototype.destinationEntryPoint = null;

        /**
         * BotMetricsMetadata threadOrigin.
         * @member {proto.BotMetricsThreadEntryPoint|null|undefined} threadOrigin
         * @memberof proto.BotMetricsMetadata
         * @instance
         */
        BotMetricsMetadata.prototype.threadOrigin = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetricsMetadata.prototype, "_destinationId", {
            get: $util.oneOfGetter($oneOfFields = ["destinationId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetricsMetadata.prototype, "_destinationEntryPoint", {
            get: $util.oneOfGetter($oneOfFields = ["destinationEntryPoint"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetricsMetadata.prototype, "_threadOrigin", {
            get: $util.oneOfGetter($oneOfFields = ["threadOrigin"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotMetricsMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotMetricsMetadata
         * @static
         * @param {proto.IBotMetricsMetadata=} [properties] Properties to set
         * @returns {proto.BotMetricsMetadata} BotMetricsMetadata instance
         */
        BotMetricsMetadata.create = function create(properties) {
            return new BotMetricsMetadata(properties);
        };

        /**
         * Encodes the specified BotMetricsMetadata message. Does not implicitly {@link proto.BotMetricsMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotMetricsMetadata
         * @static
         * @param {proto.IBotMetricsMetadata} message BotMetricsMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotMetricsMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.destinationId != null && Object.hasOwnProperty.call(message, "destinationId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.destinationId);
            if (message.destinationEntryPoint != null && Object.hasOwnProperty.call(message, "destinationEntryPoint"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.destinationEntryPoint);
            if (message.threadOrigin != null && Object.hasOwnProperty.call(message, "threadOrigin"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.threadOrigin);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotMetricsMetadata message, length delimited. Does not implicitly {@link proto.BotMetricsMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotMetricsMetadata
         * @static
         * @param {proto.IBotMetricsMetadata} message BotMetricsMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotMetricsMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotMetricsMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotMetricsMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotMetricsMetadata} BotMetricsMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotMetricsMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.destinationId = reader.stringVerify();
                        message._destinationId = "destinationId";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.destinationEntryPoint = reader.int32();
                        message._destinationEntryPoint = "destinationEntryPoint";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.threadOrigin = reader.int32();
                        message._threadOrigin = "threadOrigin";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotMetricsMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotMetricsMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotMetricsMetadata} BotMetricsMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotMetricsMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotMetricsMetadata message.
         * @function verify
         * @memberof proto.BotMetricsMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotMetricsMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.destinationId != null && Object.hasOwnProperty.call(message, "destinationId")) {
                properties._destinationId = 1;
                if (!$util.isString(message.destinationId))
                    return "destinationId: string expected";
            }
            if (message.destinationEntryPoint != null && Object.hasOwnProperty.call(message, "destinationEntryPoint")) {
                properties._destinationEntryPoint = 1;
                if (typeof message.destinationEntryPoint !== "number" || (message.destinationEntryPoint | 0) !== message.destinationEntryPoint)
                    return "destinationEntryPoint: enum value expected";
            }
            if (message.threadOrigin != null && Object.hasOwnProperty.call(message, "threadOrigin")) {
                properties._threadOrigin = 1;
                if (typeof message.threadOrigin !== "number" || (message.threadOrigin | 0) !== message.threadOrigin)
                    return "threadOrigin: enum value expected";
            }
            return null;
        };

        /**
         * Creates a BotMetricsMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotMetricsMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotMetricsMetadata} BotMetricsMetadata
         */
        BotMetricsMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotMetricsMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.destinationId != null)
                message.destinationId = String(object.destinationId);
            switch (object.destinationEntryPoint) {
            case "UNDEFINED_ENTRY_POINT":
            case 0:
                message.destinationEntryPoint = 0;
                break;
            case "FAVICON":
            case 1:
                message.destinationEntryPoint = 1;
                break;
            case "CHATLIST":
            case 2:
                message.destinationEntryPoint = 2;
                break;
            case "AISEARCH_NULL_STATE_PAPER_PLANE":
            case 3:
                message.destinationEntryPoint = 3;
                break;
            case "AISEARCH_NULL_STATE_SUGGESTION":
            case 4:
                message.destinationEntryPoint = 4;
                break;
            case "AISEARCH_TYPE_AHEAD_SUGGESTION":
            case 5:
                message.destinationEntryPoint = 5;
                break;
            case "AISEARCH_TYPE_AHEAD_PAPER_PLANE":
            case 6:
                message.destinationEntryPoint = 6;
                break;
            case "AISEARCH_TYPE_AHEAD_RESULT_CHATLIST":
            case 7:
                message.destinationEntryPoint = 7;
                break;
            case "AISEARCH_TYPE_AHEAD_RESULT_MESSAGES":
            case 8:
                message.destinationEntryPoint = 8;
                break;
            case "AIVOICE_SEARCH_BAR":
            case 9:
                message.destinationEntryPoint = 9;
                break;
            case "AIVOICE_FAVICON":
            case 10:
                message.destinationEntryPoint = 10;
                break;
            case "AISTUDIO":
            case 11:
                message.destinationEntryPoint = 11;
                break;
            case "DEEPLINK":
            case 12:
                message.destinationEntryPoint = 12;
                break;
            case "NOTIFICATION":
            case 13:
                message.destinationEntryPoint = 13;
                break;
            case "PROFILE_MESSAGE_BUTTON":
            case 14:
                message.destinationEntryPoint = 14;
                break;
            case "FORWARD":
            case 15:
                message.destinationEntryPoint = 15;
                break;
            case "APP_SHORTCUT":
            case 16:
                message.destinationEntryPoint = 16;
                break;
            case "FF_FAMILY":
            case 17:
                message.destinationEntryPoint = 17;
                break;
            case "AI_TAB":
            case 18:
                message.destinationEntryPoint = 18;
                break;
            case "AI_HOME":
            case 19:
                message.destinationEntryPoint = 19;
                break;
            case "AI_DEEPLINK_IMMERSIVE":
            case 20:
                message.destinationEntryPoint = 20;
                break;
            case "AI_DEEPLINK":
            case 21:
                message.destinationEntryPoint = 21;
                break;
            case "META_AI_CHAT_SHORTCUT_AI_STUDIO":
            case 22:
                message.destinationEntryPoint = 22;
                break;
            case "UGC_CHAT_SHORTCUT_AI_STUDIO":
            case 23:
                message.destinationEntryPoint = 23;
                break;
            case "NEW_CHAT_AI_STUDIO":
            case 24:
                message.destinationEntryPoint = 24;
                break;
            case "AIVOICE_FAVICON_CALL_HISTORY":
            case 25:
                message.destinationEntryPoint = 25;
                break;
            case "ASK_META_AI_CONTEXT_MENU":
            case 26:
                message.destinationEntryPoint = 26;
                break;
            case "ASK_META_AI_CONTEXT_MENU_1ON1":
            case 27:
                message.destinationEntryPoint = 27;
                break;
            case "ASK_META_AI_CONTEXT_MENU_GROUP":
            case 28:
                message.destinationEntryPoint = 28;
                break;
            case "INVOKE_META_AI_1ON1":
            case 29:
                message.destinationEntryPoint = 29;
                break;
            case "INVOKE_META_AI_GROUP":
            case 30:
                message.destinationEntryPoint = 30;
                break;
            case "META_AI_FORWARD":
            case 31:
                message.destinationEntryPoint = 31;
                break;
            case "NEW_CHAT_AI_CONTACT":
            case 32:
                message.destinationEntryPoint = 32;
                break;
            case "MESSAGE_QUICK_ACTION_1_ON_1_CHAT":
            case 33:
                message.destinationEntryPoint = 33;
                break;
            case "MESSAGE_QUICK_ACTION_GROUP_CHAT":
            case 34:
                message.destinationEntryPoint = 34;
                break;
            case "ATTACHMENT_TRAY_1_ON_1_CHAT":
            case 35:
                message.destinationEntryPoint = 35;
                break;
            case "ATTACHMENT_TRAY_GROUP_CHAT":
            case 36:
                message.destinationEntryPoint = 36;
                break;
            case "ASK_META_AI_MEDIA_VIEWER_1ON1":
            case 37:
                message.destinationEntryPoint = 37;
                break;
            case "ASK_META_AI_MEDIA_VIEWER_GROUP":
            case 38:
                message.destinationEntryPoint = 38;
                break;
            case "MEDIA_PICKER_1_ON_1_CHAT":
            case 39:
                message.destinationEntryPoint = 39;
                break;
            case "MEDIA_PICKER_GROUP_CHAT":
            case 40:
                message.destinationEntryPoint = 40;
                break;
            case "ASK_META_AI_NO_SEARCH_RESULTS":
            case 41:
                message.destinationEntryPoint = 41;
                break;
            case "META_AI_SETTINGS":
            case 45:
                message.destinationEntryPoint = 45;
                break;
            case "WEB_INTRO_PANEL":
            case 46:
                message.destinationEntryPoint = 46;
                break;
            case "WEB_NAVIGATION_BAR":
            case 47:
                message.destinationEntryPoint = 47;
                break;
            case "GROUP_MEMBER":
            case 54:
                message.destinationEntryPoint = 54;
                break;
            case "CHATLIST_SEARCH":
            case 55:
                message.destinationEntryPoint = 55;
                break;
            case "NEW_CHAT_LIST":
            case 56:
                message.destinationEntryPoint = 56;
                break;
            default:
                if (typeof object.destinationEntryPoint === "number" && (object.destinationEntryPoint | 0) === object.destinationEntryPoint)
                    message.destinationEntryPoint = object.destinationEntryPoint;
            }
            switch (object.threadOrigin) {
            case "AI_TAB_THREAD":
            case 1:
                message.threadOrigin = 1;
                break;
            case "AI_HOME_THREAD":
            case 2:
                message.threadOrigin = 2;
                break;
            case "AI_DEEPLINK_IMMERSIVE_THREAD":
            case 3:
                message.threadOrigin = 3;
                break;
            case "AI_DEEPLINK_THREAD":
            case 4:
                message.threadOrigin = 4;
                break;
            case "ASK_META_AI_CONTEXT_MENU_THREAD":
            case 5:
                message.threadOrigin = 5;
                break;
            default:
                if (typeof object.threadOrigin === "number" && (object.threadOrigin | 0) === object.threadOrigin)
                    message.threadOrigin = object.threadOrigin;
            }
            return message;
        };

        /**
         * Creates a plain object from a BotMetricsMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotMetricsMetadata
         * @static
         * @param {proto.BotMetricsMetadata} message BotMetricsMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotMetricsMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.destinationId != null && Object.hasOwnProperty.call(message, "destinationId"))
                object.destinationId = message.destinationId;
            if (message.destinationEntryPoint != null && Object.hasOwnProperty.call(message, "destinationEntryPoint"))
                object.destinationEntryPoint = options.enums === String ? $root.proto.BotMetricsEntryPoint[message.destinationEntryPoint] === undefined ? message.destinationEntryPoint : $root.proto.BotMetricsEntryPoint[message.destinationEntryPoint] : message.destinationEntryPoint;
            if (message.threadOrigin != null && Object.hasOwnProperty.call(message, "threadOrigin"))
                object.threadOrigin = options.enums === String ? $root.proto.BotMetricsThreadEntryPoint[message.threadOrigin] === undefined ? message.threadOrigin : $root.proto.BotMetricsThreadEntryPoint[message.threadOrigin] : message.threadOrigin;
            return object;
        };

        /**
         * Converts this BotMetricsMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotMetricsMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotMetricsMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotMetricsMetadata
         * @function getTypeUrl
         * @memberof proto.BotMetricsMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotMetricsMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotMetricsMetadata";
        };

        return BotMetricsMetadata;
    })();

    /**
     * BotMetricsThreadEntryPoint enum.
     * @name proto.BotMetricsThreadEntryPoint
     * @enum {number}
     * @property {number} AI_TAB_THREAD=1 AI_TAB_THREAD value
     * @property {number} AI_HOME_THREAD=2 AI_HOME_THREAD value
     * @property {number} AI_DEEPLINK_IMMERSIVE_THREAD=3 AI_DEEPLINK_IMMERSIVE_THREAD value
     * @property {number} AI_DEEPLINK_THREAD=4 AI_DEEPLINK_THREAD value
     * @property {number} ASK_META_AI_CONTEXT_MENU_THREAD=5 ASK_META_AI_CONTEXT_MENU_THREAD value
     */
    proto.BotMetricsThreadEntryPoint = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[1] = "AI_TAB_THREAD"] = 1;
        values[valuesById[2] = "AI_HOME_THREAD"] = 2;
        values[valuesById[3] = "AI_DEEPLINK_IMMERSIVE_THREAD"] = 3;
        values[valuesById[4] = "AI_DEEPLINK_THREAD"] = 4;
        values[valuesById[5] = "ASK_META_AI_CONTEXT_MENU_THREAD"] = 5;
        return values;
    })();

    proto.BotModeSelectionMetadata = (function() {

        /**
         * Properties of a BotModeSelectionMetadata.
         * @memberof proto
         * @interface IBotModeSelectionMetadata
         * @property {Array.<proto.BotModeSelectionMetadata.BotUserSelectionMode>|null} [mode] BotModeSelectionMetadata mode
         * @property {Array.<number>|null} [overrideMode] BotModeSelectionMetadata overrideMode
         */

        /**
         * Constructs a new BotModeSelectionMetadata.
         * @memberof proto
         * @classdesc Represents a BotModeSelectionMetadata.
         * @implements IBotModeSelectionMetadata
         * @constructor
         * @param {proto.IBotModeSelectionMetadata=} [properties] Properties to set
         */
        function BotModeSelectionMetadata(properties) {
            this.mode = [];
            this.overrideMode = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotModeSelectionMetadata mode.
         * @member {Array.<proto.BotModeSelectionMetadata.BotUserSelectionMode>} mode
         * @memberof proto.BotModeSelectionMetadata
         * @instance
         */
        BotModeSelectionMetadata.prototype.mode = $util.emptyArray;

        /**
         * BotModeSelectionMetadata overrideMode.
         * @member {Array.<number>} overrideMode
         * @memberof proto.BotModeSelectionMetadata
         * @instance
         */
        BotModeSelectionMetadata.prototype.overrideMode = $util.emptyArray;

        /**
         * Creates a new BotModeSelectionMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotModeSelectionMetadata
         * @static
         * @param {proto.IBotModeSelectionMetadata=} [properties] Properties to set
         * @returns {proto.BotModeSelectionMetadata} BotModeSelectionMetadata instance
         */
        BotModeSelectionMetadata.create = function create(properties) {
            return new BotModeSelectionMetadata(properties);
        };

        /**
         * Encodes the specified BotModeSelectionMetadata message. Does not implicitly {@link proto.BotModeSelectionMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotModeSelectionMetadata
         * @static
         * @param {proto.IBotModeSelectionMetadata} message BotModeSelectionMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotModeSelectionMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.mode != null && message.mode.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (var i = 0; i < message.mode.length; ++i)
                    writer.int32(message.mode[i]);
                writer.ldelim();
            }
            if (message.overrideMode != null && message.overrideMode.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (var i = 0; i < message.overrideMode.length; ++i)
                    writer.uint32(message.overrideMode[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotModeSelectionMetadata message, length delimited. Does not implicitly {@link proto.BotModeSelectionMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotModeSelectionMetadata
         * @static
         * @param {proto.IBotModeSelectionMetadata} message BotModeSelectionMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotModeSelectionMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotModeSelectionMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotModeSelectionMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotModeSelectionMetadata} BotModeSelectionMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotModeSelectionMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u === 2) {
                            if (!(message.mode && message.mode.length))
                                message.mode = [];
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.mode.push(reader.int32());
                            continue;
                        }
                        if (u !== 0)
                            break;
                        if (!(message.mode && message.mode.length))
                            message.mode = [];
                        message.mode.push(reader.int32());
                        continue;
                    }
                case 2: {
                        if (u === 2) {
                            if (!(message.overrideMode && message.overrideMode.length))
                                message.overrideMode = [];
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.overrideMode.push(reader.uint32());
                            continue;
                        }
                        if (u !== 0)
                            break;
                        if (!(message.overrideMode && message.overrideMode.length))
                            message.overrideMode = [];
                        message.overrideMode.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotModeSelectionMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotModeSelectionMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotModeSelectionMetadata} BotModeSelectionMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotModeSelectionMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotModeSelectionMetadata message.
         * @function verify
         * @memberof proto.BotModeSelectionMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotModeSelectionMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            if (message.mode != null && Object.hasOwnProperty.call(message, "mode")) {
                if (!Array.isArray(message.mode))
                    return "mode: array expected";
                for (var i = 0; i < message.mode.length; ++i)
                    if (typeof message.mode[i] !== "number" || (message.mode[i] | 0) !== message.mode[i])
                        return "mode: enum value[] expected";
            }
            if (message.overrideMode != null && Object.hasOwnProperty.call(message, "overrideMode")) {
                if (!Array.isArray(message.overrideMode))
                    return "overrideMode: array expected";
                for (var i = 0; i < message.overrideMode.length; ++i)
                    if (!$util.isInteger(message.overrideMode[i]))
                        return "overrideMode: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a BotModeSelectionMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotModeSelectionMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotModeSelectionMetadata} BotModeSelectionMetadata
         */
        BotModeSelectionMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotModeSelectionMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.mode) {
                if (!Array.isArray(object.mode))
                    throw TypeError(".proto.BotModeSelectionMetadata.mode: array expected");
                message.mode = [];
                for (var i = 0; i < object.mode.length; ++i)
                    switch (object.mode[i]) {
                    case "DEFAULT_MODE":
                    case 0:
                        message.mode[message.mode.length] = 0;
                        break;
                    case "THINK_HARD_MODE":
                    case 1:
                        message.mode[message.mode.length] = 1;
                        break;
                    default:
                        if (typeof object.mode[i] === "number" && (object.mode[i] | 0) === object.mode[i])
                            message.mode[message.mode.length] = object.mode[i];
                    }
            }
            if (object.overrideMode) {
                if (!Array.isArray(object.overrideMode))
                    throw TypeError(".proto.BotModeSelectionMetadata.overrideMode: array expected");
                message.overrideMode = Array(object.overrideMode.length);
                for (var i = 0; i < object.overrideMode.length; ++i)
                    message.overrideMode[i] = object.overrideMode[i] >>> 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a BotModeSelectionMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotModeSelectionMetadata
         * @static
         * @param {proto.BotModeSelectionMetadata} message BotModeSelectionMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotModeSelectionMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults) {
                object.mode = [];
                object.overrideMode = [];
            }
            if (message.mode && message.mode.length) {
                object.mode = Array(message.mode.length);
                for (var j = 0; j < message.mode.length; ++j)
                    object.mode[j] = options.enums === String ? $root.proto.BotModeSelectionMetadata.BotUserSelectionMode[message.mode[j]] === undefined ? message.mode[j] : $root.proto.BotModeSelectionMetadata.BotUserSelectionMode[message.mode[j]] : message.mode[j];
            }
            if (message.overrideMode && message.overrideMode.length) {
                object.overrideMode = Array(message.overrideMode.length);
                for (var j = 0; j < message.overrideMode.length; ++j)
                    object.overrideMode[j] = message.overrideMode[j];
            }
            return object;
        };

        /**
         * Converts this BotModeSelectionMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotModeSelectionMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotModeSelectionMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotModeSelectionMetadata
         * @function getTypeUrl
         * @memberof proto.BotModeSelectionMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotModeSelectionMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotModeSelectionMetadata";
        };

        /**
         * BotUserSelectionMode enum.
         * @name proto.BotModeSelectionMetadata.BotUserSelectionMode
         * @enum {number}
         * @property {number} DEFAULT_MODE=0 DEFAULT_MODE value
         * @property {number} THINK_HARD_MODE=1 THINK_HARD_MODE value
         */
        BotModeSelectionMetadata.BotUserSelectionMode = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "DEFAULT_MODE"] = 0;
            values[valuesById[1] = "THINK_HARD_MODE"] = 1;
            return values;
        })();

        return BotModeSelectionMetadata;
    })();

    proto.BotModelMetadata = (function() {

        /**
         * Properties of a BotModelMetadata.
         * @memberof proto
         * @interface IBotModelMetadata
         * @property {proto.BotModelMetadata.ModelType|null} [modelType] BotModelMetadata modelType
         * @property {proto.BotModelMetadata.PremiumModelStatus|null} [premiumModelStatus] BotModelMetadata premiumModelStatus
         * @property {string|null} [modelNameOverride] BotModelMetadata modelNameOverride
         */

        /**
         * Constructs a new BotModelMetadata.
         * @memberof proto
         * @classdesc Represents a BotModelMetadata.
         * @implements IBotModelMetadata
         * @constructor
         * @param {proto.IBotModelMetadata=} [properties] Properties to set
         */
        function BotModelMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotModelMetadata modelType.
         * @member {proto.BotModelMetadata.ModelType|null|undefined} modelType
         * @memberof proto.BotModelMetadata
         * @instance
         */
        BotModelMetadata.prototype.modelType = null;

        /**
         * BotModelMetadata premiumModelStatus.
         * @member {proto.BotModelMetadata.PremiumModelStatus|null|undefined} premiumModelStatus
         * @memberof proto.BotModelMetadata
         * @instance
         */
        BotModelMetadata.prototype.premiumModelStatus = null;

        /**
         * BotModelMetadata modelNameOverride.
         * @member {string|null|undefined} modelNameOverride
         * @memberof proto.BotModelMetadata
         * @instance
         */
        BotModelMetadata.prototype.modelNameOverride = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotModelMetadata.prototype, "_modelType", {
            get: $util.oneOfGetter($oneOfFields = ["modelType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotModelMetadata.prototype, "_premiumModelStatus", {
            get: $util.oneOfGetter($oneOfFields = ["premiumModelStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotModelMetadata.prototype, "_modelNameOverride", {
            get: $util.oneOfGetter($oneOfFields = ["modelNameOverride"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotModelMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotModelMetadata
         * @static
         * @param {proto.IBotModelMetadata=} [properties] Properties to set
         * @returns {proto.BotModelMetadata} BotModelMetadata instance
         */
        BotModelMetadata.create = function create(properties) {
            return new BotModelMetadata(properties);
        };

        /**
         * Encodes the specified BotModelMetadata message. Does not implicitly {@link proto.BotModelMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotModelMetadata
         * @static
         * @param {proto.IBotModelMetadata} message BotModelMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotModelMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.modelType != null && Object.hasOwnProperty.call(message, "modelType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.modelType);
            if (message.premiumModelStatus != null && Object.hasOwnProperty.call(message, "premiumModelStatus"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.premiumModelStatus);
            if (message.modelNameOverride != null && Object.hasOwnProperty.call(message, "modelNameOverride"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.modelNameOverride);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotModelMetadata message, length delimited. Does not implicitly {@link proto.BotModelMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotModelMetadata
         * @static
         * @param {proto.IBotModelMetadata} message BotModelMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotModelMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotModelMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotModelMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotModelMetadata} BotModelMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotModelMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.modelType = reader.int32();
                        message._modelType = "modelType";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.premiumModelStatus = reader.int32();
                        message._premiumModelStatus = "premiumModelStatus";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.modelNameOverride = reader.stringVerify();
                        message._modelNameOverride = "modelNameOverride";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotModelMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotModelMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotModelMetadata} BotModelMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotModelMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotModelMetadata message.
         * @function verify
         * @memberof proto.BotModelMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotModelMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.modelType != null && Object.hasOwnProperty.call(message, "modelType")) {
                properties._modelType = 1;
                if (typeof message.modelType !== "number" || (message.modelType | 0) !== message.modelType)
                    return "modelType: enum value expected";
            }
            if (message.premiumModelStatus != null && Object.hasOwnProperty.call(message, "premiumModelStatus")) {
                properties._premiumModelStatus = 1;
                if (typeof message.premiumModelStatus !== "number" || (message.premiumModelStatus | 0) !== message.premiumModelStatus)
                    return "premiumModelStatus: enum value expected";
            }
            if (message.modelNameOverride != null && Object.hasOwnProperty.call(message, "modelNameOverride")) {
                properties._modelNameOverride = 1;
                if (!$util.isString(message.modelNameOverride))
                    return "modelNameOverride: string expected";
            }
            return null;
        };

        /**
         * Creates a BotModelMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotModelMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotModelMetadata} BotModelMetadata
         */
        BotModelMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotModelMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.modelType) {
            case "UNKNOWN_TYPE":
            case 0:
                message.modelType = 0;
                break;
            case "LLAMA_PROD":
            case 1:
                message.modelType = 1;
                break;
            case "LLAMA_PROD_PREMIUM":
            case 2:
                message.modelType = 2;
                break;
            default:
                if (typeof object.modelType === "number" && (object.modelType | 0) === object.modelType)
                    message.modelType = object.modelType;
            }
            switch (object.premiumModelStatus) {
            case "UNKNOWN_STATUS":
            case 0:
                message.premiumModelStatus = 0;
                break;
            case "AVAILABLE":
            case 1:
                message.premiumModelStatus = 1;
                break;
            case "QUOTA_EXCEED_LIMIT":
            case 2:
                message.premiumModelStatus = 2;
                break;
            default:
                if (typeof object.premiumModelStatus === "number" && (object.premiumModelStatus | 0) === object.premiumModelStatus)
                    message.premiumModelStatus = object.premiumModelStatus;
            }
            if (object.modelNameOverride != null)
                message.modelNameOverride = String(object.modelNameOverride);
            return message;
        };

        /**
         * Creates a plain object from a BotModelMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotModelMetadata
         * @static
         * @param {proto.BotModelMetadata} message BotModelMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotModelMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.modelType != null && Object.hasOwnProperty.call(message, "modelType"))
                object.modelType = options.enums === String ? $root.proto.BotModelMetadata.ModelType[message.modelType] === undefined ? message.modelType : $root.proto.BotModelMetadata.ModelType[message.modelType] : message.modelType;
            if (message.premiumModelStatus != null && Object.hasOwnProperty.call(message, "premiumModelStatus"))
                object.premiumModelStatus = options.enums === String ? $root.proto.BotModelMetadata.PremiumModelStatus[message.premiumModelStatus] === undefined ? message.premiumModelStatus : $root.proto.BotModelMetadata.PremiumModelStatus[message.premiumModelStatus] : message.premiumModelStatus;
            if (message.modelNameOverride != null && Object.hasOwnProperty.call(message, "modelNameOverride"))
                object.modelNameOverride = message.modelNameOverride;
            return object;
        };

        /**
         * Converts this BotModelMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotModelMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotModelMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotModelMetadata
         * @function getTypeUrl
         * @memberof proto.BotModelMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotModelMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotModelMetadata";
        };

        /**
         * ModelType enum.
         * @name proto.BotModelMetadata.ModelType
         * @enum {number}
         * @property {number} UNKNOWN_TYPE=0 UNKNOWN_TYPE value
         * @property {number} LLAMA_PROD=1 LLAMA_PROD value
         * @property {number} LLAMA_PROD_PREMIUM=2 LLAMA_PROD_PREMIUM value
         */
        BotModelMetadata.ModelType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_TYPE"] = 0;
            values[valuesById[1] = "LLAMA_PROD"] = 1;
            values[valuesById[2] = "LLAMA_PROD_PREMIUM"] = 2;
            return values;
        })();

        /**
         * PremiumModelStatus enum.
         * @name proto.BotModelMetadata.PremiumModelStatus
         * @enum {number}
         * @property {number} UNKNOWN_STATUS=0 UNKNOWN_STATUS value
         * @property {number} AVAILABLE=1 AVAILABLE value
         * @property {number} QUOTA_EXCEED_LIMIT=2 QUOTA_EXCEED_LIMIT value
         */
        BotModelMetadata.PremiumModelStatus = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_STATUS"] = 0;
            values[valuesById[1] = "AVAILABLE"] = 1;
            values[valuesById[2] = "QUOTA_EXCEED_LIMIT"] = 2;
            return values;
        })();

        return BotModelMetadata;
    })();

    proto.BotPluginMetadata = (function() {

        /**
         * Properties of a BotPluginMetadata.
         * @memberof proto
         * @interface IBotPluginMetadata
         * @property {proto.BotPluginMetadata.SearchProvider|null} [provider] BotPluginMetadata provider
         * @property {proto.BotPluginMetadata.PluginType|null} [pluginType] BotPluginMetadata pluginType
         * @property {string|null} [thumbnailCdnUrl] BotPluginMetadata thumbnailCdnUrl
         * @property {string|null} [profilePhotoCdnUrl] BotPluginMetadata profilePhotoCdnUrl
         * @property {string|null} [searchProviderUrl] BotPluginMetadata searchProviderUrl
         * @property {number|null} [referenceIndex] BotPluginMetadata referenceIndex
         * @property {number|null} [expectedLinksCount] BotPluginMetadata expectedLinksCount
         * @property {string|null} [searchQuery] BotPluginMetadata searchQuery
         * @property {proto.IMessageKey|null} [parentPluginMessageKey] BotPluginMetadata parentPluginMessageKey
         * @property {proto.BotPluginMetadata.PluginType|null} [deprecatedField] BotPluginMetadata deprecatedField
         * @property {proto.BotPluginMetadata.PluginType|null} [parentPluginType] BotPluginMetadata parentPluginType
         * @property {string|null} [faviconCdnUrl] BotPluginMetadata faviconCdnUrl
         */

        /**
         * Constructs a new BotPluginMetadata.
         * @memberof proto
         * @classdesc Represents a BotPluginMetadata.
         * @implements IBotPluginMetadata
         * @constructor
         * @param {proto.IBotPluginMetadata=} [properties] Properties to set
         */
        function BotPluginMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotPluginMetadata provider.
         * @member {proto.BotPluginMetadata.SearchProvider|null|undefined} provider
         * @memberof proto.BotPluginMetadata
         * @instance
         */
        BotPluginMetadata.prototype.provider = null;

        /**
         * BotPluginMetadata pluginType.
         * @member {proto.BotPluginMetadata.PluginType|null|undefined} pluginType
         * @memberof proto.BotPluginMetadata
         * @instance
         */
        BotPluginMetadata.prototype.pluginType = null;

        /**
         * BotPluginMetadata thumbnailCdnUrl.
         * @member {string|null|undefined} thumbnailCdnUrl
         * @memberof proto.BotPluginMetadata
         * @instance
         */
        BotPluginMetadata.prototype.thumbnailCdnUrl = null;

        /**
         * BotPluginMetadata profilePhotoCdnUrl.
         * @member {string|null|undefined} profilePhotoCdnUrl
         * @memberof proto.BotPluginMetadata
         * @instance
         */
        BotPluginMetadata.prototype.profilePhotoCdnUrl = null;

        /**
         * BotPluginMetadata searchProviderUrl.
         * @member {string|null|undefined} searchProviderUrl
         * @memberof proto.BotPluginMetadata
         * @instance
         */
        BotPluginMetadata.prototype.searchProviderUrl = null;

        /**
         * BotPluginMetadata referenceIndex.
         * @member {number|null|undefined} referenceIndex
         * @memberof proto.BotPluginMetadata
         * @instance
         */
        BotPluginMetadata.prototype.referenceIndex = null;

        /**
         * BotPluginMetadata expectedLinksCount.
         * @member {number|null|undefined} expectedLinksCount
         * @memberof proto.BotPluginMetadata
         * @instance
         */
        BotPluginMetadata.prototype.expectedLinksCount = null;

        /**
         * BotPluginMetadata searchQuery.
         * @member {string|null|undefined} searchQuery
         * @memberof proto.BotPluginMetadata
         * @instance
         */
        BotPluginMetadata.prototype.searchQuery = null;

        /**
         * BotPluginMetadata parentPluginMessageKey.
         * @member {proto.IMessageKey|null|undefined} parentPluginMessageKey
         * @memberof proto.BotPluginMetadata
         * @instance
         */
        BotPluginMetadata.prototype.parentPluginMessageKey = null;

        /**
         * BotPluginMetadata deprecatedField.
         * @member {proto.BotPluginMetadata.PluginType|null|undefined} deprecatedField
         * @memberof proto.BotPluginMetadata
         * @instance
         */
        BotPluginMetadata.prototype.deprecatedField = null;

        /**
         * BotPluginMetadata parentPluginType.
         * @member {proto.BotPluginMetadata.PluginType|null|undefined} parentPluginType
         * @memberof proto.BotPluginMetadata
         * @instance
         */
        BotPluginMetadata.prototype.parentPluginType = null;

        /**
         * BotPluginMetadata faviconCdnUrl.
         * @member {string|null|undefined} faviconCdnUrl
         * @memberof proto.BotPluginMetadata
         * @instance
         */
        BotPluginMetadata.prototype.faviconCdnUrl = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_provider", {
            get: $util.oneOfGetter($oneOfFields = ["provider"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_pluginType", {
            get: $util.oneOfGetter($oneOfFields = ["pluginType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_thumbnailCdnUrl", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailCdnUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_profilePhotoCdnUrl", {
            get: $util.oneOfGetter($oneOfFields = ["profilePhotoCdnUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_searchProviderUrl", {
            get: $util.oneOfGetter($oneOfFields = ["searchProviderUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_referenceIndex", {
            get: $util.oneOfGetter($oneOfFields = ["referenceIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_expectedLinksCount", {
            get: $util.oneOfGetter($oneOfFields = ["expectedLinksCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_searchQuery", {
            get: $util.oneOfGetter($oneOfFields = ["searchQuery"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_parentPluginMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["parentPluginMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_deprecatedField", {
            get: $util.oneOfGetter($oneOfFields = ["deprecatedField"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_parentPluginType", {
            get: $util.oneOfGetter($oneOfFields = ["parentPluginType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_faviconCdnUrl", {
            get: $util.oneOfGetter($oneOfFields = ["faviconCdnUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotPluginMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotPluginMetadata
         * @static
         * @param {proto.IBotPluginMetadata=} [properties] Properties to set
         * @returns {proto.BotPluginMetadata} BotPluginMetadata instance
         */
        BotPluginMetadata.create = function create(properties) {
            return new BotPluginMetadata(properties);
        };

        /**
         * Encodes the specified BotPluginMetadata message. Does not implicitly {@link proto.BotPluginMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotPluginMetadata
         * @static
         * @param {proto.IBotPluginMetadata} message BotPluginMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotPluginMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.provider != null && Object.hasOwnProperty.call(message, "provider"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.provider);
            if (message.pluginType != null && Object.hasOwnProperty.call(message, "pluginType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.pluginType);
            if (message.thumbnailCdnUrl != null && Object.hasOwnProperty.call(message, "thumbnailCdnUrl"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.thumbnailCdnUrl);
            if (message.profilePhotoCdnUrl != null && Object.hasOwnProperty.call(message, "profilePhotoCdnUrl"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.profilePhotoCdnUrl);
            if (message.searchProviderUrl != null && Object.hasOwnProperty.call(message, "searchProviderUrl"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.searchProviderUrl);
            if (message.referenceIndex != null && Object.hasOwnProperty.call(message, "referenceIndex"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.referenceIndex);
            if (message.expectedLinksCount != null && Object.hasOwnProperty.call(message, "expectedLinksCount"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.expectedLinksCount);
            if (message.searchQuery != null && Object.hasOwnProperty.call(message, "searchQuery"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.searchQuery);
            if (message.parentPluginMessageKey != null && Object.hasOwnProperty.call(message, "parentPluginMessageKey"))
                $root.proto.MessageKey.encode(message.parentPluginMessageKey, writer.uint32(/* id 10, wireType 2 =*/82).fork(), q + 1).ldelim();
            if (message.deprecatedField != null && Object.hasOwnProperty.call(message, "deprecatedField"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.deprecatedField);
            if (message.parentPluginType != null && Object.hasOwnProperty.call(message, "parentPluginType"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.parentPluginType);
            if (message.faviconCdnUrl != null && Object.hasOwnProperty.call(message, "faviconCdnUrl"))
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.faviconCdnUrl);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotPluginMetadata message, length delimited. Does not implicitly {@link proto.BotPluginMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotPluginMetadata
         * @static
         * @param {proto.IBotPluginMetadata} message BotPluginMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotPluginMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotPluginMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotPluginMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotPluginMetadata} BotPluginMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotPluginMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.provider = reader.int32();
                        message._provider = "provider";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.pluginType = reader.int32();
                        message._pluginType = "pluginType";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.thumbnailCdnUrl = reader.stringVerify();
                        message._thumbnailCdnUrl = "thumbnailCdnUrl";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.profilePhotoCdnUrl = reader.stringVerify();
                        message._profilePhotoCdnUrl = "profilePhotoCdnUrl";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.searchProviderUrl = reader.stringVerify();
                        message._searchProviderUrl = "searchProviderUrl";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        message.referenceIndex = reader.uint32();
                        message._referenceIndex = "referenceIndex";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        message.expectedLinksCount = reader.uint32();
                        message._expectedLinksCount = "expectedLinksCount";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        message.searchQuery = reader.stringVerify();
                        message._searchQuery = "searchQuery";
                        continue;
                    }
                case 10: {
                        if (u !== 2)
                            break;
                        message.parentPluginMessageKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.parentPluginMessageKey);
                        message._parentPluginMessageKey = "parentPluginMessageKey";
                        continue;
                    }
                case 11: {
                        if (u !== 0)
                            break;
                        message.deprecatedField = reader.int32();
                        message._deprecatedField = "deprecatedField";
                        continue;
                    }
                case 12: {
                        if (u !== 0)
                            break;
                        message.parentPluginType = reader.int32();
                        message._parentPluginType = "parentPluginType";
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        message.faviconCdnUrl = reader.stringVerify();
                        message._faviconCdnUrl = "faviconCdnUrl";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotPluginMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotPluginMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotPluginMetadata} BotPluginMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotPluginMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotPluginMetadata message.
         * @function verify
         * @memberof proto.BotPluginMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotPluginMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.provider != null && Object.hasOwnProperty.call(message, "provider")) {
                properties._provider = 1;
                if (typeof message.provider !== "number" || (message.provider | 0) !== message.provider)
                    return "provider: enum value expected";
            }
            if (message.pluginType != null && Object.hasOwnProperty.call(message, "pluginType")) {
                properties._pluginType = 1;
                if (typeof message.pluginType !== "number" || (message.pluginType | 0) !== message.pluginType)
                    return "pluginType: enum value expected";
            }
            if (message.thumbnailCdnUrl != null && Object.hasOwnProperty.call(message, "thumbnailCdnUrl")) {
                properties._thumbnailCdnUrl = 1;
                if (!$util.isString(message.thumbnailCdnUrl))
                    return "thumbnailCdnUrl: string expected";
            }
            if (message.profilePhotoCdnUrl != null && Object.hasOwnProperty.call(message, "profilePhotoCdnUrl")) {
                properties._profilePhotoCdnUrl = 1;
                if (!$util.isString(message.profilePhotoCdnUrl))
                    return "profilePhotoCdnUrl: string expected";
            }
            if (message.searchProviderUrl != null && Object.hasOwnProperty.call(message, "searchProviderUrl")) {
                properties._searchProviderUrl = 1;
                if (!$util.isString(message.searchProviderUrl))
                    return "searchProviderUrl: string expected";
            }
            if (message.referenceIndex != null && Object.hasOwnProperty.call(message, "referenceIndex")) {
                properties._referenceIndex = 1;
                if (!$util.isInteger(message.referenceIndex))
                    return "referenceIndex: integer expected";
            }
            if (message.expectedLinksCount != null && Object.hasOwnProperty.call(message, "expectedLinksCount")) {
                properties._expectedLinksCount = 1;
                if (!$util.isInteger(message.expectedLinksCount))
                    return "expectedLinksCount: integer expected";
            }
            if (message.searchQuery != null && Object.hasOwnProperty.call(message, "searchQuery")) {
                properties._searchQuery = 1;
                if (!$util.isString(message.searchQuery))
                    return "searchQuery: string expected";
            }
            if (message.parentPluginMessageKey != null && Object.hasOwnProperty.call(message, "parentPluginMessageKey")) {
                properties._parentPluginMessageKey = 1;
                {
                    var error = $root.proto.MessageKey.verify(message.parentPluginMessageKey, q + 1);
                    if (error)
                        return "parentPluginMessageKey." + error;
                }
            }
            if (message.deprecatedField != null && Object.hasOwnProperty.call(message, "deprecatedField")) {
                properties._deprecatedField = 1;
                if (typeof message.deprecatedField !== "number" || (message.deprecatedField | 0) !== message.deprecatedField)
                    return "deprecatedField: enum value expected";
            }
            if (message.parentPluginType != null && Object.hasOwnProperty.call(message, "parentPluginType")) {
                properties._parentPluginType = 1;
                if (typeof message.parentPluginType !== "number" || (message.parentPluginType | 0) !== message.parentPluginType)
                    return "parentPluginType: enum value expected";
            }
            if (message.faviconCdnUrl != null && Object.hasOwnProperty.call(message, "faviconCdnUrl")) {
                properties._faviconCdnUrl = 1;
                if (!$util.isString(message.faviconCdnUrl))
                    return "faviconCdnUrl: string expected";
            }
            return null;
        };

        /**
         * Creates a BotPluginMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotPluginMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotPluginMetadata} BotPluginMetadata
         */
        BotPluginMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotPluginMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.provider) {
            case "UNKNOWN":
            case 0:
                message.provider = 0;
                break;
            case "BING":
            case 1:
                message.provider = 1;
                break;
            case "GOOGLE":
            case 2:
                message.provider = 2;
                break;
            case "SUPPORT":
            case 3:
                message.provider = 3;
                break;
            default:
                if (typeof object.provider === "number" && (object.provider | 0) === object.provider)
                    message.provider = object.provider;
            }
            switch (object.pluginType) {
            case "UNKNOWN_PLUGIN":
            case 0:
                message.pluginType = 0;
                break;
            case "REELS":
            case 1:
                message.pluginType = 1;
                break;
            case "SEARCH":
            case 2:
                message.pluginType = 2;
                break;
            default:
                if (typeof object.pluginType === "number" && (object.pluginType | 0) === object.pluginType)
                    message.pluginType = object.pluginType;
            }
            if (object.thumbnailCdnUrl != null)
                message.thumbnailCdnUrl = String(object.thumbnailCdnUrl);
            if (object.profilePhotoCdnUrl != null)
                message.profilePhotoCdnUrl = String(object.profilePhotoCdnUrl);
            if (object.searchProviderUrl != null)
                message.searchProviderUrl = String(object.searchProviderUrl);
            if (object.referenceIndex != null)
                message.referenceIndex = object.referenceIndex >>> 0;
            if (object.expectedLinksCount != null)
                message.expectedLinksCount = object.expectedLinksCount >>> 0;
            if (object.searchQuery != null)
                message.searchQuery = String(object.searchQuery);
            if (object.parentPluginMessageKey != null) {
                if (!$util.isObject(object.parentPluginMessageKey))
                    throw TypeError(".proto.BotPluginMetadata.parentPluginMessageKey: object expected");
                message.parentPluginMessageKey = $root.proto.MessageKey.fromObject(object.parentPluginMessageKey, q + 1);
            }
            switch (object.deprecatedField) {
            case "UNKNOWN_PLUGIN":
            case 0:
                message.deprecatedField = 0;
                break;
            case "REELS":
            case 1:
                message.deprecatedField = 1;
                break;
            case "SEARCH":
            case 2:
                message.deprecatedField = 2;
                break;
            default:
                if (typeof object.deprecatedField === "number" && (object.deprecatedField | 0) === object.deprecatedField)
                    message.deprecatedField = object.deprecatedField;
            }
            switch (object.parentPluginType) {
            case "UNKNOWN_PLUGIN":
            case 0:
                message.parentPluginType = 0;
                break;
            case "REELS":
            case 1:
                message.parentPluginType = 1;
                break;
            case "SEARCH":
            case 2:
                message.parentPluginType = 2;
                break;
            default:
                if (typeof object.parentPluginType === "number" && (object.parentPluginType | 0) === object.parentPluginType)
                    message.parentPluginType = object.parentPluginType;
            }
            if (object.faviconCdnUrl != null)
                message.faviconCdnUrl = String(object.faviconCdnUrl);
            return message;
        };

        /**
         * Creates a plain object from a BotPluginMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotPluginMetadata
         * @static
         * @param {proto.BotPluginMetadata} message BotPluginMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotPluginMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.provider != null && Object.hasOwnProperty.call(message, "provider"))
                object.provider = options.enums === String ? $root.proto.BotPluginMetadata.SearchProvider[message.provider] === undefined ? message.provider : $root.proto.BotPluginMetadata.SearchProvider[message.provider] : message.provider;
            if (message.pluginType != null && Object.hasOwnProperty.call(message, "pluginType"))
                object.pluginType = options.enums === String ? $root.proto.BotPluginMetadata.PluginType[message.pluginType] === undefined ? message.pluginType : $root.proto.BotPluginMetadata.PluginType[message.pluginType] : message.pluginType;
            if (message.thumbnailCdnUrl != null && Object.hasOwnProperty.call(message, "thumbnailCdnUrl"))
                object.thumbnailCdnUrl = message.thumbnailCdnUrl;
            if (message.profilePhotoCdnUrl != null && Object.hasOwnProperty.call(message, "profilePhotoCdnUrl"))
                object.profilePhotoCdnUrl = message.profilePhotoCdnUrl;
            if (message.searchProviderUrl != null && Object.hasOwnProperty.call(message, "searchProviderUrl"))
                object.searchProviderUrl = message.searchProviderUrl;
            if (message.referenceIndex != null && Object.hasOwnProperty.call(message, "referenceIndex"))
                object.referenceIndex = message.referenceIndex;
            if (message.expectedLinksCount != null && Object.hasOwnProperty.call(message, "expectedLinksCount"))
                object.expectedLinksCount = message.expectedLinksCount;
            if (message.searchQuery != null && Object.hasOwnProperty.call(message, "searchQuery"))
                object.searchQuery = message.searchQuery;
            if (message.parentPluginMessageKey != null && Object.hasOwnProperty.call(message, "parentPluginMessageKey"))
                object.parentPluginMessageKey = $root.proto.MessageKey.toObject(message.parentPluginMessageKey, options, q + 1);
            if (message.deprecatedField != null && Object.hasOwnProperty.call(message, "deprecatedField"))
                object.deprecatedField = options.enums === String ? $root.proto.BotPluginMetadata.PluginType[message.deprecatedField] === undefined ? message.deprecatedField : $root.proto.BotPluginMetadata.PluginType[message.deprecatedField] : message.deprecatedField;
            if (message.parentPluginType != null && Object.hasOwnProperty.call(message, "parentPluginType"))
                object.parentPluginType = options.enums === String ? $root.proto.BotPluginMetadata.PluginType[message.parentPluginType] === undefined ? message.parentPluginType : $root.proto.BotPluginMetadata.PluginType[message.parentPluginType] : message.parentPluginType;
            if (message.faviconCdnUrl != null && Object.hasOwnProperty.call(message, "faviconCdnUrl"))
                object.faviconCdnUrl = message.faviconCdnUrl;
            return object;
        };

        /**
         * Converts this BotPluginMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotPluginMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotPluginMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotPluginMetadata
         * @function getTypeUrl
         * @memberof proto.BotPluginMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotPluginMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotPluginMetadata";
        };

        /**
         * PluginType enum.
         * @name proto.BotPluginMetadata.PluginType
         * @enum {number}
         * @property {number} UNKNOWN_PLUGIN=0 UNKNOWN_PLUGIN value
         * @property {number} REELS=1 REELS value
         * @property {number} SEARCH=2 SEARCH value
         */
        BotPluginMetadata.PluginType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_PLUGIN"] = 0;
            values[valuesById[1] = "REELS"] = 1;
            values[valuesById[2] = "SEARCH"] = 2;
            return values;
        })();

        /**
         * SearchProvider enum.
         * @name proto.BotPluginMetadata.SearchProvider
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} BING=1 BING value
         * @property {number} GOOGLE=2 GOOGLE value
         * @property {number} SUPPORT=3 SUPPORT value
         */
        BotPluginMetadata.SearchProvider = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "BING"] = 1;
            values[valuesById[2] = "GOOGLE"] = 2;
            values[valuesById[3] = "SUPPORT"] = 3;
            return values;
        })();

        return BotPluginMetadata;
    })();

    proto.BotProgressIndicatorMetadata = (function() {

        /**
         * Properties of a BotProgressIndicatorMetadata.
         * @memberof proto
         * @interface IBotProgressIndicatorMetadata
         * @property {string|null} [progressDescription] BotProgressIndicatorMetadata progressDescription
         * @property {Array.<proto.BotProgressIndicatorMetadata.IBotPlanningStepMetadata>|null} [stepsMetadata] BotProgressIndicatorMetadata stepsMetadata
         * @property {number|Long|null} [estimatedCompletionTime] BotProgressIndicatorMetadata estimatedCompletionTime
         */

        /**
         * Constructs a new BotProgressIndicatorMetadata.
         * @memberof proto
         * @classdesc Represents a BotProgressIndicatorMetadata.
         * @implements IBotProgressIndicatorMetadata
         * @constructor
         * @param {proto.IBotProgressIndicatorMetadata=} [properties] Properties to set
         */
        function BotProgressIndicatorMetadata(properties) {
            this.stepsMetadata = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotProgressIndicatorMetadata progressDescription.
         * @member {string|null|undefined} progressDescription
         * @memberof proto.BotProgressIndicatorMetadata
         * @instance
         */
        BotProgressIndicatorMetadata.prototype.progressDescription = null;

        /**
         * BotProgressIndicatorMetadata stepsMetadata.
         * @member {Array.<proto.BotProgressIndicatorMetadata.IBotPlanningStepMetadata>} stepsMetadata
         * @memberof proto.BotProgressIndicatorMetadata
         * @instance
         */
        BotProgressIndicatorMetadata.prototype.stepsMetadata = $util.emptyArray;

        /**
         * BotProgressIndicatorMetadata estimatedCompletionTime.
         * @member {number|Long|null|undefined} estimatedCompletionTime
         * @memberof proto.BotProgressIndicatorMetadata
         * @instance
         */
        BotProgressIndicatorMetadata.prototype.estimatedCompletionTime = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotProgressIndicatorMetadata.prototype, "_progressDescription", {
            get: $util.oneOfGetter($oneOfFields = ["progressDescription"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotProgressIndicatorMetadata.prototype, "_estimatedCompletionTime", {
            get: $util.oneOfGetter($oneOfFields = ["estimatedCompletionTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotProgressIndicatorMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotProgressIndicatorMetadata
         * @static
         * @param {proto.IBotProgressIndicatorMetadata=} [properties] Properties to set
         * @returns {proto.BotProgressIndicatorMetadata} BotProgressIndicatorMetadata instance
         */
        BotProgressIndicatorMetadata.create = function create(properties) {
            return new BotProgressIndicatorMetadata(properties);
        };

        /**
         * Encodes the specified BotProgressIndicatorMetadata message. Does not implicitly {@link proto.BotProgressIndicatorMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotProgressIndicatorMetadata
         * @static
         * @param {proto.IBotProgressIndicatorMetadata} message BotProgressIndicatorMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotProgressIndicatorMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.progressDescription != null && Object.hasOwnProperty.call(message, "progressDescription"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.progressDescription);
            if (message.stepsMetadata != null && message.stepsMetadata.length)
                for (var i = 0; i < message.stepsMetadata.length; ++i)
                    $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.encode(message.stepsMetadata[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.estimatedCompletionTime != null && Object.hasOwnProperty.call(message, "estimatedCompletionTime"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.estimatedCompletionTime);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotProgressIndicatorMetadata message, length delimited. Does not implicitly {@link proto.BotProgressIndicatorMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotProgressIndicatorMetadata
         * @static
         * @param {proto.IBotProgressIndicatorMetadata} message BotProgressIndicatorMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotProgressIndicatorMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotProgressIndicatorMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotProgressIndicatorMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotProgressIndicatorMetadata} BotProgressIndicatorMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotProgressIndicatorMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.progressDescription = reader.stringVerify();
                        message._progressDescription = "progressDescription";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(message.stepsMetadata && message.stepsMetadata.length))
                            message.stepsMetadata = [];
                        message.stepsMetadata.push($root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.estimatedCompletionTime = reader.int64();
                        message._estimatedCompletionTime = "estimatedCompletionTime";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotProgressIndicatorMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotProgressIndicatorMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotProgressIndicatorMetadata} BotProgressIndicatorMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotProgressIndicatorMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotProgressIndicatorMetadata message.
         * @function verify
         * @memberof proto.BotProgressIndicatorMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotProgressIndicatorMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.progressDescription != null && Object.hasOwnProperty.call(message, "progressDescription")) {
                properties._progressDescription = 1;
                if (!$util.isString(message.progressDescription))
                    return "progressDescription: string expected";
            }
            if (message.stepsMetadata != null && Object.hasOwnProperty.call(message, "stepsMetadata")) {
                if (!Array.isArray(message.stepsMetadata))
                    return "stepsMetadata: array expected";
                for (var i = 0; i < message.stepsMetadata.length; ++i) {
                    var error = $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.verify(message.stepsMetadata[i], q + 1);
                    if (error)
                        return "stepsMetadata." + error;
                }
            }
            if (message.estimatedCompletionTime != null && Object.hasOwnProperty.call(message, "estimatedCompletionTime")) {
                properties._estimatedCompletionTime = 1;
                if (!$util.isInteger(message.estimatedCompletionTime) && !(message.estimatedCompletionTime && $util.isInteger(message.estimatedCompletionTime.low) && $util.isInteger(message.estimatedCompletionTime.high)))
                    return "estimatedCompletionTime: integer|Long expected";
            }
            return null;
        };

        /**
         * Creates a BotProgressIndicatorMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotProgressIndicatorMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotProgressIndicatorMetadata} BotProgressIndicatorMetadata
         */
        BotProgressIndicatorMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotProgressIndicatorMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.progressDescription != null)
                message.progressDescription = String(object.progressDescription);
            if (object.stepsMetadata) {
                if (!Array.isArray(object.stepsMetadata))
                    throw TypeError(".proto.BotProgressIndicatorMetadata.stepsMetadata: array expected");
                message.stepsMetadata = Array(object.stepsMetadata.length);
                for (var i = 0; i < object.stepsMetadata.length; ++i) {
                    if (!$util.isObject(object.stepsMetadata[i]))
                        throw TypeError(".proto.BotProgressIndicatorMetadata.stepsMetadata: object expected");
                    message.stepsMetadata[i] = $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.fromObject(object.stepsMetadata[i], q + 1);
                }
            }
            if (object.estimatedCompletionTime != null)
                if ($util.Long)
                    message.estimatedCompletionTime = $util.Long.fromValue(object.estimatedCompletionTime, false);
                else if (typeof object.estimatedCompletionTime === "string")
                    message.estimatedCompletionTime = parseInt(object.estimatedCompletionTime, 10);
                else if (typeof object.estimatedCompletionTime === "number")
                    message.estimatedCompletionTime = object.estimatedCompletionTime;
                else if (typeof object.estimatedCompletionTime === "object")
                    message.estimatedCompletionTime = new $util.LongBits(object.estimatedCompletionTime.low >>> 0, object.estimatedCompletionTime.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a BotProgressIndicatorMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotProgressIndicatorMetadata
         * @static
         * @param {proto.BotProgressIndicatorMetadata} message BotProgressIndicatorMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotProgressIndicatorMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.stepsMetadata = [];
            if (message.progressDescription != null && Object.hasOwnProperty.call(message, "progressDescription"))
                object.progressDescription = message.progressDescription;
            if (message.stepsMetadata && message.stepsMetadata.length) {
                object.stepsMetadata = Array(message.stepsMetadata.length);
                for (var j = 0; j < message.stepsMetadata.length; ++j)
                    object.stepsMetadata[j] = $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.toObject(message.stepsMetadata[j], options, q + 1);
            }
            if (message.estimatedCompletionTime != null && Object.hasOwnProperty.call(message, "estimatedCompletionTime"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.estimatedCompletionTime = typeof message.estimatedCompletionTime === "number" ? BigInt(message.estimatedCompletionTime) : $util.Long.fromBits(message.estimatedCompletionTime.low >>> 0, message.estimatedCompletionTime.high >>> 0, false).toBigInt();
                else if (typeof message.estimatedCompletionTime === "number")
                    object.estimatedCompletionTime = options.longs === String ? String(message.estimatedCompletionTime) : message.estimatedCompletionTime;
                else
                    object.estimatedCompletionTime = options.longs === String ? $util.Long.prototype.toString.call(message.estimatedCompletionTime) : options.longs === Number ? new $util.LongBits(message.estimatedCompletionTime.low >>> 0, message.estimatedCompletionTime.high >>> 0).toNumber() : message.estimatedCompletionTime;
            return object;
        };

        /**
         * Converts this BotProgressIndicatorMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotProgressIndicatorMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotProgressIndicatorMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotProgressIndicatorMetadata
         * @function getTypeUrl
         * @memberof proto.BotProgressIndicatorMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotProgressIndicatorMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotProgressIndicatorMetadata";
        };

        BotProgressIndicatorMetadata.BotPlanningStepMetadata = (function() {

            /**
             * Properties of a BotPlanningStepMetadata.
             * @memberof proto.BotProgressIndicatorMetadata
             * @interface IBotPlanningStepMetadata
             * @property {string|null} [statusTitle] BotPlanningStepMetadata statusTitle
             * @property {string|null} [statusBody] BotPlanningStepMetadata statusBody
             * @property {Array.<proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.IBotPlanningSearchSourcesMetadata>|null} [sourcesMetadata] BotPlanningStepMetadata sourcesMetadata
             * @property {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.PlanningStepStatus|null} [status] BotPlanningStepMetadata status
             * @property {boolean|null} [isReasoning] BotPlanningStepMetadata isReasoning
             * @property {boolean|null} [isEnhancedSearch] BotPlanningStepMetadata isEnhancedSearch
             * @property {Array.<proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.IBotPlanningStepSectionMetadata>|null} [sections] BotPlanningStepMetadata sections
             */

            /**
             * Constructs a new BotPlanningStepMetadata.
             * @memberof proto.BotProgressIndicatorMetadata
             * @classdesc Represents a BotPlanningStepMetadata.
             * @implements IBotPlanningStepMetadata
             * @constructor
             * @param {proto.BotProgressIndicatorMetadata.IBotPlanningStepMetadata=} [properties] Properties to set
             */
            function BotPlanningStepMetadata(properties) {
                this.sourcesMetadata = [];
                this.sections = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * BotPlanningStepMetadata statusTitle.
             * @member {string|null|undefined} statusTitle
             * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
             * @instance
             */
            BotPlanningStepMetadata.prototype.statusTitle = null;

            /**
             * BotPlanningStepMetadata statusBody.
             * @member {string|null|undefined} statusBody
             * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
             * @instance
             */
            BotPlanningStepMetadata.prototype.statusBody = null;

            /**
             * BotPlanningStepMetadata sourcesMetadata.
             * @member {Array.<proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.IBotPlanningSearchSourcesMetadata>} sourcesMetadata
             * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
             * @instance
             */
            BotPlanningStepMetadata.prototype.sourcesMetadata = $util.emptyArray;

            /**
             * BotPlanningStepMetadata status.
             * @member {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.PlanningStepStatus|null|undefined} status
             * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
             * @instance
             */
            BotPlanningStepMetadata.prototype.status = null;

            /**
             * BotPlanningStepMetadata isReasoning.
             * @member {boolean|null|undefined} isReasoning
             * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
             * @instance
             */
            BotPlanningStepMetadata.prototype.isReasoning = null;

            /**
             * BotPlanningStepMetadata isEnhancedSearch.
             * @member {boolean|null|undefined} isEnhancedSearch
             * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
             * @instance
             */
            BotPlanningStepMetadata.prototype.isEnhancedSearch = null;

            /**
             * BotPlanningStepMetadata sections.
             * @member {Array.<proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.IBotPlanningStepSectionMetadata>} sections
             * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
             * @instance
             */
            BotPlanningStepMetadata.prototype.sections = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotPlanningStepMetadata.prototype, "_statusTitle", {
                get: $util.oneOfGetter($oneOfFields = ["statusTitle"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotPlanningStepMetadata.prototype, "_statusBody", {
                get: $util.oneOfGetter($oneOfFields = ["statusBody"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotPlanningStepMetadata.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotPlanningStepMetadata.prototype, "_isReasoning", {
                get: $util.oneOfGetter($oneOfFields = ["isReasoning"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotPlanningStepMetadata.prototype, "_isEnhancedSearch", {
                get: $util.oneOfGetter($oneOfFields = ["isEnhancedSearch"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BotPlanningStepMetadata instance using the specified properties.
             * @function create
             * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
             * @static
             * @param {proto.BotProgressIndicatorMetadata.IBotPlanningStepMetadata=} [properties] Properties to set
             * @returns {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata} BotPlanningStepMetadata instance
             */
            BotPlanningStepMetadata.create = function create(properties) {
                return new BotPlanningStepMetadata(properties);
            };

            /**
             * Encodes the specified BotPlanningStepMetadata message. Does not implicitly {@link proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.verify|verify} messages.
             * @function encode
             * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
             * @static
             * @param {proto.BotProgressIndicatorMetadata.IBotPlanningStepMetadata} message BotPlanningStepMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BotPlanningStepMetadata.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.statusTitle != null && Object.hasOwnProperty.call(message, "statusTitle"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.statusTitle);
                if (message.statusBody != null && Object.hasOwnProperty.call(message, "statusBody"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.statusBody);
                if (message.sourcesMetadata != null && message.sourcesMetadata.length)
                    for (var i = 0; i < message.sourcesMetadata.length; ++i)
                        $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.encode(message.sourcesMetadata[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int32(message.status);
                if (message.isReasoning != null && Object.hasOwnProperty.call(message, "isReasoning"))
                    writer.uint32(/* id 5, wireType 0 =*/40).bool(message.isReasoning);
                if (message.isEnhancedSearch != null && Object.hasOwnProperty.call(message, "isEnhancedSearch"))
                    writer.uint32(/* id 6, wireType 0 =*/48).bool(message.isEnhancedSearch);
                if (message.sections != null && message.sections.length)
                    for (var i = 0; i < message.sections.length; ++i)
                        $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata.encode(message.sections[i], writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified BotPlanningStepMetadata message, length delimited. Does not implicitly {@link proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
             * @static
             * @param {proto.BotProgressIndicatorMetadata.IBotPlanningStepMetadata} message BotPlanningStepMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BotPlanningStepMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a BotPlanningStepMetadata message from the specified reader or buffer.
             * @function decode
             * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata} BotPlanningStepMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BotPlanningStepMetadata.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.statusTitle = reader.stringVerify();
                            message._statusTitle = "statusTitle";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.statusBody = reader.stringVerify();
                            message._statusBody = "statusBody";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            if (!(message.sourcesMetadata && message.sourcesMetadata.length))
                                message.sourcesMetadata = [];
                            message.sourcesMetadata.push($root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.status = reader.int32();
                            message._status = "status";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.isReasoning = reader.bool();
                            message._isReasoning = "isReasoning";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.isEnhancedSearch = reader.bool();
                            message._isEnhancedSearch = "isEnhancedSearch";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            if (!(message.sections && message.sections.length))
                                message.sections = [];
                            message.sections.push($root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a BotPlanningStepMetadata message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata} BotPlanningStepMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BotPlanningStepMetadata.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BotPlanningStepMetadata message.
             * @function verify
             * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BotPlanningStepMetadata.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.statusTitle != null && Object.hasOwnProperty.call(message, "statusTitle")) {
                    properties._statusTitle = 1;
                    if (!$util.isString(message.statusTitle))
                        return "statusTitle: string expected";
                }
                if (message.statusBody != null && Object.hasOwnProperty.call(message, "statusBody")) {
                    properties._statusBody = 1;
                    if (!$util.isString(message.statusBody))
                        return "statusBody: string expected";
                }
                if (message.sourcesMetadata != null && Object.hasOwnProperty.call(message, "sourcesMetadata")) {
                    if (!Array.isArray(message.sourcesMetadata))
                        return "sourcesMetadata: array expected";
                    for (var i = 0; i < message.sourcesMetadata.length; ++i) {
                        var error = $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.verify(message.sourcesMetadata[i], q + 1);
                        if (error)
                            return "sourcesMetadata." + error;
                    }
                }
                if (message.status != null && Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (typeof message.status !== "number" || (message.status | 0) !== message.status)
                        return "status: enum value expected";
                }
                if (message.isReasoning != null && Object.hasOwnProperty.call(message, "isReasoning")) {
                    properties._isReasoning = 1;
                    if (typeof message.isReasoning !== "boolean")
                        return "isReasoning: boolean expected";
                }
                if (message.isEnhancedSearch != null && Object.hasOwnProperty.call(message, "isEnhancedSearch")) {
                    properties._isEnhancedSearch = 1;
                    if (typeof message.isEnhancedSearch !== "boolean")
                        return "isEnhancedSearch: boolean expected";
                }
                if (message.sections != null && Object.hasOwnProperty.call(message, "sections")) {
                    if (!Array.isArray(message.sections))
                        return "sections: array expected";
                    for (var i = 0; i < message.sections.length; ++i) {
                        var error = $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata.verify(message.sections[i], q + 1);
                        if (error)
                            return "sections." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a BotPlanningStepMetadata message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata} BotPlanningStepMetadata
             */
            BotPlanningStepMetadata.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.statusTitle != null)
                    message.statusTitle = String(object.statusTitle);
                if (object.statusBody != null)
                    message.statusBody = String(object.statusBody);
                if (object.sourcesMetadata) {
                    if (!Array.isArray(object.sourcesMetadata))
                        throw TypeError(".proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.sourcesMetadata: array expected");
                    message.sourcesMetadata = Array(object.sourcesMetadata.length);
                    for (var i = 0; i < object.sourcesMetadata.length; ++i) {
                        if (!$util.isObject(object.sourcesMetadata[i]))
                            throw TypeError(".proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.sourcesMetadata: object expected");
                        message.sourcesMetadata[i] = $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.fromObject(object.sourcesMetadata[i], q + 1);
                    }
                }
                switch (object.status) {
                case "UNKNOWN":
                case 0:
                    message.status = 0;
                    break;
                case "PLANNED":
                case 1:
                    message.status = 1;
                    break;
                case "EXECUTING":
                case 2:
                    message.status = 2;
                    break;
                case "FINISHED":
                case 3:
                    message.status = 3;
                    break;
                default:
                    if (typeof object.status === "number" && (object.status | 0) === object.status)
                        message.status = object.status;
                }
                if (object.isReasoning != null)
                    message.isReasoning = Boolean(object.isReasoning);
                if (object.isEnhancedSearch != null)
                    message.isEnhancedSearch = Boolean(object.isEnhancedSearch);
                if (object.sections) {
                    if (!Array.isArray(object.sections))
                        throw TypeError(".proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.sections: array expected");
                    message.sections = Array(object.sections.length);
                    for (var i = 0; i < object.sections.length; ++i) {
                        if (!$util.isObject(object.sections[i]))
                            throw TypeError(".proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.sections: object expected");
                        message.sections[i] = $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata.fromObject(object.sections[i], q + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a BotPlanningStepMetadata message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
             * @static
             * @param {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata} message BotPlanningStepMetadata
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BotPlanningStepMetadata.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.sourcesMetadata = [];
                    object.sections = [];
                }
                if (message.statusTitle != null && Object.hasOwnProperty.call(message, "statusTitle"))
                    object.statusTitle = message.statusTitle;
                if (message.statusBody != null && Object.hasOwnProperty.call(message, "statusBody"))
                    object.statusBody = message.statusBody;
                if (message.sourcesMetadata && message.sourcesMetadata.length) {
                    object.sourcesMetadata = Array(message.sourcesMetadata.length);
                    for (var j = 0; j < message.sourcesMetadata.length; ++j)
                        object.sourcesMetadata[j] = $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.toObject(message.sourcesMetadata[j], options, q + 1);
                }
                if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                    object.status = options.enums === String ? $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.PlanningStepStatus[message.status] === undefined ? message.status : $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.PlanningStepStatus[message.status] : message.status;
                if (message.isReasoning != null && Object.hasOwnProperty.call(message, "isReasoning"))
                    object.isReasoning = message.isReasoning;
                if (message.isEnhancedSearch != null && Object.hasOwnProperty.call(message, "isEnhancedSearch"))
                    object.isEnhancedSearch = message.isEnhancedSearch;
                if (message.sections && message.sections.length) {
                    object.sections = Array(message.sections.length);
                    for (var j = 0; j < message.sections.length; ++j)
                        object.sections[j] = $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata.toObject(message.sections[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this BotPlanningStepMetadata to JSON.
             * @function toJSON
             * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BotPlanningStepMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for BotPlanningStepMetadata
             * @function getTypeUrl
             * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            BotPlanningStepMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata";
            };

            BotPlanningStepMetadata.BotPlanningSearchSourceMetadata = (function() {

                /**
                 * Properties of a BotPlanningSearchSourceMetadata.
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
                 * @interface IBotPlanningSearchSourceMetadata
                 * @property {string|null} [title] BotPlanningSearchSourceMetadata title
                 * @property {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotSearchSourceProvider|null} [provider] BotPlanningSearchSourceMetadata provider
                 * @property {string|null} [sourceUrl] BotPlanningSearchSourceMetadata sourceUrl
                 * @property {string|null} [favIconUrl] BotPlanningSearchSourceMetadata favIconUrl
                 */

                /**
                 * Constructs a new BotPlanningSearchSourceMetadata.
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
                 * @classdesc Represents a BotPlanningSearchSourceMetadata.
                 * @implements IBotPlanningSearchSourceMetadata
                 * @constructor
                 * @param {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.IBotPlanningSearchSourceMetadata=} [properties] Properties to set
                 */
                function BotPlanningSearchSourceMetadata(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * BotPlanningSearchSourceMetadata title.
                 * @member {string|null|undefined} title
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata
                 * @instance
                 */
                BotPlanningSearchSourceMetadata.prototype.title = null;

                /**
                 * BotPlanningSearchSourceMetadata provider.
                 * @member {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotSearchSourceProvider|null|undefined} provider
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata
                 * @instance
                 */
                BotPlanningSearchSourceMetadata.prototype.provider = null;

                /**
                 * BotPlanningSearchSourceMetadata sourceUrl.
                 * @member {string|null|undefined} sourceUrl
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata
                 * @instance
                 */
                BotPlanningSearchSourceMetadata.prototype.sourceUrl = null;

                /**
                 * BotPlanningSearchSourceMetadata favIconUrl.
                 * @member {string|null|undefined} favIconUrl
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata
                 * @instance
                 */
                BotPlanningSearchSourceMetadata.prototype.favIconUrl = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BotPlanningSearchSourceMetadata.prototype, "_title", {
                    get: $util.oneOfGetter($oneOfFields = ["title"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BotPlanningSearchSourceMetadata.prototype, "_provider", {
                    get: $util.oneOfGetter($oneOfFields = ["provider"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BotPlanningSearchSourceMetadata.prototype, "_sourceUrl", {
                    get: $util.oneOfGetter($oneOfFields = ["sourceUrl"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BotPlanningSearchSourceMetadata.prototype, "_favIconUrl", {
                    get: $util.oneOfGetter($oneOfFields = ["favIconUrl"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new BotPlanningSearchSourceMetadata instance using the specified properties.
                 * @function create
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata
                 * @static
                 * @param {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.IBotPlanningSearchSourceMetadata=} [properties] Properties to set
                 * @returns {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata} BotPlanningSearchSourceMetadata instance
                 */
                BotPlanningSearchSourceMetadata.create = function create(properties) {
                    return new BotPlanningSearchSourceMetadata(properties);
                };

                /**
                 * Encodes the specified BotPlanningSearchSourceMetadata message. Does not implicitly {@link proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata.verify|verify} messages.
                 * @function encode
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata
                 * @static
                 * @param {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.IBotPlanningSearchSourceMetadata} message BotPlanningSearchSourceMetadata message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BotPlanningSearchSourceMetadata.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.title);
                    if (message.provider != null && Object.hasOwnProperty.call(message, "provider"))
                        writer.uint32(/* id 2, wireType 0 =*/16).int32(message.provider);
                    if (message.sourceUrl != null && Object.hasOwnProperty.call(message, "sourceUrl"))
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.sourceUrl);
                    if (message.favIconUrl != null && Object.hasOwnProperty.call(message, "favIconUrl"))
                        writer.uint32(/* id 4, wireType 2 =*/34).string(message.favIconUrl);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified BotPlanningSearchSourceMetadata message, length delimited. Does not implicitly {@link proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata
                 * @static
                 * @param {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.IBotPlanningSearchSourceMetadata} message BotPlanningSearchSourceMetadata message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BotPlanningSearchSourceMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a BotPlanningSearchSourceMetadata message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata} BotPlanningSearchSourceMetadata
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BotPlanningSearchSourceMetadata.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.title = reader.stringVerify();
                                message._title = "title";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                message.provider = reader.int32();
                                message._provider = "provider";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.sourceUrl = reader.stringVerify();
                                message._sourceUrl = "sourceUrl";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                message.favIconUrl = reader.stringVerify();
                                message._favIconUrl = "favIconUrl";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a BotPlanningSearchSourceMetadata message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata} BotPlanningSearchSourceMetadata
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BotPlanningSearchSourceMetadata.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a BotPlanningSearchSourceMetadata message.
                 * @function verify
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                BotPlanningSearchSourceMetadata.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                        properties._title = 1;
                        if (!$util.isString(message.title))
                            return "title: string expected";
                    }
                    if (message.provider != null && Object.hasOwnProperty.call(message, "provider")) {
                        properties._provider = 1;
                        if (typeof message.provider !== "number" || (message.provider | 0) !== message.provider)
                            return "provider: enum value expected";
                    }
                    if (message.sourceUrl != null && Object.hasOwnProperty.call(message, "sourceUrl")) {
                        properties._sourceUrl = 1;
                        if (!$util.isString(message.sourceUrl))
                            return "sourceUrl: string expected";
                    }
                    if (message.favIconUrl != null && Object.hasOwnProperty.call(message, "favIconUrl")) {
                        properties._favIconUrl = 1;
                        if (!$util.isString(message.favIconUrl))
                            return "favIconUrl: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a BotPlanningSearchSourceMetadata message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata} BotPlanningSearchSourceMetadata
                 */
                BotPlanningSearchSourceMetadata.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.title != null)
                        message.title = String(object.title);
                    switch (object.provider) {
                    case "UNKNOWN_PROVIDER":
                    case 0:
                        message.provider = 0;
                        break;
                    case "OTHER":
                    case 1:
                        message.provider = 1;
                        break;
                    case "GOOGLE":
                    case 2:
                        message.provider = 2;
                        break;
                    case "BING":
                    case 3:
                        message.provider = 3;
                        break;
                    default:
                        if (typeof object.provider === "number" && (object.provider | 0) === object.provider)
                            message.provider = object.provider;
                    }
                    if (object.sourceUrl != null)
                        message.sourceUrl = String(object.sourceUrl);
                    if (object.favIconUrl != null)
                        message.favIconUrl = String(object.favIconUrl);
                    return message;
                };

                /**
                 * Creates a plain object from a BotPlanningSearchSourceMetadata message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata
                 * @static
                 * @param {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata} message BotPlanningSearchSourceMetadata
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                BotPlanningSearchSourceMetadata.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                        object.title = message.title;
                    if (message.provider != null && Object.hasOwnProperty.call(message, "provider"))
                        object.provider = options.enums === String ? $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotSearchSourceProvider[message.provider] === undefined ? message.provider : $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotSearchSourceProvider[message.provider] : message.provider;
                    if (message.sourceUrl != null && Object.hasOwnProperty.call(message, "sourceUrl"))
                        object.sourceUrl = message.sourceUrl;
                    if (message.favIconUrl != null && Object.hasOwnProperty.call(message, "favIconUrl"))
                        object.favIconUrl = message.favIconUrl;
                    return object;
                };

                /**
                 * Converts this BotPlanningSearchSourceMetadata to JSON.
                 * @function toJSON
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                BotPlanningSearchSourceMetadata.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for BotPlanningSearchSourceMetadata
                 * @function getTypeUrl
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                BotPlanningSearchSourceMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata";
                };

                return BotPlanningSearchSourceMetadata;
            })();

            BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata = (function() {

                /**
                 * Properties of a BotPlanningSearchSourcesMetadata.
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
                 * @interface IBotPlanningSearchSourcesMetadata
                 * @property {string|null} [sourceTitle] BotPlanningSearchSourcesMetadata sourceTitle
                 * @property {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.BotPlanningSearchSourceProvider|null} [provider] BotPlanningSearchSourcesMetadata provider
                 * @property {string|null} [sourceUrl] BotPlanningSearchSourcesMetadata sourceUrl
                 */

                /**
                 * Constructs a new BotPlanningSearchSourcesMetadata.
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
                 * @classdesc Represents a BotPlanningSearchSourcesMetadata.
                 * @implements IBotPlanningSearchSourcesMetadata
                 * @constructor
                 * @param {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.IBotPlanningSearchSourcesMetadata=} [properties] Properties to set
                 */
                function BotPlanningSearchSourcesMetadata(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * BotPlanningSearchSourcesMetadata sourceTitle.
                 * @member {string|null|undefined} sourceTitle
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata
                 * @instance
                 */
                BotPlanningSearchSourcesMetadata.prototype.sourceTitle = null;

                /**
                 * BotPlanningSearchSourcesMetadata provider.
                 * @member {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.BotPlanningSearchSourceProvider|null|undefined} provider
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata
                 * @instance
                 */
                BotPlanningSearchSourcesMetadata.prototype.provider = null;

                /**
                 * BotPlanningSearchSourcesMetadata sourceUrl.
                 * @member {string|null|undefined} sourceUrl
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata
                 * @instance
                 */
                BotPlanningSearchSourcesMetadata.prototype.sourceUrl = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BotPlanningSearchSourcesMetadata.prototype, "_sourceTitle", {
                    get: $util.oneOfGetter($oneOfFields = ["sourceTitle"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BotPlanningSearchSourcesMetadata.prototype, "_provider", {
                    get: $util.oneOfGetter($oneOfFields = ["provider"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BotPlanningSearchSourcesMetadata.prototype, "_sourceUrl", {
                    get: $util.oneOfGetter($oneOfFields = ["sourceUrl"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new BotPlanningSearchSourcesMetadata instance using the specified properties.
                 * @function create
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata
                 * @static
                 * @param {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.IBotPlanningSearchSourcesMetadata=} [properties] Properties to set
                 * @returns {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata} BotPlanningSearchSourcesMetadata instance
                 */
                BotPlanningSearchSourcesMetadata.create = function create(properties) {
                    return new BotPlanningSearchSourcesMetadata(properties);
                };

                /**
                 * Encodes the specified BotPlanningSearchSourcesMetadata message. Does not implicitly {@link proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.verify|verify} messages.
                 * @function encode
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata
                 * @static
                 * @param {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.IBotPlanningSearchSourcesMetadata} message BotPlanningSearchSourcesMetadata message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BotPlanningSearchSourcesMetadata.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.sourceTitle != null && Object.hasOwnProperty.call(message, "sourceTitle"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.sourceTitle);
                    if (message.provider != null && Object.hasOwnProperty.call(message, "provider"))
                        writer.uint32(/* id 2, wireType 0 =*/16).int32(message.provider);
                    if (message.sourceUrl != null && Object.hasOwnProperty.call(message, "sourceUrl"))
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.sourceUrl);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified BotPlanningSearchSourcesMetadata message, length delimited. Does not implicitly {@link proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata
                 * @static
                 * @param {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.IBotPlanningSearchSourcesMetadata} message BotPlanningSearchSourcesMetadata message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BotPlanningSearchSourcesMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a BotPlanningSearchSourcesMetadata message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata} BotPlanningSearchSourcesMetadata
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BotPlanningSearchSourcesMetadata.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.sourceTitle = reader.stringVerify();
                                message._sourceTitle = "sourceTitle";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                message.provider = reader.int32();
                                message._provider = "provider";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.sourceUrl = reader.stringVerify();
                                message._sourceUrl = "sourceUrl";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a BotPlanningSearchSourcesMetadata message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata} BotPlanningSearchSourcesMetadata
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BotPlanningSearchSourcesMetadata.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a BotPlanningSearchSourcesMetadata message.
                 * @function verify
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                BotPlanningSearchSourcesMetadata.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.sourceTitle != null && Object.hasOwnProperty.call(message, "sourceTitle")) {
                        properties._sourceTitle = 1;
                        if (!$util.isString(message.sourceTitle))
                            return "sourceTitle: string expected";
                    }
                    if (message.provider != null && Object.hasOwnProperty.call(message, "provider")) {
                        properties._provider = 1;
                        if (typeof message.provider !== "number" || (message.provider | 0) !== message.provider)
                            return "provider: enum value expected";
                    }
                    if (message.sourceUrl != null && Object.hasOwnProperty.call(message, "sourceUrl")) {
                        properties._sourceUrl = 1;
                        if (!$util.isString(message.sourceUrl))
                            return "sourceUrl: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a BotPlanningSearchSourcesMetadata message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata} BotPlanningSearchSourcesMetadata
                 */
                BotPlanningSearchSourcesMetadata.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.sourceTitle != null)
                        message.sourceTitle = String(object.sourceTitle);
                    switch (object.provider) {
                    case "UNKNOWN":
                    case 0:
                        message.provider = 0;
                        break;
                    case "OTHER":
                    case 1:
                        message.provider = 1;
                        break;
                    case "GOOGLE":
                    case 2:
                        message.provider = 2;
                        break;
                    case "BING":
                    case 3:
                        message.provider = 3;
                        break;
                    default:
                        if (typeof object.provider === "number" && (object.provider | 0) === object.provider)
                            message.provider = object.provider;
                    }
                    if (object.sourceUrl != null)
                        message.sourceUrl = String(object.sourceUrl);
                    return message;
                };

                /**
                 * Creates a plain object from a BotPlanningSearchSourcesMetadata message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata
                 * @static
                 * @param {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata} message BotPlanningSearchSourcesMetadata
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                BotPlanningSearchSourcesMetadata.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.sourceTitle != null && Object.hasOwnProperty.call(message, "sourceTitle"))
                        object.sourceTitle = message.sourceTitle;
                    if (message.provider != null && Object.hasOwnProperty.call(message, "provider"))
                        object.provider = options.enums === String ? $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.BotPlanningSearchSourceProvider[message.provider] === undefined ? message.provider : $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.BotPlanningSearchSourceProvider[message.provider] : message.provider;
                    if (message.sourceUrl != null && Object.hasOwnProperty.call(message, "sourceUrl"))
                        object.sourceUrl = message.sourceUrl;
                    return object;
                };

                /**
                 * Converts this BotPlanningSearchSourcesMetadata to JSON.
                 * @function toJSON
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                BotPlanningSearchSourcesMetadata.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for BotPlanningSearchSourcesMetadata
                 * @function getTypeUrl
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                BotPlanningSearchSourcesMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata";
                };

                /**
                 * BotPlanningSearchSourceProvider enum.
                 * @name proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.BotPlanningSearchSourceProvider
                 * @enum {number}
                 * @property {number} UNKNOWN=0 UNKNOWN value
                 * @property {number} OTHER=1 OTHER value
                 * @property {number} GOOGLE=2 GOOGLE value
                 * @property {number} BING=3 BING value
                 */
                BotPlanningSearchSourcesMetadata.BotPlanningSearchSourceProvider = (function() {
                    var valuesById = {}, values = Object.create(valuesById);
                    values[valuesById[0] = "UNKNOWN"] = 0;
                    values[valuesById[1] = "OTHER"] = 1;
                    values[valuesById[2] = "GOOGLE"] = 2;
                    values[valuesById[3] = "BING"] = 3;
                    return values;
                })();

                return BotPlanningSearchSourcesMetadata;
            })();

            BotPlanningStepMetadata.BotPlanningStepSectionMetadata = (function() {

                /**
                 * Properties of a BotPlanningStepSectionMetadata.
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
                 * @interface IBotPlanningStepSectionMetadata
                 * @property {string|null} [sectionTitle] BotPlanningStepSectionMetadata sectionTitle
                 * @property {string|null} [sectionBody] BotPlanningStepSectionMetadata sectionBody
                 * @property {Array.<proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.IBotPlanningSearchSourceMetadata>|null} [sourcesMetadata] BotPlanningStepSectionMetadata sourcesMetadata
                 */

                /**
                 * Constructs a new BotPlanningStepSectionMetadata.
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata
                 * @classdesc Represents a BotPlanningStepSectionMetadata.
                 * @implements IBotPlanningStepSectionMetadata
                 * @constructor
                 * @param {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.IBotPlanningStepSectionMetadata=} [properties] Properties to set
                 */
                function BotPlanningStepSectionMetadata(properties) {
                    this.sourcesMetadata = [];
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * BotPlanningStepSectionMetadata sectionTitle.
                 * @member {string|null|undefined} sectionTitle
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata
                 * @instance
                 */
                BotPlanningStepSectionMetadata.prototype.sectionTitle = null;

                /**
                 * BotPlanningStepSectionMetadata sectionBody.
                 * @member {string|null|undefined} sectionBody
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata
                 * @instance
                 */
                BotPlanningStepSectionMetadata.prototype.sectionBody = null;

                /**
                 * BotPlanningStepSectionMetadata sourcesMetadata.
                 * @member {Array.<proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.IBotPlanningSearchSourceMetadata>} sourcesMetadata
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata
                 * @instance
                 */
                BotPlanningStepSectionMetadata.prototype.sourcesMetadata = $util.emptyArray;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BotPlanningStepSectionMetadata.prototype, "_sectionTitle", {
                    get: $util.oneOfGetter($oneOfFields = ["sectionTitle"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BotPlanningStepSectionMetadata.prototype, "_sectionBody", {
                    get: $util.oneOfGetter($oneOfFields = ["sectionBody"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new BotPlanningStepSectionMetadata instance using the specified properties.
                 * @function create
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata
                 * @static
                 * @param {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.IBotPlanningStepSectionMetadata=} [properties] Properties to set
                 * @returns {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata} BotPlanningStepSectionMetadata instance
                 */
                BotPlanningStepSectionMetadata.create = function create(properties) {
                    return new BotPlanningStepSectionMetadata(properties);
                };

                /**
                 * Encodes the specified BotPlanningStepSectionMetadata message. Does not implicitly {@link proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata.verify|verify} messages.
                 * @function encode
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata
                 * @static
                 * @param {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.IBotPlanningStepSectionMetadata} message BotPlanningStepSectionMetadata message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BotPlanningStepSectionMetadata.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.sectionTitle != null && Object.hasOwnProperty.call(message, "sectionTitle"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.sectionTitle);
                    if (message.sectionBody != null && Object.hasOwnProperty.call(message, "sectionBody"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.sectionBody);
                    if (message.sourcesMetadata != null && message.sourcesMetadata.length)
                        for (var i = 0; i < message.sourcesMetadata.length; ++i)
                            $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata.encode(message.sourcesMetadata[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified BotPlanningStepSectionMetadata message, length delimited. Does not implicitly {@link proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata
                 * @static
                 * @param {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.IBotPlanningStepSectionMetadata} message BotPlanningStepSectionMetadata message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BotPlanningStepSectionMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a BotPlanningStepSectionMetadata message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata} BotPlanningStepSectionMetadata
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BotPlanningStepSectionMetadata.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.sectionTitle = reader.stringVerify();
                                message._sectionTitle = "sectionTitle";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.sectionBody = reader.stringVerify();
                                message._sectionBody = "sectionBody";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                if (!(message.sourcesMetadata && message.sourcesMetadata.length))
                                    message.sourcesMetadata = [];
                                message.sourcesMetadata.push($root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata.decode(reader, reader.uint32(), undefined, q + 1));
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a BotPlanningStepSectionMetadata message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata} BotPlanningStepSectionMetadata
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BotPlanningStepSectionMetadata.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a BotPlanningStepSectionMetadata message.
                 * @function verify
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                BotPlanningStepSectionMetadata.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.sectionTitle != null && Object.hasOwnProperty.call(message, "sectionTitle")) {
                        properties._sectionTitle = 1;
                        if (!$util.isString(message.sectionTitle))
                            return "sectionTitle: string expected";
                    }
                    if (message.sectionBody != null && Object.hasOwnProperty.call(message, "sectionBody")) {
                        properties._sectionBody = 1;
                        if (!$util.isString(message.sectionBody))
                            return "sectionBody: string expected";
                    }
                    if (message.sourcesMetadata != null && Object.hasOwnProperty.call(message, "sourcesMetadata")) {
                        if (!Array.isArray(message.sourcesMetadata))
                            return "sourcesMetadata: array expected";
                        for (var i = 0; i < message.sourcesMetadata.length; ++i) {
                            var error = $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata.verify(message.sourcesMetadata[i], q + 1);
                            if (error)
                                return "sourcesMetadata." + error;
                        }
                    }
                    return null;
                };

                /**
                 * Creates a BotPlanningStepSectionMetadata message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata} BotPlanningStepSectionMetadata
                 */
                BotPlanningStepSectionMetadata.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.sectionTitle != null)
                        message.sectionTitle = String(object.sectionTitle);
                    if (object.sectionBody != null)
                        message.sectionBody = String(object.sectionBody);
                    if (object.sourcesMetadata) {
                        if (!Array.isArray(object.sourcesMetadata))
                            throw TypeError(".proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata.sourcesMetadata: array expected");
                        message.sourcesMetadata = Array(object.sourcesMetadata.length);
                        for (var i = 0; i < object.sourcesMetadata.length; ++i) {
                            if (!$util.isObject(object.sourcesMetadata[i]))
                                throw TypeError(".proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata.sourcesMetadata: object expected");
                            message.sourcesMetadata[i] = $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata.fromObject(object.sourcesMetadata[i], q + 1);
                        }
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a BotPlanningStepSectionMetadata message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata
                 * @static
                 * @param {proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata} message BotPlanningStepSectionMetadata
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                BotPlanningStepSectionMetadata.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (options.arrays || options.defaults)
                        object.sourcesMetadata = [];
                    if (message.sectionTitle != null && Object.hasOwnProperty.call(message, "sectionTitle"))
                        object.sectionTitle = message.sectionTitle;
                    if (message.sectionBody != null && Object.hasOwnProperty.call(message, "sectionBody"))
                        object.sectionBody = message.sectionBody;
                    if (message.sourcesMetadata && message.sourcesMetadata.length) {
                        object.sourcesMetadata = Array(message.sourcesMetadata.length);
                        for (var j = 0; j < message.sourcesMetadata.length; ++j)
                            object.sourcesMetadata[j] = $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata.toObject(message.sourcesMetadata[j], options, q + 1);
                    }
                    return object;
                };

                /**
                 * Converts this BotPlanningStepSectionMetadata to JSON.
                 * @function toJSON
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                BotPlanningStepSectionMetadata.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for BotPlanningStepSectionMetadata
                 * @function getTypeUrl
                 * @memberof proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                BotPlanningStepSectionMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata";
                };

                return BotPlanningStepSectionMetadata;
            })();

            /**
             * BotSearchSourceProvider enum.
             * @name proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotSearchSourceProvider
             * @enum {number}
             * @property {number} UNKNOWN_PROVIDER=0 UNKNOWN_PROVIDER value
             * @property {number} OTHER=1 OTHER value
             * @property {number} GOOGLE=2 GOOGLE value
             * @property {number} BING=3 BING value
             */
            BotPlanningStepMetadata.BotSearchSourceProvider = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN_PROVIDER"] = 0;
                values[valuesById[1] = "OTHER"] = 1;
                values[valuesById[2] = "GOOGLE"] = 2;
                values[valuesById[3] = "BING"] = 3;
                return values;
            })();

            /**
             * PlanningStepStatus enum.
             * @name proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.PlanningStepStatus
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} PLANNED=1 PLANNED value
             * @property {number} EXECUTING=2 EXECUTING value
             * @property {number} FINISHED=3 FINISHED value
             */
            BotPlanningStepMetadata.PlanningStepStatus = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "PLANNED"] = 1;
                values[valuesById[2] = "EXECUTING"] = 2;
                values[valuesById[3] = "FINISHED"] = 3;
                return values;
            })();

            return BotPlanningStepMetadata;
        })();

        return BotProgressIndicatorMetadata;
    })();

    proto.BotPromotionMessageMetadata = (function() {

        /**
         * Properties of a BotPromotionMessageMetadata.
         * @memberof proto
         * @interface IBotPromotionMessageMetadata
         * @property {proto.BotPromotionMessageMetadata.BotPromotionType|null} [promotionType] BotPromotionMessageMetadata promotionType
         * @property {string|null} [buttonTitle] BotPromotionMessageMetadata buttonTitle
         */

        /**
         * Constructs a new BotPromotionMessageMetadata.
         * @memberof proto
         * @classdesc Represents a BotPromotionMessageMetadata.
         * @implements IBotPromotionMessageMetadata
         * @constructor
         * @param {proto.IBotPromotionMessageMetadata=} [properties] Properties to set
         */
        function BotPromotionMessageMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotPromotionMessageMetadata promotionType.
         * @member {proto.BotPromotionMessageMetadata.BotPromotionType|null|undefined} promotionType
         * @memberof proto.BotPromotionMessageMetadata
         * @instance
         */
        BotPromotionMessageMetadata.prototype.promotionType = null;

        /**
         * BotPromotionMessageMetadata buttonTitle.
         * @member {string|null|undefined} buttonTitle
         * @memberof proto.BotPromotionMessageMetadata
         * @instance
         */
        BotPromotionMessageMetadata.prototype.buttonTitle = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPromotionMessageMetadata.prototype, "_promotionType", {
            get: $util.oneOfGetter($oneOfFields = ["promotionType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPromotionMessageMetadata.prototype, "_buttonTitle", {
            get: $util.oneOfGetter($oneOfFields = ["buttonTitle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotPromotionMessageMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotPromotionMessageMetadata
         * @static
         * @param {proto.IBotPromotionMessageMetadata=} [properties] Properties to set
         * @returns {proto.BotPromotionMessageMetadata} BotPromotionMessageMetadata instance
         */
        BotPromotionMessageMetadata.create = function create(properties) {
            return new BotPromotionMessageMetadata(properties);
        };

        /**
         * Encodes the specified BotPromotionMessageMetadata message. Does not implicitly {@link proto.BotPromotionMessageMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotPromotionMessageMetadata
         * @static
         * @param {proto.IBotPromotionMessageMetadata} message BotPromotionMessageMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotPromotionMessageMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.promotionType != null && Object.hasOwnProperty.call(message, "promotionType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.promotionType);
            if (message.buttonTitle != null && Object.hasOwnProperty.call(message, "buttonTitle"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.buttonTitle);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotPromotionMessageMetadata message, length delimited. Does not implicitly {@link proto.BotPromotionMessageMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotPromotionMessageMetadata
         * @static
         * @param {proto.IBotPromotionMessageMetadata} message BotPromotionMessageMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotPromotionMessageMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotPromotionMessageMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotPromotionMessageMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotPromotionMessageMetadata} BotPromotionMessageMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotPromotionMessageMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.promotionType = reader.int32();
                        message._promotionType = "promotionType";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.buttonTitle = reader.stringVerify();
                        message._buttonTitle = "buttonTitle";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotPromotionMessageMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotPromotionMessageMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotPromotionMessageMetadata} BotPromotionMessageMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotPromotionMessageMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotPromotionMessageMetadata message.
         * @function verify
         * @memberof proto.BotPromotionMessageMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotPromotionMessageMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.promotionType != null && Object.hasOwnProperty.call(message, "promotionType")) {
                properties._promotionType = 1;
                if (typeof message.promotionType !== "number" || (message.promotionType | 0) !== message.promotionType)
                    return "promotionType: enum value expected";
            }
            if (message.buttonTitle != null && Object.hasOwnProperty.call(message, "buttonTitle")) {
                properties._buttonTitle = 1;
                if (!$util.isString(message.buttonTitle))
                    return "buttonTitle: string expected";
            }
            return null;
        };

        /**
         * Creates a BotPromotionMessageMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotPromotionMessageMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotPromotionMessageMetadata} BotPromotionMessageMetadata
         */
        BotPromotionMessageMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotPromotionMessageMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.promotionType) {
            case "UNKNOWN_TYPE":
            case 0:
                message.promotionType = 0;
                break;
            case "C50":
            case 1:
                message.promotionType = 1;
                break;
            case "SURVEY_PLATFORM":
            case 2:
                message.promotionType = 2;
                break;
            default:
                if (typeof object.promotionType === "number" && (object.promotionType | 0) === object.promotionType)
                    message.promotionType = object.promotionType;
            }
            if (object.buttonTitle != null)
                message.buttonTitle = String(object.buttonTitle);
            return message;
        };

        /**
         * Creates a plain object from a BotPromotionMessageMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotPromotionMessageMetadata
         * @static
         * @param {proto.BotPromotionMessageMetadata} message BotPromotionMessageMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotPromotionMessageMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.promotionType != null && Object.hasOwnProperty.call(message, "promotionType"))
                object.promotionType = options.enums === String ? $root.proto.BotPromotionMessageMetadata.BotPromotionType[message.promotionType] === undefined ? message.promotionType : $root.proto.BotPromotionMessageMetadata.BotPromotionType[message.promotionType] : message.promotionType;
            if (message.buttonTitle != null && Object.hasOwnProperty.call(message, "buttonTitle"))
                object.buttonTitle = message.buttonTitle;
            return object;
        };

        /**
         * Converts this BotPromotionMessageMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotPromotionMessageMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotPromotionMessageMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotPromotionMessageMetadata
         * @function getTypeUrl
         * @memberof proto.BotPromotionMessageMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotPromotionMessageMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotPromotionMessageMetadata";
        };

        /**
         * BotPromotionType enum.
         * @name proto.BotPromotionMessageMetadata.BotPromotionType
         * @enum {number}
         * @property {number} UNKNOWN_TYPE=0 UNKNOWN_TYPE value
         * @property {number} C50=1 C50 value
         * @property {number} SURVEY_PLATFORM=2 SURVEY_PLATFORM value
         */
        BotPromotionMessageMetadata.BotPromotionType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_TYPE"] = 0;
            values[valuesById[1] = "C50"] = 1;
            values[valuesById[2] = "SURVEY_PLATFORM"] = 2;
            return values;
        })();

        return BotPromotionMessageMetadata;
    })();

    proto.BotPromptSuggestion = (function() {

        /**
         * Properties of a BotPromptSuggestion.
         * @memberof proto
         * @interface IBotPromptSuggestion
         * @property {string|null} [prompt] BotPromptSuggestion prompt
         * @property {string|null} [promptId] BotPromptSuggestion promptId
         */

        /**
         * Constructs a new BotPromptSuggestion.
         * @memberof proto
         * @classdesc Represents a BotPromptSuggestion.
         * @implements IBotPromptSuggestion
         * @constructor
         * @param {proto.IBotPromptSuggestion=} [properties] Properties to set
         */
        function BotPromptSuggestion(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotPromptSuggestion prompt.
         * @member {string|null|undefined} prompt
         * @memberof proto.BotPromptSuggestion
         * @instance
         */
        BotPromptSuggestion.prototype.prompt = null;

        /**
         * BotPromptSuggestion promptId.
         * @member {string|null|undefined} promptId
         * @memberof proto.BotPromptSuggestion
         * @instance
         */
        BotPromptSuggestion.prototype.promptId = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPromptSuggestion.prototype, "_prompt", {
            get: $util.oneOfGetter($oneOfFields = ["prompt"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPromptSuggestion.prototype, "_promptId", {
            get: $util.oneOfGetter($oneOfFields = ["promptId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotPromptSuggestion instance using the specified properties.
         * @function create
         * @memberof proto.BotPromptSuggestion
         * @static
         * @param {proto.IBotPromptSuggestion=} [properties] Properties to set
         * @returns {proto.BotPromptSuggestion} BotPromptSuggestion instance
         */
        BotPromptSuggestion.create = function create(properties) {
            return new BotPromptSuggestion(properties);
        };

        /**
         * Encodes the specified BotPromptSuggestion message. Does not implicitly {@link proto.BotPromptSuggestion.verify|verify} messages.
         * @function encode
         * @memberof proto.BotPromptSuggestion
         * @static
         * @param {proto.IBotPromptSuggestion} message BotPromptSuggestion message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotPromptSuggestion.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.prompt != null && Object.hasOwnProperty.call(message, "prompt"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.prompt);
            if (message.promptId != null && Object.hasOwnProperty.call(message, "promptId"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.promptId);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotPromptSuggestion message, length delimited. Does not implicitly {@link proto.BotPromptSuggestion.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotPromptSuggestion
         * @static
         * @param {proto.IBotPromptSuggestion} message BotPromptSuggestion message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotPromptSuggestion.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotPromptSuggestion message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotPromptSuggestion
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotPromptSuggestion} BotPromptSuggestion
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotPromptSuggestion.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.prompt = reader.stringVerify();
                        message._prompt = "prompt";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.promptId = reader.stringVerify();
                        message._promptId = "promptId";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotPromptSuggestion message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotPromptSuggestion
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotPromptSuggestion} BotPromptSuggestion
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotPromptSuggestion.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotPromptSuggestion message.
         * @function verify
         * @memberof proto.BotPromptSuggestion
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotPromptSuggestion.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.prompt != null && Object.hasOwnProperty.call(message, "prompt")) {
                properties._prompt = 1;
                if (!$util.isString(message.prompt))
                    return "prompt: string expected";
            }
            if (message.promptId != null && Object.hasOwnProperty.call(message, "promptId")) {
                properties._promptId = 1;
                if (!$util.isString(message.promptId))
                    return "promptId: string expected";
            }
            return null;
        };

        /**
         * Creates a BotPromptSuggestion message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotPromptSuggestion
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotPromptSuggestion} BotPromptSuggestion
         */
        BotPromptSuggestion.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotPromptSuggestion: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.prompt != null)
                message.prompt = String(object.prompt);
            if (object.promptId != null)
                message.promptId = String(object.promptId);
            return message;
        };

        /**
         * Creates a plain object from a BotPromptSuggestion message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotPromptSuggestion
         * @static
         * @param {proto.BotPromptSuggestion} message BotPromptSuggestion
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotPromptSuggestion.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.prompt != null && Object.hasOwnProperty.call(message, "prompt"))
                object.prompt = message.prompt;
            if (message.promptId != null && Object.hasOwnProperty.call(message, "promptId"))
                object.promptId = message.promptId;
            return object;
        };

        /**
         * Converts this BotPromptSuggestion to JSON.
         * @function toJSON
         * @memberof proto.BotPromptSuggestion
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotPromptSuggestion.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotPromptSuggestion
         * @function getTypeUrl
         * @memberof proto.BotPromptSuggestion
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotPromptSuggestion.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotPromptSuggestion";
        };

        return BotPromptSuggestion;
    })();

    proto.BotPromptSuggestions = (function() {

        /**
         * Properties of a BotPromptSuggestions.
         * @memberof proto
         * @interface IBotPromptSuggestions
         * @property {Array.<proto.IBotPromptSuggestion>|null} [suggestions] BotPromptSuggestions suggestions
         */

        /**
         * Constructs a new BotPromptSuggestions.
         * @memberof proto
         * @classdesc Represents a BotPromptSuggestions.
         * @implements IBotPromptSuggestions
         * @constructor
         * @param {proto.IBotPromptSuggestions=} [properties] Properties to set
         */
        function BotPromptSuggestions(properties) {
            this.suggestions = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotPromptSuggestions suggestions.
         * @member {Array.<proto.IBotPromptSuggestion>} suggestions
         * @memberof proto.BotPromptSuggestions
         * @instance
         */
        BotPromptSuggestions.prototype.suggestions = $util.emptyArray;

        /**
         * Creates a new BotPromptSuggestions instance using the specified properties.
         * @function create
         * @memberof proto.BotPromptSuggestions
         * @static
         * @param {proto.IBotPromptSuggestions=} [properties] Properties to set
         * @returns {proto.BotPromptSuggestions} BotPromptSuggestions instance
         */
        BotPromptSuggestions.create = function create(properties) {
            return new BotPromptSuggestions(properties);
        };

        /**
         * Encodes the specified BotPromptSuggestions message. Does not implicitly {@link proto.BotPromptSuggestions.verify|verify} messages.
         * @function encode
         * @memberof proto.BotPromptSuggestions
         * @static
         * @param {proto.IBotPromptSuggestions} message BotPromptSuggestions message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotPromptSuggestions.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.suggestions != null && message.suggestions.length)
                for (var i = 0; i < message.suggestions.length; ++i)
                    $root.proto.BotPromptSuggestion.encode(message.suggestions[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotPromptSuggestions message, length delimited. Does not implicitly {@link proto.BotPromptSuggestions.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotPromptSuggestions
         * @static
         * @param {proto.IBotPromptSuggestions} message BotPromptSuggestions message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotPromptSuggestions.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotPromptSuggestions message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotPromptSuggestions
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotPromptSuggestions} BotPromptSuggestions
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotPromptSuggestions.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(message.suggestions && message.suggestions.length))
                            message.suggestions = [];
                        message.suggestions.push($root.proto.BotPromptSuggestion.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotPromptSuggestions message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotPromptSuggestions
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotPromptSuggestions} BotPromptSuggestions
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotPromptSuggestions.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotPromptSuggestions message.
         * @function verify
         * @memberof proto.BotPromptSuggestions
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotPromptSuggestions.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            if (message.suggestions != null && Object.hasOwnProperty.call(message, "suggestions")) {
                if (!Array.isArray(message.suggestions))
                    return "suggestions: array expected";
                for (var i = 0; i < message.suggestions.length; ++i) {
                    var error = $root.proto.BotPromptSuggestion.verify(message.suggestions[i], q + 1);
                    if (error)
                        return "suggestions." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BotPromptSuggestions message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotPromptSuggestions
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotPromptSuggestions} BotPromptSuggestions
         */
        BotPromptSuggestions.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotPromptSuggestions: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.suggestions) {
                if (!Array.isArray(object.suggestions))
                    throw TypeError(".proto.BotPromptSuggestions.suggestions: array expected");
                message.suggestions = Array(object.suggestions.length);
                for (var i = 0; i < object.suggestions.length; ++i) {
                    if (!$util.isObject(object.suggestions[i]))
                        throw TypeError(".proto.BotPromptSuggestions.suggestions: object expected");
                    message.suggestions[i] = $root.proto.BotPromptSuggestion.fromObject(object.suggestions[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a BotPromptSuggestions message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotPromptSuggestions
         * @static
         * @param {proto.BotPromptSuggestions} message BotPromptSuggestions
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotPromptSuggestions.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.suggestions = [];
            if (message.suggestions && message.suggestions.length) {
                object.suggestions = Array(message.suggestions.length);
                for (var j = 0; j < message.suggestions.length; ++j)
                    object.suggestions[j] = $root.proto.BotPromptSuggestion.toObject(message.suggestions[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this BotPromptSuggestions to JSON.
         * @function toJSON
         * @memberof proto.BotPromptSuggestions
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotPromptSuggestions.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotPromptSuggestions
         * @function getTypeUrl
         * @memberof proto.BotPromptSuggestions
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotPromptSuggestions.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotPromptSuggestions";
        };

        return BotPromptSuggestions;
    })();

    proto.BotPttPromptMetadata = (function() {

        /**
         * Properties of a BotPttPromptMetadata.
         * @memberof proto
         * @interface IBotPttPromptMetadata
         * @property {string|null} [transcript] BotPttPromptMetadata transcript
         */

        /**
         * Constructs a new BotPttPromptMetadata.
         * @memberof proto
         * @classdesc Represents a BotPttPromptMetadata.
         * @implements IBotPttPromptMetadata
         * @constructor
         * @param {proto.IBotPttPromptMetadata=} [properties] Properties to set
         */
        function BotPttPromptMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotPttPromptMetadata transcript.
         * @member {string|null|undefined} transcript
         * @memberof proto.BotPttPromptMetadata
         * @instance
         */
        BotPttPromptMetadata.prototype.transcript = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPttPromptMetadata.prototype, "_transcript", {
            get: $util.oneOfGetter($oneOfFields = ["transcript"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotPttPromptMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotPttPromptMetadata
         * @static
         * @param {proto.IBotPttPromptMetadata=} [properties] Properties to set
         * @returns {proto.BotPttPromptMetadata} BotPttPromptMetadata instance
         */
        BotPttPromptMetadata.create = function create(properties) {
            return new BotPttPromptMetadata(properties);
        };

        /**
         * Encodes the specified BotPttPromptMetadata message. Does not implicitly {@link proto.BotPttPromptMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotPttPromptMetadata
         * @static
         * @param {proto.IBotPttPromptMetadata} message BotPttPromptMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotPttPromptMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.transcript != null && Object.hasOwnProperty.call(message, "transcript"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.transcript);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotPttPromptMetadata message, length delimited. Does not implicitly {@link proto.BotPttPromptMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotPttPromptMetadata
         * @static
         * @param {proto.IBotPttPromptMetadata} message BotPttPromptMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotPttPromptMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotPttPromptMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotPttPromptMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotPttPromptMetadata} BotPttPromptMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotPttPromptMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.transcript = reader.stringVerify();
                        message._transcript = "transcript";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotPttPromptMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotPttPromptMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotPttPromptMetadata} BotPttPromptMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotPttPromptMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotPttPromptMetadata message.
         * @function verify
         * @memberof proto.BotPttPromptMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotPttPromptMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.transcript != null && Object.hasOwnProperty.call(message, "transcript")) {
                properties._transcript = 1;
                if (!$util.isString(message.transcript))
                    return "transcript: string expected";
            }
            return null;
        };

        /**
         * Creates a BotPttPromptMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotPttPromptMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotPttPromptMetadata} BotPttPromptMetadata
         */
        BotPttPromptMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotPttPromptMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.transcript != null)
                message.transcript = String(object.transcript);
            return message;
        };

        /**
         * Creates a plain object from a BotPttPromptMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotPttPromptMetadata
         * @static
         * @param {proto.BotPttPromptMetadata} message BotPttPromptMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotPttPromptMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.transcript != null && Object.hasOwnProperty.call(message, "transcript"))
                object.transcript = message.transcript;
            return object;
        };

        /**
         * Converts this BotPttPromptMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotPttPromptMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotPttPromptMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotPttPromptMetadata
         * @function getTypeUrl
         * @memberof proto.BotPttPromptMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotPttPromptMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotPttPromptMetadata";
        };

        return BotPttPromptMetadata;
    })();

    proto.BotQuotaMetadata = (function() {

        /**
         * Properties of a BotQuotaMetadata.
         * @memberof proto
         * @interface IBotQuotaMetadata
         * @property {Array.<proto.BotQuotaMetadata.IBotFeatureQuotaMetadata>|null} [botFeatureQuotaMetadata] BotQuotaMetadata botFeatureQuotaMetadata
         */

        /**
         * Constructs a new BotQuotaMetadata.
         * @memberof proto
         * @classdesc Represents a BotQuotaMetadata.
         * @implements IBotQuotaMetadata
         * @constructor
         * @param {proto.IBotQuotaMetadata=} [properties] Properties to set
         */
        function BotQuotaMetadata(properties) {
            this.botFeatureQuotaMetadata = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotQuotaMetadata botFeatureQuotaMetadata.
         * @member {Array.<proto.BotQuotaMetadata.IBotFeatureQuotaMetadata>} botFeatureQuotaMetadata
         * @memberof proto.BotQuotaMetadata
         * @instance
         */
        BotQuotaMetadata.prototype.botFeatureQuotaMetadata = $util.emptyArray;

        /**
         * Creates a new BotQuotaMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotQuotaMetadata
         * @static
         * @param {proto.IBotQuotaMetadata=} [properties] Properties to set
         * @returns {proto.BotQuotaMetadata} BotQuotaMetadata instance
         */
        BotQuotaMetadata.create = function create(properties) {
            return new BotQuotaMetadata(properties);
        };

        /**
         * Encodes the specified BotQuotaMetadata message. Does not implicitly {@link proto.BotQuotaMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotQuotaMetadata
         * @static
         * @param {proto.IBotQuotaMetadata} message BotQuotaMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotQuotaMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.botFeatureQuotaMetadata != null && message.botFeatureQuotaMetadata.length)
                for (var i = 0; i < message.botFeatureQuotaMetadata.length; ++i)
                    $root.proto.BotQuotaMetadata.BotFeatureQuotaMetadata.encode(message.botFeatureQuotaMetadata[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotQuotaMetadata message, length delimited. Does not implicitly {@link proto.BotQuotaMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotQuotaMetadata
         * @static
         * @param {proto.IBotQuotaMetadata} message BotQuotaMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotQuotaMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotQuotaMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotQuotaMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotQuotaMetadata} BotQuotaMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotQuotaMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(message.botFeatureQuotaMetadata && message.botFeatureQuotaMetadata.length))
                            message.botFeatureQuotaMetadata = [];
                        message.botFeatureQuotaMetadata.push($root.proto.BotQuotaMetadata.BotFeatureQuotaMetadata.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotQuotaMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotQuotaMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotQuotaMetadata} BotQuotaMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotQuotaMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotQuotaMetadata message.
         * @function verify
         * @memberof proto.BotQuotaMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotQuotaMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            if (message.botFeatureQuotaMetadata != null && Object.hasOwnProperty.call(message, "botFeatureQuotaMetadata")) {
                if (!Array.isArray(message.botFeatureQuotaMetadata))
                    return "botFeatureQuotaMetadata: array expected";
                for (var i = 0; i < message.botFeatureQuotaMetadata.length; ++i) {
                    var error = $root.proto.BotQuotaMetadata.BotFeatureQuotaMetadata.verify(message.botFeatureQuotaMetadata[i], q + 1);
                    if (error)
                        return "botFeatureQuotaMetadata." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BotQuotaMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotQuotaMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotQuotaMetadata} BotQuotaMetadata
         */
        BotQuotaMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotQuotaMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.botFeatureQuotaMetadata) {
                if (!Array.isArray(object.botFeatureQuotaMetadata))
                    throw TypeError(".proto.BotQuotaMetadata.botFeatureQuotaMetadata: array expected");
                message.botFeatureQuotaMetadata = Array(object.botFeatureQuotaMetadata.length);
                for (var i = 0; i < object.botFeatureQuotaMetadata.length; ++i) {
                    if (!$util.isObject(object.botFeatureQuotaMetadata[i]))
                        throw TypeError(".proto.BotQuotaMetadata.botFeatureQuotaMetadata: object expected");
                    message.botFeatureQuotaMetadata[i] = $root.proto.BotQuotaMetadata.BotFeatureQuotaMetadata.fromObject(object.botFeatureQuotaMetadata[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a BotQuotaMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotQuotaMetadata
         * @static
         * @param {proto.BotQuotaMetadata} message BotQuotaMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotQuotaMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.botFeatureQuotaMetadata = [];
            if (message.botFeatureQuotaMetadata && message.botFeatureQuotaMetadata.length) {
                object.botFeatureQuotaMetadata = Array(message.botFeatureQuotaMetadata.length);
                for (var j = 0; j < message.botFeatureQuotaMetadata.length; ++j)
                    object.botFeatureQuotaMetadata[j] = $root.proto.BotQuotaMetadata.BotFeatureQuotaMetadata.toObject(message.botFeatureQuotaMetadata[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this BotQuotaMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotQuotaMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotQuotaMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotQuotaMetadata
         * @function getTypeUrl
         * @memberof proto.BotQuotaMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotQuotaMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotQuotaMetadata";
        };

        BotQuotaMetadata.BotFeatureQuotaMetadata = (function() {

            /**
             * Properties of a BotFeatureQuotaMetadata.
             * @memberof proto.BotQuotaMetadata
             * @interface IBotFeatureQuotaMetadata
             * @property {proto.BotQuotaMetadata.BotFeatureQuotaMetadata.BotFeatureType|null} [featureType] BotFeatureQuotaMetadata featureType
             * @property {number|null} [remainingQuota] BotFeatureQuotaMetadata remainingQuota
             * @property {number|Long|null} [expirationTimestamp] BotFeatureQuotaMetadata expirationTimestamp
             */

            /**
             * Constructs a new BotFeatureQuotaMetadata.
             * @memberof proto.BotQuotaMetadata
             * @classdesc Represents a BotFeatureQuotaMetadata.
             * @implements IBotFeatureQuotaMetadata
             * @constructor
             * @param {proto.BotQuotaMetadata.IBotFeatureQuotaMetadata=} [properties] Properties to set
             */
            function BotFeatureQuotaMetadata(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * BotFeatureQuotaMetadata featureType.
             * @member {proto.BotQuotaMetadata.BotFeatureQuotaMetadata.BotFeatureType|null|undefined} featureType
             * @memberof proto.BotQuotaMetadata.BotFeatureQuotaMetadata
             * @instance
             */
            BotFeatureQuotaMetadata.prototype.featureType = null;

            /**
             * BotFeatureQuotaMetadata remainingQuota.
             * @member {number|null|undefined} remainingQuota
             * @memberof proto.BotQuotaMetadata.BotFeatureQuotaMetadata
             * @instance
             */
            BotFeatureQuotaMetadata.prototype.remainingQuota = null;

            /**
             * BotFeatureQuotaMetadata expirationTimestamp.
             * @member {number|Long|null|undefined} expirationTimestamp
             * @memberof proto.BotQuotaMetadata.BotFeatureQuotaMetadata
             * @instance
             */
            BotFeatureQuotaMetadata.prototype.expirationTimestamp = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotFeatureQuotaMetadata.prototype, "_featureType", {
                get: $util.oneOfGetter($oneOfFields = ["featureType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotFeatureQuotaMetadata.prototype, "_remainingQuota", {
                get: $util.oneOfGetter($oneOfFields = ["remainingQuota"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotFeatureQuotaMetadata.prototype, "_expirationTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["expirationTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BotFeatureQuotaMetadata instance using the specified properties.
             * @function create
             * @memberof proto.BotQuotaMetadata.BotFeatureQuotaMetadata
             * @static
             * @param {proto.BotQuotaMetadata.IBotFeatureQuotaMetadata=} [properties] Properties to set
             * @returns {proto.BotQuotaMetadata.BotFeatureQuotaMetadata} BotFeatureQuotaMetadata instance
             */
            BotFeatureQuotaMetadata.create = function create(properties) {
                return new BotFeatureQuotaMetadata(properties);
            };

            /**
             * Encodes the specified BotFeatureQuotaMetadata message. Does not implicitly {@link proto.BotQuotaMetadata.BotFeatureQuotaMetadata.verify|verify} messages.
             * @function encode
             * @memberof proto.BotQuotaMetadata.BotFeatureQuotaMetadata
             * @static
             * @param {proto.BotQuotaMetadata.IBotFeatureQuotaMetadata} message BotFeatureQuotaMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BotFeatureQuotaMetadata.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.featureType != null && Object.hasOwnProperty.call(message, "featureType"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.featureType);
                if (message.remainingQuota != null && Object.hasOwnProperty.call(message, "remainingQuota"))
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.remainingQuota);
                if (message.expirationTimestamp != null && Object.hasOwnProperty.call(message, "expirationTimestamp"))
                    writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.expirationTimestamp);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified BotFeatureQuotaMetadata message, length delimited. Does not implicitly {@link proto.BotQuotaMetadata.BotFeatureQuotaMetadata.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.BotQuotaMetadata.BotFeatureQuotaMetadata
             * @static
             * @param {proto.BotQuotaMetadata.IBotFeatureQuotaMetadata} message BotFeatureQuotaMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BotFeatureQuotaMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a BotFeatureQuotaMetadata message from the specified reader or buffer.
             * @function decode
             * @memberof proto.BotQuotaMetadata.BotFeatureQuotaMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.BotQuotaMetadata.BotFeatureQuotaMetadata} BotFeatureQuotaMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BotFeatureQuotaMetadata.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.featureType = reader.int32();
                            message._featureType = "featureType";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.remainingQuota = reader.uint32();
                            message._remainingQuota = "remainingQuota";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.expirationTimestamp = reader.uint64();
                            message._expirationTimestamp = "expirationTimestamp";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a BotFeatureQuotaMetadata message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.BotQuotaMetadata.BotFeatureQuotaMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.BotQuotaMetadata.BotFeatureQuotaMetadata} BotFeatureQuotaMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BotFeatureQuotaMetadata.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BotFeatureQuotaMetadata message.
             * @function verify
             * @memberof proto.BotQuotaMetadata.BotFeatureQuotaMetadata
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BotFeatureQuotaMetadata.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.featureType != null && Object.hasOwnProperty.call(message, "featureType")) {
                    properties._featureType = 1;
                    if (typeof message.featureType !== "number" || (message.featureType | 0) !== message.featureType)
                        return "featureType: enum value expected";
                }
                if (message.remainingQuota != null && Object.hasOwnProperty.call(message, "remainingQuota")) {
                    properties._remainingQuota = 1;
                    if (!$util.isInteger(message.remainingQuota))
                        return "remainingQuota: integer expected";
                }
                if (message.expirationTimestamp != null && Object.hasOwnProperty.call(message, "expirationTimestamp")) {
                    properties._expirationTimestamp = 1;
                    if (!$util.isInteger(message.expirationTimestamp) && !(message.expirationTimestamp && $util.isInteger(message.expirationTimestamp.low) && $util.isInteger(message.expirationTimestamp.high)))
                        return "expirationTimestamp: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a BotFeatureQuotaMetadata message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.BotQuotaMetadata.BotFeatureQuotaMetadata
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.BotQuotaMetadata.BotFeatureQuotaMetadata} BotFeatureQuotaMetadata
             */
            BotFeatureQuotaMetadata.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.BotQuotaMetadata.BotFeatureQuotaMetadata: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.featureType) {
                case "UNKNOWN_FEATURE":
                case 0:
                    message.featureType = 0;
                    break;
                case "REASONING_FEATURE":
                case 1:
                    message.featureType = 1;
                    break;
                default:
                    if (typeof object.featureType === "number" && (object.featureType | 0) === object.featureType)
                        message.featureType = object.featureType;
                }
                if (object.remainingQuota != null)
                    message.remainingQuota = object.remainingQuota >>> 0;
                if (object.expirationTimestamp != null)
                    if ($util.Long)
                        message.expirationTimestamp = $util.Long.fromValue(object.expirationTimestamp, true);
                    else if (typeof object.expirationTimestamp === "string")
                        message.expirationTimestamp = parseInt(object.expirationTimestamp, 10);
                    else if (typeof object.expirationTimestamp === "number")
                        message.expirationTimestamp = object.expirationTimestamp;
                    else if (typeof object.expirationTimestamp === "object")
                        message.expirationTimestamp = new $util.LongBits(object.expirationTimestamp.low >>> 0, object.expirationTimestamp.high >>> 0).toNumber(true);
                return message;
            };

            /**
             * Creates a plain object from a BotFeatureQuotaMetadata message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.BotQuotaMetadata.BotFeatureQuotaMetadata
             * @static
             * @param {proto.BotQuotaMetadata.BotFeatureQuotaMetadata} message BotFeatureQuotaMetadata
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BotFeatureQuotaMetadata.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.featureType != null && Object.hasOwnProperty.call(message, "featureType"))
                    object.featureType = options.enums === String ? $root.proto.BotQuotaMetadata.BotFeatureQuotaMetadata.BotFeatureType[message.featureType] === undefined ? message.featureType : $root.proto.BotQuotaMetadata.BotFeatureQuotaMetadata.BotFeatureType[message.featureType] : message.featureType;
                if (message.remainingQuota != null && Object.hasOwnProperty.call(message, "remainingQuota"))
                    object.remainingQuota = message.remainingQuota;
                if (message.expirationTimestamp != null && Object.hasOwnProperty.call(message, "expirationTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.expirationTimestamp = typeof message.expirationTimestamp === "number" ? BigInt(message.expirationTimestamp) : $util.Long.fromBits(message.expirationTimestamp.low >>> 0, message.expirationTimestamp.high >>> 0, true).toBigInt();
                    else if (typeof message.expirationTimestamp === "number")
                        object.expirationTimestamp = options.longs === String ? String(message.expirationTimestamp) : message.expirationTimestamp;
                    else
                        object.expirationTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.expirationTimestamp) : options.longs === Number ? new $util.LongBits(message.expirationTimestamp.low >>> 0, message.expirationTimestamp.high >>> 0).toNumber(true) : message.expirationTimestamp;
                return object;
            };

            /**
             * Converts this BotFeatureQuotaMetadata to JSON.
             * @function toJSON
             * @memberof proto.BotQuotaMetadata.BotFeatureQuotaMetadata
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BotFeatureQuotaMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for BotFeatureQuotaMetadata
             * @function getTypeUrl
             * @memberof proto.BotQuotaMetadata.BotFeatureQuotaMetadata
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            BotFeatureQuotaMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.BotQuotaMetadata.BotFeatureQuotaMetadata";
            };

            /**
             * BotFeatureType enum.
             * @name proto.BotQuotaMetadata.BotFeatureQuotaMetadata.BotFeatureType
             * @enum {number}
             * @property {number} UNKNOWN_FEATURE=0 UNKNOWN_FEATURE value
             * @property {number} REASONING_FEATURE=1 REASONING_FEATURE value
             */
            BotFeatureQuotaMetadata.BotFeatureType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN_FEATURE"] = 0;
                values[valuesById[1] = "REASONING_FEATURE"] = 1;
                return values;
            })();

            return BotFeatureQuotaMetadata;
        })();

        return BotQuotaMetadata;
    })();

    proto.BotReminderMetadata = (function() {

        /**
         * Properties of a BotReminderMetadata.
         * @memberof proto
         * @interface IBotReminderMetadata
         * @property {proto.IMessageKey|null} [requestMessageKey] BotReminderMetadata requestMessageKey
         * @property {proto.BotReminderMetadata.ReminderAction|null} [action] BotReminderMetadata action
         * @property {string|null} [name] BotReminderMetadata name
         * @property {number|Long|null} [nextTriggerTimestamp] BotReminderMetadata nextTriggerTimestamp
         * @property {proto.BotReminderMetadata.ReminderFrequency|null} [frequency] BotReminderMetadata frequency
         */

        /**
         * Constructs a new BotReminderMetadata.
         * @memberof proto
         * @classdesc Represents a BotReminderMetadata.
         * @implements IBotReminderMetadata
         * @constructor
         * @param {proto.IBotReminderMetadata=} [properties] Properties to set
         */
        function BotReminderMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotReminderMetadata requestMessageKey.
         * @member {proto.IMessageKey|null|undefined} requestMessageKey
         * @memberof proto.BotReminderMetadata
         * @instance
         */
        BotReminderMetadata.prototype.requestMessageKey = null;

        /**
         * BotReminderMetadata action.
         * @member {proto.BotReminderMetadata.ReminderAction|null|undefined} action
         * @memberof proto.BotReminderMetadata
         * @instance
         */
        BotReminderMetadata.prototype.action = null;

        /**
         * BotReminderMetadata name.
         * @member {string|null|undefined} name
         * @memberof proto.BotReminderMetadata
         * @instance
         */
        BotReminderMetadata.prototype.name = null;

        /**
         * BotReminderMetadata nextTriggerTimestamp.
         * @member {number|Long|null|undefined} nextTriggerTimestamp
         * @memberof proto.BotReminderMetadata
         * @instance
         */
        BotReminderMetadata.prototype.nextTriggerTimestamp = null;

        /**
         * BotReminderMetadata frequency.
         * @member {proto.BotReminderMetadata.ReminderFrequency|null|undefined} frequency
         * @memberof proto.BotReminderMetadata
         * @instance
         */
        BotReminderMetadata.prototype.frequency = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotReminderMetadata.prototype, "_requestMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["requestMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotReminderMetadata.prototype, "_action", {
            get: $util.oneOfGetter($oneOfFields = ["action"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotReminderMetadata.prototype, "_name", {
            get: $util.oneOfGetter($oneOfFields = ["name"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotReminderMetadata.prototype, "_nextTriggerTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["nextTriggerTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotReminderMetadata.prototype, "_frequency", {
            get: $util.oneOfGetter($oneOfFields = ["frequency"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotReminderMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotReminderMetadata
         * @static
         * @param {proto.IBotReminderMetadata=} [properties] Properties to set
         * @returns {proto.BotReminderMetadata} BotReminderMetadata instance
         */
        BotReminderMetadata.create = function create(properties) {
            return new BotReminderMetadata(properties);
        };

        /**
         * Encodes the specified BotReminderMetadata message. Does not implicitly {@link proto.BotReminderMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotReminderMetadata
         * @static
         * @param {proto.IBotReminderMetadata} message BotReminderMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotReminderMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.requestMessageKey != null && Object.hasOwnProperty.call(message, "requestMessageKey"))
                $root.proto.MessageKey.encode(message.requestMessageKey, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.action != null && Object.hasOwnProperty.call(message, "action"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.action);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.name);
            if (message.nextTriggerTimestamp != null && Object.hasOwnProperty.call(message, "nextTriggerTimestamp"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.nextTriggerTimestamp);
            if (message.frequency != null && Object.hasOwnProperty.call(message, "frequency"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.frequency);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotReminderMetadata message, length delimited. Does not implicitly {@link proto.BotReminderMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotReminderMetadata
         * @static
         * @param {proto.IBotReminderMetadata} message BotReminderMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotReminderMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotReminderMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotReminderMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotReminderMetadata} BotReminderMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotReminderMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.requestMessageKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.requestMessageKey);
                        message._requestMessageKey = "requestMessageKey";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.action = reader.int32();
                        message._action = "action";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.name = reader.stringVerify();
                        message._name = "name";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.nextTriggerTimestamp = reader.uint64();
                        message._nextTriggerTimestamp = "nextTriggerTimestamp";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.frequency = reader.int32();
                        message._frequency = "frequency";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotReminderMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotReminderMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotReminderMetadata} BotReminderMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotReminderMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotReminderMetadata message.
         * @function verify
         * @memberof proto.BotReminderMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotReminderMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.requestMessageKey != null && Object.hasOwnProperty.call(message, "requestMessageKey")) {
                properties._requestMessageKey = 1;
                {
                    var error = $root.proto.MessageKey.verify(message.requestMessageKey, q + 1);
                    if (error)
                        return "requestMessageKey." + error;
                }
            }
            if (message.action != null && Object.hasOwnProperty.call(message, "action")) {
                properties._action = 1;
                if (typeof message.action !== "number" || (message.action | 0) !== message.action)
                    return "action: enum value expected";
            }
            if (message.name != null && Object.hasOwnProperty.call(message, "name")) {
                properties._name = 1;
                if (!$util.isString(message.name))
                    return "name: string expected";
            }
            if (message.nextTriggerTimestamp != null && Object.hasOwnProperty.call(message, "nextTriggerTimestamp")) {
                properties._nextTriggerTimestamp = 1;
                if (!$util.isInteger(message.nextTriggerTimestamp) && !(message.nextTriggerTimestamp && $util.isInteger(message.nextTriggerTimestamp.low) && $util.isInteger(message.nextTriggerTimestamp.high)))
                    return "nextTriggerTimestamp: integer|Long expected";
            }
            if (message.frequency != null && Object.hasOwnProperty.call(message, "frequency")) {
                properties._frequency = 1;
                if (typeof message.frequency !== "number" || (message.frequency | 0) !== message.frequency)
                    return "frequency: enum value expected";
            }
            return null;
        };

        /**
         * Creates a BotReminderMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotReminderMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotReminderMetadata} BotReminderMetadata
         */
        BotReminderMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotReminderMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.requestMessageKey != null) {
                if (!$util.isObject(object.requestMessageKey))
                    throw TypeError(".proto.BotReminderMetadata.requestMessageKey: object expected");
                message.requestMessageKey = $root.proto.MessageKey.fromObject(object.requestMessageKey, q + 1);
            }
            switch (object.action) {
            case "NOTIFY":
            case 1:
                message.action = 1;
                break;
            case "CREATE":
            case 2:
                message.action = 2;
                break;
            case "DELETE":
            case 3:
                message.action = 3;
                break;
            case "UPDATE":
            case 4:
                message.action = 4;
                break;
            default:
                if (typeof object.action === "number" && (object.action | 0) === object.action)
                    message.action = object.action;
            }
            if (object.name != null)
                message.name = String(object.name);
            if (object.nextTriggerTimestamp != null)
                if ($util.Long)
                    message.nextTriggerTimestamp = $util.Long.fromValue(object.nextTriggerTimestamp, true);
                else if (typeof object.nextTriggerTimestamp === "string")
                    message.nextTriggerTimestamp = parseInt(object.nextTriggerTimestamp, 10);
                else if (typeof object.nextTriggerTimestamp === "number")
                    message.nextTriggerTimestamp = object.nextTriggerTimestamp;
                else if (typeof object.nextTriggerTimestamp === "object")
                    message.nextTriggerTimestamp = new $util.LongBits(object.nextTriggerTimestamp.low >>> 0, object.nextTriggerTimestamp.high >>> 0).toNumber(true);
            switch (object.frequency) {
            case "ONCE":
            case 1:
                message.frequency = 1;
                break;
            case "DAILY":
            case 2:
                message.frequency = 2;
                break;
            case "WEEKLY":
            case 3:
                message.frequency = 3;
                break;
            case "BIWEEKLY":
            case 4:
                message.frequency = 4;
                break;
            case "MONTHLY":
            case 5:
                message.frequency = 5;
                break;
            default:
                if (typeof object.frequency === "number" && (object.frequency | 0) === object.frequency)
                    message.frequency = object.frequency;
            }
            return message;
        };

        /**
         * Creates a plain object from a BotReminderMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotReminderMetadata
         * @static
         * @param {proto.BotReminderMetadata} message BotReminderMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotReminderMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.requestMessageKey != null && Object.hasOwnProperty.call(message, "requestMessageKey"))
                object.requestMessageKey = $root.proto.MessageKey.toObject(message.requestMessageKey, options, q + 1);
            if (message.action != null && Object.hasOwnProperty.call(message, "action"))
                object.action = options.enums === String ? $root.proto.BotReminderMetadata.ReminderAction[message.action] === undefined ? message.action : $root.proto.BotReminderMetadata.ReminderAction[message.action] : message.action;
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.nextTriggerTimestamp != null && Object.hasOwnProperty.call(message, "nextTriggerTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.nextTriggerTimestamp = typeof message.nextTriggerTimestamp === "number" ? BigInt(message.nextTriggerTimestamp) : $util.Long.fromBits(message.nextTriggerTimestamp.low >>> 0, message.nextTriggerTimestamp.high >>> 0, true).toBigInt();
                else if (typeof message.nextTriggerTimestamp === "number")
                    object.nextTriggerTimestamp = options.longs === String ? String(message.nextTriggerTimestamp) : message.nextTriggerTimestamp;
                else
                    object.nextTriggerTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.nextTriggerTimestamp) : options.longs === Number ? new $util.LongBits(message.nextTriggerTimestamp.low >>> 0, message.nextTriggerTimestamp.high >>> 0).toNumber(true) : message.nextTriggerTimestamp;
            if (message.frequency != null && Object.hasOwnProperty.call(message, "frequency"))
                object.frequency = options.enums === String ? $root.proto.BotReminderMetadata.ReminderFrequency[message.frequency] === undefined ? message.frequency : $root.proto.BotReminderMetadata.ReminderFrequency[message.frequency] : message.frequency;
            return object;
        };

        /**
         * Converts this BotReminderMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotReminderMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotReminderMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotReminderMetadata
         * @function getTypeUrl
         * @memberof proto.BotReminderMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotReminderMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotReminderMetadata";
        };

        /**
         * ReminderAction enum.
         * @name proto.BotReminderMetadata.ReminderAction
         * @enum {number}
         * @property {number} NOTIFY=1 NOTIFY value
         * @property {number} CREATE=2 CREATE value
         * @property {number} DELETE=3 DELETE value
         * @property {number} UPDATE=4 UPDATE value
         */
        BotReminderMetadata.ReminderAction = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[1] = "NOTIFY"] = 1;
            values[valuesById[2] = "CREATE"] = 2;
            values[valuesById[3] = "DELETE"] = 3;
            values[valuesById[4] = "UPDATE"] = 4;
            return values;
        })();

        /**
         * ReminderFrequency enum.
         * @name proto.BotReminderMetadata.ReminderFrequency
         * @enum {number}
         * @property {number} ONCE=1 ONCE value
         * @property {number} DAILY=2 DAILY value
         * @property {number} WEEKLY=3 WEEKLY value
         * @property {number} BIWEEKLY=4 BIWEEKLY value
         * @property {number} MONTHLY=5 MONTHLY value
         */
        BotReminderMetadata.ReminderFrequency = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[1] = "ONCE"] = 1;
            values[valuesById[2] = "DAILY"] = 2;
            values[valuesById[3] = "WEEKLY"] = 3;
            values[valuesById[4] = "BIWEEKLY"] = 4;
            values[valuesById[5] = "MONTHLY"] = 5;
            return values;
        })();

        return BotReminderMetadata;
    })();

    proto.BotRenderingConfigMetadata = (function() {

        /**
         * Properties of a BotRenderingConfigMetadata.
         * @memberof proto
         * @interface IBotRenderingConfigMetadata
         * @property {string|null} [bloksVersioningId] BotRenderingConfigMetadata bloksVersioningId
         * @property {number|null} [pixelDensity] BotRenderingConfigMetadata pixelDensity
         */

        /**
         * Constructs a new BotRenderingConfigMetadata.
         * @memberof proto
         * @classdesc Represents a BotRenderingConfigMetadata.
         * @implements IBotRenderingConfigMetadata
         * @constructor
         * @param {proto.IBotRenderingConfigMetadata=} [properties] Properties to set
         */
        function BotRenderingConfigMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotRenderingConfigMetadata bloksVersioningId.
         * @member {string|null|undefined} bloksVersioningId
         * @memberof proto.BotRenderingConfigMetadata
         * @instance
         */
        BotRenderingConfigMetadata.prototype.bloksVersioningId = null;

        /**
         * BotRenderingConfigMetadata pixelDensity.
         * @member {number|null|undefined} pixelDensity
         * @memberof proto.BotRenderingConfigMetadata
         * @instance
         */
        BotRenderingConfigMetadata.prototype.pixelDensity = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotRenderingConfigMetadata.prototype, "_bloksVersioningId", {
            get: $util.oneOfGetter($oneOfFields = ["bloksVersioningId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotRenderingConfigMetadata.prototype, "_pixelDensity", {
            get: $util.oneOfGetter($oneOfFields = ["pixelDensity"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotRenderingConfigMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotRenderingConfigMetadata
         * @static
         * @param {proto.IBotRenderingConfigMetadata=} [properties] Properties to set
         * @returns {proto.BotRenderingConfigMetadata} BotRenderingConfigMetadata instance
         */
        BotRenderingConfigMetadata.create = function create(properties) {
            return new BotRenderingConfigMetadata(properties);
        };

        /**
         * Encodes the specified BotRenderingConfigMetadata message. Does not implicitly {@link proto.BotRenderingConfigMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotRenderingConfigMetadata
         * @static
         * @param {proto.IBotRenderingConfigMetadata} message BotRenderingConfigMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotRenderingConfigMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.bloksVersioningId != null && Object.hasOwnProperty.call(message, "bloksVersioningId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.bloksVersioningId);
            if (message.pixelDensity != null && Object.hasOwnProperty.call(message, "pixelDensity"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.pixelDensity);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotRenderingConfigMetadata message, length delimited. Does not implicitly {@link proto.BotRenderingConfigMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotRenderingConfigMetadata
         * @static
         * @param {proto.IBotRenderingConfigMetadata} message BotRenderingConfigMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotRenderingConfigMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotRenderingConfigMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotRenderingConfigMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotRenderingConfigMetadata} BotRenderingConfigMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotRenderingConfigMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.bloksVersioningId = reader.stringVerify();
                        message._bloksVersioningId = "bloksVersioningId";
                        continue;
                    }
                case 2: {
                        if (u !== 1)
                            break;
                        message.pixelDensity = reader.double();
                        message._pixelDensity = "pixelDensity";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotRenderingConfigMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotRenderingConfigMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotRenderingConfigMetadata} BotRenderingConfigMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotRenderingConfigMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotRenderingConfigMetadata message.
         * @function verify
         * @memberof proto.BotRenderingConfigMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotRenderingConfigMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.bloksVersioningId != null && Object.hasOwnProperty.call(message, "bloksVersioningId")) {
                properties._bloksVersioningId = 1;
                if (!$util.isString(message.bloksVersioningId))
                    return "bloksVersioningId: string expected";
            }
            if (message.pixelDensity != null && Object.hasOwnProperty.call(message, "pixelDensity")) {
                properties._pixelDensity = 1;
                if (typeof message.pixelDensity !== "number")
                    return "pixelDensity: number expected";
            }
            return null;
        };

        /**
         * Creates a BotRenderingConfigMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotRenderingConfigMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotRenderingConfigMetadata} BotRenderingConfigMetadata
         */
        BotRenderingConfigMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotRenderingConfigMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.bloksVersioningId != null)
                message.bloksVersioningId = String(object.bloksVersioningId);
            if (object.pixelDensity != null)
                message.pixelDensity = Number(object.pixelDensity);
            return message;
        };

        /**
         * Creates a plain object from a BotRenderingConfigMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotRenderingConfigMetadata
         * @static
         * @param {proto.BotRenderingConfigMetadata} message BotRenderingConfigMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotRenderingConfigMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.bloksVersioningId != null && Object.hasOwnProperty.call(message, "bloksVersioningId"))
                object.bloksVersioningId = message.bloksVersioningId;
            if (message.pixelDensity != null && Object.hasOwnProperty.call(message, "pixelDensity"))
                object.pixelDensity = options.json && !isFinite(message.pixelDensity) ? String(message.pixelDensity) : message.pixelDensity;
            return object;
        };

        /**
         * Converts this BotRenderingConfigMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotRenderingConfigMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotRenderingConfigMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotRenderingConfigMetadata
         * @function getTypeUrl
         * @memberof proto.BotRenderingConfigMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotRenderingConfigMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotRenderingConfigMetadata";
        };

        return BotRenderingConfigMetadata;
    })();

    proto.BotRenderingMetadata = (function() {

        /**
         * Properties of a BotRenderingMetadata.
         * @memberof proto
         * @interface IBotRenderingMetadata
         * @property {Array.<proto.BotRenderingMetadata.IKeyword>|null} [keywords] BotRenderingMetadata keywords
         */

        /**
         * Constructs a new BotRenderingMetadata.
         * @memberof proto
         * @classdesc Represents a BotRenderingMetadata.
         * @implements IBotRenderingMetadata
         * @constructor
         * @param {proto.IBotRenderingMetadata=} [properties] Properties to set
         */
        function BotRenderingMetadata(properties) {
            this.keywords = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotRenderingMetadata keywords.
         * @member {Array.<proto.BotRenderingMetadata.IKeyword>} keywords
         * @memberof proto.BotRenderingMetadata
         * @instance
         */
        BotRenderingMetadata.prototype.keywords = $util.emptyArray;

        /**
         * Creates a new BotRenderingMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotRenderingMetadata
         * @static
         * @param {proto.IBotRenderingMetadata=} [properties] Properties to set
         * @returns {proto.BotRenderingMetadata} BotRenderingMetadata instance
         */
        BotRenderingMetadata.create = function create(properties) {
            return new BotRenderingMetadata(properties);
        };

        /**
         * Encodes the specified BotRenderingMetadata message. Does not implicitly {@link proto.BotRenderingMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotRenderingMetadata
         * @static
         * @param {proto.IBotRenderingMetadata} message BotRenderingMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotRenderingMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.keywords != null && message.keywords.length)
                for (var i = 0; i < message.keywords.length; ++i)
                    $root.proto.BotRenderingMetadata.Keyword.encode(message.keywords[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotRenderingMetadata message, length delimited. Does not implicitly {@link proto.BotRenderingMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotRenderingMetadata
         * @static
         * @param {proto.IBotRenderingMetadata} message BotRenderingMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotRenderingMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotRenderingMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotRenderingMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotRenderingMetadata} BotRenderingMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotRenderingMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(message.keywords && message.keywords.length))
                            message.keywords = [];
                        message.keywords.push($root.proto.BotRenderingMetadata.Keyword.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotRenderingMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotRenderingMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotRenderingMetadata} BotRenderingMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotRenderingMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotRenderingMetadata message.
         * @function verify
         * @memberof proto.BotRenderingMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotRenderingMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            if (message.keywords != null && Object.hasOwnProperty.call(message, "keywords")) {
                if (!Array.isArray(message.keywords))
                    return "keywords: array expected";
                for (var i = 0; i < message.keywords.length; ++i) {
                    var error = $root.proto.BotRenderingMetadata.Keyword.verify(message.keywords[i], q + 1);
                    if (error)
                        return "keywords." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BotRenderingMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotRenderingMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotRenderingMetadata} BotRenderingMetadata
         */
        BotRenderingMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotRenderingMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.keywords) {
                if (!Array.isArray(object.keywords))
                    throw TypeError(".proto.BotRenderingMetadata.keywords: array expected");
                message.keywords = Array(object.keywords.length);
                for (var i = 0; i < object.keywords.length; ++i) {
                    if (!$util.isObject(object.keywords[i]))
                        throw TypeError(".proto.BotRenderingMetadata.keywords: object expected");
                    message.keywords[i] = $root.proto.BotRenderingMetadata.Keyword.fromObject(object.keywords[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a BotRenderingMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotRenderingMetadata
         * @static
         * @param {proto.BotRenderingMetadata} message BotRenderingMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotRenderingMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.keywords = [];
            if (message.keywords && message.keywords.length) {
                object.keywords = Array(message.keywords.length);
                for (var j = 0; j < message.keywords.length; ++j)
                    object.keywords[j] = $root.proto.BotRenderingMetadata.Keyword.toObject(message.keywords[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this BotRenderingMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotRenderingMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotRenderingMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotRenderingMetadata
         * @function getTypeUrl
         * @memberof proto.BotRenderingMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotRenderingMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotRenderingMetadata";
        };

        BotRenderingMetadata.Keyword = (function() {

            /**
             * Properties of a Keyword.
             * @memberof proto.BotRenderingMetadata
             * @interface IKeyword
             * @property {string|null} [value] Keyword value
             * @property {Array.<string>|null} [associatedPrompts] Keyword associatedPrompts
             */

            /**
             * Constructs a new Keyword.
             * @memberof proto.BotRenderingMetadata
             * @classdesc Represents a Keyword.
             * @implements IKeyword
             * @constructor
             * @param {proto.BotRenderingMetadata.IKeyword=} [properties] Properties to set
             */
            function Keyword(properties) {
                this.associatedPrompts = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Keyword value.
             * @member {string|null|undefined} value
             * @memberof proto.BotRenderingMetadata.Keyword
             * @instance
             */
            Keyword.prototype.value = null;

            /**
             * Keyword associatedPrompts.
             * @member {Array.<string>} associatedPrompts
             * @memberof proto.BotRenderingMetadata.Keyword
             * @instance
             */
            Keyword.prototype.associatedPrompts = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Keyword.prototype, "_value", {
                get: $util.oneOfGetter($oneOfFields = ["value"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new Keyword instance using the specified properties.
             * @function create
             * @memberof proto.BotRenderingMetadata.Keyword
             * @static
             * @param {proto.BotRenderingMetadata.IKeyword=} [properties] Properties to set
             * @returns {proto.BotRenderingMetadata.Keyword} Keyword instance
             */
            Keyword.create = function create(properties) {
                return new Keyword(properties);
            };

            /**
             * Encodes the specified Keyword message. Does not implicitly {@link proto.BotRenderingMetadata.Keyword.verify|verify} messages.
             * @function encode
             * @memberof proto.BotRenderingMetadata.Keyword
             * @static
             * @param {proto.BotRenderingMetadata.IKeyword} message Keyword message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Keyword.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.value);
                if (message.associatedPrompts != null && message.associatedPrompts.length)
                    for (var i = 0; i < message.associatedPrompts.length; ++i)
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.associatedPrompts[i]);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Keyword message, length delimited. Does not implicitly {@link proto.BotRenderingMetadata.Keyword.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.BotRenderingMetadata.Keyword
             * @static
             * @param {proto.BotRenderingMetadata.IKeyword} message Keyword message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Keyword.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a Keyword message from the specified reader or buffer.
             * @function decode
             * @memberof proto.BotRenderingMetadata.Keyword
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.BotRenderingMetadata.Keyword} Keyword
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Keyword.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.value = reader.stringVerify();
                            message._value = "value";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(message.associatedPrompts && message.associatedPrompts.length))
                                message.associatedPrompts = [];
                            message.associatedPrompts.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a Keyword message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.BotRenderingMetadata.Keyword
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.BotRenderingMetadata.Keyword} Keyword
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Keyword.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Keyword message.
             * @function verify
             * @memberof proto.BotRenderingMetadata.Keyword
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Keyword.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.value != null && Object.hasOwnProperty.call(message, "value")) {
                    properties._value = 1;
                    if (!$util.isString(message.value))
                        return "value: string expected";
                }
                if (message.associatedPrompts != null && Object.hasOwnProperty.call(message, "associatedPrompts")) {
                    if (!Array.isArray(message.associatedPrompts))
                        return "associatedPrompts: array expected";
                    for (var i = 0; i < message.associatedPrompts.length; ++i)
                        if (!$util.isString(message.associatedPrompts[i]))
                            return "associatedPrompts: string[] expected";
                }
                return null;
            };

            /**
             * Creates a Keyword message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.BotRenderingMetadata.Keyword
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.BotRenderingMetadata.Keyword} Keyword
             */
            Keyword.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.BotRenderingMetadata.Keyword: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.value != null)
                    message.value = String(object.value);
                if (object.associatedPrompts) {
                    if (!Array.isArray(object.associatedPrompts))
                        throw TypeError(".proto.BotRenderingMetadata.Keyword.associatedPrompts: array expected");
                    message.associatedPrompts = Array(object.associatedPrompts.length);
                    for (var i = 0; i < object.associatedPrompts.length; ++i)
                        message.associatedPrompts[i] = String(object.associatedPrompts[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a Keyword message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.BotRenderingMetadata.Keyword
             * @static
             * @param {proto.BotRenderingMetadata.Keyword} message Keyword
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Keyword.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.associatedPrompts = [];
                if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                    object.value = message.value;
                if (message.associatedPrompts && message.associatedPrompts.length) {
                    object.associatedPrompts = Array(message.associatedPrompts.length);
                    for (var j = 0; j < message.associatedPrompts.length; ++j)
                        object.associatedPrompts[j] = message.associatedPrompts[j];
                }
                return object;
            };

            /**
             * Converts this Keyword to JSON.
             * @function toJSON
             * @memberof proto.BotRenderingMetadata.Keyword
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Keyword.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Keyword
             * @function getTypeUrl
             * @memberof proto.BotRenderingMetadata.Keyword
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Keyword.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.BotRenderingMetadata.Keyword";
            };

            return Keyword;
        })();

        return BotRenderingMetadata;
    })();

    proto.BotResolvedToolCallMetadata = (function() {

        /**
         * Properties of a BotResolvedToolCallMetadata.
         * @memberof proto
         * @interface IBotResolvedToolCallMetadata
         * @property {string|null} [toolCallId] BotResolvedToolCallMetadata toolCallId
         * @property {string|null} [resolutionDataSerialized] BotResolvedToolCallMetadata resolutionDataSerialized
         */

        /**
         * Constructs a new BotResolvedToolCallMetadata.
         * @memberof proto
         * @classdesc Represents a BotResolvedToolCallMetadata.
         * @implements IBotResolvedToolCallMetadata
         * @constructor
         * @param {proto.IBotResolvedToolCallMetadata=} [properties] Properties to set
         */
        function BotResolvedToolCallMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotResolvedToolCallMetadata toolCallId.
         * @member {string|null|undefined} toolCallId
         * @memberof proto.BotResolvedToolCallMetadata
         * @instance
         */
        BotResolvedToolCallMetadata.prototype.toolCallId = null;

        /**
         * BotResolvedToolCallMetadata resolutionDataSerialized.
         * @member {string|null|undefined} resolutionDataSerialized
         * @memberof proto.BotResolvedToolCallMetadata
         * @instance
         */
        BotResolvedToolCallMetadata.prototype.resolutionDataSerialized = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotResolvedToolCallMetadata.prototype, "_toolCallId", {
            get: $util.oneOfGetter($oneOfFields = ["toolCallId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotResolvedToolCallMetadata.prototype, "_resolutionDataSerialized", {
            get: $util.oneOfGetter($oneOfFields = ["resolutionDataSerialized"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotResolvedToolCallMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotResolvedToolCallMetadata
         * @static
         * @param {proto.IBotResolvedToolCallMetadata=} [properties] Properties to set
         * @returns {proto.BotResolvedToolCallMetadata} BotResolvedToolCallMetadata instance
         */
        BotResolvedToolCallMetadata.create = function create(properties) {
            return new BotResolvedToolCallMetadata(properties);
        };

        /**
         * Encodes the specified BotResolvedToolCallMetadata message. Does not implicitly {@link proto.BotResolvedToolCallMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotResolvedToolCallMetadata
         * @static
         * @param {proto.IBotResolvedToolCallMetadata} message BotResolvedToolCallMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotResolvedToolCallMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.toolCallId != null && Object.hasOwnProperty.call(message, "toolCallId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.toolCallId);
            if (message.resolutionDataSerialized != null && Object.hasOwnProperty.call(message, "resolutionDataSerialized"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.resolutionDataSerialized);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotResolvedToolCallMetadata message, length delimited. Does not implicitly {@link proto.BotResolvedToolCallMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotResolvedToolCallMetadata
         * @static
         * @param {proto.IBotResolvedToolCallMetadata} message BotResolvedToolCallMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotResolvedToolCallMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotResolvedToolCallMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotResolvedToolCallMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotResolvedToolCallMetadata} BotResolvedToolCallMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotResolvedToolCallMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.toolCallId = reader.stringVerify();
                        message._toolCallId = "toolCallId";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.resolutionDataSerialized = reader.stringVerify();
                        message._resolutionDataSerialized = "resolutionDataSerialized";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotResolvedToolCallMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotResolvedToolCallMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotResolvedToolCallMetadata} BotResolvedToolCallMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotResolvedToolCallMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotResolvedToolCallMetadata message.
         * @function verify
         * @memberof proto.BotResolvedToolCallMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotResolvedToolCallMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.toolCallId != null && Object.hasOwnProperty.call(message, "toolCallId")) {
                properties._toolCallId = 1;
                if (!$util.isString(message.toolCallId))
                    return "toolCallId: string expected";
            }
            if (message.resolutionDataSerialized != null && Object.hasOwnProperty.call(message, "resolutionDataSerialized")) {
                properties._resolutionDataSerialized = 1;
                if (!$util.isString(message.resolutionDataSerialized))
                    return "resolutionDataSerialized: string expected";
            }
            return null;
        };

        /**
         * Creates a BotResolvedToolCallMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotResolvedToolCallMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotResolvedToolCallMetadata} BotResolvedToolCallMetadata
         */
        BotResolvedToolCallMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotResolvedToolCallMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.toolCallId != null)
                message.toolCallId = String(object.toolCallId);
            if (object.resolutionDataSerialized != null)
                message.resolutionDataSerialized = String(object.resolutionDataSerialized);
            return message;
        };

        /**
         * Creates a plain object from a BotResolvedToolCallMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotResolvedToolCallMetadata
         * @static
         * @param {proto.BotResolvedToolCallMetadata} message BotResolvedToolCallMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotResolvedToolCallMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.toolCallId != null && Object.hasOwnProperty.call(message, "toolCallId"))
                object.toolCallId = message.toolCallId;
            if (message.resolutionDataSerialized != null && Object.hasOwnProperty.call(message, "resolutionDataSerialized"))
                object.resolutionDataSerialized = message.resolutionDataSerialized;
            return object;
        };

        /**
         * Converts this BotResolvedToolCallMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotResolvedToolCallMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotResolvedToolCallMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotResolvedToolCallMetadata
         * @function getTypeUrl
         * @memberof proto.BotResolvedToolCallMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotResolvedToolCallMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotResolvedToolCallMetadata";
        };

        return BotResolvedToolCallMetadata;
    })();

    proto.BotSessionMetadata = (function() {

        /**
         * Properties of a BotSessionMetadata.
         * @memberof proto
         * @interface IBotSessionMetadata
         * @property {string|null} [sessionId] BotSessionMetadata sessionId
         * @property {proto.BotSessionSource|null} [sessionSource] BotSessionMetadata sessionSource
         */

        /**
         * Constructs a new BotSessionMetadata.
         * @memberof proto
         * @classdesc Represents a BotSessionMetadata.
         * @implements IBotSessionMetadata
         * @constructor
         * @param {proto.IBotSessionMetadata=} [properties] Properties to set
         */
        function BotSessionMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotSessionMetadata sessionId.
         * @member {string|null|undefined} sessionId
         * @memberof proto.BotSessionMetadata
         * @instance
         */
        BotSessionMetadata.prototype.sessionId = null;

        /**
         * BotSessionMetadata sessionSource.
         * @member {proto.BotSessionSource|null|undefined} sessionSource
         * @memberof proto.BotSessionMetadata
         * @instance
         */
        BotSessionMetadata.prototype.sessionSource = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotSessionMetadata.prototype, "_sessionId", {
            get: $util.oneOfGetter($oneOfFields = ["sessionId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotSessionMetadata.prototype, "_sessionSource", {
            get: $util.oneOfGetter($oneOfFields = ["sessionSource"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotSessionMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotSessionMetadata
         * @static
         * @param {proto.IBotSessionMetadata=} [properties] Properties to set
         * @returns {proto.BotSessionMetadata} BotSessionMetadata instance
         */
        BotSessionMetadata.create = function create(properties) {
            return new BotSessionMetadata(properties);
        };

        /**
         * Encodes the specified BotSessionMetadata message. Does not implicitly {@link proto.BotSessionMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotSessionMetadata
         * @static
         * @param {proto.IBotSessionMetadata} message BotSessionMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotSessionMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.sessionId != null && Object.hasOwnProperty.call(message, "sessionId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.sessionId);
            if (message.sessionSource != null && Object.hasOwnProperty.call(message, "sessionSource"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.sessionSource);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotSessionMetadata message, length delimited. Does not implicitly {@link proto.BotSessionMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotSessionMetadata
         * @static
         * @param {proto.IBotSessionMetadata} message BotSessionMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotSessionMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotSessionMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotSessionMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotSessionMetadata} BotSessionMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotSessionMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.sessionId = reader.stringVerify();
                        message._sessionId = "sessionId";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.sessionSource = reader.int32();
                        message._sessionSource = "sessionSource";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotSessionMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotSessionMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotSessionMetadata} BotSessionMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotSessionMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotSessionMetadata message.
         * @function verify
         * @memberof proto.BotSessionMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotSessionMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.sessionId != null && Object.hasOwnProperty.call(message, "sessionId")) {
                properties._sessionId = 1;
                if (!$util.isString(message.sessionId))
                    return "sessionId: string expected";
            }
            if (message.sessionSource != null && Object.hasOwnProperty.call(message, "sessionSource")) {
                properties._sessionSource = 1;
                if (typeof message.sessionSource !== "number" || (message.sessionSource | 0) !== message.sessionSource)
                    return "sessionSource: enum value expected";
            }
            return null;
        };

        /**
         * Creates a BotSessionMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotSessionMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotSessionMetadata} BotSessionMetadata
         */
        BotSessionMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotSessionMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.sessionId != null)
                message.sessionId = String(object.sessionId);
            switch (object.sessionSource) {
            case "NONE":
            case 0:
                message.sessionSource = 0;
                break;
            case "NULL_STATE":
            case 1:
                message.sessionSource = 1;
                break;
            case "TYPEAHEAD":
            case 2:
                message.sessionSource = 2;
                break;
            case "USER_INPUT":
            case 3:
                message.sessionSource = 3;
                break;
            case "EMU_FLASH":
            case 4:
                message.sessionSource = 4;
                break;
            case "EMU_FLASH_FOLLOWUP":
            case 5:
                message.sessionSource = 5;
                break;
            case "VOICE":
            case 6:
                message.sessionSource = 6;
                break;
            case "AI_HOME_SESSION":
            case 7:
                message.sessionSource = 7;
                break;
            default:
                if (typeof object.sessionSource === "number" && (object.sessionSource | 0) === object.sessionSource)
                    message.sessionSource = object.sessionSource;
            }
            return message;
        };

        /**
         * Creates a plain object from a BotSessionMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotSessionMetadata
         * @static
         * @param {proto.BotSessionMetadata} message BotSessionMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotSessionMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.sessionId != null && Object.hasOwnProperty.call(message, "sessionId"))
                object.sessionId = message.sessionId;
            if (message.sessionSource != null && Object.hasOwnProperty.call(message, "sessionSource"))
                object.sessionSource = options.enums === String ? $root.proto.BotSessionSource[message.sessionSource] === undefined ? message.sessionSource : $root.proto.BotSessionSource[message.sessionSource] : message.sessionSource;
            return object;
        };

        /**
         * Converts this BotSessionMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotSessionMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotSessionMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotSessionMetadata
         * @function getTypeUrl
         * @memberof proto.BotSessionMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotSessionMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotSessionMetadata";
        };

        return BotSessionMetadata;
    })();

    /**
     * BotSessionSource enum.
     * @name proto.BotSessionSource
     * @enum {number}
     * @property {number} NONE=0 NONE value
     * @property {number} NULL_STATE=1 NULL_STATE value
     * @property {number} TYPEAHEAD=2 TYPEAHEAD value
     * @property {number} USER_INPUT=3 USER_INPUT value
     * @property {number} EMU_FLASH=4 EMU_FLASH value
     * @property {number} EMU_FLASH_FOLLOWUP=5 EMU_FLASH_FOLLOWUP value
     * @property {number} VOICE=6 VOICE value
     * @property {number} AI_HOME_SESSION=7 AI_HOME_SESSION value
     */
    proto.BotSessionSource = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "NONE"] = 0;
        values[valuesById[1] = "NULL_STATE"] = 1;
        values[valuesById[2] = "TYPEAHEAD"] = 2;
        values[valuesById[3] = "USER_INPUT"] = 3;
        values[valuesById[4] = "EMU_FLASH"] = 4;
        values[valuesById[5] = "EMU_FLASH_FOLLOWUP"] = 5;
        values[valuesById[6] = "VOICE"] = 6;
        values[valuesById[7] = "AI_HOME_SESSION"] = 7;
        return values;
    })();

    proto.BotSignatureVerificationMetadata = (function() {

        /**
         * Properties of a BotSignatureVerificationMetadata.
         * @memberof proto
         * @interface IBotSignatureVerificationMetadata
         * @property {Array.<proto.IBotSignatureVerificationUseCaseProof>|null} [proofs] BotSignatureVerificationMetadata proofs
         */

        /**
         * Constructs a new BotSignatureVerificationMetadata.
         * @memberof proto
         * @classdesc Represents a BotSignatureVerificationMetadata.
         * @implements IBotSignatureVerificationMetadata
         * @constructor
         * @param {proto.IBotSignatureVerificationMetadata=} [properties] Properties to set
         */
        function BotSignatureVerificationMetadata(properties) {
            this.proofs = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotSignatureVerificationMetadata proofs.
         * @member {Array.<proto.IBotSignatureVerificationUseCaseProof>} proofs
         * @memberof proto.BotSignatureVerificationMetadata
         * @instance
         */
        BotSignatureVerificationMetadata.prototype.proofs = $util.emptyArray;

        /**
         * Creates a new BotSignatureVerificationMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotSignatureVerificationMetadata
         * @static
         * @param {proto.IBotSignatureVerificationMetadata=} [properties] Properties to set
         * @returns {proto.BotSignatureVerificationMetadata} BotSignatureVerificationMetadata instance
         */
        BotSignatureVerificationMetadata.create = function create(properties) {
            return new BotSignatureVerificationMetadata(properties);
        };

        /**
         * Encodes the specified BotSignatureVerificationMetadata message. Does not implicitly {@link proto.BotSignatureVerificationMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotSignatureVerificationMetadata
         * @static
         * @param {proto.IBotSignatureVerificationMetadata} message BotSignatureVerificationMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotSignatureVerificationMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.proofs != null && message.proofs.length)
                for (var i = 0; i < message.proofs.length; ++i)
                    $root.proto.BotSignatureVerificationUseCaseProof.encode(message.proofs[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotSignatureVerificationMetadata message, length delimited. Does not implicitly {@link proto.BotSignatureVerificationMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotSignatureVerificationMetadata
         * @static
         * @param {proto.IBotSignatureVerificationMetadata} message BotSignatureVerificationMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotSignatureVerificationMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotSignatureVerificationMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotSignatureVerificationMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotSignatureVerificationMetadata} BotSignatureVerificationMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotSignatureVerificationMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(message.proofs && message.proofs.length))
                            message.proofs = [];
                        message.proofs.push($root.proto.BotSignatureVerificationUseCaseProof.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotSignatureVerificationMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotSignatureVerificationMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotSignatureVerificationMetadata} BotSignatureVerificationMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotSignatureVerificationMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotSignatureVerificationMetadata message.
         * @function verify
         * @memberof proto.BotSignatureVerificationMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotSignatureVerificationMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            if (message.proofs != null && Object.hasOwnProperty.call(message, "proofs")) {
                if (!Array.isArray(message.proofs))
                    return "proofs: array expected";
                for (var i = 0; i < message.proofs.length; ++i) {
                    var error = $root.proto.BotSignatureVerificationUseCaseProof.verify(message.proofs[i], q + 1);
                    if (error)
                        return "proofs." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BotSignatureVerificationMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotSignatureVerificationMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotSignatureVerificationMetadata} BotSignatureVerificationMetadata
         */
        BotSignatureVerificationMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotSignatureVerificationMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.proofs) {
                if (!Array.isArray(object.proofs))
                    throw TypeError(".proto.BotSignatureVerificationMetadata.proofs: array expected");
                message.proofs = Array(object.proofs.length);
                for (var i = 0; i < object.proofs.length; ++i) {
                    if (!$util.isObject(object.proofs[i]))
                        throw TypeError(".proto.BotSignatureVerificationMetadata.proofs: object expected");
                    message.proofs[i] = $root.proto.BotSignatureVerificationUseCaseProof.fromObject(object.proofs[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a BotSignatureVerificationMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotSignatureVerificationMetadata
         * @static
         * @param {proto.BotSignatureVerificationMetadata} message BotSignatureVerificationMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotSignatureVerificationMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.proofs = [];
            if (message.proofs && message.proofs.length) {
                object.proofs = Array(message.proofs.length);
                for (var j = 0; j < message.proofs.length; ++j)
                    object.proofs[j] = $root.proto.BotSignatureVerificationUseCaseProof.toObject(message.proofs[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this BotSignatureVerificationMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotSignatureVerificationMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotSignatureVerificationMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotSignatureVerificationMetadata
         * @function getTypeUrl
         * @memberof proto.BotSignatureVerificationMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotSignatureVerificationMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotSignatureVerificationMetadata";
        };

        return BotSignatureVerificationMetadata;
    })();

    proto.BotSignatureVerificationUseCaseProof = (function() {

        /**
         * Properties of a BotSignatureVerificationUseCaseProof.
         * @memberof proto
         * @interface IBotSignatureVerificationUseCaseProof
         * @property {number|null} [version] BotSignatureVerificationUseCaseProof version
         * @property {proto.BotSignatureVerificationUseCaseProof.BotSignatureUseCase|null} [useCase] BotSignatureVerificationUseCaseProof useCase
         * @property {Uint8Array|null} [signature] BotSignatureVerificationUseCaseProof signature
         * @property {Array.<Uint8Array>|null} [certificateChain] BotSignatureVerificationUseCaseProof certificateChain
         */

        /**
         * Constructs a new BotSignatureVerificationUseCaseProof.
         * @memberof proto
         * @classdesc Represents a BotSignatureVerificationUseCaseProof.
         * @implements IBotSignatureVerificationUseCaseProof
         * @constructor
         * @param {proto.IBotSignatureVerificationUseCaseProof=} [properties] Properties to set
         */
        function BotSignatureVerificationUseCaseProof(properties) {
            this.certificateChain = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotSignatureVerificationUseCaseProof version.
         * @member {number|null|undefined} version
         * @memberof proto.BotSignatureVerificationUseCaseProof
         * @instance
         */
        BotSignatureVerificationUseCaseProof.prototype.version = null;

        /**
         * BotSignatureVerificationUseCaseProof useCase.
         * @member {proto.BotSignatureVerificationUseCaseProof.BotSignatureUseCase|null|undefined} useCase
         * @memberof proto.BotSignatureVerificationUseCaseProof
         * @instance
         */
        BotSignatureVerificationUseCaseProof.prototype.useCase = null;

        /**
         * BotSignatureVerificationUseCaseProof signature.
         * @member {Uint8Array|null|undefined} signature
         * @memberof proto.BotSignatureVerificationUseCaseProof
         * @instance
         */
        BotSignatureVerificationUseCaseProof.prototype.signature = null;

        /**
         * BotSignatureVerificationUseCaseProof certificateChain.
         * @member {Array.<Uint8Array>} certificateChain
         * @memberof proto.BotSignatureVerificationUseCaseProof
         * @instance
         */
        BotSignatureVerificationUseCaseProof.prototype.certificateChain = $util.emptyArray;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotSignatureVerificationUseCaseProof.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotSignatureVerificationUseCaseProof.prototype, "_useCase", {
            get: $util.oneOfGetter($oneOfFields = ["useCase"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotSignatureVerificationUseCaseProof.prototype, "_signature", {
            get: $util.oneOfGetter($oneOfFields = ["signature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotSignatureVerificationUseCaseProof instance using the specified properties.
         * @function create
         * @memberof proto.BotSignatureVerificationUseCaseProof
         * @static
         * @param {proto.IBotSignatureVerificationUseCaseProof=} [properties] Properties to set
         * @returns {proto.BotSignatureVerificationUseCaseProof} BotSignatureVerificationUseCaseProof instance
         */
        BotSignatureVerificationUseCaseProof.create = function create(properties) {
            return new BotSignatureVerificationUseCaseProof(properties);
        };

        /**
         * Encodes the specified BotSignatureVerificationUseCaseProof message. Does not implicitly {@link proto.BotSignatureVerificationUseCaseProof.verify|verify} messages.
         * @function encode
         * @memberof proto.BotSignatureVerificationUseCaseProof
         * @static
         * @param {proto.IBotSignatureVerificationUseCaseProof} message BotSignatureVerificationUseCaseProof message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotSignatureVerificationUseCaseProof.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.version);
            if (message.useCase != null && Object.hasOwnProperty.call(message, "useCase"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.useCase);
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.signature);
            if (message.certificateChain != null && message.certificateChain.length)
                for (var i = 0; i < message.certificateChain.length; ++i)
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.certificateChain[i]);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotSignatureVerificationUseCaseProof message, length delimited. Does not implicitly {@link proto.BotSignatureVerificationUseCaseProof.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotSignatureVerificationUseCaseProof
         * @static
         * @param {proto.IBotSignatureVerificationUseCaseProof} message BotSignatureVerificationUseCaseProof message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotSignatureVerificationUseCaseProof.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotSignatureVerificationUseCaseProof message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotSignatureVerificationUseCaseProof
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotSignatureVerificationUseCaseProof} BotSignatureVerificationUseCaseProof
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotSignatureVerificationUseCaseProof.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.version = reader.int32();
                        message._version = "version";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.useCase = reader.int32();
                        message._useCase = "useCase";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.signature = reader.bytes();
                        message._signature = "signature";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        if (!(message.certificateChain && message.certificateChain.length))
                            message.certificateChain = [];
                        message.certificateChain.push(reader.bytes());
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotSignatureVerificationUseCaseProof message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotSignatureVerificationUseCaseProof
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotSignatureVerificationUseCaseProof} BotSignatureVerificationUseCaseProof
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotSignatureVerificationUseCaseProof.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotSignatureVerificationUseCaseProof message.
         * @function verify
         * @memberof proto.BotSignatureVerificationUseCaseProof
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotSignatureVerificationUseCaseProof.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.version != null && Object.hasOwnProperty.call(message, "version")) {
                properties._version = 1;
                if (!$util.isInteger(message.version))
                    return "version: integer expected";
            }
            if (message.useCase != null && Object.hasOwnProperty.call(message, "useCase")) {
                properties._useCase = 1;
                if (typeof message.useCase !== "number" || (message.useCase | 0) !== message.useCase)
                    return "useCase: enum value expected";
            }
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature")) {
                properties._signature = 1;
                if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                    return "signature: buffer expected";
            }
            if (message.certificateChain != null && Object.hasOwnProperty.call(message, "certificateChain")) {
                if (!Array.isArray(message.certificateChain))
                    return "certificateChain: array expected";
                for (var i = 0; i < message.certificateChain.length; ++i)
                    if (!(message.certificateChain[i] && typeof message.certificateChain[i].length === "number" || $util.isString(message.certificateChain[i])))
                        return "certificateChain: buffer[] expected";
            }
            return null;
        };

        /**
         * Creates a BotSignatureVerificationUseCaseProof message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotSignatureVerificationUseCaseProof
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotSignatureVerificationUseCaseProof} BotSignatureVerificationUseCaseProof
         */
        BotSignatureVerificationUseCaseProof.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotSignatureVerificationUseCaseProof: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.version != null)
                message.version = object.version | 0;
            switch (object.useCase) {
            case "UNSPECIFIED":
            case 0:
                message.useCase = 0;
                break;
            case "WA_BOT_MSG":
            case 1:
                message.useCase = 1;
                break;
            case "WA_TEE_BOT_MSG":
            case 2:
                message.useCase = 2;
                break;
            case "P2P_PILLS":
            case 3:
                message.useCase = 3;
                break;
            default:
                if (typeof object.useCase === "number" && (object.useCase | 0) === object.useCase)
                    message.useCase = object.useCase;
            }
            if (object.signature != null)
                if (typeof object.signature === "string")
                    $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                else if (object.signature.length >= 0)
                    message.signature = object.signature;
            if (object.certificateChain) {
                if (!Array.isArray(object.certificateChain))
                    throw TypeError(".proto.BotSignatureVerificationUseCaseProof.certificateChain: array expected");
                message.certificateChain = Array(object.certificateChain.length);
                for (var i = 0; i < object.certificateChain.length; ++i)
                    if (typeof object.certificateChain[i] === "string")
                        $util.base64.decode(object.certificateChain[i], message.certificateChain[i] = $util.newBuffer($util.base64.length(object.certificateChain[i])), 0);
                    else if (object.certificateChain[i].length >= 0)
                        message.certificateChain[i] = object.certificateChain[i];
            }
            return message;
        };

        /**
         * Creates a plain object from a BotSignatureVerificationUseCaseProof message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotSignatureVerificationUseCaseProof
         * @static
         * @param {proto.BotSignatureVerificationUseCaseProof} message BotSignatureVerificationUseCaseProof
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotSignatureVerificationUseCaseProof.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.certificateChain = [];
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                object.version = message.version;
            if (message.useCase != null && Object.hasOwnProperty.call(message, "useCase"))
                object.useCase = options.enums === String ? $root.proto.BotSignatureVerificationUseCaseProof.BotSignatureUseCase[message.useCase] === undefined ? message.useCase : $root.proto.BotSignatureVerificationUseCaseProof.BotSignatureUseCase[message.useCase] : message.useCase;
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                object.signature = options.bytes === String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === Array ? Array.prototype.slice.call(message.signature) : message.signature;
            if (message.certificateChain && message.certificateChain.length) {
                object.certificateChain = Array(message.certificateChain.length);
                for (var j = 0; j < message.certificateChain.length; ++j)
                    object.certificateChain[j] = options.bytes === String ? $util.base64.encode(message.certificateChain[j], 0, message.certificateChain[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.certificateChain[j]) : message.certificateChain[j];
            }
            return object;
        };

        /**
         * Converts this BotSignatureVerificationUseCaseProof to JSON.
         * @function toJSON
         * @memberof proto.BotSignatureVerificationUseCaseProof
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotSignatureVerificationUseCaseProof.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotSignatureVerificationUseCaseProof
         * @function getTypeUrl
         * @memberof proto.BotSignatureVerificationUseCaseProof
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotSignatureVerificationUseCaseProof.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotSignatureVerificationUseCaseProof";
        };

        /**
         * BotSignatureUseCase enum.
         * @name proto.BotSignatureVerificationUseCaseProof.BotSignatureUseCase
         * @enum {number}
         * @property {number} UNSPECIFIED=0 UNSPECIFIED value
         * @property {number} WA_BOT_MSG=1 WA_BOT_MSG value
         * @property {number} WA_TEE_BOT_MSG=2 WA_TEE_BOT_MSG value
         * @property {number} P2P_PILLS=3 P2P_PILLS value
         */
        BotSignatureVerificationUseCaseProof.BotSignatureUseCase = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNSPECIFIED"] = 0;
            values[valuesById[1] = "WA_BOT_MSG"] = 1;
            values[valuesById[2] = "WA_TEE_BOT_MSG"] = 2;
            values[valuesById[3] = "P2P_PILLS"] = 3;
            return values;
        })();

        return BotSignatureVerificationUseCaseProof;
    })();

    proto.BotSourcesMetadata = (function() {

        /**
         * Properties of a BotSourcesMetadata.
         * @memberof proto
         * @interface IBotSourcesMetadata
         * @property {Array.<proto.BotSourcesMetadata.IBotSourceItem>|null} [sources] BotSourcesMetadata sources
         */

        /**
         * Constructs a new BotSourcesMetadata.
         * @memberof proto
         * @classdesc Represents a BotSourcesMetadata.
         * @implements IBotSourcesMetadata
         * @constructor
         * @param {proto.IBotSourcesMetadata=} [properties] Properties to set
         */
        function BotSourcesMetadata(properties) {
            this.sources = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotSourcesMetadata sources.
         * @member {Array.<proto.BotSourcesMetadata.IBotSourceItem>} sources
         * @memberof proto.BotSourcesMetadata
         * @instance
         */
        BotSourcesMetadata.prototype.sources = $util.emptyArray;

        /**
         * Creates a new BotSourcesMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotSourcesMetadata
         * @static
         * @param {proto.IBotSourcesMetadata=} [properties] Properties to set
         * @returns {proto.BotSourcesMetadata} BotSourcesMetadata instance
         */
        BotSourcesMetadata.create = function create(properties) {
            return new BotSourcesMetadata(properties);
        };

        /**
         * Encodes the specified BotSourcesMetadata message. Does not implicitly {@link proto.BotSourcesMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotSourcesMetadata
         * @static
         * @param {proto.IBotSourcesMetadata} message BotSourcesMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotSourcesMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.sources != null && message.sources.length)
                for (var i = 0; i < message.sources.length; ++i)
                    $root.proto.BotSourcesMetadata.BotSourceItem.encode(message.sources[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotSourcesMetadata message, length delimited. Does not implicitly {@link proto.BotSourcesMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotSourcesMetadata
         * @static
         * @param {proto.IBotSourcesMetadata} message BotSourcesMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotSourcesMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotSourcesMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotSourcesMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotSourcesMetadata} BotSourcesMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotSourcesMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(message.sources && message.sources.length))
                            message.sources = [];
                        message.sources.push($root.proto.BotSourcesMetadata.BotSourceItem.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotSourcesMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotSourcesMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotSourcesMetadata} BotSourcesMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotSourcesMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotSourcesMetadata message.
         * @function verify
         * @memberof proto.BotSourcesMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotSourcesMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            if (message.sources != null && Object.hasOwnProperty.call(message, "sources")) {
                if (!Array.isArray(message.sources))
                    return "sources: array expected";
                for (var i = 0; i < message.sources.length; ++i) {
                    var error = $root.proto.BotSourcesMetadata.BotSourceItem.verify(message.sources[i], q + 1);
                    if (error)
                        return "sources." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BotSourcesMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotSourcesMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotSourcesMetadata} BotSourcesMetadata
         */
        BotSourcesMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotSourcesMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.sources) {
                if (!Array.isArray(object.sources))
                    throw TypeError(".proto.BotSourcesMetadata.sources: array expected");
                message.sources = Array(object.sources.length);
                for (var i = 0; i < object.sources.length; ++i) {
                    if (!$util.isObject(object.sources[i]))
                        throw TypeError(".proto.BotSourcesMetadata.sources: object expected");
                    message.sources[i] = $root.proto.BotSourcesMetadata.BotSourceItem.fromObject(object.sources[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a BotSourcesMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotSourcesMetadata
         * @static
         * @param {proto.BotSourcesMetadata} message BotSourcesMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotSourcesMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.sources = [];
            if (message.sources && message.sources.length) {
                object.sources = Array(message.sources.length);
                for (var j = 0; j < message.sources.length; ++j)
                    object.sources[j] = $root.proto.BotSourcesMetadata.BotSourceItem.toObject(message.sources[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this BotSourcesMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotSourcesMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotSourcesMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotSourcesMetadata
         * @function getTypeUrl
         * @memberof proto.BotSourcesMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotSourcesMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotSourcesMetadata";
        };

        BotSourcesMetadata.BotSourceItem = (function() {

            /**
             * Properties of a BotSourceItem.
             * @memberof proto.BotSourcesMetadata
             * @interface IBotSourceItem
             * @property {proto.BotSourcesMetadata.BotSourceItem.SourceProvider|null} [provider] BotSourceItem provider
             * @property {string|null} [thumbnailCdnUrl] BotSourceItem thumbnailCdnUrl
             * @property {string|null} [sourceProviderUrl] BotSourceItem sourceProviderUrl
             * @property {string|null} [sourceQuery] BotSourceItem sourceQuery
             * @property {string|null} [faviconCdnUrl] BotSourceItem faviconCdnUrl
             * @property {number|null} [citationNumber] BotSourceItem citationNumber
             * @property {string|null} [sourceTitle] BotSourceItem sourceTitle
             */

            /**
             * Constructs a new BotSourceItem.
             * @memberof proto.BotSourcesMetadata
             * @classdesc Represents a BotSourceItem.
             * @implements IBotSourceItem
             * @constructor
             * @param {proto.BotSourcesMetadata.IBotSourceItem=} [properties] Properties to set
             */
            function BotSourceItem(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * BotSourceItem provider.
             * @member {proto.BotSourcesMetadata.BotSourceItem.SourceProvider|null|undefined} provider
             * @memberof proto.BotSourcesMetadata.BotSourceItem
             * @instance
             */
            BotSourceItem.prototype.provider = null;

            /**
             * BotSourceItem thumbnailCdnUrl.
             * @member {string|null|undefined} thumbnailCdnUrl
             * @memberof proto.BotSourcesMetadata.BotSourceItem
             * @instance
             */
            BotSourceItem.prototype.thumbnailCdnUrl = null;

            /**
             * BotSourceItem sourceProviderUrl.
             * @member {string|null|undefined} sourceProviderUrl
             * @memberof proto.BotSourcesMetadata.BotSourceItem
             * @instance
             */
            BotSourceItem.prototype.sourceProviderUrl = null;

            /**
             * BotSourceItem sourceQuery.
             * @member {string|null|undefined} sourceQuery
             * @memberof proto.BotSourcesMetadata.BotSourceItem
             * @instance
             */
            BotSourceItem.prototype.sourceQuery = null;

            /**
             * BotSourceItem faviconCdnUrl.
             * @member {string|null|undefined} faviconCdnUrl
             * @memberof proto.BotSourcesMetadata.BotSourceItem
             * @instance
             */
            BotSourceItem.prototype.faviconCdnUrl = null;

            /**
             * BotSourceItem citationNumber.
             * @member {number|null|undefined} citationNumber
             * @memberof proto.BotSourcesMetadata.BotSourceItem
             * @instance
             */
            BotSourceItem.prototype.citationNumber = null;

            /**
             * BotSourceItem sourceTitle.
             * @member {string|null|undefined} sourceTitle
             * @memberof proto.BotSourcesMetadata.BotSourceItem
             * @instance
             */
            BotSourceItem.prototype.sourceTitle = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotSourceItem.prototype, "_provider", {
                get: $util.oneOfGetter($oneOfFields = ["provider"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotSourceItem.prototype, "_thumbnailCdnUrl", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailCdnUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotSourceItem.prototype, "_sourceProviderUrl", {
                get: $util.oneOfGetter($oneOfFields = ["sourceProviderUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotSourceItem.prototype, "_sourceQuery", {
                get: $util.oneOfGetter($oneOfFields = ["sourceQuery"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotSourceItem.prototype, "_faviconCdnUrl", {
                get: $util.oneOfGetter($oneOfFields = ["faviconCdnUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotSourceItem.prototype, "_citationNumber", {
                get: $util.oneOfGetter($oneOfFields = ["citationNumber"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotSourceItem.prototype, "_sourceTitle", {
                get: $util.oneOfGetter($oneOfFields = ["sourceTitle"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BotSourceItem instance using the specified properties.
             * @function create
             * @memberof proto.BotSourcesMetadata.BotSourceItem
             * @static
             * @param {proto.BotSourcesMetadata.IBotSourceItem=} [properties] Properties to set
             * @returns {proto.BotSourcesMetadata.BotSourceItem} BotSourceItem instance
             */
            BotSourceItem.create = function create(properties) {
                return new BotSourceItem(properties);
            };

            /**
             * Encodes the specified BotSourceItem message. Does not implicitly {@link proto.BotSourcesMetadata.BotSourceItem.verify|verify} messages.
             * @function encode
             * @memberof proto.BotSourcesMetadata.BotSourceItem
             * @static
             * @param {proto.BotSourcesMetadata.IBotSourceItem} message BotSourceItem message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BotSourceItem.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.provider != null && Object.hasOwnProperty.call(message, "provider"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.provider);
                if (message.thumbnailCdnUrl != null && Object.hasOwnProperty.call(message, "thumbnailCdnUrl"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.thumbnailCdnUrl);
                if (message.sourceProviderUrl != null && Object.hasOwnProperty.call(message, "sourceProviderUrl"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.sourceProviderUrl);
                if (message.sourceQuery != null && Object.hasOwnProperty.call(message, "sourceQuery"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.sourceQuery);
                if (message.faviconCdnUrl != null && Object.hasOwnProperty.call(message, "faviconCdnUrl"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.faviconCdnUrl);
                if (message.citationNumber != null && Object.hasOwnProperty.call(message, "citationNumber"))
                    writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.citationNumber);
                if (message.sourceTitle != null && Object.hasOwnProperty.call(message, "sourceTitle"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.sourceTitle);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified BotSourceItem message, length delimited. Does not implicitly {@link proto.BotSourcesMetadata.BotSourceItem.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.BotSourcesMetadata.BotSourceItem
             * @static
             * @param {proto.BotSourcesMetadata.IBotSourceItem} message BotSourceItem message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BotSourceItem.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a BotSourceItem message from the specified reader or buffer.
             * @function decode
             * @memberof proto.BotSourcesMetadata.BotSourceItem
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.BotSourcesMetadata.BotSourceItem} BotSourceItem
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BotSourceItem.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.provider = reader.int32();
                            message._provider = "provider";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.thumbnailCdnUrl = reader.stringVerify();
                            message._thumbnailCdnUrl = "thumbnailCdnUrl";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.sourceProviderUrl = reader.stringVerify();
                            message._sourceProviderUrl = "sourceProviderUrl";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.sourceQuery = reader.stringVerify();
                            message._sourceQuery = "sourceQuery";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.faviconCdnUrl = reader.stringVerify();
                            message._faviconCdnUrl = "faviconCdnUrl";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.citationNumber = reader.uint32();
                            message._citationNumber = "citationNumber";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.sourceTitle = reader.stringVerify();
                            message._sourceTitle = "sourceTitle";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a BotSourceItem message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.BotSourcesMetadata.BotSourceItem
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.BotSourcesMetadata.BotSourceItem} BotSourceItem
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BotSourceItem.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BotSourceItem message.
             * @function verify
             * @memberof proto.BotSourcesMetadata.BotSourceItem
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BotSourceItem.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.provider != null && Object.hasOwnProperty.call(message, "provider")) {
                    properties._provider = 1;
                    if (typeof message.provider !== "number" || (message.provider | 0) !== message.provider)
                        return "provider: enum value expected";
                }
                if (message.thumbnailCdnUrl != null && Object.hasOwnProperty.call(message, "thumbnailCdnUrl")) {
                    properties._thumbnailCdnUrl = 1;
                    if (!$util.isString(message.thumbnailCdnUrl))
                        return "thumbnailCdnUrl: string expected";
                }
                if (message.sourceProviderUrl != null && Object.hasOwnProperty.call(message, "sourceProviderUrl")) {
                    properties._sourceProviderUrl = 1;
                    if (!$util.isString(message.sourceProviderUrl))
                        return "sourceProviderUrl: string expected";
                }
                if (message.sourceQuery != null && Object.hasOwnProperty.call(message, "sourceQuery")) {
                    properties._sourceQuery = 1;
                    if (!$util.isString(message.sourceQuery))
                        return "sourceQuery: string expected";
                }
                if (message.faviconCdnUrl != null && Object.hasOwnProperty.call(message, "faviconCdnUrl")) {
                    properties._faviconCdnUrl = 1;
                    if (!$util.isString(message.faviconCdnUrl))
                        return "faviconCdnUrl: string expected";
                }
                if (message.citationNumber != null && Object.hasOwnProperty.call(message, "citationNumber")) {
                    properties._citationNumber = 1;
                    if (!$util.isInteger(message.citationNumber))
                        return "citationNumber: integer expected";
                }
                if (message.sourceTitle != null && Object.hasOwnProperty.call(message, "sourceTitle")) {
                    properties._sourceTitle = 1;
                    if (!$util.isString(message.sourceTitle))
                        return "sourceTitle: string expected";
                }
                return null;
            };

            /**
             * Creates a BotSourceItem message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.BotSourcesMetadata.BotSourceItem
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.BotSourcesMetadata.BotSourceItem} BotSourceItem
             */
            BotSourceItem.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.BotSourcesMetadata.BotSourceItem: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.provider) {
                case "UNKNOWN":
                case 0:
                    message.provider = 0;
                    break;
                case "BING":
                case 1:
                    message.provider = 1;
                    break;
                case "GOOGLE":
                case 2:
                    message.provider = 2;
                    break;
                case "SUPPORT":
                case 3:
                    message.provider = 3;
                    break;
                case "OTHER":
                case 4:
                    message.provider = 4;
                    break;
                default:
                    if (typeof object.provider === "number" && (object.provider | 0) === object.provider)
                        message.provider = object.provider;
                }
                if (object.thumbnailCdnUrl != null)
                    message.thumbnailCdnUrl = String(object.thumbnailCdnUrl);
                if (object.sourceProviderUrl != null)
                    message.sourceProviderUrl = String(object.sourceProviderUrl);
                if (object.sourceQuery != null)
                    message.sourceQuery = String(object.sourceQuery);
                if (object.faviconCdnUrl != null)
                    message.faviconCdnUrl = String(object.faviconCdnUrl);
                if (object.citationNumber != null)
                    message.citationNumber = object.citationNumber >>> 0;
                if (object.sourceTitle != null)
                    message.sourceTitle = String(object.sourceTitle);
                return message;
            };

            /**
             * Creates a plain object from a BotSourceItem message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.BotSourcesMetadata.BotSourceItem
             * @static
             * @param {proto.BotSourcesMetadata.BotSourceItem} message BotSourceItem
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BotSourceItem.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.provider != null && Object.hasOwnProperty.call(message, "provider"))
                    object.provider = options.enums === String ? $root.proto.BotSourcesMetadata.BotSourceItem.SourceProvider[message.provider] === undefined ? message.provider : $root.proto.BotSourcesMetadata.BotSourceItem.SourceProvider[message.provider] : message.provider;
                if (message.thumbnailCdnUrl != null && Object.hasOwnProperty.call(message, "thumbnailCdnUrl"))
                    object.thumbnailCdnUrl = message.thumbnailCdnUrl;
                if (message.sourceProviderUrl != null && Object.hasOwnProperty.call(message, "sourceProviderUrl"))
                    object.sourceProviderUrl = message.sourceProviderUrl;
                if (message.sourceQuery != null && Object.hasOwnProperty.call(message, "sourceQuery"))
                    object.sourceQuery = message.sourceQuery;
                if (message.faviconCdnUrl != null && Object.hasOwnProperty.call(message, "faviconCdnUrl"))
                    object.faviconCdnUrl = message.faviconCdnUrl;
                if (message.citationNumber != null && Object.hasOwnProperty.call(message, "citationNumber"))
                    object.citationNumber = message.citationNumber;
                if (message.sourceTitle != null && Object.hasOwnProperty.call(message, "sourceTitle"))
                    object.sourceTitle = message.sourceTitle;
                return object;
            };

            /**
             * Converts this BotSourceItem to JSON.
             * @function toJSON
             * @memberof proto.BotSourcesMetadata.BotSourceItem
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BotSourceItem.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for BotSourceItem
             * @function getTypeUrl
             * @memberof proto.BotSourcesMetadata.BotSourceItem
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            BotSourceItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.BotSourcesMetadata.BotSourceItem";
            };

            /**
             * SourceProvider enum.
             * @name proto.BotSourcesMetadata.BotSourceItem.SourceProvider
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} BING=1 BING value
             * @property {number} GOOGLE=2 GOOGLE value
             * @property {number} SUPPORT=3 SUPPORT value
             * @property {number} OTHER=4 OTHER value
             */
            BotSourceItem.SourceProvider = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "BING"] = 1;
                values[valuesById[2] = "GOOGLE"] = 2;
                values[valuesById[3] = "SUPPORT"] = 3;
                values[valuesById[4] = "OTHER"] = 4;
                return values;
            })();

            return BotSourceItem;
        })();

        return BotSourcesMetadata;
    })();

    proto.BotSuggestedPromptMetadata = (function() {

        /**
         * Properties of a BotSuggestedPromptMetadata.
         * @memberof proto
         * @interface IBotSuggestedPromptMetadata
         * @property {Array.<string>|null} [suggestedPrompts] BotSuggestedPromptMetadata suggestedPrompts
         * @property {number|null} [selectedPromptIndex] BotSuggestedPromptMetadata selectedPromptIndex
         * @property {proto.IBotPromptSuggestions|null} [promptSuggestions] BotSuggestedPromptMetadata promptSuggestions
         * @property {string|null} [selectedPromptId] BotSuggestedPromptMetadata selectedPromptId
         */

        /**
         * Constructs a new BotSuggestedPromptMetadata.
         * @memberof proto
         * @classdesc Represents a BotSuggestedPromptMetadata.
         * @implements IBotSuggestedPromptMetadata
         * @constructor
         * @param {proto.IBotSuggestedPromptMetadata=} [properties] Properties to set
         */
        function BotSuggestedPromptMetadata(properties) {
            this.suggestedPrompts = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotSuggestedPromptMetadata suggestedPrompts.
         * @member {Array.<string>} suggestedPrompts
         * @memberof proto.BotSuggestedPromptMetadata
         * @instance
         */
        BotSuggestedPromptMetadata.prototype.suggestedPrompts = $util.emptyArray;

        /**
         * BotSuggestedPromptMetadata selectedPromptIndex.
         * @member {number|null|undefined} selectedPromptIndex
         * @memberof proto.BotSuggestedPromptMetadata
         * @instance
         */
        BotSuggestedPromptMetadata.prototype.selectedPromptIndex = null;

        /**
         * BotSuggestedPromptMetadata promptSuggestions.
         * @member {proto.IBotPromptSuggestions|null|undefined} promptSuggestions
         * @memberof proto.BotSuggestedPromptMetadata
         * @instance
         */
        BotSuggestedPromptMetadata.prototype.promptSuggestions = null;

        /**
         * BotSuggestedPromptMetadata selectedPromptId.
         * @member {string|null|undefined} selectedPromptId
         * @memberof proto.BotSuggestedPromptMetadata
         * @instance
         */
        BotSuggestedPromptMetadata.prototype.selectedPromptId = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotSuggestedPromptMetadata.prototype, "_selectedPromptIndex", {
            get: $util.oneOfGetter($oneOfFields = ["selectedPromptIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotSuggestedPromptMetadata.prototype, "_promptSuggestions", {
            get: $util.oneOfGetter($oneOfFields = ["promptSuggestions"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotSuggestedPromptMetadata.prototype, "_selectedPromptId", {
            get: $util.oneOfGetter($oneOfFields = ["selectedPromptId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotSuggestedPromptMetadata instance using the specified properties.
         * @function create
         * @memberof proto.BotSuggestedPromptMetadata
         * @static
         * @param {proto.IBotSuggestedPromptMetadata=} [properties] Properties to set
         * @returns {proto.BotSuggestedPromptMetadata} BotSuggestedPromptMetadata instance
         */
        BotSuggestedPromptMetadata.create = function create(properties) {
            return new BotSuggestedPromptMetadata(properties);
        };

        /**
         * Encodes the specified BotSuggestedPromptMetadata message. Does not implicitly {@link proto.BotSuggestedPromptMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.BotSuggestedPromptMetadata
         * @static
         * @param {proto.IBotSuggestedPromptMetadata} message BotSuggestedPromptMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotSuggestedPromptMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.suggestedPrompts != null && message.suggestedPrompts.length)
                for (var i = 0; i < message.suggestedPrompts.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.suggestedPrompts[i]);
            if (message.selectedPromptIndex != null && Object.hasOwnProperty.call(message, "selectedPromptIndex"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.selectedPromptIndex);
            if (message.promptSuggestions != null && Object.hasOwnProperty.call(message, "promptSuggestions"))
                $root.proto.BotPromptSuggestions.encode(message.promptSuggestions, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.selectedPromptId != null && Object.hasOwnProperty.call(message, "selectedPromptId"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.selectedPromptId);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotSuggestedPromptMetadata message, length delimited. Does not implicitly {@link proto.BotSuggestedPromptMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotSuggestedPromptMetadata
         * @static
         * @param {proto.IBotSuggestedPromptMetadata} message BotSuggestedPromptMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotSuggestedPromptMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotSuggestedPromptMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotSuggestedPromptMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotSuggestedPromptMetadata} BotSuggestedPromptMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotSuggestedPromptMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(message.suggestedPrompts && message.suggestedPrompts.length))
                            message.suggestedPrompts = [];
                        message.suggestedPrompts.push(reader.stringVerify());
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.selectedPromptIndex = reader.uint32();
                        message._selectedPromptIndex = "selectedPromptIndex";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.promptSuggestions = $root.proto.BotPromptSuggestions.decode(reader, reader.uint32(), undefined, q + 1, message.promptSuggestions);
                        message._promptSuggestions = "promptSuggestions";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.selectedPromptId = reader.stringVerify();
                        message._selectedPromptId = "selectedPromptId";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotSuggestedPromptMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotSuggestedPromptMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotSuggestedPromptMetadata} BotSuggestedPromptMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotSuggestedPromptMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotSuggestedPromptMetadata message.
         * @function verify
         * @memberof proto.BotSuggestedPromptMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotSuggestedPromptMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.suggestedPrompts != null && Object.hasOwnProperty.call(message, "suggestedPrompts")) {
                if (!Array.isArray(message.suggestedPrompts))
                    return "suggestedPrompts: array expected";
                for (var i = 0; i < message.suggestedPrompts.length; ++i)
                    if (!$util.isString(message.suggestedPrompts[i]))
                        return "suggestedPrompts: string[] expected";
            }
            if (message.selectedPromptIndex != null && Object.hasOwnProperty.call(message, "selectedPromptIndex")) {
                properties._selectedPromptIndex = 1;
                if (!$util.isInteger(message.selectedPromptIndex))
                    return "selectedPromptIndex: integer expected";
            }
            if (message.promptSuggestions != null && Object.hasOwnProperty.call(message, "promptSuggestions")) {
                properties._promptSuggestions = 1;
                {
                    var error = $root.proto.BotPromptSuggestions.verify(message.promptSuggestions, q + 1);
                    if (error)
                        return "promptSuggestions." + error;
                }
            }
            if (message.selectedPromptId != null && Object.hasOwnProperty.call(message, "selectedPromptId")) {
                properties._selectedPromptId = 1;
                if (!$util.isString(message.selectedPromptId))
                    return "selectedPromptId: string expected";
            }
            return null;
        };

        /**
         * Creates a BotSuggestedPromptMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotSuggestedPromptMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotSuggestedPromptMetadata} BotSuggestedPromptMetadata
         */
        BotSuggestedPromptMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotSuggestedPromptMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.suggestedPrompts) {
                if (!Array.isArray(object.suggestedPrompts))
                    throw TypeError(".proto.BotSuggestedPromptMetadata.suggestedPrompts: array expected");
                message.suggestedPrompts = Array(object.suggestedPrompts.length);
                for (var i = 0; i < object.suggestedPrompts.length; ++i)
                    message.suggestedPrompts[i] = String(object.suggestedPrompts[i]);
            }
            if (object.selectedPromptIndex != null)
                message.selectedPromptIndex = object.selectedPromptIndex >>> 0;
            if (object.promptSuggestions != null) {
                if (!$util.isObject(object.promptSuggestions))
                    throw TypeError(".proto.BotSuggestedPromptMetadata.promptSuggestions: object expected");
                message.promptSuggestions = $root.proto.BotPromptSuggestions.fromObject(object.promptSuggestions, q + 1);
            }
            if (object.selectedPromptId != null)
                message.selectedPromptId = String(object.selectedPromptId);
            return message;
        };

        /**
         * Creates a plain object from a BotSuggestedPromptMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotSuggestedPromptMetadata
         * @static
         * @param {proto.BotSuggestedPromptMetadata} message BotSuggestedPromptMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotSuggestedPromptMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.suggestedPrompts = [];
            if (message.suggestedPrompts && message.suggestedPrompts.length) {
                object.suggestedPrompts = Array(message.suggestedPrompts.length);
                for (var j = 0; j < message.suggestedPrompts.length; ++j)
                    object.suggestedPrompts[j] = message.suggestedPrompts[j];
            }
            if (message.selectedPromptIndex != null && Object.hasOwnProperty.call(message, "selectedPromptIndex"))
                object.selectedPromptIndex = message.selectedPromptIndex;
            if (message.promptSuggestions != null && Object.hasOwnProperty.call(message, "promptSuggestions"))
                object.promptSuggestions = $root.proto.BotPromptSuggestions.toObject(message.promptSuggestions, options, q + 1);
            if (message.selectedPromptId != null && Object.hasOwnProperty.call(message, "selectedPromptId"))
                object.selectedPromptId = message.selectedPromptId;
            return object;
        };

        /**
         * Converts this BotSuggestedPromptMetadata to JSON.
         * @function toJSON
         * @memberof proto.BotSuggestedPromptMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotSuggestedPromptMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotSuggestedPromptMetadata
         * @function getTypeUrl
         * @memberof proto.BotSuggestedPromptMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotSuggestedPromptMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotSuggestedPromptMetadata";
        };

        return BotSuggestedPromptMetadata;
    })();

    proto.BotUnifiedResponseMutation = (function() {

        /**
         * Properties of a BotUnifiedResponseMutation.
         * @memberof proto
         * @interface IBotUnifiedResponseMutation
         * @property {proto.BotUnifiedResponseMutation.ISideBySideMetadata|null} [sbsMetadata] BotUnifiedResponseMutation sbsMetadata
         * @property {Array.<proto.BotUnifiedResponseMutation.IMediaDetailsMetadata>|null} [mediaDetailsMetadataList] BotUnifiedResponseMutation mediaDetailsMetadataList
         */

        /**
         * Constructs a new BotUnifiedResponseMutation.
         * @memberof proto
         * @classdesc Represents a BotUnifiedResponseMutation.
         * @implements IBotUnifiedResponseMutation
         * @constructor
         * @param {proto.IBotUnifiedResponseMutation=} [properties] Properties to set
         */
        function BotUnifiedResponseMutation(properties) {
            this.mediaDetailsMetadataList = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BotUnifiedResponseMutation sbsMetadata.
         * @member {proto.BotUnifiedResponseMutation.ISideBySideMetadata|null|undefined} sbsMetadata
         * @memberof proto.BotUnifiedResponseMutation
         * @instance
         */
        BotUnifiedResponseMutation.prototype.sbsMetadata = null;

        /**
         * BotUnifiedResponseMutation mediaDetailsMetadataList.
         * @member {Array.<proto.BotUnifiedResponseMutation.IMediaDetailsMetadata>} mediaDetailsMetadataList
         * @memberof proto.BotUnifiedResponseMutation
         * @instance
         */
        BotUnifiedResponseMutation.prototype.mediaDetailsMetadataList = $util.emptyArray;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotUnifiedResponseMutation.prototype, "_sbsMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["sbsMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new BotUnifiedResponseMutation instance using the specified properties.
         * @function create
         * @memberof proto.BotUnifiedResponseMutation
         * @static
         * @param {proto.IBotUnifiedResponseMutation=} [properties] Properties to set
         * @returns {proto.BotUnifiedResponseMutation} BotUnifiedResponseMutation instance
         */
        BotUnifiedResponseMutation.create = function create(properties) {
            return new BotUnifiedResponseMutation(properties);
        };

        /**
         * Encodes the specified BotUnifiedResponseMutation message. Does not implicitly {@link proto.BotUnifiedResponseMutation.verify|verify} messages.
         * @function encode
         * @memberof proto.BotUnifiedResponseMutation
         * @static
         * @param {proto.IBotUnifiedResponseMutation} message BotUnifiedResponseMutation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotUnifiedResponseMutation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.sbsMetadata != null && Object.hasOwnProperty.call(message, "sbsMetadata"))
                $root.proto.BotUnifiedResponseMutation.SideBySideMetadata.encode(message.sbsMetadata, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.mediaDetailsMetadataList != null && message.mediaDetailsMetadataList.length)
                for (var i = 0; i < message.mediaDetailsMetadataList.length; ++i)
                    $root.proto.BotUnifiedResponseMutation.MediaDetailsMetadata.encode(message.mediaDetailsMetadataList[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified BotUnifiedResponseMutation message, length delimited. Does not implicitly {@link proto.BotUnifiedResponseMutation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.BotUnifiedResponseMutation
         * @static
         * @param {proto.IBotUnifiedResponseMutation} message BotUnifiedResponseMutation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BotUnifiedResponseMutation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BotUnifiedResponseMutation message from the specified reader or buffer.
         * @function decode
         * @memberof proto.BotUnifiedResponseMutation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.BotUnifiedResponseMutation} BotUnifiedResponseMutation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotUnifiedResponseMutation.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.sbsMetadata = $root.proto.BotUnifiedResponseMutation.SideBySideMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.sbsMetadata);
                        message._sbsMetadata = "sbsMetadata";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(message.mediaDetailsMetadataList && message.mediaDetailsMetadataList.length))
                            message.mediaDetailsMetadataList = [];
                        message.mediaDetailsMetadataList.push($root.proto.BotUnifiedResponseMutation.MediaDetailsMetadata.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a BotUnifiedResponseMutation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.BotUnifiedResponseMutation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.BotUnifiedResponseMutation} BotUnifiedResponseMutation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BotUnifiedResponseMutation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BotUnifiedResponseMutation message.
         * @function verify
         * @memberof proto.BotUnifiedResponseMutation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BotUnifiedResponseMutation.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.sbsMetadata != null && Object.hasOwnProperty.call(message, "sbsMetadata")) {
                properties._sbsMetadata = 1;
                {
                    var error = $root.proto.BotUnifiedResponseMutation.SideBySideMetadata.verify(message.sbsMetadata, q + 1);
                    if (error)
                        return "sbsMetadata." + error;
                }
            }
            if (message.mediaDetailsMetadataList != null && Object.hasOwnProperty.call(message, "mediaDetailsMetadataList")) {
                if (!Array.isArray(message.mediaDetailsMetadataList))
                    return "mediaDetailsMetadataList: array expected";
                for (var i = 0; i < message.mediaDetailsMetadataList.length; ++i) {
                    var error = $root.proto.BotUnifiedResponseMutation.MediaDetailsMetadata.verify(message.mediaDetailsMetadataList[i], q + 1);
                    if (error)
                        return "mediaDetailsMetadataList." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BotUnifiedResponseMutation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.BotUnifiedResponseMutation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.BotUnifiedResponseMutation} BotUnifiedResponseMutation
         */
        BotUnifiedResponseMutation.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.BotUnifiedResponseMutation: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.sbsMetadata != null) {
                if (!$util.isObject(object.sbsMetadata))
                    throw TypeError(".proto.BotUnifiedResponseMutation.sbsMetadata: object expected");
                message.sbsMetadata = $root.proto.BotUnifiedResponseMutation.SideBySideMetadata.fromObject(object.sbsMetadata, q + 1);
            }
            if (object.mediaDetailsMetadataList) {
                if (!Array.isArray(object.mediaDetailsMetadataList))
                    throw TypeError(".proto.BotUnifiedResponseMutation.mediaDetailsMetadataList: array expected");
                message.mediaDetailsMetadataList = Array(object.mediaDetailsMetadataList.length);
                for (var i = 0; i < object.mediaDetailsMetadataList.length; ++i) {
                    if (!$util.isObject(object.mediaDetailsMetadataList[i]))
                        throw TypeError(".proto.BotUnifiedResponseMutation.mediaDetailsMetadataList: object expected");
                    message.mediaDetailsMetadataList[i] = $root.proto.BotUnifiedResponseMutation.MediaDetailsMetadata.fromObject(object.mediaDetailsMetadataList[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a BotUnifiedResponseMutation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.BotUnifiedResponseMutation
         * @static
         * @param {proto.BotUnifiedResponseMutation} message BotUnifiedResponseMutation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BotUnifiedResponseMutation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.mediaDetailsMetadataList = [];
            if (message.sbsMetadata != null && Object.hasOwnProperty.call(message, "sbsMetadata"))
                object.sbsMetadata = $root.proto.BotUnifiedResponseMutation.SideBySideMetadata.toObject(message.sbsMetadata, options, q + 1);
            if (message.mediaDetailsMetadataList && message.mediaDetailsMetadataList.length) {
                object.mediaDetailsMetadataList = Array(message.mediaDetailsMetadataList.length);
                for (var j = 0; j < message.mediaDetailsMetadataList.length; ++j)
                    object.mediaDetailsMetadataList[j] = $root.proto.BotUnifiedResponseMutation.MediaDetailsMetadata.toObject(message.mediaDetailsMetadataList[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this BotUnifiedResponseMutation to JSON.
         * @function toJSON
         * @memberof proto.BotUnifiedResponseMutation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BotUnifiedResponseMutation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BotUnifiedResponseMutation
         * @function getTypeUrl
         * @memberof proto.BotUnifiedResponseMutation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BotUnifiedResponseMutation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.BotUnifiedResponseMutation";
        };

        BotUnifiedResponseMutation.MediaDetailsMetadata = (function() {

            /**
             * Properties of a MediaDetailsMetadata.
             * @memberof proto.BotUnifiedResponseMutation
             * @interface IMediaDetailsMetadata
             * @property {string|null} [id] MediaDetailsMetadata id
             * @property {proto.IBotMediaMetadata|null} [highResMedia] MediaDetailsMetadata highResMedia
             * @property {proto.IBotMediaMetadata|null} [previewMedia] MediaDetailsMetadata previewMedia
             */

            /**
             * Constructs a new MediaDetailsMetadata.
             * @memberof proto.BotUnifiedResponseMutation
             * @classdesc Represents a MediaDetailsMetadata.
             * @implements IMediaDetailsMetadata
             * @constructor
             * @param {proto.BotUnifiedResponseMutation.IMediaDetailsMetadata=} [properties] Properties to set
             */
            function MediaDetailsMetadata(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MediaDetailsMetadata id.
             * @member {string|null|undefined} id
             * @memberof proto.BotUnifiedResponseMutation.MediaDetailsMetadata
             * @instance
             */
            MediaDetailsMetadata.prototype.id = null;

            /**
             * MediaDetailsMetadata highResMedia.
             * @member {proto.IBotMediaMetadata|null|undefined} highResMedia
             * @memberof proto.BotUnifiedResponseMutation.MediaDetailsMetadata
             * @instance
             */
            MediaDetailsMetadata.prototype.highResMedia = null;

            /**
             * MediaDetailsMetadata previewMedia.
             * @member {proto.IBotMediaMetadata|null|undefined} previewMedia
             * @memberof proto.BotUnifiedResponseMutation.MediaDetailsMetadata
             * @instance
             */
            MediaDetailsMetadata.prototype.previewMedia = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MediaDetailsMetadata.prototype, "_id", {
                get: $util.oneOfGetter($oneOfFields = ["id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MediaDetailsMetadata.prototype, "_highResMedia", {
                get: $util.oneOfGetter($oneOfFields = ["highResMedia"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MediaDetailsMetadata.prototype, "_previewMedia", {
                get: $util.oneOfGetter($oneOfFields = ["previewMedia"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new MediaDetailsMetadata instance using the specified properties.
             * @function create
             * @memberof proto.BotUnifiedResponseMutation.MediaDetailsMetadata
             * @static
             * @param {proto.BotUnifiedResponseMutation.IMediaDetailsMetadata=} [properties] Properties to set
             * @returns {proto.BotUnifiedResponseMutation.MediaDetailsMetadata} MediaDetailsMetadata instance
             */
            MediaDetailsMetadata.create = function create(properties) {
                return new MediaDetailsMetadata(properties);
            };

            /**
             * Encodes the specified MediaDetailsMetadata message. Does not implicitly {@link proto.BotUnifiedResponseMutation.MediaDetailsMetadata.verify|verify} messages.
             * @function encode
             * @memberof proto.BotUnifiedResponseMutation.MediaDetailsMetadata
             * @static
             * @param {proto.BotUnifiedResponseMutation.IMediaDetailsMetadata} message MediaDetailsMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MediaDetailsMetadata.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
                if (message.highResMedia != null && Object.hasOwnProperty.call(message, "highResMedia"))
                    $root.proto.BotMediaMetadata.encode(message.highResMedia, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.previewMedia != null && Object.hasOwnProperty.call(message, "previewMedia"))
                    $root.proto.BotMediaMetadata.encode(message.previewMedia, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MediaDetailsMetadata message, length delimited. Does not implicitly {@link proto.BotUnifiedResponseMutation.MediaDetailsMetadata.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.BotUnifiedResponseMutation.MediaDetailsMetadata
             * @static
             * @param {proto.BotUnifiedResponseMutation.IMediaDetailsMetadata} message MediaDetailsMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MediaDetailsMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a MediaDetailsMetadata message from the specified reader or buffer.
             * @function decode
             * @memberof proto.BotUnifiedResponseMutation.MediaDetailsMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.BotUnifiedResponseMutation.MediaDetailsMetadata} MediaDetailsMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MediaDetailsMetadata.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.id = reader.stringVerify();
                            message._id = "id";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.highResMedia = $root.proto.BotMediaMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.highResMedia);
                            message._highResMedia = "highResMedia";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.previewMedia = $root.proto.BotMediaMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.previewMedia);
                            message._previewMedia = "previewMedia";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a MediaDetailsMetadata message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.BotUnifiedResponseMutation.MediaDetailsMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.BotUnifiedResponseMutation.MediaDetailsMetadata} MediaDetailsMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MediaDetailsMetadata.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MediaDetailsMetadata message.
             * @function verify
             * @memberof proto.BotUnifiedResponseMutation.MediaDetailsMetadata
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MediaDetailsMetadata.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.id != null && Object.hasOwnProperty.call(message, "id")) {
                    properties._id = 1;
                    if (!$util.isString(message.id))
                        return "id: string expected";
                }
                if (message.highResMedia != null && Object.hasOwnProperty.call(message, "highResMedia")) {
                    properties._highResMedia = 1;
                    {
                        var error = $root.proto.BotMediaMetadata.verify(message.highResMedia, q + 1);
                        if (error)
                            return "highResMedia." + error;
                    }
                }
                if (message.previewMedia != null && Object.hasOwnProperty.call(message, "previewMedia")) {
                    properties._previewMedia = 1;
                    {
                        var error = $root.proto.BotMediaMetadata.verify(message.previewMedia, q + 1);
                        if (error)
                            return "previewMedia." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a MediaDetailsMetadata message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.BotUnifiedResponseMutation.MediaDetailsMetadata
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.BotUnifiedResponseMutation.MediaDetailsMetadata} MediaDetailsMetadata
             */
            MediaDetailsMetadata.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.BotUnifiedResponseMutation.MediaDetailsMetadata: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.id != null)
                    message.id = String(object.id);
                if (object.highResMedia != null) {
                    if (!$util.isObject(object.highResMedia))
                        throw TypeError(".proto.BotUnifiedResponseMutation.MediaDetailsMetadata.highResMedia: object expected");
                    message.highResMedia = $root.proto.BotMediaMetadata.fromObject(object.highResMedia, q + 1);
                }
                if (object.previewMedia != null) {
                    if (!$util.isObject(object.previewMedia))
                        throw TypeError(".proto.BotUnifiedResponseMutation.MediaDetailsMetadata.previewMedia: object expected");
                    message.previewMedia = $root.proto.BotMediaMetadata.fromObject(object.previewMedia, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a MediaDetailsMetadata message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.BotUnifiedResponseMutation.MediaDetailsMetadata
             * @static
             * @param {proto.BotUnifiedResponseMutation.MediaDetailsMetadata} message MediaDetailsMetadata
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MediaDetailsMetadata.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    object.id = message.id;
                if (message.highResMedia != null && Object.hasOwnProperty.call(message, "highResMedia"))
                    object.highResMedia = $root.proto.BotMediaMetadata.toObject(message.highResMedia, options, q + 1);
                if (message.previewMedia != null && Object.hasOwnProperty.call(message, "previewMedia"))
                    object.previewMedia = $root.proto.BotMediaMetadata.toObject(message.previewMedia, options, q + 1);
                return object;
            };

            /**
             * Converts this MediaDetailsMetadata to JSON.
             * @function toJSON
             * @memberof proto.BotUnifiedResponseMutation.MediaDetailsMetadata
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MediaDetailsMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MediaDetailsMetadata
             * @function getTypeUrl
             * @memberof proto.BotUnifiedResponseMutation.MediaDetailsMetadata
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MediaDetailsMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.BotUnifiedResponseMutation.MediaDetailsMetadata";
            };

            return MediaDetailsMetadata;
        })();

        BotUnifiedResponseMutation.SideBySideMetadata = (function() {

            /**
             * Properties of a SideBySideMetadata.
             * @memberof proto.BotUnifiedResponseMutation
             * @interface ISideBySideMetadata
             * @property {string|null} [primaryResponseId] SideBySideMetadata primaryResponseId
             * @property {boolean|null} [surveyCtaHasRendered] SideBySideMetadata surveyCtaHasRendered
             */

            /**
             * Constructs a new SideBySideMetadata.
             * @memberof proto.BotUnifiedResponseMutation
             * @classdesc Represents a SideBySideMetadata.
             * @implements ISideBySideMetadata
             * @constructor
             * @param {proto.BotUnifiedResponseMutation.ISideBySideMetadata=} [properties] Properties to set
             */
            function SideBySideMetadata(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * SideBySideMetadata primaryResponseId.
             * @member {string|null|undefined} primaryResponseId
             * @memberof proto.BotUnifiedResponseMutation.SideBySideMetadata
             * @instance
             */
            SideBySideMetadata.prototype.primaryResponseId = null;

            /**
             * SideBySideMetadata surveyCtaHasRendered.
             * @member {boolean|null|undefined} surveyCtaHasRendered
             * @memberof proto.BotUnifiedResponseMutation.SideBySideMetadata
             * @instance
             */
            SideBySideMetadata.prototype.surveyCtaHasRendered = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideMetadata.prototype, "_primaryResponseId", {
                get: $util.oneOfGetter($oneOfFields = ["primaryResponseId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideMetadata.prototype, "_surveyCtaHasRendered", {
                get: $util.oneOfGetter($oneOfFields = ["surveyCtaHasRendered"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new SideBySideMetadata instance using the specified properties.
             * @function create
             * @memberof proto.BotUnifiedResponseMutation.SideBySideMetadata
             * @static
             * @param {proto.BotUnifiedResponseMutation.ISideBySideMetadata=} [properties] Properties to set
             * @returns {proto.BotUnifiedResponseMutation.SideBySideMetadata} SideBySideMetadata instance
             */
            SideBySideMetadata.create = function create(properties) {
                return new SideBySideMetadata(properties);
            };

            /**
             * Encodes the specified SideBySideMetadata message. Does not implicitly {@link proto.BotUnifiedResponseMutation.SideBySideMetadata.verify|verify} messages.
             * @function encode
             * @memberof proto.BotUnifiedResponseMutation.SideBySideMetadata
             * @static
             * @param {proto.BotUnifiedResponseMutation.ISideBySideMetadata} message SideBySideMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SideBySideMetadata.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.primaryResponseId != null && Object.hasOwnProperty.call(message, "primaryResponseId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.primaryResponseId);
                if (message.surveyCtaHasRendered != null && Object.hasOwnProperty.call(message, "surveyCtaHasRendered"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.surveyCtaHasRendered);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SideBySideMetadata message, length delimited. Does not implicitly {@link proto.BotUnifiedResponseMutation.SideBySideMetadata.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.BotUnifiedResponseMutation.SideBySideMetadata
             * @static
             * @param {proto.BotUnifiedResponseMutation.ISideBySideMetadata} message SideBySideMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SideBySideMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a SideBySideMetadata message from the specified reader or buffer.
             * @function decode
             * @memberof proto.BotUnifiedResponseMutation.SideBySideMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.BotUnifiedResponseMutation.SideBySideMetadata} SideBySideMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SideBySideMetadata.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.primaryResponseId = reader.stringVerify();
                            message._primaryResponseId = "primaryResponseId";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.surveyCtaHasRendered = reader.bool();
                            message._surveyCtaHasRendered = "surveyCtaHasRendered";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a SideBySideMetadata message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.BotUnifiedResponseMutation.SideBySideMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.BotUnifiedResponseMutation.SideBySideMetadata} SideBySideMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SideBySideMetadata.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SideBySideMetadata message.
             * @function verify
             * @memberof proto.BotUnifiedResponseMutation.SideBySideMetadata
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SideBySideMetadata.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.primaryResponseId != null && Object.hasOwnProperty.call(message, "primaryResponseId")) {
                    properties._primaryResponseId = 1;
                    if (!$util.isString(message.primaryResponseId))
                        return "primaryResponseId: string expected";
                }
                if (message.surveyCtaHasRendered != null && Object.hasOwnProperty.call(message, "surveyCtaHasRendered")) {
                    properties._surveyCtaHasRendered = 1;
                    if (typeof message.surveyCtaHasRendered !== "boolean")
                        return "surveyCtaHasRendered: boolean expected";
                }
                return null;
            };

            /**
             * Creates a SideBySideMetadata message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.BotUnifiedResponseMutation.SideBySideMetadata
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.BotUnifiedResponseMutation.SideBySideMetadata} SideBySideMetadata
             */
            SideBySideMetadata.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.BotUnifiedResponseMutation.SideBySideMetadata: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.primaryResponseId != null)
                    message.primaryResponseId = String(object.primaryResponseId);
                if (object.surveyCtaHasRendered != null)
                    message.surveyCtaHasRendered = Boolean(object.surveyCtaHasRendered);
                return message;
            };

            /**
             * Creates a plain object from a SideBySideMetadata message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.BotUnifiedResponseMutation.SideBySideMetadata
             * @static
             * @param {proto.BotUnifiedResponseMutation.SideBySideMetadata} message SideBySideMetadata
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SideBySideMetadata.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.primaryResponseId != null && Object.hasOwnProperty.call(message, "primaryResponseId"))
                    object.primaryResponseId = message.primaryResponseId;
                if (message.surveyCtaHasRendered != null && Object.hasOwnProperty.call(message, "surveyCtaHasRendered"))
                    object.surveyCtaHasRendered = message.surveyCtaHasRendered;
                return object;
            };

            /**
             * Converts this SideBySideMetadata to JSON.
             * @function toJSON
             * @memberof proto.BotUnifiedResponseMutation.SideBySideMetadata
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SideBySideMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for SideBySideMetadata
             * @function getTypeUrl
             * @memberof proto.BotUnifiedResponseMutation.SideBySideMetadata
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            SideBySideMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.BotUnifiedResponseMutation.SideBySideMetadata";
            };

            return SideBySideMetadata;
        })();

        return BotUnifiedResponseMutation;
    })();

    proto.CallLogRecord = (function() {

        /**
         * Properties of a CallLogRecord.
         * @memberof proto
         * @interface ICallLogRecord
         * @property {proto.CallLogRecord.CallResult|null} [callResult] CallLogRecord callResult
         * @property {boolean|null} [isDndMode] CallLogRecord isDndMode
         * @property {proto.CallLogRecord.SilenceReason|null} [silenceReason] CallLogRecord silenceReason
         * @property {number|Long|null} [duration] CallLogRecord duration
         * @property {number|Long|null} [startTime] CallLogRecord startTime
         * @property {boolean|null} [isIncoming] CallLogRecord isIncoming
         * @property {boolean|null} [isVideo] CallLogRecord isVideo
         * @property {boolean|null} [isCallLink] CallLogRecord isCallLink
         * @property {string|null} [callLinkToken] CallLogRecord callLinkToken
         * @property {string|null} [scheduledCallId] CallLogRecord scheduledCallId
         * @property {string|null} [callId] CallLogRecord callId
         * @property {string|null} [callCreatorJid] CallLogRecord callCreatorJid
         * @property {string|null} [groupJid] CallLogRecord groupJid
         * @property {Array.<proto.CallLogRecord.IParticipantInfo>|null} [participants] CallLogRecord participants
         * @property {proto.CallLogRecord.CallType|null} [callType] CallLogRecord callType
         */

        /**
         * Constructs a new CallLogRecord.
         * @memberof proto
         * @classdesc Represents a CallLogRecord.
         * @implements ICallLogRecord
         * @constructor
         * @param {proto.ICallLogRecord=} [properties] Properties to set
         */
        function CallLogRecord(properties) {
            this.participants = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CallLogRecord callResult.
         * @member {proto.CallLogRecord.CallResult|null|undefined} callResult
         * @memberof proto.CallLogRecord
         * @instance
         */
        CallLogRecord.prototype.callResult = null;

        /**
         * CallLogRecord isDndMode.
         * @member {boolean|null|undefined} isDndMode
         * @memberof proto.CallLogRecord
         * @instance
         */
        CallLogRecord.prototype.isDndMode = null;

        /**
         * CallLogRecord silenceReason.
         * @member {proto.CallLogRecord.SilenceReason|null|undefined} silenceReason
         * @memberof proto.CallLogRecord
         * @instance
         */
        CallLogRecord.prototype.silenceReason = null;

        /**
         * CallLogRecord duration.
         * @member {number|Long|null|undefined} duration
         * @memberof proto.CallLogRecord
         * @instance
         */
        CallLogRecord.prototype.duration = null;

        /**
         * CallLogRecord startTime.
         * @member {number|Long|null|undefined} startTime
         * @memberof proto.CallLogRecord
         * @instance
         */
        CallLogRecord.prototype.startTime = null;

        /**
         * CallLogRecord isIncoming.
         * @member {boolean|null|undefined} isIncoming
         * @memberof proto.CallLogRecord
         * @instance
         */
        CallLogRecord.prototype.isIncoming = null;

        /**
         * CallLogRecord isVideo.
         * @member {boolean|null|undefined} isVideo
         * @memberof proto.CallLogRecord
         * @instance
         */
        CallLogRecord.prototype.isVideo = null;

        /**
         * CallLogRecord isCallLink.
         * @member {boolean|null|undefined} isCallLink
         * @memberof proto.CallLogRecord
         * @instance
         */
        CallLogRecord.prototype.isCallLink = null;

        /**
         * CallLogRecord callLinkToken.
         * @member {string|null|undefined} callLinkToken
         * @memberof proto.CallLogRecord
         * @instance
         */
        CallLogRecord.prototype.callLinkToken = null;

        /**
         * CallLogRecord scheduledCallId.
         * @member {string|null|undefined} scheduledCallId
         * @memberof proto.CallLogRecord
         * @instance
         */
        CallLogRecord.prototype.scheduledCallId = null;

        /**
         * CallLogRecord callId.
         * @member {string|null|undefined} callId
         * @memberof proto.CallLogRecord
         * @instance
         */
        CallLogRecord.prototype.callId = null;

        /**
         * CallLogRecord callCreatorJid.
         * @member {string|null|undefined} callCreatorJid
         * @memberof proto.CallLogRecord
         * @instance
         */
        CallLogRecord.prototype.callCreatorJid = null;

        /**
         * CallLogRecord groupJid.
         * @member {string|null|undefined} groupJid
         * @memberof proto.CallLogRecord
         * @instance
         */
        CallLogRecord.prototype.groupJid = null;

        /**
         * CallLogRecord participants.
         * @member {Array.<proto.CallLogRecord.IParticipantInfo>} participants
         * @memberof proto.CallLogRecord
         * @instance
         */
        CallLogRecord.prototype.participants = $util.emptyArray;

        /**
         * CallLogRecord callType.
         * @member {proto.CallLogRecord.CallType|null|undefined} callType
         * @memberof proto.CallLogRecord
         * @instance
         */
        CallLogRecord.prototype.callType = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CallLogRecord.prototype, "_callResult", {
            get: $util.oneOfGetter($oneOfFields = ["callResult"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CallLogRecord.prototype, "_isDndMode", {
            get: $util.oneOfGetter($oneOfFields = ["isDndMode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CallLogRecord.prototype, "_silenceReason", {
            get: $util.oneOfGetter($oneOfFields = ["silenceReason"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CallLogRecord.prototype, "_duration", {
            get: $util.oneOfGetter($oneOfFields = ["duration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CallLogRecord.prototype, "_startTime", {
            get: $util.oneOfGetter($oneOfFields = ["startTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CallLogRecord.prototype, "_isIncoming", {
            get: $util.oneOfGetter($oneOfFields = ["isIncoming"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CallLogRecord.prototype, "_isVideo", {
            get: $util.oneOfGetter($oneOfFields = ["isVideo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CallLogRecord.prototype, "_isCallLink", {
            get: $util.oneOfGetter($oneOfFields = ["isCallLink"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CallLogRecord.prototype, "_callLinkToken", {
            get: $util.oneOfGetter($oneOfFields = ["callLinkToken"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CallLogRecord.prototype, "_scheduledCallId", {
            get: $util.oneOfGetter($oneOfFields = ["scheduledCallId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CallLogRecord.prototype, "_callId", {
            get: $util.oneOfGetter($oneOfFields = ["callId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CallLogRecord.prototype, "_callCreatorJid", {
            get: $util.oneOfGetter($oneOfFields = ["callCreatorJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CallLogRecord.prototype, "_groupJid", {
            get: $util.oneOfGetter($oneOfFields = ["groupJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CallLogRecord.prototype, "_callType", {
            get: $util.oneOfGetter($oneOfFields = ["callType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new CallLogRecord instance using the specified properties.
         * @function create
         * @memberof proto.CallLogRecord
         * @static
         * @param {proto.ICallLogRecord=} [properties] Properties to set
         * @returns {proto.CallLogRecord} CallLogRecord instance
         */
        CallLogRecord.create = function create(properties) {
            return new CallLogRecord(properties);
        };

        /**
         * Encodes the specified CallLogRecord message. Does not implicitly {@link proto.CallLogRecord.verify|verify} messages.
         * @function encode
         * @memberof proto.CallLogRecord
         * @static
         * @param {proto.ICallLogRecord} message CallLogRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CallLogRecord.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.callResult != null && Object.hasOwnProperty.call(message, "callResult"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.callResult);
            if (message.isDndMode != null && Object.hasOwnProperty.call(message, "isDndMode"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.isDndMode);
            if (message.silenceReason != null && Object.hasOwnProperty.call(message, "silenceReason"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.silenceReason);
            if (message.duration != null && Object.hasOwnProperty.call(message, "duration"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.duration);
            if (message.startTime != null && Object.hasOwnProperty.call(message, "startTime"))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.startTime);
            if (message.isIncoming != null && Object.hasOwnProperty.call(message, "isIncoming"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.isIncoming);
            if (message.isVideo != null && Object.hasOwnProperty.call(message, "isVideo"))
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.isVideo);
            if (message.isCallLink != null && Object.hasOwnProperty.call(message, "isCallLink"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.isCallLink);
            if (message.callLinkToken != null && Object.hasOwnProperty.call(message, "callLinkToken"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.callLinkToken);
            if (message.scheduledCallId != null && Object.hasOwnProperty.call(message, "scheduledCallId"))
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.scheduledCallId);
            if (message.callId != null && Object.hasOwnProperty.call(message, "callId"))
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.callId);
            if (message.callCreatorJid != null && Object.hasOwnProperty.call(message, "callCreatorJid"))
                writer.uint32(/* id 12, wireType 2 =*/98).string(message.callCreatorJid);
            if (message.groupJid != null && Object.hasOwnProperty.call(message, "groupJid"))
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.groupJid);
            if (message.participants != null && message.participants.length)
                for (var i = 0; i < message.participants.length; ++i)
                    $root.proto.CallLogRecord.ParticipantInfo.encode(message.participants[i], writer.uint32(/* id 14, wireType 2 =*/114).fork(), q + 1).ldelim();
            if (message.callType != null && Object.hasOwnProperty.call(message, "callType"))
                writer.uint32(/* id 15, wireType 0 =*/120).int32(message.callType);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CallLogRecord message, length delimited. Does not implicitly {@link proto.CallLogRecord.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CallLogRecord
         * @static
         * @param {proto.ICallLogRecord} message CallLogRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CallLogRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CallLogRecord message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CallLogRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CallLogRecord} CallLogRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CallLogRecord.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.callResult = reader.int32();
                        message._callResult = "callResult";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.isDndMode = reader.bool();
                        message._isDndMode = "isDndMode";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.silenceReason = reader.int32();
                        message._silenceReason = "silenceReason";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.duration = reader.int64();
                        message._duration = "duration";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.startTime = reader.int64();
                        message._startTime = "startTime";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        message.isIncoming = reader.bool();
                        message._isIncoming = "isIncoming";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        message.isVideo = reader.bool();
                        message._isVideo = "isVideo";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        message.isCallLink = reader.bool();
                        message._isCallLink = "isCallLink";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        message.callLinkToken = reader.stringVerify();
                        message._callLinkToken = "callLinkToken";
                        continue;
                    }
                case 10: {
                        if (u !== 2)
                            break;
                        message.scheduledCallId = reader.stringVerify();
                        message._scheduledCallId = "scheduledCallId";
                        continue;
                    }
                case 11: {
                        if (u !== 2)
                            break;
                        message.callId = reader.stringVerify();
                        message._callId = "callId";
                        continue;
                    }
                case 12: {
                        if (u !== 2)
                            break;
                        message.callCreatorJid = reader.stringVerify();
                        message._callCreatorJid = "callCreatorJid";
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        message.groupJid = reader.stringVerify();
                        message._groupJid = "groupJid";
                        continue;
                    }
                case 14: {
                        if (u !== 2)
                            break;
                        if (!(message.participants && message.participants.length))
                            message.participants = [];
                        message.participants.push($root.proto.CallLogRecord.ParticipantInfo.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 15: {
                        if (u !== 0)
                            break;
                        message.callType = reader.int32();
                        message._callType = "callType";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a CallLogRecord message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CallLogRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CallLogRecord} CallLogRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CallLogRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CallLogRecord message.
         * @function verify
         * @memberof proto.CallLogRecord
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CallLogRecord.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.callResult != null && Object.hasOwnProperty.call(message, "callResult")) {
                properties._callResult = 1;
                if (typeof message.callResult !== "number" || (message.callResult | 0) !== message.callResult)
                    return "callResult: enum value expected";
            }
            if (message.isDndMode != null && Object.hasOwnProperty.call(message, "isDndMode")) {
                properties._isDndMode = 1;
                if (typeof message.isDndMode !== "boolean")
                    return "isDndMode: boolean expected";
            }
            if (message.silenceReason != null && Object.hasOwnProperty.call(message, "silenceReason")) {
                properties._silenceReason = 1;
                if (typeof message.silenceReason !== "number" || (message.silenceReason | 0) !== message.silenceReason)
                    return "silenceReason: enum value expected";
            }
            if (message.duration != null && Object.hasOwnProperty.call(message, "duration")) {
                properties._duration = 1;
                if (!$util.isInteger(message.duration) && !(message.duration && $util.isInteger(message.duration.low) && $util.isInteger(message.duration.high)))
                    return "duration: integer|Long expected";
            }
            if (message.startTime != null && Object.hasOwnProperty.call(message, "startTime")) {
                properties._startTime = 1;
                if (!$util.isInteger(message.startTime) && !(message.startTime && $util.isInteger(message.startTime.low) && $util.isInteger(message.startTime.high)))
                    return "startTime: integer|Long expected";
            }
            if (message.isIncoming != null && Object.hasOwnProperty.call(message, "isIncoming")) {
                properties._isIncoming = 1;
                if (typeof message.isIncoming !== "boolean")
                    return "isIncoming: boolean expected";
            }
            if (message.isVideo != null && Object.hasOwnProperty.call(message, "isVideo")) {
                properties._isVideo = 1;
                if (typeof message.isVideo !== "boolean")
                    return "isVideo: boolean expected";
            }
            if (message.isCallLink != null && Object.hasOwnProperty.call(message, "isCallLink")) {
                properties._isCallLink = 1;
                if (typeof message.isCallLink !== "boolean")
                    return "isCallLink: boolean expected";
            }
            if (message.callLinkToken != null && Object.hasOwnProperty.call(message, "callLinkToken")) {
                properties._callLinkToken = 1;
                if (!$util.isString(message.callLinkToken))
                    return "callLinkToken: string expected";
            }
            if (message.scheduledCallId != null && Object.hasOwnProperty.call(message, "scheduledCallId")) {
                properties._scheduledCallId = 1;
                if (!$util.isString(message.scheduledCallId))
                    return "scheduledCallId: string expected";
            }
            if (message.callId != null && Object.hasOwnProperty.call(message, "callId")) {
                properties._callId = 1;
                if (!$util.isString(message.callId))
                    return "callId: string expected";
            }
            if (message.callCreatorJid != null && Object.hasOwnProperty.call(message, "callCreatorJid")) {
                properties._callCreatorJid = 1;
                if (!$util.isString(message.callCreatorJid))
                    return "callCreatorJid: string expected";
            }
            if (message.groupJid != null && Object.hasOwnProperty.call(message, "groupJid")) {
                properties._groupJid = 1;
                if (!$util.isString(message.groupJid))
                    return "groupJid: string expected";
            }
            if (message.participants != null && Object.hasOwnProperty.call(message, "participants")) {
                if (!Array.isArray(message.participants))
                    return "participants: array expected";
                for (var i = 0; i < message.participants.length; ++i) {
                    var error = $root.proto.CallLogRecord.ParticipantInfo.verify(message.participants[i], q + 1);
                    if (error)
                        return "participants." + error;
                }
            }
            if (message.callType != null && Object.hasOwnProperty.call(message, "callType")) {
                properties._callType = 1;
                if (typeof message.callType !== "number" || (message.callType | 0) !== message.callType)
                    return "callType: enum value expected";
            }
            return null;
        };

        /**
         * Creates a CallLogRecord message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CallLogRecord
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CallLogRecord} CallLogRecord
         */
        CallLogRecord.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.CallLogRecord: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.callResult) {
            case "CONNECTED":
            case 0:
                message.callResult = 0;
                break;
            case "REJECTED":
            case 1:
                message.callResult = 1;
                break;
            case "CANCELLED":
            case 2:
                message.callResult = 2;
                break;
            case "ACCEPTEDELSEWHERE":
            case 3:
                message.callResult = 3;
                break;
            case "MISSED":
            case 4:
                message.callResult = 4;
                break;
            case "INVALID":
            case 5:
                message.callResult = 5;
                break;
            case "UNAVAILABLE":
            case 6:
                message.callResult = 6;
                break;
            case "UPCOMING":
            case 7:
                message.callResult = 7;
                break;
            case "FAILED":
            case 8:
                message.callResult = 8;
                break;
            case "ABANDONED":
            case 9:
                message.callResult = 9;
                break;
            case "ONGOING":
            case 10:
                message.callResult = 10;
                break;
            default:
                if (typeof object.callResult === "number" && (object.callResult | 0) === object.callResult)
                    message.callResult = object.callResult;
            }
            if (object.isDndMode != null)
                message.isDndMode = Boolean(object.isDndMode);
            switch (object.silenceReason) {
            case "NONE":
            case 0:
                message.silenceReason = 0;
                break;
            case "SCHEDULED":
            case 1:
                message.silenceReason = 1;
                break;
            case "PRIVACY":
            case 2:
                message.silenceReason = 2;
                break;
            case "LIGHTWEIGHT":
            case 3:
                message.silenceReason = 3;
                break;
            default:
                if (typeof object.silenceReason === "number" && (object.silenceReason | 0) === object.silenceReason)
                    message.silenceReason = object.silenceReason;
            }
            if (object.duration != null)
                if ($util.Long)
                    message.duration = $util.Long.fromValue(object.duration, false);
                else if (typeof object.duration === "string")
                    message.duration = parseInt(object.duration, 10);
                else if (typeof object.duration === "number")
                    message.duration = object.duration;
                else if (typeof object.duration === "object")
                    message.duration = new $util.LongBits(object.duration.low >>> 0, object.duration.high >>> 0).toNumber();
            if (object.startTime != null)
                if ($util.Long)
                    message.startTime = $util.Long.fromValue(object.startTime, false);
                else if (typeof object.startTime === "string")
                    message.startTime = parseInt(object.startTime, 10);
                else if (typeof object.startTime === "number")
                    message.startTime = object.startTime;
                else if (typeof object.startTime === "object")
                    message.startTime = new $util.LongBits(object.startTime.low >>> 0, object.startTime.high >>> 0).toNumber();
            if (object.isIncoming != null)
                message.isIncoming = Boolean(object.isIncoming);
            if (object.isVideo != null)
                message.isVideo = Boolean(object.isVideo);
            if (object.isCallLink != null)
                message.isCallLink = Boolean(object.isCallLink);
            if (object.callLinkToken != null)
                message.callLinkToken = String(object.callLinkToken);
            if (object.scheduledCallId != null)
                message.scheduledCallId = String(object.scheduledCallId);
            if (object.callId != null)
                message.callId = String(object.callId);
            if (object.callCreatorJid != null)
                message.callCreatorJid = String(object.callCreatorJid);
            if (object.groupJid != null)
                message.groupJid = String(object.groupJid);
            if (object.participants) {
                if (!Array.isArray(object.participants))
                    throw TypeError(".proto.CallLogRecord.participants: array expected");
                message.participants = Array(object.participants.length);
                for (var i = 0; i < object.participants.length; ++i) {
                    if (!$util.isObject(object.participants[i]))
                        throw TypeError(".proto.CallLogRecord.participants: object expected");
                    message.participants[i] = $root.proto.CallLogRecord.ParticipantInfo.fromObject(object.participants[i], q + 1);
                }
            }
            switch (object.callType) {
            case "REGULAR":
            case 0:
                message.callType = 0;
                break;
            case "SCHEDULED_CALL":
            case 1:
                message.callType = 1;
                break;
            case "VOICE_CHAT":
            case 2:
                message.callType = 2;
                break;
            default:
                if (typeof object.callType === "number" && (object.callType | 0) === object.callType)
                    message.callType = object.callType;
            }
            return message;
        };

        /**
         * Creates a plain object from a CallLogRecord message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CallLogRecord
         * @static
         * @param {proto.CallLogRecord} message CallLogRecord
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CallLogRecord.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.participants = [];
            if (message.callResult != null && Object.hasOwnProperty.call(message, "callResult"))
                object.callResult = options.enums === String ? $root.proto.CallLogRecord.CallResult[message.callResult] === undefined ? message.callResult : $root.proto.CallLogRecord.CallResult[message.callResult] : message.callResult;
            if (message.isDndMode != null && Object.hasOwnProperty.call(message, "isDndMode"))
                object.isDndMode = message.isDndMode;
            if (message.silenceReason != null && Object.hasOwnProperty.call(message, "silenceReason"))
                object.silenceReason = options.enums === String ? $root.proto.CallLogRecord.SilenceReason[message.silenceReason] === undefined ? message.silenceReason : $root.proto.CallLogRecord.SilenceReason[message.silenceReason] : message.silenceReason;
            if (message.duration != null && Object.hasOwnProperty.call(message, "duration"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.duration = typeof message.duration === "number" ? BigInt(message.duration) : $util.Long.fromBits(message.duration.low >>> 0, message.duration.high >>> 0, false).toBigInt();
                else if (typeof message.duration === "number")
                    object.duration = options.longs === String ? String(message.duration) : message.duration;
                else
                    object.duration = options.longs === String ? $util.Long.prototype.toString.call(message.duration) : options.longs === Number ? new $util.LongBits(message.duration.low >>> 0, message.duration.high >>> 0).toNumber() : message.duration;
            if (message.startTime != null && Object.hasOwnProperty.call(message, "startTime"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.startTime = typeof message.startTime === "number" ? BigInt(message.startTime) : $util.Long.fromBits(message.startTime.low >>> 0, message.startTime.high >>> 0, false).toBigInt();
                else if (typeof message.startTime === "number")
                    object.startTime = options.longs === String ? String(message.startTime) : message.startTime;
                else
                    object.startTime = options.longs === String ? $util.Long.prototype.toString.call(message.startTime) : options.longs === Number ? new $util.LongBits(message.startTime.low >>> 0, message.startTime.high >>> 0).toNumber() : message.startTime;
            if (message.isIncoming != null && Object.hasOwnProperty.call(message, "isIncoming"))
                object.isIncoming = message.isIncoming;
            if (message.isVideo != null && Object.hasOwnProperty.call(message, "isVideo"))
                object.isVideo = message.isVideo;
            if (message.isCallLink != null && Object.hasOwnProperty.call(message, "isCallLink"))
                object.isCallLink = message.isCallLink;
            if (message.callLinkToken != null && Object.hasOwnProperty.call(message, "callLinkToken"))
                object.callLinkToken = message.callLinkToken;
            if (message.scheduledCallId != null && Object.hasOwnProperty.call(message, "scheduledCallId"))
                object.scheduledCallId = message.scheduledCallId;
            if (message.callId != null && Object.hasOwnProperty.call(message, "callId"))
                object.callId = message.callId;
            if (message.callCreatorJid != null && Object.hasOwnProperty.call(message, "callCreatorJid"))
                object.callCreatorJid = message.callCreatorJid;
            if (message.groupJid != null && Object.hasOwnProperty.call(message, "groupJid"))
                object.groupJid = message.groupJid;
            if (message.participants && message.participants.length) {
                object.participants = Array(message.participants.length);
                for (var j = 0; j < message.participants.length; ++j)
                    object.participants[j] = $root.proto.CallLogRecord.ParticipantInfo.toObject(message.participants[j], options, q + 1);
            }
            if (message.callType != null && Object.hasOwnProperty.call(message, "callType"))
                object.callType = options.enums === String ? $root.proto.CallLogRecord.CallType[message.callType] === undefined ? message.callType : $root.proto.CallLogRecord.CallType[message.callType] : message.callType;
            return object;
        };

        /**
         * Converts this CallLogRecord to JSON.
         * @function toJSON
         * @memberof proto.CallLogRecord
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CallLogRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CallLogRecord
         * @function getTypeUrl
         * @memberof proto.CallLogRecord
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CallLogRecord.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.CallLogRecord";
        };

        /**
         * CallResult enum.
         * @name proto.CallLogRecord.CallResult
         * @enum {number}
         * @property {number} CONNECTED=0 CONNECTED value
         * @property {number} REJECTED=1 REJECTED value
         * @property {number} CANCELLED=2 CANCELLED value
         * @property {number} ACCEPTEDELSEWHERE=3 ACCEPTEDELSEWHERE value
         * @property {number} MISSED=4 MISSED value
         * @property {number} INVALID=5 INVALID value
         * @property {number} UNAVAILABLE=6 UNAVAILABLE value
         * @property {number} UPCOMING=7 UPCOMING value
         * @property {number} FAILED=8 FAILED value
         * @property {number} ABANDONED=9 ABANDONED value
         * @property {number} ONGOING=10 ONGOING value
         */
        CallLogRecord.CallResult = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "CONNECTED"] = 0;
            values[valuesById[1] = "REJECTED"] = 1;
            values[valuesById[2] = "CANCELLED"] = 2;
            values[valuesById[3] = "ACCEPTEDELSEWHERE"] = 3;
            values[valuesById[4] = "MISSED"] = 4;
            values[valuesById[5] = "INVALID"] = 5;
            values[valuesById[6] = "UNAVAILABLE"] = 6;
            values[valuesById[7] = "UPCOMING"] = 7;
            values[valuesById[8] = "FAILED"] = 8;
            values[valuesById[9] = "ABANDONED"] = 9;
            values[valuesById[10] = "ONGOING"] = 10;
            return values;
        })();

        /**
         * CallType enum.
         * @name proto.CallLogRecord.CallType
         * @enum {number}
         * @property {number} REGULAR=0 REGULAR value
         * @property {number} SCHEDULED_CALL=1 SCHEDULED_CALL value
         * @property {number} VOICE_CHAT=2 VOICE_CHAT value
         */
        CallLogRecord.CallType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "REGULAR"] = 0;
            values[valuesById[1] = "SCHEDULED_CALL"] = 1;
            values[valuesById[2] = "VOICE_CHAT"] = 2;
            return values;
        })();

        CallLogRecord.ParticipantInfo = (function() {

            /**
             * Properties of a ParticipantInfo.
             * @memberof proto.CallLogRecord
             * @interface IParticipantInfo
             * @property {string|null} [userJid] ParticipantInfo userJid
             * @property {proto.CallLogRecord.CallResult|null} [callResult] ParticipantInfo callResult
             */

            /**
             * Constructs a new ParticipantInfo.
             * @memberof proto.CallLogRecord
             * @classdesc Represents a ParticipantInfo.
             * @implements IParticipantInfo
             * @constructor
             * @param {proto.CallLogRecord.IParticipantInfo=} [properties] Properties to set
             */
            function ParticipantInfo(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ParticipantInfo userJid.
             * @member {string|null|undefined} userJid
             * @memberof proto.CallLogRecord.ParticipantInfo
             * @instance
             */
            ParticipantInfo.prototype.userJid = null;

            /**
             * ParticipantInfo callResult.
             * @member {proto.CallLogRecord.CallResult|null|undefined} callResult
             * @memberof proto.CallLogRecord.ParticipantInfo
             * @instance
             */
            ParticipantInfo.prototype.callResult = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ParticipantInfo.prototype, "_userJid", {
                get: $util.oneOfGetter($oneOfFields = ["userJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ParticipantInfo.prototype, "_callResult", {
                get: $util.oneOfGetter($oneOfFields = ["callResult"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ParticipantInfo instance using the specified properties.
             * @function create
             * @memberof proto.CallLogRecord.ParticipantInfo
             * @static
             * @param {proto.CallLogRecord.IParticipantInfo=} [properties] Properties to set
             * @returns {proto.CallLogRecord.ParticipantInfo} ParticipantInfo instance
             */
            ParticipantInfo.create = function create(properties) {
                return new ParticipantInfo(properties);
            };

            /**
             * Encodes the specified ParticipantInfo message. Does not implicitly {@link proto.CallLogRecord.ParticipantInfo.verify|verify} messages.
             * @function encode
             * @memberof proto.CallLogRecord.ParticipantInfo
             * @static
             * @param {proto.CallLogRecord.IParticipantInfo} message ParticipantInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ParticipantInfo.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.userJid != null && Object.hasOwnProperty.call(message, "userJid"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.userJid);
                if (message.callResult != null && Object.hasOwnProperty.call(message, "callResult"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.callResult);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ParticipantInfo message, length delimited. Does not implicitly {@link proto.CallLogRecord.ParticipantInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.CallLogRecord.ParticipantInfo
             * @static
             * @param {proto.CallLogRecord.IParticipantInfo} message ParticipantInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ParticipantInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ParticipantInfo message from the specified reader or buffer.
             * @function decode
             * @memberof proto.CallLogRecord.ParticipantInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.CallLogRecord.ParticipantInfo} ParticipantInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ParticipantInfo.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.userJid = reader.stringVerify();
                            message._userJid = "userJid";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.callResult = reader.int32();
                            message._callResult = "callResult";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ParticipantInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.CallLogRecord.ParticipantInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.CallLogRecord.ParticipantInfo} ParticipantInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ParticipantInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ParticipantInfo message.
             * @function verify
             * @memberof proto.CallLogRecord.ParticipantInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ParticipantInfo.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.userJid != null && Object.hasOwnProperty.call(message, "userJid")) {
                    properties._userJid = 1;
                    if (!$util.isString(message.userJid))
                        return "userJid: string expected";
                }
                if (message.callResult != null && Object.hasOwnProperty.call(message, "callResult")) {
                    properties._callResult = 1;
                    if (typeof message.callResult !== "number" || (message.callResult | 0) !== message.callResult)
                        return "callResult: enum value expected";
                }
                return null;
            };

            /**
             * Creates a ParticipantInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.CallLogRecord.ParticipantInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.CallLogRecord.ParticipantInfo} ParticipantInfo
             */
            ParticipantInfo.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.CallLogRecord.ParticipantInfo: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.userJid != null)
                    message.userJid = String(object.userJid);
                switch (object.callResult) {
                case "CONNECTED":
                case 0:
                    message.callResult = 0;
                    break;
                case "REJECTED":
                case 1:
                    message.callResult = 1;
                    break;
                case "CANCELLED":
                case 2:
                    message.callResult = 2;
                    break;
                case "ACCEPTEDELSEWHERE":
                case 3:
                    message.callResult = 3;
                    break;
                case "MISSED":
                case 4:
                    message.callResult = 4;
                    break;
                case "INVALID":
                case 5:
                    message.callResult = 5;
                    break;
                case "UNAVAILABLE":
                case 6:
                    message.callResult = 6;
                    break;
                case "UPCOMING":
                case 7:
                    message.callResult = 7;
                    break;
                case "FAILED":
                case 8:
                    message.callResult = 8;
                    break;
                case "ABANDONED":
                case 9:
                    message.callResult = 9;
                    break;
                case "ONGOING":
                case 10:
                    message.callResult = 10;
                    break;
                default:
                    if (typeof object.callResult === "number" && (object.callResult | 0) === object.callResult)
                        message.callResult = object.callResult;
                }
                return message;
            };

            /**
             * Creates a plain object from a ParticipantInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.CallLogRecord.ParticipantInfo
             * @static
             * @param {proto.CallLogRecord.ParticipantInfo} message ParticipantInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ParticipantInfo.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.userJid != null && Object.hasOwnProperty.call(message, "userJid"))
                    object.userJid = message.userJid;
                if (message.callResult != null && Object.hasOwnProperty.call(message, "callResult"))
                    object.callResult = options.enums === String ? $root.proto.CallLogRecord.CallResult[message.callResult] === undefined ? message.callResult : $root.proto.CallLogRecord.CallResult[message.callResult] : message.callResult;
                return object;
            };

            /**
             * Converts this ParticipantInfo to JSON.
             * @function toJSON
             * @memberof proto.CallLogRecord.ParticipantInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ParticipantInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ParticipantInfo
             * @function getTypeUrl
             * @memberof proto.CallLogRecord.ParticipantInfo
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ParticipantInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.CallLogRecord.ParticipantInfo";
            };

            return ParticipantInfo;
        })();

        /**
         * SilenceReason enum.
         * @name proto.CallLogRecord.SilenceReason
         * @enum {number}
         * @property {number} NONE=0 NONE value
         * @property {number} SCHEDULED=1 SCHEDULED value
         * @property {number} PRIVACY=2 PRIVACY value
         * @property {number} LIGHTWEIGHT=3 LIGHTWEIGHT value
         */
        CallLogRecord.SilenceReason = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "NONE"] = 0;
            values[valuesById[1] = "SCHEDULED"] = 1;
            values[valuesById[2] = "PRIVACY"] = 2;
            values[valuesById[3] = "LIGHTWEIGHT"] = 3;
            return values;
        })();

        return CallLogRecord;
    })();

    proto.CertChain = (function() {

        /**
         * Properties of a CertChain.
         * @memberof proto
         * @interface ICertChain
         * @property {proto.CertChain.INoiseCertificate|null} [leaf] CertChain leaf
         * @property {proto.CertChain.INoiseCertificate|null} [intermediate] CertChain intermediate
         */

        /**
         * Constructs a new CertChain.
         * @memberof proto
         * @classdesc Represents a CertChain.
         * @implements ICertChain
         * @constructor
         * @param {proto.ICertChain=} [properties] Properties to set
         */
        function CertChain(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CertChain leaf.
         * @member {proto.CertChain.INoiseCertificate|null|undefined} leaf
         * @memberof proto.CertChain
         * @instance
         */
        CertChain.prototype.leaf = null;

        /**
         * CertChain intermediate.
         * @member {proto.CertChain.INoiseCertificate|null|undefined} intermediate
         * @memberof proto.CertChain
         * @instance
         */
        CertChain.prototype.intermediate = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CertChain.prototype, "_leaf", {
            get: $util.oneOfGetter($oneOfFields = ["leaf"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CertChain.prototype, "_intermediate", {
            get: $util.oneOfGetter($oneOfFields = ["intermediate"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new CertChain instance using the specified properties.
         * @function create
         * @memberof proto.CertChain
         * @static
         * @param {proto.ICertChain=} [properties] Properties to set
         * @returns {proto.CertChain} CertChain instance
         */
        CertChain.create = function create(properties) {
            return new CertChain(properties);
        };

        /**
         * Encodes the specified CertChain message. Does not implicitly {@link proto.CertChain.verify|verify} messages.
         * @function encode
         * @memberof proto.CertChain
         * @static
         * @param {proto.ICertChain} message CertChain message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CertChain.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.leaf != null && Object.hasOwnProperty.call(message, "leaf"))
                $root.proto.CertChain.NoiseCertificate.encode(message.leaf, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.intermediate != null && Object.hasOwnProperty.call(message, "intermediate"))
                $root.proto.CertChain.NoiseCertificate.encode(message.intermediate, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CertChain message, length delimited. Does not implicitly {@link proto.CertChain.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CertChain
         * @static
         * @param {proto.ICertChain} message CertChain message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CertChain.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CertChain message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CertChain
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CertChain} CertChain
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CertChain.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.leaf = $root.proto.CertChain.NoiseCertificate.decode(reader, reader.uint32(), undefined, q + 1, message.leaf);
                        message._leaf = "leaf";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.intermediate = $root.proto.CertChain.NoiseCertificate.decode(reader, reader.uint32(), undefined, q + 1, message.intermediate);
                        message._intermediate = "intermediate";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a CertChain message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CertChain
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CertChain} CertChain
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CertChain.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CertChain message.
         * @function verify
         * @memberof proto.CertChain
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CertChain.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.leaf != null && Object.hasOwnProperty.call(message, "leaf")) {
                properties._leaf = 1;
                {
                    var error = $root.proto.CertChain.NoiseCertificate.verify(message.leaf, q + 1);
                    if (error)
                        return "leaf." + error;
                }
            }
            if (message.intermediate != null && Object.hasOwnProperty.call(message, "intermediate")) {
                properties._intermediate = 1;
                {
                    var error = $root.proto.CertChain.NoiseCertificate.verify(message.intermediate, q + 1);
                    if (error)
                        return "intermediate." + error;
                }
            }
            return null;
        };

        /**
         * Creates a CertChain message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CertChain
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CertChain} CertChain
         */
        CertChain.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.CertChain: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.leaf != null) {
                if (!$util.isObject(object.leaf))
                    throw TypeError(".proto.CertChain.leaf: object expected");
                message.leaf = $root.proto.CertChain.NoiseCertificate.fromObject(object.leaf, q + 1);
            }
            if (object.intermediate != null) {
                if (!$util.isObject(object.intermediate))
                    throw TypeError(".proto.CertChain.intermediate: object expected");
                message.intermediate = $root.proto.CertChain.NoiseCertificate.fromObject(object.intermediate, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a CertChain message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CertChain
         * @static
         * @param {proto.CertChain} message CertChain
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CertChain.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.leaf != null && Object.hasOwnProperty.call(message, "leaf"))
                object.leaf = $root.proto.CertChain.NoiseCertificate.toObject(message.leaf, options, q + 1);
            if (message.intermediate != null && Object.hasOwnProperty.call(message, "intermediate"))
                object.intermediate = $root.proto.CertChain.NoiseCertificate.toObject(message.intermediate, options, q + 1);
            return object;
        };

        /**
         * Converts this CertChain to JSON.
         * @function toJSON
         * @memberof proto.CertChain
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CertChain.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CertChain
         * @function getTypeUrl
         * @memberof proto.CertChain
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CertChain.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.CertChain";
        };

        CertChain.NoiseCertificate = (function() {

            /**
             * Properties of a NoiseCertificate.
             * @memberof proto.CertChain
             * @interface INoiseCertificate
             * @property {Uint8Array|null} [details] NoiseCertificate details
             * @property {Uint8Array|null} [signature] NoiseCertificate signature
             */

            /**
             * Constructs a new NoiseCertificate.
             * @memberof proto.CertChain
             * @classdesc Represents a NoiseCertificate.
             * @implements INoiseCertificate
             * @constructor
             * @param {proto.CertChain.INoiseCertificate=} [properties] Properties to set
             */
            function NoiseCertificate(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * NoiseCertificate details.
             * @member {Uint8Array|null|undefined} details
             * @memberof proto.CertChain.NoiseCertificate
             * @instance
             */
            NoiseCertificate.prototype.details = null;

            /**
             * NoiseCertificate signature.
             * @member {Uint8Array|null|undefined} signature
             * @memberof proto.CertChain.NoiseCertificate
             * @instance
             */
            NoiseCertificate.prototype.signature = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NoiseCertificate.prototype, "_details", {
                get: $util.oneOfGetter($oneOfFields = ["details"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NoiseCertificate.prototype, "_signature", {
                get: $util.oneOfGetter($oneOfFields = ["signature"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new NoiseCertificate instance using the specified properties.
             * @function create
             * @memberof proto.CertChain.NoiseCertificate
             * @static
             * @param {proto.CertChain.INoiseCertificate=} [properties] Properties to set
             * @returns {proto.CertChain.NoiseCertificate} NoiseCertificate instance
             */
            NoiseCertificate.create = function create(properties) {
                return new NoiseCertificate(properties);
            };

            /**
             * Encodes the specified NoiseCertificate message. Does not implicitly {@link proto.CertChain.NoiseCertificate.verify|verify} messages.
             * @function encode
             * @memberof proto.CertChain.NoiseCertificate
             * @static
             * @param {proto.CertChain.INoiseCertificate} message NoiseCertificate message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            NoiseCertificate.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.details != null && Object.hasOwnProperty.call(message, "details"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.details);
                if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.signature);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified NoiseCertificate message, length delimited. Does not implicitly {@link proto.CertChain.NoiseCertificate.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.CertChain.NoiseCertificate
             * @static
             * @param {proto.CertChain.INoiseCertificate} message NoiseCertificate message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            NoiseCertificate.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a NoiseCertificate message from the specified reader or buffer.
             * @function decode
             * @memberof proto.CertChain.NoiseCertificate
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.CertChain.NoiseCertificate} NoiseCertificate
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            NoiseCertificate.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.details = reader.bytes();
                            message._details = "details";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.signature = reader.bytes();
                            message._signature = "signature";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a NoiseCertificate message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.CertChain.NoiseCertificate
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.CertChain.NoiseCertificate} NoiseCertificate
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            NoiseCertificate.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a NoiseCertificate message.
             * @function verify
             * @memberof proto.CertChain.NoiseCertificate
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            NoiseCertificate.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.details != null && Object.hasOwnProperty.call(message, "details")) {
                    properties._details = 1;
                    if (!(message.details && typeof message.details.length === "number" || $util.isString(message.details)))
                        return "details: buffer expected";
                }
                if (message.signature != null && Object.hasOwnProperty.call(message, "signature")) {
                    properties._signature = 1;
                    if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                        return "signature: buffer expected";
                }
                return null;
            };

            /**
             * Creates a NoiseCertificate message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.CertChain.NoiseCertificate
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.CertChain.NoiseCertificate} NoiseCertificate
             */
            NoiseCertificate.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.CertChain.NoiseCertificate: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.details != null)
                    if (typeof object.details === "string")
                        $util.base64.decode(object.details, message.details = $util.newBuffer($util.base64.length(object.details)), 0);
                    else if (object.details.length >= 0)
                        message.details = object.details;
                if (object.signature != null)
                    if (typeof object.signature === "string")
                        $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                    else if (object.signature.length >= 0)
                        message.signature = object.signature;
                return message;
            };

            /**
             * Creates a plain object from a NoiseCertificate message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.CertChain.NoiseCertificate
             * @static
             * @param {proto.CertChain.NoiseCertificate} message NoiseCertificate
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            NoiseCertificate.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.details != null && Object.hasOwnProperty.call(message, "details"))
                    object.details = options.bytes === String ? $util.base64.encode(message.details, 0, message.details.length) : options.bytes === Array ? Array.prototype.slice.call(message.details) : message.details;
                if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                    object.signature = options.bytes === String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === Array ? Array.prototype.slice.call(message.signature) : message.signature;
                return object;
            };

            /**
             * Converts this NoiseCertificate to JSON.
             * @function toJSON
             * @memberof proto.CertChain.NoiseCertificate
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            NoiseCertificate.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for NoiseCertificate
             * @function getTypeUrl
             * @memberof proto.CertChain.NoiseCertificate
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            NoiseCertificate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.CertChain.NoiseCertificate";
            };

            NoiseCertificate.Details = (function() {

                /**
                 * Properties of a Details.
                 * @memberof proto.CertChain.NoiseCertificate
                 * @interface IDetails
                 * @property {number|null} [serial] Details serial
                 * @property {number|null} [issuerSerial] Details issuerSerial
                 * @property {Uint8Array|null} [key] Details key
                 * @property {number|Long|null} [notBefore] Details notBefore
                 * @property {number|Long|null} [notAfter] Details notAfter
                 */

                /**
                 * Constructs a new Details.
                 * @memberof proto.CertChain.NoiseCertificate
                 * @classdesc Represents a Details.
                 * @implements IDetails
                 * @constructor
                 * @param {proto.CertChain.NoiseCertificate.IDetails=} [properties] Properties to set
                 */
                function Details(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * Details serial.
                 * @member {number|null|undefined} serial
                 * @memberof proto.CertChain.NoiseCertificate.Details
                 * @instance
                 */
                Details.prototype.serial = null;

                /**
                 * Details issuerSerial.
                 * @member {number|null|undefined} issuerSerial
                 * @memberof proto.CertChain.NoiseCertificate.Details
                 * @instance
                 */
                Details.prototype.issuerSerial = null;

                /**
                 * Details key.
                 * @member {Uint8Array|null|undefined} key
                 * @memberof proto.CertChain.NoiseCertificate.Details
                 * @instance
                 */
                Details.prototype.key = null;

                /**
                 * Details notBefore.
                 * @member {number|Long|null|undefined} notBefore
                 * @memberof proto.CertChain.NoiseCertificate.Details
                 * @instance
                 */
                Details.prototype.notBefore = null;

                /**
                 * Details notAfter.
                 * @member {number|Long|null|undefined} notAfter
                 * @memberof proto.CertChain.NoiseCertificate.Details
                 * @instance
                 */
                Details.prototype.notAfter = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Details.prototype, "_serial", {
                    get: $util.oneOfGetter($oneOfFields = ["serial"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Details.prototype, "_issuerSerial", {
                    get: $util.oneOfGetter($oneOfFields = ["issuerSerial"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Details.prototype, "_key", {
                    get: $util.oneOfGetter($oneOfFields = ["key"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Details.prototype, "_notBefore", {
                    get: $util.oneOfGetter($oneOfFields = ["notBefore"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Details.prototype, "_notAfter", {
                    get: $util.oneOfGetter($oneOfFields = ["notAfter"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new Details instance using the specified properties.
                 * @function create
                 * @memberof proto.CertChain.NoiseCertificate.Details
                 * @static
                 * @param {proto.CertChain.NoiseCertificate.IDetails=} [properties] Properties to set
                 * @returns {proto.CertChain.NoiseCertificate.Details} Details instance
                 */
                Details.create = function create(properties) {
                    return new Details(properties);
                };

                /**
                 * Encodes the specified Details message. Does not implicitly {@link proto.CertChain.NoiseCertificate.Details.verify|verify} messages.
                 * @function encode
                 * @memberof proto.CertChain.NoiseCertificate.Details
                 * @static
                 * @param {proto.CertChain.NoiseCertificate.IDetails} message Details message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Details.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.serial != null && Object.hasOwnProperty.call(message, "serial"))
                        writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.serial);
                    if (message.issuerSerial != null && Object.hasOwnProperty.call(message, "issuerSerial"))
                        writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.issuerSerial);
                    if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                        writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.key);
                    if (message.notBefore != null && Object.hasOwnProperty.call(message, "notBefore"))
                        writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.notBefore);
                    if (message.notAfter != null && Object.hasOwnProperty.call(message, "notAfter"))
                        writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.notAfter);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified Details message, length delimited. Does not implicitly {@link proto.CertChain.NoiseCertificate.Details.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.CertChain.NoiseCertificate.Details
                 * @static
                 * @param {proto.CertChain.NoiseCertificate.IDetails} message Details message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Details.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a Details message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.CertChain.NoiseCertificate.Details
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.CertChain.NoiseCertificate.Details} Details
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Details.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                message.serial = reader.uint32();
                                message._serial = "serial";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                message.issuerSerial = reader.uint32();
                                message._issuerSerial = "issuerSerial";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.key = reader.bytes();
                                message._key = "key";
                                continue;
                            }
                        case 4: {
                                if (u !== 0)
                                    break;
                                message.notBefore = reader.uint64();
                                message._notBefore = "notBefore";
                                continue;
                            }
                        case 5: {
                                if (u !== 0)
                                    break;
                                message.notAfter = reader.uint64();
                                message._notAfter = "notAfter";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a Details message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.CertChain.NoiseCertificate.Details
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.CertChain.NoiseCertificate.Details} Details
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Details.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a Details message.
                 * @function verify
                 * @memberof proto.CertChain.NoiseCertificate.Details
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                Details.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.serial != null && Object.hasOwnProperty.call(message, "serial")) {
                        properties._serial = 1;
                        if (!$util.isInteger(message.serial))
                            return "serial: integer expected";
                    }
                    if (message.issuerSerial != null && Object.hasOwnProperty.call(message, "issuerSerial")) {
                        properties._issuerSerial = 1;
                        if (!$util.isInteger(message.issuerSerial))
                            return "issuerSerial: integer expected";
                    }
                    if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                        properties._key = 1;
                        if (!(message.key && typeof message.key.length === "number" || $util.isString(message.key)))
                            return "key: buffer expected";
                    }
                    if (message.notBefore != null && Object.hasOwnProperty.call(message, "notBefore")) {
                        properties._notBefore = 1;
                        if (!$util.isInteger(message.notBefore) && !(message.notBefore && $util.isInteger(message.notBefore.low) && $util.isInteger(message.notBefore.high)))
                            return "notBefore: integer|Long expected";
                    }
                    if (message.notAfter != null && Object.hasOwnProperty.call(message, "notAfter")) {
                        properties._notAfter = 1;
                        if (!$util.isInteger(message.notAfter) && !(message.notAfter && $util.isInteger(message.notAfter.low) && $util.isInteger(message.notAfter.high)))
                            return "notAfter: integer|Long expected";
                    }
                    return null;
                };

                /**
                 * Creates a Details message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.CertChain.NoiseCertificate.Details
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.CertChain.NoiseCertificate.Details} Details
                 */
                Details.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.CertChain.NoiseCertificate.Details: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.serial != null)
                        message.serial = object.serial >>> 0;
                    if (object.issuerSerial != null)
                        message.issuerSerial = object.issuerSerial >>> 0;
                    if (object.key != null)
                        if (typeof object.key === "string")
                            $util.base64.decode(object.key, message.key = $util.newBuffer($util.base64.length(object.key)), 0);
                        else if (object.key.length >= 0)
                            message.key = object.key;
                    if (object.notBefore != null)
                        if ($util.Long)
                            message.notBefore = $util.Long.fromValue(object.notBefore, true);
                        else if (typeof object.notBefore === "string")
                            message.notBefore = parseInt(object.notBefore, 10);
                        else if (typeof object.notBefore === "number")
                            message.notBefore = object.notBefore;
                        else if (typeof object.notBefore === "object")
                            message.notBefore = new $util.LongBits(object.notBefore.low >>> 0, object.notBefore.high >>> 0).toNumber(true);
                    if (object.notAfter != null)
                        if ($util.Long)
                            message.notAfter = $util.Long.fromValue(object.notAfter, true);
                        else if (typeof object.notAfter === "string")
                            message.notAfter = parseInt(object.notAfter, 10);
                        else if (typeof object.notAfter === "number")
                            message.notAfter = object.notAfter;
                        else if (typeof object.notAfter === "object")
                            message.notAfter = new $util.LongBits(object.notAfter.low >>> 0, object.notAfter.high >>> 0).toNumber(true);
                    return message;
                };

                /**
                 * Creates a plain object from a Details message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.CertChain.NoiseCertificate.Details
                 * @static
                 * @param {proto.CertChain.NoiseCertificate.Details} message Details
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                Details.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.serial != null && Object.hasOwnProperty.call(message, "serial"))
                        object.serial = message.serial;
                    if (message.issuerSerial != null && Object.hasOwnProperty.call(message, "issuerSerial"))
                        object.issuerSerial = message.issuerSerial;
                    if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                        object.key = options.bytes === String ? $util.base64.encode(message.key, 0, message.key.length) : options.bytes === Array ? Array.prototype.slice.call(message.key) : message.key;
                    if (message.notBefore != null && Object.hasOwnProperty.call(message, "notBefore"))
                        if (typeof BigInt !== "undefined" && options.longs === BigInt)
                            object.notBefore = typeof message.notBefore === "number" ? BigInt(message.notBefore) : $util.Long.fromBits(message.notBefore.low >>> 0, message.notBefore.high >>> 0, true).toBigInt();
                        else if (typeof message.notBefore === "number")
                            object.notBefore = options.longs === String ? String(message.notBefore) : message.notBefore;
                        else
                            object.notBefore = options.longs === String ? $util.Long.prototype.toString.call(message.notBefore) : options.longs === Number ? new $util.LongBits(message.notBefore.low >>> 0, message.notBefore.high >>> 0).toNumber(true) : message.notBefore;
                    if (message.notAfter != null && Object.hasOwnProperty.call(message, "notAfter"))
                        if (typeof BigInt !== "undefined" && options.longs === BigInt)
                            object.notAfter = typeof message.notAfter === "number" ? BigInt(message.notAfter) : $util.Long.fromBits(message.notAfter.low >>> 0, message.notAfter.high >>> 0, true).toBigInt();
                        else if (typeof message.notAfter === "number")
                            object.notAfter = options.longs === String ? String(message.notAfter) : message.notAfter;
                        else
                            object.notAfter = options.longs === String ? $util.Long.prototype.toString.call(message.notAfter) : options.longs === Number ? new $util.LongBits(message.notAfter.low >>> 0, message.notAfter.high >>> 0).toNumber(true) : message.notAfter;
                    return object;
                };

                /**
                 * Converts this Details to JSON.
                 * @function toJSON
                 * @memberof proto.CertChain.NoiseCertificate.Details
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                Details.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for Details
                 * @function getTypeUrl
                 * @memberof proto.CertChain.NoiseCertificate.Details
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                Details.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.CertChain.NoiseCertificate.Details";
                };

                return Details;
            })();

            return NoiseCertificate;
        })();

        return CertChain;
    })();

    proto.ChatLockSettings = (function() {

        /**
         * Properties of a ChatLockSettings.
         * @memberof proto
         * @interface IChatLockSettings
         * @property {boolean|null} [hideLockedChats] ChatLockSettings hideLockedChats
         * @property {proto.IUserPassword|null} [secretCode] ChatLockSettings secretCode
         */

        /**
         * Constructs a new ChatLockSettings.
         * @memberof proto
         * @classdesc Represents a ChatLockSettings.
         * @implements IChatLockSettings
         * @constructor
         * @param {proto.IChatLockSettings=} [properties] Properties to set
         */
        function ChatLockSettings(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ChatLockSettings hideLockedChats.
         * @member {boolean|null|undefined} hideLockedChats
         * @memberof proto.ChatLockSettings
         * @instance
         */
        ChatLockSettings.prototype.hideLockedChats = null;

        /**
         * ChatLockSettings secretCode.
         * @member {proto.IUserPassword|null|undefined} secretCode
         * @memberof proto.ChatLockSettings
         * @instance
         */
        ChatLockSettings.prototype.secretCode = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ChatLockSettings.prototype, "_hideLockedChats", {
            get: $util.oneOfGetter($oneOfFields = ["hideLockedChats"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ChatLockSettings.prototype, "_secretCode", {
            get: $util.oneOfGetter($oneOfFields = ["secretCode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ChatLockSettings instance using the specified properties.
         * @function create
         * @memberof proto.ChatLockSettings
         * @static
         * @param {proto.IChatLockSettings=} [properties] Properties to set
         * @returns {proto.ChatLockSettings} ChatLockSettings instance
         */
        ChatLockSettings.create = function create(properties) {
            return new ChatLockSettings(properties);
        };

        /**
         * Encodes the specified ChatLockSettings message. Does not implicitly {@link proto.ChatLockSettings.verify|verify} messages.
         * @function encode
         * @memberof proto.ChatLockSettings
         * @static
         * @param {proto.IChatLockSettings} message ChatLockSettings message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChatLockSettings.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.hideLockedChats != null && Object.hasOwnProperty.call(message, "hideLockedChats"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.hideLockedChats);
            if (message.secretCode != null && Object.hasOwnProperty.call(message, "secretCode"))
                $root.proto.UserPassword.encode(message.secretCode, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChatLockSettings message, length delimited. Does not implicitly {@link proto.ChatLockSettings.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ChatLockSettings
         * @static
         * @param {proto.IChatLockSettings} message ChatLockSettings message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChatLockSettings.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChatLockSettings message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ChatLockSettings
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ChatLockSettings} ChatLockSettings
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChatLockSettings.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.hideLockedChats = reader.bool();
                        message._hideLockedChats = "hideLockedChats";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.secretCode = $root.proto.UserPassword.decode(reader, reader.uint32(), undefined, q + 1, message.secretCode);
                        message._secretCode = "secretCode";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChatLockSettings message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ChatLockSettings
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ChatLockSettings} ChatLockSettings
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChatLockSettings.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChatLockSettings message.
         * @function verify
         * @memberof proto.ChatLockSettings
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChatLockSettings.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.hideLockedChats != null && Object.hasOwnProperty.call(message, "hideLockedChats")) {
                properties._hideLockedChats = 1;
                if (typeof message.hideLockedChats !== "boolean")
                    return "hideLockedChats: boolean expected";
            }
            if (message.secretCode != null && Object.hasOwnProperty.call(message, "secretCode")) {
                properties._secretCode = 1;
                {
                    var error = $root.proto.UserPassword.verify(message.secretCode, q + 1);
                    if (error)
                        return "secretCode." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ChatLockSettings message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ChatLockSettings
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ChatLockSettings} ChatLockSettings
         */
        ChatLockSettings.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.ChatLockSettings: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.hideLockedChats != null)
                message.hideLockedChats = Boolean(object.hideLockedChats);
            if (object.secretCode != null) {
                if (!$util.isObject(object.secretCode))
                    throw TypeError(".proto.ChatLockSettings.secretCode: object expected");
                message.secretCode = $root.proto.UserPassword.fromObject(object.secretCode, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ChatLockSettings message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ChatLockSettings
         * @static
         * @param {proto.ChatLockSettings} message ChatLockSettings
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChatLockSettings.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.hideLockedChats != null && Object.hasOwnProperty.call(message, "hideLockedChats"))
                object.hideLockedChats = message.hideLockedChats;
            if (message.secretCode != null && Object.hasOwnProperty.call(message, "secretCode"))
                object.secretCode = $root.proto.UserPassword.toObject(message.secretCode, options, q + 1);
            return object;
        };

        /**
         * Converts this ChatLockSettings to JSON.
         * @function toJSON
         * @memberof proto.ChatLockSettings
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChatLockSettings.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ChatLockSettings
         * @function getTypeUrl
         * @memberof proto.ChatLockSettings
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ChatLockSettings.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.ChatLockSettings";
        };

        return ChatLockSettings;
    })();

    proto.ChatRowOpaqueData = (function() {

        /**
         * Properties of a ChatRowOpaqueData.
         * @memberof proto
         * @interface IChatRowOpaqueData
         * @property {proto.ChatRowOpaqueData.IDraftMessage|null} [draftMessage] ChatRowOpaqueData draftMessage
         */

        /**
         * Constructs a new ChatRowOpaqueData.
         * @memberof proto
         * @classdesc Represents a ChatRowOpaqueData.
         * @implements IChatRowOpaqueData
         * @constructor
         * @param {proto.IChatRowOpaqueData=} [properties] Properties to set
         */
        function ChatRowOpaqueData(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ChatRowOpaqueData draftMessage.
         * @member {proto.ChatRowOpaqueData.IDraftMessage|null|undefined} draftMessage
         * @memberof proto.ChatRowOpaqueData
         * @instance
         */
        ChatRowOpaqueData.prototype.draftMessage = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ChatRowOpaqueData.prototype, "_draftMessage", {
            get: $util.oneOfGetter($oneOfFields = ["draftMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ChatRowOpaqueData instance using the specified properties.
         * @function create
         * @memberof proto.ChatRowOpaqueData
         * @static
         * @param {proto.IChatRowOpaqueData=} [properties] Properties to set
         * @returns {proto.ChatRowOpaqueData} ChatRowOpaqueData instance
         */
        ChatRowOpaqueData.create = function create(properties) {
            return new ChatRowOpaqueData(properties);
        };

        /**
         * Encodes the specified ChatRowOpaqueData message. Does not implicitly {@link proto.ChatRowOpaqueData.verify|verify} messages.
         * @function encode
         * @memberof proto.ChatRowOpaqueData
         * @static
         * @param {proto.IChatRowOpaqueData} message ChatRowOpaqueData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChatRowOpaqueData.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.draftMessage != null && Object.hasOwnProperty.call(message, "draftMessage"))
                $root.proto.ChatRowOpaqueData.DraftMessage.encode(message.draftMessage, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ChatRowOpaqueData message, length delimited. Does not implicitly {@link proto.ChatRowOpaqueData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ChatRowOpaqueData
         * @static
         * @param {proto.IChatRowOpaqueData} message ChatRowOpaqueData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChatRowOpaqueData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChatRowOpaqueData message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ChatRowOpaqueData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ChatRowOpaqueData} ChatRowOpaqueData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChatRowOpaqueData.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.draftMessage = $root.proto.ChatRowOpaqueData.DraftMessage.decode(reader, reader.uint32(), undefined, q + 1, message.draftMessage);
                        message._draftMessage = "draftMessage";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a ChatRowOpaqueData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ChatRowOpaqueData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ChatRowOpaqueData} ChatRowOpaqueData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChatRowOpaqueData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChatRowOpaqueData message.
         * @function verify
         * @memberof proto.ChatRowOpaqueData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChatRowOpaqueData.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.draftMessage != null && Object.hasOwnProperty.call(message, "draftMessage")) {
                properties._draftMessage = 1;
                {
                    var error = $root.proto.ChatRowOpaqueData.DraftMessage.verify(message.draftMessage, q + 1);
                    if (error)
                        return "draftMessage." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ChatRowOpaqueData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ChatRowOpaqueData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ChatRowOpaqueData} ChatRowOpaqueData
         */
        ChatRowOpaqueData.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.ChatRowOpaqueData: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.draftMessage != null) {
                if (!$util.isObject(object.draftMessage))
                    throw TypeError(".proto.ChatRowOpaqueData.draftMessage: object expected");
                message.draftMessage = $root.proto.ChatRowOpaqueData.DraftMessage.fromObject(object.draftMessage, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ChatRowOpaqueData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ChatRowOpaqueData
         * @static
         * @param {proto.ChatRowOpaqueData} message ChatRowOpaqueData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChatRowOpaqueData.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.draftMessage != null && Object.hasOwnProperty.call(message, "draftMessage"))
                object.draftMessage = $root.proto.ChatRowOpaqueData.DraftMessage.toObject(message.draftMessage, options, q + 1);
            return object;
        };

        /**
         * Converts this ChatRowOpaqueData to JSON.
         * @function toJSON
         * @memberof proto.ChatRowOpaqueData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChatRowOpaqueData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ChatRowOpaqueData
         * @function getTypeUrl
         * @memberof proto.ChatRowOpaqueData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ChatRowOpaqueData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.ChatRowOpaqueData";
        };

        ChatRowOpaqueData.DraftMessage = (function() {

            /**
             * Properties of a DraftMessage.
             * @memberof proto.ChatRowOpaqueData
             * @interface IDraftMessage
             * @property {string|null} [text] DraftMessage text
             * @property {string|null} [omittedUrl] DraftMessage omittedUrl
             * @property {proto.ChatRowOpaqueData.DraftMessage.ICtwaContextLinkData|null} [ctwaContextLinkData] DraftMessage ctwaContextLinkData
             * @property {proto.ChatRowOpaqueData.DraftMessage.ICtwaContextData|null} [ctwaContext] DraftMessage ctwaContext
             * @property {number|Long|null} [timestamp] DraftMessage timestamp
             */

            /**
             * Constructs a new DraftMessage.
             * @memberof proto.ChatRowOpaqueData
             * @classdesc Represents a DraftMessage.
             * @implements IDraftMessage
             * @constructor
             * @param {proto.ChatRowOpaqueData.IDraftMessage=} [properties] Properties to set
             */
            function DraftMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * DraftMessage text.
             * @member {string|null|undefined} text
             * @memberof proto.ChatRowOpaqueData.DraftMessage
             * @instance
             */
            DraftMessage.prototype.text = null;

            /**
             * DraftMessage omittedUrl.
             * @member {string|null|undefined} omittedUrl
             * @memberof proto.ChatRowOpaqueData.DraftMessage
             * @instance
             */
            DraftMessage.prototype.omittedUrl = null;

            /**
             * DraftMessage ctwaContextLinkData.
             * @member {proto.ChatRowOpaqueData.DraftMessage.ICtwaContextLinkData|null|undefined} ctwaContextLinkData
             * @memberof proto.ChatRowOpaqueData.DraftMessage
             * @instance
             */
            DraftMessage.prototype.ctwaContextLinkData = null;

            /**
             * DraftMessage ctwaContext.
             * @member {proto.ChatRowOpaqueData.DraftMessage.ICtwaContextData|null|undefined} ctwaContext
             * @memberof proto.ChatRowOpaqueData.DraftMessage
             * @instance
             */
            DraftMessage.prototype.ctwaContext = null;

            /**
             * DraftMessage timestamp.
             * @member {number|Long|null|undefined} timestamp
             * @memberof proto.ChatRowOpaqueData.DraftMessage
             * @instance
             */
            DraftMessage.prototype.timestamp = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DraftMessage.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DraftMessage.prototype, "_omittedUrl", {
                get: $util.oneOfGetter($oneOfFields = ["omittedUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DraftMessage.prototype, "_ctwaContextLinkData", {
                get: $util.oneOfGetter($oneOfFields = ["ctwaContextLinkData"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DraftMessage.prototype, "_ctwaContext", {
                get: $util.oneOfGetter($oneOfFields = ["ctwaContext"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DraftMessage.prototype, "_timestamp", {
                get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new DraftMessage instance using the specified properties.
             * @function create
             * @memberof proto.ChatRowOpaqueData.DraftMessage
             * @static
             * @param {proto.ChatRowOpaqueData.IDraftMessage=} [properties] Properties to set
             * @returns {proto.ChatRowOpaqueData.DraftMessage} DraftMessage instance
             */
            DraftMessage.create = function create(properties) {
                return new DraftMessage(properties);
            };

            /**
             * Encodes the specified DraftMessage message. Does not implicitly {@link proto.ChatRowOpaqueData.DraftMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.ChatRowOpaqueData.DraftMessage
             * @static
             * @param {proto.ChatRowOpaqueData.IDraftMessage} message DraftMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DraftMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.text);
                if (message.omittedUrl != null && Object.hasOwnProperty.call(message, "omittedUrl"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.omittedUrl);
                if (message.ctwaContextLinkData != null && Object.hasOwnProperty.call(message, "ctwaContextLinkData"))
                    $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData.encode(message.ctwaContextLinkData, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.ctwaContext != null && Object.hasOwnProperty.call(message, "ctwaContext"))
                    $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextData.encode(message.ctwaContext, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.timestamp);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DraftMessage message, length delimited. Does not implicitly {@link proto.ChatRowOpaqueData.DraftMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.ChatRowOpaqueData.DraftMessage
             * @static
             * @param {proto.ChatRowOpaqueData.IDraftMessage} message DraftMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DraftMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a DraftMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.ChatRowOpaqueData.DraftMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.ChatRowOpaqueData.DraftMessage} DraftMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DraftMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.text = reader.stringVerify();
                            message._text = "text";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.omittedUrl = reader.stringVerify();
                            message._omittedUrl = "omittedUrl";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.ctwaContextLinkData = $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData.decode(reader, reader.uint32(), undefined, q + 1, message.ctwaContextLinkData);
                            message._ctwaContextLinkData = "ctwaContextLinkData";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.ctwaContext = $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextData.decode(reader, reader.uint32(), undefined, q + 1, message.ctwaContext);
                            message._ctwaContext = "ctwaContext";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.timestamp = reader.int64();
                            message._timestamp = "timestamp";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a DraftMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.ChatRowOpaqueData.DraftMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.ChatRowOpaqueData.DraftMessage} DraftMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DraftMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DraftMessage message.
             * @function verify
             * @memberof proto.ChatRowOpaqueData.DraftMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DraftMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.text != null && Object.hasOwnProperty.call(message, "text")) {
                    properties._text = 1;
                    if (!$util.isString(message.text))
                        return "text: string expected";
                }
                if (message.omittedUrl != null && Object.hasOwnProperty.call(message, "omittedUrl")) {
                    properties._omittedUrl = 1;
                    if (!$util.isString(message.omittedUrl))
                        return "omittedUrl: string expected";
                }
                if (message.ctwaContextLinkData != null && Object.hasOwnProperty.call(message, "ctwaContextLinkData")) {
                    properties._ctwaContextLinkData = 1;
                    {
                        var error = $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData.verify(message.ctwaContextLinkData, q + 1);
                        if (error)
                            return "ctwaContextLinkData." + error;
                    }
                }
                if (message.ctwaContext != null && Object.hasOwnProperty.call(message, "ctwaContext")) {
                    properties._ctwaContext = 1;
                    {
                        var error = $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextData.verify(message.ctwaContext, q + 1);
                        if (error)
                            return "ctwaContext." + error;
                    }
                }
                if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp")) {
                    properties._timestamp = 1;
                    if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                        return "timestamp: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a DraftMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.ChatRowOpaqueData.DraftMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.ChatRowOpaqueData.DraftMessage} DraftMessage
             */
            DraftMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.ChatRowOpaqueData.DraftMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.text != null)
                    message.text = String(object.text);
                if (object.omittedUrl != null)
                    message.omittedUrl = String(object.omittedUrl);
                if (object.ctwaContextLinkData != null) {
                    if (!$util.isObject(object.ctwaContextLinkData))
                        throw TypeError(".proto.ChatRowOpaqueData.DraftMessage.ctwaContextLinkData: object expected");
                    message.ctwaContextLinkData = $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData.fromObject(object.ctwaContextLinkData, q + 1);
                }
                if (object.ctwaContext != null) {
                    if (!$util.isObject(object.ctwaContext))
                        throw TypeError(".proto.ChatRowOpaqueData.DraftMessage.ctwaContext: object expected");
                    message.ctwaContext = $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextData.fromObject(object.ctwaContext, q + 1);
                }
                if (object.timestamp != null)
                    if ($util.Long)
                        message.timestamp = $util.Long.fromValue(object.timestamp, false);
                    else if (typeof object.timestamp === "string")
                        message.timestamp = parseInt(object.timestamp, 10);
                    else if (typeof object.timestamp === "number")
                        message.timestamp = object.timestamp;
                    else if (typeof object.timestamp === "object")
                        message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a DraftMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.ChatRowOpaqueData.DraftMessage
             * @static
             * @param {proto.ChatRowOpaqueData.DraftMessage} message DraftMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DraftMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                    object.text = message.text;
                if (message.omittedUrl != null && Object.hasOwnProperty.call(message, "omittedUrl"))
                    object.omittedUrl = message.omittedUrl;
                if (message.ctwaContextLinkData != null && Object.hasOwnProperty.call(message, "ctwaContextLinkData"))
                    object.ctwaContextLinkData = $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData.toObject(message.ctwaContextLinkData, options, q + 1);
                if (message.ctwaContext != null && Object.hasOwnProperty.call(message, "ctwaContext"))
                    object.ctwaContext = $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextData.toObject(message.ctwaContext, options, q + 1);
                if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.timestamp = typeof message.timestamp === "number" ? BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.timestamp === "number")
                        object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                    else
                        object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
                return object;
            };

            /**
             * Converts this DraftMessage to JSON.
             * @function toJSON
             * @memberof proto.ChatRowOpaqueData.DraftMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DraftMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for DraftMessage
             * @function getTypeUrl
             * @memberof proto.ChatRowOpaqueData.DraftMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            DraftMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.ChatRowOpaqueData.DraftMessage";
            };

            DraftMessage.CtwaContextData = (function() {

                /**
                 * Properties of a CtwaContextData.
                 * @memberof proto.ChatRowOpaqueData.DraftMessage
                 * @interface ICtwaContextData
                 * @property {string|null} [conversionSource] CtwaContextData conversionSource
                 * @property {Uint8Array|null} [conversionData] CtwaContextData conversionData
                 * @property {string|null} [sourceUrl] CtwaContextData sourceUrl
                 * @property {string|null} [sourceId] CtwaContextData sourceId
                 * @property {string|null} [sourceType] CtwaContextData sourceType
                 * @property {string|null} [title] CtwaContextData title
                 * @property {string|null} [description] CtwaContextData description
                 * @property {string|null} [thumbnail] CtwaContextData thumbnail
                 * @property {string|null} [thumbnailUrl] CtwaContextData thumbnailUrl
                 * @property {proto.ChatRowOpaqueData.DraftMessage.CtwaContextData.ContextInfoExternalAdReplyInfoMediaType|null} [mediaType] CtwaContextData mediaType
                 * @property {string|null} [mediaUrl] CtwaContextData mediaUrl
                 * @property {boolean|null} [isSuspiciousLink] CtwaContextData isSuspiciousLink
                 */

                /**
                 * Constructs a new CtwaContextData.
                 * @memberof proto.ChatRowOpaqueData.DraftMessage
                 * @classdesc Represents a CtwaContextData.
                 * @implements ICtwaContextData
                 * @constructor
                 * @param {proto.ChatRowOpaqueData.DraftMessage.ICtwaContextData=} [properties] Properties to set
                 */
                function CtwaContextData(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * CtwaContextData conversionSource.
                 * @member {string|null|undefined} conversionSource
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @instance
                 */
                CtwaContextData.prototype.conversionSource = null;

                /**
                 * CtwaContextData conversionData.
                 * @member {Uint8Array|null|undefined} conversionData
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @instance
                 */
                CtwaContextData.prototype.conversionData = null;

                /**
                 * CtwaContextData sourceUrl.
                 * @member {string|null|undefined} sourceUrl
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @instance
                 */
                CtwaContextData.prototype.sourceUrl = null;

                /**
                 * CtwaContextData sourceId.
                 * @member {string|null|undefined} sourceId
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @instance
                 */
                CtwaContextData.prototype.sourceId = null;

                /**
                 * CtwaContextData sourceType.
                 * @member {string|null|undefined} sourceType
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @instance
                 */
                CtwaContextData.prototype.sourceType = null;

                /**
                 * CtwaContextData title.
                 * @member {string|null|undefined} title
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @instance
                 */
                CtwaContextData.prototype.title = null;

                /**
                 * CtwaContextData description.
                 * @member {string|null|undefined} description
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @instance
                 */
                CtwaContextData.prototype.description = null;

                /**
                 * CtwaContextData thumbnail.
                 * @member {string|null|undefined} thumbnail
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @instance
                 */
                CtwaContextData.prototype.thumbnail = null;

                /**
                 * CtwaContextData thumbnailUrl.
                 * @member {string|null|undefined} thumbnailUrl
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @instance
                 */
                CtwaContextData.prototype.thumbnailUrl = null;

                /**
                 * CtwaContextData mediaType.
                 * @member {proto.ChatRowOpaqueData.DraftMessage.CtwaContextData.ContextInfoExternalAdReplyInfoMediaType|null|undefined} mediaType
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @instance
                 */
                CtwaContextData.prototype.mediaType = null;

                /**
                 * CtwaContextData mediaUrl.
                 * @member {string|null|undefined} mediaUrl
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @instance
                 */
                CtwaContextData.prototype.mediaUrl = null;

                /**
                 * CtwaContextData isSuspiciousLink.
                 * @member {boolean|null|undefined} isSuspiciousLink
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @instance
                 */
                CtwaContextData.prototype.isSuspiciousLink = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CtwaContextData.prototype, "_conversionSource", {
                    get: $util.oneOfGetter($oneOfFields = ["conversionSource"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CtwaContextData.prototype, "_conversionData", {
                    get: $util.oneOfGetter($oneOfFields = ["conversionData"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CtwaContextData.prototype, "_sourceUrl", {
                    get: $util.oneOfGetter($oneOfFields = ["sourceUrl"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CtwaContextData.prototype, "_sourceId", {
                    get: $util.oneOfGetter($oneOfFields = ["sourceId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CtwaContextData.prototype, "_sourceType", {
                    get: $util.oneOfGetter($oneOfFields = ["sourceType"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CtwaContextData.prototype, "_title", {
                    get: $util.oneOfGetter($oneOfFields = ["title"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CtwaContextData.prototype, "_description", {
                    get: $util.oneOfGetter($oneOfFields = ["description"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CtwaContextData.prototype, "_thumbnail", {
                    get: $util.oneOfGetter($oneOfFields = ["thumbnail"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CtwaContextData.prototype, "_thumbnailUrl", {
                    get: $util.oneOfGetter($oneOfFields = ["thumbnailUrl"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CtwaContextData.prototype, "_mediaType", {
                    get: $util.oneOfGetter($oneOfFields = ["mediaType"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CtwaContextData.prototype, "_mediaUrl", {
                    get: $util.oneOfGetter($oneOfFields = ["mediaUrl"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CtwaContextData.prototype, "_isSuspiciousLink", {
                    get: $util.oneOfGetter($oneOfFields = ["isSuspiciousLink"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new CtwaContextData instance using the specified properties.
                 * @function create
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @static
                 * @param {proto.ChatRowOpaqueData.DraftMessage.ICtwaContextData=} [properties] Properties to set
                 * @returns {proto.ChatRowOpaqueData.DraftMessage.CtwaContextData} CtwaContextData instance
                 */
                CtwaContextData.create = function create(properties) {
                    return new CtwaContextData(properties);
                };

                /**
                 * Encodes the specified CtwaContextData message. Does not implicitly {@link proto.ChatRowOpaqueData.DraftMessage.CtwaContextData.verify|verify} messages.
                 * @function encode
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @static
                 * @param {proto.ChatRowOpaqueData.DraftMessage.ICtwaContextData} message CtwaContextData message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                CtwaContextData.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.conversionSource != null && Object.hasOwnProperty.call(message, "conversionSource"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.conversionSource);
                    if (message.conversionData != null && Object.hasOwnProperty.call(message, "conversionData"))
                        writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.conversionData);
                    if (message.sourceUrl != null && Object.hasOwnProperty.call(message, "sourceUrl"))
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.sourceUrl);
                    if (message.sourceId != null && Object.hasOwnProperty.call(message, "sourceId"))
                        writer.uint32(/* id 4, wireType 2 =*/34).string(message.sourceId);
                    if (message.sourceType != null && Object.hasOwnProperty.call(message, "sourceType"))
                        writer.uint32(/* id 5, wireType 2 =*/42).string(message.sourceType);
                    if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                        writer.uint32(/* id 6, wireType 2 =*/50).string(message.title);
                    if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                        writer.uint32(/* id 7, wireType 2 =*/58).string(message.description);
                    if (message.thumbnail != null && Object.hasOwnProperty.call(message, "thumbnail"))
                        writer.uint32(/* id 8, wireType 2 =*/66).string(message.thumbnail);
                    if (message.thumbnailUrl != null && Object.hasOwnProperty.call(message, "thumbnailUrl"))
                        writer.uint32(/* id 9, wireType 2 =*/74).string(message.thumbnailUrl);
                    if (message.mediaType != null && Object.hasOwnProperty.call(message, "mediaType"))
                        writer.uint32(/* id 10, wireType 0 =*/80).int32(message.mediaType);
                    if (message.mediaUrl != null && Object.hasOwnProperty.call(message, "mediaUrl"))
                        writer.uint32(/* id 11, wireType 2 =*/90).string(message.mediaUrl);
                    if (message.isSuspiciousLink != null && Object.hasOwnProperty.call(message, "isSuspiciousLink"))
                        writer.uint32(/* id 12, wireType 0 =*/96).bool(message.isSuspiciousLink);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified CtwaContextData message, length delimited. Does not implicitly {@link proto.ChatRowOpaqueData.DraftMessage.CtwaContextData.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @static
                 * @param {proto.ChatRowOpaqueData.DraftMessage.ICtwaContextData} message CtwaContextData message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                CtwaContextData.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a CtwaContextData message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.ChatRowOpaqueData.DraftMessage.CtwaContextData} CtwaContextData
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                CtwaContextData.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.conversionSource = reader.stringVerify();
                                message._conversionSource = "conversionSource";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.conversionData = reader.bytes();
                                message._conversionData = "conversionData";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.sourceUrl = reader.stringVerify();
                                message._sourceUrl = "sourceUrl";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                message.sourceId = reader.stringVerify();
                                message._sourceId = "sourceId";
                                continue;
                            }
                        case 5: {
                                if (u !== 2)
                                    break;
                                message.sourceType = reader.stringVerify();
                                message._sourceType = "sourceType";
                                continue;
                            }
                        case 6: {
                                if (u !== 2)
                                    break;
                                message.title = reader.stringVerify();
                                message._title = "title";
                                continue;
                            }
                        case 7: {
                                if (u !== 2)
                                    break;
                                message.description = reader.stringVerify();
                                message._description = "description";
                                continue;
                            }
                        case 8: {
                                if (u !== 2)
                                    break;
                                message.thumbnail = reader.stringVerify();
                                message._thumbnail = "thumbnail";
                                continue;
                            }
                        case 9: {
                                if (u !== 2)
                                    break;
                                message.thumbnailUrl = reader.stringVerify();
                                message._thumbnailUrl = "thumbnailUrl";
                                continue;
                            }
                        case 10: {
                                if (u !== 0)
                                    break;
                                message.mediaType = reader.int32();
                                message._mediaType = "mediaType";
                                continue;
                            }
                        case 11: {
                                if (u !== 2)
                                    break;
                                message.mediaUrl = reader.stringVerify();
                                message._mediaUrl = "mediaUrl";
                                continue;
                            }
                        case 12: {
                                if (u !== 0)
                                    break;
                                message.isSuspiciousLink = reader.bool();
                                message._isSuspiciousLink = "isSuspiciousLink";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a CtwaContextData message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.ChatRowOpaqueData.DraftMessage.CtwaContextData} CtwaContextData
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                CtwaContextData.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a CtwaContextData message.
                 * @function verify
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                CtwaContextData.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.conversionSource != null && Object.hasOwnProperty.call(message, "conversionSource")) {
                        properties._conversionSource = 1;
                        if (!$util.isString(message.conversionSource))
                            return "conversionSource: string expected";
                    }
                    if (message.conversionData != null && Object.hasOwnProperty.call(message, "conversionData")) {
                        properties._conversionData = 1;
                        if (!(message.conversionData && typeof message.conversionData.length === "number" || $util.isString(message.conversionData)))
                            return "conversionData: buffer expected";
                    }
                    if (message.sourceUrl != null && Object.hasOwnProperty.call(message, "sourceUrl")) {
                        properties._sourceUrl = 1;
                        if (!$util.isString(message.sourceUrl))
                            return "sourceUrl: string expected";
                    }
                    if (message.sourceId != null && Object.hasOwnProperty.call(message, "sourceId")) {
                        properties._sourceId = 1;
                        if (!$util.isString(message.sourceId))
                            return "sourceId: string expected";
                    }
                    if (message.sourceType != null && Object.hasOwnProperty.call(message, "sourceType")) {
                        properties._sourceType = 1;
                        if (!$util.isString(message.sourceType))
                            return "sourceType: string expected";
                    }
                    if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                        properties._title = 1;
                        if (!$util.isString(message.title))
                            return "title: string expected";
                    }
                    if (message.description != null && Object.hasOwnProperty.call(message, "description")) {
                        properties._description = 1;
                        if (!$util.isString(message.description))
                            return "description: string expected";
                    }
                    if (message.thumbnail != null && Object.hasOwnProperty.call(message, "thumbnail")) {
                        properties._thumbnail = 1;
                        if (!$util.isString(message.thumbnail))
                            return "thumbnail: string expected";
                    }
                    if (message.thumbnailUrl != null && Object.hasOwnProperty.call(message, "thumbnailUrl")) {
                        properties._thumbnailUrl = 1;
                        if (!$util.isString(message.thumbnailUrl))
                            return "thumbnailUrl: string expected";
                    }
                    if (message.mediaType != null && Object.hasOwnProperty.call(message, "mediaType")) {
                        properties._mediaType = 1;
                        if (typeof message.mediaType !== "number" || (message.mediaType | 0) !== message.mediaType)
                            return "mediaType: enum value expected";
                    }
                    if (message.mediaUrl != null && Object.hasOwnProperty.call(message, "mediaUrl")) {
                        properties._mediaUrl = 1;
                        if (!$util.isString(message.mediaUrl))
                            return "mediaUrl: string expected";
                    }
                    if (message.isSuspiciousLink != null && Object.hasOwnProperty.call(message, "isSuspiciousLink")) {
                        properties._isSuspiciousLink = 1;
                        if (typeof message.isSuspiciousLink !== "boolean")
                            return "isSuspiciousLink: boolean expected";
                    }
                    return null;
                };

                /**
                 * Creates a CtwaContextData message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.ChatRowOpaqueData.DraftMessage.CtwaContextData} CtwaContextData
                 */
                CtwaContextData.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.ChatRowOpaqueData.DraftMessage.CtwaContextData: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.conversionSource != null)
                        message.conversionSource = String(object.conversionSource);
                    if (object.conversionData != null)
                        if (typeof object.conversionData === "string")
                            $util.base64.decode(object.conversionData, message.conversionData = $util.newBuffer($util.base64.length(object.conversionData)), 0);
                        else if (object.conversionData.length >= 0)
                            message.conversionData = object.conversionData;
                    if (object.sourceUrl != null)
                        message.sourceUrl = String(object.sourceUrl);
                    if (object.sourceId != null)
                        message.sourceId = String(object.sourceId);
                    if (object.sourceType != null)
                        message.sourceType = String(object.sourceType);
                    if (object.title != null)
                        message.title = String(object.title);
                    if (object.description != null)
                        message.description = String(object.description);
                    if (object.thumbnail != null)
                        message.thumbnail = String(object.thumbnail);
                    if (object.thumbnailUrl != null)
                        message.thumbnailUrl = String(object.thumbnailUrl);
                    switch (object.mediaType) {
                    case "NONE":
                    case 0:
                        message.mediaType = 0;
                        break;
                    case "IMAGE":
                    case 1:
                        message.mediaType = 1;
                        break;
                    case "VIDEO":
                    case 2:
                        message.mediaType = 2;
                        break;
                    default:
                        if (typeof object.mediaType === "number" && (object.mediaType | 0) === object.mediaType)
                            message.mediaType = object.mediaType;
                    }
                    if (object.mediaUrl != null)
                        message.mediaUrl = String(object.mediaUrl);
                    if (object.isSuspiciousLink != null)
                        message.isSuspiciousLink = Boolean(object.isSuspiciousLink);
                    return message;
                };

                /**
                 * Creates a plain object from a CtwaContextData message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @static
                 * @param {proto.ChatRowOpaqueData.DraftMessage.CtwaContextData} message CtwaContextData
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                CtwaContextData.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.conversionSource != null && Object.hasOwnProperty.call(message, "conversionSource"))
                        object.conversionSource = message.conversionSource;
                    if (message.conversionData != null && Object.hasOwnProperty.call(message, "conversionData"))
                        object.conversionData = options.bytes === String ? $util.base64.encode(message.conversionData, 0, message.conversionData.length) : options.bytes === Array ? Array.prototype.slice.call(message.conversionData) : message.conversionData;
                    if (message.sourceUrl != null && Object.hasOwnProperty.call(message, "sourceUrl"))
                        object.sourceUrl = message.sourceUrl;
                    if (message.sourceId != null && Object.hasOwnProperty.call(message, "sourceId"))
                        object.sourceId = message.sourceId;
                    if (message.sourceType != null && Object.hasOwnProperty.call(message, "sourceType"))
                        object.sourceType = message.sourceType;
                    if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                        object.title = message.title;
                    if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                        object.description = message.description;
                    if (message.thumbnail != null && Object.hasOwnProperty.call(message, "thumbnail"))
                        object.thumbnail = message.thumbnail;
                    if (message.thumbnailUrl != null && Object.hasOwnProperty.call(message, "thumbnailUrl"))
                        object.thumbnailUrl = message.thumbnailUrl;
                    if (message.mediaType != null && Object.hasOwnProperty.call(message, "mediaType"))
                        object.mediaType = options.enums === String ? $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextData.ContextInfoExternalAdReplyInfoMediaType[message.mediaType] === undefined ? message.mediaType : $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextData.ContextInfoExternalAdReplyInfoMediaType[message.mediaType] : message.mediaType;
                    if (message.mediaUrl != null && Object.hasOwnProperty.call(message, "mediaUrl"))
                        object.mediaUrl = message.mediaUrl;
                    if (message.isSuspiciousLink != null && Object.hasOwnProperty.call(message, "isSuspiciousLink"))
                        object.isSuspiciousLink = message.isSuspiciousLink;
                    return object;
                };

                /**
                 * Converts this CtwaContextData to JSON.
                 * @function toJSON
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                CtwaContextData.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for CtwaContextData
                 * @function getTypeUrl
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextData
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                CtwaContextData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.ChatRowOpaqueData.DraftMessage.CtwaContextData";
                };

                /**
                 * ContextInfoExternalAdReplyInfoMediaType enum.
                 * @name proto.ChatRowOpaqueData.DraftMessage.CtwaContextData.ContextInfoExternalAdReplyInfoMediaType
                 * @enum {number}
                 * @property {number} NONE=0 NONE value
                 * @property {number} IMAGE=1 IMAGE value
                 * @property {number} VIDEO=2 VIDEO value
                 */
                CtwaContextData.ContextInfoExternalAdReplyInfoMediaType = (function() {
                    var valuesById = {}, values = Object.create(valuesById);
                    values[valuesById[0] = "NONE"] = 0;
                    values[valuesById[1] = "IMAGE"] = 1;
                    values[valuesById[2] = "VIDEO"] = 2;
                    return values;
                })();

                return CtwaContextData;
            })();

            DraftMessage.CtwaContextLinkData = (function() {

                /**
                 * Properties of a CtwaContextLinkData.
                 * @memberof proto.ChatRowOpaqueData.DraftMessage
                 * @interface ICtwaContextLinkData
                 * @property {string|null} [context] CtwaContextLinkData context
                 * @property {string|null} [sourceUrl] CtwaContextLinkData sourceUrl
                 * @property {string|null} [icebreaker] CtwaContextLinkData icebreaker
                 * @property {string|null} [phone] CtwaContextLinkData phone
                 */

                /**
                 * Constructs a new CtwaContextLinkData.
                 * @memberof proto.ChatRowOpaqueData.DraftMessage
                 * @classdesc Represents a CtwaContextLinkData.
                 * @implements ICtwaContextLinkData
                 * @constructor
                 * @param {proto.ChatRowOpaqueData.DraftMessage.ICtwaContextLinkData=} [properties] Properties to set
                 */
                function CtwaContextLinkData(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * CtwaContextLinkData context.
                 * @member {string|null|undefined} context
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData
                 * @instance
                 */
                CtwaContextLinkData.prototype.context = null;

                /**
                 * CtwaContextLinkData sourceUrl.
                 * @member {string|null|undefined} sourceUrl
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData
                 * @instance
                 */
                CtwaContextLinkData.prototype.sourceUrl = null;

                /**
                 * CtwaContextLinkData icebreaker.
                 * @member {string|null|undefined} icebreaker
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData
                 * @instance
                 */
                CtwaContextLinkData.prototype.icebreaker = null;

                /**
                 * CtwaContextLinkData phone.
                 * @member {string|null|undefined} phone
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData
                 * @instance
                 */
                CtwaContextLinkData.prototype.phone = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CtwaContextLinkData.prototype, "_context", {
                    get: $util.oneOfGetter($oneOfFields = ["context"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CtwaContextLinkData.prototype, "_sourceUrl", {
                    get: $util.oneOfGetter($oneOfFields = ["sourceUrl"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CtwaContextLinkData.prototype, "_icebreaker", {
                    get: $util.oneOfGetter($oneOfFields = ["icebreaker"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CtwaContextLinkData.prototype, "_phone", {
                    get: $util.oneOfGetter($oneOfFields = ["phone"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new CtwaContextLinkData instance using the specified properties.
                 * @function create
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData
                 * @static
                 * @param {proto.ChatRowOpaqueData.DraftMessage.ICtwaContextLinkData=} [properties] Properties to set
                 * @returns {proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData} CtwaContextLinkData instance
                 */
                CtwaContextLinkData.create = function create(properties) {
                    return new CtwaContextLinkData(properties);
                };

                /**
                 * Encodes the specified CtwaContextLinkData message. Does not implicitly {@link proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData.verify|verify} messages.
                 * @function encode
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData
                 * @static
                 * @param {proto.ChatRowOpaqueData.DraftMessage.ICtwaContextLinkData} message CtwaContextLinkData message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                CtwaContextLinkData.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.context != null && Object.hasOwnProperty.call(message, "context"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.context);
                    if (message.sourceUrl != null && Object.hasOwnProperty.call(message, "sourceUrl"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.sourceUrl);
                    if (message.icebreaker != null && Object.hasOwnProperty.call(message, "icebreaker"))
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.icebreaker);
                    if (message.phone != null && Object.hasOwnProperty.call(message, "phone"))
                        writer.uint32(/* id 4, wireType 2 =*/34).string(message.phone);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified CtwaContextLinkData message, length delimited. Does not implicitly {@link proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData
                 * @static
                 * @param {proto.ChatRowOpaqueData.DraftMessage.ICtwaContextLinkData} message CtwaContextLinkData message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                CtwaContextLinkData.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a CtwaContextLinkData message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData} CtwaContextLinkData
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                CtwaContextLinkData.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.context = reader.stringVerify();
                                message._context = "context";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.sourceUrl = reader.stringVerify();
                                message._sourceUrl = "sourceUrl";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.icebreaker = reader.stringVerify();
                                message._icebreaker = "icebreaker";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                message.phone = reader.stringVerify();
                                message._phone = "phone";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a CtwaContextLinkData message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData} CtwaContextLinkData
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                CtwaContextLinkData.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a CtwaContextLinkData message.
                 * @function verify
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                CtwaContextLinkData.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.context != null && Object.hasOwnProperty.call(message, "context")) {
                        properties._context = 1;
                        if (!$util.isString(message.context))
                            return "context: string expected";
                    }
                    if (message.sourceUrl != null && Object.hasOwnProperty.call(message, "sourceUrl")) {
                        properties._sourceUrl = 1;
                        if (!$util.isString(message.sourceUrl))
                            return "sourceUrl: string expected";
                    }
                    if (message.icebreaker != null && Object.hasOwnProperty.call(message, "icebreaker")) {
                        properties._icebreaker = 1;
                        if (!$util.isString(message.icebreaker))
                            return "icebreaker: string expected";
                    }
                    if (message.phone != null && Object.hasOwnProperty.call(message, "phone")) {
                        properties._phone = 1;
                        if (!$util.isString(message.phone))
                            return "phone: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a CtwaContextLinkData message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData} CtwaContextLinkData
                 */
                CtwaContextLinkData.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.context != null)
                        message.context = String(object.context);
                    if (object.sourceUrl != null)
                        message.sourceUrl = String(object.sourceUrl);
                    if (object.icebreaker != null)
                        message.icebreaker = String(object.icebreaker);
                    if (object.phone != null)
                        message.phone = String(object.phone);
                    return message;
                };

                /**
                 * Creates a plain object from a CtwaContextLinkData message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData
                 * @static
                 * @param {proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData} message CtwaContextLinkData
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                CtwaContextLinkData.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.context != null && Object.hasOwnProperty.call(message, "context"))
                        object.context = message.context;
                    if (message.sourceUrl != null && Object.hasOwnProperty.call(message, "sourceUrl"))
                        object.sourceUrl = message.sourceUrl;
                    if (message.icebreaker != null && Object.hasOwnProperty.call(message, "icebreaker"))
                        object.icebreaker = message.icebreaker;
                    if (message.phone != null && Object.hasOwnProperty.call(message, "phone"))
                        object.phone = message.phone;
                    return object;
                };

                /**
                 * Converts this CtwaContextLinkData to JSON.
                 * @function toJSON
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                CtwaContextLinkData.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for CtwaContextLinkData
                 * @function getTypeUrl
                 * @memberof proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                CtwaContextLinkData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData";
                };

                return CtwaContextLinkData;
            })();

            return DraftMessage;
        })();

        return ChatRowOpaqueData;
    })();

    proto.Citation = (function() {

        /**
         * Properties of a Citation.
         * @memberof proto
         * @interface ICitation
         * @property {string|null} [title] Citation title
         * @property {string|null} [subtitle] Citation subtitle
         * @property {string|null} [cmsId] Citation cmsId
         * @property {string|null} [imageUrl] Citation imageUrl
         */

        /**
         * Constructs a new Citation.
         * @memberof proto
         * @classdesc Represents a Citation.
         * @implements ICitation
         * @constructor
         * @param {proto.ICitation=} [properties] Properties to set
         */
        function Citation(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Citation title.
         * @member {string|null|undefined} title
         * @memberof proto.Citation
         * @instance
         */
        Citation.prototype.title = null;

        /**
         * Citation subtitle.
         * @member {string|null|undefined} subtitle
         * @memberof proto.Citation
         * @instance
         */
        Citation.prototype.subtitle = null;

        /**
         * Citation cmsId.
         * @member {string|null|undefined} cmsId
         * @memberof proto.Citation
         * @instance
         */
        Citation.prototype.cmsId = null;

        /**
         * Citation imageUrl.
         * @member {string|null|undefined} imageUrl
         * @memberof proto.Citation
         * @instance
         */
        Citation.prototype.imageUrl = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Citation.prototype, "_title", {
            get: $util.oneOfGetter($oneOfFields = ["title"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Citation.prototype, "_subtitle", {
            get: $util.oneOfGetter($oneOfFields = ["subtitle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Citation.prototype, "_cmsId", {
            get: $util.oneOfGetter($oneOfFields = ["cmsId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Citation.prototype, "_imageUrl", {
            get: $util.oneOfGetter($oneOfFields = ["imageUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new Citation instance using the specified properties.
         * @function create
         * @memberof proto.Citation
         * @static
         * @param {proto.ICitation=} [properties] Properties to set
         * @returns {proto.Citation} Citation instance
         */
        Citation.create = function create(properties) {
            return new Citation(properties);
        };

        /**
         * Encodes the specified Citation message. Does not implicitly {@link proto.Citation.verify|verify} messages.
         * @function encode
         * @memberof proto.Citation
         * @static
         * @param {proto.ICitation} message Citation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Citation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.title);
            if (message.subtitle != null && Object.hasOwnProperty.call(message, "subtitle"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.subtitle);
            if (message.cmsId != null && Object.hasOwnProperty.call(message, "cmsId"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.cmsId);
            if (message.imageUrl != null && Object.hasOwnProperty.call(message, "imageUrl"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.imageUrl);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Citation message, length delimited. Does not implicitly {@link proto.Citation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Citation
         * @static
         * @param {proto.ICitation} message Citation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Citation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Citation message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Citation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Citation} Citation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Citation.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.title = reader.stringVerify();
                        message._title = "title";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.subtitle = reader.stringVerify();
                        message._subtitle = "subtitle";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.cmsId = reader.stringVerify();
                        message._cmsId = "cmsId";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.imageUrl = reader.stringVerify();
                        message._imageUrl = "imageUrl";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a Citation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Citation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Citation} Citation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Citation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Citation message.
         * @function verify
         * @memberof proto.Citation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Citation.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                properties._title = 1;
                if (!$util.isString(message.title))
                    return "title: string expected";
            }
            if (message.subtitle != null && Object.hasOwnProperty.call(message, "subtitle")) {
                properties._subtitle = 1;
                if (!$util.isString(message.subtitle))
                    return "subtitle: string expected";
            }
            if (message.cmsId != null && Object.hasOwnProperty.call(message, "cmsId")) {
                properties._cmsId = 1;
                if (!$util.isString(message.cmsId))
                    return "cmsId: string expected";
            }
            if (message.imageUrl != null && Object.hasOwnProperty.call(message, "imageUrl")) {
                properties._imageUrl = 1;
                if (!$util.isString(message.imageUrl))
                    return "imageUrl: string expected";
            }
            return null;
        };

        /**
         * Creates a Citation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Citation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Citation} Citation
         */
        Citation.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.Citation: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.title != null)
                message.title = String(object.title);
            if (object.subtitle != null)
                message.subtitle = String(object.subtitle);
            if (object.cmsId != null)
                message.cmsId = String(object.cmsId);
            if (object.imageUrl != null)
                message.imageUrl = String(object.imageUrl);
            return message;
        };

        /**
         * Creates a plain object from a Citation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Citation
         * @static
         * @param {proto.Citation} message Citation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Citation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                object.title = message.title;
            if (message.subtitle != null && Object.hasOwnProperty.call(message, "subtitle"))
                object.subtitle = message.subtitle;
            if (message.cmsId != null && Object.hasOwnProperty.call(message, "cmsId"))
                object.cmsId = message.cmsId;
            if (message.imageUrl != null && Object.hasOwnProperty.call(message, "imageUrl"))
                object.imageUrl = message.imageUrl;
            return object;
        };

        /**
         * Converts this Citation to JSON.
         * @function toJSON
         * @memberof proto.Citation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Citation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Citation
         * @function getTypeUrl
         * @memberof proto.Citation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Citation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.Citation";
        };

        return Citation;
    })();

    proto.ClientPairingProps = (function() {

        /**
         * Properties of a ClientPairingProps.
         * @memberof proto
         * @interface IClientPairingProps
         * @property {boolean|null} [isChatDbLidMigrated] ClientPairingProps isChatDbLidMigrated
         * @property {boolean|null} [isSyncdPureLidSession] ClientPairingProps isSyncdPureLidSession
         * @property {boolean|null} [isSyncdSnapshotRecoveryEnabled] ClientPairingProps isSyncdSnapshotRecoveryEnabled
         * @property {boolean|null} [isHsThumbnailSyncEnabled] ClientPairingProps isHsThumbnailSyncEnabled
         * @property {Uint8Array|null} [subscriptionSyncPayload] ClientPairingProps subscriptionSyncPayload
         */

        /**
         * Constructs a new ClientPairingProps.
         * @memberof proto
         * @classdesc Represents a ClientPairingProps.
         * @implements IClientPairingProps
         * @constructor
         * @param {proto.IClientPairingProps=} [properties] Properties to set
         */
        function ClientPairingProps(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ClientPairingProps isChatDbLidMigrated.
         * @member {boolean|null|undefined} isChatDbLidMigrated
         * @memberof proto.ClientPairingProps
         * @instance
         */
        ClientPairingProps.prototype.isChatDbLidMigrated = null;

        /**
         * ClientPairingProps isSyncdPureLidSession.
         * @member {boolean|null|undefined} isSyncdPureLidSession
         * @memberof proto.ClientPairingProps
         * @instance
         */
        ClientPairingProps.prototype.isSyncdPureLidSession = null;

        /**
         * ClientPairingProps isSyncdSnapshotRecoveryEnabled.
         * @member {boolean|null|undefined} isSyncdSnapshotRecoveryEnabled
         * @memberof proto.ClientPairingProps
         * @instance
         */
        ClientPairingProps.prototype.isSyncdSnapshotRecoveryEnabled = null;

        /**
         * ClientPairingProps isHsThumbnailSyncEnabled.
         * @member {boolean|null|undefined} isHsThumbnailSyncEnabled
         * @memberof proto.ClientPairingProps
         * @instance
         */
        ClientPairingProps.prototype.isHsThumbnailSyncEnabled = null;

        /**
         * ClientPairingProps subscriptionSyncPayload.
         * @member {Uint8Array|null|undefined} subscriptionSyncPayload
         * @memberof proto.ClientPairingProps
         * @instance
         */
        ClientPairingProps.prototype.subscriptionSyncPayload = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPairingProps.prototype, "_isChatDbLidMigrated", {
            get: $util.oneOfGetter($oneOfFields = ["isChatDbLidMigrated"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPairingProps.prototype, "_isSyncdPureLidSession", {
            get: $util.oneOfGetter($oneOfFields = ["isSyncdPureLidSession"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPairingProps.prototype, "_isSyncdSnapshotRecoveryEnabled", {
            get: $util.oneOfGetter($oneOfFields = ["isSyncdSnapshotRecoveryEnabled"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPairingProps.prototype, "_isHsThumbnailSyncEnabled", {
            get: $util.oneOfGetter($oneOfFields = ["isHsThumbnailSyncEnabled"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPairingProps.prototype, "_subscriptionSyncPayload", {
            get: $util.oneOfGetter($oneOfFields = ["subscriptionSyncPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ClientPairingProps instance using the specified properties.
         * @function create
         * @memberof proto.ClientPairingProps
         * @static
         * @param {proto.IClientPairingProps=} [properties] Properties to set
         * @returns {proto.ClientPairingProps} ClientPairingProps instance
         */
        ClientPairingProps.create = function create(properties) {
            return new ClientPairingProps(properties);
        };

        /**
         * Encodes the specified ClientPairingProps message. Does not implicitly {@link proto.ClientPairingProps.verify|verify} messages.
         * @function encode
         * @memberof proto.ClientPairingProps
         * @static
         * @param {proto.IClientPairingProps} message ClientPairingProps message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ClientPairingProps.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.isChatDbLidMigrated != null && Object.hasOwnProperty.call(message, "isChatDbLidMigrated"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.isChatDbLidMigrated);
            if (message.isSyncdPureLidSession != null && Object.hasOwnProperty.call(message, "isSyncdPureLidSession"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.isSyncdPureLidSession);
            if (message.isSyncdSnapshotRecoveryEnabled != null && Object.hasOwnProperty.call(message, "isSyncdSnapshotRecoveryEnabled"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.isSyncdSnapshotRecoveryEnabled);
            if (message.isHsThumbnailSyncEnabled != null && Object.hasOwnProperty.call(message, "isHsThumbnailSyncEnabled"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.isHsThumbnailSyncEnabled);
            if (message.subscriptionSyncPayload != null && Object.hasOwnProperty.call(message, "subscriptionSyncPayload"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.subscriptionSyncPayload);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ClientPairingProps message, length delimited. Does not implicitly {@link proto.ClientPairingProps.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ClientPairingProps
         * @static
         * @param {proto.IClientPairingProps} message ClientPairingProps message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ClientPairingProps.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ClientPairingProps message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ClientPairingProps
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ClientPairingProps} ClientPairingProps
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ClientPairingProps.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.isChatDbLidMigrated = reader.bool();
                        message._isChatDbLidMigrated = "isChatDbLidMigrated";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.isSyncdPureLidSession = reader.bool();
                        message._isSyncdPureLidSession = "isSyncdPureLidSession";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.isSyncdSnapshotRecoveryEnabled = reader.bool();
                        message._isSyncdSnapshotRecoveryEnabled = "isSyncdSnapshotRecoveryEnabled";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.isHsThumbnailSyncEnabled = reader.bool();
                        message._isHsThumbnailSyncEnabled = "isHsThumbnailSyncEnabled";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.subscriptionSyncPayload = reader.bytes();
                        message._subscriptionSyncPayload = "subscriptionSyncPayload";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a ClientPairingProps message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ClientPairingProps
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ClientPairingProps} ClientPairingProps
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ClientPairingProps.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ClientPairingProps message.
         * @function verify
         * @memberof proto.ClientPairingProps
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ClientPairingProps.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.isChatDbLidMigrated != null && Object.hasOwnProperty.call(message, "isChatDbLidMigrated")) {
                properties._isChatDbLidMigrated = 1;
                if (typeof message.isChatDbLidMigrated !== "boolean")
                    return "isChatDbLidMigrated: boolean expected";
            }
            if (message.isSyncdPureLidSession != null && Object.hasOwnProperty.call(message, "isSyncdPureLidSession")) {
                properties._isSyncdPureLidSession = 1;
                if (typeof message.isSyncdPureLidSession !== "boolean")
                    return "isSyncdPureLidSession: boolean expected";
            }
            if (message.isSyncdSnapshotRecoveryEnabled != null && Object.hasOwnProperty.call(message, "isSyncdSnapshotRecoveryEnabled")) {
                properties._isSyncdSnapshotRecoveryEnabled = 1;
                if (typeof message.isSyncdSnapshotRecoveryEnabled !== "boolean")
                    return "isSyncdSnapshotRecoveryEnabled: boolean expected";
            }
            if (message.isHsThumbnailSyncEnabled != null && Object.hasOwnProperty.call(message, "isHsThumbnailSyncEnabled")) {
                properties._isHsThumbnailSyncEnabled = 1;
                if (typeof message.isHsThumbnailSyncEnabled !== "boolean")
                    return "isHsThumbnailSyncEnabled: boolean expected";
            }
            if (message.subscriptionSyncPayload != null && Object.hasOwnProperty.call(message, "subscriptionSyncPayload")) {
                properties._subscriptionSyncPayload = 1;
                if (!(message.subscriptionSyncPayload && typeof message.subscriptionSyncPayload.length === "number" || $util.isString(message.subscriptionSyncPayload)))
                    return "subscriptionSyncPayload: buffer expected";
            }
            return null;
        };

        /**
         * Creates a ClientPairingProps message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ClientPairingProps
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ClientPairingProps} ClientPairingProps
         */
        ClientPairingProps.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.ClientPairingProps: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.isChatDbLidMigrated != null)
                message.isChatDbLidMigrated = Boolean(object.isChatDbLidMigrated);
            if (object.isSyncdPureLidSession != null)
                message.isSyncdPureLidSession = Boolean(object.isSyncdPureLidSession);
            if (object.isSyncdSnapshotRecoveryEnabled != null)
                message.isSyncdSnapshotRecoveryEnabled = Boolean(object.isSyncdSnapshotRecoveryEnabled);
            if (object.isHsThumbnailSyncEnabled != null)
                message.isHsThumbnailSyncEnabled = Boolean(object.isHsThumbnailSyncEnabled);
            if (object.subscriptionSyncPayload != null)
                if (typeof object.subscriptionSyncPayload === "string")
                    $util.base64.decode(object.subscriptionSyncPayload, message.subscriptionSyncPayload = $util.newBuffer($util.base64.length(object.subscriptionSyncPayload)), 0);
                else if (object.subscriptionSyncPayload.length >= 0)
                    message.subscriptionSyncPayload = object.subscriptionSyncPayload;
            return message;
        };

        /**
         * Creates a plain object from a ClientPairingProps message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ClientPairingProps
         * @static
         * @param {proto.ClientPairingProps} message ClientPairingProps
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ClientPairingProps.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.isChatDbLidMigrated != null && Object.hasOwnProperty.call(message, "isChatDbLidMigrated"))
                object.isChatDbLidMigrated = message.isChatDbLidMigrated;
            if (message.isSyncdPureLidSession != null && Object.hasOwnProperty.call(message, "isSyncdPureLidSession"))
                object.isSyncdPureLidSession = message.isSyncdPureLidSession;
            if (message.isSyncdSnapshotRecoveryEnabled != null && Object.hasOwnProperty.call(message, "isSyncdSnapshotRecoveryEnabled"))
                object.isSyncdSnapshotRecoveryEnabled = message.isSyncdSnapshotRecoveryEnabled;
            if (message.isHsThumbnailSyncEnabled != null && Object.hasOwnProperty.call(message, "isHsThumbnailSyncEnabled"))
                object.isHsThumbnailSyncEnabled = message.isHsThumbnailSyncEnabled;
            if (message.subscriptionSyncPayload != null && Object.hasOwnProperty.call(message, "subscriptionSyncPayload"))
                object.subscriptionSyncPayload = options.bytes === String ? $util.base64.encode(message.subscriptionSyncPayload, 0, message.subscriptionSyncPayload.length) : options.bytes === Array ? Array.prototype.slice.call(message.subscriptionSyncPayload) : message.subscriptionSyncPayload;
            return object;
        };

        /**
         * Converts this ClientPairingProps to JSON.
         * @function toJSON
         * @memberof proto.ClientPairingProps
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ClientPairingProps.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ClientPairingProps
         * @function getTypeUrl
         * @memberof proto.ClientPairingProps
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ClientPairingProps.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.ClientPairingProps";
        };

        return ClientPairingProps;
    })();

    proto.ClientPayload = (function() {

        /**
         * Properties of a ClientPayload.
         * @memberof proto
         * @interface IClientPayload
         * @property {number|Long|null} [username] ClientPayload username
         * @property {boolean|null} [passive] ClientPayload passive
         * @property {proto.ClientPayload.IUserAgent|null} [userAgent] ClientPayload userAgent
         * @property {proto.ClientPayload.IWebInfo|null} [webInfo] ClientPayload webInfo
         * @property {string|null} [pushName] ClientPayload pushName
         * @property {number|null} [sessionId] ClientPayload sessionId
         * @property {boolean|null} [shortConnect] ClientPayload shortConnect
         * @property {proto.ClientPayload.ConnectType|null} [connectType] ClientPayload connectType
         * @property {proto.ClientPayload.ConnectReason|null} [connectReason] ClientPayload connectReason
         * @property {Array.<number>|null} [shards] ClientPayload shards
         * @property {proto.ClientPayload.IDNSSource|null} [dnsSource] ClientPayload dnsSource
         * @property {number|null} [connectAttemptCount] ClientPayload connectAttemptCount
         * @property {number|null} [device] ClientPayload device
         * @property {proto.ClientPayload.IDevicePairingRegistrationData|null} [devicePairingData] ClientPayload devicePairingData
         * @property {proto.ClientPayload.Product|null} [product] ClientPayload product
         * @property {Uint8Array|null} [fbCat] ClientPayload fbCat
         * @property {Uint8Array|null} [fbUserAgent] ClientPayload fbUserAgent
         * @property {boolean|null} [oc] ClientPayload oc
         * @property {number|null} [lc] ClientPayload lc
         * @property {proto.ClientPayload.IOSAppExtension|null} [iosAppExtension] ClientPayload iosAppExtension
         * @property {number|Long|null} [fbAppId] ClientPayload fbAppId
         * @property {Uint8Array|null} [fbDeviceId] ClientPayload fbDeviceId
         * @property {boolean|null} [pull] ClientPayload pull
         * @property {Uint8Array|null} [paddingBytes] ClientPayload paddingBytes
         * @property {number|null} [yearClass] ClientPayload yearClass
         * @property {number|null} [memClass] ClientPayload memClass
         * @property {proto.ClientPayload.IInteropData|null} [interopData] ClientPayload interopData
         * @property {proto.ClientPayload.TrafficAnonymization|null} [trafficAnonymization] ClientPayload trafficAnonymization
         * @property {boolean|null} [lidDbMigrated] ClientPayload lidDbMigrated
         * @property {proto.ClientPayload.AccountType|null} [accountType] ClientPayload accountType
         * @property {number|null} [connectionSequenceInfo] ClientPayload connectionSequenceInfo
         * @property {boolean|null} [paaLink] ClientPayload paaLink
         * @property {number|null} [preacksCount] ClientPayload preacksCount
         * @property {number|null} [processingQueueSize] ClientPayload processingQueueSize
         * @property {Array.<string>|null} [pairedPeripherals] ClientPayload pairedPeripherals
         * @property {Uint8Array|null} [testIsolationId] ClientPayload testIsolationId
         */

        /**
         * Constructs a new ClientPayload.
         * @memberof proto
         * @classdesc Represents a ClientPayload.
         * @implements IClientPayload
         * @constructor
         * @param {proto.IClientPayload=} [properties] Properties to set
         */
        function ClientPayload(properties) {
            this.shards = [];
            this.pairedPeripherals = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ClientPayload username.
         * @member {number|Long|null|undefined} username
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.username = null;

        /**
         * ClientPayload passive.
         * @member {boolean|null|undefined} passive
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.passive = null;

        /**
         * ClientPayload userAgent.
         * @member {proto.ClientPayload.IUserAgent|null|undefined} userAgent
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.userAgent = null;

        /**
         * ClientPayload webInfo.
         * @member {proto.ClientPayload.IWebInfo|null|undefined} webInfo
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.webInfo = null;

        /**
         * ClientPayload pushName.
         * @member {string|null|undefined} pushName
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.pushName = null;

        /**
         * ClientPayload sessionId.
         * @member {number|null|undefined} sessionId
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.sessionId = null;

        /**
         * ClientPayload shortConnect.
         * @member {boolean|null|undefined} shortConnect
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.shortConnect = null;

        /**
         * ClientPayload connectType.
         * @member {proto.ClientPayload.ConnectType|null|undefined} connectType
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.connectType = null;

        /**
         * ClientPayload connectReason.
         * @member {proto.ClientPayload.ConnectReason|null|undefined} connectReason
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.connectReason = null;

        /**
         * ClientPayload shards.
         * @member {Array.<number>} shards
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.shards = $util.emptyArray;

        /**
         * ClientPayload dnsSource.
         * @member {proto.ClientPayload.IDNSSource|null|undefined} dnsSource
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.dnsSource = null;

        /**
         * ClientPayload connectAttemptCount.
         * @member {number|null|undefined} connectAttemptCount
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.connectAttemptCount = null;

        /**
         * ClientPayload device.
         * @member {number|null|undefined} device
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.device = null;

        /**
         * ClientPayload devicePairingData.
         * @member {proto.ClientPayload.IDevicePairingRegistrationData|null|undefined} devicePairingData
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.devicePairingData = null;

        /**
         * ClientPayload product.
         * @member {proto.ClientPayload.Product|null|undefined} product
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.product = null;

        /**
         * ClientPayload fbCat.
         * @member {Uint8Array|null|undefined} fbCat
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.fbCat = null;

        /**
         * ClientPayload fbUserAgent.
         * @member {Uint8Array|null|undefined} fbUserAgent
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.fbUserAgent = null;

        /**
         * ClientPayload oc.
         * @member {boolean|null|undefined} oc
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.oc = null;

        /**
         * ClientPayload lc.
         * @member {number|null|undefined} lc
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.lc = null;

        /**
         * ClientPayload iosAppExtension.
         * @member {proto.ClientPayload.IOSAppExtension|null|undefined} iosAppExtension
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.iosAppExtension = null;

        /**
         * ClientPayload fbAppId.
         * @member {number|Long|null|undefined} fbAppId
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.fbAppId = null;

        /**
         * ClientPayload fbDeviceId.
         * @member {Uint8Array|null|undefined} fbDeviceId
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.fbDeviceId = null;

        /**
         * ClientPayload pull.
         * @member {boolean|null|undefined} pull
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.pull = null;

        /**
         * ClientPayload paddingBytes.
         * @member {Uint8Array|null|undefined} paddingBytes
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.paddingBytes = null;

        /**
         * ClientPayload yearClass.
         * @member {number|null|undefined} yearClass
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.yearClass = null;

        /**
         * ClientPayload memClass.
         * @member {number|null|undefined} memClass
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.memClass = null;

        /**
         * ClientPayload interopData.
         * @member {proto.ClientPayload.IInteropData|null|undefined} interopData
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.interopData = null;

        /**
         * ClientPayload trafficAnonymization.
         * @member {proto.ClientPayload.TrafficAnonymization|null|undefined} trafficAnonymization
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.trafficAnonymization = null;

        /**
         * ClientPayload lidDbMigrated.
         * @member {boolean|null|undefined} lidDbMigrated
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.lidDbMigrated = null;

        /**
         * ClientPayload accountType.
         * @member {proto.ClientPayload.AccountType|null|undefined} accountType
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.accountType = null;

        /**
         * ClientPayload connectionSequenceInfo.
         * @member {number|null|undefined} connectionSequenceInfo
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.connectionSequenceInfo = null;

        /**
         * ClientPayload paaLink.
         * @member {boolean|null|undefined} paaLink
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.paaLink = null;

        /**
         * ClientPayload preacksCount.
         * @member {number|null|undefined} preacksCount
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.preacksCount = null;

        /**
         * ClientPayload processingQueueSize.
         * @member {number|null|undefined} processingQueueSize
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.processingQueueSize = null;

        /**
         * ClientPayload pairedPeripherals.
         * @member {Array.<string>} pairedPeripherals
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.pairedPeripherals = $util.emptyArray;

        /**
         * ClientPayload testIsolationId.
         * @member {Uint8Array|null|undefined} testIsolationId
         * @memberof proto.ClientPayload
         * @instance
         */
        ClientPayload.prototype.testIsolationId = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_username", {
            get: $util.oneOfGetter($oneOfFields = ["username"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_passive", {
            get: $util.oneOfGetter($oneOfFields = ["passive"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_userAgent", {
            get: $util.oneOfGetter($oneOfFields = ["userAgent"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_webInfo", {
            get: $util.oneOfGetter($oneOfFields = ["webInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_pushName", {
            get: $util.oneOfGetter($oneOfFields = ["pushName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_sessionId", {
            get: $util.oneOfGetter($oneOfFields = ["sessionId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_shortConnect", {
            get: $util.oneOfGetter($oneOfFields = ["shortConnect"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_connectType", {
            get: $util.oneOfGetter($oneOfFields = ["connectType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_connectReason", {
            get: $util.oneOfGetter($oneOfFields = ["connectReason"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_dnsSource", {
            get: $util.oneOfGetter($oneOfFields = ["dnsSource"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_connectAttemptCount", {
            get: $util.oneOfGetter($oneOfFields = ["connectAttemptCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_device", {
            get: $util.oneOfGetter($oneOfFields = ["device"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_devicePairingData", {
            get: $util.oneOfGetter($oneOfFields = ["devicePairingData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_product", {
            get: $util.oneOfGetter($oneOfFields = ["product"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_fbCat", {
            get: $util.oneOfGetter($oneOfFields = ["fbCat"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_fbUserAgent", {
            get: $util.oneOfGetter($oneOfFields = ["fbUserAgent"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_oc", {
            get: $util.oneOfGetter($oneOfFields = ["oc"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_lc", {
            get: $util.oneOfGetter($oneOfFields = ["lc"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_iosAppExtension", {
            get: $util.oneOfGetter($oneOfFields = ["iosAppExtension"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_fbAppId", {
            get: $util.oneOfGetter($oneOfFields = ["fbAppId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_fbDeviceId", {
            get: $util.oneOfGetter($oneOfFields = ["fbDeviceId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_pull", {
            get: $util.oneOfGetter($oneOfFields = ["pull"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_paddingBytes", {
            get: $util.oneOfGetter($oneOfFields = ["paddingBytes"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_yearClass", {
            get: $util.oneOfGetter($oneOfFields = ["yearClass"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_memClass", {
            get: $util.oneOfGetter($oneOfFields = ["memClass"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_interopData", {
            get: $util.oneOfGetter($oneOfFields = ["interopData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_trafficAnonymization", {
            get: $util.oneOfGetter($oneOfFields = ["trafficAnonymization"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_lidDbMigrated", {
            get: $util.oneOfGetter($oneOfFields = ["lidDbMigrated"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_accountType", {
            get: $util.oneOfGetter($oneOfFields = ["accountType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_connectionSequenceInfo", {
            get: $util.oneOfGetter($oneOfFields = ["connectionSequenceInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_paaLink", {
            get: $util.oneOfGetter($oneOfFields = ["paaLink"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_preacksCount", {
            get: $util.oneOfGetter($oneOfFields = ["preacksCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_processingQueueSize", {
            get: $util.oneOfGetter($oneOfFields = ["processingQueueSize"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPayload.prototype, "_testIsolationId", {
            get: $util.oneOfGetter($oneOfFields = ["testIsolationId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ClientPayload instance using the specified properties.
         * @function create
         * @memberof proto.ClientPayload
         * @static
         * @param {proto.IClientPayload=} [properties] Properties to set
         * @returns {proto.ClientPayload} ClientPayload instance
         */
        ClientPayload.create = function create(properties) {
            return new ClientPayload(properties);
        };

        /**
         * Encodes the specified ClientPayload message. Does not implicitly {@link proto.ClientPayload.verify|verify} messages.
         * @function encode
         * @memberof proto.ClientPayload
         * @static
         * @param {proto.IClientPayload} message ClientPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ClientPayload.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.username);
            if (message.passive != null && Object.hasOwnProperty.call(message, "passive"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.passive);
            if (message.userAgent != null && Object.hasOwnProperty.call(message, "userAgent"))
                $root.proto.ClientPayload.UserAgent.encode(message.userAgent, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
            if (message.webInfo != null && Object.hasOwnProperty.call(message, "webInfo"))
                $root.proto.ClientPayload.WebInfo.encode(message.webInfo, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
            if (message.pushName != null && Object.hasOwnProperty.call(message, "pushName"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.pushName);
            if (message.sessionId != null && Object.hasOwnProperty.call(message, "sessionId"))
                writer.uint32(/* id 9, wireType 5 =*/77).sfixed32(message.sessionId);
            if (message.shortConnect != null && Object.hasOwnProperty.call(message, "shortConnect"))
                writer.uint32(/* id 10, wireType 0 =*/80).bool(message.shortConnect);
            if (message.connectType != null && Object.hasOwnProperty.call(message, "connectType"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.connectType);
            if (message.connectReason != null && Object.hasOwnProperty.call(message, "connectReason"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.connectReason);
            if (message.shards != null && message.shards.length) {
                writer.uint32(/* id 14, wireType 2 =*/114).fork();
                for (var i = 0; i < message.shards.length; ++i)
                    writer.int32(message.shards[i]);
                writer.ldelim();
            }
            if (message.dnsSource != null && Object.hasOwnProperty.call(message, "dnsSource"))
                $root.proto.ClientPayload.DNSSource.encode(message.dnsSource, writer.uint32(/* id 15, wireType 2 =*/122).fork(), q + 1).ldelim();
            if (message.connectAttemptCount != null && Object.hasOwnProperty.call(message, "connectAttemptCount"))
                writer.uint32(/* id 16, wireType 0 =*/128).uint32(message.connectAttemptCount);
            if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                writer.uint32(/* id 18, wireType 0 =*/144).uint32(message.device);
            if (message.devicePairingData != null && Object.hasOwnProperty.call(message, "devicePairingData"))
                $root.proto.ClientPayload.DevicePairingRegistrationData.encode(message.devicePairingData, writer.uint32(/* id 19, wireType 2 =*/154).fork(), q + 1).ldelim();
            if (message.product != null && Object.hasOwnProperty.call(message, "product"))
                writer.uint32(/* id 20, wireType 0 =*/160).int32(message.product);
            if (message.fbCat != null && Object.hasOwnProperty.call(message, "fbCat"))
                writer.uint32(/* id 21, wireType 2 =*/170).bytes(message.fbCat);
            if (message.fbUserAgent != null && Object.hasOwnProperty.call(message, "fbUserAgent"))
                writer.uint32(/* id 22, wireType 2 =*/178).bytes(message.fbUserAgent);
            if (message.oc != null && Object.hasOwnProperty.call(message, "oc"))
                writer.uint32(/* id 23, wireType 0 =*/184).bool(message.oc);
            if (message.lc != null && Object.hasOwnProperty.call(message, "lc"))
                writer.uint32(/* id 24, wireType 0 =*/192).int32(message.lc);
            if (message.iosAppExtension != null && Object.hasOwnProperty.call(message, "iosAppExtension"))
                writer.uint32(/* id 30, wireType 0 =*/240).int32(message.iosAppExtension);
            if (message.fbAppId != null && Object.hasOwnProperty.call(message, "fbAppId"))
                writer.uint32(/* id 31, wireType 0 =*/248).uint64(message.fbAppId);
            if (message.fbDeviceId != null && Object.hasOwnProperty.call(message, "fbDeviceId"))
                writer.uint32(/* id 32, wireType 2 =*/258).bytes(message.fbDeviceId);
            if (message.pull != null && Object.hasOwnProperty.call(message, "pull"))
                writer.uint32(/* id 33, wireType 0 =*/264).bool(message.pull);
            if (message.paddingBytes != null && Object.hasOwnProperty.call(message, "paddingBytes"))
                writer.uint32(/* id 34, wireType 2 =*/274).bytes(message.paddingBytes);
            if (message.yearClass != null && Object.hasOwnProperty.call(message, "yearClass"))
                writer.uint32(/* id 36, wireType 0 =*/288).int32(message.yearClass);
            if (message.memClass != null && Object.hasOwnProperty.call(message, "memClass"))
                writer.uint32(/* id 37, wireType 0 =*/296).int32(message.memClass);
            if (message.interopData != null && Object.hasOwnProperty.call(message, "interopData"))
                $root.proto.ClientPayload.InteropData.encode(message.interopData, writer.uint32(/* id 38, wireType 2 =*/306).fork(), q + 1).ldelim();
            if (message.trafficAnonymization != null && Object.hasOwnProperty.call(message, "trafficAnonymization"))
                writer.uint32(/* id 40, wireType 0 =*/320).int32(message.trafficAnonymization);
            if (message.lidDbMigrated != null && Object.hasOwnProperty.call(message, "lidDbMigrated"))
                writer.uint32(/* id 41, wireType 0 =*/328).bool(message.lidDbMigrated);
            if (message.accountType != null && Object.hasOwnProperty.call(message, "accountType"))
                writer.uint32(/* id 42, wireType 0 =*/336).int32(message.accountType);
            if (message.connectionSequenceInfo != null && Object.hasOwnProperty.call(message, "connectionSequenceInfo"))
                writer.uint32(/* id 43, wireType 5 =*/349).sfixed32(message.connectionSequenceInfo);
            if (message.paaLink != null && Object.hasOwnProperty.call(message, "paaLink"))
                writer.uint32(/* id 44, wireType 0 =*/352).bool(message.paaLink);
            if (message.preacksCount != null && Object.hasOwnProperty.call(message, "preacksCount"))
                writer.uint32(/* id 45, wireType 0 =*/360).int32(message.preacksCount);
            if (message.processingQueueSize != null && Object.hasOwnProperty.call(message, "processingQueueSize"))
                writer.uint32(/* id 46, wireType 0 =*/368).int32(message.processingQueueSize);
            if (message.pairedPeripherals != null && message.pairedPeripherals.length)
                for (var i = 0; i < message.pairedPeripherals.length; ++i)
                    writer.uint32(/* id 47, wireType 2 =*/378).string(message.pairedPeripherals[i]);
            if (message.testIsolationId != null && Object.hasOwnProperty.call(message, "testIsolationId"))
                writer.uint32(/* id 48, wireType 2 =*/386).bytes(message.testIsolationId);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ClientPayload message, length delimited. Does not implicitly {@link proto.ClientPayload.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ClientPayload
         * @static
         * @param {proto.IClientPayload} message ClientPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ClientPayload.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ClientPayload message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ClientPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ClientPayload} ClientPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ClientPayload.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.username = reader.uint64();
                        message._username = "username";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.passive = reader.bool();
                        message._passive = "passive";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.userAgent = $root.proto.ClientPayload.UserAgent.decode(reader, reader.uint32(), undefined, q + 1, message.userAgent);
                        message._userAgent = "userAgent";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        message.webInfo = $root.proto.ClientPayload.WebInfo.decode(reader, reader.uint32(), undefined, q + 1, message.webInfo);
                        message._webInfo = "webInfo";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        message.pushName = reader.stringVerify();
                        message._pushName = "pushName";
                        continue;
                    }
                case 9: {
                        if (u !== 5)
                            break;
                        message.sessionId = reader.sfixed32();
                        message._sessionId = "sessionId";
                        continue;
                    }
                case 10: {
                        if (u !== 0)
                            break;
                        message.shortConnect = reader.bool();
                        message._shortConnect = "shortConnect";
                        continue;
                    }
                case 12: {
                        if (u !== 0)
                            break;
                        message.connectType = reader.int32();
                        message._connectType = "connectType";
                        continue;
                    }
                case 13: {
                        if (u !== 0)
                            break;
                        message.connectReason = reader.int32();
                        message._connectReason = "connectReason";
                        continue;
                    }
                case 14: {
                        if (u === 2) {
                            if (!(message.shards && message.shards.length))
                                message.shards = [];
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.shards.push(reader.int32());
                            continue;
                        }
                        if (u !== 0)
                            break;
                        if (!(message.shards && message.shards.length))
                            message.shards = [];
                        message.shards.push(reader.int32());
                        continue;
                    }
                case 15: {
                        if (u !== 2)
                            break;
                        message.dnsSource = $root.proto.ClientPayload.DNSSource.decode(reader, reader.uint32(), undefined, q + 1, message.dnsSource);
                        message._dnsSource = "dnsSource";
                        continue;
                    }
                case 16: {
                        if (u !== 0)
                            break;
                        message.connectAttemptCount = reader.uint32();
                        message._connectAttemptCount = "connectAttemptCount";
                        continue;
                    }
                case 18: {
                        if (u !== 0)
                            break;
                        message.device = reader.uint32();
                        message._device = "device";
                        continue;
                    }
                case 19: {
                        if (u !== 2)
                            break;
                        message.devicePairingData = $root.proto.ClientPayload.DevicePairingRegistrationData.decode(reader, reader.uint32(), undefined, q + 1, message.devicePairingData);
                        message._devicePairingData = "devicePairingData";
                        continue;
                    }
                case 20: {
                        if (u !== 0)
                            break;
                        message.product = reader.int32();
                        message._product = "product";
                        continue;
                    }
                case 21: {
                        if (u !== 2)
                            break;
                        message.fbCat = reader.bytes();
                        message._fbCat = "fbCat";
                        continue;
                    }
                case 22: {
                        if (u !== 2)
                            break;
                        message.fbUserAgent = reader.bytes();
                        message._fbUserAgent = "fbUserAgent";
                        continue;
                    }
                case 23: {
                        if (u !== 0)
                            break;
                        message.oc = reader.bool();
                        message._oc = "oc";
                        continue;
                    }
                case 24: {
                        if (u !== 0)
                            break;
                        message.lc = reader.int32();
                        message._lc = "lc";
                        continue;
                    }
                case 30: {
                        if (u !== 0)
                            break;
                        message.iosAppExtension = reader.int32();
                        message._iosAppExtension = "iosAppExtension";
                        continue;
                    }
                case 31: {
                        if (u !== 0)
                            break;
                        message.fbAppId = reader.uint64();
                        message._fbAppId = "fbAppId";
                        continue;
                    }
                case 32: {
                        if (u !== 2)
                            break;
                        message.fbDeviceId = reader.bytes();
                        message._fbDeviceId = "fbDeviceId";
                        continue;
                    }
                case 33: {
                        if (u !== 0)
                            break;
                        message.pull = reader.bool();
                        message._pull = "pull";
                        continue;
                    }
                case 34: {
                        if (u !== 2)
                            break;
                        message.paddingBytes = reader.bytes();
                        message._paddingBytes = "paddingBytes";
                        continue;
                    }
                case 36: {
                        if (u !== 0)
                            break;
                        message.yearClass = reader.int32();
                        message._yearClass = "yearClass";
                        continue;
                    }
                case 37: {
                        if (u !== 0)
                            break;
                        message.memClass = reader.int32();
                        message._memClass = "memClass";
                        continue;
                    }
                case 38: {
                        if (u !== 2)
                            break;
                        message.interopData = $root.proto.ClientPayload.InteropData.decode(reader, reader.uint32(), undefined, q + 1, message.interopData);
                        message._interopData = "interopData";
                        continue;
                    }
                case 40: {
                        if (u !== 0)
                            break;
                        message.trafficAnonymization = reader.int32();
                        message._trafficAnonymization = "trafficAnonymization";
                        continue;
                    }
                case 41: {
                        if (u !== 0)
                            break;
                        message.lidDbMigrated = reader.bool();
                        message._lidDbMigrated = "lidDbMigrated";
                        continue;
                    }
                case 42: {
                        if (u !== 0)
                            break;
                        message.accountType = reader.int32();
                        message._accountType = "accountType";
                        continue;
                    }
                case 43: {
                        if (u !== 5)
                            break;
                        message.connectionSequenceInfo = reader.sfixed32();
                        message._connectionSequenceInfo = "connectionSequenceInfo";
                        continue;
                    }
                case 44: {
                        if (u !== 0)
                            break;
                        message.paaLink = reader.bool();
                        message._paaLink = "paaLink";
                        continue;
                    }
                case 45: {
                        if (u !== 0)
                            break;
                        message.preacksCount = reader.int32();
                        message._preacksCount = "preacksCount";
                        continue;
                    }
                case 46: {
                        if (u !== 0)
                            break;
                        message.processingQueueSize = reader.int32();
                        message._processingQueueSize = "processingQueueSize";
                        continue;
                    }
                case 47: {
                        if (u !== 2)
                            break;
                        if (!(message.pairedPeripherals && message.pairedPeripherals.length))
                            message.pairedPeripherals = [];
                        message.pairedPeripherals.push(reader.stringVerify());
                        continue;
                    }
                case 48: {
                        if (u !== 2)
                            break;
                        message.testIsolationId = reader.bytes();
                        message._testIsolationId = "testIsolationId";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a ClientPayload message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ClientPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ClientPayload} ClientPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ClientPayload.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ClientPayload message.
         * @function verify
         * @memberof proto.ClientPayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ClientPayload.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.username != null && Object.hasOwnProperty.call(message, "username")) {
                properties._username = 1;
                if (!$util.isInteger(message.username) && !(message.username && $util.isInteger(message.username.low) && $util.isInteger(message.username.high)))
                    return "username: integer|Long expected";
            }
            if (message.passive != null && Object.hasOwnProperty.call(message, "passive")) {
                properties._passive = 1;
                if (typeof message.passive !== "boolean")
                    return "passive: boolean expected";
            }
            if (message.userAgent != null && Object.hasOwnProperty.call(message, "userAgent")) {
                properties._userAgent = 1;
                {
                    var error = $root.proto.ClientPayload.UserAgent.verify(message.userAgent, q + 1);
                    if (error)
                        return "userAgent." + error;
                }
            }
            if (message.webInfo != null && Object.hasOwnProperty.call(message, "webInfo")) {
                properties._webInfo = 1;
                {
                    var error = $root.proto.ClientPayload.WebInfo.verify(message.webInfo, q + 1);
                    if (error)
                        return "webInfo." + error;
                }
            }
            if (message.pushName != null && Object.hasOwnProperty.call(message, "pushName")) {
                properties._pushName = 1;
                if (!$util.isString(message.pushName))
                    return "pushName: string expected";
            }
            if (message.sessionId != null && Object.hasOwnProperty.call(message, "sessionId")) {
                properties._sessionId = 1;
                if (!$util.isInteger(message.sessionId))
                    return "sessionId: integer expected";
            }
            if (message.shortConnect != null && Object.hasOwnProperty.call(message, "shortConnect")) {
                properties._shortConnect = 1;
                if (typeof message.shortConnect !== "boolean")
                    return "shortConnect: boolean expected";
            }
            if (message.connectType != null && Object.hasOwnProperty.call(message, "connectType")) {
                properties._connectType = 1;
                if (typeof message.connectType !== "number" || (message.connectType | 0) !== message.connectType)
                    return "connectType: enum value expected";
            }
            if (message.connectReason != null && Object.hasOwnProperty.call(message, "connectReason")) {
                properties._connectReason = 1;
                if (typeof message.connectReason !== "number" || (message.connectReason | 0) !== message.connectReason)
                    return "connectReason: enum value expected";
            }
            if (message.shards != null && Object.hasOwnProperty.call(message, "shards")) {
                if (!Array.isArray(message.shards))
                    return "shards: array expected";
                for (var i = 0; i < message.shards.length; ++i)
                    if (!$util.isInteger(message.shards[i]))
                        return "shards: integer[] expected";
            }
            if (message.dnsSource != null && Object.hasOwnProperty.call(message, "dnsSource")) {
                properties._dnsSource = 1;
                {
                    var error = $root.proto.ClientPayload.DNSSource.verify(message.dnsSource, q + 1);
                    if (error)
                        return "dnsSource." + error;
                }
            }
            if (message.connectAttemptCount != null && Object.hasOwnProperty.call(message, "connectAttemptCount")) {
                properties._connectAttemptCount = 1;
                if (!$util.isInteger(message.connectAttemptCount))
                    return "connectAttemptCount: integer expected";
            }
            if (message.device != null && Object.hasOwnProperty.call(message, "device")) {
                properties._device = 1;
                if (!$util.isInteger(message.device))
                    return "device: integer expected";
            }
            if (message.devicePairingData != null && Object.hasOwnProperty.call(message, "devicePairingData")) {
                properties._devicePairingData = 1;
                {
                    var error = $root.proto.ClientPayload.DevicePairingRegistrationData.verify(message.devicePairingData, q + 1);
                    if (error)
                        return "devicePairingData." + error;
                }
            }
            if (message.product != null && Object.hasOwnProperty.call(message, "product")) {
                properties._product = 1;
                if (typeof message.product !== "number" || (message.product | 0) !== message.product)
                    return "product: enum value expected";
            }
            if (message.fbCat != null && Object.hasOwnProperty.call(message, "fbCat")) {
                properties._fbCat = 1;
                if (!(message.fbCat && typeof message.fbCat.length === "number" || $util.isString(message.fbCat)))
                    return "fbCat: buffer expected";
            }
            if (message.fbUserAgent != null && Object.hasOwnProperty.call(message, "fbUserAgent")) {
                properties._fbUserAgent = 1;
                if (!(message.fbUserAgent && typeof message.fbUserAgent.length === "number" || $util.isString(message.fbUserAgent)))
                    return "fbUserAgent: buffer expected";
            }
            if (message.oc != null && Object.hasOwnProperty.call(message, "oc")) {
                properties._oc = 1;
                if (typeof message.oc !== "boolean")
                    return "oc: boolean expected";
            }
            if (message.lc != null && Object.hasOwnProperty.call(message, "lc")) {
                properties._lc = 1;
                if (!$util.isInteger(message.lc))
                    return "lc: integer expected";
            }
            if (message.iosAppExtension != null && Object.hasOwnProperty.call(message, "iosAppExtension")) {
                properties._iosAppExtension = 1;
                if (typeof message.iosAppExtension !== "number" || (message.iosAppExtension | 0) !== message.iosAppExtension)
                    return "iosAppExtension: enum value expected";
            }
            if (message.fbAppId != null && Object.hasOwnProperty.call(message, "fbAppId")) {
                properties._fbAppId = 1;
                if (!$util.isInteger(message.fbAppId) && !(message.fbAppId && $util.isInteger(message.fbAppId.low) && $util.isInteger(message.fbAppId.high)))
                    return "fbAppId: integer|Long expected";
            }
            if (message.fbDeviceId != null && Object.hasOwnProperty.call(message, "fbDeviceId")) {
                properties._fbDeviceId = 1;
                if (!(message.fbDeviceId && typeof message.fbDeviceId.length === "number" || $util.isString(message.fbDeviceId)))
                    return "fbDeviceId: buffer expected";
            }
            if (message.pull != null && Object.hasOwnProperty.call(message, "pull")) {
                properties._pull = 1;
                if (typeof message.pull !== "boolean")
                    return "pull: boolean expected";
            }
            if (message.paddingBytes != null && Object.hasOwnProperty.call(message, "paddingBytes")) {
                properties._paddingBytes = 1;
                if (!(message.paddingBytes && typeof message.paddingBytes.length === "number" || $util.isString(message.paddingBytes)))
                    return "paddingBytes: buffer expected";
            }
            if (message.yearClass != null && Object.hasOwnProperty.call(message, "yearClass")) {
                properties._yearClass = 1;
                if (!$util.isInteger(message.yearClass))
                    return "yearClass: integer expected";
            }
            if (message.memClass != null && Object.hasOwnProperty.call(message, "memClass")) {
                properties._memClass = 1;
                if (!$util.isInteger(message.memClass))
                    return "memClass: integer expected";
            }
            if (message.interopData != null && Object.hasOwnProperty.call(message, "interopData")) {
                properties._interopData = 1;
                {
                    var error = $root.proto.ClientPayload.InteropData.verify(message.interopData, q + 1);
                    if (error)
                        return "interopData." + error;
                }
            }
            if (message.trafficAnonymization != null && Object.hasOwnProperty.call(message, "trafficAnonymization")) {
                properties._trafficAnonymization = 1;
                if (typeof message.trafficAnonymization !== "number" || (message.trafficAnonymization | 0) !== message.trafficAnonymization)
                    return "trafficAnonymization: enum value expected";
            }
            if (message.lidDbMigrated != null && Object.hasOwnProperty.call(message, "lidDbMigrated")) {
                properties._lidDbMigrated = 1;
                if (typeof message.lidDbMigrated !== "boolean")
                    return "lidDbMigrated: boolean expected";
            }
            if (message.accountType != null && Object.hasOwnProperty.call(message, "accountType")) {
                properties._accountType = 1;
                if (typeof message.accountType !== "number" || (message.accountType | 0) !== message.accountType)
                    return "accountType: enum value expected";
            }
            if (message.connectionSequenceInfo != null && Object.hasOwnProperty.call(message, "connectionSequenceInfo")) {
                properties._connectionSequenceInfo = 1;
                if (!$util.isInteger(message.connectionSequenceInfo))
                    return "connectionSequenceInfo: integer expected";
            }
            if (message.paaLink != null && Object.hasOwnProperty.call(message, "paaLink")) {
                properties._paaLink = 1;
                if (typeof message.paaLink !== "boolean")
                    return "paaLink: boolean expected";
            }
            if (message.preacksCount != null && Object.hasOwnProperty.call(message, "preacksCount")) {
                properties._preacksCount = 1;
                if (!$util.isInteger(message.preacksCount))
                    return "preacksCount: integer expected";
            }
            if (message.processingQueueSize != null && Object.hasOwnProperty.call(message, "processingQueueSize")) {
                properties._processingQueueSize = 1;
                if (!$util.isInteger(message.processingQueueSize))
                    return "processingQueueSize: integer expected";
            }
            if (message.pairedPeripherals != null && Object.hasOwnProperty.call(message, "pairedPeripherals")) {
                if (!Array.isArray(message.pairedPeripherals))
                    return "pairedPeripherals: array expected";
                for (var i = 0; i < message.pairedPeripherals.length; ++i)
                    if (!$util.isString(message.pairedPeripherals[i]))
                        return "pairedPeripherals: string[] expected";
            }
            if (message.testIsolationId != null && Object.hasOwnProperty.call(message, "testIsolationId")) {
                properties._testIsolationId = 1;
                if (!(message.testIsolationId && typeof message.testIsolationId.length === "number" || $util.isString(message.testIsolationId)))
                    return "testIsolationId: buffer expected";
            }
            return null;
        };

        /**
         * Creates a ClientPayload message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ClientPayload
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ClientPayload} ClientPayload
         */
        ClientPayload.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.ClientPayload: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.username != null)
                if ($util.Long)
                    message.username = $util.Long.fromValue(object.username, true);
                else if (typeof object.username === "string")
                    message.username = parseInt(object.username, 10);
                else if (typeof object.username === "number")
                    message.username = object.username;
                else if (typeof object.username === "object")
                    message.username = new $util.LongBits(object.username.low >>> 0, object.username.high >>> 0).toNumber(true);
            if (object.passive != null)
                message.passive = Boolean(object.passive);
            if (object.userAgent != null) {
                if (!$util.isObject(object.userAgent))
                    throw TypeError(".proto.ClientPayload.userAgent: object expected");
                message.userAgent = $root.proto.ClientPayload.UserAgent.fromObject(object.userAgent, q + 1);
            }
            if (object.webInfo != null) {
                if (!$util.isObject(object.webInfo))
                    throw TypeError(".proto.ClientPayload.webInfo: object expected");
                message.webInfo = $root.proto.ClientPayload.WebInfo.fromObject(object.webInfo, q + 1);
            }
            if (object.pushName != null)
                message.pushName = String(object.pushName);
            if (object.sessionId != null)
                message.sessionId = object.sessionId | 0;
            if (object.shortConnect != null)
                message.shortConnect = Boolean(object.shortConnect);
            switch (object.connectType) {
            case "CELLULAR_UNKNOWN":
            case 0:
                message.connectType = 0;
                break;
            case "WIFI_UNKNOWN":
            case 1:
                message.connectType = 1;
                break;
            case "CELLULAR_EDGE":
            case 100:
                message.connectType = 100;
                break;
            case "CELLULAR_IDEN":
            case 101:
                message.connectType = 101;
                break;
            case "CELLULAR_UMTS":
            case 102:
                message.connectType = 102;
                break;
            case "CELLULAR_EVDO":
            case 103:
                message.connectType = 103;
                break;
            case "CELLULAR_GPRS":
            case 104:
                message.connectType = 104;
                break;
            case "CELLULAR_HSDPA":
            case 105:
                message.connectType = 105;
                break;
            case "CELLULAR_HSUPA":
            case 106:
                message.connectType = 106;
                break;
            case "CELLULAR_HSPA":
            case 107:
                message.connectType = 107;
                break;
            case "CELLULAR_CDMA":
            case 108:
                message.connectType = 108;
                break;
            case "CELLULAR_1XRTT":
            case 109:
                message.connectType = 109;
                break;
            case "CELLULAR_EHRPD":
            case 110:
                message.connectType = 110;
                break;
            case "CELLULAR_LTE":
            case 111:
                message.connectType = 111;
                break;
            case "CELLULAR_HSPAP":
            case 112:
                message.connectType = 112;
                break;
            default:
                if (typeof object.connectType === "number" && (object.connectType | 0) === object.connectType)
                    message.connectType = object.connectType;
            }
            switch (object.connectReason) {
            case "PUSH":
            case 0:
                message.connectReason = 0;
                break;
            case "USER_ACTIVATED":
            case 1:
                message.connectReason = 1;
                break;
            case "SCHEDULED":
            case 2:
                message.connectReason = 2;
                break;
            case "ERROR_RECONNECT":
            case 3:
                message.connectReason = 3;
                break;
            case "NETWORK_SWITCH":
            case 4:
                message.connectReason = 4;
                break;
            case "PING_RECONNECT":
            case 5:
                message.connectReason = 5;
                break;
            case "UNKNOWN":
            case 6:
                message.connectReason = 6;
                break;
            default:
                if (typeof object.connectReason === "number" && (object.connectReason | 0) === object.connectReason)
                    message.connectReason = object.connectReason;
            }
            if (object.shards) {
                if (!Array.isArray(object.shards))
                    throw TypeError(".proto.ClientPayload.shards: array expected");
                message.shards = Array(object.shards.length);
                for (var i = 0; i < object.shards.length; ++i)
                    message.shards[i] = object.shards[i] | 0;
            }
            if (object.dnsSource != null) {
                if (!$util.isObject(object.dnsSource))
                    throw TypeError(".proto.ClientPayload.dnsSource: object expected");
                message.dnsSource = $root.proto.ClientPayload.DNSSource.fromObject(object.dnsSource, q + 1);
            }
            if (object.connectAttemptCount != null)
                message.connectAttemptCount = object.connectAttemptCount >>> 0;
            if (object.device != null)
                message.device = object.device >>> 0;
            if (object.devicePairingData != null) {
                if (!$util.isObject(object.devicePairingData))
                    throw TypeError(".proto.ClientPayload.devicePairingData: object expected");
                message.devicePairingData = $root.proto.ClientPayload.DevicePairingRegistrationData.fromObject(object.devicePairingData, q + 1);
            }
            switch (object.product) {
            case "WHATSAPP":
            case 0:
                message.product = 0;
                break;
            case "MESSENGER":
            case 1:
                message.product = 1;
                break;
            case "INTEROP":
            case 2:
                message.product = 2;
                break;
            case "INTEROP_MSGR":
            case 3:
                message.product = 3;
                break;
            case "WHATSAPP_LID":
            case 4:
                message.product = 4;
                break;
            default:
                if (typeof object.product === "number" && (object.product | 0) === object.product)
                    message.product = object.product;
            }
            if (object.fbCat != null)
                if (typeof object.fbCat === "string")
                    $util.base64.decode(object.fbCat, message.fbCat = $util.newBuffer($util.base64.length(object.fbCat)), 0);
                else if (object.fbCat.length >= 0)
                    message.fbCat = object.fbCat;
            if (object.fbUserAgent != null)
                if (typeof object.fbUserAgent === "string")
                    $util.base64.decode(object.fbUserAgent, message.fbUserAgent = $util.newBuffer($util.base64.length(object.fbUserAgent)), 0);
                else if (object.fbUserAgent.length >= 0)
                    message.fbUserAgent = object.fbUserAgent;
            if (object.oc != null)
                message.oc = Boolean(object.oc);
            if (object.lc != null)
                message.lc = object.lc | 0;
            switch (object.iosAppExtension) {
            case "SHARE_EXTENSION":
            case 0:
                message.iosAppExtension = 0;
                break;
            case "SERVICE_EXTENSION":
            case 1:
                message.iosAppExtension = 1;
                break;
            case "INTENTS_EXTENSION":
            case 2:
                message.iosAppExtension = 2;
                break;
            default:
                if (typeof object.iosAppExtension === "number" && (object.iosAppExtension | 0) === object.iosAppExtension)
                    message.iosAppExtension = object.iosAppExtension;
            }
            if (object.fbAppId != null)
                if ($util.Long)
                    message.fbAppId = $util.Long.fromValue(object.fbAppId, true);
                else if (typeof object.fbAppId === "string")
                    message.fbAppId = parseInt(object.fbAppId, 10);
                else if (typeof object.fbAppId === "number")
                    message.fbAppId = object.fbAppId;
                else if (typeof object.fbAppId === "object")
                    message.fbAppId = new $util.LongBits(object.fbAppId.low >>> 0, object.fbAppId.high >>> 0).toNumber(true);
            if (object.fbDeviceId != null)
                if (typeof object.fbDeviceId === "string")
                    $util.base64.decode(object.fbDeviceId, message.fbDeviceId = $util.newBuffer($util.base64.length(object.fbDeviceId)), 0);
                else if (object.fbDeviceId.length >= 0)
                    message.fbDeviceId = object.fbDeviceId;
            if (object.pull != null)
                message.pull = Boolean(object.pull);
            if (object.paddingBytes != null)
                if (typeof object.paddingBytes === "string")
                    $util.base64.decode(object.paddingBytes, message.paddingBytes = $util.newBuffer($util.base64.length(object.paddingBytes)), 0);
                else if (object.paddingBytes.length >= 0)
                    message.paddingBytes = object.paddingBytes;
            if (object.yearClass != null)
                message.yearClass = object.yearClass | 0;
            if (object.memClass != null)
                message.memClass = object.memClass | 0;
            if (object.interopData != null) {
                if (!$util.isObject(object.interopData))
                    throw TypeError(".proto.ClientPayload.interopData: object expected");
                message.interopData = $root.proto.ClientPayload.InteropData.fromObject(object.interopData, q + 1);
            }
            switch (object.trafficAnonymization) {
            case "OFF":
            case 0:
                message.trafficAnonymization = 0;
                break;
            case "STANDARD":
            case 1:
                message.trafficAnonymization = 1;
                break;
            default:
                if (typeof object.trafficAnonymization === "number" && (object.trafficAnonymization | 0) === object.trafficAnonymization)
                    message.trafficAnonymization = object.trafficAnonymization;
            }
            if (object.lidDbMigrated != null)
                message.lidDbMigrated = Boolean(object.lidDbMigrated);
            switch (object.accountType) {
            case "DEFAULT":
            case 0:
                message.accountType = 0;
                break;
            case "GUEST":
            case 1:
                message.accountType = 1;
                break;
            default:
                if (typeof object.accountType === "number" && (object.accountType | 0) === object.accountType)
                    message.accountType = object.accountType;
            }
            if (object.connectionSequenceInfo != null)
                message.connectionSequenceInfo = object.connectionSequenceInfo | 0;
            if (object.paaLink != null)
                message.paaLink = Boolean(object.paaLink);
            if (object.preacksCount != null)
                message.preacksCount = object.preacksCount | 0;
            if (object.processingQueueSize != null)
                message.processingQueueSize = object.processingQueueSize | 0;
            if (object.pairedPeripherals) {
                if (!Array.isArray(object.pairedPeripherals))
                    throw TypeError(".proto.ClientPayload.pairedPeripherals: array expected");
                message.pairedPeripherals = Array(object.pairedPeripherals.length);
                for (var i = 0; i < object.pairedPeripherals.length; ++i)
                    message.pairedPeripherals[i] = String(object.pairedPeripherals[i]);
            }
            if (object.testIsolationId != null)
                if (typeof object.testIsolationId === "string")
                    $util.base64.decode(object.testIsolationId, message.testIsolationId = $util.newBuffer($util.base64.length(object.testIsolationId)), 0);
                else if (object.testIsolationId.length >= 0)
                    message.testIsolationId = object.testIsolationId;
            return message;
        };

        /**
         * Creates a plain object from a ClientPayload message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ClientPayload
         * @static
         * @param {proto.ClientPayload} message ClientPayload
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ClientPayload.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults) {
                object.shards = [];
                object.pairedPeripherals = [];
            }
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.username = typeof message.username === "number" ? BigInt(message.username) : $util.Long.fromBits(message.username.low >>> 0, message.username.high >>> 0, true).toBigInt();
                else if (typeof message.username === "number")
                    object.username = options.longs === String ? String(message.username) : message.username;
                else
                    object.username = options.longs === String ? $util.Long.prototype.toString.call(message.username) : options.longs === Number ? new $util.LongBits(message.username.low >>> 0, message.username.high >>> 0).toNumber(true) : message.username;
            if (message.passive != null && Object.hasOwnProperty.call(message, "passive"))
                object.passive = message.passive;
            if (message.userAgent != null && Object.hasOwnProperty.call(message, "userAgent"))
                object.userAgent = $root.proto.ClientPayload.UserAgent.toObject(message.userAgent, options, q + 1);
            if (message.webInfo != null && Object.hasOwnProperty.call(message, "webInfo"))
                object.webInfo = $root.proto.ClientPayload.WebInfo.toObject(message.webInfo, options, q + 1);
            if (message.pushName != null && Object.hasOwnProperty.call(message, "pushName"))
                object.pushName = message.pushName;
            if (message.sessionId != null && Object.hasOwnProperty.call(message, "sessionId"))
                object.sessionId = message.sessionId;
            if (message.shortConnect != null && Object.hasOwnProperty.call(message, "shortConnect"))
                object.shortConnect = message.shortConnect;
            if (message.connectType != null && Object.hasOwnProperty.call(message, "connectType"))
                object.connectType = options.enums === String ? $root.proto.ClientPayload.ConnectType[message.connectType] === undefined ? message.connectType : $root.proto.ClientPayload.ConnectType[message.connectType] : message.connectType;
            if (message.connectReason != null && Object.hasOwnProperty.call(message, "connectReason"))
                object.connectReason = options.enums === String ? $root.proto.ClientPayload.ConnectReason[message.connectReason] === undefined ? message.connectReason : $root.proto.ClientPayload.ConnectReason[message.connectReason] : message.connectReason;
            if (message.shards && message.shards.length) {
                object.shards = Array(message.shards.length);
                for (var j = 0; j < message.shards.length; ++j)
                    object.shards[j] = message.shards[j];
            }
            if (message.dnsSource != null && Object.hasOwnProperty.call(message, "dnsSource"))
                object.dnsSource = $root.proto.ClientPayload.DNSSource.toObject(message.dnsSource, options, q + 1);
            if (message.connectAttemptCount != null && Object.hasOwnProperty.call(message, "connectAttemptCount"))
                object.connectAttemptCount = message.connectAttemptCount;
            if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                object.device = message.device;
            if (message.devicePairingData != null && Object.hasOwnProperty.call(message, "devicePairingData"))
                object.devicePairingData = $root.proto.ClientPayload.DevicePairingRegistrationData.toObject(message.devicePairingData, options, q + 1);
            if (message.product != null && Object.hasOwnProperty.call(message, "product"))
                object.product = options.enums === String ? $root.proto.ClientPayload.Product[message.product] === undefined ? message.product : $root.proto.ClientPayload.Product[message.product] : message.product;
            if (message.fbCat != null && Object.hasOwnProperty.call(message, "fbCat"))
                object.fbCat = options.bytes === String ? $util.base64.encode(message.fbCat, 0, message.fbCat.length) : options.bytes === Array ? Array.prototype.slice.call(message.fbCat) : message.fbCat;
            if (message.fbUserAgent != null && Object.hasOwnProperty.call(message, "fbUserAgent"))
                object.fbUserAgent = options.bytes === String ? $util.base64.encode(message.fbUserAgent, 0, message.fbUserAgent.length) : options.bytes === Array ? Array.prototype.slice.call(message.fbUserAgent) : message.fbUserAgent;
            if (message.oc != null && Object.hasOwnProperty.call(message, "oc"))
                object.oc = message.oc;
            if (message.lc != null && Object.hasOwnProperty.call(message, "lc"))
                object.lc = message.lc;
            if (message.iosAppExtension != null && Object.hasOwnProperty.call(message, "iosAppExtension"))
                object.iosAppExtension = options.enums === String ? $root.proto.ClientPayload.IOSAppExtension[message.iosAppExtension] === undefined ? message.iosAppExtension : $root.proto.ClientPayload.IOSAppExtension[message.iosAppExtension] : message.iosAppExtension;
            if (message.fbAppId != null && Object.hasOwnProperty.call(message, "fbAppId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.fbAppId = typeof message.fbAppId === "number" ? BigInt(message.fbAppId) : $util.Long.fromBits(message.fbAppId.low >>> 0, message.fbAppId.high >>> 0, true).toBigInt();
                else if (typeof message.fbAppId === "number")
                    object.fbAppId = options.longs === String ? String(message.fbAppId) : message.fbAppId;
                else
                    object.fbAppId = options.longs === String ? $util.Long.prototype.toString.call(message.fbAppId) : options.longs === Number ? new $util.LongBits(message.fbAppId.low >>> 0, message.fbAppId.high >>> 0).toNumber(true) : message.fbAppId;
            if (message.fbDeviceId != null && Object.hasOwnProperty.call(message, "fbDeviceId"))
                object.fbDeviceId = options.bytes === String ? $util.base64.encode(message.fbDeviceId, 0, message.fbDeviceId.length) : options.bytes === Array ? Array.prototype.slice.call(message.fbDeviceId) : message.fbDeviceId;
            if (message.pull != null && Object.hasOwnProperty.call(message, "pull"))
                object.pull = message.pull;
            if (message.paddingBytes != null && Object.hasOwnProperty.call(message, "paddingBytes"))
                object.paddingBytes = options.bytes === String ? $util.base64.encode(message.paddingBytes, 0, message.paddingBytes.length) : options.bytes === Array ? Array.prototype.slice.call(message.paddingBytes) : message.paddingBytes;
            if (message.yearClass != null && Object.hasOwnProperty.call(message, "yearClass"))
                object.yearClass = message.yearClass;
            if (message.memClass != null && Object.hasOwnProperty.call(message, "memClass"))
                object.memClass = message.memClass;
            if (message.interopData != null && Object.hasOwnProperty.call(message, "interopData"))
                object.interopData = $root.proto.ClientPayload.InteropData.toObject(message.interopData, options, q + 1);
            if (message.trafficAnonymization != null && Object.hasOwnProperty.call(message, "trafficAnonymization"))
                object.trafficAnonymization = options.enums === String ? $root.proto.ClientPayload.TrafficAnonymization[message.trafficAnonymization] === undefined ? message.trafficAnonymization : $root.proto.ClientPayload.TrafficAnonymization[message.trafficAnonymization] : message.trafficAnonymization;
            if (message.lidDbMigrated != null && Object.hasOwnProperty.call(message, "lidDbMigrated"))
                object.lidDbMigrated = message.lidDbMigrated;
            if (message.accountType != null && Object.hasOwnProperty.call(message, "accountType"))
                object.accountType = options.enums === String ? $root.proto.ClientPayload.AccountType[message.accountType] === undefined ? message.accountType : $root.proto.ClientPayload.AccountType[message.accountType] : message.accountType;
            if (message.connectionSequenceInfo != null && Object.hasOwnProperty.call(message, "connectionSequenceInfo"))
                object.connectionSequenceInfo = message.connectionSequenceInfo;
            if (message.paaLink != null && Object.hasOwnProperty.call(message, "paaLink"))
                object.paaLink = message.paaLink;
            if (message.preacksCount != null && Object.hasOwnProperty.call(message, "preacksCount"))
                object.preacksCount = message.preacksCount;
            if (message.processingQueueSize != null && Object.hasOwnProperty.call(message, "processingQueueSize"))
                object.processingQueueSize = message.processingQueueSize;
            if (message.pairedPeripherals && message.pairedPeripherals.length) {
                object.pairedPeripherals = Array(message.pairedPeripherals.length);
                for (var j = 0; j < message.pairedPeripherals.length; ++j)
                    object.pairedPeripherals[j] = message.pairedPeripherals[j];
            }
            if (message.testIsolationId != null && Object.hasOwnProperty.call(message, "testIsolationId"))
                object.testIsolationId = options.bytes === String ? $util.base64.encode(message.testIsolationId, 0, message.testIsolationId.length) : options.bytes === Array ? Array.prototype.slice.call(message.testIsolationId) : message.testIsolationId;
            return object;
        };

        /**
         * Converts this ClientPayload to JSON.
         * @function toJSON
         * @memberof proto.ClientPayload
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ClientPayload.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ClientPayload
         * @function getTypeUrl
         * @memberof proto.ClientPayload
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ClientPayload.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.ClientPayload";
        };

        /**
         * AccountType enum.
         * @name proto.ClientPayload.AccountType
         * @enum {number}
         * @property {number} DEFAULT=0 DEFAULT value
         * @property {number} GUEST=1 GUEST value
         */
        ClientPayload.AccountType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "DEFAULT"] = 0;
            values[valuesById[1] = "GUEST"] = 1;
            return values;
        })();

        /**
         * ConnectReason enum.
         * @name proto.ClientPayload.ConnectReason
         * @enum {number}
         * @property {number} PUSH=0 PUSH value
         * @property {number} USER_ACTIVATED=1 USER_ACTIVATED value
         * @property {number} SCHEDULED=2 SCHEDULED value
         * @property {number} ERROR_RECONNECT=3 ERROR_RECONNECT value
         * @property {number} NETWORK_SWITCH=4 NETWORK_SWITCH value
         * @property {number} PING_RECONNECT=5 PING_RECONNECT value
         * @property {number} UNKNOWN=6 UNKNOWN value
         */
        ClientPayload.ConnectReason = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "PUSH"] = 0;
            values[valuesById[1] = "USER_ACTIVATED"] = 1;
            values[valuesById[2] = "SCHEDULED"] = 2;
            values[valuesById[3] = "ERROR_RECONNECT"] = 3;
            values[valuesById[4] = "NETWORK_SWITCH"] = 4;
            values[valuesById[5] = "PING_RECONNECT"] = 5;
            values[valuesById[6] = "UNKNOWN"] = 6;
            return values;
        })();

        /**
         * ConnectType enum.
         * @name proto.ClientPayload.ConnectType
         * @enum {number}
         * @property {number} CELLULAR_UNKNOWN=0 CELLULAR_UNKNOWN value
         * @property {number} WIFI_UNKNOWN=1 WIFI_UNKNOWN value
         * @property {number} CELLULAR_EDGE=100 CELLULAR_EDGE value
         * @property {number} CELLULAR_IDEN=101 CELLULAR_IDEN value
         * @property {number} CELLULAR_UMTS=102 CELLULAR_UMTS value
         * @property {number} CELLULAR_EVDO=103 CELLULAR_EVDO value
         * @property {number} CELLULAR_GPRS=104 CELLULAR_GPRS value
         * @property {number} CELLULAR_HSDPA=105 CELLULAR_HSDPA value
         * @property {number} CELLULAR_HSUPA=106 CELLULAR_HSUPA value
         * @property {number} CELLULAR_HSPA=107 CELLULAR_HSPA value
         * @property {number} CELLULAR_CDMA=108 CELLULAR_CDMA value
         * @property {number} CELLULAR_1XRTT=109 CELLULAR_1XRTT value
         * @property {number} CELLULAR_EHRPD=110 CELLULAR_EHRPD value
         * @property {number} CELLULAR_LTE=111 CELLULAR_LTE value
         * @property {number} CELLULAR_HSPAP=112 CELLULAR_HSPAP value
         */
        ClientPayload.ConnectType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "CELLULAR_UNKNOWN"] = 0;
            values[valuesById[1] = "WIFI_UNKNOWN"] = 1;
            values[valuesById[100] = "CELLULAR_EDGE"] = 100;
            values[valuesById[101] = "CELLULAR_IDEN"] = 101;
            values[valuesById[102] = "CELLULAR_UMTS"] = 102;
            values[valuesById[103] = "CELLULAR_EVDO"] = 103;
            values[valuesById[104] = "CELLULAR_GPRS"] = 104;
            values[valuesById[105] = "CELLULAR_HSDPA"] = 105;
            values[valuesById[106] = "CELLULAR_HSUPA"] = 106;
            values[valuesById[107] = "CELLULAR_HSPA"] = 107;
            values[valuesById[108] = "CELLULAR_CDMA"] = 108;
            values[valuesById[109] = "CELLULAR_1XRTT"] = 109;
            values[valuesById[110] = "CELLULAR_EHRPD"] = 110;
            values[valuesById[111] = "CELLULAR_LTE"] = 111;
            values[valuesById[112] = "CELLULAR_HSPAP"] = 112;
            return values;
        })();

        ClientPayload.DNSSource = (function() {

            /**
             * Properties of a DNSSource.
             * @memberof proto.ClientPayload
             * @interface IDNSSource
             * @property {proto.ClientPayload.DNSSource.DNSResolutionMethod|null} [dnsMethod] DNSSource dnsMethod
             * @property {boolean|null} [appCached] DNSSource appCached
             */

            /**
             * Constructs a new DNSSource.
             * @memberof proto.ClientPayload
             * @classdesc Represents a DNSSource.
             * @implements IDNSSource
             * @constructor
             * @param {proto.ClientPayload.IDNSSource=} [properties] Properties to set
             */
            function DNSSource(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * DNSSource dnsMethod.
             * @member {proto.ClientPayload.DNSSource.DNSResolutionMethod|null|undefined} dnsMethod
             * @memberof proto.ClientPayload.DNSSource
             * @instance
             */
            DNSSource.prototype.dnsMethod = null;

            /**
             * DNSSource appCached.
             * @member {boolean|null|undefined} appCached
             * @memberof proto.ClientPayload.DNSSource
             * @instance
             */
            DNSSource.prototype.appCached = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DNSSource.prototype, "_dnsMethod", {
                get: $util.oneOfGetter($oneOfFields = ["dnsMethod"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DNSSource.prototype, "_appCached", {
                get: $util.oneOfGetter($oneOfFields = ["appCached"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new DNSSource instance using the specified properties.
             * @function create
             * @memberof proto.ClientPayload.DNSSource
             * @static
             * @param {proto.ClientPayload.IDNSSource=} [properties] Properties to set
             * @returns {proto.ClientPayload.DNSSource} DNSSource instance
             */
            DNSSource.create = function create(properties) {
                return new DNSSource(properties);
            };

            /**
             * Encodes the specified DNSSource message. Does not implicitly {@link proto.ClientPayload.DNSSource.verify|verify} messages.
             * @function encode
             * @memberof proto.ClientPayload.DNSSource
             * @static
             * @param {proto.ClientPayload.IDNSSource} message DNSSource message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DNSSource.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.dnsMethod != null && Object.hasOwnProperty.call(message, "dnsMethod"))
                    writer.uint32(/* id 15, wireType 0 =*/120).int32(message.dnsMethod);
                if (message.appCached != null && Object.hasOwnProperty.call(message, "appCached"))
                    writer.uint32(/* id 16, wireType 0 =*/128).bool(message.appCached);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DNSSource message, length delimited. Does not implicitly {@link proto.ClientPayload.DNSSource.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.ClientPayload.DNSSource
             * @static
             * @param {proto.ClientPayload.IDNSSource} message DNSSource message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DNSSource.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a DNSSource message from the specified reader or buffer.
             * @function decode
             * @memberof proto.ClientPayload.DNSSource
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.ClientPayload.DNSSource} DNSSource
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DNSSource.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 15: {
                            if (u !== 0)
                                break;
                            message.dnsMethod = reader.int32();
                            message._dnsMethod = "dnsMethod";
                            continue;
                        }
                    case 16: {
                            if (u !== 0)
                                break;
                            message.appCached = reader.bool();
                            message._appCached = "appCached";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a DNSSource message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.ClientPayload.DNSSource
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.ClientPayload.DNSSource} DNSSource
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DNSSource.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DNSSource message.
             * @function verify
             * @memberof proto.ClientPayload.DNSSource
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DNSSource.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.dnsMethod != null && Object.hasOwnProperty.call(message, "dnsMethod")) {
                    properties._dnsMethod = 1;
                    if (typeof message.dnsMethod !== "number" || (message.dnsMethod | 0) !== message.dnsMethod)
                        return "dnsMethod: enum value expected";
                }
                if (message.appCached != null && Object.hasOwnProperty.call(message, "appCached")) {
                    properties._appCached = 1;
                    if (typeof message.appCached !== "boolean")
                        return "appCached: boolean expected";
                }
                return null;
            };

            /**
             * Creates a DNSSource message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.ClientPayload.DNSSource
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.ClientPayload.DNSSource} DNSSource
             */
            DNSSource.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.ClientPayload.DNSSource: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.dnsMethod) {
                case "SYSTEM":
                case 0:
                    message.dnsMethod = 0;
                    break;
                case "GOOGLE":
                case 1:
                    message.dnsMethod = 1;
                    break;
                case "HARDCODED":
                case 2:
                    message.dnsMethod = 2;
                    break;
                case "OVERRIDE":
                case 3:
                    message.dnsMethod = 3;
                    break;
                case "FALLBACK":
                case 4:
                    message.dnsMethod = 4;
                    break;
                case "MNS":
                case 5:
                    message.dnsMethod = 5;
                    break;
                case "MNS_SECONDARY":
                case 6:
                    message.dnsMethod = 6;
                    break;
                case "SOCKS_PROXY":
                case 7:
                    message.dnsMethod = 7;
                    break;
                default:
                    if (typeof object.dnsMethod === "number" && (object.dnsMethod | 0) === object.dnsMethod)
                        message.dnsMethod = object.dnsMethod;
                }
                if (object.appCached != null)
                    message.appCached = Boolean(object.appCached);
                return message;
            };

            /**
             * Creates a plain object from a DNSSource message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.ClientPayload.DNSSource
             * @static
             * @param {proto.ClientPayload.DNSSource} message DNSSource
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DNSSource.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.dnsMethod != null && Object.hasOwnProperty.call(message, "dnsMethod"))
                    object.dnsMethod = options.enums === String ? $root.proto.ClientPayload.DNSSource.DNSResolutionMethod[message.dnsMethod] === undefined ? message.dnsMethod : $root.proto.ClientPayload.DNSSource.DNSResolutionMethod[message.dnsMethod] : message.dnsMethod;
                if (message.appCached != null && Object.hasOwnProperty.call(message, "appCached"))
                    object.appCached = message.appCached;
                return object;
            };

            /**
             * Converts this DNSSource to JSON.
             * @function toJSON
             * @memberof proto.ClientPayload.DNSSource
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DNSSource.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for DNSSource
             * @function getTypeUrl
             * @memberof proto.ClientPayload.DNSSource
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            DNSSource.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.ClientPayload.DNSSource";
            };

            /**
             * DNSResolutionMethod enum.
             * @name proto.ClientPayload.DNSSource.DNSResolutionMethod
             * @enum {number}
             * @property {number} SYSTEM=0 SYSTEM value
             * @property {number} GOOGLE=1 GOOGLE value
             * @property {number} HARDCODED=2 HARDCODED value
             * @property {number} OVERRIDE=3 OVERRIDE value
             * @property {number} FALLBACK=4 FALLBACK value
             * @property {number} MNS=5 MNS value
             * @property {number} MNS_SECONDARY=6 MNS_SECONDARY value
             * @property {number} SOCKS_PROXY=7 SOCKS_PROXY value
             */
            DNSSource.DNSResolutionMethod = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "SYSTEM"] = 0;
                values[valuesById[1] = "GOOGLE"] = 1;
                values[valuesById[2] = "HARDCODED"] = 2;
                values[valuesById[3] = "OVERRIDE"] = 3;
                values[valuesById[4] = "FALLBACK"] = 4;
                values[valuesById[5] = "MNS"] = 5;
                values[valuesById[6] = "MNS_SECONDARY"] = 6;
                values[valuesById[7] = "SOCKS_PROXY"] = 7;
                return values;
            })();

            return DNSSource;
        })();

        ClientPayload.DevicePairingRegistrationData = (function() {

            /**
             * Properties of a DevicePairingRegistrationData.
             * @memberof proto.ClientPayload
             * @interface IDevicePairingRegistrationData
             * @property {Uint8Array|null} [eRegid] DevicePairingRegistrationData eRegid
             * @property {Uint8Array|null} [eKeytype] DevicePairingRegistrationData eKeytype
             * @property {Uint8Array|null} [eIdent] DevicePairingRegistrationData eIdent
             * @property {Uint8Array|null} [eSkeyId] DevicePairingRegistrationData eSkeyId
             * @property {Uint8Array|null} [eSkeyVal] DevicePairingRegistrationData eSkeyVal
             * @property {Uint8Array|null} [eSkeySig] DevicePairingRegistrationData eSkeySig
             * @property {Uint8Array|null} [buildHash] DevicePairingRegistrationData buildHash
             * @property {Uint8Array|null} [deviceProps] DevicePairingRegistrationData deviceProps
             */

            /**
             * Constructs a new DevicePairingRegistrationData.
             * @memberof proto.ClientPayload
             * @classdesc Represents a DevicePairingRegistrationData.
             * @implements IDevicePairingRegistrationData
             * @constructor
             * @param {proto.ClientPayload.IDevicePairingRegistrationData=} [properties] Properties to set
             */
            function DevicePairingRegistrationData(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * DevicePairingRegistrationData eRegid.
             * @member {Uint8Array|null|undefined} eRegid
             * @memberof proto.ClientPayload.DevicePairingRegistrationData
             * @instance
             */
            DevicePairingRegistrationData.prototype.eRegid = null;

            /**
             * DevicePairingRegistrationData eKeytype.
             * @member {Uint8Array|null|undefined} eKeytype
             * @memberof proto.ClientPayload.DevicePairingRegistrationData
             * @instance
             */
            DevicePairingRegistrationData.prototype.eKeytype = null;

            /**
             * DevicePairingRegistrationData eIdent.
             * @member {Uint8Array|null|undefined} eIdent
             * @memberof proto.ClientPayload.DevicePairingRegistrationData
             * @instance
             */
            DevicePairingRegistrationData.prototype.eIdent = null;

            /**
             * DevicePairingRegistrationData eSkeyId.
             * @member {Uint8Array|null|undefined} eSkeyId
             * @memberof proto.ClientPayload.DevicePairingRegistrationData
             * @instance
             */
            DevicePairingRegistrationData.prototype.eSkeyId = null;

            /**
             * DevicePairingRegistrationData eSkeyVal.
             * @member {Uint8Array|null|undefined} eSkeyVal
             * @memberof proto.ClientPayload.DevicePairingRegistrationData
             * @instance
             */
            DevicePairingRegistrationData.prototype.eSkeyVal = null;

            /**
             * DevicePairingRegistrationData eSkeySig.
             * @member {Uint8Array|null|undefined} eSkeySig
             * @memberof proto.ClientPayload.DevicePairingRegistrationData
             * @instance
             */
            DevicePairingRegistrationData.prototype.eSkeySig = null;

            /**
             * DevicePairingRegistrationData buildHash.
             * @member {Uint8Array|null|undefined} buildHash
             * @memberof proto.ClientPayload.DevicePairingRegistrationData
             * @instance
             */
            DevicePairingRegistrationData.prototype.buildHash = null;

            /**
             * DevicePairingRegistrationData deviceProps.
             * @member {Uint8Array|null|undefined} deviceProps
             * @memberof proto.ClientPayload.DevicePairingRegistrationData
             * @instance
             */
            DevicePairingRegistrationData.prototype.deviceProps = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DevicePairingRegistrationData.prototype, "_eRegid", {
                get: $util.oneOfGetter($oneOfFields = ["eRegid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DevicePairingRegistrationData.prototype, "_eKeytype", {
                get: $util.oneOfGetter($oneOfFields = ["eKeytype"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DevicePairingRegistrationData.prototype, "_eIdent", {
                get: $util.oneOfGetter($oneOfFields = ["eIdent"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DevicePairingRegistrationData.prototype, "_eSkeyId", {
                get: $util.oneOfGetter($oneOfFields = ["eSkeyId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DevicePairingRegistrationData.prototype, "_eSkeyVal", {
                get: $util.oneOfGetter($oneOfFields = ["eSkeyVal"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DevicePairingRegistrationData.prototype, "_eSkeySig", {
                get: $util.oneOfGetter($oneOfFields = ["eSkeySig"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DevicePairingRegistrationData.prototype, "_buildHash", {
                get: $util.oneOfGetter($oneOfFields = ["buildHash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DevicePairingRegistrationData.prototype, "_deviceProps", {
                get: $util.oneOfGetter($oneOfFields = ["deviceProps"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new DevicePairingRegistrationData instance using the specified properties.
             * @function create
             * @memberof proto.ClientPayload.DevicePairingRegistrationData
             * @static
             * @param {proto.ClientPayload.IDevicePairingRegistrationData=} [properties] Properties to set
             * @returns {proto.ClientPayload.DevicePairingRegistrationData} DevicePairingRegistrationData instance
             */
            DevicePairingRegistrationData.create = function create(properties) {
                return new DevicePairingRegistrationData(properties);
            };

            /**
             * Encodes the specified DevicePairingRegistrationData message. Does not implicitly {@link proto.ClientPayload.DevicePairingRegistrationData.verify|verify} messages.
             * @function encode
             * @memberof proto.ClientPayload.DevicePairingRegistrationData
             * @static
             * @param {proto.ClientPayload.IDevicePairingRegistrationData} message DevicePairingRegistrationData message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DevicePairingRegistrationData.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.eRegid != null && Object.hasOwnProperty.call(message, "eRegid"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.eRegid);
                if (message.eKeytype != null && Object.hasOwnProperty.call(message, "eKeytype"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.eKeytype);
                if (message.eIdent != null && Object.hasOwnProperty.call(message, "eIdent"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.eIdent);
                if (message.eSkeyId != null && Object.hasOwnProperty.call(message, "eSkeyId"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.eSkeyId);
                if (message.eSkeyVal != null && Object.hasOwnProperty.call(message, "eSkeyVal"))
                    writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.eSkeyVal);
                if (message.eSkeySig != null && Object.hasOwnProperty.call(message, "eSkeySig"))
                    writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.eSkeySig);
                if (message.buildHash != null && Object.hasOwnProperty.call(message, "buildHash"))
                    writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.buildHash);
                if (message.deviceProps != null && Object.hasOwnProperty.call(message, "deviceProps"))
                    writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.deviceProps);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DevicePairingRegistrationData message, length delimited. Does not implicitly {@link proto.ClientPayload.DevicePairingRegistrationData.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.ClientPayload.DevicePairingRegistrationData
             * @static
             * @param {proto.ClientPayload.IDevicePairingRegistrationData} message DevicePairingRegistrationData message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DevicePairingRegistrationData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a DevicePairingRegistrationData message from the specified reader or buffer.
             * @function decode
             * @memberof proto.ClientPayload.DevicePairingRegistrationData
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.ClientPayload.DevicePairingRegistrationData} DevicePairingRegistrationData
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DevicePairingRegistrationData.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.eRegid = reader.bytes();
                            message._eRegid = "eRegid";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.eKeytype = reader.bytes();
                            message._eKeytype = "eKeytype";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.eIdent = reader.bytes();
                            message._eIdent = "eIdent";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.eSkeyId = reader.bytes();
                            message._eSkeyId = "eSkeyId";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.eSkeyVal = reader.bytes();
                            message._eSkeyVal = "eSkeyVal";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.eSkeySig = reader.bytes();
                            message._eSkeySig = "eSkeySig";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.buildHash = reader.bytes();
                            message._buildHash = "buildHash";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.deviceProps = reader.bytes();
                            message._deviceProps = "deviceProps";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a DevicePairingRegistrationData message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.ClientPayload.DevicePairingRegistrationData
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.ClientPayload.DevicePairingRegistrationData} DevicePairingRegistrationData
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DevicePairingRegistrationData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DevicePairingRegistrationData message.
             * @function verify
             * @memberof proto.ClientPayload.DevicePairingRegistrationData
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DevicePairingRegistrationData.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.eRegid != null && Object.hasOwnProperty.call(message, "eRegid")) {
                    properties._eRegid = 1;
                    if (!(message.eRegid && typeof message.eRegid.length === "number" || $util.isString(message.eRegid)))
                        return "eRegid: buffer expected";
                }
                if (message.eKeytype != null && Object.hasOwnProperty.call(message, "eKeytype")) {
                    properties._eKeytype = 1;
                    if (!(message.eKeytype && typeof message.eKeytype.length === "number" || $util.isString(message.eKeytype)))
                        return "eKeytype: buffer expected";
                }
                if (message.eIdent != null && Object.hasOwnProperty.call(message, "eIdent")) {
                    properties._eIdent = 1;
                    if (!(message.eIdent && typeof message.eIdent.length === "number" || $util.isString(message.eIdent)))
                        return "eIdent: buffer expected";
                }
                if (message.eSkeyId != null && Object.hasOwnProperty.call(message, "eSkeyId")) {
                    properties._eSkeyId = 1;
                    if (!(message.eSkeyId && typeof message.eSkeyId.length === "number" || $util.isString(message.eSkeyId)))
                        return "eSkeyId: buffer expected";
                }
                if (message.eSkeyVal != null && Object.hasOwnProperty.call(message, "eSkeyVal")) {
                    properties._eSkeyVal = 1;
                    if (!(message.eSkeyVal && typeof message.eSkeyVal.length === "number" || $util.isString(message.eSkeyVal)))
                        return "eSkeyVal: buffer expected";
                }
                if (message.eSkeySig != null && Object.hasOwnProperty.call(message, "eSkeySig")) {
                    properties._eSkeySig = 1;
                    if (!(message.eSkeySig && typeof message.eSkeySig.length === "number" || $util.isString(message.eSkeySig)))
                        return "eSkeySig: buffer expected";
                }
                if (message.buildHash != null && Object.hasOwnProperty.call(message, "buildHash")) {
                    properties._buildHash = 1;
                    if (!(message.buildHash && typeof message.buildHash.length === "number" || $util.isString(message.buildHash)))
                        return "buildHash: buffer expected";
                }
                if (message.deviceProps != null && Object.hasOwnProperty.call(message, "deviceProps")) {
                    properties._deviceProps = 1;
                    if (!(message.deviceProps && typeof message.deviceProps.length === "number" || $util.isString(message.deviceProps)))
                        return "deviceProps: buffer expected";
                }
                return null;
            };

            /**
             * Creates a DevicePairingRegistrationData message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.ClientPayload.DevicePairingRegistrationData
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.ClientPayload.DevicePairingRegistrationData} DevicePairingRegistrationData
             */
            DevicePairingRegistrationData.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.ClientPayload.DevicePairingRegistrationData: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.eRegid != null)
                    if (typeof object.eRegid === "string")
                        $util.base64.decode(object.eRegid, message.eRegid = $util.newBuffer($util.base64.length(object.eRegid)), 0);
                    else if (object.eRegid.length >= 0)
                        message.eRegid = object.eRegid;
                if (object.eKeytype != null)
                    if (typeof object.eKeytype === "string")
                        $util.base64.decode(object.eKeytype, message.eKeytype = $util.newBuffer($util.base64.length(object.eKeytype)), 0);
                    else if (object.eKeytype.length >= 0)
                        message.eKeytype = object.eKeytype;
                if (object.eIdent != null)
                    if (typeof object.eIdent === "string")
                        $util.base64.decode(object.eIdent, message.eIdent = $util.newBuffer($util.base64.length(object.eIdent)), 0);
                    else if (object.eIdent.length >= 0)
                        message.eIdent = object.eIdent;
                if (object.eSkeyId != null)
                    if (typeof object.eSkeyId === "string")
                        $util.base64.decode(object.eSkeyId, message.eSkeyId = $util.newBuffer($util.base64.length(object.eSkeyId)), 0);
                    else if (object.eSkeyId.length >= 0)
                        message.eSkeyId = object.eSkeyId;
                if (object.eSkeyVal != null)
                    if (typeof object.eSkeyVal === "string")
                        $util.base64.decode(object.eSkeyVal, message.eSkeyVal = $util.newBuffer($util.base64.length(object.eSkeyVal)), 0);
                    else if (object.eSkeyVal.length >= 0)
                        message.eSkeyVal = object.eSkeyVal;
                if (object.eSkeySig != null)
                    if (typeof object.eSkeySig === "string")
                        $util.base64.decode(object.eSkeySig, message.eSkeySig = $util.newBuffer($util.base64.length(object.eSkeySig)), 0);
                    else if (object.eSkeySig.length >= 0)
                        message.eSkeySig = object.eSkeySig;
                if (object.buildHash != null)
                    if (typeof object.buildHash === "string")
                        $util.base64.decode(object.buildHash, message.buildHash = $util.newBuffer($util.base64.length(object.buildHash)), 0);
                    else if (object.buildHash.length >= 0)
                        message.buildHash = object.buildHash;
                if (object.deviceProps != null)
                    if (typeof object.deviceProps === "string")
                        $util.base64.decode(object.deviceProps, message.deviceProps = $util.newBuffer($util.base64.length(object.deviceProps)), 0);
                    else if (object.deviceProps.length >= 0)
                        message.deviceProps = object.deviceProps;
                return message;
            };

            /**
             * Creates a plain object from a DevicePairingRegistrationData message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.ClientPayload.DevicePairingRegistrationData
             * @static
             * @param {proto.ClientPayload.DevicePairingRegistrationData} message DevicePairingRegistrationData
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DevicePairingRegistrationData.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.eRegid != null && Object.hasOwnProperty.call(message, "eRegid"))
                    object.eRegid = options.bytes === String ? $util.base64.encode(message.eRegid, 0, message.eRegid.length) : options.bytes === Array ? Array.prototype.slice.call(message.eRegid) : message.eRegid;
                if (message.eKeytype != null && Object.hasOwnProperty.call(message, "eKeytype"))
                    object.eKeytype = options.bytes === String ? $util.base64.encode(message.eKeytype, 0, message.eKeytype.length) : options.bytes === Array ? Array.prototype.slice.call(message.eKeytype) : message.eKeytype;
                if (message.eIdent != null && Object.hasOwnProperty.call(message, "eIdent"))
                    object.eIdent = options.bytes === String ? $util.base64.encode(message.eIdent, 0, message.eIdent.length) : options.bytes === Array ? Array.prototype.slice.call(message.eIdent) : message.eIdent;
                if (message.eSkeyId != null && Object.hasOwnProperty.call(message, "eSkeyId"))
                    object.eSkeyId = options.bytes === String ? $util.base64.encode(message.eSkeyId, 0, message.eSkeyId.length) : options.bytes === Array ? Array.prototype.slice.call(message.eSkeyId) : message.eSkeyId;
                if (message.eSkeyVal != null && Object.hasOwnProperty.call(message, "eSkeyVal"))
                    object.eSkeyVal = options.bytes === String ? $util.base64.encode(message.eSkeyVal, 0, message.eSkeyVal.length) : options.bytes === Array ? Array.prototype.slice.call(message.eSkeyVal) : message.eSkeyVal;
                if (message.eSkeySig != null && Object.hasOwnProperty.call(message, "eSkeySig"))
                    object.eSkeySig = options.bytes === String ? $util.base64.encode(message.eSkeySig, 0, message.eSkeySig.length) : options.bytes === Array ? Array.prototype.slice.call(message.eSkeySig) : message.eSkeySig;
                if (message.buildHash != null && Object.hasOwnProperty.call(message, "buildHash"))
                    object.buildHash = options.bytes === String ? $util.base64.encode(message.buildHash, 0, message.buildHash.length) : options.bytes === Array ? Array.prototype.slice.call(message.buildHash) : message.buildHash;
                if (message.deviceProps != null && Object.hasOwnProperty.call(message, "deviceProps"))
                    object.deviceProps = options.bytes === String ? $util.base64.encode(message.deviceProps, 0, message.deviceProps.length) : options.bytes === Array ? Array.prototype.slice.call(message.deviceProps) : message.deviceProps;
                return object;
            };

            /**
             * Converts this DevicePairingRegistrationData to JSON.
             * @function toJSON
             * @memberof proto.ClientPayload.DevicePairingRegistrationData
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DevicePairingRegistrationData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for DevicePairingRegistrationData
             * @function getTypeUrl
             * @memberof proto.ClientPayload.DevicePairingRegistrationData
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            DevicePairingRegistrationData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.ClientPayload.DevicePairingRegistrationData";
            };

            return DevicePairingRegistrationData;
        })();

        /**
         * IOSAppExtension enum.
         * @name proto.ClientPayload.IOSAppExtension
         * @enum {number}
         * @property {number} SHARE_EXTENSION=0 SHARE_EXTENSION value
         * @property {number} SERVICE_EXTENSION=1 SERVICE_EXTENSION value
         * @property {number} INTENTS_EXTENSION=2 INTENTS_EXTENSION value
         */
        ClientPayload.IOSAppExtension = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "SHARE_EXTENSION"] = 0;
            values[valuesById[1] = "SERVICE_EXTENSION"] = 1;
            values[valuesById[2] = "INTENTS_EXTENSION"] = 2;
            return values;
        })();

        ClientPayload.InteropData = (function() {

            /**
             * Properties of an InteropData.
             * @memberof proto.ClientPayload
             * @interface IInteropData
             * @property {number|Long|null} [accountId] InteropData accountId
             * @property {Uint8Array|null} [token] InteropData token
             * @property {boolean|null} [enableReadReceipts] InteropData enableReadReceipts
             */

            /**
             * Constructs a new InteropData.
             * @memberof proto.ClientPayload
             * @classdesc Represents an InteropData.
             * @implements IInteropData
             * @constructor
             * @param {proto.ClientPayload.IInteropData=} [properties] Properties to set
             */
            function InteropData(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * InteropData accountId.
             * @member {number|Long|null|undefined} accountId
             * @memberof proto.ClientPayload.InteropData
             * @instance
             */
            InteropData.prototype.accountId = null;

            /**
             * InteropData token.
             * @member {Uint8Array|null|undefined} token
             * @memberof proto.ClientPayload.InteropData
             * @instance
             */
            InteropData.prototype.token = null;

            /**
             * InteropData enableReadReceipts.
             * @member {boolean|null|undefined} enableReadReceipts
             * @memberof proto.ClientPayload.InteropData
             * @instance
             */
            InteropData.prototype.enableReadReceipts = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InteropData.prototype, "_accountId", {
                get: $util.oneOfGetter($oneOfFields = ["accountId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InteropData.prototype, "_token", {
                get: $util.oneOfGetter($oneOfFields = ["token"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InteropData.prototype, "_enableReadReceipts", {
                get: $util.oneOfGetter($oneOfFields = ["enableReadReceipts"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new InteropData instance using the specified properties.
             * @function create
             * @memberof proto.ClientPayload.InteropData
             * @static
             * @param {proto.ClientPayload.IInteropData=} [properties] Properties to set
             * @returns {proto.ClientPayload.InteropData} InteropData instance
             */
            InteropData.create = function create(properties) {
                return new InteropData(properties);
            };

            /**
             * Encodes the specified InteropData message. Does not implicitly {@link proto.ClientPayload.InteropData.verify|verify} messages.
             * @function encode
             * @memberof proto.ClientPayload.InteropData
             * @static
             * @param {proto.ClientPayload.IInteropData} message InteropData message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InteropData.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.accountId != null && Object.hasOwnProperty.call(message, "accountId"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.accountId);
                if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.token);
                if (message.enableReadReceipts != null && Object.hasOwnProperty.call(message, "enableReadReceipts"))
                    writer.uint32(/* id 3, wireType 0 =*/24).bool(message.enableReadReceipts);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified InteropData message, length delimited. Does not implicitly {@link proto.ClientPayload.InteropData.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.ClientPayload.InteropData
             * @static
             * @param {proto.ClientPayload.IInteropData} message InteropData message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InteropData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an InteropData message from the specified reader or buffer.
             * @function decode
             * @memberof proto.ClientPayload.InteropData
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.ClientPayload.InteropData} InteropData
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InteropData.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.accountId = reader.uint64();
                            message._accountId = "accountId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.token = reader.bytes();
                            message._token = "token";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.enableReadReceipts = reader.bool();
                            message._enableReadReceipts = "enableReadReceipts";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an InteropData message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.ClientPayload.InteropData
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.ClientPayload.InteropData} InteropData
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InteropData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an InteropData message.
             * @function verify
             * @memberof proto.ClientPayload.InteropData
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            InteropData.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.accountId != null && Object.hasOwnProperty.call(message, "accountId")) {
                    properties._accountId = 1;
                    if (!$util.isInteger(message.accountId) && !(message.accountId && $util.isInteger(message.accountId.low) && $util.isInteger(message.accountId.high)))
                        return "accountId: integer|Long expected";
                }
                if (message.token != null && Object.hasOwnProperty.call(message, "token")) {
                    properties._token = 1;
                    if (!(message.token && typeof message.token.length === "number" || $util.isString(message.token)))
                        return "token: buffer expected";
                }
                if (message.enableReadReceipts != null && Object.hasOwnProperty.call(message, "enableReadReceipts")) {
                    properties._enableReadReceipts = 1;
                    if (typeof message.enableReadReceipts !== "boolean")
                        return "enableReadReceipts: boolean expected";
                }
                return null;
            };

            /**
             * Creates an InteropData message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.ClientPayload.InteropData
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.ClientPayload.InteropData} InteropData
             */
            InteropData.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.ClientPayload.InteropData: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.accountId != null)
                    if ($util.Long)
                        message.accountId = $util.Long.fromValue(object.accountId, true);
                    else if (typeof object.accountId === "string")
                        message.accountId = parseInt(object.accountId, 10);
                    else if (typeof object.accountId === "number")
                        message.accountId = object.accountId;
                    else if (typeof object.accountId === "object")
                        message.accountId = new $util.LongBits(object.accountId.low >>> 0, object.accountId.high >>> 0).toNumber(true);
                if (object.token != null)
                    if (typeof object.token === "string")
                        $util.base64.decode(object.token, message.token = $util.newBuffer($util.base64.length(object.token)), 0);
                    else if (object.token.length >= 0)
                        message.token = object.token;
                if (object.enableReadReceipts != null)
                    message.enableReadReceipts = Boolean(object.enableReadReceipts);
                return message;
            };

            /**
             * Creates a plain object from an InteropData message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.ClientPayload.InteropData
             * @static
             * @param {proto.ClientPayload.InteropData} message InteropData
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            InteropData.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.accountId != null && Object.hasOwnProperty.call(message, "accountId"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.accountId = typeof message.accountId === "number" ? BigInt(message.accountId) : $util.Long.fromBits(message.accountId.low >>> 0, message.accountId.high >>> 0, true).toBigInt();
                    else if (typeof message.accountId === "number")
                        object.accountId = options.longs === String ? String(message.accountId) : message.accountId;
                    else
                        object.accountId = options.longs === String ? $util.Long.prototype.toString.call(message.accountId) : options.longs === Number ? new $util.LongBits(message.accountId.low >>> 0, message.accountId.high >>> 0).toNumber(true) : message.accountId;
                if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                    object.token = options.bytes === String ? $util.base64.encode(message.token, 0, message.token.length) : options.bytes === Array ? Array.prototype.slice.call(message.token) : message.token;
                if (message.enableReadReceipts != null && Object.hasOwnProperty.call(message, "enableReadReceipts"))
                    object.enableReadReceipts = message.enableReadReceipts;
                return object;
            };

            /**
             * Converts this InteropData to JSON.
             * @function toJSON
             * @memberof proto.ClientPayload.InteropData
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            InteropData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for InteropData
             * @function getTypeUrl
             * @memberof proto.ClientPayload.InteropData
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            InteropData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.ClientPayload.InteropData";
            };

            return InteropData;
        })();

        /**
         * Product enum.
         * @name proto.ClientPayload.Product
         * @enum {number}
         * @property {number} WHATSAPP=0 WHATSAPP value
         * @property {number} MESSENGER=1 MESSENGER value
         * @property {number} INTEROP=2 INTEROP value
         * @property {number} INTEROP_MSGR=3 INTEROP_MSGR value
         * @property {number} WHATSAPP_LID=4 WHATSAPP_LID value
         */
        ClientPayload.Product = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "WHATSAPP"] = 0;
            values[valuesById[1] = "MESSENGER"] = 1;
            values[valuesById[2] = "INTEROP"] = 2;
            values[valuesById[3] = "INTEROP_MSGR"] = 3;
            values[valuesById[4] = "WHATSAPP_LID"] = 4;
            return values;
        })();

        /**
         * TrafficAnonymization enum.
         * @name proto.ClientPayload.TrafficAnonymization
         * @enum {number}
         * @property {number} OFF=0 OFF value
         * @property {number} STANDARD=1 STANDARD value
         */
        ClientPayload.TrafficAnonymization = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "OFF"] = 0;
            values[valuesById[1] = "STANDARD"] = 1;
            return values;
        })();

        ClientPayload.UserAgent = (function() {

            /**
             * Properties of a UserAgent.
             * @memberof proto.ClientPayload
             * @interface IUserAgent
             * @property {proto.ClientPayload.UserAgent.Platform|null} [platform] UserAgent platform
             * @property {proto.ClientPayload.UserAgent.IAppVersion|null} [appVersion] UserAgent appVersion
             * @property {string|null} [mcc] UserAgent mcc
             * @property {string|null} [mnc] UserAgent mnc
             * @property {string|null} [osVersion] UserAgent osVersion
             * @property {string|null} [manufacturer] UserAgent manufacturer
             * @property {string|null} [device] UserAgent device
             * @property {string|null} [osBuildNumber] UserAgent osBuildNumber
             * @property {string|null} [phoneId] UserAgent phoneId
             * @property {proto.ClientPayload.UserAgent.ReleaseChannel|null} [releaseChannel] UserAgent releaseChannel
             * @property {string|null} [localeLanguageIso6391] UserAgent localeLanguageIso6391
             * @property {string|null} [localeCountryIso31661Alpha2] UserAgent localeCountryIso31661Alpha2
             * @property {string|null} [deviceBoard] UserAgent deviceBoard
             * @property {string|null} [deviceExpId] UserAgent deviceExpId
             * @property {proto.ClientPayload.UserAgent.DeviceType|null} [deviceType] UserAgent deviceType
             * @property {string|null} [deviceModelType] UserAgent deviceModelType
             * @property {proto.ClientPayload.UserAgent.DistributionChannel|null} [distributionChannel] UserAgent distributionChannel
             */

            /**
             * Constructs a new UserAgent.
             * @memberof proto.ClientPayload
             * @classdesc Represents a UserAgent.
             * @implements IUserAgent
             * @constructor
             * @param {proto.ClientPayload.IUserAgent=} [properties] Properties to set
             */
            function UserAgent(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * UserAgent platform.
             * @member {proto.ClientPayload.UserAgent.Platform|null|undefined} platform
             * @memberof proto.ClientPayload.UserAgent
             * @instance
             */
            UserAgent.prototype.platform = null;

            /**
             * UserAgent appVersion.
             * @member {proto.ClientPayload.UserAgent.IAppVersion|null|undefined} appVersion
             * @memberof proto.ClientPayload.UserAgent
             * @instance
             */
            UserAgent.prototype.appVersion = null;

            /**
             * UserAgent mcc.
             * @member {string|null|undefined} mcc
             * @memberof proto.ClientPayload.UserAgent
             * @instance
             */
            UserAgent.prototype.mcc = null;

            /**
             * UserAgent mnc.
             * @member {string|null|undefined} mnc
             * @memberof proto.ClientPayload.UserAgent
             * @instance
             */
            UserAgent.prototype.mnc = null;

            /**
             * UserAgent osVersion.
             * @member {string|null|undefined} osVersion
             * @memberof proto.ClientPayload.UserAgent
             * @instance
             */
            UserAgent.prototype.osVersion = null;

            /**
             * UserAgent manufacturer.
             * @member {string|null|undefined} manufacturer
             * @memberof proto.ClientPayload.UserAgent
             * @instance
             */
            UserAgent.prototype.manufacturer = null;

            /**
             * UserAgent device.
             * @member {string|null|undefined} device
             * @memberof proto.ClientPayload.UserAgent
             * @instance
             */
            UserAgent.prototype.device = null;

            /**
             * UserAgent osBuildNumber.
             * @member {string|null|undefined} osBuildNumber
             * @memberof proto.ClientPayload.UserAgent
             * @instance
             */
            UserAgent.prototype.osBuildNumber = null;

            /**
             * UserAgent phoneId.
             * @member {string|null|undefined} phoneId
             * @memberof proto.ClientPayload.UserAgent
             * @instance
             */
            UserAgent.prototype.phoneId = null;

            /**
             * UserAgent releaseChannel.
             * @member {proto.ClientPayload.UserAgent.ReleaseChannel|null|undefined} releaseChannel
             * @memberof proto.ClientPayload.UserAgent
             * @instance
             */
            UserAgent.prototype.releaseChannel = null;

            /**
             * UserAgent localeLanguageIso6391.
             * @member {string|null|undefined} localeLanguageIso6391
             * @memberof proto.ClientPayload.UserAgent
             * @instance
             */
            UserAgent.prototype.localeLanguageIso6391 = null;

            /**
             * UserAgent localeCountryIso31661Alpha2.
             * @member {string|null|undefined} localeCountryIso31661Alpha2
             * @memberof proto.ClientPayload.UserAgent
             * @instance
             */
            UserAgent.prototype.localeCountryIso31661Alpha2 = null;

            /**
             * UserAgent deviceBoard.
             * @member {string|null|undefined} deviceBoard
             * @memberof proto.ClientPayload.UserAgent
             * @instance
             */
            UserAgent.prototype.deviceBoard = null;

            /**
             * UserAgent deviceExpId.
             * @member {string|null|undefined} deviceExpId
             * @memberof proto.ClientPayload.UserAgent
             * @instance
             */
            UserAgent.prototype.deviceExpId = null;

            /**
             * UserAgent deviceType.
             * @member {proto.ClientPayload.UserAgent.DeviceType|null|undefined} deviceType
             * @memberof proto.ClientPayload.UserAgent
             * @instance
             */
            UserAgent.prototype.deviceType = null;

            /**
             * UserAgent deviceModelType.
             * @member {string|null|undefined} deviceModelType
             * @memberof proto.ClientPayload.UserAgent
             * @instance
             */
            UserAgent.prototype.deviceModelType = null;

            /**
             * UserAgent distributionChannel.
             * @member {proto.ClientPayload.UserAgent.DistributionChannel|null|undefined} distributionChannel
             * @memberof proto.ClientPayload.UserAgent
             * @instance
             */
            UserAgent.prototype.distributionChannel = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UserAgent.prototype, "_platform", {
                get: $util.oneOfGetter($oneOfFields = ["platform"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UserAgent.prototype, "_appVersion", {
                get: $util.oneOfGetter($oneOfFields = ["appVersion"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UserAgent.prototype, "_mcc", {
                get: $util.oneOfGetter($oneOfFields = ["mcc"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UserAgent.prototype, "_mnc", {
                get: $util.oneOfGetter($oneOfFields = ["mnc"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UserAgent.prototype, "_osVersion", {
                get: $util.oneOfGetter($oneOfFields = ["osVersion"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UserAgent.prototype, "_manufacturer", {
                get: $util.oneOfGetter($oneOfFields = ["manufacturer"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UserAgent.prototype, "_device", {
                get: $util.oneOfGetter($oneOfFields = ["device"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UserAgent.prototype, "_osBuildNumber", {
                get: $util.oneOfGetter($oneOfFields = ["osBuildNumber"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UserAgent.prototype, "_phoneId", {
                get: $util.oneOfGetter($oneOfFields = ["phoneId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UserAgent.prototype, "_releaseChannel", {
                get: $util.oneOfGetter($oneOfFields = ["releaseChannel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UserAgent.prototype, "_localeLanguageIso6391", {
                get: $util.oneOfGetter($oneOfFields = ["localeLanguageIso6391"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UserAgent.prototype, "_localeCountryIso31661Alpha2", {
                get: $util.oneOfGetter($oneOfFields = ["localeCountryIso31661Alpha2"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UserAgent.prototype, "_deviceBoard", {
                get: $util.oneOfGetter($oneOfFields = ["deviceBoard"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UserAgent.prototype, "_deviceExpId", {
                get: $util.oneOfGetter($oneOfFields = ["deviceExpId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UserAgent.prototype, "_deviceType", {
                get: $util.oneOfGetter($oneOfFields = ["deviceType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UserAgent.prototype, "_deviceModelType", {
                get: $util.oneOfGetter($oneOfFields = ["deviceModelType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UserAgent.prototype, "_distributionChannel", {
                get: $util.oneOfGetter($oneOfFields = ["distributionChannel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new UserAgent instance using the specified properties.
             * @function create
             * @memberof proto.ClientPayload.UserAgent
             * @static
             * @param {proto.ClientPayload.IUserAgent=} [properties] Properties to set
             * @returns {proto.ClientPayload.UserAgent} UserAgent instance
             */
            UserAgent.create = function create(properties) {
                return new UserAgent(properties);
            };

            /**
             * Encodes the specified UserAgent message. Does not implicitly {@link proto.ClientPayload.UserAgent.verify|verify} messages.
             * @function encode
             * @memberof proto.ClientPayload.UserAgent
             * @static
             * @param {proto.ClientPayload.IUserAgent} message UserAgent message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UserAgent.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.platform != null && Object.hasOwnProperty.call(message, "platform"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.platform);
                if (message.appVersion != null && Object.hasOwnProperty.call(message, "appVersion"))
                    $root.proto.ClientPayload.UserAgent.AppVersion.encode(message.appVersion, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.mcc != null && Object.hasOwnProperty.call(message, "mcc"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.mcc);
                if (message.mnc != null && Object.hasOwnProperty.call(message, "mnc"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.mnc);
                if (message.osVersion != null && Object.hasOwnProperty.call(message, "osVersion"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.osVersion);
                if (message.manufacturer != null && Object.hasOwnProperty.call(message, "manufacturer"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.manufacturer);
                if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.device);
                if (message.osBuildNumber != null && Object.hasOwnProperty.call(message, "osBuildNumber"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.osBuildNumber);
                if (message.phoneId != null && Object.hasOwnProperty.call(message, "phoneId"))
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.phoneId);
                if (message.releaseChannel != null && Object.hasOwnProperty.call(message, "releaseChannel"))
                    writer.uint32(/* id 10, wireType 0 =*/80).int32(message.releaseChannel);
                if (message.localeLanguageIso6391 != null && Object.hasOwnProperty.call(message, "localeLanguageIso6391"))
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.localeLanguageIso6391);
                if (message.localeCountryIso31661Alpha2 != null && Object.hasOwnProperty.call(message, "localeCountryIso31661Alpha2"))
                    writer.uint32(/* id 12, wireType 2 =*/98).string(message.localeCountryIso31661Alpha2);
                if (message.deviceBoard != null && Object.hasOwnProperty.call(message, "deviceBoard"))
                    writer.uint32(/* id 13, wireType 2 =*/106).string(message.deviceBoard);
                if (message.deviceExpId != null && Object.hasOwnProperty.call(message, "deviceExpId"))
                    writer.uint32(/* id 14, wireType 2 =*/114).string(message.deviceExpId);
                if (message.deviceType != null && Object.hasOwnProperty.call(message, "deviceType"))
                    writer.uint32(/* id 15, wireType 0 =*/120).int32(message.deviceType);
                if (message.deviceModelType != null && Object.hasOwnProperty.call(message, "deviceModelType"))
                    writer.uint32(/* id 16, wireType 2 =*/130).string(message.deviceModelType);
                if (message.distributionChannel != null && Object.hasOwnProperty.call(message, "distributionChannel"))
                    writer.uint32(/* id 17, wireType 0 =*/136).int32(message.distributionChannel);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified UserAgent message, length delimited. Does not implicitly {@link proto.ClientPayload.UserAgent.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.ClientPayload.UserAgent
             * @static
             * @param {proto.ClientPayload.IUserAgent} message UserAgent message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UserAgent.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a UserAgent message from the specified reader or buffer.
             * @function decode
             * @memberof proto.ClientPayload.UserAgent
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.ClientPayload.UserAgent} UserAgent
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UserAgent.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.platform = reader.int32();
                            message._platform = "platform";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.appVersion = $root.proto.ClientPayload.UserAgent.AppVersion.decode(reader, reader.uint32(), undefined, q + 1, message.appVersion);
                            message._appVersion = "appVersion";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.mcc = reader.stringVerify();
                            message._mcc = "mcc";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.mnc = reader.stringVerify();
                            message._mnc = "mnc";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.osVersion = reader.stringVerify();
                            message._osVersion = "osVersion";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.manufacturer = reader.stringVerify();
                            message._manufacturer = "manufacturer";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.device = reader.stringVerify();
                            message._device = "device";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.osBuildNumber = reader.stringVerify();
                            message._osBuildNumber = "osBuildNumber";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            message.phoneId = reader.stringVerify();
                            message._phoneId = "phoneId";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            message.releaseChannel = reader.int32();
                            message._releaseChannel = "releaseChannel";
                            continue;
                        }
                    case 11: {
                            if (u !== 2)
                                break;
                            message.localeLanguageIso6391 = reader.stringVerify();
                            message._localeLanguageIso6391 = "localeLanguageIso6391";
                            continue;
                        }
                    case 12: {
                            if (u !== 2)
                                break;
                            message.localeCountryIso31661Alpha2 = reader.stringVerify();
                            message._localeCountryIso31661Alpha2 = "localeCountryIso31661Alpha2";
                            continue;
                        }
                    case 13: {
                            if (u !== 2)
                                break;
                            message.deviceBoard = reader.stringVerify();
                            message._deviceBoard = "deviceBoard";
                            continue;
                        }
                    case 14: {
                            if (u !== 2)
                                break;
                            message.deviceExpId = reader.stringVerify();
                            message._deviceExpId = "deviceExpId";
                            continue;
                        }
                    case 15: {
                            if (u !== 0)
                                break;
                            message.deviceType = reader.int32();
                            message._deviceType = "deviceType";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            message.deviceModelType = reader.stringVerify();
                            message._deviceModelType = "deviceModelType";
                            continue;
                        }
                    case 17: {
                            if (u !== 0)
                                break;
                            message.distributionChannel = reader.int32();
                            message._distributionChannel = "distributionChannel";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a UserAgent message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.ClientPayload.UserAgent
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.ClientPayload.UserAgent} UserAgent
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UserAgent.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a UserAgent message.
             * @function verify
             * @memberof proto.ClientPayload.UserAgent
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            UserAgent.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.platform != null && Object.hasOwnProperty.call(message, "platform")) {
                    properties._platform = 1;
                    if (typeof message.platform !== "number" || (message.platform | 0) !== message.platform)
                        return "platform: enum value expected";
                }
                if (message.appVersion != null && Object.hasOwnProperty.call(message, "appVersion")) {
                    properties._appVersion = 1;
                    {
                        var error = $root.proto.ClientPayload.UserAgent.AppVersion.verify(message.appVersion, q + 1);
                        if (error)
                            return "appVersion." + error;
                    }
                }
                if (message.mcc != null && Object.hasOwnProperty.call(message, "mcc")) {
                    properties._mcc = 1;
                    if (!$util.isString(message.mcc))
                        return "mcc: string expected";
                }
                if (message.mnc != null && Object.hasOwnProperty.call(message, "mnc")) {
                    properties._mnc = 1;
                    if (!$util.isString(message.mnc))
                        return "mnc: string expected";
                }
                if (message.osVersion != null && Object.hasOwnProperty.call(message, "osVersion")) {
                    properties._osVersion = 1;
                    if (!$util.isString(message.osVersion))
                        return "osVersion: string expected";
                }
                if (message.manufacturer != null && Object.hasOwnProperty.call(message, "manufacturer")) {
                    properties._manufacturer = 1;
                    if (!$util.isString(message.manufacturer))
                        return "manufacturer: string expected";
                }
                if (message.device != null && Object.hasOwnProperty.call(message, "device")) {
                    properties._device = 1;
                    if (!$util.isString(message.device))
                        return "device: string expected";
                }
                if (message.osBuildNumber != null && Object.hasOwnProperty.call(message, "osBuildNumber")) {
                    properties._osBuildNumber = 1;
                    if (!$util.isString(message.osBuildNumber))
                        return "osBuildNumber: string expected";
                }
                if (message.phoneId != null && Object.hasOwnProperty.call(message, "phoneId")) {
                    properties._phoneId = 1;
                    if (!$util.isString(message.phoneId))
                        return "phoneId: string expected";
                }
                if (message.releaseChannel != null && Object.hasOwnProperty.call(message, "releaseChannel")) {
                    properties._releaseChannel = 1;
                    if (typeof message.releaseChannel !== "number" || (message.releaseChannel | 0) !== message.releaseChannel)
                        return "releaseChannel: enum value expected";
                }
                if (message.localeLanguageIso6391 != null && Object.hasOwnProperty.call(message, "localeLanguageIso6391")) {
                    properties._localeLanguageIso6391 = 1;
                    if (!$util.isString(message.localeLanguageIso6391))
                        return "localeLanguageIso6391: string expected";
                }
                if (message.localeCountryIso31661Alpha2 != null && Object.hasOwnProperty.call(message, "localeCountryIso31661Alpha2")) {
                    properties._localeCountryIso31661Alpha2 = 1;
                    if (!$util.isString(message.localeCountryIso31661Alpha2))
                        return "localeCountryIso31661Alpha2: string expected";
                }
                if (message.deviceBoard != null && Object.hasOwnProperty.call(message, "deviceBoard")) {
                    properties._deviceBoard = 1;
                    if (!$util.isString(message.deviceBoard))
                        return "deviceBoard: string expected";
                }
                if (message.deviceExpId != null && Object.hasOwnProperty.call(message, "deviceExpId")) {
                    properties._deviceExpId = 1;
                    if (!$util.isString(message.deviceExpId))
                        return "deviceExpId: string expected";
                }
                if (message.deviceType != null && Object.hasOwnProperty.call(message, "deviceType")) {
                    properties._deviceType = 1;
                    if (typeof message.deviceType !== "number" || (message.deviceType | 0) !== message.deviceType)
                        return "deviceType: enum value expected";
                }
                if (message.deviceModelType != null && Object.hasOwnProperty.call(message, "deviceModelType")) {
                    properties._deviceModelType = 1;
                    if (!$util.isString(message.deviceModelType))
                        return "deviceModelType: string expected";
                }
                if (message.distributionChannel != null && Object.hasOwnProperty.call(message, "distributionChannel")) {
                    properties._distributionChannel = 1;
                    if (typeof message.distributionChannel !== "number" || (message.distributionChannel | 0) !== message.distributionChannel)
                        return "distributionChannel: enum value expected";
                }
                return null;
            };

            /**
             * Creates a UserAgent message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.ClientPayload.UserAgent
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.ClientPayload.UserAgent} UserAgent
             */
            UserAgent.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.ClientPayload.UserAgent: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.platform) {
                case "ANDROID":
                case 0:
                    message.platform = 0;
                    break;
                case "IOS":
                case 1:
                    message.platform = 1;
                    break;
                case "WINDOWS_PHONE":
                case 2:
                    message.platform = 2;
                    break;
                case "BLACKBERRY":
                case 3:
                    message.platform = 3;
                    break;
                case "BLACKBERRYX":
                case 4:
                    message.platform = 4;
                    break;
                case "S40":
                case 5:
                    message.platform = 5;
                    break;
                case "S60":
                case 6:
                    message.platform = 6;
                    break;
                case "PYTHON_CLIENT":
                case 7:
                    message.platform = 7;
                    break;
                case "TIZEN":
                case 8:
                    message.platform = 8;
                    break;
                case "ENTERPRISE":
                case 9:
                    message.platform = 9;
                    break;
                case "SMB_ANDROID":
                case 10:
                    message.platform = 10;
                    break;
                case "KAIOS":
                case 11:
                    message.platform = 11;
                    break;
                case "SMB_IOS":
                case 12:
                    message.platform = 12;
                    break;
                case "WINDOWS":
                case 13:
                    message.platform = 13;
                    break;
                case "WEB":
                case 14:
                    message.platform = 14;
                    break;
                case "PORTAL":
                case 15:
                    message.platform = 15;
                    break;
                case "GREEN_ANDROID":
                case 16:
                    message.platform = 16;
                    break;
                case "GREEN_IPHONE":
                case 17:
                    message.platform = 17;
                    break;
                case "BLUE_ANDROID":
                case 18:
                    message.platform = 18;
                    break;
                case "BLUE_IPHONE":
                case 19:
                    message.platform = 19;
                    break;
                case "FBLITE_ANDROID":
                case 20:
                    message.platform = 20;
                    break;
                case "MLITE_ANDROID":
                case 21:
                    message.platform = 21;
                    break;
                case "IGLITE_ANDROID":
                case 22:
                    message.platform = 22;
                    break;
                case "PAGE":
                case 23:
                    message.platform = 23;
                    break;
                case "MACOS":
                case 24:
                    message.platform = 24;
                    break;
                case "OCULUS_MSG":
                case 25:
                    message.platform = 25;
                    break;
                case "OCULUS_CALL":
                case 26:
                    message.platform = 26;
                    break;
                case "MILAN":
                case 27:
                    message.platform = 27;
                    break;
                case "CAPI":
                case 28:
                    message.platform = 28;
                    break;
                case "WEAROS":
                case 29:
                    message.platform = 29;
                    break;
                case "ARDEVICE":
                case 30:
                    message.platform = 30;
                    break;
                case "VRDEVICE":
                case 31:
                    message.platform = 31;
                    break;
                case "BLUE_WEB":
                case 32:
                    message.platform = 32;
                    break;
                case "IPAD":
                case 33:
                    message.platform = 33;
                    break;
                case "TEST":
                case 34:
                    message.platform = 34;
                    break;
                case "SMART_GLASSES":
                case 35:
                    message.platform = 35;
                    break;
                case "BLUE_VR":
                case 36:
                    message.platform = 36;
                    break;
                case "AR_WRIST":
                case 37:
                    message.platform = 37;
                    break;
                default:
                    if (typeof object.platform === "number" && (object.platform | 0) === object.platform)
                        message.platform = object.platform;
                }
                if (object.appVersion != null) {
                    if (!$util.isObject(object.appVersion))
                        throw TypeError(".proto.ClientPayload.UserAgent.appVersion: object expected");
                    message.appVersion = $root.proto.ClientPayload.UserAgent.AppVersion.fromObject(object.appVersion, q + 1);
                }
                if (object.mcc != null)
                    message.mcc = String(object.mcc);
                if (object.mnc != null)
                    message.mnc = String(object.mnc);
                if (object.osVersion != null)
                    message.osVersion = String(object.osVersion);
                if (object.manufacturer != null)
                    message.manufacturer = String(object.manufacturer);
                if (object.device != null)
                    message.device = String(object.device);
                if (object.osBuildNumber != null)
                    message.osBuildNumber = String(object.osBuildNumber);
                if (object.phoneId != null)
                    message.phoneId = String(object.phoneId);
                switch (object.releaseChannel) {
                case "RELEASE":
                case 0:
                    message.releaseChannel = 0;
                    break;
                case "BETA":
                case 1:
                    message.releaseChannel = 1;
                    break;
                case "ALPHA":
                case 2:
                    message.releaseChannel = 2;
                    break;
                case "DEBUG":
                case 3:
                    message.releaseChannel = 3;
                    break;
                default:
                    if (typeof object.releaseChannel === "number" && (object.releaseChannel | 0) === object.releaseChannel)
                        message.releaseChannel = object.releaseChannel;
                }
                if (object.localeLanguageIso6391 != null)
                    message.localeLanguageIso6391 = String(object.localeLanguageIso6391);
                if (object.localeCountryIso31661Alpha2 != null)
                    message.localeCountryIso31661Alpha2 = String(object.localeCountryIso31661Alpha2);
                if (object.deviceBoard != null)
                    message.deviceBoard = String(object.deviceBoard);
                if (object.deviceExpId != null)
                    message.deviceExpId = String(object.deviceExpId);
                switch (object.deviceType) {
                case "PHONE":
                case 0:
                    message.deviceType = 0;
                    break;
                case "TABLET":
                case 1:
                    message.deviceType = 1;
                    break;
                case "DESKTOP":
                case 2:
                    message.deviceType = 2;
                    break;
                case "WEARABLE":
                case 3:
                    message.deviceType = 3;
                    break;
                case "VR":
                case 4:
                    message.deviceType = 4;
                    break;
                default:
                    if (typeof object.deviceType === "number" && (object.deviceType | 0) === object.deviceType)
                        message.deviceType = object.deviceType;
                }
                if (object.deviceModelType != null)
                    message.deviceModelType = String(object.deviceModelType);
                switch (object.distributionChannel) {
                case "APPSTORE":
                case 0:
                    message.distributionChannel = 0;
                    break;
                case "WEBSITE":
                case 1:
                    message.distributionChannel = 1;
                    break;
                case "TESTFLIGHT":
                case 2:
                    message.distributionChannel = 2;
                    break;
                case "INTERNAL":
                case 3:
                    message.distributionChannel = 3;
                    break;
                default:
                    if (typeof object.distributionChannel === "number" && (object.distributionChannel | 0) === object.distributionChannel)
                        message.distributionChannel = object.distributionChannel;
                }
                return message;
            };

            /**
             * Creates a plain object from a UserAgent message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.ClientPayload.UserAgent
             * @static
             * @param {proto.ClientPayload.UserAgent} message UserAgent
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            UserAgent.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.platform != null && Object.hasOwnProperty.call(message, "platform"))
                    object.platform = options.enums === String ? $root.proto.ClientPayload.UserAgent.Platform[message.platform] === undefined ? message.platform : $root.proto.ClientPayload.UserAgent.Platform[message.platform] : message.platform;
                if (message.appVersion != null && Object.hasOwnProperty.call(message, "appVersion"))
                    object.appVersion = $root.proto.ClientPayload.UserAgent.AppVersion.toObject(message.appVersion, options, q + 1);
                if (message.mcc != null && Object.hasOwnProperty.call(message, "mcc"))
                    object.mcc = message.mcc;
                if (message.mnc != null && Object.hasOwnProperty.call(message, "mnc"))
                    object.mnc = message.mnc;
                if (message.osVersion != null && Object.hasOwnProperty.call(message, "osVersion"))
                    object.osVersion = message.osVersion;
                if (message.manufacturer != null && Object.hasOwnProperty.call(message, "manufacturer"))
                    object.manufacturer = message.manufacturer;
                if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                    object.device = message.device;
                if (message.osBuildNumber != null && Object.hasOwnProperty.call(message, "osBuildNumber"))
                    object.osBuildNumber = message.osBuildNumber;
                if (message.phoneId != null && Object.hasOwnProperty.call(message, "phoneId"))
                    object.phoneId = message.phoneId;
                if (message.releaseChannel != null && Object.hasOwnProperty.call(message, "releaseChannel"))
                    object.releaseChannel = options.enums === String ? $root.proto.ClientPayload.UserAgent.ReleaseChannel[message.releaseChannel] === undefined ? message.releaseChannel : $root.proto.ClientPayload.UserAgent.ReleaseChannel[message.releaseChannel] : message.releaseChannel;
                if (message.localeLanguageIso6391 != null && Object.hasOwnProperty.call(message, "localeLanguageIso6391"))
                    object.localeLanguageIso6391 = message.localeLanguageIso6391;
                if (message.localeCountryIso31661Alpha2 != null && Object.hasOwnProperty.call(message, "localeCountryIso31661Alpha2"))
                    object.localeCountryIso31661Alpha2 = message.localeCountryIso31661Alpha2;
                if (message.deviceBoard != null && Object.hasOwnProperty.call(message, "deviceBoard"))
                    object.deviceBoard = message.deviceBoard;
                if (message.deviceExpId != null && Object.hasOwnProperty.call(message, "deviceExpId"))
                    object.deviceExpId = message.deviceExpId;
                if (message.deviceType != null && Object.hasOwnProperty.call(message, "deviceType"))
                    object.deviceType = options.enums === String ? $root.proto.ClientPayload.UserAgent.DeviceType[message.deviceType] === undefined ? message.deviceType : $root.proto.ClientPayload.UserAgent.DeviceType[message.deviceType] : message.deviceType;
                if (message.deviceModelType != null && Object.hasOwnProperty.call(message, "deviceModelType"))
                    object.deviceModelType = message.deviceModelType;
                if (message.distributionChannel != null && Object.hasOwnProperty.call(message, "distributionChannel"))
                    object.distributionChannel = options.enums === String ? $root.proto.ClientPayload.UserAgent.DistributionChannel[message.distributionChannel] === undefined ? message.distributionChannel : $root.proto.ClientPayload.UserAgent.DistributionChannel[message.distributionChannel] : message.distributionChannel;
                return object;
            };

            /**
             * Converts this UserAgent to JSON.
             * @function toJSON
             * @memberof proto.ClientPayload.UserAgent
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            UserAgent.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for UserAgent
             * @function getTypeUrl
             * @memberof proto.ClientPayload.UserAgent
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            UserAgent.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.ClientPayload.UserAgent";
            };

            UserAgent.AppVersion = (function() {

                /**
                 * Properties of an AppVersion.
                 * @memberof proto.ClientPayload.UserAgent
                 * @interface IAppVersion
                 * @property {number|null} [primary] AppVersion primary
                 * @property {number|null} [secondary] AppVersion secondary
                 * @property {number|null} [tertiary] AppVersion tertiary
                 * @property {number|null} [quaternary] AppVersion quaternary
                 * @property {number|null} [quinary] AppVersion quinary
                 */

                /**
                 * Constructs a new AppVersion.
                 * @memberof proto.ClientPayload.UserAgent
                 * @classdesc Represents an AppVersion.
                 * @implements IAppVersion
                 * @constructor
                 * @param {proto.ClientPayload.UserAgent.IAppVersion=} [properties] Properties to set
                 */
                function AppVersion(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * AppVersion primary.
                 * @member {number|null|undefined} primary
                 * @memberof proto.ClientPayload.UserAgent.AppVersion
                 * @instance
                 */
                AppVersion.prototype.primary = null;

                /**
                 * AppVersion secondary.
                 * @member {number|null|undefined} secondary
                 * @memberof proto.ClientPayload.UserAgent.AppVersion
                 * @instance
                 */
                AppVersion.prototype.secondary = null;

                /**
                 * AppVersion tertiary.
                 * @member {number|null|undefined} tertiary
                 * @memberof proto.ClientPayload.UserAgent.AppVersion
                 * @instance
                 */
                AppVersion.prototype.tertiary = null;

                /**
                 * AppVersion quaternary.
                 * @member {number|null|undefined} quaternary
                 * @memberof proto.ClientPayload.UserAgent.AppVersion
                 * @instance
                 */
                AppVersion.prototype.quaternary = null;

                /**
                 * AppVersion quinary.
                 * @member {number|null|undefined} quinary
                 * @memberof proto.ClientPayload.UserAgent.AppVersion
                 * @instance
                 */
                AppVersion.prototype.quinary = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(AppVersion.prototype, "_primary", {
                    get: $util.oneOfGetter($oneOfFields = ["primary"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(AppVersion.prototype, "_secondary", {
                    get: $util.oneOfGetter($oneOfFields = ["secondary"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(AppVersion.prototype, "_tertiary", {
                    get: $util.oneOfGetter($oneOfFields = ["tertiary"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(AppVersion.prototype, "_quaternary", {
                    get: $util.oneOfGetter($oneOfFields = ["quaternary"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(AppVersion.prototype, "_quinary", {
                    get: $util.oneOfGetter($oneOfFields = ["quinary"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new AppVersion instance using the specified properties.
                 * @function create
                 * @memberof proto.ClientPayload.UserAgent.AppVersion
                 * @static
                 * @param {proto.ClientPayload.UserAgent.IAppVersion=} [properties] Properties to set
                 * @returns {proto.ClientPayload.UserAgent.AppVersion} AppVersion instance
                 */
                AppVersion.create = function create(properties) {
                    return new AppVersion(properties);
                };

                /**
                 * Encodes the specified AppVersion message. Does not implicitly {@link proto.ClientPayload.UserAgent.AppVersion.verify|verify} messages.
                 * @function encode
                 * @memberof proto.ClientPayload.UserAgent.AppVersion
                 * @static
                 * @param {proto.ClientPayload.UserAgent.IAppVersion} message AppVersion message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                AppVersion.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.primary != null && Object.hasOwnProperty.call(message, "primary"))
                        writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.primary);
                    if (message.secondary != null && Object.hasOwnProperty.call(message, "secondary"))
                        writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.secondary);
                    if (message.tertiary != null && Object.hasOwnProperty.call(message, "tertiary"))
                        writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.tertiary);
                    if (message.quaternary != null && Object.hasOwnProperty.call(message, "quaternary"))
                        writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.quaternary);
                    if (message.quinary != null && Object.hasOwnProperty.call(message, "quinary"))
                        writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.quinary);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified AppVersion message, length delimited. Does not implicitly {@link proto.ClientPayload.UserAgent.AppVersion.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.ClientPayload.UserAgent.AppVersion
                 * @static
                 * @param {proto.ClientPayload.UserAgent.IAppVersion} message AppVersion message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                AppVersion.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes an AppVersion message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.ClientPayload.UserAgent.AppVersion
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.ClientPayload.UserAgent.AppVersion} AppVersion
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                AppVersion.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                message.primary = reader.uint32();
                                message._primary = "primary";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                message.secondary = reader.uint32();
                                message._secondary = "secondary";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                message.tertiary = reader.uint32();
                                message._tertiary = "tertiary";
                                continue;
                            }
                        case 4: {
                                if (u !== 0)
                                    break;
                                message.quaternary = reader.uint32();
                                message._quaternary = "quaternary";
                                continue;
                            }
                        case 5: {
                                if (u !== 0)
                                    break;
                                message.quinary = reader.uint32();
                                message._quinary = "quinary";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes an AppVersion message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.ClientPayload.UserAgent.AppVersion
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.ClientPayload.UserAgent.AppVersion} AppVersion
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                AppVersion.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies an AppVersion message.
                 * @function verify
                 * @memberof proto.ClientPayload.UserAgent.AppVersion
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                AppVersion.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.primary != null && Object.hasOwnProperty.call(message, "primary")) {
                        properties._primary = 1;
                        if (!$util.isInteger(message.primary))
                            return "primary: integer expected";
                    }
                    if (message.secondary != null && Object.hasOwnProperty.call(message, "secondary")) {
                        properties._secondary = 1;
                        if (!$util.isInteger(message.secondary))
                            return "secondary: integer expected";
                    }
                    if (message.tertiary != null && Object.hasOwnProperty.call(message, "tertiary")) {
                        properties._tertiary = 1;
                        if (!$util.isInteger(message.tertiary))
                            return "tertiary: integer expected";
                    }
                    if (message.quaternary != null && Object.hasOwnProperty.call(message, "quaternary")) {
                        properties._quaternary = 1;
                        if (!$util.isInteger(message.quaternary))
                            return "quaternary: integer expected";
                    }
                    if (message.quinary != null && Object.hasOwnProperty.call(message, "quinary")) {
                        properties._quinary = 1;
                        if (!$util.isInteger(message.quinary))
                            return "quinary: integer expected";
                    }
                    return null;
                };

                /**
                 * Creates an AppVersion message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.ClientPayload.UserAgent.AppVersion
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.ClientPayload.UserAgent.AppVersion} AppVersion
                 */
                AppVersion.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.ClientPayload.UserAgent.AppVersion: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.primary != null)
                        message.primary = object.primary >>> 0;
                    if (object.secondary != null)
                        message.secondary = object.secondary >>> 0;
                    if (object.tertiary != null)
                        message.tertiary = object.tertiary >>> 0;
                    if (object.quaternary != null)
                        message.quaternary = object.quaternary >>> 0;
                    if (object.quinary != null)
                        message.quinary = object.quinary >>> 0;
                    return message;
                };

                /**
                 * Creates a plain object from an AppVersion message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.ClientPayload.UserAgent.AppVersion
                 * @static
                 * @param {proto.ClientPayload.UserAgent.AppVersion} message AppVersion
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                AppVersion.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.primary != null && Object.hasOwnProperty.call(message, "primary"))
                        object.primary = message.primary;
                    if (message.secondary != null && Object.hasOwnProperty.call(message, "secondary"))
                        object.secondary = message.secondary;
                    if (message.tertiary != null && Object.hasOwnProperty.call(message, "tertiary"))
                        object.tertiary = message.tertiary;
                    if (message.quaternary != null && Object.hasOwnProperty.call(message, "quaternary"))
                        object.quaternary = message.quaternary;
                    if (message.quinary != null && Object.hasOwnProperty.call(message, "quinary"))
                        object.quinary = message.quinary;
                    return object;
                };

                /**
                 * Converts this AppVersion to JSON.
                 * @function toJSON
                 * @memberof proto.ClientPayload.UserAgent.AppVersion
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                AppVersion.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for AppVersion
                 * @function getTypeUrl
                 * @memberof proto.ClientPayload.UserAgent.AppVersion
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                AppVersion.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.ClientPayload.UserAgent.AppVersion";
                };

                return AppVersion;
            })();

            /**
             * DeviceType enum.
             * @name proto.ClientPayload.UserAgent.DeviceType
             * @enum {number}
             * @property {number} PHONE=0 PHONE value
             * @property {number} TABLET=1 TABLET value
             * @property {number} DESKTOP=2 DESKTOP value
             * @property {number} WEARABLE=3 WEARABLE value
             * @property {number} VR=4 VR value
             */
            UserAgent.DeviceType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "PHONE"] = 0;
                values[valuesById[1] = "TABLET"] = 1;
                values[valuesById[2] = "DESKTOP"] = 2;
                values[valuesById[3] = "WEARABLE"] = 3;
                values[valuesById[4] = "VR"] = 4;
                return values;
            })();

            /**
             * DistributionChannel enum.
             * @name proto.ClientPayload.UserAgent.DistributionChannel
             * @enum {number}
             * @property {number} APPSTORE=0 APPSTORE value
             * @property {number} WEBSITE=1 WEBSITE value
             * @property {number} TESTFLIGHT=2 TESTFLIGHT value
             * @property {number} INTERNAL=3 INTERNAL value
             */
            UserAgent.DistributionChannel = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "APPSTORE"] = 0;
                values[valuesById[1] = "WEBSITE"] = 1;
                values[valuesById[2] = "TESTFLIGHT"] = 2;
                values[valuesById[3] = "INTERNAL"] = 3;
                return values;
            })();

            /**
             * Platform enum.
             * @name proto.ClientPayload.UserAgent.Platform
             * @enum {number}
             * @property {number} ANDROID=0 ANDROID value
             * @property {number} IOS=1 IOS value
             * @property {number} WINDOWS_PHONE=2 WINDOWS_PHONE value
             * @property {number} BLACKBERRY=3 BLACKBERRY value
             * @property {number} BLACKBERRYX=4 BLACKBERRYX value
             * @property {number} S40=5 S40 value
             * @property {number} S60=6 S60 value
             * @property {number} PYTHON_CLIENT=7 PYTHON_CLIENT value
             * @property {number} TIZEN=8 TIZEN value
             * @property {number} ENTERPRISE=9 ENTERPRISE value
             * @property {number} SMB_ANDROID=10 SMB_ANDROID value
             * @property {number} KAIOS=11 KAIOS value
             * @property {number} SMB_IOS=12 SMB_IOS value
             * @property {number} WINDOWS=13 WINDOWS value
             * @property {number} WEB=14 WEB value
             * @property {number} PORTAL=15 PORTAL value
             * @property {number} GREEN_ANDROID=16 GREEN_ANDROID value
             * @property {number} GREEN_IPHONE=17 GREEN_IPHONE value
             * @property {number} BLUE_ANDROID=18 BLUE_ANDROID value
             * @property {number} BLUE_IPHONE=19 BLUE_IPHONE value
             * @property {number} FBLITE_ANDROID=20 FBLITE_ANDROID value
             * @property {number} MLITE_ANDROID=21 MLITE_ANDROID value
             * @property {number} IGLITE_ANDROID=22 IGLITE_ANDROID value
             * @property {number} PAGE=23 PAGE value
             * @property {number} MACOS=24 MACOS value
             * @property {number} OCULUS_MSG=25 OCULUS_MSG value
             * @property {number} OCULUS_CALL=26 OCULUS_CALL value
             * @property {number} MILAN=27 MILAN value
             * @property {number} CAPI=28 CAPI value
             * @property {number} WEAROS=29 WEAROS value
             * @property {number} ARDEVICE=30 ARDEVICE value
             * @property {number} VRDEVICE=31 VRDEVICE value
             * @property {number} BLUE_WEB=32 BLUE_WEB value
             * @property {number} IPAD=33 IPAD value
             * @property {number} TEST=34 TEST value
             * @property {number} SMART_GLASSES=35 SMART_GLASSES value
             * @property {number} BLUE_VR=36 BLUE_VR value
             * @property {number} AR_WRIST=37 AR_WRIST value
             */
            UserAgent.Platform = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "ANDROID"] = 0;
                values[valuesById[1] = "IOS"] = 1;
                values[valuesById[2] = "WINDOWS_PHONE"] = 2;
                values[valuesById[3] = "BLACKBERRY"] = 3;
                values[valuesById[4] = "BLACKBERRYX"] = 4;
                values[valuesById[5] = "S40"] = 5;
                values[valuesById[6] = "S60"] = 6;
                values[valuesById[7] = "PYTHON_CLIENT"] = 7;
                values[valuesById[8] = "TIZEN"] = 8;
                values[valuesById[9] = "ENTERPRISE"] = 9;
                values[valuesById[10] = "SMB_ANDROID"] = 10;
                values[valuesById[11] = "KAIOS"] = 11;
                values[valuesById[12] = "SMB_IOS"] = 12;
                values[valuesById[13] = "WINDOWS"] = 13;
                values[valuesById[14] = "WEB"] = 14;
                values[valuesById[15] = "PORTAL"] = 15;
                values[valuesById[16] = "GREEN_ANDROID"] = 16;
                values[valuesById[17] = "GREEN_IPHONE"] = 17;
                values[valuesById[18] = "BLUE_ANDROID"] = 18;
                values[valuesById[19] = "BLUE_IPHONE"] = 19;
                values[valuesById[20] = "FBLITE_ANDROID"] = 20;
                values[valuesById[21] = "MLITE_ANDROID"] = 21;
                values[valuesById[22] = "IGLITE_ANDROID"] = 22;
                values[valuesById[23] = "PAGE"] = 23;
                values[valuesById[24] = "MACOS"] = 24;
                values[valuesById[25] = "OCULUS_MSG"] = 25;
                values[valuesById[26] = "OCULUS_CALL"] = 26;
                values[valuesById[27] = "MILAN"] = 27;
                values[valuesById[28] = "CAPI"] = 28;
                values[valuesById[29] = "WEAROS"] = 29;
                values[valuesById[30] = "ARDEVICE"] = 30;
                values[valuesById[31] = "VRDEVICE"] = 31;
                values[valuesById[32] = "BLUE_WEB"] = 32;
                values[valuesById[33] = "IPAD"] = 33;
                values[valuesById[34] = "TEST"] = 34;
                values[valuesById[35] = "SMART_GLASSES"] = 35;
                values[valuesById[36] = "BLUE_VR"] = 36;
                values[valuesById[37] = "AR_WRIST"] = 37;
                return values;
            })();

            /**
             * ReleaseChannel enum.
             * @name proto.ClientPayload.UserAgent.ReleaseChannel
             * @enum {number}
             * @property {number} RELEASE=0 RELEASE value
             * @property {number} BETA=1 BETA value
             * @property {number} ALPHA=2 ALPHA value
             * @property {number} DEBUG=3 DEBUG value
             */
            UserAgent.ReleaseChannel = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "RELEASE"] = 0;
                values[valuesById[1] = "BETA"] = 1;
                values[valuesById[2] = "ALPHA"] = 2;
                values[valuesById[3] = "DEBUG"] = 3;
                return values;
            })();

            return UserAgent;
        })();

        ClientPayload.WebInfo = (function() {

            /**
             * Properties of a WebInfo.
             * @memberof proto.ClientPayload
             * @interface IWebInfo
             * @property {string|null} [refToken] WebInfo refToken
             * @property {string|null} [version] WebInfo version
             * @property {proto.ClientPayload.WebInfo.IWebdPayload|null} [webdPayload] WebInfo webdPayload
             * @property {proto.ClientPayload.WebInfo.WebSubPlatform|null} [webSubPlatform] WebInfo webSubPlatform
             * @property {string|null} [browser] WebInfo browser
             * @property {string|null} [browserVersion] WebInfo browserVersion
             */

            /**
             * Constructs a new WebInfo.
             * @memberof proto.ClientPayload
             * @classdesc Represents a WebInfo.
             * @implements IWebInfo
             * @constructor
             * @param {proto.ClientPayload.IWebInfo=} [properties] Properties to set
             */
            function WebInfo(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * WebInfo refToken.
             * @member {string|null|undefined} refToken
             * @memberof proto.ClientPayload.WebInfo
             * @instance
             */
            WebInfo.prototype.refToken = null;

            /**
             * WebInfo version.
             * @member {string|null|undefined} version
             * @memberof proto.ClientPayload.WebInfo
             * @instance
             */
            WebInfo.prototype.version = null;

            /**
             * WebInfo webdPayload.
             * @member {proto.ClientPayload.WebInfo.IWebdPayload|null|undefined} webdPayload
             * @memberof proto.ClientPayload.WebInfo
             * @instance
             */
            WebInfo.prototype.webdPayload = null;

            /**
             * WebInfo webSubPlatform.
             * @member {proto.ClientPayload.WebInfo.WebSubPlatform|null|undefined} webSubPlatform
             * @memberof proto.ClientPayload.WebInfo
             * @instance
             */
            WebInfo.prototype.webSubPlatform = null;

            /**
             * WebInfo browser.
             * @member {string|null|undefined} browser
             * @memberof proto.ClientPayload.WebInfo
             * @instance
             */
            WebInfo.prototype.browser = null;

            /**
             * WebInfo browserVersion.
             * @member {string|null|undefined} browserVersion
             * @memberof proto.ClientPayload.WebInfo
             * @instance
             */
            WebInfo.prototype.browserVersion = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(WebInfo.prototype, "_refToken", {
                get: $util.oneOfGetter($oneOfFields = ["refToken"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(WebInfo.prototype, "_version", {
                get: $util.oneOfGetter($oneOfFields = ["version"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(WebInfo.prototype, "_webdPayload", {
                get: $util.oneOfGetter($oneOfFields = ["webdPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(WebInfo.prototype, "_webSubPlatform", {
                get: $util.oneOfGetter($oneOfFields = ["webSubPlatform"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(WebInfo.prototype, "_browser", {
                get: $util.oneOfGetter($oneOfFields = ["browser"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(WebInfo.prototype, "_browserVersion", {
                get: $util.oneOfGetter($oneOfFields = ["browserVersion"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new WebInfo instance using the specified properties.
             * @function create
             * @memberof proto.ClientPayload.WebInfo
             * @static
             * @param {proto.ClientPayload.IWebInfo=} [properties] Properties to set
             * @returns {proto.ClientPayload.WebInfo} WebInfo instance
             */
            WebInfo.create = function create(properties) {
                return new WebInfo(properties);
            };

            /**
             * Encodes the specified WebInfo message. Does not implicitly {@link proto.ClientPayload.WebInfo.verify|verify} messages.
             * @function encode
             * @memberof proto.ClientPayload.WebInfo
             * @static
             * @param {proto.ClientPayload.IWebInfo} message WebInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            WebInfo.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.refToken != null && Object.hasOwnProperty.call(message, "refToken"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.refToken);
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.version);
                if (message.webdPayload != null && Object.hasOwnProperty.call(message, "webdPayload"))
                    $root.proto.ClientPayload.WebInfo.WebdPayload.encode(message.webdPayload, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.webSubPlatform != null && Object.hasOwnProperty.call(message, "webSubPlatform"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int32(message.webSubPlatform);
                if (message.browser != null && Object.hasOwnProperty.call(message, "browser"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.browser);
                if (message.browserVersion != null && Object.hasOwnProperty.call(message, "browserVersion"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.browserVersion);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified WebInfo message, length delimited. Does not implicitly {@link proto.ClientPayload.WebInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.ClientPayload.WebInfo
             * @static
             * @param {proto.ClientPayload.IWebInfo} message WebInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            WebInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a WebInfo message from the specified reader or buffer.
             * @function decode
             * @memberof proto.ClientPayload.WebInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.ClientPayload.WebInfo} WebInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            WebInfo.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.refToken = reader.stringVerify();
                            message._refToken = "refToken";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.version = reader.stringVerify();
                            message._version = "version";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.webdPayload = $root.proto.ClientPayload.WebInfo.WebdPayload.decode(reader, reader.uint32(), undefined, q + 1, message.webdPayload);
                            message._webdPayload = "webdPayload";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.webSubPlatform = reader.int32();
                            message._webSubPlatform = "webSubPlatform";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.browser = reader.stringVerify();
                            message._browser = "browser";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.browserVersion = reader.stringVerify();
                            message._browserVersion = "browserVersion";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a WebInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.ClientPayload.WebInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.ClientPayload.WebInfo} WebInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            WebInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a WebInfo message.
             * @function verify
             * @memberof proto.ClientPayload.WebInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            WebInfo.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.refToken != null && Object.hasOwnProperty.call(message, "refToken")) {
                    properties._refToken = 1;
                    if (!$util.isString(message.refToken))
                        return "refToken: string expected";
                }
                if (message.version != null && Object.hasOwnProperty.call(message, "version")) {
                    properties._version = 1;
                    if (!$util.isString(message.version))
                        return "version: string expected";
                }
                if (message.webdPayload != null && Object.hasOwnProperty.call(message, "webdPayload")) {
                    properties._webdPayload = 1;
                    {
                        var error = $root.proto.ClientPayload.WebInfo.WebdPayload.verify(message.webdPayload, q + 1);
                        if (error)
                            return "webdPayload." + error;
                    }
                }
                if (message.webSubPlatform != null && Object.hasOwnProperty.call(message, "webSubPlatform")) {
                    properties._webSubPlatform = 1;
                    if (typeof message.webSubPlatform !== "number" || (message.webSubPlatform | 0) !== message.webSubPlatform)
                        return "webSubPlatform: enum value expected";
                }
                if (message.browser != null && Object.hasOwnProperty.call(message, "browser")) {
                    properties._browser = 1;
                    if (!$util.isString(message.browser))
                        return "browser: string expected";
                }
                if (message.browserVersion != null && Object.hasOwnProperty.call(message, "browserVersion")) {
                    properties._browserVersion = 1;
                    if (!$util.isString(message.browserVersion))
                        return "browserVersion: string expected";
                }
                return null;
            };

            /**
             * Creates a WebInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.ClientPayload.WebInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.ClientPayload.WebInfo} WebInfo
             */
            WebInfo.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.ClientPayload.WebInfo: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.refToken != null)
                    message.refToken = String(object.refToken);
                if (object.version != null)
                    message.version = String(object.version);
                if (object.webdPayload != null) {
                    if (!$util.isObject(object.webdPayload))
                        throw TypeError(".proto.ClientPayload.WebInfo.webdPayload: object expected");
                    message.webdPayload = $root.proto.ClientPayload.WebInfo.WebdPayload.fromObject(object.webdPayload, q + 1);
                }
                switch (object.webSubPlatform) {
                case "WEB_BROWSER":
                case 0:
                    message.webSubPlatform = 0;
                    break;
                case "APP_STORE":
                case 1:
                    message.webSubPlatform = 1;
                    break;
                case "WIN_STORE":
                case 2:
                    message.webSubPlatform = 2;
                    break;
                case "DARWIN":
                case 3:
                    message.webSubPlatform = 3;
                    break;
                case "WIN32":
                case 4:
                    message.webSubPlatform = 4;
                    break;
                case "WIN_HYBRID":
                case 5:
                    message.webSubPlatform = 5;
                    break;
                default:
                    if (typeof object.webSubPlatform === "number" && (object.webSubPlatform | 0) === object.webSubPlatform)
                        message.webSubPlatform = object.webSubPlatform;
                }
                if (object.browser != null)
                    message.browser = String(object.browser);
                if (object.browserVersion != null)
                    message.browserVersion = String(object.browserVersion);
                return message;
            };

            /**
             * Creates a plain object from a WebInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.ClientPayload.WebInfo
             * @static
             * @param {proto.ClientPayload.WebInfo} message WebInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            WebInfo.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.refToken != null && Object.hasOwnProperty.call(message, "refToken"))
                    object.refToken = message.refToken;
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    object.version = message.version;
                if (message.webdPayload != null && Object.hasOwnProperty.call(message, "webdPayload"))
                    object.webdPayload = $root.proto.ClientPayload.WebInfo.WebdPayload.toObject(message.webdPayload, options, q + 1);
                if (message.webSubPlatform != null && Object.hasOwnProperty.call(message, "webSubPlatform"))
                    object.webSubPlatform = options.enums === String ? $root.proto.ClientPayload.WebInfo.WebSubPlatform[message.webSubPlatform] === undefined ? message.webSubPlatform : $root.proto.ClientPayload.WebInfo.WebSubPlatform[message.webSubPlatform] : message.webSubPlatform;
                if (message.browser != null && Object.hasOwnProperty.call(message, "browser"))
                    object.browser = message.browser;
                if (message.browserVersion != null && Object.hasOwnProperty.call(message, "browserVersion"))
                    object.browserVersion = message.browserVersion;
                return object;
            };

            /**
             * Converts this WebInfo to JSON.
             * @function toJSON
             * @memberof proto.ClientPayload.WebInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            WebInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for WebInfo
             * @function getTypeUrl
             * @memberof proto.ClientPayload.WebInfo
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            WebInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.ClientPayload.WebInfo";
            };

            /**
             * WebSubPlatform enum.
             * @name proto.ClientPayload.WebInfo.WebSubPlatform
             * @enum {number}
             * @property {number} WEB_BROWSER=0 WEB_BROWSER value
             * @property {number} APP_STORE=1 APP_STORE value
             * @property {number} WIN_STORE=2 WIN_STORE value
             * @property {number} DARWIN=3 DARWIN value
             * @property {number} WIN32=4 WIN32 value
             * @property {number} WIN_HYBRID=5 WIN_HYBRID value
             */
            WebInfo.WebSubPlatform = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "WEB_BROWSER"] = 0;
                values[valuesById[1] = "APP_STORE"] = 1;
                values[valuesById[2] = "WIN_STORE"] = 2;
                values[valuesById[3] = "DARWIN"] = 3;
                values[valuesById[4] = "WIN32"] = 4;
                values[valuesById[5] = "WIN_HYBRID"] = 5;
                return values;
            })();

            WebInfo.WebdPayload = (function() {

                /**
                 * Properties of a WebdPayload.
                 * @memberof proto.ClientPayload.WebInfo
                 * @interface IWebdPayload
                 * @property {boolean|null} [usesParticipantInKey] WebdPayload usesParticipantInKey
                 * @property {boolean|null} [supportsStarredMessages] WebdPayload supportsStarredMessages
                 * @property {boolean|null} [supportsDocumentMessages] WebdPayload supportsDocumentMessages
                 * @property {boolean|null} [supportsUrlMessages] WebdPayload supportsUrlMessages
                 * @property {boolean|null} [supportsMediaRetry] WebdPayload supportsMediaRetry
                 * @property {boolean|null} [supportsE2EImage] WebdPayload supportsE2EImage
                 * @property {boolean|null} [supportsE2EVideo] WebdPayload supportsE2EVideo
                 * @property {boolean|null} [supportsE2EAudio] WebdPayload supportsE2EAudio
                 * @property {boolean|null} [supportsE2EDocument] WebdPayload supportsE2EDocument
                 * @property {string|null} [documentTypes] WebdPayload documentTypes
                 * @property {Uint8Array|null} [features] WebdPayload features
                 */

                /**
                 * Constructs a new WebdPayload.
                 * @memberof proto.ClientPayload.WebInfo
                 * @classdesc Represents a WebdPayload.
                 * @implements IWebdPayload
                 * @constructor
                 * @param {proto.ClientPayload.WebInfo.IWebdPayload=} [properties] Properties to set
                 */
                function WebdPayload(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * WebdPayload usesParticipantInKey.
                 * @member {boolean|null|undefined} usesParticipantInKey
                 * @memberof proto.ClientPayload.WebInfo.WebdPayload
                 * @instance
                 */
                WebdPayload.prototype.usesParticipantInKey = null;

                /**
                 * WebdPayload supportsStarredMessages.
                 * @member {boolean|null|undefined} supportsStarredMessages
                 * @memberof proto.ClientPayload.WebInfo.WebdPayload
                 * @instance
                 */
                WebdPayload.prototype.supportsStarredMessages = null;

                /**
                 * WebdPayload supportsDocumentMessages.
                 * @member {boolean|null|undefined} supportsDocumentMessages
                 * @memberof proto.ClientPayload.WebInfo.WebdPayload
                 * @instance
                 */
                WebdPayload.prototype.supportsDocumentMessages = null;

                /**
                 * WebdPayload supportsUrlMessages.
                 * @member {boolean|null|undefined} supportsUrlMessages
                 * @memberof proto.ClientPayload.WebInfo.WebdPayload
                 * @instance
                 */
                WebdPayload.prototype.supportsUrlMessages = null;

                /**
                 * WebdPayload supportsMediaRetry.
                 * @member {boolean|null|undefined} supportsMediaRetry
                 * @memberof proto.ClientPayload.WebInfo.WebdPayload
                 * @instance
                 */
                WebdPayload.prototype.supportsMediaRetry = null;

                /**
                 * WebdPayload supportsE2EImage.
                 * @member {boolean|null|undefined} supportsE2EImage
                 * @memberof proto.ClientPayload.WebInfo.WebdPayload
                 * @instance
                 */
                WebdPayload.prototype.supportsE2EImage = null;

                /**
                 * WebdPayload supportsE2EVideo.
                 * @member {boolean|null|undefined} supportsE2EVideo
                 * @memberof proto.ClientPayload.WebInfo.WebdPayload
                 * @instance
                 */
                WebdPayload.prototype.supportsE2EVideo = null;

                /**
                 * WebdPayload supportsE2EAudio.
                 * @member {boolean|null|undefined} supportsE2EAudio
                 * @memberof proto.ClientPayload.WebInfo.WebdPayload
                 * @instance
                 */
                WebdPayload.prototype.supportsE2EAudio = null;

                /**
                 * WebdPayload supportsE2EDocument.
                 * @member {boolean|null|undefined} supportsE2EDocument
                 * @memberof proto.ClientPayload.WebInfo.WebdPayload
                 * @instance
                 */
                WebdPayload.prototype.supportsE2EDocument = null;

                /**
                 * WebdPayload documentTypes.
                 * @member {string|null|undefined} documentTypes
                 * @memberof proto.ClientPayload.WebInfo.WebdPayload
                 * @instance
                 */
                WebdPayload.prototype.documentTypes = null;

                /**
                 * WebdPayload features.
                 * @member {Uint8Array|null|undefined} features
                 * @memberof proto.ClientPayload.WebInfo.WebdPayload
                 * @instance
                 */
                WebdPayload.prototype.features = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(WebdPayload.prototype, "_usesParticipantInKey", {
                    get: $util.oneOfGetter($oneOfFields = ["usesParticipantInKey"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(WebdPayload.prototype, "_supportsStarredMessages", {
                    get: $util.oneOfGetter($oneOfFields = ["supportsStarredMessages"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(WebdPayload.prototype, "_supportsDocumentMessages", {
                    get: $util.oneOfGetter($oneOfFields = ["supportsDocumentMessages"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(WebdPayload.prototype, "_supportsUrlMessages", {
                    get: $util.oneOfGetter($oneOfFields = ["supportsUrlMessages"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(WebdPayload.prototype, "_supportsMediaRetry", {
                    get: $util.oneOfGetter($oneOfFields = ["supportsMediaRetry"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(WebdPayload.prototype, "_supportsE2EImage", {
                    get: $util.oneOfGetter($oneOfFields = ["supportsE2EImage"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(WebdPayload.prototype, "_supportsE2EVideo", {
                    get: $util.oneOfGetter($oneOfFields = ["supportsE2EVideo"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(WebdPayload.prototype, "_supportsE2EAudio", {
                    get: $util.oneOfGetter($oneOfFields = ["supportsE2EAudio"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(WebdPayload.prototype, "_supportsE2EDocument", {
                    get: $util.oneOfGetter($oneOfFields = ["supportsE2EDocument"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(WebdPayload.prototype, "_documentTypes", {
                    get: $util.oneOfGetter($oneOfFields = ["documentTypes"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(WebdPayload.prototype, "_features", {
                    get: $util.oneOfGetter($oneOfFields = ["features"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new WebdPayload instance using the specified properties.
                 * @function create
                 * @memberof proto.ClientPayload.WebInfo.WebdPayload
                 * @static
                 * @param {proto.ClientPayload.WebInfo.IWebdPayload=} [properties] Properties to set
                 * @returns {proto.ClientPayload.WebInfo.WebdPayload} WebdPayload instance
                 */
                WebdPayload.create = function create(properties) {
                    return new WebdPayload(properties);
                };

                /**
                 * Encodes the specified WebdPayload message. Does not implicitly {@link proto.ClientPayload.WebInfo.WebdPayload.verify|verify} messages.
                 * @function encode
                 * @memberof proto.ClientPayload.WebInfo.WebdPayload
                 * @static
                 * @param {proto.ClientPayload.WebInfo.IWebdPayload} message WebdPayload message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                WebdPayload.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.usesParticipantInKey != null && Object.hasOwnProperty.call(message, "usesParticipantInKey"))
                        writer.uint32(/* id 1, wireType 0 =*/8).bool(message.usesParticipantInKey);
                    if (message.supportsStarredMessages != null && Object.hasOwnProperty.call(message, "supportsStarredMessages"))
                        writer.uint32(/* id 2, wireType 0 =*/16).bool(message.supportsStarredMessages);
                    if (message.supportsDocumentMessages != null && Object.hasOwnProperty.call(message, "supportsDocumentMessages"))
                        writer.uint32(/* id 3, wireType 0 =*/24).bool(message.supportsDocumentMessages);
                    if (message.supportsUrlMessages != null && Object.hasOwnProperty.call(message, "supportsUrlMessages"))
                        writer.uint32(/* id 4, wireType 0 =*/32).bool(message.supportsUrlMessages);
                    if (message.supportsMediaRetry != null && Object.hasOwnProperty.call(message, "supportsMediaRetry"))
                        writer.uint32(/* id 5, wireType 0 =*/40).bool(message.supportsMediaRetry);
                    if (message.supportsE2EImage != null && Object.hasOwnProperty.call(message, "supportsE2EImage"))
                        writer.uint32(/* id 6, wireType 0 =*/48).bool(message.supportsE2EImage);
                    if (message.supportsE2EVideo != null && Object.hasOwnProperty.call(message, "supportsE2EVideo"))
                        writer.uint32(/* id 7, wireType 0 =*/56).bool(message.supportsE2EVideo);
                    if (message.supportsE2EAudio != null && Object.hasOwnProperty.call(message, "supportsE2EAudio"))
                        writer.uint32(/* id 8, wireType 0 =*/64).bool(message.supportsE2EAudio);
                    if (message.supportsE2EDocument != null && Object.hasOwnProperty.call(message, "supportsE2EDocument"))
                        writer.uint32(/* id 9, wireType 0 =*/72).bool(message.supportsE2EDocument);
                    if (message.documentTypes != null && Object.hasOwnProperty.call(message, "documentTypes"))
                        writer.uint32(/* id 10, wireType 2 =*/82).string(message.documentTypes);
                    if (message.features != null && Object.hasOwnProperty.call(message, "features"))
                        writer.uint32(/* id 11, wireType 2 =*/90).bytes(message.features);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified WebdPayload message, length delimited. Does not implicitly {@link proto.ClientPayload.WebInfo.WebdPayload.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.ClientPayload.WebInfo.WebdPayload
                 * @static
                 * @param {proto.ClientPayload.WebInfo.IWebdPayload} message WebdPayload message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                WebdPayload.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a WebdPayload message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.ClientPayload.WebInfo.WebdPayload
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.ClientPayload.WebInfo.WebdPayload} WebdPayload
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                WebdPayload.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                message.usesParticipantInKey = reader.bool();
                                message._usesParticipantInKey = "usesParticipantInKey";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                message.supportsStarredMessages = reader.bool();
                                message._supportsStarredMessages = "supportsStarredMessages";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                message.supportsDocumentMessages = reader.bool();
                                message._supportsDocumentMessages = "supportsDocumentMessages";
                                continue;
                            }
                        case 4: {
                                if (u !== 0)
                                    break;
                                message.supportsUrlMessages = reader.bool();
                                message._supportsUrlMessages = "supportsUrlMessages";
                                continue;
                            }
                        case 5: {
                                if (u !== 0)
                                    break;
                                message.supportsMediaRetry = reader.bool();
                                message._supportsMediaRetry = "supportsMediaRetry";
                                continue;
                            }
                        case 6: {
                                if (u !== 0)
                                    break;
                                message.supportsE2EImage = reader.bool();
                                message._supportsE2EImage = "supportsE2EImage";
                                continue;
                            }
                        case 7: {
                                if (u !== 0)
                                    break;
                                message.supportsE2EVideo = reader.bool();
                                message._supportsE2EVideo = "supportsE2EVideo";
                                continue;
                            }
                        case 8: {
                                if (u !== 0)
                                    break;
                                message.supportsE2EAudio = reader.bool();
                                message._supportsE2EAudio = "supportsE2EAudio";
                                continue;
                            }
                        case 9: {
                                if (u !== 0)
                                    break;
                                message.supportsE2EDocument = reader.bool();
                                message._supportsE2EDocument = "supportsE2EDocument";
                                continue;
                            }
                        case 10: {
                                if (u !== 2)
                                    break;
                                message.documentTypes = reader.stringVerify();
                                message._documentTypes = "documentTypes";
                                continue;
                            }
                        case 11: {
                                if (u !== 2)
                                    break;
                                message.features = reader.bytes();
                                message._features = "features";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a WebdPayload message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.ClientPayload.WebInfo.WebdPayload
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.ClientPayload.WebInfo.WebdPayload} WebdPayload
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                WebdPayload.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a WebdPayload message.
                 * @function verify
                 * @memberof proto.ClientPayload.WebInfo.WebdPayload
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                WebdPayload.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.usesParticipantInKey != null && Object.hasOwnProperty.call(message, "usesParticipantInKey")) {
                        properties._usesParticipantInKey = 1;
                        if (typeof message.usesParticipantInKey !== "boolean")
                            return "usesParticipantInKey: boolean expected";
                    }
                    if (message.supportsStarredMessages != null && Object.hasOwnProperty.call(message, "supportsStarredMessages")) {
                        properties._supportsStarredMessages = 1;
                        if (typeof message.supportsStarredMessages !== "boolean")
                            return "supportsStarredMessages: boolean expected";
                    }
                    if (message.supportsDocumentMessages != null && Object.hasOwnProperty.call(message, "supportsDocumentMessages")) {
                        properties._supportsDocumentMessages = 1;
                        if (typeof message.supportsDocumentMessages !== "boolean")
                            return "supportsDocumentMessages: boolean expected";
                    }
                    if (message.supportsUrlMessages != null && Object.hasOwnProperty.call(message, "supportsUrlMessages")) {
                        properties._supportsUrlMessages = 1;
                        if (typeof message.supportsUrlMessages !== "boolean")
                            return "supportsUrlMessages: boolean expected";
                    }
                    if (message.supportsMediaRetry != null && Object.hasOwnProperty.call(message, "supportsMediaRetry")) {
                        properties._supportsMediaRetry = 1;
                        if (typeof message.supportsMediaRetry !== "boolean")
                            return "supportsMediaRetry: boolean expected";
                    }
                    if (message.supportsE2EImage != null && Object.hasOwnProperty.call(message, "supportsE2EImage")) {
                        properties._supportsE2EImage = 1;
                        if (typeof message.supportsE2EImage !== "boolean")
                            return "supportsE2EImage: boolean expected";
                    }
                    if (message.supportsE2EVideo != null && Object.hasOwnProperty.call(message, "supportsE2EVideo")) {
                        properties._supportsE2EVideo = 1;
                        if (typeof message.supportsE2EVideo !== "boolean")
                            return "supportsE2EVideo: boolean expected";
                    }
                    if (message.supportsE2EAudio != null && Object.hasOwnProperty.call(message, "supportsE2EAudio")) {
                        properties._supportsE2EAudio = 1;
                        if (typeof message.supportsE2EAudio !== "boolean")
                            return "supportsE2EAudio: boolean expected";
                    }
                    if (message.supportsE2EDocument != null && Object.hasOwnProperty.call(message, "supportsE2EDocument")) {
                        properties._supportsE2EDocument = 1;
                        if (typeof message.supportsE2EDocument !== "boolean")
                            return "supportsE2EDocument: boolean expected";
                    }
                    if (message.documentTypes != null && Object.hasOwnProperty.call(message, "documentTypes")) {
                        properties._documentTypes = 1;
                        if (!$util.isString(message.documentTypes))
                            return "documentTypes: string expected";
                    }
                    if (message.features != null && Object.hasOwnProperty.call(message, "features")) {
                        properties._features = 1;
                        if (!(message.features && typeof message.features.length === "number" || $util.isString(message.features)))
                            return "features: buffer expected";
                    }
                    return null;
                };

                /**
                 * Creates a WebdPayload message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.ClientPayload.WebInfo.WebdPayload
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.ClientPayload.WebInfo.WebdPayload} WebdPayload
                 */
                WebdPayload.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.ClientPayload.WebInfo.WebdPayload: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.usesParticipantInKey != null)
                        message.usesParticipantInKey = Boolean(object.usesParticipantInKey);
                    if (object.supportsStarredMessages != null)
                        message.supportsStarredMessages = Boolean(object.supportsStarredMessages);
                    if (object.supportsDocumentMessages != null)
                        message.supportsDocumentMessages = Boolean(object.supportsDocumentMessages);
                    if (object.supportsUrlMessages != null)
                        message.supportsUrlMessages = Boolean(object.supportsUrlMessages);
                    if (object.supportsMediaRetry != null)
                        message.supportsMediaRetry = Boolean(object.supportsMediaRetry);
                    if (object.supportsE2EImage != null)
                        message.supportsE2EImage = Boolean(object.supportsE2EImage);
                    if (object.supportsE2EVideo != null)
                        message.supportsE2EVideo = Boolean(object.supportsE2EVideo);
                    if (object.supportsE2EAudio != null)
                        message.supportsE2EAudio = Boolean(object.supportsE2EAudio);
                    if (object.supportsE2EDocument != null)
                        message.supportsE2EDocument = Boolean(object.supportsE2EDocument);
                    if (object.documentTypes != null)
                        message.documentTypes = String(object.documentTypes);
                    if (object.features != null)
                        if (typeof object.features === "string")
                            $util.base64.decode(object.features, message.features = $util.newBuffer($util.base64.length(object.features)), 0);
                        else if (object.features.length >= 0)
                            message.features = object.features;
                    return message;
                };

                /**
                 * Creates a plain object from a WebdPayload message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.ClientPayload.WebInfo.WebdPayload
                 * @static
                 * @param {proto.ClientPayload.WebInfo.WebdPayload} message WebdPayload
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                WebdPayload.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.usesParticipantInKey != null && Object.hasOwnProperty.call(message, "usesParticipantInKey"))
                        object.usesParticipantInKey = message.usesParticipantInKey;
                    if (message.supportsStarredMessages != null && Object.hasOwnProperty.call(message, "supportsStarredMessages"))
                        object.supportsStarredMessages = message.supportsStarredMessages;
                    if (message.supportsDocumentMessages != null && Object.hasOwnProperty.call(message, "supportsDocumentMessages"))
                        object.supportsDocumentMessages = message.supportsDocumentMessages;
                    if (message.supportsUrlMessages != null && Object.hasOwnProperty.call(message, "supportsUrlMessages"))
                        object.supportsUrlMessages = message.supportsUrlMessages;
                    if (message.supportsMediaRetry != null && Object.hasOwnProperty.call(message, "supportsMediaRetry"))
                        object.supportsMediaRetry = message.supportsMediaRetry;
                    if (message.supportsE2EImage != null && Object.hasOwnProperty.call(message, "supportsE2EImage"))
                        object.supportsE2EImage = message.supportsE2EImage;
                    if (message.supportsE2EVideo != null && Object.hasOwnProperty.call(message, "supportsE2EVideo"))
                        object.supportsE2EVideo = message.supportsE2EVideo;
                    if (message.supportsE2EAudio != null && Object.hasOwnProperty.call(message, "supportsE2EAudio"))
                        object.supportsE2EAudio = message.supportsE2EAudio;
                    if (message.supportsE2EDocument != null && Object.hasOwnProperty.call(message, "supportsE2EDocument"))
                        object.supportsE2EDocument = message.supportsE2EDocument;
                    if (message.documentTypes != null && Object.hasOwnProperty.call(message, "documentTypes"))
                        object.documentTypes = message.documentTypes;
                    if (message.features != null && Object.hasOwnProperty.call(message, "features"))
                        object.features = options.bytes === String ? $util.base64.encode(message.features, 0, message.features.length) : options.bytes === Array ? Array.prototype.slice.call(message.features) : message.features;
                    return object;
                };

                /**
                 * Converts this WebdPayload to JSON.
                 * @function toJSON
                 * @memberof proto.ClientPayload.WebInfo.WebdPayload
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                WebdPayload.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for WebdPayload
                 * @function getTypeUrl
                 * @memberof proto.ClientPayload.WebInfo.WebdPayload
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                WebdPayload.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.ClientPayload.WebInfo.WebdPayload";
                };

                return WebdPayload;
            })();

            return WebInfo;
        })();

        return ClientPayload;
    })();

    /**
     * CollectionName enum.
     * @name proto.CollectionName
     * @enum {number}
     * @property {number} COLLECTION_NAME_UNKNOWN=0 COLLECTION_NAME_UNKNOWN value
     * @property {number} REGULAR=1 REGULAR value
     * @property {number} REGULAR_LOW=2 REGULAR_LOW value
     * @property {number} REGULAR_HIGH=3 REGULAR_HIGH value
     * @property {number} CRITICAL_BLOCK=4 CRITICAL_BLOCK value
     * @property {number} CRITICAL_UNBLOCK_LOW=5 CRITICAL_UNBLOCK_LOW value
     */
    proto.CollectionName = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "COLLECTION_NAME_UNKNOWN"] = 0;
        values[valuesById[1] = "REGULAR"] = 1;
        values[valuesById[2] = "REGULAR_LOW"] = 2;
        values[valuesById[3] = "REGULAR_HIGH"] = 3;
        values[valuesById[4] = "CRITICAL_BLOCK"] = 4;
        values[valuesById[5] = "CRITICAL_UNBLOCK_LOW"] = 5;
        return values;
    })();

    proto.CommentMetadata = (function() {

        /**
         * Properties of a CommentMetadata.
         * @memberof proto
         * @interface ICommentMetadata
         * @property {proto.IMessageKey|null} [commentParentKey] CommentMetadata commentParentKey
         * @property {number|null} [replyCount] CommentMetadata replyCount
         */

        /**
         * Constructs a new CommentMetadata.
         * @memberof proto
         * @classdesc Represents a CommentMetadata.
         * @implements ICommentMetadata
         * @constructor
         * @param {proto.ICommentMetadata=} [properties] Properties to set
         */
        function CommentMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CommentMetadata commentParentKey.
         * @member {proto.IMessageKey|null|undefined} commentParentKey
         * @memberof proto.CommentMetadata
         * @instance
         */
        CommentMetadata.prototype.commentParentKey = null;

        /**
         * CommentMetadata replyCount.
         * @member {number|null|undefined} replyCount
         * @memberof proto.CommentMetadata
         * @instance
         */
        CommentMetadata.prototype.replyCount = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CommentMetadata.prototype, "_commentParentKey", {
            get: $util.oneOfGetter($oneOfFields = ["commentParentKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CommentMetadata.prototype, "_replyCount", {
            get: $util.oneOfGetter($oneOfFields = ["replyCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new CommentMetadata instance using the specified properties.
         * @function create
         * @memberof proto.CommentMetadata
         * @static
         * @param {proto.ICommentMetadata=} [properties] Properties to set
         * @returns {proto.CommentMetadata} CommentMetadata instance
         */
        CommentMetadata.create = function create(properties) {
            return new CommentMetadata(properties);
        };

        /**
         * Encodes the specified CommentMetadata message. Does not implicitly {@link proto.CommentMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.CommentMetadata
         * @static
         * @param {proto.ICommentMetadata} message CommentMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CommentMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.commentParentKey != null && Object.hasOwnProperty.call(message, "commentParentKey"))
                $root.proto.MessageKey.encode(message.commentParentKey, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.replyCount != null && Object.hasOwnProperty.call(message, "replyCount"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.replyCount);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CommentMetadata message, length delimited. Does not implicitly {@link proto.CommentMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CommentMetadata
         * @static
         * @param {proto.ICommentMetadata} message CommentMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CommentMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CommentMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CommentMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CommentMetadata} CommentMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CommentMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.commentParentKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.commentParentKey);
                        message._commentParentKey = "commentParentKey";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.replyCount = reader.uint32();
                        message._replyCount = "replyCount";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a CommentMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CommentMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CommentMetadata} CommentMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CommentMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CommentMetadata message.
         * @function verify
         * @memberof proto.CommentMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CommentMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.commentParentKey != null && Object.hasOwnProperty.call(message, "commentParentKey")) {
                properties._commentParentKey = 1;
                {
                    var error = $root.proto.MessageKey.verify(message.commentParentKey, q + 1);
                    if (error)
                        return "commentParentKey." + error;
                }
            }
            if (message.replyCount != null && Object.hasOwnProperty.call(message, "replyCount")) {
                properties._replyCount = 1;
                if (!$util.isInteger(message.replyCount))
                    return "replyCount: integer expected";
            }
            return null;
        };

        /**
         * Creates a CommentMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CommentMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CommentMetadata} CommentMetadata
         */
        CommentMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.CommentMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.commentParentKey != null) {
                if (!$util.isObject(object.commentParentKey))
                    throw TypeError(".proto.CommentMetadata.commentParentKey: object expected");
                message.commentParentKey = $root.proto.MessageKey.fromObject(object.commentParentKey, q + 1);
            }
            if (object.replyCount != null)
                message.replyCount = object.replyCount >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a CommentMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CommentMetadata
         * @static
         * @param {proto.CommentMetadata} message CommentMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CommentMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.commentParentKey != null && Object.hasOwnProperty.call(message, "commentParentKey"))
                object.commentParentKey = $root.proto.MessageKey.toObject(message.commentParentKey, options, q + 1);
            if (message.replyCount != null && Object.hasOwnProperty.call(message, "replyCount"))
                object.replyCount = message.replyCount;
            return object;
        };

        /**
         * Converts this CommentMetadata to JSON.
         * @function toJSON
         * @memberof proto.CommentMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CommentMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CommentMetadata
         * @function getTypeUrl
         * @memberof proto.CommentMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CommentMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.CommentMetadata";
        };

        return CommentMetadata;
    })();

    proto.CompanionCommitment = (function() {

        /**
         * Properties of a CompanionCommitment.
         * @memberof proto
         * @interface ICompanionCommitment
         * @property {Uint8Array|null} [hash] CompanionCommitment hash
         */

        /**
         * Constructs a new CompanionCommitment.
         * @memberof proto
         * @classdesc Represents a CompanionCommitment.
         * @implements ICompanionCommitment
         * @constructor
         * @param {proto.ICompanionCommitment=} [properties] Properties to set
         */
        function CompanionCommitment(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CompanionCommitment hash.
         * @member {Uint8Array|null|undefined} hash
         * @memberof proto.CompanionCommitment
         * @instance
         */
        CompanionCommitment.prototype.hash = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CompanionCommitment.prototype, "_hash", {
            get: $util.oneOfGetter($oneOfFields = ["hash"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new CompanionCommitment instance using the specified properties.
         * @function create
         * @memberof proto.CompanionCommitment
         * @static
         * @param {proto.ICompanionCommitment=} [properties] Properties to set
         * @returns {proto.CompanionCommitment} CompanionCommitment instance
         */
        CompanionCommitment.create = function create(properties) {
            return new CompanionCommitment(properties);
        };

        /**
         * Encodes the specified CompanionCommitment message. Does not implicitly {@link proto.CompanionCommitment.verify|verify} messages.
         * @function encode
         * @memberof proto.CompanionCommitment
         * @static
         * @param {proto.ICompanionCommitment} message CompanionCommitment message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompanionCommitment.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.hash != null && Object.hasOwnProperty.call(message, "hash"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.hash);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CompanionCommitment message, length delimited. Does not implicitly {@link proto.CompanionCommitment.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CompanionCommitment
         * @static
         * @param {proto.ICompanionCommitment} message CompanionCommitment message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompanionCommitment.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CompanionCommitment message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CompanionCommitment
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CompanionCommitment} CompanionCommitment
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompanionCommitment.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.hash = reader.bytes();
                        message._hash = "hash";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a CompanionCommitment message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CompanionCommitment
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CompanionCommitment} CompanionCommitment
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompanionCommitment.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CompanionCommitment message.
         * @function verify
         * @memberof proto.CompanionCommitment
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CompanionCommitment.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.hash != null && Object.hasOwnProperty.call(message, "hash")) {
                properties._hash = 1;
                if (!(message.hash && typeof message.hash.length === "number" || $util.isString(message.hash)))
                    return "hash: buffer expected";
            }
            return null;
        };

        /**
         * Creates a CompanionCommitment message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CompanionCommitment
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CompanionCommitment} CompanionCommitment
         */
        CompanionCommitment.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.CompanionCommitment: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.hash != null)
                if (typeof object.hash === "string")
                    $util.base64.decode(object.hash, message.hash = $util.newBuffer($util.base64.length(object.hash)), 0);
                else if (object.hash.length >= 0)
                    message.hash = object.hash;
            return message;
        };

        /**
         * Creates a plain object from a CompanionCommitment message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CompanionCommitment
         * @static
         * @param {proto.CompanionCommitment} message CompanionCommitment
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CompanionCommitment.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.hash != null && Object.hasOwnProperty.call(message, "hash"))
                object.hash = options.bytes === String ? $util.base64.encode(message.hash, 0, message.hash.length) : options.bytes === Array ? Array.prototype.slice.call(message.hash) : message.hash;
            return object;
        };

        /**
         * Converts this CompanionCommitment to JSON.
         * @function toJSON
         * @memberof proto.CompanionCommitment
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CompanionCommitment.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CompanionCommitment
         * @function getTypeUrl
         * @memberof proto.CompanionCommitment
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CompanionCommitment.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.CompanionCommitment";
        };

        return CompanionCommitment;
    })();

    proto.CompanionEphemeralIdentity = (function() {

        /**
         * Properties of a CompanionEphemeralIdentity.
         * @memberof proto
         * @interface ICompanionEphemeralIdentity
         * @property {Uint8Array|null} [publicKey] CompanionEphemeralIdentity publicKey
         * @property {proto.DeviceProps.PlatformType|null} [deviceType] CompanionEphemeralIdentity deviceType
         * @property {string|null} [ref] CompanionEphemeralIdentity ref
         */

        /**
         * Constructs a new CompanionEphemeralIdentity.
         * @memberof proto
         * @classdesc Represents a CompanionEphemeralIdentity.
         * @implements ICompanionEphemeralIdentity
         * @constructor
         * @param {proto.ICompanionEphemeralIdentity=} [properties] Properties to set
         */
        function CompanionEphemeralIdentity(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CompanionEphemeralIdentity publicKey.
         * @member {Uint8Array|null|undefined} publicKey
         * @memberof proto.CompanionEphemeralIdentity
         * @instance
         */
        CompanionEphemeralIdentity.prototype.publicKey = null;

        /**
         * CompanionEphemeralIdentity deviceType.
         * @member {proto.DeviceProps.PlatformType|null|undefined} deviceType
         * @memberof proto.CompanionEphemeralIdentity
         * @instance
         */
        CompanionEphemeralIdentity.prototype.deviceType = null;

        /**
         * CompanionEphemeralIdentity ref.
         * @member {string|null|undefined} ref
         * @memberof proto.CompanionEphemeralIdentity
         * @instance
         */
        CompanionEphemeralIdentity.prototype.ref = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CompanionEphemeralIdentity.prototype, "_publicKey", {
            get: $util.oneOfGetter($oneOfFields = ["publicKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CompanionEphemeralIdentity.prototype, "_deviceType", {
            get: $util.oneOfGetter($oneOfFields = ["deviceType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CompanionEphemeralIdentity.prototype, "_ref", {
            get: $util.oneOfGetter($oneOfFields = ["ref"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new CompanionEphemeralIdentity instance using the specified properties.
         * @function create
         * @memberof proto.CompanionEphemeralIdentity
         * @static
         * @param {proto.ICompanionEphemeralIdentity=} [properties] Properties to set
         * @returns {proto.CompanionEphemeralIdentity} CompanionEphemeralIdentity instance
         */
        CompanionEphemeralIdentity.create = function create(properties) {
            return new CompanionEphemeralIdentity(properties);
        };

        /**
         * Encodes the specified CompanionEphemeralIdentity message. Does not implicitly {@link proto.CompanionEphemeralIdentity.verify|verify} messages.
         * @function encode
         * @memberof proto.CompanionEphemeralIdentity
         * @static
         * @param {proto.ICompanionEphemeralIdentity} message CompanionEphemeralIdentity message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompanionEphemeralIdentity.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.publicKey);
            if (message.deviceType != null && Object.hasOwnProperty.call(message, "deviceType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.deviceType);
            if (message.ref != null && Object.hasOwnProperty.call(message, "ref"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.ref);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified CompanionEphemeralIdentity message, length delimited. Does not implicitly {@link proto.CompanionEphemeralIdentity.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.CompanionEphemeralIdentity
         * @static
         * @param {proto.ICompanionEphemeralIdentity} message CompanionEphemeralIdentity message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompanionEphemeralIdentity.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CompanionEphemeralIdentity message from the specified reader or buffer.
         * @function decode
         * @memberof proto.CompanionEphemeralIdentity
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.CompanionEphemeralIdentity} CompanionEphemeralIdentity
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompanionEphemeralIdentity.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.publicKey = reader.bytes();
                        message._publicKey = "publicKey";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.deviceType = reader.int32();
                        message._deviceType = "deviceType";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.ref = reader.stringVerify();
                        message._ref = "ref";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a CompanionEphemeralIdentity message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.CompanionEphemeralIdentity
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.CompanionEphemeralIdentity} CompanionEphemeralIdentity
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompanionEphemeralIdentity.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CompanionEphemeralIdentity message.
         * @function verify
         * @memberof proto.CompanionEphemeralIdentity
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CompanionEphemeralIdentity.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey")) {
                properties._publicKey = 1;
                if (!(message.publicKey && typeof message.publicKey.length === "number" || $util.isString(message.publicKey)))
                    return "publicKey: buffer expected";
            }
            if (message.deviceType != null && Object.hasOwnProperty.call(message, "deviceType")) {
                properties._deviceType = 1;
                if (typeof message.deviceType !== "number" || (message.deviceType | 0) !== message.deviceType)
                    return "deviceType: enum value expected";
            }
            if (message.ref != null && Object.hasOwnProperty.call(message, "ref")) {
                properties._ref = 1;
                if (!$util.isString(message.ref))
                    return "ref: string expected";
            }
            return null;
        };

        /**
         * Creates a CompanionEphemeralIdentity message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.CompanionEphemeralIdentity
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.CompanionEphemeralIdentity} CompanionEphemeralIdentity
         */
        CompanionEphemeralIdentity.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.CompanionEphemeralIdentity: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.publicKey != null)
                if (typeof object.publicKey === "string")
                    $util.base64.decode(object.publicKey, message.publicKey = $util.newBuffer($util.base64.length(object.publicKey)), 0);
                else if (object.publicKey.length >= 0)
                    message.publicKey = object.publicKey;
            switch (object.deviceType) {
            case "UNKNOWN":
            case 0:
                message.deviceType = 0;
                break;
            case "CHROME":
            case 1:
                message.deviceType = 1;
                break;
            case "FIREFOX":
            case 2:
                message.deviceType = 2;
                break;
            case "IE":
            case 3:
                message.deviceType = 3;
                break;
            case "OPERA":
            case 4:
                message.deviceType = 4;
                break;
            case "SAFARI":
            case 5:
                message.deviceType = 5;
                break;
            case "EDGE":
            case 6:
                message.deviceType = 6;
                break;
            case "DESKTOP":
            case 7:
                message.deviceType = 7;
                break;
            case "IPAD":
            case 8:
                message.deviceType = 8;
                break;
            case "ANDROID_TABLET":
            case 9:
                message.deviceType = 9;
                break;
            case "OHANA":
            case 10:
                message.deviceType = 10;
                break;
            case "ALOHA":
            case 11:
                message.deviceType = 11;
                break;
            case "CATALINA":
            case 12:
                message.deviceType = 12;
                break;
            case "TCL_TV":
            case 13:
                message.deviceType = 13;
                break;
            case "IOS_PHONE":
            case 14:
                message.deviceType = 14;
                break;
            case "IOS_CATALYST":
            case 15:
                message.deviceType = 15;
                break;
            case "ANDROID_PHONE":
            case 16:
                message.deviceType = 16;
                break;
            case "ANDROID_AMBIGUOUS":
            case 17:
                message.deviceType = 17;
                break;
            case "WEAR_OS":
            case 18:
                message.deviceType = 18;
                break;
            case "AR_WRIST":
            case 19:
                message.deviceType = 19;
                break;
            case "AR_DEVICE":
            case 20:
                message.deviceType = 20;
                break;
            case "UWP":
            case 21:
                message.deviceType = 21;
                break;
            case "VR":
            case 22:
                message.deviceType = 22;
                break;
            case "CLOUD_API":
            case 23:
                message.deviceType = 23;
                break;
            case "SMARTGLASSES":
            case 24:
                message.deviceType = 24;
                break;
            default:
                if (typeof object.deviceType === "number" && (object.deviceType | 0) === object.deviceType)
                    message.deviceType = object.deviceType;
            }
            if (object.ref != null)
                message.ref = String(object.ref);
            return message;
        };

        /**
         * Creates a plain object from a CompanionEphemeralIdentity message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.CompanionEphemeralIdentity
         * @static
         * @param {proto.CompanionEphemeralIdentity} message CompanionEphemeralIdentity
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CompanionEphemeralIdentity.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                object.publicKey = options.bytes === String ? $util.base64.encode(message.publicKey, 0, message.publicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.publicKey) : message.publicKey;
            if (message.deviceType != null && Object.hasOwnProperty.call(message, "deviceType"))
                object.deviceType = options.enums === String ? $root.proto.DeviceProps.PlatformType[message.deviceType] === undefined ? message.deviceType : $root.proto.DeviceProps.PlatformType[message.deviceType] : message.deviceType;
            if (message.ref != null && Object.hasOwnProperty.call(message, "ref"))
                object.ref = message.ref;
            return object;
        };

        /**
         * Converts this CompanionEphemeralIdentity to JSON.
         * @function toJSON
         * @memberof proto.CompanionEphemeralIdentity
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CompanionEphemeralIdentity.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CompanionEphemeralIdentity
         * @function getTypeUrl
         * @memberof proto.CompanionEphemeralIdentity
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CompanionEphemeralIdentity.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.CompanionEphemeralIdentity";
        };

        return CompanionEphemeralIdentity;
    })();

    proto.Config = (function() {

        /**
         * Properties of a Config.
         * @memberof proto
         * @interface IConfig
         * @property {Object.<string,proto.IField>|null} [field] Config field
         * @property {number|null} [version] Config version
         */

        /**
         * Constructs a new Config.
         * @memberof proto
         * @classdesc Represents a Config.
         * @implements IConfig
         * @constructor
         * @param {proto.IConfig=} [properties] Properties to set
         */
        function Config(properties) {
            this.field = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Config field.
         * @member {Object.<string,proto.IField>} field
         * @memberof proto.Config
         * @instance
         */
        Config.prototype.field = $util.emptyObject;

        /**
         * Config version.
         * @member {number|null|undefined} version
         * @memberof proto.Config
         * @instance
         */
        Config.prototype.version = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Config.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new Config instance using the specified properties.
         * @function create
         * @memberof proto.Config
         * @static
         * @param {proto.IConfig=} [properties] Properties to set
         * @returns {proto.Config} Config instance
         */
        Config.create = function create(properties) {
            return new Config(properties);
        };

        /**
         * Encodes the specified Config message. Does not implicitly {@link proto.Config.verify|verify} messages.
         * @function encode
         * @memberof proto.Config
         * @static
         * @param {proto.IConfig} message Config message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Config.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.field != null && Object.hasOwnProperty.call(message, "field"))
                for (var keys = Object.keys(message.field), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 0 =*/8).uint32(keys[i]);
                    $root.proto.Field.encode(message.field[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim().ldelim();
                }
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.version);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Config message, length delimited. Does not implicitly {@link proto.Config.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Config
         * @static
         * @param {proto.IConfig} message Config message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Config.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Config message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Config
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Config} Config
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Config.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), key, v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (message.field === $util.emptyObject)
                            message.field = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        v = null;
                        while (reader.pos < end2) {
                            var t2 = reader.tag();
                            u = t2 & 7;
                            switch (t2 >>>= 3) {
                            case 1:
                                if (u !== 0)
                                    break;
                                key = reader.uint32();
                                continue;
                            case 2:
                                if (u !== 2)
                                    break;
                                v = $root.proto.Field.decode(reader, reader.uint32(), undefined, q + 1);
                                continue;
                            }
                            reader.skipType(u, q, t2);
                        }
                        message.field[key] = v || new $root.proto.Field.ctor();
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.version = reader.uint32();
                        message._version = "version";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a Config message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Config
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Config} Config
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Config.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Config message.
         * @function verify
         * @memberof proto.Config
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Config.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.field != null && Object.hasOwnProperty.call(message, "field")) {
                if (!$util.isObject(message.field))
                    return "field: object expected";
                var key = Object.keys(message.field);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "field: integer key{k:uint32} expected";
                    {
                        var error = $root.proto.Field.verify(message.field[key[i]], q + 1);
                        if (error)
                            return "field." + error;
                    }
                }
            }
            if (message.version != null && Object.hasOwnProperty.call(message, "version")) {
                properties._version = 1;
                if (!$util.isInteger(message.version))
                    return "version: integer expected";
            }
            return null;
        };

        /**
         * Creates a Config message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Config
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Config} Config
         */
        Config.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.Config: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.field) {
                if (!$util.isObject(object.field))
                    throw TypeError(".proto.Config.field: object expected");
                message.field = {};
                for (var keys = Object.keys(object.field), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.field, keys[i]);
                    if (!$util.isObject(object.field[keys[i]]))
                        throw TypeError(".proto.Config.field: object expected");
                    message.field[keys[i]] = $root.proto.Field.fromObject(object.field[keys[i]], q + 1);
                }
            }
            if (object.version != null)
                message.version = object.version >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a Config message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Config
         * @static
         * @param {proto.Config} message Config
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Config.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.objects || options.defaults)
                object.field = {};
            var keys2;
            if (message.field && (keys2 = Object.keys(message.field)).length) {
                object.field = {};
                for (var j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.field, keys2[j]);
                    object.field[keys2[j]] = $root.proto.Field.toObject(message.field[keys2[j]], options, q + 1);
                }
            }
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                object.version = message.version;
            return object;
        };

        /**
         * Converts this Config to JSON.
         * @function toJSON
         * @memberof proto.Config
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Config.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Config
         * @function getTypeUrl
         * @memberof proto.Config
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Config.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.Config";
        };

        return Config;
    })();

    proto.ContextInfo = (function() {

        /**
         * Properties of a ContextInfo.
         * @memberof proto
         * @interface IContextInfo
         * @property {string|null} [stanzaId] ContextInfo stanzaId
         * @property {string|null} [participant] ContextInfo participant
         * @property {proto.IMessage|null} [quotedMessage] ContextInfo quotedMessage
         * @property {string|null} [remoteJid] ContextInfo remoteJid
         * @property {Array.<string>|null} [mentionedJid] ContextInfo mentionedJid
         * @property {string|null} [conversionSource] ContextInfo conversionSource
         * @property {Uint8Array|null} [conversionData] ContextInfo conversionData
         * @property {number|null} [conversionDelaySeconds] ContextInfo conversionDelaySeconds
         * @property {number|null} [forwardingScore] ContextInfo forwardingScore
         * @property {boolean|null} [isForwarded] ContextInfo isForwarded
         * @property {proto.ContextInfo.IAdReplyInfo|null} [quotedAd] ContextInfo quotedAd
         * @property {proto.IMessageKey|null} [placeholderKey] ContextInfo placeholderKey
         * @property {number|null} [expiration] ContextInfo expiration
         * @property {number|Long|null} [ephemeralSettingTimestamp] ContextInfo ephemeralSettingTimestamp
         * @property {Uint8Array|null} [ephemeralSharedSecret] ContextInfo ephemeralSharedSecret
         * @property {proto.ContextInfo.IExternalAdReplyInfo|null} [externalAdReply] ContextInfo externalAdReply
         * @property {string|null} [entryPointConversionSource] ContextInfo entryPointConversionSource
         * @property {string|null} [entryPointConversionApp] ContextInfo entryPointConversionApp
         * @property {number|null} [entryPointConversionDelaySeconds] ContextInfo entryPointConversionDelaySeconds
         * @property {proto.IDisappearingMode|null} [disappearingMode] ContextInfo disappearingMode
         * @property {proto.IActionLink|null} [actionLink] ContextInfo actionLink
         * @property {string|null} [groupSubject] ContextInfo groupSubject
         * @property {string|null} [parentGroupJid] ContextInfo parentGroupJid
         * @property {string|null} [trustBannerType] ContextInfo trustBannerType
         * @property {number|null} [trustBannerAction] ContextInfo trustBannerAction
         * @property {boolean|null} [isSampled] ContextInfo isSampled
         * @property {Array.<proto.IGroupMention>|null} [groupMentions] ContextInfo groupMentions
         * @property {proto.ContextInfo.IUTMInfo|null} [utm] ContextInfo utm
         * @property {proto.ContextInfo.IForwardedNewsletterMessageInfo|null} [forwardedNewsletterMessageInfo] ContextInfo forwardedNewsletterMessageInfo
         * @property {proto.ContextInfo.IBusinessMessageForwardInfo|null} [businessMessageForwardInfo] ContextInfo businessMessageForwardInfo
         * @property {string|null} [smbClientCampaignId] ContextInfo smbClientCampaignId
         * @property {string|null} [smbServerCampaignId] ContextInfo smbServerCampaignId
         * @property {proto.ContextInfo.IDataSharingContext|null} [dataSharingContext] ContextInfo dataSharingContext
         * @property {boolean|null} [alwaysShowAdAttribution] ContextInfo alwaysShowAdAttribution
         * @property {proto.ContextInfo.IFeatureEligibilities|null} [featureEligibilities] ContextInfo featureEligibilities
         * @property {string|null} [entryPointConversionExternalSource] ContextInfo entryPointConversionExternalSource
         * @property {string|null} [entryPointConversionExternalMedium] ContextInfo entryPointConversionExternalMedium
         * @property {string|null} [ctwaSignals] ContextInfo ctwaSignals
         * @property {Uint8Array|null} [ctwaPayload] ContextInfo ctwaPayload
         * @property {proto.IForwardedAIBotMessageInfo|null} [forwardedAiBotMessageInfo] ContextInfo forwardedAiBotMessageInfo
         * @property {proto.ContextInfo.StatusAttributionType|null} [statusAttributionType] ContextInfo statusAttributionType
         * @property {proto.IUrlTrackingMap|null} [urlTrackingMap] ContextInfo urlTrackingMap
         * @property {proto.ContextInfo.PairedMediaType|null} [pairedMediaType] ContextInfo pairedMediaType
         * @property {number|null} [rankingVersion] ContextInfo rankingVersion
         * @property {proto.IMemberLabel|null} [memberLabel] ContextInfo memberLabel
         * @property {boolean|null} [isQuestion] ContextInfo isQuestion
         * @property {proto.ContextInfo.StatusSourceType|null} [statusSourceType] ContextInfo statusSourceType
         * @property {Array.<proto.IStatusAttribution>|null} [statusAttributions] ContextInfo statusAttributions
         * @property {boolean|null} [isGroupStatus] ContextInfo isGroupStatus
         * @property {proto.ContextInfo.ForwardOrigin|null} [forwardOrigin] ContextInfo forwardOrigin
         * @property {proto.ContextInfo.IQuestionReplyQuotedMessage|null} [questionReplyQuotedMessage] ContextInfo questionReplyQuotedMessage
         * @property {proto.ContextInfo.IStatusAudienceMetadata|null} [statusAudienceMetadata] ContextInfo statusAudienceMetadata
         * @property {number|null} [nonJidMentions] ContextInfo nonJidMentions
         * @property {proto.ContextInfo.QuotedType|null} [quotedType] ContextInfo quotedType
         * @property {proto.IBotMessageSharingInfo|null} [botMessageSharingInfo] ContextInfo botMessageSharingInfo
         * @property {boolean|null} [isSpoiler] ContextInfo isSpoiler
         * @property {proto.IMediaDomainInfo|null} [mediaDomainInfo] ContextInfo mediaDomainInfo
         * @property {proto.ContextInfo.IPartiallySelectedContent|null} [partiallySelectedContent] ContextInfo partiallySelectedContent
         * @property {number|null} [afterReadDuration] ContextInfo afterReadDuration
         * @property {proto.ContextInfo.CrossAppSource|null} [crossAppSource] ContextInfo crossAppSource
         * @property {proto.ContextInfo.IBusinessInteractionPills|null} [businessInteractionPills] ContextInfo businessInteractionPills
         * @property {string|null} [posterStatusId] ContextInfo posterStatusId
         */

        /**
         * Constructs a new ContextInfo.
         * @memberof proto
         * @classdesc Represents a ContextInfo.
         * @implements IContextInfo
         * @constructor
         * @param {proto.IContextInfo=} [properties] Properties to set
         */
        function ContextInfo(properties) {
            this.mentionedJid = [];
            this.groupMentions = [];
            this.statusAttributions = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ContextInfo stanzaId.
         * @member {string|null|undefined} stanzaId
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.stanzaId = null;

        /**
         * ContextInfo participant.
         * @member {string|null|undefined} participant
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.participant = null;

        /**
         * ContextInfo quotedMessage.
         * @member {proto.IMessage|null|undefined} quotedMessage
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.quotedMessage = null;

        /**
         * ContextInfo remoteJid.
         * @member {string|null|undefined} remoteJid
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.remoteJid = null;

        /**
         * ContextInfo mentionedJid.
         * @member {Array.<string>} mentionedJid
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.mentionedJid = $util.emptyArray;

        /**
         * ContextInfo conversionSource.
         * @member {string|null|undefined} conversionSource
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.conversionSource = null;

        /**
         * ContextInfo conversionData.
         * @member {Uint8Array|null|undefined} conversionData
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.conversionData = null;

        /**
         * ContextInfo conversionDelaySeconds.
         * @member {number|null|undefined} conversionDelaySeconds
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.conversionDelaySeconds = null;

        /**
         * ContextInfo forwardingScore.
         * @member {number|null|undefined} forwardingScore
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.forwardingScore = null;

        /**
         * ContextInfo isForwarded.
         * @member {boolean|null|undefined} isForwarded
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.isForwarded = null;

        /**
         * ContextInfo quotedAd.
         * @member {proto.ContextInfo.IAdReplyInfo|null|undefined} quotedAd
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.quotedAd = null;

        /**
         * ContextInfo placeholderKey.
         * @member {proto.IMessageKey|null|undefined} placeholderKey
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.placeholderKey = null;

        /**
         * ContextInfo expiration.
         * @member {number|null|undefined} expiration
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.expiration = null;

        /**
         * ContextInfo ephemeralSettingTimestamp.
         * @member {number|Long|null|undefined} ephemeralSettingTimestamp
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.ephemeralSettingTimestamp = null;

        /**
         * ContextInfo ephemeralSharedSecret.
         * @member {Uint8Array|null|undefined} ephemeralSharedSecret
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.ephemeralSharedSecret = null;

        /**
         * ContextInfo externalAdReply.
         * @member {proto.ContextInfo.IExternalAdReplyInfo|null|undefined} externalAdReply
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.externalAdReply = null;

        /**
         * ContextInfo entryPointConversionSource.
         * @member {string|null|undefined} entryPointConversionSource
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.entryPointConversionSource = null;

        /**
         * ContextInfo entryPointConversionApp.
         * @member {string|null|undefined} entryPointConversionApp
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.entryPointConversionApp = null;

        /**
         * ContextInfo entryPointConversionDelaySeconds.
         * @member {number|null|undefined} entryPointConversionDelaySeconds
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.entryPointConversionDelaySeconds = null;

        /**
         * ContextInfo disappearingMode.
         * @member {proto.IDisappearingMode|null|undefined} disappearingMode
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.disappearingMode = null;

        /**
         * ContextInfo actionLink.
         * @member {proto.IActionLink|null|undefined} actionLink
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.actionLink = null;

        /**
         * ContextInfo groupSubject.
         * @member {string|null|undefined} groupSubject
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.groupSubject = null;

        /**
         * ContextInfo parentGroupJid.
         * @member {string|null|undefined} parentGroupJid
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.parentGroupJid = null;

        /**
         * ContextInfo trustBannerType.
         * @member {string|null|undefined} trustBannerType
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.trustBannerType = null;

        /**
         * ContextInfo trustBannerAction.
         * @member {number|null|undefined} trustBannerAction
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.trustBannerAction = null;

        /**
         * ContextInfo isSampled.
         * @member {boolean|null|undefined} isSampled
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.isSampled = null;

        /**
         * ContextInfo groupMentions.
         * @member {Array.<proto.IGroupMention>} groupMentions
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.groupMentions = $util.emptyArray;

        /**
         * ContextInfo utm.
         * @member {proto.ContextInfo.IUTMInfo|null|undefined} utm
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.utm = null;

        /**
         * ContextInfo forwardedNewsletterMessageInfo.
         * @member {proto.ContextInfo.IForwardedNewsletterMessageInfo|null|undefined} forwardedNewsletterMessageInfo
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.forwardedNewsletterMessageInfo = null;

        /**
         * ContextInfo businessMessageForwardInfo.
         * @member {proto.ContextInfo.IBusinessMessageForwardInfo|null|undefined} businessMessageForwardInfo
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.businessMessageForwardInfo = null;

        /**
         * ContextInfo smbClientCampaignId.
         * @member {string|null|undefined} smbClientCampaignId
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.smbClientCampaignId = null;

        /**
         * ContextInfo smbServerCampaignId.
         * @member {string|null|undefined} smbServerCampaignId
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.smbServerCampaignId = null;

        /**
         * ContextInfo dataSharingContext.
         * @member {proto.ContextInfo.IDataSharingContext|null|undefined} dataSharingContext
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.dataSharingContext = null;

        /**
         * ContextInfo alwaysShowAdAttribution.
         * @member {boolean|null|undefined} alwaysShowAdAttribution
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.alwaysShowAdAttribution = null;

        /**
         * ContextInfo featureEligibilities.
         * @member {proto.ContextInfo.IFeatureEligibilities|null|undefined} featureEligibilities
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.featureEligibilities = null;

        /**
         * ContextInfo entryPointConversionExternalSource.
         * @member {string|null|undefined} entryPointConversionExternalSource
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.entryPointConversionExternalSource = null;

        /**
         * ContextInfo entryPointConversionExternalMedium.
         * @member {string|null|undefined} entryPointConversionExternalMedium
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.entryPointConversionExternalMedium = null;

        /**
         * ContextInfo ctwaSignals.
         * @member {string|null|undefined} ctwaSignals
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.ctwaSignals = null;

        /**
         * ContextInfo ctwaPayload.
         * @member {Uint8Array|null|undefined} ctwaPayload
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.ctwaPayload = null;

        /**
         * ContextInfo forwardedAiBotMessageInfo.
         * @member {proto.IForwardedAIBotMessageInfo|null|undefined} forwardedAiBotMessageInfo
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.forwardedAiBotMessageInfo = null;

        /**
         * ContextInfo statusAttributionType.
         * @member {proto.ContextInfo.StatusAttributionType|null|undefined} statusAttributionType
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.statusAttributionType = null;

        /**
         * ContextInfo urlTrackingMap.
         * @member {proto.IUrlTrackingMap|null|undefined} urlTrackingMap
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.urlTrackingMap = null;

        /**
         * ContextInfo pairedMediaType.
         * @member {proto.ContextInfo.PairedMediaType|null|undefined} pairedMediaType
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.pairedMediaType = null;

        /**
         * ContextInfo rankingVersion.
         * @member {number|null|undefined} rankingVersion
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.rankingVersion = null;

        /**
         * ContextInfo memberLabel.
         * @member {proto.IMemberLabel|null|undefined} memberLabel
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.memberLabel = null;

        /**
         * ContextInfo isQuestion.
         * @member {boolean|null|undefined} isQuestion
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.isQuestion = null;

        /**
         * ContextInfo statusSourceType.
         * @member {proto.ContextInfo.StatusSourceType|null|undefined} statusSourceType
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.statusSourceType = null;

        /**
         * ContextInfo statusAttributions.
         * @member {Array.<proto.IStatusAttribution>} statusAttributions
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.statusAttributions = $util.emptyArray;

        /**
         * ContextInfo isGroupStatus.
         * @member {boolean|null|undefined} isGroupStatus
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.isGroupStatus = null;

        /**
         * ContextInfo forwardOrigin.
         * @member {proto.ContextInfo.ForwardOrigin|null|undefined} forwardOrigin
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.forwardOrigin = null;

        /**
         * ContextInfo questionReplyQuotedMessage.
         * @member {proto.ContextInfo.IQuestionReplyQuotedMessage|null|undefined} questionReplyQuotedMessage
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.questionReplyQuotedMessage = null;

        /**
         * ContextInfo statusAudienceMetadata.
         * @member {proto.ContextInfo.IStatusAudienceMetadata|null|undefined} statusAudienceMetadata
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.statusAudienceMetadata = null;

        /**
         * ContextInfo nonJidMentions.
         * @member {number|null|undefined} nonJidMentions
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.nonJidMentions = null;

        /**
         * ContextInfo quotedType.
         * @member {proto.ContextInfo.QuotedType|null|undefined} quotedType
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.quotedType = null;

        /**
         * ContextInfo botMessageSharingInfo.
         * @member {proto.IBotMessageSharingInfo|null|undefined} botMessageSharingInfo
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.botMessageSharingInfo = null;

        /**
         * ContextInfo isSpoiler.
         * @member {boolean|null|undefined} isSpoiler
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.isSpoiler = null;

        /**
         * ContextInfo mediaDomainInfo.
         * @member {proto.IMediaDomainInfo|null|undefined} mediaDomainInfo
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.mediaDomainInfo = null;

        /**
         * ContextInfo partiallySelectedContent.
         * @member {proto.ContextInfo.IPartiallySelectedContent|null|undefined} partiallySelectedContent
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.partiallySelectedContent = null;

        /**
         * ContextInfo afterReadDuration.
         * @member {number|null|undefined} afterReadDuration
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.afterReadDuration = null;

        /**
         * ContextInfo crossAppSource.
         * @member {proto.ContextInfo.CrossAppSource|null|undefined} crossAppSource
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.crossAppSource = null;

        /**
         * ContextInfo businessInteractionPills.
         * @member {proto.ContextInfo.IBusinessInteractionPills|null|undefined} businessInteractionPills
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.businessInteractionPills = null;

        /**
         * ContextInfo posterStatusId.
         * @member {string|null|undefined} posterStatusId
         * @memberof proto.ContextInfo
         * @instance
         */
        ContextInfo.prototype.posterStatusId = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_stanzaId", {
            get: $util.oneOfGetter($oneOfFields = ["stanzaId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_participant", {
            get: $util.oneOfGetter($oneOfFields = ["participant"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_quotedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["quotedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_remoteJid", {
            get: $util.oneOfGetter($oneOfFields = ["remoteJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_conversionSource", {
            get: $util.oneOfGetter($oneOfFields = ["conversionSource"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_conversionData", {
            get: $util.oneOfGetter($oneOfFields = ["conversionData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_conversionDelaySeconds", {
            get: $util.oneOfGetter($oneOfFields = ["conversionDelaySeconds"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_forwardingScore", {
            get: $util.oneOfGetter($oneOfFields = ["forwardingScore"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_isForwarded", {
            get: $util.oneOfGetter($oneOfFields = ["isForwarded"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_quotedAd", {
            get: $util.oneOfGetter($oneOfFields = ["quotedAd"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_placeholderKey", {
            get: $util.oneOfGetter($oneOfFields = ["placeholderKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_expiration", {
            get: $util.oneOfGetter($oneOfFields = ["expiration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_ephemeralSettingTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralSettingTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_ephemeralSharedSecret", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralSharedSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_externalAdReply", {
            get: $util.oneOfGetter($oneOfFields = ["externalAdReply"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_entryPointConversionSource", {
            get: $util.oneOfGetter($oneOfFields = ["entryPointConversionSource"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_entryPointConversionApp", {
            get: $util.oneOfGetter($oneOfFields = ["entryPointConversionApp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_entryPointConversionDelaySeconds", {
            get: $util.oneOfGetter($oneOfFields = ["entryPointConversionDelaySeconds"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_disappearingMode", {
            get: $util.oneOfGetter($oneOfFields = ["disappearingMode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_actionLink", {
            get: $util.oneOfGetter($oneOfFields = ["actionLink"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_groupSubject", {
            get: $util.oneOfGetter($oneOfFields = ["groupSubject"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_parentGroupJid", {
            get: $util.oneOfGetter($oneOfFields = ["parentGroupJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_trustBannerType", {
            get: $util.oneOfGetter($oneOfFields = ["trustBannerType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_trustBannerAction", {
            get: $util.oneOfGetter($oneOfFields = ["trustBannerAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_isSampled", {
            get: $util.oneOfGetter($oneOfFields = ["isSampled"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_utm", {
            get: $util.oneOfGetter($oneOfFields = ["utm"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_forwardedNewsletterMessageInfo", {
            get: $util.oneOfGetter($oneOfFields = ["forwardedNewsletterMessageInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_businessMessageForwardInfo", {
            get: $util.oneOfGetter($oneOfFields = ["businessMessageForwardInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_smbClientCampaignId", {
            get: $util.oneOfGetter($oneOfFields = ["smbClientCampaignId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_smbServerCampaignId", {
            get: $util.oneOfGetter($oneOfFields = ["smbServerCampaignId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_dataSharingContext", {
            get: $util.oneOfGetter($oneOfFields = ["dataSharingContext"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_alwaysShowAdAttribution", {
            get: $util.oneOfGetter($oneOfFields = ["alwaysShowAdAttribution"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_featureEligibilities", {
            get: $util.oneOfGetter($oneOfFields = ["featureEligibilities"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_entryPointConversionExternalSource", {
            get: $util.oneOfGetter($oneOfFields = ["entryPointConversionExternalSource"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_entryPointConversionExternalMedium", {
            get: $util.oneOfGetter($oneOfFields = ["entryPointConversionExternalMedium"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_ctwaSignals", {
            get: $util.oneOfGetter($oneOfFields = ["ctwaSignals"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_ctwaPayload", {
            get: $util.oneOfGetter($oneOfFields = ["ctwaPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_forwardedAiBotMessageInfo", {
            get: $util.oneOfGetter($oneOfFields = ["forwardedAiBotMessageInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_statusAttributionType", {
            get: $util.oneOfGetter($oneOfFields = ["statusAttributionType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_urlTrackingMap", {
            get: $util.oneOfGetter($oneOfFields = ["urlTrackingMap"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_pairedMediaType", {
            get: $util.oneOfGetter($oneOfFields = ["pairedMediaType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_rankingVersion", {
            get: $util.oneOfGetter($oneOfFields = ["rankingVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_memberLabel", {
            get: $util.oneOfGetter($oneOfFields = ["memberLabel"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_isQuestion", {
            get: $util.oneOfGetter($oneOfFields = ["isQuestion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_statusSourceType", {
            get: $util.oneOfGetter($oneOfFields = ["statusSourceType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_isGroupStatus", {
            get: $util.oneOfGetter($oneOfFields = ["isGroupStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_forwardOrigin", {
            get: $util.oneOfGetter($oneOfFields = ["forwardOrigin"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_questionReplyQuotedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["questionReplyQuotedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_statusAudienceMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["statusAudienceMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_nonJidMentions", {
            get: $util.oneOfGetter($oneOfFields = ["nonJidMentions"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_quotedType", {
            get: $util.oneOfGetter($oneOfFields = ["quotedType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_botMessageSharingInfo", {
            get: $util.oneOfGetter($oneOfFields = ["botMessageSharingInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_isSpoiler", {
            get: $util.oneOfGetter($oneOfFields = ["isSpoiler"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_mediaDomainInfo", {
            get: $util.oneOfGetter($oneOfFields = ["mediaDomainInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_partiallySelectedContent", {
            get: $util.oneOfGetter($oneOfFields = ["partiallySelectedContent"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_afterReadDuration", {
            get: $util.oneOfGetter($oneOfFields = ["afterReadDuration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_crossAppSource", {
            get: $util.oneOfGetter($oneOfFields = ["crossAppSource"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_businessInteractionPills", {
            get: $util.oneOfGetter($oneOfFields = ["businessInteractionPills"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_posterStatusId", {
            get: $util.oneOfGetter($oneOfFields = ["posterStatusId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ContextInfo instance using the specified properties.
         * @function create
         * @memberof proto.ContextInfo
         * @static
         * @param {proto.IContextInfo=} [properties] Properties to set
         * @returns {proto.ContextInfo} ContextInfo instance
         */
        ContextInfo.create = function create(properties) {
            return new ContextInfo(properties);
        };

        /**
         * Encodes the specified ContextInfo message. Does not implicitly {@link proto.ContextInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.ContextInfo
         * @static
         * @param {proto.IContextInfo} message ContextInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContextInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.stanzaId != null && Object.hasOwnProperty.call(message, "stanzaId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.stanzaId);
            if (message.participant != null && Object.hasOwnProperty.call(message, "participant"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.participant);
            if (message.quotedMessage != null && Object.hasOwnProperty.call(message, "quotedMessage"))
                $root.proto.Message.encode(message.quotedMessage, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.remoteJid != null && Object.hasOwnProperty.call(message, "remoteJid"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.remoteJid);
            if (message.mentionedJid != null && message.mentionedJid.length)
                for (var i = 0; i < message.mentionedJid.length; ++i)
                    writer.uint32(/* id 15, wireType 2 =*/122).string(message.mentionedJid[i]);
            if (message.conversionSource != null && Object.hasOwnProperty.call(message, "conversionSource"))
                writer.uint32(/* id 18, wireType 2 =*/146).string(message.conversionSource);
            if (message.conversionData != null && Object.hasOwnProperty.call(message, "conversionData"))
                writer.uint32(/* id 19, wireType 2 =*/154).bytes(message.conversionData);
            if (message.conversionDelaySeconds != null && Object.hasOwnProperty.call(message, "conversionDelaySeconds"))
                writer.uint32(/* id 20, wireType 0 =*/160).uint32(message.conversionDelaySeconds);
            if (message.forwardingScore != null && Object.hasOwnProperty.call(message, "forwardingScore"))
                writer.uint32(/* id 21, wireType 0 =*/168).uint32(message.forwardingScore);
            if (message.isForwarded != null && Object.hasOwnProperty.call(message, "isForwarded"))
                writer.uint32(/* id 22, wireType 0 =*/176).bool(message.isForwarded);
            if (message.quotedAd != null && Object.hasOwnProperty.call(message, "quotedAd"))
                $root.proto.ContextInfo.AdReplyInfo.encode(message.quotedAd, writer.uint32(/* id 23, wireType 2 =*/186).fork(), q + 1).ldelim();
            if (message.placeholderKey != null && Object.hasOwnProperty.call(message, "placeholderKey"))
                $root.proto.MessageKey.encode(message.placeholderKey, writer.uint32(/* id 24, wireType 2 =*/194).fork(), q + 1).ldelim();
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                writer.uint32(/* id 25, wireType 0 =*/200).uint32(message.expiration);
            if (message.ephemeralSettingTimestamp != null && Object.hasOwnProperty.call(message, "ephemeralSettingTimestamp"))
                writer.uint32(/* id 26, wireType 0 =*/208).int64(message.ephemeralSettingTimestamp);
            if (message.ephemeralSharedSecret != null && Object.hasOwnProperty.call(message, "ephemeralSharedSecret"))
                writer.uint32(/* id 27, wireType 2 =*/218).bytes(message.ephemeralSharedSecret);
            if (message.externalAdReply != null && Object.hasOwnProperty.call(message, "externalAdReply"))
                $root.proto.ContextInfo.ExternalAdReplyInfo.encode(message.externalAdReply, writer.uint32(/* id 28, wireType 2 =*/226).fork(), q + 1).ldelim();
            if (message.entryPointConversionSource != null && Object.hasOwnProperty.call(message, "entryPointConversionSource"))
                writer.uint32(/* id 29, wireType 2 =*/234).string(message.entryPointConversionSource);
            if (message.entryPointConversionApp != null && Object.hasOwnProperty.call(message, "entryPointConversionApp"))
                writer.uint32(/* id 30, wireType 2 =*/242).string(message.entryPointConversionApp);
            if (message.entryPointConversionDelaySeconds != null && Object.hasOwnProperty.call(message, "entryPointConversionDelaySeconds"))
                writer.uint32(/* id 31, wireType 0 =*/248).uint32(message.entryPointConversionDelaySeconds);
            if (message.disappearingMode != null && Object.hasOwnProperty.call(message, "disappearingMode"))
                $root.proto.DisappearingMode.encode(message.disappearingMode, writer.uint32(/* id 32, wireType 2 =*/258).fork(), q + 1).ldelim();
            if (message.actionLink != null && Object.hasOwnProperty.call(message, "actionLink"))
                $root.proto.ActionLink.encode(message.actionLink, writer.uint32(/* id 33, wireType 2 =*/266).fork(), q + 1).ldelim();
            if (message.groupSubject != null && Object.hasOwnProperty.call(message, "groupSubject"))
                writer.uint32(/* id 34, wireType 2 =*/274).string(message.groupSubject);
            if (message.parentGroupJid != null && Object.hasOwnProperty.call(message, "parentGroupJid"))
                writer.uint32(/* id 35, wireType 2 =*/282).string(message.parentGroupJid);
            if (message.trustBannerType != null && Object.hasOwnProperty.call(message, "trustBannerType"))
                writer.uint32(/* id 37, wireType 2 =*/298).string(message.trustBannerType);
            if (message.trustBannerAction != null && Object.hasOwnProperty.call(message, "trustBannerAction"))
                writer.uint32(/* id 38, wireType 0 =*/304).uint32(message.trustBannerAction);
            if (message.isSampled != null && Object.hasOwnProperty.call(message, "isSampled"))
                writer.uint32(/* id 39, wireType 0 =*/312).bool(message.isSampled);
            if (message.groupMentions != null && message.groupMentions.length)
                for (var i = 0; i < message.groupMentions.length; ++i)
                    $root.proto.GroupMention.encode(message.groupMentions[i], writer.uint32(/* id 40, wireType 2 =*/322).fork(), q + 1).ldelim();
            if (message.utm != null && Object.hasOwnProperty.call(message, "utm"))
                $root.proto.ContextInfo.UTMInfo.encode(message.utm, writer.uint32(/* id 41, wireType 2 =*/330).fork(), q + 1).ldelim();
            if (message.forwardedNewsletterMessageInfo != null && Object.hasOwnProperty.call(message, "forwardedNewsletterMessageInfo"))
                $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.encode(message.forwardedNewsletterMessageInfo, writer.uint32(/* id 43, wireType 2 =*/346).fork(), q + 1).ldelim();
            if (message.businessMessageForwardInfo != null && Object.hasOwnProperty.call(message, "businessMessageForwardInfo"))
                $root.proto.ContextInfo.BusinessMessageForwardInfo.encode(message.businessMessageForwardInfo, writer.uint32(/* id 44, wireType 2 =*/354).fork(), q + 1).ldelim();
            if (message.smbClientCampaignId != null && Object.hasOwnProperty.call(message, "smbClientCampaignId"))
                writer.uint32(/* id 45, wireType 2 =*/362).string(message.smbClientCampaignId);
            if (message.smbServerCampaignId != null && Object.hasOwnProperty.call(message, "smbServerCampaignId"))
                writer.uint32(/* id 46, wireType 2 =*/370).string(message.smbServerCampaignId);
            if (message.dataSharingContext != null && Object.hasOwnProperty.call(message, "dataSharingContext"))
                $root.proto.ContextInfo.DataSharingContext.encode(message.dataSharingContext, writer.uint32(/* id 47, wireType 2 =*/378).fork(), q + 1).ldelim();
            if (message.alwaysShowAdAttribution != null && Object.hasOwnProperty.call(message, "alwaysShowAdAttribution"))
                writer.uint32(/* id 48, wireType 0 =*/384).bool(message.alwaysShowAdAttribution);
            if (message.featureEligibilities != null && Object.hasOwnProperty.call(message, "featureEligibilities"))
                $root.proto.ContextInfo.FeatureEligibilities.encode(message.featureEligibilities, writer.uint32(/* id 49, wireType 2 =*/394).fork(), q + 1).ldelim();
            if (message.entryPointConversionExternalSource != null && Object.hasOwnProperty.call(message, "entryPointConversionExternalSource"))
                writer.uint32(/* id 50, wireType 2 =*/402).string(message.entryPointConversionExternalSource);
            if (message.entryPointConversionExternalMedium != null && Object.hasOwnProperty.call(message, "entryPointConversionExternalMedium"))
                writer.uint32(/* id 51, wireType 2 =*/410).string(message.entryPointConversionExternalMedium);
            if (message.ctwaSignals != null && Object.hasOwnProperty.call(message, "ctwaSignals"))
                writer.uint32(/* id 54, wireType 2 =*/434).string(message.ctwaSignals);
            if (message.ctwaPayload != null && Object.hasOwnProperty.call(message, "ctwaPayload"))
                writer.uint32(/* id 55, wireType 2 =*/442).bytes(message.ctwaPayload);
            if (message.forwardedAiBotMessageInfo != null && Object.hasOwnProperty.call(message, "forwardedAiBotMessageInfo"))
                $root.proto.ForwardedAIBotMessageInfo.encode(message.forwardedAiBotMessageInfo, writer.uint32(/* id 56, wireType 2 =*/450).fork(), q + 1).ldelim();
            if (message.statusAttributionType != null && Object.hasOwnProperty.call(message, "statusAttributionType"))
                writer.uint32(/* id 57, wireType 0 =*/456).int32(message.statusAttributionType);
            if (message.urlTrackingMap != null && Object.hasOwnProperty.call(message, "urlTrackingMap"))
                $root.proto.UrlTrackingMap.encode(message.urlTrackingMap, writer.uint32(/* id 58, wireType 2 =*/466).fork(), q + 1).ldelim();
            if (message.pairedMediaType != null && Object.hasOwnProperty.call(message, "pairedMediaType"))
                writer.uint32(/* id 59, wireType 0 =*/472).int32(message.pairedMediaType);
            if (message.rankingVersion != null && Object.hasOwnProperty.call(message, "rankingVersion"))
                writer.uint32(/* id 60, wireType 0 =*/480).uint32(message.rankingVersion);
            if (message.memberLabel != null && Object.hasOwnProperty.call(message, "memberLabel"))
                $root.proto.MemberLabel.encode(message.memberLabel, writer.uint32(/* id 62, wireType 2 =*/498).fork(), q + 1).ldelim();
            if (message.isQuestion != null && Object.hasOwnProperty.call(message, "isQuestion"))
                writer.uint32(/* id 63, wireType 0 =*/504).bool(message.isQuestion);
            if (message.statusSourceType != null && Object.hasOwnProperty.call(message, "statusSourceType"))
                writer.uint32(/* id 64, wireType 0 =*/512).int32(message.statusSourceType);
            if (message.statusAttributions != null && message.statusAttributions.length)
                for (var i = 0; i < message.statusAttributions.length; ++i)
                    $root.proto.StatusAttribution.encode(message.statusAttributions[i], writer.uint32(/* id 65, wireType 2 =*/522).fork(), q + 1).ldelim();
            if (message.isGroupStatus != null && Object.hasOwnProperty.call(message, "isGroupStatus"))
                writer.uint32(/* id 66, wireType 0 =*/528).bool(message.isGroupStatus);
            if (message.forwardOrigin != null && Object.hasOwnProperty.call(message, "forwardOrigin"))
                writer.uint32(/* id 67, wireType 0 =*/536).int32(message.forwardOrigin);
            if (message.questionReplyQuotedMessage != null && Object.hasOwnProperty.call(message, "questionReplyQuotedMessage"))
                $root.proto.ContextInfo.QuestionReplyQuotedMessage.encode(message.questionReplyQuotedMessage, writer.uint32(/* id 68, wireType 2 =*/546).fork(), q + 1).ldelim();
            if (message.statusAudienceMetadata != null && Object.hasOwnProperty.call(message, "statusAudienceMetadata"))
                $root.proto.ContextInfo.StatusAudienceMetadata.encode(message.statusAudienceMetadata, writer.uint32(/* id 69, wireType 2 =*/554).fork(), q + 1).ldelim();
            if (message.nonJidMentions != null && Object.hasOwnProperty.call(message, "nonJidMentions"))
                writer.uint32(/* id 70, wireType 0 =*/560).uint32(message.nonJidMentions);
            if (message.quotedType != null && Object.hasOwnProperty.call(message, "quotedType"))
                writer.uint32(/* id 71, wireType 0 =*/568).int32(message.quotedType);
            if (message.botMessageSharingInfo != null && Object.hasOwnProperty.call(message, "botMessageSharingInfo"))
                $root.proto.BotMessageSharingInfo.encode(message.botMessageSharingInfo, writer.uint32(/* id 72, wireType 2 =*/578).fork(), q + 1).ldelim();
            if (message.isSpoiler != null && Object.hasOwnProperty.call(message, "isSpoiler"))
                writer.uint32(/* id 73, wireType 0 =*/584).bool(message.isSpoiler);
            if (message.mediaDomainInfo != null && Object.hasOwnProperty.call(message, "mediaDomainInfo"))
                $root.proto.MediaDomainInfo.encode(message.mediaDomainInfo, writer.uint32(/* id 74, wireType 2 =*/594).fork(), q + 1).ldelim();
            if (message.partiallySelectedContent != null && Object.hasOwnProperty.call(message, "partiallySelectedContent"))
                $root.proto.ContextInfo.PartiallySelectedContent.encode(message.partiallySelectedContent, writer.uint32(/* id 75, wireType 2 =*/602).fork(), q + 1).ldelim();
            if (message.afterReadDuration != null && Object.hasOwnProperty.call(message, "afterReadDuration"))
                writer.uint32(/* id 76, wireType 0 =*/608).uint32(message.afterReadDuration);
            if (message.crossAppSource != null && Object.hasOwnProperty.call(message, "crossAppSource"))
                writer.uint32(/* id 77, wireType 0 =*/616).int32(message.crossAppSource);
            if (message.businessInteractionPills != null && Object.hasOwnProperty.call(message, "businessInteractionPills"))
                $root.proto.ContextInfo.BusinessInteractionPills.encode(message.businessInteractionPills, writer.uint32(/* id 78, wireType 2 =*/626).fork(), q + 1).ldelim();
            if (message.posterStatusId != null && Object.hasOwnProperty.call(message, "posterStatusId"))
                writer.uint32(/* id 79, wireType 2 =*/634).string(message.posterStatusId);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ContextInfo message, length delimited. Does not implicitly {@link proto.ContextInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ContextInfo
         * @static
         * @param {proto.IContextInfo} message ContextInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ContextInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ContextInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ContextInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ContextInfo} ContextInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContextInfo.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.stanzaId = reader.stringVerify();
                        message._stanzaId = "stanzaId";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.participant = reader.stringVerify();
                        message._participant = "participant";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.quotedMessage = $root.proto.Message.decode(reader, reader.uint32(), undefined, q + 1, message.quotedMessage);
                        message._quotedMessage = "quotedMessage";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.remoteJid = reader.stringVerify();
                        message._remoteJid = "remoteJid";
                        continue;
                    }
                case 15: {
                        if (u !== 2)
                            break;
                        if (!(message.mentionedJid && message.mentionedJid.length))
                            message.mentionedJid = [];
                        message.mentionedJid.push(reader.stringVerify());
                        continue;
                    }
                case 18: {
                        if (u !== 2)
                            break;
                        message.conversionSource = reader.stringVerify();
                        message._conversionSource = "conversionSource";
                        continue;
                    }
                case 19: {
                        if (u !== 2)
                            break;
                        message.conversionData = reader.bytes();
                        message._conversionData = "conversionData";
                        continue;
                    }
                case 20: {
                        if (u !== 0)
                            break;
                        message.conversionDelaySeconds = reader.uint32();
                        message._conversionDelaySeconds = "conversionDelaySeconds";
                        continue;
                    }
                case 21: {
                        if (u !== 0)
                            break;
                        message.forwardingScore = reader.uint32();
                        message._forwardingScore = "forwardingScore";
                        continue;
                    }
                case 22: {
                        if (u !== 0)
                            break;
                        message.isForwarded = reader.bool();
                        message._isForwarded = "isForwarded";
                        continue;
                    }
                case 23: {
                        if (u !== 2)
                            break;
                        message.quotedAd = $root.proto.ContextInfo.AdReplyInfo.decode(reader, reader.uint32(), undefined, q + 1, message.quotedAd);
                        message._quotedAd = "quotedAd";
                        continue;
                    }
                case 24: {
                        if (u !== 2)
                            break;
                        message.placeholderKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.placeholderKey);
                        message._placeholderKey = "placeholderKey";
                        continue;
                    }
                case 25: {
                        if (u !== 0)
                            break;
                        message.expiration = reader.uint32();
                        message._expiration = "expiration";
                        continue;
                    }
                case 26: {
                        if (u !== 0)
                            break;
                        message.ephemeralSettingTimestamp = reader.int64();
                        message._ephemeralSettingTimestamp = "ephemeralSettingTimestamp";
                        continue;
                    }
                case 27: {
                        if (u !== 2)
                            break;
                        message.ephemeralSharedSecret = reader.bytes();
                        message._ephemeralSharedSecret = "ephemeralSharedSecret";
                        continue;
                    }
                case 28: {
                        if (u !== 2)
                            break;
                        message.externalAdReply = $root.proto.ContextInfo.ExternalAdReplyInfo.decode(reader, reader.uint32(), undefined, q + 1, message.externalAdReply);
                        message._externalAdReply = "externalAdReply";
                        continue;
                    }
                case 29: {
                        if (u !== 2)
                            break;
                        message.entryPointConversionSource = reader.stringVerify();
                        message._entryPointConversionSource = "entryPointConversionSource";
                        continue;
                    }
                case 30: {
                        if (u !== 2)
                            break;
                        message.entryPointConversionApp = reader.stringVerify();
                        message._entryPointConversionApp = "entryPointConversionApp";
                        continue;
                    }
                case 31: {
                        if (u !== 0)
                            break;
                        message.entryPointConversionDelaySeconds = reader.uint32();
                        message._entryPointConversionDelaySeconds = "entryPointConversionDelaySeconds";
                        continue;
                    }
                case 32: {
                        if (u !== 2)
                            break;
                        message.disappearingMode = $root.proto.DisappearingMode.decode(reader, reader.uint32(), undefined, q + 1, message.disappearingMode);
                        message._disappearingMode = "disappearingMode";
                        continue;
                    }
                case 33: {
                        if (u !== 2)
                            break;
                        message.actionLink = $root.proto.ActionLink.decode(reader, reader.uint32(), undefined, q + 1, message.actionLink);
                        message._actionLink = "actionLink";
                        continue;
                    }
                case 34: {
                        if (u !== 2)
                            break;
                        message.groupSubject = reader.stringVerify();
                        message._groupSubject = "groupSubject";
                        continue;
                    }
                case 35: {
                        if (u !== 2)
                            break;
                        message.parentGroupJid = reader.stringVerify();
                        message._parentGroupJid = "parentGroupJid";
                        continue;
                    }
                case 37: {
                        if (u !== 2)
                            break;
                        message.trustBannerType = reader.stringVerify();
                        message._trustBannerType = "trustBannerType";
                        continue;
                    }
                case 38: {
                        if (u !== 0)
                            break;
                        message.trustBannerAction = reader.uint32();
                        message._trustBannerAction = "trustBannerAction";
                        continue;
                    }
                case 39: {
                        if (u !== 0)
                            break;
                        message.isSampled = reader.bool();
                        message._isSampled = "isSampled";
                        continue;
                    }
                case 40: {
                        if (u !== 2)
                            break;
                        if (!(message.groupMentions && message.groupMentions.length))
                            message.groupMentions = [];
                        message.groupMentions.push($root.proto.GroupMention.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 41: {
                        if (u !== 2)
                            break;
                        message.utm = $root.proto.ContextInfo.UTMInfo.decode(reader, reader.uint32(), undefined, q + 1, message.utm);
                        message._utm = "utm";
                        continue;
                    }
                case 43: {
                        if (u !== 2)
                            break;
                        message.forwardedNewsletterMessageInfo = $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.decode(reader, reader.uint32(), undefined, q + 1, message.forwardedNewsletterMessageInfo);
                        message._forwardedNewsletterMessageInfo = "forwardedNewsletterMessageInfo";
                        continue;
                    }
                case 44: {
                        if (u !== 2)
                            break;
                        message.businessMessageForwardInfo = $root.proto.ContextInfo.BusinessMessageForwardInfo.decode(reader, reader.uint32(), undefined, q + 1, message.businessMessageForwardInfo);
                        message._businessMessageForwardInfo = "businessMessageForwardInfo";
                        continue;
                    }
                case 45: {
                        if (u !== 2)
                            break;
                        message.smbClientCampaignId = reader.stringVerify();
                        message._smbClientCampaignId = "smbClientCampaignId";
                        continue;
                    }
                case 46: {
                        if (u !== 2)
                            break;
                        message.smbServerCampaignId = reader.stringVerify();
                        message._smbServerCampaignId = "smbServerCampaignId";
                        continue;
                    }
                case 47: {
                        if (u !== 2)
                            break;
                        message.dataSharingContext = $root.proto.ContextInfo.DataSharingContext.decode(reader, reader.uint32(), undefined, q + 1, message.dataSharingContext);
                        message._dataSharingContext = "dataSharingContext";
                        continue;
                    }
                case 48: {
                        if (u !== 0)
                            break;
                        message.alwaysShowAdAttribution = reader.bool();
                        message._alwaysShowAdAttribution = "alwaysShowAdAttribution";
                        continue;
                    }
                case 49: {
                        if (u !== 2)
                            break;
                        message.featureEligibilities = $root.proto.ContextInfo.FeatureEligibilities.decode(reader, reader.uint32(), undefined, q + 1, message.featureEligibilities);
                        message._featureEligibilities = "featureEligibilities";
                        continue;
                    }
                case 50: {
                        if (u !== 2)
                            break;
                        message.entryPointConversionExternalSource = reader.stringVerify();
                        message._entryPointConversionExternalSource = "entryPointConversionExternalSource";
                        continue;
                    }
                case 51: {
                        if (u !== 2)
                            break;
                        message.entryPointConversionExternalMedium = reader.stringVerify();
                        message._entryPointConversionExternalMedium = "entryPointConversionExternalMedium";
                        continue;
                    }
                case 54: {
                        if (u !== 2)
                            break;
                        message.ctwaSignals = reader.stringVerify();
                        message._ctwaSignals = "ctwaSignals";
                        continue;
                    }
                case 55: {
                        if (u !== 2)
                            break;
                        message.ctwaPayload = reader.bytes();
                        message._ctwaPayload = "ctwaPayload";
                        continue;
                    }
                case 56: {
                        if (u !== 2)
                            break;
                        message.forwardedAiBotMessageInfo = $root.proto.ForwardedAIBotMessageInfo.decode(reader, reader.uint32(), undefined, q + 1, message.forwardedAiBotMessageInfo);
                        message._forwardedAiBotMessageInfo = "forwardedAiBotMessageInfo";
                        continue;
                    }
                case 57: {
                        if (u !== 0)
                            break;
                        message.statusAttributionType = reader.int32();
                        message._statusAttributionType = "statusAttributionType";
                        continue;
                    }
                case 58: {
                        if (u !== 2)
                            break;
                        message.urlTrackingMap = $root.proto.UrlTrackingMap.decode(reader, reader.uint32(), undefined, q + 1, message.urlTrackingMap);
                        message._urlTrackingMap = "urlTrackingMap";
                        continue;
                    }
                case 59: {
                        if (u !== 0)
                            break;
                        message.pairedMediaType = reader.int32();
                        message._pairedMediaType = "pairedMediaType";
                        continue;
                    }
                case 60: {
                        if (u !== 0)
                            break;
                        message.rankingVersion = reader.uint32();
                        message._rankingVersion = "rankingVersion";
                        continue;
                    }
                case 62: {
                        if (u !== 2)
                            break;
                        message.memberLabel = $root.proto.MemberLabel.decode(reader, reader.uint32(), undefined, q + 1, message.memberLabel);
                        message._memberLabel = "memberLabel";
                        continue;
                    }
                case 63: {
                        if (u !== 0)
                            break;
                        message.isQuestion = reader.bool();
                        message._isQuestion = "isQuestion";
                        continue;
                    }
                case 64: {
                        if (u !== 0)
                            break;
                        message.statusSourceType = reader.int32();
                        message._statusSourceType = "statusSourceType";
                        continue;
                    }
                case 65: {
                        if (u !== 2)
                            break;
                        if (!(message.statusAttributions && message.statusAttributions.length))
                            message.statusAttributions = [];
                        message.statusAttributions.push($root.proto.StatusAttribution.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 66: {
                        if (u !== 0)
                            break;
                        message.isGroupStatus = reader.bool();
                        message._isGroupStatus = "isGroupStatus";
                        continue;
                    }
                case 67: {
                        if (u !== 0)
                            break;
                        message.forwardOrigin = reader.int32();
                        message._forwardOrigin = "forwardOrigin";
                        continue;
                    }
                case 68: {
                        if (u !== 2)
                            break;
                        message.questionReplyQuotedMessage = $root.proto.ContextInfo.QuestionReplyQuotedMessage.decode(reader, reader.uint32(), undefined, q + 1, message.questionReplyQuotedMessage);
                        message._questionReplyQuotedMessage = "questionReplyQuotedMessage";
                        continue;
                    }
                case 69: {
                        if (u !== 2)
                            break;
                        message.statusAudienceMetadata = $root.proto.ContextInfo.StatusAudienceMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.statusAudienceMetadata);
                        message._statusAudienceMetadata = "statusAudienceMetadata";
                        continue;
                    }
                case 70: {
                        if (u !== 0)
                            break;
                        message.nonJidMentions = reader.uint32();
                        message._nonJidMentions = "nonJidMentions";
                        continue;
                    }
                case 71: {
                        if (u !== 0)
                            break;
                        message.quotedType = reader.int32();
                        message._quotedType = "quotedType";
                        continue;
                    }
                case 72: {
                        if (u !== 2)
                            break;
                        message.botMessageSharingInfo = $root.proto.BotMessageSharingInfo.decode(reader, reader.uint32(), undefined, q + 1, message.botMessageSharingInfo);
                        message._botMessageSharingInfo = "botMessageSharingInfo";
                        continue;
                    }
                case 73: {
                        if (u !== 0)
                            break;
                        message.isSpoiler = reader.bool();
                        message._isSpoiler = "isSpoiler";
                        continue;
                    }
                case 74: {
                        if (u !== 2)
                            break;
                        message.mediaDomainInfo = $root.proto.MediaDomainInfo.decode(reader, reader.uint32(), undefined, q + 1, message.mediaDomainInfo);
                        message._mediaDomainInfo = "mediaDomainInfo";
                        continue;
                    }
                case 75: {
                        if (u !== 2)
                            break;
                        message.partiallySelectedContent = $root.proto.ContextInfo.PartiallySelectedContent.decode(reader, reader.uint32(), undefined, q + 1, message.partiallySelectedContent);
                        message._partiallySelectedContent = "partiallySelectedContent";
                        continue;
                    }
                case 76: {
                        if (u !== 0)
                            break;
                        message.afterReadDuration = reader.uint32();
                        message._afterReadDuration = "afterReadDuration";
                        continue;
                    }
                case 77: {
                        if (u !== 0)
                            break;
                        message.crossAppSource = reader.int32();
                        message._crossAppSource = "crossAppSource";
                        continue;
                    }
                case 78: {
                        if (u !== 2)
                            break;
                        message.businessInteractionPills = $root.proto.ContextInfo.BusinessInteractionPills.decode(reader, reader.uint32(), undefined, q + 1, message.businessInteractionPills);
                        message._businessInteractionPills = "businessInteractionPills";
                        continue;
                    }
                case 79: {
                        if (u !== 2)
                            break;
                        message.posterStatusId = reader.stringVerify();
                        message._posterStatusId = "posterStatusId";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a ContextInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ContextInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ContextInfo} ContextInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ContextInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ContextInfo message.
         * @function verify
         * @memberof proto.ContextInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ContextInfo.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.stanzaId != null && Object.hasOwnProperty.call(message, "stanzaId")) {
                properties._stanzaId = 1;
                if (!$util.isString(message.stanzaId))
                    return "stanzaId: string expected";
            }
            if (message.participant != null && Object.hasOwnProperty.call(message, "participant")) {
                properties._participant = 1;
                if (!$util.isString(message.participant))
                    return "participant: string expected";
            }
            if (message.quotedMessage != null && Object.hasOwnProperty.call(message, "quotedMessage")) {
                properties._quotedMessage = 1;
                {
                    var error = $root.proto.Message.verify(message.quotedMessage, q + 1);
                    if (error)
                        return "quotedMessage." + error;
                }
            }
            if (message.remoteJid != null && Object.hasOwnProperty.call(message, "remoteJid")) {
                properties._remoteJid = 1;
                if (!$util.isString(message.remoteJid))
                    return "remoteJid: string expected";
            }
            if (message.mentionedJid != null && Object.hasOwnProperty.call(message, "mentionedJid")) {
                if (!Array.isArray(message.mentionedJid))
                    return "mentionedJid: array expected";
                for (var i = 0; i < message.mentionedJid.length; ++i)
                    if (!$util.isString(message.mentionedJid[i]))
                        return "mentionedJid: string[] expected";
            }
            if (message.conversionSource != null && Object.hasOwnProperty.call(message, "conversionSource")) {
                properties._conversionSource = 1;
                if (!$util.isString(message.conversionSource))
                    return "conversionSource: string expected";
            }
            if (message.conversionData != null && Object.hasOwnProperty.call(message, "conversionData")) {
                properties._conversionData = 1;
                if (!(message.conversionData && typeof message.conversionData.length === "number" || $util.isString(message.conversionData)))
                    return "conversionData: buffer expected";
            }
            if (message.conversionDelaySeconds != null && Object.hasOwnProperty.call(message, "conversionDelaySeconds")) {
                properties._conversionDelaySeconds = 1;
                if (!$util.isInteger(message.conversionDelaySeconds))
                    return "conversionDelaySeconds: integer expected";
            }
            if (message.forwardingScore != null && Object.hasOwnProperty.call(message, "forwardingScore")) {
                properties._forwardingScore = 1;
                if (!$util.isInteger(message.forwardingScore))
                    return "forwardingScore: integer expected";
            }
            if (message.isForwarded != null && Object.hasOwnProperty.call(message, "isForwarded")) {
                properties._isForwarded = 1;
                if (typeof message.isForwarded !== "boolean")
                    return "isForwarded: boolean expected";
            }
            if (message.quotedAd != null && Object.hasOwnProperty.call(message, "quotedAd")) {
                properties._quotedAd = 1;
                {
                    var error = $root.proto.ContextInfo.AdReplyInfo.verify(message.quotedAd, q + 1);
                    if (error)
                        return "quotedAd." + error;
                }
            }
            if (message.placeholderKey != null && Object.hasOwnProperty.call(message, "placeholderKey")) {
                properties._placeholderKey = 1;
                {
                    var error = $root.proto.MessageKey.verify(message.placeholderKey, q + 1);
                    if (error)
                        return "placeholderKey." + error;
                }
            }
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration")) {
                properties._expiration = 1;
                if (!$util.isInteger(message.expiration))
                    return "expiration: integer expected";
            }
            if (message.ephemeralSettingTimestamp != null && Object.hasOwnProperty.call(message, "ephemeralSettingTimestamp")) {
                properties._ephemeralSettingTimestamp = 1;
                if (!$util.isInteger(message.ephemeralSettingTimestamp) && !(message.ephemeralSettingTimestamp && $util.isInteger(message.ephemeralSettingTimestamp.low) && $util.isInteger(message.ephemeralSettingTimestamp.high)))
                    return "ephemeralSettingTimestamp: integer|Long expected";
            }
            if (message.ephemeralSharedSecret != null && Object.hasOwnProperty.call(message, "ephemeralSharedSecret")) {
                properties._ephemeralSharedSecret = 1;
                if (!(message.ephemeralSharedSecret && typeof message.ephemeralSharedSecret.length === "number" || $util.isString(message.ephemeralSharedSecret)))
                    return "ephemeralSharedSecret: buffer expected";
            }
            if (message.externalAdReply != null && Object.hasOwnProperty.call(message, "externalAdReply")) {
                properties._externalAdReply = 1;
                {
                    var error = $root.proto.ContextInfo.ExternalAdReplyInfo.verify(message.externalAdReply, q + 1);
                    if (error)
                        return "externalAdReply." + error;
                }
            }
            if (message.entryPointConversionSource != null && Object.hasOwnProperty.call(message, "entryPointConversionSource")) {
                properties._entryPointConversionSource = 1;
                if (!$util.isString(message.entryPointConversionSource))
                    return "entryPointConversionSource: string expected";
            }
            if (message.entryPointConversionApp != null && Object.hasOwnProperty.call(message, "entryPointConversionApp")) {
                properties._entryPointConversionApp = 1;
                if (!$util.isString(message.entryPointConversionApp))
                    return "entryPointConversionApp: string expected";
            }
            if (message.entryPointConversionDelaySeconds != null && Object.hasOwnProperty.call(message, "entryPointConversionDelaySeconds")) {
                properties._entryPointConversionDelaySeconds = 1;
                if (!$util.isInteger(message.entryPointConversionDelaySeconds))
                    return "entryPointConversionDelaySeconds: integer expected";
            }
            if (message.disappearingMode != null && Object.hasOwnProperty.call(message, "disappearingMode")) {
                properties._disappearingMode = 1;
                {
                    var error = $root.proto.DisappearingMode.verify(message.disappearingMode, q + 1);
                    if (error)
                        return "disappearingMode." + error;
                }
            }
            if (message.actionLink != null && Object.hasOwnProperty.call(message, "actionLink")) {
                properties._actionLink = 1;
                {
                    var error = $root.proto.ActionLink.verify(message.actionLink, q + 1);
                    if (error)
                        return "actionLink." + error;
                }
            }
            if (message.groupSubject != null && Object.hasOwnProperty.call(message, "groupSubject")) {
                properties._groupSubject = 1;
                if (!$util.isString(message.groupSubject))
                    return "groupSubject: string expected";
            }
            if (message.parentGroupJid != null && Object.hasOwnProperty.call(message, "parentGroupJid")) {
                properties._parentGroupJid = 1;
                if (!$util.isString(message.parentGroupJid))
                    return "parentGroupJid: string expected";
            }
            if (message.trustBannerType != null && Object.hasOwnProperty.call(message, "trustBannerType")) {
                properties._trustBannerType = 1;
                if (!$util.isString(message.trustBannerType))
                    return "trustBannerType: string expected";
            }
            if (message.trustBannerAction != null && Object.hasOwnProperty.call(message, "trustBannerAction")) {
                properties._trustBannerAction = 1;
                if (!$util.isInteger(message.trustBannerAction))
                    return "trustBannerAction: integer expected";
            }
            if (message.isSampled != null && Object.hasOwnProperty.call(message, "isSampled")) {
                properties._isSampled = 1;
                if (typeof message.isSampled !== "boolean")
                    return "isSampled: boolean expected";
            }
            if (message.groupMentions != null && Object.hasOwnProperty.call(message, "groupMentions")) {
                if (!Array.isArray(message.groupMentions))
                    return "groupMentions: array expected";
                for (var i = 0; i < message.groupMentions.length; ++i) {
                    var error = $root.proto.GroupMention.verify(message.groupMentions[i], q + 1);
                    if (error)
                        return "groupMentions." + error;
                }
            }
            if (message.utm != null && Object.hasOwnProperty.call(message, "utm")) {
                properties._utm = 1;
                {
                    var error = $root.proto.ContextInfo.UTMInfo.verify(message.utm, q + 1);
                    if (error)
                        return "utm." + error;
                }
            }
            if (message.forwardedNewsletterMessageInfo != null && Object.hasOwnProperty.call(message, "forwardedNewsletterMessageInfo")) {
                properties._forwardedNewsletterMessageInfo = 1;
                {
                    var error = $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.verify(message.forwardedNewsletterMessageInfo, q + 1);
                    if (error)
                        return "forwardedNewsletterMessageInfo." + error;
                }
            }
            if (message.businessMessageForwardInfo != null && Object.hasOwnProperty.call(message, "businessMessageForwardInfo")) {
                properties._businessMessageForwardInfo = 1;
                {
                    var error = $root.proto.ContextInfo.BusinessMessageForwardInfo.verify(message.businessMessageForwardInfo, q + 1);
                    if (error)
                        return "businessMessageForwardInfo." + error;
                }
            }
            if (message.smbClientCampaignId != null && Object.hasOwnProperty.call(message, "smbClientCampaignId")) {
                properties._smbClientCampaignId = 1;
                if (!$util.isString(message.smbClientCampaignId))
                    return "smbClientCampaignId: string expected";
            }
            if (message.smbServerCampaignId != null && Object.hasOwnProperty.call(message, "smbServerCampaignId")) {
                properties._smbServerCampaignId = 1;
                if (!$util.isString(message.smbServerCampaignId))
                    return "smbServerCampaignId: string expected";
            }
            if (message.dataSharingContext != null && Object.hasOwnProperty.call(message, "dataSharingContext")) {
                properties._dataSharingContext = 1;
                {
                    var error = $root.proto.ContextInfo.DataSharingContext.verify(message.dataSharingContext, q + 1);
                    if (error)
                        return "dataSharingContext." + error;
                }
            }
            if (message.alwaysShowAdAttribution != null && Object.hasOwnProperty.call(message, "alwaysShowAdAttribution")) {
                properties._alwaysShowAdAttribution = 1;
                if (typeof message.alwaysShowAdAttribution !== "boolean")
                    return "alwaysShowAdAttribution: boolean expected";
            }
            if (message.featureEligibilities != null && Object.hasOwnProperty.call(message, "featureEligibilities")) {
                properties._featureEligibilities = 1;
                {
                    var error = $root.proto.ContextInfo.FeatureEligibilities.verify(message.featureEligibilities, q + 1);
                    if (error)
                        return "featureEligibilities." + error;
                }
            }
            if (message.entryPointConversionExternalSource != null && Object.hasOwnProperty.call(message, "entryPointConversionExternalSource")) {
                properties._entryPointConversionExternalSource = 1;
                if (!$util.isString(message.entryPointConversionExternalSource))
                    return "entryPointConversionExternalSource: string expected";
            }
            if (message.entryPointConversionExternalMedium != null && Object.hasOwnProperty.call(message, "entryPointConversionExternalMedium")) {
                properties._entryPointConversionExternalMedium = 1;
                if (!$util.isString(message.entryPointConversionExternalMedium))
                    return "entryPointConversionExternalMedium: string expected";
            }
            if (message.ctwaSignals != null && Object.hasOwnProperty.call(message, "ctwaSignals")) {
                properties._ctwaSignals = 1;
                if (!$util.isString(message.ctwaSignals))
                    return "ctwaSignals: string expected";
            }
            if (message.ctwaPayload != null && Object.hasOwnProperty.call(message, "ctwaPayload")) {
                properties._ctwaPayload = 1;
                if (!(message.ctwaPayload && typeof message.ctwaPayload.length === "number" || $util.isString(message.ctwaPayload)))
                    return "ctwaPayload: buffer expected";
            }
            if (message.forwardedAiBotMessageInfo != null && Object.hasOwnProperty.call(message, "forwardedAiBotMessageInfo")) {
                properties._forwardedAiBotMessageInfo = 1;
                {
                    var error = $root.proto.ForwardedAIBotMessageInfo.verify(message.forwardedAiBotMessageInfo, q + 1);
                    if (error)
                        return "forwardedAiBotMessageInfo." + error;
                }
            }
            if (message.statusAttributionType != null && Object.hasOwnProperty.call(message, "statusAttributionType")) {
                properties._statusAttributionType = 1;
                if (typeof message.statusAttributionType !== "number" || (message.statusAttributionType | 0) !== message.statusAttributionType)
                    return "statusAttributionType: enum value expected";
            }
            if (message.urlTrackingMap != null && Object.hasOwnProperty.call(message, "urlTrackingMap")) {
                properties._urlTrackingMap = 1;
                {
                    var error = $root.proto.UrlTrackingMap.verify(message.urlTrackingMap, q + 1);
                    if (error)
                        return "urlTrackingMap." + error;
                }
            }
            if (message.pairedMediaType != null && Object.hasOwnProperty.call(message, "pairedMediaType")) {
                properties._pairedMediaType = 1;
                if (typeof message.pairedMediaType !== "number" || (message.pairedMediaType | 0) !== message.pairedMediaType)
                    return "pairedMediaType: enum value expected";
            }
            if (message.rankingVersion != null && Object.hasOwnProperty.call(message, "rankingVersion")) {
                properties._rankingVersion = 1;
                if (!$util.isInteger(message.rankingVersion))
                    return "rankingVersion: integer expected";
            }
            if (message.memberLabel != null && Object.hasOwnProperty.call(message, "memberLabel")) {
                properties._memberLabel = 1;
                {
                    var error = $root.proto.MemberLabel.verify(message.memberLabel, q + 1);
                    if (error)
                        return "memberLabel." + error;
                }
            }
            if (message.isQuestion != null && Object.hasOwnProperty.call(message, "isQuestion")) {
                properties._isQuestion = 1;
                if (typeof message.isQuestion !== "boolean")
                    return "isQuestion: boolean expected";
            }
            if (message.statusSourceType != null && Object.hasOwnProperty.call(message, "statusSourceType")) {
                properties._statusSourceType = 1;
                if (typeof message.statusSourceType !== "number" || (message.statusSourceType | 0) !== message.statusSourceType)
                    return "statusSourceType: enum value expected";
            }
            if (message.statusAttributions != null && Object.hasOwnProperty.call(message, "statusAttributions")) {
                if (!Array.isArray(message.statusAttributions))
                    return "statusAttributions: array expected";
                for (var i = 0; i < message.statusAttributions.length; ++i) {
                    var error = $root.proto.StatusAttribution.verify(message.statusAttributions[i], q + 1);
                    if (error)
                        return "statusAttributions." + error;
                }
            }
            if (message.isGroupStatus != null && Object.hasOwnProperty.call(message, "isGroupStatus")) {
                properties._isGroupStatus = 1;
                if (typeof message.isGroupStatus !== "boolean")
                    return "isGroupStatus: boolean expected";
            }
            if (message.forwardOrigin != null && Object.hasOwnProperty.call(message, "forwardOrigin")) {
                properties._forwardOrigin = 1;
                if (typeof message.forwardOrigin !== "number" || (message.forwardOrigin | 0) !== message.forwardOrigin)
                    return "forwardOrigin: enum value expected";
            }
            if (message.questionReplyQuotedMessage != null && Object.hasOwnProperty.call(message, "questionReplyQuotedMessage")) {
                properties._questionReplyQuotedMessage = 1;
                {
                    var error = $root.proto.ContextInfo.QuestionReplyQuotedMessage.verify(message.questionReplyQuotedMessage, q + 1);
                    if (error)
                        return "questionReplyQuotedMessage." + error;
                }
            }
            if (message.statusAudienceMetadata != null && Object.hasOwnProperty.call(message, "statusAudienceMetadata")) {
                properties._statusAudienceMetadata = 1;
                {
                    var error = $root.proto.ContextInfo.StatusAudienceMetadata.verify(message.statusAudienceMetadata, q + 1);
                    if (error)
                        return "statusAudienceMetadata." + error;
                }
            }
            if (message.nonJidMentions != null && Object.hasOwnProperty.call(message, "nonJidMentions")) {
                properties._nonJidMentions = 1;
                if (!$util.isInteger(message.nonJidMentions))
                    return "nonJidMentions: integer expected";
            }
            if (message.quotedType != null && Object.hasOwnProperty.call(message, "quotedType")) {
                properties._quotedType = 1;
                if (typeof message.quotedType !== "number" || (message.quotedType | 0) !== message.quotedType)
                    return "quotedType: enum value expected";
            }
            if (message.botMessageSharingInfo != null && Object.hasOwnProperty.call(message, "botMessageSharingInfo")) {
                properties._botMessageSharingInfo = 1;
                {
                    var error = $root.proto.BotMessageSharingInfo.verify(message.botMessageSharingInfo, q + 1);
                    if (error)
                        return "botMessageSharingInfo." + error;
                }
            }
            if (message.isSpoiler != null && Object.hasOwnProperty.call(message, "isSpoiler")) {
                properties._isSpoiler = 1;
                if (typeof message.isSpoiler !== "boolean")
                    return "isSpoiler: boolean expected";
            }
            if (message.mediaDomainInfo != null && Object.hasOwnProperty.call(message, "mediaDomainInfo")) {
                properties._mediaDomainInfo = 1;
                {
                    var error = $root.proto.MediaDomainInfo.verify(message.mediaDomainInfo, q + 1);
                    if (error)
                        return "mediaDomainInfo." + error;
                }
            }
            if (message.partiallySelectedContent != null && Object.hasOwnProperty.call(message, "partiallySelectedContent")) {
                properties._partiallySelectedContent = 1;
                {
                    var error = $root.proto.ContextInfo.PartiallySelectedContent.verify(message.partiallySelectedContent, q + 1);
                    if (error)
                        return "partiallySelectedContent." + error;
                }
            }
            if (message.afterReadDuration != null && Object.hasOwnProperty.call(message, "afterReadDuration")) {
                properties._afterReadDuration = 1;
                if (!$util.isInteger(message.afterReadDuration))
                    return "afterReadDuration: integer expected";
            }
            if (message.crossAppSource != null && Object.hasOwnProperty.call(message, "crossAppSource")) {
                properties._crossAppSource = 1;
                if (typeof message.crossAppSource !== "number" || (message.crossAppSource | 0) !== message.crossAppSource)
                    return "crossAppSource: enum value expected";
            }
            if (message.businessInteractionPills != null && Object.hasOwnProperty.call(message, "businessInteractionPills")) {
                properties._businessInteractionPills = 1;
                {
                    var error = $root.proto.ContextInfo.BusinessInteractionPills.verify(message.businessInteractionPills, q + 1);
                    if (error)
                        return "businessInteractionPills." + error;
                }
            }
            if (message.posterStatusId != null && Object.hasOwnProperty.call(message, "posterStatusId")) {
                properties._posterStatusId = 1;
                if (!$util.isString(message.posterStatusId))
                    return "posterStatusId: string expected";
            }
            return null;
        };

        /**
         * Creates a ContextInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ContextInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ContextInfo} ContextInfo
         */
        ContextInfo.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.ContextInfo: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.stanzaId != null)
                message.stanzaId = String(object.stanzaId);
            if (object.participant != null)
                message.participant = String(object.participant);
            if (object.quotedMessage != null) {
                if (!$util.isObject(object.quotedMessage))
                    throw TypeError(".proto.ContextInfo.quotedMessage: object expected");
                message.quotedMessage = $root.proto.Message.fromObject(object.quotedMessage, q + 1);
            }
            if (object.remoteJid != null)
                message.remoteJid = String(object.remoteJid);
            if (object.mentionedJid) {
                if (!Array.isArray(object.mentionedJid))
                    throw TypeError(".proto.ContextInfo.mentionedJid: array expected");
                message.mentionedJid = Array(object.mentionedJid.length);
                for (var i = 0; i < object.mentionedJid.length; ++i)
                    message.mentionedJid[i] = String(object.mentionedJid[i]);
            }
            if (object.conversionSource != null)
                message.conversionSource = String(object.conversionSource);
            if (object.conversionData != null)
                if (typeof object.conversionData === "string")
                    $util.base64.decode(object.conversionData, message.conversionData = $util.newBuffer($util.base64.length(object.conversionData)), 0);
                else if (object.conversionData.length >= 0)
                    message.conversionData = object.conversionData;
            if (object.conversionDelaySeconds != null)
                message.conversionDelaySeconds = object.conversionDelaySeconds >>> 0;
            if (object.forwardingScore != null)
                message.forwardingScore = object.forwardingScore >>> 0;
            if (object.isForwarded != null)
                message.isForwarded = Boolean(object.isForwarded);
            if (object.quotedAd != null) {
                if (!$util.isObject(object.quotedAd))
                    throw TypeError(".proto.ContextInfo.quotedAd: object expected");
                message.quotedAd = $root.proto.ContextInfo.AdReplyInfo.fromObject(object.quotedAd, q + 1);
            }
            if (object.placeholderKey != null) {
                if (!$util.isObject(object.placeholderKey))
                    throw TypeError(".proto.ContextInfo.placeholderKey: object expected");
                message.placeholderKey = $root.proto.MessageKey.fromObject(object.placeholderKey, q + 1);
            }
            if (object.expiration != null)
                message.expiration = object.expiration >>> 0;
            if (object.ephemeralSettingTimestamp != null)
                if ($util.Long)
                    message.ephemeralSettingTimestamp = $util.Long.fromValue(object.ephemeralSettingTimestamp, false);
                else if (typeof object.ephemeralSettingTimestamp === "string")
                    message.ephemeralSettingTimestamp = parseInt(object.ephemeralSettingTimestamp, 10);
                else if (typeof object.ephemeralSettingTimestamp === "number")
                    message.ephemeralSettingTimestamp = object.ephemeralSettingTimestamp;
                else if (typeof object.ephemeralSettingTimestamp === "object")
                    message.ephemeralSettingTimestamp = new $util.LongBits(object.ephemeralSettingTimestamp.low >>> 0, object.ephemeralSettingTimestamp.high >>> 0).toNumber();
            if (object.ephemeralSharedSecret != null)
                if (typeof object.ephemeralSharedSecret === "string")
                    $util.base64.decode(object.ephemeralSharedSecret, message.ephemeralSharedSecret = $util.newBuffer($util.base64.length(object.ephemeralSharedSecret)), 0);
                else if (object.ephemeralSharedSecret.length >= 0)
                    message.ephemeralSharedSecret = object.ephemeralSharedSecret;
            if (object.externalAdReply != null) {
                if (!$util.isObject(object.externalAdReply))
                    throw TypeError(".proto.ContextInfo.externalAdReply: object expected");
                message.externalAdReply = $root.proto.ContextInfo.ExternalAdReplyInfo.fromObject(object.externalAdReply, q + 1);
            }
            if (object.entryPointConversionSource != null)
                message.entryPointConversionSource = String(object.entryPointConversionSource);
            if (object.entryPointConversionApp != null)
                message.entryPointConversionApp = String(object.entryPointConversionApp);
            if (object.entryPointConversionDelaySeconds != null)
                message.entryPointConversionDelaySeconds = object.entryPointConversionDelaySeconds >>> 0;
            if (object.disappearingMode != null) {
                if (!$util.isObject(object.disappearingMode))
                    throw TypeError(".proto.ContextInfo.disappearingMode: object expected");
                message.disappearingMode = $root.proto.DisappearingMode.fromObject(object.disappearingMode, q + 1);
            }
            if (object.actionLink != null) {
                if (!$util.isObject(object.actionLink))
                    throw TypeError(".proto.ContextInfo.actionLink: object expected");
                message.actionLink = $root.proto.ActionLink.fromObject(object.actionLink, q + 1);
            }
            if (object.groupSubject != null)
                message.groupSubject = String(object.groupSubject);
            if (object.parentGroupJid != null)
                message.parentGroupJid = String(object.parentGroupJid);
            if (object.trustBannerType != null)
                message.trustBannerType = String(object.trustBannerType);
            if (object.trustBannerAction != null)
                message.trustBannerAction = object.trustBannerAction >>> 0;
            if (object.isSampled != null)
                message.isSampled = Boolean(object.isSampled);
            if (object.groupMentions) {
                if (!Array.isArray(object.groupMentions))
                    throw TypeError(".proto.ContextInfo.groupMentions: array expected");
                message.groupMentions = Array(object.groupMentions.length);
                for (var i = 0; i < object.groupMentions.length; ++i) {
                    if (!$util.isObject(object.groupMentions[i]))
                        throw TypeError(".proto.ContextInfo.groupMentions: object expected");
                    message.groupMentions[i] = $root.proto.GroupMention.fromObject(object.groupMentions[i], q + 1);
                }
            }
            if (object.utm != null) {
                if (!$util.isObject(object.utm))
                    throw TypeError(".proto.ContextInfo.utm: object expected");
                message.utm = $root.proto.ContextInfo.UTMInfo.fromObject(object.utm, q + 1);
            }
            if (object.forwardedNewsletterMessageInfo != null) {
                if (!$util.isObject(object.forwardedNewsletterMessageInfo))
                    throw TypeError(".proto.ContextInfo.forwardedNewsletterMessageInfo: object expected");
                message.forwardedNewsletterMessageInfo = $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.fromObject(object.forwardedNewsletterMessageInfo, q + 1);
            }
            if (object.businessMessageForwardInfo != null) {
                if (!$util.isObject(object.businessMessageForwardInfo))
                    throw TypeError(".proto.ContextInfo.businessMessageForwardInfo: object expected");
                message.businessMessageForwardInfo = $root.proto.ContextInfo.BusinessMessageForwardInfo.fromObject(object.businessMessageForwardInfo, q + 1);
            }
            if (object.smbClientCampaignId != null)
                message.smbClientCampaignId = String(object.smbClientCampaignId);
            if (object.smbServerCampaignId != null)
                message.smbServerCampaignId = String(object.smbServerCampaignId);
            if (object.dataSharingContext != null) {
                if (!$util.isObject(object.dataSharingContext))
                    throw TypeError(".proto.ContextInfo.dataSharingContext: object expected");
                message.dataSharingContext = $root.proto.ContextInfo.DataSharingContext.fromObject(object.dataSharingContext, q + 1);
            }
            if (object.alwaysShowAdAttribution != null)
                message.alwaysShowAdAttribution = Boolean(object.alwaysShowAdAttribution);
            if (object.featureEligibilities != null) {
                if (!$util.isObject(object.featureEligibilities))
                    throw TypeError(".proto.ContextInfo.featureEligibilities: object expected");
                message.featureEligibilities = $root.proto.ContextInfo.FeatureEligibilities.fromObject(object.featureEligibilities, q + 1);
            }
            if (object.entryPointConversionExternalSource != null)
                message.entryPointConversionExternalSource = String(object.entryPointConversionExternalSource);
            if (object.entryPointConversionExternalMedium != null)
                message.entryPointConversionExternalMedium = String(object.entryPointConversionExternalMedium);
            if (object.ctwaSignals != null)
                message.ctwaSignals = String(object.ctwaSignals);
            if (object.ctwaPayload != null)
                if (typeof object.ctwaPayload === "string")
                    $util.base64.decode(object.ctwaPayload, message.ctwaPayload = $util.newBuffer($util.base64.length(object.ctwaPayload)), 0);
                else if (object.ctwaPayload.length >= 0)
                    message.ctwaPayload = object.ctwaPayload;
            if (object.forwardedAiBotMessageInfo != null) {
                if (!$util.isObject(object.forwardedAiBotMessageInfo))
                    throw TypeError(".proto.ContextInfo.forwardedAiBotMessageInfo: object expected");
                message.forwardedAiBotMessageInfo = $root.proto.ForwardedAIBotMessageInfo.fromObject(object.forwardedAiBotMessageInfo, q + 1);
            }
            switch (object.statusAttributionType) {
            case "NONE":
            case 0:
                message.statusAttributionType = 0;
                break;
            case "RESHARED_FROM_MENTION":
            case 1:
                message.statusAttributionType = 1;
                break;
            case "RESHARED_FROM_POST":
            case 2:
                message.statusAttributionType = 2;
                break;
            case "RESHARED_FROM_POST_MANY_TIMES":
            case 3:
                message.statusAttributionType = 3;
                break;
            case "FORWARDED_FROM_STATUS":
            case 4:
                message.statusAttributionType = 4;
                break;
            default:
                if (typeof object.statusAttributionType === "number" && (object.statusAttributionType | 0) === object.statusAttributionType)
                    message.statusAttributionType = object.statusAttributionType;
            }
            if (object.urlTrackingMap != null) {
                if (!$util.isObject(object.urlTrackingMap))
                    throw TypeError(".proto.ContextInfo.urlTrackingMap: object expected");
                message.urlTrackingMap = $root.proto.UrlTrackingMap.fromObject(object.urlTrackingMap, q + 1);
            }
            switch (object.pairedMediaType) {
            case "NOT_PAIRED_MEDIA":
            case 0:
                message.pairedMediaType = 0;
                break;
            case "SD_VIDEO_PARENT":
            case 1:
                message.pairedMediaType = 1;
                break;
            case "HD_VIDEO_CHILD":
            case 2:
                message.pairedMediaType = 2;
                break;
            case "SD_IMAGE_PARENT":
            case 3:
                message.pairedMediaType = 3;
                break;
            case "HD_IMAGE_CHILD":
            case 4:
                message.pairedMediaType = 4;
                break;
            case "MOTION_PHOTO_PARENT":
            case 5:
                message.pairedMediaType = 5;
                break;
            case "MOTION_PHOTO_CHILD":
            case 6:
                message.pairedMediaType = 6;
                break;
            case "HEVC_VIDEO_PARENT":
            case 7:
                message.pairedMediaType = 7;
                break;
            case "HEVC_VIDEO_CHILD":
            case 8:
                message.pairedMediaType = 8;
                break;
            default:
                if (typeof object.pairedMediaType === "number" && (object.pairedMediaType | 0) === object.pairedMediaType)
                    message.pairedMediaType = object.pairedMediaType;
            }
            if (object.rankingVersion != null)
                message.rankingVersion = object.rankingVersion >>> 0;
            if (object.memberLabel != null) {
                if (!$util.isObject(object.memberLabel))
                    throw TypeError(".proto.ContextInfo.memberLabel: object expected");
                message.memberLabel = $root.proto.MemberLabel.fromObject(object.memberLabel, q + 1);
            }
            if (object.isQuestion != null)
                message.isQuestion = Boolean(object.isQuestion);
            switch (object.statusSourceType) {
            case "IMAGE":
            case 0:
                message.statusSourceType = 0;
                break;
            case "VIDEO":
            case 1:
                message.statusSourceType = 1;
                break;
            case "GIF":
            case 2:
                message.statusSourceType = 2;
                break;
            case "AUDIO":
            case 3:
                message.statusSourceType = 3;
                break;
            case "TEXT":
            case 4:
                message.statusSourceType = 4;
                break;
            case "MUSIC_STANDALONE":
            case 5:
                message.statusSourceType = 5;
                break;
            default:
                if (typeof object.statusSourceType === "number" && (object.statusSourceType | 0) === object.statusSourceType)
                    message.statusSourceType = object.statusSourceType;
            }
            if (object.statusAttributions) {
                if (!Array.isArray(object.statusAttributions))
                    throw TypeError(".proto.ContextInfo.statusAttributions: array expected");
                message.statusAttributions = Array(object.statusAttributions.length);
                for (var i = 0; i < object.statusAttributions.length; ++i) {
                    if (!$util.isObject(object.statusAttributions[i]))
                        throw TypeError(".proto.ContextInfo.statusAttributions: object expected");
                    message.statusAttributions[i] = $root.proto.StatusAttribution.fromObject(object.statusAttributions[i], q + 1);
                }
            }
            if (object.isGroupStatus != null)
                message.isGroupStatus = Boolean(object.isGroupStatus);
            switch (object.forwardOrigin) {
            case "UNKNOWN":
            case 0:
                message.forwardOrigin = 0;
                break;
            case "CHAT":
            case 1:
                message.forwardOrigin = 1;
                break;
            case "STATUS":
            case 2:
                message.forwardOrigin = 2;
                break;
            case "CHANNELS":
            case 3:
                message.forwardOrigin = 3;
                break;
            case "META_AI":
            case 4:
                message.forwardOrigin = 4;
                break;
            case "UGC":
            case 5:
                message.forwardOrigin = 5;
                break;
            default:
                if (typeof object.forwardOrigin === "number" && (object.forwardOrigin | 0) === object.forwardOrigin)
                    message.forwardOrigin = object.forwardOrigin;
            }
            if (object.questionReplyQuotedMessage != null) {
                if (!$util.isObject(object.questionReplyQuotedMessage))
                    throw TypeError(".proto.ContextInfo.questionReplyQuotedMessage: object expected");
                message.questionReplyQuotedMessage = $root.proto.ContextInfo.QuestionReplyQuotedMessage.fromObject(object.questionReplyQuotedMessage, q + 1);
            }
            if (object.statusAudienceMetadata != null) {
                if (!$util.isObject(object.statusAudienceMetadata))
                    throw TypeError(".proto.ContextInfo.statusAudienceMetadata: object expected");
                message.statusAudienceMetadata = $root.proto.ContextInfo.StatusAudienceMetadata.fromObject(object.statusAudienceMetadata, q + 1);
            }
            if (object.nonJidMentions != null)
                message.nonJidMentions = object.nonJidMentions >>> 0;
            switch (object.quotedType) {
            case "EXPLICIT":
            case 0:
                message.quotedType = 0;
                break;
            case "AUTO":
            case 1:
                message.quotedType = 1;
                break;
            default:
                if (typeof object.quotedType === "number" && (object.quotedType | 0) === object.quotedType)
                    message.quotedType = object.quotedType;
            }
            if (object.botMessageSharingInfo != null) {
                if (!$util.isObject(object.botMessageSharingInfo))
                    throw TypeError(".proto.ContextInfo.botMessageSharingInfo: object expected");
                message.botMessageSharingInfo = $root.proto.BotMessageSharingInfo.fromObject(object.botMessageSharingInfo, q + 1);
            }
            if (object.isSpoiler != null)
                message.isSpoiler = Boolean(object.isSpoiler);
            if (object.mediaDomainInfo != null) {
                if (!$util.isObject(object.mediaDomainInfo))
                    throw TypeError(".proto.ContextInfo.mediaDomainInfo: object expected");
                message.mediaDomainInfo = $root.proto.MediaDomainInfo.fromObject(object.mediaDomainInfo, q + 1);
            }
            if (object.partiallySelectedContent != null) {
                if (!$util.isObject(object.partiallySelectedContent))
                    throw TypeError(".proto.ContextInfo.partiallySelectedContent: object expected");
                message.partiallySelectedContent = $root.proto.ContextInfo.PartiallySelectedContent.fromObject(object.partiallySelectedContent, q + 1);
            }
            if (object.afterReadDuration != null)
                message.afterReadDuration = object.afterReadDuration >>> 0;
            switch (object.crossAppSource) {
            case "CROSS_APP_SOURCE_UNKNOWN":
            case 0:
                message.crossAppSource = 0;
                break;
            case "CROSS_APP_SOURCE_INSTAGRAM":
            case 1:
                message.crossAppSource = 1;
                break;
            case "CROSS_APP_SOURCE_FACEBOOK":
            case 2:
                message.crossAppSource = 2;
                break;
            default:
                if (typeof object.crossAppSource === "number" && (object.crossAppSource | 0) === object.crossAppSource)
                    message.crossAppSource = object.crossAppSource;
            }
            if (object.businessInteractionPills != null) {
                if (!$util.isObject(object.businessInteractionPills))
                    throw TypeError(".proto.ContextInfo.businessInteractionPills: object expected");
                message.businessInteractionPills = $root.proto.ContextInfo.BusinessInteractionPills.fromObject(object.businessInteractionPills, q + 1);
            }
            if (object.posterStatusId != null)
                message.posterStatusId = String(object.posterStatusId);
            return message;
        };

        /**
         * Creates a plain object from a ContextInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ContextInfo
         * @static
         * @param {proto.ContextInfo} message ContextInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ContextInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults) {
                object.mentionedJid = [];
                object.groupMentions = [];
                object.statusAttributions = [];
            }
            if (message.stanzaId != null && Object.hasOwnProperty.call(message, "stanzaId"))
                object.stanzaId = message.stanzaId;
            if (message.participant != null && Object.hasOwnProperty.call(message, "participant"))
                object.participant = message.participant;
            if (message.quotedMessage != null && Object.hasOwnProperty.call(message, "quotedMessage"))
                object.quotedMessage = $root.proto.Message.toObject(message.quotedMessage, options, q + 1);
            if (message.remoteJid != null && Object.hasOwnProperty.call(message, "remoteJid"))
                object.remoteJid = message.remoteJid;
            if (message.mentionedJid && message.mentionedJid.length) {
                object.mentionedJid = Array(message.mentionedJid.length);
                for (var j = 0; j < message.mentionedJid.length; ++j)
                    object.mentionedJid[j] = message.mentionedJid[j];
            }
            if (message.conversionSource != null && Object.hasOwnProperty.call(message, "conversionSource"))
                object.conversionSource = message.conversionSource;
            if (message.conversionData != null && Object.hasOwnProperty.call(message, "conversionData"))
                object.conversionData = options.bytes === String ? $util.base64.encode(message.conversionData, 0, message.conversionData.length) : options.bytes === Array ? Array.prototype.slice.call(message.conversionData) : message.conversionData;
            if (message.conversionDelaySeconds != null && Object.hasOwnProperty.call(message, "conversionDelaySeconds"))
                object.conversionDelaySeconds = message.conversionDelaySeconds;
            if (message.forwardingScore != null && Object.hasOwnProperty.call(message, "forwardingScore"))
                object.forwardingScore = message.forwardingScore;
            if (message.isForwarded != null && Object.hasOwnProperty.call(message, "isForwarded"))
                object.isForwarded = message.isForwarded;
            if (message.quotedAd != null && Object.hasOwnProperty.call(message, "quotedAd"))
                object.quotedAd = $root.proto.ContextInfo.AdReplyInfo.toObject(message.quotedAd, options, q + 1);
            if (message.placeholderKey != null && Object.hasOwnProperty.call(message, "placeholderKey"))
                object.placeholderKey = $root.proto.MessageKey.toObject(message.placeholderKey, options, q + 1);
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                object.expiration = message.expiration;
            if (message.ephemeralSettingTimestamp != null && Object.hasOwnProperty.call(message, "ephemeralSettingTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.ephemeralSettingTimestamp = typeof message.ephemeralSettingTimestamp === "number" ? BigInt(message.ephemeralSettingTimestamp) : $util.Long.fromBits(message.ephemeralSettingTimestamp.low >>> 0, message.ephemeralSettingTimestamp.high >>> 0, false).toBigInt();
                else if (typeof message.ephemeralSettingTimestamp === "number")
                    object.ephemeralSettingTimestamp = options.longs === String ? String(message.ephemeralSettingTimestamp) : message.ephemeralSettingTimestamp;
                else
                    object.ephemeralSettingTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.ephemeralSettingTimestamp) : options.longs === Number ? new $util.LongBits(message.ephemeralSettingTimestamp.low >>> 0, message.ephemeralSettingTimestamp.high >>> 0).toNumber() : message.ephemeralSettingTimestamp;
            if (message.ephemeralSharedSecret != null && Object.hasOwnProperty.call(message, "ephemeralSharedSecret"))
                object.ephemeralSharedSecret = options.bytes === String ? $util.base64.encode(message.ephemeralSharedSecret, 0, message.ephemeralSharedSecret.length) : options.bytes === Array ? Array.prototype.slice.call(message.ephemeralSharedSecret) : message.ephemeralSharedSecret;
            if (message.externalAdReply != null && Object.hasOwnProperty.call(message, "externalAdReply"))
                object.externalAdReply = $root.proto.ContextInfo.ExternalAdReplyInfo.toObject(message.externalAdReply, options, q + 1);
            if (message.entryPointConversionSource != null && Object.hasOwnProperty.call(message, "entryPointConversionSource"))
                object.entryPointConversionSource = message.entryPointConversionSource;
            if (message.entryPointConversionApp != null && Object.hasOwnProperty.call(message, "entryPointConversionApp"))
                object.entryPointConversionApp = message.entryPointConversionApp;
            if (message.entryPointConversionDelaySeconds != null && Object.hasOwnProperty.call(message, "entryPointConversionDelaySeconds"))
                object.entryPointConversionDelaySeconds = message.entryPointConversionDelaySeconds;
            if (message.disappearingMode != null && Object.hasOwnProperty.call(message, "disappearingMode"))
                object.disappearingMode = $root.proto.DisappearingMode.toObject(message.disappearingMode, options, q + 1);
            if (message.actionLink != null && Object.hasOwnProperty.call(message, "actionLink"))
                object.actionLink = $root.proto.ActionLink.toObject(message.actionLink, options, q + 1);
            if (message.groupSubject != null && Object.hasOwnProperty.call(message, "groupSubject"))
                object.groupSubject = message.groupSubject;
            if (message.parentGroupJid != null && Object.hasOwnProperty.call(message, "parentGroupJid"))
                object.parentGroupJid = message.parentGroupJid;
            if (message.trustBannerType != null && Object.hasOwnProperty.call(message, "trustBannerType"))
                object.trustBannerType = message.trustBannerType;
            if (message.trustBannerAction != null && Object.hasOwnProperty.call(message, "trustBannerAction"))
                object.trustBannerAction = message.trustBannerAction;
            if (message.isSampled != null && Object.hasOwnProperty.call(message, "isSampled"))
                object.isSampled = message.isSampled;
            if (message.groupMentions && message.groupMentions.length) {
                object.groupMentions = Array(message.groupMentions.length);
                for (var j = 0; j < message.groupMentions.length; ++j)
                    object.groupMentions[j] = $root.proto.GroupMention.toObject(message.groupMentions[j], options, q + 1);
            }
            if (message.utm != null && Object.hasOwnProperty.call(message, "utm"))
                object.utm = $root.proto.ContextInfo.UTMInfo.toObject(message.utm, options, q + 1);
            if (message.forwardedNewsletterMessageInfo != null && Object.hasOwnProperty.call(message, "forwardedNewsletterMessageInfo"))
                object.forwardedNewsletterMessageInfo = $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.toObject(message.forwardedNewsletterMessageInfo, options, q + 1);
            if (message.businessMessageForwardInfo != null && Object.hasOwnProperty.call(message, "businessMessageForwardInfo"))
                object.businessMessageForwardInfo = $root.proto.ContextInfo.BusinessMessageForwardInfo.toObject(message.businessMessageForwardInfo, options, q + 1);
            if (message.smbClientCampaignId != null && Object.hasOwnProperty.call(message, "smbClientCampaignId"))
                object.smbClientCampaignId = message.smbClientCampaignId;
            if (message.smbServerCampaignId != null && Object.hasOwnProperty.call(message, "smbServerCampaignId"))
                object.smbServerCampaignId = message.smbServerCampaignId;
            if (message.dataSharingContext != null && Object.hasOwnProperty.call(message, "dataSharingContext"))
                object.dataSharingContext = $root.proto.ContextInfo.DataSharingContext.toObject(message.dataSharingContext, options, q + 1);
            if (message.alwaysShowAdAttribution != null && Object.hasOwnProperty.call(message, "alwaysShowAdAttribution"))
                object.alwaysShowAdAttribution = message.alwaysShowAdAttribution;
            if (message.featureEligibilities != null && Object.hasOwnProperty.call(message, "featureEligibilities"))
                object.featureEligibilities = $root.proto.ContextInfo.FeatureEligibilities.toObject(message.featureEligibilities, options, q + 1);
            if (message.entryPointConversionExternalSource != null && Object.hasOwnProperty.call(message, "entryPointConversionExternalSource"))
                object.entryPointConversionExternalSource = message.entryPointConversionExternalSource;
            if (message.entryPointConversionExternalMedium != null && Object.hasOwnProperty.call(message, "entryPointConversionExternalMedium"))
                object.entryPointConversionExternalMedium = message.entryPointConversionExternalMedium;
            if (message.ctwaSignals != null && Object.hasOwnProperty.call(message, "ctwaSignals"))
                object.ctwaSignals = message.ctwaSignals;
            if (message.ctwaPayload != null && Object.hasOwnProperty.call(message, "ctwaPayload"))
                object.ctwaPayload = options.bytes === String ? $util.base64.encode(message.ctwaPayload, 0, message.ctwaPayload.length) : options.bytes === Array ? Array.prototype.slice.call(message.ctwaPayload) : message.ctwaPayload;
            if (message.forwardedAiBotMessageInfo != null && Object.hasOwnProperty.call(message, "forwardedAiBotMessageInfo"))
                object.forwardedAiBotMessageInfo = $root.proto.ForwardedAIBotMessageInfo.toObject(message.forwardedAiBotMessageInfo, options, q + 1);
            if (message.statusAttributionType != null && Object.hasOwnProperty.call(message, "statusAttributionType"))
                object.statusAttributionType = options.enums === String ? $root.proto.ContextInfo.StatusAttributionType[message.statusAttributionType] === undefined ? message.statusAttributionType : $root.proto.ContextInfo.StatusAttributionType[message.statusAttributionType] : message.statusAttributionType;
            if (message.urlTrackingMap != null && Object.hasOwnProperty.call(message, "urlTrackingMap"))
                object.urlTrackingMap = $root.proto.UrlTrackingMap.toObject(message.urlTrackingMap, options, q + 1);
            if (message.pairedMediaType != null && Object.hasOwnProperty.call(message, "pairedMediaType"))
                object.pairedMediaType = options.enums === String ? $root.proto.ContextInfo.PairedMediaType[message.pairedMediaType] === undefined ? message.pairedMediaType : $root.proto.ContextInfo.PairedMediaType[message.pairedMediaType] : message.pairedMediaType;
            if (message.rankingVersion != null && Object.hasOwnProperty.call(message, "rankingVersion"))
                object.rankingVersion = message.rankingVersion;
            if (message.memberLabel != null && Object.hasOwnProperty.call(message, "memberLabel"))
                object.memberLabel = $root.proto.MemberLabel.toObject(message.memberLabel, options, q + 1);
            if (message.isQuestion != null && Object.hasOwnProperty.call(message, "isQuestion"))
                object.isQuestion = message.isQuestion;
            if (message.statusSourceType != null && Object.hasOwnProperty.call(message, "statusSourceType"))
                object.statusSourceType = options.enums === String ? $root.proto.ContextInfo.StatusSourceType[message.statusSourceType] === undefined ? message.statusSourceType : $root.proto.ContextInfo.StatusSourceType[message.statusSourceType] : message.statusSourceType;
            if (message.statusAttributions && message.statusAttributions.length) {
                object.statusAttributions = Array(message.statusAttributions.length);
                for (var j = 0; j < message.statusAttributions.length; ++j)
                    object.statusAttributions[j] = $root.proto.StatusAttribution.toObject(message.statusAttributions[j], options, q + 1);
            }
            if (message.isGroupStatus != null && Object.hasOwnProperty.call(message, "isGroupStatus"))
                object.isGroupStatus = message.isGroupStatus;
            if (message.forwardOrigin != null && Object.hasOwnProperty.call(message, "forwardOrigin"))
                object.forwardOrigin = options.enums === String ? $root.proto.ContextInfo.ForwardOrigin[message.forwardOrigin] === undefined ? message.forwardOrigin : $root.proto.ContextInfo.ForwardOrigin[message.forwardOrigin] : message.forwardOrigin;
            if (message.questionReplyQuotedMessage != null && Object.hasOwnProperty.call(message, "questionReplyQuotedMessage"))
                object.questionReplyQuotedMessage = $root.proto.ContextInfo.QuestionReplyQuotedMessage.toObject(message.questionReplyQuotedMessage, options, q + 1);
            if (message.statusAudienceMetadata != null && Object.hasOwnProperty.call(message, "statusAudienceMetadata"))
                object.statusAudienceMetadata = $root.proto.ContextInfo.StatusAudienceMetadata.toObject(message.statusAudienceMetadata, options, q + 1);
            if (message.nonJidMentions != null && Object.hasOwnProperty.call(message, "nonJidMentions"))
                object.nonJidMentions = message.nonJidMentions;
            if (message.quotedType != null && Object.hasOwnProperty.call(message, "quotedType"))
                object.quotedType = options.enums === String ? $root.proto.ContextInfo.QuotedType[message.quotedType] === undefined ? message.quotedType : $root.proto.ContextInfo.QuotedType[message.quotedType] : message.quotedType;
            if (message.botMessageSharingInfo != null && Object.hasOwnProperty.call(message, "botMessageSharingInfo"))
                object.botMessageSharingInfo = $root.proto.BotMessageSharingInfo.toObject(message.botMessageSharingInfo, options, q + 1);
            if (message.isSpoiler != null && Object.hasOwnProperty.call(message, "isSpoiler"))
                object.isSpoiler = message.isSpoiler;
            if (message.mediaDomainInfo != null && Object.hasOwnProperty.call(message, "mediaDomainInfo"))
                object.mediaDomainInfo = $root.proto.MediaDomainInfo.toObject(message.mediaDomainInfo, options, q + 1);
            if (message.partiallySelectedContent != null && Object.hasOwnProperty.call(message, "partiallySelectedContent"))
                object.partiallySelectedContent = $root.proto.ContextInfo.PartiallySelectedContent.toObject(message.partiallySelectedContent, options, q + 1);
            if (message.afterReadDuration != null && Object.hasOwnProperty.call(message, "afterReadDuration"))
                object.afterReadDuration = message.afterReadDuration;
            if (message.crossAppSource != null && Object.hasOwnProperty.call(message, "crossAppSource"))
                object.crossAppSource = options.enums === String ? $root.proto.ContextInfo.CrossAppSource[message.crossAppSource] === undefined ? message.crossAppSource : $root.proto.ContextInfo.CrossAppSource[message.crossAppSource] : message.crossAppSource;
            if (message.businessInteractionPills != null && Object.hasOwnProperty.call(message, "businessInteractionPills"))
                object.businessInteractionPills = $root.proto.ContextInfo.BusinessInteractionPills.toObject(message.businessInteractionPills, options, q + 1);
            if (message.posterStatusId != null && Object.hasOwnProperty.call(message, "posterStatusId"))
                object.posterStatusId = message.posterStatusId;
            return object;
        };

        /**
         * Converts this ContextInfo to JSON.
         * @function toJSON
         * @memberof proto.ContextInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ContextInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ContextInfo
         * @function getTypeUrl
         * @memberof proto.ContextInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ContextInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.ContextInfo";
        };

        ContextInfo.AdReplyInfo = (function() {

            /**
             * Properties of an AdReplyInfo.
             * @memberof proto.ContextInfo
             * @interface IAdReplyInfo
             * @property {string|null} [advertiserName] AdReplyInfo advertiserName
             * @property {proto.ContextInfo.AdReplyInfo.MediaType|null} [mediaType] AdReplyInfo mediaType
             * @property {Uint8Array|null} [jpegThumbnail] AdReplyInfo jpegThumbnail
             * @property {string|null} [caption] AdReplyInfo caption
             */

            /**
             * Constructs a new AdReplyInfo.
             * @memberof proto.ContextInfo
             * @classdesc Represents an AdReplyInfo.
             * @implements IAdReplyInfo
             * @constructor
             * @param {proto.ContextInfo.IAdReplyInfo=} [properties] Properties to set
             */
            function AdReplyInfo(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AdReplyInfo advertiserName.
             * @member {string|null|undefined} advertiserName
             * @memberof proto.ContextInfo.AdReplyInfo
             * @instance
             */
            AdReplyInfo.prototype.advertiserName = null;

            /**
             * AdReplyInfo mediaType.
             * @member {proto.ContextInfo.AdReplyInfo.MediaType|null|undefined} mediaType
             * @memberof proto.ContextInfo.AdReplyInfo
             * @instance
             */
            AdReplyInfo.prototype.mediaType = null;

            /**
             * AdReplyInfo jpegThumbnail.
             * @member {Uint8Array|null|undefined} jpegThumbnail
             * @memberof proto.ContextInfo.AdReplyInfo
             * @instance
             */
            AdReplyInfo.prototype.jpegThumbnail = null;

            /**
             * AdReplyInfo caption.
             * @member {string|null|undefined} caption
             * @memberof proto.ContextInfo.AdReplyInfo
             * @instance
             */
            AdReplyInfo.prototype.caption = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AdReplyInfo.prototype, "_advertiserName", {
                get: $util.oneOfGetter($oneOfFields = ["advertiserName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AdReplyInfo.prototype, "_mediaType", {
                get: $util.oneOfGetter($oneOfFields = ["mediaType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AdReplyInfo.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AdReplyInfo.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AdReplyInfo instance using the specified properties.
             * @function create
             * @memberof proto.ContextInfo.AdReplyInfo
             * @static
             * @param {proto.ContextInfo.IAdReplyInfo=} [properties] Properties to set
             * @returns {proto.ContextInfo.AdReplyInfo} AdReplyInfo instance
             */
            AdReplyInfo.create = function create(properties) {
                return new AdReplyInfo(properties);
            };

            /**
             * Encodes the specified AdReplyInfo message. Does not implicitly {@link proto.ContextInfo.AdReplyInfo.verify|verify} messages.
             * @function encode
             * @memberof proto.ContextInfo.AdReplyInfo
             * @static
             * @param {proto.ContextInfo.IAdReplyInfo} message AdReplyInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AdReplyInfo.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.advertiserName != null && Object.hasOwnProperty.call(message, "advertiserName"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.advertiserName);
                if (message.mediaType != null && Object.hasOwnProperty.call(message, "mediaType"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.mediaType);
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    writer.uint32(/* id 16, wireType 2 =*/130).bytes(message.jpegThumbnail);
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    writer.uint32(/* id 17, wireType 2 =*/138).string(message.caption);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AdReplyInfo message, length delimited. Does not implicitly {@link proto.ContextInfo.AdReplyInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.ContextInfo.AdReplyInfo
             * @static
             * @param {proto.ContextInfo.IAdReplyInfo} message AdReplyInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AdReplyInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an AdReplyInfo message from the specified reader or buffer.
             * @function decode
             * @memberof proto.ContextInfo.AdReplyInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.ContextInfo.AdReplyInfo} AdReplyInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AdReplyInfo.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.advertiserName = reader.stringVerify();
                            message._advertiserName = "advertiserName";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.mediaType = reader.int32();
                            message._mediaType = "mediaType";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            message.jpegThumbnail = reader.bytes();
                            message._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            message.caption = reader.stringVerify();
                            message._caption = "caption";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an AdReplyInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.ContextInfo.AdReplyInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.ContextInfo.AdReplyInfo} AdReplyInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AdReplyInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AdReplyInfo message.
             * @function verify
             * @memberof proto.ContextInfo.AdReplyInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AdReplyInfo.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.advertiserName != null && Object.hasOwnProperty.call(message, "advertiserName")) {
                    properties._advertiserName = 1;
                    if (!$util.isString(message.advertiserName))
                        return "advertiserName: string expected";
                }
                if (message.mediaType != null && Object.hasOwnProperty.call(message, "mediaType")) {
                    properties._mediaType = 1;
                    if (typeof message.mediaType !== "number" || (message.mediaType | 0) !== message.mediaType)
                        return "mediaType: enum value expected";
                }
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail")) {
                    properties._jpegThumbnail = 1;
                    if (!(message.jpegThumbnail && typeof message.jpegThumbnail.length === "number" || $util.isString(message.jpegThumbnail)))
                        return "jpegThumbnail: buffer expected";
                }
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption")) {
                    properties._caption = 1;
                    if (!$util.isString(message.caption))
                        return "caption: string expected";
                }
                return null;
            };

            /**
             * Creates an AdReplyInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.ContextInfo.AdReplyInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.ContextInfo.AdReplyInfo} AdReplyInfo
             */
            AdReplyInfo.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.ContextInfo.AdReplyInfo: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.advertiserName != null)
                    message.advertiserName = String(object.advertiserName);
                switch (object.mediaType) {
                case "NONE":
                case 0:
                    message.mediaType = 0;
                    break;
                case "IMAGE":
                case 1:
                    message.mediaType = 1;
                    break;
                case "VIDEO":
                case 2:
                    message.mediaType = 2;
                    break;
                default:
                    if (typeof object.mediaType === "number" && (object.mediaType | 0) === object.mediaType)
                        message.mediaType = object.mediaType;
                }
                if (object.jpegThumbnail != null)
                    if (typeof object.jpegThumbnail === "string")
                        $util.base64.decode(object.jpegThumbnail, message.jpegThumbnail = $util.newBuffer($util.base64.length(object.jpegThumbnail)), 0);
                    else if (object.jpegThumbnail.length >= 0)
                        message.jpegThumbnail = object.jpegThumbnail;
                if (object.caption != null)
                    message.caption = String(object.caption);
                return message;
            };

            /**
             * Creates a plain object from an AdReplyInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.ContextInfo.AdReplyInfo
             * @static
             * @param {proto.ContextInfo.AdReplyInfo} message AdReplyInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AdReplyInfo.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.advertiserName != null && Object.hasOwnProperty.call(message, "advertiserName"))
                    object.advertiserName = message.advertiserName;
                if (message.mediaType != null && Object.hasOwnProperty.call(message, "mediaType"))
                    object.mediaType = options.enums === String ? $root.proto.ContextInfo.AdReplyInfo.MediaType[message.mediaType] === undefined ? message.mediaType : $root.proto.ContextInfo.AdReplyInfo.MediaType[message.mediaType] : message.mediaType;
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    object.jpegThumbnail = options.bytes === String ? $util.base64.encode(message.jpegThumbnail, 0, message.jpegThumbnail.length) : options.bytes === Array ? Array.prototype.slice.call(message.jpegThumbnail) : message.jpegThumbnail;
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    object.caption = message.caption;
                return object;
            };

            /**
             * Converts this AdReplyInfo to JSON.
             * @function toJSON
             * @memberof proto.ContextInfo.AdReplyInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AdReplyInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AdReplyInfo
             * @function getTypeUrl
             * @memberof proto.ContextInfo.AdReplyInfo
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AdReplyInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.ContextInfo.AdReplyInfo";
            };

            /**
             * MediaType enum.
             * @name proto.ContextInfo.AdReplyInfo.MediaType
             * @enum {number}
             * @property {number} NONE=0 NONE value
             * @property {number} IMAGE=1 IMAGE value
             * @property {number} VIDEO=2 VIDEO value
             */
            AdReplyInfo.MediaType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "NONE"] = 0;
                values[valuesById[1] = "IMAGE"] = 1;
                values[valuesById[2] = "VIDEO"] = 2;
                return values;
            })();

            return AdReplyInfo;
        })();

        ContextInfo.BusinessInteractionPills = (function() {

            /**
             * Properties of a BusinessInteractionPills.
             * @memberof proto.ContextInfo
             * @interface IBusinessInteractionPills
             * @property {string|null} [businessJid] BusinessInteractionPills businessJid
             * @property {Array.<proto.ContextInfo.BusinessInteractionPills.IPill>|null} [pills] BusinessInteractionPills pills
             * @property {proto.ContextInfo.BusinessInteractionPills.EntryPoint|null} [entryPoint] BusinessInteractionPills entryPoint
             * @property {Uint8Array|null} [signedPayload] BusinessInteractionPills signedPayload
             * @property {proto.IBotSignatureVerificationMetadata|null} [signatureEnvelope] BusinessInteractionPills signatureEnvelope
             */

            /**
             * Constructs a new BusinessInteractionPills.
             * @memberof proto.ContextInfo
             * @classdesc Represents a BusinessInteractionPills.
             * @implements IBusinessInteractionPills
             * @constructor
             * @param {proto.ContextInfo.IBusinessInteractionPills=} [properties] Properties to set
             */
            function BusinessInteractionPills(properties) {
                this.pills = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * BusinessInteractionPills businessJid.
             * @member {string|null|undefined} businessJid
             * @memberof proto.ContextInfo.BusinessInteractionPills
             * @instance
             */
            BusinessInteractionPills.prototype.businessJid = null;

            /**
             * BusinessInteractionPills pills.
             * @member {Array.<proto.ContextInfo.BusinessInteractionPills.IPill>} pills
             * @memberof proto.ContextInfo.BusinessInteractionPills
             * @instance
             */
            BusinessInteractionPills.prototype.pills = $util.emptyArray;

            /**
             * BusinessInteractionPills entryPoint.
             * @member {proto.ContextInfo.BusinessInteractionPills.EntryPoint|null|undefined} entryPoint
             * @memberof proto.ContextInfo.BusinessInteractionPills
             * @instance
             */
            BusinessInteractionPills.prototype.entryPoint = null;

            /**
             * BusinessInteractionPills signedPayload.
             * @member {Uint8Array|null|undefined} signedPayload
             * @memberof proto.ContextInfo.BusinessInteractionPills
             * @instance
             */
            BusinessInteractionPills.prototype.signedPayload = null;

            /**
             * BusinessInteractionPills signatureEnvelope.
             * @member {proto.IBotSignatureVerificationMetadata|null|undefined} signatureEnvelope
             * @memberof proto.ContextInfo.BusinessInteractionPills
             * @instance
             */
            BusinessInteractionPills.prototype.signatureEnvelope = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessInteractionPills.prototype, "_businessJid", {
                get: $util.oneOfGetter($oneOfFields = ["businessJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessInteractionPills.prototype, "_entryPoint", {
                get: $util.oneOfGetter($oneOfFields = ["entryPoint"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessInteractionPills.prototype, "_signedPayload", {
                get: $util.oneOfGetter($oneOfFields = ["signedPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessInteractionPills.prototype, "_signatureEnvelope", {
                get: $util.oneOfGetter($oneOfFields = ["signatureEnvelope"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BusinessInteractionPills instance using the specified properties.
             * @function create
             * @memberof proto.ContextInfo.BusinessInteractionPills
             * @static
             * @param {proto.ContextInfo.IBusinessInteractionPills=} [properties] Properties to set
             * @returns {proto.ContextInfo.BusinessInteractionPills} BusinessInteractionPills instance
             */
            BusinessInteractionPills.create = function create(properties) {
                return new BusinessInteractionPills(properties);
            };

            /**
             * Encodes the specified BusinessInteractionPills message. Does not implicitly {@link proto.ContextInfo.BusinessInteractionPills.verify|verify} messages.
             * @function encode
             * @memberof proto.ContextInfo.BusinessInteractionPills
             * @static
             * @param {proto.ContextInfo.IBusinessInteractionPills} message BusinessInteractionPills message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BusinessInteractionPills.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.businessJid != null && Object.hasOwnProperty.call(message, "businessJid"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.businessJid);
                if (message.pills != null && message.pills.length)
                    for (var i = 0; i < message.pills.length; ++i)
                        $root.proto.ContextInfo.BusinessInteractionPills.Pill.encode(message.pills[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.entryPoint != null && Object.hasOwnProperty.call(message, "entryPoint"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.entryPoint);
                if (message.signedPayload != null && Object.hasOwnProperty.call(message, "signedPayload"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.signedPayload);
                if (message.signatureEnvelope != null && Object.hasOwnProperty.call(message, "signatureEnvelope"))
                    $root.proto.BotSignatureVerificationMetadata.encode(message.signatureEnvelope, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified BusinessInteractionPills message, length delimited. Does not implicitly {@link proto.ContextInfo.BusinessInteractionPills.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.ContextInfo.BusinessInteractionPills
             * @static
             * @param {proto.ContextInfo.IBusinessInteractionPills} message BusinessInteractionPills message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BusinessInteractionPills.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a BusinessInteractionPills message from the specified reader or buffer.
             * @function decode
             * @memberof proto.ContextInfo.BusinessInteractionPills
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.ContextInfo.BusinessInteractionPills} BusinessInteractionPills
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BusinessInteractionPills.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.businessJid = reader.stringVerify();
                            message._businessJid = "businessJid";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(message.pills && message.pills.length))
                                message.pills = [];
                            message.pills.push($root.proto.ContextInfo.BusinessInteractionPills.Pill.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.entryPoint = reader.int32();
                            message._entryPoint = "entryPoint";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.signedPayload = reader.bytes();
                            message._signedPayload = "signedPayload";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.signatureEnvelope = $root.proto.BotSignatureVerificationMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.signatureEnvelope);
                            message._signatureEnvelope = "signatureEnvelope";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a BusinessInteractionPills message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.ContextInfo.BusinessInteractionPills
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.ContextInfo.BusinessInteractionPills} BusinessInteractionPills
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BusinessInteractionPills.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BusinessInteractionPills message.
             * @function verify
             * @memberof proto.ContextInfo.BusinessInteractionPills
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BusinessInteractionPills.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.businessJid != null && Object.hasOwnProperty.call(message, "businessJid")) {
                    properties._businessJid = 1;
                    if (!$util.isString(message.businessJid))
                        return "businessJid: string expected";
                }
                if (message.pills != null && Object.hasOwnProperty.call(message, "pills")) {
                    if (!Array.isArray(message.pills))
                        return "pills: array expected";
                    for (var i = 0; i < message.pills.length; ++i) {
                        var error = $root.proto.ContextInfo.BusinessInteractionPills.Pill.verify(message.pills[i], q + 1);
                        if (error)
                            return "pills." + error;
                    }
                }
                if (message.entryPoint != null && Object.hasOwnProperty.call(message, "entryPoint")) {
                    properties._entryPoint = 1;
                    if (typeof message.entryPoint !== "number" || (message.entryPoint | 0) !== message.entryPoint)
                        return "entryPoint: enum value expected";
                }
                if (message.signedPayload != null && Object.hasOwnProperty.call(message, "signedPayload")) {
                    properties._signedPayload = 1;
                    if (!(message.signedPayload && typeof message.signedPayload.length === "number" || $util.isString(message.signedPayload)))
                        return "signedPayload: buffer expected";
                }
                if (message.signatureEnvelope != null && Object.hasOwnProperty.call(message, "signatureEnvelope")) {
                    properties._signatureEnvelope = 1;
                    {
                        var error = $root.proto.BotSignatureVerificationMetadata.verify(message.signatureEnvelope, q + 1);
                        if (error)
                            return "signatureEnvelope." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a BusinessInteractionPills message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.ContextInfo.BusinessInteractionPills
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.ContextInfo.BusinessInteractionPills} BusinessInteractionPills
             */
            BusinessInteractionPills.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.ContextInfo.BusinessInteractionPills: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.businessJid != null)
                    message.businessJid = String(object.businessJid);
                if (object.pills) {
                    if (!Array.isArray(object.pills))
                        throw TypeError(".proto.ContextInfo.BusinessInteractionPills.pills: array expected");
                    message.pills = Array(object.pills.length);
                    for (var i = 0; i < object.pills.length; ++i) {
                        if (!$util.isObject(object.pills[i]))
                            throw TypeError(".proto.ContextInfo.BusinessInteractionPills.pills: object expected");
                        message.pills[i] = $root.proto.ContextInfo.BusinessInteractionPills.Pill.fromObject(object.pills[i], q + 1);
                    }
                }
                switch (object.entryPoint) {
                case "ENTRY_POINT_UNKNOWN":
                case 0:
                    message.entryPoint = 0;
                    break;
                case "P2P_LINK_SHARE":
                case 1:
                    message.entryPoint = 1;
                    break;
                case "CONTACT_CARD_SHARING":
                case 2:
                    message.entryPoint = 2;
                    break;
                case "PHONE_NUMBER":
                case 3:
                    message.entryPoint = 3;
                    break;
                case "STATUS":
                case 4:
                    message.entryPoint = 4;
                    break;
                case "IN_THREAD_CONTEXT_CARD":
                case 5:
                    message.entryPoint = 5;
                    break;
                default:
                    if (typeof object.entryPoint === "number" && (object.entryPoint | 0) === object.entryPoint)
                        message.entryPoint = object.entryPoint;
                }
                if (object.signedPayload != null)
                    if (typeof object.signedPayload === "string")
                        $util.base64.decode(object.signedPayload, message.signedPayload = $util.newBuffer($util.base64.length(object.signedPayload)), 0);
                    else if (object.signedPayload.length >= 0)
                        message.signedPayload = object.signedPayload;
                if (object.signatureEnvelope != null) {
                    if (!$util.isObject(object.signatureEnvelope))
                        throw TypeError(".proto.ContextInfo.BusinessInteractionPills.signatureEnvelope: object expected");
                    message.signatureEnvelope = $root.proto.BotSignatureVerificationMetadata.fromObject(object.signatureEnvelope, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a BusinessInteractionPills message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.ContextInfo.BusinessInteractionPills
             * @static
             * @param {proto.ContextInfo.BusinessInteractionPills} message BusinessInteractionPills
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BusinessInteractionPills.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.pills = [];
                if (message.businessJid != null && Object.hasOwnProperty.call(message, "businessJid"))
                    object.businessJid = message.businessJid;
                if (message.pills && message.pills.length) {
                    object.pills = Array(message.pills.length);
                    for (var j = 0; j < message.pills.length; ++j)
                        object.pills[j] = $root.proto.ContextInfo.BusinessInteractionPills.Pill.toObject(message.pills[j], options, q + 1);
                }
                if (message.entryPoint != null && Object.hasOwnProperty.call(message, "entryPoint"))
                    object.entryPoint = options.enums === String ? $root.proto.ContextInfo.BusinessInteractionPills.EntryPoint[message.entryPoint] === undefined ? message.entryPoint : $root.proto.ContextInfo.BusinessInteractionPills.EntryPoint[message.entryPoint] : message.entryPoint;
                if (message.signedPayload != null && Object.hasOwnProperty.call(message, "signedPayload"))
                    object.signedPayload = options.bytes === String ? $util.base64.encode(message.signedPayload, 0, message.signedPayload.length) : options.bytes === Array ? Array.prototype.slice.call(message.signedPayload) : message.signedPayload;
                if (message.signatureEnvelope != null && Object.hasOwnProperty.call(message, "signatureEnvelope"))
                    object.signatureEnvelope = $root.proto.BotSignatureVerificationMetadata.toObject(message.signatureEnvelope, options, q + 1);
                return object;
            };

            /**
             * Converts this BusinessInteractionPills to JSON.
             * @function toJSON
             * @memberof proto.ContextInfo.BusinessInteractionPills
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BusinessInteractionPills.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for BusinessInteractionPills
             * @function getTypeUrl
             * @memberof proto.ContextInfo.BusinessInteractionPills
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            BusinessInteractionPills.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.ContextInfo.BusinessInteractionPills";
            };

            /**
             * EntryPoint enum.
             * @name proto.ContextInfo.BusinessInteractionPills.EntryPoint
             * @enum {number}
             * @property {number} ENTRY_POINT_UNKNOWN=0 ENTRY_POINT_UNKNOWN value
             * @property {number} P2P_LINK_SHARE=1 P2P_LINK_SHARE value
             * @property {number} CONTACT_CARD_SHARING=2 CONTACT_CARD_SHARING value
             * @property {number} PHONE_NUMBER=3 PHONE_NUMBER value
             * @property {number} STATUS=4 STATUS value
             * @property {number} IN_THREAD_CONTEXT_CARD=5 IN_THREAD_CONTEXT_CARD value
             */
            BusinessInteractionPills.EntryPoint = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "ENTRY_POINT_UNKNOWN"] = 0;
                values[valuesById[1] = "P2P_LINK_SHARE"] = 1;
                values[valuesById[2] = "CONTACT_CARD_SHARING"] = 2;
                values[valuesById[3] = "PHONE_NUMBER"] = 3;
                values[valuesById[4] = "STATUS"] = 4;
                values[valuesById[5] = "IN_THREAD_CONTEXT_CARD"] = 5;
                return values;
            })();

            BusinessInteractionPills.Pill = (function() {

                /**
                 * Properties of a Pill.
                 * @memberof proto.ContextInfo.BusinessInteractionPills
                 * @interface IPill
                 * @property {proto.ContextInfo.BusinessInteractionPills.PillType|null} [pillType] Pill pillType
                 * @property {string|null} [actionUrl] Pill actionUrl
                 */

                /**
                 * Constructs a new Pill.
                 * @memberof proto.ContextInfo.BusinessInteractionPills
                 * @classdesc Represents a Pill.
                 * @implements IPill
                 * @constructor
                 * @param {proto.ContextInfo.BusinessInteractionPills.IPill=} [properties] Properties to set
                 */
                function Pill(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * Pill pillType.
                 * @member {proto.ContextInfo.BusinessInteractionPills.PillType|null|undefined} pillType
                 * @memberof proto.ContextInfo.BusinessInteractionPills.Pill
                 * @instance
                 */
                Pill.prototype.pillType = null;

                /**
                 * Pill actionUrl.
                 * @member {string|null|undefined} actionUrl
                 * @memberof proto.ContextInfo.BusinessInteractionPills.Pill
                 * @instance
                 */
                Pill.prototype.actionUrl = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Pill.prototype, "_pillType", {
                    get: $util.oneOfGetter($oneOfFields = ["pillType"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Pill.prototype, "_actionUrl", {
                    get: $util.oneOfGetter($oneOfFields = ["actionUrl"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new Pill instance using the specified properties.
                 * @function create
                 * @memberof proto.ContextInfo.BusinessInteractionPills.Pill
                 * @static
                 * @param {proto.ContextInfo.BusinessInteractionPills.IPill=} [properties] Properties to set
                 * @returns {proto.ContextInfo.BusinessInteractionPills.Pill} Pill instance
                 */
                Pill.create = function create(properties) {
                    return new Pill(properties);
                };

                /**
                 * Encodes the specified Pill message. Does not implicitly {@link proto.ContextInfo.BusinessInteractionPills.Pill.verify|verify} messages.
                 * @function encode
                 * @memberof proto.ContextInfo.BusinessInteractionPills.Pill
                 * @static
                 * @param {proto.ContextInfo.BusinessInteractionPills.IPill} message Pill message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Pill.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.pillType != null && Object.hasOwnProperty.call(message, "pillType"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int32(message.pillType);
                    if (message.actionUrl != null && Object.hasOwnProperty.call(message, "actionUrl"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.actionUrl);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified Pill message, length delimited. Does not implicitly {@link proto.ContextInfo.BusinessInteractionPills.Pill.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.ContextInfo.BusinessInteractionPills.Pill
                 * @static
                 * @param {proto.ContextInfo.BusinessInteractionPills.IPill} message Pill message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Pill.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a Pill message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.ContextInfo.BusinessInteractionPills.Pill
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.ContextInfo.BusinessInteractionPills.Pill} Pill
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Pill.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                message.pillType = reader.int32();
                                message._pillType = "pillType";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.actionUrl = reader.stringVerify();
                                message._actionUrl = "actionUrl";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a Pill message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.ContextInfo.BusinessInteractionPills.Pill
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.ContextInfo.BusinessInteractionPills.Pill} Pill
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Pill.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a Pill message.
                 * @function verify
                 * @memberof proto.ContextInfo.BusinessInteractionPills.Pill
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                Pill.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.pillType != null && Object.hasOwnProperty.call(message, "pillType")) {
                        properties._pillType = 1;
                        if (typeof message.pillType !== "number" || (message.pillType | 0) !== message.pillType)
                            return "pillType: enum value expected";
                    }
                    if (message.actionUrl != null && Object.hasOwnProperty.call(message, "actionUrl")) {
                        properties._actionUrl = 1;
                        if (!$util.isString(message.actionUrl))
                            return "actionUrl: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a Pill message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.ContextInfo.BusinessInteractionPills.Pill
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.ContextInfo.BusinessInteractionPills.Pill} Pill
                 */
                Pill.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.ContextInfo.BusinessInteractionPills.Pill: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    switch (object.pillType) {
                    case "UNKNOWN":
                    case 0:
                        message.pillType = 0;
                        break;
                    case "VIEW_BUSINESS":
                    case 1:
                        message.pillType = 1;
                        break;
                    case "CHAT":
                    case 2:
                        message.pillType = 2;
                        break;
                    case "CALL":
                    case 3:
                        message.pillType = 3;
                        break;
                    case "CATALOG":
                    case 4:
                        message.pillType = 4;
                        break;
                    case "CHANNEL":
                    case 5:
                        message.pillType = 5;
                        break;
                    case "BOOK_APPOINTMENT":
                    case 6:
                        message.pillType = 6;
                        break;
                    case "OFFERS":
                    case 7:
                        message.pillType = 7;
                        break;
                    case "BESTSELLERS":
                    case 8:
                        message.pillType = 8;
                        break;
                    case "MENU":
                    case 9:
                        message.pillType = 9;
                        break;
                    case "ABOUT":
                    case 10:
                        message.pillType = 10;
                        break;
                    case "SHOP":
                    case 11:
                        message.pillType = 11;
                        break;
                    case "ORDER":
                    case 12:
                        message.pillType = 12;
                        break;
                    default:
                        if (typeof object.pillType === "number" && (object.pillType | 0) === object.pillType)
                            message.pillType = object.pillType;
                    }
                    if (object.actionUrl != null)
                        message.actionUrl = String(object.actionUrl);
                    return message;
                };

                /**
                 * Creates a plain object from a Pill message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.ContextInfo.BusinessInteractionPills.Pill
                 * @static
                 * @param {proto.ContextInfo.BusinessInteractionPills.Pill} message Pill
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                Pill.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.pillType != null && Object.hasOwnProperty.call(message, "pillType"))
                        object.pillType = options.enums === String ? $root.proto.ContextInfo.BusinessInteractionPills.PillType[message.pillType] === undefined ? message.pillType : $root.proto.ContextInfo.BusinessInteractionPills.PillType[message.pillType] : message.pillType;
                    if (message.actionUrl != null && Object.hasOwnProperty.call(message, "actionUrl"))
                        object.actionUrl = message.actionUrl;
                    return object;
                };

                /**
                 * Converts this Pill to JSON.
                 * @function toJSON
                 * @memberof proto.ContextInfo.BusinessInteractionPills.Pill
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                Pill.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for Pill
                 * @function getTypeUrl
                 * @memberof proto.ContextInfo.BusinessInteractionPills.Pill
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                Pill.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.ContextInfo.BusinessInteractionPills.Pill";
                };

                return Pill;
            })();

            /**
             * PillType enum.
             * @name proto.ContextInfo.BusinessInteractionPills.PillType
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} VIEW_BUSINESS=1 VIEW_BUSINESS value
             * @property {number} CHAT=2 CHAT value
             * @property {number} CALL=3 CALL value
             * @property {number} CATALOG=4 CATALOG value
             * @property {number} CHANNEL=5 CHANNEL value
             * @property {number} BOOK_APPOINTMENT=6 BOOK_APPOINTMENT value
             * @property {number} OFFERS=7 OFFERS value
             * @property {number} BESTSELLERS=8 BESTSELLERS value
             * @property {number} MENU=9 MENU value
             * @property {number} ABOUT=10 ABOUT value
             * @property {number} SHOP=11 SHOP value
             * @property {number} ORDER=12 ORDER value
             */
            BusinessInteractionPills.PillType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "VIEW_BUSINESS"] = 1;
                values[valuesById[2] = "CHAT"] = 2;
                values[valuesById[3] = "CALL"] = 3;
                values[valuesById[4] = "CATALOG"] = 4;
                values[valuesById[5] = "CHANNEL"] = 5;
                values[valuesById[6] = "BOOK_APPOINTMENT"] = 6;
                values[valuesById[7] = "OFFERS"] = 7;
                values[valuesById[8] = "BESTSELLERS"] = 8;
                values[valuesById[9] = "MENU"] = 9;
                values[valuesById[10] = "ABOUT"] = 10;
                values[valuesById[11] = "SHOP"] = 11;
                values[valuesById[12] = "ORDER"] = 12;
                return values;
            })();

            BusinessInteractionPills.SignedPayload = (function() {

                /**
                 * Properties of a SignedPayload.
                 * @memberof proto.ContextInfo.BusinessInteractionPills
                 * @interface ISignedPayload
                 * @property {string|null} [verifiedName] SignedPayload verifiedName
                 * @property {Array.<proto.ContextInfo.BusinessInteractionPills.IPill>|null} [pills] SignedPayload pills
                 */

                /**
                 * Constructs a new SignedPayload.
                 * @memberof proto.ContextInfo.BusinessInteractionPills
                 * @classdesc Represents a SignedPayload.
                 * @implements ISignedPayload
                 * @constructor
                 * @param {proto.ContextInfo.BusinessInteractionPills.ISignedPayload=} [properties] Properties to set
                 */
                function SignedPayload(properties) {
                    this.pills = [];
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * SignedPayload verifiedName.
                 * @member {string|null|undefined} verifiedName
                 * @memberof proto.ContextInfo.BusinessInteractionPills.SignedPayload
                 * @instance
                 */
                SignedPayload.prototype.verifiedName = null;

                /**
                 * SignedPayload pills.
                 * @member {Array.<proto.ContextInfo.BusinessInteractionPills.IPill>} pills
                 * @memberof proto.ContextInfo.BusinessInteractionPills.SignedPayload
                 * @instance
                 */
                SignedPayload.prototype.pills = $util.emptyArray;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SignedPayload.prototype, "_verifiedName", {
                    get: $util.oneOfGetter($oneOfFields = ["verifiedName"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new SignedPayload instance using the specified properties.
                 * @function create
                 * @memberof proto.ContextInfo.BusinessInteractionPills.SignedPayload
                 * @static
                 * @param {proto.ContextInfo.BusinessInteractionPills.ISignedPayload=} [properties] Properties to set
                 * @returns {proto.ContextInfo.BusinessInteractionPills.SignedPayload} SignedPayload instance
                 */
                SignedPayload.create = function create(properties) {
                    return new SignedPayload(properties);
                };

                /**
                 * Encodes the specified SignedPayload message. Does not implicitly {@link proto.ContextInfo.BusinessInteractionPills.SignedPayload.verify|verify} messages.
                 * @function encode
                 * @memberof proto.ContextInfo.BusinessInteractionPills.SignedPayload
                 * @static
                 * @param {proto.ContextInfo.BusinessInteractionPills.ISignedPayload} message SignedPayload message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                SignedPayload.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.verifiedName != null && Object.hasOwnProperty.call(message, "verifiedName"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.verifiedName);
                    if (message.pills != null && message.pills.length)
                        for (var i = 0; i < message.pills.length; ++i)
                            $root.proto.ContextInfo.BusinessInteractionPills.Pill.encode(message.pills[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified SignedPayload message, length delimited. Does not implicitly {@link proto.ContextInfo.BusinessInteractionPills.SignedPayload.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.ContextInfo.BusinessInteractionPills.SignedPayload
                 * @static
                 * @param {proto.ContextInfo.BusinessInteractionPills.ISignedPayload} message SignedPayload message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                SignedPayload.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a SignedPayload message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.ContextInfo.BusinessInteractionPills.SignedPayload
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.ContextInfo.BusinessInteractionPills.SignedPayload} SignedPayload
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                SignedPayload.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.verifiedName = reader.stringVerify();
                                message._verifiedName = "verifiedName";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                if (!(message.pills && message.pills.length))
                                    message.pills = [];
                                message.pills.push($root.proto.ContextInfo.BusinessInteractionPills.Pill.decode(reader, reader.uint32(), undefined, q + 1));
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a SignedPayload message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.ContextInfo.BusinessInteractionPills.SignedPayload
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.ContextInfo.BusinessInteractionPills.SignedPayload} SignedPayload
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                SignedPayload.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a SignedPayload message.
                 * @function verify
                 * @memberof proto.ContextInfo.BusinessInteractionPills.SignedPayload
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                SignedPayload.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.verifiedName != null && Object.hasOwnProperty.call(message, "verifiedName")) {
                        properties._verifiedName = 1;
                        if (!$util.isString(message.verifiedName))
                            return "verifiedName: string expected";
                    }
                    if (message.pills != null && Object.hasOwnProperty.call(message, "pills")) {
                        if (!Array.isArray(message.pills))
                            return "pills: array expected";
                        for (var i = 0; i < message.pills.length; ++i) {
                            var error = $root.proto.ContextInfo.BusinessInteractionPills.Pill.verify(message.pills[i], q + 1);
                            if (error)
                                return "pills." + error;
                        }
                    }
                    return null;
                };

                /**
                 * Creates a SignedPayload message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.ContextInfo.BusinessInteractionPills.SignedPayload
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.ContextInfo.BusinessInteractionPills.SignedPayload} SignedPayload
                 */
                SignedPayload.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.ContextInfo.BusinessInteractionPills.SignedPayload: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.verifiedName != null)
                        message.verifiedName = String(object.verifiedName);
                    if (object.pills) {
                        if (!Array.isArray(object.pills))
                            throw TypeError(".proto.ContextInfo.BusinessInteractionPills.SignedPayload.pills: array expected");
                        message.pills = Array(object.pills.length);
                        for (var i = 0; i < object.pills.length; ++i) {
                            if (!$util.isObject(object.pills[i]))
                                throw TypeError(".proto.ContextInfo.BusinessInteractionPills.SignedPayload.pills: object expected");
                            message.pills[i] = $root.proto.ContextInfo.BusinessInteractionPills.Pill.fromObject(object.pills[i], q + 1);
                        }
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a SignedPayload message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.ContextInfo.BusinessInteractionPills.SignedPayload
                 * @static
                 * @param {proto.ContextInfo.BusinessInteractionPills.SignedPayload} message SignedPayload
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                SignedPayload.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (options.arrays || options.defaults)
                        object.pills = [];
                    if (message.verifiedName != null && Object.hasOwnProperty.call(message, "verifiedName"))
                        object.verifiedName = message.verifiedName;
                    if (message.pills && message.pills.length) {
                        object.pills = Array(message.pills.length);
                        for (var j = 0; j < message.pills.length; ++j)
                            object.pills[j] = $root.proto.ContextInfo.BusinessInteractionPills.Pill.toObject(message.pills[j], options, q + 1);
                    }
                    return object;
                };

                /**
                 * Converts this SignedPayload to JSON.
                 * @function toJSON
                 * @memberof proto.ContextInfo.BusinessInteractionPills.SignedPayload
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                SignedPayload.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for SignedPayload
                 * @function getTypeUrl
                 * @memberof proto.ContextInfo.BusinessInteractionPills.SignedPayload
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                SignedPayload.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.ContextInfo.BusinessInteractionPills.SignedPayload";
                };

                return SignedPayload;
            })();

            return BusinessInteractionPills;
        })();

        ContextInfo.BusinessMessageForwardInfo = (function() {

            /**
             * Properties of a BusinessMessageForwardInfo.
             * @memberof proto.ContextInfo
             * @interface IBusinessMessageForwardInfo
             * @property {string|null} [businessOwnerJid] BusinessMessageForwardInfo businessOwnerJid
             */

            /**
             * Constructs a new BusinessMessageForwardInfo.
             * @memberof proto.ContextInfo
             * @classdesc Represents a BusinessMessageForwardInfo.
             * @implements IBusinessMessageForwardInfo
             * @constructor
             * @param {proto.ContextInfo.IBusinessMessageForwardInfo=} [properties] Properties to set
             */
            function BusinessMessageForwardInfo(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * BusinessMessageForwardInfo businessOwnerJid.
             * @member {string|null|undefined} businessOwnerJid
             * @memberof proto.ContextInfo.BusinessMessageForwardInfo
             * @instance
             */
            BusinessMessageForwardInfo.prototype.businessOwnerJid = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessMessageForwardInfo.prototype, "_businessOwnerJid", {
                get: $util.oneOfGetter($oneOfFields = ["businessOwnerJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BusinessMessageForwardInfo instance using the specified properties.
             * @function create
             * @memberof proto.ContextInfo.BusinessMessageForwardInfo
             * @static
             * @param {proto.ContextInfo.IBusinessMessageForwardInfo=} [properties] Properties to set
             * @returns {proto.ContextInfo.BusinessMessageForwardInfo} BusinessMessageForwardInfo instance
             */
            BusinessMessageForwardInfo.create = function create(properties) {
                return new BusinessMessageForwardInfo(properties);
            };

            /**
             * Encodes the specified BusinessMessageForwardInfo message. Does not implicitly {@link proto.ContextInfo.BusinessMessageForwardInfo.verify|verify} messages.
             * @function encode
             * @memberof proto.ContextInfo.BusinessMessageForwardInfo
             * @static
             * @param {proto.ContextInfo.IBusinessMessageForwardInfo} message BusinessMessageForwardInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BusinessMessageForwardInfo.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.businessOwnerJid != null && Object.hasOwnProperty.call(message, "businessOwnerJid"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.businessOwnerJid);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified BusinessMessageForwardInfo message, length delimited. Does not implicitly {@link proto.ContextInfo.BusinessMessageForwardInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.ContextInfo.BusinessMessageForwardInfo
             * @static
             * @param {proto.ContextInfo.IBusinessMessageForwardInfo} message BusinessMessageForwardInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BusinessMessageForwardInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a BusinessMessageForwardInfo message from the specified reader or buffer.
             * @function decode
             * @memberof proto.ContextInfo.BusinessMessageForwardInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.ContextInfo.BusinessMessageForwardInfo} BusinessMessageForwardInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BusinessMessageForwardInfo.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.businessOwnerJid = reader.stringVerify();
                            message._businessOwnerJid = "businessOwnerJid";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a BusinessMessageForwardInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.ContextInfo.BusinessMessageForwardInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.ContextInfo.BusinessMessageForwardInfo} BusinessMessageForwardInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BusinessMessageForwardInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BusinessMessageForwardInfo message.
             * @function verify
             * @memberof proto.ContextInfo.BusinessMessageForwardInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BusinessMessageForwardInfo.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.businessOwnerJid != null && Object.hasOwnProperty.call(message, "businessOwnerJid")) {
                    properties._businessOwnerJid = 1;
                    if (!$util.isString(message.businessOwnerJid))
                        return "businessOwnerJid: string expected";
                }
                return null;
            };

            /**
             * Creates a BusinessMessageForwardInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.ContextInfo.BusinessMessageForwardInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.ContextInfo.BusinessMessageForwardInfo} BusinessMessageForwardInfo
             */
            BusinessMessageForwardInfo.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.ContextInfo.BusinessMessageForwardInfo: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.businessOwnerJid != null)
                    message.businessOwnerJid = String(object.businessOwnerJid);
                return message;
            };

            /**
             * Creates a plain object from a BusinessMessageForwardInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.ContextInfo.BusinessMessageForwardInfo
             * @static
             * @param {proto.ContextInfo.BusinessMessageForwardInfo} message BusinessMessageForwardInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BusinessMessageForwardInfo.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.businessOwnerJid != null && Object.hasOwnProperty.call(message, "businessOwnerJid"))
                    object.businessOwnerJid = message.businessOwnerJid;
                return object;
            };

            /**
             * Converts this BusinessMessageForwardInfo to JSON.
             * @function toJSON
             * @memberof proto.ContextInfo.BusinessMessageForwardInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BusinessMessageForwardInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for BusinessMessageForwardInfo
             * @function getTypeUrl
             * @memberof proto.ContextInfo.BusinessMessageForwardInfo
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            BusinessMessageForwardInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.ContextInfo.BusinessMessageForwardInfo";
            };

            return BusinessMessageForwardInfo;
        })();

        /**
         * CrossAppSource enum.
         * @name proto.ContextInfo.CrossAppSource
         * @enum {number}
         * @property {number} CROSS_APP_SOURCE_UNKNOWN=0 CROSS_APP_SOURCE_UNKNOWN value
         * @property {number} CROSS_APP_SOURCE_INSTAGRAM=1 CROSS_APP_SOURCE_INSTAGRAM value
         * @property {number} CROSS_APP_SOURCE_FACEBOOK=2 CROSS_APP_SOURCE_FACEBOOK value
         */
        ContextInfo.CrossAppSource = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "CROSS_APP_SOURCE_UNKNOWN"] = 0;
            values[valuesById[1] = "CROSS_APP_SOURCE_INSTAGRAM"] = 1;
            values[valuesById[2] = "CROSS_APP_SOURCE_FACEBOOK"] = 2;
            return values;
        })();

        ContextInfo.DataSharingContext = (function() {

            /**
             * Properties of a DataSharingContext.
             * @memberof proto.ContextInfo
             * @interface IDataSharingContext
             * @property {boolean|null} [showMmDisclosure] DataSharingContext showMmDisclosure
             * @property {string|null} [encryptedSignalTokenConsented] DataSharingContext encryptedSignalTokenConsented
             * @property {Array.<proto.ContextInfo.DataSharingContext.IParameters>|null} [parameters] DataSharingContext parameters
             * @property {number|null} [dataSharingFlags] DataSharingContext dataSharingFlags
             */

            /**
             * Constructs a new DataSharingContext.
             * @memberof proto.ContextInfo
             * @classdesc Represents a DataSharingContext.
             * @implements IDataSharingContext
             * @constructor
             * @param {proto.ContextInfo.IDataSharingContext=} [properties] Properties to set
             */
            function DataSharingContext(properties) {
                this.parameters = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * DataSharingContext showMmDisclosure.
             * @member {boolean|null|undefined} showMmDisclosure
             * @memberof proto.ContextInfo.DataSharingContext
             * @instance
             */
            DataSharingContext.prototype.showMmDisclosure = null;

            /**
             * DataSharingContext encryptedSignalTokenConsented.
             * @member {string|null|undefined} encryptedSignalTokenConsented
             * @memberof proto.ContextInfo.DataSharingContext
             * @instance
             */
            DataSharingContext.prototype.encryptedSignalTokenConsented = null;

            /**
             * DataSharingContext parameters.
             * @member {Array.<proto.ContextInfo.DataSharingContext.IParameters>} parameters
             * @memberof proto.ContextInfo.DataSharingContext
             * @instance
             */
            DataSharingContext.prototype.parameters = $util.emptyArray;

            /**
             * DataSharingContext dataSharingFlags.
             * @member {number|null|undefined} dataSharingFlags
             * @memberof proto.ContextInfo.DataSharingContext
             * @instance
             */
            DataSharingContext.prototype.dataSharingFlags = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DataSharingContext.prototype, "_showMmDisclosure", {
                get: $util.oneOfGetter($oneOfFields = ["showMmDisclosure"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DataSharingContext.prototype, "_encryptedSignalTokenConsented", {
                get: $util.oneOfGetter($oneOfFields = ["encryptedSignalTokenConsented"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DataSharingContext.prototype, "_dataSharingFlags", {
                get: $util.oneOfGetter($oneOfFields = ["dataSharingFlags"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new DataSharingContext instance using the specified properties.
             * @function create
             * @memberof proto.ContextInfo.DataSharingContext
             * @static
             * @param {proto.ContextInfo.IDataSharingContext=} [properties] Properties to set
             * @returns {proto.ContextInfo.DataSharingContext} DataSharingContext instance
             */
            DataSharingContext.create = function create(properties) {
                return new DataSharingContext(properties);
            };

            /**
             * Encodes the specified DataSharingContext message. Does not implicitly {@link proto.ContextInfo.DataSharingContext.verify|verify} messages.
             * @function encode
             * @memberof proto.ContextInfo.DataSharingContext
             * @static
             * @param {proto.ContextInfo.IDataSharingContext} message DataSharingContext message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DataSharingContext.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.showMmDisclosure != null && Object.hasOwnProperty.call(message, "showMmDisclosure"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.showMmDisclosure);
                if (message.encryptedSignalTokenConsented != null && Object.hasOwnProperty.call(message, "encryptedSignalTokenConsented"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.encryptedSignalTokenConsented);
                if (message.parameters != null && message.parameters.length)
                    for (var i = 0; i < message.parameters.length; ++i)
                        $root.proto.ContextInfo.DataSharingContext.Parameters.encode(message.parameters[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.dataSharingFlags != null && Object.hasOwnProperty.call(message, "dataSharingFlags"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int32(message.dataSharingFlags);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DataSharingContext message, length delimited. Does not implicitly {@link proto.ContextInfo.DataSharingContext.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.ContextInfo.DataSharingContext
             * @static
             * @param {proto.ContextInfo.IDataSharingContext} message DataSharingContext message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DataSharingContext.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a DataSharingContext message from the specified reader or buffer.
             * @function decode
             * @memberof proto.ContextInfo.DataSharingContext
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.ContextInfo.DataSharingContext} DataSharingContext
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DataSharingContext.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.showMmDisclosure = reader.bool();
                            message._showMmDisclosure = "showMmDisclosure";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.encryptedSignalTokenConsented = reader.stringVerify();
                            message._encryptedSignalTokenConsented = "encryptedSignalTokenConsented";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            if (!(message.parameters && message.parameters.length))
                                message.parameters = [];
                            message.parameters.push($root.proto.ContextInfo.DataSharingContext.Parameters.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.dataSharingFlags = reader.int32();
                            message._dataSharingFlags = "dataSharingFlags";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a DataSharingContext message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.ContextInfo.DataSharingContext
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.ContextInfo.DataSharingContext} DataSharingContext
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DataSharingContext.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DataSharingContext message.
             * @function verify
             * @memberof proto.ContextInfo.DataSharingContext
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DataSharingContext.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.showMmDisclosure != null && Object.hasOwnProperty.call(message, "showMmDisclosure")) {
                    properties._showMmDisclosure = 1;
                    if (typeof message.showMmDisclosure !== "boolean")
                        return "showMmDisclosure: boolean expected";
                }
                if (message.encryptedSignalTokenConsented != null && Object.hasOwnProperty.call(message, "encryptedSignalTokenConsented")) {
                    properties._encryptedSignalTokenConsented = 1;
                    if (!$util.isString(message.encryptedSignalTokenConsented))
                        return "encryptedSignalTokenConsented: string expected";
                }
                if (message.parameters != null && Object.hasOwnProperty.call(message, "parameters")) {
                    if (!Array.isArray(message.parameters))
                        return "parameters: array expected";
                    for (var i = 0; i < message.parameters.length; ++i) {
                        var error = $root.proto.ContextInfo.DataSharingContext.Parameters.verify(message.parameters[i], q + 1);
                        if (error)
                            return "parameters." + error;
                    }
                }
                if (message.dataSharingFlags != null && Object.hasOwnProperty.call(message, "dataSharingFlags")) {
                    properties._dataSharingFlags = 1;
                    if (!$util.isInteger(message.dataSharingFlags))
                        return "dataSharingFlags: integer expected";
                }
                return null;
            };

            /**
             * Creates a DataSharingContext message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.ContextInfo.DataSharingContext
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.ContextInfo.DataSharingContext} DataSharingContext
             */
            DataSharingContext.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.ContextInfo.DataSharingContext: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.showMmDisclosure != null)
                    message.showMmDisclosure = Boolean(object.showMmDisclosure);
                if (object.encryptedSignalTokenConsented != null)
                    message.encryptedSignalTokenConsented = String(object.encryptedSignalTokenConsented);
                if (object.parameters) {
                    if (!Array.isArray(object.parameters))
                        throw TypeError(".proto.ContextInfo.DataSharingContext.parameters: array expected");
                    message.parameters = Array(object.parameters.length);
                    for (var i = 0; i < object.parameters.length; ++i) {
                        if (!$util.isObject(object.parameters[i]))
                            throw TypeError(".proto.ContextInfo.DataSharingContext.parameters: object expected");
                        message.parameters[i] = $root.proto.ContextInfo.DataSharingContext.Parameters.fromObject(object.parameters[i], q + 1);
                    }
                }
                if (object.dataSharingFlags != null)
                    message.dataSharingFlags = object.dataSharingFlags | 0;
                return message;
            };

            /**
             * Creates a plain object from a DataSharingContext message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.ContextInfo.DataSharingContext
             * @static
             * @param {proto.ContextInfo.DataSharingContext} message DataSharingContext
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DataSharingContext.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.parameters = [];
                if (message.showMmDisclosure != null && Object.hasOwnProperty.call(message, "showMmDisclosure"))
                    object.showMmDisclosure = message.showMmDisclosure;
                if (message.encryptedSignalTokenConsented != null && Object.hasOwnProperty.call(message, "encryptedSignalTokenConsented"))
                    object.encryptedSignalTokenConsented = message.encryptedSignalTokenConsented;
                if (message.parameters && message.parameters.length) {
                    object.parameters = Array(message.parameters.length);
                    for (var j = 0; j < message.parameters.length; ++j)
                        object.parameters[j] = $root.proto.ContextInfo.DataSharingContext.Parameters.toObject(message.parameters[j], options, q + 1);
                }
                if (message.dataSharingFlags != null && Object.hasOwnProperty.call(message, "dataSharingFlags"))
                    object.dataSharingFlags = message.dataSharingFlags;
                return object;
            };

            /**
             * Converts this DataSharingContext to JSON.
             * @function toJSON
             * @memberof proto.ContextInfo.DataSharingContext
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DataSharingContext.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for DataSharingContext
             * @function getTypeUrl
             * @memberof proto.ContextInfo.DataSharingContext
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            DataSharingContext.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.ContextInfo.DataSharingContext";
            };

            /**
             * DataSharingFlags enum.
             * @name proto.ContextInfo.DataSharingContext.DataSharingFlags
             * @enum {number}
             * @property {number} SHOW_MM_DISCLOSURE_ON_CLICK=1 SHOW_MM_DISCLOSURE_ON_CLICK value
             * @property {number} SHOW_MM_DISCLOSURE_ON_READ=2 SHOW_MM_DISCLOSURE_ON_READ value
             */
            DataSharingContext.DataSharingFlags = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[1] = "SHOW_MM_DISCLOSURE_ON_CLICK"] = 1;
                values[valuesById[2] = "SHOW_MM_DISCLOSURE_ON_READ"] = 2;
                return values;
            })();

            DataSharingContext.Parameters = (function() {

                /**
                 * Properties of a Parameters.
                 * @memberof proto.ContextInfo.DataSharingContext
                 * @interface IParameters
                 * @property {string|null} [key] Parameters key
                 * @property {string|null} [stringData] Parameters stringData
                 * @property {number|Long|null} [intData] Parameters intData
                 * @property {number|null} [floatData] Parameters floatData
                 * @property {proto.ContextInfo.DataSharingContext.IParameters|null} [contents] Parameters contents
                 */

                /**
                 * Constructs a new Parameters.
                 * @memberof proto.ContextInfo.DataSharingContext
                 * @classdesc Represents a Parameters.
                 * @implements IParameters
                 * @constructor
                 * @param {proto.ContextInfo.DataSharingContext.IParameters=} [properties] Properties to set
                 */
                function Parameters(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * Parameters key.
                 * @member {string|null|undefined} key
                 * @memberof proto.ContextInfo.DataSharingContext.Parameters
                 * @instance
                 */
                Parameters.prototype.key = null;

                /**
                 * Parameters stringData.
                 * @member {string|null|undefined} stringData
                 * @memberof proto.ContextInfo.DataSharingContext.Parameters
                 * @instance
                 */
                Parameters.prototype.stringData = null;

                /**
                 * Parameters intData.
                 * @member {number|Long|null|undefined} intData
                 * @memberof proto.ContextInfo.DataSharingContext.Parameters
                 * @instance
                 */
                Parameters.prototype.intData = null;

                /**
                 * Parameters floatData.
                 * @member {number|null|undefined} floatData
                 * @memberof proto.ContextInfo.DataSharingContext.Parameters
                 * @instance
                 */
                Parameters.prototype.floatData = null;

                /**
                 * Parameters contents.
                 * @member {proto.ContextInfo.DataSharingContext.IParameters|null|undefined} contents
                 * @memberof proto.ContextInfo.DataSharingContext.Parameters
                 * @instance
                 */
                Parameters.prototype.contents = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Parameters.prototype, "_key", {
                    get: $util.oneOfGetter($oneOfFields = ["key"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Parameters.prototype, "_stringData", {
                    get: $util.oneOfGetter($oneOfFields = ["stringData"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Parameters.prototype, "_intData", {
                    get: $util.oneOfGetter($oneOfFields = ["intData"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Parameters.prototype, "_floatData", {
                    get: $util.oneOfGetter($oneOfFields = ["floatData"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Parameters.prototype, "_contents", {
                    get: $util.oneOfGetter($oneOfFields = ["contents"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new Parameters instance using the specified properties.
                 * @function create
                 * @memberof proto.ContextInfo.DataSharingContext.Parameters
                 * @static
                 * @param {proto.ContextInfo.DataSharingContext.IParameters=} [properties] Properties to set
                 * @returns {proto.ContextInfo.DataSharingContext.Parameters} Parameters instance
                 */
                Parameters.create = function create(properties) {
                    return new Parameters(properties);
                };

                /**
                 * Encodes the specified Parameters message. Does not implicitly {@link proto.ContextInfo.DataSharingContext.Parameters.verify|verify} messages.
                 * @function encode
                 * @memberof proto.ContextInfo.DataSharingContext.Parameters
                 * @static
                 * @param {proto.ContextInfo.DataSharingContext.IParameters} message Parameters message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Parameters.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
                    if (message.stringData != null && Object.hasOwnProperty.call(message, "stringData"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.stringData);
                    if (message.intData != null && Object.hasOwnProperty.call(message, "intData"))
                        writer.uint32(/* id 3, wireType 0 =*/24).int64(message.intData);
                    if (message.floatData != null && Object.hasOwnProperty.call(message, "floatData"))
                        writer.uint32(/* id 4, wireType 5 =*/37).float(message.floatData);
                    if (message.contents != null && Object.hasOwnProperty.call(message, "contents"))
                        $root.proto.ContextInfo.DataSharingContext.Parameters.encode(message.contents, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified Parameters message, length delimited. Does not implicitly {@link proto.ContextInfo.DataSharingContext.Parameters.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.ContextInfo.DataSharingContext.Parameters
                 * @static
                 * @param {proto.ContextInfo.DataSharingContext.IParameters} message Parameters message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Parameters.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a Parameters message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.ContextInfo.DataSharingContext.Parameters
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.ContextInfo.DataSharingContext.Parameters} Parameters
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Parameters.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.key = reader.stringVerify();
                                message._key = "key";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.stringData = reader.stringVerify();
                                message._stringData = "stringData";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                message.intData = reader.int64();
                                message._intData = "intData";
                                continue;
                            }
                        case 4: {
                                if (u !== 5)
                                    break;
                                message.floatData = reader.float();
                                message._floatData = "floatData";
                                continue;
                            }
                        case 5: {
                                if (u !== 2)
                                    break;
                                message.contents = $root.proto.ContextInfo.DataSharingContext.Parameters.decode(reader, reader.uint32(), undefined, q + 1, message.contents);
                                message._contents = "contents";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a Parameters message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.ContextInfo.DataSharingContext.Parameters
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.ContextInfo.DataSharingContext.Parameters} Parameters
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Parameters.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a Parameters message.
                 * @function verify
                 * @memberof proto.ContextInfo.DataSharingContext.Parameters
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                Parameters.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                        properties._key = 1;
                        if (!$util.isString(message.key))
                            return "key: string expected";
                    }
                    if (message.stringData != null && Object.hasOwnProperty.call(message, "stringData")) {
                        properties._stringData = 1;
                        if (!$util.isString(message.stringData))
                            return "stringData: string expected";
                    }
                    if (message.intData != null && Object.hasOwnProperty.call(message, "intData")) {
                        properties._intData = 1;
                        if (!$util.isInteger(message.intData) && !(message.intData && $util.isInteger(message.intData.low) && $util.isInteger(message.intData.high)))
                            return "intData: integer|Long expected";
                    }
                    if (message.floatData != null && Object.hasOwnProperty.call(message, "floatData")) {
                        properties._floatData = 1;
                        if (typeof message.floatData !== "number")
                            return "floatData: number expected";
                    }
                    if (message.contents != null && Object.hasOwnProperty.call(message, "contents")) {
                        properties._contents = 1;
                        {
                            var error = $root.proto.ContextInfo.DataSharingContext.Parameters.verify(message.contents, q + 1);
                            if (error)
                                return "contents." + error;
                        }
                    }
                    return null;
                };

                /**
                 * Creates a Parameters message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.ContextInfo.DataSharingContext.Parameters
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.ContextInfo.DataSharingContext.Parameters} Parameters
                 */
                Parameters.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.ContextInfo.DataSharingContext.Parameters: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.key != null)
                        message.key = String(object.key);
                    if (object.stringData != null)
                        message.stringData = String(object.stringData);
                    if (object.intData != null)
                        if ($util.Long)
                            message.intData = $util.Long.fromValue(object.intData, false);
                        else if (typeof object.intData === "string")
                            message.intData = parseInt(object.intData, 10);
                        else if (typeof object.intData === "number")
                            message.intData = object.intData;
                        else if (typeof object.intData === "object")
                            message.intData = new $util.LongBits(object.intData.low >>> 0, object.intData.high >>> 0).toNumber();
                    if (object.floatData != null)
                        message.floatData = Number(object.floatData);
                    if (object.contents != null) {
                        if (!$util.isObject(object.contents))
                            throw TypeError(".proto.ContextInfo.DataSharingContext.Parameters.contents: object expected");
                        message.contents = $root.proto.ContextInfo.DataSharingContext.Parameters.fromObject(object.contents, q + 1);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a Parameters message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.ContextInfo.DataSharingContext.Parameters
                 * @static
                 * @param {proto.ContextInfo.DataSharingContext.Parameters} message Parameters
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                Parameters.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                        object.key = message.key;
                    if (message.stringData != null && Object.hasOwnProperty.call(message, "stringData"))
                        object.stringData = message.stringData;
                    if (message.intData != null && Object.hasOwnProperty.call(message, "intData"))
                        if (typeof BigInt !== "undefined" && options.longs === BigInt)
                            object.intData = typeof message.intData === "number" ? BigInt(message.intData) : $util.Long.fromBits(message.intData.low >>> 0, message.intData.high >>> 0, false).toBigInt();
                        else if (typeof message.intData === "number")
                            object.intData = options.longs === String ? String(message.intData) : message.intData;
                        else
                            object.intData = options.longs === String ? $util.Long.prototype.toString.call(message.intData) : options.longs === Number ? new $util.LongBits(message.intData.low >>> 0, message.intData.high >>> 0).toNumber() : message.intData;
                    if (message.floatData != null && Object.hasOwnProperty.call(message, "floatData"))
                        object.floatData = options.json && !isFinite(message.floatData) ? String(message.floatData) : message.floatData;
                    if (message.contents != null && Object.hasOwnProperty.call(message, "contents"))
                        object.contents = $root.proto.ContextInfo.DataSharingContext.Parameters.toObject(message.contents, options, q + 1);
                    return object;
                };

                /**
                 * Converts this Parameters to JSON.
                 * @function toJSON
                 * @memberof proto.ContextInfo.DataSharingContext.Parameters
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                Parameters.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for Parameters
                 * @function getTypeUrl
                 * @memberof proto.ContextInfo.DataSharingContext.Parameters
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                Parameters.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.ContextInfo.DataSharingContext.Parameters";
                };

                return Parameters;
            })();

            return DataSharingContext;
        })();

        ContextInfo.ExternalAdReplyInfo = (function() {

            /**
             * Properties of an ExternalAdReplyInfo.
             * @memberof proto.ContextInfo
             * @interface IExternalAdReplyInfo
             * @property {string|null} [title] ExternalAdReplyInfo title
             * @property {string|null} [body] ExternalAdReplyInfo body
             * @property {proto.ContextInfo.ExternalAdReplyInfo.MediaType|null} [mediaType] ExternalAdReplyInfo mediaType
             * @property {string|null} [thumbnailUrl] ExternalAdReplyInfo thumbnailUrl
             * @property {string|null} [mediaUrl] ExternalAdReplyInfo mediaUrl
             * @property {Uint8Array|null} [thumbnail] ExternalAdReplyInfo thumbnail
             * @property {string|null} [sourceType] ExternalAdReplyInfo sourceType
             * @property {string|null} [sourceId] ExternalAdReplyInfo sourceId
             * @property {string|null} [sourceUrl] ExternalAdReplyInfo sourceUrl
             * @property {boolean|null} [containsAutoReply] ExternalAdReplyInfo containsAutoReply
             * @property {boolean|null} [renderLargerThumbnail] ExternalAdReplyInfo renderLargerThumbnail
             * @property {boolean|null} [showAdAttribution] ExternalAdReplyInfo showAdAttribution
             * @property {string|null} [ctwaClid] ExternalAdReplyInfo ctwaClid
             * @property {string|null} [ref] ExternalAdReplyInfo ref
             * @property {boolean|null} [clickToWhatsappCall] ExternalAdReplyInfo clickToWhatsappCall
             * @property {boolean|null} [adContextPreviewDismissed] ExternalAdReplyInfo adContextPreviewDismissed
             * @property {string|null} [sourceApp] ExternalAdReplyInfo sourceApp
             * @property {boolean|null} [automatedGreetingMessageShown] ExternalAdReplyInfo automatedGreetingMessageShown
             * @property {string|null} [greetingMessageBody] ExternalAdReplyInfo greetingMessageBody
             * @property {string|null} [ctaPayload] ExternalAdReplyInfo ctaPayload
             * @property {boolean|null} [disableNudge] ExternalAdReplyInfo disableNudge
             * @property {string|null} [originalImageUrl] ExternalAdReplyInfo originalImageUrl
             * @property {string|null} [automatedGreetingMessageCtaType] ExternalAdReplyInfo automatedGreetingMessageCtaType
             * @property {boolean|null} [wtwaAdFormat] ExternalAdReplyInfo wtwaAdFormat
             * @property {proto.ContextInfo.ExternalAdReplyInfo.AdType|null} [adType] ExternalAdReplyInfo adType
             * @property {string|null} [wtwaWebsiteUrl] ExternalAdReplyInfo wtwaWebsiteUrl
             * @property {string|null} [adPreviewUrl] ExternalAdReplyInfo adPreviewUrl
             * @property {boolean|null} [containsCtwaFlowsAutoReply] ExternalAdReplyInfo containsCtwaFlowsAutoReply
             * @property {number|null} [agmThumbnailStrategy] ExternalAdReplyInfo agmThumbnailStrategy
             * @property {number|null} [agmTitleStrategy] ExternalAdReplyInfo agmTitleStrategy
             * @property {number|null} [agmSubtitleStrategy] ExternalAdReplyInfo agmSubtitleStrategy
             * @property {number|null} [agmHeaderInteractionStrategy] ExternalAdReplyInfo agmHeaderInteractionStrategy
             */

            /**
             * Constructs a new ExternalAdReplyInfo.
             * @memberof proto.ContextInfo
             * @classdesc Represents an ExternalAdReplyInfo.
             * @implements IExternalAdReplyInfo
             * @constructor
             * @param {proto.ContextInfo.IExternalAdReplyInfo=} [properties] Properties to set
             */
            function ExternalAdReplyInfo(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ExternalAdReplyInfo title.
             * @member {string|null|undefined} title
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.title = null;

            /**
             * ExternalAdReplyInfo body.
             * @member {string|null|undefined} body
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.body = null;

            /**
             * ExternalAdReplyInfo mediaType.
             * @member {proto.ContextInfo.ExternalAdReplyInfo.MediaType|null|undefined} mediaType
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.mediaType = null;

            /**
             * ExternalAdReplyInfo thumbnailUrl.
             * @member {string|null|undefined} thumbnailUrl
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.thumbnailUrl = null;

            /**
             * ExternalAdReplyInfo mediaUrl.
             * @member {string|null|undefined} mediaUrl
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.mediaUrl = null;

            /**
             * ExternalAdReplyInfo thumbnail.
             * @member {Uint8Array|null|undefined} thumbnail
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.thumbnail = null;

            /**
             * ExternalAdReplyInfo sourceType.
             * @member {string|null|undefined} sourceType
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.sourceType = null;

            /**
             * ExternalAdReplyInfo sourceId.
             * @member {string|null|undefined} sourceId
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.sourceId = null;

            /**
             * ExternalAdReplyInfo sourceUrl.
             * @member {string|null|undefined} sourceUrl
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.sourceUrl = null;

            /**
             * ExternalAdReplyInfo containsAutoReply.
             * @member {boolean|null|undefined} containsAutoReply
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.containsAutoReply = null;

            /**
             * ExternalAdReplyInfo renderLargerThumbnail.
             * @member {boolean|null|undefined} renderLargerThumbnail
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.renderLargerThumbnail = null;

            /**
             * ExternalAdReplyInfo showAdAttribution.
             * @member {boolean|null|undefined} showAdAttribution
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.showAdAttribution = null;

            /**
             * ExternalAdReplyInfo ctwaClid.
             * @member {string|null|undefined} ctwaClid
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.ctwaClid = null;

            /**
             * ExternalAdReplyInfo ref.
             * @member {string|null|undefined} ref
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.ref = null;

            /**
             * ExternalAdReplyInfo clickToWhatsappCall.
             * @member {boolean|null|undefined} clickToWhatsappCall
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.clickToWhatsappCall = null;

            /**
             * ExternalAdReplyInfo adContextPreviewDismissed.
             * @member {boolean|null|undefined} adContextPreviewDismissed
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.adContextPreviewDismissed = null;

            /**
             * ExternalAdReplyInfo sourceApp.
             * @member {string|null|undefined} sourceApp
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.sourceApp = null;

            /**
             * ExternalAdReplyInfo automatedGreetingMessageShown.
             * @member {boolean|null|undefined} automatedGreetingMessageShown
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.automatedGreetingMessageShown = null;

            /**
             * ExternalAdReplyInfo greetingMessageBody.
             * @member {string|null|undefined} greetingMessageBody
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.greetingMessageBody = null;

            /**
             * ExternalAdReplyInfo ctaPayload.
             * @member {string|null|undefined} ctaPayload
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.ctaPayload = null;

            /**
             * ExternalAdReplyInfo disableNudge.
             * @member {boolean|null|undefined} disableNudge
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.disableNudge = null;

            /**
             * ExternalAdReplyInfo originalImageUrl.
             * @member {string|null|undefined} originalImageUrl
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.originalImageUrl = null;

            /**
             * ExternalAdReplyInfo automatedGreetingMessageCtaType.
             * @member {string|null|undefined} automatedGreetingMessageCtaType
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.automatedGreetingMessageCtaType = null;

            /**
             * ExternalAdReplyInfo wtwaAdFormat.
             * @member {boolean|null|undefined} wtwaAdFormat
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.wtwaAdFormat = null;

            /**
             * ExternalAdReplyInfo adType.
             * @member {proto.ContextInfo.ExternalAdReplyInfo.AdType|null|undefined} adType
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.adType = null;

            /**
             * ExternalAdReplyInfo wtwaWebsiteUrl.
             * @member {string|null|undefined} wtwaWebsiteUrl
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.wtwaWebsiteUrl = null;

            /**
             * ExternalAdReplyInfo adPreviewUrl.
             * @member {string|null|undefined} adPreviewUrl
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.adPreviewUrl = null;

            /**
             * ExternalAdReplyInfo containsCtwaFlowsAutoReply.
             * @member {boolean|null|undefined} containsCtwaFlowsAutoReply
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.containsCtwaFlowsAutoReply = null;

            /**
             * ExternalAdReplyInfo agmThumbnailStrategy.
             * @member {number|null|undefined} agmThumbnailStrategy
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.agmThumbnailStrategy = null;

            /**
             * ExternalAdReplyInfo agmTitleStrategy.
             * @member {number|null|undefined} agmTitleStrategy
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.agmTitleStrategy = null;

            /**
             * ExternalAdReplyInfo agmSubtitleStrategy.
             * @member {number|null|undefined} agmSubtitleStrategy
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.agmSubtitleStrategy = null;

            /**
             * ExternalAdReplyInfo agmHeaderInteractionStrategy.
             * @member {number|null|undefined} agmHeaderInteractionStrategy
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             */
            ExternalAdReplyInfo.prototype.agmHeaderInteractionStrategy = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_body", {
                get: $util.oneOfGetter($oneOfFields = ["body"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_mediaType", {
                get: $util.oneOfGetter($oneOfFields = ["mediaType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_thumbnailUrl", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_mediaUrl", {
                get: $util.oneOfGetter($oneOfFields = ["mediaUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_thumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_sourceType", {
                get: $util.oneOfGetter($oneOfFields = ["sourceType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_sourceId", {
                get: $util.oneOfGetter($oneOfFields = ["sourceId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_sourceUrl", {
                get: $util.oneOfGetter($oneOfFields = ["sourceUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_containsAutoReply", {
                get: $util.oneOfGetter($oneOfFields = ["containsAutoReply"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_renderLargerThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["renderLargerThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_showAdAttribution", {
                get: $util.oneOfGetter($oneOfFields = ["showAdAttribution"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_ctwaClid", {
                get: $util.oneOfGetter($oneOfFields = ["ctwaClid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_ref", {
                get: $util.oneOfGetter($oneOfFields = ["ref"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_clickToWhatsappCall", {
                get: $util.oneOfGetter($oneOfFields = ["clickToWhatsappCall"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_adContextPreviewDismissed", {
                get: $util.oneOfGetter($oneOfFields = ["adContextPreviewDismissed"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_sourceApp", {
                get: $util.oneOfGetter($oneOfFields = ["sourceApp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_automatedGreetingMessageShown", {
                get: $util.oneOfGetter($oneOfFields = ["automatedGreetingMessageShown"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_greetingMessageBody", {
                get: $util.oneOfGetter($oneOfFields = ["greetingMessageBody"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_ctaPayload", {
                get: $util.oneOfGetter($oneOfFields = ["ctaPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_disableNudge", {
                get: $util.oneOfGetter($oneOfFields = ["disableNudge"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_originalImageUrl", {
                get: $util.oneOfGetter($oneOfFields = ["originalImageUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_automatedGreetingMessageCtaType", {
                get: $util.oneOfGetter($oneOfFields = ["automatedGreetingMessageCtaType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_wtwaAdFormat", {
                get: $util.oneOfGetter($oneOfFields = ["wtwaAdFormat"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_adType", {
                get: $util.oneOfGetter($oneOfFields = ["adType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_wtwaWebsiteUrl", {
                get: $util.oneOfGetter($oneOfFields = ["wtwaWebsiteUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_adPreviewUrl", {
                get: $util.oneOfGetter($oneOfFields = ["adPreviewUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_containsCtwaFlowsAutoReply", {
                get: $util.oneOfGetter($oneOfFields = ["containsCtwaFlowsAutoReply"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_agmThumbnailStrategy", {
                get: $util.oneOfGetter($oneOfFields = ["agmThumbnailStrategy"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_agmTitleStrategy", {
                get: $util.oneOfGetter($oneOfFields = ["agmTitleStrategy"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_agmSubtitleStrategy", {
                get: $util.oneOfGetter($oneOfFields = ["agmSubtitleStrategy"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_agmHeaderInteractionStrategy", {
                get: $util.oneOfGetter($oneOfFields = ["agmHeaderInteractionStrategy"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ExternalAdReplyInfo instance using the specified properties.
             * @function create
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @static
             * @param {proto.ContextInfo.IExternalAdReplyInfo=} [properties] Properties to set
             * @returns {proto.ContextInfo.ExternalAdReplyInfo} ExternalAdReplyInfo instance
             */
            ExternalAdReplyInfo.create = function create(properties) {
                return new ExternalAdReplyInfo(properties);
            };

            /**
             * Encodes the specified ExternalAdReplyInfo message. Does not implicitly {@link proto.ContextInfo.ExternalAdReplyInfo.verify|verify} messages.
             * @function encode
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @static
             * @param {proto.ContextInfo.IExternalAdReplyInfo} message ExternalAdReplyInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ExternalAdReplyInfo.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.title);
                if (message.body != null && Object.hasOwnProperty.call(message, "body"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.body);
                if (message.mediaType != null && Object.hasOwnProperty.call(message, "mediaType"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.mediaType);
                if (message.thumbnailUrl != null && Object.hasOwnProperty.call(message, "thumbnailUrl"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.thumbnailUrl);
                if (message.mediaUrl != null && Object.hasOwnProperty.call(message, "mediaUrl"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.mediaUrl);
                if (message.thumbnail != null && Object.hasOwnProperty.call(message, "thumbnail"))
                    writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.thumbnail);
                if (message.sourceType != null && Object.hasOwnProperty.call(message, "sourceType"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.sourceType);
                if (message.sourceId != null && Object.hasOwnProperty.call(message, "sourceId"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.sourceId);
                if (message.sourceUrl != null && Object.hasOwnProperty.call(message, "sourceUrl"))
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.sourceUrl);
                if (message.containsAutoReply != null && Object.hasOwnProperty.call(message, "containsAutoReply"))
                    writer.uint32(/* id 10, wireType 0 =*/80).bool(message.containsAutoReply);
                if (message.renderLargerThumbnail != null && Object.hasOwnProperty.call(message, "renderLargerThumbnail"))
                    writer.uint32(/* id 11, wireType 0 =*/88).bool(message.renderLargerThumbnail);
                if (message.showAdAttribution != null && Object.hasOwnProperty.call(message, "showAdAttribution"))
                    writer.uint32(/* id 12, wireType 0 =*/96).bool(message.showAdAttribution);
                if (message.ctwaClid != null && Object.hasOwnProperty.call(message, "ctwaClid"))
                    writer.uint32(/* id 13, wireType 2 =*/106).string(message.ctwaClid);
                if (message.ref != null && Object.hasOwnProperty.call(message, "ref"))
                    writer.uint32(/* id 14, wireType 2 =*/114).string(message.ref);
                if (message.clickToWhatsappCall != null && Object.hasOwnProperty.call(message, "clickToWhatsappCall"))
                    writer.uint32(/* id 15, wireType 0 =*/120).bool(message.clickToWhatsappCall);
                if (message.adContextPreviewDismissed != null && Object.hasOwnProperty.call(message, "adContextPreviewDismissed"))
                    writer.uint32(/* id 16, wireType 0 =*/128).bool(message.adContextPreviewDismissed);
                if (message.sourceApp != null && Object.hasOwnProperty.call(message, "sourceApp"))
                    writer.uint32(/* id 17, wireType 2 =*/138).string(message.sourceApp);
                if (message.automatedGreetingMessageShown != null && Object.hasOwnProperty.call(message, "automatedGreetingMessageShown"))
                    writer.uint32(/* id 18, wireType 0 =*/144).bool(message.automatedGreetingMessageShown);
                if (message.greetingMessageBody != null && Object.hasOwnProperty.call(message, "greetingMessageBody"))
                    writer.uint32(/* id 19, wireType 2 =*/154).string(message.greetingMessageBody);
                if (message.ctaPayload != null && Object.hasOwnProperty.call(message, "ctaPayload"))
                    writer.uint32(/* id 20, wireType 2 =*/162).string(message.ctaPayload);
                if (message.disableNudge != null && Object.hasOwnProperty.call(message, "disableNudge"))
                    writer.uint32(/* id 21, wireType 0 =*/168).bool(message.disableNudge);
                if (message.originalImageUrl != null && Object.hasOwnProperty.call(message, "originalImageUrl"))
                    writer.uint32(/* id 22, wireType 2 =*/178).string(message.originalImageUrl);
                if (message.automatedGreetingMessageCtaType != null && Object.hasOwnProperty.call(message, "automatedGreetingMessageCtaType"))
                    writer.uint32(/* id 23, wireType 2 =*/186).string(message.automatedGreetingMessageCtaType);
                if (message.wtwaAdFormat != null && Object.hasOwnProperty.call(message, "wtwaAdFormat"))
                    writer.uint32(/* id 24, wireType 0 =*/192).bool(message.wtwaAdFormat);
                if (message.adType != null && Object.hasOwnProperty.call(message, "adType"))
                    writer.uint32(/* id 25, wireType 0 =*/200).int32(message.adType);
                if (message.wtwaWebsiteUrl != null && Object.hasOwnProperty.call(message, "wtwaWebsiteUrl"))
                    writer.uint32(/* id 26, wireType 2 =*/210).string(message.wtwaWebsiteUrl);
                if (message.adPreviewUrl != null && Object.hasOwnProperty.call(message, "adPreviewUrl"))
                    writer.uint32(/* id 27, wireType 2 =*/218).string(message.adPreviewUrl);
                if (message.containsCtwaFlowsAutoReply != null && Object.hasOwnProperty.call(message, "containsCtwaFlowsAutoReply"))
                    writer.uint32(/* id 28, wireType 0 =*/224).bool(message.containsCtwaFlowsAutoReply);
                if (message.agmThumbnailStrategy != null && Object.hasOwnProperty.call(message, "agmThumbnailStrategy"))
                    writer.uint32(/* id 29, wireType 0 =*/232).int32(message.agmThumbnailStrategy);
                if (message.agmTitleStrategy != null && Object.hasOwnProperty.call(message, "agmTitleStrategy"))
                    writer.uint32(/* id 30, wireType 0 =*/240).int32(message.agmTitleStrategy);
                if (message.agmSubtitleStrategy != null && Object.hasOwnProperty.call(message, "agmSubtitleStrategy"))
                    writer.uint32(/* id 31, wireType 0 =*/248).int32(message.agmSubtitleStrategy);
                if (message.agmHeaderInteractionStrategy != null && Object.hasOwnProperty.call(message, "agmHeaderInteractionStrategy"))
                    writer.uint32(/* id 32, wireType 0 =*/256).int32(message.agmHeaderInteractionStrategy);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ExternalAdReplyInfo message, length delimited. Does not implicitly {@link proto.ContextInfo.ExternalAdReplyInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @static
             * @param {proto.ContextInfo.IExternalAdReplyInfo} message ExternalAdReplyInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ExternalAdReplyInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an ExternalAdReplyInfo message from the specified reader or buffer.
             * @function decode
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.ContextInfo.ExternalAdReplyInfo} ExternalAdReplyInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ExternalAdReplyInfo.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.title = reader.stringVerify();
                            message._title = "title";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.body = reader.stringVerify();
                            message._body = "body";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.mediaType = reader.int32();
                            message._mediaType = "mediaType";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.thumbnailUrl = reader.stringVerify();
                            message._thumbnailUrl = "thumbnailUrl";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.mediaUrl = reader.stringVerify();
                            message._mediaUrl = "mediaUrl";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.thumbnail = reader.bytes();
                            message._thumbnail = "thumbnail";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.sourceType = reader.stringVerify();
                            message._sourceType = "sourceType";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.sourceId = reader.stringVerify();
                            message._sourceId = "sourceId";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            message.sourceUrl = reader.stringVerify();
                            message._sourceUrl = "sourceUrl";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            message.containsAutoReply = reader.bool();
                            message._containsAutoReply = "containsAutoReply";
                            continue;
                        }
                    case 11: {
                            if (u !== 0)
                                break;
                            message.renderLargerThumbnail = reader.bool();
                            message._renderLargerThumbnail = "renderLargerThumbnail";
                            continue;
                        }
                    case 12: {
                            if (u !== 0)
                                break;
                            message.showAdAttribution = reader.bool();
                            message._showAdAttribution = "showAdAttribution";
                            continue;
                        }
                    case 13: {
                            if (u !== 2)
                                break;
                            message.ctwaClid = reader.stringVerify();
                            message._ctwaClid = "ctwaClid";
                            continue;
                        }
                    case 14: {
                            if (u !== 2)
                                break;
                            message.ref = reader.stringVerify();
                            message._ref = "ref";
                            continue;
                        }
                    case 15: {
                            if (u !== 0)
                                break;
                            message.clickToWhatsappCall = reader.bool();
                            message._clickToWhatsappCall = "clickToWhatsappCall";
                            continue;
                        }
                    case 16: {
                            if (u !== 0)
                                break;
                            message.adContextPreviewDismissed = reader.bool();
                            message._adContextPreviewDismissed = "adContextPreviewDismissed";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            message.sourceApp = reader.stringVerify();
                            message._sourceApp = "sourceApp";
                            continue;
                        }
                    case 18: {
                            if (u !== 0)
                                break;
                            message.automatedGreetingMessageShown = reader.bool();
                            message._automatedGreetingMessageShown = "automatedGreetingMessageShown";
                            continue;
                        }
                    case 19: {
                            if (u !== 2)
                                break;
                            message.greetingMessageBody = reader.stringVerify();
                            message._greetingMessageBody = "greetingMessageBody";
                            continue;
                        }
                    case 20: {
                            if (u !== 2)
                                break;
                            message.ctaPayload = reader.stringVerify();
                            message._ctaPayload = "ctaPayload";
                            continue;
                        }
                    case 21: {
                            if (u !== 0)
                                break;
                            message.disableNudge = reader.bool();
                            message._disableNudge = "disableNudge";
                            continue;
                        }
                    case 22: {
                            if (u !== 2)
                                break;
                            message.originalImageUrl = reader.stringVerify();
                            message._originalImageUrl = "originalImageUrl";
                            continue;
                        }
                    case 23: {
                            if (u !== 2)
                                break;
                            message.automatedGreetingMessageCtaType = reader.stringVerify();
                            message._automatedGreetingMessageCtaType = "automatedGreetingMessageCtaType";
                            continue;
                        }
                    case 24: {
                            if (u !== 0)
                                break;
                            message.wtwaAdFormat = reader.bool();
                            message._wtwaAdFormat = "wtwaAdFormat";
                            continue;
                        }
                    case 25: {
                            if (u !== 0)
                                break;
                            message.adType = reader.int32();
                            message._adType = "adType";
                            continue;
                        }
                    case 26: {
                            if (u !== 2)
                                break;
                            message.wtwaWebsiteUrl = reader.stringVerify();
                            message._wtwaWebsiteUrl = "wtwaWebsiteUrl";
                            continue;
                        }
                    case 27: {
                            if (u !== 2)
                                break;
                            message.adPreviewUrl = reader.stringVerify();
                            message._adPreviewUrl = "adPreviewUrl";
                            continue;
                        }
                    case 28: {
                            if (u !== 0)
                                break;
                            message.containsCtwaFlowsAutoReply = reader.bool();
                            message._containsCtwaFlowsAutoReply = "containsCtwaFlowsAutoReply";
                            continue;
                        }
                    case 29: {
                            if (u !== 0)
                                break;
                            message.agmThumbnailStrategy = reader.int32();
                            message._agmThumbnailStrategy = "agmThumbnailStrategy";
                            continue;
                        }
                    case 30: {
                            if (u !== 0)
                                break;
                            message.agmTitleStrategy = reader.int32();
                            message._agmTitleStrategy = "agmTitleStrategy";
                            continue;
                        }
                    case 31: {
                            if (u !== 0)
                                break;
                            message.agmSubtitleStrategy = reader.int32();
                            message._agmSubtitleStrategy = "agmSubtitleStrategy";
                            continue;
                        }
                    case 32: {
                            if (u !== 0)
                                break;
                            message.agmHeaderInteractionStrategy = reader.int32();
                            message._agmHeaderInteractionStrategy = "agmHeaderInteractionStrategy";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an ExternalAdReplyInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.ContextInfo.ExternalAdReplyInfo} ExternalAdReplyInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ExternalAdReplyInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an ExternalAdReplyInfo message.
             * @function verify
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ExternalAdReplyInfo.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                    properties._title = 1;
                    if (!$util.isString(message.title))
                        return "title: string expected";
                }
                if (message.body != null && Object.hasOwnProperty.call(message, "body")) {
                    properties._body = 1;
                    if (!$util.isString(message.body))
                        return "body: string expected";
                }
                if (message.mediaType != null && Object.hasOwnProperty.call(message, "mediaType")) {
                    properties._mediaType = 1;
                    if (typeof message.mediaType !== "number" || (message.mediaType | 0) !== message.mediaType)
                        return "mediaType: enum value expected";
                }
                if (message.thumbnailUrl != null && Object.hasOwnProperty.call(message, "thumbnailUrl")) {
                    properties._thumbnailUrl = 1;
                    if (!$util.isString(message.thumbnailUrl))
                        return "thumbnailUrl: string expected";
                }
                if (message.mediaUrl != null && Object.hasOwnProperty.call(message, "mediaUrl")) {
                    properties._mediaUrl = 1;
                    if (!$util.isString(message.mediaUrl))
                        return "mediaUrl: string expected";
                }
                if (message.thumbnail != null && Object.hasOwnProperty.call(message, "thumbnail")) {
                    properties._thumbnail = 1;
                    if (!(message.thumbnail && typeof message.thumbnail.length === "number" || $util.isString(message.thumbnail)))
                        return "thumbnail: buffer expected";
                }
                if (message.sourceType != null && Object.hasOwnProperty.call(message, "sourceType")) {
                    properties._sourceType = 1;
                    if (!$util.isString(message.sourceType))
                        return "sourceType: string expected";
                }
                if (message.sourceId != null && Object.hasOwnProperty.call(message, "sourceId")) {
                    properties._sourceId = 1;
                    if (!$util.isString(message.sourceId))
                        return "sourceId: string expected";
                }
                if (message.sourceUrl != null && Object.hasOwnProperty.call(message, "sourceUrl")) {
                    properties._sourceUrl = 1;
                    if (!$util.isString(message.sourceUrl))
                        return "sourceUrl: string expected";
                }
                if (message.containsAutoReply != null && Object.hasOwnProperty.call(message, "containsAutoReply")) {
                    properties._containsAutoReply = 1;
                    if (typeof message.containsAutoReply !== "boolean")
                        return "containsAutoReply: boolean expected";
                }
                if (message.renderLargerThumbnail != null && Object.hasOwnProperty.call(message, "renderLargerThumbnail")) {
                    properties._renderLargerThumbnail = 1;
                    if (typeof message.renderLargerThumbnail !== "boolean")
                        return "renderLargerThumbnail: boolean expected";
                }
                if (message.showAdAttribution != null && Object.hasOwnProperty.call(message, "showAdAttribution")) {
                    properties._showAdAttribution = 1;
                    if (typeof message.showAdAttribution !== "boolean")
                        return "showAdAttribution: boolean expected";
                }
                if (message.ctwaClid != null && Object.hasOwnProperty.call(message, "ctwaClid")) {
                    properties._ctwaClid = 1;
                    if (!$util.isString(message.ctwaClid))
                        return "ctwaClid: string expected";
                }
                if (message.ref != null && Object.hasOwnProperty.call(message, "ref")) {
                    properties._ref = 1;
                    if (!$util.isString(message.ref))
                        return "ref: string expected";
                }
                if (message.clickToWhatsappCall != null && Object.hasOwnProperty.call(message, "clickToWhatsappCall")) {
                    properties._clickToWhatsappCall = 1;
                    if (typeof message.clickToWhatsappCall !== "boolean")
                        return "clickToWhatsappCall: boolean expected";
                }
                if (message.adContextPreviewDismissed != null && Object.hasOwnProperty.call(message, "adContextPreviewDismissed")) {
                    properties._adContextPreviewDismissed = 1;
                    if (typeof message.adContextPreviewDismissed !== "boolean")
                        return "adContextPreviewDismissed: boolean expected";
                }
                if (message.sourceApp != null && Object.hasOwnProperty.call(message, "sourceApp")) {
                    properties._sourceApp = 1;
                    if (!$util.isString(message.sourceApp))
                        return "sourceApp: string expected";
                }
                if (message.automatedGreetingMessageShown != null && Object.hasOwnProperty.call(message, "automatedGreetingMessageShown")) {
                    properties._automatedGreetingMessageShown = 1;
                    if (typeof message.automatedGreetingMessageShown !== "boolean")
                        return "automatedGreetingMessageShown: boolean expected";
                }
                if (message.greetingMessageBody != null && Object.hasOwnProperty.call(message, "greetingMessageBody")) {
                    properties._greetingMessageBody = 1;
                    if (!$util.isString(message.greetingMessageBody))
                        return "greetingMessageBody: string expected";
                }
                if (message.ctaPayload != null && Object.hasOwnProperty.call(message, "ctaPayload")) {
                    properties._ctaPayload = 1;
                    if (!$util.isString(message.ctaPayload))
                        return "ctaPayload: string expected";
                }
                if (message.disableNudge != null && Object.hasOwnProperty.call(message, "disableNudge")) {
                    properties._disableNudge = 1;
                    if (typeof message.disableNudge !== "boolean")
                        return "disableNudge: boolean expected";
                }
                if (message.originalImageUrl != null && Object.hasOwnProperty.call(message, "originalImageUrl")) {
                    properties._originalImageUrl = 1;
                    if (!$util.isString(message.originalImageUrl))
                        return "originalImageUrl: string expected";
                }
                if (message.automatedGreetingMessageCtaType != null && Object.hasOwnProperty.call(message, "automatedGreetingMessageCtaType")) {
                    properties._automatedGreetingMessageCtaType = 1;
                    if (!$util.isString(message.automatedGreetingMessageCtaType))
                        return "automatedGreetingMessageCtaType: string expected";
                }
                if (message.wtwaAdFormat != null && Object.hasOwnProperty.call(message, "wtwaAdFormat")) {
                    properties._wtwaAdFormat = 1;
                    if (typeof message.wtwaAdFormat !== "boolean")
                        return "wtwaAdFormat: boolean expected";
                }
                if (message.adType != null && Object.hasOwnProperty.call(message, "adType")) {
                    properties._adType = 1;
                    if (typeof message.adType !== "number" || (message.adType | 0) !== message.adType)
                        return "adType: enum value expected";
                }
                if (message.wtwaWebsiteUrl != null && Object.hasOwnProperty.call(message, "wtwaWebsiteUrl")) {
                    properties._wtwaWebsiteUrl = 1;
                    if (!$util.isString(message.wtwaWebsiteUrl))
                        return "wtwaWebsiteUrl: string expected";
                }
                if (message.adPreviewUrl != null && Object.hasOwnProperty.call(message, "adPreviewUrl")) {
                    properties._adPreviewUrl = 1;
                    if (!$util.isString(message.adPreviewUrl))
                        return "adPreviewUrl: string expected";
                }
                if (message.containsCtwaFlowsAutoReply != null && Object.hasOwnProperty.call(message, "containsCtwaFlowsAutoReply")) {
                    properties._containsCtwaFlowsAutoReply = 1;
                    if (typeof message.containsCtwaFlowsAutoReply !== "boolean")
                        return "containsCtwaFlowsAutoReply: boolean expected";
                }
                if (message.agmThumbnailStrategy != null && Object.hasOwnProperty.call(message, "agmThumbnailStrategy")) {
                    properties._agmThumbnailStrategy = 1;
                    if (!$util.isInteger(message.agmThumbnailStrategy))
                        return "agmThumbnailStrategy: integer expected";
                }
                if (message.agmTitleStrategy != null && Object.hasOwnProperty.call(message, "agmTitleStrategy")) {
                    properties._agmTitleStrategy = 1;
                    if (!$util.isInteger(message.agmTitleStrategy))
                        return "agmTitleStrategy: integer expected";
                }
                if (message.agmSubtitleStrategy != null && Object.hasOwnProperty.call(message, "agmSubtitleStrategy")) {
                    properties._agmSubtitleStrategy = 1;
                    if (!$util.isInteger(message.agmSubtitleStrategy))
                        return "agmSubtitleStrategy: integer expected";
                }
                if (message.agmHeaderInteractionStrategy != null && Object.hasOwnProperty.call(message, "agmHeaderInteractionStrategy")) {
                    properties._agmHeaderInteractionStrategy = 1;
                    if (!$util.isInteger(message.agmHeaderInteractionStrategy))
                        return "agmHeaderInteractionStrategy: integer expected";
                }
                return null;
            };

            /**
             * Creates an ExternalAdReplyInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.ContextInfo.ExternalAdReplyInfo} ExternalAdReplyInfo
             */
            ExternalAdReplyInfo.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.ContextInfo.ExternalAdReplyInfo: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.title != null)
                    message.title = String(object.title);
                if (object.body != null)
                    message.body = String(object.body);
                switch (object.mediaType) {
                case "NONE":
                case 0:
                    message.mediaType = 0;
                    break;
                case "IMAGE":
                case 1:
                    message.mediaType = 1;
                    break;
                case "VIDEO":
                case 2:
                    message.mediaType = 2;
                    break;
                default:
                    if (typeof object.mediaType === "number" && (object.mediaType | 0) === object.mediaType)
                        message.mediaType = object.mediaType;
                }
                if (object.thumbnailUrl != null)
                    message.thumbnailUrl = String(object.thumbnailUrl);
                if (object.mediaUrl != null)
                    message.mediaUrl = String(object.mediaUrl);
                if (object.thumbnail != null)
                    if (typeof object.thumbnail === "string")
                        $util.base64.decode(object.thumbnail, message.thumbnail = $util.newBuffer($util.base64.length(object.thumbnail)), 0);
                    else if (object.thumbnail.length >= 0)
                        message.thumbnail = object.thumbnail;
                if (object.sourceType != null)
                    message.sourceType = String(object.sourceType);
                if (object.sourceId != null)
                    message.sourceId = String(object.sourceId);
                if (object.sourceUrl != null)
                    message.sourceUrl = String(object.sourceUrl);
                if (object.containsAutoReply != null)
                    message.containsAutoReply = Boolean(object.containsAutoReply);
                if (object.renderLargerThumbnail != null)
                    message.renderLargerThumbnail = Boolean(object.renderLargerThumbnail);
                if (object.showAdAttribution != null)
                    message.showAdAttribution = Boolean(object.showAdAttribution);
                if (object.ctwaClid != null)
                    message.ctwaClid = String(object.ctwaClid);
                if (object.ref != null)
                    message.ref = String(object.ref);
                if (object.clickToWhatsappCall != null)
                    message.clickToWhatsappCall = Boolean(object.clickToWhatsappCall);
                if (object.adContextPreviewDismissed != null)
                    message.adContextPreviewDismissed = Boolean(object.adContextPreviewDismissed);
                if (object.sourceApp != null)
                    message.sourceApp = String(object.sourceApp);
                if (object.automatedGreetingMessageShown != null)
                    message.automatedGreetingMessageShown = Boolean(object.automatedGreetingMessageShown);
                if (object.greetingMessageBody != null)
                    message.greetingMessageBody = String(object.greetingMessageBody);
                if (object.ctaPayload != null)
                    message.ctaPayload = String(object.ctaPayload);
                if (object.disableNudge != null)
                    message.disableNudge = Boolean(object.disableNudge);
                if (object.originalImageUrl != null)
                    message.originalImageUrl = String(object.originalImageUrl);
                if (object.automatedGreetingMessageCtaType != null)
                    message.automatedGreetingMessageCtaType = String(object.automatedGreetingMessageCtaType);
                if (object.wtwaAdFormat != null)
                    message.wtwaAdFormat = Boolean(object.wtwaAdFormat);
                switch (object.adType) {
                case "CTWA":
                case 0:
                    message.adType = 0;
                    break;
                case "CAWC":
                case 1:
                    message.adType = 1;
                    break;
                default:
                    if (typeof object.adType === "number" && (object.adType | 0) === object.adType)
                        message.adType = object.adType;
                }
                if (object.wtwaWebsiteUrl != null)
                    message.wtwaWebsiteUrl = String(object.wtwaWebsiteUrl);
                if (object.adPreviewUrl != null)
                    message.adPreviewUrl = String(object.adPreviewUrl);
                if (object.containsCtwaFlowsAutoReply != null)
                    message.containsCtwaFlowsAutoReply = Boolean(object.containsCtwaFlowsAutoReply);
                if (object.agmThumbnailStrategy != null)
                    message.agmThumbnailStrategy = object.agmThumbnailStrategy | 0;
                if (object.agmTitleStrategy != null)
                    message.agmTitleStrategy = object.agmTitleStrategy | 0;
                if (object.agmSubtitleStrategy != null)
                    message.agmSubtitleStrategy = object.agmSubtitleStrategy | 0;
                if (object.agmHeaderInteractionStrategy != null)
                    message.agmHeaderInteractionStrategy = object.agmHeaderInteractionStrategy | 0;
                return message;
            };

            /**
             * Creates a plain object from an ExternalAdReplyInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @static
             * @param {proto.ContextInfo.ExternalAdReplyInfo} message ExternalAdReplyInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ExternalAdReplyInfo.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    object.title = message.title;
                if (message.body != null && Object.hasOwnProperty.call(message, "body"))
                    object.body = message.body;
                if (message.mediaType != null && Object.hasOwnProperty.call(message, "mediaType"))
                    object.mediaType = options.enums === String ? $root.proto.ContextInfo.ExternalAdReplyInfo.MediaType[message.mediaType] === undefined ? message.mediaType : $root.proto.ContextInfo.ExternalAdReplyInfo.MediaType[message.mediaType] : message.mediaType;
                if (message.thumbnailUrl != null && Object.hasOwnProperty.call(message, "thumbnailUrl"))
                    object.thumbnailUrl = message.thumbnailUrl;
                if (message.mediaUrl != null && Object.hasOwnProperty.call(message, "mediaUrl"))
                    object.mediaUrl = message.mediaUrl;
                if (message.thumbnail != null && Object.hasOwnProperty.call(message, "thumbnail"))
                    object.thumbnail = options.bytes === String ? $util.base64.encode(message.thumbnail, 0, message.thumbnail.length) : options.bytes === Array ? Array.prototype.slice.call(message.thumbnail) : message.thumbnail;
                if (message.sourceType != null && Object.hasOwnProperty.call(message, "sourceType"))
                    object.sourceType = message.sourceType;
                if (message.sourceId != null && Object.hasOwnProperty.call(message, "sourceId"))
                    object.sourceId = message.sourceId;
                if (message.sourceUrl != null && Object.hasOwnProperty.call(message, "sourceUrl"))
                    object.sourceUrl = message.sourceUrl;
                if (message.containsAutoReply != null && Object.hasOwnProperty.call(message, "containsAutoReply"))
                    object.containsAutoReply = message.containsAutoReply;
                if (message.renderLargerThumbnail != null && Object.hasOwnProperty.call(message, "renderLargerThumbnail"))
                    object.renderLargerThumbnail = message.renderLargerThumbnail;
                if (message.showAdAttribution != null && Object.hasOwnProperty.call(message, "showAdAttribution"))
                    object.showAdAttribution = message.showAdAttribution;
                if (message.ctwaClid != null && Object.hasOwnProperty.call(message, "ctwaClid"))
                    object.ctwaClid = message.ctwaClid;
                if (message.ref != null && Object.hasOwnProperty.call(message, "ref"))
                    object.ref = message.ref;
                if (message.clickToWhatsappCall != null && Object.hasOwnProperty.call(message, "clickToWhatsappCall"))
                    object.clickToWhatsappCall = message.clickToWhatsappCall;
                if (message.adContextPreviewDismissed != null && Object.hasOwnProperty.call(message, "adContextPreviewDismissed"))
                    object.adContextPreviewDismissed = message.adContextPreviewDismissed;
                if (message.sourceApp != null && Object.hasOwnProperty.call(message, "sourceApp"))
                    object.sourceApp = message.sourceApp;
                if (message.automatedGreetingMessageShown != null && Object.hasOwnProperty.call(message, "automatedGreetingMessageShown"))
                    object.automatedGreetingMessageShown = message.automatedGreetingMessageShown;
                if (message.greetingMessageBody != null && Object.hasOwnProperty.call(message, "greetingMessageBody"))
                    object.greetingMessageBody = message.greetingMessageBody;
                if (message.ctaPayload != null && Object.hasOwnProperty.call(message, "ctaPayload"))
                    object.ctaPayload = message.ctaPayload;
                if (message.disableNudge != null && Object.hasOwnProperty.call(message, "disableNudge"))
                    object.disableNudge = message.disableNudge;
                if (message.originalImageUrl != null && Object.hasOwnProperty.call(message, "originalImageUrl"))
                    object.originalImageUrl = message.originalImageUrl;
                if (message.automatedGreetingMessageCtaType != null && Object.hasOwnProperty.call(message, "automatedGreetingMessageCtaType"))
                    object.automatedGreetingMessageCtaType = message.automatedGreetingMessageCtaType;
                if (message.wtwaAdFormat != null && Object.hasOwnProperty.call(message, "wtwaAdFormat"))
                    object.wtwaAdFormat = message.wtwaAdFormat;
                if (message.adType != null && Object.hasOwnProperty.call(message, "adType"))
                    object.adType = options.enums === String ? $root.proto.ContextInfo.ExternalAdReplyInfo.AdType[message.adType] === undefined ? message.adType : $root.proto.ContextInfo.ExternalAdReplyInfo.AdType[message.adType] : message.adType;
                if (message.wtwaWebsiteUrl != null && Object.hasOwnProperty.call(message, "wtwaWebsiteUrl"))
                    object.wtwaWebsiteUrl = message.wtwaWebsiteUrl;
                if (message.adPreviewUrl != null && Object.hasOwnProperty.call(message, "adPreviewUrl"))
                    object.adPreviewUrl = message.adPreviewUrl;
                if (message.containsCtwaFlowsAutoReply != null && Object.hasOwnProperty.call(message, "containsCtwaFlowsAutoReply"))
                    object.containsCtwaFlowsAutoReply = message.containsCtwaFlowsAutoReply;
                if (message.agmThumbnailStrategy != null && Object.hasOwnProperty.call(message, "agmThumbnailStrategy"))
                    object.agmThumbnailStrategy = message.agmThumbnailStrategy;
                if (message.agmTitleStrategy != null && Object.hasOwnProperty.call(message, "agmTitleStrategy"))
                    object.agmTitleStrategy = message.agmTitleStrategy;
                if (message.agmSubtitleStrategy != null && Object.hasOwnProperty.call(message, "agmSubtitleStrategy"))
                    object.agmSubtitleStrategy = message.agmSubtitleStrategy;
                if (message.agmHeaderInteractionStrategy != null && Object.hasOwnProperty.call(message, "agmHeaderInteractionStrategy"))
                    object.agmHeaderInteractionStrategy = message.agmHeaderInteractionStrategy;
                return object;
            };

            /**
             * Converts this ExternalAdReplyInfo to JSON.
             * @function toJSON
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ExternalAdReplyInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ExternalAdReplyInfo
             * @function getTypeUrl
             * @memberof proto.ContextInfo.ExternalAdReplyInfo
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ExternalAdReplyInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.ContextInfo.ExternalAdReplyInfo";
            };

            /**
             * AdType enum.
             * @name proto.ContextInfo.ExternalAdReplyInfo.AdType
             * @enum {number}
             * @property {number} CTWA=0 CTWA value
             * @property {number} CAWC=1 CAWC value
             */
            ExternalAdReplyInfo.AdType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "CTWA"] = 0;
                values[valuesById[1] = "CAWC"] = 1;
                return values;
            })();

            /**
             * MediaType enum.
             * @name proto.ContextInfo.ExternalAdReplyInfo.MediaType
             * @enum {number}
             * @property {number} NONE=0 NONE value
             * @property {number} IMAGE=1 IMAGE value
             * @property {number} VIDEO=2 VIDEO value
             */
            ExternalAdReplyInfo.MediaType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "NONE"] = 0;
                values[valuesById[1] = "IMAGE"] = 1;
                values[valuesById[2] = "VIDEO"] = 2;
                return values;
            })();

            return ExternalAdReplyInfo;
        })();

        ContextInfo.FeatureEligibilities = (function() {

            /**
             * Properties of a FeatureEligibilities.
             * @memberof proto.ContextInfo
             * @interface IFeatureEligibilities
             * @property {boolean|null} [cannotBeReactedTo] FeatureEligibilities cannotBeReactedTo
             * @property {boolean|null} [cannotBeRanked] FeatureEligibilities cannotBeRanked
             * @property {boolean|null} [canRequestFeedback] FeatureEligibilities canRequestFeedback
             * @property {boolean|null} [canBeReshared] FeatureEligibilities canBeReshared
             * @property {boolean|null} [canReceiveMultiReact] FeatureEligibilities canReceiveMultiReact
             */

            /**
             * Constructs a new FeatureEligibilities.
             * @memberof proto.ContextInfo
             * @classdesc Represents a FeatureEligibilities.
             * @implements IFeatureEligibilities
             * @constructor
             * @param {proto.ContextInfo.IFeatureEligibilities=} [properties] Properties to set
             */
            function FeatureEligibilities(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * FeatureEligibilities cannotBeReactedTo.
             * @member {boolean|null|undefined} cannotBeReactedTo
             * @memberof proto.ContextInfo.FeatureEligibilities
             * @instance
             */
            FeatureEligibilities.prototype.cannotBeReactedTo = null;

            /**
             * FeatureEligibilities cannotBeRanked.
             * @member {boolean|null|undefined} cannotBeRanked
             * @memberof proto.ContextInfo.FeatureEligibilities
             * @instance
             */
            FeatureEligibilities.prototype.cannotBeRanked = null;

            /**
             * FeatureEligibilities canRequestFeedback.
             * @member {boolean|null|undefined} canRequestFeedback
             * @memberof proto.ContextInfo.FeatureEligibilities
             * @instance
             */
            FeatureEligibilities.prototype.canRequestFeedback = null;

            /**
             * FeatureEligibilities canBeReshared.
             * @member {boolean|null|undefined} canBeReshared
             * @memberof proto.ContextInfo.FeatureEligibilities
             * @instance
             */
            FeatureEligibilities.prototype.canBeReshared = null;

            /**
             * FeatureEligibilities canReceiveMultiReact.
             * @member {boolean|null|undefined} canReceiveMultiReact
             * @memberof proto.ContextInfo.FeatureEligibilities
             * @instance
             */
            FeatureEligibilities.prototype.canReceiveMultiReact = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(FeatureEligibilities.prototype, "_cannotBeReactedTo", {
                get: $util.oneOfGetter($oneOfFields = ["cannotBeReactedTo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(FeatureEligibilities.prototype, "_cannotBeRanked", {
                get: $util.oneOfGetter($oneOfFields = ["cannotBeRanked"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(FeatureEligibilities.prototype, "_canRequestFeedback", {
                get: $util.oneOfGetter($oneOfFields = ["canRequestFeedback"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(FeatureEligibilities.prototype, "_canBeReshared", {
                get: $util.oneOfGetter($oneOfFields = ["canBeReshared"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(FeatureEligibilities.prototype, "_canReceiveMultiReact", {
                get: $util.oneOfGetter($oneOfFields = ["canReceiveMultiReact"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new FeatureEligibilities instance using the specified properties.
             * @function create
             * @memberof proto.ContextInfo.FeatureEligibilities
             * @static
             * @param {proto.ContextInfo.IFeatureEligibilities=} [properties] Properties to set
             * @returns {proto.ContextInfo.FeatureEligibilities} FeatureEligibilities instance
             */
            FeatureEligibilities.create = function create(properties) {
                return new FeatureEligibilities(properties);
            };

            /**
             * Encodes the specified FeatureEligibilities message. Does not implicitly {@link proto.ContextInfo.FeatureEligibilities.verify|verify} messages.
             * @function encode
             * @memberof proto.ContextInfo.FeatureEligibilities
             * @static
             * @param {proto.ContextInfo.IFeatureEligibilities} message FeatureEligibilities message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FeatureEligibilities.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.cannotBeReactedTo != null && Object.hasOwnProperty.call(message, "cannotBeReactedTo"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.cannotBeReactedTo);
                if (message.cannotBeRanked != null && Object.hasOwnProperty.call(message, "cannotBeRanked"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.cannotBeRanked);
                if (message.canRequestFeedback != null && Object.hasOwnProperty.call(message, "canRequestFeedback"))
                    writer.uint32(/* id 3, wireType 0 =*/24).bool(message.canRequestFeedback);
                if (message.canBeReshared != null && Object.hasOwnProperty.call(message, "canBeReshared"))
                    writer.uint32(/* id 4, wireType 0 =*/32).bool(message.canBeReshared);
                if (message.canReceiveMultiReact != null && Object.hasOwnProperty.call(message, "canReceiveMultiReact"))
                    writer.uint32(/* id 5, wireType 0 =*/40).bool(message.canReceiveMultiReact);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified FeatureEligibilities message, length delimited. Does not implicitly {@link proto.ContextInfo.FeatureEligibilities.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.ContextInfo.FeatureEligibilities
             * @static
             * @param {proto.ContextInfo.IFeatureEligibilities} message FeatureEligibilities message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FeatureEligibilities.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a FeatureEligibilities message from the specified reader or buffer.
             * @function decode
             * @memberof proto.ContextInfo.FeatureEligibilities
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.ContextInfo.FeatureEligibilities} FeatureEligibilities
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FeatureEligibilities.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.cannotBeReactedTo = reader.bool();
                            message._cannotBeReactedTo = "cannotBeReactedTo";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.cannotBeRanked = reader.bool();
                            message._cannotBeRanked = "cannotBeRanked";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.canRequestFeedback = reader.bool();
                            message._canRequestFeedback = "canRequestFeedback";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.canBeReshared = reader.bool();
                            message._canBeReshared = "canBeReshared";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.canReceiveMultiReact = reader.bool();
                            message._canReceiveMultiReact = "canReceiveMultiReact";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a FeatureEligibilities message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.ContextInfo.FeatureEligibilities
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.ContextInfo.FeatureEligibilities} FeatureEligibilities
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FeatureEligibilities.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FeatureEligibilities message.
             * @function verify
             * @memberof proto.ContextInfo.FeatureEligibilities
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FeatureEligibilities.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.cannotBeReactedTo != null && Object.hasOwnProperty.call(message, "cannotBeReactedTo")) {
                    properties._cannotBeReactedTo = 1;
                    if (typeof message.cannotBeReactedTo !== "boolean")
                        return "cannotBeReactedTo: boolean expected";
                }
                if (message.cannotBeRanked != null && Object.hasOwnProperty.call(message, "cannotBeRanked")) {
                    properties._cannotBeRanked = 1;
                    if (typeof message.cannotBeRanked !== "boolean")
                        return "cannotBeRanked: boolean expected";
                }
                if (message.canRequestFeedback != null && Object.hasOwnProperty.call(message, "canRequestFeedback")) {
                    properties._canRequestFeedback = 1;
                    if (typeof message.canRequestFeedback !== "boolean")
                        return "canRequestFeedback: boolean expected";
                }
                if (message.canBeReshared != null && Object.hasOwnProperty.call(message, "canBeReshared")) {
                    properties._canBeReshared = 1;
                    if (typeof message.canBeReshared !== "boolean")
                        return "canBeReshared: boolean expected";
                }
                if (message.canReceiveMultiReact != null && Object.hasOwnProperty.call(message, "canReceiveMultiReact")) {
                    properties._canReceiveMultiReact = 1;
                    if (typeof message.canReceiveMultiReact !== "boolean")
                        return "canReceiveMultiReact: boolean expected";
                }
                return null;
            };

            /**
             * Creates a FeatureEligibilities message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.ContextInfo.FeatureEligibilities
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.ContextInfo.FeatureEligibilities} FeatureEligibilities
             */
            FeatureEligibilities.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.ContextInfo.FeatureEligibilities: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.cannotBeReactedTo != null)
                    message.cannotBeReactedTo = Boolean(object.cannotBeReactedTo);
                if (object.cannotBeRanked != null)
                    message.cannotBeRanked = Boolean(object.cannotBeRanked);
                if (object.canRequestFeedback != null)
                    message.canRequestFeedback = Boolean(object.canRequestFeedback);
                if (object.canBeReshared != null)
                    message.canBeReshared = Boolean(object.canBeReshared);
                if (object.canReceiveMultiReact != null)
                    message.canReceiveMultiReact = Boolean(object.canReceiveMultiReact);
                return message;
            };

            /**
             * Creates a plain object from a FeatureEligibilities message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.ContextInfo.FeatureEligibilities
             * @static
             * @param {proto.ContextInfo.FeatureEligibilities} message FeatureEligibilities
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FeatureEligibilities.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.cannotBeReactedTo != null && Object.hasOwnProperty.call(message, "cannotBeReactedTo"))
                    object.cannotBeReactedTo = message.cannotBeReactedTo;
                if (message.cannotBeRanked != null && Object.hasOwnProperty.call(message, "cannotBeRanked"))
                    object.cannotBeRanked = message.cannotBeRanked;
                if (message.canRequestFeedback != null && Object.hasOwnProperty.call(message, "canRequestFeedback"))
                    object.canRequestFeedback = message.canRequestFeedback;
                if (message.canBeReshared != null && Object.hasOwnProperty.call(message, "canBeReshared"))
                    object.canBeReshared = message.canBeReshared;
                if (message.canReceiveMultiReact != null && Object.hasOwnProperty.call(message, "canReceiveMultiReact"))
                    object.canReceiveMultiReact = message.canReceiveMultiReact;
                return object;
            };

            /**
             * Converts this FeatureEligibilities to JSON.
             * @function toJSON
             * @memberof proto.ContextInfo.FeatureEligibilities
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FeatureEligibilities.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for FeatureEligibilities
             * @function getTypeUrl
             * @memberof proto.ContextInfo.FeatureEligibilities
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            FeatureEligibilities.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.ContextInfo.FeatureEligibilities";
            };

            return FeatureEligibilities;
        })();

        /**
         * ForwardOrigin enum.
         * @name proto.ContextInfo.ForwardOrigin
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} CHAT=1 CHAT value
         * @property {number} STATUS=2 STATUS value
         * @property {number} CHANNELS=3 CHANNELS value
         * @property {number} META_AI=4 META_AI value
         * @property {number} UGC=5 UGC value
         */
        ContextInfo.ForwardOrigin = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "CHAT"] = 1;
            values[valuesById[2] = "STATUS"] = 2;
            values[valuesById[3] = "CHANNELS"] = 3;
            values[valuesById[4] = "META_AI"] = 4;
            values[valuesById[5] = "UGC"] = 5;
            return values;
        })();

        ContextInfo.ForwardedNewsletterMessageInfo = (function() {

            /**
             * Properties of a ForwardedNewsletterMessageInfo.
             * @memberof proto.ContextInfo
             * @interface IForwardedNewsletterMessageInfo
             * @property {string|null} [newsletterJid] ForwardedNewsletterMessageInfo newsletterJid
             * @property {number|null} [serverMessageId] ForwardedNewsletterMessageInfo serverMessageId
             * @property {string|null} [newsletterName] ForwardedNewsletterMessageInfo newsletterName
             * @property {proto.ContextInfo.ForwardedNewsletterMessageInfo.ContentType|null} [contentType] ForwardedNewsletterMessageInfo contentType
             * @property {string|null} [accessibilityText] ForwardedNewsletterMessageInfo accessibilityText
             * @property {string|null} [profileName] ForwardedNewsletterMessageInfo profileName
             */

            /**
             * Constructs a new ForwardedNewsletterMessageInfo.
             * @memberof proto.ContextInfo
             * @classdesc Represents a ForwardedNewsletterMessageInfo.
             * @implements IForwardedNewsletterMessageInfo
             * @constructor
             * @param {proto.ContextInfo.IForwardedNewsletterMessageInfo=} [properties] Properties to set
             */
            function ForwardedNewsletterMessageInfo(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ForwardedNewsletterMessageInfo newsletterJid.
             * @member {string|null|undefined} newsletterJid
             * @memberof proto.ContextInfo.ForwardedNewsletterMessageInfo
             * @instance
             */
            ForwardedNewsletterMessageInfo.prototype.newsletterJid = null;

            /**
             * ForwardedNewsletterMessageInfo serverMessageId.
             * @member {number|null|undefined} serverMessageId
             * @memberof proto.ContextInfo.ForwardedNewsletterMessageInfo
             * @instance
             */
            ForwardedNewsletterMessageInfo.prototype.serverMessageId = null;

            /**
             * ForwardedNewsletterMessageInfo newsletterName.
             * @member {string|null|undefined} newsletterName
             * @memberof proto.ContextInfo.ForwardedNewsletterMessageInfo
             * @instance
             */
            ForwardedNewsletterMessageInfo.prototype.newsletterName = null;

            /**
             * ForwardedNewsletterMessageInfo contentType.
             * @member {proto.ContextInfo.ForwardedNewsletterMessageInfo.ContentType|null|undefined} contentType
             * @memberof proto.ContextInfo.ForwardedNewsletterMessageInfo
             * @instance
             */
            ForwardedNewsletterMessageInfo.prototype.contentType = null;

            /**
             * ForwardedNewsletterMessageInfo accessibilityText.
             * @member {string|null|undefined} accessibilityText
             * @memberof proto.ContextInfo.ForwardedNewsletterMessageInfo
             * @instance
             */
            ForwardedNewsletterMessageInfo.prototype.accessibilityText = null;

            /**
             * ForwardedNewsletterMessageInfo profileName.
             * @member {string|null|undefined} profileName
             * @memberof proto.ContextInfo.ForwardedNewsletterMessageInfo
             * @instance
             */
            ForwardedNewsletterMessageInfo.prototype.profileName = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ForwardedNewsletterMessageInfo.prototype, "_newsletterJid", {
                get: $util.oneOfGetter($oneOfFields = ["newsletterJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ForwardedNewsletterMessageInfo.prototype, "_serverMessageId", {
                get: $util.oneOfGetter($oneOfFields = ["serverMessageId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ForwardedNewsletterMessageInfo.prototype, "_newsletterName", {
                get: $util.oneOfGetter($oneOfFields = ["newsletterName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ForwardedNewsletterMessageInfo.prototype, "_contentType", {
                get: $util.oneOfGetter($oneOfFields = ["contentType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ForwardedNewsletterMessageInfo.prototype, "_accessibilityText", {
                get: $util.oneOfGetter($oneOfFields = ["accessibilityText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ForwardedNewsletterMessageInfo.prototype, "_profileName", {
                get: $util.oneOfGetter($oneOfFields = ["profileName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ForwardedNewsletterMessageInfo instance using the specified properties.
             * @function create
             * @memberof proto.ContextInfo.ForwardedNewsletterMessageInfo
             * @static
             * @param {proto.ContextInfo.IForwardedNewsletterMessageInfo=} [properties] Properties to set
             * @returns {proto.ContextInfo.ForwardedNewsletterMessageInfo} ForwardedNewsletterMessageInfo instance
             */
            ForwardedNewsletterMessageInfo.create = function create(properties) {
                return new ForwardedNewsletterMessageInfo(properties);
            };

            /**
             * Encodes the specified ForwardedNewsletterMessageInfo message. Does not implicitly {@link proto.ContextInfo.ForwardedNewsletterMessageInfo.verify|verify} messages.
             * @function encode
             * @memberof proto.ContextInfo.ForwardedNewsletterMessageInfo
             * @static
             * @param {proto.ContextInfo.IForwardedNewsletterMessageInfo} message ForwardedNewsletterMessageInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ForwardedNewsletterMessageInfo.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.newsletterJid != null && Object.hasOwnProperty.call(message, "newsletterJid"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.newsletterJid);
                if (message.serverMessageId != null && Object.hasOwnProperty.call(message, "serverMessageId"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.serverMessageId);
                if (message.newsletterName != null && Object.hasOwnProperty.call(message, "newsletterName"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.newsletterName);
                if (message.contentType != null && Object.hasOwnProperty.call(message, "contentType"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int32(message.contentType);
                if (message.accessibilityText != null && Object.hasOwnProperty.call(message, "accessibilityText"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.accessibilityText);
                if (message.profileName != null && Object.hasOwnProperty.call(message, "profileName"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.profileName);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ForwardedNewsletterMessageInfo message, length delimited. Does not implicitly {@link proto.ContextInfo.ForwardedNewsletterMessageInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.ContextInfo.ForwardedNewsletterMessageInfo
             * @static
             * @param {proto.ContextInfo.IForwardedNewsletterMessageInfo} message ForwardedNewsletterMessageInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ForwardedNewsletterMessageInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ForwardedNewsletterMessageInfo message from the specified reader or buffer.
             * @function decode
             * @memberof proto.ContextInfo.ForwardedNewsletterMessageInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.ContextInfo.ForwardedNewsletterMessageInfo} ForwardedNewsletterMessageInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ForwardedNewsletterMessageInfo.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.newsletterJid = reader.stringVerify();
                            message._newsletterJid = "newsletterJid";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.serverMessageId = reader.int32();
                            message._serverMessageId = "serverMessageId";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.newsletterName = reader.stringVerify();
                            message._newsletterName = "newsletterName";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.contentType = reader.int32();
                            message._contentType = "contentType";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.accessibilityText = reader.stringVerify();
                            message._accessibilityText = "accessibilityText";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.profileName = reader.stringVerify();
                            message._profileName = "profileName";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ForwardedNewsletterMessageInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.ContextInfo.ForwardedNewsletterMessageInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.ContextInfo.ForwardedNewsletterMessageInfo} ForwardedNewsletterMessageInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ForwardedNewsletterMessageInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ForwardedNewsletterMessageInfo message.
             * @function verify
             * @memberof proto.ContextInfo.ForwardedNewsletterMessageInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ForwardedNewsletterMessageInfo.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.newsletterJid != null && Object.hasOwnProperty.call(message, "newsletterJid")) {
                    properties._newsletterJid = 1;
                    if (!$util.isString(message.newsletterJid))
                        return "newsletterJid: string expected";
                }
                if (message.serverMessageId != null && Object.hasOwnProperty.call(message, "serverMessageId")) {
                    properties._serverMessageId = 1;
                    if (!$util.isInteger(message.serverMessageId))
                        return "serverMessageId: integer expected";
                }
                if (message.newsletterName != null && Object.hasOwnProperty.call(message, "newsletterName")) {
                    properties._newsletterName = 1;
                    if (!$util.isString(message.newsletterName))
                        return "newsletterName: string expected";
                }
                if (message.contentType != null && Object.hasOwnProperty.call(message, "contentType")) {
                    properties._contentType = 1;
                    if (typeof message.contentType !== "number" || (message.contentType | 0) !== message.contentType)
                        return "contentType: enum value expected";
                }
                if (message.accessibilityText != null && Object.hasOwnProperty.call(message, "accessibilityText")) {
                    properties._accessibilityText = 1;
                    if (!$util.isString(message.accessibilityText))
                        return "accessibilityText: string expected";
                }
                if (message.profileName != null && Object.hasOwnProperty.call(message, "profileName")) {
                    properties._profileName = 1;
                    if (!$util.isString(message.profileName))
                        return "profileName: string expected";
                }
                return null;
            };

            /**
             * Creates a ForwardedNewsletterMessageInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.ContextInfo.ForwardedNewsletterMessageInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.ContextInfo.ForwardedNewsletterMessageInfo} ForwardedNewsletterMessageInfo
             */
            ForwardedNewsletterMessageInfo.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.ContextInfo.ForwardedNewsletterMessageInfo: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.newsletterJid != null)
                    message.newsletterJid = String(object.newsletterJid);
                if (object.serverMessageId != null)
                    message.serverMessageId = object.serverMessageId | 0;
                if (object.newsletterName != null)
                    message.newsletterName = String(object.newsletterName);
                switch (object.contentType) {
                case "UPDATE":
                case 1:
                    message.contentType = 1;
                    break;
                case "UPDATE_CARD":
                case 2:
                    message.contentType = 2;
                    break;
                case "LINK_CARD":
                case 3:
                    message.contentType = 3;
                    break;
                default:
                    if (typeof object.contentType === "number" && (object.contentType | 0) === object.contentType)
                        message.contentType = object.contentType;
                }
                if (object.accessibilityText != null)
                    message.accessibilityText = String(object.accessibilityText);
                if (object.profileName != null)
                    message.profileName = String(object.profileName);
                return message;
            };

            /**
             * Creates a plain object from a ForwardedNewsletterMessageInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.ContextInfo.ForwardedNewsletterMessageInfo
             * @static
             * @param {proto.ContextInfo.ForwardedNewsletterMessageInfo} message ForwardedNewsletterMessageInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ForwardedNewsletterMessageInfo.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.newsletterJid != null && Object.hasOwnProperty.call(message, "newsletterJid"))
                    object.newsletterJid = message.newsletterJid;
                if (message.serverMessageId != null && Object.hasOwnProperty.call(message, "serverMessageId"))
                    object.serverMessageId = message.serverMessageId;
                if (message.newsletterName != null && Object.hasOwnProperty.call(message, "newsletterName"))
                    object.newsletterName = message.newsletterName;
                if (message.contentType != null && Object.hasOwnProperty.call(message, "contentType"))
                    object.contentType = options.enums === String ? $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.ContentType[message.contentType] === undefined ? message.contentType : $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.ContentType[message.contentType] : message.contentType;
                if (message.accessibilityText != null && Object.hasOwnProperty.call(message, "accessibilityText"))
                    object.accessibilityText = message.accessibilityText;
                if (message.profileName != null && Object.hasOwnProperty.call(message, "profileName"))
                    object.profileName = message.profileName;
                return object;
            };

            /**
             * Converts this ForwardedNewsletterMessageInfo to JSON.
             * @function toJSON
             * @memberof proto.ContextInfo.ForwardedNewsletterMessageInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ForwardedNewsletterMessageInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ForwardedNewsletterMessageInfo
             * @function getTypeUrl
             * @memberof proto.ContextInfo.ForwardedNewsletterMessageInfo
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ForwardedNewsletterMessageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.ContextInfo.ForwardedNewsletterMessageInfo";
            };

            /**
             * ContentType enum.
             * @name proto.ContextInfo.ForwardedNewsletterMessageInfo.ContentType
             * @enum {number}
             * @property {number} UPDATE=1 UPDATE value
             * @property {number} UPDATE_CARD=2 UPDATE_CARD value
             * @property {number} LINK_CARD=3 LINK_CARD value
             */
            ForwardedNewsletterMessageInfo.ContentType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[1] = "UPDATE"] = 1;
                values[valuesById[2] = "UPDATE_CARD"] = 2;
                values[valuesById[3] = "LINK_CARD"] = 3;
                return values;
            })();

            return ForwardedNewsletterMessageInfo;
        })();

        /**
         * PairedMediaType enum.
         * @name proto.ContextInfo.PairedMediaType
         * @enum {number}
         * @property {number} NOT_PAIRED_MEDIA=0 NOT_PAIRED_MEDIA value
         * @property {number} SD_VIDEO_PARENT=1 SD_VIDEO_PARENT value
         * @property {number} HD_VIDEO_CHILD=2 HD_VIDEO_CHILD value
         * @property {number} SD_IMAGE_PARENT=3 SD_IMAGE_PARENT value
         * @property {number} HD_IMAGE_CHILD=4 HD_IMAGE_CHILD value
         * @property {number} MOTION_PHOTO_PARENT=5 MOTION_PHOTO_PARENT value
         * @property {number} MOTION_PHOTO_CHILD=6 MOTION_PHOTO_CHILD value
         * @property {number} HEVC_VIDEO_PARENT=7 HEVC_VIDEO_PARENT value
         * @property {number} HEVC_VIDEO_CHILD=8 HEVC_VIDEO_CHILD value
         */
        ContextInfo.PairedMediaType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "NOT_PAIRED_MEDIA"] = 0;
            values[valuesById[1] = "SD_VIDEO_PARENT"] = 1;
            values[valuesById[2] = "HD_VIDEO_CHILD"] = 2;
            values[valuesById[3] = "SD_IMAGE_PARENT"] = 3;
            values[valuesById[4] = "HD_IMAGE_CHILD"] = 4;
            values[valuesById[5] = "MOTION_PHOTO_PARENT"] = 5;
            values[valuesById[6] = "MOTION_PHOTO_CHILD"] = 6;
            values[valuesById[7] = "HEVC_VIDEO_PARENT"] = 7;
            values[valuesById[8] = "HEVC_VIDEO_CHILD"] = 8;
            return values;
        })();

        ContextInfo.PartiallySelectedContent = (function() {

            /**
             * Properties of a PartiallySelectedContent.
             * @memberof proto.ContextInfo
             * @interface IPartiallySelectedContent
             * @property {string|null} [text] PartiallySelectedContent text
             */

            /**
             * Constructs a new PartiallySelectedContent.
             * @memberof proto.ContextInfo
             * @classdesc Represents a PartiallySelectedContent.
             * @implements IPartiallySelectedContent
             * @constructor
             * @param {proto.ContextInfo.IPartiallySelectedContent=} [properties] Properties to set
             */
            function PartiallySelectedContent(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PartiallySelectedContent text.
             * @member {string|null|undefined} text
             * @memberof proto.ContextInfo.PartiallySelectedContent
             * @instance
             */
            PartiallySelectedContent.prototype.text = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PartiallySelectedContent.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PartiallySelectedContent instance using the specified properties.
             * @function create
             * @memberof proto.ContextInfo.PartiallySelectedContent
             * @static
             * @param {proto.ContextInfo.IPartiallySelectedContent=} [properties] Properties to set
             * @returns {proto.ContextInfo.PartiallySelectedContent} PartiallySelectedContent instance
             */
            PartiallySelectedContent.create = function create(properties) {
                return new PartiallySelectedContent(properties);
            };

            /**
             * Encodes the specified PartiallySelectedContent message. Does not implicitly {@link proto.ContextInfo.PartiallySelectedContent.verify|verify} messages.
             * @function encode
             * @memberof proto.ContextInfo.PartiallySelectedContent
             * @static
             * @param {proto.ContextInfo.IPartiallySelectedContent} message PartiallySelectedContent message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PartiallySelectedContent.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.text);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PartiallySelectedContent message, length delimited. Does not implicitly {@link proto.ContextInfo.PartiallySelectedContent.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.ContextInfo.PartiallySelectedContent
             * @static
             * @param {proto.ContextInfo.IPartiallySelectedContent} message PartiallySelectedContent message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PartiallySelectedContent.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PartiallySelectedContent message from the specified reader or buffer.
             * @function decode
             * @memberof proto.ContextInfo.PartiallySelectedContent
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.ContextInfo.PartiallySelectedContent} PartiallySelectedContent
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PartiallySelectedContent.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.text = reader.stringVerify();
                            message._text = "text";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PartiallySelectedContent message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.ContextInfo.PartiallySelectedContent
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.ContextInfo.PartiallySelectedContent} PartiallySelectedContent
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PartiallySelectedContent.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PartiallySelectedContent message.
             * @function verify
             * @memberof proto.ContextInfo.PartiallySelectedContent
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PartiallySelectedContent.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.text != null && Object.hasOwnProperty.call(message, "text")) {
                    properties._text = 1;
                    if (!$util.isString(message.text))
                        return "text: string expected";
                }
                return null;
            };

            /**
             * Creates a PartiallySelectedContent message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.ContextInfo.PartiallySelectedContent
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.ContextInfo.PartiallySelectedContent} PartiallySelectedContent
             */
            PartiallySelectedContent.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.ContextInfo.PartiallySelectedContent: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.text != null)
                    message.text = String(object.text);
                return message;
            };

            /**
             * Creates a plain object from a PartiallySelectedContent message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.ContextInfo.PartiallySelectedContent
             * @static
             * @param {proto.ContextInfo.PartiallySelectedContent} message PartiallySelectedContent
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PartiallySelectedContent.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                    object.text = message.text;
                return object;
            };

            /**
             * Converts this PartiallySelectedContent to JSON.
             * @function toJSON
             * @memberof proto.ContextInfo.PartiallySelectedContent
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PartiallySelectedContent.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PartiallySelectedContent
             * @function getTypeUrl
             * @memberof proto.ContextInfo.PartiallySelectedContent
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PartiallySelectedContent.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.ContextInfo.PartiallySelectedContent";
            };

            return PartiallySelectedContent;
        })();

        ContextInfo.QuestionReplyQuotedMessage = (function() {

            /**
             * Properties of a QuestionReplyQuotedMessage.
             * @memberof proto.ContextInfo
             * @interface IQuestionReplyQuotedMessage
             * @property {number|null} [serverQuestionId] QuestionReplyQuotedMessage serverQuestionId
             * @property {proto.IMessage|null} [quotedQuestion] QuestionReplyQuotedMessage quotedQuestion
             * @property {proto.IMessage|null} [quotedResponse] QuestionReplyQuotedMessage quotedResponse
             */

            /**
             * Constructs a new QuestionReplyQuotedMessage.
             * @memberof proto.ContextInfo
             * @classdesc Represents a QuestionReplyQuotedMessage.
             * @implements IQuestionReplyQuotedMessage
             * @constructor
             * @param {proto.ContextInfo.IQuestionReplyQuotedMessage=} [properties] Properties to set
             */
            function QuestionReplyQuotedMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * QuestionReplyQuotedMessage serverQuestionId.
             * @member {number|null|undefined} serverQuestionId
             * @memberof proto.ContextInfo.QuestionReplyQuotedMessage
             * @instance
             */
            QuestionReplyQuotedMessage.prototype.serverQuestionId = null;

            /**
             * QuestionReplyQuotedMessage quotedQuestion.
             * @member {proto.IMessage|null|undefined} quotedQuestion
             * @memberof proto.ContextInfo.QuestionReplyQuotedMessage
             * @instance
             */
            QuestionReplyQuotedMessage.prototype.quotedQuestion = null;

            /**
             * QuestionReplyQuotedMessage quotedResponse.
             * @member {proto.IMessage|null|undefined} quotedResponse
             * @memberof proto.ContextInfo.QuestionReplyQuotedMessage
             * @instance
             */
            QuestionReplyQuotedMessage.prototype.quotedResponse = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(QuestionReplyQuotedMessage.prototype, "_serverQuestionId", {
                get: $util.oneOfGetter($oneOfFields = ["serverQuestionId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(QuestionReplyQuotedMessage.prototype, "_quotedQuestion", {
                get: $util.oneOfGetter($oneOfFields = ["quotedQuestion"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(QuestionReplyQuotedMessage.prototype, "_quotedResponse", {
                get: $util.oneOfGetter($oneOfFields = ["quotedResponse"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new QuestionReplyQuotedMessage instance using the specified properties.
             * @function create
             * @memberof proto.ContextInfo.QuestionReplyQuotedMessage
             * @static
             * @param {proto.ContextInfo.IQuestionReplyQuotedMessage=} [properties] Properties to set
             * @returns {proto.ContextInfo.QuestionReplyQuotedMessage} QuestionReplyQuotedMessage instance
             */
            QuestionReplyQuotedMessage.create = function create(properties) {
                return new QuestionReplyQuotedMessage(properties);
            };

            /**
             * Encodes the specified QuestionReplyQuotedMessage message. Does not implicitly {@link proto.ContextInfo.QuestionReplyQuotedMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.ContextInfo.QuestionReplyQuotedMessage
             * @static
             * @param {proto.ContextInfo.IQuestionReplyQuotedMessage} message QuestionReplyQuotedMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            QuestionReplyQuotedMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.serverQuestionId != null && Object.hasOwnProperty.call(message, "serverQuestionId"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.serverQuestionId);
                if (message.quotedQuestion != null && Object.hasOwnProperty.call(message, "quotedQuestion"))
                    $root.proto.Message.encode(message.quotedQuestion, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.quotedResponse != null && Object.hasOwnProperty.call(message, "quotedResponse"))
                    $root.proto.Message.encode(message.quotedResponse, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified QuestionReplyQuotedMessage message, length delimited. Does not implicitly {@link proto.ContextInfo.QuestionReplyQuotedMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.ContextInfo.QuestionReplyQuotedMessage
             * @static
             * @param {proto.ContextInfo.IQuestionReplyQuotedMessage} message QuestionReplyQuotedMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            QuestionReplyQuotedMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a QuestionReplyQuotedMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.ContextInfo.QuestionReplyQuotedMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.ContextInfo.QuestionReplyQuotedMessage} QuestionReplyQuotedMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            QuestionReplyQuotedMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.serverQuestionId = reader.int32();
                            message._serverQuestionId = "serverQuestionId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.quotedQuestion = $root.proto.Message.decode(reader, reader.uint32(), undefined, q + 1, message.quotedQuestion);
                            message._quotedQuestion = "quotedQuestion";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.quotedResponse = $root.proto.Message.decode(reader, reader.uint32(), undefined, q + 1, message.quotedResponse);
                            message._quotedResponse = "quotedResponse";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a QuestionReplyQuotedMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.ContextInfo.QuestionReplyQuotedMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.ContextInfo.QuestionReplyQuotedMessage} QuestionReplyQuotedMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            QuestionReplyQuotedMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a QuestionReplyQuotedMessage message.
             * @function verify
             * @memberof proto.ContextInfo.QuestionReplyQuotedMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            QuestionReplyQuotedMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.serverQuestionId != null && Object.hasOwnProperty.call(message, "serverQuestionId")) {
                    properties._serverQuestionId = 1;
                    if (!$util.isInteger(message.serverQuestionId))
                        return "serverQuestionId: integer expected";
                }
                if (message.quotedQuestion != null && Object.hasOwnProperty.call(message, "quotedQuestion")) {
                    properties._quotedQuestion = 1;
                    {
                        var error = $root.proto.Message.verify(message.quotedQuestion, q + 1);
                        if (error)
                            return "quotedQuestion." + error;
                    }
                }
                if (message.quotedResponse != null && Object.hasOwnProperty.call(message, "quotedResponse")) {
                    properties._quotedResponse = 1;
                    {
                        var error = $root.proto.Message.verify(message.quotedResponse, q + 1);
                        if (error)
                            return "quotedResponse." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a QuestionReplyQuotedMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.ContextInfo.QuestionReplyQuotedMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.ContextInfo.QuestionReplyQuotedMessage} QuestionReplyQuotedMessage
             */
            QuestionReplyQuotedMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.ContextInfo.QuestionReplyQuotedMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.serverQuestionId != null)
                    message.serverQuestionId = object.serverQuestionId | 0;
                if (object.quotedQuestion != null) {
                    if (!$util.isObject(object.quotedQuestion))
                        throw TypeError(".proto.ContextInfo.QuestionReplyQuotedMessage.quotedQuestion: object expected");
                    message.quotedQuestion = $root.proto.Message.fromObject(object.quotedQuestion, q + 1);
                }
                if (object.quotedResponse != null) {
                    if (!$util.isObject(object.quotedResponse))
                        throw TypeError(".proto.ContextInfo.QuestionReplyQuotedMessage.quotedResponse: object expected");
                    message.quotedResponse = $root.proto.Message.fromObject(object.quotedResponse, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a QuestionReplyQuotedMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.ContextInfo.QuestionReplyQuotedMessage
             * @static
             * @param {proto.ContextInfo.QuestionReplyQuotedMessage} message QuestionReplyQuotedMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            QuestionReplyQuotedMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.serverQuestionId != null && Object.hasOwnProperty.call(message, "serverQuestionId"))
                    object.serverQuestionId = message.serverQuestionId;
                if (message.quotedQuestion != null && Object.hasOwnProperty.call(message, "quotedQuestion"))
                    object.quotedQuestion = $root.proto.Message.toObject(message.quotedQuestion, options, q + 1);
                if (message.quotedResponse != null && Object.hasOwnProperty.call(message, "quotedResponse"))
                    object.quotedResponse = $root.proto.Message.toObject(message.quotedResponse, options, q + 1);
                return object;
            };

            /**
             * Converts this QuestionReplyQuotedMessage to JSON.
             * @function toJSON
             * @memberof proto.ContextInfo.QuestionReplyQuotedMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            QuestionReplyQuotedMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for QuestionReplyQuotedMessage
             * @function getTypeUrl
             * @memberof proto.ContextInfo.QuestionReplyQuotedMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            QuestionReplyQuotedMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.ContextInfo.QuestionReplyQuotedMessage";
            };

            return QuestionReplyQuotedMessage;
        })();

        /**
         * QuotedType enum.
         * @name proto.ContextInfo.QuotedType
         * @enum {number}
         * @property {number} EXPLICIT=0 EXPLICIT value
         * @property {number} AUTO=1 AUTO value
         */
        ContextInfo.QuotedType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "EXPLICIT"] = 0;
            values[valuesById[1] = "AUTO"] = 1;
            return values;
        })();

        /**
         * StatusAttributionType enum.
         * @name proto.ContextInfo.StatusAttributionType
         * @enum {number}
         * @property {number} NONE=0 NONE value
         * @property {number} RESHARED_FROM_MENTION=1 RESHARED_FROM_MENTION value
         * @property {number} RESHARED_FROM_POST=2 RESHARED_FROM_POST value
         * @property {number} RESHARED_FROM_POST_MANY_TIMES=3 RESHARED_FROM_POST_MANY_TIMES value
         * @property {number} FORWARDED_FROM_STATUS=4 FORWARDED_FROM_STATUS value
         */
        ContextInfo.StatusAttributionType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "NONE"] = 0;
            values[valuesById[1] = "RESHARED_FROM_MENTION"] = 1;
            values[valuesById[2] = "RESHARED_FROM_POST"] = 2;
            values[valuesById[3] = "RESHARED_FROM_POST_MANY_TIMES"] = 3;
            values[valuesById[4] = "FORWARDED_FROM_STATUS"] = 4;
            return values;
        })();

        ContextInfo.StatusAudienceMetadata = (function() {

            /**
             * Properties of a StatusAudienceMetadata.
             * @memberof proto.ContextInfo
             * @interface IStatusAudienceMetadata
             * @property {proto.ContextInfo.StatusAudienceMetadata.AudienceType|null} [audienceType] StatusAudienceMetadata audienceType
             * @property {string|null} [listName] StatusAudienceMetadata listName
             * @property {string|null} [listEmoji] StatusAudienceMetadata listEmoji
             */

            /**
             * Constructs a new StatusAudienceMetadata.
             * @memberof proto.ContextInfo
             * @classdesc Represents a StatusAudienceMetadata.
             * @implements IStatusAudienceMetadata
             * @constructor
             * @param {proto.ContextInfo.IStatusAudienceMetadata=} [properties] Properties to set
             */
            function StatusAudienceMetadata(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * StatusAudienceMetadata audienceType.
             * @member {proto.ContextInfo.StatusAudienceMetadata.AudienceType|null|undefined} audienceType
             * @memberof proto.ContextInfo.StatusAudienceMetadata
             * @instance
             */
            StatusAudienceMetadata.prototype.audienceType = null;

            /**
             * StatusAudienceMetadata listName.
             * @member {string|null|undefined} listName
             * @memberof proto.ContextInfo.StatusAudienceMetadata
             * @instance
             */
            StatusAudienceMetadata.prototype.listName = null;

            /**
             * StatusAudienceMetadata listEmoji.
             * @member {string|null|undefined} listEmoji
             * @memberof proto.ContextInfo.StatusAudienceMetadata
             * @instance
             */
            StatusAudienceMetadata.prototype.listEmoji = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusAudienceMetadata.prototype, "_audienceType", {
                get: $util.oneOfGetter($oneOfFields = ["audienceType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusAudienceMetadata.prototype, "_listName", {
                get: $util.oneOfGetter($oneOfFields = ["listName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusAudienceMetadata.prototype, "_listEmoji", {
                get: $util.oneOfGetter($oneOfFields = ["listEmoji"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StatusAudienceMetadata instance using the specified properties.
             * @function create
             * @memberof proto.ContextInfo.StatusAudienceMetadata
             * @static
             * @param {proto.ContextInfo.IStatusAudienceMetadata=} [properties] Properties to set
             * @returns {proto.ContextInfo.StatusAudienceMetadata} StatusAudienceMetadata instance
             */
            StatusAudienceMetadata.create = function create(properties) {
                return new StatusAudienceMetadata(properties);
            };

            /**
             * Encodes the specified StatusAudienceMetadata message. Does not implicitly {@link proto.ContextInfo.StatusAudienceMetadata.verify|verify} messages.
             * @function encode
             * @memberof proto.ContextInfo.StatusAudienceMetadata
             * @static
             * @param {proto.ContextInfo.IStatusAudienceMetadata} message StatusAudienceMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StatusAudienceMetadata.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.audienceType != null && Object.hasOwnProperty.call(message, "audienceType"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.audienceType);
                if (message.listName != null && Object.hasOwnProperty.call(message, "listName"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.listName);
                if (message.listEmoji != null && Object.hasOwnProperty.call(message, "listEmoji"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.listEmoji);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified StatusAudienceMetadata message, length delimited. Does not implicitly {@link proto.ContextInfo.StatusAudienceMetadata.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.ContextInfo.StatusAudienceMetadata
             * @static
             * @param {proto.ContextInfo.IStatusAudienceMetadata} message StatusAudienceMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StatusAudienceMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a StatusAudienceMetadata message from the specified reader or buffer.
             * @function decode
             * @memberof proto.ContextInfo.StatusAudienceMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.ContextInfo.StatusAudienceMetadata} StatusAudienceMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StatusAudienceMetadata.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.audienceType = reader.int32();
                            message._audienceType = "audienceType";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.listName = reader.stringVerify();
                            message._listName = "listName";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.listEmoji = reader.stringVerify();
                            message._listEmoji = "listEmoji";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a StatusAudienceMetadata message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.ContextInfo.StatusAudienceMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.ContextInfo.StatusAudienceMetadata} StatusAudienceMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StatusAudienceMetadata.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a StatusAudienceMetadata message.
             * @function verify
             * @memberof proto.ContextInfo.StatusAudienceMetadata
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StatusAudienceMetadata.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.audienceType != null && Object.hasOwnProperty.call(message, "audienceType")) {
                    properties._audienceType = 1;
                    if (typeof message.audienceType !== "number" || (message.audienceType | 0) !== message.audienceType)
                        return "audienceType: enum value expected";
                }
                if (message.listName != null && Object.hasOwnProperty.call(message, "listName")) {
                    properties._listName = 1;
                    if (!$util.isString(message.listName))
                        return "listName: string expected";
                }
                if (message.listEmoji != null && Object.hasOwnProperty.call(message, "listEmoji")) {
                    properties._listEmoji = 1;
                    if (!$util.isString(message.listEmoji))
                        return "listEmoji: string expected";
                }
                return null;
            };

            /**
             * Creates a StatusAudienceMetadata message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.ContextInfo.StatusAudienceMetadata
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.ContextInfo.StatusAudienceMetadata} StatusAudienceMetadata
             */
            StatusAudienceMetadata.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.ContextInfo.StatusAudienceMetadata: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.audienceType) {
                case "UNKNOWN":
                case 0:
                    message.audienceType = 0;
                    break;
                case "CLOSE_FRIENDS":
                case 1:
                    message.audienceType = 1;
                    break;
                default:
                    if (typeof object.audienceType === "number" && (object.audienceType | 0) === object.audienceType)
                        message.audienceType = object.audienceType;
                }
                if (object.listName != null)
                    message.listName = String(object.listName);
                if (object.listEmoji != null)
                    message.listEmoji = String(object.listEmoji);
                return message;
            };

            /**
             * Creates a plain object from a StatusAudienceMetadata message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.ContextInfo.StatusAudienceMetadata
             * @static
             * @param {proto.ContextInfo.StatusAudienceMetadata} message StatusAudienceMetadata
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StatusAudienceMetadata.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.audienceType != null && Object.hasOwnProperty.call(message, "audienceType"))
                    object.audienceType = options.enums === String ? $root.proto.ContextInfo.StatusAudienceMetadata.AudienceType[message.audienceType] === undefined ? message.audienceType : $root.proto.ContextInfo.StatusAudienceMetadata.AudienceType[message.audienceType] : message.audienceType;
                if (message.listName != null && Object.hasOwnProperty.call(message, "listName"))
                    object.listName = message.listName;
                if (message.listEmoji != null && Object.hasOwnProperty.call(message, "listEmoji"))
                    object.listEmoji = message.listEmoji;
                return object;
            };

            /**
             * Converts this StatusAudienceMetadata to JSON.
             * @function toJSON
             * @memberof proto.ContextInfo.StatusAudienceMetadata
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StatusAudienceMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for StatusAudienceMetadata
             * @function getTypeUrl
             * @memberof proto.ContextInfo.StatusAudienceMetadata
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            StatusAudienceMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.ContextInfo.StatusAudienceMetadata";
            };

            /**
             * AudienceType enum.
             * @name proto.ContextInfo.StatusAudienceMetadata.AudienceType
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} CLOSE_FRIENDS=1 CLOSE_FRIENDS value
             */
            StatusAudienceMetadata.AudienceType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "CLOSE_FRIENDS"] = 1;
                return values;
            })();

            return StatusAudienceMetadata;
        })();

        /**
         * StatusSourceType enum.
         * @name proto.ContextInfo.StatusSourceType
         * @enum {number}
         * @property {number} IMAGE=0 IMAGE value
         * @property {number} VIDEO=1 VIDEO value
         * @property {number} GIF=2 GIF value
         * @property {number} AUDIO=3 AUDIO value
         * @property {number} TEXT=4 TEXT value
         * @property {number} MUSIC_STANDALONE=5 MUSIC_STANDALONE value
         */
        ContextInfo.StatusSourceType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "IMAGE"] = 0;
            values[valuesById[1] = "VIDEO"] = 1;
            values[valuesById[2] = "GIF"] = 2;
            values[valuesById[3] = "AUDIO"] = 3;
            values[valuesById[4] = "TEXT"] = 4;
            values[valuesById[5] = "MUSIC_STANDALONE"] = 5;
            return values;
        })();

        ContextInfo.UTMInfo = (function() {

            /**
             * Properties of a UTMInfo.
             * @memberof proto.ContextInfo
             * @interface IUTMInfo
             * @property {string|null} [utmSource] UTMInfo utmSource
             * @property {string|null} [utmCampaign] UTMInfo utmCampaign
             */

            /**
             * Constructs a new UTMInfo.
             * @memberof proto.ContextInfo
             * @classdesc Represents a UTMInfo.
             * @implements IUTMInfo
             * @constructor
             * @param {proto.ContextInfo.IUTMInfo=} [properties] Properties to set
             */
            function UTMInfo(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * UTMInfo utmSource.
             * @member {string|null|undefined} utmSource
             * @memberof proto.ContextInfo.UTMInfo
             * @instance
             */
            UTMInfo.prototype.utmSource = null;

            /**
             * UTMInfo utmCampaign.
             * @member {string|null|undefined} utmCampaign
             * @memberof proto.ContextInfo.UTMInfo
             * @instance
             */
            UTMInfo.prototype.utmCampaign = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UTMInfo.prototype, "_utmSource", {
                get: $util.oneOfGetter($oneOfFields = ["utmSource"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UTMInfo.prototype, "_utmCampaign", {
                get: $util.oneOfGetter($oneOfFields = ["utmCampaign"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new UTMInfo instance using the specified properties.
             * @function create
             * @memberof proto.ContextInfo.UTMInfo
             * @static
             * @param {proto.ContextInfo.IUTMInfo=} [properties] Properties to set
             * @returns {proto.ContextInfo.UTMInfo} UTMInfo instance
             */
            UTMInfo.create = function create(properties) {
                return new UTMInfo(properties);
            };

            /**
             * Encodes the specified UTMInfo message. Does not implicitly {@link proto.ContextInfo.UTMInfo.verify|verify} messages.
             * @function encode
             * @memberof proto.ContextInfo.UTMInfo
             * @static
             * @param {proto.ContextInfo.IUTMInfo} message UTMInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UTMInfo.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.utmSource != null && Object.hasOwnProperty.call(message, "utmSource"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.utmSource);
                if (message.utmCampaign != null && Object.hasOwnProperty.call(message, "utmCampaign"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.utmCampaign);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified UTMInfo message, length delimited. Does not implicitly {@link proto.ContextInfo.UTMInfo.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.ContextInfo.UTMInfo
             * @static
             * @param {proto.ContextInfo.IUTMInfo} message UTMInfo message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UTMInfo.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a UTMInfo message from the specified reader or buffer.
             * @function decode
             * @memberof proto.ContextInfo.UTMInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.ContextInfo.UTMInfo} UTMInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UTMInfo.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.utmSource = reader.stringVerify();
                            message._utmSource = "utmSource";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.utmCampaign = reader.stringVerify();
                            message._utmCampaign = "utmCampaign";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a UTMInfo message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.ContextInfo.UTMInfo
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.ContextInfo.UTMInfo} UTMInfo
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UTMInfo.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a UTMInfo message.
             * @function verify
             * @memberof proto.ContextInfo.UTMInfo
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            UTMInfo.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.utmSource != null && Object.hasOwnProperty.call(message, "utmSource")) {
                    properties._utmSource = 1;
                    if (!$util.isString(message.utmSource))
                        return "utmSource: string expected";
                }
                if (message.utmCampaign != null && Object.hasOwnProperty.call(message, "utmCampaign")) {
                    properties._utmCampaign = 1;
                    if (!$util.isString(message.utmCampaign))
                        return "utmCampaign: string expected";
                }
                return null;
            };

            /**
             * Creates a UTMInfo message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.ContextInfo.UTMInfo
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.ContextInfo.UTMInfo} UTMInfo
             */
            UTMInfo.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.ContextInfo.UTMInfo: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.utmSource != null)
                    message.utmSource = String(object.utmSource);
                if (object.utmCampaign != null)
                    message.utmCampaign = String(object.utmCampaign);
                return message;
            };

            /**
             * Creates a plain object from a UTMInfo message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.ContextInfo.UTMInfo
             * @static
             * @param {proto.ContextInfo.UTMInfo} message UTMInfo
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            UTMInfo.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.utmSource != null && Object.hasOwnProperty.call(message, "utmSource"))
                    object.utmSource = message.utmSource;
                if (message.utmCampaign != null && Object.hasOwnProperty.call(message, "utmCampaign"))
                    object.utmCampaign = message.utmCampaign;
                return object;
            };

            /**
             * Converts this UTMInfo to JSON.
             * @function toJSON
             * @memberof proto.ContextInfo.UTMInfo
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            UTMInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for UTMInfo
             * @function getTypeUrl
             * @memberof proto.ContextInfo.UTMInfo
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            UTMInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.ContextInfo.UTMInfo";
            };

            return UTMInfo;
        })();

        return ContextInfo;
    })();

    proto.Conversation = (function() {

        /**
         * Properties of a Conversation.
         * @memberof proto
         * @interface IConversation
         * @property {string|null} [id] Conversation id
         * @property {Array.<proto.IHistorySyncMsg>|null} [messages] Conversation messages
         * @property {string|null} [newJid] Conversation newJid
         * @property {string|null} [oldJid] Conversation oldJid
         * @property {number|Long|null} [lastMsgTimestamp] Conversation lastMsgTimestamp
         * @property {number|null} [unreadCount] Conversation unreadCount
         * @property {boolean|null} [readOnly] Conversation readOnly
         * @property {boolean|null} [endOfHistoryTransfer] Conversation endOfHistoryTransfer
         * @property {number|null} [ephemeralExpiration] Conversation ephemeralExpiration
         * @property {number|Long|null} [ephemeralSettingTimestamp] Conversation ephemeralSettingTimestamp
         * @property {proto.Conversation.EndOfHistoryTransferType|null} [endOfHistoryTransferType] Conversation endOfHistoryTransferType
         * @property {number|Long|null} [conversationTimestamp] Conversation conversationTimestamp
         * @property {string|null} [name] Conversation name
         * @property {string|null} [pHash] Conversation pHash
         * @property {boolean|null} [notSpam] Conversation notSpam
         * @property {boolean|null} [archived] Conversation archived
         * @property {proto.IDisappearingMode|null} [disappearingMode] Conversation disappearingMode
         * @property {number|null} [unreadMentionCount] Conversation unreadMentionCount
         * @property {boolean|null} [markedAsUnread] Conversation markedAsUnread
         * @property {Array.<proto.IGroupParticipant>|null} [participant] Conversation participant
         * @property {Uint8Array|null} [tcToken] Conversation tcToken
         * @property {number|Long|null} [tcTokenTimestamp] Conversation tcTokenTimestamp
         * @property {Uint8Array|null} [contactPrimaryIdentityKey] Conversation contactPrimaryIdentityKey
         * @property {number|null} [pinned] Conversation pinned
         * @property {number|Long|null} [muteEndTime] Conversation muteEndTime
         * @property {proto.IWallpaperSettings|null} [wallpaper] Conversation wallpaper
         * @property {proto.MediaVisibility|null} [mediaVisibility] Conversation mediaVisibility
         * @property {number|Long|null} [tcTokenSenderTimestamp] Conversation tcTokenSenderTimestamp
         * @property {boolean|null} [suspended] Conversation suspended
         * @property {boolean|null} [terminated] Conversation terminated
         * @property {number|Long|null} [createdAt] Conversation createdAt
         * @property {string|null} [createdBy] Conversation createdBy
         * @property {string|null} [description] Conversation description
         * @property {boolean|null} [support] Conversation support
         * @property {boolean|null} [isParentGroup] Conversation isParentGroup
         * @property {string|null} [parentGroupId] Conversation parentGroupId
         * @property {boolean|null} [isDefaultSubgroup] Conversation isDefaultSubgroup
         * @property {string|null} [displayName] Conversation displayName
         * @property {string|null} [pnJid] Conversation pnJid
         * @property {boolean|null} [shareOwnPn] Conversation shareOwnPn
         * @property {boolean|null} [pnhDuplicateLidThread] Conversation pnhDuplicateLidThread
         * @property {string|null} [lidJid] Conversation lidJid
         * @property {string|null} [username] Conversation username
         * @property {string|null} [lidOriginType] Conversation lidOriginType
         * @property {number|null} [commentsCount] Conversation commentsCount
         * @property {boolean|null} [locked] Conversation locked
         * @property {proto.PrivacySystemMessage|null} [systemMessageToInsert] Conversation systemMessageToInsert
         * @property {boolean|null} [capiCreatedGroup] Conversation capiCreatedGroup
         * @property {string|null} [accountLid] Conversation accountLid
         * @property {boolean|null} [limitSharing] Conversation limitSharing
         * @property {number|Long|null} [limitSharingSettingTimestamp] Conversation limitSharingSettingTimestamp
         * @property {proto.LimitSharing.TriggerType|null} [limitSharingTrigger] Conversation limitSharingTrigger
         * @property {boolean|null} [limitSharingInitiatedByMe] Conversation limitSharingInitiatedByMe
         * @property {boolean|null} [maibaAiThreadEnabled] Conversation maibaAiThreadEnabled
         * @property {boolean|null} [isMarketingMessageThread] Conversation isMarketingMessageThread
         * @property {boolean|null} [isSenderNewAccount] Conversation isSenderNewAccount
         * @property {number|null} [afterReadDuration] Conversation afterReadDuration
         * @property {boolean|null} [isSenderSuspicious] Conversation isSenderSuspicious
         * @property {proto.Conversation.GroupAppealStatus|null} [appealStatus] Conversation appealStatus
         * @property {number|Long|null} [appealUpdateTime] Conversation appealUpdateTime
         * @property {string|null} [authAgentParentCompanyName] Conversation authAgentParentCompanyName
         * @property {string|null} [authAgentObaPhoneNumber] Conversation authAgentObaPhoneNumber
         */

        /**
         * Constructs a new Conversation.
         * @memberof proto
         * @classdesc Represents a Conversation.
         * @implements IConversation
         * @constructor
         * @param {proto.IConversation=} [properties] Properties to set
         */
        function Conversation(properties) {
            this.messages = [];
            this.participant = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Conversation id.
         * @member {string|null|undefined} id
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.id = null;

        /**
         * Conversation messages.
         * @member {Array.<proto.IHistorySyncMsg>} messages
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.messages = $util.emptyArray;

        /**
         * Conversation newJid.
         * @member {string|null|undefined} newJid
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.newJid = null;

        /**
         * Conversation oldJid.
         * @member {string|null|undefined} oldJid
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.oldJid = null;

        /**
         * Conversation lastMsgTimestamp.
         * @member {number|Long|null|undefined} lastMsgTimestamp
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.lastMsgTimestamp = null;

        /**
         * Conversation unreadCount.
         * @member {number|null|undefined} unreadCount
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.unreadCount = null;

        /**
         * Conversation readOnly.
         * @member {boolean|null|undefined} readOnly
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.readOnly = null;

        /**
         * Conversation endOfHistoryTransfer.
         * @member {boolean|null|undefined} endOfHistoryTransfer
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.endOfHistoryTransfer = null;

        /**
         * Conversation ephemeralExpiration.
         * @member {number|null|undefined} ephemeralExpiration
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.ephemeralExpiration = null;

        /**
         * Conversation ephemeralSettingTimestamp.
         * @member {number|Long|null|undefined} ephemeralSettingTimestamp
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.ephemeralSettingTimestamp = null;

        /**
         * Conversation endOfHistoryTransferType.
         * @member {proto.Conversation.EndOfHistoryTransferType|null|undefined} endOfHistoryTransferType
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.endOfHistoryTransferType = null;

        /**
         * Conversation conversationTimestamp.
         * @member {number|Long|null|undefined} conversationTimestamp
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.conversationTimestamp = null;

        /**
         * Conversation name.
         * @member {string|null|undefined} name
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.name = null;

        /**
         * Conversation pHash.
         * @member {string|null|undefined} pHash
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.pHash = null;

        /**
         * Conversation notSpam.
         * @member {boolean|null|undefined} notSpam
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.notSpam = null;

        /**
         * Conversation archived.
         * @member {boolean|null|undefined} archived
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.archived = null;

        /**
         * Conversation disappearingMode.
         * @member {proto.IDisappearingMode|null|undefined} disappearingMode
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.disappearingMode = null;

        /**
         * Conversation unreadMentionCount.
         * @member {number|null|undefined} unreadMentionCount
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.unreadMentionCount = null;

        /**
         * Conversation markedAsUnread.
         * @member {boolean|null|undefined} markedAsUnread
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.markedAsUnread = null;

        /**
         * Conversation participant.
         * @member {Array.<proto.IGroupParticipant>} participant
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.participant = $util.emptyArray;

        /**
         * Conversation tcToken.
         * @member {Uint8Array|null|undefined} tcToken
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.tcToken = null;

        /**
         * Conversation tcTokenTimestamp.
         * @member {number|Long|null|undefined} tcTokenTimestamp
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.tcTokenTimestamp = null;

        /**
         * Conversation contactPrimaryIdentityKey.
         * @member {Uint8Array|null|undefined} contactPrimaryIdentityKey
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.contactPrimaryIdentityKey = null;

        /**
         * Conversation pinned.
         * @member {number|null|undefined} pinned
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.pinned = null;

        /**
         * Conversation muteEndTime.
         * @member {number|Long|null|undefined} muteEndTime
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.muteEndTime = null;

        /**
         * Conversation wallpaper.
         * @member {proto.IWallpaperSettings|null|undefined} wallpaper
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.wallpaper = null;

        /**
         * Conversation mediaVisibility.
         * @member {proto.MediaVisibility|null|undefined} mediaVisibility
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.mediaVisibility = null;

        /**
         * Conversation tcTokenSenderTimestamp.
         * @member {number|Long|null|undefined} tcTokenSenderTimestamp
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.tcTokenSenderTimestamp = null;

        /**
         * Conversation suspended.
         * @member {boolean|null|undefined} suspended
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.suspended = null;

        /**
         * Conversation terminated.
         * @member {boolean|null|undefined} terminated
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.terminated = null;

        /**
         * Conversation createdAt.
         * @member {number|Long|null|undefined} createdAt
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.createdAt = null;

        /**
         * Conversation createdBy.
         * @member {string|null|undefined} createdBy
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.createdBy = null;

        /**
         * Conversation description.
         * @member {string|null|undefined} description
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.description = null;

        /**
         * Conversation support.
         * @member {boolean|null|undefined} support
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.support = null;

        /**
         * Conversation isParentGroup.
         * @member {boolean|null|undefined} isParentGroup
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.isParentGroup = null;

        /**
         * Conversation parentGroupId.
         * @member {string|null|undefined} parentGroupId
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.parentGroupId = null;

        /**
         * Conversation isDefaultSubgroup.
         * @member {boolean|null|undefined} isDefaultSubgroup
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.isDefaultSubgroup = null;

        /**
         * Conversation displayName.
         * @member {string|null|undefined} displayName
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.displayName = null;

        /**
         * Conversation pnJid.
         * @member {string|null|undefined} pnJid
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.pnJid = null;

        /**
         * Conversation shareOwnPn.
         * @member {boolean|null|undefined} shareOwnPn
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.shareOwnPn = null;

        /**
         * Conversation pnhDuplicateLidThread.
         * @member {boolean|null|undefined} pnhDuplicateLidThread
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.pnhDuplicateLidThread = null;

        /**
         * Conversation lidJid.
         * @member {string|null|undefined} lidJid
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.lidJid = null;

        /**
         * Conversation username.
         * @member {string|null|undefined} username
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.username = null;

        /**
         * Conversation lidOriginType.
         * @member {string|null|undefined} lidOriginType
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.lidOriginType = null;

        /**
         * Conversation commentsCount.
         * @member {number|null|undefined} commentsCount
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.commentsCount = null;

        /**
         * Conversation locked.
         * @member {boolean|null|undefined} locked
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.locked = null;

        /**
         * Conversation systemMessageToInsert.
         * @member {proto.PrivacySystemMessage|null|undefined} systemMessageToInsert
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.systemMessageToInsert = null;

        /**
         * Conversation capiCreatedGroup.
         * @member {boolean|null|undefined} capiCreatedGroup
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.capiCreatedGroup = null;

        /**
         * Conversation accountLid.
         * @member {string|null|undefined} accountLid
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.accountLid = null;

        /**
         * Conversation limitSharing.
         * @member {boolean|null|undefined} limitSharing
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.limitSharing = null;

        /**
         * Conversation limitSharingSettingTimestamp.
         * @member {number|Long|null|undefined} limitSharingSettingTimestamp
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.limitSharingSettingTimestamp = null;

        /**
         * Conversation limitSharingTrigger.
         * @member {proto.LimitSharing.TriggerType|null|undefined} limitSharingTrigger
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.limitSharingTrigger = null;

        /**
         * Conversation limitSharingInitiatedByMe.
         * @member {boolean|null|undefined} limitSharingInitiatedByMe
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.limitSharingInitiatedByMe = null;

        /**
         * Conversation maibaAiThreadEnabled.
         * @member {boolean|null|undefined} maibaAiThreadEnabled
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.maibaAiThreadEnabled = null;

        /**
         * Conversation isMarketingMessageThread.
         * @member {boolean|null|undefined} isMarketingMessageThread
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.isMarketingMessageThread = null;

        /**
         * Conversation isSenderNewAccount.
         * @member {boolean|null|undefined} isSenderNewAccount
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.isSenderNewAccount = null;

        /**
         * Conversation afterReadDuration.
         * @member {number|null|undefined} afterReadDuration
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.afterReadDuration = null;

        /**
         * Conversation isSenderSuspicious.
         * @member {boolean|null|undefined} isSenderSuspicious
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.isSenderSuspicious = null;

        /**
         * Conversation appealStatus.
         * @member {proto.Conversation.GroupAppealStatus|null|undefined} appealStatus
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.appealStatus = null;

        /**
         * Conversation appealUpdateTime.
         * @member {number|Long|null|undefined} appealUpdateTime
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.appealUpdateTime = null;

        /**
         * Conversation authAgentParentCompanyName.
         * @member {string|null|undefined} authAgentParentCompanyName
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.authAgentParentCompanyName = null;

        /**
         * Conversation authAgentObaPhoneNumber.
         * @member {string|null|undefined} authAgentObaPhoneNumber
         * @memberof proto.Conversation
         * @instance
         */
        Conversation.prototype.authAgentObaPhoneNumber = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_id", {
            get: $util.oneOfGetter($oneOfFields = ["id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_newJid", {
            get: $util.oneOfGetter($oneOfFields = ["newJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_oldJid", {
            get: $util.oneOfGetter($oneOfFields = ["oldJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_lastMsgTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["lastMsgTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_unreadCount", {
            get: $util.oneOfGetter($oneOfFields = ["unreadCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_readOnly", {
            get: $util.oneOfGetter($oneOfFields = ["readOnly"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_endOfHistoryTransfer", {
            get: $util.oneOfGetter($oneOfFields = ["endOfHistoryTransfer"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_ephemeralExpiration", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralExpiration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_ephemeralSettingTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralSettingTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_endOfHistoryTransferType", {
            get: $util.oneOfGetter($oneOfFields = ["endOfHistoryTransferType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_conversationTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["conversationTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_name", {
            get: $util.oneOfGetter($oneOfFields = ["name"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_pHash", {
            get: $util.oneOfGetter($oneOfFields = ["pHash"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_notSpam", {
            get: $util.oneOfGetter($oneOfFields = ["notSpam"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_archived", {
            get: $util.oneOfGetter($oneOfFields = ["archived"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_disappearingMode", {
            get: $util.oneOfGetter($oneOfFields = ["disappearingMode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_unreadMentionCount", {
            get: $util.oneOfGetter($oneOfFields = ["unreadMentionCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_markedAsUnread", {
            get: $util.oneOfGetter($oneOfFields = ["markedAsUnread"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_tcToken", {
            get: $util.oneOfGetter($oneOfFields = ["tcToken"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_tcTokenTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["tcTokenTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_contactPrimaryIdentityKey", {
            get: $util.oneOfGetter($oneOfFields = ["contactPrimaryIdentityKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_pinned", {
            get: $util.oneOfGetter($oneOfFields = ["pinned"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_muteEndTime", {
            get: $util.oneOfGetter($oneOfFields = ["muteEndTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_wallpaper", {
            get: $util.oneOfGetter($oneOfFields = ["wallpaper"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_mediaVisibility", {
            get: $util.oneOfGetter($oneOfFields = ["mediaVisibility"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_tcTokenSenderTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["tcTokenSenderTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_suspended", {
            get: $util.oneOfGetter($oneOfFields = ["suspended"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_terminated", {
            get: $util.oneOfGetter($oneOfFields = ["terminated"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_createdAt", {
            get: $util.oneOfGetter($oneOfFields = ["createdAt"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_createdBy", {
            get: $util.oneOfGetter($oneOfFields = ["createdBy"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_description", {
            get: $util.oneOfGetter($oneOfFields = ["description"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_support", {
            get: $util.oneOfGetter($oneOfFields = ["support"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_isParentGroup", {
            get: $util.oneOfGetter($oneOfFields = ["isParentGroup"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_parentGroupId", {
            get: $util.oneOfGetter($oneOfFields = ["parentGroupId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_isDefaultSubgroup", {
            get: $util.oneOfGetter($oneOfFields = ["isDefaultSubgroup"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_displayName", {
            get: $util.oneOfGetter($oneOfFields = ["displayName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_pnJid", {
            get: $util.oneOfGetter($oneOfFields = ["pnJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_shareOwnPn", {
            get: $util.oneOfGetter($oneOfFields = ["shareOwnPn"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_pnhDuplicateLidThread", {
            get: $util.oneOfGetter($oneOfFields = ["pnhDuplicateLidThread"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_lidJid", {
            get: $util.oneOfGetter($oneOfFields = ["lidJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_username", {
            get: $util.oneOfGetter($oneOfFields = ["username"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_lidOriginType", {
            get: $util.oneOfGetter($oneOfFields = ["lidOriginType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_commentsCount", {
            get: $util.oneOfGetter($oneOfFields = ["commentsCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_locked", {
            get: $util.oneOfGetter($oneOfFields = ["locked"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_systemMessageToInsert", {
            get: $util.oneOfGetter($oneOfFields = ["systemMessageToInsert"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_capiCreatedGroup", {
            get: $util.oneOfGetter($oneOfFields = ["capiCreatedGroup"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_accountLid", {
            get: $util.oneOfGetter($oneOfFields = ["accountLid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_limitSharing", {
            get: $util.oneOfGetter($oneOfFields = ["limitSharing"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_limitSharingSettingTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["limitSharingSettingTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_limitSharingTrigger", {
            get: $util.oneOfGetter($oneOfFields = ["limitSharingTrigger"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_limitSharingInitiatedByMe", {
            get: $util.oneOfGetter($oneOfFields = ["limitSharingInitiatedByMe"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_maibaAiThreadEnabled", {
            get: $util.oneOfGetter($oneOfFields = ["maibaAiThreadEnabled"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_isMarketingMessageThread", {
            get: $util.oneOfGetter($oneOfFields = ["isMarketingMessageThread"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_isSenderNewAccount", {
            get: $util.oneOfGetter($oneOfFields = ["isSenderNewAccount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_afterReadDuration", {
            get: $util.oneOfGetter($oneOfFields = ["afterReadDuration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_isSenderSuspicious", {
            get: $util.oneOfGetter($oneOfFields = ["isSenderSuspicious"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_appealStatus", {
            get: $util.oneOfGetter($oneOfFields = ["appealStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_appealUpdateTime", {
            get: $util.oneOfGetter($oneOfFields = ["appealUpdateTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_authAgentParentCompanyName", {
            get: $util.oneOfGetter($oneOfFields = ["authAgentParentCompanyName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Conversation.prototype, "_authAgentObaPhoneNumber", {
            get: $util.oneOfGetter($oneOfFields = ["authAgentObaPhoneNumber"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new Conversation instance using the specified properties.
         * @function create
         * @memberof proto.Conversation
         * @static
         * @param {proto.IConversation=} [properties] Properties to set
         * @returns {proto.Conversation} Conversation instance
         */
        Conversation.create = function create(properties) {
            return new Conversation(properties);
        };

        /**
         * Encodes the specified Conversation message. Does not implicitly {@link proto.Conversation.verify|verify} messages.
         * @function encode
         * @memberof proto.Conversation
         * @static
         * @param {proto.IConversation} message Conversation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Conversation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.messages != null && message.messages.length)
                for (var i = 0; i < message.messages.length; ++i)
                    $root.proto.HistorySyncMsg.encode(message.messages[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.newJid != null && Object.hasOwnProperty.call(message, "newJid"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.newJid);
            if (message.oldJid != null && Object.hasOwnProperty.call(message, "oldJid"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.oldJid);
            if (message.lastMsgTimestamp != null && Object.hasOwnProperty.call(message, "lastMsgTimestamp"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.lastMsgTimestamp);
            if (message.unreadCount != null && Object.hasOwnProperty.call(message, "unreadCount"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.unreadCount);
            if (message.readOnly != null && Object.hasOwnProperty.call(message, "readOnly"))
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.readOnly);
            if (message.endOfHistoryTransfer != null && Object.hasOwnProperty.call(message, "endOfHistoryTransfer"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.endOfHistoryTransfer);
            if (message.ephemeralExpiration != null && Object.hasOwnProperty.call(message, "ephemeralExpiration"))
                writer.uint32(/* id 9, wireType 0 =*/72).uint32(message.ephemeralExpiration);
            if (message.ephemeralSettingTimestamp != null && Object.hasOwnProperty.call(message, "ephemeralSettingTimestamp"))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.ephemeralSettingTimestamp);
            if (message.endOfHistoryTransferType != null && Object.hasOwnProperty.call(message, "endOfHistoryTransferType"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.endOfHistoryTransferType);
            if (message.conversationTimestamp != null && Object.hasOwnProperty.call(message, "conversationTimestamp"))
                writer.uint32(/* id 12, wireType 0 =*/96).uint64(message.conversationTimestamp);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.name);
            if (message.pHash != null && Object.hasOwnProperty.call(message, "pHash"))
                writer.uint32(/* id 14, wireType 2 =*/114).string(message.pHash);
            if (message.notSpam != null && Object.hasOwnProperty.call(message, "notSpam"))
                writer.uint32(/* id 15, wireType 0 =*/120).bool(message.notSpam);
            if (message.archived != null && Object.hasOwnProperty.call(message, "archived"))
                writer.uint32(/* id 16, wireType 0 =*/128).bool(message.archived);
            if (message.disappearingMode != null && Object.hasOwnProperty.call(message, "disappearingMode"))
                $root.proto.DisappearingMode.encode(message.disappearingMode, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
            if (message.unreadMentionCount != null && Object.hasOwnProperty.call(message, "unreadMentionCount"))
                writer.uint32(/* id 18, wireType 0 =*/144).uint32(message.unreadMentionCount);
            if (message.markedAsUnread != null && Object.hasOwnProperty.call(message, "markedAsUnread"))
                writer.uint32(/* id 19, wireType 0 =*/152).bool(message.markedAsUnread);
            if (message.participant != null && message.participant.length)
                for (var i = 0; i < message.participant.length; ++i)
                    $root.proto.GroupParticipant.encode(message.participant[i], writer.uint32(/* id 20, wireType 2 =*/162).fork(), q + 1).ldelim();
            if (message.tcToken != null && Object.hasOwnProperty.call(message, "tcToken"))
                writer.uint32(/* id 21, wireType 2 =*/170).bytes(message.tcToken);
            if (message.tcTokenTimestamp != null && Object.hasOwnProperty.call(message, "tcTokenTimestamp"))
                writer.uint32(/* id 22, wireType 0 =*/176).uint64(message.tcTokenTimestamp);
            if (message.contactPrimaryIdentityKey != null && Object.hasOwnProperty.call(message, "contactPrimaryIdentityKey"))
                writer.uint32(/* id 23, wireType 2 =*/186).bytes(message.contactPrimaryIdentityKey);
            if (message.pinned != null && Object.hasOwnProperty.call(message, "pinned"))
                writer.uint32(/* id 24, wireType 0 =*/192).uint32(message.pinned);
            if (message.muteEndTime != null && Object.hasOwnProperty.call(message, "muteEndTime"))
                writer.uint32(/* id 25, wireType 0 =*/200).uint64(message.muteEndTime);
            if (message.wallpaper != null && Object.hasOwnProperty.call(message, "wallpaper"))
                $root.proto.WallpaperSettings.encode(message.wallpaper, writer.uint32(/* id 26, wireType 2 =*/210).fork(), q + 1).ldelim();
            if (message.mediaVisibility != null && Object.hasOwnProperty.call(message, "mediaVisibility"))
                writer.uint32(/* id 27, wireType 0 =*/216).int32(message.mediaVisibility);
            if (message.tcTokenSenderTimestamp != null && Object.hasOwnProperty.call(message, "tcTokenSenderTimestamp"))
                writer.uint32(/* id 28, wireType 0 =*/224).uint64(message.tcTokenSenderTimestamp);
            if (message.suspended != null && Object.hasOwnProperty.call(message, "suspended"))
                writer.uint32(/* id 29, wireType 0 =*/232).bool(message.suspended);
            if (message.terminated != null && Object.hasOwnProperty.call(message, "terminated"))
                writer.uint32(/* id 30, wireType 0 =*/240).bool(message.terminated);
            if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                writer.uint32(/* id 31, wireType 0 =*/248).uint64(message.createdAt);
            if (message.createdBy != null && Object.hasOwnProperty.call(message, "createdBy"))
                writer.uint32(/* id 32, wireType 2 =*/258).string(message.createdBy);
            if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                writer.uint32(/* id 33, wireType 2 =*/266).string(message.description);
            if (message.support != null && Object.hasOwnProperty.call(message, "support"))
                writer.uint32(/* id 34, wireType 0 =*/272).bool(message.support);
            if (message.isParentGroup != null && Object.hasOwnProperty.call(message, "isParentGroup"))
                writer.uint32(/* id 35, wireType 0 =*/280).bool(message.isParentGroup);
            if (message.isDefaultSubgroup != null && Object.hasOwnProperty.call(message, "isDefaultSubgroup"))
                writer.uint32(/* id 36, wireType 0 =*/288).bool(message.isDefaultSubgroup);
            if (message.parentGroupId != null && Object.hasOwnProperty.call(message, "parentGroupId"))
                writer.uint32(/* id 37, wireType 2 =*/298).string(message.parentGroupId);
            if (message.displayName != null && Object.hasOwnProperty.call(message, "displayName"))
                writer.uint32(/* id 38, wireType 2 =*/306).string(message.displayName);
            if (message.pnJid != null && Object.hasOwnProperty.call(message, "pnJid"))
                writer.uint32(/* id 39, wireType 2 =*/314).string(message.pnJid);
            if (message.shareOwnPn != null && Object.hasOwnProperty.call(message, "shareOwnPn"))
                writer.uint32(/* id 40, wireType 0 =*/320).bool(message.shareOwnPn);
            if (message.pnhDuplicateLidThread != null && Object.hasOwnProperty.call(message, "pnhDuplicateLidThread"))
                writer.uint32(/* id 41, wireType 0 =*/328).bool(message.pnhDuplicateLidThread);
            if (message.lidJid != null && Object.hasOwnProperty.call(message, "lidJid"))
                writer.uint32(/* id 42, wireType 2 =*/338).string(message.lidJid);
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 43, wireType 2 =*/346).string(message.username);
            if (message.lidOriginType != null && Object.hasOwnProperty.call(message, "lidOriginType"))
                writer.uint32(/* id 44, wireType 2 =*/354).string(message.lidOriginType);
            if (message.commentsCount != null && Object.hasOwnProperty.call(message, "commentsCount"))
                writer.uint32(/* id 45, wireType 0 =*/360).uint32(message.commentsCount);
            if (message.locked != null && Object.hasOwnProperty.call(message, "locked"))
                writer.uint32(/* id 46, wireType 0 =*/368).bool(message.locked);
            if (message.systemMessageToInsert != null && Object.hasOwnProperty.call(message, "systemMessageToInsert"))
                writer.uint32(/* id 47, wireType 0 =*/376).int32(message.systemMessageToInsert);
            if (message.capiCreatedGroup != null && Object.hasOwnProperty.call(message, "capiCreatedGroup"))
                writer.uint32(/* id 48, wireType 0 =*/384).bool(message.capiCreatedGroup);
            if (message.accountLid != null && Object.hasOwnProperty.call(message, "accountLid"))
                writer.uint32(/* id 49, wireType 2 =*/394).string(message.accountLid);
            if (message.limitSharing != null && Object.hasOwnProperty.call(message, "limitSharing"))
                writer.uint32(/* id 50, wireType 0 =*/400).bool(message.limitSharing);
            if (message.limitSharingSettingTimestamp != null && Object.hasOwnProperty.call(message, "limitSharingSettingTimestamp"))
                writer.uint32(/* id 51, wireType 0 =*/408).int64(message.limitSharingSettingTimestamp);
            if (message.limitSharingTrigger != null && Object.hasOwnProperty.call(message, "limitSharingTrigger"))
                writer.uint32(/* id 52, wireType 0 =*/416).int32(message.limitSharingTrigger);
            if (message.limitSharingInitiatedByMe != null && Object.hasOwnProperty.call(message, "limitSharingInitiatedByMe"))
                writer.uint32(/* id 53, wireType 0 =*/424).bool(message.limitSharingInitiatedByMe);
            if (message.maibaAiThreadEnabled != null && Object.hasOwnProperty.call(message, "maibaAiThreadEnabled"))
                writer.uint32(/* id 54, wireType 0 =*/432).bool(message.maibaAiThreadEnabled);
            if (message.isMarketingMessageThread != null && Object.hasOwnProperty.call(message, "isMarketingMessageThread"))
                writer.uint32(/* id 55, wireType 0 =*/440).bool(message.isMarketingMessageThread);
            if (message.isSenderNewAccount != null && Object.hasOwnProperty.call(message, "isSenderNewAccount"))
                writer.uint32(/* id 56, wireType 0 =*/448).bool(message.isSenderNewAccount);
            if (message.afterReadDuration != null && Object.hasOwnProperty.call(message, "afterReadDuration"))
                writer.uint32(/* id 57, wireType 0 =*/456).uint32(message.afterReadDuration);
            if (message.isSenderSuspicious != null && Object.hasOwnProperty.call(message, "isSenderSuspicious"))
                writer.uint32(/* id 58, wireType 0 =*/464).bool(message.isSenderSuspicious);
            if (message.appealStatus != null && Object.hasOwnProperty.call(message, "appealStatus"))
                writer.uint32(/* id 59, wireType 0 =*/472).int32(message.appealStatus);
            if (message.appealUpdateTime != null && Object.hasOwnProperty.call(message, "appealUpdateTime"))
                writer.uint32(/* id 60, wireType 0 =*/480).uint64(message.appealUpdateTime);
            if (message.authAgentParentCompanyName != null && Object.hasOwnProperty.call(message, "authAgentParentCompanyName"))
                writer.uint32(/* id 61, wireType 2 =*/490).string(message.authAgentParentCompanyName);
            if (message.authAgentObaPhoneNumber != null && Object.hasOwnProperty.call(message, "authAgentObaPhoneNumber"))
                writer.uint32(/* id 62, wireType 2 =*/498).string(message.authAgentObaPhoneNumber);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Conversation message, length delimited. Does not implicitly {@link proto.Conversation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Conversation
         * @static
         * @param {proto.IConversation} message Conversation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Conversation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Conversation message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Conversation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Conversation} Conversation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Conversation.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.id = reader.stringVerify();
                        message._id = "id";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(message.messages && message.messages.length))
                            message.messages = [];
                        message.messages.push($root.proto.HistorySyncMsg.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.newJid = reader.stringVerify();
                        message._newJid = "newJid";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.oldJid = reader.stringVerify();
                        message._oldJid = "oldJid";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.lastMsgTimestamp = reader.uint64();
                        message._lastMsgTimestamp = "lastMsgTimestamp";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        message.unreadCount = reader.uint32();
                        message._unreadCount = "unreadCount";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        message.readOnly = reader.bool();
                        message._readOnly = "readOnly";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        message.endOfHistoryTransfer = reader.bool();
                        message._endOfHistoryTransfer = "endOfHistoryTransfer";
                        continue;
                    }
                case 9: {
                        if (u !== 0)
                            break;
                        message.ephemeralExpiration = reader.uint32();
                        message._ephemeralExpiration = "ephemeralExpiration";
                        continue;
                    }
                case 10: {
                        if (u !== 0)
                            break;
                        message.ephemeralSettingTimestamp = reader.int64();
                        message._ephemeralSettingTimestamp = "ephemeralSettingTimestamp";
                        continue;
                    }
                case 11: {
                        if (u !== 0)
                            break;
                        message.endOfHistoryTransferType = reader.int32();
                        message._endOfHistoryTransferType = "endOfHistoryTransferType";
                        continue;
                    }
                case 12: {
                        if (u !== 0)
                            break;
                        message.conversationTimestamp = reader.uint64();
                        message._conversationTimestamp = "conversationTimestamp";
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        message.name = reader.stringVerify();
                        message._name = "name";
                        continue;
                    }
                case 14: {
                        if (u !== 2)
                            break;
                        message.pHash = reader.stringVerify();
                        message._pHash = "pHash";
                        continue;
                    }
                case 15: {
                        if (u !== 0)
                            break;
                        message.notSpam = reader.bool();
                        message._notSpam = "notSpam";
                        continue;
                    }
                case 16: {
                        if (u !== 0)
                            break;
                        message.archived = reader.bool();
                        message._archived = "archived";
                        continue;
                    }
                case 17: {
                        if (u !== 2)
                            break;
                        message.disappearingMode = $root.proto.DisappearingMode.decode(reader, reader.uint32(), undefined, q + 1, message.disappearingMode);
                        message._disappearingMode = "disappearingMode";
                        continue;
                    }
                case 18: {
                        if (u !== 0)
                            break;
                        message.unreadMentionCount = reader.uint32();
                        message._unreadMentionCount = "unreadMentionCount";
                        continue;
                    }
                case 19: {
                        if (u !== 0)
                            break;
                        message.markedAsUnread = reader.bool();
                        message._markedAsUnread = "markedAsUnread";
                        continue;
                    }
                case 20: {
                        if (u !== 2)
                            break;
                        if (!(message.participant && message.participant.length))
                            message.participant = [];
                        message.participant.push($root.proto.GroupParticipant.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 21: {
                        if (u !== 2)
                            break;
                        message.tcToken = reader.bytes();
                        message._tcToken = "tcToken";
                        continue;
                    }
                case 22: {
                        if (u !== 0)
                            break;
                        message.tcTokenTimestamp = reader.uint64();
                        message._tcTokenTimestamp = "tcTokenTimestamp";
                        continue;
                    }
                case 23: {
                        if (u !== 2)
                            break;
                        message.contactPrimaryIdentityKey = reader.bytes();
                        message._contactPrimaryIdentityKey = "contactPrimaryIdentityKey";
                        continue;
                    }
                case 24: {
                        if (u !== 0)
                            break;
                        message.pinned = reader.uint32();
                        message._pinned = "pinned";
                        continue;
                    }
                case 25: {
                        if (u !== 0)
                            break;
                        message.muteEndTime = reader.uint64();
                        message._muteEndTime = "muteEndTime";
                        continue;
                    }
                case 26: {
                        if (u !== 2)
                            break;
                        message.wallpaper = $root.proto.WallpaperSettings.decode(reader, reader.uint32(), undefined, q + 1, message.wallpaper);
                        message._wallpaper = "wallpaper";
                        continue;
                    }
                case 27: {
                        if (u !== 0)
                            break;
                        message.mediaVisibility = reader.int32();
                        message._mediaVisibility = "mediaVisibility";
                        continue;
                    }
                case 28: {
                        if (u !== 0)
                            break;
                        message.tcTokenSenderTimestamp = reader.uint64();
                        message._tcTokenSenderTimestamp = "tcTokenSenderTimestamp";
                        continue;
                    }
                case 29: {
                        if (u !== 0)
                            break;
                        message.suspended = reader.bool();
                        message._suspended = "suspended";
                        continue;
                    }
                case 30: {
                        if (u !== 0)
                            break;
                        message.terminated = reader.bool();
                        message._terminated = "terminated";
                        continue;
                    }
                case 31: {
                        if (u !== 0)
                            break;
                        message.createdAt = reader.uint64();
                        message._createdAt = "createdAt";
                        continue;
                    }
                case 32: {
                        if (u !== 2)
                            break;
                        message.createdBy = reader.stringVerify();
                        message._createdBy = "createdBy";
                        continue;
                    }
                case 33: {
                        if (u !== 2)
                            break;
                        message.description = reader.stringVerify();
                        message._description = "description";
                        continue;
                    }
                case 34: {
                        if (u !== 0)
                            break;
                        message.support = reader.bool();
                        message._support = "support";
                        continue;
                    }
                case 35: {
                        if (u !== 0)
                            break;
                        message.isParentGroup = reader.bool();
                        message._isParentGroup = "isParentGroup";
                        continue;
                    }
                case 37: {
                        if (u !== 2)
                            break;
                        message.parentGroupId = reader.stringVerify();
                        message._parentGroupId = "parentGroupId";
                        continue;
                    }
                case 36: {
                        if (u !== 0)
                            break;
                        message.isDefaultSubgroup = reader.bool();
                        message._isDefaultSubgroup = "isDefaultSubgroup";
                        continue;
                    }
                case 38: {
                        if (u !== 2)
                            break;
                        message.displayName = reader.stringVerify();
                        message._displayName = "displayName";
                        continue;
                    }
                case 39: {
                        if (u !== 2)
                            break;
                        message.pnJid = reader.stringVerify();
                        message._pnJid = "pnJid";
                        continue;
                    }
                case 40: {
                        if (u !== 0)
                            break;
                        message.shareOwnPn = reader.bool();
                        message._shareOwnPn = "shareOwnPn";
                        continue;
                    }
                case 41: {
                        if (u !== 0)
                            break;
                        message.pnhDuplicateLidThread = reader.bool();
                        message._pnhDuplicateLidThread = "pnhDuplicateLidThread";
                        continue;
                    }
                case 42: {
                        if (u !== 2)
                            break;
                        message.lidJid = reader.stringVerify();
                        message._lidJid = "lidJid";
                        continue;
                    }
                case 43: {
                        if (u !== 2)
                            break;
                        message.username = reader.stringVerify();
                        message._username = "username";
                        continue;
                    }
                case 44: {
                        if (u !== 2)
                            break;
                        message.lidOriginType = reader.stringVerify();
                        message._lidOriginType = "lidOriginType";
                        continue;
                    }
                case 45: {
                        if (u !== 0)
                            break;
                        message.commentsCount = reader.uint32();
                        message._commentsCount = "commentsCount";
                        continue;
                    }
                case 46: {
                        if (u !== 0)
                            break;
                        message.locked = reader.bool();
                        message._locked = "locked";
                        continue;
                    }
                case 47: {
                        if (u !== 0)
                            break;
                        message.systemMessageToInsert = reader.int32();
                        message._systemMessageToInsert = "systemMessageToInsert";
                        continue;
                    }
                case 48: {
                        if (u !== 0)
                            break;
                        message.capiCreatedGroup = reader.bool();
                        message._capiCreatedGroup = "capiCreatedGroup";
                        continue;
                    }
                case 49: {
                        if (u !== 2)
                            break;
                        message.accountLid = reader.stringVerify();
                        message._accountLid = "accountLid";
                        continue;
                    }
                case 50: {
                        if (u !== 0)
                            break;
                        message.limitSharing = reader.bool();
                        message._limitSharing = "limitSharing";
                        continue;
                    }
                case 51: {
                        if (u !== 0)
                            break;
                        message.limitSharingSettingTimestamp = reader.int64();
                        message._limitSharingSettingTimestamp = "limitSharingSettingTimestamp";
                        continue;
                    }
                case 52: {
                        if (u !== 0)
                            break;
                        message.limitSharingTrigger = reader.int32();
                        message._limitSharingTrigger = "limitSharingTrigger";
                        continue;
                    }
                case 53: {
                        if (u !== 0)
                            break;
                        message.limitSharingInitiatedByMe = reader.bool();
                        message._limitSharingInitiatedByMe = "limitSharingInitiatedByMe";
                        continue;
                    }
                case 54: {
                        if (u !== 0)
                            break;
                        message.maibaAiThreadEnabled = reader.bool();
                        message._maibaAiThreadEnabled = "maibaAiThreadEnabled";
                        continue;
                    }
                case 55: {
                        if (u !== 0)
                            break;
                        message.isMarketingMessageThread = reader.bool();
                        message._isMarketingMessageThread = "isMarketingMessageThread";
                        continue;
                    }
                case 56: {
                        if (u !== 0)
                            break;
                        message.isSenderNewAccount = reader.bool();
                        message._isSenderNewAccount = "isSenderNewAccount";
                        continue;
                    }
                case 57: {
                        if (u !== 0)
                            break;
                        message.afterReadDuration = reader.uint32();
                        message._afterReadDuration = "afterReadDuration";
                        continue;
                    }
                case 58: {
                        if (u !== 0)
                            break;
                        message.isSenderSuspicious = reader.bool();
                        message._isSenderSuspicious = "isSenderSuspicious";
                        continue;
                    }
                case 59: {
                        if (u !== 0)
                            break;
                        message.appealStatus = reader.int32();
                        message._appealStatus = "appealStatus";
                        continue;
                    }
                case 60: {
                        if (u !== 0)
                            break;
                        message.appealUpdateTime = reader.uint64();
                        message._appealUpdateTime = "appealUpdateTime";
                        continue;
                    }
                case 61: {
                        if (u !== 2)
                            break;
                        message.authAgentParentCompanyName = reader.stringVerify();
                        message._authAgentParentCompanyName = "authAgentParentCompanyName";
                        continue;
                    }
                case 62: {
                        if (u !== 2)
                            break;
                        message.authAgentObaPhoneNumber = reader.stringVerify();
                        message._authAgentObaPhoneNumber = "authAgentObaPhoneNumber";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a Conversation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Conversation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Conversation} Conversation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Conversation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Conversation message.
         * @function verify
         * @memberof proto.Conversation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Conversation.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.id != null && Object.hasOwnProperty.call(message, "id")) {
                properties._id = 1;
                if (!$util.isString(message.id))
                    return "id: string expected";
            }
            if (message.messages != null && Object.hasOwnProperty.call(message, "messages")) {
                if (!Array.isArray(message.messages))
                    return "messages: array expected";
                for (var i = 0; i < message.messages.length; ++i) {
                    var error = $root.proto.HistorySyncMsg.verify(message.messages[i], q + 1);
                    if (error)
                        return "messages." + error;
                }
            }
            if (message.newJid != null && Object.hasOwnProperty.call(message, "newJid")) {
                properties._newJid = 1;
                if (!$util.isString(message.newJid))
                    return "newJid: string expected";
            }
            if (message.oldJid != null && Object.hasOwnProperty.call(message, "oldJid")) {
                properties._oldJid = 1;
                if (!$util.isString(message.oldJid))
                    return "oldJid: string expected";
            }
            if (message.lastMsgTimestamp != null && Object.hasOwnProperty.call(message, "lastMsgTimestamp")) {
                properties._lastMsgTimestamp = 1;
                if (!$util.isInteger(message.lastMsgTimestamp) && !(message.lastMsgTimestamp && $util.isInteger(message.lastMsgTimestamp.low) && $util.isInteger(message.lastMsgTimestamp.high)))
                    return "lastMsgTimestamp: integer|Long expected";
            }
            if (message.unreadCount != null && Object.hasOwnProperty.call(message, "unreadCount")) {
                properties._unreadCount = 1;
                if (!$util.isInteger(message.unreadCount))
                    return "unreadCount: integer expected";
            }
            if (message.readOnly != null && Object.hasOwnProperty.call(message, "readOnly")) {
                properties._readOnly = 1;
                if (typeof message.readOnly !== "boolean")
                    return "readOnly: boolean expected";
            }
            if (message.endOfHistoryTransfer != null && Object.hasOwnProperty.call(message, "endOfHistoryTransfer")) {
                properties._endOfHistoryTransfer = 1;
                if (typeof message.endOfHistoryTransfer !== "boolean")
                    return "endOfHistoryTransfer: boolean expected";
            }
            if (message.ephemeralExpiration != null && Object.hasOwnProperty.call(message, "ephemeralExpiration")) {
                properties._ephemeralExpiration = 1;
                if (!$util.isInteger(message.ephemeralExpiration))
                    return "ephemeralExpiration: integer expected";
            }
            if (message.ephemeralSettingTimestamp != null && Object.hasOwnProperty.call(message, "ephemeralSettingTimestamp")) {
                properties._ephemeralSettingTimestamp = 1;
                if (!$util.isInteger(message.ephemeralSettingTimestamp) && !(message.ephemeralSettingTimestamp && $util.isInteger(message.ephemeralSettingTimestamp.low) && $util.isInteger(message.ephemeralSettingTimestamp.high)))
                    return "ephemeralSettingTimestamp: integer|Long expected";
            }
            if (message.endOfHistoryTransferType != null && Object.hasOwnProperty.call(message, "endOfHistoryTransferType")) {
                properties._endOfHistoryTransferType = 1;
                if (typeof message.endOfHistoryTransferType !== "number" || (message.endOfHistoryTransferType | 0) !== message.endOfHistoryTransferType)
                    return "endOfHistoryTransferType: enum value expected";
            }
            if (message.conversationTimestamp != null && Object.hasOwnProperty.call(message, "conversationTimestamp")) {
                properties._conversationTimestamp = 1;
                if (!$util.isInteger(message.conversationTimestamp) && !(message.conversationTimestamp && $util.isInteger(message.conversationTimestamp.low) && $util.isInteger(message.conversationTimestamp.high)))
                    return "conversationTimestamp: integer|Long expected";
            }
            if (message.name != null && Object.hasOwnProperty.call(message, "name")) {
                properties._name = 1;
                if (!$util.isString(message.name))
                    return "name: string expected";
            }
            if (message.pHash != null && Object.hasOwnProperty.call(message, "pHash")) {
                properties._pHash = 1;
                if (!$util.isString(message.pHash))
                    return "pHash: string expected";
            }
            if (message.notSpam != null && Object.hasOwnProperty.call(message, "notSpam")) {
                properties._notSpam = 1;
                if (typeof message.notSpam !== "boolean")
                    return "notSpam: boolean expected";
            }
            if (message.archived != null && Object.hasOwnProperty.call(message, "archived")) {
                properties._archived = 1;
                if (typeof message.archived !== "boolean")
                    return "archived: boolean expected";
            }
            if (message.disappearingMode != null && Object.hasOwnProperty.call(message, "disappearingMode")) {
                properties._disappearingMode = 1;
                {
                    var error = $root.proto.DisappearingMode.verify(message.disappearingMode, q + 1);
                    if (error)
                        return "disappearingMode." + error;
                }
            }
            if (message.unreadMentionCount != null && Object.hasOwnProperty.call(message, "unreadMentionCount")) {
                properties._unreadMentionCount = 1;
                if (!$util.isInteger(message.unreadMentionCount))
                    return "unreadMentionCount: integer expected";
            }
            if (message.markedAsUnread != null && Object.hasOwnProperty.call(message, "markedAsUnread")) {
                properties._markedAsUnread = 1;
                if (typeof message.markedAsUnread !== "boolean")
                    return "markedAsUnread: boolean expected";
            }
            if (message.participant != null && Object.hasOwnProperty.call(message, "participant")) {
                if (!Array.isArray(message.participant))
                    return "participant: array expected";
                for (var i = 0; i < message.participant.length; ++i) {
                    var error = $root.proto.GroupParticipant.verify(message.participant[i], q + 1);
                    if (error)
                        return "participant." + error;
                }
            }
            if (message.tcToken != null && Object.hasOwnProperty.call(message, "tcToken")) {
                properties._tcToken = 1;
                if (!(message.tcToken && typeof message.tcToken.length === "number" || $util.isString(message.tcToken)))
                    return "tcToken: buffer expected";
            }
            if (message.tcTokenTimestamp != null && Object.hasOwnProperty.call(message, "tcTokenTimestamp")) {
                properties._tcTokenTimestamp = 1;
                if (!$util.isInteger(message.tcTokenTimestamp) && !(message.tcTokenTimestamp && $util.isInteger(message.tcTokenTimestamp.low) && $util.isInteger(message.tcTokenTimestamp.high)))
                    return "tcTokenTimestamp: integer|Long expected";
            }
            if (message.contactPrimaryIdentityKey != null && Object.hasOwnProperty.call(message, "contactPrimaryIdentityKey")) {
                properties._contactPrimaryIdentityKey = 1;
                if (!(message.contactPrimaryIdentityKey && typeof message.contactPrimaryIdentityKey.length === "number" || $util.isString(message.contactPrimaryIdentityKey)))
                    return "contactPrimaryIdentityKey: buffer expected";
            }
            if (message.pinned != null && Object.hasOwnProperty.call(message, "pinned")) {
                properties._pinned = 1;
                if (!$util.isInteger(message.pinned))
                    return "pinned: integer expected";
            }
            if (message.muteEndTime != null && Object.hasOwnProperty.call(message, "muteEndTime")) {
                properties._muteEndTime = 1;
                if (!$util.isInteger(message.muteEndTime) && !(message.muteEndTime && $util.isInteger(message.muteEndTime.low) && $util.isInteger(message.muteEndTime.high)))
                    return "muteEndTime: integer|Long expected";
            }
            if (message.wallpaper != null && Object.hasOwnProperty.call(message, "wallpaper")) {
                properties._wallpaper = 1;
                {
                    var error = $root.proto.WallpaperSettings.verify(message.wallpaper, q + 1);
                    if (error)
                        return "wallpaper." + error;
                }
            }
            if (message.mediaVisibility != null && Object.hasOwnProperty.call(message, "mediaVisibility")) {
                properties._mediaVisibility = 1;
                if (typeof message.mediaVisibility !== "number" || (message.mediaVisibility | 0) !== message.mediaVisibility)
                    return "mediaVisibility: enum value expected";
            }
            if (message.tcTokenSenderTimestamp != null && Object.hasOwnProperty.call(message, "tcTokenSenderTimestamp")) {
                properties._tcTokenSenderTimestamp = 1;
                if (!$util.isInteger(message.tcTokenSenderTimestamp) && !(message.tcTokenSenderTimestamp && $util.isInteger(message.tcTokenSenderTimestamp.low) && $util.isInteger(message.tcTokenSenderTimestamp.high)))
                    return "tcTokenSenderTimestamp: integer|Long expected";
            }
            if (message.suspended != null && Object.hasOwnProperty.call(message, "suspended")) {
                properties._suspended = 1;
                if (typeof message.suspended !== "boolean")
                    return "suspended: boolean expected";
            }
            if (message.terminated != null && Object.hasOwnProperty.call(message, "terminated")) {
                properties._terminated = 1;
                if (typeof message.terminated !== "boolean")
                    return "terminated: boolean expected";
            }
            if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt")) {
                properties._createdAt = 1;
                if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
                    return "createdAt: integer|Long expected";
            }
            if (message.createdBy != null && Object.hasOwnProperty.call(message, "createdBy")) {
                properties._createdBy = 1;
                if (!$util.isString(message.createdBy))
                    return "createdBy: string expected";
            }
            if (message.description != null && Object.hasOwnProperty.call(message, "description")) {
                properties._description = 1;
                if (!$util.isString(message.description))
                    return "description: string expected";
            }
            if (message.support != null && Object.hasOwnProperty.call(message, "support")) {
                properties._support = 1;
                if (typeof message.support !== "boolean")
                    return "support: boolean expected";
            }
            if (message.isParentGroup != null && Object.hasOwnProperty.call(message, "isParentGroup")) {
                properties._isParentGroup = 1;
                if (typeof message.isParentGroup !== "boolean")
                    return "isParentGroup: boolean expected";
            }
            if (message.parentGroupId != null && Object.hasOwnProperty.call(message, "parentGroupId")) {
                properties._parentGroupId = 1;
                if (!$util.isString(message.parentGroupId))
                    return "parentGroupId: string expected";
            }
            if (message.isDefaultSubgroup != null && Object.hasOwnProperty.call(message, "isDefaultSubgroup")) {
                properties._isDefaultSubgroup = 1;
                if (typeof message.isDefaultSubgroup !== "boolean")
                    return "isDefaultSubgroup: boolean expected";
            }
            if (message.displayName != null && Object.hasOwnProperty.call(message, "displayName")) {
                properties._displayName = 1;
                if (!$util.isString(message.displayName))
                    return "displayName: string expected";
            }
            if (message.pnJid != null && Object.hasOwnProperty.call(message, "pnJid")) {
                properties._pnJid = 1;
                if (!$util.isString(message.pnJid))
                    return "pnJid: string expected";
            }
            if (message.shareOwnPn != null && Object.hasOwnProperty.call(message, "shareOwnPn")) {
                properties._shareOwnPn = 1;
                if (typeof message.shareOwnPn !== "boolean")
                    return "shareOwnPn: boolean expected";
            }
            if (message.pnhDuplicateLidThread != null && Object.hasOwnProperty.call(message, "pnhDuplicateLidThread")) {
                properties._pnhDuplicateLidThread = 1;
                if (typeof message.pnhDuplicateLidThread !== "boolean")
                    return "pnhDuplicateLidThread: boolean expected";
            }
            if (message.lidJid != null && Object.hasOwnProperty.call(message, "lidJid")) {
                properties._lidJid = 1;
                if (!$util.isString(message.lidJid))
                    return "lidJid: string expected";
            }
            if (message.username != null && Object.hasOwnProperty.call(message, "username")) {
                properties._username = 1;
                if (!$util.isString(message.username))
                    return "username: string expected";
            }
            if (message.lidOriginType != null && Object.hasOwnProperty.call(message, "lidOriginType")) {
                properties._lidOriginType = 1;
                if (!$util.isString(message.lidOriginType))
                    return "lidOriginType: string expected";
            }
            if (message.commentsCount != null && Object.hasOwnProperty.call(message, "commentsCount")) {
                properties._commentsCount = 1;
                if (!$util.isInteger(message.commentsCount))
                    return "commentsCount: integer expected";
            }
            if (message.locked != null && Object.hasOwnProperty.call(message, "locked")) {
                properties._locked = 1;
                if (typeof message.locked !== "boolean")
                    return "locked: boolean expected";
            }
            if (message.systemMessageToInsert != null && Object.hasOwnProperty.call(message, "systemMessageToInsert")) {
                properties._systemMessageToInsert = 1;
                if (typeof message.systemMessageToInsert !== "number" || (message.systemMessageToInsert | 0) !== message.systemMessageToInsert)
                    return "systemMessageToInsert: enum value expected";
            }
            if (message.capiCreatedGroup != null && Object.hasOwnProperty.call(message, "capiCreatedGroup")) {
                properties._capiCreatedGroup = 1;
                if (typeof message.capiCreatedGroup !== "boolean")
                    return "capiCreatedGroup: boolean expected";
            }
            if (message.accountLid != null && Object.hasOwnProperty.call(message, "accountLid")) {
                properties._accountLid = 1;
                if (!$util.isString(message.accountLid))
                    return "accountLid: string expected";
            }
            if (message.limitSharing != null && Object.hasOwnProperty.call(message, "limitSharing")) {
                properties._limitSharing = 1;
                if (typeof message.limitSharing !== "boolean")
                    return "limitSharing: boolean expected";
            }
            if (message.limitSharingSettingTimestamp != null && Object.hasOwnProperty.call(message, "limitSharingSettingTimestamp")) {
                properties._limitSharingSettingTimestamp = 1;
                if (!$util.isInteger(message.limitSharingSettingTimestamp) && !(message.limitSharingSettingTimestamp && $util.isInteger(message.limitSharingSettingTimestamp.low) && $util.isInteger(message.limitSharingSettingTimestamp.high)))
                    return "limitSharingSettingTimestamp: integer|Long expected";
            }
            if (message.limitSharingTrigger != null && Object.hasOwnProperty.call(message, "limitSharingTrigger")) {
                properties._limitSharingTrigger = 1;
                if (typeof message.limitSharingTrigger !== "number" || (message.limitSharingTrigger | 0) !== message.limitSharingTrigger)
                    return "limitSharingTrigger: enum value expected";
            }
            if (message.limitSharingInitiatedByMe != null && Object.hasOwnProperty.call(message, "limitSharingInitiatedByMe")) {
                properties._limitSharingInitiatedByMe = 1;
                if (typeof message.limitSharingInitiatedByMe !== "boolean")
                    return "limitSharingInitiatedByMe: boolean expected";
            }
            if (message.maibaAiThreadEnabled != null && Object.hasOwnProperty.call(message, "maibaAiThreadEnabled")) {
                properties._maibaAiThreadEnabled = 1;
                if (typeof message.maibaAiThreadEnabled !== "boolean")
                    return "maibaAiThreadEnabled: boolean expected";
            }
            if (message.isMarketingMessageThread != null && Object.hasOwnProperty.call(message, "isMarketingMessageThread")) {
                properties._isMarketingMessageThread = 1;
                if (typeof message.isMarketingMessageThread !== "boolean")
                    return "isMarketingMessageThread: boolean expected";
            }
            if (message.isSenderNewAccount != null && Object.hasOwnProperty.call(message, "isSenderNewAccount")) {
                properties._isSenderNewAccount = 1;
                if (typeof message.isSenderNewAccount !== "boolean")
                    return "isSenderNewAccount: boolean expected";
            }
            if (message.afterReadDuration != null && Object.hasOwnProperty.call(message, "afterReadDuration")) {
                properties._afterReadDuration = 1;
                if (!$util.isInteger(message.afterReadDuration))
                    return "afterReadDuration: integer expected";
            }
            if (message.isSenderSuspicious != null && Object.hasOwnProperty.call(message, "isSenderSuspicious")) {
                properties._isSenderSuspicious = 1;
                if (typeof message.isSenderSuspicious !== "boolean")
                    return "isSenderSuspicious: boolean expected";
            }
            if (message.appealStatus != null && Object.hasOwnProperty.call(message, "appealStatus")) {
                properties._appealStatus = 1;
                if (typeof message.appealStatus !== "number" || (message.appealStatus | 0) !== message.appealStatus)
                    return "appealStatus: enum value expected";
            }
            if (message.appealUpdateTime != null && Object.hasOwnProperty.call(message, "appealUpdateTime")) {
                properties._appealUpdateTime = 1;
                if (!$util.isInteger(message.appealUpdateTime) && !(message.appealUpdateTime && $util.isInteger(message.appealUpdateTime.low) && $util.isInteger(message.appealUpdateTime.high)))
                    return "appealUpdateTime: integer|Long expected";
            }
            if (message.authAgentParentCompanyName != null && Object.hasOwnProperty.call(message, "authAgentParentCompanyName")) {
                properties._authAgentParentCompanyName = 1;
                if (!$util.isString(message.authAgentParentCompanyName))
                    return "authAgentParentCompanyName: string expected";
            }
            if (message.authAgentObaPhoneNumber != null && Object.hasOwnProperty.call(message, "authAgentObaPhoneNumber")) {
                properties._authAgentObaPhoneNumber = 1;
                if (!$util.isString(message.authAgentObaPhoneNumber))
                    return "authAgentObaPhoneNumber: string expected";
            }
            return null;
        };

        /**
         * Creates a Conversation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Conversation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Conversation} Conversation
         */
        Conversation.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.Conversation: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.id != null)
                message.id = String(object.id);
            if (object.messages) {
                if (!Array.isArray(object.messages))
                    throw TypeError(".proto.Conversation.messages: array expected");
                message.messages = Array(object.messages.length);
                for (var i = 0; i < object.messages.length; ++i) {
                    if (!$util.isObject(object.messages[i]))
                        throw TypeError(".proto.Conversation.messages: object expected");
                    message.messages[i] = $root.proto.HistorySyncMsg.fromObject(object.messages[i], q + 1);
                }
            }
            if (object.newJid != null)
                message.newJid = String(object.newJid);
            if (object.oldJid != null)
                message.oldJid = String(object.oldJid);
            if (object.lastMsgTimestamp != null)
                if ($util.Long)
                    message.lastMsgTimestamp = $util.Long.fromValue(object.lastMsgTimestamp, true);
                else if (typeof object.lastMsgTimestamp === "string")
                    message.lastMsgTimestamp = parseInt(object.lastMsgTimestamp, 10);
                else if (typeof object.lastMsgTimestamp === "number")
                    message.lastMsgTimestamp = object.lastMsgTimestamp;
                else if (typeof object.lastMsgTimestamp === "object")
                    message.lastMsgTimestamp = new $util.LongBits(object.lastMsgTimestamp.low >>> 0, object.lastMsgTimestamp.high >>> 0).toNumber(true);
            if (object.unreadCount != null)
                message.unreadCount = object.unreadCount >>> 0;
            if (object.readOnly != null)
                message.readOnly = Boolean(object.readOnly);
            if (object.endOfHistoryTransfer != null)
                message.endOfHistoryTransfer = Boolean(object.endOfHistoryTransfer);
            if (object.ephemeralExpiration != null)
                message.ephemeralExpiration = object.ephemeralExpiration >>> 0;
            if (object.ephemeralSettingTimestamp != null)
                if ($util.Long)
                    message.ephemeralSettingTimestamp = $util.Long.fromValue(object.ephemeralSettingTimestamp, false);
                else if (typeof object.ephemeralSettingTimestamp === "string")
                    message.ephemeralSettingTimestamp = parseInt(object.ephemeralSettingTimestamp, 10);
                else if (typeof object.ephemeralSettingTimestamp === "number")
                    message.ephemeralSettingTimestamp = object.ephemeralSettingTimestamp;
                else if (typeof object.ephemeralSettingTimestamp === "object")
                    message.ephemeralSettingTimestamp = new $util.LongBits(object.ephemeralSettingTimestamp.low >>> 0, object.ephemeralSettingTimestamp.high >>> 0).toNumber();
            switch (object.endOfHistoryTransferType) {
            case "COMPLETE_BUT_MORE_MESSAGES_REMAIN_ON_PRIMARY":
            case 0:
                message.endOfHistoryTransferType = 0;
                break;
            case "COMPLETE_AND_NO_MORE_MESSAGE_REMAIN_ON_PRIMARY":
            case 1:
                message.endOfHistoryTransferType = 1;
                break;
            case "COMPLETE_ON_DEMAND_SYNC_BUT_MORE_MSG_REMAIN_ON_PRIMARY":
            case 2:
                message.endOfHistoryTransferType = 2;
                break;
            case "COMPLETE_ON_DEMAND_SYNC_WITH_MORE_MSG_ON_PRIMARY_BUT_NO_ACCESS":
            case 3:
                message.endOfHistoryTransferType = 3;
                break;
            default:
                if (typeof object.endOfHistoryTransferType === "number" && (object.endOfHistoryTransferType | 0) === object.endOfHistoryTransferType)
                    message.endOfHistoryTransferType = object.endOfHistoryTransferType;
            }
            if (object.conversationTimestamp != null)
                if ($util.Long)
                    message.conversationTimestamp = $util.Long.fromValue(object.conversationTimestamp, true);
                else if (typeof object.conversationTimestamp === "string")
                    message.conversationTimestamp = parseInt(object.conversationTimestamp, 10);
                else if (typeof object.conversationTimestamp === "number")
                    message.conversationTimestamp = object.conversationTimestamp;
                else if (typeof object.conversationTimestamp === "object")
                    message.conversationTimestamp = new $util.LongBits(object.conversationTimestamp.low >>> 0, object.conversationTimestamp.high >>> 0).toNumber(true);
            if (object.name != null)
                message.name = String(object.name);
            if (object.pHash != null)
                message.pHash = String(object.pHash);
            if (object.notSpam != null)
                message.notSpam = Boolean(object.notSpam);
            if (object.archived != null)
                message.archived = Boolean(object.archived);
            if (object.disappearingMode != null) {
                if (!$util.isObject(object.disappearingMode))
                    throw TypeError(".proto.Conversation.disappearingMode: object expected");
                message.disappearingMode = $root.proto.DisappearingMode.fromObject(object.disappearingMode, q + 1);
            }
            if (object.unreadMentionCount != null)
                message.unreadMentionCount = object.unreadMentionCount >>> 0;
            if (object.markedAsUnread != null)
                message.markedAsUnread = Boolean(object.markedAsUnread);
            if (object.participant) {
                if (!Array.isArray(object.participant))
                    throw TypeError(".proto.Conversation.participant: array expected");
                message.participant = Array(object.participant.length);
                for (var i = 0; i < object.participant.length; ++i) {
                    if (!$util.isObject(object.participant[i]))
                        throw TypeError(".proto.Conversation.participant: object expected");
                    message.participant[i] = $root.proto.GroupParticipant.fromObject(object.participant[i], q + 1);
                }
            }
            if (object.tcToken != null)
                if (typeof object.tcToken === "string")
                    $util.base64.decode(object.tcToken, message.tcToken = $util.newBuffer($util.base64.length(object.tcToken)), 0);
                else if (object.tcToken.length >= 0)
                    message.tcToken = object.tcToken;
            if (object.tcTokenTimestamp != null)
                if ($util.Long)
                    message.tcTokenTimestamp = $util.Long.fromValue(object.tcTokenTimestamp, true);
                else if (typeof object.tcTokenTimestamp === "string")
                    message.tcTokenTimestamp = parseInt(object.tcTokenTimestamp, 10);
                else if (typeof object.tcTokenTimestamp === "number")
                    message.tcTokenTimestamp = object.tcTokenTimestamp;
                else if (typeof object.tcTokenTimestamp === "object")
                    message.tcTokenTimestamp = new $util.LongBits(object.tcTokenTimestamp.low >>> 0, object.tcTokenTimestamp.high >>> 0).toNumber(true);
            if (object.contactPrimaryIdentityKey != null)
                if (typeof object.contactPrimaryIdentityKey === "string")
                    $util.base64.decode(object.contactPrimaryIdentityKey, message.contactPrimaryIdentityKey = $util.newBuffer($util.base64.length(object.contactPrimaryIdentityKey)), 0);
                else if (object.contactPrimaryIdentityKey.length >= 0)
                    message.contactPrimaryIdentityKey = object.contactPrimaryIdentityKey;
            if (object.pinned != null)
                message.pinned = object.pinned >>> 0;
            if (object.muteEndTime != null)
                if ($util.Long)
                    message.muteEndTime = $util.Long.fromValue(object.muteEndTime, true);
                else if (typeof object.muteEndTime === "string")
                    message.muteEndTime = parseInt(object.muteEndTime, 10);
                else if (typeof object.muteEndTime === "number")
                    message.muteEndTime = object.muteEndTime;
                else if (typeof object.muteEndTime === "object")
                    message.muteEndTime = new $util.LongBits(object.muteEndTime.low >>> 0, object.muteEndTime.high >>> 0).toNumber(true);
            if (object.wallpaper != null) {
                if (!$util.isObject(object.wallpaper))
                    throw TypeError(".proto.Conversation.wallpaper: object expected");
                message.wallpaper = $root.proto.WallpaperSettings.fromObject(object.wallpaper, q + 1);
            }
            switch (object.mediaVisibility) {
            case "DEFAULT":
            case 0:
                message.mediaVisibility = 0;
                break;
            case "OFF":
            case 1:
                message.mediaVisibility = 1;
                break;
            case "ON":
            case 2:
                message.mediaVisibility = 2;
                break;
            default:
                if (typeof object.mediaVisibility === "number" && (object.mediaVisibility | 0) === object.mediaVisibility)
                    message.mediaVisibility = object.mediaVisibility;
            }
            if (object.tcTokenSenderTimestamp != null)
                if ($util.Long)
                    message.tcTokenSenderTimestamp = $util.Long.fromValue(object.tcTokenSenderTimestamp, true);
                else if (typeof object.tcTokenSenderTimestamp === "string")
                    message.tcTokenSenderTimestamp = parseInt(object.tcTokenSenderTimestamp, 10);
                else if (typeof object.tcTokenSenderTimestamp === "number")
                    message.tcTokenSenderTimestamp = object.tcTokenSenderTimestamp;
                else if (typeof object.tcTokenSenderTimestamp === "object")
                    message.tcTokenSenderTimestamp = new $util.LongBits(object.tcTokenSenderTimestamp.low >>> 0, object.tcTokenSenderTimestamp.high >>> 0).toNumber(true);
            if (object.suspended != null)
                message.suspended = Boolean(object.suspended);
            if (object.terminated != null)
                message.terminated = Boolean(object.terminated);
            if (object.createdAt != null)
                if ($util.Long)
                    message.createdAt = $util.Long.fromValue(object.createdAt, true);
                else if (typeof object.createdAt === "string")
                    message.createdAt = parseInt(object.createdAt, 10);
                else if (typeof object.createdAt === "number")
                    message.createdAt = object.createdAt;
                else if (typeof object.createdAt === "object")
                    message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber(true);
            if (object.createdBy != null)
                message.createdBy = String(object.createdBy);
            if (object.description != null)
                message.description = String(object.description);
            if (object.support != null)
                message.support = Boolean(object.support);
            if (object.isParentGroup != null)
                message.isParentGroup = Boolean(object.isParentGroup);
            if (object.parentGroupId != null)
                message.parentGroupId = String(object.parentGroupId);
            if (object.isDefaultSubgroup != null)
                message.isDefaultSubgroup = Boolean(object.isDefaultSubgroup);
            if (object.displayName != null)
                message.displayName = String(object.displayName);
            if (object.pnJid != null)
                message.pnJid = String(object.pnJid);
            if (object.shareOwnPn != null)
                message.shareOwnPn = Boolean(object.shareOwnPn);
            if (object.pnhDuplicateLidThread != null)
                message.pnhDuplicateLidThread = Boolean(object.pnhDuplicateLidThread);
            if (object.lidJid != null)
                message.lidJid = String(object.lidJid);
            if (object.username != null)
                message.username = String(object.username);
            if (object.lidOriginType != null)
                message.lidOriginType = String(object.lidOriginType);
            if (object.commentsCount != null)
                message.commentsCount = object.commentsCount >>> 0;
            if (object.locked != null)
                message.locked = Boolean(object.locked);
            switch (object.systemMessageToInsert) {
            case "E2EE_MSG":
            case 1:
                message.systemMessageToInsert = 1;
                break;
            case "NE2EE_SELF":
            case 2:
                message.systemMessageToInsert = 2;
                break;
            case "NE2EE_OTHER":
            case 3:
                message.systemMessageToInsert = 3;
                break;
            default:
                if (typeof object.systemMessageToInsert === "number" && (object.systemMessageToInsert | 0) === object.systemMessageToInsert)
                    message.systemMessageToInsert = object.systemMessageToInsert;
            }
            if (object.capiCreatedGroup != null)
                message.capiCreatedGroup = Boolean(object.capiCreatedGroup);
            if (object.accountLid != null)
                message.accountLid = String(object.accountLid);
            if (object.limitSharing != null)
                message.limitSharing = Boolean(object.limitSharing);
            if (object.limitSharingSettingTimestamp != null)
                if ($util.Long)
                    message.limitSharingSettingTimestamp = $util.Long.fromValue(object.limitSharingSettingTimestamp, false);
                else if (typeof object.limitSharingSettingTimestamp === "string")
                    message.limitSharingSettingTimestamp = parseInt(object.limitSharingSettingTimestamp, 10);
                else if (typeof object.limitSharingSettingTimestamp === "number")
                    message.limitSharingSettingTimestamp = object.limitSharingSettingTimestamp;
                else if (typeof object.limitSharingSettingTimestamp === "object")
                    message.limitSharingSettingTimestamp = new $util.LongBits(object.limitSharingSettingTimestamp.low >>> 0, object.limitSharingSettingTimestamp.high >>> 0).toNumber();
            switch (object.limitSharingTrigger) {
            case "UNKNOWN":
            case 0:
                message.limitSharingTrigger = 0;
                break;
            case "CHAT_SETTING":
            case 1:
                message.limitSharingTrigger = 1;
                break;
            case "BIZ_SUPPORTS_FB_HOSTING":
            case 2:
                message.limitSharingTrigger = 2;
                break;
            case "UNKNOWN_GROUP":
            case 3:
                message.limitSharingTrigger = 3;
                break;
            default:
                if (typeof object.limitSharingTrigger === "number" && (object.limitSharingTrigger | 0) === object.limitSharingTrigger)
                    message.limitSharingTrigger = object.limitSharingTrigger;
            }
            if (object.limitSharingInitiatedByMe != null)
                message.limitSharingInitiatedByMe = Boolean(object.limitSharingInitiatedByMe);
            if (object.maibaAiThreadEnabled != null)
                message.maibaAiThreadEnabled = Boolean(object.maibaAiThreadEnabled);
            if (object.isMarketingMessageThread != null)
                message.isMarketingMessageThread = Boolean(object.isMarketingMessageThread);
            if (object.isSenderNewAccount != null)
                message.isSenderNewAccount = Boolean(object.isSenderNewAccount);
            if (object.afterReadDuration != null)
                message.afterReadDuration = object.afterReadDuration >>> 0;
            if (object.isSenderSuspicious != null)
                message.isSenderSuspicious = Boolean(object.isSenderSuspicious);
            switch (object.appealStatus) {
            case "NO_APPEAL":
            case 0:
                message.appealStatus = 0;
                break;
            case "APPEAL_IN_REVIEW":
            case 1:
                message.appealStatus = 1;
                break;
            case "APPEAL_APPROVED":
            case 2:
                message.appealStatus = 2;
                break;
            case "APPEAL_REJECTED":
            case 3:
                message.appealStatus = 3;
                break;
            default:
                if (typeof object.appealStatus === "number" && (object.appealStatus | 0) === object.appealStatus)
                    message.appealStatus = object.appealStatus;
            }
            if (object.appealUpdateTime != null)
                if ($util.Long)
                    message.appealUpdateTime = $util.Long.fromValue(object.appealUpdateTime, true);
                else if (typeof object.appealUpdateTime === "string")
                    message.appealUpdateTime = parseInt(object.appealUpdateTime, 10);
                else if (typeof object.appealUpdateTime === "number")
                    message.appealUpdateTime = object.appealUpdateTime;
                else if (typeof object.appealUpdateTime === "object")
                    message.appealUpdateTime = new $util.LongBits(object.appealUpdateTime.low >>> 0, object.appealUpdateTime.high >>> 0).toNumber(true);
            if (object.authAgentParentCompanyName != null)
                message.authAgentParentCompanyName = String(object.authAgentParentCompanyName);
            if (object.authAgentObaPhoneNumber != null)
                message.authAgentObaPhoneNumber = String(object.authAgentObaPhoneNumber);
            return message;
        };

        /**
         * Creates a plain object from a Conversation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Conversation
         * @static
         * @param {proto.Conversation} message Conversation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Conversation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults) {
                object.messages = [];
                object.participant = [];
            }
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.messages && message.messages.length) {
                object.messages = Array(message.messages.length);
                for (var j = 0; j < message.messages.length; ++j)
                    object.messages[j] = $root.proto.HistorySyncMsg.toObject(message.messages[j], options, q + 1);
            }
            if (message.newJid != null && Object.hasOwnProperty.call(message, "newJid"))
                object.newJid = message.newJid;
            if (message.oldJid != null && Object.hasOwnProperty.call(message, "oldJid"))
                object.oldJid = message.oldJid;
            if (message.lastMsgTimestamp != null && Object.hasOwnProperty.call(message, "lastMsgTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastMsgTimestamp = typeof message.lastMsgTimestamp === "number" ? BigInt(message.lastMsgTimestamp) : $util.Long.fromBits(message.lastMsgTimestamp.low >>> 0, message.lastMsgTimestamp.high >>> 0, true).toBigInt();
                else if (typeof message.lastMsgTimestamp === "number")
                    object.lastMsgTimestamp = options.longs === String ? String(message.lastMsgTimestamp) : message.lastMsgTimestamp;
                else
                    object.lastMsgTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.lastMsgTimestamp) : options.longs === Number ? new $util.LongBits(message.lastMsgTimestamp.low >>> 0, message.lastMsgTimestamp.high >>> 0).toNumber(true) : message.lastMsgTimestamp;
            if (message.unreadCount != null && Object.hasOwnProperty.call(message, "unreadCount"))
                object.unreadCount = message.unreadCount;
            if (message.readOnly != null && Object.hasOwnProperty.call(message, "readOnly"))
                object.readOnly = message.readOnly;
            if (message.endOfHistoryTransfer != null && Object.hasOwnProperty.call(message, "endOfHistoryTransfer"))
                object.endOfHistoryTransfer = message.endOfHistoryTransfer;
            if (message.ephemeralExpiration != null && Object.hasOwnProperty.call(message, "ephemeralExpiration"))
                object.ephemeralExpiration = message.ephemeralExpiration;
            if (message.ephemeralSettingTimestamp != null && Object.hasOwnProperty.call(message, "ephemeralSettingTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.ephemeralSettingTimestamp = typeof message.ephemeralSettingTimestamp === "number" ? BigInt(message.ephemeralSettingTimestamp) : $util.Long.fromBits(message.ephemeralSettingTimestamp.low >>> 0, message.ephemeralSettingTimestamp.high >>> 0, false).toBigInt();
                else if (typeof message.ephemeralSettingTimestamp === "number")
                    object.ephemeralSettingTimestamp = options.longs === String ? String(message.ephemeralSettingTimestamp) : message.ephemeralSettingTimestamp;
                else
                    object.ephemeralSettingTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.ephemeralSettingTimestamp) : options.longs === Number ? new $util.LongBits(message.ephemeralSettingTimestamp.low >>> 0, message.ephemeralSettingTimestamp.high >>> 0).toNumber() : message.ephemeralSettingTimestamp;
            if (message.endOfHistoryTransferType != null && Object.hasOwnProperty.call(message, "endOfHistoryTransferType"))
                object.endOfHistoryTransferType = options.enums === String ? $root.proto.Conversation.EndOfHistoryTransferType[message.endOfHistoryTransferType] === undefined ? message.endOfHistoryTransferType : $root.proto.Conversation.EndOfHistoryTransferType[message.endOfHistoryTransferType] : message.endOfHistoryTransferType;
            if (message.conversationTimestamp != null && Object.hasOwnProperty.call(message, "conversationTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.conversationTimestamp = typeof message.conversationTimestamp === "number" ? BigInt(message.conversationTimestamp) : $util.Long.fromBits(message.conversationTimestamp.low >>> 0, message.conversationTimestamp.high >>> 0, true).toBigInt();
                else if (typeof message.conversationTimestamp === "number")
                    object.conversationTimestamp = options.longs === String ? String(message.conversationTimestamp) : message.conversationTimestamp;
                else
                    object.conversationTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.conversationTimestamp) : options.longs === Number ? new $util.LongBits(message.conversationTimestamp.low >>> 0, message.conversationTimestamp.high >>> 0).toNumber(true) : message.conversationTimestamp;
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.pHash != null && Object.hasOwnProperty.call(message, "pHash"))
                object.pHash = message.pHash;
            if (message.notSpam != null && Object.hasOwnProperty.call(message, "notSpam"))
                object.notSpam = message.notSpam;
            if (message.archived != null && Object.hasOwnProperty.call(message, "archived"))
                object.archived = message.archived;
            if (message.disappearingMode != null && Object.hasOwnProperty.call(message, "disappearingMode"))
                object.disappearingMode = $root.proto.DisappearingMode.toObject(message.disappearingMode, options, q + 1);
            if (message.unreadMentionCount != null && Object.hasOwnProperty.call(message, "unreadMentionCount"))
                object.unreadMentionCount = message.unreadMentionCount;
            if (message.markedAsUnread != null && Object.hasOwnProperty.call(message, "markedAsUnread"))
                object.markedAsUnread = message.markedAsUnread;
            if (message.participant && message.participant.length) {
                object.participant = Array(message.participant.length);
                for (var j = 0; j < message.participant.length; ++j)
                    object.participant[j] = $root.proto.GroupParticipant.toObject(message.participant[j], options, q + 1);
            }
            if (message.tcToken != null && Object.hasOwnProperty.call(message, "tcToken"))
                object.tcToken = options.bytes === String ? $util.base64.encode(message.tcToken, 0, message.tcToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.tcToken) : message.tcToken;
            if (message.tcTokenTimestamp != null && Object.hasOwnProperty.call(message, "tcTokenTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.tcTokenTimestamp = typeof message.tcTokenTimestamp === "number" ? BigInt(message.tcTokenTimestamp) : $util.Long.fromBits(message.tcTokenTimestamp.low >>> 0, message.tcTokenTimestamp.high >>> 0, true).toBigInt();
                else if (typeof message.tcTokenTimestamp === "number")
                    object.tcTokenTimestamp = options.longs === String ? String(message.tcTokenTimestamp) : message.tcTokenTimestamp;
                else
                    object.tcTokenTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.tcTokenTimestamp) : options.longs === Number ? new $util.LongBits(message.tcTokenTimestamp.low >>> 0, message.tcTokenTimestamp.high >>> 0).toNumber(true) : message.tcTokenTimestamp;
            if (message.contactPrimaryIdentityKey != null && Object.hasOwnProperty.call(message, "contactPrimaryIdentityKey"))
                object.contactPrimaryIdentityKey = options.bytes === String ? $util.base64.encode(message.contactPrimaryIdentityKey, 0, message.contactPrimaryIdentityKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.contactPrimaryIdentityKey) : message.contactPrimaryIdentityKey;
            if (message.pinned != null && Object.hasOwnProperty.call(message, "pinned"))
                object.pinned = message.pinned;
            if (message.muteEndTime != null && Object.hasOwnProperty.call(message, "muteEndTime"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.muteEndTime = typeof message.muteEndTime === "number" ? BigInt(message.muteEndTime) : $util.Long.fromBits(message.muteEndTime.low >>> 0, message.muteEndTime.high >>> 0, true).toBigInt();
                else if (typeof message.muteEndTime === "number")
                    object.muteEndTime = options.longs === String ? String(message.muteEndTime) : message.muteEndTime;
                else
                    object.muteEndTime = options.longs === String ? $util.Long.prototype.toString.call(message.muteEndTime) : options.longs === Number ? new $util.LongBits(message.muteEndTime.low >>> 0, message.muteEndTime.high >>> 0).toNumber(true) : message.muteEndTime;
            if (message.wallpaper != null && Object.hasOwnProperty.call(message, "wallpaper"))
                object.wallpaper = $root.proto.WallpaperSettings.toObject(message.wallpaper, options, q + 1);
            if (message.mediaVisibility != null && Object.hasOwnProperty.call(message, "mediaVisibility"))
                object.mediaVisibility = options.enums === String ? $root.proto.MediaVisibility[message.mediaVisibility] === undefined ? message.mediaVisibility : $root.proto.MediaVisibility[message.mediaVisibility] : message.mediaVisibility;
            if (message.tcTokenSenderTimestamp != null && Object.hasOwnProperty.call(message, "tcTokenSenderTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.tcTokenSenderTimestamp = typeof message.tcTokenSenderTimestamp === "number" ? BigInt(message.tcTokenSenderTimestamp) : $util.Long.fromBits(message.tcTokenSenderTimestamp.low >>> 0, message.tcTokenSenderTimestamp.high >>> 0, true).toBigInt();
                else if (typeof message.tcTokenSenderTimestamp === "number")
                    object.tcTokenSenderTimestamp = options.longs === String ? String(message.tcTokenSenderTimestamp) : message.tcTokenSenderTimestamp;
                else
                    object.tcTokenSenderTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.tcTokenSenderTimestamp) : options.longs === Number ? new $util.LongBits(message.tcTokenSenderTimestamp.low >>> 0, message.tcTokenSenderTimestamp.high >>> 0).toNumber(true) : message.tcTokenSenderTimestamp;
            if (message.suspended != null && Object.hasOwnProperty.call(message, "suspended"))
                object.suspended = message.suspended;
            if (message.terminated != null && Object.hasOwnProperty.call(message, "terminated"))
                object.terminated = message.terminated;
            if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.createdAt = typeof message.createdAt === "number" ? BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, true).toBigInt();
                else if (typeof message.createdAt === "number")
                    object.createdAt = options.longs === String ? String(message.createdAt) : message.createdAt;
                else
                    object.createdAt = options.longs === String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber(true) : message.createdAt;
            if (message.createdBy != null && Object.hasOwnProperty.call(message, "createdBy"))
                object.createdBy = message.createdBy;
            if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                object.description = message.description;
            if (message.support != null && Object.hasOwnProperty.call(message, "support"))
                object.support = message.support;
            if (message.isParentGroup != null && Object.hasOwnProperty.call(message, "isParentGroup"))
                object.isParentGroup = message.isParentGroup;
            if (message.isDefaultSubgroup != null && Object.hasOwnProperty.call(message, "isDefaultSubgroup"))
                object.isDefaultSubgroup = message.isDefaultSubgroup;
            if (message.parentGroupId != null && Object.hasOwnProperty.call(message, "parentGroupId"))
                object.parentGroupId = message.parentGroupId;
            if (message.displayName != null && Object.hasOwnProperty.call(message, "displayName"))
                object.displayName = message.displayName;
            if (message.pnJid != null && Object.hasOwnProperty.call(message, "pnJid"))
                object.pnJid = message.pnJid;
            if (message.shareOwnPn != null && Object.hasOwnProperty.call(message, "shareOwnPn"))
                object.shareOwnPn = message.shareOwnPn;
            if (message.pnhDuplicateLidThread != null && Object.hasOwnProperty.call(message, "pnhDuplicateLidThread"))
                object.pnhDuplicateLidThread = message.pnhDuplicateLidThread;
            if (message.lidJid != null && Object.hasOwnProperty.call(message, "lidJid"))
                object.lidJid = message.lidJid;
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.lidOriginType != null && Object.hasOwnProperty.call(message, "lidOriginType"))
                object.lidOriginType = message.lidOriginType;
            if (message.commentsCount != null && Object.hasOwnProperty.call(message, "commentsCount"))
                object.commentsCount = message.commentsCount;
            if (message.locked != null && Object.hasOwnProperty.call(message, "locked"))
                object.locked = message.locked;
            if (message.systemMessageToInsert != null && Object.hasOwnProperty.call(message, "systemMessageToInsert"))
                object.systemMessageToInsert = options.enums === String ? $root.proto.PrivacySystemMessage[message.systemMessageToInsert] === undefined ? message.systemMessageToInsert : $root.proto.PrivacySystemMessage[message.systemMessageToInsert] : message.systemMessageToInsert;
            if (message.capiCreatedGroup != null && Object.hasOwnProperty.call(message, "capiCreatedGroup"))
                object.capiCreatedGroup = message.capiCreatedGroup;
            if (message.accountLid != null && Object.hasOwnProperty.call(message, "accountLid"))
                object.accountLid = message.accountLid;
            if (message.limitSharing != null && Object.hasOwnProperty.call(message, "limitSharing"))
                object.limitSharing = message.limitSharing;
            if (message.limitSharingSettingTimestamp != null && Object.hasOwnProperty.call(message, "limitSharingSettingTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.limitSharingSettingTimestamp = typeof message.limitSharingSettingTimestamp === "number" ? BigInt(message.limitSharingSettingTimestamp) : $util.Long.fromBits(message.limitSharingSettingTimestamp.low >>> 0, message.limitSharingSettingTimestamp.high >>> 0, false).toBigInt();
                else if (typeof message.limitSharingSettingTimestamp === "number")
                    object.limitSharingSettingTimestamp = options.longs === String ? String(message.limitSharingSettingTimestamp) : message.limitSharingSettingTimestamp;
                else
                    object.limitSharingSettingTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.limitSharingSettingTimestamp) : options.longs === Number ? new $util.LongBits(message.limitSharingSettingTimestamp.low >>> 0, message.limitSharingSettingTimestamp.high >>> 0).toNumber() : message.limitSharingSettingTimestamp;
            if (message.limitSharingTrigger != null && Object.hasOwnProperty.call(message, "limitSharingTrigger"))
                object.limitSharingTrigger = options.enums === String ? $root.proto.LimitSharing.TriggerType[message.limitSharingTrigger] === undefined ? message.limitSharingTrigger : $root.proto.LimitSharing.TriggerType[message.limitSharingTrigger] : message.limitSharingTrigger;
            if (message.limitSharingInitiatedByMe != null && Object.hasOwnProperty.call(message, "limitSharingInitiatedByMe"))
                object.limitSharingInitiatedByMe = message.limitSharingInitiatedByMe;
            if (message.maibaAiThreadEnabled != null && Object.hasOwnProperty.call(message, "maibaAiThreadEnabled"))
                object.maibaAiThreadEnabled = message.maibaAiThreadEnabled;
            if (message.isMarketingMessageThread != null && Object.hasOwnProperty.call(message, "isMarketingMessageThread"))
                object.isMarketingMessageThread = message.isMarketingMessageThread;
            if (message.isSenderNewAccount != null && Object.hasOwnProperty.call(message, "isSenderNewAccount"))
                object.isSenderNewAccount = message.isSenderNewAccount;
            if (message.afterReadDuration != null && Object.hasOwnProperty.call(message, "afterReadDuration"))
                object.afterReadDuration = message.afterReadDuration;
            if (message.isSenderSuspicious != null && Object.hasOwnProperty.call(message, "isSenderSuspicious"))
                object.isSenderSuspicious = message.isSenderSuspicious;
            if (message.appealStatus != null && Object.hasOwnProperty.call(message, "appealStatus"))
                object.appealStatus = options.enums === String ? $root.proto.Conversation.GroupAppealStatus[message.appealStatus] === undefined ? message.appealStatus : $root.proto.Conversation.GroupAppealStatus[message.appealStatus] : message.appealStatus;
            if (message.appealUpdateTime != null && Object.hasOwnProperty.call(message, "appealUpdateTime"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.appealUpdateTime = typeof message.appealUpdateTime === "number" ? BigInt(message.appealUpdateTime) : $util.Long.fromBits(message.appealUpdateTime.low >>> 0, message.appealUpdateTime.high >>> 0, true).toBigInt();
                else if (typeof message.appealUpdateTime === "number")
                    object.appealUpdateTime = options.longs === String ? String(message.appealUpdateTime) : message.appealUpdateTime;
                else
                    object.appealUpdateTime = options.longs === String ? $util.Long.prototype.toString.call(message.appealUpdateTime) : options.longs === Number ? new $util.LongBits(message.appealUpdateTime.low >>> 0, message.appealUpdateTime.high >>> 0).toNumber(true) : message.appealUpdateTime;
            if (message.authAgentParentCompanyName != null && Object.hasOwnProperty.call(message, "authAgentParentCompanyName"))
                object.authAgentParentCompanyName = message.authAgentParentCompanyName;
            if (message.authAgentObaPhoneNumber != null && Object.hasOwnProperty.call(message, "authAgentObaPhoneNumber"))
                object.authAgentObaPhoneNumber = message.authAgentObaPhoneNumber;
            return object;
        };

        /**
         * Converts this Conversation to JSON.
         * @function toJSON
         * @memberof proto.Conversation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Conversation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Conversation
         * @function getTypeUrl
         * @memberof proto.Conversation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Conversation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.Conversation";
        };

        /**
         * EndOfHistoryTransferType enum.
         * @name proto.Conversation.EndOfHistoryTransferType
         * @enum {number}
         * @property {number} COMPLETE_BUT_MORE_MESSAGES_REMAIN_ON_PRIMARY=0 COMPLETE_BUT_MORE_MESSAGES_REMAIN_ON_PRIMARY value
         * @property {number} COMPLETE_AND_NO_MORE_MESSAGE_REMAIN_ON_PRIMARY=1 COMPLETE_AND_NO_MORE_MESSAGE_REMAIN_ON_PRIMARY value
         * @property {number} COMPLETE_ON_DEMAND_SYNC_BUT_MORE_MSG_REMAIN_ON_PRIMARY=2 COMPLETE_ON_DEMAND_SYNC_BUT_MORE_MSG_REMAIN_ON_PRIMARY value
         * @property {number} COMPLETE_ON_DEMAND_SYNC_WITH_MORE_MSG_ON_PRIMARY_BUT_NO_ACCESS=3 COMPLETE_ON_DEMAND_SYNC_WITH_MORE_MSG_ON_PRIMARY_BUT_NO_ACCESS value
         */
        Conversation.EndOfHistoryTransferType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "COMPLETE_BUT_MORE_MESSAGES_REMAIN_ON_PRIMARY"] = 0;
            values[valuesById[1] = "COMPLETE_AND_NO_MORE_MESSAGE_REMAIN_ON_PRIMARY"] = 1;
            values[valuesById[2] = "COMPLETE_ON_DEMAND_SYNC_BUT_MORE_MSG_REMAIN_ON_PRIMARY"] = 2;
            values[valuesById[3] = "COMPLETE_ON_DEMAND_SYNC_WITH_MORE_MSG_ON_PRIMARY_BUT_NO_ACCESS"] = 3;
            return values;
        })();

        /**
         * GroupAppealStatus enum.
         * @name proto.Conversation.GroupAppealStatus
         * @enum {number}
         * @property {number} NO_APPEAL=0 NO_APPEAL value
         * @property {number} APPEAL_IN_REVIEW=1 APPEAL_IN_REVIEW value
         * @property {number} APPEAL_APPROVED=2 APPEAL_APPROVED value
         * @property {number} APPEAL_REJECTED=3 APPEAL_REJECTED value
         */
        Conversation.GroupAppealStatus = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "NO_APPEAL"] = 0;
            values[valuesById[1] = "APPEAL_IN_REVIEW"] = 1;
            values[valuesById[2] = "APPEAL_APPROVED"] = 2;
            values[valuesById[3] = "APPEAL_REJECTED"] = 3;
            return values;
        })();

        return Conversation;
    })();

    proto.DeviceCapabilities = (function() {

        /**
         * Properties of a DeviceCapabilities.
         * @memberof proto
         * @interface IDeviceCapabilities
         * @property {proto.DeviceCapabilities.ChatLockSupportLevel|null} [chatLockSupportLevel] DeviceCapabilities chatLockSupportLevel
         * @property {proto.DeviceCapabilities.ILIDMigration|null} [lidMigration] DeviceCapabilities lidMigration
         * @property {proto.DeviceCapabilities.IBusinessBroadcast|null} [businessBroadcast] DeviceCapabilities businessBroadcast
         * @property {proto.DeviceCapabilities.IUserHasAvatar|null} [userHasAvatar] DeviceCapabilities userHasAvatar
         * @property {proto.DeviceCapabilities.MemberNameTagPrimarySupport|null} [memberNameTagPrimarySupport] DeviceCapabilities memberNameTagPrimarySupport
         * @property {proto.DeviceCapabilities.IAiThread|null} [aiThread] DeviceCapabilities aiThread
         */

        /**
         * Constructs a new DeviceCapabilities.
         * @memberof proto
         * @classdesc Represents a DeviceCapabilities.
         * @implements IDeviceCapabilities
         * @constructor
         * @param {proto.IDeviceCapabilities=} [properties] Properties to set
         */
        function DeviceCapabilities(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DeviceCapabilities chatLockSupportLevel.
         * @member {proto.DeviceCapabilities.ChatLockSupportLevel|null|undefined} chatLockSupportLevel
         * @memberof proto.DeviceCapabilities
         * @instance
         */
        DeviceCapabilities.prototype.chatLockSupportLevel = null;

        /**
         * DeviceCapabilities lidMigration.
         * @member {proto.DeviceCapabilities.ILIDMigration|null|undefined} lidMigration
         * @memberof proto.DeviceCapabilities
         * @instance
         */
        DeviceCapabilities.prototype.lidMigration = null;

        /**
         * DeviceCapabilities businessBroadcast.
         * @member {proto.DeviceCapabilities.IBusinessBroadcast|null|undefined} businessBroadcast
         * @memberof proto.DeviceCapabilities
         * @instance
         */
        DeviceCapabilities.prototype.businessBroadcast = null;

        /**
         * DeviceCapabilities userHasAvatar.
         * @member {proto.DeviceCapabilities.IUserHasAvatar|null|undefined} userHasAvatar
         * @memberof proto.DeviceCapabilities
         * @instance
         */
        DeviceCapabilities.prototype.userHasAvatar = null;

        /**
         * DeviceCapabilities memberNameTagPrimarySupport.
         * @member {proto.DeviceCapabilities.MemberNameTagPrimarySupport|null|undefined} memberNameTagPrimarySupport
         * @memberof proto.DeviceCapabilities
         * @instance
         */
        DeviceCapabilities.prototype.memberNameTagPrimarySupport = null;

        /**
         * DeviceCapabilities aiThread.
         * @member {proto.DeviceCapabilities.IAiThread|null|undefined} aiThread
         * @memberof proto.DeviceCapabilities
         * @instance
         */
        DeviceCapabilities.prototype.aiThread = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceCapabilities.prototype, "_chatLockSupportLevel", {
            get: $util.oneOfGetter($oneOfFields = ["chatLockSupportLevel"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceCapabilities.prototype, "_lidMigration", {
            get: $util.oneOfGetter($oneOfFields = ["lidMigration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceCapabilities.prototype, "_businessBroadcast", {
            get: $util.oneOfGetter($oneOfFields = ["businessBroadcast"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceCapabilities.prototype, "_userHasAvatar", {
            get: $util.oneOfGetter($oneOfFields = ["userHasAvatar"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceCapabilities.prototype, "_memberNameTagPrimarySupport", {
            get: $util.oneOfGetter($oneOfFields = ["memberNameTagPrimarySupport"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceCapabilities.prototype, "_aiThread", {
            get: $util.oneOfGetter($oneOfFields = ["aiThread"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new DeviceCapabilities instance using the specified properties.
         * @function create
         * @memberof proto.DeviceCapabilities
         * @static
         * @param {proto.IDeviceCapabilities=} [properties] Properties to set
         * @returns {proto.DeviceCapabilities} DeviceCapabilities instance
         */
        DeviceCapabilities.create = function create(properties) {
            return new DeviceCapabilities(properties);
        };

        /**
         * Encodes the specified DeviceCapabilities message. Does not implicitly {@link proto.DeviceCapabilities.verify|verify} messages.
         * @function encode
         * @memberof proto.DeviceCapabilities
         * @static
         * @param {proto.IDeviceCapabilities} message DeviceCapabilities message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceCapabilities.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.chatLockSupportLevel != null && Object.hasOwnProperty.call(message, "chatLockSupportLevel"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.chatLockSupportLevel);
            if (message.lidMigration != null && Object.hasOwnProperty.call(message, "lidMigration"))
                $root.proto.DeviceCapabilities.LIDMigration.encode(message.lidMigration, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.businessBroadcast != null && Object.hasOwnProperty.call(message, "businessBroadcast"))
                $root.proto.DeviceCapabilities.BusinessBroadcast.encode(message.businessBroadcast, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.userHasAvatar != null && Object.hasOwnProperty.call(message, "userHasAvatar"))
                $root.proto.DeviceCapabilities.UserHasAvatar.encode(message.userHasAvatar, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.memberNameTagPrimarySupport != null && Object.hasOwnProperty.call(message, "memberNameTagPrimarySupport"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.memberNameTagPrimarySupport);
            if (message.aiThread != null && Object.hasOwnProperty.call(message, "aiThread"))
                $root.proto.DeviceCapabilities.AiThread.encode(message.aiThread, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DeviceCapabilities message, length delimited. Does not implicitly {@link proto.DeviceCapabilities.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.DeviceCapabilities
         * @static
         * @param {proto.IDeviceCapabilities} message DeviceCapabilities message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceCapabilities.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DeviceCapabilities message from the specified reader or buffer.
         * @function decode
         * @memberof proto.DeviceCapabilities
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.DeviceCapabilities} DeviceCapabilities
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceCapabilities.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.chatLockSupportLevel = reader.int32();
                        message._chatLockSupportLevel = "chatLockSupportLevel";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.lidMigration = $root.proto.DeviceCapabilities.LIDMigration.decode(reader, reader.uint32(), undefined, q + 1, message.lidMigration);
                        message._lidMigration = "lidMigration";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.businessBroadcast = $root.proto.DeviceCapabilities.BusinessBroadcast.decode(reader, reader.uint32(), undefined, q + 1, message.businessBroadcast);
                        message._businessBroadcast = "businessBroadcast";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.userHasAvatar = $root.proto.DeviceCapabilities.UserHasAvatar.decode(reader, reader.uint32(), undefined, q + 1, message.userHasAvatar);
                        message._userHasAvatar = "userHasAvatar";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.memberNameTagPrimarySupport = reader.int32();
                        message._memberNameTagPrimarySupport = "memberNameTagPrimarySupport";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        message.aiThread = $root.proto.DeviceCapabilities.AiThread.decode(reader, reader.uint32(), undefined, q + 1, message.aiThread);
                        message._aiThread = "aiThread";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a DeviceCapabilities message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.DeviceCapabilities
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.DeviceCapabilities} DeviceCapabilities
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceCapabilities.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DeviceCapabilities message.
         * @function verify
         * @memberof proto.DeviceCapabilities
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DeviceCapabilities.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.chatLockSupportLevel != null && Object.hasOwnProperty.call(message, "chatLockSupportLevel")) {
                properties._chatLockSupportLevel = 1;
                if (typeof message.chatLockSupportLevel !== "number" || (message.chatLockSupportLevel | 0) !== message.chatLockSupportLevel)
                    return "chatLockSupportLevel: enum value expected";
            }
            if (message.lidMigration != null && Object.hasOwnProperty.call(message, "lidMigration")) {
                properties._lidMigration = 1;
                {
                    var error = $root.proto.DeviceCapabilities.LIDMigration.verify(message.lidMigration, q + 1);
                    if (error)
                        return "lidMigration." + error;
                }
            }
            if (message.businessBroadcast != null && Object.hasOwnProperty.call(message, "businessBroadcast")) {
                properties._businessBroadcast = 1;
                {
                    var error = $root.proto.DeviceCapabilities.BusinessBroadcast.verify(message.businessBroadcast, q + 1);
                    if (error)
                        return "businessBroadcast." + error;
                }
            }
            if (message.userHasAvatar != null && Object.hasOwnProperty.call(message, "userHasAvatar")) {
                properties._userHasAvatar = 1;
                {
                    var error = $root.proto.DeviceCapabilities.UserHasAvatar.verify(message.userHasAvatar, q + 1);
                    if (error)
                        return "userHasAvatar." + error;
                }
            }
            if (message.memberNameTagPrimarySupport != null && Object.hasOwnProperty.call(message, "memberNameTagPrimarySupport")) {
                properties._memberNameTagPrimarySupport = 1;
                if (typeof message.memberNameTagPrimarySupport !== "number" || (message.memberNameTagPrimarySupport | 0) !== message.memberNameTagPrimarySupport)
                    return "memberNameTagPrimarySupport: enum value expected";
            }
            if (message.aiThread != null && Object.hasOwnProperty.call(message, "aiThread")) {
                properties._aiThread = 1;
                {
                    var error = $root.proto.DeviceCapabilities.AiThread.verify(message.aiThread, q + 1);
                    if (error)
                        return "aiThread." + error;
                }
            }
            return null;
        };

        /**
         * Creates a DeviceCapabilities message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.DeviceCapabilities
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.DeviceCapabilities} DeviceCapabilities
         */
        DeviceCapabilities.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.DeviceCapabilities: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.chatLockSupportLevel) {
            case "NONE":
            case 0:
                message.chatLockSupportLevel = 0;
                break;
            case "MINIMAL":
            case 1:
                message.chatLockSupportLevel = 1;
                break;
            case "FULL":
            case 2:
                message.chatLockSupportLevel = 2;
                break;
            default:
                if (typeof object.chatLockSupportLevel === "number" && (object.chatLockSupportLevel | 0) === object.chatLockSupportLevel)
                    message.chatLockSupportLevel = object.chatLockSupportLevel;
            }
            if (object.lidMigration != null) {
                if (!$util.isObject(object.lidMigration))
                    throw TypeError(".proto.DeviceCapabilities.lidMigration: object expected");
                message.lidMigration = $root.proto.DeviceCapabilities.LIDMigration.fromObject(object.lidMigration, q + 1);
            }
            if (object.businessBroadcast != null) {
                if (!$util.isObject(object.businessBroadcast))
                    throw TypeError(".proto.DeviceCapabilities.businessBroadcast: object expected");
                message.businessBroadcast = $root.proto.DeviceCapabilities.BusinessBroadcast.fromObject(object.businessBroadcast, q + 1);
            }
            if (object.userHasAvatar != null) {
                if (!$util.isObject(object.userHasAvatar))
                    throw TypeError(".proto.DeviceCapabilities.userHasAvatar: object expected");
                message.userHasAvatar = $root.proto.DeviceCapabilities.UserHasAvatar.fromObject(object.userHasAvatar, q + 1);
            }
            switch (object.memberNameTagPrimarySupport) {
            case "DISABLED":
            case 0:
                message.memberNameTagPrimarySupport = 0;
                break;
            case "RECEIVER_ENABLED":
            case 1:
                message.memberNameTagPrimarySupport = 1;
                break;
            case "SENDER_ENABLED":
            case 2:
                message.memberNameTagPrimarySupport = 2;
                break;
            default:
                if (typeof object.memberNameTagPrimarySupport === "number" && (object.memberNameTagPrimarySupport | 0) === object.memberNameTagPrimarySupport)
                    message.memberNameTagPrimarySupport = object.memberNameTagPrimarySupport;
            }
            if (object.aiThread != null) {
                if (!$util.isObject(object.aiThread))
                    throw TypeError(".proto.DeviceCapabilities.aiThread: object expected");
                message.aiThread = $root.proto.DeviceCapabilities.AiThread.fromObject(object.aiThread, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a DeviceCapabilities message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.DeviceCapabilities
         * @static
         * @param {proto.DeviceCapabilities} message DeviceCapabilities
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeviceCapabilities.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.chatLockSupportLevel != null && Object.hasOwnProperty.call(message, "chatLockSupportLevel"))
                object.chatLockSupportLevel = options.enums === String ? $root.proto.DeviceCapabilities.ChatLockSupportLevel[message.chatLockSupportLevel] === undefined ? message.chatLockSupportLevel : $root.proto.DeviceCapabilities.ChatLockSupportLevel[message.chatLockSupportLevel] : message.chatLockSupportLevel;
            if (message.lidMigration != null && Object.hasOwnProperty.call(message, "lidMigration"))
                object.lidMigration = $root.proto.DeviceCapabilities.LIDMigration.toObject(message.lidMigration, options, q + 1);
            if (message.businessBroadcast != null && Object.hasOwnProperty.call(message, "businessBroadcast"))
                object.businessBroadcast = $root.proto.DeviceCapabilities.BusinessBroadcast.toObject(message.businessBroadcast, options, q + 1);
            if (message.userHasAvatar != null && Object.hasOwnProperty.call(message, "userHasAvatar"))
                object.userHasAvatar = $root.proto.DeviceCapabilities.UserHasAvatar.toObject(message.userHasAvatar, options, q + 1);
            if (message.memberNameTagPrimarySupport != null && Object.hasOwnProperty.call(message, "memberNameTagPrimarySupport"))
                object.memberNameTagPrimarySupport = options.enums === String ? $root.proto.DeviceCapabilities.MemberNameTagPrimarySupport[message.memberNameTagPrimarySupport] === undefined ? message.memberNameTagPrimarySupport : $root.proto.DeviceCapabilities.MemberNameTagPrimarySupport[message.memberNameTagPrimarySupport] : message.memberNameTagPrimarySupport;
            if (message.aiThread != null && Object.hasOwnProperty.call(message, "aiThread"))
                object.aiThread = $root.proto.DeviceCapabilities.AiThread.toObject(message.aiThread, options, q + 1);
            return object;
        };

        /**
         * Converts this DeviceCapabilities to JSON.
         * @function toJSON
         * @memberof proto.DeviceCapabilities
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeviceCapabilities.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DeviceCapabilities
         * @function getTypeUrl
         * @memberof proto.DeviceCapabilities
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DeviceCapabilities.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.DeviceCapabilities";
        };

        DeviceCapabilities.AiThread = (function() {

            /**
             * Properties of an AiThread.
             * @memberof proto.DeviceCapabilities
             * @interface IAiThread
             * @property {proto.DeviceCapabilities.AiThread.SupportLevel|null} [supportLevel] AiThread supportLevel
             */

            /**
             * Constructs a new AiThread.
             * @memberof proto.DeviceCapabilities
             * @classdesc Represents an AiThread.
             * @implements IAiThread
             * @constructor
             * @param {proto.DeviceCapabilities.IAiThread=} [properties] Properties to set
             */
            function AiThread(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AiThread supportLevel.
             * @member {proto.DeviceCapabilities.AiThread.SupportLevel|null|undefined} supportLevel
             * @memberof proto.DeviceCapabilities.AiThread
             * @instance
             */
            AiThread.prototype.supportLevel = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AiThread.prototype, "_supportLevel", {
                get: $util.oneOfGetter($oneOfFields = ["supportLevel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AiThread instance using the specified properties.
             * @function create
             * @memberof proto.DeviceCapabilities.AiThread
             * @static
             * @param {proto.DeviceCapabilities.IAiThread=} [properties] Properties to set
             * @returns {proto.DeviceCapabilities.AiThread} AiThread instance
             */
            AiThread.create = function create(properties) {
                return new AiThread(properties);
            };

            /**
             * Encodes the specified AiThread message. Does not implicitly {@link proto.DeviceCapabilities.AiThread.verify|verify} messages.
             * @function encode
             * @memberof proto.DeviceCapabilities.AiThread
             * @static
             * @param {proto.DeviceCapabilities.IAiThread} message AiThread message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AiThread.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.supportLevel != null && Object.hasOwnProperty.call(message, "supportLevel"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.supportLevel);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AiThread message, length delimited. Does not implicitly {@link proto.DeviceCapabilities.AiThread.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.DeviceCapabilities.AiThread
             * @static
             * @param {proto.DeviceCapabilities.IAiThread} message AiThread message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AiThread.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an AiThread message from the specified reader or buffer.
             * @function decode
             * @memberof proto.DeviceCapabilities.AiThread
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.DeviceCapabilities.AiThread} AiThread
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AiThread.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.supportLevel = reader.int32();
                            message._supportLevel = "supportLevel";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an AiThread message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.DeviceCapabilities.AiThread
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.DeviceCapabilities.AiThread} AiThread
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AiThread.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AiThread message.
             * @function verify
             * @memberof proto.DeviceCapabilities.AiThread
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AiThread.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.supportLevel != null && Object.hasOwnProperty.call(message, "supportLevel")) {
                    properties._supportLevel = 1;
                    if (typeof message.supportLevel !== "number" || (message.supportLevel | 0) !== message.supportLevel)
                        return "supportLevel: enum value expected";
                }
                return null;
            };

            /**
             * Creates an AiThread message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.DeviceCapabilities.AiThread
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.DeviceCapabilities.AiThread} AiThread
             */
            AiThread.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.DeviceCapabilities.AiThread: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.supportLevel) {
                case "NONE":
                case 0:
                    message.supportLevel = 0;
                    break;
                case "INFRA":
                case 1:
                    message.supportLevel = 1;
                    break;
                case "FULL":
                case 2:
                    message.supportLevel = 2;
                    break;
                default:
                    if (typeof object.supportLevel === "number" && (object.supportLevel | 0) === object.supportLevel)
                        message.supportLevel = object.supportLevel;
                }
                return message;
            };

            /**
             * Creates a plain object from an AiThread message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.DeviceCapabilities.AiThread
             * @static
             * @param {proto.DeviceCapabilities.AiThread} message AiThread
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AiThread.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.supportLevel != null && Object.hasOwnProperty.call(message, "supportLevel"))
                    object.supportLevel = options.enums === String ? $root.proto.DeviceCapabilities.AiThread.SupportLevel[message.supportLevel] === undefined ? message.supportLevel : $root.proto.DeviceCapabilities.AiThread.SupportLevel[message.supportLevel] : message.supportLevel;
                return object;
            };

            /**
             * Converts this AiThread to JSON.
             * @function toJSON
             * @memberof proto.DeviceCapabilities.AiThread
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AiThread.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AiThread
             * @function getTypeUrl
             * @memberof proto.DeviceCapabilities.AiThread
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AiThread.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.DeviceCapabilities.AiThread";
            };

            /**
             * SupportLevel enum.
             * @name proto.DeviceCapabilities.AiThread.SupportLevel
             * @enum {number}
             * @property {number} NONE=0 NONE value
             * @property {number} INFRA=1 INFRA value
             * @property {number} FULL=2 FULL value
             */
            AiThread.SupportLevel = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "NONE"] = 0;
                values[valuesById[1] = "INFRA"] = 1;
                values[valuesById[2] = "FULL"] = 2;
                return values;
            })();

            return AiThread;
        })();

        DeviceCapabilities.BusinessBroadcast = (function() {

            /**
             * Properties of a BusinessBroadcast.
             * @memberof proto.DeviceCapabilities
             * @interface IBusinessBroadcast
             * @property {boolean|null} [importListEnabled] BusinessBroadcast importListEnabled
             * @property {boolean|null} [companionSupportEnabled] BusinessBroadcast companionSupportEnabled
             * @property {boolean|null} [campaignSyncEnabled] BusinessBroadcast campaignSyncEnabled
             * @property {boolean|null} [insightsSyncEnabled] BusinessBroadcast insightsSyncEnabled
             * @property {number|null} [recipientLimit] BusinessBroadcast recipientLimit
             */

            /**
             * Constructs a new BusinessBroadcast.
             * @memberof proto.DeviceCapabilities
             * @classdesc Represents a BusinessBroadcast.
             * @implements IBusinessBroadcast
             * @constructor
             * @param {proto.DeviceCapabilities.IBusinessBroadcast=} [properties] Properties to set
             */
            function BusinessBroadcast(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * BusinessBroadcast importListEnabled.
             * @member {boolean|null|undefined} importListEnabled
             * @memberof proto.DeviceCapabilities.BusinessBroadcast
             * @instance
             */
            BusinessBroadcast.prototype.importListEnabled = null;

            /**
             * BusinessBroadcast companionSupportEnabled.
             * @member {boolean|null|undefined} companionSupportEnabled
             * @memberof proto.DeviceCapabilities.BusinessBroadcast
             * @instance
             */
            BusinessBroadcast.prototype.companionSupportEnabled = null;

            /**
             * BusinessBroadcast campaignSyncEnabled.
             * @member {boolean|null|undefined} campaignSyncEnabled
             * @memberof proto.DeviceCapabilities.BusinessBroadcast
             * @instance
             */
            BusinessBroadcast.prototype.campaignSyncEnabled = null;

            /**
             * BusinessBroadcast insightsSyncEnabled.
             * @member {boolean|null|undefined} insightsSyncEnabled
             * @memberof proto.DeviceCapabilities.BusinessBroadcast
             * @instance
             */
            BusinessBroadcast.prototype.insightsSyncEnabled = null;

            /**
             * BusinessBroadcast recipientLimit.
             * @member {number|null|undefined} recipientLimit
             * @memberof proto.DeviceCapabilities.BusinessBroadcast
             * @instance
             */
            BusinessBroadcast.prototype.recipientLimit = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcast.prototype, "_importListEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["importListEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcast.prototype, "_companionSupportEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["companionSupportEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcast.prototype, "_campaignSyncEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["campaignSyncEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcast.prototype, "_insightsSyncEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["insightsSyncEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcast.prototype, "_recipientLimit", {
                get: $util.oneOfGetter($oneOfFields = ["recipientLimit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BusinessBroadcast instance using the specified properties.
             * @function create
             * @memberof proto.DeviceCapabilities.BusinessBroadcast
             * @static
             * @param {proto.DeviceCapabilities.IBusinessBroadcast=} [properties] Properties to set
             * @returns {proto.DeviceCapabilities.BusinessBroadcast} BusinessBroadcast instance
             */
            BusinessBroadcast.create = function create(properties) {
                return new BusinessBroadcast(properties);
            };

            /**
             * Encodes the specified BusinessBroadcast message. Does not implicitly {@link proto.DeviceCapabilities.BusinessBroadcast.verify|verify} messages.
             * @function encode
             * @memberof proto.DeviceCapabilities.BusinessBroadcast
             * @static
             * @param {proto.DeviceCapabilities.IBusinessBroadcast} message BusinessBroadcast message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BusinessBroadcast.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.importListEnabled != null && Object.hasOwnProperty.call(message, "importListEnabled"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.importListEnabled);
                if (message.companionSupportEnabled != null && Object.hasOwnProperty.call(message, "companionSupportEnabled"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.companionSupportEnabled);
                if (message.campaignSyncEnabled != null && Object.hasOwnProperty.call(message, "campaignSyncEnabled"))
                    writer.uint32(/* id 3, wireType 0 =*/24).bool(message.campaignSyncEnabled);
                if (message.insightsSyncEnabled != null && Object.hasOwnProperty.call(message, "insightsSyncEnabled"))
                    writer.uint32(/* id 4, wireType 0 =*/32).bool(message.insightsSyncEnabled);
                if (message.recipientLimit != null && Object.hasOwnProperty.call(message, "recipientLimit"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int32(message.recipientLimit);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified BusinessBroadcast message, length delimited. Does not implicitly {@link proto.DeviceCapabilities.BusinessBroadcast.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.DeviceCapabilities.BusinessBroadcast
             * @static
             * @param {proto.DeviceCapabilities.IBusinessBroadcast} message BusinessBroadcast message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BusinessBroadcast.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a BusinessBroadcast message from the specified reader or buffer.
             * @function decode
             * @memberof proto.DeviceCapabilities.BusinessBroadcast
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.DeviceCapabilities.BusinessBroadcast} BusinessBroadcast
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BusinessBroadcast.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.importListEnabled = reader.bool();
                            message._importListEnabled = "importListEnabled";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.companionSupportEnabled = reader.bool();
                            message._companionSupportEnabled = "companionSupportEnabled";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.campaignSyncEnabled = reader.bool();
                            message._campaignSyncEnabled = "campaignSyncEnabled";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.insightsSyncEnabled = reader.bool();
                            message._insightsSyncEnabled = "insightsSyncEnabled";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.recipientLimit = reader.int32();
                            message._recipientLimit = "recipientLimit";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a BusinessBroadcast message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.DeviceCapabilities.BusinessBroadcast
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.DeviceCapabilities.BusinessBroadcast} BusinessBroadcast
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BusinessBroadcast.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BusinessBroadcast message.
             * @function verify
             * @memberof proto.DeviceCapabilities.BusinessBroadcast
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BusinessBroadcast.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.importListEnabled != null && Object.hasOwnProperty.call(message, "importListEnabled")) {
                    properties._importListEnabled = 1;
                    if (typeof message.importListEnabled !== "boolean")
                        return "importListEnabled: boolean expected";
                }
                if (message.companionSupportEnabled != null && Object.hasOwnProperty.call(message, "companionSupportEnabled")) {
                    properties._companionSupportEnabled = 1;
                    if (typeof message.companionSupportEnabled !== "boolean")
                        return "companionSupportEnabled: boolean expected";
                }
                if (message.campaignSyncEnabled != null && Object.hasOwnProperty.call(message, "campaignSyncEnabled")) {
                    properties._campaignSyncEnabled = 1;
                    if (typeof message.campaignSyncEnabled !== "boolean")
                        return "campaignSyncEnabled: boolean expected";
                }
                if (message.insightsSyncEnabled != null && Object.hasOwnProperty.call(message, "insightsSyncEnabled")) {
                    properties._insightsSyncEnabled = 1;
                    if (typeof message.insightsSyncEnabled !== "boolean")
                        return "insightsSyncEnabled: boolean expected";
                }
                if (message.recipientLimit != null && Object.hasOwnProperty.call(message, "recipientLimit")) {
                    properties._recipientLimit = 1;
                    if (!$util.isInteger(message.recipientLimit))
                        return "recipientLimit: integer expected";
                }
                return null;
            };

            /**
             * Creates a BusinessBroadcast message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.DeviceCapabilities.BusinessBroadcast
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.DeviceCapabilities.BusinessBroadcast} BusinessBroadcast
             */
            BusinessBroadcast.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.DeviceCapabilities.BusinessBroadcast: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.importListEnabled != null)
                    message.importListEnabled = Boolean(object.importListEnabled);
                if (object.companionSupportEnabled != null)
                    message.companionSupportEnabled = Boolean(object.companionSupportEnabled);
                if (object.campaignSyncEnabled != null)
                    message.campaignSyncEnabled = Boolean(object.campaignSyncEnabled);
                if (object.insightsSyncEnabled != null)
                    message.insightsSyncEnabled = Boolean(object.insightsSyncEnabled);
                if (object.recipientLimit != null)
                    message.recipientLimit = object.recipientLimit | 0;
                return message;
            };

            /**
             * Creates a plain object from a BusinessBroadcast message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.DeviceCapabilities.BusinessBroadcast
             * @static
             * @param {proto.DeviceCapabilities.BusinessBroadcast} message BusinessBroadcast
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BusinessBroadcast.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.importListEnabled != null && Object.hasOwnProperty.call(message, "importListEnabled"))
                    object.importListEnabled = message.importListEnabled;
                if (message.companionSupportEnabled != null && Object.hasOwnProperty.call(message, "companionSupportEnabled"))
                    object.companionSupportEnabled = message.companionSupportEnabled;
                if (message.campaignSyncEnabled != null && Object.hasOwnProperty.call(message, "campaignSyncEnabled"))
                    object.campaignSyncEnabled = message.campaignSyncEnabled;
                if (message.insightsSyncEnabled != null && Object.hasOwnProperty.call(message, "insightsSyncEnabled"))
                    object.insightsSyncEnabled = message.insightsSyncEnabled;
                if (message.recipientLimit != null && Object.hasOwnProperty.call(message, "recipientLimit"))
                    object.recipientLimit = message.recipientLimit;
                return object;
            };

            /**
             * Converts this BusinessBroadcast to JSON.
             * @function toJSON
             * @memberof proto.DeviceCapabilities.BusinessBroadcast
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BusinessBroadcast.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for BusinessBroadcast
             * @function getTypeUrl
             * @memberof proto.DeviceCapabilities.BusinessBroadcast
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            BusinessBroadcast.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.DeviceCapabilities.BusinessBroadcast";
            };

            return BusinessBroadcast;
        })();

        /**
         * ChatLockSupportLevel enum.
         * @name proto.DeviceCapabilities.ChatLockSupportLevel
         * @enum {number}
         * @property {number} NONE=0 NONE value
         * @property {number} MINIMAL=1 MINIMAL value
         * @property {number} FULL=2 FULL value
         */
        DeviceCapabilities.ChatLockSupportLevel = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "NONE"] = 0;
            values[valuesById[1] = "MINIMAL"] = 1;
            values[valuesById[2] = "FULL"] = 2;
            return values;
        })();

        DeviceCapabilities.LIDMigration = (function() {

            /**
             * Properties of a LIDMigration.
             * @memberof proto.DeviceCapabilities
             * @interface ILIDMigration
             * @property {number|Long|null} [chatDbMigrationTimestamp] LIDMigration chatDbMigrationTimestamp
             */

            /**
             * Constructs a new LIDMigration.
             * @memberof proto.DeviceCapabilities
             * @classdesc Represents a LIDMigration.
             * @implements ILIDMigration
             * @constructor
             * @param {proto.DeviceCapabilities.ILIDMigration=} [properties] Properties to set
             */
            function LIDMigration(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * LIDMigration chatDbMigrationTimestamp.
             * @member {number|Long|null|undefined} chatDbMigrationTimestamp
             * @memberof proto.DeviceCapabilities.LIDMigration
             * @instance
             */
            LIDMigration.prototype.chatDbMigrationTimestamp = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LIDMigration.prototype, "_chatDbMigrationTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["chatDbMigrationTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new LIDMigration instance using the specified properties.
             * @function create
             * @memberof proto.DeviceCapabilities.LIDMigration
             * @static
             * @param {proto.DeviceCapabilities.ILIDMigration=} [properties] Properties to set
             * @returns {proto.DeviceCapabilities.LIDMigration} LIDMigration instance
             */
            LIDMigration.create = function create(properties) {
                return new LIDMigration(properties);
            };

            /**
             * Encodes the specified LIDMigration message. Does not implicitly {@link proto.DeviceCapabilities.LIDMigration.verify|verify} messages.
             * @function encode
             * @memberof proto.DeviceCapabilities.LIDMigration
             * @static
             * @param {proto.DeviceCapabilities.ILIDMigration} message LIDMigration message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LIDMigration.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.chatDbMigrationTimestamp != null && Object.hasOwnProperty.call(message, "chatDbMigrationTimestamp"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.chatDbMigrationTimestamp);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LIDMigration message, length delimited. Does not implicitly {@link proto.DeviceCapabilities.LIDMigration.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.DeviceCapabilities.LIDMigration
             * @static
             * @param {proto.DeviceCapabilities.ILIDMigration} message LIDMigration message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LIDMigration.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a LIDMigration message from the specified reader or buffer.
             * @function decode
             * @memberof proto.DeviceCapabilities.LIDMigration
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.DeviceCapabilities.LIDMigration} LIDMigration
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LIDMigration.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.chatDbMigrationTimestamp = reader.uint64();
                            message._chatDbMigrationTimestamp = "chatDbMigrationTimestamp";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a LIDMigration message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.DeviceCapabilities.LIDMigration
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.DeviceCapabilities.LIDMigration} LIDMigration
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LIDMigration.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LIDMigration message.
             * @function verify
             * @memberof proto.DeviceCapabilities.LIDMigration
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LIDMigration.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.chatDbMigrationTimestamp != null && Object.hasOwnProperty.call(message, "chatDbMigrationTimestamp")) {
                    properties._chatDbMigrationTimestamp = 1;
                    if (!$util.isInteger(message.chatDbMigrationTimestamp) && !(message.chatDbMigrationTimestamp && $util.isInteger(message.chatDbMigrationTimestamp.low) && $util.isInteger(message.chatDbMigrationTimestamp.high)))
                        return "chatDbMigrationTimestamp: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a LIDMigration message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.DeviceCapabilities.LIDMigration
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.DeviceCapabilities.LIDMigration} LIDMigration
             */
            LIDMigration.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.DeviceCapabilities.LIDMigration: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.chatDbMigrationTimestamp != null)
                    if ($util.Long)
                        message.chatDbMigrationTimestamp = $util.Long.fromValue(object.chatDbMigrationTimestamp, true);
                    else if (typeof object.chatDbMigrationTimestamp === "string")
                        message.chatDbMigrationTimestamp = parseInt(object.chatDbMigrationTimestamp, 10);
                    else if (typeof object.chatDbMigrationTimestamp === "number")
                        message.chatDbMigrationTimestamp = object.chatDbMigrationTimestamp;
                    else if (typeof object.chatDbMigrationTimestamp === "object")
                        message.chatDbMigrationTimestamp = new $util.LongBits(object.chatDbMigrationTimestamp.low >>> 0, object.chatDbMigrationTimestamp.high >>> 0).toNumber(true);
                return message;
            };

            /**
             * Creates a plain object from a LIDMigration message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.DeviceCapabilities.LIDMigration
             * @static
             * @param {proto.DeviceCapabilities.LIDMigration} message LIDMigration
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LIDMigration.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.chatDbMigrationTimestamp != null && Object.hasOwnProperty.call(message, "chatDbMigrationTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.chatDbMigrationTimestamp = typeof message.chatDbMigrationTimestamp === "number" ? BigInt(message.chatDbMigrationTimestamp) : $util.Long.fromBits(message.chatDbMigrationTimestamp.low >>> 0, message.chatDbMigrationTimestamp.high >>> 0, true).toBigInt();
                    else if (typeof message.chatDbMigrationTimestamp === "number")
                        object.chatDbMigrationTimestamp = options.longs === String ? String(message.chatDbMigrationTimestamp) : message.chatDbMigrationTimestamp;
                    else
                        object.chatDbMigrationTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.chatDbMigrationTimestamp) : options.longs === Number ? new $util.LongBits(message.chatDbMigrationTimestamp.low >>> 0, message.chatDbMigrationTimestamp.high >>> 0).toNumber(true) : message.chatDbMigrationTimestamp;
                return object;
            };

            /**
             * Converts this LIDMigration to JSON.
             * @function toJSON
             * @memberof proto.DeviceCapabilities.LIDMigration
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LIDMigration.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for LIDMigration
             * @function getTypeUrl
             * @memberof proto.DeviceCapabilities.LIDMigration
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            LIDMigration.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.DeviceCapabilities.LIDMigration";
            };

            return LIDMigration;
        })();

        /**
         * MemberNameTagPrimarySupport enum.
         * @name proto.DeviceCapabilities.MemberNameTagPrimarySupport
         * @enum {number}
         * @property {number} DISABLED=0 DISABLED value
         * @property {number} RECEIVER_ENABLED=1 RECEIVER_ENABLED value
         * @property {number} SENDER_ENABLED=2 SENDER_ENABLED value
         */
        DeviceCapabilities.MemberNameTagPrimarySupport = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "DISABLED"] = 0;
            values[valuesById[1] = "RECEIVER_ENABLED"] = 1;
            values[valuesById[2] = "SENDER_ENABLED"] = 2;
            return values;
        })();

        DeviceCapabilities.UserHasAvatar = (function() {

            /**
             * Properties of a UserHasAvatar.
             * @memberof proto.DeviceCapabilities
             * @interface IUserHasAvatar
             * @property {boolean|null} [userHasAvatar] UserHasAvatar userHasAvatar
             */

            /**
             * Constructs a new UserHasAvatar.
             * @memberof proto.DeviceCapabilities
             * @classdesc Represents a UserHasAvatar.
             * @implements IUserHasAvatar
             * @constructor
             * @param {proto.DeviceCapabilities.IUserHasAvatar=} [properties] Properties to set
             */
            function UserHasAvatar(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * UserHasAvatar userHasAvatar.
             * @member {boolean|null|undefined} userHasAvatar
             * @memberof proto.DeviceCapabilities.UserHasAvatar
             * @instance
             */
            UserHasAvatar.prototype.userHasAvatar = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UserHasAvatar.prototype, "_userHasAvatar", {
                get: $util.oneOfGetter($oneOfFields = ["userHasAvatar"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new UserHasAvatar instance using the specified properties.
             * @function create
             * @memberof proto.DeviceCapabilities.UserHasAvatar
             * @static
             * @param {proto.DeviceCapabilities.IUserHasAvatar=} [properties] Properties to set
             * @returns {proto.DeviceCapabilities.UserHasAvatar} UserHasAvatar instance
             */
            UserHasAvatar.create = function create(properties) {
                return new UserHasAvatar(properties);
            };

            /**
             * Encodes the specified UserHasAvatar message. Does not implicitly {@link proto.DeviceCapabilities.UserHasAvatar.verify|verify} messages.
             * @function encode
             * @memberof proto.DeviceCapabilities.UserHasAvatar
             * @static
             * @param {proto.DeviceCapabilities.IUserHasAvatar} message UserHasAvatar message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UserHasAvatar.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.userHasAvatar != null && Object.hasOwnProperty.call(message, "userHasAvatar"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.userHasAvatar);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified UserHasAvatar message, length delimited. Does not implicitly {@link proto.DeviceCapabilities.UserHasAvatar.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.DeviceCapabilities.UserHasAvatar
             * @static
             * @param {proto.DeviceCapabilities.IUserHasAvatar} message UserHasAvatar message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UserHasAvatar.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a UserHasAvatar message from the specified reader or buffer.
             * @function decode
             * @memberof proto.DeviceCapabilities.UserHasAvatar
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.DeviceCapabilities.UserHasAvatar} UserHasAvatar
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UserHasAvatar.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.userHasAvatar = reader.bool();
                            message._userHasAvatar = "userHasAvatar";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a UserHasAvatar message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.DeviceCapabilities.UserHasAvatar
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.DeviceCapabilities.UserHasAvatar} UserHasAvatar
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UserHasAvatar.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a UserHasAvatar message.
             * @function verify
             * @memberof proto.DeviceCapabilities.UserHasAvatar
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            UserHasAvatar.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.userHasAvatar != null && Object.hasOwnProperty.call(message, "userHasAvatar")) {
                    properties._userHasAvatar = 1;
                    if (typeof message.userHasAvatar !== "boolean")
                        return "userHasAvatar: boolean expected";
                }
                return null;
            };

            /**
             * Creates a UserHasAvatar message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.DeviceCapabilities.UserHasAvatar
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.DeviceCapabilities.UserHasAvatar} UserHasAvatar
             */
            UserHasAvatar.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.DeviceCapabilities.UserHasAvatar: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.userHasAvatar != null)
                    message.userHasAvatar = Boolean(object.userHasAvatar);
                return message;
            };

            /**
             * Creates a plain object from a UserHasAvatar message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.DeviceCapabilities.UserHasAvatar
             * @static
             * @param {proto.DeviceCapabilities.UserHasAvatar} message UserHasAvatar
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            UserHasAvatar.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.userHasAvatar != null && Object.hasOwnProperty.call(message, "userHasAvatar"))
                    object.userHasAvatar = message.userHasAvatar;
                return object;
            };

            /**
             * Converts this UserHasAvatar to JSON.
             * @function toJSON
             * @memberof proto.DeviceCapabilities.UserHasAvatar
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            UserHasAvatar.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for UserHasAvatar
             * @function getTypeUrl
             * @memberof proto.DeviceCapabilities.UserHasAvatar
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            UserHasAvatar.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.DeviceCapabilities.UserHasAvatar";
            };

            return UserHasAvatar;
        })();

        return DeviceCapabilities;
    })();

    proto.DeviceConsistencyCodeMessage = (function() {

        /**
         * Properties of a DeviceConsistencyCodeMessage.
         * @memberof proto
         * @interface IDeviceConsistencyCodeMessage
         * @property {number|null} [generation] DeviceConsistencyCodeMessage generation
         * @property {Uint8Array|null} [signature] DeviceConsistencyCodeMessage signature
         */

        /**
         * Constructs a new DeviceConsistencyCodeMessage.
         * @memberof proto
         * @classdesc Represents a DeviceConsistencyCodeMessage.
         * @implements IDeviceConsistencyCodeMessage
         * @constructor
         * @param {proto.IDeviceConsistencyCodeMessage=} [properties] Properties to set
         */
        function DeviceConsistencyCodeMessage(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DeviceConsistencyCodeMessage generation.
         * @member {number|null|undefined} generation
         * @memberof proto.DeviceConsistencyCodeMessage
         * @instance
         */
        DeviceConsistencyCodeMessage.prototype.generation = null;

        /**
         * DeviceConsistencyCodeMessage signature.
         * @member {Uint8Array|null|undefined} signature
         * @memberof proto.DeviceConsistencyCodeMessage
         * @instance
         */
        DeviceConsistencyCodeMessage.prototype.signature = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceConsistencyCodeMessage.prototype, "_generation", {
            get: $util.oneOfGetter($oneOfFields = ["generation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceConsistencyCodeMessage.prototype, "_signature", {
            get: $util.oneOfGetter($oneOfFields = ["signature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new DeviceConsistencyCodeMessage instance using the specified properties.
         * @function create
         * @memberof proto.DeviceConsistencyCodeMessage
         * @static
         * @param {proto.IDeviceConsistencyCodeMessage=} [properties] Properties to set
         * @returns {proto.DeviceConsistencyCodeMessage} DeviceConsistencyCodeMessage instance
         */
        DeviceConsistencyCodeMessage.create = function create(properties) {
            return new DeviceConsistencyCodeMessage(properties);
        };

        /**
         * Encodes the specified DeviceConsistencyCodeMessage message. Does not implicitly {@link proto.DeviceConsistencyCodeMessage.verify|verify} messages.
         * @function encode
         * @memberof proto.DeviceConsistencyCodeMessage
         * @static
         * @param {proto.IDeviceConsistencyCodeMessage} message DeviceConsistencyCodeMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceConsistencyCodeMessage.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.generation != null && Object.hasOwnProperty.call(message, "generation"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.generation);
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.signature);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DeviceConsistencyCodeMessage message, length delimited. Does not implicitly {@link proto.DeviceConsistencyCodeMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.DeviceConsistencyCodeMessage
         * @static
         * @param {proto.IDeviceConsistencyCodeMessage} message DeviceConsistencyCodeMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceConsistencyCodeMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DeviceConsistencyCodeMessage message from the specified reader or buffer.
         * @function decode
         * @memberof proto.DeviceConsistencyCodeMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.DeviceConsistencyCodeMessage} DeviceConsistencyCodeMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceConsistencyCodeMessage.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.generation = reader.uint32();
                        message._generation = "generation";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.signature = reader.bytes();
                        message._signature = "signature";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a DeviceConsistencyCodeMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.DeviceConsistencyCodeMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.DeviceConsistencyCodeMessage} DeviceConsistencyCodeMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceConsistencyCodeMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DeviceConsistencyCodeMessage message.
         * @function verify
         * @memberof proto.DeviceConsistencyCodeMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DeviceConsistencyCodeMessage.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.generation != null && Object.hasOwnProperty.call(message, "generation")) {
                properties._generation = 1;
                if (!$util.isInteger(message.generation))
                    return "generation: integer expected";
            }
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature")) {
                properties._signature = 1;
                if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                    return "signature: buffer expected";
            }
            return null;
        };

        /**
         * Creates a DeviceConsistencyCodeMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.DeviceConsistencyCodeMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.DeviceConsistencyCodeMessage} DeviceConsistencyCodeMessage
         */
        DeviceConsistencyCodeMessage.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.DeviceConsistencyCodeMessage: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.generation != null)
                message.generation = object.generation >>> 0;
            if (object.signature != null)
                if (typeof object.signature === "string")
                    $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                else if (object.signature.length >= 0)
                    message.signature = object.signature;
            return message;
        };

        /**
         * Creates a plain object from a DeviceConsistencyCodeMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.DeviceConsistencyCodeMessage
         * @static
         * @param {proto.DeviceConsistencyCodeMessage} message DeviceConsistencyCodeMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeviceConsistencyCodeMessage.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.generation != null && Object.hasOwnProperty.call(message, "generation"))
                object.generation = message.generation;
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                object.signature = options.bytes === String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === Array ? Array.prototype.slice.call(message.signature) : message.signature;
            return object;
        };

        /**
         * Converts this DeviceConsistencyCodeMessage to JSON.
         * @function toJSON
         * @memberof proto.DeviceConsistencyCodeMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeviceConsistencyCodeMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DeviceConsistencyCodeMessage
         * @function getTypeUrl
         * @memberof proto.DeviceConsistencyCodeMessage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DeviceConsistencyCodeMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.DeviceConsistencyCodeMessage";
        };

        return DeviceConsistencyCodeMessage;
    })();

    proto.DeviceListMetadata = (function() {

        /**
         * Properties of a DeviceListMetadata.
         * @memberof proto
         * @interface IDeviceListMetadata
         * @property {Uint8Array|null} [senderKeyHash] DeviceListMetadata senderKeyHash
         * @property {number|Long|null} [senderTimestamp] DeviceListMetadata senderTimestamp
         * @property {Array.<number>|null} [senderKeyIndexes] DeviceListMetadata senderKeyIndexes
         * @property {proto.ADVEncryptionType|null} [senderAccountType] DeviceListMetadata senderAccountType
         * @property {proto.ADVEncryptionType|null} [receiverAccountType] DeviceListMetadata receiverAccountType
         * @property {Uint8Array|null} [recipientKeyHash] DeviceListMetadata recipientKeyHash
         * @property {number|Long|null} [recipientTimestamp] DeviceListMetadata recipientTimestamp
         * @property {Array.<number>|null} [recipientKeyIndexes] DeviceListMetadata recipientKeyIndexes
         */

        /**
         * Constructs a new DeviceListMetadata.
         * @memberof proto
         * @classdesc Represents a DeviceListMetadata.
         * @implements IDeviceListMetadata
         * @constructor
         * @param {proto.IDeviceListMetadata=} [properties] Properties to set
         */
        function DeviceListMetadata(properties) {
            this.senderKeyIndexes = [];
            this.recipientKeyIndexes = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DeviceListMetadata senderKeyHash.
         * @member {Uint8Array|null|undefined} senderKeyHash
         * @memberof proto.DeviceListMetadata
         * @instance
         */
        DeviceListMetadata.prototype.senderKeyHash = null;

        /**
         * DeviceListMetadata senderTimestamp.
         * @member {number|Long|null|undefined} senderTimestamp
         * @memberof proto.DeviceListMetadata
         * @instance
         */
        DeviceListMetadata.prototype.senderTimestamp = null;

        /**
         * DeviceListMetadata senderKeyIndexes.
         * @member {Array.<number>} senderKeyIndexes
         * @memberof proto.DeviceListMetadata
         * @instance
         */
        DeviceListMetadata.prototype.senderKeyIndexes = $util.emptyArray;

        /**
         * DeviceListMetadata senderAccountType.
         * @member {proto.ADVEncryptionType|null|undefined} senderAccountType
         * @memberof proto.DeviceListMetadata
         * @instance
         */
        DeviceListMetadata.prototype.senderAccountType = null;

        /**
         * DeviceListMetadata receiverAccountType.
         * @member {proto.ADVEncryptionType|null|undefined} receiverAccountType
         * @memberof proto.DeviceListMetadata
         * @instance
         */
        DeviceListMetadata.prototype.receiverAccountType = null;

        /**
         * DeviceListMetadata recipientKeyHash.
         * @member {Uint8Array|null|undefined} recipientKeyHash
         * @memberof proto.DeviceListMetadata
         * @instance
         */
        DeviceListMetadata.prototype.recipientKeyHash = null;

        /**
         * DeviceListMetadata recipientTimestamp.
         * @member {number|Long|null|undefined} recipientTimestamp
         * @memberof proto.DeviceListMetadata
         * @instance
         */
        DeviceListMetadata.prototype.recipientTimestamp = null;

        /**
         * DeviceListMetadata recipientKeyIndexes.
         * @member {Array.<number>} recipientKeyIndexes
         * @memberof proto.DeviceListMetadata
         * @instance
         */
        DeviceListMetadata.prototype.recipientKeyIndexes = $util.emptyArray;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceListMetadata.prototype, "_senderKeyHash", {
            get: $util.oneOfGetter($oneOfFields = ["senderKeyHash"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceListMetadata.prototype, "_senderTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["senderTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceListMetadata.prototype, "_senderAccountType", {
            get: $util.oneOfGetter($oneOfFields = ["senderAccountType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceListMetadata.prototype, "_receiverAccountType", {
            get: $util.oneOfGetter($oneOfFields = ["receiverAccountType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceListMetadata.prototype, "_recipientKeyHash", {
            get: $util.oneOfGetter($oneOfFields = ["recipientKeyHash"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceListMetadata.prototype, "_recipientTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["recipientTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new DeviceListMetadata instance using the specified properties.
         * @function create
         * @memberof proto.DeviceListMetadata
         * @static
         * @param {proto.IDeviceListMetadata=} [properties] Properties to set
         * @returns {proto.DeviceListMetadata} DeviceListMetadata instance
         */
        DeviceListMetadata.create = function create(properties) {
            return new DeviceListMetadata(properties);
        };

        /**
         * Encodes the specified DeviceListMetadata message. Does not implicitly {@link proto.DeviceListMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.DeviceListMetadata
         * @static
         * @param {proto.IDeviceListMetadata} message DeviceListMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceListMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.senderKeyHash != null && Object.hasOwnProperty.call(message, "senderKeyHash"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.senderKeyHash);
            if (message.senderTimestamp != null && Object.hasOwnProperty.call(message, "senderTimestamp"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.senderTimestamp);
            if (message.senderKeyIndexes != null && message.senderKeyIndexes.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (var i = 0; i < message.senderKeyIndexes.length; ++i)
                    writer.uint32(message.senderKeyIndexes[i]);
                writer.ldelim();
            }
            if (message.senderAccountType != null && Object.hasOwnProperty.call(message, "senderAccountType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.senderAccountType);
            if (message.receiverAccountType != null && Object.hasOwnProperty.call(message, "receiverAccountType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.receiverAccountType);
            if (message.recipientKeyHash != null && Object.hasOwnProperty.call(message, "recipientKeyHash"))
                writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.recipientKeyHash);
            if (message.recipientTimestamp != null && Object.hasOwnProperty.call(message, "recipientTimestamp"))
                writer.uint32(/* id 9, wireType 0 =*/72).uint64(message.recipientTimestamp);
            if (message.recipientKeyIndexes != null && message.recipientKeyIndexes.length) {
                writer.uint32(/* id 10, wireType 2 =*/82).fork();
                for (var i = 0; i < message.recipientKeyIndexes.length; ++i)
                    writer.uint32(message.recipientKeyIndexes[i]);
                writer.ldelim();
            }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DeviceListMetadata message, length delimited. Does not implicitly {@link proto.DeviceListMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.DeviceListMetadata
         * @static
         * @param {proto.IDeviceListMetadata} message DeviceListMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceListMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DeviceListMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.DeviceListMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.DeviceListMetadata} DeviceListMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceListMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.senderKeyHash = reader.bytes();
                        message._senderKeyHash = "senderKeyHash";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.senderTimestamp = reader.uint64();
                        message._senderTimestamp = "senderTimestamp";
                        continue;
                    }
                case 3: {
                        if (u === 2) {
                            if (!(message.senderKeyIndexes && message.senderKeyIndexes.length))
                                message.senderKeyIndexes = [];
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.senderKeyIndexes.push(reader.uint32());
                            continue;
                        }
                        if (u !== 0)
                            break;
                        if (!(message.senderKeyIndexes && message.senderKeyIndexes.length))
                            message.senderKeyIndexes = [];
                        message.senderKeyIndexes.push(reader.uint32());
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.senderAccountType = reader.int32();
                        message._senderAccountType = "senderAccountType";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.receiverAccountType = reader.int32();
                        message._receiverAccountType = "receiverAccountType";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        message.recipientKeyHash = reader.bytes();
                        message._recipientKeyHash = "recipientKeyHash";
                        continue;
                    }
                case 9: {
                        if (u !== 0)
                            break;
                        message.recipientTimestamp = reader.uint64();
                        message._recipientTimestamp = "recipientTimestamp";
                        continue;
                    }
                case 10: {
                        if (u === 2) {
                            if (!(message.recipientKeyIndexes && message.recipientKeyIndexes.length))
                                message.recipientKeyIndexes = [];
                            var end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.recipientKeyIndexes.push(reader.uint32());
                            continue;
                        }
                        if (u !== 0)
                            break;
                        if (!(message.recipientKeyIndexes && message.recipientKeyIndexes.length))
                            message.recipientKeyIndexes = [];
                        message.recipientKeyIndexes.push(reader.uint32());
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a DeviceListMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.DeviceListMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.DeviceListMetadata} DeviceListMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceListMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DeviceListMetadata message.
         * @function verify
         * @memberof proto.DeviceListMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DeviceListMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.senderKeyHash != null && Object.hasOwnProperty.call(message, "senderKeyHash")) {
                properties._senderKeyHash = 1;
                if (!(message.senderKeyHash && typeof message.senderKeyHash.length === "number" || $util.isString(message.senderKeyHash)))
                    return "senderKeyHash: buffer expected";
            }
            if (message.senderTimestamp != null && Object.hasOwnProperty.call(message, "senderTimestamp")) {
                properties._senderTimestamp = 1;
                if (!$util.isInteger(message.senderTimestamp) && !(message.senderTimestamp && $util.isInteger(message.senderTimestamp.low) && $util.isInteger(message.senderTimestamp.high)))
                    return "senderTimestamp: integer|Long expected";
            }
            if (message.senderKeyIndexes != null && Object.hasOwnProperty.call(message, "senderKeyIndexes")) {
                if (!Array.isArray(message.senderKeyIndexes))
                    return "senderKeyIndexes: array expected";
                for (var i = 0; i < message.senderKeyIndexes.length; ++i)
                    if (!$util.isInteger(message.senderKeyIndexes[i]))
                        return "senderKeyIndexes: integer[] expected";
            }
            if (message.senderAccountType != null && Object.hasOwnProperty.call(message, "senderAccountType")) {
                properties._senderAccountType = 1;
                if (typeof message.senderAccountType !== "number" || (message.senderAccountType | 0) !== message.senderAccountType)
                    return "senderAccountType: enum value expected";
            }
            if (message.receiverAccountType != null && Object.hasOwnProperty.call(message, "receiverAccountType")) {
                properties._receiverAccountType = 1;
                if (typeof message.receiverAccountType !== "number" || (message.receiverAccountType | 0) !== message.receiverAccountType)
                    return "receiverAccountType: enum value expected";
            }
            if (message.recipientKeyHash != null && Object.hasOwnProperty.call(message, "recipientKeyHash")) {
                properties._recipientKeyHash = 1;
                if (!(message.recipientKeyHash && typeof message.recipientKeyHash.length === "number" || $util.isString(message.recipientKeyHash)))
                    return "recipientKeyHash: buffer expected";
            }
            if (message.recipientTimestamp != null && Object.hasOwnProperty.call(message, "recipientTimestamp")) {
                properties._recipientTimestamp = 1;
                if (!$util.isInteger(message.recipientTimestamp) && !(message.recipientTimestamp && $util.isInteger(message.recipientTimestamp.low) && $util.isInteger(message.recipientTimestamp.high)))
                    return "recipientTimestamp: integer|Long expected";
            }
            if (message.recipientKeyIndexes != null && Object.hasOwnProperty.call(message, "recipientKeyIndexes")) {
                if (!Array.isArray(message.recipientKeyIndexes))
                    return "recipientKeyIndexes: array expected";
                for (var i = 0; i < message.recipientKeyIndexes.length; ++i)
                    if (!$util.isInteger(message.recipientKeyIndexes[i]))
                        return "recipientKeyIndexes: integer[] expected";
            }
            return null;
        };

        /**
         * Creates a DeviceListMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.DeviceListMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.DeviceListMetadata} DeviceListMetadata
         */
        DeviceListMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.DeviceListMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.senderKeyHash != null)
                if (typeof object.senderKeyHash === "string")
                    $util.base64.decode(object.senderKeyHash, message.senderKeyHash = $util.newBuffer($util.base64.length(object.senderKeyHash)), 0);
                else if (object.senderKeyHash.length >= 0)
                    message.senderKeyHash = object.senderKeyHash;
            if (object.senderTimestamp != null)
                if ($util.Long)
                    message.senderTimestamp = $util.Long.fromValue(object.senderTimestamp, true);
                else if (typeof object.senderTimestamp === "string")
                    message.senderTimestamp = parseInt(object.senderTimestamp, 10);
                else if (typeof object.senderTimestamp === "number")
                    message.senderTimestamp = object.senderTimestamp;
                else if (typeof object.senderTimestamp === "object")
                    message.senderTimestamp = new $util.LongBits(object.senderTimestamp.low >>> 0, object.senderTimestamp.high >>> 0).toNumber(true);
            if (object.senderKeyIndexes) {
                if (!Array.isArray(object.senderKeyIndexes))
                    throw TypeError(".proto.DeviceListMetadata.senderKeyIndexes: array expected");
                message.senderKeyIndexes = Array(object.senderKeyIndexes.length);
                for (var i = 0; i < object.senderKeyIndexes.length; ++i)
                    message.senderKeyIndexes[i] = object.senderKeyIndexes[i] >>> 0;
            }
            switch (object.senderAccountType) {
            case "E2EE":
            case 0:
                message.senderAccountType = 0;
                break;
            case "HOSTED":
            case 1:
                message.senderAccountType = 1;
                break;
            case "NON_E2EE":
            case 2:
                message.senderAccountType = 2;
                break;
            default:
                if (typeof object.senderAccountType === "number" && (object.senderAccountType | 0) === object.senderAccountType)
                    message.senderAccountType = object.senderAccountType;
            }
            switch (object.receiverAccountType) {
            case "E2EE":
            case 0:
                message.receiverAccountType = 0;
                break;
            case "HOSTED":
            case 1:
                message.receiverAccountType = 1;
                break;
            case "NON_E2EE":
            case 2:
                message.receiverAccountType = 2;
                break;
            default:
                if (typeof object.receiverAccountType === "number" && (object.receiverAccountType | 0) === object.receiverAccountType)
                    message.receiverAccountType = object.receiverAccountType;
            }
            if (object.recipientKeyHash != null)
                if (typeof object.recipientKeyHash === "string")
                    $util.base64.decode(object.recipientKeyHash, message.recipientKeyHash = $util.newBuffer($util.base64.length(object.recipientKeyHash)), 0);
                else if (object.recipientKeyHash.length >= 0)
                    message.recipientKeyHash = object.recipientKeyHash;
            if (object.recipientTimestamp != null)
                if ($util.Long)
                    message.recipientTimestamp = $util.Long.fromValue(object.recipientTimestamp, true);
                else if (typeof object.recipientTimestamp === "string")
                    message.recipientTimestamp = parseInt(object.recipientTimestamp, 10);
                else if (typeof object.recipientTimestamp === "number")
                    message.recipientTimestamp = object.recipientTimestamp;
                else if (typeof object.recipientTimestamp === "object")
                    message.recipientTimestamp = new $util.LongBits(object.recipientTimestamp.low >>> 0, object.recipientTimestamp.high >>> 0).toNumber(true);
            if (object.recipientKeyIndexes) {
                if (!Array.isArray(object.recipientKeyIndexes))
                    throw TypeError(".proto.DeviceListMetadata.recipientKeyIndexes: array expected");
                message.recipientKeyIndexes = Array(object.recipientKeyIndexes.length);
                for (var i = 0; i < object.recipientKeyIndexes.length; ++i)
                    message.recipientKeyIndexes[i] = object.recipientKeyIndexes[i] >>> 0;
            }
            return message;
        };

        /**
         * Creates a plain object from a DeviceListMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.DeviceListMetadata
         * @static
         * @param {proto.DeviceListMetadata} message DeviceListMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeviceListMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults) {
                object.senderKeyIndexes = [];
                object.recipientKeyIndexes = [];
            }
            if (message.senderKeyHash != null && Object.hasOwnProperty.call(message, "senderKeyHash"))
                object.senderKeyHash = options.bytes === String ? $util.base64.encode(message.senderKeyHash, 0, message.senderKeyHash.length) : options.bytes === Array ? Array.prototype.slice.call(message.senderKeyHash) : message.senderKeyHash;
            if (message.senderTimestamp != null && Object.hasOwnProperty.call(message, "senderTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.senderTimestamp = typeof message.senderTimestamp === "number" ? BigInt(message.senderTimestamp) : $util.Long.fromBits(message.senderTimestamp.low >>> 0, message.senderTimestamp.high >>> 0, true).toBigInt();
                else if (typeof message.senderTimestamp === "number")
                    object.senderTimestamp = options.longs === String ? String(message.senderTimestamp) : message.senderTimestamp;
                else
                    object.senderTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.senderTimestamp) : options.longs === Number ? new $util.LongBits(message.senderTimestamp.low >>> 0, message.senderTimestamp.high >>> 0).toNumber(true) : message.senderTimestamp;
            if (message.senderKeyIndexes && message.senderKeyIndexes.length) {
                object.senderKeyIndexes = Array(message.senderKeyIndexes.length);
                for (var j = 0; j < message.senderKeyIndexes.length; ++j)
                    object.senderKeyIndexes[j] = message.senderKeyIndexes[j];
            }
            if (message.senderAccountType != null && Object.hasOwnProperty.call(message, "senderAccountType"))
                object.senderAccountType = options.enums === String ? $root.proto.ADVEncryptionType[message.senderAccountType] === undefined ? message.senderAccountType : $root.proto.ADVEncryptionType[message.senderAccountType] : message.senderAccountType;
            if (message.receiverAccountType != null && Object.hasOwnProperty.call(message, "receiverAccountType"))
                object.receiverAccountType = options.enums === String ? $root.proto.ADVEncryptionType[message.receiverAccountType] === undefined ? message.receiverAccountType : $root.proto.ADVEncryptionType[message.receiverAccountType] : message.receiverAccountType;
            if (message.recipientKeyHash != null && Object.hasOwnProperty.call(message, "recipientKeyHash"))
                object.recipientKeyHash = options.bytes === String ? $util.base64.encode(message.recipientKeyHash, 0, message.recipientKeyHash.length) : options.bytes === Array ? Array.prototype.slice.call(message.recipientKeyHash) : message.recipientKeyHash;
            if (message.recipientTimestamp != null && Object.hasOwnProperty.call(message, "recipientTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.recipientTimestamp = typeof message.recipientTimestamp === "number" ? BigInt(message.recipientTimestamp) : $util.Long.fromBits(message.recipientTimestamp.low >>> 0, message.recipientTimestamp.high >>> 0, true).toBigInt();
                else if (typeof message.recipientTimestamp === "number")
                    object.recipientTimestamp = options.longs === String ? String(message.recipientTimestamp) : message.recipientTimestamp;
                else
                    object.recipientTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.recipientTimestamp) : options.longs === Number ? new $util.LongBits(message.recipientTimestamp.low >>> 0, message.recipientTimestamp.high >>> 0).toNumber(true) : message.recipientTimestamp;
            if (message.recipientKeyIndexes && message.recipientKeyIndexes.length) {
                object.recipientKeyIndexes = Array(message.recipientKeyIndexes.length);
                for (var j = 0; j < message.recipientKeyIndexes.length; ++j)
                    object.recipientKeyIndexes[j] = message.recipientKeyIndexes[j];
            }
            return object;
        };

        /**
         * Converts this DeviceListMetadata to JSON.
         * @function toJSON
         * @memberof proto.DeviceListMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeviceListMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DeviceListMetadata
         * @function getTypeUrl
         * @memberof proto.DeviceListMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DeviceListMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.DeviceListMetadata";
        };

        return DeviceListMetadata;
    })();

    proto.DeviceProps = (function() {

        /**
         * Properties of a DeviceProps.
         * @memberof proto
         * @interface IDeviceProps
         * @property {string|null} [os] DeviceProps os
         * @property {proto.DeviceProps.IAppVersion|null} [version] DeviceProps version
         * @property {proto.DeviceProps.PlatformType|null} [platformType] DeviceProps platformType
         * @property {boolean|null} [requireFullSync] DeviceProps requireFullSync
         * @property {proto.DeviceProps.IHistorySyncConfig|null} [historySyncConfig] DeviceProps historySyncConfig
         */

        /**
         * Constructs a new DeviceProps.
         * @memberof proto
         * @classdesc Represents a DeviceProps.
         * @implements IDeviceProps
         * @constructor
         * @param {proto.IDeviceProps=} [properties] Properties to set
         */
        function DeviceProps(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DeviceProps os.
         * @member {string|null|undefined} os
         * @memberof proto.DeviceProps
         * @instance
         */
        DeviceProps.prototype.os = null;

        /**
         * DeviceProps version.
         * @member {proto.DeviceProps.IAppVersion|null|undefined} version
         * @memberof proto.DeviceProps
         * @instance
         */
        DeviceProps.prototype.version = null;

        /**
         * DeviceProps platformType.
         * @member {proto.DeviceProps.PlatformType|null|undefined} platformType
         * @memberof proto.DeviceProps
         * @instance
         */
        DeviceProps.prototype.platformType = null;

        /**
         * DeviceProps requireFullSync.
         * @member {boolean|null|undefined} requireFullSync
         * @memberof proto.DeviceProps
         * @instance
         */
        DeviceProps.prototype.requireFullSync = null;

        /**
         * DeviceProps historySyncConfig.
         * @member {proto.DeviceProps.IHistorySyncConfig|null|undefined} historySyncConfig
         * @memberof proto.DeviceProps
         * @instance
         */
        DeviceProps.prototype.historySyncConfig = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceProps.prototype, "_os", {
            get: $util.oneOfGetter($oneOfFields = ["os"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceProps.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceProps.prototype, "_platformType", {
            get: $util.oneOfGetter($oneOfFields = ["platformType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceProps.prototype, "_requireFullSync", {
            get: $util.oneOfGetter($oneOfFields = ["requireFullSync"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceProps.prototype, "_historySyncConfig", {
            get: $util.oneOfGetter($oneOfFields = ["historySyncConfig"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new DeviceProps instance using the specified properties.
         * @function create
         * @memberof proto.DeviceProps
         * @static
         * @param {proto.IDeviceProps=} [properties] Properties to set
         * @returns {proto.DeviceProps} DeviceProps instance
         */
        DeviceProps.create = function create(properties) {
            return new DeviceProps(properties);
        };

        /**
         * Encodes the specified DeviceProps message. Does not implicitly {@link proto.DeviceProps.verify|verify} messages.
         * @function encode
         * @memberof proto.DeviceProps
         * @static
         * @param {proto.IDeviceProps} message DeviceProps message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceProps.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.os != null && Object.hasOwnProperty.call(message, "os"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.os);
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                $root.proto.DeviceProps.AppVersion.encode(message.version, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.platformType != null && Object.hasOwnProperty.call(message, "platformType"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.platformType);
            if (message.requireFullSync != null && Object.hasOwnProperty.call(message, "requireFullSync"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.requireFullSync);
            if (message.historySyncConfig != null && Object.hasOwnProperty.call(message, "historySyncConfig"))
                $root.proto.DeviceProps.HistorySyncConfig.encode(message.historySyncConfig, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DeviceProps message, length delimited. Does not implicitly {@link proto.DeviceProps.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.DeviceProps
         * @static
         * @param {proto.IDeviceProps} message DeviceProps message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceProps.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DeviceProps message from the specified reader or buffer.
         * @function decode
         * @memberof proto.DeviceProps
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.DeviceProps} DeviceProps
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceProps.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.os = reader.stringVerify();
                        message._os = "os";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.version = $root.proto.DeviceProps.AppVersion.decode(reader, reader.uint32(), undefined, q + 1, message.version);
                        message._version = "version";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.platformType = reader.int32();
                        message._platformType = "platformType";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.requireFullSync = reader.bool();
                        message._requireFullSync = "requireFullSync";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.historySyncConfig = $root.proto.DeviceProps.HistorySyncConfig.decode(reader, reader.uint32(), undefined, q + 1, message.historySyncConfig);
                        message._historySyncConfig = "historySyncConfig";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a DeviceProps message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.DeviceProps
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.DeviceProps} DeviceProps
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceProps.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DeviceProps message.
         * @function verify
         * @memberof proto.DeviceProps
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DeviceProps.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.os != null && Object.hasOwnProperty.call(message, "os")) {
                properties._os = 1;
                if (!$util.isString(message.os))
                    return "os: string expected";
            }
            if (message.version != null && Object.hasOwnProperty.call(message, "version")) {
                properties._version = 1;
                {
                    var error = $root.proto.DeviceProps.AppVersion.verify(message.version, q + 1);
                    if (error)
                        return "version." + error;
                }
            }
            if (message.platformType != null && Object.hasOwnProperty.call(message, "platformType")) {
                properties._platformType = 1;
                if (typeof message.platformType !== "number" || (message.platformType | 0) !== message.platformType)
                    return "platformType: enum value expected";
            }
            if (message.requireFullSync != null && Object.hasOwnProperty.call(message, "requireFullSync")) {
                properties._requireFullSync = 1;
                if (typeof message.requireFullSync !== "boolean")
                    return "requireFullSync: boolean expected";
            }
            if (message.historySyncConfig != null && Object.hasOwnProperty.call(message, "historySyncConfig")) {
                properties._historySyncConfig = 1;
                {
                    var error = $root.proto.DeviceProps.HistorySyncConfig.verify(message.historySyncConfig, q + 1);
                    if (error)
                        return "historySyncConfig." + error;
                }
            }
            return null;
        };

        /**
         * Creates a DeviceProps message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.DeviceProps
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.DeviceProps} DeviceProps
         */
        DeviceProps.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.DeviceProps: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.os != null)
                message.os = String(object.os);
            if (object.version != null) {
                if (!$util.isObject(object.version))
                    throw TypeError(".proto.DeviceProps.version: object expected");
                message.version = $root.proto.DeviceProps.AppVersion.fromObject(object.version, q + 1);
            }
            switch (object.platformType) {
            case "UNKNOWN":
            case 0:
                message.platformType = 0;
                break;
            case "CHROME":
            case 1:
                message.platformType = 1;
                break;
            case "FIREFOX":
            case 2:
                message.platformType = 2;
                break;
            case "IE":
            case 3:
                message.platformType = 3;
                break;
            case "OPERA":
            case 4:
                message.platformType = 4;
                break;
            case "SAFARI":
            case 5:
                message.platformType = 5;
                break;
            case "EDGE":
            case 6:
                message.platformType = 6;
                break;
            case "DESKTOP":
            case 7:
                message.platformType = 7;
                break;
            case "IPAD":
            case 8:
                message.platformType = 8;
                break;
            case "ANDROID_TABLET":
            case 9:
                message.platformType = 9;
                break;
            case "OHANA":
            case 10:
                message.platformType = 10;
                break;
            case "ALOHA":
            case 11:
                message.platformType = 11;
                break;
            case "CATALINA":
            case 12:
                message.platformType = 12;
                break;
            case "TCL_TV":
            case 13:
                message.platformType = 13;
                break;
            case "IOS_PHONE":
            case 14:
                message.platformType = 14;
                break;
            case "IOS_CATALYST":
            case 15:
                message.platformType = 15;
                break;
            case "ANDROID_PHONE":
            case 16:
                message.platformType = 16;
                break;
            case "ANDROID_AMBIGUOUS":
            case 17:
                message.platformType = 17;
                break;
            case "WEAR_OS":
            case 18:
                message.platformType = 18;
                break;
            case "AR_WRIST":
            case 19:
                message.platformType = 19;
                break;
            case "AR_DEVICE":
            case 20:
                message.platformType = 20;
                break;
            case "UWP":
            case 21:
                message.platformType = 21;
                break;
            case "VR":
            case 22:
                message.platformType = 22;
                break;
            case "CLOUD_API":
            case 23:
                message.platformType = 23;
                break;
            case "SMARTGLASSES":
            case 24:
                message.platformType = 24;
                break;
            default:
                if (typeof object.platformType === "number" && (object.platformType | 0) === object.platformType)
                    message.platformType = object.platformType;
            }
            if (object.requireFullSync != null)
                message.requireFullSync = Boolean(object.requireFullSync);
            if (object.historySyncConfig != null) {
                if (!$util.isObject(object.historySyncConfig))
                    throw TypeError(".proto.DeviceProps.historySyncConfig: object expected");
                message.historySyncConfig = $root.proto.DeviceProps.HistorySyncConfig.fromObject(object.historySyncConfig, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a DeviceProps message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.DeviceProps
         * @static
         * @param {proto.DeviceProps} message DeviceProps
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeviceProps.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.os != null && Object.hasOwnProperty.call(message, "os"))
                object.os = message.os;
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                object.version = $root.proto.DeviceProps.AppVersion.toObject(message.version, options, q + 1);
            if (message.platformType != null && Object.hasOwnProperty.call(message, "platformType"))
                object.platformType = options.enums === String ? $root.proto.DeviceProps.PlatformType[message.platformType] === undefined ? message.platformType : $root.proto.DeviceProps.PlatformType[message.platformType] : message.platformType;
            if (message.requireFullSync != null && Object.hasOwnProperty.call(message, "requireFullSync"))
                object.requireFullSync = message.requireFullSync;
            if (message.historySyncConfig != null && Object.hasOwnProperty.call(message, "historySyncConfig"))
                object.historySyncConfig = $root.proto.DeviceProps.HistorySyncConfig.toObject(message.historySyncConfig, options, q + 1);
            return object;
        };

        /**
         * Converts this DeviceProps to JSON.
         * @function toJSON
         * @memberof proto.DeviceProps
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeviceProps.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DeviceProps
         * @function getTypeUrl
         * @memberof proto.DeviceProps
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DeviceProps.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.DeviceProps";
        };

        DeviceProps.AppVersion = (function() {

            /**
             * Properties of an AppVersion.
             * @memberof proto.DeviceProps
             * @interface IAppVersion
             * @property {number|null} [primary] AppVersion primary
             * @property {number|null} [secondary] AppVersion secondary
             * @property {number|null} [tertiary] AppVersion tertiary
             * @property {number|null} [quaternary] AppVersion quaternary
             * @property {number|null} [quinary] AppVersion quinary
             */

            /**
             * Constructs a new AppVersion.
             * @memberof proto.DeviceProps
             * @classdesc Represents an AppVersion.
             * @implements IAppVersion
             * @constructor
             * @param {proto.DeviceProps.IAppVersion=} [properties] Properties to set
             */
            function AppVersion(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AppVersion primary.
             * @member {number|null|undefined} primary
             * @memberof proto.DeviceProps.AppVersion
             * @instance
             */
            AppVersion.prototype.primary = null;

            /**
             * AppVersion secondary.
             * @member {number|null|undefined} secondary
             * @memberof proto.DeviceProps.AppVersion
             * @instance
             */
            AppVersion.prototype.secondary = null;

            /**
             * AppVersion tertiary.
             * @member {number|null|undefined} tertiary
             * @memberof proto.DeviceProps.AppVersion
             * @instance
             */
            AppVersion.prototype.tertiary = null;

            /**
             * AppVersion quaternary.
             * @member {number|null|undefined} quaternary
             * @memberof proto.DeviceProps.AppVersion
             * @instance
             */
            AppVersion.prototype.quaternary = null;

            /**
             * AppVersion quinary.
             * @member {number|null|undefined} quinary
             * @memberof proto.DeviceProps.AppVersion
             * @instance
             */
            AppVersion.prototype.quinary = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AppVersion.prototype, "_primary", {
                get: $util.oneOfGetter($oneOfFields = ["primary"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AppVersion.prototype, "_secondary", {
                get: $util.oneOfGetter($oneOfFields = ["secondary"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AppVersion.prototype, "_tertiary", {
                get: $util.oneOfGetter($oneOfFields = ["tertiary"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AppVersion.prototype, "_quaternary", {
                get: $util.oneOfGetter($oneOfFields = ["quaternary"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AppVersion.prototype, "_quinary", {
                get: $util.oneOfGetter($oneOfFields = ["quinary"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AppVersion instance using the specified properties.
             * @function create
             * @memberof proto.DeviceProps.AppVersion
             * @static
             * @param {proto.DeviceProps.IAppVersion=} [properties] Properties to set
             * @returns {proto.DeviceProps.AppVersion} AppVersion instance
             */
            AppVersion.create = function create(properties) {
                return new AppVersion(properties);
            };

            /**
             * Encodes the specified AppVersion message. Does not implicitly {@link proto.DeviceProps.AppVersion.verify|verify} messages.
             * @function encode
             * @memberof proto.DeviceProps.AppVersion
             * @static
             * @param {proto.DeviceProps.IAppVersion} message AppVersion message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AppVersion.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.primary != null && Object.hasOwnProperty.call(message, "primary"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.primary);
                if (message.secondary != null && Object.hasOwnProperty.call(message, "secondary"))
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.secondary);
                if (message.tertiary != null && Object.hasOwnProperty.call(message, "tertiary"))
                    writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.tertiary);
                if (message.quaternary != null && Object.hasOwnProperty.call(message, "quaternary"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.quaternary);
                if (message.quinary != null && Object.hasOwnProperty.call(message, "quinary"))
                    writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.quinary);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AppVersion message, length delimited. Does not implicitly {@link proto.DeviceProps.AppVersion.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.DeviceProps.AppVersion
             * @static
             * @param {proto.DeviceProps.IAppVersion} message AppVersion message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AppVersion.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an AppVersion message from the specified reader or buffer.
             * @function decode
             * @memberof proto.DeviceProps.AppVersion
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.DeviceProps.AppVersion} AppVersion
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AppVersion.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.primary = reader.uint32();
                            message._primary = "primary";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.secondary = reader.uint32();
                            message._secondary = "secondary";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.tertiary = reader.uint32();
                            message._tertiary = "tertiary";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.quaternary = reader.uint32();
                            message._quaternary = "quaternary";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.quinary = reader.uint32();
                            message._quinary = "quinary";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an AppVersion message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.DeviceProps.AppVersion
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.DeviceProps.AppVersion} AppVersion
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AppVersion.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AppVersion message.
             * @function verify
             * @memberof proto.DeviceProps.AppVersion
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AppVersion.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.primary != null && Object.hasOwnProperty.call(message, "primary")) {
                    properties._primary = 1;
                    if (!$util.isInteger(message.primary))
                        return "primary: integer expected";
                }
                if (message.secondary != null && Object.hasOwnProperty.call(message, "secondary")) {
                    properties._secondary = 1;
                    if (!$util.isInteger(message.secondary))
                        return "secondary: integer expected";
                }
                if (message.tertiary != null && Object.hasOwnProperty.call(message, "tertiary")) {
                    properties._tertiary = 1;
                    if (!$util.isInteger(message.tertiary))
                        return "tertiary: integer expected";
                }
                if (message.quaternary != null && Object.hasOwnProperty.call(message, "quaternary")) {
                    properties._quaternary = 1;
                    if (!$util.isInteger(message.quaternary))
                        return "quaternary: integer expected";
                }
                if (message.quinary != null && Object.hasOwnProperty.call(message, "quinary")) {
                    properties._quinary = 1;
                    if (!$util.isInteger(message.quinary))
                        return "quinary: integer expected";
                }
                return null;
            };

            /**
             * Creates an AppVersion message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.DeviceProps.AppVersion
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.DeviceProps.AppVersion} AppVersion
             */
            AppVersion.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.DeviceProps.AppVersion: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.primary != null)
                    message.primary = object.primary >>> 0;
                if (object.secondary != null)
                    message.secondary = object.secondary >>> 0;
                if (object.tertiary != null)
                    message.tertiary = object.tertiary >>> 0;
                if (object.quaternary != null)
                    message.quaternary = object.quaternary >>> 0;
                if (object.quinary != null)
                    message.quinary = object.quinary >>> 0;
                return message;
            };

            /**
             * Creates a plain object from an AppVersion message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.DeviceProps.AppVersion
             * @static
             * @param {proto.DeviceProps.AppVersion} message AppVersion
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AppVersion.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.primary != null && Object.hasOwnProperty.call(message, "primary"))
                    object.primary = message.primary;
                if (message.secondary != null && Object.hasOwnProperty.call(message, "secondary"))
                    object.secondary = message.secondary;
                if (message.tertiary != null && Object.hasOwnProperty.call(message, "tertiary"))
                    object.tertiary = message.tertiary;
                if (message.quaternary != null && Object.hasOwnProperty.call(message, "quaternary"))
                    object.quaternary = message.quaternary;
                if (message.quinary != null && Object.hasOwnProperty.call(message, "quinary"))
                    object.quinary = message.quinary;
                return object;
            };

            /**
             * Converts this AppVersion to JSON.
             * @function toJSON
             * @memberof proto.DeviceProps.AppVersion
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AppVersion.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AppVersion
             * @function getTypeUrl
             * @memberof proto.DeviceProps.AppVersion
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AppVersion.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.DeviceProps.AppVersion";
            };

            return AppVersion;
        })();

        DeviceProps.HistorySyncConfig = (function() {

            /**
             * Properties of a HistorySyncConfig.
             * @memberof proto.DeviceProps
             * @interface IHistorySyncConfig
             * @property {number|null} [fullSyncDaysLimit] HistorySyncConfig fullSyncDaysLimit
             * @property {number|null} [fullSyncSizeMbLimit] HistorySyncConfig fullSyncSizeMbLimit
             * @property {number|null} [storageQuotaMb] HistorySyncConfig storageQuotaMb
             * @property {boolean|null} [inlineInitialPayloadInE2EeMsg] HistorySyncConfig inlineInitialPayloadInE2EeMsg
             * @property {number|null} [recentSyncDaysLimit] HistorySyncConfig recentSyncDaysLimit
             * @property {boolean|null} [supportCallLogHistory] HistorySyncConfig supportCallLogHistory
             * @property {boolean|null} [supportBotUserAgentChatHistory] HistorySyncConfig supportBotUserAgentChatHistory
             * @property {boolean|null} [supportCagReactionsAndPolls] HistorySyncConfig supportCagReactionsAndPolls
             * @property {boolean|null} [supportBizHostedMsg] HistorySyncConfig supportBizHostedMsg
             * @property {boolean|null} [supportRecentSyncChunkMessageCountTuning] HistorySyncConfig supportRecentSyncChunkMessageCountTuning
             * @property {boolean|null} [supportHostedGroupMsg] HistorySyncConfig supportHostedGroupMsg
             * @property {boolean|null} [supportFbidBotChatHistory] HistorySyncConfig supportFbidBotChatHistory
             * @property {boolean|null} [supportAddOnHistorySyncMigration] HistorySyncConfig supportAddOnHistorySyncMigration
             * @property {boolean|null} [supportMessageAssociation] HistorySyncConfig supportMessageAssociation
             * @property {boolean|null} [supportGroupHistory] HistorySyncConfig supportGroupHistory
             * @property {boolean|null} [onDemandReady] HistorySyncConfig onDemandReady
             * @property {boolean|null} [supportGuestChat] HistorySyncConfig supportGuestChat
             * @property {boolean|null} [completeOnDemandReady] HistorySyncConfig completeOnDemandReady
             * @property {number|null} [thumbnailSyncDaysLimit] HistorySyncConfig thumbnailSyncDaysLimit
             * @property {number|null} [initialSyncMaxMessagesPerChat] HistorySyncConfig initialSyncMaxMessagesPerChat
             * @property {boolean|null} [supportManusHistory] HistorySyncConfig supportManusHistory
             * @property {boolean|null} [supportHatchHistory] HistorySyncConfig supportHatchHistory
             * @property {Array.<string>|null} [supportedBotChannelFbids] HistorySyncConfig supportedBotChannelFbids
             * @property {boolean|null} [supportInlineContacts] HistorySyncConfig supportInlineContacts
             */

            /**
             * Constructs a new HistorySyncConfig.
             * @memberof proto.DeviceProps
             * @classdesc Represents a HistorySyncConfig.
             * @implements IHistorySyncConfig
             * @constructor
             * @param {proto.DeviceProps.IHistorySyncConfig=} [properties] Properties to set
             */
            function HistorySyncConfig(properties) {
                this.supportedBotChannelFbids = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * HistorySyncConfig fullSyncDaysLimit.
             * @member {number|null|undefined} fullSyncDaysLimit
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.fullSyncDaysLimit = null;

            /**
             * HistorySyncConfig fullSyncSizeMbLimit.
             * @member {number|null|undefined} fullSyncSizeMbLimit
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.fullSyncSizeMbLimit = null;

            /**
             * HistorySyncConfig storageQuotaMb.
             * @member {number|null|undefined} storageQuotaMb
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.storageQuotaMb = null;

            /**
             * HistorySyncConfig inlineInitialPayloadInE2EeMsg.
             * @member {boolean|null|undefined} inlineInitialPayloadInE2EeMsg
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.inlineInitialPayloadInE2EeMsg = null;

            /**
             * HistorySyncConfig recentSyncDaysLimit.
             * @member {number|null|undefined} recentSyncDaysLimit
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.recentSyncDaysLimit = null;

            /**
             * HistorySyncConfig supportCallLogHistory.
             * @member {boolean|null|undefined} supportCallLogHistory
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.supportCallLogHistory = null;

            /**
             * HistorySyncConfig supportBotUserAgentChatHistory.
             * @member {boolean|null|undefined} supportBotUserAgentChatHistory
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.supportBotUserAgentChatHistory = null;

            /**
             * HistorySyncConfig supportCagReactionsAndPolls.
             * @member {boolean|null|undefined} supportCagReactionsAndPolls
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.supportCagReactionsAndPolls = null;

            /**
             * HistorySyncConfig supportBizHostedMsg.
             * @member {boolean|null|undefined} supportBizHostedMsg
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.supportBizHostedMsg = null;

            /**
             * HistorySyncConfig supportRecentSyncChunkMessageCountTuning.
             * @member {boolean|null|undefined} supportRecentSyncChunkMessageCountTuning
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.supportRecentSyncChunkMessageCountTuning = null;

            /**
             * HistorySyncConfig supportHostedGroupMsg.
             * @member {boolean|null|undefined} supportHostedGroupMsg
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.supportHostedGroupMsg = null;

            /**
             * HistorySyncConfig supportFbidBotChatHistory.
             * @member {boolean|null|undefined} supportFbidBotChatHistory
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.supportFbidBotChatHistory = null;

            /**
             * HistorySyncConfig supportAddOnHistorySyncMigration.
             * @member {boolean|null|undefined} supportAddOnHistorySyncMigration
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.supportAddOnHistorySyncMigration = null;

            /**
             * HistorySyncConfig supportMessageAssociation.
             * @member {boolean|null|undefined} supportMessageAssociation
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.supportMessageAssociation = null;

            /**
             * HistorySyncConfig supportGroupHistory.
             * @member {boolean|null|undefined} supportGroupHistory
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.supportGroupHistory = null;

            /**
             * HistorySyncConfig onDemandReady.
             * @member {boolean|null|undefined} onDemandReady
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.onDemandReady = null;

            /**
             * HistorySyncConfig supportGuestChat.
             * @member {boolean|null|undefined} supportGuestChat
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.supportGuestChat = null;

            /**
             * HistorySyncConfig completeOnDemandReady.
             * @member {boolean|null|undefined} completeOnDemandReady
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.completeOnDemandReady = null;

            /**
             * HistorySyncConfig thumbnailSyncDaysLimit.
             * @member {number|null|undefined} thumbnailSyncDaysLimit
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.thumbnailSyncDaysLimit = null;

            /**
             * HistorySyncConfig initialSyncMaxMessagesPerChat.
             * @member {number|null|undefined} initialSyncMaxMessagesPerChat
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.initialSyncMaxMessagesPerChat = null;

            /**
             * HistorySyncConfig supportManusHistory.
             * @member {boolean|null|undefined} supportManusHistory
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.supportManusHistory = null;

            /**
             * HistorySyncConfig supportHatchHistory.
             * @member {boolean|null|undefined} supportHatchHistory
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.supportHatchHistory = null;

            /**
             * HistorySyncConfig supportedBotChannelFbids.
             * @member {Array.<string>} supportedBotChannelFbids
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.supportedBotChannelFbids = $util.emptyArray;

            /**
             * HistorySyncConfig supportInlineContacts.
             * @member {boolean|null|undefined} supportInlineContacts
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             */
            HistorySyncConfig.prototype.supportInlineContacts = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_fullSyncDaysLimit", {
                get: $util.oneOfGetter($oneOfFields = ["fullSyncDaysLimit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_fullSyncSizeMbLimit", {
                get: $util.oneOfGetter($oneOfFields = ["fullSyncSizeMbLimit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_storageQuotaMb", {
                get: $util.oneOfGetter($oneOfFields = ["storageQuotaMb"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_inlineInitialPayloadInE2EeMsg", {
                get: $util.oneOfGetter($oneOfFields = ["inlineInitialPayloadInE2EeMsg"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_recentSyncDaysLimit", {
                get: $util.oneOfGetter($oneOfFields = ["recentSyncDaysLimit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportCallLogHistory", {
                get: $util.oneOfGetter($oneOfFields = ["supportCallLogHistory"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportBotUserAgentChatHistory", {
                get: $util.oneOfGetter($oneOfFields = ["supportBotUserAgentChatHistory"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportCagReactionsAndPolls", {
                get: $util.oneOfGetter($oneOfFields = ["supportCagReactionsAndPolls"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportBizHostedMsg", {
                get: $util.oneOfGetter($oneOfFields = ["supportBizHostedMsg"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportRecentSyncChunkMessageCountTuning", {
                get: $util.oneOfGetter($oneOfFields = ["supportRecentSyncChunkMessageCountTuning"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportHostedGroupMsg", {
                get: $util.oneOfGetter($oneOfFields = ["supportHostedGroupMsg"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportFbidBotChatHistory", {
                get: $util.oneOfGetter($oneOfFields = ["supportFbidBotChatHistory"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportAddOnHistorySyncMigration", {
                get: $util.oneOfGetter($oneOfFields = ["supportAddOnHistorySyncMigration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportMessageAssociation", {
                get: $util.oneOfGetter($oneOfFields = ["supportMessageAssociation"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportGroupHistory", {
                get: $util.oneOfGetter($oneOfFields = ["supportGroupHistory"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_onDemandReady", {
                get: $util.oneOfGetter($oneOfFields = ["onDemandReady"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportGuestChat", {
                get: $util.oneOfGetter($oneOfFields = ["supportGuestChat"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_completeOnDemandReady", {
                get: $util.oneOfGetter($oneOfFields = ["completeOnDemandReady"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_thumbnailSyncDaysLimit", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailSyncDaysLimit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_initialSyncMaxMessagesPerChat", {
                get: $util.oneOfGetter($oneOfFields = ["initialSyncMaxMessagesPerChat"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportManusHistory", {
                get: $util.oneOfGetter($oneOfFields = ["supportManusHistory"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportHatchHistory", {
                get: $util.oneOfGetter($oneOfFields = ["supportHatchHistory"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportInlineContacts", {
                get: $util.oneOfGetter($oneOfFields = ["supportInlineContacts"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new HistorySyncConfig instance using the specified properties.
             * @function create
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @static
             * @param {proto.DeviceProps.IHistorySyncConfig=} [properties] Properties to set
             * @returns {proto.DeviceProps.HistorySyncConfig} HistorySyncConfig instance
             */
            HistorySyncConfig.create = function create(properties) {
                return new HistorySyncConfig(properties);
            };

            /**
             * Encodes the specified HistorySyncConfig message. Does not implicitly {@link proto.DeviceProps.HistorySyncConfig.verify|verify} messages.
             * @function encode
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @static
             * @param {proto.DeviceProps.IHistorySyncConfig} message HistorySyncConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HistorySyncConfig.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.fullSyncDaysLimit != null && Object.hasOwnProperty.call(message, "fullSyncDaysLimit"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.fullSyncDaysLimit);
                if (message.fullSyncSizeMbLimit != null && Object.hasOwnProperty.call(message, "fullSyncSizeMbLimit"))
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.fullSyncSizeMbLimit);
                if (message.storageQuotaMb != null && Object.hasOwnProperty.call(message, "storageQuotaMb"))
                    writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.storageQuotaMb);
                if (message.inlineInitialPayloadInE2EeMsg != null && Object.hasOwnProperty.call(message, "inlineInitialPayloadInE2EeMsg"))
                    writer.uint32(/* id 4, wireType 0 =*/32).bool(message.inlineInitialPayloadInE2EeMsg);
                if (message.recentSyncDaysLimit != null && Object.hasOwnProperty.call(message, "recentSyncDaysLimit"))
                    writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.recentSyncDaysLimit);
                if (message.supportCallLogHistory != null && Object.hasOwnProperty.call(message, "supportCallLogHistory"))
                    writer.uint32(/* id 6, wireType 0 =*/48).bool(message.supportCallLogHistory);
                if (message.supportBotUserAgentChatHistory != null && Object.hasOwnProperty.call(message, "supportBotUserAgentChatHistory"))
                    writer.uint32(/* id 7, wireType 0 =*/56).bool(message.supportBotUserAgentChatHistory);
                if (message.supportCagReactionsAndPolls != null && Object.hasOwnProperty.call(message, "supportCagReactionsAndPolls"))
                    writer.uint32(/* id 8, wireType 0 =*/64).bool(message.supportCagReactionsAndPolls);
                if (message.supportBizHostedMsg != null && Object.hasOwnProperty.call(message, "supportBizHostedMsg"))
                    writer.uint32(/* id 9, wireType 0 =*/72).bool(message.supportBizHostedMsg);
                if (message.supportRecentSyncChunkMessageCountTuning != null && Object.hasOwnProperty.call(message, "supportRecentSyncChunkMessageCountTuning"))
                    writer.uint32(/* id 10, wireType 0 =*/80).bool(message.supportRecentSyncChunkMessageCountTuning);
                if (message.supportHostedGroupMsg != null && Object.hasOwnProperty.call(message, "supportHostedGroupMsg"))
                    writer.uint32(/* id 11, wireType 0 =*/88).bool(message.supportHostedGroupMsg);
                if (message.supportFbidBotChatHistory != null && Object.hasOwnProperty.call(message, "supportFbidBotChatHistory"))
                    writer.uint32(/* id 12, wireType 0 =*/96).bool(message.supportFbidBotChatHistory);
                if (message.supportAddOnHistorySyncMigration != null && Object.hasOwnProperty.call(message, "supportAddOnHistorySyncMigration"))
                    writer.uint32(/* id 13, wireType 0 =*/104).bool(message.supportAddOnHistorySyncMigration);
                if (message.supportMessageAssociation != null && Object.hasOwnProperty.call(message, "supportMessageAssociation"))
                    writer.uint32(/* id 14, wireType 0 =*/112).bool(message.supportMessageAssociation);
                if (message.supportGroupHistory != null && Object.hasOwnProperty.call(message, "supportGroupHistory"))
                    writer.uint32(/* id 15, wireType 0 =*/120).bool(message.supportGroupHistory);
                if (message.onDemandReady != null && Object.hasOwnProperty.call(message, "onDemandReady"))
                    writer.uint32(/* id 16, wireType 0 =*/128).bool(message.onDemandReady);
                if (message.supportGuestChat != null && Object.hasOwnProperty.call(message, "supportGuestChat"))
                    writer.uint32(/* id 17, wireType 0 =*/136).bool(message.supportGuestChat);
                if (message.completeOnDemandReady != null && Object.hasOwnProperty.call(message, "completeOnDemandReady"))
                    writer.uint32(/* id 18, wireType 0 =*/144).bool(message.completeOnDemandReady);
                if (message.thumbnailSyncDaysLimit != null && Object.hasOwnProperty.call(message, "thumbnailSyncDaysLimit"))
                    writer.uint32(/* id 19, wireType 0 =*/152).uint32(message.thumbnailSyncDaysLimit);
                if (message.initialSyncMaxMessagesPerChat != null && Object.hasOwnProperty.call(message, "initialSyncMaxMessagesPerChat"))
                    writer.uint32(/* id 20, wireType 0 =*/160).uint32(message.initialSyncMaxMessagesPerChat);
                if (message.supportManusHistory != null && Object.hasOwnProperty.call(message, "supportManusHistory"))
                    writer.uint32(/* id 21, wireType 0 =*/168).bool(message.supportManusHistory);
                if (message.supportHatchHistory != null && Object.hasOwnProperty.call(message, "supportHatchHistory"))
                    writer.uint32(/* id 22, wireType 0 =*/176).bool(message.supportHatchHistory);
                if (message.supportedBotChannelFbids != null && message.supportedBotChannelFbids.length)
                    for (var i = 0; i < message.supportedBotChannelFbids.length; ++i)
                        writer.uint32(/* id 23, wireType 2 =*/186).string(message.supportedBotChannelFbids[i]);
                if (message.supportInlineContacts != null && Object.hasOwnProperty.call(message, "supportInlineContacts"))
                    writer.uint32(/* id 24, wireType 0 =*/192).bool(message.supportInlineContacts);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified HistorySyncConfig message, length delimited. Does not implicitly {@link proto.DeviceProps.HistorySyncConfig.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @static
             * @param {proto.DeviceProps.IHistorySyncConfig} message HistorySyncConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HistorySyncConfig.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a HistorySyncConfig message from the specified reader or buffer.
             * @function decode
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.DeviceProps.HistorySyncConfig} HistorySyncConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HistorySyncConfig.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.fullSyncDaysLimit = reader.uint32();
                            message._fullSyncDaysLimit = "fullSyncDaysLimit";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.fullSyncSizeMbLimit = reader.uint32();
                            message._fullSyncSizeMbLimit = "fullSyncSizeMbLimit";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.storageQuotaMb = reader.uint32();
                            message._storageQuotaMb = "storageQuotaMb";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.inlineInitialPayloadInE2EeMsg = reader.bool();
                            message._inlineInitialPayloadInE2EeMsg = "inlineInitialPayloadInE2EeMsg";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.recentSyncDaysLimit = reader.uint32();
                            message._recentSyncDaysLimit = "recentSyncDaysLimit";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.supportCallLogHistory = reader.bool();
                            message._supportCallLogHistory = "supportCallLogHistory";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            message.supportBotUserAgentChatHistory = reader.bool();
                            message._supportBotUserAgentChatHistory = "supportBotUserAgentChatHistory";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            message.supportCagReactionsAndPolls = reader.bool();
                            message._supportCagReactionsAndPolls = "supportCagReactionsAndPolls";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            message.supportBizHostedMsg = reader.bool();
                            message._supportBizHostedMsg = "supportBizHostedMsg";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            message.supportRecentSyncChunkMessageCountTuning = reader.bool();
                            message._supportRecentSyncChunkMessageCountTuning = "supportRecentSyncChunkMessageCountTuning";
                            continue;
                        }
                    case 11: {
                            if (u !== 0)
                                break;
                            message.supportHostedGroupMsg = reader.bool();
                            message._supportHostedGroupMsg = "supportHostedGroupMsg";
                            continue;
                        }
                    case 12: {
                            if (u !== 0)
                                break;
                            message.supportFbidBotChatHistory = reader.bool();
                            message._supportFbidBotChatHistory = "supportFbidBotChatHistory";
                            continue;
                        }
                    case 13: {
                            if (u !== 0)
                                break;
                            message.supportAddOnHistorySyncMigration = reader.bool();
                            message._supportAddOnHistorySyncMigration = "supportAddOnHistorySyncMigration";
                            continue;
                        }
                    case 14: {
                            if (u !== 0)
                                break;
                            message.supportMessageAssociation = reader.bool();
                            message._supportMessageAssociation = "supportMessageAssociation";
                            continue;
                        }
                    case 15: {
                            if (u !== 0)
                                break;
                            message.supportGroupHistory = reader.bool();
                            message._supportGroupHistory = "supportGroupHistory";
                            continue;
                        }
                    case 16: {
                            if (u !== 0)
                                break;
                            message.onDemandReady = reader.bool();
                            message._onDemandReady = "onDemandReady";
                            continue;
                        }
                    case 17: {
                            if (u !== 0)
                                break;
                            message.supportGuestChat = reader.bool();
                            message._supportGuestChat = "supportGuestChat";
                            continue;
                        }
                    case 18: {
                            if (u !== 0)
                                break;
                            message.completeOnDemandReady = reader.bool();
                            message._completeOnDemandReady = "completeOnDemandReady";
                            continue;
                        }
                    case 19: {
                            if (u !== 0)
                                break;
                            message.thumbnailSyncDaysLimit = reader.uint32();
                            message._thumbnailSyncDaysLimit = "thumbnailSyncDaysLimit";
                            continue;
                        }
                    case 20: {
                            if (u !== 0)
                                break;
                            message.initialSyncMaxMessagesPerChat = reader.uint32();
                            message._initialSyncMaxMessagesPerChat = "initialSyncMaxMessagesPerChat";
                            continue;
                        }
                    case 21: {
                            if (u !== 0)
                                break;
                            message.supportManusHistory = reader.bool();
                            message._supportManusHistory = "supportManusHistory";
                            continue;
                        }
                    case 22: {
                            if (u !== 0)
                                break;
                            message.supportHatchHistory = reader.bool();
                            message._supportHatchHistory = "supportHatchHistory";
                            continue;
                        }
                    case 23: {
                            if (u !== 2)
                                break;
                            if (!(message.supportedBotChannelFbids && message.supportedBotChannelFbids.length))
                                message.supportedBotChannelFbids = [];
                            message.supportedBotChannelFbids.push(reader.stringVerify());
                            continue;
                        }
                    case 24: {
                            if (u !== 0)
                                break;
                            message.supportInlineContacts = reader.bool();
                            message._supportInlineContacts = "supportInlineContacts";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a HistorySyncConfig message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.DeviceProps.HistorySyncConfig} HistorySyncConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HistorySyncConfig.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a HistorySyncConfig message.
             * @function verify
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            HistorySyncConfig.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.fullSyncDaysLimit != null && Object.hasOwnProperty.call(message, "fullSyncDaysLimit")) {
                    properties._fullSyncDaysLimit = 1;
                    if (!$util.isInteger(message.fullSyncDaysLimit))
                        return "fullSyncDaysLimit: integer expected";
                }
                if (message.fullSyncSizeMbLimit != null && Object.hasOwnProperty.call(message, "fullSyncSizeMbLimit")) {
                    properties._fullSyncSizeMbLimit = 1;
                    if (!$util.isInteger(message.fullSyncSizeMbLimit))
                        return "fullSyncSizeMbLimit: integer expected";
                }
                if (message.storageQuotaMb != null && Object.hasOwnProperty.call(message, "storageQuotaMb")) {
                    properties._storageQuotaMb = 1;
                    if (!$util.isInteger(message.storageQuotaMb))
                        return "storageQuotaMb: integer expected";
                }
                if (message.inlineInitialPayloadInE2EeMsg != null && Object.hasOwnProperty.call(message, "inlineInitialPayloadInE2EeMsg")) {
                    properties._inlineInitialPayloadInE2EeMsg = 1;
                    if (typeof message.inlineInitialPayloadInE2EeMsg !== "boolean")
                        return "inlineInitialPayloadInE2EeMsg: boolean expected";
                }
                if (message.recentSyncDaysLimit != null && Object.hasOwnProperty.call(message, "recentSyncDaysLimit")) {
                    properties._recentSyncDaysLimit = 1;
                    if (!$util.isInteger(message.recentSyncDaysLimit))
                        return "recentSyncDaysLimit: integer expected";
                }
                if (message.supportCallLogHistory != null && Object.hasOwnProperty.call(message, "supportCallLogHistory")) {
                    properties._supportCallLogHistory = 1;
                    if (typeof message.supportCallLogHistory !== "boolean")
                        return "supportCallLogHistory: boolean expected";
                }
                if (message.supportBotUserAgentChatHistory != null && Object.hasOwnProperty.call(message, "supportBotUserAgentChatHistory")) {
                    properties._supportBotUserAgentChatHistory = 1;
                    if (typeof message.supportBotUserAgentChatHistory !== "boolean")
                        return "supportBotUserAgentChatHistory: boolean expected";
                }
                if (message.supportCagReactionsAndPolls != null && Object.hasOwnProperty.call(message, "supportCagReactionsAndPolls")) {
                    properties._supportCagReactionsAndPolls = 1;
                    if (typeof message.supportCagReactionsAndPolls !== "boolean")
                        return "supportCagReactionsAndPolls: boolean expected";
                }
                if (message.supportBizHostedMsg != null && Object.hasOwnProperty.call(message, "supportBizHostedMsg")) {
                    properties._supportBizHostedMsg = 1;
                    if (typeof message.supportBizHostedMsg !== "boolean")
                        return "supportBizHostedMsg: boolean expected";
                }
                if (message.supportRecentSyncChunkMessageCountTuning != null && Object.hasOwnProperty.call(message, "supportRecentSyncChunkMessageCountTuning")) {
                    properties._supportRecentSyncChunkMessageCountTuning = 1;
                    if (typeof message.supportRecentSyncChunkMessageCountTuning !== "boolean")
                        return "supportRecentSyncChunkMessageCountTuning: boolean expected";
                }
                if (message.supportHostedGroupMsg != null && Object.hasOwnProperty.call(message, "supportHostedGroupMsg")) {
                    properties._supportHostedGroupMsg = 1;
                    if (typeof message.supportHostedGroupMsg !== "boolean")
                        return "supportHostedGroupMsg: boolean expected";
                }
                if (message.supportFbidBotChatHistory != null && Object.hasOwnProperty.call(message, "supportFbidBotChatHistory")) {
                    properties._supportFbidBotChatHistory = 1;
                    if (typeof message.supportFbidBotChatHistory !== "boolean")
                        return "supportFbidBotChatHistory: boolean expected";
                }
                if (message.supportAddOnHistorySyncMigration != null && Object.hasOwnProperty.call(message, "supportAddOnHistorySyncMigration")) {
                    properties._supportAddOnHistorySyncMigration = 1;
                    if (typeof message.supportAddOnHistorySyncMigration !== "boolean")
                        return "supportAddOnHistorySyncMigration: boolean expected";
                }
                if (message.supportMessageAssociation != null && Object.hasOwnProperty.call(message, "supportMessageAssociation")) {
                    properties._supportMessageAssociation = 1;
                    if (typeof message.supportMessageAssociation !== "boolean")
                        return "supportMessageAssociation: boolean expected";
                }
                if (message.supportGroupHistory != null && Object.hasOwnProperty.call(message, "supportGroupHistory")) {
                    properties._supportGroupHistory = 1;
                    if (typeof message.supportGroupHistory !== "boolean")
                        return "supportGroupHistory: boolean expected";
                }
                if (message.onDemandReady != null && Object.hasOwnProperty.call(message, "onDemandReady")) {
                    properties._onDemandReady = 1;
                    if (typeof message.onDemandReady !== "boolean")
                        return "onDemandReady: boolean expected";
                }
                if (message.supportGuestChat != null && Object.hasOwnProperty.call(message, "supportGuestChat")) {
                    properties._supportGuestChat = 1;
                    if (typeof message.supportGuestChat !== "boolean")
                        return "supportGuestChat: boolean expected";
                }
                if (message.completeOnDemandReady != null && Object.hasOwnProperty.call(message, "completeOnDemandReady")) {
                    properties._completeOnDemandReady = 1;
                    if (typeof message.completeOnDemandReady !== "boolean")
                        return "completeOnDemandReady: boolean expected";
                }
                if (message.thumbnailSyncDaysLimit != null && Object.hasOwnProperty.call(message, "thumbnailSyncDaysLimit")) {
                    properties._thumbnailSyncDaysLimit = 1;
                    if (!$util.isInteger(message.thumbnailSyncDaysLimit))
                        return "thumbnailSyncDaysLimit: integer expected";
                }
                if (message.initialSyncMaxMessagesPerChat != null && Object.hasOwnProperty.call(message, "initialSyncMaxMessagesPerChat")) {
                    properties._initialSyncMaxMessagesPerChat = 1;
                    if (!$util.isInteger(message.initialSyncMaxMessagesPerChat))
                        return "initialSyncMaxMessagesPerChat: integer expected";
                }
                if (message.supportManusHistory != null && Object.hasOwnProperty.call(message, "supportManusHistory")) {
                    properties._supportManusHistory = 1;
                    if (typeof message.supportManusHistory !== "boolean")
                        return "supportManusHistory: boolean expected";
                }
                if (message.supportHatchHistory != null && Object.hasOwnProperty.call(message, "supportHatchHistory")) {
                    properties._supportHatchHistory = 1;
                    if (typeof message.supportHatchHistory !== "boolean")
                        return "supportHatchHistory: boolean expected";
                }
                if (message.supportedBotChannelFbids != null && Object.hasOwnProperty.call(message, "supportedBotChannelFbids")) {
                    if (!Array.isArray(message.supportedBotChannelFbids))
                        return "supportedBotChannelFbids: array expected";
                    for (var i = 0; i < message.supportedBotChannelFbids.length; ++i)
                        if (!$util.isString(message.supportedBotChannelFbids[i]))
                            return "supportedBotChannelFbids: string[] expected";
                }
                if (message.supportInlineContacts != null && Object.hasOwnProperty.call(message, "supportInlineContacts")) {
                    properties._supportInlineContacts = 1;
                    if (typeof message.supportInlineContacts !== "boolean")
                        return "supportInlineContacts: boolean expected";
                }
                return null;
            };

            /**
             * Creates a HistorySyncConfig message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.DeviceProps.HistorySyncConfig} HistorySyncConfig
             */
            HistorySyncConfig.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.DeviceProps.HistorySyncConfig: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.fullSyncDaysLimit != null)
                    message.fullSyncDaysLimit = object.fullSyncDaysLimit >>> 0;
                if (object.fullSyncSizeMbLimit != null)
                    message.fullSyncSizeMbLimit = object.fullSyncSizeMbLimit >>> 0;
                if (object.storageQuotaMb != null)
                    message.storageQuotaMb = object.storageQuotaMb >>> 0;
                if (object.inlineInitialPayloadInE2EeMsg != null)
                    message.inlineInitialPayloadInE2EeMsg = Boolean(object.inlineInitialPayloadInE2EeMsg);
                if (object.recentSyncDaysLimit != null)
                    message.recentSyncDaysLimit = object.recentSyncDaysLimit >>> 0;
                if (object.supportCallLogHistory != null)
                    message.supportCallLogHistory = Boolean(object.supportCallLogHistory);
                if (object.supportBotUserAgentChatHistory != null)
                    message.supportBotUserAgentChatHistory = Boolean(object.supportBotUserAgentChatHistory);
                if (object.supportCagReactionsAndPolls != null)
                    message.supportCagReactionsAndPolls = Boolean(object.supportCagReactionsAndPolls);
                if (object.supportBizHostedMsg != null)
                    message.supportBizHostedMsg = Boolean(object.supportBizHostedMsg);
                if (object.supportRecentSyncChunkMessageCountTuning != null)
                    message.supportRecentSyncChunkMessageCountTuning = Boolean(object.supportRecentSyncChunkMessageCountTuning);
                if (object.supportHostedGroupMsg != null)
                    message.supportHostedGroupMsg = Boolean(object.supportHostedGroupMsg);
                if (object.supportFbidBotChatHistory != null)
                    message.supportFbidBotChatHistory = Boolean(object.supportFbidBotChatHistory);
                if (object.supportAddOnHistorySyncMigration != null)
                    message.supportAddOnHistorySyncMigration = Boolean(object.supportAddOnHistorySyncMigration);
                if (object.supportMessageAssociation != null)
                    message.supportMessageAssociation = Boolean(object.supportMessageAssociation);
                if (object.supportGroupHistory != null)
                    message.supportGroupHistory = Boolean(object.supportGroupHistory);
                if (object.onDemandReady != null)
                    message.onDemandReady = Boolean(object.onDemandReady);
                if (object.supportGuestChat != null)
                    message.supportGuestChat = Boolean(object.supportGuestChat);
                if (object.completeOnDemandReady != null)
                    message.completeOnDemandReady = Boolean(object.completeOnDemandReady);
                if (object.thumbnailSyncDaysLimit != null)
                    message.thumbnailSyncDaysLimit = object.thumbnailSyncDaysLimit >>> 0;
                if (object.initialSyncMaxMessagesPerChat != null)
                    message.initialSyncMaxMessagesPerChat = object.initialSyncMaxMessagesPerChat >>> 0;
                if (object.supportManusHistory != null)
                    message.supportManusHistory = Boolean(object.supportManusHistory);
                if (object.supportHatchHistory != null)
                    message.supportHatchHistory = Boolean(object.supportHatchHistory);
                if (object.supportedBotChannelFbids) {
                    if (!Array.isArray(object.supportedBotChannelFbids))
                        throw TypeError(".proto.DeviceProps.HistorySyncConfig.supportedBotChannelFbids: array expected");
                    message.supportedBotChannelFbids = Array(object.supportedBotChannelFbids.length);
                    for (var i = 0; i < object.supportedBotChannelFbids.length; ++i)
                        message.supportedBotChannelFbids[i] = String(object.supportedBotChannelFbids[i]);
                }
                if (object.supportInlineContacts != null)
                    message.supportInlineContacts = Boolean(object.supportInlineContacts);
                return message;
            };

            /**
             * Creates a plain object from a HistorySyncConfig message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @static
             * @param {proto.DeviceProps.HistorySyncConfig} message HistorySyncConfig
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            HistorySyncConfig.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.supportedBotChannelFbids = [];
                if (message.fullSyncDaysLimit != null && Object.hasOwnProperty.call(message, "fullSyncDaysLimit"))
                    object.fullSyncDaysLimit = message.fullSyncDaysLimit;
                if (message.fullSyncSizeMbLimit != null && Object.hasOwnProperty.call(message, "fullSyncSizeMbLimit"))
                    object.fullSyncSizeMbLimit = message.fullSyncSizeMbLimit;
                if (message.storageQuotaMb != null && Object.hasOwnProperty.call(message, "storageQuotaMb"))
                    object.storageQuotaMb = message.storageQuotaMb;
                if (message.inlineInitialPayloadInE2EeMsg != null && Object.hasOwnProperty.call(message, "inlineInitialPayloadInE2EeMsg"))
                    object.inlineInitialPayloadInE2EeMsg = message.inlineInitialPayloadInE2EeMsg;
                if (message.recentSyncDaysLimit != null && Object.hasOwnProperty.call(message, "recentSyncDaysLimit"))
                    object.recentSyncDaysLimit = message.recentSyncDaysLimit;
                if (message.supportCallLogHistory != null && Object.hasOwnProperty.call(message, "supportCallLogHistory"))
                    object.supportCallLogHistory = message.supportCallLogHistory;
                if (message.supportBotUserAgentChatHistory != null && Object.hasOwnProperty.call(message, "supportBotUserAgentChatHistory"))
                    object.supportBotUserAgentChatHistory = message.supportBotUserAgentChatHistory;
                if (message.supportCagReactionsAndPolls != null && Object.hasOwnProperty.call(message, "supportCagReactionsAndPolls"))
                    object.supportCagReactionsAndPolls = message.supportCagReactionsAndPolls;
                if (message.supportBizHostedMsg != null && Object.hasOwnProperty.call(message, "supportBizHostedMsg"))
                    object.supportBizHostedMsg = message.supportBizHostedMsg;
                if (message.supportRecentSyncChunkMessageCountTuning != null && Object.hasOwnProperty.call(message, "supportRecentSyncChunkMessageCountTuning"))
                    object.supportRecentSyncChunkMessageCountTuning = message.supportRecentSyncChunkMessageCountTuning;
                if (message.supportHostedGroupMsg != null && Object.hasOwnProperty.call(message, "supportHostedGroupMsg"))
                    object.supportHostedGroupMsg = message.supportHostedGroupMsg;
                if (message.supportFbidBotChatHistory != null && Object.hasOwnProperty.call(message, "supportFbidBotChatHistory"))
                    object.supportFbidBotChatHistory = message.supportFbidBotChatHistory;
                if (message.supportAddOnHistorySyncMigration != null && Object.hasOwnProperty.call(message, "supportAddOnHistorySyncMigration"))
                    object.supportAddOnHistorySyncMigration = message.supportAddOnHistorySyncMigration;
                if (message.supportMessageAssociation != null && Object.hasOwnProperty.call(message, "supportMessageAssociation"))
                    object.supportMessageAssociation = message.supportMessageAssociation;
                if (message.supportGroupHistory != null && Object.hasOwnProperty.call(message, "supportGroupHistory"))
                    object.supportGroupHistory = message.supportGroupHistory;
                if (message.onDemandReady != null && Object.hasOwnProperty.call(message, "onDemandReady"))
                    object.onDemandReady = message.onDemandReady;
                if (message.supportGuestChat != null && Object.hasOwnProperty.call(message, "supportGuestChat"))
                    object.supportGuestChat = message.supportGuestChat;
                if (message.completeOnDemandReady != null && Object.hasOwnProperty.call(message, "completeOnDemandReady"))
                    object.completeOnDemandReady = message.completeOnDemandReady;
                if (message.thumbnailSyncDaysLimit != null && Object.hasOwnProperty.call(message, "thumbnailSyncDaysLimit"))
                    object.thumbnailSyncDaysLimit = message.thumbnailSyncDaysLimit;
                if (message.initialSyncMaxMessagesPerChat != null && Object.hasOwnProperty.call(message, "initialSyncMaxMessagesPerChat"))
                    object.initialSyncMaxMessagesPerChat = message.initialSyncMaxMessagesPerChat;
                if (message.supportManusHistory != null && Object.hasOwnProperty.call(message, "supportManusHistory"))
                    object.supportManusHistory = message.supportManusHistory;
                if (message.supportHatchHistory != null && Object.hasOwnProperty.call(message, "supportHatchHistory"))
                    object.supportHatchHistory = message.supportHatchHistory;
                if (message.supportedBotChannelFbids && message.supportedBotChannelFbids.length) {
                    object.supportedBotChannelFbids = Array(message.supportedBotChannelFbids.length);
                    for (var j = 0; j < message.supportedBotChannelFbids.length; ++j)
                        object.supportedBotChannelFbids[j] = message.supportedBotChannelFbids[j];
                }
                if (message.supportInlineContacts != null && Object.hasOwnProperty.call(message, "supportInlineContacts"))
                    object.supportInlineContacts = message.supportInlineContacts;
                return object;
            };

            /**
             * Converts this HistorySyncConfig to JSON.
             * @function toJSON
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            HistorySyncConfig.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for HistorySyncConfig
             * @function getTypeUrl
             * @memberof proto.DeviceProps.HistorySyncConfig
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            HistorySyncConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.DeviceProps.HistorySyncConfig";
            };

            return HistorySyncConfig;
        })();

        /**
         * PlatformType enum.
         * @name proto.DeviceProps.PlatformType
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} CHROME=1 CHROME value
         * @property {number} FIREFOX=2 FIREFOX value
         * @property {number} IE=3 IE value
         * @property {number} OPERA=4 OPERA value
         * @property {number} SAFARI=5 SAFARI value
         * @property {number} EDGE=6 EDGE value
         * @property {number} DESKTOP=7 DESKTOP value
         * @property {number} IPAD=8 IPAD value
         * @property {number} ANDROID_TABLET=9 ANDROID_TABLET value
         * @property {number} OHANA=10 OHANA value
         * @property {number} ALOHA=11 ALOHA value
         * @property {number} CATALINA=12 CATALINA value
         * @property {number} TCL_TV=13 TCL_TV value
         * @property {number} IOS_PHONE=14 IOS_PHONE value
         * @property {number} IOS_CATALYST=15 IOS_CATALYST value
         * @property {number} ANDROID_PHONE=16 ANDROID_PHONE value
         * @property {number} ANDROID_AMBIGUOUS=17 ANDROID_AMBIGUOUS value
         * @property {number} WEAR_OS=18 WEAR_OS value
         * @property {number} AR_WRIST=19 AR_WRIST value
         * @property {number} AR_DEVICE=20 AR_DEVICE value
         * @property {number} UWP=21 UWP value
         * @property {number} VR=22 VR value
         * @property {number} CLOUD_API=23 CLOUD_API value
         * @property {number} SMARTGLASSES=24 SMARTGLASSES value
         */
        DeviceProps.PlatformType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "CHROME"] = 1;
            values[valuesById[2] = "FIREFOX"] = 2;
            values[valuesById[3] = "IE"] = 3;
            values[valuesById[4] = "OPERA"] = 4;
            values[valuesById[5] = "SAFARI"] = 5;
            values[valuesById[6] = "EDGE"] = 6;
            values[valuesById[7] = "DESKTOP"] = 7;
            values[valuesById[8] = "IPAD"] = 8;
            values[valuesById[9] = "ANDROID_TABLET"] = 9;
            values[valuesById[10] = "OHANA"] = 10;
            values[valuesById[11] = "ALOHA"] = 11;
            values[valuesById[12] = "CATALINA"] = 12;
            values[valuesById[13] = "TCL_TV"] = 13;
            values[valuesById[14] = "IOS_PHONE"] = 14;
            values[valuesById[15] = "IOS_CATALYST"] = 15;
            values[valuesById[16] = "ANDROID_PHONE"] = 16;
            values[valuesById[17] = "ANDROID_AMBIGUOUS"] = 17;
            values[valuesById[18] = "WEAR_OS"] = 18;
            values[valuesById[19] = "AR_WRIST"] = 19;
            values[valuesById[20] = "AR_DEVICE"] = 20;
            values[valuesById[21] = "UWP"] = 21;
            values[valuesById[22] = "VR"] = 22;
            values[valuesById[23] = "CLOUD_API"] = 23;
            values[valuesById[24] = "SMARTGLASSES"] = 24;
            return values;
        })();

        return DeviceProps;
    })();

    proto.DisappearingMode = (function() {

        /**
         * Properties of a DisappearingMode.
         * @memberof proto
         * @interface IDisappearingMode
         * @property {proto.DisappearingMode.Initiator|null} [initiator] DisappearingMode initiator
         * @property {proto.DisappearingMode.Trigger|null} [trigger] DisappearingMode trigger
         * @property {string|null} [initiatorDeviceJid] DisappearingMode initiatorDeviceJid
         * @property {boolean|null} [initiatedByMe] DisappearingMode initiatedByMe
         */

        /**
         * Constructs a new DisappearingMode.
         * @memberof proto
         * @classdesc Represents a DisappearingMode.
         * @implements IDisappearingMode
         * @constructor
         * @param {proto.IDisappearingMode=} [properties] Properties to set
         */
        function DisappearingMode(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DisappearingMode initiator.
         * @member {proto.DisappearingMode.Initiator|null|undefined} initiator
         * @memberof proto.DisappearingMode
         * @instance
         */
        DisappearingMode.prototype.initiator = null;

        /**
         * DisappearingMode trigger.
         * @member {proto.DisappearingMode.Trigger|null|undefined} trigger
         * @memberof proto.DisappearingMode
         * @instance
         */
        DisappearingMode.prototype.trigger = null;

        /**
         * DisappearingMode initiatorDeviceJid.
         * @member {string|null|undefined} initiatorDeviceJid
         * @memberof proto.DisappearingMode
         * @instance
         */
        DisappearingMode.prototype.initiatorDeviceJid = null;

        /**
         * DisappearingMode initiatedByMe.
         * @member {boolean|null|undefined} initiatedByMe
         * @memberof proto.DisappearingMode
         * @instance
         */
        DisappearingMode.prototype.initiatedByMe = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DisappearingMode.prototype, "_initiator", {
            get: $util.oneOfGetter($oneOfFields = ["initiator"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DisappearingMode.prototype, "_trigger", {
            get: $util.oneOfGetter($oneOfFields = ["trigger"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DisappearingMode.prototype, "_initiatorDeviceJid", {
            get: $util.oneOfGetter($oneOfFields = ["initiatorDeviceJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DisappearingMode.prototype, "_initiatedByMe", {
            get: $util.oneOfGetter($oneOfFields = ["initiatedByMe"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new DisappearingMode instance using the specified properties.
         * @function create
         * @memberof proto.DisappearingMode
         * @static
         * @param {proto.IDisappearingMode=} [properties] Properties to set
         * @returns {proto.DisappearingMode} DisappearingMode instance
         */
        DisappearingMode.create = function create(properties) {
            return new DisappearingMode(properties);
        };

        /**
         * Encodes the specified DisappearingMode message. Does not implicitly {@link proto.DisappearingMode.verify|verify} messages.
         * @function encode
         * @memberof proto.DisappearingMode
         * @static
         * @param {proto.IDisappearingMode} message DisappearingMode message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DisappearingMode.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.initiator != null && Object.hasOwnProperty.call(message, "initiator"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.initiator);
            if (message.trigger != null && Object.hasOwnProperty.call(message, "trigger"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.trigger);
            if (message.initiatorDeviceJid != null && Object.hasOwnProperty.call(message, "initiatorDeviceJid"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.initiatorDeviceJid);
            if (message.initiatedByMe != null && Object.hasOwnProperty.call(message, "initiatedByMe"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.initiatedByMe);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified DisappearingMode message, length delimited. Does not implicitly {@link proto.DisappearingMode.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.DisappearingMode
         * @static
         * @param {proto.IDisappearingMode} message DisappearingMode message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DisappearingMode.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DisappearingMode message from the specified reader or buffer.
         * @function decode
         * @memberof proto.DisappearingMode
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.DisappearingMode} DisappearingMode
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DisappearingMode.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.initiator = reader.int32();
                        message._initiator = "initiator";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.trigger = reader.int32();
                        message._trigger = "trigger";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.initiatorDeviceJid = reader.stringVerify();
                        message._initiatorDeviceJid = "initiatorDeviceJid";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.initiatedByMe = reader.bool();
                        message._initiatedByMe = "initiatedByMe";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a DisappearingMode message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.DisappearingMode
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.DisappearingMode} DisappearingMode
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DisappearingMode.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DisappearingMode message.
         * @function verify
         * @memberof proto.DisappearingMode
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DisappearingMode.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.initiator != null && Object.hasOwnProperty.call(message, "initiator")) {
                properties._initiator = 1;
                if (typeof message.initiator !== "number" || (message.initiator | 0) !== message.initiator)
                    return "initiator: enum value expected";
            }
            if (message.trigger != null && Object.hasOwnProperty.call(message, "trigger")) {
                properties._trigger = 1;
                if (typeof message.trigger !== "number" || (message.trigger | 0) !== message.trigger)
                    return "trigger: enum value expected";
            }
            if (message.initiatorDeviceJid != null && Object.hasOwnProperty.call(message, "initiatorDeviceJid")) {
                properties._initiatorDeviceJid = 1;
                if (!$util.isString(message.initiatorDeviceJid))
                    return "initiatorDeviceJid: string expected";
            }
            if (message.initiatedByMe != null && Object.hasOwnProperty.call(message, "initiatedByMe")) {
                properties._initiatedByMe = 1;
                if (typeof message.initiatedByMe !== "boolean")
                    return "initiatedByMe: boolean expected";
            }
            return null;
        };

        /**
         * Creates a DisappearingMode message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.DisappearingMode
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.DisappearingMode} DisappearingMode
         */
        DisappearingMode.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.DisappearingMode: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.initiator) {
            case "CHANGED_IN_CHAT":
            case 0:
                message.initiator = 0;
                break;
            case "INITIATED_BY_ME":
            case 1:
                message.initiator = 1;
                break;
            case "INITIATED_BY_OTHER":
            case 2:
                message.initiator = 2;
                break;
            case "BIZ_UPGRADE_FB_HOSTING":
            case 3:
                message.initiator = 3;
                break;
            default:
                if (typeof object.initiator === "number" && (object.initiator | 0) === object.initiator)
                    message.initiator = object.initiator;
            }
            switch (object.trigger) {
            case "UNKNOWN":
            case 0:
                message.trigger = 0;
                break;
            case "CHAT_SETTING":
            case 1:
                message.trigger = 1;
                break;
            case "ACCOUNT_SETTING":
            case 2:
                message.trigger = 2;
                break;
            case "BULK_CHANGE":
            case 3:
                message.trigger = 3;
                break;
            case "BIZ_SUPPORTS_FB_HOSTING":
            case 4:
                message.trigger = 4;
                break;
            case "UNKNOWN_GROUPS":
            case 5:
                message.trigger = 5;
                break;
            default:
                if (typeof object.trigger === "number" && (object.trigger | 0) === object.trigger)
                    message.trigger = object.trigger;
            }
            if (object.initiatorDeviceJid != null)
                message.initiatorDeviceJid = String(object.initiatorDeviceJid);
            if (object.initiatedByMe != null)
                message.initiatedByMe = Boolean(object.initiatedByMe);
            return message;
        };

        /**
         * Creates a plain object from a DisappearingMode message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.DisappearingMode
         * @static
         * @param {proto.DisappearingMode} message DisappearingMode
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DisappearingMode.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.initiator != null && Object.hasOwnProperty.call(message, "initiator"))
                object.initiator = options.enums === String ? $root.proto.DisappearingMode.Initiator[message.initiator] === undefined ? message.initiator : $root.proto.DisappearingMode.Initiator[message.initiator] : message.initiator;
            if (message.trigger != null && Object.hasOwnProperty.call(message, "trigger"))
                object.trigger = options.enums === String ? $root.proto.DisappearingMode.Trigger[message.trigger] === undefined ? message.trigger : $root.proto.DisappearingMode.Trigger[message.trigger] : message.trigger;
            if (message.initiatorDeviceJid != null && Object.hasOwnProperty.call(message, "initiatorDeviceJid"))
                object.initiatorDeviceJid = message.initiatorDeviceJid;
            if (message.initiatedByMe != null && Object.hasOwnProperty.call(message, "initiatedByMe"))
                object.initiatedByMe = message.initiatedByMe;
            return object;
        };

        /**
         * Converts this DisappearingMode to JSON.
         * @function toJSON
         * @memberof proto.DisappearingMode
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DisappearingMode.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DisappearingMode
         * @function getTypeUrl
         * @memberof proto.DisappearingMode
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DisappearingMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.DisappearingMode";
        };

        /**
         * Initiator enum.
         * @name proto.DisappearingMode.Initiator
         * @enum {number}
         * @property {number} CHANGED_IN_CHAT=0 CHANGED_IN_CHAT value
         * @property {number} INITIATED_BY_ME=1 INITIATED_BY_ME value
         * @property {number} INITIATED_BY_OTHER=2 INITIATED_BY_OTHER value
         * @property {number} BIZ_UPGRADE_FB_HOSTING=3 BIZ_UPGRADE_FB_HOSTING value
         */
        DisappearingMode.Initiator = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "CHANGED_IN_CHAT"] = 0;
            values[valuesById[1] = "INITIATED_BY_ME"] = 1;
            values[valuesById[2] = "INITIATED_BY_OTHER"] = 2;
            values[valuesById[3] = "BIZ_UPGRADE_FB_HOSTING"] = 3;
            return values;
        })();

        /**
         * Trigger enum.
         * @name proto.DisappearingMode.Trigger
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} CHAT_SETTING=1 CHAT_SETTING value
         * @property {number} ACCOUNT_SETTING=2 ACCOUNT_SETTING value
         * @property {number} BULK_CHANGE=3 BULK_CHANGE value
         * @property {number} BIZ_SUPPORTS_FB_HOSTING=4 BIZ_SUPPORTS_FB_HOSTING value
         * @property {number} UNKNOWN_GROUPS=5 UNKNOWN_GROUPS value
         */
        DisappearingMode.Trigger = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "CHAT_SETTING"] = 1;
            values[valuesById[2] = "ACCOUNT_SETTING"] = 2;
            values[valuesById[3] = "BULK_CHANGE"] = 3;
            values[valuesById[4] = "BIZ_SUPPORTS_FB_HOSTING"] = 4;
            values[valuesById[5] = "UNKNOWN_GROUPS"] = 5;
            return values;
        })();

        return DisappearingMode;
    })();

    proto.EmbeddedContent = (function() {

        /**
         * Properties of an EmbeddedContent.
         * @memberof proto
         * @interface IEmbeddedContent
         * @property {proto.IEmbeddedMessage|null} [embeddedMessage] EmbeddedContent embeddedMessage
         * @property {proto.IEmbeddedMusic|null} [embeddedMusic] EmbeddedContent embeddedMusic
         */

        /**
         * Constructs a new EmbeddedContent.
         * @memberof proto
         * @classdesc Represents an EmbeddedContent.
         * @implements IEmbeddedContent
         * @constructor
         * @param {proto.IEmbeddedContent=} [properties] Properties to set
         */
        function EmbeddedContent(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EmbeddedContent embeddedMessage.
         * @member {proto.IEmbeddedMessage|null|undefined} embeddedMessage
         * @memberof proto.EmbeddedContent
         * @instance
         */
        EmbeddedContent.prototype.embeddedMessage = null;

        /**
         * EmbeddedContent embeddedMusic.
         * @member {proto.IEmbeddedMusic|null|undefined} embeddedMusic
         * @memberof proto.EmbeddedContent
         * @instance
         */
        EmbeddedContent.prototype.embeddedMusic = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        /**
         * EmbeddedContent content.
         * @member {"embeddedMessage"|"embeddedMusic"|undefined} content
         * @memberof proto.EmbeddedContent
         * @instance
         */
        Object.defineProperty(EmbeddedContent.prototype, "content", {
            get: $util.oneOfGetter($oneOfFields = ["embeddedMessage", "embeddedMusic"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new EmbeddedContent instance using the specified properties.
         * @function create
         * @memberof proto.EmbeddedContent
         * @static
         * @param {proto.IEmbeddedContent=} [properties] Properties to set
         * @returns {proto.EmbeddedContent} EmbeddedContent instance
         */
        EmbeddedContent.create = function create(properties) {
            return new EmbeddedContent(properties);
        };

        /**
         * Encodes the specified EmbeddedContent message. Does not implicitly {@link proto.EmbeddedContent.verify|verify} messages.
         * @function encode
         * @memberof proto.EmbeddedContent
         * @static
         * @param {proto.IEmbeddedContent} message EmbeddedContent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EmbeddedContent.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.embeddedMessage != null && Object.hasOwnProperty.call(message, "embeddedMessage"))
                $root.proto.EmbeddedMessage.encode(message.embeddedMessage, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.embeddedMusic != null && Object.hasOwnProperty.call(message, "embeddedMusic"))
                $root.proto.EmbeddedMusic.encode(message.embeddedMusic, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified EmbeddedContent message, length delimited. Does not implicitly {@link proto.EmbeddedContent.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.EmbeddedContent
         * @static
         * @param {proto.IEmbeddedContent} message EmbeddedContent message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EmbeddedContent.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an EmbeddedContent message from the specified reader or buffer.
         * @function decode
         * @memberof proto.EmbeddedContent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.EmbeddedContent} EmbeddedContent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EmbeddedContent.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.embeddedMessage = $root.proto.EmbeddedMessage.decode(reader, reader.uint32(), undefined, q + 1, message.embeddedMessage);
                        message.content = "embeddedMessage";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.embeddedMusic = $root.proto.EmbeddedMusic.decode(reader, reader.uint32(), undefined, q + 1, message.embeddedMusic);
                        message.content = "embeddedMusic";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes an EmbeddedContent message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.EmbeddedContent
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.EmbeddedContent} EmbeddedContent
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EmbeddedContent.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EmbeddedContent message.
         * @function verify
         * @memberof proto.EmbeddedContent
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EmbeddedContent.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.embeddedMessage != null && Object.hasOwnProperty.call(message, "embeddedMessage")) {
                properties.content = 1;
                {
                    var error = $root.proto.EmbeddedMessage.verify(message.embeddedMessage, q + 1);
                    if (error)
                        return "embeddedMessage." + error;
                }
            }
            if (message.embeddedMusic != null && Object.hasOwnProperty.call(message, "embeddedMusic")) {
                if (properties.content === 1)
                    return "content: multiple values";
                properties.content = 1;
                {
                    var error = $root.proto.EmbeddedMusic.verify(message.embeddedMusic, q + 1);
                    if (error)
                        return "embeddedMusic." + error;
                }
            }
            return null;
        };

        /**
         * Creates an EmbeddedContent message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.EmbeddedContent
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.EmbeddedContent} EmbeddedContent
         */
        EmbeddedContent.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.EmbeddedContent: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.embeddedMessage != null) {
                if (!$util.isObject(object.embeddedMessage))
                    throw TypeError(".proto.EmbeddedContent.embeddedMessage: object expected");
                message.embeddedMessage = $root.proto.EmbeddedMessage.fromObject(object.embeddedMessage, q + 1);
            }
            if (object.embeddedMusic != null) {
                if (!$util.isObject(object.embeddedMusic))
                    throw TypeError(".proto.EmbeddedContent.embeddedMusic: object expected");
                message.embeddedMusic = $root.proto.EmbeddedMusic.fromObject(object.embeddedMusic, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an EmbeddedContent message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.EmbeddedContent
         * @static
         * @param {proto.EmbeddedContent} message EmbeddedContent
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EmbeddedContent.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.embeddedMessage != null && Object.hasOwnProperty.call(message, "embeddedMessage")) {
                object.embeddedMessage = $root.proto.EmbeddedMessage.toObject(message.embeddedMessage, options, q + 1);
                if (options.oneofs)
                    object.content = "embeddedMessage";
            }
            if (message.embeddedMusic != null && Object.hasOwnProperty.call(message, "embeddedMusic")) {
                object.embeddedMusic = $root.proto.EmbeddedMusic.toObject(message.embeddedMusic, options, q + 1);
                if (options.oneofs)
                    object.content = "embeddedMusic";
            }
            return object;
        };

        /**
         * Converts this EmbeddedContent to JSON.
         * @function toJSON
         * @memberof proto.EmbeddedContent
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EmbeddedContent.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EmbeddedContent
         * @function getTypeUrl
         * @memberof proto.EmbeddedContent
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EmbeddedContent.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.EmbeddedContent";
        };

        return EmbeddedContent;
    })();

    proto.EmbeddedMessage = (function() {

        /**
         * Properties of an EmbeddedMessage.
         * @memberof proto
         * @interface IEmbeddedMessage
         * @property {string|null} [stanzaId] EmbeddedMessage stanzaId
         * @property {proto.IMessage|null} [message] EmbeddedMessage message
         */

        /**
         * Constructs a new EmbeddedMessage.
         * @memberof proto
         * @classdesc Represents an EmbeddedMessage.
         * @implements IEmbeddedMessage
         * @constructor
         * @param {proto.IEmbeddedMessage=} [properties] Properties to set
         */
        function EmbeddedMessage(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EmbeddedMessage stanzaId.
         * @member {string|null|undefined} stanzaId
         * @memberof proto.EmbeddedMessage
         * @instance
         */
        EmbeddedMessage.prototype.stanzaId = null;

        /**
         * EmbeddedMessage message.
         * @member {proto.IMessage|null|undefined} message
         * @memberof proto.EmbeddedMessage
         * @instance
         */
        EmbeddedMessage.prototype.message = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMessage.prototype, "_stanzaId", {
            get: $util.oneOfGetter($oneOfFields = ["stanzaId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMessage.prototype, "_message", {
            get: $util.oneOfGetter($oneOfFields = ["message"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new EmbeddedMessage instance using the specified properties.
         * @function create
         * @memberof proto.EmbeddedMessage
         * @static
         * @param {proto.IEmbeddedMessage=} [properties] Properties to set
         * @returns {proto.EmbeddedMessage} EmbeddedMessage instance
         */
        EmbeddedMessage.create = function create(properties) {
            return new EmbeddedMessage(properties);
        };

        /**
         * Encodes the specified EmbeddedMessage message. Does not implicitly {@link proto.EmbeddedMessage.verify|verify} messages.
         * @function encode
         * @memberof proto.EmbeddedMessage
         * @static
         * @param {proto.IEmbeddedMessage} message EmbeddedMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EmbeddedMessage.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.stanzaId != null && Object.hasOwnProperty.call(message, "stanzaId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.stanzaId);
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                $root.proto.Message.encode(message.message, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified EmbeddedMessage message, length delimited. Does not implicitly {@link proto.EmbeddedMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.EmbeddedMessage
         * @static
         * @param {proto.IEmbeddedMessage} message EmbeddedMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EmbeddedMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an EmbeddedMessage message from the specified reader or buffer.
         * @function decode
         * @memberof proto.EmbeddedMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.EmbeddedMessage} EmbeddedMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EmbeddedMessage.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.stanzaId = reader.stringVerify();
                        message._stanzaId = "stanzaId";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.message = $root.proto.Message.decode(reader, reader.uint32(), undefined, q + 1, message.message);
                        message._message = "message";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes an EmbeddedMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.EmbeddedMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.EmbeddedMessage} EmbeddedMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EmbeddedMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EmbeddedMessage message.
         * @function verify
         * @memberof proto.EmbeddedMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EmbeddedMessage.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.stanzaId != null && Object.hasOwnProperty.call(message, "stanzaId")) {
                properties._stanzaId = 1;
                if (!$util.isString(message.stanzaId))
                    return "stanzaId: string expected";
            }
            if (message.message != null && Object.hasOwnProperty.call(message, "message")) {
                properties._message = 1;
                {
                    var error = $root.proto.Message.verify(message.message, q + 1);
                    if (error)
                        return "message." + error;
                }
            }
            return null;
        };

        /**
         * Creates an EmbeddedMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.EmbeddedMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.EmbeddedMessage} EmbeddedMessage
         */
        EmbeddedMessage.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.EmbeddedMessage: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.stanzaId != null)
                message.stanzaId = String(object.stanzaId);
            if (object.message != null) {
                if (!$util.isObject(object.message))
                    throw TypeError(".proto.EmbeddedMessage.message: object expected");
                message.message = $root.proto.Message.fromObject(object.message, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an EmbeddedMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.EmbeddedMessage
         * @static
         * @param {proto.EmbeddedMessage} message EmbeddedMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EmbeddedMessage.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.stanzaId != null && Object.hasOwnProperty.call(message, "stanzaId"))
                object.stanzaId = message.stanzaId;
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                object.message = $root.proto.Message.toObject(message.message, options, q + 1);
            return object;
        };

        /**
         * Converts this EmbeddedMessage to JSON.
         * @function toJSON
         * @memberof proto.EmbeddedMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EmbeddedMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EmbeddedMessage
         * @function getTypeUrl
         * @memberof proto.EmbeddedMessage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EmbeddedMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.EmbeddedMessage";
        };

        return EmbeddedMessage;
    })();

    proto.EmbeddedMusic = (function() {

        /**
         * Properties of an EmbeddedMusic.
         * @memberof proto
         * @interface IEmbeddedMusic
         * @property {string|null} [musicContentMediaId] EmbeddedMusic musicContentMediaId
         * @property {string|null} [songId] EmbeddedMusic songId
         * @property {string|null} [author] EmbeddedMusic author
         * @property {string|null} [title] EmbeddedMusic title
         * @property {string|null} [artworkDirectPath] EmbeddedMusic artworkDirectPath
         * @property {Uint8Array|null} [artworkSha256] EmbeddedMusic artworkSha256
         * @property {Uint8Array|null} [artworkEncSha256] EmbeddedMusic artworkEncSha256
         * @property {string|null} [artistAttribution] EmbeddedMusic artistAttribution
         * @property {Uint8Array|null} [countryBlocklist] EmbeddedMusic countryBlocklist
         * @property {boolean|null} [isExplicit] EmbeddedMusic isExplicit
         * @property {Uint8Array|null} [artworkMediaKey] EmbeddedMusic artworkMediaKey
         * @property {number|Long|null} [musicSongStartTimeInMs] EmbeddedMusic musicSongStartTimeInMs
         * @property {number|Long|null} [derivedContentStartTimeInMs] EmbeddedMusic derivedContentStartTimeInMs
         * @property {number|Long|null} [overlapDurationInMs] EmbeddedMusic overlapDurationInMs
         */

        /**
         * Constructs a new EmbeddedMusic.
         * @memberof proto
         * @classdesc Represents an EmbeddedMusic.
         * @implements IEmbeddedMusic
         * @constructor
         * @param {proto.IEmbeddedMusic=} [properties] Properties to set
         */
        function EmbeddedMusic(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EmbeddedMusic musicContentMediaId.
         * @member {string|null|undefined} musicContentMediaId
         * @memberof proto.EmbeddedMusic
         * @instance
         */
        EmbeddedMusic.prototype.musicContentMediaId = null;

        /**
         * EmbeddedMusic songId.
         * @member {string|null|undefined} songId
         * @memberof proto.EmbeddedMusic
         * @instance
         */
        EmbeddedMusic.prototype.songId = null;

        /**
         * EmbeddedMusic author.
         * @member {string|null|undefined} author
         * @memberof proto.EmbeddedMusic
         * @instance
         */
        EmbeddedMusic.prototype.author = null;

        /**
         * EmbeddedMusic title.
         * @member {string|null|undefined} title
         * @memberof proto.EmbeddedMusic
         * @instance
         */
        EmbeddedMusic.prototype.title = null;

        /**
         * EmbeddedMusic artworkDirectPath.
         * @member {string|null|undefined} artworkDirectPath
         * @memberof proto.EmbeddedMusic
         * @instance
         */
        EmbeddedMusic.prototype.artworkDirectPath = null;

        /**
         * EmbeddedMusic artworkSha256.
         * @member {Uint8Array|null|undefined} artworkSha256
         * @memberof proto.EmbeddedMusic
         * @instance
         */
        EmbeddedMusic.prototype.artworkSha256 = null;

        /**
         * EmbeddedMusic artworkEncSha256.
         * @member {Uint8Array|null|undefined} artworkEncSha256
         * @memberof proto.EmbeddedMusic
         * @instance
         */
        EmbeddedMusic.prototype.artworkEncSha256 = null;

        /**
         * EmbeddedMusic artistAttribution.
         * @member {string|null|undefined} artistAttribution
         * @memberof proto.EmbeddedMusic
         * @instance
         */
        EmbeddedMusic.prototype.artistAttribution = null;

        /**
         * EmbeddedMusic countryBlocklist.
         * @member {Uint8Array|null|undefined} countryBlocklist
         * @memberof proto.EmbeddedMusic
         * @instance
         */
        EmbeddedMusic.prototype.countryBlocklist = null;

        /**
         * EmbeddedMusic isExplicit.
         * @member {boolean|null|undefined} isExplicit
         * @memberof proto.EmbeddedMusic
         * @instance
         */
        EmbeddedMusic.prototype.isExplicit = null;

        /**
         * EmbeddedMusic artworkMediaKey.
         * @member {Uint8Array|null|undefined} artworkMediaKey
         * @memberof proto.EmbeddedMusic
         * @instance
         */
        EmbeddedMusic.prototype.artworkMediaKey = null;

        /**
         * EmbeddedMusic musicSongStartTimeInMs.
         * @member {number|Long|null|undefined} musicSongStartTimeInMs
         * @memberof proto.EmbeddedMusic
         * @instance
         */
        EmbeddedMusic.prototype.musicSongStartTimeInMs = null;

        /**
         * EmbeddedMusic derivedContentStartTimeInMs.
         * @member {number|Long|null|undefined} derivedContentStartTimeInMs
         * @memberof proto.EmbeddedMusic
         * @instance
         */
        EmbeddedMusic.prototype.derivedContentStartTimeInMs = null;

        /**
         * EmbeddedMusic overlapDurationInMs.
         * @member {number|Long|null|undefined} overlapDurationInMs
         * @memberof proto.EmbeddedMusic
         * @instance
         */
        EmbeddedMusic.prototype.overlapDurationInMs = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_musicContentMediaId", {
            get: $util.oneOfGetter($oneOfFields = ["musicContentMediaId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_songId", {
            get: $util.oneOfGetter($oneOfFields = ["songId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_author", {
            get: $util.oneOfGetter($oneOfFields = ["author"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_title", {
            get: $util.oneOfGetter($oneOfFields = ["title"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_artworkDirectPath", {
            get: $util.oneOfGetter($oneOfFields = ["artworkDirectPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_artworkSha256", {
            get: $util.oneOfGetter($oneOfFields = ["artworkSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_artworkEncSha256", {
            get: $util.oneOfGetter($oneOfFields = ["artworkEncSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_artistAttribution", {
            get: $util.oneOfGetter($oneOfFields = ["artistAttribution"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_countryBlocklist", {
            get: $util.oneOfGetter($oneOfFields = ["countryBlocklist"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_isExplicit", {
            get: $util.oneOfGetter($oneOfFields = ["isExplicit"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_artworkMediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["artworkMediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_musicSongStartTimeInMs", {
            get: $util.oneOfGetter($oneOfFields = ["musicSongStartTimeInMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_derivedContentStartTimeInMs", {
            get: $util.oneOfGetter($oneOfFields = ["derivedContentStartTimeInMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_overlapDurationInMs", {
            get: $util.oneOfGetter($oneOfFields = ["overlapDurationInMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new EmbeddedMusic instance using the specified properties.
         * @function create
         * @memberof proto.EmbeddedMusic
         * @static
         * @param {proto.IEmbeddedMusic=} [properties] Properties to set
         * @returns {proto.EmbeddedMusic} EmbeddedMusic instance
         */
        EmbeddedMusic.create = function create(properties) {
            return new EmbeddedMusic(properties);
        };

        /**
         * Encodes the specified EmbeddedMusic message. Does not implicitly {@link proto.EmbeddedMusic.verify|verify} messages.
         * @function encode
         * @memberof proto.EmbeddedMusic
         * @static
         * @param {proto.IEmbeddedMusic} message EmbeddedMusic message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EmbeddedMusic.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.musicContentMediaId != null && Object.hasOwnProperty.call(message, "musicContentMediaId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.musicContentMediaId);
            if (message.songId != null && Object.hasOwnProperty.call(message, "songId"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.songId);
            if (message.author != null && Object.hasOwnProperty.call(message, "author"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.author);
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.title);
            if (message.artworkDirectPath != null && Object.hasOwnProperty.call(message, "artworkDirectPath"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.artworkDirectPath);
            if (message.artworkSha256 != null && Object.hasOwnProperty.call(message, "artworkSha256"))
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.artworkSha256);
            if (message.artworkEncSha256 != null && Object.hasOwnProperty.call(message, "artworkEncSha256"))
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.artworkEncSha256);
            if (message.artistAttribution != null && Object.hasOwnProperty.call(message, "artistAttribution"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.artistAttribution);
            if (message.countryBlocklist != null && Object.hasOwnProperty.call(message, "countryBlocklist"))
                writer.uint32(/* id 9, wireType 2 =*/74).bytes(message.countryBlocklist);
            if (message.isExplicit != null && Object.hasOwnProperty.call(message, "isExplicit"))
                writer.uint32(/* id 10, wireType 0 =*/80).bool(message.isExplicit);
            if (message.artworkMediaKey != null && Object.hasOwnProperty.call(message, "artworkMediaKey"))
                writer.uint32(/* id 11, wireType 2 =*/90).bytes(message.artworkMediaKey);
            if (message.musicSongStartTimeInMs != null && Object.hasOwnProperty.call(message, "musicSongStartTimeInMs"))
                writer.uint32(/* id 12, wireType 0 =*/96).int64(message.musicSongStartTimeInMs);
            if (message.derivedContentStartTimeInMs != null && Object.hasOwnProperty.call(message, "derivedContentStartTimeInMs"))
                writer.uint32(/* id 13, wireType 0 =*/104).int64(message.derivedContentStartTimeInMs);
            if (message.overlapDurationInMs != null && Object.hasOwnProperty.call(message, "overlapDurationInMs"))
                writer.uint32(/* id 14, wireType 0 =*/112).int64(message.overlapDurationInMs);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified EmbeddedMusic message, length delimited. Does not implicitly {@link proto.EmbeddedMusic.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.EmbeddedMusic
         * @static
         * @param {proto.IEmbeddedMusic} message EmbeddedMusic message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EmbeddedMusic.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an EmbeddedMusic message from the specified reader or buffer.
         * @function decode
         * @memberof proto.EmbeddedMusic
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.EmbeddedMusic} EmbeddedMusic
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EmbeddedMusic.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.musicContentMediaId = reader.stringVerify();
                        message._musicContentMediaId = "musicContentMediaId";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.songId = reader.stringVerify();
                        message._songId = "songId";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.author = reader.stringVerify();
                        message._author = "author";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.title = reader.stringVerify();
                        message._title = "title";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.artworkDirectPath = reader.stringVerify();
                        message._artworkDirectPath = "artworkDirectPath";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        message.artworkSha256 = reader.bytes();
                        message._artworkSha256 = "artworkSha256";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        message.artworkEncSha256 = reader.bytes();
                        message._artworkEncSha256 = "artworkEncSha256";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        message.artistAttribution = reader.stringVerify();
                        message._artistAttribution = "artistAttribution";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        message.countryBlocklist = reader.bytes();
                        message._countryBlocklist = "countryBlocklist";
                        continue;
                    }
                case 10: {
                        if (u !== 0)
                            break;
                        message.isExplicit = reader.bool();
                        message._isExplicit = "isExplicit";
                        continue;
                    }
                case 11: {
                        if (u !== 2)
                            break;
                        message.artworkMediaKey = reader.bytes();
                        message._artworkMediaKey = "artworkMediaKey";
                        continue;
                    }
                case 12: {
                        if (u !== 0)
                            break;
                        message.musicSongStartTimeInMs = reader.int64();
                        message._musicSongStartTimeInMs = "musicSongStartTimeInMs";
                        continue;
                    }
                case 13: {
                        if (u !== 0)
                            break;
                        message.derivedContentStartTimeInMs = reader.int64();
                        message._derivedContentStartTimeInMs = "derivedContentStartTimeInMs";
                        continue;
                    }
                case 14: {
                        if (u !== 0)
                            break;
                        message.overlapDurationInMs = reader.int64();
                        message._overlapDurationInMs = "overlapDurationInMs";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes an EmbeddedMusic message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.EmbeddedMusic
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.EmbeddedMusic} EmbeddedMusic
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EmbeddedMusic.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EmbeddedMusic message.
         * @function verify
         * @memberof proto.EmbeddedMusic
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EmbeddedMusic.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.musicContentMediaId != null && Object.hasOwnProperty.call(message, "musicContentMediaId")) {
                properties._musicContentMediaId = 1;
                if (!$util.isString(message.musicContentMediaId))
                    return "musicContentMediaId: string expected";
            }
            if (message.songId != null && Object.hasOwnProperty.call(message, "songId")) {
                properties._songId = 1;
                if (!$util.isString(message.songId))
                    return "songId: string expected";
            }
            if (message.author != null && Object.hasOwnProperty.call(message, "author")) {
                properties._author = 1;
                if (!$util.isString(message.author))
                    return "author: string expected";
            }
            if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                properties._title = 1;
                if (!$util.isString(message.title))
                    return "title: string expected";
            }
            if (message.artworkDirectPath != null && Object.hasOwnProperty.call(message, "artworkDirectPath")) {
                properties._artworkDirectPath = 1;
                if (!$util.isString(message.artworkDirectPath))
                    return "artworkDirectPath: string expected";
            }
            if (message.artworkSha256 != null && Object.hasOwnProperty.call(message, "artworkSha256")) {
                properties._artworkSha256 = 1;
                if (!(message.artworkSha256 && typeof message.artworkSha256.length === "number" || $util.isString(message.artworkSha256)))
                    return "artworkSha256: buffer expected";
            }
            if (message.artworkEncSha256 != null && Object.hasOwnProperty.call(message, "artworkEncSha256")) {
                properties._artworkEncSha256 = 1;
                if (!(message.artworkEncSha256 && typeof message.artworkEncSha256.length === "number" || $util.isString(message.artworkEncSha256)))
                    return "artworkEncSha256: buffer expected";
            }
            if (message.artistAttribution != null && Object.hasOwnProperty.call(message, "artistAttribution")) {
                properties._artistAttribution = 1;
                if (!$util.isString(message.artistAttribution))
                    return "artistAttribution: string expected";
            }
            if (message.countryBlocklist != null && Object.hasOwnProperty.call(message, "countryBlocklist")) {
                properties._countryBlocklist = 1;
                if (!(message.countryBlocklist && typeof message.countryBlocklist.length === "number" || $util.isString(message.countryBlocklist)))
                    return "countryBlocklist: buffer expected";
            }
            if (message.isExplicit != null && Object.hasOwnProperty.call(message, "isExplicit")) {
                properties._isExplicit = 1;
                if (typeof message.isExplicit !== "boolean")
                    return "isExplicit: boolean expected";
            }
            if (message.artworkMediaKey != null && Object.hasOwnProperty.call(message, "artworkMediaKey")) {
                properties._artworkMediaKey = 1;
                if (!(message.artworkMediaKey && typeof message.artworkMediaKey.length === "number" || $util.isString(message.artworkMediaKey)))
                    return "artworkMediaKey: buffer expected";
            }
            if (message.musicSongStartTimeInMs != null && Object.hasOwnProperty.call(message, "musicSongStartTimeInMs")) {
                properties._musicSongStartTimeInMs = 1;
                if (!$util.isInteger(message.musicSongStartTimeInMs) && !(message.musicSongStartTimeInMs && $util.isInteger(message.musicSongStartTimeInMs.low) && $util.isInteger(message.musicSongStartTimeInMs.high)))
                    return "musicSongStartTimeInMs: integer|Long expected";
            }
            if (message.derivedContentStartTimeInMs != null && Object.hasOwnProperty.call(message, "derivedContentStartTimeInMs")) {
                properties._derivedContentStartTimeInMs = 1;
                if (!$util.isInteger(message.derivedContentStartTimeInMs) && !(message.derivedContentStartTimeInMs && $util.isInteger(message.derivedContentStartTimeInMs.low) && $util.isInteger(message.derivedContentStartTimeInMs.high)))
                    return "derivedContentStartTimeInMs: integer|Long expected";
            }
            if (message.overlapDurationInMs != null && Object.hasOwnProperty.call(message, "overlapDurationInMs")) {
                properties._overlapDurationInMs = 1;
                if (!$util.isInteger(message.overlapDurationInMs) && !(message.overlapDurationInMs && $util.isInteger(message.overlapDurationInMs.low) && $util.isInteger(message.overlapDurationInMs.high)))
                    return "overlapDurationInMs: integer|Long expected";
            }
            return null;
        };

        /**
         * Creates an EmbeddedMusic message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.EmbeddedMusic
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.EmbeddedMusic} EmbeddedMusic
         */
        EmbeddedMusic.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.EmbeddedMusic: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.musicContentMediaId != null)
                message.musicContentMediaId = String(object.musicContentMediaId);
            if (object.songId != null)
                message.songId = String(object.songId);
            if (object.author != null)
                message.author = String(object.author);
            if (object.title != null)
                message.title = String(object.title);
            if (object.artworkDirectPath != null)
                message.artworkDirectPath = String(object.artworkDirectPath);
            if (object.artworkSha256 != null)
                if (typeof object.artworkSha256 === "string")
                    $util.base64.decode(object.artworkSha256, message.artworkSha256 = $util.newBuffer($util.base64.length(object.artworkSha256)), 0);
                else if (object.artworkSha256.length >= 0)
                    message.artworkSha256 = object.artworkSha256;
            if (object.artworkEncSha256 != null)
                if (typeof object.artworkEncSha256 === "string")
                    $util.base64.decode(object.artworkEncSha256, message.artworkEncSha256 = $util.newBuffer($util.base64.length(object.artworkEncSha256)), 0);
                else if (object.artworkEncSha256.length >= 0)
                    message.artworkEncSha256 = object.artworkEncSha256;
            if (object.artistAttribution != null)
                message.artistAttribution = String(object.artistAttribution);
            if (object.countryBlocklist != null)
                if (typeof object.countryBlocklist === "string")
                    $util.base64.decode(object.countryBlocklist, message.countryBlocklist = $util.newBuffer($util.base64.length(object.countryBlocklist)), 0);
                else if (object.countryBlocklist.length >= 0)
                    message.countryBlocklist = object.countryBlocklist;
            if (object.isExplicit != null)
                message.isExplicit = Boolean(object.isExplicit);
            if (object.artworkMediaKey != null)
                if (typeof object.artworkMediaKey === "string")
                    $util.base64.decode(object.artworkMediaKey, message.artworkMediaKey = $util.newBuffer($util.base64.length(object.artworkMediaKey)), 0);
                else if (object.artworkMediaKey.length >= 0)
                    message.artworkMediaKey = object.artworkMediaKey;
            if (object.musicSongStartTimeInMs != null)
                if ($util.Long)
                    message.musicSongStartTimeInMs = $util.Long.fromValue(object.musicSongStartTimeInMs, false);
                else if (typeof object.musicSongStartTimeInMs === "string")
                    message.musicSongStartTimeInMs = parseInt(object.musicSongStartTimeInMs, 10);
                else if (typeof object.musicSongStartTimeInMs === "number")
                    message.musicSongStartTimeInMs = object.musicSongStartTimeInMs;
                else if (typeof object.musicSongStartTimeInMs === "object")
                    message.musicSongStartTimeInMs = new $util.LongBits(object.musicSongStartTimeInMs.low >>> 0, object.musicSongStartTimeInMs.high >>> 0).toNumber();
            if (object.derivedContentStartTimeInMs != null)
                if ($util.Long)
                    message.derivedContentStartTimeInMs = $util.Long.fromValue(object.derivedContentStartTimeInMs, false);
                else if (typeof object.derivedContentStartTimeInMs === "string")
                    message.derivedContentStartTimeInMs = parseInt(object.derivedContentStartTimeInMs, 10);
                else if (typeof object.derivedContentStartTimeInMs === "number")
                    message.derivedContentStartTimeInMs = object.derivedContentStartTimeInMs;
                else if (typeof object.derivedContentStartTimeInMs === "object")
                    message.derivedContentStartTimeInMs = new $util.LongBits(object.derivedContentStartTimeInMs.low >>> 0, object.derivedContentStartTimeInMs.high >>> 0).toNumber();
            if (object.overlapDurationInMs != null)
                if ($util.Long)
                    message.overlapDurationInMs = $util.Long.fromValue(object.overlapDurationInMs, false);
                else if (typeof object.overlapDurationInMs === "string")
                    message.overlapDurationInMs = parseInt(object.overlapDurationInMs, 10);
                else if (typeof object.overlapDurationInMs === "number")
                    message.overlapDurationInMs = object.overlapDurationInMs;
                else if (typeof object.overlapDurationInMs === "object")
                    message.overlapDurationInMs = new $util.LongBits(object.overlapDurationInMs.low >>> 0, object.overlapDurationInMs.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from an EmbeddedMusic message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.EmbeddedMusic
         * @static
         * @param {proto.EmbeddedMusic} message EmbeddedMusic
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EmbeddedMusic.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.musicContentMediaId != null && Object.hasOwnProperty.call(message, "musicContentMediaId"))
                object.musicContentMediaId = message.musicContentMediaId;
            if (message.songId != null && Object.hasOwnProperty.call(message, "songId"))
                object.songId = message.songId;
            if (message.author != null && Object.hasOwnProperty.call(message, "author"))
                object.author = message.author;
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                object.title = message.title;
            if (message.artworkDirectPath != null && Object.hasOwnProperty.call(message, "artworkDirectPath"))
                object.artworkDirectPath = message.artworkDirectPath;
            if (message.artworkSha256 != null && Object.hasOwnProperty.call(message, "artworkSha256"))
                object.artworkSha256 = options.bytes === String ? $util.base64.encode(message.artworkSha256, 0, message.artworkSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.artworkSha256) : message.artworkSha256;
            if (message.artworkEncSha256 != null && Object.hasOwnProperty.call(message, "artworkEncSha256"))
                object.artworkEncSha256 = options.bytes === String ? $util.base64.encode(message.artworkEncSha256, 0, message.artworkEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.artworkEncSha256) : message.artworkEncSha256;
            if (message.artistAttribution != null && Object.hasOwnProperty.call(message, "artistAttribution"))
                object.artistAttribution = message.artistAttribution;
            if (message.countryBlocklist != null && Object.hasOwnProperty.call(message, "countryBlocklist"))
                object.countryBlocklist = options.bytes === String ? $util.base64.encode(message.countryBlocklist, 0, message.countryBlocklist.length) : options.bytes === Array ? Array.prototype.slice.call(message.countryBlocklist) : message.countryBlocklist;
            if (message.isExplicit != null && Object.hasOwnProperty.call(message, "isExplicit"))
                object.isExplicit = message.isExplicit;
            if (message.artworkMediaKey != null && Object.hasOwnProperty.call(message, "artworkMediaKey"))
                object.artworkMediaKey = options.bytes === String ? $util.base64.encode(message.artworkMediaKey, 0, message.artworkMediaKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.artworkMediaKey) : message.artworkMediaKey;
            if (message.musicSongStartTimeInMs != null && Object.hasOwnProperty.call(message, "musicSongStartTimeInMs"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.musicSongStartTimeInMs = typeof message.musicSongStartTimeInMs === "number" ? BigInt(message.musicSongStartTimeInMs) : $util.Long.fromBits(message.musicSongStartTimeInMs.low >>> 0, message.musicSongStartTimeInMs.high >>> 0, false).toBigInt();
                else if (typeof message.musicSongStartTimeInMs === "number")
                    object.musicSongStartTimeInMs = options.longs === String ? String(message.musicSongStartTimeInMs) : message.musicSongStartTimeInMs;
                else
                    object.musicSongStartTimeInMs = options.longs === String ? $util.Long.prototype.toString.call(message.musicSongStartTimeInMs) : options.longs === Number ? new $util.LongBits(message.musicSongStartTimeInMs.low >>> 0, message.musicSongStartTimeInMs.high >>> 0).toNumber() : message.musicSongStartTimeInMs;
            if (message.derivedContentStartTimeInMs != null && Object.hasOwnProperty.call(message, "derivedContentStartTimeInMs"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.derivedContentStartTimeInMs = typeof message.derivedContentStartTimeInMs === "number" ? BigInt(message.derivedContentStartTimeInMs) : $util.Long.fromBits(message.derivedContentStartTimeInMs.low >>> 0, message.derivedContentStartTimeInMs.high >>> 0, false).toBigInt();
                else if (typeof message.derivedContentStartTimeInMs === "number")
                    object.derivedContentStartTimeInMs = options.longs === String ? String(message.derivedContentStartTimeInMs) : message.derivedContentStartTimeInMs;
                else
                    object.derivedContentStartTimeInMs = options.longs === String ? $util.Long.prototype.toString.call(message.derivedContentStartTimeInMs) : options.longs === Number ? new $util.LongBits(message.derivedContentStartTimeInMs.low >>> 0, message.derivedContentStartTimeInMs.high >>> 0).toNumber() : message.derivedContentStartTimeInMs;
            if (message.overlapDurationInMs != null && Object.hasOwnProperty.call(message, "overlapDurationInMs"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.overlapDurationInMs = typeof message.overlapDurationInMs === "number" ? BigInt(message.overlapDurationInMs) : $util.Long.fromBits(message.overlapDurationInMs.low >>> 0, message.overlapDurationInMs.high >>> 0, false).toBigInt();
                else if (typeof message.overlapDurationInMs === "number")
                    object.overlapDurationInMs = options.longs === String ? String(message.overlapDurationInMs) : message.overlapDurationInMs;
                else
                    object.overlapDurationInMs = options.longs === String ? $util.Long.prototype.toString.call(message.overlapDurationInMs) : options.longs === Number ? new $util.LongBits(message.overlapDurationInMs.low >>> 0, message.overlapDurationInMs.high >>> 0).toNumber() : message.overlapDurationInMs;
            return object;
        };

        /**
         * Converts this EmbeddedMusic to JSON.
         * @function toJSON
         * @memberof proto.EmbeddedMusic
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EmbeddedMusic.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EmbeddedMusic
         * @function getTypeUrl
         * @memberof proto.EmbeddedMusic
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EmbeddedMusic.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.EmbeddedMusic";
        };

        return EmbeddedMusic;
    })();

    proto.EncryptedPairingRequest = (function() {

        /**
         * Properties of an EncryptedPairingRequest.
         * @memberof proto
         * @interface IEncryptedPairingRequest
         * @property {Uint8Array|null} [encryptedPayload] EncryptedPairingRequest encryptedPayload
         * @property {Uint8Array|null} [iv] EncryptedPairingRequest iv
         */

        /**
         * Constructs a new EncryptedPairingRequest.
         * @memberof proto
         * @classdesc Represents an EncryptedPairingRequest.
         * @implements IEncryptedPairingRequest
         * @constructor
         * @param {proto.IEncryptedPairingRequest=} [properties] Properties to set
         */
        function EncryptedPairingRequest(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EncryptedPairingRequest encryptedPayload.
         * @member {Uint8Array|null|undefined} encryptedPayload
         * @memberof proto.EncryptedPairingRequest
         * @instance
         */
        EncryptedPairingRequest.prototype.encryptedPayload = null;

        /**
         * EncryptedPairingRequest iv.
         * @member {Uint8Array|null|undefined} iv
         * @memberof proto.EncryptedPairingRequest
         * @instance
         */
        EncryptedPairingRequest.prototype.iv = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EncryptedPairingRequest.prototype, "_encryptedPayload", {
            get: $util.oneOfGetter($oneOfFields = ["encryptedPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EncryptedPairingRequest.prototype, "_iv", {
            get: $util.oneOfGetter($oneOfFields = ["iv"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new EncryptedPairingRequest instance using the specified properties.
         * @function create
         * @memberof proto.EncryptedPairingRequest
         * @static
         * @param {proto.IEncryptedPairingRequest=} [properties] Properties to set
         * @returns {proto.EncryptedPairingRequest} EncryptedPairingRequest instance
         */
        EncryptedPairingRequest.create = function create(properties) {
            return new EncryptedPairingRequest(properties);
        };

        /**
         * Encodes the specified EncryptedPairingRequest message. Does not implicitly {@link proto.EncryptedPairingRequest.verify|verify} messages.
         * @function encode
         * @memberof proto.EncryptedPairingRequest
         * @static
         * @param {proto.IEncryptedPairingRequest} message EncryptedPairingRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EncryptedPairingRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedPayload != null && Object.hasOwnProperty.call(message, "encryptedPayload"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedPayload);
            if (message.iv != null && Object.hasOwnProperty.call(message, "iv"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.iv);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified EncryptedPairingRequest message, length delimited. Does not implicitly {@link proto.EncryptedPairingRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.EncryptedPairingRequest
         * @static
         * @param {proto.IEncryptedPairingRequest} message EncryptedPairingRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EncryptedPairingRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an EncryptedPairingRequest message from the specified reader or buffer.
         * @function decode
         * @memberof proto.EncryptedPairingRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.EncryptedPairingRequest} EncryptedPairingRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EncryptedPairingRequest.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.encryptedPayload = reader.bytes();
                        message._encryptedPayload = "encryptedPayload";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.iv = reader.bytes();
                        message._iv = "iv";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes an EncryptedPairingRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.EncryptedPairingRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.EncryptedPairingRequest} EncryptedPairingRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EncryptedPairingRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EncryptedPairingRequest message.
         * @function verify
         * @memberof proto.EncryptedPairingRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EncryptedPairingRequest.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.encryptedPayload != null && Object.hasOwnProperty.call(message, "encryptedPayload")) {
                properties._encryptedPayload = 1;
                if (!(message.encryptedPayload && typeof message.encryptedPayload.length === "number" || $util.isString(message.encryptedPayload)))
                    return "encryptedPayload: buffer expected";
            }
            if (message.iv != null && Object.hasOwnProperty.call(message, "iv")) {
                properties._iv = 1;
                if (!(message.iv && typeof message.iv.length === "number" || $util.isString(message.iv)))
                    return "iv: buffer expected";
            }
            return null;
        };

        /**
         * Creates an EncryptedPairingRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.EncryptedPairingRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.EncryptedPairingRequest} EncryptedPairingRequest
         */
        EncryptedPairingRequest.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.EncryptedPairingRequest: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.encryptedPayload != null)
                if (typeof object.encryptedPayload === "string")
                    $util.base64.decode(object.encryptedPayload, message.encryptedPayload = $util.newBuffer($util.base64.length(object.encryptedPayload)), 0);
                else if (object.encryptedPayload.length >= 0)
                    message.encryptedPayload = object.encryptedPayload;
            if (object.iv != null)
                if (typeof object.iv === "string")
                    $util.base64.decode(object.iv, message.iv = $util.newBuffer($util.base64.length(object.iv)), 0);
                else if (object.iv.length >= 0)
                    message.iv = object.iv;
            return message;
        };

        /**
         * Creates a plain object from an EncryptedPairingRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.EncryptedPairingRequest
         * @static
         * @param {proto.EncryptedPairingRequest} message EncryptedPairingRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EncryptedPairingRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.encryptedPayload != null && Object.hasOwnProperty.call(message, "encryptedPayload"))
                object.encryptedPayload = options.bytes === String ? $util.base64.encode(message.encryptedPayload, 0, message.encryptedPayload.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedPayload) : message.encryptedPayload;
            if (message.iv != null && Object.hasOwnProperty.call(message, "iv"))
                object.iv = options.bytes === String ? $util.base64.encode(message.iv, 0, message.iv.length) : options.bytes === Array ? Array.prototype.slice.call(message.iv) : message.iv;
            return object;
        };

        /**
         * Converts this EncryptedPairingRequest to JSON.
         * @function toJSON
         * @memberof proto.EncryptedPairingRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EncryptedPairingRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EncryptedPairingRequest
         * @function getTypeUrl
         * @memberof proto.EncryptedPairingRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EncryptedPairingRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.EncryptedPairingRequest";
        };

        return EncryptedPairingRequest;
    })();

    proto.EphemeralSetting = (function() {

        /**
         * Properties of an EphemeralSetting.
         * @memberof proto
         * @interface IEphemeralSetting
         * @property {number|null} [duration] EphemeralSetting duration
         * @property {number|Long|null} [timestamp] EphemeralSetting timestamp
         */

        /**
         * Constructs a new EphemeralSetting.
         * @memberof proto
         * @classdesc Represents an EphemeralSetting.
         * @implements IEphemeralSetting
         * @constructor
         * @param {proto.IEphemeralSetting=} [properties] Properties to set
         */
        function EphemeralSetting(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EphemeralSetting duration.
         * @member {number|null|undefined} duration
         * @memberof proto.EphemeralSetting
         * @instance
         */
        EphemeralSetting.prototype.duration = null;

        /**
         * EphemeralSetting timestamp.
         * @member {number|Long|null|undefined} timestamp
         * @memberof proto.EphemeralSetting
         * @instance
         */
        EphemeralSetting.prototype.timestamp = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EphemeralSetting.prototype, "_duration", {
            get: $util.oneOfGetter($oneOfFields = ["duration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EphemeralSetting.prototype, "_timestamp", {
            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new EphemeralSetting instance using the specified properties.
         * @function create
         * @memberof proto.EphemeralSetting
         * @static
         * @param {proto.IEphemeralSetting=} [properties] Properties to set
         * @returns {proto.EphemeralSetting} EphemeralSetting instance
         */
        EphemeralSetting.create = function create(properties) {
            return new EphemeralSetting(properties);
        };

        /**
         * Encodes the specified EphemeralSetting message. Does not implicitly {@link proto.EphemeralSetting.verify|verify} messages.
         * @function encode
         * @memberof proto.EphemeralSetting
         * @static
         * @param {proto.IEphemeralSetting} message EphemeralSetting message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EphemeralSetting.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.duration != null && Object.hasOwnProperty.call(message, "duration"))
                writer.uint32(/* id 1, wireType 5 =*/13).sfixed32(message.duration);
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                writer.uint32(/* id 2, wireType 1 =*/17).sfixed64(message.timestamp);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified EphemeralSetting message, length delimited. Does not implicitly {@link proto.EphemeralSetting.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.EphemeralSetting
         * @static
         * @param {proto.IEphemeralSetting} message EphemeralSetting message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EphemeralSetting.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an EphemeralSetting message from the specified reader or buffer.
         * @function decode
         * @memberof proto.EphemeralSetting
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.EphemeralSetting} EphemeralSetting
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EphemeralSetting.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 5)
                            break;
                        message.duration = reader.sfixed32();
                        message._duration = "duration";
                        continue;
                    }
                case 2: {
                        if (u !== 1)
                            break;
                        message.timestamp = reader.sfixed64();
                        message._timestamp = "timestamp";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes an EphemeralSetting message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.EphemeralSetting
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.EphemeralSetting} EphemeralSetting
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EphemeralSetting.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EphemeralSetting message.
         * @function verify
         * @memberof proto.EphemeralSetting
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EphemeralSetting.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.duration != null && Object.hasOwnProperty.call(message, "duration")) {
                properties._duration = 1;
                if (!$util.isInteger(message.duration))
                    return "duration: integer expected";
            }
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp")) {
                properties._timestamp = 1;
                if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                    return "timestamp: integer|Long expected";
            }
            return null;
        };

        /**
         * Creates an EphemeralSetting message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.EphemeralSetting
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.EphemeralSetting} EphemeralSetting
         */
        EphemeralSetting.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.EphemeralSetting: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.duration != null)
                message.duration = object.duration | 0;
            if (object.timestamp != null)
                if ($util.Long)
                    message.timestamp = $util.Long.fromValue(object.timestamp, false);
                else if (typeof object.timestamp === "string")
                    message.timestamp = parseInt(object.timestamp, 10);
                else if (typeof object.timestamp === "number")
                    message.timestamp = object.timestamp;
                else if (typeof object.timestamp === "object")
                    message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from an EphemeralSetting message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.EphemeralSetting
         * @static
         * @param {proto.EphemeralSetting} message EphemeralSetting
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EphemeralSetting.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.duration != null && Object.hasOwnProperty.call(message, "duration"))
                object.duration = message.duration;
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.timestamp = typeof message.timestamp === "number" ? BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
                else if (typeof message.timestamp === "number")
                    object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                else
                    object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
            return object;
        };

        /**
         * Converts this EphemeralSetting to JSON.
         * @function toJSON
         * @memberof proto.EphemeralSetting
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EphemeralSetting.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EphemeralSetting
         * @function getTypeUrl
         * @memberof proto.EphemeralSetting
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EphemeralSetting.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.EphemeralSetting";
        };

        return EphemeralSetting;
    })();

    proto.EventAdditionalMetadata = (function() {

        /**
         * Properties of an EventAdditionalMetadata.
         * @memberof proto
         * @interface IEventAdditionalMetadata
         * @property {boolean|null} [isStale] EventAdditionalMetadata isStale
         */

        /**
         * Constructs a new EventAdditionalMetadata.
         * @memberof proto
         * @classdesc Represents an EventAdditionalMetadata.
         * @implements IEventAdditionalMetadata
         * @constructor
         * @param {proto.IEventAdditionalMetadata=} [properties] Properties to set
         */
        function EventAdditionalMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EventAdditionalMetadata isStale.
         * @member {boolean|null|undefined} isStale
         * @memberof proto.EventAdditionalMetadata
         * @instance
         */
        EventAdditionalMetadata.prototype.isStale = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventAdditionalMetadata.prototype, "_isStale", {
            get: $util.oneOfGetter($oneOfFields = ["isStale"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new EventAdditionalMetadata instance using the specified properties.
         * @function create
         * @memberof proto.EventAdditionalMetadata
         * @static
         * @param {proto.IEventAdditionalMetadata=} [properties] Properties to set
         * @returns {proto.EventAdditionalMetadata} EventAdditionalMetadata instance
         */
        EventAdditionalMetadata.create = function create(properties) {
            return new EventAdditionalMetadata(properties);
        };

        /**
         * Encodes the specified EventAdditionalMetadata message. Does not implicitly {@link proto.EventAdditionalMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.EventAdditionalMetadata
         * @static
         * @param {proto.IEventAdditionalMetadata} message EventAdditionalMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EventAdditionalMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.isStale != null && Object.hasOwnProperty.call(message, "isStale"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.isStale);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified EventAdditionalMetadata message, length delimited. Does not implicitly {@link proto.EventAdditionalMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.EventAdditionalMetadata
         * @static
         * @param {proto.IEventAdditionalMetadata} message EventAdditionalMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EventAdditionalMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an EventAdditionalMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.EventAdditionalMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.EventAdditionalMetadata} EventAdditionalMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EventAdditionalMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.isStale = reader.bool();
                        message._isStale = "isStale";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes an EventAdditionalMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.EventAdditionalMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.EventAdditionalMetadata} EventAdditionalMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EventAdditionalMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EventAdditionalMetadata message.
         * @function verify
         * @memberof proto.EventAdditionalMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EventAdditionalMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.isStale != null && Object.hasOwnProperty.call(message, "isStale")) {
                properties._isStale = 1;
                if (typeof message.isStale !== "boolean")
                    return "isStale: boolean expected";
            }
            return null;
        };

        /**
         * Creates an EventAdditionalMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.EventAdditionalMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.EventAdditionalMetadata} EventAdditionalMetadata
         */
        EventAdditionalMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.EventAdditionalMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.isStale != null)
                message.isStale = Boolean(object.isStale);
            return message;
        };

        /**
         * Creates a plain object from an EventAdditionalMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.EventAdditionalMetadata
         * @static
         * @param {proto.EventAdditionalMetadata} message EventAdditionalMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EventAdditionalMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.isStale != null && Object.hasOwnProperty.call(message, "isStale"))
                object.isStale = message.isStale;
            return object;
        };

        /**
         * Converts this EventAdditionalMetadata to JSON.
         * @function toJSON
         * @memberof proto.EventAdditionalMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EventAdditionalMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EventAdditionalMetadata
         * @function getTypeUrl
         * @memberof proto.EventAdditionalMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EventAdditionalMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.EventAdditionalMetadata";
        };

        return EventAdditionalMetadata;
    })();

    proto.EventResponse = (function() {

        /**
         * Properties of an EventResponse.
         * @memberof proto
         * @interface IEventResponse
         * @property {proto.IMessageKey|null} [eventResponseMessageKey] EventResponse eventResponseMessageKey
         * @property {number|Long|null} [timestampMs] EventResponse timestampMs
         * @property {proto.Message.IEventResponseMessage|null} [eventResponseMessage] EventResponse eventResponseMessage
         * @property {boolean|null} [unread] EventResponse unread
         */

        /**
         * Constructs a new EventResponse.
         * @memberof proto
         * @classdesc Represents an EventResponse.
         * @implements IEventResponse
         * @constructor
         * @param {proto.IEventResponse=} [properties] Properties to set
         */
        function EventResponse(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EventResponse eventResponseMessageKey.
         * @member {proto.IMessageKey|null|undefined} eventResponseMessageKey
         * @memberof proto.EventResponse
         * @instance
         */
        EventResponse.prototype.eventResponseMessageKey = null;

        /**
         * EventResponse timestampMs.
         * @member {number|Long|null|undefined} timestampMs
         * @memberof proto.EventResponse
         * @instance
         */
        EventResponse.prototype.timestampMs = null;

        /**
         * EventResponse eventResponseMessage.
         * @member {proto.Message.IEventResponseMessage|null|undefined} eventResponseMessage
         * @memberof proto.EventResponse
         * @instance
         */
        EventResponse.prototype.eventResponseMessage = null;

        /**
         * EventResponse unread.
         * @member {boolean|null|undefined} unread
         * @memberof proto.EventResponse
         * @instance
         */
        EventResponse.prototype.unread = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventResponse.prototype, "_eventResponseMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["eventResponseMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventResponse.prototype, "_timestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["timestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventResponse.prototype, "_eventResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["eventResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventResponse.prototype, "_unread", {
            get: $util.oneOfGetter($oneOfFields = ["unread"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new EventResponse instance using the specified properties.
         * @function create
         * @memberof proto.EventResponse
         * @static
         * @param {proto.IEventResponse=} [properties] Properties to set
         * @returns {proto.EventResponse} EventResponse instance
         */
        EventResponse.create = function create(properties) {
            return new EventResponse(properties);
        };

        /**
         * Encodes the specified EventResponse message. Does not implicitly {@link proto.EventResponse.verify|verify} messages.
         * @function encode
         * @memberof proto.EventResponse
         * @static
         * @param {proto.IEventResponse} message EventResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EventResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.eventResponseMessageKey != null && Object.hasOwnProperty.call(message, "eventResponseMessageKey"))
                $root.proto.MessageKey.encode(message.eventResponseMessageKey, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.timestampMs != null && Object.hasOwnProperty.call(message, "timestampMs"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.timestampMs);
            if (message.eventResponseMessage != null && Object.hasOwnProperty.call(message, "eventResponseMessage"))
                $root.proto.Message.EventResponseMessage.encode(message.eventResponseMessage, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.unread != null && Object.hasOwnProperty.call(message, "unread"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.unread);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified EventResponse message, length delimited. Does not implicitly {@link proto.EventResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.EventResponse
         * @static
         * @param {proto.IEventResponse} message EventResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EventResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an EventResponse message from the specified reader or buffer.
         * @function decode
         * @memberof proto.EventResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.EventResponse} EventResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EventResponse.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.eventResponseMessageKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.eventResponseMessageKey);
                        message._eventResponseMessageKey = "eventResponseMessageKey";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.timestampMs = reader.int64();
                        message._timestampMs = "timestampMs";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.eventResponseMessage = $root.proto.Message.EventResponseMessage.decode(reader, reader.uint32(), undefined, q + 1, message.eventResponseMessage);
                        message._eventResponseMessage = "eventResponseMessage";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.unread = reader.bool();
                        message._unread = "unread";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes an EventResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.EventResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.EventResponse} EventResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EventResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EventResponse message.
         * @function verify
         * @memberof proto.EventResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EventResponse.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.eventResponseMessageKey != null && Object.hasOwnProperty.call(message, "eventResponseMessageKey")) {
                properties._eventResponseMessageKey = 1;
                {
                    var error = $root.proto.MessageKey.verify(message.eventResponseMessageKey, q + 1);
                    if (error)
                        return "eventResponseMessageKey." + error;
                }
            }
            if (message.timestampMs != null && Object.hasOwnProperty.call(message, "timestampMs")) {
                properties._timestampMs = 1;
                if (!$util.isInteger(message.timestampMs) && !(message.timestampMs && $util.isInteger(message.timestampMs.low) && $util.isInteger(message.timestampMs.high)))
                    return "timestampMs: integer|Long expected";
            }
            if (message.eventResponseMessage != null && Object.hasOwnProperty.call(message, "eventResponseMessage")) {
                properties._eventResponseMessage = 1;
                {
                    var error = $root.proto.Message.EventResponseMessage.verify(message.eventResponseMessage, q + 1);
                    if (error)
                        return "eventResponseMessage." + error;
                }
            }
            if (message.unread != null && Object.hasOwnProperty.call(message, "unread")) {
                properties._unread = 1;
                if (typeof message.unread !== "boolean")
                    return "unread: boolean expected";
            }
            return null;
        };

        /**
         * Creates an EventResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.EventResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.EventResponse} EventResponse
         */
        EventResponse.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.EventResponse: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.eventResponseMessageKey != null) {
                if (!$util.isObject(object.eventResponseMessageKey))
                    throw TypeError(".proto.EventResponse.eventResponseMessageKey: object expected");
                message.eventResponseMessageKey = $root.proto.MessageKey.fromObject(object.eventResponseMessageKey, q + 1);
            }
            if (object.timestampMs != null)
                if ($util.Long)
                    message.timestampMs = $util.Long.fromValue(object.timestampMs, false);
                else if (typeof object.timestampMs === "string")
                    message.timestampMs = parseInt(object.timestampMs, 10);
                else if (typeof object.timestampMs === "number")
                    message.timestampMs = object.timestampMs;
                else if (typeof object.timestampMs === "object")
                    message.timestampMs = new $util.LongBits(object.timestampMs.low >>> 0, object.timestampMs.high >>> 0).toNumber();
            if (object.eventResponseMessage != null) {
                if (!$util.isObject(object.eventResponseMessage))
                    throw TypeError(".proto.EventResponse.eventResponseMessage: object expected");
                message.eventResponseMessage = $root.proto.Message.EventResponseMessage.fromObject(object.eventResponseMessage, q + 1);
            }
            if (object.unread != null)
                message.unread = Boolean(object.unread);
            return message;
        };

        /**
         * Creates a plain object from an EventResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.EventResponse
         * @static
         * @param {proto.EventResponse} message EventResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EventResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.eventResponseMessageKey != null && Object.hasOwnProperty.call(message, "eventResponseMessageKey"))
                object.eventResponseMessageKey = $root.proto.MessageKey.toObject(message.eventResponseMessageKey, options, q + 1);
            if (message.timestampMs != null && Object.hasOwnProperty.call(message, "timestampMs"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.timestampMs = typeof message.timestampMs === "number" ? BigInt(message.timestampMs) : $util.Long.fromBits(message.timestampMs.low >>> 0, message.timestampMs.high >>> 0, false).toBigInt();
                else if (typeof message.timestampMs === "number")
                    object.timestampMs = options.longs === String ? String(message.timestampMs) : message.timestampMs;
                else
                    object.timestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.timestampMs) : options.longs === Number ? new $util.LongBits(message.timestampMs.low >>> 0, message.timestampMs.high >>> 0).toNumber() : message.timestampMs;
            if (message.eventResponseMessage != null && Object.hasOwnProperty.call(message, "eventResponseMessage"))
                object.eventResponseMessage = $root.proto.Message.EventResponseMessage.toObject(message.eventResponseMessage, options, q + 1);
            if (message.unread != null && Object.hasOwnProperty.call(message, "unread"))
                object.unread = message.unread;
            return object;
        };

        /**
         * Converts this EventResponse to JSON.
         * @function toJSON
         * @memberof proto.EventResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EventResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EventResponse
         * @function getTypeUrl
         * @memberof proto.EventResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EventResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.EventResponse";
        };

        return EventResponse;
    })();

    proto.ExitCode = (function() {

        /**
         * Properties of an ExitCode.
         * @memberof proto
         * @interface IExitCode
         * @property {number|Long|null} [code] ExitCode code
         * @property {string|null} [text] ExitCode text
         */

        /**
         * Constructs a new ExitCode.
         * @memberof proto
         * @classdesc Represents an ExitCode.
         * @implements IExitCode
         * @constructor
         * @param {proto.IExitCode=} [properties] Properties to set
         */
        function ExitCode(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ExitCode code.
         * @member {number|Long|null|undefined} code
         * @memberof proto.ExitCode
         * @instance
         */
        ExitCode.prototype.code = null;

        /**
         * ExitCode text.
         * @member {string|null|undefined} text
         * @memberof proto.ExitCode
         * @instance
         */
        ExitCode.prototype.text = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExitCode.prototype, "_code", {
            get: $util.oneOfGetter($oneOfFields = ["code"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExitCode.prototype, "_text", {
            get: $util.oneOfGetter($oneOfFields = ["text"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ExitCode instance using the specified properties.
         * @function create
         * @memberof proto.ExitCode
         * @static
         * @param {proto.IExitCode=} [properties] Properties to set
         * @returns {proto.ExitCode} ExitCode instance
         */
        ExitCode.create = function create(properties) {
            return new ExitCode(properties);
        };

        /**
         * Encodes the specified ExitCode message. Does not implicitly {@link proto.ExitCode.verify|verify} messages.
         * @function encode
         * @memberof proto.ExitCode
         * @static
         * @param {proto.IExitCode} message ExitCode message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExitCode.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.code != null && Object.hasOwnProperty.call(message, "code"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.code);
            if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.text);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ExitCode message, length delimited. Does not implicitly {@link proto.ExitCode.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ExitCode
         * @static
         * @param {proto.IExitCode} message ExitCode message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExitCode.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ExitCode message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ExitCode
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ExitCode} ExitCode
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExitCode.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.code = reader.uint64();
                        message._code = "code";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.text = reader.stringVerify();
                        message._text = "text";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes an ExitCode message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ExitCode
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ExitCode} ExitCode
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExitCode.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ExitCode message.
         * @function verify
         * @memberof proto.ExitCode
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ExitCode.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.code != null && Object.hasOwnProperty.call(message, "code")) {
                properties._code = 1;
                if (!$util.isInteger(message.code) && !(message.code && $util.isInteger(message.code.low) && $util.isInteger(message.code.high)))
                    return "code: integer|Long expected";
            }
            if (message.text != null && Object.hasOwnProperty.call(message, "text")) {
                properties._text = 1;
                if (!$util.isString(message.text))
                    return "text: string expected";
            }
            return null;
        };

        /**
         * Creates an ExitCode message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ExitCode
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ExitCode} ExitCode
         */
        ExitCode.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.ExitCode: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.code != null)
                if ($util.Long)
                    message.code = $util.Long.fromValue(object.code, true);
                else if (typeof object.code === "string")
                    message.code = parseInt(object.code, 10);
                else if (typeof object.code === "number")
                    message.code = object.code;
                else if (typeof object.code === "object")
                    message.code = new $util.LongBits(object.code.low >>> 0, object.code.high >>> 0).toNumber(true);
            if (object.text != null)
                message.text = String(object.text);
            return message;
        };

        /**
         * Creates a plain object from an ExitCode message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ExitCode
         * @static
         * @param {proto.ExitCode} message ExitCode
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ExitCode.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.code != null && Object.hasOwnProperty.call(message, "code"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.code = typeof message.code === "number" ? BigInt(message.code) : $util.Long.fromBits(message.code.low >>> 0, message.code.high >>> 0, true).toBigInt();
                else if (typeof message.code === "number")
                    object.code = options.longs === String ? String(message.code) : message.code;
                else
                    object.code = options.longs === String ? $util.Long.prototype.toString.call(message.code) : options.longs === Number ? new $util.LongBits(message.code.low >>> 0, message.code.high >>> 0).toNumber(true) : message.code;
            if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                object.text = message.text;
            return object;
        };

        /**
         * Converts this ExitCode to JSON.
         * @function toJSON
         * @memberof proto.ExitCode
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ExitCode.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ExitCode
         * @function getTypeUrl
         * @memberof proto.ExitCode
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ExitCode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.ExitCode";
        };

        return ExitCode;
    })();

    proto.ExternalBlobReference = (function() {

        /**
         * Properties of an ExternalBlobReference.
         * @memberof proto
         * @interface IExternalBlobReference
         * @property {Uint8Array|null} [mediaKey] ExternalBlobReference mediaKey
         * @property {string|null} [directPath] ExternalBlobReference directPath
         * @property {string|null} [handle] ExternalBlobReference handle
         * @property {number|Long|null} [fileSizeBytes] ExternalBlobReference fileSizeBytes
         * @property {Uint8Array|null} [fileSha256] ExternalBlobReference fileSha256
         * @property {Uint8Array|null} [fileEncSha256] ExternalBlobReference fileEncSha256
         */

        /**
         * Constructs a new ExternalBlobReference.
         * @memberof proto
         * @classdesc Represents an ExternalBlobReference.
         * @implements IExternalBlobReference
         * @constructor
         * @param {proto.IExternalBlobReference=} [properties] Properties to set
         */
        function ExternalBlobReference(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ExternalBlobReference mediaKey.
         * @member {Uint8Array|null|undefined} mediaKey
         * @memberof proto.ExternalBlobReference
         * @instance
         */
        ExternalBlobReference.prototype.mediaKey = null;

        /**
         * ExternalBlobReference directPath.
         * @member {string|null|undefined} directPath
         * @memberof proto.ExternalBlobReference
         * @instance
         */
        ExternalBlobReference.prototype.directPath = null;

        /**
         * ExternalBlobReference handle.
         * @member {string|null|undefined} handle
         * @memberof proto.ExternalBlobReference
         * @instance
         */
        ExternalBlobReference.prototype.handle = null;

        /**
         * ExternalBlobReference fileSizeBytes.
         * @member {number|Long|null|undefined} fileSizeBytes
         * @memberof proto.ExternalBlobReference
         * @instance
         */
        ExternalBlobReference.prototype.fileSizeBytes = null;

        /**
         * ExternalBlobReference fileSha256.
         * @member {Uint8Array|null|undefined} fileSha256
         * @memberof proto.ExternalBlobReference
         * @instance
         */
        ExternalBlobReference.prototype.fileSha256 = null;

        /**
         * ExternalBlobReference fileEncSha256.
         * @member {Uint8Array|null|undefined} fileEncSha256
         * @memberof proto.ExternalBlobReference
         * @instance
         */
        ExternalBlobReference.prototype.fileEncSha256 = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExternalBlobReference.prototype, "_mediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExternalBlobReference.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExternalBlobReference.prototype, "_handle", {
            get: $util.oneOfGetter($oneOfFields = ["handle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExternalBlobReference.prototype, "_fileSizeBytes", {
            get: $util.oneOfGetter($oneOfFields = ["fileSizeBytes"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExternalBlobReference.prototype, "_fileSha256", {
            get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExternalBlobReference.prototype, "_fileEncSha256", {
            get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ExternalBlobReference instance using the specified properties.
         * @function create
         * @memberof proto.ExternalBlobReference
         * @static
         * @param {proto.IExternalBlobReference=} [properties] Properties to set
         * @returns {proto.ExternalBlobReference} ExternalBlobReference instance
         */
        ExternalBlobReference.create = function create(properties) {
            return new ExternalBlobReference(properties);
        };

        /**
         * Encodes the specified ExternalBlobReference message. Does not implicitly {@link proto.ExternalBlobReference.verify|verify} messages.
         * @function encode
         * @memberof proto.ExternalBlobReference
         * @static
         * @param {proto.IExternalBlobReference} message ExternalBlobReference message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExternalBlobReference.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.mediaKey);
            if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.directPath);
            if (message.handle != null && Object.hasOwnProperty.call(message, "handle"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.handle);
            if (message.fileSizeBytes != null && Object.hasOwnProperty.call(message, "fileSizeBytes"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.fileSizeBytes);
            if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.fileSha256);
            if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.fileEncSha256);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ExternalBlobReference message, length delimited. Does not implicitly {@link proto.ExternalBlobReference.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ExternalBlobReference
         * @static
         * @param {proto.IExternalBlobReference} message ExternalBlobReference message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExternalBlobReference.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ExternalBlobReference message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ExternalBlobReference
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ExternalBlobReference} ExternalBlobReference
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExternalBlobReference.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.mediaKey = reader.bytes();
                        message._mediaKey = "mediaKey";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.directPath = reader.stringVerify();
                        message._directPath = "directPath";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.handle = reader.stringVerify();
                        message._handle = "handle";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.fileSizeBytes = reader.uint64();
                        message._fileSizeBytes = "fileSizeBytes";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.fileSha256 = reader.bytes();
                        message._fileSha256 = "fileSha256";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        message.fileEncSha256 = reader.bytes();
                        message._fileEncSha256 = "fileEncSha256";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes an ExternalBlobReference message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ExternalBlobReference
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ExternalBlobReference} ExternalBlobReference
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExternalBlobReference.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ExternalBlobReference message.
         * @function verify
         * @memberof proto.ExternalBlobReference
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ExternalBlobReference.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey")) {
                properties._mediaKey = 1;
                if (!(message.mediaKey && typeof message.mediaKey.length === "number" || $util.isString(message.mediaKey)))
                    return "mediaKey: buffer expected";
            }
            if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath")) {
                properties._directPath = 1;
                if (!$util.isString(message.directPath))
                    return "directPath: string expected";
            }
            if (message.handle != null && Object.hasOwnProperty.call(message, "handle")) {
                properties._handle = 1;
                if (!$util.isString(message.handle))
                    return "handle: string expected";
            }
            if (message.fileSizeBytes != null && Object.hasOwnProperty.call(message, "fileSizeBytes")) {
                properties._fileSizeBytes = 1;
                if (!$util.isInteger(message.fileSizeBytes) && !(message.fileSizeBytes && $util.isInteger(message.fileSizeBytes.low) && $util.isInteger(message.fileSizeBytes.high)))
                    return "fileSizeBytes: integer|Long expected";
            }
            if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256")) {
                properties._fileSha256 = 1;
                if (!(message.fileSha256 && typeof message.fileSha256.length === "number" || $util.isString(message.fileSha256)))
                    return "fileSha256: buffer expected";
            }
            if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256")) {
                properties._fileEncSha256 = 1;
                if (!(message.fileEncSha256 && typeof message.fileEncSha256.length === "number" || $util.isString(message.fileEncSha256)))
                    return "fileEncSha256: buffer expected";
            }
            return null;
        };

        /**
         * Creates an ExternalBlobReference message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ExternalBlobReference
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ExternalBlobReference} ExternalBlobReference
         */
        ExternalBlobReference.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.ExternalBlobReference: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.mediaKey != null)
                if (typeof object.mediaKey === "string")
                    $util.base64.decode(object.mediaKey, message.mediaKey = $util.newBuffer($util.base64.length(object.mediaKey)), 0);
                else if (object.mediaKey.length >= 0)
                    message.mediaKey = object.mediaKey;
            if (object.directPath != null)
                message.directPath = String(object.directPath);
            if (object.handle != null)
                message.handle = String(object.handle);
            if (object.fileSizeBytes != null)
                if ($util.Long)
                    message.fileSizeBytes = $util.Long.fromValue(object.fileSizeBytes, true);
                else if (typeof object.fileSizeBytes === "string")
                    message.fileSizeBytes = parseInt(object.fileSizeBytes, 10);
                else if (typeof object.fileSizeBytes === "number")
                    message.fileSizeBytes = object.fileSizeBytes;
                else if (typeof object.fileSizeBytes === "object")
                    message.fileSizeBytes = new $util.LongBits(object.fileSizeBytes.low >>> 0, object.fileSizeBytes.high >>> 0).toNumber(true);
            if (object.fileSha256 != null)
                if (typeof object.fileSha256 === "string")
                    $util.base64.decode(object.fileSha256, message.fileSha256 = $util.newBuffer($util.base64.length(object.fileSha256)), 0);
                else if (object.fileSha256.length >= 0)
                    message.fileSha256 = object.fileSha256;
            if (object.fileEncSha256 != null)
                if (typeof object.fileEncSha256 === "string")
                    $util.base64.decode(object.fileEncSha256, message.fileEncSha256 = $util.newBuffer($util.base64.length(object.fileEncSha256)), 0);
                else if (object.fileEncSha256.length >= 0)
                    message.fileEncSha256 = object.fileEncSha256;
            return message;
        };

        /**
         * Creates a plain object from an ExternalBlobReference message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ExternalBlobReference
         * @static
         * @param {proto.ExternalBlobReference} message ExternalBlobReference
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ExternalBlobReference.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                object.mediaKey = options.bytes === String ? $util.base64.encode(message.mediaKey, 0, message.mediaKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.mediaKey) : message.mediaKey;
            if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                object.directPath = message.directPath;
            if (message.handle != null && Object.hasOwnProperty.call(message, "handle"))
                object.handle = message.handle;
            if (message.fileSizeBytes != null && Object.hasOwnProperty.call(message, "fileSizeBytes"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.fileSizeBytes = typeof message.fileSizeBytes === "number" ? BigInt(message.fileSizeBytes) : $util.Long.fromBits(message.fileSizeBytes.low >>> 0, message.fileSizeBytes.high >>> 0, true).toBigInt();
                else if (typeof message.fileSizeBytes === "number")
                    object.fileSizeBytes = options.longs === String ? String(message.fileSizeBytes) : message.fileSizeBytes;
                else
                    object.fileSizeBytes = options.longs === String ? $util.Long.prototype.toString.call(message.fileSizeBytes) : options.longs === Number ? new $util.LongBits(message.fileSizeBytes.low >>> 0, message.fileSizeBytes.high >>> 0).toNumber(true) : message.fileSizeBytes;
            if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                object.fileSha256 = options.bytes === String ? $util.base64.encode(message.fileSha256, 0, message.fileSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileSha256) : message.fileSha256;
            if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                object.fileEncSha256 = options.bytes === String ? $util.base64.encode(message.fileEncSha256, 0, message.fileEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileEncSha256) : message.fileEncSha256;
            return object;
        };

        /**
         * Converts this ExternalBlobReference to JSON.
         * @function toJSON
         * @memberof proto.ExternalBlobReference
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ExternalBlobReference.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ExternalBlobReference
         * @function getTypeUrl
         * @memberof proto.ExternalBlobReference
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ExternalBlobReference.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.ExternalBlobReference";
        };

        return ExternalBlobReference;
    })();

    proto.Field = (function() {

        /**
         * Properties of a Field.
         * @memberof proto
         * @interface IField
         * @property {number|null} [minVersion] Field minVersion
         * @property {number|null} [maxVersion] Field maxVersion
         * @property {number|null} [notReportableMinVersion] Field notReportableMinVersion
         * @property {boolean|null} [isMessage] Field isMessage
         * @property {Object.<string,proto.IField>|null} [subfield] Field subfield
         */

        /**
         * Constructs a new Field.
         * @memberof proto
         * @classdesc Represents a Field.
         * @implements IField
         * @constructor
         * @param {proto.IField=} [properties] Properties to set
         */
        function Field(properties) {
            this.subfield = {};
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Field minVersion.
         * @member {number|null|undefined} minVersion
         * @memberof proto.Field
         * @instance
         */
        Field.prototype.minVersion = null;

        /**
         * Field maxVersion.
         * @member {number|null|undefined} maxVersion
         * @memberof proto.Field
         * @instance
         */
        Field.prototype.maxVersion = null;

        /**
         * Field notReportableMinVersion.
         * @member {number|null|undefined} notReportableMinVersion
         * @memberof proto.Field
         * @instance
         */
        Field.prototype.notReportableMinVersion = null;

        /**
         * Field isMessage.
         * @member {boolean|null|undefined} isMessage
         * @memberof proto.Field
         * @instance
         */
        Field.prototype.isMessage = null;

        /**
         * Field subfield.
         * @member {Object.<string,proto.IField>} subfield
         * @memberof proto.Field
         * @instance
         */
        Field.prototype.subfield = $util.emptyObject;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Field.prototype, "_minVersion", {
            get: $util.oneOfGetter($oneOfFields = ["minVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Field.prototype, "_maxVersion", {
            get: $util.oneOfGetter($oneOfFields = ["maxVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Field.prototype, "_notReportableMinVersion", {
            get: $util.oneOfGetter($oneOfFields = ["notReportableMinVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Field.prototype, "_isMessage", {
            get: $util.oneOfGetter($oneOfFields = ["isMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new Field instance using the specified properties.
         * @function create
         * @memberof proto.Field
         * @static
         * @param {proto.IField=} [properties] Properties to set
         * @returns {proto.Field} Field instance
         */
        Field.create = function create(properties) {
            return new Field(properties);
        };

        /**
         * Encodes the specified Field message. Does not implicitly {@link proto.Field.verify|verify} messages.
         * @function encode
         * @memberof proto.Field
         * @static
         * @param {proto.IField} message Field message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Field.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.minVersion != null && Object.hasOwnProperty.call(message, "minVersion"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.minVersion);
            if (message.maxVersion != null && Object.hasOwnProperty.call(message, "maxVersion"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.maxVersion);
            if (message.notReportableMinVersion != null && Object.hasOwnProperty.call(message, "notReportableMinVersion"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.notReportableMinVersion);
            if (message.isMessage != null && Object.hasOwnProperty.call(message, "isMessage"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.isMessage);
            if (message.subfield != null && Object.hasOwnProperty.call(message, "subfield"))
                for (var keys = Object.keys(message.subfield), i = 0; i < keys.length; ++i) {
                    writer.uint32(/* id 5, wireType 2 =*/42).fork().uint32(/* id 1, wireType 0 =*/8).uint32(keys[i]);
                    $root.proto.Field.encode(message.subfield[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim().ldelim();
                }
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Field message, length delimited. Does not implicitly {@link proto.Field.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Field
         * @static
         * @param {proto.IField} message Field message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Field.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Field message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Field
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Field} Field
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Field.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), key, v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.minVersion = reader.uint32();
                        message._minVersion = "minVersion";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.maxVersion = reader.uint32();
                        message._maxVersion = "maxVersion";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.notReportableMinVersion = reader.uint32();
                        message._notReportableMinVersion = "notReportableMinVersion";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.isMessage = reader.bool();
                        message._isMessage = "isMessage";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        if (message.subfield === $util.emptyObject)
                            message.subfield = {};
                        var end2 = reader.uint32() + reader.pos;
                        key = 0;
                        v = null;
                        while (reader.pos < end2) {
                            var t2 = reader.tag();
                            u = t2 & 7;
                            switch (t2 >>>= 3) {
                            case 1:
                                if (u !== 0)
                                    break;
                                key = reader.uint32();
                                continue;
                            case 2:
                                if (u !== 2)
                                    break;
                                v = $root.proto.Field.decode(reader, reader.uint32(), undefined, q + 1);
                                continue;
                            }
                            reader.skipType(u, q, t2);
                        }
                        message.subfield[key] = v || new $root.proto.Field.ctor();
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a Field message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Field
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Field} Field
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Field.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Field message.
         * @function verify
         * @memberof proto.Field
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Field.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.minVersion != null && Object.hasOwnProperty.call(message, "minVersion")) {
                properties._minVersion = 1;
                if (!$util.isInteger(message.minVersion))
                    return "minVersion: integer expected";
            }
            if (message.maxVersion != null && Object.hasOwnProperty.call(message, "maxVersion")) {
                properties._maxVersion = 1;
                if (!$util.isInteger(message.maxVersion))
                    return "maxVersion: integer expected";
            }
            if (message.notReportableMinVersion != null && Object.hasOwnProperty.call(message, "notReportableMinVersion")) {
                properties._notReportableMinVersion = 1;
                if (!$util.isInteger(message.notReportableMinVersion))
                    return "notReportableMinVersion: integer expected";
            }
            if (message.isMessage != null && Object.hasOwnProperty.call(message, "isMessage")) {
                properties._isMessage = 1;
                if (typeof message.isMessage !== "boolean")
                    return "isMessage: boolean expected";
            }
            if (message.subfield != null && Object.hasOwnProperty.call(message, "subfield")) {
                if (!$util.isObject(message.subfield))
                    return "subfield: object expected";
                var key = Object.keys(message.subfield);
                for (var i = 0; i < key.length; ++i) {
                    if (!$util.key32Re.test(key[i]))
                        return "subfield: integer key{k:uint32} expected";
                    {
                        var error = $root.proto.Field.verify(message.subfield[key[i]], q + 1);
                        if (error)
                            return "subfield." + error;
                    }
                }
            }
            return null;
        };

        /**
         * Creates a Field message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Field
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Field} Field
         */
        Field.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.Field: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.minVersion != null)
                message.minVersion = object.minVersion >>> 0;
            if (object.maxVersion != null)
                message.maxVersion = object.maxVersion >>> 0;
            if (object.notReportableMinVersion != null)
                message.notReportableMinVersion = object.notReportableMinVersion >>> 0;
            if (object.isMessage != null)
                message.isMessage = Boolean(object.isMessage);
            if (object.subfield) {
                if (!$util.isObject(object.subfield))
                    throw TypeError(".proto.Field.subfield: object expected");
                message.subfield = {};
                for (var keys = Object.keys(object.subfield), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.subfield, keys[i]);
                    if (!$util.isObject(object.subfield[keys[i]]))
                        throw TypeError(".proto.Field.subfield: object expected");
                    message.subfield[keys[i]] = $root.proto.Field.fromObject(object.subfield[keys[i]], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Field message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Field
         * @static
         * @param {proto.Field} message Field
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Field.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.objects || options.defaults)
                object.subfield = {};
            if (message.minVersion != null && Object.hasOwnProperty.call(message, "minVersion"))
                object.minVersion = message.minVersion;
            if (message.maxVersion != null && Object.hasOwnProperty.call(message, "maxVersion"))
                object.maxVersion = message.maxVersion;
            if (message.notReportableMinVersion != null && Object.hasOwnProperty.call(message, "notReportableMinVersion"))
                object.notReportableMinVersion = message.notReportableMinVersion;
            if (message.isMessage != null && Object.hasOwnProperty.call(message, "isMessage"))
                object.isMessage = message.isMessage;
            var keys2;
            if (message.subfield && (keys2 = Object.keys(message.subfield)).length) {
                object.subfield = {};
                for (var j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.subfield, keys2[j]);
                    object.subfield[keys2[j]] = $root.proto.Field.toObject(message.subfield[keys2[j]], options, q + 1);
                }
            }
            return object;
        };

        /**
         * Converts this Field to JSON.
         * @function toJSON
         * @memberof proto.Field
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Field.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Field
         * @function getTypeUrl
         * @memberof proto.Field
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Field.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.Field";
        };

        return Field;
    })();

    proto.ForwardedAIBotMessageInfo = (function() {

        /**
         * Properties of a ForwardedAIBotMessageInfo.
         * @memberof proto
         * @interface IForwardedAIBotMessageInfo
         * @property {string|null} [botName] ForwardedAIBotMessageInfo botName
         * @property {string|null} [botJid] ForwardedAIBotMessageInfo botJid
         * @property {string|null} [creatorName] ForwardedAIBotMessageInfo creatorName
         */

        /**
         * Constructs a new ForwardedAIBotMessageInfo.
         * @memberof proto
         * @classdesc Represents a ForwardedAIBotMessageInfo.
         * @implements IForwardedAIBotMessageInfo
         * @constructor
         * @param {proto.IForwardedAIBotMessageInfo=} [properties] Properties to set
         */
        function ForwardedAIBotMessageInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ForwardedAIBotMessageInfo botName.
         * @member {string|null|undefined} botName
         * @memberof proto.ForwardedAIBotMessageInfo
         * @instance
         */
        ForwardedAIBotMessageInfo.prototype.botName = null;

        /**
         * ForwardedAIBotMessageInfo botJid.
         * @member {string|null|undefined} botJid
         * @memberof proto.ForwardedAIBotMessageInfo
         * @instance
         */
        ForwardedAIBotMessageInfo.prototype.botJid = null;

        /**
         * ForwardedAIBotMessageInfo creatorName.
         * @member {string|null|undefined} creatorName
         * @memberof proto.ForwardedAIBotMessageInfo
         * @instance
         */
        ForwardedAIBotMessageInfo.prototype.creatorName = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ForwardedAIBotMessageInfo.prototype, "_botName", {
            get: $util.oneOfGetter($oneOfFields = ["botName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ForwardedAIBotMessageInfo.prototype, "_botJid", {
            get: $util.oneOfGetter($oneOfFields = ["botJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ForwardedAIBotMessageInfo.prototype, "_creatorName", {
            get: $util.oneOfGetter($oneOfFields = ["creatorName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ForwardedAIBotMessageInfo instance using the specified properties.
         * @function create
         * @memberof proto.ForwardedAIBotMessageInfo
         * @static
         * @param {proto.IForwardedAIBotMessageInfo=} [properties] Properties to set
         * @returns {proto.ForwardedAIBotMessageInfo} ForwardedAIBotMessageInfo instance
         */
        ForwardedAIBotMessageInfo.create = function create(properties) {
            return new ForwardedAIBotMessageInfo(properties);
        };

        /**
         * Encodes the specified ForwardedAIBotMessageInfo message. Does not implicitly {@link proto.ForwardedAIBotMessageInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.ForwardedAIBotMessageInfo
         * @static
         * @param {proto.IForwardedAIBotMessageInfo} message ForwardedAIBotMessageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ForwardedAIBotMessageInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.botName != null && Object.hasOwnProperty.call(message, "botName"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.botName);
            if (message.botJid != null && Object.hasOwnProperty.call(message, "botJid"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.botJid);
            if (message.creatorName != null && Object.hasOwnProperty.call(message, "creatorName"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.creatorName);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ForwardedAIBotMessageInfo message, length delimited. Does not implicitly {@link proto.ForwardedAIBotMessageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ForwardedAIBotMessageInfo
         * @static
         * @param {proto.IForwardedAIBotMessageInfo} message ForwardedAIBotMessageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ForwardedAIBotMessageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ForwardedAIBotMessageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ForwardedAIBotMessageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ForwardedAIBotMessageInfo} ForwardedAIBotMessageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ForwardedAIBotMessageInfo.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.botName = reader.stringVerify();
                        message._botName = "botName";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.botJid = reader.stringVerify();
                        message._botJid = "botJid";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.creatorName = reader.stringVerify();
                        message._creatorName = "creatorName";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a ForwardedAIBotMessageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ForwardedAIBotMessageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ForwardedAIBotMessageInfo} ForwardedAIBotMessageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ForwardedAIBotMessageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ForwardedAIBotMessageInfo message.
         * @function verify
         * @memberof proto.ForwardedAIBotMessageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ForwardedAIBotMessageInfo.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.botName != null && Object.hasOwnProperty.call(message, "botName")) {
                properties._botName = 1;
                if (!$util.isString(message.botName))
                    return "botName: string expected";
            }
            if (message.botJid != null && Object.hasOwnProperty.call(message, "botJid")) {
                properties._botJid = 1;
                if (!$util.isString(message.botJid))
                    return "botJid: string expected";
            }
            if (message.creatorName != null && Object.hasOwnProperty.call(message, "creatorName")) {
                properties._creatorName = 1;
                if (!$util.isString(message.creatorName))
                    return "creatorName: string expected";
            }
            return null;
        };

        /**
         * Creates a ForwardedAIBotMessageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ForwardedAIBotMessageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ForwardedAIBotMessageInfo} ForwardedAIBotMessageInfo
         */
        ForwardedAIBotMessageInfo.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.ForwardedAIBotMessageInfo: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.botName != null)
                message.botName = String(object.botName);
            if (object.botJid != null)
                message.botJid = String(object.botJid);
            if (object.creatorName != null)
                message.creatorName = String(object.creatorName);
            return message;
        };

        /**
         * Creates a plain object from a ForwardedAIBotMessageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ForwardedAIBotMessageInfo
         * @static
         * @param {proto.ForwardedAIBotMessageInfo} message ForwardedAIBotMessageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ForwardedAIBotMessageInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.botName != null && Object.hasOwnProperty.call(message, "botName"))
                object.botName = message.botName;
            if (message.botJid != null && Object.hasOwnProperty.call(message, "botJid"))
                object.botJid = message.botJid;
            if (message.creatorName != null && Object.hasOwnProperty.call(message, "creatorName"))
                object.creatorName = message.creatorName;
            return object;
        };

        /**
         * Converts this ForwardedAIBotMessageInfo to JSON.
         * @function toJSON
         * @memberof proto.ForwardedAIBotMessageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ForwardedAIBotMessageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ForwardedAIBotMessageInfo
         * @function getTypeUrl
         * @memberof proto.ForwardedAIBotMessageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ForwardedAIBotMessageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.ForwardedAIBotMessageInfo";
        };

        return ForwardedAIBotMessageInfo;
    })();

    proto.GlobalSettings = (function() {

        /**
         * Properties of a GlobalSettings.
         * @memberof proto
         * @interface IGlobalSettings
         * @property {proto.IWallpaperSettings|null} [lightThemeWallpaper] GlobalSettings lightThemeWallpaper
         * @property {proto.MediaVisibility|null} [mediaVisibility] GlobalSettings mediaVisibility
         * @property {proto.IWallpaperSettings|null} [darkThemeWallpaper] GlobalSettings darkThemeWallpaper
         * @property {proto.IAutoDownloadSettings|null} [autoDownloadWiFi] GlobalSettings autoDownloadWiFi
         * @property {proto.IAutoDownloadSettings|null} [autoDownloadCellular] GlobalSettings autoDownloadCellular
         * @property {proto.IAutoDownloadSettings|null} [autoDownloadRoaming] GlobalSettings autoDownloadRoaming
         * @property {boolean|null} [showIndividualNotificationsPreview] GlobalSettings showIndividualNotificationsPreview
         * @property {boolean|null} [showGroupNotificationsPreview] GlobalSettings showGroupNotificationsPreview
         * @property {number|null} [disappearingModeDuration] GlobalSettings disappearingModeDuration
         * @property {number|Long|null} [disappearingModeTimestamp] GlobalSettings disappearingModeTimestamp
         * @property {proto.IAvatarUserSettings|null} [avatarUserSettings] GlobalSettings avatarUserSettings
         * @property {number|null} [fontSize] GlobalSettings fontSize
         * @property {boolean|null} [securityNotifications] GlobalSettings securityNotifications
         * @property {boolean|null} [autoUnarchiveChats] GlobalSettings autoUnarchiveChats
         * @property {number|null} [videoQualityMode] GlobalSettings videoQualityMode
         * @property {number|null} [photoQualityMode] GlobalSettings photoQualityMode
         * @property {proto.INotificationSettings|null} [individualNotificationSettings] GlobalSettings individualNotificationSettings
         * @property {proto.INotificationSettings|null} [groupNotificationSettings] GlobalSettings groupNotificationSettings
         * @property {proto.IChatLockSettings|null} [chatLockSettings] GlobalSettings chatLockSettings
         * @property {number|Long|null} [chatDbLidMigrationTimestamp] GlobalSettings chatDbLidMigrationTimestamp
         */

        /**
         * Constructs a new GlobalSettings.
         * @memberof proto
         * @classdesc Represents a GlobalSettings.
         * @implements IGlobalSettings
         * @constructor
         * @param {proto.IGlobalSettings=} [properties] Properties to set
         */
        function GlobalSettings(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GlobalSettings lightThemeWallpaper.
         * @member {proto.IWallpaperSettings|null|undefined} lightThemeWallpaper
         * @memberof proto.GlobalSettings
         * @instance
         */
        GlobalSettings.prototype.lightThemeWallpaper = null;

        /**
         * GlobalSettings mediaVisibility.
         * @member {proto.MediaVisibility|null|undefined} mediaVisibility
         * @memberof proto.GlobalSettings
         * @instance
         */
        GlobalSettings.prototype.mediaVisibility = null;

        /**
         * GlobalSettings darkThemeWallpaper.
         * @member {proto.IWallpaperSettings|null|undefined} darkThemeWallpaper
         * @memberof proto.GlobalSettings
         * @instance
         */
        GlobalSettings.prototype.darkThemeWallpaper = null;

        /**
         * GlobalSettings autoDownloadWiFi.
         * @member {proto.IAutoDownloadSettings|null|undefined} autoDownloadWiFi
         * @memberof proto.GlobalSettings
         * @instance
         */
        GlobalSettings.prototype.autoDownloadWiFi = null;

        /**
         * GlobalSettings autoDownloadCellular.
         * @member {proto.IAutoDownloadSettings|null|undefined} autoDownloadCellular
         * @memberof proto.GlobalSettings
         * @instance
         */
        GlobalSettings.prototype.autoDownloadCellular = null;

        /**
         * GlobalSettings autoDownloadRoaming.
         * @member {proto.IAutoDownloadSettings|null|undefined} autoDownloadRoaming
         * @memberof proto.GlobalSettings
         * @instance
         */
        GlobalSettings.prototype.autoDownloadRoaming = null;

        /**
         * GlobalSettings showIndividualNotificationsPreview.
         * @member {boolean|null|undefined} showIndividualNotificationsPreview
         * @memberof proto.GlobalSettings
         * @instance
         */
        GlobalSettings.prototype.showIndividualNotificationsPreview = null;

        /**
         * GlobalSettings showGroupNotificationsPreview.
         * @member {boolean|null|undefined} showGroupNotificationsPreview
         * @memberof proto.GlobalSettings
         * @instance
         */
        GlobalSettings.prototype.showGroupNotificationsPreview = null;

        /**
         * GlobalSettings disappearingModeDuration.
         * @member {number|null|undefined} disappearingModeDuration
         * @memberof proto.GlobalSettings
         * @instance
         */
        GlobalSettings.prototype.disappearingModeDuration = null;

        /**
         * GlobalSettings disappearingModeTimestamp.
         * @member {number|Long|null|undefined} disappearingModeTimestamp
         * @memberof proto.GlobalSettings
         * @instance
         */
        GlobalSettings.prototype.disappearingModeTimestamp = null;

        /**
         * GlobalSettings avatarUserSettings.
         * @member {proto.IAvatarUserSettings|null|undefined} avatarUserSettings
         * @memberof proto.GlobalSettings
         * @instance
         */
        GlobalSettings.prototype.avatarUserSettings = null;

        /**
         * GlobalSettings fontSize.
         * @member {number|null|undefined} fontSize
         * @memberof proto.GlobalSettings
         * @instance
         */
        GlobalSettings.prototype.fontSize = null;

        /**
         * GlobalSettings securityNotifications.
         * @member {boolean|null|undefined} securityNotifications
         * @memberof proto.GlobalSettings
         * @instance
         */
        GlobalSettings.prototype.securityNotifications = null;

        /**
         * GlobalSettings autoUnarchiveChats.
         * @member {boolean|null|undefined} autoUnarchiveChats
         * @memberof proto.GlobalSettings
         * @instance
         */
        GlobalSettings.prototype.autoUnarchiveChats = null;

        /**
         * GlobalSettings videoQualityMode.
         * @member {number|null|undefined} videoQualityMode
         * @memberof proto.GlobalSettings
         * @instance
         */
        GlobalSettings.prototype.videoQualityMode = null;

        /**
         * GlobalSettings photoQualityMode.
         * @member {number|null|undefined} photoQualityMode
         * @memberof proto.GlobalSettings
         * @instance
         */
        GlobalSettings.prototype.photoQualityMode = null;

        /**
         * GlobalSettings individualNotificationSettings.
         * @member {proto.INotificationSettings|null|undefined} individualNotificationSettings
         * @memberof proto.GlobalSettings
         * @instance
         */
        GlobalSettings.prototype.individualNotificationSettings = null;

        /**
         * GlobalSettings groupNotificationSettings.
         * @member {proto.INotificationSettings|null|undefined} groupNotificationSettings
         * @memberof proto.GlobalSettings
         * @instance
         */
        GlobalSettings.prototype.groupNotificationSettings = null;

        /**
         * GlobalSettings chatLockSettings.
         * @member {proto.IChatLockSettings|null|undefined} chatLockSettings
         * @memberof proto.GlobalSettings
         * @instance
         */
        GlobalSettings.prototype.chatLockSettings = null;

        /**
         * GlobalSettings chatDbLidMigrationTimestamp.
         * @member {number|Long|null|undefined} chatDbLidMigrationTimestamp
         * @memberof proto.GlobalSettings
         * @instance
         */
        GlobalSettings.prototype.chatDbLidMigrationTimestamp = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GlobalSettings.prototype, "_lightThemeWallpaper", {
            get: $util.oneOfGetter($oneOfFields = ["lightThemeWallpaper"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GlobalSettings.prototype, "_mediaVisibility", {
            get: $util.oneOfGetter($oneOfFields = ["mediaVisibility"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GlobalSettings.prototype, "_darkThemeWallpaper", {
            get: $util.oneOfGetter($oneOfFields = ["darkThemeWallpaper"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GlobalSettings.prototype, "_autoDownloadWiFi", {
            get: $util.oneOfGetter($oneOfFields = ["autoDownloadWiFi"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GlobalSettings.prototype, "_autoDownloadCellular", {
            get: $util.oneOfGetter($oneOfFields = ["autoDownloadCellular"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GlobalSettings.prototype, "_autoDownloadRoaming", {
            get: $util.oneOfGetter($oneOfFields = ["autoDownloadRoaming"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GlobalSettings.prototype, "_showIndividualNotificationsPreview", {
            get: $util.oneOfGetter($oneOfFields = ["showIndividualNotificationsPreview"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GlobalSettings.prototype, "_showGroupNotificationsPreview", {
            get: $util.oneOfGetter($oneOfFields = ["showGroupNotificationsPreview"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GlobalSettings.prototype, "_disappearingModeDuration", {
            get: $util.oneOfGetter($oneOfFields = ["disappearingModeDuration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GlobalSettings.prototype, "_disappearingModeTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["disappearingModeTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GlobalSettings.prototype, "_avatarUserSettings", {
            get: $util.oneOfGetter($oneOfFields = ["avatarUserSettings"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GlobalSettings.prototype, "_fontSize", {
            get: $util.oneOfGetter($oneOfFields = ["fontSize"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GlobalSettings.prototype, "_securityNotifications", {
            get: $util.oneOfGetter($oneOfFields = ["securityNotifications"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GlobalSettings.prototype, "_autoUnarchiveChats", {
            get: $util.oneOfGetter($oneOfFields = ["autoUnarchiveChats"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GlobalSettings.prototype, "_videoQualityMode", {
            get: $util.oneOfGetter($oneOfFields = ["videoQualityMode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GlobalSettings.prototype, "_photoQualityMode", {
            get: $util.oneOfGetter($oneOfFields = ["photoQualityMode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GlobalSettings.prototype, "_individualNotificationSettings", {
            get: $util.oneOfGetter($oneOfFields = ["individualNotificationSettings"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GlobalSettings.prototype, "_groupNotificationSettings", {
            get: $util.oneOfGetter($oneOfFields = ["groupNotificationSettings"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GlobalSettings.prototype, "_chatLockSettings", {
            get: $util.oneOfGetter($oneOfFields = ["chatLockSettings"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GlobalSettings.prototype, "_chatDbLidMigrationTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["chatDbLidMigrationTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new GlobalSettings instance using the specified properties.
         * @function create
         * @memberof proto.GlobalSettings
         * @static
         * @param {proto.IGlobalSettings=} [properties] Properties to set
         * @returns {proto.GlobalSettings} GlobalSettings instance
         */
        GlobalSettings.create = function create(properties) {
            return new GlobalSettings(properties);
        };

        /**
         * Encodes the specified GlobalSettings message. Does not implicitly {@link proto.GlobalSettings.verify|verify} messages.
         * @function encode
         * @memberof proto.GlobalSettings
         * @static
         * @param {proto.IGlobalSettings} message GlobalSettings message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GlobalSettings.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.lightThemeWallpaper != null && Object.hasOwnProperty.call(message, "lightThemeWallpaper"))
                $root.proto.WallpaperSettings.encode(message.lightThemeWallpaper, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.mediaVisibility != null && Object.hasOwnProperty.call(message, "mediaVisibility"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.mediaVisibility);
            if (message.darkThemeWallpaper != null && Object.hasOwnProperty.call(message, "darkThemeWallpaper"))
                $root.proto.WallpaperSettings.encode(message.darkThemeWallpaper, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.autoDownloadWiFi != null && Object.hasOwnProperty.call(message, "autoDownloadWiFi"))
                $root.proto.AutoDownloadSettings.encode(message.autoDownloadWiFi, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.autoDownloadCellular != null && Object.hasOwnProperty.call(message, "autoDownloadCellular"))
                $root.proto.AutoDownloadSettings.encode(message.autoDownloadCellular, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
            if (message.autoDownloadRoaming != null && Object.hasOwnProperty.call(message, "autoDownloadRoaming"))
                $root.proto.AutoDownloadSettings.encode(message.autoDownloadRoaming, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
            if (message.showIndividualNotificationsPreview != null && Object.hasOwnProperty.call(message, "showIndividualNotificationsPreview"))
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.showIndividualNotificationsPreview);
            if (message.showGroupNotificationsPreview != null && Object.hasOwnProperty.call(message, "showGroupNotificationsPreview"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.showGroupNotificationsPreview);
            if (message.disappearingModeDuration != null && Object.hasOwnProperty.call(message, "disappearingModeDuration"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.disappearingModeDuration);
            if (message.disappearingModeTimestamp != null && Object.hasOwnProperty.call(message, "disappearingModeTimestamp"))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.disappearingModeTimestamp);
            if (message.avatarUserSettings != null && Object.hasOwnProperty.call(message, "avatarUserSettings"))
                $root.proto.AvatarUserSettings.encode(message.avatarUserSettings, writer.uint32(/* id 11, wireType 2 =*/90).fork(), q + 1).ldelim();
            if (message.fontSize != null && Object.hasOwnProperty.call(message, "fontSize"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.fontSize);
            if (message.securityNotifications != null && Object.hasOwnProperty.call(message, "securityNotifications"))
                writer.uint32(/* id 13, wireType 0 =*/104).bool(message.securityNotifications);
            if (message.autoUnarchiveChats != null && Object.hasOwnProperty.call(message, "autoUnarchiveChats"))
                writer.uint32(/* id 14, wireType 0 =*/112).bool(message.autoUnarchiveChats);
            if (message.videoQualityMode != null && Object.hasOwnProperty.call(message, "videoQualityMode"))
                writer.uint32(/* id 15, wireType 0 =*/120).int32(message.videoQualityMode);
            if (message.photoQualityMode != null && Object.hasOwnProperty.call(message, "photoQualityMode"))
                writer.uint32(/* id 16, wireType 0 =*/128).int32(message.photoQualityMode);
            if (message.individualNotificationSettings != null && Object.hasOwnProperty.call(message, "individualNotificationSettings"))
                $root.proto.NotificationSettings.encode(message.individualNotificationSettings, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
            if (message.groupNotificationSettings != null && Object.hasOwnProperty.call(message, "groupNotificationSettings"))
                $root.proto.NotificationSettings.encode(message.groupNotificationSettings, writer.uint32(/* id 18, wireType 2 =*/146).fork(), q + 1).ldelim();
            if (message.chatLockSettings != null && Object.hasOwnProperty.call(message, "chatLockSettings"))
                $root.proto.ChatLockSettings.encode(message.chatLockSettings, writer.uint32(/* id 19, wireType 2 =*/154).fork(), q + 1).ldelim();
            if (message.chatDbLidMigrationTimestamp != null && Object.hasOwnProperty.call(message, "chatDbLidMigrationTimestamp"))
                writer.uint32(/* id 20, wireType 0 =*/160).int64(message.chatDbLidMigrationTimestamp);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GlobalSettings message, length delimited. Does not implicitly {@link proto.GlobalSettings.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.GlobalSettings
         * @static
         * @param {proto.IGlobalSettings} message GlobalSettings message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GlobalSettings.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GlobalSettings message from the specified reader or buffer.
         * @function decode
         * @memberof proto.GlobalSettings
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.GlobalSettings} GlobalSettings
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GlobalSettings.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.lightThemeWallpaper = $root.proto.WallpaperSettings.decode(reader, reader.uint32(), undefined, q + 1, message.lightThemeWallpaper);
                        message._lightThemeWallpaper = "lightThemeWallpaper";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.mediaVisibility = reader.int32();
                        message._mediaVisibility = "mediaVisibility";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.darkThemeWallpaper = $root.proto.WallpaperSettings.decode(reader, reader.uint32(), undefined, q + 1, message.darkThemeWallpaper);
                        message._darkThemeWallpaper = "darkThemeWallpaper";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.autoDownloadWiFi = $root.proto.AutoDownloadSettings.decode(reader, reader.uint32(), undefined, q + 1, message.autoDownloadWiFi);
                        message._autoDownloadWiFi = "autoDownloadWiFi";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.autoDownloadCellular = $root.proto.AutoDownloadSettings.decode(reader, reader.uint32(), undefined, q + 1, message.autoDownloadCellular);
                        message._autoDownloadCellular = "autoDownloadCellular";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        message.autoDownloadRoaming = $root.proto.AutoDownloadSettings.decode(reader, reader.uint32(), undefined, q + 1, message.autoDownloadRoaming);
                        message._autoDownloadRoaming = "autoDownloadRoaming";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        message.showIndividualNotificationsPreview = reader.bool();
                        message._showIndividualNotificationsPreview = "showIndividualNotificationsPreview";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        message.showGroupNotificationsPreview = reader.bool();
                        message._showGroupNotificationsPreview = "showGroupNotificationsPreview";
                        continue;
                    }
                case 9: {
                        if (u !== 0)
                            break;
                        message.disappearingModeDuration = reader.int32();
                        message._disappearingModeDuration = "disappearingModeDuration";
                        continue;
                    }
                case 10: {
                        if (u !== 0)
                            break;
                        message.disappearingModeTimestamp = reader.int64();
                        message._disappearingModeTimestamp = "disappearingModeTimestamp";
                        continue;
                    }
                case 11: {
                        if (u !== 2)
                            break;
                        message.avatarUserSettings = $root.proto.AvatarUserSettings.decode(reader, reader.uint32(), undefined, q + 1, message.avatarUserSettings);
                        message._avatarUserSettings = "avatarUserSettings";
                        continue;
                    }
                case 12: {
                        if (u !== 0)
                            break;
                        message.fontSize = reader.int32();
                        message._fontSize = "fontSize";
                        continue;
                    }
                case 13: {
                        if (u !== 0)
                            break;
                        message.securityNotifications = reader.bool();
                        message._securityNotifications = "securityNotifications";
                        continue;
                    }
                case 14: {
                        if (u !== 0)
                            break;
                        message.autoUnarchiveChats = reader.bool();
                        message._autoUnarchiveChats = "autoUnarchiveChats";
                        continue;
                    }
                case 15: {
                        if (u !== 0)
                            break;
                        message.videoQualityMode = reader.int32();
                        message._videoQualityMode = "videoQualityMode";
                        continue;
                    }
                case 16: {
                        if (u !== 0)
                            break;
                        message.photoQualityMode = reader.int32();
                        message._photoQualityMode = "photoQualityMode";
                        continue;
                    }
                case 17: {
                        if (u !== 2)
                            break;
                        message.individualNotificationSettings = $root.proto.NotificationSettings.decode(reader, reader.uint32(), undefined, q + 1, message.individualNotificationSettings);
                        message._individualNotificationSettings = "individualNotificationSettings";
                        continue;
                    }
                case 18: {
                        if (u !== 2)
                            break;
                        message.groupNotificationSettings = $root.proto.NotificationSettings.decode(reader, reader.uint32(), undefined, q + 1, message.groupNotificationSettings);
                        message._groupNotificationSettings = "groupNotificationSettings";
                        continue;
                    }
                case 19: {
                        if (u !== 2)
                            break;
                        message.chatLockSettings = $root.proto.ChatLockSettings.decode(reader, reader.uint32(), undefined, q + 1, message.chatLockSettings);
                        message._chatLockSettings = "chatLockSettings";
                        continue;
                    }
                case 20: {
                        if (u !== 0)
                            break;
                        message.chatDbLidMigrationTimestamp = reader.int64();
                        message._chatDbLidMigrationTimestamp = "chatDbLidMigrationTimestamp";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a GlobalSettings message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.GlobalSettings
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.GlobalSettings} GlobalSettings
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GlobalSettings.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GlobalSettings message.
         * @function verify
         * @memberof proto.GlobalSettings
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GlobalSettings.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.lightThemeWallpaper != null && Object.hasOwnProperty.call(message, "lightThemeWallpaper")) {
                properties._lightThemeWallpaper = 1;
                {
                    var error = $root.proto.WallpaperSettings.verify(message.lightThemeWallpaper, q + 1);
                    if (error)
                        return "lightThemeWallpaper." + error;
                }
            }
            if (message.mediaVisibility != null && Object.hasOwnProperty.call(message, "mediaVisibility")) {
                properties._mediaVisibility = 1;
                if (typeof message.mediaVisibility !== "number" || (message.mediaVisibility | 0) !== message.mediaVisibility)
                    return "mediaVisibility: enum value expected";
            }
            if (message.darkThemeWallpaper != null && Object.hasOwnProperty.call(message, "darkThemeWallpaper")) {
                properties._darkThemeWallpaper = 1;
                {
                    var error = $root.proto.WallpaperSettings.verify(message.darkThemeWallpaper, q + 1);
                    if (error)
                        return "darkThemeWallpaper." + error;
                }
            }
            if (message.autoDownloadWiFi != null && Object.hasOwnProperty.call(message, "autoDownloadWiFi")) {
                properties._autoDownloadWiFi = 1;
                {
                    var error = $root.proto.AutoDownloadSettings.verify(message.autoDownloadWiFi, q + 1);
                    if (error)
                        return "autoDownloadWiFi." + error;
                }
            }
            if (message.autoDownloadCellular != null && Object.hasOwnProperty.call(message, "autoDownloadCellular")) {
                properties._autoDownloadCellular = 1;
                {
                    var error = $root.proto.AutoDownloadSettings.verify(message.autoDownloadCellular, q + 1);
                    if (error)
                        return "autoDownloadCellular." + error;
                }
            }
            if (message.autoDownloadRoaming != null && Object.hasOwnProperty.call(message, "autoDownloadRoaming")) {
                properties._autoDownloadRoaming = 1;
                {
                    var error = $root.proto.AutoDownloadSettings.verify(message.autoDownloadRoaming, q + 1);
                    if (error)
                        return "autoDownloadRoaming." + error;
                }
            }
            if (message.showIndividualNotificationsPreview != null && Object.hasOwnProperty.call(message, "showIndividualNotificationsPreview")) {
                properties._showIndividualNotificationsPreview = 1;
                if (typeof message.showIndividualNotificationsPreview !== "boolean")
                    return "showIndividualNotificationsPreview: boolean expected";
            }
            if (message.showGroupNotificationsPreview != null && Object.hasOwnProperty.call(message, "showGroupNotificationsPreview")) {
                properties._showGroupNotificationsPreview = 1;
                if (typeof message.showGroupNotificationsPreview !== "boolean")
                    return "showGroupNotificationsPreview: boolean expected";
            }
            if (message.disappearingModeDuration != null && Object.hasOwnProperty.call(message, "disappearingModeDuration")) {
                properties._disappearingModeDuration = 1;
                if (!$util.isInteger(message.disappearingModeDuration))
                    return "disappearingModeDuration: integer expected";
            }
            if (message.disappearingModeTimestamp != null && Object.hasOwnProperty.call(message, "disappearingModeTimestamp")) {
                properties._disappearingModeTimestamp = 1;
                if (!$util.isInteger(message.disappearingModeTimestamp) && !(message.disappearingModeTimestamp && $util.isInteger(message.disappearingModeTimestamp.low) && $util.isInteger(message.disappearingModeTimestamp.high)))
                    return "disappearingModeTimestamp: integer|Long expected";
            }
            if (message.avatarUserSettings != null && Object.hasOwnProperty.call(message, "avatarUserSettings")) {
                properties._avatarUserSettings = 1;
                {
                    var error = $root.proto.AvatarUserSettings.verify(message.avatarUserSettings, q + 1);
                    if (error)
                        return "avatarUserSettings." + error;
                }
            }
            if (message.fontSize != null && Object.hasOwnProperty.call(message, "fontSize")) {
                properties._fontSize = 1;
                if (!$util.isInteger(message.fontSize))
                    return "fontSize: integer expected";
            }
            if (message.securityNotifications != null && Object.hasOwnProperty.call(message, "securityNotifications")) {
                properties._securityNotifications = 1;
                if (typeof message.securityNotifications !== "boolean")
                    return "securityNotifications: boolean expected";
            }
            if (message.autoUnarchiveChats != null && Object.hasOwnProperty.call(message, "autoUnarchiveChats")) {
                properties._autoUnarchiveChats = 1;
                if (typeof message.autoUnarchiveChats !== "boolean")
                    return "autoUnarchiveChats: boolean expected";
            }
            if (message.videoQualityMode != null && Object.hasOwnProperty.call(message, "videoQualityMode")) {
                properties._videoQualityMode = 1;
                if (!$util.isInteger(message.videoQualityMode))
                    return "videoQualityMode: integer expected";
            }
            if (message.photoQualityMode != null && Object.hasOwnProperty.call(message, "photoQualityMode")) {
                properties._photoQualityMode = 1;
                if (!$util.isInteger(message.photoQualityMode))
                    return "photoQualityMode: integer expected";
            }
            if (message.individualNotificationSettings != null && Object.hasOwnProperty.call(message, "individualNotificationSettings")) {
                properties._individualNotificationSettings = 1;
                {
                    var error = $root.proto.NotificationSettings.verify(message.individualNotificationSettings, q + 1);
                    if (error)
                        return "individualNotificationSettings." + error;
                }
            }
            if (message.groupNotificationSettings != null && Object.hasOwnProperty.call(message, "groupNotificationSettings")) {
                properties._groupNotificationSettings = 1;
                {
                    var error = $root.proto.NotificationSettings.verify(message.groupNotificationSettings, q + 1);
                    if (error)
                        return "groupNotificationSettings." + error;
                }
            }
            if (message.chatLockSettings != null && Object.hasOwnProperty.call(message, "chatLockSettings")) {
                properties._chatLockSettings = 1;
                {
                    var error = $root.proto.ChatLockSettings.verify(message.chatLockSettings, q + 1);
                    if (error)
                        return "chatLockSettings." + error;
                }
            }
            if (message.chatDbLidMigrationTimestamp != null && Object.hasOwnProperty.call(message, "chatDbLidMigrationTimestamp")) {
                properties._chatDbLidMigrationTimestamp = 1;
                if (!$util.isInteger(message.chatDbLidMigrationTimestamp) && !(message.chatDbLidMigrationTimestamp && $util.isInteger(message.chatDbLidMigrationTimestamp.low) && $util.isInteger(message.chatDbLidMigrationTimestamp.high)))
                    return "chatDbLidMigrationTimestamp: integer|Long expected";
            }
            return null;
        };

        /**
         * Creates a GlobalSettings message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.GlobalSettings
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.GlobalSettings} GlobalSettings
         */
        GlobalSettings.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.GlobalSettings: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.lightThemeWallpaper != null) {
                if (!$util.isObject(object.lightThemeWallpaper))
                    throw TypeError(".proto.GlobalSettings.lightThemeWallpaper: object expected");
                message.lightThemeWallpaper = $root.proto.WallpaperSettings.fromObject(object.lightThemeWallpaper, q + 1);
            }
            switch (object.mediaVisibility) {
            case "DEFAULT":
            case 0:
                message.mediaVisibility = 0;
                break;
            case "OFF":
            case 1:
                message.mediaVisibility = 1;
                break;
            case "ON":
            case 2:
                message.mediaVisibility = 2;
                break;
            default:
                if (typeof object.mediaVisibility === "number" && (object.mediaVisibility | 0) === object.mediaVisibility)
                    message.mediaVisibility = object.mediaVisibility;
            }
            if (object.darkThemeWallpaper != null) {
                if (!$util.isObject(object.darkThemeWallpaper))
                    throw TypeError(".proto.GlobalSettings.darkThemeWallpaper: object expected");
                message.darkThemeWallpaper = $root.proto.WallpaperSettings.fromObject(object.darkThemeWallpaper, q + 1);
            }
            if (object.autoDownloadWiFi != null) {
                if (!$util.isObject(object.autoDownloadWiFi))
                    throw TypeError(".proto.GlobalSettings.autoDownloadWiFi: object expected");
                message.autoDownloadWiFi = $root.proto.AutoDownloadSettings.fromObject(object.autoDownloadWiFi, q + 1);
            }
            if (object.autoDownloadCellular != null) {
                if (!$util.isObject(object.autoDownloadCellular))
                    throw TypeError(".proto.GlobalSettings.autoDownloadCellular: object expected");
                message.autoDownloadCellular = $root.proto.AutoDownloadSettings.fromObject(object.autoDownloadCellular, q + 1);
            }
            if (object.autoDownloadRoaming != null) {
                if (!$util.isObject(object.autoDownloadRoaming))
                    throw TypeError(".proto.GlobalSettings.autoDownloadRoaming: object expected");
                message.autoDownloadRoaming = $root.proto.AutoDownloadSettings.fromObject(object.autoDownloadRoaming, q + 1);
            }
            if (object.showIndividualNotificationsPreview != null)
                message.showIndividualNotificationsPreview = Boolean(object.showIndividualNotificationsPreview);
            if (object.showGroupNotificationsPreview != null)
                message.showGroupNotificationsPreview = Boolean(object.showGroupNotificationsPreview);
            if (object.disappearingModeDuration != null)
                message.disappearingModeDuration = object.disappearingModeDuration | 0;
            if (object.disappearingModeTimestamp != null)
                if ($util.Long)
                    message.disappearingModeTimestamp = $util.Long.fromValue(object.disappearingModeTimestamp, false);
                else if (typeof object.disappearingModeTimestamp === "string")
                    message.disappearingModeTimestamp = parseInt(object.disappearingModeTimestamp, 10);
                else if (typeof object.disappearingModeTimestamp === "number")
                    message.disappearingModeTimestamp = object.disappearingModeTimestamp;
                else if (typeof object.disappearingModeTimestamp === "object")
                    message.disappearingModeTimestamp = new $util.LongBits(object.disappearingModeTimestamp.low >>> 0, object.disappearingModeTimestamp.high >>> 0).toNumber();
            if (object.avatarUserSettings != null) {
                if (!$util.isObject(object.avatarUserSettings))
                    throw TypeError(".proto.GlobalSettings.avatarUserSettings: object expected");
                message.avatarUserSettings = $root.proto.AvatarUserSettings.fromObject(object.avatarUserSettings, q + 1);
            }
            if (object.fontSize != null)
                message.fontSize = object.fontSize | 0;
            if (object.securityNotifications != null)
                message.securityNotifications = Boolean(object.securityNotifications);
            if (object.autoUnarchiveChats != null)
                message.autoUnarchiveChats = Boolean(object.autoUnarchiveChats);
            if (object.videoQualityMode != null)
                message.videoQualityMode = object.videoQualityMode | 0;
            if (object.photoQualityMode != null)
                message.photoQualityMode = object.photoQualityMode | 0;
            if (object.individualNotificationSettings != null) {
                if (!$util.isObject(object.individualNotificationSettings))
                    throw TypeError(".proto.GlobalSettings.individualNotificationSettings: object expected");
                message.individualNotificationSettings = $root.proto.NotificationSettings.fromObject(object.individualNotificationSettings, q + 1);
            }
            if (object.groupNotificationSettings != null) {
                if (!$util.isObject(object.groupNotificationSettings))
                    throw TypeError(".proto.GlobalSettings.groupNotificationSettings: object expected");
                message.groupNotificationSettings = $root.proto.NotificationSettings.fromObject(object.groupNotificationSettings, q + 1);
            }
            if (object.chatLockSettings != null) {
                if (!$util.isObject(object.chatLockSettings))
                    throw TypeError(".proto.GlobalSettings.chatLockSettings: object expected");
                message.chatLockSettings = $root.proto.ChatLockSettings.fromObject(object.chatLockSettings, q + 1);
            }
            if (object.chatDbLidMigrationTimestamp != null)
                if ($util.Long)
                    message.chatDbLidMigrationTimestamp = $util.Long.fromValue(object.chatDbLidMigrationTimestamp, false);
                else if (typeof object.chatDbLidMigrationTimestamp === "string")
                    message.chatDbLidMigrationTimestamp = parseInt(object.chatDbLidMigrationTimestamp, 10);
                else if (typeof object.chatDbLidMigrationTimestamp === "number")
                    message.chatDbLidMigrationTimestamp = object.chatDbLidMigrationTimestamp;
                else if (typeof object.chatDbLidMigrationTimestamp === "object")
                    message.chatDbLidMigrationTimestamp = new $util.LongBits(object.chatDbLidMigrationTimestamp.low >>> 0, object.chatDbLidMigrationTimestamp.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a GlobalSettings message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.GlobalSettings
         * @static
         * @param {proto.GlobalSettings} message GlobalSettings
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GlobalSettings.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.lightThemeWallpaper != null && Object.hasOwnProperty.call(message, "lightThemeWallpaper"))
                object.lightThemeWallpaper = $root.proto.WallpaperSettings.toObject(message.lightThemeWallpaper, options, q + 1);
            if (message.mediaVisibility != null && Object.hasOwnProperty.call(message, "mediaVisibility"))
                object.mediaVisibility = options.enums === String ? $root.proto.MediaVisibility[message.mediaVisibility] === undefined ? message.mediaVisibility : $root.proto.MediaVisibility[message.mediaVisibility] : message.mediaVisibility;
            if (message.darkThemeWallpaper != null && Object.hasOwnProperty.call(message, "darkThemeWallpaper"))
                object.darkThemeWallpaper = $root.proto.WallpaperSettings.toObject(message.darkThemeWallpaper, options, q + 1);
            if (message.autoDownloadWiFi != null && Object.hasOwnProperty.call(message, "autoDownloadWiFi"))
                object.autoDownloadWiFi = $root.proto.AutoDownloadSettings.toObject(message.autoDownloadWiFi, options, q + 1);
            if (message.autoDownloadCellular != null && Object.hasOwnProperty.call(message, "autoDownloadCellular"))
                object.autoDownloadCellular = $root.proto.AutoDownloadSettings.toObject(message.autoDownloadCellular, options, q + 1);
            if (message.autoDownloadRoaming != null && Object.hasOwnProperty.call(message, "autoDownloadRoaming"))
                object.autoDownloadRoaming = $root.proto.AutoDownloadSettings.toObject(message.autoDownloadRoaming, options, q + 1);
            if (message.showIndividualNotificationsPreview != null && Object.hasOwnProperty.call(message, "showIndividualNotificationsPreview"))
                object.showIndividualNotificationsPreview = message.showIndividualNotificationsPreview;
            if (message.showGroupNotificationsPreview != null && Object.hasOwnProperty.call(message, "showGroupNotificationsPreview"))
                object.showGroupNotificationsPreview = message.showGroupNotificationsPreview;
            if (message.disappearingModeDuration != null && Object.hasOwnProperty.call(message, "disappearingModeDuration"))
                object.disappearingModeDuration = message.disappearingModeDuration;
            if (message.disappearingModeTimestamp != null && Object.hasOwnProperty.call(message, "disappearingModeTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.disappearingModeTimestamp = typeof message.disappearingModeTimestamp === "number" ? BigInt(message.disappearingModeTimestamp) : $util.Long.fromBits(message.disappearingModeTimestamp.low >>> 0, message.disappearingModeTimestamp.high >>> 0, false).toBigInt();
                else if (typeof message.disappearingModeTimestamp === "number")
                    object.disappearingModeTimestamp = options.longs === String ? String(message.disappearingModeTimestamp) : message.disappearingModeTimestamp;
                else
                    object.disappearingModeTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.disappearingModeTimestamp) : options.longs === Number ? new $util.LongBits(message.disappearingModeTimestamp.low >>> 0, message.disappearingModeTimestamp.high >>> 0).toNumber() : message.disappearingModeTimestamp;
            if (message.avatarUserSettings != null && Object.hasOwnProperty.call(message, "avatarUserSettings"))
                object.avatarUserSettings = $root.proto.AvatarUserSettings.toObject(message.avatarUserSettings, options, q + 1);
            if (message.fontSize != null && Object.hasOwnProperty.call(message, "fontSize"))
                object.fontSize = message.fontSize;
            if (message.securityNotifications != null && Object.hasOwnProperty.call(message, "securityNotifications"))
                object.securityNotifications = message.securityNotifications;
            if (message.autoUnarchiveChats != null && Object.hasOwnProperty.call(message, "autoUnarchiveChats"))
                object.autoUnarchiveChats = message.autoUnarchiveChats;
            if (message.videoQualityMode != null && Object.hasOwnProperty.call(message, "videoQualityMode"))
                object.videoQualityMode = message.videoQualityMode;
            if (message.photoQualityMode != null && Object.hasOwnProperty.call(message, "photoQualityMode"))
                object.photoQualityMode = message.photoQualityMode;
            if (message.individualNotificationSettings != null && Object.hasOwnProperty.call(message, "individualNotificationSettings"))
                object.individualNotificationSettings = $root.proto.NotificationSettings.toObject(message.individualNotificationSettings, options, q + 1);
            if (message.groupNotificationSettings != null && Object.hasOwnProperty.call(message, "groupNotificationSettings"))
                object.groupNotificationSettings = $root.proto.NotificationSettings.toObject(message.groupNotificationSettings, options, q + 1);
            if (message.chatLockSettings != null && Object.hasOwnProperty.call(message, "chatLockSettings"))
                object.chatLockSettings = $root.proto.ChatLockSettings.toObject(message.chatLockSettings, options, q + 1);
            if (message.chatDbLidMigrationTimestamp != null && Object.hasOwnProperty.call(message, "chatDbLidMigrationTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.chatDbLidMigrationTimestamp = typeof message.chatDbLidMigrationTimestamp === "number" ? BigInt(message.chatDbLidMigrationTimestamp) : $util.Long.fromBits(message.chatDbLidMigrationTimestamp.low >>> 0, message.chatDbLidMigrationTimestamp.high >>> 0, false).toBigInt();
                else if (typeof message.chatDbLidMigrationTimestamp === "number")
                    object.chatDbLidMigrationTimestamp = options.longs === String ? String(message.chatDbLidMigrationTimestamp) : message.chatDbLidMigrationTimestamp;
                else
                    object.chatDbLidMigrationTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.chatDbLidMigrationTimestamp) : options.longs === Number ? new $util.LongBits(message.chatDbLidMigrationTimestamp.low >>> 0, message.chatDbLidMigrationTimestamp.high >>> 0).toNumber() : message.chatDbLidMigrationTimestamp;
            return object;
        };

        /**
         * Converts this GlobalSettings to JSON.
         * @function toJSON
         * @memberof proto.GlobalSettings
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GlobalSettings.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GlobalSettings
         * @function getTypeUrl
         * @memberof proto.GlobalSettings
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GlobalSettings.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.GlobalSettings";
        };

        return GlobalSettings;
    })();

    proto.GroupHistoryBundleInfo = (function() {

        /**
         * Properties of a GroupHistoryBundleInfo.
         * @memberof proto
         * @interface IGroupHistoryBundleInfo
         * @property {proto.Message.IMessageHistoryBundle|null} [deprecatedMessageHistoryBundle] GroupHistoryBundleInfo deprecatedMessageHistoryBundle
         * @property {proto.GroupHistoryBundleInfo.ProcessState|null} [processState] GroupHistoryBundleInfo processState
         */

        /**
         * Constructs a new GroupHistoryBundleInfo.
         * @memberof proto
         * @classdesc Represents a GroupHistoryBundleInfo.
         * @implements IGroupHistoryBundleInfo
         * @constructor
         * @param {proto.IGroupHistoryBundleInfo=} [properties] Properties to set
         */
        function GroupHistoryBundleInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GroupHistoryBundleInfo deprecatedMessageHistoryBundle.
         * @member {proto.Message.IMessageHistoryBundle|null|undefined} deprecatedMessageHistoryBundle
         * @memberof proto.GroupHistoryBundleInfo
         * @instance
         */
        GroupHistoryBundleInfo.prototype.deprecatedMessageHistoryBundle = null;

        /**
         * GroupHistoryBundleInfo processState.
         * @member {proto.GroupHistoryBundleInfo.ProcessState|null|undefined} processState
         * @memberof proto.GroupHistoryBundleInfo
         * @instance
         */
        GroupHistoryBundleInfo.prototype.processState = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupHistoryBundleInfo.prototype, "_deprecatedMessageHistoryBundle", {
            get: $util.oneOfGetter($oneOfFields = ["deprecatedMessageHistoryBundle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupHistoryBundleInfo.prototype, "_processState", {
            get: $util.oneOfGetter($oneOfFields = ["processState"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new GroupHistoryBundleInfo instance using the specified properties.
         * @function create
         * @memberof proto.GroupHistoryBundleInfo
         * @static
         * @param {proto.IGroupHistoryBundleInfo=} [properties] Properties to set
         * @returns {proto.GroupHistoryBundleInfo} GroupHistoryBundleInfo instance
         */
        GroupHistoryBundleInfo.create = function create(properties) {
            return new GroupHistoryBundleInfo(properties);
        };

        /**
         * Encodes the specified GroupHistoryBundleInfo message. Does not implicitly {@link proto.GroupHistoryBundleInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.GroupHistoryBundleInfo
         * @static
         * @param {proto.IGroupHistoryBundleInfo} message GroupHistoryBundleInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GroupHistoryBundleInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.deprecatedMessageHistoryBundle != null && Object.hasOwnProperty.call(message, "deprecatedMessageHistoryBundle"))
                $root.proto.Message.MessageHistoryBundle.encode(message.deprecatedMessageHistoryBundle, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.processState != null && Object.hasOwnProperty.call(message, "processState"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.processState);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GroupHistoryBundleInfo message, length delimited. Does not implicitly {@link proto.GroupHistoryBundleInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.GroupHistoryBundleInfo
         * @static
         * @param {proto.IGroupHistoryBundleInfo} message GroupHistoryBundleInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GroupHistoryBundleInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GroupHistoryBundleInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.GroupHistoryBundleInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.GroupHistoryBundleInfo} GroupHistoryBundleInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GroupHistoryBundleInfo.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.deprecatedMessageHistoryBundle = $root.proto.Message.MessageHistoryBundle.decode(reader, reader.uint32(), undefined, q + 1, message.deprecatedMessageHistoryBundle);
                        message._deprecatedMessageHistoryBundle = "deprecatedMessageHistoryBundle";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.processState = reader.int32();
                        message._processState = "processState";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a GroupHistoryBundleInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.GroupHistoryBundleInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.GroupHistoryBundleInfo} GroupHistoryBundleInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GroupHistoryBundleInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GroupHistoryBundleInfo message.
         * @function verify
         * @memberof proto.GroupHistoryBundleInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GroupHistoryBundleInfo.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.deprecatedMessageHistoryBundle != null && Object.hasOwnProperty.call(message, "deprecatedMessageHistoryBundle")) {
                properties._deprecatedMessageHistoryBundle = 1;
                {
                    var error = $root.proto.Message.MessageHistoryBundle.verify(message.deprecatedMessageHistoryBundle, q + 1);
                    if (error)
                        return "deprecatedMessageHistoryBundle." + error;
                }
            }
            if (message.processState != null && Object.hasOwnProperty.call(message, "processState")) {
                properties._processState = 1;
                if (typeof message.processState !== "number" || (message.processState | 0) !== message.processState)
                    return "processState: enum value expected";
            }
            return null;
        };

        /**
         * Creates a GroupHistoryBundleInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.GroupHistoryBundleInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.GroupHistoryBundleInfo} GroupHistoryBundleInfo
         */
        GroupHistoryBundleInfo.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.GroupHistoryBundleInfo: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.deprecatedMessageHistoryBundle != null) {
                if (!$util.isObject(object.deprecatedMessageHistoryBundle))
                    throw TypeError(".proto.GroupHistoryBundleInfo.deprecatedMessageHistoryBundle: object expected");
                message.deprecatedMessageHistoryBundle = $root.proto.Message.MessageHistoryBundle.fromObject(object.deprecatedMessageHistoryBundle, q + 1);
            }
            switch (object.processState) {
            case "NOT_INJECTED":
            case 0:
                message.processState = 0;
                break;
            case "INJECTED":
            case 1:
                message.processState = 1;
                break;
            case "INJECTED_PARTIAL":
            case 2:
                message.processState = 2;
                break;
            case "INJECTION_FAILED":
            case 3:
                message.processState = 3;
                break;
            case "INJECTION_FAILED_NO_RETRY":
            case 4:
                message.processState = 4;
                break;
            case "DEDUPED":
            case 5:
                message.processState = 5;
                break;
            default:
                if (typeof object.processState === "number" && (object.processState | 0) === object.processState)
                    message.processState = object.processState;
            }
            return message;
        };

        /**
         * Creates a plain object from a GroupHistoryBundleInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.GroupHistoryBundleInfo
         * @static
         * @param {proto.GroupHistoryBundleInfo} message GroupHistoryBundleInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GroupHistoryBundleInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.deprecatedMessageHistoryBundle != null && Object.hasOwnProperty.call(message, "deprecatedMessageHistoryBundle"))
                object.deprecatedMessageHistoryBundle = $root.proto.Message.MessageHistoryBundle.toObject(message.deprecatedMessageHistoryBundle, options, q + 1);
            if (message.processState != null && Object.hasOwnProperty.call(message, "processState"))
                object.processState = options.enums === String ? $root.proto.GroupHistoryBundleInfo.ProcessState[message.processState] === undefined ? message.processState : $root.proto.GroupHistoryBundleInfo.ProcessState[message.processState] : message.processState;
            return object;
        };

        /**
         * Converts this GroupHistoryBundleInfo to JSON.
         * @function toJSON
         * @memberof proto.GroupHistoryBundleInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GroupHistoryBundleInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GroupHistoryBundleInfo
         * @function getTypeUrl
         * @memberof proto.GroupHistoryBundleInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GroupHistoryBundleInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.GroupHistoryBundleInfo";
        };

        /**
         * ProcessState enum.
         * @name proto.GroupHistoryBundleInfo.ProcessState
         * @enum {number}
         * @property {number} NOT_INJECTED=0 NOT_INJECTED value
         * @property {number} INJECTED=1 INJECTED value
         * @property {number} INJECTED_PARTIAL=2 INJECTED_PARTIAL value
         * @property {number} INJECTION_FAILED=3 INJECTION_FAILED value
         * @property {number} INJECTION_FAILED_NO_RETRY=4 INJECTION_FAILED_NO_RETRY value
         * @property {number} DEDUPED=5 DEDUPED value
         */
        GroupHistoryBundleInfo.ProcessState = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "NOT_INJECTED"] = 0;
            values[valuesById[1] = "INJECTED"] = 1;
            values[valuesById[2] = "INJECTED_PARTIAL"] = 2;
            values[valuesById[3] = "INJECTION_FAILED"] = 3;
            values[valuesById[4] = "INJECTION_FAILED_NO_RETRY"] = 4;
            values[valuesById[5] = "DEDUPED"] = 5;
            return values;
        })();

        return GroupHistoryBundleInfo;
    })();

    proto.GroupHistoryIndividualMessageInfo = (function() {

        /**
         * Properties of a GroupHistoryIndividualMessageInfo.
         * @memberof proto
         * @interface IGroupHistoryIndividualMessageInfo
         * @property {proto.IMessageKey|null} [bundleMessageKey] GroupHistoryIndividualMessageInfo bundleMessageKey
         * @property {boolean|null} [editedAfterReceivedAsHistory] GroupHistoryIndividualMessageInfo editedAfterReceivedAsHistory
         */

        /**
         * Constructs a new GroupHistoryIndividualMessageInfo.
         * @memberof proto
         * @classdesc Represents a GroupHistoryIndividualMessageInfo.
         * @implements IGroupHistoryIndividualMessageInfo
         * @constructor
         * @param {proto.IGroupHistoryIndividualMessageInfo=} [properties] Properties to set
         */
        function GroupHistoryIndividualMessageInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GroupHistoryIndividualMessageInfo bundleMessageKey.
         * @member {proto.IMessageKey|null|undefined} bundleMessageKey
         * @memberof proto.GroupHistoryIndividualMessageInfo
         * @instance
         */
        GroupHistoryIndividualMessageInfo.prototype.bundleMessageKey = null;

        /**
         * GroupHistoryIndividualMessageInfo editedAfterReceivedAsHistory.
         * @member {boolean|null|undefined} editedAfterReceivedAsHistory
         * @memberof proto.GroupHistoryIndividualMessageInfo
         * @instance
         */
        GroupHistoryIndividualMessageInfo.prototype.editedAfterReceivedAsHistory = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupHistoryIndividualMessageInfo.prototype, "_bundleMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["bundleMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupHistoryIndividualMessageInfo.prototype, "_editedAfterReceivedAsHistory", {
            get: $util.oneOfGetter($oneOfFields = ["editedAfterReceivedAsHistory"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new GroupHistoryIndividualMessageInfo instance using the specified properties.
         * @function create
         * @memberof proto.GroupHistoryIndividualMessageInfo
         * @static
         * @param {proto.IGroupHistoryIndividualMessageInfo=} [properties] Properties to set
         * @returns {proto.GroupHistoryIndividualMessageInfo} GroupHistoryIndividualMessageInfo instance
         */
        GroupHistoryIndividualMessageInfo.create = function create(properties) {
            return new GroupHistoryIndividualMessageInfo(properties);
        };

        /**
         * Encodes the specified GroupHistoryIndividualMessageInfo message. Does not implicitly {@link proto.GroupHistoryIndividualMessageInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.GroupHistoryIndividualMessageInfo
         * @static
         * @param {proto.IGroupHistoryIndividualMessageInfo} message GroupHistoryIndividualMessageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GroupHistoryIndividualMessageInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.bundleMessageKey != null && Object.hasOwnProperty.call(message, "bundleMessageKey"))
                $root.proto.MessageKey.encode(message.bundleMessageKey, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.editedAfterReceivedAsHistory != null && Object.hasOwnProperty.call(message, "editedAfterReceivedAsHistory"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.editedAfterReceivedAsHistory);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GroupHistoryIndividualMessageInfo message, length delimited. Does not implicitly {@link proto.GroupHistoryIndividualMessageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.GroupHistoryIndividualMessageInfo
         * @static
         * @param {proto.IGroupHistoryIndividualMessageInfo} message GroupHistoryIndividualMessageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GroupHistoryIndividualMessageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GroupHistoryIndividualMessageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.GroupHistoryIndividualMessageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.GroupHistoryIndividualMessageInfo} GroupHistoryIndividualMessageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GroupHistoryIndividualMessageInfo.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.bundleMessageKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.bundleMessageKey);
                        message._bundleMessageKey = "bundleMessageKey";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.editedAfterReceivedAsHistory = reader.bool();
                        message._editedAfterReceivedAsHistory = "editedAfterReceivedAsHistory";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a GroupHistoryIndividualMessageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.GroupHistoryIndividualMessageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.GroupHistoryIndividualMessageInfo} GroupHistoryIndividualMessageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GroupHistoryIndividualMessageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GroupHistoryIndividualMessageInfo message.
         * @function verify
         * @memberof proto.GroupHistoryIndividualMessageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GroupHistoryIndividualMessageInfo.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.bundleMessageKey != null && Object.hasOwnProperty.call(message, "bundleMessageKey")) {
                properties._bundleMessageKey = 1;
                {
                    var error = $root.proto.MessageKey.verify(message.bundleMessageKey, q + 1);
                    if (error)
                        return "bundleMessageKey." + error;
                }
            }
            if (message.editedAfterReceivedAsHistory != null && Object.hasOwnProperty.call(message, "editedAfterReceivedAsHistory")) {
                properties._editedAfterReceivedAsHistory = 1;
                if (typeof message.editedAfterReceivedAsHistory !== "boolean")
                    return "editedAfterReceivedAsHistory: boolean expected";
            }
            return null;
        };

        /**
         * Creates a GroupHistoryIndividualMessageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.GroupHistoryIndividualMessageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.GroupHistoryIndividualMessageInfo} GroupHistoryIndividualMessageInfo
         */
        GroupHistoryIndividualMessageInfo.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.GroupHistoryIndividualMessageInfo: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.bundleMessageKey != null) {
                if (!$util.isObject(object.bundleMessageKey))
                    throw TypeError(".proto.GroupHistoryIndividualMessageInfo.bundleMessageKey: object expected");
                message.bundleMessageKey = $root.proto.MessageKey.fromObject(object.bundleMessageKey, q + 1);
            }
            if (object.editedAfterReceivedAsHistory != null)
                message.editedAfterReceivedAsHistory = Boolean(object.editedAfterReceivedAsHistory);
            return message;
        };

        /**
         * Creates a plain object from a GroupHistoryIndividualMessageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.GroupHistoryIndividualMessageInfo
         * @static
         * @param {proto.GroupHistoryIndividualMessageInfo} message GroupHistoryIndividualMessageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GroupHistoryIndividualMessageInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.bundleMessageKey != null && Object.hasOwnProperty.call(message, "bundleMessageKey"))
                object.bundleMessageKey = $root.proto.MessageKey.toObject(message.bundleMessageKey, options, q + 1);
            if (message.editedAfterReceivedAsHistory != null && Object.hasOwnProperty.call(message, "editedAfterReceivedAsHistory"))
                object.editedAfterReceivedAsHistory = message.editedAfterReceivedAsHistory;
            return object;
        };

        /**
         * Converts this GroupHistoryIndividualMessageInfo to JSON.
         * @function toJSON
         * @memberof proto.GroupHistoryIndividualMessageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GroupHistoryIndividualMessageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GroupHistoryIndividualMessageInfo
         * @function getTypeUrl
         * @memberof proto.GroupHistoryIndividualMessageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GroupHistoryIndividualMessageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.GroupHistoryIndividualMessageInfo";
        };

        return GroupHistoryIndividualMessageInfo;
    })();

    proto.GroupMention = (function() {

        /**
         * Properties of a GroupMention.
         * @memberof proto
         * @interface IGroupMention
         * @property {string|null} [groupJid] GroupMention groupJid
         * @property {string|null} [groupSubject] GroupMention groupSubject
         */

        /**
         * Constructs a new GroupMention.
         * @memberof proto
         * @classdesc Represents a GroupMention.
         * @implements IGroupMention
         * @constructor
         * @param {proto.IGroupMention=} [properties] Properties to set
         */
        function GroupMention(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GroupMention groupJid.
         * @member {string|null|undefined} groupJid
         * @memberof proto.GroupMention
         * @instance
         */
        GroupMention.prototype.groupJid = null;

        /**
         * GroupMention groupSubject.
         * @member {string|null|undefined} groupSubject
         * @memberof proto.GroupMention
         * @instance
         */
        GroupMention.prototype.groupSubject = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupMention.prototype, "_groupJid", {
            get: $util.oneOfGetter($oneOfFields = ["groupJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupMention.prototype, "_groupSubject", {
            get: $util.oneOfGetter($oneOfFields = ["groupSubject"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new GroupMention instance using the specified properties.
         * @function create
         * @memberof proto.GroupMention
         * @static
         * @param {proto.IGroupMention=} [properties] Properties to set
         * @returns {proto.GroupMention} GroupMention instance
         */
        GroupMention.create = function create(properties) {
            return new GroupMention(properties);
        };

        /**
         * Encodes the specified GroupMention message. Does not implicitly {@link proto.GroupMention.verify|verify} messages.
         * @function encode
         * @memberof proto.GroupMention
         * @static
         * @param {proto.IGroupMention} message GroupMention message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GroupMention.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.groupJid != null && Object.hasOwnProperty.call(message, "groupJid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.groupJid);
            if (message.groupSubject != null && Object.hasOwnProperty.call(message, "groupSubject"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.groupSubject);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GroupMention message, length delimited. Does not implicitly {@link proto.GroupMention.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.GroupMention
         * @static
         * @param {proto.IGroupMention} message GroupMention message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GroupMention.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GroupMention message from the specified reader or buffer.
         * @function decode
         * @memberof proto.GroupMention
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.GroupMention} GroupMention
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GroupMention.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.groupJid = reader.stringVerify();
                        message._groupJid = "groupJid";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.groupSubject = reader.stringVerify();
                        message._groupSubject = "groupSubject";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a GroupMention message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.GroupMention
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.GroupMention} GroupMention
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GroupMention.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GroupMention message.
         * @function verify
         * @memberof proto.GroupMention
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GroupMention.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.groupJid != null && Object.hasOwnProperty.call(message, "groupJid")) {
                properties._groupJid = 1;
                if (!$util.isString(message.groupJid))
                    return "groupJid: string expected";
            }
            if (message.groupSubject != null && Object.hasOwnProperty.call(message, "groupSubject")) {
                properties._groupSubject = 1;
                if (!$util.isString(message.groupSubject))
                    return "groupSubject: string expected";
            }
            return null;
        };

        /**
         * Creates a GroupMention message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.GroupMention
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.GroupMention} GroupMention
         */
        GroupMention.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.GroupMention: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.groupJid != null)
                message.groupJid = String(object.groupJid);
            if (object.groupSubject != null)
                message.groupSubject = String(object.groupSubject);
            return message;
        };

        /**
         * Creates a plain object from a GroupMention message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.GroupMention
         * @static
         * @param {proto.GroupMention} message GroupMention
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GroupMention.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.groupJid != null && Object.hasOwnProperty.call(message, "groupJid"))
                object.groupJid = message.groupJid;
            if (message.groupSubject != null && Object.hasOwnProperty.call(message, "groupSubject"))
                object.groupSubject = message.groupSubject;
            return object;
        };

        /**
         * Converts this GroupMention to JSON.
         * @function toJSON
         * @memberof proto.GroupMention
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GroupMention.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GroupMention
         * @function getTypeUrl
         * @memberof proto.GroupMention
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GroupMention.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.GroupMention";
        };

        return GroupMention;
    })();

    proto.GroupParticipant = (function() {

        /**
         * Properties of a GroupParticipant.
         * @memberof proto
         * @interface IGroupParticipant
         * @property {string|null} [userJid] GroupParticipant userJid
         * @property {proto.GroupParticipant.Rank|null} [rank] GroupParticipant rank
         * @property {proto.IMemberLabel|null} [memberLabel] GroupParticipant memberLabel
         */

        /**
         * Constructs a new GroupParticipant.
         * @memberof proto
         * @classdesc Represents a GroupParticipant.
         * @implements IGroupParticipant
         * @constructor
         * @param {proto.IGroupParticipant=} [properties] Properties to set
         */
        function GroupParticipant(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GroupParticipant userJid.
         * @member {string|null|undefined} userJid
         * @memberof proto.GroupParticipant
         * @instance
         */
        GroupParticipant.prototype.userJid = null;

        /**
         * GroupParticipant rank.
         * @member {proto.GroupParticipant.Rank|null|undefined} rank
         * @memberof proto.GroupParticipant
         * @instance
         */
        GroupParticipant.prototype.rank = null;

        /**
         * GroupParticipant memberLabel.
         * @member {proto.IMemberLabel|null|undefined} memberLabel
         * @memberof proto.GroupParticipant
         * @instance
         */
        GroupParticipant.prototype.memberLabel = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupParticipant.prototype, "_userJid", {
            get: $util.oneOfGetter($oneOfFields = ["userJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupParticipant.prototype, "_rank", {
            get: $util.oneOfGetter($oneOfFields = ["rank"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupParticipant.prototype, "_memberLabel", {
            get: $util.oneOfGetter($oneOfFields = ["memberLabel"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new GroupParticipant instance using the specified properties.
         * @function create
         * @memberof proto.GroupParticipant
         * @static
         * @param {proto.IGroupParticipant=} [properties] Properties to set
         * @returns {proto.GroupParticipant} GroupParticipant instance
         */
        GroupParticipant.create = function create(properties) {
            return new GroupParticipant(properties);
        };

        /**
         * Encodes the specified GroupParticipant message. Does not implicitly {@link proto.GroupParticipant.verify|verify} messages.
         * @function encode
         * @memberof proto.GroupParticipant
         * @static
         * @param {proto.IGroupParticipant} message GroupParticipant message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GroupParticipant.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.userJid != null && Object.hasOwnProperty.call(message, "userJid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.userJid);
            if (message.rank != null && Object.hasOwnProperty.call(message, "rank"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.rank);
            if (message.memberLabel != null && Object.hasOwnProperty.call(message, "memberLabel"))
                $root.proto.MemberLabel.encode(message.memberLabel, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GroupParticipant message, length delimited. Does not implicitly {@link proto.GroupParticipant.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.GroupParticipant
         * @static
         * @param {proto.IGroupParticipant} message GroupParticipant message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GroupParticipant.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GroupParticipant message from the specified reader or buffer.
         * @function decode
         * @memberof proto.GroupParticipant
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.GroupParticipant} GroupParticipant
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GroupParticipant.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.userJid = reader.stringVerify();
                        message._userJid = "userJid";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.rank = reader.int32();
                        message._rank = "rank";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.memberLabel = $root.proto.MemberLabel.decode(reader, reader.uint32(), undefined, q + 1, message.memberLabel);
                        message._memberLabel = "memberLabel";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a GroupParticipant message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.GroupParticipant
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.GroupParticipant} GroupParticipant
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GroupParticipant.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GroupParticipant message.
         * @function verify
         * @memberof proto.GroupParticipant
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GroupParticipant.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.userJid != null && Object.hasOwnProperty.call(message, "userJid")) {
                properties._userJid = 1;
                if (!$util.isString(message.userJid))
                    return "userJid: string expected";
            }
            if (message.rank != null && Object.hasOwnProperty.call(message, "rank")) {
                properties._rank = 1;
                if (typeof message.rank !== "number" || (message.rank | 0) !== message.rank)
                    return "rank: enum value expected";
            }
            if (message.memberLabel != null && Object.hasOwnProperty.call(message, "memberLabel")) {
                properties._memberLabel = 1;
                {
                    var error = $root.proto.MemberLabel.verify(message.memberLabel, q + 1);
                    if (error)
                        return "memberLabel." + error;
                }
            }
            return null;
        };

        /**
         * Creates a GroupParticipant message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.GroupParticipant
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.GroupParticipant} GroupParticipant
         */
        GroupParticipant.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.GroupParticipant: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.userJid != null)
                message.userJid = String(object.userJid);
            switch (object.rank) {
            case "REGULAR":
            case 0:
                message.rank = 0;
                break;
            case "ADMIN":
            case 1:
                message.rank = 1;
                break;
            case "SUPERADMIN":
            case 2:
                message.rank = 2;
                break;
            default:
                if (typeof object.rank === "number" && (object.rank | 0) === object.rank)
                    message.rank = object.rank;
            }
            if (object.memberLabel != null) {
                if (!$util.isObject(object.memberLabel))
                    throw TypeError(".proto.GroupParticipant.memberLabel: object expected");
                message.memberLabel = $root.proto.MemberLabel.fromObject(object.memberLabel, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a GroupParticipant message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.GroupParticipant
         * @static
         * @param {proto.GroupParticipant} message GroupParticipant
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GroupParticipant.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.userJid != null && Object.hasOwnProperty.call(message, "userJid"))
                object.userJid = message.userJid;
            if (message.rank != null && Object.hasOwnProperty.call(message, "rank"))
                object.rank = options.enums === String ? $root.proto.GroupParticipant.Rank[message.rank] === undefined ? message.rank : $root.proto.GroupParticipant.Rank[message.rank] : message.rank;
            if (message.memberLabel != null && Object.hasOwnProperty.call(message, "memberLabel"))
                object.memberLabel = $root.proto.MemberLabel.toObject(message.memberLabel, options, q + 1);
            return object;
        };

        /**
         * Converts this GroupParticipant to JSON.
         * @function toJSON
         * @memberof proto.GroupParticipant
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GroupParticipant.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GroupParticipant
         * @function getTypeUrl
         * @memberof proto.GroupParticipant
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GroupParticipant.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.GroupParticipant";
        };

        /**
         * Rank enum.
         * @name proto.GroupParticipant.Rank
         * @enum {number}
         * @property {number} REGULAR=0 REGULAR value
         * @property {number} ADMIN=1 ADMIN value
         * @property {number} SUPERADMIN=2 SUPERADMIN value
         */
        GroupParticipant.Rank = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "REGULAR"] = 0;
            values[valuesById[1] = "ADMIN"] = 1;
            values[valuesById[2] = "SUPERADMIN"] = 2;
            return values;
        })();

        return GroupParticipant;
    })();

    proto.GroupRootKeyShare = (function() {

        /**
         * Properties of a GroupRootKeyShare.
         * @memberof proto
         * @interface IGroupRootKeyShare
         * @property {Array.<proto.IGroupRootKeyShareEntry>|null} [keys] GroupRootKeyShare keys
         */

        /**
         * Constructs a new GroupRootKeyShare.
         * @memberof proto
         * @classdesc Represents a GroupRootKeyShare.
         * @implements IGroupRootKeyShare
         * @constructor
         * @param {proto.IGroupRootKeyShare=} [properties] Properties to set
         */
        function GroupRootKeyShare(properties) {
            this.keys = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GroupRootKeyShare keys.
         * @member {Array.<proto.IGroupRootKeyShareEntry>} keys
         * @memberof proto.GroupRootKeyShare
         * @instance
         */
        GroupRootKeyShare.prototype.keys = $util.emptyArray;

        /**
         * Creates a new GroupRootKeyShare instance using the specified properties.
         * @function create
         * @memberof proto.GroupRootKeyShare
         * @static
         * @param {proto.IGroupRootKeyShare=} [properties] Properties to set
         * @returns {proto.GroupRootKeyShare} GroupRootKeyShare instance
         */
        GroupRootKeyShare.create = function create(properties) {
            return new GroupRootKeyShare(properties);
        };

        /**
         * Encodes the specified GroupRootKeyShare message. Does not implicitly {@link proto.GroupRootKeyShare.verify|verify} messages.
         * @function encode
         * @memberof proto.GroupRootKeyShare
         * @static
         * @param {proto.IGroupRootKeyShare} message GroupRootKeyShare message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GroupRootKeyShare.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.keys != null && message.keys.length)
                for (var i = 0; i < message.keys.length; ++i)
                    $root.proto.GroupRootKeyShareEntry.encode(message.keys[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GroupRootKeyShare message, length delimited. Does not implicitly {@link proto.GroupRootKeyShare.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.GroupRootKeyShare
         * @static
         * @param {proto.IGroupRootKeyShare} message GroupRootKeyShare message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GroupRootKeyShare.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GroupRootKeyShare message from the specified reader or buffer.
         * @function decode
         * @memberof proto.GroupRootKeyShare
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.GroupRootKeyShare} GroupRootKeyShare
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GroupRootKeyShare.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(message.keys && message.keys.length))
                            message.keys = [];
                        message.keys.push($root.proto.GroupRootKeyShareEntry.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a GroupRootKeyShare message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.GroupRootKeyShare
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.GroupRootKeyShare} GroupRootKeyShare
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GroupRootKeyShare.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GroupRootKeyShare message.
         * @function verify
         * @memberof proto.GroupRootKeyShare
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GroupRootKeyShare.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            if (message.keys != null && Object.hasOwnProperty.call(message, "keys")) {
                if (!Array.isArray(message.keys))
                    return "keys: array expected";
                for (var i = 0; i < message.keys.length; ++i) {
                    var error = $root.proto.GroupRootKeyShareEntry.verify(message.keys[i], q + 1);
                    if (error)
                        return "keys." + error;
                }
            }
            return null;
        };

        /**
         * Creates a GroupRootKeyShare message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.GroupRootKeyShare
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.GroupRootKeyShare} GroupRootKeyShare
         */
        GroupRootKeyShare.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.GroupRootKeyShare: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.keys) {
                if (!Array.isArray(object.keys))
                    throw TypeError(".proto.GroupRootKeyShare.keys: array expected");
                message.keys = Array(object.keys.length);
                for (var i = 0; i < object.keys.length; ++i) {
                    if (!$util.isObject(object.keys[i]))
                        throw TypeError(".proto.GroupRootKeyShare.keys: object expected");
                    message.keys[i] = $root.proto.GroupRootKeyShareEntry.fromObject(object.keys[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GroupRootKeyShare message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.GroupRootKeyShare
         * @static
         * @param {proto.GroupRootKeyShare} message GroupRootKeyShare
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GroupRootKeyShare.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.keys = [];
            if (message.keys && message.keys.length) {
                object.keys = Array(message.keys.length);
                for (var j = 0; j < message.keys.length; ++j)
                    object.keys[j] = $root.proto.GroupRootKeyShareEntry.toObject(message.keys[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this GroupRootKeyShare to JSON.
         * @function toJSON
         * @memberof proto.GroupRootKeyShare
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GroupRootKeyShare.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GroupRootKeyShare
         * @function getTypeUrl
         * @memberof proto.GroupRootKeyShare
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GroupRootKeyShare.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.GroupRootKeyShare";
        };

        return GroupRootKeyShare;
    })();

    proto.GroupRootKeyShareEntry = (function() {

        /**
         * Properties of a GroupRootKeyShareEntry.
         * @memberof proto
         * @interface IGroupRootKeyShareEntry
         * @property {Uint8Array|null} [groupRootKey] GroupRootKeyShareEntry groupRootKey
         * @property {string|null} [keyId] GroupRootKeyShareEntry keyId
         * @property {number|Long|null} [expiryTimestampMs] GroupRootKeyShareEntry expiryTimestampMs
         * @property {number|Long|null} [createdTimestampMs] GroupRootKeyShareEntry createdTimestampMs
         */

        /**
         * Constructs a new GroupRootKeyShareEntry.
         * @memberof proto
         * @classdesc Represents a GroupRootKeyShareEntry.
         * @implements IGroupRootKeyShareEntry
         * @constructor
         * @param {proto.IGroupRootKeyShareEntry=} [properties] Properties to set
         */
        function GroupRootKeyShareEntry(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GroupRootKeyShareEntry groupRootKey.
         * @member {Uint8Array|null|undefined} groupRootKey
         * @memberof proto.GroupRootKeyShareEntry
         * @instance
         */
        GroupRootKeyShareEntry.prototype.groupRootKey = null;

        /**
         * GroupRootKeyShareEntry keyId.
         * @member {string|null|undefined} keyId
         * @memberof proto.GroupRootKeyShareEntry
         * @instance
         */
        GroupRootKeyShareEntry.prototype.keyId = null;

        /**
         * GroupRootKeyShareEntry expiryTimestampMs.
         * @member {number|Long|null|undefined} expiryTimestampMs
         * @memberof proto.GroupRootKeyShareEntry
         * @instance
         */
        GroupRootKeyShareEntry.prototype.expiryTimestampMs = null;

        /**
         * GroupRootKeyShareEntry createdTimestampMs.
         * @member {number|Long|null|undefined} createdTimestampMs
         * @memberof proto.GroupRootKeyShareEntry
         * @instance
         */
        GroupRootKeyShareEntry.prototype.createdTimestampMs = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupRootKeyShareEntry.prototype, "_groupRootKey", {
            get: $util.oneOfGetter($oneOfFields = ["groupRootKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupRootKeyShareEntry.prototype, "_keyId", {
            get: $util.oneOfGetter($oneOfFields = ["keyId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupRootKeyShareEntry.prototype, "_expiryTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["expiryTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupRootKeyShareEntry.prototype, "_createdTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["createdTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new GroupRootKeyShareEntry instance using the specified properties.
         * @function create
         * @memberof proto.GroupRootKeyShareEntry
         * @static
         * @param {proto.IGroupRootKeyShareEntry=} [properties] Properties to set
         * @returns {proto.GroupRootKeyShareEntry} GroupRootKeyShareEntry instance
         */
        GroupRootKeyShareEntry.create = function create(properties) {
            return new GroupRootKeyShareEntry(properties);
        };

        /**
         * Encodes the specified GroupRootKeyShareEntry message. Does not implicitly {@link proto.GroupRootKeyShareEntry.verify|verify} messages.
         * @function encode
         * @memberof proto.GroupRootKeyShareEntry
         * @static
         * @param {proto.IGroupRootKeyShareEntry} message GroupRootKeyShareEntry message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GroupRootKeyShareEntry.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.groupRootKey != null && Object.hasOwnProperty.call(message, "groupRootKey"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.groupRootKey);
            if (message.keyId != null && Object.hasOwnProperty.call(message, "keyId"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.keyId);
            if (message.expiryTimestampMs != null && Object.hasOwnProperty.call(message, "expiryTimestampMs"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.expiryTimestampMs);
            if (message.createdTimestampMs != null && Object.hasOwnProperty.call(message, "createdTimestampMs"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.createdTimestampMs);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified GroupRootKeyShareEntry message, length delimited. Does not implicitly {@link proto.GroupRootKeyShareEntry.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.GroupRootKeyShareEntry
         * @static
         * @param {proto.IGroupRootKeyShareEntry} message GroupRootKeyShareEntry message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GroupRootKeyShareEntry.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GroupRootKeyShareEntry message from the specified reader or buffer.
         * @function decode
         * @memberof proto.GroupRootKeyShareEntry
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.GroupRootKeyShareEntry} GroupRootKeyShareEntry
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GroupRootKeyShareEntry.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.groupRootKey = reader.bytes();
                        message._groupRootKey = "groupRootKey";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.keyId = reader.stringVerify();
                        message._keyId = "keyId";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.expiryTimestampMs = reader.int64();
                        message._expiryTimestampMs = "expiryTimestampMs";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.createdTimestampMs = reader.int64();
                        message._createdTimestampMs = "createdTimestampMs";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a GroupRootKeyShareEntry message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.GroupRootKeyShareEntry
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.GroupRootKeyShareEntry} GroupRootKeyShareEntry
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GroupRootKeyShareEntry.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GroupRootKeyShareEntry message.
         * @function verify
         * @memberof proto.GroupRootKeyShareEntry
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GroupRootKeyShareEntry.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.groupRootKey != null && Object.hasOwnProperty.call(message, "groupRootKey")) {
                properties._groupRootKey = 1;
                if (!(message.groupRootKey && typeof message.groupRootKey.length === "number" || $util.isString(message.groupRootKey)))
                    return "groupRootKey: buffer expected";
            }
            if (message.keyId != null && Object.hasOwnProperty.call(message, "keyId")) {
                properties._keyId = 1;
                if (!$util.isString(message.keyId))
                    return "keyId: string expected";
            }
            if (message.expiryTimestampMs != null && Object.hasOwnProperty.call(message, "expiryTimestampMs")) {
                properties._expiryTimestampMs = 1;
                if (!$util.isInteger(message.expiryTimestampMs) && !(message.expiryTimestampMs && $util.isInteger(message.expiryTimestampMs.low) && $util.isInteger(message.expiryTimestampMs.high)))
                    return "expiryTimestampMs: integer|Long expected";
            }
            if (message.createdTimestampMs != null && Object.hasOwnProperty.call(message, "createdTimestampMs")) {
                properties._createdTimestampMs = 1;
                if (!$util.isInteger(message.createdTimestampMs) && !(message.createdTimestampMs && $util.isInteger(message.createdTimestampMs.low) && $util.isInteger(message.createdTimestampMs.high)))
                    return "createdTimestampMs: integer|Long expected";
            }
            return null;
        };

        /**
         * Creates a GroupRootKeyShareEntry message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.GroupRootKeyShareEntry
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.GroupRootKeyShareEntry} GroupRootKeyShareEntry
         */
        GroupRootKeyShareEntry.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.GroupRootKeyShareEntry: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.groupRootKey != null)
                if (typeof object.groupRootKey === "string")
                    $util.base64.decode(object.groupRootKey, message.groupRootKey = $util.newBuffer($util.base64.length(object.groupRootKey)), 0);
                else if (object.groupRootKey.length >= 0)
                    message.groupRootKey = object.groupRootKey;
            if (object.keyId != null)
                message.keyId = String(object.keyId);
            if (object.expiryTimestampMs != null)
                if ($util.Long)
                    message.expiryTimestampMs = $util.Long.fromValue(object.expiryTimestampMs, false);
                else if (typeof object.expiryTimestampMs === "string")
                    message.expiryTimestampMs = parseInt(object.expiryTimestampMs, 10);
                else if (typeof object.expiryTimestampMs === "number")
                    message.expiryTimestampMs = object.expiryTimestampMs;
                else if (typeof object.expiryTimestampMs === "object")
                    message.expiryTimestampMs = new $util.LongBits(object.expiryTimestampMs.low >>> 0, object.expiryTimestampMs.high >>> 0).toNumber();
            if (object.createdTimestampMs != null)
                if ($util.Long)
                    message.createdTimestampMs = $util.Long.fromValue(object.createdTimestampMs, false);
                else if (typeof object.createdTimestampMs === "string")
                    message.createdTimestampMs = parseInt(object.createdTimestampMs, 10);
                else if (typeof object.createdTimestampMs === "number")
                    message.createdTimestampMs = object.createdTimestampMs;
                else if (typeof object.createdTimestampMs === "object")
                    message.createdTimestampMs = new $util.LongBits(object.createdTimestampMs.low >>> 0, object.createdTimestampMs.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a GroupRootKeyShareEntry message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.GroupRootKeyShareEntry
         * @static
         * @param {proto.GroupRootKeyShareEntry} message GroupRootKeyShareEntry
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GroupRootKeyShareEntry.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.groupRootKey != null && Object.hasOwnProperty.call(message, "groupRootKey"))
                object.groupRootKey = options.bytes === String ? $util.base64.encode(message.groupRootKey, 0, message.groupRootKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.groupRootKey) : message.groupRootKey;
            if (message.keyId != null && Object.hasOwnProperty.call(message, "keyId"))
                object.keyId = message.keyId;
            if (message.expiryTimestampMs != null && Object.hasOwnProperty.call(message, "expiryTimestampMs"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.expiryTimestampMs = typeof message.expiryTimestampMs === "number" ? BigInt(message.expiryTimestampMs) : $util.Long.fromBits(message.expiryTimestampMs.low >>> 0, message.expiryTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof message.expiryTimestampMs === "number")
                    object.expiryTimestampMs = options.longs === String ? String(message.expiryTimestampMs) : message.expiryTimestampMs;
                else
                    object.expiryTimestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.expiryTimestampMs) : options.longs === Number ? new $util.LongBits(message.expiryTimestampMs.low >>> 0, message.expiryTimestampMs.high >>> 0).toNumber() : message.expiryTimestampMs;
            if (message.createdTimestampMs != null && Object.hasOwnProperty.call(message, "createdTimestampMs"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.createdTimestampMs = typeof message.createdTimestampMs === "number" ? BigInt(message.createdTimestampMs) : $util.Long.fromBits(message.createdTimestampMs.low >>> 0, message.createdTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof message.createdTimestampMs === "number")
                    object.createdTimestampMs = options.longs === String ? String(message.createdTimestampMs) : message.createdTimestampMs;
                else
                    object.createdTimestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.createdTimestampMs) : options.longs === Number ? new $util.LongBits(message.createdTimestampMs.low >>> 0, message.createdTimestampMs.high >>> 0).toNumber() : message.createdTimestampMs;
            return object;
        };

        /**
         * Converts this GroupRootKeyShareEntry to JSON.
         * @function toJSON
         * @memberof proto.GroupRootKeyShareEntry
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GroupRootKeyShareEntry.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GroupRootKeyShareEntry
         * @function getTypeUrl
         * @memberof proto.GroupRootKeyShareEntry
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GroupRootKeyShareEntry.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.GroupRootKeyShareEntry";
        };

        return GroupRootKeyShareEntry;
    })();

    proto.HandshakeMessage = (function() {

        /**
         * Properties of a HandshakeMessage.
         * @memberof proto
         * @interface IHandshakeMessage
         * @property {proto.HandshakeMessage.IClientHello|null} [clientHello] HandshakeMessage clientHello
         * @property {proto.HandshakeMessage.IServerHello|null} [serverHello] HandshakeMessage serverHello
         * @property {proto.HandshakeMessage.IClientFinish|null} [clientFinish] HandshakeMessage clientFinish
         */

        /**
         * Constructs a new HandshakeMessage.
         * @memberof proto
         * @classdesc Represents a HandshakeMessage.
         * @implements IHandshakeMessage
         * @constructor
         * @param {proto.IHandshakeMessage=} [properties] Properties to set
         */
        function HandshakeMessage(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * HandshakeMessage clientHello.
         * @member {proto.HandshakeMessage.IClientHello|null|undefined} clientHello
         * @memberof proto.HandshakeMessage
         * @instance
         */
        HandshakeMessage.prototype.clientHello = null;

        /**
         * HandshakeMessage serverHello.
         * @member {proto.HandshakeMessage.IServerHello|null|undefined} serverHello
         * @memberof proto.HandshakeMessage
         * @instance
         */
        HandshakeMessage.prototype.serverHello = null;

        /**
         * HandshakeMessage clientFinish.
         * @member {proto.HandshakeMessage.IClientFinish|null|undefined} clientFinish
         * @memberof proto.HandshakeMessage
         * @instance
         */
        HandshakeMessage.prototype.clientFinish = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HandshakeMessage.prototype, "_clientHello", {
            get: $util.oneOfGetter($oneOfFields = ["clientHello"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HandshakeMessage.prototype, "_serverHello", {
            get: $util.oneOfGetter($oneOfFields = ["serverHello"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HandshakeMessage.prototype, "_clientFinish", {
            get: $util.oneOfGetter($oneOfFields = ["clientFinish"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new HandshakeMessage instance using the specified properties.
         * @function create
         * @memberof proto.HandshakeMessage
         * @static
         * @param {proto.IHandshakeMessage=} [properties] Properties to set
         * @returns {proto.HandshakeMessage} HandshakeMessage instance
         */
        HandshakeMessage.create = function create(properties) {
            return new HandshakeMessage(properties);
        };

        /**
         * Encodes the specified HandshakeMessage message. Does not implicitly {@link proto.HandshakeMessage.verify|verify} messages.
         * @function encode
         * @memberof proto.HandshakeMessage
         * @static
         * @param {proto.IHandshakeMessage} message HandshakeMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HandshakeMessage.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.clientHello != null && Object.hasOwnProperty.call(message, "clientHello"))
                $root.proto.HandshakeMessage.ClientHello.encode(message.clientHello, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.serverHello != null && Object.hasOwnProperty.call(message, "serverHello"))
                $root.proto.HandshakeMessage.ServerHello.encode(message.serverHello, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.clientFinish != null && Object.hasOwnProperty.call(message, "clientFinish"))
                $root.proto.HandshakeMessage.ClientFinish.encode(message.clientFinish, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified HandshakeMessage message, length delimited. Does not implicitly {@link proto.HandshakeMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.HandshakeMessage
         * @static
         * @param {proto.IHandshakeMessage} message HandshakeMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HandshakeMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a HandshakeMessage message from the specified reader or buffer.
         * @function decode
         * @memberof proto.HandshakeMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.HandshakeMessage} HandshakeMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HandshakeMessage.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (u !== 2)
                            break;
                        message.clientHello = $root.proto.HandshakeMessage.ClientHello.decode(reader, reader.uint32(), undefined, q + 1, message.clientHello);
                        message._clientHello = "clientHello";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.serverHello = $root.proto.HandshakeMessage.ServerHello.decode(reader, reader.uint32(), undefined, q + 1, message.serverHello);
                        message._serverHello = "serverHello";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.clientFinish = $root.proto.HandshakeMessage.ClientFinish.decode(reader, reader.uint32(), undefined, q + 1, message.clientFinish);
                        message._clientFinish = "clientFinish";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a HandshakeMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.HandshakeMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.HandshakeMessage} HandshakeMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HandshakeMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HandshakeMessage message.
         * @function verify
         * @memberof proto.HandshakeMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HandshakeMessage.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.clientHello != null && Object.hasOwnProperty.call(message, "clientHello")) {
                properties._clientHello = 1;
                {
                    var error = $root.proto.HandshakeMessage.ClientHello.verify(message.clientHello, q + 1);
                    if (error)
                        return "clientHello." + error;
                }
            }
            if (message.serverHello != null && Object.hasOwnProperty.call(message, "serverHello")) {
                properties._serverHello = 1;
                {
                    var error = $root.proto.HandshakeMessage.ServerHello.verify(message.serverHello, q + 1);
                    if (error)
                        return "serverHello." + error;
                }
            }
            if (message.clientFinish != null && Object.hasOwnProperty.call(message, "clientFinish")) {
                properties._clientFinish = 1;
                {
                    var error = $root.proto.HandshakeMessage.ClientFinish.verify(message.clientFinish, q + 1);
                    if (error)
                        return "clientFinish." + error;
                }
            }
            return null;
        };

        /**
         * Creates a HandshakeMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.HandshakeMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.HandshakeMessage} HandshakeMessage
         */
        HandshakeMessage.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.HandshakeMessage: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.clientHello != null) {
                if (!$util.isObject(object.clientHello))
                    throw TypeError(".proto.HandshakeMessage.clientHello: object expected");
                message.clientHello = $root.proto.HandshakeMessage.ClientHello.fromObject(object.clientHello, q + 1);
            }
            if (object.serverHello != null) {
                if (!$util.isObject(object.serverHello))
                    throw TypeError(".proto.HandshakeMessage.serverHello: object expected");
                message.serverHello = $root.proto.HandshakeMessage.ServerHello.fromObject(object.serverHello, q + 1);
            }
            if (object.clientFinish != null) {
                if (!$util.isObject(object.clientFinish))
                    throw TypeError(".proto.HandshakeMessage.clientFinish: object expected");
                message.clientFinish = $root.proto.HandshakeMessage.ClientFinish.fromObject(object.clientFinish, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a HandshakeMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.HandshakeMessage
         * @static
         * @param {proto.HandshakeMessage} message HandshakeMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HandshakeMessage.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.clientHello != null && Object.hasOwnProperty.call(message, "clientHello"))
                object.clientHello = $root.proto.HandshakeMessage.ClientHello.toObject(message.clientHello, options, q + 1);
            if (message.serverHello != null && Object.hasOwnProperty.call(message, "serverHello"))
                object.serverHello = $root.proto.HandshakeMessage.ServerHello.toObject(message.serverHello, options, q + 1);
            if (message.clientFinish != null && Object.hasOwnProperty.call(message, "clientFinish"))
                object.clientFinish = $root.proto.HandshakeMessage.ClientFinish.toObject(message.clientFinish, options, q + 1);
            return object;
        };

        /**
         * Converts this HandshakeMessage to JSON.
         * @function toJSON
         * @memberof proto.HandshakeMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HandshakeMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for HandshakeMessage
         * @function getTypeUrl
         * @memberof proto.HandshakeMessage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        HandshakeMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.HandshakeMessage";
        };

        HandshakeMessage.ClientFinish = (function() {

            /**
             * Properties of a ClientFinish.
             * @memberof proto.HandshakeMessage
             * @interface IClientFinish
             * @property {Uint8Array|null} ["static"] ClientFinish static
             * @property {Uint8Array|null} [payload] ClientFinish payload
             * @property {Uint8Array|null} [extendedCiphertext] ClientFinish extendedCiphertext
             * @property {Uint8Array|null} [paddedBytes] ClientFinish paddedBytes
             * @property {boolean|null} [simulateXxkemFs] ClientFinish simulateXxkemFs
             */

            /**
             * Constructs a new ClientFinish.
             * @memberof proto.HandshakeMessage
             * @classdesc Represents a ClientFinish.
             * @implements IClientFinish
             * @constructor
             * @param {proto.HandshakeMessage.IClientFinish=} [properties] Properties to set
             */
            function ClientFinish(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ClientFinish static.
             * @member {Uint8Array|null|undefined} static
             * @memberof proto.HandshakeMessage.ClientFinish
             * @instance
             */
            ClientFinish.prototype["static"] = null;

            /**
             * ClientFinish payload.
             * @member {Uint8Array|null|undefined} payload
             * @memberof proto.HandshakeMessage.ClientFinish
             * @instance
             */
            ClientFinish.prototype.payload = null;

            /**
             * ClientFinish extendedCiphertext.
             * @member {Uint8Array|null|undefined} extendedCiphertext
             * @memberof proto.HandshakeMessage.ClientFinish
             * @instance
             */
            ClientFinish.prototype.extendedCiphertext = null;

            /**
             * ClientFinish paddedBytes.
             * @member {Uint8Array|null|undefined} paddedBytes
             * @memberof proto.HandshakeMessage.ClientFinish
             * @instance
             */
            ClientFinish.prototype.paddedBytes = null;

            /**
             * ClientFinish simulateXxkemFs.
             * @member {boolean|null|undefined} simulateXxkemFs
             * @memberof proto.HandshakeMessage.ClientFinish
             * @instance
             */
            ClientFinish.prototype.simulateXxkemFs = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ClientFinish.prototype, "_static", {
                get: $util.oneOfGetter($oneOfFields = ["static"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ClientFinish.prototype, "_payload", {
                get: $util.oneOfGetter($oneOfFields = ["payload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ClientFinish.prototype, "_extendedCiphertext", {
                get: $util.oneOfGetter($oneOfFields = ["extendedCiphertext"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ClientFinish.prototype, "_paddedBytes", {
                get: $util.oneOfGetter($oneOfFields = ["paddedBytes"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ClientFinish.prototype, "_simulateXxkemFs", {
                get: $util.oneOfGetter($oneOfFields = ["simulateXxkemFs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ClientFinish instance using the specified properties.
             * @function create
             * @memberof proto.HandshakeMessage.ClientFinish
             * @static
             * @param {proto.HandshakeMessage.IClientFinish=} [properties] Properties to set
             * @returns {proto.HandshakeMessage.ClientFinish} ClientFinish instance
             */
            ClientFinish.create = function create(properties) {
                return new ClientFinish(properties);
            };

            /**
             * Encodes the specified ClientFinish message. Does not implicitly {@link proto.HandshakeMessage.ClientFinish.verify|verify} messages.
             * @function encode
             * @memberof proto.HandshakeMessage.ClientFinish
             * @static
             * @param {proto.HandshakeMessage.IClientFinish} message ClientFinish message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ClientFinish.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message["static"] != null && Object.hasOwnProperty.call(message, "static"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message["static"]);
                if (message.payload != null && Object.hasOwnProperty.call(message, "payload"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.payload);
                if (message.extendedCiphertext != null && Object.hasOwnProperty.call(message, "extendedCiphertext"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.extendedCiphertext);
                if (message.paddedBytes != null && Object.hasOwnProperty.call(message, "paddedBytes"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.paddedBytes);
                if (message.simulateXxkemFs != null && Object.hasOwnProperty.call(message, "simulateXxkemFs"))
                    writer.uint32(/* id 5, wireType 0 =*/40).bool(message.simulateXxkemFs);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ClientFinish message, length delimited. Does not implicitly {@link proto.HandshakeMessage.ClientFinish.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.HandshakeMessage.ClientFinish
             * @static
             * @param {proto.HandshakeMessage.IClientFinish} message ClientFinish message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ClientFinish.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ClientFinish message from the specified reader or buffer.
             * @function decode
             * @memberof proto.HandshakeMessage.ClientFinish
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.HandshakeMessage.ClientFinish} ClientFinish
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ClientFinish.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message["static"] = reader.bytes();
                            message._static = "static";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.payload = reader.bytes();
                            message._payload = "payload";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.extendedCiphertext = reader.bytes();
                            message._extendedCiphertext = "extendedCiphertext";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.paddedBytes = reader.bytes();
                            message._paddedBytes = "paddedBytes";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.simulateXxkemFs = reader.bool();
                            message._simulateXxkemFs = "simulateXxkemFs";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ClientFinish message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.HandshakeMessage.ClientFinish
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.HandshakeMessage.ClientFinish} ClientFinish
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ClientFinish.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ClientFinish message.
             * @function verify
             * @memberof proto.HandshakeMessage.ClientFinish
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ClientFinish.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message["static"] != null && Object.hasOwnProperty.call(message, "static")) {
                    properties._static = 1;
                    if (!(message["static"] && typeof message["static"].length === "number" || $util.isString(message["static"])))
                        return "static: buffer expected";
                }
                if (message.payload != null && Object.hasOwnProperty.call(message, "payload")) {
                    properties._payload = 1;
                    if (!(message.payload && typeof message.payload.length === "number" || $util.isString(message.payload)))
                        return "payload: buffer expected";
                }
                if (message.extendedCiphertext != null && Object.hasOwnProperty.call(message, "extendedCiphertext")) {
                    properties._extendedCiphertext = 1;
                    if (!(message.extendedCiphertext && typeof message.extendedCiphertext.length === "number" || $util.isString(message.extendedCiphertext)))
                        return "extendedCiphertext: buffer expected";
                }
                if (message.paddedBytes != null && Object.hasOwnProperty.call(message, "paddedBytes")) {
                    properties._paddedBytes = 1;
                    if (!(message.paddedBytes && typeof message.paddedBytes.length === "number" || $util.isString(message.paddedBytes)))
                        return "paddedBytes: buffer expected";
                }
                if (message.simulateXxkemFs != null && Object.hasOwnProperty.call(message, "simulateXxkemFs")) {
                    properties._simulateXxkemFs = 1;
                    if (typeof message.simulateXxkemFs !== "boolean")
                        return "simulateXxkemFs: boolean expected";
                }
                return null;
            };

            /**
             * Creates a ClientFinish message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.HandshakeMessage.ClientFinish
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.HandshakeMessage.ClientFinish} ClientFinish
             */
            ClientFinish.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.HandshakeMessage.ClientFinish: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object["static"] != null)
                    if (typeof object["static"] === "string")
                        $util.base64.decode(object["static"], message["static"] = $util.newBuffer($util.base64.length(object["static"])), 0);
                    else if (object["static"].length >= 0)
                        message["static"] = object["static"];
                if (object.payload != null)
                    if (typeof object.payload === "string")
                        $util.base64.decode(object.payload, message.payload = $util.newBuffer($util.base64.length(object.payload)), 0);
                    else if (object.payload.length >= 0)
                        message.payload = object.payload;
                if (object.extendedCiphertext != null)
                    if (typeof object.extendedCiphertext === "string")
                        $util.base64.decode(object.extendedCiphertext, message.extendedCiphertext = $util.newBuffer($util.base64.length(object.extendedCiphertext)), 0);
                    else if (object.extendedCiphertext.length >= 0)
                        message.extendedCiphertext = object.extendedCiphertext;
                if (object.paddedBytes != null)
                    if (typeof object.paddedBytes === "string")
                        $util.base64.decode(object.paddedBytes, message.paddedBytes = $util.newBuffer($util.base64.length(object.paddedBytes)), 0);
                    else if (object.paddedBytes.length >= 0)
                        message.paddedBytes = object.paddedBytes;
                if (object.simulateXxkemFs != null)
                    message.simulateXxkemFs = Boolean(object.simulateXxkemFs);
                return message;
            };

            /**
             * Creates a plain object from a ClientFinish message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.HandshakeMessage.ClientFinish
             * @static
             * @param {proto.HandshakeMessage.ClientFinish} message ClientFinish
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ClientFinish.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message["static"] != null && Object.hasOwnProperty.call(message, "static"))
                    object["static"] = options.bytes === String ? $util.base64.encode(message["static"], 0, message["static"].length) : options.bytes === Array ? Array.prototype.slice.call(message["static"]) : message["static"];
                if (message.payload != null && Object.hasOwnProperty.call(message, "payload"))
                    object.payload = options.bytes === String ? $util.base64.encode(message.payload, 0, message.payload.length) : options.bytes === Array ? Array.prototype.slice.call(message.payload) : message.payload;
                if (message.extendedCiphertext != null && Object.hasOwnProperty.call(message, "extendedCiphertext"))
                    object.extendedCiphertext = options.bytes === String ? $util.base64.encode(message.extendedCiphertext, 0, message.extendedCiphertext.length) : options.bytes === Array ? Array.prototype.slice.call(message.extendedCiphertext) : message.extendedCiphertext;
                if (message.paddedBytes != null && Object.hasOwnProperty.call(message, "paddedBytes"))
                    object.paddedBytes = options.bytes === String ? $util.base64.encode(message.paddedBytes, 0, message.paddedBytes.length) : options.bytes === Array ? Array.prototype.slice.call(message.paddedBytes) : message.paddedBytes;
                if (message.simulateXxkemFs != null && Object.hasOwnProperty.call(message, "simulateXxkemFs"))
                    object.simulateXxkemFs = message.simulateXxkemFs;
                return object;
            };

            /**
             * Converts this ClientFinish to JSON.
             * @function toJSON
             * @memberof proto.HandshakeMessage.ClientFinish
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ClientFinish.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ClientFinish
             * @function getTypeUrl
             * @memberof proto.HandshakeMessage.ClientFinish
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ClientFinish.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.HandshakeMessage.ClientFinish";
            };

            return ClientFinish;
        })();

        HandshakeMessage.ClientHello = (function() {

            /**
             * Properties of a ClientHello.
             * @memberof proto.HandshakeMessage
             * @interface IClientHello
             * @property {Uint8Array|null} [ephemeral] ClientHello ephemeral
             * @property {Uint8Array|null} ["static"] ClientHello static
             * @property {Uint8Array|null} [payload] ClientHello payload
             * @property {boolean|null} [useExtended] ClientHello useExtended
             * @property {Uint8Array|null} [extendedCiphertext] ClientHello extendedCiphertext
             * @property {Uint8Array|null} [paddedBytes] ClientHello paddedBytes
             * @property {boolean|null} [sendServerHelloPaddedBytes] ClientHello sendServerHelloPaddedBytes
             * @property {boolean|null} [simulateXxkemFs] ClientHello simulateXxkemFs
             * @property {proto.HandshakeMessage.HandshakePqMode|null} [pqMode] ClientHello pqMode
             * @property {Uint8Array|null} [extendedEphemeral] ClientHello extendedEphemeral
             */

            /**
             * Constructs a new ClientHello.
             * @memberof proto.HandshakeMessage
             * @classdesc Represents a ClientHello.
             * @implements IClientHello
             * @constructor
             * @param {proto.HandshakeMessage.IClientHello=} [properties] Properties to set
             */
            function ClientHello(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ClientHello ephemeral.
             * @member {Uint8Array|null|undefined} ephemeral
             * @memberof proto.HandshakeMessage.ClientHello
             * @instance
             */
            ClientHello.prototype.ephemeral = null;

            /**
             * ClientHello static.
             * @member {Uint8Array|null|undefined} static
             * @memberof proto.HandshakeMessage.ClientHello
             * @instance
             */
            ClientHello.prototype["static"] = null;

            /**
             * ClientHello payload.
             * @member {Uint8Array|null|undefined} payload
             * @memberof proto.HandshakeMessage.ClientHello
             * @instance
             */
            ClientHello.prototype.payload = null;

            /**
             * ClientHello useExtended.
             * @member {boolean|null|undefined} useExtended
             * @memberof proto.HandshakeMessage.ClientHello
             * @instance
             */
            ClientHello.prototype.useExtended = null;

            /**
             * ClientHello extendedCiphertext.
             * @member {Uint8Array|null|undefined} extendedCiphertext
             * @memberof proto.HandshakeMessage.ClientHello
             * @instance
             */
            ClientHello.prototype.extendedCiphertext = null;

            /**
             * ClientHello paddedBytes.
             * @member {Uint8Array|null|undefined} paddedBytes
             * @memberof proto.HandshakeMessage.ClientHello
             * @instance
             */
            ClientHello.prototype.paddedBytes = null;

            /**
             * ClientHello sendServerHelloPaddedBytes.
             * @member {boolean|null|undefined} sendServerHelloPaddedBytes
             * @memberof proto.HandshakeMessage.ClientHello
             * @instance
             */
            ClientHello.prototype.sendServerHelloPaddedBytes = null;

            /**
             * ClientHello simulateXxkemFs.
             * @member {boolean|null|undefined} simulateXxkemFs
             * @memberof proto.HandshakeMessage.ClientHello
             * @instance
             */
            ClientHello.prototype.simulateXxkemFs = null;

            /**
             * ClientHello pqMode.
             * @member {proto.HandshakeMessage.HandshakePqMode|null|undefined} pqMode
             * @memberof proto.HandshakeMessage.ClientHello
             * @instance
             */
            ClientHello.prototype.pqMode = null;

            /**
             * ClientHello extendedEphemeral.
             * @member {Uint8Array|null|undefined} extendedEphemeral
             * @memberof proto.HandshakeMessage.ClientHello
             * @instance
             */
            ClientHello.prototype.extendedEphemeral = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ClientHello.prototype, "_ephemeral", {
                get: $util.oneOfGetter($oneOfFields = ["ephemeral"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ClientHello.prototype, "_static", {
                get: $util.oneOfGetter($oneOfFields = ["static"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ClientHello.prototype, "_payload", {
                get: $util.oneOfGetter($oneOfFields = ["payload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ClientHello.prototype, "_useExtended", {
                get: $util.oneOfGetter($oneOfFields = ["useExtended"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ClientHello.prototype, "_extendedCiphertext", {
                get: $util.oneOfGetter($oneOfFields = ["extendedCiphertext"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ClientHello.prototype, "_paddedBytes", {
                get: $util.oneOfGetter($oneOfFields = ["paddedBytes"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ClientHello.prototype, "_sendServerHelloPaddedBytes", {
                get: $util.oneOfGetter($oneOfFields = ["sendServerHelloPaddedBytes"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ClientHello.prototype, "_simulateXxkemFs", {
                get: $util.oneOfGetter($oneOfFields = ["simulateXxkemFs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ClientHello.prototype, "_pqMode", {
                get: $util.oneOfGetter($oneOfFields = ["pqMode"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ClientHello.prototype, "_extendedEphemeral", {
                get: $util.oneOfGetter($oneOfFields = ["extendedEphemeral"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ClientHello instance using the specified properties.
             * @function create
             * @memberof proto.HandshakeMessage.ClientHello
             * @static
             * @param {proto.HandshakeMessage.IClientHello=} [properties] Properties to set
             * @returns {proto.HandshakeMessage.ClientHello} ClientHello instance
             */
            ClientHello.create = function create(properties) {
                return new ClientHello(properties);
            };

            /**
             * Encodes the specified ClientHello message. Does not implicitly {@link proto.HandshakeMessage.ClientHello.verify|verify} messages.
             * @function encode
             * @memberof proto.HandshakeMessage.ClientHello
             * @static
             * @param {proto.HandshakeMessage.IClientHello} message ClientHello message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ClientHello.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.ephemeral != null && Object.hasOwnProperty.call(message, "ephemeral"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.ephemeral);
                if (message["static"] != null && Object.hasOwnProperty.call(message, "static"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message["static"]);
                if (message.payload != null && Object.hasOwnProperty.call(message, "payload"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.payload);
                if (message.useExtended != null && Object.hasOwnProperty.call(message, "useExtended"))
                    writer.uint32(/* id 4, wireType 0 =*/32).bool(message.useExtended);
                if (message.extendedCiphertext != null && Object.hasOwnProperty.call(message, "extendedCiphertext"))
                    writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.extendedCiphertext);
                if (message.paddedBytes != null && Object.hasOwnProperty.call(message, "paddedBytes"))
                    writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.paddedBytes);
                if (message.sendServerHelloPaddedBytes != null && Object.hasOwnProperty.call(message, "sendServerHelloPaddedBytes"))
                    writer.uint32(/* id 7, wireType 0 =*/56).bool(message.sendServerHelloPaddedBytes);
                if (message.simulateXxkemFs != null && Object.hasOwnProperty.call(message, "simulateXxkemFs"))
                    writer.uint32(/* id 8, wireType 0 =*/64).bool(message.simulateXxkemFs);
                if (message.pqMode != null && Object.hasOwnProperty.call(message, "pqMode"))
                    writer.uint32(/* id 9, wireType 0 =*/72).int32(message.pqMode);
                if (message.extendedEphemeral != null && Object.hasOwnProperty.call(message, "extendedEphemeral"))
                    writer.uint32(/* id 10, wireType 2 =*/82).bytes(message.extendedEphemeral);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ClientHello message, length delimited. Does not implicitly {@link proto.HandshakeMessage.ClientHello.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.HandshakeMessage.ClientHello
             * @static
             * @param {proto.HandshakeMessage.IClientHello} message ClientHello message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ClientHello.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ClientHello message from the specified reader or buffer.
             * @function decode
             * @memberof proto.HandshakeMessage.ClientHello
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.HandshakeMessage.ClientHello} ClientHello
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ClientHello.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.ephemeral = reader.bytes();
                            message._ephemeral = "ephemeral";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message["static"] = reader.bytes();
                            message._static = "static";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.payload = reader.bytes();
                            message._payload = "payload";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.useExtended = reader.bool();
                            message._useExtended = "useExtended";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.extendedCiphertext = reader.bytes();
                            message._extendedCiphertext = "extendedCiphertext";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.paddedBytes = reader.bytes();
                            message._paddedBytes = "paddedBytes";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            message.sendServerHelloPaddedBytes = reader.bool();
                            message._sendServerHelloPaddedBytes = "sendServerHelloPaddedBytes";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            message.simulateXxkemFs = reader.bool();
                            message._simulateXxkemFs = "simulateXxkemFs";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            message.pqMode = reader.int32();
                            message._pqMode = "pqMode";
                            continue;
                        }
                    case 10: {
                            if (u !== 2)
                                break;
                            message.extendedEphemeral = reader.bytes();
                            message._extendedEphemeral = "extendedEphemeral";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ClientHello message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.HandshakeMessage.ClientHello
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.HandshakeMessage.ClientHello} ClientHello
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ClientHello.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ClientHello message.
             * @function verify
             * @memberof proto.HandshakeMessage.ClientHello
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ClientHello.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.ephemeral != null && Object.hasOwnProperty.call(message, "ephemeral")) {
                    properties._ephemeral = 1;
                    if (!(message.ephemeral && typeof message.ephemeral.length === "number" || $util.isString(message.ephemeral)))
                        return "ephemeral: buffer expected";
                }
                if (message["static"] != null && Object.hasOwnProperty.call(message, "static")) {
                    properties._static = 1;
                    if (!(message["static"] && typeof message["static"].length === "number" || $util.isString(message["static"])))
                        return "static: buffer expected";
                }
                if (message.payload != null && Object.hasOwnProperty.call(message, "payload")) {
                    properties._payload = 1;
                    if (!(message.payload && typeof message.payload.length === "number" || $util.isString(message.payload)))
                        return "payload: buffer expected";
                }
                if (message.useExtended != null && Object.hasOwnProperty.call(message, "useExtended")) {
                    properties._useExtended = 1;
                    if (typeof message.useExtended !== "boolean")
                        return "useExtended: boolean expected";
                }
                if (message.extendedCiphertext != null && Object.hasOwnProperty.call(message, "extendedCiphertext")) {
                    properties._extendedCiphertext = 1;
                    if (!(message.extendedCiphertext && typeof message.extendedCiphertext.length === "number" || $util.isString(message.extendedCiphertext)))
                        return "extendedCiphertext: buffer expected";
                }
                if (message.paddedBytes != null && Object.hasOwnProperty.call(message, "paddedBytes")) {
                    properties._paddedBytes = 1;
                    if (!(message.paddedBytes && typeof message.paddedBytes.length === "number" || $util.isString(message.paddedBytes)))
                        return "paddedBytes: buffer expected";
                }
                if (message.sendServerHelloPaddedBytes != null && Object.hasOwnProperty.call(message, "sendServerHelloPaddedBytes")) {
                    properties._sendServerHelloPaddedBytes = 1;
                    if (typeof message.sendServerHelloPaddedBytes !== "boolean")
                        return "sendServerHelloPaddedBytes: boolean expected";
                }
                if (message.simulateXxkemFs != null && Object.hasOwnProperty.call(message, "simulateXxkemFs")) {
                    properties._simulateXxkemFs = 1;
                    if (typeof message.simulateXxkemFs !== "boolean")
                        return "simulateXxkemFs: boolean expected";
                }
                if (message.pqMode != null && Object.hasOwnProperty.call(message, "pqMode")) {
                    properties._pqMode = 1;
                    if (typeof message.pqMode !== "number" || (message.pqMode | 0) !== message.pqMode)
                        return "pqMode: enum value expected";
                }
                if (message.extendedEphemeral != null && Object.hasOwnProperty.call(message, "extendedEphemeral")) {
                    properties._extendedEphemeral = 1;
                    if (!(message.extendedEphemeral && typeof message.extendedEphemeral.length === "number" || $util.isString(message.extendedEphemeral)))
                        return "extendedEphemeral: buffer expected";
                }
                return null;
            };

            /**
             * Creates a ClientHello message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.HandshakeMessage.ClientHello
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.HandshakeMessage.ClientHello} ClientHello
             */
            ClientHello.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.HandshakeMessage.ClientHello: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.ephemeral != null)
                    if (typeof object.ephemeral === "string")
                        $util.base64.decode(object.ephemeral, message.ephemeral = $util.newBuffer($util.base64.length(object.ephemeral)), 0);
                    else if (object.ephemeral.length >= 0)
                        message.ephemeral = object.ephemeral;
                if (object["static"] != null)
                    if (typeof object["static"] === "string")
                        $util.base64.decode(object["static"], message["static"] = $util.newBuffer($util.base64.length(object["static"])), 0);
                    else if (object["static"].length >= 0)
                        message["static"] = object["static"];
                if (object.payload != null)
                    if (typeof object.payload === "string")
                        $util.base64.decode(object.payload, message.payload = $util.newBuffer($util.base64.length(object.payload)), 0);
                    else if (object.payload.length >= 0)
                        message.payload = object.payload;
                if (object.useExtended != null)
                    message.useExtended = Boolean(object.useExtended);
                if (object.extendedCiphertext != null)
                    if (typeof object.extendedCiphertext === "string")
                        $util.base64.decode(object.extendedCiphertext, message.extendedCiphertext = $util.newBuffer($util.base64.length(object.extendedCiphertext)), 0);
                    else if (object.extendedCiphertext.length >= 0)
                        message.extendedCiphertext = object.extendedCiphertext;
                if (object.paddedBytes != null)
                    if (typeof object.paddedBytes === "string")
                        $util.base64.decode(object.paddedBytes, message.paddedBytes = $util.newBuffer($util.base64.length(object.paddedBytes)), 0);
                    else if (object.paddedBytes.length >= 0)
                        message.paddedBytes = object.paddedBytes;
                if (object.sendServerHelloPaddedBytes != null)
                    message.sendServerHelloPaddedBytes = Boolean(object.sendServerHelloPaddedBytes);
                if (object.simulateXxkemFs != null)
                    message.simulateXxkemFs = Boolean(object.simulateXxkemFs);
                switch (object.pqMode) {
                case "HANDSHAKE_PQ_MODE_UNKNOWN":
                case 0:
                    message.pqMode = 0;
                    break;
                case "XXKEM":
                case 1:
                    message.pqMode = 1;
                    break;
                case "XXKEM_FS":
                case 2:
                    message.pqMode = 2;
                    break;
                case "WA_CLASSICAL":
                case 3:
                    message.pqMode = 3;
                    break;
                case "WA_PQ":
                case 4:
                    message.pqMode = 4;
                    break;
                case "IKKEM":
                case 5:
                    message.pqMode = 5;
                    break;
                case "IKKEM_FS":
                case 6:
                    message.pqMode = 6;
                    break;
                case "XXKEM_2":
                case 7:
                    message.pqMode = 7;
                    break;
                case "IKKEM_2":
                case 8:
                    message.pqMode = 8;
                    break;
                default:
                    if (typeof object.pqMode === "number" && (object.pqMode | 0) === object.pqMode)
                        message.pqMode = object.pqMode;
                }
                if (object.extendedEphemeral != null)
                    if (typeof object.extendedEphemeral === "string")
                        $util.base64.decode(object.extendedEphemeral, message.extendedEphemeral = $util.newBuffer($util.base64.length(object.extendedEphemeral)), 0);
                    else if (object.extendedEphemeral.length >= 0)
                        message.extendedEphemeral = object.extendedEphemeral;
                return message;
            };

            /**
             * Creates a plain object from a ClientHello message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.HandshakeMessage.ClientHello
             * @static
             * @param {proto.HandshakeMessage.ClientHello} message ClientHello
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ClientHello.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.ephemeral != null && Object.hasOwnProperty.call(message, "ephemeral"))
                    object.ephemeral = options.bytes === String ? $util.base64.encode(message.ephemeral, 0, message.ephemeral.length) : options.bytes === Array ? Array.prototype.slice.call(message.ephemeral) : message.ephemeral;
                if (message["static"] != null && Object.hasOwnProperty.call(message, "static"))
                    object["static"] = options.bytes === String ? $util.base64.encode(message["static"], 0, message["static"].length) : options.bytes === Array ? Array.prototype.slice.call(message["static"]) : message["static"];
                if (message.payload != null && Object.hasOwnProperty.call(message, "payload"))
                    object.payload = options.bytes === String ? $util.base64.encode(message.payload, 0, message.payload.length) : options.bytes === Array ? Array.prototype.slice.call(message.payload) : message.payload;
                if (message.useExtended != null && Object.hasOwnProperty.call(message, "useExtended"))
                    object.useExtended = message.useExtended;
                if (message.extendedCiphertext != null && Object.hasOwnProperty.call(message, "extendedCiphertext"))
                    object.extendedCiphertext = options.bytes === String ? $util.base64.encode(message.extendedCiphertext, 0, message.extendedCiphertext.length) : options.bytes === Array ? Array.prototype.slice.call(message.extendedCiphertext) : message.extendedCiphertext;
                if (message.paddedBytes != null && Object.hasOwnProperty.call(message, "paddedBytes"))
                    object.paddedBytes = options.bytes === String ? $util.base64.encode(message.paddedBytes, 0, message.paddedBytes.length) : options.bytes === Array ? Array.prototype.slice.call(message.paddedBytes) : message.paddedBytes;
                if (message.sendServerHelloPaddedBytes != null && Object.hasOwnProperty.call(message, "sendServerHelloPaddedBytes"))
                    object.sendServerHelloPaddedBytes = message.sendServerHelloPaddedBytes;
                if (message.simulateXxkemFs != null && Object.hasOwnProperty.call(message, "simulateXxkemFs"))
                    object.simulateXxkemFs = message.simulateXxkemFs;
                if (message.pqMode != null && Object.hasOwnProperty.call(message, "pqMode"))
                    object.pqMode = options.enums === String ? $root.proto.HandshakeMessage.HandshakePqMode[message.pqMode] === undefined ? message.pqMode : $root.proto.HandshakeMessage.HandshakePqMode[message.pqMode] : message.pqMode;
                if (message.extendedEphemeral != null && Object.hasOwnProperty.call(message, "extendedEphemeral"))
                    object.extendedEphemeral = options.bytes === String ? $util.base64.encode(message.extendedEphemeral, 0, message.extendedEphemeral.length) : options.bytes === Array ? Array.prototype.slice.call(message.extendedEphemeral) : message.extendedEphemeral;
                return object;
            };

            /**
             * Converts this ClientHello to JSON.
             * @function toJSON
             * @memberof proto.HandshakeMessage.ClientHello
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ClientHello.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ClientHello
             * @function getTypeUrl
             * @memberof proto.HandshakeMessage.ClientHello
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ClientHello.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.HandshakeMessage.ClientHello";
            };

            return ClientHello;
        })();

        /**
         * HandshakePqMode enum.
         * @name proto.HandshakeMessage.HandshakePqMode
         * @enum {number}
         * @property {number} HANDSHAKE_PQ_MODE_UNKNOWN=0 HANDSHAKE_PQ_MODE_UNKNOWN value
         * @property {number} XXKEM=1 XXKEM value
         * @property {number} XXKEM_FS=2 XXKEM_FS value
         * @property {number} WA_CLASSICAL=3 WA_CLASSICAL value
         * @property {number} WA_PQ=4 WA_PQ value
         * @property {number} IKKEM=5 IKKEM value
         * @property {number} IKKEM_FS=6 IKKEM_FS value
         * @property {number} XXKEM_2=7 XXKEM_2 value
         * @property {number} IKKEM_2=8 IKKEM_2 value
         */
        HandshakeMessage.HandshakePqMode = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "HANDSHAKE_PQ_MODE_UNKNOWN"] = 0;
            values[valuesById[1] = "XXKEM"] = 1;
            values[valuesById[2] = "XXKEM_FS"] = 2;
            values[valuesById[3] = "WA_CLASSICAL"] = 3;
            values[valuesById[4] = "WA_PQ"] = 4;
            values[valuesById[5] = "IKKEM"] = 5;
            values[valuesById[6] = "IKKEM_FS"] = 6;
            values[valuesById[7] = "XXKEM_2"] = 7;
            values[valuesById[8] = "IKKEM_2"] = 8;
            return values;
        })();

        HandshakeMessage.ServerHello = (function() {

            /**
             * Properties of a ServerHello.
             * @memberof proto.HandshakeMessage
             * @interface IServerHello
             * @property {Uint8Array|null} [ephemeral] ServerHello ephemeral
             * @property {Uint8Array|null} ["static"] ServerHello static
             * @property {Uint8Array|null} [payload] ServerHello payload
             * @property {Uint8Array|null} [extendedStatic] ServerHello extendedStatic
             * @property {Uint8Array|null} [paddingBytes] ServerHello paddingBytes
             * @property {Uint8Array|null} [extendedCiphertext] ServerHello extendedCiphertext
             */

            /**
             * Constructs a new ServerHello.
             * @memberof proto.HandshakeMessage
             * @classdesc Represents a ServerHello.
             * @implements IServerHello
             * @constructor
             * @param {proto.HandshakeMessage.IServerHello=} [properties] Properties to set
             */
            function ServerHello(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ServerHello ephemeral.
             * @member {Uint8Array|null|undefined} ephemeral
             * @memberof proto.HandshakeMessage.ServerHello
             * @instance
             */
            ServerHello.prototype.ephemeral = null;

            /**
             * ServerHello static.
             * @member {Uint8Array|null|undefined} static
             * @memberof proto.HandshakeMessage.ServerHello
             * @instance
             */
            ServerHello.prototype["static"] = null;

            /**
             * ServerHello payload.
             * @member {Uint8Array|null|undefined} payload
             * @memberof proto.HandshakeMessage.ServerHello
             * @instance
             */
            ServerHello.prototype.payload = null;

            /**
             * ServerHello extendedStatic.
             * @member {Uint8Array|null|undefined} extendedStatic
             * @memberof proto.HandshakeMessage.ServerHello
             * @instance
             */
            ServerHello.prototype.extendedStatic = null;

            /**
             * ServerHello paddingBytes.
             * @member {Uint8Array|null|undefined} paddingBytes
             * @memberof proto.HandshakeMessage.ServerHello
             * @instance
             */
            ServerHello.prototype.paddingBytes = null;

            /**
             * ServerHello extendedCiphertext.
             * @member {Uint8Array|null|undefined} extendedCiphertext
             * @memberof proto.HandshakeMessage.ServerHello
             * @instance
             */
            ServerHello.prototype.extendedCiphertext = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ServerHello.prototype, "_ephemeral", {
                get: $util.oneOfGetter($oneOfFields = ["ephemeral"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ServerHello.prototype, "_static", {
                get: $util.oneOfGetter($oneOfFields = ["static"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ServerHello.prototype, "_payload", {
                get: $util.oneOfGetter($oneOfFields = ["payload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ServerHello.prototype, "_extendedStatic", {
                get: $util.oneOfGetter($oneOfFields = ["extendedStatic"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ServerHello.prototype, "_paddingBytes", {
                get: $util.oneOfGetter($oneOfFields = ["paddingBytes"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ServerHello.prototype, "_extendedCiphertext", {
                get: $util.oneOfGetter($oneOfFields = ["extendedCiphertext"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ServerHello instance using the specified properties.
             * @function create
             * @memberof proto.HandshakeMessage.ServerHello
             * @static
             * @param {proto.HandshakeMessage.IServerHello=} [properties] Properties to set
             * @returns {proto.HandshakeMessage.ServerHello} ServerHello instance
             */
            ServerHello.create = function create(properties) {
                return new ServerHello(properties);
            };

            /**
             * Encodes the specified ServerHello message. Does not implicitly {@link proto.HandshakeMessage.ServerHello.verify|verify} messages.
             * @function encode
             * @memberof proto.HandshakeMessage.ServerHello
             * @static
             * @param {proto.HandshakeMessage.IServerHello} message ServerHello message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ServerHello.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.ephemeral != null && Object.hasOwnProperty.call(message, "ephemeral"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.ephemeral);
                if (message["static"] != null && Object.hasOwnProperty.call(message, "static"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message["static"]);
                if (message.payload != null && Object.hasOwnProperty.call(message, "payload"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.payload);
                if (message.extendedStatic != null && Object.hasOwnProperty.call(message, "extendedStatic"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.extendedStatic);
                if (message.paddingBytes != null && Object.hasOwnProperty.call(message, "paddingBytes"))
                    writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.paddingBytes);
                if (message.extendedCiphertext != null && Object.hasOwnProperty.call(message, "extendedCiphertext"))
                    writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.extendedCiphertext);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ServerHello message, length delimited. Does not implicitly {@link proto.HandshakeMessage.ServerHello.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.HandshakeMessage.ServerHello
             * @static
             * @param {proto.HandshakeMessage.IServerHello} message ServerHello message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ServerHello.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ServerHello message from the specified reader or buffer.
             * @function decode
             * @memberof proto.HandshakeMessage.ServerHello
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.HandshakeMessage.ServerHello} ServerHello
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ServerHello.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.ephemeral = reader.bytes();
                            message._ephemeral = "ephemeral";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message["static"] = reader.bytes();
                            message._static = "static";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.payload = reader.bytes();
                            message._payload = "payload";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.extendedStatic = reader.bytes();
                            message._extendedStatic = "extendedStatic";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.paddingBytes = reader.bytes();
                            message._paddingBytes = "paddingBytes";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.extendedCiphertext = reader.bytes();
                            message._extendedCiphertext = "extendedCiphertext";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ServerHello message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.HandshakeMessage.ServerHello
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.HandshakeMessage.ServerHello} ServerHello
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ServerHello.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ServerHello message.
             * @function verify
             * @memberof proto.HandshakeMessage.ServerHello
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ServerHello.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.ephemeral != null && Object.hasOwnProperty.call(message, "ephemeral")) {
                    properties._ephemeral = 1;
                    if (!(message.ephemeral && typeof message.ephemeral.length === "number" || $util.isString(message.ephemeral)))
                        return "ephemeral: buffer expected";
                }
                if (message["static"] != null && Object.hasOwnProperty.call(message, "static")) {
                    properties._static = 1;
                    if (!(message["static"] && typeof message["static"].length === "number" || $util.isString(message["static"])))
                        return "static: buffer expected";
                }
                if (message.payload != null && Object.hasOwnProperty.call(message, "payload")) {
                    properties._payload = 1;
                    if (!(message.payload && typeof message.payload.length === "number" || $util.isString(message.payload)))
                        return "payload: buffer expected";
                }
                if (message.extendedStatic != null && Object.hasOwnProperty.call(message, "extendedStatic")) {
                    properties._extendedStatic = 1;
                    if (!(message.extendedStatic && typeof message.extendedStatic.length === "number" || $util.isString(message.extendedStatic)))
                        return "extendedStatic: buffer expected";
                }
                if (message.paddingBytes != null && Object.hasOwnProperty.call(message, "paddingBytes")) {
                    properties._paddingBytes = 1;
                    if (!(message.paddingBytes && typeof message.paddingBytes.length === "number" || $util.isString(message.paddingBytes)))
                        return "paddingBytes: buffer expected";
                }
                if (message.extendedCiphertext != null && Object.hasOwnProperty.call(message, "extendedCiphertext")) {
                    properties._extendedCiphertext = 1;
                    if (!(message.extendedCiphertext && typeof message.extendedCiphertext.length === "number" || $util.isString(message.extendedCiphertext)))
                        return "extendedCiphertext: buffer expected";
                }
                return null;
            };

            /**
             * Creates a ServerHello message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.HandshakeMessage.ServerHello
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.HandshakeMessage.ServerHello} ServerHello
             */
            ServerHello.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.HandshakeMessage.ServerHello: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.ephemeral != null)
                    if (typeof object.ephemeral === "string")
                        $util.base64.decode(object.ephemeral, message.ephemeral = $util.newBuffer($util.base64.length(object.ephemeral)), 0);
                    else if (object.ephemeral.length >= 0)
                        message.ephemeral = object.ephemeral;
                if (object["static"] != null)
                    if (typeof object["static"] === "string")
                        $util.base64.decode(object["static"], message["static"] = $util.newBuffer($util.base64.length(object["static"])), 0);
                    else if (object["static"].length >= 0)
                        message["static"] = object["static"];
                if (object.payload != null)
                    if (typeof object.payload === "string")
                        $util.base64.decode(object.payload, message.payload = $util.newBuffer($util.base64.length(object.payload)), 0);
                    else if (object.payload.length >= 0)
                        message.payload = object.payload;
                if (object.extendedStatic != null)
                    if (typeof object.extendedStatic === "string")
                        $util.base64.decode(object.extendedStatic, message.extendedStatic = $util.newBuffer($util.base64.length(object.extendedStatic)), 0);
                    else if (object.extendedStatic.length >= 0)
                        message.extendedStatic = object.extendedStatic;
                if (object.paddingBytes != null)
                    if (typeof object.paddingBytes === "string")
                        $util.base64.decode(object.paddingBytes, message.paddingBytes = $util.newBuffer($util.base64.length(object.paddingBytes)), 0);
                    else if (object.paddingBytes.length >= 0)
                        message.paddingBytes = object.paddingBytes;
                if (object.extendedCiphertext != null)
                    if (typeof object.extendedCiphertext === "string")
                        $util.base64.decode(object.extendedCiphertext, message.extendedCiphertext = $util.newBuffer($util.base64.length(object.extendedCiphertext)), 0);
                    else if (object.extendedCiphertext.length >= 0)
                        message.extendedCiphertext = object.extendedCiphertext;
                return message;
            };

            /**
             * Creates a plain object from a ServerHello message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.HandshakeMessage.ServerHello
             * @static
             * @param {proto.HandshakeMessage.ServerHello} message ServerHello
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ServerHello.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.ephemeral != null && Object.hasOwnProperty.call(message, "ephemeral"))
                    object.ephemeral = options.bytes === String ? $util.base64.encode(message.ephemeral, 0, message.ephemeral.length) : options.bytes === Array ? Array.prototype.slice.call(message.ephemeral) : message.ephemeral;
                if (message["static"] != null && Object.hasOwnProperty.call(message, "static"))
                    object["static"] = options.bytes === String ? $util.base64.encode(message["static"], 0, message["static"].length) : options.bytes === Array ? Array.prototype.slice.call(message["static"]) : message["static"];
                if (message.payload != null && Object.hasOwnProperty.call(message, "payload"))
                    object.payload = options.bytes === String ? $util.base64.encode(message.payload, 0, message.payload.length) : options.bytes === Array ? Array.prototype.slice.call(message.payload) : message.payload;
                if (message.extendedStatic != null && Object.hasOwnProperty.call(message, "extendedStatic"))
                    object.extendedStatic = options.bytes === String ? $util.base64.encode(message.extendedStatic, 0, message.extendedStatic.length) : options.bytes === Array ? Array.prototype.slice.call(message.extendedStatic) : message.extendedStatic;
                if (message.paddingBytes != null && Object.hasOwnProperty.call(message, "paddingBytes"))
                    object.paddingBytes = options.bytes === String ? $util.base64.encode(message.paddingBytes, 0, message.paddingBytes.length) : options.bytes === Array ? Array.prototype.slice.call(message.paddingBytes) : message.paddingBytes;
                if (message.extendedCiphertext != null && Object.hasOwnProperty.call(message, "extendedCiphertext"))
                    object.extendedCiphertext = options.bytes === String ? $util.base64.encode(message.extendedCiphertext, 0, message.extendedCiphertext.length) : options.bytes === Array ? Array.prototype.slice.call(message.extendedCiphertext) : message.extendedCiphertext;
                return object;
            };

            /**
             * Converts this ServerHello to JSON.
             * @function toJSON
             * @memberof proto.HandshakeMessage.ServerHello
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ServerHello.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ServerHello
             * @function getTypeUrl
             * @memberof proto.HandshakeMessage.ServerHello
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ServerHello.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.HandshakeMessage.ServerHello";
            };

            return ServerHello;
        })();

        return HandshakeMessage;
    })();

    proto.HatchMetadataSync = (function() {

        /**
         * Properties of a HatchMetadataSync.
         * @memberof proto
         * @interface IHatchMetadataSync
         * @property {Uint8Array|null} [data] HatchMetadataSync data
         * @property {number|Long|null} [timestampMs] HatchMetadataSync timestampMs
         * @property {string|null} [requestId] HatchMetadataSync requestId
         */

        /**
         * Constructs a new HatchMetadataSync.
         * @memberof proto
         * @classdesc Represents a HatchMetadataSync.
         * @implements IHatchMetadataSync
         * @constructor
         * @param {proto.IHatchMetadataSync=} [properties] Properties to set
         */
        function HatchMetadataSync(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * HatchMetadataSync data.
         * @member {Uint8Array|null|undefined} data
         * @memberof proto.HatchMetadataSync
         * @instance
         */
        HatchMetadataSync.prototype.data = null;

        /**
         * HatchMetadataSync timestampMs.
         * @member {number|Long|null|undefined} timestampMs
         * @memberof proto.HatchMetadataSync
         * @instance
         */
        HatchMetadataSync.prototype.timestampMs = null;

        /**
         * HatchMetadataSync requestId.
         * @member {string|null|undefined} requestId
         * @memberof proto.HatchMetadataSync
         * @instance
         */
        HatchMetadataSync.prototype.requestId = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HatchMetadataSync.prototype, "_data", {
            get: $util.oneOfGetter($oneOfFields = ["data"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HatchMetadataSync.prototype, "_timestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["timestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HatchMetadataSync.prototype, "_requestId", {
            get: $util.oneOfGetter($oneOfFields = ["requestId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new HatchMetadataSync instance using the specified properties.
         * @function create
         * @memberof proto.HatchMetadataSync
         * @static
         * @param {proto.IHatchMetadataSync=} [properties] Properties to set
         * @returns {proto.HatchMetadataSync} HatchMetadataSync instance
         */
        HatchMetadataSync.create = function create(properties) {
            return new HatchMetadataSync(properties);
        };

        /**
         * Encodes the specified HatchMetadataSync message. Does not implicitly {@link proto.HatchMetadataSync.verify|verify} messages.
         * @function encode
         * @memberof proto.HatchMetadataSync
         * @static
         * @param {proto.IHatchMetadataSync} message HatchMetadataSync message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HatchMetadataSync.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.data);
            if (message.timestampMs != null && Object.hasOwnProperty.call(message, "timestampMs"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.timestampMs);
            if (message.requestId != null && Object.hasOwnProperty.call(message, "requestId"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.requestId);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified HatchMetadataSync message, length delimited. Does not implicitly {@link proto.HatchMetadataSync.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.HatchMetadataSync
         * @static
         * @param {proto.IHatchMetadataSync} message HatchMetadataSync message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HatchMetadataSync.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a HatchMetadataSync message from the specified reader or buffer.
         * @function decode
         * @memberof proto.HatchMetadataSync
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.HatchMetadataSync} HatchMetadataSync
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HatchMetadataSync.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.data = reader.bytes();
                        message._data = "data";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.timestampMs = reader.int64();
                        message._timestampMs = "timestampMs";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.requestId = reader.stringVerify();
                        message._requestId = "requestId";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a HatchMetadataSync message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.HatchMetadataSync
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.HatchMetadataSync} HatchMetadataSync
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HatchMetadataSync.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HatchMetadataSync message.
         * @function verify
         * @memberof proto.HatchMetadataSync
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HatchMetadataSync.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.data != null && Object.hasOwnProperty.call(message, "data")) {
                properties._data = 1;
                if (!(message.data && typeof message.data.length === "number" || $util.isString(message.data)))
                    return "data: buffer expected";
            }
            if (message.timestampMs != null && Object.hasOwnProperty.call(message, "timestampMs")) {
                properties._timestampMs = 1;
                if (!$util.isInteger(message.timestampMs) && !(message.timestampMs && $util.isInteger(message.timestampMs.low) && $util.isInteger(message.timestampMs.high)))
                    return "timestampMs: integer|Long expected";
            }
            if (message.requestId != null && Object.hasOwnProperty.call(message, "requestId")) {
                properties._requestId = 1;
                if (!$util.isString(message.requestId))
                    return "requestId: string expected";
            }
            return null;
        };

        /**
         * Creates a HatchMetadataSync message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.HatchMetadataSync
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.HatchMetadataSync} HatchMetadataSync
         */
        HatchMetadataSync.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.HatchMetadataSync: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.data != null)
                if (typeof object.data === "string")
                    $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                else if (object.data.length >= 0)
                    message.data = object.data;
            if (object.timestampMs != null)
                if ($util.Long)
                    message.timestampMs = $util.Long.fromValue(object.timestampMs, false);
                else if (typeof object.timestampMs === "string")
                    message.timestampMs = parseInt(object.timestampMs, 10);
                else if (typeof object.timestampMs === "number")
                    message.timestampMs = object.timestampMs;
                else if (typeof object.timestampMs === "object")
                    message.timestampMs = new $util.LongBits(object.timestampMs.low >>> 0, object.timestampMs.high >>> 0).toNumber();
            if (object.requestId != null)
                message.requestId = String(object.requestId);
            return message;
        };

        /**
         * Creates a plain object from a HatchMetadataSync message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.HatchMetadataSync
         * @static
         * @param {proto.HatchMetadataSync} message HatchMetadataSync
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HatchMetadataSync.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                object.data = options.bytes === String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === Array ? Array.prototype.slice.call(message.data) : message.data;
            if (message.timestampMs != null && Object.hasOwnProperty.call(message, "timestampMs"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.timestampMs = typeof message.timestampMs === "number" ? BigInt(message.timestampMs) : $util.Long.fromBits(message.timestampMs.low >>> 0, message.timestampMs.high >>> 0, false).toBigInt();
                else if (typeof message.timestampMs === "number")
                    object.timestampMs = options.longs === String ? String(message.timestampMs) : message.timestampMs;
                else
                    object.timestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.timestampMs) : options.longs === Number ? new $util.LongBits(message.timestampMs.low >>> 0, message.timestampMs.high >>> 0).toNumber() : message.timestampMs;
            if (message.requestId != null && Object.hasOwnProperty.call(message, "requestId"))
                object.requestId = message.requestId;
            return object;
        };

        /**
         * Converts this HatchMetadataSync to JSON.
         * @function toJSON
         * @memberof proto.HatchMetadataSync
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HatchMetadataSync.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for HatchMetadataSync
         * @function getTypeUrl
         * @memberof proto.HatchMetadataSync
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        HatchMetadataSync.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.HatchMetadataSync";
        };

        return HatchMetadataSync;
    })();

    proto.HistorySync = (function() {

        /**
         * Properties of a HistorySync.
         * @memberof proto
         * @interface IHistorySync
         * @property {proto.HistorySync.HistorySyncType|null} [syncType] HistorySync syncType
         * @property {Array.<proto.IConversation>|null} [conversations] HistorySync conversations
         * @property {Array.<proto.IWebMessageInfo>|null} [statusV3Messages] HistorySync statusV3Messages
         * @property {number|null} [chunkOrder] HistorySync chunkOrder
         * @property {number|null} [progress] HistorySync progress
         * @property {Array.<proto.IPushname>|null} [pushnames] HistorySync pushnames
         * @property {proto.IGlobalSettings|null} [globalSettings] HistorySync globalSettings
         * @property {Uint8Array|null} [threadIdUserSecret] HistorySync threadIdUserSecret
         * @property {number|null} [threadDsTimeframeOffset] HistorySync threadDsTimeframeOffset
         * @property {Array.<proto.IStickerMetadata>|null} [recentStickers] HistorySync recentStickers
         * @property {Array.<proto.IPastParticipants>|null} [pastParticipants] HistorySync pastParticipants
         * @property {Array.<proto.ICallLogRecord>|null} [callLogRecords] HistorySync callLogRecords
         * @property {proto.HistorySync.BotAIWaitListState|null} [aiWaitListState] HistorySync aiWaitListState
         * @property {Array.<proto.IPhoneNumberToLIDMapping>|null} [phoneNumberToLidMappings] HistorySync phoneNumberToLidMappings
         * @property {string|null} [companionMetaNonce] HistorySync companionMetaNonce
         * @property {Uint8Array|null} [shareableChatIdentifierEncryptionKey] HistorySync shareableChatIdentifierEncryptionKey
         * @property {Array.<proto.IAccount>|null} [accounts] HistorySync accounts
         * @property {Uint8Array|null} [nctSalt] HistorySync nctSalt
         * @property {Array.<proto.IInlineContact>|null} [inlineContacts] HistorySync inlineContacts
         * @property {boolean|null} [inlineContactsProvided] HistorySync inlineContactsProvided
         */

        /**
         * Constructs a new HistorySync.
         * @memberof proto
         * @classdesc Represents a HistorySync.
         * @implements IHistorySync
         * @constructor
         * @param {proto.IHistorySync=} [properties] Properties to set
         */
        function HistorySync(properties) {
            this.conversations = [];
            this.statusV3Messages = [];
            this.pushnames = [];
            this.recentStickers = [];
            this.pastParticipants = [];
            this.callLogRecords = [];
            this.phoneNumberToLidMappings = [];
            this.accounts = [];
            this.inlineContacts = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * HistorySync syncType.
         * @member {proto.HistorySync.HistorySyncType|null|undefined} syncType
         * @memberof proto.HistorySync
         * @instance
         */
        HistorySync.prototype.syncType = null;

        /**
         * HistorySync conversations.
         * @member {Array.<proto.IConversation>} conversations
         * @memberof proto.HistorySync
         * @instance
         */
        HistorySync.prototype.conversations = $util.emptyArray;

        /**
         * HistorySync statusV3Messages.
         * @member {Array.<proto.IWebMessageInfo>} statusV3Messages
         * @memberof proto.HistorySync
         * @instance
         */
        HistorySync.prototype.statusV3Messages = $util.emptyArray;

        /**
         * HistorySync chunkOrder.
         * @member {number|null|undefined} chunkOrder
         * @memberof proto.HistorySync
         * @instance
         */
        HistorySync.prototype.chunkOrder = null;

        /**
         * HistorySync progress.
         * @member {number|null|undefined} progress
         * @memberof proto.HistorySync
         * @instance
         */
        HistorySync.prototype.progress = null;

        /**
         * HistorySync pushnames.
         * @member {Array.<proto.IPushname>} pushnames
         * @memberof proto.HistorySync
         * @instance
         */
        HistorySync.prototype.pushnames = $util.emptyArray;

        /**
         * HistorySync globalSettings.
         * @member {proto.IGlobalSettings|null|undefined} globalSettings
         * @memberof proto.HistorySync
         * @instance
         */
        HistorySync.prototype.globalSettings = null;

        /**
         * HistorySync threadIdUserSecret.
         * @member {Uint8Array|null|undefined} threadIdUserSecret
         * @memberof proto.HistorySync
         * @instance
         */
        HistorySync.prototype.threadIdUserSecret = null;

        /**
         * HistorySync threadDsTimeframeOffset.
         * @member {number|null|undefined} threadDsTimeframeOffset
         * @memberof proto.HistorySync
         * @instance
         */
        HistorySync.prototype.threadDsTimeframeOffset = null;

        /**
         * HistorySync recentStickers.
         * @member {Array.<proto.IStickerMetadata>} recentStickers
         * @memberof proto.HistorySync
         * @instance
         */
        HistorySync.prototype.recentStickers = $util.emptyArray;

        /**
         * HistorySync pastParticipants.
         * @member {Array.<proto.IPastParticipants>} pastParticipants
         * @memberof proto.HistorySync
         * @instance
         */
        HistorySync.prototype.pastParticipants = $util.emptyArray;

        /**
         * HistorySync callLogRecords.
         * @member {Array.<proto.ICallLogRecord>} callLogRecords
         * @memberof proto.HistorySync
         * @instance
         */
        HistorySync.prototype.callLogRecords = $util.emptyArray;

        /**
         * HistorySync aiWaitListState.
         * @member {proto.HistorySync.BotAIWaitListState|null|undefined} aiWaitListState
         * @memberof proto.HistorySync
         * @instance
         */
        HistorySync.prototype.aiWaitListState = null;

        /**
         * HistorySync phoneNumberToLidMappings.
         * @member {Array.<proto.IPhoneNumberToLIDMapping>} phoneNumberToLidMappings
         * @memberof proto.HistorySync
         * @instance
         */
        HistorySync.prototype.phoneNumberToLidMappings = $util.emptyArray;

        /**
         * HistorySync companionMetaNonce.
         * @member {string|null|undefined} companionMetaNonce
         * @memberof proto.HistorySync
         * @instance
         */
        HistorySync.prototype.companionMetaNonce = null;

        /**
         * HistorySync shareableChatIdentifierEncryptionKey.
         * @member {Uint8Array|null|undefined} shareableChatIdentifierEncryptionKey
         * @memberof proto.HistorySync
         * @instance
         */
        HistorySync.prototype.shareableChatIdentifierEncryptionKey = null;

        /**
         * HistorySync accounts.
         * @member {Array.<proto.IAccount>} accounts
         * @memberof proto.HistorySync
         * @instance
         */
        HistorySync.prototype.accounts = $util.emptyArray;

        /**
         * HistorySync nctSalt.
         * @member {Uint8Array|null|undefined} nctSalt
         * @memberof proto.HistorySync
         * @instance
         */
        HistorySync.prototype.nctSalt = null;

        /**
         * HistorySync inlineContacts.
         * @member {Array.<proto.IInlineContact>} inlineContacts
         * @memberof proto.HistorySync
         * @instance
         */
        HistorySync.prototype.inlineContacts = $util.emptyArray;

        /**
         * HistorySync inlineContactsProvided.
         * @member {boolean|null|undefined} inlineContactsProvided
         * @memberof proto.HistorySync
         * @instance
         */
        HistorySync.prototype.inlineContactsProvided = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySync.prototype, "_syncType", {
            get: $util.oneOfGetter($oneOfFields = ["syncType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySync.prototype, "_chunkOrder", {
            get: $util.oneOfGetter($oneOfFields = ["chunkOrder"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySync.prototype, "_progress", {
            get: $util.oneOfGetter($oneOfFields = ["progress"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySync.prototype, "_globalSettings", {
            get: $util.oneOfGetter($oneOfFields = ["globalSettings"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySync.prototype, "_threadIdUserSecret", {
            get: $util.oneOfGetter($oneOfFields = ["threadIdUserSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySync.prototype, "_threadDsTimeframeOffset", {
            get: $util.oneOfGetter($oneOfFields = ["threadDsTimeframeOffset"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySync.prototype, "_aiWaitListState", {
            get: $util.oneOfGetter($oneOfFields = ["aiWaitListState"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySync.prototype, "_companionMetaNonce", {
            get: $util.oneOfGetter($oneOfFields = ["companionMetaNonce"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySync.prototype, "_shareableChatIdentifierEncryptionKey", {
            get: $util.oneOfGetter($oneOfFields = ["shareableChatIdentifierEncryptionKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySync.prototype, "_nctSalt", {
            get: $util.oneOfGetter($oneOfFields = ["nctSalt"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySync.prototype, "_inlineContactsProvided", {
            get: $util.oneOfGetter($oneOfFields = ["inlineContactsProvided"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new HistorySync instance using the specified properties.
         * @function create
         * @memberof proto.HistorySync
         * @static
         * @param {proto.IHistorySync=} [properties] Properties to set
         * @returns {proto.HistorySync} HistorySync instance
         */
        HistorySync.create = function create(properties) {
            return new HistorySync(properties);
        };

        /**
         * Encodes the specified HistorySync message. Does not implicitly {@link proto.HistorySync.verify|verify} messages.
         * @function encode
         * @memberof proto.HistorySync
         * @static
         * @param {proto.IHistorySync} message HistorySync message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HistorySync.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.syncType != null && Object.hasOwnProperty.call(message, "syncType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.syncType);
            if (message.conversations != null && message.conversations.length)
                for (var i = 0; i < message.conversations.length; ++i)
                    $root.proto.Conversation.encode(message.conversations[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.statusV3Messages != null && message.statusV3Messages.length)
                for (var i = 0; i < message.statusV3Messages.length; ++i)
                    $root.proto.WebMessageInfo.encode(message.statusV3Messages[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.chunkOrder != null && Object.hasOwnProperty.call(message, "chunkOrder"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.chunkOrder);
            if (message.progress != null && Object.hasOwnProperty.call(message, "progress"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.progress);
            if (message.pushnames != null && message.pushnames.length)
                for (var i = 0; i < message.pushnames.length; ++i)
                    $root.proto.Pushname.encode(message.pushnames[i], writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
            if (message.globalSettings != null && Object.hasOwnProperty.call(message, "globalSettings"))
                $root.proto.GlobalSettings.encode(message.globalSettings, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
            if (message.threadIdUserSecret != null && Object.hasOwnProperty.call(message, "threadIdUserSecret"))
                writer.uint32(/* id 9, wireType 2 =*/74).bytes(message.threadIdUserSecret);
            if (message.threadDsTimeframeOffset != null && Object.hasOwnProperty.call(message, "threadDsTimeframeOffset"))
                writer.uint32(/* id 10, wireType 0 =*/80).uint32(message.threadDsTimeframeOffset);
            if (message.recentStickers != null && message.recentStickers.length)
                for (var i = 0; i < message.recentStickers.length; ++i)
                    $root.proto.StickerMetadata.encode(message.recentStickers[i], writer.uint32(/* id 11, wireType 2 =*/90).fork(), q + 1).ldelim();
            if (message.pastParticipants != null && message.pastParticipants.length)
                for (var i = 0; i < message.pastParticipants.length; ++i)
                    $root.proto.PastParticipants.encode(message.pastParticipants[i], writer.uint32(/* id 12, wireType 2 =*/98).fork(), q + 1).ldelim();
            if (message.callLogRecords != null && message.callLogRecords.length)
                for (var i = 0; i < message.callLogRecords.length; ++i)
                    $root.proto.CallLogRecord.encode(message.callLogRecords[i], writer.uint32(/* id 13, wireType 2 =*/106).fork(), q + 1).ldelim();
            if (message.aiWaitListState != null && Object.hasOwnProperty.call(message, "aiWaitListState"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.aiWaitListState);
            if (message.phoneNumberToLidMappings != null && message.phoneNumberToLidMappings.length)
                for (var i = 0; i < message.phoneNumberToLidMappings.length; ++i)
                    $root.proto.PhoneNumberToLIDMapping.encode(message.phoneNumberToLidMappings[i], writer.uint32(/* id 15, wireType 2 =*/122).fork(), q + 1).ldelim();
            if (message.companionMetaNonce != null && Object.hasOwnProperty.call(message, "companionMetaNonce"))
                writer.uint32(/* id 16, wireType 2 =*/130).string(message.companionMetaNonce);
            if (message.shareableChatIdentifierEncryptionKey != null && Object.hasOwnProperty.call(message, "shareableChatIdentifierEncryptionKey"))
                writer.uint32(/* id 17, wireType 2 =*/138).bytes(message.shareableChatIdentifierEncryptionKey);
            if (message.accounts != null && message.accounts.length)
                for (var i = 0; i < message.accounts.length; ++i)
                    $root.proto.Account.encode(message.accounts[i], writer.uint32(/* id 18, wireType 2 =*/146).fork(), q + 1).ldelim();
            if (message.nctSalt != null && Object.hasOwnProperty.call(message, "nctSalt"))
                writer.uint32(/* id 19, wireType 2 =*/154).bytes(message.nctSalt);
            if (message.inlineContacts != null && message.inlineContacts.length)
                for (var i = 0; i < message.inlineContacts.length; ++i)
                    $root.proto.InlineContact.encode(message.inlineContacts[i], writer.uint32(/* id 20, wireType 2 =*/162).fork(), q + 1).ldelim();
            if (message.inlineContactsProvided != null && Object.hasOwnProperty.call(message, "inlineContactsProvided"))
                writer.uint32(/* id 21, wireType 0 =*/168).bool(message.inlineContactsProvided);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified HistorySync message, length delimited. Does not implicitly {@link proto.HistorySync.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.HistorySync
         * @static
         * @param {proto.IHistorySync} message HistorySync message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HistorySync.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a HistorySync message from the specified reader or buffer.
         * @function decode
         * @memberof proto.HistorySync
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.HistorySync} HistorySync
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HistorySync.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.syncType = reader.int32();
                        message._syncType = "syncType";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(message.conversations && message.conversations.length))
                            message.conversations = [];
                        message.conversations.push($root.proto.Conversation.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        if (!(message.statusV3Messages && message.statusV3Messages.length))
                            message.statusV3Messages = [];
                        message.statusV3Messages.push($root.proto.WebMessageInfo.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.chunkOrder = reader.uint32();
                        message._chunkOrder = "chunkOrder";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        message.progress = reader.uint32();
                        message._progress = "progress";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        if (!(message.pushnames && message.pushnames.length))
                            message.pushnames = [];
                        message.pushnames.push($root.proto.Pushname.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        message.globalSettings = $root.proto.GlobalSettings.decode(reader, reader.uint32(), undefined, q + 1, message.globalSettings);
                        message._globalSettings = "globalSettings";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        message.threadIdUserSecret = reader.bytes();
                        message._threadIdUserSecret = "threadIdUserSecret";
                        continue;
                    }
                case 10: {
                        if (u !== 0)
                            break;
                        message.threadDsTimeframeOffset = reader.uint32();
                        message._threadDsTimeframeOffset = "threadDsTimeframeOffset";
                        continue;
                    }
                case 11: {
                        if (u !== 2)
                            break;
                        if (!(message.recentStickers && message.recentStickers.length))
                            message.recentStickers = [];
                        message.recentStickers.push($root.proto.StickerMetadata.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 12: {
                        if (u !== 2)
                            break;
                        if (!(message.pastParticipants && message.pastParticipants.length))
                            message.pastParticipants = [];
                        message.pastParticipants.push($root.proto.PastParticipants.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        if (!(message.callLogRecords && message.callLogRecords.length))
                            message.callLogRecords = [];
                        message.callLogRecords.push($root.proto.CallLogRecord.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 14: {
                        if (u !== 0)
                            break;
                        message.aiWaitListState = reader.int32();
                        message._aiWaitListState = "aiWaitListState";
                        continue;
                    }
                case 15: {
                        if (u !== 2)
                            break;
                        if (!(message.phoneNumberToLidMappings && message.phoneNumberToLidMappings.length))
                            message.phoneNumberToLidMappings = [];
                        message.phoneNumberToLidMappings.push($root.proto.PhoneNumberToLIDMapping.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 16: {
                        if (u !== 2)
                            break;
                        message.companionMetaNonce = reader.stringVerify();
                        message._companionMetaNonce = "companionMetaNonce";
                        continue;
                    }
                case 17: {
                        if (u !== 2)
                            break;
                        message.shareableChatIdentifierEncryptionKey = reader.bytes();
                        message._shareableChatIdentifierEncryptionKey = "shareableChatIdentifierEncryptionKey";
                        continue;
                    }
                case 18: {
                        if (u !== 2)
                            break;
                        if (!(message.accounts && message.accounts.length))
                            message.accounts = [];
                        message.accounts.push($root.proto.Account.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 19: {
                        if (u !== 2)
                            break;
                        message.nctSalt = reader.bytes();
                        message._nctSalt = "nctSalt";
                        continue;
                    }
                case 20: {
                        if (u !== 2)
                            break;
                        if (!(message.inlineContacts && message.inlineContacts.length))
                            message.inlineContacts = [];
                        message.inlineContacts.push($root.proto.InlineContact.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 21: {
                        if (u !== 0)
                            break;
                        message.inlineContactsProvided = reader.bool();
                        message._inlineContactsProvided = "inlineContactsProvided";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a HistorySync message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.HistorySync
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.HistorySync} HistorySync
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HistorySync.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HistorySync message.
         * @function verify
         * @memberof proto.HistorySync
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HistorySync.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.syncType != null && Object.hasOwnProperty.call(message, "syncType")) {
                properties._syncType = 1;
                if (typeof message.syncType !== "number" || (message.syncType | 0) !== message.syncType)
                    return "syncType: enum value expected";
            }
            if (message.conversations != null && Object.hasOwnProperty.call(message, "conversations")) {
                if (!Array.isArray(message.conversations))
                    return "conversations: array expected";
                for (var i = 0; i < message.conversations.length; ++i) {
                    var error = $root.proto.Conversation.verify(message.conversations[i], q + 1);
                    if (error)
                        return "conversations." + error;
                }
            }
            if (message.statusV3Messages != null && Object.hasOwnProperty.call(message, "statusV3Messages")) {
                if (!Array.isArray(message.statusV3Messages))
                    return "statusV3Messages: array expected";
                for (var i = 0; i < message.statusV3Messages.length; ++i) {
                    var error = $root.proto.WebMessageInfo.verify(message.statusV3Messages[i], q + 1);
                    if (error)
                        return "statusV3Messages." + error;
                }
            }
            if (message.chunkOrder != null && Object.hasOwnProperty.call(message, "chunkOrder")) {
                properties._chunkOrder = 1;
                if (!$util.isInteger(message.chunkOrder))
                    return "chunkOrder: integer expected";
            }
            if (message.progress != null && Object.hasOwnProperty.call(message, "progress")) {
                properties._progress = 1;
                if (!$util.isInteger(message.progress))
                    return "progress: integer expected";
            }
            if (message.pushnames != null && Object.hasOwnProperty.call(message, "pushnames")) {
                if (!Array.isArray(message.pushnames))
                    return "pushnames: array expected";
                for (var i = 0; i < message.pushnames.length; ++i) {
                    var error = $root.proto.Pushname.verify(message.pushnames[i], q + 1);
                    if (error)
                        return "pushnames." + error;
                }
            }
            if (message.globalSettings != null && Object.hasOwnProperty.call(message, "globalSettings")) {
                properties._globalSettings = 1;
                {
                    var error = $root.proto.GlobalSettings.verify(message.globalSettings, q + 1);
                    if (error)
                        return "globalSettings." + error;
                }
            }
            if (message.threadIdUserSecret != null && Object.hasOwnProperty.call(message, "threadIdUserSecret")) {
                properties._threadIdUserSecret = 1;
                if (!(message.threadIdUserSecret && typeof message.threadIdUserSecret.length === "number" || $util.isString(message.threadIdUserSecret)))
                    return "threadIdUserSecret: buffer expected";
            }
            if (message.threadDsTimeframeOffset != null && Object.hasOwnProperty.call(message, "threadDsTimeframeOffset")) {
                properties._threadDsTimeframeOffset = 1;
                if (!$util.isInteger(message.threadDsTimeframeOffset))
                    return "threadDsTimeframeOffset: integer expected";
            }
            if (message.recentStickers != null && Object.hasOwnProperty.call(message, "recentStickers")) {
                if (!Array.isArray(message.recentStickers))
                    return "recentStickers: array expected";
                for (var i = 0; i < message.recentStickers.length; ++i) {
                    var error = $root.proto.StickerMetadata.verify(message.recentStickers[i], q + 1);
                    if (error)
                        return "recentStickers." + error;
                }
            }
            if (message.pastParticipants != null && Object.hasOwnProperty.call(message, "pastParticipants")) {
                if (!Array.isArray(message.pastParticipants))
                    return "pastParticipants: array expected";
                for (var i = 0; i < message.pastParticipants.length; ++i) {
                    var error = $root.proto.PastParticipants.verify(message.pastParticipants[i], q + 1);
                    if (error)
                        return "pastParticipants." + error;
                }
            }
            if (message.callLogRecords != null && Object.hasOwnProperty.call(message, "callLogRecords")) {
                if (!Array.isArray(message.callLogRecords))
                    return "callLogRecords: array expected";
                for (var i = 0; i < message.callLogRecords.length; ++i) {
                    var error = $root.proto.CallLogRecord.verify(message.callLogRecords[i], q + 1);
                    if (error)
                        return "callLogRecords." + error;
                }
            }
            if (message.aiWaitListState != null && Object.hasOwnProperty.call(message, "aiWaitListState")) {
                properties._aiWaitListState = 1;
                if (typeof message.aiWaitListState !== "number" || (message.aiWaitListState | 0) !== message.aiWaitListState)
                    return "aiWaitListState: enum value expected";
            }
            if (message.phoneNumberToLidMappings != null && Object.hasOwnProperty.call(message, "phoneNumberToLidMappings")) {
                if (!Array.isArray(message.phoneNumberToLidMappings))
                    return "phoneNumberToLidMappings: array expected";
                for (var i = 0; i < message.phoneNumberToLidMappings.length; ++i) {
                    var error = $root.proto.PhoneNumberToLIDMapping.verify(message.phoneNumberToLidMappings[i], q + 1);
                    if (error)
                        return "phoneNumberToLidMappings." + error;
                }
            }
            if (message.companionMetaNonce != null && Object.hasOwnProperty.call(message, "companionMetaNonce")) {
                properties._companionMetaNonce = 1;
                if (!$util.isString(message.companionMetaNonce))
                    return "companionMetaNonce: string expected";
            }
            if (message.shareableChatIdentifierEncryptionKey != null && Object.hasOwnProperty.call(message, "shareableChatIdentifierEncryptionKey")) {
                properties._shareableChatIdentifierEncryptionKey = 1;
                if (!(message.shareableChatIdentifierEncryptionKey && typeof message.shareableChatIdentifierEncryptionKey.length === "number" || $util.isString(message.shareableChatIdentifierEncryptionKey)))
                    return "shareableChatIdentifierEncryptionKey: buffer expected";
            }
            if (message.accounts != null && Object.hasOwnProperty.call(message, "accounts")) {
                if (!Array.isArray(message.accounts))
                    return "accounts: array expected";
                for (var i = 0; i < message.accounts.length; ++i) {
                    var error = $root.proto.Account.verify(message.accounts[i], q + 1);
                    if (error)
                        return "accounts." + error;
                }
            }
            if (message.nctSalt != null && Object.hasOwnProperty.call(message, "nctSalt")) {
                properties._nctSalt = 1;
                if (!(message.nctSalt && typeof message.nctSalt.length === "number" || $util.isString(message.nctSalt)))
                    return "nctSalt: buffer expected";
            }
            if (message.inlineContacts != null && Object.hasOwnProperty.call(message, "inlineContacts")) {
                if (!Array.isArray(message.inlineContacts))
                    return "inlineContacts: array expected";
                for (var i = 0; i < message.inlineContacts.length; ++i) {
                    var error = $root.proto.InlineContact.verify(message.inlineContacts[i], q + 1);
                    if (error)
                        return "inlineContacts." + error;
                }
            }
            if (message.inlineContactsProvided != null && Object.hasOwnProperty.call(message, "inlineContactsProvided")) {
                properties._inlineContactsProvided = 1;
                if (typeof message.inlineContactsProvided !== "boolean")
                    return "inlineContactsProvided: boolean expected";
            }
            return null;
        };

        /**
         * Creates a HistorySync message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.HistorySync
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.HistorySync} HistorySync
         */
        HistorySync.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.HistorySync: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.syncType) {
            case "INITIAL_BOOTSTRAP":
            case 0:
                message.syncType = 0;
                break;
            case "INITIAL_STATUS_V3":
            case 1:
                message.syncType = 1;
                break;
            case "FULL":
            case 2:
                message.syncType = 2;
                break;
            case "RECENT":
            case 3:
                message.syncType = 3;
                break;
            case "PUSH_NAME":
            case 4:
                message.syncType = 4;
                break;
            case "NON_BLOCKING_DATA":
            case 5:
                message.syncType = 5;
                break;
            case "ON_DEMAND":
            case 6:
                message.syncType = 6;
                break;
            default:
                if (typeof object.syncType === "number" && (object.syncType | 0) === object.syncType)
                    message.syncType = object.syncType;
            }
            if (object.conversations) {
                if (!Array.isArray(object.conversations))
                    throw TypeError(".proto.HistorySync.conversations: array expected");
                message.conversations = Array(object.conversations.length);
                for (var i = 0; i < object.conversations.length; ++i) {
                    if (!$util.isObject(object.conversations[i]))
                        throw TypeError(".proto.HistorySync.conversations: object expected");
                    message.conversations[i] = $root.proto.Conversation.fromObject(object.conversations[i], q + 1);
                }
            }
            if (object.statusV3Messages) {
                if (!Array.isArray(object.statusV3Messages))
                    throw TypeError(".proto.HistorySync.statusV3Messages: array expected");
                message.statusV3Messages = Array(object.statusV3Messages.length);
                for (var i = 0; i < object.statusV3Messages.length; ++i) {
                    if (!$util.isObject(object.statusV3Messages[i]))
                        throw TypeError(".proto.HistorySync.statusV3Messages: object expected");
                    message.statusV3Messages[i] = $root.proto.WebMessageInfo.fromObject(object.statusV3Messages[i], q + 1);
                }
            }
            if (object.chunkOrder != null)
                message.chunkOrder = object.chunkOrder >>> 0;
            if (object.progress != null)
                message.progress = object.progress >>> 0;
            if (object.pushnames) {
                if (!Array.isArray(object.pushnames))
                    throw TypeError(".proto.HistorySync.pushnames: array expected");
                message.pushnames = Array(object.pushnames.length);
                for (var i = 0; i < object.pushnames.length; ++i) {
                    if (!$util.isObject(object.pushnames[i]))
                        throw TypeError(".proto.HistorySync.pushnames: object expected");
                    message.pushnames[i] = $root.proto.Pushname.fromObject(object.pushnames[i], q + 1);
                }
            }
            if (object.globalSettings != null) {
                if (!$util.isObject(object.globalSettings))
                    throw TypeError(".proto.HistorySync.globalSettings: object expected");
                message.globalSettings = $root.proto.GlobalSettings.fromObject(object.globalSettings, q + 1);
            }
            if (object.threadIdUserSecret != null)
                if (typeof object.threadIdUserSecret === "string")
                    $util.base64.decode(object.threadIdUserSecret, message.threadIdUserSecret = $util.newBuffer($util.base64.length(object.threadIdUserSecret)), 0);
                else if (object.threadIdUserSecret.length >= 0)
                    message.threadIdUserSecret = object.threadIdUserSecret;
            if (object.threadDsTimeframeOffset != null)
                message.threadDsTimeframeOffset = object.threadDsTimeframeOffset >>> 0;
            if (object.recentStickers) {
                if (!Array.isArray(object.recentStickers))
                    throw TypeError(".proto.HistorySync.recentStickers: array expected");
                message.recentStickers = Array(object.recentStickers.length);
                for (var i = 0; i < object.recentStickers.length; ++i) {
                    if (!$util.isObject(object.recentStickers[i]))
                        throw TypeError(".proto.HistorySync.recentStickers: object expected");
                    message.recentStickers[i] = $root.proto.StickerMetadata.fromObject(object.recentStickers[i], q + 1);
                }
            }
            if (object.pastParticipants) {
                if (!Array.isArray(object.pastParticipants))
                    throw TypeError(".proto.HistorySync.pastParticipants: array expected");
                message.pastParticipants = Array(object.pastParticipants.length);
                for (var i = 0; i < object.pastParticipants.length; ++i) {
                    if (!$util.isObject(object.pastParticipants[i]))
                        throw TypeError(".proto.HistorySync.pastParticipants: object expected");
                    message.pastParticipants[i] = $root.proto.PastParticipants.fromObject(object.pastParticipants[i], q + 1);
                }
            }
            if (object.callLogRecords) {
                if (!Array.isArray(object.callLogRecords))
                    throw TypeError(".proto.HistorySync.callLogRecords: array expected");
                message.callLogRecords = Array(object.callLogRecords.length);
                for (var i = 0; i < object.callLogRecords.length; ++i) {
                    if (!$util.isObject(object.callLogRecords[i]))
                        throw TypeError(".proto.HistorySync.callLogRecords: object expected");
                    message.callLogRecords[i] = $root.proto.CallLogRecord.fromObject(object.callLogRecords[i], q + 1);
                }
            }
            switch (object.aiWaitListState) {
            case "IN_WAITLIST":
            case 0:
                message.aiWaitListState = 0;
                break;
            case "AI_AVAILABLE":
            case 1:
                message.aiWaitListState = 1;
                break;
            default:
                if (typeof object.aiWaitListState === "number" && (object.aiWaitListState | 0) === object.aiWaitListState)
                    message.aiWaitListState = object.aiWaitListState;
            }
            if (object.phoneNumberToLidMappings) {
                if (!Array.isArray(object.phoneNumberToLidMappings))
                    throw TypeError(".proto.HistorySync.phoneNumberToLidMappings: array expected");
                message.phoneNumberToLidMappings = Array(object.phoneNumberToLidMappings.length);
                for (var i = 0; i < object.phoneNumberToLidMappings.length; ++i) {
                    if (!$util.isObject(object.phoneNumberToLidMappings[i]))
                        throw TypeError(".proto.HistorySync.phoneNumberToLidMappings: object expected");
                    message.phoneNumberToLidMappings[i] = $root.proto.PhoneNumberToLIDMapping.fromObject(object.phoneNumberToLidMappings[i], q + 1);
                }
            }
            if (object.companionMetaNonce != null)
                message.companionMetaNonce = String(object.companionMetaNonce);
            if (object.shareableChatIdentifierEncryptionKey != null)
                if (typeof object.shareableChatIdentifierEncryptionKey === "string")
                    $util.base64.decode(object.shareableChatIdentifierEncryptionKey, message.shareableChatIdentifierEncryptionKey = $util.newBuffer($util.base64.length(object.shareableChatIdentifierEncryptionKey)), 0);
                else if (object.shareableChatIdentifierEncryptionKey.length >= 0)
                    message.shareableChatIdentifierEncryptionKey = object.shareableChatIdentifierEncryptionKey;
            if (object.accounts) {
                if (!Array.isArray(object.accounts))
                    throw TypeError(".proto.HistorySync.accounts: array expected");
                message.accounts = Array(object.accounts.length);
                for (var i = 0; i < object.accounts.length; ++i) {
                    if (!$util.isObject(object.accounts[i]))
                        throw TypeError(".proto.HistorySync.accounts: object expected");
                    message.accounts[i] = $root.proto.Account.fromObject(object.accounts[i], q + 1);
                }
            }
            if (object.nctSalt != null)
                if (typeof object.nctSalt === "string")
                    $util.base64.decode(object.nctSalt, message.nctSalt = $util.newBuffer($util.base64.length(object.nctSalt)), 0);
                else if (object.nctSalt.length >= 0)
                    message.nctSalt = object.nctSalt;
            if (object.inlineContacts) {
                if (!Array.isArray(object.inlineContacts))
                    throw TypeError(".proto.HistorySync.inlineContacts: array expected");
                message.inlineContacts = Array(object.inlineContacts.length);
                for (var i = 0; i < object.inlineContacts.length; ++i) {
                    if (!$util.isObject(object.inlineContacts[i]))
                        throw TypeError(".proto.HistorySync.inlineContacts: object expected");
                    message.inlineContacts[i] = $root.proto.InlineContact.fromObject(object.inlineContacts[i], q + 1);
                }
            }
            if (object.inlineContactsProvided != null)
                message.inlineContactsProvided = Boolean(object.inlineContactsProvided);
            return message;
        };

        /**
         * Creates a plain object from a HistorySync message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.HistorySync
         * @static
         * @param {proto.HistorySync} message HistorySync
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HistorySync.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults) {
                object.conversations = [];
                object.statusV3Messages = [];
                object.pushnames = [];
                object.recentStickers = [];
                object.pastParticipants = [];
                object.callLogRecords = [];
                object.phoneNumberToLidMappings = [];
                object.accounts = [];
                object.inlineContacts = [];
            }
            if (message.syncType != null && Object.hasOwnProperty.call(message, "syncType"))
                object.syncType = options.enums === String ? $root.proto.HistorySync.HistorySyncType[message.syncType] === undefined ? message.syncType : $root.proto.HistorySync.HistorySyncType[message.syncType] : message.syncType;
            if (message.conversations && message.conversations.length) {
                object.conversations = Array(message.conversations.length);
                for (var j = 0; j < message.conversations.length; ++j)
                    object.conversations[j] = $root.proto.Conversation.toObject(message.conversations[j], options, q + 1);
            }
            if (message.statusV3Messages && message.statusV3Messages.length) {
                object.statusV3Messages = Array(message.statusV3Messages.length);
                for (var j = 0; j < message.statusV3Messages.length; ++j)
                    object.statusV3Messages[j] = $root.proto.WebMessageInfo.toObject(message.statusV3Messages[j], options, q + 1);
            }
            if (message.chunkOrder != null && Object.hasOwnProperty.call(message, "chunkOrder"))
                object.chunkOrder = message.chunkOrder;
            if (message.progress != null && Object.hasOwnProperty.call(message, "progress"))
                object.progress = message.progress;
            if (message.pushnames && message.pushnames.length) {
                object.pushnames = Array(message.pushnames.length);
                for (var j = 0; j < message.pushnames.length; ++j)
                    object.pushnames[j] = $root.proto.Pushname.toObject(message.pushnames[j], options, q + 1);
            }
            if (message.globalSettings != null && Object.hasOwnProperty.call(message, "globalSettings"))
                object.globalSettings = $root.proto.GlobalSettings.toObject(message.globalSettings, options, q + 1);
            if (message.threadIdUserSecret != null && Object.hasOwnProperty.call(message, "threadIdUserSecret"))
                object.threadIdUserSecret = options.bytes === String ? $util.base64.encode(message.threadIdUserSecret, 0, message.threadIdUserSecret.length) : options.bytes === Array ? Array.prototype.slice.call(message.threadIdUserSecret) : message.threadIdUserSecret;
            if (message.threadDsTimeframeOffset != null && Object.hasOwnProperty.call(message, "threadDsTimeframeOffset"))
                object.threadDsTimeframeOffset = message.threadDsTimeframeOffset;
            if (message.recentStickers && message.recentStickers.length) {
                object.recentStickers = Array(message.recentStickers.length);
                for (var j = 0; j < message.recentStickers.length; ++j)
                    object.recentStickers[j] = $root.proto.StickerMetadata.toObject(message.recentStickers[j], options, q + 1);
            }
            if (message.pastParticipants && message.pastParticipants.length) {
                object.pastParticipants = Array(message.pastParticipants.length);
                for (var j = 0; j < message.pastParticipants.length; ++j)
                    object.pastParticipants[j] = $root.proto.PastParticipants.toObject(message.pastParticipants[j], options, q + 1);
            }
            if (message.callLogRecords && message.callLogRecords.length) {
                object.callLogRecords = Array(message.callLogRecords.length);
                for (var j = 0; j < message.callLogRecords.length; ++j)
                    object.callLogRecords[j] = $root.proto.CallLogRecord.toObject(message.callLogRecords[j], options, q + 1);
            }
            if (message.aiWaitListState != null && Object.hasOwnProperty.call(message, "aiWaitListState"))
                object.aiWaitListState = options.enums === String ? $root.proto.HistorySync.BotAIWaitListState[message.aiWaitListState] === undefined ? message.aiWaitListState : $root.proto.HistorySync.BotAIWaitListState[message.aiWaitListState] : message.aiWaitListState;
            if (message.phoneNumberToLidMappings && message.phoneNumberToLidMappings.length) {
                object.phoneNumberToLidMappings = Array(message.phoneNumberToLidMappings.length);
                for (var j = 0; j < message.phoneNumberToLidMappings.length; ++j)
                    object.phoneNumberToLidMappings[j] = $root.proto.PhoneNumberToLIDMapping.toObject(message.phoneNumberToLidMappings[j], options, q + 1);
            }
            if (message.companionMetaNonce != null && Object.hasOwnProperty.call(message, "companionMetaNonce"))
                object.companionMetaNonce = message.companionMetaNonce;
            if (message.shareableChatIdentifierEncryptionKey != null && Object.hasOwnProperty.call(message, "shareableChatIdentifierEncryptionKey"))
                object.shareableChatIdentifierEncryptionKey = options.bytes === String ? $util.base64.encode(message.shareableChatIdentifierEncryptionKey, 0, message.shareableChatIdentifierEncryptionKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.shareableChatIdentifierEncryptionKey) : message.shareableChatIdentifierEncryptionKey;
            if (message.accounts && message.accounts.length) {
                object.accounts = Array(message.accounts.length);
                for (var j = 0; j < message.accounts.length; ++j)
                    object.accounts[j] = $root.proto.Account.toObject(message.accounts[j], options, q + 1);
            }
            if (message.nctSalt != null && Object.hasOwnProperty.call(message, "nctSalt"))
                object.nctSalt = options.bytes === String ? $util.base64.encode(message.nctSalt, 0, message.nctSalt.length) : options.bytes === Array ? Array.prototype.slice.call(message.nctSalt) : message.nctSalt;
            if (message.inlineContacts && message.inlineContacts.length) {
                object.inlineContacts = Array(message.inlineContacts.length);
                for (var j = 0; j < message.inlineContacts.length; ++j)
                    object.inlineContacts[j] = $root.proto.InlineContact.toObject(message.inlineContacts[j], options, q + 1);
            }
            if (message.inlineContactsProvided != null && Object.hasOwnProperty.call(message, "inlineContactsProvided"))
                object.inlineContactsProvided = message.inlineContactsProvided;
            return object;
        };

        /**
         * Converts this HistorySync to JSON.
         * @function toJSON
         * @memberof proto.HistorySync
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HistorySync.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for HistorySync
         * @function getTypeUrl
         * @memberof proto.HistorySync
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        HistorySync.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.HistorySync";
        };

        /**
         * BotAIWaitListState enum.
         * @name proto.HistorySync.BotAIWaitListState
         * @enum {number}
         * @property {number} IN_WAITLIST=0 IN_WAITLIST value
         * @property {number} AI_AVAILABLE=1 AI_AVAILABLE value
         */
        HistorySync.BotAIWaitListState = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "IN_WAITLIST"] = 0;
            values[valuesById[1] = "AI_AVAILABLE"] = 1;
            return values;
        })();

        /**
         * HistorySyncType enum.
         * @name proto.HistorySync.HistorySyncType
         * @enum {number}
         * @property {number} INITIAL_BOOTSTRAP=0 INITIAL_BOOTSTRAP value
         * @property {number} INITIAL_STATUS_V3=1 INITIAL_STATUS_V3 value
         * @property {number} FULL=2 FULL value
         * @property {number} RECENT=3 RECENT value
         * @property {number} PUSH_NAME=4 PUSH_NAME value
         * @property {number} NON_BLOCKING_DATA=5 NON_BLOCKING_DATA value
         * @property {number} ON_DEMAND=6 ON_DEMAND value
         */
        HistorySync.HistorySyncType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "INITIAL_BOOTSTRAP"] = 0;
            values[valuesById[1] = "INITIAL_STATUS_V3"] = 1;
            values[valuesById[2] = "FULL"] = 2;
            values[valuesById[3] = "RECENT"] = 3;
            values[valuesById[4] = "PUSH_NAME"] = 4;
            values[valuesById[5] = "NON_BLOCKING_DATA"] = 5;
            values[valuesById[6] = "ON_DEMAND"] = 6;
            return values;
        })();

        return HistorySync;
    })();

    proto.HistorySyncMsg = (function() {

        /**
         * Properties of a HistorySyncMsg.
         * @memberof proto
         * @interface IHistorySyncMsg
         * @property {proto.IWebMessageInfo|null} [message] HistorySyncMsg message
         * @property {number|Long|null} [msgOrderId] HistorySyncMsg msgOrderId
         */

        /**
         * Constructs a new HistorySyncMsg.
         * @memberof proto
         * @classdesc Represents a HistorySyncMsg.
         * @implements IHistorySyncMsg
         * @constructor
         * @param {proto.IHistorySyncMsg=} [properties] Properties to set
         */
        function HistorySyncMsg(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * HistorySyncMsg message.
         * @member {proto.IWebMessageInfo|null|undefined} message
         * @memberof proto.HistorySyncMsg
         * @instance
         */
        HistorySyncMsg.prototype.message = null;

        /**
         * HistorySyncMsg msgOrderId.
         * @member {number|Long|null|undefined} msgOrderId
         * @memberof proto.HistorySyncMsg
         * @instance
         */
        HistorySyncMsg.prototype.msgOrderId = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySyncMsg.prototype, "_message", {
            get: $util.oneOfGetter($oneOfFields = ["message"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySyncMsg.prototype, "_msgOrderId", {
            get: $util.oneOfGetter($oneOfFields = ["msgOrderId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new HistorySyncMsg instance using the specified properties.
         * @function create
         * @memberof proto.HistorySyncMsg
         * @static
         * @param {proto.IHistorySyncMsg=} [properties] Properties to set
         * @returns {proto.HistorySyncMsg} HistorySyncMsg instance
         */
        HistorySyncMsg.create = function create(properties) {
            return new HistorySyncMsg(properties);
        };

        /**
         * Encodes the specified HistorySyncMsg message. Does not implicitly {@link proto.HistorySyncMsg.verify|verify} messages.
         * @function encode
         * @memberof proto.HistorySyncMsg
         * @static
         * @param {proto.IHistorySyncMsg} message HistorySyncMsg message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HistorySyncMsg.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                $root.proto.WebMessageInfo.encode(message.message, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.msgOrderId != null && Object.hasOwnProperty.call(message, "msgOrderId"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.msgOrderId);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified HistorySyncMsg message, length delimited. Does not implicitly {@link proto.HistorySyncMsg.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.HistorySyncMsg
         * @static
         * @param {proto.IHistorySyncMsg} message HistorySyncMsg message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HistorySyncMsg.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a HistorySyncMsg message from the specified reader or buffer.
         * @function decode
         * @memberof proto.HistorySyncMsg
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.HistorySyncMsg} HistorySyncMsg
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HistorySyncMsg.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.message = $root.proto.WebMessageInfo.decode(reader, reader.uint32(), undefined, q + 1, message.message);
                        message._message = "message";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.msgOrderId = reader.uint64();
                        message._msgOrderId = "msgOrderId";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a HistorySyncMsg message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.HistorySyncMsg
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.HistorySyncMsg} HistorySyncMsg
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HistorySyncMsg.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HistorySyncMsg message.
         * @function verify
         * @memberof proto.HistorySyncMsg
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HistorySyncMsg.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.message != null && Object.hasOwnProperty.call(message, "message")) {
                properties._message = 1;
                {
                    var error = $root.proto.WebMessageInfo.verify(message.message, q + 1);
                    if (error)
                        return "message." + error;
                }
            }
            if (message.msgOrderId != null && Object.hasOwnProperty.call(message, "msgOrderId")) {
                properties._msgOrderId = 1;
                if (!$util.isInteger(message.msgOrderId) && !(message.msgOrderId && $util.isInteger(message.msgOrderId.low) && $util.isInteger(message.msgOrderId.high)))
                    return "msgOrderId: integer|Long expected";
            }
            return null;
        };

        /**
         * Creates a HistorySyncMsg message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.HistorySyncMsg
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.HistorySyncMsg} HistorySyncMsg
         */
        HistorySyncMsg.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.HistorySyncMsg: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.message != null) {
                if (!$util.isObject(object.message))
                    throw TypeError(".proto.HistorySyncMsg.message: object expected");
                message.message = $root.proto.WebMessageInfo.fromObject(object.message, q + 1);
            }
            if (object.msgOrderId != null)
                if ($util.Long)
                    message.msgOrderId = $util.Long.fromValue(object.msgOrderId, true);
                else if (typeof object.msgOrderId === "string")
                    message.msgOrderId = parseInt(object.msgOrderId, 10);
                else if (typeof object.msgOrderId === "number")
                    message.msgOrderId = object.msgOrderId;
                else if (typeof object.msgOrderId === "object")
                    message.msgOrderId = new $util.LongBits(object.msgOrderId.low >>> 0, object.msgOrderId.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a HistorySyncMsg message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.HistorySyncMsg
         * @static
         * @param {proto.HistorySyncMsg} message HistorySyncMsg
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HistorySyncMsg.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                object.message = $root.proto.WebMessageInfo.toObject(message.message, options, q + 1);
            if (message.msgOrderId != null && Object.hasOwnProperty.call(message, "msgOrderId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.msgOrderId = typeof message.msgOrderId === "number" ? BigInt(message.msgOrderId) : $util.Long.fromBits(message.msgOrderId.low >>> 0, message.msgOrderId.high >>> 0, true).toBigInt();
                else if (typeof message.msgOrderId === "number")
                    object.msgOrderId = options.longs === String ? String(message.msgOrderId) : message.msgOrderId;
                else
                    object.msgOrderId = options.longs === String ? $util.Long.prototype.toString.call(message.msgOrderId) : options.longs === Number ? new $util.LongBits(message.msgOrderId.low >>> 0, message.msgOrderId.high >>> 0).toNumber(true) : message.msgOrderId;
            return object;
        };

        /**
         * Converts this HistorySyncMsg to JSON.
         * @function toJSON
         * @memberof proto.HistorySyncMsg
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HistorySyncMsg.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for HistorySyncMsg
         * @function getTypeUrl
         * @memberof proto.HistorySyncMsg
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        HistorySyncMsg.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.HistorySyncMsg";
        };

        return HistorySyncMsg;
    })();

    proto.HydratedTemplateButton = (function() {

        /**
         * Properties of a HydratedTemplateButton.
         * @memberof proto
         * @interface IHydratedTemplateButton
         * @property {number|null} [index] HydratedTemplateButton index
         * @property {proto.HydratedTemplateButton.IHydratedQuickReplyButton|null} [quickReplyButton] HydratedTemplateButton quickReplyButton
         * @property {proto.HydratedTemplateButton.IHydratedURLButton|null} [urlButton] HydratedTemplateButton urlButton
         * @property {proto.HydratedTemplateButton.IHydratedCallButton|null} [callButton] HydratedTemplateButton callButton
         */

        /**
         * Constructs a new HydratedTemplateButton.
         * @memberof proto
         * @classdesc Represents a HydratedTemplateButton.
         * @implements IHydratedTemplateButton
         * @constructor
         * @param {proto.IHydratedTemplateButton=} [properties] Properties to set
         */
        function HydratedTemplateButton(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * HydratedTemplateButton index.
         * @member {number|null|undefined} index
         * @memberof proto.HydratedTemplateButton
         * @instance
         */
        HydratedTemplateButton.prototype.index = null;

        /**
         * HydratedTemplateButton quickReplyButton.
         * @member {proto.HydratedTemplateButton.IHydratedQuickReplyButton|null|undefined} quickReplyButton
         * @memberof proto.HydratedTemplateButton
         * @instance
         */
        HydratedTemplateButton.prototype.quickReplyButton = null;

        /**
         * HydratedTemplateButton urlButton.
         * @member {proto.HydratedTemplateButton.IHydratedURLButton|null|undefined} urlButton
         * @memberof proto.HydratedTemplateButton
         * @instance
         */
        HydratedTemplateButton.prototype.urlButton = null;

        /**
         * HydratedTemplateButton callButton.
         * @member {proto.HydratedTemplateButton.IHydratedCallButton|null|undefined} callButton
         * @memberof proto.HydratedTemplateButton
         * @instance
         */
        HydratedTemplateButton.prototype.callButton = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HydratedTemplateButton.prototype, "_index", {
            get: $util.oneOfGetter($oneOfFields = ["index"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * HydratedTemplateButton hydratedButton.
         * @member {"quickReplyButton"|"urlButton"|"callButton"|undefined} hydratedButton
         * @memberof proto.HydratedTemplateButton
         * @instance
         */
        Object.defineProperty(HydratedTemplateButton.prototype, "hydratedButton", {
            get: $util.oneOfGetter($oneOfFields = ["quickReplyButton", "urlButton", "callButton"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new HydratedTemplateButton instance using the specified properties.
         * @function create
         * @memberof proto.HydratedTemplateButton
         * @static
         * @param {proto.IHydratedTemplateButton=} [properties] Properties to set
         * @returns {proto.HydratedTemplateButton} HydratedTemplateButton instance
         */
        HydratedTemplateButton.create = function create(properties) {
            return new HydratedTemplateButton(properties);
        };

        /**
         * Encodes the specified HydratedTemplateButton message. Does not implicitly {@link proto.HydratedTemplateButton.verify|verify} messages.
         * @function encode
         * @memberof proto.HydratedTemplateButton
         * @static
         * @param {proto.IHydratedTemplateButton} message HydratedTemplateButton message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HydratedTemplateButton.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.quickReplyButton != null && Object.hasOwnProperty.call(message, "quickReplyButton"))
                $root.proto.HydratedTemplateButton.HydratedQuickReplyButton.encode(message.quickReplyButton, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.urlButton != null && Object.hasOwnProperty.call(message, "urlButton"))
                $root.proto.HydratedTemplateButton.HydratedURLButton.encode(message.urlButton, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.callButton != null && Object.hasOwnProperty.call(message, "callButton"))
                $root.proto.HydratedTemplateButton.HydratedCallButton.encode(message.callButton, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.index);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified HydratedTemplateButton message, length delimited. Does not implicitly {@link proto.HydratedTemplateButton.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.HydratedTemplateButton
         * @static
         * @param {proto.IHydratedTemplateButton} message HydratedTemplateButton message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        HydratedTemplateButton.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a HydratedTemplateButton message from the specified reader or buffer.
         * @function decode
         * @memberof proto.HydratedTemplateButton
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.HydratedTemplateButton} HydratedTemplateButton
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HydratedTemplateButton.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 4: {
                        if (u !== 0)
                            break;
                        message.index = reader.uint32();
                        message._index = "index";
                        continue;
                    }
                case 1: {
                        if (u !== 2)
                            break;
                        message.quickReplyButton = $root.proto.HydratedTemplateButton.HydratedQuickReplyButton.decode(reader, reader.uint32(), undefined, q + 1, message.quickReplyButton);
                        message.hydratedButton = "quickReplyButton";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.urlButton = $root.proto.HydratedTemplateButton.HydratedURLButton.decode(reader, reader.uint32(), undefined, q + 1, message.urlButton);
                        message.hydratedButton = "urlButton";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.callButton = $root.proto.HydratedTemplateButton.HydratedCallButton.decode(reader, reader.uint32(), undefined, q + 1, message.callButton);
                        message.hydratedButton = "callButton";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a HydratedTemplateButton message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.HydratedTemplateButton
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.HydratedTemplateButton} HydratedTemplateButton
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        HydratedTemplateButton.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a HydratedTemplateButton message.
         * @function verify
         * @memberof proto.HydratedTemplateButton
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        HydratedTemplateButton.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.index != null && Object.hasOwnProperty.call(message, "index")) {
                properties._index = 1;
                if (!$util.isInteger(message.index))
                    return "index: integer expected";
            }
            if (message.quickReplyButton != null && Object.hasOwnProperty.call(message, "quickReplyButton")) {
                properties.hydratedButton = 1;
                {
                    var error = $root.proto.HydratedTemplateButton.HydratedQuickReplyButton.verify(message.quickReplyButton, q + 1);
                    if (error)
                        return "quickReplyButton." + error;
                }
            }
            if (message.urlButton != null && Object.hasOwnProperty.call(message, "urlButton")) {
                if (properties.hydratedButton === 1)
                    return "hydratedButton: multiple values";
                properties.hydratedButton = 1;
                {
                    var error = $root.proto.HydratedTemplateButton.HydratedURLButton.verify(message.urlButton, q + 1);
                    if (error)
                        return "urlButton." + error;
                }
            }
            if (message.callButton != null && Object.hasOwnProperty.call(message, "callButton")) {
                if (properties.hydratedButton === 1)
                    return "hydratedButton: multiple values";
                properties.hydratedButton = 1;
                {
                    var error = $root.proto.HydratedTemplateButton.HydratedCallButton.verify(message.callButton, q + 1);
                    if (error)
                        return "callButton." + error;
                }
            }
            return null;
        };

        /**
         * Creates a HydratedTemplateButton message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.HydratedTemplateButton
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.HydratedTemplateButton} HydratedTemplateButton
         */
        HydratedTemplateButton.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.HydratedTemplateButton: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.index != null)
                message.index = object.index >>> 0;
            if (object.quickReplyButton != null) {
                if (!$util.isObject(object.quickReplyButton))
                    throw TypeError(".proto.HydratedTemplateButton.quickReplyButton: object expected");
                message.quickReplyButton = $root.proto.HydratedTemplateButton.HydratedQuickReplyButton.fromObject(object.quickReplyButton, q + 1);
            }
            if (object.urlButton != null) {
                if (!$util.isObject(object.urlButton))
                    throw TypeError(".proto.HydratedTemplateButton.urlButton: object expected");
                message.urlButton = $root.proto.HydratedTemplateButton.HydratedURLButton.fromObject(object.urlButton, q + 1);
            }
            if (object.callButton != null) {
                if (!$util.isObject(object.callButton))
                    throw TypeError(".proto.HydratedTemplateButton.callButton: object expected");
                message.callButton = $root.proto.HydratedTemplateButton.HydratedCallButton.fromObject(object.callButton, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a HydratedTemplateButton message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.HydratedTemplateButton
         * @static
         * @param {proto.HydratedTemplateButton} message HydratedTemplateButton
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        HydratedTemplateButton.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.quickReplyButton != null && Object.hasOwnProperty.call(message, "quickReplyButton")) {
                object.quickReplyButton = $root.proto.HydratedTemplateButton.HydratedQuickReplyButton.toObject(message.quickReplyButton, options, q + 1);
                if (options.oneofs)
                    object.hydratedButton = "quickReplyButton";
            }
            if (message.urlButton != null && Object.hasOwnProperty.call(message, "urlButton")) {
                object.urlButton = $root.proto.HydratedTemplateButton.HydratedURLButton.toObject(message.urlButton, options, q + 1);
                if (options.oneofs)
                    object.hydratedButton = "urlButton";
            }
            if (message.callButton != null && Object.hasOwnProperty.call(message, "callButton")) {
                object.callButton = $root.proto.HydratedTemplateButton.HydratedCallButton.toObject(message.callButton, options, q + 1);
                if (options.oneofs)
                    object.hydratedButton = "callButton";
            }
            if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                object.index = message.index;
            return object;
        };

        /**
         * Converts this HydratedTemplateButton to JSON.
         * @function toJSON
         * @memberof proto.HydratedTemplateButton
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        HydratedTemplateButton.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for HydratedTemplateButton
         * @function getTypeUrl
         * @memberof proto.HydratedTemplateButton
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        HydratedTemplateButton.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.HydratedTemplateButton";
        };

        HydratedTemplateButton.HydratedCallButton = (function() {

            /**
             * Properties of a HydratedCallButton.
             * @memberof proto.HydratedTemplateButton
             * @interface IHydratedCallButton
             * @property {string|null} [displayText] HydratedCallButton displayText
             * @property {string|null} [phoneNumber] HydratedCallButton phoneNumber
             */

            /**
             * Constructs a new HydratedCallButton.
             * @memberof proto.HydratedTemplateButton
             * @classdesc Represents a HydratedCallButton.
             * @implements IHydratedCallButton
             * @constructor
             * @param {proto.HydratedTemplateButton.IHydratedCallButton=} [properties] Properties to set
             */
            function HydratedCallButton(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * HydratedCallButton displayText.
             * @member {string|null|undefined} displayText
             * @memberof proto.HydratedTemplateButton.HydratedCallButton
             * @instance
             */
            HydratedCallButton.prototype.displayText = null;

            /**
             * HydratedCallButton phoneNumber.
             * @member {string|null|undefined} phoneNumber
             * @memberof proto.HydratedTemplateButton.HydratedCallButton
             * @instance
             */
            HydratedCallButton.prototype.phoneNumber = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HydratedCallButton.prototype, "_displayText", {
                get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HydratedCallButton.prototype, "_phoneNumber", {
                get: $util.oneOfGetter($oneOfFields = ["phoneNumber"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new HydratedCallButton instance using the specified properties.
             * @function create
             * @memberof proto.HydratedTemplateButton.HydratedCallButton
             * @static
             * @param {proto.HydratedTemplateButton.IHydratedCallButton=} [properties] Properties to set
             * @returns {proto.HydratedTemplateButton.HydratedCallButton} HydratedCallButton instance
             */
            HydratedCallButton.create = function create(properties) {
                return new HydratedCallButton(properties);
            };

            /**
             * Encodes the specified HydratedCallButton message. Does not implicitly {@link proto.HydratedTemplateButton.HydratedCallButton.verify|verify} messages.
             * @function encode
             * @memberof proto.HydratedTemplateButton.HydratedCallButton
             * @static
             * @param {proto.HydratedTemplateButton.IHydratedCallButton} message HydratedCallButton message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HydratedCallButton.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.displayText);
                if (message.phoneNumber != null && Object.hasOwnProperty.call(message, "phoneNumber"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.phoneNumber);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified HydratedCallButton message, length delimited. Does not implicitly {@link proto.HydratedTemplateButton.HydratedCallButton.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.HydratedTemplateButton.HydratedCallButton
             * @static
             * @param {proto.HydratedTemplateButton.IHydratedCallButton} message HydratedCallButton message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HydratedCallButton.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a HydratedCallButton message from the specified reader or buffer.
             * @function decode
             * @memberof proto.HydratedTemplateButton.HydratedCallButton
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.HydratedTemplateButton.HydratedCallButton} HydratedCallButton
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HydratedCallButton.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.displayText = reader.stringVerify();
                            message._displayText = "displayText";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.phoneNumber = reader.stringVerify();
                            message._phoneNumber = "phoneNumber";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a HydratedCallButton message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.HydratedTemplateButton.HydratedCallButton
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.HydratedTemplateButton.HydratedCallButton} HydratedCallButton
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HydratedCallButton.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a HydratedCallButton message.
             * @function verify
             * @memberof proto.HydratedTemplateButton.HydratedCallButton
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            HydratedCallButton.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText")) {
                    properties._displayText = 1;
                    if (!$util.isString(message.displayText))
                        return "displayText: string expected";
                }
                if (message.phoneNumber != null && Object.hasOwnProperty.call(message, "phoneNumber")) {
                    properties._phoneNumber = 1;
                    if (!$util.isString(message.phoneNumber))
                        return "phoneNumber: string expected";
                }
                return null;
            };

            /**
             * Creates a HydratedCallButton message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.HydratedTemplateButton.HydratedCallButton
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.HydratedTemplateButton.HydratedCallButton} HydratedCallButton
             */
            HydratedCallButton.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.HydratedTemplateButton.HydratedCallButton: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.displayText != null)
                    message.displayText = String(object.displayText);
                if (object.phoneNumber != null)
                    message.phoneNumber = String(object.phoneNumber);
                return message;
            };

            /**
             * Creates a plain object from a HydratedCallButton message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.HydratedTemplateButton.HydratedCallButton
             * @static
             * @param {proto.HydratedTemplateButton.HydratedCallButton} message HydratedCallButton
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            HydratedCallButton.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText"))
                    object.displayText = message.displayText;
                if (message.phoneNumber != null && Object.hasOwnProperty.call(message, "phoneNumber"))
                    object.phoneNumber = message.phoneNumber;
                return object;
            };

            /**
             * Converts this HydratedCallButton to JSON.
             * @function toJSON
             * @memberof proto.HydratedTemplateButton.HydratedCallButton
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            HydratedCallButton.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for HydratedCallButton
             * @function getTypeUrl
             * @memberof proto.HydratedTemplateButton.HydratedCallButton
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            HydratedCallButton.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.HydratedTemplateButton.HydratedCallButton";
            };

            return HydratedCallButton;
        })();

        HydratedTemplateButton.HydratedQuickReplyButton = (function() {

            /**
             * Properties of a HydratedQuickReplyButton.
             * @memberof proto.HydratedTemplateButton
             * @interface IHydratedQuickReplyButton
             * @property {string|null} [displayText] HydratedQuickReplyButton displayText
             * @property {string|null} [id] HydratedQuickReplyButton id
             */

            /**
             * Constructs a new HydratedQuickReplyButton.
             * @memberof proto.HydratedTemplateButton
             * @classdesc Represents a HydratedQuickReplyButton.
             * @implements IHydratedQuickReplyButton
             * @constructor
             * @param {proto.HydratedTemplateButton.IHydratedQuickReplyButton=} [properties] Properties to set
             */
            function HydratedQuickReplyButton(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * HydratedQuickReplyButton displayText.
             * @member {string|null|undefined} displayText
             * @memberof proto.HydratedTemplateButton.HydratedQuickReplyButton
             * @instance
             */
            HydratedQuickReplyButton.prototype.displayText = null;

            /**
             * HydratedQuickReplyButton id.
             * @member {string|null|undefined} id
             * @memberof proto.HydratedTemplateButton.HydratedQuickReplyButton
             * @instance
             */
            HydratedQuickReplyButton.prototype.id = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HydratedQuickReplyButton.prototype, "_displayText", {
                get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HydratedQuickReplyButton.prototype, "_id", {
                get: $util.oneOfGetter($oneOfFields = ["id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new HydratedQuickReplyButton instance using the specified properties.
             * @function create
             * @memberof proto.HydratedTemplateButton.HydratedQuickReplyButton
             * @static
             * @param {proto.HydratedTemplateButton.IHydratedQuickReplyButton=} [properties] Properties to set
             * @returns {proto.HydratedTemplateButton.HydratedQuickReplyButton} HydratedQuickReplyButton instance
             */
            HydratedQuickReplyButton.create = function create(properties) {
                return new HydratedQuickReplyButton(properties);
            };

            /**
             * Encodes the specified HydratedQuickReplyButton message. Does not implicitly {@link proto.HydratedTemplateButton.HydratedQuickReplyButton.verify|verify} messages.
             * @function encode
             * @memberof proto.HydratedTemplateButton.HydratedQuickReplyButton
             * @static
             * @param {proto.HydratedTemplateButton.IHydratedQuickReplyButton} message HydratedQuickReplyButton message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HydratedQuickReplyButton.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.displayText);
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.id);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified HydratedQuickReplyButton message, length delimited. Does not implicitly {@link proto.HydratedTemplateButton.HydratedQuickReplyButton.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.HydratedTemplateButton.HydratedQuickReplyButton
             * @static
             * @param {proto.HydratedTemplateButton.IHydratedQuickReplyButton} message HydratedQuickReplyButton message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HydratedQuickReplyButton.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a HydratedQuickReplyButton message from the specified reader or buffer.
             * @function decode
             * @memberof proto.HydratedTemplateButton.HydratedQuickReplyButton
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.HydratedTemplateButton.HydratedQuickReplyButton} HydratedQuickReplyButton
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HydratedQuickReplyButton.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.displayText = reader.stringVerify();
                            message._displayText = "displayText";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.id = reader.stringVerify();
                            message._id = "id";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a HydratedQuickReplyButton message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.HydratedTemplateButton.HydratedQuickReplyButton
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.HydratedTemplateButton.HydratedQuickReplyButton} HydratedQuickReplyButton
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HydratedQuickReplyButton.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a HydratedQuickReplyButton message.
             * @function verify
             * @memberof proto.HydratedTemplateButton.HydratedQuickReplyButton
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            HydratedQuickReplyButton.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText")) {
                    properties._displayText = 1;
                    if (!$util.isString(message.displayText))
                        return "displayText: string expected";
                }
                if (message.id != null && Object.hasOwnProperty.call(message, "id")) {
                    properties._id = 1;
                    if (!$util.isString(message.id))
                        return "id: string expected";
                }
                return null;
            };

            /**
             * Creates a HydratedQuickReplyButton message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.HydratedTemplateButton.HydratedQuickReplyButton
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.HydratedTemplateButton.HydratedQuickReplyButton} HydratedQuickReplyButton
             */
            HydratedQuickReplyButton.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.HydratedTemplateButton.HydratedQuickReplyButton: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.displayText != null)
                    message.displayText = String(object.displayText);
                if (object.id != null)
                    message.id = String(object.id);
                return message;
            };

            /**
             * Creates a plain object from a HydratedQuickReplyButton message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.HydratedTemplateButton.HydratedQuickReplyButton
             * @static
             * @param {proto.HydratedTemplateButton.HydratedQuickReplyButton} message HydratedQuickReplyButton
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            HydratedQuickReplyButton.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText"))
                    object.displayText = message.displayText;
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    object.id = message.id;
                return object;
            };

            /**
             * Converts this HydratedQuickReplyButton to JSON.
             * @function toJSON
             * @memberof proto.HydratedTemplateButton.HydratedQuickReplyButton
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            HydratedQuickReplyButton.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for HydratedQuickReplyButton
             * @function getTypeUrl
             * @memberof proto.HydratedTemplateButton.HydratedQuickReplyButton
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            HydratedQuickReplyButton.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.HydratedTemplateButton.HydratedQuickReplyButton";
            };

            return HydratedQuickReplyButton;
        })();

        HydratedTemplateButton.HydratedURLButton = (function() {

            /**
             * Properties of a HydratedURLButton.
             * @memberof proto.HydratedTemplateButton
             * @interface IHydratedURLButton
             * @property {string|null} [displayText] HydratedURLButton displayText
             * @property {string|null} [url] HydratedURLButton url
             * @property {string|null} [consentedUsersUrl] HydratedURLButton consentedUsersUrl
             * @property {proto.HydratedTemplateButton.HydratedURLButton.WebviewPresentationType|null} [webviewPresentation] HydratedURLButton webviewPresentation
             */

            /**
             * Constructs a new HydratedURLButton.
             * @memberof proto.HydratedTemplateButton
             * @classdesc Represents a HydratedURLButton.
             * @implements IHydratedURLButton
             * @constructor
             * @param {proto.HydratedTemplateButton.IHydratedURLButton=} [properties] Properties to set
             */
            function HydratedURLButton(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * HydratedURLButton displayText.
             * @member {string|null|undefined} displayText
             * @memberof proto.HydratedTemplateButton.HydratedURLButton
             * @instance
             */
            HydratedURLButton.prototype.displayText = null;

            /**
             * HydratedURLButton url.
             * @member {string|null|undefined} url
             * @memberof proto.HydratedTemplateButton.HydratedURLButton
             * @instance
             */
            HydratedURLButton.prototype.url = null;

            /**
             * HydratedURLButton consentedUsersUrl.
             * @member {string|null|undefined} consentedUsersUrl
             * @memberof proto.HydratedTemplateButton.HydratedURLButton
             * @instance
             */
            HydratedURLButton.prototype.consentedUsersUrl = null;

            /**
             * HydratedURLButton webviewPresentation.
             * @member {proto.HydratedTemplateButton.HydratedURLButton.WebviewPresentationType|null|undefined} webviewPresentation
             * @memberof proto.HydratedTemplateButton.HydratedURLButton
             * @instance
             */
            HydratedURLButton.prototype.webviewPresentation = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HydratedURLButton.prototype, "_displayText", {
                get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HydratedURLButton.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HydratedURLButton.prototype, "_consentedUsersUrl", {
                get: $util.oneOfGetter($oneOfFields = ["consentedUsersUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HydratedURLButton.prototype, "_webviewPresentation", {
                get: $util.oneOfGetter($oneOfFields = ["webviewPresentation"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new HydratedURLButton instance using the specified properties.
             * @function create
             * @memberof proto.HydratedTemplateButton.HydratedURLButton
             * @static
             * @param {proto.HydratedTemplateButton.IHydratedURLButton=} [properties] Properties to set
             * @returns {proto.HydratedTemplateButton.HydratedURLButton} HydratedURLButton instance
             */
            HydratedURLButton.create = function create(properties) {
                return new HydratedURLButton(properties);
            };

            /**
             * Encodes the specified HydratedURLButton message. Does not implicitly {@link proto.HydratedTemplateButton.HydratedURLButton.verify|verify} messages.
             * @function encode
             * @memberof proto.HydratedTemplateButton.HydratedURLButton
             * @static
             * @param {proto.HydratedTemplateButton.IHydratedURLButton} message HydratedURLButton message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HydratedURLButton.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.displayText);
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.url);
                if (message.consentedUsersUrl != null && Object.hasOwnProperty.call(message, "consentedUsersUrl"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.consentedUsersUrl);
                if (message.webviewPresentation != null && Object.hasOwnProperty.call(message, "webviewPresentation"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int32(message.webviewPresentation);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified HydratedURLButton message, length delimited. Does not implicitly {@link proto.HydratedTemplateButton.HydratedURLButton.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.HydratedTemplateButton.HydratedURLButton
             * @static
             * @param {proto.HydratedTemplateButton.IHydratedURLButton} message HydratedURLButton message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HydratedURLButton.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a HydratedURLButton message from the specified reader or buffer.
             * @function decode
             * @memberof proto.HydratedTemplateButton.HydratedURLButton
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.HydratedTemplateButton.HydratedURLButton} HydratedURLButton
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HydratedURLButton.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.displayText = reader.stringVerify();
                            message._displayText = "displayText";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.url = reader.stringVerify();
                            message._url = "url";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.consentedUsersUrl = reader.stringVerify();
                            message._consentedUsersUrl = "consentedUsersUrl";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.webviewPresentation = reader.int32();
                            message._webviewPresentation = "webviewPresentation";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a HydratedURLButton message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.HydratedTemplateButton.HydratedURLButton
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.HydratedTemplateButton.HydratedURLButton} HydratedURLButton
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HydratedURLButton.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a HydratedURLButton message.
             * @function verify
             * @memberof proto.HydratedTemplateButton.HydratedURLButton
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            HydratedURLButton.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText")) {
                    properties._displayText = 1;
                    if (!$util.isString(message.displayText))
                        return "displayText: string expected";
                }
                if (message.url != null && Object.hasOwnProperty.call(message, "url")) {
                    properties._url = 1;
                    if (!$util.isString(message.url))
                        return "url: string expected";
                }
                if (message.consentedUsersUrl != null && Object.hasOwnProperty.call(message, "consentedUsersUrl")) {
                    properties._consentedUsersUrl = 1;
                    if (!$util.isString(message.consentedUsersUrl))
                        return "consentedUsersUrl: string expected";
                }
                if (message.webviewPresentation != null && Object.hasOwnProperty.call(message, "webviewPresentation")) {
                    properties._webviewPresentation = 1;
                    if (typeof message.webviewPresentation !== "number" || (message.webviewPresentation | 0) !== message.webviewPresentation)
                        return "webviewPresentation: enum value expected";
                }
                return null;
            };

            /**
             * Creates a HydratedURLButton message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.HydratedTemplateButton.HydratedURLButton
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.HydratedTemplateButton.HydratedURLButton} HydratedURLButton
             */
            HydratedURLButton.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.HydratedTemplateButton.HydratedURLButton: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.displayText != null)
                    message.displayText = String(object.displayText);
                if (object.url != null)
                    message.url = String(object.url);
                if (object.consentedUsersUrl != null)
                    message.consentedUsersUrl = String(object.consentedUsersUrl);
                switch (object.webviewPresentation) {
                case "FULL":
                case 1:
                    message.webviewPresentation = 1;
                    break;
                case "TALL":
                case 2:
                    message.webviewPresentation = 2;
                    break;
                case "COMPACT":
                case 3:
                    message.webviewPresentation = 3;
                    break;
                default:
                    if (typeof object.webviewPresentation === "number" && (object.webviewPresentation | 0) === object.webviewPresentation)
                        message.webviewPresentation = object.webviewPresentation;
                }
                return message;
            };

            /**
             * Creates a plain object from a HydratedURLButton message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.HydratedTemplateButton.HydratedURLButton
             * @static
             * @param {proto.HydratedTemplateButton.HydratedURLButton} message HydratedURLButton
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            HydratedURLButton.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText"))
                    object.displayText = message.displayText;
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    object.url = message.url;
                if (message.consentedUsersUrl != null && Object.hasOwnProperty.call(message, "consentedUsersUrl"))
                    object.consentedUsersUrl = message.consentedUsersUrl;
                if (message.webviewPresentation != null && Object.hasOwnProperty.call(message, "webviewPresentation"))
                    object.webviewPresentation = options.enums === String ? $root.proto.HydratedTemplateButton.HydratedURLButton.WebviewPresentationType[message.webviewPresentation] === undefined ? message.webviewPresentation : $root.proto.HydratedTemplateButton.HydratedURLButton.WebviewPresentationType[message.webviewPresentation] : message.webviewPresentation;
                return object;
            };

            /**
             * Converts this HydratedURLButton to JSON.
             * @function toJSON
             * @memberof proto.HydratedTemplateButton.HydratedURLButton
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            HydratedURLButton.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for HydratedURLButton
             * @function getTypeUrl
             * @memberof proto.HydratedTemplateButton.HydratedURLButton
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            HydratedURLButton.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.HydratedTemplateButton.HydratedURLButton";
            };

            /**
             * WebviewPresentationType enum.
             * @name proto.HydratedTemplateButton.HydratedURLButton.WebviewPresentationType
             * @enum {number}
             * @property {number} FULL=1 FULL value
             * @property {number} TALL=2 TALL value
             * @property {number} COMPACT=3 COMPACT value
             */
            HydratedURLButton.WebviewPresentationType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[1] = "FULL"] = 1;
                values[valuesById[2] = "TALL"] = 2;
                values[valuesById[3] = "COMPACT"] = 3;
                return values;
            })();

            return HydratedURLButton;
        })();

        return HydratedTemplateButton;
    })();

    proto.IdentityKeyPairStructure = (function() {

        /**
         * Properties of an IdentityKeyPairStructure.
         * @memberof proto
         * @interface IIdentityKeyPairStructure
         * @property {Uint8Array|null} [publicKey] IdentityKeyPairStructure publicKey
         * @property {Uint8Array|null} [privateKey] IdentityKeyPairStructure privateKey
         */

        /**
         * Constructs a new IdentityKeyPairStructure.
         * @memberof proto
         * @classdesc Represents an IdentityKeyPairStructure.
         * @implements IIdentityKeyPairStructure
         * @constructor
         * @param {proto.IIdentityKeyPairStructure=} [properties] Properties to set
         */
        function IdentityKeyPairStructure(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * IdentityKeyPairStructure publicKey.
         * @member {Uint8Array|null|undefined} publicKey
         * @memberof proto.IdentityKeyPairStructure
         * @instance
         */
        IdentityKeyPairStructure.prototype.publicKey = null;

        /**
         * IdentityKeyPairStructure privateKey.
         * @member {Uint8Array|null|undefined} privateKey
         * @memberof proto.IdentityKeyPairStructure
         * @instance
         */
        IdentityKeyPairStructure.prototype.privateKey = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(IdentityKeyPairStructure.prototype, "_publicKey", {
            get: $util.oneOfGetter($oneOfFields = ["publicKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(IdentityKeyPairStructure.prototype, "_privateKey", {
            get: $util.oneOfGetter($oneOfFields = ["privateKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new IdentityKeyPairStructure instance using the specified properties.
         * @function create
         * @memberof proto.IdentityKeyPairStructure
         * @static
         * @param {proto.IIdentityKeyPairStructure=} [properties] Properties to set
         * @returns {proto.IdentityKeyPairStructure} IdentityKeyPairStructure instance
         */
        IdentityKeyPairStructure.create = function create(properties) {
            return new IdentityKeyPairStructure(properties);
        };

        /**
         * Encodes the specified IdentityKeyPairStructure message. Does not implicitly {@link proto.IdentityKeyPairStructure.verify|verify} messages.
         * @function encode
         * @memberof proto.IdentityKeyPairStructure
         * @static
         * @param {proto.IIdentityKeyPairStructure} message IdentityKeyPairStructure message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IdentityKeyPairStructure.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.publicKey);
            if (message.privateKey != null && Object.hasOwnProperty.call(message, "privateKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.privateKey);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified IdentityKeyPairStructure message, length delimited. Does not implicitly {@link proto.IdentityKeyPairStructure.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.IdentityKeyPairStructure
         * @static
         * @param {proto.IIdentityKeyPairStructure} message IdentityKeyPairStructure message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IdentityKeyPairStructure.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an IdentityKeyPairStructure message from the specified reader or buffer.
         * @function decode
         * @memberof proto.IdentityKeyPairStructure
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.IdentityKeyPairStructure} IdentityKeyPairStructure
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IdentityKeyPairStructure.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.publicKey = reader.bytes();
                        message._publicKey = "publicKey";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.privateKey = reader.bytes();
                        message._privateKey = "privateKey";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes an IdentityKeyPairStructure message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.IdentityKeyPairStructure
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.IdentityKeyPairStructure} IdentityKeyPairStructure
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IdentityKeyPairStructure.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an IdentityKeyPairStructure message.
         * @function verify
         * @memberof proto.IdentityKeyPairStructure
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        IdentityKeyPairStructure.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey")) {
                properties._publicKey = 1;
                if (!(message.publicKey && typeof message.publicKey.length === "number" || $util.isString(message.publicKey)))
                    return "publicKey: buffer expected";
            }
            if (message.privateKey != null && Object.hasOwnProperty.call(message, "privateKey")) {
                properties._privateKey = 1;
                if (!(message.privateKey && typeof message.privateKey.length === "number" || $util.isString(message.privateKey)))
                    return "privateKey: buffer expected";
            }
            return null;
        };

        /**
         * Creates an IdentityKeyPairStructure message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.IdentityKeyPairStructure
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.IdentityKeyPairStructure} IdentityKeyPairStructure
         */
        IdentityKeyPairStructure.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.IdentityKeyPairStructure: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.publicKey != null)
                if (typeof object.publicKey === "string")
                    $util.base64.decode(object.publicKey, message.publicKey = $util.newBuffer($util.base64.length(object.publicKey)), 0);
                else if (object.publicKey.length >= 0)
                    message.publicKey = object.publicKey;
            if (object.privateKey != null)
                if (typeof object.privateKey === "string")
                    $util.base64.decode(object.privateKey, message.privateKey = $util.newBuffer($util.base64.length(object.privateKey)), 0);
                else if (object.privateKey.length >= 0)
                    message.privateKey = object.privateKey;
            return message;
        };

        /**
         * Creates a plain object from an IdentityKeyPairStructure message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.IdentityKeyPairStructure
         * @static
         * @param {proto.IdentityKeyPairStructure} message IdentityKeyPairStructure
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        IdentityKeyPairStructure.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                object.publicKey = options.bytes === String ? $util.base64.encode(message.publicKey, 0, message.publicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.publicKey) : message.publicKey;
            if (message.privateKey != null && Object.hasOwnProperty.call(message, "privateKey"))
                object.privateKey = options.bytes === String ? $util.base64.encode(message.privateKey, 0, message.privateKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.privateKey) : message.privateKey;
            return object;
        };

        /**
         * Converts this IdentityKeyPairStructure to JSON.
         * @function toJSON
         * @memberof proto.IdentityKeyPairStructure
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        IdentityKeyPairStructure.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for IdentityKeyPairStructure
         * @function getTypeUrl
         * @memberof proto.IdentityKeyPairStructure
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        IdentityKeyPairStructure.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.IdentityKeyPairStructure";
        };

        return IdentityKeyPairStructure;
    })();

    proto.InThreadSurveyMetadata = (function() {

        /**
         * Properties of an InThreadSurveyMetadata.
         * @memberof proto
         * @interface IInThreadSurveyMetadata
         * @property {string|null} [tessaSessionId] InThreadSurveyMetadata tessaSessionId
         * @property {string|null} [simonSessionId] InThreadSurveyMetadata simonSessionId
         * @property {string|null} [simonSurveyId] InThreadSurveyMetadata simonSurveyId
         * @property {string|null} [tessaRootId] InThreadSurveyMetadata tessaRootId
         * @property {string|null} [requestId] InThreadSurveyMetadata requestId
         * @property {string|null} [tessaEvent] InThreadSurveyMetadata tessaEvent
         * @property {string|null} [invitationHeaderText] InThreadSurveyMetadata invitationHeaderText
         * @property {string|null} [invitationBodyText] InThreadSurveyMetadata invitationBodyText
         * @property {string|null} [invitationCtaText] InThreadSurveyMetadata invitationCtaText
         * @property {string|null} [invitationCtaUrl] InThreadSurveyMetadata invitationCtaUrl
         * @property {string|null} [surveyTitle] InThreadSurveyMetadata surveyTitle
         * @property {Array.<proto.InThreadSurveyMetadata.IInThreadSurveyQuestion>|null} [questions] InThreadSurveyMetadata questions
         * @property {string|null} [surveyContinueButtonText] InThreadSurveyMetadata surveyContinueButtonText
         * @property {string|null} [surveySubmitButtonText] InThreadSurveyMetadata surveySubmitButtonText
         * @property {string|null} [privacyStatementFull] InThreadSurveyMetadata privacyStatementFull
         * @property {Array.<proto.InThreadSurveyMetadata.IInThreadSurveyPrivacyStatementPart>|null} [privacyStatementParts] InThreadSurveyMetadata privacyStatementParts
         * @property {string|null} [feedbackToastText] InThreadSurveyMetadata feedbackToastText
         * @property {number|null} [startQuestionIndex] InThreadSurveyMetadata startQuestionIndex
         */

        /**
         * Constructs a new InThreadSurveyMetadata.
         * @memberof proto
         * @classdesc Represents an InThreadSurveyMetadata.
         * @implements IInThreadSurveyMetadata
         * @constructor
         * @param {proto.IInThreadSurveyMetadata=} [properties] Properties to set
         */
        function InThreadSurveyMetadata(properties) {
            this.questions = [];
            this.privacyStatementParts = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * InThreadSurveyMetadata tessaSessionId.
         * @member {string|null|undefined} tessaSessionId
         * @memberof proto.InThreadSurveyMetadata
         * @instance
         */
        InThreadSurveyMetadata.prototype.tessaSessionId = null;

        /**
         * InThreadSurveyMetadata simonSessionId.
         * @member {string|null|undefined} simonSessionId
         * @memberof proto.InThreadSurveyMetadata
         * @instance
         */
        InThreadSurveyMetadata.prototype.simonSessionId = null;

        /**
         * InThreadSurveyMetadata simonSurveyId.
         * @member {string|null|undefined} simonSurveyId
         * @memberof proto.InThreadSurveyMetadata
         * @instance
         */
        InThreadSurveyMetadata.prototype.simonSurveyId = null;

        /**
         * InThreadSurveyMetadata tessaRootId.
         * @member {string|null|undefined} tessaRootId
         * @memberof proto.InThreadSurveyMetadata
         * @instance
         */
        InThreadSurveyMetadata.prototype.tessaRootId = null;

        /**
         * InThreadSurveyMetadata requestId.
         * @member {string|null|undefined} requestId
         * @memberof proto.InThreadSurveyMetadata
         * @instance
         */
        InThreadSurveyMetadata.prototype.requestId = null;

        /**
         * InThreadSurveyMetadata tessaEvent.
         * @member {string|null|undefined} tessaEvent
         * @memberof proto.InThreadSurveyMetadata
         * @instance
         */
        InThreadSurveyMetadata.prototype.tessaEvent = null;

        /**
         * InThreadSurveyMetadata invitationHeaderText.
         * @member {string|null|undefined} invitationHeaderText
         * @memberof proto.InThreadSurveyMetadata
         * @instance
         */
        InThreadSurveyMetadata.prototype.invitationHeaderText = null;

        /**
         * InThreadSurveyMetadata invitationBodyText.
         * @member {string|null|undefined} invitationBodyText
         * @memberof proto.InThreadSurveyMetadata
         * @instance
         */
        InThreadSurveyMetadata.prototype.invitationBodyText = null;

        /**
         * InThreadSurveyMetadata invitationCtaText.
         * @member {string|null|undefined} invitationCtaText
         * @memberof proto.InThreadSurveyMetadata
         * @instance
         */
        InThreadSurveyMetadata.prototype.invitationCtaText = null;

        /**
         * InThreadSurveyMetadata invitationCtaUrl.
         * @member {string|null|undefined} invitationCtaUrl
         * @memberof proto.InThreadSurveyMetadata
         * @instance
         */
        InThreadSurveyMetadata.prototype.invitationCtaUrl = null;

        /**
         * InThreadSurveyMetadata surveyTitle.
         * @member {string|null|undefined} surveyTitle
         * @memberof proto.InThreadSurveyMetadata
         * @instance
         */
        InThreadSurveyMetadata.prototype.surveyTitle = null;

        /**
         * InThreadSurveyMetadata questions.
         * @member {Array.<proto.InThreadSurveyMetadata.IInThreadSurveyQuestion>} questions
         * @memberof proto.InThreadSurveyMetadata
         * @instance
         */
        InThreadSurveyMetadata.prototype.questions = $util.emptyArray;

        /**
         * InThreadSurveyMetadata surveyContinueButtonText.
         * @member {string|null|undefined} surveyContinueButtonText
         * @memberof proto.InThreadSurveyMetadata
         * @instance
         */
        InThreadSurveyMetadata.prototype.surveyContinueButtonText = null;

        /**
         * InThreadSurveyMetadata surveySubmitButtonText.
         * @member {string|null|undefined} surveySubmitButtonText
         * @memberof proto.InThreadSurveyMetadata
         * @instance
         */
        InThreadSurveyMetadata.prototype.surveySubmitButtonText = null;

        /**
         * InThreadSurveyMetadata privacyStatementFull.
         * @member {string|null|undefined} privacyStatementFull
         * @memberof proto.InThreadSurveyMetadata
         * @instance
         */
        InThreadSurveyMetadata.prototype.privacyStatementFull = null;

        /**
         * InThreadSurveyMetadata privacyStatementParts.
         * @member {Array.<proto.InThreadSurveyMetadata.IInThreadSurveyPrivacyStatementPart>} privacyStatementParts
         * @memberof proto.InThreadSurveyMetadata
         * @instance
         */
        InThreadSurveyMetadata.prototype.privacyStatementParts = $util.emptyArray;

        /**
         * InThreadSurveyMetadata feedbackToastText.
         * @member {string|null|undefined} feedbackToastText
         * @memberof proto.InThreadSurveyMetadata
         * @instance
         */
        InThreadSurveyMetadata.prototype.feedbackToastText = null;

        /**
         * InThreadSurveyMetadata startQuestionIndex.
         * @member {number|null|undefined} startQuestionIndex
         * @memberof proto.InThreadSurveyMetadata
         * @instance
         */
        InThreadSurveyMetadata.prototype.startQuestionIndex = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_tessaSessionId", {
            get: $util.oneOfGetter($oneOfFields = ["tessaSessionId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_simonSessionId", {
            get: $util.oneOfGetter($oneOfFields = ["simonSessionId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_simonSurveyId", {
            get: $util.oneOfGetter($oneOfFields = ["simonSurveyId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_tessaRootId", {
            get: $util.oneOfGetter($oneOfFields = ["tessaRootId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_requestId", {
            get: $util.oneOfGetter($oneOfFields = ["requestId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_tessaEvent", {
            get: $util.oneOfGetter($oneOfFields = ["tessaEvent"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_invitationHeaderText", {
            get: $util.oneOfGetter($oneOfFields = ["invitationHeaderText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_invitationBodyText", {
            get: $util.oneOfGetter($oneOfFields = ["invitationBodyText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_invitationCtaText", {
            get: $util.oneOfGetter($oneOfFields = ["invitationCtaText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_invitationCtaUrl", {
            get: $util.oneOfGetter($oneOfFields = ["invitationCtaUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_surveyTitle", {
            get: $util.oneOfGetter($oneOfFields = ["surveyTitle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_surveyContinueButtonText", {
            get: $util.oneOfGetter($oneOfFields = ["surveyContinueButtonText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_surveySubmitButtonText", {
            get: $util.oneOfGetter($oneOfFields = ["surveySubmitButtonText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_privacyStatementFull", {
            get: $util.oneOfGetter($oneOfFields = ["privacyStatementFull"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_feedbackToastText", {
            get: $util.oneOfGetter($oneOfFields = ["feedbackToastText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_startQuestionIndex", {
            get: $util.oneOfGetter($oneOfFields = ["startQuestionIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new InThreadSurveyMetadata instance using the specified properties.
         * @function create
         * @memberof proto.InThreadSurveyMetadata
         * @static
         * @param {proto.IInThreadSurveyMetadata=} [properties] Properties to set
         * @returns {proto.InThreadSurveyMetadata} InThreadSurveyMetadata instance
         */
        InThreadSurveyMetadata.create = function create(properties) {
            return new InThreadSurveyMetadata(properties);
        };

        /**
         * Encodes the specified InThreadSurveyMetadata message. Does not implicitly {@link proto.InThreadSurveyMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.InThreadSurveyMetadata
         * @static
         * @param {proto.IInThreadSurveyMetadata} message InThreadSurveyMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InThreadSurveyMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.tessaSessionId != null && Object.hasOwnProperty.call(message, "tessaSessionId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.tessaSessionId);
            if (message.simonSessionId != null && Object.hasOwnProperty.call(message, "simonSessionId"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.simonSessionId);
            if (message.simonSurveyId != null && Object.hasOwnProperty.call(message, "simonSurveyId"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.simonSurveyId);
            if (message.tessaRootId != null && Object.hasOwnProperty.call(message, "tessaRootId"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.tessaRootId);
            if (message.requestId != null && Object.hasOwnProperty.call(message, "requestId"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.requestId);
            if (message.tessaEvent != null && Object.hasOwnProperty.call(message, "tessaEvent"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.tessaEvent);
            if (message.invitationHeaderText != null && Object.hasOwnProperty.call(message, "invitationHeaderText"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.invitationHeaderText);
            if (message.invitationBodyText != null && Object.hasOwnProperty.call(message, "invitationBodyText"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.invitationBodyText);
            if (message.invitationCtaText != null && Object.hasOwnProperty.call(message, "invitationCtaText"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.invitationCtaText);
            if (message.invitationCtaUrl != null && Object.hasOwnProperty.call(message, "invitationCtaUrl"))
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.invitationCtaUrl);
            if (message.surveyTitle != null && Object.hasOwnProperty.call(message, "surveyTitle"))
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.surveyTitle);
            if (message.questions != null && message.questions.length)
                for (var i = 0; i < message.questions.length; ++i)
                    $root.proto.InThreadSurveyMetadata.InThreadSurveyQuestion.encode(message.questions[i], writer.uint32(/* id 12, wireType 2 =*/98).fork(), q + 1).ldelim();
            if (message.surveyContinueButtonText != null && Object.hasOwnProperty.call(message, "surveyContinueButtonText"))
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.surveyContinueButtonText);
            if (message.surveySubmitButtonText != null && Object.hasOwnProperty.call(message, "surveySubmitButtonText"))
                writer.uint32(/* id 14, wireType 2 =*/114).string(message.surveySubmitButtonText);
            if (message.privacyStatementFull != null && Object.hasOwnProperty.call(message, "privacyStatementFull"))
                writer.uint32(/* id 15, wireType 2 =*/122).string(message.privacyStatementFull);
            if (message.privacyStatementParts != null && message.privacyStatementParts.length)
                for (var i = 0; i < message.privacyStatementParts.length; ++i)
                    $root.proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart.encode(message.privacyStatementParts[i], writer.uint32(/* id 16, wireType 2 =*/130).fork(), q + 1).ldelim();
            if (message.feedbackToastText != null && Object.hasOwnProperty.call(message, "feedbackToastText"))
                writer.uint32(/* id 17, wireType 2 =*/138).string(message.feedbackToastText);
            if (message.startQuestionIndex != null && Object.hasOwnProperty.call(message, "startQuestionIndex"))
                writer.uint32(/* id 18, wireType 0 =*/144).int32(message.startQuestionIndex);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified InThreadSurveyMetadata message, length delimited. Does not implicitly {@link proto.InThreadSurveyMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.InThreadSurveyMetadata
         * @static
         * @param {proto.IInThreadSurveyMetadata} message InThreadSurveyMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InThreadSurveyMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an InThreadSurveyMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.InThreadSurveyMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.InThreadSurveyMetadata} InThreadSurveyMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InThreadSurveyMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.tessaSessionId = reader.stringVerify();
                        message._tessaSessionId = "tessaSessionId";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.simonSessionId = reader.stringVerify();
                        message._simonSessionId = "simonSessionId";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.simonSurveyId = reader.stringVerify();
                        message._simonSurveyId = "simonSurveyId";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.tessaRootId = reader.stringVerify();
                        message._tessaRootId = "tessaRootId";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.requestId = reader.stringVerify();
                        message._requestId = "requestId";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        message.tessaEvent = reader.stringVerify();
                        message._tessaEvent = "tessaEvent";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        message.invitationHeaderText = reader.stringVerify();
                        message._invitationHeaderText = "invitationHeaderText";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        message.invitationBodyText = reader.stringVerify();
                        message._invitationBodyText = "invitationBodyText";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        message.invitationCtaText = reader.stringVerify();
                        message._invitationCtaText = "invitationCtaText";
                        continue;
                    }
                case 10: {
                        if (u !== 2)
                            break;
                        message.invitationCtaUrl = reader.stringVerify();
                        message._invitationCtaUrl = "invitationCtaUrl";
                        continue;
                    }
                case 11: {
                        if (u !== 2)
                            break;
                        message.surveyTitle = reader.stringVerify();
                        message._surveyTitle = "surveyTitle";
                        continue;
                    }
                case 12: {
                        if (u !== 2)
                            break;
                        if (!(message.questions && message.questions.length))
                            message.questions = [];
                        message.questions.push($root.proto.InThreadSurveyMetadata.InThreadSurveyQuestion.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        message.surveyContinueButtonText = reader.stringVerify();
                        message._surveyContinueButtonText = "surveyContinueButtonText";
                        continue;
                    }
                case 14: {
                        if (u !== 2)
                            break;
                        message.surveySubmitButtonText = reader.stringVerify();
                        message._surveySubmitButtonText = "surveySubmitButtonText";
                        continue;
                    }
                case 15: {
                        if (u !== 2)
                            break;
                        message.privacyStatementFull = reader.stringVerify();
                        message._privacyStatementFull = "privacyStatementFull";
                        continue;
                    }
                case 16: {
                        if (u !== 2)
                            break;
                        if (!(message.privacyStatementParts && message.privacyStatementParts.length))
                            message.privacyStatementParts = [];
                        message.privacyStatementParts.push($root.proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 17: {
                        if (u !== 2)
                            break;
                        message.feedbackToastText = reader.stringVerify();
                        message._feedbackToastText = "feedbackToastText";
                        continue;
                    }
                case 18: {
                        if (u !== 0)
                            break;
                        message.startQuestionIndex = reader.int32();
                        message._startQuestionIndex = "startQuestionIndex";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes an InThreadSurveyMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.InThreadSurveyMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.InThreadSurveyMetadata} InThreadSurveyMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InThreadSurveyMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InThreadSurveyMetadata message.
         * @function verify
         * @memberof proto.InThreadSurveyMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InThreadSurveyMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.tessaSessionId != null && Object.hasOwnProperty.call(message, "tessaSessionId")) {
                properties._tessaSessionId = 1;
                if (!$util.isString(message.tessaSessionId))
                    return "tessaSessionId: string expected";
            }
            if (message.simonSessionId != null && Object.hasOwnProperty.call(message, "simonSessionId")) {
                properties._simonSessionId = 1;
                if (!$util.isString(message.simonSessionId))
                    return "simonSessionId: string expected";
            }
            if (message.simonSurveyId != null && Object.hasOwnProperty.call(message, "simonSurveyId")) {
                properties._simonSurveyId = 1;
                if (!$util.isString(message.simonSurveyId))
                    return "simonSurveyId: string expected";
            }
            if (message.tessaRootId != null && Object.hasOwnProperty.call(message, "tessaRootId")) {
                properties._tessaRootId = 1;
                if (!$util.isString(message.tessaRootId))
                    return "tessaRootId: string expected";
            }
            if (message.requestId != null && Object.hasOwnProperty.call(message, "requestId")) {
                properties._requestId = 1;
                if (!$util.isString(message.requestId))
                    return "requestId: string expected";
            }
            if (message.tessaEvent != null && Object.hasOwnProperty.call(message, "tessaEvent")) {
                properties._tessaEvent = 1;
                if (!$util.isString(message.tessaEvent))
                    return "tessaEvent: string expected";
            }
            if (message.invitationHeaderText != null && Object.hasOwnProperty.call(message, "invitationHeaderText")) {
                properties._invitationHeaderText = 1;
                if (!$util.isString(message.invitationHeaderText))
                    return "invitationHeaderText: string expected";
            }
            if (message.invitationBodyText != null && Object.hasOwnProperty.call(message, "invitationBodyText")) {
                properties._invitationBodyText = 1;
                if (!$util.isString(message.invitationBodyText))
                    return "invitationBodyText: string expected";
            }
            if (message.invitationCtaText != null && Object.hasOwnProperty.call(message, "invitationCtaText")) {
                properties._invitationCtaText = 1;
                if (!$util.isString(message.invitationCtaText))
                    return "invitationCtaText: string expected";
            }
            if (message.invitationCtaUrl != null && Object.hasOwnProperty.call(message, "invitationCtaUrl")) {
                properties._invitationCtaUrl = 1;
                if (!$util.isString(message.invitationCtaUrl))
                    return "invitationCtaUrl: string expected";
            }
            if (message.surveyTitle != null && Object.hasOwnProperty.call(message, "surveyTitle")) {
                properties._surveyTitle = 1;
                if (!$util.isString(message.surveyTitle))
                    return "surveyTitle: string expected";
            }
            if (message.questions != null && Object.hasOwnProperty.call(message, "questions")) {
                if (!Array.isArray(message.questions))
                    return "questions: array expected";
                for (var i = 0; i < message.questions.length; ++i) {
                    var error = $root.proto.InThreadSurveyMetadata.InThreadSurveyQuestion.verify(message.questions[i], q + 1);
                    if (error)
                        return "questions." + error;
                }
            }
            if (message.surveyContinueButtonText != null && Object.hasOwnProperty.call(message, "surveyContinueButtonText")) {
                properties._surveyContinueButtonText = 1;
                if (!$util.isString(message.surveyContinueButtonText))
                    return "surveyContinueButtonText: string expected";
            }
            if (message.surveySubmitButtonText != null && Object.hasOwnProperty.call(message, "surveySubmitButtonText")) {
                properties._surveySubmitButtonText = 1;
                if (!$util.isString(message.surveySubmitButtonText))
                    return "surveySubmitButtonText: string expected";
            }
            if (message.privacyStatementFull != null && Object.hasOwnProperty.call(message, "privacyStatementFull")) {
                properties._privacyStatementFull = 1;
                if (!$util.isString(message.privacyStatementFull))
                    return "privacyStatementFull: string expected";
            }
            if (message.privacyStatementParts != null && Object.hasOwnProperty.call(message, "privacyStatementParts")) {
                if (!Array.isArray(message.privacyStatementParts))
                    return "privacyStatementParts: array expected";
                for (var i = 0; i < message.privacyStatementParts.length; ++i) {
                    var error = $root.proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart.verify(message.privacyStatementParts[i], q + 1);
                    if (error)
                        return "privacyStatementParts." + error;
                }
            }
            if (message.feedbackToastText != null && Object.hasOwnProperty.call(message, "feedbackToastText")) {
                properties._feedbackToastText = 1;
                if (!$util.isString(message.feedbackToastText))
                    return "feedbackToastText: string expected";
            }
            if (message.startQuestionIndex != null && Object.hasOwnProperty.call(message, "startQuestionIndex")) {
                properties._startQuestionIndex = 1;
                if (!$util.isInteger(message.startQuestionIndex))
                    return "startQuestionIndex: integer expected";
            }
            return null;
        };

        /**
         * Creates an InThreadSurveyMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.InThreadSurveyMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.InThreadSurveyMetadata} InThreadSurveyMetadata
         */
        InThreadSurveyMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.InThreadSurveyMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.tessaSessionId != null)
                message.tessaSessionId = String(object.tessaSessionId);
            if (object.simonSessionId != null)
                message.simonSessionId = String(object.simonSessionId);
            if (object.simonSurveyId != null)
                message.simonSurveyId = String(object.simonSurveyId);
            if (object.tessaRootId != null)
                message.tessaRootId = String(object.tessaRootId);
            if (object.requestId != null)
                message.requestId = String(object.requestId);
            if (object.tessaEvent != null)
                message.tessaEvent = String(object.tessaEvent);
            if (object.invitationHeaderText != null)
                message.invitationHeaderText = String(object.invitationHeaderText);
            if (object.invitationBodyText != null)
                message.invitationBodyText = String(object.invitationBodyText);
            if (object.invitationCtaText != null)
                message.invitationCtaText = String(object.invitationCtaText);
            if (object.invitationCtaUrl != null)
                message.invitationCtaUrl = String(object.invitationCtaUrl);
            if (object.surveyTitle != null)
                message.surveyTitle = String(object.surveyTitle);
            if (object.questions) {
                if (!Array.isArray(object.questions))
                    throw TypeError(".proto.InThreadSurveyMetadata.questions: array expected");
                message.questions = Array(object.questions.length);
                for (var i = 0; i < object.questions.length; ++i) {
                    if (!$util.isObject(object.questions[i]))
                        throw TypeError(".proto.InThreadSurveyMetadata.questions: object expected");
                    message.questions[i] = $root.proto.InThreadSurveyMetadata.InThreadSurveyQuestion.fromObject(object.questions[i], q + 1);
                }
            }
            if (object.surveyContinueButtonText != null)
                message.surveyContinueButtonText = String(object.surveyContinueButtonText);
            if (object.surveySubmitButtonText != null)
                message.surveySubmitButtonText = String(object.surveySubmitButtonText);
            if (object.privacyStatementFull != null)
                message.privacyStatementFull = String(object.privacyStatementFull);
            if (object.privacyStatementParts) {
                if (!Array.isArray(object.privacyStatementParts))
                    throw TypeError(".proto.InThreadSurveyMetadata.privacyStatementParts: array expected");
                message.privacyStatementParts = Array(object.privacyStatementParts.length);
                for (var i = 0; i < object.privacyStatementParts.length; ++i) {
                    if (!$util.isObject(object.privacyStatementParts[i]))
                        throw TypeError(".proto.InThreadSurveyMetadata.privacyStatementParts: object expected");
                    message.privacyStatementParts[i] = $root.proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart.fromObject(object.privacyStatementParts[i], q + 1);
                }
            }
            if (object.feedbackToastText != null)
                message.feedbackToastText = String(object.feedbackToastText);
            if (object.startQuestionIndex != null)
                message.startQuestionIndex = object.startQuestionIndex | 0;
            return message;
        };

        /**
         * Creates a plain object from an InThreadSurveyMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.InThreadSurveyMetadata
         * @static
         * @param {proto.InThreadSurveyMetadata} message InThreadSurveyMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InThreadSurveyMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults) {
                object.questions = [];
                object.privacyStatementParts = [];
            }
            if (message.tessaSessionId != null && Object.hasOwnProperty.call(message, "tessaSessionId"))
                object.tessaSessionId = message.tessaSessionId;
            if (message.simonSessionId != null && Object.hasOwnProperty.call(message, "simonSessionId"))
                object.simonSessionId = message.simonSessionId;
            if (message.simonSurveyId != null && Object.hasOwnProperty.call(message, "simonSurveyId"))
                object.simonSurveyId = message.simonSurveyId;
            if (message.tessaRootId != null && Object.hasOwnProperty.call(message, "tessaRootId"))
                object.tessaRootId = message.tessaRootId;
            if (message.requestId != null && Object.hasOwnProperty.call(message, "requestId"))
                object.requestId = message.requestId;
            if (message.tessaEvent != null && Object.hasOwnProperty.call(message, "tessaEvent"))
                object.tessaEvent = message.tessaEvent;
            if (message.invitationHeaderText != null && Object.hasOwnProperty.call(message, "invitationHeaderText"))
                object.invitationHeaderText = message.invitationHeaderText;
            if (message.invitationBodyText != null && Object.hasOwnProperty.call(message, "invitationBodyText"))
                object.invitationBodyText = message.invitationBodyText;
            if (message.invitationCtaText != null && Object.hasOwnProperty.call(message, "invitationCtaText"))
                object.invitationCtaText = message.invitationCtaText;
            if (message.invitationCtaUrl != null && Object.hasOwnProperty.call(message, "invitationCtaUrl"))
                object.invitationCtaUrl = message.invitationCtaUrl;
            if (message.surveyTitle != null && Object.hasOwnProperty.call(message, "surveyTitle"))
                object.surveyTitle = message.surveyTitle;
            if (message.questions && message.questions.length) {
                object.questions = Array(message.questions.length);
                for (var j = 0; j < message.questions.length; ++j)
                    object.questions[j] = $root.proto.InThreadSurveyMetadata.InThreadSurveyQuestion.toObject(message.questions[j], options, q + 1);
            }
            if (message.surveyContinueButtonText != null && Object.hasOwnProperty.call(message, "surveyContinueButtonText"))
                object.surveyContinueButtonText = message.surveyContinueButtonText;
            if (message.surveySubmitButtonText != null && Object.hasOwnProperty.call(message, "surveySubmitButtonText"))
                object.surveySubmitButtonText = message.surveySubmitButtonText;
            if (message.privacyStatementFull != null && Object.hasOwnProperty.call(message, "privacyStatementFull"))
                object.privacyStatementFull = message.privacyStatementFull;
            if (message.privacyStatementParts && message.privacyStatementParts.length) {
                object.privacyStatementParts = Array(message.privacyStatementParts.length);
                for (var j = 0; j < message.privacyStatementParts.length; ++j)
                    object.privacyStatementParts[j] = $root.proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart.toObject(message.privacyStatementParts[j], options, q + 1);
            }
            if (message.feedbackToastText != null && Object.hasOwnProperty.call(message, "feedbackToastText"))
                object.feedbackToastText = message.feedbackToastText;
            if (message.startQuestionIndex != null && Object.hasOwnProperty.call(message, "startQuestionIndex"))
                object.startQuestionIndex = message.startQuestionIndex;
            return object;
        };

        /**
         * Converts this InThreadSurveyMetadata to JSON.
         * @function toJSON
         * @memberof proto.InThreadSurveyMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InThreadSurveyMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for InThreadSurveyMetadata
         * @function getTypeUrl
         * @memberof proto.InThreadSurveyMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        InThreadSurveyMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.InThreadSurveyMetadata";
        };

        InThreadSurveyMetadata.InThreadSurveyOption = (function() {

            /**
             * Properties of an InThreadSurveyOption.
             * @memberof proto.InThreadSurveyMetadata
             * @interface IInThreadSurveyOption
             * @property {string|null} [stringValue] InThreadSurveyOption stringValue
             * @property {number|null} [numericValue] InThreadSurveyOption numericValue
             * @property {string|null} [textTranslated] InThreadSurveyOption textTranslated
             */

            /**
             * Constructs a new InThreadSurveyOption.
             * @memberof proto.InThreadSurveyMetadata
             * @classdesc Represents an InThreadSurveyOption.
             * @implements IInThreadSurveyOption
             * @constructor
             * @param {proto.InThreadSurveyMetadata.IInThreadSurveyOption=} [properties] Properties to set
             */
            function InThreadSurveyOption(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * InThreadSurveyOption stringValue.
             * @member {string|null|undefined} stringValue
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyOption
             * @instance
             */
            InThreadSurveyOption.prototype.stringValue = null;

            /**
             * InThreadSurveyOption numericValue.
             * @member {number|null|undefined} numericValue
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyOption
             * @instance
             */
            InThreadSurveyOption.prototype.numericValue = null;

            /**
             * InThreadSurveyOption textTranslated.
             * @member {string|null|undefined} textTranslated
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyOption
             * @instance
             */
            InThreadSurveyOption.prototype.textTranslated = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InThreadSurveyOption.prototype, "_stringValue", {
                get: $util.oneOfGetter($oneOfFields = ["stringValue"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InThreadSurveyOption.prototype, "_numericValue", {
                get: $util.oneOfGetter($oneOfFields = ["numericValue"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InThreadSurveyOption.prototype, "_textTranslated", {
                get: $util.oneOfGetter($oneOfFields = ["textTranslated"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new InThreadSurveyOption instance using the specified properties.
             * @function create
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyOption
             * @static
             * @param {proto.InThreadSurveyMetadata.IInThreadSurveyOption=} [properties] Properties to set
             * @returns {proto.InThreadSurveyMetadata.InThreadSurveyOption} InThreadSurveyOption instance
             */
            InThreadSurveyOption.create = function create(properties) {
                return new InThreadSurveyOption(properties);
            };

            /**
             * Encodes the specified InThreadSurveyOption message. Does not implicitly {@link proto.InThreadSurveyMetadata.InThreadSurveyOption.verify|verify} messages.
             * @function encode
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyOption
             * @static
             * @param {proto.InThreadSurveyMetadata.IInThreadSurveyOption} message InThreadSurveyOption message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InThreadSurveyOption.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.stringValue != null && Object.hasOwnProperty.call(message, "stringValue"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.stringValue);
                if (message.numericValue != null && Object.hasOwnProperty.call(message, "numericValue"))
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.numericValue);
                if (message.textTranslated != null && Object.hasOwnProperty.call(message, "textTranslated"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.textTranslated);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified InThreadSurveyOption message, length delimited. Does not implicitly {@link proto.InThreadSurveyMetadata.InThreadSurveyOption.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyOption
             * @static
             * @param {proto.InThreadSurveyMetadata.IInThreadSurveyOption} message InThreadSurveyOption message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InThreadSurveyOption.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an InThreadSurveyOption message from the specified reader or buffer.
             * @function decode
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyOption
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.InThreadSurveyMetadata.InThreadSurveyOption} InThreadSurveyOption
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InThreadSurveyOption.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.stringValue = reader.stringVerify();
                            message._stringValue = "stringValue";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.numericValue = reader.uint32();
                            message._numericValue = "numericValue";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.textTranslated = reader.stringVerify();
                            message._textTranslated = "textTranslated";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an InThreadSurveyOption message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyOption
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.InThreadSurveyMetadata.InThreadSurveyOption} InThreadSurveyOption
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InThreadSurveyOption.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an InThreadSurveyOption message.
             * @function verify
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyOption
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            InThreadSurveyOption.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.stringValue != null && Object.hasOwnProperty.call(message, "stringValue")) {
                    properties._stringValue = 1;
                    if (!$util.isString(message.stringValue))
                        return "stringValue: string expected";
                }
                if (message.numericValue != null && Object.hasOwnProperty.call(message, "numericValue")) {
                    properties._numericValue = 1;
                    if (!$util.isInteger(message.numericValue))
                        return "numericValue: integer expected";
                }
                if (message.textTranslated != null && Object.hasOwnProperty.call(message, "textTranslated")) {
                    properties._textTranslated = 1;
                    if (!$util.isString(message.textTranslated))
                        return "textTranslated: string expected";
                }
                return null;
            };

            /**
             * Creates an InThreadSurveyOption message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyOption
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.InThreadSurveyMetadata.InThreadSurveyOption} InThreadSurveyOption
             */
            InThreadSurveyOption.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.InThreadSurveyMetadata.InThreadSurveyOption: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.stringValue != null)
                    message.stringValue = String(object.stringValue);
                if (object.numericValue != null)
                    message.numericValue = object.numericValue >>> 0;
                if (object.textTranslated != null)
                    message.textTranslated = String(object.textTranslated);
                return message;
            };

            /**
             * Creates a plain object from an InThreadSurveyOption message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyOption
             * @static
             * @param {proto.InThreadSurveyMetadata.InThreadSurveyOption} message InThreadSurveyOption
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            InThreadSurveyOption.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.stringValue != null && Object.hasOwnProperty.call(message, "stringValue"))
                    object.stringValue = message.stringValue;
                if (message.numericValue != null && Object.hasOwnProperty.call(message, "numericValue"))
                    object.numericValue = message.numericValue;
                if (message.textTranslated != null && Object.hasOwnProperty.call(message, "textTranslated"))
                    object.textTranslated = message.textTranslated;
                return object;
            };

            /**
             * Converts this InThreadSurveyOption to JSON.
             * @function toJSON
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyOption
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            InThreadSurveyOption.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for InThreadSurveyOption
             * @function getTypeUrl
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyOption
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            InThreadSurveyOption.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.InThreadSurveyMetadata.InThreadSurveyOption";
            };

            return InThreadSurveyOption;
        })();

        InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart = (function() {

            /**
             * Properties of an InThreadSurveyPrivacyStatementPart.
             * @memberof proto.InThreadSurveyMetadata
             * @interface IInThreadSurveyPrivacyStatementPart
             * @property {string|null} [text] InThreadSurveyPrivacyStatementPart text
             * @property {string|null} [url] InThreadSurveyPrivacyStatementPart url
             */

            /**
             * Constructs a new InThreadSurveyPrivacyStatementPart.
             * @memberof proto.InThreadSurveyMetadata
             * @classdesc Represents an InThreadSurveyPrivacyStatementPart.
             * @implements IInThreadSurveyPrivacyStatementPart
             * @constructor
             * @param {proto.InThreadSurveyMetadata.IInThreadSurveyPrivacyStatementPart=} [properties] Properties to set
             */
            function InThreadSurveyPrivacyStatementPart(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * InThreadSurveyPrivacyStatementPart text.
             * @member {string|null|undefined} text
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart
             * @instance
             */
            InThreadSurveyPrivacyStatementPart.prototype.text = null;

            /**
             * InThreadSurveyPrivacyStatementPart url.
             * @member {string|null|undefined} url
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart
             * @instance
             */
            InThreadSurveyPrivacyStatementPart.prototype.url = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InThreadSurveyPrivacyStatementPart.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InThreadSurveyPrivacyStatementPart.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new InThreadSurveyPrivacyStatementPart instance using the specified properties.
             * @function create
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart
             * @static
             * @param {proto.InThreadSurveyMetadata.IInThreadSurveyPrivacyStatementPart=} [properties] Properties to set
             * @returns {proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart} InThreadSurveyPrivacyStatementPart instance
             */
            InThreadSurveyPrivacyStatementPart.create = function create(properties) {
                return new InThreadSurveyPrivacyStatementPart(properties);
            };

            /**
             * Encodes the specified InThreadSurveyPrivacyStatementPart message. Does not implicitly {@link proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart.verify|verify} messages.
             * @function encode
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart
             * @static
             * @param {proto.InThreadSurveyMetadata.IInThreadSurveyPrivacyStatementPart} message InThreadSurveyPrivacyStatementPart message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InThreadSurveyPrivacyStatementPart.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.text);
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.url);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified InThreadSurveyPrivacyStatementPart message, length delimited. Does not implicitly {@link proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart
             * @static
             * @param {proto.InThreadSurveyMetadata.IInThreadSurveyPrivacyStatementPart} message InThreadSurveyPrivacyStatementPart message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InThreadSurveyPrivacyStatementPart.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an InThreadSurveyPrivacyStatementPart message from the specified reader or buffer.
             * @function decode
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart} InThreadSurveyPrivacyStatementPart
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InThreadSurveyPrivacyStatementPart.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.text = reader.stringVerify();
                            message._text = "text";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.url = reader.stringVerify();
                            message._url = "url";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an InThreadSurveyPrivacyStatementPart message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart} InThreadSurveyPrivacyStatementPart
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InThreadSurveyPrivacyStatementPart.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an InThreadSurveyPrivacyStatementPart message.
             * @function verify
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            InThreadSurveyPrivacyStatementPart.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.text != null && Object.hasOwnProperty.call(message, "text")) {
                    properties._text = 1;
                    if (!$util.isString(message.text))
                        return "text: string expected";
                }
                if (message.url != null && Object.hasOwnProperty.call(message, "url")) {
                    properties._url = 1;
                    if (!$util.isString(message.url))
                        return "url: string expected";
                }
                return null;
            };

            /**
             * Creates an InThreadSurveyPrivacyStatementPart message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart} InThreadSurveyPrivacyStatementPart
             */
            InThreadSurveyPrivacyStatementPart.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.text != null)
                    message.text = String(object.text);
                if (object.url != null)
                    message.url = String(object.url);
                return message;
            };

            /**
             * Creates a plain object from an InThreadSurveyPrivacyStatementPart message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart
             * @static
             * @param {proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart} message InThreadSurveyPrivacyStatementPart
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            InThreadSurveyPrivacyStatementPart.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                    object.text = message.text;
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    object.url = message.url;
                return object;
            };

            /**
             * Converts this InThreadSurveyPrivacyStatementPart to JSON.
             * @function toJSON
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            InThreadSurveyPrivacyStatementPart.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for InThreadSurveyPrivacyStatementPart
             * @function getTypeUrl
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            InThreadSurveyPrivacyStatementPart.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart";
            };

            return InThreadSurveyPrivacyStatementPart;
        })();

        InThreadSurveyMetadata.InThreadSurveyQuestion = (function() {

            /**
             * Properties of an InThreadSurveyQuestion.
             * @memberof proto.InThreadSurveyMetadata
             * @interface IInThreadSurveyQuestion
             * @property {string|null} [questionText] InThreadSurveyQuestion questionText
             * @property {string|null} [questionId] InThreadSurveyQuestion questionId
             * @property {Array.<proto.InThreadSurveyMetadata.IInThreadSurveyOption>|null} [questionOptions] InThreadSurveyQuestion questionOptions
             */

            /**
             * Constructs a new InThreadSurveyQuestion.
             * @memberof proto.InThreadSurveyMetadata
             * @classdesc Represents an InThreadSurveyQuestion.
             * @implements IInThreadSurveyQuestion
             * @constructor
             * @param {proto.InThreadSurveyMetadata.IInThreadSurveyQuestion=} [properties] Properties to set
             */
            function InThreadSurveyQuestion(properties) {
                this.questionOptions = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * InThreadSurveyQuestion questionText.
             * @member {string|null|undefined} questionText
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyQuestion
             * @instance
             */
            InThreadSurveyQuestion.prototype.questionText = null;

            /**
             * InThreadSurveyQuestion questionId.
             * @member {string|null|undefined} questionId
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyQuestion
             * @instance
             */
            InThreadSurveyQuestion.prototype.questionId = null;

            /**
             * InThreadSurveyQuestion questionOptions.
             * @member {Array.<proto.InThreadSurveyMetadata.IInThreadSurveyOption>} questionOptions
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyQuestion
             * @instance
             */
            InThreadSurveyQuestion.prototype.questionOptions = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InThreadSurveyQuestion.prototype, "_questionText", {
                get: $util.oneOfGetter($oneOfFields = ["questionText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InThreadSurveyQuestion.prototype, "_questionId", {
                get: $util.oneOfGetter($oneOfFields = ["questionId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new InThreadSurveyQuestion instance using the specified properties.
             * @function create
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyQuestion
             * @static
             * @param {proto.InThreadSurveyMetadata.IInThreadSurveyQuestion=} [properties] Properties to set
             * @returns {proto.InThreadSurveyMetadata.InThreadSurveyQuestion} InThreadSurveyQuestion instance
             */
            InThreadSurveyQuestion.create = function create(properties) {
                return new InThreadSurveyQuestion(properties);
            };

            /**
             * Encodes the specified InThreadSurveyQuestion message. Does not implicitly {@link proto.InThreadSurveyMetadata.InThreadSurveyQuestion.verify|verify} messages.
             * @function encode
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyQuestion
             * @static
             * @param {proto.InThreadSurveyMetadata.IInThreadSurveyQuestion} message InThreadSurveyQuestion message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InThreadSurveyQuestion.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.questionText != null && Object.hasOwnProperty.call(message, "questionText"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.questionText);
                if (message.questionId != null && Object.hasOwnProperty.call(message, "questionId"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.questionId);
                if (message.questionOptions != null && message.questionOptions.length)
                    for (var i = 0; i < message.questionOptions.length; ++i)
                        $root.proto.InThreadSurveyMetadata.InThreadSurveyOption.encode(message.questionOptions[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified InThreadSurveyQuestion message, length delimited. Does not implicitly {@link proto.InThreadSurveyMetadata.InThreadSurveyQuestion.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyQuestion
             * @static
             * @param {proto.InThreadSurveyMetadata.IInThreadSurveyQuestion} message InThreadSurveyQuestion message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InThreadSurveyQuestion.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an InThreadSurveyQuestion message from the specified reader or buffer.
             * @function decode
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyQuestion
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.InThreadSurveyMetadata.InThreadSurveyQuestion} InThreadSurveyQuestion
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InThreadSurveyQuestion.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.questionText = reader.stringVerify();
                            message._questionText = "questionText";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.questionId = reader.stringVerify();
                            message._questionId = "questionId";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            if (!(message.questionOptions && message.questionOptions.length))
                                message.questionOptions = [];
                            message.questionOptions.push($root.proto.InThreadSurveyMetadata.InThreadSurveyOption.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an InThreadSurveyQuestion message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyQuestion
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.InThreadSurveyMetadata.InThreadSurveyQuestion} InThreadSurveyQuestion
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InThreadSurveyQuestion.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an InThreadSurveyQuestion message.
             * @function verify
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyQuestion
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            InThreadSurveyQuestion.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.questionText != null && Object.hasOwnProperty.call(message, "questionText")) {
                    properties._questionText = 1;
                    if (!$util.isString(message.questionText))
                        return "questionText: string expected";
                }
                if (message.questionId != null && Object.hasOwnProperty.call(message, "questionId")) {
                    properties._questionId = 1;
                    if (!$util.isString(message.questionId))
                        return "questionId: string expected";
                }
                if (message.questionOptions != null && Object.hasOwnProperty.call(message, "questionOptions")) {
                    if (!Array.isArray(message.questionOptions))
                        return "questionOptions: array expected";
                    for (var i = 0; i < message.questionOptions.length; ++i) {
                        var error = $root.proto.InThreadSurveyMetadata.InThreadSurveyOption.verify(message.questionOptions[i], q + 1);
                        if (error)
                            return "questionOptions." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an InThreadSurveyQuestion message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyQuestion
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.InThreadSurveyMetadata.InThreadSurveyQuestion} InThreadSurveyQuestion
             */
            InThreadSurveyQuestion.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.InThreadSurveyMetadata.InThreadSurveyQuestion: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.questionText != null)
                    message.questionText = String(object.questionText);
                if (object.questionId != null)
                    message.questionId = String(object.questionId);
                if (object.questionOptions) {
                    if (!Array.isArray(object.questionOptions))
                        throw TypeError(".proto.InThreadSurveyMetadata.InThreadSurveyQuestion.questionOptions: array expected");
                    message.questionOptions = Array(object.questionOptions.length);
                    for (var i = 0; i < object.questionOptions.length; ++i) {
                        if (!$util.isObject(object.questionOptions[i]))
                            throw TypeError(".proto.InThreadSurveyMetadata.InThreadSurveyQuestion.questionOptions: object expected");
                        message.questionOptions[i] = $root.proto.InThreadSurveyMetadata.InThreadSurveyOption.fromObject(object.questionOptions[i], q + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from an InThreadSurveyQuestion message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyQuestion
             * @static
             * @param {proto.InThreadSurveyMetadata.InThreadSurveyQuestion} message InThreadSurveyQuestion
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            InThreadSurveyQuestion.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.questionOptions = [];
                if (message.questionText != null && Object.hasOwnProperty.call(message, "questionText"))
                    object.questionText = message.questionText;
                if (message.questionId != null && Object.hasOwnProperty.call(message, "questionId"))
                    object.questionId = message.questionId;
                if (message.questionOptions && message.questionOptions.length) {
                    object.questionOptions = Array(message.questionOptions.length);
                    for (var j = 0; j < message.questionOptions.length; ++j)
                        object.questionOptions[j] = $root.proto.InThreadSurveyMetadata.InThreadSurveyOption.toObject(message.questionOptions[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this InThreadSurveyQuestion to JSON.
             * @function toJSON
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyQuestion
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            InThreadSurveyQuestion.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for InThreadSurveyQuestion
             * @function getTypeUrl
             * @memberof proto.InThreadSurveyMetadata.InThreadSurveyQuestion
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            InThreadSurveyQuestion.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.InThreadSurveyMetadata.InThreadSurveyQuestion";
            };

            return InThreadSurveyQuestion;
        })();

        return InThreadSurveyMetadata;
    })();

    proto.InlineContact = (function() {

        /**
         * Properties of an InlineContact.
         * @memberof proto
         * @interface IInlineContact
         * @property {string|null} [pnJid] InlineContact pnJid
         * @property {string|null} [lidJid] InlineContact lidJid
         * @property {string|null} [fullName] InlineContact fullName
         * @property {string|null} [firstName] InlineContact firstName
         * @property {string|null} [username] InlineContact username
         */

        /**
         * Constructs a new InlineContact.
         * @memberof proto
         * @classdesc Represents an InlineContact.
         * @implements IInlineContact
         * @constructor
         * @param {proto.IInlineContact=} [properties] Properties to set
         */
        function InlineContact(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * InlineContact pnJid.
         * @member {string|null|undefined} pnJid
         * @memberof proto.InlineContact
         * @instance
         */
        InlineContact.prototype.pnJid = null;

        /**
         * InlineContact lidJid.
         * @member {string|null|undefined} lidJid
         * @memberof proto.InlineContact
         * @instance
         */
        InlineContact.prototype.lidJid = null;

        /**
         * InlineContact fullName.
         * @member {string|null|undefined} fullName
         * @memberof proto.InlineContact
         * @instance
         */
        InlineContact.prototype.fullName = null;

        /**
         * InlineContact firstName.
         * @member {string|null|undefined} firstName
         * @memberof proto.InlineContact
         * @instance
         */
        InlineContact.prototype.firstName = null;

        /**
         * InlineContact username.
         * @member {string|null|undefined} username
         * @memberof proto.InlineContact
         * @instance
         */
        InlineContact.prototype.username = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InlineContact.prototype, "_pnJid", {
            get: $util.oneOfGetter($oneOfFields = ["pnJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InlineContact.prototype, "_lidJid", {
            get: $util.oneOfGetter($oneOfFields = ["lidJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InlineContact.prototype, "_fullName", {
            get: $util.oneOfGetter($oneOfFields = ["fullName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InlineContact.prototype, "_firstName", {
            get: $util.oneOfGetter($oneOfFields = ["firstName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InlineContact.prototype, "_username", {
            get: $util.oneOfGetter($oneOfFields = ["username"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new InlineContact instance using the specified properties.
         * @function create
         * @memberof proto.InlineContact
         * @static
         * @param {proto.IInlineContact=} [properties] Properties to set
         * @returns {proto.InlineContact} InlineContact instance
         */
        InlineContact.create = function create(properties) {
            return new InlineContact(properties);
        };

        /**
         * Encodes the specified InlineContact message. Does not implicitly {@link proto.InlineContact.verify|verify} messages.
         * @function encode
         * @memberof proto.InlineContact
         * @static
         * @param {proto.IInlineContact} message InlineContact message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InlineContact.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.pnJid != null && Object.hasOwnProperty.call(message, "pnJid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.pnJid);
            if (message.lidJid != null && Object.hasOwnProperty.call(message, "lidJid"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.lidJid);
            if (message.fullName != null && Object.hasOwnProperty.call(message, "fullName"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.fullName);
            if (message.firstName != null && Object.hasOwnProperty.call(message, "firstName"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.firstName);
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.username);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified InlineContact message, length delimited. Does not implicitly {@link proto.InlineContact.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.InlineContact
         * @static
         * @param {proto.IInlineContact} message InlineContact message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InlineContact.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an InlineContact message from the specified reader or buffer.
         * @function decode
         * @memberof proto.InlineContact
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.InlineContact} InlineContact
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InlineContact.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.pnJid = reader.stringVerify();
                        message._pnJid = "pnJid";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.lidJid = reader.stringVerify();
                        message._lidJid = "lidJid";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.fullName = reader.stringVerify();
                        message._fullName = "fullName";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.firstName = reader.stringVerify();
                        message._firstName = "firstName";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.username = reader.stringVerify();
                        message._username = "username";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes an InlineContact message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.InlineContact
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.InlineContact} InlineContact
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InlineContact.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InlineContact message.
         * @function verify
         * @memberof proto.InlineContact
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InlineContact.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.pnJid != null && Object.hasOwnProperty.call(message, "pnJid")) {
                properties._pnJid = 1;
                if (!$util.isString(message.pnJid))
                    return "pnJid: string expected";
            }
            if (message.lidJid != null && Object.hasOwnProperty.call(message, "lidJid")) {
                properties._lidJid = 1;
                if (!$util.isString(message.lidJid))
                    return "lidJid: string expected";
            }
            if (message.fullName != null && Object.hasOwnProperty.call(message, "fullName")) {
                properties._fullName = 1;
                if (!$util.isString(message.fullName))
                    return "fullName: string expected";
            }
            if (message.firstName != null && Object.hasOwnProperty.call(message, "firstName")) {
                properties._firstName = 1;
                if (!$util.isString(message.firstName))
                    return "firstName: string expected";
            }
            if (message.username != null && Object.hasOwnProperty.call(message, "username")) {
                properties._username = 1;
                if (!$util.isString(message.username))
                    return "username: string expected";
            }
            return null;
        };

        /**
         * Creates an InlineContact message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.InlineContact
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.InlineContact} InlineContact
         */
        InlineContact.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.InlineContact: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.pnJid != null)
                message.pnJid = String(object.pnJid);
            if (object.lidJid != null)
                message.lidJid = String(object.lidJid);
            if (object.fullName != null)
                message.fullName = String(object.fullName);
            if (object.firstName != null)
                message.firstName = String(object.firstName);
            if (object.username != null)
                message.username = String(object.username);
            return message;
        };

        /**
         * Creates a plain object from an InlineContact message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.InlineContact
         * @static
         * @param {proto.InlineContact} message InlineContact
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InlineContact.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.pnJid != null && Object.hasOwnProperty.call(message, "pnJid"))
                object.pnJid = message.pnJid;
            if (message.lidJid != null && Object.hasOwnProperty.call(message, "lidJid"))
                object.lidJid = message.lidJid;
            if (message.fullName != null && Object.hasOwnProperty.call(message, "fullName"))
                object.fullName = message.fullName;
            if (message.firstName != null && Object.hasOwnProperty.call(message, "firstName"))
                object.firstName = message.firstName;
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            return object;
        };

        /**
         * Converts this InlineContact to JSON.
         * @function toJSON
         * @memberof proto.InlineContact
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InlineContact.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for InlineContact
         * @function getTypeUrl
         * @memberof proto.InlineContact
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        InlineContact.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.InlineContact";
        };

        return InlineContact;
    })();

    proto.InteractiveAnnotation = (function() {

        /**
         * Properties of an InteractiveAnnotation.
         * @memberof proto
         * @interface IInteractiveAnnotation
         * @property {Array.<proto.IPoint>|null} [polygonVertices] InteractiveAnnotation polygonVertices
         * @property {boolean|null} [shouldSkipConfirmation] InteractiveAnnotation shouldSkipConfirmation
         * @property {proto.IEmbeddedContent|null} [embeddedContent] InteractiveAnnotation embeddedContent
         * @property {proto.InteractiveAnnotation.StatusLinkType|null} [statusLinkType] InteractiveAnnotation statusLinkType
         * @property {proto.ILocation|null} [location] InteractiveAnnotation location
         * @property {proto.ContextInfo.IForwardedNewsletterMessageInfo|null} [newsletter] InteractiveAnnotation newsletter
         * @property {boolean|null} [embeddedAction] InteractiveAnnotation embeddedAction
         * @property {proto.ITapLinkAction|null} [tapAction] InteractiveAnnotation tapAction
         */

        /**
         * Constructs a new InteractiveAnnotation.
         * @memberof proto
         * @classdesc Represents an InteractiveAnnotation.
         * @implements IInteractiveAnnotation
         * @constructor
         * @param {proto.IInteractiveAnnotation=} [properties] Properties to set
         */
        function InteractiveAnnotation(properties) {
            this.polygonVertices = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * InteractiveAnnotation polygonVertices.
         * @member {Array.<proto.IPoint>} polygonVertices
         * @memberof proto.InteractiveAnnotation
         * @instance
         */
        InteractiveAnnotation.prototype.polygonVertices = $util.emptyArray;

        /**
         * InteractiveAnnotation shouldSkipConfirmation.
         * @member {boolean|null|undefined} shouldSkipConfirmation
         * @memberof proto.InteractiveAnnotation
         * @instance
         */
        InteractiveAnnotation.prototype.shouldSkipConfirmation = null;

        /**
         * InteractiveAnnotation embeddedContent.
         * @member {proto.IEmbeddedContent|null|undefined} embeddedContent
         * @memberof proto.InteractiveAnnotation
         * @instance
         */
        InteractiveAnnotation.prototype.embeddedContent = null;

        /**
         * InteractiveAnnotation statusLinkType.
         * @member {proto.InteractiveAnnotation.StatusLinkType|null|undefined} statusLinkType
         * @memberof proto.InteractiveAnnotation
         * @instance
         */
        InteractiveAnnotation.prototype.statusLinkType = null;

        /**
         * InteractiveAnnotation location.
         * @member {proto.ILocation|null|undefined} location
         * @memberof proto.InteractiveAnnotation
         * @instance
         */
        InteractiveAnnotation.prototype.location = null;

        /**
         * InteractiveAnnotation newsletter.
         * @member {proto.ContextInfo.IForwardedNewsletterMessageInfo|null|undefined} newsletter
         * @memberof proto.InteractiveAnnotation
         * @instance
         */
        InteractiveAnnotation.prototype.newsletter = null;

        /**
         * InteractiveAnnotation embeddedAction.
         * @member {boolean|null|undefined} embeddedAction
         * @memberof proto.InteractiveAnnotation
         * @instance
         */
        InteractiveAnnotation.prototype.embeddedAction = null;

        /**
         * InteractiveAnnotation tapAction.
         * @member {proto.ITapLinkAction|null|undefined} tapAction
         * @memberof proto.InteractiveAnnotation
         * @instance
         */
        InteractiveAnnotation.prototype.tapAction = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InteractiveAnnotation.prototype, "_shouldSkipConfirmation", {
            get: $util.oneOfGetter($oneOfFields = ["shouldSkipConfirmation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InteractiveAnnotation.prototype, "_embeddedContent", {
            get: $util.oneOfGetter($oneOfFields = ["embeddedContent"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InteractiveAnnotation.prototype, "_statusLinkType", {
            get: $util.oneOfGetter($oneOfFields = ["statusLinkType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * InteractiveAnnotation action.
         * @member {"location"|"newsletter"|"embeddedAction"|"tapAction"|undefined} action
         * @memberof proto.InteractiveAnnotation
         * @instance
         */
        Object.defineProperty(InteractiveAnnotation.prototype, "action", {
            get: $util.oneOfGetter($oneOfFields = ["location", "newsletter", "embeddedAction", "tapAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new InteractiveAnnotation instance using the specified properties.
         * @function create
         * @memberof proto.InteractiveAnnotation
         * @static
         * @param {proto.IInteractiveAnnotation=} [properties] Properties to set
         * @returns {proto.InteractiveAnnotation} InteractiveAnnotation instance
         */
        InteractiveAnnotation.create = function create(properties) {
            return new InteractiveAnnotation(properties);
        };

        /**
         * Encodes the specified InteractiveAnnotation message. Does not implicitly {@link proto.InteractiveAnnotation.verify|verify} messages.
         * @function encode
         * @memberof proto.InteractiveAnnotation
         * @static
         * @param {proto.IInteractiveAnnotation} message InteractiveAnnotation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InteractiveAnnotation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.polygonVertices != null && message.polygonVertices.length)
                for (var i = 0; i < message.polygonVertices.length; ++i)
                    $root.proto.Point.encode(message.polygonVertices[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                $root.proto.Location.encode(message.location, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.newsletter != null && Object.hasOwnProperty.call(message, "newsletter"))
                $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.encode(message.newsletter, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.shouldSkipConfirmation != null && Object.hasOwnProperty.call(message, "shouldSkipConfirmation"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.shouldSkipConfirmation);
            if (message.embeddedContent != null && Object.hasOwnProperty.call(message, "embeddedContent"))
                $root.proto.EmbeddedContent.encode(message.embeddedContent, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
            if (message.embeddedAction != null && Object.hasOwnProperty.call(message, "embeddedAction"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.embeddedAction);
            if (message.tapAction != null && Object.hasOwnProperty.call(message, "tapAction"))
                $root.proto.TapLinkAction.encode(message.tapAction, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
            if (message.statusLinkType != null && Object.hasOwnProperty.call(message, "statusLinkType"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.statusLinkType);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified InteractiveAnnotation message, length delimited. Does not implicitly {@link proto.InteractiveAnnotation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.InteractiveAnnotation
         * @static
         * @param {proto.IInteractiveAnnotation} message InteractiveAnnotation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InteractiveAnnotation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an InteractiveAnnotation message from the specified reader or buffer.
         * @function decode
         * @memberof proto.InteractiveAnnotation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.InteractiveAnnotation} InteractiveAnnotation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InteractiveAnnotation.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(message.polygonVertices && message.polygonVertices.length))
                            message.polygonVertices = [];
                        message.polygonVertices.push($root.proto.Point.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.shouldSkipConfirmation = reader.bool();
                        message._shouldSkipConfirmation = "shouldSkipConfirmation";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.embeddedContent = $root.proto.EmbeddedContent.decode(reader, reader.uint32(), undefined, q + 1, message.embeddedContent);
                        message._embeddedContent = "embeddedContent";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        message.statusLinkType = reader.int32();
                        message._statusLinkType = "statusLinkType";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.location = $root.proto.Location.decode(reader, reader.uint32(), undefined, q + 1, message.location);
                        message.action = "location";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.newsletter = $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.decode(reader, reader.uint32(), undefined, q + 1, message.newsletter);
                        message.action = "newsletter";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        message.embeddedAction = reader.bool();
                        message.action = "embeddedAction";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        message.tapAction = $root.proto.TapLinkAction.decode(reader, reader.uint32(), undefined, q + 1, message.tapAction);
                        message.action = "tapAction";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes an InteractiveAnnotation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.InteractiveAnnotation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.InteractiveAnnotation} InteractiveAnnotation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InteractiveAnnotation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InteractiveAnnotation message.
         * @function verify
         * @memberof proto.InteractiveAnnotation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InteractiveAnnotation.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.polygonVertices != null && Object.hasOwnProperty.call(message, "polygonVertices")) {
                if (!Array.isArray(message.polygonVertices))
                    return "polygonVertices: array expected";
                for (var i = 0; i < message.polygonVertices.length; ++i) {
                    var error = $root.proto.Point.verify(message.polygonVertices[i], q + 1);
                    if (error)
                        return "polygonVertices." + error;
                }
            }
            if (message.shouldSkipConfirmation != null && Object.hasOwnProperty.call(message, "shouldSkipConfirmation")) {
                properties._shouldSkipConfirmation = 1;
                if (typeof message.shouldSkipConfirmation !== "boolean")
                    return "shouldSkipConfirmation: boolean expected";
            }
            if (message.embeddedContent != null && Object.hasOwnProperty.call(message, "embeddedContent")) {
                properties._embeddedContent = 1;
                {
                    var error = $root.proto.EmbeddedContent.verify(message.embeddedContent, q + 1);
                    if (error)
                        return "embeddedContent." + error;
                }
            }
            if (message.statusLinkType != null && Object.hasOwnProperty.call(message, "statusLinkType")) {
                properties._statusLinkType = 1;
                if (typeof message.statusLinkType !== "number" || (message.statusLinkType | 0) !== message.statusLinkType)
                    return "statusLinkType: enum value expected";
            }
            if (message.location != null && Object.hasOwnProperty.call(message, "location")) {
                properties.action = 1;
                {
                    var error = $root.proto.Location.verify(message.location, q + 1);
                    if (error)
                        return "location." + error;
                }
            }
            if (message.newsletter != null && Object.hasOwnProperty.call(message, "newsletter")) {
                if (properties.action === 1)
                    return "action: multiple values";
                properties.action = 1;
                {
                    var error = $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.verify(message.newsletter, q + 1);
                    if (error)
                        return "newsletter." + error;
                }
            }
            if (message.embeddedAction != null && Object.hasOwnProperty.call(message, "embeddedAction")) {
                if (properties.action === 1)
                    return "action: multiple values";
                properties.action = 1;
                if (typeof message.embeddedAction !== "boolean")
                    return "embeddedAction: boolean expected";
            }
            if (message.tapAction != null && Object.hasOwnProperty.call(message, "tapAction")) {
                if (properties.action === 1)
                    return "action: multiple values";
                properties.action = 1;
                {
                    var error = $root.proto.TapLinkAction.verify(message.tapAction, q + 1);
                    if (error)
                        return "tapAction." + error;
                }
            }
            return null;
        };

        /**
         * Creates an InteractiveAnnotation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.InteractiveAnnotation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.InteractiveAnnotation} InteractiveAnnotation
         */
        InteractiveAnnotation.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.InteractiveAnnotation: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.polygonVertices) {
                if (!Array.isArray(object.polygonVertices))
                    throw TypeError(".proto.InteractiveAnnotation.polygonVertices: array expected");
                message.polygonVertices = Array(object.polygonVertices.length);
                for (var i = 0; i < object.polygonVertices.length; ++i) {
                    if (!$util.isObject(object.polygonVertices[i]))
                        throw TypeError(".proto.InteractiveAnnotation.polygonVertices: object expected");
                    message.polygonVertices[i] = $root.proto.Point.fromObject(object.polygonVertices[i], q + 1);
                }
            }
            if (object.shouldSkipConfirmation != null)
                message.shouldSkipConfirmation = Boolean(object.shouldSkipConfirmation);
            if (object.embeddedContent != null) {
                if (!$util.isObject(object.embeddedContent))
                    throw TypeError(".proto.InteractiveAnnotation.embeddedContent: object expected");
                message.embeddedContent = $root.proto.EmbeddedContent.fromObject(object.embeddedContent, q + 1);
            }
            switch (object.statusLinkType) {
            case "RASTERIZED_LINK_PREVIEW":
            case 1:
                message.statusLinkType = 1;
                break;
            case "RASTERIZED_LINK_TRUNCATED":
            case 2:
                message.statusLinkType = 2;
                break;
            case "RASTERIZED_LINK_FULL_URL":
            case 3:
                message.statusLinkType = 3;
                break;
            default:
                if (typeof object.statusLinkType === "number" && (object.statusLinkType | 0) === object.statusLinkType)
                    message.statusLinkType = object.statusLinkType;
            }
            if (object.location != null) {
                if (!$util.isObject(object.location))
                    throw TypeError(".proto.InteractiveAnnotation.location: object expected");
                message.location = $root.proto.Location.fromObject(object.location, q + 1);
            }
            if (object.newsletter != null) {
                if (!$util.isObject(object.newsletter))
                    throw TypeError(".proto.InteractiveAnnotation.newsletter: object expected");
                message.newsletter = $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.fromObject(object.newsletter, q + 1);
            }
            if (object.embeddedAction != null)
                message.embeddedAction = Boolean(object.embeddedAction);
            if (object.tapAction != null) {
                if (!$util.isObject(object.tapAction))
                    throw TypeError(".proto.InteractiveAnnotation.tapAction: object expected");
                message.tapAction = $root.proto.TapLinkAction.fromObject(object.tapAction, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an InteractiveAnnotation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.InteractiveAnnotation
         * @static
         * @param {proto.InteractiveAnnotation} message InteractiveAnnotation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InteractiveAnnotation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.polygonVertices = [];
            if (message.polygonVertices && message.polygonVertices.length) {
                object.polygonVertices = Array(message.polygonVertices.length);
                for (var j = 0; j < message.polygonVertices.length; ++j)
                    object.polygonVertices[j] = $root.proto.Point.toObject(message.polygonVertices[j], options, q + 1);
            }
            if (message.location != null && Object.hasOwnProperty.call(message, "location")) {
                object.location = $root.proto.Location.toObject(message.location, options, q + 1);
                if (options.oneofs)
                    object.action = "location";
            }
            if (message.newsletter != null && Object.hasOwnProperty.call(message, "newsletter")) {
                object.newsletter = $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.toObject(message.newsletter, options, q + 1);
                if (options.oneofs)
                    object.action = "newsletter";
            }
            if (message.shouldSkipConfirmation != null && Object.hasOwnProperty.call(message, "shouldSkipConfirmation"))
                object.shouldSkipConfirmation = message.shouldSkipConfirmation;
            if (message.embeddedContent != null && Object.hasOwnProperty.call(message, "embeddedContent"))
                object.embeddedContent = $root.proto.EmbeddedContent.toObject(message.embeddedContent, options, q + 1);
            if (message.embeddedAction != null && Object.hasOwnProperty.call(message, "embeddedAction")) {
                object.embeddedAction = message.embeddedAction;
                if (options.oneofs)
                    object.action = "embeddedAction";
            }
            if (message.tapAction != null && Object.hasOwnProperty.call(message, "tapAction")) {
                object.tapAction = $root.proto.TapLinkAction.toObject(message.tapAction, options, q + 1);
                if (options.oneofs)
                    object.action = "tapAction";
            }
            if (message.statusLinkType != null && Object.hasOwnProperty.call(message, "statusLinkType"))
                object.statusLinkType = options.enums === String ? $root.proto.InteractiveAnnotation.StatusLinkType[message.statusLinkType] === undefined ? message.statusLinkType : $root.proto.InteractiveAnnotation.StatusLinkType[message.statusLinkType] : message.statusLinkType;
            return object;
        };

        /**
         * Converts this InteractiveAnnotation to JSON.
         * @function toJSON
         * @memberof proto.InteractiveAnnotation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InteractiveAnnotation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for InteractiveAnnotation
         * @function getTypeUrl
         * @memberof proto.InteractiveAnnotation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        InteractiveAnnotation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.InteractiveAnnotation";
        };

        /**
         * StatusLinkType enum.
         * @name proto.InteractiveAnnotation.StatusLinkType
         * @enum {number}
         * @property {number} RASTERIZED_LINK_PREVIEW=1 RASTERIZED_LINK_PREVIEW value
         * @property {number} RASTERIZED_LINK_TRUNCATED=2 RASTERIZED_LINK_TRUNCATED value
         * @property {number} RASTERIZED_LINK_FULL_URL=3 RASTERIZED_LINK_FULL_URL value
         */
        InteractiveAnnotation.StatusLinkType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[1] = "RASTERIZED_LINK_PREVIEW"] = 1;
            values[valuesById[2] = "RASTERIZED_LINK_TRUNCATED"] = 2;
            values[valuesById[3] = "RASTERIZED_LINK_FULL_URL"] = 3;
            return values;
        })();

        return InteractiveAnnotation;
    })();

    proto.InteractiveMessageAdditionalMetadata = (function() {

        /**
         * Properties of an InteractiveMessageAdditionalMetadata.
         * @memberof proto
         * @interface IInteractiveMessageAdditionalMetadata
         * @property {boolean|null} [isGalaxyFlowCompleted] InteractiveMessageAdditionalMetadata isGalaxyFlowCompleted
         */

        /**
         * Constructs a new InteractiveMessageAdditionalMetadata.
         * @memberof proto
         * @classdesc Represents an InteractiveMessageAdditionalMetadata.
         * @implements IInteractiveMessageAdditionalMetadata
         * @constructor
         * @param {proto.IInteractiveMessageAdditionalMetadata=} [properties] Properties to set
         */
        function InteractiveMessageAdditionalMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * InteractiveMessageAdditionalMetadata isGalaxyFlowCompleted.
         * @member {boolean|null|undefined} isGalaxyFlowCompleted
         * @memberof proto.InteractiveMessageAdditionalMetadata
         * @instance
         */
        InteractiveMessageAdditionalMetadata.prototype.isGalaxyFlowCompleted = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InteractiveMessageAdditionalMetadata.prototype, "_isGalaxyFlowCompleted", {
            get: $util.oneOfGetter($oneOfFields = ["isGalaxyFlowCompleted"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new InteractiveMessageAdditionalMetadata instance using the specified properties.
         * @function create
         * @memberof proto.InteractiveMessageAdditionalMetadata
         * @static
         * @param {proto.IInteractiveMessageAdditionalMetadata=} [properties] Properties to set
         * @returns {proto.InteractiveMessageAdditionalMetadata} InteractiveMessageAdditionalMetadata instance
         */
        InteractiveMessageAdditionalMetadata.create = function create(properties) {
            return new InteractiveMessageAdditionalMetadata(properties);
        };

        /**
         * Encodes the specified InteractiveMessageAdditionalMetadata message. Does not implicitly {@link proto.InteractiveMessageAdditionalMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.InteractiveMessageAdditionalMetadata
         * @static
         * @param {proto.IInteractiveMessageAdditionalMetadata} message InteractiveMessageAdditionalMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InteractiveMessageAdditionalMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.isGalaxyFlowCompleted != null && Object.hasOwnProperty.call(message, "isGalaxyFlowCompleted"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.isGalaxyFlowCompleted);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified InteractiveMessageAdditionalMetadata message, length delimited. Does not implicitly {@link proto.InteractiveMessageAdditionalMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.InteractiveMessageAdditionalMetadata
         * @static
         * @param {proto.IInteractiveMessageAdditionalMetadata} message InteractiveMessageAdditionalMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        InteractiveMessageAdditionalMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an InteractiveMessageAdditionalMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.InteractiveMessageAdditionalMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.InteractiveMessageAdditionalMetadata} InteractiveMessageAdditionalMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InteractiveMessageAdditionalMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.isGalaxyFlowCompleted = reader.bool();
                        message._isGalaxyFlowCompleted = "isGalaxyFlowCompleted";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes an InteractiveMessageAdditionalMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.InteractiveMessageAdditionalMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.InteractiveMessageAdditionalMetadata} InteractiveMessageAdditionalMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        InteractiveMessageAdditionalMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an InteractiveMessageAdditionalMetadata message.
         * @function verify
         * @memberof proto.InteractiveMessageAdditionalMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        InteractiveMessageAdditionalMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.isGalaxyFlowCompleted != null && Object.hasOwnProperty.call(message, "isGalaxyFlowCompleted")) {
                properties._isGalaxyFlowCompleted = 1;
                if (typeof message.isGalaxyFlowCompleted !== "boolean")
                    return "isGalaxyFlowCompleted: boolean expected";
            }
            return null;
        };

        /**
         * Creates an InteractiveMessageAdditionalMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.InteractiveMessageAdditionalMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.InteractiveMessageAdditionalMetadata} InteractiveMessageAdditionalMetadata
         */
        InteractiveMessageAdditionalMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.InteractiveMessageAdditionalMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.isGalaxyFlowCompleted != null)
                message.isGalaxyFlowCompleted = Boolean(object.isGalaxyFlowCompleted);
            return message;
        };

        /**
         * Creates a plain object from an InteractiveMessageAdditionalMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.InteractiveMessageAdditionalMetadata
         * @static
         * @param {proto.InteractiveMessageAdditionalMetadata} message InteractiveMessageAdditionalMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        InteractiveMessageAdditionalMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.isGalaxyFlowCompleted != null && Object.hasOwnProperty.call(message, "isGalaxyFlowCompleted"))
                object.isGalaxyFlowCompleted = message.isGalaxyFlowCompleted;
            return object;
        };

        /**
         * Converts this InteractiveMessageAdditionalMetadata to JSON.
         * @function toJSON
         * @memberof proto.InteractiveMessageAdditionalMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        InteractiveMessageAdditionalMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for InteractiveMessageAdditionalMetadata
         * @function getTypeUrl
         * @memberof proto.InteractiveMessageAdditionalMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        InteractiveMessageAdditionalMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.InteractiveMessageAdditionalMetadata";
        };

        return InteractiveMessageAdditionalMetadata;
    })();

    proto.KeepInChat = (function() {

        /**
         * Properties of a KeepInChat.
         * @memberof proto
         * @interface IKeepInChat
         * @property {proto.KeepType|null} [keepType] KeepInChat keepType
         * @property {number|Long|null} [serverTimestamp] KeepInChat serverTimestamp
         * @property {proto.IMessageKey|null} [key] KeepInChat key
         * @property {string|null} [deviceJid] KeepInChat deviceJid
         * @property {number|Long|null} [clientTimestampMs] KeepInChat clientTimestampMs
         * @property {number|Long|null} [serverTimestampMs] KeepInChat serverTimestampMs
         */

        /**
         * Constructs a new KeepInChat.
         * @memberof proto
         * @classdesc Represents a KeepInChat.
         * @implements IKeepInChat
         * @constructor
         * @param {proto.IKeepInChat=} [properties] Properties to set
         */
        function KeepInChat(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * KeepInChat keepType.
         * @member {proto.KeepType|null|undefined} keepType
         * @memberof proto.KeepInChat
         * @instance
         */
        KeepInChat.prototype.keepType = null;

        /**
         * KeepInChat serverTimestamp.
         * @member {number|Long|null|undefined} serverTimestamp
         * @memberof proto.KeepInChat
         * @instance
         */
        KeepInChat.prototype.serverTimestamp = null;

        /**
         * KeepInChat key.
         * @member {proto.IMessageKey|null|undefined} key
         * @memberof proto.KeepInChat
         * @instance
         */
        KeepInChat.prototype.key = null;

        /**
         * KeepInChat deviceJid.
         * @member {string|null|undefined} deviceJid
         * @memberof proto.KeepInChat
         * @instance
         */
        KeepInChat.prototype.deviceJid = null;

        /**
         * KeepInChat clientTimestampMs.
         * @member {number|Long|null|undefined} clientTimestampMs
         * @memberof proto.KeepInChat
         * @instance
         */
        KeepInChat.prototype.clientTimestampMs = null;

        /**
         * KeepInChat serverTimestampMs.
         * @member {number|Long|null|undefined} serverTimestampMs
         * @memberof proto.KeepInChat
         * @instance
         */
        KeepInChat.prototype.serverTimestampMs = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(KeepInChat.prototype, "_keepType", {
            get: $util.oneOfGetter($oneOfFields = ["keepType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(KeepInChat.prototype, "_serverTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["serverTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(KeepInChat.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(KeepInChat.prototype, "_deviceJid", {
            get: $util.oneOfGetter($oneOfFields = ["deviceJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(KeepInChat.prototype, "_clientTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["clientTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(KeepInChat.prototype, "_serverTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["serverTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new KeepInChat instance using the specified properties.
         * @function create
         * @memberof proto.KeepInChat
         * @static
         * @param {proto.IKeepInChat=} [properties] Properties to set
         * @returns {proto.KeepInChat} KeepInChat instance
         */
        KeepInChat.create = function create(properties) {
            return new KeepInChat(properties);
        };

        /**
         * Encodes the specified KeepInChat message. Does not implicitly {@link proto.KeepInChat.verify|verify} messages.
         * @function encode
         * @memberof proto.KeepInChat
         * @static
         * @param {proto.IKeepInChat} message KeepInChat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KeepInChat.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.keepType != null && Object.hasOwnProperty.call(message, "keepType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.keepType);
            if (message.serverTimestamp != null && Object.hasOwnProperty.call(message, "serverTimestamp"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.serverTimestamp);
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                $root.proto.MessageKey.encode(message.key, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.deviceJid != null && Object.hasOwnProperty.call(message, "deviceJid"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.deviceJid);
            if (message.clientTimestampMs != null && Object.hasOwnProperty.call(message, "clientTimestampMs"))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.clientTimestampMs);
            if (message.serverTimestampMs != null && Object.hasOwnProperty.call(message, "serverTimestampMs"))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.serverTimestampMs);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified KeepInChat message, length delimited. Does not implicitly {@link proto.KeepInChat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.KeepInChat
         * @static
         * @param {proto.IKeepInChat} message KeepInChat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KeepInChat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a KeepInChat message from the specified reader or buffer.
         * @function decode
         * @memberof proto.KeepInChat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.KeepInChat} KeepInChat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KeepInChat.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.keepType = reader.int32();
                        message._keepType = "keepType";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.serverTimestamp = reader.int64();
                        message._serverTimestamp = "serverTimestamp";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.key = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.key);
                        message._key = "key";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.deviceJid = reader.stringVerify();
                        message._deviceJid = "deviceJid";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.clientTimestampMs = reader.int64();
                        message._clientTimestampMs = "clientTimestampMs";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        message.serverTimestampMs = reader.int64();
                        message._serverTimestampMs = "serverTimestampMs";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a KeepInChat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.KeepInChat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.KeepInChat} KeepInChat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KeepInChat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a KeepInChat message.
         * @function verify
         * @memberof proto.KeepInChat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        KeepInChat.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.keepType != null && Object.hasOwnProperty.call(message, "keepType")) {
                properties._keepType = 1;
                if (typeof message.keepType !== "number" || (message.keepType | 0) !== message.keepType)
                    return "keepType: enum value expected";
            }
            if (message.serverTimestamp != null && Object.hasOwnProperty.call(message, "serverTimestamp")) {
                properties._serverTimestamp = 1;
                if (!$util.isInteger(message.serverTimestamp) && !(message.serverTimestamp && $util.isInteger(message.serverTimestamp.low) && $util.isInteger(message.serverTimestamp.high)))
                    return "serverTimestamp: integer|Long expected";
            }
            if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                properties._key = 1;
                {
                    var error = $root.proto.MessageKey.verify(message.key, q + 1);
                    if (error)
                        return "key." + error;
                }
            }
            if (message.deviceJid != null && Object.hasOwnProperty.call(message, "deviceJid")) {
                properties._deviceJid = 1;
                if (!$util.isString(message.deviceJid))
                    return "deviceJid: string expected";
            }
            if (message.clientTimestampMs != null && Object.hasOwnProperty.call(message, "clientTimestampMs")) {
                properties._clientTimestampMs = 1;
                if (!$util.isInteger(message.clientTimestampMs) && !(message.clientTimestampMs && $util.isInteger(message.clientTimestampMs.low) && $util.isInteger(message.clientTimestampMs.high)))
                    return "clientTimestampMs: integer|Long expected";
            }
            if (message.serverTimestampMs != null && Object.hasOwnProperty.call(message, "serverTimestampMs")) {
                properties._serverTimestampMs = 1;
                if (!$util.isInteger(message.serverTimestampMs) && !(message.serverTimestampMs && $util.isInteger(message.serverTimestampMs.low) && $util.isInteger(message.serverTimestampMs.high)))
                    return "serverTimestampMs: integer|Long expected";
            }
            return null;
        };

        /**
         * Creates a KeepInChat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.KeepInChat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.KeepInChat} KeepInChat
         */
        KeepInChat.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.KeepInChat: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.keepType) {
            case "UNKNOWN":
            case 0:
                message.keepType = 0;
                break;
            case "KEEP_FOR_ALL":
            case 1:
                message.keepType = 1;
                break;
            case "UNDO_KEEP_FOR_ALL":
            case 2:
                message.keepType = 2;
                break;
            default:
                if (typeof object.keepType === "number" && (object.keepType | 0) === object.keepType)
                    message.keepType = object.keepType;
            }
            if (object.serverTimestamp != null)
                if ($util.Long)
                    message.serverTimestamp = $util.Long.fromValue(object.serverTimestamp, false);
                else if (typeof object.serverTimestamp === "string")
                    message.serverTimestamp = parseInt(object.serverTimestamp, 10);
                else if (typeof object.serverTimestamp === "number")
                    message.serverTimestamp = object.serverTimestamp;
                else if (typeof object.serverTimestamp === "object")
                    message.serverTimestamp = new $util.LongBits(object.serverTimestamp.low >>> 0, object.serverTimestamp.high >>> 0).toNumber();
            if (object.key != null) {
                if (!$util.isObject(object.key))
                    throw TypeError(".proto.KeepInChat.key: object expected");
                message.key = $root.proto.MessageKey.fromObject(object.key, q + 1);
            }
            if (object.deviceJid != null)
                message.deviceJid = String(object.deviceJid);
            if (object.clientTimestampMs != null)
                if ($util.Long)
                    message.clientTimestampMs = $util.Long.fromValue(object.clientTimestampMs, false);
                else if (typeof object.clientTimestampMs === "string")
                    message.clientTimestampMs = parseInt(object.clientTimestampMs, 10);
                else if (typeof object.clientTimestampMs === "number")
                    message.clientTimestampMs = object.clientTimestampMs;
                else if (typeof object.clientTimestampMs === "object")
                    message.clientTimestampMs = new $util.LongBits(object.clientTimestampMs.low >>> 0, object.clientTimestampMs.high >>> 0).toNumber();
            if (object.serverTimestampMs != null)
                if ($util.Long)
                    message.serverTimestampMs = $util.Long.fromValue(object.serverTimestampMs, false);
                else if (typeof object.serverTimestampMs === "string")
                    message.serverTimestampMs = parseInt(object.serverTimestampMs, 10);
                else if (typeof object.serverTimestampMs === "number")
                    message.serverTimestampMs = object.serverTimestampMs;
                else if (typeof object.serverTimestampMs === "object")
                    message.serverTimestampMs = new $util.LongBits(object.serverTimestampMs.low >>> 0, object.serverTimestampMs.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a KeepInChat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.KeepInChat
         * @static
         * @param {proto.KeepInChat} message KeepInChat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        KeepInChat.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.keepType != null && Object.hasOwnProperty.call(message, "keepType"))
                object.keepType = options.enums === String ? $root.proto.KeepType[message.keepType] === undefined ? message.keepType : $root.proto.KeepType[message.keepType] : message.keepType;
            if (message.serverTimestamp != null && Object.hasOwnProperty.call(message, "serverTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.serverTimestamp = typeof message.serverTimestamp === "number" ? BigInt(message.serverTimestamp) : $util.Long.fromBits(message.serverTimestamp.low >>> 0, message.serverTimestamp.high >>> 0, false).toBigInt();
                else if (typeof message.serverTimestamp === "number")
                    object.serverTimestamp = options.longs === String ? String(message.serverTimestamp) : message.serverTimestamp;
                else
                    object.serverTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.serverTimestamp) : options.longs === Number ? new $util.LongBits(message.serverTimestamp.low >>> 0, message.serverTimestamp.high >>> 0).toNumber() : message.serverTimestamp;
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                object.key = $root.proto.MessageKey.toObject(message.key, options, q + 1);
            if (message.deviceJid != null && Object.hasOwnProperty.call(message, "deviceJid"))
                object.deviceJid = message.deviceJid;
            if (message.clientTimestampMs != null && Object.hasOwnProperty.call(message, "clientTimestampMs"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.clientTimestampMs = typeof message.clientTimestampMs === "number" ? BigInt(message.clientTimestampMs) : $util.Long.fromBits(message.clientTimestampMs.low >>> 0, message.clientTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof message.clientTimestampMs === "number")
                    object.clientTimestampMs = options.longs === String ? String(message.clientTimestampMs) : message.clientTimestampMs;
                else
                    object.clientTimestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.clientTimestampMs) : options.longs === Number ? new $util.LongBits(message.clientTimestampMs.low >>> 0, message.clientTimestampMs.high >>> 0).toNumber() : message.clientTimestampMs;
            if (message.serverTimestampMs != null && Object.hasOwnProperty.call(message, "serverTimestampMs"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.serverTimestampMs = typeof message.serverTimestampMs === "number" ? BigInt(message.serverTimestampMs) : $util.Long.fromBits(message.serverTimestampMs.low >>> 0, message.serverTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof message.serverTimestampMs === "number")
                    object.serverTimestampMs = options.longs === String ? String(message.serverTimestampMs) : message.serverTimestampMs;
                else
                    object.serverTimestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.serverTimestampMs) : options.longs === Number ? new $util.LongBits(message.serverTimestampMs.low >>> 0, message.serverTimestampMs.high >>> 0).toNumber() : message.serverTimestampMs;
            return object;
        };

        /**
         * Converts this KeepInChat to JSON.
         * @function toJSON
         * @memberof proto.KeepInChat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        KeepInChat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for KeepInChat
         * @function getTypeUrl
         * @memberof proto.KeepInChat
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        KeepInChat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.KeepInChat";
        };

        return KeepInChat;
    })();

    /**
     * KeepType enum.
     * @name proto.KeepType
     * @enum {number}
     * @property {number} UNKNOWN=0 UNKNOWN value
     * @property {number} KEEP_FOR_ALL=1 KEEP_FOR_ALL value
     * @property {number} UNDO_KEEP_FOR_ALL=2 UNDO_KEEP_FOR_ALL value
     */
    proto.KeepType = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "UNKNOWN"] = 0;
        values[valuesById[1] = "KEEP_FOR_ALL"] = 1;
        values[valuesById[2] = "UNDO_KEEP_FOR_ALL"] = 2;
        return values;
    })();

    proto.KeyExchangeMessage = (function() {

        /**
         * Properties of a KeyExchangeMessage.
         * @memberof proto
         * @interface IKeyExchangeMessage
         * @property {number|null} [id] KeyExchangeMessage id
         * @property {Uint8Array|null} [baseKey] KeyExchangeMessage baseKey
         * @property {Uint8Array|null} [ratchetKey] KeyExchangeMessage ratchetKey
         * @property {Uint8Array|null} [identityKey] KeyExchangeMessage identityKey
         * @property {Uint8Array|null} [baseKeySignature] KeyExchangeMessage baseKeySignature
         */

        /**
         * Constructs a new KeyExchangeMessage.
         * @memberof proto
         * @classdesc Represents a KeyExchangeMessage.
         * @implements IKeyExchangeMessage
         * @constructor
         * @param {proto.IKeyExchangeMessage=} [properties] Properties to set
         */
        function KeyExchangeMessage(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * KeyExchangeMessage id.
         * @member {number|null|undefined} id
         * @memberof proto.KeyExchangeMessage
         * @instance
         */
        KeyExchangeMessage.prototype.id = null;

        /**
         * KeyExchangeMessage baseKey.
         * @member {Uint8Array|null|undefined} baseKey
         * @memberof proto.KeyExchangeMessage
         * @instance
         */
        KeyExchangeMessage.prototype.baseKey = null;

        /**
         * KeyExchangeMessage ratchetKey.
         * @member {Uint8Array|null|undefined} ratchetKey
         * @memberof proto.KeyExchangeMessage
         * @instance
         */
        KeyExchangeMessage.prototype.ratchetKey = null;

        /**
         * KeyExchangeMessage identityKey.
         * @member {Uint8Array|null|undefined} identityKey
         * @memberof proto.KeyExchangeMessage
         * @instance
         */
        KeyExchangeMessage.prototype.identityKey = null;

        /**
         * KeyExchangeMessage baseKeySignature.
         * @member {Uint8Array|null|undefined} baseKeySignature
         * @memberof proto.KeyExchangeMessage
         * @instance
         */
        KeyExchangeMessage.prototype.baseKeySignature = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(KeyExchangeMessage.prototype, "_id", {
            get: $util.oneOfGetter($oneOfFields = ["id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(KeyExchangeMessage.prototype, "_baseKey", {
            get: $util.oneOfGetter($oneOfFields = ["baseKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(KeyExchangeMessage.prototype, "_ratchetKey", {
            get: $util.oneOfGetter($oneOfFields = ["ratchetKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(KeyExchangeMessage.prototype, "_identityKey", {
            get: $util.oneOfGetter($oneOfFields = ["identityKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(KeyExchangeMessage.prototype, "_baseKeySignature", {
            get: $util.oneOfGetter($oneOfFields = ["baseKeySignature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new KeyExchangeMessage instance using the specified properties.
         * @function create
         * @memberof proto.KeyExchangeMessage
         * @static
         * @param {proto.IKeyExchangeMessage=} [properties] Properties to set
         * @returns {proto.KeyExchangeMessage} KeyExchangeMessage instance
         */
        KeyExchangeMessage.create = function create(properties) {
            return new KeyExchangeMessage(properties);
        };

        /**
         * Encodes the specified KeyExchangeMessage message. Does not implicitly {@link proto.KeyExchangeMessage.verify|verify} messages.
         * @function encode
         * @memberof proto.KeyExchangeMessage
         * @static
         * @param {proto.IKeyExchangeMessage} message KeyExchangeMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KeyExchangeMessage.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.id);
            if (message.baseKey != null && Object.hasOwnProperty.call(message, "baseKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.baseKey);
            if (message.ratchetKey != null && Object.hasOwnProperty.call(message, "ratchetKey"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.ratchetKey);
            if (message.identityKey != null && Object.hasOwnProperty.call(message, "identityKey"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.identityKey);
            if (message.baseKeySignature != null && Object.hasOwnProperty.call(message, "baseKeySignature"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.baseKeySignature);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified KeyExchangeMessage message, length delimited. Does not implicitly {@link proto.KeyExchangeMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.KeyExchangeMessage
         * @static
         * @param {proto.IKeyExchangeMessage} message KeyExchangeMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KeyExchangeMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a KeyExchangeMessage message from the specified reader or buffer.
         * @function decode
         * @memberof proto.KeyExchangeMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.KeyExchangeMessage} KeyExchangeMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KeyExchangeMessage.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.id = reader.uint32();
                        message._id = "id";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.baseKey = reader.bytes();
                        message._baseKey = "baseKey";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.ratchetKey = reader.bytes();
                        message._ratchetKey = "ratchetKey";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.identityKey = reader.bytes();
                        message._identityKey = "identityKey";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.baseKeySignature = reader.bytes();
                        message._baseKeySignature = "baseKeySignature";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a KeyExchangeMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.KeyExchangeMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.KeyExchangeMessage} KeyExchangeMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KeyExchangeMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a KeyExchangeMessage message.
         * @function verify
         * @memberof proto.KeyExchangeMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        KeyExchangeMessage.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.id != null && Object.hasOwnProperty.call(message, "id")) {
                properties._id = 1;
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            }
            if (message.baseKey != null && Object.hasOwnProperty.call(message, "baseKey")) {
                properties._baseKey = 1;
                if (!(message.baseKey && typeof message.baseKey.length === "number" || $util.isString(message.baseKey)))
                    return "baseKey: buffer expected";
            }
            if (message.ratchetKey != null && Object.hasOwnProperty.call(message, "ratchetKey")) {
                properties._ratchetKey = 1;
                if (!(message.ratchetKey && typeof message.ratchetKey.length === "number" || $util.isString(message.ratchetKey)))
                    return "ratchetKey: buffer expected";
            }
            if (message.identityKey != null && Object.hasOwnProperty.call(message, "identityKey")) {
                properties._identityKey = 1;
                if (!(message.identityKey && typeof message.identityKey.length === "number" || $util.isString(message.identityKey)))
                    return "identityKey: buffer expected";
            }
            if (message.baseKeySignature != null && Object.hasOwnProperty.call(message, "baseKeySignature")) {
                properties._baseKeySignature = 1;
                if (!(message.baseKeySignature && typeof message.baseKeySignature.length === "number" || $util.isString(message.baseKeySignature)))
                    return "baseKeySignature: buffer expected";
            }
            return null;
        };

        /**
         * Creates a KeyExchangeMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.KeyExchangeMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.KeyExchangeMessage} KeyExchangeMessage
         */
        KeyExchangeMessage.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.KeyExchangeMessage: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.id != null)
                message.id = object.id >>> 0;
            if (object.baseKey != null)
                if (typeof object.baseKey === "string")
                    $util.base64.decode(object.baseKey, message.baseKey = $util.newBuffer($util.base64.length(object.baseKey)), 0);
                else if (object.baseKey.length >= 0)
                    message.baseKey = object.baseKey;
            if (object.ratchetKey != null)
                if (typeof object.ratchetKey === "string")
                    $util.base64.decode(object.ratchetKey, message.ratchetKey = $util.newBuffer($util.base64.length(object.ratchetKey)), 0);
                else if (object.ratchetKey.length >= 0)
                    message.ratchetKey = object.ratchetKey;
            if (object.identityKey != null)
                if (typeof object.identityKey === "string")
                    $util.base64.decode(object.identityKey, message.identityKey = $util.newBuffer($util.base64.length(object.identityKey)), 0);
                else if (object.identityKey.length >= 0)
                    message.identityKey = object.identityKey;
            if (object.baseKeySignature != null)
                if (typeof object.baseKeySignature === "string")
                    $util.base64.decode(object.baseKeySignature, message.baseKeySignature = $util.newBuffer($util.base64.length(object.baseKeySignature)), 0);
                else if (object.baseKeySignature.length >= 0)
                    message.baseKeySignature = object.baseKeySignature;
            return message;
        };

        /**
         * Creates a plain object from a KeyExchangeMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.KeyExchangeMessage
         * @static
         * @param {proto.KeyExchangeMessage} message KeyExchangeMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        KeyExchangeMessage.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.baseKey != null && Object.hasOwnProperty.call(message, "baseKey"))
                object.baseKey = options.bytes === String ? $util.base64.encode(message.baseKey, 0, message.baseKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.baseKey) : message.baseKey;
            if (message.ratchetKey != null && Object.hasOwnProperty.call(message, "ratchetKey"))
                object.ratchetKey = options.bytes === String ? $util.base64.encode(message.ratchetKey, 0, message.ratchetKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.ratchetKey) : message.ratchetKey;
            if (message.identityKey != null && Object.hasOwnProperty.call(message, "identityKey"))
                object.identityKey = options.bytes === String ? $util.base64.encode(message.identityKey, 0, message.identityKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.identityKey) : message.identityKey;
            if (message.baseKeySignature != null && Object.hasOwnProperty.call(message, "baseKeySignature"))
                object.baseKeySignature = options.bytes === String ? $util.base64.encode(message.baseKeySignature, 0, message.baseKeySignature.length) : options.bytes === Array ? Array.prototype.slice.call(message.baseKeySignature) : message.baseKeySignature;
            return object;
        };

        /**
         * Converts this KeyExchangeMessage to JSON.
         * @function toJSON
         * @memberof proto.KeyExchangeMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        KeyExchangeMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for KeyExchangeMessage
         * @function getTypeUrl
         * @memberof proto.KeyExchangeMessage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        KeyExchangeMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.KeyExchangeMessage";
        };

        return KeyExchangeMessage;
    })();

    proto.KeyId = (function() {

        /**
         * Properties of a KeyId.
         * @memberof proto
         * @interface IKeyId
         * @property {Uint8Array|null} [id] KeyId id
         */

        /**
         * Constructs a new KeyId.
         * @memberof proto
         * @classdesc Represents a KeyId.
         * @implements IKeyId
         * @constructor
         * @param {proto.IKeyId=} [properties] Properties to set
         */
        function KeyId(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * KeyId id.
         * @member {Uint8Array|null|undefined} id
         * @memberof proto.KeyId
         * @instance
         */
        KeyId.prototype.id = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(KeyId.prototype, "_id", {
            get: $util.oneOfGetter($oneOfFields = ["id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new KeyId instance using the specified properties.
         * @function create
         * @memberof proto.KeyId
         * @static
         * @param {proto.IKeyId=} [properties] Properties to set
         * @returns {proto.KeyId} KeyId instance
         */
        KeyId.create = function create(properties) {
            return new KeyId(properties);
        };

        /**
         * Encodes the specified KeyId message. Does not implicitly {@link proto.KeyId.verify|verify} messages.
         * @function encode
         * @memberof proto.KeyId
         * @static
         * @param {proto.IKeyId} message KeyId message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KeyId.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.id);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified KeyId message, length delimited. Does not implicitly {@link proto.KeyId.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.KeyId
         * @static
         * @param {proto.IKeyId} message KeyId message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KeyId.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a KeyId message from the specified reader or buffer.
         * @function decode
         * @memberof proto.KeyId
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.KeyId} KeyId
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KeyId.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.id = reader.bytes();
                        message._id = "id";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a KeyId message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.KeyId
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.KeyId} KeyId
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KeyId.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a KeyId message.
         * @function verify
         * @memberof proto.KeyId
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        KeyId.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.id != null && Object.hasOwnProperty.call(message, "id")) {
                properties._id = 1;
                if (!(message.id && typeof message.id.length === "number" || $util.isString(message.id)))
                    return "id: buffer expected";
            }
            return null;
        };

        /**
         * Creates a KeyId message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.KeyId
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.KeyId} KeyId
         */
        KeyId.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.KeyId: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.id != null)
                if (typeof object.id === "string")
                    $util.base64.decode(object.id, message.id = $util.newBuffer($util.base64.length(object.id)), 0);
                else if (object.id.length >= 0)
                    message.id = object.id;
            return message;
        };

        /**
         * Creates a plain object from a KeyId message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.KeyId
         * @static
         * @param {proto.KeyId} message KeyId
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        KeyId.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                object.id = options.bytes === String ? $util.base64.encode(message.id, 0, message.id.length) : options.bytes === Array ? Array.prototype.slice.call(message.id) : message.id;
            return object;
        };

        /**
         * Converts this KeyId to JSON.
         * @function toJSON
         * @memberof proto.KeyId
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        KeyId.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for KeyId
         * @function getTypeUrl
         * @memberof proto.KeyId
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        KeyId.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.KeyId";
        };

        return KeyId;
    })();

    proto.LIDMigrationMappingSyncMessage = (function() {

        /**
         * Properties of a LIDMigrationMappingSyncMessage.
         * @memberof proto
         * @interface ILIDMigrationMappingSyncMessage
         * @property {Uint8Array|null} [encodedMappingPayload] LIDMigrationMappingSyncMessage encodedMappingPayload
         */

        /**
         * Constructs a new LIDMigrationMappingSyncMessage.
         * @memberof proto
         * @classdesc Represents a LIDMigrationMappingSyncMessage.
         * @implements ILIDMigrationMappingSyncMessage
         * @constructor
         * @param {proto.ILIDMigrationMappingSyncMessage=} [properties] Properties to set
         */
        function LIDMigrationMappingSyncMessage(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LIDMigrationMappingSyncMessage encodedMappingPayload.
         * @member {Uint8Array|null|undefined} encodedMappingPayload
         * @memberof proto.LIDMigrationMappingSyncMessage
         * @instance
         */
        LIDMigrationMappingSyncMessage.prototype.encodedMappingPayload = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LIDMigrationMappingSyncMessage.prototype, "_encodedMappingPayload", {
            get: $util.oneOfGetter($oneOfFields = ["encodedMappingPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new LIDMigrationMappingSyncMessage instance using the specified properties.
         * @function create
         * @memberof proto.LIDMigrationMappingSyncMessage
         * @static
         * @param {proto.ILIDMigrationMappingSyncMessage=} [properties] Properties to set
         * @returns {proto.LIDMigrationMappingSyncMessage} LIDMigrationMappingSyncMessage instance
         */
        LIDMigrationMappingSyncMessage.create = function create(properties) {
            return new LIDMigrationMappingSyncMessage(properties);
        };

        /**
         * Encodes the specified LIDMigrationMappingSyncMessage message. Does not implicitly {@link proto.LIDMigrationMappingSyncMessage.verify|verify} messages.
         * @function encode
         * @memberof proto.LIDMigrationMappingSyncMessage
         * @static
         * @param {proto.ILIDMigrationMappingSyncMessage} message LIDMigrationMappingSyncMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LIDMigrationMappingSyncMessage.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encodedMappingPayload != null && Object.hasOwnProperty.call(message, "encodedMappingPayload"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encodedMappingPayload);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LIDMigrationMappingSyncMessage message, length delimited. Does not implicitly {@link proto.LIDMigrationMappingSyncMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.LIDMigrationMappingSyncMessage
         * @static
         * @param {proto.ILIDMigrationMappingSyncMessage} message LIDMigrationMappingSyncMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LIDMigrationMappingSyncMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LIDMigrationMappingSyncMessage message from the specified reader or buffer.
         * @function decode
         * @memberof proto.LIDMigrationMappingSyncMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.LIDMigrationMappingSyncMessage} LIDMigrationMappingSyncMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LIDMigrationMappingSyncMessage.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.encodedMappingPayload = reader.bytes();
                        message._encodedMappingPayload = "encodedMappingPayload";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a LIDMigrationMappingSyncMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.LIDMigrationMappingSyncMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.LIDMigrationMappingSyncMessage} LIDMigrationMappingSyncMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LIDMigrationMappingSyncMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LIDMigrationMappingSyncMessage message.
         * @function verify
         * @memberof proto.LIDMigrationMappingSyncMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LIDMigrationMappingSyncMessage.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.encodedMappingPayload != null && Object.hasOwnProperty.call(message, "encodedMappingPayload")) {
                properties._encodedMappingPayload = 1;
                if (!(message.encodedMappingPayload && typeof message.encodedMappingPayload.length === "number" || $util.isString(message.encodedMappingPayload)))
                    return "encodedMappingPayload: buffer expected";
            }
            return null;
        };

        /**
         * Creates a LIDMigrationMappingSyncMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.LIDMigrationMappingSyncMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.LIDMigrationMappingSyncMessage} LIDMigrationMappingSyncMessage
         */
        LIDMigrationMappingSyncMessage.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.LIDMigrationMappingSyncMessage: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.encodedMappingPayload != null)
                if (typeof object.encodedMappingPayload === "string")
                    $util.base64.decode(object.encodedMappingPayload, message.encodedMappingPayload = $util.newBuffer($util.base64.length(object.encodedMappingPayload)), 0);
                else if (object.encodedMappingPayload.length >= 0)
                    message.encodedMappingPayload = object.encodedMappingPayload;
            return message;
        };

        /**
         * Creates a plain object from a LIDMigrationMappingSyncMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.LIDMigrationMappingSyncMessage
         * @static
         * @param {proto.LIDMigrationMappingSyncMessage} message LIDMigrationMappingSyncMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LIDMigrationMappingSyncMessage.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.encodedMappingPayload != null && Object.hasOwnProperty.call(message, "encodedMappingPayload"))
                object.encodedMappingPayload = options.bytes === String ? $util.base64.encode(message.encodedMappingPayload, 0, message.encodedMappingPayload.length) : options.bytes === Array ? Array.prototype.slice.call(message.encodedMappingPayload) : message.encodedMappingPayload;
            return object;
        };

        /**
         * Converts this LIDMigrationMappingSyncMessage to JSON.
         * @function toJSON
         * @memberof proto.LIDMigrationMappingSyncMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LIDMigrationMappingSyncMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LIDMigrationMappingSyncMessage
         * @function getTypeUrl
         * @memberof proto.LIDMigrationMappingSyncMessage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LIDMigrationMappingSyncMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.LIDMigrationMappingSyncMessage";
        };

        return LIDMigrationMappingSyncMessage;
    })();

    proto.LegacyMessage = (function() {

        /**
         * Properties of a LegacyMessage.
         * @memberof proto
         * @interface ILegacyMessage
         * @property {proto.Message.IEventResponseMessage|null} [eventResponseMessage] LegacyMessage eventResponseMessage
         * @property {proto.Message.IPollVoteMessage|null} [pollVote] LegacyMessage pollVote
         */

        /**
         * Constructs a new LegacyMessage.
         * @memberof proto
         * @classdesc Represents a LegacyMessage.
         * @implements ILegacyMessage
         * @constructor
         * @param {proto.ILegacyMessage=} [properties] Properties to set
         */
        function LegacyMessage(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LegacyMessage eventResponseMessage.
         * @member {proto.Message.IEventResponseMessage|null|undefined} eventResponseMessage
         * @memberof proto.LegacyMessage
         * @instance
         */
        LegacyMessage.prototype.eventResponseMessage = null;

        /**
         * LegacyMessage pollVote.
         * @member {proto.Message.IPollVoteMessage|null|undefined} pollVote
         * @memberof proto.LegacyMessage
         * @instance
         */
        LegacyMessage.prototype.pollVote = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LegacyMessage.prototype, "_eventResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["eventResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LegacyMessage.prototype, "_pollVote", {
            get: $util.oneOfGetter($oneOfFields = ["pollVote"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new LegacyMessage instance using the specified properties.
         * @function create
         * @memberof proto.LegacyMessage
         * @static
         * @param {proto.ILegacyMessage=} [properties] Properties to set
         * @returns {proto.LegacyMessage} LegacyMessage instance
         */
        LegacyMessage.create = function create(properties) {
            return new LegacyMessage(properties);
        };

        /**
         * Encodes the specified LegacyMessage message. Does not implicitly {@link proto.LegacyMessage.verify|verify} messages.
         * @function encode
         * @memberof proto.LegacyMessage
         * @static
         * @param {proto.ILegacyMessage} message LegacyMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LegacyMessage.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.eventResponseMessage != null && Object.hasOwnProperty.call(message, "eventResponseMessage"))
                $root.proto.Message.EventResponseMessage.encode(message.eventResponseMessage, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.pollVote != null && Object.hasOwnProperty.call(message, "pollVote"))
                $root.proto.Message.PollVoteMessage.encode(message.pollVote, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LegacyMessage message, length delimited. Does not implicitly {@link proto.LegacyMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.LegacyMessage
         * @static
         * @param {proto.ILegacyMessage} message LegacyMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LegacyMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LegacyMessage message from the specified reader or buffer.
         * @function decode
         * @memberof proto.LegacyMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.LegacyMessage} LegacyMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LegacyMessage.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.eventResponseMessage = $root.proto.Message.EventResponseMessage.decode(reader, reader.uint32(), undefined, q + 1, message.eventResponseMessage);
                        message._eventResponseMessage = "eventResponseMessage";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.pollVote = $root.proto.Message.PollVoteMessage.decode(reader, reader.uint32(), undefined, q + 1, message.pollVote);
                        message._pollVote = "pollVote";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a LegacyMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.LegacyMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.LegacyMessage} LegacyMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LegacyMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LegacyMessage message.
         * @function verify
         * @memberof proto.LegacyMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LegacyMessage.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.eventResponseMessage != null && Object.hasOwnProperty.call(message, "eventResponseMessage")) {
                properties._eventResponseMessage = 1;
                {
                    var error = $root.proto.Message.EventResponseMessage.verify(message.eventResponseMessage, q + 1);
                    if (error)
                        return "eventResponseMessage." + error;
                }
            }
            if (message.pollVote != null && Object.hasOwnProperty.call(message, "pollVote")) {
                properties._pollVote = 1;
                {
                    var error = $root.proto.Message.PollVoteMessage.verify(message.pollVote, q + 1);
                    if (error)
                        return "pollVote." + error;
                }
            }
            return null;
        };

        /**
         * Creates a LegacyMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.LegacyMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.LegacyMessage} LegacyMessage
         */
        LegacyMessage.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.LegacyMessage: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.eventResponseMessage != null) {
                if (!$util.isObject(object.eventResponseMessage))
                    throw TypeError(".proto.LegacyMessage.eventResponseMessage: object expected");
                message.eventResponseMessage = $root.proto.Message.EventResponseMessage.fromObject(object.eventResponseMessage, q + 1);
            }
            if (object.pollVote != null) {
                if (!$util.isObject(object.pollVote))
                    throw TypeError(".proto.LegacyMessage.pollVote: object expected");
                message.pollVote = $root.proto.Message.PollVoteMessage.fromObject(object.pollVote, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a LegacyMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.LegacyMessage
         * @static
         * @param {proto.LegacyMessage} message LegacyMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LegacyMessage.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.eventResponseMessage != null && Object.hasOwnProperty.call(message, "eventResponseMessage"))
                object.eventResponseMessage = $root.proto.Message.EventResponseMessage.toObject(message.eventResponseMessage, options, q + 1);
            if (message.pollVote != null && Object.hasOwnProperty.call(message, "pollVote"))
                object.pollVote = $root.proto.Message.PollVoteMessage.toObject(message.pollVote, options, q + 1);
            return object;
        };

        /**
         * Converts this LegacyMessage to JSON.
         * @function toJSON
         * @memberof proto.LegacyMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LegacyMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LegacyMessage
         * @function getTypeUrl
         * @memberof proto.LegacyMessage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LegacyMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.LegacyMessage";
        };

        return LegacyMessage;
    })();

    proto.LimitSharing = (function() {

        /**
         * Properties of a LimitSharing.
         * @memberof proto
         * @interface ILimitSharing
         * @property {boolean|null} [sharingLimited] LimitSharing sharingLimited
         * @property {proto.LimitSharing.TriggerType|null} [trigger] LimitSharing trigger
         * @property {number|Long|null} [limitSharingSettingTimestamp] LimitSharing limitSharingSettingTimestamp
         * @property {boolean|null} [initiatedByMe] LimitSharing initiatedByMe
         */

        /**
         * Constructs a new LimitSharing.
         * @memberof proto
         * @classdesc Represents a LimitSharing.
         * @implements ILimitSharing
         * @constructor
         * @param {proto.ILimitSharing=} [properties] Properties to set
         */
        function LimitSharing(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LimitSharing sharingLimited.
         * @member {boolean|null|undefined} sharingLimited
         * @memberof proto.LimitSharing
         * @instance
         */
        LimitSharing.prototype.sharingLimited = null;

        /**
         * LimitSharing trigger.
         * @member {proto.LimitSharing.TriggerType|null|undefined} trigger
         * @memberof proto.LimitSharing
         * @instance
         */
        LimitSharing.prototype.trigger = null;

        /**
         * LimitSharing limitSharingSettingTimestamp.
         * @member {number|Long|null|undefined} limitSharingSettingTimestamp
         * @memberof proto.LimitSharing
         * @instance
         */
        LimitSharing.prototype.limitSharingSettingTimestamp = null;

        /**
         * LimitSharing initiatedByMe.
         * @member {boolean|null|undefined} initiatedByMe
         * @memberof proto.LimitSharing
         * @instance
         */
        LimitSharing.prototype.initiatedByMe = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LimitSharing.prototype, "_sharingLimited", {
            get: $util.oneOfGetter($oneOfFields = ["sharingLimited"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LimitSharing.prototype, "_trigger", {
            get: $util.oneOfGetter($oneOfFields = ["trigger"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LimitSharing.prototype, "_limitSharingSettingTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["limitSharingSettingTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LimitSharing.prototype, "_initiatedByMe", {
            get: $util.oneOfGetter($oneOfFields = ["initiatedByMe"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new LimitSharing instance using the specified properties.
         * @function create
         * @memberof proto.LimitSharing
         * @static
         * @param {proto.ILimitSharing=} [properties] Properties to set
         * @returns {proto.LimitSharing} LimitSharing instance
         */
        LimitSharing.create = function create(properties) {
            return new LimitSharing(properties);
        };

        /**
         * Encodes the specified LimitSharing message. Does not implicitly {@link proto.LimitSharing.verify|verify} messages.
         * @function encode
         * @memberof proto.LimitSharing
         * @static
         * @param {proto.ILimitSharing} message LimitSharing message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LimitSharing.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.sharingLimited != null && Object.hasOwnProperty.call(message, "sharingLimited"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.sharingLimited);
            if (message.trigger != null && Object.hasOwnProperty.call(message, "trigger"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.trigger);
            if (message.limitSharingSettingTimestamp != null && Object.hasOwnProperty.call(message, "limitSharingSettingTimestamp"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.limitSharingSettingTimestamp);
            if (message.initiatedByMe != null && Object.hasOwnProperty.call(message, "initiatedByMe"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.initiatedByMe);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LimitSharing message, length delimited. Does not implicitly {@link proto.LimitSharing.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.LimitSharing
         * @static
         * @param {proto.ILimitSharing} message LimitSharing message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LimitSharing.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LimitSharing message from the specified reader or buffer.
         * @function decode
         * @memberof proto.LimitSharing
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.LimitSharing} LimitSharing
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LimitSharing.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.sharingLimited = reader.bool();
                        message._sharingLimited = "sharingLimited";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.trigger = reader.int32();
                        message._trigger = "trigger";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.limitSharingSettingTimestamp = reader.int64();
                        message._limitSharingSettingTimestamp = "limitSharingSettingTimestamp";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.initiatedByMe = reader.bool();
                        message._initiatedByMe = "initiatedByMe";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a LimitSharing message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.LimitSharing
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.LimitSharing} LimitSharing
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LimitSharing.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LimitSharing message.
         * @function verify
         * @memberof proto.LimitSharing
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LimitSharing.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.sharingLimited != null && Object.hasOwnProperty.call(message, "sharingLimited")) {
                properties._sharingLimited = 1;
                if (typeof message.sharingLimited !== "boolean")
                    return "sharingLimited: boolean expected";
            }
            if (message.trigger != null && Object.hasOwnProperty.call(message, "trigger")) {
                properties._trigger = 1;
                if (typeof message.trigger !== "number" || (message.trigger | 0) !== message.trigger)
                    return "trigger: enum value expected";
            }
            if (message.limitSharingSettingTimestamp != null && Object.hasOwnProperty.call(message, "limitSharingSettingTimestamp")) {
                properties._limitSharingSettingTimestamp = 1;
                if (!$util.isInteger(message.limitSharingSettingTimestamp) && !(message.limitSharingSettingTimestamp && $util.isInteger(message.limitSharingSettingTimestamp.low) && $util.isInteger(message.limitSharingSettingTimestamp.high)))
                    return "limitSharingSettingTimestamp: integer|Long expected";
            }
            if (message.initiatedByMe != null && Object.hasOwnProperty.call(message, "initiatedByMe")) {
                properties._initiatedByMe = 1;
                if (typeof message.initiatedByMe !== "boolean")
                    return "initiatedByMe: boolean expected";
            }
            return null;
        };

        /**
         * Creates a LimitSharing message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.LimitSharing
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.LimitSharing} LimitSharing
         */
        LimitSharing.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.LimitSharing: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.sharingLimited != null)
                message.sharingLimited = Boolean(object.sharingLimited);
            switch (object.trigger) {
            case "UNKNOWN":
            case 0:
                message.trigger = 0;
                break;
            case "CHAT_SETTING":
            case 1:
                message.trigger = 1;
                break;
            case "BIZ_SUPPORTS_FB_HOSTING":
            case 2:
                message.trigger = 2;
                break;
            case "UNKNOWN_GROUP":
            case 3:
                message.trigger = 3;
                break;
            default:
                if (typeof object.trigger === "number" && (object.trigger | 0) === object.trigger)
                    message.trigger = object.trigger;
            }
            if (object.limitSharingSettingTimestamp != null)
                if ($util.Long)
                    message.limitSharingSettingTimestamp = $util.Long.fromValue(object.limitSharingSettingTimestamp, false);
                else if (typeof object.limitSharingSettingTimestamp === "string")
                    message.limitSharingSettingTimestamp = parseInt(object.limitSharingSettingTimestamp, 10);
                else if (typeof object.limitSharingSettingTimestamp === "number")
                    message.limitSharingSettingTimestamp = object.limitSharingSettingTimestamp;
                else if (typeof object.limitSharingSettingTimestamp === "object")
                    message.limitSharingSettingTimestamp = new $util.LongBits(object.limitSharingSettingTimestamp.low >>> 0, object.limitSharingSettingTimestamp.high >>> 0).toNumber();
            if (object.initiatedByMe != null)
                message.initiatedByMe = Boolean(object.initiatedByMe);
            return message;
        };

        /**
         * Creates a plain object from a LimitSharing message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.LimitSharing
         * @static
         * @param {proto.LimitSharing} message LimitSharing
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LimitSharing.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.sharingLimited != null && Object.hasOwnProperty.call(message, "sharingLimited"))
                object.sharingLimited = message.sharingLimited;
            if (message.trigger != null && Object.hasOwnProperty.call(message, "trigger"))
                object.trigger = options.enums === String ? $root.proto.LimitSharing.TriggerType[message.trigger] === undefined ? message.trigger : $root.proto.LimitSharing.TriggerType[message.trigger] : message.trigger;
            if (message.limitSharingSettingTimestamp != null && Object.hasOwnProperty.call(message, "limitSharingSettingTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.limitSharingSettingTimestamp = typeof message.limitSharingSettingTimestamp === "number" ? BigInt(message.limitSharingSettingTimestamp) : $util.Long.fromBits(message.limitSharingSettingTimestamp.low >>> 0, message.limitSharingSettingTimestamp.high >>> 0, false).toBigInt();
                else if (typeof message.limitSharingSettingTimestamp === "number")
                    object.limitSharingSettingTimestamp = options.longs === String ? String(message.limitSharingSettingTimestamp) : message.limitSharingSettingTimestamp;
                else
                    object.limitSharingSettingTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.limitSharingSettingTimestamp) : options.longs === Number ? new $util.LongBits(message.limitSharingSettingTimestamp.low >>> 0, message.limitSharingSettingTimestamp.high >>> 0).toNumber() : message.limitSharingSettingTimestamp;
            if (message.initiatedByMe != null && Object.hasOwnProperty.call(message, "initiatedByMe"))
                object.initiatedByMe = message.initiatedByMe;
            return object;
        };

        /**
         * Converts this LimitSharing to JSON.
         * @function toJSON
         * @memberof proto.LimitSharing
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LimitSharing.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LimitSharing
         * @function getTypeUrl
         * @memberof proto.LimitSharing
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LimitSharing.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.LimitSharing";
        };

        /**
         * TriggerType enum.
         * @name proto.LimitSharing.TriggerType
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} CHAT_SETTING=1 CHAT_SETTING value
         * @property {number} BIZ_SUPPORTS_FB_HOSTING=2 BIZ_SUPPORTS_FB_HOSTING value
         * @property {number} UNKNOWN_GROUP=3 UNKNOWN_GROUP value
         */
        LimitSharing.TriggerType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "CHAT_SETTING"] = 1;
            values[valuesById[2] = "BIZ_SUPPORTS_FB_HOSTING"] = 2;
            values[valuesById[3] = "UNKNOWN_GROUP"] = 3;
            return values;
        })();

        return LimitSharing;
    })();

    proto.LocalizedName = (function() {

        /**
         * Properties of a LocalizedName.
         * @memberof proto
         * @interface ILocalizedName
         * @property {string|null} [lg] LocalizedName lg
         * @property {string|null} [lc] LocalizedName lc
         * @property {string|null} [verifiedName] LocalizedName verifiedName
         */

        /**
         * Constructs a new LocalizedName.
         * @memberof proto
         * @classdesc Represents a LocalizedName.
         * @implements ILocalizedName
         * @constructor
         * @param {proto.ILocalizedName=} [properties] Properties to set
         */
        function LocalizedName(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LocalizedName lg.
         * @member {string|null|undefined} lg
         * @memberof proto.LocalizedName
         * @instance
         */
        LocalizedName.prototype.lg = null;

        /**
         * LocalizedName lc.
         * @member {string|null|undefined} lc
         * @memberof proto.LocalizedName
         * @instance
         */
        LocalizedName.prototype.lc = null;

        /**
         * LocalizedName verifiedName.
         * @member {string|null|undefined} verifiedName
         * @memberof proto.LocalizedName
         * @instance
         */
        LocalizedName.prototype.verifiedName = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LocalizedName.prototype, "_lg", {
            get: $util.oneOfGetter($oneOfFields = ["lg"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LocalizedName.prototype, "_lc", {
            get: $util.oneOfGetter($oneOfFields = ["lc"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LocalizedName.prototype, "_verifiedName", {
            get: $util.oneOfGetter($oneOfFields = ["verifiedName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new LocalizedName instance using the specified properties.
         * @function create
         * @memberof proto.LocalizedName
         * @static
         * @param {proto.ILocalizedName=} [properties] Properties to set
         * @returns {proto.LocalizedName} LocalizedName instance
         */
        LocalizedName.create = function create(properties) {
            return new LocalizedName(properties);
        };

        /**
         * Encodes the specified LocalizedName message. Does not implicitly {@link proto.LocalizedName.verify|verify} messages.
         * @function encode
         * @memberof proto.LocalizedName
         * @static
         * @param {proto.ILocalizedName} message LocalizedName message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LocalizedName.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.lg != null && Object.hasOwnProperty.call(message, "lg"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.lg);
            if (message.lc != null && Object.hasOwnProperty.call(message, "lc"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.lc);
            if (message.verifiedName != null && Object.hasOwnProperty.call(message, "verifiedName"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.verifiedName);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified LocalizedName message, length delimited. Does not implicitly {@link proto.LocalizedName.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.LocalizedName
         * @static
         * @param {proto.ILocalizedName} message LocalizedName message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LocalizedName.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LocalizedName message from the specified reader or buffer.
         * @function decode
         * @memberof proto.LocalizedName
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.LocalizedName} LocalizedName
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LocalizedName.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.lg = reader.stringVerify();
                        message._lg = "lg";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.lc = reader.stringVerify();
                        message._lc = "lc";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.verifiedName = reader.stringVerify();
                        message._verifiedName = "verifiedName";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a LocalizedName message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.LocalizedName
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.LocalizedName} LocalizedName
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LocalizedName.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LocalizedName message.
         * @function verify
         * @memberof proto.LocalizedName
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LocalizedName.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.lg != null && Object.hasOwnProperty.call(message, "lg")) {
                properties._lg = 1;
                if (!$util.isString(message.lg))
                    return "lg: string expected";
            }
            if (message.lc != null && Object.hasOwnProperty.call(message, "lc")) {
                properties._lc = 1;
                if (!$util.isString(message.lc))
                    return "lc: string expected";
            }
            if (message.verifiedName != null && Object.hasOwnProperty.call(message, "verifiedName")) {
                properties._verifiedName = 1;
                if (!$util.isString(message.verifiedName))
                    return "verifiedName: string expected";
            }
            return null;
        };

        /**
         * Creates a LocalizedName message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.LocalizedName
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.LocalizedName} LocalizedName
         */
        LocalizedName.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.LocalizedName: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.lg != null)
                message.lg = String(object.lg);
            if (object.lc != null)
                message.lc = String(object.lc);
            if (object.verifiedName != null)
                message.verifiedName = String(object.verifiedName);
            return message;
        };

        /**
         * Creates a plain object from a LocalizedName message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.LocalizedName
         * @static
         * @param {proto.LocalizedName} message LocalizedName
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LocalizedName.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.lg != null && Object.hasOwnProperty.call(message, "lg"))
                object.lg = message.lg;
            if (message.lc != null && Object.hasOwnProperty.call(message, "lc"))
                object.lc = message.lc;
            if (message.verifiedName != null && Object.hasOwnProperty.call(message, "verifiedName"))
                object.verifiedName = message.verifiedName;
            return object;
        };

        /**
         * Converts this LocalizedName to JSON.
         * @function toJSON
         * @memberof proto.LocalizedName
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LocalizedName.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LocalizedName
         * @function getTypeUrl
         * @memberof proto.LocalizedName
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LocalizedName.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.LocalizedName";
        };

        return LocalizedName;
    })();

    proto.Location = (function() {

        /**
         * Properties of a Location.
         * @memberof proto
         * @interface ILocation
         * @property {number|null} [degreesLatitude] Location degreesLatitude
         * @property {number|null} [degreesLongitude] Location degreesLongitude
         * @property {string|null} [name] Location name
         */

        /**
         * Constructs a new Location.
         * @memberof proto
         * @classdesc Represents a Location.
         * @implements ILocation
         * @constructor
         * @param {proto.ILocation=} [properties] Properties to set
         */
        function Location(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Location degreesLatitude.
         * @member {number|null|undefined} degreesLatitude
         * @memberof proto.Location
         * @instance
         */
        Location.prototype.degreesLatitude = null;

        /**
         * Location degreesLongitude.
         * @member {number|null|undefined} degreesLongitude
         * @memberof proto.Location
         * @instance
         */
        Location.prototype.degreesLongitude = null;

        /**
         * Location name.
         * @member {string|null|undefined} name
         * @memberof proto.Location
         * @instance
         */
        Location.prototype.name = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Location.prototype, "_degreesLatitude", {
            get: $util.oneOfGetter($oneOfFields = ["degreesLatitude"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Location.prototype, "_degreesLongitude", {
            get: $util.oneOfGetter($oneOfFields = ["degreesLongitude"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Location.prototype, "_name", {
            get: $util.oneOfGetter($oneOfFields = ["name"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new Location instance using the specified properties.
         * @function create
         * @memberof proto.Location
         * @static
         * @param {proto.ILocation=} [properties] Properties to set
         * @returns {proto.Location} Location instance
         */
        Location.create = function create(properties) {
            return new Location(properties);
        };

        /**
         * Encodes the specified Location message. Does not implicitly {@link proto.Location.verify|verify} messages.
         * @function encode
         * @memberof proto.Location
         * @static
         * @param {proto.ILocation} message Location message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Location.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.degreesLatitude != null && Object.hasOwnProperty.call(message, "degreesLatitude"))
                writer.uint32(/* id 1, wireType 1 =*/9).double(message.degreesLatitude);
            if (message.degreesLongitude != null && Object.hasOwnProperty.call(message, "degreesLongitude"))
                writer.uint32(/* id 2, wireType 1 =*/17).double(message.degreesLongitude);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.name);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Location message, length delimited. Does not implicitly {@link proto.Location.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Location
         * @static
         * @param {proto.ILocation} message Location message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Location.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Location message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Location
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Location} Location
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Location.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 1)
                            break;
                        message.degreesLatitude = reader.double();
                        message._degreesLatitude = "degreesLatitude";
                        continue;
                    }
                case 2: {
                        if (u !== 1)
                            break;
                        message.degreesLongitude = reader.double();
                        message._degreesLongitude = "degreesLongitude";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.name = reader.stringVerify();
                        message._name = "name";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a Location message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Location
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Location} Location
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Location.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Location message.
         * @function verify
         * @memberof proto.Location
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Location.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.degreesLatitude != null && Object.hasOwnProperty.call(message, "degreesLatitude")) {
                properties._degreesLatitude = 1;
                if (typeof message.degreesLatitude !== "number")
                    return "degreesLatitude: number expected";
            }
            if (message.degreesLongitude != null && Object.hasOwnProperty.call(message, "degreesLongitude")) {
                properties._degreesLongitude = 1;
                if (typeof message.degreesLongitude !== "number")
                    return "degreesLongitude: number expected";
            }
            if (message.name != null && Object.hasOwnProperty.call(message, "name")) {
                properties._name = 1;
                if (!$util.isString(message.name))
                    return "name: string expected";
            }
            return null;
        };

        /**
         * Creates a Location message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Location
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Location} Location
         */
        Location.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.Location: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.degreesLatitude != null)
                message.degreesLatitude = Number(object.degreesLatitude);
            if (object.degreesLongitude != null)
                message.degreesLongitude = Number(object.degreesLongitude);
            if (object.name != null)
                message.name = String(object.name);
            return message;
        };

        /**
         * Creates a plain object from a Location message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Location
         * @static
         * @param {proto.Location} message Location
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Location.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.degreesLatitude != null && Object.hasOwnProperty.call(message, "degreesLatitude"))
                object.degreesLatitude = options.json && !isFinite(message.degreesLatitude) ? String(message.degreesLatitude) : message.degreesLatitude;
            if (message.degreesLongitude != null && Object.hasOwnProperty.call(message, "degreesLongitude"))
                object.degreesLongitude = options.json && !isFinite(message.degreesLongitude) ? String(message.degreesLongitude) : message.degreesLongitude;
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            return object;
        };

        /**
         * Converts this Location to JSON.
         * @function toJSON
         * @memberof proto.Location
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Location.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Location
         * @function getTypeUrl
         * @memberof proto.Location
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Location.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.Location";
        };

        return Location;
    })();

    proto.MediaData = (function() {

        /**
         * Properties of a MediaData.
         * @memberof proto
         * @interface IMediaData
         * @property {string|null} [localPath] MediaData localPath
         */

        /**
         * Constructs a new MediaData.
         * @memberof proto
         * @classdesc Represents a MediaData.
         * @implements IMediaData
         * @constructor
         * @param {proto.IMediaData=} [properties] Properties to set
         */
        function MediaData(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MediaData localPath.
         * @member {string|null|undefined} localPath
         * @memberof proto.MediaData
         * @instance
         */
        MediaData.prototype.localPath = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MediaData.prototype, "_localPath", {
            get: $util.oneOfGetter($oneOfFields = ["localPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new MediaData instance using the specified properties.
         * @function create
         * @memberof proto.MediaData
         * @static
         * @param {proto.IMediaData=} [properties] Properties to set
         * @returns {proto.MediaData} MediaData instance
         */
        MediaData.create = function create(properties) {
            return new MediaData(properties);
        };

        /**
         * Encodes the specified MediaData message. Does not implicitly {@link proto.MediaData.verify|verify} messages.
         * @function encode
         * @memberof proto.MediaData
         * @static
         * @param {proto.IMediaData} message MediaData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MediaData.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.localPath != null && Object.hasOwnProperty.call(message, "localPath"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.localPath);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MediaData message, length delimited. Does not implicitly {@link proto.MediaData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.MediaData
         * @static
         * @param {proto.IMediaData} message MediaData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MediaData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MediaData message from the specified reader or buffer.
         * @function decode
         * @memberof proto.MediaData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.MediaData} MediaData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MediaData.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.localPath = reader.stringVerify();
                        message._localPath = "localPath";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a MediaData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.MediaData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.MediaData} MediaData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MediaData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MediaData message.
         * @function verify
         * @memberof proto.MediaData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MediaData.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.localPath != null && Object.hasOwnProperty.call(message, "localPath")) {
                properties._localPath = 1;
                if (!$util.isString(message.localPath))
                    return "localPath: string expected";
            }
            return null;
        };

        /**
         * Creates a MediaData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.MediaData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.MediaData} MediaData
         */
        MediaData.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.MediaData: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.localPath != null)
                message.localPath = String(object.localPath);
            return message;
        };

        /**
         * Creates a plain object from a MediaData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.MediaData
         * @static
         * @param {proto.MediaData} message MediaData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MediaData.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.localPath != null && Object.hasOwnProperty.call(message, "localPath"))
                object.localPath = message.localPath;
            return object;
        };

        /**
         * Converts this MediaData to JSON.
         * @function toJSON
         * @memberof proto.MediaData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MediaData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MediaData
         * @function getTypeUrl
         * @memberof proto.MediaData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MediaData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.MediaData";
        };

        return MediaData;
    })();

    proto.MediaDomainInfo = (function() {

        /**
         * Properties of a MediaDomainInfo.
         * @memberof proto
         * @interface IMediaDomainInfo
         * @property {proto.MediaKeyDomain|null} [mediaKeyDomain] MediaDomainInfo mediaKeyDomain
         * @property {Uint8Array|null} [e2EeMediaKey] MediaDomainInfo e2EeMediaKey
         */

        /**
         * Constructs a new MediaDomainInfo.
         * @memberof proto
         * @classdesc Represents a MediaDomainInfo.
         * @implements IMediaDomainInfo
         * @constructor
         * @param {proto.IMediaDomainInfo=} [properties] Properties to set
         */
        function MediaDomainInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MediaDomainInfo mediaKeyDomain.
         * @member {proto.MediaKeyDomain|null|undefined} mediaKeyDomain
         * @memberof proto.MediaDomainInfo
         * @instance
         */
        MediaDomainInfo.prototype.mediaKeyDomain = null;

        /**
         * MediaDomainInfo e2EeMediaKey.
         * @member {Uint8Array|null|undefined} e2EeMediaKey
         * @memberof proto.MediaDomainInfo
         * @instance
         */
        MediaDomainInfo.prototype.e2EeMediaKey = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MediaDomainInfo.prototype, "_mediaKeyDomain", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKeyDomain"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MediaDomainInfo.prototype, "_e2EeMediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["e2EeMediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new MediaDomainInfo instance using the specified properties.
         * @function create
         * @memberof proto.MediaDomainInfo
         * @static
         * @param {proto.IMediaDomainInfo=} [properties] Properties to set
         * @returns {proto.MediaDomainInfo} MediaDomainInfo instance
         */
        MediaDomainInfo.create = function create(properties) {
            return new MediaDomainInfo(properties);
        };

        /**
         * Encodes the specified MediaDomainInfo message. Does not implicitly {@link proto.MediaDomainInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.MediaDomainInfo
         * @static
         * @param {proto.IMediaDomainInfo} message MediaDomainInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MediaDomainInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.mediaKeyDomain != null && Object.hasOwnProperty.call(message, "mediaKeyDomain"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.mediaKeyDomain);
            if (message.e2EeMediaKey != null && Object.hasOwnProperty.call(message, "e2EeMediaKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.e2EeMediaKey);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MediaDomainInfo message, length delimited. Does not implicitly {@link proto.MediaDomainInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.MediaDomainInfo
         * @static
         * @param {proto.IMediaDomainInfo} message MediaDomainInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MediaDomainInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MediaDomainInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.MediaDomainInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.MediaDomainInfo} MediaDomainInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MediaDomainInfo.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.mediaKeyDomain = reader.int32();
                        message._mediaKeyDomain = "mediaKeyDomain";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.e2EeMediaKey = reader.bytes();
                        message._e2EeMediaKey = "e2EeMediaKey";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a MediaDomainInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.MediaDomainInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.MediaDomainInfo} MediaDomainInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MediaDomainInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MediaDomainInfo message.
         * @function verify
         * @memberof proto.MediaDomainInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MediaDomainInfo.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.mediaKeyDomain != null && Object.hasOwnProperty.call(message, "mediaKeyDomain")) {
                properties._mediaKeyDomain = 1;
                if (typeof message.mediaKeyDomain !== "number" || (message.mediaKeyDomain | 0) !== message.mediaKeyDomain)
                    return "mediaKeyDomain: enum value expected";
            }
            if (message.e2EeMediaKey != null && Object.hasOwnProperty.call(message, "e2EeMediaKey")) {
                properties._e2EeMediaKey = 1;
                if (!(message.e2EeMediaKey && typeof message.e2EeMediaKey.length === "number" || $util.isString(message.e2EeMediaKey)))
                    return "e2EeMediaKey: buffer expected";
            }
            return null;
        };

        /**
         * Creates a MediaDomainInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.MediaDomainInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.MediaDomainInfo} MediaDomainInfo
         */
        MediaDomainInfo.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.MediaDomainInfo: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.mediaKeyDomain) {
            case "MEDIA_KEY_DOMAIN_UNKNOWN":
            case 0:
                message.mediaKeyDomain = 0;
                break;
            case "MEDIA_KEY_DOMAIN_E2EE":
            case 1:
                message.mediaKeyDomain = 1;
                break;
            case "MEDIA_KEY_DOMAIN_NON_E2EE":
            case 2:
                message.mediaKeyDomain = 2;
                break;
            default:
                if (typeof object.mediaKeyDomain === "number" && (object.mediaKeyDomain | 0) === object.mediaKeyDomain)
                    message.mediaKeyDomain = object.mediaKeyDomain;
            }
            if (object.e2EeMediaKey != null)
                if (typeof object.e2EeMediaKey === "string")
                    $util.base64.decode(object.e2EeMediaKey, message.e2EeMediaKey = $util.newBuffer($util.base64.length(object.e2EeMediaKey)), 0);
                else if (object.e2EeMediaKey.length >= 0)
                    message.e2EeMediaKey = object.e2EeMediaKey;
            return message;
        };

        /**
         * Creates a plain object from a MediaDomainInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.MediaDomainInfo
         * @static
         * @param {proto.MediaDomainInfo} message MediaDomainInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MediaDomainInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.mediaKeyDomain != null && Object.hasOwnProperty.call(message, "mediaKeyDomain"))
                object.mediaKeyDomain = options.enums === String ? $root.proto.MediaKeyDomain[message.mediaKeyDomain] === undefined ? message.mediaKeyDomain : $root.proto.MediaKeyDomain[message.mediaKeyDomain] : message.mediaKeyDomain;
            if (message.e2EeMediaKey != null && Object.hasOwnProperty.call(message, "e2EeMediaKey"))
                object.e2EeMediaKey = options.bytes === String ? $util.base64.encode(message.e2EeMediaKey, 0, message.e2EeMediaKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.e2EeMediaKey) : message.e2EeMediaKey;
            return object;
        };

        /**
         * Converts this MediaDomainInfo to JSON.
         * @function toJSON
         * @memberof proto.MediaDomainInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MediaDomainInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MediaDomainInfo
         * @function getTypeUrl
         * @memberof proto.MediaDomainInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MediaDomainInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.MediaDomainInfo";
        };

        return MediaDomainInfo;
    })();

    /**
     * MediaKeyDomain enum.
     * @name proto.MediaKeyDomain
     * @enum {number}
     * @property {number} MEDIA_KEY_DOMAIN_UNKNOWN=0 MEDIA_KEY_DOMAIN_UNKNOWN value
     * @property {number} MEDIA_KEY_DOMAIN_E2EE=1 MEDIA_KEY_DOMAIN_E2EE value
     * @property {number} MEDIA_KEY_DOMAIN_NON_E2EE=2 MEDIA_KEY_DOMAIN_NON_E2EE value
     */
    proto.MediaKeyDomain = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "MEDIA_KEY_DOMAIN_UNKNOWN"] = 0;
        values[valuesById[1] = "MEDIA_KEY_DOMAIN_E2EE"] = 1;
        values[valuesById[2] = "MEDIA_KEY_DOMAIN_NON_E2EE"] = 2;
        return values;
    })();

    proto.MediaNotifyMessage = (function() {

        /**
         * Properties of a MediaNotifyMessage.
         * @memberof proto
         * @interface IMediaNotifyMessage
         * @property {string|null} [expressPathUrl] MediaNotifyMessage expressPathUrl
         * @property {Uint8Array|null} [fileEncSha256] MediaNotifyMessage fileEncSha256
         * @property {number|Long|null} [fileLength] MediaNotifyMessage fileLength
         */

        /**
         * Constructs a new MediaNotifyMessage.
         * @memberof proto
         * @classdesc Represents a MediaNotifyMessage.
         * @implements IMediaNotifyMessage
         * @constructor
         * @param {proto.IMediaNotifyMessage=} [properties] Properties to set
         */
        function MediaNotifyMessage(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MediaNotifyMessage expressPathUrl.
         * @member {string|null|undefined} expressPathUrl
         * @memberof proto.MediaNotifyMessage
         * @instance
         */
        MediaNotifyMessage.prototype.expressPathUrl = null;

        /**
         * MediaNotifyMessage fileEncSha256.
         * @member {Uint8Array|null|undefined} fileEncSha256
         * @memberof proto.MediaNotifyMessage
         * @instance
         */
        MediaNotifyMessage.prototype.fileEncSha256 = null;

        /**
         * MediaNotifyMessage fileLength.
         * @member {number|Long|null|undefined} fileLength
         * @memberof proto.MediaNotifyMessage
         * @instance
         */
        MediaNotifyMessage.prototype.fileLength = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MediaNotifyMessage.prototype, "_expressPathUrl", {
            get: $util.oneOfGetter($oneOfFields = ["expressPathUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MediaNotifyMessage.prototype, "_fileEncSha256", {
            get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MediaNotifyMessage.prototype, "_fileLength", {
            get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new MediaNotifyMessage instance using the specified properties.
         * @function create
         * @memberof proto.MediaNotifyMessage
         * @static
         * @param {proto.IMediaNotifyMessage=} [properties] Properties to set
         * @returns {proto.MediaNotifyMessage} MediaNotifyMessage instance
         */
        MediaNotifyMessage.create = function create(properties) {
            return new MediaNotifyMessage(properties);
        };

        /**
         * Encodes the specified MediaNotifyMessage message. Does not implicitly {@link proto.MediaNotifyMessage.verify|verify} messages.
         * @function encode
         * @memberof proto.MediaNotifyMessage
         * @static
         * @param {proto.IMediaNotifyMessage} message MediaNotifyMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MediaNotifyMessage.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.expressPathUrl != null && Object.hasOwnProperty.call(message, "expressPathUrl"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.expressPathUrl);
            if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.fileEncSha256);
            if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.fileLength);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MediaNotifyMessage message, length delimited. Does not implicitly {@link proto.MediaNotifyMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.MediaNotifyMessage
         * @static
         * @param {proto.IMediaNotifyMessage} message MediaNotifyMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MediaNotifyMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MediaNotifyMessage message from the specified reader or buffer.
         * @function decode
         * @memberof proto.MediaNotifyMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.MediaNotifyMessage} MediaNotifyMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MediaNotifyMessage.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.expressPathUrl = reader.stringVerify();
                        message._expressPathUrl = "expressPathUrl";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.fileEncSha256 = reader.bytes();
                        message._fileEncSha256 = "fileEncSha256";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.fileLength = reader.uint64();
                        message._fileLength = "fileLength";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a MediaNotifyMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.MediaNotifyMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.MediaNotifyMessage} MediaNotifyMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MediaNotifyMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MediaNotifyMessage message.
         * @function verify
         * @memberof proto.MediaNotifyMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MediaNotifyMessage.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.expressPathUrl != null && Object.hasOwnProperty.call(message, "expressPathUrl")) {
                properties._expressPathUrl = 1;
                if (!$util.isString(message.expressPathUrl))
                    return "expressPathUrl: string expected";
            }
            if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256")) {
                properties._fileEncSha256 = 1;
                if (!(message.fileEncSha256 && typeof message.fileEncSha256.length === "number" || $util.isString(message.fileEncSha256)))
                    return "fileEncSha256: buffer expected";
            }
            if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength")) {
                properties._fileLength = 1;
                if (!$util.isInteger(message.fileLength) && !(message.fileLength && $util.isInteger(message.fileLength.low) && $util.isInteger(message.fileLength.high)))
                    return "fileLength: integer|Long expected";
            }
            return null;
        };

        /**
         * Creates a MediaNotifyMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.MediaNotifyMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.MediaNotifyMessage} MediaNotifyMessage
         */
        MediaNotifyMessage.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.MediaNotifyMessage: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.expressPathUrl != null)
                message.expressPathUrl = String(object.expressPathUrl);
            if (object.fileEncSha256 != null)
                if (typeof object.fileEncSha256 === "string")
                    $util.base64.decode(object.fileEncSha256, message.fileEncSha256 = $util.newBuffer($util.base64.length(object.fileEncSha256)), 0);
                else if (object.fileEncSha256.length >= 0)
                    message.fileEncSha256 = object.fileEncSha256;
            if (object.fileLength != null)
                if ($util.Long)
                    message.fileLength = $util.Long.fromValue(object.fileLength, true);
                else if (typeof object.fileLength === "string")
                    message.fileLength = parseInt(object.fileLength, 10);
                else if (typeof object.fileLength === "number")
                    message.fileLength = object.fileLength;
                else if (typeof object.fileLength === "object")
                    message.fileLength = new $util.LongBits(object.fileLength.low >>> 0, object.fileLength.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a MediaNotifyMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.MediaNotifyMessage
         * @static
         * @param {proto.MediaNotifyMessage} message MediaNotifyMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MediaNotifyMessage.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.expressPathUrl != null && Object.hasOwnProperty.call(message, "expressPathUrl"))
                object.expressPathUrl = message.expressPathUrl;
            if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                object.fileEncSha256 = options.bytes === String ? $util.base64.encode(message.fileEncSha256, 0, message.fileEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileEncSha256) : message.fileEncSha256;
            if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.fileLength = typeof message.fileLength === "number" ? BigInt(message.fileLength) : $util.Long.fromBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0, true).toBigInt();
                else if (typeof message.fileLength === "number")
                    object.fileLength = options.longs === String ? String(message.fileLength) : message.fileLength;
                else
                    object.fileLength = options.longs === String ? $util.Long.prototype.toString.call(message.fileLength) : options.longs === Number ? new $util.LongBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0).toNumber(true) : message.fileLength;
            return object;
        };

        /**
         * Converts this MediaNotifyMessage to JSON.
         * @function toJSON
         * @memberof proto.MediaNotifyMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MediaNotifyMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MediaNotifyMessage
         * @function getTypeUrl
         * @memberof proto.MediaNotifyMessage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MediaNotifyMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.MediaNotifyMessage";
        };

        return MediaNotifyMessage;
    })();

    proto.MediaRetryNotification = (function() {

        /**
         * Properties of a MediaRetryNotification.
         * @memberof proto
         * @interface IMediaRetryNotification
         * @property {string|null} [stanzaId] MediaRetryNotification stanzaId
         * @property {string|null} [directPath] MediaRetryNotification directPath
         * @property {proto.MediaRetryNotification.ResultType|null} [result] MediaRetryNotification result
         * @property {Uint8Array|null} [messageSecret] MediaRetryNotification messageSecret
         */

        /**
         * Constructs a new MediaRetryNotification.
         * @memberof proto
         * @classdesc Represents a MediaRetryNotification.
         * @implements IMediaRetryNotification
         * @constructor
         * @param {proto.IMediaRetryNotification=} [properties] Properties to set
         */
        function MediaRetryNotification(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MediaRetryNotification stanzaId.
         * @member {string|null|undefined} stanzaId
         * @memberof proto.MediaRetryNotification
         * @instance
         */
        MediaRetryNotification.prototype.stanzaId = null;

        /**
         * MediaRetryNotification directPath.
         * @member {string|null|undefined} directPath
         * @memberof proto.MediaRetryNotification
         * @instance
         */
        MediaRetryNotification.prototype.directPath = null;

        /**
         * MediaRetryNotification result.
         * @member {proto.MediaRetryNotification.ResultType|null|undefined} result
         * @memberof proto.MediaRetryNotification
         * @instance
         */
        MediaRetryNotification.prototype.result = null;

        /**
         * MediaRetryNotification messageSecret.
         * @member {Uint8Array|null|undefined} messageSecret
         * @memberof proto.MediaRetryNotification
         * @instance
         */
        MediaRetryNotification.prototype.messageSecret = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MediaRetryNotification.prototype, "_stanzaId", {
            get: $util.oneOfGetter($oneOfFields = ["stanzaId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MediaRetryNotification.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MediaRetryNotification.prototype, "_result", {
            get: $util.oneOfGetter($oneOfFields = ["result"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MediaRetryNotification.prototype, "_messageSecret", {
            get: $util.oneOfGetter($oneOfFields = ["messageSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new MediaRetryNotification instance using the specified properties.
         * @function create
         * @memberof proto.MediaRetryNotification
         * @static
         * @param {proto.IMediaRetryNotification=} [properties] Properties to set
         * @returns {proto.MediaRetryNotification} MediaRetryNotification instance
         */
        MediaRetryNotification.create = function create(properties) {
            return new MediaRetryNotification(properties);
        };

        /**
         * Encodes the specified MediaRetryNotification message. Does not implicitly {@link proto.MediaRetryNotification.verify|verify} messages.
         * @function encode
         * @memberof proto.MediaRetryNotification
         * @static
         * @param {proto.IMediaRetryNotification} message MediaRetryNotification message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MediaRetryNotification.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.stanzaId != null && Object.hasOwnProperty.call(message, "stanzaId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.stanzaId);
            if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.directPath);
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.result);
            if (message.messageSecret != null && Object.hasOwnProperty.call(message, "messageSecret"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.messageSecret);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MediaRetryNotification message, length delimited. Does not implicitly {@link proto.MediaRetryNotification.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.MediaRetryNotification
         * @static
         * @param {proto.IMediaRetryNotification} message MediaRetryNotification message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MediaRetryNotification.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MediaRetryNotification message from the specified reader or buffer.
         * @function decode
         * @memberof proto.MediaRetryNotification
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.MediaRetryNotification} MediaRetryNotification
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MediaRetryNotification.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.stanzaId = reader.stringVerify();
                        message._stanzaId = "stanzaId";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.directPath = reader.stringVerify();
                        message._directPath = "directPath";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.result = reader.int32();
                        message._result = "result";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.messageSecret = reader.bytes();
                        message._messageSecret = "messageSecret";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a MediaRetryNotification message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.MediaRetryNotification
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.MediaRetryNotification} MediaRetryNotification
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MediaRetryNotification.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MediaRetryNotification message.
         * @function verify
         * @memberof proto.MediaRetryNotification
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MediaRetryNotification.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.stanzaId != null && Object.hasOwnProperty.call(message, "stanzaId")) {
                properties._stanzaId = 1;
                if (!$util.isString(message.stanzaId))
                    return "stanzaId: string expected";
            }
            if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath")) {
                properties._directPath = 1;
                if (!$util.isString(message.directPath))
                    return "directPath: string expected";
            }
            if (message.result != null && Object.hasOwnProperty.call(message, "result")) {
                properties._result = 1;
                if (typeof message.result !== "number" || (message.result | 0) !== message.result)
                    return "result: enum value expected";
            }
            if (message.messageSecret != null && Object.hasOwnProperty.call(message, "messageSecret")) {
                properties._messageSecret = 1;
                if (!(message.messageSecret && typeof message.messageSecret.length === "number" || $util.isString(message.messageSecret)))
                    return "messageSecret: buffer expected";
            }
            return null;
        };

        /**
         * Creates a MediaRetryNotification message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.MediaRetryNotification
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.MediaRetryNotification} MediaRetryNotification
         */
        MediaRetryNotification.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.MediaRetryNotification: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.stanzaId != null)
                message.stanzaId = String(object.stanzaId);
            if (object.directPath != null)
                message.directPath = String(object.directPath);
            switch (object.result) {
            case "GENERAL_ERROR":
            case 0:
                message.result = 0;
                break;
            case "SUCCESS":
            case 1:
                message.result = 1;
                break;
            case "NOT_FOUND":
            case 2:
                message.result = 2;
                break;
            case "DECRYPTION_ERROR":
            case 3:
                message.result = 3;
                break;
            default:
                if (typeof object.result === "number" && (object.result | 0) === object.result)
                    message.result = object.result;
            }
            if (object.messageSecret != null)
                if (typeof object.messageSecret === "string")
                    $util.base64.decode(object.messageSecret, message.messageSecret = $util.newBuffer($util.base64.length(object.messageSecret)), 0);
                else if (object.messageSecret.length >= 0)
                    message.messageSecret = object.messageSecret;
            return message;
        };

        /**
         * Creates a plain object from a MediaRetryNotification message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.MediaRetryNotification
         * @static
         * @param {proto.MediaRetryNotification} message MediaRetryNotification
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MediaRetryNotification.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.stanzaId != null && Object.hasOwnProperty.call(message, "stanzaId"))
                object.stanzaId = message.stanzaId;
            if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                object.directPath = message.directPath;
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                object.result = options.enums === String ? $root.proto.MediaRetryNotification.ResultType[message.result] === undefined ? message.result : $root.proto.MediaRetryNotification.ResultType[message.result] : message.result;
            if (message.messageSecret != null && Object.hasOwnProperty.call(message, "messageSecret"))
                object.messageSecret = options.bytes === String ? $util.base64.encode(message.messageSecret, 0, message.messageSecret.length) : options.bytes === Array ? Array.prototype.slice.call(message.messageSecret) : message.messageSecret;
            return object;
        };

        /**
         * Converts this MediaRetryNotification to JSON.
         * @function toJSON
         * @memberof proto.MediaRetryNotification
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MediaRetryNotification.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MediaRetryNotification
         * @function getTypeUrl
         * @memberof proto.MediaRetryNotification
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MediaRetryNotification.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.MediaRetryNotification";
        };

        /**
         * ResultType enum.
         * @name proto.MediaRetryNotification.ResultType
         * @enum {number}
         * @property {number} GENERAL_ERROR=0 GENERAL_ERROR value
         * @property {number} SUCCESS=1 SUCCESS value
         * @property {number} NOT_FOUND=2 NOT_FOUND value
         * @property {number} DECRYPTION_ERROR=3 DECRYPTION_ERROR value
         */
        MediaRetryNotification.ResultType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "GENERAL_ERROR"] = 0;
            values[valuesById[1] = "SUCCESS"] = 1;
            values[valuesById[2] = "NOT_FOUND"] = 2;
            values[valuesById[3] = "DECRYPTION_ERROR"] = 3;
            return values;
        })();

        return MediaRetryNotification;
    })();

    /**
     * MediaVisibility enum.
     * @name proto.MediaVisibility
     * @enum {number}
     * @property {number} DEFAULT=0 DEFAULT value
     * @property {number} OFF=1 OFF value
     * @property {number} ON=2 ON value
     */
    proto.MediaVisibility = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "DEFAULT"] = 0;
        values[valuesById[1] = "OFF"] = 1;
        values[valuesById[2] = "ON"] = 2;
        return values;
    })();

    proto.MemberLabel = (function() {

        /**
         * Properties of a MemberLabel.
         * @memberof proto
         * @interface IMemberLabel
         * @property {string|null} [label] MemberLabel label
         * @property {number|Long|null} [labelTimestamp] MemberLabel labelTimestamp
         */

        /**
         * Constructs a new MemberLabel.
         * @memberof proto
         * @classdesc Represents a MemberLabel.
         * @implements IMemberLabel
         * @constructor
         * @param {proto.IMemberLabel=} [properties] Properties to set
         */
        function MemberLabel(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MemberLabel label.
         * @member {string|null|undefined} label
         * @memberof proto.MemberLabel
         * @instance
         */
        MemberLabel.prototype.label = null;

        /**
         * MemberLabel labelTimestamp.
         * @member {number|Long|null|undefined} labelTimestamp
         * @memberof proto.MemberLabel
         * @instance
         */
        MemberLabel.prototype.labelTimestamp = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MemberLabel.prototype, "_label", {
            get: $util.oneOfGetter($oneOfFields = ["label"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MemberLabel.prototype, "_labelTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["labelTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new MemberLabel instance using the specified properties.
         * @function create
         * @memberof proto.MemberLabel
         * @static
         * @param {proto.IMemberLabel=} [properties] Properties to set
         * @returns {proto.MemberLabel} MemberLabel instance
         */
        MemberLabel.create = function create(properties) {
            return new MemberLabel(properties);
        };

        /**
         * Encodes the specified MemberLabel message. Does not implicitly {@link proto.MemberLabel.verify|verify} messages.
         * @function encode
         * @memberof proto.MemberLabel
         * @static
         * @param {proto.IMemberLabel} message MemberLabel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MemberLabel.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.label != null && Object.hasOwnProperty.call(message, "label"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.label);
            if (message.labelTimestamp != null && Object.hasOwnProperty.call(message, "labelTimestamp"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.labelTimestamp);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MemberLabel message, length delimited. Does not implicitly {@link proto.MemberLabel.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.MemberLabel
         * @static
         * @param {proto.IMemberLabel} message MemberLabel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MemberLabel.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MemberLabel message from the specified reader or buffer.
         * @function decode
         * @memberof proto.MemberLabel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.MemberLabel} MemberLabel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MemberLabel.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.label = reader.stringVerify();
                        message._label = "label";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.labelTimestamp = reader.int64();
                        message._labelTimestamp = "labelTimestamp";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a MemberLabel message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.MemberLabel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.MemberLabel} MemberLabel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MemberLabel.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MemberLabel message.
         * @function verify
         * @memberof proto.MemberLabel
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MemberLabel.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.label != null && Object.hasOwnProperty.call(message, "label")) {
                properties._label = 1;
                if (!$util.isString(message.label))
                    return "label: string expected";
            }
            if (message.labelTimestamp != null && Object.hasOwnProperty.call(message, "labelTimestamp")) {
                properties._labelTimestamp = 1;
                if (!$util.isInteger(message.labelTimestamp) && !(message.labelTimestamp && $util.isInteger(message.labelTimestamp.low) && $util.isInteger(message.labelTimestamp.high)))
                    return "labelTimestamp: integer|Long expected";
            }
            return null;
        };

        /**
         * Creates a MemberLabel message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.MemberLabel
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.MemberLabel} MemberLabel
         */
        MemberLabel.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.MemberLabel: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.label != null)
                message.label = String(object.label);
            if (object.labelTimestamp != null)
                if ($util.Long)
                    message.labelTimestamp = $util.Long.fromValue(object.labelTimestamp, false);
                else if (typeof object.labelTimestamp === "string")
                    message.labelTimestamp = parseInt(object.labelTimestamp, 10);
                else if (typeof object.labelTimestamp === "number")
                    message.labelTimestamp = object.labelTimestamp;
                else if (typeof object.labelTimestamp === "object")
                    message.labelTimestamp = new $util.LongBits(object.labelTimestamp.low >>> 0, object.labelTimestamp.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a MemberLabel message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.MemberLabel
         * @static
         * @param {proto.MemberLabel} message MemberLabel
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MemberLabel.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.label != null && Object.hasOwnProperty.call(message, "label"))
                object.label = message.label;
            if (message.labelTimestamp != null && Object.hasOwnProperty.call(message, "labelTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.labelTimestamp = typeof message.labelTimestamp === "number" ? BigInt(message.labelTimestamp) : $util.Long.fromBits(message.labelTimestamp.low >>> 0, message.labelTimestamp.high >>> 0, false).toBigInt();
                else if (typeof message.labelTimestamp === "number")
                    object.labelTimestamp = options.longs === String ? String(message.labelTimestamp) : message.labelTimestamp;
                else
                    object.labelTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.labelTimestamp) : options.longs === Number ? new $util.LongBits(message.labelTimestamp.low >>> 0, message.labelTimestamp.high >>> 0).toNumber() : message.labelTimestamp;
            return object;
        };

        /**
         * Converts this MemberLabel to JSON.
         * @function toJSON
         * @memberof proto.MemberLabel
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MemberLabel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MemberLabel
         * @function getTypeUrl
         * @memberof proto.MemberLabel
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MemberLabel.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.MemberLabel";
        };

        return MemberLabel;
    })();

    proto.Message = (function() {

        /**
         * Properties of a Message.
         * @memberof proto
         * @interface IMessage
         * @property {string|null} [conversation] Message conversation
         * @property {proto.Message.ISenderKeyDistributionMessage|null} [senderKeyDistributionMessage] Message senderKeyDistributionMessage
         * @property {proto.Message.IImageMessage|null} [imageMessage] Message imageMessage
         * @property {proto.Message.IContactMessage|null} [contactMessage] Message contactMessage
         * @property {proto.Message.ILocationMessage|null} [locationMessage] Message locationMessage
         * @property {proto.Message.IExtendedTextMessage|null} [extendedTextMessage] Message extendedTextMessage
         * @property {proto.Message.IDocumentMessage|null} [documentMessage] Message documentMessage
         * @property {proto.Message.IAudioMessage|null} [audioMessage] Message audioMessage
         * @property {proto.Message.IVideoMessage|null} [videoMessage] Message videoMessage
         * @property {proto.Message.ICall|null} [call] Message call
         * @property {proto.Message.IChat|null} [chat] Message chat
         * @property {proto.Message.IProtocolMessage|null} [protocolMessage] Message protocolMessage
         * @property {proto.Message.IContactsArrayMessage|null} [contactsArrayMessage] Message contactsArrayMessage
         * @property {proto.Message.IHighlyStructuredMessage|null} [highlyStructuredMessage] Message highlyStructuredMessage
         * @property {proto.Message.ISenderKeyDistributionMessage|null} [fastRatchetKeySenderKeyDistributionMessage] Message fastRatchetKeySenderKeyDistributionMessage
         * @property {proto.Message.ISendPaymentMessage|null} [sendPaymentMessage] Message sendPaymentMessage
         * @property {proto.Message.ILiveLocationMessage|null} [liveLocationMessage] Message liveLocationMessage
         * @property {proto.Message.IRequestPaymentMessage|null} [requestPaymentMessage] Message requestPaymentMessage
         * @property {proto.Message.IDeclinePaymentRequestMessage|null} [declinePaymentRequestMessage] Message declinePaymentRequestMessage
         * @property {proto.Message.ICancelPaymentRequestMessage|null} [cancelPaymentRequestMessage] Message cancelPaymentRequestMessage
         * @property {proto.Message.ITemplateMessage|null} [templateMessage] Message templateMessage
         * @property {proto.Message.IStickerMessage|null} [stickerMessage] Message stickerMessage
         * @property {proto.Message.IGroupInviteMessage|null} [groupInviteMessage] Message groupInviteMessage
         * @property {proto.Message.ITemplateButtonReplyMessage|null} [templateButtonReplyMessage] Message templateButtonReplyMessage
         * @property {proto.Message.IProductMessage|null} [productMessage] Message productMessage
         * @property {proto.Message.IDeviceSentMessage|null} [deviceSentMessage] Message deviceSentMessage
         * @property {proto.IMessageContextInfo|null} [messageContextInfo] Message messageContextInfo
         * @property {proto.Message.IListMessage|null} [listMessage] Message listMessage
         * @property {proto.Message.IFutureProofMessage|null} [viewOnceMessage] Message viewOnceMessage
         * @property {proto.Message.IOrderMessage|null} [orderMessage] Message orderMessage
         * @property {proto.Message.IListResponseMessage|null} [listResponseMessage] Message listResponseMessage
         * @property {proto.Message.IFutureProofMessage|null} [ephemeralMessage] Message ephemeralMessage
         * @property {proto.Message.IInvoiceMessage|null} [invoiceMessage] Message invoiceMessage
         * @property {proto.Message.IButtonsMessage|null} [buttonsMessage] Message buttonsMessage
         * @property {proto.Message.IButtonsResponseMessage|null} [buttonsResponseMessage] Message buttonsResponseMessage
         * @property {proto.Message.IPaymentInviteMessage|null} [paymentInviteMessage] Message paymentInviteMessage
         * @property {proto.Message.IInteractiveMessage|null} [interactiveMessage] Message interactiveMessage
         * @property {proto.Message.IReactionMessage|null} [reactionMessage] Message reactionMessage
         * @property {proto.Message.IStickerSyncRMRMessage|null} [stickerSyncRmrMessage] Message stickerSyncRmrMessage
         * @property {proto.Message.IInteractiveResponseMessage|null} [interactiveResponseMessage] Message interactiveResponseMessage
         * @property {proto.Message.IPollCreationMessage|null} [pollCreationMessage] Message pollCreationMessage
         * @property {proto.Message.IPollUpdateMessage|null} [pollUpdateMessage] Message pollUpdateMessage
         * @property {proto.Message.IKeepInChatMessage|null} [keepInChatMessage] Message keepInChatMessage
         * @property {proto.Message.IFutureProofMessage|null} [documentWithCaptionMessage] Message documentWithCaptionMessage
         * @property {proto.Message.IRequestPhoneNumberMessage|null} [requestPhoneNumberMessage] Message requestPhoneNumberMessage
         * @property {proto.Message.IFutureProofMessage|null} [viewOnceMessageV2] Message viewOnceMessageV2
         * @property {proto.Message.IEncReactionMessage|null} [encReactionMessage] Message encReactionMessage
         * @property {proto.Message.IFutureProofMessage|null} [editedMessage] Message editedMessage
         * @property {proto.Message.IFutureProofMessage|null} [viewOnceMessageV2Extension] Message viewOnceMessageV2Extension
         * @property {proto.Message.IPollCreationMessage|null} [pollCreationMessageV2] Message pollCreationMessageV2
         * @property {proto.Message.IScheduledCallCreationMessage|null} [scheduledCallCreationMessage] Message scheduledCallCreationMessage
         * @property {proto.Message.IFutureProofMessage|null} [groupMentionedMessage] Message groupMentionedMessage
         * @property {proto.Message.IPinInChatMessage|null} [pinInChatMessage] Message pinInChatMessage
         * @property {proto.Message.IPollCreationMessage|null} [pollCreationMessageV3] Message pollCreationMessageV3
         * @property {proto.Message.IScheduledCallEditMessage|null} [scheduledCallEditMessage] Message scheduledCallEditMessage
         * @property {proto.Message.IVideoMessage|null} [ptvMessage] Message ptvMessage
         * @property {proto.Message.IFutureProofMessage|null} [botInvokeMessage] Message botInvokeMessage
         * @property {proto.Message.ICallLogMessage|null} [callLogMesssage] Message callLogMesssage
         * @property {proto.Message.IMessageHistoryBundle|null} [messageHistoryBundle] Message messageHistoryBundle
         * @property {proto.Message.IEncCommentMessage|null} [encCommentMessage] Message encCommentMessage
         * @property {proto.Message.IBCallMessage|null} [bcallMessage] Message bcallMessage
         * @property {proto.Message.IFutureProofMessage|null} [lottieStickerMessage] Message lottieStickerMessage
         * @property {proto.Message.IEventMessage|null} [eventMessage] Message eventMessage
         * @property {proto.Message.IEncEventResponseMessage|null} [encEventResponseMessage] Message encEventResponseMessage
         * @property {proto.Message.ICommentMessage|null} [commentMessage] Message commentMessage
         * @property {proto.Message.INewsletterAdminInviteMessage|null} [newsletterAdminInviteMessage] Message newsletterAdminInviteMessage
         * @property {proto.Message.IPlaceholderMessage|null} [placeholderMessage] Message placeholderMessage
         * @property {proto.Message.ISecretEncryptedMessage|null} [secretEncryptedMessage] Message secretEncryptedMessage
         * @property {proto.Message.IAlbumMessage|null} [albumMessage] Message albumMessage
         * @property {proto.Message.IFutureProofMessage|null} [eventCoverImage] Message eventCoverImage
         * @property {proto.Message.IStickerPackMessage|null} [stickerPackMessage] Message stickerPackMessage
         * @property {proto.Message.IFutureProofMessage|null} [statusMentionMessage] Message statusMentionMessage
         * @property {proto.Message.IPollResultSnapshotMessage|null} [pollResultSnapshotMessage] Message pollResultSnapshotMessage
         * @property {proto.Message.IFutureProofMessage|null} [pollCreationOptionImageMessage] Message pollCreationOptionImageMessage
         * @property {proto.Message.IFutureProofMessage|null} [associatedChildMessage] Message associatedChildMessage
         * @property {proto.Message.IFutureProofMessage|null} [groupStatusMentionMessage] Message groupStatusMentionMessage
         * @property {proto.Message.IFutureProofMessage|null} [pollCreationMessageV4] Message pollCreationMessageV4
         * @property {proto.Message.IFutureProofMessage|null} [statusAddYours] Message statusAddYours
         * @property {proto.Message.IFutureProofMessage|null} [groupStatusMessage] Message groupStatusMessage
         * @property {proto.IAIRichResponseMessage|null} [richResponseMessage] Message richResponseMessage
         * @property {proto.Message.IStatusNotificationMessage|null} [statusNotificationMessage] Message statusNotificationMessage
         * @property {proto.Message.IFutureProofMessage|null} [limitSharingMessage] Message limitSharingMessage
         * @property {proto.Message.IFutureProofMessage|null} [botTaskMessage] Message botTaskMessage
         * @property {proto.Message.IFutureProofMessage|null} [questionMessage] Message questionMessage
         * @property {proto.Message.IMessageHistoryNotice|null} [messageHistoryNotice] Message messageHistoryNotice
         * @property {proto.Message.IFutureProofMessage|null} [groupStatusMessageV2] Message groupStatusMessageV2
         * @property {proto.Message.IFutureProofMessage|null} [botForwardedMessage] Message botForwardedMessage
         * @property {proto.Message.IStatusQuestionAnswerMessage|null} [statusQuestionAnswerMessage] Message statusQuestionAnswerMessage
         * @property {proto.Message.IFutureProofMessage|null} [questionReplyMessage] Message questionReplyMessage
         * @property {proto.Message.IQuestionResponseMessage|null} [questionResponseMessage] Message questionResponseMessage
         * @property {proto.Message.IStatusQuotedMessage|null} [statusQuotedMessage] Message statusQuotedMessage
         * @property {proto.Message.IStatusStickerInteractionMessage|null} [statusStickerInteractionMessage] Message statusStickerInteractionMessage
         * @property {proto.Message.IPollCreationMessage|null} [pollCreationMessageV5] Message pollCreationMessageV5
         * @property {proto.Message.INewsletterFollowerInviteMessage|null} [newsletterFollowerInviteMessageV2] Message newsletterFollowerInviteMessageV2
         * @property {proto.Message.IPollResultSnapshotMessage|null} [pollResultSnapshotMessageV3] Message pollResultSnapshotMessageV3
         * @property {proto.Message.IFutureProofMessage|null} [newsletterAdminProfileMessage] Message newsletterAdminProfileMessage
         * @property {proto.Message.IFutureProofMessage|null} [newsletterAdminProfileMessageV2] Message newsletterAdminProfileMessageV2
         * @property {proto.Message.IFutureProofMessage|null} [spoilerMessage] Message spoilerMessage
         * @property {proto.Message.IPollCreationMessage|null} [pollCreationMessageV6] Message pollCreationMessageV6
         * @property {proto.Message.IConditionalRevealMessage|null} [conditionalRevealMessage] Message conditionalRevealMessage
         * @property {proto.Message.IPollAddOptionMessage|null} [pollAddOptionMessage] Message pollAddOptionMessage
         * @property {proto.Message.IEventInviteMessage|null} [eventInviteMessage] Message eventInviteMessage
         * @property {proto.IGroupRootKeyShare|null} [groupRootKeyShare] Message groupRootKeyShare
         * @property {proto.Message.IPaymentReminderMessage|null} [paymentReminderMessage] Message paymentReminderMessage
         * @property {proto.Message.ISplitPaymentMessage|null} [splitPaymentMessage] Message splitPaymentMessage
         * @property {proto.Message.IFutureProofMessage|null} [newsletterAdminProfileStatusMessage] Message newsletterAdminProfileStatusMessage
         * @property {proto.Message.IRootSecretDistributeMessage|null} [rootSecretDistributeMessage] Message rootSecretDistributeMessage
         */

        /**
         * Constructs a new Message.
         * @memberof proto
         * @classdesc Represents a Message.
         * @implements IMessage
         * @constructor
         * @param {proto.IMessage=} [properties] Properties to set
         */
        function Message(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Message conversation.
         * @member {string|null|undefined} conversation
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.conversation = null;

        /**
         * Message senderKeyDistributionMessage.
         * @member {proto.Message.ISenderKeyDistributionMessage|null|undefined} senderKeyDistributionMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.senderKeyDistributionMessage = null;

        /**
         * Message imageMessage.
         * @member {proto.Message.IImageMessage|null|undefined} imageMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.imageMessage = null;

        /**
         * Message contactMessage.
         * @member {proto.Message.IContactMessage|null|undefined} contactMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.contactMessage = null;

        /**
         * Message locationMessage.
         * @member {proto.Message.ILocationMessage|null|undefined} locationMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.locationMessage = null;

        /**
         * Message extendedTextMessage.
         * @member {proto.Message.IExtendedTextMessage|null|undefined} extendedTextMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.extendedTextMessage = null;

        /**
         * Message documentMessage.
         * @member {proto.Message.IDocumentMessage|null|undefined} documentMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.documentMessage = null;

        /**
         * Message audioMessage.
         * @member {proto.Message.IAudioMessage|null|undefined} audioMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.audioMessage = null;

        /**
         * Message videoMessage.
         * @member {proto.Message.IVideoMessage|null|undefined} videoMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.videoMessage = null;

        /**
         * Message call.
         * @member {proto.Message.ICall|null|undefined} call
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.call = null;

        /**
         * Message chat.
         * @member {proto.Message.IChat|null|undefined} chat
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.chat = null;

        /**
         * Message protocolMessage.
         * @member {proto.Message.IProtocolMessage|null|undefined} protocolMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.protocolMessage = null;

        /**
         * Message contactsArrayMessage.
         * @member {proto.Message.IContactsArrayMessage|null|undefined} contactsArrayMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.contactsArrayMessage = null;

        /**
         * Message highlyStructuredMessage.
         * @member {proto.Message.IHighlyStructuredMessage|null|undefined} highlyStructuredMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.highlyStructuredMessage = null;

        /**
         * Message fastRatchetKeySenderKeyDistributionMessage.
         * @member {proto.Message.ISenderKeyDistributionMessage|null|undefined} fastRatchetKeySenderKeyDistributionMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.fastRatchetKeySenderKeyDistributionMessage = null;

        /**
         * Message sendPaymentMessage.
         * @member {proto.Message.ISendPaymentMessage|null|undefined} sendPaymentMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.sendPaymentMessage = null;

        /**
         * Message liveLocationMessage.
         * @member {proto.Message.ILiveLocationMessage|null|undefined} liveLocationMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.liveLocationMessage = null;

        /**
         * Message requestPaymentMessage.
         * @member {proto.Message.IRequestPaymentMessage|null|undefined} requestPaymentMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.requestPaymentMessage = null;

        /**
         * Message declinePaymentRequestMessage.
         * @member {proto.Message.IDeclinePaymentRequestMessage|null|undefined} declinePaymentRequestMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.declinePaymentRequestMessage = null;

        /**
         * Message cancelPaymentRequestMessage.
         * @member {proto.Message.ICancelPaymentRequestMessage|null|undefined} cancelPaymentRequestMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.cancelPaymentRequestMessage = null;

        /**
         * Message templateMessage.
         * @member {proto.Message.ITemplateMessage|null|undefined} templateMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.templateMessage = null;

        /**
         * Message stickerMessage.
         * @member {proto.Message.IStickerMessage|null|undefined} stickerMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.stickerMessage = null;

        /**
         * Message groupInviteMessage.
         * @member {proto.Message.IGroupInviteMessage|null|undefined} groupInviteMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.groupInviteMessage = null;

        /**
         * Message templateButtonReplyMessage.
         * @member {proto.Message.ITemplateButtonReplyMessage|null|undefined} templateButtonReplyMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.templateButtonReplyMessage = null;

        /**
         * Message productMessage.
         * @member {proto.Message.IProductMessage|null|undefined} productMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.productMessage = null;

        /**
         * Message deviceSentMessage.
         * @member {proto.Message.IDeviceSentMessage|null|undefined} deviceSentMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.deviceSentMessage = null;

        /**
         * Message messageContextInfo.
         * @member {proto.IMessageContextInfo|null|undefined} messageContextInfo
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.messageContextInfo = null;

        /**
         * Message listMessage.
         * @member {proto.Message.IListMessage|null|undefined} listMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.listMessage = null;

        /**
         * Message viewOnceMessage.
         * @member {proto.Message.IFutureProofMessage|null|undefined} viewOnceMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.viewOnceMessage = null;

        /**
         * Message orderMessage.
         * @member {proto.Message.IOrderMessage|null|undefined} orderMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.orderMessage = null;

        /**
         * Message listResponseMessage.
         * @member {proto.Message.IListResponseMessage|null|undefined} listResponseMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.listResponseMessage = null;

        /**
         * Message ephemeralMessage.
         * @member {proto.Message.IFutureProofMessage|null|undefined} ephemeralMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.ephemeralMessage = null;

        /**
         * Message invoiceMessage.
         * @member {proto.Message.IInvoiceMessage|null|undefined} invoiceMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.invoiceMessage = null;

        /**
         * Message buttonsMessage.
         * @member {proto.Message.IButtonsMessage|null|undefined} buttonsMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.buttonsMessage = null;

        /**
         * Message buttonsResponseMessage.
         * @member {proto.Message.IButtonsResponseMessage|null|undefined} buttonsResponseMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.buttonsResponseMessage = null;

        /**
         * Message paymentInviteMessage.
         * @member {proto.Message.IPaymentInviteMessage|null|undefined} paymentInviteMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.paymentInviteMessage = null;

        /**
         * Message interactiveMessage.
         * @member {proto.Message.IInteractiveMessage|null|undefined} interactiveMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.interactiveMessage = null;

        /**
         * Message reactionMessage.
         * @member {proto.Message.IReactionMessage|null|undefined} reactionMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.reactionMessage = null;

        /**
         * Message stickerSyncRmrMessage.
         * @member {proto.Message.IStickerSyncRMRMessage|null|undefined} stickerSyncRmrMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.stickerSyncRmrMessage = null;

        /**
         * Message interactiveResponseMessage.
         * @member {proto.Message.IInteractiveResponseMessage|null|undefined} interactiveResponseMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.interactiveResponseMessage = null;

        /**
         * Message pollCreationMessage.
         * @member {proto.Message.IPollCreationMessage|null|undefined} pollCreationMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.pollCreationMessage = null;

        /**
         * Message pollUpdateMessage.
         * @member {proto.Message.IPollUpdateMessage|null|undefined} pollUpdateMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.pollUpdateMessage = null;

        /**
         * Message keepInChatMessage.
         * @member {proto.Message.IKeepInChatMessage|null|undefined} keepInChatMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.keepInChatMessage = null;

        /**
         * Message documentWithCaptionMessage.
         * @member {proto.Message.IFutureProofMessage|null|undefined} documentWithCaptionMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.documentWithCaptionMessage = null;

        /**
         * Message requestPhoneNumberMessage.
         * @member {proto.Message.IRequestPhoneNumberMessage|null|undefined} requestPhoneNumberMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.requestPhoneNumberMessage = null;

        /**
         * Message viewOnceMessageV2.
         * @member {proto.Message.IFutureProofMessage|null|undefined} viewOnceMessageV2
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.viewOnceMessageV2 = null;

        /**
         * Message encReactionMessage.
         * @member {proto.Message.IEncReactionMessage|null|undefined} encReactionMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.encReactionMessage = null;

        /**
         * Message editedMessage.
         * @member {proto.Message.IFutureProofMessage|null|undefined} editedMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.editedMessage = null;

        /**
         * Message viewOnceMessageV2Extension.
         * @member {proto.Message.IFutureProofMessage|null|undefined} viewOnceMessageV2Extension
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.viewOnceMessageV2Extension = null;

        /**
         * Message pollCreationMessageV2.
         * @member {proto.Message.IPollCreationMessage|null|undefined} pollCreationMessageV2
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.pollCreationMessageV2 = null;

        /**
         * Message scheduledCallCreationMessage.
         * @member {proto.Message.IScheduledCallCreationMessage|null|undefined} scheduledCallCreationMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.scheduledCallCreationMessage = null;

        /**
         * Message groupMentionedMessage.
         * @member {proto.Message.IFutureProofMessage|null|undefined} groupMentionedMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.groupMentionedMessage = null;

        /**
         * Message pinInChatMessage.
         * @member {proto.Message.IPinInChatMessage|null|undefined} pinInChatMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.pinInChatMessage = null;

        /**
         * Message pollCreationMessageV3.
         * @member {proto.Message.IPollCreationMessage|null|undefined} pollCreationMessageV3
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.pollCreationMessageV3 = null;

        /**
         * Message scheduledCallEditMessage.
         * @member {proto.Message.IScheduledCallEditMessage|null|undefined} scheduledCallEditMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.scheduledCallEditMessage = null;

        /**
         * Message ptvMessage.
         * @member {proto.Message.IVideoMessage|null|undefined} ptvMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.ptvMessage = null;

        /**
         * Message botInvokeMessage.
         * @member {proto.Message.IFutureProofMessage|null|undefined} botInvokeMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.botInvokeMessage = null;

        /**
         * Message callLogMesssage.
         * @member {proto.Message.ICallLogMessage|null|undefined} callLogMesssage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.callLogMesssage = null;

        /**
         * Message messageHistoryBundle.
         * @member {proto.Message.IMessageHistoryBundle|null|undefined} messageHistoryBundle
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.messageHistoryBundle = null;

        /**
         * Message encCommentMessage.
         * @member {proto.Message.IEncCommentMessage|null|undefined} encCommentMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.encCommentMessage = null;

        /**
         * Message bcallMessage.
         * @member {proto.Message.IBCallMessage|null|undefined} bcallMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.bcallMessage = null;

        /**
         * Message lottieStickerMessage.
         * @member {proto.Message.IFutureProofMessage|null|undefined} lottieStickerMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.lottieStickerMessage = null;

        /**
         * Message eventMessage.
         * @member {proto.Message.IEventMessage|null|undefined} eventMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.eventMessage = null;

        /**
         * Message encEventResponseMessage.
         * @member {proto.Message.IEncEventResponseMessage|null|undefined} encEventResponseMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.encEventResponseMessage = null;

        /**
         * Message commentMessage.
         * @member {proto.Message.ICommentMessage|null|undefined} commentMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.commentMessage = null;

        /**
         * Message newsletterAdminInviteMessage.
         * @member {proto.Message.INewsletterAdminInviteMessage|null|undefined} newsletterAdminInviteMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.newsletterAdminInviteMessage = null;

        /**
         * Message placeholderMessage.
         * @member {proto.Message.IPlaceholderMessage|null|undefined} placeholderMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.placeholderMessage = null;

        /**
         * Message secretEncryptedMessage.
         * @member {proto.Message.ISecretEncryptedMessage|null|undefined} secretEncryptedMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.secretEncryptedMessage = null;

        /**
         * Message albumMessage.
         * @member {proto.Message.IAlbumMessage|null|undefined} albumMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.albumMessage = null;

        /**
         * Message eventCoverImage.
         * @member {proto.Message.IFutureProofMessage|null|undefined} eventCoverImage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.eventCoverImage = null;

        /**
         * Message stickerPackMessage.
         * @member {proto.Message.IStickerPackMessage|null|undefined} stickerPackMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.stickerPackMessage = null;

        /**
         * Message statusMentionMessage.
         * @member {proto.Message.IFutureProofMessage|null|undefined} statusMentionMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.statusMentionMessage = null;

        /**
         * Message pollResultSnapshotMessage.
         * @member {proto.Message.IPollResultSnapshotMessage|null|undefined} pollResultSnapshotMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.pollResultSnapshotMessage = null;

        /**
         * Message pollCreationOptionImageMessage.
         * @member {proto.Message.IFutureProofMessage|null|undefined} pollCreationOptionImageMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.pollCreationOptionImageMessage = null;

        /**
         * Message associatedChildMessage.
         * @member {proto.Message.IFutureProofMessage|null|undefined} associatedChildMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.associatedChildMessage = null;

        /**
         * Message groupStatusMentionMessage.
         * @member {proto.Message.IFutureProofMessage|null|undefined} groupStatusMentionMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.groupStatusMentionMessage = null;

        /**
         * Message pollCreationMessageV4.
         * @member {proto.Message.IFutureProofMessage|null|undefined} pollCreationMessageV4
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.pollCreationMessageV4 = null;

        /**
         * Message statusAddYours.
         * @member {proto.Message.IFutureProofMessage|null|undefined} statusAddYours
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.statusAddYours = null;

        /**
         * Message groupStatusMessage.
         * @member {proto.Message.IFutureProofMessage|null|undefined} groupStatusMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.groupStatusMessage = null;

        /**
         * Message richResponseMessage.
         * @member {proto.IAIRichResponseMessage|null|undefined} richResponseMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.richResponseMessage = null;

        /**
         * Message statusNotificationMessage.
         * @member {proto.Message.IStatusNotificationMessage|null|undefined} statusNotificationMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.statusNotificationMessage = null;

        /**
         * Message limitSharingMessage.
         * @member {proto.Message.IFutureProofMessage|null|undefined} limitSharingMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.limitSharingMessage = null;

        /**
         * Message botTaskMessage.
         * @member {proto.Message.IFutureProofMessage|null|undefined} botTaskMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.botTaskMessage = null;

        /**
         * Message questionMessage.
         * @member {proto.Message.IFutureProofMessage|null|undefined} questionMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.questionMessage = null;

        /**
         * Message messageHistoryNotice.
         * @member {proto.Message.IMessageHistoryNotice|null|undefined} messageHistoryNotice
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.messageHistoryNotice = null;

        /**
         * Message groupStatusMessageV2.
         * @member {proto.Message.IFutureProofMessage|null|undefined} groupStatusMessageV2
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.groupStatusMessageV2 = null;

        /**
         * Message botForwardedMessage.
         * @member {proto.Message.IFutureProofMessage|null|undefined} botForwardedMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.botForwardedMessage = null;

        /**
         * Message statusQuestionAnswerMessage.
         * @member {proto.Message.IStatusQuestionAnswerMessage|null|undefined} statusQuestionAnswerMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.statusQuestionAnswerMessage = null;

        /**
         * Message questionReplyMessage.
         * @member {proto.Message.IFutureProofMessage|null|undefined} questionReplyMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.questionReplyMessage = null;

        /**
         * Message questionResponseMessage.
         * @member {proto.Message.IQuestionResponseMessage|null|undefined} questionResponseMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.questionResponseMessage = null;

        /**
         * Message statusQuotedMessage.
         * @member {proto.Message.IStatusQuotedMessage|null|undefined} statusQuotedMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.statusQuotedMessage = null;

        /**
         * Message statusStickerInteractionMessage.
         * @member {proto.Message.IStatusStickerInteractionMessage|null|undefined} statusStickerInteractionMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.statusStickerInteractionMessage = null;

        /**
         * Message pollCreationMessageV5.
         * @member {proto.Message.IPollCreationMessage|null|undefined} pollCreationMessageV5
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.pollCreationMessageV5 = null;

        /**
         * Message newsletterFollowerInviteMessageV2.
         * @member {proto.Message.INewsletterFollowerInviteMessage|null|undefined} newsletterFollowerInviteMessageV2
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.newsletterFollowerInviteMessageV2 = null;

        /**
         * Message pollResultSnapshotMessageV3.
         * @member {proto.Message.IPollResultSnapshotMessage|null|undefined} pollResultSnapshotMessageV3
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.pollResultSnapshotMessageV3 = null;

        /**
         * Message newsletterAdminProfileMessage.
         * @member {proto.Message.IFutureProofMessage|null|undefined} newsletterAdminProfileMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.newsletterAdminProfileMessage = null;

        /**
         * Message newsletterAdminProfileMessageV2.
         * @member {proto.Message.IFutureProofMessage|null|undefined} newsletterAdminProfileMessageV2
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.newsletterAdminProfileMessageV2 = null;

        /**
         * Message spoilerMessage.
         * @member {proto.Message.IFutureProofMessage|null|undefined} spoilerMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.spoilerMessage = null;

        /**
         * Message pollCreationMessageV6.
         * @member {proto.Message.IPollCreationMessage|null|undefined} pollCreationMessageV6
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.pollCreationMessageV6 = null;

        /**
         * Message conditionalRevealMessage.
         * @member {proto.Message.IConditionalRevealMessage|null|undefined} conditionalRevealMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.conditionalRevealMessage = null;

        /**
         * Message pollAddOptionMessage.
         * @member {proto.Message.IPollAddOptionMessage|null|undefined} pollAddOptionMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.pollAddOptionMessage = null;

        /**
         * Message eventInviteMessage.
         * @member {proto.Message.IEventInviteMessage|null|undefined} eventInviteMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.eventInviteMessage = null;

        /**
         * Message groupRootKeyShare.
         * @member {proto.IGroupRootKeyShare|null|undefined} groupRootKeyShare
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.groupRootKeyShare = null;

        /**
         * Message paymentReminderMessage.
         * @member {proto.Message.IPaymentReminderMessage|null|undefined} paymentReminderMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.paymentReminderMessage = null;

        /**
         * Message splitPaymentMessage.
         * @member {proto.Message.ISplitPaymentMessage|null|undefined} splitPaymentMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.splitPaymentMessage = null;

        /**
         * Message newsletterAdminProfileStatusMessage.
         * @member {proto.Message.IFutureProofMessage|null|undefined} newsletterAdminProfileStatusMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.newsletterAdminProfileStatusMessage = null;

        /**
         * Message rootSecretDistributeMessage.
         * @member {proto.Message.IRootSecretDistributeMessage|null|undefined} rootSecretDistributeMessage
         * @memberof proto.Message
         * @instance
         */
        Message.prototype.rootSecretDistributeMessage = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_conversation", {
            get: $util.oneOfGetter($oneOfFields = ["conversation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_senderKeyDistributionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["senderKeyDistributionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_imageMessage", {
            get: $util.oneOfGetter($oneOfFields = ["imageMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_contactMessage", {
            get: $util.oneOfGetter($oneOfFields = ["contactMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_locationMessage", {
            get: $util.oneOfGetter($oneOfFields = ["locationMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_extendedTextMessage", {
            get: $util.oneOfGetter($oneOfFields = ["extendedTextMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_documentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["documentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_audioMessage", {
            get: $util.oneOfGetter($oneOfFields = ["audioMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_videoMessage", {
            get: $util.oneOfGetter($oneOfFields = ["videoMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_call", {
            get: $util.oneOfGetter($oneOfFields = ["call"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_chat", {
            get: $util.oneOfGetter($oneOfFields = ["chat"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_protocolMessage", {
            get: $util.oneOfGetter($oneOfFields = ["protocolMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_contactsArrayMessage", {
            get: $util.oneOfGetter($oneOfFields = ["contactsArrayMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_highlyStructuredMessage", {
            get: $util.oneOfGetter($oneOfFields = ["highlyStructuredMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_fastRatchetKeySenderKeyDistributionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["fastRatchetKeySenderKeyDistributionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_sendPaymentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["sendPaymentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_liveLocationMessage", {
            get: $util.oneOfGetter($oneOfFields = ["liveLocationMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_requestPaymentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["requestPaymentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_declinePaymentRequestMessage", {
            get: $util.oneOfGetter($oneOfFields = ["declinePaymentRequestMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_cancelPaymentRequestMessage", {
            get: $util.oneOfGetter($oneOfFields = ["cancelPaymentRequestMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_templateMessage", {
            get: $util.oneOfGetter($oneOfFields = ["templateMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_stickerMessage", {
            get: $util.oneOfGetter($oneOfFields = ["stickerMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_groupInviteMessage", {
            get: $util.oneOfGetter($oneOfFields = ["groupInviteMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_templateButtonReplyMessage", {
            get: $util.oneOfGetter($oneOfFields = ["templateButtonReplyMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_productMessage", {
            get: $util.oneOfGetter($oneOfFields = ["productMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_deviceSentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["deviceSentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_messageContextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["messageContextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_listMessage", {
            get: $util.oneOfGetter($oneOfFields = ["listMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_viewOnceMessage", {
            get: $util.oneOfGetter($oneOfFields = ["viewOnceMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_orderMessage", {
            get: $util.oneOfGetter($oneOfFields = ["orderMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_listResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["listResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_ephemeralMessage", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_invoiceMessage", {
            get: $util.oneOfGetter($oneOfFields = ["invoiceMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_buttonsMessage", {
            get: $util.oneOfGetter($oneOfFields = ["buttonsMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_buttonsResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["buttonsResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_paymentInviteMessage", {
            get: $util.oneOfGetter($oneOfFields = ["paymentInviteMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_interactiveMessage", {
            get: $util.oneOfGetter($oneOfFields = ["interactiveMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_reactionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["reactionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_stickerSyncRmrMessage", {
            get: $util.oneOfGetter($oneOfFields = ["stickerSyncRmrMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_interactiveResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["interactiveResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollCreationMessage", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollUpdateMessage", {
            get: $util.oneOfGetter($oneOfFields = ["pollUpdateMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_keepInChatMessage", {
            get: $util.oneOfGetter($oneOfFields = ["keepInChatMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_documentWithCaptionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["documentWithCaptionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_requestPhoneNumberMessage", {
            get: $util.oneOfGetter($oneOfFields = ["requestPhoneNumberMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_viewOnceMessageV2", {
            get: $util.oneOfGetter($oneOfFields = ["viewOnceMessageV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_encReactionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["encReactionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_editedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["editedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_viewOnceMessageV2Extension", {
            get: $util.oneOfGetter($oneOfFields = ["viewOnceMessageV2Extension"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollCreationMessageV2", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_scheduledCallCreationMessage", {
            get: $util.oneOfGetter($oneOfFields = ["scheduledCallCreationMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_groupMentionedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["groupMentionedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pinInChatMessage", {
            get: $util.oneOfGetter($oneOfFields = ["pinInChatMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollCreationMessageV3", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageV3"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_scheduledCallEditMessage", {
            get: $util.oneOfGetter($oneOfFields = ["scheduledCallEditMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_ptvMessage", {
            get: $util.oneOfGetter($oneOfFields = ["ptvMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_botInvokeMessage", {
            get: $util.oneOfGetter($oneOfFields = ["botInvokeMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_callLogMesssage", {
            get: $util.oneOfGetter($oneOfFields = ["callLogMesssage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_messageHistoryBundle", {
            get: $util.oneOfGetter($oneOfFields = ["messageHistoryBundle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_encCommentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["encCommentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_bcallMessage", {
            get: $util.oneOfGetter($oneOfFields = ["bcallMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_lottieStickerMessage", {
            get: $util.oneOfGetter($oneOfFields = ["lottieStickerMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_eventMessage", {
            get: $util.oneOfGetter($oneOfFields = ["eventMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_encEventResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["encEventResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_commentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["commentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_newsletterAdminInviteMessage", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterAdminInviteMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_placeholderMessage", {
            get: $util.oneOfGetter($oneOfFields = ["placeholderMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_secretEncryptedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["secretEncryptedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_albumMessage", {
            get: $util.oneOfGetter($oneOfFields = ["albumMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_eventCoverImage", {
            get: $util.oneOfGetter($oneOfFields = ["eventCoverImage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_stickerPackMessage", {
            get: $util.oneOfGetter($oneOfFields = ["stickerPackMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_statusMentionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["statusMentionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollResultSnapshotMessage", {
            get: $util.oneOfGetter($oneOfFields = ["pollResultSnapshotMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollCreationOptionImageMessage", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationOptionImageMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_associatedChildMessage", {
            get: $util.oneOfGetter($oneOfFields = ["associatedChildMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_groupStatusMentionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["groupStatusMentionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollCreationMessageV4", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageV4"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_statusAddYours", {
            get: $util.oneOfGetter($oneOfFields = ["statusAddYours"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_groupStatusMessage", {
            get: $util.oneOfGetter($oneOfFields = ["groupStatusMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_richResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["richResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_statusNotificationMessage", {
            get: $util.oneOfGetter($oneOfFields = ["statusNotificationMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_limitSharingMessage", {
            get: $util.oneOfGetter($oneOfFields = ["limitSharingMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_botTaskMessage", {
            get: $util.oneOfGetter($oneOfFields = ["botTaskMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_questionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["questionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_messageHistoryNotice", {
            get: $util.oneOfGetter($oneOfFields = ["messageHistoryNotice"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_groupStatusMessageV2", {
            get: $util.oneOfGetter($oneOfFields = ["groupStatusMessageV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_botForwardedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["botForwardedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_statusQuestionAnswerMessage", {
            get: $util.oneOfGetter($oneOfFields = ["statusQuestionAnswerMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_questionReplyMessage", {
            get: $util.oneOfGetter($oneOfFields = ["questionReplyMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_questionResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["questionResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_statusQuotedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["statusQuotedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_statusStickerInteractionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["statusStickerInteractionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollCreationMessageV5", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageV5"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_newsletterFollowerInviteMessageV2", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterFollowerInviteMessageV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollResultSnapshotMessageV3", {
            get: $util.oneOfGetter($oneOfFields = ["pollResultSnapshotMessageV3"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_newsletterAdminProfileMessage", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterAdminProfileMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_newsletterAdminProfileMessageV2", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterAdminProfileMessageV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_spoilerMessage", {
            get: $util.oneOfGetter($oneOfFields = ["spoilerMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollCreationMessageV6", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageV6"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_conditionalRevealMessage", {
            get: $util.oneOfGetter($oneOfFields = ["conditionalRevealMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollAddOptionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["pollAddOptionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_eventInviteMessage", {
            get: $util.oneOfGetter($oneOfFields = ["eventInviteMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_groupRootKeyShare", {
            get: $util.oneOfGetter($oneOfFields = ["groupRootKeyShare"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_paymentReminderMessage", {
            get: $util.oneOfGetter($oneOfFields = ["paymentReminderMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_splitPaymentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["splitPaymentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_newsletterAdminProfileStatusMessage", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterAdminProfileStatusMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_rootSecretDistributeMessage", {
            get: $util.oneOfGetter($oneOfFields = ["rootSecretDistributeMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new Message instance using the specified properties.
         * @function create
         * @memberof proto.Message
         * @static
         * @param {proto.IMessage=} [properties] Properties to set
         * @returns {proto.Message} Message instance
         */
        Message.create = function create(properties) {
            return new Message(properties);
        };

        /**
         * Encodes the specified Message message. Does not implicitly {@link proto.Message.verify|verify} messages.
         * @function encode
         * @memberof proto.Message
         * @static
         * @param {proto.IMessage} message Message message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Message.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.conversation != null && Object.hasOwnProperty.call(message, "conversation"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.conversation);
            if (message.senderKeyDistributionMessage != null && Object.hasOwnProperty.call(message, "senderKeyDistributionMessage"))
                $root.proto.Message.SenderKeyDistributionMessage.encode(message.senderKeyDistributionMessage, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.imageMessage != null && Object.hasOwnProperty.call(message, "imageMessage"))
                $root.proto.Message.ImageMessage.encode(message.imageMessage, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.contactMessage != null && Object.hasOwnProperty.call(message, "contactMessage"))
                $root.proto.Message.ContactMessage.encode(message.contactMessage, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.locationMessage != null && Object.hasOwnProperty.call(message, "locationMessage"))
                $root.proto.Message.LocationMessage.encode(message.locationMessage, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
            if (message.extendedTextMessage != null && Object.hasOwnProperty.call(message, "extendedTextMessage"))
                $root.proto.Message.ExtendedTextMessage.encode(message.extendedTextMessage, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
            if (message.documentMessage != null && Object.hasOwnProperty.call(message, "documentMessage"))
                $root.proto.Message.DocumentMessage.encode(message.documentMessage, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
            if (message.audioMessage != null && Object.hasOwnProperty.call(message, "audioMessage"))
                $root.proto.Message.AudioMessage.encode(message.audioMessage, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
            if (message.videoMessage != null && Object.hasOwnProperty.call(message, "videoMessage"))
                $root.proto.Message.VideoMessage.encode(message.videoMessage, writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
            if (message.call != null && Object.hasOwnProperty.call(message, "call"))
                $root.proto.Message.Call.encode(message.call, writer.uint32(/* id 10, wireType 2 =*/82).fork(), q + 1).ldelim();
            if (message.chat != null && Object.hasOwnProperty.call(message, "chat"))
                $root.proto.Message.Chat.encode(message.chat, writer.uint32(/* id 11, wireType 2 =*/90).fork(), q + 1).ldelim();
            if (message.protocolMessage != null && Object.hasOwnProperty.call(message, "protocolMessage"))
                $root.proto.Message.ProtocolMessage.encode(message.protocolMessage, writer.uint32(/* id 12, wireType 2 =*/98).fork(), q + 1).ldelim();
            if (message.contactsArrayMessage != null && Object.hasOwnProperty.call(message, "contactsArrayMessage"))
                $root.proto.Message.ContactsArrayMessage.encode(message.contactsArrayMessage, writer.uint32(/* id 13, wireType 2 =*/106).fork(), q + 1).ldelim();
            if (message.highlyStructuredMessage != null && Object.hasOwnProperty.call(message, "highlyStructuredMessage"))
                $root.proto.Message.HighlyStructuredMessage.encode(message.highlyStructuredMessage, writer.uint32(/* id 14, wireType 2 =*/114).fork(), q + 1).ldelim();
            if (message.fastRatchetKeySenderKeyDistributionMessage != null && Object.hasOwnProperty.call(message, "fastRatchetKeySenderKeyDistributionMessage"))
                $root.proto.Message.SenderKeyDistributionMessage.encode(message.fastRatchetKeySenderKeyDistributionMessage, writer.uint32(/* id 15, wireType 2 =*/122).fork(), q + 1).ldelim();
            if (message.sendPaymentMessage != null && Object.hasOwnProperty.call(message, "sendPaymentMessage"))
                $root.proto.Message.SendPaymentMessage.encode(message.sendPaymentMessage, writer.uint32(/* id 16, wireType 2 =*/130).fork(), q + 1).ldelim();
            if (message.liveLocationMessage != null && Object.hasOwnProperty.call(message, "liveLocationMessage"))
                $root.proto.Message.LiveLocationMessage.encode(message.liveLocationMessage, writer.uint32(/* id 18, wireType 2 =*/146).fork(), q + 1).ldelim();
            if (message.requestPaymentMessage != null && Object.hasOwnProperty.call(message, "requestPaymentMessage"))
                $root.proto.Message.RequestPaymentMessage.encode(message.requestPaymentMessage, writer.uint32(/* id 22, wireType 2 =*/178).fork(), q + 1).ldelim();
            if (message.declinePaymentRequestMessage != null && Object.hasOwnProperty.call(message, "declinePaymentRequestMessage"))
                $root.proto.Message.DeclinePaymentRequestMessage.encode(message.declinePaymentRequestMessage, writer.uint32(/* id 23, wireType 2 =*/186).fork(), q + 1).ldelim();
            if (message.cancelPaymentRequestMessage != null && Object.hasOwnProperty.call(message, "cancelPaymentRequestMessage"))
                $root.proto.Message.CancelPaymentRequestMessage.encode(message.cancelPaymentRequestMessage, writer.uint32(/* id 24, wireType 2 =*/194).fork(), q + 1).ldelim();
            if (message.templateMessage != null && Object.hasOwnProperty.call(message, "templateMessage"))
                $root.proto.Message.TemplateMessage.encode(message.templateMessage, writer.uint32(/* id 25, wireType 2 =*/202).fork(), q + 1).ldelim();
            if (message.stickerMessage != null && Object.hasOwnProperty.call(message, "stickerMessage"))
                $root.proto.Message.StickerMessage.encode(message.stickerMessage, writer.uint32(/* id 26, wireType 2 =*/210).fork(), q + 1).ldelim();
            if (message.groupInviteMessage != null && Object.hasOwnProperty.call(message, "groupInviteMessage"))
                $root.proto.Message.GroupInviteMessage.encode(message.groupInviteMessage, writer.uint32(/* id 28, wireType 2 =*/226).fork(), q + 1).ldelim();
            if (message.templateButtonReplyMessage != null && Object.hasOwnProperty.call(message, "templateButtonReplyMessage"))
                $root.proto.Message.TemplateButtonReplyMessage.encode(message.templateButtonReplyMessage, writer.uint32(/* id 29, wireType 2 =*/234).fork(), q + 1).ldelim();
            if (message.productMessage != null && Object.hasOwnProperty.call(message, "productMessage"))
                $root.proto.Message.ProductMessage.encode(message.productMessage, writer.uint32(/* id 30, wireType 2 =*/242).fork(), q + 1).ldelim();
            if (message.deviceSentMessage != null && Object.hasOwnProperty.call(message, "deviceSentMessage"))
                $root.proto.Message.DeviceSentMessage.encode(message.deviceSentMessage, writer.uint32(/* id 31, wireType 2 =*/250).fork(), q + 1).ldelim();
            if (message.messageContextInfo != null && Object.hasOwnProperty.call(message, "messageContextInfo"))
                $root.proto.MessageContextInfo.encode(message.messageContextInfo, writer.uint32(/* id 35, wireType 2 =*/282).fork(), q + 1).ldelim();
            if (message.listMessage != null && Object.hasOwnProperty.call(message, "listMessage"))
                $root.proto.Message.ListMessage.encode(message.listMessage, writer.uint32(/* id 36, wireType 2 =*/290).fork(), q + 1).ldelim();
            if (message.viewOnceMessage != null && Object.hasOwnProperty.call(message, "viewOnceMessage"))
                $root.proto.Message.FutureProofMessage.encode(message.viewOnceMessage, writer.uint32(/* id 37, wireType 2 =*/298).fork(), q + 1).ldelim();
            if (message.orderMessage != null && Object.hasOwnProperty.call(message, "orderMessage"))
                $root.proto.Message.OrderMessage.encode(message.orderMessage, writer.uint32(/* id 38, wireType 2 =*/306).fork(), q + 1).ldelim();
            if (message.listResponseMessage != null && Object.hasOwnProperty.call(message, "listResponseMessage"))
                $root.proto.Message.ListResponseMessage.encode(message.listResponseMessage, writer.uint32(/* id 39, wireType 2 =*/314).fork(), q + 1).ldelim();
            if (message.ephemeralMessage != null && Object.hasOwnProperty.call(message, "ephemeralMessage"))
                $root.proto.Message.FutureProofMessage.encode(message.ephemeralMessage, writer.uint32(/* id 40, wireType 2 =*/322).fork(), q + 1).ldelim();
            if (message.invoiceMessage != null && Object.hasOwnProperty.call(message, "invoiceMessage"))
                $root.proto.Message.InvoiceMessage.encode(message.invoiceMessage, writer.uint32(/* id 41, wireType 2 =*/330).fork(), q + 1).ldelim();
            if (message.buttonsMessage != null && Object.hasOwnProperty.call(message, "buttonsMessage"))
                $root.proto.Message.ButtonsMessage.encode(message.buttonsMessage, writer.uint32(/* id 42, wireType 2 =*/338).fork(), q + 1).ldelim();
            if (message.buttonsResponseMessage != null && Object.hasOwnProperty.call(message, "buttonsResponseMessage"))
                $root.proto.Message.ButtonsResponseMessage.encode(message.buttonsResponseMessage, writer.uint32(/* id 43, wireType 2 =*/346).fork(), q + 1).ldelim();
            if (message.paymentInviteMessage != null && Object.hasOwnProperty.call(message, "paymentInviteMessage"))
                $root.proto.Message.PaymentInviteMessage.encode(message.paymentInviteMessage, writer.uint32(/* id 44, wireType 2 =*/354).fork(), q + 1).ldelim();
            if (message.interactiveMessage != null && Object.hasOwnProperty.call(message, "interactiveMessage"))
                $root.proto.Message.InteractiveMessage.encode(message.interactiveMessage, writer.uint32(/* id 45, wireType 2 =*/362).fork(), q + 1).ldelim();
            if (message.reactionMessage != null && Object.hasOwnProperty.call(message, "reactionMessage"))
                $root.proto.Message.ReactionMessage.encode(message.reactionMessage, writer.uint32(/* id 46, wireType 2 =*/370).fork(), q + 1).ldelim();
            if (message.stickerSyncRmrMessage != null && Object.hasOwnProperty.call(message, "stickerSyncRmrMessage"))
                $root.proto.Message.StickerSyncRMRMessage.encode(message.stickerSyncRmrMessage, writer.uint32(/* id 47, wireType 2 =*/378).fork(), q + 1).ldelim();
            if (message.interactiveResponseMessage != null && Object.hasOwnProperty.call(message, "interactiveResponseMessage"))
                $root.proto.Message.InteractiveResponseMessage.encode(message.interactiveResponseMessage, writer.uint32(/* id 48, wireType 2 =*/386).fork(), q + 1).ldelim();
            if (message.pollCreationMessage != null && Object.hasOwnProperty.call(message, "pollCreationMessage"))
                $root.proto.Message.PollCreationMessage.encode(message.pollCreationMessage, writer.uint32(/* id 49, wireType 2 =*/394).fork(), q + 1).ldelim();
            if (message.pollUpdateMessage != null && Object.hasOwnProperty.call(message, "pollUpdateMessage"))
                $root.proto.Message.PollUpdateMessage.encode(message.pollUpdateMessage, writer.uint32(/* id 50, wireType 2 =*/402).fork(), q + 1).ldelim();
            if (message.keepInChatMessage != null && Object.hasOwnProperty.call(message, "keepInChatMessage"))
                $root.proto.Message.KeepInChatMessage.encode(message.keepInChatMessage, writer.uint32(/* id 51, wireType 2 =*/410).fork(), q + 1).ldelim();
            if (message.documentWithCaptionMessage != null && Object.hasOwnProperty.call(message, "documentWithCaptionMessage"))
                $root.proto.Message.FutureProofMessage.encode(message.documentWithCaptionMessage, writer.uint32(/* id 53, wireType 2 =*/426).fork(), q + 1).ldelim();
            if (message.requestPhoneNumberMessage != null && Object.hasOwnProperty.call(message, "requestPhoneNumberMessage"))
                $root.proto.Message.RequestPhoneNumberMessage.encode(message.requestPhoneNumberMessage, writer.uint32(/* id 54, wireType 2 =*/434).fork(), q + 1).ldelim();
            if (message.viewOnceMessageV2 != null && Object.hasOwnProperty.call(message, "viewOnceMessageV2"))
                $root.proto.Message.FutureProofMessage.encode(message.viewOnceMessageV2, writer.uint32(/* id 55, wireType 2 =*/442).fork(), q + 1).ldelim();
            if (message.encReactionMessage != null && Object.hasOwnProperty.call(message, "encReactionMessage"))
                $root.proto.Message.EncReactionMessage.encode(message.encReactionMessage, writer.uint32(/* id 56, wireType 2 =*/450).fork(), q + 1).ldelim();
            if (message.editedMessage != null && Object.hasOwnProperty.call(message, "editedMessage"))
                $root.proto.Message.FutureProofMessage.encode(message.editedMessage, writer.uint32(/* id 58, wireType 2 =*/466).fork(), q + 1).ldelim();
            if (message.viewOnceMessageV2Extension != null && Object.hasOwnProperty.call(message, "viewOnceMessageV2Extension"))
                $root.proto.Message.FutureProofMessage.encode(message.viewOnceMessageV2Extension, writer.uint32(/* id 59, wireType 2 =*/474).fork(), q + 1).ldelim();
            if (message.pollCreationMessageV2 != null && Object.hasOwnProperty.call(message, "pollCreationMessageV2"))
                $root.proto.Message.PollCreationMessage.encode(message.pollCreationMessageV2, writer.uint32(/* id 60, wireType 2 =*/482).fork(), q + 1).ldelim();
            if (message.scheduledCallCreationMessage != null && Object.hasOwnProperty.call(message, "scheduledCallCreationMessage"))
                $root.proto.Message.ScheduledCallCreationMessage.encode(message.scheduledCallCreationMessage, writer.uint32(/* id 61, wireType 2 =*/490).fork(), q + 1).ldelim();
            if (message.groupMentionedMessage != null && Object.hasOwnProperty.call(message, "groupMentionedMessage"))
                $root.proto.Message.FutureProofMessage.encode(message.groupMentionedMessage, writer.uint32(/* id 62, wireType 2 =*/498).fork(), q + 1).ldelim();
            if (message.pinInChatMessage != null && Object.hasOwnProperty.call(message, "pinInChatMessage"))
                $root.proto.Message.PinInChatMessage.encode(message.pinInChatMessage, writer.uint32(/* id 63, wireType 2 =*/506).fork(), q + 1).ldelim();
            if (message.pollCreationMessageV3 != null && Object.hasOwnProperty.call(message, "pollCreationMessageV3"))
                $root.proto.Message.PollCreationMessage.encode(message.pollCreationMessageV3, writer.uint32(/* id 64, wireType 2 =*/514).fork(), q + 1).ldelim();
            if (message.scheduledCallEditMessage != null && Object.hasOwnProperty.call(message, "scheduledCallEditMessage"))
                $root.proto.Message.ScheduledCallEditMessage.encode(message.scheduledCallEditMessage, writer.uint32(/* id 65, wireType 2 =*/522).fork(), q + 1).ldelim();
            if (message.ptvMessage != null && Object.hasOwnProperty.call(message, "ptvMessage"))
                $root.proto.Message.VideoMessage.encode(message.ptvMessage, writer.uint32(/* id 66, wireType 2 =*/530).fork(), q + 1).ldelim();
            if (message.botInvokeMessage != null && Object.hasOwnProperty.call(message, "botInvokeMessage"))
                $root.proto.Message.FutureProofMessage.encode(message.botInvokeMessage, writer.uint32(/* id 67, wireType 2 =*/538).fork(), q + 1).ldelim();
            if (message.callLogMesssage != null && Object.hasOwnProperty.call(message, "callLogMesssage"))
                $root.proto.Message.CallLogMessage.encode(message.callLogMesssage, writer.uint32(/* id 69, wireType 2 =*/554).fork(), q + 1).ldelim();
            if (message.messageHistoryBundle != null && Object.hasOwnProperty.call(message, "messageHistoryBundle"))
                $root.proto.Message.MessageHistoryBundle.encode(message.messageHistoryBundle, writer.uint32(/* id 70, wireType 2 =*/562).fork(), q + 1).ldelim();
            if (message.encCommentMessage != null && Object.hasOwnProperty.call(message, "encCommentMessage"))
                $root.proto.Message.EncCommentMessage.encode(message.encCommentMessage, writer.uint32(/* id 71, wireType 2 =*/570).fork(), q + 1).ldelim();
            if (message.bcallMessage != null && Object.hasOwnProperty.call(message, "bcallMessage"))
                $root.proto.Message.BCallMessage.encode(message.bcallMessage, writer.uint32(/* id 72, wireType 2 =*/578).fork(), q + 1).ldelim();
            if (message.lottieStickerMessage != null && Object.hasOwnProperty.call(message, "lottieStickerMessage"))
                $root.proto.Message.FutureProofMessage.encode(message.lottieStickerMessage, writer.uint32(/* id 74, wireType 2 =*/594).fork(), q + 1).ldelim();
            if (message.eventMessage != null && Object.hasOwnProperty.call(message, "eventMessage"))
                $root.proto.Message.EventMessage.encode(message.eventMessage, writer.uint32(/* id 75, wireType 2 =*/602).fork(), q + 1).ldelim();
            if (message.encEventResponseMessage != null && Object.hasOwnProperty.call(message, "encEventResponseMessage"))
                $root.proto.Message.EncEventResponseMessage.encode(message.encEventResponseMessage, writer.uint32(/* id 76, wireType 2 =*/610).fork(), q + 1).ldelim();
            if (message.commentMessage != null && Object.hasOwnProperty.call(message, "commentMessage"))
                $root.proto.Message.CommentMessage.encode(message.commentMessage, writer.uint32(/* id 77, wireType 2 =*/618).fork(), q + 1).ldelim();
            if (message.newsletterAdminInviteMessage != null && Object.hasOwnProperty.call(message, "newsletterAdminInviteMessage"))
                $root.proto.Message.NewsletterAdminInviteMessage.encode(message.newsletterAdminInviteMessage, writer.uint32(/* id 78, wireType 2 =*/626).fork(), q + 1).ldelim();
            if (message.placeholderMessage != null && Object.hasOwnProperty.call(message, "placeholderMessage"))
                $root.proto.Message.PlaceholderMessage.encode(message.placeholderMessage, writer.uint32(/* id 80, wireType 2 =*/642).fork(), q + 1).ldelim();
            if (message.secretEncryptedMessage != null && Object.hasOwnProperty.call(message, "secretEncryptedMessage"))
                $root.proto.Message.SecretEncryptedMessage.encode(message.secretEncryptedMessage, writer.uint32(/* id 82, wireType 2 =*/658).fork(), q + 1).ldelim();
            if (message.albumMessage != null && Object.hasOwnProperty.call(message, "albumMessage"))
                $root.proto.Message.AlbumMessage.encode(message.albumMessage, writer.uint32(/* id 83, wireType 2 =*/666).fork(), q + 1).ldelim();
            if (message.eventCoverImage != null && Object.hasOwnProperty.call(message, "eventCoverImage"))
                $root.proto.Message.FutureProofMessage.encode(message.eventCoverImage, writer.uint32(/* id 85, wireType 2 =*/682).fork(), q + 1).ldelim();
            if (message.stickerPackMessage != null && Object.hasOwnProperty.call(message, "stickerPackMessage"))
                $root.proto.Message.StickerPackMessage.encode(message.stickerPackMessage, writer.uint32(/* id 86, wireType 2 =*/690).fork(), q + 1).ldelim();
            if (message.statusMentionMessage != null && Object.hasOwnProperty.call(message, "statusMentionMessage"))
                $root.proto.Message.FutureProofMessage.encode(message.statusMentionMessage, writer.uint32(/* id 87, wireType 2 =*/698).fork(), q + 1).ldelim();
            if (message.pollResultSnapshotMessage != null && Object.hasOwnProperty.call(message, "pollResultSnapshotMessage"))
                $root.proto.Message.PollResultSnapshotMessage.encode(message.pollResultSnapshotMessage, writer.uint32(/* id 88, wireType 2 =*/706).fork(), q + 1).ldelim();
            if (message.pollCreationOptionImageMessage != null && Object.hasOwnProperty.call(message, "pollCreationOptionImageMessage"))
                $root.proto.Message.FutureProofMessage.encode(message.pollCreationOptionImageMessage, writer.uint32(/* id 90, wireType 2 =*/722).fork(), q + 1).ldelim();
            if (message.associatedChildMessage != null && Object.hasOwnProperty.call(message, "associatedChildMessage"))
                $root.proto.Message.FutureProofMessage.encode(message.associatedChildMessage, writer.uint32(/* id 91, wireType 2 =*/730).fork(), q + 1).ldelim();
            if (message.groupStatusMentionMessage != null && Object.hasOwnProperty.call(message, "groupStatusMentionMessage"))
                $root.proto.Message.FutureProofMessage.encode(message.groupStatusMentionMessage, writer.uint32(/* id 92, wireType 2 =*/738).fork(), q + 1).ldelim();
            if (message.pollCreationMessageV4 != null && Object.hasOwnProperty.call(message, "pollCreationMessageV4"))
                $root.proto.Message.FutureProofMessage.encode(message.pollCreationMessageV4, writer.uint32(/* id 93, wireType 2 =*/746).fork(), q + 1).ldelim();
            if (message.statusAddYours != null && Object.hasOwnProperty.call(message, "statusAddYours"))
                $root.proto.Message.FutureProofMessage.encode(message.statusAddYours, writer.uint32(/* id 95, wireType 2 =*/762).fork(), q + 1).ldelim();
            if (message.groupStatusMessage != null && Object.hasOwnProperty.call(message, "groupStatusMessage"))
                $root.proto.Message.FutureProofMessage.encode(message.groupStatusMessage, writer.uint32(/* id 96, wireType 2 =*/770).fork(), q + 1).ldelim();
            if (message.richResponseMessage != null && Object.hasOwnProperty.call(message, "richResponseMessage"))
                $root.proto.AIRichResponseMessage.encode(message.richResponseMessage, writer.uint32(/* id 97, wireType 2 =*/778).fork(), q + 1).ldelim();
            if (message.statusNotificationMessage != null && Object.hasOwnProperty.call(message, "statusNotificationMessage"))
                $root.proto.Message.StatusNotificationMessage.encode(message.statusNotificationMessage, writer.uint32(/* id 98, wireType 2 =*/786).fork(), q + 1).ldelim();
            if (message.limitSharingMessage != null && Object.hasOwnProperty.call(message, "limitSharingMessage"))
                $root.proto.Message.FutureProofMessage.encode(message.limitSharingMessage, writer.uint32(/* id 99, wireType 2 =*/794).fork(), q + 1).ldelim();
            if (message.botTaskMessage != null && Object.hasOwnProperty.call(message, "botTaskMessage"))
                $root.proto.Message.FutureProofMessage.encode(message.botTaskMessage, writer.uint32(/* id 100, wireType 2 =*/802).fork(), q + 1).ldelim();
            if (message.questionMessage != null && Object.hasOwnProperty.call(message, "questionMessage"))
                $root.proto.Message.FutureProofMessage.encode(message.questionMessage, writer.uint32(/* id 101, wireType 2 =*/810).fork(), q + 1).ldelim();
            if (message.messageHistoryNotice != null && Object.hasOwnProperty.call(message, "messageHistoryNotice"))
                $root.proto.Message.MessageHistoryNotice.encode(message.messageHistoryNotice, writer.uint32(/* id 102, wireType 2 =*/818).fork(), q + 1).ldelim();
            if (message.groupStatusMessageV2 != null && Object.hasOwnProperty.call(message, "groupStatusMessageV2"))
                $root.proto.Message.FutureProofMessage.encode(message.groupStatusMessageV2, writer.uint32(/* id 103, wireType 2 =*/826).fork(), q + 1).ldelim();
            if (message.botForwardedMessage != null && Object.hasOwnProperty.call(message, "botForwardedMessage"))
                $root.proto.Message.FutureProofMessage.encode(message.botForwardedMessage, writer.uint32(/* id 104, wireType 2 =*/834).fork(), q + 1).ldelim();
            if (message.statusQuestionAnswerMessage != null && Object.hasOwnProperty.call(message, "statusQuestionAnswerMessage"))
                $root.proto.Message.StatusQuestionAnswerMessage.encode(message.statusQuestionAnswerMessage, writer.uint32(/* id 105, wireType 2 =*/842).fork(), q + 1).ldelim();
            if (message.questionReplyMessage != null && Object.hasOwnProperty.call(message, "questionReplyMessage"))
                $root.proto.Message.FutureProofMessage.encode(message.questionReplyMessage, writer.uint32(/* id 106, wireType 2 =*/850).fork(), q + 1).ldelim();
            if (message.questionResponseMessage != null && Object.hasOwnProperty.call(message, "questionResponseMessage"))
                $root.proto.Message.QuestionResponseMessage.encode(message.questionResponseMessage, writer.uint32(/* id 107, wireType 2 =*/858).fork(), q + 1).ldelim();
            if (message.statusQuotedMessage != null && Object.hasOwnProperty.call(message, "statusQuotedMessage"))
                $root.proto.Message.StatusQuotedMessage.encode(message.statusQuotedMessage, writer.uint32(/* id 109, wireType 2 =*/874).fork(), q + 1).ldelim();
            if (message.statusStickerInteractionMessage != null && Object.hasOwnProperty.call(message, "statusStickerInteractionMessage"))
                $root.proto.Message.StatusStickerInteractionMessage.encode(message.statusStickerInteractionMessage, writer.uint32(/* id 110, wireType 2 =*/882).fork(), q + 1).ldelim();
            if (message.pollCreationMessageV5 != null && Object.hasOwnProperty.call(message, "pollCreationMessageV5"))
                $root.proto.Message.PollCreationMessage.encode(message.pollCreationMessageV5, writer.uint32(/* id 111, wireType 2 =*/890).fork(), q + 1).ldelim();
            if (message.newsletterFollowerInviteMessageV2 != null && Object.hasOwnProperty.call(message, "newsletterFollowerInviteMessageV2"))
                $root.proto.Message.NewsletterFollowerInviteMessage.encode(message.newsletterFollowerInviteMessageV2, writer.uint32(/* id 113, wireType 2 =*/906).fork(), q + 1).ldelim();
            if (message.pollResultSnapshotMessageV3 != null && Object.hasOwnProperty.call(message, "pollResultSnapshotMessageV3"))
                $root.proto.Message.PollResultSnapshotMessage.encode(message.pollResultSnapshotMessageV3, writer.uint32(/* id 115, wireType 2 =*/922).fork(), q + 1).ldelim();
            if (message.newsletterAdminProfileMessage != null && Object.hasOwnProperty.call(message, "newsletterAdminProfileMessage"))
                $root.proto.Message.FutureProofMessage.encode(message.newsletterAdminProfileMessage, writer.uint32(/* id 116, wireType 2 =*/930).fork(), q + 1).ldelim();
            if (message.newsletterAdminProfileMessageV2 != null && Object.hasOwnProperty.call(message, "newsletterAdminProfileMessageV2"))
                $root.proto.Message.FutureProofMessage.encode(message.newsletterAdminProfileMessageV2, writer.uint32(/* id 117, wireType 2 =*/938).fork(), q + 1).ldelim();
            if (message.spoilerMessage != null && Object.hasOwnProperty.call(message, "spoilerMessage"))
                $root.proto.Message.FutureProofMessage.encode(message.spoilerMessage, writer.uint32(/* id 118, wireType 2 =*/946).fork(), q + 1).ldelim();
            if (message.pollCreationMessageV6 != null && Object.hasOwnProperty.call(message, "pollCreationMessageV6"))
                $root.proto.Message.PollCreationMessage.encode(message.pollCreationMessageV6, writer.uint32(/* id 119, wireType 2 =*/954).fork(), q + 1).ldelim();
            if (message.conditionalRevealMessage != null && Object.hasOwnProperty.call(message, "conditionalRevealMessage"))
                $root.proto.Message.ConditionalRevealMessage.encode(message.conditionalRevealMessage, writer.uint32(/* id 120, wireType 2 =*/962).fork(), q + 1).ldelim();
            if (message.pollAddOptionMessage != null && Object.hasOwnProperty.call(message, "pollAddOptionMessage"))
                $root.proto.Message.PollAddOptionMessage.encode(message.pollAddOptionMessage, writer.uint32(/* id 121, wireType 2 =*/970).fork(), q + 1).ldelim();
            if (message.eventInviteMessage != null && Object.hasOwnProperty.call(message, "eventInviteMessage"))
                $root.proto.Message.EventInviteMessage.encode(message.eventInviteMessage, writer.uint32(/* id 122, wireType 2 =*/978).fork(), q + 1).ldelim();
            if (message.groupRootKeyShare != null && Object.hasOwnProperty.call(message, "groupRootKeyShare"))
                $root.proto.GroupRootKeyShare.encode(message.groupRootKeyShare, writer.uint32(/* id 123, wireType 2 =*/986).fork(), q + 1).ldelim();
            if (message.paymentReminderMessage != null && Object.hasOwnProperty.call(message, "paymentReminderMessage"))
                $root.proto.Message.PaymentReminderMessage.encode(message.paymentReminderMessage, writer.uint32(/* id 124, wireType 2 =*/994).fork(), q + 1).ldelim();
            if (message.splitPaymentMessage != null && Object.hasOwnProperty.call(message, "splitPaymentMessage"))
                $root.proto.Message.SplitPaymentMessage.encode(message.splitPaymentMessage, writer.uint32(/* id 125, wireType 2 =*/1002).fork(), q + 1).ldelim();
            if (message.newsletterAdminProfileStatusMessage != null && Object.hasOwnProperty.call(message, "newsletterAdminProfileStatusMessage"))
                $root.proto.Message.FutureProofMessage.encode(message.newsletterAdminProfileStatusMessage, writer.uint32(/* id 126, wireType 2 =*/1010).fork(), q + 1).ldelim();
            if (message.rootSecretDistributeMessage != null && Object.hasOwnProperty.call(message, "rootSecretDistributeMessage"))
                $root.proto.Message.RootSecretDistributeMessage.encode(message.rootSecretDistributeMessage, writer.uint32(/* id 127, wireType 2 =*/1018).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Message message, length delimited. Does not implicitly {@link proto.Message.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Message
         * @static
         * @param {proto.IMessage} message Message message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Message.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Message message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Message
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Message} Message
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Message.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.conversation = reader.stringVerify();
                        message._conversation = "conversation";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.senderKeyDistributionMessage = $root.proto.Message.SenderKeyDistributionMessage.decode(reader, reader.uint32(), undefined, q + 1, message.senderKeyDistributionMessage);
                        message._senderKeyDistributionMessage = "senderKeyDistributionMessage";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.imageMessage = $root.proto.Message.ImageMessage.decode(reader, reader.uint32(), undefined, q + 1, message.imageMessage);
                        message._imageMessage = "imageMessage";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.contactMessage = $root.proto.Message.ContactMessage.decode(reader, reader.uint32(), undefined, q + 1, message.contactMessage);
                        message._contactMessage = "contactMessage";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.locationMessage = $root.proto.Message.LocationMessage.decode(reader, reader.uint32(), undefined, q + 1, message.locationMessage);
                        message._locationMessage = "locationMessage";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        message.extendedTextMessage = $root.proto.Message.ExtendedTextMessage.decode(reader, reader.uint32(), undefined, q + 1, message.extendedTextMessage);
                        message._extendedTextMessage = "extendedTextMessage";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        message.documentMessage = $root.proto.Message.DocumentMessage.decode(reader, reader.uint32(), undefined, q + 1, message.documentMessage);
                        message._documentMessage = "documentMessage";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        message.audioMessage = $root.proto.Message.AudioMessage.decode(reader, reader.uint32(), undefined, q + 1, message.audioMessage);
                        message._audioMessage = "audioMessage";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        message.videoMessage = $root.proto.Message.VideoMessage.decode(reader, reader.uint32(), undefined, q + 1, message.videoMessage);
                        message._videoMessage = "videoMessage";
                        continue;
                    }
                case 10: {
                        if (u !== 2)
                            break;
                        message.call = $root.proto.Message.Call.decode(reader, reader.uint32(), undefined, q + 1, message.call);
                        message._call = "call";
                        continue;
                    }
                case 11: {
                        if (u !== 2)
                            break;
                        message.chat = $root.proto.Message.Chat.decode(reader, reader.uint32(), undefined, q + 1, message.chat);
                        message._chat = "chat";
                        continue;
                    }
                case 12: {
                        if (u !== 2)
                            break;
                        message.protocolMessage = $root.proto.Message.ProtocolMessage.decode(reader, reader.uint32(), undefined, q + 1, message.protocolMessage);
                        message._protocolMessage = "protocolMessage";
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        message.contactsArrayMessage = $root.proto.Message.ContactsArrayMessage.decode(reader, reader.uint32(), undefined, q + 1, message.contactsArrayMessage);
                        message._contactsArrayMessage = "contactsArrayMessage";
                        continue;
                    }
                case 14: {
                        if (u !== 2)
                            break;
                        message.highlyStructuredMessage = $root.proto.Message.HighlyStructuredMessage.decode(reader, reader.uint32(), undefined, q + 1, message.highlyStructuredMessage);
                        message._highlyStructuredMessage = "highlyStructuredMessage";
                        continue;
                    }
                case 15: {
                        if (u !== 2)
                            break;
                        message.fastRatchetKeySenderKeyDistributionMessage = $root.proto.Message.SenderKeyDistributionMessage.decode(reader, reader.uint32(), undefined, q + 1, message.fastRatchetKeySenderKeyDistributionMessage);
                        message._fastRatchetKeySenderKeyDistributionMessage = "fastRatchetKeySenderKeyDistributionMessage";
                        continue;
                    }
                case 16: {
                        if (u !== 2)
                            break;
                        message.sendPaymentMessage = $root.proto.Message.SendPaymentMessage.decode(reader, reader.uint32(), undefined, q + 1, message.sendPaymentMessage);
                        message._sendPaymentMessage = "sendPaymentMessage";
                        continue;
                    }
                case 18: {
                        if (u !== 2)
                            break;
                        message.liveLocationMessage = $root.proto.Message.LiveLocationMessage.decode(reader, reader.uint32(), undefined, q + 1, message.liveLocationMessage);
                        message._liveLocationMessage = "liveLocationMessage";
                        continue;
                    }
                case 22: {
                        if (u !== 2)
                            break;
                        message.requestPaymentMessage = $root.proto.Message.RequestPaymentMessage.decode(reader, reader.uint32(), undefined, q + 1, message.requestPaymentMessage);
                        message._requestPaymentMessage = "requestPaymentMessage";
                        continue;
                    }
                case 23: {
                        if (u !== 2)
                            break;
                        message.declinePaymentRequestMessage = $root.proto.Message.DeclinePaymentRequestMessage.decode(reader, reader.uint32(), undefined, q + 1, message.declinePaymentRequestMessage);
                        message._declinePaymentRequestMessage = "declinePaymentRequestMessage";
                        continue;
                    }
                case 24: {
                        if (u !== 2)
                            break;
                        message.cancelPaymentRequestMessage = $root.proto.Message.CancelPaymentRequestMessage.decode(reader, reader.uint32(), undefined, q + 1, message.cancelPaymentRequestMessage);
                        message._cancelPaymentRequestMessage = "cancelPaymentRequestMessage";
                        continue;
                    }
                case 25: {
                        if (u !== 2)
                            break;
                        message.templateMessage = $root.proto.Message.TemplateMessage.decode(reader, reader.uint32(), undefined, q + 1, message.templateMessage);
                        message._templateMessage = "templateMessage";
                        continue;
                    }
                case 26: {
                        if (u !== 2)
                            break;
                        message.stickerMessage = $root.proto.Message.StickerMessage.decode(reader, reader.uint32(), undefined, q + 1, message.stickerMessage);
                        message._stickerMessage = "stickerMessage";
                        continue;
                    }
                case 28: {
                        if (u !== 2)
                            break;
                        message.groupInviteMessage = $root.proto.Message.GroupInviteMessage.decode(reader, reader.uint32(), undefined, q + 1, message.groupInviteMessage);
                        message._groupInviteMessage = "groupInviteMessage";
                        continue;
                    }
                case 29: {
                        if (u !== 2)
                            break;
                        message.templateButtonReplyMessage = $root.proto.Message.TemplateButtonReplyMessage.decode(reader, reader.uint32(), undefined, q + 1, message.templateButtonReplyMessage);
                        message._templateButtonReplyMessage = "templateButtonReplyMessage";
                        continue;
                    }
                case 30: {
                        if (u !== 2)
                            break;
                        message.productMessage = $root.proto.Message.ProductMessage.decode(reader, reader.uint32(), undefined, q + 1, message.productMessage);
                        message._productMessage = "productMessage";
                        continue;
                    }
                case 31: {
                        if (u !== 2)
                            break;
                        message.deviceSentMessage = $root.proto.Message.DeviceSentMessage.decode(reader, reader.uint32(), undefined, q + 1, message.deviceSentMessage);
                        message._deviceSentMessage = "deviceSentMessage";
                        continue;
                    }
                case 35: {
                        if (u !== 2)
                            break;
                        message.messageContextInfo = $root.proto.MessageContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.messageContextInfo);
                        message._messageContextInfo = "messageContextInfo";
                        continue;
                    }
                case 36: {
                        if (u !== 2)
                            break;
                        message.listMessage = $root.proto.Message.ListMessage.decode(reader, reader.uint32(), undefined, q + 1, message.listMessage);
                        message._listMessage = "listMessage";
                        continue;
                    }
                case 37: {
                        if (u !== 2)
                            break;
                        message.viewOnceMessage = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.viewOnceMessage);
                        message._viewOnceMessage = "viewOnceMessage";
                        continue;
                    }
                case 38: {
                        if (u !== 2)
                            break;
                        message.orderMessage = $root.proto.Message.OrderMessage.decode(reader, reader.uint32(), undefined, q + 1, message.orderMessage);
                        message._orderMessage = "orderMessage";
                        continue;
                    }
                case 39: {
                        if (u !== 2)
                            break;
                        message.listResponseMessage = $root.proto.Message.ListResponseMessage.decode(reader, reader.uint32(), undefined, q + 1, message.listResponseMessage);
                        message._listResponseMessage = "listResponseMessage";
                        continue;
                    }
                case 40: {
                        if (u !== 2)
                            break;
                        message.ephemeralMessage = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.ephemeralMessage);
                        message._ephemeralMessage = "ephemeralMessage";
                        continue;
                    }
                case 41: {
                        if (u !== 2)
                            break;
                        message.invoiceMessage = $root.proto.Message.InvoiceMessage.decode(reader, reader.uint32(), undefined, q + 1, message.invoiceMessage);
                        message._invoiceMessage = "invoiceMessage";
                        continue;
                    }
                case 42: {
                        if (u !== 2)
                            break;
                        message.buttonsMessage = $root.proto.Message.ButtonsMessage.decode(reader, reader.uint32(), undefined, q + 1, message.buttonsMessage);
                        message._buttonsMessage = "buttonsMessage";
                        continue;
                    }
                case 43: {
                        if (u !== 2)
                            break;
                        message.buttonsResponseMessage = $root.proto.Message.ButtonsResponseMessage.decode(reader, reader.uint32(), undefined, q + 1, message.buttonsResponseMessage);
                        message._buttonsResponseMessage = "buttonsResponseMessage";
                        continue;
                    }
                case 44: {
                        if (u !== 2)
                            break;
                        message.paymentInviteMessage = $root.proto.Message.PaymentInviteMessage.decode(reader, reader.uint32(), undefined, q + 1, message.paymentInviteMessage);
                        message._paymentInviteMessage = "paymentInviteMessage";
                        continue;
                    }
                case 45: {
                        if (u !== 2)
                            break;
                        message.interactiveMessage = $root.proto.Message.InteractiveMessage.decode(reader, reader.uint32(), undefined, q + 1, message.interactiveMessage);
                        message._interactiveMessage = "interactiveMessage";
                        continue;
                    }
                case 46: {
                        if (u !== 2)
                            break;
                        message.reactionMessage = $root.proto.Message.ReactionMessage.decode(reader, reader.uint32(), undefined, q + 1, message.reactionMessage);
                        message._reactionMessage = "reactionMessage";
                        continue;
                    }
                case 47: {
                        if (u !== 2)
                            break;
                        message.stickerSyncRmrMessage = $root.proto.Message.StickerSyncRMRMessage.decode(reader, reader.uint32(), undefined, q + 1, message.stickerSyncRmrMessage);
                        message._stickerSyncRmrMessage = "stickerSyncRmrMessage";
                        continue;
                    }
                case 48: {
                        if (u !== 2)
                            break;
                        message.interactiveResponseMessage = $root.proto.Message.InteractiveResponseMessage.decode(reader, reader.uint32(), undefined, q + 1, message.interactiveResponseMessage);
                        message._interactiveResponseMessage = "interactiveResponseMessage";
                        continue;
                    }
                case 49: {
                        if (u !== 2)
                            break;
                        message.pollCreationMessage = $root.proto.Message.PollCreationMessage.decode(reader, reader.uint32(), undefined, q + 1, message.pollCreationMessage);
                        message._pollCreationMessage = "pollCreationMessage";
                        continue;
                    }
                case 50: {
                        if (u !== 2)
                            break;
                        message.pollUpdateMessage = $root.proto.Message.PollUpdateMessage.decode(reader, reader.uint32(), undefined, q + 1, message.pollUpdateMessage);
                        message._pollUpdateMessage = "pollUpdateMessage";
                        continue;
                    }
                case 51: {
                        if (u !== 2)
                            break;
                        message.keepInChatMessage = $root.proto.Message.KeepInChatMessage.decode(reader, reader.uint32(), undefined, q + 1, message.keepInChatMessage);
                        message._keepInChatMessage = "keepInChatMessage";
                        continue;
                    }
                case 53: {
                        if (u !== 2)
                            break;
                        message.documentWithCaptionMessage = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.documentWithCaptionMessage);
                        message._documentWithCaptionMessage = "documentWithCaptionMessage";
                        continue;
                    }
                case 54: {
                        if (u !== 2)
                            break;
                        message.requestPhoneNumberMessage = $root.proto.Message.RequestPhoneNumberMessage.decode(reader, reader.uint32(), undefined, q + 1, message.requestPhoneNumberMessage);
                        message._requestPhoneNumberMessage = "requestPhoneNumberMessage";
                        continue;
                    }
                case 55: {
                        if (u !== 2)
                            break;
                        message.viewOnceMessageV2 = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.viewOnceMessageV2);
                        message._viewOnceMessageV2 = "viewOnceMessageV2";
                        continue;
                    }
                case 56: {
                        if (u !== 2)
                            break;
                        message.encReactionMessage = $root.proto.Message.EncReactionMessage.decode(reader, reader.uint32(), undefined, q + 1, message.encReactionMessage);
                        message._encReactionMessage = "encReactionMessage";
                        continue;
                    }
                case 58: {
                        if (u !== 2)
                            break;
                        message.editedMessage = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.editedMessage);
                        message._editedMessage = "editedMessage";
                        continue;
                    }
                case 59: {
                        if (u !== 2)
                            break;
                        message.viewOnceMessageV2Extension = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.viewOnceMessageV2Extension);
                        message._viewOnceMessageV2Extension = "viewOnceMessageV2Extension";
                        continue;
                    }
                case 60: {
                        if (u !== 2)
                            break;
                        message.pollCreationMessageV2 = $root.proto.Message.PollCreationMessage.decode(reader, reader.uint32(), undefined, q + 1, message.pollCreationMessageV2);
                        message._pollCreationMessageV2 = "pollCreationMessageV2";
                        continue;
                    }
                case 61: {
                        if (u !== 2)
                            break;
                        message.scheduledCallCreationMessage = $root.proto.Message.ScheduledCallCreationMessage.decode(reader, reader.uint32(), undefined, q + 1, message.scheduledCallCreationMessage);
                        message._scheduledCallCreationMessage = "scheduledCallCreationMessage";
                        continue;
                    }
                case 62: {
                        if (u !== 2)
                            break;
                        message.groupMentionedMessage = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.groupMentionedMessage);
                        message._groupMentionedMessage = "groupMentionedMessage";
                        continue;
                    }
                case 63: {
                        if (u !== 2)
                            break;
                        message.pinInChatMessage = $root.proto.Message.PinInChatMessage.decode(reader, reader.uint32(), undefined, q + 1, message.pinInChatMessage);
                        message._pinInChatMessage = "pinInChatMessage";
                        continue;
                    }
                case 64: {
                        if (u !== 2)
                            break;
                        message.pollCreationMessageV3 = $root.proto.Message.PollCreationMessage.decode(reader, reader.uint32(), undefined, q + 1, message.pollCreationMessageV3);
                        message._pollCreationMessageV3 = "pollCreationMessageV3";
                        continue;
                    }
                case 65: {
                        if (u !== 2)
                            break;
                        message.scheduledCallEditMessage = $root.proto.Message.ScheduledCallEditMessage.decode(reader, reader.uint32(), undefined, q + 1, message.scheduledCallEditMessage);
                        message._scheduledCallEditMessage = "scheduledCallEditMessage";
                        continue;
                    }
                case 66: {
                        if (u !== 2)
                            break;
                        message.ptvMessage = $root.proto.Message.VideoMessage.decode(reader, reader.uint32(), undefined, q + 1, message.ptvMessage);
                        message._ptvMessage = "ptvMessage";
                        continue;
                    }
                case 67: {
                        if (u !== 2)
                            break;
                        message.botInvokeMessage = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.botInvokeMessage);
                        message._botInvokeMessage = "botInvokeMessage";
                        continue;
                    }
                case 69: {
                        if (u !== 2)
                            break;
                        message.callLogMesssage = $root.proto.Message.CallLogMessage.decode(reader, reader.uint32(), undefined, q + 1, message.callLogMesssage);
                        message._callLogMesssage = "callLogMesssage";
                        continue;
                    }
                case 70: {
                        if (u !== 2)
                            break;
                        message.messageHistoryBundle = $root.proto.Message.MessageHistoryBundle.decode(reader, reader.uint32(), undefined, q + 1, message.messageHistoryBundle);
                        message._messageHistoryBundle = "messageHistoryBundle";
                        continue;
                    }
                case 71: {
                        if (u !== 2)
                            break;
                        message.encCommentMessage = $root.proto.Message.EncCommentMessage.decode(reader, reader.uint32(), undefined, q + 1, message.encCommentMessage);
                        message._encCommentMessage = "encCommentMessage";
                        continue;
                    }
                case 72: {
                        if (u !== 2)
                            break;
                        message.bcallMessage = $root.proto.Message.BCallMessage.decode(reader, reader.uint32(), undefined, q + 1, message.bcallMessage);
                        message._bcallMessage = "bcallMessage";
                        continue;
                    }
                case 74: {
                        if (u !== 2)
                            break;
                        message.lottieStickerMessage = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.lottieStickerMessage);
                        message._lottieStickerMessage = "lottieStickerMessage";
                        continue;
                    }
                case 75: {
                        if (u !== 2)
                            break;
                        message.eventMessage = $root.proto.Message.EventMessage.decode(reader, reader.uint32(), undefined, q + 1, message.eventMessage);
                        message._eventMessage = "eventMessage";
                        continue;
                    }
                case 76: {
                        if (u !== 2)
                            break;
                        message.encEventResponseMessage = $root.proto.Message.EncEventResponseMessage.decode(reader, reader.uint32(), undefined, q + 1, message.encEventResponseMessage);
                        message._encEventResponseMessage = "encEventResponseMessage";
                        continue;
                    }
                case 77: {
                        if (u !== 2)
                            break;
                        message.commentMessage = $root.proto.Message.CommentMessage.decode(reader, reader.uint32(), undefined, q + 1, message.commentMessage);
                        message._commentMessage = "commentMessage";
                        continue;
                    }
                case 78: {
                        if (u !== 2)
                            break;
                        message.newsletterAdminInviteMessage = $root.proto.Message.NewsletterAdminInviteMessage.decode(reader, reader.uint32(), undefined, q + 1, message.newsletterAdminInviteMessage);
                        message._newsletterAdminInviteMessage = "newsletterAdminInviteMessage";
                        continue;
                    }
                case 80: {
                        if (u !== 2)
                            break;
                        message.placeholderMessage = $root.proto.Message.PlaceholderMessage.decode(reader, reader.uint32(), undefined, q + 1, message.placeholderMessage);
                        message._placeholderMessage = "placeholderMessage";
                        continue;
                    }
                case 82: {
                        if (u !== 2)
                            break;
                        message.secretEncryptedMessage = $root.proto.Message.SecretEncryptedMessage.decode(reader, reader.uint32(), undefined, q + 1, message.secretEncryptedMessage);
                        message._secretEncryptedMessage = "secretEncryptedMessage";
                        continue;
                    }
                case 83: {
                        if (u !== 2)
                            break;
                        message.albumMessage = $root.proto.Message.AlbumMessage.decode(reader, reader.uint32(), undefined, q + 1, message.albumMessage);
                        message._albumMessage = "albumMessage";
                        continue;
                    }
                case 85: {
                        if (u !== 2)
                            break;
                        message.eventCoverImage = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.eventCoverImage);
                        message._eventCoverImage = "eventCoverImage";
                        continue;
                    }
                case 86: {
                        if (u !== 2)
                            break;
                        message.stickerPackMessage = $root.proto.Message.StickerPackMessage.decode(reader, reader.uint32(), undefined, q + 1, message.stickerPackMessage);
                        message._stickerPackMessage = "stickerPackMessage";
                        continue;
                    }
                case 87: {
                        if (u !== 2)
                            break;
                        message.statusMentionMessage = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.statusMentionMessage);
                        message._statusMentionMessage = "statusMentionMessage";
                        continue;
                    }
                case 88: {
                        if (u !== 2)
                            break;
                        message.pollResultSnapshotMessage = $root.proto.Message.PollResultSnapshotMessage.decode(reader, reader.uint32(), undefined, q + 1, message.pollResultSnapshotMessage);
                        message._pollResultSnapshotMessage = "pollResultSnapshotMessage";
                        continue;
                    }
                case 90: {
                        if (u !== 2)
                            break;
                        message.pollCreationOptionImageMessage = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.pollCreationOptionImageMessage);
                        message._pollCreationOptionImageMessage = "pollCreationOptionImageMessage";
                        continue;
                    }
                case 91: {
                        if (u !== 2)
                            break;
                        message.associatedChildMessage = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.associatedChildMessage);
                        message._associatedChildMessage = "associatedChildMessage";
                        continue;
                    }
                case 92: {
                        if (u !== 2)
                            break;
                        message.groupStatusMentionMessage = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.groupStatusMentionMessage);
                        message._groupStatusMentionMessage = "groupStatusMentionMessage";
                        continue;
                    }
                case 93: {
                        if (u !== 2)
                            break;
                        message.pollCreationMessageV4 = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.pollCreationMessageV4);
                        message._pollCreationMessageV4 = "pollCreationMessageV4";
                        continue;
                    }
                case 95: {
                        if (u !== 2)
                            break;
                        message.statusAddYours = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.statusAddYours);
                        message._statusAddYours = "statusAddYours";
                        continue;
                    }
                case 96: {
                        if (u !== 2)
                            break;
                        message.groupStatusMessage = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.groupStatusMessage);
                        message._groupStatusMessage = "groupStatusMessage";
                        continue;
                    }
                case 97: {
                        if (u !== 2)
                            break;
                        message.richResponseMessage = $root.proto.AIRichResponseMessage.decode(reader, reader.uint32(), undefined, q + 1, message.richResponseMessage);
                        message._richResponseMessage = "richResponseMessage";
                        continue;
                    }
                case 98: {
                        if (u !== 2)
                            break;
                        message.statusNotificationMessage = $root.proto.Message.StatusNotificationMessage.decode(reader, reader.uint32(), undefined, q + 1, message.statusNotificationMessage);
                        message._statusNotificationMessage = "statusNotificationMessage";
                        continue;
                    }
                case 99: {
                        if (u !== 2)
                            break;
                        message.limitSharingMessage = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.limitSharingMessage);
                        message._limitSharingMessage = "limitSharingMessage";
                        continue;
                    }
                case 100: {
                        if (u !== 2)
                            break;
                        message.botTaskMessage = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.botTaskMessage);
                        message._botTaskMessage = "botTaskMessage";
                        continue;
                    }
                case 101: {
                        if (u !== 2)
                            break;
                        message.questionMessage = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.questionMessage);
                        message._questionMessage = "questionMessage";
                        continue;
                    }
                case 102: {
                        if (u !== 2)
                            break;
                        message.messageHistoryNotice = $root.proto.Message.MessageHistoryNotice.decode(reader, reader.uint32(), undefined, q + 1, message.messageHistoryNotice);
                        message._messageHistoryNotice = "messageHistoryNotice";
                        continue;
                    }
                case 103: {
                        if (u !== 2)
                            break;
                        message.groupStatusMessageV2 = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.groupStatusMessageV2);
                        message._groupStatusMessageV2 = "groupStatusMessageV2";
                        continue;
                    }
                case 104: {
                        if (u !== 2)
                            break;
                        message.botForwardedMessage = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.botForwardedMessage);
                        message._botForwardedMessage = "botForwardedMessage";
                        continue;
                    }
                case 105: {
                        if (u !== 2)
                            break;
                        message.statusQuestionAnswerMessage = $root.proto.Message.StatusQuestionAnswerMessage.decode(reader, reader.uint32(), undefined, q + 1, message.statusQuestionAnswerMessage);
                        message._statusQuestionAnswerMessage = "statusQuestionAnswerMessage";
                        continue;
                    }
                case 106: {
                        if (u !== 2)
                            break;
                        message.questionReplyMessage = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.questionReplyMessage);
                        message._questionReplyMessage = "questionReplyMessage";
                        continue;
                    }
                case 107: {
                        if (u !== 2)
                            break;
                        message.questionResponseMessage = $root.proto.Message.QuestionResponseMessage.decode(reader, reader.uint32(), undefined, q + 1, message.questionResponseMessage);
                        message._questionResponseMessage = "questionResponseMessage";
                        continue;
                    }
                case 109: {
                        if (u !== 2)
                            break;
                        message.statusQuotedMessage = $root.proto.Message.StatusQuotedMessage.decode(reader, reader.uint32(), undefined, q + 1, message.statusQuotedMessage);
                        message._statusQuotedMessage = "statusQuotedMessage";
                        continue;
                    }
                case 110: {
                        if (u !== 2)
                            break;
                        message.statusStickerInteractionMessage = $root.proto.Message.StatusStickerInteractionMessage.decode(reader, reader.uint32(), undefined, q + 1, message.statusStickerInteractionMessage);
                        message._statusStickerInteractionMessage = "statusStickerInteractionMessage";
                        continue;
                    }
                case 111: {
                        if (u !== 2)
                            break;
                        message.pollCreationMessageV5 = $root.proto.Message.PollCreationMessage.decode(reader, reader.uint32(), undefined, q + 1, message.pollCreationMessageV5);
                        message._pollCreationMessageV5 = "pollCreationMessageV5";
                        continue;
                    }
                case 113: {
                        if (u !== 2)
                            break;
                        message.newsletterFollowerInviteMessageV2 = $root.proto.Message.NewsletterFollowerInviteMessage.decode(reader, reader.uint32(), undefined, q + 1, message.newsletterFollowerInviteMessageV2);
                        message._newsletterFollowerInviteMessageV2 = "newsletterFollowerInviteMessageV2";
                        continue;
                    }
                case 115: {
                        if (u !== 2)
                            break;
                        message.pollResultSnapshotMessageV3 = $root.proto.Message.PollResultSnapshotMessage.decode(reader, reader.uint32(), undefined, q + 1, message.pollResultSnapshotMessageV3);
                        message._pollResultSnapshotMessageV3 = "pollResultSnapshotMessageV3";
                        continue;
                    }
                case 116: {
                        if (u !== 2)
                            break;
                        message.newsletterAdminProfileMessage = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.newsletterAdminProfileMessage);
                        message._newsletterAdminProfileMessage = "newsletterAdminProfileMessage";
                        continue;
                    }
                case 117: {
                        if (u !== 2)
                            break;
                        message.newsletterAdminProfileMessageV2 = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.newsletterAdminProfileMessageV2);
                        message._newsletterAdminProfileMessageV2 = "newsletterAdminProfileMessageV2";
                        continue;
                    }
                case 118: {
                        if (u !== 2)
                            break;
                        message.spoilerMessage = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.spoilerMessage);
                        message._spoilerMessage = "spoilerMessage";
                        continue;
                    }
                case 119: {
                        if (u !== 2)
                            break;
                        message.pollCreationMessageV6 = $root.proto.Message.PollCreationMessage.decode(reader, reader.uint32(), undefined, q + 1, message.pollCreationMessageV6);
                        message._pollCreationMessageV6 = "pollCreationMessageV6";
                        continue;
                    }
                case 120: {
                        if (u !== 2)
                            break;
                        message.conditionalRevealMessage = $root.proto.Message.ConditionalRevealMessage.decode(reader, reader.uint32(), undefined, q + 1, message.conditionalRevealMessage);
                        message._conditionalRevealMessage = "conditionalRevealMessage";
                        continue;
                    }
                case 121: {
                        if (u !== 2)
                            break;
                        message.pollAddOptionMessage = $root.proto.Message.PollAddOptionMessage.decode(reader, reader.uint32(), undefined, q + 1, message.pollAddOptionMessage);
                        message._pollAddOptionMessage = "pollAddOptionMessage";
                        continue;
                    }
                case 122: {
                        if (u !== 2)
                            break;
                        message.eventInviteMessage = $root.proto.Message.EventInviteMessage.decode(reader, reader.uint32(), undefined, q + 1, message.eventInviteMessage);
                        message._eventInviteMessage = "eventInviteMessage";
                        continue;
                    }
                case 123: {
                        if (u !== 2)
                            break;
                        message.groupRootKeyShare = $root.proto.GroupRootKeyShare.decode(reader, reader.uint32(), undefined, q + 1, message.groupRootKeyShare);
                        message._groupRootKeyShare = "groupRootKeyShare";
                        continue;
                    }
                case 124: {
                        if (u !== 2)
                            break;
                        message.paymentReminderMessage = $root.proto.Message.PaymentReminderMessage.decode(reader, reader.uint32(), undefined, q + 1, message.paymentReminderMessage);
                        message._paymentReminderMessage = "paymentReminderMessage";
                        continue;
                    }
                case 125: {
                        if (u !== 2)
                            break;
                        message.splitPaymentMessage = $root.proto.Message.SplitPaymentMessage.decode(reader, reader.uint32(), undefined, q + 1, message.splitPaymentMessage);
                        message._splitPaymentMessage = "splitPaymentMessage";
                        continue;
                    }
                case 126: {
                        if (u !== 2)
                            break;
                        message.newsletterAdminProfileStatusMessage = $root.proto.Message.FutureProofMessage.decode(reader, reader.uint32(), undefined, q + 1, message.newsletterAdminProfileStatusMessage);
                        message._newsletterAdminProfileStatusMessage = "newsletterAdminProfileStatusMessage";
                        continue;
                    }
                case 127: {
                        if (u !== 2)
                            break;
                        message.rootSecretDistributeMessage = $root.proto.Message.RootSecretDistributeMessage.decode(reader, reader.uint32(), undefined, q + 1, message.rootSecretDistributeMessage);
                        message._rootSecretDistributeMessage = "rootSecretDistributeMessage";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a Message message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Message
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Message} Message
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Message.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Message message.
         * @function verify
         * @memberof proto.Message
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Message.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.conversation != null && Object.hasOwnProperty.call(message, "conversation")) {
                properties._conversation = 1;
                if (!$util.isString(message.conversation))
                    return "conversation: string expected";
            }
            if (message.senderKeyDistributionMessage != null && Object.hasOwnProperty.call(message, "senderKeyDistributionMessage")) {
                properties._senderKeyDistributionMessage = 1;
                {
                    var error = $root.proto.Message.SenderKeyDistributionMessage.verify(message.senderKeyDistributionMessage, q + 1);
                    if (error)
                        return "senderKeyDistributionMessage." + error;
                }
            }
            if (message.imageMessage != null && Object.hasOwnProperty.call(message, "imageMessage")) {
                properties._imageMessage = 1;
                {
                    var error = $root.proto.Message.ImageMessage.verify(message.imageMessage, q + 1);
                    if (error)
                        return "imageMessage." + error;
                }
            }
            if (message.contactMessage != null && Object.hasOwnProperty.call(message, "contactMessage")) {
                properties._contactMessage = 1;
                {
                    var error = $root.proto.Message.ContactMessage.verify(message.contactMessage, q + 1);
                    if (error)
                        return "contactMessage." + error;
                }
            }
            if (message.locationMessage != null && Object.hasOwnProperty.call(message, "locationMessage")) {
                properties._locationMessage = 1;
                {
                    var error = $root.proto.Message.LocationMessage.verify(message.locationMessage, q + 1);
                    if (error)
                        return "locationMessage." + error;
                }
            }
            if (message.extendedTextMessage != null && Object.hasOwnProperty.call(message, "extendedTextMessage")) {
                properties._extendedTextMessage = 1;
                {
                    var error = $root.proto.Message.ExtendedTextMessage.verify(message.extendedTextMessage, q + 1);
                    if (error)
                        return "extendedTextMessage." + error;
                }
            }
            if (message.documentMessage != null && Object.hasOwnProperty.call(message, "documentMessage")) {
                properties._documentMessage = 1;
                {
                    var error = $root.proto.Message.DocumentMessage.verify(message.documentMessage, q + 1);
                    if (error)
                        return "documentMessage." + error;
                }
            }
            if (message.audioMessage != null && Object.hasOwnProperty.call(message, "audioMessage")) {
                properties._audioMessage = 1;
                {
                    var error = $root.proto.Message.AudioMessage.verify(message.audioMessage, q + 1);
                    if (error)
                        return "audioMessage." + error;
                }
            }
            if (message.videoMessage != null && Object.hasOwnProperty.call(message, "videoMessage")) {
                properties._videoMessage = 1;
                {
                    var error = $root.proto.Message.VideoMessage.verify(message.videoMessage, q + 1);
                    if (error)
                        return "videoMessage." + error;
                }
            }
            if (message.call != null && Object.hasOwnProperty.call(message, "call")) {
                properties._call = 1;
                {
                    var error = $root.proto.Message.Call.verify(message.call, q + 1);
                    if (error)
                        return "call." + error;
                }
            }
            if (message.chat != null && Object.hasOwnProperty.call(message, "chat")) {
                properties._chat = 1;
                {
                    var error = $root.proto.Message.Chat.verify(message.chat, q + 1);
                    if (error)
                        return "chat." + error;
                }
            }
            if (message.protocolMessage != null && Object.hasOwnProperty.call(message, "protocolMessage")) {
                properties._protocolMessage = 1;
                {
                    var error = $root.proto.Message.ProtocolMessage.verify(message.protocolMessage, q + 1);
                    if (error)
                        return "protocolMessage." + error;
                }
            }
            if (message.contactsArrayMessage != null && Object.hasOwnProperty.call(message, "contactsArrayMessage")) {
                properties._contactsArrayMessage = 1;
                {
                    var error = $root.proto.Message.ContactsArrayMessage.verify(message.contactsArrayMessage, q + 1);
                    if (error)
                        return "contactsArrayMessage." + error;
                }
            }
            if (message.highlyStructuredMessage != null && Object.hasOwnProperty.call(message, "highlyStructuredMessage")) {
                properties._highlyStructuredMessage = 1;
                {
                    var error = $root.proto.Message.HighlyStructuredMessage.verify(message.highlyStructuredMessage, q + 1);
                    if (error)
                        return "highlyStructuredMessage." + error;
                }
            }
            if (message.fastRatchetKeySenderKeyDistributionMessage != null && Object.hasOwnProperty.call(message, "fastRatchetKeySenderKeyDistributionMessage")) {
                properties._fastRatchetKeySenderKeyDistributionMessage = 1;
                {
                    var error = $root.proto.Message.SenderKeyDistributionMessage.verify(message.fastRatchetKeySenderKeyDistributionMessage, q + 1);
                    if (error)
                        return "fastRatchetKeySenderKeyDistributionMessage." + error;
                }
            }
            if (message.sendPaymentMessage != null && Object.hasOwnProperty.call(message, "sendPaymentMessage")) {
                properties._sendPaymentMessage = 1;
                {
                    var error = $root.proto.Message.SendPaymentMessage.verify(message.sendPaymentMessage, q + 1);
                    if (error)
                        return "sendPaymentMessage." + error;
                }
            }
            if (message.liveLocationMessage != null && Object.hasOwnProperty.call(message, "liveLocationMessage")) {
                properties._liveLocationMessage = 1;
                {
                    var error = $root.proto.Message.LiveLocationMessage.verify(message.liveLocationMessage, q + 1);
                    if (error)
                        return "liveLocationMessage." + error;
                }
            }
            if (message.requestPaymentMessage != null && Object.hasOwnProperty.call(message, "requestPaymentMessage")) {
                properties._requestPaymentMessage = 1;
                {
                    var error = $root.proto.Message.RequestPaymentMessage.verify(message.requestPaymentMessage, q + 1);
                    if (error)
                        return "requestPaymentMessage." + error;
                }
            }
            if (message.declinePaymentRequestMessage != null && Object.hasOwnProperty.call(message, "declinePaymentRequestMessage")) {
                properties._declinePaymentRequestMessage = 1;
                {
                    var error = $root.proto.Message.DeclinePaymentRequestMessage.verify(message.declinePaymentRequestMessage, q + 1);
                    if (error)
                        return "declinePaymentRequestMessage." + error;
                }
            }
            if (message.cancelPaymentRequestMessage != null && Object.hasOwnProperty.call(message, "cancelPaymentRequestMessage")) {
                properties._cancelPaymentRequestMessage = 1;
                {
                    var error = $root.proto.Message.CancelPaymentRequestMessage.verify(message.cancelPaymentRequestMessage, q + 1);
                    if (error)
                        return "cancelPaymentRequestMessage." + error;
                }
            }
            if (message.templateMessage != null && Object.hasOwnProperty.call(message, "templateMessage")) {
                properties._templateMessage = 1;
                {
                    var error = $root.proto.Message.TemplateMessage.verify(message.templateMessage, q + 1);
                    if (error)
                        return "templateMessage." + error;
                }
            }
            if (message.stickerMessage != null && Object.hasOwnProperty.call(message, "stickerMessage")) {
                properties._stickerMessage = 1;
                {
                    var error = $root.proto.Message.StickerMessage.verify(message.stickerMessage, q + 1);
                    if (error)
                        return "stickerMessage." + error;
                }
            }
            if (message.groupInviteMessage != null && Object.hasOwnProperty.call(message, "groupInviteMessage")) {
                properties._groupInviteMessage = 1;
                {
                    var error = $root.proto.Message.GroupInviteMessage.verify(message.groupInviteMessage, q + 1);
                    if (error)
                        return "groupInviteMessage." + error;
                }
            }
            if (message.templateButtonReplyMessage != null && Object.hasOwnProperty.call(message, "templateButtonReplyMessage")) {
                properties._templateButtonReplyMessage = 1;
                {
                    var error = $root.proto.Message.TemplateButtonReplyMessage.verify(message.templateButtonReplyMessage, q + 1);
                    if (error)
                        return "templateButtonReplyMessage." + error;
                }
            }
            if (message.productMessage != null && Object.hasOwnProperty.call(message, "productMessage")) {
                properties._productMessage = 1;
                {
                    var error = $root.proto.Message.ProductMessage.verify(message.productMessage, q + 1);
                    if (error)
                        return "productMessage." + error;
                }
            }
            if (message.deviceSentMessage != null && Object.hasOwnProperty.call(message, "deviceSentMessage")) {
                properties._deviceSentMessage = 1;
                {
                    var error = $root.proto.Message.DeviceSentMessage.verify(message.deviceSentMessage, q + 1);
                    if (error)
                        return "deviceSentMessage." + error;
                }
            }
            if (message.messageContextInfo != null && Object.hasOwnProperty.call(message, "messageContextInfo")) {
                properties._messageContextInfo = 1;
                {
                    var error = $root.proto.MessageContextInfo.verify(message.messageContextInfo, q + 1);
                    if (error)
                        return "messageContextInfo." + error;
                }
            }
            if (message.listMessage != null && Object.hasOwnProperty.call(message, "listMessage")) {
                properties._listMessage = 1;
                {
                    var error = $root.proto.Message.ListMessage.verify(message.listMessage, q + 1);
                    if (error)
                        return "listMessage." + error;
                }
            }
            if (message.viewOnceMessage != null && Object.hasOwnProperty.call(message, "viewOnceMessage")) {
                properties._viewOnceMessage = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.viewOnceMessage, q + 1);
                    if (error)
                        return "viewOnceMessage." + error;
                }
            }
            if (message.orderMessage != null && Object.hasOwnProperty.call(message, "orderMessage")) {
                properties._orderMessage = 1;
                {
                    var error = $root.proto.Message.OrderMessage.verify(message.orderMessage, q + 1);
                    if (error)
                        return "orderMessage." + error;
                }
            }
            if (message.listResponseMessage != null && Object.hasOwnProperty.call(message, "listResponseMessage")) {
                properties._listResponseMessage = 1;
                {
                    var error = $root.proto.Message.ListResponseMessage.verify(message.listResponseMessage, q + 1);
                    if (error)
                        return "listResponseMessage." + error;
                }
            }
            if (message.ephemeralMessage != null && Object.hasOwnProperty.call(message, "ephemeralMessage")) {
                properties._ephemeralMessage = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.ephemeralMessage, q + 1);
                    if (error)
                        return "ephemeralMessage." + error;
                }
            }
            if (message.invoiceMessage != null && Object.hasOwnProperty.call(message, "invoiceMessage")) {
                properties._invoiceMessage = 1;
                {
                    var error = $root.proto.Message.InvoiceMessage.verify(message.invoiceMessage, q + 1);
                    if (error)
                        return "invoiceMessage." + error;
                }
            }
            if (message.buttonsMessage != null && Object.hasOwnProperty.call(message, "buttonsMessage")) {
                properties._buttonsMessage = 1;
                {
                    var error = $root.proto.Message.ButtonsMessage.verify(message.buttonsMessage, q + 1);
                    if (error)
                        return "buttonsMessage." + error;
                }
            }
            if (message.buttonsResponseMessage != null && Object.hasOwnProperty.call(message, "buttonsResponseMessage")) {
                properties._buttonsResponseMessage = 1;
                {
                    var error = $root.proto.Message.ButtonsResponseMessage.verify(message.buttonsResponseMessage, q + 1);
                    if (error)
                        return "buttonsResponseMessage." + error;
                }
            }
            if (message.paymentInviteMessage != null && Object.hasOwnProperty.call(message, "paymentInviteMessage")) {
                properties._paymentInviteMessage = 1;
                {
                    var error = $root.proto.Message.PaymentInviteMessage.verify(message.paymentInviteMessage, q + 1);
                    if (error)
                        return "paymentInviteMessage." + error;
                }
            }
            if (message.interactiveMessage != null && Object.hasOwnProperty.call(message, "interactiveMessage")) {
                properties._interactiveMessage = 1;
                {
                    var error = $root.proto.Message.InteractiveMessage.verify(message.interactiveMessage, q + 1);
                    if (error)
                        return "interactiveMessage." + error;
                }
            }
            if (message.reactionMessage != null && Object.hasOwnProperty.call(message, "reactionMessage")) {
                properties._reactionMessage = 1;
                {
                    var error = $root.proto.Message.ReactionMessage.verify(message.reactionMessage, q + 1);
                    if (error)
                        return "reactionMessage." + error;
                }
            }
            if (message.stickerSyncRmrMessage != null && Object.hasOwnProperty.call(message, "stickerSyncRmrMessage")) {
                properties._stickerSyncRmrMessage = 1;
                {
                    var error = $root.proto.Message.StickerSyncRMRMessage.verify(message.stickerSyncRmrMessage, q + 1);
                    if (error)
                        return "stickerSyncRmrMessage." + error;
                }
            }
            if (message.interactiveResponseMessage != null && Object.hasOwnProperty.call(message, "interactiveResponseMessage")) {
                properties._interactiveResponseMessage = 1;
                {
                    var error = $root.proto.Message.InteractiveResponseMessage.verify(message.interactiveResponseMessage, q + 1);
                    if (error)
                        return "interactiveResponseMessage." + error;
                }
            }
            if (message.pollCreationMessage != null && Object.hasOwnProperty.call(message, "pollCreationMessage")) {
                properties._pollCreationMessage = 1;
                {
                    var error = $root.proto.Message.PollCreationMessage.verify(message.pollCreationMessage, q + 1);
                    if (error)
                        return "pollCreationMessage." + error;
                }
            }
            if (message.pollUpdateMessage != null && Object.hasOwnProperty.call(message, "pollUpdateMessage")) {
                properties._pollUpdateMessage = 1;
                {
                    var error = $root.proto.Message.PollUpdateMessage.verify(message.pollUpdateMessage, q + 1);
                    if (error)
                        return "pollUpdateMessage." + error;
                }
            }
            if (message.keepInChatMessage != null && Object.hasOwnProperty.call(message, "keepInChatMessage")) {
                properties._keepInChatMessage = 1;
                {
                    var error = $root.proto.Message.KeepInChatMessage.verify(message.keepInChatMessage, q + 1);
                    if (error)
                        return "keepInChatMessage." + error;
                }
            }
            if (message.documentWithCaptionMessage != null && Object.hasOwnProperty.call(message, "documentWithCaptionMessage")) {
                properties._documentWithCaptionMessage = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.documentWithCaptionMessage, q + 1);
                    if (error)
                        return "documentWithCaptionMessage." + error;
                }
            }
            if (message.requestPhoneNumberMessage != null && Object.hasOwnProperty.call(message, "requestPhoneNumberMessage")) {
                properties._requestPhoneNumberMessage = 1;
                {
                    var error = $root.proto.Message.RequestPhoneNumberMessage.verify(message.requestPhoneNumberMessage, q + 1);
                    if (error)
                        return "requestPhoneNumberMessage." + error;
                }
            }
            if (message.viewOnceMessageV2 != null && Object.hasOwnProperty.call(message, "viewOnceMessageV2")) {
                properties._viewOnceMessageV2 = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.viewOnceMessageV2, q + 1);
                    if (error)
                        return "viewOnceMessageV2." + error;
                }
            }
            if (message.encReactionMessage != null && Object.hasOwnProperty.call(message, "encReactionMessage")) {
                properties._encReactionMessage = 1;
                {
                    var error = $root.proto.Message.EncReactionMessage.verify(message.encReactionMessage, q + 1);
                    if (error)
                        return "encReactionMessage." + error;
                }
            }
            if (message.editedMessage != null && Object.hasOwnProperty.call(message, "editedMessage")) {
                properties._editedMessage = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.editedMessage, q + 1);
                    if (error)
                        return "editedMessage." + error;
                }
            }
            if (message.viewOnceMessageV2Extension != null && Object.hasOwnProperty.call(message, "viewOnceMessageV2Extension")) {
                properties._viewOnceMessageV2Extension = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.viewOnceMessageV2Extension, q + 1);
                    if (error)
                        return "viewOnceMessageV2Extension." + error;
                }
            }
            if (message.pollCreationMessageV2 != null && Object.hasOwnProperty.call(message, "pollCreationMessageV2")) {
                properties._pollCreationMessageV2 = 1;
                {
                    var error = $root.proto.Message.PollCreationMessage.verify(message.pollCreationMessageV2, q + 1);
                    if (error)
                        return "pollCreationMessageV2." + error;
                }
            }
            if (message.scheduledCallCreationMessage != null && Object.hasOwnProperty.call(message, "scheduledCallCreationMessage")) {
                properties._scheduledCallCreationMessage = 1;
                {
                    var error = $root.proto.Message.ScheduledCallCreationMessage.verify(message.scheduledCallCreationMessage, q + 1);
                    if (error)
                        return "scheduledCallCreationMessage." + error;
                }
            }
            if (message.groupMentionedMessage != null && Object.hasOwnProperty.call(message, "groupMentionedMessage")) {
                properties._groupMentionedMessage = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.groupMentionedMessage, q + 1);
                    if (error)
                        return "groupMentionedMessage." + error;
                }
            }
            if (message.pinInChatMessage != null && Object.hasOwnProperty.call(message, "pinInChatMessage")) {
                properties._pinInChatMessage = 1;
                {
                    var error = $root.proto.Message.PinInChatMessage.verify(message.pinInChatMessage, q + 1);
                    if (error)
                        return "pinInChatMessage." + error;
                }
            }
            if (message.pollCreationMessageV3 != null && Object.hasOwnProperty.call(message, "pollCreationMessageV3")) {
                properties._pollCreationMessageV3 = 1;
                {
                    var error = $root.proto.Message.PollCreationMessage.verify(message.pollCreationMessageV3, q + 1);
                    if (error)
                        return "pollCreationMessageV3." + error;
                }
            }
            if (message.scheduledCallEditMessage != null && Object.hasOwnProperty.call(message, "scheduledCallEditMessage")) {
                properties._scheduledCallEditMessage = 1;
                {
                    var error = $root.proto.Message.ScheduledCallEditMessage.verify(message.scheduledCallEditMessage, q + 1);
                    if (error)
                        return "scheduledCallEditMessage." + error;
                }
            }
            if (message.ptvMessage != null && Object.hasOwnProperty.call(message, "ptvMessage")) {
                properties._ptvMessage = 1;
                {
                    var error = $root.proto.Message.VideoMessage.verify(message.ptvMessage, q + 1);
                    if (error)
                        return "ptvMessage." + error;
                }
            }
            if (message.botInvokeMessage != null && Object.hasOwnProperty.call(message, "botInvokeMessage")) {
                properties._botInvokeMessage = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.botInvokeMessage, q + 1);
                    if (error)
                        return "botInvokeMessage." + error;
                }
            }
            if (message.callLogMesssage != null && Object.hasOwnProperty.call(message, "callLogMesssage")) {
                properties._callLogMesssage = 1;
                {
                    var error = $root.proto.Message.CallLogMessage.verify(message.callLogMesssage, q + 1);
                    if (error)
                        return "callLogMesssage." + error;
                }
            }
            if (message.messageHistoryBundle != null && Object.hasOwnProperty.call(message, "messageHistoryBundle")) {
                properties._messageHistoryBundle = 1;
                {
                    var error = $root.proto.Message.MessageHistoryBundle.verify(message.messageHistoryBundle, q + 1);
                    if (error)
                        return "messageHistoryBundle." + error;
                }
            }
            if (message.encCommentMessage != null && Object.hasOwnProperty.call(message, "encCommentMessage")) {
                properties._encCommentMessage = 1;
                {
                    var error = $root.proto.Message.EncCommentMessage.verify(message.encCommentMessage, q + 1);
                    if (error)
                        return "encCommentMessage." + error;
                }
            }
            if (message.bcallMessage != null && Object.hasOwnProperty.call(message, "bcallMessage")) {
                properties._bcallMessage = 1;
                {
                    var error = $root.proto.Message.BCallMessage.verify(message.bcallMessage, q + 1);
                    if (error)
                        return "bcallMessage." + error;
                }
            }
            if (message.lottieStickerMessage != null && Object.hasOwnProperty.call(message, "lottieStickerMessage")) {
                properties._lottieStickerMessage = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.lottieStickerMessage, q + 1);
                    if (error)
                        return "lottieStickerMessage." + error;
                }
            }
            if (message.eventMessage != null && Object.hasOwnProperty.call(message, "eventMessage")) {
                properties._eventMessage = 1;
                {
                    var error = $root.proto.Message.EventMessage.verify(message.eventMessage, q + 1);
                    if (error)
                        return "eventMessage." + error;
                }
            }
            if (message.encEventResponseMessage != null && Object.hasOwnProperty.call(message, "encEventResponseMessage")) {
                properties._encEventResponseMessage = 1;
                {
                    var error = $root.proto.Message.EncEventResponseMessage.verify(message.encEventResponseMessage, q + 1);
                    if (error)
                        return "encEventResponseMessage." + error;
                }
            }
            if (message.commentMessage != null && Object.hasOwnProperty.call(message, "commentMessage")) {
                properties._commentMessage = 1;
                {
                    var error = $root.proto.Message.CommentMessage.verify(message.commentMessage, q + 1);
                    if (error)
                        return "commentMessage." + error;
                }
            }
            if (message.newsletterAdminInviteMessage != null && Object.hasOwnProperty.call(message, "newsletterAdminInviteMessage")) {
                properties._newsletterAdminInviteMessage = 1;
                {
                    var error = $root.proto.Message.NewsletterAdminInviteMessage.verify(message.newsletterAdminInviteMessage, q + 1);
                    if (error)
                        return "newsletterAdminInviteMessage." + error;
                }
            }
            if (message.placeholderMessage != null && Object.hasOwnProperty.call(message, "placeholderMessage")) {
                properties._placeholderMessage = 1;
                {
                    var error = $root.proto.Message.PlaceholderMessage.verify(message.placeholderMessage, q + 1);
                    if (error)
                        return "placeholderMessage." + error;
                }
            }
            if (message.secretEncryptedMessage != null && Object.hasOwnProperty.call(message, "secretEncryptedMessage")) {
                properties._secretEncryptedMessage = 1;
                {
                    var error = $root.proto.Message.SecretEncryptedMessage.verify(message.secretEncryptedMessage, q + 1);
                    if (error)
                        return "secretEncryptedMessage." + error;
                }
            }
            if (message.albumMessage != null && Object.hasOwnProperty.call(message, "albumMessage")) {
                properties._albumMessage = 1;
                {
                    var error = $root.proto.Message.AlbumMessage.verify(message.albumMessage, q + 1);
                    if (error)
                        return "albumMessage." + error;
                }
            }
            if (message.eventCoverImage != null && Object.hasOwnProperty.call(message, "eventCoverImage")) {
                properties._eventCoverImage = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.eventCoverImage, q + 1);
                    if (error)
                        return "eventCoverImage." + error;
                }
            }
            if (message.stickerPackMessage != null && Object.hasOwnProperty.call(message, "stickerPackMessage")) {
                properties._stickerPackMessage = 1;
                {
                    var error = $root.proto.Message.StickerPackMessage.verify(message.stickerPackMessage, q + 1);
                    if (error)
                        return "stickerPackMessage." + error;
                }
            }
            if (message.statusMentionMessage != null && Object.hasOwnProperty.call(message, "statusMentionMessage")) {
                properties._statusMentionMessage = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.statusMentionMessage, q + 1);
                    if (error)
                        return "statusMentionMessage." + error;
                }
            }
            if (message.pollResultSnapshotMessage != null && Object.hasOwnProperty.call(message, "pollResultSnapshotMessage")) {
                properties._pollResultSnapshotMessage = 1;
                {
                    var error = $root.proto.Message.PollResultSnapshotMessage.verify(message.pollResultSnapshotMessage, q + 1);
                    if (error)
                        return "pollResultSnapshotMessage." + error;
                }
            }
            if (message.pollCreationOptionImageMessage != null && Object.hasOwnProperty.call(message, "pollCreationOptionImageMessage")) {
                properties._pollCreationOptionImageMessage = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.pollCreationOptionImageMessage, q + 1);
                    if (error)
                        return "pollCreationOptionImageMessage." + error;
                }
            }
            if (message.associatedChildMessage != null && Object.hasOwnProperty.call(message, "associatedChildMessage")) {
                properties._associatedChildMessage = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.associatedChildMessage, q + 1);
                    if (error)
                        return "associatedChildMessage." + error;
                }
            }
            if (message.groupStatusMentionMessage != null && Object.hasOwnProperty.call(message, "groupStatusMentionMessage")) {
                properties._groupStatusMentionMessage = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.groupStatusMentionMessage, q + 1);
                    if (error)
                        return "groupStatusMentionMessage." + error;
                }
            }
            if (message.pollCreationMessageV4 != null && Object.hasOwnProperty.call(message, "pollCreationMessageV4")) {
                properties._pollCreationMessageV4 = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.pollCreationMessageV4, q + 1);
                    if (error)
                        return "pollCreationMessageV4." + error;
                }
            }
            if (message.statusAddYours != null && Object.hasOwnProperty.call(message, "statusAddYours")) {
                properties._statusAddYours = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.statusAddYours, q + 1);
                    if (error)
                        return "statusAddYours." + error;
                }
            }
            if (message.groupStatusMessage != null && Object.hasOwnProperty.call(message, "groupStatusMessage")) {
                properties._groupStatusMessage = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.groupStatusMessage, q + 1);
                    if (error)
                        return "groupStatusMessage." + error;
                }
            }
            if (message.richResponseMessage != null && Object.hasOwnProperty.call(message, "richResponseMessage")) {
                properties._richResponseMessage = 1;
                {
                    var error = $root.proto.AIRichResponseMessage.verify(message.richResponseMessage, q + 1);
                    if (error)
                        return "richResponseMessage." + error;
                }
            }
            if (message.statusNotificationMessage != null && Object.hasOwnProperty.call(message, "statusNotificationMessage")) {
                properties._statusNotificationMessage = 1;
                {
                    var error = $root.proto.Message.StatusNotificationMessage.verify(message.statusNotificationMessage, q + 1);
                    if (error)
                        return "statusNotificationMessage." + error;
                }
            }
            if (message.limitSharingMessage != null && Object.hasOwnProperty.call(message, "limitSharingMessage")) {
                properties._limitSharingMessage = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.limitSharingMessage, q + 1);
                    if (error)
                        return "limitSharingMessage." + error;
                }
            }
            if (message.botTaskMessage != null && Object.hasOwnProperty.call(message, "botTaskMessage")) {
                properties._botTaskMessage = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.botTaskMessage, q + 1);
                    if (error)
                        return "botTaskMessage." + error;
                }
            }
            if (message.questionMessage != null && Object.hasOwnProperty.call(message, "questionMessage")) {
                properties._questionMessage = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.questionMessage, q + 1);
                    if (error)
                        return "questionMessage." + error;
                }
            }
            if (message.messageHistoryNotice != null && Object.hasOwnProperty.call(message, "messageHistoryNotice")) {
                properties._messageHistoryNotice = 1;
                {
                    var error = $root.proto.Message.MessageHistoryNotice.verify(message.messageHistoryNotice, q + 1);
                    if (error)
                        return "messageHistoryNotice." + error;
                }
            }
            if (message.groupStatusMessageV2 != null && Object.hasOwnProperty.call(message, "groupStatusMessageV2")) {
                properties._groupStatusMessageV2 = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.groupStatusMessageV2, q + 1);
                    if (error)
                        return "groupStatusMessageV2." + error;
                }
            }
            if (message.botForwardedMessage != null && Object.hasOwnProperty.call(message, "botForwardedMessage")) {
                properties._botForwardedMessage = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.botForwardedMessage, q + 1);
                    if (error)
                        return "botForwardedMessage." + error;
                }
            }
            if (message.statusQuestionAnswerMessage != null && Object.hasOwnProperty.call(message, "statusQuestionAnswerMessage")) {
                properties._statusQuestionAnswerMessage = 1;
                {
                    var error = $root.proto.Message.StatusQuestionAnswerMessage.verify(message.statusQuestionAnswerMessage, q + 1);
                    if (error)
                        return "statusQuestionAnswerMessage." + error;
                }
            }
            if (message.questionReplyMessage != null && Object.hasOwnProperty.call(message, "questionReplyMessage")) {
                properties._questionReplyMessage = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.questionReplyMessage, q + 1);
                    if (error)
                        return "questionReplyMessage." + error;
                }
            }
            if (message.questionResponseMessage != null && Object.hasOwnProperty.call(message, "questionResponseMessage")) {
                properties._questionResponseMessage = 1;
                {
                    var error = $root.proto.Message.QuestionResponseMessage.verify(message.questionResponseMessage, q + 1);
                    if (error)
                        return "questionResponseMessage." + error;
                }
            }
            if (message.statusQuotedMessage != null && Object.hasOwnProperty.call(message, "statusQuotedMessage")) {
                properties._statusQuotedMessage = 1;
                {
                    var error = $root.proto.Message.StatusQuotedMessage.verify(message.statusQuotedMessage, q + 1);
                    if (error)
                        return "statusQuotedMessage." + error;
                }
            }
            if (message.statusStickerInteractionMessage != null && Object.hasOwnProperty.call(message, "statusStickerInteractionMessage")) {
                properties._statusStickerInteractionMessage = 1;
                {
                    var error = $root.proto.Message.StatusStickerInteractionMessage.verify(message.statusStickerInteractionMessage, q + 1);
                    if (error)
                        return "statusStickerInteractionMessage." + error;
                }
            }
            if (message.pollCreationMessageV5 != null && Object.hasOwnProperty.call(message, "pollCreationMessageV5")) {
                properties._pollCreationMessageV5 = 1;
                {
                    var error = $root.proto.Message.PollCreationMessage.verify(message.pollCreationMessageV5, q + 1);
                    if (error)
                        return "pollCreationMessageV5." + error;
                }
            }
            if (message.newsletterFollowerInviteMessageV2 != null && Object.hasOwnProperty.call(message, "newsletterFollowerInviteMessageV2")) {
                properties._newsletterFollowerInviteMessageV2 = 1;
                {
                    var error = $root.proto.Message.NewsletterFollowerInviteMessage.verify(message.newsletterFollowerInviteMessageV2, q + 1);
                    if (error)
                        return "newsletterFollowerInviteMessageV2." + error;
                }
            }
            if (message.pollResultSnapshotMessageV3 != null && Object.hasOwnProperty.call(message, "pollResultSnapshotMessageV3")) {
                properties._pollResultSnapshotMessageV3 = 1;
                {
                    var error = $root.proto.Message.PollResultSnapshotMessage.verify(message.pollResultSnapshotMessageV3, q + 1);
                    if (error)
                        return "pollResultSnapshotMessageV3." + error;
                }
            }
            if (message.newsletterAdminProfileMessage != null && Object.hasOwnProperty.call(message, "newsletterAdminProfileMessage")) {
                properties._newsletterAdminProfileMessage = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.newsletterAdminProfileMessage, q + 1);
                    if (error)
                        return "newsletterAdminProfileMessage." + error;
                }
            }
            if (message.newsletterAdminProfileMessageV2 != null && Object.hasOwnProperty.call(message, "newsletterAdminProfileMessageV2")) {
                properties._newsletterAdminProfileMessageV2 = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.newsletterAdminProfileMessageV2, q + 1);
                    if (error)
                        return "newsletterAdminProfileMessageV2." + error;
                }
            }
            if (message.spoilerMessage != null && Object.hasOwnProperty.call(message, "spoilerMessage")) {
                properties._spoilerMessage = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.spoilerMessage, q + 1);
                    if (error)
                        return "spoilerMessage." + error;
                }
            }
            if (message.pollCreationMessageV6 != null && Object.hasOwnProperty.call(message, "pollCreationMessageV6")) {
                properties._pollCreationMessageV6 = 1;
                {
                    var error = $root.proto.Message.PollCreationMessage.verify(message.pollCreationMessageV6, q + 1);
                    if (error)
                        return "pollCreationMessageV6." + error;
                }
            }
            if (message.conditionalRevealMessage != null && Object.hasOwnProperty.call(message, "conditionalRevealMessage")) {
                properties._conditionalRevealMessage = 1;
                {
                    var error = $root.proto.Message.ConditionalRevealMessage.verify(message.conditionalRevealMessage, q + 1);
                    if (error)
                        return "conditionalRevealMessage." + error;
                }
            }
            if (message.pollAddOptionMessage != null && Object.hasOwnProperty.call(message, "pollAddOptionMessage")) {
                properties._pollAddOptionMessage = 1;
                {
                    var error = $root.proto.Message.PollAddOptionMessage.verify(message.pollAddOptionMessage, q + 1);
                    if (error)
                        return "pollAddOptionMessage." + error;
                }
            }
            if (message.eventInviteMessage != null && Object.hasOwnProperty.call(message, "eventInviteMessage")) {
                properties._eventInviteMessage = 1;
                {
                    var error = $root.proto.Message.EventInviteMessage.verify(message.eventInviteMessage, q + 1);
                    if (error)
                        return "eventInviteMessage." + error;
                }
            }
            if (message.groupRootKeyShare != null && Object.hasOwnProperty.call(message, "groupRootKeyShare")) {
                properties._groupRootKeyShare = 1;
                {
                    var error = $root.proto.GroupRootKeyShare.verify(message.groupRootKeyShare, q + 1);
                    if (error)
                        return "groupRootKeyShare." + error;
                }
            }
            if (message.paymentReminderMessage != null && Object.hasOwnProperty.call(message, "paymentReminderMessage")) {
                properties._paymentReminderMessage = 1;
                {
                    var error = $root.proto.Message.PaymentReminderMessage.verify(message.paymentReminderMessage, q + 1);
                    if (error)
                        return "paymentReminderMessage." + error;
                }
            }
            if (message.splitPaymentMessage != null && Object.hasOwnProperty.call(message, "splitPaymentMessage")) {
                properties._splitPaymentMessage = 1;
                {
                    var error = $root.proto.Message.SplitPaymentMessage.verify(message.splitPaymentMessage, q + 1);
                    if (error)
                        return "splitPaymentMessage." + error;
                }
            }
            if (message.newsletterAdminProfileStatusMessage != null && Object.hasOwnProperty.call(message, "newsletterAdminProfileStatusMessage")) {
                properties._newsletterAdminProfileStatusMessage = 1;
                {
                    var error = $root.proto.Message.FutureProofMessage.verify(message.newsletterAdminProfileStatusMessage, q + 1);
                    if (error)
                        return "newsletterAdminProfileStatusMessage." + error;
                }
            }
            if (message.rootSecretDistributeMessage != null && Object.hasOwnProperty.call(message, "rootSecretDistributeMessage")) {
                properties._rootSecretDistributeMessage = 1;
                {
                    var error = $root.proto.Message.RootSecretDistributeMessage.verify(message.rootSecretDistributeMessage, q + 1);
                    if (error)
                        return "rootSecretDistributeMessage." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Message message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Message
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Message} Message
         */
        Message.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.Message: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.conversation != null)
                message.conversation = String(object.conversation);
            if (object.senderKeyDistributionMessage != null) {
                if (!$util.isObject(object.senderKeyDistributionMessage))
                    throw TypeError(".proto.Message.senderKeyDistributionMessage: object expected");
                message.senderKeyDistributionMessage = $root.proto.Message.SenderKeyDistributionMessage.fromObject(object.senderKeyDistributionMessage, q + 1);
            }
            if (object.imageMessage != null) {
                if (!$util.isObject(object.imageMessage))
                    throw TypeError(".proto.Message.imageMessage: object expected");
                message.imageMessage = $root.proto.Message.ImageMessage.fromObject(object.imageMessage, q + 1);
            }
            if (object.contactMessage != null) {
                if (!$util.isObject(object.contactMessage))
                    throw TypeError(".proto.Message.contactMessage: object expected");
                message.contactMessage = $root.proto.Message.ContactMessage.fromObject(object.contactMessage, q + 1);
            }
            if (object.locationMessage != null) {
                if (!$util.isObject(object.locationMessage))
                    throw TypeError(".proto.Message.locationMessage: object expected");
                message.locationMessage = $root.proto.Message.LocationMessage.fromObject(object.locationMessage, q + 1);
            }
            if (object.extendedTextMessage != null) {
                if (!$util.isObject(object.extendedTextMessage))
                    throw TypeError(".proto.Message.extendedTextMessage: object expected");
                message.extendedTextMessage = $root.proto.Message.ExtendedTextMessage.fromObject(object.extendedTextMessage, q + 1);
            }
            if (object.documentMessage != null) {
                if (!$util.isObject(object.documentMessage))
                    throw TypeError(".proto.Message.documentMessage: object expected");
                message.documentMessage = $root.proto.Message.DocumentMessage.fromObject(object.documentMessage, q + 1);
            }
            if (object.audioMessage != null) {
                if (!$util.isObject(object.audioMessage))
                    throw TypeError(".proto.Message.audioMessage: object expected");
                message.audioMessage = $root.proto.Message.AudioMessage.fromObject(object.audioMessage, q + 1);
            }
            if (object.videoMessage != null) {
                if (!$util.isObject(object.videoMessage))
                    throw TypeError(".proto.Message.videoMessage: object expected");
                message.videoMessage = $root.proto.Message.VideoMessage.fromObject(object.videoMessage, q + 1);
            }
            if (object.call != null) {
                if (!$util.isObject(object.call))
                    throw TypeError(".proto.Message.call: object expected");
                message.call = $root.proto.Message.Call.fromObject(object.call, q + 1);
            }
            if (object.chat != null) {
                if (!$util.isObject(object.chat))
                    throw TypeError(".proto.Message.chat: object expected");
                message.chat = $root.proto.Message.Chat.fromObject(object.chat, q + 1);
            }
            if (object.protocolMessage != null) {
                if (!$util.isObject(object.protocolMessage))
                    throw TypeError(".proto.Message.protocolMessage: object expected");
                message.protocolMessage = $root.proto.Message.ProtocolMessage.fromObject(object.protocolMessage, q + 1);
            }
            if (object.contactsArrayMessage != null) {
                if (!$util.isObject(object.contactsArrayMessage))
                    throw TypeError(".proto.Message.contactsArrayMessage: object expected");
                message.contactsArrayMessage = $root.proto.Message.ContactsArrayMessage.fromObject(object.contactsArrayMessage, q + 1);
            }
            if (object.highlyStructuredMessage != null) {
                if (!$util.isObject(object.highlyStructuredMessage))
                    throw TypeError(".proto.Message.highlyStructuredMessage: object expected");
                message.highlyStructuredMessage = $root.proto.Message.HighlyStructuredMessage.fromObject(object.highlyStructuredMessage, q + 1);
            }
            if (object.fastRatchetKeySenderKeyDistributionMessage != null) {
                if (!$util.isObject(object.fastRatchetKeySenderKeyDistributionMessage))
                    throw TypeError(".proto.Message.fastRatchetKeySenderKeyDistributionMessage: object expected");
                message.fastRatchetKeySenderKeyDistributionMessage = $root.proto.Message.SenderKeyDistributionMessage.fromObject(object.fastRatchetKeySenderKeyDistributionMessage, q + 1);
            }
            if (object.sendPaymentMessage != null) {
                if (!$util.isObject(object.sendPaymentMessage))
                    throw TypeError(".proto.Message.sendPaymentMessage: object expected");
                message.sendPaymentMessage = $root.proto.Message.SendPaymentMessage.fromObject(object.sendPaymentMessage, q + 1);
            }
            if (object.liveLocationMessage != null) {
                if (!$util.isObject(object.liveLocationMessage))
                    throw TypeError(".proto.Message.liveLocationMessage: object expected");
                message.liveLocationMessage = $root.proto.Message.LiveLocationMessage.fromObject(object.liveLocationMessage, q + 1);
            }
            if (object.requestPaymentMessage != null) {
                if (!$util.isObject(object.requestPaymentMessage))
                    throw TypeError(".proto.Message.requestPaymentMessage: object expected");
                message.requestPaymentMessage = $root.proto.Message.RequestPaymentMessage.fromObject(object.requestPaymentMessage, q + 1);
            }
            if (object.declinePaymentRequestMessage != null) {
                if (!$util.isObject(object.declinePaymentRequestMessage))
                    throw TypeError(".proto.Message.declinePaymentRequestMessage: object expected");
                message.declinePaymentRequestMessage = $root.proto.Message.DeclinePaymentRequestMessage.fromObject(object.declinePaymentRequestMessage, q + 1);
            }
            if (object.cancelPaymentRequestMessage != null) {
                if (!$util.isObject(object.cancelPaymentRequestMessage))
                    throw TypeError(".proto.Message.cancelPaymentRequestMessage: object expected");
                message.cancelPaymentRequestMessage = $root.proto.Message.CancelPaymentRequestMessage.fromObject(object.cancelPaymentRequestMessage, q + 1);
            }
            if (object.templateMessage != null) {
                if (!$util.isObject(object.templateMessage))
                    throw TypeError(".proto.Message.templateMessage: object expected");
                message.templateMessage = $root.proto.Message.TemplateMessage.fromObject(object.templateMessage, q + 1);
            }
            if (object.stickerMessage != null) {
                if (!$util.isObject(object.stickerMessage))
                    throw TypeError(".proto.Message.stickerMessage: object expected");
                message.stickerMessage = $root.proto.Message.StickerMessage.fromObject(object.stickerMessage, q + 1);
            }
            if (object.groupInviteMessage != null) {
                if (!$util.isObject(object.groupInviteMessage))
                    throw TypeError(".proto.Message.groupInviteMessage: object expected");
                message.groupInviteMessage = $root.proto.Message.GroupInviteMessage.fromObject(object.groupInviteMessage, q + 1);
            }
            if (object.templateButtonReplyMessage != null) {
                if (!$util.isObject(object.templateButtonReplyMessage))
                    throw TypeError(".proto.Message.templateButtonReplyMessage: object expected");
                message.templateButtonReplyMessage = $root.proto.Message.TemplateButtonReplyMessage.fromObject(object.templateButtonReplyMessage, q + 1);
            }
            if (object.productMessage != null) {
                if (!$util.isObject(object.productMessage))
                    throw TypeError(".proto.Message.productMessage: object expected");
                message.productMessage = $root.proto.Message.ProductMessage.fromObject(object.productMessage, q + 1);
            }
            if (object.deviceSentMessage != null) {
                if (!$util.isObject(object.deviceSentMessage))
                    throw TypeError(".proto.Message.deviceSentMessage: object expected");
                message.deviceSentMessage = $root.proto.Message.DeviceSentMessage.fromObject(object.deviceSentMessage, q + 1);
            }
            if (object.messageContextInfo != null) {
                if (!$util.isObject(object.messageContextInfo))
                    throw TypeError(".proto.Message.messageContextInfo: object expected");
                message.messageContextInfo = $root.proto.MessageContextInfo.fromObject(object.messageContextInfo, q + 1);
            }
            if (object.listMessage != null) {
                if (!$util.isObject(object.listMessage))
                    throw TypeError(".proto.Message.listMessage: object expected");
                message.listMessage = $root.proto.Message.ListMessage.fromObject(object.listMessage, q + 1);
            }
            if (object.viewOnceMessage != null) {
                if (!$util.isObject(object.viewOnceMessage))
                    throw TypeError(".proto.Message.viewOnceMessage: object expected");
                message.viewOnceMessage = $root.proto.Message.FutureProofMessage.fromObject(object.viewOnceMessage, q + 1);
            }
            if (object.orderMessage != null) {
                if (!$util.isObject(object.orderMessage))
                    throw TypeError(".proto.Message.orderMessage: object expected");
                message.orderMessage = $root.proto.Message.OrderMessage.fromObject(object.orderMessage, q + 1);
            }
            if (object.listResponseMessage != null) {
                if (!$util.isObject(object.listResponseMessage))
                    throw TypeError(".proto.Message.listResponseMessage: object expected");
                message.listResponseMessage = $root.proto.Message.ListResponseMessage.fromObject(object.listResponseMessage, q + 1);
            }
            if (object.ephemeralMessage != null) {
                if (!$util.isObject(object.ephemeralMessage))
                    throw TypeError(".proto.Message.ephemeralMessage: object expected");
                message.ephemeralMessage = $root.proto.Message.FutureProofMessage.fromObject(object.ephemeralMessage, q + 1);
            }
            if (object.invoiceMessage != null) {
                if (!$util.isObject(object.invoiceMessage))
                    throw TypeError(".proto.Message.invoiceMessage: object expected");
                message.invoiceMessage = $root.proto.Message.InvoiceMessage.fromObject(object.invoiceMessage, q + 1);
            }
            if (object.buttonsMessage != null) {
                if (!$util.isObject(object.buttonsMessage))
                    throw TypeError(".proto.Message.buttonsMessage: object expected");
                message.buttonsMessage = $root.proto.Message.ButtonsMessage.fromObject(object.buttonsMessage, q + 1);
            }
            if (object.buttonsResponseMessage != null) {
                if (!$util.isObject(object.buttonsResponseMessage))
                    throw TypeError(".proto.Message.buttonsResponseMessage: object expected");
                message.buttonsResponseMessage = $root.proto.Message.ButtonsResponseMessage.fromObject(object.buttonsResponseMessage, q + 1);
            }
            if (object.paymentInviteMessage != null) {
                if (!$util.isObject(object.paymentInviteMessage))
                    throw TypeError(".proto.Message.paymentInviteMessage: object expected");
                message.paymentInviteMessage = $root.proto.Message.PaymentInviteMessage.fromObject(object.paymentInviteMessage, q + 1);
            }
            if (object.interactiveMessage != null) {
                if (!$util.isObject(object.interactiveMessage))
                    throw TypeError(".proto.Message.interactiveMessage: object expected");
                message.interactiveMessage = $root.proto.Message.InteractiveMessage.fromObject(object.interactiveMessage, q + 1);
            }
            if (object.reactionMessage != null) {
                if (!$util.isObject(object.reactionMessage))
                    throw TypeError(".proto.Message.reactionMessage: object expected");
                message.reactionMessage = $root.proto.Message.ReactionMessage.fromObject(object.reactionMessage, q + 1);
            }
            if (object.stickerSyncRmrMessage != null) {
                if (!$util.isObject(object.stickerSyncRmrMessage))
                    throw TypeError(".proto.Message.stickerSyncRmrMessage: object expected");
                message.stickerSyncRmrMessage = $root.proto.Message.StickerSyncRMRMessage.fromObject(object.stickerSyncRmrMessage, q + 1);
            }
            if (object.interactiveResponseMessage != null) {
                if (!$util.isObject(object.interactiveResponseMessage))
                    throw TypeError(".proto.Message.interactiveResponseMessage: object expected");
                message.interactiveResponseMessage = $root.proto.Message.InteractiveResponseMessage.fromObject(object.interactiveResponseMessage, q + 1);
            }
            if (object.pollCreationMessage != null) {
                if (!$util.isObject(object.pollCreationMessage))
                    throw TypeError(".proto.Message.pollCreationMessage: object expected");
                message.pollCreationMessage = $root.proto.Message.PollCreationMessage.fromObject(object.pollCreationMessage, q + 1);
            }
            if (object.pollUpdateMessage != null) {
                if (!$util.isObject(object.pollUpdateMessage))
                    throw TypeError(".proto.Message.pollUpdateMessage: object expected");
                message.pollUpdateMessage = $root.proto.Message.PollUpdateMessage.fromObject(object.pollUpdateMessage, q + 1);
            }
            if (object.keepInChatMessage != null) {
                if (!$util.isObject(object.keepInChatMessage))
                    throw TypeError(".proto.Message.keepInChatMessage: object expected");
                message.keepInChatMessage = $root.proto.Message.KeepInChatMessage.fromObject(object.keepInChatMessage, q + 1);
            }
            if (object.documentWithCaptionMessage != null) {
                if (!$util.isObject(object.documentWithCaptionMessage))
                    throw TypeError(".proto.Message.documentWithCaptionMessage: object expected");
                message.documentWithCaptionMessage = $root.proto.Message.FutureProofMessage.fromObject(object.documentWithCaptionMessage, q + 1);
            }
            if (object.requestPhoneNumberMessage != null) {
                if (!$util.isObject(object.requestPhoneNumberMessage))
                    throw TypeError(".proto.Message.requestPhoneNumberMessage: object expected");
                message.requestPhoneNumberMessage = $root.proto.Message.RequestPhoneNumberMessage.fromObject(object.requestPhoneNumberMessage, q + 1);
            }
            if (object.viewOnceMessageV2 != null) {
                if (!$util.isObject(object.viewOnceMessageV2))
                    throw TypeError(".proto.Message.viewOnceMessageV2: object expected");
                message.viewOnceMessageV2 = $root.proto.Message.FutureProofMessage.fromObject(object.viewOnceMessageV2, q + 1);
            }
            if (object.encReactionMessage != null) {
                if (!$util.isObject(object.encReactionMessage))
                    throw TypeError(".proto.Message.encReactionMessage: object expected");
                message.encReactionMessage = $root.proto.Message.EncReactionMessage.fromObject(object.encReactionMessage, q + 1);
            }
            if (object.editedMessage != null) {
                if (!$util.isObject(object.editedMessage))
                    throw TypeError(".proto.Message.editedMessage: object expected");
                message.editedMessage = $root.proto.Message.FutureProofMessage.fromObject(object.editedMessage, q + 1);
            }
            if (object.viewOnceMessageV2Extension != null) {
                if (!$util.isObject(object.viewOnceMessageV2Extension))
                    throw TypeError(".proto.Message.viewOnceMessageV2Extension: object expected");
                message.viewOnceMessageV2Extension = $root.proto.Message.FutureProofMessage.fromObject(object.viewOnceMessageV2Extension, q + 1);
            }
            if (object.pollCreationMessageV2 != null) {
                if (!$util.isObject(object.pollCreationMessageV2))
                    throw TypeError(".proto.Message.pollCreationMessageV2: object expected");
                message.pollCreationMessageV2 = $root.proto.Message.PollCreationMessage.fromObject(object.pollCreationMessageV2, q + 1);
            }
            if (object.scheduledCallCreationMessage != null) {
                if (!$util.isObject(object.scheduledCallCreationMessage))
                    throw TypeError(".proto.Message.scheduledCallCreationMessage: object expected");
                message.scheduledCallCreationMessage = $root.proto.Message.ScheduledCallCreationMessage.fromObject(object.scheduledCallCreationMessage, q + 1);
            }
            if (object.groupMentionedMessage != null) {
                if (!$util.isObject(object.groupMentionedMessage))
                    throw TypeError(".proto.Message.groupMentionedMessage: object expected");
                message.groupMentionedMessage = $root.proto.Message.FutureProofMessage.fromObject(object.groupMentionedMessage, q + 1);
            }
            if (object.pinInChatMessage != null) {
                if (!$util.isObject(object.pinInChatMessage))
                    throw TypeError(".proto.Message.pinInChatMessage: object expected");
                message.pinInChatMessage = $root.proto.Message.PinInChatMessage.fromObject(object.pinInChatMessage, q + 1);
            }
            if (object.pollCreationMessageV3 != null) {
                if (!$util.isObject(object.pollCreationMessageV3))
                    throw TypeError(".proto.Message.pollCreationMessageV3: object expected");
                message.pollCreationMessageV3 = $root.proto.Message.PollCreationMessage.fromObject(object.pollCreationMessageV3, q + 1);
            }
            if (object.scheduledCallEditMessage != null) {
                if (!$util.isObject(object.scheduledCallEditMessage))
                    throw TypeError(".proto.Message.scheduledCallEditMessage: object expected");
                message.scheduledCallEditMessage = $root.proto.Message.ScheduledCallEditMessage.fromObject(object.scheduledCallEditMessage, q + 1);
            }
            if (object.ptvMessage != null) {
                if (!$util.isObject(object.ptvMessage))
                    throw TypeError(".proto.Message.ptvMessage: object expected");
                message.ptvMessage = $root.proto.Message.VideoMessage.fromObject(object.ptvMessage, q + 1);
            }
            if (object.botInvokeMessage != null) {
                if (!$util.isObject(object.botInvokeMessage))
                    throw TypeError(".proto.Message.botInvokeMessage: object expected");
                message.botInvokeMessage = $root.proto.Message.FutureProofMessage.fromObject(object.botInvokeMessage, q + 1);
            }
            if (object.callLogMesssage != null) {
                if (!$util.isObject(object.callLogMesssage))
                    throw TypeError(".proto.Message.callLogMesssage: object expected");
                message.callLogMesssage = $root.proto.Message.CallLogMessage.fromObject(object.callLogMesssage, q + 1);
            }
            if (object.messageHistoryBundle != null) {
                if (!$util.isObject(object.messageHistoryBundle))
                    throw TypeError(".proto.Message.messageHistoryBundle: object expected");
                message.messageHistoryBundle = $root.proto.Message.MessageHistoryBundle.fromObject(object.messageHistoryBundle, q + 1);
            }
            if (object.encCommentMessage != null) {
                if (!$util.isObject(object.encCommentMessage))
                    throw TypeError(".proto.Message.encCommentMessage: object expected");
                message.encCommentMessage = $root.proto.Message.EncCommentMessage.fromObject(object.encCommentMessage, q + 1);
            }
            if (object.bcallMessage != null) {
                if (!$util.isObject(object.bcallMessage))
                    throw TypeError(".proto.Message.bcallMessage: object expected");
                message.bcallMessage = $root.proto.Message.BCallMessage.fromObject(object.bcallMessage, q + 1);
            }
            if (object.lottieStickerMessage != null) {
                if (!$util.isObject(object.lottieStickerMessage))
                    throw TypeError(".proto.Message.lottieStickerMessage: object expected");
                message.lottieStickerMessage = $root.proto.Message.FutureProofMessage.fromObject(object.lottieStickerMessage, q + 1);
            }
            if (object.eventMessage != null) {
                if (!$util.isObject(object.eventMessage))
                    throw TypeError(".proto.Message.eventMessage: object expected");
                message.eventMessage = $root.proto.Message.EventMessage.fromObject(object.eventMessage, q + 1);
            }
            if (object.encEventResponseMessage != null) {
                if (!$util.isObject(object.encEventResponseMessage))
                    throw TypeError(".proto.Message.encEventResponseMessage: object expected");
                message.encEventResponseMessage = $root.proto.Message.EncEventResponseMessage.fromObject(object.encEventResponseMessage, q + 1);
            }
            if (object.commentMessage != null) {
                if (!$util.isObject(object.commentMessage))
                    throw TypeError(".proto.Message.commentMessage: object expected");
                message.commentMessage = $root.proto.Message.CommentMessage.fromObject(object.commentMessage, q + 1);
            }
            if (object.newsletterAdminInviteMessage != null) {
                if (!$util.isObject(object.newsletterAdminInviteMessage))
                    throw TypeError(".proto.Message.newsletterAdminInviteMessage: object expected");
                message.newsletterAdminInviteMessage = $root.proto.Message.NewsletterAdminInviteMessage.fromObject(object.newsletterAdminInviteMessage, q + 1);
            }
            if (object.placeholderMessage != null) {
                if (!$util.isObject(object.placeholderMessage))
                    throw TypeError(".proto.Message.placeholderMessage: object expected");
                message.placeholderMessage = $root.proto.Message.PlaceholderMessage.fromObject(object.placeholderMessage, q + 1);
            }
            if (object.secretEncryptedMessage != null) {
                if (!$util.isObject(object.secretEncryptedMessage))
                    throw TypeError(".proto.Message.secretEncryptedMessage: object expected");
                message.secretEncryptedMessage = $root.proto.Message.SecretEncryptedMessage.fromObject(object.secretEncryptedMessage, q + 1);
            }
            if (object.albumMessage != null) {
                if (!$util.isObject(object.albumMessage))
                    throw TypeError(".proto.Message.albumMessage: object expected");
                message.albumMessage = $root.proto.Message.AlbumMessage.fromObject(object.albumMessage, q + 1);
            }
            if (object.eventCoverImage != null) {
                if (!$util.isObject(object.eventCoverImage))
                    throw TypeError(".proto.Message.eventCoverImage: object expected");
                message.eventCoverImage = $root.proto.Message.FutureProofMessage.fromObject(object.eventCoverImage, q + 1);
            }
            if (object.stickerPackMessage != null) {
                if (!$util.isObject(object.stickerPackMessage))
                    throw TypeError(".proto.Message.stickerPackMessage: object expected");
                message.stickerPackMessage = $root.proto.Message.StickerPackMessage.fromObject(object.stickerPackMessage, q + 1);
            }
            if (object.statusMentionMessage != null) {
                if (!$util.isObject(object.statusMentionMessage))
                    throw TypeError(".proto.Message.statusMentionMessage: object expected");
                message.statusMentionMessage = $root.proto.Message.FutureProofMessage.fromObject(object.statusMentionMessage, q + 1);
            }
            if (object.pollResultSnapshotMessage != null) {
                if (!$util.isObject(object.pollResultSnapshotMessage))
                    throw TypeError(".proto.Message.pollResultSnapshotMessage: object expected");
                message.pollResultSnapshotMessage = $root.proto.Message.PollResultSnapshotMessage.fromObject(object.pollResultSnapshotMessage, q + 1);
            }
            if (object.pollCreationOptionImageMessage != null) {
                if (!$util.isObject(object.pollCreationOptionImageMessage))
                    throw TypeError(".proto.Message.pollCreationOptionImageMessage: object expected");
                message.pollCreationOptionImageMessage = $root.proto.Message.FutureProofMessage.fromObject(object.pollCreationOptionImageMessage, q + 1);
            }
            if (object.associatedChildMessage != null) {
                if (!$util.isObject(object.associatedChildMessage))
                    throw TypeError(".proto.Message.associatedChildMessage: object expected");
                message.associatedChildMessage = $root.proto.Message.FutureProofMessage.fromObject(object.associatedChildMessage, q + 1);
            }
            if (object.groupStatusMentionMessage != null) {
                if (!$util.isObject(object.groupStatusMentionMessage))
                    throw TypeError(".proto.Message.groupStatusMentionMessage: object expected");
                message.groupStatusMentionMessage = $root.proto.Message.FutureProofMessage.fromObject(object.groupStatusMentionMessage, q + 1);
            }
            if (object.pollCreationMessageV4 != null) {
                if (!$util.isObject(object.pollCreationMessageV4))
                    throw TypeError(".proto.Message.pollCreationMessageV4: object expected");
                message.pollCreationMessageV4 = $root.proto.Message.FutureProofMessage.fromObject(object.pollCreationMessageV4, q + 1);
            }
            if (object.statusAddYours != null) {
                if (!$util.isObject(object.statusAddYours))
                    throw TypeError(".proto.Message.statusAddYours: object expected");
                message.statusAddYours = $root.proto.Message.FutureProofMessage.fromObject(object.statusAddYours, q + 1);
            }
            if (object.groupStatusMessage != null) {
                if (!$util.isObject(object.groupStatusMessage))
                    throw TypeError(".proto.Message.groupStatusMessage: object expected");
                message.groupStatusMessage = $root.proto.Message.FutureProofMessage.fromObject(object.groupStatusMessage, q + 1);
            }
            if (object.richResponseMessage != null) {
                if (!$util.isObject(object.richResponseMessage))
                    throw TypeError(".proto.Message.richResponseMessage: object expected");
                message.richResponseMessage = $root.proto.AIRichResponseMessage.fromObject(object.richResponseMessage, q + 1);
            }
            if (object.statusNotificationMessage != null) {
                if (!$util.isObject(object.statusNotificationMessage))
                    throw TypeError(".proto.Message.statusNotificationMessage: object expected");
                message.statusNotificationMessage = $root.proto.Message.StatusNotificationMessage.fromObject(object.statusNotificationMessage, q + 1);
            }
            if (object.limitSharingMessage != null) {
                if (!$util.isObject(object.limitSharingMessage))
                    throw TypeError(".proto.Message.limitSharingMessage: object expected");
                message.limitSharingMessage = $root.proto.Message.FutureProofMessage.fromObject(object.limitSharingMessage, q + 1);
            }
            if (object.botTaskMessage != null) {
                if (!$util.isObject(object.botTaskMessage))
                    throw TypeError(".proto.Message.botTaskMessage: object expected");
                message.botTaskMessage = $root.proto.Message.FutureProofMessage.fromObject(object.botTaskMessage, q + 1);
            }
            if (object.questionMessage != null) {
                if (!$util.isObject(object.questionMessage))
                    throw TypeError(".proto.Message.questionMessage: object expected");
                message.questionMessage = $root.proto.Message.FutureProofMessage.fromObject(object.questionMessage, q + 1);
            }
            if (object.messageHistoryNotice != null) {
                if (!$util.isObject(object.messageHistoryNotice))
                    throw TypeError(".proto.Message.messageHistoryNotice: object expected");
                message.messageHistoryNotice = $root.proto.Message.MessageHistoryNotice.fromObject(object.messageHistoryNotice, q + 1);
            }
            if (object.groupStatusMessageV2 != null) {
                if (!$util.isObject(object.groupStatusMessageV2))
                    throw TypeError(".proto.Message.groupStatusMessageV2: object expected");
                message.groupStatusMessageV2 = $root.proto.Message.FutureProofMessage.fromObject(object.groupStatusMessageV2, q + 1);
            }
            if (object.botForwardedMessage != null) {
                if (!$util.isObject(object.botForwardedMessage))
                    throw TypeError(".proto.Message.botForwardedMessage: object expected");
                message.botForwardedMessage = $root.proto.Message.FutureProofMessage.fromObject(object.botForwardedMessage, q + 1);
            }
            if (object.statusQuestionAnswerMessage != null) {
                if (!$util.isObject(object.statusQuestionAnswerMessage))
                    throw TypeError(".proto.Message.statusQuestionAnswerMessage: object expected");
                message.statusQuestionAnswerMessage = $root.proto.Message.StatusQuestionAnswerMessage.fromObject(object.statusQuestionAnswerMessage, q + 1);
            }
            if (object.questionReplyMessage != null) {
                if (!$util.isObject(object.questionReplyMessage))
                    throw TypeError(".proto.Message.questionReplyMessage: object expected");
                message.questionReplyMessage = $root.proto.Message.FutureProofMessage.fromObject(object.questionReplyMessage, q + 1);
            }
            if (object.questionResponseMessage != null) {
                if (!$util.isObject(object.questionResponseMessage))
                    throw TypeError(".proto.Message.questionResponseMessage: object expected");
                message.questionResponseMessage = $root.proto.Message.QuestionResponseMessage.fromObject(object.questionResponseMessage, q + 1);
            }
            if (object.statusQuotedMessage != null) {
                if (!$util.isObject(object.statusQuotedMessage))
                    throw TypeError(".proto.Message.statusQuotedMessage: object expected");
                message.statusQuotedMessage = $root.proto.Message.StatusQuotedMessage.fromObject(object.statusQuotedMessage, q + 1);
            }
            if (object.statusStickerInteractionMessage != null) {
                if (!$util.isObject(object.statusStickerInteractionMessage))
                    throw TypeError(".proto.Message.statusStickerInteractionMessage: object expected");
                message.statusStickerInteractionMessage = $root.proto.Message.StatusStickerInteractionMessage.fromObject(object.statusStickerInteractionMessage, q + 1);
            }
            if (object.pollCreationMessageV5 != null) {
                if (!$util.isObject(object.pollCreationMessageV5))
                    throw TypeError(".proto.Message.pollCreationMessageV5: object expected");
                message.pollCreationMessageV5 = $root.proto.Message.PollCreationMessage.fromObject(object.pollCreationMessageV5, q + 1);
            }
            if (object.newsletterFollowerInviteMessageV2 != null) {
                if (!$util.isObject(object.newsletterFollowerInviteMessageV2))
                    throw TypeError(".proto.Message.newsletterFollowerInviteMessageV2: object expected");
                message.newsletterFollowerInviteMessageV2 = $root.proto.Message.NewsletterFollowerInviteMessage.fromObject(object.newsletterFollowerInviteMessageV2, q + 1);
            }
            if (object.pollResultSnapshotMessageV3 != null) {
                if (!$util.isObject(object.pollResultSnapshotMessageV3))
                    throw TypeError(".proto.Message.pollResultSnapshotMessageV3: object expected");
                message.pollResultSnapshotMessageV3 = $root.proto.Message.PollResultSnapshotMessage.fromObject(object.pollResultSnapshotMessageV3, q + 1);
            }
            if (object.newsletterAdminProfileMessage != null) {
                if (!$util.isObject(object.newsletterAdminProfileMessage))
                    throw TypeError(".proto.Message.newsletterAdminProfileMessage: object expected");
                message.newsletterAdminProfileMessage = $root.proto.Message.FutureProofMessage.fromObject(object.newsletterAdminProfileMessage, q + 1);
            }
            if (object.newsletterAdminProfileMessageV2 != null) {
                if (!$util.isObject(object.newsletterAdminProfileMessageV2))
                    throw TypeError(".proto.Message.newsletterAdminProfileMessageV2: object expected");
                message.newsletterAdminProfileMessageV2 = $root.proto.Message.FutureProofMessage.fromObject(object.newsletterAdminProfileMessageV2, q + 1);
            }
            if (object.spoilerMessage != null) {
                if (!$util.isObject(object.spoilerMessage))
                    throw TypeError(".proto.Message.spoilerMessage: object expected");
                message.spoilerMessage = $root.proto.Message.FutureProofMessage.fromObject(object.spoilerMessage, q + 1);
            }
            if (object.pollCreationMessageV6 != null) {
                if (!$util.isObject(object.pollCreationMessageV6))
                    throw TypeError(".proto.Message.pollCreationMessageV6: object expected");
                message.pollCreationMessageV6 = $root.proto.Message.PollCreationMessage.fromObject(object.pollCreationMessageV6, q + 1);
            }
            if (object.conditionalRevealMessage != null) {
                if (!$util.isObject(object.conditionalRevealMessage))
                    throw TypeError(".proto.Message.conditionalRevealMessage: object expected");
                message.conditionalRevealMessage = $root.proto.Message.ConditionalRevealMessage.fromObject(object.conditionalRevealMessage, q + 1);
            }
            if (object.pollAddOptionMessage != null) {
                if (!$util.isObject(object.pollAddOptionMessage))
                    throw TypeError(".proto.Message.pollAddOptionMessage: object expected");
                message.pollAddOptionMessage = $root.proto.Message.PollAddOptionMessage.fromObject(object.pollAddOptionMessage, q + 1);
            }
            if (object.eventInviteMessage != null) {
                if (!$util.isObject(object.eventInviteMessage))
                    throw TypeError(".proto.Message.eventInviteMessage: object expected");
                message.eventInviteMessage = $root.proto.Message.EventInviteMessage.fromObject(object.eventInviteMessage, q + 1);
            }
            if (object.groupRootKeyShare != null) {
                if (!$util.isObject(object.groupRootKeyShare))
                    throw TypeError(".proto.Message.groupRootKeyShare: object expected");
                message.groupRootKeyShare = $root.proto.GroupRootKeyShare.fromObject(object.groupRootKeyShare, q + 1);
            }
            if (object.paymentReminderMessage != null) {
                if (!$util.isObject(object.paymentReminderMessage))
                    throw TypeError(".proto.Message.paymentReminderMessage: object expected");
                message.paymentReminderMessage = $root.proto.Message.PaymentReminderMessage.fromObject(object.paymentReminderMessage, q + 1);
            }
            if (object.splitPaymentMessage != null) {
                if (!$util.isObject(object.splitPaymentMessage))
                    throw TypeError(".proto.Message.splitPaymentMessage: object expected");
                message.splitPaymentMessage = $root.proto.Message.SplitPaymentMessage.fromObject(object.splitPaymentMessage, q + 1);
            }
            if (object.newsletterAdminProfileStatusMessage != null) {
                if (!$util.isObject(object.newsletterAdminProfileStatusMessage))
                    throw TypeError(".proto.Message.newsletterAdminProfileStatusMessage: object expected");
                message.newsletterAdminProfileStatusMessage = $root.proto.Message.FutureProofMessage.fromObject(object.newsletterAdminProfileStatusMessage, q + 1);
            }
            if (object.rootSecretDistributeMessage != null) {
                if (!$util.isObject(object.rootSecretDistributeMessage))
                    throw TypeError(".proto.Message.rootSecretDistributeMessage: object expected");
                message.rootSecretDistributeMessage = $root.proto.Message.RootSecretDistributeMessage.fromObject(object.rootSecretDistributeMessage, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a Message message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Message
         * @static
         * @param {proto.Message} message Message
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Message.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.conversation != null && Object.hasOwnProperty.call(message, "conversation"))
                object.conversation = message.conversation;
            if (message.senderKeyDistributionMessage != null && Object.hasOwnProperty.call(message, "senderKeyDistributionMessage"))
                object.senderKeyDistributionMessage = $root.proto.Message.SenderKeyDistributionMessage.toObject(message.senderKeyDistributionMessage, options, q + 1);
            if (message.imageMessage != null && Object.hasOwnProperty.call(message, "imageMessage"))
                object.imageMessage = $root.proto.Message.ImageMessage.toObject(message.imageMessage, options, q + 1);
            if (message.contactMessage != null && Object.hasOwnProperty.call(message, "contactMessage"))
                object.contactMessage = $root.proto.Message.ContactMessage.toObject(message.contactMessage, options, q + 1);
            if (message.locationMessage != null && Object.hasOwnProperty.call(message, "locationMessage"))
                object.locationMessage = $root.proto.Message.LocationMessage.toObject(message.locationMessage, options, q + 1);
            if (message.extendedTextMessage != null && Object.hasOwnProperty.call(message, "extendedTextMessage"))
                object.extendedTextMessage = $root.proto.Message.ExtendedTextMessage.toObject(message.extendedTextMessage, options, q + 1);
            if (message.documentMessage != null && Object.hasOwnProperty.call(message, "documentMessage"))
                object.documentMessage = $root.proto.Message.DocumentMessage.toObject(message.documentMessage, options, q + 1);
            if (message.audioMessage != null && Object.hasOwnProperty.call(message, "audioMessage"))
                object.audioMessage = $root.proto.Message.AudioMessage.toObject(message.audioMessage, options, q + 1);
            if (message.videoMessage != null && Object.hasOwnProperty.call(message, "videoMessage"))
                object.videoMessage = $root.proto.Message.VideoMessage.toObject(message.videoMessage, options, q + 1);
            if (message.call != null && Object.hasOwnProperty.call(message, "call"))
                object.call = $root.proto.Message.Call.toObject(message.call, options, q + 1);
            if (message.chat != null && Object.hasOwnProperty.call(message, "chat"))
                object.chat = $root.proto.Message.Chat.toObject(message.chat, options, q + 1);
            if (message.protocolMessage != null && Object.hasOwnProperty.call(message, "protocolMessage"))
                object.protocolMessage = $root.proto.Message.ProtocolMessage.toObject(message.protocolMessage, options, q + 1);
            if (message.contactsArrayMessage != null && Object.hasOwnProperty.call(message, "contactsArrayMessage"))
                object.contactsArrayMessage = $root.proto.Message.ContactsArrayMessage.toObject(message.contactsArrayMessage, options, q + 1);
            if (message.highlyStructuredMessage != null && Object.hasOwnProperty.call(message, "highlyStructuredMessage"))
                object.highlyStructuredMessage = $root.proto.Message.HighlyStructuredMessage.toObject(message.highlyStructuredMessage, options, q + 1);
            if (message.fastRatchetKeySenderKeyDistributionMessage != null && Object.hasOwnProperty.call(message, "fastRatchetKeySenderKeyDistributionMessage"))
                object.fastRatchetKeySenderKeyDistributionMessage = $root.proto.Message.SenderKeyDistributionMessage.toObject(message.fastRatchetKeySenderKeyDistributionMessage, options, q + 1);
            if (message.sendPaymentMessage != null && Object.hasOwnProperty.call(message, "sendPaymentMessage"))
                object.sendPaymentMessage = $root.proto.Message.SendPaymentMessage.toObject(message.sendPaymentMessage, options, q + 1);
            if (message.liveLocationMessage != null && Object.hasOwnProperty.call(message, "liveLocationMessage"))
                object.liveLocationMessage = $root.proto.Message.LiveLocationMessage.toObject(message.liveLocationMessage, options, q + 1);
            if (message.requestPaymentMessage != null && Object.hasOwnProperty.call(message, "requestPaymentMessage"))
                object.requestPaymentMessage = $root.proto.Message.RequestPaymentMessage.toObject(message.requestPaymentMessage, options, q + 1);
            if (message.declinePaymentRequestMessage != null && Object.hasOwnProperty.call(message, "declinePaymentRequestMessage"))
                object.declinePaymentRequestMessage = $root.proto.Message.DeclinePaymentRequestMessage.toObject(message.declinePaymentRequestMessage, options, q + 1);
            if (message.cancelPaymentRequestMessage != null && Object.hasOwnProperty.call(message, "cancelPaymentRequestMessage"))
                object.cancelPaymentRequestMessage = $root.proto.Message.CancelPaymentRequestMessage.toObject(message.cancelPaymentRequestMessage, options, q + 1);
            if (message.templateMessage != null && Object.hasOwnProperty.call(message, "templateMessage"))
                object.templateMessage = $root.proto.Message.TemplateMessage.toObject(message.templateMessage, options, q + 1);
            if (message.stickerMessage != null && Object.hasOwnProperty.call(message, "stickerMessage"))
                object.stickerMessage = $root.proto.Message.StickerMessage.toObject(message.stickerMessage, options, q + 1);
            if (message.groupInviteMessage != null && Object.hasOwnProperty.call(message, "groupInviteMessage"))
                object.groupInviteMessage = $root.proto.Message.GroupInviteMessage.toObject(message.groupInviteMessage, options, q + 1);
            if (message.templateButtonReplyMessage != null && Object.hasOwnProperty.call(message, "templateButtonReplyMessage"))
                object.templateButtonReplyMessage = $root.proto.Message.TemplateButtonReplyMessage.toObject(message.templateButtonReplyMessage, options, q + 1);
            if (message.productMessage != null && Object.hasOwnProperty.call(message, "productMessage"))
                object.productMessage = $root.proto.Message.ProductMessage.toObject(message.productMessage, options, q + 1);
            if (message.deviceSentMessage != null && Object.hasOwnProperty.call(message, "deviceSentMessage"))
                object.deviceSentMessage = $root.proto.Message.DeviceSentMessage.toObject(message.deviceSentMessage, options, q + 1);
            if (message.messageContextInfo != null && Object.hasOwnProperty.call(message, "messageContextInfo"))
                object.messageContextInfo = $root.proto.MessageContextInfo.toObject(message.messageContextInfo, options, q + 1);
            if (message.listMessage != null && Object.hasOwnProperty.call(message, "listMessage"))
                object.listMessage = $root.proto.Message.ListMessage.toObject(message.listMessage, options, q + 1);
            if (message.viewOnceMessage != null && Object.hasOwnProperty.call(message, "viewOnceMessage"))
                object.viewOnceMessage = $root.proto.Message.FutureProofMessage.toObject(message.viewOnceMessage, options, q + 1);
            if (message.orderMessage != null && Object.hasOwnProperty.call(message, "orderMessage"))
                object.orderMessage = $root.proto.Message.OrderMessage.toObject(message.orderMessage, options, q + 1);
            if (message.listResponseMessage != null && Object.hasOwnProperty.call(message, "listResponseMessage"))
                object.listResponseMessage = $root.proto.Message.ListResponseMessage.toObject(message.listResponseMessage, options, q + 1);
            if (message.ephemeralMessage != null && Object.hasOwnProperty.call(message, "ephemeralMessage"))
                object.ephemeralMessage = $root.proto.Message.FutureProofMessage.toObject(message.ephemeralMessage, options, q + 1);
            if (message.invoiceMessage != null && Object.hasOwnProperty.call(message, "invoiceMessage"))
                object.invoiceMessage = $root.proto.Message.InvoiceMessage.toObject(message.invoiceMessage, options, q + 1);
            if (message.buttonsMessage != null && Object.hasOwnProperty.call(message, "buttonsMessage"))
                object.buttonsMessage = $root.proto.Message.ButtonsMessage.toObject(message.buttonsMessage, options, q + 1);
            if (message.buttonsResponseMessage != null && Object.hasOwnProperty.call(message, "buttonsResponseMessage"))
                object.buttonsResponseMessage = $root.proto.Message.ButtonsResponseMessage.toObject(message.buttonsResponseMessage, options, q + 1);
            if (message.paymentInviteMessage != null && Object.hasOwnProperty.call(message, "paymentInviteMessage"))
                object.paymentInviteMessage = $root.proto.Message.PaymentInviteMessage.toObject(message.paymentInviteMessage, options, q + 1);
            if (message.interactiveMessage != null && Object.hasOwnProperty.call(message, "interactiveMessage"))
                object.interactiveMessage = $root.proto.Message.InteractiveMessage.toObject(message.interactiveMessage, options, q + 1);
            if (message.reactionMessage != null && Object.hasOwnProperty.call(message, "reactionMessage"))
                object.reactionMessage = $root.proto.Message.ReactionMessage.toObject(message.reactionMessage, options, q + 1);
            if (message.stickerSyncRmrMessage != null && Object.hasOwnProperty.call(message, "stickerSyncRmrMessage"))
                object.stickerSyncRmrMessage = $root.proto.Message.StickerSyncRMRMessage.toObject(message.stickerSyncRmrMessage, options, q + 1);
            if (message.interactiveResponseMessage != null && Object.hasOwnProperty.call(message, "interactiveResponseMessage"))
                object.interactiveResponseMessage = $root.proto.Message.InteractiveResponseMessage.toObject(message.interactiveResponseMessage, options, q + 1);
            if (message.pollCreationMessage != null && Object.hasOwnProperty.call(message, "pollCreationMessage"))
                object.pollCreationMessage = $root.proto.Message.PollCreationMessage.toObject(message.pollCreationMessage, options, q + 1);
            if (message.pollUpdateMessage != null && Object.hasOwnProperty.call(message, "pollUpdateMessage"))
                object.pollUpdateMessage = $root.proto.Message.PollUpdateMessage.toObject(message.pollUpdateMessage, options, q + 1);
            if (message.keepInChatMessage != null && Object.hasOwnProperty.call(message, "keepInChatMessage"))
                object.keepInChatMessage = $root.proto.Message.KeepInChatMessage.toObject(message.keepInChatMessage, options, q + 1);
            if (message.documentWithCaptionMessage != null && Object.hasOwnProperty.call(message, "documentWithCaptionMessage"))
                object.documentWithCaptionMessage = $root.proto.Message.FutureProofMessage.toObject(message.documentWithCaptionMessage, options, q + 1);
            if (message.requestPhoneNumberMessage != null && Object.hasOwnProperty.call(message, "requestPhoneNumberMessage"))
                object.requestPhoneNumberMessage = $root.proto.Message.RequestPhoneNumberMessage.toObject(message.requestPhoneNumberMessage, options, q + 1);
            if (message.viewOnceMessageV2 != null && Object.hasOwnProperty.call(message, "viewOnceMessageV2"))
                object.viewOnceMessageV2 = $root.proto.Message.FutureProofMessage.toObject(message.viewOnceMessageV2, options, q + 1);
            if (message.encReactionMessage != null && Object.hasOwnProperty.call(message, "encReactionMessage"))
                object.encReactionMessage = $root.proto.Message.EncReactionMessage.toObject(message.encReactionMessage, options, q + 1);
            if (message.editedMessage != null && Object.hasOwnProperty.call(message, "editedMessage"))
                object.editedMessage = $root.proto.Message.FutureProofMessage.toObject(message.editedMessage, options, q + 1);
            if (message.viewOnceMessageV2Extension != null && Object.hasOwnProperty.call(message, "viewOnceMessageV2Extension"))
                object.viewOnceMessageV2Extension = $root.proto.Message.FutureProofMessage.toObject(message.viewOnceMessageV2Extension, options, q + 1);
            if (message.pollCreationMessageV2 != null && Object.hasOwnProperty.call(message, "pollCreationMessageV2"))
                object.pollCreationMessageV2 = $root.proto.Message.PollCreationMessage.toObject(message.pollCreationMessageV2, options, q + 1);
            if (message.scheduledCallCreationMessage != null && Object.hasOwnProperty.call(message, "scheduledCallCreationMessage"))
                object.scheduledCallCreationMessage = $root.proto.Message.ScheduledCallCreationMessage.toObject(message.scheduledCallCreationMessage, options, q + 1);
            if (message.groupMentionedMessage != null && Object.hasOwnProperty.call(message, "groupMentionedMessage"))
                object.groupMentionedMessage = $root.proto.Message.FutureProofMessage.toObject(message.groupMentionedMessage, options, q + 1);
            if (message.pinInChatMessage != null && Object.hasOwnProperty.call(message, "pinInChatMessage"))
                object.pinInChatMessage = $root.proto.Message.PinInChatMessage.toObject(message.pinInChatMessage, options, q + 1);
            if (message.pollCreationMessageV3 != null && Object.hasOwnProperty.call(message, "pollCreationMessageV3"))
                object.pollCreationMessageV3 = $root.proto.Message.PollCreationMessage.toObject(message.pollCreationMessageV3, options, q + 1);
            if (message.scheduledCallEditMessage != null && Object.hasOwnProperty.call(message, "scheduledCallEditMessage"))
                object.scheduledCallEditMessage = $root.proto.Message.ScheduledCallEditMessage.toObject(message.scheduledCallEditMessage, options, q + 1);
            if (message.ptvMessage != null && Object.hasOwnProperty.call(message, "ptvMessage"))
                object.ptvMessage = $root.proto.Message.VideoMessage.toObject(message.ptvMessage, options, q + 1);
            if (message.botInvokeMessage != null && Object.hasOwnProperty.call(message, "botInvokeMessage"))
                object.botInvokeMessage = $root.proto.Message.FutureProofMessage.toObject(message.botInvokeMessage, options, q + 1);
            if (message.callLogMesssage != null && Object.hasOwnProperty.call(message, "callLogMesssage"))
                object.callLogMesssage = $root.proto.Message.CallLogMessage.toObject(message.callLogMesssage, options, q + 1);
            if (message.messageHistoryBundle != null && Object.hasOwnProperty.call(message, "messageHistoryBundle"))
                object.messageHistoryBundle = $root.proto.Message.MessageHistoryBundle.toObject(message.messageHistoryBundle, options, q + 1);
            if (message.encCommentMessage != null && Object.hasOwnProperty.call(message, "encCommentMessage"))
                object.encCommentMessage = $root.proto.Message.EncCommentMessage.toObject(message.encCommentMessage, options, q + 1);
            if (message.bcallMessage != null && Object.hasOwnProperty.call(message, "bcallMessage"))
                object.bcallMessage = $root.proto.Message.BCallMessage.toObject(message.bcallMessage, options, q + 1);
            if (message.lottieStickerMessage != null && Object.hasOwnProperty.call(message, "lottieStickerMessage"))
                object.lottieStickerMessage = $root.proto.Message.FutureProofMessage.toObject(message.lottieStickerMessage, options, q + 1);
            if (message.eventMessage != null && Object.hasOwnProperty.call(message, "eventMessage"))
                object.eventMessage = $root.proto.Message.EventMessage.toObject(message.eventMessage, options, q + 1);
            if (message.encEventResponseMessage != null && Object.hasOwnProperty.call(message, "encEventResponseMessage"))
                object.encEventResponseMessage = $root.proto.Message.EncEventResponseMessage.toObject(message.encEventResponseMessage, options, q + 1);
            if (message.commentMessage != null && Object.hasOwnProperty.call(message, "commentMessage"))
                object.commentMessage = $root.proto.Message.CommentMessage.toObject(message.commentMessage, options, q + 1);
            if (message.newsletterAdminInviteMessage != null && Object.hasOwnProperty.call(message, "newsletterAdminInviteMessage"))
                object.newsletterAdminInviteMessage = $root.proto.Message.NewsletterAdminInviteMessage.toObject(message.newsletterAdminInviteMessage, options, q + 1);
            if (message.placeholderMessage != null && Object.hasOwnProperty.call(message, "placeholderMessage"))
                object.placeholderMessage = $root.proto.Message.PlaceholderMessage.toObject(message.placeholderMessage, options, q + 1);
            if (message.secretEncryptedMessage != null && Object.hasOwnProperty.call(message, "secretEncryptedMessage"))
                object.secretEncryptedMessage = $root.proto.Message.SecretEncryptedMessage.toObject(message.secretEncryptedMessage, options, q + 1);
            if (message.albumMessage != null && Object.hasOwnProperty.call(message, "albumMessage"))
                object.albumMessage = $root.proto.Message.AlbumMessage.toObject(message.albumMessage, options, q + 1);
            if (message.eventCoverImage != null && Object.hasOwnProperty.call(message, "eventCoverImage"))
                object.eventCoverImage = $root.proto.Message.FutureProofMessage.toObject(message.eventCoverImage, options, q + 1);
            if (message.stickerPackMessage != null && Object.hasOwnProperty.call(message, "stickerPackMessage"))
                object.stickerPackMessage = $root.proto.Message.StickerPackMessage.toObject(message.stickerPackMessage, options, q + 1);
            if (message.statusMentionMessage != null && Object.hasOwnProperty.call(message, "statusMentionMessage"))
                object.statusMentionMessage = $root.proto.Message.FutureProofMessage.toObject(message.statusMentionMessage, options, q + 1);
            if (message.pollResultSnapshotMessage != null && Object.hasOwnProperty.call(message, "pollResultSnapshotMessage"))
                object.pollResultSnapshotMessage = $root.proto.Message.PollResultSnapshotMessage.toObject(message.pollResultSnapshotMessage, options, q + 1);
            if (message.pollCreationOptionImageMessage != null && Object.hasOwnProperty.call(message, "pollCreationOptionImageMessage"))
                object.pollCreationOptionImageMessage = $root.proto.Message.FutureProofMessage.toObject(message.pollCreationOptionImageMessage, options, q + 1);
            if (message.associatedChildMessage != null && Object.hasOwnProperty.call(message, "associatedChildMessage"))
                object.associatedChildMessage = $root.proto.Message.FutureProofMessage.toObject(message.associatedChildMessage, options, q + 1);
            if (message.groupStatusMentionMessage != null && Object.hasOwnProperty.call(message, "groupStatusMentionMessage"))
                object.groupStatusMentionMessage = $root.proto.Message.FutureProofMessage.toObject(message.groupStatusMentionMessage, options, q + 1);
            if (message.pollCreationMessageV4 != null && Object.hasOwnProperty.call(message, "pollCreationMessageV4"))
                object.pollCreationMessageV4 = $root.proto.Message.FutureProofMessage.toObject(message.pollCreationMessageV4, options, q + 1);
            if (message.statusAddYours != null && Object.hasOwnProperty.call(message, "statusAddYours"))
                object.statusAddYours = $root.proto.Message.FutureProofMessage.toObject(message.statusAddYours, options, q + 1);
            if (message.groupStatusMessage != null && Object.hasOwnProperty.call(message, "groupStatusMessage"))
                object.groupStatusMessage = $root.proto.Message.FutureProofMessage.toObject(message.groupStatusMessage, options, q + 1);
            if (message.richResponseMessage != null && Object.hasOwnProperty.call(message, "richResponseMessage"))
                object.richResponseMessage = $root.proto.AIRichResponseMessage.toObject(message.richResponseMessage, options, q + 1);
            if (message.statusNotificationMessage != null && Object.hasOwnProperty.call(message, "statusNotificationMessage"))
                object.statusNotificationMessage = $root.proto.Message.StatusNotificationMessage.toObject(message.statusNotificationMessage, options, q + 1);
            if (message.limitSharingMessage != null && Object.hasOwnProperty.call(message, "limitSharingMessage"))
                object.limitSharingMessage = $root.proto.Message.FutureProofMessage.toObject(message.limitSharingMessage, options, q + 1);
            if (message.botTaskMessage != null && Object.hasOwnProperty.call(message, "botTaskMessage"))
                object.botTaskMessage = $root.proto.Message.FutureProofMessage.toObject(message.botTaskMessage, options, q + 1);
            if (message.questionMessage != null && Object.hasOwnProperty.call(message, "questionMessage"))
                object.questionMessage = $root.proto.Message.FutureProofMessage.toObject(message.questionMessage, options, q + 1);
            if (message.messageHistoryNotice != null && Object.hasOwnProperty.call(message, "messageHistoryNotice"))
                object.messageHistoryNotice = $root.proto.Message.MessageHistoryNotice.toObject(message.messageHistoryNotice, options, q + 1);
            if (message.groupStatusMessageV2 != null && Object.hasOwnProperty.call(message, "groupStatusMessageV2"))
                object.groupStatusMessageV2 = $root.proto.Message.FutureProofMessage.toObject(message.groupStatusMessageV2, options, q + 1);
            if (message.botForwardedMessage != null && Object.hasOwnProperty.call(message, "botForwardedMessage"))
                object.botForwardedMessage = $root.proto.Message.FutureProofMessage.toObject(message.botForwardedMessage, options, q + 1);
            if (message.statusQuestionAnswerMessage != null && Object.hasOwnProperty.call(message, "statusQuestionAnswerMessage"))
                object.statusQuestionAnswerMessage = $root.proto.Message.StatusQuestionAnswerMessage.toObject(message.statusQuestionAnswerMessage, options, q + 1);
            if (message.questionReplyMessage != null && Object.hasOwnProperty.call(message, "questionReplyMessage"))
                object.questionReplyMessage = $root.proto.Message.FutureProofMessage.toObject(message.questionReplyMessage, options, q + 1);
            if (message.questionResponseMessage != null && Object.hasOwnProperty.call(message, "questionResponseMessage"))
                object.questionResponseMessage = $root.proto.Message.QuestionResponseMessage.toObject(message.questionResponseMessage, options, q + 1);
            if (message.statusQuotedMessage != null && Object.hasOwnProperty.call(message, "statusQuotedMessage"))
                object.statusQuotedMessage = $root.proto.Message.StatusQuotedMessage.toObject(message.statusQuotedMessage, options, q + 1);
            if (message.statusStickerInteractionMessage != null && Object.hasOwnProperty.call(message, "statusStickerInteractionMessage"))
                object.statusStickerInteractionMessage = $root.proto.Message.StatusStickerInteractionMessage.toObject(message.statusStickerInteractionMessage, options, q + 1);
            if (message.pollCreationMessageV5 != null && Object.hasOwnProperty.call(message, "pollCreationMessageV5"))
                object.pollCreationMessageV5 = $root.proto.Message.PollCreationMessage.toObject(message.pollCreationMessageV5, options, q + 1);
            if (message.newsletterFollowerInviteMessageV2 != null && Object.hasOwnProperty.call(message, "newsletterFollowerInviteMessageV2"))
                object.newsletterFollowerInviteMessageV2 = $root.proto.Message.NewsletterFollowerInviteMessage.toObject(message.newsletterFollowerInviteMessageV2, options, q + 1);
            if (message.pollResultSnapshotMessageV3 != null && Object.hasOwnProperty.call(message, "pollResultSnapshotMessageV3"))
                object.pollResultSnapshotMessageV3 = $root.proto.Message.PollResultSnapshotMessage.toObject(message.pollResultSnapshotMessageV3, options, q + 1);
            if (message.newsletterAdminProfileMessage != null && Object.hasOwnProperty.call(message, "newsletterAdminProfileMessage"))
                object.newsletterAdminProfileMessage = $root.proto.Message.FutureProofMessage.toObject(message.newsletterAdminProfileMessage, options, q + 1);
            if (message.newsletterAdminProfileMessageV2 != null && Object.hasOwnProperty.call(message, "newsletterAdminProfileMessageV2"))
                object.newsletterAdminProfileMessageV2 = $root.proto.Message.FutureProofMessage.toObject(message.newsletterAdminProfileMessageV2, options, q + 1);
            if (message.spoilerMessage != null && Object.hasOwnProperty.call(message, "spoilerMessage"))
                object.spoilerMessage = $root.proto.Message.FutureProofMessage.toObject(message.spoilerMessage, options, q + 1);
            if (message.pollCreationMessageV6 != null && Object.hasOwnProperty.call(message, "pollCreationMessageV6"))
                object.pollCreationMessageV6 = $root.proto.Message.PollCreationMessage.toObject(message.pollCreationMessageV6, options, q + 1);
            if (message.conditionalRevealMessage != null && Object.hasOwnProperty.call(message, "conditionalRevealMessage"))
                object.conditionalRevealMessage = $root.proto.Message.ConditionalRevealMessage.toObject(message.conditionalRevealMessage, options, q + 1);
            if (message.pollAddOptionMessage != null && Object.hasOwnProperty.call(message, "pollAddOptionMessage"))
                object.pollAddOptionMessage = $root.proto.Message.PollAddOptionMessage.toObject(message.pollAddOptionMessage, options, q + 1);
            if (message.eventInviteMessage != null && Object.hasOwnProperty.call(message, "eventInviteMessage"))
                object.eventInviteMessage = $root.proto.Message.EventInviteMessage.toObject(message.eventInviteMessage, options, q + 1);
            if (message.groupRootKeyShare != null && Object.hasOwnProperty.call(message, "groupRootKeyShare"))
                object.groupRootKeyShare = $root.proto.GroupRootKeyShare.toObject(message.groupRootKeyShare, options, q + 1);
            if (message.paymentReminderMessage != null && Object.hasOwnProperty.call(message, "paymentReminderMessage"))
                object.paymentReminderMessage = $root.proto.Message.PaymentReminderMessage.toObject(message.paymentReminderMessage, options, q + 1);
            if (message.splitPaymentMessage != null && Object.hasOwnProperty.call(message, "splitPaymentMessage"))
                object.splitPaymentMessage = $root.proto.Message.SplitPaymentMessage.toObject(message.splitPaymentMessage, options, q + 1);
            if (message.newsletterAdminProfileStatusMessage != null && Object.hasOwnProperty.call(message, "newsletterAdminProfileStatusMessage"))
                object.newsletterAdminProfileStatusMessage = $root.proto.Message.FutureProofMessage.toObject(message.newsletterAdminProfileStatusMessage, options, q + 1);
            if (message.rootSecretDistributeMessage != null && Object.hasOwnProperty.call(message, "rootSecretDistributeMessage"))
                object.rootSecretDistributeMessage = $root.proto.Message.RootSecretDistributeMessage.toObject(message.rootSecretDistributeMessage, options, q + 1);
            return object;
        };

        /**
         * Converts this Message to JSON.
         * @function toJSON
         * @memberof proto.Message
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Message.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Message
         * @function getTypeUrl
         * @memberof proto.Message
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Message.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.Message";
        };

        Message.AlbumMessage = (function() {

            /**
             * Properties of an AlbumMessage.
             * @memberof proto.Message
             * @interface IAlbumMessage
             * @property {number|null} [expectedImageCount] AlbumMessage expectedImageCount
             * @property {number|null} [expectedVideoCount] AlbumMessage expectedVideoCount
             * @property {proto.IContextInfo|null} [contextInfo] AlbumMessage contextInfo
             */

            /**
             * Constructs a new AlbumMessage.
             * @memberof proto.Message
             * @classdesc Represents an AlbumMessage.
             * @implements IAlbumMessage
             * @constructor
             * @param {proto.Message.IAlbumMessage=} [properties] Properties to set
             */
            function AlbumMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AlbumMessage expectedImageCount.
             * @member {number|null|undefined} expectedImageCount
             * @memberof proto.Message.AlbumMessage
             * @instance
             */
            AlbumMessage.prototype.expectedImageCount = null;

            /**
             * AlbumMessage expectedVideoCount.
             * @member {number|null|undefined} expectedVideoCount
             * @memberof proto.Message.AlbumMessage
             * @instance
             */
            AlbumMessage.prototype.expectedVideoCount = null;

            /**
             * AlbumMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.AlbumMessage
             * @instance
             */
            AlbumMessage.prototype.contextInfo = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AlbumMessage.prototype, "_expectedImageCount", {
                get: $util.oneOfGetter($oneOfFields = ["expectedImageCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AlbumMessage.prototype, "_expectedVideoCount", {
                get: $util.oneOfGetter($oneOfFields = ["expectedVideoCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AlbumMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AlbumMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.AlbumMessage
             * @static
             * @param {proto.Message.IAlbumMessage=} [properties] Properties to set
             * @returns {proto.Message.AlbumMessage} AlbumMessage instance
             */
            AlbumMessage.create = function create(properties) {
                return new AlbumMessage(properties);
            };

            /**
             * Encodes the specified AlbumMessage message. Does not implicitly {@link proto.Message.AlbumMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.AlbumMessage
             * @static
             * @param {proto.Message.IAlbumMessage} message AlbumMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AlbumMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.expectedImageCount != null && Object.hasOwnProperty.call(message, "expectedImageCount"))
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.expectedImageCount);
                if (message.expectedVideoCount != null && Object.hasOwnProperty.call(message, "expectedVideoCount"))
                    writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.expectedVideoCount);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AlbumMessage message, length delimited. Does not implicitly {@link proto.Message.AlbumMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.AlbumMessage
             * @static
             * @param {proto.Message.IAlbumMessage} message AlbumMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AlbumMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an AlbumMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.AlbumMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.AlbumMessage} AlbumMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AlbumMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 2: {
                            if (u !== 0)
                                break;
                            message.expectedImageCount = reader.uint32();
                            message._expectedImageCount = "expectedImageCount";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.expectedVideoCount = reader.uint32();
                            message._expectedVideoCount = "expectedVideoCount";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an AlbumMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.AlbumMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.AlbumMessage} AlbumMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AlbumMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AlbumMessage message.
             * @function verify
             * @memberof proto.Message.AlbumMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AlbumMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.expectedImageCount != null && Object.hasOwnProperty.call(message, "expectedImageCount")) {
                    properties._expectedImageCount = 1;
                    if (!$util.isInteger(message.expectedImageCount))
                        return "expectedImageCount: integer expected";
                }
                if (message.expectedVideoCount != null && Object.hasOwnProperty.call(message, "expectedVideoCount")) {
                    properties._expectedVideoCount = 1;
                    if (!$util.isInteger(message.expectedVideoCount))
                        return "expectedVideoCount: integer expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an AlbumMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.AlbumMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.AlbumMessage} AlbumMessage
             */
            AlbumMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.AlbumMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.expectedImageCount != null)
                    message.expectedImageCount = object.expectedImageCount >>> 0;
                if (object.expectedVideoCount != null)
                    message.expectedVideoCount = object.expectedVideoCount >>> 0;
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.AlbumMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from an AlbumMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.AlbumMessage
             * @static
             * @param {proto.Message.AlbumMessage} message AlbumMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AlbumMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.expectedImageCount != null && Object.hasOwnProperty.call(message, "expectedImageCount"))
                    object.expectedImageCount = message.expectedImageCount;
                if (message.expectedVideoCount != null && Object.hasOwnProperty.call(message, "expectedVideoCount"))
                    object.expectedVideoCount = message.expectedVideoCount;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                return object;
            };

            /**
             * Converts this AlbumMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.AlbumMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AlbumMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AlbumMessage
             * @function getTypeUrl
             * @memberof proto.Message.AlbumMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AlbumMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.AlbumMessage";
            };

            return AlbumMessage;
        })();

        Message.AppStateFatalExceptionNotification = (function() {

            /**
             * Properties of an AppStateFatalExceptionNotification.
             * @memberof proto.Message
             * @interface IAppStateFatalExceptionNotification
             * @property {Array.<string>|null} [collectionNames] AppStateFatalExceptionNotification collectionNames
             * @property {number|Long|null} [timestamp] AppStateFatalExceptionNotification timestamp
             */

            /**
             * Constructs a new AppStateFatalExceptionNotification.
             * @memberof proto.Message
             * @classdesc Represents an AppStateFatalExceptionNotification.
             * @implements IAppStateFatalExceptionNotification
             * @constructor
             * @param {proto.Message.IAppStateFatalExceptionNotification=} [properties] Properties to set
             */
            function AppStateFatalExceptionNotification(properties) {
                this.collectionNames = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AppStateFatalExceptionNotification collectionNames.
             * @member {Array.<string>} collectionNames
             * @memberof proto.Message.AppStateFatalExceptionNotification
             * @instance
             */
            AppStateFatalExceptionNotification.prototype.collectionNames = $util.emptyArray;

            /**
             * AppStateFatalExceptionNotification timestamp.
             * @member {number|Long|null|undefined} timestamp
             * @memberof proto.Message.AppStateFatalExceptionNotification
             * @instance
             */
            AppStateFatalExceptionNotification.prototype.timestamp = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AppStateFatalExceptionNotification.prototype, "_timestamp", {
                get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AppStateFatalExceptionNotification instance using the specified properties.
             * @function create
             * @memberof proto.Message.AppStateFatalExceptionNotification
             * @static
             * @param {proto.Message.IAppStateFatalExceptionNotification=} [properties] Properties to set
             * @returns {proto.Message.AppStateFatalExceptionNotification} AppStateFatalExceptionNotification instance
             */
            AppStateFatalExceptionNotification.create = function create(properties) {
                return new AppStateFatalExceptionNotification(properties);
            };

            /**
             * Encodes the specified AppStateFatalExceptionNotification message. Does not implicitly {@link proto.Message.AppStateFatalExceptionNotification.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.AppStateFatalExceptionNotification
             * @static
             * @param {proto.Message.IAppStateFatalExceptionNotification} message AppStateFatalExceptionNotification message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AppStateFatalExceptionNotification.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.collectionNames != null && message.collectionNames.length)
                    for (var i = 0; i < message.collectionNames.length; ++i)
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.collectionNames[i]);
                if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.timestamp);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AppStateFatalExceptionNotification message, length delimited. Does not implicitly {@link proto.Message.AppStateFatalExceptionNotification.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.AppStateFatalExceptionNotification
             * @static
             * @param {proto.Message.IAppStateFatalExceptionNotification} message AppStateFatalExceptionNotification message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AppStateFatalExceptionNotification.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an AppStateFatalExceptionNotification message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.AppStateFatalExceptionNotification
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.AppStateFatalExceptionNotification} AppStateFatalExceptionNotification
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AppStateFatalExceptionNotification.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(message.collectionNames && message.collectionNames.length))
                                message.collectionNames = [];
                            message.collectionNames.push(reader.stringVerify());
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.timestamp = reader.int64();
                            message._timestamp = "timestamp";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an AppStateFatalExceptionNotification message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.AppStateFatalExceptionNotification
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.AppStateFatalExceptionNotification} AppStateFatalExceptionNotification
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AppStateFatalExceptionNotification.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AppStateFatalExceptionNotification message.
             * @function verify
             * @memberof proto.Message.AppStateFatalExceptionNotification
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AppStateFatalExceptionNotification.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.collectionNames != null && Object.hasOwnProperty.call(message, "collectionNames")) {
                    if (!Array.isArray(message.collectionNames))
                        return "collectionNames: array expected";
                    for (var i = 0; i < message.collectionNames.length; ++i)
                        if (!$util.isString(message.collectionNames[i]))
                            return "collectionNames: string[] expected";
                }
                if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp")) {
                    properties._timestamp = 1;
                    if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                        return "timestamp: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates an AppStateFatalExceptionNotification message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.AppStateFatalExceptionNotification
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.AppStateFatalExceptionNotification} AppStateFatalExceptionNotification
             */
            AppStateFatalExceptionNotification.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.AppStateFatalExceptionNotification: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.collectionNames) {
                    if (!Array.isArray(object.collectionNames))
                        throw TypeError(".proto.Message.AppStateFatalExceptionNotification.collectionNames: array expected");
                    message.collectionNames = Array(object.collectionNames.length);
                    for (var i = 0; i < object.collectionNames.length; ++i)
                        message.collectionNames[i] = String(object.collectionNames[i]);
                }
                if (object.timestamp != null)
                    if ($util.Long)
                        message.timestamp = $util.Long.fromValue(object.timestamp, false);
                    else if (typeof object.timestamp === "string")
                        message.timestamp = parseInt(object.timestamp, 10);
                    else if (typeof object.timestamp === "number")
                        message.timestamp = object.timestamp;
                    else if (typeof object.timestamp === "object")
                        message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from an AppStateFatalExceptionNotification message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.AppStateFatalExceptionNotification
             * @static
             * @param {proto.Message.AppStateFatalExceptionNotification} message AppStateFatalExceptionNotification
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AppStateFatalExceptionNotification.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.collectionNames = [];
                if (message.collectionNames && message.collectionNames.length) {
                    object.collectionNames = Array(message.collectionNames.length);
                    for (var j = 0; j < message.collectionNames.length; ++j)
                        object.collectionNames[j] = message.collectionNames[j];
                }
                if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.timestamp = typeof message.timestamp === "number" ? BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.timestamp === "number")
                        object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                    else
                        object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
                return object;
            };

            /**
             * Converts this AppStateFatalExceptionNotification to JSON.
             * @function toJSON
             * @memberof proto.Message.AppStateFatalExceptionNotification
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AppStateFatalExceptionNotification.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AppStateFatalExceptionNotification
             * @function getTypeUrl
             * @memberof proto.Message.AppStateFatalExceptionNotification
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AppStateFatalExceptionNotification.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.AppStateFatalExceptionNotification";
            };

            return AppStateFatalExceptionNotification;
        })();

        Message.AppStateSyncKey = (function() {

            /**
             * Properties of an AppStateSyncKey.
             * @memberof proto.Message
             * @interface IAppStateSyncKey
             * @property {proto.Message.IAppStateSyncKeyId|null} [keyId] AppStateSyncKey keyId
             * @property {proto.Message.IAppStateSyncKeyData|null} [keyData] AppStateSyncKey keyData
             */

            /**
             * Constructs a new AppStateSyncKey.
             * @memberof proto.Message
             * @classdesc Represents an AppStateSyncKey.
             * @implements IAppStateSyncKey
             * @constructor
             * @param {proto.Message.IAppStateSyncKey=} [properties] Properties to set
             */
            function AppStateSyncKey(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AppStateSyncKey keyId.
             * @member {proto.Message.IAppStateSyncKeyId|null|undefined} keyId
             * @memberof proto.Message.AppStateSyncKey
             * @instance
             */
            AppStateSyncKey.prototype.keyId = null;

            /**
             * AppStateSyncKey keyData.
             * @member {proto.Message.IAppStateSyncKeyData|null|undefined} keyData
             * @memberof proto.Message.AppStateSyncKey
             * @instance
             */
            AppStateSyncKey.prototype.keyData = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AppStateSyncKey.prototype, "_keyId", {
                get: $util.oneOfGetter($oneOfFields = ["keyId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AppStateSyncKey.prototype, "_keyData", {
                get: $util.oneOfGetter($oneOfFields = ["keyData"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AppStateSyncKey instance using the specified properties.
             * @function create
             * @memberof proto.Message.AppStateSyncKey
             * @static
             * @param {proto.Message.IAppStateSyncKey=} [properties] Properties to set
             * @returns {proto.Message.AppStateSyncKey} AppStateSyncKey instance
             */
            AppStateSyncKey.create = function create(properties) {
                return new AppStateSyncKey(properties);
            };

            /**
             * Encodes the specified AppStateSyncKey message. Does not implicitly {@link proto.Message.AppStateSyncKey.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.AppStateSyncKey
             * @static
             * @param {proto.Message.IAppStateSyncKey} message AppStateSyncKey message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AppStateSyncKey.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.keyId != null && Object.hasOwnProperty.call(message, "keyId"))
                    $root.proto.Message.AppStateSyncKeyId.encode(message.keyId, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.keyData != null && Object.hasOwnProperty.call(message, "keyData"))
                    $root.proto.Message.AppStateSyncKeyData.encode(message.keyData, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AppStateSyncKey message, length delimited. Does not implicitly {@link proto.Message.AppStateSyncKey.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.AppStateSyncKey
             * @static
             * @param {proto.Message.IAppStateSyncKey} message AppStateSyncKey message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AppStateSyncKey.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an AppStateSyncKey message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.AppStateSyncKey
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.AppStateSyncKey} AppStateSyncKey
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AppStateSyncKey.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.keyId = $root.proto.Message.AppStateSyncKeyId.decode(reader, reader.uint32(), undefined, q + 1, message.keyId);
                            message._keyId = "keyId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.keyData = $root.proto.Message.AppStateSyncKeyData.decode(reader, reader.uint32(), undefined, q + 1, message.keyData);
                            message._keyData = "keyData";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an AppStateSyncKey message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.AppStateSyncKey
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.AppStateSyncKey} AppStateSyncKey
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AppStateSyncKey.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AppStateSyncKey message.
             * @function verify
             * @memberof proto.Message.AppStateSyncKey
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AppStateSyncKey.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.keyId != null && Object.hasOwnProperty.call(message, "keyId")) {
                    properties._keyId = 1;
                    {
                        var error = $root.proto.Message.AppStateSyncKeyId.verify(message.keyId, q + 1);
                        if (error)
                            return "keyId." + error;
                    }
                }
                if (message.keyData != null && Object.hasOwnProperty.call(message, "keyData")) {
                    properties._keyData = 1;
                    {
                        var error = $root.proto.Message.AppStateSyncKeyData.verify(message.keyData, q + 1);
                        if (error)
                            return "keyData." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an AppStateSyncKey message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.AppStateSyncKey
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.AppStateSyncKey} AppStateSyncKey
             */
            AppStateSyncKey.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.AppStateSyncKey: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.keyId != null) {
                    if (!$util.isObject(object.keyId))
                        throw TypeError(".proto.Message.AppStateSyncKey.keyId: object expected");
                    message.keyId = $root.proto.Message.AppStateSyncKeyId.fromObject(object.keyId, q + 1);
                }
                if (object.keyData != null) {
                    if (!$util.isObject(object.keyData))
                        throw TypeError(".proto.Message.AppStateSyncKey.keyData: object expected");
                    message.keyData = $root.proto.Message.AppStateSyncKeyData.fromObject(object.keyData, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from an AppStateSyncKey message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.AppStateSyncKey
             * @static
             * @param {proto.Message.AppStateSyncKey} message AppStateSyncKey
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AppStateSyncKey.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.keyId != null && Object.hasOwnProperty.call(message, "keyId"))
                    object.keyId = $root.proto.Message.AppStateSyncKeyId.toObject(message.keyId, options, q + 1);
                if (message.keyData != null && Object.hasOwnProperty.call(message, "keyData"))
                    object.keyData = $root.proto.Message.AppStateSyncKeyData.toObject(message.keyData, options, q + 1);
                return object;
            };

            /**
             * Converts this AppStateSyncKey to JSON.
             * @function toJSON
             * @memberof proto.Message.AppStateSyncKey
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AppStateSyncKey.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AppStateSyncKey
             * @function getTypeUrl
             * @memberof proto.Message.AppStateSyncKey
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AppStateSyncKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.AppStateSyncKey";
            };

            return AppStateSyncKey;
        })();

        Message.AppStateSyncKeyData = (function() {

            /**
             * Properties of an AppStateSyncKeyData.
             * @memberof proto.Message
             * @interface IAppStateSyncKeyData
             * @property {Uint8Array|null} [keyData] AppStateSyncKeyData keyData
             * @property {proto.Message.IAppStateSyncKeyFingerprint|null} [fingerprint] AppStateSyncKeyData fingerprint
             * @property {number|Long|null} [timestamp] AppStateSyncKeyData timestamp
             */

            /**
             * Constructs a new AppStateSyncKeyData.
             * @memberof proto.Message
             * @classdesc Represents an AppStateSyncKeyData.
             * @implements IAppStateSyncKeyData
             * @constructor
             * @param {proto.Message.IAppStateSyncKeyData=} [properties] Properties to set
             */
            function AppStateSyncKeyData(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AppStateSyncKeyData keyData.
             * @member {Uint8Array|null|undefined} keyData
             * @memberof proto.Message.AppStateSyncKeyData
             * @instance
             */
            AppStateSyncKeyData.prototype.keyData = null;

            /**
             * AppStateSyncKeyData fingerprint.
             * @member {proto.Message.IAppStateSyncKeyFingerprint|null|undefined} fingerprint
             * @memberof proto.Message.AppStateSyncKeyData
             * @instance
             */
            AppStateSyncKeyData.prototype.fingerprint = null;

            /**
             * AppStateSyncKeyData timestamp.
             * @member {number|Long|null|undefined} timestamp
             * @memberof proto.Message.AppStateSyncKeyData
             * @instance
             */
            AppStateSyncKeyData.prototype.timestamp = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AppStateSyncKeyData.prototype, "_keyData", {
                get: $util.oneOfGetter($oneOfFields = ["keyData"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AppStateSyncKeyData.prototype, "_fingerprint", {
                get: $util.oneOfGetter($oneOfFields = ["fingerprint"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AppStateSyncKeyData.prototype, "_timestamp", {
                get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AppStateSyncKeyData instance using the specified properties.
             * @function create
             * @memberof proto.Message.AppStateSyncKeyData
             * @static
             * @param {proto.Message.IAppStateSyncKeyData=} [properties] Properties to set
             * @returns {proto.Message.AppStateSyncKeyData} AppStateSyncKeyData instance
             */
            AppStateSyncKeyData.create = function create(properties) {
                return new AppStateSyncKeyData(properties);
            };

            /**
             * Encodes the specified AppStateSyncKeyData message. Does not implicitly {@link proto.Message.AppStateSyncKeyData.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.AppStateSyncKeyData
             * @static
             * @param {proto.Message.IAppStateSyncKeyData} message AppStateSyncKeyData message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AppStateSyncKeyData.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.keyData != null && Object.hasOwnProperty.call(message, "keyData"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.keyData);
                if (message.fingerprint != null && Object.hasOwnProperty.call(message, "fingerprint"))
                    $root.proto.Message.AppStateSyncKeyFingerprint.encode(message.fingerprint, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.timestamp);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AppStateSyncKeyData message, length delimited. Does not implicitly {@link proto.Message.AppStateSyncKeyData.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.AppStateSyncKeyData
             * @static
             * @param {proto.Message.IAppStateSyncKeyData} message AppStateSyncKeyData message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AppStateSyncKeyData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an AppStateSyncKeyData message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.AppStateSyncKeyData
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.AppStateSyncKeyData} AppStateSyncKeyData
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AppStateSyncKeyData.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.keyData = reader.bytes();
                            message._keyData = "keyData";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.fingerprint = $root.proto.Message.AppStateSyncKeyFingerprint.decode(reader, reader.uint32(), undefined, q + 1, message.fingerprint);
                            message._fingerprint = "fingerprint";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.timestamp = reader.int64();
                            message._timestamp = "timestamp";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an AppStateSyncKeyData message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.AppStateSyncKeyData
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.AppStateSyncKeyData} AppStateSyncKeyData
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AppStateSyncKeyData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AppStateSyncKeyData message.
             * @function verify
             * @memberof proto.Message.AppStateSyncKeyData
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AppStateSyncKeyData.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.keyData != null && Object.hasOwnProperty.call(message, "keyData")) {
                    properties._keyData = 1;
                    if (!(message.keyData && typeof message.keyData.length === "number" || $util.isString(message.keyData)))
                        return "keyData: buffer expected";
                }
                if (message.fingerprint != null && Object.hasOwnProperty.call(message, "fingerprint")) {
                    properties._fingerprint = 1;
                    {
                        var error = $root.proto.Message.AppStateSyncKeyFingerprint.verify(message.fingerprint, q + 1);
                        if (error)
                            return "fingerprint." + error;
                    }
                }
                if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp")) {
                    properties._timestamp = 1;
                    if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                        return "timestamp: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates an AppStateSyncKeyData message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.AppStateSyncKeyData
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.AppStateSyncKeyData} AppStateSyncKeyData
             */
            AppStateSyncKeyData.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.AppStateSyncKeyData: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.keyData != null)
                    if (typeof object.keyData === "string")
                        $util.base64.decode(object.keyData, message.keyData = $util.newBuffer($util.base64.length(object.keyData)), 0);
                    else if (object.keyData.length >= 0)
                        message.keyData = object.keyData;
                if (object.fingerprint != null) {
                    if (!$util.isObject(object.fingerprint))
                        throw TypeError(".proto.Message.AppStateSyncKeyData.fingerprint: object expected");
                    message.fingerprint = $root.proto.Message.AppStateSyncKeyFingerprint.fromObject(object.fingerprint, q + 1);
                }
                if (object.timestamp != null)
                    if ($util.Long)
                        message.timestamp = $util.Long.fromValue(object.timestamp, false);
                    else if (typeof object.timestamp === "string")
                        message.timestamp = parseInt(object.timestamp, 10);
                    else if (typeof object.timestamp === "number")
                        message.timestamp = object.timestamp;
                    else if (typeof object.timestamp === "object")
                        message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from an AppStateSyncKeyData message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.AppStateSyncKeyData
             * @static
             * @param {proto.Message.AppStateSyncKeyData} message AppStateSyncKeyData
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AppStateSyncKeyData.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.keyData != null && Object.hasOwnProperty.call(message, "keyData"))
                    object.keyData = options.bytes === String ? $util.base64.encode(message.keyData, 0, message.keyData.length) : options.bytes === Array ? Array.prototype.slice.call(message.keyData) : message.keyData;
                if (message.fingerprint != null && Object.hasOwnProperty.call(message, "fingerprint"))
                    object.fingerprint = $root.proto.Message.AppStateSyncKeyFingerprint.toObject(message.fingerprint, options, q + 1);
                if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.timestamp = typeof message.timestamp === "number" ? BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.timestamp === "number")
                        object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                    else
                        object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
                return object;
            };

            /**
             * Converts this AppStateSyncKeyData to JSON.
             * @function toJSON
             * @memberof proto.Message.AppStateSyncKeyData
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AppStateSyncKeyData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AppStateSyncKeyData
             * @function getTypeUrl
             * @memberof proto.Message.AppStateSyncKeyData
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AppStateSyncKeyData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.AppStateSyncKeyData";
            };

            return AppStateSyncKeyData;
        })();

        Message.AppStateSyncKeyFingerprint = (function() {

            /**
             * Properties of an AppStateSyncKeyFingerprint.
             * @memberof proto.Message
             * @interface IAppStateSyncKeyFingerprint
             * @property {number|null} [rawId] AppStateSyncKeyFingerprint rawId
             * @property {number|null} [currentIndex] AppStateSyncKeyFingerprint currentIndex
             * @property {Array.<number>|null} [deviceIndexes] AppStateSyncKeyFingerprint deviceIndexes
             */

            /**
             * Constructs a new AppStateSyncKeyFingerprint.
             * @memberof proto.Message
             * @classdesc Represents an AppStateSyncKeyFingerprint.
             * @implements IAppStateSyncKeyFingerprint
             * @constructor
             * @param {proto.Message.IAppStateSyncKeyFingerprint=} [properties] Properties to set
             */
            function AppStateSyncKeyFingerprint(properties) {
                this.deviceIndexes = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AppStateSyncKeyFingerprint rawId.
             * @member {number|null|undefined} rawId
             * @memberof proto.Message.AppStateSyncKeyFingerprint
             * @instance
             */
            AppStateSyncKeyFingerprint.prototype.rawId = null;

            /**
             * AppStateSyncKeyFingerprint currentIndex.
             * @member {number|null|undefined} currentIndex
             * @memberof proto.Message.AppStateSyncKeyFingerprint
             * @instance
             */
            AppStateSyncKeyFingerprint.prototype.currentIndex = null;

            /**
             * AppStateSyncKeyFingerprint deviceIndexes.
             * @member {Array.<number>} deviceIndexes
             * @memberof proto.Message.AppStateSyncKeyFingerprint
             * @instance
             */
            AppStateSyncKeyFingerprint.prototype.deviceIndexes = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AppStateSyncKeyFingerprint.prototype, "_rawId", {
                get: $util.oneOfGetter($oneOfFields = ["rawId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AppStateSyncKeyFingerprint.prototype, "_currentIndex", {
                get: $util.oneOfGetter($oneOfFields = ["currentIndex"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AppStateSyncKeyFingerprint instance using the specified properties.
             * @function create
             * @memberof proto.Message.AppStateSyncKeyFingerprint
             * @static
             * @param {proto.Message.IAppStateSyncKeyFingerprint=} [properties] Properties to set
             * @returns {proto.Message.AppStateSyncKeyFingerprint} AppStateSyncKeyFingerprint instance
             */
            AppStateSyncKeyFingerprint.create = function create(properties) {
                return new AppStateSyncKeyFingerprint(properties);
            };

            /**
             * Encodes the specified AppStateSyncKeyFingerprint message. Does not implicitly {@link proto.Message.AppStateSyncKeyFingerprint.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.AppStateSyncKeyFingerprint
             * @static
             * @param {proto.Message.IAppStateSyncKeyFingerprint} message AppStateSyncKeyFingerprint message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AppStateSyncKeyFingerprint.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.rawId != null && Object.hasOwnProperty.call(message, "rawId"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.rawId);
                if (message.currentIndex != null && Object.hasOwnProperty.call(message, "currentIndex"))
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.currentIndex);
                if (message.deviceIndexes != null && message.deviceIndexes.length) {
                    writer.uint32(/* id 3, wireType 2 =*/26).fork();
                    for (var i = 0; i < message.deviceIndexes.length; ++i)
                        writer.uint32(message.deviceIndexes[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AppStateSyncKeyFingerprint message, length delimited. Does not implicitly {@link proto.Message.AppStateSyncKeyFingerprint.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.AppStateSyncKeyFingerprint
             * @static
             * @param {proto.Message.IAppStateSyncKeyFingerprint} message AppStateSyncKeyFingerprint message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AppStateSyncKeyFingerprint.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an AppStateSyncKeyFingerprint message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.AppStateSyncKeyFingerprint
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.AppStateSyncKeyFingerprint} AppStateSyncKeyFingerprint
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AppStateSyncKeyFingerprint.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.rawId = reader.uint32();
                            message._rawId = "rawId";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.currentIndex = reader.uint32();
                            message._currentIndex = "currentIndex";
                            continue;
                        }
                    case 3: {
                            if (u === 2) {
                                if (!(message.deviceIndexes && message.deviceIndexes.length))
                                    message.deviceIndexes = [];
                                var end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.deviceIndexes.push(reader.uint32());
                                continue;
                            }
                            if (u !== 0)
                                break;
                            if (!(message.deviceIndexes && message.deviceIndexes.length))
                                message.deviceIndexes = [];
                            message.deviceIndexes.push(reader.uint32());
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an AppStateSyncKeyFingerprint message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.AppStateSyncKeyFingerprint
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.AppStateSyncKeyFingerprint} AppStateSyncKeyFingerprint
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AppStateSyncKeyFingerprint.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AppStateSyncKeyFingerprint message.
             * @function verify
             * @memberof proto.Message.AppStateSyncKeyFingerprint
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AppStateSyncKeyFingerprint.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.rawId != null && Object.hasOwnProperty.call(message, "rawId")) {
                    properties._rawId = 1;
                    if (!$util.isInteger(message.rawId))
                        return "rawId: integer expected";
                }
                if (message.currentIndex != null && Object.hasOwnProperty.call(message, "currentIndex")) {
                    properties._currentIndex = 1;
                    if (!$util.isInteger(message.currentIndex))
                        return "currentIndex: integer expected";
                }
                if (message.deviceIndexes != null && Object.hasOwnProperty.call(message, "deviceIndexes")) {
                    if (!Array.isArray(message.deviceIndexes))
                        return "deviceIndexes: array expected";
                    for (var i = 0; i < message.deviceIndexes.length; ++i)
                        if (!$util.isInteger(message.deviceIndexes[i]))
                            return "deviceIndexes: integer[] expected";
                }
                return null;
            };

            /**
             * Creates an AppStateSyncKeyFingerprint message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.AppStateSyncKeyFingerprint
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.AppStateSyncKeyFingerprint} AppStateSyncKeyFingerprint
             */
            AppStateSyncKeyFingerprint.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.AppStateSyncKeyFingerprint: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.rawId != null)
                    message.rawId = object.rawId >>> 0;
                if (object.currentIndex != null)
                    message.currentIndex = object.currentIndex >>> 0;
                if (object.deviceIndexes) {
                    if (!Array.isArray(object.deviceIndexes))
                        throw TypeError(".proto.Message.AppStateSyncKeyFingerprint.deviceIndexes: array expected");
                    message.deviceIndexes = Array(object.deviceIndexes.length);
                    for (var i = 0; i < object.deviceIndexes.length; ++i)
                        message.deviceIndexes[i] = object.deviceIndexes[i] >>> 0;
                }
                return message;
            };

            /**
             * Creates a plain object from an AppStateSyncKeyFingerprint message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.AppStateSyncKeyFingerprint
             * @static
             * @param {proto.Message.AppStateSyncKeyFingerprint} message AppStateSyncKeyFingerprint
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AppStateSyncKeyFingerprint.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.deviceIndexes = [];
                if (message.rawId != null && Object.hasOwnProperty.call(message, "rawId"))
                    object.rawId = message.rawId;
                if (message.currentIndex != null && Object.hasOwnProperty.call(message, "currentIndex"))
                    object.currentIndex = message.currentIndex;
                if (message.deviceIndexes && message.deviceIndexes.length) {
                    object.deviceIndexes = Array(message.deviceIndexes.length);
                    for (var j = 0; j < message.deviceIndexes.length; ++j)
                        object.deviceIndexes[j] = message.deviceIndexes[j];
                }
                return object;
            };

            /**
             * Converts this AppStateSyncKeyFingerprint to JSON.
             * @function toJSON
             * @memberof proto.Message.AppStateSyncKeyFingerprint
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AppStateSyncKeyFingerprint.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AppStateSyncKeyFingerprint
             * @function getTypeUrl
             * @memberof proto.Message.AppStateSyncKeyFingerprint
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AppStateSyncKeyFingerprint.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.AppStateSyncKeyFingerprint";
            };

            return AppStateSyncKeyFingerprint;
        })();

        Message.AppStateSyncKeyId = (function() {

            /**
             * Properties of an AppStateSyncKeyId.
             * @memberof proto.Message
             * @interface IAppStateSyncKeyId
             * @property {Uint8Array|null} [keyId] AppStateSyncKeyId keyId
             */

            /**
             * Constructs a new AppStateSyncKeyId.
             * @memberof proto.Message
             * @classdesc Represents an AppStateSyncKeyId.
             * @implements IAppStateSyncKeyId
             * @constructor
             * @param {proto.Message.IAppStateSyncKeyId=} [properties] Properties to set
             */
            function AppStateSyncKeyId(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AppStateSyncKeyId keyId.
             * @member {Uint8Array|null|undefined} keyId
             * @memberof proto.Message.AppStateSyncKeyId
             * @instance
             */
            AppStateSyncKeyId.prototype.keyId = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AppStateSyncKeyId.prototype, "_keyId", {
                get: $util.oneOfGetter($oneOfFields = ["keyId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AppStateSyncKeyId instance using the specified properties.
             * @function create
             * @memberof proto.Message.AppStateSyncKeyId
             * @static
             * @param {proto.Message.IAppStateSyncKeyId=} [properties] Properties to set
             * @returns {proto.Message.AppStateSyncKeyId} AppStateSyncKeyId instance
             */
            AppStateSyncKeyId.create = function create(properties) {
                return new AppStateSyncKeyId(properties);
            };

            /**
             * Encodes the specified AppStateSyncKeyId message. Does not implicitly {@link proto.Message.AppStateSyncKeyId.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.AppStateSyncKeyId
             * @static
             * @param {proto.Message.IAppStateSyncKeyId} message AppStateSyncKeyId message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AppStateSyncKeyId.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.keyId != null && Object.hasOwnProperty.call(message, "keyId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.keyId);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AppStateSyncKeyId message, length delimited. Does not implicitly {@link proto.Message.AppStateSyncKeyId.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.AppStateSyncKeyId
             * @static
             * @param {proto.Message.IAppStateSyncKeyId} message AppStateSyncKeyId message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AppStateSyncKeyId.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an AppStateSyncKeyId message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.AppStateSyncKeyId
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.AppStateSyncKeyId} AppStateSyncKeyId
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AppStateSyncKeyId.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.keyId = reader.bytes();
                            message._keyId = "keyId";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an AppStateSyncKeyId message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.AppStateSyncKeyId
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.AppStateSyncKeyId} AppStateSyncKeyId
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AppStateSyncKeyId.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AppStateSyncKeyId message.
             * @function verify
             * @memberof proto.Message.AppStateSyncKeyId
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AppStateSyncKeyId.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.keyId != null && Object.hasOwnProperty.call(message, "keyId")) {
                    properties._keyId = 1;
                    if (!(message.keyId && typeof message.keyId.length === "number" || $util.isString(message.keyId)))
                        return "keyId: buffer expected";
                }
                return null;
            };

            /**
             * Creates an AppStateSyncKeyId message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.AppStateSyncKeyId
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.AppStateSyncKeyId} AppStateSyncKeyId
             */
            AppStateSyncKeyId.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.AppStateSyncKeyId: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.keyId != null)
                    if (typeof object.keyId === "string")
                        $util.base64.decode(object.keyId, message.keyId = $util.newBuffer($util.base64.length(object.keyId)), 0);
                    else if (object.keyId.length >= 0)
                        message.keyId = object.keyId;
                return message;
            };

            /**
             * Creates a plain object from an AppStateSyncKeyId message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.AppStateSyncKeyId
             * @static
             * @param {proto.Message.AppStateSyncKeyId} message AppStateSyncKeyId
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AppStateSyncKeyId.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.keyId != null && Object.hasOwnProperty.call(message, "keyId"))
                    object.keyId = options.bytes === String ? $util.base64.encode(message.keyId, 0, message.keyId.length) : options.bytes === Array ? Array.prototype.slice.call(message.keyId) : message.keyId;
                return object;
            };

            /**
             * Converts this AppStateSyncKeyId to JSON.
             * @function toJSON
             * @memberof proto.Message.AppStateSyncKeyId
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AppStateSyncKeyId.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AppStateSyncKeyId
             * @function getTypeUrl
             * @memberof proto.Message.AppStateSyncKeyId
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AppStateSyncKeyId.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.AppStateSyncKeyId";
            };

            return AppStateSyncKeyId;
        })();

        Message.AppStateSyncKeyRequest = (function() {

            /**
             * Properties of an AppStateSyncKeyRequest.
             * @memberof proto.Message
             * @interface IAppStateSyncKeyRequest
             * @property {Array.<proto.Message.IAppStateSyncKeyId>|null} [keyIds] AppStateSyncKeyRequest keyIds
             */

            /**
             * Constructs a new AppStateSyncKeyRequest.
             * @memberof proto.Message
             * @classdesc Represents an AppStateSyncKeyRequest.
             * @implements IAppStateSyncKeyRequest
             * @constructor
             * @param {proto.Message.IAppStateSyncKeyRequest=} [properties] Properties to set
             */
            function AppStateSyncKeyRequest(properties) {
                this.keyIds = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AppStateSyncKeyRequest keyIds.
             * @member {Array.<proto.Message.IAppStateSyncKeyId>} keyIds
             * @memberof proto.Message.AppStateSyncKeyRequest
             * @instance
             */
            AppStateSyncKeyRequest.prototype.keyIds = $util.emptyArray;

            /**
             * Creates a new AppStateSyncKeyRequest instance using the specified properties.
             * @function create
             * @memberof proto.Message.AppStateSyncKeyRequest
             * @static
             * @param {proto.Message.IAppStateSyncKeyRequest=} [properties] Properties to set
             * @returns {proto.Message.AppStateSyncKeyRequest} AppStateSyncKeyRequest instance
             */
            AppStateSyncKeyRequest.create = function create(properties) {
                return new AppStateSyncKeyRequest(properties);
            };

            /**
             * Encodes the specified AppStateSyncKeyRequest message. Does not implicitly {@link proto.Message.AppStateSyncKeyRequest.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.AppStateSyncKeyRequest
             * @static
             * @param {proto.Message.IAppStateSyncKeyRequest} message AppStateSyncKeyRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AppStateSyncKeyRequest.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.keyIds != null && message.keyIds.length)
                    for (var i = 0; i < message.keyIds.length; ++i)
                        $root.proto.Message.AppStateSyncKeyId.encode(message.keyIds[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AppStateSyncKeyRequest message, length delimited. Does not implicitly {@link proto.Message.AppStateSyncKeyRequest.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.AppStateSyncKeyRequest
             * @static
             * @param {proto.Message.IAppStateSyncKeyRequest} message AppStateSyncKeyRequest message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AppStateSyncKeyRequest.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an AppStateSyncKeyRequest message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.AppStateSyncKeyRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.AppStateSyncKeyRequest} AppStateSyncKeyRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AppStateSyncKeyRequest.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(message.keyIds && message.keyIds.length))
                                message.keyIds = [];
                            message.keyIds.push($root.proto.Message.AppStateSyncKeyId.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an AppStateSyncKeyRequest message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.AppStateSyncKeyRequest
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.AppStateSyncKeyRequest} AppStateSyncKeyRequest
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AppStateSyncKeyRequest.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AppStateSyncKeyRequest message.
             * @function verify
             * @memberof proto.Message.AppStateSyncKeyRequest
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AppStateSyncKeyRequest.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.keyIds != null && Object.hasOwnProperty.call(message, "keyIds")) {
                    if (!Array.isArray(message.keyIds))
                        return "keyIds: array expected";
                    for (var i = 0; i < message.keyIds.length; ++i) {
                        var error = $root.proto.Message.AppStateSyncKeyId.verify(message.keyIds[i], q + 1);
                        if (error)
                            return "keyIds." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an AppStateSyncKeyRequest message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.AppStateSyncKeyRequest
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.AppStateSyncKeyRequest} AppStateSyncKeyRequest
             */
            AppStateSyncKeyRequest.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.AppStateSyncKeyRequest: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.keyIds) {
                    if (!Array.isArray(object.keyIds))
                        throw TypeError(".proto.Message.AppStateSyncKeyRequest.keyIds: array expected");
                    message.keyIds = Array(object.keyIds.length);
                    for (var i = 0; i < object.keyIds.length; ++i) {
                        if (!$util.isObject(object.keyIds[i]))
                            throw TypeError(".proto.Message.AppStateSyncKeyRequest.keyIds: object expected");
                        message.keyIds[i] = $root.proto.Message.AppStateSyncKeyId.fromObject(object.keyIds[i], q + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from an AppStateSyncKeyRequest message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.AppStateSyncKeyRequest
             * @static
             * @param {proto.Message.AppStateSyncKeyRequest} message AppStateSyncKeyRequest
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AppStateSyncKeyRequest.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.keyIds = [];
                if (message.keyIds && message.keyIds.length) {
                    object.keyIds = Array(message.keyIds.length);
                    for (var j = 0; j < message.keyIds.length; ++j)
                        object.keyIds[j] = $root.proto.Message.AppStateSyncKeyId.toObject(message.keyIds[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this AppStateSyncKeyRequest to JSON.
             * @function toJSON
             * @memberof proto.Message.AppStateSyncKeyRequest
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AppStateSyncKeyRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AppStateSyncKeyRequest
             * @function getTypeUrl
             * @memberof proto.Message.AppStateSyncKeyRequest
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AppStateSyncKeyRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.AppStateSyncKeyRequest";
            };

            return AppStateSyncKeyRequest;
        })();

        Message.AppStateSyncKeyShare = (function() {

            /**
             * Properties of an AppStateSyncKeyShare.
             * @memberof proto.Message
             * @interface IAppStateSyncKeyShare
             * @property {Array.<proto.Message.IAppStateSyncKey>|null} [keys] AppStateSyncKeyShare keys
             */

            /**
             * Constructs a new AppStateSyncKeyShare.
             * @memberof proto.Message
             * @classdesc Represents an AppStateSyncKeyShare.
             * @implements IAppStateSyncKeyShare
             * @constructor
             * @param {proto.Message.IAppStateSyncKeyShare=} [properties] Properties to set
             */
            function AppStateSyncKeyShare(properties) {
                this.keys = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AppStateSyncKeyShare keys.
             * @member {Array.<proto.Message.IAppStateSyncKey>} keys
             * @memberof proto.Message.AppStateSyncKeyShare
             * @instance
             */
            AppStateSyncKeyShare.prototype.keys = $util.emptyArray;

            /**
             * Creates a new AppStateSyncKeyShare instance using the specified properties.
             * @function create
             * @memberof proto.Message.AppStateSyncKeyShare
             * @static
             * @param {proto.Message.IAppStateSyncKeyShare=} [properties] Properties to set
             * @returns {proto.Message.AppStateSyncKeyShare} AppStateSyncKeyShare instance
             */
            AppStateSyncKeyShare.create = function create(properties) {
                return new AppStateSyncKeyShare(properties);
            };

            /**
             * Encodes the specified AppStateSyncKeyShare message. Does not implicitly {@link proto.Message.AppStateSyncKeyShare.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.AppStateSyncKeyShare
             * @static
             * @param {proto.Message.IAppStateSyncKeyShare} message AppStateSyncKeyShare message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AppStateSyncKeyShare.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.keys != null && message.keys.length)
                    for (var i = 0; i < message.keys.length; ++i)
                        $root.proto.Message.AppStateSyncKey.encode(message.keys[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AppStateSyncKeyShare message, length delimited. Does not implicitly {@link proto.Message.AppStateSyncKeyShare.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.AppStateSyncKeyShare
             * @static
             * @param {proto.Message.IAppStateSyncKeyShare} message AppStateSyncKeyShare message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AppStateSyncKeyShare.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an AppStateSyncKeyShare message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.AppStateSyncKeyShare
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.AppStateSyncKeyShare} AppStateSyncKeyShare
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AppStateSyncKeyShare.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(message.keys && message.keys.length))
                                message.keys = [];
                            message.keys.push($root.proto.Message.AppStateSyncKey.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an AppStateSyncKeyShare message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.AppStateSyncKeyShare
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.AppStateSyncKeyShare} AppStateSyncKeyShare
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AppStateSyncKeyShare.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AppStateSyncKeyShare message.
             * @function verify
             * @memberof proto.Message.AppStateSyncKeyShare
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AppStateSyncKeyShare.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.keys != null && Object.hasOwnProperty.call(message, "keys")) {
                    if (!Array.isArray(message.keys))
                        return "keys: array expected";
                    for (var i = 0; i < message.keys.length; ++i) {
                        var error = $root.proto.Message.AppStateSyncKey.verify(message.keys[i], q + 1);
                        if (error)
                            return "keys." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an AppStateSyncKeyShare message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.AppStateSyncKeyShare
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.AppStateSyncKeyShare} AppStateSyncKeyShare
             */
            AppStateSyncKeyShare.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.AppStateSyncKeyShare: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.keys) {
                    if (!Array.isArray(object.keys))
                        throw TypeError(".proto.Message.AppStateSyncKeyShare.keys: array expected");
                    message.keys = Array(object.keys.length);
                    for (var i = 0; i < object.keys.length; ++i) {
                        if (!$util.isObject(object.keys[i]))
                            throw TypeError(".proto.Message.AppStateSyncKeyShare.keys: object expected");
                        message.keys[i] = $root.proto.Message.AppStateSyncKey.fromObject(object.keys[i], q + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from an AppStateSyncKeyShare message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.AppStateSyncKeyShare
             * @static
             * @param {proto.Message.AppStateSyncKeyShare} message AppStateSyncKeyShare
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AppStateSyncKeyShare.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.keys = [];
                if (message.keys && message.keys.length) {
                    object.keys = Array(message.keys.length);
                    for (var j = 0; j < message.keys.length; ++j)
                        object.keys[j] = $root.proto.Message.AppStateSyncKey.toObject(message.keys[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this AppStateSyncKeyShare to JSON.
             * @function toJSON
             * @memberof proto.Message.AppStateSyncKeyShare
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AppStateSyncKeyShare.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AppStateSyncKeyShare
             * @function getTypeUrl
             * @memberof proto.Message.AppStateSyncKeyShare
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AppStateSyncKeyShare.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.AppStateSyncKeyShare";
            };

            return AppStateSyncKeyShare;
        })();

        Message.AudioMessage = (function() {

            /**
             * Properties of an AudioMessage.
             * @memberof proto.Message
             * @interface IAudioMessage
             * @property {string|null} [url] AudioMessage url
             * @property {string|null} [mimetype] AudioMessage mimetype
             * @property {Uint8Array|null} [fileSha256] AudioMessage fileSha256
             * @property {number|Long|null} [fileLength] AudioMessage fileLength
             * @property {number|null} [seconds] AudioMessage seconds
             * @property {boolean|null} [ptt] AudioMessage ptt
             * @property {Uint8Array|null} [mediaKey] AudioMessage mediaKey
             * @property {Uint8Array|null} [fileEncSha256] AudioMessage fileEncSha256
             * @property {string|null} [directPath] AudioMessage directPath
             * @property {number|Long|null} [mediaKeyTimestamp] AudioMessage mediaKeyTimestamp
             * @property {proto.IContextInfo|null} [contextInfo] AudioMessage contextInfo
             * @property {Uint8Array|null} [streamingSidecar] AudioMessage streamingSidecar
             * @property {Uint8Array|null} [waveform] AudioMessage waveform
             * @property {number|null} [backgroundArgb] AudioMessage backgroundArgb
             * @property {boolean|null} [viewOnce] AudioMessage viewOnce
             * @property {string|null} [accessibilityLabel] AudioMessage accessibilityLabel
             */

            /**
             * Constructs a new AudioMessage.
             * @memberof proto.Message
             * @classdesc Represents an AudioMessage.
             * @implements IAudioMessage
             * @constructor
             * @param {proto.Message.IAudioMessage=} [properties] Properties to set
             */
            function AudioMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AudioMessage url.
             * @member {string|null|undefined} url
             * @memberof proto.Message.AudioMessage
             * @instance
             */
            AudioMessage.prototype.url = null;

            /**
             * AudioMessage mimetype.
             * @member {string|null|undefined} mimetype
             * @memberof proto.Message.AudioMessage
             * @instance
             */
            AudioMessage.prototype.mimetype = null;

            /**
             * AudioMessage fileSha256.
             * @member {Uint8Array|null|undefined} fileSha256
             * @memberof proto.Message.AudioMessage
             * @instance
             */
            AudioMessage.prototype.fileSha256 = null;

            /**
             * AudioMessage fileLength.
             * @member {number|Long|null|undefined} fileLength
             * @memberof proto.Message.AudioMessage
             * @instance
             */
            AudioMessage.prototype.fileLength = null;

            /**
             * AudioMessage seconds.
             * @member {number|null|undefined} seconds
             * @memberof proto.Message.AudioMessage
             * @instance
             */
            AudioMessage.prototype.seconds = null;

            /**
             * AudioMessage ptt.
             * @member {boolean|null|undefined} ptt
             * @memberof proto.Message.AudioMessage
             * @instance
             */
            AudioMessage.prototype.ptt = null;

            /**
             * AudioMessage mediaKey.
             * @member {Uint8Array|null|undefined} mediaKey
             * @memberof proto.Message.AudioMessage
             * @instance
             */
            AudioMessage.prototype.mediaKey = null;

            /**
             * AudioMessage fileEncSha256.
             * @member {Uint8Array|null|undefined} fileEncSha256
             * @memberof proto.Message.AudioMessage
             * @instance
             */
            AudioMessage.prototype.fileEncSha256 = null;

            /**
             * AudioMessage directPath.
             * @member {string|null|undefined} directPath
             * @memberof proto.Message.AudioMessage
             * @instance
             */
            AudioMessage.prototype.directPath = null;

            /**
             * AudioMessage mediaKeyTimestamp.
             * @member {number|Long|null|undefined} mediaKeyTimestamp
             * @memberof proto.Message.AudioMessage
             * @instance
             */
            AudioMessage.prototype.mediaKeyTimestamp = null;

            /**
             * AudioMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.AudioMessage
             * @instance
             */
            AudioMessage.prototype.contextInfo = null;

            /**
             * AudioMessage streamingSidecar.
             * @member {Uint8Array|null|undefined} streamingSidecar
             * @memberof proto.Message.AudioMessage
             * @instance
             */
            AudioMessage.prototype.streamingSidecar = null;

            /**
             * AudioMessage waveform.
             * @member {Uint8Array|null|undefined} waveform
             * @memberof proto.Message.AudioMessage
             * @instance
             */
            AudioMessage.prototype.waveform = null;

            /**
             * AudioMessage backgroundArgb.
             * @member {number|null|undefined} backgroundArgb
             * @memberof proto.Message.AudioMessage
             * @instance
             */
            AudioMessage.prototype.backgroundArgb = null;

            /**
             * AudioMessage viewOnce.
             * @member {boolean|null|undefined} viewOnce
             * @memberof proto.Message.AudioMessage
             * @instance
             */
            AudioMessage.prototype.viewOnce = null;

            /**
             * AudioMessage accessibilityLabel.
             * @member {string|null|undefined} accessibilityLabel
             * @memberof proto.Message.AudioMessage
             * @instance
             */
            AudioMessage.prototype.accessibilityLabel = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AudioMessage.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AudioMessage.prototype, "_mimetype", {
                get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AudioMessage.prototype, "_fileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AudioMessage.prototype, "_fileLength", {
                get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AudioMessage.prototype, "_seconds", {
                get: $util.oneOfGetter($oneOfFields = ["seconds"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AudioMessage.prototype, "_ptt", {
                get: $util.oneOfGetter($oneOfFields = ["ptt"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AudioMessage.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AudioMessage.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AudioMessage.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AudioMessage.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AudioMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AudioMessage.prototype, "_streamingSidecar", {
                get: $util.oneOfGetter($oneOfFields = ["streamingSidecar"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AudioMessage.prototype, "_waveform", {
                get: $util.oneOfGetter($oneOfFields = ["waveform"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AudioMessage.prototype, "_backgroundArgb", {
                get: $util.oneOfGetter($oneOfFields = ["backgroundArgb"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AudioMessage.prototype, "_viewOnce", {
                get: $util.oneOfGetter($oneOfFields = ["viewOnce"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AudioMessage.prototype, "_accessibilityLabel", {
                get: $util.oneOfGetter($oneOfFields = ["accessibilityLabel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AudioMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.AudioMessage
             * @static
             * @param {proto.Message.IAudioMessage=} [properties] Properties to set
             * @returns {proto.Message.AudioMessage} AudioMessage instance
             */
            AudioMessage.create = function create(properties) {
                return new AudioMessage(properties);
            };

            /**
             * Encodes the specified AudioMessage message. Does not implicitly {@link proto.Message.AudioMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.AudioMessage
             * @static
             * @param {proto.Message.IAudioMessage} message AudioMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AudioMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.url);
                if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.mimetype);
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.fileSha256);
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.fileLength);
                if (message.seconds != null && Object.hasOwnProperty.call(message, "seconds"))
                    writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.seconds);
                if (message.ptt != null && Object.hasOwnProperty.call(message, "ptt"))
                    writer.uint32(/* id 6, wireType 0 =*/48).bool(message.ptt);
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.mediaKey);
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.fileEncSha256);
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.directPath);
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                    writer.uint32(/* id 10, wireType 0 =*/80).int64(message.mediaKeyTimestamp);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
                if (message.streamingSidecar != null && Object.hasOwnProperty.call(message, "streamingSidecar"))
                    writer.uint32(/* id 18, wireType 2 =*/146).bytes(message.streamingSidecar);
                if (message.waveform != null && Object.hasOwnProperty.call(message, "waveform"))
                    writer.uint32(/* id 19, wireType 2 =*/154).bytes(message.waveform);
                if (message.backgroundArgb != null && Object.hasOwnProperty.call(message, "backgroundArgb"))
                    writer.uint32(/* id 20, wireType 5 =*/165).fixed32(message.backgroundArgb);
                if (message.viewOnce != null && Object.hasOwnProperty.call(message, "viewOnce"))
                    writer.uint32(/* id 21, wireType 0 =*/168).bool(message.viewOnce);
                if (message.accessibilityLabel != null && Object.hasOwnProperty.call(message, "accessibilityLabel"))
                    writer.uint32(/* id 22, wireType 2 =*/178).string(message.accessibilityLabel);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AudioMessage message, length delimited. Does not implicitly {@link proto.Message.AudioMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.AudioMessage
             * @static
             * @param {proto.Message.IAudioMessage} message AudioMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AudioMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an AudioMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.AudioMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.AudioMessage} AudioMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AudioMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.url = reader.stringVerify();
                            message._url = "url";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.mimetype = reader.stringVerify();
                            message._mimetype = "mimetype";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.fileSha256 = reader.bytes();
                            message._fileSha256 = "fileSha256";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.fileLength = reader.uint64();
                            message._fileLength = "fileLength";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.seconds = reader.uint32();
                            message._seconds = "seconds";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.ptt = reader.bool();
                            message._ptt = "ptt";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.mediaKey = reader.bytes();
                            message._mediaKey = "mediaKey";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.fileEncSha256 = reader.bytes();
                            message._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            message.directPath = reader.stringVerify();
                            message._directPath = "directPath";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            message.mediaKeyTimestamp = reader.int64();
                            message._mediaKeyTimestamp = "mediaKeyTimestamp";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 18: {
                            if (u !== 2)
                                break;
                            message.streamingSidecar = reader.bytes();
                            message._streamingSidecar = "streamingSidecar";
                            continue;
                        }
                    case 19: {
                            if (u !== 2)
                                break;
                            message.waveform = reader.bytes();
                            message._waveform = "waveform";
                            continue;
                        }
                    case 20: {
                            if (u !== 5)
                                break;
                            message.backgroundArgb = reader.fixed32();
                            message._backgroundArgb = "backgroundArgb";
                            continue;
                        }
                    case 21: {
                            if (u !== 0)
                                break;
                            message.viewOnce = reader.bool();
                            message._viewOnce = "viewOnce";
                            continue;
                        }
                    case 22: {
                            if (u !== 2)
                                break;
                            message.accessibilityLabel = reader.stringVerify();
                            message._accessibilityLabel = "accessibilityLabel";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an AudioMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.AudioMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.AudioMessage} AudioMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AudioMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AudioMessage message.
             * @function verify
             * @memberof proto.Message.AudioMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AudioMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.url != null && Object.hasOwnProperty.call(message, "url")) {
                    properties._url = 1;
                    if (!$util.isString(message.url))
                        return "url: string expected";
                }
                if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype")) {
                    properties._mimetype = 1;
                    if (!$util.isString(message.mimetype))
                        return "mimetype: string expected";
                }
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256")) {
                    properties._fileSha256 = 1;
                    if (!(message.fileSha256 && typeof message.fileSha256.length === "number" || $util.isString(message.fileSha256)))
                        return "fileSha256: buffer expected";
                }
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength")) {
                    properties._fileLength = 1;
                    if (!$util.isInteger(message.fileLength) && !(message.fileLength && $util.isInteger(message.fileLength.low) && $util.isInteger(message.fileLength.high)))
                        return "fileLength: integer|Long expected";
                }
                if (message.seconds != null && Object.hasOwnProperty.call(message, "seconds")) {
                    properties._seconds = 1;
                    if (!$util.isInteger(message.seconds))
                        return "seconds: integer expected";
                }
                if (message.ptt != null && Object.hasOwnProperty.call(message, "ptt")) {
                    properties._ptt = 1;
                    if (typeof message.ptt !== "boolean")
                        return "ptt: boolean expected";
                }
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey")) {
                    properties._mediaKey = 1;
                    if (!(message.mediaKey && typeof message.mediaKey.length === "number" || $util.isString(message.mediaKey)))
                        return "mediaKey: buffer expected";
                }
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256")) {
                    properties._fileEncSha256 = 1;
                    if (!(message.fileEncSha256 && typeof message.fileEncSha256.length === "number" || $util.isString(message.fileEncSha256)))
                        return "fileEncSha256: buffer expected";
                }
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath")) {
                    properties._directPath = 1;
                    if (!$util.isString(message.directPath))
                        return "directPath: string expected";
                }
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp")) {
                    properties._mediaKeyTimestamp = 1;
                    if (!$util.isInteger(message.mediaKeyTimestamp) && !(message.mediaKeyTimestamp && $util.isInteger(message.mediaKeyTimestamp.low) && $util.isInteger(message.mediaKeyTimestamp.high)))
                        return "mediaKeyTimestamp: integer|Long expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.streamingSidecar != null && Object.hasOwnProperty.call(message, "streamingSidecar")) {
                    properties._streamingSidecar = 1;
                    if (!(message.streamingSidecar && typeof message.streamingSidecar.length === "number" || $util.isString(message.streamingSidecar)))
                        return "streamingSidecar: buffer expected";
                }
                if (message.waveform != null && Object.hasOwnProperty.call(message, "waveform")) {
                    properties._waveform = 1;
                    if (!(message.waveform && typeof message.waveform.length === "number" || $util.isString(message.waveform)))
                        return "waveform: buffer expected";
                }
                if (message.backgroundArgb != null && Object.hasOwnProperty.call(message, "backgroundArgb")) {
                    properties._backgroundArgb = 1;
                    if (!$util.isInteger(message.backgroundArgb))
                        return "backgroundArgb: integer expected";
                }
                if (message.viewOnce != null && Object.hasOwnProperty.call(message, "viewOnce")) {
                    properties._viewOnce = 1;
                    if (typeof message.viewOnce !== "boolean")
                        return "viewOnce: boolean expected";
                }
                if (message.accessibilityLabel != null && Object.hasOwnProperty.call(message, "accessibilityLabel")) {
                    properties._accessibilityLabel = 1;
                    if (!$util.isString(message.accessibilityLabel))
                        return "accessibilityLabel: string expected";
                }
                return null;
            };

            /**
             * Creates an AudioMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.AudioMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.AudioMessage} AudioMessage
             */
            AudioMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.AudioMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.url != null)
                    message.url = String(object.url);
                if (object.mimetype != null)
                    message.mimetype = String(object.mimetype);
                if (object.fileSha256 != null)
                    if (typeof object.fileSha256 === "string")
                        $util.base64.decode(object.fileSha256, message.fileSha256 = $util.newBuffer($util.base64.length(object.fileSha256)), 0);
                    else if (object.fileSha256.length >= 0)
                        message.fileSha256 = object.fileSha256;
                if (object.fileLength != null)
                    if ($util.Long)
                        message.fileLength = $util.Long.fromValue(object.fileLength, true);
                    else if (typeof object.fileLength === "string")
                        message.fileLength = parseInt(object.fileLength, 10);
                    else if (typeof object.fileLength === "number")
                        message.fileLength = object.fileLength;
                    else if (typeof object.fileLength === "object")
                        message.fileLength = new $util.LongBits(object.fileLength.low >>> 0, object.fileLength.high >>> 0).toNumber(true);
                if (object.seconds != null)
                    message.seconds = object.seconds >>> 0;
                if (object.ptt != null)
                    message.ptt = Boolean(object.ptt);
                if (object.mediaKey != null)
                    if (typeof object.mediaKey === "string")
                        $util.base64.decode(object.mediaKey, message.mediaKey = $util.newBuffer($util.base64.length(object.mediaKey)), 0);
                    else if (object.mediaKey.length >= 0)
                        message.mediaKey = object.mediaKey;
                if (object.fileEncSha256 != null)
                    if (typeof object.fileEncSha256 === "string")
                        $util.base64.decode(object.fileEncSha256, message.fileEncSha256 = $util.newBuffer($util.base64.length(object.fileEncSha256)), 0);
                    else if (object.fileEncSha256.length >= 0)
                        message.fileEncSha256 = object.fileEncSha256;
                if (object.directPath != null)
                    message.directPath = String(object.directPath);
                if (object.mediaKeyTimestamp != null)
                    if ($util.Long)
                        message.mediaKeyTimestamp = $util.Long.fromValue(object.mediaKeyTimestamp, false);
                    else if (typeof object.mediaKeyTimestamp === "string")
                        message.mediaKeyTimestamp = parseInt(object.mediaKeyTimestamp, 10);
                    else if (typeof object.mediaKeyTimestamp === "number")
                        message.mediaKeyTimestamp = object.mediaKeyTimestamp;
                    else if (typeof object.mediaKeyTimestamp === "object")
                        message.mediaKeyTimestamp = new $util.LongBits(object.mediaKeyTimestamp.low >>> 0, object.mediaKeyTimestamp.high >>> 0).toNumber();
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.AudioMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                if (object.streamingSidecar != null)
                    if (typeof object.streamingSidecar === "string")
                        $util.base64.decode(object.streamingSidecar, message.streamingSidecar = $util.newBuffer($util.base64.length(object.streamingSidecar)), 0);
                    else if (object.streamingSidecar.length >= 0)
                        message.streamingSidecar = object.streamingSidecar;
                if (object.waveform != null)
                    if (typeof object.waveform === "string")
                        $util.base64.decode(object.waveform, message.waveform = $util.newBuffer($util.base64.length(object.waveform)), 0);
                    else if (object.waveform.length >= 0)
                        message.waveform = object.waveform;
                if (object.backgroundArgb != null)
                    message.backgroundArgb = object.backgroundArgb >>> 0;
                if (object.viewOnce != null)
                    message.viewOnce = Boolean(object.viewOnce);
                if (object.accessibilityLabel != null)
                    message.accessibilityLabel = String(object.accessibilityLabel);
                return message;
            };

            /**
             * Creates a plain object from an AudioMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.AudioMessage
             * @static
             * @param {proto.Message.AudioMessage} message AudioMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AudioMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    object.url = message.url;
                if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                    object.mimetype = message.mimetype;
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                    object.fileSha256 = options.bytes === String ? $util.base64.encode(message.fileSha256, 0, message.fileSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileSha256) : message.fileSha256;
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.fileLength = typeof message.fileLength === "number" ? BigInt(message.fileLength) : $util.Long.fromBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0, true).toBigInt();
                    else if (typeof message.fileLength === "number")
                        object.fileLength = options.longs === String ? String(message.fileLength) : message.fileLength;
                    else
                        object.fileLength = options.longs === String ? $util.Long.prototype.toString.call(message.fileLength) : options.longs === Number ? new $util.LongBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0).toNumber(true) : message.fileLength;
                if (message.seconds != null && Object.hasOwnProperty.call(message, "seconds"))
                    object.seconds = message.seconds;
                if (message.ptt != null && Object.hasOwnProperty.call(message, "ptt"))
                    object.ptt = message.ptt;
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    object.mediaKey = options.bytes === String ? $util.base64.encode(message.mediaKey, 0, message.mediaKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.mediaKey) : message.mediaKey;
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    object.fileEncSha256 = options.bytes === String ? $util.base64.encode(message.fileEncSha256, 0, message.fileEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileEncSha256) : message.fileEncSha256;
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    object.directPath = message.directPath;
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.mediaKeyTimestamp = typeof message.mediaKeyTimestamp === "number" ? BigInt(message.mediaKeyTimestamp) : $util.Long.fromBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.mediaKeyTimestamp === "number")
                        object.mediaKeyTimestamp = options.longs === String ? String(message.mediaKeyTimestamp) : message.mediaKeyTimestamp;
                    else
                        object.mediaKeyTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.mediaKeyTimestamp) : options.longs === Number ? new $util.LongBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0).toNumber() : message.mediaKeyTimestamp;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.streamingSidecar != null && Object.hasOwnProperty.call(message, "streamingSidecar"))
                    object.streamingSidecar = options.bytes === String ? $util.base64.encode(message.streamingSidecar, 0, message.streamingSidecar.length) : options.bytes === Array ? Array.prototype.slice.call(message.streamingSidecar) : message.streamingSidecar;
                if (message.waveform != null && Object.hasOwnProperty.call(message, "waveform"))
                    object.waveform = options.bytes === String ? $util.base64.encode(message.waveform, 0, message.waveform.length) : options.bytes === Array ? Array.prototype.slice.call(message.waveform) : message.waveform;
                if (message.backgroundArgb != null && Object.hasOwnProperty.call(message, "backgroundArgb"))
                    object.backgroundArgb = message.backgroundArgb;
                if (message.viewOnce != null && Object.hasOwnProperty.call(message, "viewOnce"))
                    object.viewOnce = message.viewOnce;
                if (message.accessibilityLabel != null && Object.hasOwnProperty.call(message, "accessibilityLabel"))
                    object.accessibilityLabel = message.accessibilityLabel;
                return object;
            };

            /**
             * Converts this AudioMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.AudioMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AudioMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AudioMessage
             * @function getTypeUrl
             * @memberof proto.Message.AudioMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AudioMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.AudioMessage";
            };

            return AudioMessage;
        })();

        Message.BCallMessage = (function() {

            /**
             * Properties of a BCallMessage.
             * @memberof proto.Message
             * @interface IBCallMessage
             * @property {string|null} [sessionId] BCallMessage sessionId
             * @property {proto.Message.BCallMessage.MediaType|null} [mediaType] BCallMessage mediaType
             * @property {Uint8Array|null} [masterKey] BCallMessage masterKey
             * @property {string|null} [caption] BCallMessage caption
             */

            /**
             * Constructs a new BCallMessage.
             * @memberof proto.Message
             * @classdesc Represents a BCallMessage.
             * @implements IBCallMessage
             * @constructor
             * @param {proto.Message.IBCallMessage=} [properties] Properties to set
             */
            function BCallMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * BCallMessage sessionId.
             * @member {string|null|undefined} sessionId
             * @memberof proto.Message.BCallMessage
             * @instance
             */
            BCallMessage.prototype.sessionId = null;

            /**
             * BCallMessage mediaType.
             * @member {proto.Message.BCallMessage.MediaType|null|undefined} mediaType
             * @memberof proto.Message.BCallMessage
             * @instance
             */
            BCallMessage.prototype.mediaType = null;

            /**
             * BCallMessage masterKey.
             * @member {Uint8Array|null|undefined} masterKey
             * @memberof proto.Message.BCallMessage
             * @instance
             */
            BCallMessage.prototype.masterKey = null;

            /**
             * BCallMessage caption.
             * @member {string|null|undefined} caption
             * @memberof proto.Message.BCallMessage
             * @instance
             */
            BCallMessage.prototype.caption = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BCallMessage.prototype, "_sessionId", {
                get: $util.oneOfGetter($oneOfFields = ["sessionId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BCallMessage.prototype, "_mediaType", {
                get: $util.oneOfGetter($oneOfFields = ["mediaType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BCallMessage.prototype, "_masterKey", {
                get: $util.oneOfGetter($oneOfFields = ["masterKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BCallMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BCallMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.BCallMessage
             * @static
             * @param {proto.Message.IBCallMessage=} [properties] Properties to set
             * @returns {proto.Message.BCallMessage} BCallMessage instance
             */
            BCallMessage.create = function create(properties) {
                return new BCallMessage(properties);
            };

            /**
             * Encodes the specified BCallMessage message. Does not implicitly {@link proto.Message.BCallMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.BCallMessage
             * @static
             * @param {proto.Message.IBCallMessage} message BCallMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BCallMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.sessionId != null && Object.hasOwnProperty.call(message, "sessionId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.sessionId);
                if (message.mediaType != null && Object.hasOwnProperty.call(message, "mediaType"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.mediaType);
                if (message.masterKey != null && Object.hasOwnProperty.call(message, "masterKey"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.masterKey);
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.caption);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified BCallMessage message, length delimited. Does not implicitly {@link proto.Message.BCallMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.BCallMessage
             * @static
             * @param {proto.Message.IBCallMessage} message BCallMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BCallMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a BCallMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.BCallMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.BCallMessage} BCallMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BCallMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.sessionId = reader.stringVerify();
                            message._sessionId = "sessionId";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.mediaType = reader.int32();
                            message._mediaType = "mediaType";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.masterKey = reader.bytes();
                            message._masterKey = "masterKey";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.caption = reader.stringVerify();
                            message._caption = "caption";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a BCallMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.BCallMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.BCallMessage} BCallMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BCallMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BCallMessage message.
             * @function verify
             * @memberof proto.Message.BCallMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BCallMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.sessionId != null && Object.hasOwnProperty.call(message, "sessionId")) {
                    properties._sessionId = 1;
                    if (!$util.isString(message.sessionId))
                        return "sessionId: string expected";
                }
                if (message.mediaType != null && Object.hasOwnProperty.call(message, "mediaType")) {
                    properties._mediaType = 1;
                    if (typeof message.mediaType !== "number" || (message.mediaType | 0) !== message.mediaType)
                        return "mediaType: enum value expected";
                }
                if (message.masterKey != null && Object.hasOwnProperty.call(message, "masterKey")) {
                    properties._masterKey = 1;
                    if (!(message.masterKey && typeof message.masterKey.length === "number" || $util.isString(message.masterKey)))
                        return "masterKey: buffer expected";
                }
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption")) {
                    properties._caption = 1;
                    if (!$util.isString(message.caption))
                        return "caption: string expected";
                }
                return null;
            };

            /**
             * Creates a BCallMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.BCallMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.BCallMessage} BCallMessage
             */
            BCallMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.BCallMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.sessionId != null)
                    message.sessionId = String(object.sessionId);
                switch (object.mediaType) {
                case "UNKNOWN":
                case 0:
                    message.mediaType = 0;
                    break;
                case "AUDIO":
                case 1:
                    message.mediaType = 1;
                    break;
                case "VIDEO":
                case 2:
                    message.mediaType = 2;
                    break;
                default:
                    if (typeof object.mediaType === "number" && (object.mediaType | 0) === object.mediaType)
                        message.mediaType = object.mediaType;
                }
                if (object.masterKey != null)
                    if (typeof object.masterKey === "string")
                        $util.base64.decode(object.masterKey, message.masterKey = $util.newBuffer($util.base64.length(object.masterKey)), 0);
                    else if (object.masterKey.length >= 0)
                        message.masterKey = object.masterKey;
                if (object.caption != null)
                    message.caption = String(object.caption);
                return message;
            };

            /**
             * Creates a plain object from a BCallMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.BCallMessage
             * @static
             * @param {proto.Message.BCallMessage} message BCallMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BCallMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.sessionId != null && Object.hasOwnProperty.call(message, "sessionId"))
                    object.sessionId = message.sessionId;
                if (message.mediaType != null && Object.hasOwnProperty.call(message, "mediaType"))
                    object.mediaType = options.enums === String ? $root.proto.Message.BCallMessage.MediaType[message.mediaType] === undefined ? message.mediaType : $root.proto.Message.BCallMessage.MediaType[message.mediaType] : message.mediaType;
                if (message.masterKey != null && Object.hasOwnProperty.call(message, "masterKey"))
                    object.masterKey = options.bytes === String ? $util.base64.encode(message.masterKey, 0, message.masterKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.masterKey) : message.masterKey;
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    object.caption = message.caption;
                return object;
            };

            /**
             * Converts this BCallMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.BCallMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BCallMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for BCallMessage
             * @function getTypeUrl
             * @memberof proto.Message.BCallMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            BCallMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.BCallMessage";
            };

            /**
             * MediaType enum.
             * @name proto.Message.BCallMessage.MediaType
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} AUDIO=1 AUDIO value
             * @property {number} VIDEO=2 VIDEO value
             */
            BCallMessage.MediaType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "AUDIO"] = 1;
                values[valuesById[2] = "VIDEO"] = 2;
                return values;
            })();

            return BCallMessage;
        })();

        Message.ButtonsMessage = (function() {

            /**
             * Properties of a ButtonsMessage.
             * @memberof proto.Message
             * @interface IButtonsMessage
             * @property {string|null} [contentText] ButtonsMessage contentText
             * @property {string|null} [footerText] ButtonsMessage footerText
             * @property {proto.IContextInfo|null} [contextInfo] ButtonsMessage contextInfo
             * @property {Array.<proto.Message.ButtonsMessage.IButton>|null} [buttons] ButtonsMessage buttons
             * @property {proto.Message.ButtonsMessage.HeaderType|null} [headerType] ButtonsMessage headerType
             * @property {string|null} [text] ButtonsMessage text
             * @property {proto.Message.IDocumentMessage|null} [documentMessage] ButtonsMessage documentMessage
             * @property {proto.Message.IImageMessage|null} [imageMessage] ButtonsMessage imageMessage
             * @property {proto.Message.IVideoMessage|null} [videoMessage] ButtonsMessage videoMessage
             * @property {proto.Message.ILocationMessage|null} [locationMessage] ButtonsMessage locationMessage
             */

            /**
             * Constructs a new ButtonsMessage.
             * @memberof proto.Message
             * @classdesc Represents a ButtonsMessage.
             * @implements IButtonsMessage
             * @constructor
             * @param {proto.Message.IButtonsMessage=} [properties] Properties to set
             */
            function ButtonsMessage(properties) {
                this.buttons = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ButtonsMessage contentText.
             * @member {string|null|undefined} contentText
             * @memberof proto.Message.ButtonsMessage
             * @instance
             */
            ButtonsMessage.prototype.contentText = null;

            /**
             * ButtonsMessage footerText.
             * @member {string|null|undefined} footerText
             * @memberof proto.Message.ButtonsMessage
             * @instance
             */
            ButtonsMessage.prototype.footerText = null;

            /**
             * ButtonsMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.ButtonsMessage
             * @instance
             */
            ButtonsMessage.prototype.contextInfo = null;

            /**
             * ButtonsMessage buttons.
             * @member {Array.<proto.Message.ButtonsMessage.IButton>} buttons
             * @memberof proto.Message.ButtonsMessage
             * @instance
             */
            ButtonsMessage.prototype.buttons = $util.emptyArray;

            /**
             * ButtonsMessage headerType.
             * @member {proto.Message.ButtonsMessage.HeaderType|null|undefined} headerType
             * @memberof proto.Message.ButtonsMessage
             * @instance
             */
            ButtonsMessage.prototype.headerType = null;

            /**
             * ButtonsMessage text.
             * @member {string|null|undefined} text
             * @memberof proto.Message.ButtonsMessage
             * @instance
             */
            ButtonsMessage.prototype.text = null;

            /**
             * ButtonsMessage documentMessage.
             * @member {proto.Message.IDocumentMessage|null|undefined} documentMessage
             * @memberof proto.Message.ButtonsMessage
             * @instance
             */
            ButtonsMessage.prototype.documentMessage = null;

            /**
             * ButtonsMessage imageMessage.
             * @member {proto.Message.IImageMessage|null|undefined} imageMessage
             * @memberof proto.Message.ButtonsMessage
             * @instance
             */
            ButtonsMessage.prototype.imageMessage = null;

            /**
             * ButtonsMessage videoMessage.
             * @member {proto.Message.IVideoMessage|null|undefined} videoMessage
             * @memberof proto.Message.ButtonsMessage
             * @instance
             */
            ButtonsMessage.prototype.videoMessage = null;

            /**
             * ButtonsMessage locationMessage.
             * @member {proto.Message.ILocationMessage|null|undefined} locationMessage
             * @memberof proto.Message.ButtonsMessage
             * @instance
             */
            ButtonsMessage.prototype.locationMessage = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ButtonsMessage.prototype, "_contentText", {
                get: $util.oneOfGetter($oneOfFields = ["contentText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ButtonsMessage.prototype, "_footerText", {
                get: $util.oneOfGetter($oneOfFields = ["footerText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ButtonsMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ButtonsMessage.prototype, "_headerType", {
                get: $util.oneOfGetter($oneOfFields = ["headerType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ButtonsMessage header.
             * @member {"text"|"documentMessage"|"imageMessage"|"videoMessage"|"locationMessage"|undefined} header
             * @memberof proto.Message.ButtonsMessage
             * @instance
             */
            Object.defineProperty(ButtonsMessage.prototype, "header", {
                get: $util.oneOfGetter($oneOfFields = ["text", "documentMessage", "imageMessage", "videoMessage", "locationMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ButtonsMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.ButtonsMessage
             * @static
             * @param {proto.Message.IButtonsMessage=} [properties] Properties to set
             * @returns {proto.Message.ButtonsMessage} ButtonsMessage instance
             */
            ButtonsMessage.create = function create(properties) {
                return new ButtonsMessage(properties);
            };

            /**
             * Encodes the specified ButtonsMessage message. Does not implicitly {@link proto.Message.ButtonsMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.ButtonsMessage
             * @static
             * @param {proto.Message.IButtonsMessage} message ButtonsMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ButtonsMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.text);
                if (message.documentMessage != null && Object.hasOwnProperty.call(message, "documentMessage"))
                    $root.proto.Message.DocumentMessage.encode(message.documentMessage, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.imageMessage != null && Object.hasOwnProperty.call(message, "imageMessage"))
                    $root.proto.Message.ImageMessage.encode(message.imageMessage, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.videoMessage != null && Object.hasOwnProperty.call(message, "videoMessage"))
                    $root.proto.Message.VideoMessage.encode(message.videoMessage, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.locationMessage != null && Object.hasOwnProperty.call(message, "locationMessage"))
                    $root.proto.Message.LocationMessage.encode(message.locationMessage, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                if (message.contentText != null && Object.hasOwnProperty.call(message, "contentText"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.contentText);
                if (message.footerText != null && Object.hasOwnProperty.call(message, "footerText"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.footerText);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
                if (message.buttons != null && message.buttons.length)
                    for (var i = 0; i < message.buttons.length; ++i)
                        $root.proto.Message.ButtonsMessage.Button.encode(message.buttons[i], writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
                if (message.headerType != null && Object.hasOwnProperty.call(message, "headerType"))
                    writer.uint32(/* id 10, wireType 0 =*/80).int32(message.headerType);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ButtonsMessage message, length delimited. Does not implicitly {@link proto.Message.ButtonsMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.ButtonsMessage
             * @static
             * @param {proto.Message.IButtonsMessage} message ButtonsMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ButtonsMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ButtonsMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.ButtonsMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.ButtonsMessage} ButtonsMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ButtonsMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 6: {
                            if (u !== 2)
                                break;
                            message.contentText = reader.stringVerify();
                            message._contentText = "contentText";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.footerText = reader.stringVerify();
                            message._footerText = "footerText";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            if (!(message.buttons && message.buttons.length))
                                message.buttons = [];
                            message.buttons.push($root.proto.Message.ButtonsMessage.Button.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            message.headerType = reader.int32();
                            message._headerType = "headerType";
                            continue;
                        }
                    case 1: {
                            if (u !== 2)
                                break;
                            message.text = reader.stringVerify();
                            message.header = "text";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.documentMessage = $root.proto.Message.DocumentMessage.decode(reader, reader.uint32(), undefined, q + 1, message.documentMessage);
                            message.header = "documentMessage";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.imageMessage = $root.proto.Message.ImageMessage.decode(reader, reader.uint32(), undefined, q + 1, message.imageMessage);
                            message.header = "imageMessage";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.videoMessage = $root.proto.Message.VideoMessage.decode(reader, reader.uint32(), undefined, q + 1, message.videoMessage);
                            message.header = "videoMessage";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.locationMessage = $root.proto.Message.LocationMessage.decode(reader, reader.uint32(), undefined, q + 1, message.locationMessage);
                            message.header = "locationMessage";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ButtonsMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.ButtonsMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.ButtonsMessage} ButtonsMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ButtonsMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ButtonsMessage message.
             * @function verify
             * @memberof proto.Message.ButtonsMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ButtonsMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.contentText != null && Object.hasOwnProperty.call(message, "contentText")) {
                    properties._contentText = 1;
                    if (!$util.isString(message.contentText))
                        return "contentText: string expected";
                }
                if (message.footerText != null && Object.hasOwnProperty.call(message, "footerText")) {
                    properties._footerText = 1;
                    if (!$util.isString(message.footerText))
                        return "footerText: string expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.buttons != null && Object.hasOwnProperty.call(message, "buttons")) {
                    if (!Array.isArray(message.buttons))
                        return "buttons: array expected";
                    for (var i = 0; i < message.buttons.length; ++i) {
                        var error = $root.proto.Message.ButtonsMessage.Button.verify(message.buttons[i], q + 1);
                        if (error)
                            return "buttons." + error;
                    }
                }
                if (message.headerType != null && Object.hasOwnProperty.call(message, "headerType")) {
                    properties._headerType = 1;
                    if (typeof message.headerType !== "number" || (message.headerType | 0) !== message.headerType)
                        return "headerType: enum value expected";
                }
                if (message.text != null && Object.hasOwnProperty.call(message, "text")) {
                    properties.header = 1;
                    if (!$util.isString(message.text))
                        return "text: string expected";
                }
                if (message.documentMessage != null && Object.hasOwnProperty.call(message, "documentMessage")) {
                    if (properties.header === 1)
                        return "header: multiple values";
                    properties.header = 1;
                    {
                        var error = $root.proto.Message.DocumentMessage.verify(message.documentMessage, q + 1);
                        if (error)
                            return "documentMessage." + error;
                    }
                }
                if (message.imageMessage != null && Object.hasOwnProperty.call(message, "imageMessage")) {
                    if (properties.header === 1)
                        return "header: multiple values";
                    properties.header = 1;
                    {
                        var error = $root.proto.Message.ImageMessage.verify(message.imageMessage, q + 1);
                        if (error)
                            return "imageMessage." + error;
                    }
                }
                if (message.videoMessage != null && Object.hasOwnProperty.call(message, "videoMessage")) {
                    if (properties.header === 1)
                        return "header: multiple values";
                    properties.header = 1;
                    {
                        var error = $root.proto.Message.VideoMessage.verify(message.videoMessage, q + 1);
                        if (error)
                            return "videoMessage." + error;
                    }
                }
                if (message.locationMessage != null && Object.hasOwnProperty.call(message, "locationMessage")) {
                    if (properties.header === 1)
                        return "header: multiple values";
                    properties.header = 1;
                    {
                        var error = $root.proto.Message.LocationMessage.verify(message.locationMessage, q + 1);
                        if (error)
                            return "locationMessage." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a ButtonsMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.ButtonsMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.ButtonsMessage} ButtonsMessage
             */
            ButtonsMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.ButtonsMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.contentText != null)
                    message.contentText = String(object.contentText);
                if (object.footerText != null)
                    message.footerText = String(object.footerText);
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.ButtonsMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                if (object.buttons) {
                    if (!Array.isArray(object.buttons))
                        throw TypeError(".proto.Message.ButtonsMessage.buttons: array expected");
                    message.buttons = Array(object.buttons.length);
                    for (var i = 0; i < object.buttons.length; ++i) {
                        if (!$util.isObject(object.buttons[i]))
                            throw TypeError(".proto.Message.ButtonsMessage.buttons: object expected");
                        message.buttons[i] = $root.proto.Message.ButtonsMessage.Button.fromObject(object.buttons[i], q + 1);
                    }
                }
                switch (object.headerType) {
                case "UNKNOWN":
                case 0:
                    message.headerType = 0;
                    break;
                case "EMPTY":
                case 1:
                    message.headerType = 1;
                    break;
                case "TEXT":
                case 2:
                    message.headerType = 2;
                    break;
                case "DOCUMENT":
                case 3:
                    message.headerType = 3;
                    break;
                case "IMAGE":
                case 4:
                    message.headerType = 4;
                    break;
                case "VIDEO":
                case 5:
                    message.headerType = 5;
                    break;
                case "LOCATION":
                case 6:
                    message.headerType = 6;
                    break;
                default:
                    if (typeof object.headerType === "number" && (object.headerType | 0) === object.headerType)
                        message.headerType = object.headerType;
                }
                if (object.text != null)
                    message.text = String(object.text);
                if (object.documentMessage != null) {
                    if (!$util.isObject(object.documentMessage))
                        throw TypeError(".proto.Message.ButtonsMessage.documentMessage: object expected");
                    message.documentMessage = $root.proto.Message.DocumentMessage.fromObject(object.documentMessage, q + 1);
                }
                if (object.imageMessage != null) {
                    if (!$util.isObject(object.imageMessage))
                        throw TypeError(".proto.Message.ButtonsMessage.imageMessage: object expected");
                    message.imageMessage = $root.proto.Message.ImageMessage.fromObject(object.imageMessage, q + 1);
                }
                if (object.videoMessage != null) {
                    if (!$util.isObject(object.videoMessage))
                        throw TypeError(".proto.Message.ButtonsMessage.videoMessage: object expected");
                    message.videoMessage = $root.proto.Message.VideoMessage.fromObject(object.videoMessage, q + 1);
                }
                if (object.locationMessage != null) {
                    if (!$util.isObject(object.locationMessage))
                        throw TypeError(".proto.Message.ButtonsMessage.locationMessage: object expected");
                    message.locationMessage = $root.proto.Message.LocationMessage.fromObject(object.locationMessage, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a ButtonsMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.ButtonsMessage
             * @static
             * @param {proto.Message.ButtonsMessage} message ButtonsMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ButtonsMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.buttons = [];
                if (message.text != null && Object.hasOwnProperty.call(message, "text")) {
                    object.text = message.text;
                    if (options.oneofs)
                        object.header = "text";
                }
                if (message.documentMessage != null && Object.hasOwnProperty.call(message, "documentMessage")) {
                    object.documentMessage = $root.proto.Message.DocumentMessage.toObject(message.documentMessage, options, q + 1);
                    if (options.oneofs)
                        object.header = "documentMessage";
                }
                if (message.imageMessage != null && Object.hasOwnProperty.call(message, "imageMessage")) {
                    object.imageMessage = $root.proto.Message.ImageMessage.toObject(message.imageMessage, options, q + 1);
                    if (options.oneofs)
                        object.header = "imageMessage";
                }
                if (message.videoMessage != null && Object.hasOwnProperty.call(message, "videoMessage")) {
                    object.videoMessage = $root.proto.Message.VideoMessage.toObject(message.videoMessage, options, q + 1);
                    if (options.oneofs)
                        object.header = "videoMessage";
                }
                if (message.locationMessage != null && Object.hasOwnProperty.call(message, "locationMessage")) {
                    object.locationMessage = $root.proto.Message.LocationMessage.toObject(message.locationMessage, options, q + 1);
                    if (options.oneofs)
                        object.header = "locationMessage";
                }
                if (message.contentText != null && Object.hasOwnProperty.call(message, "contentText"))
                    object.contentText = message.contentText;
                if (message.footerText != null && Object.hasOwnProperty.call(message, "footerText"))
                    object.footerText = message.footerText;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.buttons && message.buttons.length) {
                    object.buttons = Array(message.buttons.length);
                    for (var j = 0; j < message.buttons.length; ++j)
                        object.buttons[j] = $root.proto.Message.ButtonsMessage.Button.toObject(message.buttons[j], options, q + 1);
                }
                if (message.headerType != null && Object.hasOwnProperty.call(message, "headerType"))
                    object.headerType = options.enums === String ? $root.proto.Message.ButtonsMessage.HeaderType[message.headerType] === undefined ? message.headerType : $root.proto.Message.ButtonsMessage.HeaderType[message.headerType] : message.headerType;
                return object;
            };

            /**
             * Converts this ButtonsMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.ButtonsMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ButtonsMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ButtonsMessage
             * @function getTypeUrl
             * @memberof proto.Message.ButtonsMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ButtonsMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.ButtonsMessage";
            };

            ButtonsMessage.Button = (function() {

                /**
                 * Properties of a Button.
                 * @memberof proto.Message.ButtonsMessage
                 * @interface IButton
                 * @property {string|null} [buttonId] Button buttonId
                 * @property {proto.Message.ButtonsMessage.Button.IButtonText|null} [buttonText] Button buttonText
                 * @property {proto.Message.ButtonsMessage.Button.Type|null} [type] Button type
                 * @property {proto.Message.ButtonsMessage.Button.INativeFlowInfo|null} [nativeFlowInfo] Button nativeFlowInfo
                 */

                /**
                 * Constructs a new Button.
                 * @memberof proto.Message.ButtonsMessage
                 * @classdesc Represents a Button.
                 * @implements IButton
                 * @constructor
                 * @param {proto.Message.ButtonsMessage.IButton=} [properties] Properties to set
                 */
                function Button(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * Button buttonId.
                 * @member {string|null|undefined} buttonId
                 * @memberof proto.Message.ButtonsMessage.Button
                 * @instance
                 */
                Button.prototype.buttonId = null;

                /**
                 * Button buttonText.
                 * @member {proto.Message.ButtonsMessage.Button.IButtonText|null|undefined} buttonText
                 * @memberof proto.Message.ButtonsMessage.Button
                 * @instance
                 */
                Button.prototype.buttonText = null;

                /**
                 * Button type.
                 * @member {proto.Message.ButtonsMessage.Button.Type|null|undefined} type
                 * @memberof proto.Message.ButtonsMessage.Button
                 * @instance
                 */
                Button.prototype.type = null;

                /**
                 * Button nativeFlowInfo.
                 * @member {proto.Message.ButtonsMessage.Button.INativeFlowInfo|null|undefined} nativeFlowInfo
                 * @memberof proto.Message.ButtonsMessage.Button
                 * @instance
                 */
                Button.prototype.nativeFlowInfo = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Button.prototype, "_buttonId", {
                    get: $util.oneOfGetter($oneOfFields = ["buttonId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Button.prototype, "_buttonText", {
                    get: $util.oneOfGetter($oneOfFields = ["buttonText"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Button.prototype, "_type", {
                    get: $util.oneOfGetter($oneOfFields = ["type"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Button.prototype, "_nativeFlowInfo", {
                    get: $util.oneOfGetter($oneOfFields = ["nativeFlowInfo"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new Button instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.ButtonsMessage.Button
                 * @static
                 * @param {proto.Message.ButtonsMessage.IButton=} [properties] Properties to set
                 * @returns {proto.Message.ButtonsMessage.Button} Button instance
                 */
                Button.create = function create(properties) {
                    return new Button(properties);
                };

                /**
                 * Encodes the specified Button message. Does not implicitly {@link proto.Message.ButtonsMessage.Button.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.ButtonsMessage.Button
                 * @static
                 * @param {proto.Message.ButtonsMessage.IButton} message Button message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Button.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.buttonId != null && Object.hasOwnProperty.call(message, "buttonId"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.buttonId);
                    if (message.buttonText != null && Object.hasOwnProperty.call(message, "buttonText"))
                        $root.proto.Message.ButtonsMessage.Button.ButtonText.encode(message.buttonText, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                    if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                        writer.uint32(/* id 3, wireType 0 =*/24).int32(message.type);
                    if (message.nativeFlowInfo != null && Object.hasOwnProperty.call(message, "nativeFlowInfo"))
                        $root.proto.Message.ButtonsMessage.Button.NativeFlowInfo.encode(message.nativeFlowInfo, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified Button message, length delimited. Does not implicitly {@link proto.Message.ButtonsMessage.Button.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.ButtonsMessage.Button
                 * @static
                 * @param {proto.Message.ButtonsMessage.IButton} message Button message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Button.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a Button message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.ButtonsMessage.Button
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.ButtonsMessage.Button} Button
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Button.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.buttonId = reader.stringVerify();
                                message._buttonId = "buttonId";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.buttonText = $root.proto.Message.ButtonsMessage.Button.ButtonText.decode(reader, reader.uint32(), undefined, q + 1, message.buttonText);
                                message._buttonText = "buttonText";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                message.type = reader.int32();
                                message._type = "type";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                message.nativeFlowInfo = $root.proto.Message.ButtonsMessage.Button.NativeFlowInfo.decode(reader, reader.uint32(), undefined, q + 1, message.nativeFlowInfo);
                                message._nativeFlowInfo = "nativeFlowInfo";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a Button message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.ButtonsMessage.Button
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.ButtonsMessage.Button} Button
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Button.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a Button message.
                 * @function verify
                 * @memberof proto.Message.ButtonsMessage.Button
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                Button.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.buttonId != null && Object.hasOwnProperty.call(message, "buttonId")) {
                        properties._buttonId = 1;
                        if (!$util.isString(message.buttonId))
                            return "buttonId: string expected";
                    }
                    if (message.buttonText != null && Object.hasOwnProperty.call(message, "buttonText")) {
                        properties._buttonText = 1;
                        {
                            var error = $root.proto.Message.ButtonsMessage.Button.ButtonText.verify(message.buttonText, q + 1);
                            if (error)
                                return "buttonText." + error;
                        }
                    }
                    if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                        properties._type = 1;
                        if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                            return "type: enum value expected";
                    }
                    if (message.nativeFlowInfo != null && Object.hasOwnProperty.call(message, "nativeFlowInfo")) {
                        properties._nativeFlowInfo = 1;
                        {
                            var error = $root.proto.Message.ButtonsMessage.Button.NativeFlowInfo.verify(message.nativeFlowInfo, q + 1);
                            if (error)
                                return "nativeFlowInfo." + error;
                        }
                    }
                    return null;
                };

                /**
                 * Creates a Button message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.ButtonsMessage.Button
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.ButtonsMessage.Button} Button
                 */
                Button.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.ButtonsMessage.Button: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.buttonId != null)
                        message.buttonId = String(object.buttonId);
                    if (object.buttonText != null) {
                        if (!$util.isObject(object.buttonText))
                            throw TypeError(".proto.Message.ButtonsMessage.Button.buttonText: object expected");
                        message.buttonText = $root.proto.Message.ButtonsMessage.Button.ButtonText.fromObject(object.buttonText, q + 1);
                    }
                    switch (object.type) {
                    case "UNKNOWN":
                    case 0:
                        message.type = 0;
                        break;
                    case "RESPONSE":
                    case 1:
                        message.type = 1;
                        break;
                    case "NATIVE_FLOW":
                    case 2:
                        message.type = 2;
                        break;
                    default:
                        if (typeof object.type === "number" && (object.type | 0) === object.type)
                            message.type = object.type;
                    }
                    if (object.nativeFlowInfo != null) {
                        if (!$util.isObject(object.nativeFlowInfo))
                            throw TypeError(".proto.Message.ButtonsMessage.Button.nativeFlowInfo: object expected");
                        message.nativeFlowInfo = $root.proto.Message.ButtonsMessage.Button.NativeFlowInfo.fromObject(object.nativeFlowInfo, q + 1);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a Button message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.ButtonsMessage.Button
                 * @static
                 * @param {proto.Message.ButtonsMessage.Button} message Button
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                Button.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.buttonId != null && Object.hasOwnProperty.call(message, "buttonId"))
                        object.buttonId = message.buttonId;
                    if (message.buttonText != null && Object.hasOwnProperty.call(message, "buttonText"))
                        object.buttonText = $root.proto.Message.ButtonsMessage.Button.ButtonText.toObject(message.buttonText, options, q + 1);
                    if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                        object.type = options.enums === String ? $root.proto.Message.ButtonsMessage.Button.Type[message.type] === undefined ? message.type : $root.proto.Message.ButtonsMessage.Button.Type[message.type] : message.type;
                    if (message.nativeFlowInfo != null && Object.hasOwnProperty.call(message, "nativeFlowInfo"))
                        object.nativeFlowInfo = $root.proto.Message.ButtonsMessage.Button.NativeFlowInfo.toObject(message.nativeFlowInfo, options, q + 1);
                    return object;
                };

                /**
                 * Converts this Button to JSON.
                 * @function toJSON
                 * @memberof proto.Message.ButtonsMessage.Button
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                Button.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for Button
                 * @function getTypeUrl
                 * @memberof proto.Message.ButtonsMessage.Button
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                Button.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.ButtonsMessage.Button";
                };

                Button.ButtonText = (function() {

                    /**
                     * Properties of a ButtonText.
                     * @memberof proto.Message.ButtonsMessage.Button
                     * @interface IButtonText
                     * @property {string|null} [displayText] ButtonText displayText
                     */

                    /**
                     * Constructs a new ButtonText.
                     * @memberof proto.Message.ButtonsMessage.Button
                     * @classdesc Represents a ButtonText.
                     * @implements IButtonText
                     * @constructor
                     * @param {proto.Message.ButtonsMessage.Button.IButtonText=} [properties] Properties to set
                     */
                    function ButtonText(properties) {
                        if (properties)
                            for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    /**
                     * ButtonText displayText.
                     * @member {string|null|undefined} displayText
                     * @memberof proto.Message.ButtonsMessage.Button.ButtonText
                     * @instance
                     */
                    ButtonText.prototype.displayText = null;

                    // OneOf field names bound to virtual getters and setters
                    var $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(ButtonText.prototype, "_displayText", {
                        get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    /**
                     * Creates a new ButtonText instance using the specified properties.
                     * @function create
                     * @memberof proto.Message.ButtonsMessage.Button.ButtonText
                     * @static
                     * @param {proto.Message.ButtonsMessage.Button.IButtonText=} [properties] Properties to set
                     * @returns {proto.Message.ButtonsMessage.Button.ButtonText} ButtonText instance
                     */
                    ButtonText.create = function create(properties) {
                        return new ButtonText(properties);
                    };

                    /**
                     * Encodes the specified ButtonText message. Does not implicitly {@link proto.Message.ButtonsMessage.Button.ButtonText.verify|verify} messages.
                     * @function encode
                     * @memberof proto.Message.ButtonsMessage.Button.ButtonText
                     * @static
                     * @param {proto.Message.ButtonsMessage.Button.IButtonText} message ButtonText message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    ButtonText.encode = function encode(message, writer, q) {
                        if (!writer)
                            writer = $Writer.create();
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText"))
                            writer.uint32(/* id 1, wireType 2 =*/10).string(message.displayText);
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (var i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    /**
                     * Encodes the specified ButtonText message, length delimited. Does not implicitly {@link proto.Message.ButtonsMessage.Button.ButtonText.verify|verify} messages.
                     * @function encodeDelimited
                     * @memberof proto.Message.ButtonsMessage.Button.ButtonText
                     * @static
                     * @param {proto.Message.ButtonsMessage.Button.IButtonText} message ButtonText message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    ButtonText.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                    };

                    /**
                     * Decodes a ButtonText message from the specified reader or buffer.
                     * @function decode
                     * @memberof proto.Message.ButtonsMessage.Button.ButtonText
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @param {number} [length] Message length if known beforehand
                     * @returns {proto.Message.ButtonsMessage.Button.ButtonText} ButtonText
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    ButtonText.decode = function decode(reader, length, z, q, g) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (q === undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                        while (reader.pos < end) {
                            var s = reader.pos;
                            var tag = reader.tag();
                            if (tag === z) {
                                z = undefined;
                                break;
                            }
                            var u = tag & 7;
                            switch (tag >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    message.displayText = reader.stringVerify();
                                    message._displayText = "displayText";
                                    continue;
                                }
                            }
                            reader.skipType(u, q, tag);
                            if (!reader.discardUnknown) {
                                $util.makeProp(message, "$unknowns", false);
                                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                            }
                        }
                        if (z !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    /**
                     * Decodes a ButtonText message from the specified reader or buffer, length delimited.
                     * @function decodeDelimited
                     * @memberof proto.Message.ButtonsMessage.Button.ButtonText
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @returns {proto.Message.ButtonsMessage.Button.ButtonText} ButtonText
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    ButtonText.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    /**
                     * Verifies a ButtonText message.
                     * @function verify
                     * @memberof proto.Message.ButtonsMessage.Button.ButtonText
                     * @static
                     * @param {Object.<string,*>} message Plain object to verify
                     * @returns {string|null} `null` if valid, otherwise the reason why it is not
                     */
                    ButtonText.verify = function verify(message, q) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            return "max depth exceeded";
                        var properties = {};
                        if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText")) {
                            properties._displayText = 1;
                            if (!$util.isString(message.displayText))
                                return "displayText: string expected";
                        }
                        return null;
                    };

                    /**
                     * Creates a ButtonText message from a plain object. Also converts values to their respective internal types.
                     * @function fromObject
                     * @memberof proto.Message.ButtonsMessage.Button.ButtonText
                     * @static
                     * @param {Object.<string,*>} object Plain object
                     * @returns {proto.Message.ButtonsMessage.Button.ButtonText} ButtonText
                     */
                    ButtonText.fromObject = function fromObject(object, q) {
                        if (object instanceof C)
                            return object;
                        if (!$util.isObject(object))
                            throw TypeError(".proto.Message.ButtonsMessage.Button.ButtonText: object expected");
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var message = new C();
                        if (object.displayText != null)
                            message.displayText = String(object.displayText);
                        return message;
                    };

                    /**
                     * Creates a plain object from a ButtonText message. Also converts values to other types if specified.
                     * @function toObject
                     * @memberof proto.Message.ButtonsMessage.Button.ButtonText
                     * @static
                     * @param {proto.Message.ButtonsMessage.Button.ButtonText} message ButtonText
                     * @param {$protobuf.IConversionOptions} [options] Conversion options
                     * @returns {Object.<string,*>} Plain object
                     */
                    ButtonText.toObject = function toObject(message, options, q) {
                        if (!options)
                            options = {};
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var object = {};
                        if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText"))
                            object.displayText = message.displayText;
                        return object;
                    };

                    /**
                     * Converts this ButtonText to JSON.
                     * @function toJSON
                     * @memberof proto.Message.ButtonsMessage.Button.ButtonText
                     * @instance
                     * @returns {Object.<string,*>} JSON object
                     */
                    ButtonText.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    /**
                     * Gets the default type url for ButtonText
                     * @function getTypeUrl
                     * @memberof proto.Message.ButtonsMessage.Button.ButtonText
                     * @static
                     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                     * @returns {string} The default type url
                     */
                    ButtonText.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/proto.Message.ButtonsMessage.Button.ButtonText";
                    };

                    return ButtonText;
                })();

                Button.NativeFlowInfo = (function() {

                    /**
                     * Properties of a NativeFlowInfo.
                     * @memberof proto.Message.ButtonsMessage.Button
                     * @interface INativeFlowInfo
                     * @property {string|null} [name] NativeFlowInfo name
                     * @property {string|null} [paramsJson] NativeFlowInfo paramsJson
                     */

                    /**
                     * Constructs a new NativeFlowInfo.
                     * @memberof proto.Message.ButtonsMessage.Button
                     * @classdesc Represents a NativeFlowInfo.
                     * @implements INativeFlowInfo
                     * @constructor
                     * @param {proto.Message.ButtonsMessage.Button.INativeFlowInfo=} [properties] Properties to set
                     */
                    function NativeFlowInfo(properties) {
                        if (properties)
                            for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    /**
                     * NativeFlowInfo name.
                     * @member {string|null|undefined} name
                     * @memberof proto.Message.ButtonsMessage.Button.NativeFlowInfo
                     * @instance
                     */
                    NativeFlowInfo.prototype.name = null;

                    /**
                     * NativeFlowInfo paramsJson.
                     * @member {string|null|undefined} paramsJson
                     * @memberof proto.Message.ButtonsMessage.Button.NativeFlowInfo
                     * @instance
                     */
                    NativeFlowInfo.prototype.paramsJson = null;

                    // OneOf field names bound to virtual getters and setters
                    var $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(NativeFlowInfo.prototype, "_name", {
                        get: $util.oneOfGetter($oneOfFields = ["name"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(NativeFlowInfo.prototype, "_paramsJson", {
                        get: $util.oneOfGetter($oneOfFields = ["paramsJson"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    /**
                     * Creates a new NativeFlowInfo instance using the specified properties.
                     * @function create
                     * @memberof proto.Message.ButtonsMessage.Button.NativeFlowInfo
                     * @static
                     * @param {proto.Message.ButtonsMessage.Button.INativeFlowInfo=} [properties] Properties to set
                     * @returns {proto.Message.ButtonsMessage.Button.NativeFlowInfo} NativeFlowInfo instance
                     */
                    NativeFlowInfo.create = function create(properties) {
                        return new NativeFlowInfo(properties);
                    };

                    /**
                     * Encodes the specified NativeFlowInfo message. Does not implicitly {@link proto.Message.ButtonsMessage.Button.NativeFlowInfo.verify|verify} messages.
                     * @function encode
                     * @memberof proto.Message.ButtonsMessage.Button.NativeFlowInfo
                     * @static
                     * @param {proto.Message.ButtonsMessage.Button.INativeFlowInfo} message NativeFlowInfo message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    NativeFlowInfo.encode = function encode(message, writer, q) {
                        if (!writer)
                            writer = $Writer.create();
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                            writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                        if (message.paramsJson != null && Object.hasOwnProperty.call(message, "paramsJson"))
                            writer.uint32(/* id 2, wireType 2 =*/18).string(message.paramsJson);
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (var i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    /**
                     * Encodes the specified NativeFlowInfo message, length delimited. Does not implicitly {@link proto.Message.ButtonsMessage.Button.NativeFlowInfo.verify|verify} messages.
                     * @function encodeDelimited
                     * @memberof proto.Message.ButtonsMessage.Button.NativeFlowInfo
                     * @static
                     * @param {proto.Message.ButtonsMessage.Button.INativeFlowInfo} message NativeFlowInfo message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    NativeFlowInfo.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                    };

                    /**
                     * Decodes a NativeFlowInfo message from the specified reader or buffer.
                     * @function decode
                     * @memberof proto.Message.ButtonsMessage.Button.NativeFlowInfo
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @param {number} [length] Message length if known beforehand
                     * @returns {proto.Message.ButtonsMessage.Button.NativeFlowInfo} NativeFlowInfo
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    NativeFlowInfo.decode = function decode(reader, length, z, q, g) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (q === undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                        while (reader.pos < end) {
                            var s = reader.pos;
                            var tag = reader.tag();
                            if (tag === z) {
                                z = undefined;
                                break;
                            }
                            var u = tag & 7;
                            switch (tag >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    message.name = reader.stringVerify();
                                    message._name = "name";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 2)
                                        break;
                                    message.paramsJson = reader.stringVerify();
                                    message._paramsJson = "paramsJson";
                                    continue;
                                }
                            }
                            reader.skipType(u, q, tag);
                            if (!reader.discardUnknown) {
                                $util.makeProp(message, "$unknowns", false);
                                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                            }
                        }
                        if (z !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    /**
                     * Decodes a NativeFlowInfo message from the specified reader or buffer, length delimited.
                     * @function decodeDelimited
                     * @memberof proto.Message.ButtonsMessage.Button.NativeFlowInfo
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @returns {proto.Message.ButtonsMessage.Button.NativeFlowInfo} NativeFlowInfo
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    NativeFlowInfo.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    /**
                     * Verifies a NativeFlowInfo message.
                     * @function verify
                     * @memberof proto.Message.ButtonsMessage.Button.NativeFlowInfo
                     * @static
                     * @param {Object.<string,*>} message Plain object to verify
                     * @returns {string|null} `null` if valid, otherwise the reason why it is not
                     */
                    NativeFlowInfo.verify = function verify(message, q) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            return "max depth exceeded";
                        var properties = {};
                        if (message.name != null && Object.hasOwnProperty.call(message, "name")) {
                            properties._name = 1;
                            if (!$util.isString(message.name))
                                return "name: string expected";
                        }
                        if (message.paramsJson != null && Object.hasOwnProperty.call(message, "paramsJson")) {
                            properties._paramsJson = 1;
                            if (!$util.isString(message.paramsJson))
                                return "paramsJson: string expected";
                        }
                        return null;
                    };

                    /**
                     * Creates a NativeFlowInfo message from a plain object. Also converts values to their respective internal types.
                     * @function fromObject
                     * @memberof proto.Message.ButtonsMessage.Button.NativeFlowInfo
                     * @static
                     * @param {Object.<string,*>} object Plain object
                     * @returns {proto.Message.ButtonsMessage.Button.NativeFlowInfo} NativeFlowInfo
                     */
                    NativeFlowInfo.fromObject = function fromObject(object, q) {
                        if (object instanceof C)
                            return object;
                        if (!$util.isObject(object))
                            throw TypeError(".proto.Message.ButtonsMessage.Button.NativeFlowInfo: object expected");
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var message = new C();
                        if (object.name != null)
                            message.name = String(object.name);
                        if (object.paramsJson != null)
                            message.paramsJson = String(object.paramsJson);
                        return message;
                    };

                    /**
                     * Creates a plain object from a NativeFlowInfo message. Also converts values to other types if specified.
                     * @function toObject
                     * @memberof proto.Message.ButtonsMessage.Button.NativeFlowInfo
                     * @static
                     * @param {proto.Message.ButtonsMessage.Button.NativeFlowInfo} message NativeFlowInfo
                     * @param {$protobuf.IConversionOptions} [options] Conversion options
                     * @returns {Object.<string,*>} Plain object
                     */
                    NativeFlowInfo.toObject = function toObject(message, options, q) {
                        if (!options)
                            options = {};
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var object = {};
                        if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                            object.name = message.name;
                        if (message.paramsJson != null && Object.hasOwnProperty.call(message, "paramsJson"))
                            object.paramsJson = message.paramsJson;
                        return object;
                    };

                    /**
                     * Converts this NativeFlowInfo to JSON.
                     * @function toJSON
                     * @memberof proto.Message.ButtonsMessage.Button.NativeFlowInfo
                     * @instance
                     * @returns {Object.<string,*>} JSON object
                     */
                    NativeFlowInfo.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    /**
                     * Gets the default type url for NativeFlowInfo
                     * @function getTypeUrl
                     * @memberof proto.Message.ButtonsMessage.Button.NativeFlowInfo
                     * @static
                     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                     * @returns {string} The default type url
                     */
                    NativeFlowInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/proto.Message.ButtonsMessage.Button.NativeFlowInfo";
                    };

                    return NativeFlowInfo;
                })();

                /**
                 * Type enum.
                 * @name proto.Message.ButtonsMessage.Button.Type
                 * @enum {number}
                 * @property {number} UNKNOWN=0 UNKNOWN value
                 * @property {number} RESPONSE=1 RESPONSE value
                 * @property {number} NATIVE_FLOW=2 NATIVE_FLOW value
                 */
                Button.Type = (function() {
                    var valuesById = {}, values = Object.create(valuesById);
                    values[valuesById[0] = "UNKNOWN"] = 0;
                    values[valuesById[1] = "RESPONSE"] = 1;
                    values[valuesById[2] = "NATIVE_FLOW"] = 2;
                    return values;
                })();

                return Button;
            })();

            /**
             * HeaderType enum.
             * @name proto.Message.ButtonsMessage.HeaderType
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} EMPTY=1 EMPTY value
             * @property {number} TEXT=2 TEXT value
             * @property {number} DOCUMENT=3 DOCUMENT value
             * @property {number} IMAGE=4 IMAGE value
             * @property {number} VIDEO=5 VIDEO value
             * @property {number} LOCATION=6 LOCATION value
             */
            ButtonsMessage.HeaderType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "EMPTY"] = 1;
                values[valuesById[2] = "TEXT"] = 2;
                values[valuesById[3] = "DOCUMENT"] = 3;
                values[valuesById[4] = "IMAGE"] = 4;
                values[valuesById[5] = "VIDEO"] = 5;
                values[valuesById[6] = "LOCATION"] = 6;
                return values;
            })();

            return ButtonsMessage;
        })();

        Message.ButtonsResponseMessage = (function() {

            /**
             * Properties of a ButtonsResponseMessage.
             * @memberof proto.Message
             * @interface IButtonsResponseMessage
             * @property {string|null} [selectedButtonId] ButtonsResponseMessage selectedButtonId
             * @property {proto.IContextInfo|null} [contextInfo] ButtonsResponseMessage contextInfo
             * @property {proto.Message.ButtonsResponseMessage.Type|null} [type] ButtonsResponseMessage type
             * @property {string|null} [selectedDisplayText] ButtonsResponseMessage selectedDisplayText
             */

            /**
             * Constructs a new ButtonsResponseMessage.
             * @memberof proto.Message
             * @classdesc Represents a ButtonsResponseMessage.
             * @implements IButtonsResponseMessage
             * @constructor
             * @param {proto.Message.IButtonsResponseMessage=} [properties] Properties to set
             */
            function ButtonsResponseMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ButtonsResponseMessage selectedButtonId.
             * @member {string|null|undefined} selectedButtonId
             * @memberof proto.Message.ButtonsResponseMessage
             * @instance
             */
            ButtonsResponseMessage.prototype.selectedButtonId = null;

            /**
             * ButtonsResponseMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.ButtonsResponseMessage
             * @instance
             */
            ButtonsResponseMessage.prototype.contextInfo = null;

            /**
             * ButtonsResponseMessage type.
             * @member {proto.Message.ButtonsResponseMessage.Type|null|undefined} type
             * @memberof proto.Message.ButtonsResponseMessage
             * @instance
             */
            ButtonsResponseMessage.prototype.type = null;

            /**
             * ButtonsResponseMessage selectedDisplayText.
             * @member {string|null|undefined} selectedDisplayText
             * @memberof proto.Message.ButtonsResponseMessage
             * @instance
             */
            ButtonsResponseMessage.prototype.selectedDisplayText = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ButtonsResponseMessage.prototype, "_selectedButtonId", {
                get: $util.oneOfGetter($oneOfFields = ["selectedButtonId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ButtonsResponseMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ButtonsResponseMessage.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ButtonsResponseMessage response.
             * @member {"selectedDisplayText"|undefined} response
             * @memberof proto.Message.ButtonsResponseMessage
             * @instance
             */
            Object.defineProperty(ButtonsResponseMessage.prototype, "response", {
                get: $util.oneOfGetter($oneOfFields = ["selectedDisplayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ButtonsResponseMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.ButtonsResponseMessage
             * @static
             * @param {proto.Message.IButtonsResponseMessage=} [properties] Properties to set
             * @returns {proto.Message.ButtonsResponseMessage} ButtonsResponseMessage instance
             */
            ButtonsResponseMessage.create = function create(properties) {
                return new ButtonsResponseMessage(properties);
            };

            /**
             * Encodes the specified ButtonsResponseMessage message. Does not implicitly {@link proto.Message.ButtonsResponseMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.ButtonsResponseMessage
             * @static
             * @param {proto.Message.IButtonsResponseMessage} message ButtonsResponseMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ButtonsResponseMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.selectedButtonId != null && Object.hasOwnProperty.call(message, "selectedButtonId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.selectedButtonId);
                if (message.selectedDisplayText != null && Object.hasOwnProperty.call(message, "selectedDisplayText"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.selectedDisplayText);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int32(message.type);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ButtonsResponseMessage message, length delimited. Does not implicitly {@link proto.Message.ButtonsResponseMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.ButtonsResponseMessage
             * @static
             * @param {proto.Message.IButtonsResponseMessage} message ButtonsResponseMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ButtonsResponseMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ButtonsResponseMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.ButtonsResponseMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.ButtonsResponseMessage} ButtonsResponseMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ButtonsResponseMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.selectedButtonId = reader.stringVerify();
                            message._selectedButtonId = "selectedButtonId";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.type = reader.int32();
                            message._type = "type";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.selectedDisplayText = reader.stringVerify();
                            message.response = "selectedDisplayText";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ButtonsResponseMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.ButtonsResponseMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.ButtonsResponseMessage} ButtonsResponseMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ButtonsResponseMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ButtonsResponseMessage message.
             * @function verify
             * @memberof proto.Message.ButtonsResponseMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ButtonsResponseMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.selectedButtonId != null && Object.hasOwnProperty.call(message, "selectedButtonId")) {
                    properties._selectedButtonId = 1;
                    if (!$util.isString(message.selectedButtonId))
                        return "selectedButtonId: string expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                    properties._type = 1;
                    if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                        return "type: enum value expected";
                }
                if (message.selectedDisplayText != null && Object.hasOwnProperty.call(message, "selectedDisplayText")) {
                    properties.response = 1;
                    if (!$util.isString(message.selectedDisplayText))
                        return "selectedDisplayText: string expected";
                }
                return null;
            };

            /**
             * Creates a ButtonsResponseMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.ButtonsResponseMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.ButtonsResponseMessage} ButtonsResponseMessage
             */
            ButtonsResponseMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.ButtonsResponseMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.selectedButtonId != null)
                    message.selectedButtonId = String(object.selectedButtonId);
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.ButtonsResponseMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                switch (object.type) {
                case "UNKNOWN":
                case 0:
                    message.type = 0;
                    break;
                case "DISPLAY_TEXT":
                case 1:
                    message.type = 1;
                    break;
                default:
                    if (typeof object.type === "number" && (object.type | 0) === object.type)
                        message.type = object.type;
                }
                if (object.selectedDisplayText != null)
                    message.selectedDisplayText = String(object.selectedDisplayText);
                return message;
            };

            /**
             * Creates a plain object from a ButtonsResponseMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.ButtonsResponseMessage
             * @static
             * @param {proto.Message.ButtonsResponseMessage} message ButtonsResponseMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ButtonsResponseMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.selectedButtonId != null && Object.hasOwnProperty.call(message, "selectedButtonId"))
                    object.selectedButtonId = message.selectedButtonId;
                if (message.selectedDisplayText != null && Object.hasOwnProperty.call(message, "selectedDisplayText")) {
                    object.selectedDisplayText = message.selectedDisplayText;
                    if (options.oneofs)
                        object.response = "selectedDisplayText";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    object.type = options.enums === String ? $root.proto.Message.ButtonsResponseMessage.Type[message.type] === undefined ? message.type : $root.proto.Message.ButtonsResponseMessage.Type[message.type] : message.type;
                return object;
            };

            /**
             * Converts this ButtonsResponseMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.ButtonsResponseMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ButtonsResponseMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ButtonsResponseMessage
             * @function getTypeUrl
             * @memberof proto.Message.ButtonsResponseMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ButtonsResponseMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.ButtonsResponseMessage";
            };

            /**
             * Type enum.
             * @name proto.Message.ButtonsResponseMessage.Type
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} DISPLAY_TEXT=1 DISPLAY_TEXT value
             */
            ButtonsResponseMessage.Type = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "DISPLAY_TEXT"] = 1;
                return values;
            })();

            return ButtonsResponseMessage;
        })();

        Message.Call = (function() {

            /**
             * Properties of a Call.
             * @memberof proto.Message
             * @interface ICall
             * @property {Uint8Array|null} [callKey] Call callKey
             * @property {string|null} [conversionSource] Call conversionSource
             * @property {Uint8Array|null} [conversionData] Call conversionData
             * @property {number|null} [conversionDelaySeconds] Call conversionDelaySeconds
             * @property {string|null} [ctwaSignals] Call ctwaSignals
             * @property {Uint8Array|null} [ctwaPayload] Call ctwaPayload
             * @property {proto.IContextInfo|null} [contextInfo] Call contextInfo
             * @property {string|null} [nativeFlowCallButtonPayload] Call nativeFlowCallButtonPayload
             * @property {string|null} [deeplinkPayload] Call deeplinkPayload
             * @property {proto.IMessageContextInfo|null} [messageContextInfo] Call messageContextInfo
             * @property {number|null} [callEntryPoint] Call callEntryPoint
             */

            /**
             * Constructs a new Call.
             * @memberof proto.Message
             * @classdesc Represents a Call.
             * @implements ICall
             * @constructor
             * @param {proto.Message.ICall=} [properties] Properties to set
             */
            function Call(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Call callKey.
             * @member {Uint8Array|null|undefined} callKey
             * @memberof proto.Message.Call
             * @instance
             */
            Call.prototype.callKey = null;

            /**
             * Call conversionSource.
             * @member {string|null|undefined} conversionSource
             * @memberof proto.Message.Call
             * @instance
             */
            Call.prototype.conversionSource = null;

            /**
             * Call conversionData.
             * @member {Uint8Array|null|undefined} conversionData
             * @memberof proto.Message.Call
             * @instance
             */
            Call.prototype.conversionData = null;

            /**
             * Call conversionDelaySeconds.
             * @member {number|null|undefined} conversionDelaySeconds
             * @memberof proto.Message.Call
             * @instance
             */
            Call.prototype.conversionDelaySeconds = null;

            /**
             * Call ctwaSignals.
             * @member {string|null|undefined} ctwaSignals
             * @memberof proto.Message.Call
             * @instance
             */
            Call.prototype.ctwaSignals = null;

            /**
             * Call ctwaPayload.
             * @member {Uint8Array|null|undefined} ctwaPayload
             * @memberof proto.Message.Call
             * @instance
             */
            Call.prototype.ctwaPayload = null;

            /**
             * Call contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.Call
             * @instance
             */
            Call.prototype.contextInfo = null;

            /**
             * Call nativeFlowCallButtonPayload.
             * @member {string|null|undefined} nativeFlowCallButtonPayload
             * @memberof proto.Message.Call
             * @instance
             */
            Call.prototype.nativeFlowCallButtonPayload = null;

            /**
             * Call deeplinkPayload.
             * @member {string|null|undefined} deeplinkPayload
             * @memberof proto.Message.Call
             * @instance
             */
            Call.prototype.deeplinkPayload = null;

            /**
             * Call messageContextInfo.
             * @member {proto.IMessageContextInfo|null|undefined} messageContextInfo
             * @memberof proto.Message.Call
             * @instance
             */
            Call.prototype.messageContextInfo = null;

            /**
             * Call callEntryPoint.
             * @member {number|null|undefined} callEntryPoint
             * @memberof proto.Message.Call
             * @instance
             */
            Call.prototype.callEntryPoint = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Call.prototype, "_callKey", {
                get: $util.oneOfGetter($oneOfFields = ["callKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Call.prototype, "_conversionSource", {
                get: $util.oneOfGetter($oneOfFields = ["conversionSource"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Call.prototype, "_conversionData", {
                get: $util.oneOfGetter($oneOfFields = ["conversionData"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Call.prototype, "_conversionDelaySeconds", {
                get: $util.oneOfGetter($oneOfFields = ["conversionDelaySeconds"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Call.prototype, "_ctwaSignals", {
                get: $util.oneOfGetter($oneOfFields = ["ctwaSignals"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Call.prototype, "_ctwaPayload", {
                get: $util.oneOfGetter($oneOfFields = ["ctwaPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Call.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Call.prototype, "_nativeFlowCallButtonPayload", {
                get: $util.oneOfGetter($oneOfFields = ["nativeFlowCallButtonPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Call.prototype, "_deeplinkPayload", {
                get: $util.oneOfGetter($oneOfFields = ["deeplinkPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Call.prototype, "_messageContextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["messageContextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Call.prototype, "_callEntryPoint", {
                get: $util.oneOfGetter($oneOfFields = ["callEntryPoint"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new Call instance using the specified properties.
             * @function create
             * @memberof proto.Message.Call
             * @static
             * @param {proto.Message.ICall=} [properties] Properties to set
             * @returns {proto.Message.Call} Call instance
             */
            Call.create = function create(properties) {
                return new Call(properties);
            };

            /**
             * Encodes the specified Call message. Does not implicitly {@link proto.Message.Call.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.Call
             * @static
             * @param {proto.Message.ICall} message Call message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Call.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.callKey != null && Object.hasOwnProperty.call(message, "callKey"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.callKey);
                if (message.conversionSource != null && Object.hasOwnProperty.call(message, "conversionSource"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.conversionSource);
                if (message.conversionData != null && Object.hasOwnProperty.call(message, "conversionData"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.conversionData);
                if (message.conversionDelaySeconds != null && Object.hasOwnProperty.call(message, "conversionDelaySeconds"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.conversionDelaySeconds);
                if (message.ctwaSignals != null && Object.hasOwnProperty.call(message, "ctwaSignals"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.ctwaSignals);
                if (message.ctwaPayload != null && Object.hasOwnProperty.call(message, "ctwaPayload"))
                    writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.ctwaPayload);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
                if (message.nativeFlowCallButtonPayload != null && Object.hasOwnProperty.call(message, "nativeFlowCallButtonPayload"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.nativeFlowCallButtonPayload);
                if (message.deeplinkPayload != null && Object.hasOwnProperty.call(message, "deeplinkPayload"))
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.deeplinkPayload);
                if (message.messageContextInfo != null && Object.hasOwnProperty.call(message, "messageContextInfo"))
                    $root.proto.MessageContextInfo.encode(message.messageContextInfo, writer.uint32(/* id 10, wireType 2 =*/82).fork(), q + 1).ldelim();
                if (message.callEntryPoint != null && Object.hasOwnProperty.call(message, "callEntryPoint"))
                    writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.callEntryPoint);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Call message, length delimited. Does not implicitly {@link proto.Message.Call.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.Call
             * @static
             * @param {proto.Message.ICall} message Call message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Call.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a Call message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.Call
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.Call} Call
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Call.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.callKey = reader.bytes();
                            message._callKey = "callKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.conversionSource = reader.stringVerify();
                            message._conversionSource = "conversionSource";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.conversionData = reader.bytes();
                            message._conversionData = "conversionData";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.conversionDelaySeconds = reader.uint32();
                            message._conversionDelaySeconds = "conversionDelaySeconds";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.ctwaSignals = reader.stringVerify();
                            message._ctwaSignals = "ctwaSignals";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.ctwaPayload = reader.bytes();
                            message._ctwaPayload = "ctwaPayload";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.nativeFlowCallButtonPayload = reader.stringVerify();
                            message._nativeFlowCallButtonPayload = "nativeFlowCallButtonPayload";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            message.deeplinkPayload = reader.stringVerify();
                            message._deeplinkPayload = "deeplinkPayload";
                            continue;
                        }
                    case 10: {
                            if (u !== 2)
                                break;
                            message.messageContextInfo = $root.proto.MessageContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.messageContextInfo);
                            message._messageContextInfo = "messageContextInfo";
                            continue;
                        }
                    case 11: {
                            if (u !== 0)
                                break;
                            message.callEntryPoint = reader.uint32();
                            message._callEntryPoint = "callEntryPoint";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a Call message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.Call
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.Call} Call
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Call.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Call message.
             * @function verify
             * @memberof proto.Message.Call
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Call.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.callKey != null && Object.hasOwnProperty.call(message, "callKey")) {
                    properties._callKey = 1;
                    if (!(message.callKey && typeof message.callKey.length === "number" || $util.isString(message.callKey)))
                        return "callKey: buffer expected";
                }
                if (message.conversionSource != null && Object.hasOwnProperty.call(message, "conversionSource")) {
                    properties._conversionSource = 1;
                    if (!$util.isString(message.conversionSource))
                        return "conversionSource: string expected";
                }
                if (message.conversionData != null && Object.hasOwnProperty.call(message, "conversionData")) {
                    properties._conversionData = 1;
                    if (!(message.conversionData && typeof message.conversionData.length === "number" || $util.isString(message.conversionData)))
                        return "conversionData: buffer expected";
                }
                if (message.conversionDelaySeconds != null && Object.hasOwnProperty.call(message, "conversionDelaySeconds")) {
                    properties._conversionDelaySeconds = 1;
                    if (!$util.isInteger(message.conversionDelaySeconds))
                        return "conversionDelaySeconds: integer expected";
                }
                if (message.ctwaSignals != null && Object.hasOwnProperty.call(message, "ctwaSignals")) {
                    properties._ctwaSignals = 1;
                    if (!$util.isString(message.ctwaSignals))
                        return "ctwaSignals: string expected";
                }
                if (message.ctwaPayload != null && Object.hasOwnProperty.call(message, "ctwaPayload")) {
                    properties._ctwaPayload = 1;
                    if (!(message.ctwaPayload && typeof message.ctwaPayload.length === "number" || $util.isString(message.ctwaPayload)))
                        return "ctwaPayload: buffer expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.nativeFlowCallButtonPayload != null && Object.hasOwnProperty.call(message, "nativeFlowCallButtonPayload")) {
                    properties._nativeFlowCallButtonPayload = 1;
                    if (!$util.isString(message.nativeFlowCallButtonPayload))
                        return "nativeFlowCallButtonPayload: string expected";
                }
                if (message.deeplinkPayload != null && Object.hasOwnProperty.call(message, "deeplinkPayload")) {
                    properties._deeplinkPayload = 1;
                    if (!$util.isString(message.deeplinkPayload))
                        return "deeplinkPayload: string expected";
                }
                if (message.messageContextInfo != null && Object.hasOwnProperty.call(message, "messageContextInfo")) {
                    properties._messageContextInfo = 1;
                    {
                        var error = $root.proto.MessageContextInfo.verify(message.messageContextInfo, q + 1);
                        if (error)
                            return "messageContextInfo." + error;
                    }
                }
                if (message.callEntryPoint != null && Object.hasOwnProperty.call(message, "callEntryPoint")) {
                    properties._callEntryPoint = 1;
                    if (!$util.isInteger(message.callEntryPoint))
                        return "callEntryPoint: integer expected";
                }
                return null;
            };

            /**
             * Creates a Call message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.Call
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.Call} Call
             */
            Call.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.Call: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.callKey != null)
                    if (typeof object.callKey === "string")
                        $util.base64.decode(object.callKey, message.callKey = $util.newBuffer($util.base64.length(object.callKey)), 0);
                    else if (object.callKey.length >= 0)
                        message.callKey = object.callKey;
                if (object.conversionSource != null)
                    message.conversionSource = String(object.conversionSource);
                if (object.conversionData != null)
                    if (typeof object.conversionData === "string")
                        $util.base64.decode(object.conversionData, message.conversionData = $util.newBuffer($util.base64.length(object.conversionData)), 0);
                    else if (object.conversionData.length >= 0)
                        message.conversionData = object.conversionData;
                if (object.conversionDelaySeconds != null)
                    message.conversionDelaySeconds = object.conversionDelaySeconds >>> 0;
                if (object.ctwaSignals != null)
                    message.ctwaSignals = String(object.ctwaSignals);
                if (object.ctwaPayload != null)
                    if (typeof object.ctwaPayload === "string")
                        $util.base64.decode(object.ctwaPayload, message.ctwaPayload = $util.newBuffer($util.base64.length(object.ctwaPayload)), 0);
                    else if (object.ctwaPayload.length >= 0)
                        message.ctwaPayload = object.ctwaPayload;
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.Call.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                if (object.nativeFlowCallButtonPayload != null)
                    message.nativeFlowCallButtonPayload = String(object.nativeFlowCallButtonPayload);
                if (object.deeplinkPayload != null)
                    message.deeplinkPayload = String(object.deeplinkPayload);
                if (object.messageContextInfo != null) {
                    if (!$util.isObject(object.messageContextInfo))
                        throw TypeError(".proto.Message.Call.messageContextInfo: object expected");
                    message.messageContextInfo = $root.proto.MessageContextInfo.fromObject(object.messageContextInfo, q + 1);
                }
                if (object.callEntryPoint != null)
                    message.callEntryPoint = object.callEntryPoint >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a Call message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.Call
             * @static
             * @param {proto.Message.Call} message Call
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Call.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.callKey != null && Object.hasOwnProperty.call(message, "callKey"))
                    object.callKey = options.bytes === String ? $util.base64.encode(message.callKey, 0, message.callKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.callKey) : message.callKey;
                if (message.conversionSource != null && Object.hasOwnProperty.call(message, "conversionSource"))
                    object.conversionSource = message.conversionSource;
                if (message.conversionData != null && Object.hasOwnProperty.call(message, "conversionData"))
                    object.conversionData = options.bytes === String ? $util.base64.encode(message.conversionData, 0, message.conversionData.length) : options.bytes === Array ? Array.prototype.slice.call(message.conversionData) : message.conversionData;
                if (message.conversionDelaySeconds != null && Object.hasOwnProperty.call(message, "conversionDelaySeconds"))
                    object.conversionDelaySeconds = message.conversionDelaySeconds;
                if (message.ctwaSignals != null && Object.hasOwnProperty.call(message, "ctwaSignals"))
                    object.ctwaSignals = message.ctwaSignals;
                if (message.ctwaPayload != null && Object.hasOwnProperty.call(message, "ctwaPayload"))
                    object.ctwaPayload = options.bytes === String ? $util.base64.encode(message.ctwaPayload, 0, message.ctwaPayload.length) : options.bytes === Array ? Array.prototype.slice.call(message.ctwaPayload) : message.ctwaPayload;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.nativeFlowCallButtonPayload != null && Object.hasOwnProperty.call(message, "nativeFlowCallButtonPayload"))
                    object.nativeFlowCallButtonPayload = message.nativeFlowCallButtonPayload;
                if (message.deeplinkPayload != null && Object.hasOwnProperty.call(message, "deeplinkPayload"))
                    object.deeplinkPayload = message.deeplinkPayload;
                if (message.messageContextInfo != null && Object.hasOwnProperty.call(message, "messageContextInfo"))
                    object.messageContextInfo = $root.proto.MessageContextInfo.toObject(message.messageContextInfo, options, q + 1);
                if (message.callEntryPoint != null && Object.hasOwnProperty.call(message, "callEntryPoint"))
                    object.callEntryPoint = message.callEntryPoint;
                return object;
            };

            /**
             * Converts this Call to JSON.
             * @function toJSON
             * @memberof proto.Message.Call
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Call.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Call
             * @function getTypeUrl
             * @memberof proto.Message.Call
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Call.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.Call";
            };

            return Call;
        })();

        Message.CallLogMessage = (function() {

            /**
             * Properties of a CallLogMessage.
             * @memberof proto.Message
             * @interface ICallLogMessage
             * @property {boolean|null} [isVideo] CallLogMessage isVideo
             * @property {proto.Message.CallLogMessage.CallOutcome|null} [callOutcome] CallLogMessage callOutcome
             * @property {number|Long|null} [durationSecs] CallLogMessage durationSecs
             * @property {proto.Message.CallLogMessage.CallType|null} [callType] CallLogMessage callType
             * @property {Array.<proto.Message.CallLogMessage.ICallParticipant>|null} [participants] CallLogMessage participants
             */

            /**
             * Constructs a new CallLogMessage.
             * @memberof proto.Message
             * @classdesc Represents a CallLogMessage.
             * @implements ICallLogMessage
             * @constructor
             * @param {proto.Message.ICallLogMessage=} [properties] Properties to set
             */
            function CallLogMessage(properties) {
                this.participants = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * CallLogMessage isVideo.
             * @member {boolean|null|undefined} isVideo
             * @memberof proto.Message.CallLogMessage
             * @instance
             */
            CallLogMessage.prototype.isVideo = null;

            /**
             * CallLogMessage callOutcome.
             * @member {proto.Message.CallLogMessage.CallOutcome|null|undefined} callOutcome
             * @memberof proto.Message.CallLogMessage
             * @instance
             */
            CallLogMessage.prototype.callOutcome = null;

            /**
             * CallLogMessage durationSecs.
             * @member {number|Long|null|undefined} durationSecs
             * @memberof proto.Message.CallLogMessage
             * @instance
             */
            CallLogMessage.prototype.durationSecs = null;

            /**
             * CallLogMessage callType.
             * @member {proto.Message.CallLogMessage.CallType|null|undefined} callType
             * @memberof proto.Message.CallLogMessage
             * @instance
             */
            CallLogMessage.prototype.callType = null;

            /**
             * CallLogMessage participants.
             * @member {Array.<proto.Message.CallLogMessage.ICallParticipant>} participants
             * @memberof proto.Message.CallLogMessage
             * @instance
             */
            CallLogMessage.prototype.participants = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CallLogMessage.prototype, "_isVideo", {
                get: $util.oneOfGetter($oneOfFields = ["isVideo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CallLogMessage.prototype, "_callOutcome", {
                get: $util.oneOfGetter($oneOfFields = ["callOutcome"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CallLogMessage.prototype, "_durationSecs", {
                get: $util.oneOfGetter($oneOfFields = ["durationSecs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CallLogMessage.prototype, "_callType", {
                get: $util.oneOfGetter($oneOfFields = ["callType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new CallLogMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.CallLogMessage
             * @static
             * @param {proto.Message.ICallLogMessage=} [properties] Properties to set
             * @returns {proto.Message.CallLogMessage} CallLogMessage instance
             */
            CallLogMessage.create = function create(properties) {
                return new CallLogMessage(properties);
            };

            /**
             * Encodes the specified CallLogMessage message. Does not implicitly {@link proto.Message.CallLogMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.CallLogMessage
             * @static
             * @param {proto.Message.ICallLogMessage} message CallLogMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CallLogMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.isVideo != null && Object.hasOwnProperty.call(message, "isVideo"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.isVideo);
                if (message.callOutcome != null && Object.hasOwnProperty.call(message, "callOutcome"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.callOutcome);
                if (message.durationSecs != null && Object.hasOwnProperty.call(message, "durationSecs"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.durationSecs);
                if (message.callType != null && Object.hasOwnProperty.call(message, "callType"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int32(message.callType);
                if (message.participants != null && message.participants.length)
                    for (var i = 0; i < message.participants.length; ++i)
                        $root.proto.Message.CallLogMessage.CallParticipant.encode(message.participants[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CallLogMessage message, length delimited. Does not implicitly {@link proto.Message.CallLogMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.CallLogMessage
             * @static
             * @param {proto.Message.ICallLogMessage} message CallLogMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CallLogMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a CallLogMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.CallLogMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.CallLogMessage} CallLogMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CallLogMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.isVideo = reader.bool();
                            message._isVideo = "isVideo";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.callOutcome = reader.int32();
                            message._callOutcome = "callOutcome";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.durationSecs = reader.int64();
                            message._durationSecs = "durationSecs";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.callType = reader.int32();
                            message._callType = "callType";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            if (!(message.participants && message.participants.length))
                                message.participants = [];
                            message.participants.push($root.proto.Message.CallLogMessage.CallParticipant.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a CallLogMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.CallLogMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.CallLogMessage} CallLogMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CallLogMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CallLogMessage message.
             * @function verify
             * @memberof proto.Message.CallLogMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CallLogMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.isVideo != null && Object.hasOwnProperty.call(message, "isVideo")) {
                    properties._isVideo = 1;
                    if (typeof message.isVideo !== "boolean")
                        return "isVideo: boolean expected";
                }
                if (message.callOutcome != null && Object.hasOwnProperty.call(message, "callOutcome")) {
                    properties._callOutcome = 1;
                    if (typeof message.callOutcome !== "number" || (message.callOutcome | 0) !== message.callOutcome)
                        return "callOutcome: enum value expected";
                }
                if (message.durationSecs != null && Object.hasOwnProperty.call(message, "durationSecs")) {
                    properties._durationSecs = 1;
                    if (!$util.isInteger(message.durationSecs) && !(message.durationSecs && $util.isInteger(message.durationSecs.low) && $util.isInteger(message.durationSecs.high)))
                        return "durationSecs: integer|Long expected";
                }
                if (message.callType != null && Object.hasOwnProperty.call(message, "callType")) {
                    properties._callType = 1;
                    if (typeof message.callType !== "number" || (message.callType | 0) !== message.callType)
                        return "callType: enum value expected";
                }
                if (message.participants != null && Object.hasOwnProperty.call(message, "participants")) {
                    if (!Array.isArray(message.participants))
                        return "participants: array expected";
                    for (var i = 0; i < message.participants.length; ++i) {
                        var error = $root.proto.Message.CallLogMessage.CallParticipant.verify(message.participants[i], q + 1);
                        if (error)
                            return "participants." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a CallLogMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.CallLogMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.CallLogMessage} CallLogMessage
             */
            CallLogMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.CallLogMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.isVideo != null)
                    message.isVideo = Boolean(object.isVideo);
                switch (object.callOutcome) {
                case "CONNECTED":
                case 0:
                    message.callOutcome = 0;
                    break;
                case "MISSED":
                case 1:
                    message.callOutcome = 1;
                    break;
                case "FAILED":
                case 2:
                    message.callOutcome = 2;
                    break;
                case "REJECTED":
                case 3:
                    message.callOutcome = 3;
                    break;
                case "ACCEPTED_ELSEWHERE":
                case 4:
                    message.callOutcome = 4;
                    break;
                case "ONGOING":
                case 5:
                    message.callOutcome = 5;
                    break;
                case "SILENCED_BY_DND":
                case 6:
                    message.callOutcome = 6;
                    break;
                case "SILENCED_UNKNOWN_CALLER":
                case 7:
                    message.callOutcome = 7;
                    break;
                default:
                    if (typeof object.callOutcome === "number" && (object.callOutcome | 0) === object.callOutcome)
                        message.callOutcome = object.callOutcome;
                }
                if (object.durationSecs != null)
                    if ($util.Long)
                        message.durationSecs = $util.Long.fromValue(object.durationSecs, false);
                    else if (typeof object.durationSecs === "string")
                        message.durationSecs = parseInt(object.durationSecs, 10);
                    else if (typeof object.durationSecs === "number")
                        message.durationSecs = object.durationSecs;
                    else if (typeof object.durationSecs === "object")
                        message.durationSecs = new $util.LongBits(object.durationSecs.low >>> 0, object.durationSecs.high >>> 0).toNumber();
                switch (object.callType) {
                case "REGULAR":
                case 0:
                    message.callType = 0;
                    break;
                case "SCHEDULED_CALL":
                case 1:
                    message.callType = 1;
                    break;
                case "VOICE_CHAT":
                case 2:
                    message.callType = 2;
                    break;
                default:
                    if (typeof object.callType === "number" && (object.callType | 0) === object.callType)
                        message.callType = object.callType;
                }
                if (object.participants) {
                    if (!Array.isArray(object.participants))
                        throw TypeError(".proto.Message.CallLogMessage.participants: array expected");
                    message.participants = Array(object.participants.length);
                    for (var i = 0; i < object.participants.length; ++i) {
                        if (!$util.isObject(object.participants[i]))
                            throw TypeError(".proto.Message.CallLogMessage.participants: object expected");
                        message.participants[i] = $root.proto.Message.CallLogMessage.CallParticipant.fromObject(object.participants[i], q + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a CallLogMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.CallLogMessage
             * @static
             * @param {proto.Message.CallLogMessage} message CallLogMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CallLogMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.participants = [];
                if (message.isVideo != null && Object.hasOwnProperty.call(message, "isVideo"))
                    object.isVideo = message.isVideo;
                if (message.callOutcome != null && Object.hasOwnProperty.call(message, "callOutcome"))
                    object.callOutcome = options.enums === String ? $root.proto.Message.CallLogMessage.CallOutcome[message.callOutcome] === undefined ? message.callOutcome : $root.proto.Message.CallLogMessage.CallOutcome[message.callOutcome] : message.callOutcome;
                if (message.durationSecs != null && Object.hasOwnProperty.call(message, "durationSecs"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.durationSecs = typeof message.durationSecs === "number" ? BigInt(message.durationSecs) : $util.Long.fromBits(message.durationSecs.low >>> 0, message.durationSecs.high >>> 0, false).toBigInt();
                    else if (typeof message.durationSecs === "number")
                        object.durationSecs = options.longs === String ? String(message.durationSecs) : message.durationSecs;
                    else
                        object.durationSecs = options.longs === String ? $util.Long.prototype.toString.call(message.durationSecs) : options.longs === Number ? new $util.LongBits(message.durationSecs.low >>> 0, message.durationSecs.high >>> 0).toNumber() : message.durationSecs;
                if (message.callType != null && Object.hasOwnProperty.call(message, "callType"))
                    object.callType = options.enums === String ? $root.proto.Message.CallLogMessage.CallType[message.callType] === undefined ? message.callType : $root.proto.Message.CallLogMessage.CallType[message.callType] : message.callType;
                if (message.participants && message.participants.length) {
                    object.participants = Array(message.participants.length);
                    for (var j = 0; j < message.participants.length; ++j)
                        object.participants[j] = $root.proto.Message.CallLogMessage.CallParticipant.toObject(message.participants[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this CallLogMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.CallLogMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CallLogMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for CallLogMessage
             * @function getTypeUrl
             * @memberof proto.Message.CallLogMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            CallLogMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.CallLogMessage";
            };

            /**
             * CallOutcome enum.
             * @name proto.Message.CallLogMessage.CallOutcome
             * @enum {number}
             * @property {number} CONNECTED=0 CONNECTED value
             * @property {number} MISSED=1 MISSED value
             * @property {number} FAILED=2 FAILED value
             * @property {number} REJECTED=3 REJECTED value
             * @property {number} ACCEPTED_ELSEWHERE=4 ACCEPTED_ELSEWHERE value
             * @property {number} ONGOING=5 ONGOING value
             * @property {number} SILENCED_BY_DND=6 SILENCED_BY_DND value
             * @property {number} SILENCED_UNKNOWN_CALLER=7 SILENCED_UNKNOWN_CALLER value
             */
            CallLogMessage.CallOutcome = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "CONNECTED"] = 0;
                values[valuesById[1] = "MISSED"] = 1;
                values[valuesById[2] = "FAILED"] = 2;
                values[valuesById[3] = "REJECTED"] = 3;
                values[valuesById[4] = "ACCEPTED_ELSEWHERE"] = 4;
                values[valuesById[5] = "ONGOING"] = 5;
                values[valuesById[6] = "SILENCED_BY_DND"] = 6;
                values[valuesById[7] = "SILENCED_UNKNOWN_CALLER"] = 7;
                return values;
            })();

            CallLogMessage.CallParticipant = (function() {

                /**
                 * Properties of a CallParticipant.
                 * @memberof proto.Message.CallLogMessage
                 * @interface ICallParticipant
                 * @property {string|null} [jid] CallParticipant jid
                 * @property {proto.Message.CallLogMessage.CallOutcome|null} [callOutcome] CallParticipant callOutcome
                 */

                /**
                 * Constructs a new CallParticipant.
                 * @memberof proto.Message.CallLogMessage
                 * @classdesc Represents a CallParticipant.
                 * @implements ICallParticipant
                 * @constructor
                 * @param {proto.Message.CallLogMessage.ICallParticipant=} [properties] Properties to set
                 */
                function CallParticipant(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * CallParticipant jid.
                 * @member {string|null|undefined} jid
                 * @memberof proto.Message.CallLogMessage.CallParticipant
                 * @instance
                 */
                CallParticipant.prototype.jid = null;

                /**
                 * CallParticipant callOutcome.
                 * @member {proto.Message.CallLogMessage.CallOutcome|null|undefined} callOutcome
                 * @memberof proto.Message.CallLogMessage.CallParticipant
                 * @instance
                 */
                CallParticipant.prototype.callOutcome = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CallParticipant.prototype, "_jid", {
                    get: $util.oneOfGetter($oneOfFields = ["jid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CallParticipant.prototype, "_callOutcome", {
                    get: $util.oneOfGetter($oneOfFields = ["callOutcome"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new CallParticipant instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.CallLogMessage.CallParticipant
                 * @static
                 * @param {proto.Message.CallLogMessage.ICallParticipant=} [properties] Properties to set
                 * @returns {proto.Message.CallLogMessage.CallParticipant} CallParticipant instance
                 */
                CallParticipant.create = function create(properties) {
                    return new CallParticipant(properties);
                };

                /**
                 * Encodes the specified CallParticipant message. Does not implicitly {@link proto.Message.CallLogMessage.CallParticipant.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.CallLogMessage.CallParticipant
                 * @static
                 * @param {proto.Message.CallLogMessage.ICallParticipant} message CallParticipant message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                CallParticipant.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.jid != null && Object.hasOwnProperty.call(message, "jid"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.jid);
                    if (message.callOutcome != null && Object.hasOwnProperty.call(message, "callOutcome"))
                        writer.uint32(/* id 2, wireType 0 =*/16).int32(message.callOutcome);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified CallParticipant message, length delimited. Does not implicitly {@link proto.Message.CallLogMessage.CallParticipant.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.CallLogMessage.CallParticipant
                 * @static
                 * @param {proto.Message.CallLogMessage.ICallParticipant} message CallParticipant message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                CallParticipant.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a CallParticipant message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.CallLogMessage.CallParticipant
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.CallLogMessage.CallParticipant} CallParticipant
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                CallParticipant.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.jid = reader.stringVerify();
                                message._jid = "jid";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                message.callOutcome = reader.int32();
                                message._callOutcome = "callOutcome";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a CallParticipant message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.CallLogMessage.CallParticipant
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.CallLogMessage.CallParticipant} CallParticipant
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                CallParticipant.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a CallParticipant message.
                 * @function verify
                 * @memberof proto.Message.CallLogMessage.CallParticipant
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                CallParticipant.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.jid != null && Object.hasOwnProperty.call(message, "jid")) {
                        properties._jid = 1;
                        if (!$util.isString(message.jid))
                            return "jid: string expected";
                    }
                    if (message.callOutcome != null && Object.hasOwnProperty.call(message, "callOutcome")) {
                        properties._callOutcome = 1;
                        if (typeof message.callOutcome !== "number" || (message.callOutcome | 0) !== message.callOutcome)
                            return "callOutcome: enum value expected";
                    }
                    return null;
                };

                /**
                 * Creates a CallParticipant message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.CallLogMessage.CallParticipant
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.CallLogMessage.CallParticipant} CallParticipant
                 */
                CallParticipant.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.CallLogMessage.CallParticipant: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.jid != null)
                        message.jid = String(object.jid);
                    switch (object.callOutcome) {
                    case "CONNECTED":
                    case 0:
                        message.callOutcome = 0;
                        break;
                    case "MISSED":
                    case 1:
                        message.callOutcome = 1;
                        break;
                    case "FAILED":
                    case 2:
                        message.callOutcome = 2;
                        break;
                    case "REJECTED":
                    case 3:
                        message.callOutcome = 3;
                        break;
                    case "ACCEPTED_ELSEWHERE":
                    case 4:
                        message.callOutcome = 4;
                        break;
                    case "ONGOING":
                    case 5:
                        message.callOutcome = 5;
                        break;
                    case "SILENCED_BY_DND":
                    case 6:
                        message.callOutcome = 6;
                        break;
                    case "SILENCED_UNKNOWN_CALLER":
                    case 7:
                        message.callOutcome = 7;
                        break;
                    default:
                        if (typeof object.callOutcome === "number" && (object.callOutcome | 0) === object.callOutcome)
                            message.callOutcome = object.callOutcome;
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a CallParticipant message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.CallLogMessage.CallParticipant
                 * @static
                 * @param {proto.Message.CallLogMessage.CallParticipant} message CallParticipant
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                CallParticipant.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.jid != null && Object.hasOwnProperty.call(message, "jid"))
                        object.jid = message.jid;
                    if (message.callOutcome != null && Object.hasOwnProperty.call(message, "callOutcome"))
                        object.callOutcome = options.enums === String ? $root.proto.Message.CallLogMessage.CallOutcome[message.callOutcome] === undefined ? message.callOutcome : $root.proto.Message.CallLogMessage.CallOutcome[message.callOutcome] : message.callOutcome;
                    return object;
                };

                /**
                 * Converts this CallParticipant to JSON.
                 * @function toJSON
                 * @memberof proto.Message.CallLogMessage.CallParticipant
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                CallParticipant.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for CallParticipant
                 * @function getTypeUrl
                 * @memberof proto.Message.CallLogMessage.CallParticipant
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                CallParticipant.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.CallLogMessage.CallParticipant";
                };

                return CallParticipant;
            })();

            /**
             * CallType enum.
             * @name proto.Message.CallLogMessage.CallType
             * @enum {number}
             * @property {number} REGULAR=0 REGULAR value
             * @property {number} SCHEDULED_CALL=1 SCHEDULED_CALL value
             * @property {number} VOICE_CHAT=2 VOICE_CHAT value
             */
            CallLogMessage.CallType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "REGULAR"] = 0;
                values[valuesById[1] = "SCHEDULED_CALL"] = 1;
                values[valuesById[2] = "VOICE_CHAT"] = 2;
                return values;
            })();

            return CallLogMessage;
        })();

        Message.CancelPaymentRequestMessage = (function() {

            /**
             * Properties of a CancelPaymentRequestMessage.
             * @memberof proto.Message
             * @interface ICancelPaymentRequestMessage
             * @property {proto.IMessageKey|null} [key] CancelPaymentRequestMessage key
             */

            /**
             * Constructs a new CancelPaymentRequestMessage.
             * @memberof proto.Message
             * @classdesc Represents a CancelPaymentRequestMessage.
             * @implements ICancelPaymentRequestMessage
             * @constructor
             * @param {proto.Message.ICancelPaymentRequestMessage=} [properties] Properties to set
             */
            function CancelPaymentRequestMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * CancelPaymentRequestMessage key.
             * @member {proto.IMessageKey|null|undefined} key
             * @memberof proto.Message.CancelPaymentRequestMessage
             * @instance
             */
            CancelPaymentRequestMessage.prototype.key = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CancelPaymentRequestMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new CancelPaymentRequestMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.CancelPaymentRequestMessage
             * @static
             * @param {proto.Message.ICancelPaymentRequestMessage=} [properties] Properties to set
             * @returns {proto.Message.CancelPaymentRequestMessage} CancelPaymentRequestMessage instance
             */
            CancelPaymentRequestMessage.create = function create(properties) {
                return new CancelPaymentRequestMessage(properties);
            };

            /**
             * Encodes the specified CancelPaymentRequestMessage message. Does not implicitly {@link proto.Message.CancelPaymentRequestMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.CancelPaymentRequestMessage
             * @static
             * @param {proto.Message.ICancelPaymentRequestMessage} message CancelPaymentRequestMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CancelPaymentRequestMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    $root.proto.MessageKey.encode(message.key, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CancelPaymentRequestMessage message, length delimited. Does not implicitly {@link proto.Message.CancelPaymentRequestMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.CancelPaymentRequestMessage
             * @static
             * @param {proto.Message.ICancelPaymentRequestMessage} message CancelPaymentRequestMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CancelPaymentRequestMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a CancelPaymentRequestMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.CancelPaymentRequestMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.CancelPaymentRequestMessage} CancelPaymentRequestMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CancelPaymentRequestMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.key = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.key);
                            message._key = "key";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a CancelPaymentRequestMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.CancelPaymentRequestMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.CancelPaymentRequestMessage} CancelPaymentRequestMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CancelPaymentRequestMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CancelPaymentRequestMessage message.
             * @function verify
             * @memberof proto.Message.CancelPaymentRequestMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CancelPaymentRequestMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                    properties._key = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.key, q + 1);
                        if (error)
                            return "key." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a CancelPaymentRequestMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.CancelPaymentRequestMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.CancelPaymentRequestMessage} CancelPaymentRequestMessage
             */
            CancelPaymentRequestMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.CancelPaymentRequestMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.key != null) {
                    if (!$util.isObject(object.key))
                        throw TypeError(".proto.Message.CancelPaymentRequestMessage.key: object expected");
                    message.key = $root.proto.MessageKey.fromObject(object.key, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a CancelPaymentRequestMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.CancelPaymentRequestMessage
             * @static
             * @param {proto.Message.CancelPaymentRequestMessage} message CancelPaymentRequestMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CancelPaymentRequestMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    object.key = $root.proto.MessageKey.toObject(message.key, options, q + 1);
                return object;
            };

            /**
             * Converts this CancelPaymentRequestMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.CancelPaymentRequestMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CancelPaymentRequestMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for CancelPaymentRequestMessage
             * @function getTypeUrl
             * @memberof proto.Message.CancelPaymentRequestMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            CancelPaymentRequestMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.CancelPaymentRequestMessage";
            };

            return CancelPaymentRequestMessage;
        })();

        Message.Chat = (function() {

            /**
             * Properties of a Chat.
             * @memberof proto.Message
             * @interface IChat
             * @property {string|null} [displayName] Chat displayName
             * @property {string|null} [id] Chat id
             */

            /**
             * Constructs a new Chat.
             * @memberof proto.Message
             * @classdesc Represents a Chat.
             * @implements IChat
             * @constructor
             * @param {proto.Message.IChat=} [properties] Properties to set
             */
            function Chat(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Chat displayName.
             * @member {string|null|undefined} displayName
             * @memberof proto.Message.Chat
             * @instance
             */
            Chat.prototype.displayName = null;

            /**
             * Chat id.
             * @member {string|null|undefined} id
             * @memberof proto.Message.Chat
             * @instance
             */
            Chat.prototype.id = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Chat.prototype, "_displayName", {
                get: $util.oneOfGetter($oneOfFields = ["displayName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Chat.prototype, "_id", {
                get: $util.oneOfGetter($oneOfFields = ["id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new Chat instance using the specified properties.
             * @function create
             * @memberof proto.Message.Chat
             * @static
             * @param {proto.Message.IChat=} [properties] Properties to set
             * @returns {proto.Message.Chat} Chat instance
             */
            Chat.create = function create(properties) {
                return new Chat(properties);
            };

            /**
             * Encodes the specified Chat message. Does not implicitly {@link proto.Message.Chat.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.Chat
             * @static
             * @param {proto.Message.IChat} message Chat message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Chat.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.displayName != null && Object.hasOwnProperty.call(message, "displayName"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.displayName);
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.id);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Chat message, length delimited. Does not implicitly {@link proto.Message.Chat.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.Chat
             * @static
             * @param {proto.Message.IChat} message Chat message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Chat.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a Chat message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.Chat
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.Chat} Chat
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Chat.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.displayName = reader.stringVerify();
                            message._displayName = "displayName";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.id = reader.stringVerify();
                            message._id = "id";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a Chat message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.Chat
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.Chat} Chat
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Chat.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Chat message.
             * @function verify
             * @memberof proto.Message.Chat
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Chat.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.displayName != null && Object.hasOwnProperty.call(message, "displayName")) {
                    properties._displayName = 1;
                    if (!$util.isString(message.displayName))
                        return "displayName: string expected";
                }
                if (message.id != null && Object.hasOwnProperty.call(message, "id")) {
                    properties._id = 1;
                    if (!$util.isString(message.id))
                        return "id: string expected";
                }
                return null;
            };

            /**
             * Creates a Chat message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.Chat
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.Chat} Chat
             */
            Chat.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.Chat: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.displayName != null)
                    message.displayName = String(object.displayName);
                if (object.id != null)
                    message.id = String(object.id);
                return message;
            };

            /**
             * Creates a plain object from a Chat message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.Chat
             * @static
             * @param {proto.Message.Chat} message Chat
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Chat.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.displayName != null && Object.hasOwnProperty.call(message, "displayName"))
                    object.displayName = message.displayName;
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    object.id = message.id;
                return object;
            };

            /**
             * Converts this Chat to JSON.
             * @function toJSON
             * @memberof proto.Message.Chat
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Chat.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Chat
             * @function getTypeUrl
             * @memberof proto.Message.Chat
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Chat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.Chat";
            };

            return Chat;
        })();

        Message.ChatCustomImageWallpaper = (function() {

            /**
             * Properties of a ChatCustomImageWallpaper.
             * @memberof proto.Message
             * @interface IChatCustomImageWallpaper
             * @property {string|null} [directPath] ChatCustomImageWallpaper directPath
             * @property {Uint8Array|null} [mediaKey] ChatCustomImageWallpaper mediaKey
             * @property {Uint8Array|null} [fileEncSha256] ChatCustomImageWallpaper fileEncSha256
             * @property {Uint8Array|null} [fileSha256] ChatCustomImageWallpaper fileSha256
             * @property {number|null} [dimLevel] ChatCustomImageWallpaper dimLevel
             */

            /**
             * Constructs a new ChatCustomImageWallpaper.
             * @memberof proto.Message
             * @classdesc Represents a ChatCustomImageWallpaper.
             * @implements IChatCustomImageWallpaper
             * @constructor
             * @param {proto.Message.IChatCustomImageWallpaper=} [properties] Properties to set
             */
            function ChatCustomImageWallpaper(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ChatCustomImageWallpaper directPath.
             * @member {string|null|undefined} directPath
             * @memberof proto.Message.ChatCustomImageWallpaper
             * @instance
             */
            ChatCustomImageWallpaper.prototype.directPath = null;

            /**
             * ChatCustomImageWallpaper mediaKey.
             * @member {Uint8Array|null|undefined} mediaKey
             * @memberof proto.Message.ChatCustomImageWallpaper
             * @instance
             */
            ChatCustomImageWallpaper.prototype.mediaKey = null;

            /**
             * ChatCustomImageWallpaper fileEncSha256.
             * @member {Uint8Array|null|undefined} fileEncSha256
             * @memberof proto.Message.ChatCustomImageWallpaper
             * @instance
             */
            ChatCustomImageWallpaper.prototype.fileEncSha256 = null;

            /**
             * ChatCustomImageWallpaper fileSha256.
             * @member {Uint8Array|null|undefined} fileSha256
             * @memberof proto.Message.ChatCustomImageWallpaper
             * @instance
             */
            ChatCustomImageWallpaper.prototype.fileSha256 = null;

            /**
             * ChatCustomImageWallpaper dimLevel.
             * @member {number|null|undefined} dimLevel
             * @memberof proto.Message.ChatCustomImageWallpaper
             * @instance
             */
            ChatCustomImageWallpaper.prototype.dimLevel = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ChatCustomImageWallpaper.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ChatCustomImageWallpaper.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ChatCustomImageWallpaper.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ChatCustomImageWallpaper.prototype, "_fileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ChatCustomImageWallpaper.prototype, "_dimLevel", {
                get: $util.oneOfGetter($oneOfFields = ["dimLevel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ChatCustomImageWallpaper instance using the specified properties.
             * @function create
             * @memberof proto.Message.ChatCustomImageWallpaper
             * @static
             * @param {proto.Message.IChatCustomImageWallpaper=} [properties] Properties to set
             * @returns {proto.Message.ChatCustomImageWallpaper} ChatCustomImageWallpaper instance
             */
            ChatCustomImageWallpaper.create = function create(properties) {
                return new ChatCustomImageWallpaper(properties);
            };

            /**
             * Encodes the specified ChatCustomImageWallpaper message. Does not implicitly {@link proto.Message.ChatCustomImageWallpaper.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.ChatCustomImageWallpaper
             * @static
             * @param {proto.Message.IChatCustomImageWallpaper} message ChatCustomImageWallpaper message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ChatCustomImageWallpaper.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.directPath);
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.mediaKey);
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.fileEncSha256);
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.fileSha256);
                if (message.dimLevel != null && Object.hasOwnProperty.call(message, "dimLevel"))
                    writer.uint32(/* id 5, wireType 5 =*/45).float(message.dimLevel);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ChatCustomImageWallpaper message, length delimited. Does not implicitly {@link proto.Message.ChatCustomImageWallpaper.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.ChatCustomImageWallpaper
             * @static
             * @param {proto.Message.IChatCustomImageWallpaper} message ChatCustomImageWallpaper message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ChatCustomImageWallpaper.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ChatCustomImageWallpaper message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.ChatCustomImageWallpaper
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.ChatCustomImageWallpaper} ChatCustomImageWallpaper
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ChatCustomImageWallpaper.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.directPath = reader.stringVerify();
                            message._directPath = "directPath";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.mediaKey = reader.bytes();
                            message._mediaKey = "mediaKey";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.fileEncSha256 = reader.bytes();
                            message._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.fileSha256 = reader.bytes();
                            message._fileSha256 = "fileSha256";
                            continue;
                        }
                    case 5: {
                            if (u !== 5)
                                break;
                            message.dimLevel = reader.float();
                            message._dimLevel = "dimLevel";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ChatCustomImageWallpaper message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.ChatCustomImageWallpaper
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.ChatCustomImageWallpaper} ChatCustomImageWallpaper
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ChatCustomImageWallpaper.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ChatCustomImageWallpaper message.
             * @function verify
             * @memberof proto.Message.ChatCustomImageWallpaper
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ChatCustomImageWallpaper.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath")) {
                    properties._directPath = 1;
                    if (!$util.isString(message.directPath))
                        return "directPath: string expected";
                }
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey")) {
                    properties._mediaKey = 1;
                    if (!(message.mediaKey && typeof message.mediaKey.length === "number" || $util.isString(message.mediaKey)))
                        return "mediaKey: buffer expected";
                }
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256")) {
                    properties._fileEncSha256 = 1;
                    if (!(message.fileEncSha256 && typeof message.fileEncSha256.length === "number" || $util.isString(message.fileEncSha256)))
                        return "fileEncSha256: buffer expected";
                }
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256")) {
                    properties._fileSha256 = 1;
                    if (!(message.fileSha256 && typeof message.fileSha256.length === "number" || $util.isString(message.fileSha256)))
                        return "fileSha256: buffer expected";
                }
                if (message.dimLevel != null && Object.hasOwnProperty.call(message, "dimLevel")) {
                    properties._dimLevel = 1;
                    if (typeof message.dimLevel !== "number")
                        return "dimLevel: number expected";
                }
                return null;
            };

            /**
             * Creates a ChatCustomImageWallpaper message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.ChatCustomImageWallpaper
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.ChatCustomImageWallpaper} ChatCustomImageWallpaper
             */
            ChatCustomImageWallpaper.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.ChatCustomImageWallpaper: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.directPath != null)
                    message.directPath = String(object.directPath);
                if (object.mediaKey != null)
                    if (typeof object.mediaKey === "string")
                        $util.base64.decode(object.mediaKey, message.mediaKey = $util.newBuffer($util.base64.length(object.mediaKey)), 0);
                    else if (object.mediaKey.length >= 0)
                        message.mediaKey = object.mediaKey;
                if (object.fileEncSha256 != null)
                    if (typeof object.fileEncSha256 === "string")
                        $util.base64.decode(object.fileEncSha256, message.fileEncSha256 = $util.newBuffer($util.base64.length(object.fileEncSha256)), 0);
                    else if (object.fileEncSha256.length >= 0)
                        message.fileEncSha256 = object.fileEncSha256;
                if (object.fileSha256 != null)
                    if (typeof object.fileSha256 === "string")
                        $util.base64.decode(object.fileSha256, message.fileSha256 = $util.newBuffer($util.base64.length(object.fileSha256)), 0);
                    else if (object.fileSha256.length >= 0)
                        message.fileSha256 = object.fileSha256;
                if (object.dimLevel != null)
                    message.dimLevel = Number(object.dimLevel);
                return message;
            };

            /**
             * Creates a plain object from a ChatCustomImageWallpaper message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.ChatCustomImageWallpaper
             * @static
             * @param {proto.Message.ChatCustomImageWallpaper} message ChatCustomImageWallpaper
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ChatCustomImageWallpaper.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    object.directPath = message.directPath;
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    object.mediaKey = options.bytes === String ? $util.base64.encode(message.mediaKey, 0, message.mediaKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.mediaKey) : message.mediaKey;
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    object.fileEncSha256 = options.bytes === String ? $util.base64.encode(message.fileEncSha256, 0, message.fileEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileEncSha256) : message.fileEncSha256;
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                    object.fileSha256 = options.bytes === String ? $util.base64.encode(message.fileSha256, 0, message.fileSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileSha256) : message.fileSha256;
                if (message.dimLevel != null && Object.hasOwnProperty.call(message, "dimLevel"))
                    object.dimLevel = options.json && !isFinite(message.dimLevel) ? String(message.dimLevel) : message.dimLevel;
                return object;
            };

            /**
             * Converts this ChatCustomImageWallpaper to JSON.
             * @function toJSON
             * @memberof proto.Message.ChatCustomImageWallpaper
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ChatCustomImageWallpaper.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ChatCustomImageWallpaper
             * @function getTypeUrl
             * @memberof proto.Message.ChatCustomImageWallpaper
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ChatCustomImageWallpaper.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.ChatCustomImageWallpaper";
            };

            return ChatCustomImageWallpaper;
        })();

        Message.ChatDefaultWallpaper = (function() {

            /**
             * Properties of a ChatDefaultWallpaper.
             * @memberof proto.Message
             * @interface IChatDefaultWallpaper
             * @property {boolean|null} [isDoodleEnabled] ChatDefaultWallpaper isDoodleEnabled
             */

            /**
             * Constructs a new ChatDefaultWallpaper.
             * @memberof proto.Message
             * @classdesc Represents a ChatDefaultWallpaper.
             * @implements IChatDefaultWallpaper
             * @constructor
             * @param {proto.Message.IChatDefaultWallpaper=} [properties] Properties to set
             */
            function ChatDefaultWallpaper(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ChatDefaultWallpaper isDoodleEnabled.
             * @member {boolean|null|undefined} isDoodleEnabled
             * @memberof proto.Message.ChatDefaultWallpaper
             * @instance
             */
            ChatDefaultWallpaper.prototype.isDoodleEnabled = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ChatDefaultWallpaper.prototype, "_isDoodleEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isDoodleEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ChatDefaultWallpaper instance using the specified properties.
             * @function create
             * @memberof proto.Message.ChatDefaultWallpaper
             * @static
             * @param {proto.Message.IChatDefaultWallpaper=} [properties] Properties to set
             * @returns {proto.Message.ChatDefaultWallpaper} ChatDefaultWallpaper instance
             */
            ChatDefaultWallpaper.create = function create(properties) {
                return new ChatDefaultWallpaper(properties);
            };

            /**
             * Encodes the specified ChatDefaultWallpaper message. Does not implicitly {@link proto.Message.ChatDefaultWallpaper.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.ChatDefaultWallpaper
             * @static
             * @param {proto.Message.IChatDefaultWallpaper} message ChatDefaultWallpaper message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ChatDefaultWallpaper.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.isDoodleEnabled != null && Object.hasOwnProperty.call(message, "isDoodleEnabled"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.isDoodleEnabled);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ChatDefaultWallpaper message, length delimited. Does not implicitly {@link proto.Message.ChatDefaultWallpaper.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.ChatDefaultWallpaper
             * @static
             * @param {proto.Message.IChatDefaultWallpaper} message ChatDefaultWallpaper message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ChatDefaultWallpaper.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ChatDefaultWallpaper message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.ChatDefaultWallpaper
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.ChatDefaultWallpaper} ChatDefaultWallpaper
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ChatDefaultWallpaper.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.isDoodleEnabled = reader.bool();
                            message._isDoodleEnabled = "isDoodleEnabled";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ChatDefaultWallpaper message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.ChatDefaultWallpaper
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.ChatDefaultWallpaper} ChatDefaultWallpaper
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ChatDefaultWallpaper.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ChatDefaultWallpaper message.
             * @function verify
             * @memberof proto.Message.ChatDefaultWallpaper
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ChatDefaultWallpaper.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.isDoodleEnabled != null && Object.hasOwnProperty.call(message, "isDoodleEnabled")) {
                    properties._isDoodleEnabled = 1;
                    if (typeof message.isDoodleEnabled !== "boolean")
                        return "isDoodleEnabled: boolean expected";
                }
                return null;
            };

            /**
             * Creates a ChatDefaultWallpaper message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.ChatDefaultWallpaper
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.ChatDefaultWallpaper} ChatDefaultWallpaper
             */
            ChatDefaultWallpaper.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.ChatDefaultWallpaper: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.isDoodleEnabled != null)
                    message.isDoodleEnabled = Boolean(object.isDoodleEnabled);
                return message;
            };

            /**
             * Creates a plain object from a ChatDefaultWallpaper message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.ChatDefaultWallpaper
             * @static
             * @param {proto.Message.ChatDefaultWallpaper} message ChatDefaultWallpaper
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ChatDefaultWallpaper.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.isDoodleEnabled != null && Object.hasOwnProperty.call(message, "isDoodleEnabled"))
                    object.isDoodleEnabled = message.isDoodleEnabled;
                return object;
            };

            /**
             * Converts this ChatDefaultWallpaper to JSON.
             * @function toJSON
             * @memberof proto.Message.ChatDefaultWallpaper
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ChatDefaultWallpaper.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ChatDefaultWallpaper
             * @function getTypeUrl
             * @memberof proto.Message.ChatDefaultWallpaper
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ChatDefaultWallpaper.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.ChatDefaultWallpaper";
            };

            return ChatDefaultWallpaper;
        })();

        Message.ChatSolidColorWallpaper = (function() {

            /**
             * Properties of a ChatSolidColorWallpaper.
             * @memberof proto.Message
             * @interface IChatSolidColorWallpaper
             * @property {string|null} [colorLight] ChatSolidColorWallpaper colorLight
             * @property {string|null} [colorDark] ChatSolidColorWallpaper colorDark
             * @property {boolean|null} [isDoodleEnabled] ChatSolidColorWallpaper isDoodleEnabled
             */

            /**
             * Constructs a new ChatSolidColorWallpaper.
             * @memberof proto.Message
             * @classdesc Represents a ChatSolidColorWallpaper.
             * @implements IChatSolidColorWallpaper
             * @constructor
             * @param {proto.Message.IChatSolidColorWallpaper=} [properties] Properties to set
             */
            function ChatSolidColorWallpaper(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ChatSolidColorWallpaper colorLight.
             * @member {string|null|undefined} colorLight
             * @memberof proto.Message.ChatSolidColorWallpaper
             * @instance
             */
            ChatSolidColorWallpaper.prototype.colorLight = null;

            /**
             * ChatSolidColorWallpaper colorDark.
             * @member {string|null|undefined} colorDark
             * @memberof proto.Message.ChatSolidColorWallpaper
             * @instance
             */
            ChatSolidColorWallpaper.prototype.colorDark = null;

            /**
             * ChatSolidColorWallpaper isDoodleEnabled.
             * @member {boolean|null|undefined} isDoodleEnabled
             * @memberof proto.Message.ChatSolidColorWallpaper
             * @instance
             */
            ChatSolidColorWallpaper.prototype.isDoodleEnabled = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ChatSolidColorWallpaper.prototype, "_colorLight", {
                get: $util.oneOfGetter($oneOfFields = ["colorLight"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ChatSolidColorWallpaper.prototype, "_colorDark", {
                get: $util.oneOfGetter($oneOfFields = ["colorDark"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ChatSolidColorWallpaper.prototype, "_isDoodleEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isDoodleEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ChatSolidColorWallpaper instance using the specified properties.
             * @function create
             * @memberof proto.Message.ChatSolidColorWallpaper
             * @static
             * @param {proto.Message.IChatSolidColorWallpaper=} [properties] Properties to set
             * @returns {proto.Message.ChatSolidColorWallpaper} ChatSolidColorWallpaper instance
             */
            ChatSolidColorWallpaper.create = function create(properties) {
                return new ChatSolidColorWallpaper(properties);
            };

            /**
             * Encodes the specified ChatSolidColorWallpaper message. Does not implicitly {@link proto.Message.ChatSolidColorWallpaper.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.ChatSolidColorWallpaper
             * @static
             * @param {proto.Message.IChatSolidColorWallpaper} message ChatSolidColorWallpaper message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ChatSolidColorWallpaper.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.colorLight != null && Object.hasOwnProperty.call(message, "colorLight"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.colorLight);
                if (message.colorDark != null && Object.hasOwnProperty.call(message, "colorDark"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.colorDark);
                if (message.isDoodleEnabled != null && Object.hasOwnProperty.call(message, "isDoodleEnabled"))
                    writer.uint32(/* id 3, wireType 0 =*/24).bool(message.isDoodleEnabled);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ChatSolidColorWallpaper message, length delimited. Does not implicitly {@link proto.Message.ChatSolidColorWallpaper.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.ChatSolidColorWallpaper
             * @static
             * @param {proto.Message.IChatSolidColorWallpaper} message ChatSolidColorWallpaper message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ChatSolidColorWallpaper.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ChatSolidColorWallpaper message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.ChatSolidColorWallpaper
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.ChatSolidColorWallpaper} ChatSolidColorWallpaper
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ChatSolidColorWallpaper.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.colorLight = reader.stringVerify();
                            message._colorLight = "colorLight";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.colorDark = reader.stringVerify();
                            message._colorDark = "colorDark";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.isDoodleEnabled = reader.bool();
                            message._isDoodleEnabled = "isDoodleEnabled";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ChatSolidColorWallpaper message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.ChatSolidColorWallpaper
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.ChatSolidColorWallpaper} ChatSolidColorWallpaper
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ChatSolidColorWallpaper.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ChatSolidColorWallpaper message.
             * @function verify
             * @memberof proto.Message.ChatSolidColorWallpaper
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ChatSolidColorWallpaper.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.colorLight != null && Object.hasOwnProperty.call(message, "colorLight")) {
                    properties._colorLight = 1;
                    if (!$util.isString(message.colorLight))
                        return "colorLight: string expected";
                }
                if (message.colorDark != null && Object.hasOwnProperty.call(message, "colorDark")) {
                    properties._colorDark = 1;
                    if (!$util.isString(message.colorDark))
                        return "colorDark: string expected";
                }
                if (message.isDoodleEnabled != null && Object.hasOwnProperty.call(message, "isDoodleEnabled")) {
                    properties._isDoodleEnabled = 1;
                    if (typeof message.isDoodleEnabled !== "boolean")
                        return "isDoodleEnabled: boolean expected";
                }
                return null;
            };

            /**
             * Creates a ChatSolidColorWallpaper message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.ChatSolidColorWallpaper
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.ChatSolidColorWallpaper} ChatSolidColorWallpaper
             */
            ChatSolidColorWallpaper.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.ChatSolidColorWallpaper: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.colorLight != null)
                    message.colorLight = String(object.colorLight);
                if (object.colorDark != null)
                    message.colorDark = String(object.colorDark);
                if (object.isDoodleEnabled != null)
                    message.isDoodleEnabled = Boolean(object.isDoodleEnabled);
                return message;
            };

            /**
             * Creates a plain object from a ChatSolidColorWallpaper message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.ChatSolidColorWallpaper
             * @static
             * @param {proto.Message.ChatSolidColorWallpaper} message ChatSolidColorWallpaper
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ChatSolidColorWallpaper.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.colorLight != null && Object.hasOwnProperty.call(message, "colorLight"))
                    object.colorLight = message.colorLight;
                if (message.colorDark != null && Object.hasOwnProperty.call(message, "colorDark"))
                    object.colorDark = message.colorDark;
                if (message.isDoodleEnabled != null && Object.hasOwnProperty.call(message, "isDoodleEnabled"))
                    object.isDoodleEnabled = message.isDoodleEnabled;
                return object;
            };

            /**
             * Converts this ChatSolidColorWallpaper to JSON.
             * @function toJSON
             * @memberof proto.Message.ChatSolidColorWallpaper
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ChatSolidColorWallpaper.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ChatSolidColorWallpaper
             * @function getTypeUrl
             * @memberof proto.Message.ChatSolidColorWallpaper
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ChatSolidColorWallpaper.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.ChatSolidColorWallpaper";
            };

            return ChatSolidColorWallpaper;
        })();

        Message.ChatStockImageWallpaper = (function() {

            /**
             * Properties of a ChatStockImageWallpaper.
             * @memberof proto.Message
             * @interface IChatStockImageWallpaper
             * @property {string|null} [stockImageId] ChatStockImageWallpaper stockImageId
             * @property {number|null} [dimLevel] ChatStockImageWallpaper dimLevel
             */

            /**
             * Constructs a new ChatStockImageWallpaper.
             * @memberof proto.Message
             * @classdesc Represents a ChatStockImageWallpaper.
             * @implements IChatStockImageWallpaper
             * @constructor
             * @param {proto.Message.IChatStockImageWallpaper=} [properties] Properties to set
             */
            function ChatStockImageWallpaper(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ChatStockImageWallpaper stockImageId.
             * @member {string|null|undefined} stockImageId
             * @memberof proto.Message.ChatStockImageWallpaper
             * @instance
             */
            ChatStockImageWallpaper.prototype.stockImageId = null;

            /**
             * ChatStockImageWallpaper dimLevel.
             * @member {number|null|undefined} dimLevel
             * @memberof proto.Message.ChatStockImageWallpaper
             * @instance
             */
            ChatStockImageWallpaper.prototype.dimLevel = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ChatStockImageWallpaper.prototype, "_stockImageId", {
                get: $util.oneOfGetter($oneOfFields = ["stockImageId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ChatStockImageWallpaper.prototype, "_dimLevel", {
                get: $util.oneOfGetter($oneOfFields = ["dimLevel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ChatStockImageWallpaper instance using the specified properties.
             * @function create
             * @memberof proto.Message.ChatStockImageWallpaper
             * @static
             * @param {proto.Message.IChatStockImageWallpaper=} [properties] Properties to set
             * @returns {proto.Message.ChatStockImageWallpaper} ChatStockImageWallpaper instance
             */
            ChatStockImageWallpaper.create = function create(properties) {
                return new ChatStockImageWallpaper(properties);
            };

            /**
             * Encodes the specified ChatStockImageWallpaper message. Does not implicitly {@link proto.Message.ChatStockImageWallpaper.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.ChatStockImageWallpaper
             * @static
             * @param {proto.Message.IChatStockImageWallpaper} message ChatStockImageWallpaper message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ChatStockImageWallpaper.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.stockImageId != null && Object.hasOwnProperty.call(message, "stockImageId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.stockImageId);
                if (message.dimLevel != null && Object.hasOwnProperty.call(message, "dimLevel"))
                    writer.uint32(/* id 2, wireType 5 =*/21).float(message.dimLevel);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ChatStockImageWallpaper message, length delimited. Does not implicitly {@link proto.Message.ChatStockImageWallpaper.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.ChatStockImageWallpaper
             * @static
             * @param {proto.Message.IChatStockImageWallpaper} message ChatStockImageWallpaper message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ChatStockImageWallpaper.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ChatStockImageWallpaper message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.ChatStockImageWallpaper
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.ChatStockImageWallpaper} ChatStockImageWallpaper
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ChatStockImageWallpaper.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.stockImageId = reader.stringVerify();
                            message._stockImageId = "stockImageId";
                            continue;
                        }
                    case 2: {
                            if (u !== 5)
                                break;
                            message.dimLevel = reader.float();
                            message._dimLevel = "dimLevel";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ChatStockImageWallpaper message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.ChatStockImageWallpaper
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.ChatStockImageWallpaper} ChatStockImageWallpaper
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ChatStockImageWallpaper.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ChatStockImageWallpaper message.
             * @function verify
             * @memberof proto.Message.ChatStockImageWallpaper
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ChatStockImageWallpaper.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.stockImageId != null && Object.hasOwnProperty.call(message, "stockImageId")) {
                    properties._stockImageId = 1;
                    if (!$util.isString(message.stockImageId))
                        return "stockImageId: string expected";
                }
                if (message.dimLevel != null && Object.hasOwnProperty.call(message, "dimLevel")) {
                    properties._dimLevel = 1;
                    if (typeof message.dimLevel !== "number")
                        return "dimLevel: number expected";
                }
                return null;
            };

            /**
             * Creates a ChatStockImageWallpaper message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.ChatStockImageWallpaper
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.ChatStockImageWallpaper} ChatStockImageWallpaper
             */
            ChatStockImageWallpaper.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.ChatStockImageWallpaper: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.stockImageId != null)
                    message.stockImageId = String(object.stockImageId);
                if (object.dimLevel != null)
                    message.dimLevel = Number(object.dimLevel);
                return message;
            };

            /**
             * Creates a plain object from a ChatStockImageWallpaper message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.ChatStockImageWallpaper
             * @static
             * @param {proto.Message.ChatStockImageWallpaper} message ChatStockImageWallpaper
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ChatStockImageWallpaper.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.stockImageId != null && Object.hasOwnProperty.call(message, "stockImageId"))
                    object.stockImageId = message.stockImageId;
                if (message.dimLevel != null && Object.hasOwnProperty.call(message, "dimLevel"))
                    object.dimLevel = options.json && !isFinite(message.dimLevel) ? String(message.dimLevel) : message.dimLevel;
                return object;
            };

            /**
             * Converts this ChatStockImageWallpaper to JSON.
             * @function toJSON
             * @memberof proto.Message.ChatStockImageWallpaper
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ChatStockImageWallpaper.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ChatStockImageWallpaper
             * @function getTypeUrl
             * @memberof proto.Message.ChatStockImageWallpaper
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ChatStockImageWallpaper.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.ChatStockImageWallpaper";
            };

            return ChatStockImageWallpaper;
        })();

        Message.ChatThemeSetting = (function() {

            /**
             * Properties of a ChatThemeSetting.
             * @memberof proto.Message
             * @interface IChatThemeSetting
             * @property {number|Long|null} [settingTimestampMs] ChatThemeSetting settingTimestampMs
             * @property {boolean|null} [clearTheme] ChatThemeSetting clearTheme
             * @property {string|null} [colorSchemeId] ChatThemeSetting colorSchemeId
             * @property {proto.Message.IChatDefaultWallpaper|null} [defaultWallpaper] ChatThemeSetting defaultWallpaper
             * @property {proto.Message.IChatSolidColorWallpaper|null} [solidColor] ChatThemeSetting solidColor
             * @property {proto.Message.IChatStockImageWallpaper|null} [stockImage] ChatThemeSetting stockImage
             * @property {proto.Message.IChatCustomImageWallpaper|null} [customImage] ChatThemeSetting customImage
             */

            /**
             * Constructs a new ChatThemeSetting.
             * @memberof proto.Message
             * @classdesc Represents a ChatThemeSetting.
             * @implements IChatThemeSetting
             * @constructor
             * @param {proto.Message.IChatThemeSetting=} [properties] Properties to set
             */
            function ChatThemeSetting(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ChatThemeSetting settingTimestampMs.
             * @member {number|Long|null|undefined} settingTimestampMs
             * @memberof proto.Message.ChatThemeSetting
             * @instance
             */
            ChatThemeSetting.prototype.settingTimestampMs = null;

            /**
             * ChatThemeSetting clearTheme.
             * @member {boolean|null|undefined} clearTheme
             * @memberof proto.Message.ChatThemeSetting
             * @instance
             */
            ChatThemeSetting.prototype.clearTheme = null;

            /**
             * ChatThemeSetting colorSchemeId.
             * @member {string|null|undefined} colorSchemeId
             * @memberof proto.Message.ChatThemeSetting
             * @instance
             */
            ChatThemeSetting.prototype.colorSchemeId = null;

            /**
             * ChatThemeSetting defaultWallpaper.
             * @member {proto.Message.IChatDefaultWallpaper|null|undefined} defaultWallpaper
             * @memberof proto.Message.ChatThemeSetting
             * @instance
             */
            ChatThemeSetting.prototype.defaultWallpaper = null;

            /**
             * ChatThemeSetting solidColor.
             * @member {proto.Message.IChatSolidColorWallpaper|null|undefined} solidColor
             * @memberof proto.Message.ChatThemeSetting
             * @instance
             */
            ChatThemeSetting.prototype.solidColor = null;

            /**
             * ChatThemeSetting stockImage.
             * @member {proto.Message.IChatStockImageWallpaper|null|undefined} stockImage
             * @memberof proto.Message.ChatThemeSetting
             * @instance
             */
            ChatThemeSetting.prototype.stockImage = null;

            /**
             * ChatThemeSetting customImage.
             * @member {proto.Message.IChatCustomImageWallpaper|null|undefined} customImage
             * @memberof proto.Message.ChatThemeSetting
             * @instance
             */
            ChatThemeSetting.prototype.customImage = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ChatThemeSetting.prototype, "_settingTimestampMs", {
                get: $util.oneOfGetter($oneOfFields = ["settingTimestampMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ChatThemeSetting.prototype, "_clearTheme", {
                get: $util.oneOfGetter($oneOfFields = ["clearTheme"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ChatThemeSetting.prototype, "_colorSchemeId", {
                get: $util.oneOfGetter($oneOfFields = ["colorSchemeId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * ChatThemeSetting wallpaper.
             * @member {"defaultWallpaper"|"solidColor"|"stockImage"|"customImage"|undefined} wallpaper
             * @memberof proto.Message.ChatThemeSetting
             * @instance
             */
            Object.defineProperty(ChatThemeSetting.prototype, "wallpaper", {
                get: $util.oneOfGetter($oneOfFields = ["defaultWallpaper", "solidColor", "stockImage", "customImage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ChatThemeSetting instance using the specified properties.
             * @function create
             * @memberof proto.Message.ChatThemeSetting
             * @static
             * @param {proto.Message.IChatThemeSetting=} [properties] Properties to set
             * @returns {proto.Message.ChatThemeSetting} ChatThemeSetting instance
             */
            ChatThemeSetting.create = function create(properties) {
                return new ChatThemeSetting(properties);
            };

            /**
             * Encodes the specified ChatThemeSetting message. Does not implicitly {@link proto.Message.ChatThemeSetting.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.ChatThemeSetting
             * @static
             * @param {proto.Message.IChatThemeSetting} message ChatThemeSetting message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ChatThemeSetting.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.settingTimestampMs != null && Object.hasOwnProperty.call(message, "settingTimestampMs"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int64(message.settingTimestampMs);
                if (message.clearTheme != null && Object.hasOwnProperty.call(message, "clearTheme"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.clearTheme);
                if (message.colorSchemeId != null && Object.hasOwnProperty.call(message, "colorSchemeId"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.colorSchemeId);
                if (message.defaultWallpaper != null && Object.hasOwnProperty.call(message, "defaultWallpaper"))
                    $root.proto.Message.ChatDefaultWallpaper.encode(message.defaultWallpaper, writer.uint32(/* id 10, wireType 2 =*/82).fork(), q + 1).ldelim();
                if (message.solidColor != null && Object.hasOwnProperty.call(message, "solidColor"))
                    $root.proto.Message.ChatSolidColorWallpaper.encode(message.solidColor, writer.uint32(/* id 11, wireType 2 =*/90).fork(), q + 1).ldelim();
                if (message.stockImage != null && Object.hasOwnProperty.call(message, "stockImage"))
                    $root.proto.Message.ChatStockImageWallpaper.encode(message.stockImage, writer.uint32(/* id 12, wireType 2 =*/98).fork(), q + 1).ldelim();
                if (message.customImage != null && Object.hasOwnProperty.call(message, "customImage"))
                    $root.proto.Message.ChatCustomImageWallpaper.encode(message.customImage, writer.uint32(/* id 13, wireType 2 =*/106).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ChatThemeSetting message, length delimited. Does not implicitly {@link proto.Message.ChatThemeSetting.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.ChatThemeSetting
             * @static
             * @param {proto.Message.IChatThemeSetting} message ChatThemeSetting message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ChatThemeSetting.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ChatThemeSetting message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.ChatThemeSetting
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.ChatThemeSetting} ChatThemeSetting
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ChatThemeSetting.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.settingTimestampMs = reader.int64();
                            message._settingTimestampMs = "settingTimestampMs";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.clearTheme = reader.bool();
                            message._clearTheme = "clearTheme";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.colorSchemeId = reader.stringVerify();
                            message._colorSchemeId = "colorSchemeId";
                            continue;
                        }
                    case 10: {
                            if (u !== 2)
                                break;
                            message.defaultWallpaper = $root.proto.Message.ChatDefaultWallpaper.decode(reader, reader.uint32(), undefined, q + 1, message.defaultWallpaper);
                            message.wallpaper = "defaultWallpaper";
                            continue;
                        }
                    case 11: {
                            if (u !== 2)
                                break;
                            message.solidColor = $root.proto.Message.ChatSolidColorWallpaper.decode(reader, reader.uint32(), undefined, q + 1, message.solidColor);
                            message.wallpaper = "solidColor";
                            continue;
                        }
                    case 12: {
                            if (u !== 2)
                                break;
                            message.stockImage = $root.proto.Message.ChatStockImageWallpaper.decode(reader, reader.uint32(), undefined, q + 1, message.stockImage);
                            message.wallpaper = "stockImage";
                            continue;
                        }
                    case 13: {
                            if (u !== 2)
                                break;
                            message.customImage = $root.proto.Message.ChatCustomImageWallpaper.decode(reader, reader.uint32(), undefined, q + 1, message.customImage);
                            message.wallpaper = "customImage";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ChatThemeSetting message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.ChatThemeSetting
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.ChatThemeSetting} ChatThemeSetting
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ChatThemeSetting.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ChatThemeSetting message.
             * @function verify
             * @memberof proto.Message.ChatThemeSetting
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ChatThemeSetting.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.settingTimestampMs != null && Object.hasOwnProperty.call(message, "settingTimestampMs")) {
                    properties._settingTimestampMs = 1;
                    if (!$util.isInteger(message.settingTimestampMs) && !(message.settingTimestampMs && $util.isInteger(message.settingTimestampMs.low) && $util.isInteger(message.settingTimestampMs.high)))
                        return "settingTimestampMs: integer|Long expected";
                }
                if (message.clearTheme != null && Object.hasOwnProperty.call(message, "clearTheme")) {
                    properties._clearTheme = 1;
                    if (typeof message.clearTheme !== "boolean")
                        return "clearTheme: boolean expected";
                }
                if (message.colorSchemeId != null && Object.hasOwnProperty.call(message, "colorSchemeId")) {
                    properties._colorSchemeId = 1;
                    if (!$util.isString(message.colorSchemeId))
                        return "colorSchemeId: string expected";
                }
                if (message.defaultWallpaper != null && Object.hasOwnProperty.call(message, "defaultWallpaper")) {
                    properties.wallpaper = 1;
                    {
                        var error = $root.proto.Message.ChatDefaultWallpaper.verify(message.defaultWallpaper, q + 1);
                        if (error)
                            return "defaultWallpaper." + error;
                    }
                }
                if (message.solidColor != null && Object.hasOwnProperty.call(message, "solidColor")) {
                    if (properties.wallpaper === 1)
                        return "wallpaper: multiple values";
                    properties.wallpaper = 1;
                    {
                        var error = $root.proto.Message.ChatSolidColorWallpaper.verify(message.solidColor, q + 1);
                        if (error)
                            return "solidColor." + error;
                    }
                }
                if (message.stockImage != null && Object.hasOwnProperty.call(message, "stockImage")) {
                    if (properties.wallpaper === 1)
                        return "wallpaper: multiple values";
                    properties.wallpaper = 1;
                    {
                        var error = $root.proto.Message.ChatStockImageWallpaper.verify(message.stockImage, q + 1);
                        if (error)
                            return "stockImage." + error;
                    }
                }
                if (message.customImage != null && Object.hasOwnProperty.call(message, "customImage")) {
                    if (properties.wallpaper === 1)
                        return "wallpaper: multiple values";
                    properties.wallpaper = 1;
                    {
                        var error = $root.proto.Message.ChatCustomImageWallpaper.verify(message.customImage, q + 1);
                        if (error)
                            return "customImage." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a ChatThemeSetting message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.ChatThemeSetting
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.ChatThemeSetting} ChatThemeSetting
             */
            ChatThemeSetting.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.ChatThemeSetting: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.settingTimestampMs != null)
                    if ($util.Long)
                        message.settingTimestampMs = $util.Long.fromValue(object.settingTimestampMs, false);
                    else if (typeof object.settingTimestampMs === "string")
                        message.settingTimestampMs = parseInt(object.settingTimestampMs, 10);
                    else if (typeof object.settingTimestampMs === "number")
                        message.settingTimestampMs = object.settingTimestampMs;
                    else if (typeof object.settingTimestampMs === "object")
                        message.settingTimestampMs = new $util.LongBits(object.settingTimestampMs.low >>> 0, object.settingTimestampMs.high >>> 0).toNumber();
                if (object.clearTheme != null)
                    message.clearTheme = Boolean(object.clearTheme);
                if (object.colorSchemeId != null)
                    message.colorSchemeId = String(object.colorSchemeId);
                if (object.defaultWallpaper != null) {
                    if (!$util.isObject(object.defaultWallpaper))
                        throw TypeError(".proto.Message.ChatThemeSetting.defaultWallpaper: object expected");
                    message.defaultWallpaper = $root.proto.Message.ChatDefaultWallpaper.fromObject(object.defaultWallpaper, q + 1);
                }
                if (object.solidColor != null) {
                    if (!$util.isObject(object.solidColor))
                        throw TypeError(".proto.Message.ChatThemeSetting.solidColor: object expected");
                    message.solidColor = $root.proto.Message.ChatSolidColorWallpaper.fromObject(object.solidColor, q + 1);
                }
                if (object.stockImage != null) {
                    if (!$util.isObject(object.stockImage))
                        throw TypeError(".proto.Message.ChatThemeSetting.stockImage: object expected");
                    message.stockImage = $root.proto.Message.ChatStockImageWallpaper.fromObject(object.stockImage, q + 1);
                }
                if (object.customImage != null) {
                    if (!$util.isObject(object.customImage))
                        throw TypeError(".proto.Message.ChatThemeSetting.customImage: object expected");
                    message.customImage = $root.proto.Message.ChatCustomImageWallpaper.fromObject(object.customImage, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a ChatThemeSetting message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.ChatThemeSetting
             * @static
             * @param {proto.Message.ChatThemeSetting} message ChatThemeSetting
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ChatThemeSetting.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.settingTimestampMs != null && Object.hasOwnProperty.call(message, "settingTimestampMs"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.settingTimestampMs = typeof message.settingTimestampMs === "number" ? BigInt(message.settingTimestampMs) : $util.Long.fromBits(message.settingTimestampMs.low >>> 0, message.settingTimestampMs.high >>> 0, false).toBigInt();
                    else if (typeof message.settingTimestampMs === "number")
                        object.settingTimestampMs = options.longs === String ? String(message.settingTimestampMs) : message.settingTimestampMs;
                    else
                        object.settingTimestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.settingTimestampMs) : options.longs === Number ? new $util.LongBits(message.settingTimestampMs.low >>> 0, message.settingTimestampMs.high >>> 0).toNumber() : message.settingTimestampMs;
                if (message.clearTheme != null && Object.hasOwnProperty.call(message, "clearTheme"))
                    object.clearTheme = message.clearTheme;
                if (message.colorSchemeId != null && Object.hasOwnProperty.call(message, "colorSchemeId"))
                    object.colorSchemeId = message.colorSchemeId;
                if (message.defaultWallpaper != null && Object.hasOwnProperty.call(message, "defaultWallpaper")) {
                    object.defaultWallpaper = $root.proto.Message.ChatDefaultWallpaper.toObject(message.defaultWallpaper, options, q + 1);
                    if (options.oneofs)
                        object.wallpaper = "defaultWallpaper";
                }
                if (message.solidColor != null && Object.hasOwnProperty.call(message, "solidColor")) {
                    object.solidColor = $root.proto.Message.ChatSolidColorWallpaper.toObject(message.solidColor, options, q + 1);
                    if (options.oneofs)
                        object.wallpaper = "solidColor";
                }
                if (message.stockImage != null && Object.hasOwnProperty.call(message, "stockImage")) {
                    object.stockImage = $root.proto.Message.ChatStockImageWallpaper.toObject(message.stockImage, options, q + 1);
                    if (options.oneofs)
                        object.wallpaper = "stockImage";
                }
                if (message.customImage != null && Object.hasOwnProperty.call(message, "customImage")) {
                    object.customImage = $root.proto.Message.ChatCustomImageWallpaper.toObject(message.customImage, options, q + 1);
                    if (options.oneofs)
                        object.wallpaper = "customImage";
                }
                return object;
            };

            /**
             * Converts this ChatThemeSetting to JSON.
             * @function toJSON
             * @memberof proto.Message.ChatThemeSetting
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ChatThemeSetting.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ChatThemeSetting
             * @function getTypeUrl
             * @memberof proto.Message.ChatThemeSetting
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ChatThemeSetting.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.ChatThemeSetting";
            };

            return ChatThemeSetting;
        })();

        Message.CloudAPIThreadControlNotification = (function() {

            /**
             * Properties of a CloudAPIThreadControlNotification.
             * @memberof proto.Message
             * @interface ICloudAPIThreadControlNotification
             * @property {proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControl|null} [status] CloudAPIThreadControlNotification status
             * @property {number|Long|null} [senderNotificationTimestampMs] CloudAPIThreadControlNotification senderNotificationTimestampMs
             * @property {string|null} [consumerLid] CloudAPIThreadControlNotification consumerLid
             * @property {string|null} [consumerPhoneNumber] CloudAPIThreadControlNotification consumerPhoneNumber
             * @property {proto.Message.CloudAPIThreadControlNotification.ICloudAPIThreadControlNotificationContent|null} [notificationContent] CloudAPIThreadControlNotification notificationContent
             * @property {boolean|null} [shouldSuppressNotification] CloudAPIThreadControlNotification shouldSuppressNotification
             */

            /**
             * Constructs a new CloudAPIThreadControlNotification.
             * @memberof proto.Message
             * @classdesc Represents a CloudAPIThreadControlNotification.
             * @implements ICloudAPIThreadControlNotification
             * @constructor
             * @param {proto.Message.ICloudAPIThreadControlNotification=} [properties] Properties to set
             */
            function CloudAPIThreadControlNotification(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * CloudAPIThreadControlNotification status.
             * @member {proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControl|null|undefined} status
             * @memberof proto.Message.CloudAPIThreadControlNotification
             * @instance
             */
            CloudAPIThreadControlNotification.prototype.status = null;

            /**
             * CloudAPIThreadControlNotification senderNotificationTimestampMs.
             * @member {number|Long|null|undefined} senderNotificationTimestampMs
             * @memberof proto.Message.CloudAPIThreadControlNotification
             * @instance
             */
            CloudAPIThreadControlNotification.prototype.senderNotificationTimestampMs = null;

            /**
             * CloudAPIThreadControlNotification consumerLid.
             * @member {string|null|undefined} consumerLid
             * @memberof proto.Message.CloudAPIThreadControlNotification
             * @instance
             */
            CloudAPIThreadControlNotification.prototype.consumerLid = null;

            /**
             * CloudAPIThreadControlNotification consumerPhoneNumber.
             * @member {string|null|undefined} consumerPhoneNumber
             * @memberof proto.Message.CloudAPIThreadControlNotification
             * @instance
             */
            CloudAPIThreadControlNotification.prototype.consumerPhoneNumber = null;

            /**
             * CloudAPIThreadControlNotification notificationContent.
             * @member {proto.Message.CloudAPIThreadControlNotification.ICloudAPIThreadControlNotificationContent|null|undefined} notificationContent
             * @memberof proto.Message.CloudAPIThreadControlNotification
             * @instance
             */
            CloudAPIThreadControlNotification.prototype.notificationContent = null;

            /**
             * CloudAPIThreadControlNotification shouldSuppressNotification.
             * @member {boolean|null|undefined} shouldSuppressNotification
             * @memberof proto.Message.CloudAPIThreadControlNotification
             * @instance
             */
            CloudAPIThreadControlNotification.prototype.shouldSuppressNotification = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CloudAPIThreadControlNotification.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CloudAPIThreadControlNotification.prototype, "_senderNotificationTimestampMs", {
                get: $util.oneOfGetter($oneOfFields = ["senderNotificationTimestampMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CloudAPIThreadControlNotification.prototype, "_consumerLid", {
                get: $util.oneOfGetter($oneOfFields = ["consumerLid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CloudAPIThreadControlNotification.prototype, "_consumerPhoneNumber", {
                get: $util.oneOfGetter($oneOfFields = ["consumerPhoneNumber"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CloudAPIThreadControlNotification.prototype, "_notificationContent", {
                get: $util.oneOfGetter($oneOfFields = ["notificationContent"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CloudAPIThreadControlNotification.prototype, "_shouldSuppressNotification", {
                get: $util.oneOfGetter($oneOfFields = ["shouldSuppressNotification"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new CloudAPIThreadControlNotification instance using the specified properties.
             * @function create
             * @memberof proto.Message.CloudAPIThreadControlNotification
             * @static
             * @param {proto.Message.ICloudAPIThreadControlNotification=} [properties] Properties to set
             * @returns {proto.Message.CloudAPIThreadControlNotification} CloudAPIThreadControlNotification instance
             */
            CloudAPIThreadControlNotification.create = function create(properties) {
                return new CloudAPIThreadControlNotification(properties);
            };

            /**
             * Encodes the specified CloudAPIThreadControlNotification message. Does not implicitly {@link proto.Message.CloudAPIThreadControlNotification.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.CloudAPIThreadControlNotification
             * @static
             * @param {proto.Message.ICloudAPIThreadControlNotification} message CloudAPIThreadControlNotification message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CloudAPIThreadControlNotification.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.status);
                if (message.senderNotificationTimestampMs != null && Object.hasOwnProperty.call(message, "senderNotificationTimestampMs"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.senderNotificationTimestampMs);
                if (message.consumerLid != null && Object.hasOwnProperty.call(message, "consumerLid"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.consumerLid);
                if (message.consumerPhoneNumber != null && Object.hasOwnProperty.call(message, "consumerPhoneNumber"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.consumerPhoneNumber);
                if (message.notificationContent != null && Object.hasOwnProperty.call(message, "notificationContent"))
                    $root.proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent.encode(message.notificationContent, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                if (message.shouldSuppressNotification != null && Object.hasOwnProperty.call(message, "shouldSuppressNotification"))
                    writer.uint32(/* id 6, wireType 0 =*/48).bool(message.shouldSuppressNotification);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CloudAPIThreadControlNotification message, length delimited. Does not implicitly {@link proto.Message.CloudAPIThreadControlNotification.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.CloudAPIThreadControlNotification
             * @static
             * @param {proto.Message.ICloudAPIThreadControlNotification} message CloudAPIThreadControlNotification message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CloudAPIThreadControlNotification.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a CloudAPIThreadControlNotification message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.CloudAPIThreadControlNotification
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.CloudAPIThreadControlNotification} CloudAPIThreadControlNotification
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CloudAPIThreadControlNotification.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.status = reader.int32();
                            message._status = "status";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.senderNotificationTimestampMs = reader.int64();
                            message._senderNotificationTimestampMs = "senderNotificationTimestampMs";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.consumerLid = reader.stringVerify();
                            message._consumerLid = "consumerLid";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.consumerPhoneNumber = reader.stringVerify();
                            message._consumerPhoneNumber = "consumerPhoneNumber";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.notificationContent = $root.proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent.decode(reader, reader.uint32(), undefined, q + 1, message.notificationContent);
                            message._notificationContent = "notificationContent";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.shouldSuppressNotification = reader.bool();
                            message._shouldSuppressNotification = "shouldSuppressNotification";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a CloudAPIThreadControlNotification message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.CloudAPIThreadControlNotification
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.CloudAPIThreadControlNotification} CloudAPIThreadControlNotification
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CloudAPIThreadControlNotification.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CloudAPIThreadControlNotification message.
             * @function verify
             * @memberof proto.Message.CloudAPIThreadControlNotification
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CloudAPIThreadControlNotification.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.status != null && Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (typeof message.status !== "number" || (message.status | 0) !== message.status)
                        return "status: enum value expected";
                }
                if (message.senderNotificationTimestampMs != null && Object.hasOwnProperty.call(message, "senderNotificationTimestampMs")) {
                    properties._senderNotificationTimestampMs = 1;
                    if (!$util.isInteger(message.senderNotificationTimestampMs) && !(message.senderNotificationTimestampMs && $util.isInteger(message.senderNotificationTimestampMs.low) && $util.isInteger(message.senderNotificationTimestampMs.high)))
                        return "senderNotificationTimestampMs: integer|Long expected";
                }
                if (message.consumerLid != null && Object.hasOwnProperty.call(message, "consumerLid")) {
                    properties._consumerLid = 1;
                    if (!$util.isString(message.consumerLid))
                        return "consumerLid: string expected";
                }
                if (message.consumerPhoneNumber != null && Object.hasOwnProperty.call(message, "consumerPhoneNumber")) {
                    properties._consumerPhoneNumber = 1;
                    if (!$util.isString(message.consumerPhoneNumber))
                        return "consumerPhoneNumber: string expected";
                }
                if (message.notificationContent != null && Object.hasOwnProperty.call(message, "notificationContent")) {
                    properties._notificationContent = 1;
                    {
                        var error = $root.proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent.verify(message.notificationContent, q + 1);
                        if (error)
                            return "notificationContent." + error;
                    }
                }
                if (message.shouldSuppressNotification != null && Object.hasOwnProperty.call(message, "shouldSuppressNotification")) {
                    properties._shouldSuppressNotification = 1;
                    if (typeof message.shouldSuppressNotification !== "boolean")
                        return "shouldSuppressNotification: boolean expected";
                }
                return null;
            };

            /**
             * Creates a CloudAPIThreadControlNotification message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.CloudAPIThreadControlNotification
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.CloudAPIThreadControlNotification} CloudAPIThreadControlNotification
             */
            CloudAPIThreadControlNotification.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.CloudAPIThreadControlNotification: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.status) {
                case "UNKNOWN":
                case 0:
                    message.status = 0;
                    break;
                case "CONTROL_PASSED":
                case 1:
                    message.status = 1;
                    break;
                case "CONTROL_TAKEN":
                case 2:
                    message.status = 2;
                    break;
                case "INFO":
                case 3:
                    message.status = 3;
                    break;
                default:
                    if (typeof object.status === "number" && (object.status | 0) === object.status)
                        message.status = object.status;
                }
                if (object.senderNotificationTimestampMs != null)
                    if ($util.Long)
                        message.senderNotificationTimestampMs = $util.Long.fromValue(object.senderNotificationTimestampMs, false);
                    else if (typeof object.senderNotificationTimestampMs === "string")
                        message.senderNotificationTimestampMs = parseInt(object.senderNotificationTimestampMs, 10);
                    else if (typeof object.senderNotificationTimestampMs === "number")
                        message.senderNotificationTimestampMs = object.senderNotificationTimestampMs;
                    else if (typeof object.senderNotificationTimestampMs === "object")
                        message.senderNotificationTimestampMs = new $util.LongBits(object.senderNotificationTimestampMs.low >>> 0, object.senderNotificationTimestampMs.high >>> 0).toNumber();
                if (object.consumerLid != null)
                    message.consumerLid = String(object.consumerLid);
                if (object.consumerPhoneNumber != null)
                    message.consumerPhoneNumber = String(object.consumerPhoneNumber);
                if (object.notificationContent != null) {
                    if (!$util.isObject(object.notificationContent))
                        throw TypeError(".proto.Message.CloudAPIThreadControlNotification.notificationContent: object expected");
                    message.notificationContent = $root.proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent.fromObject(object.notificationContent, q + 1);
                }
                if (object.shouldSuppressNotification != null)
                    message.shouldSuppressNotification = Boolean(object.shouldSuppressNotification);
                return message;
            };

            /**
             * Creates a plain object from a CloudAPIThreadControlNotification message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.CloudAPIThreadControlNotification
             * @static
             * @param {proto.Message.CloudAPIThreadControlNotification} message CloudAPIThreadControlNotification
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CloudAPIThreadControlNotification.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                    object.status = options.enums === String ? $root.proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControl[message.status] === undefined ? message.status : $root.proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControl[message.status] : message.status;
                if (message.senderNotificationTimestampMs != null && Object.hasOwnProperty.call(message, "senderNotificationTimestampMs"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.senderNotificationTimestampMs = typeof message.senderNotificationTimestampMs === "number" ? BigInt(message.senderNotificationTimestampMs) : $util.Long.fromBits(message.senderNotificationTimestampMs.low >>> 0, message.senderNotificationTimestampMs.high >>> 0, false).toBigInt();
                    else if (typeof message.senderNotificationTimestampMs === "number")
                        object.senderNotificationTimestampMs = options.longs === String ? String(message.senderNotificationTimestampMs) : message.senderNotificationTimestampMs;
                    else
                        object.senderNotificationTimestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.senderNotificationTimestampMs) : options.longs === Number ? new $util.LongBits(message.senderNotificationTimestampMs.low >>> 0, message.senderNotificationTimestampMs.high >>> 0).toNumber() : message.senderNotificationTimestampMs;
                if (message.consumerLid != null && Object.hasOwnProperty.call(message, "consumerLid"))
                    object.consumerLid = message.consumerLid;
                if (message.consumerPhoneNumber != null && Object.hasOwnProperty.call(message, "consumerPhoneNumber"))
                    object.consumerPhoneNumber = message.consumerPhoneNumber;
                if (message.notificationContent != null && Object.hasOwnProperty.call(message, "notificationContent"))
                    object.notificationContent = $root.proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent.toObject(message.notificationContent, options, q + 1);
                if (message.shouldSuppressNotification != null && Object.hasOwnProperty.call(message, "shouldSuppressNotification"))
                    object.shouldSuppressNotification = message.shouldSuppressNotification;
                return object;
            };

            /**
             * Converts this CloudAPIThreadControlNotification to JSON.
             * @function toJSON
             * @memberof proto.Message.CloudAPIThreadControlNotification
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CloudAPIThreadControlNotification.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for CloudAPIThreadControlNotification
             * @function getTypeUrl
             * @memberof proto.Message.CloudAPIThreadControlNotification
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            CloudAPIThreadControlNotification.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.CloudAPIThreadControlNotification";
            };

            /**
             * CloudAPIThreadControl enum.
             * @name proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControl
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} CONTROL_PASSED=1 CONTROL_PASSED value
             * @property {number} CONTROL_TAKEN=2 CONTROL_TAKEN value
             * @property {number} INFO=3 INFO value
             */
            CloudAPIThreadControlNotification.CloudAPIThreadControl = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "CONTROL_PASSED"] = 1;
                values[valuesById[2] = "CONTROL_TAKEN"] = 2;
                values[valuesById[3] = "INFO"] = 3;
                return values;
            })();

            CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent = (function() {

                /**
                 * Properties of a CloudAPIThreadControlNotificationContent.
                 * @memberof proto.Message.CloudAPIThreadControlNotification
                 * @interface ICloudAPIThreadControlNotificationContent
                 * @property {string|null} [handoffNotificationText] CloudAPIThreadControlNotificationContent handoffNotificationText
                 * @property {string|null} [extraJson] CloudAPIThreadControlNotificationContent extraJson
                 */

                /**
                 * Constructs a new CloudAPIThreadControlNotificationContent.
                 * @memberof proto.Message.CloudAPIThreadControlNotification
                 * @classdesc Represents a CloudAPIThreadControlNotificationContent.
                 * @implements ICloudAPIThreadControlNotificationContent
                 * @constructor
                 * @param {proto.Message.CloudAPIThreadControlNotification.ICloudAPIThreadControlNotificationContent=} [properties] Properties to set
                 */
                function CloudAPIThreadControlNotificationContent(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * CloudAPIThreadControlNotificationContent handoffNotificationText.
                 * @member {string|null|undefined} handoffNotificationText
                 * @memberof proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent
                 * @instance
                 */
                CloudAPIThreadControlNotificationContent.prototype.handoffNotificationText = null;

                /**
                 * CloudAPIThreadControlNotificationContent extraJson.
                 * @member {string|null|undefined} extraJson
                 * @memberof proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent
                 * @instance
                 */
                CloudAPIThreadControlNotificationContent.prototype.extraJson = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CloudAPIThreadControlNotificationContent.prototype, "_handoffNotificationText", {
                    get: $util.oneOfGetter($oneOfFields = ["handoffNotificationText"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CloudAPIThreadControlNotificationContent.prototype, "_extraJson", {
                    get: $util.oneOfGetter($oneOfFields = ["extraJson"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new CloudAPIThreadControlNotificationContent instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent
                 * @static
                 * @param {proto.Message.CloudAPIThreadControlNotification.ICloudAPIThreadControlNotificationContent=} [properties] Properties to set
                 * @returns {proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent} CloudAPIThreadControlNotificationContent instance
                 */
                CloudAPIThreadControlNotificationContent.create = function create(properties) {
                    return new CloudAPIThreadControlNotificationContent(properties);
                };

                /**
                 * Encodes the specified CloudAPIThreadControlNotificationContent message. Does not implicitly {@link proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent
                 * @static
                 * @param {proto.Message.CloudAPIThreadControlNotification.ICloudAPIThreadControlNotificationContent} message CloudAPIThreadControlNotificationContent message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                CloudAPIThreadControlNotificationContent.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.handoffNotificationText != null && Object.hasOwnProperty.call(message, "handoffNotificationText"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.handoffNotificationText);
                    if (message.extraJson != null && Object.hasOwnProperty.call(message, "extraJson"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.extraJson);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified CloudAPIThreadControlNotificationContent message, length delimited. Does not implicitly {@link proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent
                 * @static
                 * @param {proto.Message.CloudAPIThreadControlNotification.ICloudAPIThreadControlNotificationContent} message CloudAPIThreadControlNotificationContent message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                CloudAPIThreadControlNotificationContent.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a CloudAPIThreadControlNotificationContent message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent} CloudAPIThreadControlNotificationContent
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                CloudAPIThreadControlNotificationContent.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.handoffNotificationText = reader.stringVerify();
                                message._handoffNotificationText = "handoffNotificationText";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.extraJson = reader.stringVerify();
                                message._extraJson = "extraJson";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a CloudAPIThreadControlNotificationContent message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent} CloudAPIThreadControlNotificationContent
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                CloudAPIThreadControlNotificationContent.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a CloudAPIThreadControlNotificationContent message.
                 * @function verify
                 * @memberof proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                CloudAPIThreadControlNotificationContent.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.handoffNotificationText != null && Object.hasOwnProperty.call(message, "handoffNotificationText")) {
                        properties._handoffNotificationText = 1;
                        if (!$util.isString(message.handoffNotificationText))
                            return "handoffNotificationText: string expected";
                    }
                    if (message.extraJson != null && Object.hasOwnProperty.call(message, "extraJson")) {
                        properties._extraJson = 1;
                        if (!$util.isString(message.extraJson))
                            return "extraJson: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a CloudAPIThreadControlNotificationContent message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent} CloudAPIThreadControlNotificationContent
                 */
                CloudAPIThreadControlNotificationContent.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.handoffNotificationText != null)
                        message.handoffNotificationText = String(object.handoffNotificationText);
                    if (object.extraJson != null)
                        message.extraJson = String(object.extraJson);
                    return message;
                };

                /**
                 * Creates a plain object from a CloudAPIThreadControlNotificationContent message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent
                 * @static
                 * @param {proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent} message CloudAPIThreadControlNotificationContent
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                CloudAPIThreadControlNotificationContent.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.handoffNotificationText != null && Object.hasOwnProperty.call(message, "handoffNotificationText"))
                        object.handoffNotificationText = message.handoffNotificationText;
                    if (message.extraJson != null && Object.hasOwnProperty.call(message, "extraJson"))
                        object.extraJson = message.extraJson;
                    return object;
                };

                /**
                 * Converts this CloudAPIThreadControlNotificationContent to JSON.
                 * @function toJSON
                 * @memberof proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                CloudAPIThreadControlNotificationContent.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for CloudAPIThreadControlNotificationContent
                 * @function getTypeUrl
                 * @memberof proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                CloudAPIThreadControlNotificationContent.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent";
                };

                return CloudAPIThreadControlNotificationContent;
            })();

            return CloudAPIThreadControlNotification;
        })();

        Message.CommentMessage = (function() {

            /**
             * Properties of a CommentMessage.
             * @memberof proto.Message
             * @interface ICommentMessage
             * @property {proto.IMessage|null} [message] CommentMessage message
             * @property {proto.IMessageKey|null} [targetMessageKey] CommentMessage targetMessageKey
             */

            /**
             * Constructs a new CommentMessage.
             * @memberof proto.Message
             * @classdesc Represents a CommentMessage.
             * @implements ICommentMessage
             * @constructor
             * @param {proto.Message.ICommentMessage=} [properties] Properties to set
             */
            function CommentMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * CommentMessage message.
             * @member {proto.IMessage|null|undefined} message
             * @memberof proto.Message.CommentMessage
             * @instance
             */
            CommentMessage.prototype.message = null;

            /**
             * CommentMessage targetMessageKey.
             * @member {proto.IMessageKey|null|undefined} targetMessageKey
             * @memberof proto.Message.CommentMessage
             * @instance
             */
            CommentMessage.prototype.targetMessageKey = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CommentMessage.prototype, "_message", {
                get: $util.oneOfGetter($oneOfFields = ["message"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CommentMessage.prototype, "_targetMessageKey", {
                get: $util.oneOfGetter($oneOfFields = ["targetMessageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new CommentMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.CommentMessage
             * @static
             * @param {proto.Message.ICommentMessage=} [properties] Properties to set
             * @returns {proto.Message.CommentMessage} CommentMessage instance
             */
            CommentMessage.create = function create(properties) {
                return new CommentMessage(properties);
            };

            /**
             * Encodes the specified CommentMessage message. Does not implicitly {@link proto.Message.CommentMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.CommentMessage
             * @static
             * @param {proto.Message.ICommentMessage} message CommentMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CommentMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                    $root.proto.Message.encode(message.message, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.targetMessageKey != null && Object.hasOwnProperty.call(message, "targetMessageKey"))
                    $root.proto.MessageKey.encode(message.targetMessageKey, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CommentMessage message, length delimited. Does not implicitly {@link proto.Message.CommentMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.CommentMessage
             * @static
             * @param {proto.Message.ICommentMessage} message CommentMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CommentMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a CommentMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.CommentMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.CommentMessage} CommentMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CommentMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.message = $root.proto.Message.decode(reader, reader.uint32(), undefined, q + 1, message.message);
                            message._message = "message";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.targetMessageKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.targetMessageKey);
                            message._targetMessageKey = "targetMessageKey";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a CommentMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.CommentMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.CommentMessage} CommentMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CommentMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CommentMessage message.
             * @function verify
             * @memberof proto.Message.CommentMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CommentMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.message != null && Object.hasOwnProperty.call(message, "message")) {
                    properties._message = 1;
                    {
                        var error = $root.proto.Message.verify(message.message, q + 1);
                        if (error)
                            return "message." + error;
                    }
                }
                if (message.targetMessageKey != null && Object.hasOwnProperty.call(message, "targetMessageKey")) {
                    properties._targetMessageKey = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.targetMessageKey, q + 1);
                        if (error)
                            return "targetMessageKey." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a CommentMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.CommentMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.CommentMessage} CommentMessage
             */
            CommentMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.CommentMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.message != null) {
                    if (!$util.isObject(object.message))
                        throw TypeError(".proto.Message.CommentMessage.message: object expected");
                    message.message = $root.proto.Message.fromObject(object.message, q + 1);
                }
                if (object.targetMessageKey != null) {
                    if (!$util.isObject(object.targetMessageKey))
                        throw TypeError(".proto.Message.CommentMessage.targetMessageKey: object expected");
                    message.targetMessageKey = $root.proto.MessageKey.fromObject(object.targetMessageKey, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a CommentMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.CommentMessage
             * @static
             * @param {proto.Message.CommentMessage} message CommentMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CommentMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                    object.message = $root.proto.Message.toObject(message.message, options, q + 1);
                if (message.targetMessageKey != null && Object.hasOwnProperty.call(message, "targetMessageKey"))
                    object.targetMessageKey = $root.proto.MessageKey.toObject(message.targetMessageKey, options, q + 1);
                return object;
            };

            /**
             * Converts this CommentMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.CommentMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CommentMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for CommentMessage
             * @function getTypeUrl
             * @memberof proto.Message.CommentMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            CommentMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.CommentMessage";
            };

            return CommentMessage;
        })();

        Message.ConditionalRevealMessage = (function() {

            /**
             * Properties of a ConditionalRevealMessage.
             * @memberof proto.Message
             * @interface IConditionalRevealMessage
             * @property {Uint8Array|null} [encPayload] ConditionalRevealMessage encPayload
             * @property {Uint8Array|null} [encIv] ConditionalRevealMessage encIv
             * @property {proto.Message.ConditionalRevealMessage.ConditionalRevealMessageType|null} [conditionalRevealMessageType] ConditionalRevealMessage conditionalRevealMessageType
             * @property {string|null} [revealKeyId] ConditionalRevealMessage revealKeyId
             */

            /**
             * Constructs a new ConditionalRevealMessage.
             * @memberof proto.Message
             * @classdesc Represents a ConditionalRevealMessage.
             * @implements IConditionalRevealMessage
             * @constructor
             * @param {proto.Message.IConditionalRevealMessage=} [properties] Properties to set
             */
            function ConditionalRevealMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ConditionalRevealMessage encPayload.
             * @member {Uint8Array|null|undefined} encPayload
             * @memberof proto.Message.ConditionalRevealMessage
             * @instance
             */
            ConditionalRevealMessage.prototype.encPayload = null;

            /**
             * ConditionalRevealMessage encIv.
             * @member {Uint8Array|null|undefined} encIv
             * @memberof proto.Message.ConditionalRevealMessage
             * @instance
             */
            ConditionalRevealMessage.prototype.encIv = null;

            /**
             * ConditionalRevealMessage conditionalRevealMessageType.
             * @member {proto.Message.ConditionalRevealMessage.ConditionalRevealMessageType|null|undefined} conditionalRevealMessageType
             * @memberof proto.Message.ConditionalRevealMessage
             * @instance
             */
            ConditionalRevealMessage.prototype.conditionalRevealMessageType = null;

            /**
             * ConditionalRevealMessage revealKeyId.
             * @member {string|null|undefined} revealKeyId
             * @memberof proto.Message.ConditionalRevealMessage
             * @instance
             */
            ConditionalRevealMessage.prototype.revealKeyId = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ConditionalRevealMessage.prototype, "_encPayload", {
                get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ConditionalRevealMessage.prototype, "_encIv", {
                get: $util.oneOfGetter($oneOfFields = ["encIv"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ConditionalRevealMessage.prototype, "_conditionalRevealMessageType", {
                get: $util.oneOfGetter($oneOfFields = ["conditionalRevealMessageType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ConditionalRevealMessage.prototype, "_revealKeyId", {
                get: $util.oneOfGetter($oneOfFields = ["revealKeyId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ConditionalRevealMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.ConditionalRevealMessage
             * @static
             * @param {proto.Message.IConditionalRevealMessage=} [properties] Properties to set
             * @returns {proto.Message.ConditionalRevealMessage} ConditionalRevealMessage instance
             */
            ConditionalRevealMessage.create = function create(properties) {
                return new ConditionalRevealMessage(properties);
            };

            /**
             * Encodes the specified ConditionalRevealMessage message. Does not implicitly {@link proto.Message.ConditionalRevealMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.ConditionalRevealMessage
             * @static
             * @param {proto.Message.IConditionalRevealMessage} message ConditionalRevealMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ConditionalRevealMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encPayload);
                if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encIv);
                if (message.conditionalRevealMessageType != null && Object.hasOwnProperty.call(message, "conditionalRevealMessageType"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.conditionalRevealMessageType);
                if (message.revealKeyId != null && Object.hasOwnProperty.call(message, "revealKeyId"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.revealKeyId);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ConditionalRevealMessage message, length delimited. Does not implicitly {@link proto.Message.ConditionalRevealMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.ConditionalRevealMessage
             * @static
             * @param {proto.Message.IConditionalRevealMessage} message ConditionalRevealMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ConditionalRevealMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ConditionalRevealMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.ConditionalRevealMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.ConditionalRevealMessage} ConditionalRevealMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ConditionalRevealMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.encPayload = reader.bytes();
                            message._encPayload = "encPayload";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.encIv = reader.bytes();
                            message._encIv = "encIv";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.conditionalRevealMessageType = reader.int32();
                            message._conditionalRevealMessageType = "conditionalRevealMessageType";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.revealKeyId = reader.stringVerify();
                            message._revealKeyId = "revealKeyId";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ConditionalRevealMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.ConditionalRevealMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.ConditionalRevealMessage} ConditionalRevealMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ConditionalRevealMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ConditionalRevealMessage message.
             * @function verify
             * @memberof proto.Message.ConditionalRevealMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ConditionalRevealMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload")) {
                    properties._encPayload = 1;
                    if (!(message.encPayload && typeof message.encPayload.length === "number" || $util.isString(message.encPayload)))
                        return "encPayload: buffer expected";
                }
                if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv")) {
                    properties._encIv = 1;
                    if (!(message.encIv && typeof message.encIv.length === "number" || $util.isString(message.encIv)))
                        return "encIv: buffer expected";
                }
                if (message.conditionalRevealMessageType != null && Object.hasOwnProperty.call(message, "conditionalRevealMessageType")) {
                    properties._conditionalRevealMessageType = 1;
                    if (typeof message.conditionalRevealMessageType !== "number" || (message.conditionalRevealMessageType | 0) !== message.conditionalRevealMessageType)
                        return "conditionalRevealMessageType: enum value expected";
                }
                if (message.revealKeyId != null && Object.hasOwnProperty.call(message, "revealKeyId")) {
                    properties._revealKeyId = 1;
                    if (!$util.isString(message.revealKeyId))
                        return "revealKeyId: string expected";
                }
                return null;
            };

            /**
             * Creates a ConditionalRevealMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.ConditionalRevealMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.ConditionalRevealMessage} ConditionalRevealMessage
             */
            ConditionalRevealMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.ConditionalRevealMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.encPayload != null)
                    if (typeof object.encPayload === "string")
                        $util.base64.decode(object.encPayload, message.encPayload = $util.newBuffer($util.base64.length(object.encPayload)), 0);
                    else if (object.encPayload.length >= 0)
                        message.encPayload = object.encPayload;
                if (object.encIv != null)
                    if (typeof object.encIv === "string")
                        $util.base64.decode(object.encIv, message.encIv = $util.newBuffer($util.base64.length(object.encIv)), 0);
                    else if (object.encIv.length >= 0)
                        message.encIv = object.encIv;
                switch (object.conditionalRevealMessageType) {
                case "UNKNOWN":
                case 0:
                    message.conditionalRevealMessageType = 0;
                    break;
                case "SCHEDULED_MESSAGE":
                case 1:
                    message.conditionalRevealMessageType = 1;
                    break;
                default:
                    if (typeof object.conditionalRevealMessageType === "number" && (object.conditionalRevealMessageType | 0) === object.conditionalRevealMessageType)
                        message.conditionalRevealMessageType = object.conditionalRevealMessageType;
                }
                if (object.revealKeyId != null)
                    message.revealKeyId = String(object.revealKeyId);
                return message;
            };

            /**
             * Creates a plain object from a ConditionalRevealMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.ConditionalRevealMessage
             * @static
             * @param {proto.Message.ConditionalRevealMessage} message ConditionalRevealMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ConditionalRevealMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload"))
                    object.encPayload = options.bytes === String ? $util.base64.encode(message.encPayload, 0, message.encPayload.length) : options.bytes === Array ? Array.prototype.slice.call(message.encPayload) : message.encPayload;
                if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv"))
                    object.encIv = options.bytes === String ? $util.base64.encode(message.encIv, 0, message.encIv.length) : options.bytes === Array ? Array.prototype.slice.call(message.encIv) : message.encIv;
                if (message.conditionalRevealMessageType != null && Object.hasOwnProperty.call(message, "conditionalRevealMessageType"))
                    object.conditionalRevealMessageType = options.enums === String ? $root.proto.Message.ConditionalRevealMessage.ConditionalRevealMessageType[message.conditionalRevealMessageType] === undefined ? message.conditionalRevealMessageType : $root.proto.Message.ConditionalRevealMessage.ConditionalRevealMessageType[message.conditionalRevealMessageType] : message.conditionalRevealMessageType;
                if (message.revealKeyId != null && Object.hasOwnProperty.call(message, "revealKeyId"))
                    object.revealKeyId = message.revealKeyId;
                return object;
            };

            /**
             * Converts this ConditionalRevealMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.ConditionalRevealMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ConditionalRevealMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ConditionalRevealMessage
             * @function getTypeUrl
             * @memberof proto.Message.ConditionalRevealMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ConditionalRevealMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.ConditionalRevealMessage";
            };

            /**
             * ConditionalRevealMessageType enum.
             * @name proto.Message.ConditionalRevealMessage.ConditionalRevealMessageType
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} SCHEDULED_MESSAGE=1 SCHEDULED_MESSAGE value
             */
            ConditionalRevealMessage.ConditionalRevealMessageType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "SCHEDULED_MESSAGE"] = 1;
                return values;
            })();

            return ConditionalRevealMessage;
        })();

        Message.ContactMessage = (function() {

            /**
             * Properties of a ContactMessage.
             * @memberof proto.Message
             * @interface IContactMessage
             * @property {string|null} [displayName] ContactMessage displayName
             * @property {string|null} [vcard] ContactMessage vcard
             * @property {proto.IContextInfo|null} [contextInfo] ContactMessage contextInfo
             * @property {boolean|null} [isSelfContact] ContactMessage isSelfContact
             */

            /**
             * Constructs a new ContactMessage.
             * @memberof proto.Message
             * @classdesc Represents a ContactMessage.
             * @implements IContactMessage
             * @constructor
             * @param {proto.Message.IContactMessage=} [properties] Properties to set
             */
            function ContactMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ContactMessage displayName.
             * @member {string|null|undefined} displayName
             * @memberof proto.Message.ContactMessage
             * @instance
             */
            ContactMessage.prototype.displayName = null;

            /**
             * ContactMessage vcard.
             * @member {string|null|undefined} vcard
             * @memberof proto.Message.ContactMessage
             * @instance
             */
            ContactMessage.prototype.vcard = null;

            /**
             * ContactMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.ContactMessage
             * @instance
             */
            ContactMessage.prototype.contextInfo = null;

            /**
             * ContactMessage isSelfContact.
             * @member {boolean|null|undefined} isSelfContact
             * @memberof proto.Message.ContactMessage
             * @instance
             */
            ContactMessage.prototype.isSelfContact = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ContactMessage.prototype, "_displayName", {
                get: $util.oneOfGetter($oneOfFields = ["displayName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ContactMessage.prototype, "_vcard", {
                get: $util.oneOfGetter($oneOfFields = ["vcard"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ContactMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ContactMessage.prototype, "_isSelfContact", {
                get: $util.oneOfGetter($oneOfFields = ["isSelfContact"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ContactMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.ContactMessage
             * @static
             * @param {proto.Message.IContactMessage=} [properties] Properties to set
             * @returns {proto.Message.ContactMessage} ContactMessage instance
             */
            ContactMessage.create = function create(properties) {
                return new ContactMessage(properties);
            };

            /**
             * Encodes the specified ContactMessage message. Does not implicitly {@link proto.Message.ContactMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.ContactMessage
             * @static
             * @param {proto.Message.IContactMessage} message ContactMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ContactMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.displayName != null && Object.hasOwnProperty.call(message, "displayName"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.displayName);
                if (message.vcard != null && Object.hasOwnProperty.call(message, "vcard"))
                    writer.uint32(/* id 16, wireType 2 =*/130).string(message.vcard);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
                if (message.isSelfContact != null && Object.hasOwnProperty.call(message, "isSelfContact"))
                    writer.uint32(/* id 18, wireType 0 =*/144).bool(message.isSelfContact);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ContactMessage message, length delimited. Does not implicitly {@link proto.Message.ContactMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.ContactMessage
             * @static
             * @param {proto.Message.IContactMessage} message ContactMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ContactMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ContactMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.ContactMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.ContactMessage} ContactMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ContactMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.displayName = reader.stringVerify();
                            message._displayName = "displayName";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            message.vcard = reader.stringVerify();
                            message._vcard = "vcard";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 18: {
                            if (u !== 0)
                                break;
                            message.isSelfContact = reader.bool();
                            message._isSelfContact = "isSelfContact";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ContactMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.ContactMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.ContactMessage} ContactMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ContactMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ContactMessage message.
             * @function verify
             * @memberof proto.Message.ContactMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ContactMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.displayName != null && Object.hasOwnProperty.call(message, "displayName")) {
                    properties._displayName = 1;
                    if (!$util.isString(message.displayName))
                        return "displayName: string expected";
                }
                if (message.vcard != null && Object.hasOwnProperty.call(message, "vcard")) {
                    properties._vcard = 1;
                    if (!$util.isString(message.vcard))
                        return "vcard: string expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.isSelfContact != null && Object.hasOwnProperty.call(message, "isSelfContact")) {
                    properties._isSelfContact = 1;
                    if (typeof message.isSelfContact !== "boolean")
                        return "isSelfContact: boolean expected";
                }
                return null;
            };

            /**
             * Creates a ContactMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.ContactMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.ContactMessage} ContactMessage
             */
            ContactMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.ContactMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.displayName != null)
                    message.displayName = String(object.displayName);
                if (object.vcard != null)
                    message.vcard = String(object.vcard);
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.ContactMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                if (object.isSelfContact != null)
                    message.isSelfContact = Boolean(object.isSelfContact);
                return message;
            };

            /**
             * Creates a plain object from a ContactMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.ContactMessage
             * @static
             * @param {proto.Message.ContactMessage} message ContactMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ContactMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.displayName != null && Object.hasOwnProperty.call(message, "displayName"))
                    object.displayName = message.displayName;
                if (message.vcard != null && Object.hasOwnProperty.call(message, "vcard"))
                    object.vcard = message.vcard;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.isSelfContact != null && Object.hasOwnProperty.call(message, "isSelfContact"))
                    object.isSelfContact = message.isSelfContact;
                return object;
            };

            /**
             * Converts this ContactMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.ContactMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ContactMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ContactMessage
             * @function getTypeUrl
             * @memberof proto.Message.ContactMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ContactMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.ContactMessage";
            };

            return ContactMessage;
        })();

        Message.ContactsArrayMessage = (function() {

            /**
             * Properties of a ContactsArrayMessage.
             * @memberof proto.Message
             * @interface IContactsArrayMessage
             * @property {string|null} [displayName] ContactsArrayMessage displayName
             * @property {Array.<proto.Message.IContactMessage>|null} [contacts] ContactsArrayMessage contacts
             * @property {proto.IContextInfo|null} [contextInfo] ContactsArrayMessage contextInfo
             */

            /**
             * Constructs a new ContactsArrayMessage.
             * @memberof proto.Message
             * @classdesc Represents a ContactsArrayMessage.
             * @implements IContactsArrayMessage
             * @constructor
             * @param {proto.Message.IContactsArrayMessage=} [properties] Properties to set
             */
            function ContactsArrayMessage(properties) {
                this.contacts = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ContactsArrayMessage displayName.
             * @member {string|null|undefined} displayName
             * @memberof proto.Message.ContactsArrayMessage
             * @instance
             */
            ContactsArrayMessage.prototype.displayName = null;

            /**
             * ContactsArrayMessage contacts.
             * @member {Array.<proto.Message.IContactMessage>} contacts
             * @memberof proto.Message.ContactsArrayMessage
             * @instance
             */
            ContactsArrayMessage.prototype.contacts = $util.emptyArray;

            /**
             * ContactsArrayMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.ContactsArrayMessage
             * @instance
             */
            ContactsArrayMessage.prototype.contextInfo = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ContactsArrayMessage.prototype, "_displayName", {
                get: $util.oneOfGetter($oneOfFields = ["displayName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ContactsArrayMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ContactsArrayMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.ContactsArrayMessage
             * @static
             * @param {proto.Message.IContactsArrayMessage=} [properties] Properties to set
             * @returns {proto.Message.ContactsArrayMessage} ContactsArrayMessage instance
             */
            ContactsArrayMessage.create = function create(properties) {
                return new ContactsArrayMessage(properties);
            };

            /**
             * Encodes the specified ContactsArrayMessage message. Does not implicitly {@link proto.Message.ContactsArrayMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.ContactsArrayMessage
             * @static
             * @param {proto.Message.IContactsArrayMessage} message ContactsArrayMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ContactsArrayMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.displayName != null && Object.hasOwnProperty.call(message, "displayName"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.displayName);
                if (message.contacts != null && message.contacts.length)
                    for (var i = 0; i < message.contacts.length; ++i)
                        $root.proto.Message.ContactMessage.encode(message.contacts[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ContactsArrayMessage message, length delimited. Does not implicitly {@link proto.Message.ContactsArrayMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.ContactsArrayMessage
             * @static
             * @param {proto.Message.IContactsArrayMessage} message ContactsArrayMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ContactsArrayMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ContactsArrayMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.ContactsArrayMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.ContactsArrayMessage} ContactsArrayMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ContactsArrayMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.displayName = reader.stringVerify();
                            message._displayName = "displayName";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(message.contacts && message.contacts.length))
                                message.contacts = [];
                            message.contacts.push($root.proto.Message.ContactMessage.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ContactsArrayMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.ContactsArrayMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.ContactsArrayMessage} ContactsArrayMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ContactsArrayMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ContactsArrayMessage message.
             * @function verify
             * @memberof proto.Message.ContactsArrayMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ContactsArrayMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.displayName != null && Object.hasOwnProperty.call(message, "displayName")) {
                    properties._displayName = 1;
                    if (!$util.isString(message.displayName))
                        return "displayName: string expected";
                }
                if (message.contacts != null && Object.hasOwnProperty.call(message, "contacts")) {
                    if (!Array.isArray(message.contacts))
                        return "contacts: array expected";
                    for (var i = 0; i < message.contacts.length; ++i) {
                        var error = $root.proto.Message.ContactMessage.verify(message.contacts[i], q + 1);
                        if (error)
                            return "contacts." + error;
                    }
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a ContactsArrayMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.ContactsArrayMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.ContactsArrayMessage} ContactsArrayMessage
             */
            ContactsArrayMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.ContactsArrayMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.displayName != null)
                    message.displayName = String(object.displayName);
                if (object.contacts) {
                    if (!Array.isArray(object.contacts))
                        throw TypeError(".proto.Message.ContactsArrayMessage.contacts: array expected");
                    message.contacts = Array(object.contacts.length);
                    for (var i = 0; i < object.contacts.length; ++i) {
                        if (!$util.isObject(object.contacts[i]))
                            throw TypeError(".proto.Message.ContactsArrayMessage.contacts: object expected");
                        message.contacts[i] = $root.proto.Message.ContactMessage.fromObject(object.contacts[i], q + 1);
                    }
                }
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.ContactsArrayMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a ContactsArrayMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.ContactsArrayMessage
             * @static
             * @param {proto.Message.ContactsArrayMessage} message ContactsArrayMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ContactsArrayMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.contacts = [];
                if (message.displayName != null && Object.hasOwnProperty.call(message, "displayName"))
                    object.displayName = message.displayName;
                if (message.contacts && message.contacts.length) {
                    object.contacts = Array(message.contacts.length);
                    for (var j = 0; j < message.contacts.length; ++j)
                        object.contacts[j] = $root.proto.Message.ContactMessage.toObject(message.contacts[j], options, q + 1);
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                return object;
            };

            /**
             * Converts this ContactsArrayMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.ContactsArrayMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ContactsArrayMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ContactsArrayMessage
             * @function getTypeUrl
             * @memberof proto.Message.ContactsArrayMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ContactsArrayMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.ContactsArrayMessage";
            };

            return ContactsArrayMessage;
        })();

        Message.DeclinePaymentRequestMessage = (function() {

            /**
             * Properties of a DeclinePaymentRequestMessage.
             * @memberof proto.Message
             * @interface IDeclinePaymentRequestMessage
             * @property {proto.IMessageKey|null} [key] DeclinePaymentRequestMessage key
             */

            /**
             * Constructs a new DeclinePaymentRequestMessage.
             * @memberof proto.Message
             * @classdesc Represents a DeclinePaymentRequestMessage.
             * @implements IDeclinePaymentRequestMessage
             * @constructor
             * @param {proto.Message.IDeclinePaymentRequestMessage=} [properties] Properties to set
             */
            function DeclinePaymentRequestMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * DeclinePaymentRequestMessage key.
             * @member {proto.IMessageKey|null|undefined} key
             * @memberof proto.Message.DeclinePaymentRequestMessage
             * @instance
             */
            DeclinePaymentRequestMessage.prototype.key = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DeclinePaymentRequestMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new DeclinePaymentRequestMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.DeclinePaymentRequestMessage
             * @static
             * @param {proto.Message.IDeclinePaymentRequestMessage=} [properties] Properties to set
             * @returns {proto.Message.DeclinePaymentRequestMessage} DeclinePaymentRequestMessage instance
             */
            DeclinePaymentRequestMessage.create = function create(properties) {
                return new DeclinePaymentRequestMessage(properties);
            };

            /**
             * Encodes the specified DeclinePaymentRequestMessage message. Does not implicitly {@link proto.Message.DeclinePaymentRequestMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.DeclinePaymentRequestMessage
             * @static
             * @param {proto.Message.IDeclinePaymentRequestMessage} message DeclinePaymentRequestMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DeclinePaymentRequestMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    $root.proto.MessageKey.encode(message.key, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DeclinePaymentRequestMessage message, length delimited. Does not implicitly {@link proto.Message.DeclinePaymentRequestMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.DeclinePaymentRequestMessage
             * @static
             * @param {proto.Message.IDeclinePaymentRequestMessage} message DeclinePaymentRequestMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DeclinePaymentRequestMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a DeclinePaymentRequestMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.DeclinePaymentRequestMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.DeclinePaymentRequestMessage} DeclinePaymentRequestMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DeclinePaymentRequestMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.key = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.key);
                            message._key = "key";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a DeclinePaymentRequestMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.DeclinePaymentRequestMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.DeclinePaymentRequestMessage} DeclinePaymentRequestMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DeclinePaymentRequestMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DeclinePaymentRequestMessage message.
             * @function verify
             * @memberof proto.Message.DeclinePaymentRequestMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DeclinePaymentRequestMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                    properties._key = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.key, q + 1);
                        if (error)
                            return "key." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a DeclinePaymentRequestMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.DeclinePaymentRequestMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.DeclinePaymentRequestMessage} DeclinePaymentRequestMessage
             */
            DeclinePaymentRequestMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.DeclinePaymentRequestMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.key != null) {
                    if (!$util.isObject(object.key))
                        throw TypeError(".proto.Message.DeclinePaymentRequestMessage.key: object expected");
                    message.key = $root.proto.MessageKey.fromObject(object.key, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a DeclinePaymentRequestMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.DeclinePaymentRequestMessage
             * @static
             * @param {proto.Message.DeclinePaymentRequestMessage} message DeclinePaymentRequestMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DeclinePaymentRequestMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    object.key = $root.proto.MessageKey.toObject(message.key, options, q + 1);
                return object;
            };

            /**
             * Converts this DeclinePaymentRequestMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.DeclinePaymentRequestMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DeclinePaymentRequestMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for DeclinePaymentRequestMessage
             * @function getTypeUrl
             * @memberof proto.Message.DeclinePaymentRequestMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            DeclinePaymentRequestMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.DeclinePaymentRequestMessage";
            };

            return DeclinePaymentRequestMessage;
        })();

        Message.DeviceSentMessage = (function() {

            /**
             * Properties of a DeviceSentMessage.
             * @memberof proto.Message
             * @interface IDeviceSentMessage
             * @property {string|null} [destinationJid] DeviceSentMessage destinationJid
             * @property {proto.IMessage|null} [message] DeviceSentMessage message
             * @property {string|null} [phash] DeviceSentMessage phash
             */

            /**
             * Constructs a new DeviceSentMessage.
             * @memberof proto.Message
             * @classdesc Represents a DeviceSentMessage.
             * @implements IDeviceSentMessage
             * @constructor
             * @param {proto.Message.IDeviceSentMessage=} [properties] Properties to set
             */
            function DeviceSentMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * DeviceSentMessage destinationJid.
             * @member {string|null|undefined} destinationJid
             * @memberof proto.Message.DeviceSentMessage
             * @instance
             */
            DeviceSentMessage.prototype.destinationJid = null;

            /**
             * DeviceSentMessage message.
             * @member {proto.IMessage|null|undefined} message
             * @memberof proto.Message.DeviceSentMessage
             * @instance
             */
            DeviceSentMessage.prototype.message = null;

            /**
             * DeviceSentMessage phash.
             * @member {string|null|undefined} phash
             * @memberof proto.Message.DeviceSentMessage
             * @instance
             */
            DeviceSentMessage.prototype.phash = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DeviceSentMessage.prototype, "_destinationJid", {
                get: $util.oneOfGetter($oneOfFields = ["destinationJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DeviceSentMessage.prototype, "_message", {
                get: $util.oneOfGetter($oneOfFields = ["message"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DeviceSentMessage.prototype, "_phash", {
                get: $util.oneOfGetter($oneOfFields = ["phash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new DeviceSentMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.DeviceSentMessage
             * @static
             * @param {proto.Message.IDeviceSentMessage=} [properties] Properties to set
             * @returns {proto.Message.DeviceSentMessage} DeviceSentMessage instance
             */
            DeviceSentMessage.create = function create(properties) {
                return new DeviceSentMessage(properties);
            };

            /**
             * Encodes the specified DeviceSentMessage message. Does not implicitly {@link proto.Message.DeviceSentMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.DeviceSentMessage
             * @static
             * @param {proto.Message.IDeviceSentMessage} message DeviceSentMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DeviceSentMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.destinationJid != null && Object.hasOwnProperty.call(message, "destinationJid"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.destinationJid);
                if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                    $root.proto.Message.encode(message.message, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.phash != null && Object.hasOwnProperty.call(message, "phash"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.phash);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DeviceSentMessage message, length delimited. Does not implicitly {@link proto.Message.DeviceSentMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.DeviceSentMessage
             * @static
             * @param {proto.Message.IDeviceSentMessage} message DeviceSentMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DeviceSentMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a DeviceSentMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.DeviceSentMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.DeviceSentMessage} DeviceSentMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DeviceSentMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.destinationJid = reader.stringVerify();
                            message._destinationJid = "destinationJid";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.message = $root.proto.Message.decode(reader, reader.uint32(), undefined, q + 1, message.message);
                            message._message = "message";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.phash = reader.stringVerify();
                            message._phash = "phash";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a DeviceSentMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.DeviceSentMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.DeviceSentMessage} DeviceSentMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DeviceSentMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DeviceSentMessage message.
             * @function verify
             * @memberof proto.Message.DeviceSentMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DeviceSentMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.destinationJid != null && Object.hasOwnProperty.call(message, "destinationJid")) {
                    properties._destinationJid = 1;
                    if (!$util.isString(message.destinationJid))
                        return "destinationJid: string expected";
                }
                if (message.message != null && Object.hasOwnProperty.call(message, "message")) {
                    properties._message = 1;
                    {
                        var error = $root.proto.Message.verify(message.message, q + 1);
                        if (error)
                            return "message." + error;
                    }
                }
                if (message.phash != null && Object.hasOwnProperty.call(message, "phash")) {
                    properties._phash = 1;
                    if (!$util.isString(message.phash))
                        return "phash: string expected";
                }
                return null;
            };

            /**
             * Creates a DeviceSentMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.DeviceSentMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.DeviceSentMessage} DeviceSentMessage
             */
            DeviceSentMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.DeviceSentMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.destinationJid != null)
                    message.destinationJid = String(object.destinationJid);
                if (object.message != null) {
                    if (!$util.isObject(object.message))
                        throw TypeError(".proto.Message.DeviceSentMessage.message: object expected");
                    message.message = $root.proto.Message.fromObject(object.message, q + 1);
                }
                if (object.phash != null)
                    message.phash = String(object.phash);
                return message;
            };

            /**
             * Creates a plain object from a DeviceSentMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.DeviceSentMessage
             * @static
             * @param {proto.Message.DeviceSentMessage} message DeviceSentMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DeviceSentMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.destinationJid != null && Object.hasOwnProperty.call(message, "destinationJid"))
                    object.destinationJid = message.destinationJid;
                if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                    object.message = $root.proto.Message.toObject(message.message, options, q + 1);
                if (message.phash != null && Object.hasOwnProperty.call(message, "phash"))
                    object.phash = message.phash;
                return object;
            };

            /**
             * Converts this DeviceSentMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.DeviceSentMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DeviceSentMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for DeviceSentMessage
             * @function getTypeUrl
             * @memberof proto.Message.DeviceSentMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            DeviceSentMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.DeviceSentMessage";
            };

            return DeviceSentMessage;
        })();

        Message.DocumentMessage = (function() {

            /**
             * Properties of a DocumentMessage.
             * @memberof proto.Message
             * @interface IDocumentMessage
             * @property {string|null} [url] DocumentMessage url
             * @property {string|null} [mimetype] DocumentMessage mimetype
             * @property {string|null} [title] DocumentMessage title
             * @property {Uint8Array|null} [fileSha256] DocumentMessage fileSha256
             * @property {number|Long|null} [fileLength] DocumentMessage fileLength
             * @property {number|null} [pageCount] DocumentMessage pageCount
             * @property {Uint8Array|null} [mediaKey] DocumentMessage mediaKey
             * @property {string|null} [fileName] DocumentMessage fileName
             * @property {Uint8Array|null} [fileEncSha256] DocumentMessage fileEncSha256
             * @property {string|null} [directPath] DocumentMessage directPath
             * @property {number|Long|null} [mediaKeyTimestamp] DocumentMessage mediaKeyTimestamp
             * @property {boolean|null} [contactVcard] DocumentMessage contactVcard
             * @property {string|null} [thumbnailDirectPath] DocumentMessage thumbnailDirectPath
             * @property {Uint8Array|null} [thumbnailSha256] DocumentMessage thumbnailSha256
             * @property {Uint8Array|null} [thumbnailEncSha256] DocumentMessage thumbnailEncSha256
             * @property {Uint8Array|null} [jpegThumbnail] DocumentMessage jpegThumbnail
             * @property {proto.IContextInfo|null} [contextInfo] DocumentMessage contextInfo
             * @property {number|null} [thumbnailHeight] DocumentMessage thumbnailHeight
             * @property {number|null} [thumbnailWidth] DocumentMessage thumbnailWidth
             * @property {string|null} [caption] DocumentMessage caption
             * @property {string|null} [accessibilityLabel] DocumentMessage accessibilityLabel
             */

            /**
             * Constructs a new DocumentMessage.
             * @memberof proto.Message
             * @classdesc Represents a DocumentMessage.
             * @implements IDocumentMessage
             * @constructor
             * @param {proto.Message.IDocumentMessage=} [properties] Properties to set
             */
            function DocumentMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * DocumentMessage url.
             * @member {string|null|undefined} url
             * @memberof proto.Message.DocumentMessage
             * @instance
             */
            DocumentMessage.prototype.url = null;

            /**
             * DocumentMessage mimetype.
             * @member {string|null|undefined} mimetype
             * @memberof proto.Message.DocumentMessage
             * @instance
             */
            DocumentMessage.prototype.mimetype = null;

            /**
             * DocumentMessage title.
             * @member {string|null|undefined} title
             * @memberof proto.Message.DocumentMessage
             * @instance
             */
            DocumentMessage.prototype.title = null;

            /**
             * DocumentMessage fileSha256.
             * @member {Uint8Array|null|undefined} fileSha256
             * @memberof proto.Message.DocumentMessage
             * @instance
             */
            DocumentMessage.prototype.fileSha256 = null;

            /**
             * DocumentMessage fileLength.
             * @member {number|Long|null|undefined} fileLength
             * @memberof proto.Message.DocumentMessage
             * @instance
             */
            DocumentMessage.prototype.fileLength = null;

            /**
             * DocumentMessage pageCount.
             * @member {number|null|undefined} pageCount
             * @memberof proto.Message.DocumentMessage
             * @instance
             */
            DocumentMessage.prototype.pageCount = null;

            /**
             * DocumentMessage mediaKey.
             * @member {Uint8Array|null|undefined} mediaKey
             * @memberof proto.Message.DocumentMessage
             * @instance
             */
            DocumentMessage.prototype.mediaKey = null;

            /**
             * DocumentMessage fileName.
             * @member {string|null|undefined} fileName
             * @memberof proto.Message.DocumentMessage
             * @instance
             */
            DocumentMessage.prototype.fileName = null;

            /**
             * DocumentMessage fileEncSha256.
             * @member {Uint8Array|null|undefined} fileEncSha256
             * @memberof proto.Message.DocumentMessage
             * @instance
             */
            DocumentMessage.prototype.fileEncSha256 = null;

            /**
             * DocumentMessage directPath.
             * @member {string|null|undefined} directPath
             * @memberof proto.Message.DocumentMessage
             * @instance
             */
            DocumentMessage.prototype.directPath = null;

            /**
             * DocumentMessage mediaKeyTimestamp.
             * @member {number|Long|null|undefined} mediaKeyTimestamp
             * @memberof proto.Message.DocumentMessage
             * @instance
             */
            DocumentMessage.prototype.mediaKeyTimestamp = null;

            /**
             * DocumentMessage contactVcard.
             * @member {boolean|null|undefined} contactVcard
             * @memberof proto.Message.DocumentMessage
             * @instance
             */
            DocumentMessage.prototype.contactVcard = null;

            /**
             * DocumentMessage thumbnailDirectPath.
             * @member {string|null|undefined} thumbnailDirectPath
             * @memberof proto.Message.DocumentMessage
             * @instance
             */
            DocumentMessage.prototype.thumbnailDirectPath = null;

            /**
             * DocumentMessage thumbnailSha256.
             * @member {Uint8Array|null|undefined} thumbnailSha256
             * @memberof proto.Message.DocumentMessage
             * @instance
             */
            DocumentMessage.prototype.thumbnailSha256 = null;

            /**
             * DocumentMessage thumbnailEncSha256.
             * @member {Uint8Array|null|undefined} thumbnailEncSha256
             * @memberof proto.Message.DocumentMessage
             * @instance
             */
            DocumentMessage.prototype.thumbnailEncSha256 = null;

            /**
             * DocumentMessage jpegThumbnail.
             * @member {Uint8Array|null|undefined} jpegThumbnail
             * @memberof proto.Message.DocumentMessage
             * @instance
             */
            DocumentMessage.prototype.jpegThumbnail = null;

            /**
             * DocumentMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.DocumentMessage
             * @instance
             */
            DocumentMessage.prototype.contextInfo = null;

            /**
             * DocumentMessage thumbnailHeight.
             * @member {number|null|undefined} thumbnailHeight
             * @memberof proto.Message.DocumentMessage
             * @instance
             */
            DocumentMessage.prototype.thumbnailHeight = null;

            /**
             * DocumentMessage thumbnailWidth.
             * @member {number|null|undefined} thumbnailWidth
             * @memberof proto.Message.DocumentMessage
             * @instance
             */
            DocumentMessage.prototype.thumbnailWidth = null;

            /**
             * DocumentMessage caption.
             * @member {string|null|undefined} caption
             * @memberof proto.Message.DocumentMessage
             * @instance
             */
            DocumentMessage.prototype.caption = null;

            /**
             * DocumentMessage accessibilityLabel.
             * @member {string|null|undefined} accessibilityLabel
             * @memberof proto.Message.DocumentMessage
             * @instance
             */
            DocumentMessage.prototype.accessibilityLabel = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DocumentMessage.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DocumentMessage.prototype, "_mimetype", {
                get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DocumentMessage.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DocumentMessage.prototype, "_fileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DocumentMessage.prototype, "_fileLength", {
                get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DocumentMessage.prototype, "_pageCount", {
                get: $util.oneOfGetter($oneOfFields = ["pageCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DocumentMessage.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DocumentMessage.prototype, "_fileName", {
                get: $util.oneOfGetter($oneOfFields = ["fileName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DocumentMessage.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DocumentMessage.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DocumentMessage.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DocumentMessage.prototype, "_contactVcard", {
                get: $util.oneOfGetter($oneOfFields = ["contactVcard"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DocumentMessage.prototype, "_thumbnailDirectPath", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailDirectPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DocumentMessage.prototype, "_thumbnailSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DocumentMessage.prototype, "_thumbnailEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DocumentMessage.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DocumentMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DocumentMessage.prototype, "_thumbnailHeight", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailHeight"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DocumentMessage.prototype, "_thumbnailWidth", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailWidth"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DocumentMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DocumentMessage.prototype, "_accessibilityLabel", {
                get: $util.oneOfGetter($oneOfFields = ["accessibilityLabel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new DocumentMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.DocumentMessage
             * @static
             * @param {proto.Message.IDocumentMessage=} [properties] Properties to set
             * @returns {proto.Message.DocumentMessage} DocumentMessage instance
             */
            DocumentMessage.create = function create(properties) {
                return new DocumentMessage(properties);
            };

            /**
             * Encodes the specified DocumentMessage message. Does not implicitly {@link proto.Message.DocumentMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.DocumentMessage
             * @static
             * @param {proto.Message.IDocumentMessage} message DocumentMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DocumentMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.url);
                if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.mimetype);
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.title);
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.fileSha256);
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                    writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.fileLength);
                if (message.pageCount != null && Object.hasOwnProperty.call(message, "pageCount"))
                    writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.pageCount);
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.mediaKey);
                if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.fileName);
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    writer.uint32(/* id 9, wireType 2 =*/74).bytes(message.fileEncSha256);
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    writer.uint32(/* id 10, wireType 2 =*/82).string(message.directPath);
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                    writer.uint32(/* id 11, wireType 0 =*/88).int64(message.mediaKeyTimestamp);
                if (message.contactVcard != null && Object.hasOwnProperty.call(message, "contactVcard"))
                    writer.uint32(/* id 12, wireType 0 =*/96).bool(message.contactVcard);
                if (message.thumbnailDirectPath != null && Object.hasOwnProperty.call(message, "thumbnailDirectPath"))
                    writer.uint32(/* id 13, wireType 2 =*/106).string(message.thumbnailDirectPath);
                if (message.thumbnailSha256 != null && Object.hasOwnProperty.call(message, "thumbnailSha256"))
                    writer.uint32(/* id 14, wireType 2 =*/114).bytes(message.thumbnailSha256);
                if (message.thumbnailEncSha256 != null && Object.hasOwnProperty.call(message, "thumbnailEncSha256"))
                    writer.uint32(/* id 15, wireType 2 =*/122).bytes(message.thumbnailEncSha256);
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    writer.uint32(/* id 16, wireType 2 =*/130).bytes(message.jpegThumbnail);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
                if (message.thumbnailHeight != null && Object.hasOwnProperty.call(message, "thumbnailHeight"))
                    writer.uint32(/* id 18, wireType 0 =*/144).uint32(message.thumbnailHeight);
                if (message.thumbnailWidth != null && Object.hasOwnProperty.call(message, "thumbnailWidth"))
                    writer.uint32(/* id 19, wireType 0 =*/152).uint32(message.thumbnailWidth);
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    writer.uint32(/* id 20, wireType 2 =*/162).string(message.caption);
                if (message.accessibilityLabel != null && Object.hasOwnProperty.call(message, "accessibilityLabel"))
                    writer.uint32(/* id 21, wireType 2 =*/170).string(message.accessibilityLabel);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DocumentMessage message, length delimited. Does not implicitly {@link proto.Message.DocumentMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.DocumentMessage
             * @static
             * @param {proto.Message.IDocumentMessage} message DocumentMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DocumentMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a DocumentMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.DocumentMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.DocumentMessage} DocumentMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DocumentMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.url = reader.stringVerify();
                            message._url = "url";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.mimetype = reader.stringVerify();
                            message._mimetype = "mimetype";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.title = reader.stringVerify();
                            message._title = "title";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.fileSha256 = reader.bytes();
                            message._fileSha256 = "fileSha256";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.fileLength = reader.uint64();
                            message._fileLength = "fileLength";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.pageCount = reader.uint32();
                            message._pageCount = "pageCount";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.mediaKey = reader.bytes();
                            message._mediaKey = "mediaKey";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.fileName = reader.stringVerify();
                            message._fileName = "fileName";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            message.fileEncSha256 = reader.bytes();
                            message._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 10: {
                            if (u !== 2)
                                break;
                            message.directPath = reader.stringVerify();
                            message._directPath = "directPath";
                            continue;
                        }
                    case 11: {
                            if (u !== 0)
                                break;
                            message.mediaKeyTimestamp = reader.int64();
                            message._mediaKeyTimestamp = "mediaKeyTimestamp";
                            continue;
                        }
                    case 12: {
                            if (u !== 0)
                                break;
                            message.contactVcard = reader.bool();
                            message._contactVcard = "contactVcard";
                            continue;
                        }
                    case 13: {
                            if (u !== 2)
                                break;
                            message.thumbnailDirectPath = reader.stringVerify();
                            message._thumbnailDirectPath = "thumbnailDirectPath";
                            continue;
                        }
                    case 14: {
                            if (u !== 2)
                                break;
                            message.thumbnailSha256 = reader.bytes();
                            message._thumbnailSha256 = "thumbnailSha256";
                            continue;
                        }
                    case 15: {
                            if (u !== 2)
                                break;
                            message.thumbnailEncSha256 = reader.bytes();
                            message._thumbnailEncSha256 = "thumbnailEncSha256";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            message.jpegThumbnail = reader.bytes();
                            message._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 18: {
                            if (u !== 0)
                                break;
                            message.thumbnailHeight = reader.uint32();
                            message._thumbnailHeight = "thumbnailHeight";
                            continue;
                        }
                    case 19: {
                            if (u !== 0)
                                break;
                            message.thumbnailWidth = reader.uint32();
                            message._thumbnailWidth = "thumbnailWidth";
                            continue;
                        }
                    case 20: {
                            if (u !== 2)
                                break;
                            message.caption = reader.stringVerify();
                            message._caption = "caption";
                            continue;
                        }
                    case 21: {
                            if (u !== 2)
                                break;
                            message.accessibilityLabel = reader.stringVerify();
                            message._accessibilityLabel = "accessibilityLabel";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a DocumentMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.DocumentMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.DocumentMessage} DocumentMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DocumentMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DocumentMessage message.
             * @function verify
             * @memberof proto.Message.DocumentMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DocumentMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.url != null && Object.hasOwnProperty.call(message, "url")) {
                    properties._url = 1;
                    if (!$util.isString(message.url))
                        return "url: string expected";
                }
                if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype")) {
                    properties._mimetype = 1;
                    if (!$util.isString(message.mimetype))
                        return "mimetype: string expected";
                }
                if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                    properties._title = 1;
                    if (!$util.isString(message.title))
                        return "title: string expected";
                }
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256")) {
                    properties._fileSha256 = 1;
                    if (!(message.fileSha256 && typeof message.fileSha256.length === "number" || $util.isString(message.fileSha256)))
                        return "fileSha256: buffer expected";
                }
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength")) {
                    properties._fileLength = 1;
                    if (!$util.isInteger(message.fileLength) && !(message.fileLength && $util.isInteger(message.fileLength.low) && $util.isInteger(message.fileLength.high)))
                        return "fileLength: integer|Long expected";
                }
                if (message.pageCount != null && Object.hasOwnProperty.call(message, "pageCount")) {
                    properties._pageCount = 1;
                    if (!$util.isInteger(message.pageCount))
                        return "pageCount: integer expected";
                }
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey")) {
                    properties._mediaKey = 1;
                    if (!(message.mediaKey && typeof message.mediaKey.length === "number" || $util.isString(message.mediaKey)))
                        return "mediaKey: buffer expected";
                }
                if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName")) {
                    properties._fileName = 1;
                    if (!$util.isString(message.fileName))
                        return "fileName: string expected";
                }
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256")) {
                    properties._fileEncSha256 = 1;
                    if (!(message.fileEncSha256 && typeof message.fileEncSha256.length === "number" || $util.isString(message.fileEncSha256)))
                        return "fileEncSha256: buffer expected";
                }
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath")) {
                    properties._directPath = 1;
                    if (!$util.isString(message.directPath))
                        return "directPath: string expected";
                }
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp")) {
                    properties._mediaKeyTimestamp = 1;
                    if (!$util.isInteger(message.mediaKeyTimestamp) && !(message.mediaKeyTimestamp && $util.isInteger(message.mediaKeyTimestamp.low) && $util.isInteger(message.mediaKeyTimestamp.high)))
                        return "mediaKeyTimestamp: integer|Long expected";
                }
                if (message.contactVcard != null && Object.hasOwnProperty.call(message, "contactVcard")) {
                    properties._contactVcard = 1;
                    if (typeof message.contactVcard !== "boolean")
                        return "contactVcard: boolean expected";
                }
                if (message.thumbnailDirectPath != null && Object.hasOwnProperty.call(message, "thumbnailDirectPath")) {
                    properties._thumbnailDirectPath = 1;
                    if (!$util.isString(message.thumbnailDirectPath))
                        return "thumbnailDirectPath: string expected";
                }
                if (message.thumbnailSha256 != null && Object.hasOwnProperty.call(message, "thumbnailSha256")) {
                    properties._thumbnailSha256 = 1;
                    if (!(message.thumbnailSha256 && typeof message.thumbnailSha256.length === "number" || $util.isString(message.thumbnailSha256)))
                        return "thumbnailSha256: buffer expected";
                }
                if (message.thumbnailEncSha256 != null && Object.hasOwnProperty.call(message, "thumbnailEncSha256")) {
                    properties._thumbnailEncSha256 = 1;
                    if (!(message.thumbnailEncSha256 && typeof message.thumbnailEncSha256.length === "number" || $util.isString(message.thumbnailEncSha256)))
                        return "thumbnailEncSha256: buffer expected";
                }
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail")) {
                    properties._jpegThumbnail = 1;
                    if (!(message.jpegThumbnail && typeof message.jpegThumbnail.length === "number" || $util.isString(message.jpegThumbnail)))
                        return "jpegThumbnail: buffer expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.thumbnailHeight != null && Object.hasOwnProperty.call(message, "thumbnailHeight")) {
                    properties._thumbnailHeight = 1;
                    if (!$util.isInteger(message.thumbnailHeight))
                        return "thumbnailHeight: integer expected";
                }
                if (message.thumbnailWidth != null && Object.hasOwnProperty.call(message, "thumbnailWidth")) {
                    properties._thumbnailWidth = 1;
                    if (!$util.isInteger(message.thumbnailWidth))
                        return "thumbnailWidth: integer expected";
                }
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption")) {
                    properties._caption = 1;
                    if (!$util.isString(message.caption))
                        return "caption: string expected";
                }
                if (message.accessibilityLabel != null && Object.hasOwnProperty.call(message, "accessibilityLabel")) {
                    properties._accessibilityLabel = 1;
                    if (!$util.isString(message.accessibilityLabel))
                        return "accessibilityLabel: string expected";
                }
                return null;
            };

            /**
             * Creates a DocumentMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.DocumentMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.DocumentMessage} DocumentMessage
             */
            DocumentMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.DocumentMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.url != null)
                    message.url = String(object.url);
                if (object.mimetype != null)
                    message.mimetype = String(object.mimetype);
                if (object.title != null)
                    message.title = String(object.title);
                if (object.fileSha256 != null)
                    if (typeof object.fileSha256 === "string")
                        $util.base64.decode(object.fileSha256, message.fileSha256 = $util.newBuffer($util.base64.length(object.fileSha256)), 0);
                    else if (object.fileSha256.length >= 0)
                        message.fileSha256 = object.fileSha256;
                if (object.fileLength != null)
                    if ($util.Long)
                        message.fileLength = $util.Long.fromValue(object.fileLength, true);
                    else if (typeof object.fileLength === "string")
                        message.fileLength = parseInt(object.fileLength, 10);
                    else if (typeof object.fileLength === "number")
                        message.fileLength = object.fileLength;
                    else if (typeof object.fileLength === "object")
                        message.fileLength = new $util.LongBits(object.fileLength.low >>> 0, object.fileLength.high >>> 0).toNumber(true);
                if (object.pageCount != null)
                    message.pageCount = object.pageCount >>> 0;
                if (object.mediaKey != null)
                    if (typeof object.mediaKey === "string")
                        $util.base64.decode(object.mediaKey, message.mediaKey = $util.newBuffer($util.base64.length(object.mediaKey)), 0);
                    else if (object.mediaKey.length >= 0)
                        message.mediaKey = object.mediaKey;
                if (object.fileName != null)
                    message.fileName = String(object.fileName);
                if (object.fileEncSha256 != null)
                    if (typeof object.fileEncSha256 === "string")
                        $util.base64.decode(object.fileEncSha256, message.fileEncSha256 = $util.newBuffer($util.base64.length(object.fileEncSha256)), 0);
                    else if (object.fileEncSha256.length >= 0)
                        message.fileEncSha256 = object.fileEncSha256;
                if (object.directPath != null)
                    message.directPath = String(object.directPath);
                if (object.mediaKeyTimestamp != null)
                    if ($util.Long)
                        message.mediaKeyTimestamp = $util.Long.fromValue(object.mediaKeyTimestamp, false);
                    else if (typeof object.mediaKeyTimestamp === "string")
                        message.mediaKeyTimestamp = parseInt(object.mediaKeyTimestamp, 10);
                    else if (typeof object.mediaKeyTimestamp === "number")
                        message.mediaKeyTimestamp = object.mediaKeyTimestamp;
                    else if (typeof object.mediaKeyTimestamp === "object")
                        message.mediaKeyTimestamp = new $util.LongBits(object.mediaKeyTimestamp.low >>> 0, object.mediaKeyTimestamp.high >>> 0).toNumber();
                if (object.contactVcard != null)
                    message.contactVcard = Boolean(object.contactVcard);
                if (object.thumbnailDirectPath != null)
                    message.thumbnailDirectPath = String(object.thumbnailDirectPath);
                if (object.thumbnailSha256 != null)
                    if (typeof object.thumbnailSha256 === "string")
                        $util.base64.decode(object.thumbnailSha256, message.thumbnailSha256 = $util.newBuffer($util.base64.length(object.thumbnailSha256)), 0);
                    else if (object.thumbnailSha256.length >= 0)
                        message.thumbnailSha256 = object.thumbnailSha256;
                if (object.thumbnailEncSha256 != null)
                    if (typeof object.thumbnailEncSha256 === "string")
                        $util.base64.decode(object.thumbnailEncSha256, message.thumbnailEncSha256 = $util.newBuffer($util.base64.length(object.thumbnailEncSha256)), 0);
                    else if (object.thumbnailEncSha256.length >= 0)
                        message.thumbnailEncSha256 = object.thumbnailEncSha256;
                if (object.jpegThumbnail != null)
                    if (typeof object.jpegThumbnail === "string")
                        $util.base64.decode(object.jpegThumbnail, message.jpegThumbnail = $util.newBuffer($util.base64.length(object.jpegThumbnail)), 0);
                    else if (object.jpegThumbnail.length >= 0)
                        message.jpegThumbnail = object.jpegThumbnail;
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.DocumentMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                if (object.thumbnailHeight != null)
                    message.thumbnailHeight = object.thumbnailHeight >>> 0;
                if (object.thumbnailWidth != null)
                    message.thumbnailWidth = object.thumbnailWidth >>> 0;
                if (object.caption != null)
                    message.caption = String(object.caption);
                if (object.accessibilityLabel != null)
                    message.accessibilityLabel = String(object.accessibilityLabel);
                return message;
            };

            /**
             * Creates a plain object from a DocumentMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.DocumentMessage
             * @static
             * @param {proto.Message.DocumentMessage} message DocumentMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DocumentMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    object.url = message.url;
                if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                    object.mimetype = message.mimetype;
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    object.title = message.title;
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                    object.fileSha256 = options.bytes === String ? $util.base64.encode(message.fileSha256, 0, message.fileSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileSha256) : message.fileSha256;
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.fileLength = typeof message.fileLength === "number" ? BigInt(message.fileLength) : $util.Long.fromBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0, true).toBigInt();
                    else if (typeof message.fileLength === "number")
                        object.fileLength = options.longs === String ? String(message.fileLength) : message.fileLength;
                    else
                        object.fileLength = options.longs === String ? $util.Long.prototype.toString.call(message.fileLength) : options.longs === Number ? new $util.LongBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0).toNumber(true) : message.fileLength;
                if (message.pageCount != null && Object.hasOwnProperty.call(message, "pageCount"))
                    object.pageCount = message.pageCount;
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    object.mediaKey = options.bytes === String ? $util.base64.encode(message.mediaKey, 0, message.mediaKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.mediaKey) : message.mediaKey;
                if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                    object.fileName = message.fileName;
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    object.fileEncSha256 = options.bytes === String ? $util.base64.encode(message.fileEncSha256, 0, message.fileEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileEncSha256) : message.fileEncSha256;
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    object.directPath = message.directPath;
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.mediaKeyTimestamp = typeof message.mediaKeyTimestamp === "number" ? BigInt(message.mediaKeyTimestamp) : $util.Long.fromBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.mediaKeyTimestamp === "number")
                        object.mediaKeyTimestamp = options.longs === String ? String(message.mediaKeyTimestamp) : message.mediaKeyTimestamp;
                    else
                        object.mediaKeyTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.mediaKeyTimestamp) : options.longs === Number ? new $util.LongBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0).toNumber() : message.mediaKeyTimestamp;
                if (message.contactVcard != null && Object.hasOwnProperty.call(message, "contactVcard"))
                    object.contactVcard = message.contactVcard;
                if (message.thumbnailDirectPath != null && Object.hasOwnProperty.call(message, "thumbnailDirectPath"))
                    object.thumbnailDirectPath = message.thumbnailDirectPath;
                if (message.thumbnailSha256 != null && Object.hasOwnProperty.call(message, "thumbnailSha256"))
                    object.thumbnailSha256 = options.bytes === String ? $util.base64.encode(message.thumbnailSha256, 0, message.thumbnailSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.thumbnailSha256) : message.thumbnailSha256;
                if (message.thumbnailEncSha256 != null && Object.hasOwnProperty.call(message, "thumbnailEncSha256"))
                    object.thumbnailEncSha256 = options.bytes === String ? $util.base64.encode(message.thumbnailEncSha256, 0, message.thumbnailEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.thumbnailEncSha256) : message.thumbnailEncSha256;
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    object.jpegThumbnail = options.bytes === String ? $util.base64.encode(message.jpegThumbnail, 0, message.jpegThumbnail.length) : options.bytes === Array ? Array.prototype.slice.call(message.jpegThumbnail) : message.jpegThumbnail;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.thumbnailHeight != null && Object.hasOwnProperty.call(message, "thumbnailHeight"))
                    object.thumbnailHeight = message.thumbnailHeight;
                if (message.thumbnailWidth != null && Object.hasOwnProperty.call(message, "thumbnailWidth"))
                    object.thumbnailWidth = message.thumbnailWidth;
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    object.caption = message.caption;
                if (message.accessibilityLabel != null && Object.hasOwnProperty.call(message, "accessibilityLabel"))
                    object.accessibilityLabel = message.accessibilityLabel;
                return object;
            };

            /**
             * Converts this DocumentMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.DocumentMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DocumentMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for DocumentMessage
             * @function getTypeUrl
             * @memberof proto.Message.DocumentMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            DocumentMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.DocumentMessage";
            };

            return DocumentMessage;
        })();

        Message.EncCommentMessage = (function() {

            /**
             * Properties of an EncCommentMessage.
             * @memberof proto.Message
             * @interface IEncCommentMessage
             * @property {proto.IMessageKey|null} [targetMessageKey] EncCommentMessage targetMessageKey
             * @property {Uint8Array|null} [encPayload] EncCommentMessage encPayload
             * @property {Uint8Array|null} [encIv] EncCommentMessage encIv
             */

            /**
             * Constructs a new EncCommentMessage.
             * @memberof proto.Message
             * @classdesc Represents an EncCommentMessage.
             * @implements IEncCommentMessage
             * @constructor
             * @param {proto.Message.IEncCommentMessage=} [properties] Properties to set
             */
            function EncCommentMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * EncCommentMessage targetMessageKey.
             * @member {proto.IMessageKey|null|undefined} targetMessageKey
             * @memberof proto.Message.EncCommentMessage
             * @instance
             */
            EncCommentMessage.prototype.targetMessageKey = null;

            /**
             * EncCommentMessage encPayload.
             * @member {Uint8Array|null|undefined} encPayload
             * @memberof proto.Message.EncCommentMessage
             * @instance
             */
            EncCommentMessage.prototype.encPayload = null;

            /**
             * EncCommentMessage encIv.
             * @member {Uint8Array|null|undefined} encIv
             * @memberof proto.Message.EncCommentMessage
             * @instance
             */
            EncCommentMessage.prototype.encIv = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EncCommentMessage.prototype, "_targetMessageKey", {
                get: $util.oneOfGetter($oneOfFields = ["targetMessageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EncCommentMessage.prototype, "_encPayload", {
                get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EncCommentMessage.prototype, "_encIv", {
                get: $util.oneOfGetter($oneOfFields = ["encIv"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new EncCommentMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.EncCommentMessage
             * @static
             * @param {proto.Message.IEncCommentMessage=} [properties] Properties to set
             * @returns {proto.Message.EncCommentMessage} EncCommentMessage instance
             */
            EncCommentMessage.create = function create(properties) {
                return new EncCommentMessage(properties);
            };

            /**
             * Encodes the specified EncCommentMessage message. Does not implicitly {@link proto.Message.EncCommentMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.EncCommentMessage
             * @static
             * @param {proto.Message.IEncCommentMessage} message EncCommentMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EncCommentMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.targetMessageKey != null && Object.hasOwnProperty.call(message, "targetMessageKey"))
                    $root.proto.MessageKey.encode(message.targetMessageKey, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encPayload);
                if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.encIv);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified EncCommentMessage message, length delimited. Does not implicitly {@link proto.Message.EncCommentMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.EncCommentMessage
             * @static
             * @param {proto.Message.IEncCommentMessage} message EncCommentMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EncCommentMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an EncCommentMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.EncCommentMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.EncCommentMessage} EncCommentMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EncCommentMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.targetMessageKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.targetMessageKey);
                            message._targetMessageKey = "targetMessageKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.encPayload = reader.bytes();
                            message._encPayload = "encPayload";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.encIv = reader.bytes();
                            message._encIv = "encIv";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an EncCommentMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.EncCommentMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.EncCommentMessage} EncCommentMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EncCommentMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an EncCommentMessage message.
             * @function verify
             * @memberof proto.Message.EncCommentMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            EncCommentMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.targetMessageKey != null && Object.hasOwnProperty.call(message, "targetMessageKey")) {
                    properties._targetMessageKey = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.targetMessageKey, q + 1);
                        if (error)
                            return "targetMessageKey." + error;
                    }
                }
                if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload")) {
                    properties._encPayload = 1;
                    if (!(message.encPayload && typeof message.encPayload.length === "number" || $util.isString(message.encPayload)))
                        return "encPayload: buffer expected";
                }
                if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv")) {
                    properties._encIv = 1;
                    if (!(message.encIv && typeof message.encIv.length === "number" || $util.isString(message.encIv)))
                        return "encIv: buffer expected";
                }
                return null;
            };

            /**
             * Creates an EncCommentMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.EncCommentMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.EncCommentMessage} EncCommentMessage
             */
            EncCommentMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.EncCommentMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.targetMessageKey != null) {
                    if (!$util.isObject(object.targetMessageKey))
                        throw TypeError(".proto.Message.EncCommentMessage.targetMessageKey: object expected");
                    message.targetMessageKey = $root.proto.MessageKey.fromObject(object.targetMessageKey, q + 1);
                }
                if (object.encPayload != null)
                    if (typeof object.encPayload === "string")
                        $util.base64.decode(object.encPayload, message.encPayload = $util.newBuffer($util.base64.length(object.encPayload)), 0);
                    else if (object.encPayload.length >= 0)
                        message.encPayload = object.encPayload;
                if (object.encIv != null)
                    if (typeof object.encIv === "string")
                        $util.base64.decode(object.encIv, message.encIv = $util.newBuffer($util.base64.length(object.encIv)), 0);
                    else if (object.encIv.length >= 0)
                        message.encIv = object.encIv;
                return message;
            };

            /**
             * Creates a plain object from an EncCommentMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.EncCommentMessage
             * @static
             * @param {proto.Message.EncCommentMessage} message EncCommentMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            EncCommentMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.targetMessageKey != null && Object.hasOwnProperty.call(message, "targetMessageKey"))
                    object.targetMessageKey = $root.proto.MessageKey.toObject(message.targetMessageKey, options, q + 1);
                if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload"))
                    object.encPayload = options.bytes === String ? $util.base64.encode(message.encPayload, 0, message.encPayload.length) : options.bytes === Array ? Array.prototype.slice.call(message.encPayload) : message.encPayload;
                if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv"))
                    object.encIv = options.bytes === String ? $util.base64.encode(message.encIv, 0, message.encIv.length) : options.bytes === Array ? Array.prototype.slice.call(message.encIv) : message.encIv;
                return object;
            };

            /**
             * Converts this EncCommentMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.EncCommentMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            EncCommentMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for EncCommentMessage
             * @function getTypeUrl
             * @memberof proto.Message.EncCommentMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            EncCommentMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.EncCommentMessage";
            };

            return EncCommentMessage;
        })();

        Message.EncEventResponseMessage = (function() {

            /**
             * Properties of an EncEventResponseMessage.
             * @memberof proto.Message
             * @interface IEncEventResponseMessage
             * @property {proto.IMessageKey|null} [eventCreationMessageKey] EncEventResponseMessage eventCreationMessageKey
             * @property {Uint8Array|null} [encPayload] EncEventResponseMessage encPayload
             * @property {Uint8Array|null} [encIv] EncEventResponseMessage encIv
             */

            /**
             * Constructs a new EncEventResponseMessage.
             * @memberof proto.Message
             * @classdesc Represents an EncEventResponseMessage.
             * @implements IEncEventResponseMessage
             * @constructor
             * @param {proto.Message.IEncEventResponseMessage=} [properties] Properties to set
             */
            function EncEventResponseMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * EncEventResponseMessage eventCreationMessageKey.
             * @member {proto.IMessageKey|null|undefined} eventCreationMessageKey
             * @memberof proto.Message.EncEventResponseMessage
             * @instance
             */
            EncEventResponseMessage.prototype.eventCreationMessageKey = null;

            /**
             * EncEventResponseMessage encPayload.
             * @member {Uint8Array|null|undefined} encPayload
             * @memberof proto.Message.EncEventResponseMessage
             * @instance
             */
            EncEventResponseMessage.prototype.encPayload = null;

            /**
             * EncEventResponseMessage encIv.
             * @member {Uint8Array|null|undefined} encIv
             * @memberof proto.Message.EncEventResponseMessage
             * @instance
             */
            EncEventResponseMessage.prototype.encIv = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EncEventResponseMessage.prototype, "_eventCreationMessageKey", {
                get: $util.oneOfGetter($oneOfFields = ["eventCreationMessageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EncEventResponseMessage.prototype, "_encPayload", {
                get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EncEventResponseMessage.prototype, "_encIv", {
                get: $util.oneOfGetter($oneOfFields = ["encIv"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new EncEventResponseMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.EncEventResponseMessage
             * @static
             * @param {proto.Message.IEncEventResponseMessage=} [properties] Properties to set
             * @returns {proto.Message.EncEventResponseMessage} EncEventResponseMessage instance
             */
            EncEventResponseMessage.create = function create(properties) {
                return new EncEventResponseMessage(properties);
            };

            /**
             * Encodes the specified EncEventResponseMessage message. Does not implicitly {@link proto.Message.EncEventResponseMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.EncEventResponseMessage
             * @static
             * @param {proto.Message.IEncEventResponseMessage} message EncEventResponseMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EncEventResponseMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.eventCreationMessageKey != null && Object.hasOwnProperty.call(message, "eventCreationMessageKey"))
                    $root.proto.MessageKey.encode(message.eventCreationMessageKey, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encPayload);
                if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.encIv);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified EncEventResponseMessage message, length delimited. Does not implicitly {@link proto.Message.EncEventResponseMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.EncEventResponseMessage
             * @static
             * @param {proto.Message.IEncEventResponseMessage} message EncEventResponseMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EncEventResponseMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an EncEventResponseMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.EncEventResponseMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.EncEventResponseMessage} EncEventResponseMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EncEventResponseMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.eventCreationMessageKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.eventCreationMessageKey);
                            message._eventCreationMessageKey = "eventCreationMessageKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.encPayload = reader.bytes();
                            message._encPayload = "encPayload";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.encIv = reader.bytes();
                            message._encIv = "encIv";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an EncEventResponseMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.EncEventResponseMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.EncEventResponseMessage} EncEventResponseMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EncEventResponseMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an EncEventResponseMessage message.
             * @function verify
             * @memberof proto.Message.EncEventResponseMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            EncEventResponseMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.eventCreationMessageKey != null && Object.hasOwnProperty.call(message, "eventCreationMessageKey")) {
                    properties._eventCreationMessageKey = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.eventCreationMessageKey, q + 1);
                        if (error)
                            return "eventCreationMessageKey." + error;
                    }
                }
                if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload")) {
                    properties._encPayload = 1;
                    if (!(message.encPayload && typeof message.encPayload.length === "number" || $util.isString(message.encPayload)))
                        return "encPayload: buffer expected";
                }
                if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv")) {
                    properties._encIv = 1;
                    if (!(message.encIv && typeof message.encIv.length === "number" || $util.isString(message.encIv)))
                        return "encIv: buffer expected";
                }
                return null;
            };

            /**
             * Creates an EncEventResponseMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.EncEventResponseMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.EncEventResponseMessage} EncEventResponseMessage
             */
            EncEventResponseMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.EncEventResponseMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.eventCreationMessageKey != null) {
                    if (!$util.isObject(object.eventCreationMessageKey))
                        throw TypeError(".proto.Message.EncEventResponseMessage.eventCreationMessageKey: object expected");
                    message.eventCreationMessageKey = $root.proto.MessageKey.fromObject(object.eventCreationMessageKey, q + 1);
                }
                if (object.encPayload != null)
                    if (typeof object.encPayload === "string")
                        $util.base64.decode(object.encPayload, message.encPayload = $util.newBuffer($util.base64.length(object.encPayload)), 0);
                    else if (object.encPayload.length >= 0)
                        message.encPayload = object.encPayload;
                if (object.encIv != null)
                    if (typeof object.encIv === "string")
                        $util.base64.decode(object.encIv, message.encIv = $util.newBuffer($util.base64.length(object.encIv)), 0);
                    else if (object.encIv.length >= 0)
                        message.encIv = object.encIv;
                return message;
            };

            /**
             * Creates a plain object from an EncEventResponseMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.EncEventResponseMessage
             * @static
             * @param {proto.Message.EncEventResponseMessage} message EncEventResponseMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            EncEventResponseMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.eventCreationMessageKey != null && Object.hasOwnProperty.call(message, "eventCreationMessageKey"))
                    object.eventCreationMessageKey = $root.proto.MessageKey.toObject(message.eventCreationMessageKey, options, q + 1);
                if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload"))
                    object.encPayload = options.bytes === String ? $util.base64.encode(message.encPayload, 0, message.encPayload.length) : options.bytes === Array ? Array.prototype.slice.call(message.encPayload) : message.encPayload;
                if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv"))
                    object.encIv = options.bytes === String ? $util.base64.encode(message.encIv, 0, message.encIv.length) : options.bytes === Array ? Array.prototype.slice.call(message.encIv) : message.encIv;
                return object;
            };

            /**
             * Converts this EncEventResponseMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.EncEventResponseMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            EncEventResponseMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for EncEventResponseMessage
             * @function getTypeUrl
             * @memberof proto.Message.EncEventResponseMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            EncEventResponseMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.EncEventResponseMessage";
            };

            return EncEventResponseMessage;
        })();

        Message.EncReactionMessage = (function() {

            /**
             * Properties of an EncReactionMessage.
             * @memberof proto.Message
             * @interface IEncReactionMessage
             * @property {proto.IMessageKey|null} [targetMessageKey] EncReactionMessage targetMessageKey
             * @property {Uint8Array|null} [encPayload] EncReactionMessage encPayload
             * @property {Uint8Array|null} [encIv] EncReactionMessage encIv
             */

            /**
             * Constructs a new EncReactionMessage.
             * @memberof proto.Message
             * @classdesc Represents an EncReactionMessage.
             * @implements IEncReactionMessage
             * @constructor
             * @param {proto.Message.IEncReactionMessage=} [properties] Properties to set
             */
            function EncReactionMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * EncReactionMessage targetMessageKey.
             * @member {proto.IMessageKey|null|undefined} targetMessageKey
             * @memberof proto.Message.EncReactionMessage
             * @instance
             */
            EncReactionMessage.prototype.targetMessageKey = null;

            /**
             * EncReactionMessage encPayload.
             * @member {Uint8Array|null|undefined} encPayload
             * @memberof proto.Message.EncReactionMessage
             * @instance
             */
            EncReactionMessage.prototype.encPayload = null;

            /**
             * EncReactionMessage encIv.
             * @member {Uint8Array|null|undefined} encIv
             * @memberof proto.Message.EncReactionMessage
             * @instance
             */
            EncReactionMessage.prototype.encIv = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EncReactionMessage.prototype, "_targetMessageKey", {
                get: $util.oneOfGetter($oneOfFields = ["targetMessageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EncReactionMessage.prototype, "_encPayload", {
                get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EncReactionMessage.prototype, "_encIv", {
                get: $util.oneOfGetter($oneOfFields = ["encIv"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new EncReactionMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.EncReactionMessage
             * @static
             * @param {proto.Message.IEncReactionMessage=} [properties] Properties to set
             * @returns {proto.Message.EncReactionMessage} EncReactionMessage instance
             */
            EncReactionMessage.create = function create(properties) {
                return new EncReactionMessage(properties);
            };

            /**
             * Encodes the specified EncReactionMessage message. Does not implicitly {@link proto.Message.EncReactionMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.EncReactionMessage
             * @static
             * @param {proto.Message.IEncReactionMessage} message EncReactionMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EncReactionMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.targetMessageKey != null && Object.hasOwnProperty.call(message, "targetMessageKey"))
                    $root.proto.MessageKey.encode(message.targetMessageKey, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encPayload);
                if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.encIv);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified EncReactionMessage message, length delimited. Does not implicitly {@link proto.Message.EncReactionMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.EncReactionMessage
             * @static
             * @param {proto.Message.IEncReactionMessage} message EncReactionMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EncReactionMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an EncReactionMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.EncReactionMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.EncReactionMessage} EncReactionMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EncReactionMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.targetMessageKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.targetMessageKey);
                            message._targetMessageKey = "targetMessageKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.encPayload = reader.bytes();
                            message._encPayload = "encPayload";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.encIv = reader.bytes();
                            message._encIv = "encIv";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an EncReactionMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.EncReactionMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.EncReactionMessage} EncReactionMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EncReactionMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an EncReactionMessage message.
             * @function verify
             * @memberof proto.Message.EncReactionMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            EncReactionMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.targetMessageKey != null && Object.hasOwnProperty.call(message, "targetMessageKey")) {
                    properties._targetMessageKey = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.targetMessageKey, q + 1);
                        if (error)
                            return "targetMessageKey." + error;
                    }
                }
                if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload")) {
                    properties._encPayload = 1;
                    if (!(message.encPayload && typeof message.encPayload.length === "number" || $util.isString(message.encPayload)))
                        return "encPayload: buffer expected";
                }
                if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv")) {
                    properties._encIv = 1;
                    if (!(message.encIv && typeof message.encIv.length === "number" || $util.isString(message.encIv)))
                        return "encIv: buffer expected";
                }
                return null;
            };

            /**
             * Creates an EncReactionMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.EncReactionMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.EncReactionMessage} EncReactionMessage
             */
            EncReactionMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.EncReactionMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.targetMessageKey != null) {
                    if (!$util.isObject(object.targetMessageKey))
                        throw TypeError(".proto.Message.EncReactionMessage.targetMessageKey: object expected");
                    message.targetMessageKey = $root.proto.MessageKey.fromObject(object.targetMessageKey, q + 1);
                }
                if (object.encPayload != null)
                    if (typeof object.encPayload === "string")
                        $util.base64.decode(object.encPayload, message.encPayload = $util.newBuffer($util.base64.length(object.encPayload)), 0);
                    else if (object.encPayload.length >= 0)
                        message.encPayload = object.encPayload;
                if (object.encIv != null)
                    if (typeof object.encIv === "string")
                        $util.base64.decode(object.encIv, message.encIv = $util.newBuffer($util.base64.length(object.encIv)), 0);
                    else if (object.encIv.length >= 0)
                        message.encIv = object.encIv;
                return message;
            };

            /**
             * Creates a plain object from an EncReactionMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.EncReactionMessage
             * @static
             * @param {proto.Message.EncReactionMessage} message EncReactionMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            EncReactionMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.targetMessageKey != null && Object.hasOwnProperty.call(message, "targetMessageKey"))
                    object.targetMessageKey = $root.proto.MessageKey.toObject(message.targetMessageKey, options, q + 1);
                if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload"))
                    object.encPayload = options.bytes === String ? $util.base64.encode(message.encPayload, 0, message.encPayload.length) : options.bytes === Array ? Array.prototype.slice.call(message.encPayload) : message.encPayload;
                if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv"))
                    object.encIv = options.bytes === String ? $util.base64.encode(message.encIv, 0, message.encIv.length) : options.bytes === Array ? Array.prototype.slice.call(message.encIv) : message.encIv;
                return object;
            };

            /**
             * Converts this EncReactionMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.EncReactionMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            EncReactionMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for EncReactionMessage
             * @function getTypeUrl
             * @memberof proto.Message.EncReactionMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            EncReactionMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.EncReactionMessage";
            };

            return EncReactionMessage;
        })();

        Message.EventInviteMessage = (function() {

            /**
             * Properties of an EventInviteMessage.
             * @memberof proto.Message
             * @interface IEventInviteMessage
             * @property {proto.IContextInfo|null} [contextInfo] EventInviteMessage contextInfo
             * @property {string|null} [eventId] EventInviteMessage eventId
             * @property {string|null} [eventTitle] EventInviteMessage eventTitle
             * @property {Uint8Array|null} [jpegThumbnail] EventInviteMessage jpegThumbnail
             * @property {number|Long|null} [startTime] EventInviteMessage startTime
             * @property {string|null} [caption] EventInviteMessage caption
             * @property {boolean|null} [isCanceled] EventInviteMessage isCanceled
             * @property {number|Long|null} [endTime] EventInviteMessage endTime
             * @property {string|null} [callLink] EventInviteMessage callLink
             */

            /**
             * Constructs a new EventInviteMessage.
             * @memberof proto.Message
             * @classdesc Represents an EventInviteMessage.
             * @implements IEventInviteMessage
             * @constructor
             * @param {proto.Message.IEventInviteMessage=} [properties] Properties to set
             */
            function EventInviteMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * EventInviteMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.EventInviteMessage
             * @instance
             */
            EventInviteMessage.prototype.contextInfo = null;

            /**
             * EventInviteMessage eventId.
             * @member {string|null|undefined} eventId
             * @memberof proto.Message.EventInviteMessage
             * @instance
             */
            EventInviteMessage.prototype.eventId = null;

            /**
             * EventInviteMessage eventTitle.
             * @member {string|null|undefined} eventTitle
             * @memberof proto.Message.EventInviteMessage
             * @instance
             */
            EventInviteMessage.prototype.eventTitle = null;

            /**
             * EventInviteMessage jpegThumbnail.
             * @member {Uint8Array|null|undefined} jpegThumbnail
             * @memberof proto.Message.EventInviteMessage
             * @instance
             */
            EventInviteMessage.prototype.jpegThumbnail = null;

            /**
             * EventInviteMessage startTime.
             * @member {number|Long|null|undefined} startTime
             * @memberof proto.Message.EventInviteMessage
             * @instance
             */
            EventInviteMessage.prototype.startTime = null;

            /**
             * EventInviteMessage caption.
             * @member {string|null|undefined} caption
             * @memberof proto.Message.EventInviteMessage
             * @instance
             */
            EventInviteMessage.prototype.caption = null;

            /**
             * EventInviteMessage isCanceled.
             * @member {boolean|null|undefined} isCanceled
             * @memberof proto.Message.EventInviteMessage
             * @instance
             */
            EventInviteMessage.prototype.isCanceled = null;

            /**
             * EventInviteMessage endTime.
             * @member {number|Long|null|undefined} endTime
             * @memberof proto.Message.EventInviteMessage
             * @instance
             */
            EventInviteMessage.prototype.endTime = null;

            /**
             * EventInviteMessage callLink.
             * @member {string|null|undefined} callLink
             * @memberof proto.Message.EventInviteMessage
             * @instance
             */
            EventInviteMessage.prototype.callLink = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventInviteMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventInviteMessage.prototype, "_eventId", {
                get: $util.oneOfGetter($oneOfFields = ["eventId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventInviteMessage.prototype, "_eventTitle", {
                get: $util.oneOfGetter($oneOfFields = ["eventTitle"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventInviteMessage.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventInviteMessage.prototype, "_startTime", {
                get: $util.oneOfGetter($oneOfFields = ["startTime"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventInviteMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventInviteMessage.prototype, "_isCanceled", {
                get: $util.oneOfGetter($oneOfFields = ["isCanceled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventInviteMessage.prototype, "_endTime", {
                get: $util.oneOfGetter($oneOfFields = ["endTime"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventInviteMessage.prototype, "_callLink", {
                get: $util.oneOfGetter($oneOfFields = ["callLink"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new EventInviteMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.EventInviteMessage
             * @static
             * @param {proto.Message.IEventInviteMessage=} [properties] Properties to set
             * @returns {proto.Message.EventInviteMessage} EventInviteMessage instance
             */
            EventInviteMessage.create = function create(properties) {
                return new EventInviteMessage(properties);
            };

            /**
             * Encodes the specified EventInviteMessage message. Does not implicitly {@link proto.Message.EventInviteMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.EventInviteMessage
             * @static
             * @param {proto.Message.IEventInviteMessage} message EventInviteMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EventInviteMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.eventId != null && Object.hasOwnProperty.call(message, "eventId"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.eventId);
                if (message.eventTitle != null && Object.hasOwnProperty.call(message, "eventTitle"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.eventTitle);
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.jpegThumbnail);
                if (message.startTime != null && Object.hasOwnProperty.call(message, "startTime"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.startTime);
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.caption);
                if (message.isCanceled != null && Object.hasOwnProperty.call(message, "isCanceled"))
                    writer.uint32(/* id 7, wireType 0 =*/56).bool(message.isCanceled);
                if (message.endTime != null && Object.hasOwnProperty.call(message, "endTime"))
                    writer.uint32(/* id 8, wireType 0 =*/64).int64(message.endTime);
                if (message.callLink != null && Object.hasOwnProperty.call(message, "callLink"))
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.callLink);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified EventInviteMessage message, length delimited. Does not implicitly {@link proto.Message.EventInviteMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.EventInviteMessage
             * @static
             * @param {proto.Message.IEventInviteMessage} message EventInviteMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EventInviteMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an EventInviteMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.EventInviteMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.EventInviteMessage} EventInviteMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EventInviteMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.eventId = reader.stringVerify();
                            message._eventId = "eventId";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.eventTitle = reader.stringVerify();
                            message._eventTitle = "eventTitle";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.jpegThumbnail = reader.bytes();
                            message._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.startTime = reader.int64();
                            message._startTime = "startTime";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.caption = reader.stringVerify();
                            message._caption = "caption";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            message.isCanceled = reader.bool();
                            message._isCanceled = "isCanceled";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            message.endTime = reader.int64();
                            message._endTime = "endTime";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            message.callLink = reader.stringVerify();
                            message._callLink = "callLink";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an EventInviteMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.EventInviteMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.EventInviteMessage} EventInviteMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EventInviteMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an EventInviteMessage message.
             * @function verify
             * @memberof proto.Message.EventInviteMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            EventInviteMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.eventId != null && Object.hasOwnProperty.call(message, "eventId")) {
                    properties._eventId = 1;
                    if (!$util.isString(message.eventId))
                        return "eventId: string expected";
                }
                if (message.eventTitle != null && Object.hasOwnProperty.call(message, "eventTitle")) {
                    properties._eventTitle = 1;
                    if (!$util.isString(message.eventTitle))
                        return "eventTitle: string expected";
                }
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail")) {
                    properties._jpegThumbnail = 1;
                    if (!(message.jpegThumbnail && typeof message.jpegThumbnail.length === "number" || $util.isString(message.jpegThumbnail)))
                        return "jpegThumbnail: buffer expected";
                }
                if (message.startTime != null && Object.hasOwnProperty.call(message, "startTime")) {
                    properties._startTime = 1;
                    if (!$util.isInteger(message.startTime) && !(message.startTime && $util.isInteger(message.startTime.low) && $util.isInteger(message.startTime.high)))
                        return "startTime: integer|Long expected";
                }
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption")) {
                    properties._caption = 1;
                    if (!$util.isString(message.caption))
                        return "caption: string expected";
                }
                if (message.isCanceled != null && Object.hasOwnProperty.call(message, "isCanceled")) {
                    properties._isCanceled = 1;
                    if (typeof message.isCanceled !== "boolean")
                        return "isCanceled: boolean expected";
                }
                if (message.endTime != null && Object.hasOwnProperty.call(message, "endTime")) {
                    properties._endTime = 1;
                    if (!$util.isInteger(message.endTime) && !(message.endTime && $util.isInteger(message.endTime.low) && $util.isInteger(message.endTime.high)))
                        return "endTime: integer|Long expected";
                }
                if (message.callLink != null && Object.hasOwnProperty.call(message, "callLink")) {
                    properties._callLink = 1;
                    if (!$util.isString(message.callLink))
                        return "callLink: string expected";
                }
                return null;
            };

            /**
             * Creates an EventInviteMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.EventInviteMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.EventInviteMessage} EventInviteMessage
             */
            EventInviteMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.EventInviteMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.EventInviteMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                if (object.eventId != null)
                    message.eventId = String(object.eventId);
                if (object.eventTitle != null)
                    message.eventTitle = String(object.eventTitle);
                if (object.jpegThumbnail != null)
                    if (typeof object.jpegThumbnail === "string")
                        $util.base64.decode(object.jpegThumbnail, message.jpegThumbnail = $util.newBuffer($util.base64.length(object.jpegThumbnail)), 0);
                    else if (object.jpegThumbnail.length >= 0)
                        message.jpegThumbnail = object.jpegThumbnail;
                if (object.startTime != null)
                    if ($util.Long)
                        message.startTime = $util.Long.fromValue(object.startTime, false);
                    else if (typeof object.startTime === "string")
                        message.startTime = parseInt(object.startTime, 10);
                    else if (typeof object.startTime === "number")
                        message.startTime = object.startTime;
                    else if (typeof object.startTime === "object")
                        message.startTime = new $util.LongBits(object.startTime.low >>> 0, object.startTime.high >>> 0).toNumber();
                if (object.caption != null)
                    message.caption = String(object.caption);
                if (object.isCanceled != null)
                    message.isCanceled = Boolean(object.isCanceled);
                if (object.endTime != null)
                    if ($util.Long)
                        message.endTime = $util.Long.fromValue(object.endTime, false);
                    else if (typeof object.endTime === "string")
                        message.endTime = parseInt(object.endTime, 10);
                    else if (typeof object.endTime === "number")
                        message.endTime = object.endTime;
                    else if (typeof object.endTime === "object")
                        message.endTime = new $util.LongBits(object.endTime.low >>> 0, object.endTime.high >>> 0).toNumber();
                if (object.callLink != null)
                    message.callLink = String(object.callLink);
                return message;
            };

            /**
             * Creates a plain object from an EventInviteMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.EventInviteMessage
             * @static
             * @param {proto.Message.EventInviteMessage} message EventInviteMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            EventInviteMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.eventId != null && Object.hasOwnProperty.call(message, "eventId"))
                    object.eventId = message.eventId;
                if (message.eventTitle != null && Object.hasOwnProperty.call(message, "eventTitle"))
                    object.eventTitle = message.eventTitle;
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    object.jpegThumbnail = options.bytes === String ? $util.base64.encode(message.jpegThumbnail, 0, message.jpegThumbnail.length) : options.bytes === Array ? Array.prototype.slice.call(message.jpegThumbnail) : message.jpegThumbnail;
                if (message.startTime != null && Object.hasOwnProperty.call(message, "startTime"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.startTime = typeof message.startTime === "number" ? BigInt(message.startTime) : $util.Long.fromBits(message.startTime.low >>> 0, message.startTime.high >>> 0, false).toBigInt();
                    else if (typeof message.startTime === "number")
                        object.startTime = options.longs === String ? String(message.startTime) : message.startTime;
                    else
                        object.startTime = options.longs === String ? $util.Long.prototype.toString.call(message.startTime) : options.longs === Number ? new $util.LongBits(message.startTime.low >>> 0, message.startTime.high >>> 0).toNumber() : message.startTime;
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    object.caption = message.caption;
                if (message.isCanceled != null && Object.hasOwnProperty.call(message, "isCanceled"))
                    object.isCanceled = message.isCanceled;
                if (message.endTime != null && Object.hasOwnProperty.call(message, "endTime"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.endTime = typeof message.endTime === "number" ? BigInt(message.endTime) : $util.Long.fromBits(message.endTime.low >>> 0, message.endTime.high >>> 0, false).toBigInt();
                    else if (typeof message.endTime === "number")
                        object.endTime = options.longs === String ? String(message.endTime) : message.endTime;
                    else
                        object.endTime = options.longs === String ? $util.Long.prototype.toString.call(message.endTime) : options.longs === Number ? new $util.LongBits(message.endTime.low >>> 0, message.endTime.high >>> 0).toNumber() : message.endTime;
                if (message.callLink != null && Object.hasOwnProperty.call(message, "callLink"))
                    object.callLink = message.callLink;
                return object;
            };

            /**
             * Converts this EventInviteMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.EventInviteMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            EventInviteMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for EventInviteMessage
             * @function getTypeUrl
             * @memberof proto.Message.EventInviteMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            EventInviteMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.EventInviteMessage";
            };

            return EventInviteMessage;
        })();

        Message.EventMessage = (function() {

            /**
             * Properties of an EventMessage.
             * @memberof proto.Message
             * @interface IEventMessage
             * @property {proto.IContextInfo|null} [contextInfo] EventMessage contextInfo
             * @property {boolean|null} [isCanceled] EventMessage isCanceled
             * @property {string|null} [name] EventMessage name
             * @property {string|null} [description] EventMessage description
             * @property {proto.Message.ILocationMessage|null} [location] EventMessage location
             * @property {string|null} [joinLink] EventMessage joinLink
             * @property {number|Long|null} [startTime] EventMessage startTime
             * @property {number|Long|null} [endTime] EventMessage endTime
             * @property {boolean|null} [extraGuestsAllowed] EventMessage extraGuestsAllowed
             * @property {boolean|null} [isScheduleCall] EventMessage isScheduleCall
             * @property {boolean|null} [hasReminder] EventMessage hasReminder
             * @property {number|Long|null} [reminderOffsetSec] EventMessage reminderOffsetSec
             */

            /**
             * Constructs a new EventMessage.
             * @memberof proto.Message
             * @classdesc Represents an EventMessage.
             * @implements IEventMessage
             * @constructor
             * @param {proto.Message.IEventMessage=} [properties] Properties to set
             */
            function EventMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * EventMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.EventMessage
             * @instance
             */
            EventMessage.prototype.contextInfo = null;

            /**
             * EventMessage isCanceled.
             * @member {boolean|null|undefined} isCanceled
             * @memberof proto.Message.EventMessage
             * @instance
             */
            EventMessage.prototype.isCanceled = null;

            /**
             * EventMessage name.
             * @member {string|null|undefined} name
             * @memberof proto.Message.EventMessage
             * @instance
             */
            EventMessage.prototype.name = null;

            /**
             * EventMessage description.
             * @member {string|null|undefined} description
             * @memberof proto.Message.EventMessage
             * @instance
             */
            EventMessage.prototype.description = null;

            /**
             * EventMessage location.
             * @member {proto.Message.ILocationMessage|null|undefined} location
             * @memberof proto.Message.EventMessage
             * @instance
             */
            EventMessage.prototype.location = null;

            /**
             * EventMessage joinLink.
             * @member {string|null|undefined} joinLink
             * @memberof proto.Message.EventMessage
             * @instance
             */
            EventMessage.prototype.joinLink = null;

            /**
             * EventMessage startTime.
             * @member {number|Long|null|undefined} startTime
             * @memberof proto.Message.EventMessage
             * @instance
             */
            EventMessage.prototype.startTime = null;

            /**
             * EventMessage endTime.
             * @member {number|Long|null|undefined} endTime
             * @memberof proto.Message.EventMessage
             * @instance
             */
            EventMessage.prototype.endTime = null;

            /**
             * EventMessage extraGuestsAllowed.
             * @member {boolean|null|undefined} extraGuestsAllowed
             * @memberof proto.Message.EventMessage
             * @instance
             */
            EventMessage.prototype.extraGuestsAllowed = null;

            /**
             * EventMessage isScheduleCall.
             * @member {boolean|null|undefined} isScheduleCall
             * @memberof proto.Message.EventMessage
             * @instance
             */
            EventMessage.prototype.isScheduleCall = null;

            /**
             * EventMessage hasReminder.
             * @member {boolean|null|undefined} hasReminder
             * @memberof proto.Message.EventMessage
             * @instance
             */
            EventMessage.prototype.hasReminder = null;

            /**
             * EventMessage reminderOffsetSec.
             * @member {number|Long|null|undefined} reminderOffsetSec
             * @memberof proto.Message.EventMessage
             * @instance
             */
            EventMessage.prototype.reminderOffsetSec = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventMessage.prototype, "_isCanceled", {
                get: $util.oneOfGetter($oneOfFields = ["isCanceled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventMessage.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventMessage.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventMessage.prototype, "_location", {
                get: $util.oneOfGetter($oneOfFields = ["location"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventMessage.prototype, "_joinLink", {
                get: $util.oneOfGetter($oneOfFields = ["joinLink"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventMessage.prototype, "_startTime", {
                get: $util.oneOfGetter($oneOfFields = ["startTime"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventMessage.prototype, "_endTime", {
                get: $util.oneOfGetter($oneOfFields = ["endTime"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventMessage.prototype, "_extraGuestsAllowed", {
                get: $util.oneOfGetter($oneOfFields = ["extraGuestsAllowed"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventMessage.prototype, "_isScheduleCall", {
                get: $util.oneOfGetter($oneOfFields = ["isScheduleCall"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventMessage.prototype, "_hasReminder", {
                get: $util.oneOfGetter($oneOfFields = ["hasReminder"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventMessage.prototype, "_reminderOffsetSec", {
                get: $util.oneOfGetter($oneOfFields = ["reminderOffsetSec"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new EventMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.EventMessage
             * @static
             * @param {proto.Message.IEventMessage=} [properties] Properties to set
             * @returns {proto.Message.EventMessage} EventMessage instance
             */
            EventMessage.create = function create(properties) {
                return new EventMessage(properties);
            };

            /**
             * Encodes the specified EventMessage message. Does not implicitly {@link proto.Message.EventMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.EventMessage
             * @static
             * @param {proto.Message.IEventMessage} message EventMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EventMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.isCanceled != null && Object.hasOwnProperty.call(message, "isCanceled"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.isCanceled);
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.name);
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.description);
                if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                    $root.proto.Message.LocationMessage.encode(message.location, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                if (message.joinLink != null && Object.hasOwnProperty.call(message, "joinLink"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.joinLink);
                if (message.startTime != null && Object.hasOwnProperty.call(message, "startTime"))
                    writer.uint32(/* id 7, wireType 0 =*/56).int64(message.startTime);
                if (message.endTime != null && Object.hasOwnProperty.call(message, "endTime"))
                    writer.uint32(/* id 8, wireType 0 =*/64).int64(message.endTime);
                if (message.extraGuestsAllowed != null && Object.hasOwnProperty.call(message, "extraGuestsAllowed"))
                    writer.uint32(/* id 9, wireType 0 =*/72).bool(message.extraGuestsAllowed);
                if (message.isScheduleCall != null && Object.hasOwnProperty.call(message, "isScheduleCall"))
                    writer.uint32(/* id 10, wireType 0 =*/80).bool(message.isScheduleCall);
                if (message.hasReminder != null && Object.hasOwnProperty.call(message, "hasReminder"))
                    writer.uint32(/* id 11, wireType 0 =*/88).bool(message.hasReminder);
                if (message.reminderOffsetSec != null && Object.hasOwnProperty.call(message, "reminderOffsetSec"))
                    writer.uint32(/* id 12, wireType 0 =*/96).int64(message.reminderOffsetSec);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified EventMessage message, length delimited. Does not implicitly {@link proto.Message.EventMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.EventMessage
             * @static
             * @param {proto.Message.IEventMessage} message EventMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EventMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an EventMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.EventMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.EventMessage} EventMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EventMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.isCanceled = reader.bool();
                            message._isCanceled = "isCanceled";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.description = reader.stringVerify();
                            message._description = "description";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.location = $root.proto.Message.LocationMessage.decode(reader, reader.uint32(), undefined, q + 1, message.location);
                            message._location = "location";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.joinLink = reader.stringVerify();
                            message._joinLink = "joinLink";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            message.startTime = reader.int64();
                            message._startTime = "startTime";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            message.endTime = reader.int64();
                            message._endTime = "endTime";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            message.extraGuestsAllowed = reader.bool();
                            message._extraGuestsAllowed = "extraGuestsAllowed";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            message.isScheduleCall = reader.bool();
                            message._isScheduleCall = "isScheduleCall";
                            continue;
                        }
                    case 11: {
                            if (u !== 0)
                                break;
                            message.hasReminder = reader.bool();
                            message._hasReminder = "hasReminder";
                            continue;
                        }
                    case 12: {
                            if (u !== 0)
                                break;
                            message.reminderOffsetSec = reader.int64();
                            message._reminderOffsetSec = "reminderOffsetSec";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an EventMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.EventMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.EventMessage} EventMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EventMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an EventMessage message.
             * @function verify
             * @memberof proto.Message.EventMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            EventMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.isCanceled != null && Object.hasOwnProperty.call(message, "isCanceled")) {
                    properties._isCanceled = 1;
                    if (typeof message.isCanceled !== "boolean")
                        return "isCanceled: boolean expected";
                }
                if (message.name != null && Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                if (message.description != null && Object.hasOwnProperty.call(message, "description")) {
                    properties._description = 1;
                    if (!$util.isString(message.description))
                        return "description: string expected";
                }
                if (message.location != null && Object.hasOwnProperty.call(message, "location")) {
                    properties._location = 1;
                    {
                        var error = $root.proto.Message.LocationMessage.verify(message.location, q + 1);
                        if (error)
                            return "location." + error;
                    }
                }
                if (message.joinLink != null && Object.hasOwnProperty.call(message, "joinLink")) {
                    properties._joinLink = 1;
                    if (!$util.isString(message.joinLink))
                        return "joinLink: string expected";
                }
                if (message.startTime != null && Object.hasOwnProperty.call(message, "startTime")) {
                    properties._startTime = 1;
                    if (!$util.isInteger(message.startTime) && !(message.startTime && $util.isInteger(message.startTime.low) && $util.isInteger(message.startTime.high)))
                        return "startTime: integer|Long expected";
                }
                if (message.endTime != null && Object.hasOwnProperty.call(message, "endTime")) {
                    properties._endTime = 1;
                    if (!$util.isInteger(message.endTime) && !(message.endTime && $util.isInteger(message.endTime.low) && $util.isInteger(message.endTime.high)))
                        return "endTime: integer|Long expected";
                }
                if (message.extraGuestsAllowed != null && Object.hasOwnProperty.call(message, "extraGuestsAllowed")) {
                    properties._extraGuestsAllowed = 1;
                    if (typeof message.extraGuestsAllowed !== "boolean")
                        return "extraGuestsAllowed: boolean expected";
                }
                if (message.isScheduleCall != null && Object.hasOwnProperty.call(message, "isScheduleCall")) {
                    properties._isScheduleCall = 1;
                    if (typeof message.isScheduleCall !== "boolean")
                        return "isScheduleCall: boolean expected";
                }
                if (message.hasReminder != null && Object.hasOwnProperty.call(message, "hasReminder")) {
                    properties._hasReminder = 1;
                    if (typeof message.hasReminder !== "boolean")
                        return "hasReminder: boolean expected";
                }
                if (message.reminderOffsetSec != null && Object.hasOwnProperty.call(message, "reminderOffsetSec")) {
                    properties._reminderOffsetSec = 1;
                    if (!$util.isInteger(message.reminderOffsetSec) && !(message.reminderOffsetSec && $util.isInteger(message.reminderOffsetSec.low) && $util.isInteger(message.reminderOffsetSec.high)))
                        return "reminderOffsetSec: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates an EventMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.EventMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.EventMessage} EventMessage
             */
            EventMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.EventMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.EventMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                if (object.isCanceled != null)
                    message.isCanceled = Boolean(object.isCanceled);
                if (object.name != null)
                    message.name = String(object.name);
                if (object.description != null)
                    message.description = String(object.description);
                if (object.location != null) {
                    if (!$util.isObject(object.location))
                        throw TypeError(".proto.Message.EventMessage.location: object expected");
                    message.location = $root.proto.Message.LocationMessage.fromObject(object.location, q + 1);
                }
                if (object.joinLink != null)
                    message.joinLink = String(object.joinLink);
                if (object.startTime != null)
                    if ($util.Long)
                        message.startTime = $util.Long.fromValue(object.startTime, false);
                    else if (typeof object.startTime === "string")
                        message.startTime = parseInt(object.startTime, 10);
                    else if (typeof object.startTime === "number")
                        message.startTime = object.startTime;
                    else if (typeof object.startTime === "object")
                        message.startTime = new $util.LongBits(object.startTime.low >>> 0, object.startTime.high >>> 0).toNumber();
                if (object.endTime != null)
                    if ($util.Long)
                        message.endTime = $util.Long.fromValue(object.endTime, false);
                    else if (typeof object.endTime === "string")
                        message.endTime = parseInt(object.endTime, 10);
                    else if (typeof object.endTime === "number")
                        message.endTime = object.endTime;
                    else if (typeof object.endTime === "object")
                        message.endTime = new $util.LongBits(object.endTime.low >>> 0, object.endTime.high >>> 0).toNumber();
                if (object.extraGuestsAllowed != null)
                    message.extraGuestsAllowed = Boolean(object.extraGuestsAllowed);
                if (object.isScheduleCall != null)
                    message.isScheduleCall = Boolean(object.isScheduleCall);
                if (object.hasReminder != null)
                    message.hasReminder = Boolean(object.hasReminder);
                if (object.reminderOffsetSec != null)
                    if ($util.Long)
                        message.reminderOffsetSec = $util.Long.fromValue(object.reminderOffsetSec, false);
                    else if (typeof object.reminderOffsetSec === "string")
                        message.reminderOffsetSec = parseInt(object.reminderOffsetSec, 10);
                    else if (typeof object.reminderOffsetSec === "number")
                        message.reminderOffsetSec = object.reminderOffsetSec;
                    else if (typeof object.reminderOffsetSec === "object")
                        message.reminderOffsetSec = new $util.LongBits(object.reminderOffsetSec.low >>> 0, object.reminderOffsetSec.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from an EventMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.EventMessage
             * @static
             * @param {proto.Message.EventMessage} message EventMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            EventMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.isCanceled != null && Object.hasOwnProperty.call(message, "isCanceled"))
                    object.isCanceled = message.isCanceled;
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    object.description = message.description;
                if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                    object.location = $root.proto.Message.LocationMessage.toObject(message.location, options, q + 1);
                if (message.joinLink != null && Object.hasOwnProperty.call(message, "joinLink"))
                    object.joinLink = message.joinLink;
                if (message.startTime != null && Object.hasOwnProperty.call(message, "startTime"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.startTime = typeof message.startTime === "number" ? BigInt(message.startTime) : $util.Long.fromBits(message.startTime.low >>> 0, message.startTime.high >>> 0, false).toBigInt();
                    else if (typeof message.startTime === "number")
                        object.startTime = options.longs === String ? String(message.startTime) : message.startTime;
                    else
                        object.startTime = options.longs === String ? $util.Long.prototype.toString.call(message.startTime) : options.longs === Number ? new $util.LongBits(message.startTime.low >>> 0, message.startTime.high >>> 0).toNumber() : message.startTime;
                if (message.endTime != null && Object.hasOwnProperty.call(message, "endTime"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.endTime = typeof message.endTime === "number" ? BigInt(message.endTime) : $util.Long.fromBits(message.endTime.low >>> 0, message.endTime.high >>> 0, false).toBigInt();
                    else if (typeof message.endTime === "number")
                        object.endTime = options.longs === String ? String(message.endTime) : message.endTime;
                    else
                        object.endTime = options.longs === String ? $util.Long.prototype.toString.call(message.endTime) : options.longs === Number ? new $util.LongBits(message.endTime.low >>> 0, message.endTime.high >>> 0).toNumber() : message.endTime;
                if (message.extraGuestsAllowed != null && Object.hasOwnProperty.call(message, "extraGuestsAllowed"))
                    object.extraGuestsAllowed = message.extraGuestsAllowed;
                if (message.isScheduleCall != null && Object.hasOwnProperty.call(message, "isScheduleCall"))
                    object.isScheduleCall = message.isScheduleCall;
                if (message.hasReminder != null && Object.hasOwnProperty.call(message, "hasReminder"))
                    object.hasReminder = message.hasReminder;
                if (message.reminderOffsetSec != null && Object.hasOwnProperty.call(message, "reminderOffsetSec"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.reminderOffsetSec = typeof message.reminderOffsetSec === "number" ? BigInt(message.reminderOffsetSec) : $util.Long.fromBits(message.reminderOffsetSec.low >>> 0, message.reminderOffsetSec.high >>> 0, false).toBigInt();
                    else if (typeof message.reminderOffsetSec === "number")
                        object.reminderOffsetSec = options.longs === String ? String(message.reminderOffsetSec) : message.reminderOffsetSec;
                    else
                        object.reminderOffsetSec = options.longs === String ? $util.Long.prototype.toString.call(message.reminderOffsetSec) : options.longs === Number ? new $util.LongBits(message.reminderOffsetSec.low >>> 0, message.reminderOffsetSec.high >>> 0).toNumber() : message.reminderOffsetSec;
                return object;
            };

            /**
             * Converts this EventMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.EventMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            EventMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for EventMessage
             * @function getTypeUrl
             * @memberof proto.Message.EventMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            EventMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.EventMessage";
            };

            return EventMessage;
        })();

        Message.EventResponseMessage = (function() {

            /**
             * Properties of an EventResponseMessage.
             * @memberof proto.Message
             * @interface IEventResponseMessage
             * @property {proto.Message.EventResponseMessage.EventResponseType|null} [response] EventResponseMessage response
             * @property {number|Long|null} [timestampMs] EventResponseMessage timestampMs
             * @property {number|null} [extraGuestCount] EventResponseMessage extraGuestCount
             */

            /**
             * Constructs a new EventResponseMessage.
             * @memberof proto.Message
             * @classdesc Represents an EventResponseMessage.
             * @implements IEventResponseMessage
             * @constructor
             * @param {proto.Message.IEventResponseMessage=} [properties] Properties to set
             */
            function EventResponseMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * EventResponseMessage response.
             * @member {proto.Message.EventResponseMessage.EventResponseType|null|undefined} response
             * @memberof proto.Message.EventResponseMessage
             * @instance
             */
            EventResponseMessage.prototype.response = null;

            /**
             * EventResponseMessage timestampMs.
             * @member {number|Long|null|undefined} timestampMs
             * @memberof proto.Message.EventResponseMessage
             * @instance
             */
            EventResponseMessage.prototype.timestampMs = null;

            /**
             * EventResponseMessage extraGuestCount.
             * @member {number|null|undefined} extraGuestCount
             * @memberof proto.Message.EventResponseMessage
             * @instance
             */
            EventResponseMessage.prototype.extraGuestCount = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventResponseMessage.prototype, "_response", {
                get: $util.oneOfGetter($oneOfFields = ["response"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventResponseMessage.prototype, "_timestampMs", {
                get: $util.oneOfGetter($oneOfFields = ["timestampMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventResponseMessage.prototype, "_extraGuestCount", {
                get: $util.oneOfGetter($oneOfFields = ["extraGuestCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new EventResponseMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.EventResponseMessage
             * @static
             * @param {proto.Message.IEventResponseMessage=} [properties] Properties to set
             * @returns {proto.Message.EventResponseMessage} EventResponseMessage instance
             */
            EventResponseMessage.create = function create(properties) {
                return new EventResponseMessage(properties);
            };

            /**
             * Encodes the specified EventResponseMessage message. Does not implicitly {@link proto.Message.EventResponseMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.EventResponseMessage
             * @static
             * @param {proto.Message.IEventResponseMessage} message EventResponseMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EventResponseMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.response != null && Object.hasOwnProperty.call(message, "response"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.response);
                if (message.timestampMs != null && Object.hasOwnProperty.call(message, "timestampMs"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.timestampMs);
                if (message.extraGuestCount != null && Object.hasOwnProperty.call(message, "extraGuestCount"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.extraGuestCount);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified EventResponseMessage message, length delimited. Does not implicitly {@link proto.Message.EventResponseMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.EventResponseMessage
             * @static
             * @param {proto.Message.IEventResponseMessage} message EventResponseMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EventResponseMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an EventResponseMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.EventResponseMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.EventResponseMessage} EventResponseMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EventResponseMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.response = reader.int32();
                            message._response = "response";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.timestampMs = reader.int64();
                            message._timestampMs = "timestampMs";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.extraGuestCount = reader.int32();
                            message._extraGuestCount = "extraGuestCount";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an EventResponseMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.EventResponseMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.EventResponseMessage} EventResponseMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EventResponseMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an EventResponseMessage message.
             * @function verify
             * @memberof proto.Message.EventResponseMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            EventResponseMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.response != null && Object.hasOwnProperty.call(message, "response")) {
                    properties._response = 1;
                    if (typeof message.response !== "number" || (message.response | 0) !== message.response)
                        return "response: enum value expected";
                }
                if (message.timestampMs != null && Object.hasOwnProperty.call(message, "timestampMs")) {
                    properties._timestampMs = 1;
                    if (!$util.isInteger(message.timestampMs) && !(message.timestampMs && $util.isInteger(message.timestampMs.low) && $util.isInteger(message.timestampMs.high)))
                        return "timestampMs: integer|Long expected";
                }
                if (message.extraGuestCount != null && Object.hasOwnProperty.call(message, "extraGuestCount")) {
                    properties._extraGuestCount = 1;
                    if (!$util.isInteger(message.extraGuestCount))
                        return "extraGuestCount: integer expected";
                }
                return null;
            };

            /**
             * Creates an EventResponseMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.EventResponseMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.EventResponseMessage} EventResponseMessage
             */
            EventResponseMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.EventResponseMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.response) {
                case "UNKNOWN":
                case 0:
                    message.response = 0;
                    break;
                case "GOING":
                case 1:
                    message.response = 1;
                    break;
                case "NOT_GOING":
                case 2:
                    message.response = 2;
                    break;
                case "MAYBE":
                case 3:
                    message.response = 3;
                    break;
                default:
                    if (typeof object.response === "number" && (object.response | 0) === object.response)
                        message.response = object.response;
                }
                if (object.timestampMs != null)
                    if ($util.Long)
                        message.timestampMs = $util.Long.fromValue(object.timestampMs, false);
                    else if (typeof object.timestampMs === "string")
                        message.timestampMs = parseInt(object.timestampMs, 10);
                    else if (typeof object.timestampMs === "number")
                        message.timestampMs = object.timestampMs;
                    else if (typeof object.timestampMs === "object")
                        message.timestampMs = new $util.LongBits(object.timestampMs.low >>> 0, object.timestampMs.high >>> 0).toNumber();
                if (object.extraGuestCount != null)
                    message.extraGuestCount = object.extraGuestCount | 0;
                return message;
            };

            /**
             * Creates a plain object from an EventResponseMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.EventResponseMessage
             * @static
             * @param {proto.Message.EventResponseMessage} message EventResponseMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            EventResponseMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.response != null && Object.hasOwnProperty.call(message, "response"))
                    object.response = options.enums === String ? $root.proto.Message.EventResponseMessage.EventResponseType[message.response] === undefined ? message.response : $root.proto.Message.EventResponseMessage.EventResponseType[message.response] : message.response;
                if (message.timestampMs != null && Object.hasOwnProperty.call(message, "timestampMs"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.timestampMs = typeof message.timestampMs === "number" ? BigInt(message.timestampMs) : $util.Long.fromBits(message.timestampMs.low >>> 0, message.timestampMs.high >>> 0, false).toBigInt();
                    else if (typeof message.timestampMs === "number")
                        object.timestampMs = options.longs === String ? String(message.timestampMs) : message.timestampMs;
                    else
                        object.timestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.timestampMs) : options.longs === Number ? new $util.LongBits(message.timestampMs.low >>> 0, message.timestampMs.high >>> 0).toNumber() : message.timestampMs;
                if (message.extraGuestCount != null && Object.hasOwnProperty.call(message, "extraGuestCount"))
                    object.extraGuestCount = message.extraGuestCount;
                return object;
            };

            /**
             * Converts this EventResponseMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.EventResponseMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            EventResponseMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for EventResponseMessage
             * @function getTypeUrl
             * @memberof proto.Message.EventResponseMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            EventResponseMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.EventResponseMessage";
            };

            /**
             * EventResponseType enum.
             * @name proto.Message.EventResponseMessage.EventResponseType
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} GOING=1 GOING value
             * @property {number} NOT_GOING=2 NOT_GOING value
             * @property {number} MAYBE=3 MAYBE value
             */
            EventResponseMessage.EventResponseType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "GOING"] = 1;
                values[valuesById[2] = "NOT_GOING"] = 2;
                values[valuesById[3] = "MAYBE"] = 3;
                return values;
            })();

            return EventResponseMessage;
        })();

        Message.ExtendedTextMessage = (function() {

            /**
             * Properties of an ExtendedTextMessage.
             * @memberof proto.Message
             * @interface IExtendedTextMessage
             * @property {string|null} [text] ExtendedTextMessage text
             * @property {string|null} [matchedText] ExtendedTextMessage matchedText
             * @property {string|null} [description] ExtendedTextMessage description
             * @property {string|null} [title] ExtendedTextMessage title
             * @property {number|null} [textArgb] ExtendedTextMessage textArgb
             * @property {number|null} [backgroundArgb] ExtendedTextMessage backgroundArgb
             * @property {proto.Message.ExtendedTextMessage.FontType|null} [font] ExtendedTextMessage font
             * @property {proto.Message.ExtendedTextMessage.PreviewType|null} [previewType] ExtendedTextMessage previewType
             * @property {Uint8Array|null} [jpegThumbnail] ExtendedTextMessage jpegThumbnail
             * @property {proto.IContextInfo|null} [contextInfo] ExtendedTextMessage contextInfo
             * @property {boolean|null} [doNotPlayInline] ExtendedTextMessage doNotPlayInline
             * @property {string|null} [thumbnailDirectPath] ExtendedTextMessage thumbnailDirectPath
             * @property {Uint8Array|null} [thumbnailSha256] ExtendedTextMessage thumbnailSha256
             * @property {Uint8Array|null} [thumbnailEncSha256] ExtendedTextMessage thumbnailEncSha256
             * @property {Uint8Array|null} [mediaKey] ExtendedTextMessage mediaKey
             * @property {number|Long|null} [mediaKeyTimestamp] ExtendedTextMessage mediaKeyTimestamp
             * @property {number|null} [thumbnailHeight] ExtendedTextMessage thumbnailHeight
             * @property {number|null} [thumbnailWidth] ExtendedTextMessage thumbnailWidth
             * @property {proto.Message.ExtendedTextMessage.InviteLinkGroupType|null} [inviteLinkGroupType] ExtendedTextMessage inviteLinkGroupType
             * @property {string|null} [inviteLinkParentGroupSubjectV2] ExtendedTextMessage inviteLinkParentGroupSubjectV2
             * @property {Uint8Array|null} [inviteLinkParentGroupThumbnailV2] ExtendedTextMessage inviteLinkParentGroupThumbnailV2
             * @property {proto.Message.ExtendedTextMessage.InviteLinkGroupType|null} [inviteLinkGroupTypeV2] ExtendedTextMessage inviteLinkGroupTypeV2
             * @property {boolean|null} [viewOnce] ExtendedTextMessage viewOnce
             * @property {number|null} [videoHeight] ExtendedTextMessage videoHeight
             * @property {number|null} [videoWidth] ExtendedTextMessage videoWidth
             * @property {proto.Message.IMMSThumbnailMetadata|null} [faviconMMSMetadata] ExtendedTextMessage faviconMMSMetadata
             * @property {proto.Message.ILinkPreviewMetadata|null} [linkPreviewMetadata] ExtendedTextMessage linkPreviewMetadata
             * @property {proto.Message.IPaymentLinkMetadata|null} [paymentLinkMetadata] ExtendedTextMessage paymentLinkMetadata
             * @property {Array.<proto.Message.IVideoEndCard>|null} [endCardTiles] ExtendedTextMessage endCardTiles
             * @property {string|null} [videoContentUrl] ExtendedTextMessage videoContentUrl
             * @property {proto.IEmbeddedMusic|null} [musicMetadata] ExtendedTextMessage musicMetadata
             * @property {proto.Message.IPaymentExtendedMetadata|null} [paymentExtendedMetadata] ExtendedTextMessage paymentExtendedMetadata
             */

            /**
             * Constructs a new ExtendedTextMessage.
             * @memberof proto.Message
             * @classdesc Represents an ExtendedTextMessage.
             * @implements IExtendedTextMessage
             * @constructor
             * @param {proto.Message.IExtendedTextMessage=} [properties] Properties to set
             */
            function ExtendedTextMessage(properties) {
                this.endCardTiles = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ExtendedTextMessage text.
             * @member {string|null|undefined} text
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.text = null;

            /**
             * ExtendedTextMessage matchedText.
             * @member {string|null|undefined} matchedText
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.matchedText = null;

            /**
             * ExtendedTextMessage description.
             * @member {string|null|undefined} description
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.description = null;

            /**
             * ExtendedTextMessage title.
             * @member {string|null|undefined} title
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.title = null;

            /**
             * ExtendedTextMessage textArgb.
             * @member {number|null|undefined} textArgb
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.textArgb = null;

            /**
             * ExtendedTextMessage backgroundArgb.
             * @member {number|null|undefined} backgroundArgb
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.backgroundArgb = null;

            /**
             * ExtendedTextMessage font.
             * @member {proto.Message.ExtendedTextMessage.FontType|null|undefined} font
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.font = null;

            /**
             * ExtendedTextMessage previewType.
             * @member {proto.Message.ExtendedTextMessage.PreviewType|null|undefined} previewType
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.previewType = null;

            /**
             * ExtendedTextMessage jpegThumbnail.
             * @member {Uint8Array|null|undefined} jpegThumbnail
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.jpegThumbnail = null;

            /**
             * ExtendedTextMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.contextInfo = null;

            /**
             * ExtendedTextMessage doNotPlayInline.
             * @member {boolean|null|undefined} doNotPlayInline
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.doNotPlayInline = null;

            /**
             * ExtendedTextMessage thumbnailDirectPath.
             * @member {string|null|undefined} thumbnailDirectPath
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.thumbnailDirectPath = null;

            /**
             * ExtendedTextMessage thumbnailSha256.
             * @member {Uint8Array|null|undefined} thumbnailSha256
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.thumbnailSha256 = null;

            /**
             * ExtendedTextMessage thumbnailEncSha256.
             * @member {Uint8Array|null|undefined} thumbnailEncSha256
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.thumbnailEncSha256 = null;

            /**
             * ExtendedTextMessage mediaKey.
             * @member {Uint8Array|null|undefined} mediaKey
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.mediaKey = null;

            /**
             * ExtendedTextMessage mediaKeyTimestamp.
             * @member {number|Long|null|undefined} mediaKeyTimestamp
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.mediaKeyTimestamp = null;

            /**
             * ExtendedTextMessage thumbnailHeight.
             * @member {number|null|undefined} thumbnailHeight
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.thumbnailHeight = null;

            /**
             * ExtendedTextMessage thumbnailWidth.
             * @member {number|null|undefined} thumbnailWidth
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.thumbnailWidth = null;

            /**
             * ExtendedTextMessage inviteLinkGroupType.
             * @member {proto.Message.ExtendedTextMessage.InviteLinkGroupType|null|undefined} inviteLinkGroupType
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.inviteLinkGroupType = null;

            /**
             * ExtendedTextMessage inviteLinkParentGroupSubjectV2.
             * @member {string|null|undefined} inviteLinkParentGroupSubjectV2
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.inviteLinkParentGroupSubjectV2 = null;

            /**
             * ExtendedTextMessage inviteLinkParentGroupThumbnailV2.
             * @member {Uint8Array|null|undefined} inviteLinkParentGroupThumbnailV2
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.inviteLinkParentGroupThumbnailV2 = null;

            /**
             * ExtendedTextMessage inviteLinkGroupTypeV2.
             * @member {proto.Message.ExtendedTextMessage.InviteLinkGroupType|null|undefined} inviteLinkGroupTypeV2
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.inviteLinkGroupTypeV2 = null;

            /**
             * ExtendedTextMessage viewOnce.
             * @member {boolean|null|undefined} viewOnce
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.viewOnce = null;

            /**
             * ExtendedTextMessage videoHeight.
             * @member {number|null|undefined} videoHeight
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.videoHeight = null;

            /**
             * ExtendedTextMessage videoWidth.
             * @member {number|null|undefined} videoWidth
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.videoWidth = null;

            /**
             * ExtendedTextMessage faviconMMSMetadata.
             * @member {proto.Message.IMMSThumbnailMetadata|null|undefined} faviconMMSMetadata
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.faviconMMSMetadata = null;

            /**
             * ExtendedTextMessage linkPreviewMetadata.
             * @member {proto.Message.ILinkPreviewMetadata|null|undefined} linkPreviewMetadata
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.linkPreviewMetadata = null;

            /**
             * ExtendedTextMessage paymentLinkMetadata.
             * @member {proto.Message.IPaymentLinkMetadata|null|undefined} paymentLinkMetadata
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.paymentLinkMetadata = null;

            /**
             * ExtendedTextMessage endCardTiles.
             * @member {Array.<proto.Message.IVideoEndCard>} endCardTiles
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.endCardTiles = $util.emptyArray;

            /**
             * ExtendedTextMessage videoContentUrl.
             * @member {string|null|undefined} videoContentUrl
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.videoContentUrl = null;

            /**
             * ExtendedTextMessage musicMetadata.
             * @member {proto.IEmbeddedMusic|null|undefined} musicMetadata
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.musicMetadata = null;

            /**
             * ExtendedTextMessage paymentExtendedMetadata.
             * @member {proto.Message.IPaymentExtendedMetadata|null|undefined} paymentExtendedMetadata
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             */
            ExtendedTextMessage.prototype.paymentExtendedMetadata = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_matchedText", {
                get: $util.oneOfGetter($oneOfFields = ["matchedText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_textArgb", {
                get: $util.oneOfGetter($oneOfFields = ["textArgb"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_backgroundArgb", {
                get: $util.oneOfGetter($oneOfFields = ["backgroundArgb"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_font", {
                get: $util.oneOfGetter($oneOfFields = ["font"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_previewType", {
                get: $util.oneOfGetter($oneOfFields = ["previewType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_doNotPlayInline", {
                get: $util.oneOfGetter($oneOfFields = ["doNotPlayInline"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_thumbnailDirectPath", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailDirectPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_thumbnailSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_thumbnailEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_thumbnailHeight", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailHeight"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_thumbnailWidth", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailWidth"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_inviteLinkGroupType", {
                get: $util.oneOfGetter($oneOfFields = ["inviteLinkGroupType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_inviteLinkParentGroupSubjectV2", {
                get: $util.oneOfGetter($oneOfFields = ["inviteLinkParentGroupSubjectV2"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_inviteLinkParentGroupThumbnailV2", {
                get: $util.oneOfGetter($oneOfFields = ["inviteLinkParentGroupThumbnailV2"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_inviteLinkGroupTypeV2", {
                get: $util.oneOfGetter($oneOfFields = ["inviteLinkGroupTypeV2"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_viewOnce", {
                get: $util.oneOfGetter($oneOfFields = ["viewOnce"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_videoHeight", {
                get: $util.oneOfGetter($oneOfFields = ["videoHeight"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_videoWidth", {
                get: $util.oneOfGetter($oneOfFields = ["videoWidth"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_faviconMMSMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["faviconMMSMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_linkPreviewMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["linkPreviewMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_paymentLinkMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["paymentLinkMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_videoContentUrl", {
                get: $util.oneOfGetter($oneOfFields = ["videoContentUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_musicMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["musicMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExtendedTextMessage.prototype, "_paymentExtendedMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["paymentExtendedMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ExtendedTextMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.ExtendedTextMessage
             * @static
             * @param {proto.Message.IExtendedTextMessage=} [properties] Properties to set
             * @returns {proto.Message.ExtendedTextMessage} ExtendedTextMessage instance
             */
            ExtendedTextMessage.create = function create(properties) {
                return new ExtendedTextMessage(properties);
            };

            /**
             * Encodes the specified ExtendedTextMessage message. Does not implicitly {@link proto.Message.ExtendedTextMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.ExtendedTextMessage
             * @static
             * @param {proto.Message.IExtendedTextMessage} message ExtendedTextMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ExtendedTextMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.text);
                if (message.matchedText != null && Object.hasOwnProperty.call(message, "matchedText"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.matchedText);
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.description);
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.title);
                if (message.textArgb != null && Object.hasOwnProperty.call(message, "textArgb"))
                    writer.uint32(/* id 7, wireType 5 =*/61).fixed32(message.textArgb);
                if (message.backgroundArgb != null && Object.hasOwnProperty.call(message, "backgroundArgb"))
                    writer.uint32(/* id 8, wireType 5 =*/69).fixed32(message.backgroundArgb);
                if (message.font != null && Object.hasOwnProperty.call(message, "font"))
                    writer.uint32(/* id 9, wireType 0 =*/72).int32(message.font);
                if (message.previewType != null && Object.hasOwnProperty.call(message, "previewType"))
                    writer.uint32(/* id 10, wireType 0 =*/80).int32(message.previewType);
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    writer.uint32(/* id 16, wireType 2 =*/130).bytes(message.jpegThumbnail);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
                if (message.doNotPlayInline != null && Object.hasOwnProperty.call(message, "doNotPlayInline"))
                    writer.uint32(/* id 18, wireType 0 =*/144).bool(message.doNotPlayInline);
                if (message.thumbnailDirectPath != null && Object.hasOwnProperty.call(message, "thumbnailDirectPath"))
                    writer.uint32(/* id 19, wireType 2 =*/154).string(message.thumbnailDirectPath);
                if (message.thumbnailSha256 != null && Object.hasOwnProperty.call(message, "thumbnailSha256"))
                    writer.uint32(/* id 20, wireType 2 =*/162).bytes(message.thumbnailSha256);
                if (message.thumbnailEncSha256 != null && Object.hasOwnProperty.call(message, "thumbnailEncSha256"))
                    writer.uint32(/* id 21, wireType 2 =*/170).bytes(message.thumbnailEncSha256);
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    writer.uint32(/* id 22, wireType 2 =*/178).bytes(message.mediaKey);
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                    writer.uint32(/* id 23, wireType 0 =*/184).int64(message.mediaKeyTimestamp);
                if (message.thumbnailHeight != null && Object.hasOwnProperty.call(message, "thumbnailHeight"))
                    writer.uint32(/* id 24, wireType 0 =*/192).uint32(message.thumbnailHeight);
                if (message.thumbnailWidth != null && Object.hasOwnProperty.call(message, "thumbnailWidth"))
                    writer.uint32(/* id 25, wireType 0 =*/200).uint32(message.thumbnailWidth);
                if (message.inviteLinkGroupType != null && Object.hasOwnProperty.call(message, "inviteLinkGroupType"))
                    writer.uint32(/* id 26, wireType 0 =*/208).int32(message.inviteLinkGroupType);
                if (message.inviteLinkParentGroupSubjectV2 != null && Object.hasOwnProperty.call(message, "inviteLinkParentGroupSubjectV2"))
                    writer.uint32(/* id 27, wireType 2 =*/218).string(message.inviteLinkParentGroupSubjectV2);
                if (message.inviteLinkParentGroupThumbnailV2 != null && Object.hasOwnProperty.call(message, "inviteLinkParentGroupThumbnailV2"))
                    writer.uint32(/* id 28, wireType 2 =*/226).bytes(message.inviteLinkParentGroupThumbnailV2);
                if (message.inviteLinkGroupTypeV2 != null && Object.hasOwnProperty.call(message, "inviteLinkGroupTypeV2"))
                    writer.uint32(/* id 29, wireType 0 =*/232).int32(message.inviteLinkGroupTypeV2);
                if (message.viewOnce != null && Object.hasOwnProperty.call(message, "viewOnce"))
                    writer.uint32(/* id 30, wireType 0 =*/240).bool(message.viewOnce);
                if (message.videoHeight != null && Object.hasOwnProperty.call(message, "videoHeight"))
                    writer.uint32(/* id 31, wireType 0 =*/248).uint32(message.videoHeight);
                if (message.videoWidth != null && Object.hasOwnProperty.call(message, "videoWidth"))
                    writer.uint32(/* id 32, wireType 0 =*/256).uint32(message.videoWidth);
                if (message.faviconMMSMetadata != null && Object.hasOwnProperty.call(message, "faviconMMSMetadata"))
                    $root.proto.Message.MMSThumbnailMetadata.encode(message.faviconMMSMetadata, writer.uint32(/* id 33, wireType 2 =*/266).fork(), q + 1).ldelim();
                if (message.linkPreviewMetadata != null && Object.hasOwnProperty.call(message, "linkPreviewMetadata"))
                    $root.proto.Message.LinkPreviewMetadata.encode(message.linkPreviewMetadata, writer.uint32(/* id 34, wireType 2 =*/274).fork(), q + 1).ldelim();
                if (message.paymentLinkMetadata != null && Object.hasOwnProperty.call(message, "paymentLinkMetadata"))
                    $root.proto.Message.PaymentLinkMetadata.encode(message.paymentLinkMetadata, writer.uint32(/* id 35, wireType 2 =*/282).fork(), q + 1).ldelim();
                if (message.endCardTiles != null && message.endCardTiles.length)
                    for (var i = 0; i < message.endCardTiles.length; ++i)
                        $root.proto.Message.VideoEndCard.encode(message.endCardTiles[i], writer.uint32(/* id 36, wireType 2 =*/290).fork(), q + 1).ldelim();
                if (message.videoContentUrl != null && Object.hasOwnProperty.call(message, "videoContentUrl"))
                    writer.uint32(/* id 37, wireType 2 =*/298).string(message.videoContentUrl);
                if (message.musicMetadata != null && Object.hasOwnProperty.call(message, "musicMetadata"))
                    $root.proto.EmbeddedMusic.encode(message.musicMetadata, writer.uint32(/* id 38, wireType 2 =*/306).fork(), q + 1).ldelim();
                if (message.paymentExtendedMetadata != null && Object.hasOwnProperty.call(message, "paymentExtendedMetadata"))
                    $root.proto.Message.PaymentExtendedMetadata.encode(message.paymentExtendedMetadata, writer.uint32(/* id 39, wireType 2 =*/314).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ExtendedTextMessage message, length delimited. Does not implicitly {@link proto.Message.ExtendedTextMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.ExtendedTextMessage
             * @static
             * @param {proto.Message.IExtendedTextMessage} message ExtendedTextMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ExtendedTextMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an ExtendedTextMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.ExtendedTextMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.ExtendedTextMessage} ExtendedTextMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ExtendedTextMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.text = reader.stringVerify();
                            message._text = "text";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.matchedText = reader.stringVerify();
                            message._matchedText = "matchedText";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.description = reader.stringVerify();
                            message._description = "description";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.title = reader.stringVerify();
                            message._title = "title";
                            continue;
                        }
                    case 7: {
                            if (u !== 5)
                                break;
                            message.textArgb = reader.fixed32();
                            message._textArgb = "textArgb";
                            continue;
                        }
                    case 8: {
                            if (u !== 5)
                                break;
                            message.backgroundArgb = reader.fixed32();
                            message._backgroundArgb = "backgroundArgb";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            message.font = reader.int32();
                            message._font = "font";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            message.previewType = reader.int32();
                            message._previewType = "previewType";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            message.jpegThumbnail = reader.bytes();
                            message._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 18: {
                            if (u !== 0)
                                break;
                            message.doNotPlayInline = reader.bool();
                            message._doNotPlayInline = "doNotPlayInline";
                            continue;
                        }
                    case 19: {
                            if (u !== 2)
                                break;
                            message.thumbnailDirectPath = reader.stringVerify();
                            message._thumbnailDirectPath = "thumbnailDirectPath";
                            continue;
                        }
                    case 20: {
                            if (u !== 2)
                                break;
                            message.thumbnailSha256 = reader.bytes();
                            message._thumbnailSha256 = "thumbnailSha256";
                            continue;
                        }
                    case 21: {
                            if (u !== 2)
                                break;
                            message.thumbnailEncSha256 = reader.bytes();
                            message._thumbnailEncSha256 = "thumbnailEncSha256";
                            continue;
                        }
                    case 22: {
                            if (u !== 2)
                                break;
                            message.mediaKey = reader.bytes();
                            message._mediaKey = "mediaKey";
                            continue;
                        }
                    case 23: {
                            if (u !== 0)
                                break;
                            message.mediaKeyTimestamp = reader.int64();
                            message._mediaKeyTimestamp = "mediaKeyTimestamp";
                            continue;
                        }
                    case 24: {
                            if (u !== 0)
                                break;
                            message.thumbnailHeight = reader.uint32();
                            message._thumbnailHeight = "thumbnailHeight";
                            continue;
                        }
                    case 25: {
                            if (u !== 0)
                                break;
                            message.thumbnailWidth = reader.uint32();
                            message._thumbnailWidth = "thumbnailWidth";
                            continue;
                        }
                    case 26: {
                            if (u !== 0)
                                break;
                            message.inviteLinkGroupType = reader.int32();
                            message._inviteLinkGroupType = "inviteLinkGroupType";
                            continue;
                        }
                    case 27: {
                            if (u !== 2)
                                break;
                            message.inviteLinkParentGroupSubjectV2 = reader.stringVerify();
                            message._inviteLinkParentGroupSubjectV2 = "inviteLinkParentGroupSubjectV2";
                            continue;
                        }
                    case 28: {
                            if (u !== 2)
                                break;
                            message.inviteLinkParentGroupThumbnailV2 = reader.bytes();
                            message._inviteLinkParentGroupThumbnailV2 = "inviteLinkParentGroupThumbnailV2";
                            continue;
                        }
                    case 29: {
                            if (u !== 0)
                                break;
                            message.inviteLinkGroupTypeV2 = reader.int32();
                            message._inviteLinkGroupTypeV2 = "inviteLinkGroupTypeV2";
                            continue;
                        }
                    case 30: {
                            if (u !== 0)
                                break;
                            message.viewOnce = reader.bool();
                            message._viewOnce = "viewOnce";
                            continue;
                        }
                    case 31: {
                            if (u !== 0)
                                break;
                            message.videoHeight = reader.uint32();
                            message._videoHeight = "videoHeight";
                            continue;
                        }
                    case 32: {
                            if (u !== 0)
                                break;
                            message.videoWidth = reader.uint32();
                            message._videoWidth = "videoWidth";
                            continue;
                        }
                    case 33: {
                            if (u !== 2)
                                break;
                            message.faviconMMSMetadata = $root.proto.Message.MMSThumbnailMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.faviconMMSMetadata);
                            message._faviconMMSMetadata = "faviconMMSMetadata";
                            continue;
                        }
                    case 34: {
                            if (u !== 2)
                                break;
                            message.linkPreviewMetadata = $root.proto.Message.LinkPreviewMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.linkPreviewMetadata);
                            message._linkPreviewMetadata = "linkPreviewMetadata";
                            continue;
                        }
                    case 35: {
                            if (u !== 2)
                                break;
                            message.paymentLinkMetadata = $root.proto.Message.PaymentLinkMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.paymentLinkMetadata);
                            message._paymentLinkMetadata = "paymentLinkMetadata";
                            continue;
                        }
                    case 36: {
                            if (u !== 2)
                                break;
                            if (!(message.endCardTiles && message.endCardTiles.length))
                                message.endCardTiles = [];
                            message.endCardTiles.push($root.proto.Message.VideoEndCard.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 37: {
                            if (u !== 2)
                                break;
                            message.videoContentUrl = reader.stringVerify();
                            message._videoContentUrl = "videoContentUrl";
                            continue;
                        }
                    case 38: {
                            if (u !== 2)
                                break;
                            message.musicMetadata = $root.proto.EmbeddedMusic.decode(reader, reader.uint32(), undefined, q + 1, message.musicMetadata);
                            message._musicMetadata = "musicMetadata";
                            continue;
                        }
                    case 39: {
                            if (u !== 2)
                                break;
                            message.paymentExtendedMetadata = $root.proto.Message.PaymentExtendedMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.paymentExtendedMetadata);
                            message._paymentExtendedMetadata = "paymentExtendedMetadata";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an ExtendedTextMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.ExtendedTextMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.ExtendedTextMessage} ExtendedTextMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ExtendedTextMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an ExtendedTextMessage message.
             * @function verify
             * @memberof proto.Message.ExtendedTextMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ExtendedTextMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.text != null && Object.hasOwnProperty.call(message, "text")) {
                    properties._text = 1;
                    if (!$util.isString(message.text))
                        return "text: string expected";
                }
                if (message.matchedText != null && Object.hasOwnProperty.call(message, "matchedText")) {
                    properties._matchedText = 1;
                    if (!$util.isString(message.matchedText))
                        return "matchedText: string expected";
                }
                if (message.description != null && Object.hasOwnProperty.call(message, "description")) {
                    properties._description = 1;
                    if (!$util.isString(message.description))
                        return "description: string expected";
                }
                if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                    properties._title = 1;
                    if (!$util.isString(message.title))
                        return "title: string expected";
                }
                if (message.textArgb != null && Object.hasOwnProperty.call(message, "textArgb")) {
                    properties._textArgb = 1;
                    if (!$util.isInteger(message.textArgb))
                        return "textArgb: integer expected";
                }
                if (message.backgroundArgb != null && Object.hasOwnProperty.call(message, "backgroundArgb")) {
                    properties._backgroundArgb = 1;
                    if (!$util.isInteger(message.backgroundArgb))
                        return "backgroundArgb: integer expected";
                }
                if (message.font != null && Object.hasOwnProperty.call(message, "font")) {
                    properties._font = 1;
                    if (typeof message.font !== "number" || (message.font | 0) !== message.font)
                        return "font: enum value expected";
                }
                if (message.previewType != null && Object.hasOwnProperty.call(message, "previewType")) {
                    properties._previewType = 1;
                    if (typeof message.previewType !== "number" || (message.previewType | 0) !== message.previewType)
                        return "previewType: enum value expected";
                }
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail")) {
                    properties._jpegThumbnail = 1;
                    if (!(message.jpegThumbnail && typeof message.jpegThumbnail.length === "number" || $util.isString(message.jpegThumbnail)))
                        return "jpegThumbnail: buffer expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.doNotPlayInline != null && Object.hasOwnProperty.call(message, "doNotPlayInline")) {
                    properties._doNotPlayInline = 1;
                    if (typeof message.doNotPlayInline !== "boolean")
                        return "doNotPlayInline: boolean expected";
                }
                if (message.thumbnailDirectPath != null && Object.hasOwnProperty.call(message, "thumbnailDirectPath")) {
                    properties._thumbnailDirectPath = 1;
                    if (!$util.isString(message.thumbnailDirectPath))
                        return "thumbnailDirectPath: string expected";
                }
                if (message.thumbnailSha256 != null && Object.hasOwnProperty.call(message, "thumbnailSha256")) {
                    properties._thumbnailSha256 = 1;
                    if (!(message.thumbnailSha256 && typeof message.thumbnailSha256.length === "number" || $util.isString(message.thumbnailSha256)))
                        return "thumbnailSha256: buffer expected";
                }
                if (message.thumbnailEncSha256 != null && Object.hasOwnProperty.call(message, "thumbnailEncSha256")) {
                    properties._thumbnailEncSha256 = 1;
                    if (!(message.thumbnailEncSha256 && typeof message.thumbnailEncSha256.length === "number" || $util.isString(message.thumbnailEncSha256)))
                        return "thumbnailEncSha256: buffer expected";
                }
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey")) {
                    properties._mediaKey = 1;
                    if (!(message.mediaKey && typeof message.mediaKey.length === "number" || $util.isString(message.mediaKey)))
                        return "mediaKey: buffer expected";
                }
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp")) {
                    properties._mediaKeyTimestamp = 1;
                    if (!$util.isInteger(message.mediaKeyTimestamp) && !(message.mediaKeyTimestamp && $util.isInteger(message.mediaKeyTimestamp.low) && $util.isInteger(message.mediaKeyTimestamp.high)))
                        return "mediaKeyTimestamp: integer|Long expected";
                }
                if (message.thumbnailHeight != null && Object.hasOwnProperty.call(message, "thumbnailHeight")) {
                    properties._thumbnailHeight = 1;
                    if (!$util.isInteger(message.thumbnailHeight))
                        return "thumbnailHeight: integer expected";
                }
                if (message.thumbnailWidth != null && Object.hasOwnProperty.call(message, "thumbnailWidth")) {
                    properties._thumbnailWidth = 1;
                    if (!$util.isInteger(message.thumbnailWidth))
                        return "thumbnailWidth: integer expected";
                }
                if (message.inviteLinkGroupType != null && Object.hasOwnProperty.call(message, "inviteLinkGroupType")) {
                    properties._inviteLinkGroupType = 1;
                    if (typeof message.inviteLinkGroupType !== "number" || (message.inviteLinkGroupType | 0) !== message.inviteLinkGroupType)
                        return "inviteLinkGroupType: enum value expected";
                }
                if (message.inviteLinkParentGroupSubjectV2 != null && Object.hasOwnProperty.call(message, "inviteLinkParentGroupSubjectV2")) {
                    properties._inviteLinkParentGroupSubjectV2 = 1;
                    if (!$util.isString(message.inviteLinkParentGroupSubjectV2))
                        return "inviteLinkParentGroupSubjectV2: string expected";
                }
                if (message.inviteLinkParentGroupThumbnailV2 != null && Object.hasOwnProperty.call(message, "inviteLinkParentGroupThumbnailV2")) {
                    properties._inviteLinkParentGroupThumbnailV2 = 1;
                    if (!(message.inviteLinkParentGroupThumbnailV2 && typeof message.inviteLinkParentGroupThumbnailV2.length === "number" || $util.isString(message.inviteLinkParentGroupThumbnailV2)))
                        return "inviteLinkParentGroupThumbnailV2: buffer expected";
                }
                if (message.inviteLinkGroupTypeV2 != null && Object.hasOwnProperty.call(message, "inviteLinkGroupTypeV2")) {
                    properties._inviteLinkGroupTypeV2 = 1;
                    if (typeof message.inviteLinkGroupTypeV2 !== "number" || (message.inviteLinkGroupTypeV2 | 0) !== message.inviteLinkGroupTypeV2)
                        return "inviteLinkGroupTypeV2: enum value expected";
                }
                if (message.viewOnce != null && Object.hasOwnProperty.call(message, "viewOnce")) {
                    properties._viewOnce = 1;
                    if (typeof message.viewOnce !== "boolean")
                        return "viewOnce: boolean expected";
                }
                if (message.videoHeight != null && Object.hasOwnProperty.call(message, "videoHeight")) {
                    properties._videoHeight = 1;
                    if (!$util.isInteger(message.videoHeight))
                        return "videoHeight: integer expected";
                }
                if (message.videoWidth != null && Object.hasOwnProperty.call(message, "videoWidth")) {
                    properties._videoWidth = 1;
                    if (!$util.isInteger(message.videoWidth))
                        return "videoWidth: integer expected";
                }
                if (message.faviconMMSMetadata != null && Object.hasOwnProperty.call(message, "faviconMMSMetadata")) {
                    properties._faviconMMSMetadata = 1;
                    {
                        var error = $root.proto.Message.MMSThumbnailMetadata.verify(message.faviconMMSMetadata, q + 1);
                        if (error)
                            return "faviconMMSMetadata." + error;
                    }
                }
                if (message.linkPreviewMetadata != null && Object.hasOwnProperty.call(message, "linkPreviewMetadata")) {
                    properties._linkPreviewMetadata = 1;
                    {
                        var error = $root.proto.Message.LinkPreviewMetadata.verify(message.linkPreviewMetadata, q + 1);
                        if (error)
                            return "linkPreviewMetadata." + error;
                    }
                }
                if (message.paymentLinkMetadata != null && Object.hasOwnProperty.call(message, "paymentLinkMetadata")) {
                    properties._paymentLinkMetadata = 1;
                    {
                        var error = $root.proto.Message.PaymentLinkMetadata.verify(message.paymentLinkMetadata, q + 1);
                        if (error)
                            return "paymentLinkMetadata." + error;
                    }
                }
                if (message.endCardTiles != null && Object.hasOwnProperty.call(message, "endCardTiles")) {
                    if (!Array.isArray(message.endCardTiles))
                        return "endCardTiles: array expected";
                    for (var i = 0; i < message.endCardTiles.length; ++i) {
                        var error = $root.proto.Message.VideoEndCard.verify(message.endCardTiles[i], q + 1);
                        if (error)
                            return "endCardTiles." + error;
                    }
                }
                if (message.videoContentUrl != null && Object.hasOwnProperty.call(message, "videoContentUrl")) {
                    properties._videoContentUrl = 1;
                    if (!$util.isString(message.videoContentUrl))
                        return "videoContentUrl: string expected";
                }
                if (message.musicMetadata != null && Object.hasOwnProperty.call(message, "musicMetadata")) {
                    properties._musicMetadata = 1;
                    {
                        var error = $root.proto.EmbeddedMusic.verify(message.musicMetadata, q + 1);
                        if (error)
                            return "musicMetadata." + error;
                    }
                }
                if (message.paymentExtendedMetadata != null && Object.hasOwnProperty.call(message, "paymentExtendedMetadata")) {
                    properties._paymentExtendedMetadata = 1;
                    {
                        var error = $root.proto.Message.PaymentExtendedMetadata.verify(message.paymentExtendedMetadata, q + 1);
                        if (error)
                            return "paymentExtendedMetadata." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an ExtendedTextMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.ExtendedTextMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.ExtendedTextMessage} ExtendedTextMessage
             */
            ExtendedTextMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.ExtendedTextMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.text != null)
                    message.text = String(object.text);
                if (object.matchedText != null)
                    message.matchedText = String(object.matchedText);
                if (object.description != null)
                    message.description = String(object.description);
                if (object.title != null)
                    message.title = String(object.title);
                if (object.textArgb != null)
                    message.textArgb = object.textArgb >>> 0;
                if (object.backgroundArgb != null)
                    message.backgroundArgb = object.backgroundArgb >>> 0;
                switch (object.font) {
                case "SYSTEM":
                case 0:
                    message.font = 0;
                    break;
                case "SYSTEM_TEXT":
                case 1:
                    message.font = 1;
                    break;
                case "FB_SCRIPT":
                case 2:
                    message.font = 2;
                    break;
                case "SYSTEM_BOLD":
                case 6:
                    message.font = 6;
                    break;
                case "MORNINGBREEZE_REGULAR":
                case 7:
                    message.font = 7;
                    break;
                case "CALISTOGA_REGULAR":
                case 8:
                    message.font = 8;
                    break;
                case "EXO2_EXTRABOLD":
                case 9:
                    message.font = 9;
                    break;
                case "COURIERPRIME_BOLD":
                case 10:
                    message.font = 10;
                    break;
                default:
                    if (typeof object.font === "number" && (object.font | 0) === object.font)
                        message.font = object.font;
                }
                switch (object.previewType) {
                case "NONE":
                case 0:
                    message.previewType = 0;
                    break;
                case "VIDEO":
                case 1:
                    message.previewType = 1;
                    break;
                case "PLACEHOLDER":
                case 4:
                    message.previewType = 4;
                    break;
                case "IMAGE":
                case 5:
                    message.previewType = 5;
                    break;
                case "PAYMENT_LINKS":
                case 6:
                    message.previewType = 6;
                    break;
                case "PROFILE":
                case 7:
                    message.previewType = 7;
                    break;
                default:
                    if (typeof object.previewType === "number" && (object.previewType | 0) === object.previewType)
                        message.previewType = object.previewType;
                }
                if (object.jpegThumbnail != null)
                    if (typeof object.jpegThumbnail === "string")
                        $util.base64.decode(object.jpegThumbnail, message.jpegThumbnail = $util.newBuffer($util.base64.length(object.jpegThumbnail)), 0);
                    else if (object.jpegThumbnail.length >= 0)
                        message.jpegThumbnail = object.jpegThumbnail;
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.ExtendedTextMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                if (object.doNotPlayInline != null)
                    message.doNotPlayInline = Boolean(object.doNotPlayInline);
                if (object.thumbnailDirectPath != null)
                    message.thumbnailDirectPath = String(object.thumbnailDirectPath);
                if (object.thumbnailSha256 != null)
                    if (typeof object.thumbnailSha256 === "string")
                        $util.base64.decode(object.thumbnailSha256, message.thumbnailSha256 = $util.newBuffer($util.base64.length(object.thumbnailSha256)), 0);
                    else if (object.thumbnailSha256.length >= 0)
                        message.thumbnailSha256 = object.thumbnailSha256;
                if (object.thumbnailEncSha256 != null)
                    if (typeof object.thumbnailEncSha256 === "string")
                        $util.base64.decode(object.thumbnailEncSha256, message.thumbnailEncSha256 = $util.newBuffer($util.base64.length(object.thumbnailEncSha256)), 0);
                    else if (object.thumbnailEncSha256.length >= 0)
                        message.thumbnailEncSha256 = object.thumbnailEncSha256;
                if (object.mediaKey != null)
                    if (typeof object.mediaKey === "string")
                        $util.base64.decode(object.mediaKey, message.mediaKey = $util.newBuffer($util.base64.length(object.mediaKey)), 0);
                    else if (object.mediaKey.length >= 0)
                        message.mediaKey = object.mediaKey;
                if (object.mediaKeyTimestamp != null)
                    if ($util.Long)
                        message.mediaKeyTimestamp = $util.Long.fromValue(object.mediaKeyTimestamp, false);
                    else if (typeof object.mediaKeyTimestamp === "string")
                        message.mediaKeyTimestamp = parseInt(object.mediaKeyTimestamp, 10);
                    else if (typeof object.mediaKeyTimestamp === "number")
                        message.mediaKeyTimestamp = object.mediaKeyTimestamp;
                    else if (typeof object.mediaKeyTimestamp === "object")
                        message.mediaKeyTimestamp = new $util.LongBits(object.mediaKeyTimestamp.low >>> 0, object.mediaKeyTimestamp.high >>> 0).toNumber();
                if (object.thumbnailHeight != null)
                    message.thumbnailHeight = object.thumbnailHeight >>> 0;
                if (object.thumbnailWidth != null)
                    message.thumbnailWidth = object.thumbnailWidth >>> 0;
                switch (object.inviteLinkGroupType) {
                case "DEFAULT":
                case 0:
                    message.inviteLinkGroupType = 0;
                    break;
                case "PARENT":
                case 1:
                    message.inviteLinkGroupType = 1;
                    break;
                case "SUB":
                case 2:
                    message.inviteLinkGroupType = 2;
                    break;
                case "DEFAULT_SUB":
                case 3:
                    message.inviteLinkGroupType = 3;
                    break;
                default:
                    if (typeof object.inviteLinkGroupType === "number" && (object.inviteLinkGroupType | 0) === object.inviteLinkGroupType)
                        message.inviteLinkGroupType = object.inviteLinkGroupType;
                }
                if (object.inviteLinkParentGroupSubjectV2 != null)
                    message.inviteLinkParentGroupSubjectV2 = String(object.inviteLinkParentGroupSubjectV2);
                if (object.inviteLinkParentGroupThumbnailV2 != null)
                    if (typeof object.inviteLinkParentGroupThumbnailV2 === "string")
                        $util.base64.decode(object.inviteLinkParentGroupThumbnailV2, message.inviteLinkParentGroupThumbnailV2 = $util.newBuffer($util.base64.length(object.inviteLinkParentGroupThumbnailV2)), 0);
                    else if (object.inviteLinkParentGroupThumbnailV2.length >= 0)
                        message.inviteLinkParentGroupThumbnailV2 = object.inviteLinkParentGroupThumbnailV2;
                switch (object.inviteLinkGroupTypeV2) {
                case "DEFAULT":
                case 0:
                    message.inviteLinkGroupTypeV2 = 0;
                    break;
                case "PARENT":
                case 1:
                    message.inviteLinkGroupTypeV2 = 1;
                    break;
                case "SUB":
                case 2:
                    message.inviteLinkGroupTypeV2 = 2;
                    break;
                case "DEFAULT_SUB":
                case 3:
                    message.inviteLinkGroupTypeV2 = 3;
                    break;
                default:
                    if (typeof object.inviteLinkGroupTypeV2 === "number" && (object.inviteLinkGroupTypeV2 | 0) === object.inviteLinkGroupTypeV2)
                        message.inviteLinkGroupTypeV2 = object.inviteLinkGroupTypeV2;
                }
                if (object.viewOnce != null)
                    message.viewOnce = Boolean(object.viewOnce);
                if (object.videoHeight != null)
                    message.videoHeight = object.videoHeight >>> 0;
                if (object.videoWidth != null)
                    message.videoWidth = object.videoWidth >>> 0;
                if (object.faviconMMSMetadata != null) {
                    if (!$util.isObject(object.faviconMMSMetadata))
                        throw TypeError(".proto.Message.ExtendedTextMessage.faviconMMSMetadata: object expected");
                    message.faviconMMSMetadata = $root.proto.Message.MMSThumbnailMetadata.fromObject(object.faviconMMSMetadata, q + 1);
                }
                if (object.linkPreviewMetadata != null) {
                    if (!$util.isObject(object.linkPreviewMetadata))
                        throw TypeError(".proto.Message.ExtendedTextMessage.linkPreviewMetadata: object expected");
                    message.linkPreviewMetadata = $root.proto.Message.LinkPreviewMetadata.fromObject(object.linkPreviewMetadata, q + 1);
                }
                if (object.paymentLinkMetadata != null) {
                    if (!$util.isObject(object.paymentLinkMetadata))
                        throw TypeError(".proto.Message.ExtendedTextMessage.paymentLinkMetadata: object expected");
                    message.paymentLinkMetadata = $root.proto.Message.PaymentLinkMetadata.fromObject(object.paymentLinkMetadata, q + 1);
                }
                if (object.endCardTiles) {
                    if (!Array.isArray(object.endCardTiles))
                        throw TypeError(".proto.Message.ExtendedTextMessage.endCardTiles: array expected");
                    message.endCardTiles = Array(object.endCardTiles.length);
                    for (var i = 0; i < object.endCardTiles.length; ++i) {
                        if (!$util.isObject(object.endCardTiles[i]))
                            throw TypeError(".proto.Message.ExtendedTextMessage.endCardTiles: object expected");
                        message.endCardTiles[i] = $root.proto.Message.VideoEndCard.fromObject(object.endCardTiles[i], q + 1);
                    }
                }
                if (object.videoContentUrl != null)
                    message.videoContentUrl = String(object.videoContentUrl);
                if (object.musicMetadata != null) {
                    if (!$util.isObject(object.musicMetadata))
                        throw TypeError(".proto.Message.ExtendedTextMessage.musicMetadata: object expected");
                    message.musicMetadata = $root.proto.EmbeddedMusic.fromObject(object.musicMetadata, q + 1);
                }
                if (object.paymentExtendedMetadata != null) {
                    if (!$util.isObject(object.paymentExtendedMetadata))
                        throw TypeError(".proto.Message.ExtendedTextMessage.paymentExtendedMetadata: object expected");
                    message.paymentExtendedMetadata = $root.proto.Message.PaymentExtendedMetadata.fromObject(object.paymentExtendedMetadata, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from an ExtendedTextMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.ExtendedTextMessage
             * @static
             * @param {proto.Message.ExtendedTextMessage} message ExtendedTextMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ExtendedTextMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.endCardTiles = [];
                if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                    object.text = message.text;
                if (message.matchedText != null && Object.hasOwnProperty.call(message, "matchedText"))
                    object.matchedText = message.matchedText;
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    object.description = message.description;
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    object.title = message.title;
                if (message.textArgb != null && Object.hasOwnProperty.call(message, "textArgb"))
                    object.textArgb = message.textArgb;
                if (message.backgroundArgb != null && Object.hasOwnProperty.call(message, "backgroundArgb"))
                    object.backgroundArgb = message.backgroundArgb;
                if (message.font != null && Object.hasOwnProperty.call(message, "font"))
                    object.font = options.enums === String ? $root.proto.Message.ExtendedTextMessage.FontType[message.font] === undefined ? message.font : $root.proto.Message.ExtendedTextMessage.FontType[message.font] : message.font;
                if (message.previewType != null && Object.hasOwnProperty.call(message, "previewType"))
                    object.previewType = options.enums === String ? $root.proto.Message.ExtendedTextMessage.PreviewType[message.previewType] === undefined ? message.previewType : $root.proto.Message.ExtendedTextMessage.PreviewType[message.previewType] : message.previewType;
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    object.jpegThumbnail = options.bytes === String ? $util.base64.encode(message.jpegThumbnail, 0, message.jpegThumbnail.length) : options.bytes === Array ? Array.prototype.slice.call(message.jpegThumbnail) : message.jpegThumbnail;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.doNotPlayInline != null && Object.hasOwnProperty.call(message, "doNotPlayInline"))
                    object.doNotPlayInline = message.doNotPlayInline;
                if (message.thumbnailDirectPath != null && Object.hasOwnProperty.call(message, "thumbnailDirectPath"))
                    object.thumbnailDirectPath = message.thumbnailDirectPath;
                if (message.thumbnailSha256 != null && Object.hasOwnProperty.call(message, "thumbnailSha256"))
                    object.thumbnailSha256 = options.bytes === String ? $util.base64.encode(message.thumbnailSha256, 0, message.thumbnailSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.thumbnailSha256) : message.thumbnailSha256;
                if (message.thumbnailEncSha256 != null && Object.hasOwnProperty.call(message, "thumbnailEncSha256"))
                    object.thumbnailEncSha256 = options.bytes === String ? $util.base64.encode(message.thumbnailEncSha256, 0, message.thumbnailEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.thumbnailEncSha256) : message.thumbnailEncSha256;
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    object.mediaKey = options.bytes === String ? $util.base64.encode(message.mediaKey, 0, message.mediaKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.mediaKey) : message.mediaKey;
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.mediaKeyTimestamp = typeof message.mediaKeyTimestamp === "number" ? BigInt(message.mediaKeyTimestamp) : $util.Long.fromBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.mediaKeyTimestamp === "number")
                        object.mediaKeyTimestamp = options.longs === String ? String(message.mediaKeyTimestamp) : message.mediaKeyTimestamp;
                    else
                        object.mediaKeyTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.mediaKeyTimestamp) : options.longs === Number ? new $util.LongBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0).toNumber() : message.mediaKeyTimestamp;
                if (message.thumbnailHeight != null && Object.hasOwnProperty.call(message, "thumbnailHeight"))
                    object.thumbnailHeight = message.thumbnailHeight;
                if (message.thumbnailWidth != null && Object.hasOwnProperty.call(message, "thumbnailWidth"))
                    object.thumbnailWidth = message.thumbnailWidth;
                if (message.inviteLinkGroupType != null && Object.hasOwnProperty.call(message, "inviteLinkGroupType"))
                    object.inviteLinkGroupType = options.enums === String ? $root.proto.Message.ExtendedTextMessage.InviteLinkGroupType[message.inviteLinkGroupType] === undefined ? message.inviteLinkGroupType : $root.proto.Message.ExtendedTextMessage.InviteLinkGroupType[message.inviteLinkGroupType] : message.inviteLinkGroupType;
                if (message.inviteLinkParentGroupSubjectV2 != null && Object.hasOwnProperty.call(message, "inviteLinkParentGroupSubjectV2"))
                    object.inviteLinkParentGroupSubjectV2 = message.inviteLinkParentGroupSubjectV2;
                if (message.inviteLinkParentGroupThumbnailV2 != null && Object.hasOwnProperty.call(message, "inviteLinkParentGroupThumbnailV2"))
                    object.inviteLinkParentGroupThumbnailV2 = options.bytes === String ? $util.base64.encode(message.inviteLinkParentGroupThumbnailV2, 0, message.inviteLinkParentGroupThumbnailV2.length) : options.bytes === Array ? Array.prototype.slice.call(message.inviteLinkParentGroupThumbnailV2) : message.inviteLinkParentGroupThumbnailV2;
                if (message.inviteLinkGroupTypeV2 != null && Object.hasOwnProperty.call(message, "inviteLinkGroupTypeV2"))
                    object.inviteLinkGroupTypeV2 = options.enums === String ? $root.proto.Message.ExtendedTextMessage.InviteLinkGroupType[message.inviteLinkGroupTypeV2] === undefined ? message.inviteLinkGroupTypeV2 : $root.proto.Message.ExtendedTextMessage.InviteLinkGroupType[message.inviteLinkGroupTypeV2] : message.inviteLinkGroupTypeV2;
                if (message.viewOnce != null && Object.hasOwnProperty.call(message, "viewOnce"))
                    object.viewOnce = message.viewOnce;
                if (message.videoHeight != null && Object.hasOwnProperty.call(message, "videoHeight"))
                    object.videoHeight = message.videoHeight;
                if (message.videoWidth != null && Object.hasOwnProperty.call(message, "videoWidth"))
                    object.videoWidth = message.videoWidth;
                if (message.faviconMMSMetadata != null && Object.hasOwnProperty.call(message, "faviconMMSMetadata"))
                    object.faviconMMSMetadata = $root.proto.Message.MMSThumbnailMetadata.toObject(message.faviconMMSMetadata, options, q + 1);
                if (message.linkPreviewMetadata != null && Object.hasOwnProperty.call(message, "linkPreviewMetadata"))
                    object.linkPreviewMetadata = $root.proto.Message.LinkPreviewMetadata.toObject(message.linkPreviewMetadata, options, q + 1);
                if (message.paymentLinkMetadata != null && Object.hasOwnProperty.call(message, "paymentLinkMetadata"))
                    object.paymentLinkMetadata = $root.proto.Message.PaymentLinkMetadata.toObject(message.paymentLinkMetadata, options, q + 1);
                if (message.endCardTiles && message.endCardTiles.length) {
                    object.endCardTiles = Array(message.endCardTiles.length);
                    for (var j = 0; j < message.endCardTiles.length; ++j)
                        object.endCardTiles[j] = $root.proto.Message.VideoEndCard.toObject(message.endCardTiles[j], options, q + 1);
                }
                if (message.videoContentUrl != null && Object.hasOwnProperty.call(message, "videoContentUrl"))
                    object.videoContentUrl = message.videoContentUrl;
                if (message.musicMetadata != null && Object.hasOwnProperty.call(message, "musicMetadata"))
                    object.musicMetadata = $root.proto.EmbeddedMusic.toObject(message.musicMetadata, options, q + 1);
                if (message.paymentExtendedMetadata != null && Object.hasOwnProperty.call(message, "paymentExtendedMetadata"))
                    object.paymentExtendedMetadata = $root.proto.Message.PaymentExtendedMetadata.toObject(message.paymentExtendedMetadata, options, q + 1);
                return object;
            };

            /**
             * Converts this ExtendedTextMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.ExtendedTextMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ExtendedTextMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ExtendedTextMessage
             * @function getTypeUrl
             * @memberof proto.Message.ExtendedTextMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ExtendedTextMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.ExtendedTextMessage";
            };

            /**
             * FontType enum.
             * @name proto.Message.ExtendedTextMessage.FontType
             * @enum {number}
             * @property {number} SYSTEM=0 SYSTEM value
             * @property {number} SYSTEM_TEXT=1 SYSTEM_TEXT value
             * @property {number} FB_SCRIPT=2 FB_SCRIPT value
             * @property {number} SYSTEM_BOLD=6 SYSTEM_BOLD value
             * @property {number} MORNINGBREEZE_REGULAR=7 MORNINGBREEZE_REGULAR value
             * @property {number} CALISTOGA_REGULAR=8 CALISTOGA_REGULAR value
             * @property {number} EXO2_EXTRABOLD=9 EXO2_EXTRABOLD value
             * @property {number} COURIERPRIME_BOLD=10 COURIERPRIME_BOLD value
             */
            ExtendedTextMessage.FontType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "SYSTEM"] = 0;
                values[valuesById[1] = "SYSTEM_TEXT"] = 1;
                values[valuesById[2] = "FB_SCRIPT"] = 2;
                values[valuesById[6] = "SYSTEM_BOLD"] = 6;
                values[valuesById[7] = "MORNINGBREEZE_REGULAR"] = 7;
                values[valuesById[8] = "CALISTOGA_REGULAR"] = 8;
                values[valuesById[9] = "EXO2_EXTRABOLD"] = 9;
                values[valuesById[10] = "COURIERPRIME_BOLD"] = 10;
                return values;
            })();

            /**
             * InviteLinkGroupType enum.
             * @name proto.Message.ExtendedTextMessage.InviteLinkGroupType
             * @enum {number}
             * @property {number} DEFAULT=0 DEFAULT value
             * @property {number} PARENT=1 PARENT value
             * @property {number} SUB=2 SUB value
             * @property {number} DEFAULT_SUB=3 DEFAULT_SUB value
             */
            ExtendedTextMessage.InviteLinkGroupType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "DEFAULT"] = 0;
                values[valuesById[1] = "PARENT"] = 1;
                values[valuesById[2] = "SUB"] = 2;
                values[valuesById[3] = "DEFAULT_SUB"] = 3;
                return values;
            })();

            /**
             * PreviewType enum.
             * @name proto.Message.ExtendedTextMessage.PreviewType
             * @enum {number}
             * @property {number} NONE=0 NONE value
             * @property {number} VIDEO=1 VIDEO value
             * @property {number} PLACEHOLDER=4 PLACEHOLDER value
             * @property {number} IMAGE=5 IMAGE value
             * @property {number} PAYMENT_LINKS=6 PAYMENT_LINKS value
             * @property {number} PROFILE=7 PROFILE value
             */
            ExtendedTextMessage.PreviewType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "NONE"] = 0;
                values[valuesById[1] = "VIDEO"] = 1;
                values[valuesById[4] = "PLACEHOLDER"] = 4;
                values[valuesById[5] = "IMAGE"] = 5;
                values[valuesById[6] = "PAYMENT_LINKS"] = 6;
                values[valuesById[7] = "PROFILE"] = 7;
                return values;
            })();

            return ExtendedTextMessage;
        })();

        Message.FullHistorySyncOnDemandConfig = (function() {

            /**
             * Properties of a FullHistorySyncOnDemandConfig.
             * @memberof proto.Message
             * @interface IFullHistorySyncOnDemandConfig
             * @property {number|Long|null} [historyFromTimestamp] FullHistorySyncOnDemandConfig historyFromTimestamp
             * @property {number|null} [historyDurationDays] FullHistorySyncOnDemandConfig historyDurationDays
             */

            /**
             * Constructs a new FullHistorySyncOnDemandConfig.
             * @memberof proto.Message
             * @classdesc Represents a FullHistorySyncOnDemandConfig.
             * @implements IFullHistorySyncOnDemandConfig
             * @constructor
             * @param {proto.Message.IFullHistorySyncOnDemandConfig=} [properties] Properties to set
             */
            function FullHistorySyncOnDemandConfig(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * FullHistorySyncOnDemandConfig historyFromTimestamp.
             * @member {number|Long|null|undefined} historyFromTimestamp
             * @memberof proto.Message.FullHistorySyncOnDemandConfig
             * @instance
             */
            FullHistorySyncOnDemandConfig.prototype.historyFromTimestamp = null;

            /**
             * FullHistorySyncOnDemandConfig historyDurationDays.
             * @member {number|null|undefined} historyDurationDays
             * @memberof proto.Message.FullHistorySyncOnDemandConfig
             * @instance
             */
            FullHistorySyncOnDemandConfig.prototype.historyDurationDays = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(FullHistorySyncOnDemandConfig.prototype, "_historyFromTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["historyFromTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(FullHistorySyncOnDemandConfig.prototype, "_historyDurationDays", {
                get: $util.oneOfGetter($oneOfFields = ["historyDurationDays"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new FullHistorySyncOnDemandConfig instance using the specified properties.
             * @function create
             * @memberof proto.Message.FullHistorySyncOnDemandConfig
             * @static
             * @param {proto.Message.IFullHistorySyncOnDemandConfig=} [properties] Properties to set
             * @returns {proto.Message.FullHistorySyncOnDemandConfig} FullHistorySyncOnDemandConfig instance
             */
            FullHistorySyncOnDemandConfig.create = function create(properties) {
                return new FullHistorySyncOnDemandConfig(properties);
            };

            /**
             * Encodes the specified FullHistorySyncOnDemandConfig message. Does not implicitly {@link proto.Message.FullHistorySyncOnDemandConfig.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.FullHistorySyncOnDemandConfig
             * @static
             * @param {proto.Message.IFullHistorySyncOnDemandConfig} message FullHistorySyncOnDemandConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FullHistorySyncOnDemandConfig.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.historyFromTimestamp != null && Object.hasOwnProperty.call(message, "historyFromTimestamp"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.historyFromTimestamp);
                if (message.historyDurationDays != null && Object.hasOwnProperty.call(message, "historyDurationDays"))
                    writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.historyDurationDays);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified FullHistorySyncOnDemandConfig message, length delimited. Does not implicitly {@link proto.Message.FullHistorySyncOnDemandConfig.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.FullHistorySyncOnDemandConfig
             * @static
             * @param {proto.Message.IFullHistorySyncOnDemandConfig} message FullHistorySyncOnDemandConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FullHistorySyncOnDemandConfig.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a FullHistorySyncOnDemandConfig message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.FullHistorySyncOnDemandConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.FullHistorySyncOnDemandConfig} FullHistorySyncOnDemandConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FullHistorySyncOnDemandConfig.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.historyFromTimestamp = reader.uint64();
                            message._historyFromTimestamp = "historyFromTimestamp";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.historyDurationDays = reader.uint32();
                            message._historyDurationDays = "historyDurationDays";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a FullHistorySyncOnDemandConfig message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.FullHistorySyncOnDemandConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.FullHistorySyncOnDemandConfig} FullHistorySyncOnDemandConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FullHistorySyncOnDemandConfig.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FullHistorySyncOnDemandConfig message.
             * @function verify
             * @memberof proto.Message.FullHistorySyncOnDemandConfig
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FullHistorySyncOnDemandConfig.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.historyFromTimestamp != null && Object.hasOwnProperty.call(message, "historyFromTimestamp")) {
                    properties._historyFromTimestamp = 1;
                    if (!$util.isInteger(message.historyFromTimestamp) && !(message.historyFromTimestamp && $util.isInteger(message.historyFromTimestamp.low) && $util.isInteger(message.historyFromTimestamp.high)))
                        return "historyFromTimestamp: integer|Long expected";
                }
                if (message.historyDurationDays != null && Object.hasOwnProperty.call(message, "historyDurationDays")) {
                    properties._historyDurationDays = 1;
                    if (!$util.isInteger(message.historyDurationDays))
                        return "historyDurationDays: integer expected";
                }
                return null;
            };

            /**
             * Creates a FullHistorySyncOnDemandConfig message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.FullHistorySyncOnDemandConfig
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.FullHistorySyncOnDemandConfig} FullHistorySyncOnDemandConfig
             */
            FullHistorySyncOnDemandConfig.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.FullHistorySyncOnDemandConfig: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.historyFromTimestamp != null)
                    if ($util.Long)
                        message.historyFromTimestamp = $util.Long.fromValue(object.historyFromTimestamp, true);
                    else if (typeof object.historyFromTimestamp === "string")
                        message.historyFromTimestamp = parseInt(object.historyFromTimestamp, 10);
                    else if (typeof object.historyFromTimestamp === "number")
                        message.historyFromTimestamp = object.historyFromTimestamp;
                    else if (typeof object.historyFromTimestamp === "object")
                        message.historyFromTimestamp = new $util.LongBits(object.historyFromTimestamp.low >>> 0, object.historyFromTimestamp.high >>> 0).toNumber(true);
                if (object.historyDurationDays != null)
                    message.historyDurationDays = object.historyDurationDays >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a FullHistorySyncOnDemandConfig message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.FullHistorySyncOnDemandConfig
             * @static
             * @param {proto.Message.FullHistorySyncOnDemandConfig} message FullHistorySyncOnDemandConfig
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FullHistorySyncOnDemandConfig.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.historyFromTimestamp != null && Object.hasOwnProperty.call(message, "historyFromTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.historyFromTimestamp = typeof message.historyFromTimestamp === "number" ? BigInt(message.historyFromTimestamp) : $util.Long.fromBits(message.historyFromTimestamp.low >>> 0, message.historyFromTimestamp.high >>> 0, true).toBigInt();
                    else if (typeof message.historyFromTimestamp === "number")
                        object.historyFromTimestamp = options.longs === String ? String(message.historyFromTimestamp) : message.historyFromTimestamp;
                    else
                        object.historyFromTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.historyFromTimestamp) : options.longs === Number ? new $util.LongBits(message.historyFromTimestamp.low >>> 0, message.historyFromTimestamp.high >>> 0).toNumber(true) : message.historyFromTimestamp;
                if (message.historyDurationDays != null && Object.hasOwnProperty.call(message, "historyDurationDays"))
                    object.historyDurationDays = message.historyDurationDays;
                return object;
            };

            /**
             * Converts this FullHistorySyncOnDemandConfig to JSON.
             * @function toJSON
             * @memberof proto.Message.FullHistorySyncOnDemandConfig
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FullHistorySyncOnDemandConfig.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for FullHistorySyncOnDemandConfig
             * @function getTypeUrl
             * @memberof proto.Message.FullHistorySyncOnDemandConfig
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            FullHistorySyncOnDemandConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.FullHistorySyncOnDemandConfig";
            };

            return FullHistorySyncOnDemandConfig;
        })();

        Message.FullHistorySyncOnDemandRequestMetadata = (function() {

            /**
             * Properties of a FullHistorySyncOnDemandRequestMetadata.
             * @memberof proto.Message
             * @interface IFullHistorySyncOnDemandRequestMetadata
             * @property {string|null} [requestId] FullHistorySyncOnDemandRequestMetadata requestId
             * @property {string|null} [businessProduct] FullHistorySyncOnDemandRequestMetadata businessProduct
             * @property {Uint8Array|null} [opaqueClientData] FullHistorySyncOnDemandRequestMetadata opaqueClientData
             */

            /**
             * Constructs a new FullHistorySyncOnDemandRequestMetadata.
             * @memberof proto.Message
             * @classdesc Represents a FullHistorySyncOnDemandRequestMetadata.
             * @implements IFullHistorySyncOnDemandRequestMetadata
             * @constructor
             * @param {proto.Message.IFullHistorySyncOnDemandRequestMetadata=} [properties] Properties to set
             */
            function FullHistorySyncOnDemandRequestMetadata(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * FullHistorySyncOnDemandRequestMetadata requestId.
             * @member {string|null|undefined} requestId
             * @memberof proto.Message.FullHistorySyncOnDemandRequestMetadata
             * @instance
             */
            FullHistorySyncOnDemandRequestMetadata.prototype.requestId = null;

            /**
             * FullHistorySyncOnDemandRequestMetadata businessProduct.
             * @member {string|null|undefined} businessProduct
             * @memberof proto.Message.FullHistorySyncOnDemandRequestMetadata
             * @instance
             */
            FullHistorySyncOnDemandRequestMetadata.prototype.businessProduct = null;

            /**
             * FullHistorySyncOnDemandRequestMetadata opaqueClientData.
             * @member {Uint8Array|null|undefined} opaqueClientData
             * @memberof proto.Message.FullHistorySyncOnDemandRequestMetadata
             * @instance
             */
            FullHistorySyncOnDemandRequestMetadata.prototype.opaqueClientData = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(FullHistorySyncOnDemandRequestMetadata.prototype, "_requestId", {
                get: $util.oneOfGetter($oneOfFields = ["requestId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(FullHistorySyncOnDemandRequestMetadata.prototype, "_businessProduct", {
                get: $util.oneOfGetter($oneOfFields = ["businessProduct"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(FullHistorySyncOnDemandRequestMetadata.prototype, "_opaqueClientData", {
                get: $util.oneOfGetter($oneOfFields = ["opaqueClientData"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new FullHistorySyncOnDemandRequestMetadata instance using the specified properties.
             * @function create
             * @memberof proto.Message.FullHistorySyncOnDemandRequestMetadata
             * @static
             * @param {proto.Message.IFullHistorySyncOnDemandRequestMetadata=} [properties] Properties to set
             * @returns {proto.Message.FullHistorySyncOnDemandRequestMetadata} FullHistorySyncOnDemandRequestMetadata instance
             */
            FullHistorySyncOnDemandRequestMetadata.create = function create(properties) {
                return new FullHistorySyncOnDemandRequestMetadata(properties);
            };

            /**
             * Encodes the specified FullHistorySyncOnDemandRequestMetadata message. Does not implicitly {@link proto.Message.FullHistorySyncOnDemandRequestMetadata.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.FullHistorySyncOnDemandRequestMetadata
             * @static
             * @param {proto.Message.IFullHistorySyncOnDemandRequestMetadata} message FullHistorySyncOnDemandRequestMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FullHistorySyncOnDemandRequestMetadata.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.requestId != null && Object.hasOwnProperty.call(message, "requestId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.requestId);
                if (message.businessProduct != null && Object.hasOwnProperty.call(message, "businessProduct"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.businessProduct);
                if (message.opaqueClientData != null && Object.hasOwnProperty.call(message, "opaqueClientData"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.opaqueClientData);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified FullHistorySyncOnDemandRequestMetadata message, length delimited. Does not implicitly {@link proto.Message.FullHistorySyncOnDemandRequestMetadata.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.FullHistorySyncOnDemandRequestMetadata
             * @static
             * @param {proto.Message.IFullHistorySyncOnDemandRequestMetadata} message FullHistorySyncOnDemandRequestMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FullHistorySyncOnDemandRequestMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a FullHistorySyncOnDemandRequestMetadata message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.FullHistorySyncOnDemandRequestMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.FullHistorySyncOnDemandRequestMetadata} FullHistorySyncOnDemandRequestMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FullHistorySyncOnDemandRequestMetadata.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.requestId = reader.stringVerify();
                            message._requestId = "requestId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.businessProduct = reader.stringVerify();
                            message._businessProduct = "businessProduct";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.opaqueClientData = reader.bytes();
                            message._opaqueClientData = "opaqueClientData";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a FullHistorySyncOnDemandRequestMetadata message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.FullHistorySyncOnDemandRequestMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.FullHistorySyncOnDemandRequestMetadata} FullHistorySyncOnDemandRequestMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FullHistorySyncOnDemandRequestMetadata.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FullHistorySyncOnDemandRequestMetadata message.
             * @function verify
             * @memberof proto.Message.FullHistorySyncOnDemandRequestMetadata
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FullHistorySyncOnDemandRequestMetadata.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.requestId != null && Object.hasOwnProperty.call(message, "requestId")) {
                    properties._requestId = 1;
                    if (!$util.isString(message.requestId))
                        return "requestId: string expected";
                }
                if (message.businessProduct != null && Object.hasOwnProperty.call(message, "businessProduct")) {
                    properties._businessProduct = 1;
                    if (!$util.isString(message.businessProduct))
                        return "businessProduct: string expected";
                }
                if (message.opaqueClientData != null && Object.hasOwnProperty.call(message, "opaqueClientData")) {
                    properties._opaqueClientData = 1;
                    if (!(message.opaqueClientData && typeof message.opaqueClientData.length === "number" || $util.isString(message.opaqueClientData)))
                        return "opaqueClientData: buffer expected";
                }
                return null;
            };

            /**
             * Creates a FullHistorySyncOnDemandRequestMetadata message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.FullHistorySyncOnDemandRequestMetadata
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.FullHistorySyncOnDemandRequestMetadata} FullHistorySyncOnDemandRequestMetadata
             */
            FullHistorySyncOnDemandRequestMetadata.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.FullHistorySyncOnDemandRequestMetadata: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.requestId != null)
                    message.requestId = String(object.requestId);
                if (object.businessProduct != null)
                    message.businessProduct = String(object.businessProduct);
                if (object.opaqueClientData != null)
                    if (typeof object.opaqueClientData === "string")
                        $util.base64.decode(object.opaqueClientData, message.opaqueClientData = $util.newBuffer($util.base64.length(object.opaqueClientData)), 0);
                    else if (object.opaqueClientData.length >= 0)
                        message.opaqueClientData = object.opaqueClientData;
                return message;
            };

            /**
             * Creates a plain object from a FullHistorySyncOnDemandRequestMetadata message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.FullHistorySyncOnDemandRequestMetadata
             * @static
             * @param {proto.Message.FullHistorySyncOnDemandRequestMetadata} message FullHistorySyncOnDemandRequestMetadata
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FullHistorySyncOnDemandRequestMetadata.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.requestId != null && Object.hasOwnProperty.call(message, "requestId"))
                    object.requestId = message.requestId;
                if (message.businessProduct != null && Object.hasOwnProperty.call(message, "businessProduct"))
                    object.businessProduct = message.businessProduct;
                if (message.opaqueClientData != null && Object.hasOwnProperty.call(message, "opaqueClientData"))
                    object.opaqueClientData = options.bytes === String ? $util.base64.encode(message.opaqueClientData, 0, message.opaqueClientData.length) : options.bytes === Array ? Array.prototype.slice.call(message.opaqueClientData) : message.opaqueClientData;
                return object;
            };

            /**
             * Converts this FullHistorySyncOnDemandRequestMetadata to JSON.
             * @function toJSON
             * @memberof proto.Message.FullHistorySyncOnDemandRequestMetadata
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FullHistorySyncOnDemandRequestMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for FullHistorySyncOnDemandRequestMetadata
             * @function getTypeUrl
             * @memberof proto.Message.FullHistorySyncOnDemandRequestMetadata
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            FullHistorySyncOnDemandRequestMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.FullHistorySyncOnDemandRequestMetadata";
            };

            return FullHistorySyncOnDemandRequestMetadata;
        })();

        Message.FutureProofMessage = (function() {

            /**
             * Properties of a FutureProofMessage.
             * @memberof proto.Message
             * @interface IFutureProofMessage
             * @property {proto.IMessage|null} [message] FutureProofMessage message
             */

            /**
             * Constructs a new FutureProofMessage.
             * @memberof proto.Message
             * @classdesc Represents a FutureProofMessage.
             * @implements IFutureProofMessage
             * @constructor
             * @param {proto.Message.IFutureProofMessage=} [properties] Properties to set
             */
            function FutureProofMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * FutureProofMessage message.
             * @member {proto.IMessage|null|undefined} message
             * @memberof proto.Message.FutureProofMessage
             * @instance
             */
            FutureProofMessage.prototype.message = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(FutureProofMessage.prototype, "_message", {
                get: $util.oneOfGetter($oneOfFields = ["message"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new FutureProofMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.FutureProofMessage
             * @static
             * @param {proto.Message.IFutureProofMessage=} [properties] Properties to set
             * @returns {proto.Message.FutureProofMessage} FutureProofMessage instance
             */
            FutureProofMessage.create = function create(properties) {
                return new FutureProofMessage(properties);
            };

            /**
             * Encodes the specified FutureProofMessage message. Does not implicitly {@link proto.Message.FutureProofMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.FutureProofMessage
             * @static
             * @param {proto.Message.IFutureProofMessage} message FutureProofMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FutureProofMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                    $root.proto.Message.encode(message.message, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified FutureProofMessage message, length delimited. Does not implicitly {@link proto.Message.FutureProofMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.FutureProofMessage
             * @static
             * @param {proto.Message.IFutureProofMessage} message FutureProofMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FutureProofMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a FutureProofMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.FutureProofMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.FutureProofMessage} FutureProofMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FutureProofMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.message = $root.proto.Message.decode(reader, reader.uint32(), undefined, q + 1, message.message);
                            message._message = "message";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a FutureProofMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.FutureProofMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.FutureProofMessage} FutureProofMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FutureProofMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FutureProofMessage message.
             * @function verify
             * @memberof proto.Message.FutureProofMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FutureProofMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.message != null && Object.hasOwnProperty.call(message, "message")) {
                    properties._message = 1;
                    {
                        var error = $root.proto.Message.verify(message.message, q + 1);
                        if (error)
                            return "message." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a FutureProofMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.FutureProofMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.FutureProofMessage} FutureProofMessage
             */
            FutureProofMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.FutureProofMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.message != null) {
                    if (!$util.isObject(object.message))
                        throw TypeError(".proto.Message.FutureProofMessage.message: object expected");
                    message.message = $root.proto.Message.fromObject(object.message, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a FutureProofMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.FutureProofMessage
             * @static
             * @param {proto.Message.FutureProofMessage} message FutureProofMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FutureProofMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                    object.message = $root.proto.Message.toObject(message.message, options, q + 1);
                return object;
            };

            /**
             * Converts this FutureProofMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.FutureProofMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FutureProofMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for FutureProofMessage
             * @function getTypeUrl
             * @memberof proto.Message.FutureProofMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            FutureProofMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.FutureProofMessage";
            };

            return FutureProofMessage;
        })();

        Message.GroupInviteMessage = (function() {

            /**
             * Properties of a GroupInviteMessage.
             * @memberof proto.Message
             * @interface IGroupInviteMessage
             * @property {string|null} [groupJid] GroupInviteMessage groupJid
             * @property {string|null} [inviteCode] GroupInviteMessage inviteCode
             * @property {number|Long|null} [inviteExpiration] GroupInviteMessage inviteExpiration
             * @property {string|null} [groupName] GroupInviteMessage groupName
             * @property {Uint8Array|null} [jpegThumbnail] GroupInviteMessage jpegThumbnail
             * @property {string|null} [caption] GroupInviteMessage caption
             * @property {proto.IContextInfo|null} [contextInfo] GroupInviteMessage contextInfo
             * @property {proto.Message.GroupInviteMessage.GroupType|null} [groupType] GroupInviteMessage groupType
             */

            /**
             * Constructs a new GroupInviteMessage.
             * @memberof proto.Message
             * @classdesc Represents a GroupInviteMessage.
             * @implements IGroupInviteMessage
             * @constructor
             * @param {proto.Message.IGroupInviteMessage=} [properties] Properties to set
             */
            function GroupInviteMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * GroupInviteMessage groupJid.
             * @member {string|null|undefined} groupJid
             * @memberof proto.Message.GroupInviteMessage
             * @instance
             */
            GroupInviteMessage.prototype.groupJid = null;

            /**
             * GroupInviteMessage inviteCode.
             * @member {string|null|undefined} inviteCode
             * @memberof proto.Message.GroupInviteMessage
             * @instance
             */
            GroupInviteMessage.prototype.inviteCode = null;

            /**
             * GroupInviteMessage inviteExpiration.
             * @member {number|Long|null|undefined} inviteExpiration
             * @memberof proto.Message.GroupInviteMessage
             * @instance
             */
            GroupInviteMessage.prototype.inviteExpiration = null;

            /**
             * GroupInviteMessage groupName.
             * @member {string|null|undefined} groupName
             * @memberof proto.Message.GroupInviteMessage
             * @instance
             */
            GroupInviteMessage.prototype.groupName = null;

            /**
             * GroupInviteMessage jpegThumbnail.
             * @member {Uint8Array|null|undefined} jpegThumbnail
             * @memberof proto.Message.GroupInviteMessage
             * @instance
             */
            GroupInviteMessage.prototype.jpegThumbnail = null;

            /**
             * GroupInviteMessage caption.
             * @member {string|null|undefined} caption
             * @memberof proto.Message.GroupInviteMessage
             * @instance
             */
            GroupInviteMessage.prototype.caption = null;

            /**
             * GroupInviteMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.GroupInviteMessage
             * @instance
             */
            GroupInviteMessage.prototype.contextInfo = null;

            /**
             * GroupInviteMessage groupType.
             * @member {proto.Message.GroupInviteMessage.GroupType|null|undefined} groupType
             * @memberof proto.Message.GroupInviteMessage
             * @instance
             */
            GroupInviteMessage.prototype.groupType = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(GroupInviteMessage.prototype, "_groupJid", {
                get: $util.oneOfGetter($oneOfFields = ["groupJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(GroupInviteMessage.prototype, "_inviteCode", {
                get: $util.oneOfGetter($oneOfFields = ["inviteCode"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(GroupInviteMessage.prototype, "_inviteExpiration", {
                get: $util.oneOfGetter($oneOfFields = ["inviteExpiration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(GroupInviteMessage.prototype, "_groupName", {
                get: $util.oneOfGetter($oneOfFields = ["groupName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(GroupInviteMessage.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(GroupInviteMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(GroupInviteMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(GroupInviteMessage.prototype, "_groupType", {
                get: $util.oneOfGetter($oneOfFields = ["groupType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new GroupInviteMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.GroupInviteMessage
             * @static
             * @param {proto.Message.IGroupInviteMessage=} [properties] Properties to set
             * @returns {proto.Message.GroupInviteMessage} GroupInviteMessage instance
             */
            GroupInviteMessage.create = function create(properties) {
                return new GroupInviteMessage(properties);
            };

            /**
             * Encodes the specified GroupInviteMessage message. Does not implicitly {@link proto.Message.GroupInviteMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.GroupInviteMessage
             * @static
             * @param {proto.Message.IGroupInviteMessage} message GroupInviteMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GroupInviteMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.groupJid != null && Object.hasOwnProperty.call(message, "groupJid"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.groupJid);
                if (message.inviteCode != null && Object.hasOwnProperty.call(message, "inviteCode"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.inviteCode);
                if (message.inviteExpiration != null && Object.hasOwnProperty.call(message, "inviteExpiration"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.inviteExpiration);
                if (message.groupName != null && Object.hasOwnProperty.call(message, "groupName"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.groupName);
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.jpegThumbnail);
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.caption);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
                if (message.groupType != null && Object.hasOwnProperty.call(message, "groupType"))
                    writer.uint32(/* id 8, wireType 0 =*/64).int32(message.groupType);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GroupInviteMessage message, length delimited. Does not implicitly {@link proto.Message.GroupInviteMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.GroupInviteMessage
             * @static
             * @param {proto.Message.IGroupInviteMessage} message GroupInviteMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GroupInviteMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a GroupInviteMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.GroupInviteMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.GroupInviteMessage} GroupInviteMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GroupInviteMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.groupJid = reader.stringVerify();
                            message._groupJid = "groupJid";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.inviteCode = reader.stringVerify();
                            message._inviteCode = "inviteCode";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.inviteExpiration = reader.int64();
                            message._inviteExpiration = "inviteExpiration";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.groupName = reader.stringVerify();
                            message._groupName = "groupName";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.jpegThumbnail = reader.bytes();
                            message._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.caption = reader.stringVerify();
                            message._caption = "caption";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            message.groupType = reader.int32();
                            message._groupType = "groupType";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a GroupInviteMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.GroupInviteMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.GroupInviteMessage} GroupInviteMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GroupInviteMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GroupInviteMessage message.
             * @function verify
             * @memberof proto.Message.GroupInviteMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GroupInviteMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.groupJid != null && Object.hasOwnProperty.call(message, "groupJid")) {
                    properties._groupJid = 1;
                    if (!$util.isString(message.groupJid))
                        return "groupJid: string expected";
                }
                if (message.inviteCode != null && Object.hasOwnProperty.call(message, "inviteCode")) {
                    properties._inviteCode = 1;
                    if (!$util.isString(message.inviteCode))
                        return "inviteCode: string expected";
                }
                if (message.inviteExpiration != null && Object.hasOwnProperty.call(message, "inviteExpiration")) {
                    properties._inviteExpiration = 1;
                    if (!$util.isInteger(message.inviteExpiration) && !(message.inviteExpiration && $util.isInteger(message.inviteExpiration.low) && $util.isInteger(message.inviteExpiration.high)))
                        return "inviteExpiration: integer|Long expected";
                }
                if (message.groupName != null && Object.hasOwnProperty.call(message, "groupName")) {
                    properties._groupName = 1;
                    if (!$util.isString(message.groupName))
                        return "groupName: string expected";
                }
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail")) {
                    properties._jpegThumbnail = 1;
                    if (!(message.jpegThumbnail && typeof message.jpegThumbnail.length === "number" || $util.isString(message.jpegThumbnail)))
                        return "jpegThumbnail: buffer expected";
                }
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption")) {
                    properties._caption = 1;
                    if (!$util.isString(message.caption))
                        return "caption: string expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.groupType != null && Object.hasOwnProperty.call(message, "groupType")) {
                    properties._groupType = 1;
                    if (typeof message.groupType !== "number" || (message.groupType | 0) !== message.groupType)
                        return "groupType: enum value expected";
                }
                return null;
            };

            /**
             * Creates a GroupInviteMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.GroupInviteMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.GroupInviteMessage} GroupInviteMessage
             */
            GroupInviteMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.GroupInviteMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.groupJid != null)
                    message.groupJid = String(object.groupJid);
                if (object.inviteCode != null)
                    message.inviteCode = String(object.inviteCode);
                if (object.inviteExpiration != null)
                    if ($util.Long)
                        message.inviteExpiration = $util.Long.fromValue(object.inviteExpiration, false);
                    else if (typeof object.inviteExpiration === "string")
                        message.inviteExpiration = parseInt(object.inviteExpiration, 10);
                    else if (typeof object.inviteExpiration === "number")
                        message.inviteExpiration = object.inviteExpiration;
                    else if (typeof object.inviteExpiration === "object")
                        message.inviteExpiration = new $util.LongBits(object.inviteExpiration.low >>> 0, object.inviteExpiration.high >>> 0).toNumber();
                if (object.groupName != null)
                    message.groupName = String(object.groupName);
                if (object.jpegThumbnail != null)
                    if (typeof object.jpegThumbnail === "string")
                        $util.base64.decode(object.jpegThumbnail, message.jpegThumbnail = $util.newBuffer($util.base64.length(object.jpegThumbnail)), 0);
                    else if (object.jpegThumbnail.length >= 0)
                        message.jpegThumbnail = object.jpegThumbnail;
                if (object.caption != null)
                    message.caption = String(object.caption);
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.GroupInviteMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                switch (object.groupType) {
                case "DEFAULT":
                case 0:
                    message.groupType = 0;
                    break;
                case "PARENT":
                case 1:
                    message.groupType = 1;
                    break;
                default:
                    if (typeof object.groupType === "number" && (object.groupType | 0) === object.groupType)
                        message.groupType = object.groupType;
                }
                return message;
            };

            /**
             * Creates a plain object from a GroupInviteMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.GroupInviteMessage
             * @static
             * @param {proto.Message.GroupInviteMessage} message GroupInviteMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GroupInviteMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.groupJid != null && Object.hasOwnProperty.call(message, "groupJid"))
                    object.groupJid = message.groupJid;
                if (message.inviteCode != null && Object.hasOwnProperty.call(message, "inviteCode"))
                    object.inviteCode = message.inviteCode;
                if (message.inviteExpiration != null && Object.hasOwnProperty.call(message, "inviteExpiration"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.inviteExpiration = typeof message.inviteExpiration === "number" ? BigInt(message.inviteExpiration) : $util.Long.fromBits(message.inviteExpiration.low >>> 0, message.inviteExpiration.high >>> 0, false).toBigInt();
                    else if (typeof message.inviteExpiration === "number")
                        object.inviteExpiration = options.longs === String ? String(message.inviteExpiration) : message.inviteExpiration;
                    else
                        object.inviteExpiration = options.longs === String ? $util.Long.prototype.toString.call(message.inviteExpiration) : options.longs === Number ? new $util.LongBits(message.inviteExpiration.low >>> 0, message.inviteExpiration.high >>> 0).toNumber() : message.inviteExpiration;
                if (message.groupName != null && Object.hasOwnProperty.call(message, "groupName"))
                    object.groupName = message.groupName;
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    object.jpegThumbnail = options.bytes === String ? $util.base64.encode(message.jpegThumbnail, 0, message.jpegThumbnail.length) : options.bytes === Array ? Array.prototype.slice.call(message.jpegThumbnail) : message.jpegThumbnail;
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    object.caption = message.caption;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.groupType != null && Object.hasOwnProperty.call(message, "groupType"))
                    object.groupType = options.enums === String ? $root.proto.Message.GroupInviteMessage.GroupType[message.groupType] === undefined ? message.groupType : $root.proto.Message.GroupInviteMessage.GroupType[message.groupType] : message.groupType;
                return object;
            };

            /**
             * Converts this GroupInviteMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.GroupInviteMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GroupInviteMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for GroupInviteMessage
             * @function getTypeUrl
             * @memberof proto.Message.GroupInviteMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            GroupInviteMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.GroupInviteMessage";
            };

            /**
             * GroupType enum.
             * @name proto.Message.GroupInviteMessage.GroupType
             * @enum {number}
             * @property {number} DEFAULT=0 DEFAULT value
             * @property {number} PARENT=1 PARENT value
             */
            GroupInviteMessage.GroupType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "DEFAULT"] = 0;
                values[valuesById[1] = "PARENT"] = 1;
                return values;
            })();

            return GroupInviteMessage;
        })();

        Message.HighlyStructuredMessage = (function() {

            /**
             * Properties of a HighlyStructuredMessage.
             * @memberof proto.Message
             * @interface IHighlyStructuredMessage
             * @property {string|null} [namespace] HighlyStructuredMessage namespace
             * @property {string|null} [elementName] HighlyStructuredMessage elementName
             * @property {Array.<string>|null} [params] HighlyStructuredMessage params
             * @property {string|null} [fallbackLg] HighlyStructuredMessage fallbackLg
             * @property {string|null} [fallbackLc] HighlyStructuredMessage fallbackLc
             * @property {Array.<proto.Message.HighlyStructuredMessage.IHSMLocalizableParameter>|null} [localizableParams] HighlyStructuredMessage localizableParams
             * @property {string|null} [deterministicLg] HighlyStructuredMessage deterministicLg
             * @property {string|null} [deterministicLc] HighlyStructuredMessage deterministicLc
             * @property {proto.Message.ITemplateMessage|null} [hydratedHsm] HighlyStructuredMessage hydratedHsm
             */

            /**
             * Constructs a new HighlyStructuredMessage.
             * @memberof proto.Message
             * @classdesc Represents a HighlyStructuredMessage.
             * @implements IHighlyStructuredMessage
             * @constructor
             * @param {proto.Message.IHighlyStructuredMessage=} [properties] Properties to set
             */
            function HighlyStructuredMessage(properties) {
                this.params = [];
                this.localizableParams = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * HighlyStructuredMessage namespace.
             * @member {string|null|undefined} namespace
             * @memberof proto.Message.HighlyStructuredMessage
             * @instance
             */
            HighlyStructuredMessage.prototype.namespace = null;

            /**
             * HighlyStructuredMessage elementName.
             * @member {string|null|undefined} elementName
             * @memberof proto.Message.HighlyStructuredMessage
             * @instance
             */
            HighlyStructuredMessage.prototype.elementName = null;

            /**
             * HighlyStructuredMessage params.
             * @member {Array.<string>} params
             * @memberof proto.Message.HighlyStructuredMessage
             * @instance
             */
            HighlyStructuredMessage.prototype.params = $util.emptyArray;

            /**
             * HighlyStructuredMessage fallbackLg.
             * @member {string|null|undefined} fallbackLg
             * @memberof proto.Message.HighlyStructuredMessage
             * @instance
             */
            HighlyStructuredMessage.prototype.fallbackLg = null;

            /**
             * HighlyStructuredMessage fallbackLc.
             * @member {string|null|undefined} fallbackLc
             * @memberof proto.Message.HighlyStructuredMessage
             * @instance
             */
            HighlyStructuredMessage.prototype.fallbackLc = null;

            /**
             * HighlyStructuredMessage localizableParams.
             * @member {Array.<proto.Message.HighlyStructuredMessage.IHSMLocalizableParameter>} localizableParams
             * @memberof proto.Message.HighlyStructuredMessage
             * @instance
             */
            HighlyStructuredMessage.prototype.localizableParams = $util.emptyArray;

            /**
             * HighlyStructuredMessage deterministicLg.
             * @member {string|null|undefined} deterministicLg
             * @memberof proto.Message.HighlyStructuredMessage
             * @instance
             */
            HighlyStructuredMessage.prototype.deterministicLg = null;

            /**
             * HighlyStructuredMessage deterministicLc.
             * @member {string|null|undefined} deterministicLc
             * @memberof proto.Message.HighlyStructuredMessage
             * @instance
             */
            HighlyStructuredMessage.prototype.deterministicLc = null;

            /**
             * HighlyStructuredMessage hydratedHsm.
             * @member {proto.Message.ITemplateMessage|null|undefined} hydratedHsm
             * @memberof proto.Message.HighlyStructuredMessage
             * @instance
             */
            HighlyStructuredMessage.prototype.hydratedHsm = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HighlyStructuredMessage.prototype, "_namespace", {
                get: $util.oneOfGetter($oneOfFields = ["namespace"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HighlyStructuredMessage.prototype, "_elementName", {
                get: $util.oneOfGetter($oneOfFields = ["elementName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HighlyStructuredMessage.prototype, "_fallbackLg", {
                get: $util.oneOfGetter($oneOfFields = ["fallbackLg"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HighlyStructuredMessage.prototype, "_fallbackLc", {
                get: $util.oneOfGetter($oneOfFields = ["fallbackLc"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HighlyStructuredMessage.prototype, "_deterministicLg", {
                get: $util.oneOfGetter($oneOfFields = ["deterministicLg"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HighlyStructuredMessage.prototype, "_deterministicLc", {
                get: $util.oneOfGetter($oneOfFields = ["deterministicLc"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HighlyStructuredMessage.prototype, "_hydratedHsm", {
                get: $util.oneOfGetter($oneOfFields = ["hydratedHsm"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new HighlyStructuredMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.HighlyStructuredMessage
             * @static
             * @param {proto.Message.IHighlyStructuredMessage=} [properties] Properties to set
             * @returns {proto.Message.HighlyStructuredMessage} HighlyStructuredMessage instance
             */
            HighlyStructuredMessage.create = function create(properties) {
                return new HighlyStructuredMessage(properties);
            };

            /**
             * Encodes the specified HighlyStructuredMessage message. Does not implicitly {@link proto.Message.HighlyStructuredMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.HighlyStructuredMessage
             * @static
             * @param {proto.Message.IHighlyStructuredMessage} message HighlyStructuredMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HighlyStructuredMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.namespace != null && Object.hasOwnProperty.call(message, "namespace"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.namespace);
                if (message.elementName != null && Object.hasOwnProperty.call(message, "elementName"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.elementName);
                if (message.params != null && message.params.length)
                    for (var i = 0; i < message.params.length; ++i)
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.params[i]);
                if (message.fallbackLg != null && Object.hasOwnProperty.call(message, "fallbackLg"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.fallbackLg);
                if (message.fallbackLc != null && Object.hasOwnProperty.call(message, "fallbackLc"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.fallbackLc);
                if (message.localizableParams != null && message.localizableParams.length)
                    for (var i = 0; i < message.localizableParams.length; ++i)
                        $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.encode(message.localizableParams[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
                if (message.deterministicLg != null && Object.hasOwnProperty.call(message, "deterministicLg"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.deterministicLg);
                if (message.deterministicLc != null && Object.hasOwnProperty.call(message, "deterministicLc"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.deterministicLc);
                if (message.hydratedHsm != null && Object.hasOwnProperty.call(message, "hydratedHsm"))
                    $root.proto.Message.TemplateMessage.encode(message.hydratedHsm, writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified HighlyStructuredMessage message, length delimited. Does not implicitly {@link proto.Message.HighlyStructuredMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.HighlyStructuredMessage
             * @static
             * @param {proto.Message.IHighlyStructuredMessage} message HighlyStructuredMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HighlyStructuredMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a HighlyStructuredMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.HighlyStructuredMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.HighlyStructuredMessage} HighlyStructuredMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HighlyStructuredMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.namespace = reader.stringVerify();
                            message._namespace = "namespace";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.elementName = reader.stringVerify();
                            message._elementName = "elementName";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            if (!(message.params && message.params.length))
                                message.params = [];
                            message.params.push(reader.stringVerify());
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.fallbackLg = reader.stringVerify();
                            message._fallbackLg = "fallbackLg";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.fallbackLc = reader.stringVerify();
                            message._fallbackLc = "fallbackLc";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            if (!(message.localizableParams && message.localizableParams.length))
                                message.localizableParams = [];
                            message.localizableParams.push($root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.deterministicLg = reader.stringVerify();
                            message._deterministicLg = "deterministicLg";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.deterministicLc = reader.stringVerify();
                            message._deterministicLc = "deterministicLc";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            message.hydratedHsm = $root.proto.Message.TemplateMessage.decode(reader, reader.uint32(), undefined, q + 1, message.hydratedHsm);
                            message._hydratedHsm = "hydratedHsm";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a HighlyStructuredMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.HighlyStructuredMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.HighlyStructuredMessage} HighlyStructuredMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HighlyStructuredMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a HighlyStructuredMessage message.
             * @function verify
             * @memberof proto.Message.HighlyStructuredMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            HighlyStructuredMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.namespace != null && Object.hasOwnProperty.call(message, "namespace")) {
                    properties._namespace = 1;
                    if (!$util.isString(message.namespace))
                        return "namespace: string expected";
                }
                if (message.elementName != null && Object.hasOwnProperty.call(message, "elementName")) {
                    properties._elementName = 1;
                    if (!$util.isString(message.elementName))
                        return "elementName: string expected";
                }
                if (message.params != null && Object.hasOwnProperty.call(message, "params")) {
                    if (!Array.isArray(message.params))
                        return "params: array expected";
                    for (var i = 0; i < message.params.length; ++i)
                        if (!$util.isString(message.params[i]))
                            return "params: string[] expected";
                }
                if (message.fallbackLg != null && Object.hasOwnProperty.call(message, "fallbackLg")) {
                    properties._fallbackLg = 1;
                    if (!$util.isString(message.fallbackLg))
                        return "fallbackLg: string expected";
                }
                if (message.fallbackLc != null && Object.hasOwnProperty.call(message, "fallbackLc")) {
                    properties._fallbackLc = 1;
                    if (!$util.isString(message.fallbackLc))
                        return "fallbackLc: string expected";
                }
                if (message.localizableParams != null && Object.hasOwnProperty.call(message, "localizableParams")) {
                    if (!Array.isArray(message.localizableParams))
                        return "localizableParams: array expected";
                    for (var i = 0; i < message.localizableParams.length; ++i) {
                        var error = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.verify(message.localizableParams[i], q + 1);
                        if (error)
                            return "localizableParams." + error;
                    }
                }
                if (message.deterministicLg != null && Object.hasOwnProperty.call(message, "deterministicLg")) {
                    properties._deterministicLg = 1;
                    if (!$util.isString(message.deterministicLg))
                        return "deterministicLg: string expected";
                }
                if (message.deterministicLc != null && Object.hasOwnProperty.call(message, "deterministicLc")) {
                    properties._deterministicLc = 1;
                    if (!$util.isString(message.deterministicLc))
                        return "deterministicLc: string expected";
                }
                if (message.hydratedHsm != null && Object.hasOwnProperty.call(message, "hydratedHsm")) {
                    properties._hydratedHsm = 1;
                    {
                        var error = $root.proto.Message.TemplateMessage.verify(message.hydratedHsm, q + 1);
                        if (error)
                            return "hydratedHsm." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a HighlyStructuredMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.HighlyStructuredMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.HighlyStructuredMessage} HighlyStructuredMessage
             */
            HighlyStructuredMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.HighlyStructuredMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.namespace != null)
                    message.namespace = String(object.namespace);
                if (object.elementName != null)
                    message.elementName = String(object.elementName);
                if (object.params) {
                    if (!Array.isArray(object.params))
                        throw TypeError(".proto.Message.HighlyStructuredMessage.params: array expected");
                    message.params = Array(object.params.length);
                    for (var i = 0; i < object.params.length; ++i)
                        message.params[i] = String(object.params[i]);
                }
                if (object.fallbackLg != null)
                    message.fallbackLg = String(object.fallbackLg);
                if (object.fallbackLc != null)
                    message.fallbackLc = String(object.fallbackLc);
                if (object.localizableParams) {
                    if (!Array.isArray(object.localizableParams))
                        throw TypeError(".proto.Message.HighlyStructuredMessage.localizableParams: array expected");
                    message.localizableParams = Array(object.localizableParams.length);
                    for (var i = 0; i < object.localizableParams.length; ++i) {
                        if (!$util.isObject(object.localizableParams[i]))
                            throw TypeError(".proto.Message.HighlyStructuredMessage.localizableParams: object expected");
                        message.localizableParams[i] = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.fromObject(object.localizableParams[i], q + 1);
                    }
                }
                if (object.deterministicLg != null)
                    message.deterministicLg = String(object.deterministicLg);
                if (object.deterministicLc != null)
                    message.deterministicLc = String(object.deterministicLc);
                if (object.hydratedHsm != null) {
                    if (!$util.isObject(object.hydratedHsm))
                        throw TypeError(".proto.Message.HighlyStructuredMessage.hydratedHsm: object expected");
                    message.hydratedHsm = $root.proto.Message.TemplateMessage.fromObject(object.hydratedHsm, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a HighlyStructuredMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.HighlyStructuredMessage
             * @static
             * @param {proto.Message.HighlyStructuredMessage} message HighlyStructuredMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            HighlyStructuredMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.params = [];
                    object.localizableParams = [];
                }
                if (message.namespace != null && Object.hasOwnProperty.call(message, "namespace"))
                    object.namespace = message.namespace;
                if (message.elementName != null && Object.hasOwnProperty.call(message, "elementName"))
                    object.elementName = message.elementName;
                if (message.params && message.params.length) {
                    object.params = Array(message.params.length);
                    for (var j = 0; j < message.params.length; ++j)
                        object.params[j] = message.params[j];
                }
                if (message.fallbackLg != null && Object.hasOwnProperty.call(message, "fallbackLg"))
                    object.fallbackLg = message.fallbackLg;
                if (message.fallbackLc != null && Object.hasOwnProperty.call(message, "fallbackLc"))
                    object.fallbackLc = message.fallbackLc;
                if (message.localizableParams && message.localizableParams.length) {
                    object.localizableParams = Array(message.localizableParams.length);
                    for (var j = 0; j < message.localizableParams.length; ++j)
                        object.localizableParams[j] = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.toObject(message.localizableParams[j], options, q + 1);
                }
                if (message.deterministicLg != null && Object.hasOwnProperty.call(message, "deterministicLg"))
                    object.deterministicLg = message.deterministicLg;
                if (message.deterministicLc != null && Object.hasOwnProperty.call(message, "deterministicLc"))
                    object.deterministicLc = message.deterministicLc;
                if (message.hydratedHsm != null && Object.hasOwnProperty.call(message, "hydratedHsm"))
                    object.hydratedHsm = $root.proto.Message.TemplateMessage.toObject(message.hydratedHsm, options, q + 1);
                return object;
            };

            /**
             * Converts this HighlyStructuredMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.HighlyStructuredMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            HighlyStructuredMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for HighlyStructuredMessage
             * @function getTypeUrl
             * @memberof proto.Message.HighlyStructuredMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            HighlyStructuredMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.HighlyStructuredMessage";
            };

            HighlyStructuredMessage.HSMLocalizableParameter = (function() {

                /**
                 * Properties of a HSMLocalizableParameter.
                 * @memberof proto.Message.HighlyStructuredMessage
                 * @interface IHSMLocalizableParameter
                 * @property {string|null} ["default"] HSMLocalizableParameter default
                 * @property {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.IHSMCurrency|null} [currency] HSMLocalizableParameter currency
                 * @property {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.IHSMDateTime|null} [dateTime] HSMLocalizableParameter dateTime
                 */

                /**
                 * Constructs a new HSMLocalizableParameter.
                 * @memberof proto.Message.HighlyStructuredMessage
                 * @classdesc Represents a HSMLocalizableParameter.
                 * @implements IHSMLocalizableParameter
                 * @constructor
                 * @param {proto.Message.HighlyStructuredMessage.IHSMLocalizableParameter=} [properties] Properties to set
                 */
                function HSMLocalizableParameter(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * HSMLocalizableParameter default.
                 * @member {string|null|undefined} default
                 * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter
                 * @instance
                 */
                HSMLocalizableParameter.prototype["default"] = null;

                /**
                 * HSMLocalizableParameter currency.
                 * @member {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.IHSMCurrency|null|undefined} currency
                 * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter
                 * @instance
                 */
                HSMLocalizableParameter.prototype.currency = null;

                /**
                 * HSMLocalizableParameter dateTime.
                 * @member {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.IHSMDateTime|null|undefined} dateTime
                 * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter
                 * @instance
                 */
                HSMLocalizableParameter.prototype.dateTime = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HSMLocalizableParameter.prototype, "_default", {
                    get: $util.oneOfGetter($oneOfFields = ["default"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * HSMLocalizableParameter paramOneof.
                 * @member {"currency"|"dateTime"|undefined} paramOneof
                 * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter
                 * @instance
                 */
                Object.defineProperty(HSMLocalizableParameter.prototype, "paramOneof", {
                    get: $util.oneOfGetter($oneOfFields = ["currency", "dateTime"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new HSMLocalizableParameter instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter
                 * @static
                 * @param {proto.Message.HighlyStructuredMessage.IHSMLocalizableParameter=} [properties] Properties to set
                 * @returns {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter} HSMLocalizableParameter instance
                 */
                HSMLocalizableParameter.create = function create(properties) {
                    return new HSMLocalizableParameter(properties);
                };

                /**
                 * Encodes the specified HSMLocalizableParameter message. Does not implicitly {@link proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter
                 * @static
                 * @param {proto.Message.HighlyStructuredMessage.IHSMLocalizableParameter} message HSMLocalizableParameter message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                HSMLocalizableParameter.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message["default"] != null && Object.hasOwnProperty.call(message, "default"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message["default"]);
                    if (message.currency != null && Object.hasOwnProperty.call(message, "currency"))
                        $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency.encode(message.currency, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                    if (message.dateTime != null && Object.hasOwnProperty.call(message, "dateTime"))
                        $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.encode(message.dateTime, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified HSMLocalizableParameter message, length delimited. Does not implicitly {@link proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter
                 * @static
                 * @param {proto.Message.HighlyStructuredMessage.IHSMLocalizableParameter} message HSMLocalizableParameter message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                HSMLocalizableParameter.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a HSMLocalizableParameter message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter} HSMLocalizableParameter
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                HSMLocalizableParameter.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message["default"] = reader.stringVerify();
                                message._default = "default";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.currency = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency.decode(reader, reader.uint32(), undefined, q + 1, message.currency);
                                message.paramOneof = "currency";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.dateTime = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.decode(reader, reader.uint32(), undefined, q + 1, message.dateTime);
                                message.paramOneof = "dateTime";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a HSMLocalizableParameter message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter} HSMLocalizableParameter
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                HSMLocalizableParameter.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a HSMLocalizableParameter message.
                 * @function verify
                 * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                HSMLocalizableParameter.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message["default"] != null && Object.hasOwnProperty.call(message, "default")) {
                        properties._default = 1;
                        if (!$util.isString(message["default"]))
                            return "default: string expected";
                    }
                    if (message.currency != null && Object.hasOwnProperty.call(message, "currency")) {
                        properties.paramOneof = 1;
                        {
                            var error = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency.verify(message.currency, q + 1);
                            if (error)
                                return "currency." + error;
                        }
                    }
                    if (message.dateTime != null && Object.hasOwnProperty.call(message, "dateTime")) {
                        if (properties.paramOneof === 1)
                            return "paramOneof: multiple values";
                        properties.paramOneof = 1;
                        {
                            var error = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.verify(message.dateTime, q + 1);
                            if (error)
                                return "dateTime." + error;
                        }
                    }
                    return null;
                };

                /**
                 * Creates a HSMLocalizableParameter message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter} HSMLocalizableParameter
                 */
                HSMLocalizableParameter.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.HighlyStructuredMessage.HSMLocalizableParameter: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object["default"] != null)
                        message["default"] = String(object["default"]);
                    if (object.currency != null) {
                        if (!$util.isObject(object.currency))
                            throw TypeError(".proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.currency: object expected");
                        message.currency = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency.fromObject(object.currency, q + 1);
                    }
                    if (object.dateTime != null) {
                        if (!$util.isObject(object.dateTime))
                            throw TypeError(".proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.dateTime: object expected");
                        message.dateTime = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.fromObject(object.dateTime, q + 1);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a HSMLocalizableParameter message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter
                 * @static
                 * @param {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter} message HSMLocalizableParameter
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                HSMLocalizableParameter.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message["default"] != null && Object.hasOwnProperty.call(message, "default"))
                        object["default"] = message["default"];
                    if (message.currency != null && Object.hasOwnProperty.call(message, "currency")) {
                        object.currency = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency.toObject(message.currency, options, q + 1);
                        if (options.oneofs)
                            object.paramOneof = "currency";
                    }
                    if (message.dateTime != null && Object.hasOwnProperty.call(message, "dateTime")) {
                        object.dateTime = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.toObject(message.dateTime, options, q + 1);
                        if (options.oneofs)
                            object.paramOneof = "dateTime";
                    }
                    return object;
                };

                /**
                 * Converts this HSMLocalizableParameter to JSON.
                 * @function toJSON
                 * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                HSMLocalizableParameter.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for HSMLocalizableParameter
                 * @function getTypeUrl
                 * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                HSMLocalizableParameter.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.HighlyStructuredMessage.HSMLocalizableParameter";
                };

                HSMLocalizableParameter.HSMCurrency = (function() {

                    /**
                     * Properties of a HSMCurrency.
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter
                     * @interface IHSMCurrency
                     * @property {string|null} [currencyCode] HSMCurrency currencyCode
                     * @property {number|Long|null} [amount1000] HSMCurrency amount1000
                     */

                    /**
                     * Constructs a new HSMCurrency.
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter
                     * @classdesc Represents a HSMCurrency.
                     * @implements IHSMCurrency
                     * @constructor
                     * @param {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.IHSMCurrency=} [properties] Properties to set
                     */
                    function HSMCurrency(properties) {
                        if (properties)
                            for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    /**
                     * HSMCurrency currencyCode.
                     * @member {string|null|undefined} currencyCode
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency
                     * @instance
                     */
                    HSMCurrency.prototype.currencyCode = null;

                    /**
                     * HSMCurrency amount1000.
                     * @member {number|Long|null|undefined} amount1000
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency
                     * @instance
                     */
                    HSMCurrency.prototype.amount1000 = null;

                    // OneOf field names bound to virtual getters and setters
                    var $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(HSMCurrency.prototype, "_currencyCode", {
                        get: $util.oneOfGetter($oneOfFields = ["currencyCode"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(HSMCurrency.prototype, "_amount1000", {
                        get: $util.oneOfGetter($oneOfFields = ["amount1000"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    /**
                     * Creates a new HSMCurrency instance using the specified properties.
                     * @function create
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency
                     * @static
                     * @param {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.IHSMCurrency=} [properties] Properties to set
                     * @returns {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency} HSMCurrency instance
                     */
                    HSMCurrency.create = function create(properties) {
                        return new HSMCurrency(properties);
                    };

                    /**
                     * Encodes the specified HSMCurrency message. Does not implicitly {@link proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency.verify|verify} messages.
                     * @function encode
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency
                     * @static
                     * @param {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.IHSMCurrency} message HSMCurrency message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    HSMCurrency.encode = function encode(message, writer, q) {
                        if (!writer)
                            writer = $Writer.create();
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        if (message.currencyCode != null && Object.hasOwnProperty.call(message, "currencyCode"))
                            writer.uint32(/* id 1, wireType 2 =*/10).string(message.currencyCode);
                        if (message.amount1000 != null && Object.hasOwnProperty.call(message, "amount1000"))
                            writer.uint32(/* id 2, wireType 0 =*/16).int64(message.amount1000);
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (var i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    /**
                     * Encodes the specified HSMCurrency message, length delimited. Does not implicitly {@link proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency.verify|verify} messages.
                     * @function encodeDelimited
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency
                     * @static
                     * @param {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.IHSMCurrency} message HSMCurrency message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    HSMCurrency.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                    };

                    /**
                     * Decodes a HSMCurrency message from the specified reader or buffer.
                     * @function decode
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @param {number} [length] Message length if known beforehand
                     * @returns {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency} HSMCurrency
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    HSMCurrency.decode = function decode(reader, length, z, q, g) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (q === undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                        while (reader.pos < end) {
                            var s = reader.pos;
                            var tag = reader.tag();
                            if (tag === z) {
                                z = undefined;
                                break;
                            }
                            var u = tag & 7;
                            switch (tag >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    message.currencyCode = reader.stringVerify();
                                    message._currencyCode = "currencyCode";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 0)
                                        break;
                                    message.amount1000 = reader.int64();
                                    message._amount1000 = "amount1000";
                                    continue;
                                }
                            }
                            reader.skipType(u, q, tag);
                            if (!reader.discardUnknown) {
                                $util.makeProp(message, "$unknowns", false);
                                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                            }
                        }
                        if (z !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    /**
                     * Decodes a HSMCurrency message from the specified reader or buffer, length delimited.
                     * @function decodeDelimited
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @returns {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency} HSMCurrency
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    HSMCurrency.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    /**
                     * Verifies a HSMCurrency message.
                     * @function verify
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency
                     * @static
                     * @param {Object.<string,*>} message Plain object to verify
                     * @returns {string|null} `null` if valid, otherwise the reason why it is not
                     */
                    HSMCurrency.verify = function verify(message, q) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            return "max depth exceeded";
                        var properties = {};
                        if (message.currencyCode != null && Object.hasOwnProperty.call(message, "currencyCode")) {
                            properties._currencyCode = 1;
                            if (!$util.isString(message.currencyCode))
                                return "currencyCode: string expected";
                        }
                        if (message.amount1000 != null && Object.hasOwnProperty.call(message, "amount1000")) {
                            properties._amount1000 = 1;
                            if (!$util.isInteger(message.amount1000) && !(message.amount1000 && $util.isInteger(message.amount1000.low) && $util.isInteger(message.amount1000.high)))
                                return "amount1000: integer|Long expected";
                        }
                        return null;
                    };

                    /**
                     * Creates a HSMCurrency message from a plain object. Also converts values to their respective internal types.
                     * @function fromObject
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency
                     * @static
                     * @param {Object.<string,*>} object Plain object
                     * @returns {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency} HSMCurrency
                     */
                    HSMCurrency.fromObject = function fromObject(object, q) {
                        if (object instanceof C)
                            return object;
                        if (!$util.isObject(object))
                            throw TypeError(".proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency: object expected");
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var message = new C();
                        if (object.currencyCode != null)
                            message.currencyCode = String(object.currencyCode);
                        if (object.amount1000 != null)
                            if ($util.Long)
                                message.amount1000 = $util.Long.fromValue(object.amount1000, false);
                            else if (typeof object.amount1000 === "string")
                                message.amount1000 = parseInt(object.amount1000, 10);
                            else if (typeof object.amount1000 === "number")
                                message.amount1000 = object.amount1000;
                            else if (typeof object.amount1000 === "object")
                                message.amount1000 = new $util.LongBits(object.amount1000.low >>> 0, object.amount1000.high >>> 0).toNumber();
                        return message;
                    };

                    /**
                     * Creates a plain object from a HSMCurrency message. Also converts values to other types if specified.
                     * @function toObject
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency
                     * @static
                     * @param {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency} message HSMCurrency
                     * @param {$protobuf.IConversionOptions} [options] Conversion options
                     * @returns {Object.<string,*>} Plain object
                     */
                    HSMCurrency.toObject = function toObject(message, options, q) {
                        if (!options)
                            options = {};
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var object = {};
                        if (message.currencyCode != null && Object.hasOwnProperty.call(message, "currencyCode"))
                            object.currencyCode = message.currencyCode;
                        if (message.amount1000 != null && Object.hasOwnProperty.call(message, "amount1000"))
                            if (typeof BigInt !== "undefined" && options.longs === BigInt)
                                object.amount1000 = typeof message.amount1000 === "number" ? BigInt(message.amount1000) : $util.Long.fromBits(message.amount1000.low >>> 0, message.amount1000.high >>> 0, false).toBigInt();
                            else if (typeof message.amount1000 === "number")
                                object.amount1000 = options.longs === String ? String(message.amount1000) : message.amount1000;
                            else
                                object.amount1000 = options.longs === String ? $util.Long.prototype.toString.call(message.amount1000) : options.longs === Number ? new $util.LongBits(message.amount1000.low >>> 0, message.amount1000.high >>> 0).toNumber() : message.amount1000;
                        return object;
                    };

                    /**
                     * Converts this HSMCurrency to JSON.
                     * @function toJSON
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency
                     * @instance
                     * @returns {Object.<string,*>} JSON object
                     */
                    HSMCurrency.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    /**
                     * Gets the default type url for HSMCurrency
                     * @function getTypeUrl
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency
                     * @static
                     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                     * @returns {string} The default type url
                     */
                    HSMCurrency.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency";
                    };

                    return HSMCurrency;
                })();

                HSMLocalizableParameter.HSMDateTime = (function() {

                    /**
                     * Properties of a HSMDateTime.
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter
                     * @interface IHSMDateTime
                     * @property {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.IHSMDateTimeComponent|null} [component] HSMDateTime component
                     * @property {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.IHSMDateTimeUnixEpoch|null} [unixEpoch] HSMDateTime unixEpoch
                     */

                    /**
                     * Constructs a new HSMDateTime.
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter
                     * @classdesc Represents a HSMDateTime.
                     * @implements IHSMDateTime
                     * @constructor
                     * @param {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.IHSMDateTime=} [properties] Properties to set
                     */
                    function HSMDateTime(properties) {
                        if (properties)
                            for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    /**
                     * HSMDateTime component.
                     * @member {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.IHSMDateTimeComponent|null|undefined} component
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime
                     * @instance
                     */
                    HSMDateTime.prototype.component = null;

                    /**
                     * HSMDateTime unixEpoch.
                     * @member {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.IHSMDateTimeUnixEpoch|null|undefined} unixEpoch
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime
                     * @instance
                     */
                    HSMDateTime.prototype.unixEpoch = null;

                    // OneOf field names bound to virtual getters and setters
                    var $oneOfFields;

                    /**
                     * HSMDateTime datetimeOneof.
                     * @member {"component"|"unixEpoch"|undefined} datetimeOneof
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime
                     * @instance
                     */
                    Object.defineProperty(HSMDateTime.prototype, "datetimeOneof", {
                        get: $util.oneOfGetter($oneOfFields = ["component", "unixEpoch"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    /**
                     * Creates a new HSMDateTime instance using the specified properties.
                     * @function create
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime
                     * @static
                     * @param {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.IHSMDateTime=} [properties] Properties to set
                     * @returns {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime} HSMDateTime instance
                     */
                    HSMDateTime.create = function create(properties) {
                        return new HSMDateTime(properties);
                    };

                    /**
                     * Encodes the specified HSMDateTime message. Does not implicitly {@link proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.verify|verify} messages.
                     * @function encode
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime
                     * @static
                     * @param {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.IHSMDateTime} message HSMDateTime message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    HSMDateTime.encode = function encode(message, writer, q) {
                        if (!writer)
                            writer = $Writer.create();
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        if (message.component != null && Object.hasOwnProperty.call(message, "component"))
                            $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.encode(message.component, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                        if (message.unixEpoch != null && Object.hasOwnProperty.call(message, "unixEpoch"))
                            $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch.encode(message.unixEpoch, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (var i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    /**
                     * Encodes the specified HSMDateTime message, length delimited. Does not implicitly {@link proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.verify|verify} messages.
                     * @function encodeDelimited
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime
                     * @static
                     * @param {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.IHSMDateTime} message HSMDateTime message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    HSMDateTime.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                    };

                    /**
                     * Decodes a HSMDateTime message from the specified reader or buffer.
                     * @function decode
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @param {number} [length] Message length if known beforehand
                     * @returns {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime} HSMDateTime
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    HSMDateTime.decode = function decode(reader, length, z, q, g) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (q === undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                        while (reader.pos < end) {
                            var s = reader.pos;
                            var tag = reader.tag();
                            if (tag === z) {
                                z = undefined;
                                break;
                            }
                            var u = tag & 7;
                            switch (tag >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    message.component = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.decode(reader, reader.uint32(), undefined, q + 1, message.component);
                                    message.datetimeOneof = "component";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 2)
                                        break;
                                    message.unixEpoch = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch.decode(reader, reader.uint32(), undefined, q + 1, message.unixEpoch);
                                    message.datetimeOneof = "unixEpoch";
                                    continue;
                                }
                            }
                            reader.skipType(u, q, tag);
                            if (!reader.discardUnknown) {
                                $util.makeProp(message, "$unknowns", false);
                                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                            }
                        }
                        if (z !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    /**
                     * Decodes a HSMDateTime message from the specified reader or buffer, length delimited.
                     * @function decodeDelimited
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @returns {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime} HSMDateTime
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    HSMDateTime.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    /**
                     * Verifies a HSMDateTime message.
                     * @function verify
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime
                     * @static
                     * @param {Object.<string,*>} message Plain object to verify
                     * @returns {string|null} `null` if valid, otherwise the reason why it is not
                     */
                    HSMDateTime.verify = function verify(message, q) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            return "max depth exceeded";
                        var properties = {};
                        if (message.component != null && Object.hasOwnProperty.call(message, "component")) {
                            properties.datetimeOneof = 1;
                            {
                                var error = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.verify(message.component, q + 1);
                                if (error)
                                    return "component." + error;
                            }
                        }
                        if (message.unixEpoch != null && Object.hasOwnProperty.call(message, "unixEpoch")) {
                            if (properties.datetimeOneof === 1)
                                return "datetimeOneof: multiple values";
                            properties.datetimeOneof = 1;
                            {
                                var error = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch.verify(message.unixEpoch, q + 1);
                                if (error)
                                    return "unixEpoch." + error;
                            }
                        }
                        return null;
                    };

                    /**
                     * Creates a HSMDateTime message from a plain object. Also converts values to their respective internal types.
                     * @function fromObject
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime
                     * @static
                     * @param {Object.<string,*>} object Plain object
                     * @returns {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime} HSMDateTime
                     */
                    HSMDateTime.fromObject = function fromObject(object, q) {
                        if (object instanceof C)
                            return object;
                        if (!$util.isObject(object))
                            throw TypeError(".proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime: object expected");
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var message = new C();
                        if (object.component != null) {
                            if (!$util.isObject(object.component))
                                throw TypeError(".proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.component: object expected");
                            message.component = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.fromObject(object.component, q + 1);
                        }
                        if (object.unixEpoch != null) {
                            if (!$util.isObject(object.unixEpoch))
                                throw TypeError(".proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.unixEpoch: object expected");
                            message.unixEpoch = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch.fromObject(object.unixEpoch, q + 1);
                        }
                        return message;
                    };

                    /**
                     * Creates a plain object from a HSMDateTime message. Also converts values to other types if specified.
                     * @function toObject
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime
                     * @static
                     * @param {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime} message HSMDateTime
                     * @param {$protobuf.IConversionOptions} [options] Conversion options
                     * @returns {Object.<string,*>} Plain object
                     */
                    HSMDateTime.toObject = function toObject(message, options, q) {
                        if (!options)
                            options = {};
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var object = {};
                        if (message.component != null && Object.hasOwnProperty.call(message, "component")) {
                            object.component = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.toObject(message.component, options, q + 1);
                            if (options.oneofs)
                                object.datetimeOneof = "component";
                        }
                        if (message.unixEpoch != null && Object.hasOwnProperty.call(message, "unixEpoch")) {
                            object.unixEpoch = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch.toObject(message.unixEpoch, options, q + 1);
                            if (options.oneofs)
                                object.datetimeOneof = "unixEpoch";
                        }
                        return object;
                    };

                    /**
                     * Converts this HSMDateTime to JSON.
                     * @function toJSON
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime
                     * @instance
                     * @returns {Object.<string,*>} JSON object
                     */
                    HSMDateTime.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    /**
                     * Gets the default type url for HSMDateTime
                     * @function getTypeUrl
                     * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime
                     * @static
                     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                     * @returns {string} The default type url
                     */
                    HSMDateTime.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime";
                    };

                    HSMDateTime.HSMDateTimeComponent = (function() {

                        /**
                         * Properties of a HSMDateTimeComponent.
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime
                         * @interface IHSMDateTimeComponent
                         * @property {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.DayOfWeekType|null} [dayOfWeek] HSMDateTimeComponent dayOfWeek
                         * @property {number|null} [year] HSMDateTimeComponent year
                         * @property {number|null} [month] HSMDateTimeComponent month
                         * @property {number|null} [dayOfMonth] HSMDateTimeComponent dayOfMonth
                         * @property {number|null} [hour] HSMDateTimeComponent hour
                         * @property {number|null} [minute] HSMDateTimeComponent minute
                         * @property {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.CalendarType|null} [calendar] HSMDateTimeComponent calendar
                         */

                        /**
                         * Constructs a new HSMDateTimeComponent.
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime
                         * @classdesc Represents a HSMDateTimeComponent.
                         * @implements IHSMDateTimeComponent
                         * @constructor
                         * @param {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.IHSMDateTimeComponent=} [properties] Properties to set
                         */
                        function HSMDateTimeComponent(properties) {
                            if (properties)
                                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        }

                        /**
                         * HSMDateTimeComponent dayOfWeek.
                         * @member {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.DayOfWeekType|null|undefined} dayOfWeek
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent
                         * @instance
                         */
                        HSMDateTimeComponent.prototype.dayOfWeek = null;

                        /**
                         * HSMDateTimeComponent year.
                         * @member {number|null|undefined} year
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent
                         * @instance
                         */
                        HSMDateTimeComponent.prototype.year = null;

                        /**
                         * HSMDateTimeComponent month.
                         * @member {number|null|undefined} month
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent
                         * @instance
                         */
                        HSMDateTimeComponent.prototype.month = null;

                        /**
                         * HSMDateTimeComponent dayOfMonth.
                         * @member {number|null|undefined} dayOfMonth
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent
                         * @instance
                         */
                        HSMDateTimeComponent.prototype.dayOfMonth = null;

                        /**
                         * HSMDateTimeComponent hour.
                         * @member {number|null|undefined} hour
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent
                         * @instance
                         */
                        HSMDateTimeComponent.prototype.hour = null;

                        /**
                         * HSMDateTimeComponent minute.
                         * @member {number|null|undefined} minute
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent
                         * @instance
                         */
                        HSMDateTimeComponent.prototype.minute = null;

                        /**
                         * HSMDateTimeComponent calendar.
                         * @member {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.CalendarType|null|undefined} calendar
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent
                         * @instance
                         */
                        HSMDateTimeComponent.prototype.calendar = null;

                        // OneOf field names bound to virtual getters and setters
                        var $oneOfFields;

                        // Virtual OneOf for proto3 optional field
                        Object.defineProperty(HSMDateTimeComponent.prototype, "_dayOfWeek", {
                            get: $util.oneOfGetter($oneOfFields = ["dayOfWeek"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        Object.defineProperty(HSMDateTimeComponent.prototype, "_year", {
                            get: $util.oneOfGetter($oneOfFields = ["year"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        Object.defineProperty(HSMDateTimeComponent.prototype, "_month", {
                            get: $util.oneOfGetter($oneOfFields = ["month"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        Object.defineProperty(HSMDateTimeComponent.prototype, "_dayOfMonth", {
                            get: $util.oneOfGetter($oneOfFields = ["dayOfMonth"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        Object.defineProperty(HSMDateTimeComponent.prototype, "_hour", {
                            get: $util.oneOfGetter($oneOfFields = ["hour"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        Object.defineProperty(HSMDateTimeComponent.prototype, "_minute", {
                            get: $util.oneOfGetter($oneOfFields = ["minute"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        Object.defineProperty(HSMDateTimeComponent.prototype, "_calendar", {
                            get: $util.oneOfGetter($oneOfFields = ["calendar"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        /**
                         * Creates a new HSMDateTimeComponent instance using the specified properties.
                         * @function create
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent
                         * @static
                         * @param {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.IHSMDateTimeComponent=} [properties] Properties to set
                         * @returns {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent} HSMDateTimeComponent instance
                         */
                        HSMDateTimeComponent.create = function create(properties) {
                            return new HSMDateTimeComponent(properties);
                        };

                        /**
                         * Encodes the specified HSMDateTimeComponent message. Does not implicitly {@link proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.verify|verify} messages.
                         * @function encode
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent
                         * @static
                         * @param {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.IHSMDateTimeComponent} message HSMDateTimeComponent message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        HSMDateTimeComponent.encode = function encode(message, writer, q) {
                            if (!writer)
                                writer = $Writer.create();
                            if (q === undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw Error("max depth exceeded");
                            if (message.dayOfWeek != null && Object.hasOwnProperty.call(message, "dayOfWeek"))
                                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.dayOfWeek);
                            if (message.year != null && Object.hasOwnProperty.call(message, "year"))
                                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.year);
                            if (message.month != null && Object.hasOwnProperty.call(message, "month"))
                                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.month);
                            if (message.dayOfMonth != null && Object.hasOwnProperty.call(message, "dayOfMonth"))
                                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.dayOfMonth);
                            if (message.hour != null && Object.hasOwnProperty.call(message, "hour"))
                                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.hour);
                            if (message.minute != null && Object.hasOwnProperty.call(message, "minute"))
                                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.minute);
                            if (message.calendar != null && Object.hasOwnProperty.call(message, "calendar"))
                                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.calendar);
                            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                                for (var i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified HSMDateTimeComponent message, length delimited. Does not implicitly {@link proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent
                         * @static
                         * @param {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.IHSMDateTimeComponent} message HSMDateTimeComponent message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        HSMDateTimeComponent.encodeDelimited = function encodeDelimited(message, writer) {
                            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                        };

                        /**
                         * Decodes a HSMDateTimeComponent message from the specified reader or buffer.
                         * @function decode
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent} HSMDateTimeComponent
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        HSMDateTimeComponent.decode = function decode(reader, length, z, q, g) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (q === undefined)
                                q = 0;
                            if (q > $Reader.recursionLimit)
                                throw Error("max depth exceeded");
                            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                            while (reader.pos < end) {
                                var s = reader.pos;
                                var tag = reader.tag();
                                if (tag === z) {
                                    z = undefined;
                                    break;
                                }
                                var u = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (u !== 0)
                                            break;
                                        message.dayOfWeek = reader.int32();
                                        message._dayOfWeek = "dayOfWeek";
                                        continue;
                                    }
                                case 2: {
                                        if (u !== 0)
                                            break;
                                        message.year = reader.uint32();
                                        message._year = "year";
                                        continue;
                                    }
                                case 3: {
                                        if (u !== 0)
                                            break;
                                        message.month = reader.uint32();
                                        message._month = "month";
                                        continue;
                                    }
                                case 4: {
                                        if (u !== 0)
                                            break;
                                        message.dayOfMonth = reader.uint32();
                                        message._dayOfMonth = "dayOfMonth";
                                        continue;
                                    }
                                case 5: {
                                        if (u !== 0)
                                            break;
                                        message.hour = reader.uint32();
                                        message._hour = "hour";
                                        continue;
                                    }
                                case 6: {
                                        if (u !== 0)
                                            break;
                                        message.minute = reader.uint32();
                                        message._minute = "minute";
                                        continue;
                                    }
                                case 7: {
                                        if (u !== 0)
                                            break;
                                        message.calendar = reader.int32();
                                        message._calendar = "calendar";
                                        continue;
                                    }
                                }
                                reader.skipType(u, q, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                                }
                            }
                            if (z !== undefined)
                                throw Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a HSMDateTimeComponent message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent} HSMDateTimeComponent
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        HSMDateTimeComponent.decodeDelimited = function decodeDelimited(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a HSMDateTimeComponent message.
                         * @function verify
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        HSMDateTimeComponent.verify = function verify(message, q) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (q === undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                return "max depth exceeded";
                            var properties = {};
                            if (message.dayOfWeek != null && Object.hasOwnProperty.call(message, "dayOfWeek")) {
                                properties._dayOfWeek = 1;
                                if (typeof message.dayOfWeek !== "number" || (message.dayOfWeek | 0) !== message.dayOfWeek)
                                    return "dayOfWeek: enum value expected";
                            }
                            if (message.year != null && Object.hasOwnProperty.call(message, "year")) {
                                properties._year = 1;
                                if (!$util.isInteger(message.year))
                                    return "year: integer expected";
                            }
                            if (message.month != null && Object.hasOwnProperty.call(message, "month")) {
                                properties._month = 1;
                                if (!$util.isInteger(message.month))
                                    return "month: integer expected";
                            }
                            if (message.dayOfMonth != null && Object.hasOwnProperty.call(message, "dayOfMonth")) {
                                properties._dayOfMonth = 1;
                                if (!$util.isInteger(message.dayOfMonth))
                                    return "dayOfMonth: integer expected";
                            }
                            if (message.hour != null && Object.hasOwnProperty.call(message, "hour")) {
                                properties._hour = 1;
                                if (!$util.isInteger(message.hour))
                                    return "hour: integer expected";
                            }
                            if (message.minute != null && Object.hasOwnProperty.call(message, "minute")) {
                                properties._minute = 1;
                                if (!$util.isInteger(message.minute))
                                    return "minute: integer expected";
                            }
                            if (message.calendar != null && Object.hasOwnProperty.call(message, "calendar")) {
                                properties._calendar = 1;
                                if (typeof message.calendar !== "number" || (message.calendar | 0) !== message.calendar)
                                    return "calendar: enum value expected";
                            }
                            return null;
                        };

                        /**
                         * Creates a HSMDateTimeComponent message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent} HSMDateTimeComponent
                         */
                        HSMDateTimeComponent.fromObject = function fromObject(object, q) {
                            if (object instanceof C)
                                return object;
                            if (!$util.isObject(object))
                                throw TypeError(".proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent: object expected");
                            if (q === undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw Error("max depth exceeded");
                            var message = new C();
                            switch (object.dayOfWeek) {
                            case "MONDAY":
                            case 1:
                                message.dayOfWeek = 1;
                                break;
                            case "TUESDAY":
                            case 2:
                                message.dayOfWeek = 2;
                                break;
                            case "WEDNESDAY":
                            case 3:
                                message.dayOfWeek = 3;
                                break;
                            case "THURSDAY":
                            case 4:
                                message.dayOfWeek = 4;
                                break;
                            case "FRIDAY":
                            case 5:
                                message.dayOfWeek = 5;
                                break;
                            case "SATURDAY":
                            case 6:
                                message.dayOfWeek = 6;
                                break;
                            case "SUNDAY":
                            case 7:
                                message.dayOfWeek = 7;
                                break;
                            default:
                                if (typeof object.dayOfWeek === "number" && (object.dayOfWeek | 0) === object.dayOfWeek)
                                    message.dayOfWeek = object.dayOfWeek;
                            }
                            if (object.year != null)
                                message.year = object.year >>> 0;
                            if (object.month != null)
                                message.month = object.month >>> 0;
                            if (object.dayOfMonth != null)
                                message.dayOfMonth = object.dayOfMonth >>> 0;
                            if (object.hour != null)
                                message.hour = object.hour >>> 0;
                            if (object.minute != null)
                                message.minute = object.minute >>> 0;
                            switch (object.calendar) {
                            case "GREGORIAN":
                            case 1:
                                message.calendar = 1;
                                break;
                            case "SOLAR_HIJRI":
                            case 2:
                                message.calendar = 2;
                                break;
                            default:
                                if (typeof object.calendar === "number" && (object.calendar | 0) === object.calendar)
                                    message.calendar = object.calendar;
                            }
                            return message;
                        };

                        /**
                         * Creates a plain object from a HSMDateTimeComponent message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent
                         * @static
                         * @param {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent} message HSMDateTimeComponent
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        HSMDateTimeComponent.toObject = function toObject(message, options, q) {
                            if (!options)
                                options = {};
                            if (q === undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw Error("max depth exceeded");
                            var object = {};
                            if (message.dayOfWeek != null && Object.hasOwnProperty.call(message, "dayOfWeek"))
                                object.dayOfWeek = options.enums === String ? $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.DayOfWeekType[message.dayOfWeek] === undefined ? message.dayOfWeek : $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.DayOfWeekType[message.dayOfWeek] : message.dayOfWeek;
                            if (message.year != null && Object.hasOwnProperty.call(message, "year"))
                                object.year = message.year;
                            if (message.month != null && Object.hasOwnProperty.call(message, "month"))
                                object.month = message.month;
                            if (message.dayOfMonth != null && Object.hasOwnProperty.call(message, "dayOfMonth"))
                                object.dayOfMonth = message.dayOfMonth;
                            if (message.hour != null && Object.hasOwnProperty.call(message, "hour"))
                                object.hour = message.hour;
                            if (message.minute != null && Object.hasOwnProperty.call(message, "minute"))
                                object.minute = message.minute;
                            if (message.calendar != null && Object.hasOwnProperty.call(message, "calendar"))
                                object.calendar = options.enums === String ? $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.CalendarType[message.calendar] === undefined ? message.calendar : $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.CalendarType[message.calendar] : message.calendar;
                            return object;
                        };

                        /**
                         * Converts this HSMDateTimeComponent to JSON.
                         * @function toJSON
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        HSMDateTimeComponent.prototype.toJSON = function toJSON() {
                            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the default type url for HSMDateTimeComponent
                         * @function getTypeUrl
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent
                         * @static
                         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                         * @returns {string} The default type url
                         */
                        HSMDateTimeComponent.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                            if (typeUrlPrefix === undefined) {
                                typeUrlPrefix = "type.googleapis.com";
                            }
                            return typeUrlPrefix + "/proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent";
                        };

                        /**
                         * CalendarType enum.
                         * @name proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.CalendarType
                         * @enum {number}
                         * @property {number} GREGORIAN=1 GREGORIAN value
                         * @property {number} SOLAR_HIJRI=2 SOLAR_HIJRI value
                         */
                        HSMDateTimeComponent.CalendarType = (function() {
                            var valuesById = {}, values = Object.create(valuesById);
                            values[valuesById[1] = "GREGORIAN"] = 1;
                            values[valuesById[2] = "SOLAR_HIJRI"] = 2;
                            return values;
                        })();

                        /**
                         * DayOfWeekType enum.
                         * @name proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.DayOfWeekType
                         * @enum {number}
                         * @property {number} MONDAY=1 MONDAY value
                         * @property {number} TUESDAY=2 TUESDAY value
                         * @property {number} WEDNESDAY=3 WEDNESDAY value
                         * @property {number} THURSDAY=4 THURSDAY value
                         * @property {number} FRIDAY=5 FRIDAY value
                         * @property {number} SATURDAY=6 SATURDAY value
                         * @property {number} SUNDAY=7 SUNDAY value
                         */
                        HSMDateTimeComponent.DayOfWeekType = (function() {
                            var valuesById = {}, values = Object.create(valuesById);
                            values[valuesById[1] = "MONDAY"] = 1;
                            values[valuesById[2] = "TUESDAY"] = 2;
                            values[valuesById[3] = "WEDNESDAY"] = 3;
                            values[valuesById[4] = "THURSDAY"] = 4;
                            values[valuesById[5] = "FRIDAY"] = 5;
                            values[valuesById[6] = "SATURDAY"] = 6;
                            values[valuesById[7] = "SUNDAY"] = 7;
                            return values;
                        })();

                        return HSMDateTimeComponent;
                    })();

                    HSMDateTime.HSMDateTimeUnixEpoch = (function() {

                        /**
                         * Properties of a HSMDateTimeUnixEpoch.
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime
                         * @interface IHSMDateTimeUnixEpoch
                         * @property {number|Long|null} [timestamp] HSMDateTimeUnixEpoch timestamp
                         */

                        /**
                         * Constructs a new HSMDateTimeUnixEpoch.
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime
                         * @classdesc Represents a HSMDateTimeUnixEpoch.
                         * @implements IHSMDateTimeUnixEpoch
                         * @constructor
                         * @param {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.IHSMDateTimeUnixEpoch=} [properties] Properties to set
                         */
                        function HSMDateTimeUnixEpoch(properties) {
                            if (properties)
                                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        }

                        /**
                         * HSMDateTimeUnixEpoch timestamp.
                         * @member {number|Long|null|undefined} timestamp
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch
                         * @instance
                         */
                        HSMDateTimeUnixEpoch.prototype.timestamp = null;

                        // OneOf field names bound to virtual getters and setters
                        var $oneOfFields;

                        // Virtual OneOf for proto3 optional field
                        Object.defineProperty(HSMDateTimeUnixEpoch.prototype, "_timestamp", {
                            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        /**
                         * Creates a new HSMDateTimeUnixEpoch instance using the specified properties.
                         * @function create
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch
                         * @static
                         * @param {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.IHSMDateTimeUnixEpoch=} [properties] Properties to set
                         * @returns {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch} HSMDateTimeUnixEpoch instance
                         */
                        HSMDateTimeUnixEpoch.create = function create(properties) {
                            return new HSMDateTimeUnixEpoch(properties);
                        };

                        /**
                         * Encodes the specified HSMDateTimeUnixEpoch message. Does not implicitly {@link proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch.verify|verify} messages.
                         * @function encode
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch
                         * @static
                         * @param {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.IHSMDateTimeUnixEpoch} message HSMDateTimeUnixEpoch message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        HSMDateTimeUnixEpoch.encode = function encode(message, writer, q) {
                            if (!writer)
                                writer = $Writer.create();
                            if (q === undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw Error("max depth exceeded");
                            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.timestamp);
                            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                                for (var i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified HSMDateTimeUnixEpoch message, length delimited. Does not implicitly {@link proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch
                         * @static
                         * @param {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.IHSMDateTimeUnixEpoch} message HSMDateTimeUnixEpoch message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        HSMDateTimeUnixEpoch.encodeDelimited = function encodeDelimited(message, writer) {
                            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                        };

                        /**
                         * Decodes a HSMDateTimeUnixEpoch message from the specified reader or buffer.
                         * @function decode
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch} HSMDateTimeUnixEpoch
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        HSMDateTimeUnixEpoch.decode = function decode(reader, length, z, q, g) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (q === undefined)
                                q = 0;
                            if (q > $Reader.recursionLimit)
                                throw Error("max depth exceeded");
                            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                            while (reader.pos < end) {
                                var s = reader.pos;
                                var tag = reader.tag();
                                if (tag === z) {
                                    z = undefined;
                                    break;
                                }
                                var u = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (u !== 0)
                                            break;
                                        message.timestamp = reader.int64();
                                        message._timestamp = "timestamp";
                                        continue;
                                    }
                                }
                                reader.skipType(u, q, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                                }
                            }
                            if (z !== undefined)
                                throw Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a HSMDateTimeUnixEpoch message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch} HSMDateTimeUnixEpoch
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        HSMDateTimeUnixEpoch.decodeDelimited = function decodeDelimited(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a HSMDateTimeUnixEpoch message.
                         * @function verify
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        HSMDateTimeUnixEpoch.verify = function verify(message, q) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (q === undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                return "max depth exceeded";
                            var properties = {};
                            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp")) {
                                properties._timestamp = 1;
                                if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                                    return "timestamp: integer|Long expected";
                            }
                            return null;
                        };

                        /**
                         * Creates a HSMDateTimeUnixEpoch message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch} HSMDateTimeUnixEpoch
                         */
                        HSMDateTimeUnixEpoch.fromObject = function fromObject(object, q) {
                            if (object instanceof C)
                                return object;
                            if (!$util.isObject(object))
                                throw TypeError(".proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch: object expected");
                            if (q === undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw Error("max depth exceeded");
                            var message = new C();
                            if (object.timestamp != null)
                                if ($util.Long)
                                    message.timestamp = $util.Long.fromValue(object.timestamp, false);
                                else if (typeof object.timestamp === "string")
                                    message.timestamp = parseInt(object.timestamp, 10);
                                else if (typeof object.timestamp === "number")
                                    message.timestamp = object.timestamp;
                                else if (typeof object.timestamp === "object")
                                    message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
                            return message;
                        };

                        /**
                         * Creates a plain object from a HSMDateTimeUnixEpoch message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch
                         * @static
                         * @param {proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch} message HSMDateTimeUnixEpoch
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        HSMDateTimeUnixEpoch.toObject = function toObject(message, options, q) {
                            if (!options)
                                options = {};
                            if (q === undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw Error("max depth exceeded");
                            var object = {};
                            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                                    object.timestamp = typeof message.timestamp === "number" ? BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
                                else if (typeof message.timestamp === "number")
                                    object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                                else
                                    object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
                            return object;
                        };

                        /**
                         * Converts this HSMDateTimeUnixEpoch to JSON.
                         * @function toJSON
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        HSMDateTimeUnixEpoch.prototype.toJSON = function toJSON() {
                            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the default type url for HSMDateTimeUnixEpoch
                         * @function getTypeUrl
                         * @memberof proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch
                         * @static
                         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                         * @returns {string} The default type url
                         */
                        HSMDateTimeUnixEpoch.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                            if (typeUrlPrefix === undefined) {
                                typeUrlPrefix = "type.googleapis.com";
                            }
                            return typeUrlPrefix + "/proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch";
                        };

                        return HSMDateTimeUnixEpoch;
                    })();

                    return HSMDateTime;
                })();

                return HSMLocalizableParameter;
            })();

            return HighlyStructuredMessage;
        })();

        Message.HistorySyncMessageAccessStatus = (function() {

            /**
             * Properties of a HistorySyncMessageAccessStatus.
             * @memberof proto.Message
             * @interface IHistorySyncMessageAccessStatus
             * @property {boolean|null} [completeAccessGranted] HistorySyncMessageAccessStatus completeAccessGranted
             */

            /**
             * Constructs a new HistorySyncMessageAccessStatus.
             * @memberof proto.Message
             * @classdesc Represents a HistorySyncMessageAccessStatus.
             * @implements IHistorySyncMessageAccessStatus
             * @constructor
             * @param {proto.Message.IHistorySyncMessageAccessStatus=} [properties] Properties to set
             */
            function HistorySyncMessageAccessStatus(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * HistorySyncMessageAccessStatus completeAccessGranted.
             * @member {boolean|null|undefined} completeAccessGranted
             * @memberof proto.Message.HistorySyncMessageAccessStatus
             * @instance
             */
            HistorySyncMessageAccessStatus.prototype.completeAccessGranted = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncMessageAccessStatus.prototype, "_completeAccessGranted", {
                get: $util.oneOfGetter($oneOfFields = ["completeAccessGranted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new HistorySyncMessageAccessStatus instance using the specified properties.
             * @function create
             * @memberof proto.Message.HistorySyncMessageAccessStatus
             * @static
             * @param {proto.Message.IHistorySyncMessageAccessStatus=} [properties] Properties to set
             * @returns {proto.Message.HistorySyncMessageAccessStatus} HistorySyncMessageAccessStatus instance
             */
            HistorySyncMessageAccessStatus.create = function create(properties) {
                return new HistorySyncMessageAccessStatus(properties);
            };

            /**
             * Encodes the specified HistorySyncMessageAccessStatus message. Does not implicitly {@link proto.Message.HistorySyncMessageAccessStatus.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.HistorySyncMessageAccessStatus
             * @static
             * @param {proto.Message.IHistorySyncMessageAccessStatus} message HistorySyncMessageAccessStatus message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HistorySyncMessageAccessStatus.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.completeAccessGranted != null && Object.hasOwnProperty.call(message, "completeAccessGranted"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.completeAccessGranted);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified HistorySyncMessageAccessStatus message, length delimited. Does not implicitly {@link proto.Message.HistorySyncMessageAccessStatus.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.HistorySyncMessageAccessStatus
             * @static
             * @param {proto.Message.IHistorySyncMessageAccessStatus} message HistorySyncMessageAccessStatus message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HistorySyncMessageAccessStatus.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a HistorySyncMessageAccessStatus message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.HistorySyncMessageAccessStatus
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.HistorySyncMessageAccessStatus} HistorySyncMessageAccessStatus
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HistorySyncMessageAccessStatus.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.completeAccessGranted = reader.bool();
                            message._completeAccessGranted = "completeAccessGranted";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a HistorySyncMessageAccessStatus message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.HistorySyncMessageAccessStatus
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.HistorySyncMessageAccessStatus} HistorySyncMessageAccessStatus
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HistorySyncMessageAccessStatus.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a HistorySyncMessageAccessStatus message.
             * @function verify
             * @memberof proto.Message.HistorySyncMessageAccessStatus
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            HistorySyncMessageAccessStatus.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.completeAccessGranted != null && Object.hasOwnProperty.call(message, "completeAccessGranted")) {
                    properties._completeAccessGranted = 1;
                    if (typeof message.completeAccessGranted !== "boolean")
                        return "completeAccessGranted: boolean expected";
                }
                return null;
            };

            /**
             * Creates a HistorySyncMessageAccessStatus message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.HistorySyncMessageAccessStatus
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.HistorySyncMessageAccessStatus} HistorySyncMessageAccessStatus
             */
            HistorySyncMessageAccessStatus.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.HistorySyncMessageAccessStatus: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.completeAccessGranted != null)
                    message.completeAccessGranted = Boolean(object.completeAccessGranted);
                return message;
            };

            /**
             * Creates a plain object from a HistorySyncMessageAccessStatus message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.HistorySyncMessageAccessStatus
             * @static
             * @param {proto.Message.HistorySyncMessageAccessStatus} message HistorySyncMessageAccessStatus
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            HistorySyncMessageAccessStatus.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.completeAccessGranted != null && Object.hasOwnProperty.call(message, "completeAccessGranted"))
                    object.completeAccessGranted = message.completeAccessGranted;
                return object;
            };

            /**
             * Converts this HistorySyncMessageAccessStatus to JSON.
             * @function toJSON
             * @memberof proto.Message.HistorySyncMessageAccessStatus
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            HistorySyncMessageAccessStatus.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for HistorySyncMessageAccessStatus
             * @function getTypeUrl
             * @memberof proto.Message.HistorySyncMessageAccessStatus
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            HistorySyncMessageAccessStatus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.HistorySyncMessageAccessStatus";
            };

            return HistorySyncMessageAccessStatus;
        })();

        Message.HistorySyncNotification = (function() {

            /**
             * Properties of a HistorySyncNotification.
             * @memberof proto.Message
             * @interface IHistorySyncNotification
             * @property {Uint8Array|null} [fileSha256] HistorySyncNotification fileSha256
             * @property {number|Long|null} [fileLength] HistorySyncNotification fileLength
             * @property {Uint8Array|null} [mediaKey] HistorySyncNotification mediaKey
             * @property {Uint8Array|null} [fileEncSha256] HistorySyncNotification fileEncSha256
             * @property {string|null} [directPath] HistorySyncNotification directPath
             * @property {proto.Message.HistorySyncType|null} [syncType] HistorySyncNotification syncType
             * @property {number|null} [chunkOrder] HistorySyncNotification chunkOrder
             * @property {string|null} [originalMessageId] HistorySyncNotification originalMessageId
             * @property {number|null} [progress] HistorySyncNotification progress
             * @property {number|Long|null} [oldestMsgInChunkTimestampSec] HistorySyncNotification oldestMsgInChunkTimestampSec
             * @property {Uint8Array|null} [initialHistBootstrapInlinePayload] HistorySyncNotification initialHistBootstrapInlinePayload
             * @property {string|null} [peerDataRequestSessionId] HistorySyncNotification peerDataRequestSessionId
             * @property {proto.Message.IFullHistorySyncOnDemandRequestMetadata|null} [fullHistorySyncOnDemandRequestMetadata] HistorySyncNotification fullHistorySyncOnDemandRequestMetadata
             * @property {string|null} [encHandle] HistorySyncNotification encHandle
             * @property {proto.Message.IHistorySyncMessageAccessStatus|null} [messageAccessStatus] HistorySyncNotification messageAccessStatus
             */

            /**
             * Constructs a new HistorySyncNotification.
             * @memberof proto.Message
             * @classdesc Represents a HistorySyncNotification.
             * @implements IHistorySyncNotification
             * @constructor
             * @param {proto.Message.IHistorySyncNotification=} [properties] Properties to set
             */
            function HistorySyncNotification(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * HistorySyncNotification fileSha256.
             * @member {Uint8Array|null|undefined} fileSha256
             * @memberof proto.Message.HistorySyncNotification
             * @instance
             */
            HistorySyncNotification.prototype.fileSha256 = null;

            /**
             * HistorySyncNotification fileLength.
             * @member {number|Long|null|undefined} fileLength
             * @memberof proto.Message.HistorySyncNotification
             * @instance
             */
            HistorySyncNotification.prototype.fileLength = null;

            /**
             * HistorySyncNotification mediaKey.
             * @member {Uint8Array|null|undefined} mediaKey
             * @memberof proto.Message.HistorySyncNotification
             * @instance
             */
            HistorySyncNotification.prototype.mediaKey = null;

            /**
             * HistorySyncNotification fileEncSha256.
             * @member {Uint8Array|null|undefined} fileEncSha256
             * @memberof proto.Message.HistorySyncNotification
             * @instance
             */
            HistorySyncNotification.prototype.fileEncSha256 = null;

            /**
             * HistorySyncNotification directPath.
             * @member {string|null|undefined} directPath
             * @memberof proto.Message.HistorySyncNotification
             * @instance
             */
            HistorySyncNotification.prototype.directPath = null;

            /**
             * HistorySyncNotification syncType.
             * @member {proto.Message.HistorySyncType|null|undefined} syncType
             * @memberof proto.Message.HistorySyncNotification
             * @instance
             */
            HistorySyncNotification.prototype.syncType = null;

            /**
             * HistorySyncNotification chunkOrder.
             * @member {number|null|undefined} chunkOrder
             * @memberof proto.Message.HistorySyncNotification
             * @instance
             */
            HistorySyncNotification.prototype.chunkOrder = null;

            /**
             * HistorySyncNotification originalMessageId.
             * @member {string|null|undefined} originalMessageId
             * @memberof proto.Message.HistorySyncNotification
             * @instance
             */
            HistorySyncNotification.prototype.originalMessageId = null;

            /**
             * HistorySyncNotification progress.
             * @member {number|null|undefined} progress
             * @memberof proto.Message.HistorySyncNotification
             * @instance
             */
            HistorySyncNotification.prototype.progress = null;

            /**
             * HistorySyncNotification oldestMsgInChunkTimestampSec.
             * @member {number|Long|null|undefined} oldestMsgInChunkTimestampSec
             * @memberof proto.Message.HistorySyncNotification
             * @instance
             */
            HistorySyncNotification.prototype.oldestMsgInChunkTimestampSec = null;

            /**
             * HistorySyncNotification initialHistBootstrapInlinePayload.
             * @member {Uint8Array|null|undefined} initialHistBootstrapInlinePayload
             * @memberof proto.Message.HistorySyncNotification
             * @instance
             */
            HistorySyncNotification.prototype.initialHistBootstrapInlinePayload = null;

            /**
             * HistorySyncNotification peerDataRequestSessionId.
             * @member {string|null|undefined} peerDataRequestSessionId
             * @memberof proto.Message.HistorySyncNotification
             * @instance
             */
            HistorySyncNotification.prototype.peerDataRequestSessionId = null;

            /**
             * HistorySyncNotification fullHistorySyncOnDemandRequestMetadata.
             * @member {proto.Message.IFullHistorySyncOnDemandRequestMetadata|null|undefined} fullHistorySyncOnDemandRequestMetadata
             * @memberof proto.Message.HistorySyncNotification
             * @instance
             */
            HistorySyncNotification.prototype.fullHistorySyncOnDemandRequestMetadata = null;

            /**
             * HistorySyncNotification encHandle.
             * @member {string|null|undefined} encHandle
             * @memberof proto.Message.HistorySyncNotification
             * @instance
             */
            HistorySyncNotification.prototype.encHandle = null;

            /**
             * HistorySyncNotification messageAccessStatus.
             * @member {proto.Message.IHistorySyncMessageAccessStatus|null|undefined} messageAccessStatus
             * @memberof proto.Message.HistorySyncNotification
             * @instance
             */
            HistorySyncNotification.prototype.messageAccessStatus = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncNotification.prototype, "_fileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncNotification.prototype, "_fileLength", {
                get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncNotification.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncNotification.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncNotification.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncNotification.prototype, "_syncType", {
                get: $util.oneOfGetter($oneOfFields = ["syncType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncNotification.prototype, "_chunkOrder", {
                get: $util.oneOfGetter($oneOfFields = ["chunkOrder"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncNotification.prototype, "_originalMessageId", {
                get: $util.oneOfGetter($oneOfFields = ["originalMessageId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncNotification.prototype, "_progress", {
                get: $util.oneOfGetter($oneOfFields = ["progress"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncNotification.prototype, "_oldestMsgInChunkTimestampSec", {
                get: $util.oneOfGetter($oneOfFields = ["oldestMsgInChunkTimestampSec"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncNotification.prototype, "_initialHistBootstrapInlinePayload", {
                get: $util.oneOfGetter($oneOfFields = ["initialHistBootstrapInlinePayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncNotification.prototype, "_peerDataRequestSessionId", {
                get: $util.oneOfGetter($oneOfFields = ["peerDataRequestSessionId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncNotification.prototype, "_fullHistorySyncOnDemandRequestMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["fullHistorySyncOnDemandRequestMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncNotification.prototype, "_encHandle", {
                get: $util.oneOfGetter($oneOfFields = ["encHandle"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncNotification.prototype, "_messageAccessStatus", {
                get: $util.oneOfGetter($oneOfFields = ["messageAccessStatus"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new HistorySyncNotification instance using the specified properties.
             * @function create
             * @memberof proto.Message.HistorySyncNotification
             * @static
             * @param {proto.Message.IHistorySyncNotification=} [properties] Properties to set
             * @returns {proto.Message.HistorySyncNotification} HistorySyncNotification instance
             */
            HistorySyncNotification.create = function create(properties) {
                return new HistorySyncNotification(properties);
            };

            /**
             * Encodes the specified HistorySyncNotification message. Does not implicitly {@link proto.Message.HistorySyncNotification.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.HistorySyncNotification
             * @static
             * @param {proto.Message.IHistorySyncNotification} message HistorySyncNotification message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HistorySyncNotification.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.fileSha256);
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                    writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.fileLength);
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.mediaKey);
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.fileEncSha256);
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.directPath);
                if (message.syncType != null && Object.hasOwnProperty.call(message, "syncType"))
                    writer.uint32(/* id 6, wireType 0 =*/48).int32(message.syncType);
                if (message.chunkOrder != null && Object.hasOwnProperty.call(message, "chunkOrder"))
                    writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.chunkOrder);
                if (message.originalMessageId != null && Object.hasOwnProperty.call(message, "originalMessageId"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.originalMessageId);
                if (message.progress != null && Object.hasOwnProperty.call(message, "progress"))
                    writer.uint32(/* id 9, wireType 0 =*/72).uint32(message.progress);
                if (message.oldestMsgInChunkTimestampSec != null && Object.hasOwnProperty.call(message, "oldestMsgInChunkTimestampSec"))
                    writer.uint32(/* id 10, wireType 0 =*/80).int64(message.oldestMsgInChunkTimestampSec);
                if (message.initialHistBootstrapInlinePayload != null && Object.hasOwnProperty.call(message, "initialHistBootstrapInlinePayload"))
                    writer.uint32(/* id 11, wireType 2 =*/90).bytes(message.initialHistBootstrapInlinePayload);
                if (message.peerDataRequestSessionId != null && Object.hasOwnProperty.call(message, "peerDataRequestSessionId"))
                    writer.uint32(/* id 12, wireType 2 =*/98).string(message.peerDataRequestSessionId);
                if (message.fullHistorySyncOnDemandRequestMetadata != null && Object.hasOwnProperty.call(message, "fullHistorySyncOnDemandRequestMetadata"))
                    $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.encode(message.fullHistorySyncOnDemandRequestMetadata, writer.uint32(/* id 13, wireType 2 =*/106).fork(), q + 1).ldelim();
                if (message.encHandle != null && Object.hasOwnProperty.call(message, "encHandle"))
                    writer.uint32(/* id 14, wireType 2 =*/114).string(message.encHandle);
                if (message.messageAccessStatus != null && Object.hasOwnProperty.call(message, "messageAccessStatus"))
                    $root.proto.Message.HistorySyncMessageAccessStatus.encode(message.messageAccessStatus, writer.uint32(/* id 15, wireType 2 =*/122).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified HistorySyncNotification message, length delimited. Does not implicitly {@link proto.Message.HistorySyncNotification.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.HistorySyncNotification
             * @static
             * @param {proto.Message.IHistorySyncNotification} message HistorySyncNotification message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            HistorySyncNotification.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a HistorySyncNotification message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.HistorySyncNotification
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.HistorySyncNotification} HistorySyncNotification
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HistorySyncNotification.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.fileSha256 = reader.bytes();
                            message._fileSha256 = "fileSha256";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.fileLength = reader.uint64();
                            message._fileLength = "fileLength";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.mediaKey = reader.bytes();
                            message._mediaKey = "mediaKey";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.fileEncSha256 = reader.bytes();
                            message._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.directPath = reader.stringVerify();
                            message._directPath = "directPath";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.syncType = reader.int32();
                            message._syncType = "syncType";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            message.chunkOrder = reader.uint32();
                            message._chunkOrder = "chunkOrder";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.originalMessageId = reader.stringVerify();
                            message._originalMessageId = "originalMessageId";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            message.progress = reader.uint32();
                            message._progress = "progress";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            message.oldestMsgInChunkTimestampSec = reader.int64();
                            message._oldestMsgInChunkTimestampSec = "oldestMsgInChunkTimestampSec";
                            continue;
                        }
                    case 11: {
                            if (u !== 2)
                                break;
                            message.initialHistBootstrapInlinePayload = reader.bytes();
                            message._initialHistBootstrapInlinePayload = "initialHistBootstrapInlinePayload";
                            continue;
                        }
                    case 12: {
                            if (u !== 2)
                                break;
                            message.peerDataRequestSessionId = reader.stringVerify();
                            message._peerDataRequestSessionId = "peerDataRequestSessionId";
                            continue;
                        }
                    case 13: {
                            if (u !== 2)
                                break;
                            message.fullHistorySyncOnDemandRequestMetadata = $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.fullHistorySyncOnDemandRequestMetadata);
                            message._fullHistorySyncOnDemandRequestMetadata = "fullHistorySyncOnDemandRequestMetadata";
                            continue;
                        }
                    case 14: {
                            if (u !== 2)
                                break;
                            message.encHandle = reader.stringVerify();
                            message._encHandle = "encHandle";
                            continue;
                        }
                    case 15: {
                            if (u !== 2)
                                break;
                            message.messageAccessStatus = $root.proto.Message.HistorySyncMessageAccessStatus.decode(reader, reader.uint32(), undefined, q + 1, message.messageAccessStatus);
                            message._messageAccessStatus = "messageAccessStatus";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a HistorySyncNotification message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.HistorySyncNotification
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.HistorySyncNotification} HistorySyncNotification
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            HistorySyncNotification.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a HistorySyncNotification message.
             * @function verify
             * @memberof proto.Message.HistorySyncNotification
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            HistorySyncNotification.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256")) {
                    properties._fileSha256 = 1;
                    if (!(message.fileSha256 && typeof message.fileSha256.length === "number" || $util.isString(message.fileSha256)))
                        return "fileSha256: buffer expected";
                }
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength")) {
                    properties._fileLength = 1;
                    if (!$util.isInteger(message.fileLength) && !(message.fileLength && $util.isInteger(message.fileLength.low) && $util.isInteger(message.fileLength.high)))
                        return "fileLength: integer|Long expected";
                }
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey")) {
                    properties._mediaKey = 1;
                    if (!(message.mediaKey && typeof message.mediaKey.length === "number" || $util.isString(message.mediaKey)))
                        return "mediaKey: buffer expected";
                }
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256")) {
                    properties._fileEncSha256 = 1;
                    if (!(message.fileEncSha256 && typeof message.fileEncSha256.length === "number" || $util.isString(message.fileEncSha256)))
                        return "fileEncSha256: buffer expected";
                }
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath")) {
                    properties._directPath = 1;
                    if (!$util.isString(message.directPath))
                        return "directPath: string expected";
                }
                if (message.syncType != null && Object.hasOwnProperty.call(message, "syncType")) {
                    properties._syncType = 1;
                    if (typeof message.syncType !== "number" || (message.syncType | 0) !== message.syncType)
                        return "syncType: enum value expected";
                }
                if (message.chunkOrder != null && Object.hasOwnProperty.call(message, "chunkOrder")) {
                    properties._chunkOrder = 1;
                    if (!$util.isInteger(message.chunkOrder))
                        return "chunkOrder: integer expected";
                }
                if (message.originalMessageId != null && Object.hasOwnProperty.call(message, "originalMessageId")) {
                    properties._originalMessageId = 1;
                    if (!$util.isString(message.originalMessageId))
                        return "originalMessageId: string expected";
                }
                if (message.progress != null && Object.hasOwnProperty.call(message, "progress")) {
                    properties._progress = 1;
                    if (!$util.isInteger(message.progress))
                        return "progress: integer expected";
                }
                if (message.oldestMsgInChunkTimestampSec != null && Object.hasOwnProperty.call(message, "oldestMsgInChunkTimestampSec")) {
                    properties._oldestMsgInChunkTimestampSec = 1;
                    if (!$util.isInteger(message.oldestMsgInChunkTimestampSec) && !(message.oldestMsgInChunkTimestampSec && $util.isInteger(message.oldestMsgInChunkTimestampSec.low) && $util.isInteger(message.oldestMsgInChunkTimestampSec.high)))
                        return "oldestMsgInChunkTimestampSec: integer|Long expected";
                }
                if (message.initialHistBootstrapInlinePayload != null && Object.hasOwnProperty.call(message, "initialHistBootstrapInlinePayload")) {
                    properties._initialHistBootstrapInlinePayload = 1;
                    if (!(message.initialHistBootstrapInlinePayload && typeof message.initialHistBootstrapInlinePayload.length === "number" || $util.isString(message.initialHistBootstrapInlinePayload)))
                        return "initialHistBootstrapInlinePayload: buffer expected";
                }
                if (message.peerDataRequestSessionId != null && Object.hasOwnProperty.call(message, "peerDataRequestSessionId")) {
                    properties._peerDataRequestSessionId = 1;
                    if (!$util.isString(message.peerDataRequestSessionId))
                        return "peerDataRequestSessionId: string expected";
                }
                if (message.fullHistorySyncOnDemandRequestMetadata != null && Object.hasOwnProperty.call(message, "fullHistorySyncOnDemandRequestMetadata")) {
                    properties._fullHistorySyncOnDemandRequestMetadata = 1;
                    {
                        var error = $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.verify(message.fullHistorySyncOnDemandRequestMetadata, q + 1);
                        if (error)
                            return "fullHistorySyncOnDemandRequestMetadata." + error;
                    }
                }
                if (message.encHandle != null && Object.hasOwnProperty.call(message, "encHandle")) {
                    properties._encHandle = 1;
                    if (!$util.isString(message.encHandle))
                        return "encHandle: string expected";
                }
                if (message.messageAccessStatus != null && Object.hasOwnProperty.call(message, "messageAccessStatus")) {
                    properties._messageAccessStatus = 1;
                    {
                        var error = $root.proto.Message.HistorySyncMessageAccessStatus.verify(message.messageAccessStatus, q + 1);
                        if (error)
                            return "messageAccessStatus." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a HistorySyncNotification message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.HistorySyncNotification
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.HistorySyncNotification} HistorySyncNotification
             */
            HistorySyncNotification.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.HistorySyncNotification: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.fileSha256 != null)
                    if (typeof object.fileSha256 === "string")
                        $util.base64.decode(object.fileSha256, message.fileSha256 = $util.newBuffer($util.base64.length(object.fileSha256)), 0);
                    else if (object.fileSha256.length >= 0)
                        message.fileSha256 = object.fileSha256;
                if (object.fileLength != null)
                    if ($util.Long)
                        message.fileLength = $util.Long.fromValue(object.fileLength, true);
                    else if (typeof object.fileLength === "string")
                        message.fileLength = parseInt(object.fileLength, 10);
                    else if (typeof object.fileLength === "number")
                        message.fileLength = object.fileLength;
                    else if (typeof object.fileLength === "object")
                        message.fileLength = new $util.LongBits(object.fileLength.low >>> 0, object.fileLength.high >>> 0).toNumber(true);
                if (object.mediaKey != null)
                    if (typeof object.mediaKey === "string")
                        $util.base64.decode(object.mediaKey, message.mediaKey = $util.newBuffer($util.base64.length(object.mediaKey)), 0);
                    else if (object.mediaKey.length >= 0)
                        message.mediaKey = object.mediaKey;
                if (object.fileEncSha256 != null)
                    if (typeof object.fileEncSha256 === "string")
                        $util.base64.decode(object.fileEncSha256, message.fileEncSha256 = $util.newBuffer($util.base64.length(object.fileEncSha256)), 0);
                    else if (object.fileEncSha256.length >= 0)
                        message.fileEncSha256 = object.fileEncSha256;
                if (object.directPath != null)
                    message.directPath = String(object.directPath);
                switch (object.syncType) {
                case "INITIAL_BOOTSTRAP":
                case 0:
                    message.syncType = 0;
                    break;
                case "INITIAL_STATUS_V3":
                case 1:
                    message.syncType = 1;
                    break;
                case "FULL":
                case 2:
                    message.syncType = 2;
                    break;
                case "RECENT":
                case 3:
                    message.syncType = 3;
                    break;
                case "PUSH_NAME":
                case 4:
                    message.syncType = 4;
                    break;
                case "NON_BLOCKING_DATA":
                case 5:
                    message.syncType = 5;
                    break;
                case "ON_DEMAND":
                case 6:
                    message.syncType = 6;
                    break;
                case "NO_HISTORY":
                case 7:
                    message.syncType = 7;
                    break;
                case "MESSAGE_ACCESS_STATUS":
                case 8:
                    message.syncType = 8;
                    break;
                default:
                    if (typeof object.syncType === "number" && (object.syncType | 0) === object.syncType)
                        message.syncType = object.syncType;
                }
                if (object.chunkOrder != null)
                    message.chunkOrder = object.chunkOrder >>> 0;
                if (object.originalMessageId != null)
                    message.originalMessageId = String(object.originalMessageId);
                if (object.progress != null)
                    message.progress = object.progress >>> 0;
                if (object.oldestMsgInChunkTimestampSec != null)
                    if ($util.Long)
                        message.oldestMsgInChunkTimestampSec = $util.Long.fromValue(object.oldestMsgInChunkTimestampSec, false);
                    else if (typeof object.oldestMsgInChunkTimestampSec === "string")
                        message.oldestMsgInChunkTimestampSec = parseInt(object.oldestMsgInChunkTimestampSec, 10);
                    else if (typeof object.oldestMsgInChunkTimestampSec === "number")
                        message.oldestMsgInChunkTimestampSec = object.oldestMsgInChunkTimestampSec;
                    else if (typeof object.oldestMsgInChunkTimestampSec === "object")
                        message.oldestMsgInChunkTimestampSec = new $util.LongBits(object.oldestMsgInChunkTimestampSec.low >>> 0, object.oldestMsgInChunkTimestampSec.high >>> 0).toNumber();
                if (object.initialHistBootstrapInlinePayload != null)
                    if (typeof object.initialHistBootstrapInlinePayload === "string")
                        $util.base64.decode(object.initialHistBootstrapInlinePayload, message.initialHistBootstrapInlinePayload = $util.newBuffer($util.base64.length(object.initialHistBootstrapInlinePayload)), 0);
                    else if (object.initialHistBootstrapInlinePayload.length >= 0)
                        message.initialHistBootstrapInlinePayload = object.initialHistBootstrapInlinePayload;
                if (object.peerDataRequestSessionId != null)
                    message.peerDataRequestSessionId = String(object.peerDataRequestSessionId);
                if (object.fullHistorySyncOnDemandRequestMetadata != null) {
                    if (!$util.isObject(object.fullHistorySyncOnDemandRequestMetadata))
                        throw TypeError(".proto.Message.HistorySyncNotification.fullHistorySyncOnDemandRequestMetadata: object expected");
                    message.fullHistorySyncOnDemandRequestMetadata = $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.fromObject(object.fullHistorySyncOnDemandRequestMetadata, q + 1);
                }
                if (object.encHandle != null)
                    message.encHandle = String(object.encHandle);
                if (object.messageAccessStatus != null) {
                    if (!$util.isObject(object.messageAccessStatus))
                        throw TypeError(".proto.Message.HistorySyncNotification.messageAccessStatus: object expected");
                    message.messageAccessStatus = $root.proto.Message.HistorySyncMessageAccessStatus.fromObject(object.messageAccessStatus, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a HistorySyncNotification message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.HistorySyncNotification
             * @static
             * @param {proto.Message.HistorySyncNotification} message HistorySyncNotification
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            HistorySyncNotification.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                    object.fileSha256 = options.bytes === String ? $util.base64.encode(message.fileSha256, 0, message.fileSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileSha256) : message.fileSha256;
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.fileLength = typeof message.fileLength === "number" ? BigInt(message.fileLength) : $util.Long.fromBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0, true).toBigInt();
                    else if (typeof message.fileLength === "number")
                        object.fileLength = options.longs === String ? String(message.fileLength) : message.fileLength;
                    else
                        object.fileLength = options.longs === String ? $util.Long.prototype.toString.call(message.fileLength) : options.longs === Number ? new $util.LongBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0).toNumber(true) : message.fileLength;
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    object.mediaKey = options.bytes === String ? $util.base64.encode(message.mediaKey, 0, message.mediaKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.mediaKey) : message.mediaKey;
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    object.fileEncSha256 = options.bytes === String ? $util.base64.encode(message.fileEncSha256, 0, message.fileEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileEncSha256) : message.fileEncSha256;
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    object.directPath = message.directPath;
                if (message.syncType != null && Object.hasOwnProperty.call(message, "syncType"))
                    object.syncType = options.enums === String ? $root.proto.Message.HistorySyncType[message.syncType] === undefined ? message.syncType : $root.proto.Message.HistorySyncType[message.syncType] : message.syncType;
                if (message.chunkOrder != null && Object.hasOwnProperty.call(message, "chunkOrder"))
                    object.chunkOrder = message.chunkOrder;
                if (message.originalMessageId != null && Object.hasOwnProperty.call(message, "originalMessageId"))
                    object.originalMessageId = message.originalMessageId;
                if (message.progress != null && Object.hasOwnProperty.call(message, "progress"))
                    object.progress = message.progress;
                if (message.oldestMsgInChunkTimestampSec != null && Object.hasOwnProperty.call(message, "oldestMsgInChunkTimestampSec"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.oldestMsgInChunkTimestampSec = typeof message.oldestMsgInChunkTimestampSec === "number" ? BigInt(message.oldestMsgInChunkTimestampSec) : $util.Long.fromBits(message.oldestMsgInChunkTimestampSec.low >>> 0, message.oldestMsgInChunkTimestampSec.high >>> 0, false).toBigInt();
                    else if (typeof message.oldestMsgInChunkTimestampSec === "number")
                        object.oldestMsgInChunkTimestampSec = options.longs === String ? String(message.oldestMsgInChunkTimestampSec) : message.oldestMsgInChunkTimestampSec;
                    else
                        object.oldestMsgInChunkTimestampSec = options.longs === String ? $util.Long.prototype.toString.call(message.oldestMsgInChunkTimestampSec) : options.longs === Number ? new $util.LongBits(message.oldestMsgInChunkTimestampSec.low >>> 0, message.oldestMsgInChunkTimestampSec.high >>> 0).toNumber() : message.oldestMsgInChunkTimestampSec;
                if (message.initialHistBootstrapInlinePayload != null && Object.hasOwnProperty.call(message, "initialHistBootstrapInlinePayload"))
                    object.initialHistBootstrapInlinePayload = options.bytes === String ? $util.base64.encode(message.initialHistBootstrapInlinePayload, 0, message.initialHistBootstrapInlinePayload.length) : options.bytes === Array ? Array.prototype.slice.call(message.initialHistBootstrapInlinePayload) : message.initialHistBootstrapInlinePayload;
                if (message.peerDataRequestSessionId != null && Object.hasOwnProperty.call(message, "peerDataRequestSessionId"))
                    object.peerDataRequestSessionId = message.peerDataRequestSessionId;
                if (message.fullHistorySyncOnDemandRequestMetadata != null && Object.hasOwnProperty.call(message, "fullHistorySyncOnDemandRequestMetadata"))
                    object.fullHistorySyncOnDemandRequestMetadata = $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.toObject(message.fullHistorySyncOnDemandRequestMetadata, options, q + 1);
                if (message.encHandle != null && Object.hasOwnProperty.call(message, "encHandle"))
                    object.encHandle = message.encHandle;
                if (message.messageAccessStatus != null && Object.hasOwnProperty.call(message, "messageAccessStatus"))
                    object.messageAccessStatus = $root.proto.Message.HistorySyncMessageAccessStatus.toObject(message.messageAccessStatus, options, q + 1);
                return object;
            };

            /**
             * Converts this HistorySyncNotification to JSON.
             * @function toJSON
             * @memberof proto.Message.HistorySyncNotification
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            HistorySyncNotification.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for HistorySyncNotification
             * @function getTypeUrl
             * @memberof proto.Message.HistorySyncNotification
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            HistorySyncNotification.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.HistorySyncNotification";
            };

            return HistorySyncNotification;
        })();

        /**
         * HistorySyncType enum.
         * @name proto.Message.HistorySyncType
         * @enum {number}
         * @property {number} INITIAL_BOOTSTRAP=0 INITIAL_BOOTSTRAP value
         * @property {number} INITIAL_STATUS_V3=1 INITIAL_STATUS_V3 value
         * @property {number} FULL=2 FULL value
         * @property {number} RECENT=3 RECENT value
         * @property {number} PUSH_NAME=4 PUSH_NAME value
         * @property {number} NON_BLOCKING_DATA=5 NON_BLOCKING_DATA value
         * @property {number} ON_DEMAND=6 ON_DEMAND value
         * @property {number} NO_HISTORY=7 NO_HISTORY value
         * @property {number} MESSAGE_ACCESS_STATUS=8 MESSAGE_ACCESS_STATUS value
         */
        Message.HistorySyncType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "INITIAL_BOOTSTRAP"] = 0;
            values[valuesById[1] = "INITIAL_STATUS_V3"] = 1;
            values[valuesById[2] = "FULL"] = 2;
            values[valuesById[3] = "RECENT"] = 3;
            values[valuesById[4] = "PUSH_NAME"] = 4;
            values[valuesById[5] = "NON_BLOCKING_DATA"] = 5;
            values[valuesById[6] = "ON_DEMAND"] = 6;
            values[valuesById[7] = "NO_HISTORY"] = 7;
            values[valuesById[8] = "MESSAGE_ACCESS_STATUS"] = 8;
            return values;
        })();

        Message.ImageMessage = (function() {

            /**
             * Properties of an ImageMessage.
             * @memberof proto.Message
             * @interface IImageMessage
             * @property {string|null} [url] ImageMessage url
             * @property {string|null} [mimetype] ImageMessage mimetype
             * @property {string|null} [caption] ImageMessage caption
             * @property {Uint8Array|null} [fileSha256] ImageMessage fileSha256
             * @property {number|Long|null} [fileLength] ImageMessage fileLength
             * @property {number|null} [height] ImageMessage height
             * @property {number|null} [width] ImageMessage width
             * @property {Uint8Array|null} [mediaKey] ImageMessage mediaKey
             * @property {Uint8Array|null} [fileEncSha256] ImageMessage fileEncSha256
             * @property {Array.<proto.IInteractiveAnnotation>|null} [interactiveAnnotations] ImageMessage interactiveAnnotations
             * @property {string|null} [directPath] ImageMessage directPath
             * @property {number|Long|null} [mediaKeyTimestamp] ImageMessage mediaKeyTimestamp
             * @property {Uint8Array|null} [jpegThumbnail] ImageMessage jpegThumbnail
             * @property {proto.IContextInfo|null} [contextInfo] ImageMessage contextInfo
             * @property {Uint8Array|null} [firstScanSidecar] ImageMessage firstScanSidecar
             * @property {number|null} [firstScanLength] ImageMessage firstScanLength
             * @property {number|null} [experimentGroupId] ImageMessage experimentGroupId
             * @property {Uint8Array|null} [scansSidecar] ImageMessage scansSidecar
             * @property {Array.<number>|null} [scanLengths] ImageMessage scanLengths
             * @property {Uint8Array|null} [midQualityFileSha256] ImageMessage midQualityFileSha256
             * @property {Uint8Array|null} [midQualityFileEncSha256] ImageMessage midQualityFileEncSha256
             * @property {boolean|null} [viewOnce] ImageMessage viewOnce
             * @property {string|null} [thumbnailDirectPath] ImageMessage thumbnailDirectPath
             * @property {Uint8Array|null} [thumbnailSha256] ImageMessage thumbnailSha256
             * @property {Uint8Array|null} [thumbnailEncSha256] ImageMessage thumbnailEncSha256
             * @property {string|null} [staticUrl] ImageMessage staticUrl
             * @property {Array.<proto.IInteractiveAnnotation>|null} [annotations] ImageMessage annotations
             * @property {proto.Message.ImageMessage.ImageSourceType|null} [imageSourceType] ImageMessage imageSourceType
             * @property {string|null} [accessibilityLabel] ImageMessage accessibilityLabel
             * @property {string|null} [qrUrl] ImageMessage qrUrl
             */

            /**
             * Constructs a new ImageMessage.
             * @memberof proto.Message
             * @classdesc Represents an ImageMessage.
             * @implements IImageMessage
             * @constructor
             * @param {proto.Message.IImageMessage=} [properties] Properties to set
             */
            function ImageMessage(properties) {
                this.interactiveAnnotations = [];
                this.scanLengths = [];
                this.annotations = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ImageMessage url.
             * @member {string|null|undefined} url
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.url = null;

            /**
             * ImageMessage mimetype.
             * @member {string|null|undefined} mimetype
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.mimetype = null;

            /**
             * ImageMessage caption.
             * @member {string|null|undefined} caption
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.caption = null;

            /**
             * ImageMessage fileSha256.
             * @member {Uint8Array|null|undefined} fileSha256
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.fileSha256 = null;

            /**
             * ImageMessage fileLength.
             * @member {number|Long|null|undefined} fileLength
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.fileLength = null;

            /**
             * ImageMessage height.
             * @member {number|null|undefined} height
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.height = null;

            /**
             * ImageMessage width.
             * @member {number|null|undefined} width
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.width = null;

            /**
             * ImageMessage mediaKey.
             * @member {Uint8Array|null|undefined} mediaKey
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.mediaKey = null;

            /**
             * ImageMessage fileEncSha256.
             * @member {Uint8Array|null|undefined} fileEncSha256
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.fileEncSha256 = null;

            /**
             * ImageMessage interactiveAnnotations.
             * @member {Array.<proto.IInteractiveAnnotation>} interactiveAnnotations
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.interactiveAnnotations = $util.emptyArray;

            /**
             * ImageMessage directPath.
             * @member {string|null|undefined} directPath
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.directPath = null;

            /**
             * ImageMessage mediaKeyTimestamp.
             * @member {number|Long|null|undefined} mediaKeyTimestamp
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.mediaKeyTimestamp = null;

            /**
             * ImageMessage jpegThumbnail.
             * @member {Uint8Array|null|undefined} jpegThumbnail
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.jpegThumbnail = null;

            /**
             * ImageMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.contextInfo = null;

            /**
             * ImageMessage firstScanSidecar.
             * @member {Uint8Array|null|undefined} firstScanSidecar
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.firstScanSidecar = null;

            /**
             * ImageMessage firstScanLength.
             * @member {number|null|undefined} firstScanLength
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.firstScanLength = null;

            /**
             * ImageMessage experimentGroupId.
             * @member {number|null|undefined} experimentGroupId
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.experimentGroupId = null;

            /**
             * ImageMessage scansSidecar.
             * @member {Uint8Array|null|undefined} scansSidecar
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.scansSidecar = null;

            /**
             * ImageMessage scanLengths.
             * @member {Array.<number>} scanLengths
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.scanLengths = $util.emptyArray;

            /**
             * ImageMessage midQualityFileSha256.
             * @member {Uint8Array|null|undefined} midQualityFileSha256
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.midQualityFileSha256 = null;

            /**
             * ImageMessage midQualityFileEncSha256.
             * @member {Uint8Array|null|undefined} midQualityFileEncSha256
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.midQualityFileEncSha256 = null;

            /**
             * ImageMessage viewOnce.
             * @member {boolean|null|undefined} viewOnce
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.viewOnce = null;

            /**
             * ImageMessage thumbnailDirectPath.
             * @member {string|null|undefined} thumbnailDirectPath
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.thumbnailDirectPath = null;

            /**
             * ImageMessage thumbnailSha256.
             * @member {Uint8Array|null|undefined} thumbnailSha256
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.thumbnailSha256 = null;

            /**
             * ImageMessage thumbnailEncSha256.
             * @member {Uint8Array|null|undefined} thumbnailEncSha256
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.thumbnailEncSha256 = null;

            /**
             * ImageMessage staticUrl.
             * @member {string|null|undefined} staticUrl
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.staticUrl = null;

            /**
             * ImageMessage annotations.
             * @member {Array.<proto.IInteractiveAnnotation>} annotations
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.annotations = $util.emptyArray;

            /**
             * ImageMessage imageSourceType.
             * @member {proto.Message.ImageMessage.ImageSourceType|null|undefined} imageSourceType
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.imageSourceType = null;

            /**
             * ImageMessage accessibilityLabel.
             * @member {string|null|undefined} accessibilityLabel
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.accessibilityLabel = null;

            /**
             * ImageMessage qrUrl.
             * @member {string|null|undefined} qrUrl
             * @memberof proto.Message.ImageMessage
             * @instance
             */
            ImageMessage.prototype.qrUrl = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_mimetype", {
                get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_fileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_fileLength", {
                get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_height", {
                get: $util.oneOfGetter($oneOfFields = ["height"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_width", {
                get: $util.oneOfGetter($oneOfFields = ["width"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_firstScanSidecar", {
                get: $util.oneOfGetter($oneOfFields = ["firstScanSidecar"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_firstScanLength", {
                get: $util.oneOfGetter($oneOfFields = ["firstScanLength"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_experimentGroupId", {
                get: $util.oneOfGetter($oneOfFields = ["experimentGroupId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_scansSidecar", {
                get: $util.oneOfGetter($oneOfFields = ["scansSidecar"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_midQualityFileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["midQualityFileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_midQualityFileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["midQualityFileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_viewOnce", {
                get: $util.oneOfGetter($oneOfFields = ["viewOnce"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_thumbnailDirectPath", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailDirectPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_thumbnailSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_thumbnailEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_staticUrl", {
                get: $util.oneOfGetter($oneOfFields = ["staticUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_imageSourceType", {
                get: $util.oneOfGetter($oneOfFields = ["imageSourceType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_accessibilityLabel", {
                get: $util.oneOfGetter($oneOfFields = ["accessibilityLabel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ImageMessage.prototype, "_qrUrl", {
                get: $util.oneOfGetter($oneOfFields = ["qrUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ImageMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.ImageMessage
             * @static
             * @param {proto.Message.IImageMessage=} [properties] Properties to set
             * @returns {proto.Message.ImageMessage} ImageMessage instance
             */
            ImageMessage.create = function create(properties) {
                return new ImageMessage(properties);
            };

            /**
             * Encodes the specified ImageMessage message. Does not implicitly {@link proto.Message.ImageMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.ImageMessage
             * @static
             * @param {proto.Message.IImageMessage} message ImageMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ImageMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.url);
                if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.mimetype);
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.caption);
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.fileSha256);
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                    writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.fileLength);
                if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                    writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.height);
                if (message.width != null && Object.hasOwnProperty.call(message, "width"))
                    writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.width);
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.mediaKey);
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    writer.uint32(/* id 9, wireType 2 =*/74).bytes(message.fileEncSha256);
                if (message.interactiveAnnotations != null && message.interactiveAnnotations.length)
                    for (var i = 0; i < message.interactiveAnnotations.length; ++i)
                        $root.proto.InteractiveAnnotation.encode(message.interactiveAnnotations[i], writer.uint32(/* id 10, wireType 2 =*/82).fork(), q + 1).ldelim();
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.directPath);
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                    writer.uint32(/* id 12, wireType 0 =*/96).int64(message.mediaKeyTimestamp);
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    writer.uint32(/* id 16, wireType 2 =*/130).bytes(message.jpegThumbnail);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
                if (message.firstScanSidecar != null && Object.hasOwnProperty.call(message, "firstScanSidecar"))
                    writer.uint32(/* id 18, wireType 2 =*/146).bytes(message.firstScanSidecar);
                if (message.firstScanLength != null && Object.hasOwnProperty.call(message, "firstScanLength"))
                    writer.uint32(/* id 19, wireType 0 =*/152).uint32(message.firstScanLength);
                if (message.experimentGroupId != null && Object.hasOwnProperty.call(message, "experimentGroupId"))
                    writer.uint32(/* id 20, wireType 0 =*/160).uint32(message.experimentGroupId);
                if (message.scansSidecar != null && Object.hasOwnProperty.call(message, "scansSidecar"))
                    writer.uint32(/* id 21, wireType 2 =*/170).bytes(message.scansSidecar);
                if (message.scanLengths != null && message.scanLengths.length) {
                    writer.uint32(/* id 22, wireType 2 =*/178).fork();
                    for (var i = 0; i < message.scanLengths.length; ++i)
                        writer.uint32(message.scanLengths[i]);
                    writer.ldelim();
                }
                if (message.midQualityFileSha256 != null && Object.hasOwnProperty.call(message, "midQualityFileSha256"))
                    writer.uint32(/* id 23, wireType 2 =*/186).bytes(message.midQualityFileSha256);
                if (message.midQualityFileEncSha256 != null && Object.hasOwnProperty.call(message, "midQualityFileEncSha256"))
                    writer.uint32(/* id 24, wireType 2 =*/194).bytes(message.midQualityFileEncSha256);
                if (message.viewOnce != null && Object.hasOwnProperty.call(message, "viewOnce"))
                    writer.uint32(/* id 25, wireType 0 =*/200).bool(message.viewOnce);
                if (message.thumbnailDirectPath != null && Object.hasOwnProperty.call(message, "thumbnailDirectPath"))
                    writer.uint32(/* id 26, wireType 2 =*/210).string(message.thumbnailDirectPath);
                if (message.thumbnailSha256 != null && Object.hasOwnProperty.call(message, "thumbnailSha256"))
                    writer.uint32(/* id 27, wireType 2 =*/218).bytes(message.thumbnailSha256);
                if (message.thumbnailEncSha256 != null && Object.hasOwnProperty.call(message, "thumbnailEncSha256"))
                    writer.uint32(/* id 28, wireType 2 =*/226).bytes(message.thumbnailEncSha256);
                if (message.staticUrl != null && Object.hasOwnProperty.call(message, "staticUrl"))
                    writer.uint32(/* id 29, wireType 2 =*/234).string(message.staticUrl);
                if (message.annotations != null && message.annotations.length)
                    for (var i = 0; i < message.annotations.length; ++i)
                        $root.proto.InteractiveAnnotation.encode(message.annotations[i], writer.uint32(/* id 30, wireType 2 =*/242).fork(), q + 1).ldelim();
                if (message.imageSourceType != null && Object.hasOwnProperty.call(message, "imageSourceType"))
                    writer.uint32(/* id 31, wireType 0 =*/248).int32(message.imageSourceType);
                if (message.accessibilityLabel != null && Object.hasOwnProperty.call(message, "accessibilityLabel"))
                    writer.uint32(/* id 32, wireType 2 =*/258).string(message.accessibilityLabel);
                if (message.qrUrl != null && Object.hasOwnProperty.call(message, "qrUrl"))
                    writer.uint32(/* id 34, wireType 2 =*/274).string(message.qrUrl);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ImageMessage message, length delimited. Does not implicitly {@link proto.Message.ImageMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.ImageMessage
             * @static
             * @param {proto.Message.IImageMessage} message ImageMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ImageMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an ImageMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.ImageMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.ImageMessage} ImageMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ImageMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.url = reader.stringVerify();
                            message._url = "url";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.mimetype = reader.stringVerify();
                            message._mimetype = "mimetype";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.caption = reader.stringVerify();
                            message._caption = "caption";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.fileSha256 = reader.bytes();
                            message._fileSha256 = "fileSha256";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.fileLength = reader.uint64();
                            message._fileLength = "fileLength";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.height = reader.uint32();
                            message._height = "height";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            message.width = reader.uint32();
                            message._width = "width";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.mediaKey = reader.bytes();
                            message._mediaKey = "mediaKey";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            message.fileEncSha256 = reader.bytes();
                            message._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 10: {
                            if (u !== 2)
                                break;
                            if (!(message.interactiveAnnotations && message.interactiveAnnotations.length))
                                message.interactiveAnnotations = [];
                            message.interactiveAnnotations.push($root.proto.InteractiveAnnotation.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 11: {
                            if (u !== 2)
                                break;
                            message.directPath = reader.stringVerify();
                            message._directPath = "directPath";
                            continue;
                        }
                    case 12: {
                            if (u !== 0)
                                break;
                            message.mediaKeyTimestamp = reader.int64();
                            message._mediaKeyTimestamp = "mediaKeyTimestamp";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            message.jpegThumbnail = reader.bytes();
                            message._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 18: {
                            if (u !== 2)
                                break;
                            message.firstScanSidecar = reader.bytes();
                            message._firstScanSidecar = "firstScanSidecar";
                            continue;
                        }
                    case 19: {
                            if (u !== 0)
                                break;
                            message.firstScanLength = reader.uint32();
                            message._firstScanLength = "firstScanLength";
                            continue;
                        }
                    case 20: {
                            if (u !== 0)
                                break;
                            message.experimentGroupId = reader.uint32();
                            message._experimentGroupId = "experimentGroupId";
                            continue;
                        }
                    case 21: {
                            if (u !== 2)
                                break;
                            message.scansSidecar = reader.bytes();
                            message._scansSidecar = "scansSidecar";
                            continue;
                        }
                    case 22: {
                            if (u === 2) {
                                if (!(message.scanLengths && message.scanLengths.length))
                                    message.scanLengths = [];
                                var end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.scanLengths.push(reader.uint32());
                                continue;
                            }
                            if (u !== 0)
                                break;
                            if (!(message.scanLengths && message.scanLengths.length))
                                message.scanLengths = [];
                            message.scanLengths.push(reader.uint32());
                            continue;
                        }
                    case 23: {
                            if (u !== 2)
                                break;
                            message.midQualityFileSha256 = reader.bytes();
                            message._midQualityFileSha256 = "midQualityFileSha256";
                            continue;
                        }
                    case 24: {
                            if (u !== 2)
                                break;
                            message.midQualityFileEncSha256 = reader.bytes();
                            message._midQualityFileEncSha256 = "midQualityFileEncSha256";
                            continue;
                        }
                    case 25: {
                            if (u !== 0)
                                break;
                            message.viewOnce = reader.bool();
                            message._viewOnce = "viewOnce";
                            continue;
                        }
                    case 26: {
                            if (u !== 2)
                                break;
                            message.thumbnailDirectPath = reader.stringVerify();
                            message._thumbnailDirectPath = "thumbnailDirectPath";
                            continue;
                        }
                    case 27: {
                            if (u !== 2)
                                break;
                            message.thumbnailSha256 = reader.bytes();
                            message._thumbnailSha256 = "thumbnailSha256";
                            continue;
                        }
                    case 28: {
                            if (u !== 2)
                                break;
                            message.thumbnailEncSha256 = reader.bytes();
                            message._thumbnailEncSha256 = "thumbnailEncSha256";
                            continue;
                        }
                    case 29: {
                            if (u !== 2)
                                break;
                            message.staticUrl = reader.stringVerify();
                            message._staticUrl = "staticUrl";
                            continue;
                        }
                    case 30: {
                            if (u !== 2)
                                break;
                            if (!(message.annotations && message.annotations.length))
                                message.annotations = [];
                            message.annotations.push($root.proto.InteractiveAnnotation.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 31: {
                            if (u !== 0)
                                break;
                            message.imageSourceType = reader.int32();
                            message._imageSourceType = "imageSourceType";
                            continue;
                        }
                    case 32: {
                            if (u !== 2)
                                break;
                            message.accessibilityLabel = reader.stringVerify();
                            message._accessibilityLabel = "accessibilityLabel";
                            continue;
                        }
                    case 34: {
                            if (u !== 2)
                                break;
                            message.qrUrl = reader.stringVerify();
                            message._qrUrl = "qrUrl";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an ImageMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.ImageMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.ImageMessage} ImageMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ImageMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an ImageMessage message.
             * @function verify
             * @memberof proto.Message.ImageMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ImageMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.url != null && Object.hasOwnProperty.call(message, "url")) {
                    properties._url = 1;
                    if (!$util.isString(message.url))
                        return "url: string expected";
                }
                if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype")) {
                    properties._mimetype = 1;
                    if (!$util.isString(message.mimetype))
                        return "mimetype: string expected";
                }
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption")) {
                    properties._caption = 1;
                    if (!$util.isString(message.caption))
                        return "caption: string expected";
                }
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256")) {
                    properties._fileSha256 = 1;
                    if (!(message.fileSha256 && typeof message.fileSha256.length === "number" || $util.isString(message.fileSha256)))
                        return "fileSha256: buffer expected";
                }
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength")) {
                    properties._fileLength = 1;
                    if (!$util.isInteger(message.fileLength) && !(message.fileLength && $util.isInteger(message.fileLength.low) && $util.isInteger(message.fileLength.high)))
                        return "fileLength: integer|Long expected";
                }
                if (message.height != null && Object.hasOwnProperty.call(message, "height")) {
                    properties._height = 1;
                    if (!$util.isInteger(message.height))
                        return "height: integer expected";
                }
                if (message.width != null && Object.hasOwnProperty.call(message, "width")) {
                    properties._width = 1;
                    if (!$util.isInteger(message.width))
                        return "width: integer expected";
                }
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey")) {
                    properties._mediaKey = 1;
                    if (!(message.mediaKey && typeof message.mediaKey.length === "number" || $util.isString(message.mediaKey)))
                        return "mediaKey: buffer expected";
                }
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256")) {
                    properties._fileEncSha256 = 1;
                    if (!(message.fileEncSha256 && typeof message.fileEncSha256.length === "number" || $util.isString(message.fileEncSha256)))
                        return "fileEncSha256: buffer expected";
                }
                if (message.interactiveAnnotations != null && Object.hasOwnProperty.call(message, "interactiveAnnotations")) {
                    if (!Array.isArray(message.interactiveAnnotations))
                        return "interactiveAnnotations: array expected";
                    for (var i = 0; i < message.interactiveAnnotations.length; ++i) {
                        var error = $root.proto.InteractiveAnnotation.verify(message.interactiveAnnotations[i], q + 1);
                        if (error)
                            return "interactiveAnnotations." + error;
                    }
                }
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath")) {
                    properties._directPath = 1;
                    if (!$util.isString(message.directPath))
                        return "directPath: string expected";
                }
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp")) {
                    properties._mediaKeyTimestamp = 1;
                    if (!$util.isInteger(message.mediaKeyTimestamp) && !(message.mediaKeyTimestamp && $util.isInteger(message.mediaKeyTimestamp.low) && $util.isInteger(message.mediaKeyTimestamp.high)))
                        return "mediaKeyTimestamp: integer|Long expected";
                }
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail")) {
                    properties._jpegThumbnail = 1;
                    if (!(message.jpegThumbnail && typeof message.jpegThumbnail.length === "number" || $util.isString(message.jpegThumbnail)))
                        return "jpegThumbnail: buffer expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.firstScanSidecar != null && Object.hasOwnProperty.call(message, "firstScanSidecar")) {
                    properties._firstScanSidecar = 1;
                    if (!(message.firstScanSidecar && typeof message.firstScanSidecar.length === "number" || $util.isString(message.firstScanSidecar)))
                        return "firstScanSidecar: buffer expected";
                }
                if (message.firstScanLength != null && Object.hasOwnProperty.call(message, "firstScanLength")) {
                    properties._firstScanLength = 1;
                    if (!$util.isInteger(message.firstScanLength))
                        return "firstScanLength: integer expected";
                }
                if (message.experimentGroupId != null && Object.hasOwnProperty.call(message, "experimentGroupId")) {
                    properties._experimentGroupId = 1;
                    if (!$util.isInteger(message.experimentGroupId))
                        return "experimentGroupId: integer expected";
                }
                if (message.scansSidecar != null && Object.hasOwnProperty.call(message, "scansSidecar")) {
                    properties._scansSidecar = 1;
                    if (!(message.scansSidecar && typeof message.scansSidecar.length === "number" || $util.isString(message.scansSidecar)))
                        return "scansSidecar: buffer expected";
                }
                if (message.scanLengths != null && Object.hasOwnProperty.call(message, "scanLengths")) {
                    if (!Array.isArray(message.scanLengths))
                        return "scanLengths: array expected";
                    for (var i = 0; i < message.scanLengths.length; ++i)
                        if (!$util.isInteger(message.scanLengths[i]))
                            return "scanLengths: integer[] expected";
                }
                if (message.midQualityFileSha256 != null && Object.hasOwnProperty.call(message, "midQualityFileSha256")) {
                    properties._midQualityFileSha256 = 1;
                    if (!(message.midQualityFileSha256 && typeof message.midQualityFileSha256.length === "number" || $util.isString(message.midQualityFileSha256)))
                        return "midQualityFileSha256: buffer expected";
                }
                if (message.midQualityFileEncSha256 != null && Object.hasOwnProperty.call(message, "midQualityFileEncSha256")) {
                    properties._midQualityFileEncSha256 = 1;
                    if (!(message.midQualityFileEncSha256 && typeof message.midQualityFileEncSha256.length === "number" || $util.isString(message.midQualityFileEncSha256)))
                        return "midQualityFileEncSha256: buffer expected";
                }
                if (message.viewOnce != null && Object.hasOwnProperty.call(message, "viewOnce")) {
                    properties._viewOnce = 1;
                    if (typeof message.viewOnce !== "boolean")
                        return "viewOnce: boolean expected";
                }
                if (message.thumbnailDirectPath != null && Object.hasOwnProperty.call(message, "thumbnailDirectPath")) {
                    properties._thumbnailDirectPath = 1;
                    if (!$util.isString(message.thumbnailDirectPath))
                        return "thumbnailDirectPath: string expected";
                }
                if (message.thumbnailSha256 != null && Object.hasOwnProperty.call(message, "thumbnailSha256")) {
                    properties._thumbnailSha256 = 1;
                    if (!(message.thumbnailSha256 && typeof message.thumbnailSha256.length === "number" || $util.isString(message.thumbnailSha256)))
                        return "thumbnailSha256: buffer expected";
                }
                if (message.thumbnailEncSha256 != null && Object.hasOwnProperty.call(message, "thumbnailEncSha256")) {
                    properties._thumbnailEncSha256 = 1;
                    if (!(message.thumbnailEncSha256 && typeof message.thumbnailEncSha256.length === "number" || $util.isString(message.thumbnailEncSha256)))
                        return "thumbnailEncSha256: buffer expected";
                }
                if (message.staticUrl != null && Object.hasOwnProperty.call(message, "staticUrl")) {
                    properties._staticUrl = 1;
                    if (!$util.isString(message.staticUrl))
                        return "staticUrl: string expected";
                }
                if (message.annotations != null && Object.hasOwnProperty.call(message, "annotations")) {
                    if (!Array.isArray(message.annotations))
                        return "annotations: array expected";
                    for (var i = 0; i < message.annotations.length; ++i) {
                        var error = $root.proto.InteractiveAnnotation.verify(message.annotations[i], q + 1);
                        if (error)
                            return "annotations." + error;
                    }
                }
                if (message.imageSourceType != null && Object.hasOwnProperty.call(message, "imageSourceType")) {
                    properties._imageSourceType = 1;
                    if (typeof message.imageSourceType !== "number" || (message.imageSourceType | 0) !== message.imageSourceType)
                        return "imageSourceType: enum value expected";
                }
                if (message.accessibilityLabel != null && Object.hasOwnProperty.call(message, "accessibilityLabel")) {
                    properties._accessibilityLabel = 1;
                    if (!$util.isString(message.accessibilityLabel))
                        return "accessibilityLabel: string expected";
                }
                if (message.qrUrl != null && Object.hasOwnProperty.call(message, "qrUrl")) {
                    properties._qrUrl = 1;
                    if (!$util.isString(message.qrUrl))
                        return "qrUrl: string expected";
                }
                return null;
            };

            /**
             * Creates an ImageMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.ImageMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.ImageMessage} ImageMessage
             */
            ImageMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.ImageMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.url != null)
                    message.url = String(object.url);
                if (object.mimetype != null)
                    message.mimetype = String(object.mimetype);
                if (object.caption != null)
                    message.caption = String(object.caption);
                if (object.fileSha256 != null)
                    if (typeof object.fileSha256 === "string")
                        $util.base64.decode(object.fileSha256, message.fileSha256 = $util.newBuffer($util.base64.length(object.fileSha256)), 0);
                    else if (object.fileSha256.length >= 0)
                        message.fileSha256 = object.fileSha256;
                if (object.fileLength != null)
                    if ($util.Long)
                        message.fileLength = $util.Long.fromValue(object.fileLength, true);
                    else if (typeof object.fileLength === "string")
                        message.fileLength = parseInt(object.fileLength, 10);
                    else if (typeof object.fileLength === "number")
                        message.fileLength = object.fileLength;
                    else if (typeof object.fileLength === "object")
                        message.fileLength = new $util.LongBits(object.fileLength.low >>> 0, object.fileLength.high >>> 0).toNumber(true);
                if (object.height != null)
                    message.height = object.height >>> 0;
                if (object.width != null)
                    message.width = object.width >>> 0;
                if (object.mediaKey != null)
                    if (typeof object.mediaKey === "string")
                        $util.base64.decode(object.mediaKey, message.mediaKey = $util.newBuffer($util.base64.length(object.mediaKey)), 0);
                    else if (object.mediaKey.length >= 0)
                        message.mediaKey = object.mediaKey;
                if (object.fileEncSha256 != null)
                    if (typeof object.fileEncSha256 === "string")
                        $util.base64.decode(object.fileEncSha256, message.fileEncSha256 = $util.newBuffer($util.base64.length(object.fileEncSha256)), 0);
                    else if (object.fileEncSha256.length >= 0)
                        message.fileEncSha256 = object.fileEncSha256;
                if (object.interactiveAnnotations) {
                    if (!Array.isArray(object.interactiveAnnotations))
                        throw TypeError(".proto.Message.ImageMessage.interactiveAnnotations: array expected");
                    message.interactiveAnnotations = Array(object.interactiveAnnotations.length);
                    for (var i = 0; i < object.interactiveAnnotations.length; ++i) {
                        if (!$util.isObject(object.interactiveAnnotations[i]))
                            throw TypeError(".proto.Message.ImageMessage.interactiveAnnotations: object expected");
                        message.interactiveAnnotations[i] = $root.proto.InteractiveAnnotation.fromObject(object.interactiveAnnotations[i], q + 1);
                    }
                }
                if (object.directPath != null)
                    message.directPath = String(object.directPath);
                if (object.mediaKeyTimestamp != null)
                    if ($util.Long)
                        message.mediaKeyTimestamp = $util.Long.fromValue(object.mediaKeyTimestamp, false);
                    else if (typeof object.mediaKeyTimestamp === "string")
                        message.mediaKeyTimestamp = parseInt(object.mediaKeyTimestamp, 10);
                    else if (typeof object.mediaKeyTimestamp === "number")
                        message.mediaKeyTimestamp = object.mediaKeyTimestamp;
                    else if (typeof object.mediaKeyTimestamp === "object")
                        message.mediaKeyTimestamp = new $util.LongBits(object.mediaKeyTimestamp.low >>> 0, object.mediaKeyTimestamp.high >>> 0).toNumber();
                if (object.jpegThumbnail != null)
                    if (typeof object.jpegThumbnail === "string")
                        $util.base64.decode(object.jpegThumbnail, message.jpegThumbnail = $util.newBuffer($util.base64.length(object.jpegThumbnail)), 0);
                    else if (object.jpegThumbnail.length >= 0)
                        message.jpegThumbnail = object.jpegThumbnail;
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.ImageMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                if (object.firstScanSidecar != null)
                    if (typeof object.firstScanSidecar === "string")
                        $util.base64.decode(object.firstScanSidecar, message.firstScanSidecar = $util.newBuffer($util.base64.length(object.firstScanSidecar)), 0);
                    else if (object.firstScanSidecar.length >= 0)
                        message.firstScanSidecar = object.firstScanSidecar;
                if (object.firstScanLength != null)
                    message.firstScanLength = object.firstScanLength >>> 0;
                if (object.experimentGroupId != null)
                    message.experimentGroupId = object.experimentGroupId >>> 0;
                if (object.scansSidecar != null)
                    if (typeof object.scansSidecar === "string")
                        $util.base64.decode(object.scansSidecar, message.scansSidecar = $util.newBuffer($util.base64.length(object.scansSidecar)), 0);
                    else if (object.scansSidecar.length >= 0)
                        message.scansSidecar = object.scansSidecar;
                if (object.scanLengths) {
                    if (!Array.isArray(object.scanLengths))
                        throw TypeError(".proto.Message.ImageMessage.scanLengths: array expected");
                    message.scanLengths = Array(object.scanLengths.length);
                    for (var i = 0; i < object.scanLengths.length; ++i)
                        message.scanLengths[i] = object.scanLengths[i] >>> 0;
                }
                if (object.midQualityFileSha256 != null)
                    if (typeof object.midQualityFileSha256 === "string")
                        $util.base64.decode(object.midQualityFileSha256, message.midQualityFileSha256 = $util.newBuffer($util.base64.length(object.midQualityFileSha256)), 0);
                    else if (object.midQualityFileSha256.length >= 0)
                        message.midQualityFileSha256 = object.midQualityFileSha256;
                if (object.midQualityFileEncSha256 != null)
                    if (typeof object.midQualityFileEncSha256 === "string")
                        $util.base64.decode(object.midQualityFileEncSha256, message.midQualityFileEncSha256 = $util.newBuffer($util.base64.length(object.midQualityFileEncSha256)), 0);
                    else if (object.midQualityFileEncSha256.length >= 0)
                        message.midQualityFileEncSha256 = object.midQualityFileEncSha256;
                if (object.viewOnce != null)
                    message.viewOnce = Boolean(object.viewOnce);
                if (object.thumbnailDirectPath != null)
                    message.thumbnailDirectPath = String(object.thumbnailDirectPath);
                if (object.thumbnailSha256 != null)
                    if (typeof object.thumbnailSha256 === "string")
                        $util.base64.decode(object.thumbnailSha256, message.thumbnailSha256 = $util.newBuffer($util.base64.length(object.thumbnailSha256)), 0);
                    else if (object.thumbnailSha256.length >= 0)
                        message.thumbnailSha256 = object.thumbnailSha256;
                if (object.thumbnailEncSha256 != null)
                    if (typeof object.thumbnailEncSha256 === "string")
                        $util.base64.decode(object.thumbnailEncSha256, message.thumbnailEncSha256 = $util.newBuffer($util.base64.length(object.thumbnailEncSha256)), 0);
                    else if (object.thumbnailEncSha256.length >= 0)
                        message.thumbnailEncSha256 = object.thumbnailEncSha256;
                if (object.staticUrl != null)
                    message.staticUrl = String(object.staticUrl);
                if (object.annotations) {
                    if (!Array.isArray(object.annotations))
                        throw TypeError(".proto.Message.ImageMessage.annotations: array expected");
                    message.annotations = Array(object.annotations.length);
                    for (var i = 0; i < object.annotations.length; ++i) {
                        if (!$util.isObject(object.annotations[i]))
                            throw TypeError(".proto.Message.ImageMessage.annotations: object expected");
                        message.annotations[i] = $root.proto.InteractiveAnnotation.fromObject(object.annotations[i], q + 1);
                    }
                }
                switch (object.imageSourceType) {
                case "USER_IMAGE":
                case 0:
                    message.imageSourceType = 0;
                    break;
                case "AI_GENERATED":
                case 1:
                    message.imageSourceType = 1;
                    break;
                case "AI_MODIFIED":
                case 2:
                    message.imageSourceType = 2;
                    break;
                case "RASTERIZED_TEXT_STATUS":
                case 3:
                    message.imageSourceType = 3;
                    break;
                default:
                    if (typeof object.imageSourceType === "number" && (object.imageSourceType | 0) === object.imageSourceType)
                        message.imageSourceType = object.imageSourceType;
                }
                if (object.accessibilityLabel != null)
                    message.accessibilityLabel = String(object.accessibilityLabel);
                if (object.qrUrl != null)
                    message.qrUrl = String(object.qrUrl);
                return message;
            };

            /**
             * Creates a plain object from an ImageMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.ImageMessage
             * @static
             * @param {proto.Message.ImageMessage} message ImageMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ImageMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.interactiveAnnotations = [];
                    object.scanLengths = [];
                    object.annotations = [];
                }
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    object.url = message.url;
                if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                    object.mimetype = message.mimetype;
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    object.caption = message.caption;
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                    object.fileSha256 = options.bytes === String ? $util.base64.encode(message.fileSha256, 0, message.fileSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileSha256) : message.fileSha256;
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.fileLength = typeof message.fileLength === "number" ? BigInt(message.fileLength) : $util.Long.fromBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0, true).toBigInt();
                    else if (typeof message.fileLength === "number")
                        object.fileLength = options.longs === String ? String(message.fileLength) : message.fileLength;
                    else
                        object.fileLength = options.longs === String ? $util.Long.prototype.toString.call(message.fileLength) : options.longs === Number ? new $util.LongBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0).toNumber(true) : message.fileLength;
                if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                    object.height = message.height;
                if (message.width != null && Object.hasOwnProperty.call(message, "width"))
                    object.width = message.width;
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    object.mediaKey = options.bytes === String ? $util.base64.encode(message.mediaKey, 0, message.mediaKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.mediaKey) : message.mediaKey;
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    object.fileEncSha256 = options.bytes === String ? $util.base64.encode(message.fileEncSha256, 0, message.fileEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileEncSha256) : message.fileEncSha256;
                if (message.interactiveAnnotations && message.interactiveAnnotations.length) {
                    object.interactiveAnnotations = Array(message.interactiveAnnotations.length);
                    for (var j = 0; j < message.interactiveAnnotations.length; ++j)
                        object.interactiveAnnotations[j] = $root.proto.InteractiveAnnotation.toObject(message.interactiveAnnotations[j], options, q + 1);
                }
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    object.directPath = message.directPath;
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.mediaKeyTimestamp = typeof message.mediaKeyTimestamp === "number" ? BigInt(message.mediaKeyTimestamp) : $util.Long.fromBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.mediaKeyTimestamp === "number")
                        object.mediaKeyTimestamp = options.longs === String ? String(message.mediaKeyTimestamp) : message.mediaKeyTimestamp;
                    else
                        object.mediaKeyTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.mediaKeyTimestamp) : options.longs === Number ? new $util.LongBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0).toNumber() : message.mediaKeyTimestamp;
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    object.jpegThumbnail = options.bytes === String ? $util.base64.encode(message.jpegThumbnail, 0, message.jpegThumbnail.length) : options.bytes === Array ? Array.prototype.slice.call(message.jpegThumbnail) : message.jpegThumbnail;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.firstScanSidecar != null && Object.hasOwnProperty.call(message, "firstScanSidecar"))
                    object.firstScanSidecar = options.bytes === String ? $util.base64.encode(message.firstScanSidecar, 0, message.firstScanSidecar.length) : options.bytes === Array ? Array.prototype.slice.call(message.firstScanSidecar) : message.firstScanSidecar;
                if (message.firstScanLength != null && Object.hasOwnProperty.call(message, "firstScanLength"))
                    object.firstScanLength = message.firstScanLength;
                if (message.experimentGroupId != null && Object.hasOwnProperty.call(message, "experimentGroupId"))
                    object.experimentGroupId = message.experimentGroupId;
                if (message.scansSidecar != null && Object.hasOwnProperty.call(message, "scansSidecar"))
                    object.scansSidecar = options.bytes === String ? $util.base64.encode(message.scansSidecar, 0, message.scansSidecar.length) : options.bytes === Array ? Array.prototype.slice.call(message.scansSidecar) : message.scansSidecar;
                if (message.scanLengths && message.scanLengths.length) {
                    object.scanLengths = Array(message.scanLengths.length);
                    for (var j = 0; j < message.scanLengths.length; ++j)
                        object.scanLengths[j] = message.scanLengths[j];
                }
                if (message.midQualityFileSha256 != null && Object.hasOwnProperty.call(message, "midQualityFileSha256"))
                    object.midQualityFileSha256 = options.bytes === String ? $util.base64.encode(message.midQualityFileSha256, 0, message.midQualityFileSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.midQualityFileSha256) : message.midQualityFileSha256;
                if (message.midQualityFileEncSha256 != null && Object.hasOwnProperty.call(message, "midQualityFileEncSha256"))
                    object.midQualityFileEncSha256 = options.bytes === String ? $util.base64.encode(message.midQualityFileEncSha256, 0, message.midQualityFileEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.midQualityFileEncSha256) : message.midQualityFileEncSha256;
                if (message.viewOnce != null && Object.hasOwnProperty.call(message, "viewOnce"))
                    object.viewOnce = message.viewOnce;
                if (message.thumbnailDirectPath != null && Object.hasOwnProperty.call(message, "thumbnailDirectPath"))
                    object.thumbnailDirectPath = message.thumbnailDirectPath;
                if (message.thumbnailSha256 != null && Object.hasOwnProperty.call(message, "thumbnailSha256"))
                    object.thumbnailSha256 = options.bytes === String ? $util.base64.encode(message.thumbnailSha256, 0, message.thumbnailSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.thumbnailSha256) : message.thumbnailSha256;
                if (message.thumbnailEncSha256 != null && Object.hasOwnProperty.call(message, "thumbnailEncSha256"))
                    object.thumbnailEncSha256 = options.bytes === String ? $util.base64.encode(message.thumbnailEncSha256, 0, message.thumbnailEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.thumbnailEncSha256) : message.thumbnailEncSha256;
                if (message.staticUrl != null && Object.hasOwnProperty.call(message, "staticUrl"))
                    object.staticUrl = message.staticUrl;
                if (message.annotations && message.annotations.length) {
                    object.annotations = Array(message.annotations.length);
                    for (var j = 0; j < message.annotations.length; ++j)
                        object.annotations[j] = $root.proto.InteractiveAnnotation.toObject(message.annotations[j], options, q + 1);
                }
                if (message.imageSourceType != null && Object.hasOwnProperty.call(message, "imageSourceType"))
                    object.imageSourceType = options.enums === String ? $root.proto.Message.ImageMessage.ImageSourceType[message.imageSourceType] === undefined ? message.imageSourceType : $root.proto.Message.ImageMessage.ImageSourceType[message.imageSourceType] : message.imageSourceType;
                if (message.accessibilityLabel != null && Object.hasOwnProperty.call(message, "accessibilityLabel"))
                    object.accessibilityLabel = message.accessibilityLabel;
                if (message.qrUrl != null && Object.hasOwnProperty.call(message, "qrUrl"))
                    object.qrUrl = message.qrUrl;
                return object;
            };

            /**
             * Converts this ImageMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.ImageMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ImageMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ImageMessage
             * @function getTypeUrl
             * @memberof proto.Message.ImageMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ImageMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.ImageMessage";
            };

            /**
             * ImageSourceType enum.
             * @name proto.Message.ImageMessage.ImageSourceType
             * @enum {number}
             * @property {number} USER_IMAGE=0 USER_IMAGE value
             * @property {number} AI_GENERATED=1 AI_GENERATED value
             * @property {number} AI_MODIFIED=2 AI_MODIFIED value
             * @property {number} RASTERIZED_TEXT_STATUS=3 RASTERIZED_TEXT_STATUS value
             */
            ImageMessage.ImageSourceType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "USER_IMAGE"] = 0;
                values[valuesById[1] = "AI_GENERATED"] = 1;
                values[valuesById[2] = "AI_MODIFIED"] = 2;
                values[valuesById[3] = "RASTERIZED_TEXT_STATUS"] = 3;
                return values;
            })();

            return ImageMessage;
        })();

        Message.InitialSecurityNotificationSettingSync = (function() {

            /**
             * Properties of an InitialSecurityNotificationSettingSync.
             * @memberof proto.Message
             * @interface IInitialSecurityNotificationSettingSync
             * @property {boolean|null} [securityNotificationEnabled] InitialSecurityNotificationSettingSync securityNotificationEnabled
             */

            /**
             * Constructs a new InitialSecurityNotificationSettingSync.
             * @memberof proto.Message
             * @classdesc Represents an InitialSecurityNotificationSettingSync.
             * @implements IInitialSecurityNotificationSettingSync
             * @constructor
             * @param {proto.Message.IInitialSecurityNotificationSettingSync=} [properties] Properties to set
             */
            function InitialSecurityNotificationSettingSync(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * InitialSecurityNotificationSettingSync securityNotificationEnabled.
             * @member {boolean|null|undefined} securityNotificationEnabled
             * @memberof proto.Message.InitialSecurityNotificationSettingSync
             * @instance
             */
            InitialSecurityNotificationSettingSync.prototype.securityNotificationEnabled = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InitialSecurityNotificationSettingSync.prototype, "_securityNotificationEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["securityNotificationEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new InitialSecurityNotificationSettingSync instance using the specified properties.
             * @function create
             * @memberof proto.Message.InitialSecurityNotificationSettingSync
             * @static
             * @param {proto.Message.IInitialSecurityNotificationSettingSync=} [properties] Properties to set
             * @returns {proto.Message.InitialSecurityNotificationSettingSync} InitialSecurityNotificationSettingSync instance
             */
            InitialSecurityNotificationSettingSync.create = function create(properties) {
                return new InitialSecurityNotificationSettingSync(properties);
            };

            /**
             * Encodes the specified InitialSecurityNotificationSettingSync message. Does not implicitly {@link proto.Message.InitialSecurityNotificationSettingSync.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.InitialSecurityNotificationSettingSync
             * @static
             * @param {proto.Message.IInitialSecurityNotificationSettingSync} message InitialSecurityNotificationSettingSync message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InitialSecurityNotificationSettingSync.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.securityNotificationEnabled != null && Object.hasOwnProperty.call(message, "securityNotificationEnabled"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.securityNotificationEnabled);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified InitialSecurityNotificationSettingSync message, length delimited. Does not implicitly {@link proto.Message.InitialSecurityNotificationSettingSync.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.InitialSecurityNotificationSettingSync
             * @static
             * @param {proto.Message.IInitialSecurityNotificationSettingSync} message InitialSecurityNotificationSettingSync message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InitialSecurityNotificationSettingSync.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an InitialSecurityNotificationSettingSync message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.InitialSecurityNotificationSettingSync
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.InitialSecurityNotificationSettingSync} InitialSecurityNotificationSettingSync
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InitialSecurityNotificationSettingSync.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.securityNotificationEnabled = reader.bool();
                            message._securityNotificationEnabled = "securityNotificationEnabled";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an InitialSecurityNotificationSettingSync message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.InitialSecurityNotificationSettingSync
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.InitialSecurityNotificationSettingSync} InitialSecurityNotificationSettingSync
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InitialSecurityNotificationSettingSync.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an InitialSecurityNotificationSettingSync message.
             * @function verify
             * @memberof proto.Message.InitialSecurityNotificationSettingSync
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            InitialSecurityNotificationSettingSync.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.securityNotificationEnabled != null && Object.hasOwnProperty.call(message, "securityNotificationEnabled")) {
                    properties._securityNotificationEnabled = 1;
                    if (typeof message.securityNotificationEnabled !== "boolean")
                        return "securityNotificationEnabled: boolean expected";
                }
                return null;
            };

            /**
             * Creates an InitialSecurityNotificationSettingSync message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.InitialSecurityNotificationSettingSync
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.InitialSecurityNotificationSettingSync} InitialSecurityNotificationSettingSync
             */
            InitialSecurityNotificationSettingSync.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.InitialSecurityNotificationSettingSync: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.securityNotificationEnabled != null)
                    message.securityNotificationEnabled = Boolean(object.securityNotificationEnabled);
                return message;
            };

            /**
             * Creates a plain object from an InitialSecurityNotificationSettingSync message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.InitialSecurityNotificationSettingSync
             * @static
             * @param {proto.Message.InitialSecurityNotificationSettingSync} message InitialSecurityNotificationSettingSync
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            InitialSecurityNotificationSettingSync.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.securityNotificationEnabled != null && Object.hasOwnProperty.call(message, "securityNotificationEnabled"))
                    object.securityNotificationEnabled = message.securityNotificationEnabled;
                return object;
            };

            /**
             * Converts this InitialSecurityNotificationSettingSync to JSON.
             * @function toJSON
             * @memberof proto.Message.InitialSecurityNotificationSettingSync
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            InitialSecurityNotificationSettingSync.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for InitialSecurityNotificationSettingSync
             * @function getTypeUrl
             * @memberof proto.Message.InitialSecurityNotificationSettingSync
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            InitialSecurityNotificationSettingSync.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.InitialSecurityNotificationSettingSync";
            };

            return InitialSecurityNotificationSettingSync;
        })();

        /**
         * InsightDeliveryState enum.
         * @name proto.Message.InsightDeliveryState
         * @enum {number}
         * @property {number} SENT=0 SENT value
         * @property {number} DELIVERED=1 DELIVERED value
         * @property {number} READ=2 READ value
         * @property {number} REPLIED=3 REPLIED value
         * @property {number} QUICK_REPLIED=4 QUICK_REPLIED value
         */
        Message.InsightDeliveryState = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "SENT"] = 0;
            values[valuesById[1] = "DELIVERED"] = 1;
            values[valuesById[2] = "READ"] = 2;
            values[valuesById[3] = "REPLIED"] = 3;
            values[valuesById[4] = "QUICK_REPLIED"] = 4;
            return values;
        })();

        Message.InteractiveMessage = (function() {

            /**
             * Properties of an InteractiveMessage.
             * @memberof proto.Message
             * @interface IInteractiveMessage
             * @property {proto.Message.InteractiveMessage.IHeader|null} [header] InteractiveMessage header
             * @property {proto.Message.InteractiveMessage.IBody|null} [body] InteractiveMessage body
             * @property {proto.Message.InteractiveMessage.IFooter|null} [footer] InteractiveMessage footer
             * @property {proto.Message.InteractiveMessage.IBloksWidget|null} [bloksWidget] InteractiveMessage bloksWidget
             * @property {proto.IContextInfo|null} [contextInfo] InteractiveMessage contextInfo
             * @property {proto.IUrlTrackingMap|null} [urlTrackingMap] InteractiveMessage urlTrackingMap
             * @property {proto.Message.InteractiveMessage.IShopMessage|null} [shopStorefrontMessage] InteractiveMessage shopStorefrontMessage
             * @property {proto.Message.InteractiveMessage.ICollectionMessage|null} [collectionMessage] InteractiveMessage collectionMessage
             * @property {proto.Message.InteractiveMessage.INativeFlowMessage|null} [nativeFlowMessage] InteractiveMessage nativeFlowMessage
             * @property {proto.Message.InteractiveMessage.ICarouselMessage|null} [carouselMessage] InteractiveMessage carouselMessage
             */

            /**
             * Constructs a new InteractiveMessage.
             * @memberof proto.Message
             * @classdesc Represents an InteractiveMessage.
             * @implements IInteractiveMessage
             * @constructor
             * @param {proto.Message.IInteractiveMessage=} [properties] Properties to set
             */
            function InteractiveMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * InteractiveMessage header.
             * @member {proto.Message.InteractiveMessage.IHeader|null|undefined} header
             * @memberof proto.Message.InteractiveMessage
             * @instance
             */
            InteractiveMessage.prototype.header = null;

            /**
             * InteractiveMessage body.
             * @member {proto.Message.InteractiveMessage.IBody|null|undefined} body
             * @memberof proto.Message.InteractiveMessage
             * @instance
             */
            InteractiveMessage.prototype.body = null;

            /**
             * InteractiveMessage footer.
             * @member {proto.Message.InteractiveMessage.IFooter|null|undefined} footer
             * @memberof proto.Message.InteractiveMessage
             * @instance
             */
            InteractiveMessage.prototype.footer = null;

            /**
             * InteractiveMessage bloksWidget.
             * @member {proto.Message.InteractiveMessage.IBloksWidget|null|undefined} bloksWidget
             * @memberof proto.Message.InteractiveMessage
             * @instance
             */
            InteractiveMessage.prototype.bloksWidget = null;

            /**
             * InteractiveMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.InteractiveMessage
             * @instance
             */
            InteractiveMessage.prototype.contextInfo = null;

            /**
             * InteractiveMessage urlTrackingMap.
             * @member {proto.IUrlTrackingMap|null|undefined} urlTrackingMap
             * @memberof proto.Message.InteractiveMessage
             * @instance
             */
            InteractiveMessage.prototype.urlTrackingMap = null;

            /**
             * InteractiveMessage shopStorefrontMessage.
             * @member {proto.Message.InteractiveMessage.IShopMessage|null|undefined} shopStorefrontMessage
             * @memberof proto.Message.InteractiveMessage
             * @instance
             */
            InteractiveMessage.prototype.shopStorefrontMessage = null;

            /**
             * InteractiveMessage collectionMessage.
             * @member {proto.Message.InteractiveMessage.ICollectionMessage|null|undefined} collectionMessage
             * @memberof proto.Message.InteractiveMessage
             * @instance
             */
            InteractiveMessage.prototype.collectionMessage = null;

            /**
             * InteractiveMessage nativeFlowMessage.
             * @member {proto.Message.InteractiveMessage.INativeFlowMessage|null|undefined} nativeFlowMessage
             * @memberof proto.Message.InteractiveMessage
             * @instance
             */
            InteractiveMessage.prototype.nativeFlowMessage = null;

            /**
             * InteractiveMessage carouselMessage.
             * @member {proto.Message.InteractiveMessage.ICarouselMessage|null|undefined} carouselMessage
             * @memberof proto.Message.InteractiveMessage
             * @instance
             */
            InteractiveMessage.prototype.carouselMessage = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InteractiveMessage.prototype, "_header", {
                get: $util.oneOfGetter($oneOfFields = ["header"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InteractiveMessage.prototype, "_body", {
                get: $util.oneOfGetter($oneOfFields = ["body"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InteractiveMessage.prototype, "_footer", {
                get: $util.oneOfGetter($oneOfFields = ["footer"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InteractiveMessage.prototype, "_bloksWidget", {
                get: $util.oneOfGetter($oneOfFields = ["bloksWidget"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InteractiveMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InteractiveMessage.prototype, "_urlTrackingMap", {
                get: $util.oneOfGetter($oneOfFields = ["urlTrackingMap"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * InteractiveMessage interactiveMessage.
             * @member {"shopStorefrontMessage"|"collectionMessage"|"nativeFlowMessage"|"carouselMessage"|undefined} interactiveMessage
             * @memberof proto.Message.InteractiveMessage
             * @instance
             */
            Object.defineProperty(InteractiveMessage.prototype, "interactiveMessage", {
                get: $util.oneOfGetter($oneOfFields = ["shopStorefrontMessage", "collectionMessage", "nativeFlowMessage", "carouselMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new InteractiveMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.InteractiveMessage
             * @static
             * @param {proto.Message.IInteractiveMessage=} [properties] Properties to set
             * @returns {proto.Message.InteractiveMessage} InteractiveMessage instance
             */
            InteractiveMessage.create = function create(properties) {
                return new InteractiveMessage(properties);
            };

            /**
             * Encodes the specified InteractiveMessage message. Does not implicitly {@link proto.Message.InteractiveMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.InteractiveMessage
             * @static
             * @param {proto.Message.IInteractiveMessage} message InteractiveMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InteractiveMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.header != null && Object.hasOwnProperty.call(message, "header"))
                    $root.proto.Message.InteractiveMessage.Header.encode(message.header, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.body != null && Object.hasOwnProperty.call(message, "body"))
                    $root.proto.Message.InteractiveMessage.Body.encode(message.body, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.footer != null && Object.hasOwnProperty.call(message, "footer"))
                    $root.proto.Message.InteractiveMessage.Footer.encode(message.footer, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.shopStorefrontMessage != null && Object.hasOwnProperty.call(message, "shopStorefrontMessage"))
                    $root.proto.Message.InteractiveMessage.ShopMessage.encode(message.shopStorefrontMessage, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.collectionMessage != null && Object.hasOwnProperty.call(message, "collectionMessage"))
                    $root.proto.Message.InteractiveMessage.CollectionMessage.encode(message.collectionMessage, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                if (message.nativeFlowMessage != null && Object.hasOwnProperty.call(message, "nativeFlowMessage"))
                    $root.proto.Message.InteractiveMessage.NativeFlowMessage.encode(message.nativeFlowMessage, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
                if (message.carouselMessage != null && Object.hasOwnProperty.call(message, "carouselMessage"))
                    $root.proto.Message.InteractiveMessage.CarouselMessage.encode(message.carouselMessage, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
                if (message.bloksWidget != null && Object.hasOwnProperty.call(message, "bloksWidget"))
                    $root.proto.Message.InteractiveMessage.BloksWidget.encode(message.bloksWidget, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 15, wireType 2 =*/122).fork(), q + 1).ldelim();
                if (message.urlTrackingMap != null && Object.hasOwnProperty.call(message, "urlTrackingMap"))
                    $root.proto.UrlTrackingMap.encode(message.urlTrackingMap, writer.uint32(/* id 16, wireType 2 =*/130).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified InteractiveMessage message, length delimited. Does not implicitly {@link proto.Message.InteractiveMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.InteractiveMessage
             * @static
             * @param {proto.Message.IInteractiveMessage} message InteractiveMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InteractiveMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an InteractiveMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.InteractiveMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.InteractiveMessage} InteractiveMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InteractiveMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.header = $root.proto.Message.InteractiveMessage.Header.decode(reader, reader.uint32(), undefined, q + 1, message.header);
                            message._header = "header";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.body = $root.proto.Message.InteractiveMessage.Body.decode(reader, reader.uint32(), undefined, q + 1, message.body);
                            message._body = "body";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.footer = $root.proto.Message.InteractiveMessage.Footer.decode(reader, reader.uint32(), undefined, q + 1, message.footer);
                            message._footer = "footer";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.bloksWidget = $root.proto.Message.InteractiveMessage.BloksWidget.decode(reader, reader.uint32(), undefined, q + 1, message.bloksWidget);
                            message._bloksWidget = "bloksWidget";
                            continue;
                        }
                    case 15: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            message.urlTrackingMap = $root.proto.UrlTrackingMap.decode(reader, reader.uint32(), undefined, q + 1, message.urlTrackingMap);
                            message._urlTrackingMap = "urlTrackingMap";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.shopStorefrontMessage = $root.proto.Message.InteractiveMessage.ShopMessage.decode(reader, reader.uint32(), undefined, q + 1, message.shopStorefrontMessage);
                            message.interactiveMessage = "shopStorefrontMessage";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.collectionMessage = $root.proto.Message.InteractiveMessage.CollectionMessage.decode(reader, reader.uint32(), undefined, q + 1, message.collectionMessage);
                            message.interactiveMessage = "collectionMessage";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.nativeFlowMessage = $root.proto.Message.InteractiveMessage.NativeFlowMessage.decode(reader, reader.uint32(), undefined, q + 1, message.nativeFlowMessage);
                            message.interactiveMessage = "nativeFlowMessage";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.carouselMessage = $root.proto.Message.InteractiveMessage.CarouselMessage.decode(reader, reader.uint32(), undefined, q + 1, message.carouselMessage);
                            message.interactiveMessage = "carouselMessage";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an InteractiveMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.InteractiveMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.InteractiveMessage} InteractiveMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InteractiveMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an InteractiveMessage message.
             * @function verify
             * @memberof proto.Message.InteractiveMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            InteractiveMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.header != null && Object.hasOwnProperty.call(message, "header")) {
                    properties._header = 1;
                    {
                        var error = $root.proto.Message.InteractiveMessage.Header.verify(message.header, q + 1);
                        if (error)
                            return "header." + error;
                    }
                }
                if (message.body != null && Object.hasOwnProperty.call(message, "body")) {
                    properties._body = 1;
                    {
                        var error = $root.proto.Message.InteractiveMessage.Body.verify(message.body, q + 1);
                        if (error)
                            return "body." + error;
                    }
                }
                if (message.footer != null && Object.hasOwnProperty.call(message, "footer")) {
                    properties._footer = 1;
                    {
                        var error = $root.proto.Message.InteractiveMessage.Footer.verify(message.footer, q + 1);
                        if (error)
                            return "footer." + error;
                    }
                }
                if (message.bloksWidget != null && Object.hasOwnProperty.call(message, "bloksWidget")) {
                    properties._bloksWidget = 1;
                    {
                        var error = $root.proto.Message.InteractiveMessage.BloksWidget.verify(message.bloksWidget, q + 1);
                        if (error)
                            return "bloksWidget." + error;
                    }
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.urlTrackingMap != null && Object.hasOwnProperty.call(message, "urlTrackingMap")) {
                    properties._urlTrackingMap = 1;
                    {
                        var error = $root.proto.UrlTrackingMap.verify(message.urlTrackingMap, q + 1);
                        if (error)
                            return "urlTrackingMap." + error;
                    }
                }
                if (message.shopStorefrontMessage != null && Object.hasOwnProperty.call(message, "shopStorefrontMessage")) {
                    properties.interactiveMessage = 1;
                    {
                        var error = $root.proto.Message.InteractiveMessage.ShopMessage.verify(message.shopStorefrontMessage, q + 1);
                        if (error)
                            return "shopStorefrontMessage." + error;
                    }
                }
                if (message.collectionMessage != null && Object.hasOwnProperty.call(message, "collectionMessage")) {
                    if (properties.interactiveMessage === 1)
                        return "interactiveMessage: multiple values";
                    properties.interactiveMessage = 1;
                    {
                        var error = $root.proto.Message.InteractiveMessage.CollectionMessage.verify(message.collectionMessage, q + 1);
                        if (error)
                            return "collectionMessage." + error;
                    }
                }
                if (message.nativeFlowMessage != null && Object.hasOwnProperty.call(message, "nativeFlowMessage")) {
                    if (properties.interactiveMessage === 1)
                        return "interactiveMessage: multiple values";
                    properties.interactiveMessage = 1;
                    {
                        var error = $root.proto.Message.InteractiveMessage.NativeFlowMessage.verify(message.nativeFlowMessage, q + 1);
                        if (error)
                            return "nativeFlowMessage." + error;
                    }
                }
                if (message.carouselMessage != null && Object.hasOwnProperty.call(message, "carouselMessage")) {
                    if (properties.interactiveMessage === 1)
                        return "interactiveMessage: multiple values";
                    properties.interactiveMessage = 1;
                    {
                        var error = $root.proto.Message.InteractiveMessage.CarouselMessage.verify(message.carouselMessage, q + 1);
                        if (error)
                            return "carouselMessage." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an InteractiveMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.InteractiveMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.InteractiveMessage} InteractiveMessage
             */
            InteractiveMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.InteractiveMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.header != null) {
                    if (!$util.isObject(object.header))
                        throw TypeError(".proto.Message.InteractiveMessage.header: object expected");
                    message.header = $root.proto.Message.InteractiveMessage.Header.fromObject(object.header, q + 1);
                }
                if (object.body != null) {
                    if (!$util.isObject(object.body))
                        throw TypeError(".proto.Message.InteractiveMessage.body: object expected");
                    message.body = $root.proto.Message.InteractiveMessage.Body.fromObject(object.body, q + 1);
                }
                if (object.footer != null) {
                    if (!$util.isObject(object.footer))
                        throw TypeError(".proto.Message.InteractiveMessage.footer: object expected");
                    message.footer = $root.proto.Message.InteractiveMessage.Footer.fromObject(object.footer, q + 1);
                }
                if (object.bloksWidget != null) {
                    if (!$util.isObject(object.bloksWidget))
                        throw TypeError(".proto.Message.InteractiveMessage.bloksWidget: object expected");
                    message.bloksWidget = $root.proto.Message.InteractiveMessage.BloksWidget.fromObject(object.bloksWidget, q + 1);
                }
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.InteractiveMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                if (object.urlTrackingMap != null) {
                    if (!$util.isObject(object.urlTrackingMap))
                        throw TypeError(".proto.Message.InteractiveMessage.urlTrackingMap: object expected");
                    message.urlTrackingMap = $root.proto.UrlTrackingMap.fromObject(object.urlTrackingMap, q + 1);
                }
                if (object.shopStorefrontMessage != null) {
                    if (!$util.isObject(object.shopStorefrontMessage))
                        throw TypeError(".proto.Message.InteractiveMessage.shopStorefrontMessage: object expected");
                    message.shopStorefrontMessage = $root.proto.Message.InteractiveMessage.ShopMessage.fromObject(object.shopStorefrontMessage, q + 1);
                }
                if (object.collectionMessage != null) {
                    if (!$util.isObject(object.collectionMessage))
                        throw TypeError(".proto.Message.InteractiveMessage.collectionMessage: object expected");
                    message.collectionMessage = $root.proto.Message.InteractiveMessage.CollectionMessage.fromObject(object.collectionMessage, q + 1);
                }
                if (object.nativeFlowMessage != null) {
                    if (!$util.isObject(object.nativeFlowMessage))
                        throw TypeError(".proto.Message.InteractiveMessage.nativeFlowMessage: object expected");
                    message.nativeFlowMessage = $root.proto.Message.InteractiveMessage.NativeFlowMessage.fromObject(object.nativeFlowMessage, q + 1);
                }
                if (object.carouselMessage != null) {
                    if (!$util.isObject(object.carouselMessage))
                        throw TypeError(".proto.Message.InteractiveMessage.carouselMessage: object expected");
                    message.carouselMessage = $root.proto.Message.InteractiveMessage.CarouselMessage.fromObject(object.carouselMessage, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from an InteractiveMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.InteractiveMessage
             * @static
             * @param {proto.Message.InteractiveMessage} message InteractiveMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            InteractiveMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.header != null && Object.hasOwnProperty.call(message, "header"))
                    object.header = $root.proto.Message.InteractiveMessage.Header.toObject(message.header, options, q + 1);
                if (message.body != null && Object.hasOwnProperty.call(message, "body"))
                    object.body = $root.proto.Message.InteractiveMessage.Body.toObject(message.body, options, q + 1);
                if (message.footer != null && Object.hasOwnProperty.call(message, "footer"))
                    object.footer = $root.proto.Message.InteractiveMessage.Footer.toObject(message.footer, options, q + 1);
                if (message.shopStorefrontMessage != null && Object.hasOwnProperty.call(message, "shopStorefrontMessage")) {
                    object.shopStorefrontMessage = $root.proto.Message.InteractiveMessage.ShopMessage.toObject(message.shopStorefrontMessage, options, q + 1);
                    if (options.oneofs)
                        object.interactiveMessage = "shopStorefrontMessage";
                }
                if (message.collectionMessage != null && Object.hasOwnProperty.call(message, "collectionMessage")) {
                    object.collectionMessage = $root.proto.Message.InteractiveMessage.CollectionMessage.toObject(message.collectionMessage, options, q + 1);
                    if (options.oneofs)
                        object.interactiveMessage = "collectionMessage";
                }
                if (message.nativeFlowMessage != null && Object.hasOwnProperty.call(message, "nativeFlowMessage")) {
                    object.nativeFlowMessage = $root.proto.Message.InteractiveMessage.NativeFlowMessage.toObject(message.nativeFlowMessage, options, q + 1);
                    if (options.oneofs)
                        object.interactiveMessage = "nativeFlowMessage";
                }
                if (message.carouselMessage != null && Object.hasOwnProperty.call(message, "carouselMessage")) {
                    object.carouselMessage = $root.proto.Message.InteractiveMessage.CarouselMessage.toObject(message.carouselMessage, options, q + 1);
                    if (options.oneofs)
                        object.interactiveMessage = "carouselMessage";
                }
                if (message.bloksWidget != null && Object.hasOwnProperty.call(message, "bloksWidget"))
                    object.bloksWidget = $root.proto.Message.InteractiveMessage.BloksWidget.toObject(message.bloksWidget, options, q + 1);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.urlTrackingMap != null && Object.hasOwnProperty.call(message, "urlTrackingMap"))
                    object.urlTrackingMap = $root.proto.UrlTrackingMap.toObject(message.urlTrackingMap, options, q + 1);
                return object;
            };

            /**
             * Converts this InteractiveMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.InteractiveMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            InteractiveMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for InteractiveMessage
             * @function getTypeUrl
             * @memberof proto.Message.InteractiveMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            InteractiveMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.InteractiveMessage";
            };

            InteractiveMessage.BloksWidget = (function() {

                /**
                 * Properties of a BloksWidget.
                 * @memberof proto.Message.InteractiveMessage
                 * @interface IBloksWidget
                 * @property {string|null} [uuid] BloksWidget uuid
                 * @property {string|null} [data] BloksWidget data
                 * @property {string|null} [type] BloksWidget type
                 * @property {string|null} [fallback] BloksWidget fallback
                 */

                /**
                 * Constructs a new BloksWidget.
                 * @memberof proto.Message.InteractiveMessage
                 * @classdesc Represents a BloksWidget.
                 * @implements IBloksWidget
                 * @constructor
                 * @param {proto.Message.InteractiveMessage.IBloksWidget=} [properties] Properties to set
                 */
                function BloksWidget(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * BloksWidget uuid.
                 * @member {string|null|undefined} uuid
                 * @memberof proto.Message.InteractiveMessage.BloksWidget
                 * @instance
                 */
                BloksWidget.prototype.uuid = null;

                /**
                 * BloksWidget data.
                 * @member {string|null|undefined} data
                 * @memberof proto.Message.InteractiveMessage.BloksWidget
                 * @instance
                 */
                BloksWidget.prototype.data = null;

                /**
                 * BloksWidget type.
                 * @member {string|null|undefined} type
                 * @memberof proto.Message.InteractiveMessage.BloksWidget
                 * @instance
                 */
                BloksWidget.prototype.type = null;

                /**
                 * BloksWidget fallback.
                 * @member {string|null|undefined} fallback
                 * @memberof proto.Message.InteractiveMessage.BloksWidget
                 * @instance
                 */
                BloksWidget.prototype.fallback = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BloksWidget.prototype, "_uuid", {
                    get: $util.oneOfGetter($oneOfFields = ["uuid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BloksWidget.prototype, "_data", {
                    get: $util.oneOfGetter($oneOfFields = ["data"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BloksWidget.prototype, "_type", {
                    get: $util.oneOfGetter($oneOfFields = ["type"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BloksWidget.prototype, "_fallback", {
                    get: $util.oneOfGetter($oneOfFields = ["fallback"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new BloksWidget instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.InteractiveMessage.BloksWidget
                 * @static
                 * @param {proto.Message.InteractiveMessage.IBloksWidget=} [properties] Properties to set
                 * @returns {proto.Message.InteractiveMessage.BloksWidget} BloksWidget instance
                 */
                BloksWidget.create = function create(properties) {
                    return new BloksWidget(properties);
                };

                /**
                 * Encodes the specified BloksWidget message. Does not implicitly {@link proto.Message.InteractiveMessage.BloksWidget.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.InteractiveMessage.BloksWidget
                 * @static
                 * @param {proto.Message.InteractiveMessage.IBloksWidget} message BloksWidget message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BloksWidget.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.uuid);
                    if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.data);
                    if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.type);
                    if (message.fallback != null && Object.hasOwnProperty.call(message, "fallback"))
                        writer.uint32(/* id 4, wireType 2 =*/34).string(message.fallback);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified BloksWidget message, length delimited. Does not implicitly {@link proto.Message.InteractiveMessage.BloksWidget.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.InteractiveMessage.BloksWidget
                 * @static
                 * @param {proto.Message.InteractiveMessage.IBloksWidget} message BloksWidget message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BloksWidget.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a BloksWidget message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.InteractiveMessage.BloksWidget
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.InteractiveMessage.BloksWidget} BloksWidget
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BloksWidget.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.uuid = reader.stringVerify();
                                message._uuid = "uuid";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.data = reader.stringVerify();
                                message._data = "data";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.type = reader.stringVerify();
                                message._type = "type";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                message.fallback = reader.stringVerify();
                                message._fallback = "fallback";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a BloksWidget message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.InteractiveMessage.BloksWidget
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.InteractiveMessage.BloksWidget} BloksWidget
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BloksWidget.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a BloksWidget message.
                 * @function verify
                 * @memberof proto.Message.InteractiveMessage.BloksWidget
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                BloksWidget.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid")) {
                        properties._uuid = 1;
                        if (!$util.isString(message.uuid))
                            return "uuid: string expected";
                    }
                    if (message.data != null && Object.hasOwnProperty.call(message, "data")) {
                        properties._data = 1;
                        if (!$util.isString(message.data))
                            return "data: string expected";
                    }
                    if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                        properties._type = 1;
                        if (!$util.isString(message.type))
                            return "type: string expected";
                    }
                    if (message.fallback != null && Object.hasOwnProperty.call(message, "fallback")) {
                        properties._fallback = 1;
                        if (!$util.isString(message.fallback))
                            return "fallback: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a BloksWidget message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.InteractiveMessage.BloksWidget
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.InteractiveMessage.BloksWidget} BloksWidget
                 */
                BloksWidget.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.InteractiveMessage.BloksWidget: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.uuid != null)
                        message.uuid = String(object.uuid);
                    if (object.data != null)
                        message.data = String(object.data);
                    if (object.type != null)
                        message.type = String(object.type);
                    if (object.fallback != null)
                        message.fallback = String(object.fallback);
                    return message;
                };

                /**
                 * Creates a plain object from a BloksWidget message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.InteractiveMessage.BloksWidget
                 * @static
                 * @param {proto.Message.InteractiveMessage.BloksWidget} message BloksWidget
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                BloksWidget.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.uuid != null && Object.hasOwnProperty.call(message, "uuid"))
                        object.uuid = message.uuid;
                    if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                        object.data = message.data;
                    if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                        object.type = message.type;
                    if (message.fallback != null && Object.hasOwnProperty.call(message, "fallback"))
                        object.fallback = message.fallback;
                    return object;
                };

                /**
                 * Converts this BloksWidget to JSON.
                 * @function toJSON
                 * @memberof proto.Message.InteractiveMessage.BloksWidget
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                BloksWidget.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for BloksWidget
                 * @function getTypeUrl
                 * @memberof proto.Message.InteractiveMessage.BloksWidget
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                BloksWidget.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.InteractiveMessage.BloksWidget";
                };

                return BloksWidget;
            })();

            InteractiveMessage.Body = (function() {

                /**
                 * Properties of a Body.
                 * @memberof proto.Message.InteractiveMessage
                 * @interface IBody
                 * @property {string|null} [text] Body text
                 */

                /**
                 * Constructs a new Body.
                 * @memberof proto.Message.InteractiveMessage
                 * @classdesc Represents a Body.
                 * @implements IBody
                 * @constructor
                 * @param {proto.Message.InteractiveMessage.IBody=} [properties] Properties to set
                 */
                function Body(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * Body text.
                 * @member {string|null|undefined} text
                 * @memberof proto.Message.InteractiveMessage.Body
                 * @instance
                 */
                Body.prototype.text = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Body.prototype, "_text", {
                    get: $util.oneOfGetter($oneOfFields = ["text"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new Body instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.InteractiveMessage.Body
                 * @static
                 * @param {proto.Message.InteractiveMessage.IBody=} [properties] Properties to set
                 * @returns {proto.Message.InteractiveMessage.Body} Body instance
                 */
                Body.create = function create(properties) {
                    return new Body(properties);
                };

                /**
                 * Encodes the specified Body message. Does not implicitly {@link proto.Message.InteractiveMessage.Body.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.InteractiveMessage.Body
                 * @static
                 * @param {proto.Message.InteractiveMessage.IBody} message Body message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Body.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.text);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified Body message, length delimited. Does not implicitly {@link proto.Message.InteractiveMessage.Body.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.InteractiveMessage.Body
                 * @static
                 * @param {proto.Message.InteractiveMessage.IBody} message Body message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Body.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a Body message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.InteractiveMessage.Body
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.InteractiveMessage.Body} Body
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Body.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.text = reader.stringVerify();
                                message._text = "text";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a Body message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.InteractiveMessage.Body
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.InteractiveMessage.Body} Body
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Body.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a Body message.
                 * @function verify
                 * @memberof proto.Message.InteractiveMessage.Body
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                Body.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.text != null && Object.hasOwnProperty.call(message, "text")) {
                        properties._text = 1;
                        if (!$util.isString(message.text))
                            return "text: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a Body message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.InteractiveMessage.Body
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.InteractiveMessage.Body} Body
                 */
                Body.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.InteractiveMessage.Body: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.text != null)
                        message.text = String(object.text);
                    return message;
                };

                /**
                 * Creates a plain object from a Body message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.InteractiveMessage.Body
                 * @static
                 * @param {proto.Message.InteractiveMessage.Body} message Body
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                Body.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                        object.text = message.text;
                    return object;
                };

                /**
                 * Converts this Body to JSON.
                 * @function toJSON
                 * @memberof proto.Message.InteractiveMessage.Body
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                Body.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for Body
                 * @function getTypeUrl
                 * @memberof proto.Message.InteractiveMessage.Body
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                Body.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.InteractiveMessage.Body";
                };

                return Body;
            })();

            InteractiveMessage.CarouselMessage = (function() {

                /**
                 * Properties of a CarouselMessage.
                 * @memberof proto.Message.InteractiveMessage
                 * @interface ICarouselMessage
                 * @property {Array.<proto.Message.IInteractiveMessage>|null} [cards] CarouselMessage cards
                 * @property {number|null} [messageVersion] CarouselMessage messageVersion
                 * @property {proto.Message.InteractiveMessage.CarouselMessage.CarouselCardType|null} [carouselCardType] CarouselMessage carouselCardType
                 */

                /**
                 * Constructs a new CarouselMessage.
                 * @memberof proto.Message.InteractiveMessage
                 * @classdesc Represents a CarouselMessage.
                 * @implements ICarouselMessage
                 * @constructor
                 * @param {proto.Message.InteractiveMessage.ICarouselMessage=} [properties] Properties to set
                 */
                function CarouselMessage(properties) {
                    this.cards = [];
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * CarouselMessage cards.
                 * @member {Array.<proto.Message.IInteractiveMessage>} cards
                 * @memberof proto.Message.InteractiveMessage.CarouselMessage
                 * @instance
                 */
                CarouselMessage.prototype.cards = $util.emptyArray;

                /**
                 * CarouselMessage messageVersion.
                 * @member {number|null|undefined} messageVersion
                 * @memberof proto.Message.InteractiveMessage.CarouselMessage
                 * @instance
                 */
                CarouselMessage.prototype.messageVersion = null;

                /**
                 * CarouselMessage carouselCardType.
                 * @member {proto.Message.InteractiveMessage.CarouselMessage.CarouselCardType|null|undefined} carouselCardType
                 * @memberof proto.Message.InteractiveMessage.CarouselMessage
                 * @instance
                 */
                CarouselMessage.prototype.carouselCardType = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CarouselMessage.prototype, "_messageVersion", {
                    get: $util.oneOfGetter($oneOfFields = ["messageVersion"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CarouselMessage.prototype, "_carouselCardType", {
                    get: $util.oneOfGetter($oneOfFields = ["carouselCardType"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new CarouselMessage instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.InteractiveMessage.CarouselMessage
                 * @static
                 * @param {proto.Message.InteractiveMessage.ICarouselMessage=} [properties] Properties to set
                 * @returns {proto.Message.InteractiveMessage.CarouselMessage} CarouselMessage instance
                 */
                CarouselMessage.create = function create(properties) {
                    return new CarouselMessage(properties);
                };

                /**
                 * Encodes the specified CarouselMessage message. Does not implicitly {@link proto.Message.InteractiveMessage.CarouselMessage.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.InteractiveMessage.CarouselMessage
                 * @static
                 * @param {proto.Message.InteractiveMessage.ICarouselMessage} message CarouselMessage message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                CarouselMessage.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.cards != null && message.cards.length)
                        for (var i = 0; i < message.cards.length; ++i)
                            $root.proto.Message.InteractiveMessage.encode(message.cards[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                    if (message.messageVersion != null && Object.hasOwnProperty.call(message, "messageVersion"))
                        writer.uint32(/* id 2, wireType 0 =*/16).int32(message.messageVersion);
                    if (message.carouselCardType != null && Object.hasOwnProperty.call(message, "carouselCardType"))
                        writer.uint32(/* id 3, wireType 0 =*/24).int32(message.carouselCardType);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified CarouselMessage message, length delimited. Does not implicitly {@link proto.Message.InteractiveMessage.CarouselMessage.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.InteractiveMessage.CarouselMessage
                 * @static
                 * @param {proto.Message.InteractiveMessage.ICarouselMessage} message CarouselMessage message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                CarouselMessage.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a CarouselMessage message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.InteractiveMessage.CarouselMessage
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.InteractiveMessage.CarouselMessage} CarouselMessage
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                CarouselMessage.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                if (!(message.cards && message.cards.length))
                                    message.cards = [];
                                message.cards.push($root.proto.Message.InteractiveMessage.decode(reader, reader.uint32(), undefined, q + 1));
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                message.messageVersion = reader.int32();
                                message._messageVersion = "messageVersion";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                message.carouselCardType = reader.int32();
                                message._carouselCardType = "carouselCardType";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a CarouselMessage message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.InteractiveMessage.CarouselMessage
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.InteractiveMessage.CarouselMessage} CarouselMessage
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                CarouselMessage.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a CarouselMessage message.
                 * @function verify
                 * @memberof proto.Message.InteractiveMessage.CarouselMessage
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                CarouselMessage.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.cards != null && Object.hasOwnProperty.call(message, "cards")) {
                        if (!Array.isArray(message.cards))
                            return "cards: array expected";
                        for (var i = 0; i < message.cards.length; ++i) {
                            var error = $root.proto.Message.InteractiveMessage.verify(message.cards[i], q + 1);
                            if (error)
                                return "cards." + error;
                        }
                    }
                    if (message.messageVersion != null && Object.hasOwnProperty.call(message, "messageVersion")) {
                        properties._messageVersion = 1;
                        if (!$util.isInteger(message.messageVersion))
                            return "messageVersion: integer expected";
                    }
                    if (message.carouselCardType != null && Object.hasOwnProperty.call(message, "carouselCardType")) {
                        properties._carouselCardType = 1;
                        if (typeof message.carouselCardType !== "number" || (message.carouselCardType | 0) !== message.carouselCardType)
                            return "carouselCardType: enum value expected";
                    }
                    return null;
                };

                /**
                 * Creates a CarouselMessage message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.InteractiveMessage.CarouselMessage
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.InteractiveMessage.CarouselMessage} CarouselMessage
                 */
                CarouselMessage.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.InteractiveMessage.CarouselMessage: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.cards) {
                        if (!Array.isArray(object.cards))
                            throw TypeError(".proto.Message.InteractiveMessage.CarouselMessage.cards: array expected");
                        message.cards = Array(object.cards.length);
                        for (var i = 0; i < object.cards.length; ++i) {
                            if (!$util.isObject(object.cards[i]))
                                throw TypeError(".proto.Message.InteractiveMessage.CarouselMessage.cards: object expected");
                            message.cards[i] = $root.proto.Message.InteractiveMessage.fromObject(object.cards[i], q + 1);
                        }
                    }
                    if (object.messageVersion != null)
                        message.messageVersion = object.messageVersion | 0;
                    switch (object.carouselCardType) {
                    case "UNKNOWN":
                    case 0:
                        message.carouselCardType = 0;
                        break;
                    case "HSCROLL_CARDS":
                    case 1:
                        message.carouselCardType = 1;
                        break;
                    case "ALBUM_IMAGE":
                    case 2:
                        message.carouselCardType = 2;
                        break;
                    default:
                        if (typeof object.carouselCardType === "number" && (object.carouselCardType | 0) === object.carouselCardType)
                            message.carouselCardType = object.carouselCardType;
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a CarouselMessage message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.InteractiveMessage.CarouselMessage
                 * @static
                 * @param {proto.Message.InteractiveMessage.CarouselMessage} message CarouselMessage
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                CarouselMessage.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (options.arrays || options.defaults)
                        object.cards = [];
                    if (message.cards && message.cards.length) {
                        object.cards = Array(message.cards.length);
                        for (var j = 0; j < message.cards.length; ++j)
                            object.cards[j] = $root.proto.Message.InteractiveMessage.toObject(message.cards[j], options, q + 1);
                    }
                    if (message.messageVersion != null && Object.hasOwnProperty.call(message, "messageVersion"))
                        object.messageVersion = message.messageVersion;
                    if (message.carouselCardType != null && Object.hasOwnProperty.call(message, "carouselCardType"))
                        object.carouselCardType = options.enums === String ? $root.proto.Message.InteractiveMessage.CarouselMessage.CarouselCardType[message.carouselCardType] === undefined ? message.carouselCardType : $root.proto.Message.InteractiveMessage.CarouselMessage.CarouselCardType[message.carouselCardType] : message.carouselCardType;
                    return object;
                };

                /**
                 * Converts this CarouselMessage to JSON.
                 * @function toJSON
                 * @memberof proto.Message.InteractiveMessage.CarouselMessage
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                CarouselMessage.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for CarouselMessage
                 * @function getTypeUrl
                 * @memberof proto.Message.InteractiveMessage.CarouselMessage
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                CarouselMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.InteractiveMessage.CarouselMessage";
                };

                /**
                 * CarouselCardType enum.
                 * @name proto.Message.InteractiveMessage.CarouselMessage.CarouselCardType
                 * @enum {number}
                 * @property {number} UNKNOWN=0 UNKNOWN value
                 * @property {number} HSCROLL_CARDS=1 HSCROLL_CARDS value
                 * @property {number} ALBUM_IMAGE=2 ALBUM_IMAGE value
                 */
                CarouselMessage.CarouselCardType = (function() {
                    var valuesById = {}, values = Object.create(valuesById);
                    values[valuesById[0] = "UNKNOWN"] = 0;
                    values[valuesById[1] = "HSCROLL_CARDS"] = 1;
                    values[valuesById[2] = "ALBUM_IMAGE"] = 2;
                    return values;
                })();

                return CarouselMessage;
            })();

            InteractiveMessage.CollectionMessage = (function() {

                /**
                 * Properties of a CollectionMessage.
                 * @memberof proto.Message.InteractiveMessage
                 * @interface ICollectionMessage
                 * @property {string|null} [bizJid] CollectionMessage bizJid
                 * @property {string|null} [id] CollectionMessage id
                 * @property {number|null} [messageVersion] CollectionMessage messageVersion
                 */

                /**
                 * Constructs a new CollectionMessage.
                 * @memberof proto.Message.InteractiveMessage
                 * @classdesc Represents a CollectionMessage.
                 * @implements ICollectionMessage
                 * @constructor
                 * @param {proto.Message.InteractiveMessage.ICollectionMessage=} [properties] Properties to set
                 */
                function CollectionMessage(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * CollectionMessage bizJid.
                 * @member {string|null|undefined} bizJid
                 * @memberof proto.Message.InteractiveMessage.CollectionMessage
                 * @instance
                 */
                CollectionMessage.prototype.bizJid = null;

                /**
                 * CollectionMessage id.
                 * @member {string|null|undefined} id
                 * @memberof proto.Message.InteractiveMessage.CollectionMessage
                 * @instance
                 */
                CollectionMessage.prototype.id = null;

                /**
                 * CollectionMessage messageVersion.
                 * @member {number|null|undefined} messageVersion
                 * @memberof proto.Message.InteractiveMessage.CollectionMessage
                 * @instance
                 */
                CollectionMessage.prototype.messageVersion = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CollectionMessage.prototype, "_bizJid", {
                    get: $util.oneOfGetter($oneOfFields = ["bizJid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CollectionMessage.prototype, "_id", {
                    get: $util.oneOfGetter($oneOfFields = ["id"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CollectionMessage.prototype, "_messageVersion", {
                    get: $util.oneOfGetter($oneOfFields = ["messageVersion"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new CollectionMessage instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.InteractiveMessage.CollectionMessage
                 * @static
                 * @param {proto.Message.InteractiveMessage.ICollectionMessage=} [properties] Properties to set
                 * @returns {proto.Message.InteractiveMessage.CollectionMessage} CollectionMessage instance
                 */
                CollectionMessage.create = function create(properties) {
                    return new CollectionMessage(properties);
                };

                /**
                 * Encodes the specified CollectionMessage message. Does not implicitly {@link proto.Message.InteractiveMessage.CollectionMessage.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.InteractiveMessage.CollectionMessage
                 * @static
                 * @param {proto.Message.InteractiveMessage.ICollectionMessage} message CollectionMessage message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                CollectionMessage.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.bizJid != null && Object.hasOwnProperty.call(message, "bizJid"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.bizJid);
                    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.id);
                    if (message.messageVersion != null && Object.hasOwnProperty.call(message, "messageVersion"))
                        writer.uint32(/* id 3, wireType 0 =*/24).int32(message.messageVersion);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified CollectionMessage message, length delimited. Does not implicitly {@link proto.Message.InteractiveMessage.CollectionMessage.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.InteractiveMessage.CollectionMessage
                 * @static
                 * @param {proto.Message.InteractiveMessage.ICollectionMessage} message CollectionMessage message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                CollectionMessage.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a CollectionMessage message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.InteractiveMessage.CollectionMessage
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.InteractiveMessage.CollectionMessage} CollectionMessage
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                CollectionMessage.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.bizJid = reader.stringVerify();
                                message._bizJid = "bizJid";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.id = reader.stringVerify();
                                message._id = "id";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                message.messageVersion = reader.int32();
                                message._messageVersion = "messageVersion";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a CollectionMessage message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.InteractiveMessage.CollectionMessage
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.InteractiveMessage.CollectionMessage} CollectionMessage
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                CollectionMessage.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a CollectionMessage message.
                 * @function verify
                 * @memberof proto.Message.InteractiveMessage.CollectionMessage
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                CollectionMessage.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.bizJid != null && Object.hasOwnProperty.call(message, "bizJid")) {
                        properties._bizJid = 1;
                        if (!$util.isString(message.bizJid))
                            return "bizJid: string expected";
                    }
                    if (message.id != null && Object.hasOwnProperty.call(message, "id")) {
                        properties._id = 1;
                        if (!$util.isString(message.id))
                            return "id: string expected";
                    }
                    if (message.messageVersion != null && Object.hasOwnProperty.call(message, "messageVersion")) {
                        properties._messageVersion = 1;
                        if (!$util.isInteger(message.messageVersion))
                            return "messageVersion: integer expected";
                    }
                    return null;
                };

                /**
                 * Creates a CollectionMessage message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.InteractiveMessage.CollectionMessage
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.InteractiveMessage.CollectionMessage} CollectionMessage
                 */
                CollectionMessage.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.InteractiveMessage.CollectionMessage: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.bizJid != null)
                        message.bizJid = String(object.bizJid);
                    if (object.id != null)
                        message.id = String(object.id);
                    if (object.messageVersion != null)
                        message.messageVersion = object.messageVersion | 0;
                    return message;
                };

                /**
                 * Creates a plain object from a CollectionMessage message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.InteractiveMessage.CollectionMessage
                 * @static
                 * @param {proto.Message.InteractiveMessage.CollectionMessage} message CollectionMessage
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                CollectionMessage.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.bizJid != null && Object.hasOwnProperty.call(message, "bizJid"))
                        object.bizJid = message.bizJid;
                    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                        object.id = message.id;
                    if (message.messageVersion != null && Object.hasOwnProperty.call(message, "messageVersion"))
                        object.messageVersion = message.messageVersion;
                    return object;
                };

                /**
                 * Converts this CollectionMessage to JSON.
                 * @function toJSON
                 * @memberof proto.Message.InteractiveMessage.CollectionMessage
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                CollectionMessage.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for CollectionMessage
                 * @function getTypeUrl
                 * @memberof proto.Message.InteractiveMessage.CollectionMessage
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                CollectionMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.InteractiveMessage.CollectionMessage";
                };

                return CollectionMessage;
            })();

            InteractiveMessage.Footer = (function() {

                /**
                 * Properties of a Footer.
                 * @memberof proto.Message.InteractiveMessage
                 * @interface IFooter
                 * @property {string|null} [text] Footer text
                 * @property {boolean|null} [hasMediaAttachment] Footer hasMediaAttachment
                 * @property {proto.Message.IAudioMessage|null} [audioMessage] Footer audioMessage
                 */

                /**
                 * Constructs a new Footer.
                 * @memberof proto.Message.InteractiveMessage
                 * @classdesc Represents a Footer.
                 * @implements IFooter
                 * @constructor
                 * @param {proto.Message.InteractiveMessage.IFooter=} [properties] Properties to set
                 */
                function Footer(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * Footer text.
                 * @member {string|null|undefined} text
                 * @memberof proto.Message.InteractiveMessage.Footer
                 * @instance
                 */
                Footer.prototype.text = null;

                /**
                 * Footer hasMediaAttachment.
                 * @member {boolean|null|undefined} hasMediaAttachment
                 * @memberof proto.Message.InteractiveMessage.Footer
                 * @instance
                 */
                Footer.prototype.hasMediaAttachment = null;

                /**
                 * Footer audioMessage.
                 * @member {proto.Message.IAudioMessage|null|undefined} audioMessage
                 * @memberof proto.Message.InteractiveMessage.Footer
                 * @instance
                 */
                Footer.prototype.audioMessage = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Footer.prototype, "_text", {
                    get: $util.oneOfGetter($oneOfFields = ["text"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Footer.prototype, "_hasMediaAttachment", {
                    get: $util.oneOfGetter($oneOfFields = ["hasMediaAttachment"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Footer media.
                 * @member {"audioMessage"|undefined} media
                 * @memberof proto.Message.InteractiveMessage.Footer
                 * @instance
                 */
                Object.defineProperty(Footer.prototype, "media", {
                    get: $util.oneOfGetter($oneOfFields = ["audioMessage"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new Footer instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.InteractiveMessage.Footer
                 * @static
                 * @param {proto.Message.InteractiveMessage.IFooter=} [properties] Properties to set
                 * @returns {proto.Message.InteractiveMessage.Footer} Footer instance
                 */
                Footer.create = function create(properties) {
                    return new Footer(properties);
                };

                /**
                 * Encodes the specified Footer message. Does not implicitly {@link proto.Message.InteractiveMessage.Footer.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.InteractiveMessage.Footer
                 * @static
                 * @param {proto.Message.InteractiveMessage.IFooter} message Footer message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Footer.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.text);
                    if (message.audioMessage != null && Object.hasOwnProperty.call(message, "audioMessage"))
                        $root.proto.Message.AudioMessage.encode(message.audioMessage, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                    if (message.hasMediaAttachment != null && Object.hasOwnProperty.call(message, "hasMediaAttachment"))
                        writer.uint32(/* id 3, wireType 0 =*/24).bool(message.hasMediaAttachment);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified Footer message, length delimited. Does not implicitly {@link proto.Message.InteractiveMessage.Footer.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.InteractiveMessage.Footer
                 * @static
                 * @param {proto.Message.InteractiveMessage.IFooter} message Footer message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Footer.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a Footer message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.InteractiveMessage.Footer
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.InteractiveMessage.Footer} Footer
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Footer.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.text = reader.stringVerify();
                                message._text = "text";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                message.hasMediaAttachment = reader.bool();
                                message._hasMediaAttachment = "hasMediaAttachment";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.audioMessage = $root.proto.Message.AudioMessage.decode(reader, reader.uint32(), undefined, q + 1, message.audioMessage);
                                message.media = "audioMessage";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a Footer message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.InteractiveMessage.Footer
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.InteractiveMessage.Footer} Footer
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Footer.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a Footer message.
                 * @function verify
                 * @memberof proto.Message.InteractiveMessage.Footer
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                Footer.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.text != null && Object.hasOwnProperty.call(message, "text")) {
                        properties._text = 1;
                        if (!$util.isString(message.text))
                            return "text: string expected";
                    }
                    if (message.hasMediaAttachment != null && Object.hasOwnProperty.call(message, "hasMediaAttachment")) {
                        properties._hasMediaAttachment = 1;
                        if (typeof message.hasMediaAttachment !== "boolean")
                            return "hasMediaAttachment: boolean expected";
                    }
                    if (message.audioMessage != null && Object.hasOwnProperty.call(message, "audioMessage")) {
                        properties.media = 1;
                        {
                            var error = $root.proto.Message.AudioMessage.verify(message.audioMessage, q + 1);
                            if (error)
                                return "audioMessage." + error;
                        }
                    }
                    return null;
                };

                /**
                 * Creates a Footer message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.InteractiveMessage.Footer
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.InteractiveMessage.Footer} Footer
                 */
                Footer.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.InteractiveMessage.Footer: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.text != null)
                        message.text = String(object.text);
                    if (object.hasMediaAttachment != null)
                        message.hasMediaAttachment = Boolean(object.hasMediaAttachment);
                    if (object.audioMessage != null) {
                        if (!$util.isObject(object.audioMessage))
                            throw TypeError(".proto.Message.InteractiveMessage.Footer.audioMessage: object expected");
                        message.audioMessage = $root.proto.Message.AudioMessage.fromObject(object.audioMessage, q + 1);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a Footer message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.InteractiveMessage.Footer
                 * @static
                 * @param {proto.Message.InteractiveMessage.Footer} message Footer
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                Footer.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                        object.text = message.text;
                    if (message.audioMessage != null && Object.hasOwnProperty.call(message, "audioMessage")) {
                        object.audioMessage = $root.proto.Message.AudioMessage.toObject(message.audioMessage, options, q + 1);
                        if (options.oneofs)
                            object.media = "audioMessage";
                    }
                    if (message.hasMediaAttachment != null && Object.hasOwnProperty.call(message, "hasMediaAttachment"))
                        object.hasMediaAttachment = message.hasMediaAttachment;
                    return object;
                };

                /**
                 * Converts this Footer to JSON.
                 * @function toJSON
                 * @memberof proto.Message.InteractiveMessage.Footer
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                Footer.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for Footer
                 * @function getTypeUrl
                 * @memberof proto.Message.InteractiveMessage.Footer
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                Footer.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.InteractiveMessage.Footer";
                };

                return Footer;
            })();

            InteractiveMessage.Header = (function() {

                /**
                 * Properties of a Header.
                 * @memberof proto.Message.InteractiveMessage
                 * @interface IHeader
                 * @property {string|null} [title] Header title
                 * @property {string|null} [subtitle] Header subtitle
                 * @property {boolean|null} [hasMediaAttachment] Header hasMediaAttachment
                 * @property {proto.Message.InteractiveMessage.IBloksWidget|null} [bloksWidget] Header bloksWidget
                 * @property {proto.Message.IDocumentMessage|null} [documentMessage] Header documentMessage
                 * @property {proto.Message.IImageMessage|null} [imageMessage] Header imageMessage
                 * @property {Uint8Array|null} [jpegThumbnail] Header jpegThumbnail
                 * @property {proto.Message.IVideoMessage|null} [videoMessage] Header videoMessage
                 * @property {proto.Message.ILocationMessage|null} [locationMessage] Header locationMessage
                 * @property {proto.Message.IProductMessage|null} [productMessage] Header productMessage
                 */

                /**
                 * Constructs a new Header.
                 * @memberof proto.Message.InteractiveMessage
                 * @classdesc Represents a Header.
                 * @implements IHeader
                 * @constructor
                 * @param {proto.Message.InteractiveMessage.IHeader=} [properties] Properties to set
                 */
                function Header(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * Header title.
                 * @member {string|null|undefined} title
                 * @memberof proto.Message.InteractiveMessage.Header
                 * @instance
                 */
                Header.prototype.title = null;

                /**
                 * Header subtitle.
                 * @member {string|null|undefined} subtitle
                 * @memberof proto.Message.InteractiveMessage.Header
                 * @instance
                 */
                Header.prototype.subtitle = null;

                /**
                 * Header hasMediaAttachment.
                 * @member {boolean|null|undefined} hasMediaAttachment
                 * @memberof proto.Message.InteractiveMessage.Header
                 * @instance
                 */
                Header.prototype.hasMediaAttachment = null;

                /**
                 * Header bloksWidget.
                 * @member {proto.Message.InteractiveMessage.IBloksWidget|null|undefined} bloksWidget
                 * @memberof proto.Message.InteractiveMessage.Header
                 * @instance
                 */
                Header.prototype.bloksWidget = null;

                /**
                 * Header documentMessage.
                 * @member {proto.Message.IDocumentMessage|null|undefined} documentMessage
                 * @memberof proto.Message.InteractiveMessage.Header
                 * @instance
                 */
                Header.prototype.documentMessage = null;

                /**
                 * Header imageMessage.
                 * @member {proto.Message.IImageMessage|null|undefined} imageMessage
                 * @memberof proto.Message.InteractiveMessage.Header
                 * @instance
                 */
                Header.prototype.imageMessage = null;

                /**
                 * Header jpegThumbnail.
                 * @member {Uint8Array|null|undefined} jpegThumbnail
                 * @memberof proto.Message.InteractiveMessage.Header
                 * @instance
                 */
                Header.prototype.jpegThumbnail = null;

                /**
                 * Header videoMessage.
                 * @member {proto.Message.IVideoMessage|null|undefined} videoMessage
                 * @memberof proto.Message.InteractiveMessage.Header
                 * @instance
                 */
                Header.prototype.videoMessage = null;

                /**
                 * Header locationMessage.
                 * @member {proto.Message.ILocationMessage|null|undefined} locationMessage
                 * @memberof proto.Message.InteractiveMessage.Header
                 * @instance
                 */
                Header.prototype.locationMessage = null;

                /**
                 * Header productMessage.
                 * @member {proto.Message.IProductMessage|null|undefined} productMessage
                 * @memberof proto.Message.InteractiveMessage.Header
                 * @instance
                 */
                Header.prototype.productMessage = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Header.prototype, "_title", {
                    get: $util.oneOfGetter($oneOfFields = ["title"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Header.prototype, "_subtitle", {
                    get: $util.oneOfGetter($oneOfFields = ["subtitle"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Header.prototype, "_hasMediaAttachment", {
                    get: $util.oneOfGetter($oneOfFields = ["hasMediaAttachment"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Header.prototype, "_bloksWidget", {
                    get: $util.oneOfGetter($oneOfFields = ["bloksWidget"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Header media.
                 * @member {"documentMessage"|"imageMessage"|"jpegThumbnail"|"videoMessage"|"locationMessage"|"productMessage"|undefined} media
                 * @memberof proto.Message.InteractiveMessage.Header
                 * @instance
                 */
                Object.defineProperty(Header.prototype, "media", {
                    get: $util.oneOfGetter($oneOfFields = ["documentMessage", "imageMessage", "jpegThumbnail", "videoMessage", "locationMessage", "productMessage"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new Header instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.InteractiveMessage.Header
                 * @static
                 * @param {proto.Message.InteractiveMessage.IHeader=} [properties] Properties to set
                 * @returns {proto.Message.InteractiveMessage.Header} Header instance
                 */
                Header.create = function create(properties) {
                    return new Header(properties);
                };

                /**
                 * Encodes the specified Header message. Does not implicitly {@link proto.Message.InteractiveMessage.Header.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.InteractiveMessage.Header
                 * @static
                 * @param {proto.Message.InteractiveMessage.IHeader} message Header message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Header.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.title);
                    if (message.subtitle != null && Object.hasOwnProperty.call(message, "subtitle"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.subtitle);
                    if (message.documentMessage != null && Object.hasOwnProperty.call(message, "documentMessage"))
                        $root.proto.Message.DocumentMessage.encode(message.documentMessage, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                    if (message.imageMessage != null && Object.hasOwnProperty.call(message, "imageMessage"))
                        $root.proto.Message.ImageMessage.encode(message.imageMessage, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                    if (message.hasMediaAttachment != null && Object.hasOwnProperty.call(message, "hasMediaAttachment"))
                        writer.uint32(/* id 5, wireType 0 =*/40).bool(message.hasMediaAttachment);
                    if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                        writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.jpegThumbnail);
                    if (message.videoMessage != null && Object.hasOwnProperty.call(message, "videoMessage"))
                        $root.proto.Message.VideoMessage.encode(message.videoMessage, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
                    if (message.locationMessage != null && Object.hasOwnProperty.call(message, "locationMessage"))
                        $root.proto.Message.LocationMessage.encode(message.locationMessage, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
                    if (message.productMessage != null && Object.hasOwnProperty.call(message, "productMessage"))
                        $root.proto.Message.ProductMessage.encode(message.productMessage, writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
                    if (message.bloksWidget != null && Object.hasOwnProperty.call(message, "bloksWidget"))
                        $root.proto.Message.InteractiveMessage.BloksWidget.encode(message.bloksWidget, writer.uint32(/* id 10, wireType 2 =*/82).fork(), q + 1).ldelim();
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified Header message, length delimited. Does not implicitly {@link proto.Message.InteractiveMessage.Header.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.InteractiveMessage.Header
                 * @static
                 * @param {proto.Message.InteractiveMessage.IHeader} message Header message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Header.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a Header message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.InteractiveMessage.Header
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.InteractiveMessage.Header} Header
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Header.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.title = reader.stringVerify();
                                message._title = "title";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.subtitle = reader.stringVerify();
                                message._subtitle = "subtitle";
                                continue;
                            }
                        case 5: {
                                if (u !== 0)
                                    break;
                                message.hasMediaAttachment = reader.bool();
                                message._hasMediaAttachment = "hasMediaAttachment";
                                continue;
                            }
                        case 10: {
                                if (u !== 2)
                                    break;
                                message.bloksWidget = $root.proto.Message.InteractiveMessage.BloksWidget.decode(reader, reader.uint32(), undefined, q + 1, message.bloksWidget);
                                message._bloksWidget = "bloksWidget";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.documentMessage = $root.proto.Message.DocumentMessage.decode(reader, reader.uint32(), undefined, q + 1, message.documentMessage);
                                message.media = "documentMessage";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                message.imageMessage = $root.proto.Message.ImageMessage.decode(reader, reader.uint32(), undefined, q + 1, message.imageMessage);
                                message.media = "imageMessage";
                                continue;
                            }
                        case 6: {
                                if (u !== 2)
                                    break;
                                message.jpegThumbnail = reader.bytes();
                                message.media = "jpegThumbnail";
                                continue;
                            }
                        case 7: {
                                if (u !== 2)
                                    break;
                                message.videoMessage = $root.proto.Message.VideoMessage.decode(reader, reader.uint32(), undefined, q + 1, message.videoMessage);
                                message.media = "videoMessage";
                                continue;
                            }
                        case 8: {
                                if (u !== 2)
                                    break;
                                message.locationMessage = $root.proto.Message.LocationMessage.decode(reader, reader.uint32(), undefined, q + 1, message.locationMessage);
                                message.media = "locationMessage";
                                continue;
                            }
                        case 9: {
                                if (u !== 2)
                                    break;
                                message.productMessage = $root.proto.Message.ProductMessage.decode(reader, reader.uint32(), undefined, q + 1, message.productMessage);
                                message.media = "productMessage";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a Header message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.InteractiveMessage.Header
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.InteractiveMessage.Header} Header
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Header.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a Header message.
                 * @function verify
                 * @memberof proto.Message.InteractiveMessage.Header
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                Header.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                        properties._title = 1;
                        if (!$util.isString(message.title))
                            return "title: string expected";
                    }
                    if (message.subtitle != null && Object.hasOwnProperty.call(message, "subtitle")) {
                        properties._subtitle = 1;
                        if (!$util.isString(message.subtitle))
                            return "subtitle: string expected";
                    }
                    if (message.hasMediaAttachment != null && Object.hasOwnProperty.call(message, "hasMediaAttachment")) {
                        properties._hasMediaAttachment = 1;
                        if (typeof message.hasMediaAttachment !== "boolean")
                            return "hasMediaAttachment: boolean expected";
                    }
                    if (message.bloksWidget != null && Object.hasOwnProperty.call(message, "bloksWidget")) {
                        properties._bloksWidget = 1;
                        {
                            var error = $root.proto.Message.InteractiveMessage.BloksWidget.verify(message.bloksWidget, q + 1);
                            if (error)
                                return "bloksWidget." + error;
                        }
                    }
                    if (message.documentMessage != null && Object.hasOwnProperty.call(message, "documentMessage")) {
                        properties.media = 1;
                        {
                            var error = $root.proto.Message.DocumentMessage.verify(message.documentMessage, q + 1);
                            if (error)
                                return "documentMessage." + error;
                        }
                    }
                    if (message.imageMessage != null && Object.hasOwnProperty.call(message, "imageMessage")) {
                        if (properties.media === 1)
                            return "media: multiple values";
                        properties.media = 1;
                        {
                            var error = $root.proto.Message.ImageMessage.verify(message.imageMessage, q + 1);
                            if (error)
                                return "imageMessage." + error;
                        }
                    }
                    if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail")) {
                        if (properties.media === 1)
                            return "media: multiple values";
                        properties.media = 1;
                        if (!(message.jpegThumbnail && typeof message.jpegThumbnail.length === "number" || $util.isString(message.jpegThumbnail)))
                            return "jpegThumbnail: buffer expected";
                    }
                    if (message.videoMessage != null && Object.hasOwnProperty.call(message, "videoMessage")) {
                        if (properties.media === 1)
                            return "media: multiple values";
                        properties.media = 1;
                        {
                            var error = $root.proto.Message.VideoMessage.verify(message.videoMessage, q + 1);
                            if (error)
                                return "videoMessage." + error;
                        }
                    }
                    if (message.locationMessage != null && Object.hasOwnProperty.call(message, "locationMessage")) {
                        if (properties.media === 1)
                            return "media: multiple values";
                        properties.media = 1;
                        {
                            var error = $root.proto.Message.LocationMessage.verify(message.locationMessage, q + 1);
                            if (error)
                                return "locationMessage." + error;
                        }
                    }
                    if (message.productMessage != null && Object.hasOwnProperty.call(message, "productMessage")) {
                        if (properties.media === 1)
                            return "media: multiple values";
                        properties.media = 1;
                        {
                            var error = $root.proto.Message.ProductMessage.verify(message.productMessage, q + 1);
                            if (error)
                                return "productMessage." + error;
                        }
                    }
                    return null;
                };

                /**
                 * Creates a Header message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.InteractiveMessage.Header
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.InteractiveMessage.Header} Header
                 */
                Header.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.InteractiveMessage.Header: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.title != null)
                        message.title = String(object.title);
                    if (object.subtitle != null)
                        message.subtitle = String(object.subtitle);
                    if (object.hasMediaAttachment != null)
                        message.hasMediaAttachment = Boolean(object.hasMediaAttachment);
                    if (object.bloksWidget != null) {
                        if (!$util.isObject(object.bloksWidget))
                            throw TypeError(".proto.Message.InteractiveMessage.Header.bloksWidget: object expected");
                        message.bloksWidget = $root.proto.Message.InteractiveMessage.BloksWidget.fromObject(object.bloksWidget, q + 1);
                    }
                    if (object.documentMessage != null) {
                        if (!$util.isObject(object.documentMessage))
                            throw TypeError(".proto.Message.InteractiveMessage.Header.documentMessage: object expected");
                        message.documentMessage = $root.proto.Message.DocumentMessage.fromObject(object.documentMessage, q + 1);
                    }
                    if (object.imageMessage != null) {
                        if (!$util.isObject(object.imageMessage))
                            throw TypeError(".proto.Message.InteractiveMessage.Header.imageMessage: object expected");
                        message.imageMessage = $root.proto.Message.ImageMessage.fromObject(object.imageMessage, q + 1);
                    }
                    if (object.jpegThumbnail != null)
                        if (typeof object.jpegThumbnail === "string")
                            $util.base64.decode(object.jpegThumbnail, message.jpegThumbnail = $util.newBuffer($util.base64.length(object.jpegThumbnail)), 0);
                        else if (object.jpegThumbnail.length >= 0)
                            message.jpegThumbnail = object.jpegThumbnail;
                    if (object.videoMessage != null) {
                        if (!$util.isObject(object.videoMessage))
                            throw TypeError(".proto.Message.InteractiveMessage.Header.videoMessage: object expected");
                        message.videoMessage = $root.proto.Message.VideoMessage.fromObject(object.videoMessage, q + 1);
                    }
                    if (object.locationMessage != null) {
                        if (!$util.isObject(object.locationMessage))
                            throw TypeError(".proto.Message.InteractiveMessage.Header.locationMessage: object expected");
                        message.locationMessage = $root.proto.Message.LocationMessage.fromObject(object.locationMessage, q + 1);
                    }
                    if (object.productMessage != null) {
                        if (!$util.isObject(object.productMessage))
                            throw TypeError(".proto.Message.InteractiveMessage.Header.productMessage: object expected");
                        message.productMessage = $root.proto.Message.ProductMessage.fromObject(object.productMessage, q + 1);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a Header message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.InteractiveMessage.Header
                 * @static
                 * @param {proto.Message.InteractiveMessage.Header} message Header
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                Header.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                        object.title = message.title;
                    if (message.subtitle != null && Object.hasOwnProperty.call(message, "subtitle"))
                        object.subtitle = message.subtitle;
                    if (message.documentMessage != null && Object.hasOwnProperty.call(message, "documentMessage")) {
                        object.documentMessage = $root.proto.Message.DocumentMessage.toObject(message.documentMessage, options, q + 1);
                        if (options.oneofs)
                            object.media = "documentMessage";
                    }
                    if (message.imageMessage != null && Object.hasOwnProperty.call(message, "imageMessage")) {
                        object.imageMessage = $root.proto.Message.ImageMessage.toObject(message.imageMessage, options, q + 1);
                        if (options.oneofs)
                            object.media = "imageMessage";
                    }
                    if (message.hasMediaAttachment != null && Object.hasOwnProperty.call(message, "hasMediaAttachment"))
                        object.hasMediaAttachment = message.hasMediaAttachment;
                    if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail")) {
                        object.jpegThumbnail = options.bytes === String ? $util.base64.encode(message.jpegThumbnail, 0, message.jpegThumbnail.length) : options.bytes === Array ? Array.prototype.slice.call(message.jpegThumbnail) : message.jpegThumbnail;
                        if (options.oneofs)
                            object.media = "jpegThumbnail";
                    }
                    if (message.videoMessage != null && Object.hasOwnProperty.call(message, "videoMessage")) {
                        object.videoMessage = $root.proto.Message.VideoMessage.toObject(message.videoMessage, options, q + 1);
                        if (options.oneofs)
                            object.media = "videoMessage";
                    }
                    if (message.locationMessage != null && Object.hasOwnProperty.call(message, "locationMessage")) {
                        object.locationMessage = $root.proto.Message.LocationMessage.toObject(message.locationMessage, options, q + 1);
                        if (options.oneofs)
                            object.media = "locationMessage";
                    }
                    if (message.productMessage != null && Object.hasOwnProperty.call(message, "productMessage")) {
                        object.productMessage = $root.proto.Message.ProductMessage.toObject(message.productMessage, options, q + 1);
                        if (options.oneofs)
                            object.media = "productMessage";
                    }
                    if (message.bloksWidget != null && Object.hasOwnProperty.call(message, "bloksWidget"))
                        object.bloksWidget = $root.proto.Message.InteractiveMessage.BloksWidget.toObject(message.bloksWidget, options, q + 1);
                    return object;
                };

                /**
                 * Converts this Header to JSON.
                 * @function toJSON
                 * @memberof proto.Message.InteractiveMessage.Header
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                Header.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for Header
                 * @function getTypeUrl
                 * @memberof proto.Message.InteractiveMessage.Header
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                Header.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.InteractiveMessage.Header";
                };

                return Header;
            })();

            InteractiveMessage.NativeFlowMessage = (function() {

                /**
                 * Properties of a NativeFlowMessage.
                 * @memberof proto.Message.InteractiveMessage
                 * @interface INativeFlowMessage
                 * @property {Array.<proto.Message.InteractiveMessage.NativeFlowMessage.INativeFlowButton>|null} [buttons] NativeFlowMessage buttons
                 * @property {string|null} [messageParamsJson] NativeFlowMessage messageParamsJson
                 * @property {number|null} [messageVersion] NativeFlowMessage messageVersion
                 */

                /**
                 * Constructs a new NativeFlowMessage.
                 * @memberof proto.Message.InteractiveMessage
                 * @classdesc Represents a NativeFlowMessage.
                 * @implements INativeFlowMessage
                 * @constructor
                 * @param {proto.Message.InteractiveMessage.INativeFlowMessage=} [properties] Properties to set
                 */
                function NativeFlowMessage(properties) {
                    this.buttons = [];
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * NativeFlowMessage buttons.
                 * @member {Array.<proto.Message.InteractiveMessage.NativeFlowMessage.INativeFlowButton>} buttons
                 * @memberof proto.Message.InteractiveMessage.NativeFlowMessage
                 * @instance
                 */
                NativeFlowMessage.prototype.buttons = $util.emptyArray;

                /**
                 * NativeFlowMessage messageParamsJson.
                 * @member {string|null|undefined} messageParamsJson
                 * @memberof proto.Message.InteractiveMessage.NativeFlowMessage
                 * @instance
                 */
                NativeFlowMessage.prototype.messageParamsJson = null;

                /**
                 * NativeFlowMessage messageVersion.
                 * @member {number|null|undefined} messageVersion
                 * @memberof proto.Message.InteractiveMessage.NativeFlowMessage
                 * @instance
                 */
                NativeFlowMessage.prototype.messageVersion = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(NativeFlowMessage.prototype, "_messageParamsJson", {
                    get: $util.oneOfGetter($oneOfFields = ["messageParamsJson"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(NativeFlowMessage.prototype, "_messageVersion", {
                    get: $util.oneOfGetter($oneOfFields = ["messageVersion"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new NativeFlowMessage instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.InteractiveMessage.NativeFlowMessage
                 * @static
                 * @param {proto.Message.InteractiveMessage.INativeFlowMessage=} [properties] Properties to set
                 * @returns {proto.Message.InteractiveMessage.NativeFlowMessage} NativeFlowMessage instance
                 */
                NativeFlowMessage.create = function create(properties) {
                    return new NativeFlowMessage(properties);
                };

                /**
                 * Encodes the specified NativeFlowMessage message. Does not implicitly {@link proto.Message.InteractiveMessage.NativeFlowMessage.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.InteractiveMessage.NativeFlowMessage
                 * @static
                 * @param {proto.Message.InteractiveMessage.INativeFlowMessage} message NativeFlowMessage message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                NativeFlowMessage.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.buttons != null && message.buttons.length)
                        for (var i = 0; i < message.buttons.length; ++i)
                            $root.proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton.encode(message.buttons[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                    if (message.messageParamsJson != null && Object.hasOwnProperty.call(message, "messageParamsJson"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.messageParamsJson);
                    if (message.messageVersion != null && Object.hasOwnProperty.call(message, "messageVersion"))
                        writer.uint32(/* id 3, wireType 0 =*/24).int32(message.messageVersion);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified NativeFlowMessage message, length delimited. Does not implicitly {@link proto.Message.InteractiveMessage.NativeFlowMessage.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.InteractiveMessage.NativeFlowMessage
                 * @static
                 * @param {proto.Message.InteractiveMessage.INativeFlowMessage} message NativeFlowMessage message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                NativeFlowMessage.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a NativeFlowMessage message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.InteractiveMessage.NativeFlowMessage
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.InteractiveMessage.NativeFlowMessage} NativeFlowMessage
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                NativeFlowMessage.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                if (!(message.buttons && message.buttons.length))
                                    message.buttons = [];
                                message.buttons.push($root.proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton.decode(reader, reader.uint32(), undefined, q + 1));
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.messageParamsJson = reader.stringVerify();
                                message._messageParamsJson = "messageParamsJson";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                message.messageVersion = reader.int32();
                                message._messageVersion = "messageVersion";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a NativeFlowMessage message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.InteractiveMessage.NativeFlowMessage
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.InteractiveMessage.NativeFlowMessage} NativeFlowMessage
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                NativeFlowMessage.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a NativeFlowMessage message.
                 * @function verify
                 * @memberof proto.Message.InteractiveMessage.NativeFlowMessage
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                NativeFlowMessage.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.buttons != null && Object.hasOwnProperty.call(message, "buttons")) {
                        if (!Array.isArray(message.buttons))
                            return "buttons: array expected";
                        for (var i = 0; i < message.buttons.length; ++i) {
                            var error = $root.proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton.verify(message.buttons[i], q + 1);
                            if (error)
                                return "buttons." + error;
                        }
                    }
                    if (message.messageParamsJson != null && Object.hasOwnProperty.call(message, "messageParamsJson")) {
                        properties._messageParamsJson = 1;
                        if (!$util.isString(message.messageParamsJson))
                            return "messageParamsJson: string expected";
                    }
                    if (message.messageVersion != null && Object.hasOwnProperty.call(message, "messageVersion")) {
                        properties._messageVersion = 1;
                        if (!$util.isInteger(message.messageVersion))
                            return "messageVersion: integer expected";
                    }
                    return null;
                };

                /**
                 * Creates a NativeFlowMessage message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.InteractiveMessage.NativeFlowMessage
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.InteractiveMessage.NativeFlowMessage} NativeFlowMessage
                 */
                NativeFlowMessage.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.InteractiveMessage.NativeFlowMessage: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.buttons) {
                        if (!Array.isArray(object.buttons))
                            throw TypeError(".proto.Message.InteractiveMessage.NativeFlowMessage.buttons: array expected");
                        message.buttons = Array(object.buttons.length);
                        for (var i = 0; i < object.buttons.length; ++i) {
                            if (!$util.isObject(object.buttons[i]))
                                throw TypeError(".proto.Message.InteractiveMessage.NativeFlowMessage.buttons: object expected");
                            message.buttons[i] = $root.proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton.fromObject(object.buttons[i], q + 1);
                        }
                    }
                    if (object.messageParamsJson != null)
                        message.messageParamsJson = String(object.messageParamsJson);
                    if (object.messageVersion != null)
                        message.messageVersion = object.messageVersion | 0;
                    return message;
                };

                /**
                 * Creates a plain object from a NativeFlowMessage message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.InteractiveMessage.NativeFlowMessage
                 * @static
                 * @param {proto.Message.InteractiveMessage.NativeFlowMessage} message NativeFlowMessage
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                NativeFlowMessage.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (options.arrays || options.defaults)
                        object.buttons = [];
                    if (message.buttons && message.buttons.length) {
                        object.buttons = Array(message.buttons.length);
                        for (var j = 0; j < message.buttons.length; ++j)
                            object.buttons[j] = $root.proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton.toObject(message.buttons[j], options, q + 1);
                    }
                    if (message.messageParamsJson != null && Object.hasOwnProperty.call(message, "messageParamsJson"))
                        object.messageParamsJson = message.messageParamsJson;
                    if (message.messageVersion != null && Object.hasOwnProperty.call(message, "messageVersion"))
                        object.messageVersion = message.messageVersion;
                    return object;
                };

                /**
                 * Converts this NativeFlowMessage to JSON.
                 * @function toJSON
                 * @memberof proto.Message.InteractiveMessage.NativeFlowMessage
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                NativeFlowMessage.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for NativeFlowMessage
                 * @function getTypeUrl
                 * @memberof proto.Message.InteractiveMessage.NativeFlowMessage
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                NativeFlowMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.InteractiveMessage.NativeFlowMessage";
                };

                NativeFlowMessage.NativeFlowButton = (function() {

                    /**
                     * Properties of a NativeFlowButton.
                     * @memberof proto.Message.InteractiveMessage.NativeFlowMessage
                     * @interface INativeFlowButton
                     * @property {string|null} [name] NativeFlowButton name
                     * @property {string|null} [buttonParamsJson] NativeFlowButton buttonParamsJson
                     */

                    /**
                     * Constructs a new NativeFlowButton.
                     * @memberof proto.Message.InteractiveMessage.NativeFlowMessage
                     * @classdesc Represents a NativeFlowButton.
                     * @implements INativeFlowButton
                     * @constructor
                     * @param {proto.Message.InteractiveMessage.NativeFlowMessage.INativeFlowButton=} [properties] Properties to set
                     */
                    function NativeFlowButton(properties) {
                        if (properties)
                            for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    /**
                     * NativeFlowButton name.
                     * @member {string|null|undefined} name
                     * @memberof proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton
                     * @instance
                     */
                    NativeFlowButton.prototype.name = null;

                    /**
                     * NativeFlowButton buttonParamsJson.
                     * @member {string|null|undefined} buttonParamsJson
                     * @memberof proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton
                     * @instance
                     */
                    NativeFlowButton.prototype.buttonParamsJson = null;

                    // OneOf field names bound to virtual getters and setters
                    var $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(NativeFlowButton.prototype, "_name", {
                        get: $util.oneOfGetter($oneOfFields = ["name"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(NativeFlowButton.prototype, "_buttonParamsJson", {
                        get: $util.oneOfGetter($oneOfFields = ["buttonParamsJson"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    /**
                     * Creates a new NativeFlowButton instance using the specified properties.
                     * @function create
                     * @memberof proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton
                     * @static
                     * @param {proto.Message.InteractiveMessage.NativeFlowMessage.INativeFlowButton=} [properties] Properties to set
                     * @returns {proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton} NativeFlowButton instance
                     */
                    NativeFlowButton.create = function create(properties) {
                        return new NativeFlowButton(properties);
                    };

                    /**
                     * Encodes the specified NativeFlowButton message. Does not implicitly {@link proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton.verify|verify} messages.
                     * @function encode
                     * @memberof proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton
                     * @static
                     * @param {proto.Message.InteractiveMessage.NativeFlowMessage.INativeFlowButton} message NativeFlowButton message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    NativeFlowButton.encode = function encode(message, writer, q) {
                        if (!writer)
                            writer = $Writer.create();
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                            writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                        if (message.buttonParamsJson != null && Object.hasOwnProperty.call(message, "buttonParamsJson"))
                            writer.uint32(/* id 2, wireType 2 =*/18).string(message.buttonParamsJson);
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (var i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    /**
                     * Encodes the specified NativeFlowButton message, length delimited. Does not implicitly {@link proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton.verify|verify} messages.
                     * @function encodeDelimited
                     * @memberof proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton
                     * @static
                     * @param {proto.Message.InteractiveMessage.NativeFlowMessage.INativeFlowButton} message NativeFlowButton message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    NativeFlowButton.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                    };

                    /**
                     * Decodes a NativeFlowButton message from the specified reader or buffer.
                     * @function decode
                     * @memberof proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @param {number} [length] Message length if known beforehand
                     * @returns {proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton} NativeFlowButton
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    NativeFlowButton.decode = function decode(reader, length, z, q, g) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (q === undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                        while (reader.pos < end) {
                            var s = reader.pos;
                            var tag = reader.tag();
                            if (tag === z) {
                                z = undefined;
                                break;
                            }
                            var u = tag & 7;
                            switch (tag >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    message.name = reader.stringVerify();
                                    message._name = "name";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 2)
                                        break;
                                    message.buttonParamsJson = reader.stringVerify();
                                    message._buttonParamsJson = "buttonParamsJson";
                                    continue;
                                }
                            }
                            reader.skipType(u, q, tag);
                            if (!reader.discardUnknown) {
                                $util.makeProp(message, "$unknowns", false);
                                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                            }
                        }
                        if (z !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    /**
                     * Decodes a NativeFlowButton message from the specified reader or buffer, length delimited.
                     * @function decodeDelimited
                     * @memberof proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @returns {proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton} NativeFlowButton
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    NativeFlowButton.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    /**
                     * Verifies a NativeFlowButton message.
                     * @function verify
                     * @memberof proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton
                     * @static
                     * @param {Object.<string,*>} message Plain object to verify
                     * @returns {string|null} `null` if valid, otherwise the reason why it is not
                     */
                    NativeFlowButton.verify = function verify(message, q) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            return "max depth exceeded";
                        var properties = {};
                        if (message.name != null && Object.hasOwnProperty.call(message, "name")) {
                            properties._name = 1;
                            if (!$util.isString(message.name))
                                return "name: string expected";
                        }
                        if (message.buttonParamsJson != null && Object.hasOwnProperty.call(message, "buttonParamsJson")) {
                            properties._buttonParamsJson = 1;
                            if (!$util.isString(message.buttonParamsJson))
                                return "buttonParamsJson: string expected";
                        }
                        return null;
                    };

                    /**
                     * Creates a NativeFlowButton message from a plain object. Also converts values to their respective internal types.
                     * @function fromObject
                     * @memberof proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton
                     * @static
                     * @param {Object.<string,*>} object Plain object
                     * @returns {proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton} NativeFlowButton
                     */
                    NativeFlowButton.fromObject = function fromObject(object, q) {
                        if (object instanceof C)
                            return object;
                        if (!$util.isObject(object))
                            throw TypeError(".proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton: object expected");
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var message = new C();
                        if (object.name != null)
                            message.name = String(object.name);
                        if (object.buttonParamsJson != null)
                            message.buttonParamsJson = String(object.buttonParamsJson);
                        return message;
                    };

                    /**
                     * Creates a plain object from a NativeFlowButton message. Also converts values to other types if specified.
                     * @function toObject
                     * @memberof proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton
                     * @static
                     * @param {proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton} message NativeFlowButton
                     * @param {$protobuf.IConversionOptions} [options] Conversion options
                     * @returns {Object.<string,*>} Plain object
                     */
                    NativeFlowButton.toObject = function toObject(message, options, q) {
                        if (!options)
                            options = {};
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var object = {};
                        if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                            object.name = message.name;
                        if (message.buttonParamsJson != null && Object.hasOwnProperty.call(message, "buttonParamsJson"))
                            object.buttonParamsJson = message.buttonParamsJson;
                        return object;
                    };

                    /**
                     * Converts this NativeFlowButton to JSON.
                     * @function toJSON
                     * @memberof proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton
                     * @instance
                     * @returns {Object.<string,*>} JSON object
                     */
                    NativeFlowButton.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    /**
                     * Gets the default type url for NativeFlowButton
                     * @function getTypeUrl
                     * @memberof proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton
                     * @static
                     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                     * @returns {string} The default type url
                     */
                    NativeFlowButton.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton";
                    };

                    return NativeFlowButton;
                })();

                return NativeFlowMessage;
            })();

            InteractiveMessage.ShopMessage = (function() {

                /**
                 * Properties of a ShopMessage.
                 * @memberof proto.Message.InteractiveMessage
                 * @interface IShopMessage
                 * @property {string|null} [id] ShopMessage id
                 * @property {proto.Message.InteractiveMessage.ShopMessage.Surface|null} [surface] ShopMessage surface
                 * @property {number|null} [messageVersion] ShopMessage messageVersion
                 */

                /**
                 * Constructs a new ShopMessage.
                 * @memberof proto.Message.InteractiveMessage
                 * @classdesc Represents a ShopMessage.
                 * @implements IShopMessage
                 * @constructor
                 * @param {proto.Message.InteractiveMessage.IShopMessage=} [properties] Properties to set
                 */
                function ShopMessage(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * ShopMessage id.
                 * @member {string|null|undefined} id
                 * @memberof proto.Message.InteractiveMessage.ShopMessage
                 * @instance
                 */
                ShopMessage.prototype.id = null;

                /**
                 * ShopMessage surface.
                 * @member {proto.Message.InteractiveMessage.ShopMessage.Surface|null|undefined} surface
                 * @memberof proto.Message.InteractiveMessage.ShopMessage
                 * @instance
                 */
                ShopMessage.prototype.surface = null;

                /**
                 * ShopMessage messageVersion.
                 * @member {number|null|undefined} messageVersion
                 * @memberof proto.Message.InteractiveMessage.ShopMessage
                 * @instance
                 */
                ShopMessage.prototype.messageVersion = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ShopMessage.prototype, "_id", {
                    get: $util.oneOfGetter($oneOfFields = ["id"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ShopMessage.prototype, "_surface", {
                    get: $util.oneOfGetter($oneOfFields = ["surface"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ShopMessage.prototype, "_messageVersion", {
                    get: $util.oneOfGetter($oneOfFields = ["messageVersion"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new ShopMessage instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.InteractiveMessage.ShopMessage
                 * @static
                 * @param {proto.Message.InteractiveMessage.IShopMessage=} [properties] Properties to set
                 * @returns {proto.Message.InteractiveMessage.ShopMessage} ShopMessage instance
                 */
                ShopMessage.create = function create(properties) {
                    return new ShopMessage(properties);
                };

                /**
                 * Encodes the specified ShopMessage message. Does not implicitly {@link proto.Message.InteractiveMessage.ShopMessage.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.InteractiveMessage.ShopMessage
                 * @static
                 * @param {proto.Message.InteractiveMessage.IShopMessage} message ShopMessage message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ShopMessage.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
                    if (message.surface != null && Object.hasOwnProperty.call(message, "surface"))
                        writer.uint32(/* id 2, wireType 0 =*/16).int32(message.surface);
                    if (message.messageVersion != null && Object.hasOwnProperty.call(message, "messageVersion"))
                        writer.uint32(/* id 3, wireType 0 =*/24).int32(message.messageVersion);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified ShopMessage message, length delimited. Does not implicitly {@link proto.Message.InteractiveMessage.ShopMessage.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.InteractiveMessage.ShopMessage
                 * @static
                 * @param {proto.Message.InteractiveMessage.IShopMessage} message ShopMessage message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ShopMessage.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a ShopMessage message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.InteractiveMessage.ShopMessage
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.InteractiveMessage.ShopMessage} ShopMessage
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ShopMessage.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.id = reader.stringVerify();
                                message._id = "id";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                message.surface = reader.int32();
                                message._surface = "surface";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                message.messageVersion = reader.int32();
                                message._messageVersion = "messageVersion";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a ShopMessage message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.InteractiveMessage.ShopMessage
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.InteractiveMessage.ShopMessage} ShopMessage
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ShopMessage.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a ShopMessage message.
                 * @function verify
                 * @memberof proto.Message.InteractiveMessage.ShopMessage
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                ShopMessage.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.id != null && Object.hasOwnProperty.call(message, "id")) {
                        properties._id = 1;
                        if (!$util.isString(message.id))
                            return "id: string expected";
                    }
                    if (message.surface != null && Object.hasOwnProperty.call(message, "surface")) {
                        properties._surface = 1;
                        if (typeof message.surface !== "number" || (message.surface | 0) !== message.surface)
                            return "surface: enum value expected";
                    }
                    if (message.messageVersion != null && Object.hasOwnProperty.call(message, "messageVersion")) {
                        properties._messageVersion = 1;
                        if (!$util.isInteger(message.messageVersion))
                            return "messageVersion: integer expected";
                    }
                    return null;
                };

                /**
                 * Creates a ShopMessage message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.InteractiveMessage.ShopMessage
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.InteractiveMessage.ShopMessage} ShopMessage
                 */
                ShopMessage.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.InteractiveMessage.ShopMessage: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.id != null)
                        message.id = String(object.id);
                    switch (object.surface) {
                    case "UNKNOWN_SURFACE":
                    case 0:
                        message.surface = 0;
                        break;
                    case "FB":
                    case 1:
                        message.surface = 1;
                        break;
                    case "IG":
                    case 2:
                        message.surface = 2;
                        break;
                    case "WA":
                    case 3:
                        message.surface = 3;
                        break;
                    default:
                        if (typeof object.surface === "number" && (object.surface | 0) === object.surface)
                            message.surface = object.surface;
                    }
                    if (object.messageVersion != null)
                        message.messageVersion = object.messageVersion | 0;
                    return message;
                };

                /**
                 * Creates a plain object from a ShopMessage message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.InteractiveMessage.ShopMessage
                 * @static
                 * @param {proto.Message.InteractiveMessage.ShopMessage} message ShopMessage
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                ShopMessage.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                        object.id = message.id;
                    if (message.surface != null && Object.hasOwnProperty.call(message, "surface"))
                        object.surface = options.enums === String ? $root.proto.Message.InteractiveMessage.ShopMessage.Surface[message.surface] === undefined ? message.surface : $root.proto.Message.InteractiveMessage.ShopMessage.Surface[message.surface] : message.surface;
                    if (message.messageVersion != null && Object.hasOwnProperty.call(message, "messageVersion"))
                        object.messageVersion = message.messageVersion;
                    return object;
                };

                /**
                 * Converts this ShopMessage to JSON.
                 * @function toJSON
                 * @memberof proto.Message.InteractiveMessage.ShopMessage
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                ShopMessage.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for ShopMessage
                 * @function getTypeUrl
                 * @memberof proto.Message.InteractiveMessage.ShopMessage
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                ShopMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.InteractiveMessage.ShopMessage";
                };

                /**
                 * Surface enum.
                 * @name proto.Message.InteractiveMessage.ShopMessage.Surface
                 * @enum {number}
                 * @property {number} UNKNOWN_SURFACE=0 UNKNOWN_SURFACE value
                 * @property {number} FB=1 FB value
                 * @property {number} IG=2 IG value
                 * @property {number} WA=3 WA value
                 */
                ShopMessage.Surface = (function() {
                    var valuesById = {}, values = Object.create(valuesById);
                    values[valuesById[0] = "UNKNOWN_SURFACE"] = 0;
                    values[valuesById[1] = "FB"] = 1;
                    values[valuesById[2] = "IG"] = 2;
                    values[valuesById[3] = "WA"] = 3;
                    return values;
                })();

                return ShopMessage;
            })();

            return InteractiveMessage;
        })();

        Message.InteractiveResponseMessage = (function() {

            /**
             * Properties of an InteractiveResponseMessage.
             * @memberof proto.Message
             * @interface IInteractiveResponseMessage
             * @property {proto.Message.InteractiveResponseMessage.IBody|null} [body] InteractiveResponseMessage body
             * @property {proto.IContextInfo|null} [contextInfo] InteractiveResponseMessage contextInfo
             * @property {proto.Message.InteractiveResponseMessage.INativeFlowResponseMessage|null} [nativeFlowResponseMessage] InteractiveResponseMessage nativeFlowResponseMessage
             */

            /**
             * Constructs a new InteractiveResponseMessage.
             * @memberof proto.Message
             * @classdesc Represents an InteractiveResponseMessage.
             * @implements IInteractiveResponseMessage
             * @constructor
             * @param {proto.Message.IInteractiveResponseMessage=} [properties] Properties to set
             */
            function InteractiveResponseMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * InteractiveResponseMessage body.
             * @member {proto.Message.InteractiveResponseMessage.IBody|null|undefined} body
             * @memberof proto.Message.InteractiveResponseMessage
             * @instance
             */
            InteractiveResponseMessage.prototype.body = null;

            /**
             * InteractiveResponseMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.InteractiveResponseMessage
             * @instance
             */
            InteractiveResponseMessage.prototype.contextInfo = null;

            /**
             * InteractiveResponseMessage nativeFlowResponseMessage.
             * @member {proto.Message.InteractiveResponseMessage.INativeFlowResponseMessage|null|undefined} nativeFlowResponseMessage
             * @memberof proto.Message.InteractiveResponseMessage
             * @instance
             */
            InteractiveResponseMessage.prototype.nativeFlowResponseMessage = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InteractiveResponseMessage.prototype, "_body", {
                get: $util.oneOfGetter($oneOfFields = ["body"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InteractiveResponseMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * InteractiveResponseMessage interactiveResponseMessage.
             * @member {"nativeFlowResponseMessage"|undefined} interactiveResponseMessage
             * @memberof proto.Message.InteractiveResponseMessage
             * @instance
             */
            Object.defineProperty(InteractiveResponseMessage.prototype, "interactiveResponseMessage", {
                get: $util.oneOfGetter($oneOfFields = ["nativeFlowResponseMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new InteractiveResponseMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.InteractiveResponseMessage
             * @static
             * @param {proto.Message.IInteractiveResponseMessage=} [properties] Properties to set
             * @returns {proto.Message.InteractiveResponseMessage} InteractiveResponseMessage instance
             */
            InteractiveResponseMessage.create = function create(properties) {
                return new InteractiveResponseMessage(properties);
            };

            /**
             * Encodes the specified InteractiveResponseMessage message. Does not implicitly {@link proto.Message.InteractiveResponseMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.InteractiveResponseMessage
             * @static
             * @param {proto.Message.IInteractiveResponseMessage} message InteractiveResponseMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InteractiveResponseMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.body != null && Object.hasOwnProperty.call(message, "body"))
                    $root.proto.Message.InteractiveResponseMessage.Body.encode(message.body, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.nativeFlowResponseMessage != null && Object.hasOwnProperty.call(message, "nativeFlowResponseMessage"))
                    $root.proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage.encode(message.nativeFlowResponseMessage, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 15, wireType 2 =*/122).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified InteractiveResponseMessage message, length delimited. Does not implicitly {@link proto.Message.InteractiveResponseMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.InteractiveResponseMessage
             * @static
             * @param {proto.Message.IInteractiveResponseMessage} message InteractiveResponseMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InteractiveResponseMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an InteractiveResponseMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.InteractiveResponseMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.InteractiveResponseMessage} InteractiveResponseMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InteractiveResponseMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.body = $root.proto.Message.InteractiveResponseMessage.Body.decode(reader, reader.uint32(), undefined, q + 1, message.body);
                            message._body = "body";
                            continue;
                        }
                    case 15: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.nativeFlowResponseMessage = $root.proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage.decode(reader, reader.uint32(), undefined, q + 1, message.nativeFlowResponseMessage);
                            message.interactiveResponseMessage = "nativeFlowResponseMessage";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an InteractiveResponseMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.InteractiveResponseMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.InteractiveResponseMessage} InteractiveResponseMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InteractiveResponseMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an InteractiveResponseMessage message.
             * @function verify
             * @memberof proto.Message.InteractiveResponseMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            InteractiveResponseMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.body != null && Object.hasOwnProperty.call(message, "body")) {
                    properties._body = 1;
                    {
                        var error = $root.proto.Message.InteractiveResponseMessage.Body.verify(message.body, q + 1);
                        if (error)
                            return "body." + error;
                    }
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.nativeFlowResponseMessage != null && Object.hasOwnProperty.call(message, "nativeFlowResponseMessage")) {
                    properties.interactiveResponseMessage = 1;
                    {
                        var error = $root.proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage.verify(message.nativeFlowResponseMessage, q + 1);
                        if (error)
                            return "nativeFlowResponseMessage." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an InteractiveResponseMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.InteractiveResponseMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.InteractiveResponseMessage} InteractiveResponseMessage
             */
            InteractiveResponseMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.InteractiveResponseMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.body != null) {
                    if (!$util.isObject(object.body))
                        throw TypeError(".proto.Message.InteractiveResponseMessage.body: object expected");
                    message.body = $root.proto.Message.InteractiveResponseMessage.Body.fromObject(object.body, q + 1);
                }
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.InteractiveResponseMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                if (object.nativeFlowResponseMessage != null) {
                    if (!$util.isObject(object.nativeFlowResponseMessage))
                        throw TypeError(".proto.Message.InteractiveResponseMessage.nativeFlowResponseMessage: object expected");
                    message.nativeFlowResponseMessage = $root.proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage.fromObject(object.nativeFlowResponseMessage, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from an InteractiveResponseMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.InteractiveResponseMessage
             * @static
             * @param {proto.Message.InteractiveResponseMessage} message InteractiveResponseMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            InteractiveResponseMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.body != null && Object.hasOwnProperty.call(message, "body"))
                    object.body = $root.proto.Message.InteractiveResponseMessage.Body.toObject(message.body, options, q + 1);
                if (message.nativeFlowResponseMessage != null && Object.hasOwnProperty.call(message, "nativeFlowResponseMessage")) {
                    object.nativeFlowResponseMessage = $root.proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage.toObject(message.nativeFlowResponseMessage, options, q + 1);
                    if (options.oneofs)
                        object.interactiveResponseMessage = "nativeFlowResponseMessage";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                return object;
            };

            /**
             * Converts this InteractiveResponseMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.InteractiveResponseMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            InteractiveResponseMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for InteractiveResponseMessage
             * @function getTypeUrl
             * @memberof proto.Message.InteractiveResponseMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            InteractiveResponseMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.InteractiveResponseMessage";
            };

            InteractiveResponseMessage.Body = (function() {

                /**
                 * Properties of a Body.
                 * @memberof proto.Message.InteractiveResponseMessage
                 * @interface IBody
                 * @property {string|null} [text] Body text
                 * @property {proto.Message.InteractiveResponseMessage.Body.Format|null} [format] Body format
                 */

                /**
                 * Constructs a new Body.
                 * @memberof proto.Message.InteractiveResponseMessage
                 * @classdesc Represents a Body.
                 * @implements IBody
                 * @constructor
                 * @param {proto.Message.InteractiveResponseMessage.IBody=} [properties] Properties to set
                 */
                function Body(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * Body text.
                 * @member {string|null|undefined} text
                 * @memberof proto.Message.InteractiveResponseMessage.Body
                 * @instance
                 */
                Body.prototype.text = null;

                /**
                 * Body format.
                 * @member {proto.Message.InteractiveResponseMessage.Body.Format|null|undefined} format
                 * @memberof proto.Message.InteractiveResponseMessage.Body
                 * @instance
                 */
                Body.prototype.format = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Body.prototype, "_text", {
                    get: $util.oneOfGetter($oneOfFields = ["text"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Body.prototype, "_format", {
                    get: $util.oneOfGetter($oneOfFields = ["format"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new Body instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.InteractiveResponseMessage.Body
                 * @static
                 * @param {proto.Message.InteractiveResponseMessage.IBody=} [properties] Properties to set
                 * @returns {proto.Message.InteractiveResponseMessage.Body} Body instance
                 */
                Body.create = function create(properties) {
                    return new Body(properties);
                };

                /**
                 * Encodes the specified Body message. Does not implicitly {@link proto.Message.InteractiveResponseMessage.Body.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.InteractiveResponseMessage.Body
                 * @static
                 * @param {proto.Message.InteractiveResponseMessage.IBody} message Body message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Body.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.text);
                    if (message.format != null && Object.hasOwnProperty.call(message, "format"))
                        writer.uint32(/* id 2, wireType 0 =*/16).int32(message.format);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified Body message, length delimited. Does not implicitly {@link proto.Message.InteractiveResponseMessage.Body.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.InteractiveResponseMessage.Body
                 * @static
                 * @param {proto.Message.InteractiveResponseMessage.IBody} message Body message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Body.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a Body message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.InteractiveResponseMessage.Body
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.InteractiveResponseMessage.Body} Body
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Body.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.text = reader.stringVerify();
                                message._text = "text";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                message.format = reader.int32();
                                message._format = "format";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a Body message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.InteractiveResponseMessage.Body
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.InteractiveResponseMessage.Body} Body
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Body.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a Body message.
                 * @function verify
                 * @memberof proto.Message.InteractiveResponseMessage.Body
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                Body.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.text != null && Object.hasOwnProperty.call(message, "text")) {
                        properties._text = 1;
                        if (!$util.isString(message.text))
                            return "text: string expected";
                    }
                    if (message.format != null && Object.hasOwnProperty.call(message, "format")) {
                        properties._format = 1;
                        if (typeof message.format !== "number" || (message.format | 0) !== message.format)
                            return "format: enum value expected";
                    }
                    return null;
                };

                /**
                 * Creates a Body message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.InteractiveResponseMessage.Body
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.InteractiveResponseMessage.Body} Body
                 */
                Body.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.InteractiveResponseMessage.Body: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.text != null)
                        message.text = String(object.text);
                    switch (object.format) {
                    case "DEFAULT":
                    case 0:
                        message.format = 0;
                        break;
                    case "EXTENSIONS_1":
                    case 1:
                        message.format = 1;
                        break;
                    default:
                        if (typeof object.format === "number" && (object.format | 0) === object.format)
                            message.format = object.format;
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a Body message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.InteractiveResponseMessage.Body
                 * @static
                 * @param {proto.Message.InteractiveResponseMessage.Body} message Body
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                Body.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                        object.text = message.text;
                    if (message.format != null && Object.hasOwnProperty.call(message, "format"))
                        object.format = options.enums === String ? $root.proto.Message.InteractiveResponseMessage.Body.Format[message.format] === undefined ? message.format : $root.proto.Message.InteractiveResponseMessage.Body.Format[message.format] : message.format;
                    return object;
                };

                /**
                 * Converts this Body to JSON.
                 * @function toJSON
                 * @memberof proto.Message.InteractiveResponseMessage.Body
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                Body.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for Body
                 * @function getTypeUrl
                 * @memberof proto.Message.InteractiveResponseMessage.Body
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                Body.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.InteractiveResponseMessage.Body";
                };

                /**
                 * Format enum.
                 * @name proto.Message.InteractiveResponseMessage.Body.Format
                 * @enum {number}
                 * @property {number} DEFAULT=0 DEFAULT value
                 * @property {number} EXTENSIONS_1=1 EXTENSIONS_1 value
                 */
                Body.Format = (function() {
                    var valuesById = {}, values = Object.create(valuesById);
                    values[valuesById[0] = "DEFAULT"] = 0;
                    values[valuesById[1] = "EXTENSIONS_1"] = 1;
                    return values;
                })();

                return Body;
            })();

            InteractiveResponseMessage.NativeFlowResponseMessage = (function() {

                /**
                 * Properties of a NativeFlowResponseMessage.
                 * @memberof proto.Message.InteractiveResponseMessage
                 * @interface INativeFlowResponseMessage
                 * @property {string|null} [name] NativeFlowResponseMessage name
                 * @property {string|null} [paramsJson] NativeFlowResponseMessage paramsJson
                 * @property {number|null} [version] NativeFlowResponseMessage version
                 */

                /**
                 * Constructs a new NativeFlowResponseMessage.
                 * @memberof proto.Message.InteractiveResponseMessage
                 * @classdesc Represents a NativeFlowResponseMessage.
                 * @implements INativeFlowResponseMessage
                 * @constructor
                 * @param {proto.Message.InteractiveResponseMessage.INativeFlowResponseMessage=} [properties] Properties to set
                 */
                function NativeFlowResponseMessage(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * NativeFlowResponseMessage name.
                 * @member {string|null|undefined} name
                 * @memberof proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage
                 * @instance
                 */
                NativeFlowResponseMessage.prototype.name = null;

                /**
                 * NativeFlowResponseMessage paramsJson.
                 * @member {string|null|undefined} paramsJson
                 * @memberof proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage
                 * @instance
                 */
                NativeFlowResponseMessage.prototype.paramsJson = null;

                /**
                 * NativeFlowResponseMessage version.
                 * @member {number|null|undefined} version
                 * @memberof proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage
                 * @instance
                 */
                NativeFlowResponseMessage.prototype.version = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(NativeFlowResponseMessage.prototype, "_name", {
                    get: $util.oneOfGetter($oneOfFields = ["name"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(NativeFlowResponseMessage.prototype, "_paramsJson", {
                    get: $util.oneOfGetter($oneOfFields = ["paramsJson"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(NativeFlowResponseMessage.prototype, "_version", {
                    get: $util.oneOfGetter($oneOfFields = ["version"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new NativeFlowResponseMessage instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage
                 * @static
                 * @param {proto.Message.InteractiveResponseMessage.INativeFlowResponseMessage=} [properties] Properties to set
                 * @returns {proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage} NativeFlowResponseMessage instance
                 */
                NativeFlowResponseMessage.create = function create(properties) {
                    return new NativeFlowResponseMessage(properties);
                };

                /**
                 * Encodes the specified NativeFlowResponseMessage message. Does not implicitly {@link proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage
                 * @static
                 * @param {proto.Message.InteractiveResponseMessage.INativeFlowResponseMessage} message NativeFlowResponseMessage message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                NativeFlowResponseMessage.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                    if (message.paramsJson != null && Object.hasOwnProperty.call(message, "paramsJson"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.paramsJson);
                    if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                        writer.uint32(/* id 3, wireType 0 =*/24).int32(message.version);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified NativeFlowResponseMessage message, length delimited. Does not implicitly {@link proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage
                 * @static
                 * @param {proto.Message.InteractiveResponseMessage.INativeFlowResponseMessage} message NativeFlowResponseMessage message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                NativeFlowResponseMessage.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a NativeFlowResponseMessage message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage} NativeFlowResponseMessage
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                NativeFlowResponseMessage.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.name = reader.stringVerify();
                                message._name = "name";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.paramsJson = reader.stringVerify();
                                message._paramsJson = "paramsJson";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                message.version = reader.int32();
                                message._version = "version";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a NativeFlowResponseMessage message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage} NativeFlowResponseMessage
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                NativeFlowResponseMessage.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a NativeFlowResponseMessage message.
                 * @function verify
                 * @memberof proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                NativeFlowResponseMessage.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.name != null && Object.hasOwnProperty.call(message, "name")) {
                        properties._name = 1;
                        if (!$util.isString(message.name))
                            return "name: string expected";
                    }
                    if (message.paramsJson != null && Object.hasOwnProperty.call(message, "paramsJson")) {
                        properties._paramsJson = 1;
                        if (!$util.isString(message.paramsJson))
                            return "paramsJson: string expected";
                    }
                    if (message.version != null && Object.hasOwnProperty.call(message, "version")) {
                        properties._version = 1;
                        if (!$util.isInteger(message.version))
                            return "version: integer expected";
                    }
                    return null;
                };

                /**
                 * Creates a NativeFlowResponseMessage message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage} NativeFlowResponseMessage
                 */
                NativeFlowResponseMessage.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.name != null)
                        message.name = String(object.name);
                    if (object.paramsJson != null)
                        message.paramsJson = String(object.paramsJson);
                    if (object.version != null)
                        message.version = object.version | 0;
                    return message;
                };

                /**
                 * Creates a plain object from a NativeFlowResponseMessage message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage
                 * @static
                 * @param {proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage} message NativeFlowResponseMessage
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                NativeFlowResponseMessage.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                        object.name = message.name;
                    if (message.paramsJson != null && Object.hasOwnProperty.call(message, "paramsJson"))
                        object.paramsJson = message.paramsJson;
                    if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                        object.version = message.version;
                    return object;
                };

                /**
                 * Converts this NativeFlowResponseMessage to JSON.
                 * @function toJSON
                 * @memberof proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                NativeFlowResponseMessage.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for NativeFlowResponseMessage
                 * @function getTypeUrl
                 * @memberof proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                NativeFlowResponseMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage";
                };

                return NativeFlowResponseMessage;
            })();

            return InteractiveResponseMessage;
        })();

        Message.InvoiceMessage = (function() {

            /**
             * Properties of an InvoiceMessage.
             * @memberof proto.Message
             * @interface IInvoiceMessage
             * @property {string|null} [note] InvoiceMessage note
             * @property {string|null} [token] InvoiceMessage token
             * @property {proto.Message.InvoiceMessage.AttachmentType|null} [attachmentType] InvoiceMessage attachmentType
             * @property {string|null} [attachmentMimetype] InvoiceMessage attachmentMimetype
             * @property {Uint8Array|null} [attachmentMediaKey] InvoiceMessage attachmentMediaKey
             * @property {number|Long|null} [attachmentMediaKeyTimestamp] InvoiceMessage attachmentMediaKeyTimestamp
             * @property {Uint8Array|null} [attachmentFileSha256] InvoiceMessage attachmentFileSha256
             * @property {Uint8Array|null} [attachmentFileEncSha256] InvoiceMessage attachmentFileEncSha256
             * @property {string|null} [attachmentDirectPath] InvoiceMessage attachmentDirectPath
             * @property {Uint8Array|null} [attachmentJpegThumbnail] InvoiceMessage attachmentJpegThumbnail
             */

            /**
             * Constructs a new InvoiceMessage.
             * @memberof proto.Message
             * @classdesc Represents an InvoiceMessage.
             * @implements IInvoiceMessage
             * @constructor
             * @param {proto.Message.IInvoiceMessage=} [properties] Properties to set
             */
            function InvoiceMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * InvoiceMessage note.
             * @member {string|null|undefined} note
             * @memberof proto.Message.InvoiceMessage
             * @instance
             */
            InvoiceMessage.prototype.note = null;

            /**
             * InvoiceMessage token.
             * @member {string|null|undefined} token
             * @memberof proto.Message.InvoiceMessage
             * @instance
             */
            InvoiceMessage.prototype.token = null;

            /**
             * InvoiceMessage attachmentType.
             * @member {proto.Message.InvoiceMessage.AttachmentType|null|undefined} attachmentType
             * @memberof proto.Message.InvoiceMessage
             * @instance
             */
            InvoiceMessage.prototype.attachmentType = null;

            /**
             * InvoiceMessage attachmentMimetype.
             * @member {string|null|undefined} attachmentMimetype
             * @memberof proto.Message.InvoiceMessage
             * @instance
             */
            InvoiceMessage.prototype.attachmentMimetype = null;

            /**
             * InvoiceMessage attachmentMediaKey.
             * @member {Uint8Array|null|undefined} attachmentMediaKey
             * @memberof proto.Message.InvoiceMessage
             * @instance
             */
            InvoiceMessage.prototype.attachmentMediaKey = null;

            /**
             * InvoiceMessage attachmentMediaKeyTimestamp.
             * @member {number|Long|null|undefined} attachmentMediaKeyTimestamp
             * @memberof proto.Message.InvoiceMessage
             * @instance
             */
            InvoiceMessage.prototype.attachmentMediaKeyTimestamp = null;

            /**
             * InvoiceMessage attachmentFileSha256.
             * @member {Uint8Array|null|undefined} attachmentFileSha256
             * @memberof proto.Message.InvoiceMessage
             * @instance
             */
            InvoiceMessage.prototype.attachmentFileSha256 = null;

            /**
             * InvoiceMessage attachmentFileEncSha256.
             * @member {Uint8Array|null|undefined} attachmentFileEncSha256
             * @memberof proto.Message.InvoiceMessage
             * @instance
             */
            InvoiceMessage.prototype.attachmentFileEncSha256 = null;

            /**
             * InvoiceMessage attachmentDirectPath.
             * @member {string|null|undefined} attachmentDirectPath
             * @memberof proto.Message.InvoiceMessage
             * @instance
             */
            InvoiceMessage.prototype.attachmentDirectPath = null;

            /**
             * InvoiceMessage attachmentJpegThumbnail.
             * @member {Uint8Array|null|undefined} attachmentJpegThumbnail
             * @memberof proto.Message.InvoiceMessage
             * @instance
             */
            InvoiceMessage.prototype.attachmentJpegThumbnail = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InvoiceMessage.prototype, "_note", {
                get: $util.oneOfGetter($oneOfFields = ["note"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InvoiceMessage.prototype, "_token", {
                get: $util.oneOfGetter($oneOfFields = ["token"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InvoiceMessage.prototype, "_attachmentType", {
                get: $util.oneOfGetter($oneOfFields = ["attachmentType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InvoiceMessage.prototype, "_attachmentMimetype", {
                get: $util.oneOfGetter($oneOfFields = ["attachmentMimetype"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InvoiceMessage.prototype, "_attachmentMediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["attachmentMediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InvoiceMessage.prototype, "_attachmentMediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["attachmentMediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InvoiceMessage.prototype, "_attachmentFileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["attachmentFileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InvoiceMessage.prototype, "_attachmentFileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["attachmentFileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InvoiceMessage.prototype, "_attachmentDirectPath", {
                get: $util.oneOfGetter($oneOfFields = ["attachmentDirectPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InvoiceMessage.prototype, "_attachmentJpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["attachmentJpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new InvoiceMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.InvoiceMessage
             * @static
             * @param {proto.Message.IInvoiceMessage=} [properties] Properties to set
             * @returns {proto.Message.InvoiceMessage} InvoiceMessage instance
             */
            InvoiceMessage.create = function create(properties) {
                return new InvoiceMessage(properties);
            };

            /**
             * Encodes the specified InvoiceMessage message. Does not implicitly {@link proto.Message.InvoiceMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.InvoiceMessage
             * @static
             * @param {proto.Message.IInvoiceMessage} message InvoiceMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InvoiceMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.note != null && Object.hasOwnProperty.call(message, "note"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.note);
                if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.token);
                if (message.attachmentType != null && Object.hasOwnProperty.call(message, "attachmentType"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.attachmentType);
                if (message.attachmentMimetype != null && Object.hasOwnProperty.call(message, "attachmentMimetype"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.attachmentMimetype);
                if (message.attachmentMediaKey != null && Object.hasOwnProperty.call(message, "attachmentMediaKey"))
                    writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.attachmentMediaKey);
                if (message.attachmentMediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "attachmentMediaKeyTimestamp"))
                    writer.uint32(/* id 6, wireType 0 =*/48).int64(message.attachmentMediaKeyTimestamp);
                if (message.attachmentFileSha256 != null && Object.hasOwnProperty.call(message, "attachmentFileSha256"))
                    writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.attachmentFileSha256);
                if (message.attachmentFileEncSha256 != null && Object.hasOwnProperty.call(message, "attachmentFileEncSha256"))
                    writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.attachmentFileEncSha256);
                if (message.attachmentDirectPath != null && Object.hasOwnProperty.call(message, "attachmentDirectPath"))
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.attachmentDirectPath);
                if (message.attachmentJpegThumbnail != null && Object.hasOwnProperty.call(message, "attachmentJpegThumbnail"))
                    writer.uint32(/* id 10, wireType 2 =*/82).bytes(message.attachmentJpegThumbnail);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified InvoiceMessage message, length delimited. Does not implicitly {@link proto.Message.InvoiceMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.InvoiceMessage
             * @static
             * @param {proto.Message.IInvoiceMessage} message InvoiceMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InvoiceMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an InvoiceMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.InvoiceMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.InvoiceMessage} InvoiceMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InvoiceMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.note = reader.stringVerify();
                            message._note = "note";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.token = reader.stringVerify();
                            message._token = "token";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.attachmentType = reader.int32();
                            message._attachmentType = "attachmentType";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.attachmentMimetype = reader.stringVerify();
                            message._attachmentMimetype = "attachmentMimetype";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.attachmentMediaKey = reader.bytes();
                            message._attachmentMediaKey = "attachmentMediaKey";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.attachmentMediaKeyTimestamp = reader.int64();
                            message._attachmentMediaKeyTimestamp = "attachmentMediaKeyTimestamp";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.attachmentFileSha256 = reader.bytes();
                            message._attachmentFileSha256 = "attachmentFileSha256";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.attachmentFileEncSha256 = reader.bytes();
                            message._attachmentFileEncSha256 = "attachmentFileEncSha256";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            message.attachmentDirectPath = reader.stringVerify();
                            message._attachmentDirectPath = "attachmentDirectPath";
                            continue;
                        }
                    case 10: {
                            if (u !== 2)
                                break;
                            message.attachmentJpegThumbnail = reader.bytes();
                            message._attachmentJpegThumbnail = "attachmentJpegThumbnail";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an InvoiceMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.InvoiceMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.InvoiceMessage} InvoiceMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InvoiceMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an InvoiceMessage message.
             * @function verify
             * @memberof proto.Message.InvoiceMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            InvoiceMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.note != null && Object.hasOwnProperty.call(message, "note")) {
                    properties._note = 1;
                    if (!$util.isString(message.note))
                        return "note: string expected";
                }
                if (message.token != null && Object.hasOwnProperty.call(message, "token")) {
                    properties._token = 1;
                    if (!$util.isString(message.token))
                        return "token: string expected";
                }
                if (message.attachmentType != null && Object.hasOwnProperty.call(message, "attachmentType")) {
                    properties._attachmentType = 1;
                    if (typeof message.attachmentType !== "number" || (message.attachmentType | 0) !== message.attachmentType)
                        return "attachmentType: enum value expected";
                }
                if (message.attachmentMimetype != null && Object.hasOwnProperty.call(message, "attachmentMimetype")) {
                    properties._attachmentMimetype = 1;
                    if (!$util.isString(message.attachmentMimetype))
                        return "attachmentMimetype: string expected";
                }
                if (message.attachmentMediaKey != null && Object.hasOwnProperty.call(message, "attachmentMediaKey")) {
                    properties._attachmentMediaKey = 1;
                    if (!(message.attachmentMediaKey && typeof message.attachmentMediaKey.length === "number" || $util.isString(message.attachmentMediaKey)))
                        return "attachmentMediaKey: buffer expected";
                }
                if (message.attachmentMediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "attachmentMediaKeyTimestamp")) {
                    properties._attachmentMediaKeyTimestamp = 1;
                    if (!$util.isInteger(message.attachmentMediaKeyTimestamp) && !(message.attachmentMediaKeyTimestamp && $util.isInteger(message.attachmentMediaKeyTimestamp.low) && $util.isInteger(message.attachmentMediaKeyTimestamp.high)))
                        return "attachmentMediaKeyTimestamp: integer|Long expected";
                }
                if (message.attachmentFileSha256 != null && Object.hasOwnProperty.call(message, "attachmentFileSha256")) {
                    properties._attachmentFileSha256 = 1;
                    if (!(message.attachmentFileSha256 && typeof message.attachmentFileSha256.length === "number" || $util.isString(message.attachmentFileSha256)))
                        return "attachmentFileSha256: buffer expected";
                }
                if (message.attachmentFileEncSha256 != null && Object.hasOwnProperty.call(message, "attachmentFileEncSha256")) {
                    properties._attachmentFileEncSha256 = 1;
                    if (!(message.attachmentFileEncSha256 && typeof message.attachmentFileEncSha256.length === "number" || $util.isString(message.attachmentFileEncSha256)))
                        return "attachmentFileEncSha256: buffer expected";
                }
                if (message.attachmentDirectPath != null && Object.hasOwnProperty.call(message, "attachmentDirectPath")) {
                    properties._attachmentDirectPath = 1;
                    if (!$util.isString(message.attachmentDirectPath))
                        return "attachmentDirectPath: string expected";
                }
                if (message.attachmentJpegThumbnail != null && Object.hasOwnProperty.call(message, "attachmentJpegThumbnail")) {
                    properties._attachmentJpegThumbnail = 1;
                    if (!(message.attachmentJpegThumbnail && typeof message.attachmentJpegThumbnail.length === "number" || $util.isString(message.attachmentJpegThumbnail)))
                        return "attachmentJpegThumbnail: buffer expected";
                }
                return null;
            };

            /**
             * Creates an InvoiceMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.InvoiceMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.InvoiceMessage} InvoiceMessage
             */
            InvoiceMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.InvoiceMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.note != null)
                    message.note = String(object.note);
                if (object.token != null)
                    message.token = String(object.token);
                switch (object.attachmentType) {
                case "IMAGE":
                case 0:
                    message.attachmentType = 0;
                    break;
                case "PDF":
                case 1:
                    message.attachmentType = 1;
                    break;
                default:
                    if (typeof object.attachmentType === "number" && (object.attachmentType | 0) === object.attachmentType)
                        message.attachmentType = object.attachmentType;
                }
                if (object.attachmentMimetype != null)
                    message.attachmentMimetype = String(object.attachmentMimetype);
                if (object.attachmentMediaKey != null)
                    if (typeof object.attachmentMediaKey === "string")
                        $util.base64.decode(object.attachmentMediaKey, message.attachmentMediaKey = $util.newBuffer($util.base64.length(object.attachmentMediaKey)), 0);
                    else if (object.attachmentMediaKey.length >= 0)
                        message.attachmentMediaKey = object.attachmentMediaKey;
                if (object.attachmentMediaKeyTimestamp != null)
                    if ($util.Long)
                        message.attachmentMediaKeyTimestamp = $util.Long.fromValue(object.attachmentMediaKeyTimestamp, false);
                    else if (typeof object.attachmentMediaKeyTimestamp === "string")
                        message.attachmentMediaKeyTimestamp = parseInt(object.attachmentMediaKeyTimestamp, 10);
                    else if (typeof object.attachmentMediaKeyTimestamp === "number")
                        message.attachmentMediaKeyTimestamp = object.attachmentMediaKeyTimestamp;
                    else if (typeof object.attachmentMediaKeyTimestamp === "object")
                        message.attachmentMediaKeyTimestamp = new $util.LongBits(object.attachmentMediaKeyTimestamp.low >>> 0, object.attachmentMediaKeyTimestamp.high >>> 0).toNumber();
                if (object.attachmentFileSha256 != null)
                    if (typeof object.attachmentFileSha256 === "string")
                        $util.base64.decode(object.attachmentFileSha256, message.attachmentFileSha256 = $util.newBuffer($util.base64.length(object.attachmentFileSha256)), 0);
                    else if (object.attachmentFileSha256.length >= 0)
                        message.attachmentFileSha256 = object.attachmentFileSha256;
                if (object.attachmentFileEncSha256 != null)
                    if (typeof object.attachmentFileEncSha256 === "string")
                        $util.base64.decode(object.attachmentFileEncSha256, message.attachmentFileEncSha256 = $util.newBuffer($util.base64.length(object.attachmentFileEncSha256)), 0);
                    else if (object.attachmentFileEncSha256.length >= 0)
                        message.attachmentFileEncSha256 = object.attachmentFileEncSha256;
                if (object.attachmentDirectPath != null)
                    message.attachmentDirectPath = String(object.attachmentDirectPath);
                if (object.attachmentJpegThumbnail != null)
                    if (typeof object.attachmentJpegThumbnail === "string")
                        $util.base64.decode(object.attachmentJpegThumbnail, message.attachmentJpegThumbnail = $util.newBuffer($util.base64.length(object.attachmentJpegThumbnail)), 0);
                    else if (object.attachmentJpegThumbnail.length >= 0)
                        message.attachmentJpegThumbnail = object.attachmentJpegThumbnail;
                return message;
            };

            /**
             * Creates a plain object from an InvoiceMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.InvoiceMessage
             * @static
             * @param {proto.Message.InvoiceMessage} message InvoiceMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            InvoiceMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.note != null && Object.hasOwnProperty.call(message, "note"))
                    object.note = message.note;
                if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                    object.token = message.token;
                if (message.attachmentType != null && Object.hasOwnProperty.call(message, "attachmentType"))
                    object.attachmentType = options.enums === String ? $root.proto.Message.InvoiceMessage.AttachmentType[message.attachmentType] === undefined ? message.attachmentType : $root.proto.Message.InvoiceMessage.AttachmentType[message.attachmentType] : message.attachmentType;
                if (message.attachmentMimetype != null && Object.hasOwnProperty.call(message, "attachmentMimetype"))
                    object.attachmentMimetype = message.attachmentMimetype;
                if (message.attachmentMediaKey != null && Object.hasOwnProperty.call(message, "attachmentMediaKey"))
                    object.attachmentMediaKey = options.bytes === String ? $util.base64.encode(message.attachmentMediaKey, 0, message.attachmentMediaKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.attachmentMediaKey) : message.attachmentMediaKey;
                if (message.attachmentMediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "attachmentMediaKeyTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.attachmentMediaKeyTimestamp = typeof message.attachmentMediaKeyTimestamp === "number" ? BigInt(message.attachmentMediaKeyTimestamp) : $util.Long.fromBits(message.attachmentMediaKeyTimestamp.low >>> 0, message.attachmentMediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.attachmentMediaKeyTimestamp === "number")
                        object.attachmentMediaKeyTimestamp = options.longs === String ? String(message.attachmentMediaKeyTimestamp) : message.attachmentMediaKeyTimestamp;
                    else
                        object.attachmentMediaKeyTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.attachmentMediaKeyTimestamp) : options.longs === Number ? new $util.LongBits(message.attachmentMediaKeyTimestamp.low >>> 0, message.attachmentMediaKeyTimestamp.high >>> 0).toNumber() : message.attachmentMediaKeyTimestamp;
                if (message.attachmentFileSha256 != null && Object.hasOwnProperty.call(message, "attachmentFileSha256"))
                    object.attachmentFileSha256 = options.bytes === String ? $util.base64.encode(message.attachmentFileSha256, 0, message.attachmentFileSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.attachmentFileSha256) : message.attachmentFileSha256;
                if (message.attachmentFileEncSha256 != null && Object.hasOwnProperty.call(message, "attachmentFileEncSha256"))
                    object.attachmentFileEncSha256 = options.bytes === String ? $util.base64.encode(message.attachmentFileEncSha256, 0, message.attachmentFileEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.attachmentFileEncSha256) : message.attachmentFileEncSha256;
                if (message.attachmentDirectPath != null && Object.hasOwnProperty.call(message, "attachmentDirectPath"))
                    object.attachmentDirectPath = message.attachmentDirectPath;
                if (message.attachmentJpegThumbnail != null && Object.hasOwnProperty.call(message, "attachmentJpegThumbnail"))
                    object.attachmentJpegThumbnail = options.bytes === String ? $util.base64.encode(message.attachmentJpegThumbnail, 0, message.attachmentJpegThumbnail.length) : options.bytes === Array ? Array.prototype.slice.call(message.attachmentJpegThumbnail) : message.attachmentJpegThumbnail;
                return object;
            };

            /**
             * Converts this InvoiceMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.InvoiceMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            InvoiceMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for InvoiceMessage
             * @function getTypeUrl
             * @memberof proto.Message.InvoiceMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            InvoiceMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.InvoiceMessage";
            };

            /**
             * AttachmentType enum.
             * @name proto.Message.InvoiceMessage.AttachmentType
             * @enum {number}
             * @property {number} IMAGE=0 IMAGE value
             * @property {number} PDF=1 PDF value
             */
            InvoiceMessage.AttachmentType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "IMAGE"] = 0;
                values[valuesById[1] = "PDF"] = 1;
                return values;
            })();

            return InvoiceMessage;
        })();

        Message.KeepInChatMessage = (function() {

            /**
             * Properties of a KeepInChatMessage.
             * @memberof proto.Message
             * @interface IKeepInChatMessage
             * @property {proto.IMessageKey|null} [key] KeepInChatMessage key
             * @property {proto.KeepType|null} [keepType] KeepInChatMessage keepType
             * @property {number|Long|null} [timestampMs] KeepInChatMessage timestampMs
             */

            /**
             * Constructs a new KeepInChatMessage.
             * @memberof proto.Message
             * @classdesc Represents a KeepInChatMessage.
             * @implements IKeepInChatMessage
             * @constructor
             * @param {proto.Message.IKeepInChatMessage=} [properties] Properties to set
             */
            function KeepInChatMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * KeepInChatMessage key.
             * @member {proto.IMessageKey|null|undefined} key
             * @memberof proto.Message.KeepInChatMessage
             * @instance
             */
            KeepInChatMessage.prototype.key = null;

            /**
             * KeepInChatMessage keepType.
             * @member {proto.KeepType|null|undefined} keepType
             * @memberof proto.Message.KeepInChatMessage
             * @instance
             */
            KeepInChatMessage.prototype.keepType = null;

            /**
             * KeepInChatMessage timestampMs.
             * @member {number|Long|null|undefined} timestampMs
             * @memberof proto.Message.KeepInChatMessage
             * @instance
             */
            KeepInChatMessage.prototype.timestampMs = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(KeepInChatMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(KeepInChatMessage.prototype, "_keepType", {
                get: $util.oneOfGetter($oneOfFields = ["keepType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(KeepInChatMessage.prototype, "_timestampMs", {
                get: $util.oneOfGetter($oneOfFields = ["timestampMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new KeepInChatMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.KeepInChatMessage
             * @static
             * @param {proto.Message.IKeepInChatMessage=} [properties] Properties to set
             * @returns {proto.Message.KeepInChatMessage} KeepInChatMessage instance
             */
            KeepInChatMessage.create = function create(properties) {
                return new KeepInChatMessage(properties);
            };

            /**
             * Encodes the specified KeepInChatMessage message. Does not implicitly {@link proto.Message.KeepInChatMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.KeepInChatMessage
             * @static
             * @param {proto.Message.IKeepInChatMessage} message KeepInChatMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            KeepInChatMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    $root.proto.MessageKey.encode(message.key, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.keepType != null && Object.hasOwnProperty.call(message, "keepType"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.keepType);
                if (message.timestampMs != null && Object.hasOwnProperty.call(message, "timestampMs"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.timestampMs);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified KeepInChatMessage message, length delimited. Does not implicitly {@link proto.Message.KeepInChatMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.KeepInChatMessage
             * @static
             * @param {proto.Message.IKeepInChatMessage} message KeepInChatMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            KeepInChatMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a KeepInChatMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.KeepInChatMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.KeepInChatMessage} KeepInChatMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            KeepInChatMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.key = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.key);
                            message._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.keepType = reader.int32();
                            message._keepType = "keepType";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.timestampMs = reader.int64();
                            message._timestampMs = "timestampMs";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a KeepInChatMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.KeepInChatMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.KeepInChatMessage} KeepInChatMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            KeepInChatMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a KeepInChatMessage message.
             * @function verify
             * @memberof proto.Message.KeepInChatMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            KeepInChatMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                    properties._key = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.key, q + 1);
                        if (error)
                            return "key." + error;
                    }
                }
                if (message.keepType != null && Object.hasOwnProperty.call(message, "keepType")) {
                    properties._keepType = 1;
                    if (typeof message.keepType !== "number" || (message.keepType | 0) !== message.keepType)
                        return "keepType: enum value expected";
                }
                if (message.timestampMs != null && Object.hasOwnProperty.call(message, "timestampMs")) {
                    properties._timestampMs = 1;
                    if (!$util.isInteger(message.timestampMs) && !(message.timestampMs && $util.isInteger(message.timestampMs.low) && $util.isInteger(message.timestampMs.high)))
                        return "timestampMs: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a KeepInChatMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.KeepInChatMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.KeepInChatMessage} KeepInChatMessage
             */
            KeepInChatMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.KeepInChatMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.key != null) {
                    if (!$util.isObject(object.key))
                        throw TypeError(".proto.Message.KeepInChatMessage.key: object expected");
                    message.key = $root.proto.MessageKey.fromObject(object.key, q + 1);
                }
                switch (object.keepType) {
                case "UNKNOWN":
                case 0:
                    message.keepType = 0;
                    break;
                case "KEEP_FOR_ALL":
                case 1:
                    message.keepType = 1;
                    break;
                case "UNDO_KEEP_FOR_ALL":
                case 2:
                    message.keepType = 2;
                    break;
                default:
                    if (typeof object.keepType === "number" && (object.keepType | 0) === object.keepType)
                        message.keepType = object.keepType;
                }
                if (object.timestampMs != null)
                    if ($util.Long)
                        message.timestampMs = $util.Long.fromValue(object.timestampMs, false);
                    else if (typeof object.timestampMs === "string")
                        message.timestampMs = parseInt(object.timestampMs, 10);
                    else if (typeof object.timestampMs === "number")
                        message.timestampMs = object.timestampMs;
                    else if (typeof object.timestampMs === "object")
                        message.timestampMs = new $util.LongBits(object.timestampMs.low >>> 0, object.timestampMs.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a KeepInChatMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.KeepInChatMessage
             * @static
             * @param {proto.Message.KeepInChatMessage} message KeepInChatMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            KeepInChatMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    object.key = $root.proto.MessageKey.toObject(message.key, options, q + 1);
                if (message.keepType != null && Object.hasOwnProperty.call(message, "keepType"))
                    object.keepType = options.enums === String ? $root.proto.KeepType[message.keepType] === undefined ? message.keepType : $root.proto.KeepType[message.keepType] : message.keepType;
                if (message.timestampMs != null && Object.hasOwnProperty.call(message, "timestampMs"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.timestampMs = typeof message.timestampMs === "number" ? BigInt(message.timestampMs) : $util.Long.fromBits(message.timestampMs.low >>> 0, message.timestampMs.high >>> 0, false).toBigInt();
                    else if (typeof message.timestampMs === "number")
                        object.timestampMs = options.longs === String ? String(message.timestampMs) : message.timestampMs;
                    else
                        object.timestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.timestampMs) : options.longs === Number ? new $util.LongBits(message.timestampMs.low >>> 0, message.timestampMs.high >>> 0).toNumber() : message.timestampMs;
                return object;
            };

            /**
             * Converts this KeepInChatMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.KeepInChatMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            KeepInChatMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for KeepInChatMessage
             * @function getTypeUrl
             * @memberof proto.Message.KeepInChatMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            KeepInChatMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.KeepInChatMessage";
            };

            return KeepInChatMessage;
        })();

        Message.LinkPreviewMetadata = (function() {

            /**
             * Properties of a LinkPreviewMetadata.
             * @memberof proto.Message
             * @interface ILinkPreviewMetadata
             * @property {proto.Message.IPaymentLinkMetadata|null} [paymentLinkMetadata] LinkPreviewMetadata paymentLinkMetadata
             * @property {proto.Message.IURLMetadata|null} [urlMetadata] LinkPreviewMetadata urlMetadata
             * @property {number|null} [fbExperimentId] LinkPreviewMetadata fbExperimentId
             * @property {number|null} [linkMediaDuration] LinkPreviewMetadata linkMediaDuration
             * @property {proto.Message.LinkPreviewMetadata.SocialMediaPostType|null} [socialMediaPostType] LinkPreviewMetadata socialMediaPostType
             * @property {boolean|null} [linkInlineVideoMuted] LinkPreviewMetadata linkInlineVideoMuted
             * @property {string|null} [videoContentUrl] LinkPreviewMetadata videoContentUrl
             * @property {proto.IEmbeddedMusic|null} [musicMetadata] LinkPreviewMetadata musicMetadata
             * @property {string|null} [videoContentCaption] LinkPreviewMetadata videoContentCaption
             */

            /**
             * Constructs a new LinkPreviewMetadata.
             * @memberof proto.Message
             * @classdesc Represents a LinkPreviewMetadata.
             * @implements ILinkPreviewMetadata
             * @constructor
             * @param {proto.Message.ILinkPreviewMetadata=} [properties] Properties to set
             */
            function LinkPreviewMetadata(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * LinkPreviewMetadata paymentLinkMetadata.
             * @member {proto.Message.IPaymentLinkMetadata|null|undefined} paymentLinkMetadata
             * @memberof proto.Message.LinkPreviewMetadata
             * @instance
             */
            LinkPreviewMetadata.prototype.paymentLinkMetadata = null;

            /**
             * LinkPreviewMetadata urlMetadata.
             * @member {proto.Message.IURLMetadata|null|undefined} urlMetadata
             * @memberof proto.Message.LinkPreviewMetadata
             * @instance
             */
            LinkPreviewMetadata.prototype.urlMetadata = null;

            /**
             * LinkPreviewMetadata fbExperimentId.
             * @member {number|null|undefined} fbExperimentId
             * @memberof proto.Message.LinkPreviewMetadata
             * @instance
             */
            LinkPreviewMetadata.prototype.fbExperimentId = null;

            /**
             * LinkPreviewMetadata linkMediaDuration.
             * @member {number|null|undefined} linkMediaDuration
             * @memberof proto.Message.LinkPreviewMetadata
             * @instance
             */
            LinkPreviewMetadata.prototype.linkMediaDuration = null;

            /**
             * LinkPreviewMetadata socialMediaPostType.
             * @member {proto.Message.LinkPreviewMetadata.SocialMediaPostType|null|undefined} socialMediaPostType
             * @memberof proto.Message.LinkPreviewMetadata
             * @instance
             */
            LinkPreviewMetadata.prototype.socialMediaPostType = null;

            /**
             * LinkPreviewMetadata linkInlineVideoMuted.
             * @member {boolean|null|undefined} linkInlineVideoMuted
             * @memberof proto.Message.LinkPreviewMetadata
             * @instance
             */
            LinkPreviewMetadata.prototype.linkInlineVideoMuted = null;

            /**
             * LinkPreviewMetadata videoContentUrl.
             * @member {string|null|undefined} videoContentUrl
             * @memberof proto.Message.LinkPreviewMetadata
             * @instance
             */
            LinkPreviewMetadata.prototype.videoContentUrl = null;

            /**
             * LinkPreviewMetadata musicMetadata.
             * @member {proto.IEmbeddedMusic|null|undefined} musicMetadata
             * @memberof proto.Message.LinkPreviewMetadata
             * @instance
             */
            LinkPreviewMetadata.prototype.musicMetadata = null;

            /**
             * LinkPreviewMetadata videoContentCaption.
             * @member {string|null|undefined} videoContentCaption
             * @memberof proto.Message.LinkPreviewMetadata
             * @instance
             */
            LinkPreviewMetadata.prototype.videoContentCaption = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LinkPreviewMetadata.prototype, "_paymentLinkMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["paymentLinkMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LinkPreviewMetadata.prototype, "_urlMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["urlMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LinkPreviewMetadata.prototype, "_fbExperimentId", {
                get: $util.oneOfGetter($oneOfFields = ["fbExperimentId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LinkPreviewMetadata.prototype, "_linkMediaDuration", {
                get: $util.oneOfGetter($oneOfFields = ["linkMediaDuration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LinkPreviewMetadata.prototype, "_socialMediaPostType", {
                get: $util.oneOfGetter($oneOfFields = ["socialMediaPostType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LinkPreviewMetadata.prototype, "_linkInlineVideoMuted", {
                get: $util.oneOfGetter($oneOfFields = ["linkInlineVideoMuted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LinkPreviewMetadata.prototype, "_videoContentUrl", {
                get: $util.oneOfGetter($oneOfFields = ["videoContentUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LinkPreviewMetadata.prototype, "_musicMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["musicMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LinkPreviewMetadata.prototype, "_videoContentCaption", {
                get: $util.oneOfGetter($oneOfFields = ["videoContentCaption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new LinkPreviewMetadata instance using the specified properties.
             * @function create
             * @memberof proto.Message.LinkPreviewMetadata
             * @static
             * @param {proto.Message.ILinkPreviewMetadata=} [properties] Properties to set
             * @returns {proto.Message.LinkPreviewMetadata} LinkPreviewMetadata instance
             */
            LinkPreviewMetadata.create = function create(properties) {
                return new LinkPreviewMetadata(properties);
            };

            /**
             * Encodes the specified LinkPreviewMetadata message. Does not implicitly {@link proto.Message.LinkPreviewMetadata.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.LinkPreviewMetadata
             * @static
             * @param {proto.Message.ILinkPreviewMetadata} message LinkPreviewMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LinkPreviewMetadata.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.paymentLinkMetadata != null && Object.hasOwnProperty.call(message, "paymentLinkMetadata"))
                    $root.proto.Message.PaymentLinkMetadata.encode(message.paymentLinkMetadata, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.urlMetadata != null && Object.hasOwnProperty.call(message, "urlMetadata"))
                    $root.proto.Message.URLMetadata.encode(message.urlMetadata, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.fbExperimentId != null && Object.hasOwnProperty.call(message, "fbExperimentId"))
                    writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.fbExperimentId);
                if (message.linkMediaDuration != null && Object.hasOwnProperty.call(message, "linkMediaDuration"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.linkMediaDuration);
                if (message.socialMediaPostType != null && Object.hasOwnProperty.call(message, "socialMediaPostType"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int32(message.socialMediaPostType);
                if (message.linkInlineVideoMuted != null && Object.hasOwnProperty.call(message, "linkInlineVideoMuted"))
                    writer.uint32(/* id 6, wireType 0 =*/48).bool(message.linkInlineVideoMuted);
                if (message.videoContentUrl != null && Object.hasOwnProperty.call(message, "videoContentUrl"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.videoContentUrl);
                if (message.musicMetadata != null && Object.hasOwnProperty.call(message, "musicMetadata"))
                    $root.proto.EmbeddedMusic.encode(message.musicMetadata, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
                if (message.videoContentCaption != null && Object.hasOwnProperty.call(message, "videoContentCaption"))
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.videoContentCaption);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LinkPreviewMetadata message, length delimited. Does not implicitly {@link proto.Message.LinkPreviewMetadata.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.LinkPreviewMetadata
             * @static
             * @param {proto.Message.ILinkPreviewMetadata} message LinkPreviewMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LinkPreviewMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a LinkPreviewMetadata message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.LinkPreviewMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.LinkPreviewMetadata} LinkPreviewMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LinkPreviewMetadata.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.paymentLinkMetadata = $root.proto.Message.PaymentLinkMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.paymentLinkMetadata);
                            message._paymentLinkMetadata = "paymentLinkMetadata";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.urlMetadata = $root.proto.Message.URLMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.urlMetadata);
                            message._urlMetadata = "urlMetadata";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.fbExperimentId = reader.uint32();
                            message._fbExperimentId = "fbExperimentId";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.linkMediaDuration = reader.uint32();
                            message._linkMediaDuration = "linkMediaDuration";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.socialMediaPostType = reader.int32();
                            message._socialMediaPostType = "socialMediaPostType";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.linkInlineVideoMuted = reader.bool();
                            message._linkInlineVideoMuted = "linkInlineVideoMuted";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.videoContentUrl = reader.stringVerify();
                            message._videoContentUrl = "videoContentUrl";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.musicMetadata = $root.proto.EmbeddedMusic.decode(reader, reader.uint32(), undefined, q + 1, message.musicMetadata);
                            message._musicMetadata = "musicMetadata";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            message.videoContentCaption = reader.stringVerify();
                            message._videoContentCaption = "videoContentCaption";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a LinkPreviewMetadata message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.LinkPreviewMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.LinkPreviewMetadata} LinkPreviewMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LinkPreviewMetadata.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LinkPreviewMetadata message.
             * @function verify
             * @memberof proto.Message.LinkPreviewMetadata
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LinkPreviewMetadata.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.paymentLinkMetadata != null && Object.hasOwnProperty.call(message, "paymentLinkMetadata")) {
                    properties._paymentLinkMetadata = 1;
                    {
                        var error = $root.proto.Message.PaymentLinkMetadata.verify(message.paymentLinkMetadata, q + 1);
                        if (error)
                            return "paymentLinkMetadata." + error;
                    }
                }
                if (message.urlMetadata != null && Object.hasOwnProperty.call(message, "urlMetadata")) {
                    properties._urlMetadata = 1;
                    {
                        var error = $root.proto.Message.URLMetadata.verify(message.urlMetadata, q + 1);
                        if (error)
                            return "urlMetadata." + error;
                    }
                }
                if (message.fbExperimentId != null && Object.hasOwnProperty.call(message, "fbExperimentId")) {
                    properties._fbExperimentId = 1;
                    if (!$util.isInteger(message.fbExperimentId))
                        return "fbExperimentId: integer expected";
                }
                if (message.linkMediaDuration != null && Object.hasOwnProperty.call(message, "linkMediaDuration")) {
                    properties._linkMediaDuration = 1;
                    if (!$util.isInteger(message.linkMediaDuration))
                        return "linkMediaDuration: integer expected";
                }
                if (message.socialMediaPostType != null && Object.hasOwnProperty.call(message, "socialMediaPostType")) {
                    properties._socialMediaPostType = 1;
                    if (typeof message.socialMediaPostType !== "number" || (message.socialMediaPostType | 0) !== message.socialMediaPostType)
                        return "socialMediaPostType: enum value expected";
                }
                if (message.linkInlineVideoMuted != null && Object.hasOwnProperty.call(message, "linkInlineVideoMuted")) {
                    properties._linkInlineVideoMuted = 1;
                    if (typeof message.linkInlineVideoMuted !== "boolean")
                        return "linkInlineVideoMuted: boolean expected";
                }
                if (message.videoContentUrl != null && Object.hasOwnProperty.call(message, "videoContentUrl")) {
                    properties._videoContentUrl = 1;
                    if (!$util.isString(message.videoContentUrl))
                        return "videoContentUrl: string expected";
                }
                if (message.musicMetadata != null && Object.hasOwnProperty.call(message, "musicMetadata")) {
                    properties._musicMetadata = 1;
                    {
                        var error = $root.proto.EmbeddedMusic.verify(message.musicMetadata, q + 1);
                        if (error)
                            return "musicMetadata." + error;
                    }
                }
                if (message.videoContentCaption != null && Object.hasOwnProperty.call(message, "videoContentCaption")) {
                    properties._videoContentCaption = 1;
                    if (!$util.isString(message.videoContentCaption))
                        return "videoContentCaption: string expected";
                }
                return null;
            };

            /**
             * Creates a LinkPreviewMetadata message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.LinkPreviewMetadata
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.LinkPreviewMetadata} LinkPreviewMetadata
             */
            LinkPreviewMetadata.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.LinkPreviewMetadata: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.paymentLinkMetadata != null) {
                    if (!$util.isObject(object.paymentLinkMetadata))
                        throw TypeError(".proto.Message.LinkPreviewMetadata.paymentLinkMetadata: object expected");
                    message.paymentLinkMetadata = $root.proto.Message.PaymentLinkMetadata.fromObject(object.paymentLinkMetadata, q + 1);
                }
                if (object.urlMetadata != null) {
                    if (!$util.isObject(object.urlMetadata))
                        throw TypeError(".proto.Message.LinkPreviewMetadata.urlMetadata: object expected");
                    message.urlMetadata = $root.proto.Message.URLMetadata.fromObject(object.urlMetadata, q + 1);
                }
                if (object.fbExperimentId != null)
                    message.fbExperimentId = object.fbExperimentId >>> 0;
                if (object.linkMediaDuration != null)
                    message.linkMediaDuration = object.linkMediaDuration >>> 0;
                switch (object.socialMediaPostType) {
                case "NONE":
                case 0:
                    message.socialMediaPostType = 0;
                    break;
                case "REEL":
                case 1:
                    message.socialMediaPostType = 1;
                    break;
                case "LIVE_VIDEO":
                case 2:
                    message.socialMediaPostType = 2;
                    break;
                case "LONG_VIDEO":
                case 3:
                    message.socialMediaPostType = 3;
                    break;
                case "SINGLE_IMAGE":
                case 4:
                    message.socialMediaPostType = 4;
                    break;
                case "CAROUSEL":
                case 5:
                    message.socialMediaPostType = 5;
                    break;
                default:
                    if (typeof object.socialMediaPostType === "number" && (object.socialMediaPostType | 0) === object.socialMediaPostType)
                        message.socialMediaPostType = object.socialMediaPostType;
                }
                if (object.linkInlineVideoMuted != null)
                    message.linkInlineVideoMuted = Boolean(object.linkInlineVideoMuted);
                if (object.videoContentUrl != null)
                    message.videoContentUrl = String(object.videoContentUrl);
                if (object.musicMetadata != null) {
                    if (!$util.isObject(object.musicMetadata))
                        throw TypeError(".proto.Message.LinkPreviewMetadata.musicMetadata: object expected");
                    message.musicMetadata = $root.proto.EmbeddedMusic.fromObject(object.musicMetadata, q + 1);
                }
                if (object.videoContentCaption != null)
                    message.videoContentCaption = String(object.videoContentCaption);
                return message;
            };

            /**
             * Creates a plain object from a LinkPreviewMetadata message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.LinkPreviewMetadata
             * @static
             * @param {proto.Message.LinkPreviewMetadata} message LinkPreviewMetadata
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LinkPreviewMetadata.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.paymentLinkMetadata != null && Object.hasOwnProperty.call(message, "paymentLinkMetadata"))
                    object.paymentLinkMetadata = $root.proto.Message.PaymentLinkMetadata.toObject(message.paymentLinkMetadata, options, q + 1);
                if (message.urlMetadata != null && Object.hasOwnProperty.call(message, "urlMetadata"))
                    object.urlMetadata = $root.proto.Message.URLMetadata.toObject(message.urlMetadata, options, q + 1);
                if (message.fbExperimentId != null && Object.hasOwnProperty.call(message, "fbExperimentId"))
                    object.fbExperimentId = message.fbExperimentId;
                if (message.linkMediaDuration != null && Object.hasOwnProperty.call(message, "linkMediaDuration"))
                    object.linkMediaDuration = message.linkMediaDuration;
                if (message.socialMediaPostType != null && Object.hasOwnProperty.call(message, "socialMediaPostType"))
                    object.socialMediaPostType = options.enums === String ? $root.proto.Message.LinkPreviewMetadata.SocialMediaPostType[message.socialMediaPostType] === undefined ? message.socialMediaPostType : $root.proto.Message.LinkPreviewMetadata.SocialMediaPostType[message.socialMediaPostType] : message.socialMediaPostType;
                if (message.linkInlineVideoMuted != null && Object.hasOwnProperty.call(message, "linkInlineVideoMuted"))
                    object.linkInlineVideoMuted = message.linkInlineVideoMuted;
                if (message.videoContentUrl != null && Object.hasOwnProperty.call(message, "videoContentUrl"))
                    object.videoContentUrl = message.videoContentUrl;
                if (message.musicMetadata != null && Object.hasOwnProperty.call(message, "musicMetadata"))
                    object.musicMetadata = $root.proto.EmbeddedMusic.toObject(message.musicMetadata, options, q + 1);
                if (message.videoContentCaption != null && Object.hasOwnProperty.call(message, "videoContentCaption"))
                    object.videoContentCaption = message.videoContentCaption;
                return object;
            };

            /**
             * Converts this LinkPreviewMetadata to JSON.
             * @function toJSON
             * @memberof proto.Message.LinkPreviewMetadata
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LinkPreviewMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for LinkPreviewMetadata
             * @function getTypeUrl
             * @memberof proto.Message.LinkPreviewMetadata
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            LinkPreviewMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.LinkPreviewMetadata";
            };

            /**
             * SocialMediaPostType enum.
             * @name proto.Message.LinkPreviewMetadata.SocialMediaPostType
             * @enum {number}
             * @property {number} NONE=0 NONE value
             * @property {number} REEL=1 REEL value
             * @property {number} LIVE_VIDEO=2 LIVE_VIDEO value
             * @property {number} LONG_VIDEO=3 LONG_VIDEO value
             * @property {number} SINGLE_IMAGE=4 SINGLE_IMAGE value
             * @property {number} CAROUSEL=5 CAROUSEL value
             */
            LinkPreviewMetadata.SocialMediaPostType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "NONE"] = 0;
                values[valuesById[1] = "REEL"] = 1;
                values[valuesById[2] = "LIVE_VIDEO"] = 2;
                values[valuesById[3] = "LONG_VIDEO"] = 3;
                values[valuesById[4] = "SINGLE_IMAGE"] = 4;
                values[valuesById[5] = "CAROUSEL"] = 5;
                return values;
            })();

            return LinkPreviewMetadata;
        })();

        Message.ListMessage = (function() {

            /**
             * Properties of a ListMessage.
             * @memberof proto.Message
             * @interface IListMessage
             * @property {string|null} [title] ListMessage title
             * @property {string|null} [description] ListMessage description
             * @property {string|null} [buttonText] ListMessage buttonText
             * @property {proto.Message.ListMessage.ListType|null} [listType] ListMessage listType
             * @property {Array.<proto.Message.ListMessage.ISection>|null} [sections] ListMessage sections
             * @property {proto.Message.ListMessage.IProductListInfo|null} [productListInfo] ListMessage productListInfo
             * @property {string|null} [footerText] ListMessage footerText
             * @property {proto.IContextInfo|null} [contextInfo] ListMessage contextInfo
             */

            /**
             * Constructs a new ListMessage.
             * @memberof proto.Message
             * @classdesc Represents a ListMessage.
             * @implements IListMessage
             * @constructor
             * @param {proto.Message.IListMessage=} [properties] Properties to set
             */
            function ListMessage(properties) {
                this.sections = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ListMessage title.
             * @member {string|null|undefined} title
             * @memberof proto.Message.ListMessage
             * @instance
             */
            ListMessage.prototype.title = null;

            /**
             * ListMessage description.
             * @member {string|null|undefined} description
             * @memberof proto.Message.ListMessage
             * @instance
             */
            ListMessage.prototype.description = null;

            /**
             * ListMessage buttonText.
             * @member {string|null|undefined} buttonText
             * @memberof proto.Message.ListMessage
             * @instance
             */
            ListMessage.prototype.buttonText = null;

            /**
             * ListMessage listType.
             * @member {proto.Message.ListMessage.ListType|null|undefined} listType
             * @memberof proto.Message.ListMessage
             * @instance
             */
            ListMessage.prototype.listType = null;

            /**
             * ListMessage sections.
             * @member {Array.<proto.Message.ListMessage.ISection>} sections
             * @memberof proto.Message.ListMessage
             * @instance
             */
            ListMessage.prototype.sections = $util.emptyArray;

            /**
             * ListMessage productListInfo.
             * @member {proto.Message.ListMessage.IProductListInfo|null|undefined} productListInfo
             * @memberof proto.Message.ListMessage
             * @instance
             */
            ListMessage.prototype.productListInfo = null;

            /**
             * ListMessage footerText.
             * @member {string|null|undefined} footerText
             * @memberof proto.Message.ListMessage
             * @instance
             */
            ListMessage.prototype.footerText = null;

            /**
             * ListMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.ListMessage
             * @instance
             */
            ListMessage.prototype.contextInfo = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ListMessage.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ListMessage.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ListMessage.prototype, "_buttonText", {
                get: $util.oneOfGetter($oneOfFields = ["buttonText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ListMessage.prototype, "_listType", {
                get: $util.oneOfGetter($oneOfFields = ["listType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ListMessage.prototype, "_productListInfo", {
                get: $util.oneOfGetter($oneOfFields = ["productListInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ListMessage.prototype, "_footerText", {
                get: $util.oneOfGetter($oneOfFields = ["footerText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ListMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ListMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.ListMessage
             * @static
             * @param {proto.Message.IListMessage=} [properties] Properties to set
             * @returns {proto.Message.ListMessage} ListMessage instance
             */
            ListMessage.create = function create(properties) {
                return new ListMessage(properties);
            };

            /**
             * Encodes the specified ListMessage message. Does not implicitly {@link proto.Message.ListMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.ListMessage
             * @static
             * @param {proto.Message.IListMessage} message ListMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ListMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.title);
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.description);
                if (message.buttonText != null && Object.hasOwnProperty.call(message, "buttonText"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.buttonText);
                if (message.listType != null && Object.hasOwnProperty.call(message, "listType"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int32(message.listType);
                if (message.sections != null && message.sections.length)
                    for (var i = 0; i < message.sections.length; ++i)
                        $root.proto.Message.ListMessage.Section.encode(message.sections[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                if (message.productListInfo != null && Object.hasOwnProperty.call(message, "productListInfo"))
                    $root.proto.Message.ListMessage.ProductListInfo.encode(message.productListInfo, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
                if (message.footerText != null && Object.hasOwnProperty.call(message, "footerText"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.footerText);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ListMessage message, length delimited. Does not implicitly {@link proto.Message.ListMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.ListMessage
             * @static
             * @param {proto.Message.IListMessage} message ListMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ListMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ListMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.ListMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.ListMessage} ListMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ListMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.title = reader.stringVerify();
                            message._title = "title";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.description = reader.stringVerify();
                            message._description = "description";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.buttonText = reader.stringVerify();
                            message._buttonText = "buttonText";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.listType = reader.int32();
                            message._listType = "listType";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            if (!(message.sections && message.sections.length))
                                message.sections = [];
                            message.sections.push($root.proto.Message.ListMessage.Section.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.productListInfo = $root.proto.Message.ListMessage.ProductListInfo.decode(reader, reader.uint32(), undefined, q + 1, message.productListInfo);
                            message._productListInfo = "productListInfo";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.footerText = reader.stringVerify();
                            message._footerText = "footerText";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ListMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.ListMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.ListMessage} ListMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ListMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ListMessage message.
             * @function verify
             * @memberof proto.Message.ListMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ListMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                    properties._title = 1;
                    if (!$util.isString(message.title))
                        return "title: string expected";
                }
                if (message.description != null && Object.hasOwnProperty.call(message, "description")) {
                    properties._description = 1;
                    if (!$util.isString(message.description))
                        return "description: string expected";
                }
                if (message.buttonText != null && Object.hasOwnProperty.call(message, "buttonText")) {
                    properties._buttonText = 1;
                    if (!$util.isString(message.buttonText))
                        return "buttonText: string expected";
                }
                if (message.listType != null && Object.hasOwnProperty.call(message, "listType")) {
                    properties._listType = 1;
                    if (typeof message.listType !== "number" || (message.listType | 0) !== message.listType)
                        return "listType: enum value expected";
                }
                if (message.sections != null && Object.hasOwnProperty.call(message, "sections")) {
                    if (!Array.isArray(message.sections))
                        return "sections: array expected";
                    for (var i = 0; i < message.sections.length; ++i) {
                        var error = $root.proto.Message.ListMessage.Section.verify(message.sections[i], q + 1);
                        if (error)
                            return "sections." + error;
                    }
                }
                if (message.productListInfo != null && Object.hasOwnProperty.call(message, "productListInfo")) {
                    properties._productListInfo = 1;
                    {
                        var error = $root.proto.Message.ListMessage.ProductListInfo.verify(message.productListInfo, q + 1);
                        if (error)
                            return "productListInfo." + error;
                    }
                }
                if (message.footerText != null && Object.hasOwnProperty.call(message, "footerText")) {
                    properties._footerText = 1;
                    if (!$util.isString(message.footerText))
                        return "footerText: string expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a ListMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.ListMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.ListMessage} ListMessage
             */
            ListMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.ListMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.title != null)
                    message.title = String(object.title);
                if (object.description != null)
                    message.description = String(object.description);
                if (object.buttonText != null)
                    message.buttonText = String(object.buttonText);
                switch (object.listType) {
                case "UNKNOWN":
                case 0:
                    message.listType = 0;
                    break;
                case "SINGLE_SELECT":
                case 1:
                    message.listType = 1;
                    break;
                case "PRODUCT_LIST":
                case 2:
                    message.listType = 2;
                    break;
                default:
                    if (typeof object.listType === "number" && (object.listType | 0) === object.listType)
                        message.listType = object.listType;
                }
                if (object.sections) {
                    if (!Array.isArray(object.sections))
                        throw TypeError(".proto.Message.ListMessage.sections: array expected");
                    message.sections = Array(object.sections.length);
                    for (var i = 0; i < object.sections.length; ++i) {
                        if (!$util.isObject(object.sections[i]))
                            throw TypeError(".proto.Message.ListMessage.sections: object expected");
                        message.sections[i] = $root.proto.Message.ListMessage.Section.fromObject(object.sections[i], q + 1);
                    }
                }
                if (object.productListInfo != null) {
                    if (!$util.isObject(object.productListInfo))
                        throw TypeError(".proto.Message.ListMessage.productListInfo: object expected");
                    message.productListInfo = $root.proto.Message.ListMessage.ProductListInfo.fromObject(object.productListInfo, q + 1);
                }
                if (object.footerText != null)
                    message.footerText = String(object.footerText);
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.ListMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a ListMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.ListMessage
             * @static
             * @param {proto.Message.ListMessage} message ListMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ListMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.sections = [];
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    object.title = message.title;
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    object.description = message.description;
                if (message.buttonText != null && Object.hasOwnProperty.call(message, "buttonText"))
                    object.buttonText = message.buttonText;
                if (message.listType != null && Object.hasOwnProperty.call(message, "listType"))
                    object.listType = options.enums === String ? $root.proto.Message.ListMessage.ListType[message.listType] === undefined ? message.listType : $root.proto.Message.ListMessage.ListType[message.listType] : message.listType;
                if (message.sections && message.sections.length) {
                    object.sections = Array(message.sections.length);
                    for (var j = 0; j < message.sections.length; ++j)
                        object.sections[j] = $root.proto.Message.ListMessage.Section.toObject(message.sections[j], options, q + 1);
                }
                if (message.productListInfo != null && Object.hasOwnProperty.call(message, "productListInfo"))
                    object.productListInfo = $root.proto.Message.ListMessage.ProductListInfo.toObject(message.productListInfo, options, q + 1);
                if (message.footerText != null && Object.hasOwnProperty.call(message, "footerText"))
                    object.footerText = message.footerText;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                return object;
            };

            /**
             * Converts this ListMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.ListMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ListMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ListMessage
             * @function getTypeUrl
             * @memberof proto.Message.ListMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ListMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.ListMessage";
            };

            /**
             * ListType enum.
             * @name proto.Message.ListMessage.ListType
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} SINGLE_SELECT=1 SINGLE_SELECT value
             * @property {number} PRODUCT_LIST=2 PRODUCT_LIST value
             */
            ListMessage.ListType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "SINGLE_SELECT"] = 1;
                values[valuesById[2] = "PRODUCT_LIST"] = 2;
                return values;
            })();

            ListMessage.Product = (function() {

                /**
                 * Properties of a Product.
                 * @memberof proto.Message.ListMessage
                 * @interface IProduct
                 * @property {string|null} [productId] Product productId
                 */

                /**
                 * Constructs a new Product.
                 * @memberof proto.Message.ListMessage
                 * @classdesc Represents a Product.
                 * @implements IProduct
                 * @constructor
                 * @param {proto.Message.ListMessage.IProduct=} [properties] Properties to set
                 */
                function Product(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * Product productId.
                 * @member {string|null|undefined} productId
                 * @memberof proto.Message.ListMessage.Product
                 * @instance
                 */
                Product.prototype.productId = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Product.prototype, "_productId", {
                    get: $util.oneOfGetter($oneOfFields = ["productId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new Product instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.ListMessage.Product
                 * @static
                 * @param {proto.Message.ListMessage.IProduct=} [properties] Properties to set
                 * @returns {proto.Message.ListMessage.Product} Product instance
                 */
                Product.create = function create(properties) {
                    return new Product(properties);
                };

                /**
                 * Encodes the specified Product message. Does not implicitly {@link proto.Message.ListMessage.Product.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.ListMessage.Product
                 * @static
                 * @param {proto.Message.ListMessage.IProduct} message Product message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Product.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.productId != null && Object.hasOwnProperty.call(message, "productId"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.productId);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified Product message, length delimited. Does not implicitly {@link proto.Message.ListMessage.Product.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.ListMessage.Product
                 * @static
                 * @param {proto.Message.ListMessage.IProduct} message Product message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Product.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a Product message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.ListMessage.Product
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.ListMessage.Product} Product
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Product.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.productId = reader.stringVerify();
                                message._productId = "productId";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a Product message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.ListMessage.Product
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.ListMessage.Product} Product
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Product.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a Product message.
                 * @function verify
                 * @memberof proto.Message.ListMessage.Product
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                Product.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.productId != null && Object.hasOwnProperty.call(message, "productId")) {
                        properties._productId = 1;
                        if (!$util.isString(message.productId))
                            return "productId: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a Product message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.ListMessage.Product
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.ListMessage.Product} Product
                 */
                Product.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.ListMessage.Product: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.productId != null)
                        message.productId = String(object.productId);
                    return message;
                };

                /**
                 * Creates a plain object from a Product message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.ListMessage.Product
                 * @static
                 * @param {proto.Message.ListMessage.Product} message Product
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                Product.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.productId != null && Object.hasOwnProperty.call(message, "productId"))
                        object.productId = message.productId;
                    return object;
                };

                /**
                 * Converts this Product to JSON.
                 * @function toJSON
                 * @memberof proto.Message.ListMessage.Product
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                Product.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for Product
                 * @function getTypeUrl
                 * @memberof proto.Message.ListMessage.Product
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                Product.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.ListMessage.Product";
                };

                return Product;
            })();

            ListMessage.ProductListHeaderImage = (function() {

                /**
                 * Properties of a ProductListHeaderImage.
                 * @memberof proto.Message.ListMessage
                 * @interface IProductListHeaderImage
                 * @property {string|null} [productId] ProductListHeaderImage productId
                 * @property {Uint8Array|null} [jpegThumbnail] ProductListHeaderImage jpegThumbnail
                 */

                /**
                 * Constructs a new ProductListHeaderImage.
                 * @memberof proto.Message.ListMessage
                 * @classdesc Represents a ProductListHeaderImage.
                 * @implements IProductListHeaderImage
                 * @constructor
                 * @param {proto.Message.ListMessage.IProductListHeaderImage=} [properties] Properties to set
                 */
                function ProductListHeaderImage(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * ProductListHeaderImage productId.
                 * @member {string|null|undefined} productId
                 * @memberof proto.Message.ListMessage.ProductListHeaderImage
                 * @instance
                 */
                ProductListHeaderImage.prototype.productId = null;

                /**
                 * ProductListHeaderImage jpegThumbnail.
                 * @member {Uint8Array|null|undefined} jpegThumbnail
                 * @memberof proto.Message.ListMessage.ProductListHeaderImage
                 * @instance
                 */
                ProductListHeaderImage.prototype.jpegThumbnail = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ProductListHeaderImage.prototype, "_productId", {
                    get: $util.oneOfGetter($oneOfFields = ["productId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ProductListHeaderImage.prototype, "_jpegThumbnail", {
                    get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new ProductListHeaderImage instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.ListMessage.ProductListHeaderImage
                 * @static
                 * @param {proto.Message.ListMessage.IProductListHeaderImage=} [properties] Properties to set
                 * @returns {proto.Message.ListMessage.ProductListHeaderImage} ProductListHeaderImage instance
                 */
                ProductListHeaderImage.create = function create(properties) {
                    return new ProductListHeaderImage(properties);
                };

                /**
                 * Encodes the specified ProductListHeaderImage message. Does not implicitly {@link proto.Message.ListMessage.ProductListHeaderImage.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.ListMessage.ProductListHeaderImage
                 * @static
                 * @param {proto.Message.ListMessage.IProductListHeaderImage} message ProductListHeaderImage message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ProductListHeaderImage.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.productId != null && Object.hasOwnProperty.call(message, "productId"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.productId);
                    if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                        writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.jpegThumbnail);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified ProductListHeaderImage message, length delimited. Does not implicitly {@link proto.Message.ListMessage.ProductListHeaderImage.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.ListMessage.ProductListHeaderImage
                 * @static
                 * @param {proto.Message.ListMessage.IProductListHeaderImage} message ProductListHeaderImage message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ProductListHeaderImage.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a ProductListHeaderImage message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.ListMessage.ProductListHeaderImage
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.ListMessage.ProductListHeaderImage} ProductListHeaderImage
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ProductListHeaderImage.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.productId = reader.stringVerify();
                                message._productId = "productId";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.jpegThumbnail = reader.bytes();
                                message._jpegThumbnail = "jpegThumbnail";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a ProductListHeaderImage message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.ListMessage.ProductListHeaderImage
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.ListMessage.ProductListHeaderImage} ProductListHeaderImage
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ProductListHeaderImage.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a ProductListHeaderImage message.
                 * @function verify
                 * @memberof proto.Message.ListMessage.ProductListHeaderImage
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                ProductListHeaderImage.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.productId != null && Object.hasOwnProperty.call(message, "productId")) {
                        properties._productId = 1;
                        if (!$util.isString(message.productId))
                            return "productId: string expected";
                    }
                    if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail")) {
                        properties._jpegThumbnail = 1;
                        if (!(message.jpegThumbnail && typeof message.jpegThumbnail.length === "number" || $util.isString(message.jpegThumbnail)))
                            return "jpegThumbnail: buffer expected";
                    }
                    return null;
                };

                /**
                 * Creates a ProductListHeaderImage message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.ListMessage.ProductListHeaderImage
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.ListMessage.ProductListHeaderImage} ProductListHeaderImage
                 */
                ProductListHeaderImage.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.ListMessage.ProductListHeaderImage: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.productId != null)
                        message.productId = String(object.productId);
                    if (object.jpegThumbnail != null)
                        if (typeof object.jpegThumbnail === "string")
                            $util.base64.decode(object.jpegThumbnail, message.jpegThumbnail = $util.newBuffer($util.base64.length(object.jpegThumbnail)), 0);
                        else if (object.jpegThumbnail.length >= 0)
                            message.jpegThumbnail = object.jpegThumbnail;
                    return message;
                };

                /**
                 * Creates a plain object from a ProductListHeaderImage message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.ListMessage.ProductListHeaderImage
                 * @static
                 * @param {proto.Message.ListMessage.ProductListHeaderImage} message ProductListHeaderImage
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                ProductListHeaderImage.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.productId != null && Object.hasOwnProperty.call(message, "productId"))
                        object.productId = message.productId;
                    if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                        object.jpegThumbnail = options.bytes === String ? $util.base64.encode(message.jpegThumbnail, 0, message.jpegThumbnail.length) : options.bytes === Array ? Array.prototype.slice.call(message.jpegThumbnail) : message.jpegThumbnail;
                    return object;
                };

                /**
                 * Converts this ProductListHeaderImage to JSON.
                 * @function toJSON
                 * @memberof proto.Message.ListMessage.ProductListHeaderImage
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                ProductListHeaderImage.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for ProductListHeaderImage
                 * @function getTypeUrl
                 * @memberof proto.Message.ListMessage.ProductListHeaderImage
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                ProductListHeaderImage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.ListMessage.ProductListHeaderImage";
                };

                return ProductListHeaderImage;
            })();

            ListMessage.ProductListInfo = (function() {

                /**
                 * Properties of a ProductListInfo.
                 * @memberof proto.Message.ListMessage
                 * @interface IProductListInfo
                 * @property {Array.<proto.Message.ListMessage.IProductSection>|null} [productSections] ProductListInfo productSections
                 * @property {proto.Message.ListMessage.IProductListHeaderImage|null} [headerImage] ProductListInfo headerImage
                 * @property {string|null} [businessOwnerJid] ProductListInfo businessOwnerJid
                 */

                /**
                 * Constructs a new ProductListInfo.
                 * @memberof proto.Message.ListMessage
                 * @classdesc Represents a ProductListInfo.
                 * @implements IProductListInfo
                 * @constructor
                 * @param {proto.Message.ListMessage.IProductListInfo=} [properties] Properties to set
                 */
                function ProductListInfo(properties) {
                    this.productSections = [];
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * ProductListInfo productSections.
                 * @member {Array.<proto.Message.ListMessage.IProductSection>} productSections
                 * @memberof proto.Message.ListMessage.ProductListInfo
                 * @instance
                 */
                ProductListInfo.prototype.productSections = $util.emptyArray;

                /**
                 * ProductListInfo headerImage.
                 * @member {proto.Message.ListMessage.IProductListHeaderImage|null|undefined} headerImage
                 * @memberof proto.Message.ListMessage.ProductListInfo
                 * @instance
                 */
                ProductListInfo.prototype.headerImage = null;

                /**
                 * ProductListInfo businessOwnerJid.
                 * @member {string|null|undefined} businessOwnerJid
                 * @memberof proto.Message.ListMessage.ProductListInfo
                 * @instance
                 */
                ProductListInfo.prototype.businessOwnerJid = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ProductListInfo.prototype, "_headerImage", {
                    get: $util.oneOfGetter($oneOfFields = ["headerImage"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ProductListInfo.prototype, "_businessOwnerJid", {
                    get: $util.oneOfGetter($oneOfFields = ["businessOwnerJid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new ProductListInfo instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.ListMessage.ProductListInfo
                 * @static
                 * @param {proto.Message.ListMessage.IProductListInfo=} [properties] Properties to set
                 * @returns {proto.Message.ListMessage.ProductListInfo} ProductListInfo instance
                 */
                ProductListInfo.create = function create(properties) {
                    return new ProductListInfo(properties);
                };

                /**
                 * Encodes the specified ProductListInfo message. Does not implicitly {@link proto.Message.ListMessage.ProductListInfo.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.ListMessage.ProductListInfo
                 * @static
                 * @param {proto.Message.ListMessage.IProductListInfo} message ProductListInfo message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ProductListInfo.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.productSections != null && message.productSections.length)
                        for (var i = 0; i < message.productSections.length; ++i)
                            $root.proto.Message.ListMessage.ProductSection.encode(message.productSections[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                    if (message.headerImage != null && Object.hasOwnProperty.call(message, "headerImage"))
                        $root.proto.Message.ListMessage.ProductListHeaderImage.encode(message.headerImage, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                    if (message.businessOwnerJid != null && Object.hasOwnProperty.call(message, "businessOwnerJid"))
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.businessOwnerJid);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified ProductListInfo message, length delimited. Does not implicitly {@link proto.Message.ListMessage.ProductListInfo.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.ListMessage.ProductListInfo
                 * @static
                 * @param {proto.Message.ListMessage.IProductListInfo} message ProductListInfo message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ProductListInfo.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a ProductListInfo message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.ListMessage.ProductListInfo
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.ListMessage.ProductListInfo} ProductListInfo
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ProductListInfo.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                if (!(message.productSections && message.productSections.length))
                                    message.productSections = [];
                                message.productSections.push($root.proto.Message.ListMessage.ProductSection.decode(reader, reader.uint32(), undefined, q + 1));
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.headerImage = $root.proto.Message.ListMessage.ProductListHeaderImage.decode(reader, reader.uint32(), undefined, q + 1, message.headerImage);
                                message._headerImage = "headerImage";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.businessOwnerJid = reader.stringVerify();
                                message._businessOwnerJid = "businessOwnerJid";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a ProductListInfo message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.ListMessage.ProductListInfo
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.ListMessage.ProductListInfo} ProductListInfo
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ProductListInfo.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a ProductListInfo message.
                 * @function verify
                 * @memberof proto.Message.ListMessage.ProductListInfo
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                ProductListInfo.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.productSections != null && Object.hasOwnProperty.call(message, "productSections")) {
                        if (!Array.isArray(message.productSections))
                            return "productSections: array expected";
                        for (var i = 0; i < message.productSections.length; ++i) {
                            var error = $root.proto.Message.ListMessage.ProductSection.verify(message.productSections[i], q + 1);
                            if (error)
                                return "productSections." + error;
                        }
                    }
                    if (message.headerImage != null && Object.hasOwnProperty.call(message, "headerImage")) {
                        properties._headerImage = 1;
                        {
                            var error = $root.proto.Message.ListMessage.ProductListHeaderImage.verify(message.headerImage, q + 1);
                            if (error)
                                return "headerImage." + error;
                        }
                    }
                    if (message.businessOwnerJid != null && Object.hasOwnProperty.call(message, "businessOwnerJid")) {
                        properties._businessOwnerJid = 1;
                        if (!$util.isString(message.businessOwnerJid))
                            return "businessOwnerJid: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a ProductListInfo message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.ListMessage.ProductListInfo
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.ListMessage.ProductListInfo} ProductListInfo
                 */
                ProductListInfo.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.ListMessage.ProductListInfo: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.productSections) {
                        if (!Array.isArray(object.productSections))
                            throw TypeError(".proto.Message.ListMessage.ProductListInfo.productSections: array expected");
                        message.productSections = Array(object.productSections.length);
                        for (var i = 0; i < object.productSections.length; ++i) {
                            if (!$util.isObject(object.productSections[i]))
                                throw TypeError(".proto.Message.ListMessage.ProductListInfo.productSections: object expected");
                            message.productSections[i] = $root.proto.Message.ListMessage.ProductSection.fromObject(object.productSections[i], q + 1);
                        }
                    }
                    if (object.headerImage != null) {
                        if (!$util.isObject(object.headerImage))
                            throw TypeError(".proto.Message.ListMessage.ProductListInfo.headerImage: object expected");
                        message.headerImage = $root.proto.Message.ListMessage.ProductListHeaderImage.fromObject(object.headerImage, q + 1);
                    }
                    if (object.businessOwnerJid != null)
                        message.businessOwnerJid = String(object.businessOwnerJid);
                    return message;
                };

                /**
                 * Creates a plain object from a ProductListInfo message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.ListMessage.ProductListInfo
                 * @static
                 * @param {proto.Message.ListMessage.ProductListInfo} message ProductListInfo
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                ProductListInfo.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (options.arrays || options.defaults)
                        object.productSections = [];
                    if (message.productSections && message.productSections.length) {
                        object.productSections = Array(message.productSections.length);
                        for (var j = 0; j < message.productSections.length; ++j)
                            object.productSections[j] = $root.proto.Message.ListMessage.ProductSection.toObject(message.productSections[j], options, q + 1);
                    }
                    if (message.headerImage != null && Object.hasOwnProperty.call(message, "headerImage"))
                        object.headerImage = $root.proto.Message.ListMessage.ProductListHeaderImage.toObject(message.headerImage, options, q + 1);
                    if (message.businessOwnerJid != null && Object.hasOwnProperty.call(message, "businessOwnerJid"))
                        object.businessOwnerJid = message.businessOwnerJid;
                    return object;
                };

                /**
                 * Converts this ProductListInfo to JSON.
                 * @function toJSON
                 * @memberof proto.Message.ListMessage.ProductListInfo
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                ProductListInfo.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for ProductListInfo
                 * @function getTypeUrl
                 * @memberof proto.Message.ListMessage.ProductListInfo
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                ProductListInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.ListMessage.ProductListInfo";
                };

                return ProductListInfo;
            })();

            ListMessage.ProductSection = (function() {

                /**
                 * Properties of a ProductSection.
                 * @memberof proto.Message.ListMessage
                 * @interface IProductSection
                 * @property {string|null} [title] ProductSection title
                 * @property {Array.<proto.Message.ListMessage.IProduct>|null} [products] ProductSection products
                 */

                /**
                 * Constructs a new ProductSection.
                 * @memberof proto.Message.ListMessage
                 * @classdesc Represents a ProductSection.
                 * @implements IProductSection
                 * @constructor
                 * @param {proto.Message.ListMessage.IProductSection=} [properties] Properties to set
                 */
                function ProductSection(properties) {
                    this.products = [];
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * ProductSection title.
                 * @member {string|null|undefined} title
                 * @memberof proto.Message.ListMessage.ProductSection
                 * @instance
                 */
                ProductSection.prototype.title = null;

                /**
                 * ProductSection products.
                 * @member {Array.<proto.Message.ListMessage.IProduct>} products
                 * @memberof proto.Message.ListMessage.ProductSection
                 * @instance
                 */
                ProductSection.prototype.products = $util.emptyArray;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ProductSection.prototype, "_title", {
                    get: $util.oneOfGetter($oneOfFields = ["title"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new ProductSection instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.ListMessage.ProductSection
                 * @static
                 * @param {proto.Message.ListMessage.IProductSection=} [properties] Properties to set
                 * @returns {proto.Message.ListMessage.ProductSection} ProductSection instance
                 */
                ProductSection.create = function create(properties) {
                    return new ProductSection(properties);
                };

                /**
                 * Encodes the specified ProductSection message. Does not implicitly {@link proto.Message.ListMessage.ProductSection.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.ListMessage.ProductSection
                 * @static
                 * @param {proto.Message.ListMessage.IProductSection} message ProductSection message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ProductSection.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.title);
                    if (message.products != null && message.products.length)
                        for (var i = 0; i < message.products.length; ++i)
                            $root.proto.Message.ListMessage.Product.encode(message.products[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified ProductSection message, length delimited. Does not implicitly {@link proto.Message.ListMessage.ProductSection.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.ListMessage.ProductSection
                 * @static
                 * @param {proto.Message.ListMessage.IProductSection} message ProductSection message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ProductSection.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a ProductSection message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.ListMessage.ProductSection
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.ListMessage.ProductSection} ProductSection
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ProductSection.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.title = reader.stringVerify();
                                message._title = "title";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                if (!(message.products && message.products.length))
                                    message.products = [];
                                message.products.push($root.proto.Message.ListMessage.Product.decode(reader, reader.uint32(), undefined, q + 1));
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a ProductSection message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.ListMessage.ProductSection
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.ListMessage.ProductSection} ProductSection
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ProductSection.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a ProductSection message.
                 * @function verify
                 * @memberof proto.Message.ListMessage.ProductSection
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                ProductSection.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                        properties._title = 1;
                        if (!$util.isString(message.title))
                            return "title: string expected";
                    }
                    if (message.products != null && Object.hasOwnProperty.call(message, "products")) {
                        if (!Array.isArray(message.products))
                            return "products: array expected";
                        for (var i = 0; i < message.products.length; ++i) {
                            var error = $root.proto.Message.ListMessage.Product.verify(message.products[i], q + 1);
                            if (error)
                                return "products." + error;
                        }
                    }
                    return null;
                };

                /**
                 * Creates a ProductSection message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.ListMessage.ProductSection
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.ListMessage.ProductSection} ProductSection
                 */
                ProductSection.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.ListMessage.ProductSection: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.title != null)
                        message.title = String(object.title);
                    if (object.products) {
                        if (!Array.isArray(object.products))
                            throw TypeError(".proto.Message.ListMessage.ProductSection.products: array expected");
                        message.products = Array(object.products.length);
                        for (var i = 0; i < object.products.length; ++i) {
                            if (!$util.isObject(object.products[i]))
                                throw TypeError(".proto.Message.ListMessage.ProductSection.products: object expected");
                            message.products[i] = $root.proto.Message.ListMessage.Product.fromObject(object.products[i], q + 1);
                        }
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a ProductSection message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.ListMessage.ProductSection
                 * @static
                 * @param {proto.Message.ListMessage.ProductSection} message ProductSection
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                ProductSection.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (options.arrays || options.defaults)
                        object.products = [];
                    if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                        object.title = message.title;
                    if (message.products && message.products.length) {
                        object.products = Array(message.products.length);
                        for (var j = 0; j < message.products.length; ++j)
                            object.products[j] = $root.proto.Message.ListMessage.Product.toObject(message.products[j], options, q + 1);
                    }
                    return object;
                };

                /**
                 * Converts this ProductSection to JSON.
                 * @function toJSON
                 * @memberof proto.Message.ListMessage.ProductSection
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                ProductSection.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for ProductSection
                 * @function getTypeUrl
                 * @memberof proto.Message.ListMessage.ProductSection
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                ProductSection.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.ListMessage.ProductSection";
                };

                return ProductSection;
            })();

            ListMessage.Row = (function() {

                /**
                 * Properties of a Row.
                 * @memberof proto.Message.ListMessage
                 * @interface IRow
                 * @property {string|null} [title] Row title
                 * @property {string|null} [description] Row description
                 * @property {string|null} [rowId] Row rowId
                 */

                /**
                 * Constructs a new Row.
                 * @memberof proto.Message.ListMessage
                 * @classdesc Represents a Row.
                 * @implements IRow
                 * @constructor
                 * @param {proto.Message.ListMessage.IRow=} [properties] Properties to set
                 */
                function Row(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * Row title.
                 * @member {string|null|undefined} title
                 * @memberof proto.Message.ListMessage.Row
                 * @instance
                 */
                Row.prototype.title = null;

                /**
                 * Row description.
                 * @member {string|null|undefined} description
                 * @memberof proto.Message.ListMessage.Row
                 * @instance
                 */
                Row.prototype.description = null;

                /**
                 * Row rowId.
                 * @member {string|null|undefined} rowId
                 * @memberof proto.Message.ListMessage.Row
                 * @instance
                 */
                Row.prototype.rowId = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Row.prototype, "_title", {
                    get: $util.oneOfGetter($oneOfFields = ["title"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Row.prototype, "_description", {
                    get: $util.oneOfGetter($oneOfFields = ["description"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Row.prototype, "_rowId", {
                    get: $util.oneOfGetter($oneOfFields = ["rowId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new Row instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.ListMessage.Row
                 * @static
                 * @param {proto.Message.ListMessage.IRow=} [properties] Properties to set
                 * @returns {proto.Message.ListMessage.Row} Row instance
                 */
                Row.create = function create(properties) {
                    return new Row(properties);
                };

                /**
                 * Encodes the specified Row message. Does not implicitly {@link proto.Message.ListMessage.Row.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.ListMessage.Row
                 * @static
                 * @param {proto.Message.ListMessage.IRow} message Row message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Row.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.title);
                    if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.description);
                    if (message.rowId != null && Object.hasOwnProperty.call(message, "rowId"))
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.rowId);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified Row message, length delimited. Does not implicitly {@link proto.Message.ListMessage.Row.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.ListMessage.Row
                 * @static
                 * @param {proto.Message.ListMessage.IRow} message Row message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Row.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a Row message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.ListMessage.Row
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.ListMessage.Row} Row
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Row.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.title = reader.stringVerify();
                                message._title = "title";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.description = reader.stringVerify();
                                message._description = "description";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.rowId = reader.stringVerify();
                                message._rowId = "rowId";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a Row message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.ListMessage.Row
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.ListMessage.Row} Row
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Row.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a Row message.
                 * @function verify
                 * @memberof proto.Message.ListMessage.Row
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                Row.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                        properties._title = 1;
                        if (!$util.isString(message.title))
                            return "title: string expected";
                    }
                    if (message.description != null && Object.hasOwnProperty.call(message, "description")) {
                        properties._description = 1;
                        if (!$util.isString(message.description))
                            return "description: string expected";
                    }
                    if (message.rowId != null && Object.hasOwnProperty.call(message, "rowId")) {
                        properties._rowId = 1;
                        if (!$util.isString(message.rowId))
                            return "rowId: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a Row message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.ListMessage.Row
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.ListMessage.Row} Row
                 */
                Row.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.ListMessage.Row: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.title != null)
                        message.title = String(object.title);
                    if (object.description != null)
                        message.description = String(object.description);
                    if (object.rowId != null)
                        message.rowId = String(object.rowId);
                    return message;
                };

                /**
                 * Creates a plain object from a Row message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.ListMessage.Row
                 * @static
                 * @param {proto.Message.ListMessage.Row} message Row
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                Row.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                        object.title = message.title;
                    if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                        object.description = message.description;
                    if (message.rowId != null && Object.hasOwnProperty.call(message, "rowId"))
                        object.rowId = message.rowId;
                    return object;
                };

                /**
                 * Converts this Row to JSON.
                 * @function toJSON
                 * @memberof proto.Message.ListMessage.Row
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                Row.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for Row
                 * @function getTypeUrl
                 * @memberof proto.Message.ListMessage.Row
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                Row.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.ListMessage.Row";
                };

                return Row;
            })();

            ListMessage.Section = (function() {

                /**
                 * Properties of a Section.
                 * @memberof proto.Message.ListMessage
                 * @interface ISection
                 * @property {string|null} [title] Section title
                 * @property {Array.<proto.Message.ListMessage.IRow>|null} [rows] Section rows
                 */

                /**
                 * Constructs a new Section.
                 * @memberof proto.Message.ListMessage
                 * @classdesc Represents a Section.
                 * @implements ISection
                 * @constructor
                 * @param {proto.Message.ListMessage.ISection=} [properties] Properties to set
                 */
                function Section(properties) {
                    this.rows = [];
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * Section title.
                 * @member {string|null|undefined} title
                 * @memberof proto.Message.ListMessage.Section
                 * @instance
                 */
                Section.prototype.title = null;

                /**
                 * Section rows.
                 * @member {Array.<proto.Message.ListMessage.IRow>} rows
                 * @memberof proto.Message.ListMessage.Section
                 * @instance
                 */
                Section.prototype.rows = $util.emptyArray;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Section.prototype, "_title", {
                    get: $util.oneOfGetter($oneOfFields = ["title"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new Section instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.ListMessage.Section
                 * @static
                 * @param {proto.Message.ListMessage.ISection=} [properties] Properties to set
                 * @returns {proto.Message.ListMessage.Section} Section instance
                 */
                Section.create = function create(properties) {
                    return new Section(properties);
                };

                /**
                 * Encodes the specified Section message. Does not implicitly {@link proto.Message.ListMessage.Section.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.ListMessage.Section
                 * @static
                 * @param {proto.Message.ListMessage.ISection} message Section message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Section.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.title);
                    if (message.rows != null && message.rows.length)
                        for (var i = 0; i < message.rows.length; ++i)
                            $root.proto.Message.ListMessage.Row.encode(message.rows[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified Section message, length delimited. Does not implicitly {@link proto.Message.ListMessage.Section.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.ListMessage.Section
                 * @static
                 * @param {proto.Message.ListMessage.ISection} message Section message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Section.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a Section message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.ListMessage.Section
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.ListMessage.Section} Section
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Section.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.title = reader.stringVerify();
                                message._title = "title";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                if (!(message.rows && message.rows.length))
                                    message.rows = [];
                                message.rows.push($root.proto.Message.ListMessage.Row.decode(reader, reader.uint32(), undefined, q + 1));
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a Section message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.ListMessage.Section
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.ListMessage.Section} Section
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Section.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a Section message.
                 * @function verify
                 * @memberof proto.Message.ListMessage.Section
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                Section.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                        properties._title = 1;
                        if (!$util.isString(message.title))
                            return "title: string expected";
                    }
                    if (message.rows != null && Object.hasOwnProperty.call(message, "rows")) {
                        if (!Array.isArray(message.rows))
                            return "rows: array expected";
                        for (var i = 0; i < message.rows.length; ++i) {
                            var error = $root.proto.Message.ListMessage.Row.verify(message.rows[i], q + 1);
                            if (error)
                                return "rows." + error;
                        }
                    }
                    return null;
                };

                /**
                 * Creates a Section message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.ListMessage.Section
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.ListMessage.Section} Section
                 */
                Section.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.ListMessage.Section: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.title != null)
                        message.title = String(object.title);
                    if (object.rows) {
                        if (!Array.isArray(object.rows))
                            throw TypeError(".proto.Message.ListMessage.Section.rows: array expected");
                        message.rows = Array(object.rows.length);
                        for (var i = 0; i < object.rows.length; ++i) {
                            if (!$util.isObject(object.rows[i]))
                                throw TypeError(".proto.Message.ListMessage.Section.rows: object expected");
                            message.rows[i] = $root.proto.Message.ListMessage.Row.fromObject(object.rows[i], q + 1);
                        }
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a Section message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.ListMessage.Section
                 * @static
                 * @param {proto.Message.ListMessage.Section} message Section
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                Section.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (options.arrays || options.defaults)
                        object.rows = [];
                    if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                        object.title = message.title;
                    if (message.rows && message.rows.length) {
                        object.rows = Array(message.rows.length);
                        for (var j = 0; j < message.rows.length; ++j)
                            object.rows[j] = $root.proto.Message.ListMessage.Row.toObject(message.rows[j], options, q + 1);
                    }
                    return object;
                };

                /**
                 * Converts this Section to JSON.
                 * @function toJSON
                 * @memberof proto.Message.ListMessage.Section
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                Section.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for Section
                 * @function getTypeUrl
                 * @memberof proto.Message.ListMessage.Section
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                Section.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.ListMessage.Section";
                };

                return Section;
            })();

            return ListMessage;
        })();

        Message.ListResponseMessage = (function() {

            /**
             * Properties of a ListResponseMessage.
             * @memberof proto.Message
             * @interface IListResponseMessage
             * @property {string|null} [title] ListResponseMessage title
             * @property {proto.Message.ListResponseMessage.ListType|null} [listType] ListResponseMessage listType
             * @property {proto.Message.ListResponseMessage.ISingleSelectReply|null} [singleSelectReply] ListResponseMessage singleSelectReply
             * @property {proto.IContextInfo|null} [contextInfo] ListResponseMessage contextInfo
             * @property {string|null} [description] ListResponseMessage description
             */

            /**
             * Constructs a new ListResponseMessage.
             * @memberof proto.Message
             * @classdesc Represents a ListResponseMessage.
             * @implements IListResponseMessage
             * @constructor
             * @param {proto.Message.IListResponseMessage=} [properties] Properties to set
             */
            function ListResponseMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ListResponseMessage title.
             * @member {string|null|undefined} title
             * @memberof proto.Message.ListResponseMessage
             * @instance
             */
            ListResponseMessage.prototype.title = null;

            /**
             * ListResponseMessage listType.
             * @member {proto.Message.ListResponseMessage.ListType|null|undefined} listType
             * @memberof proto.Message.ListResponseMessage
             * @instance
             */
            ListResponseMessage.prototype.listType = null;

            /**
             * ListResponseMessage singleSelectReply.
             * @member {proto.Message.ListResponseMessage.ISingleSelectReply|null|undefined} singleSelectReply
             * @memberof proto.Message.ListResponseMessage
             * @instance
             */
            ListResponseMessage.prototype.singleSelectReply = null;

            /**
             * ListResponseMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.ListResponseMessage
             * @instance
             */
            ListResponseMessage.prototype.contextInfo = null;

            /**
             * ListResponseMessage description.
             * @member {string|null|undefined} description
             * @memberof proto.Message.ListResponseMessage
             * @instance
             */
            ListResponseMessage.prototype.description = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ListResponseMessage.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ListResponseMessage.prototype, "_listType", {
                get: $util.oneOfGetter($oneOfFields = ["listType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ListResponseMessage.prototype, "_singleSelectReply", {
                get: $util.oneOfGetter($oneOfFields = ["singleSelectReply"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ListResponseMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ListResponseMessage.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ListResponseMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.ListResponseMessage
             * @static
             * @param {proto.Message.IListResponseMessage=} [properties] Properties to set
             * @returns {proto.Message.ListResponseMessage} ListResponseMessage instance
             */
            ListResponseMessage.create = function create(properties) {
                return new ListResponseMessage(properties);
            };

            /**
             * Encodes the specified ListResponseMessage message. Does not implicitly {@link proto.Message.ListResponseMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.ListResponseMessage
             * @static
             * @param {proto.Message.IListResponseMessage} message ListResponseMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ListResponseMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.title);
                if (message.listType != null && Object.hasOwnProperty.call(message, "listType"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.listType);
                if (message.singleSelectReply != null && Object.hasOwnProperty.call(message, "singleSelectReply"))
                    $root.proto.Message.ListResponseMessage.SingleSelectReply.encode(message.singleSelectReply, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.description);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ListResponseMessage message, length delimited. Does not implicitly {@link proto.Message.ListResponseMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.ListResponseMessage
             * @static
             * @param {proto.Message.IListResponseMessage} message ListResponseMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ListResponseMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ListResponseMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.ListResponseMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.ListResponseMessage} ListResponseMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ListResponseMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.title = reader.stringVerify();
                            message._title = "title";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.listType = reader.int32();
                            message._listType = "listType";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.singleSelectReply = $root.proto.Message.ListResponseMessage.SingleSelectReply.decode(reader, reader.uint32(), undefined, q + 1, message.singleSelectReply);
                            message._singleSelectReply = "singleSelectReply";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.description = reader.stringVerify();
                            message._description = "description";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ListResponseMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.ListResponseMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.ListResponseMessage} ListResponseMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ListResponseMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ListResponseMessage message.
             * @function verify
             * @memberof proto.Message.ListResponseMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ListResponseMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                    properties._title = 1;
                    if (!$util.isString(message.title))
                        return "title: string expected";
                }
                if (message.listType != null && Object.hasOwnProperty.call(message, "listType")) {
                    properties._listType = 1;
                    if (typeof message.listType !== "number" || (message.listType | 0) !== message.listType)
                        return "listType: enum value expected";
                }
                if (message.singleSelectReply != null && Object.hasOwnProperty.call(message, "singleSelectReply")) {
                    properties._singleSelectReply = 1;
                    {
                        var error = $root.proto.Message.ListResponseMessage.SingleSelectReply.verify(message.singleSelectReply, q + 1);
                        if (error)
                            return "singleSelectReply." + error;
                    }
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.description != null && Object.hasOwnProperty.call(message, "description")) {
                    properties._description = 1;
                    if (!$util.isString(message.description))
                        return "description: string expected";
                }
                return null;
            };

            /**
             * Creates a ListResponseMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.ListResponseMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.ListResponseMessage} ListResponseMessage
             */
            ListResponseMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.ListResponseMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.title != null)
                    message.title = String(object.title);
                switch (object.listType) {
                case "UNKNOWN":
                case 0:
                    message.listType = 0;
                    break;
                case "SINGLE_SELECT":
                case 1:
                    message.listType = 1;
                    break;
                default:
                    if (typeof object.listType === "number" && (object.listType | 0) === object.listType)
                        message.listType = object.listType;
                }
                if (object.singleSelectReply != null) {
                    if (!$util.isObject(object.singleSelectReply))
                        throw TypeError(".proto.Message.ListResponseMessage.singleSelectReply: object expected");
                    message.singleSelectReply = $root.proto.Message.ListResponseMessage.SingleSelectReply.fromObject(object.singleSelectReply, q + 1);
                }
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.ListResponseMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                if (object.description != null)
                    message.description = String(object.description);
                return message;
            };

            /**
             * Creates a plain object from a ListResponseMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.ListResponseMessage
             * @static
             * @param {proto.Message.ListResponseMessage} message ListResponseMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ListResponseMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    object.title = message.title;
                if (message.listType != null && Object.hasOwnProperty.call(message, "listType"))
                    object.listType = options.enums === String ? $root.proto.Message.ListResponseMessage.ListType[message.listType] === undefined ? message.listType : $root.proto.Message.ListResponseMessage.ListType[message.listType] : message.listType;
                if (message.singleSelectReply != null && Object.hasOwnProperty.call(message, "singleSelectReply"))
                    object.singleSelectReply = $root.proto.Message.ListResponseMessage.SingleSelectReply.toObject(message.singleSelectReply, options, q + 1);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    object.description = message.description;
                return object;
            };

            /**
             * Converts this ListResponseMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.ListResponseMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ListResponseMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ListResponseMessage
             * @function getTypeUrl
             * @memberof proto.Message.ListResponseMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ListResponseMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.ListResponseMessage";
            };

            /**
             * ListType enum.
             * @name proto.Message.ListResponseMessage.ListType
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} SINGLE_SELECT=1 SINGLE_SELECT value
             */
            ListResponseMessage.ListType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "SINGLE_SELECT"] = 1;
                return values;
            })();

            ListResponseMessage.SingleSelectReply = (function() {

                /**
                 * Properties of a SingleSelectReply.
                 * @memberof proto.Message.ListResponseMessage
                 * @interface ISingleSelectReply
                 * @property {string|null} [selectedRowId] SingleSelectReply selectedRowId
                 */

                /**
                 * Constructs a new SingleSelectReply.
                 * @memberof proto.Message.ListResponseMessage
                 * @classdesc Represents a SingleSelectReply.
                 * @implements ISingleSelectReply
                 * @constructor
                 * @param {proto.Message.ListResponseMessage.ISingleSelectReply=} [properties] Properties to set
                 */
                function SingleSelectReply(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * SingleSelectReply selectedRowId.
                 * @member {string|null|undefined} selectedRowId
                 * @memberof proto.Message.ListResponseMessage.SingleSelectReply
                 * @instance
                 */
                SingleSelectReply.prototype.selectedRowId = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SingleSelectReply.prototype, "_selectedRowId", {
                    get: $util.oneOfGetter($oneOfFields = ["selectedRowId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new SingleSelectReply instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.ListResponseMessage.SingleSelectReply
                 * @static
                 * @param {proto.Message.ListResponseMessage.ISingleSelectReply=} [properties] Properties to set
                 * @returns {proto.Message.ListResponseMessage.SingleSelectReply} SingleSelectReply instance
                 */
                SingleSelectReply.create = function create(properties) {
                    return new SingleSelectReply(properties);
                };

                /**
                 * Encodes the specified SingleSelectReply message. Does not implicitly {@link proto.Message.ListResponseMessage.SingleSelectReply.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.ListResponseMessage.SingleSelectReply
                 * @static
                 * @param {proto.Message.ListResponseMessage.ISingleSelectReply} message SingleSelectReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                SingleSelectReply.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.selectedRowId != null && Object.hasOwnProperty.call(message, "selectedRowId"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.selectedRowId);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified SingleSelectReply message, length delimited. Does not implicitly {@link proto.Message.ListResponseMessage.SingleSelectReply.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.ListResponseMessage.SingleSelectReply
                 * @static
                 * @param {proto.Message.ListResponseMessage.ISingleSelectReply} message SingleSelectReply message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                SingleSelectReply.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a SingleSelectReply message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.ListResponseMessage.SingleSelectReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.ListResponseMessage.SingleSelectReply} SingleSelectReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                SingleSelectReply.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.selectedRowId = reader.stringVerify();
                                message._selectedRowId = "selectedRowId";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a SingleSelectReply message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.ListResponseMessage.SingleSelectReply
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.ListResponseMessage.SingleSelectReply} SingleSelectReply
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                SingleSelectReply.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a SingleSelectReply message.
                 * @function verify
                 * @memberof proto.Message.ListResponseMessage.SingleSelectReply
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                SingleSelectReply.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.selectedRowId != null && Object.hasOwnProperty.call(message, "selectedRowId")) {
                        properties._selectedRowId = 1;
                        if (!$util.isString(message.selectedRowId))
                            return "selectedRowId: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a SingleSelectReply message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.ListResponseMessage.SingleSelectReply
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.ListResponseMessage.SingleSelectReply} SingleSelectReply
                 */
                SingleSelectReply.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.ListResponseMessage.SingleSelectReply: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.selectedRowId != null)
                        message.selectedRowId = String(object.selectedRowId);
                    return message;
                };

                /**
                 * Creates a plain object from a SingleSelectReply message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.ListResponseMessage.SingleSelectReply
                 * @static
                 * @param {proto.Message.ListResponseMessage.SingleSelectReply} message SingleSelectReply
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                SingleSelectReply.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.selectedRowId != null && Object.hasOwnProperty.call(message, "selectedRowId"))
                        object.selectedRowId = message.selectedRowId;
                    return object;
                };

                /**
                 * Converts this SingleSelectReply to JSON.
                 * @function toJSON
                 * @memberof proto.Message.ListResponseMessage.SingleSelectReply
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                SingleSelectReply.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for SingleSelectReply
                 * @function getTypeUrl
                 * @memberof proto.Message.ListResponseMessage.SingleSelectReply
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                SingleSelectReply.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.ListResponseMessage.SingleSelectReply";
                };

                return SingleSelectReply;
            })();

            return ListResponseMessage;
        })();

        Message.LiveLocationMessage = (function() {

            /**
             * Properties of a LiveLocationMessage.
             * @memberof proto.Message
             * @interface ILiveLocationMessage
             * @property {number|null} [degreesLatitude] LiveLocationMessage degreesLatitude
             * @property {number|null} [degreesLongitude] LiveLocationMessage degreesLongitude
             * @property {number|null} [accuracyInMeters] LiveLocationMessage accuracyInMeters
             * @property {number|null} [speedInMps] LiveLocationMessage speedInMps
             * @property {number|null} [degreesClockwiseFromMagneticNorth] LiveLocationMessage degreesClockwiseFromMagneticNorth
             * @property {string|null} [caption] LiveLocationMessage caption
             * @property {number|Long|null} [sequenceNumber] LiveLocationMessage sequenceNumber
             * @property {number|null} [timeOffset] LiveLocationMessage timeOffset
             * @property {Uint8Array|null} [jpegThumbnail] LiveLocationMessage jpegThumbnail
             * @property {proto.IContextInfo|null} [contextInfo] LiveLocationMessage contextInfo
             */

            /**
             * Constructs a new LiveLocationMessage.
             * @memberof proto.Message
             * @classdesc Represents a LiveLocationMessage.
             * @implements ILiveLocationMessage
             * @constructor
             * @param {proto.Message.ILiveLocationMessage=} [properties] Properties to set
             */
            function LiveLocationMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * LiveLocationMessage degreesLatitude.
             * @member {number|null|undefined} degreesLatitude
             * @memberof proto.Message.LiveLocationMessage
             * @instance
             */
            LiveLocationMessage.prototype.degreesLatitude = null;

            /**
             * LiveLocationMessage degreesLongitude.
             * @member {number|null|undefined} degreesLongitude
             * @memberof proto.Message.LiveLocationMessage
             * @instance
             */
            LiveLocationMessage.prototype.degreesLongitude = null;

            /**
             * LiveLocationMessage accuracyInMeters.
             * @member {number|null|undefined} accuracyInMeters
             * @memberof proto.Message.LiveLocationMessage
             * @instance
             */
            LiveLocationMessage.prototype.accuracyInMeters = null;

            /**
             * LiveLocationMessage speedInMps.
             * @member {number|null|undefined} speedInMps
             * @memberof proto.Message.LiveLocationMessage
             * @instance
             */
            LiveLocationMessage.prototype.speedInMps = null;

            /**
             * LiveLocationMessage degreesClockwiseFromMagneticNorth.
             * @member {number|null|undefined} degreesClockwiseFromMagneticNorth
             * @memberof proto.Message.LiveLocationMessage
             * @instance
             */
            LiveLocationMessage.prototype.degreesClockwiseFromMagneticNorth = null;

            /**
             * LiveLocationMessage caption.
             * @member {string|null|undefined} caption
             * @memberof proto.Message.LiveLocationMessage
             * @instance
             */
            LiveLocationMessage.prototype.caption = null;

            /**
             * LiveLocationMessage sequenceNumber.
             * @member {number|Long|null|undefined} sequenceNumber
             * @memberof proto.Message.LiveLocationMessage
             * @instance
             */
            LiveLocationMessage.prototype.sequenceNumber = null;

            /**
             * LiveLocationMessage timeOffset.
             * @member {number|null|undefined} timeOffset
             * @memberof proto.Message.LiveLocationMessage
             * @instance
             */
            LiveLocationMessage.prototype.timeOffset = null;

            /**
             * LiveLocationMessage jpegThumbnail.
             * @member {Uint8Array|null|undefined} jpegThumbnail
             * @memberof proto.Message.LiveLocationMessage
             * @instance
             */
            LiveLocationMessage.prototype.jpegThumbnail = null;

            /**
             * LiveLocationMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.LiveLocationMessage
             * @instance
             */
            LiveLocationMessage.prototype.contextInfo = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LiveLocationMessage.prototype, "_degreesLatitude", {
                get: $util.oneOfGetter($oneOfFields = ["degreesLatitude"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LiveLocationMessage.prototype, "_degreesLongitude", {
                get: $util.oneOfGetter($oneOfFields = ["degreesLongitude"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LiveLocationMessage.prototype, "_accuracyInMeters", {
                get: $util.oneOfGetter($oneOfFields = ["accuracyInMeters"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LiveLocationMessage.prototype, "_speedInMps", {
                get: $util.oneOfGetter($oneOfFields = ["speedInMps"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LiveLocationMessage.prototype, "_degreesClockwiseFromMagneticNorth", {
                get: $util.oneOfGetter($oneOfFields = ["degreesClockwiseFromMagneticNorth"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LiveLocationMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LiveLocationMessage.prototype, "_sequenceNumber", {
                get: $util.oneOfGetter($oneOfFields = ["sequenceNumber"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LiveLocationMessage.prototype, "_timeOffset", {
                get: $util.oneOfGetter($oneOfFields = ["timeOffset"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LiveLocationMessage.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LiveLocationMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new LiveLocationMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.LiveLocationMessage
             * @static
             * @param {proto.Message.ILiveLocationMessage=} [properties] Properties to set
             * @returns {proto.Message.LiveLocationMessage} LiveLocationMessage instance
             */
            LiveLocationMessage.create = function create(properties) {
                return new LiveLocationMessage(properties);
            };

            /**
             * Encodes the specified LiveLocationMessage message. Does not implicitly {@link proto.Message.LiveLocationMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.LiveLocationMessage
             * @static
             * @param {proto.Message.ILiveLocationMessage} message LiveLocationMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LiveLocationMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.degreesLatitude != null && Object.hasOwnProperty.call(message, "degreesLatitude"))
                    writer.uint32(/* id 1, wireType 1 =*/9).double(message.degreesLatitude);
                if (message.degreesLongitude != null && Object.hasOwnProperty.call(message, "degreesLongitude"))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.degreesLongitude);
                if (message.accuracyInMeters != null && Object.hasOwnProperty.call(message, "accuracyInMeters"))
                    writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.accuracyInMeters);
                if (message.speedInMps != null && Object.hasOwnProperty.call(message, "speedInMps"))
                    writer.uint32(/* id 4, wireType 5 =*/37).float(message.speedInMps);
                if (message.degreesClockwiseFromMagneticNorth != null && Object.hasOwnProperty.call(message, "degreesClockwiseFromMagneticNorth"))
                    writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.degreesClockwiseFromMagneticNorth);
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.caption);
                if (message.sequenceNumber != null && Object.hasOwnProperty.call(message, "sequenceNumber"))
                    writer.uint32(/* id 7, wireType 0 =*/56).int64(message.sequenceNumber);
                if (message.timeOffset != null && Object.hasOwnProperty.call(message, "timeOffset"))
                    writer.uint32(/* id 8, wireType 0 =*/64).uint32(message.timeOffset);
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    writer.uint32(/* id 16, wireType 2 =*/130).bytes(message.jpegThumbnail);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LiveLocationMessage message, length delimited. Does not implicitly {@link proto.Message.LiveLocationMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.LiveLocationMessage
             * @static
             * @param {proto.Message.ILiveLocationMessage} message LiveLocationMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LiveLocationMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a LiveLocationMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.LiveLocationMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.LiveLocationMessage} LiveLocationMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LiveLocationMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 1)
                                break;
                            message.degreesLatitude = reader.double();
                            message._degreesLatitude = "degreesLatitude";
                            continue;
                        }
                    case 2: {
                            if (u !== 1)
                                break;
                            message.degreesLongitude = reader.double();
                            message._degreesLongitude = "degreesLongitude";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.accuracyInMeters = reader.uint32();
                            message._accuracyInMeters = "accuracyInMeters";
                            continue;
                        }
                    case 4: {
                            if (u !== 5)
                                break;
                            message.speedInMps = reader.float();
                            message._speedInMps = "speedInMps";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.degreesClockwiseFromMagneticNorth = reader.uint32();
                            message._degreesClockwiseFromMagneticNorth = "degreesClockwiseFromMagneticNorth";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.caption = reader.stringVerify();
                            message._caption = "caption";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            message.sequenceNumber = reader.int64();
                            message._sequenceNumber = "sequenceNumber";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            message.timeOffset = reader.uint32();
                            message._timeOffset = "timeOffset";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            message.jpegThumbnail = reader.bytes();
                            message._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a LiveLocationMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.LiveLocationMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.LiveLocationMessage} LiveLocationMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LiveLocationMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LiveLocationMessage message.
             * @function verify
             * @memberof proto.Message.LiveLocationMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LiveLocationMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.degreesLatitude != null && Object.hasOwnProperty.call(message, "degreesLatitude")) {
                    properties._degreesLatitude = 1;
                    if (typeof message.degreesLatitude !== "number")
                        return "degreesLatitude: number expected";
                }
                if (message.degreesLongitude != null && Object.hasOwnProperty.call(message, "degreesLongitude")) {
                    properties._degreesLongitude = 1;
                    if (typeof message.degreesLongitude !== "number")
                        return "degreesLongitude: number expected";
                }
                if (message.accuracyInMeters != null && Object.hasOwnProperty.call(message, "accuracyInMeters")) {
                    properties._accuracyInMeters = 1;
                    if (!$util.isInteger(message.accuracyInMeters))
                        return "accuracyInMeters: integer expected";
                }
                if (message.speedInMps != null && Object.hasOwnProperty.call(message, "speedInMps")) {
                    properties._speedInMps = 1;
                    if (typeof message.speedInMps !== "number")
                        return "speedInMps: number expected";
                }
                if (message.degreesClockwiseFromMagneticNorth != null && Object.hasOwnProperty.call(message, "degreesClockwiseFromMagneticNorth")) {
                    properties._degreesClockwiseFromMagneticNorth = 1;
                    if (!$util.isInteger(message.degreesClockwiseFromMagneticNorth))
                        return "degreesClockwiseFromMagneticNorth: integer expected";
                }
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption")) {
                    properties._caption = 1;
                    if (!$util.isString(message.caption))
                        return "caption: string expected";
                }
                if (message.sequenceNumber != null && Object.hasOwnProperty.call(message, "sequenceNumber")) {
                    properties._sequenceNumber = 1;
                    if (!$util.isInteger(message.sequenceNumber) && !(message.sequenceNumber && $util.isInteger(message.sequenceNumber.low) && $util.isInteger(message.sequenceNumber.high)))
                        return "sequenceNumber: integer|Long expected";
                }
                if (message.timeOffset != null && Object.hasOwnProperty.call(message, "timeOffset")) {
                    properties._timeOffset = 1;
                    if (!$util.isInteger(message.timeOffset))
                        return "timeOffset: integer expected";
                }
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail")) {
                    properties._jpegThumbnail = 1;
                    if (!(message.jpegThumbnail && typeof message.jpegThumbnail.length === "number" || $util.isString(message.jpegThumbnail)))
                        return "jpegThumbnail: buffer expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a LiveLocationMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.LiveLocationMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.LiveLocationMessage} LiveLocationMessage
             */
            LiveLocationMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.LiveLocationMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.degreesLatitude != null)
                    message.degreesLatitude = Number(object.degreesLatitude);
                if (object.degreesLongitude != null)
                    message.degreesLongitude = Number(object.degreesLongitude);
                if (object.accuracyInMeters != null)
                    message.accuracyInMeters = object.accuracyInMeters >>> 0;
                if (object.speedInMps != null)
                    message.speedInMps = Number(object.speedInMps);
                if (object.degreesClockwiseFromMagneticNorth != null)
                    message.degreesClockwiseFromMagneticNorth = object.degreesClockwiseFromMagneticNorth >>> 0;
                if (object.caption != null)
                    message.caption = String(object.caption);
                if (object.sequenceNumber != null)
                    if ($util.Long)
                        message.sequenceNumber = $util.Long.fromValue(object.sequenceNumber, false);
                    else if (typeof object.sequenceNumber === "string")
                        message.sequenceNumber = parseInt(object.sequenceNumber, 10);
                    else if (typeof object.sequenceNumber === "number")
                        message.sequenceNumber = object.sequenceNumber;
                    else if (typeof object.sequenceNumber === "object")
                        message.sequenceNumber = new $util.LongBits(object.sequenceNumber.low >>> 0, object.sequenceNumber.high >>> 0).toNumber();
                if (object.timeOffset != null)
                    message.timeOffset = object.timeOffset >>> 0;
                if (object.jpegThumbnail != null)
                    if (typeof object.jpegThumbnail === "string")
                        $util.base64.decode(object.jpegThumbnail, message.jpegThumbnail = $util.newBuffer($util.base64.length(object.jpegThumbnail)), 0);
                    else if (object.jpegThumbnail.length >= 0)
                        message.jpegThumbnail = object.jpegThumbnail;
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.LiveLocationMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a LiveLocationMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.LiveLocationMessage
             * @static
             * @param {proto.Message.LiveLocationMessage} message LiveLocationMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LiveLocationMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.degreesLatitude != null && Object.hasOwnProperty.call(message, "degreesLatitude"))
                    object.degreesLatitude = options.json && !isFinite(message.degreesLatitude) ? String(message.degreesLatitude) : message.degreesLatitude;
                if (message.degreesLongitude != null && Object.hasOwnProperty.call(message, "degreesLongitude"))
                    object.degreesLongitude = options.json && !isFinite(message.degreesLongitude) ? String(message.degreesLongitude) : message.degreesLongitude;
                if (message.accuracyInMeters != null && Object.hasOwnProperty.call(message, "accuracyInMeters"))
                    object.accuracyInMeters = message.accuracyInMeters;
                if (message.speedInMps != null && Object.hasOwnProperty.call(message, "speedInMps"))
                    object.speedInMps = options.json && !isFinite(message.speedInMps) ? String(message.speedInMps) : message.speedInMps;
                if (message.degreesClockwiseFromMagneticNorth != null && Object.hasOwnProperty.call(message, "degreesClockwiseFromMagneticNorth"))
                    object.degreesClockwiseFromMagneticNorth = message.degreesClockwiseFromMagneticNorth;
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    object.caption = message.caption;
                if (message.sequenceNumber != null && Object.hasOwnProperty.call(message, "sequenceNumber"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.sequenceNumber = typeof message.sequenceNumber === "number" ? BigInt(message.sequenceNumber) : $util.Long.fromBits(message.sequenceNumber.low >>> 0, message.sequenceNumber.high >>> 0, false).toBigInt();
                    else if (typeof message.sequenceNumber === "number")
                        object.sequenceNumber = options.longs === String ? String(message.sequenceNumber) : message.sequenceNumber;
                    else
                        object.sequenceNumber = options.longs === String ? $util.Long.prototype.toString.call(message.sequenceNumber) : options.longs === Number ? new $util.LongBits(message.sequenceNumber.low >>> 0, message.sequenceNumber.high >>> 0).toNumber() : message.sequenceNumber;
                if (message.timeOffset != null && Object.hasOwnProperty.call(message, "timeOffset"))
                    object.timeOffset = message.timeOffset;
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    object.jpegThumbnail = options.bytes === String ? $util.base64.encode(message.jpegThumbnail, 0, message.jpegThumbnail.length) : options.bytes === Array ? Array.prototype.slice.call(message.jpegThumbnail) : message.jpegThumbnail;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                return object;
            };

            /**
             * Converts this LiveLocationMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.LiveLocationMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LiveLocationMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for LiveLocationMessage
             * @function getTypeUrl
             * @memberof proto.Message.LiveLocationMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            LiveLocationMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.LiveLocationMessage";
            };

            return LiveLocationMessage;
        })();

        Message.LocationMessage = (function() {

            /**
             * Properties of a LocationMessage.
             * @memberof proto.Message
             * @interface ILocationMessage
             * @property {number|null} [degreesLatitude] LocationMessage degreesLatitude
             * @property {number|null} [degreesLongitude] LocationMessage degreesLongitude
             * @property {string|null} [name] LocationMessage name
             * @property {string|null} [address] LocationMessage address
             * @property {string|null} [url] LocationMessage url
             * @property {boolean|null} [isLive] LocationMessage isLive
             * @property {number|null} [accuracyInMeters] LocationMessage accuracyInMeters
             * @property {number|null} [speedInMps] LocationMessage speedInMps
             * @property {number|null} [degreesClockwiseFromMagneticNorth] LocationMessage degreesClockwiseFromMagneticNorth
             * @property {string|null} [comment] LocationMessage comment
             * @property {Uint8Array|null} [jpegThumbnail] LocationMessage jpegThumbnail
             * @property {proto.IContextInfo|null} [contextInfo] LocationMessage contextInfo
             */

            /**
             * Constructs a new LocationMessage.
             * @memberof proto.Message
             * @classdesc Represents a LocationMessage.
             * @implements ILocationMessage
             * @constructor
             * @param {proto.Message.ILocationMessage=} [properties] Properties to set
             */
            function LocationMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * LocationMessage degreesLatitude.
             * @member {number|null|undefined} degreesLatitude
             * @memberof proto.Message.LocationMessage
             * @instance
             */
            LocationMessage.prototype.degreesLatitude = null;

            /**
             * LocationMessage degreesLongitude.
             * @member {number|null|undefined} degreesLongitude
             * @memberof proto.Message.LocationMessage
             * @instance
             */
            LocationMessage.prototype.degreesLongitude = null;

            /**
             * LocationMessage name.
             * @member {string|null|undefined} name
             * @memberof proto.Message.LocationMessage
             * @instance
             */
            LocationMessage.prototype.name = null;

            /**
             * LocationMessage address.
             * @member {string|null|undefined} address
             * @memberof proto.Message.LocationMessage
             * @instance
             */
            LocationMessage.prototype.address = null;

            /**
             * LocationMessage url.
             * @member {string|null|undefined} url
             * @memberof proto.Message.LocationMessage
             * @instance
             */
            LocationMessage.prototype.url = null;

            /**
             * LocationMessage isLive.
             * @member {boolean|null|undefined} isLive
             * @memberof proto.Message.LocationMessage
             * @instance
             */
            LocationMessage.prototype.isLive = null;

            /**
             * LocationMessage accuracyInMeters.
             * @member {number|null|undefined} accuracyInMeters
             * @memberof proto.Message.LocationMessage
             * @instance
             */
            LocationMessage.prototype.accuracyInMeters = null;

            /**
             * LocationMessage speedInMps.
             * @member {number|null|undefined} speedInMps
             * @memberof proto.Message.LocationMessage
             * @instance
             */
            LocationMessage.prototype.speedInMps = null;

            /**
             * LocationMessage degreesClockwiseFromMagneticNorth.
             * @member {number|null|undefined} degreesClockwiseFromMagneticNorth
             * @memberof proto.Message.LocationMessage
             * @instance
             */
            LocationMessage.prototype.degreesClockwiseFromMagneticNorth = null;

            /**
             * LocationMessage comment.
             * @member {string|null|undefined} comment
             * @memberof proto.Message.LocationMessage
             * @instance
             */
            LocationMessage.prototype.comment = null;

            /**
             * LocationMessage jpegThumbnail.
             * @member {Uint8Array|null|undefined} jpegThumbnail
             * @memberof proto.Message.LocationMessage
             * @instance
             */
            LocationMessage.prototype.jpegThumbnail = null;

            /**
             * LocationMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.LocationMessage
             * @instance
             */
            LocationMessage.prototype.contextInfo = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LocationMessage.prototype, "_degreesLatitude", {
                get: $util.oneOfGetter($oneOfFields = ["degreesLatitude"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LocationMessage.prototype, "_degreesLongitude", {
                get: $util.oneOfGetter($oneOfFields = ["degreesLongitude"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LocationMessage.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LocationMessage.prototype, "_address", {
                get: $util.oneOfGetter($oneOfFields = ["address"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LocationMessage.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LocationMessage.prototype, "_isLive", {
                get: $util.oneOfGetter($oneOfFields = ["isLive"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LocationMessage.prototype, "_accuracyInMeters", {
                get: $util.oneOfGetter($oneOfFields = ["accuracyInMeters"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LocationMessage.prototype, "_speedInMps", {
                get: $util.oneOfGetter($oneOfFields = ["speedInMps"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LocationMessage.prototype, "_degreesClockwiseFromMagneticNorth", {
                get: $util.oneOfGetter($oneOfFields = ["degreesClockwiseFromMagneticNorth"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LocationMessage.prototype, "_comment", {
                get: $util.oneOfGetter($oneOfFields = ["comment"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LocationMessage.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LocationMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new LocationMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.LocationMessage
             * @static
             * @param {proto.Message.ILocationMessage=} [properties] Properties to set
             * @returns {proto.Message.LocationMessage} LocationMessage instance
             */
            LocationMessage.create = function create(properties) {
                return new LocationMessage(properties);
            };

            /**
             * Encodes the specified LocationMessage message. Does not implicitly {@link proto.Message.LocationMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.LocationMessage
             * @static
             * @param {proto.Message.ILocationMessage} message LocationMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LocationMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.degreesLatitude != null && Object.hasOwnProperty.call(message, "degreesLatitude"))
                    writer.uint32(/* id 1, wireType 1 =*/9).double(message.degreesLatitude);
                if (message.degreesLongitude != null && Object.hasOwnProperty.call(message, "degreesLongitude"))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.degreesLongitude);
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.name);
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.address);
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.url);
                if (message.isLive != null && Object.hasOwnProperty.call(message, "isLive"))
                    writer.uint32(/* id 6, wireType 0 =*/48).bool(message.isLive);
                if (message.accuracyInMeters != null && Object.hasOwnProperty.call(message, "accuracyInMeters"))
                    writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.accuracyInMeters);
                if (message.speedInMps != null && Object.hasOwnProperty.call(message, "speedInMps"))
                    writer.uint32(/* id 8, wireType 5 =*/69).float(message.speedInMps);
                if (message.degreesClockwiseFromMagneticNorth != null && Object.hasOwnProperty.call(message, "degreesClockwiseFromMagneticNorth"))
                    writer.uint32(/* id 9, wireType 0 =*/72).uint32(message.degreesClockwiseFromMagneticNorth);
                if (message.comment != null && Object.hasOwnProperty.call(message, "comment"))
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.comment);
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    writer.uint32(/* id 16, wireType 2 =*/130).bytes(message.jpegThumbnail);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LocationMessage message, length delimited. Does not implicitly {@link proto.Message.LocationMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.LocationMessage
             * @static
             * @param {proto.Message.ILocationMessage} message LocationMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LocationMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a LocationMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.LocationMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.LocationMessage} LocationMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LocationMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 1)
                                break;
                            message.degreesLatitude = reader.double();
                            message._degreesLatitude = "degreesLatitude";
                            continue;
                        }
                    case 2: {
                            if (u !== 1)
                                break;
                            message.degreesLongitude = reader.double();
                            message._degreesLongitude = "degreesLongitude";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.address = reader.stringVerify();
                            message._address = "address";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.url = reader.stringVerify();
                            message._url = "url";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.isLive = reader.bool();
                            message._isLive = "isLive";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            message.accuracyInMeters = reader.uint32();
                            message._accuracyInMeters = "accuracyInMeters";
                            continue;
                        }
                    case 8: {
                            if (u !== 5)
                                break;
                            message.speedInMps = reader.float();
                            message._speedInMps = "speedInMps";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            message.degreesClockwiseFromMagneticNorth = reader.uint32();
                            message._degreesClockwiseFromMagneticNorth = "degreesClockwiseFromMagneticNorth";
                            continue;
                        }
                    case 11: {
                            if (u !== 2)
                                break;
                            message.comment = reader.stringVerify();
                            message._comment = "comment";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            message.jpegThumbnail = reader.bytes();
                            message._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a LocationMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.LocationMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.LocationMessage} LocationMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LocationMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LocationMessage message.
             * @function verify
             * @memberof proto.Message.LocationMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LocationMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.degreesLatitude != null && Object.hasOwnProperty.call(message, "degreesLatitude")) {
                    properties._degreesLatitude = 1;
                    if (typeof message.degreesLatitude !== "number")
                        return "degreesLatitude: number expected";
                }
                if (message.degreesLongitude != null && Object.hasOwnProperty.call(message, "degreesLongitude")) {
                    properties._degreesLongitude = 1;
                    if (typeof message.degreesLongitude !== "number")
                        return "degreesLongitude: number expected";
                }
                if (message.name != null && Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                if (message.address != null && Object.hasOwnProperty.call(message, "address")) {
                    properties._address = 1;
                    if (!$util.isString(message.address))
                        return "address: string expected";
                }
                if (message.url != null && Object.hasOwnProperty.call(message, "url")) {
                    properties._url = 1;
                    if (!$util.isString(message.url))
                        return "url: string expected";
                }
                if (message.isLive != null && Object.hasOwnProperty.call(message, "isLive")) {
                    properties._isLive = 1;
                    if (typeof message.isLive !== "boolean")
                        return "isLive: boolean expected";
                }
                if (message.accuracyInMeters != null && Object.hasOwnProperty.call(message, "accuracyInMeters")) {
                    properties._accuracyInMeters = 1;
                    if (!$util.isInteger(message.accuracyInMeters))
                        return "accuracyInMeters: integer expected";
                }
                if (message.speedInMps != null && Object.hasOwnProperty.call(message, "speedInMps")) {
                    properties._speedInMps = 1;
                    if (typeof message.speedInMps !== "number")
                        return "speedInMps: number expected";
                }
                if (message.degreesClockwiseFromMagneticNorth != null && Object.hasOwnProperty.call(message, "degreesClockwiseFromMagneticNorth")) {
                    properties._degreesClockwiseFromMagneticNorth = 1;
                    if (!$util.isInteger(message.degreesClockwiseFromMagneticNorth))
                        return "degreesClockwiseFromMagneticNorth: integer expected";
                }
                if (message.comment != null && Object.hasOwnProperty.call(message, "comment")) {
                    properties._comment = 1;
                    if (!$util.isString(message.comment))
                        return "comment: string expected";
                }
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail")) {
                    properties._jpegThumbnail = 1;
                    if (!(message.jpegThumbnail && typeof message.jpegThumbnail.length === "number" || $util.isString(message.jpegThumbnail)))
                        return "jpegThumbnail: buffer expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a LocationMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.LocationMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.LocationMessage} LocationMessage
             */
            LocationMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.LocationMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.degreesLatitude != null)
                    message.degreesLatitude = Number(object.degreesLatitude);
                if (object.degreesLongitude != null)
                    message.degreesLongitude = Number(object.degreesLongitude);
                if (object.name != null)
                    message.name = String(object.name);
                if (object.address != null)
                    message.address = String(object.address);
                if (object.url != null)
                    message.url = String(object.url);
                if (object.isLive != null)
                    message.isLive = Boolean(object.isLive);
                if (object.accuracyInMeters != null)
                    message.accuracyInMeters = object.accuracyInMeters >>> 0;
                if (object.speedInMps != null)
                    message.speedInMps = Number(object.speedInMps);
                if (object.degreesClockwiseFromMagneticNorth != null)
                    message.degreesClockwiseFromMagneticNorth = object.degreesClockwiseFromMagneticNorth >>> 0;
                if (object.comment != null)
                    message.comment = String(object.comment);
                if (object.jpegThumbnail != null)
                    if (typeof object.jpegThumbnail === "string")
                        $util.base64.decode(object.jpegThumbnail, message.jpegThumbnail = $util.newBuffer($util.base64.length(object.jpegThumbnail)), 0);
                    else if (object.jpegThumbnail.length >= 0)
                        message.jpegThumbnail = object.jpegThumbnail;
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.LocationMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a LocationMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.LocationMessage
             * @static
             * @param {proto.Message.LocationMessage} message LocationMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LocationMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.degreesLatitude != null && Object.hasOwnProperty.call(message, "degreesLatitude"))
                    object.degreesLatitude = options.json && !isFinite(message.degreesLatitude) ? String(message.degreesLatitude) : message.degreesLatitude;
                if (message.degreesLongitude != null && Object.hasOwnProperty.call(message, "degreesLongitude"))
                    object.degreesLongitude = options.json && !isFinite(message.degreesLongitude) ? String(message.degreesLongitude) : message.degreesLongitude;
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    object.address = message.address;
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    object.url = message.url;
                if (message.isLive != null && Object.hasOwnProperty.call(message, "isLive"))
                    object.isLive = message.isLive;
                if (message.accuracyInMeters != null && Object.hasOwnProperty.call(message, "accuracyInMeters"))
                    object.accuracyInMeters = message.accuracyInMeters;
                if (message.speedInMps != null && Object.hasOwnProperty.call(message, "speedInMps"))
                    object.speedInMps = options.json && !isFinite(message.speedInMps) ? String(message.speedInMps) : message.speedInMps;
                if (message.degreesClockwiseFromMagneticNorth != null && Object.hasOwnProperty.call(message, "degreesClockwiseFromMagneticNorth"))
                    object.degreesClockwiseFromMagneticNorth = message.degreesClockwiseFromMagneticNorth;
                if (message.comment != null && Object.hasOwnProperty.call(message, "comment"))
                    object.comment = message.comment;
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    object.jpegThumbnail = options.bytes === String ? $util.base64.encode(message.jpegThumbnail, 0, message.jpegThumbnail.length) : options.bytes === Array ? Array.prototype.slice.call(message.jpegThumbnail) : message.jpegThumbnail;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                return object;
            };

            /**
             * Converts this LocationMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.LocationMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LocationMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for LocationMessage
             * @function getTypeUrl
             * @memberof proto.Message.LocationMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            LocationMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.LocationMessage";
            };

            return LocationMessage;
        })();

        Message.MMSThumbnailMetadata = (function() {

            /**
             * Properties of a MMSThumbnailMetadata.
             * @memberof proto.Message
             * @interface IMMSThumbnailMetadata
             * @property {string|null} [thumbnailDirectPath] MMSThumbnailMetadata thumbnailDirectPath
             * @property {Uint8Array|null} [thumbnailSha256] MMSThumbnailMetadata thumbnailSha256
             * @property {Uint8Array|null} [thumbnailEncSha256] MMSThumbnailMetadata thumbnailEncSha256
             * @property {Uint8Array|null} [mediaKey] MMSThumbnailMetadata mediaKey
             * @property {number|Long|null} [mediaKeyTimestamp] MMSThumbnailMetadata mediaKeyTimestamp
             * @property {number|null} [thumbnailHeight] MMSThumbnailMetadata thumbnailHeight
             * @property {number|null} [thumbnailWidth] MMSThumbnailMetadata thumbnailWidth
             */

            /**
             * Constructs a new MMSThumbnailMetadata.
             * @memberof proto.Message
             * @classdesc Represents a MMSThumbnailMetadata.
             * @implements IMMSThumbnailMetadata
             * @constructor
             * @param {proto.Message.IMMSThumbnailMetadata=} [properties] Properties to set
             */
            function MMSThumbnailMetadata(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MMSThumbnailMetadata thumbnailDirectPath.
             * @member {string|null|undefined} thumbnailDirectPath
             * @memberof proto.Message.MMSThumbnailMetadata
             * @instance
             */
            MMSThumbnailMetadata.prototype.thumbnailDirectPath = null;

            /**
             * MMSThumbnailMetadata thumbnailSha256.
             * @member {Uint8Array|null|undefined} thumbnailSha256
             * @memberof proto.Message.MMSThumbnailMetadata
             * @instance
             */
            MMSThumbnailMetadata.prototype.thumbnailSha256 = null;

            /**
             * MMSThumbnailMetadata thumbnailEncSha256.
             * @member {Uint8Array|null|undefined} thumbnailEncSha256
             * @memberof proto.Message.MMSThumbnailMetadata
             * @instance
             */
            MMSThumbnailMetadata.prototype.thumbnailEncSha256 = null;

            /**
             * MMSThumbnailMetadata mediaKey.
             * @member {Uint8Array|null|undefined} mediaKey
             * @memberof proto.Message.MMSThumbnailMetadata
             * @instance
             */
            MMSThumbnailMetadata.prototype.mediaKey = null;

            /**
             * MMSThumbnailMetadata mediaKeyTimestamp.
             * @member {number|Long|null|undefined} mediaKeyTimestamp
             * @memberof proto.Message.MMSThumbnailMetadata
             * @instance
             */
            MMSThumbnailMetadata.prototype.mediaKeyTimestamp = null;

            /**
             * MMSThumbnailMetadata thumbnailHeight.
             * @member {number|null|undefined} thumbnailHeight
             * @memberof proto.Message.MMSThumbnailMetadata
             * @instance
             */
            MMSThumbnailMetadata.prototype.thumbnailHeight = null;

            /**
             * MMSThumbnailMetadata thumbnailWidth.
             * @member {number|null|undefined} thumbnailWidth
             * @memberof proto.Message.MMSThumbnailMetadata
             * @instance
             */
            MMSThumbnailMetadata.prototype.thumbnailWidth = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MMSThumbnailMetadata.prototype, "_thumbnailDirectPath", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailDirectPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MMSThumbnailMetadata.prototype, "_thumbnailSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MMSThumbnailMetadata.prototype, "_thumbnailEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MMSThumbnailMetadata.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MMSThumbnailMetadata.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MMSThumbnailMetadata.prototype, "_thumbnailHeight", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailHeight"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MMSThumbnailMetadata.prototype, "_thumbnailWidth", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailWidth"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new MMSThumbnailMetadata instance using the specified properties.
             * @function create
             * @memberof proto.Message.MMSThumbnailMetadata
             * @static
             * @param {proto.Message.IMMSThumbnailMetadata=} [properties] Properties to set
             * @returns {proto.Message.MMSThumbnailMetadata} MMSThumbnailMetadata instance
             */
            MMSThumbnailMetadata.create = function create(properties) {
                return new MMSThumbnailMetadata(properties);
            };

            /**
             * Encodes the specified MMSThumbnailMetadata message. Does not implicitly {@link proto.Message.MMSThumbnailMetadata.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.MMSThumbnailMetadata
             * @static
             * @param {proto.Message.IMMSThumbnailMetadata} message MMSThumbnailMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MMSThumbnailMetadata.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.thumbnailDirectPath != null && Object.hasOwnProperty.call(message, "thumbnailDirectPath"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.thumbnailDirectPath);
                if (message.thumbnailSha256 != null && Object.hasOwnProperty.call(message, "thumbnailSha256"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.thumbnailSha256);
                if (message.thumbnailEncSha256 != null && Object.hasOwnProperty.call(message, "thumbnailEncSha256"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.thumbnailEncSha256);
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.mediaKey);
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.mediaKeyTimestamp);
                if (message.thumbnailHeight != null && Object.hasOwnProperty.call(message, "thumbnailHeight"))
                    writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.thumbnailHeight);
                if (message.thumbnailWidth != null && Object.hasOwnProperty.call(message, "thumbnailWidth"))
                    writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.thumbnailWidth);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MMSThumbnailMetadata message, length delimited. Does not implicitly {@link proto.Message.MMSThumbnailMetadata.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.MMSThumbnailMetadata
             * @static
             * @param {proto.Message.IMMSThumbnailMetadata} message MMSThumbnailMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MMSThumbnailMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a MMSThumbnailMetadata message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.MMSThumbnailMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.MMSThumbnailMetadata} MMSThumbnailMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MMSThumbnailMetadata.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.thumbnailDirectPath = reader.stringVerify();
                            message._thumbnailDirectPath = "thumbnailDirectPath";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.thumbnailSha256 = reader.bytes();
                            message._thumbnailSha256 = "thumbnailSha256";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.thumbnailEncSha256 = reader.bytes();
                            message._thumbnailEncSha256 = "thumbnailEncSha256";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.mediaKey = reader.bytes();
                            message._mediaKey = "mediaKey";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.mediaKeyTimestamp = reader.int64();
                            message._mediaKeyTimestamp = "mediaKeyTimestamp";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.thumbnailHeight = reader.uint32();
                            message._thumbnailHeight = "thumbnailHeight";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            message.thumbnailWidth = reader.uint32();
                            message._thumbnailWidth = "thumbnailWidth";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a MMSThumbnailMetadata message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.MMSThumbnailMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.MMSThumbnailMetadata} MMSThumbnailMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MMSThumbnailMetadata.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MMSThumbnailMetadata message.
             * @function verify
             * @memberof proto.Message.MMSThumbnailMetadata
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MMSThumbnailMetadata.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.thumbnailDirectPath != null && Object.hasOwnProperty.call(message, "thumbnailDirectPath")) {
                    properties._thumbnailDirectPath = 1;
                    if (!$util.isString(message.thumbnailDirectPath))
                        return "thumbnailDirectPath: string expected";
                }
                if (message.thumbnailSha256 != null && Object.hasOwnProperty.call(message, "thumbnailSha256")) {
                    properties._thumbnailSha256 = 1;
                    if (!(message.thumbnailSha256 && typeof message.thumbnailSha256.length === "number" || $util.isString(message.thumbnailSha256)))
                        return "thumbnailSha256: buffer expected";
                }
                if (message.thumbnailEncSha256 != null && Object.hasOwnProperty.call(message, "thumbnailEncSha256")) {
                    properties._thumbnailEncSha256 = 1;
                    if (!(message.thumbnailEncSha256 && typeof message.thumbnailEncSha256.length === "number" || $util.isString(message.thumbnailEncSha256)))
                        return "thumbnailEncSha256: buffer expected";
                }
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey")) {
                    properties._mediaKey = 1;
                    if (!(message.mediaKey && typeof message.mediaKey.length === "number" || $util.isString(message.mediaKey)))
                        return "mediaKey: buffer expected";
                }
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp")) {
                    properties._mediaKeyTimestamp = 1;
                    if (!$util.isInteger(message.mediaKeyTimestamp) && !(message.mediaKeyTimestamp && $util.isInteger(message.mediaKeyTimestamp.low) && $util.isInteger(message.mediaKeyTimestamp.high)))
                        return "mediaKeyTimestamp: integer|Long expected";
                }
                if (message.thumbnailHeight != null && Object.hasOwnProperty.call(message, "thumbnailHeight")) {
                    properties._thumbnailHeight = 1;
                    if (!$util.isInteger(message.thumbnailHeight))
                        return "thumbnailHeight: integer expected";
                }
                if (message.thumbnailWidth != null && Object.hasOwnProperty.call(message, "thumbnailWidth")) {
                    properties._thumbnailWidth = 1;
                    if (!$util.isInteger(message.thumbnailWidth))
                        return "thumbnailWidth: integer expected";
                }
                return null;
            };

            /**
             * Creates a MMSThumbnailMetadata message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.MMSThumbnailMetadata
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.MMSThumbnailMetadata} MMSThumbnailMetadata
             */
            MMSThumbnailMetadata.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.MMSThumbnailMetadata: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.thumbnailDirectPath != null)
                    message.thumbnailDirectPath = String(object.thumbnailDirectPath);
                if (object.thumbnailSha256 != null)
                    if (typeof object.thumbnailSha256 === "string")
                        $util.base64.decode(object.thumbnailSha256, message.thumbnailSha256 = $util.newBuffer($util.base64.length(object.thumbnailSha256)), 0);
                    else if (object.thumbnailSha256.length >= 0)
                        message.thumbnailSha256 = object.thumbnailSha256;
                if (object.thumbnailEncSha256 != null)
                    if (typeof object.thumbnailEncSha256 === "string")
                        $util.base64.decode(object.thumbnailEncSha256, message.thumbnailEncSha256 = $util.newBuffer($util.base64.length(object.thumbnailEncSha256)), 0);
                    else if (object.thumbnailEncSha256.length >= 0)
                        message.thumbnailEncSha256 = object.thumbnailEncSha256;
                if (object.mediaKey != null)
                    if (typeof object.mediaKey === "string")
                        $util.base64.decode(object.mediaKey, message.mediaKey = $util.newBuffer($util.base64.length(object.mediaKey)), 0);
                    else if (object.mediaKey.length >= 0)
                        message.mediaKey = object.mediaKey;
                if (object.mediaKeyTimestamp != null)
                    if ($util.Long)
                        message.mediaKeyTimestamp = $util.Long.fromValue(object.mediaKeyTimestamp, false);
                    else if (typeof object.mediaKeyTimestamp === "string")
                        message.mediaKeyTimestamp = parseInt(object.mediaKeyTimestamp, 10);
                    else if (typeof object.mediaKeyTimestamp === "number")
                        message.mediaKeyTimestamp = object.mediaKeyTimestamp;
                    else if (typeof object.mediaKeyTimestamp === "object")
                        message.mediaKeyTimestamp = new $util.LongBits(object.mediaKeyTimestamp.low >>> 0, object.mediaKeyTimestamp.high >>> 0).toNumber();
                if (object.thumbnailHeight != null)
                    message.thumbnailHeight = object.thumbnailHeight >>> 0;
                if (object.thumbnailWidth != null)
                    message.thumbnailWidth = object.thumbnailWidth >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a MMSThumbnailMetadata message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.MMSThumbnailMetadata
             * @static
             * @param {proto.Message.MMSThumbnailMetadata} message MMSThumbnailMetadata
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MMSThumbnailMetadata.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.thumbnailDirectPath != null && Object.hasOwnProperty.call(message, "thumbnailDirectPath"))
                    object.thumbnailDirectPath = message.thumbnailDirectPath;
                if (message.thumbnailSha256 != null && Object.hasOwnProperty.call(message, "thumbnailSha256"))
                    object.thumbnailSha256 = options.bytes === String ? $util.base64.encode(message.thumbnailSha256, 0, message.thumbnailSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.thumbnailSha256) : message.thumbnailSha256;
                if (message.thumbnailEncSha256 != null && Object.hasOwnProperty.call(message, "thumbnailEncSha256"))
                    object.thumbnailEncSha256 = options.bytes === String ? $util.base64.encode(message.thumbnailEncSha256, 0, message.thumbnailEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.thumbnailEncSha256) : message.thumbnailEncSha256;
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    object.mediaKey = options.bytes === String ? $util.base64.encode(message.mediaKey, 0, message.mediaKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.mediaKey) : message.mediaKey;
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.mediaKeyTimestamp = typeof message.mediaKeyTimestamp === "number" ? BigInt(message.mediaKeyTimestamp) : $util.Long.fromBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.mediaKeyTimestamp === "number")
                        object.mediaKeyTimestamp = options.longs === String ? String(message.mediaKeyTimestamp) : message.mediaKeyTimestamp;
                    else
                        object.mediaKeyTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.mediaKeyTimestamp) : options.longs === Number ? new $util.LongBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0).toNumber() : message.mediaKeyTimestamp;
                if (message.thumbnailHeight != null && Object.hasOwnProperty.call(message, "thumbnailHeight"))
                    object.thumbnailHeight = message.thumbnailHeight;
                if (message.thumbnailWidth != null && Object.hasOwnProperty.call(message, "thumbnailWidth"))
                    object.thumbnailWidth = message.thumbnailWidth;
                return object;
            };

            /**
             * Converts this MMSThumbnailMetadata to JSON.
             * @function toJSON
             * @memberof proto.Message.MMSThumbnailMetadata
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MMSThumbnailMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MMSThumbnailMetadata
             * @function getTypeUrl
             * @memberof proto.Message.MMSThumbnailMetadata
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MMSThumbnailMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.MMSThumbnailMetadata";
            };

            return MMSThumbnailMetadata;
        })();

        Message.MessageHistoryBundle = (function() {

            /**
             * Properties of a MessageHistoryBundle.
             * @memberof proto.Message
             * @interface IMessageHistoryBundle
             * @property {string|null} [mimetype] MessageHistoryBundle mimetype
             * @property {Uint8Array|null} [fileSha256] MessageHistoryBundle fileSha256
             * @property {Uint8Array|null} [mediaKey] MessageHistoryBundle mediaKey
             * @property {Uint8Array|null} [fileEncSha256] MessageHistoryBundle fileEncSha256
             * @property {string|null} [directPath] MessageHistoryBundle directPath
             * @property {number|Long|null} [mediaKeyTimestamp] MessageHistoryBundle mediaKeyTimestamp
             * @property {proto.IContextInfo|null} [contextInfo] MessageHistoryBundle contextInfo
             * @property {proto.Message.IMessageHistoryMetadata|null} [messageHistoryMetadata] MessageHistoryBundle messageHistoryMetadata
             */

            /**
             * Constructs a new MessageHistoryBundle.
             * @memberof proto.Message
             * @classdesc Represents a MessageHistoryBundle.
             * @implements IMessageHistoryBundle
             * @constructor
             * @param {proto.Message.IMessageHistoryBundle=} [properties] Properties to set
             */
            function MessageHistoryBundle(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MessageHistoryBundle mimetype.
             * @member {string|null|undefined} mimetype
             * @memberof proto.Message.MessageHistoryBundle
             * @instance
             */
            MessageHistoryBundle.prototype.mimetype = null;

            /**
             * MessageHistoryBundle fileSha256.
             * @member {Uint8Array|null|undefined} fileSha256
             * @memberof proto.Message.MessageHistoryBundle
             * @instance
             */
            MessageHistoryBundle.prototype.fileSha256 = null;

            /**
             * MessageHistoryBundle mediaKey.
             * @member {Uint8Array|null|undefined} mediaKey
             * @memberof proto.Message.MessageHistoryBundle
             * @instance
             */
            MessageHistoryBundle.prototype.mediaKey = null;

            /**
             * MessageHistoryBundle fileEncSha256.
             * @member {Uint8Array|null|undefined} fileEncSha256
             * @memberof proto.Message.MessageHistoryBundle
             * @instance
             */
            MessageHistoryBundle.prototype.fileEncSha256 = null;

            /**
             * MessageHistoryBundle directPath.
             * @member {string|null|undefined} directPath
             * @memberof proto.Message.MessageHistoryBundle
             * @instance
             */
            MessageHistoryBundle.prototype.directPath = null;

            /**
             * MessageHistoryBundle mediaKeyTimestamp.
             * @member {number|Long|null|undefined} mediaKeyTimestamp
             * @memberof proto.Message.MessageHistoryBundle
             * @instance
             */
            MessageHistoryBundle.prototype.mediaKeyTimestamp = null;

            /**
             * MessageHistoryBundle contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.MessageHistoryBundle
             * @instance
             */
            MessageHistoryBundle.prototype.contextInfo = null;

            /**
             * MessageHistoryBundle messageHistoryMetadata.
             * @member {proto.Message.IMessageHistoryMetadata|null|undefined} messageHistoryMetadata
             * @memberof proto.Message.MessageHistoryBundle
             * @instance
             */
            MessageHistoryBundle.prototype.messageHistoryMetadata = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MessageHistoryBundle.prototype, "_mimetype", {
                get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MessageHistoryBundle.prototype, "_fileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MessageHistoryBundle.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MessageHistoryBundle.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MessageHistoryBundle.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MessageHistoryBundle.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MessageHistoryBundle.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MessageHistoryBundle.prototype, "_messageHistoryMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["messageHistoryMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new MessageHistoryBundle instance using the specified properties.
             * @function create
             * @memberof proto.Message.MessageHistoryBundle
             * @static
             * @param {proto.Message.IMessageHistoryBundle=} [properties] Properties to set
             * @returns {proto.Message.MessageHistoryBundle} MessageHistoryBundle instance
             */
            MessageHistoryBundle.create = function create(properties) {
                return new MessageHistoryBundle(properties);
            };

            /**
             * Encodes the specified MessageHistoryBundle message. Does not implicitly {@link proto.Message.MessageHistoryBundle.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.MessageHistoryBundle
             * @static
             * @param {proto.Message.IMessageHistoryBundle} message MessageHistoryBundle message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MessageHistoryBundle.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.mimetype);
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.fileSha256);
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.mediaKey);
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.fileEncSha256);
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.directPath);
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                    writer.uint32(/* id 6, wireType 0 =*/48).int64(message.mediaKeyTimestamp);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
                if (message.messageHistoryMetadata != null && Object.hasOwnProperty.call(message, "messageHistoryMetadata"))
                    $root.proto.Message.MessageHistoryMetadata.encode(message.messageHistoryMetadata, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MessageHistoryBundle message, length delimited. Does not implicitly {@link proto.Message.MessageHistoryBundle.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.MessageHistoryBundle
             * @static
             * @param {proto.Message.IMessageHistoryBundle} message MessageHistoryBundle message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MessageHistoryBundle.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a MessageHistoryBundle message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.MessageHistoryBundle
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.MessageHistoryBundle} MessageHistoryBundle
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MessageHistoryBundle.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.mimetype = reader.stringVerify();
                            message._mimetype = "mimetype";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.fileSha256 = reader.bytes();
                            message._fileSha256 = "fileSha256";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.mediaKey = reader.bytes();
                            message._mediaKey = "mediaKey";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.fileEncSha256 = reader.bytes();
                            message._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.directPath = reader.stringVerify();
                            message._directPath = "directPath";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.mediaKeyTimestamp = reader.int64();
                            message._mediaKeyTimestamp = "mediaKeyTimestamp";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.messageHistoryMetadata = $root.proto.Message.MessageHistoryMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.messageHistoryMetadata);
                            message._messageHistoryMetadata = "messageHistoryMetadata";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a MessageHistoryBundle message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.MessageHistoryBundle
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.MessageHistoryBundle} MessageHistoryBundle
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MessageHistoryBundle.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MessageHistoryBundle message.
             * @function verify
             * @memberof proto.Message.MessageHistoryBundle
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MessageHistoryBundle.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype")) {
                    properties._mimetype = 1;
                    if (!$util.isString(message.mimetype))
                        return "mimetype: string expected";
                }
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256")) {
                    properties._fileSha256 = 1;
                    if (!(message.fileSha256 && typeof message.fileSha256.length === "number" || $util.isString(message.fileSha256)))
                        return "fileSha256: buffer expected";
                }
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey")) {
                    properties._mediaKey = 1;
                    if (!(message.mediaKey && typeof message.mediaKey.length === "number" || $util.isString(message.mediaKey)))
                        return "mediaKey: buffer expected";
                }
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256")) {
                    properties._fileEncSha256 = 1;
                    if (!(message.fileEncSha256 && typeof message.fileEncSha256.length === "number" || $util.isString(message.fileEncSha256)))
                        return "fileEncSha256: buffer expected";
                }
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath")) {
                    properties._directPath = 1;
                    if (!$util.isString(message.directPath))
                        return "directPath: string expected";
                }
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp")) {
                    properties._mediaKeyTimestamp = 1;
                    if (!$util.isInteger(message.mediaKeyTimestamp) && !(message.mediaKeyTimestamp && $util.isInteger(message.mediaKeyTimestamp.low) && $util.isInteger(message.mediaKeyTimestamp.high)))
                        return "mediaKeyTimestamp: integer|Long expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.messageHistoryMetadata != null && Object.hasOwnProperty.call(message, "messageHistoryMetadata")) {
                    properties._messageHistoryMetadata = 1;
                    {
                        var error = $root.proto.Message.MessageHistoryMetadata.verify(message.messageHistoryMetadata, q + 1);
                        if (error)
                            return "messageHistoryMetadata." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a MessageHistoryBundle message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.MessageHistoryBundle
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.MessageHistoryBundle} MessageHistoryBundle
             */
            MessageHistoryBundle.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.MessageHistoryBundle: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.mimetype != null)
                    message.mimetype = String(object.mimetype);
                if (object.fileSha256 != null)
                    if (typeof object.fileSha256 === "string")
                        $util.base64.decode(object.fileSha256, message.fileSha256 = $util.newBuffer($util.base64.length(object.fileSha256)), 0);
                    else if (object.fileSha256.length >= 0)
                        message.fileSha256 = object.fileSha256;
                if (object.mediaKey != null)
                    if (typeof object.mediaKey === "string")
                        $util.base64.decode(object.mediaKey, message.mediaKey = $util.newBuffer($util.base64.length(object.mediaKey)), 0);
                    else if (object.mediaKey.length >= 0)
                        message.mediaKey = object.mediaKey;
                if (object.fileEncSha256 != null)
                    if (typeof object.fileEncSha256 === "string")
                        $util.base64.decode(object.fileEncSha256, message.fileEncSha256 = $util.newBuffer($util.base64.length(object.fileEncSha256)), 0);
                    else if (object.fileEncSha256.length >= 0)
                        message.fileEncSha256 = object.fileEncSha256;
                if (object.directPath != null)
                    message.directPath = String(object.directPath);
                if (object.mediaKeyTimestamp != null)
                    if ($util.Long)
                        message.mediaKeyTimestamp = $util.Long.fromValue(object.mediaKeyTimestamp, false);
                    else if (typeof object.mediaKeyTimestamp === "string")
                        message.mediaKeyTimestamp = parseInt(object.mediaKeyTimestamp, 10);
                    else if (typeof object.mediaKeyTimestamp === "number")
                        message.mediaKeyTimestamp = object.mediaKeyTimestamp;
                    else if (typeof object.mediaKeyTimestamp === "object")
                        message.mediaKeyTimestamp = new $util.LongBits(object.mediaKeyTimestamp.low >>> 0, object.mediaKeyTimestamp.high >>> 0).toNumber();
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.MessageHistoryBundle.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                if (object.messageHistoryMetadata != null) {
                    if (!$util.isObject(object.messageHistoryMetadata))
                        throw TypeError(".proto.Message.MessageHistoryBundle.messageHistoryMetadata: object expected");
                    message.messageHistoryMetadata = $root.proto.Message.MessageHistoryMetadata.fromObject(object.messageHistoryMetadata, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a MessageHistoryBundle message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.MessageHistoryBundle
             * @static
             * @param {proto.Message.MessageHistoryBundle} message MessageHistoryBundle
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MessageHistoryBundle.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                    object.mimetype = message.mimetype;
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                    object.fileSha256 = options.bytes === String ? $util.base64.encode(message.fileSha256, 0, message.fileSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileSha256) : message.fileSha256;
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    object.mediaKey = options.bytes === String ? $util.base64.encode(message.mediaKey, 0, message.mediaKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.mediaKey) : message.mediaKey;
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    object.fileEncSha256 = options.bytes === String ? $util.base64.encode(message.fileEncSha256, 0, message.fileEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileEncSha256) : message.fileEncSha256;
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    object.directPath = message.directPath;
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.mediaKeyTimestamp = typeof message.mediaKeyTimestamp === "number" ? BigInt(message.mediaKeyTimestamp) : $util.Long.fromBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.mediaKeyTimestamp === "number")
                        object.mediaKeyTimestamp = options.longs === String ? String(message.mediaKeyTimestamp) : message.mediaKeyTimestamp;
                    else
                        object.mediaKeyTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.mediaKeyTimestamp) : options.longs === Number ? new $util.LongBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0).toNumber() : message.mediaKeyTimestamp;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.messageHistoryMetadata != null && Object.hasOwnProperty.call(message, "messageHistoryMetadata"))
                    object.messageHistoryMetadata = $root.proto.Message.MessageHistoryMetadata.toObject(message.messageHistoryMetadata, options, q + 1);
                return object;
            };

            /**
             * Converts this MessageHistoryBundle to JSON.
             * @function toJSON
             * @memberof proto.Message.MessageHistoryBundle
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MessageHistoryBundle.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MessageHistoryBundle
             * @function getTypeUrl
             * @memberof proto.Message.MessageHistoryBundle
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MessageHistoryBundle.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.MessageHistoryBundle";
            };

            return MessageHistoryBundle;
        })();

        Message.MessageHistoryMetadata = (function() {

            /**
             * Properties of a MessageHistoryMetadata.
             * @memberof proto.Message
             * @interface IMessageHistoryMetadata
             * @property {Array.<string>|null} [historyReceivers] MessageHistoryMetadata historyReceivers
             * @property {number|Long|null} [oldestMessageTimestampInWindow] MessageHistoryMetadata oldestMessageTimestampInWindow
             * @property {number|Long|null} [messageCount] MessageHistoryMetadata messageCount
             * @property {Array.<string>|null} [nonHistoryReceivers] MessageHistoryMetadata nonHistoryReceivers
             * @property {number|Long|null} [oldestMessageTimestampInBundle] MessageHistoryMetadata oldestMessageTimestampInBundle
             */

            /**
             * Constructs a new MessageHistoryMetadata.
             * @memberof proto.Message
             * @classdesc Represents a MessageHistoryMetadata.
             * @implements IMessageHistoryMetadata
             * @constructor
             * @param {proto.Message.IMessageHistoryMetadata=} [properties] Properties to set
             */
            function MessageHistoryMetadata(properties) {
                this.historyReceivers = [];
                this.nonHistoryReceivers = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MessageHistoryMetadata historyReceivers.
             * @member {Array.<string>} historyReceivers
             * @memberof proto.Message.MessageHistoryMetadata
             * @instance
             */
            MessageHistoryMetadata.prototype.historyReceivers = $util.emptyArray;

            /**
             * MessageHistoryMetadata oldestMessageTimestampInWindow.
             * @member {number|Long|null|undefined} oldestMessageTimestampInWindow
             * @memberof proto.Message.MessageHistoryMetadata
             * @instance
             */
            MessageHistoryMetadata.prototype.oldestMessageTimestampInWindow = null;

            /**
             * MessageHistoryMetadata messageCount.
             * @member {number|Long|null|undefined} messageCount
             * @memberof proto.Message.MessageHistoryMetadata
             * @instance
             */
            MessageHistoryMetadata.prototype.messageCount = null;

            /**
             * MessageHistoryMetadata nonHistoryReceivers.
             * @member {Array.<string>} nonHistoryReceivers
             * @memberof proto.Message.MessageHistoryMetadata
             * @instance
             */
            MessageHistoryMetadata.prototype.nonHistoryReceivers = $util.emptyArray;

            /**
             * MessageHistoryMetadata oldestMessageTimestampInBundle.
             * @member {number|Long|null|undefined} oldestMessageTimestampInBundle
             * @memberof proto.Message.MessageHistoryMetadata
             * @instance
             */
            MessageHistoryMetadata.prototype.oldestMessageTimestampInBundle = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MessageHistoryMetadata.prototype, "_oldestMessageTimestampInWindow", {
                get: $util.oneOfGetter($oneOfFields = ["oldestMessageTimestampInWindow"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MessageHistoryMetadata.prototype, "_messageCount", {
                get: $util.oneOfGetter($oneOfFields = ["messageCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MessageHistoryMetadata.prototype, "_oldestMessageTimestampInBundle", {
                get: $util.oneOfGetter($oneOfFields = ["oldestMessageTimestampInBundle"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new MessageHistoryMetadata instance using the specified properties.
             * @function create
             * @memberof proto.Message.MessageHistoryMetadata
             * @static
             * @param {proto.Message.IMessageHistoryMetadata=} [properties] Properties to set
             * @returns {proto.Message.MessageHistoryMetadata} MessageHistoryMetadata instance
             */
            MessageHistoryMetadata.create = function create(properties) {
                return new MessageHistoryMetadata(properties);
            };

            /**
             * Encodes the specified MessageHistoryMetadata message. Does not implicitly {@link proto.Message.MessageHistoryMetadata.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.MessageHistoryMetadata
             * @static
             * @param {proto.Message.IMessageHistoryMetadata} message MessageHistoryMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MessageHistoryMetadata.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.historyReceivers != null && message.historyReceivers.length)
                    for (var i = 0; i < message.historyReceivers.length; ++i)
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.historyReceivers[i]);
                if (message.oldestMessageTimestampInWindow != null && Object.hasOwnProperty.call(message, "oldestMessageTimestampInWindow"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.oldestMessageTimestampInWindow);
                if (message.messageCount != null && Object.hasOwnProperty.call(message, "messageCount"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.messageCount);
                if (message.nonHistoryReceivers != null && message.nonHistoryReceivers.length)
                    for (var i = 0; i < message.nonHistoryReceivers.length; ++i)
                        writer.uint32(/* id 4, wireType 2 =*/34).string(message.nonHistoryReceivers[i]);
                if (message.oldestMessageTimestampInBundle != null && Object.hasOwnProperty.call(message, "oldestMessageTimestampInBundle"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.oldestMessageTimestampInBundle);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MessageHistoryMetadata message, length delimited. Does not implicitly {@link proto.Message.MessageHistoryMetadata.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.MessageHistoryMetadata
             * @static
             * @param {proto.Message.IMessageHistoryMetadata} message MessageHistoryMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MessageHistoryMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a MessageHistoryMetadata message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.MessageHistoryMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.MessageHistoryMetadata} MessageHistoryMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MessageHistoryMetadata.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(message.historyReceivers && message.historyReceivers.length))
                                message.historyReceivers = [];
                            message.historyReceivers.push(reader.stringVerify());
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.oldestMessageTimestampInWindow = reader.int64();
                            message._oldestMessageTimestampInWindow = "oldestMessageTimestampInWindow";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.messageCount = reader.int64();
                            message._messageCount = "messageCount";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            if (!(message.nonHistoryReceivers && message.nonHistoryReceivers.length))
                                message.nonHistoryReceivers = [];
                            message.nonHistoryReceivers.push(reader.stringVerify());
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.oldestMessageTimestampInBundle = reader.int64();
                            message._oldestMessageTimestampInBundle = "oldestMessageTimestampInBundle";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a MessageHistoryMetadata message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.MessageHistoryMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.MessageHistoryMetadata} MessageHistoryMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MessageHistoryMetadata.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MessageHistoryMetadata message.
             * @function verify
             * @memberof proto.Message.MessageHistoryMetadata
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MessageHistoryMetadata.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.historyReceivers != null && Object.hasOwnProperty.call(message, "historyReceivers")) {
                    if (!Array.isArray(message.historyReceivers))
                        return "historyReceivers: array expected";
                    for (var i = 0; i < message.historyReceivers.length; ++i)
                        if (!$util.isString(message.historyReceivers[i]))
                            return "historyReceivers: string[] expected";
                }
                if (message.oldestMessageTimestampInWindow != null && Object.hasOwnProperty.call(message, "oldestMessageTimestampInWindow")) {
                    properties._oldestMessageTimestampInWindow = 1;
                    if (!$util.isInteger(message.oldestMessageTimestampInWindow) && !(message.oldestMessageTimestampInWindow && $util.isInteger(message.oldestMessageTimestampInWindow.low) && $util.isInteger(message.oldestMessageTimestampInWindow.high)))
                        return "oldestMessageTimestampInWindow: integer|Long expected";
                }
                if (message.messageCount != null && Object.hasOwnProperty.call(message, "messageCount")) {
                    properties._messageCount = 1;
                    if (!$util.isInteger(message.messageCount) && !(message.messageCount && $util.isInteger(message.messageCount.low) && $util.isInteger(message.messageCount.high)))
                        return "messageCount: integer|Long expected";
                }
                if (message.nonHistoryReceivers != null && Object.hasOwnProperty.call(message, "nonHistoryReceivers")) {
                    if (!Array.isArray(message.nonHistoryReceivers))
                        return "nonHistoryReceivers: array expected";
                    for (var i = 0; i < message.nonHistoryReceivers.length; ++i)
                        if (!$util.isString(message.nonHistoryReceivers[i]))
                            return "nonHistoryReceivers: string[] expected";
                }
                if (message.oldestMessageTimestampInBundle != null && Object.hasOwnProperty.call(message, "oldestMessageTimestampInBundle")) {
                    properties._oldestMessageTimestampInBundle = 1;
                    if (!$util.isInteger(message.oldestMessageTimestampInBundle) && !(message.oldestMessageTimestampInBundle && $util.isInteger(message.oldestMessageTimestampInBundle.low) && $util.isInteger(message.oldestMessageTimestampInBundle.high)))
                        return "oldestMessageTimestampInBundle: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a MessageHistoryMetadata message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.MessageHistoryMetadata
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.MessageHistoryMetadata} MessageHistoryMetadata
             */
            MessageHistoryMetadata.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.MessageHistoryMetadata: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.historyReceivers) {
                    if (!Array.isArray(object.historyReceivers))
                        throw TypeError(".proto.Message.MessageHistoryMetadata.historyReceivers: array expected");
                    message.historyReceivers = Array(object.historyReceivers.length);
                    for (var i = 0; i < object.historyReceivers.length; ++i)
                        message.historyReceivers[i] = String(object.historyReceivers[i]);
                }
                if (object.oldestMessageTimestampInWindow != null)
                    if ($util.Long)
                        message.oldestMessageTimestampInWindow = $util.Long.fromValue(object.oldestMessageTimestampInWindow, false);
                    else if (typeof object.oldestMessageTimestampInWindow === "string")
                        message.oldestMessageTimestampInWindow = parseInt(object.oldestMessageTimestampInWindow, 10);
                    else if (typeof object.oldestMessageTimestampInWindow === "number")
                        message.oldestMessageTimestampInWindow = object.oldestMessageTimestampInWindow;
                    else if (typeof object.oldestMessageTimestampInWindow === "object")
                        message.oldestMessageTimestampInWindow = new $util.LongBits(object.oldestMessageTimestampInWindow.low >>> 0, object.oldestMessageTimestampInWindow.high >>> 0).toNumber();
                if (object.messageCount != null)
                    if ($util.Long)
                        message.messageCount = $util.Long.fromValue(object.messageCount, false);
                    else if (typeof object.messageCount === "string")
                        message.messageCount = parseInt(object.messageCount, 10);
                    else if (typeof object.messageCount === "number")
                        message.messageCount = object.messageCount;
                    else if (typeof object.messageCount === "object")
                        message.messageCount = new $util.LongBits(object.messageCount.low >>> 0, object.messageCount.high >>> 0).toNumber();
                if (object.nonHistoryReceivers) {
                    if (!Array.isArray(object.nonHistoryReceivers))
                        throw TypeError(".proto.Message.MessageHistoryMetadata.nonHistoryReceivers: array expected");
                    message.nonHistoryReceivers = Array(object.nonHistoryReceivers.length);
                    for (var i = 0; i < object.nonHistoryReceivers.length; ++i)
                        message.nonHistoryReceivers[i] = String(object.nonHistoryReceivers[i]);
                }
                if (object.oldestMessageTimestampInBundle != null)
                    if ($util.Long)
                        message.oldestMessageTimestampInBundle = $util.Long.fromValue(object.oldestMessageTimestampInBundle, false);
                    else if (typeof object.oldestMessageTimestampInBundle === "string")
                        message.oldestMessageTimestampInBundle = parseInt(object.oldestMessageTimestampInBundle, 10);
                    else if (typeof object.oldestMessageTimestampInBundle === "number")
                        message.oldestMessageTimestampInBundle = object.oldestMessageTimestampInBundle;
                    else if (typeof object.oldestMessageTimestampInBundle === "object")
                        message.oldestMessageTimestampInBundle = new $util.LongBits(object.oldestMessageTimestampInBundle.low >>> 0, object.oldestMessageTimestampInBundle.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a MessageHistoryMetadata message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.MessageHistoryMetadata
             * @static
             * @param {proto.Message.MessageHistoryMetadata} message MessageHistoryMetadata
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MessageHistoryMetadata.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.historyReceivers = [];
                    object.nonHistoryReceivers = [];
                }
                if (message.historyReceivers && message.historyReceivers.length) {
                    object.historyReceivers = Array(message.historyReceivers.length);
                    for (var j = 0; j < message.historyReceivers.length; ++j)
                        object.historyReceivers[j] = message.historyReceivers[j];
                }
                if (message.oldestMessageTimestampInWindow != null && Object.hasOwnProperty.call(message, "oldestMessageTimestampInWindow"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.oldestMessageTimestampInWindow = typeof message.oldestMessageTimestampInWindow === "number" ? BigInt(message.oldestMessageTimestampInWindow) : $util.Long.fromBits(message.oldestMessageTimestampInWindow.low >>> 0, message.oldestMessageTimestampInWindow.high >>> 0, false).toBigInt();
                    else if (typeof message.oldestMessageTimestampInWindow === "number")
                        object.oldestMessageTimestampInWindow = options.longs === String ? String(message.oldestMessageTimestampInWindow) : message.oldestMessageTimestampInWindow;
                    else
                        object.oldestMessageTimestampInWindow = options.longs === String ? $util.Long.prototype.toString.call(message.oldestMessageTimestampInWindow) : options.longs === Number ? new $util.LongBits(message.oldestMessageTimestampInWindow.low >>> 0, message.oldestMessageTimestampInWindow.high >>> 0).toNumber() : message.oldestMessageTimestampInWindow;
                if (message.messageCount != null && Object.hasOwnProperty.call(message, "messageCount"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.messageCount = typeof message.messageCount === "number" ? BigInt(message.messageCount) : $util.Long.fromBits(message.messageCount.low >>> 0, message.messageCount.high >>> 0, false).toBigInt();
                    else if (typeof message.messageCount === "number")
                        object.messageCount = options.longs === String ? String(message.messageCount) : message.messageCount;
                    else
                        object.messageCount = options.longs === String ? $util.Long.prototype.toString.call(message.messageCount) : options.longs === Number ? new $util.LongBits(message.messageCount.low >>> 0, message.messageCount.high >>> 0).toNumber() : message.messageCount;
                if (message.nonHistoryReceivers && message.nonHistoryReceivers.length) {
                    object.nonHistoryReceivers = Array(message.nonHistoryReceivers.length);
                    for (var j = 0; j < message.nonHistoryReceivers.length; ++j)
                        object.nonHistoryReceivers[j] = message.nonHistoryReceivers[j];
                }
                if (message.oldestMessageTimestampInBundle != null && Object.hasOwnProperty.call(message, "oldestMessageTimestampInBundle"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.oldestMessageTimestampInBundle = typeof message.oldestMessageTimestampInBundle === "number" ? BigInt(message.oldestMessageTimestampInBundle) : $util.Long.fromBits(message.oldestMessageTimestampInBundle.low >>> 0, message.oldestMessageTimestampInBundle.high >>> 0, false).toBigInt();
                    else if (typeof message.oldestMessageTimestampInBundle === "number")
                        object.oldestMessageTimestampInBundle = options.longs === String ? String(message.oldestMessageTimestampInBundle) : message.oldestMessageTimestampInBundle;
                    else
                        object.oldestMessageTimestampInBundle = options.longs === String ? $util.Long.prototype.toString.call(message.oldestMessageTimestampInBundle) : options.longs === Number ? new $util.LongBits(message.oldestMessageTimestampInBundle.low >>> 0, message.oldestMessageTimestampInBundle.high >>> 0).toNumber() : message.oldestMessageTimestampInBundle;
                return object;
            };

            /**
             * Converts this MessageHistoryMetadata to JSON.
             * @function toJSON
             * @memberof proto.Message.MessageHistoryMetadata
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MessageHistoryMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MessageHistoryMetadata
             * @function getTypeUrl
             * @memberof proto.Message.MessageHistoryMetadata
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MessageHistoryMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.MessageHistoryMetadata";
            };

            return MessageHistoryMetadata;
        })();

        Message.MessageHistoryNotice = (function() {

            /**
             * Properties of a MessageHistoryNotice.
             * @memberof proto.Message
             * @interface IMessageHistoryNotice
             * @property {proto.IContextInfo|null} [contextInfo] MessageHistoryNotice contextInfo
             * @property {proto.Message.IMessageHistoryMetadata|null} [messageHistoryMetadata] MessageHistoryNotice messageHistoryMetadata
             */

            /**
             * Constructs a new MessageHistoryNotice.
             * @memberof proto.Message
             * @classdesc Represents a MessageHistoryNotice.
             * @implements IMessageHistoryNotice
             * @constructor
             * @param {proto.Message.IMessageHistoryNotice=} [properties] Properties to set
             */
            function MessageHistoryNotice(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MessageHistoryNotice contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.MessageHistoryNotice
             * @instance
             */
            MessageHistoryNotice.prototype.contextInfo = null;

            /**
             * MessageHistoryNotice messageHistoryMetadata.
             * @member {proto.Message.IMessageHistoryMetadata|null|undefined} messageHistoryMetadata
             * @memberof proto.Message.MessageHistoryNotice
             * @instance
             */
            MessageHistoryNotice.prototype.messageHistoryMetadata = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MessageHistoryNotice.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MessageHistoryNotice.prototype, "_messageHistoryMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["messageHistoryMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new MessageHistoryNotice instance using the specified properties.
             * @function create
             * @memberof proto.Message.MessageHistoryNotice
             * @static
             * @param {proto.Message.IMessageHistoryNotice=} [properties] Properties to set
             * @returns {proto.Message.MessageHistoryNotice} MessageHistoryNotice instance
             */
            MessageHistoryNotice.create = function create(properties) {
                return new MessageHistoryNotice(properties);
            };

            /**
             * Encodes the specified MessageHistoryNotice message. Does not implicitly {@link proto.Message.MessageHistoryNotice.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.MessageHistoryNotice
             * @static
             * @param {proto.Message.IMessageHistoryNotice} message MessageHistoryNotice message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MessageHistoryNotice.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.messageHistoryMetadata != null && Object.hasOwnProperty.call(message, "messageHistoryMetadata"))
                    $root.proto.Message.MessageHistoryMetadata.encode(message.messageHistoryMetadata, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MessageHistoryNotice message, length delimited. Does not implicitly {@link proto.Message.MessageHistoryNotice.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.MessageHistoryNotice
             * @static
             * @param {proto.Message.IMessageHistoryNotice} message MessageHistoryNotice message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MessageHistoryNotice.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a MessageHistoryNotice message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.MessageHistoryNotice
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.MessageHistoryNotice} MessageHistoryNotice
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MessageHistoryNotice.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.messageHistoryMetadata = $root.proto.Message.MessageHistoryMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.messageHistoryMetadata);
                            message._messageHistoryMetadata = "messageHistoryMetadata";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a MessageHistoryNotice message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.MessageHistoryNotice
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.MessageHistoryNotice} MessageHistoryNotice
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MessageHistoryNotice.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MessageHistoryNotice message.
             * @function verify
             * @memberof proto.Message.MessageHistoryNotice
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MessageHistoryNotice.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.messageHistoryMetadata != null && Object.hasOwnProperty.call(message, "messageHistoryMetadata")) {
                    properties._messageHistoryMetadata = 1;
                    {
                        var error = $root.proto.Message.MessageHistoryMetadata.verify(message.messageHistoryMetadata, q + 1);
                        if (error)
                            return "messageHistoryMetadata." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a MessageHistoryNotice message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.MessageHistoryNotice
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.MessageHistoryNotice} MessageHistoryNotice
             */
            MessageHistoryNotice.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.MessageHistoryNotice: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.MessageHistoryNotice.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                if (object.messageHistoryMetadata != null) {
                    if (!$util.isObject(object.messageHistoryMetadata))
                        throw TypeError(".proto.Message.MessageHistoryNotice.messageHistoryMetadata: object expected");
                    message.messageHistoryMetadata = $root.proto.Message.MessageHistoryMetadata.fromObject(object.messageHistoryMetadata, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a MessageHistoryNotice message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.MessageHistoryNotice
             * @static
             * @param {proto.Message.MessageHistoryNotice} message MessageHistoryNotice
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MessageHistoryNotice.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.messageHistoryMetadata != null && Object.hasOwnProperty.call(message, "messageHistoryMetadata"))
                    object.messageHistoryMetadata = $root.proto.Message.MessageHistoryMetadata.toObject(message.messageHistoryMetadata, options, q + 1);
                return object;
            };

            /**
             * Converts this MessageHistoryNotice to JSON.
             * @function toJSON
             * @memberof proto.Message.MessageHistoryNotice
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MessageHistoryNotice.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MessageHistoryNotice
             * @function getTypeUrl
             * @memberof proto.Message.MessageHistoryNotice
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MessageHistoryNotice.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.MessageHistoryNotice";
            };

            return MessageHistoryNotice;
        })();

        Message.NewsletterAdminInviteMessage = (function() {

            /**
             * Properties of a NewsletterAdminInviteMessage.
             * @memberof proto.Message
             * @interface INewsletterAdminInviteMessage
             * @property {string|null} [newsletterJid] NewsletterAdminInviteMessage newsletterJid
             * @property {string|null} [newsletterName] NewsletterAdminInviteMessage newsletterName
             * @property {Uint8Array|null} [jpegThumbnail] NewsletterAdminInviteMessage jpegThumbnail
             * @property {string|null} [caption] NewsletterAdminInviteMessage caption
             * @property {number|Long|null} [inviteExpiration] NewsletterAdminInviteMessage inviteExpiration
             * @property {proto.IContextInfo|null} [contextInfo] NewsletterAdminInviteMessage contextInfo
             */

            /**
             * Constructs a new NewsletterAdminInviteMessage.
             * @memberof proto.Message
             * @classdesc Represents a NewsletterAdminInviteMessage.
             * @implements INewsletterAdminInviteMessage
             * @constructor
             * @param {proto.Message.INewsletterAdminInviteMessage=} [properties] Properties to set
             */
            function NewsletterAdminInviteMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * NewsletterAdminInviteMessage newsletterJid.
             * @member {string|null|undefined} newsletterJid
             * @memberof proto.Message.NewsletterAdminInviteMessage
             * @instance
             */
            NewsletterAdminInviteMessage.prototype.newsletterJid = null;

            /**
             * NewsletterAdminInviteMessage newsletterName.
             * @member {string|null|undefined} newsletterName
             * @memberof proto.Message.NewsletterAdminInviteMessage
             * @instance
             */
            NewsletterAdminInviteMessage.prototype.newsletterName = null;

            /**
             * NewsletterAdminInviteMessage jpegThumbnail.
             * @member {Uint8Array|null|undefined} jpegThumbnail
             * @memberof proto.Message.NewsletterAdminInviteMessage
             * @instance
             */
            NewsletterAdminInviteMessage.prototype.jpegThumbnail = null;

            /**
             * NewsletterAdminInviteMessage caption.
             * @member {string|null|undefined} caption
             * @memberof proto.Message.NewsletterAdminInviteMessage
             * @instance
             */
            NewsletterAdminInviteMessage.prototype.caption = null;

            /**
             * NewsletterAdminInviteMessage inviteExpiration.
             * @member {number|Long|null|undefined} inviteExpiration
             * @memberof proto.Message.NewsletterAdminInviteMessage
             * @instance
             */
            NewsletterAdminInviteMessage.prototype.inviteExpiration = null;

            /**
             * NewsletterAdminInviteMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.NewsletterAdminInviteMessage
             * @instance
             */
            NewsletterAdminInviteMessage.prototype.contextInfo = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NewsletterAdminInviteMessage.prototype, "_newsletterJid", {
                get: $util.oneOfGetter($oneOfFields = ["newsletterJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NewsletterAdminInviteMessage.prototype, "_newsletterName", {
                get: $util.oneOfGetter($oneOfFields = ["newsletterName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NewsletterAdminInviteMessage.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NewsletterAdminInviteMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NewsletterAdminInviteMessage.prototype, "_inviteExpiration", {
                get: $util.oneOfGetter($oneOfFields = ["inviteExpiration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NewsletterAdminInviteMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new NewsletterAdminInviteMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.NewsletterAdminInviteMessage
             * @static
             * @param {proto.Message.INewsletterAdminInviteMessage=} [properties] Properties to set
             * @returns {proto.Message.NewsletterAdminInviteMessage} NewsletterAdminInviteMessage instance
             */
            NewsletterAdminInviteMessage.create = function create(properties) {
                return new NewsletterAdminInviteMessage(properties);
            };

            /**
             * Encodes the specified NewsletterAdminInviteMessage message. Does not implicitly {@link proto.Message.NewsletterAdminInviteMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.NewsletterAdminInviteMessage
             * @static
             * @param {proto.Message.INewsletterAdminInviteMessage} message NewsletterAdminInviteMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            NewsletterAdminInviteMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.newsletterJid != null && Object.hasOwnProperty.call(message, "newsletterJid"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.newsletterJid);
                if (message.newsletterName != null && Object.hasOwnProperty.call(message, "newsletterName"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.newsletterName);
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.jpegThumbnail);
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.caption);
                if (message.inviteExpiration != null && Object.hasOwnProperty.call(message, "inviteExpiration"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.inviteExpiration);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified NewsletterAdminInviteMessage message, length delimited. Does not implicitly {@link proto.Message.NewsletterAdminInviteMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.NewsletterAdminInviteMessage
             * @static
             * @param {proto.Message.INewsletterAdminInviteMessage} message NewsletterAdminInviteMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            NewsletterAdminInviteMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a NewsletterAdminInviteMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.NewsletterAdminInviteMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.NewsletterAdminInviteMessage} NewsletterAdminInviteMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            NewsletterAdminInviteMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.newsletterJid = reader.stringVerify();
                            message._newsletterJid = "newsletterJid";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.newsletterName = reader.stringVerify();
                            message._newsletterName = "newsletterName";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.jpegThumbnail = reader.bytes();
                            message._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.caption = reader.stringVerify();
                            message._caption = "caption";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.inviteExpiration = reader.int64();
                            message._inviteExpiration = "inviteExpiration";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a NewsletterAdminInviteMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.NewsletterAdminInviteMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.NewsletterAdminInviteMessage} NewsletterAdminInviteMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            NewsletterAdminInviteMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a NewsletterAdminInviteMessage message.
             * @function verify
             * @memberof proto.Message.NewsletterAdminInviteMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            NewsletterAdminInviteMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.newsletterJid != null && Object.hasOwnProperty.call(message, "newsletterJid")) {
                    properties._newsletterJid = 1;
                    if (!$util.isString(message.newsletterJid))
                        return "newsletterJid: string expected";
                }
                if (message.newsletterName != null && Object.hasOwnProperty.call(message, "newsletterName")) {
                    properties._newsletterName = 1;
                    if (!$util.isString(message.newsletterName))
                        return "newsletterName: string expected";
                }
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail")) {
                    properties._jpegThumbnail = 1;
                    if (!(message.jpegThumbnail && typeof message.jpegThumbnail.length === "number" || $util.isString(message.jpegThumbnail)))
                        return "jpegThumbnail: buffer expected";
                }
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption")) {
                    properties._caption = 1;
                    if (!$util.isString(message.caption))
                        return "caption: string expected";
                }
                if (message.inviteExpiration != null && Object.hasOwnProperty.call(message, "inviteExpiration")) {
                    properties._inviteExpiration = 1;
                    if (!$util.isInteger(message.inviteExpiration) && !(message.inviteExpiration && $util.isInteger(message.inviteExpiration.low) && $util.isInteger(message.inviteExpiration.high)))
                        return "inviteExpiration: integer|Long expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a NewsletterAdminInviteMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.NewsletterAdminInviteMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.NewsletterAdminInviteMessage} NewsletterAdminInviteMessage
             */
            NewsletterAdminInviteMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.NewsletterAdminInviteMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.newsletterJid != null)
                    message.newsletterJid = String(object.newsletterJid);
                if (object.newsletterName != null)
                    message.newsletterName = String(object.newsletterName);
                if (object.jpegThumbnail != null)
                    if (typeof object.jpegThumbnail === "string")
                        $util.base64.decode(object.jpegThumbnail, message.jpegThumbnail = $util.newBuffer($util.base64.length(object.jpegThumbnail)), 0);
                    else if (object.jpegThumbnail.length >= 0)
                        message.jpegThumbnail = object.jpegThumbnail;
                if (object.caption != null)
                    message.caption = String(object.caption);
                if (object.inviteExpiration != null)
                    if ($util.Long)
                        message.inviteExpiration = $util.Long.fromValue(object.inviteExpiration, false);
                    else if (typeof object.inviteExpiration === "string")
                        message.inviteExpiration = parseInt(object.inviteExpiration, 10);
                    else if (typeof object.inviteExpiration === "number")
                        message.inviteExpiration = object.inviteExpiration;
                    else if (typeof object.inviteExpiration === "object")
                        message.inviteExpiration = new $util.LongBits(object.inviteExpiration.low >>> 0, object.inviteExpiration.high >>> 0).toNumber();
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.NewsletterAdminInviteMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a NewsletterAdminInviteMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.NewsletterAdminInviteMessage
             * @static
             * @param {proto.Message.NewsletterAdminInviteMessage} message NewsletterAdminInviteMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            NewsletterAdminInviteMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.newsletterJid != null && Object.hasOwnProperty.call(message, "newsletterJid"))
                    object.newsletterJid = message.newsletterJid;
                if (message.newsletterName != null && Object.hasOwnProperty.call(message, "newsletterName"))
                    object.newsletterName = message.newsletterName;
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    object.jpegThumbnail = options.bytes === String ? $util.base64.encode(message.jpegThumbnail, 0, message.jpegThumbnail.length) : options.bytes === Array ? Array.prototype.slice.call(message.jpegThumbnail) : message.jpegThumbnail;
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    object.caption = message.caption;
                if (message.inviteExpiration != null && Object.hasOwnProperty.call(message, "inviteExpiration"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.inviteExpiration = typeof message.inviteExpiration === "number" ? BigInt(message.inviteExpiration) : $util.Long.fromBits(message.inviteExpiration.low >>> 0, message.inviteExpiration.high >>> 0, false).toBigInt();
                    else if (typeof message.inviteExpiration === "number")
                        object.inviteExpiration = options.longs === String ? String(message.inviteExpiration) : message.inviteExpiration;
                    else
                        object.inviteExpiration = options.longs === String ? $util.Long.prototype.toString.call(message.inviteExpiration) : options.longs === Number ? new $util.LongBits(message.inviteExpiration.low >>> 0, message.inviteExpiration.high >>> 0).toNumber() : message.inviteExpiration;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                return object;
            };

            /**
             * Converts this NewsletterAdminInviteMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.NewsletterAdminInviteMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            NewsletterAdminInviteMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for NewsletterAdminInviteMessage
             * @function getTypeUrl
             * @memberof proto.Message.NewsletterAdminInviteMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            NewsletterAdminInviteMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.NewsletterAdminInviteMessage";
            };

            return NewsletterAdminInviteMessage;
        })();

        Message.NewsletterFollowerInviteMessage = (function() {

            /**
             * Properties of a NewsletterFollowerInviteMessage.
             * @memberof proto.Message
             * @interface INewsletterFollowerInviteMessage
             * @property {string|null} [newsletterJid] NewsletterFollowerInviteMessage newsletterJid
             * @property {string|null} [newsletterName] NewsletterFollowerInviteMessage newsletterName
             * @property {Uint8Array|null} [jpegThumbnail] NewsletterFollowerInviteMessage jpegThumbnail
             * @property {string|null} [caption] NewsletterFollowerInviteMessage caption
             * @property {proto.IContextInfo|null} [contextInfo] NewsletterFollowerInviteMessage contextInfo
             */

            /**
             * Constructs a new NewsletterFollowerInviteMessage.
             * @memberof proto.Message
             * @classdesc Represents a NewsletterFollowerInviteMessage.
             * @implements INewsletterFollowerInviteMessage
             * @constructor
             * @param {proto.Message.INewsletterFollowerInviteMessage=} [properties] Properties to set
             */
            function NewsletterFollowerInviteMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * NewsletterFollowerInviteMessage newsletterJid.
             * @member {string|null|undefined} newsletterJid
             * @memberof proto.Message.NewsletterFollowerInviteMessage
             * @instance
             */
            NewsletterFollowerInviteMessage.prototype.newsletterJid = null;

            /**
             * NewsletterFollowerInviteMessage newsletterName.
             * @member {string|null|undefined} newsletterName
             * @memberof proto.Message.NewsletterFollowerInviteMessage
             * @instance
             */
            NewsletterFollowerInviteMessage.prototype.newsletterName = null;

            /**
             * NewsletterFollowerInviteMessage jpegThumbnail.
             * @member {Uint8Array|null|undefined} jpegThumbnail
             * @memberof proto.Message.NewsletterFollowerInviteMessage
             * @instance
             */
            NewsletterFollowerInviteMessage.prototype.jpegThumbnail = null;

            /**
             * NewsletterFollowerInviteMessage caption.
             * @member {string|null|undefined} caption
             * @memberof proto.Message.NewsletterFollowerInviteMessage
             * @instance
             */
            NewsletterFollowerInviteMessage.prototype.caption = null;

            /**
             * NewsletterFollowerInviteMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.NewsletterFollowerInviteMessage
             * @instance
             */
            NewsletterFollowerInviteMessage.prototype.contextInfo = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NewsletterFollowerInviteMessage.prototype, "_newsletterJid", {
                get: $util.oneOfGetter($oneOfFields = ["newsletterJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NewsletterFollowerInviteMessage.prototype, "_newsletterName", {
                get: $util.oneOfGetter($oneOfFields = ["newsletterName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NewsletterFollowerInviteMessage.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NewsletterFollowerInviteMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NewsletterFollowerInviteMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new NewsletterFollowerInviteMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.NewsletterFollowerInviteMessage
             * @static
             * @param {proto.Message.INewsletterFollowerInviteMessage=} [properties] Properties to set
             * @returns {proto.Message.NewsletterFollowerInviteMessage} NewsletterFollowerInviteMessage instance
             */
            NewsletterFollowerInviteMessage.create = function create(properties) {
                return new NewsletterFollowerInviteMessage(properties);
            };

            /**
             * Encodes the specified NewsletterFollowerInviteMessage message. Does not implicitly {@link proto.Message.NewsletterFollowerInviteMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.NewsletterFollowerInviteMessage
             * @static
             * @param {proto.Message.INewsletterFollowerInviteMessage} message NewsletterFollowerInviteMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            NewsletterFollowerInviteMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.newsletterJid != null && Object.hasOwnProperty.call(message, "newsletterJid"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.newsletterJid);
                if (message.newsletterName != null && Object.hasOwnProperty.call(message, "newsletterName"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.newsletterName);
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.jpegThumbnail);
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.caption);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified NewsletterFollowerInviteMessage message, length delimited. Does not implicitly {@link proto.Message.NewsletterFollowerInviteMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.NewsletterFollowerInviteMessage
             * @static
             * @param {proto.Message.INewsletterFollowerInviteMessage} message NewsletterFollowerInviteMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            NewsletterFollowerInviteMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a NewsletterFollowerInviteMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.NewsletterFollowerInviteMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.NewsletterFollowerInviteMessage} NewsletterFollowerInviteMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            NewsletterFollowerInviteMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.newsletterJid = reader.stringVerify();
                            message._newsletterJid = "newsletterJid";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.newsletterName = reader.stringVerify();
                            message._newsletterName = "newsletterName";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.jpegThumbnail = reader.bytes();
                            message._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.caption = reader.stringVerify();
                            message._caption = "caption";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a NewsletterFollowerInviteMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.NewsletterFollowerInviteMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.NewsletterFollowerInviteMessage} NewsletterFollowerInviteMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            NewsletterFollowerInviteMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a NewsletterFollowerInviteMessage message.
             * @function verify
             * @memberof proto.Message.NewsletterFollowerInviteMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            NewsletterFollowerInviteMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.newsletterJid != null && Object.hasOwnProperty.call(message, "newsletterJid")) {
                    properties._newsletterJid = 1;
                    if (!$util.isString(message.newsletterJid))
                        return "newsletterJid: string expected";
                }
                if (message.newsletterName != null && Object.hasOwnProperty.call(message, "newsletterName")) {
                    properties._newsletterName = 1;
                    if (!$util.isString(message.newsletterName))
                        return "newsletterName: string expected";
                }
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail")) {
                    properties._jpegThumbnail = 1;
                    if (!(message.jpegThumbnail && typeof message.jpegThumbnail.length === "number" || $util.isString(message.jpegThumbnail)))
                        return "jpegThumbnail: buffer expected";
                }
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption")) {
                    properties._caption = 1;
                    if (!$util.isString(message.caption))
                        return "caption: string expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a NewsletterFollowerInviteMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.NewsletterFollowerInviteMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.NewsletterFollowerInviteMessage} NewsletterFollowerInviteMessage
             */
            NewsletterFollowerInviteMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.NewsletterFollowerInviteMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.newsletterJid != null)
                    message.newsletterJid = String(object.newsletterJid);
                if (object.newsletterName != null)
                    message.newsletterName = String(object.newsletterName);
                if (object.jpegThumbnail != null)
                    if (typeof object.jpegThumbnail === "string")
                        $util.base64.decode(object.jpegThumbnail, message.jpegThumbnail = $util.newBuffer($util.base64.length(object.jpegThumbnail)), 0);
                    else if (object.jpegThumbnail.length >= 0)
                        message.jpegThumbnail = object.jpegThumbnail;
                if (object.caption != null)
                    message.caption = String(object.caption);
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.NewsletterFollowerInviteMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a NewsletterFollowerInviteMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.NewsletterFollowerInviteMessage
             * @static
             * @param {proto.Message.NewsletterFollowerInviteMessage} message NewsletterFollowerInviteMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            NewsletterFollowerInviteMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.newsletterJid != null && Object.hasOwnProperty.call(message, "newsletterJid"))
                    object.newsletterJid = message.newsletterJid;
                if (message.newsletterName != null && Object.hasOwnProperty.call(message, "newsletterName"))
                    object.newsletterName = message.newsletterName;
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    object.jpegThumbnail = options.bytes === String ? $util.base64.encode(message.jpegThumbnail, 0, message.jpegThumbnail.length) : options.bytes === Array ? Array.prototype.slice.call(message.jpegThumbnail) : message.jpegThumbnail;
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    object.caption = message.caption;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                return object;
            };

            /**
             * Converts this NewsletterFollowerInviteMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.NewsletterFollowerInviteMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            NewsletterFollowerInviteMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for NewsletterFollowerInviteMessage
             * @function getTypeUrl
             * @memberof proto.Message.NewsletterFollowerInviteMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            NewsletterFollowerInviteMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.NewsletterFollowerInviteMessage";
            };

            return NewsletterFollowerInviteMessage;
        })();

        Message.OrderMessage = (function() {

            /**
             * Properties of an OrderMessage.
             * @memberof proto.Message
             * @interface IOrderMessage
             * @property {string|null} [orderId] OrderMessage orderId
             * @property {Uint8Array|null} [thumbnail] OrderMessage thumbnail
             * @property {number|null} [itemCount] OrderMessage itemCount
             * @property {proto.Message.OrderMessage.OrderStatus|null} [status] OrderMessage status
             * @property {proto.Message.OrderMessage.OrderSurface|null} [surface] OrderMessage surface
             * @property {string|null} [message] OrderMessage message
             * @property {string|null} [orderTitle] OrderMessage orderTitle
             * @property {string|null} [sellerJid] OrderMessage sellerJid
             * @property {string|null} [token] OrderMessage token
             * @property {number|Long|null} [totalAmount1000] OrderMessage totalAmount1000
             * @property {string|null} [totalCurrencyCode] OrderMessage totalCurrencyCode
             * @property {proto.IContextInfo|null} [contextInfo] OrderMessage contextInfo
             * @property {number|null} [messageVersion] OrderMessage messageVersion
             * @property {proto.IMessageKey|null} [orderRequestMessageId] OrderMessage orderRequestMessageId
             * @property {string|null} [catalogType] OrderMessage catalogType
             */

            /**
             * Constructs a new OrderMessage.
             * @memberof proto.Message
             * @classdesc Represents an OrderMessage.
             * @implements IOrderMessage
             * @constructor
             * @param {proto.Message.IOrderMessage=} [properties] Properties to set
             */
            function OrderMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * OrderMessage orderId.
             * @member {string|null|undefined} orderId
             * @memberof proto.Message.OrderMessage
             * @instance
             */
            OrderMessage.prototype.orderId = null;

            /**
             * OrderMessage thumbnail.
             * @member {Uint8Array|null|undefined} thumbnail
             * @memberof proto.Message.OrderMessage
             * @instance
             */
            OrderMessage.prototype.thumbnail = null;

            /**
             * OrderMessage itemCount.
             * @member {number|null|undefined} itemCount
             * @memberof proto.Message.OrderMessage
             * @instance
             */
            OrderMessage.prototype.itemCount = null;

            /**
             * OrderMessage status.
             * @member {proto.Message.OrderMessage.OrderStatus|null|undefined} status
             * @memberof proto.Message.OrderMessage
             * @instance
             */
            OrderMessage.prototype.status = null;

            /**
             * OrderMessage surface.
             * @member {proto.Message.OrderMessage.OrderSurface|null|undefined} surface
             * @memberof proto.Message.OrderMessage
             * @instance
             */
            OrderMessage.prototype.surface = null;

            /**
             * OrderMessage message.
             * @member {string|null|undefined} message
             * @memberof proto.Message.OrderMessage
             * @instance
             */
            OrderMessage.prototype.message = null;

            /**
             * OrderMessage orderTitle.
             * @member {string|null|undefined} orderTitle
             * @memberof proto.Message.OrderMessage
             * @instance
             */
            OrderMessage.prototype.orderTitle = null;

            /**
             * OrderMessage sellerJid.
             * @member {string|null|undefined} sellerJid
             * @memberof proto.Message.OrderMessage
             * @instance
             */
            OrderMessage.prototype.sellerJid = null;

            /**
             * OrderMessage token.
             * @member {string|null|undefined} token
             * @memberof proto.Message.OrderMessage
             * @instance
             */
            OrderMessage.prototype.token = null;

            /**
             * OrderMessage totalAmount1000.
             * @member {number|Long|null|undefined} totalAmount1000
             * @memberof proto.Message.OrderMessage
             * @instance
             */
            OrderMessage.prototype.totalAmount1000 = null;

            /**
             * OrderMessage totalCurrencyCode.
             * @member {string|null|undefined} totalCurrencyCode
             * @memberof proto.Message.OrderMessage
             * @instance
             */
            OrderMessage.prototype.totalCurrencyCode = null;

            /**
             * OrderMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.OrderMessage
             * @instance
             */
            OrderMessage.prototype.contextInfo = null;

            /**
             * OrderMessage messageVersion.
             * @member {number|null|undefined} messageVersion
             * @memberof proto.Message.OrderMessage
             * @instance
             */
            OrderMessage.prototype.messageVersion = null;

            /**
             * OrderMessage orderRequestMessageId.
             * @member {proto.IMessageKey|null|undefined} orderRequestMessageId
             * @memberof proto.Message.OrderMessage
             * @instance
             */
            OrderMessage.prototype.orderRequestMessageId = null;

            /**
             * OrderMessage catalogType.
             * @member {string|null|undefined} catalogType
             * @memberof proto.Message.OrderMessage
             * @instance
             */
            OrderMessage.prototype.catalogType = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(OrderMessage.prototype, "_orderId", {
                get: $util.oneOfGetter($oneOfFields = ["orderId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(OrderMessage.prototype, "_thumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(OrderMessage.prototype, "_itemCount", {
                get: $util.oneOfGetter($oneOfFields = ["itemCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(OrderMessage.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(OrderMessage.prototype, "_surface", {
                get: $util.oneOfGetter($oneOfFields = ["surface"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(OrderMessage.prototype, "_message", {
                get: $util.oneOfGetter($oneOfFields = ["message"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(OrderMessage.prototype, "_orderTitle", {
                get: $util.oneOfGetter($oneOfFields = ["orderTitle"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(OrderMessage.prototype, "_sellerJid", {
                get: $util.oneOfGetter($oneOfFields = ["sellerJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(OrderMessage.prototype, "_token", {
                get: $util.oneOfGetter($oneOfFields = ["token"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(OrderMessage.prototype, "_totalAmount1000", {
                get: $util.oneOfGetter($oneOfFields = ["totalAmount1000"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(OrderMessage.prototype, "_totalCurrencyCode", {
                get: $util.oneOfGetter($oneOfFields = ["totalCurrencyCode"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(OrderMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(OrderMessage.prototype, "_messageVersion", {
                get: $util.oneOfGetter($oneOfFields = ["messageVersion"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(OrderMessage.prototype, "_orderRequestMessageId", {
                get: $util.oneOfGetter($oneOfFields = ["orderRequestMessageId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(OrderMessage.prototype, "_catalogType", {
                get: $util.oneOfGetter($oneOfFields = ["catalogType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new OrderMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.OrderMessage
             * @static
             * @param {proto.Message.IOrderMessage=} [properties] Properties to set
             * @returns {proto.Message.OrderMessage} OrderMessage instance
             */
            OrderMessage.create = function create(properties) {
                return new OrderMessage(properties);
            };

            /**
             * Encodes the specified OrderMessage message. Does not implicitly {@link proto.Message.OrderMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.OrderMessage
             * @static
             * @param {proto.Message.IOrderMessage} message OrderMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OrderMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.orderId != null && Object.hasOwnProperty.call(message, "orderId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.orderId);
                if (message.thumbnail != null && Object.hasOwnProperty.call(message, "thumbnail"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.thumbnail);
                if (message.itemCount != null && Object.hasOwnProperty.call(message, "itemCount"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.itemCount);
                if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int32(message.status);
                if (message.surface != null && Object.hasOwnProperty.call(message, "surface"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int32(message.surface);
                if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.message);
                if (message.orderTitle != null && Object.hasOwnProperty.call(message, "orderTitle"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.orderTitle);
                if (message.sellerJid != null && Object.hasOwnProperty.call(message, "sellerJid"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.sellerJid);
                if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.token);
                if (message.totalAmount1000 != null && Object.hasOwnProperty.call(message, "totalAmount1000"))
                    writer.uint32(/* id 10, wireType 0 =*/80).int64(message.totalAmount1000);
                if (message.totalCurrencyCode != null && Object.hasOwnProperty.call(message, "totalCurrencyCode"))
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.totalCurrencyCode);
                if (message.messageVersion != null && Object.hasOwnProperty.call(message, "messageVersion"))
                    writer.uint32(/* id 12, wireType 0 =*/96).int32(message.messageVersion);
                if (message.orderRequestMessageId != null && Object.hasOwnProperty.call(message, "orderRequestMessageId"))
                    $root.proto.MessageKey.encode(message.orderRequestMessageId, writer.uint32(/* id 13, wireType 2 =*/106).fork(), q + 1).ldelim();
                if (message.catalogType != null && Object.hasOwnProperty.call(message, "catalogType"))
                    writer.uint32(/* id 15, wireType 2 =*/122).string(message.catalogType);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified OrderMessage message, length delimited. Does not implicitly {@link proto.Message.OrderMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.OrderMessage
             * @static
             * @param {proto.Message.IOrderMessage} message OrderMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OrderMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an OrderMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.OrderMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.OrderMessage} OrderMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OrderMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.orderId = reader.stringVerify();
                            message._orderId = "orderId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.thumbnail = reader.bytes();
                            message._thumbnail = "thumbnail";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.itemCount = reader.int32();
                            message._itemCount = "itemCount";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.status = reader.int32();
                            message._status = "status";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.surface = reader.int32();
                            message._surface = "surface";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.message = reader.stringVerify();
                            message._message = "message";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.orderTitle = reader.stringVerify();
                            message._orderTitle = "orderTitle";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.sellerJid = reader.stringVerify();
                            message._sellerJid = "sellerJid";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            message.token = reader.stringVerify();
                            message._token = "token";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            message.totalAmount1000 = reader.int64();
                            message._totalAmount1000 = "totalAmount1000";
                            continue;
                        }
                    case 11: {
                            if (u !== 2)
                                break;
                            message.totalCurrencyCode = reader.stringVerify();
                            message._totalCurrencyCode = "totalCurrencyCode";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 12: {
                            if (u !== 0)
                                break;
                            message.messageVersion = reader.int32();
                            message._messageVersion = "messageVersion";
                            continue;
                        }
                    case 13: {
                            if (u !== 2)
                                break;
                            message.orderRequestMessageId = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.orderRequestMessageId);
                            message._orderRequestMessageId = "orderRequestMessageId";
                            continue;
                        }
                    case 15: {
                            if (u !== 2)
                                break;
                            message.catalogType = reader.stringVerify();
                            message._catalogType = "catalogType";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an OrderMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.OrderMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.OrderMessage} OrderMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OrderMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an OrderMessage message.
             * @function verify
             * @memberof proto.Message.OrderMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            OrderMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.orderId != null && Object.hasOwnProperty.call(message, "orderId")) {
                    properties._orderId = 1;
                    if (!$util.isString(message.orderId))
                        return "orderId: string expected";
                }
                if (message.thumbnail != null && Object.hasOwnProperty.call(message, "thumbnail")) {
                    properties._thumbnail = 1;
                    if (!(message.thumbnail && typeof message.thumbnail.length === "number" || $util.isString(message.thumbnail)))
                        return "thumbnail: buffer expected";
                }
                if (message.itemCount != null && Object.hasOwnProperty.call(message, "itemCount")) {
                    properties._itemCount = 1;
                    if (!$util.isInteger(message.itemCount))
                        return "itemCount: integer expected";
                }
                if (message.status != null && Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (typeof message.status !== "number" || (message.status | 0) !== message.status)
                        return "status: enum value expected";
                }
                if (message.surface != null && Object.hasOwnProperty.call(message, "surface")) {
                    properties._surface = 1;
                    if (typeof message.surface !== "number" || (message.surface | 0) !== message.surface)
                        return "surface: enum value expected";
                }
                if (message.message != null && Object.hasOwnProperty.call(message, "message")) {
                    properties._message = 1;
                    if (!$util.isString(message.message))
                        return "message: string expected";
                }
                if (message.orderTitle != null && Object.hasOwnProperty.call(message, "orderTitle")) {
                    properties._orderTitle = 1;
                    if (!$util.isString(message.orderTitle))
                        return "orderTitle: string expected";
                }
                if (message.sellerJid != null && Object.hasOwnProperty.call(message, "sellerJid")) {
                    properties._sellerJid = 1;
                    if (!$util.isString(message.sellerJid))
                        return "sellerJid: string expected";
                }
                if (message.token != null && Object.hasOwnProperty.call(message, "token")) {
                    properties._token = 1;
                    if (!$util.isString(message.token))
                        return "token: string expected";
                }
                if (message.totalAmount1000 != null && Object.hasOwnProperty.call(message, "totalAmount1000")) {
                    properties._totalAmount1000 = 1;
                    if (!$util.isInteger(message.totalAmount1000) && !(message.totalAmount1000 && $util.isInteger(message.totalAmount1000.low) && $util.isInteger(message.totalAmount1000.high)))
                        return "totalAmount1000: integer|Long expected";
                }
                if (message.totalCurrencyCode != null && Object.hasOwnProperty.call(message, "totalCurrencyCode")) {
                    properties._totalCurrencyCode = 1;
                    if (!$util.isString(message.totalCurrencyCode))
                        return "totalCurrencyCode: string expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.messageVersion != null && Object.hasOwnProperty.call(message, "messageVersion")) {
                    properties._messageVersion = 1;
                    if (!$util.isInteger(message.messageVersion))
                        return "messageVersion: integer expected";
                }
                if (message.orderRequestMessageId != null && Object.hasOwnProperty.call(message, "orderRequestMessageId")) {
                    properties._orderRequestMessageId = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.orderRequestMessageId, q + 1);
                        if (error)
                            return "orderRequestMessageId." + error;
                    }
                }
                if (message.catalogType != null && Object.hasOwnProperty.call(message, "catalogType")) {
                    properties._catalogType = 1;
                    if (!$util.isString(message.catalogType))
                        return "catalogType: string expected";
                }
                return null;
            };

            /**
             * Creates an OrderMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.OrderMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.OrderMessage} OrderMessage
             */
            OrderMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.OrderMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.orderId != null)
                    message.orderId = String(object.orderId);
                if (object.thumbnail != null)
                    if (typeof object.thumbnail === "string")
                        $util.base64.decode(object.thumbnail, message.thumbnail = $util.newBuffer($util.base64.length(object.thumbnail)), 0);
                    else if (object.thumbnail.length >= 0)
                        message.thumbnail = object.thumbnail;
                if (object.itemCount != null)
                    message.itemCount = object.itemCount | 0;
                switch (object.status) {
                case "INQUIRY":
                case 1:
                    message.status = 1;
                    break;
                case "ACCEPTED":
                case 2:
                    message.status = 2;
                    break;
                case "DECLINED":
                case 3:
                    message.status = 3;
                    break;
                default:
                    if (typeof object.status === "number" && (object.status | 0) === object.status)
                        message.status = object.status;
                }
                switch (object.surface) {
                case "CATALOG":
                case 1:
                    message.surface = 1;
                    break;
                default:
                    if (typeof object.surface === "number" && (object.surface | 0) === object.surface)
                        message.surface = object.surface;
                }
                if (object.message != null)
                    message.message = String(object.message);
                if (object.orderTitle != null)
                    message.orderTitle = String(object.orderTitle);
                if (object.sellerJid != null)
                    message.sellerJid = String(object.sellerJid);
                if (object.token != null)
                    message.token = String(object.token);
                if (object.totalAmount1000 != null)
                    if ($util.Long)
                        message.totalAmount1000 = $util.Long.fromValue(object.totalAmount1000, false);
                    else if (typeof object.totalAmount1000 === "string")
                        message.totalAmount1000 = parseInt(object.totalAmount1000, 10);
                    else if (typeof object.totalAmount1000 === "number")
                        message.totalAmount1000 = object.totalAmount1000;
                    else if (typeof object.totalAmount1000 === "object")
                        message.totalAmount1000 = new $util.LongBits(object.totalAmount1000.low >>> 0, object.totalAmount1000.high >>> 0).toNumber();
                if (object.totalCurrencyCode != null)
                    message.totalCurrencyCode = String(object.totalCurrencyCode);
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.OrderMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                if (object.messageVersion != null)
                    message.messageVersion = object.messageVersion | 0;
                if (object.orderRequestMessageId != null) {
                    if (!$util.isObject(object.orderRequestMessageId))
                        throw TypeError(".proto.Message.OrderMessage.orderRequestMessageId: object expected");
                    message.orderRequestMessageId = $root.proto.MessageKey.fromObject(object.orderRequestMessageId, q + 1);
                }
                if (object.catalogType != null)
                    message.catalogType = String(object.catalogType);
                return message;
            };

            /**
             * Creates a plain object from an OrderMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.OrderMessage
             * @static
             * @param {proto.Message.OrderMessage} message OrderMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            OrderMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.orderId != null && Object.hasOwnProperty.call(message, "orderId"))
                    object.orderId = message.orderId;
                if (message.thumbnail != null && Object.hasOwnProperty.call(message, "thumbnail"))
                    object.thumbnail = options.bytes === String ? $util.base64.encode(message.thumbnail, 0, message.thumbnail.length) : options.bytes === Array ? Array.prototype.slice.call(message.thumbnail) : message.thumbnail;
                if (message.itemCount != null && Object.hasOwnProperty.call(message, "itemCount"))
                    object.itemCount = message.itemCount;
                if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                    object.status = options.enums === String ? $root.proto.Message.OrderMessage.OrderStatus[message.status] === undefined ? message.status : $root.proto.Message.OrderMessage.OrderStatus[message.status] : message.status;
                if (message.surface != null && Object.hasOwnProperty.call(message, "surface"))
                    object.surface = options.enums === String ? $root.proto.Message.OrderMessage.OrderSurface[message.surface] === undefined ? message.surface : $root.proto.Message.OrderMessage.OrderSurface[message.surface] : message.surface;
                if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                    object.message = message.message;
                if (message.orderTitle != null && Object.hasOwnProperty.call(message, "orderTitle"))
                    object.orderTitle = message.orderTitle;
                if (message.sellerJid != null && Object.hasOwnProperty.call(message, "sellerJid"))
                    object.sellerJid = message.sellerJid;
                if (message.token != null && Object.hasOwnProperty.call(message, "token"))
                    object.token = message.token;
                if (message.totalAmount1000 != null && Object.hasOwnProperty.call(message, "totalAmount1000"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.totalAmount1000 = typeof message.totalAmount1000 === "number" ? BigInt(message.totalAmount1000) : $util.Long.fromBits(message.totalAmount1000.low >>> 0, message.totalAmount1000.high >>> 0, false).toBigInt();
                    else if (typeof message.totalAmount1000 === "number")
                        object.totalAmount1000 = options.longs === String ? String(message.totalAmount1000) : message.totalAmount1000;
                    else
                        object.totalAmount1000 = options.longs === String ? $util.Long.prototype.toString.call(message.totalAmount1000) : options.longs === Number ? new $util.LongBits(message.totalAmount1000.low >>> 0, message.totalAmount1000.high >>> 0).toNumber() : message.totalAmount1000;
                if (message.totalCurrencyCode != null && Object.hasOwnProperty.call(message, "totalCurrencyCode"))
                    object.totalCurrencyCode = message.totalCurrencyCode;
                if (message.messageVersion != null && Object.hasOwnProperty.call(message, "messageVersion"))
                    object.messageVersion = message.messageVersion;
                if (message.orderRequestMessageId != null && Object.hasOwnProperty.call(message, "orderRequestMessageId"))
                    object.orderRequestMessageId = $root.proto.MessageKey.toObject(message.orderRequestMessageId, options, q + 1);
                if (message.catalogType != null && Object.hasOwnProperty.call(message, "catalogType"))
                    object.catalogType = message.catalogType;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                return object;
            };

            /**
             * Converts this OrderMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.OrderMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            OrderMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for OrderMessage
             * @function getTypeUrl
             * @memberof proto.Message.OrderMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            OrderMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.OrderMessage";
            };

            /**
             * OrderStatus enum.
             * @name proto.Message.OrderMessage.OrderStatus
             * @enum {number}
             * @property {number} INQUIRY=1 INQUIRY value
             * @property {number} ACCEPTED=2 ACCEPTED value
             * @property {number} DECLINED=3 DECLINED value
             */
            OrderMessage.OrderStatus = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[1] = "INQUIRY"] = 1;
                values[valuesById[2] = "ACCEPTED"] = 2;
                values[valuesById[3] = "DECLINED"] = 3;
                return values;
            })();

            /**
             * OrderSurface enum.
             * @name proto.Message.OrderMessage.OrderSurface
             * @enum {number}
             * @property {number} CATALOG=1 CATALOG value
             */
            OrderMessage.OrderSurface = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[1] = "CATALOG"] = 1;
                return values;
            })();

            return OrderMessage;
        })();

        Message.PaymentExtendedMetadata = (function() {

            /**
             * Properties of a PaymentExtendedMetadata.
             * @memberof proto.Message
             * @interface IPaymentExtendedMetadata
             * @property {number|null} [type] PaymentExtendedMetadata type
             * @property {string|null} [platform] PaymentExtendedMetadata platform
             */

            /**
             * Constructs a new PaymentExtendedMetadata.
             * @memberof proto.Message
             * @classdesc Represents a PaymentExtendedMetadata.
             * @implements IPaymentExtendedMetadata
             * @constructor
             * @param {proto.Message.IPaymentExtendedMetadata=} [properties] Properties to set
             */
            function PaymentExtendedMetadata(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PaymentExtendedMetadata type.
             * @member {number|null|undefined} type
             * @memberof proto.Message.PaymentExtendedMetadata
             * @instance
             */
            PaymentExtendedMetadata.prototype.type = null;

            /**
             * PaymentExtendedMetadata platform.
             * @member {string|null|undefined} platform
             * @memberof proto.Message.PaymentExtendedMetadata
             * @instance
             */
            PaymentExtendedMetadata.prototype.platform = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentExtendedMetadata.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentExtendedMetadata.prototype, "_platform", {
                get: $util.oneOfGetter($oneOfFields = ["platform"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PaymentExtendedMetadata instance using the specified properties.
             * @function create
             * @memberof proto.Message.PaymentExtendedMetadata
             * @static
             * @param {proto.Message.IPaymentExtendedMetadata=} [properties] Properties to set
             * @returns {proto.Message.PaymentExtendedMetadata} PaymentExtendedMetadata instance
             */
            PaymentExtendedMetadata.create = function create(properties) {
                return new PaymentExtendedMetadata(properties);
            };

            /**
             * Encodes the specified PaymentExtendedMetadata message. Does not implicitly {@link proto.Message.PaymentExtendedMetadata.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.PaymentExtendedMetadata
             * @static
             * @param {proto.Message.IPaymentExtendedMetadata} message PaymentExtendedMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PaymentExtendedMetadata.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.type);
                if (message.platform != null && Object.hasOwnProperty.call(message, "platform"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.platform);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PaymentExtendedMetadata message, length delimited. Does not implicitly {@link proto.Message.PaymentExtendedMetadata.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.PaymentExtendedMetadata
             * @static
             * @param {proto.Message.IPaymentExtendedMetadata} message PaymentExtendedMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PaymentExtendedMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PaymentExtendedMetadata message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.PaymentExtendedMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.PaymentExtendedMetadata} PaymentExtendedMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PaymentExtendedMetadata.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.type = reader.uint32();
                            message._type = "type";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.platform = reader.stringVerify();
                            message._platform = "platform";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PaymentExtendedMetadata message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.PaymentExtendedMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.PaymentExtendedMetadata} PaymentExtendedMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PaymentExtendedMetadata.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PaymentExtendedMetadata message.
             * @function verify
             * @memberof proto.Message.PaymentExtendedMetadata
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PaymentExtendedMetadata.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                    properties._type = 1;
                    if (!$util.isInteger(message.type))
                        return "type: integer expected";
                }
                if (message.platform != null && Object.hasOwnProperty.call(message, "platform")) {
                    properties._platform = 1;
                    if (!$util.isString(message.platform))
                        return "platform: string expected";
                }
                return null;
            };

            /**
             * Creates a PaymentExtendedMetadata message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.PaymentExtendedMetadata
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.PaymentExtendedMetadata} PaymentExtendedMetadata
             */
            PaymentExtendedMetadata.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.PaymentExtendedMetadata: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.type != null)
                    message.type = object.type >>> 0;
                if (object.platform != null)
                    message.platform = String(object.platform);
                return message;
            };

            /**
             * Creates a plain object from a PaymentExtendedMetadata message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.PaymentExtendedMetadata
             * @static
             * @param {proto.Message.PaymentExtendedMetadata} message PaymentExtendedMetadata
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PaymentExtendedMetadata.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    object.type = message.type;
                if (message.platform != null && Object.hasOwnProperty.call(message, "platform"))
                    object.platform = message.platform;
                return object;
            };

            /**
             * Converts this PaymentExtendedMetadata to JSON.
             * @function toJSON
             * @memberof proto.Message.PaymentExtendedMetadata
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PaymentExtendedMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PaymentExtendedMetadata
             * @function getTypeUrl
             * @memberof proto.Message.PaymentExtendedMetadata
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PaymentExtendedMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.PaymentExtendedMetadata";
            };

            return PaymentExtendedMetadata;
        })();

        Message.PaymentInviteMessage = (function() {

            /**
             * Properties of a PaymentInviteMessage.
             * @memberof proto.Message
             * @interface IPaymentInviteMessage
             * @property {proto.Message.PaymentInviteMessage.ServiceType|null} [serviceType] PaymentInviteMessage serviceType
             * @property {number|Long|null} [expiryTimestamp] PaymentInviteMessage expiryTimestamp
             * @property {boolean|null} [incentiveEligible] PaymentInviteMessage incentiveEligible
             * @property {string|null} [referralId] PaymentInviteMessage referralId
             * @property {proto.Message.PaymentInviteMessage.InviteType|null} [inviteType] PaymentInviteMessage inviteType
             */

            /**
             * Constructs a new PaymentInviteMessage.
             * @memberof proto.Message
             * @classdesc Represents a PaymentInviteMessage.
             * @implements IPaymentInviteMessage
             * @constructor
             * @param {proto.Message.IPaymentInviteMessage=} [properties] Properties to set
             */
            function PaymentInviteMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PaymentInviteMessage serviceType.
             * @member {proto.Message.PaymentInviteMessage.ServiceType|null|undefined} serviceType
             * @memberof proto.Message.PaymentInviteMessage
             * @instance
             */
            PaymentInviteMessage.prototype.serviceType = null;

            /**
             * PaymentInviteMessage expiryTimestamp.
             * @member {number|Long|null|undefined} expiryTimestamp
             * @memberof proto.Message.PaymentInviteMessage
             * @instance
             */
            PaymentInviteMessage.prototype.expiryTimestamp = null;

            /**
             * PaymentInviteMessage incentiveEligible.
             * @member {boolean|null|undefined} incentiveEligible
             * @memberof proto.Message.PaymentInviteMessage
             * @instance
             */
            PaymentInviteMessage.prototype.incentiveEligible = null;

            /**
             * PaymentInviteMessage referralId.
             * @member {string|null|undefined} referralId
             * @memberof proto.Message.PaymentInviteMessage
             * @instance
             */
            PaymentInviteMessage.prototype.referralId = null;

            /**
             * PaymentInviteMessage inviteType.
             * @member {proto.Message.PaymentInviteMessage.InviteType|null|undefined} inviteType
             * @memberof proto.Message.PaymentInviteMessage
             * @instance
             */
            PaymentInviteMessage.prototype.inviteType = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentInviteMessage.prototype, "_serviceType", {
                get: $util.oneOfGetter($oneOfFields = ["serviceType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentInviteMessage.prototype, "_expiryTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["expiryTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentInviteMessage.prototype, "_incentiveEligible", {
                get: $util.oneOfGetter($oneOfFields = ["incentiveEligible"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentInviteMessage.prototype, "_referralId", {
                get: $util.oneOfGetter($oneOfFields = ["referralId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentInviteMessage.prototype, "_inviteType", {
                get: $util.oneOfGetter($oneOfFields = ["inviteType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PaymentInviteMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.PaymentInviteMessage
             * @static
             * @param {proto.Message.IPaymentInviteMessage=} [properties] Properties to set
             * @returns {proto.Message.PaymentInviteMessage} PaymentInviteMessage instance
             */
            PaymentInviteMessage.create = function create(properties) {
                return new PaymentInviteMessage(properties);
            };

            /**
             * Encodes the specified PaymentInviteMessage message. Does not implicitly {@link proto.Message.PaymentInviteMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.PaymentInviteMessage
             * @static
             * @param {proto.Message.IPaymentInviteMessage} message PaymentInviteMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PaymentInviteMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.serviceType != null && Object.hasOwnProperty.call(message, "serviceType"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.serviceType);
                if (message.expiryTimestamp != null && Object.hasOwnProperty.call(message, "expiryTimestamp"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.expiryTimestamp);
                if (message.incentiveEligible != null && Object.hasOwnProperty.call(message, "incentiveEligible"))
                    writer.uint32(/* id 3, wireType 0 =*/24).bool(message.incentiveEligible);
                if (message.referralId != null && Object.hasOwnProperty.call(message, "referralId"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.referralId);
                if (message.inviteType != null && Object.hasOwnProperty.call(message, "inviteType"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int32(message.inviteType);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PaymentInviteMessage message, length delimited. Does not implicitly {@link proto.Message.PaymentInviteMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.PaymentInviteMessage
             * @static
             * @param {proto.Message.IPaymentInviteMessage} message PaymentInviteMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PaymentInviteMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PaymentInviteMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.PaymentInviteMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.PaymentInviteMessage} PaymentInviteMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PaymentInviteMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.serviceType = reader.int32();
                            message._serviceType = "serviceType";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.expiryTimestamp = reader.int64();
                            message._expiryTimestamp = "expiryTimestamp";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.incentiveEligible = reader.bool();
                            message._incentiveEligible = "incentiveEligible";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.referralId = reader.stringVerify();
                            message._referralId = "referralId";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.inviteType = reader.int32();
                            message._inviteType = "inviteType";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PaymentInviteMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.PaymentInviteMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.PaymentInviteMessage} PaymentInviteMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PaymentInviteMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PaymentInviteMessage message.
             * @function verify
             * @memberof proto.Message.PaymentInviteMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PaymentInviteMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.serviceType != null && Object.hasOwnProperty.call(message, "serviceType")) {
                    properties._serviceType = 1;
                    if (typeof message.serviceType !== "number" || (message.serviceType | 0) !== message.serviceType)
                        return "serviceType: enum value expected";
                }
                if (message.expiryTimestamp != null && Object.hasOwnProperty.call(message, "expiryTimestamp")) {
                    properties._expiryTimestamp = 1;
                    if (!$util.isInteger(message.expiryTimestamp) && !(message.expiryTimestamp && $util.isInteger(message.expiryTimestamp.low) && $util.isInteger(message.expiryTimestamp.high)))
                        return "expiryTimestamp: integer|Long expected";
                }
                if (message.incentiveEligible != null && Object.hasOwnProperty.call(message, "incentiveEligible")) {
                    properties._incentiveEligible = 1;
                    if (typeof message.incentiveEligible !== "boolean")
                        return "incentiveEligible: boolean expected";
                }
                if (message.referralId != null && Object.hasOwnProperty.call(message, "referralId")) {
                    properties._referralId = 1;
                    if (!$util.isString(message.referralId))
                        return "referralId: string expected";
                }
                if (message.inviteType != null && Object.hasOwnProperty.call(message, "inviteType")) {
                    properties._inviteType = 1;
                    if (typeof message.inviteType !== "number" || (message.inviteType | 0) !== message.inviteType)
                        return "inviteType: enum value expected";
                }
                return null;
            };

            /**
             * Creates a PaymentInviteMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.PaymentInviteMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.PaymentInviteMessage} PaymentInviteMessage
             */
            PaymentInviteMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.PaymentInviteMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.serviceType) {
                case "UNKNOWN":
                case 0:
                    message.serviceType = 0;
                    break;
                case "FBPAY":
                case 1:
                    message.serviceType = 1;
                    break;
                case "NOVI":
                case 2:
                    message.serviceType = 2;
                    break;
                case "UPI":
                case 3:
                    message.serviceType = 3;
                    break;
                default:
                    if (typeof object.serviceType === "number" && (object.serviceType | 0) === object.serviceType)
                        message.serviceType = object.serviceType;
                }
                if (object.expiryTimestamp != null)
                    if ($util.Long)
                        message.expiryTimestamp = $util.Long.fromValue(object.expiryTimestamp, false);
                    else if (typeof object.expiryTimestamp === "string")
                        message.expiryTimestamp = parseInt(object.expiryTimestamp, 10);
                    else if (typeof object.expiryTimestamp === "number")
                        message.expiryTimestamp = object.expiryTimestamp;
                    else if (typeof object.expiryTimestamp === "object")
                        message.expiryTimestamp = new $util.LongBits(object.expiryTimestamp.low >>> 0, object.expiryTimestamp.high >>> 0).toNumber();
                if (object.incentiveEligible != null)
                    message.incentiveEligible = Boolean(object.incentiveEligible);
                if (object.referralId != null)
                    message.referralId = String(object.referralId);
                switch (object.inviteType) {
                case "DEFAULT":
                case 0:
                    message.inviteType = 0;
                    break;
                case "MAPPER":
                case 1:
                    message.inviteType = 1;
                    break;
                default:
                    if (typeof object.inviteType === "number" && (object.inviteType | 0) === object.inviteType)
                        message.inviteType = object.inviteType;
                }
                return message;
            };

            /**
             * Creates a plain object from a PaymentInviteMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.PaymentInviteMessage
             * @static
             * @param {proto.Message.PaymentInviteMessage} message PaymentInviteMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PaymentInviteMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.serviceType != null && Object.hasOwnProperty.call(message, "serviceType"))
                    object.serviceType = options.enums === String ? $root.proto.Message.PaymentInviteMessage.ServiceType[message.serviceType] === undefined ? message.serviceType : $root.proto.Message.PaymentInviteMessage.ServiceType[message.serviceType] : message.serviceType;
                if (message.expiryTimestamp != null && Object.hasOwnProperty.call(message, "expiryTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.expiryTimestamp = typeof message.expiryTimestamp === "number" ? BigInt(message.expiryTimestamp) : $util.Long.fromBits(message.expiryTimestamp.low >>> 0, message.expiryTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.expiryTimestamp === "number")
                        object.expiryTimestamp = options.longs === String ? String(message.expiryTimestamp) : message.expiryTimestamp;
                    else
                        object.expiryTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.expiryTimestamp) : options.longs === Number ? new $util.LongBits(message.expiryTimestamp.low >>> 0, message.expiryTimestamp.high >>> 0).toNumber() : message.expiryTimestamp;
                if (message.incentiveEligible != null && Object.hasOwnProperty.call(message, "incentiveEligible"))
                    object.incentiveEligible = message.incentiveEligible;
                if (message.referralId != null && Object.hasOwnProperty.call(message, "referralId"))
                    object.referralId = message.referralId;
                if (message.inviteType != null && Object.hasOwnProperty.call(message, "inviteType"))
                    object.inviteType = options.enums === String ? $root.proto.Message.PaymentInviteMessage.InviteType[message.inviteType] === undefined ? message.inviteType : $root.proto.Message.PaymentInviteMessage.InviteType[message.inviteType] : message.inviteType;
                return object;
            };

            /**
             * Converts this PaymentInviteMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.PaymentInviteMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PaymentInviteMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PaymentInviteMessage
             * @function getTypeUrl
             * @memberof proto.Message.PaymentInviteMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PaymentInviteMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.PaymentInviteMessage";
            };

            /**
             * InviteType enum.
             * @name proto.Message.PaymentInviteMessage.InviteType
             * @enum {number}
             * @property {number} DEFAULT=0 DEFAULT value
             * @property {number} MAPPER=1 MAPPER value
             */
            PaymentInviteMessage.InviteType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "DEFAULT"] = 0;
                values[valuesById[1] = "MAPPER"] = 1;
                return values;
            })();

            /**
             * ServiceType enum.
             * @name proto.Message.PaymentInviteMessage.ServiceType
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} FBPAY=1 FBPAY value
             * @property {number} NOVI=2 NOVI value
             * @property {number} UPI=3 UPI value
             */
            PaymentInviteMessage.ServiceType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "FBPAY"] = 1;
                values[valuesById[2] = "NOVI"] = 2;
                values[valuesById[3] = "UPI"] = 3;
                return values;
            })();

            return PaymentInviteMessage;
        })();

        Message.PaymentLinkMetadata = (function() {

            /**
             * Properties of a PaymentLinkMetadata.
             * @memberof proto.Message
             * @interface IPaymentLinkMetadata
             * @property {proto.Message.PaymentLinkMetadata.IPaymentLinkButton|null} [button] PaymentLinkMetadata button
             * @property {proto.Message.PaymentLinkMetadata.IPaymentLinkHeader|null} [header] PaymentLinkMetadata header
             * @property {proto.Message.PaymentLinkMetadata.IPaymentLinkProvider|null} [provider] PaymentLinkMetadata provider
             */

            /**
             * Constructs a new PaymentLinkMetadata.
             * @memberof proto.Message
             * @classdesc Represents a PaymentLinkMetadata.
             * @implements IPaymentLinkMetadata
             * @constructor
             * @param {proto.Message.IPaymentLinkMetadata=} [properties] Properties to set
             */
            function PaymentLinkMetadata(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PaymentLinkMetadata button.
             * @member {proto.Message.PaymentLinkMetadata.IPaymentLinkButton|null|undefined} button
             * @memberof proto.Message.PaymentLinkMetadata
             * @instance
             */
            PaymentLinkMetadata.prototype.button = null;

            /**
             * PaymentLinkMetadata header.
             * @member {proto.Message.PaymentLinkMetadata.IPaymentLinkHeader|null|undefined} header
             * @memberof proto.Message.PaymentLinkMetadata
             * @instance
             */
            PaymentLinkMetadata.prototype.header = null;

            /**
             * PaymentLinkMetadata provider.
             * @member {proto.Message.PaymentLinkMetadata.IPaymentLinkProvider|null|undefined} provider
             * @memberof proto.Message.PaymentLinkMetadata
             * @instance
             */
            PaymentLinkMetadata.prototype.provider = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentLinkMetadata.prototype, "_button", {
                get: $util.oneOfGetter($oneOfFields = ["button"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentLinkMetadata.prototype, "_header", {
                get: $util.oneOfGetter($oneOfFields = ["header"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentLinkMetadata.prototype, "_provider", {
                get: $util.oneOfGetter($oneOfFields = ["provider"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PaymentLinkMetadata instance using the specified properties.
             * @function create
             * @memberof proto.Message.PaymentLinkMetadata
             * @static
             * @param {proto.Message.IPaymentLinkMetadata=} [properties] Properties to set
             * @returns {proto.Message.PaymentLinkMetadata} PaymentLinkMetadata instance
             */
            PaymentLinkMetadata.create = function create(properties) {
                return new PaymentLinkMetadata(properties);
            };

            /**
             * Encodes the specified PaymentLinkMetadata message. Does not implicitly {@link proto.Message.PaymentLinkMetadata.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.PaymentLinkMetadata
             * @static
             * @param {proto.Message.IPaymentLinkMetadata} message PaymentLinkMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PaymentLinkMetadata.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.button != null && Object.hasOwnProperty.call(message, "button"))
                    $root.proto.Message.PaymentLinkMetadata.PaymentLinkButton.encode(message.button, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.header != null && Object.hasOwnProperty.call(message, "header"))
                    $root.proto.Message.PaymentLinkMetadata.PaymentLinkHeader.encode(message.header, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.provider != null && Object.hasOwnProperty.call(message, "provider"))
                    $root.proto.Message.PaymentLinkMetadata.PaymentLinkProvider.encode(message.provider, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PaymentLinkMetadata message, length delimited. Does not implicitly {@link proto.Message.PaymentLinkMetadata.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.PaymentLinkMetadata
             * @static
             * @param {proto.Message.IPaymentLinkMetadata} message PaymentLinkMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PaymentLinkMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PaymentLinkMetadata message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.PaymentLinkMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.PaymentLinkMetadata} PaymentLinkMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PaymentLinkMetadata.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.button = $root.proto.Message.PaymentLinkMetadata.PaymentLinkButton.decode(reader, reader.uint32(), undefined, q + 1, message.button);
                            message._button = "button";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.header = $root.proto.Message.PaymentLinkMetadata.PaymentLinkHeader.decode(reader, reader.uint32(), undefined, q + 1, message.header);
                            message._header = "header";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.provider = $root.proto.Message.PaymentLinkMetadata.PaymentLinkProvider.decode(reader, reader.uint32(), undefined, q + 1, message.provider);
                            message._provider = "provider";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PaymentLinkMetadata message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.PaymentLinkMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.PaymentLinkMetadata} PaymentLinkMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PaymentLinkMetadata.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PaymentLinkMetadata message.
             * @function verify
             * @memberof proto.Message.PaymentLinkMetadata
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PaymentLinkMetadata.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.button != null && Object.hasOwnProperty.call(message, "button")) {
                    properties._button = 1;
                    {
                        var error = $root.proto.Message.PaymentLinkMetadata.PaymentLinkButton.verify(message.button, q + 1);
                        if (error)
                            return "button." + error;
                    }
                }
                if (message.header != null && Object.hasOwnProperty.call(message, "header")) {
                    properties._header = 1;
                    {
                        var error = $root.proto.Message.PaymentLinkMetadata.PaymentLinkHeader.verify(message.header, q + 1);
                        if (error)
                            return "header." + error;
                    }
                }
                if (message.provider != null && Object.hasOwnProperty.call(message, "provider")) {
                    properties._provider = 1;
                    {
                        var error = $root.proto.Message.PaymentLinkMetadata.PaymentLinkProvider.verify(message.provider, q + 1);
                        if (error)
                            return "provider." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a PaymentLinkMetadata message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.PaymentLinkMetadata
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.PaymentLinkMetadata} PaymentLinkMetadata
             */
            PaymentLinkMetadata.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.PaymentLinkMetadata: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.button != null) {
                    if (!$util.isObject(object.button))
                        throw TypeError(".proto.Message.PaymentLinkMetadata.button: object expected");
                    message.button = $root.proto.Message.PaymentLinkMetadata.PaymentLinkButton.fromObject(object.button, q + 1);
                }
                if (object.header != null) {
                    if (!$util.isObject(object.header))
                        throw TypeError(".proto.Message.PaymentLinkMetadata.header: object expected");
                    message.header = $root.proto.Message.PaymentLinkMetadata.PaymentLinkHeader.fromObject(object.header, q + 1);
                }
                if (object.provider != null) {
                    if (!$util.isObject(object.provider))
                        throw TypeError(".proto.Message.PaymentLinkMetadata.provider: object expected");
                    message.provider = $root.proto.Message.PaymentLinkMetadata.PaymentLinkProvider.fromObject(object.provider, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a PaymentLinkMetadata message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.PaymentLinkMetadata
             * @static
             * @param {proto.Message.PaymentLinkMetadata} message PaymentLinkMetadata
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PaymentLinkMetadata.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.button != null && Object.hasOwnProperty.call(message, "button"))
                    object.button = $root.proto.Message.PaymentLinkMetadata.PaymentLinkButton.toObject(message.button, options, q + 1);
                if (message.header != null && Object.hasOwnProperty.call(message, "header"))
                    object.header = $root.proto.Message.PaymentLinkMetadata.PaymentLinkHeader.toObject(message.header, options, q + 1);
                if (message.provider != null && Object.hasOwnProperty.call(message, "provider"))
                    object.provider = $root.proto.Message.PaymentLinkMetadata.PaymentLinkProvider.toObject(message.provider, options, q + 1);
                return object;
            };

            /**
             * Converts this PaymentLinkMetadata to JSON.
             * @function toJSON
             * @memberof proto.Message.PaymentLinkMetadata
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PaymentLinkMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PaymentLinkMetadata
             * @function getTypeUrl
             * @memberof proto.Message.PaymentLinkMetadata
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PaymentLinkMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.PaymentLinkMetadata";
            };

            PaymentLinkMetadata.PaymentLinkButton = (function() {

                /**
                 * Properties of a PaymentLinkButton.
                 * @memberof proto.Message.PaymentLinkMetadata
                 * @interface IPaymentLinkButton
                 * @property {string|null} [displayText] PaymentLinkButton displayText
                 */

                /**
                 * Constructs a new PaymentLinkButton.
                 * @memberof proto.Message.PaymentLinkMetadata
                 * @classdesc Represents a PaymentLinkButton.
                 * @implements IPaymentLinkButton
                 * @constructor
                 * @param {proto.Message.PaymentLinkMetadata.IPaymentLinkButton=} [properties] Properties to set
                 */
                function PaymentLinkButton(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * PaymentLinkButton displayText.
                 * @member {string|null|undefined} displayText
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkButton
                 * @instance
                 */
                PaymentLinkButton.prototype.displayText = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PaymentLinkButton.prototype, "_displayText", {
                    get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new PaymentLinkButton instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkButton
                 * @static
                 * @param {proto.Message.PaymentLinkMetadata.IPaymentLinkButton=} [properties] Properties to set
                 * @returns {proto.Message.PaymentLinkMetadata.PaymentLinkButton} PaymentLinkButton instance
                 */
                PaymentLinkButton.create = function create(properties) {
                    return new PaymentLinkButton(properties);
                };

                /**
                 * Encodes the specified PaymentLinkButton message. Does not implicitly {@link proto.Message.PaymentLinkMetadata.PaymentLinkButton.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkButton
                 * @static
                 * @param {proto.Message.PaymentLinkMetadata.IPaymentLinkButton} message PaymentLinkButton message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PaymentLinkButton.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.displayText);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified PaymentLinkButton message, length delimited. Does not implicitly {@link proto.Message.PaymentLinkMetadata.PaymentLinkButton.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkButton
                 * @static
                 * @param {proto.Message.PaymentLinkMetadata.IPaymentLinkButton} message PaymentLinkButton message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PaymentLinkButton.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a PaymentLinkButton message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkButton
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.PaymentLinkMetadata.PaymentLinkButton} PaymentLinkButton
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PaymentLinkButton.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.displayText = reader.stringVerify();
                                message._displayText = "displayText";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a PaymentLinkButton message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkButton
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.PaymentLinkMetadata.PaymentLinkButton} PaymentLinkButton
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PaymentLinkButton.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a PaymentLinkButton message.
                 * @function verify
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkButton
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                PaymentLinkButton.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText")) {
                        properties._displayText = 1;
                        if (!$util.isString(message.displayText))
                            return "displayText: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a PaymentLinkButton message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkButton
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.PaymentLinkMetadata.PaymentLinkButton} PaymentLinkButton
                 */
                PaymentLinkButton.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.PaymentLinkMetadata.PaymentLinkButton: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.displayText != null)
                        message.displayText = String(object.displayText);
                    return message;
                };

                /**
                 * Creates a plain object from a PaymentLinkButton message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkButton
                 * @static
                 * @param {proto.Message.PaymentLinkMetadata.PaymentLinkButton} message PaymentLinkButton
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                PaymentLinkButton.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText"))
                        object.displayText = message.displayText;
                    return object;
                };

                /**
                 * Converts this PaymentLinkButton to JSON.
                 * @function toJSON
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkButton
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                PaymentLinkButton.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for PaymentLinkButton
                 * @function getTypeUrl
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkButton
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                PaymentLinkButton.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.PaymentLinkMetadata.PaymentLinkButton";
                };

                return PaymentLinkButton;
            })();

            PaymentLinkMetadata.PaymentLinkHeader = (function() {

                /**
                 * Properties of a PaymentLinkHeader.
                 * @memberof proto.Message.PaymentLinkMetadata
                 * @interface IPaymentLinkHeader
                 * @property {proto.Message.PaymentLinkMetadata.PaymentLinkHeader.PaymentLinkHeaderType|null} [headerType] PaymentLinkHeader headerType
                 */

                /**
                 * Constructs a new PaymentLinkHeader.
                 * @memberof proto.Message.PaymentLinkMetadata
                 * @classdesc Represents a PaymentLinkHeader.
                 * @implements IPaymentLinkHeader
                 * @constructor
                 * @param {proto.Message.PaymentLinkMetadata.IPaymentLinkHeader=} [properties] Properties to set
                 */
                function PaymentLinkHeader(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * PaymentLinkHeader headerType.
                 * @member {proto.Message.PaymentLinkMetadata.PaymentLinkHeader.PaymentLinkHeaderType|null|undefined} headerType
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkHeader
                 * @instance
                 */
                PaymentLinkHeader.prototype.headerType = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PaymentLinkHeader.prototype, "_headerType", {
                    get: $util.oneOfGetter($oneOfFields = ["headerType"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new PaymentLinkHeader instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkHeader
                 * @static
                 * @param {proto.Message.PaymentLinkMetadata.IPaymentLinkHeader=} [properties] Properties to set
                 * @returns {proto.Message.PaymentLinkMetadata.PaymentLinkHeader} PaymentLinkHeader instance
                 */
                PaymentLinkHeader.create = function create(properties) {
                    return new PaymentLinkHeader(properties);
                };

                /**
                 * Encodes the specified PaymentLinkHeader message. Does not implicitly {@link proto.Message.PaymentLinkMetadata.PaymentLinkHeader.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkHeader
                 * @static
                 * @param {proto.Message.PaymentLinkMetadata.IPaymentLinkHeader} message PaymentLinkHeader message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PaymentLinkHeader.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.headerType != null && Object.hasOwnProperty.call(message, "headerType"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int32(message.headerType);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified PaymentLinkHeader message, length delimited. Does not implicitly {@link proto.Message.PaymentLinkMetadata.PaymentLinkHeader.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkHeader
                 * @static
                 * @param {proto.Message.PaymentLinkMetadata.IPaymentLinkHeader} message PaymentLinkHeader message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PaymentLinkHeader.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a PaymentLinkHeader message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkHeader
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.PaymentLinkMetadata.PaymentLinkHeader} PaymentLinkHeader
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PaymentLinkHeader.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                message.headerType = reader.int32();
                                message._headerType = "headerType";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a PaymentLinkHeader message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkHeader
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.PaymentLinkMetadata.PaymentLinkHeader} PaymentLinkHeader
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PaymentLinkHeader.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a PaymentLinkHeader message.
                 * @function verify
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkHeader
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                PaymentLinkHeader.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.headerType != null && Object.hasOwnProperty.call(message, "headerType")) {
                        properties._headerType = 1;
                        if (typeof message.headerType !== "number" || (message.headerType | 0) !== message.headerType)
                            return "headerType: enum value expected";
                    }
                    return null;
                };

                /**
                 * Creates a PaymentLinkHeader message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkHeader
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.PaymentLinkMetadata.PaymentLinkHeader} PaymentLinkHeader
                 */
                PaymentLinkHeader.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.PaymentLinkMetadata.PaymentLinkHeader: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    switch (object.headerType) {
                    case "LINK_PREVIEW":
                    case 0:
                        message.headerType = 0;
                        break;
                    case "ORDER":
                    case 1:
                        message.headerType = 1;
                        break;
                    default:
                        if (typeof object.headerType === "number" && (object.headerType | 0) === object.headerType)
                            message.headerType = object.headerType;
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a PaymentLinkHeader message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkHeader
                 * @static
                 * @param {proto.Message.PaymentLinkMetadata.PaymentLinkHeader} message PaymentLinkHeader
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                PaymentLinkHeader.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.headerType != null && Object.hasOwnProperty.call(message, "headerType"))
                        object.headerType = options.enums === String ? $root.proto.Message.PaymentLinkMetadata.PaymentLinkHeader.PaymentLinkHeaderType[message.headerType] === undefined ? message.headerType : $root.proto.Message.PaymentLinkMetadata.PaymentLinkHeader.PaymentLinkHeaderType[message.headerType] : message.headerType;
                    return object;
                };

                /**
                 * Converts this PaymentLinkHeader to JSON.
                 * @function toJSON
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkHeader
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                PaymentLinkHeader.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for PaymentLinkHeader
                 * @function getTypeUrl
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkHeader
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                PaymentLinkHeader.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.PaymentLinkMetadata.PaymentLinkHeader";
                };

                /**
                 * PaymentLinkHeaderType enum.
                 * @name proto.Message.PaymentLinkMetadata.PaymentLinkHeader.PaymentLinkHeaderType
                 * @enum {number}
                 * @property {number} LINK_PREVIEW=0 LINK_PREVIEW value
                 * @property {number} ORDER=1 ORDER value
                 */
                PaymentLinkHeader.PaymentLinkHeaderType = (function() {
                    var valuesById = {}, values = Object.create(valuesById);
                    values[valuesById[0] = "LINK_PREVIEW"] = 0;
                    values[valuesById[1] = "ORDER"] = 1;
                    return values;
                })();

                return PaymentLinkHeader;
            })();

            PaymentLinkMetadata.PaymentLinkProvider = (function() {

                /**
                 * Properties of a PaymentLinkProvider.
                 * @memberof proto.Message.PaymentLinkMetadata
                 * @interface IPaymentLinkProvider
                 * @property {string|null} [paramsJson] PaymentLinkProvider paramsJson
                 */

                /**
                 * Constructs a new PaymentLinkProvider.
                 * @memberof proto.Message.PaymentLinkMetadata
                 * @classdesc Represents a PaymentLinkProvider.
                 * @implements IPaymentLinkProvider
                 * @constructor
                 * @param {proto.Message.PaymentLinkMetadata.IPaymentLinkProvider=} [properties] Properties to set
                 */
                function PaymentLinkProvider(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * PaymentLinkProvider paramsJson.
                 * @member {string|null|undefined} paramsJson
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkProvider
                 * @instance
                 */
                PaymentLinkProvider.prototype.paramsJson = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PaymentLinkProvider.prototype, "_paramsJson", {
                    get: $util.oneOfGetter($oneOfFields = ["paramsJson"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new PaymentLinkProvider instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkProvider
                 * @static
                 * @param {proto.Message.PaymentLinkMetadata.IPaymentLinkProvider=} [properties] Properties to set
                 * @returns {proto.Message.PaymentLinkMetadata.PaymentLinkProvider} PaymentLinkProvider instance
                 */
                PaymentLinkProvider.create = function create(properties) {
                    return new PaymentLinkProvider(properties);
                };

                /**
                 * Encodes the specified PaymentLinkProvider message. Does not implicitly {@link proto.Message.PaymentLinkMetadata.PaymentLinkProvider.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkProvider
                 * @static
                 * @param {proto.Message.PaymentLinkMetadata.IPaymentLinkProvider} message PaymentLinkProvider message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PaymentLinkProvider.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.paramsJson != null && Object.hasOwnProperty.call(message, "paramsJson"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.paramsJson);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified PaymentLinkProvider message, length delimited. Does not implicitly {@link proto.Message.PaymentLinkMetadata.PaymentLinkProvider.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkProvider
                 * @static
                 * @param {proto.Message.PaymentLinkMetadata.IPaymentLinkProvider} message PaymentLinkProvider message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PaymentLinkProvider.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a PaymentLinkProvider message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkProvider
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.PaymentLinkMetadata.PaymentLinkProvider} PaymentLinkProvider
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PaymentLinkProvider.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.paramsJson = reader.stringVerify();
                                message._paramsJson = "paramsJson";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a PaymentLinkProvider message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkProvider
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.PaymentLinkMetadata.PaymentLinkProvider} PaymentLinkProvider
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PaymentLinkProvider.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a PaymentLinkProvider message.
                 * @function verify
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkProvider
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                PaymentLinkProvider.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.paramsJson != null && Object.hasOwnProperty.call(message, "paramsJson")) {
                        properties._paramsJson = 1;
                        if (!$util.isString(message.paramsJson))
                            return "paramsJson: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a PaymentLinkProvider message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkProvider
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.PaymentLinkMetadata.PaymentLinkProvider} PaymentLinkProvider
                 */
                PaymentLinkProvider.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.PaymentLinkMetadata.PaymentLinkProvider: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.paramsJson != null)
                        message.paramsJson = String(object.paramsJson);
                    return message;
                };

                /**
                 * Creates a plain object from a PaymentLinkProvider message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkProvider
                 * @static
                 * @param {proto.Message.PaymentLinkMetadata.PaymentLinkProvider} message PaymentLinkProvider
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                PaymentLinkProvider.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.paramsJson != null && Object.hasOwnProperty.call(message, "paramsJson"))
                        object.paramsJson = message.paramsJson;
                    return object;
                };

                /**
                 * Converts this PaymentLinkProvider to JSON.
                 * @function toJSON
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkProvider
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                PaymentLinkProvider.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for PaymentLinkProvider
                 * @function getTypeUrl
                 * @memberof proto.Message.PaymentLinkMetadata.PaymentLinkProvider
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                PaymentLinkProvider.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.PaymentLinkMetadata.PaymentLinkProvider";
                };

                return PaymentLinkProvider;
            })();

            return PaymentLinkMetadata;
        })();

        Message.PaymentReminderMessage = (function() {

            /**
             * Properties of a PaymentReminderMessage.
             * @memberof proto.Message
             * @interface IPaymentReminderMessage
             * @property {string|null} [reminderId] PaymentReminderMessage reminderId
             * @property {string|null} [instanceId] PaymentReminderMessage instanceId
             * @property {string|null} [description] PaymentReminderMessage description
             * @property {proto.Message.PaymentReminderMessage.ReminderFrequency|null} [frequency] PaymentReminderMessage frequency
             * @property {proto.Message.PaymentReminderMessage.ReminderStatus|null} [status] PaymentReminderMessage status
             * @property {string|null} [payeeVpa] PaymentReminderMessage payeeVpa
             * @property {string|null} [payeeJid] PaymentReminderMessage payeeJid
             * @property {string|null} [payerJid] PaymentReminderMessage payerJid
             * @property {proto.IMoney|null} [amount] PaymentReminderMessage amount
             */

            /**
             * Constructs a new PaymentReminderMessage.
             * @memberof proto.Message
             * @classdesc Represents a PaymentReminderMessage.
             * @implements IPaymentReminderMessage
             * @constructor
             * @param {proto.Message.IPaymentReminderMessage=} [properties] Properties to set
             */
            function PaymentReminderMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PaymentReminderMessage reminderId.
             * @member {string|null|undefined} reminderId
             * @memberof proto.Message.PaymentReminderMessage
             * @instance
             */
            PaymentReminderMessage.prototype.reminderId = null;

            /**
             * PaymentReminderMessage instanceId.
             * @member {string|null|undefined} instanceId
             * @memberof proto.Message.PaymentReminderMessage
             * @instance
             */
            PaymentReminderMessage.prototype.instanceId = null;

            /**
             * PaymentReminderMessage description.
             * @member {string|null|undefined} description
             * @memberof proto.Message.PaymentReminderMessage
             * @instance
             */
            PaymentReminderMessage.prototype.description = null;

            /**
             * PaymentReminderMessage frequency.
             * @member {proto.Message.PaymentReminderMessage.ReminderFrequency|null|undefined} frequency
             * @memberof proto.Message.PaymentReminderMessage
             * @instance
             */
            PaymentReminderMessage.prototype.frequency = null;

            /**
             * PaymentReminderMessage status.
             * @member {proto.Message.PaymentReminderMessage.ReminderStatus|null|undefined} status
             * @memberof proto.Message.PaymentReminderMessage
             * @instance
             */
            PaymentReminderMessage.prototype.status = null;

            /**
             * PaymentReminderMessage payeeVpa.
             * @member {string|null|undefined} payeeVpa
             * @memberof proto.Message.PaymentReminderMessage
             * @instance
             */
            PaymentReminderMessage.prototype.payeeVpa = null;

            /**
             * PaymentReminderMessage payeeJid.
             * @member {string|null|undefined} payeeJid
             * @memberof proto.Message.PaymentReminderMessage
             * @instance
             */
            PaymentReminderMessage.prototype.payeeJid = null;

            /**
             * PaymentReminderMessage payerJid.
             * @member {string|null|undefined} payerJid
             * @memberof proto.Message.PaymentReminderMessage
             * @instance
             */
            PaymentReminderMessage.prototype.payerJid = null;

            /**
             * PaymentReminderMessage amount.
             * @member {proto.IMoney|null|undefined} amount
             * @memberof proto.Message.PaymentReminderMessage
             * @instance
             */
            PaymentReminderMessage.prototype.amount = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentReminderMessage.prototype, "_reminderId", {
                get: $util.oneOfGetter($oneOfFields = ["reminderId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentReminderMessage.prototype, "_instanceId", {
                get: $util.oneOfGetter($oneOfFields = ["instanceId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentReminderMessage.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentReminderMessage.prototype, "_frequency", {
                get: $util.oneOfGetter($oneOfFields = ["frequency"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentReminderMessage.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentReminderMessage.prototype, "_payeeVpa", {
                get: $util.oneOfGetter($oneOfFields = ["payeeVpa"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentReminderMessage.prototype, "_payeeJid", {
                get: $util.oneOfGetter($oneOfFields = ["payeeJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentReminderMessage.prototype, "_payerJid", {
                get: $util.oneOfGetter($oneOfFields = ["payerJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentReminderMessage.prototype, "_amount", {
                get: $util.oneOfGetter($oneOfFields = ["amount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PaymentReminderMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.PaymentReminderMessage
             * @static
             * @param {proto.Message.IPaymentReminderMessage=} [properties] Properties to set
             * @returns {proto.Message.PaymentReminderMessage} PaymentReminderMessage instance
             */
            PaymentReminderMessage.create = function create(properties) {
                return new PaymentReminderMessage(properties);
            };

            /**
             * Encodes the specified PaymentReminderMessage message. Does not implicitly {@link proto.Message.PaymentReminderMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.PaymentReminderMessage
             * @static
             * @param {proto.Message.IPaymentReminderMessage} message PaymentReminderMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PaymentReminderMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.reminderId != null && Object.hasOwnProperty.call(message, "reminderId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.reminderId);
                if (message.instanceId != null && Object.hasOwnProperty.call(message, "instanceId"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.instanceId);
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.description);
                if (message.frequency != null && Object.hasOwnProperty.call(message, "frequency"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int32(message.frequency);
                if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int32(message.status);
                if (message.payeeVpa != null && Object.hasOwnProperty.call(message, "payeeVpa"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.payeeVpa);
                if (message.payeeJid != null && Object.hasOwnProperty.call(message, "payeeJid"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.payeeJid);
                if (message.payerJid != null && Object.hasOwnProperty.call(message, "payerJid"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.payerJid);
                if (message.amount != null && Object.hasOwnProperty.call(message, "amount"))
                    $root.proto.Money.encode(message.amount, writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PaymentReminderMessage message, length delimited. Does not implicitly {@link proto.Message.PaymentReminderMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.PaymentReminderMessage
             * @static
             * @param {proto.Message.IPaymentReminderMessage} message PaymentReminderMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PaymentReminderMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PaymentReminderMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.PaymentReminderMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.PaymentReminderMessage} PaymentReminderMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PaymentReminderMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.reminderId = reader.stringVerify();
                            message._reminderId = "reminderId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.instanceId = reader.stringVerify();
                            message._instanceId = "instanceId";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.description = reader.stringVerify();
                            message._description = "description";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.frequency = reader.int32();
                            message._frequency = "frequency";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.status = reader.int32();
                            message._status = "status";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.payeeVpa = reader.stringVerify();
                            message._payeeVpa = "payeeVpa";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.payeeJid = reader.stringVerify();
                            message._payeeJid = "payeeJid";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.payerJid = reader.stringVerify();
                            message._payerJid = "payerJid";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            message.amount = $root.proto.Money.decode(reader, reader.uint32(), undefined, q + 1, message.amount);
                            message._amount = "amount";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PaymentReminderMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.PaymentReminderMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.PaymentReminderMessage} PaymentReminderMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PaymentReminderMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PaymentReminderMessage message.
             * @function verify
             * @memberof proto.Message.PaymentReminderMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PaymentReminderMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.reminderId != null && Object.hasOwnProperty.call(message, "reminderId")) {
                    properties._reminderId = 1;
                    if (!$util.isString(message.reminderId))
                        return "reminderId: string expected";
                }
                if (message.instanceId != null && Object.hasOwnProperty.call(message, "instanceId")) {
                    properties._instanceId = 1;
                    if (!$util.isString(message.instanceId))
                        return "instanceId: string expected";
                }
                if (message.description != null && Object.hasOwnProperty.call(message, "description")) {
                    properties._description = 1;
                    if (!$util.isString(message.description))
                        return "description: string expected";
                }
                if (message.frequency != null && Object.hasOwnProperty.call(message, "frequency")) {
                    properties._frequency = 1;
                    if (typeof message.frequency !== "number" || (message.frequency | 0) !== message.frequency)
                        return "frequency: enum value expected";
                }
                if (message.status != null && Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (typeof message.status !== "number" || (message.status | 0) !== message.status)
                        return "status: enum value expected";
                }
                if (message.payeeVpa != null && Object.hasOwnProperty.call(message, "payeeVpa")) {
                    properties._payeeVpa = 1;
                    if (!$util.isString(message.payeeVpa))
                        return "payeeVpa: string expected";
                }
                if (message.payeeJid != null && Object.hasOwnProperty.call(message, "payeeJid")) {
                    properties._payeeJid = 1;
                    if (!$util.isString(message.payeeJid))
                        return "payeeJid: string expected";
                }
                if (message.payerJid != null && Object.hasOwnProperty.call(message, "payerJid")) {
                    properties._payerJid = 1;
                    if (!$util.isString(message.payerJid))
                        return "payerJid: string expected";
                }
                if (message.amount != null && Object.hasOwnProperty.call(message, "amount")) {
                    properties._amount = 1;
                    {
                        var error = $root.proto.Money.verify(message.amount, q + 1);
                        if (error)
                            return "amount." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a PaymentReminderMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.PaymentReminderMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.PaymentReminderMessage} PaymentReminderMessage
             */
            PaymentReminderMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.PaymentReminderMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.reminderId != null)
                    message.reminderId = String(object.reminderId);
                if (object.instanceId != null)
                    message.instanceId = String(object.instanceId);
                if (object.description != null)
                    message.description = String(object.description);
                switch (object.frequency) {
                case "REMINDER_FREQUENCY_UNKNOWN":
                case 0:
                    message.frequency = 0;
                    break;
                case "WEEKLY":
                case 1:
                    message.frequency = 1;
                    break;
                case "BI_WEEKLY":
                case 2:
                    message.frequency = 2;
                    break;
                case "MONTHLY":
                case 3:
                    message.frequency = 3;
                    break;
                case "QUARTERLY":
                case 4:
                    message.frequency = 4;
                    break;
                default:
                    if (typeof object.frequency === "number" && (object.frequency | 0) === object.frequency)
                        message.frequency = object.frequency;
                }
                switch (object.status) {
                case "REMINDER_STATUS_UNKNOWN":
                case 0:
                    message.status = 0;
                    break;
                case "ACTIVE":
                case 1:
                    message.status = 1;
                    break;
                case "CANCELLED_BY_CREATOR":
                case 2:
                    message.status = 2;
                    break;
                case "STOPPED_BY_RECEIVER":
                case 3:
                    message.status = 3;
                    break;
                case "EXPIRED":
                case 4:
                    message.status = 4;
                    break;
                case "PAID":
                case 5:
                    message.status = 5;
                    break;
                default:
                    if (typeof object.status === "number" && (object.status | 0) === object.status)
                        message.status = object.status;
                }
                if (object.payeeVpa != null)
                    message.payeeVpa = String(object.payeeVpa);
                if (object.payeeJid != null)
                    message.payeeJid = String(object.payeeJid);
                if (object.payerJid != null)
                    message.payerJid = String(object.payerJid);
                if (object.amount != null) {
                    if (!$util.isObject(object.amount))
                        throw TypeError(".proto.Message.PaymentReminderMessage.amount: object expected");
                    message.amount = $root.proto.Money.fromObject(object.amount, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a PaymentReminderMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.PaymentReminderMessage
             * @static
             * @param {proto.Message.PaymentReminderMessage} message PaymentReminderMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PaymentReminderMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.reminderId != null && Object.hasOwnProperty.call(message, "reminderId"))
                    object.reminderId = message.reminderId;
                if (message.instanceId != null && Object.hasOwnProperty.call(message, "instanceId"))
                    object.instanceId = message.instanceId;
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    object.description = message.description;
                if (message.frequency != null && Object.hasOwnProperty.call(message, "frequency"))
                    object.frequency = options.enums === String ? $root.proto.Message.PaymentReminderMessage.ReminderFrequency[message.frequency] === undefined ? message.frequency : $root.proto.Message.PaymentReminderMessage.ReminderFrequency[message.frequency] : message.frequency;
                if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                    object.status = options.enums === String ? $root.proto.Message.PaymentReminderMessage.ReminderStatus[message.status] === undefined ? message.status : $root.proto.Message.PaymentReminderMessage.ReminderStatus[message.status] : message.status;
                if (message.payeeVpa != null && Object.hasOwnProperty.call(message, "payeeVpa"))
                    object.payeeVpa = message.payeeVpa;
                if (message.payeeJid != null && Object.hasOwnProperty.call(message, "payeeJid"))
                    object.payeeJid = message.payeeJid;
                if (message.payerJid != null && Object.hasOwnProperty.call(message, "payerJid"))
                    object.payerJid = message.payerJid;
                if (message.amount != null && Object.hasOwnProperty.call(message, "amount"))
                    object.amount = $root.proto.Money.toObject(message.amount, options, q + 1);
                return object;
            };

            /**
             * Converts this PaymentReminderMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.PaymentReminderMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PaymentReminderMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PaymentReminderMessage
             * @function getTypeUrl
             * @memberof proto.Message.PaymentReminderMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PaymentReminderMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.PaymentReminderMessage";
            };

            /**
             * ReminderFrequency enum.
             * @name proto.Message.PaymentReminderMessage.ReminderFrequency
             * @enum {number}
             * @property {number} REMINDER_FREQUENCY_UNKNOWN=0 REMINDER_FREQUENCY_UNKNOWN value
             * @property {number} WEEKLY=1 WEEKLY value
             * @property {number} BI_WEEKLY=2 BI_WEEKLY value
             * @property {number} MONTHLY=3 MONTHLY value
             * @property {number} QUARTERLY=4 QUARTERLY value
             */
            PaymentReminderMessage.ReminderFrequency = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "REMINDER_FREQUENCY_UNKNOWN"] = 0;
                values[valuesById[1] = "WEEKLY"] = 1;
                values[valuesById[2] = "BI_WEEKLY"] = 2;
                values[valuesById[3] = "MONTHLY"] = 3;
                values[valuesById[4] = "QUARTERLY"] = 4;
                return values;
            })();

            /**
             * ReminderStatus enum.
             * @name proto.Message.PaymentReminderMessage.ReminderStatus
             * @enum {number}
             * @property {number} REMINDER_STATUS_UNKNOWN=0 REMINDER_STATUS_UNKNOWN value
             * @property {number} ACTIVE=1 ACTIVE value
             * @property {number} CANCELLED_BY_CREATOR=2 CANCELLED_BY_CREATOR value
             * @property {number} STOPPED_BY_RECEIVER=3 STOPPED_BY_RECEIVER value
             * @property {number} EXPIRED=4 EXPIRED value
             * @property {number} PAID=5 PAID value
             */
            PaymentReminderMessage.ReminderStatus = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "REMINDER_STATUS_UNKNOWN"] = 0;
                values[valuesById[1] = "ACTIVE"] = 1;
                values[valuesById[2] = "CANCELLED_BY_CREATOR"] = 2;
                values[valuesById[3] = "STOPPED_BY_RECEIVER"] = 3;
                values[valuesById[4] = "EXPIRED"] = 4;
                values[valuesById[5] = "PAID"] = 5;
                return values;
            })();

            return PaymentReminderMessage;
        })();

        Message.PeerDataOperationRequestMessage = (function() {

            /**
             * Properties of a PeerDataOperationRequestMessage.
             * @memberof proto.Message
             * @interface IPeerDataOperationRequestMessage
             * @property {proto.Message.PeerDataOperationRequestType|null} [peerDataOperationRequestType] PeerDataOperationRequestMessage peerDataOperationRequestType
             * @property {Array.<proto.Message.PeerDataOperationRequestMessage.IRequestStickerReupload>|null} [requestStickerReupload] PeerDataOperationRequestMessage requestStickerReupload
             * @property {Array.<proto.Message.PeerDataOperationRequestMessage.IRequestUrlPreview>|null} [requestUrlPreview] PeerDataOperationRequestMessage requestUrlPreview
             * @property {proto.Message.PeerDataOperationRequestMessage.IHistorySyncOnDemandRequest|null} [historySyncOnDemandRequest] PeerDataOperationRequestMessage historySyncOnDemandRequest
             * @property {Array.<proto.Message.PeerDataOperationRequestMessage.IPlaceholderMessageResendRequest>|null} [placeholderMessageResendRequest] PeerDataOperationRequestMessage placeholderMessageResendRequest
             * @property {proto.Message.PeerDataOperationRequestMessage.IFullHistorySyncOnDemandRequest|null} [fullHistorySyncOnDemandRequest] PeerDataOperationRequestMessage fullHistorySyncOnDemandRequest
             * @property {proto.Message.PeerDataOperationRequestMessage.ISyncDCollectionFatalRecoveryRequest|null} [syncdCollectionFatalRecoveryRequest] PeerDataOperationRequestMessage syncdCollectionFatalRecoveryRequest
             * @property {proto.Message.PeerDataOperationRequestMessage.IHistorySyncChunkRetryRequest|null} [historySyncChunkRetryRequest] PeerDataOperationRequestMessage historySyncChunkRetryRequest
             * @property {proto.Message.PeerDataOperationRequestMessage.IGalaxyFlowAction|null} [galaxyFlowAction] PeerDataOperationRequestMessage galaxyFlowAction
             * @property {proto.Message.PeerDataOperationRequestMessage.ICompanionCanonicalUserNonceFetchRequest|null} [companionCanonicalUserNonceFetchRequest] PeerDataOperationRequestMessage companionCanonicalUserNonceFetchRequest
             * @property {proto.Message.PeerDataOperationRequestMessage.IBizBroadcastInsightsContactListRequest|null} [bizBroadcastInsightsContactListRequest] PeerDataOperationRequestMessage bizBroadcastInsightsContactListRequest
             * @property {proto.Message.PeerDataOperationRequestMessage.IBizBroadcastInsightsRefreshRequest|null} [bizBroadcastInsightsRefreshRequest] PeerDataOperationRequestMessage bizBroadcastInsightsRefreshRequest
             */

            /**
             * Constructs a new PeerDataOperationRequestMessage.
             * @memberof proto.Message
             * @classdesc Represents a PeerDataOperationRequestMessage.
             * @implements IPeerDataOperationRequestMessage
             * @constructor
             * @param {proto.Message.IPeerDataOperationRequestMessage=} [properties] Properties to set
             */
            function PeerDataOperationRequestMessage(properties) {
                this.requestStickerReupload = [];
                this.requestUrlPreview = [];
                this.placeholderMessageResendRequest = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PeerDataOperationRequestMessage peerDataOperationRequestType.
             * @member {proto.Message.PeerDataOperationRequestType|null|undefined} peerDataOperationRequestType
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @instance
             */
            PeerDataOperationRequestMessage.prototype.peerDataOperationRequestType = null;

            /**
             * PeerDataOperationRequestMessage requestStickerReupload.
             * @member {Array.<proto.Message.PeerDataOperationRequestMessage.IRequestStickerReupload>} requestStickerReupload
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @instance
             */
            PeerDataOperationRequestMessage.prototype.requestStickerReupload = $util.emptyArray;

            /**
             * PeerDataOperationRequestMessage requestUrlPreview.
             * @member {Array.<proto.Message.PeerDataOperationRequestMessage.IRequestUrlPreview>} requestUrlPreview
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @instance
             */
            PeerDataOperationRequestMessage.prototype.requestUrlPreview = $util.emptyArray;

            /**
             * PeerDataOperationRequestMessage historySyncOnDemandRequest.
             * @member {proto.Message.PeerDataOperationRequestMessage.IHistorySyncOnDemandRequest|null|undefined} historySyncOnDemandRequest
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @instance
             */
            PeerDataOperationRequestMessage.prototype.historySyncOnDemandRequest = null;

            /**
             * PeerDataOperationRequestMessage placeholderMessageResendRequest.
             * @member {Array.<proto.Message.PeerDataOperationRequestMessage.IPlaceholderMessageResendRequest>} placeholderMessageResendRequest
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @instance
             */
            PeerDataOperationRequestMessage.prototype.placeholderMessageResendRequest = $util.emptyArray;

            /**
             * PeerDataOperationRequestMessage fullHistorySyncOnDemandRequest.
             * @member {proto.Message.PeerDataOperationRequestMessage.IFullHistorySyncOnDemandRequest|null|undefined} fullHistorySyncOnDemandRequest
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @instance
             */
            PeerDataOperationRequestMessage.prototype.fullHistorySyncOnDemandRequest = null;

            /**
             * PeerDataOperationRequestMessage syncdCollectionFatalRecoveryRequest.
             * @member {proto.Message.PeerDataOperationRequestMessage.ISyncDCollectionFatalRecoveryRequest|null|undefined} syncdCollectionFatalRecoveryRequest
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @instance
             */
            PeerDataOperationRequestMessage.prototype.syncdCollectionFatalRecoveryRequest = null;

            /**
             * PeerDataOperationRequestMessage historySyncChunkRetryRequest.
             * @member {proto.Message.PeerDataOperationRequestMessage.IHistorySyncChunkRetryRequest|null|undefined} historySyncChunkRetryRequest
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @instance
             */
            PeerDataOperationRequestMessage.prototype.historySyncChunkRetryRequest = null;

            /**
             * PeerDataOperationRequestMessage galaxyFlowAction.
             * @member {proto.Message.PeerDataOperationRequestMessage.IGalaxyFlowAction|null|undefined} galaxyFlowAction
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @instance
             */
            PeerDataOperationRequestMessage.prototype.galaxyFlowAction = null;

            /**
             * PeerDataOperationRequestMessage companionCanonicalUserNonceFetchRequest.
             * @member {proto.Message.PeerDataOperationRequestMessage.ICompanionCanonicalUserNonceFetchRequest|null|undefined} companionCanonicalUserNonceFetchRequest
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @instance
             */
            PeerDataOperationRequestMessage.prototype.companionCanonicalUserNonceFetchRequest = null;

            /**
             * PeerDataOperationRequestMessage bizBroadcastInsightsContactListRequest.
             * @member {proto.Message.PeerDataOperationRequestMessage.IBizBroadcastInsightsContactListRequest|null|undefined} bizBroadcastInsightsContactListRequest
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @instance
             */
            PeerDataOperationRequestMessage.prototype.bizBroadcastInsightsContactListRequest = null;

            /**
             * PeerDataOperationRequestMessage bizBroadcastInsightsRefreshRequest.
             * @member {proto.Message.PeerDataOperationRequestMessage.IBizBroadcastInsightsRefreshRequest|null|undefined} bizBroadcastInsightsRefreshRequest
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @instance
             */
            PeerDataOperationRequestMessage.prototype.bizBroadcastInsightsRefreshRequest = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_peerDataOperationRequestType", {
                get: $util.oneOfGetter($oneOfFields = ["peerDataOperationRequestType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_historySyncOnDemandRequest", {
                get: $util.oneOfGetter($oneOfFields = ["historySyncOnDemandRequest"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_fullHistorySyncOnDemandRequest", {
                get: $util.oneOfGetter($oneOfFields = ["fullHistorySyncOnDemandRequest"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_syncdCollectionFatalRecoveryRequest", {
                get: $util.oneOfGetter($oneOfFields = ["syncdCollectionFatalRecoveryRequest"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_historySyncChunkRetryRequest", {
                get: $util.oneOfGetter($oneOfFields = ["historySyncChunkRetryRequest"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_galaxyFlowAction", {
                get: $util.oneOfGetter($oneOfFields = ["galaxyFlowAction"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_companionCanonicalUserNonceFetchRequest", {
                get: $util.oneOfGetter($oneOfFields = ["companionCanonicalUserNonceFetchRequest"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_bizBroadcastInsightsContactListRequest", {
                get: $util.oneOfGetter($oneOfFields = ["bizBroadcastInsightsContactListRequest"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_bizBroadcastInsightsRefreshRequest", {
                get: $util.oneOfGetter($oneOfFields = ["bizBroadcastInsightsRefreshRequest"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PeerDataOperationRequestMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @static
             * @param {proto.Message.IPeerDataOperationRequestMessage=} [properties] Properties to set
             * @returns {proto.Message.PeerDataOperationRequestMessage} PeerDataOperationRequestMessage instance
             */
            PeerDataOperationRequestMessage.create = function create(properties) {
                return new PeerDataOperationRequestMessage(properties);
            };

            /**
             * Encodes the specified PeerDataOperationRequestMessage message. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @static
             * @param {proto.Message.IPeerDataOperationRequestMessage} message PeerDataOperationRequestMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PeerDataOperationRequestMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.peerDataOperationRequestType != null && Object.hasOwnProperty.call(message, "peerDataOperationRequestType"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.peerDataOperationRequestType);
                if (message.requestStickerReupload != null && message.requestStickerReupload.length)
                    for (var i = 0; i < message.requestStickerReupload.length; ++i)
                        $root.proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload.encode(message.requestStickerReupload[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.requestUrlPreview != null && message.requestUrlPreview.length)
                    for (var i = 0; i < message.requestUrlPreview.length; ++i)
                        $root.proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview.encode(message.requestUrlPreview[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.historySyncOnDemandRequest != null && Object.hasOwnProperty.call(message, "historySyncOnDemandRequest"))
                    $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest.encode(message.historySyncOnDemandRequest, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.placeholderMessageResendRequest != null && message.placeholderMessageResendRequest.length)
                    for (var i = 0; i < message.placeholderMessageResendRequest.length; ++i)
                        $root.proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest.encode(message.placeholderMessageResendRequest[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                if (message.fullHistorySyncOnDemandRequest != null && Object.hasOwnProperty.call(message, "fullHistorySyncOnDemandRequest"))
                    $root.proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.encode(message.fullHistorySyncOnDemandRequest, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
                if (message.syncdCollectionFatalRecoveryRequest != null && Object.hasOwnProperty.call(message, "syncdCollectionFatalRecoveryRequest"))
                    $root.proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest.encode(message.syncdCollectionFatalRecoveryRequest, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
                if (message.historySyncChunkRetryRequest != null && Object.hasOwnProperty.call(message, "historySyncChunkRetryRequest"))
                    $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest.encode(message.historySyncChunkRetryRequest, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
                if (message.galaxyFlowAction != null && Object.hasOwnProperty.call(message, "galaxyFlowAction"))
                    $root.proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction.encode(message.galaxyFlowAction, writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
                if (message.companionCanonicalUserNonceFetchRequest != null && Object.hasOwnProperty.call(message, "companionCanonicalUserNonceFetchRequest"))
                    $root.proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest.encode(message.companionCanonicalUserNonceFetchRequest, writer.uint32(/* id 10, wireType 2 =*/82).fork(), q + 1).ldelim();
                if (message.bizBroadcastInsightsContactListRequest != null && Object.hasOwnProperty.call(message, "bizBroadcastInsightsContactListRequest"))
                    $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest.encode(message.bizBroadcastInsightsContactListRequest, writer.uint32(/* id 11, wireType 2 =*/90).fork(), q + 1).ldelim();
                if (message.bizBroadcastInsightsRefreshRequest != null && Object.hasOwnProperty.call(message, "bizBroadcastInsightsRefreshRequest"))
                    $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest.encode(message.bizBroadcastInsightsRefreshRequest, writer.uint32(/* id 12, wireType 2 =*/98).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PeerDataOperationRequestMessage message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @static
             * @param {proto.Message.IPeerDataOperationRequestMessage} message PeerDataOperationRequestMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PeerDataOperationRequestMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PeerDataOperationRequestMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.PeerDataOperationRequestMessage} PeerDataOperationRequestMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PeerDataOperationRequestMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.peerDataOperationRequestType = reader.int32();
                            message._peerDataOperationRequestType = "peerDataOperationRequestType";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(message.requestStickerReupload && message.requestStickerReupload.length))
                                message.requestStickerReupload = [];
                            message.requestStickerReupload.push($root.proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            if (!(message.requestUrlPreview && message.requestUrlPreview.length))
                                message.requestUrlPreview = [];
                            message.requestUrlPreview.push($root.proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.historySyncOnDemandRequest = $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest.decode(reader, reader.uint32(), undefined, q + 1, message.historySyncOnDemandRequest);
                            message._historySyncOnDemandRequest = "historySyncOnDemandRequest";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            if (!(message.placeholderMessageResendRequest && message.placeholderMessageResendRequest.length))
                                message.placeholderMessageResendRequest = [];
                            message.placeholderMessageResendRequest.push($root.proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.fullHistorySyncOnDemandRequest = $root.proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.decode(reader, reader.uint32(), undefined, q + 1, message.fullHistorySyncOnDemandRequest);
                            message._fullHistorySyncOnDemandRequest = "fullHistorySyncOnDemandRequest";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.syncdCollectionFatalRecoveryRequest = $root.proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest.decode(reader, reader.uint32(), undefined, q + 1, message.syncdCollectionFatalRecoveryRequest);
                            message._syncdCollectionFatalRecoveryRequest = "syncdCollectionFatalRecoveryRequest";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.historySyncChunkRetryRequest = $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest.decode(reader, reader.uint32(), undefined, q + 1, message.historySyncChunkRetryRequest);
                            message._historySyncChunkRetryRequest = "historySyncChunkRetryRequest";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            message.galaxyFlowAction = $root.proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction.decode(reader, reader.uint32(), undefined, q + 1, message.galaxyFlowAction);
                            message._galaxyFlowAction = "galaxyFlowAction";
                            continue;
                        }
                    case 10: {
                            if (u !== 2)
                                break;
                            message.companionCanonicalUserNonceFetchRequest = $root.proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest.decode(reader, reader.uint32(), undefined, q + 1, message.companionCanonicalUserNonceFetchRequest);
                            message._companionCanonicalUserNonceFetchRequest = "companionCanonicalUserNonceFetchRequest";
                            continue;
                        }
                    case 11: {
                            if (u !== 2)
                                break;
                            message.bizBroadcastInsightsContactListRequest = $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest.decode(reader, reader.uint32(), undefined, q + 1, message.bizBroadcastInsightsContactListRequest);
                            message._bizBroadcastInsightsContactListRequest = "bizBroadcastInsightsContactListRequest";
                            continue;
                        }
                    case 12: {
                            if (u !== 2)
                                break;
                            message.bizBroadcastInsightsRefreshRequest = $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest.decode(reader, reader.uint32(), undefined, q + 1, message.bizBroadcastInsightsRefreshRequest);
                            message._bizBroadcastInsightsRefreshRequest = "bizBroadcastInsightsRefreshRequest";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PeerDataOperationRequestMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.PeerDataOperationRequestMessage} PeerDataOperationRequestMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PeerDataOperationRequestMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PeerDataOperationRequestMessage message.
             * @function verify
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PeerDataOperationRequestMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.peerDataOperationRequestType != null && Object.hasOwnProperty.call(message, "peerDataOperationRequestType")) {
                    properties._peerDataOperationRequestType = 1;
                    if (typeof message.peerDataOperationRequestType !== "number" || (message.peerDataOperationRequestType | 0) !== message.peerDataOperationRequestType)
                        return "peerDataOperationRequestType: enum value expected";
                }
                if (message.requestStickerReupload != null && Object.hasOwnProperty.call(message, "requestStickerReupload")) {
                    if (!Array.isArray(message.requestStickerReupload))
                        return "requestStickerReupload: array expected";
                    for (var i = 0; i < message.requestStickerReupload.length; ++i) {
                        var error = $root.proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload.verify(message.requestStickerReupload[i], q + 1);
                        if (error)
                            return "requestStickerReupload." + error;
                    }
                }
                if (message.requestUrlPreview != null && Object.hasOwnProperty.call(message, "requestUrlPreview")) {
                    if (!Array.isArray(message.requestUrlPreview))
                        return "requestUrlPreview: array expected";
                    for (var i = 0; i < message.requestUrlPreview.length; ++i) {
                        var error = $root.proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview.verify(message.requestUrlPreview[i], q + 1);
                        if (error)
                            return "requestUrlPreview." + error;
                    }
                }
                if (message.historySyncOnDemandRequest != null && Object.hasOwnProperty.call(message, "historySyncOnDemandRequest")) {
                    properties._historySyncOnDemandRequest = 1;
                    {
                        var error = $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest.verify(message.historySyncOnDemandRequest, q + 1);
                        if (error)
                            return "historySyncOnDemandRequest." + error;
                    }
                }
                if (message.placeholderMessageResendRequest != null && Object.hasOwnProperty.call(message, "placeholderMessageResendRequest")) {
                    if (!Array.isArray(message.placeholderMessageResendRequest))
                        return "placeholderMessageResendRequest: array expected";
                    for (var i = 0; i < message.placeholderMessageResendRequest.length; ++i) {
                        var error = $root.proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest.verify(message.placeholderMessageResendRequest[i], q + 1);
                        if (error)
                            return "placeholderMessageResendRequest." + error;
                    }
                }
                if (message.fullHistorySyncOnDemandRequest != null && Object.hasOwnProperty.call(message, "fullHistorySyncOnDemandRequest")) {
                    properties._fullHistorySyncOnDemandRequest = 1;
                    {
                        var error = $root.proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.verify(message.fullHistorySyncOnDemandRequest, q + 1);
                        if (error)
                            return "fullHistorySyncOnDemandRequest." + error;
                    }
                }
                if (message.syncdCollectionFatalRecoveryRequest != null && Object.hasOwnProperty.call(message, "syncdCollectionFatalRecoveryRequest")) {
                    properties._syncdCollectionFatalRecoveryRequest = 1;
                    {
                        var error = $root.proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest.verify(message.syncdCollectionFatalRecoveryRequest, q + 1);
                        if (error)
                            return "syncdCollectionFatalRecoveryRequest." + error;
                    }
                }
                if (message.historySyncChunkRetryRequest != null && Object.hasOwnProperty.call(message, "historySyncChunkRetryRequest")) {
                    properties._historySyncChunkRetryRequest = 1;
                    {
                        var error = $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest.verify(message.historySyncChunkRetryRequest, q + 1);
                        if (error)
                            return "historySyncChunkRetryRequest." + error;
                    }
                }
                if (message.galaxyFlowAction != null && Object.hasOwnProperty.call(message, "galaxyFlowAction")) {
                    properties._galaxyFlowAction = 1;
                    {
                        var error = $root.proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction.verify(message.galaxyFlowAction, q + 1);
                        if (error)
                            return "galaxyFlowAction." + error;
                    }
                }
                if (message.companionCanonicalUserNonceFetchRequest != null && Object.hasOwnProperty.call(message, "companionCanonicalUserNonceFetchRequest")) {
                    properties._companionCanonicalUserNonceFetchRequest = 1;
                    {
                        var error = $root.proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest.verify(message.companionCanonicalUserNonceFetchRequest, q + 1);
                        if (error)
                            return "companionCanonicalUserNonceFetchRequest." + error;
                    }
                }
                if (message.bizBroadcastInsightsContactListRequest != null && Object.hasOwnProperty.call(message, "bizBroadcastInsightsContactListRequest")) {
                    properties._bizBroadcastInsightsContactListRequest = 1;
                    {
                        var error = $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest.verify(message.bizBroadcastInsightsContactListRequest, q + 1);
                        if (error)
                            return "bizBroadcastInsightsContactListRequest." + error;
                    }
                }
                if (message.bizBroadcastInsightsRefreshRequest != null && Object.hasOwnProperty.call(message, "bizBroadcastInsightsRefreshRequest")) {
                    properties._bizBroadcastInsightsRefreshRequest = 1;
                    {
                        var error = $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest.verify(message.bizBroadcastInsightsRefreshRequest, q + 1);
                        if (error)
                            return "bizBroadcastInsightsRefreshRequest." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a PeerDataOperationRequestMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.PeerDataOperationRequestMessage} PeerDataOperationRequestMessage
             */
            PeerDataOperationRequestMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.PeerDataOperationRequestMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.peerDataOperationRequestType) {
                case "UPLOAD_STICKER":
                case 0:
                    message.peerDataOperationRequestType = 0;
                    break;
                case "SEND_RECENT_STICKER_BOOTSTRAP":
                case 1:
                    message.peerDataOperationRequestType = 1;
                    break;
                case "GENERATE_LINK_PREVIEW":
                case 2:
                    message.peerDataOperationRequestType = 2;
                    break;
                case "HISTORY_SYNC_ON_DEMAND":
                case 3:
                    message.peerDataOperationRequestType = 3;
                    break;
                case "PLACEHOLDER_MESSAGE_RESEND":
                case 4:
                    message.peerDataOperationRequestType = 4;
                    break;
                case "WAFFLE_LINKING_NONCE_FETCH":
                case 5:
                    message.peerDataOperationRequestType = 5;
                    break;
                case "FULL_HISTORY_SYNC_ON_DEMAND":
                case 6:
                    message.peerDataOperationRequestType = 6;
                    break;
                case "COMPANION_META_NONCE_FETCH":
                case 7:
                    message.peerDataOperationRequestType = 7;
                    break;
                case "COMPANION_SYNCD_SNAPSHOT_FATAL_RECOVERY":
                case 8:
                    message.peerDataOperationRequestType = 8;
                    break;
                case "COMPANION_CANONICAL_USER_NONCE_FETCH":
                case 9:
                    message.peerDataOperationRequestType = 9;
                    break;
                case "HISTORY_SYNC_CHUNK_RETRY":
                case 10:
                    message.peerDataOperationRequestType = 10;
                    break;
                case "GALAXY_FLOW_ACTION":
                case 11:
                    message.peerDataOperationRequestType = 11;
                    break;
                case "BUSINESS_BROADCAST_INSIGHTS_DELIVERED_TO":
                case 12:
                    message.peerDataOperationRequestType = 12;
                    break;
                case "BUSINESS_BROADCAST_INSIGHTS_REFRESH":
                case 13:
                    message.peerDataOperationRequestType = 13;
                    break;
                default:
                    if (typeof object.peerDataOperationRequestType === "number" && (object.peerDataOperationRequestType | 0) === object.peerDataOperationRequestType)
                        message.peerDataOperationRequestType = object.peerDataOperationRequestType;
                }
                if (object.requestStickerReupload) {
                    if (!Array.isArray(object.requestStickerReupload))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.requestStickerReupload: array expected");
                    message.requestStickerReupload = Array(object.requestStickerReupload.length);
                    for (var i = 0; i < object.requestStickerReupload.length; ++i) {
                        if (!$util.isObject(object.requestStickerReupload[i]))
                            throw TypeError(".proto.Message.PeerDataOperationRequestMessage.requestStickerReupload: object expected");
                        message.requestStickerReupload[i] = $root.proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload.fromObject(object.requestStickerReupload[i], q + 1);
                    }
                }
                if (object.requestUrlPreview) {
                    if (!Array.isArray(object.requestUrlPreview))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.requestUrlPreview: array expected");
                    message.requestUrlPreview = Array(object.requestUrlPreview.length);
                    for (var i = 0; i < object.requestUrlPreview.length; ++i) {
                        if (!$util.isObject(object.requestUrlPreview[i]))
                            throw TypeError(".proto.Message.PeerDataOperationRequestMessage.requestUrlPreview: object expected");
                        message.requestUrlPreview[i] = $root.proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview.fromObject(object.requestUrlPreview[i], q + 1);
                    }
                }
                if (object.historySyncOnDemandRequest != null) {
                    if (!$util.isObject(object.historySyncOnDemandRequest))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.historySyncOnDemandRequest: object expected");
                    message.historySyncOnDemandRequest = $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest.fromObject(object.historySyncOnDemandRequest, q + 1);
                }
                if (object.placeholderMessageResendRequest) {
                    if (!Array.isArray(object.placeholderMessageResendRequest))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.placeholderMessageResendRequest: array expected");
                    message.placeholderMessageResendRequest = Array(object.placeholderMessageResendRequest.length);
                    for (var i = 0; i < object.placeholderMessageResendRequest.length; ++i) {
                        if (!$util.isObject(object.placeholderMessageResendRequest[i]))
                            throw TypeError(".proto.Message.PeerDataOperationRequestMessage.placeholderMessageResendRequest: object expected");
                        message.placeholderMessageResendRequest[i] = $root.proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest.fromObject(object.placeholderMessageResendRequest[i], q + 1);
                    }
                }
                if (object.fullHistorySyncOnDemandRequest != null) {
                    if (!$util.isObject(object.fullHistorySyncOnDemandRequest))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.fullHistorySyncOnDemandRequest: object expected");
                    message.fullHistorySyncOnDemandRequest = $root.proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.fromObject(object.fullHistorySyncOnDemandRequest, q + 1);
                }
                if (object.syncdCollectionFatalRecoveryRequest != null) {
                    if (!$util.isObject(object.syncdCollectionFatalRecoveryRequest))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.syncdCollectionFatalRecoveryRequest: object expected");
                    message.syncdCollectionFatalRecoveryRequest = $root.proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest.fromObject(object.syncdCollectionFatalRecoveryRequest, q + 1);
                }
                if (object.historySyncChunkRetryRequest != null) {
                    if (!$util.isObject(object.historySyncChunkRetryRequest))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.historySyncChunkRetryRequest: object expected");
                    message.historySyncChunkRetryRequest = $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest.fromObject(object.historySyncChunkRetryRequest, q + 1);
                }
                if (object.galaxyFlowAction != null) {
                    if (!$util.isObject(object.galaxyFlowAction))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.galaxyFlowAction: object expected");
                    message.galaxyFlowAction = $root.proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction.fromObject(object.galaxyFlowAction, q + 1);
                }
                if (object.companionCanonicalUserNonceFetchRequest != null) {
                    if (!$util.isObject(object.companionCanonicalUserNonceFetchRequest))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.companionCanonicalUserNonceFetchRequest: object expected");
                    message.companionCanonicalUserNonceFetchRequest = $root.proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest.fromObject(object.companionCanonicalUserNonceFetchRequest, q + 1);
                }
                if (object.bizBroadcastInsightsContactListRequest != null) {
                    if (!$util.isObject(object.bizBroadcastInsightsContactListRequest))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.bizBroadcastInsightsContactListRequest: object expected");
                    message.bizBroadcastInsightsContactListRequest = $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest.fromObject(object.bizBroadcastInsightsContactListRequest, q + 1);
                }
                if (object.bizBroadcastInsightsRefreshRequest != null) {
                    if (!$util.isObject(object.bizBroadcastInsightsRefreshRequest))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.bizBroadcastInsightsRefreshRequest: object expected");
                    message.bizBroadcastInsightsRefreshRequest = $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest.fromObject(object.bizBroadcastInsightsRefreshRequest, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a PeerDataOperationRequestMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @static
             * @param {proto.Message.PeerDataOperationRequestMessage} message PeerDataOperationRequestMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PeerDataOperationRequestMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.requestStickerReupload = [];
                    object.requestUrlPreview = [];
                    object.placeholderMessageResendRequest = [];
                }
                if (message.peerDataOperationRequestType != null && Object.hasOwnProperty.call(message, "peerDataOperationRequestType"))
                    object.peerDataOperationRequestType = options.enums === String ? $root.proto.Message.PeerDataOperationRequestType[message.peerDataOperationRequestType] === undefined ? message.peerDataOperationRequestType : $root.proto.Message.PeerDataOperationRequestType[message.peerDataOperationRequestType] : message.peerDataOperationRequestType;
                if (message.requestStickerReupload && message.requestStickerReupload.length) {
                    object.requestStickerReupload = Array(message.requestStickerReupload.length);
                    for (var j = 0; j < message.requestStickerReupload.length; ++j)
                        object.requestStickerReupload[j] = $root.proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload.toObject(message.requestStickerReupload[j], options, q + 1);
                }
                if (message.requestUrlPreview && message.requestUrlPreview.length) {
                    object.requestUrlPreview = Array(message.requestUrlPreview.length);
                    for (var j = 0; j < message.requestUrlPreview.length; ++j)
                        object.requestUrlPreview[j] = $root.proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview.toObject(message.requestUrlPreview[j], options, q + 1);
                }
                if (message.historySyncOnDemandRequest != null && Object.hasOwnProperty.call(message, "historySyncOnDemandRequest"))
                    object.historySyncOnDemandRequest = $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest.toObject(message.historySyncOnDemandRequest, options, q + 1);
                if (message.placeholderMessageResendRequest && message.placeholderMessageResendRequest.length) {
                    object.placeholderMessageResendRequest = Array(message.placeholderMessageResendRequest.length);
                    for (var j = 0; j < message.placeholderMessageResendRequest.length; ++j)
                        object.placeholderMessageResendRequest[j] = $root.proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest.toObject(message.placeholderMessageResendRequest[j], options, q + 1);
                }
                if (message.fullHistorySyncOnDemandRequest != null && Object.hasOwnProperty.call(message, "fullHistorySyncOnDemandRequest"))
                    object.fullHistorySyncOnDemandRequest = $root.proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.toObject(message.fullHistorySyncOnDemandRequest, options, q + 1);
                if (message.syncdCollectionFatalRecoveryRequest != null && Object.hasOwnProperty.call(message, "syncdCollectionFatalRecoveryRequest"))
                    object.syncdCollectionFatalRecoveryRequest = $root.proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest.toObject(message.syncdCollectionFatalRecoveryRequest, options, q + 1);
                if (message.historySyncChunkRetryRequest != null && Object.hasOwnProperty.call(message, "historySyncChunkRetryRequest"))
                    object.historySyncChunkRetryRequest = $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest.toObject(message.historySyncChunkRetryRequest, options, q + 1);
                if (message.galaxyFlowAction != null && Object.hasOwnProperty.call(message, "galaxyFlowAction"))
                    object.galaxyFlowAction = $root.proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction.toObject(message.galaxyFlowAction, options, q + 1);
                if (message.companionCanonicalUserNonceFetchRequest != null && Object.hasOwnProperty.call(message, "companionCanonicalUserNonceFetchRequest"))
                    object.companionCanonicalUserNonceFetchRequest = $root.proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest.toObject(message.companionCanonicalUserNonceFetchRequest, options, q + 1);
                if (message.bizBroadcastInsightsContactListRequest != null && Object.hasOwnProperty.call(message, "bizBroadcastInsightsContactListRequest"))
                    object.bizBroadcastInsightsContactListRequest = $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest.toObject(message.bizBroadcastInsightsContactListRequest, options, q + 1);
                if (message.bizBroadcastInsightsRefreshRequest != null && Object.hasOwnProperty.call(message, "bizBroadcastInsightsRefreshRequest"))
                    object.bizBroadcastInsightsRefreshRequest = $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest.toObject(message.bizBroadcastInsightsRefreshRequest, options, q + 1);
                return object;
            };

            /**
             * Converts this PeerDataOperationRequestMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PeerDataOperationRequestMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PeerDataOperationRequestMessage
             * @function getTypeUrl
             * @memberof proto.Message.PeerDataOperationRequestMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PeerDataOperationRequestMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestMessage";
            };

            PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest = (function() {

                /**
                 * Properties of a BizBroadcastInsightsContactListRequest.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @interface IBizBroadcastInsightsContactListRequest
                 * @property {string|null} [campaignId] BizBroadcastInsightsContactListRequest campaignId
                 */

                /**
                 * Constructs a new BizBroadcastInsightsContactListRequest.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @classdesc Represents a BizBroadcastInsightsContactListRequest.
                 * @implements IBizBroadcastInsightsContactListRequest
                 * @constructor
                 * @param {proto.Message.PeerDataOperationRequestMessage.IBizBroadcastInsightsContactListRequest=} [properties] Properties to set
                 */
                function BizBroadcastInsightsContactListRequest(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * BizBroadcastInsightsContactListRequest campaignId.
                 * @member {string|null|undefined} campaignId
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest
                 * @instance
                 */
                BizBroadcastInsightsContactListRequest.prototype.campaignId = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BizBroadcastInsightsContactListRequest.prototype, "_campaignId", {
                    get: $util.oneOfGetter($oneOfFields = ["campaignId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new BizBroadcastInsightsContactListRequest instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IBizBroadcastInsightsContactListRequest=} [properties] Properties to set
                 * @returns {proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest} BizBroadcastInsightsContactListRequest instance
                 */
                BizBroadcastInsightsContactListRequest.create = function create(properties) {
                    return new BizBroadcastInsightsContactListRequest(properties);
                };

                /**
                 * Encodes the specified BizBroadcastInsightsContactListRequest message. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IBizBroadcastInsightsContactListRequest} message BizBroadcastInsightsContactListRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BizBroadcastInsightsContactListRequest.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.campaignId != null && Object.hasOwnProperty.call(message, "campaignId"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.campaignId);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified BizBroadcastInsightsContactListRequest message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IBizBroadcastInsightsContactListRequest} message BizBroadcastInsightsContactListRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BizBroadcastInsightsContactListRequest.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a BizBroadcastInsightsContactListRequest message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest} BizBroadcastInsightsContactListRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BizBroadcastInsightsContactListRequest.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.campaignId = reader.stringVerify();
                                message._campaignId = "campaignId";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a BizBroadcastInsightsContactListRequest message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest} BizBroadcastInsightsContactListRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BizBroadcastInsightsContactListRequest.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a BizBroadcastInsightsContactListRequest message.
                 * @function verify
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                BizBroadcastInsightsContactListRequest.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.campaignId != null && Object.hasOwnProperty.call(message, "campaignId")) {
                        properties._campaignId = 1;
                        if (!$util.isString(message.campaignId))
                            return "campaignId: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a BizBroadcastInsightsContactListRequest message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest} BizBroadcastInsightsContactListRequest
                 */
                BizBroadcastInsightsContactListRequest.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.campaignId != null)
                        message.campaignId = String(object.campaignId);
                    return message;
                };

                /**
                 * Creates a plain object from a BizBroadcastInsightsContactListRequest message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest} message BizBroadcastInsightsContactListRequest
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                BizBroadcastInsightsContactListRequest.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.campaignId != null && Object.hasOwnProperty.call(message, "campaignId"))
                        object.campaignId = message.campaignId;
                    return object;
                };

                /**
                 * Converts this BizBroadcastInsightsContactListRequest to JSON.
                 * @function toJSON
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                BizBroadcastInsightsContactListRequest.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for BizBroadcastInsightsContactListRequest
                 * @function getTypeUrl
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                BizBroadcastInsightsContactListRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest";
                };

                return BizBroadcastInsightsContactListRequest;
            })();

            PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest = (function() {

                /**
                 * Properties of a BizBroadcastInsightsRefreshRequest.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @interface IBizBroadcastInsightsRefreshRequest
                 * @property {string|null} [campaignId] BizBroadcastInsightsRefreshRequest campaignId
                 */

                /**
                 * Constructs a new BizBroadcastInsightsRefreshRequest.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @classdesc Represents a BizBroadcastInsightsRefreshRequest.
                 * @implements IBizBroadcastInsightsRefreshRequest
                 * @constructor
                 * @param {proto.Message.PeerDataOperationRequestMessage.IBizBroadcastInsightsRefreshRequest=} [properties] Properties to set
                 */
                function BizBroadcastInsightsRefreshRequest(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * BizBroadcastInsightsRefreshRequest campaignId.
                 * @member {string|null|undefined} campaignId
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest
                 * @instance
                 */
                BizBroadcastInsightsRefreshRequest.prototype.campaignId = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BizBroadcastInsightsRefreshRequest.prototype, "_campaignId", {
                    get: $util.oneOfGetter($oneOfFields = ["campaignId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new BizBroadcastInsightsRefreshRequest instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IBizBroadcastInsightsRefreshRequest=} [properties] Properties to set
                 * @returns {proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest} BizBroadcastInsightsRefreshRequest instance
                 */
                BizBroadcastInsightsRefreshRequest.create = function create(properties) {
                    return new BizBroadcastInsightsRefreshRequest(properties);
                };

                /**
                 * Encodes the specified BizBroadcastInsightsRefreshRequest message. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IBizBroadcastInsightsRefreshRequest} message BizBroadcastInsightsRefreshRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BizBroadcastInsightsRefreshRequest.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.campaignId != null && Object.hasOwnProperty.call(message, "campaignId"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.campaignId);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified BizBroadcastInsightsRefreshRequest message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IBizBroadcastInsightsRefreshRequest} message BizBroadcastInsightsRefreshRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                BizBroadcastInsightsRefreshRequest.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a BizBroadcastInsightsRefreshRequest message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest} BizBroadcastInsightsRefreshRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BizBroadcastInsightsRefreshRequest.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.campaignId = reader.stringVerify();
                                message._campaignId = "campaignId";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a BizBroadcastInsightsRefreshRequest message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest} BizBroadcastInsightsRefreshRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                BizBroadcastInsightsRefreshRequest.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a BizBroadcastInsightsRefreshRequest message.
                 * @function verify
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                BizBroadcastInsightsRefreshRequest.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.campaignId != null && Object.hasOwnProperty.call(message, "campaignId")) {
                        properties._campaignId = 1;
                        if (!$util.isString(message.campaignId))
                            return "campaignId: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a BizBroadcastInsightsRefreshRequest message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest} BizBroadcastInsightsRefreshRequest
                 */
                BizBroadcastInsightsRefreshRequest.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.campaignId != null)
                        message.campaignId = String(object.campaignId);
                    return message;
                };

                /**
                 * Creates a plain object from a BizBroadcastInsightsRefreshRequest message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest} message BizBroadcastInsightsRefreshRequest
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                BizBroadcastInsightsRefreshRequest.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.campaignId != null && Object.hasOwnProperty.call(message, "campaignId"))
                        object.campaignId = message.campaignId;
                    return object;
                };

                /**
                 * Converts this BizBroadcastInsightsRefreshRequest to JSON.
                 * @function toJSON
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                BizBroadcastInsightsRefreshRequest.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for BizBroadcastInsightsRefreshRequest
                 * @function getTypeUrl
                 * @memberof proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                BizBroadcastInsightsRefreshRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest";
                };

                return BizBroadcastInsightsRefreshRequest;
            })();

            PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest = (function() {

                /**
                 * Properties of a CompanionCanonicalUserNonceFetchRequest.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @interface ICompanionCanonicalUserNonceFetchRequest
                 * @property {string|null} [registrationTraceId] CompanionCanonicalUserNonceFetchRequest registrationTraceId
                 */

                /**
                 * Constructs a new CompanionCanonicalUserNonceFetchRequest.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @classdesc Represents a CompanionCanonicalUserNonceFetchRequest.
                 * @implements ICompanionCanonicalUserNonceFetchRequest
                 * @constructor
                 * @param {proto.Message.PeerDataOperationRequestMessage.ICompanionCanonicalUserNonceFetchRequest=} [properties] Properties to set
                 */
                function CompanionCanonicalUserNonceFetchRequest(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * CompanionCanonicalUserNonceFetchRequest registrationTraceId.
                 * @member {string|null|undefined} registrationTraceId
                 * @memberof proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest
                 * @instance
                 */
                CompanionCanonicalUserNonceFetchRequest.prototype.registrationTraceId = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CompanionCanonicalUserNonceFetchRequest.prototype, "_registrationTraceId", {
                    get: $util.oneOfGetter($oneOfFields = ["registrationTraceId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new CompanionCanonicalUserNonceFetchRequest instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.ICompanionCanonicalUserNonceFetchRequest=} [properties] Properties to set
                 * @returns {proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest} CompanionCanonicalUserNonceFetchRequest instance
                 */
                CompanionCanonicalUserNonceFetchRequest.create = function create(properties) {
                    return new CompanionCanonicalUserNonceFetchRequest(properties);
                };

                /**
                 * Encodes the specified CompanionCanonicalUserNonceFetchRequest message. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.ICompanionCanonicalUserNonceFetchRequest} message CompanionCanonicalUserNonceFetchRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                CompanionCanonicalUserNonceFetchRequest.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.registrationTraceId != null && Object.hasOwnProperty.call(message, "registrationTraceId"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.registrationTraceId);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified CompanionCanonicalUserNonceFetchRequest message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.ICompanionCanonicalUserNonceFetchRequest} message CompanionCanonicalUserNonceFetchRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                CompanionCanonicalUserNonceFetchRequest.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a CompanionCanonicalUserNonceFetchRequest message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest} CompanionCanonicalUserNonceFetchRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                CompanionCanonicalUserNonceFetchRequest.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.registrationTraceId = reader.stringVerify();
                                message._registrationTraceId = "registrationTraceId";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a CompanionCanonicalUserNonceFetchRequest message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest} CompanionCanonicalUserNonceFetchRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                CompanionCanonicalUserNonceFetchRequest.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a CompanionCanonicalUserNonceFetchRequest message.
                 * @function verify
                 * @memberof proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                CompanionCanonicalUserNonceFetchRequest.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.registrationTraceId != null && Object.hasOwnProperty.call(message, "registrationTraceId")) {
                        properties._registrationTraceId = 1;
                        if (!$util.isString(message.registrationTraceId))
                            return "registrationTraceId: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a CompanionCanonicalUserNonceFetchRequest message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest} CompanionCanonicalUserNonceFetchRequest
                 */
                CompanionCanonicalUserNonceFetchRequest.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.registrationTraceId != null)
                        message.registrationTraceId = String(object.registrationTraceId);
                    return message;
                };

                /**
                 * Creates a plain object from a CompanionCanonicalUserNonceFetchRequest message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest} message CompanionCanonicalUserNonceFetchRequest
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                CompanionCanonicalUserNonceFetchRequest.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.registrationTraceId != null && Object.hasOwnProperty.call(message, "registrationTraceId"))
                        object.registrationTraceId = message.registrationTraceId;
                    return object;
                };

                /**
                 * Converts this CompanionCanonicalUserNonceFetchRequest to JSON.
                 * @function toJSON
                 * @memberof proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                CompanionCanonicalUserNonceFetchRequest.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for CompanionCanonicalUserNonceFetchRequest
                 * @function getTypeUrl
                 * @memberof proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                CompanionCanonicalUserNonceFetchRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest";
                };

                return CompanionCanonicalUserNonceFetchRequest;
            })();

            PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest = (function() {

                /**
                 * Properties of a FullHistorySyncOnDemandRequest.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @interface IFullHistorySyncOnDemandRequest
                 * @property {proto.Message.IFullHistorySyncOnDemandRequestMetadata|null} [requestMetadata] FullHistorySyncOnDemandRequest requestMetadata
                 * @property {proto.DeviceProps.IHistorySyncConfig|null} [historySyncConfig] FullHistorySyncOnDemandRequest historySyncConfig
                 * @property {proto.Message.IFullHistorySyncOnDemandConfig|null} [fullHistorySyncOnDemandConfig] FullHistorySyncOnDemandRequest fullHistorySyncOnDemandConfig
                 */

                /**
                 * Constructs a new FullHistorySyncOnDemandRequest.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @classdesc Represents a FullHistorySyncOnDemandRequest.
                 * @implements IFullHistorySyncOnDemandRequest
                 * @constructor
                 * @param {proto.Message.PeerDataOperationRequestMessage.IFullHistorySyncOnDemandRequest=} [properties] Properties to set
                 */
                function FullHistorySyncOnDemandRequest(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * FullHistorySyncOnDemandRequest requestMetadata.
                 * @member {proto.Message.IFullHistorySyncOnDemandRequestMetadata|null|undefined} requestMetadata
                 * @memberof proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest
                 * @instance
                 */
                FullHistorySyncOnDemandRequest.prototype.requestMetadata = null;

                /**
                 * FullHistorySyncOnDemandRequest historySyncConfig.
                 * @member {proto.DeviceProps.IHistorySyncConfig|null|undefined} historySyncConfig
                 * @memberof proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest
                 * @instance
                 */
                FullHistorySyncOnDemandRequest.prototype.historySyncConfig = null;

                /**
                 * FullHistorySyncOnDemandRequest fullHistorySyncOnDemandConfig.
                 * @member {proto.Message.IFullHistorySyncOnDemandConfig|null|undefined} fullHistorySyncOnDemandConfig
                 * @memberof proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest
                 * @instance
                 */
                FullHistorySyncOnDemandRequest.prototype.fullHistorySyncOnDemandConfig = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(FullHistorySyncOnDemandRequest.prototype, "_requestMetadata", {
                    get: $util.oneOfGetter($oneOfFields = ["requestMetadata"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(FullHistorySyncOnDemandRequest.prototype, "_historySyncConfig", {
                    get: $util.oneOfGetter($oneOfFields = ["historySyncConfig"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(FullHistorySyncOnDemandRequest.prototype, "_fullHistorySyncOnDemandConfig", {
                    get: $util.oneOfGetter($oneOfFields = ["fullHistorySyncOnDemandConfig"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new FullHistorySyncOnDemandRequest instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IFullHistorySyncOnDemandRequest=} [properties] Properties to set
                 * @returns {proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest} FullHistorySyncOnDemandRequest instance
                 */
                FullHistorySyncOnDemandRequest.create = function create(properties) {
                    return new FullHistorySyncOnDemandRequest(properties);
                };

                /**
                 * Encodes the specified FullHistorySyncOnDemandRequest message. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IFullHistorySyncOnDemandRequest} message FullHistorySyncOnDemandRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                FullHistorySyncOnDemandRequest.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.requestMetadata != null && Object.hasOwnProperty.call(message, "requestMetadata"))
                        $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.encode(message.requestMetadata, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                    if (message.historySyncConfig != null && Object.hasOwnProperty.call(message, "historySyncConfig"))
                        $root.proto.DeviceProps.HistorySyncConfig.encode(message.historySyncConfig, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                    if (message.fullHistorySyncOnDemandConfig != null && Object.hasOwnProperty.call(message, "fullHistorySyncOnDemandConfig"))
                        $root.proto.Message.FullHistorySyncOnDemandConfig.encode(message.fullHistorySyncOnDemandConfig, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified FullHistorySyncOnDemandRequest message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IFullHistorySyncOnDemandRequest} message FullHistorySyncOnDemandRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                FullHistorySyncOnDemandRequest.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a FullHistorySyncOnDemandRequest message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest} FullHistorySyncOnDemandRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                FullHistorySyncOnDemandRequest.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.requestMetadata = $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.requestMetadata);
                                message._requestMetadata = "requestMetadata";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.historySyncConfig = $root.proto.DeviceProps.HistorySyncConfig.decode(reader, reader.uint32(), undefined, q + 1, message.historySyncConfig);
                                message._historySyncConfig = "historySyncConfig";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.fullHistorySyncOnDemandConfig = $root.proto.Message.FullHistorySyncOnDemandConfig.decode(reader, reader.uint32(), undefined, q + 1, message.fullHistorySyncOnDemandConfig);
                                message._fullHistorySyncOnDemandConfig = "fullHistorySyncOnDemandConfig";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a FullHistorySyncOnDemandRequest message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest} FullHistorySyncOnDemandRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                FullHistorySyncOnDemandRequest.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a FullHistorySyncOnDemandRequest message.
                 * @function verify
                 * @memberof proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                FullHistorySyncOnDemandRequest.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.requestMetadata != null && Object.hasOwnProperty.call(message, "requestMetadata")) {
                        properties._requestMetadata = 1;
                        {
                            var error = $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.verify(message.requestMetadata, q + 1);
                            if (error)
                                return "requestMetadata." + error;
                        }
                    }
                    if (message.historySyncConfig != null && Object.hasOwnProperty.call(message, "historySyncConfig")) {
                        properties._historySyncConfig = 1;
                        {
                            var error = $root.proto.DeviceProps.HistorySyncConfig.verify(message.historySyncConfig, q + 1);
                            if (error)
                                return "historySyncConfig." + error;
                        }
                    }
                    if (message.fullHistorySyncOnDemandConfig != null && Object.hasOwnProperty.call(message, "fullHistorySyncOnDemandConfig")) {
                        properties._fullHistorySyncOnDemandConfig = 1;
                        {
                            var error = $root.proto.Message.FullHistorySyncOnDemandConfig.verify(message.fullHistorySyncOnDemandConfig, q + 1);
                            if (error)
                                return "fullHistorySyncOnDemandConfig." + error;
                        }
                    }
                    return null;
                };

                /**
                 * Creates a FullHistorySyncOnDemandRequest message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest} FullHistorySyncOnDemandRequest
                 */
                FullHistorySyncOnDemandRequest.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.requestMetadata != null) {
                        if (!$util.isObject(object.requestMetadata))
                            throw TypeError(".proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.requestMetadata: object expected");
                        message.requestMetadata = $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.fromObject(object.requestMetadata, q + 1);
                    }
                    if (object.historySyncConfig != null) {
                        if (!$util.isObject(object.historySyncConfig))
                            throw TypeError(".proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.historySyncConfig: object expected");
                        message.historySyncConfig = $root.proto.DeviceProps.HistorySyncConfig.fromObject(object.historySyncConfig, q + 1);
                    }
                    if (object.fullHistorySyncOnDemandConfig != null) {
                        if (!$util.isObject(object.fullHistorySyncOnDemandConfig))
                            throw TypeError(".proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.fullHistorySyncOnDemandConfig: object expected");
                        message.fullHistorySyncOnDemandConfig = $root.proto.Message.FullHistorySyncOnDemandConfig.fromObject(object.fullHistorySyncOnDemandConfig, q + 1);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a FullHistorySyncOnDemandRequest message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest} message FullHistorySyncOnDemandRequest
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                FullHistorySyncOnDemandRequest.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.requestMetadata != null && Object.hasOwnProperty.call(message, "requestMetadata"))
                        object.requestMetadata = $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.toObject(message.requestMetadata, options, q + 1);
                    if (message.historySyncConfig != null && Object.hasOwnProperty.call(message, "historySyncConfig"))
                        object.historySyncConfig = $root.proto.DeviceProps.HistorySyncConfig.toObject(message.historySyncConfig, options, q + 1);
                    if (message.fullHistorySyncOnDemandConfig != null && Object.hasOwnProperty.call(message, "fullHistorySyncOnDemandConfig"))
                        object.fullHistorySyncOnDemandConfig = $root.proto.Message.FullHistorySyncOnDemandConfig.toObject(message.fullHistorySyncOnDemandConfig, options, q + 1);
                    return object;
                };

                /**
                 * Converts this FullHistorySyncOnDemandRequest to JSON.
                 * @function toJSON
                 * @memberof proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                FullHistorySyncOnDemandRequest.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for FullHistorySyncOnDemandRequest
                 * @function getTypeUrl
                 * @memberof proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                FullHistorySyncOnDemandRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest";
                };

                return FullHistorySyncOnDemandRequest;
            })();

            PeerDataOperationRequestMessage.GalaxyFlowAction = (function() {

                /**
                 * Properties of a GalaxyFlowAction.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @interface IGalaxyFlowAction
                 * @property {proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction.GalaxyFlowActionType|null} [type] GalaxyFlowAction type
                 * @property {string|null} [flowId] GalaxyFlowAction flowId
                 * @property {string|null} [stanzaId] GalaxyFlowAction stanzaId
                 * @property {string|null} [galaxyFlowDownloadRequestId] GalaxyFlowAction galaxyFlowDownloadRequestId
                 * @property {string|null} [agmId] GalaxyFlowAction agmId
                 */

                /**
                 * Constructs a new GalaxyFlowAction.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @classdesc Represents a GalaxyFlowAction.
                 * @implements IGalaxyFlowAction
                 * @constructor
                 * @param {proto.Message.PeerDataOperationRequestMessage.IGalaxyFlowAction=} [properties] Properties to set
                 */
                function GalaxyFlowAction(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * GalaxyFlowAction type.
                 * @member {proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction.GalaxyFlowActionType|null|undefined} type
                 * @memberof proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction
                 * @instance
                 */
                GalaxyFlowAction.prototype.type = null;

                /**
                 * GalaxyFlowAction flowId.
                 * @member {string|null|undefined} flowId
                 * @memberof proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction
                 * @instance
                 */
                GalaxyFlowAction.prototype.flowId = null;

                /**
                 * GalaxyFlowAction stanzaId.
                 * @member {string|null|undefined} stanzaId
                 * @memberof proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction
                 * @instance
                 */
                GalaxyFlowAction.prototype.stanzaId = null;

                /**
                 * GalaxyFlowAction galaxyFlowDownloadRequestId.
                 * @member {string|null|undefined} galaxyFlowDownloadRequestId
                 * @memberof proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction
                 * @instance
                 */
                GalaxyFlowAction.prototype.galaxyFlowDownloadRequestId = null;

                /**
                 * GalaxyFlowAction agmId.
                 * @member {string|null|undefined} agmId
                 * @memberof proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction
                 * @instance
                 */
                GalaxyFlowAction.prototype.agmId = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(GalaxyFlowAction.prototype, "_type", {
                    get: $util.oneOfGetter($oneOfFields = ["type"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(GalaxyFlowAction.prototype, "_flowId", {
                    get: $util.oneOfGetter($oneOfFields = ["flowId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(GalaxyFlowAction.prototype, "_stanzaId", {
                    get: $util.oneOfGetter($oneOfFields = ["stanzaId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(GalaxyFlowAction.prototype, "_galaxyFlowDownloadRequestId", {
                    get: $util.oneOfGetter($oneOfFields = ["galaxyFlowDownloadRequestId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(GalaxyFlowAction.prototype, "_agmId", {
                    get: $util.oneOfGetter($oneOfFields = ["agmId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new GalaxyFlowAction instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IGalaxyFlowAction=} [properties] Properties to set
                 * @returns {proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction} GalaxyFlowAction instance
                 */
                GalaxyFlowAction.create = function create(properties) {
                    return new GalaxyFlowAction(properties);
                };

                /**
                 * Encodes the specified GalaxyFlowAction message. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IGalaxyFlowAction} message GalaxyFlowAction message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                GalaxyFlowAction.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
                    if (message.flowId != null && Object.hasOwnProperty.call(message, "flowId"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.flowId);
                    if (message.stanzaId != null && Object.hasOwnProperty.call(message, "stanzaId"))
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.stanzaId);
                    if (message.galaxyFlowDownloadRequestId != null && Object.hasOwnProperty.call(message, "galaxyFlowDownloadRequestId"))
                        writer.uint32(/* id 4, wireType 2 =*/34).string(message.galaxyFlowDownloadRequestId);
                    if (message.agmId != null && Object.hasOwnProperty.call(message, "agmId"))
                        writer.uint32(/* id 5, wireType 2 =*/42).string(message.agmId);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified GalaxyFlowAction message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IGalaxyFlowAction} message GalaxyFlowAction message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                GalaxyFlowAction.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a GalaxyFlowAction message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction} GalaxyFlowAction
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                GalaxyFlowAction.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                message.type = reader.int32();
                                message._type = "type";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.flowId = reader.stringVerify();
                                message._flowId = "flowId";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.stanzaId = reader.stringVerify();
                                message._stanzaId = "stanzaId";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                message.galaxyFlowDownloadRequestId = reader.stringVerify();
                                message._galaxyFlowDownloadRequestId = "galaxyFlowDownloadRequestId";
                                continue;
                            }
                        case 5: {
                                if (u !== 2)
                                    break;
                                message.agmId = reader.stringVerify();
                                message._agmId = "agmId";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a GalaxyFlowAction message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction} GalaxyFlowAction
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                GalaxyFlowAction.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a GalaxyFlowAction message.
                 * @function verify
                 * @memberof proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                GalaxyFlowAction.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                        properties._type = 1;
                        if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                            return "type: enum value expected";
                    }
                    if (message.flowId != null && Object.hasOwnProperty.call(message, "flowId")) {
                        properties._flowId = 1;
                        if (!$util.isString(message.flowId))
                            return "flowId: string expected";
                    }
                    if (message.stanzaId != null && Object.hasOwnProperty.call(message, "stanzaId")) {
                        properties._stanzaId = 1;
                        if (!$util.isString(message.stanzaId))
                            return "stanzaId: string expected";
                    }
                    if (message.galaxyFlowDownloadRequestId != null && Object.hasOwnProperty.call(message, "galaxyFlowDownloadRequestId")) {
                        properties._galaxyFlowDownloadRequestId = 1;
                        if (!$util.isString(message.galaxyFlowDownloadRequestId))
                            return "galaxyFlowDownloadRequestId: string expected";
                    }
                    if (message.agmId != null && Object.hasOwnProperty.call(message, "agmId")) {
                        properties._agmId = 1;
                        if (!$util.isString(message.agmId))
                            return "agmId: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a GalaxyFlowAction message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction} GalaxyFlowAction
                 */
                GalaxyFlowAction.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    switch (object.type) {
                    case "NOTIFY_LAUNCH":
                    case 1:
                        message.type = 1;
                        break;
                    case "DOWNLOAD_RESPONSES":
                    case 2:
                        message.type = 2;
                        break;
                    default:
                        if (typeof object.type === "number" && (object.type | 0) === object.type)
                            message.type = object.type;
                    }
                    if (object.flowId != null)
                        message.flowId = String(object.flowId);
                    if (object.stanzaId != null)
                        message.stanzaId = String(object.stanzaId);
                    if (object.galaxyFlowDownloadRequestId != null)
                        message.galaxyFlowDownloadRequestId = String(object.galaxyFlowDownloadRequestId);
                    if (object.agmId != null)
                        message.agmId = String(object.agmId);
                    return message;
                };

                /**
                 * Creates a plain object from a GalaxyFlowAction message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction} message GalaxyFlowAction
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                GalaxyFlowAction.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                        object.type = options.enums === String ? $root.proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction.GalaxyFlowActionType[message.type] === undefined ? message.type : $root.proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction.GalaxyFlowActionType[message.type] : message.type;
                    if (message.flowId != null && Object.hasOwnProperty.call(message, "flowId"))
                        object.flowId = message.flowId;
                    if (message.stanzaId != null && Object.hasOwnProperty.call(message, "stanzaId"))
                        object.stanzaId = message.stanzaId;
                    if (message.galaxyFlowDownloadRequestId != null && Object.hasOwnProperty.call(message, "galaxyFlowDownloadRequestId"))
                        object.galaxyFlowDownloadRequestId = message.galaxyFlowDownloadRequestId;
                    if (message.agmId != null && Object.hasOwnProperty.call(message, "agmId"))
                        object.agmId = message.agmId;
                    return object;
                };

                /**
                 * Converts this GalaxyFlowAction to JSON.
                 * @function toJSON
                 * @memberof proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                GalaxyFlowAction.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for GalaxyFlowAction
                 * @function getTypeUrl
                 * @memberof proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                GalaxyFlowAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction";
                };

                /**
                 * GalaxyFlowActionType enum.
                 * @name proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction.GalaxyFlowActionType
                 * @enum {number}
                 * @property {number} NOTIFY_LAUNCH=1 NOTIFY_LAUNCH value
                 * @property {number} DOWNLOAD_RESPONSES=2 DOWNLOAD_RESPONSES value
                 */
                GalaxyFlowAction.GalaxyFlowActionType = (function() {
                    var valuesById = {}, values = Object.create(valuesById);
                    values[valuesById[1] = "NOTIFY_LAUNCH"] = 1;
                    values[valuesById[2] = "DOWNLOAD_RESPONSES"] = 2;
                    return values;
                })();

                return GalaxyFlowAction;
            })();

            PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest = (function() {

                /**
                 * Properties of a HistorySyncChunkRetryRequest.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @interface IHistorySyncChunkRetryRequest
                 * @property {proto.Message.HistorySyncType|null} [syncType] HistorySyncChunkRetryRequest syncType
                 * @property {number|null} [chunkOrder] HistorySyncChunkRetryRequest chunkOrder
                 * @property {string|null} [chunkNotificationId] HistorySyncChunkRetryRequest chunkNotificationId
                 * @property {boolean|null} [regenerateChunk] HistorySyncChunkRetryRequest regenerateChunk
                 */

                /**
                 * Constructs a new HistorySyncChunkRetryRequest.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @classdesc Represents a HistorySyncChunkRetryRequest.
                 * @implements IHistorySyncChunkRetryRequest
                 * @constructor
                 * @param {proto.Message.PeerDataOperationRequestMessage.IHistorySyncChunkRetryRequest=} [properties] Properties to set
                 */
                function HistorySyncChunkRetryRequest(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * HistorySyncChunkRetryRequest syncType.
                 * @member {proto.Message.HistorySyncType|null|undefined} syncType
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest
                 * @instance
                 */
                HistorySyncChunkRetryRequest.prototype.syncType = null;

                /**
                 * HistorySyncChunkRetryRequest chunkOrder.
                 * @member {number|null|undefined} chunkOrder
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest
                 * @instance
                 */
                HistorySyncChunkRetryRequest.prototype.chunkOrder = null;

                /**
                 * HistorySyncChunkRetryRequest chunkNotificationId.
                 * @member {string|null|undefined} chunkNotificationId
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest
                 * @instance
                 */
                HistorySyncChunkRetryRequest.prototype.chunkNotificationId = null;

                /**
                 * HistorySyncChunkRetryRequest regenerateChunk.
                 * @member {boolean|null|undefined} regenerateChunk
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest
                 * @instance
                 */
                HistorySyncChunkRetryRequest.prototype.regenerateChunk = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HistorySyncChunkRetryRequest.prototype, "_syncType", {
                    get: $util.oneOfGetter($oneOfFields = ["syncType"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HistorySyncChunkRetryRequest.prototype, "_chunkOrder", {
                    get: $util.oneOfGetter($oneOfFields = ["chunkOrder"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HistorySyncChunkRetryRequest.prototype, "_chunkNotificationId", {
                    get: $util.oneOfGetter($oneOfFields = ["chunkNotificationId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HistorySyncChunkRetryRequest.prototype, "_regenerateChunk", {
                    get: $util.oneOfGetter($oneOfFields = ["regenerateChunk"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new HistorySyncChunkRetryRequest instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IHistorySyncChunkRetryRequest=} [properties] Properties to set
                 * @returns {proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest} HistorySyncChunkRetryRequest instance
                 */
                HistorySyncChunkRetryRequest.create = function create(properties) {
                    return new HistorySyncChunkRetryRequest(properties);
                };

                /**
                 * Encodes the specified HistorySyncChunkRetryRequest message. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IHistorySyncChunkRetryRequest} message HistorySyncChunkRetryRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                HistorySyncChunkRetryRequest.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.syncType != null && Object.hasOwnProperty.call(message, "syncType"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int32(message.syncType);
                    if (message.chunkOrder != null && Object.hasOwnProperty.call(message, "chunkOrder"))
                        writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.chunkOrder);
                    if (message.chunkNotificationId != null && Object.hasOwnProperty.call(message, "chunkNotificationId"))
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.chunkNotificationId);
                    if (message.regenerateChunk != null && Object.hasOwnProperty.call(message, "regenerateChunk"))
                        writer.uint32(/* id 4, wireType 0 =*/32).bool(message.regenerateChunk);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified HistorySyncChunkRetryRequest message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IHistorySyncChunkRetryRequest} message HistorySyncChunkRetryRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                HistorySyncChunkRetryRequest.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a HistorySyncChunkRetryRequest message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest} HistorySyncChunkRetryRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                HistorySyncChunkRetryRequest.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                message.syncType = reader.int32();
                                message._syncType = "syncType";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                message.chunkOrder = reader.uint32();
                                message._chunkOrder = "chunkOrder";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.chunkNotificationId = reader.stringVerify();
                                message._chunkNotificationId = "chunkNotificationId";
                                continue;
                            }
                        case 4: {
                                if (u !== 0)
                                    break;
                                message.regenerateChunk = reader.bool();
                                message._regenerateChunk = "regenerateChunk";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a HistorySyncChunkRetryRequest message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest} HistorySyncChunkRetryRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                HistorySyncChunkRetryRequest.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a HistorySyncChunkRetryRequest message.
                 * @function verify
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                HistorySyncChunkRetryRequest.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.syncType != null && Object.hasOwnProperty.call(message, "syncType")) {
                        properties._syncType = 1;
                        if (typeof message.syncType !== "number" || (message.syncType | 0) !== message.syncType)
                            return "syncType: enum value expected";
                    }
                    if (message.chunkOrder != null && Object.hasOwnProperty.call(message, "chunkOrder")) {
                        properties._chunkOrder = 1;
                        if (!$util.isInteger(message.chunkOrder))
                            return "chunkOrder: integer expected";
                    }
                    if (message.chunkNotificationId != null && Object.hasOwnProperty.call(message, "chunkNotificationId")) {
                        properties._chunkNotificationId = 1;
                        if (!$util.isString(message.chunkNotificationId))
                            return "chunkNotificationId: string expected";
                    }
                    if (message.regenerateChunk != null && Object.hasOwnProperty.call(message, "regenerateChunk")) {
                        properties._regenerateChunk = 1;
                        if (typeof message.regenerateChunk !== "boolean")
                            return "regenerateChunk: boolean expected";
                    }
                    return null;
                };

                /**
                 * Creates a HistorySyncChunkRetryRequest message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest} HistorySyncChunkRetryRequest
                 */
                HistorySyncChunkRetryRequest.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    switch (object.syncType) {
                    case "INITIAL_BOOTSTRAP":
                    case 0:
                        message.syncType = 0;
                        break;
                    case "INITIAL_STATUS_V3":
                    case 1:
                        message.syncType = 1;
                        break;
                    case "FULL":
                    case 2:
                        message.syncType = 2;
                        break;
                    case "RECENT":
                    case 3:
                        message.syncType = 3;
                        break;
                    case "PUSH_NAME":
                    case 4:
                        message.syncType = 4;
                        break;
                    case "NON_BLOCKING_DATA":
                    case 5:
                        message.syncType = 5;
                        break;
                    case "ON_DEMAND":
                    case 6:
                        message.syncType = 6;
                        break;
                    case "NO_HISTORY":
                    case 7:
                        message.syncType = 7;
                        break;
                    case "MESSAGE_ACCESS_STATUS":
                    case 8:
                        message.syncType = 8;
                        break;
                    default:
                        if (typeof object.syncType === "number" && (object.syncType | 0) === object.syncType)
                            message.syncType = object.syncType;
                    }
                    if (object.chunkOrder != null)
                        message.chunkOrder = object.chunkOrder >>> 0;
                    if (object.chunkNotificationId != null)
                        message.chunkNotificationId = String(object.chunkNotificationId);
                    if (object.regenerateChunk != null)
                        message.regenerateChunk = Boolean(object.regenerateChunk);
                    return message;
                };

                /**
                 * Creates a plain object from a HistorySyncChunkRetryRequest message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest} message HistorySyncChunkRetryRequest
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                HistorySyncChunkRetryRequest.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.syncType != null && Object.hasOwnProperty.call(message, "syncType"))
                        object.syncType = options.enums === String ? $root.proto.Message.HistorySyncType[message.syncType] === undefined ? message.syncType : $root.proto.Message.HistorySyncType[message.syncType] : message.syncType;
                    if (message.chunkOrder != null && Object.hasOwnProperty.call(message, "chunkOrder"))
                        object.chunkOrder = message.chunkOrder;
                    if (message.chunkNotificationId != null && Object.hasOwnProperty.call(message, "chunkNotificationId"))
                        object.chunkNotificationId = message.chunkNotificationId;
                    if (message.regenerateChunk != null && Object.hasOwnProperty.call(message, "regenerateChunk"))
                        object.regenerateChunk = message.regenerateChunk;
                    return object;
                };

                /**
                 * Converts this HistorySyncChunkRetryRequest to JSON.
                 * @function toJSON
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                HistorySyncChunkRetryRequest.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for HistorySyncChunkRetryRequest
                 * @function getTypeUrl
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                HistorySyncChunkRetryRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest";
                };

                return HistorySyncChunkRetryRequest;
            })();

            PeerDataOperationRequestMessage.HistorySyncOnDemandRequest = (function() {

                /**
                 * Properties of a HistorySyncOnDemandRequest.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @interface IHistorySyncOnDemandRequest
                 * @property {string|null} [chatJid] HistorySyncOnDemandRequest chatJid
                 * @property {string|null} [oldestMsgId] HistorySyncOnDemandRequest oldestMsgId
                 * @property {boolean|null} [oldestMsgFromMe] HistorySyncOnDemandRequest oldestMsgFromMe
                 * @property {number|null} [onDemandMsgCount] HistorySyncOnDemandRequest onDemandMsgCount
                 * @property {number|Long|null} [oldestMsgTimestampMs] HistorySyncOnDemandRequest oldestMsgTimestampMs
                 * @property {string|null} [accountLid] HistorySyncOnDemandRequest accountLid
                 * @property {boolean|null} [supportInlineResponse] HistorySyncOnDemandRequest supportInlineResponse
                 */

                /**
                 * Constructs a new HistorySyncOnDemandRequest.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @classdesc Represents a HistorySyncOnDemandRequest.
                 * @implements IHistorySyncOnDemandRequest
                 * @constructor
                 * @param {proto.Message.PeerDataOperationRequestMessage.IHistorySyncOnDemandRequest=} [properties] Properties to set
                 */
                function HistorySyncOnDemandRequest(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * HistorySyncOnDemandRequest chatJid.
                 * @member {string|null|undefined} chatJid
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest
                 * @instance
                 */
                HistorySyncOnDemandRequest.prototype.chatJid = null;

                /**
                 * HistorySyncOnDemandRequest oldestMsgId.
                 * @member {string|null|undefined} oldestMsgId
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest
                 * @instance
                 */
                HistorySyncOnDemandRequest.prototype.oldestMsgId = null;

                /**
                 * HistorySyncOnDemandRequest oldestMsgFromMe.
                 * @member {boolean|null|undefined} oldestMsgFromMe
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest
                 * @instance
                 */
                HistorySyncOnDemandRequest.prototype.oldestMsgFromMe = null;

                /**
                 * HistorySyncOnDemandRequest onDemandMsgCount.
                 * @member {number|null|undefined} onDemandMsgCount
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest
                 * @instance
                 */
                HistorySyncOnDemandRequest.prototype.onDemandMsgCount = null;

                /**
                 * HistorySyncOnDemandRequest oldestMsgTimestampMs.
                 * @member {number|Long|null|undefined} oldestMsgTimestampMs
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest
                 * @instance
                 */
                HistorySyncOnDemandRequest.prototype.oldestMsgTimestampMs = null;

                /**
                 * HistorySyncOnDemandRequest accountLid.
                 * @member {string|null|undefined} accountLid
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest
                 * @instance
                 */
                HistorySyncOnDemandRequest.prototype.accountLid = null;

                /**
                 * HistorySyncOnDemandRequest supportInlineResponse.
                 * @member {boolean|null|undefined} supportInlineResponse
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest
                 * @instance
                 */
                HistorySyncOnDemandRequest.prototype.supportInlineResponse = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HistorySyncOnDemandRequest.prototype, "_chatJid", {
                    get: $util.oneOfGetter($oneOfFields = ["chatJid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HistorySyncOnDemandRequest.prototype, "_oldestMsgId", {
                    get: $util.oneOfGetter($oneOfFields = ["oldestMsgId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HistorySyncOnDemandRequest.prototype, "_oldestMsgFromMe", {
                    get: $util.oneOfGetter($oneOfFields = ["oldestMsgFromMe"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HistorySyncOnDemandRequest.prototype, "_onDemandMsgCount", {
                    get: $util.oneOfGetter($oneOfFields = ["onDemandMsgCount"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HistorySyncOnDemandRequest.prototype, "_oldestMsgTimestampMs", {
                    get: $util.oneOfGetter($oneOfFields = ["oldestMsgTimestampMs"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HistorySyncOnDemandRequest.prototype, "_accountLid", {
                    get: $util.oneOfGetter($oneOfFields = ["accountLid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HistorySyncOnDemandRequest.prototype, "_supportInlineResponse", {
                    get: $util.oneOfGetter($oneOfFields = ["supportInlineResponse"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new HistorySyncOnDemandRequest instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IHistorySyncOnDemandRequest=} [properties] Properties to set
                 * @returns {proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest} HistorySyncOnDemandRequest instance
                 */
                HistorySyncOnDemandRequest.create = function create(properties) {
                    return new HistorySyncOnDemandRequest(properties);
                };

                /**
                 * Encodes the specified HistorySyncOnDemandRequest message. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IHistorySyncOnDemandRequest} message HistorySyncOnDemandRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                HistorySyncOnDemandRequest.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.chatJid != null && Object.hasOwnProperty.call(message, "chatJid"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.chatJid);
                    if (message.oldestMsgId != null && Object.hasOwnProperty.call(message, "oldestMsgId"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.oldestMsgId);
                    if (message.oldestMsgFromMe != null && Object.hasOwnProperty.call(message, "oldestMsgFromMe"))
                        writer.uint32(/* id 3, wireType 0 =*/24).bool(message.oldestMsgFromMe);
                    if (message.onDemandMsgCount != null && Object.hasOwnProperty.call(message, "onDemandMsgCount"))
                        writer.uint32(/* id 4, wireType 0 =*/32).int32(message.onDemandMsgCount);
                    if (message.oldestMsgTimestampMs != null && Object.hasOwnProperty.call(message, "oldestMsgTimestampMs"))
                        writer.uint32(/* id 5, wireType 0 =*/40).int64(message.oldestMsgTimestampMs);
                    if (message.accountLid != null && Object.hasOwnProperty.call(message, "accountLid"))
                        writer.uint32(/* id 6, wireType 2 =*/50).string(message.accountLid);
                    if (message.supportInlineResponse != null && Object.hasOwnProperty.call(message, "supportInlineResponse"))
                        writer.uint32(/* id 7, wireType 0 =*/56).bool(message.supportInlineResponse);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified HistorySyncOnDemandRequest message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IHistorySyncOnDemandRequest} message HistorySyncOnDemandRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                HistorySyncOnDemandRequest.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a HistorySyncOnDemandRequest message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest} HistorySyncOnDemandRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                HistorySyncOnDemandRequest.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.chatJid = reader.stringVerify();
                                message._chatJid = "chatJid";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.oldestMsgId = reader.stringVerify();
                                message._oldestMsgId = "oldestMsgId";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                message.oldestMsgFromMe = reader.bool();
                                message._oldestMsgFromMe = "oldestMsgFromMe";
                                continue;
                            }
                        case 4: {
                                if (u !== 0)
                                    break;
                                message.onDemandMsgCount = reader.int32();
                                message._onDemandMsgCount = "onDemandMsgCount";
                                continue;
                            }
                        case 5: {
                                if (u !== 0)
                                    break;
                                message.oldestMsgTimestampMs = reader.int64();
                                message._oldestMsgTimestampMs = "oldestMsgTimestampMs";
                                continue;
                            }
                        case 6: {
                                if (u !== 2)
                                    break;
                                message.accountLid = reader.stringVerify();
                                message._accountLid = "accountLid";
                                continue;
                            }
                        case 7: {
                                if (u !== 0)
                                    break;
                                message.supportInlineResponse = reader.bool();
                                message._supportInlineResponse = "supportInlineResponse";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a HistorySyncOnDemandRequest message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest} HistorySyncOnDemandRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                HistorySyncOnDemandRequest.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a HistorySyncOnDemandRequest message.
                 * @function verify
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                HistorySyncOnDemandRequest.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.chatJid != null && Object.hasOwnProperty.call(message, "chatJid")) {
                        properties._chatJid = 1;
                        if (!$util.isString(message.chatJid))
                            return "chatJid: string expected";
                    }
                    if (message.oldestMsgId != null && Object.hasOwnProperty.call(message, "oldestMsgId")) {
                        properties._oldestMsgId = 1;
                        if (!$util.isString(message.oldestMsgId))
                            return "oldestMsgId: string expected";
                    }
                    if (message.oldestMsgFromMe != null && Object.hasOwnProperty.call(message, "oldestMsgFromMe")) {
                        properties._oldestMsgFromMe = 1;
                        if (typeof message.oldestMsgFromMe !== "boolean")
                            return "oldestMsgFromMe: boolean expected";
                    }
                    if (message.onDemandMsgCount != null && Object.hasOwnProperty.call(message, "onDemandMsgCount")) {
                        properties._onDemandMsgCount = 1;
                        if (!$util.isInteger(message.onDemandMsgCount))
                            return "onDemandMsgCount: integer expected";
                    }
                    if (message.oldestMsgTimestampMs != null && Object.hasOwnProperty.call(message, "oldestMsgTimestampMs")) {
                        properties._oldestMsgTimestampMs = 1;
                        if (!$util.isInteger(message.oldestMsgTimestampMs) && !(message.oldestMsgTimestampMs && $util.isInteger(message.oldestMsgTimestampMs.low) && $util.isInteger(message.oldestMsgTimestampMs.high)))
                            return "oldestMsgTimestampMs: integer|Long expected";
                    }
                    if (message.accountLid != null && Object.hasOwnProperty.call(message, "accountLid")) {
                        properties._accountLid = 1;
                        if (!$util.isString(message.accountLid))
                            return "accountLid: string expected";
                    }
                    if (message.supportInlineResponse != null && Object.hasOwnProperty.call(message, "supportInlineResponse")) {
                        properties._supportInlineResponse = 1;
                        if (typeof message.supportInlineResponse !== "boolean")
                            return "supportInlineResponse: boolean expected";
                    }
                    return null;
                };

                /**
                 * Creates a HistorySyncOnDemandRequest message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest} HistorySyncOnDemandRequest
                 */
                HistorySyncOnDemandRequest.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.chatJid != null)
                        message.chatJid = String(object.chatJid);
                    if (object.oldestMsgId != null)
                        message.oldestMsgId = String(object.oldestMsgId);
                    if (object.oldestMsgFromMe != null)
                        message.oldestMsgFromMe = Boolean(object.oldestMsgFromMe);
                    if (object.onDemandMsgCount != null)
                        message.onDemandMsgCount = object.onDemandMsgCount | 0;
                    if (object.oldestMsgTimestampMs != null)
                        if ($util.Long)
                            message.oldestMsgTimestampMs = $util.Long.fromValue(object.oldestMsgTimestampMs, false);
                        else if (typeof object.oldestMsgTimestampMs === "string")
                            message.oldestMsgTimestampMs = parseInt(object.oldestMsgTimestampMs, 10);
                        else if (typeof object.oldestMsgTimestampMs === "number")
                            message.oldestMsgTimestampMs = object.oldestMsgTimestampMs;
                        else if (typeof object.oldestMsgTimestampMs === "object")
                            message.oldestMsgTimestampMs = new $util.LongBits(object.oldestMsgTimestampMs.low >>> 0, object.oldestMsgTimestampMs.high >>> 0).toNumber();
                    if (object.accountLid != null)
                        message.accountLid = String(object.accountLid);
                    if (object.supportInlineResponse != null)
                        message.supportInlineResponse = Boolean(object.supportInlineResponse);
                    return message;
                };

                /**
                 * Creates a plain object from a HistorySyncOnDemandRequest message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest} message HistorySyncOnDemandRequest
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                HistorySyncOnDemandRequest.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.chatJid != null && Object.hasOwnProperty.call(message, "chatJid"))
                        object.chatJid = message.chatJid;
                    if (message.oldestMsgId != null && Object.hasOwnProperty.call(message, "oldestMsgId"))
                        object.oldestMsgId = message.oldestMsgId;
                    if (message.oldestMsgFromMe != null && Object.hasOwnProperty.call(message, "oldestMsgFromMe"))
                        object.oldestMsgFromMe = message.oldestMsgFromMe;
                    if (message.onDemandMsgCount != null && Object.hasOwnProperty.call(message, "onDemandMsgCount"))
                        object.onDemandMsgCount = message.onDemandMsgCount;
                    if (message.oldestMsgTimestampMs != null && Object.hasOwnProperty.call(message, "oldestMsgTimestampMs"))
                        if (typeof BigInt !== "undefined" && options.longs === BigInt)
                            object.oldestMsgTimestampMs = typeof message.oldestMsgTimestampMs === "number" ? BigInt(message.oldestMsgTimestampMs) : $util.Long.fromBits(message.oldestMsgTimestampMs.low >>> 0, message.oldestMsgTimestampMs.high >>> 0, false).toBigInt();
                        else if (typeof message.oldestMsgTimestampMs === "number")
                            object.oldestMsgTimestampMs = options.longs === String ? String(message.oldestMsgTimestampMs) : message.oldestMsgTimestampMs;
                        else
                            object.oldestMsgTimestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.oldestMsgTimestampMs) : options.longs === Number ? new $util.LongBits(message.oldestMsgTimestampMs.low >>> 0, message.oldestMsgTimestampMs.high >>> 0).toNumber() : message.oldestMsgTimestampMs;
                    if (message.accountLid != null && Object.hasOwnProperty.call(message, "accountLid"))
                        object.accountLid = message.accountLid;
                    if (message.supportInlineResponse != null && Object.hasOwnProperty.call(message, "supportInlineResponse"))
                        object.supportInlineResponse = message.supportInlineResponse;
                    return object;
                };

                /**
                 * Converts this HistorySyncOnDemandRequest to JSON.
                 * @function toJSON
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                HistorySyncOnDemandRequest.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for HistorySyncOnDemandRequest
                 * @function getTypeUrl
                 * @memberof proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                HistorySyncOnDemandRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest";
                };

                return HistorySyncOnDemandRequest;
            })();

            PeerDataOperationRequestMessage.PlaceholderMessageResendRequest = (function() {

                /**
                 * Properties of a PlaceholderMessageResendRequest.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @interface IPlaceholderMessageResendRequest
                 * @property {proto.IMessageKey|null} [messageKey] PlaceholderMessageResendRequest messageKey
                 */

                /**
                 * Constructs a new PlaceholderMessageResendRequest.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @classdesc Represents a PlaceholderMessageResendRequest.
                 * @implements IPlaceholderMessageResendRequest
                 * @constructor
                 * @param {proto.Message.PeerDataOperationRequestMessage.IPlaceholderMessageResendRequest=} [properties] Properties to set
                 */
                function PlaceholderMessageResendRequest(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * PlaceholderMessageResendRequest messageKey.
                 * @member {proto.IMessageKey|null|undefined} messageKey
                 * @memberof proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest
                 * @instance
                 */
                PlaceholderMessageResendRequest.prototype.messageKey = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PlaceholderMessageResendRequest.prototype, "_messageKey", {
                    get: $util.oneOfGetter($oneOfFields = ["messageKey"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new PlaceholderMessageResendRequest instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IPlaceholderMessageResendRequest=} [properties] Properties to set
                 * @returns {proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest} PlaceholderMessageResendRequest instance
                 */
                PlaceholderMessageResendRequest.create = function create(properties) {
                    return new PlaceholderMessageResendRequest(properties);
                };

                /**
                 * Encodes the specified PlaceholderMessageResendRequest message. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IPlaceholderMessageResendRequest} message PlaceholderMessageResendRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PlaceholderMessageResendRequest.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.messageKey != null && Object.hasOwnProperty.call(message, "messageKey"))
                        $root.proto.MessageKey.encode(message.messageKey, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified PlaceholderMessageResendRequest message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IPlaceholderMessageResendRequest} message PlaceholderMessageResendRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PlaceholderMessageResendRequest.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a PlaceholderMessageResendRequest message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest} PlaceholderMessageResendRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PlaceholderMessageResendRequest.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.messageKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.messageKey);
                                message._messageKey = "messageKey";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a PlaceholderMessageResendRequest message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest} PlaceholderMessageResendRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PlaceholderMessageResendRequest.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a PlaceholderMessageResendRequest message.
                 * @function verify
                 * @memberof proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                PlaceholderMessageResendRequest.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.messageKey != null && Object.hasOwnProperty.call(message, "messageKey")) {
                        properties._messageKey = 1;
                        {
                            var error = $root.proto.MessageKey.verify(message.messageKey, q + 1);
                            if (error)
                                return "messageKey." + error;
                        }
                    }
                    return null;
                };

                /**
                 * Creates a PlaceholderMessageResendRequest message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest} PlaceholderMessageResendRequest
                 */
                PlaceholderMessageResendRequest.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.messageKey != null) {
                        if (!$util.isObject(object.messageKey))
                            throw TypeError(".proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest.messageKey: object expected");
                        message.messageKey = $root.proto.MessageKey.fromObject(object.messageKey, q + 1);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a PlaceholderMessageResendRequest message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest} message PlaceholderMessageResendRequest
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                PlaceholderMessageResendRequest.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.messageKey != null && Object.hasOwnProperty.call(message, "messageKey"))
                        object.messageKey = $root.proto.MessageKey.toObject(message.messageKey, options, q + 1);
                    return object;
                };

                /**
                 * Converts this PlaceholderMessageResendRequest to JSON.
                 * @function toJSON
                 * @memberof proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                PlaceholderMessageResendRequest.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for PlaceholderMessageResendRequest
                 * @function getTypeUrl
                 * @memberof proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                PlaceholderMessageResendRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest";
                };

                return PlaceholderMessageResendRequest;
            })();

            PeerDataOperationRequestMessage.RequestStickerReupload = (function() {

                /**
                 * Properties of a RequestStickerReupload.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @interface IRequestStickerReupload
                 * @property {string|null} [fileSha256] RequestStickerReupload fileSha256
                 */

                /**
                 * Constructs a new RequestStickerReupload.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @classdesc Represents a RequestStickerReupload.
                 * @implements IRequestStickerReupload
                 * @constructor
                 * @param {proto.Message.PeerDataOperationRequestMessage.IRequestStickerReupload=} [properties] Properties to set
                 */
                function RequestStickerReupload(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * RequestStickerReupload fileSha256.
                 * @member {string|null|undefined} fileSha256
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload
                 * @instance
                 */
                RequestStickerReupload.prototype.fileSha256 = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(RequestStickerReupload.prototype, "_fileSha256", {
                    get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new RequestStickerReupload instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IRequestStickerReupload=} [properties] Properties to set
                 * @returns {proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload} RequestStickerReupload instance
                 */
                RequestStickerReupload.create = function create(properties) {
                    return new RequestStickerReupload(properties);
                };

                /**
                 * Encodes the specified RequestStickerReupload message. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IRequestStickerReupload} message RequestStickerReupload message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                RequestStickerReupload.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.fileSha256);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified RequestStickerReupload message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IRequestStickerReupload} message RequestStickerReupload message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                RequestStickerReupload.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a RequestStickerReupload message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload} RequestStickerReupload
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                RequestStickerReupload.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.fileSha256 = reader.stringVerify();
                                message._fileSha256 = "fileSha256";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a RequestStickerReupload message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload} RequestStickerReupload
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                RequestStickerReupload.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a RequestStickerReupload message.
                 * @function verify
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                RequestStickerReupload.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256")) {
                        properties._fileSha256 = 1;
                        if (!$util.isString(message.fileSha256))
                            return "fileSha256: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a RequestStickerReupload message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload} RequestStickerReupload
                 */
                RequestStickerReupload.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.fileSha256 != null)
                        message.fileSha256 = String(object.fileSha256);
                    return message;
                };

                /**
                 * Creates a plain object from a RequestStickerReupload message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload} message RequestStickerReupload
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                RequestStickerReupload.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                        object.fileSha256 = message.fileSha256;
                    return object;
                };

                /**
                 * Converts this RequestStickerReupload to JSON.
                 * @function toJSON
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                RequestStickerReupload.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for RequestStickerReupload
                 * @function getTypeUrl
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                RequestStickerReupload.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload";
                };

                return RequestStickerReupload;
            })();

            PeerDataOperationRequestMessage.RequestUrlPreview = (function() {

                /**
                 * Properties of a RequestUrlPreview.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @interface IRequestUrlPreview
                 * @property {string|null} [url] RequestUrlPreview url
                 * @property {boolean|null} [includeHqThumbnail] RequestUrlPreview includeHqThumbnail
                 */

                /**
                 * Constructs a new RequestUrlPreview.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @classdesc Represents a RequestUrlPreview.
                 * @implements IRequestUrlPreview
                 * @constructor
                 * @param {proto.Message.PeerDataOperationRequestMessage.IRequestUrlPreview=} [properties] Properties to set
                 */
                function RequestUrlPreview(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * RequestUrlPreview url.
                 * @member {string|null|undefined} url
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview
                 * @instance
                 */
                RequestUrlPreview.prototype.url = null;

                /**
                 * RequestUrlPreview includeHqThumbnail.
                 * @member {boolean|null|undefined} includeHqThumbnail
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview
                 * @instance
                 */
                RequestUrlPreview.prototype.includeHqThumbnail = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(RequestUrlPreview.prototype, "_url", {
                    get: $util.oneOfGetter($oneOfFields = ["url"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(RequestUrlPreview.prototype, "_includeHqThumbnail", {
                    get: $util.oneOfGetter($oneOfFields = ["includeHqThumbnail"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new RequestUrlPreview instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IRequestUrlPreview=} [properties] Properties to set
                 * @returns {proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview} RequestUrlPreview instance
                 */
                RequestUrlPreview.create = function create(properties) {
                    return new RequestUrlPreview(properties);
                };

                /**
                 * Encodes the specified RequestUrlPreview message. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IRequestUrlPreview} message RequestUrlPreview message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                RequestUrlPreview.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.url);
                    if (message.includeHqThumbnail != null && Object.hasOwnProperty.call(message, "includeHqThumbnail"))
                        writer.uint32(/* id 2, wireType 0 =*/16).bool(message.includeHqThumbnail);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified RequestUrlPreview message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.IRequestUrlPreview} message RequestUrlPreview message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                RequestUrlPreview.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a RequestUrlPreview message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview} RequestUrlPreview
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                RequestUrlPreview.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.url = reader.stringVerify();
                                message._url = "url";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                message.includeHqThumbnail = reader.bool();
                                message._includeHqThumbnail = "includeHqThumbnail";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a RequestUrlPreview message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview} RequestUrlPreview
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                RequestUrlPreview.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a RequestUrlPreview message.
                 * @function verify
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                RequestUrlPreview.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.url != null && Object.hasOwnProperty.call(message, "url")) {
                        properties._url = 1;
                        if (!$util.isString(message.url))
                            return "url: string expected";
                    }
                    if (message.includeHqThumbnail != null && Object.hasOwnProperty.call(message, "includeHqThumbnail")) {
                        properties._includeHqThumbnail = 1;
                        if (typeof message.includeHqThumbnail !== "boolean")
                            return "includeHqThumbnail: boolean expected";
                    }
                    return null;
                };

                /**
                 * Creates a RequestUrlPreview message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview} RequestUrlPreview
                 */
                RequestUrlPreview.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.url != null)
                        message.url = String(object.url);
                    if (object.includeHqThumbnail != null)
                        message.includeHqThumbnail = Boolean(object.includeHqThumbnail);
                    return message;
                };

                /**
                 * Creates a plain object from a RequestUrlPreview message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview} message RequestUrlPreview
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                RequestUrlPreview.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                        object.url = message.url;
                    if (message.includeHqThumbnail != null && Object.hasOwnProperty.call(message, "includeHqThumbnail"))
                        object.includeHqThumbnail = message.includeHqThumbnail;
                    return object;
                };

                /**
                 * Converts this RequestUrlPreview to JSON.
                 * @function toJSON
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                RequestUrlPreview.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for RequestUrlPreview
                 * @function getTypeUrl
                 * @memberof proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                RequestUrlPreview.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview";
                };

                return RequestUrlPreview;
            })();

            PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest = (function() {

                /**
                 * Properties of a SyncDCollectionFatalRecoveryRequest.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @interface ISyncDCollectionFatalRecoveryRequest
                 * @property {string|null} [collectionName] SyncDCollectionFatalRecoveryRequest collectionName
                 * @property {number|Long|null} [timestamp] SyncDCollectionFatalRecoveryRequest timestamp
                 */

                /**
                 * Constructs a new SyncDCollectionFatalRecoveryRequest.
                 * @memberof proto.Message.PeerDataOperationRequestMessage
                 * @classdesc Represents a SyncDCollectionFatalRecoveryRequest.
                 * @implements ISyncDCollectionFatalRecoveryRequest
                 * @constructor
                 * @param {proto.Message.PeerDataOperationRequestMessage.ISyncDCollectionFatalRecoveryRequest=} [properties] Properties to set
                 */
                function SyncDCollectionFatalRecoveryRequest(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * SyncDCollectionFatalRecoveryRequest collectionName.
                 * @member {string|null|undefined} collectionName
                 * @memberof proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest
                 * @instance
                 */
                SyncDCollectionFatalRecoveryRequest.prototype.collectionName = null;

                /**
                 * SyncDCollectionFatalRecoveryRequest timestamp.
                 * @member {number|Long|null|undefined} timestamp
                 * @memberof proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest
                 * @instance
                 */
                SyncDCollectionFatalRecoveryRequest.prototype.timestamp = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SyncDCollectionFatalRecoveryRequest.prototype, "_collectionName", {
                    get: $util.oneOfGetter($oneOfFields = ["collectionName"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SyncDCollectionFatalRecoveryRequest.prototype, "_timestamp", {
                    get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new SyncDCollectionFatalRecoveryRequest instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.ISyncDCollectionFatalRecoveryRequest=} [properties] Properties to set
                 * @returns {proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest} SyncDCollectionFatalRecoveryRequest instance
                 */
                SyncDCollectionFatalRecoveryRequest.create = function create(properties) {
                    return new SyncDCollectionFatalRecoveryRequest(properties);
                };

                /**
                 * Encodes the specified SyncDCollectionFatalRecoveryRequest message. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.ISyncDCollectionFatalRecoveryRequest} message SyncDCollectionFatalRecoveryRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                SyncDCollectionFatalRecoveryRequest.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.collectionName != null && Object.hasOwnProperty.call(message, "collectionName"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.collectionName);
                    if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                        writer.uint32(/* id 2, wireType 0 =*/16).int64(message.timestamp);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified SyncDCollectionFatalRecoveryRequest message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.ISyncDCollectionFatalRecoveryRequest} message SyncDCollectionFatalRecoveryRequest message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                SyncDCollectionFatalRecoveryRequest.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a SyncDCollectionFatalRecoveryRequest message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest} SyncDCollectionFatalRecoveryRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                SyncDCollectionFatalRecoveryRequest.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.collectionName = reader.stringVerify();
                                message._collectionName = "collectionName";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                message.timestamp = reader.int64();
                                message._timestamp = "timestamp";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a SyncDCollectionFatalRecoveryRequest message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest} SyncDCollectionFatalRecoveryRequest
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                SyncDCollectionFatalRecoveryRequest.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a SyncDCollectionFatalRecoveryRequest message.
                 * @function verify
                 * @memberof proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                SyncDCollectionFatalRecoveryRequest.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.collectionName != null && Object.hasOwnProperty.call(message, "collectionName")) {
                        properties._collectionName = 1;
                        if (!$util.isString(message.collectionName))
                            return "collectionName: string expected";
                    }
                    if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp")) {
                        properties._timestamp = 1;
                        if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                            return "timestamp: integer|Long expected";
                    }
                    return null;
                };

                /**
                 * Creates a SyncDCollectionFatalRecoveryRequest message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest} SyncDCollectionFatalRecoveryRequest
                 */
                SyncDCollectionFatalRecoveryRequest.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.collectionName != null)
                        message.collectionName = String(object.collectionName);
                    if (object.timestamp != null)
                        if ($util.Long)
                            message.timestamp = $util.Long.fromValue(object.timestamp, false);
                        else if (typeof object.timestamp === "string")
                            message.timestamp = parseInt(object.timestamp, 10);
                        else if (typeof object.timestamp === "number")
                            message.timestamp = object.timestamp;
                        else if (typeof object.timestamp === "object")
                            message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
                    return message;
                };

                /**
                 * Creates a plain object from a SyncDCollectionFatalRecoveryRequest message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest} message SyncDCollectionFatalRecoveryRequest
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                SyncDCollectionFatalRecoveryRequest.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.collectionName != null && Object.hasOwnProperty.call(message, "collectionName"))
                        object.collectionName = message.collectionName;
                    if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                        if (typeof BigInt !== "undefined" && options.longs === BigInt)
                            object.timestamp = typeof message.timestamp === "number" ? BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
                        else if (typeof message.timestamp === "number")
                            object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                        else
                            object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
                    return object;
                };

                /**
                 * Converts this SyncDCollectionFatalRecoveryRequest to JSON.
                 * @function toJSON
                 * @memberof proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                SyncDCollectionFatalRecoveryRequest.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for SyncDCollectionFatalRecoveryRequest
                 * @function getTypeUrl
                 * @memberof proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                SyncDCollectionFatalRecoveryRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest";
                };

                return SyncDCollectionFatalRecoveryRequest;
            })();

            return PeerDataOperationRequestMessage;
        })();

        Message.PeerDataOperationRequestResponseMessage = (function() {

            /**
             * Properties of a PeerDataOperationRequestResponseMessage.
             * @memberof proto.Message
             * @interface IPeerDataOperationRequestResponseMessage
             * @property {proto.Message.PeerDataOperationRequestType|null} [peerDataOperationRequestType] PeerDataOperationRequestResponseMessage peerDataOperationRequestType
             * @property {string|null} [stanzaId] PeerDataOperationRequestResponseMessage stanzaId
             * @property {Array.<proto.Message.PeerDataOperationRequestResponseMessage.IPeerDataOperationResult>|null} [peerDataOperationResult] PeerDataOperationRequestResponseMessage peerDataOperationResult
             */

            /**
             * Constructs a new PeerDataOperationRequestResponseMessage.
             * @memberof proto.Message
             * @classdesc Represents a PeerDataOperationRequestResponseMessage.
             * @implements IPeerDataOperationRequestResponseMessage
             * @constructor
             * @param {proto.Message.IPeerDataOperationRequestResponseMessage=} [properties] Properties to set
             */
            function PeerDataOperationRequestResponseMessage(properties) {
                this.peerDataOperationResult = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PeerDataOperationRequestResponseMessage peerDataOperationRequestType.
             * @member {proto.Message.PeerDataOperationRequestType|null|undefined} peerDataOperationRequestType
             * @memberof proto.Message.PeerDataOperationRequestResponseMessage
             * @instance
             */
            PeerDataOperationRequestResponseMessage.prototype.peerDataOperationRequestType = null;

            /**
             * PeerDataOperationRequestResponseMessage stanzaId.
             * @member {string|null|undefined} stanzaId
             * @memberof proto.Message.PeerDataOperationRequestResponseMessage
             * @instance
             */
            PeerDataOperationRequestResponseMessage.prototype.stanzaId = null;

            /**
             * PeerDataOperationRequestResponseMessage peerDataOperationResult.
             * @member {Array.<proto.Message.PeerDataOperationRequestResponseMessage.IPeerDataOperationResult>} peerDataOperationResult
             * @memberof proto.Message.PeerDataOperationRequestResponseMessage
             * @instance
             */
            PeerDataOperationRequestResponseMessage.prototype.peerDataOperationResult = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationRequestResponseMessage.prototype, "_peerDataOperationRequestType", {
                get: $util.oneOfGetter($oneOfFields = ["peerDataOperationRequestType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationRequestResponseMessage.prototype, "_stanzaId", {
                get: $util.oneOfGetter($oneOfFields = ["stanzaId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PeerDataOperationRequestResponseMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.PeerDataOperationRequestResponseMessage
             * @static
             * @param {proto.Message.IPeerDataOperationRequestResponseMessage=} [properties] Properties to set
             * @returns {proto.Message.PeerDataOperationRequestResponseMessage} PeerDataOperationRequestResponseMessage instance
             */
            PeerDataOperationRequestResponseMessage.create = function create(properties) {
                return new PeerDataOperationRequestResponseMessage(properties);
            };

            /**
             * Encodes the specified PeerDataOperationRequestResponseMessage message. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.PeerDataOperationRequestResponseMessage
             * @static
             * @param {proto.Message.IPeerDataOperationRequestResponseMessage} message PeerDataOperationRequestResponseMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PeerDataOperationRequestResponseMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.peerDataOperationRequestType != null && Object.hasOwnProperty.call(message, "peerDataOperationRequestType"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.peerDataOperationRequestType);
                if (message.stanzaId != null && Object.hasOwnProperty.call(message, "stanzaId"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.stanzaId);
                if (message.peerDataOperationResult != null && message.peerDataOperationResult.length)
                    for (var i = 0; i < message.peerDataOperationResult.length; ++i)
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.encode(message.peerDataOperationResult[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PeerDataOperationRequestResponseMessage message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.PeerDataOperationRequestResponseMessage
             * @static
             * @param {proto.Message.IPeerDataOperationRequestResponseMessage} message PeerDataOperationRequestResponseMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PeerDataOperationRequestResponseMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PeerDataOperationRequestResponseMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.PeerDataOperationRequestResponseMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.PeerDataOperationRequestResponseMessage} PeerDataOperationRequestResponseMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PeerDataOperationRequestResponseMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.peerDataOperationRequestType = reader.int32();
                            message._peerDataOperationRequestType = "peerDataOperationRequestType";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.stanzaId = reader.stringVerify();
                            message._stanzaId = "stanzaId";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            if (!(message.peerDataOperationResult && message.peerDataOperationResult.length))
                                message.peerDataOperationResult = [];
                            message.peerDataOperationResult.push($root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PeerDataOperationRequestResponseMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.PeerDataOperationRequestResponseMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.PeerDataOperationRequestResponseMessage} PeerDataOperationRequestResponseMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PeerDataOperationRequestResponseMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PeerDataOperationRequestResponseMessage message.
             * @function verify
             * @memberof proto.Message.PeerDataOperationRequestResponseMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PeerDataOperationRequestResponseMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.peerDataOperationRequestType != null && Object.hasOwnProperty.call(message, "peerDataOperationRequestType")) {
                    properties._peerDataOperationRequestType = 1;
                    if (typeof message.peerDataOperationRequestType !== "number" || (message.peerDataOperationRequestType | 0) !== message.peerDataOperationRequestType)
                        return "peerDataOperationRequestType: enum value expected";
                }
                if (message.stanzaId != null && Object.hasOwnProperty.call(message, "stanzaId")) {
                    properties._stanzaId = 1;
                    if (!$util.isString(message.stanzaId))
                        return "stanzaId: string expected";
                }
                if (message.peerDataOperationResult != null && Object.hasOwnProperty.call(message, "peerDataOperationResult")) {
                    if (!Array.isArray(message.peerDataOperationResult))
                        return "peerDataOperationResult: array expected";
                    for (var i = 0; i < message.peerDataOperationResult.length; ++i) {
                        var error = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.verify(message.peerDataOperationResult[i], q + 1);
                        if (error)
                            return "peerDataOperationResult." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a PeerDataOperationRequestResponseMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.PeerDataOperationRequestResponseMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.PeerDataOperationRequestResponseMessage} PeerDataOperationRequestResponseMessage
             */
            PeerDataOperationRequestResponseMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.peerDataOperationRequestType) {
                case "UPLOAD_STICKER":
                case 0:
                    message.peerDataOperationRequestType = 0;
                    break;
                case "SEND_RECENT_STICKER_BOOTSTRAP":
                case 1:
                    message.peerDataOperationRequestType = 1;
                    break;
                case "GENERATE_LINK_PREVIEW":
                case 2:
                    message.peerDataOperationRequestType = 2;
                    break;
                case "HISTORY_SYNC_ON_DEMAND":
                case 3:
                    message.peerDataOperationRequestType = 3;
                    break;
                case "PLACEHOLDER_MESSAGE_RESEND":
                case 4:
                    message.peerDataOperationRequestType = 4;
                    break;
                case "WAFFLE_LINKING_NONCE_FETCH":
                case 5:
                    message.peerDataOperationRequestType = 5;
                    break;
                case "FULL_HISTORY_SYNC_ON_DEMAND":
                case 6:
                    message.peerDataOperationRequestType = 6;
                    break;
                case "COMPANION_META_NONCE_FETCH":
                case 7:
                    message.peerDataOperationRequestType = 7;
                    break;
                case "COMPANION_SYNCD_SNAPSHOT_FATAL_RECOVERY":
                case 8:
                    message.peerDataOperationRequestType = 8;
                    break;
                case "COMPANION_CANONICAL_USER_NONCE_FETCH":
                case 9:
                    message.peerDataOperationRequestType = 9;
                    break;
                case "HISTORY_SYNC_CHUNK_RETRY":
                case 10:
                    message.peerDataOperationRequestType = 10;
                    break;
                case "GALAXY_FLOW_ACTION":
                case 11:
                    message.peerDataOperationRequestType = 11;
                    break;
                case "BUSINESS_BROADCAST_INSIGHTS_DELIVERED_TO":
                case 12:
                    message.peerDataOperationRequestType = 12;
                    break;
                case "BUSINESS_BROADCAST_INSIGHTS_REFRESH":
                case 13:
                    message.peerDataOperationRequestType = 13;
                    break;
                default:
                    if (typeof object.peerDataOperationRequestType === "number" && (object.peerDataOperationRequestType | 0) === object.peerDataOperationRequestType)
                        message.peerDataOperationRequestType = object.peerDataOperationRequestType;
                }
                if (object.stanzaId != null)
                    message.stanzaId = String(object.stanzaId);
                if (object.peerDataOperationResult) {
                    if (!Array.isArray(object.peerDataOperationResult))
                        throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.peerDataOperationResult: array expected");
                    message.peerDataOperationResult = Array(object.peerDataOperationResult.length);
                    for (var i = 0; i < object.peerDataOperationResult.length; ++i) {
                        if (!$util.isObject(object.peerDataOperationResult[i]))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.peerDataOperationResult: object expected");
                        message.peerDataOperationResult[i] = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.fromObject(object.peerDataOperationResult[i], q + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a PeerDataOperationRequestResponseMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.PeerDataOperationRequestResponseMessage
             * @static
             * @param {proto.Message.PeerDataOperationRequestResponseMessage} message PeerDataOperationRequestResponseMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PeerDataOperationRequestResponseMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.peerDataOperationResult = [];
                if (message.peerDataOperationRequestType != null && Object.hasOwnProperty.call(message, "peerDataOperationRequestType"))
                    object.peerDataOperationRequestType = options.enums === String ? $root.proto.Message.PeerDataOperationRequestType[message.peerDataOperationRequestType] === undefined ? message.peerDataOperationRequestType : $root.proto.Message.PeerDataOperationRequestType[message.peerDataOperationRequestType] : message.peerDataOperationRequestType;
                if (message.stanzaId != null && Object.hasOwnProperty.call(message, "stanzaId"))
                    object.stanzaId = message.stanzaId;
                if (message.peerDataOperationResult && message.peerDataOperationResult.length) {
                    object.peerDataOperationResult = Array(message.peerDataOperationResult.length);
                    for (var j = 0; j < message.peerDataOperationResult.length; ++j)
                        object.peerDataOperationResult[j] = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.toObject(message.peerDataOperationResult[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this PeerDataOperationRequestResponseMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.PeerDataOperationRequestResponseMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PeerDataOperationRequestResponseMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PeerDataOperationRequestResponseMessage
             * @function getTypeUrl
             * @memberof proto.Message.PeerDataOperationRequestResponseMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PeerDataOperationRequestResponseMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestResponseMessage";
            };

            PeerDataOperationRequestResponseMessage.PeerDataOperationResult = (function() {

                /**
                 * Properties of a PeerDataOperationResult.
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage
                 * @interface IPeerDataOperationResult
                 * @property {proto.MediaRetryNotification.ResultType|null} [mediaUploadResult] PeerDataOperationResult mediaUploadResult
                 * @property {proto.Message.IStickerMessage|null} [stickerMessage] PeerDataOperationResult stickerMessage
                 * @property {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ILinkPreviewResponse|null} [linkPreviewResponse] PeerDataOperationResult linkPreviewResponse
                 * @property {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IPlaceholderMessageResendResponse|null} [placeholderMessageResendResponse] PeerDataOperationResult placeholderMessageResendResponse
                 * @property {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IWaffleNonceFetchResponse|null} [waffleNonceFetchRequestResponse] PeerDataOperationResult waffleNonceFetchRequestResponse
                 * @property {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IFullHistorySyncOnDemandRequestResponse|null} [fullHistorySyncOnDemandRequestResponse] PeerDataOperationResult fullHistorySyncOnDemandRequestResponse
                 * @property {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ICompanionMetaNonceFetchResponse|null} [companionMetaNonceFetchRequestResponse] PeerDataOperationResult companionMetaNonceFetchRequestResponse
                 * @property {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ISyncDSnapshotFatalRecoveryResponse|null} [syncdSnapshotFatalRecoveryResponse] PeerDataOperationResult syncdSnapshotFatalRecoveryResponse
                 * @property {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ICompanionCanonicalUserNonceFetchResponse|null} [companionCanonicalUserNonceFetchRequestResponse] PeerDataOperationResult companionCanonicalUserNonceFetchRequestResponse
                 * @property {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IHistorySyncChunkRetryResponse|null} [historySyncChunkRetryResponse] PeerDataOperationResult historySyncChunkRetryResponse
                 * @property {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IFlowResponsesCsvBundle|null} [flowResponsesCsvBundle] PeerDataOperationResult flowResponsesCsvBundle
                 * @property {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IBizBroadcastInsightsContactListResponse|null} [bizBroadcastInsightsContactListResponse] PeerDataOperationResult bizBroadcastInsightsContactListResponse
                 */

                /**
                 * Constructs a new PeerDataOperationResult.
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage
                 * @classdesc Represents a PeerDataOperationResult.
                 * @implements IPeerDataOperationResult
                 * @constructor
                 * @param {proto.Message.PeerDataOperationRequestResponseMessage.IPeerDataOperationResult=} [properties] Properties to set
                 */
                function PeerDataOperationResult(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * PeerDataOperationResult mediaUploadResult.
                 * @member {proto.MediaRetryNotification.ResultType|null|undefined} mediaUploadResult
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @instance
                 */
                PeerDataOperationResult.prototype.mediaUploadResult = null;

                /**
                 * PeerDataOperationResult stickerMessage.
                 * @member {proto.Message.IStickerMessage|null|undefined} stickerMessage
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @instance
                 */
                PeerDataOperationResult.prototype.stickerMessage = null;

                /**
                 * PeerDataOperationResult linkPreviewResponse.
                 * @member {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ILinkPreviewResponse|null|undefined} linkPreviewResponse
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @instance
                 */
                PeerDataOperationResult.prototype.linkPreviewResponse = null;

                /**
                 * PeerDataOperationResult placeholderMessageResendResponse.
                 * @member {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IPlaceholderMessageResendResponse|null|undefined} placeholderMessageResendResponse
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @instance
                 */
                PeerDataOperationResult.prototype.placeholderMessageResendResponse = null;

                /**
                 * PeerDataOperationResult waffleNonceFetchRequestResponse.
                 * @member {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IWaffleNonceFetchResponse|null|undefined} waffleNonceFetchRequestResponse
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @instance
                 */
                PeerDataOperationResult.prototype.waffleNonceFetchRequestResponse = null;

                /**
                 * PeerDataOperationResult fullHistorySyncOnDemandRequestResponse.
                 * @member {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IFullHistorySyncOnDemandRequestResponse|null|undefined} fullHistorySyncOnDemandRequestResponse
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @instance
                 */
                PeerDataOperationResult.prototype.fullHistorySyncOnDemandRequestResponse = null;

                /**
                 * PeerDataOperationResult companionMetaNonceFetchRequestResponse.
                 * @member {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ICompanionMetaNonceFetchResponse|null|undefined} companionMetaNonceFetchRequestResponse
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @instance
                 */
                PeerDataOperationResult.prototype.companionMetaNonceFetchRequestResponse = null;

                /**
                 * PeerDataOperationResult syncdSnapshotFatalRecoveryResponse.
                 * @member {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ISyncDSnapshotFatalRecoveryResponse|null|undefined} syncdSnapshotFatalRecoveryResponse
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @instance
                 */
                PeerDataOperationResult.prototype.syncdSnapshotFatalRecoveryResponse = null;

                /**
                 * PeerDataOperationResult companionCanonicalUserNonceFetchRequestResponse.
                 * @member {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ICompanionCanonicalUserNonceFetchResponse|null|undefined} companionCanonicalUserNonceFetchRequestResponse
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @instance
                 */
                PeerDataOperationResult.prototype.companionCanonicalUserNonceFetchRequestResponse = null;

                /**
                 * PeerDataOperationResult historySyncChunkRetryResponse.
                 * @member {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IHistorySyncChunkRetryResponse|null|undefined} historySyncChunkRetryResponse
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @instance
                 */
                PeerDataOperationResult.prototype.historySyncChunkRetryResponse = null;

                /**
                 * PeerDataOperationResult flowResponsesCsvBundle.
                 * @member {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IFlowResponsesCsvBundle|null|undefined} flowResponsesCsvBundle
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @instance
                 */
                PeerDataOperationResult.prototype.flowResponsesCsvBundle = null;

                /**
                 * PeerDataOperationResult bizBroadcastInsightsContactListResponse.
                 * @member {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IBizBroadcastInsightsContactListResponse|null|undefined} bizBroadcastInsightsContactListResponse
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @instance
                 */
                PeerDataOperationResult.prototype.bizBroadcastInsightsContactListResponse = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PeerDataOperationResult.prototype, "_mediaUploadResult", {
                    get: $util.oneOfGetter($oneOfFields = ["mediaUploadResult"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PeerDataOperationResult.prototype, "_stickerMessage", {
                    get: $util.oneOfGetter($oneOfFields = ["stickerMessage"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PeerDataOperationResult.prototype, "_linkPreviewResponse", {
                    get: $util.oneOfGetter($oneOfFields = ["linkPreviewResponse"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PeerDataOperationResult.prototype, "_placeholderMessageResendResponse", {
                    get: $util.oneOfGetter($oneOfFields = ["placeholderMessageResendResponse"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PeerDataOperationResult.prototype, "_waffleNonceFetchRequestResponse", {
                    get: $util.oneOfGetter($oneOfFields = ["waffleNonceFetchRequestResponse"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PeerDataOperationResult.prototype, "_fullHistorySyncOnDemandRequestResponse", {
                    get: $util.oneOfGetter($oneOfFields = ["fullHistorySyncOnDemandRequestResponse"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PeerDataOperationResult.prototype, "_companionMetaNonceFetchRequestResponse", {
                    get: $util.oneOfGetter($oneOfFields = ["companionMetaNonceFetchRequestResponse"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PeerDataOperationResult.prototype, "_syncdSnapshotFatalRecoveryResponse", {
                    get: $util.oneOfGetter($oneOfFields = ["syncdSnapshotFatalRecoveryResponse"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PeerDataOperationResult.prototype, "_companionCanonicalUserNonceFetchRequestResponse", {
                    get: $util.oneOfGetter($oneOfFields = ["companionCanonicalUserNonceFetchRequestResponse"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PeerDataOperationResult.prototype, "_historySyncChunkRetryResponse", {
                    get: $util.oneOfGetter($oneOfFields = ["historySyncChunkRetryResponse"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PeerDataOperationResult.prototype, "_flowResponsesCsvBundle", {
                    get: $util.oneOfGetter($oneOfFields = ["flowResponsesCsvBundle"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PeerDataOperationResult.prototype, "_bizBroadcastInsightsContactListResponse", {
                    get: $util.oneOfGetter($oneOfFields = ["bizBroadcastInsightsContactListResponse"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new PeerDataOperationResult instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestResponseMessage.IPeerDataOperationResult=} [properties] Properties to set
                 * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult} PeerDataOperationResult instance
                 */
                PeerDataOperationResult.create = function create(properties) {
                    return new PeerDataOperationResult(properties);
                };

                /**
                 * Encodes the specified PeerDataOperationResult message. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestResponseMessage.IPeerDataOperationResult} message PeerDataOperationResult message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PeerDataOperationResult.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.mediaUploadResult != null && Object.hasOwnProperty.call(message, "mediaUploadResult"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int32(message.mediaUploadResult);
                    if (message.stickerMessage != null && Object.hasOwnProperty.call(message, "stickerMessage"))
                        $root.proto.Message.StickerMessage.encode(message.stickerMessage, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                    if (message.linkPreviewResponse != null && Object.hasOwnProperty.call(message, "linkPreviewResponse"))
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.encode(message.linkPreviewResponse, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                    if (message.placeholderMessageResendResponse != null && Object.hasOwnProperty.call(message, "placeholderMessageResendResponse"))
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse.encode(message.placeholderMessageResendResponse, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                    if (message.waffleNonceFetchRequestResponse != null && Object.hasOwnProperty.call(message, "waffleNonceFetchRequestResponse"))
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse.encode(message.waffleNonceFetchRequestResponse, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                    if (message.fullHistorySyncOnDemandRequestResponse != null && Object.hasOwnProperty.call(message, "fullHistorySyncOnDemandRequestResponse"))
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse.encode(message.fullHistorySyncOnDemandRequestResponse, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
                    if (message.companionMetaNonceFetchRequestResponse != null && Object.hasOwnProperty.call(message, "companionMetaNonceFetchRequestResponse"))
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse.encode(message.companionMetaNonceFetchRequestResponse, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
                    if (message.syncdSnapshotFatalRecoveryResponse != null && Object.hasOwnProperty.call(message, "syncdSnapshotFatalRecoveryResponse"))
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse.encode(message.syncdSnapshotFatalRecoveryResponse, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
                    if (message.companionCanonicalUserNonceFetchRequestResponse != null && Object.hasOwnProperty.call(message, "companionCanonicalUserNonceFetchRequestResponse"))
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse.encode(message.companionCanonicalUserNonceFetchRequestResponse, writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
                    if (message.historySyncChunkRetryResponse != null && Object.hasOwnProperty.call(message, "historySyncChunkRetryResponse"))
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse.encode(message.historySyncChunkRetryResponse, writer.uint32(/* id 10, wireType 2 =*/82).fork(), q + 1).ldelim();
                    if (message.flowResponsesCsvBundle != null && Object.hasOwnProperty.call(message, "flowResponsesCsvBundle"))
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle.encode(message.flowResponsesCsvBundle, writer.uint32(/* id 11, wireType 2 =*/90).fork(), q + 1).ldelim();
                    if (message.bizBroadcastInsightsContactListResponse != null && Object.hasOwnProperty.call(message, "bizBroadcastInsightsContactListResponse"))
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse.encode(message.bizBroadcastInsightsContactListResponse, writer.uint32(/* id 12, wireType 2 =*/98).fork(), q + 1).ldelim();
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified PeerDataOperationResult message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestResponseMessage.IPeerDataOperationResult} message PeerDataOperationResult message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PeerDataOperationResult.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a PeerDataOperationResult message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult} PeerDataOperationResult
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PeerDataOperationResult.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                message.mediaUploadResult = reader.int32();
                                message._mediaUploadResult = "mediaUploadResult";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.stickerMessage = $root.proto.Message.StickerMessage.decode(reader, reader.uint32(), undefined, q + 1, message.stickerMessage);
                                message._stickerMessage = "stickerMessage";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.linkPreviewResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.decode(reader, reader.uint32(), undefined, q + 1, message.linkPreviewResponse);
                                message._linkPreviewResponse = "linkPreviewResponse";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                message.placeholderMessageResendResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse.decode(reader, reader.uint32(), undefined, q + 1, message.placeholderMessageResendResponse);
                                message._placeholderMessageResendResponse = "placeholderMessageResendResponse";
                                continue;
                            }
                        case 5: {
                                if (u !== 2)
                                    break;
                                message.waffleNonceFetchRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse.decode(reader, reader.uint32(), undefined, q + 1, message.waffleNonceFetchRequestResponse);
                                message._waffleNonceFetchRequestResponse = "waffleNonceFetchRequestResponse";
                                continue;
                            }
                        case 6: {
                                if (u !== 2)
                                    break;
                                message.fullHistorySyncOnDemandRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse.decode(reader, reader.uint32(), undefined, q + 1, message.fullHistorySyncOnDemandRequestResponse);
                                message._fullHistorySyncOnDemandRequestResponse = "fullHistorySyncOnDemandRequestResponse";
                                continue;
                            }
                        case 7: {
                                if (u !== 2)
                                    break;
                                message.companionMetaNonceFetchRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse.decode(reader, reader.uint32(), undefined, q + 1, message.companionMetaNonceFetchRequestResponse);
                                message._companionMetaNonceFetchRequestResponse = "companionMetaNonceFetchRequestResponse";
                                continue;
                            }
                        case 8: {
                                if (u !== 2)
                                    break;
                                message.syncdSnapshotFatalRecoveryResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse.decode(reader, reader.uint32(), undefined, q + 1, message.syncdSnapshotFatalRecoveryResponse);
                                message._syncdSnapshotFatalRecoveryResponse = "syncdSnapshotFatalRecoveryResponse";
                                continue;
                            }
                        case 9: {
                                if (u !== 2)
                                    break;
                                message.companionCanonicalUserNonceFetchRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse.decode(reader, reader.uint32(), undefined, q + 1, message.companionCanonicalUserNonceFetchRequestResponse);
                                message._companionCanonicalUserNonceFetchRequestResponse = "companionCanonicalUserNonceFetchRequestResponse";
                                continue;
                            }
                        case 10: {
                                if (u !== 2)
                                    break;
                                message.historySyncChunkRetryResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse.decode(reader, reader.uint32(), undefined, q + 1, message.historySyncChunkRetryResponse);
                                message._historySyncChunkRetryResponse = "historySyncChunkRetryResponse";
                                continue;
                            }
                        case 11: {
                                if (u !== 2)
                                    break;
                                message.flowResponsesCsvBundle = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle.decode(reader, reader.uint32(), undefined, q + 1, message.flowResponsesCsvBundle);
                                message._flowResponsesCsvBundle = "flowResponsesCsvBundle";
                                continue;
                            }
                        case 12: {
                                if (u !== 2)
                                    break;
                                message.bizBroadcastInsightsContactListResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse.decode(reader, reader.uint32(), undefined, q + 1, message.bizBroadcastInsightsContactListResponse);
                                message._bizBroadcastInsightsContactListResponse = "bizBroadcastInsightsContactListResponse";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a PeerDataOperationResult message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult} PeerDataOperationResult
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PeerDataOperationResult.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a PeerDataOperationResult message.
                 * @function verify
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                PeerDataOperationResult.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.mediaUploadResult != null && Object.hasOwnProperty.call(message, "mediaUploadResult")) {
                        properties._mediaUploadResult = 1;
                        if (typeof message.mediaUploadResult !== "number" || (message.mediaUploadResult | 0) !== message.mediaUploadResult)
                            return "mediaUploadResult: enum value expected";
                    }
                    if (message.stickerMessage != null && Object.hasOwnProperty.call(message, "stickerMessage")) {
                        properties._stickerMessage = 1;
                        {
                            var error = $root.proto.Message.StickerMessage.verify(message.stickerMessage, q + 1);
                            if (error)
                                return "stickerMessage." + error;
                        }
                    }
                    if (message.linkPreviewResponse != null && Object.hasOwnProperty.call(message, "linkPreviewResponse")) {
                        properties._linkPreviewResponse = 1;
                        {
                            var error = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.verify(message.linkPreviewResponse, q + 1);
                            if (error)
                                return "linkPreviewResponse." + error;
                        }
                    }
                    if (message.placeholderMessageResendResponse != null && Object.hasOwnProperty.call(message, "placeholderMessageResendResponse")) {
                        properties._placeholderMessageResendResponse = 1;
                        {
                            var error = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse.verify(message.placeholderMessageResendResponse, q + 1);
                            if (error)
                                return "placeholderMessageResendResponse." + error;
                        }
                    }
                    if (message.waffleNonceFetchRequestResponse != null && Object.hasOwnProperty.call(message, "waffleNonceFetchRequestResponse")) {
                        properties._waffleNonceFetchRequestResponse = 1;
                        {
                            var error = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse.verify(message.waffleNonceFetchRequestResponse, q + 1);
                            if (error)
                                return "waffleNonceFetchRequestResponse." + error;
                        }
                    }
                    if (message.fullHistorySyncOnDemandRequestResponse != null && Object.hasOwnProperty.call(message, "fullHistorySyncOnDemandRequestResponse")) {
                        properties._fullHistorySyncOnDemandRequestResponse = 1;
                        {
                            var error = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse.verify(message.fullHistorySyncOnDemandRequestResponse, q + 1);
                            if (error)
                                return "fullHistorySyncOnDemandRequestResponse." + error;
                        }
                    }
                    if (message.companionMetaNonceFetchRequestResponse != null && Object.hasOwnProperty.call(message, "companionMetaNonceFetchRequestResponse")) {
                        properties._companionMetaNonceFetchRequestResponse = 1;
                        {
                            var error = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse.verify(message.companionMetaNonceFetchRequestResponse, q + 1);
                            if (error)
                                return "companionMetaNonceFetchRequestResponse." + error;
                        }
                    }
                    if (message.syncdSnapshotFatalRecoveryResponse != null && Object.hasOwnProperty.call(message, "syncdSnapshotFatalRecoveryResponse")) {
                        properties._syncdSnapshotFatalRecoveryResponse = 1;
                        {
                            var error = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse.verify(message.syncdSnapshotFatalRecoveryResponse, q + 1);
                            if (error)
                                return "syncdSnapshotFatalRecoveryResponse." + error;
                        }
                    }
                    if (message.companionCanonicalUserNonceFetchRequestResponse != null && Object.hasOwnProperty.call(message, "companionCanonicalUserNonceFetchRequestResponse")) {
                        properties._companionCanonicalUserNonceFetchRequestResponse = 1;
                        {
                            var error = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse.verify(message.companionCanonicalUserNonceFetchRequestResponse, q + 1);
                            if (error)
                                return "companionCanonicalUserNonceFetchRequestResponse." + error;
                        }
                    }
                    if (message.historySyncChunkRetryResponse != null && Object.hasOwnProperty.call(message, "historySyncChunkRetryResponse")) {
                        properties._historySyncChunkRetryResponse = 1;
                        {
                            var error = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse.verify(message.historySyncChunkRetryResponse, q + 1);
                            if (error)
                                return "historySyncChunkRetryResponse." + error;
                        }
                    }
                    if (message.flowResponsesCsvBundle != null && Object.hasOwnProperty.call(message, "flowResponsesCsvBundle")) {
                        properties._flowResponsesCsvBundle = 1;
                        {
                            var error = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle.verify(message.flowResponsesCsvBundle, q + 1);
                            if (error)
                                return "flowResponsesCsvBundle." + error;
                        }
                    }
                    if (message.bizBroadcastInsightsContactListResponse != null && Object.hasOwnProperty.call(message, "bizBroadcastInsightsContactListResponse")) {
                        properties._bizBroadcastInsightsContactListResponse = 1;
                        {
                            var error = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse.verify(message.bizBroadcastInsightsContactListResponse, q + 1);
                            if (error)
                                return "bizBroadcastInsightsContactListResponse." + error;
                        }
                    }
                    return null;
                };

                /**
                 * Creates a PeerDataOperationResult message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult} PeerDataOperationResult
                 */
                PeerDataOperationResult.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    switch (object.mediaUploadResult) {
                    case "GENERAL_ERROR":
                    case 0:
                        message.mediaUploadResult = 0;
                        break;
                    case "SUCCESS":
                    case 1:
                        message.mediaUploadResult = 1;
                        break;
                    case "NOT_FOUND":
                    case 2:
                        message.mediaUploadResult = 2;
                        break;
                    case "DECRYPTION_ERROR":
                    case 3:
                        message.mediaUploadResult = 3;
                        break;
                    default:
                        if (typeof object.mediaUploadResult === "number" && (object.mediaUploadResult | 0) === object.mediaUploadResult)
                            message.mediaUploadResult = object.mediaUploadResult;
                    }
                    if (object.stickerMessage != null) {
                        if (!$util.isObject(object.stickerMessage))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.stickerMessage: object expected");
                        message.stickerMessage = $root.proto.Message.StickerMessage.fromObject(object.stickerMessage, q + 1);
                    }
                    if (object.linkPreviewResponse != null) {
                        if (!$util.isObject(object.linkPreviewResponse))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.linkPreviewResponse: object expected");
                        message.linkPreviewResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.fromObject(object.linkPreviewResponse, q + 1);
                    }
                    if (object.placeholderMessageResendResponse != null) {
                        if (!$util.isObject(object.placeholderMessageResendResponse))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.placeholderMessageResendResponse: object expected");
                        message.placeholderMessageResendResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse.fromObject(object.placeholderMessageResendResponse, q + 1);
                    }
                    if (object.waffleNonceFetchRequestResponse != null) {
                        if (!$util.isObject(object.waffleNonceFetchRequestResponse))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.waffleNonceFetchRequestResponse: object expected");
                        message.waffleNonceFetchRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse.fromObject(object.waffleNonceFetchRequestResponse, q + 1);
                    }
                    if (object.fullHistorySyncOnDemandRequestResponse != null) {
                        if (!$util.isObject(object.fullHistorySyncOnDemandRequestResponse))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.fullHistorySyncOnDemandRequestResponse: object expected");
                        message.fullHistorySyncOnDemandRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse.fromObject(object.fullHistorySyncOnDemandRequestResponse, q + 1);
                    }
                    if (object.companionMetaNonceFetchRequestResponse != null) {
                        if (!$util.isObject(object.companionMetaNonceFetchRequestResponse))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.companionMetaNonceFetchRequestResponse: object expected");
                        message.companionMetaNonceFetchRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse.fromObject(object.companionMetaNonceFetchRequestResponse, q + 1);
                    }
                    if (object.syncdSnapshotFatalRecoveryResponse != null) {
                        if (!$util.isObject(object.syncdSnapshotFatalRecoveryResponse))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.syncdSnapshotFatalRecoveryResponse: object expected");
                        message.syncdSnapshotFatalRecoveryResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse.fromObject(object.syncdSnapshotFatalRecoveryResponse, q + 1);
                    }
                    if (object.companionCanonicalUserNonceFetchRequestResponse != null) {
                        if (!$util.isObject(object.companionCanonicalUserNonceFetchRequestResponse))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.companionCanonicalUserNonceFetchRequestResponse: object expected");
                        message.companionCanonicalUserNonceFetchRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse.fromObject(object.companionCanonicalUserNonceFetchRequestResponse, q + 1);
                    }
                    if (object.historySyncChunkRetryResponse != null) {
                        if (!$util.isObject(object.historySyncChunkRetryResponse))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.historySyncChunkRetryResponse: object expected");
                        message.historySyncChunkRetryResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse.fromObject(object.historySyncChunkRetryResponse, q + 1);
                    }
                    if (object.flowResponsesCsvBundle != null) {
                        if (!$util.isObject(object.flowResponsesCsvBundle))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.flowResponsesCsvBundle: object expected");
                        message.flowResponsesCsvBundle = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle.fromObject(object.flowResponsesCsvBundle, q + 1);
                    }
                    if (object.bizBroadcastInsightsContactListResponse != null) {
                        if (!$util.isObject(object.bizBroadcastInsightsContactListResponse))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.bizBroadcastInsightsContactListResponse: object expected");
                        message.bizBroadcastInsightsContactListResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse.fromObject(object.bizBroadcastInsightsContactListResponse, q + 1);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a PeerDataOperationResult message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @static
                 * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult} message PeerDataOperationResult
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                PeerDataOperationResult.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.mediaUploadResult != null && Object.hasOwnProperty.call(message, "mediaUploadResult"))
                        object.mediaUploadResult = options.enums === String ? $root.proto.MediaRetryNotification.ResultType[message.mediaUploadResult] === undefined ? message.mediaUploadResult : $root.proto.MediaRetryNotification.ResultType[message.mediaUploadResult] : message.mediaUploadResult;
                    if (message.stickerMessage != null && Object.hasOwnProperty.call(message, "stickerMessage"))
                        object.stickerMessage = $root.proto.Message.StickerMessage.toObject(message.stickerMessage, options, q + 1);
                    if (message.linkPreviewResponse != null && Object.hasOwnProperty.call(message, "linkPreviewResponse"))
                        object.linkPreviewResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.toObject(message.linkPreviewResponse, options, q + 1);
                    if (message.placeholderMessageResendResponse != null && Object.hasOwnProperty.call(message, "placeholderMessageResendResponse"))
                        object.placeholderMessageResendResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse.toObject(message.placeholderMessageResendResponse, options, q + 1);
                    if (message.waffleNonceFetchRequestResponse != null && Object.hasOwnProperty.call(message, "waffleNonceFetchRequestResponse"))
                        object.waffleNonceFetchRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse.toObject(message.waffleNonceFetchRequestResponse, options, q + 1);
                    if (message.fullHistorySyncOnDemandRequestResponse != null && Object.hasOwnProperty.call(message, "fullHistorySyncOnDemandRequestResponse"))
                        object.fullHistorySyncOnDemandRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse.toObject(message.fullHistorySyncOnDemandRequestResponse, options, q + 1);
                    if (message.companionMetaNonceFetchRequestResponse != null && Object.hasOwnProperty.call(message, "companionMetaNonceFetchRequestResponse"))
                        object.companionMetaNonceFetchRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse.toObject(message.companionMetaNonceFetchRequestResponse, options, q + 1);
                    if (message.syncdSnapshotFatalRecoveryResponse != null && Object.hasOwnProperty.call(message, "syncdSnapshotFatalRecoveryResponse"))
                        object.syncdSnapshotFatalRecoveryResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse.toObject(message.syncdSnapshotFatalRecoveryResponse, options, q + 1);
                    if (message.companionCanonicalUserNonceFetchRequestResponse != null && Object.hasOwnProperty.call(message, "companionCanonicalUserNonceFetchRequestResponse"))
                        object.companionCanonicalUserNonceFetchRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse.toObject(message.companionCanonicalUserNonceFetchRequestResponse, options, q + 1);
                    if (message.historySyncChunkRetryResponse != null && Object.hasOwnProperty.call(message, "historySyncChunkRetryResponse"))
                        object.historySyncChunkRetryResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse.toObject(message.historySyncChunkRetryResponse, options, q + 1);
                    if (message.flowResponsesCsvBundle != null && Object.hasOwnProperty.call(message, "flowResponsesCsvBundle"))
                        object.flowResponsesCsvBundle = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle.toObject(message.flowResponsesCsvBundle, options, q + 1);
                    if (message.bizBroadcastInsightsContactListResponse != null && Object.hasOwnProperty.call(message, "bizBroadcastInsightsContactListResponse"))
                        object.bizBroadcastInsightsContactListResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse.toObject(message.bizBroadcastInsightsContactListResponse, options, q + 1);
                    return object;
                };

                /**
                 * Converts this PeerDataOperationResult to JSON.
                 * @function toJSON
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                PeerDataOperationResult.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for PeerDataOperationResult
                 * @function getTypeUrl
                 * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                PeerDataOperationResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult";
                };

                PeerDataOperationResult.BizBroadcastInsightsContactListResponse = (function() {

                    /**
                     * Properties of a BizBroadcastInsightsContactListResponse.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @interface IBizBroadcastInsightsContactListResponse
                     * @property {string|null} [campaignId] BizBroadcastInsightsContactListResponse campaignId
                     * @property {number|Long|null} [timestampMs] BizBroadcastInsightsContactListResponse timestampMs
                     * @property {Array.<proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IBizBroadcastInsightsContactState>|null} [contacts] BizBroadcastInsightsContactListResponse contacts
                     */

                    /**
                     * Constructs a new BizBroadcastInsightsContactListResponse.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @classdesc Represents a BizBroadcastInsightsContactListResponse.
                     * @implements IBizBroadcastInsightsContactListResponse
                     * @constructor
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IBizBroadcastInsightsContactListResponse=} [properties] Properties to set
                     */
                    function BizBroadcastInsightsContactListResponse(properties) {
                        this.contacts = [];
                        if (properties)
                            for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    /**
                     * BizBroadcastInsightsContactListResponse campaignId.
                     * @member {string|null|undefined} campaignId
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse
                     * @instance
                     */
                    BizBroadcastInsightsContactListResponse.prototype.campaignId = null;

                    /**
                     * BizBroadcastInsightsContactListResponse timestampMs.
                     * @member {number|Long|null|undefined} timestampMs
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse
                     * @instance
                     */
                    BizBroadcastInsightsContactListResponse.prototype.timestampMs = null;

                    /**
                     * BizBroadcastInsightsContactListResponse contacts.
                     * @member {Array.<proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IBizBroadcastInsightsContactState>} contacts
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse
                     * @instance
                     */
                    BizBroadcastInsightsContactListResponse.prototype.contacts = $util.emptyArray;

                    // OneOf field names bound to virtual getters and setters
                    var $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(BizBroadcastInsightsContactListResponse.prototype, "_campaignId", {
                        get: $util.oneOfGetter($oneOfFields = ["campaignId"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(BizBroadcastInsightsContactListResponse.prototype, "_timestampMs", {
                        get: $util.oneOfGetter($oneOfFields = ["timestampMs"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    /**
                     * Creates a new BizBroadcastInsightsContactListResponse instance using the specified properties.
                     * @function create
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IBizBroadcastInsightsContactListResponse=} [properties] Properties to set
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse} BizBroadcastInsightsContactListResponse instance
                     */
                    BizBroadcastInsightsContactListResponse.create = function create(properties) {
                        return new BizBroadcastInsightsContactListResponse(properties);
                    };

                    /**
                     * Encodes the specified BizBroadcastInsightsContactListResponse message. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse.verify|verify} messages.
                     * @function encode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IBizBroadcastInsightsContactListResponse} message BizBroadcastInsightsContactListResponse message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    BizBroadcastInsightsContactListResponse.encode = function encode(message, writer, q) {
                        if (!writer)
                            writer = $Writer.create();
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        if (message.campaignId != null && Object.hasOwnProperty.call(message, "campaignId"))
                            writer.uint32(/* id 1, wireType 2 =*/10).string(message.campaignId);
                        if (message.timestampMs != null && Object.hasOwnProperty.call(message, "timestampMs"))
                            writer.uint32(/* id 2, wireType 0 =*/16).int64(message.timestampMs);
                        if (message.contacts != null && message.contacts.length)
                            for (var i = 0; i < message.contacts.length; ++i)
                                $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState.encode(message.contacts[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (var i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    /**
                     * Encodes the specified BizBroadcastInsightsContactListResponse message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse.verify|verify} messages.
                     * @function encodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IBizBroadcastInsightsContactListResponse} message BizBroadcastInsightsContactListResponse message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    BizBroadcastInsightsContactListResponse.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                    };

                    /**
                     * Decodes a BizBroadcastInsightsContactListResponse message from the specified reader or buffer.
                     * @function decode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @param {number} [length] Message length if known beforehand
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse} BizBroadcastInsightsContactListResponse
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    BizBroadcastInsightsContactListResponse.decode = function decode(reader, length, z, q, g) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (q === undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                        while (reader.pos < end) {
                            var s = reader.pos;
                            var tag = reader.tag();
                            if (tag === z) {
                                z = undefined;
                                break;
                            }
                            var u = tag & 7;
                            switch (tag >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    message.campaignId = reader.stringVerify();
                                    message._campaignId = "campaignId";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 0)
                                        break;
                                    message.timestampMs = reader.int64();
                                    message._timestampMs = "timestampMs";
                                    continue;
                                }
                            case 3: {
                                    if (u !== 2)
                                        break;
                                    if (!(message.contacts && message.contacts.length))
                                        message.contacts = [];
                                    message.contacts.push($root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState.decode(reader, reader.uint32(), undefined, q + 1));
                                    continue;
                                }
                            }
                            reader.skipType(u, q, tag);
                            if (!reader.discardUnknown) {
                                $util.makeProp(message, "$unknowns", false);
                                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                            }
                        }
                        if (z !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    /**
                     * Decodes a BizBroadcastInsightsContactListResponse message from the specified reader or buffer, length delimited.
                     * @function decodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse} BizBroadcastInsightsContactListResponse
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    BizBroadcastInsightsContactListResponse.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    /**
                     * Verifies a BizBroadcastInsightsContactListResponse message.
                     * @function verify
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse
                     * @static
                     * @param {Object.<string,*>} message Plain object to verify
                     * @returns {string|null} `null` if valid, otherwise the reason why it is not
                     */
                    BizBroadcastInsightsContactListResponse.verify = function verify(message, q) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            return "max depth exceeded";
                        var properties = {};
                        if (message.campaignId != null && Object.hasOwnProperty.call(message, "campaignId")) {
                            properties._campaignId = 1;
                            if (!$util.isString(message.campaignId))
                                return "campaignId: string expected";
                        }
                        if (message.timestampMs != null && Object.hasOwnProperty.call(message, "timestampMs")) {
                            properties._timestampMs = 1;
                            if (!$util.isInteger(message.timestampMs) && !(message.timestampMs && $util.isInteger(message.timestampMs.low) && $util.isInteger(message.timestampMs.high)))
                                return "timestampMs: integer|Long expected";
                        }
                        if (message.contacts != null && Object.hasOwnProperty.call(message, "contacts")) {
                            if (!Array.isArray(message.contacts))
                                return "contacts: array expected";
                            for (var i = 0; i < message.contacts.length; ++i) {
                                var error = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState.verify(message.contacts[i], q + 1);
                                if (error)
                                    return "contacts." + error;
                            }
                        }
                        return null;
                    };

                    /**
                     * Creates a BizBroadcastInsightsContactListResponse message from a plain object. Also converts values to their respective internal types.
                     * @function fromObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse
                     * @static
                     * @param {Object.<string,*>} object Plain object
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse} BizBroadcastInsightsContactListResponse
                     */
                    BizBroadcastInsightsContactListResponse.fromObject = function fromObject(object, q) {
                        if (object instanceof C)
                            return object;
                        if (!$util.isObject(object))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse: object expected");
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var message = new C();
                        if (object.campaignId != null)
                            message.campaignId = String(object.campaignId);
                        if (object.timestampMs != null)
                            if ($util.Long)
                                message.timestampMs = $util.Long.fromValue(object.timestampMs, false);
                            else if (typeof object.timestampMs === "string")
                                message.timestampMs = parseInt(object.timestampMs, 10);
                            else if (typeof object.timestampMs === "number")
                                message.timestampMs = object.timestampMs;
                            else if (typeof object.timestampMs === "object")
                                message.timestampMs = new $util.LongBits(object.timestampMs.low >>> 0, object.timestampMs.high >>> 0).toNumber();
                        if (object.contacts) {
                            if (!Array.isArray(object.contacts))
                                throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse.contacts: array expected");
                            message.contacts = Array(object.contacts.length);
                            for (var i = 0; i < object.contacts.length; ++i) {
                                if (!$util.isObject(object.contacts[i]))
                                    throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse.contacts: object expected");
                                message.contacts[i] = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState.fromObject(object.contacts[i], q + 1);
                            }
                        }
                        return message;
                    };

                    /**
                     * Creates a plain object from a BizBroadcastInsightsContactListResponse message. Also converts values to other types if specified.
                     * @function toObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse} message BizBroadcastInsightsContactListResponse
                     * @param {$protobuf.IConversionOptions} [options] Conversion options
                     * @returns {Object.<string,*>} Plain object
                     */
                    BizBroadcastInsightsContactListResponse.toObject = function toObject(message, options, q) {
                        if (!options)
                            options = {};
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var object = {};
                        if (options.arrays || options.defaults)
                            object.contacts = [];
                        if (message.campaignId != null && Object.hasOwnProperty.call(message, "campaignId"))
                            object.campaignId = message.campaignId;
                        if (message.timestampMs != null && Object.hasOwnProperty.call(message, "timestampMs"))
                            if (typeof BigInt !== "undefined" && options.longs === BigInt)
                                object.timestampMs = typeof message.timestampMs === "number" ? BigInt(message.timestampMs) : $util.Long.fromBits(message.timestampMs.low >>> 0, message.timestampMs.high >>> 0, false).toBigInt();
                            else if (typeof message.timestampMs === "number")
                                object.timestampMs = options.longs === String ? String(message.timestampMs) : message.timestampMs;
                            else
                                object.timestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.timestampMs) : options.longs === Number ? new $util.LongBits(message.timestampMs.low >>> 0, message.timestampMs.high >>> 0).toNumber() : message.timestampMs;
                        if (message.contacts && message.contacts.length) {
                            object.contacts = Array(message.contacts.length);
                            for (var j = 0; j < message.contacts.length; ++j)
                                object.contacts[j] = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState.toObject(message.contacts[j], options, q + 1);
                        }
                        return object;
                    };

                    /**
                     * Converts this BizBroadcastInsightsContactListResponse to JSON.
                     * @function toJSON
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse
                     * @instance
                     * @returns {Object.<string,*>} JSON object
                     */
                    BizBroadcastInsightsContactListResponse.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    /**
                     * Gets the default type url for BizBroadcastInsightsContactListResponse
                     * @function getTypeUrl
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse
                     * @static
                     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                     * @returns {string} The default type url
                     */
                    BizBroadcastInsightsContactListResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse";
                    };

                    return BizBroadcastInsightsContactListResponse;
                })();

                PeerDataOperationResult.BizBroadcastInsightsContactState = (function() {

                    /**
                     * Properties of a BizBroadcastInsightsContactState.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @interface IBizBroadcastInsightsContactState
                     * @property {string|null} [contactJid] BizBroadcastInsightsContactState contactJid
                     * @property {proto.Message.InsightDeliveryState|null} [state] BizBroadcastInsightsContactState state
                     */

                    /**
                     * Constructs a new BizBroadcastInsightsContactState.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @classdesc Represents a BizBroadcastInsightsContactState.
                     * @implements IBizBroadcastInsightsContactState
                     * @constructor
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IBizBroadcastInsightsContactState=} [properties] Properties to set
                     */
                    function BizBroadcastInsightsContactState(properties) {
                        if (properties)
                            for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    /**
                     * BizBroadcastInsightsContactState contactJid.
                     * @member {string|null|undefined} contactJid
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState
                     * @instance
                     */
                    BizBroadcastInsightsContactState.prototype.contactJid = null;

                    /**
                     * BizBroadcastInsightsContactState state.
                     * @member {proto.Message.InsightDeliveryState|null|undefined} state
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState
                     * @instance
                     */
                    BizBroadcastInsightsContactState.prototype.state = null;

                    // OneOf field names bound to virtual getters and setters
                    var $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(BizBroadcastInsightsContactState.prototype, "_contactJid", {
                        get: $util.oneOfGetter($oneOfFields = ["contactJid"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(BizBroadcastInsightsContactState.prototype, "_state", {
                        get: $util.oneOfGetter($oneOfFields = ["state"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    /**
                     * Creates a new BizBroadcastInsightsContactState instance using the specified properties.
                     * @function create
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IBizBroadcastInsightsContactState=} [properties] Properties to set
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState} BizBroadcastInsightsContactState instance
                     */
                    BizBroadcastInsightsContactState.create = function create(properties) {
                        return new BizBroadcastInsightsContactState(properties);
                    };

                    /**
                     * Encodes the specified BizBroadcastInsightsContactState message. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState.verify|verify} messages.
                     * @function encode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IBizBroadcastInsightsContactState} message BizBroadcastInsightsContactState message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    BizBroadcastInsightsContactState.encode = function encode(message, writer, q) {
                        if (!writer)
                            writer = $Writer.create();
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        if (message.contactJid != null && Object.hasOwnProperty.call(message, "contactJid"))
                            writer.uint32(/* id 1, wireType 2 =*/10).string(message.contactJid);
                        if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.state);
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (var i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    /**
                     * Encodes the specified BizBroadcastInsightsContactState message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState.verify|verify} messages.
                     * @function encodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IBizBroadcastInsightsContactState} message BizBroadcastInsightsContactState message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    BizBroadcastInsightsContactState.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                    };

                    /**
                     * Decodes a BizBroadcastInsightsContactState message from the specified reader or buffer.
                     * @function decode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @param {number} [length] Message length if known beforehand
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState} BizBroadcastInsightsContactState
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    BizBroadcastInsightsContactState.decode = function decode(reader, length, z, q, g) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (q === undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                        while (reader.pos < end) {
                            var s = reader.pos;
                            var tag = reader.tag();
                            if (tag === z) {
                                z = undefined;
                                break;
                            }
                            var u = tag & 7;
                            switch (tag >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    message.contactJid = reader.stringVerify();
                                    message._contactJid = "contactJid";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 0)
                                        break;
                                    message.state = reader.int32();
                                    message._state = "state";
                                    continue;
                                }
                            }
                            reader.skipType(u, q, tag);
                            if (!reader.discardUnknown) {
                                $util.makeProp(message, "$unknowns", false);
                                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                            }
                        }
                        if (z !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    /**
                     * Decodes a BizBroadcastInsightsContactState message from the specified reader or buffer, length delimited.
                     * @function decodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState} BizBroadcastInsightsContactState
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    BizBroadcastInsightsContactState.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    /**
                     * Verifies a BizBroadcastInsightsContactState message.
                     * @function verify
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState
                     * @static
                     * @param {Object.<string,*>} message Plain object to verify
                     * @returns {string|null} `null` if valid, otherwise the reason why it is not
                     */
                    BizBroadcastInsightsContactState.verify = function verify(message, q) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            return "max depth exceeded";
                        var properties = {};
                        if (message.contactJid != null && Object.hasOwnProperty.call(message, "contactJid")) {
                            properties._contactJid = 1;
                            if (!$util.isString(message.contactJid))
                                return "contactJid: string expected";
                        }
                        if (message.state != null && Object.hasOwnProperty.call(message, "state")) {
                            properties._state = 1;
                            if (typeof message.state !== "number" || (message.state | 0) !== message.state)
                                return "state: enum value expected";
                        }
                        return null;
                    };

                    /**
                     * Creates a BizBroadcastInsightsContactState message from a plain object. Also converts values to their respective internal types.
                     * @function fromObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState
                     * @static
                     * @param {Object.<string,*>} object Plain object
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState} BizBroadcastInsightsContactState
                     */
                    BizBroadcastInsightsContactState.fromObject = function fromObject(object, q) {
                        if (object instanceof C)
                            return object;
                        if (!$util.isObject(object))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState: object expected");
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var message = new C();
                        if (object.contactJid != null)
                            message.contactJid = String(object.contactJid);
                        switch (object.state) {
                        case "SENT":
                        case 0:
                            message.state = 0;
                            break;
                        case "DELIVERED":
                        case 1:
                            message.state = 1;
                            break;
                        case "READ":
                        case 2:
                            message.state = 2;
                            break;
                        case "REPLIED":
                        case 3:
                            message.state = 3;
                            break;
                        case "QUICK_REPLIED":
                        case 4:
                            message.state = 4;
                            break;
                        default:
                            if (typeof object.state === "number" && (object.state | 0) === object.state)
                                message.state = object.state;
                        }
                        return message;
                    };

                    /**
                     * Creates a plain object from a BizBroadcastInsightsContactState message. Also converts values to other types if specified.
                     * @function toObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState} message BizBroadcastInsightsContactState
                     * @param {$protobuf.IConversionOptions} [options] Conversion options
                     * @returns {Object.<string,*>} Plain object
                     */
                    BizBroadcastInsightsContactState.toObject = function toObject(message, options, q) {
                        if (!options)
                            options = {};
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var object = {};
                        if (message.contactJid != null && Object.hasOwnProperty.call(message, "contactJid"))
                            object.contactJid = message.contactJid;
                        if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                            object.state = options.enums === String ? $root.proto.Message.InsightDeliveryState[message.state] === undefined ? message.state : $root.proto.Message.InsightDeliveryState[message.state] : message.state;
                        return object;
                    };

                    /**
                     * Converts this BizBroadcastInsightsContactState to JSON.
                     * @function toJSON
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState
                     * @instance
                     * @returns {Object.<string,*>} JSON object
                     */
                    BizBroadcastInsightsContactState.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    /**
                     * Gets the default type url for BizBroadcastInsightsContactState
                     * @function getTypeUrl
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState
                     * @static
                     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                     * @returns {string} The default type url
                     */
                    BizBroadcastInsightsContactState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState";
                    };

                    return BizBroadcastInsightsContactState;
                })();

                PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse = (function() {

                    /**
                     * Properties of a CompanionCanonicalUserNonceFetchResponse.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @interface ICompanionCanonicalUserNonceFetchResponse
                     * @property {string|null} [nonce] CompanionCanonicalUserNonceFetchResponse nonce
                     * @property {string|null} [waFbid] CompanionCanonicalUserNonceFetchResponse waFbid
                     * @property {boolean|null} [forceRefresh] CompanionCanonicalUserNonceFetchResponse forceRefresh
                     */

                    /**
                     * Constructs a new CompanionCanonicalUserNonceFetchResponse.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @classdesc Represents a CompanionCanonicalUserNonceFetchResponse.
                     * @implements ICompanionCanonicalUserNonceFetchResponse
                     * @constructor
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ICompanionCanonicalUserNonceFetchResponse=} [properties] Properties to set
                     */
                    function CompanionCanonicalUserNonceFetchResponse(properties) {
                        if (properties)
                            for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    /**
                     * CompanionCanonicalUserNonceFetchResponse nonce.
                     * @member {string|null|undefined} nonce
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse
                     * @instance
                     */
                    CompanionCanonicalUserNonceFetchResponse.prototype.nonce = null;

                    /**
                     * CompanionCanonicalUserNonceFetchResponse waFbid.
                     * @member {string|null|undefined} waFbid
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse
                     * @instance
                     */
                    CompanionCanonicalUserNonceFetchResponse.prototype.waFbid = null;

                    /**
                     * CompanionCanonicalUserNonceFetchResponse forceRefresh.
                     * @member {boolean|null|undefined} forceRefresh
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse
                     * @instance
                     */
                    CompanionCanonicalUserNonceFetchResponse.prototype.forceRefresh = null;

                    // OneOf field names bound to virtual getters and setters
                    var $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(CompanionCanonicalUserNonceFetchResponse.prototype, "_nonce", {
                        get: $util.oneOfGetter($oneOfFields = ["nonce"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(CompanionCanonicalUserNonceFetchResponse.prototype, "_waFbid", {
                        get: $util.oneOfGetter($oneOfFields = ["waFbid"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(CompanionCanonicalUserNonceFetchResponse.prototype, "_forceRefresh", {
                        get: $util.oneOfGetter($oneOfFields = ["forceRefresh"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    /**
                     * Creates a new CompanionCanonicalUserNonceFetchResponse instance using the specified properties.
                     * @function create
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ICompanionCanonicalUserNonceFetchResponse=} [properties] Properties to set
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse} CompanionCanonicalUserNonceFetchResponse instance
                     */
                    CompanionCanonicalUserNonceFetchResponse.create = function create(properties) {
                        return new CompanionCanonicalUserNonceFetchResponse(properties);
                    };

                    /**
                     * Encodes the specified CompanionCanonicalUserNonceFetchResponse message. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse.verify|verify} messages.
                     * @function encode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ICompanionCanonicalUserNonceFetchResponse} message CompanionCanonicalUserNonceFetchResponse message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    CompanionCanonicalUserNonceFetchResponse.encode = function encode(message, writer, q) {
                        if (!writer)
                            writer = $Writer.create();
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                            writer.uint32(/* id 1, wireType 2 =*/10).string(message.nonce);
                        if (message.waFbid != null && Object.hasOwnProperty.call(message, "waFbid"))
                            writer.uint32(/* id 2, wireType 2 =*/18).string(message.waFbid);
                        if (message.forceRefresh != null && Object.hasOwnProperty.call(message, "forceRefresh"))
                            writer.uint32(/* id 3, wireType 0 =*/24).bool(message.forceRefresh);
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (var i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    /**
                     * Encodes the specified CompanionCanonicalUserNonceFetchResponse message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse.verify|verify} messages.
                     * @function encodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ICompanionCanonicalUserNonceFetchResponse} message CompanionCanonicalUserNonceFetchResponse message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    CompanionCanonicalUserNonceFetchResponse.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                    };

                    /**
                     * Decodes a CompanionCanonicalUserNonceFetchResponse message from the specified reader or buffer.
                     * @function decode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @param {number} [length] Message length if known beforehand
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse} CompanionCanonicalUserNonceFetchResponse
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    CompanionCanonicalUserNonceFetchResponse.decode = function decode(reader, length, z, q, g) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (q === undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                        while (reader.pos < end) {
                            var s = reader.pos;
                            var tag = reader.tag();
                            if (tag === z) {
                                z = undefined;
                                break;
                            }
                            var u = tag & 7;
                            switch (tag >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    message.nonce = reader.stringVerify();
                                    message._nonce = "nonce";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 2)
                                        break;
                                    message.waFbid = reader.stringVerify();
                                    message._waFbid = "waFbid";
                                    continue;
                                }
                            case 3: {
                                    if (u !== 0)
                                        break;
                                    message.forceRefresh = reader.bool();
                                    message._forceRefresh = "forceRefresh";
                                    continue;
                                }
                            }
                            reader.skipType(u, q, tag);
                            if (!reader.discardUnknown) {
                                $util.makeProp(message, "$unknowns", false);
                                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                            }
                        }
                        if (z !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    /**
                     * Decodes a CompanionCanonicalUserNonceFetchResponse message from the specified reader or buffer, length delimited.
                     * @function decodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse} CompanionCanonicalUserNonceFetchResponse
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    CompanionCanonicalUserNonceFetchResponse.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    /**
                     * Verifies a CompanionCanonicalUserNonceFetchResponse message.
                     * @function verify
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse
                     * @static
                     * @param {Object.<string,*>} message Plain object to verify
                     * @returns {string|null} `null` if valid, otherwise the reason why it is not
                     */
                    CompanionCanonicalUserNonceFetchResponse.verify = function verify(message, q) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            return "max depth exceeded";
                        var properties = {};
                        if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce")) {
                            properties._nonce = 1;
                            if (!$util.isString(message.nonce))
                                return "nonce: string expected";
                        }
                        if (message.waFbid != null && Object.hasOwnProperty.call(message, "waFbid")) {
                            properties._waFbid = 1;
                            if (!$util.isString(message.waFbid))
                                return "waFbid: string expected";
                        }
                        if (message.forceRefresh != null && Object.hasOwnProperty.call(message, "forceRefresh")) {
                            properties._forceRefresh = 1;
                            if (typeof message.forceRefresh !== "boolean")
                                return "forceRefresh: boolean expected";
                        }
                        return null;
                    };

                    /**
                     * Creates a CompanionCanonicalUserNonceFetchResponse message from a plain object. Also converts values to their respective internal types.
                     * @function fromObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse
                     * @static
                     * @param {Object.<string,*>} object Plain object
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse} CompanionCanonicalUserNonceFetchResponse
                     */
                    CompanionCanonicalUserNonceFetchResponse.fromObject = function fromObject(object, q) {
                        if (object instanceof C)
                            return object;
                        if (!$util.isObject(object))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse: object expected");
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var message = new C();
                        if (object.nonce != null)
                            message.nonce = String(object.nonce);
                        if (object.waFbid != null)
                            message.waFbid = String(object.waFbid);
                        if (object.forceRefresh != null)
                            message.forceRefresh = Boolean(object.forceRefresh);
                        return message;
                    };

                    /**
                     * Creates a plain object from a CompanionCanonicalUserNonceFetchResponse message. Also converts values to other types if specified.
                     * @function toObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse} message CompanionCanonicalUserNonceFetchResponse
                     * @param {$protobuf.IConversionOptions} [options] Conversion options
                     * @returns {Object.<string,*>} Plain object
                     */
                    CompanionCanonicalUserNonceFetchResponse.toObject = function toObject(message, options, q) {
                        if (!options)
                            options = {};
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var object = {};
                        if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                            object.nonce = message.nonce;
                        if (message.waFbid != null && Object.hasOwnProperty.call(message, "waFbid"))
                            object.waFbid = message.waFbid;
                        if (message.forceRefresh != null && Object.hasOwnProperty.call(message, "forceRefresh"))
                            object.forceRefresh = message.forceRefresh;
                        return object;
                    };

                    /**
                     * Converts this CompanionCanonicalUserNonceFetchResponse to JSON.
                     * @function toJSON
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse
                     * @instance
                     * @returns {Object.<string,*>} JSON object
                     */
                    CompanionCanonicalUserNonceFetchResponse.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    /**
                     * Gets the default type url for CompanionCanonicalUserNonceFetchResponse
                     * @function getTypeUrl
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse
                     * @static
                     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                     * @returns {string} The default type url
                     */
                    CompanionCanonicalUserNonceFetchResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse";
                    };

                    return CompanionCanonicalUserNonceFetchResponse;
                })();

                PeerDataOperationResult.CompanionMetaNonceFetchResponse = (function() {

                    /**
                     * Properties of a CompanionMetaNonceFetchResponse.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @interface ICompanionMetaNonceFetchResponse
                     * @property {string|null} [nonce] CompanionMetaNonceFetchResponse nonce
                     */

                    /**
                     * Constructs a new CompanionMetaNonceFetchResponse.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @classdesc Represents a CompanionMetaNonceFetchResponse.
                     * @implements ICompanionMetaNonceFetchResponse
                     * @constructor
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ICompanionMetaNonceFetchResponse=} [properties] Properties to set
                     */
                    function CompanionMetaNonceFetchResponse(properties) {
                        if (properties)
                            for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    /**
                     * CompanionMetaNonceFetchResponse nonce.
                     * @member {string|null|undefined} nonce
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse
                     * @instance
                     */
                    CompanionMetaNonceFetchResponse.prototype.nonce = null;

                    // OneOf field names bound to virtual getters and setters
                    var $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(CompanionMetaNonceFetchResponse.prototype, "_nonce", {
                        get: $util.oneOfGetter($oneOfFields = ["nonce"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    /**
                     * Creates a new CompanionMetaNonceFetchResponse instance using the specified properties.
                     * @function create
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ICompanionMetaNonceFetchResponse=} [properties] Properties to set
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse} CompanionMetaNonceFetchResponse instance
                     */
                    CompanionMetaNonceFetchResponse.create = function create(properties) {
                        return new CompanionMetaNonceFetchResponse(properties);
                    };

                    /**
                     * Encodes the specified CompanionMetaNonceFetchResponse message. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse.verify|verify} messages.
                     * @function encode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ICompanionMetaNonceFetchResponse} message CompanionMetaNonceFetchResponse message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    CompanionMetaNonceFetchResponse.encode = function encode(message, writer, q) {
                        if (!writer)
                            writer = $Writer.create();
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                            writer.uint32(/* id 1, wireType 2 =*/10).string(message.nonce);
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (var i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    /**
                     * Encodes the specified CompanionMetaNonceFetchResponse message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse.verify|verify} messages.
                     * @function encodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ICompanionMetaNonceFetchResponse} message CompanionMetaNonceFetchResponse message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    CompanionMetaNonceFetchResponse.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                    };

                    /**
                     * Decodes a CompanionMetaNonceFetchResponse message from the specified reader or buffer.
                     * @function decode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @param {number} [length] Message length if known beforehand
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse} CompanionMetaNonceFetchResponse
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    CompanionMetaNonceFetchResponse.decode = function decode(reader, length, z, q, g) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (q === undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                        while (reader.pos < end) {
                            var s = reader.pos;
                            var tag = reader.tag();
                            if (tag === z) {
                                z = undefined;
                                break;
                            }
                            var u = tag & 7;
                            switch (tag >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    message.nonce = reader.stringVerify();
                                    message._nonce = "nonce";
                                    continue;
                                }
                            }
                            reader.skipType(u, q, tag);
                            if (!reader.discardUnknown) {
                                $util.makeProp(message, "$unknowns", false);
                                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                            }
                        }
                        if (z !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    /**
                     * Decodes a CompanionMetaNonceFetchResponse message from the specified reader or buffer, length delimited.
                     * @function decodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse} CompanionMetaNonceFetchResponse
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    CompanionMetaNonceFetchResponse.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    /**
                     * Verifies a CompanionMetaNonceFetchResponse message.
                     * @function verify
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse
                     * @static
                     * @param {Object.<string,*>} message Plain object to verify
                     * @returns {string|null} `null` if valid, otherwise the reason why it is not
                     */
                    CompanionMetaNonceFetchResponse.verify = function verify(message, q) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            return "max depth exceeded";
                        var properties = {};
                        if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce")) {
                            properties._nonce = 1;
                            if (!$util.isString(message.nonce))
                                return "nonce: string expected";
                        }
                        return null;
                    };

                    /**
                     * Creates a CompanionMetaNonceFetchResponse message from a plain object. Also converts values to their respective internal types.
                     * @function fromObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse
                     * @static
                     * @param {Object.<string,*>} object Plain object
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse} CompanionMetaNonceFetchResponse
                     */
                    CompanionMetaNonceFetchResponse.fromObject = function fromObject(object, q) {
                        if (object instanceof C)
                            return object;
                        if (!$util.isObject(object))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse: object expected");
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var message = new C();
                        if (object.nonce != null)
                            message.nonce = String(object.nonce);
                        return message;
                    };

                    /**
                     * Creates a plain object from a CompanionMetaNonceFetchResponse message. Also converts values to other types if specified.
                     * @function toObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse} message CompanionMetaNonceFetchResponse
                     * @param {$protobuf.IConversionOptions} [options] Conversion options
                     * @returns {Object.<string,*>} Plain object
                     */
                    CompanionMetaNonceFetchResponse.toObject = function toObject(message, options, q) {
                        if (!options)
                            options = {};
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var object = {};
                        if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                            object.nonce = message.nonce;
                        return object;
                    };

                    /**
                     * Converts this CompanionMetaNonceFetchResponse to JSON.
                     * @function toJSON
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse
                     * @instance
                     * @returns {Object.<string,*>} JSON object
                     */
                    CompanionMetaNonceFetchResponse.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    /**
                     * Gets the default type url for CompanionMetaNonceFetchResponse
                     * @function getTypeUrl
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse
                     * @static
                     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                     * @returns {string} The default type url
                     */
                    CompanionMetaNonceFetchResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse";
                    };

                    return CompanionMetaNonceFetchResponse;
                })();

                PeerDataOperationResult.FlowResponsesCsvBundle = (function() {

                    /**
                     * Properties of a FlowResponsesCsvBundle.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @interface IFlowResponsesCsvBundle
                     * @property {string|null} [flowId] FlowResponsesCsvBundle flowId
                     * @property {string|null} [galaxyFlowDownloadRequestId] FlowResponsesCsvBundle galaxyFlowDownloadRequestId
                     * @property {string|null} [fileName] FlowResponsesCsvBundle fileName
                     * @property {string|null} [mimetype] FlowResponsesCsvBundle mimetype
                     * @property {Uint8Array|null} [fileSha256] FlowResponsesCsvBundle fileSha256
                     * @property {Uint8Array|null} [mediaKey] FlowResponsesCsvBundle mediaKey
                     * @property {Uint8Array|null} [fileEncSha256] FlowResponsesCsvBundle fileEncSha256
                     * @property {string|null} [directPath] FlowResponsesCsvBundle directPath
                     * @property {number|Long|null} [mediaKeyTimestamp] FlowResponsesCsvBundle mediaKeyTimestamp
                     * @property {number|Long|null} [fileLength] FlowResponsesCsvBundle fileLength
                     */

                    /**
                     * Constructs a new FlowResponsesCsvBundle.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @classdesc Represents a FlowResponsesCsvBundle.
                     * @implements IFlowResponsesCsvBundle
                     * @constructor
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IFlowResponsesCsvBundle=} [properties] Properties to set
                     */
                    function FlowResponsesCsvBundle(properties) {
                        if (properties)
                            for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    /**
                     * FlowResponsesCsvBundle flowId.
                     * @member {string|null|undefined} flowId
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle
                     * @instance
                     */
                    FlowResponsesCsvBundle.prototype.flowId = null;

                    /**
                     * FlowResponsesCsvBundle galaxyFlowDownloadRequestId.
                     * @member {string|null|undefined} galaxyFlowDownloadRequestId
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle
                     * @instance
                     */
                    FlowResponsesCsvBundle.prototype.galaxyFlowDownloadRequestId = null;

                    /**
                     * FlowResponsesCsvBundle fileName.
                     * @member {string|null|undefined} fileName
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle
                     * @instance
                     */
                    FlowResponsesCsvBundle.prototype.fileName = null;

                    /**
                     * FlowResponsesCsvBundle mimetype.
                     * @member {string|null|undefined} mimetype
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle
                     * @instance
                     */
                    FlowResponsesCsvBundle.prototype.mimetype = null;

                    /**
                     * FlowResponsesCsvBundle fileSha256.
                     * @member {Uint8Array|null|undefined} fileSha256
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle
                     * @instance
                     */
                    FlowResponsesCsvBundle.prototype.fileSha256 = null;

                    /**
                     * FlowResponsesCsvBundle mediaKey.
                     * @member {Uint8Array|null|undefined} mediaKey
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle
                     * @instance
                     */
                    FlowResponsesCsvBundle.prototype.mediaKey = null;

                    /**
                     * FlowResponsesCsvBundle fileEncSha256.
                     * @member {Uint8Array|null|undefined} fileEncSha256
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle
                     * @instance
                     */
                    FlowResponsesCsvBundle.prototype.fileEncSha256 = null;

                    /**
                     * FlowResponsesCsvBundle directPath.
                     * @member {string|null|undefined} directPath
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle
                     * @instance
                     */
                    FlowResponsesCsvBundle.prototype.directPath = null;

                    /**
                     * FlowResponsesCsvBundle mediaKeyTimestamp.
                     * @member {number|Long|null|undefined} mediaKeyTimestamp
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle
                     * @instance
                     */
                    FlowResponsesCsvBundle.prototype.mediaKeyTimestamp = null;

                    /**
                     * FlowResponsesCsvBundle fileLength.
                     * @member {number|Long|null|undefined} fileLength
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle
                     * @instance
                     */
                    FlowResponsesCsvBundle.prototype.fileLength = null;

                    // OneOf field names bound to virtual getters and setters
                    var $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(FlowResponsesCsvBundle.prototype, "_flowId", {
                        get: $util.oneOfGetter($oneOfFields = ["flowId"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(FlowResponsesCsvBundle.prototype, "_galaxyFlowDownloadRequestId", {
                        get: $util.oneOfGetter($oneOfFields = ["galaxyFlowDownloadRequestId"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(FlowResponsesCsvBundle.prototype, "_fileName", {
                        get: $util.oneOfGetter($oneOfFields = ["fileName"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(FlowResponsesCsvBundle.prototype, "_mimetype", {
                        get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(FlowResponsesCsvBundle.prototype, "_fileSha256", {
                        get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(FlowResponsesCsvBundle.prototype, "_mediaKey", {
                        get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(FlowResponsesCsvBundle.prototype, "_fileEncSha256", {
                        get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(FlowResponsesCsvBundle.prototype, "_directPath", {
                        get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(FlowResponsesCsvBundle.prototype, "_mediaKeyTimestamp", {
                        get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(FlowResponsesCsvBundle.prototype, "_fileLength", {
                        get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    /**
                     * Creates a new FlowResponsesCsvBundle instance using the specified properties.
                     * @function create
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IFlowResponsesCsvBundle=} [properties] Properties to set
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle} FlowResponsesCsvBundle instance
                     */
                    FlowResponsesCsvBundle.create = function create(properties) {
                        return new FlowResponsesCsvBundle(properties);
                    };

                    /**
                     * Encodes the specified FlowResponsesCsvBundle message. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle.verify|verify} messages.
                     * @function encode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IFlowResponsesCsvBundle} message FlowResponsesCsvBundle message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    FlowResponsesCsvBundle.encode = function encode(message, writer, q) {
                        if (!writer)
                            writer = $Writer.create();
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        if (message.flowId != null && Object.hasOwnProperty.call(message, "flowId"))
                            writer.uint32(/* id 1, wireType 2 =*/10).string(message.flowId);
                        if (message.galaxyFlowDownloadRequestId != null && Object.hasOwnProperty.call(message, "galaxyFlowDownloadRequestId"))
                            writer.uint32(/* id 2, wireType 2 =*/18).string(message.galaxyFlowDownloadRequestId);
                        if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                            writer.uint32(/* id 3, wireType 2 =*/26).string(message.fileName);
                        if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                            writer.uint32(/* id 4, wireType 2 =*/34).string(message.mimetype);
                        if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                            writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.fileSha256);
                        if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                            writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.mediaKey);
                        if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                            writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.fileEncSha256);
                        if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                            writer.uint32(/* id 8, wireType 2 =*/66).string(message.directPath);
                        if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                            writer.uint32(/* id 9, wireType 0 =*/72).int64(message.mediaKeyTimestamp);
                        if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                            writer.uint32(/* id 10, wireType 0 =*/80).uint64(message.fileLength);
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (var i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    /**
                     * Encodes the specified FlowResponsesCsvBundle message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle.verify|verify} messages.
                     * @function encodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IFlowResponsesCsvBundle} message FlowResponsesCsvBundle message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    FlowResponsesCsvBundle.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                    };

                    /**
                     * Decodes a FlowResponsesCsvBundle message from the specified reader or buffer.
                     * @function decode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @param {number} [length] Message length if known beforehand
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle} FlowResponsesCsvBundle
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    FlowResponsesCsvBundle.decode = function decode(reader, length, z, q, g) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (q === undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                        while (reader.pos < end) {
                            var s = reader.pos;
                            var tag = reader.tag();
                            if (tag === z) {
                                z = undefined;
                                break;
                            }
                            var u = tag & 7;
                            switch (tag >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    message.flowId = reader.stringVerify();
                                    message._flowId = "flowId";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 2)
                                        break;
                                    message.galaxyFlowDownloadRequestId = reader.stringVerify();
                                    message._galaxyFlowDownloadRequestId = "galaxyFlowDownloadRequestId";
                                    continue;
                                }
                            case 3: {
                                    if (u !== 2)
                                        break;
                                    message.fileName = reader.stringVerify();
                                    message._fileName = "fileName";
                                    continue;
                                }
                            case 4: {
                                    if (u !== 2)
                                        break;
                                    message.mimetype = reader.stringVerify();
                                    message._mimetype = "mimetype";
                                    continue;
                                }
                            case 5: {
                                    if (u !== 2)
                                        break;
                                    message.fileSha256 = reader.bytes();
                                    message._fileSha256 = "fileSha256";
                                    continue;
                                }
                            case 6: {
                                    if (u !== 2)
                                        break;
                                    message.mediaKey = reader.bytes();
                                    message._mediaKey = "mediaKey";
                                    continue;
                                }
                            case 7: {
                                    if (u !== 2)
                                        break;
                                    message.fileEncSha256 = reader.bytes();
                                    message._fileEncSha256 = "fileEncSha256";
                                    continue;
                                }
                            case 8: {
                                    if (u !== 2)
                                        break;
                                    message.directPath = reader.stringVerify();
                                    message._directPath = "directPath";
                                    continue;
                                }
                            case 9: {
                                    if (u !== 0)
                                        break;
                                    message.mediaKeyTimestamp = reader.int64();
                                    message._mediaKeyTimestamp = "mediaKeyTimestamp";
                                    continue;
                                }
                            case 10: {
                                    if (u !== 0)
                                        break;
                                    message.fileLength = reader.uint64();
                                    message._fileLength = "fileLength";
                                    continue;
                                }
                            }
                            reader.skipType(u, q, tag);
                            if (!reader.discardUnknown) {
                                $util.makeProp(message, "$unknowns", false);
                                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                            }
                        }
                        if (z !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    /**
                     * Decodes a FlowResponsesCsvBundle message from the specified reader or buffer, length delimited.
                     * @function decodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle} FlowResponsesCsvBundle
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    FlowResponsesCsvBundle.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    /**
                     * Verifies a FlowResponsesCsvBundle message.
                     * @function verify
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle
                     * @static
                     * @param {Object.<string,*>} message Plain object to verify
                     * @returns {string|null} `null` if valid, otherwise the reason why it is not
                     */
                    FlowResponsesCsvBundle.verify = function verify(message, q) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            return "max depth exceeded";
                        var properties = {};
                        if (message.flowId != null && Object.hasOwnProperty.call(message, "flowId")) {
                            properties._flowId = 1;
                            if (!$util.isString(message.flowId))
                                return "flowId: string expected";
                        }
                        if (message.galaxyFlowDownloadRequestId != null && Object.hasOwnProperty.call(message, "galaxyFlowDownloadRequestId")) {
                            properties._galaxyFlowDownloadRequestId = 1;
                            if (!$util.isString(message.galaxyFlowDownloadRequestId))
                                return "galaxyFlowDownloadRequestId: string expected";
                        }
                        if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName")) {
                            properties._fileName = 1;
                            if (!$util.isString(message.fileName))
                                return "fileName: string expected";
                        }
                        if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype")) {
                            properties._mimetype = 1;
                            if (!$util.isString(message.mimetype))
                                return "mimetype: string expected";
                        }
                        if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256")) {
                            properties._fileSha256 = 1;
                            if (!(message.fileSha256 && typeof message.fileSha256.length === "number" || $util.isString(message.fileSha256)))
                                return "fileSha256: buffer expected";
                        }
                        if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey")) {
                            properties._mediaKey = 1;
                            if (!(message.mediaKey && typeof message.mediaKey.length === "number" || $util.isString(message.mediaKey)))
                                return "mediaKey: buffer expected";
                        }
                        if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256")) {
                            properties._fileEncSha256 = 1;
                            if (!(message.fileEncSha256 && typeof message.fileEncSha256.length === "number" || $util.isString(message.fileEncSha256)))
                                return "fileEncSha256: buffer expected";
                        }
                        if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath")) {
                            properties._directPath = 1;
                            if (!$util.isString(message.directPath))
                                return "directPath: string expected";
                        }
                        if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp")) {
                            properties._mediaKeyTimestamp = 1;
                            if (!$util.isInteger(message.mediaKeyTimestamp) && !(message.mediaKeyTimestamp && $util.isInteger(message.mediaKeyTimestamp.low) && $util.isInteger(message.mediaKeyTimestamp.high)))
                                return "mediaKeyTimestamp: integer|Long expected";
                        }
                        if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength")) {
                            properties._fileLength = 1;
                            if (!$util.isInteger(message.fileLength) && !(message.fileLength && $util.isInteger(message.fileLength.low) && $util.isInteger(message.fileLength.high)))
                                return "fileLength: integer|Long expected";
                        }
                        return null;
                    };

                    /**
                     * Creates a FlowResponsesCsvBundle message from a plain object. Also converts values to their respective internal types.
                     * @function fromObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle
                     * @static
                     * @param {Object.<string,*>} object Plain object
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle} FlowResponsesCsvBundle
                     */
                    FlowResponsesCsvBundle.fromObject = function fromObject(object, q) {
                        if (object instanceof C)
                            return object;
                        if (!$util.isObject(object))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle: object expected");
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var message = new C();
                        if (object.flowId != null)
                            message.flowId = String(object.flowId);
                        if (object.galaxyFlowDownloadRequestId != null)
                            message.galaxyFlowDownloadRequestId = String(object.galaxyFlowDownloadRequestId);
                        if (object.fileName != null)
                            message.fileName = String(object.fileName);
                        if (object.mimetype != null)
                            message.mimetype = String(object.mimetype);
                        if (object.fileSha256 != null)
                            if (typeof object.fileSha256 === "string")
                                $util.base64.decode(object.fileSha256, message.fileSha256 = $util.newBuffer($util.base64.length(object.fileSha256)), 0);
                            else if (object.fileSha256.length >= 0)
                                message.fileSha256 = object.fileSha256;
                        if (object.mediaKey != null)
                            if (typeof object.mediaKey === "string")
                                $util.base64.decode(object.mediaKey, message.mediaKey = $util.newBuffer($util.base64.length(object.mediaKey)), 0);
                            else if (object.mediaKey.length >= 0)
                                message.mediaKey = object.mediaKey;
                        if (object.fileEncSha256 != null)
                            if (typeof object.fileEncSha256 === "string")
                                $util.base64.decode(object.fileEncSha256, message.fileEncSha256 = $util.newBuffer($util.base64.length(object.fileEncSha256)), 0);
                            else if (object.fileEncSha256.length >= 0)
                                message.fileEncSha256 = object.fileEncSha256;
                        if (object.directPath != null)
                            message.directPath = String(object.directPath);
                        if (object.mediaKeyTimestamp != null)
                            if ($util.Long)
                                message.mediaKeyTimestamp = $util.Long.fromValue(object.mediaKeyTimestamp, false);
                            else if (typeof object.mediaKeyTimestamp === "string")
                                message.mediaKeyTimestamp = parseInt(object.mediaKeyTimestamp, 10);
                            else if (typeof object.mediaKeyTimestamp === "number")
                                message.mediaKeyTimestamp = object.mediaKeyTimestamp;
                            else if (typeof object.mediaKeyTimestamp === "object")
                                message.mediaKeyTimestamp = new $util.LongBits(object.mediaKeyTimestamp.low >>> 0, object.mediaKeyTimestamp.high >>> 0).toNumber();
                        if (object.fileLength != null)
                            if ($util.Long)
                                message.fileLength = $util.Long.fromValue(object.fileLength, true);
                            else if (typeof object.fileLength === "string")
                                message.fileLength = parseInt(object.fileLength, 10);
                            else if (typeof object.fileLength === "number")
                                message.fileLength = object.fileLength;
                            else if (typeof object.fileLength === "object")
                                message.fileLength = new $util.LongBits(object.fileLength.low >>> 0, object.fileLength.high >>> 0).toNumber(true);
                        return message;
                    };

                    /**
                     * Creates a plain object from a FlowResponsesCsvBundle message. Also converts values to other types if specified.
                     * @function toObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle} message FlowResponsesCsvBundle
                     * @param {$protobuf.IConversionOptions} [options] Conversion options
                     * @returns {Object.<string,*>} Plain object
                     */
                    FlowResponsesCsvBundle.toObject = function toObject(message, options, q) {
                        if (!options)
                            options = {};
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var object = {};
                        if (message.flowId != null && Object.hasOwnProperty.call(message, "flowId"))
                            object.flowId = message.flowId;
                        if (message.galaxyFlowDownloadRequestId != null && Object.hasOwnProperty.call(message, "galaxyFlowDownloadRequestId"))
                            object.galaxyFlowDownloadRequestId = message.galaxyFlowDownloadRequestId;
                        if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                            object.fileName = message.fileName;
                        if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                            object.mimetype = message.mimetype;
                        if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                            object.fileSha256 = options.bytes === String ? $util.base64.encode(message.fileSha256, 0, message.fileSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileSha256) : message.fileSha256;
                        if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                            object.mediaKey = options.bytes === String ? $util.base64.encode(message.mediaKey, 0, message.mediaKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.mediaKey) : message.mediaKey;
                        if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                            object.fileEncSha256 = options.bytes === String ? $util.base64.encode(message.fileEncSha256, 0, message.fileEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileEncSha256) : message.fileEncSha256;
                        if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                            object.directPath = message.directPath;
                        if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                            if (typeof BigInt !== "undefined" && options.longs === BigInt)
                                object.mediaKeyTimestamp = typeof message.mediaKeyTimestamp === "number" ? BigInt(message.mediaKeyTimestamp) : $util.Long.fromBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                            else if (typeof message.mediaKeyTimestamp === "number")
                                object.mediaKeyTimestamp = options.longs === String ? String(message.mediaKeyTimestamp) : message.mediaKeyTimestamp;
                            else
                                object.mediaKeyTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.mediaKeyTimestamp) : options.longs === Number ? new $util.LongBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0).toNumber() : message.mediaKeyTimestamp;
                        if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                            if (typeof BigInt !== "undefined" && options.longs === BigInt)
                                object.fileLength = typeof message.fileLength === "number" ? BigInt(message.fileLength) : $util.Long.fromBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0, true).toBigInt();
                            else if (typeof message.fileLength === "number")
                                object.fileLength = options.longs === String ? String(message.fileLength) : message.fileLength;
                            else
                                object.fileLength = options.longs === String ? $util.Long.prototype.toString.call(message.fileLength) : options.longs === Number ? new $util.LongBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0).toNumber(true) : message.fileLength;
                        return object;
                    };

                    /**
                     * Converts this FlowResponsesCsvBundle to JSON.
                     * @function toJSON
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle
                     * @instance
                     * @returns {Object.<string,*>} JSON object
                     */
                    FlowResponsesCsvBundle.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    /**
                     * Gets the default type url for FlowResponsesCsvBundle
                     * @function getTypeUrl
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle
                     * @static
                     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                     * @returns {string} The default type url
                     */
                    FlowResponsesCsvBundle.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle";
                    };

                    return FlowResponsesCsvBundle;
                })();

                PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse = (function() {

                    /**
                     * Properties of a FullHistorySyncOnDemandRequestResponse.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @interface IFullHistorySyncOnDemandRequestResponse
                     * @property {proto.Message.IFullHistorySyncOnDemandRequestMetadata|null} [requestMetadata] FullHistorySyncOnDemandRequestResponse requestMetadata
                     * @property {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandResponseCode|null} [responseCode] FullHistorySyncOnDemandRequestResponse responseCode
                     */

                    /**
                     * Constructs a new FullHistorySyncOnDemandRequestResponse.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @classdesc Represents a FullHistorySyncOnDemandRequestResponse.
                     * @implements IFullHistorySyncOnDemandRequestResponse
                     * @constructor
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IFullHistorySyncOnDemandRequestResponse=} [properties] Properties to set
                     */
                    function FullHistorySyncOnDemandRequestResponse(properties) {
                        if (properties)
                            for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    /**
                     * FullHistorySyncOnDemandRequestResponse requestMetadata.
                     * @member {proto.Message.IFullHistorySyncOnDemandRequestMetadata|null|undefined} requestMetadata
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse
                     * @instance
                     */
                    FullHistorySyncOnDemandRequestResponse.prototype.requestMetadata = null;

                    /**
                     * FullHistorySyncOnDemandRequestResponse responseCode.
                     * @member {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandResponseCode|null|undefined} responseCode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse
                     * @instance
                     */
                    FullHistorySyncOnDemandRequestResponse.prototype.responseCode = null;

                    // OneOf field names bound to virtual getters and setters
                    var $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(FullHistorySyncOnDemandRequestResponse.prototype, "_requestMetadata", {
                        get: $util.oneOfGetter($oneOfFields = ["requestMetadata"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(FullHistorySyncOnDemandRequestResponse.prototype, "_responseCode", {
                        get: $util.oneOfGetter($oneOfFields = ["responseCode"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    /**
                     * Creates a new FullHistorySyncOnDemandRequestResponse instance using the specified properties.
                     * @function create
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IFullHistorySyncOnDemandRequestResponse=} [properties] Properties to set
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse} FullHistorySyncOnDemandRequestResponse instance
                     */
                    FullHistorySyncOnDemandRequestResponse.create = function create(properties) {
                        return new FullHistorySyncOnDemandRequestResponse(properties);
                    };

                    /**
                     * Encodes the specified FullHistorySyncOnDemandRequestResponse message. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse.verify|verify} messages.
                     * @function encode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IFullHistorySyncOnDemandRequestResponse} message FullHistorySyncOnDemandRequestResponse message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    FullHistorySyncOnDemandRequestResponse.encode = function encode(message, writer, q) {
                        if (!writer)
                            writer = $Writer.create();
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        if (message.requestMetadata != null && Object.hasOwnProperty.call(message, "requestMetadata"))
                            $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.encode(message.requestMetadata, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                        if (message.responseCode != null && Object.hasOwnProperty.call(message, "responseCode"))
                            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.responseCode);
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (var i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    /**
                     * Encodes the specified FullHistorySyncOnDemandRequestResponse message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse.verify|verify} messages.
                     * @function encodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IFullHistorySyncOnDemandRequestResponse} message FullHistorySyncOnDemandRequestResponse message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    FullHistorySyncOnDemandRequestResponse.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                    };

                    /**
                     * Decodes a FullHistorySyncOnDemandRequestResponse message from the specified reader or buffer.
                     * @function decode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @param {number} [length] Message length if known beforehand
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse} FullHistorySyncOnDemandRequestResponse
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    FullHistorySyncOnDemandRequestResponse.decode = function decode(reader, length, z, q, g) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (q === undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                        while (reader.pos < end) {
                            var s = reader.pos;
                            var tag = reader.tag();
                            if (tag === z) {
                                z = undefined;
                                break;
                            }
                            var u = tag & 7;
                            switch (tag >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    message.requestMetadata = $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.requestMetadata);
                                    message._requestMetadata = "requestMetadata";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 0)
                                        break;
                                    message.responseCode = reader.int32();
                                    message._responseCode = "responseCode";
                                    continue;
                                }
                            }
                            reader.skipType(u, q, tag);
                            if (!reader.discardUnknown) {
                                $util.makeProp(message, "$unknowns", false);
                                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                            }
                        }
                        if (z !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    /**
                     * Decodes a FullHistorySyncOnDemandRequestResponse message from the specified reader or buffer, length delimited.
                     * @function decodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse} FullHistorySyncOnDemandRequestResponse
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    FullHistorySyncOnDemandRequestResponse.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    /**
                     * Verifies a FullHistorySyncOnDemandRequestResponse message.
                     * @function verify
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse
                     * @static
                     * @param {Object.<string,*>} message Plain object to verify
                     * @returns {string|null} `null` if valid, otherwise the reason why it is not
                     */
                    FullHistorySyncOnDemandRequestResponse.verify = function verify(message, q) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            return "max depth exceeded";
                        var properties = {};
                        if (message.requestMetadata != null && Object.hasOwnProperty.call(message, "requestMetadata")) {
                            properties._requestMetadata = 1;
                            {
                                var error = $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.verify(message.requestMetadata, q + 1);
                                if (error)
                                    return "requestMetadata." + error;
                            }
                        }
                        if (message.responseCode != null && Object.hasOwnProperty.call(message, "responseCode")) {
                            properties._responseCode = 1;
                            if (typeof message.responseCode !== "number" || (message.responseCode | 0) !== message.responseCode)
                                return "responseCode: enum value expected";
                        }
                        return null;
                    };

                    /**
                     * Creates a FullHistorySyncOnDemandRequestResponse message from a plain object. Also converts values to their respective internal types.
                     * @function fromObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse
                     * @static
                     * @param {Object.<string,*>} object Plain object
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse} FullHistorySyncOnDemandRequestResponse
                     */
                    FullHistorySyncOnDemandRequestResponse.fromObject = function fromObject(object, q) {
                        if (object instanceof C)
                            return object;
                        if (!$util.isObject(object))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse: object expected");
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var message = new C();
                        if (object.requestMetadata != null) {
                            if (!$util.isObject(object.requestMetadata))
                                throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse.requestMetadata: object expected");
                            message.requestMetadata = $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.fromObject(object.requestMetadata, q + 1);
                        }
                        switch (object.responseCode) {
                        case "REQUEST_SUCCESS":
                        case 0:
                            message.responseCode = 0;
                            break;
                        case "REQUEST_TIME_EXPIRED":
                        case 1:
                            message.responseCode = 1;
                            break;
                        case "DECLINED_SHARING_HISTORY":
                        case 2:
                            message.responseCode = 2;
                            break;
                        case "GENERIC_ERROR":
                        case 3:
                            message.responseCode = 3;
                            break;
                        case "ERROR_REQUEST_ON_NON_SMB_PRIMARY":
                        case 4:
                            message.responseCode = 4;
                            break;
                        case "ERROR_HOSTED_DEVICE_NOT_CONNECTED":
                        case 5:
                            message.responseCode = 5;
                            break;
                        case "ERROR_HOSTED_DEVICE_LOGIN_TIME_NOT_SET":
                        case 6:
                            message.responseCode = 6;
                            break;
                        case "ERROR_MULTI_PROVIDER_NOT_CONFIGURED":
                        case 7:
                            message.responseCode = 7;
                            break;
                        default:
                            if (typeof object.responseCode === "number" && (object.responseCode | 0) === object.responseCode)
                                message.responseCode = object.responseCode;
                        }
                        return message;
                    };

                    /**
                     * Creates a plain object from a FullHistorySyncOnDemandRequestResponse message. Also converts values to other types if specified.
                     * @function toObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse} message FullHistorySyncOnDemandRequestResponse
                     * @param {$protobuf.IConversionOptions} [options] Conversion options
                     * @returns {Object.<string,*>} Plain object
                     */
                    FullHistorySyncOnDemandRequestResponse.toObject = function toObject(message, options, q) {
                        if (!options)
                            options = {};
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var object = {};
                        if (message.requestMetadata != null && Object.hasOwnProperty.call(message, "requestMetadata"))
                            object.requestMetadata = $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.toObject(message.requestMetadata, options, q + 1);
                        if (message.responseCode != null && Object.hasOwnProperty.call(message, "responseCode"))
                            object.responseCode = options.enums === String ? $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandResponseCode[message.responseCode] === undefined ? message.responseCode : $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandResponseCode[message.responseCode] : message.responseCode;
                        return object;
                    };

                    /**
                     * Converts this FullHistorySyncOnDemandRequestResponse to JSON.
                     * @function toJSON
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse
                     * @instance
                     * @returns {Object.<string,*>} JSON object
                     */
                    FullHistorySyncOnDemandRequestResponse.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    /**
                     * Gets the default type url for FullHistorySyncOnDemandRequestResponse
                     * @function getTypeUrl
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse
                     * @static
                     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                     * @returns {string} The default type url
                     */
                    FullHistorySyncOnDemandRequestResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse";
                    };

                    return FullHistorySyncOnDemandRequestResponse;
                })();

                /**
                 * FullHistorySyncOnDemandResponseCode enum.
                 * @name proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandResponseCode
                 * @enum {number}
                 * @property {number} REQUEST_SUCCESS=0 REQUEST_SUCCESS value
                 * @property {number} REQUEST_TIME_EXPIRED=1 REQUEST_TIME_EXPIRED value
                 * @property {number} DECLINED_SHARING_HISTORY=2 DECLINED_SHARING_HISTORY value
                 * @property {number} GENERIC_ERROR=3 GENERIC_ERROR value
                 * @property {number} ERROR_REQUEST_ON_NON_SMB_PRIMARY=4 ERROR_REQUEST_ON_NON_SMB_PRIMARY value
                 * @property {number} ERROR_HOSTED_DEVICE_NOT_CONNECTED=5 ERROR_HOSTED_DEVICE_NOT_CONNECTED value
                 * @property {number} ERROR_HOSTED_DEVICE_LOGIN_TIME_NOT_SET=6 ERROR_HOSTED_DEVICE_LOGIN_TIME_NOT_SET value
                 * @property {number} ERROR_MULTI_PROVIDER_NOT_CONFIGURED=7 ERROR_MULTI_PROVIDER_NOT_CONFIGURED value
                 */
                PeerDataOperationResult.FullHistorySyncOnDemandResponseCode = (function() {
                    var valuesById = {}, values = Object.create(valuesById);
                    values[valuesById[0] = "REQUEST_SUCCESS"] = 0;
                    values[valuesById[1] = "REQUEST_TIME_EXPIRED"] = 1;
                    values[valuesById[2] = "DECLINED_SHARING_HISTORY"] = 2;
                    values[valuesById[3] = "GENERIC_ERROR"] = 3;
                    values[valuesById[4] = "ERROR_REQUEST_ON_NON_SMB_PRIMARY"] = 4;
                    values[valuesById[5] = "ERROR_HOSTED_DEVICE_NOT_CONNECTED"] = 5;
                    values[valuesById[6] = "ERROR_HOSTED_DEVICE_LOGIN_TIME_NOT_SET"] = 6;
                    values[valuesById[7] = "ERROR_MULTI_PROVIDER_NOT_CONFIGURED"] = 7;
                    return values;
                })();

                PeerDataOperationResult.HistorySyncChunkRetryResponse = (function() {

                    /**
                     * Properties of a HistorySyncChunkRetryResponse.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @interface IHistorySyncChunkRetryResponse
                     * @property {proto.Message.HistorySyncType|null} [syncType] HistorySyncChunkRetryResponse syncType
                     * @property {number|null} [chunkOrder] HistorySyncChunkRetryResponse chunkOrder
                     * @property {string|null} [requestId] HistorySyncChunkRetryResponse requestId
                     * @property {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponseCode|null} [responseCode] HistorySyncChunkRetryResponse responseCode
                     * @property {boolean|null} [canRecover] HistorySyncChunkRetryResponse canRecover
                     */

                    /**
                     * Constructs a new HistorySyncChunkRetryResponse.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @classdesc Represents a HistorySyncChunkRetryResponse.
                     * @implements IHistorySyncChunkRetryResponse
                     * @constructor
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IHistorySyncChunkRetryResponse=} [properties] Properties to set
                     */
                    function HistorySyncChunkRetryResponse(properties) {
                        if (properties)
                            for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    /**
                     * HistorySyncChunkRetryResponse syncType.
                     * @member {proto.Message.HistorySyncType|null|undefined} syncType
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse
                     * @instance
                     */
                    HistorySyncChunkRetryResponse.prototype.syncType = null;

                    /**
                     * HistorySyncChunkRetryResponse chunkOrder.
                     * @member {number|null|undefined} chunkOrder
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse
                     * @instance
                     */
                    HistorySyncChunkRetryResponse.prototype.chunkOrder = null;

                    /**
                     * HistorySyncChunkRetryResponse requestId.
                     * @member {string|null|undefined} requestId
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse
                     * @instance
                     */
                    HistorySyncChunkRetryResponse.prototype.requestId = null;

                    /**
                     * HistorySyncChunkRetryResponse responseCode.
                     * @member {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponseCode|null|undefined} responseCode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse
                     * @instance
                     */
                    HistorySyncChunkRetryResponse.prototype.responseCode = null;

                    /**
                     * HistorySyncChunkRetryResponse canRecover.
                     * @member {boolean|null|undefined} canRecover
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse
                     * @instance
                     */
                    HistorySyncChunkRetryResponse.prototype.canRecover = null;

                    // OneOf field names bound to virtual getters and setters
                    var $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(HistorySyncChunkRetryResponse.prototype, "_syncType", {
                        get: $util.oneOfGetter($oneOfFields = ["syncType"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(HistorySyncChunkRetryResponse.prototype, "_chunkOrder", {
                        get: $util.oneOfGetter($oneOfFields = ["chunkOrder"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(HistorySyncChunkRetryResponse.prototype, "_requestId", {
                        get: $util.oneOfGetter($oneOfFields = ["requestId"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(HistorySyncChunkRetryResponse.prototype, "_responseCode", {
                        get: $util.oneOfGetter($oneOfFields = ["responseCode"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(HistorySyncChunkRetryResponse.prototype, "_canRecover", {
                        get: $util.oneOfGetter($oneOfFields = ["canRecover"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    /**
                     * Creates a new HistorySyncChunkRetryResponse instance using the specified properties.
                     * @function create
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IHistorySyncChunkRetryResponse=} [properties] Properties to set
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse} HistorySyncChunkRetryResponse instance
                     */
                    HistorySyncChunkRetryResponse.create = function create(properties) {
                        return new HistorySyncChunkRetryResponse(properties);
                    };

                    /**
                     * Encodes the specified HistorySyncChunkRetryResponse message. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse.verify|verify} messages.
                     * @function encode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IHistorySyncChunkRetryResponse} message HistorySyncChunkRetryResponse message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    HistorySyncChunkRetryResponse.encode = function encode(message, writer, q) {
                        if (!writer)
                            writer = $Writer.create();
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        if (message.syncType != null && Object.hasOwnProperty.call(message, "syncType"))
                            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.syncType);
                        if (message.chunkOrder != null && Object.hasOwnProperty.call(message, "chunkOrder"))
                            writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.chunkOrder);
                        if (message.requestId != null && Object.hasOwnProperty.call(message, "requestId"))
                            writer.uint32(/* id 3, wireType 2 =*/26).string(message.requestId);
                        if (message.responseCode != null && Object.hasOwnProperty.call(message, "responseCode"))
                            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.responseCode);
                        if (message.canRecover != null && Object.hasOwnProperty.call(message, "canRecover"))
                            writer.uint32(/* id 5, wireType 0 =*/40).bool(message.canRecover);
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (var i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    /**
                     * Encodes the specified HistorySyncChunkRetryResponse message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse.verify|verify} messages.
                     * @function encodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IHistorySyncChunkRetryResponse} message HistorySyncChunkRetryResponse message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    HistorySyncChunkRetryResponse.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                    };

                    /**
                     * Decodes a HistorySyncChunkRetryResponse message from the specified reader or buffer.
                     * @function decode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @param {number} [length] Message length if known beforehand
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse} HistorySyncChunkRetryResponse
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    HistorySyncChunkRetryResponse.decode = function decode(reader, length, z, q, g) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (q === undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                        while (reader.pos < end) {
                            var s = reader.pos;
                            var tag = reader.tag();
                            if (tag === z) {
                                z = undefined;
                                break;
                            }
                            var u = tag & 7;
                            switch (tag >>>= 3) {
                            case 1: {
                                    if (u !== 0)
                                        break;
                                    message.syncType = reader.int32();
                                    message._syncType = "syncType";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 0)
                                        break;
                                    message.chunkOrder = reader.uint32();
                                    message._chunkOrder = "chunkOrder";
                                    continue;
                                }
                            case 3: {
                                    if (u !== 2)
                                        break;
                                    message.requestId = reader.stringVerify();
                                    message._requestId = "requestId";
                                    continue;
                                }
                            case 4: {
                                    if (u !== 0)
                                        break;
                                    message.responseCode = reader.int32();
                                    message._responseCode = "responseCode";
                                    continue;
                                }
                            case 5: {
                                    if (u !== 0)
                                        break;
                                    message.canRecover = reader.bool();
                                    message._canRecover = "canRecover";
                                    continue;
                                }
                            }
                            reader.skipType(u, q, tag);
                            if (!reader.discardUnknown) {
                                $util.makeProp(message, "$unknowns", false);
                                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                            }
                        }
                        if (z !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    /**
                     * Decodes a HistorySyncChunkRetryResponse message from the specified reader or buffer, length delimited.
                     * @function decodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse} HistorySyncChunkRetryResponse
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    HistorySyncChunkRetryResponse.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    /**
                     * Verifies a HistorySyncChunkRetryResponse message.
                     * @function verify
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse
                     * @static
                     * @param {Object.<string,*>} message Plain object to verify
                     * @returns {string|null} `null` if valid, otherwise the reason why it is not
                     */
                    HistorySyncChunkRetryResponse.verify = function verify(message, q) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            return "max depth exceeded";
                        var properties = {};
                        if (message.syncType != null && Object.hasOwnProperty.call(message, "syncType")) {
                            properties._syncType = 1;
                            if (typeof message.syncType !== "number" || (message.syncType | 0) !== message.syncType)
                                return "syncType: enum value expected";
                        }
                        if (message.chunkOrder != null && Object.hasOwnProperty.call(message, "chunkOrder")) {
                            properties._chunkOrder = 1;
                            if (!$util.isInteger(message.chunkOrder))
                                return "chunkOrder: integer expected";
                        }
                        if (message.requestId != null && Object.hasOwnProperty.call(message, "requestId")) {
                            properties._requestId = 1;
                            if (!$util.isString(message.requestId))
                                return "requestId: string expected";
                        }
                        if (message.responseCode != null && Object.hasOwnProperty.call(message, "responseCode")) {
                            properties._responseCode = 1;
                            if (typeof message.responseCode !== "number" || (message.responseCode | 0) !== message.responseCode)
                                return "responseCode: enum value expected";
                        }
                        if (message.canRecover != null && Object.hasOwnProperty.call(message, "canRecover")) {
                            properties._canRecover = 1;
                            if (typeof message.canRecover !== "boolean")
                                return "canRecover: boolean expected";
                        }
                        return null;
                    };

                    /**
                     * Creates a HistorySyncChunkRetryResponse message from a plain object. Also converts values to their respective internal types.
                     * @function fromObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse
                     * @static
                     * @param {Object.<string,*>} object Plain object
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse} HistorySyncChunkRetryResponse
                     */
                    HistorySyncChunkRetryResponse.fromObject = function fromObject(object, q) {
                        if (object instanceof C)
                            return object;
                        if (!$util.isObject(object))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse: object expected");
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var message = new C();
                        switch (object.syncType) {
                        case "INITIAL_BOOTSTRAP":
                        case 0:
                            message.syncType = 0;
                            break;
                        case "INITIAL_STATUS_V3":
                        case 1:
                            message.syncType = 1;
                            break;
                        case "FULL":
                        case 2:
                            message.syncType = 2;
                            break;
                        case "RECENT":
                        case 3:
                            message.syncType = 3;
                            break;
                        case "PUSH_NAME":
                        case 4:
                            message.syncType = 4;
                            break;
                        case "NON_BLOCKING_DATA":
                        case 5:
                            message.syncType = 5;
                            break;
                        case "ON_DEMAND":
                        case 6:
                            message.syncType = 6;
                            break;
                        case "NO_HISTORY":
                        case 7:
                            message.syncType = 7;
                            break;
                        case "MESSAGE_ACCESS_STATUS":
                        case 8:
                            message.syncType = 8;
                            break;
                        default:
                            if (typeof object.syncType === "number" && (object.syncType | 0) === object.syncType)
                                message.syncType = object.syncType;
                        }
                        if (object.chunkOrder != null)
                            message.chunkOrder = object.chunkOrder >>> 0;
                        if (object.requestId != null)
                            message.requestId = String(object.requestId);
                        switch (object.responseCode) {
                        case "GENERATION_ERROR":
                        case 1:
                            message.responseCode = 1;
                            break;
                        case "CHUNK_CONSUMED":
                        case 2:
                            message.responseCode = 2;
                            break;
                        case "TIMEOUT":
                        case 3:
                            message.responseCode = 3;
                            break;
                        case "SESSION_EXHAUSTED":
                        case 4:
                            message.responseCode = 4;
                            break;
                        case "CHUNK_EXHAUSTED":
                        case 5:
                            message.responseCode = 5;
                            break;
                        case "DUPLICATED_REQUEST":
                        case 6:
                            message.responseCode = 6;
                            break;
                        default:
                            if (typeof object.responseCode === "number" && (object.responseCode | 0) === object.responseCode)
                                message.responseCode = object.responseCode;
                        }
                        if (object.canRecover != null)
                            message.canRecover = Boolean(object.canRecover);
                        return message;
                    };

                    /**
                     * Creates a plain object from a HistorySyncChunkRetryResponse message. Also converts values to other types if specified.
                     * @function toObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse} message HistorySyncChunkRetryResponse
                     * @param {$protobuf.IConversionOptions} [options] Conversion options
                     * @returns {Object.<string,*>} Plain object
                     */
                    HistorySyncChunkRetryResponse.toObject = function toObject(message, options, q) {
                        if (!options)
                            options = {};
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var object = {};
                        if (message.syncType != null && Object.hasOwnProperty.call(message, "syncType"))
                            object.syncType = options.enums === String ? $root.proto.Message.HistorySyncType[message.syncType] === undefined ? message.syncType : $root.proto.Message.HistorySyncType[message.syncType] : message.syncType;
                        if (message.chunkOrder != null && Object.hasOwnProperty.call(message, "chunkOrder"))
                            object.chunkOrder = message.chunkOrder;
                        if (message.requestId != null && Object.hasOwnProperty.call(message, "requestId"))
                            object.requestId = message.requestId;
                        if (message.responseCode != null && Object.hasOwnProperty.call(message, "responseCode"))
                            object.responseCode = options.enums === String ? $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponseCode[message.responseCode] === undefined ? message.responseCode : $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponseCode[message.responseCode] : message.responseCode;
                        if (message.canRecover != null && Object.hasOwnProperty.call(message, "canRecover"))
                            object.canRecover = message.canRecover;
                        return object;
                    };

                    /**
                     * Converts this HistorySyncChunkRetryResponse to JSON.
                     * @function toJSON
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse
                     * @instance
                     * @returns {Object.<string,*>} JSON object
                     */
                    HistorySyncChunkRetryResponse.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    /**
                     * Gets the default type url for HistorySyncChunkRetryResponse
                     * @function getTypeUrl
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse
                     * @static
                     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                     * @returns {string} The default type url
                     */
                    HistorySyncChunkRetryResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse";
                    };

                    return HistorySyncChunkRetryResponse;
                })();

                /**
                 * HistorySyncChunkRetryResponseCode enum.
                 * @name proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponseCode
                 * @enum {number}
                 * @property {number} GENERATION_ERROR=1 GENERATION_ERROR value
                 * @property {number} CHUNK_CONSUMED=2 CHUNK_CONSUMED value
                 * @property {number} TIMEOUT=3 TIMEOUT value
                 * @property {number} SESSION_EXHAUSTED=4 SESSION_EXHAUSTED value
                 * @property {number} CHUNK_EXHAUSTED=5 CHUNK_EXHAUSTED value
                 * @property {number} DUPLICATED_REQUEST=6 DUPLICATED_REQUEST value
                 */
                PeerDataOperationResult.HistorySyncChunkRetryResponseCode = (function() {
                    var valuesById = {}, values = Object.create(valuesById);
                    values[valuesById[1] = "GENERATION_ERROR"] = 1;
                    values[valuesById[2] = "CHUNK_CONSUMED"] = 2;
                    values[valuesById[3] = "TIMEOUT"] = 3;
                    values[valuesById[4] = "SESSION_EXHAUSTED"] = 4;
                    values[valuesById[5] = "CHUNK_EXHAUSTED"] = 5;
                    values[valuesById[6] = "DUPLICATED_REQUEST"] = 6;
                    return values;
                })();

                PeerDataOperationResult.LinkPreviewResponse = (function() {

                    /**
                     * Properties of a LinkPreviewResponse.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @interface ILinkPreviewResponse
                     * @property {string|null} [url] LinkPreviewResponse url
                     * @property {string|null} [title] LinkPreviewResponse title
                     * @property {string|null} [description] LinkPreviewResponse description
                     * @property {Uint8Array|null} [thumbData] LinkPreviewResponse thumbData
                     * @property {string|null} [matchText] LinkPreviewResponse matchText
                     * @property {string|null} [previewType] LinkPreviewResponse previewType
                     * @property {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.ILinkPreviewHighQualityThumbnail|null} [hqThumbnail] LinkPreviewResponse hqThumbnail
                     * @property {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.IPaymentLinkPreviewMetadata|null} [previewMetadata] LinkPreviewResponse previewMetadata
                     */

                    /**
                     * Constructs a new LinkPreviewResponse.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @classdesc Represents a LinkPreviewResponse.
                     * @implements ILinkPreviewResponse
                     * @constructor
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ILinkPreviewResponse=} [properties] Properties to set
                     */
                    function LinkPreviewResponse(properties) {
                        if (properties)
                            for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    /**
                     * LinkPreviewResponse url.
                     * @member {string|null|undefined} url
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                     * @instance
                     */
                    LinkPreviewResponse.prototype.url = null;

                    /**
                     * LinkPreviewResponse title.
                     * @member {string|null|undefined} title
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                     * @instance
                     */
                    LinkPreviewResponse.prototype.title = null;

                    /**
                     * LinkPreviewResponse description.
                     * @member {string|null|undefined} description
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                     * @instance
                     */
                    LinkPreviewResponse.prototype.description = null;

                    /**
                     * LinkPreviewResponse thumbData.
                     * @member {Uint8Array|null|undefined} thumbData
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                     * @instance
                     */
                    LinkPreviewResponse.prototype.thumbData = null;

                    /**
                     * LinkPreviewResponse matchText.
                     * @member {string|null|undefined} matchText
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                     * @instance
                     */
                    LinkPreviewResponse.prototype.matchText = null;

                    /**
                     * LinkPreviewResponse previewType.
                     * @member {string|null|undefined} previewType
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                     * @instance
                     */
                    LinkPreviewResponse.prototype.previewType = null;

                    /**
                     * LinkPreviewResponse hqThumbnail.
                     * @member {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.ILinkPreviewHighQualityThumbnail|null|undefined} hqThumbnail
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                     * @instance
                     */
                    LinkPreviewResponse.prototype.hqThumbnail = null;

                    /**
                     * LinkPreviewResponse previewMetadata.
                     * @member {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.IPaymentLinkPreviewMetadata|null|undefined} previewMetadata
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                     * @instance
                     */
                    LinkPreviewResponse.prototype.previewMetadata = null;

                    // OneOf field names bound to virtual getters and setters
                    var $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(LinkPreviewResponse.prototype, "_url", {
                        get: $util.oneOfGetter($oneOfFields = ["url"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(LinkPreviewResponse.prototype, "_title", {
                        get: $util.oneOfGetter($oneOfFields = ["title"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(LinkPreviewResponse.prototype, "_description", {
                        get: $util.oneOfGetter($oneOfFields = ["description"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(LinkPreviewResponse.prototype, "_thumbData", {
                        get: $util.oneOfGetter($oneOfFields = ["thumbData"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(LinkPreviewResponse.prototype, "_matchText", {
                        get: $util.oneOfGetter($oneOfFields = ["matchText"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(LinkPreviewResponse.prototype, "_previewType", {
                        get: $util.oneOfGetter($oneOfFields = ["previewType"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(LinkPreviewResponse.prototype, "_hqThumbnail", {
                        get: $util.oneOfGetter($oneOfFields = ["hqThumbnail"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(LinkPreviewResponse.prototype, "_previewMetadata", {
                        get: $util.oneOfGetter($oneOfFields = ["previewMetadata"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    /**
                     * Creates a new LinkPreviewResponse instance using the specified properties.
                     * @function create
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ILinkPreviewResponse=} [properties] Properties to set
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse} LinkPreviewResponse instance
                     */
                    LinkPreviewResponse.create = function create(properties) {
                        return new LinkPreviewResponse(properties);
                    };

                    /**
                     * Encodes the specified LinkPreviewResponse message. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.verify|verify} messages.
                     * @function encode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ILinkPreviewResponse} message LinkPreviewResponse message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    LinkPreviewResponse.encode = function encode(message, writer, q) {
                        if (!writer)
                            writer = $Writer.create();
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                            writer.uint32(/* id 1, wireType 2 =*/10).string(message.url);
                        if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                            writer.uint32(/* id 2, wireType 2 =*/18).string(message.title);
                        if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                            writer.uint32(/* id 3, wireType 2 =*/26).string(message.description);
                        if (message.thumbData != null && Object.hasOwnProperty.call(message, "thumbData"))
                            writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.thumbData);
                        if (message.matchText != null && Object.hasOwnProperty.call(message, "matchText"))
                            writer.uint32(/* id 6, wireType 2 =*/50).string(message.matchText);
                        if (message.previewType != null && Object.hasOwnProperty.call(message, "previewType"))
                            writer.uint32(/* id 7, wireType 2 =*/58).string(message.previewType);
                        if (message.hqThumbnail != null && Object.hasOwnProperty.call(message, "hqThumbnail"))
                            $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail.encode(message.hqThumbnail, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
                        if (message.previewMetadata != null && Object.hasOwnProperty.call(message, "previewMetadata"))
                            $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata.encode(message.previewMetadata, writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (var i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    /**
                     * Encodes the specified LinkPreviewResponse message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.verify|verify} messages.
                     * @function encodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ILinkPreviewResponse} message LinkPreviewResponse message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    LinkPreviewResponse.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                    };

                    /**
                     * Decodes a LinkPreviewResponse message from the specified reader or buffer.
                     * @function decode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @param {number} [length] Message length if known beforehand
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse} LinkPreviewResponse
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    LinkPreviewResponse.decode = function decode(reader, length, z, q, g) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (q === undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                        while (reader.pos < end) {
                            var s = reader.pos;
                            var tag = reader.tag();
                            if (tag === z) {
                                z = undefined;
                                break;
                            }
                            var u = tag & 7;
                            switch (tag >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    message.url = reader.stringVerify();
                                    message._url = "url";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 2)
                                        break;
                                    message.title = reader.stringVerify();
                                    message._title = "title";
                                    continue;
                                }
                            case 3: {
                                    if (u !== 2)
                                        break;
                                    message.description = reader.stringVerify();
                                    message._description = "description";
                                    continue;
                                }
                            case 4: {
                                    if (u !== 2)
                                        break;
                                    message.thumbData = reader.bytes();
                                    message._thumbData = "thumbData";
                                    continue;
                                }
                            case 6: {
                                    if (u !== 2)
                                        break;
                                    message.matchText = reader.stringVerify();
                                    message._matchText = "matchText";
                                    continue;
                                }
                            case 7: {
                                    if (u !== 2)
                                        break;
                                    message.previewType = reader.stringVerify();
                                    message._previewType = "previewType";
                                    continue;
                                }
                            case 8: {
                                    if (u !== 2)
                                        break;
                                    message.hqThumbnail = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail.decode(reader, reader.uint32(), undefined, q + 1, message.hqThumbnail);
                                    message._hqThumbnail = "hqThumbnail";
                                    continue;
                                }
                            case 9: {
                                    if (u !== 2)
                                        break;
                                    message.previewMetadata = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.previewMetadata);
                                    message._previewMetadata = "previewMetadata";
                                    continue;
                                }
                            }
                            reader.skipType(u, q, tag);
                            if (!reader.discardUnknown) {
                                $util.makeProp(message, "$unknowns", false);
                                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                            }
                        }
                        if (z !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    /**
                     * Decodes a LinkPreviewResponse message from the specified reader or buffer, length delimited.
                     * @function decodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse} LinkPreviewResponse
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    LinkPreviewResponse.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    /**
                     * Verifies a LinkPreviewResponse message.
                     * @function verify
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                     * @static
                     * @param {Object.<string,*>} message Plain object to verify
                     * @returns {string|null} `null` if valid, otherwise the reason why it is not
                     */
                    LinkPreviewResponse.verify = function verify(message, q) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            return "max depth exceeded";
                        var properties = {};
                        if (message.url != null && Object.hasOwnProperty.call(message, "url")) {
                            properties._url = 1;
                            if (!$util.isString(message.url))
                                return "url: string expected";
                        }
                        if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                            properties._title = 1;
                            if (!$util.isString(message.title))
                                return "title: string expected";
                        }
                        if (message.description != null && Object.hasOwnProperty.call(message, "description")) {
                            properties._description = 1;
                            if (!$util.isString(message.description))
                                return "description: string expected";
                        }
                        if (message.thumbData != null && Object.hasOwnProperty.call(message, "thumbData")) {
                            properties._thumbData = 1;
                            if (!(message.thumbData && typeof message.thumbData.length === "number" || $util.isString(message.thumbData)))
                                return "thumbData: buffer expected";
                        }
                        if (message.matchText != null && Object.hasOwnProperty.call(message, "matchText")) {
                            properties._matchText = 1;
                            if (!$util.isString(message.matchText))
                                return "matchText: string expected";
                        }
                        if (message.previewType != null && Object.hasOwnProperty.call(message, "previewType")) {
                            properties._previewType = 1;
                            if (!$util.isString(message.previewType))
                                return "previewType: string expected";
                        }
                        if (message.hqThumbnail != null && Object.hasOwnProperty.call(message, "hqThumbnail")) {
                            properties._hqThumbnail = 1;
                            {
                                var error = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail.verify(message.hqThumbnail, q + 1);
                                if (error)
                                    return "hqThumbnail." + error;
                            }
                        }
                        if (message.previewMetadata != null && Object.hasOwnProperty.call(message, "previewMetadata")) {
                            properties._previewMetadata = 1;
                            {
                                var error = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata.verify(message.previewMetadata, q + 1);
                                if (error)
                                    return "previewMetadata." + error;
                            }
                        }
                        return null;
                    };

                    /**
                     * Creates a LinkPreviewResponse message from a plain object. Also converts values to their respective internal types.
                     * @function fromObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                     * @static
                     * @param {Object.<string,*>} object Plain object
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse} LinkPreviewResponse
                     */
                    LinkPreviewResponse.fromObject = function fromObject(object, q) {
                        if (object instanceof C)
                            return object;
                        if (!$util.isObject(object))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse: object expected");
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var message = new C();
                        if (object.url != null)
                            message.url = String(object.url);
                        if (object.title != null)
                            message.title = String(object.title);
                        if (object.description != null)
                            message.description = String(object.description);
                        if (object.thumbData != null)
                            if (typeof object.thumbData === "string")
                                $util.base64.decode(object.thumbData, message.thumbData = $util.newBuffer($util.base64.length(object.thumbData)), 0);
                            else if (object.thumbData.length >= 0)
                                message.thumbData = object.thumbData;
                        if (object.matchText != null)
                            message.matchText = String(object.matchText);
                        if (object.previewType != null)
                            message.previewType = String(object.previewType);
                        if (object.hqThumbnail != null) {
                            if (!$util.isObject(object.hqThumbnail))
                                throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.hqThumbnail: object expected");
                            message.hqThumbnail = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail.fromObject(object.hqThumbnail, q + 1);
                        }
                        if (object.previewMetadata != null) {
                            if (!$util.isObject(object.previewMetadata))
                                throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.previewMetadata: object expected");
                            message.previewMetadata = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata.fromObject(object.previewMetadata, q + 1);
                        }
                        return message;
                    };

                    /**
                     * Creates a plain object from a LinkPreviewResponse message. Also converts values to other types if specified.
                     * @function toObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse} message LinkPreviewResponse
                     * @param {$protobuf.IConversionOptions} [options] Conversion options
                     * @returns {Object.<string,*>} Plain object
                     */
                    LinkPreviewResponse.toObject = function toObject(message, options, q) {
                        if (!options)
                            options = {};
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var object = {};
                        if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                            object.url = message.url;
                        if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                            object.title = message.title;
                        if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                            object.description = message.description;
                        if (message.thumbData != null && Object.hasOwnProperty.call(message, "thumbData"))
                            object.thumbData = options.bytes === String ? $util.base64.encode(message.thumbData, 0, message.thumbData.length) : options.bytes === Array ? Array.prototype.slice.call(message.thumbData) : message.thumbData;
                        if (message.matchText != null && Object.hasOwnProperty.call(message, "matchText"))
                            object.matchText = message.matchText;
                        if (message.previewType != null && Object.hasOwnProperty.call(message, "previewType"))
                            object.previewType = message.previewType;
                        if (message.hqThumbnail != null && Object.hasOwnProperty.call(message, "hqThumbnail"))
                            object.hqThumbnail = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail.toObject(message.hqThumbnail, options, q + 1);
                        if (message.previewMetadata != null && Object.hasOwnProperty.call(message, "previewMetadata"))
                            object.previewMetadata = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata.toObject(message.previewMetadata, options, q + 1);
                        return object;
                    };

                    /**
                     * Converts this LinkPreviewResponse to JSON.
                     * @function toJSON
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                     * @instance
                     * @returns {Object.<string,*>} JSON object
                     */
                    LinkPreviewResponse.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    /**
                     * Gets the default type url for LinkPreviewResponse
                     * @function getTypeUrl
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                     * @static
                     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                     * @returns {string} The default type url
                     */
                    LinkPreviewResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse";
                    };

                    LinkPreviewResponse.LinkPreviewHighQualityThumbnail = (function() {

                        /**
                         * Properties of a LinkPreviewHighQualityThumbnail.
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                         * @interface ILinkPreviewHighQualityThumbnail
                         * @property {string|null} [directPath] LinkPreviewHighQualityThumbnail directPath
                         * @property {string|null} [thumbHash] LinkPreviewHighQualityThumbnail thumbHash
                         * @property {string|null} [encThumbHash] LinkPreviewHighQualityThumbnail encThumbHash
                         * @property {Uint8Array|null} [mediaKey] LinkPreviewHighQualityThumbnail mediaKey
                         * @property {number|Long|null} [mediaKeyTimestampMs] LinkPreviewHighQualityThumbnail mediaKeyTimestampMs
                         * @property {number|null} [thumbWidth] LinkPreviewHighQualityThumbnail thumbWidth
                         * @property {number|null} [thumbHeight] LinkPreviewHighQualityThumbnail thumbHeight
                         */

                        /**
                         * Constructs a new LinkPreviewHighQualityThumbnail.
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                         * @classdesc Represents a LinkPreviewHighQualityThumbnail.
                         * @implements ILinkPreviewHighQualityThumbnail
                         * @constructor
                         * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.ILinkPreviewHighQualityThumbnail=} [properties] Properties to set
                         */
                        function LinkPreviewHighQualityThumbnail(properties) {
                            if (properties)
                                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        }

                        /**
                         * LinkPreviewHighQualityThumbnail directPath.
                         * @member {string|null|undefined} directPath
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail
                         * @instance
                         */
                        LinkPreviewHighQualityThumbnail.prototype.directPath = null;

                        /**
                         * LinkPreviewHighQualityThumbnail thumbHash.
                         * @member {string|null|undefined} thumbHash
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail
                         * @instance
                         */
                        LinkPreviewHighQualityThumbnail.prototype.thumbHash = null;

                        /**
                         * LinkPreviewHighQualityThumbnail encThumbHash.
                         * @member {string|null|undefined} encThumbHash
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail
                         * @instance
                         */
                        LinkPreviewHighQualityThumbnail.prototype.encThumbHash = null;

                        /**
                         * LinkPreviewHighQualityThumbnail mediaKey.
                         * @member {Uint8Array|null|undefined} mediaKey
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail
                         * @instance
                         */
                        LinkPreviewHighQualityThumbnail.prototype.mediaKey = null;

                        /**
                         * LinkPreviewHighQualityThumbnail mediaKeyTimestampMs.
                         * @member {number|Long|null|undefined} mediaKeyTimestampMs
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail
                         * @instance
                         */
                        LinkPreviewHighQualityThumbnail.prototype.mediaKeyTimestampMs = null;

                        /**
                         * LinkPreviewHighQualityThumbnail thumbWidth.
                         * @member {number|null|undefined} thumbWidth
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail
                         * @instance
                         */
                        LinkPreviewHighQualityThumbnail.prototype.thumbWidth = null;

                        /**
                         * LinkPreviewHighQualityThumbnail thumbHeight.
                         * @member {number|null|undefined} thumbHeight
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail
                         * @instance
                         */
                        LinkPreviewHighQualityThumbnail.prototype.thumbHeight = null;

                        // OneOf field names bound to virtual getters and setters
                        var $oneOfFields;

                        // Virtual OneOf for proto3 optional field
                        Object.defineProperty(LinkPreviewHighQualityThumbnail.prototype, "_directPath", {
                            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        Object.defineProperty(LinkPreviewHighQualityThumbnail.prototype, "_thumbHash", {
                            get: $util.oneOfGetter($oneOfFields = ["thumbHash"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        Object.defineProperty(LinkPreviewHighQualityThumbnail.prototype, "_encThumbHash", {
                            get: $util.oneOfGetter($oneOfFields = ["encThumbHash"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        Object.defineProperty(LinkPreviewHighQualityThumbnail.prototype, "_mediaKey", {
                            get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        Object.defineProperty(LinkPreviewHighQualityThumbnail.prototype, "_mediaKeyTimestampMs", {
                            get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestampMs"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        Object.defineProperty(LinkPreviewHighQualityThumbnail.prototype, "_thumbWidth", {
                            get: $util.oneOfGetter($oneOfFields = ["thumbWidth"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        Object.defineProperty(LinkPreviewHighQualityThumbnail.prototype, "_thumbHeight", {
                            get: $util.oneOfGetter($oneOfFields = ["thumbHeight"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        /**
                         * Creates a new LinkPreviewHighQualityThumbnail instance using the specified properties.
                         * @function create
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail
                         * @static
                         * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.ILinkPreviewHighQualityThumbnail=} [properties] Properties to set
                         * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail} LinkPreviewHighQualityThumbnail instance
                         */
                        LinkPreviewHighQualityThumbnail.create = function create(properties) {
                            return new LinkPreviewHighQualityThumbnail(properties);
                        };

                        /**
                         * Encodes the specified LinkPreviewHighQualityThumbnail message. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail.verify|verify} messages.
                         * @function encode
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail
                         * @static
                         * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.ILinkPreviewHighQualityThumbnail} message LinkPreviewHighQualityThumbnail message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        LinkPreviewHighQualityThumbnail.encode = function encode(message, writer, q) {
                            if (!writer)
                                writer = $Writer.create();
                            if (q === undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw Error("max depth exceeded");
                            if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                                writer.uint32(/* id 1, wireType 2 =*/10).string(message.directPath);
                            if (message.thumbHash != null && Object.hasOwnProperty.call(message, "thumbHash"))
                                writer.uint32(/* id 2, wireType 2 =*/18).string(message.thumbHash);
                            if (message.encThumbHash != null && Object.hasOwnProperty.call(message, "encThumbHash"))
                                writer.uint32(/* id 3, wireType 2 =*/26).string(message.encThumbHash);
                            if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.mediaKey);
                            if (message.mediaKeyTimestampMs != null && Object.hasOwnProperty.call(message, "mediaKeyTimestampMs"))
                                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.mediaKeyTimestampMs);
                            if (message.thumbWidth != null && Object.hasOwnProperty.call(message, "thumbWidth"))
                                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.thumbWidth);
                            if (message.thumbHeight != null && Object.hasOwnProperty.call(message, "thumbHeight"))
                                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.thumbHeight);
                            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                                for (var i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified LinkPreviewHighQualityThumbnail message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail
                         * @static
                         * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.ILinkPreviewHighQualityThumbnail} message LinkPreviewHighQualityThumbnail message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        LinkPreviewHighQualityThumbnail.encodeDelimited = function encodeDelimited(message, writer) {
                            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                        };

                        /**
                         * Decodes a LinkPreviewHighQualityThumbnail message from the specified reader or buffer.
                         * @function decode
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail} LinkPreviewHighQualityThumbnail
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        LinkPreviewHighQualityThumbnail.decode = function decode(reader, length, z, q, g) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (q === undefined)
                                q = 0;
                            if (q > $Reader.recursionLimit)
                                throw Error("max depth exceeded");
                            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                            while (reader.pos < end) {
                                var s = reader.pos;
                                var tag = reader.tag();
                                if (tag === z) {
                                    z = undefined;
                                    break;
                                }
                                var u = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (u !== 2)
                                            break;
                                        message.directPath = reader.stringVerify();
                                        message._directPath = "directPath";
                                        continue;
                                    }
                                case 2: {
                                        if (u !== 2)
                                            break;
                                        message.thumbHash = reader.stringVerify();
                                        message._thumbHash = "thumbHash";
                                        continue;
                                    }
                                case 3: {
                                        if (u !== 2)
                                            break;
                                        message.encThumbHash = reader.stringVerify();
                                        message._encThumbHash = "encThumbHash";
                                        continue;
                                    }
                                case 4: {
                                        if (u !== 2)
                                            break;
                                        message.mediaKey = reader.bytes();
                                        message._mediaKey = "mediaKey";
                                        continue;
                                    }
                                case 5: {
                                        if (u !== 0)
                                            break;
                                        message.mediaKeyTimestampMs = reader.int64();
                                        message._mediaKeyTimestampMs = "mediaKeyTimestampMs";
                                        continue;
                                    }
                                case 6: {
                                        if (u !== 0)
                                            break;
                                        message.thumbWidth = reader.int32();
                                        message._thumbWidth = "thumbWidth";
                                        continue;
                                    }
                                case 7: {
                                        if (u !== 0)
                                            break;
                                        message.thumbHeight = reader.int32();
                                        message._thumbHeight = "thumbHeight";
                                        continue;
                                    }
                                }
                                reader.skipType(u, q, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                                }
                            }
                            if (z !== undefined)
                                throw Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a LinkPreviewHighQualityThumbnail message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail} LinkPreviewHighQualityThumbnail
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        LinkPreviewHighQualityThumbnail.decodeDelimited = function decodeDelimited(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a LinkPreviewHighQualityThumbnail message.
                         * @function verify
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        LinkPreviewHighQualityThumbnail.verify = function verify(message, q) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (q === undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                return "max depth exceeded";
                            var properties = {};
                            if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath")) {
                                properties._directPath = 1;
                                if (!$util.isString(message.directPath))
                                    return "directPath: string expected";
                            }
                            if (message.thumbHash != null && Object.hasOwnProperty.call(message, "thumbHash")) {
                                properties._thumbHash = 1;
                                if (!$util.isString(message.thumbHash))
                                    return "thumbHash: string expected";
                            }
                            if (message.encThumbHash != null && Object.hasOwnProperty.call(message, "encThumbHash")) {
                                properties._encThumbHash = 1;
                                if (!$util.isString(message.encThumbHash))
                                    return "encThumbHash: string expected";
                            }
                            if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey")) {
                                properties._mediaKey = 1;
                                if (!(message.mediaKey && typeof message.mediaKey.length === "number" || $util.isString(message.mediaKey)))
                                    return "mediaKey: buffer expected";
                            }
                            if (message.mediaKeyTimestampMs != null && Object.hasOwnProperty.call(message, "mediaKeyTimestampMs")) {
                                properties._mediaKeyTimestampMs = 1;
                                if (!$util.isInteger(message.mediaKeyTimestampMs) && !(message.mediaKeyTimestampMs && $util.isInteger(message.mediaKeyTimestampMs.low) && $util.isInteger(message.mediaKeyTimestampMs.high)))
                                    return "mediaKeyTimestampMs: integer|Long expected";
                            }
                            if (message.thumbWidth != null && Object.hasOwnProperty.call(message, "thumbWidth")) {
                                properties._thumbWidth = 1;
                                if (!$util.isInteger(message.thumbWidth))
                                    return "thumbWidth: integer expected";
                            }
                            if (message.thumbHeight != null && Object.hasOwnProperty.call(message, "thumbHeight")) {
                                properties._thumbHeight = 1;
                                if (!$util.isInteger(message.thumbHeight))
                                    return "thumbHeight: integer expected";
                            }
                            return null;
                        };

                        /**
                         * Creates a LinkPreviewHighQualityThumbnail message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail} LinkPreviewHighQualityThumbnail
                         */
                        LinkPreviewHighQualityThumbnail.fromObject = function fromObject(object, q) {
                            if (object instanceof C)
                                return object;
                            if (!$util.isObject(object))
                                throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail: object expected");
                            if (q === undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw Error("max depth exceeded");
                            var message = new C();
                            if (object.directPath != null)
                                message.directPath = String(object.directPath);
                            if (object.thumbHash != null)
                                message.thumbHash = String(object.thumbHash);
                            if (object.encThumbHash != null)
                                message.encThumbHash = String(object.encThumbHash);
                            if (object.mediaKey != null)
                                if (typeof object.mediaKey === "string")
                                    $util.base64.decode(object.mediaKey, message.mediaKey = $util.newBuffer($util.base64.length(object.mediaKey)), 0);
                                else if (object.mediaKey.length >= 0)
                                    message.mediaKey = object.mediaKey;
                            if (object.mediaKeyTimestampMs != null)
                                if ($util.Long)
                                    message.mediaKeyTimestampMs = $util.Long.fromValue(object.mediaKeyTimestampMs, false);
                                else if (typeof object.mediaKeyTimestampMs === "string")
                                    message.mediaKeyTimestampMs = parseInt(object.mediaKeyTimestampMs, 10);
                                else if (typeof object.mediaKeyTimestampMs === "number")
                                    message.mediaKeyTimestampMs = object.mediaKeyTimestampMs;
                                else if (typeof object.mediaKeyTimestampMs === "object")
                                    message.mediaKeyTimestampMs = new $util.LongBits(object.mediaKeyTimestampMs.low >>> 0, object.mediaKeyTimestampMs.high >>> 0).toNumber();
                            if (object.thumbWidth != null)
                                message.thumbWidth = object.thumbWidth | 0;
                            if (object.thumbHeight != null)
                                message.thumbHeight = object.thumbHeight | 0;
                            return message;
                        };

                        /**
                         * Creates a plain object from a LinkPreviewHighQualityThumbnail message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail
                         * @static
                         * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail} message LinkPreviewHighQualityThumbnail
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        LinkPreviewHighQualityThumbnail.toObject = function toObject(message, options, q) {
                            if (!options)
                                options = {};
                            if (q === undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw Error("max depth exceeded");
                            var object = {};
                            if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                                object.directPath = message.directPath;
                            if (message.thumbHash != null && Object.hasOwnProperty.call(message, "thumbHash"))
                                object.thumbHash = message.thumbHash;
                            if (message.encThumbHash != null && Object.hasOwnProperty.call(message, "encThumbHash"))
                                object.encThumbHash = message.encThumbHash;
                            if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                                object.mediaKey = options.bytes === String ? $util.base64.encode(message.mediaKey, 0, message.mediaKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.mediaKey) : message.mediaKey;
                            if (message.mediaKeyTimestampMs != null && Object.hasOwnProperty.call(message, "mediaKeyTimestampMs"))
                                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                                    object.mediaKeyTimestampMs = typeof message.mediaKeyTimestampMs === "number" ? BigInt(message.mediaKeyTimestampMs) : $util.Long.fromBits(message.mediaKeyTimestampMs.low >>> 0, message.mediaKeyTimestampMs.high >>> 0, false).toBigInt();
                                else if (typeof message.mediaKeyTimestampMs === "number")
                                    object.mediaKeyTimestampMs = options.longs === String ? String(message.mediaKeyTimestampMs) : message.mediaKeyTimestampMs;
                                else
                                    object.mediaKeyTimestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.mediaKeyTimestampMs) : options.longs === Number ? new $util.LongBits(message.mediaKeyTimestampMs.low >>> 0, message.mediaKeyTimestampMs.high >>> 0).toNumber() : message.mediaKeyTimestampMs;
                            if (message.thumbWidth != null && Object.hasOwnProperty.call(message, "thumbWidth"))
                                object.thumbWidth = message.thumbWidth;
                            if (message.thumbHeight != null && Object.hasOwnProperty.call(message, "thumbHeight"))
                                object.thumbHeight = message.thumbHeight;
                            return object;
                        };

                        /**
                         * Converts this LinkPreviewHighQualityThumbnail to JSON.
                         * @function toJSON
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        LinkPreviewHighQualityThumbnail.prototype.toJSON = function toJSON() {
                            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the default type url for LinkPreviewHighQualityThumbnail
                         * @function getTypeUrl
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail
                         * @static
                         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                         * @returns {string} The default type url
                         */
                        LinkPreviewHighQualityThumbnail.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                            if (typeUrlPrefix === undefined) {
                                typeUrlPrefix = "type.googleapis.com";
                            }
                            return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail";
                        };

                        return LinkPreviewHighQualityThumbnail;
                    })();

                    LinkPreviewResponse.PaymentLinkPreviewMetadata = (function() {

                        /**
                         * Properties of a PaymentLinkPreviewMetadata.
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                         * @interface IPaymentLinkPreviewMetadata
                         * @property {boolean|null} [isBusinessVerified] PaymentLinkPreviewMetadata isBusinessVerified
                         * @property {string|null} [providerName] PaymentLinkPreviewMetadata providerName
                         * @property {string|null} [amount] PaymentLinkPreviewMetadata amount
                         * @property {string|null} [offset] PaymentLinkPreviewMetadata offset
                         * @property {string|null} [currency] PaymentLinkPreviewMetadata currency
                         */

                        /**
                         * Constructs a new PaymentLinkPreviewMetadata.
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse
                         * @classdesc Represents a PaymentLinkPreviewMetadata.
                         * @implements IPaymentLinkPreviewMetadata
                         * @constructor
                         * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.IPaymentLinkPreviewMetadata=} [properties] Properties to set
                         */
                        function PaymentLinkPreviewMetadata(properties) {
                            if (properties)
                                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                        this[keys[i]] = properties[keys[i]];
                        }

                        /**
                         * PaymentLinkPreviewMetadata isBusinessVerified.
                         * @member {boolean|null|undefined} isBusinessVerified
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata
                         * @instance
                         */
                        PaymentLinkPreviewMetadata.prototype.isBusinessVerified = null;

                        /**
                         * PaymentLinkPreviewMetadata providerName.
                         * @member {string|null|undefined} providerName
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata
                         * @instance
                         */
                        PaymentLinkPreviewMetadata.prototype.providerName = null;

                        /**
                         * PaymentLinkPreviewMetadata amount.
                         * @member {string|null|undefined} amount
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata
                         * @instance
                         */
                        PaymentLinkPreviewMetadata.prototype.amount = null;

                        /**
                         * PaymentLinkPreviewMetadata offset.
                         * @member {string|null|undefined} offset
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata
                         * @instance
                         */
                        PaymentLinkPreviewMetadata.prototype.offset = null;

                        /**
                         * PaymentLinkPreviewMetadata currency.
                         * @member {string|null|undefined} currency
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata
                         * @instance
                         */
                        PaymentLinkPreviewMetadata.prototype.currency = null;

                        // OneOf field names bound to virtual getters and setters
                        var $oneOfFields;

                        // Virtual OneOf for proto3 optional field
                        Object.defineProperty(PaymentLinkPreviewMetadata.prototype, "_isBusinessVerified", {
                            get: $util.oneOfGetter($oneOfFields = ["isBusinessVerified"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        Object.defineProperty(PaymentLinkPreviewMetadata.prototype, "_providerName", {
                            get: $util.oneOfGetter($oneOfFields = ["providerName"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        Object.defineProperty(PaymentLinkPreviewMetadata.prototype, "_amount", {
                            get: $util.oneOfGetter($oneOfFields = ["amount"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        Object.defineProperty(PaymentLinkPreviewMetadata.prototype, "_offset", {
                            get: $util.oneOfGetter($oneOfFields = ["offset"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        Object.defineProperty(PaymentLinkPreviewMetadata.prototype, "_currency", {
                            get: $util.oneOfGetter($oneOfFields = ["currency"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        /**
                         * Creates a new PaymentLinkPreviewMetadata instance using the specified properties.
                         * @function create
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata
                         * @static
                         * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.IPaymentLinkPreviewMetadata=} [properties] Properties to set
                         * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata} PaymentLinkPreviewMetadata instance
                         */
                        PaymentLinkPreviewMetadata.create = function create(properties) {
                            return new PaymentLinkPreviewMetadata(properties);
                        };

                        /**
                         * Encodes the specified PaymentLinkPreviewMetadata message. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata.verify|verify} messages.
                         * @function encode
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata
                         * @static
                         * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.IPaymentLinkPreviewMetadata} message PaymentLinkPreviewMetadata message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        PaymentLinkPreviewMetadata.encode = function encode(message, writer, q) {
                            if (!writer)
                                writer = $Writer.create();
                            if (q === undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw Error("max depth exceeded");
                            if (message.isBusinessVerified != null && Object.hasOwnProperty.call(message, "isBusinessVerified"))
                                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.isBusinessVerified);
                            if (message.providerName != null && Object.hasOwnProperty.call(message, "providerName"))
                                writer.uint32(/* id 2, wireType 2 =*/18).string(message.providerName);
                            if (message.amount != null && Object.hasOwnProperty.call(message, "amount"))
                                writer.uint32(/* id 3, wireType 2 =*/26).string(message.amount);
                            if (message.offset != null && Object.hasOwnProperty.call(message, "offset"))
                                writer.uint32(/* id 4, wireType 2 =*/34).string(message.offset);
                            if (message.currency != null && Object.hasOwnProperty.call(message, "currency"))
                                writer.uint32(/* id 5, wireType 2 =*/42).string(message.currency);
                            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                                for (var i = 0; i < message.$unknowns.length; ++i)
                                    writer.raw(message.$unknowns[i]);
                            return writer;
                        };

                        /**
                         * Encodes the specified PaymentLinkPreviewMetadata message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata.verify|verify} messages.
                         * @function encodeDelimited
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata
                         * @static
                         * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.IPaymentLinkPreviewMetadata} message PaymentLinkPreviewMetadata message or plain object to encode
                         * @param {$protobuf.Writer} [writer] Writer to encode to
                         * @returns {$protobuf.Writer} Writer
                         */
                        PaymentLinkPreviewMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                        };

                        /**
                         * Decodes a PaymentLinkPreviewMetadata message from the specified reader or buffer.
                         * @function decode
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @param {number} [length] Message length if known beforehand
                         * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata} PaymentLinkPreviewMetadata
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        PaymentLinkPreviewMetadata.decode = function decode(reader, length, z, q, g) {
                            if (!(reader instanceof $Reader))
                                reader = $Reader.create(reader);
                            if (q === undefined)
                                q = 0;
                            if (q > $Reader.recursionLimit)
                                throw Error("max depth exceeded");
                            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                            while (reader.pos < end) {
                                var s = reader.pos;
                                var tag = reader.tag();
                                if (tag === z) {
                                    z = undefined;
                                    break;
                                }
                                var u = tag & 7;
                                switch (tag >>>= 3) {
                                case 1: {
                                        if (u !== 0)
                                            break;
                                        message.isBusinessVerified = reader.bool();
                                        message._isBusinessVerified = "isBusinessVerified";
                                        continue;
                                    }
                                case 2: {
                                        if (u !== 2)
                                            break;
                                        message.providerName = reader.stringVerify();
                                        message._providerName = "providerName";
                                        continue;
                                    }
                                case 3: {
                                        if (u !== 2)
                                            break;
                                        message.amount = reader.stringVerify();
                                        message._amount = "amount";
                                        continue;
                                    }
                                case 4: {
                                        if (u !== 2)
                                            break;
                                        message.offset = reader.stringVerify();
                                        message._offset = "offset";
                                        continue;
                                    }
                                case 5: {
                                        if (u !== 2)
                                            break;
                                        message.currency = reader.stringVerify();
                                        message._currency = "currency";
                                        continue;
                                    }
                                }
                                reader.skipType(u, q, tag);
                                if (!reader.discardUnknown) {
                                    $util.makeProp(message, "$unknowns", false);
                                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                                }
                            }
                            if (z !== undefined)
                                throw Error("missing end group");
                            return message;
                        };

                        /**
                         * Decodes a PaymentLinkPreviewMetadata message from the specified reader or buffer, length delimited.
                         * @function decodeDelimited
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata
                         * @static
                         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                         * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata} PaymentLinkPreviewMetadata
                         * @throws {Error} If the payload is not a reader or valid buffer
                         * @throws {$protobuf.util.ProtocolError} If required fields are missing
                         */
                        PaymentLinkPreviewMetadata.decodeDelimited = function decodeDelimited(reader) {
                            if (!(reader instanceof $Reader))
                                reader = new $Reader(reader);
                            return this.decode(reader, reader.uint32());
                        };

                        /**
                         * Verifies a PaymentLinkPreviewMetadata message.
                         * @function verify
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata
                         * @static
                         * @param {Object.<string,*>} message Plain object to verify
                         * @returns {string|null} `null` if valid, otherwise the reason why it is not
                         */
                        PaymentLinkPreviewMetadata.verify = function verify(message, q) {
                            if (typeof message !== "object" || message === null)
                                return "object expected";
                            if (q === undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                return "max depth exceeded";
                            var properties = {};
                            if (message.isBusinessVerified != null && Object.hasOwnProperty.call(message, "isBusinessVerified")) {
                                properties._isBusinessVerified = 1;
                                if (typeof message.isBusinessVerified !== "boolean")
                                    return "isBusinessVerified: boolean expected";
                            }
                            if (message.providerName != null && Object.hasOwnProperty.call(message, "providerName")) {
                                properties._providerName = 1;
                                if (!$util.isString(message.providerName))
                                    return "providerName: string expected";
                            }
                            if (message.amount != null && Object.hasOwnProperty.call(message, "amount")) {
                                properties._amount = 1;
                                if (!$util.isString(message.amount))
                                    return "amount: string expected";
                            }
                            if (message.offset != null && Object.hasOwnProperty.call(message, "offset")) {
                                properties._offset = 1;
                                if (!$util.isString(message.offset))
                                    return "offset: string expected";
                            }
                            if (message.currency != null && Object.hasOwnProperty.call(message, "currency")) {
                                properties._currency = 1;
                                if (!$util.isString(message.currency))
                                    return "currency: string expected";
                            }
                            return null;
                        };

                        /**
                         * Creates a PaymentLinkPreviewMetadata message from a plain object. Also converts values to their respective internal types.
                         * @function fromObject
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata
                         * @static
                         * @param {Object.<string,*>} object Plain object
                         * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata} PaymentLinkPreviewMetadata
                         */
                        PaymentLinkPreviewMetadata.fromObject = function fromObject(object, q) {
                            if (object instanceof C)
                                return object;
                            if (!$util.isObject(object))
                                throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata: object expected");
                            if (q === undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw Error("max depth exceeded");
                            var message = new C();
                            if (object.isBusinessVerified != null)
                                message.isBusinessVerified = Boolean(object.isBusinessVerified);
                            if (object.providerName != null)
                                message.providerName = String(object.providerName);
                            if (object.amount != null)
                                message.amount = String(object.amount);
                            if (object.offset != null)
                                message.offset = String(object.offset);
                            if (object.currency != null)
                                message.currency = String(object.currency);
                            return message;
                        };

                        /**
                         * Creates a plain object from a PaymentLinkPreviewMetadata message. Also converts values to other types if specified.
                         * @function toObject
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata
                         * @static
                         * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata} message PaymentLinkPreviewMetadata
                         * @param {$protobuf.IConversionOptions} [options] Conversion options
                         * @returns {Object.<string,*>} Plain object
                         */
                        PaymentLinkPreviewMetadata.toObject = function toObject(message, options, q) {
                            if (!options)
                                options = {};
                            if (q === undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw Error("max depth exceeded");
                            var object = {};
                            if (message.isBusinessVerified != null && Object.hasOwnProperty.call(message, "isBusinessVerified"))
                                object.isBusinessVerified = message.isBusinessVerified;
                            if (message.providerName != null && Object.hasOwnProperty.call(message, "providerName"))
                                object.providerName = message.providerName;
                            if (message.amount != null && Object.hasOwnProperty.call(message, "amount"))
                                object.amount = message.amount;
                            if (message.offset != null && Object.hasOwnProperty.call(message, "offset"))
                                object.offset = message.offset;
                            if (message.currency != null && Object.hasOwnProperty.call(message, "currency"))
                                object.currency = message.currency;
                            return object;
                        };

                        /**
                         * Converts this PaymentLinkPreviewMetadata to JSON.
                         * @function toJSON
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata
                         * @instance
                         * @returns {Object.<string,*>} JSON object
                         */
                        PaymentLinkPreviewMetadata.prototype.toJSON = function toJSON() {
                            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        /**
                         * Gets the default type url for PaymentLinkPreviewMetadata
                         * @function getTypeUrl
                         * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata
                         * @static
                         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                         * @returns {string} The default type url
                         */
                        PaymentLinkPreviewMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                            if (typeUrlPrefix === undefined) {
                                typeUrlPrefix = "type.googleapis.com";
                            }
                            return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata";
                        };

                        return PaymentLinkPreviewMetadata;
                    })();

                    return LinkPreviewResponse;
                })();

                PeerDataOperationResult.PlaceholderMessageResendResponse = (function() {

                    /**
                     * Properties of a PlaceholderMessageResendResponse.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @interface IPlaceholderMessageResendResponse
                     * @property {Uint8Array|null} [webMessageInfoBytes] PlaceholderMessageResendResponse webMessageInfoBytes
                     */

                    /**
                     * Constructs a new PlaceholderMessageResendResponse.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @classdesc Represents a PlaceholderMessageResendResponse.
                     * @implements IPlaceholderMessageResendResponse
                     * @constructor
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IPlaceholderMessageResendResponse=} [properties] Properties to set
                     */
                    function PlaceholderMessageResendResponse(properties) {
                        if (properties)
                            for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    /**
                     * PlaceholderMessageResendResponse webMessageInfoBytes.
                     * @member {Uint8Array|null|undefined} webMessageInfoBytes
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse
                     * @instance
                     */
                    PlaceholderMessageResendResponse.prototype.webMessageInfoBytes = null;

                    // OneOf field names bound to virtual getters and setters
                    var $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(PlaceholderMessageResendResponse.prototype, "_webMessageInfoBytes", {
                        get: $util.oneOfGetter($oneOfFields = ["webMessageInfoBytes"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    /**
                     * Creates a new PlaceholderMessageResendResponse instance using the specified properties.
                     * @function create
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IPlaceholderMessageResendResponse=} [properties] Properties to set
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse} PlaceholderMessageResendResponse instance
                     */
                    PlaceholderMessageResendResponse.create = function create(properties) {
                        return new PlaceholderMessageResendResponse(properties);
                    };

                    /**
                     * Encodes the specified PlaceholderMessageResendResponse message. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse.verify|verify} messages.
                     * @function encode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IPlaceholderMessageResendResponse} message PlaceholderMessageResendResponse message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    PlaceholderMessageResendResponse.encode = function encode(message, writer, q) {
                        if (!writer)
                            writer = $Writer.create();
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        if (message.webMessageInfoBytes != null && Object.hasOwnProperty.call(message, "webMessageInfoBytes"))
                            writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.webMessageInfoBytes);
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (var i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    /**
                     * Encodes the specified PlaceholderMessageResendResponse message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse.verify|verify} messages.
                     * @function encodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IPlaceholderMessageResendResponse} message PlaceholderMessageResendResponse message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    PlaceholderMessageResendResponse.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                    };

                    /**
                     * Decodes a PlaceholderMessageResendResponse message from the specified reader or buffer.
                     * @function decode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @param {number} [length] Message length if known beforehand
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse} PlaceholderMessageResendResponse
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    PlaceholderMessageResendResponse.decode = function decode(reader, length, z, q, g) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (q === undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                        while (reader.pos < end) {
                            var s = reader.pos;
                            var tag = reader.tag();
                            if (tag === z) {
                                z = undefined;
                                break;
                            }
                            var u = tag & 7;
                            switch (tag >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    message.webMessageInfoBytes = reader.bytes();
                                    message._webMessageInfoBytes = "webMessageInfoBytes";
                                    continue;
                                }
                            }
                            reader.skipType(u, q, tag);
                            if (!reader.discardUnknown) {
                                $util.makeProp(message, "$unknowns", false);
                                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                            }
                        }
                        if (z !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    /**
                     * Decodes a PlaceholderMessageResendResponse message from the specified reader or buffer, length delimited.
                     * @function decodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse} PlaceholderMessageResendResponse
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    PlaceholderMessageResendResponse.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    /**
                     * Verifies a PlaceholderMessageResendResponse message.
                     * @function verify
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse
                     * @static
                     * @param {Object.<string,*>} message Plain object to verify
                     * @returns {string|null} `null` if valid, otherwise the reason why it is not
                     */
                    PlaceholderMessageResendResponse.verify = function verify(message, q) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            return "max depth exceeded";
                        var properties = {};
                        if (message.webMessageInfoBytes != null && Object.hasOwnProperty.call(message, "webMessageInfoBytes")) {
                            properties._webMessageInfoBytes = 1;
                            if (!(message.webMessageInfoBytes && typeof message.webMessageInfoBytes.length === "number" || $util.isString(message.webMessageInfoBytes)))
                                return "webMessageInfoBytes: buffer expected";
                        }
                        return null;
                    };

                    /**
                     * Creates a PlaceholderMessageResendResponse message from a plain object. Also converts values to their respective internal types.
                     * @function fromObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse
                     * @static
                     * @param {Object.<string,*>} object Plain object
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse} PlaceholderMessageResendResponse
                     */
                    PlaceholderMessageResendResponse.fromObject = function fromObject(object, q) {
                        if (object instanceof C)
                            return object;
                        if (!$util.isObject(object))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse: object expected");
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var message = new C();
                        if (object.webMessageInfoBytes != null)
                            if (typeof object.webMessageInfoBytes === "string")
                                $util.base64.decode(object.webMessageInfoBytes, message.webMessageInfoBytes = $util.newBuffer($util.base64.length(object.webMessageInfoBytes)), 0);
                            else if (object.webMessageInfoBytes.length >= 0)
                                message.webMessageInfoBytes = object.webMessageInfoBytes;
                        return message;
                    };

                    /**
                     * Creates a plain object from a PlaceholderMessageResendResponse message. Also converts values to other types if specified.
                     * @function toObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse} message PlaceholderMessageResendResponse
                     * @param {$protobuf.IConversionOptions} [options] Conversion options
                     * @returns {Object.<string,*>} Plain object
                     */
                    PlaceholderMessageResendResponse.toObject = function toObject(message, options, q) {
                        if (!options)
                            options = {};
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var object = {};
                        if (message.webMessageInfoBytes != null && Object.hasOwnProperty.call(message, "webMessageInfoBytes"))
                            object.webMessageInfoBytes = options.bytes === String ? $util.base64.encode(message.webMessageInfoBytes, 0, message.webMessageInfoBytes.length) : options.bytes === Array ? Array.prototype.slice.call(message.webMessageInfoBytes) : message.webMessageInfoBytes;
                        return object;
                    };

                    /**
                     * Converts this PlaceholderMessageResendResponse to JSON.
                     * @function toJSON
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse
                     * @instance
                     * @returns {Object.<string,*>} JSON object
                     */
                    PlaceholderMessageResendResponse.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    /**
                     * Gets the default type url for PlaceholderMessageResendResponse
                     * @function getTypeUrl
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse
                     * @static
                     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                     * @returns {string} The default type url
                     */
                    PlaceholderMessageResendResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse";
                    };

                    return PlaceholderMessageResendResponse;
                })();

                PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse = (function() {

                    /**
                     * Properties of a SyncDSnapshotFatalRecoveryResponse.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @interface ISyncDSnapshotFatalRecoveryResponse
                     * @property {Uint8Array|null} [collectionSnapshot] SyncDSnapshotFatalRecoveryResponse collectionSnapshot
                     * @property {boolean|null} [isCompressed] SyncDSnapshotFatalRecoveryResponse isCompressed
                     */

                    /**
                     * Constructs a new SyncDSnapshotFatalRecoveryResponse.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @classdesc Represents a SyncDSnapshotFatalRecoveryResponse.
                     * @implements ISyncDSnapshotFatalRecoveryResponse
                     * @constructor
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ISyncDSnapshotFatalRecoveryResponse=} [properties] Properties to set
                     */
                    function SyncDSnapshotFatalRecoveryResponse(properties) {
                        if (properties)
                            for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    /**
                     * SyncDSnapshotFatalRecoveryResponse collectionSnapshot.
                     * @member {Uint8Array|null|undefined} collectionSnapshot
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse
                     * @instance
                     */
                    SyncDSnapshotFatalRecoveryResponse.prototype.collectionSnapshot = null;

                    /**
                     * SyncDSnapshotFatalRecoveryResponse isCompressed.
                     * @member {boolean|null|undefined} isCompressed
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse
                     * @instance
                     */
                    SyncDSnapshotFatalRecoveryResponse.prototype.isCompressed = null;

                    // OneOf field names bound to virtual getters and setters
                    var $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(SyncDSnapshotFatalRecoveryResponse.prototype, "_collectionSnapshot", {
                        get: $util.oneOfGetter($oneOfFields = ["collectionSnapshot"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(SyncDSnapshotFatalRecoveryResponse.prototype, "_isCompressed", {
                        get: $util.oneOfGetter($oneOfFields = ["isCompressed"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    /**
                     * Creates a new SyncDSnapshotFatalRecoveryResponse instance using the specified properties.
                     * @function create
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ISyncDSnapshotFatalRecoveryResponse=} [properties] Properties to set
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse} SyncDSnapshotFatalRecoveryResponse instance
                     */
                    SyncDSnapshotFatalRecoveryResponse.create = function create(properties) {
                        return new SyncDSnapshotFatalRecoveryResponse(properties);
                    };

                    /**
                     * Encodes the specified SyncDSnapshotFatalRecoveryResponse message. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse.verify|verify} messages.
                     * @function encode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ISyncDSnapshotFatalRecoveryResponse} message SyncDSnapshotFatalRecoveryResponse message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    SyncDSnapshotFatalRecoveryResponse.encode = function encode(message, writer, q) {
                        if (!writer)
                            writer = $Writer.create();
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        if (message.collectionSnapshot != null && Object.hasOwnProperty.call(message, "collectionSnapshot"))
                            writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.collectionSnapshot);
                        if (message.isCompressed != null && Object.hasOwnProperty.call(message, "isCompressed"))
                            writer.uint32(/* id 2, wireType 0 =*/16).bool(message.isCompressed);
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (var i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    /**
                     * Encodes the specified SyncDSnapshotFatalRecoveryResponse message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse.verify|verify} messages.
                     * @function encodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ISyncDSnapshotFatalRecoveryResponse} message SyncDSnapshotFatalRecoveryResponse message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    SyncDSnapshotFatalRecoveryResponse.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                    };

                    /**
                     * Decodes a SyncDSnapshotFatalRecoveryResponse message from the specified reader or buffer.
                     * @function decode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @param {number} [length] Message length if known beforehand
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse} SyncDSnapshotFatalRecoveryResponse
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    SyncDSnapshotFatalRecoveryResponse.decode = function decode(reader, length, z, q, g) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (q === undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                        while (reader.pos < end) {
                            var s = reader.pos;
                            var tag = reader.tag();
                            if (tag === z) {
                                z = undefined;
                                break;
                            }
                            var u = tag & 7;
                            switch (tag >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    message.collectionSnapshot = reader.bytes();
                                    message._collectionSnapshot = "collectionSnapshot";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 0)
                                        break;
                                    message.isCompressed = reader.bool();
                                    message._isCompressed = "isCompressed";
                                    continue;
                                }
                            }
                            reader.skipType(u, q, tag);
                            if (!reader.discardUnknown) {
                                $util.makeProp(message, "$unknowns", false);
                                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                            }
                        }
                        if (z !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    /**
                     * Decodes a SyncDSnapshotFatalRecoveryResponse message from the specified reader or buffer, length delimited.
                     * @function decodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse} SyncDSnapshotFatalRecoveryResponse
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    SyncDSnapshotFatalRecoveryResponse.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    /**
                     * Verifies a SyncDSnapshotFatalRecoveryResponse message.
                     * @function verify
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse
                     * @static
                     * @param {Object.<string,*>} message Plain object to verify
                     * @returns {string|null} `null` if valid, otherwise the reason why it is not
                     */
                    SyncDSnapshotFatalRecoveryResponse.verify = function verify(message, q) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            return "max depth exceeded";
                        var properties = {};
                        if (message.collectionSnapshot != null && Object.hasOwnProperty.call(message, "collectionSnapshot")) {
                            properties._collectionSnapshot = 1;
                            if (!(message.collectionSnapshot && typeof message.collectionSnapshot.length === "number" || $util.isString(message.collectionSnapshot)))
                                return "collectionSnapshot: buffer expected";
                        }
                        if (message.isCompressed != null && Object.hasOwnProperty.call(message, "isCompressed")) {
                            properties._isCompressed = 1;
                            if (typeof message.isCompressed !== "boolean")
                                return "isCompressed: boolean expected";
                        }
                        return null;
                    };

                    /**
                     * Creates a SyncDSnapshotFatalRecoveryResponse message from a plain object. Also converts values to their respective internal types.
                     * @function fromObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse
                     * @static
                     * @param {Object.<string,*>} object Plain object
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse} SyncDSnapshotFatalRecoveryResponse
                     */
                    SyncDSnapshotFatalRecoveryResponse.fromObject = function fromObject(object, q) {
                        if (object instanceof C)
                            return object;
                        if (!$util.isObject(object))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse: object expected");
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var message = new C();
                        if (object.collectionSnapshot != null)
                            if (typeof object.collectionSnapshot === "string")
                                $util.base64.decode(object.collectionSnapshot, message.collectionSnapshot = $util.newBuffer($util.base64.length(object.collectionSnapshot)), 0);
                            else if (object.collectionSnapshot.length >= 0)
                                message.collectionSnapshot = object.collectionSnapshot;
                        if (object.isCompressed != null)
                            message.isCompressed = Boolean(object.isCompressed);
                        return message;
                    };

                    /**
                     * Creates a plain object from a SyncDSnapshotFatalRecoveryResponse message. Also converts values to other types if specified.
                     * @function toObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse} message SyncDSnapshotFatalRecoveryResponse
                     * @param {$protobuf.IConversionOptions} [options] Conversion options
                     * @returns {Object.<string,*>} Plain object
                     */
                    SyncDSnapshotFatalRecoveryResponse.toObject = function toObject(message, options, q) {
                        if (!options)
                            options = {};
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var object = {};
                        if (message.collectionSnapshot != null && Object.hasOwnProperty.call(message, "collectionSnapshot"))
                            object.collectionSnapshot = options.bytes === String ? $util.base64.encode(message.collectionSnapshot, 0, message.collectionSnapshot.length) : options.bytes === Array ? Array.prototype.slice.call(message.collectionSnapshot) : message.collectionSnapshot;
                        if (message.isCompressed != null && Object.hasOwnProperty.call(message, "isCompressed"))
                            object.isCompressed = message.isCompressed;
                        return object;
                    };

                    /**
                     * Converts this SyncDSnapshotFatalRecoveryResponse to JSON.
                     * @function toJSON
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse
                     * @instance
                     * @returns {Object.<string,*>} JSON object
                     */
                    SyncDSnapshotFatalRecoveryResponse.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    /**
                     * Gets the default type url for SyncDSnapshotFatalRecoveryResponse
                     * @function getTypeUrl
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse
                     * @static
                     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                     * @returns {string} The default type url
                     */
                    SyncDSnapshotFatalRecoveryResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse";
                    };

                    return SyncDSnapshotFatalRecoveryResponse;
                })();

                PeerDataOperationResult.WaffleNonceFetchResponse = (function() {

                    /**
                     * Properties of a WaffleNonceFetchResponse.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @interface IWaffleNonceFetchResponse
                     * @property {string|null} [nonce] WaffleNonceFetchResponse nonce
                     * @property {string|null} [waEntFbid] WaffleNonceFetchResponse waEntFbid
                     */

                    /**
                     * Constructs a new WaffleNonceFetchResponse.
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult
                     * @classdesc Represents a WaffleNonceFetchResponse.
                     * @implements IWaffleNonceFetchResponse
                     * @constructor
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IWaffleNonceFetchResponse=} [properties] Properties to set
                     */
                    function WaffleNonceFetchResponse(properties) {
                        if (properties)
                            for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                    this[keys[i]] = properties[keys[i]];
                    }

                    /**
                     * WaffleNonceFetchResponse nonce.
                     * @member {string|null|undefined} nonce
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse
                     * @instance
                     */
                    WaffleNonceFetchResponse.prototype.nonce = null;

                    /**
                     * WaffleNonceFetchResponse waEntFbid.
                     * @member {string|null|undefined} waEntFbid
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse
                     * @instance
                     */
                    WaffleNonceFetchResponse.prototype.waEntFbid = null;

                    // OneOf field names bound to virtual getters and setters
                    var $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(WaffleNonceFetchResponse.prototype, "_nonce", {
                        get: $util.oneOfGetter($oneOfFields = ["nonce"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(WaffleNonceFetchResponse.prototype, "_waEntFbid", {
                        get: $util.oneOfGetter($oneOfFields = ["waEntFbid"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    /**
                     * Creates a new WaffleNonceFetchResponse instance using the specified properties.
                     * @function create
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IWaffleNonceFetchResponse=} [properties] Properties to set
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse} WaffleNonceFetchResponse instance
                     */
                    WaffleNonceFetchResponse.create = function create(properties) {
                        return new WaffleNonceFetchResponse(properties);
                    };

                    /**
                     * Encodes the specified WaffleNonceFetchResponse message. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse.verify|verify} messages.
                     * @function encode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IWaffleNonceFetchResponse} message WaffleNonceFetchResponse message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    WaffleNonceFetchResponse.encode = function encode(message, writer, q) {
                        if (!writer)
                            writer = $Writer.create();
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                            writer.uint32(/* id 1, wireType 2 =*/10).string(message.nonce);
                        if (message.waEntFbid != null && Object.hasOwnProperty.call(message, "waEntFbid"))
                            writer.uint32(/* id 2, wireType 2 =*/18).string(message.waEntFbid);
                        if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                            for (var i = 0; i < message.$unknowns.length; ++i)
                                writer.raw(message.$unknowns[i]);
                        return writer;
                    };

                    /**
                     * Encodes the specified WaffleNonceFetchResponse message, length delimited. Does not implicitly {@link proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse.verify|verify} messages.
                     * @function encodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.IWaffleNonceFetchResponse} message WaffleNonceFetchResponse message or plain object to encode
                     * @param {$protobuf.Writer} [writer] Writer to encode to
                     * @returns {$protobuf.Writer} Writer
                     */
                    WaffleNonceFetchResponse.encodeDelimited = function encodeDelimited(message, writer) {
                        return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                    };

                    /**
                     * Decodes a WaffleNonceFetchResponse message from the specified reader or buffer.
                     * @function decode
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @param {number} [length] Message length if known beforehand
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse} WaffleNonceFetchResponse
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    WaffleNonceFetchResponse.decode = function decode(reader, length, z, q, g) {
                        if (!(reader instanceof $Reader))
                            reader = $Reader.create(reader);
                        if (q === undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw Error("max depth exceeded");
                        var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                        while (reader.pos < end) {
                            var s = reader.pos;
                            var tag = reader.tag();
                            if (tag === z) {
                                z = undefined;
                                break;
                            }
                            var u = tag & 7;
                            switch (tag >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    message.nonce = reader.stringVerify();
                                    message._nonce = "nonce";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 2)
                                        break;
                                    message.waEntFbid = reader.stringVerify();
                                    message._waEntFbid = "waEntFbid";
                                    continue;
                                }
                            }
                            reader.skipType(u, q, tag);
                            if (!reader.discardUnknown) {
                                $util.makeProp(message, "$unknowns", false);
                                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                            }
                        }
                        if (z !== undefined)
                            throw Error("missing end group");
                        return message;
                    };

                    /**
                     * Decodes a WaffleNonceFetchResponse message from the specified reader or buffer, length delimited.
                     * @function decodeDelimited
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse
                     * @static
                     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse} WaffleNonceFetchResponse
                     * @throws {Error} If the payload is not a reader or valid buffer
                     * @throws {$protobuf.util.ProtocolError} If required fields are missing
                     */
                    WaffleNonceFetchResponse.decodeDelimited = function decodeDelimited(reader) {
                        if (!(reader instanceof $Reader))
                            reader = new $Reader(reader);
                        return this.decode(reader, reader.uint32());
                    };

                    /**
                     * Verifies a WaffleNonceFetchResponse message.
                     * @function verify
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse
                     * @static
                     * @param {Object.<string,*>} message Plain object to verify
                     * @returns {string|null} `null` if valid, otherwise the reason why it is not
                     */
                    WaffleNonceFetchResponse.verify = function verify(message, q) {
                        if (typeof message !== "object" || message === null)
                            return "object expected";
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            return "max depth exceeded";
                        var properties = {};
                        if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce")) {
                            properties._nonce = 1;
                            if (!$util.isString(message.nonce))
                                return "nonce: string expected";
                        }
                        if (message.waEntFbid != null && Object.hasOwnProperty.call(message, "waEntFbid")) {
                            properties._waEntFbid = 1;
                            if (!$util.isString(message.waEntFbid))
                                return "waEntFbid: string expected";
                        }
                        return null;
                    };

                    /**
                     * Creates a WaffleNonceFetchResponse message from a plain object. Also converts values to their respective internal types.
                     * @function fromObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse
                     * @static
                     * @param {Object.<string,*>} object Plain object
                     * @returns {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse} WaffleNonceFetchResponse
                     */
                    WaffleNonceFetchResponse.fromObject = function fromObject(object, q) {
                        if (object instanceof C)
                            return object;
                        if (!$util.isObject(object))
                            throw TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse: object expected");
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var message = new C();
                        if (object.nonce != null)
                            message.nonce = String(object.nonce);
                        if (object.waEntFbid != null)
                            message.waEntFbid = String(object.waEntFbid);
                        return message;
                    };

                    /**
                     * Creates a plain object from a WaffleNonceFetchResponse message. Also converts values to other types if specified.
                     * @function toObject
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse
                     * @static
                     * @param {proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse} message WaffleNonceFetchResponse
                     * @param {$protobuf.IConversionOptions} [options] Conversion options
                     * @returns {Object.<string,*>} Plain object
                     */
                    WaffleNonceFetchResponse.toObject = function toObject(message, options, q) {
                        if (!options)
                            options = {};
                        if (q === undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw Error("max depth exceeded");
                        var object = {};
                        if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                            object.nonce = message.nonce;
                        if (message.waEntFbid != null && Object.hasOwnProperty.call(message, "waEntFbid"))
                            object.waEntFbid = message.waEntFbid;
                        return object;
                    };

                    /**
                     * Converts this WaffleNonceFetchResponse to JSON.
                     * @function toJSON
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse
                     * @instance
                     * @returns {Object.<string,*>} JSON object
                     */
                    WaffleNonceFetchResponse.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    /**
                     * Gets the default type url for WaffleNonceFetchResponse
                     * @function getTypeUrl
                     * @memberof proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse
                     * @static
                     * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                     * @returns {string} The default type url
                     */
                    WaffleNonceFetchResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse";
                    };

                    return WaffleNonceFetchResponse;
                })();

                return PeerDataOperationResult;
            })();

            return PeerDataOperationRequestResponseMessage;
        })();

        /**
         * PeerDataOperationRequestType enum.
         * @name proto.Message.PeerDataOperationRequestType
         * @enum {number}
         * @property {number} UPLOAD_STICKER=0 UPLOAD_STICKER value
         * @property {number} SEND_RECENT_STICKER_BOOTSTRAP=1 SEND_RECENT_STICKER_BOOTSTRAP value
         * @property {number} GENERATE_LINK_PREVIEW=2 GENERATE_LINK_PREVIEW value
         * @property {number} HISTORY_SYNC_ON_DEMAND=3 HISTORY_SYNC_ON_DEMAND value
         * @property {number} PLACEHOLDER_MESSAGE_RESEND=4 PLACEHOLDER_MESSAGE_RESEND value
         * @property {number} WAFFLE_LINKING_NONCE_FETCH=5 WAFFLE_LINKING_NONCE_FETCH value
         * @property {number} FULL_HISTORY_SYNC_ON_DEMAND=6 FULL_HISTORY_SYNC_ON_DEMAND value
         * @property {number} COMPANION_META_NONCE_FETCH=7 COMPANION_META_NONCE_FETCH value
         * @property {number} COMPANION_SYNCD_SNAPSHOT_FATAL_RECOVERY=8 COMPANION_SYNCD_SNAPSHOT_FATAL_RECOVERY value
         * @property {number} COMPANION_CANONICAL_USER_NONCE_FETCH=9 COMPANION_CANONICAL_USER_NONCE_FETCH value
         * @property {number} HISTORY_SYNC_CHUNK_RETRY=10 HISTORY_SYNC_CHUNK_RETRY value
         * @property {number} GALAXY_FLOW_ACTION=11 GALAXY_FLOW_ACTION value
         * @property {number} BUSINESS_BROADCAST_INSIGHTS_DELIVERED_TO=12 BUSINESS_BROADCAST_INSIGHTS_DELIVERED_TO value
         * @property {number} BUSINESS_BROADCAST_INSIGHTS_REFRESH=13 BUSINESS_BROADCAST_INSIGHTS_REFRESH value
         */
        Message.PeerDataOperationRequestType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UPLOAD_STICKER"] = 0;
            values[valuesById[1] = "SEND_RECENT_STICKER_BOOTSTRAP"] = 1;
            values[valuesById[2] = "GENERATE_LINK_PREVIEW"] = 2;
            values[valuesById[3] = "HISTORY_SYNC_ON_DEMAND"] = 3;
            values[valuesById[4] = "PLACEHOLDER_MESSAGE_RESEND"] = 4;
            values[valuesById[5] = "WAFFLE_LINKING_NONCE_FETCH"] = 5;
            values[valuesById[6] = "FULL_HISTORY_SYNC_ON_DEMAND"] = 6;
            values[valuesById[7] = "COMPANION_META_NONCE_FETCH"] = 7;
            values[valuesById[8] = "COMPANION_SYNCD_SNAPSHOT_FATAL_RECOVERY"] = 8;
            values[valuesById[9] = "COMPANION_CANONICAL_USER_NONCE_FETCH"] = 9;
            values[valuesById[10] = "HISTORY_SYNC_CHUNK_RETRY"] = 10;
            values[valuesById[11] = "GALAXY_FLOW_ACTION"] = 11;
            values[valuesById[12] = "BUSINESS_BROADCAST_INSIGHTS_DELIVERED_TO"] = 12;
            values[valuesById[13] = "BUSINESS_BROADCAST_INSIGHTS_REFRESH"] = 13;
            return values;
        })();

        Message.PinInChatMessage = (function() {

            /**
             * Properties of a PinInChatMessage.
             * @memberof proto.Message
             * @interface IPinInChatMessage
             * @property {proto.IMessageKey|null} [key] PinInChatMessage key
             * @property {proto.Message.PinInChatMessage.Type|null} [type] PinInChatMessage type
             * @property {number|Long|null} [senderTimestampMs] PinInChatMessage senderTimestampMs
             */

            /**
             * Constructs a new PinInChatMessage.
             * @memberof proto.Message
             * @classdesc Represents a PinInChatMessage.
             * @implements IPinInChatMessage
             * @constructor
             * @param {proto.Message.IPinInChatMessage=} [properties] Properties to set
             */
            function PinInChatMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PinInChatMessage key.
             * @member {proto.IMessageKey|null|undefined} key
             * @memberof proto.Message.PinInChatMessage
             * @instance
             */
            PinInChatMessage.prototype.key = null;

            /**
             * PinInChatMessage type.
             * @member {proto.Message.PinInChatMessage.Type|null|undefined} type
             * @memberof proto.Message.PinInChatMessage
             * @instance
             */
            PinInChatMessage.prototype.type = null;

            /**
             * PinInChatMessage senderTimestampMs.
             * @member {number|Long|null|undefined} senderTimestampMs
             * @memberof proto.Message.PinInChatMessage
             * @instance
             */
            PinInChatMessage.prototype.senderTimestampMs = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PinInChatMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PinInChatMessage.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PinInChatMessage.prototype, "_senderTimestampMs", {
                get: $util.oneOfGetter($oneOfFields = ["senderTimestampMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PinInChatMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.PinInChatMessage
             * @static
             * @param {proto.Message.IPinInChatMessage=} [properties] Properties to set
             * @returns {proto.Message.PinInChatMessage} PinInChatMessage instance
             */
            PinInChatMessage.create = function create(properties) {
                return new PinInChatMessage(properties);
            };

            /**
             * Encodes the specified PinInChatMessage message. Does not implicitly {@link proto.Message.PinInChatMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.PinInChatMessage
             * @static
             * @param {proto.Message.IPinInChatMessage} message PinInChatMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PinInChatMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    $root.proto.MessageKey.encode(message.key, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.type);
                if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.senderTimestampMs);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PinInChatMessage message, length delimited. Does not implicitly {@link proto.Message.PinInChatMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.PinInChatMessage
             * @static
             * @param {proto.Message.IPinInChatMessage} message PinInChatMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PinInChatMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PinInChatMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.PinInChatMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.PinInChatMessage} PinInChatMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PinInChatMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.key = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.key);
                            message._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.type = reader.int32();
                            message._type = "type";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.senderTimestampMs = reader.int64();
                            message._senderTimestampMs = "senderTimestampMs";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PinInChatMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.PinInChatMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.PinInChatMessage} PinInChatMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PinInChatMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PinInChatMessage message.
             * @function verify
             * @memberof proto.Message.PinInChatMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PinInChatMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                    properties._key = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.key, q + 1);
                        if (error)
                            return "key." + error;
                    }
                }
                if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                    properties._type = 1;
                    if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                        return "type: enum value expected";
                }
                if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs")) {
                    properties._senderTimestampMs = 1;
                    if (!$util.isInteger(message.senderTimestampMs) && !(message.senderTimestampMs && $util.isInteger(message.senderTimestampMs.low) && $util.isInteger(message.senderTimestampMs.high)))
                        return "senderTimestampMs: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a PinInChatMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.PinInChatMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.PinInChatMessage} PinInChatMessage
             */
            PinInChatMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.PinInChatMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.key != null) {
                    if (!$util.isObject(object.key))
                        throw TypeError(".proto.Message.PinInChatMessage.key: object expected");
                    message.key = $root.proto.MessageKey.fromObject(object.key, q + 1);
                }
                switch (object.type) {
                case "UNKNOWN_TYPE":
                case 0:
                    message.type = 0;
                    break;
                case "PIN_FOR_ALL":
                case 1:
                    message.type = 1;
                    break;
                case "UNPIN_FOR_ALL":
                case 2:
                    message.type = 2;
                    break;
                default:
                    if (typeof object.type === "number" && (object.type | 0) === object.type)
                        message.type = object.type;
                }
                if (object.senderTimestampMs != null)
                    if ($util.Long)
                        message.senderTimestampMs = $util.Long.fromValue(object.senderTimestampMs, false);
                    else if (typeof object.senderTimestampMs === "string")
                        message.senderTimestampMs = parseInt(object.senderTimestampMs, 10);
                    else if (typeof object.senderTimestampMs === "number")
                        message.senderTimestampMs = object.senderTimestampMs;
                    else if (typeof object.senderTimestampMs === "object")
                        message.senderTimestampMs = new $util.LongBits(object.senderTimestampMs.low >>> 0, object.senderTimestampMs.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a PinInChatMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.PinInChatMessage
             * @static
             * @param {proto.Message.PinInChatMessage} message PinInChatMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PinInChatMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    object.key = $root.proto.MessageKey.toObject(message.key, options, q + 1);
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    object.type = options.enums === String ? $root.proto.Message.PinInChatMessage.Type[message.type] === undefined ? message.type : $root.proto.Message.PinInChatMessage.Type[message.type] : message.type;
                if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.senderTimestampMs = typeof message.senderTimestampMs === "number" ? BigInt(message.senderTimestampMs) : $util.Long.fromBits(message.senderTimestampMs.low >>> 0, message.senderTimestampMs.high >>> 0, false).toBigInt();
                    else if (typeof message.senderTimestampMs === "number")
                        object.senderTimestampMs = options.longs === String ? String(message.senderTimestampMs) : message.senderTimestampMs;
                    else
                        object.senderTimestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.senderTimestampMs) : options.longs === Number ? new $util.LongBits(message.senderTimestampMs.low >>> 0, message.senderTimestampMs.high >>> 0).toNumber() : message.senderTimestampMs;
                return object;
            };

            /**
             * Converts this PinInChatMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.PinInChatMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PinInChatMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PinInChatMessage
             * @function getTypeUrl
             * @memberof proto.Message.PinInChatMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PinInChatMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.PinInChatMessage";
            };

            /**
             * Type enum.
             * @name proto.Message.PinInChatMessage.Type
             * @enum {number}
             * @property {number} UNKNOWN_TYPE=0 UNKNOWN_TYPE value
             * @property {number} PIN_FOR_ALL=1 PIN_FOR_ALL value
             * @property {number} UNPIN_FOR_ALL=2 UNPIN_FOR_ALL value
             */
            PinInChatMessage.Type = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN_TYPE"] = 0;
                values[valuesById[1] = "PIN_FOR_ALL"] = 1;
                values[valuesById[2] = "UNPIN_FOR_ALL"] = 2;
                return values;
            })();

            return PinInChatMessage;
        })();

        Message.PlaceholderMessage = (function() {

            /**
             * Properties of a PlaceholderMessage.
             * @memberof proto.Message
             * @interface IPlaceholderMessage
             * @property {proto.Message.PlaceholderMessage.PlaceholderType|null} [type] PlaceholderMessage type
             */

            /**
             * Constructs a new PlaceholderMessage.
             * @memberof proto.Message
             * @classdesc Represents a PlaceholderMessage.
             * @implements IPlaceholderMessage
             * @constructor
             * @param {proto.Message.IPlaceholderMessage=} [properties] Properties to set
             */
            function PlaceholderMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PlaceholderMessage type.
             * @member {proto.Message.PlaceholderMessage.PlaceholderType|null|undefined} type
             * @memberof proto.Message.PlaceholderMessage
             * @instance
             */
            PlaceholderMessage.prototype.type = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PlaceholderMessage.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PlaceholderMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.PlaceholderMessage
             * @static
             * @param {proto.Message.IPlaceholderMessage=} [properties] Properties to set
             * @returns {proto.Message.PlaceholderMessage} PlaceholderMessage instance
             */
            PlaceholderMessage.create = function create(properties) {
                return new PlaceholderMessage(properties);
            };

            /**
             * Encodes the specified PlaceholderMessage message. Does not implicitly {@link proto.Message.PlaceholderMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.PlaceholderMessage
             * @static
             * @param {proto.Message.IPlaceholderMessage} message PlaceholderMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PlaceholderMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PlaceholderMessage message, length delimited. Does not implicitly {@link proto.Message.PlaceholderMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.PlaceholderMessage
             * @static
             * @param {proto.Message.IPlaceholderMessage} message PlaceholderMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PlaceholderMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PlaceholderMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.PlaceholderMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.PlaceholderMessage} PlaceholderMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PlaceholderMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.type = reader.int32();
                            message._type = "type";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PlaceholderMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.PlaceholderMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.PlaceholderMessage} PlaceholderMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PlaceholderMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PlaceholderMessage message.
             * @function verify
             * @memberof proto.Message.PlaceholderMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PlaceholderMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                    properties._type = 1;
                    if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                        return "type: enum value expected";
                }
                return null;
            };

            /**
             * Creates a PlaceholderMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.PlaceholderMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.PlaceholderMessage} PlaceholderMessage
             */
            PlaceholderMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.PlaceholderMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.type) {
                case "MASK_LINKED_DEVICES":
                case 0:
                    message.type = 0;
                    break;
                default:
                    if (typeof object.type === "number" && (object.type | 0) === object.type)
                        message.type = object.type;
                }
                return message;
            };

            /**
             * Creates a plain object from a PlaceholderMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.PlaceholderMessage
             * @static
             * @param {proto.Message.PlaceholderMessage} message PlaceholderMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PlaceholderMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    object.type = options.enums === String ? $root.proto.Message.PlaceholderMessage.PlaceholderType[message.type] === undefined ? message.type : $root.proto.Message.PlaceholderMessage.PlaceholderType[message.type] : message.type;
                return object;
            };

            /**
             * Converts this PlaceholderMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.PlaceholderMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PlaceholderMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PlaceholderMessage
             * @function getTypeUrl
             * @memberof proto.Message.PlaceholderMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PlaceholderMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.PlaceholderMessage";
            };

            /**
             * PlaceholderType enum.
             * @name proto.Message.PlaceholderMessage.PlaceholderType
             * @enum {number}
             * @property {number} MASK_LINKED_DEVICES=0 MASK_LINKED_DEVICES value
             */
            PlaceholderMessage.PlaceholderType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "MASK_LINKED_DEVICES"] = 0;
                return values;
            })();

            return PlaceholderMessage;
        })();

        Message.PollAddOptionMessage = (function() {

            /**
             * Properties of a PollAddOptionMessage.
             * @memberof proto.Message
             * @interface IPollAddOptionMessage
             * @property {proto.IMessageKey|null} [pollCreationMessageKey] PollAddOptionMessage pollCreationMessageKey
             * @property {proto.Message.PollCreationMessage.IOption|null} [addOption] PollAddOptionMessage addOption
             * @property {proto.Message.IPollUpdateMessageMetadata|null} [metadata] PollAddOptionMessage metadata
             */

            /**
             * Constructs a new PollAddOptionMessage.
             * @memberof proto.Message
             * @classdesc Represents a PollAddOptionMessage.
             * @implements IPollAddOptionMessage
             * @constructor
             * @param {proto.Message.IPollAddOptionMessage=} [properties] Properties to set
             */
            function PollAddOptionMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PollAddOptionMessage pollCreationMessageKey.
             * @member {proto.IMessageKey|null|undefined} pollCreationMessageKey
             * @memberof proto.Message.PollAddOptionMessage
             * @instance
             */
            PollAddOptionMessage.prototype.pollCreationMessageKey = null;

            /**
             * PollAddOptionMessage addOption.
             * @member {proto.Message.PollCreationMessage.IOption|null|undefined} addOption
             * @memberof proto.Message.PollAddOptionMessage
             * @instance
             */
            PollAddOptionMessage.prototype.addOption = null;

            /**
             * PollAddOptionMessage metadata.
             * @member {proto.Message.IPollUpdateMessageMetadata|null|undefined} metadata
             * @memberof proto.Message.PollAddOptionMessage
             * @instance
             */
            PollAddOptionMessage.prototype.metadata = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollAddOptionMessage.prototype, "_pollCreationMessageKey", {
                get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollAddOptionMessage.prototype, "_addOption", {
                get: $util.oneOfGetter($oneOfFields = ["addOption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollAddOptionMessage.prototype, "_metadata", {
                get: $util.oneOfGetter($oneOfFields = ["metadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PollAddOptionMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.PollAddOptionMessage
             * @static
             * @param {proto.Message.IPollAddOptionMessage=} [properties] Properties to set
             * @returns {proto.Message.PollAddOptionMessage} PollAddOptionMessage instance
             */
            PollAddOptionMessage.create = function create(properties) {
                return new PollAddOptionMessage(properties);
            };

            /**
             * Encodes the specified PollAddOptionMessage message. Does not implicitly {@link proto.Message.PollAddOptionMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.PollAddOptionMessage
             * @static
             * @param {proto.Message.IPollAddOptionMessage} message PollAddOptionMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollAddOptionMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.pollCreationMessageKey != null && Object.hasOwnProperty.call(message, "pollCreationMessageKey"))
                    $root.proto.MessageKey.encode(message.pollCreationMessageKey, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.addOption != null && Object.hasOwnProperty.call(message, "addOption"))
                    $root.proto.Message.PollCreationMessage.Option.encode(message.addOption, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata"))
                    $root.proto.Message.PollUpdateMessageMetadata.encode(message.metadata, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PollAddOptionMessage message, length delimited. Does not implicitly {@link proto.Message.PollAddOptionMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.PollAddOptionMessage
             * @static
             * @param {proto.Message.IPollAddOptionMessage} message PollAddOptionMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollAddOptionMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PollAddOptionMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.PollAddOptionMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.PollAddOptionMessage} PollAddOptionMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollAddOptionMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.pollCreationMessageKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.pollCreationMessageKey);
                            message._pollCreationMessageKey = "pollCreationMessageKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.addOption = $root.proto.Message.PollCreationMessage.Option.decode(reader, reader.uint32(), undefined, q + 1, message.addOption);
                            message._addOption = "addOption";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.metadata = $root.proto.Message.PollUpdateMessageMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.metadata);
                            message._metadata = "metadata";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PollAddOptionMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.PollAddOptionMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.PollAddOptionMessage} PollAddOptionMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollAddOptionMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PollAddOptionMessage message.
             * @function verify
             * @memberof proto.Message.PollAddOptionMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PollAddOptionMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.pollCreationMessageKey != null && Object.hasOwnProperty.call(message, "pollCreationMessageKey")) {
                    properties._pollCreationMessageKey = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.pollCreationMessageKey, q + 1);
                        if (error)
                            return "pollCreationMessageKey." + error;
                    }
                }
                if (message.addOption != null && Object.hasOwnProperty.call(message, "addOption")) {
                    properties._addOption = 1;
                    {
                        var error = $root.proto.Message.PollCreationMessage.Option.verify(message.addOption, q + 1);
                        if (error)
                            return "addOption." + error;
                    }
                }
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata")) {
                    properties._metadata = 1;
                    {
                        var error = $root.proto.Message.PollUpdateMessageMetadata.verify(message.metadata, q + 1);
                        if (error)
                            return "metadata." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a PollAddOptionMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.PollAddOptionMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.PollAddOptionMessage} PollAddOptionMessage
             */
            PollAddOptionMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.PollAddOptionMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.pollCreationMessageKey != null) {
                    if (!$util.isObject(object.pollCreationMessageKey))
                        throw TypeError(".proto.Message.PollAddOptionMessage.pollCreationMessageKey: object expected");
                    message.pollCreationMessageKey = $root.proto.MessageKey.fromObject(object.pollCreationMessageKey, q + 1);
                }
                if (object.addOption != null) {
                    if (!$util.isObject(object.addOption))
                        throw TypeError(".proto.Message.PollAddOptionMessage.addOption: object expected");
                    message.addOption = $root.proto.Message.PollCreationMessage.Option.fromObject(object.addOption, q + 1);
                }
                if (object.metadata != null) {
                    if (!$util.isObject(object.metadata))
                        throw TypeError(".proto.Message.PollAddOptionMessage.metadata: object expected");
                    message.metadata = $root.proto.Message.PollUpdateMessageMetadata.fromObject(object.metadata, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a PollAddOptionMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.PollAddOptionMessage
             * @static
             * @param {proto.Message.PollAddOptionMessage} message PollAddOptionMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PollAddOptionMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.pollCreationMessageKey != null && Object.hasOwnProperty.call(message, "pollCreationMessageKey"))
                    object.pollCreationMessageKey = $root.proto.MessageKey.toObject(message.pollCreationMessageKey, options, q + 1);
                if (message.addOption != null && Object.hasOwnProperty.call(message, "addOption"))
                    object.addOption = $root.proto.Message.PollCreationMessage.Option.toObject(message.addOption, options, q + 1);
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata"))
                    object.metadata = $root.proto.Message.PollUpdateMessageMetadata.toObject(message.metadata, options, q + 1);
                return object;
            };

            /**
             * Converts this PollAddOptionMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.PollAddOptionMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PollAddOptionMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PollAddOptionMessage
             * @function getTypeUrl
             * @memberof proto.Message.PollAddOptionMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PollAddOptionMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.PollAddOptionMessage";
            };

            return PollAddOptionMessage;
        })();

        /**
         * PollContentType enum.
         * @name proto.Message.PollContentType
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} TEXT=1 TEXT value
         * @property {number} IMAGE=2 IMAGE value
         */
        Message.PollContentType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "TEXT"] = 1;
            values[valuesById[2] = "IMAGE"] = 2;
            return values;
        })();

        Message.PollCreationMessage = (function() {

            /**
             * Properties of a PollCreationMessage.
             * @memberof proto.Message
             * @interface IPollCreationMessage
             * @property {Uint8Array|null} [encKey] PollCreationMessage encKey
             * @property {string|null} [name] PollCreationMessage name
             * @property {Array.<proto.Message.PollCreationMessage.IOption>|null} [options] PollCreationMessage options
             * @property {number|null} [selectableOptionsCount] PollCreationMessage selectableOptionsCount
             * @property {proto.IContextInfo|null} [contextInfo] PollCreationMessage contextInfo
             * @property {proto.Message.PollContentType|null} [pollContentType] PollCreationMessage pollContentType
             * @property {proto.Message.PollType|null} [pollType] PollCreationMessage pollType
             * @property {proto.Message.PollCreationMessage.IOption|null} [correctAnswer] PollCreationMessage correctAnswer
             * @property {number|Long|null} [endTime] PollCreationMessage endTime
             * @property {boolean|null} [hideParticipantName] PollCreationMessage hideParticipantName
             * @property {boolean|null} [allowAddOption] PollCreationMessage allowAddOption
             */

            /**
             * Constructs a new PollCreationMessage.
             * @memberof proto.Message
             * @classdesc Represents a PollCreationMessage.
             * @implements IPollCreationMessage
             * @constructor
             * @param {proto.Message.IPollCreationMessage=} [properties] Properties to set
             */
            function PollCreationMessage(properties) {
                this.options = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PollCreationMessage encKey.
             * @member {Uint8Array|null|undefined} encKey
             * @memberof proto.Message.PollCreationMessage
             * @instance
             */
            PollCreationMessage.prototype.encKey = null;

            /**
             * PollCreationMessage name.
             * @member {string|null|undefined} name
             * @memberof proto.Message.PollCreationMessage
             * @instance
             */
            PollCreationMessage.prototype.name = null;

            /**
             * PollCreationMessage options.
             * @member {Array.<proto.Message.PollCreationMessage.IOption>} options
             * @memberof proto.Message.PollCreationMessage
             * @instance
             */
            PollCreationMessage.prototype.options = $util.emptyArray;

            /**
             * PollCreationMessage selectableOptionsCount.
             * @member {number|null|undefined} selectableOptionsCount
             * @memberof proto.Message.PollCreationMessage
             * @instance
             */
            PollCreationMessage.prototype.selectableOptionsCount = null;

            /**
             * PollCreationMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.PollCreationMessage
             * @instance
             */
            PollCreationMessage.prototype.contextInfo = null;

            /**
             * PollCreationMessage pollContentType.
             * @member {proto.Message.PollContentType|null|undefined} pollContentType
             * @memberof proto.Message.PollCreationMessage
             * @instance
             */
            PollCreationMessage.prototype.pollContentType = null;

            /**
             * PollCreationMessage pollType.
             * @member {proto.Message.PollType|null|undefined} pollType
             * @memberof proto.Message.PollCreationMessage
             * @instance
             */
            PollCreationMessage.prototype.pollType = null;

            /**
             * PollCreationMessage correctAnswer.
             * @member {proto.Message.PollCreationMessage.IOption|null|undefined} correctAnswer
             * @memberof proto.Message.PollCreationMessage
             * @instance
             */
            PollCreationMessage.prototype.correctAnswer = null;

            /**
             * PollCreationMessage endTime.
             * @member {number|Long|null|undefined} endTime
             * @memberof proto.Message.PollCreationMessage
             * @instance
             */
            PollCreationMessage.prototype.endTime = null;

            /**
             * PollCreationMessage hideParticipantName.
             * @member {boolean|null|undefined} hideParticipantName
             * @memberof proto.Message.PollCreationMessage
             * @instance
             */
            PollCreationMessage.prototype.hideParticipantName = null;

            /**
             * PollCreationMessage allowAddOption.
             * @member {boolean|null|undefined} allowAddOption
             * @memberof proto.Message.PollCreationMessage
             * @instance
             */
            PollCreationMessage.prototype.allowAddOption = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollCreationMessage.prototype, "_encKey", {
                get: $util.oneOfGetter($oneOfFields = ["encKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollCreationMessage.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollCreationMessage.prototype, "_selectableOptionsCount", {
                get: $util.oneOfGetter($oneOfFields = ["selectableOptionsCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollCreationMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollCreationMessage.prototype, "_pollContentType", {
                get: $util.oneOfGetter($oneOfFields = ["pollContentType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollCreationMessage.prototype, "_pollType", {
                get: $util.oneOfGetter($oneOfFields = ["pollType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollCreationMessage.prototype, "_correctAnswer", {
                get: $util.oneOfGetter($oneOfFields = ["correctAnswer"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollCreationMessage.prototype, "_endTime", {
                get: $util.oneOfGetter($oneOfFields = ["endTime"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollCreationMessage.prototype, "_hideParticipantName", {
                get: $util.oneOfGetter($oneOfFields = ["hideParticipantName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollCreationMessage.prototype, "_allowAddOption", {
                get: $util.oneOfGetter($oneOfFields = ["allowAddOption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PollCreationMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.PollCreationMessage
             * @static
             * @param {proto.Message.IPollCreationMessage=} [properties] Properties to set
             * @returns {proto.Message.PollCreationMessage} PollCreationMessage instance
             */
            PollCreationMessage.create = function create(properties) {
                return new PollCreationMessage(properties);
            };

            /**
             * Encodes the specified PollCreationMessage message. Does not implicitly {@link proto.Message.PollCreationMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.PollCreationMessage
             * @static
             * @param {proto.Message.IPollCreationMessage} message PollCreationMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollCreationMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.encKey != null && Object.hasOwnProperty.call(message, "encKey"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encKey);
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
                if (message.options != null && message.options.length)
                    for (var i = 0; i < message.options.length; ++i)
                        $root.proto.Message.PollCreationMessage.Option.encode(message.options[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.selectableOptionsCount != null && Object.hasOwnProperty.call(message, "selectableOptionsCount"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.selectableOptionsCount);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                if (message.pollContentType != null && Object.hasOwnProperty.call(message, "pollContentType"))
                    writer.uint32(/* id 6, wireType 0 =*/48).int32(message.pollContentType);
                if (message.pollType != null && Object.hasOwnProperty.call(message, "pollType"))
                    writer.uint32(/* id 7, wireType 0 =*/56).int32(message.pollType);
                if (message.correctAnswer != null && Object.hasOwnProperty.call(message, "correctAnswer"))
                    $root.proto.Message.PollCreationMessage.Option.encode(message.correctAnswer, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
                if (message.endTime != null && Object.hasOwnProperty.call(message, "endTime"))
                    writer.uint32(/* id 9, wireType 0 =*/72).int64(message.endTime);
                if (message.hideParticipantName != null && Object.hasOwnProperty.call(message, "hideParticipantName"))
                    writer.uint32(/* id 10, wireType 0 =*/80).bool(message.hideParticipantName);
                if (message.allowAddOption != null && Object.hasOwnProperty.call(message, "allowAddOption"))
                    writer.uint32(/* id 11, wireType 0 =*/88).bool(message.allowAddOption);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PollCreationMessage message, length delimited. Does not implicitly {@link proto.Message.PollCreationMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.PollCreationMessage
             * @static
             * @param {proto.Message.IPollCreationMessage} message PollCreationMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollCreationMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PollCreationMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.PollCreationMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.PollCreationMessage} PollCreationMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollCreationMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.encKey = reader.bytes();
                            message._encKey = "encKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            if (!(message.options && message.options.length))
                                message.options = [];
                            message.options.push($root.proto.Message.PollCreationMessage.Option.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.selectableOptionsCount = reader.uint32();
                            message._selectableOptionsCount = "selectableOptionsCount";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.pollContentType = reader.int32();
                            message._pollContentType = "pollContentType";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            message.pollType = reader.int32();
                            message._pollType = "pollType";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.correctAnswer = $root.proto.Message.PollCreationMessage.Option.decode(reader, reader.uint32(), undefined, q + 1, message.correctAnswer);
                            message._correctAnswer = "correctAnswer";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            message.endTime = reader.int64();
                            message._endTime = "endTime";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            message.hideParticipantName = reader.bool();
                            message._hideParticipantName = "hideParticipantName";
                            continue;
                        }
                    case 11: {
                            if (u !== 0)
                                break;
                            message.allowAddOption = reader.bool();
                            message._allowAddOption = "allowAddOption";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PollCreationMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.PollCreationMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.PollCreationMessage} PollCreationMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollCreationMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PollCreationMessage message.
             * @function verify
             * @memberof proto.Message.PollCreationMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PollCreationMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.encKey != null && Object.hasOwnProperty.call(message, "encKey")) {
                    properties._encKey = 1;
                    if (!(message.encKey && typeof message.encKey.length === "number" || $util.isString(message.encKey)))
                        return "encKey: buffer expected";
                }
                if (message.name != null && Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                if (message.options != null && Object.hasOwnProperty.call(message, "options")) {
                    if (!Array.isArray(message.options))
                        return "options: array expected";
                    for (var i = 0; i < message.options.length; ++i) {
                        var error = $root.proto.Message.PollCreationMessage.Option.verify(message.options[i], q + 1);
                        if (error)
                            return "options." + error;
                    }
                }
                if (message.selectableOptionsCount != null && Object.hasOwnProperty.call(message, "selectableOptionsCount")) {
                    properties._selectableOptionsCount = 1;
                    if (!$util.isInteger(message.selectableOptionsCount))
                        return "selectableOptionsCount: integer expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.pollContentType != null && Object.hasOwnProperty.call(message, "pollContentType")) {
                    properties._pollContentType = 1;
                    if (typeof message.pollContentType !== "number" || (message.pollContentType | 0) !== message.pollContentType)
                        return "pollContentType: enum value expected";
                }
                if (message.pollType != null && Object.hasOwnProperty.call(message, "pollType")) {
                    properties._pollType = 1;
                    if (typeof message.pollType !== "number" || (message.pollType | 0) !== message.pollType)
                        return "pollType: enum value expected";
                }
                if (message.correctAnswer != null && Object.hasOwnProperty.call(message, "correctAnswer")) {
                    properties._correctAnswer = 1;
                    {
                        var error = $root.proto.Message.PollCreationMessage.Option.verify(message.correctAnswer, q + 1);
                        if (error)
                            return "correctAnswer." + error;
                    }
                }
                if (message.endTime != null && Object.hasOwnProperty.call(message, "endTime")) {
                    properties._endTime = 1;
                    if (!$util.isInteger(message.endTime) && !(message.endTime && $util.isInteger(message.endTime.low) && $util.isInteger(message.endTime.high)))
                        return "endTime: integer|Long expected";
                }
                if (message.hideParticipantName != null && Object.hasOwnProperty.call(message, "hideParticipantName")) {
                    properties._hideParticipantName = 1;
                    if (typeof message.hideParticipantName !== "boolean")
                        return "hideParticipantName: boolean expected";
                }
                if (message.allowAddOption != null && Object.hasOwnProperty.call(message, "allowAddOption")) {
                    properties._allowAddOption = 1;
                    if (typeof message.allowAddOption !== "boolean")
                        return "allowAddOption: boolean expected";
                }
                return null;
            };

            /**
             * Creates a PollCreationMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.PollCreationMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.PollCreationMessage} PollCreationMessage
             */
            PollCreationMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.PollCreationMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.encKey != null)
                    if (typeof object.encKey === "string")
                        $util.base64.decode(object.encKey, message.encKey = $util.newBuffer($util.base64.length(object.encKey)), 0);
                    else if (object.encKey.length >= 0)
                        message.encKey = object.encKey;
                if (object.name != null)
                    message.name = String(object.name);
                if (object.options) {
                    if (!Array.isArray(object.options))
                        throw TypeError(".proto.Message.PollCreationMessage.options: array expected");
                    message.options = Array(object.options.length);
                    for (var i = 0; i < object.options.length; ++i) {
                        if (!$util.isObject(object.options[i]))
                            throw TypeError(".proto.Message.PollCreationMessage.options: object expected");
                        message.options[i] = $root.proto.Message.PollCreationMessage.Option.fromObject(object.options[i], q + 1);
                    }
                }
                if (object.selectableOptionsCount != null)
                    message.selectableOptionsCount = object.selectableOptionsCount >>> 0;
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.PollCreationMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                switch (object.pollContentType) {
                case "UNKNOWN":
                case 0:
                    message.pollContentType = 0;
                    break;
                case "TEXT":
                case 1:
                    message.pollContentType = 1;
                    break;
                case "IMAGE":
                case 2:
                    message.pollContentType = 2;
                    break;
                default:
                    if (typeof object.pollContentType === "number" && (object.pollContentType | 0) === object.pollContentType)
                        message.pollContentType = object.pollContentType;
                }
                switch (object.pollType) {
                case "POLL":
                case 0:
                    message.pollType = 0;
                    break;
                case "QUIZ":
                case 1:
                    message.pollType = 1;
                    break;
                default:
                    if (typeof object.pollType === "number" && (object.pollType | 0) === object.pollType)
                        message.pollType = object.pollType;
                }
                if (object.correctAnswer != null) {
                    if (!$util.isObject(object.correctAnswer))
                        throw TypeError(".proto.Message.PollCreationMessage.correctAnswer: object expected");
                    message.correctAnswer = $root.proto.Message.PollCreationMessage.Option.fromObject(object.correctAnswer, q + 1);
                }
                if (object.endTime != null)
                    if ($util.Long)
                        message.endTime = $util.Long.fromValue(object.endTime, false);
                    else if (typeof object.endTime === "string")
                        message.endTime = parseInt(object.endTime, 10);
                    else if (typeof object.endTime === "number")
                        message.endTime = object.endTime;
                    else if (typeof object.endTime === "object")
                        message.endTime = new $util.LongBits(object.endTime.low >>> 0, object.endTime.high >>> 0).toNumber();
                if (object.hideParticipantName != null)
                    message.hideParticipantName = Boolean(object.hideParticipantName);
                if (object.allowAddOption != null)
                    message.allowAddOption = Boolean(object.allowAddOption);
                return message;
            };

            /**
             * Creates a plain object from a PollCreationMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.PollCreationMessage
             * @static
             * @param {proto.Message.PollCreationMessage} message PollCreationMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PollCreationMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.options = [];
                if (message.encKey != null && Object.hasOwnProperty.call(message, "encKey"))
                    object.encKey = options.bytes === String ? $util.base64.encode(message.encKey, 0, message.encKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encKey) : message.encKey;
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.options && message.options.length) {
                    object.options = Array(message.options.length);
                    for (var j = 0; j < message.options.length; ++j)
                        object.options[j] = $root.proto.Message.PollCreationMessage.Option.toObject(message.options[j], options, q + 1);
                }
                if (message.selectableOptionsCount != null && Object.hasOwnProperty.call(message, "selectableOptionsCount"))
                    object.selectableOptionsCount = message.selectableOptionsCount;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.pollContentType != null && Object.hasOwnProperty.call(message, "pollContentType"))
                    object.pollContentType = options.enums === String ? $root.proto.Message.PollContentType[message.pollContentType] === undefined ? message.pollContentType : $root.proto.Message.PollContentType[message.pollContentType] : message.pollContentType;
                if (message.pollType != null && Object.hasOwnProperty.call(message, "pollType"))
                    object.pollType = options.enums === String ? $root.proto.Message.PollType[message.pollType] === undefined ? message.pollType : $root.proto.Message.PollType[message.pollType] : message.pollType;
                if (message.correctAnswer != null && Object.hasOwnProperty.call(message, "correctAnswer"))
                    object.correctAnswer = $root.proto.Message.PollCreationMessage.Option.toObject(message.correctAnswer, options, q + 1);
                if (message.endTime != null && Object.hasOwnProperty.call(message, "endTime"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.endTime = typeof message.endTime === "number" ? BigInt(message.endTime) : $util.Long.fromBits(message.endTime.low >>> 0, message.endTime.high >>> 0, false).toBigInt();
                    else if (typeof message.endTime === "number")
                        object.endTime = options.longs === String ? String(message.endTime) : message.endTime;
                    else
                        object.endTime = options.longs === String ? $util.Long.prototype.toString.call(message.endTime) : options.longs === Number ? new $util.LongBits(message.endTime.low >>> 0, message.endTime.high >>> 0).toNumber() : message.endTime;
                if (message.hideParticipantName != null && Object.hasOwnProperty.call(message, "hideParticipantName"))
                    object.hideParticipantName = message.hideParticipantName;
                if (message.allowAddOption != null && Object.hasOwnProperty.call(message, "allowAddOption"))
                    object.allowAddOption = message.allowAddOption;
                return object;
            };

            /**
             * Converts this PollCreationMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.PollCreationMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PollCreationMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PollCreationMessage
             * @function getTypeUrl
             * @memberof proto.Message.PollCreationMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PollCreationMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.PollCreationMessage";
            };

            PollCreationMessage.Option = (function() {

                /**
                 * Properties of an Option.
                 * @memberof proto.Message.PollCreationMessage
                 * @interface IOption
                 * @property {string|null} [optionName] Option optionName
                 * @property {string|null} [optionHash] Option optionHash
                 */

                /**
                 * Constructs a new Option.
                 * @memberof proto.Message.PollCreationMessage
                 * @classdesc Represents an Option.
                 * @implements IOption
                 * @constructor
                 * @param {proto.Message.PollCreationMessage.IOption=} [properties] Properties to set
                 */
                function Option(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * Option optionName.
                 * @member {string|null|undefined} optionName
                 * @memberof proto.Message.PollCreationMessage.Option
                 * @instance
                 */
                Option.prototype.optionName = null;

                /**
                 * Option optionHash.
                 * @member {string|null|undefined} optionHash
                 * @memberof proto.Message.PollCreationMessage.Option
                 * @instance
                 */
                Option.prototype.optionHash = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Option.prototype, "_optionName", {
                    get: $util.oneOfGetter($oneOfFields = ["optionName"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Option.prototype, "_optionHash", {
                    get: $util.oneOfGetter($oneOfFields = ["optionHash"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new Option instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.PollCreationMessage.Option
                 * @static
                 * @param {proto.Message.PollCreationMessage.IOption=} [properties] Properties to set
                 * @returns {proto.Message.PollCreationMessage.Option} Option instance
                 */
                Option.create = function create(properties) {
                    return new Option(properties);
                };

                /**
                 * Encodes the specified Option message. Does not implicitly {@link proto.Message.PollCreationMessage.Option.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.PollCreationMessage.Option
                 * @static
                 * @param {proto.Message.PollCreationMessage.IOption} message Option message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Option.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.optionName != null && Object.hasOwnProperty.call(message, "optionName"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.optionName);
                    if (message.optionHash != null && Object.hasOwnProperty.call(message, "optionHash"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.optionHash);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified Option message, length delimited. Does not implicitly {@link proto.Message.PollCreationMessage.Option.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.PollCreationMessage.Option
                 * @static
                 * @param {proto.Message.PollCreationMessage.IOption} message Option message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Option.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes an Option message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.PollCreationMessage.Option
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.PollCreationMessage.Option} Option
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Option.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.optionName = reader.stringVerify();
                                message._optionName = "optionName";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.optionHash = reader.stringVerify();
                                message._optionHash = "optionHash";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes an Option message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.PollCreationMessage.Option
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.PollCreationMessage.Option} Option
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Option.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies an Option message.
                 * @function verify
                 * @memberof proto.Message.PollCreationMessage.Option
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                Option.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.optionName != null && Object.hasOwnProperty.call(message, "optionName")) {
                        properties._optionName = 1;
                        if (!$util.isString(message.optionName))
                            return "optionName: string expected";
                    }
                    if (message.optionHash != null && Object.hasOwnProperty.call(message, "optionHash")) {
                        properties._optionHash = 1;
                        if (!$util.isString(message.optionHash))
                            return "optionHash: string expected";
                    }
                    return null;
                };

                /**
                 * Creates an Option message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.PollCreationMessage.Option
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.PollCreationMessage.Option} Option
                 */
                Option.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.PollCreationMessage.Option: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.optionName != null)
                        message.optionName = String(object.optionName);
                    if (object.optionHash != null)
                        message.optionHash = String(object.optionHash);
                    return message;
                };

                /**
                 * Creates a plain object from an Option message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.PollCreationMessage.Option
                 * @static
                 * @param {proto.Message.PollCreationMessage.Option} message Option
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                Option.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.optionName != null && Object.hasOwnProperty.call(message, "optionName"))
                        object.optionName = message.optionName;
                    if (message.optionHash != null && Object.hasOwnProperty.call(message, "optionHash"))
                        object.optionHash = message.optionHash;
                    return object;
                };

                /**
                 * Converts this Option to JSON.
                 * @function toJSON
                 * @memberof proto.Message.PollCreationMessage.Option
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                Option.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for Option
                 * @function getTypeUrl
                 * @memberof proto.Message.PollCreationMessage.Option
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                Option.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.PollCreationMessage.Option";
                };

                return Option;
            })();

            return PollCreationMessage;
        })();

        Message.PollEncValue = (function() {

            /**
             * Properties of a PollEncValue.
             * @memberof proto.Message
             * @interface IPollEncValue
             * @property {Uint8Array|null} [encPayload] PollEncValue encPayload
             * @property {Uint8Array|null} [encIv] PollEncValue encIv
             */

            /**
             * Constructs a new PollEncValue.
             * @memberof proto.Message
             * @classdesc Represents a PollEncValue.
             * @implements IPollEncValue
             * @constructor
             * @param {proto.Message.IPollEncValue=} [properties] Properties to set
             */
            function PollEncValue(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PollEncValue encPayload.
             * @member {Uint8Array|null|undefined} encPayload
             * @memberof proto.Message.PollEncValue
             * @instance
             */
            PollEncValue.prototype.encPayload = null;

            /**
             * PollEncValue encIv.
             * @member {Uint8Array|null|undefined} encIv
             * @memberof proto.Message.PollEncValue
             * @instance
             */
            PollEncValue.prototype.encIv = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollEncValue.prototype, "_encPayload", {
                get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollEncValue.prototype, "_encIv", {
                get: $util.oneOfGetter($oneOfFields = ["encIv"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PollEncValue instance using the specified properties.
             * @function create
             * @memberof proto.Message.PollEncValue
             * @static
             * @param {proto.Message.IPollEncValue=} [properties] Properties to set
             * @returns {proto.Message.PollEncValue} PollEncValue instance
             */
            PollEncValue.create = function create(properties) {
                return new PollEncValue(properties);
            };

            /**
             * Encodes the specified PollEncValue message. Does not implicitly {@link proto.Message.PollEncValue.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.PollEncValue
             * @static
             * @param {proto.Message.IPollEncValue} message PollEncValue message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollEncValue.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encPayload);
                if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encIv);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PollEncValue message, length delimited. Does not implicitly {@link proto.Message.PollEncValue.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.PollEncValue
             * @static
             * @param {proto.Message.IPollEncValue} message PollEncValue message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollEncValue.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PollEncValue message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.PollEncValue
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.PollEncValue} PollEncValue
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollEncValue.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.encPayload = reader.bytes();
                            message._encPayload = "encPayload";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.encIv = reader.bytes();
                            message._encIv = "encIv";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PollEncValue message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.PollEncValue
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.PollEncValue} PollEncValue
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollEncValue.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PollEncValue message.
             * @function verify
             * @memberof proto.Message.PollEncValue
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PollEncValue.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload")) {
                    properties._encPayload = 1;
                    if (!(message.encPayload && typeof message.encPayload.length === "number" || $util.isString(message.encPayload)))
                        return "encPayload: buffer expected";
                }
                if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv")) {
                    properties._encIv = 1;
                    if (!(message.encIv && typeof message.encIv.length === "number" || $util.isString(message.encIv)))
                        return "encIv: buffer expected";
                }
                return null;
            };

            /**
             * Creates a PollEncValue message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.PollEncValue
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.PollEncValue} PollEncValue
             */
            PollEncValue.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.PollEncValue: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.encPayload != null)
                    if (typeof object.encPayload === "string")
                        $util.base64.decode(object.encPayload, message.encPayload = $util.newBuffer($util.base64.length(object.encPayload)), 0);
                    else if (object.encPayload.length >= 0)
                        message.encPayload = object.encPayload;
                if (object.encIv != null)
                    if (typeof object.encIv === "string")
                        $util.base64.decode(object.encIv, message.encIv = $util.newBuffer($util.base64.length(object.encIv)), 0);
                    else if (object.encIv.length >= 0)
                        message.encIv = object.encIv;
                return message;
            };

            /**
             * Creates a plain object from a PollEncValue message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.PollEncValue
             * @static
             * @param {proto.Message.PollEncValue} message PollEncValue
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PollEncValue.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload"))
                    object.encPayload = options.bytes === String ? $util.base64.encode(message.encPayload, 0, message.encPayload.length) : options.bytes === Array ? Array.prototype.slice.call(message.encPayload) : message.encPayload;
                if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv"))
                    object.encIv = options.bytes === String ? $util.base64.encode(message.encIv, 0, message.encIv.length) : options.bytes === Array ? Array.prototype.slice.call(message.encIv) : message.encIv;
                return object;
            };

            /**
             * Converts this PollEncValue to JSON.
             * @function toJSON
             * @memberof proto.Message.PollEncValue
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PollEncValue.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PollEncValue
             * @function getTypeUrl
             * @memberof proto.Message.PollEncValue
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PollEncValue.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.PollEncValue";
            };

            return PollEncValue;
        })();

        Message.PollResultSnapshotMessage = (function() {

            /**
             * Properties of a PollResultSnapshotMessage.
             * @memberof proto.Message
             * @interface IPollResultSnapshotMessage
             * @property {string|null} [name] PollResultSnapshotMessage name
             * @property {Array.<proto.Message.PollResultSnapshotMessage.IPollVote>|null} [pollVotes] PollResultSnapshotMessage pollVotes
             * @property {proto.IContextInfo|null} [contextInfo] PollResultSnapshotMessage contextInfo
             * @property {proto.Message.PollType|null} [pollType] PollResultSnapshotMessage pollType
             */

            /**
             * Constructs a new PollResultSnapshotMessage.
             * @memberof proto.Message
             * @classdesc Represents a PollResultSnapshotMessage.
             * @implements IPollResultSnapshotMessage
             * @constructor
             * @param {proto.Message.IPollResultSnapshotMessage=} [properties] Properties to set
             */
            function PollResultSnapshotMessage(properties) {
                this.pollVotes = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PollResultSnapshotMessage name.
             * @member {string|null|undefined} name
             * @memberof proto.Message.PollResultSnapshotMessage
             * @instance
             */
            PollResultSnapshotMessage.prototype.name = null;

            /**
             * PollResultSnapshotMessage pollVotes.
             * @member {Array.<proto.Message.PollResultSnapshotMessage.IPollVote>} pollVotes
             * @memberof proto.Message.PollResultSnapshotMessage
             * @instance
             */
            PollResultSnapshotMessage.prototype.pollVotes = $util.emptyArray;

            /**
             * PollResultSnapshotMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.PollResultSnapshotMessage
             * @instance
             */
            PollResultSnapshotMessage.prototype.contextInfo = null;

            /**
             * PollResultSnapshotMessage pollType.
             * @member {proto.Message.PollType|null|undefined} pollType
             * @memberof proto.Message.PollResultSnapshotMessage
             * @instance
             */
            PollResultSnapshotMessage.prototype.pollType = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollResultSnapshotMessage.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollResultSnapshotMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollResultSnapshotMessage.prototype, "_pollType", {
                get: $util.oneOfGetter($oneOfFields = ["pollType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PollResultSnapshotMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.PollResultSnapshotMessage
             * @static
             * @param {proto.Message.IPollResultSnapshotMessage=} [properties] Properties to set
             * @returns {proto.Message.PollResultSnapshotMessage} PollResultSnapshotMessage instance
             */
            PollResultSnapshotMessage.create = function create(properties) {
                return new PollResultSnapshotMessage(properties);
            };

            /**
             * Encodes the specified PollResultSnapshotMessage message. Does not implicitly {@link proto.Message.PollResultSnapshotMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.PollResultSnapshotMessage
             * @static
             * @param {proto.Message.IPollResultSnapshotMessage} message PollResultSnapshotMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollResultSnapshotMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.pollVotes != null && message.pollVotes.length)
                    for (var i = 0; i < message.pollVotes.length; ++i)
                        $root.proto.Message.PollResultSnapshotMessage.PollVote.encode(message.pollVotes[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.pollType != null && Object.hasOwnProperty.call(message, "pollType"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int32(message.pollType);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PollResultSnapshotMessage message, length delimited. Does not implicitly {@link proto.Message.PollResultSnapshotMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.PollResultSnapshotMessage
             * @static
             * @param {proto.Message.IPollResultSnapshotMessage} message PollResultSnapshotMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollResultSnapshotMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PollResultSnapshotMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.PollResultSnapshotMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.PollResultSnapshotMessage} PollResultSnapshotMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollResultSnapshotMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(message.pollVotes && message.pollVotes.length))
                                message.pollVotes = [];
                            message.pollVotes.push($root.proto.Message.PollResultSnapshotMessage.PollVote.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.pollType = reader.int32();
                            message._pollType = "pollType";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PollResultSnapshotMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.PollResultSnapshotMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.PollResultSnapshotMessage} PollResultSnapshotMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollResultSnapshotMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PollResultSnapshotMessage message.
             * @function verify
             * @memberof proto.Message.PollResultSnapshotMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PollResultSnapshotMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.name != null && Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                if (message.pollVotes != null && Object.hasOwnProperty.call(message, "pollVotes")) {
                    if (!Array.isArray(message.pollVotes))
                        return "pollVotes: array expected";
                    for (var i = 0; i < message.pollVotes.length; ++i) {
                        var error = $root.proto.Message.PollResultSnapshotMessage.PollVote.verify(message.pollVotes[i], q + 1);
                        if (error)
                            return "pollVotes." + error;
                    }
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.pollType != null && Object.hasOwnProperty.call(message, "pollType")) {
                    properties._pollType = 1;
                    if (typeof message.pollType !== "number" || (message.pollType | 0) !== message.pollType)
                        return "pollType: enum value expected";
                }
                return null;
            };

            /**
             * Creates a PollResultSnapshotMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.PollResultSnapshotMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.PollResultSnapshotMessage} PollResultSnapshotMessage
             */
            PollResultSnapshotMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.PollResultSnapshotMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.name != null)
                    message.name = String(object.name);
                if (object.pollVotes) {
                    if (!Array.isArray(object.pollVotes))
                        throw TypeError(".proto.Message.PollResultSnapshotMessage.pollVotes: array expected");
                    message.pollVotes = Array(object.pollVotes.length);
                    for (var i = 0; i < object.pollVotes.length; ++i) {
                        if (!$util.isObject(object.pollVotes[i]))
                            throw TypeError(".proto.Message.PollResultSnapshotMessage.pollVotes: object expected");
                        message.pollVotes[i] = $root.proto.Message.PollResultSnapshotMessage.PollVote.fromObject(object.pollVotes[i], q + 1);
                    }
                }
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.PollResultSnapshotMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                switch (object.pollType) {
                case "POLL":
                case 0:
                    message.pollType = 0;
                    break;
                case "QUIZ":
                case 1:
                    message.pollType = 1;
                    break;
                default:
                    if (typeof object.pollType === "number" && (object.pollType | 0) === object.pollType)
                        message.pollType = object.pollType;
                }
                return message;
            };

            /**
             * Creates a plain object from a PollResultSnapshotMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.PollResultSnapshotMessage
             * @static
             * @param {proto.Message.PollResultSnapshotMessage} message PollResultSnapshotMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PollResultSnapshotMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.pollVotes = [];
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.pollVotes && message.pollVotes.length) {
                    object.pollVotes = Array(message.pollVotes.length);
                    for (var j = 0; j < message.pollVotes.length; ++j)
                        object.pollVotes[j] = $root.proto.Message.PollResultSnapshotMessage.PollVote.toObject(message.pollVotes[j], options, q + 1);
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.pollType != null && Object.hasOwnProperty.call(message, "pollType"))
                    object.pollType = options.enums === String ? $root.proto.Message.PollType[message.pollType] === undefined ? message.pollType : $root.proto.Message.PollType[message.pollType] : message.pollType;
                return object;
            };

            /**
             * Converts this PollResultSnapshotMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.PollResultSnapshotMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PollResultSnapshotMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PollResultSnapshotMessage
             * @function getTypeUrl
             * @memberof proto.Message.PollResultSnapshotMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PollResultSnapshotMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.PollResultSnapshotMessage";
            };

            PollResultSnapshotMessage.PollVote = (function() {

                /**
                 * Properties of a PollVote.
                 * @memberof proto.Message.PollResultSnapshotMessage
                 * @interface IPollVote
                 * @property {string|null} [optionName] PollVote optionName
                 * @property {number|Long|null} [optionVoteCount] PollVote optionVoteCount
                 */

                /**
                 * Constructs a new PollVote.
                 * @memberof proto.Message.PollResultSnapshotMessage
                 * @classdesc Represents a PollVote.
                 * @implements IPollVote
                 * @constructor
                 * @param {proto.Message.PollResultSnapshotMessage.IPollVote=} [properties] Properties to set
                 */
                function PollVote(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * PollVote optionName.
                 * @member {string|null|undefined} optionName
                 * @memberof proto.Message.PollResultSnapshotMessage.PollVote
                 * @instance
                 */
                PollVote.prototype.optionName = null;

                /**
                 * PollVote optionVoteCount.
                 * @member {number|Long|null|undefined} optionVoteCount
                 * @memberof proto.Message.PollResultSnapshotMessage.PollVote
                 * @instance
                 */
                PollVote.prototype.optionVoteCount = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PollVote.prototype, "_optionName", {
                    get: $util.oneOfGetter($oneOfFields = ["optionName"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PollVote.prototype, "_optionVoteCount", {
                    get: $util.oneOfGetter($oneOfFields = ["optionVoteCount"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new PollVote instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.PollResultSnapshotMessage.PollVote
                 * @static
                 * @param {proto.Message.PollResultSnapshotMessage.IPollVote=} [properties] Properties to set
                 * @returns {proto.Message.PollResultSnapshotMessage.PollVote} PollVote instance
                 */
                PollVote.create = function create(properties) {
                    return new PollVote(properties);
                };

                /**
                 * Encodes the specified PollVote message. Does not implicitly {@link proto.Message.PollResultSnapshotMessage.PollVote.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.PollResultSnapshotMessage.PollVote
                 * @static
                 * @param {proto.Message.PollResultSnapshotMessage.IPollVote} message PollVote message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PollVote.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.optionName != null && Object.hasOwnProperty.call(message, "optionName"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.optionName);
                    if (message.optionVoteCount != null && Object.hasOwnProperty.call(message, "optionVoteCount"))
                        writer.uint32(/* id 2, wireType 0 =*/16).int64(message.optionVoteCount);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified PollVote message, length delimited. Does not implicitly {@link proto.Message.PollResultSnapshotMessage.PollVote.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.PollResultSnapshotMessage.PollVote
                 * @static
                 * @param {proto.Message.PollResultSnapshotMessage.IPollVote} message PollVote message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PollVote.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a PollVote message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.PollResultSnapshotMessage.PollVote
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.PollResultSnapshotMessage.PollVote} PollVote
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PollVote.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.optionName = reader.stringVerify();
                                message._optionName = "optionName";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                message.optionVoteCount = reader.int64();
                                message._optionVoteCount = "optionVoteCount";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a PollVote message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.PollResultSnapshotMessage.PollVote
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.PollResultSnapshotMessage.PollVote} PollVote
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PollVote.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a PollVote message.
                 * @function verify
                 * @memberof proto.Message.PollResultSnapshotMessage.PollVote
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                PollVote.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.optionName != null && Object.hasOwnProperty.call(message, "optionName")) {
                        properties._optionName = 1;
                        if (!$util.isString(message.optionName))
                            return "optionName: string expected";
                    }
                    if (message.optionVoteCount != null && Object.hasOwnProperty.call(message, "optionVoteCount")) {
                        properties._optionVoteCount = 1;
                        if (!$util.isInteger(message.optionVoteCount) && !(message.optionVoteCount && $util.isInteger(message.optionVoteCount.low) && $util.isInteger(message.optionVoteCount.high)))
                            return "optionVoteCount: integer|Long expected";
                    }
                    return null;
                };

                /**
                 * Creates a PollVote message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.PollResultSnapshotMessage.PollVote
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.PollResultSnapshotMessage.PollVote} PollVote
                 */
                PollVote.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.PollResultSnapshotMessage.PollVote: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.optionName != null)
                        message.optionName = String(object.optionName);
                    if (object.optionVoteCount != null)
                        if ($util.Long)
                            message.optionVoteCount = $util.Long.fromValue(object.optionVoteCount, false);
                        else if (typeof object.optionVoteCount === "string")
                            message.optionVoteCount = parseInt(object.optionVoteCount, 10);
                        else if (typeof object.optionVoteCount === "number")
                            message.optionVoteCount = object.optionVoteCount;
                        else if (typeof object.optionVoteCount === "object")
                            message.optionVoteCount = new $util.LongBits(object.optionVoteCount.low >>> 0, object.optionVoteCount.high >>> 0).toNumber();
                    return message;
                };

                /**
                 * Creates a plain object from a PollVote message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.PollResultSnapshotMessage.PollVote
                 * @static
                 * @param {proto.Message.PollResultSnapshotMessage.PollVote} message PollVote
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                PollVote.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.optionName != null && Object.hasOwnProperty.call(message, "optionName"))
                        object.optionName = message.optionName;
                    if (message.optionVoteCount != null && Object.hasOwnProperty.call(message, "optionVoteCount"))
                        if (typeof BigInt !== "undefined" && options.longs === BigInt)
                            object.optionVoteCount = typeof message.optionVoteCount === "number" ? BigInt(message.optionVoteCount) : $util.Long.fromBits(message.optionVoteCount.low >>> 0, message.optionVoteCount.high >>> 0, false).toBigInt();
                        else if (typeof message.optionVoteCount === "number")
                            object.optionVoteCount = options.longs === String ? String(message.optionVoteCount) : message.optionVoteCount;
                        else
                            object.optionVoteCount = options.longs === String ? $util.Long.prototype.toString.call(message.optionVoteCount) : options.longs === Number ? new $util.LongBits(message.optionVoteCount.low >>> 0, message.optionVoteCount.high >>> 0).toNumber() : message.optionVoteCount;
                    return object;
                };

                /**
                 * Converts this PollVote to JSON.
                 * @function toJSON
                 * @memberof proto.Message.PollResultSnapshotMessage.PollVote
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                PollVote.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for PollVote
                 * @function getTypeUrl
                 * @memberof proto.Message.PollResultSnapshotMessage.PollVote
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                PollVote.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.PollResultSnapshotMessage.PollVote";
                };

                return PollVote;
            })();

            return PollResultSnapshotMessage;
        })();

        /**
         * PollType enum.
         * @name proto.Message.PollType
         * @enum {number}
         * @property {number} POLL=0 POLL value
         * @property {number} QUIZ=1 QUIZ value
         */
        Message.PollType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "POLL"] = 0;
            values[valuesById[1] = "QUIZ"] = 1;
            return values;
        })();

        Message.PollUpdateMessage = (function() {

            /**
             * Properties of a PollUpdateMessage.
             * @memberof proto.Message
             * @interface IPollUpdateMessage
             * @property {proto.IMessageKey|null} [pollCreationMessageKey] PollUpdateMessage pollCreationMessageKey
             * @property {proto.Message.IPollEncValue|null} [vote] PollUpdateMessage vote
             * @property {proto.Message.IPollUpdateMessageMetadata|null} [metadata] PollUpdateMessage metadata
             * @property {number|Long|null} [senderTimestampMs] PollUpdateMessage senderTimestampMs
             */

            /**
             * Constructs a new PollUpdateMessage.
             * @memberof proto.Message
             * @classdesc Represents a PollUpdateMessage.
             * @implements IPollUpdateMessage
             * @constructor
             * @param {proto.Message.IPollUpdateMessage=} [properties] Properties to set
             */
            function PollUpdateMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PollUpdateMessage pollCreationMessageKey.
             * @member {proto.IMessageKey|null|undefined} pollCreationMessageKey
             * @memberof proto.Message.PollUpdateMessage
             * @instance
             */
            PollUpdateMessage.prototype.pollCreationMessageKey = null;

            /**
             * PollUpdateMessage vote.
             * @member {proto.Message.IPollEncValue|null|undefined} vote
             * @memberof proto.Message.PollUpdateMessage
             * @instance
             */
            PollUpdateMessage.prototype.vote = null;

            /**
             * PollUpdateMessage metadata.
             * @member {proto.Message.IPollUpdateMessageMetadata|null|undefined} metadata
             * @memberof proto.Message.PollUpdateMessage
             * @instance
             */
            PollUpdateMessage.prototype.metadata = null;

            /**
             * PollUpdateMessage senderTimestampMs.
             * @member {number|Long|null|undefined} senderTimestampMs
             * @memberof proto.Message.PollUpdateMessage
             * @instance
             */
            PollUpdateMessage.prototype.senderTimestampMs = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollUpdateMessage.prototype, "_pollCreationMessageKey", {
                get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollUpdateMessage.prototype, "_vote", {
                get: $util.oneOfGetter($oneOfFields = ["vote"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollUpdateMessage.prototype, "_metadata", {
                get: $util.oneOfGetter($oneOfFields = ["metadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollUpdateMessage.prototype, "_senderTimestampMs", {
                get: $util.oneOfGetter($oneOfFields = ["senderTimestampMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PollUpdateMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.PollUpdateMessage
             * @static
             * @param {proto.Message.IPollUpdateMessage=} [properties] Properties to set
             * @returns {proto.Message.PollUpdateMessage} PollUpdateMessage instance
             */
            PollUpdateMessage.create = function create(properties) {
                return new PollUpdateMessage(properties);
            };

            /**
             * Encodes the specified PollUpdateMessage message. Does not implicitly {@link proto.Message.PollUpdateMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.PollUpdateMessage
             * @static
             * @param {proto.Message.IPollUpdateMessage} message PollUpdateMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollUpdateMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.pollCreationMessageKey != null && Object.hasOwnProperty.call(message, "pollCreationMessageKey"))
                    $root.proto.MessageKey.encode(message.pollCreationMessageKey, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.vote != null && Object.hasOwnProperty.call(message, "vote"))
                    $root.proto.Message.PollEncValue.encode(message.vote, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata"))
                    $root.proto.Message.PollUpdateMessageMetadata.encode(message.metadata, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int64(message.senderTimestampMs);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PollUpdateMessage message, length delimited. Does not implicitly {@link proto.Message.PollUpdateMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.PollUpdateMessage
             * @static
             * @param {proto.Message.IPollUpdateMessage} message PollUpdateMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollUpdateMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PollUpdateMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.PollUpdateMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.PollUpdateMessage} PollUpdateMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollUpdateMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.pollCreationMessageKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.pollCreationMessageKey);
                            message._pollCreationMessageKey = "pollCreationMessageKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.vote = $root.proto.Message.PollEncValue.decode(reader, reader.uint32(), undefined, q + 1, message.vote);
                            message._vote = "vote";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.metadata = $root.proto.Message.PollUpdateMessageMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.metadata);
                            message._metadata = "metadata";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.senderTimestampMs = reader.int64();
                            message._senderTimestampMs = "senderTimestampMs";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PollUpdateMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.PollUpdateMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.PollUpdateMessage} PollUpdateMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollUpdateMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PollUpdateMessage message.
             * @function verify
             * @memberof proto.Message.PollUpdateMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PollUpdateMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.pollCreationMessageKey != null && Object.hasOwnProperty.call(message, "pollCreationMessageKey")) {
                    properties._pollCreationMessageKey = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.pollCreationMessageKey, q + 1);
                        if (error)
                            return "pollCreationMessageKey." + error;
                    }
                }
                if (message.vote != null && Object.hasOwnProperty.call(message, "vote")) {
                    properties._vote = 1;
                    {
                        var error = $root.proto.Message.PollEncValue.verify(message.vote, q + 1);
                        if (error)
                            return "vote." + error;
                    }
                }
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata")) {
                    properties._metadata = 1;
                    {
                        var error = $root.proto.Message.PollUpdateMessageMetadata.verify(message.metadata, q + 1);
                        if (error)
                            return "metadata." + error;
                    }
                }
                if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs")) {
                    properties._senderTimestampMs = 1;
                    if (!$util.isInteger(message.senderTimestampMs) && !(message.senderTimestampMs && $util.isInteger(message.senderTimestampMs.low) && $util.isInteger(message.senderTimestampMs.high)))
                        return "senderTimestampMs: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a PollUpdateMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.PollUpdateMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.PollUpdateMessage} PollUpdateMessage
             */
            PollUpdateMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.PollUpdateMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.pollCreationMessageKey != null) {
                    if (!$util.isObject(object.pollCreationMessageKey))
                        throw TypeError(".proto.Message.PollUpdateMessage.pollCreationMessageKey: object expected");
                    message.pollCreationMessageKey = $root.proto.MessageKey.fromObject(object.pollCreationMessageKey, q + 1);
                }
                if (object.vote != null) {
                    if (!$util.isObject(object.vote))
                        throw TypeError(".proto.Message.PollUpdateMessage.vote: object expected");
                    message.vote = $root.proto.Message.PollEncValue.fromObject(object.vote, q + 1);
                }
                if (object.metadata != null) {
                    if (!$util.isObject(object.metadata))
                        throw TypeError(".proto.Message.PollUpdateMessage.metadata: object expected");
                    message.metadata = $root.proto.Message.PollUpdateMessageMetadata.fromObject(object.metadata, q + 1);
                }
                if (object.senderTimestampMs != null)
                    if ($util.Long)
                        message.senderTimestampMs = $util.Long.fromValue(object.senderTimestampMs, false);
                    else if (typeof object.senderTimestampMs === "string")
                        message.senderTimestampMs = parseInt(object.senderTimestampMs, 10);
                    else if (typeof object.senderTimestampMs === "number")
                        message.senderTimestampMs = object.senderTimestampMs;
                    else if (typeof object.senderTimestampMs === "object")
                        message.senderTimestampMs = new $util.LongBits(object.senderTimestampMs.low >>> 0, object.senderTimestampMs.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a PollUpdateMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.PollUpdateMessage
             * @static
             * @param {proto.Message.PollUpdateMessage} message PollUpdateMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PollUpdateMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.pollCreationMessageKey != null && Object.hasOwnProperty.call(message, "pollCreationMessageKey"))
                    object.pollCreationMessageKey = $root.proto.MessageKey.toObject(message.pollCreationMessageKey, options, q + 1);
                if (message.vote != null && Object.hasOwnProperty.call(message, "vote"))
                    object.vote = $root.proto.Message.PollEncValue.toObject(message.vote, options, q + 1);
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata"))
                    object.metadata = $root.proto.Message.PollUpdateMessageMetadata.toObject(message.metadata, options, q + 1);
                if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.senderTimestampMs = typeof message.senderTimestampMs === "number" ? BigInt(message.senderTimestampMs) : $util.Long.fromBits(message.senderTimestampMs.low >>> 0, message.senderTimestampMs.high >>> 0, false).toBigInt();
                    else if (typeof message.senderTimestampMs === "number")
                        object.senderTimestampMs = options.longs === String ? String(message.senderTimestampMs) : message.senderTimestampMs;
                    else
                        object.senderTimestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.senderTimestampMs) : options.longs === Number ? new $util.LongBits(message.senderTimestampMs.low >>> 0, message.senderTimestampMs.high >>> 0).toNumber() : message.senderTimestampMs;
                return object;
            };

            /**
             * Converts this PollUpdateMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.PollUpdateMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PollUpdateMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PollUpdateMessage
             * @function getTypeUrl
             * @memberof proto.Message.PollUpdateMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PollUpdateMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.PollUpdateMessage";
            };

            return PollUpdateMessage;
        })();

        Message.PollUpdateMessageMetadata = (function() {

            /**
             * Properties of a PollUpdateMessageMetadata.
             * @memberof proto.Message
             * @interface IPollUpdateMessageMetadata
             * @property {Uint8Array|null} [pollNameHash] PollUpdateMessageMetadata pollNameHash
             * @property {string|null} [lastEditStanzaId] PollUpdateMessageMetadata lastEditStanzaId
             */

            /**
             * Constructs a new PollUpdateMessageMetadata.
             * @memberof proto.Message
             * @classdesc Represents a PollUpdateMessageMetadata.
             * @implements IPollUpdateMessageMetadata
             * @constructor
             * @param {proto.Message.IPollUpdateMessageMetadata=} [properties] Properties to set
             */
            function PollUpdateMessageMetadata(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PollUpdateMessageMetadata pollNameHash.
             * @member {Uint8Array|null|undefined} pollNameHash
             * @memberof proto.Message.PollUpdateMessageMetadata
             * @instance
             */
            PollUpdateMessageMetadata.prototype.pollNameHash = null;

            /**
             * PollUpdateMessageMetadata lastEditStanzaId.
             * @member {string|null|undefined} lastEditStanzaId
             * @memberof proto.Message.PollUpdateMessageMetadata
             * @instance
             */
            PollUpdateMessageMetadata.prototype.lastEditStanzaId = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollUpdateMessageMetadata.prototype, "_pollNameHash", {
                get: $util.oneOfGetter($oneOfFields = ["pollNameHash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollUpdateMessageMetadata.prototype, "_lastEditStanzaId", {
                get: $util.oneOfGetter($oneOfFields = ["lastEditStanzaId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PollUpdateMessageMetadata instance using the specified properties.
             * @function create
             * @memberof proto.Message.PollUpdateMessageMetadata
             * @static
             * @param {proto.Message.IPollUpdateMessageMetadata=} [properties] Properties to set
             * @returns {proto.Message.PollUpdateMessageMetadata} PollUpdateMessageMetadata instance
             */
            PollUpdateMessageMetadata.create = function create(properties) {
                return new PollUpdateMessageMetadata(properties);
            };

            /**
             * Encodes the specified PollUpdateMessageMetadata message. Does not implicitly {@link proto.Message.PollUpdateMessageMetadata.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.PollUpdateMessageMetadata
             * @static
             * @param {proto.Message.IPollUpdateMessageMetadata} message PollUpdateMessageMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollUpdateMessageMetadata.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.pollNameHash != null && Object.hasOwnProperty.call(message, "pollNameHash"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.pollNameHash);
                if (message.lastEditStanzaId != null && Object.hasOwnProperty.call(message, "lastEditStanzaId"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.lastEditStanzaId);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PollUpdateMessageMetadata message, length delimited. Does not implicitly {@link proto.Message.PollUpdateMessageMetadata.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.PollUpdateMessageMetadata
             * @static
             * @param {proto.Message.IPollUpdateMessageMetadata} message PollUpdateMessageMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollUpdateMessageMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PollUpdateMessageMetadata message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.PollUpdateMessageMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.PollUpdateMessageMetadata} PollUpdateMessageMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollUpdateMessageMetadata.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.pollNameHash = reader.bytes();
                            message._pollNameHash = "pollNameHash";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.lastEditStanzaId = reader.stringVerify();
                            message._lastEditStanzaId = "lastEditStanzaId";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PollUpdateMessageMetadata message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.PollUpdateMessageMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.PollUpdateMessageMetadata} PollUpdateMessageMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollUpdateMessageMetadata.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PollUpdateMessageMetadata message.
             * @function verify
             * @memberof proto.Message.PollUpdateMessageMetadata
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PollUpdateMessageMetadata.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.pollNameHash != null && Object.hasOwnProperty.call(message, "pollNameHash")) {
                    properties._pollNameHash = 1;
                    if (!(message.pollNameHash && typeof message.pollNameHash.length === "number" || $util.isString(message.pollNameHash)))
                        return "pollNameHash: buffer expected";
                }
                if (message.lastEditStanzaId != null && Object.hasOwnProperty.call(message, "lastEditStanzaId")) {
                    properties._lastEditStanzaId = 1;
                    if (!$util.isString(message.lastEditStanzaId))
                        return "lastEditStanzaId: string expected";
                }
                return null;
            };

            /**
             * Creates a PollUpdateMessageMetadata message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.PollUpdateMessageMetadata
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.PollUpdateMessageMetadata} PollUpdateMessageMetadata
             */
            PollUpdateMessageMetadata.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.PollUpdateMessageMetadata: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.pollNameHash != null)
                    if (typeof object.pollNameHash === "string")
                        $util.base64.decode(object.pollNameHash, message.pollNameHash = $util.newBuffer($util.base64.length(object.pollNameHash)), 0);
                    else if (object.pollNameHash.length >= 0)
                        message.pollNameHash = object.pollNameHash;
                if (object.lastEditStanzaId != null)
                    message.lastEditStanzaId = String(object.lastEditStanzaId);
                return message;
            };

            /**
             * Creates a plain object from a PollUpdateMessageMetadata message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.PollUpdateMessageMetadata
             * @static
             * @param {proto.Message.PollUpdateMessageMetadata} message PollUpdateMessageMetadata
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PollUpdateMessageMetadata.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.pollNameHash != null && Object.hasOwnProperty.call(message, "pollNameHash"))
                    object.pollNameHash = options.bytes === String ? $util.base64.encode(message.pollNameHash, 0, message.pollNameHash.length) : options.bytes === Array ? Array.prototype.slice.call(message.pollNameHash) : message.pollNameHash;
                if (message.lastEditStanzaId != null && Object.hasOwnProperty.call(message, "lastEditStanzaId"))
                    object.lastEditStanzaId = message.lastEditStanzaId;
                return object;
            };

            /**
             * Converts this PollUpdateMessageMetadata to JSON.
             * @function toJSON
             * @memberof proto.Message.PollUpdateMessageMetadata
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PollUpdateMessageMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PollUpdateMessageMetadata
             * @function getTypeUrl
             * @memberof proto.Message.PollUpdateMessageMetadata
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PollUpdateMessageMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.PollUpdateMessageMetadata";
            };

            return PollUpdateMessageMetadata;
        })();

        Message.PollVoteMessage = (function() {

            /**
             * Properties of a PollVoteMessage.
             * @memberof proto.Message
             * @interface IPollVoteMessage
             * @property {Array.<Uint8Array>|null} [selectedOptions] PollVoteMessage selectedOptions
             */

            /**
             * Constructs a new PollVoteMessage.
             * @memberof proto.Message
             * @classdesc Represents a PollVoteMessage.
             * @implements IPollVoteMessage
             * @constructor
             * @param {proto.Message.IPollVoteMessage=} [properties] Properties to set
             */
            function PollVoteMessage(properties) {
                this.selectedOptions = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PollVoteMessage selectedOptions.
             * @member {Array.<Uint8Array>} selectedOptions
             * @memberof proto.Message.PollVoteMessage
             * @instance
             */
            PollVoteMessage.prototype.selectedOptions = $util.emptyArray;

            /**
             * Creates a new PollVoteMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.PollVoteMessage
             * @static
             * @param {proto.Message.IPollVoteMessage=} [properties] Properties to set
             * @returns {proto.Message.PollVoteMessage} PollVoteMessage instance
             */
            PollVoteMessage.create = function create(properties) {
                return new PollVoteMessage(properties);
            };

            /**
             * Encodes the specified PollVoteMessage message. Does not implicitly {@link proto.Message.PollVoteMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.PollVoteMessage
             * @static
             * @param {proto.Message.IPollVoteMessage} message PollVoteMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollVoteMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.selectedOptions != null && message.selectedOptions.length)
                    for (var i = 0; i < message.selectedOptions.length; ++i)
                        writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.selectedOptions[i]);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PollVoteMessage message, length delimited. Does not implicitly {@link proto.Message.PollVoteMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.PollVoteMessage
             * @static
             * @param {proto.Message.IPollVoteMessage} message PollVoteMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollVoteMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PollVoteMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.PollVoteMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.PollVoteMessage} PollVoteMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollVoteMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(message.selectedOptions && message.selectedOptions.length))
                                message.selectedOptions = [];
                            message.selectedOptions.push(reader.bytes());
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PollVoteMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.PollVoteMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.PollVoteMessage} PollVoteMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollVoteMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PollVoteMessage message.
             * @function verify
             * @memberof proto.Message.PollVoteMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PollVoteMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.selectedOptions != null && Object.hasOwnProperty.call(message, "selectedOptions")) {
                    if (!Array.isArray(message.selectedOptions))
                        return "selectedOptions: array expected";
                    for (var i = 0; i < message.selectedOptions.length; ++i)
                        if (!(message.selectedOptions[i] && typeof message.selectedOptions[i].length === "number" || $util.isString(message.selectedOptions[i])))
                            return "selectedOptions: buffer[] expected";
                }
                return null;
            };

            /**
             * Creates a PollVoteMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.PollVoteMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.PollVoteMessage} PollVoteMessage
             */
            PollVoteMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.PollVoteMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.selectedOptions) {
                    if (!Array.isArray(object.selectedOptions))
                        throw TypeError(".proto.Message.PollVoteMessage.selectedOptions: array expected");
                    message.selectedOptions = Array(object.selectedOptions.length);
                    for (var i = 0; i < object.selectedOptions.length; ++i)
                        if (typeof object.selectedOptions[i] === "string")
                            $util.base64.decode(object.selectedOptions[i], message.selectedOptions[i] = $util.newBuffer($util.base64.length(object.selectedOptions[i])), 0);
                        else if (object.selectedOptions[i].length >= 0)
                            message.selectedOptions[i] = object.selectedOptions[i];
                }
                return message;
            };

            /**
             * Creates a plain object from a PollVoteMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.PollVoteMessage
             * @static
             * @param {proto.Message.PollVoteMessage} message PollVoteMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PollVoteMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.selectedOptions = [];
                if (message.selectedOptions && message.selectedOptions.length) {
                    object.selectedOptions = Array(message.selectedOptions.length);
                    for (var j = 0; j < message.selectedOptions.length; ++j)
                        object.selectedOptions[j] = options.bytes === String ? $util.base64.encode(message.selectedOptions[j], 0, message.selectedOptions[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.selectedOptions[j]) : message.selectedOptions[j];
                }
                return object;
            };

            /**
             * Converts this PollVoteMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.PollVoteMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PollVoteMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PollVoteMessage
             * @function getTypeUrl
             * @memberof proto.Message.PollVoteMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PollVoteMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.PollVoteMessage";
            };

            return PollVoteMessage;
        })();

        Message.ProductMessage = (function() {

            /**
             * Properties of a ProductMessage.
             * @memberof proto.Message
             * @interface IProductMessage
             * @property {proto.Message.ProductMessage.IProductSnapshot|null} [product] ProductMessage product
             * @property {string|null} [businessOwnerJid] ProductMessage businessOwnerJid
             * @property {proto.Message.ProductMessage.ICatalogSnapshot|null} [catalog] ProductMessage catalog
             * @property {string|null} [body] ProductMessage body
             * @property {string|null} [footer] ProductMessage footer
             * @property {proto.IContextInfo|null} [contextInfo] ProductMessage contextInfo
             */

            /**
             * Constructs a new ProductMessage.
             * @memberof proto.Message
             * @classdesc Represents a ProductMessage.
             * @implements IProductMessage
             * @constructor
             * @param {proto.Message.IProductMessage=} [properties] Properties to set
             */
            function ProductMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ProductMessage product.
             * @member {proto.Message.ProductMessage.IProductSnapshot|null|undefined} product
             * @memberof proto.Message.ProductMessage
             * @instance
             */
            ProductMessage.prototype.product = null;

            /**
             * ProductMessage businessOwnerJid.
             * @member {string|null|undefined} businessOwnerJid
             * @memberof proto.Message.ProductMessage
             * @instance
             */
            ProductMessage.prototype.businessOwnerJid = null;

            /**
             * ProductMessage catalog.
             * @member {proto.Message.ProductMessage.ICatalogSnapshot|null|undefined} catalog
             * @memberof proto.Message.ProductMessage
             * @instance
             */
            ProductMessage.prototype.catalog = null;

            /**
             * ProductMessage body.
             * @member {string|null|undefined} body
             * @memberof proto.Message.ProductMessage
             * @instance
             */
            ProductMessage.prototype.body = null;

            /**
             * ProductMessage footer.
             * @member {string|null|undefined} footer
             * @memberof proto.Message.ProductMessage
             * @instance
             */
            ProductMessage.prototype.footer = null;

            /**
             * ProductMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.ProductMessage
             * @instance
             */
            ProductMessage.prototype.contextInfo = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductMessage.prototype, "_product", {
                get: $util.oneOfGetter($oneOfFields = ["product"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductMessage.prototype, "_businessOwnerJid", {
                get: $util.oneOfGetter($oneOfFields = ["businessOwnerJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductMessage.prototype, "_catalog", {
                get: $util.oneOfGetter($oneOfFields = ["catalog"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductMessage.prototype, "_body", {
                get: $util.oneOfGetter($oneOfFields = ["body"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductMessage.prototype, "_footer", {
                get: $util.oneOfGetter($oneOfFields = ["footer"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ProductMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.ProductMessage
             * @static
             * @param {proto.Message.IProductMessage=} [properties] Properties to set
             * @returns {proto.Message.ProductMessage} ProductMessage instance
             */
            ProductMessage.create = function create(properties) {
                return new ProductMessage(properties);
            };

            /**
             * Encodes the specified ProductMessage message. Does not implicitly {@link proto.Message.ProductMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.ProductMessage
             * @static
             * @param {proto.Message.IProductMessage} message ProductMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ProductMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.product != null && Object.hasOwnProperty.call(message, "product"))
                    $root.proto.Message.ProductMessage.ProductSnapshot.encode(message.product, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.businessOwnerJid != null && Object.hasOwnProperty.call(message, "businessOwnerJid"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.businessOwnerJid);
                if (message.catalog != null && Object.hasOwnProperty.call(message, "catalog"))
                    $root.proto.Message.ProductMessage.CatalogSnapshot.encode(message.catalog, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.body != null && Object.hasOwnProperty.call(message, "body"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.body);
                if (message.footer != null && Object.hasOwnProperty.call(message, "footer"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.footer);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ProductMessage message, length delimited. Does not implicitly {@link proto.Message.ProductMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.ProductMessage
             * @static
             * @param {proto.Message.IProductMessage} message ProductMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ProductMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ProductMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.ProductMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.ProductMessage} ProductMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ProductMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.product = $root.proto.Message.ProductMessage.ProductSnapshot.decode(reader, reader.uint32(), undefined, q + 1, message.product);
                            message._product = "product";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.businessOwnerJid = reader.stringVerify();
                            message._businessOwnerJid = "businessOwnerJid";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.catalog = $root.proto.Message.ProductMessage.CatalogSnapshot.decode(reader, reader.uint32(), undefined, q + 1, message.catalog);
                            message._catalog = "catalog";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.body = reader.stringVerify();
                            message._body = "body";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.footer = reader.stringVerify();
                            message._footer = "footer";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ProductMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.ProductMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.ProductMessage} ProductMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ProductMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ProductMessage message.
             * @function verify
             * @memberof proto.Message.ProductMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ProductMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.product != null && Object.hasOwnProperty.call(message, "product")) {
                    properties._product = 1;
                    {
                        var error = $root.proto.Message.ProductMessage.ProductSnapshot.verify(message.product, q + 1);
                        if (error)
                            return "product." + error;
                    }
                }
                if (message.businessOwnerJid != null && Object.hasOwnProperty.call(message, "businessOwnerJid")) {
                    properties._businessOwnerJid = 1;
                    if (!$util.isString(message.businessOwnerJid))
                        return "businessOwnerJid: string expected";
                }
                if (message.catalog != null && Object.hasOwnProperty.call(message, "catalog")) {
                    properties._catalog = 1;
                    {
                        var error = $root.proto.Message.ProductMessage.CatalogSnapshot.verify(message.catalog, q + 1);
                        if (error)
                            return "catalog." + error;
                    }
                }
                if (message.body != null && Object.hasOwnProperty.call(message, "body")) {
                    properties._body = 1;
                    if (!$util.isString(message.body))
                        return "body: string expected";
                }
                if (message.footer != null && Object.hasOwnProperty.call(message, "footer")) {
                    properties._footer = 1;
                    if (!$util.isString(message.footer))
                        return "footer: string expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a ProductMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.ProductMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.ProductMessage} ProductMessage
             */
            ProductMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.ProductMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.product != null) {
                    if (!$util.isObject(object.product))
                        throw TypeError(".proto.Message.ProductMessage.product: object expected");
                    message.product = $root.proto.Message.ProductMessage.ProductSnapshot.fromObject(object.product, q + 1);
                }
                if (object.businessOwnerJid != null)
                    message.businessOwnerJid = String(object.businessOwnerJid);
                if (object.catalog != null) {
                    if (!$util.isObject(object.catalog))
                        throw TypeError(".proto.Message.ProductMessage.catalog: object expected");
                    message.catalog = $root.proto.Message.ProductMessage.CatalogSnapshot.fromObject(object.catalog, q + 1);
                }
                if (object.body != null)
                    message.body = String(object.body);
                if (object.footer != null)
                    message.footer = String(object.footer);
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.ProductMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a ProductMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.ProductMessage
             * @static
             * @param {proto.Message.ProductMessage} message ProductMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ProductMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.product != null && Object.hasOwnProperty.call(message, "product"))
                    object.product = $root.proto.Message.ProductMessage.ProductSnapshot.toObject(message.product, options, q + 1);
                if (message.businessOwnerJid != null && Object.hasOwnProperty.call(message, "businessOwnerJid"))
                    object.businessOwnerJid = message.businessOwnerJid;
                if (message.catalog != null && Object.hasOwnProperty.call(message, "catalog"))
                    object.catalog = $root.proto.Message.ProductMessage.CatalogSnapshot.toObject(message.catalog, options, q + 1);
                if (message.body != null && Object.hasOwnProperty.call(message, "body"))
                    object.body = message.body;
                if (message.footer != null && Object.hasOwnProperty.call(message, "footer"))
                    object.footer = message.footer;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                return object;
            };

            /**
             * Converts this ProductMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.ProductMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ProductMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ProductMessage
             * @function getTypeUrl
             * @memberof proto.Message.ProductMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ProductMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.ProductMessage";
            };

            ProductMessage.CatalogSnapshot = (function() {

                /**
                 * Properties of a CatalogSnapshot.
                 * @memberof proto.Message.ProductMessage
                 * @interface ICatalogSnapshot
                 * @property {proto.Message.IImageMessage|null} [catalogImage] CatalogSnapshot catalogImage
                 * @property {string|null} [title] CatalogSnapshot title
                 * @property {string|null} [description] CatalogSnapshot description
                 */

                /**
                 * Constructs a new CatalogSnapshot.
                 * @memberof proto.Message.ProductMessage
                 * @classdesc Represents a CatalogSnapshot.
                 * @implements ICatalogSnapshot
                 * @constructor
                 * @param {proto.Message.ProductMessage.ICatalogSnapshot=} [properties] Properties to set
                 */
                function CatalogSnapshot(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * CatalogSnapshot catalogImage.
                 * @member {proto.Message.IImageMessage|null|undefined} catalogImage
                 * @memberof proto.Message.ProductMessage.CatalogSnapshot
                 * @instance
                 */
                CatalogSnapshot.prototype.catalogImage = null;

                /**
                 * CatalogSnapshot title.
                 * @member {string|null|undefined} title
                 * @memberof proto.Message.ProductMessage.CatalogSnapshot
                 * @instance
                 */
                CatalogSnapshot.prototype.title = null;

                /**
                 * CatalogSnapshot description.
                 * @member {string|null|undefined} description
                 * @memberof proto.Message.ProductMessage.CatalogSnapshot
                 * @instance
                 */
                CatalogSnapshot.prototype.description = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CatalogSnapshot.prototype, "_catalogImage", {
                    get: $util.oneOfGetter($oneOfFields = ["catalogImage"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CatalogSnapshot.prototype, "_title", {
                    get: $util.oneOfGetter($oneOfFields = ["title"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CatalogSnapshot.prototype, "_description", {
                    get: $util.oneOfGetter($oneOfFields = ["description"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new CatalogSnapshot instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.ProductMessage.CatalogSnapshot
                 * @static
                 * @param {proto.Message.ProductMessage.ICatalogSnapshot=} [properties] Properties to set
                 * @returns {proto.Message.ProductMessage.CatalogSnapshot} CatalogSnapshot instance
                 */
                CatalogSnapshot.create = function create(properties) {
                    return new CatalogSnapshot(properties);
                };

                /**
                 * Encodes the specified CatalogSnapshot message. Does not implicitly {@link proto.Message.ProductMessage.CatalogSnapshot.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.ProductMessage.CatalogSnapshot
                 * @static
                 * @param {proto.Message.ProductMessage.ICatalogSnapshot} message CatalogSnapshot message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                CatalogSnapshot.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.catalogImage != null && Object.hasOwnProperty.call(message, "catalogImage"))
                        $root.proto.Message.ImageMessage.encode(message.catalogImage, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                    if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.title);
                    if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.description);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified CatalogSnapshot message, length delimited. Does not implicitly {@link proto.Message.ProductMessage.CatalogSnapshot.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.ProductMessage.CatalogSnapshot
                 * @static
                 * @param {proto.Message.ProductMessage.ICatalogSnapshot} message CatalogSnapshot message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                CatalogSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a CatalogSnapshot message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.ProductMessage.CatalogSnapshot
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.ProductMessage.CatalogSnapshot} CatalogSnapshot
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                CatalogSnapshot.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.catalogImage = $root.proto.Message.ImageMessage.decode(reader, reader.uint32(), undefined, q + 1, message.catalogImage);
                                message._catalogImage = "catalogImage";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.title = reader.stringVerify();
                                message._title = "title";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.description = reader.stringVerify();
                                message._description = "description";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a CatalogSnapshot message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.ProductMessage.CatalogSnapshot
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.ProductMessage.CatalogSnapshot} CatalogSnapshot
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                CatalogSnapshot.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a CatalogSnapshot message.
                 * @function verify
                 * @memberof proto.Message.ProductMessage.CatalogSnapshot
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                CatalogSnapshot.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.catalogImage != null && Object.hasOwnProperty.call(message, "catalogImage")) {
                        properties._catalogImage = 1;
                        {
                            var error = $root.proto.Message.ImageMessage.verify(message.catalogImage, q + 1);
                            if (error)
                                return "catalogImage." + error;
                        }
                    }
                    if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                        properties._title = 1;
                        if (!$util.isString(message.title))
                            return "title: string expected";
                    }
                    if (message.description != null && Object.hasOwnProperty.call(message, "description")) {
                        properties._description = 1;
                        if (!$util.isString(message.description))
                            return "description: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a CatalogSnapshot message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.ProductMessage.CatalogSnapshot
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.ProductMessage.CatalogSnapshot} CatalogSnapshot
                 */
                CatalogSnapshot.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.ProductMessage.CatalogSnapshot: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.catalogImage != null) {
                        if (!$util.isObject(object.catalogImage))
                            throw TypeError(".proto.Message.ProductMessage.CatalogSnapshot.catalogImage: object expected");
                        message.catalogImage = $root.proto.Message.ImageMessage.fromObject(object.catalogImage, q + 1);
                    }
                    if (object.title != null)
                        message.title = String(object.title);
                    if (object.description != null)
                        message.description = String(object.description);
                    return message;
                };

                /**
                 * Creates a plain object from a CatalogSnapshot message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.ProductMessage.CatalogSnapshot
                 * @static
                 * @param {proto.Message.ProductMessage.CatalogSnapshot} message CatalogSnapshot
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                CatalogSnapshot.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.catalogImage != null && Object.hasOwnProperty.call(message, "catalogImage"))
                        object.catalogImage = $root.proto.Message.ImageMessage.toObject(message.catalogImage, options, q + 1);
                    if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                        object.title = message.title;
                    if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                        object.description = message.description;
                    return object;
                };

                /**
                 * Converts this CatalogSnapshot to JSON.
                 * @function toJSON
                 * @memberof proto.Message.ProductMessage.CatalogSnapshot
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                CatalogSnapshot.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for CatalogSnapshot
                 * @function getTypeUrl
                 * @memberof proto.Message.ProductMessage.CatalogSnapshot
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                CatalogSnapshot.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.ProductMessage.CatalogSnapshot";
                };

                return CatalogSnapshot;
            })();

            ProductMessage.ProductSnapshot = (function() {

                /**
                 * Properties of a ProductSnapshot.
                 * @memberof proto.Message.ProductMessage
                 * @interface IProductSnapshot
                 * @property {proto.Message.IImageMessage|null} [productImage] ProductSnapshot productImage
                 * @property {string|null} [productId] ProductSnapshot productId
                 * @property {string|null} [title] ProductSnapshot title
                 * @property {string|null} [description] ProductSnapshot description
                 * @property {string|null} [currencyCode] ProductSnapshot currencyCode
                 * @property {number|Long|null} [priceAmount1000] ProductSnapshot priceAmount1000
                 * @property {string|null} [retailerId] ProductSnapshot retailerId
                 * @property {string|null} [url] ProductSnapshot url
                 * @property {number|null} [productImageCount] ProductSnapshot productImageCount
                 * @property {string|null} [firstImageId] ProductSnapshot firstImageId
                 * @property {number|Long|null} [salePriceAmount1000] ProductSnapshot salePriceAmount1000
                 * @property {string|null} [signedUrl] ProductSnapshot signedUrl
                 */

                /**
                 * Constructs a new ProductSnapshot.
                 * @memberof proto.Message.ProductMessage
                 * @classdesc Represents a ProductSnapshot.
                 * @implements IProductSnapshot
                 * @constructor
                 * @param {proto.Message.ProductMessage.IProductSnapshot=} [properties] Properties to set
                 */
                function ProductSnapshot(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * ProductSnapshot productImage.
                 * @member {proto.Message.IImageMessage|null|undefined} productImage
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @instance
                 */
                ProductSnapshot.prototype.productImage = null;

                /**
                 * ProductSnapshot productId.
                 * @member {string|null|undefined} productId
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @instance
                 */
                ProductSnapshot.prototype.productId = null;

                /**
                 * ProductSnapshot title.
                 * @member {string|null|undefined} title
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @instance
                 */
                ProductSnapshot.prototype.title = null;

                /**
                 * ProductSnapshot description.
                 * @member {string|null|undefined} description
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @instance
                 */
                ProductSnapshot.prototype.description = null;

                /**
                 * ProductSnapshot currencyCode.
                 * @member {string|null|undefined} currencyCode
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @instance
                 */
                ProductSnapshot.prototype.currencyCode = null;

                /**
                 * ProductSnapshot priceAmount1000.
                 * @member {number|Long|null|undefined} priceAmount1000
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @instance
                 */
                ProductSnapshot.prototype.priceAmount1000 = null;

                /**
                 * ProductSnapshot retailerId.
                 * @member {string|null|undefined} retailerId
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @instance
                 */
                ProductSnapshot.prototype.retailerId = null;

                /**
                 * ProductSnapshot url.
                 * @member {string|null|undefined} url
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @instance
                 */
                ProductSnapshot.prototype.url = null;

                /**
                 * ProductSnapshot productImageCount.
                 * @member {number|null|undefined} productImageCount
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @instance
                 */
                ProductSnapshot.prototype.productImageCount = null;

                /**
                 * ProductSnapshot firstImageId.
                 * @member {string|null|undefined} firstImageId
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @instance
                 */
                ProductSnapshot.prototype.firstImageId = null;

                /**
                 * ProductSnapshot salePriceAmount1000.
                 * @member {number|Long|null|undefined} salePriceAmount1000
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @instance
                 */
                ProductSnapshot.prototype.salePriceAmount1000 = null;

                /**
                 * ProductSnapshot signedUrl.
                 * @member {string|null|undefined} signedUrl
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @instance
                 */
                ProductSnapshot.prototype.signedUrl = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ProductSnapshot.prototype, "_productImage", {
                    get: $util.oneOfGetter($oneOfFields = ["productImage"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ProductSnapshot.prototype, "_productId", {
                    get: $util.oneOfGetter($oneOfFields = ["productId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ProductSnapshot.prototype, "_title", {
                    get: $util.oneOfGetter($oneOfFields = ["title"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ProductSnapshot.prototype, "_description", {
                    get: $util.oneOfGetter($oneOfFields = ["description"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ProductSnapshot.prototype, "_currencyCode", {
                    get: $util.oneOfGetter($oneOfFields = ["currencyCode"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ProductSnapshot.prototype, "_priceAmount1000", {
                    get: $util.oneOfGetter($oneOfFields = ["priceAmount1000"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ProductSnapshot.prototype, "_retailerId", {
                    get: $util.oneOfGetter($oneOfFields = ["retailerId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ProductSnapshot.prototype, "_url", {
                    get: $util.oneOfGetter($oneOfFields = ["url"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ProductSnapshot.prototype, "_productImageCount", {
                    get: $util.oneOfGetter($oneOfFields = ["productImageCount"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ProductSnapshot.prototype, "_firstImageId", {
                    get: $util.oneOfGetter($oneOfFields = ["firstImageId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ProductSnapshot.prototype, "_salePriceAmount1000", {
                    get: $util.oneOfGetter($oneOfFields = ["salePriceAmount1000"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ProductSnapshot.prototype, "_signedUrl", {
                    get: $util.oneOfGetter($oneOfFields = ["signedUrl"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new ProductSnapshot instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @static
                 * @param {proto.Message.ProductMessage.IProductSnapshot=} [properties] Properties to set
                 * @returns {proto.Message.ProductMessage.ProductSnapshot} ProductSnapshot instance
                 */
                ProductSnapshot.create = function create(properties) {
                    return new ProductSnapshot(properties);
                };

                /**
                 * Encodes the specified ProductSnapshot message. Does not implicitly {@link proto.Message.ProductMessage.ProductSnapshot.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @static
                 * @param {proto.Message.ProductMessage.IProductSnapshot} message ProductSnapshot message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ProductSnapshot.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.productImage != null && Object.hasOwnProperty.call(message, "productImage"))
                        $root.proto.Message.ImageMessage.encode(message.productImage, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                    if (message.productId != null && Object.hasOwnProperty.call(message, "productId"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.productId);
                    if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.title);
                    if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                        writer.uint32(/* id 4, wireType 2 =*/34).string(message.description);
                    if (message.currencyCode != null && Object.hasOwnProperty.call(message, "currencyCode"))
                        writer.uint32(/* id 5, wireType 2 =*/42).string(message.currencyCode);
                    if (message.priceAmount1000 != null && Object.hasOwnProperty.call(message, "priceAmount1000"))
                        writer.uint32(/* id 6, wireType 0 =*/48).int64(message.priceAmount1000);
                    if (message.retailerId != null && Object.hasOwnProperty.call(message, "retailerId"))
                        writer.uint32(/* id 7, wireType 2 =*/58).string(message.retailerId);
                    if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                        writer.uint32(/* id 8, wireType 2 =*/66).string(message.url);
                    if (message.productImageCount != null && Object.hasOwnProperty.call(message, "productImageCount"))
                        writer.uint32(/* id 9, wireType 0 =*/72).uint32(message.productImageCount);
                    if (message.firstImageId != null && Object.hasOwnProperty.call(message, "firstImageId"))
                        writer.uint32(/* id 11, wireType 2 =*/90).string(message.firstImageId);
                    if (message.salePriceAmount1000 != null && Object.hasOwnProperty.call(message, "salePriceAmount1000"))
                        writer.uint32(/* id 12, wireType 0 =*/96).int64(message.salePriceAmount1000);
                    if (message.signedUrl != null && Object.hasOwnProperty.call(message, "signedUrl"))
                        writer.uint32(/* id 13, wireType 2 =*/106).string(message.signedUrl);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified ProductSnapshot message, length delimited. Does not implicitly {@link proto.Message.ProductMessage.ProductSnapshot.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @static
                 * @param {proto.Message.ProductMessage.IProductSnapshot} message ProductSnapshot message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ProductSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a ProductSnapshot message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.ProductMessage.ProductSnapshot} ProductSnapshot
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ProductSnapshot.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.productImage = $root.proto.Message.ImageMessage.decode(reader, reader.uint32(), undefined, q + 1, message.productImage);
                                message._productImage = "productImage";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.productId = reader.stringVerify();
                                message._productId = "productId";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.title = reader.stringVerify();
                                message._title = "title";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                message.description = reader.stringVerify();
                                message._description = "description";
                                continue;
                            }
                        case 5: {
                                if (u !== 2)
                                    break;
                                message.currencyCode = reader.stringVerify();
                                message._currencyCode = "currencyCode";
                                continue;
                            }
                        case 6: {
                                if (u !== 0)
                                    break;
                                message.priceAmount1000 = reader.int64();
                                message._priceAmount1000 = "priceAmount1000";
                                continue;
                            }
                        case 7: {
                                if (u !== 2)
                                    break;
                                message.retailerId = reader.stringVerify();
                                message._retailerId = "retailerId";
                                continue;
                            }
                        case 8: {
                                if (u !== 2)
                                    break;
                                message.url = reader.stringVerify();
                                message._url = "url";
                                continue;
                            }
                        case 9: {
                                if (u !== 0)
                                    break;
                                message.productImageCount = reader.uint32();
                                message._productImageCount = "productImageCount";
                                continue;
                            }
                        case 11: {
                                if (u !== 2)
                                    break;
                                message.firstImageId = reader.stringVerify();
                                message._firstImageId = "firstImageId";
                                continue;
                            }
                        case 12: {
                                if (u !== 0)
                                    break;
                                message.salePriceAmount1000 = reader.int64();
                                message._salePriceAmount1000 = "salePriceAmount1000";
                                continue;
                            }
                        case 13: {
                                if (u !== 2)
                                    break;
                                message.signedUrl = reader.stringVerify();
                                message._signedUrl = "signedUrl";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a ProductSnapshot message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.ProductMessage.ProductSnapshot} ProductSnapshot
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ProductSnapshot.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a ProductSnapshot message.
                 * @function verify
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                ProductSnapshot.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.productImage != null && Object.hasOwnProperty.call(message, "productImage")) {
                        properties._productImage = 1;
                        {
                            var error = $root.proto.Message.ImageMessage.verify(message.productImage, q + 1);
                            if (error)
                                return "productImage." + error;
                        }
                    }
                    if (message.productId != null && Object.hasOwnProperty.call(message, "productId")) {
                        properties._productId = 1;
                        if (!$util.isString(message.productId))
                            return "productId: string expected";
                    }
                    if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                        properties._title = 1;
                        if (!$util.isString(message.title))
                            return "title: string expected";
                    }
                    if (message.description != null && Object.hasOwnProperty.call(message, "description")) {
                        properties._description = 1;
                        if (!$util.isString(message.description))
                            return "description: string expected";
                    }
                    if (message.currencyCode != null && Object.hasOwnProperty.call(message, "currencyCode")) {
                        properties._currencyCode = 1;
                        if (!$util.isString(message.currencyCode))
                            return "currencyCode: string expected";
                    }
                    if (message.priceAmount1000 != null && Object.hasOwnProperty.call(message, "priceAmount1000")) {
                        properties._priceAmount1000 = 1;
                        if (!$util.isInteger(message.priceAmount1000) && !(message.priceAmount1000 && $util.isInteger(message.priceAmount1000.low) && $util.isInteger(message.priceAmount1000.high)))
                            return "priceAmount1000: integer|Long expected";
                    }
                    if (message.retailerId != null && Object.hasOwnProperty.call(message, "retailerId")) {
                        properties._retailerId = 1;
                        if (!$util.isString(message.retailerId))
                            return "retailerId: string expected";
                    }
                    if (message.url != null && Object.hasOwnProperty.call(message, "url")) {
                        properties._url = 1;
                        if (!$util.isString(message.url))
                            return "url: string expected";
                    }
                    if (message.productImageCount != null && Object.hasOwnProperty.call(message, "productImageCount")) {
                        properties._productImageCount = 1;
                        if (!$util.isInteger(message.productImageCount))
                            return "productImageCount: integer expected";
                    }
                    if (message.firstImageId != null && Object.hasOwnProperty.call(message, "firstImageId")) {
                        properties._firstImageId = 1;
                        if (!$util.isString(message.firstImageId))
                            return "firstImageId: string expected";
                    }
                    if (message.salePriceAmount1000 != null && Object.hasOwnProperty.call(message, "salePriceAmount1000")) {
                        properties._salePriceAmount1000 = 1;
                        if (!$util.isInteger(message.salePriceAmount1000) && !(message.salePriceAmount1000 && $util.isInteger(message.salePriceAmount1000.low) && $util.isInteger(message.salePriceAmount1000.high)))
                            return "salePriceAmount1000: integer|Long expected";
                    }
                    if (message.signedUrl != null && Object.hasOwnProperty.call(message, "signedUrl")) {
                        properties._signedUrl = 1;
                        if (!$util.isString(message.signedUrl))
                            return "signedUrl: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a ProductSnapshot message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.ProductMessage.ProductSnapshot} ProductSnapshot
                 */
                ProductSnapshot.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.ProductMessage.ProductSnapshot: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.productImage != null) {
                        if (!$util.isObject(object.productImage))
                            throw TypeError(".proto.Message.ProductMessage.ProductSnapshot.productImage: object expected");
                        message.productImage = $root.proto.Message.ImageMessage.fromObject(object.productImage, q + 1);
                    }
                    if (object.productId != null)
                        message.productId = String(object.productId);
                    if (object.title != null)
                        message.title = String(object.title);
                    if (object.description != null)
                        message.description = String(object.description);
                    if (object.currencyCode != null)
                        message.currencyCode = String(object.currencyCode);
                    if (object.priceAmount1000 != null)
                        if ($util.Long)
                            message.priceAmount1000 = $util.Long.fromValue(object.priceAmount1000, false);
                        else if (typeof object.priceAmount1000 === "string")
                            message.priceAmount1000 = parseInt(object.priceAmount1000, 10);
                        else if (typeof object.priceAmount1000 === "number")
                            message.priceAmount1000 = object.priceAmount1000;
                        else if (typeof object.priceAmount1000 === "object")
                            message.priceAmount1000 = new $util.LongBits(object.priceAmount1000.low >>> 0, object.priceAmount1000.high >>> 0).toNumber();
                    if (object.retailerId != null)
                        message.retailerId = String(object.retailerId);
                    if (object.url != null)
                        message.url = String(object.url);
                    if (object.productImageCount != null)
                        message.productImageCount = object.productImageCount >>> 0;
                    if (object.firstImageId != null)
                        message.firstImageId = String(object.firstImageId);
                    if (object.salePriceAmount1000 != null)
                        if ($util.Long)
                            message.salePriceAmount1000 = $util.Long.fromValue(object.salePriceAmount1000, false);
                        else if (typeof object.salePriceAmount1000 === "string")
                            message.salePriceAmount1000 = parseInt(object.salePriceAmount1000, 10);
                        else if (typeof object.salePriceAmount1000 === "number")
                            message.salePriceAmount1000 = object.salePriceAmount1000;
                        else if (typeof object.salePriceAmount1000 === "object")
                            message.salePriceAmount1000 = new $util.LongBits(object.salePriceAmount1000.low >>> 0, object.salePriceAmount1000.high >>> 0).toNumber();
                    if (object.signedUrl != null)
                        message.signedUrl = String(object.signedUrl);
                    return message;
                };

                /**
                 * Creates a plain object from a ProductSnapshot message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @static
                 * @param {proto.Message.ProductMessage.ProductSnapshot} message ProductSnapshot
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                ProductSnapshot.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.productImage != null && Object.hasOwnProperty.call(message, "productImage"))
                        object.productImage = $root.proto.Message.ImageMessage.toObject(message.productImage, options, q + 1);
                    if (message.productId != null && Object.hasOwnProperty.call(message, "productId"))
                        object.productId = message.productId;
                    if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                        object.title = message.title;
                    if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                        object.description = message.description;
                    if (message.currencyCode != null && Object.hasOwnProperty.call(message, "currencyCode"))
                        object.currencyCode = message.currencyCode;
                    if (message.priceAmount1000 != null && Object.hasOwnProperty.call(message, "priceAmount1000"))
                        if (typeof BigInt !== "undefined" && options.longs === BigInt)
                            object.priceAmount1000 = typeof message.priceAmount1000 === "number" ? BigInt(message.priceAmount1000) : $util.Long.fromBits(message.priceAmount1000.low >>> 0, message.priceAmount1000.high >>> 0, false).toBigInt();
                        else if (typeof message.priceAmount1000 === "number")
                            object.priceAmount1000 = options.longs === String ? String(message.priceAmount1000) : message.priceAmount1000;
                        else
                            object.priceAmount1000 = options.longs === String ? $util.Long.prototype.toString.call(message.priceAmount1000) : options.longs === Number ? new $util.LongBits(message.priceAmount1000.low >>> 0, message.priceAmount1000.high >>> 0).toNumber() : message.priceAmount1000;
                    if (message.retailerId != null && Object.hasOwnProperty.call(message, "retailerId"))
                        object.retailerId = message.retailerId;
                    if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                        object.url = message.url;
                    if (message.productImageCount != null && Object.hasOwnProperty.call(message, "productImageCount"))
                        object.productImageCount = message.productImageCount;
                    if (message.firstImageId != null && Object.hasOwnProperty.call(message, "firstImageId"))
                        object.firstImageId = message.firstImageId;
                    if (message.salePriceAmount1000 != null && Object.hasOwnProperty.call(message, "salePriceAmount1000"))
                        if (typeof BigInt !== "undefined" && options.longs === BigInt)
                            object.salePriceAmount1000 = typeof message.salePriceAmount1000 === "number" ? BigInt(message.salePriceAmount1000) : $util.Long.fromBits(message.salePriceAmount1000.low >>> 0, message.salePriceAmount1000.high >>> 0, false).toBigInt();
                        else if (typeof message.salePriceAmount1000 === "number")
                            object.salePriceAmount1000 = options.longs === String ? String(message.salePriceAmount1000) : message.salePriceAmount1000;
                        else
                            object.salePriceAmount1000 = options.longs === String ? $util.Long.prototype.toString.call(message.salePriceAmount1000) : options.longs === Number ? new $util.LongBits(message.salePriceAmount1000.low >>> 0, message.salePriceAmount1000.high >>> 0).toNumber() : message.salePriceAmount1000;
                    if (message.signedUrl != null && Object.hasOwnProperty.call(message, "signedUrl"))
                        object.signedUrl = message.signedUrl;
                    return object;
                };

                /**
                 * Converts this ProductSnapshot to JSON.
                 * @function toJSON
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                ProductSnapshot.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for ProductSnapshot
                 * @function getTypeUrl
                 * @memberof proto.Message.ProductMessage.ProductSnapshot
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                ProductSnapshot.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.ProductMessage.ProductSnapshot";
                };

                return ProductSnapshot;
            })();

            return ProductMessage;
        })();

        Message.ProtocolMessage = (function() {

            /**
             * Properties of a ProtocolMessage.
             * @memberof proto.Message
             * @interface IProtocolMessage
             * @property {proto.IMessageKey|null} [key] ProtocolMessage key
             * @property {proto.Message.ProtocolMessage.Type|null} [type] ProtocolMessage type
             * @property {number|null} [ephemeralExpiration] ProtocolMessage ephemeralExpiration
             * @property {number|Long|null} [ephemeralSettingTimestamp] ProtocolMessage ephemeralSettingTimestamp
             * @property {proto.Message.IHistorySyncNotification|null} [historySyncNotification] ProtocolMessage historySyncNotification
             * @property {proto.Message.IAppStateSyncKeyShare|null} [appStateSyncKeyShare] ProtocolMessage appStateSyncKeyShare
             * @property {proto.Message.IAppStateSyncKeyRequest|null} [appStateSyncKeyRequest] ProtocolMessage appStateSyncKeyRequest
             * @property {proto.Message.IInitialSecurityNotificationSettingSync|null} [initialSecurityNotificationSettingSync] ProtocolMessage initialSecurityNotificationSettingSync
             * @property {proto.Message.IAppStateFatalExceptionNotification|null} [appStateFatalExceptionNotification] ProtocolMessage appStateFatalExceptionNotification
             * @property {proto.IDisappearingMode|null} [disappearingMode] ProtocolMessage disappearingMode
             * @property {proto.IMessage|null} [editedMessage] ProtocolMessage editedMessage
             * @property {number|Long|null} [timestampMs] ProtocolMessage timestampMs
             * @property {proto.Message.IPeerDataOperationRequestMessage|null} [peerDataOperationRequestMessage] ProtocolMessage peerDataOperationRequestMessage
             * @property {proto.Message.IPeerDataOperationRequestResponseMessage|null} [peerDataOperationRequestResponseMessage] ProtocolMessage peerDataOperationRequestResponseMessage
             * @property {proto.IBotFeedbackMessage|null} [botFeedbackMessage] ProtocolMessage botFeedbackMessage
             * @property {string|null} [invokerJid] ProtocolMessage invokerJid
             * @property {proto.Message.IRequestWelcomeMessageMetadata|null} [requestWelcomeMessageMetadata] ProtocolMessage requestWelcomeMessageMetadata
             * @property {proto.IMediaNotifyMessage|null} [mediaNotifyMessage] ProtocolMessage mediaNotifyMessage
             * @property {proto.Message.ICloudAPIThreadControlNotification|null} [cloudApiThreadControlNotification] ProtocolMessage cloudApiThreadControlNotification
             * @property {proto.ILIDMigrationMappingSyncMessage|null} [lidMigrationMappingSyncMessage] ProtocolMessage lidMigrationMappingSyncMessage
             * @property {proto.ILimitSharing|null} [limitSharing] ProtocolMessage limitSharing
             * @property {Uint8Array|null} [aiPsiMetadata] ProtocolMessage aiPsiMetadata
             * @property {proto.IAIQueryFanout|null} [aiQueryFanout] ProtocolMessage aiQueryFanout
             * @property {proto.IMemberLabel|null} [memberLabel] ProtocolMessage memberLabel
             * @property {proto.IAIMediaCollectionMessage|null} [aiMediaCollectionMessage] ProtocolMessage aiMediaCollectionMessage
             * @property {number|null} [afterReadDuration] ProtocolMessage afterReadDuration
             * @property {proto.Message.IChatThemeSetting|null} [chatThemeSetting] ProtocolMessage chatThemeSetting
             * @property {proto.IAIMetadataOperation|null} [aiMetadataOperation] ProtocolMessage aiMetadataOperation
             */

            /**
             * Constructs a new ProtocolMessage.
             * @memberof proto.Message
             * @classdesc Represents a ProtocolMessage.
             * @implements IProtocolMessage
             * @constructor
             * @param {proto.Message.IProtocolMessage=} [properties] Properties to set
             */
            function ProtocolMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ProtocolMessage key.
             * @member {proto.IMessageKey|null|undefined} key
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.key = null;

            /**
             * ProtocolMessage type.
             * @member {proto.Message.ProtocolMessage.Type|null|undefined} type
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.type = null;

            /**
             * ProtocolMessage ephemeralExpiration.
             * @member {number|null|undefined} ephemeralExpiration
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.ephemeralExpiration = null;

            /**
             * ProtocolMessage ephemeralSettingTimestamp.
             * @member {number|Long|null|undefined} ephemeralSettingTimestamp
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.ephemeralSettingTimestamp = null;

            /**
             * ProtocolMessage historySyncNotification.
             * @member {proto.Message.IHistorySyncNotification|null|undefined} historySyncNotification
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.historySyncNotification = null;

            /**
             * ProtocolMessage appStateSyncKeyShare.
             * @member {proto.Message.IAppStateSyncKeyShare|null|undefined} appStateSyncKeyShare
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.appStateSyncKeyShare = null;

            /**
             * ProtocolMessage appStateSyncKeyRequest.
             * @member {proto.Message.IAppStateSyncKeyRequest|null|undefined} appStateSyncKeyRequest
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.appStateSyncKeyRequest = null;

            /**
             * ProtocolMessage initialSecurityNotificationSettingSync.
             * @member {proto.Message.IInitialSecurityNotificationSettingSync|null|undefined} initialSecurityNotificationSettingSync
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.initialSecurityNotificationSettingSync = null;

            /**
             * ProtocolMessage appStateFatalExceptionNotification.
             * @member {proto.Message.IAppStateFatalExceptionNotification|null|undefined} appStateFatalExceptionNotification
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.appStateFatalExceptionNotification = null;

            /**
             * ProtocolMessage disappearingMode.
             * @member {proto.IDisappearingMode|null|undefined} disappearingMode
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.disappearingMode = null;

            /**
             * ProtocolMessage editedMessage.
             * @member {proto.IMessage|null|undefined} editedMessage
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.editedMessage = null;

            /**
             * ProtocolMessage timestampMs.
             * @member {number|Long|null|undefined} timestampMs
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.timestampMs = null;

            /**
             * ProtocolMessage peerDataOperationRequestMessage.
             * @member {proto.Message.IPeerDataOperationRequestMessage|null|undefined} peerDataOperationRequestMessage
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.peerDataOperationRequestMessage = null;

            /**
             * ProtocolMessage peerDataOperationRequestResponseMessage.
             * @member {proto.Message.IPeerDataOperationRequestResponseMessage|null|undefined} peerDataOperationRequestResponseMessage
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.peerDataOperationRequestResponseMessage = null;

            /**
             * ProtocolMessage botFeedbackMessage.
             * @member {proto.IBotFeedbackMessage|null|undefined} botFeedbackMessage
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.botFeedbackMessage = null;

            /**
             * ProtocolMessage invokerJid.
             * @member {string|null|undefined} invokerJid
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.invokerJid = null;

            /**
             * ProtocolMessage requestWelcomeMessageMetadata.
             * @member {proto.Message.IRequestWelcomeMessageMetadata|null|undefined} requestWelcomeMessageMetadata
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.requestWelcomeMessageMetadata = null;

            /**
             * ProtocolMessage mediaNotifyMessage.
             * @member {proto.IMediaNotifyMessage|null|undefined} mediaNotifyMessage
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.mediaNotifyMessage = null;

            /**
             * ProtocolMessage cloudApiThreadControlNotification.
             * @member {proto.Message.ICloudAPIThreadControlNotification|null|undefined} cloudApiThreadControlNotification
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.cloudApiThreadControlNotification = null;

            /**
             * ProtocolMessage lidMigrationMappingSyncMessage.
             * @member {proto.ILIDMigrationMappingSyncMessage|null|undefined} lidMigrationMappingSyncMessage
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.lidMigrationMappingSyncMessage = null;

            /**
             * ProtocolMessage limitSharing.
             * @member {proto.ILimitSharing|null|undefined} limitSharing
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.limitSharing = null;

            /**
             * ProtocolMessage aiPsiMetadata.
             * @member {Uint8Array|null|undefined} aiPsiMetadata
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.aiPsiMetadata = null;

            /**
             * ProtocolMessage aiQueryFanout.
             * @member {proto.IAIQueryFanout|null|undefined} aiQueryFanout
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.aiQueryFanout = null;

            /**
             * ProtocolMessage memberLabel.
             * @member {proto.IMemberLabel|null|undefined} memberLabel
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.memberLabel = null;

            /**
             * ProtocolMessage aiMediaCollectionMessage.
             * @member {proto.IAIMediaCollectionMessage|null|undefined} aiMediaCollectionMessage
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.aiMediaCollectionMessage = null;

            /**
             * ProtocolMessage afterReadDuration.
             * @member {number|null|undefined} afterReadDuration
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.afterReadDuration = null;

            /**
             * ProtocolMessage chatThemeSetting.
             * @member {proto.Message.IChatThemeSetting|null|undefined} chatThemeSetting
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.chatThemeSetting = null;

            /**
             * ProtocolMessage aiMetadataOperation.
             * @member {proto.IAIMetadataOperation|null|undefined} aiMetadataOperation
             * @memberof proto.Message.ProtocolMessage
             * @instance
             */
            ProtocolMessage.prototype.aiMetadataOperation = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_ephemeralExpiration", {
                get: $util.oneOfGetter($oneOfFields = ["ephemeralExpiration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_ephemeralSettingTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["ephemeralSettingTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_historySyncNotification", {
                get: $util.oneOfGetter($oneOfFields = ["historySyncNotification"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_appStateSyncKeyShare", {
                get: $util.oneOfGetter($oneOfFields = ["appStateSyncKeyShare"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_appStateSyncKeyRequest", {
                get: $util.oneOfGetter($oneOfFields = ["appStateSyncKeyRequest"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_initialSecurityNotificationSettingSync", {
                get: $util.oneOfGetter($oneOfFields = ["initialSecurityNotificationSettingSync"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_appStateFatalExceptionNotification", {
                get: $util.oneOfGetter($oneOfFields = ["appStateFatalExceptionNotification"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_disappearingMode", {
                get: $util.oneOfGetter($oneOfFields = ["disappearingMode"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_editedMessage", {
                get: $util.oneOfGetter($oneOfFields = ["editedMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_timestampMs", {
                get: $util.oneOfGetter($oneOfFields = ["timestampMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_peerDataOperationRequestMessage", {
                get: $util.oneOfGetter($oneOfFields = ["peerDataOperationRequestMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_peerDataOperationRequestResponseMessage", {
                get: $util.oneOfGetter($oneOfFields = ["peerDataOperationRequestResponseMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_botFeedbackMessage", {
                get: $util.oneOfGetter($oneOfFields = ["botFeedbackMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_invokerJid", {
                get: $util.oneOfGetter($oneOfFields = ["invokerJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_requestWelcomeMessageMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["requestWelcomeMessageMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_mediaNotifyMessage", {
                get: $util.oneOfGetter($oneOfFields = ["mediaNotifyMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_cloudApiThreadControlNotification", {
                get: $util.oneOfGetter($oneOfFields = ["cloudApiThreadControlNotification"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_lidMigrationMappingSyncMessage", {
                get: $util.oneOfGetter($oneOfFields = ["lidMigrationMappingSyncMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_limitSharing", {
                get: $util.oneOfGetter($oneOfFields = ["limitSharing"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_aiPsiMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["aiPsiMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_aiQueryFanout", {
                get: $util.oneOfGetter($oneOfFields = ["aiQueryFanout"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_memberLabel", {
                get: $util.oneOfGetter($oneOfFields = ["memberLabel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_aiMediaCollectionMessage", {
                get: $util.oneOfGetter($oneOfFields = ["aiMediaCollectionMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_afterReadDuration", {
                get: $util.oneOfGetter($oneOfFields = ["afterReadDuration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_chatThemeSetting", {
                get: $util.oneOfGetter($oneOfFields = ["chatThemeSetting"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProtocolMessage.prototype, "_aiMetadataOperation", {
                get: $util.oneOfGetter($oneOfFields = ["aiMetadataOperation"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ProtocolMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.ProtocolMessage
             * @static
             * @param {proto.Message.IProtocolMessage=} [properties] Properties to set
             * @returns {proto.Message.ProtocolMessage} ProtocolMessage instance
             */
            ProtocolMessage.create = function create(properties) {
                return new ProtocolMessage(properties);
            };

            /**
             * Encodes the specified ProtocolMessage message. Does not implicitly {@link proto.Message.ProtocolMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.ProtocolMessage
             * @static
             * @param {proto.Message.IProtocolMessage} message ProtocolMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ProtocolMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    $root.proto.MessageKey.encode(message.key, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.type);
                if (message.ephemeralExpiration != null && Object.hasOwnProperty.call(message, "ephemeralExpiration"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.ephemeralExpiration);
                if (message.ephemeralSettingTimestamp != null && Object.hasOwnProperty.call(message, "ephemeralSettingTimestamp"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.ephemeralSettingTimestamp);
                if (message.historySyncNotification != null && Object.hasOwnProperty.call(message, "historySyncNotification"))
                    $root.proto.Message.HistorySyncNotification.encode(message.historySyncNotification, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
                if (message.appStateSyncKeyShare != null && Object.hasOwnProperty.call(message, "appStateSyncKeyShare"))
                    $root.proto.Message.AppStateSyncKeyShare.encode(message.appStateSyncKeyShare, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
                if (message.appStateSyncKeyRequest != null && Object.hasOwnProperty.call(message, "appStateSyncKeyRequest"))
                    $root.proto.Message.AppStateSyncKeyRequest.encode(message.appStateSyncKeyRequest, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
                if (message.initialSecurityNotificationSettingSync != null && Object.hasOwnProperty.call(message, "initialSecurityNotificationSettingSync"))
                    $root.proto.Message.InitialSecurityNotificationSettingSync.encode(message.initialSecurityNotificationSettingSync, writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
                if (message.appStateFatalExceptionNotification != null && Object.hasOwnProperty.call(message, "appStateFatalExceptionNotification"))
                    $root.proto.Message.AppStateFatalExceptionNotification.encode(message.appStateFatalExceptionNotification, writer.uint32(/* id 10, wireType 2 =*/82).fork(), q + 1).ldelim();
                if (message.disappearingMode != null && Object.hasOwnProperty.call(message, "disappearingMode"))
                    $root.proto.DisappearingMode.encode(message.disappearingMode, writer.uint32(/* id 11, wireType 2 =*/90).fork(), q + 1).ldelim();
                if (message.editedMessage != null && Object.hasOwnProperty.call(message, "editedMessage"))
                    $root.proto.Message.encode(message.editedMessage, writer.uint32(/* id 14, wireType 2 =*/114).fork(), q + 1).ldelim();
                if (message.timestampMs != null && Object.hasOwnProperty.call(message, "timestampMs"))
                    writer.uint32(/* id 15, wireType 0 =*/120).int64(message.timestampMs);
                if (message.peerDataOperationRequestMessage != null && Object.hasOwnProperty.call(message, "peerDataOperationRequestMessage"))
                    $root.proto.Message.PeerDataOperationRequestMessage.encode(message.peerDataOperationRequestMessage, writer.uint32(/* id 16, wireType 2 =*/130).fork(), q + 1).ldelim();
                if (message.peerDataOperationRequestResponseMessage != null && Object.hasOwnProperty.call(message, "peerDataOperationRequestResponseMessage"))
                    $root.proto.Message.PeerDataOperationRequestResponseMessage.encode(message.peerDataOperationRequestResponseMessage, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
                if (message.botFeedbackMessage != null && Object.hasOwnProperty.call(message, "botFeedbackMessage"))
                    $root.proto.BotFeedbackMessage.encode(message.botFeedbackMessage, writer.uint32(/* id 18, wireType 2 =*/146).fork(), q + 1).ldelim();
                if (message.invokerJid != null && Object.hasOwnProperty.call(message, "invokerJid"))
                    writer.uint32(/* id 19, wireType 2 =*/154).string(message.invokerJid);
                if (message.requestWelcomeMessageMetadata != null && Object.hasOwnProperty.call(message, "requestWelcomeMessageMetadata"))
                    $root.proto.Message.RequestWelcomeMessageMetadata.encode(message.requestWelcomeMessageMetadata, writer.uint32(/* id 20, wireType 2 =*/162).fork(), q + 1).ldelim();
                if (message.mediaNotifyMessage != null && Object.hasOwnProperty.call(message, "mediaNotifyMessage"))
                    $root.proto.MediaNotifyMessage.encode(message.mediaNotifyMessage, writer.uint32(/* id 21, wireType 2 =*/170).fork(), q + 1).ldelim();
                if (message.cloudApiThreadControlNotification != null && Object.hasOwnProperty.call(message, "cloudApiThreadControlNotification"))
                    $root.proto.Message.CloudAPIThreadControlNotification.encode(message.cloudApiThreadControlNotification, writer.uint32(/* id 22, wireType 2 =*/178).fork(), q + 1).ldelim();
                if (message.lidMigrationMappingSyncMessage != null && Object.hasOwnProperty.call(message, "lidMigrationMappingSyncMessage"))
                    $root.proto.LIDMigrationMappingSyncMessage.encode(message.lidMigrationMappingSyncMessage, writer.uint32(/* id 23, wireType 2 =*/186).fork(), q + 1).ldelim();
                if (message.limitSharing != null && Object.hasOwnProperty.call(message, "limitSharing"))
                    $root.proto.LimitSharing.encode(message.limitSharing, writer.uint32(/* id 24, wireType 2 =*/194).fork(), q + 1).ldelim();
                if (message.aiPsiMetadata != null && Object.hasOwnProperty.call(message, "aiPsiMetadata"))
                    writer.uint32(/* id 25, wireType 2 =*/202).bytes(message.aiPsiMetadata);
                if (message.aiQueryFanout != null && Object.hasOwnProperty.call(message, "aiQueryFanout"))
                    $root.proto.AIQueryFanout.encode(message.aiQueryFanout, writer.uint32(/* id 26, wireType 2 =*/210).fork(), q + 1).ldelim();
                if (message.memberLabel != null && Object.hasOwnProperty.call(message, "memberLabel"))
                    $root.proto.MemberLabel.encode(message.memberLabel, writer.uint32(/* id 27, wireType 2 =*/218).fork(), q + 1).ldelim();
                if (message.aiMediaCollectionMessage != null && Object.hasOwnProperty.call(message, "aiMediaCollectionMessage"))
                    $root.proto.AIMediaCollectionMessage.encode(message.aiMediaCollectionMessage, writer.uint32(/* id 28, wireType 2 =*/226).fork(), q + 1).ldelim();
                if (message.afterReadDuration != null && Object.hasOwnProperty.call(message, "afterReadDuration"))
                    writer.uint32(/* id 29, wireType 0 =*/232).uint32(message.afterReadDuration);
                if (message.chatThemeSetting != null && Object.hasOwnProperty.call(message, "chatThemeSetting"))
                    $root.proto.Message.ChatThemeSetting.encode(message.chatThemeSetting, writer.uint32(/* id 30, wireType 2 =*/242).fork(), q + 1).ldelim();
                if (message.aiMetadataOperation != null && Object.hasOwnProperty.call(message, "aiMetadataOperation"))
                    $root.proto.AIMetadataOperation.encode(message.aiMetadataOperation, writer.uint32(/* id 31, wireType 2 =*/250).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ProtocolMessage message, length delimited. Does not implicitly {@link proto.Message.ProtocolMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.ProtocolMessage
             * @static
             * @param {proto.Message.IProtocolMessage} message ProtocolMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ProtocolMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ProtocolMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.ProtocolMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.ProtocolMessage} ProtocolMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ProtocolMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.key = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.key);
                            message._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.type = reader.int32();
                            message._type = "type";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.ephemeralExpiration = reader.uint32();
                            message._ephemeralExpiration = "ephemeralExpiration";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.ephemeralSettingTimestamp = reader.int64();
                            message._ephemeralSettingTimestamp = "ephemeralSettingTimestamp";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.historySyncNotification = $root.proto.Message.HistorySyncNotification.decode(reader, reader.uint32(), undefined, q + 1, message.historySyncNotification);
                            message._historySyncNotification = "historySyncNotification";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.appStateSyncKeyShare = $root.proto.Message.AppStateSyncKeyShare.decode(reader, reader.uint32(), undefined, q + 1, message.appStateSyncKeyShare);
                            message._appStateSyncKeyShare = "appStateSyncKeyShare";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.appStateSyncKeyRequest = $root.proto.Message.AppStateSyncKeyRequest.decode(reader, reader.uint32(), undefined, q + 1, message.appStateSyncKeyRequest);
                            message._appStateSyncKeyRequest = "appStateSyncKeyRequest";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            message.initialSecurityNotificationSettingSync = $root.proto.Message.InitialSecurityNotificationSettingSync.decode(reader, reader.uint32(), undefined, q + 1, message.initialSecurityNotificationSettingSync);
                            message._initialSecurityNotificationSettingSync = "initialSecurityNotificationSettingSync";
                            continue;
                        }
                    case 10: {
                            if (u !== 2)
                                break;
                            message.appStateFatalExceptionNotification = $root.proto.Message.AppStateFatalExceptionNotification.decode(reader, reader.uint32(), undefined, q + 1, message.appStateFatalExceptionNotification);
                            message._appStateFatalExceptionNotification = "appStateFatalExceptionNotification";
                            continue;
                        }
                    case 11: {
                            if (u !== 2)
                                break;
                            message.disappearingMode = $root.proto.DisappearingMode.decode(reader, reader.uint32(), undefined, q + 1, message.disappearingMode);
                            message._disappearingMode = "disappearingMode";
                            continue;
                        }
                    case 14: {
                            if (u !== 2)
                                break;
                            message.editedMessage = $root.proto.Message.decode(reader, reader.uint32(), undefined, q + 1, message.editedMessage);
                            message._editedMessage = "editedMessage";
                            continue;
                        }
                    case 15: {
                            if (u !== 0)
                                break;
                            message.timestampMs = reader.int64();
                            message._timestampMs = "timestampMs";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            message.peerDataOperationRequestMessage = $root.proto.Message.PeerDataOperationRequestMessage.decode(reader, reader.uint32(), undefined, q + 1, message.peerDataOperationRequestMessage);
                            message._peerDataOperationRequestMessage = "peerDataOperationRequestMessage";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            message.peerDataOperationRequestResponseMessage = $root.proto.Message.PeerDataOperationRequestResponseMessage.decode(reader, reader.uint32(), undefined, q + 1, message.peerDataOperationRequestResponseMessage);
                            message._peerDataOperationRequestResponseMessage = "peerDataOperationRequestResponseMessage";
                            continue;
                        }
                    case 18: {
                            if (u !== 2)
                                break;
                            message.botFeedbackMessage = $root.proto.BotFeedbackMessage.decode(reader, reader.uint32(), undefined, q + 1, message.botFeedbackMessage);
                            message._botFeedbackMessage = "botFeedbackMessage";
                            continue;
                        }
                    case 19: {
                            if (u !== 2)
                                break;
                            message.invokerJid = reader.stringVerify();
                            message._invokerJid = "invokerJid";
                            continue;
                        }
                    case 20: {
                            if (u !== 2)
                                break;
                            message.requestWelcomeMessageMetadata = $root.proto.Message.RequestWelcomeMessageMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.requestWelcomeMessageMetadata);
                            message._requestWelcomeMessageMetadata = "requestWelcomeMessageMetadata";
                            continue;
                        }
                    case 21: {
                            if (u !== 2)
                                break;
                            message.mediaNotifyMessage = $root.proto.MediaNotifyMessage.decode(reader, reader.uint32(), undefined, q + 1, message.mediaNotifyMessage);
                            message._mediaNotifyMessage = "mediaNotifyMessage";
                            continue;
                        }
                    case 22: {
                            if (u !== 2)
                                break;
                            message.cloudApiThreadControlNotification = $root.proto.Message.CloudAPIThreadControlNotification.decode(reader, reader.uint32(), undefined, q + 1, message.cloudApiThreadControlNotification);
                            message._cloudApiThreadControlNotification = "cloudApiThreadControlNotification";
                            continue;
                        }
                    case 23: {
                            if (u !== 2)
                                break;
                            message.lidMigrationMappingSyncMessage = $root.proto.LIDMigrationMappingSyncMessage.decode(reader, reader.uint32(), undefined, q + 1, message.lidMigrationMappingSyncMessage);
                            message._lidMigrationMappingSyncMessage = "lidMigrationMappingSyncMessage";
                            continue;
                        }
                    case 24: {
                            if (u !== 2)
                                break;
                            message.limitSharing = $root.proto.LimitSharing.decode(reader, reader.uint32(), undefined, q + 1, message.limitSharing);
                            message._limitSharing = "limitSharing";
                            continue;
                        }
                    case 25: {
                            if (u !== 2)
                                break;
                            message.aiPsiMetadata = reader.bytes();
                            message._aiPsiMetadata = "aiPsiMetadata";
                            continue;
                        }
                    case 26: {
                            if (u !== 2)
                                break;
                            message.aiQueryFanout = $root.proto.AIQueryFanout.decode(reader, reader.uint32(), undefined, q + 1, message.aiQueryFanout);
                            message._aiQueryFanout = "aiQueryFanout";
                            continue;
                        }
                    case 27: {
                            if (u !== 2)
                                break;
                            message.memberLabel = $root.proto.MemberLabel.decode(reader, reader.uint32(), undefined, q + 1, message.memberLabel);
                            message._memberLabel = "memberLabel";
                            continue;
                        }
                    case 28: {
                            if (u !== 2)
                                break;
                            message.aiMediaCollectionMessage = $root.proto.AIMediaCollectionMessage.decode(reader, reader.uint32(), undefined, q + 1, message.aiMediaCollectionMessage);
                            message._aiMediaCollectionMessage = "aiMediaCollectionMessage";
                            continue;
                        }
                    case 29: {
                            if (u !== 0)
                                break;
                            message.afterReadDuration = reader.uint32();
                            message._afterReadDuration = "afterReadDuration";
                            continue;
                        }
                    case 30: {
                            if (u !== 2)
                                break;
                            message.chatThemeSetting = $root.proto.Message.ChatThemeSetting.decode(reader, reader.uint32(), undefined, q + 1, message.chatThemeSetting);
                            message._chatThemeSetting = "chatThemeSetting";
                            continue;
                        }
                    case 31: {
                            if (u !== 2)
                                break;
                            message.aiMetadataOperation = $root.proto.AIMetadataOperation.decode(reader, reader.uint32(), undefined, q + 1, message.aiMetadataOperation);
                            message._aiMetadataOperation = "aiMetadataOperation";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ProtocolMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.ProtocolMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.ProtocolMessage} ProtocolMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ProtocolMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ProtocolMessage message.
             * @function verify
             * @memberof proto.Message.ProtocolMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ProtocolMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                    properties._key = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.key, q + 1);
                        if (error)
                            return "key." + error;
                    }
                }
                if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                    properties._type = 1;
                    if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                        return "type: enum value expected";
                }
                if (message.ephemeralExpiration != null && Object.hasOwnProperty.call(message, "ephemeralExpiration")) {
                    properties._ephemeralExpiration = 1;
                    if (!$util.isInteger(message.ephemeralExpiration))
                        return "ephemeralExpiration: integer expected";
                }
                if (message.ephemeralSettingTimestamp != null && Object.hasOwnProperty.call(message, "ephemeralSettingTimestamp")) {
                    properties._ephemeralSettingTimestamp = 1;
                    if (!$util.isInteger(message.ephemeralSettingTimestamp) && !(message.ephemeralSettingTimestamp && $util.isInteger(message.ephemeralSettingTimestamp.low) && $util.isInteger(message.ephemeralSettingTimestamp.high)))
                        return "ephemeralSettingTimestamp: integer|Long expected";
                }
                if (message.historySyncNotification != null && Object.hasOwnProperty.call(message, "historySyncNotification")) {
                    properties._historySyncNotification = 1;
                    {
                        var error = $root.proto.Message.HistorySyncNotification.verify(message.historySyncNotification, q + 1);
                        if (error)
                            return "historySyncNotification." + error;
                    }
                }
                if (message.appStateSyncKeyShare != null && Object.hasOwnProperty.call(message, "appStateSyncKeyShare")) {
                    properties._appStateSyncKeyShare = 1;
                    {
                        var error = $root.proto.Message.AppStateSyncKeyShare.verify(message.appStateSyncKeyShare, q + 1);
                        if (error)
                            return "appStateSyncKeyShare." + error;
                    }
                }
                if (message.appStateSyncKeyRequest != null && Object.hasOwnProperty.call(message, "appStateSyncKeyRequest")) {
                    properties._appStateSyncKeyRequest = 1;
                    {
                        var error = $root.proto.Message.AppStateSyncKeyRequest.verify(message.appStateSyncKeyRequest, q + 1);
                        if (error)
                            return "appStateSyncKeyRequest." + error;
                    }
                }
                if (message.initialSecurityNotificationSettingSync != null && Object.hasOwnProperty.call(message, "initialSecurityNotificationSettingSync")) {
                    properties._initialSecurityNotificationSettingSync = 1;
                    {
                        var error = $root.proto.Message.InitialSecurityNotificationSettingSync.verify(message.initialSecurityNotificationSettingSync, q + 1);
                        if (error)
                            return "initialSecurityNotificationSettingSync." + error;
                    }
                }
                if (message.appStateFatalExceptionNotification != null && Object.hasOwnProperty.call(message, "appStateFatalExceptionNotification")) {
                    properties._appStateFatalExceptionNotification = 1;
                    {
                        var error = $root.proto.Message.AppStateFatalExceptionNotification.verify(message.appStateFatalExceptionNotification, q + 1);
                        if (error)
                            return "appStateFatalExceptionNotification." + error;
                    }
                }
                if (message.disappearingMode != null && Object.hasOwnProperty.call(message, "disappearingMode")) {
                    properties._disappearingMode = 1;
                    {
                        var error = $root.proto.DisappearingMode.verify(message.disappearingMode, q + 1);
                        if (error)
                            return "disappearingMode." + error;
                    }
                }
                if (message.editedMessage != null && Object.hasOwnProperty.call(message, "editedMessage")) {
                    properties._editedMessage = 1;
                    {
                        var error = $root.proto.Message.verify(message.editedMessage, q + 1);
                        if (error)
                            return "editedMessage." + error;
                    }
                }
                if (message.timestampMs != null && Object.hasOwnProperty.call(message, "timestampMs")) {
                    properties._timestampMs = 1;
                    if (!$util.isInteger(message.timestampMs) && !(message.timestampMs && $util.isInteger(message.timestampMs.low) && $util.isInteger(message.timestampMs.high)))
                        return "timestampMs: integer|Long expected";
                }
                if (message.peerDataOperationRequestMessage != null && Object.hasOwnProperty.call(message, "peerDataOperationRequestMessage")) {
                    properties._peerDataOperationRequestMessage = 1;
                    {
                        var error = $root.proto.Message.PeerDataOperationRequestMessage.verify(message.peerDataOperationRequestMessage, q + 1);
                        if (error)
                            return "peerDataOperationRequestMessage." + error;
                    }
                }
                if (message.peerDataOperationRequestResponseMessage != null && Object.hasOwnProperty.call(message, "peerDataOperationRequestResponseMessage")) {
                    properties._peerDataOperationRequestResponseMessage = 1;
                    {
                        var error = $root.proto.Message.PeerDataOperationRequestResponseMessage.verify(message.peerDataOperationRequestResponseMessage, q + 1);
                        if (error)
                            return "peerDataOperationRequestResponseMessage." + error;
                    }
                }
                if (message.botFeedbackMessage != null && Object.hasOwnProperty.call(message, "botFeedbackMessage")) {
                    properties._botFeedbackMessage = 1;
                    {
                        var error = $root.proto.BotFeedbackMessage.verify(message.botFeedbackMessage, q + 1);
                        if (error)
                            return "botFeedbackMessage." + error;
                    }
                }
                if (message.invokerJid != null && Object.hasOwnProperty.call(message, "invokerJid")) {
                    properties._invokerJid = 1;
                    if (!$util.isString(message.invokerJid))
                        return "invokerJid: string expected";
                }
                if (message.requestWelcomeMessageMetadata != null && Object.hasOwnProperty.call(message, "requestWelcomeMessageMetadata")) {
                    properties._requestWelcomeMessageMetadata = 1;
                    {
                        var error = $root.proto.Message.RequestWelcomeMessageMetadata.verify(message.requestWelcomeMessageMetadata, q + 1);
                        if (error)
                            return "requestWelcomeMessageMetadata." + error;
                    }
                }
                if (message.mediaNotifyMessage != null && Object.hasOwnProperty.call(message, "mediaNotifyMessage")) {
                    properties._mediaNotifyMessage = 1;
                    {
                        var error = $root.proto.MediaNotifyMessage.verify(message.mediaNotifyMessage, q + 1);
                        if (error)
                            return "mediaNotifyMessage." + error;
                    }
                }
                if (message.cloudApiThreadControlNotification != null && Object.hasOwnProperty.call(message, "cloudApiThreadControlNotification")) {
                    properties._cloudApiThreadControlNotification = 1;
                    {
                        var error = $root.proto.Message.CloudAPIThreadControlNotification.verify(message.cloudApiThreadControlNotification, q + 1);
                        if (error)
                            return "cloudApiThreadControlNotification." + error;
                    }
                }
                if (message.lidMigrationMappingSyncMessage != null && Object.hasOwnProperty.call(message, "lidMigrationMappingSyncMessage")) {
                    properties._lidMigrationMappingSyncMessage = 1;
                    {
                        var error = $root.proto.LIDMigrationMappingSyncMessage.verify(message.lidMigrationMappingSyncMessage, q + 1);
                        if (error)
                            return "lidMigrationMappingSyncMessage." + error;
                    }
                }
                if (message.limitSharing != null && Object.hasOwnProperty.call(message, "limitSharing")) {
                    properties._limitSharing = 1;
                    {
                        var error = $root.proto.LimitSharing.verify(message.limitSharing, q + 1);
                        if (error)
                            return "limitSharing." + error;
                    }
                }
                if (message.aiPsiMetadata != null && Object.hasOwnProperty.call(message, "aiPsiMetadata")) {
                    properties._aiPsiMetadata = 1;
                    if (!(message.aiPsiMetadata && typeof message.aiPsiMetadata.length === "number" || $util.isString(message.aiPsiMetadata)))
                        return "aiPsiMetadata: buffer expected";
                }
                if (message.aiQueryFanout != null && Object.hasOwnProperty.call(message, "aiQueryFanout")) {
                    properties._aiQueryFanout = 1;
                    {
                        var error = $root.proto.AIQueryFanout.verify(message.aiQueryFanout, q + 1);
                        if (error)
                            return "aiQueryFanout." + error;
                    }
                }
                if (message.memberLabel != null && Object.hasOwnProperty.call(message, "memberLabel")) {
                    properties._memberLabel = 1;
                    {
                        var error = $root.proto.MemberLabel.verify(message.memberLabel, q + 1);
                        if (error)
                            return "memberLabel." + error;
                    }
                }
                if (message.aiMediaCollectionMessage != null && Object.hasOwnProperty.call(message, "aiMediaCollectionMessage")) {
                    properties._aiMediaCollectionMessage = 1;
                    {
                        var error = $root.proto.AIMediaCollectionMessage.verify(message.aiMediaCollectionMessage, q + 1);
                        if (error)
                            return "aiMediaCollectionMessage." + error;
                    }
                }
                if (message.afterReadDuration != null && Object.hasOwnProperty.call(message, "afterReadDuration")) {
                    properties._afterReadDuration = 1;
                    if (!$util.isInteger(message.afterReadDuration))
                        return "afterReadDuration: integer expected";
                }
                if (message.chatThemeSetting != null && Object.hasOwnProperty.call(message, "chatThemeSetting")) {
                    properties._chatThemeSetting = 1;
                    {
                        var error = $root.proto.Message.ChatThemeSetting.verify(message.chatThemeSetting, q + 1);
                        if (error)
                            return "chatThemeSetting." + error;
                    }
                }
                if (message.aiMetadataOperation != null && Object.hasOwnProperty.call(message, "aiMetadataOperation")) {
                    properties._aiMetadataOperation = 1;
                    {
                        var error = $root.proto.AIMetadataOperation.verify(message.aiMetadataOperation, q + 1);
                        if (error)
                            return "aiMetadataOperation." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a ProtocolMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.ProtocolMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.ProtocolMessage} ProtocolMessage
             */
            ProtocolMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.ProtocolMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.key != null) {
                    if (!$util.isObject(object.key))
                        throw TypeError(".proto.Message.ProtocolMessage.key: object expected");
                    message.key = $root.proto.MessageKey.fromObject(object.key, q + 1);
                }
                switch (object.type) {
                case "REVOKE":
                case 0:
                    message.type = 0;
                    break;
                case "EPHEMERAL_SETTING":
                case 3:
                    message.type = 3;
                    break;
                case "EPHEMERAL_SYNC_RESPONSE":
                case 4:
                    message.type = 4;
                    break;
                case "HISTORY_SYNC_NOTIFICATION":
                case 5:
                    message.type = 5;
                    break;
                case "APP_STATE_SYNC_KEY_SHARE":
                case 6:
                    message.type = 6;
                    break;
                case "APP_STATE_SYNC_KEY_REQUEST":
                case 7:
                    message.type = 7;
                    break;
                case "MSG_FANOUT_BACKFILL_REQUEST":
                case 8:
                    message.type = 8;
                    break;
                case "INITIAL_SECURITY_NOTIFICATION_SETTING_SYNC":
                case 9:
                    message.type = 9;
                    break;
                case "APP_STATE_FATAL_EXCEPTION_NOTIFICATION":
                case 10:
                    message.type = 10;
                    break;
                case "SHARE_PHONE_NUMBER":
                case 11:
                    message.type = 11;
                    break;
                case "MESSAGE_EDIT":
                case 14:
                    message.type = 14;
                    break;
                case "PEER_DATA_OPERATION_REQUEST_MESSAGE":
                case 16:
                    message.type = 16;
                    break;
                case "PEER_DATA_OPERATION_REQUEST_RESPONSE_MESSAGE":
                case 17:
                    message.type = 17;
                    break;
                case "REQUEST_WELCOME_MESSAGE":
                case 18:
                    message.type = 18;
                    break;
                case "BOT_FEEDBACK_MESSAGE":
                case 19:
                    message.type = 19;
                    break;
                case "MEDIA_NOTIFY_MESSAGE":
                case 20:
                    message.type = 20;
                    break;
                case "CLOUD_API_THREAD_CONTROL_NOTIFICATION":
                case 21:
                    message.type = 21;
                    break;
                case "LID_MIGRATION_MAPPING_SYNC":
                case 22:
                    message.type = 22;
                    break;
                case "REMINDER_MESSAGE":
                case 23:
                    message.type = 23;
                    break;
                case "BOT_MEMU_ONBOARDING_MESSAGE":
                case 24:
                    message.type = 24;
                    break;
                case "STATUS_MENTION_MESSAGE":
                case 25:
                    message.type = 25;
                    break;
                case "STOP_GENERATION_MESSAGE":
                case 26:
                    message.type = 26;
                    break;
                case "LIMIT_SHARING":
                case 27:
                    message.type = 27;
                    break;
                case "AI_PSI_METADATA":
                case 28:
                    message.type = 28;
                    break;
                case "AI_QUERY_FANOUT":
                case 29:
                    message.type = 29;
                    break;
                case "GROUP_MEMBER_LABEL_CHANGE":
                case 30:
                    message.type = 30;
                    break;
                case "AI_MEDIA_COLLECTION_MESSAGE":
                case 31:
                    message.type = 31;
                    break;
                case "MESSAGE_UNSCHEDULE":
                case 32:
                    message.type = 32;
                    break;
                case "CHAT_THEME_SETTING":
                case 34:
                    message.type = 34;
                    break;
                case "AI_METADATA_OPERATION":
                case 35:
                    message.type = 35;
                    break;
                default:
                    if (typeof object.type === "number" && (object.type | 0) === object.type)
                        message.type = object.type;
                }
                if (object.ephemeralExpiration != null)
                    message.ephemeralExpiration = object.ephemeralExpiration >>> 0;
                if (object.ephemeralSettingTimestamp != null)
                    if ($util.Long)
                        message.ephemeralSettingTimestamp = $util.Long.fromValue(object.ephemeralSettingTimestamp, false);
                    else if (typeof object.ephemeralSettingTimestamp === "string")
                        message.ephemeralSettingTimestamp = parseInt(object.ephemeralSettingTimestamp, 10);
                    else if (typeof object.ephemeralSettingTimestamp === "number")
                        message.ephemeralSettingTimestamp = object.ephemeralSettingTimestamp;
                    else if (typeof object.ephemeralSettingTimestamp === "object")
                        message.ephemeralSettingTimestamp = new $util.LongBits(object.ephemeralSettingTimestamp.low >>> 0, object.ephemeralSettingTimestamp.high >>> 0).toNumber();
                if (object.historySyncNotification != null) {
                    if (!$util.isObject(object.historySyncNotification))
                        throw TypeError(".proto.Message.ProtocolMessage.historySyncNotification: object expected");
                    message.historySyncNotification = $root.proto.Message.HistorySyncNotification.fromObject(object.historySyncNotification, q + 1);
                }
                if (object.appStateSyncKeyShare != null) {
                    if (!$util.isObject(object.appStateSyncKeyShare))
                        throw TypeError(".proto.Message.ProtocolMessage.appStateSyncKeyShare: object expected");
                    message.appStateSyncKeyShare = $root.proto.Message.AppStateSyncKeyShare.fromObject(object.appStateSyncKeyShare, q + 1);
                }
                if (object.appStateSyncKeyRequest != null) {
                    if (!$util.isObject(object.appStateSyncKeyRequest))
                        throw TypeError(".proto.Message.ProtocolMessage.appStateSyncKeyRequest: object expected");
                    message.appStateSyncKeyRequest = $root.proto.Message.AppStateSyncKeyRequest.fromObject(object.appStateSyncKeyRequest, q + 1);
                }
                if (object.initialSecurityNotificationSettingSync != null) {
                    if (!$util.isObject(object.initialSecurityNotificationSettingSync))
                        throw TypeError(".proto.Message.ProtocolMessage.initialSecurityNotificationSettingSync: object expected");
                    message.initialSecurityNotificationSettingSync = $root.proto.Message.InitialSecurityNotificationSettingSync.fromObject(object.initialSecurityNotificationSettingSync, q + 1);
                }
                if (object.appStateFatalExceptionNotification != null) {
                    if (!$util.isObject(object.appStateFatalExceptionNotification))
                        throw TypeError(".proto.Message.ProtocolMessage.appStateFatalExceptionNotification: object expected");
                    message.appStateFatalExceptionNotification = $root.proto.Message.AppStateFatalExceptionNotification.fromObject(object.appStateFatalExceptionNotification, q + 1);
                }
                if (object.disappearingMode != null) {
                    if (!$util.isObject(object.disappearingMode))
                        throw TypeError(".proto.Message.ProtocolMessage.disappearingMode: object expected");
                    message.disappearingMode = $root.proto.DisappearingMode.fromObject(object.disappearingMode, q + 1);
                }
                if (object.editedMessage != null) {
                    if (!$util.isObject(object.editedMessage))
                        throw TypeError(".proto.Message.ProtocolMessage.editedMessage: object expected");
                    message.editedMessage = $root.proto.Message.fromObject(object.editedMessage, q + 1);
                }
                if (object.timestampMs != null)
                    if ($util.Long)
                        message.timestampMs = $util.Long.fromValue(object.timestampMs, false);
                    else if (typeof object.timestampMs === "string")
                        message.timestampMs = parseInt(object.timestampMs, 10);
                    else if (typeof object.timestampMs === "number")
                        message.timestampMs = object.timestampMs;
                    else if (typeof object.timestampMs === "object")
                        message.timestampMs = new $util.LongBits(object.timestampMs.low >>> 0, object.timestampMs.high >>> 0).toNumber();
                if (object.peerDataOperationRequestMessage != null) {
                    if (!$util.isObject(object.peerDataOperationRequestMessage))
                        throw TypeError(".proto.Message.ProtocolMessage.peerDataOperationRequestMessage: object expected");
                    message.peerDataOperationRequestMessage = $root.proto.Message.PeerDataOperationRequestMessage.fromObject(object.peerDataOperationRequestMessage, q + 1);
                }
                if (object.peerDataOperationRequestResponseMessage != null) {
                    if (!$util.isObject(object.peerDataOperationRequestResponseMessage))
                        throw TypeError(".proto.Message.ProtocolMessage.peerDataOperationRequestResponseMessage: object expected");
                    message.peerDataOperationRequestResponseMessage = $root.proto.Message.PeerDataOperationRequestResponseMessage.fromObject(object.peerDataOperationRequestResponseMessage, q + 1);
                }
                if (object.botFeedbackMessage != null) {
                    if (!$util.isObject(object.botFeedbackMessage))
                        throw TypeError(".proto.Message.ProtocolMessage.botFeedbackMessage: object expected");
                    message.botFeedbackMessage = $root.proto.BotFeedbackMessage.fromObject(object.botFeedbackMessage, q + 1);
                }
                if (object.invokerJid != null)
                    message.invokerJid = String(object.invokerJid);
                if (object.requestWelcomeMessageMetadata != null) {
                    if (!$util.isObject(object.requestWelcomeMessageMetadata))
                        throw TypeError(".proto.Message.ProtocolMessage.requestWelcomeMessageMetadata: object expected");
                    message.requestWelcomeMessageMetadata = $root.proto.Message.RequestWelcomeMessageMetadata.fromObject(object.requestWelcomeMessageMetadata, q + 1);
                }
                if (object.mediaNotifyMessage != null) {
                    if (!$util.isObject(object.mediaNotifyMessage))
                        throw TypeError(".proto.Message.ProtocolMessage.mediaNotifyMessage: object expected");
                    message.mediaNotifyMessage = $root.proto.MediaNotifyMessage.fromObject(object.mediaNotifyMessage, q + 1);
                }
                if (object.cloudApiThreadControlNotification != null) {
                    if (!$util.isObject(object.cloudApiThreadControlNotification))
                        throw TypeError(".proto.Message.ProtocolMessage.cloudApiThreadControlNotification: object expected");
                    message.cloudApiThreadControlNotification = $root.proto.Message.CloudAPIThreadControlNotification.fromObject(object.cloudApiThreadControlNotification, q + 1);
                }
                if (object.lidMigrationMappingSyncMessage != null) {
                    if (!$util.isObject(object.lidMigrationMappingSyncMessage))
                        throw TypeError(".proto.Message.ProtocolMessage.lidMigrationMappingSyncMessage: object expected");
                    message.lidMigrationMappingSyncMessage = $root.proto.LIDMigrationMappingSyncMessage.fromObject(object.lidMigrationMappingSyncMessage, q + 1);
                }
                if (object.limitSharing != null) {
                    if (!$util.isObject(object.limitSharing))
                        throw TypeError(".proto.Message.ProtocolMessage.limitSharing: object expected");
                    message.limitSharing = $root.proto.LimitSharing.fromObject(object.limitSharing, q + 1);
                }
                if (object.aiPsiMetadata != null)
                    if (typeof object.aiPsiMetadata === "string")
                        $util.base64.decode(object.aiPsiMetadata, message.aiPsiMetadata = $util.newBuffer($util.base64.length(object.aiPsiMetadata)), 0);
                    else if (object.aiPsiMetadata.length >= 0)
                        message.aiPsiMetadata = object.aiPsiMetadata;
                if (object.aiQueryFanout != null) {
                    if (!$util.isObject(object.aiQueryFanout))
                        throw TypeError(".proto.Message.ProtocolMessage.aiQueryFanout: object expected");
                    message.aiQueryFanout = $root.proto.AIQueryFanout.fromObject(object.aiQueryFanout, q + 1);
                }
                if (object.memberLabel != null) {
                    if (!$util.isObject(object.memberLabel))
                        throw TypeError(".proto.Message.ProtocolMessage.memberLabel: object expected");
                    message.memberLabel = $root.proto.MemberLabel.fromObject(object.memberLabel, q + 1);
                }
                if (object.aiMediaCollectionMessage != null) {
                    if (!$util.isObject(object.aiMediaCollectionMessage))
                        throw TypeError(".proto.Message.ProtocolMessage.aiMediaCollectionMessage: object expected");
                    message.aiMediaCollectionMessage = $root.proto.AIMediaCollectionMessage.fromObject(object.aiMediaCollectionMessage, q + 1);
                }
                if (object.afterReadDuration != null)
                    message.afterReadDuration = object.afterReadDuration >>> 0;
                if (object.chatThemeSetting != null) {
                    if (!$util.isObject(object.chatThemeSetting))
                        throw TypeError(".proto.Message.ProtocolMessage.chatThemeSetting: object expected");
                    message.chatThemeSetting = $root.proto.Message.ChatThemeSetting.fromObject(object.chatThemeSetting, q + 1);
                }
                if (object.aiMetadataOperation != null) {
                    if (!$util.isObject(object.aiMetadataOperation))
                        throw TypeError(".proto.Message.ProtocolMessage.aiMetadataOperation: object expected");
                    message.aiMetadataOperation = $root.proto.AIMetadataOperation.fromObject(object.aiMetadataOperation, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a ProtocolMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.ProtocolMessage
             * @static
             * @param {proto.Message.ProtocolMessage} message ProtocolMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ProtocolMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    object.key = $root.proto.MessageKey.toObject(message.key, options, q + 1);
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    object.type = options.enums === String ? $root.proto.Message.ProtocolMessage.Type[message.type] === undefined ? message.type : $root.proto.Message.ProtocolMessage.Type[message.type] : message.type;
                if (message.ephemeralExpiration != null && Object.hasOwnProperty.call(message, "ephemeralExpiration"))
                    object.ephemeralExpiration = message.ephemeralExpiration;
                if (message.ephemeralSettingTimestamp != null && Object.hasOwnProperty.call(message, "ephemeralSettingTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.ephemeralSettingTimestamp = typeof message.ephemeralSettingTimestamp === "number" ? BigInt(message.ephemeralSettingTimestamp) : $util.Long.fromBits(message.ephemeralSettingTimestamp.low >>> 0, message.ephemeralSettingTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.ephemeralSettingTimestamp === "number")
                        object.ephemeralSettingTimestamp = options.longs === String ? String(message.ephemeralSettingTimestamp) : message.ephemeralSettingTimestamp;
                    else
                        object.ephemeralSettingTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.ephemeralSettingTimestamp) : options.longs === Number ? new $util.LongBits(message.ephemeralSettingTimestamp.low >>> 0, message.ephemeralSettingTimestamp.high >>> 0).toNumber() : message.ephemeralSettingTimestamp;
                if (message.historySyncNotification != null && Object.hasOwnProperty.call(message, "historySyncNotification"))
                    object.historySyncNotification = $root.proto.Message.HistorySyncNotification.toObject(message.historySyncNotification, options, q + 1);
                if (message.appStateSyncKeyShare != null && Object.hasOwnProperty.call(message, "appStateSyncKeyShare"))
                    object.appStateSyncKeyShare = $root.proto.Message.AppStateSyncKeyShare.toObject(message.appStateSyncKeyShare, options, q + 1);
                if (message.appStateSyncKeyRequest != null && Object.hasOwnProperty.call(message, "appStateSyncKeyRequest"))
                    object.appStateSyncKeyRequest = $root.proto.Message.AppStateSyncKeyRequest.toObject(message.appStateSyncKeyRequest, options, q + 1);
                if (message.initialSecurityNotificationSettingSync != null && Object.hasOwnProperty.call(message, "initialSecurityNotificationSettingSync"))
                    object.initialSecurityNotificationSettingSync = $root.proto.Message.InitialSecurityNotificationSettingSync.toObject(message.initialSecurityNotificationSettingSync, options, q + 1);
                if (message.appStateFatalExceptionNotification != null && Object.hasOwnProperty.call(message, "appStateFatalExceptionNotification"))
                    object.appStateFatalExceptionNotification = $root.proto.Message.AppStateFatalExceptionNotification.toObject(message.appStateFatalExceptionNotification, options, q + 1);
                if (message.disappearingMode != null && Object.hasOwnProperty.call(message, "disappearingMode"))
                    object.disappearingMode = $root.proto.DisappearingMode.toObject(message.disappearingMode, options, q + 1);
                if (message.editedMessage != null && Object.hasOwnProperty.call(message, "editedMessage"))
                    object.editedMessage = $root.proto.Message.toObject(message.editedMessage, options, q + 1);
                if (message.timestampMs != null && Object.hasOwnProperty.call(message, "timestampMs"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.timestampMs = typeof message.timestampMs === "number" ? BigInt(message.timestampMs) : $util.Long.fromBits(message.timestampMs.low >>> 0, message.timestampMs.high >>> 0, false).toBigInt();
                    else if (typeof message.timestampMs === "number")
                        object.timestampMs = options.longs === String ? String(message.timestampMs) : message.timestampMs;
                    else
                        object.timestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.timestampMs) : options.longs === Number ? new $util.LongBits(message.timestampMs.low >>> 0, message.timestampMs.high >>> 0).toNumber() : message.timestampMs;
                if (message.peerDataOperationRequestMessage != null && Object.hasOwnProperty.call(message, "peerDataOperationRequestMessage"))
                    object.peerDataOperationRequestMessage = $root.proto.Message.PeerDataOperationRequestMessage.toObject(message.peerDataOperationRequestMessage, options, q + 1);
                if (message.peerDataOperationRequestResponseMessage != null && Object.hasOwnProperty.call(message, "peerDataOperationRequestResponseMessage"))
                    object.peerDataOperationRequestResponseMessage = $root.proto.Message.PeerDataOperationRequestResponseMessage.toObject(message.peerDataOperationRequestResponseMessage, options, q + 1);
                if (message.botFeedbackMessage != null && Object.hasOwnProperty.call(message, "botFeedbackMessage"))
                    object.botFeedbackMessage = $root.proto.BotFeedbackMessage.toObject(message.botFeedbackMessage, options, q + 1);
                if (message.invokerJid != null && Object.hasOwnProperty.call(message, "invokerJid"))
                    object.invokerJid = message.invokerJid;
                if (message.requestWelcomeMessageMetadata != null && Object.hasOwnProperty.call(message, "requestWelcomeMessageMetadata"))
                    object.requestWelcomeMessageMetadata = $root.proto.Message.RequestWelcomeMessageMetadata.toObject(message.requestWelcomeMessageMetadata, options, q + 1);
                if (message.mediaNotifyMessage != null && Object.hasOwnProperty.call(message, "mediaNotifyMessage"))
                    object.mediaNotifyMessage = $root.proto.MediaNotifyMessage.toObject(message.mediaNotifyMessage, options, q + 1);
                if (message.cloudApiThreadControlNotification != null && Object.hasOwnProperty.call(message, "cloudApiThreadControlNotification"))
                    object.cloudApiThreadControlNotification = $root.proto.Message.CloudAPIThreadControlNotification.toObject(message.cloudApiThreadControlNotification, options, q + 1);
                if (message.lidMigrationMappingSyncMessage != null && Object.hasOwnProperty.call(message, "lidMigrationMappingSyncMessage"))
                    object.lidMigrationMappingSyncMessage = $root.proto.LIDMigrationMappingSyncMessage.toObject(message.lidMigrationMappingSyncMessage, options, q + 1);
                if (message.limitSharing != null && Object.hasOwnProperty.call(message, "limitSharing"))
                    object.limitSharing = $root.proto.LimitSharing.toObject(message.limitSharing, options, q + 1);
                if (message.aiPsiMetadata != null && Object.hasOwnProperty.call(message, "aiPsiMetadata"))
                    object.aiPsiMetadata = options.bytes === String ? $util.base64.encode(message.aiPsiMetadata, 0, message.aiPsiMetadata.length) : options.bytes === Array ? Array.prototype.slice.call(message.aiPsiMetadata) : message.aiPsiMetadata;
                if (message.aiQueryFanout != null && Object.hasOwnProperty.call(message, "aiQueryFanout"))
                    object.aiQueryFanout = $root.proto.AIQueryFanout.toObject(message.aiQueryFanout, options, q + 1);
                if (message.memberLabel != null && Object.hasOwnProperty.call(message, "memberLabel"))
                    object.memberLabel = $root.proto.MemberLabel.toObject(message.memberLabel, options, q + 1);
                if (message.aiMediaCollectionMessage != null && Object.hasOwnProperty.call(message, "aiMediaCollectionMessage"))
                    object.aiMediaCollectionMessage = $root.proto.AIMediaCollectionMessage.toObject(message.aiMediaCollectionMessage, options, q + 1);
                if (message.afterReadDuration != null && Object.hasOwnProperty.call(message, "afterReadDuration"))
                    object.afterReadDuration = message.afterReadDuration;
                if (message.chatThemeSetting != null && Object.hasOwnProperty.call(message, "chatThemeSetting"))
                    object.chatThemeSetting = $root.proto.Message.ChatThemeSetting.toObject(message.chatThemeSetting, options, q + 1);
                if (message.aiMetadataOperation != null && Object.hasOwnProperty.call(message, "aiMetadataOperation"))
                    object.aiMetadataOperation = $root.proto.AIMetadataOperation.toObject(message.aiMetadataOperation, options, q + 1);
                return object;
            };

            /**
             * Converts this ProtocolMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.ProtocolMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ProtocolMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ProtocolMessage
             * @function getTypeUrl
             * @memberof proto.Message.ProtocolMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ProtocolMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.ProtocolMessage";
            };

            /**
             * Type enum.
             * @name proto.Message.ProtocolMessage.Type
             * @enum {number}
             * @property {number} REVOKE=0 REVOKE value
             * @property {number} EPHEMERAL_SETTING=3 EPHEMERAL_SETTING value
             * @property {number} EPHEMERAL_SYNC_RESPONSE=4 EPHEMERAL_SYNC_RESPONSE value
             * @property {number} HISTORY_SYNC_NOTIFICATION=5 HISTORY_SYNC_NOTIFICATION value
             * @property {number} APP_STATE_SYNC_KEY_SHARE=6 APP_STATE_SYNC_KEY_SHARE value
             * @property {number} APP_STATE_SYNC_KEY_REQUEST=7 APP_STATE_SYNC_KEY_REQUEST value
             * @property {number} MSG_FANOUT_BACKFILL_REQUEST=8 MSG_FANOUT_BACKFILL_REQUEST value
             * @property {number} INITIAL_SECURITY_NOTIFICATION_SETTING_SYNC=9 INITIAL_SECURITY_NOTIFICATION_SETTING_SYNC value
             * @property {number} APP_STATE_FATAL_EXCEPTION_NOTIFICATION=10 APP_STATE_FATAL_EXCEPTION_NOTIFICATION value
             * @property {number} SHARE_PHONE_NUMBER=11 SHARE_PHONE_NUMBER value
             * @property {number} MESSAGE_EDIT=14 MESSAGE_EDIT value
             * @property {number} PEER_DATA_OPERATION_REQUEST_MESSAGE=16 PEER_DATA_OPERATION_REQUEST_MESSAGE value
             * @property {number} PEER_DATA_OPERATION_REQUEST_RESPONSE_MESSAGE=17 PEER_DATA_OPERATION_REQUEST_RESPONSE_MESSAGE value
             * @property {number} REQUEST_WELCOME_MESSAGE=18 REQUEST_WELCOME_MESSAGE value
             * @property {number} BOT_FEEDBACK_MESSAGE=19 BOT_FEEDBACK_MESSAGE value
             * @property {number} MEDIA_NOTIFY_MESSAGE=20 MEDIA_NOTIFY_MESSAGE value
             * @property {number} CLOUD_API_THREAD_CONTROL_NOTIFICATION=21 CLOUD_API_THREAD_CONTROL_NOTIFICATION value
             * @property {number} LID_MIGRATION_MAPPING_SYNC=22 LID_MIGRATION_MAPPING_SYNC value
             * @property {number} REMINDER_MESSAGE=23 REMINDER_MESSAGE value
             * @property {number} BOT_MEMU_ONBOARDING_MESSAGE=24 BOT_MEMU_ONBOARDING_MESSAGE value
             * @property {number} STATUS_MENTION_MESSAGE=25 STATUS_MENTION_MESSAGE value
             * @property {number} STOP_GENERATION_MESSAGE=26 STOP_GENERATION_MESSAGE value
             * @property {number} LIMIT_SHARING=27 LIMIT_SHARING value
             * @property {number} AI_PSI_METADATA=28 AI_PSI_METADATA value
             * @property {number} AI_QUERY_FANOUT=29 AI_QUERY_FANOUT value
             * @property {number} GROUP_MEMBER_LABEL_CHANGE=30 GROUP_MEMBER_LABEL_CHANGE value
             * @property {number} AI_MEDIA_COLLECTION_MESSAGE=31 AI_MEDIA_COLLECTION_MESSAGE value
             * @property {number} MESSAGE_UNSCHEDULE=32 MESSAGE_UNSCHEDULE value
             * @property {number} CHAT_THEME_SETTING=34 CHAT_THEME_SETTING value
             * @property {number} AI_METADATA_OPERATION=35 AI_METADATA_OPERATION value
             */
            ProtocolMessage.Type = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "REVOKE"] = 0;
                values[valuesById[3] = "EPHEMERAL_SETTING"] = 3;
                values[valuesById[4] = "EPHEMERAL_SYNC_RESPONSE"] = 4;
                values[valuesById[5] = "HISTORY_SYNC_NOTIFICATION"] = 5;
                values[valuesById[6] = "APP_STATE_SYNC_KEY_SHARE"] = 6;
                values[valuesById[7] = "APP_STATE_SYNC_KEY_REQUEST"] = 7;
                values[valuesById[8] = "MSG_FANOUT_BACKFILL_REQUEST"] = 8;
                values[valuesById[9] = "INITIAL_SECURITY_NOTIFICATION_SETTING_SYNC"] = 9;
                values[valuesById[10] = "APP_STATE_FATAL_EXCEPTION_NOTIFICATION"] = 10;
                values[valuesById[11] = "SHARE_PHONE_NUMBER"] = 11;
                values[valuesById[14] = "MESSAGE_EDIT"] = 14;
                values[valuesById[16] = "PEER_DATA_OPERATION_REQUEST_MESSAGE"] = 16;
                values[valuesById[17] = "PEER_DATA_OPERATION_REQUEST_RESPONSE_MESSAGE"] = 17;
                values[valuesById[18] = "REQUEST_WELCOME_MESSAGE"] = 18;
                values[valuesById[19] = "BOT_FEEDBACK_MESSAGE"] = 19;
                values[valuesById[20] = "MEDIA_NOTIFY_MESSAGE"] = 20;
                values[valuesById[21] = "CLOUD_API_THREAD_CONTROL_NOTIFICATION"] = 21;
                values[valuesById[22] = "LID_MIGRATION_MAPPING_SYNC"] = 22;
                values[valuesById[23] = "REMINDER_MESSAGE"] = 23;
                values[valuesById[24] = "BOT_MEMU_ONBOARDING_MESSAGE"] = 24;
                values[valuesById[25] = "STATUS_MENTION_MESSAGE"] = 25;
                values[valuesById[26] = "STOP_GENERATION_MESSAGE"] = 26;
                values[valuesById[27] = "LIMIT_SHARING"] = 27;
                values[valuesById[28] = "AI_PSI_METADATA"] = 28;
                values[valuesById[29] = "AI_QUERY_FANOUT"] = 29;
                values[valuesById[30] = "GROUP_MEMBER_LABEL_CHANGE"] = 30;
                values[valuesById[31] = "AI_MEDIA_COLLECTION_MESSAGE"] = 31;
                values[valuesById[32] = "MESSAGE_UNSCHEDULE"] = 32;
                values[valuesById[34] = "CHAT_THEME_SETTING"] = 34;
                values[valuesById[35] = "AI_METADATA_OPERATION"] = 35;
                return values;
            })();

            return ProtocolMessage;
        })();

        Message.QuestionResponseMessage = (function() {

            /**
             * Properties of a QuestionResponseMessage.
             * @memberof proto.Message
             * @interface IQuestionResponseMessage
             * @property {proto.IMessageKey|null} [key] QuestionResponseMessage key
             * @property {string|null} [text] QuestionResponseMessage text
             */

            /**
             * Constructs a new QuestionResponseMessage.
             * @memberof proto.Message
             * @classdesc Represents a QuestionResponseMessage.
             * @implements IQuestionResponseMessage
             * @constructor
             * @param {proto.Message.IQuestionResponseMessage=} [properties] Properties to set
             */
            function QuestionResponseMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * QuestionResponseMessage key.
             * @member {proto.IMessageKey|null|undefined} key
             * @memberof proto.Message.QuestionResponseMessage
             * @instance
             */
            QuestionResponseMessage.prototype.key = null;

            /**
             * QuestionResponseMessage text.
             * @member {string|null|undefined} text
             * @memberof proto.Message.QuestionResponseMessage
             * @instance
             */
            QuestionResponseMessage.prototype.text = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(QuestionResponseMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(QuestionResponseMessage.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new QuestionResponseMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.QuestionResponseMessage
             * @static
             * @param {proto.Message.IQuestionResponseMessage=} [properties] Properties to set
             * @returns {proto.Message.QuestionResponseMessage} QuestionResponseMessage instance
             */
            QuestionResponseMessage.create = function create(properties) {
                return new QuestionResponseMessage(properties);
            };

            /**
             * Encodes the specified QuestionResponseMessage message. Does not implicitly {@link proto.Message.QuestionResponseMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.QuestionResponseMessage
             * @static
             * @param {proto.Message.IQuestionResponseMessage} message QuestionResponseMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            QuestionResponseMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    $root.proto.MessageKey.encode(message.key, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.text);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified QuestionResponseMessage message, length delimited. Does not implicitly {@link proto.Message.QuestionResponseMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.QuestionResponseMessage
             * @static
             * @param {proto.Message.IQuestionResponseMessage} message QuestionResponseMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            QuestionResponseMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a QuestionResponseMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.QuestionResponseMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.QuestionResponseMessage} QuestionResponseMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            QuestionResponseMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.key = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.key);
                            message._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.text = reader.stringVerify();
                            message._text = "text";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a QuestionResponseMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.QuestionResponseMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.QuestionResponseMessage} QuestionResponseMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            QuestionResponseMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a QuestionResponseMessage message.
             * @function verify
             * @memberof proto.Message.QuestionResponseMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            QuestionResponseMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                    properties._key = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.key, q + 1);
                        if (error)
                            return "key." + error;
                    }
                }
                if (message.text != null && Object.hasOwnProperty.call(message, "text")) {
                    properties._text = 1;
                    if (!$util.isString(message.text))
                        return "text: string expected";
                }
                return null;
            };

            /**
             * Creates a QuestionResponseMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.QuestionResponseMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.QuestionResponseMessage} QuestionResponseMessage
             */
            QuestionResponseMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.QuestionResponseMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.key != null) {
                    if (!$util.isObject(object.key))
                        throw TypeError(".proto.Message.QuestionResponseMessage.key: object expected");
                    message.key = $root.proto.MessageKey.fromObject(object.key, q + 1);
                }
                if (object.text != null)
                    message.text = String(object.text);
                return message;
            };

            /**
             * Creates a plain object from a QuestionResponseMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.QuestionResponseMessage
             * @static
             * @param {proto.Message.QuestionResponseMessage} message QuestionResponseMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            QuestionResponseMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    object.key = $root.proto.MessageKey.toObject(message.key, options, q + 1);
                if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                    object.text = message.text;
                return object;
            };

            /**
             * Converts this QuestionResponseMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.QuestionResponseMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            QuestionResponseMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for QuestionResponseMessage
             * @function getTypeUrl
             * @memberof proto.Message.QuestionResponseMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            QuestionResponseMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.QuestionResponseMessage";
            };

            return QuestionResponseMessage;
        })();

        Message.ReactionMessage = (function() {

            /**
             * Properties of a ReactionMessage.
             * @memberof proto.Message
             * @interface IReactionMessage
             * @property {proto.IMessageKey|null} [key] ReactionMessage key
             * @property {string|null} [text] ReactionMessage text
             * @property {string|null} [groupingKey] ReactionMessage groupingKey
             * @property {number|Long|null} [senderTimestampMs] ReactionMessage senderTimestampMs
             */

            /**
             * Constructs a new ReactionMessage.
             * @memberof proto.Message
             * @classdesc Represents a ReactionMessage.
             * @implements IReactionMessage
             * @constructor
             * @param {proto.Message.IReactionMessage=} [properties] Properties to set
             */
            function ReactionMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ReactionMessage key.
             * @member {proto.IMessageKey|null|undefined} key
             * @memberof proto.Message.ReactionMessage
             * @instance
             */
            ReactionMessage.prototype.key = null;

            /**
             * ReactionMessage text.
             * @member {string|null|undefined} text
             * @memberof proto.Message.ReactionMessage
             * @instance
             */
            ReactionMessage.prototype.text = null;

            /**
             * ReactionMessage groupingKey.
             * @member {string|null|undefined} groupingKey
             * @memberof proto.Message.ReactionMessage
             * @instance
             */
            ReactionMessage.prototype.groupingKey = null;

            /**
             * ReactionMessage senderTimestampMs.
             * @member {number|Long|null|undefined} senderTimestampMs
             * @memberof proto.Message.ReactionMessage
             * @instance
             */
            ReactionMessage.prototype.senderTimestampMs = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ReactionMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ReactionMessage.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ReactionMessage.prototype, "_groupingKey", {
                get: $util.oneOfGetter($oneOfFields = ["groupingKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ReactionMessage.prototype, "_senderTimestampMs", {
                get: $util.oneOfGetter($oneOfFields = ["senderTimestampMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ReactionMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.ReactionMessage
             * @static
             * @param {proto.Message.IReactionMessage=} [properties] Properties to set
             * @returns {proto.Message.ReactionMessage} ReactionMessage instance
             */
            ReactionMessage.create = function create(properties) {
                return new ReactionMessage(properties);
            };

            /**
             * Encodes the specified ReactionMessage message. Does not implicitly {@link proto.Message.ReactionMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.ReactionMessage
             * @static
             * @param {proto.Message.IReactionMessage} message ReactionMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ReactionMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    $root.proto.MessageKey.encode(message.key, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.text);
                if (message.groupingKey != null && Object.hasOwnProperty.call(message, "groupingKey"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.groupingKey);
                if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int64(message.senderTimestampMs);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ReactionMessage message, length delimited. Does not implicitly {@link proto.Message.ReactionMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.ReactionMessage
             * @static
             * @param {proto.Message.IReactionMessage} message ReactionMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ReactionMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ReactionMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.ReactionMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.ReactionMessage} ReactionMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ReactionMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.key = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.key);
                            message._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.text = reader.stringVerify();
                            message._text = "text";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.groupingKey = reader.stringVerify();
                            message._groupingKey = "groupingKey";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.senderTimestampMs = reader.int64();
                            message._senderTimestampMs = "senderTimestampMs";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ReactionMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.ReactionMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.ReactionMessage} ReactionMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ReactionMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ReactionMessage message.
             * @function verify
             * @memberof proto.Message.ReactionMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ReactionMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                    properties._key = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.key, q + 1);
                        if (error)
                            return "key." + error;
                    }
                }
                if (message.text != null && Object.hasOwnProperty.call(message, "text")) {
                    properties._text = 1;
                    if (!$util.isString(message.text))
                        return "text: string expected";
                }
                if (message.groupingKey != null && Object.hasOwnProperty.call(message, "groupingKey")) {
                    properties._groupingKey = 1;
                    if (!$util.isString(message.groupingKey))
                        return "groupingKey: string expected";
                }
                if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs")) {
                    properties._senderTimestampMs = 1;
                    if (!$util.isInteger(message.senderTimestampMs) && !(message.senderTimestampMs && $util.isInteger(message.senderTimestampMs.low) && $util.isInteger(message.senderTimestampMs.high)))
                        return "senderTimestampMs: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a ReactionMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.ReactionMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.ReactionMessage} ReactionMessage
             */
            ReactionMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.ReactionMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.key != null) {
                    if (!$util.isObject(object.key))
                        throw TypeError(".proto.Message.ReactionMessage.key: object expected");
                    message.key = $root.proto.MessageKey.fromObject(object.key, q + 1);
                }
                if (object.text != null)
                    message.text = String(object.text);
                if (object.groupingKey != null)
                    message.groupingKey = String(object.groupingKey);
                if (object.senderTimestampMs != null)
                    if ($util.Long)
                        message.senderTimestampMs = $util.Long.fromValue(object.senderTimestampMs, false);
                    else if (typeof object.senderTimestampMs === "string")
                        message.senderTimestampMs = parseInt(object.senderTimestampMs, 10);
                    else if (typeof object.senderTimestampMs === "number")
                        message.senderTimestampMs = object.senderTimestampMs;
                    else if (typeof object.senderTimestampMs === "object")
                        message.senderTimestampMs = new $util.LongBits(object.senderTimestampMs.low >>> 0, object.senderTimestampMs.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a ReactionMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.ReactionMessage
             * @static
             * @param {proto.Message.ReactionMessage} message ReactionMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ReactionMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    object.key = $root.proto.MessageKey.toObject(message.key, options, q + 1);
                if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                    object.text = message.text;
                if (message.groupingKey != null && Object.hasOwnProperty.call(message, "groupingKey"))
                    object.groupingKey = message.groupingKey;
                if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.senderTimestampMs = typeof message.senderTimestampMs === "number" ? BigInt(message.senderTimestampMs) : $util.Long.fromBits(message.senderTimestampMs.low >>> 0, message.senderTimestampMs.high >>> 0, false).toBigInt();
                    else if (typeof message.senderTimestampMs === "number")
                        object.senderTimestampMs = options.longs === String ? String(message.senderTimestampMs) : message.senderTimestampMs;
                    else
                        object.senderTimestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.senderTimestampMs) : options.longs === Number ? new $util.LongBits(message.senderTimestampMs.low >>> 0, message.senderTimestampMs.high >>> 0).toNumber() : message.senderTimestampMs;
                return object;
            };

            /**
             * Converts this ReactionMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.ReactionMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ReactionMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ReactionMessage
             * @function getTypeUrl
             * @memberof proto.Message.ReactionMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ReactionMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.ReactionMessage";
            };

            return ReactionMessage;
        })();

        Message.RequestPaymentMessage = (function() {

            /**
             * Properties of a RequestPaymentMessage.
             * @memberof proto.Message
             * @interface IRequestPaymentMessage
             * @property {proto.IMessage|null} [noteMessage] RequestPaymentMessage noteMessage
             * @property {string|null} [currencyCodeIso4217] RequestPaymentMessage currencyCodeIso4217
             * @property {number|Long|null} [amount1000] RequestPaymentMessage amount1000
             * @property {string|null} [requestFrom] RequestPaymentMessage requestFrom
             * @property {number|Long|null} [expiryTimestamp] RequestPaymentMessage expiryTimestamp
             * @property {proto.IMoney|null} [amount] RequestPaymentMessage amount
             * @property {proto.IPaymentBackground|null} [background] RequestPaymentMessage background
             */

            /**
             * Constructs a new RequestPaymentMessage.
             * @memberof proto.Message
             * @classdesc Represents a RequestPaymentMessage.
             * @implements IRequestPaymentMessage
             * @constructor
             * @param {proto.Message.IRequestPaymentMessage=} [properties] Properties to set
             */
            function RequestPaymentMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * RequestPaymentMessage noteMessage.
             * @member {proto.IMessage|null|undefined} noteMessage
             * @memberof proto.Message.RequestPaymentMessage
             * @instance
             */
            RequestPaymentMessage.prototype.noteMessage = null;

            /**
             * RequestPaymentMessage currencyCodeIso4217.
             * @member {string|null|undefined} currencyCodeIso4217
             * @memberof proto.Message.RequestPaymentMessage
             * @instance
             */
            RequestPaymentMessage.prototype.currencyCodeIso4217 = null;

            /**
             * RequestPaymentMessage amount1000.
             * @member {number|Long|null|undefined} amount1000
             * @memberof proto.Message.RequestPaymentMessage
             * @instance
             */
            RequestPaymentMessage.prototype.amount1000 = null;

            /**
             * RequestPaymentMessage requestFrom.
             * @member {string|null|undefined} requestFrom
             * @memberof proto.Message.RequestPaymentMessage
             * @instance
             */
            RequestPaymentMessage.prototype.requestFrom = null;

            /**
             * RequestPaymentMessage expiryTimestamp.
             * @member {number|Long|null|undefined} expiryTimestamp
             * @memberof proto.Message.RequestPaymentMessage
             * @instance
             */
            RequestPaymentMessage.prototype.expiryTimestamp = null;

            /**
             * RequestPaymentMessage amount.
             * @member {proto.IMoney|null|undefined} amount
             * @memberof proto.Message.RequestPaymentMessage
             * @instance
             */
            RequestPaymentMessage.prototype.amount = null;

            /**
             * RequestPaymentMessage background.
             * @member {proto.IPaymentBackground|null|undefined} background
             * @memberof proto.Message.RequestPaymentMessage
             * @instance
             */
            RequestPaymentMessage.prototype.background = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(RequestPaymentMessage.prototype, "_noteMessage", {
                get: $util.oneOfGetter($oneOfFields = ["noteMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(RequestPaymentMessage.prototype, "_currencyCodeIso4217", {
                get: $util.oneOfGetter($oneOfFields = ["currencyCodeIso4217"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(RequestPaymentMessage.prototype, "_amount1000", {
                get: $util.oneOfGetter($oneOfFields = ["amount1000"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(RequestPaymentMessage.prototype, "_requestFrom", {
                get: $util.oneOfGetter($oneOfFields = ["requestFrom"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(RequestPaymentMessage.prototype, "_expiryTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["expiryTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(RequestPaymentMessage.prototype, "_amount", {
                get: $util.oneOfGetter($oneOfFields = ["amount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(RequestPaymentMessage.prototype, "_background", {
                get: $util.oneOfGetter($oneOfFields = ["background"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new RequestPaymentMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.RequestPaymentMessage
             * @static
             * @param {proto.Message.IRequestPaymentMessage=} [properties] Properties to set
             * @returns {proto.Message.RequestPaymentMessage} RequestPaymentMessage instance
             */
            RequestPaymentMessage.create = function create(properties) {
                return new RequestPaymentMessage(properties);
            };

            /**
             * Encodes the specified RequestPaymentMessage message. Does not implicitly {@link proto.Message.RequestPaymentMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.RequestPaymentMessage
             * @static
             * @param {proto.Message.IRequestPaymentMessage} message RequestPaymentMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RequestPaymentMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.currencyCodeIso4217 != null && Object.hasOwnProperty.call(message, "currencyCodeIso4217"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.currencyCodeIso4217);
                if (message.amount1000 != null && Object.hasOwnProperty.call(message, "amount1000"))
                    writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.amount1000);
                if (message.requestFrom != null && Object.hasOwnProperty.call(message, "requestFrom"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.requestFrom);
                if (message.noteMessage != null && Object.hasOwnProperty.call(message, "noteMessage"))
                    $root.proto.Message.encode(message.noteMessage, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.expiryTimestamp != null && Object.hasOwnProperty.call(message, "expiryTimestamp"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.expiryTimestamp);
                if (message.amount != null && Object.hasOwnProperty.call(message, "amount"))
                    $root.proto.Money.encode(message.amount, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
                if (message.background != null && Object.hasOwnProperty.call(message, "background"))
                    $root.proto.PaymentBackground.encode(message.background, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified RequestPaymentMessage message, length delimited. Does not implicitly {@link proto.Message.RequestPaymentMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.RequestPaymentMessage
             * @static
             * @param {proto.Message.IRequestPaymentMessage} message RequestPaymentMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RequestPaymentMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a RequestPaymentMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.RequestPaymentMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.RequestPaymentMessage} RequestPaymentMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RequestPaymentMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 4: {
                            if (u !== 2)
                                break;
                            message.noteMessage = $root.proto.Message.decode(reader, reader.uint32(), undefined, q + 1, message.noteMessage);
                            message._noteMessage = "noteMessage";
                            continue;
                        }
                    case 1: {
                            if (u !== 2)
                                break;
                            message.currencyCodeIso4217 = reader.stringVerify();
                            message._currencyCodeIso4217 = "currencyCodeIso4217";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.amount1000 = reader.uint64();
                            message._amount1000 = "amount1000";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.requestFrom = reader.stringVerify();
                            message._requestFrom = "requestFrom";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.expiryTimestamp = reader.int64();
                            message._expiryTimestamp = "expiryTimestamp";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.amount = $root.proto.Money.decode(reader, reader.uint32(), undefined, q + 1, message.amount);
                            message._amount = "amount";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.background = $root.proto.PaymentBackground.decode(reader, reader.uint32(), undefined, q + 1, message.background);
                            message._background = "background";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a RequestPaymentMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.RequestPaymentMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.RequestPaymentMessage} RequestPaymentMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RequestPaymentMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a RequestPaymentMessage message.
             * @function verify
             * @memberof proto.Message.RequestPaymentMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RequestPaymentMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.noteMessage != null && Object.hasOwnProperty.call(message, "noteMessage")) {
                    properties._noteMessage = 1;
                    {
                        var error = $root.proto.Message.verify(message.noteMessage, q + 1);
                        if (error)
                            return "noteMessage." + error;
                    }
                }
                if (message.currencyCodeIso4217 != null && Object.hasOwnProperty.call(message, "currencyCodeIso4217")) {
                    properties._currencyCodeIso4217 = 1;
                    if (!$util.isString(message.currencyCodeIso4217))
                        return "currencyCodeIso4217: string expected";
                }
                if (message.amount1000 != null && Object.hasOwnProperty.call(message, "amount1000")) {
                    properties._amount1000 = 1;
                    if (!$util.isInteger(message.amount1000) && !(message.amount1000 && $util.isInteger(message.amount1000.low) && $util.isInteger(message.amount1000.high)))
                        return "amount1000: integer|Long expected";
                }
                if (message.requestFrom != null && Object.hasOwnProperty.call(message, "requestFrom")) {
                    properties._requestFrom = 1;
                    if (!$util.isString(message.requestFrom))
                        return "requestFrom: string expected";
                }
                if (message.expiryTimestamp != null && Object.hasOwnProperty.call(message, "expiryTimestamp")) {
                    properties._expiryTimestamp = 1;
                    if (!$util.isInteger(message.expiryTimestamp) && !(message.expiryTimestamp && $util.isInteger(message.expiryTimestamp.low) && $util.isInteger(message.expiryTimestamp.high)))
                        return "expiryTimestamp: integer|Long expected";
                }
                if (message.amount != null && Object.hasOwnProperty.call(message, "amount")) {
                    properties._amount = 1;
                    {
                        var error = $root.proto.Money.verify(message.amount, q + 1);
                        if (error)
                            return "amount." + error;
                    }
                }
                if (message.background != null && Object.hasOwnProperty.call(message, "background")) {
                    properties._background = 1;
                    {
                        var error = $root.proto.PaymentBackground.verify(message.background, q + 1);
                        if (error)
                            return "background." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a RequestPaymentMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.RequestPaymentMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.RequestPaymentMessage} RequestPaymentMessage
             */
            RequestPaymentMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.RequestPaymentMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.noteMessage != null) {
                    if (!$util.isObject(object.noteMessage))
                        throw TypeError(".proto.Message.RequestPaymentMessage.noteMessage: object expected");
                    message.noteMessage = $root.proto.Message.fromObject(object.noteMessage, q + 1);
                }
                if (object.currencyCodeIso4217 != null)
                    message.currencyCodeIso4217 = String(object.currencyCodeIso4217);
                if (object.amount1000 != null)
                    if ($util.Long)
                        message.amount1000 = $util.Long.fromValue(object.amount1000, true);
                    else if (typeof object.amount1000 === "string")
                        message.amount1000 = parseInt(object.amount1000, 10);
                    else if (typeof object.amount1000 === "number")
                        message.amount1000 = object.amount1000;
                    else if (typeof object.amount1000 === "object")
                        message.amount1000 = new $util.LongBits(object.amount1000.low >>> 0, object.amount1000.high >>> 0).toNumber(true);
                if (object.requestFrom != null)
                    message.requestFrom = String(object.requestFrom);
                if (object.expiryTimestamp != null)
                    if ($util.Long)
                        message.expiryTimestamp = $util.Long.fromValue(object.expiryTimestamp, false);
                    else if (typeof object.expiryTimestamp === "string")
                        message.expiryTimestamp = parseInt(object.expiryTimestamp, 10);
                    else if (typeof object.expiryTimestamp === "number")
                        message.expiryTimestamp = object.expiryTimestamp;
                    else if (typeof object.expiryTimestamp === "object")
                        message.expiryTimestamp = new $util.LongBits(object.expiryTimestamp.low >>> 0, object.expiryTimestamp.high >>> 0).toNumber();
                if (object.amount != null) {
                    if (!$util.isObject(object.amount))
                        throw TypeError(".proto.Message.RequestPaymentMessage.amount: object expected");
                    message.amount = $root.proto.Money.fromObject(object.amount, q + 1);
                }
                if (object.background != null) {
                    if (!$util.isObject(object.background))
                        throw TypeError(".proto.Message.RequestPaymentMessage.background: object expected");
                    message.background = $root.proto.PaymentBackground.fromObject(object.background, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a RequestPaymentMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.RequestPaymentMessage
             * @static
             * @param {proto.Message.RequestPaymentMessage} message RequestPaymentMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RequestPaymentMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.currencyCodeIso4217 != null && Object.hasOwnProperty.call(message, "currencyCodeIso4217"))
                    object.currencyCodeIso4217 = message.currencyCodeIso4217;
                if (message.amount1000 != null && Object.hasOwnProperty.call(message, "amount1000"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.amount1000 = typeof message.amount1000 === "number" ? BigInt(message.amount1000) : $util.Long.fromBits(message.amount1000.low >>> 0, message.amount1000.high >>> 0, true).toBigInt();
                    else if (typeof message.amount1000 === "number")
                        object.amount1000 = options.longs === String ? String(message.amount1000) : message.amount1000;
                    else
                        object.amount1000 = options.longs === String ? $util.Long.prototype.toString.call(message.amount1000) : options.longs === Number ? new $util.LongBits(message.amount1000.low >>> 0, message.amount1000.high >>> 0).toNumber(true) : message.amount1000;
                if (message.requestFrom != null && Object.hasOwnProperty.call(message, "requestFrom"))
                    object.requestFrom = message.requestFrom;
                if (message.noteMessage != null && Object.hasOwnProperty.call(message, "noteMessage"))
                    object.noteMessage = $root.proto.Message.toObject(message.noteMessage, options, q + 1);
                if (message.expiryTimestamp != null && Object.hasOwnProperty.call(message, "expiryTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.expiryTimestamp = typeof message.expiryTimestamp === "number" ? BigInt(message.expiryTimestamp) : $util.Long.fromBits(message.expiryTimestamp.low >>> 0, message.expiryTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.expiryTimestamp === "number")
                        object.expiryTimestamp = options.longs === String ? String(message.expiryTimestamp) : message.expiryTimestamp;
                    else
                        object.expiryTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.expiryTimestamp) : options.longs === Number ? new $util.LongBits(message.expiryTimestamp.low >>> 0, message.expiryTimestamp.high >>> 0).toNumber() : message.expiryTimestamp;
                if (message.amount != null && Object.hasOwnProperty.call(message, "amount"))
                    object.amount = $root.proto.Money.toObject(message.amount, options, q + 1);
                if (message.background != null && Object.hasOwnProperty.call(message, "background"))
                    object.background = $root.proto.PaymentBackground.toObject(message.background, options, q + 1);
                return object;
            };

            /**
             * Converts this RequestPaymentMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.RequestPaymentMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RequestPaymentMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for RequestPaymentMessage
             * @function getTypeUrl
             * @memberof proto.Message.RequestPaymentMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            RequestPaymentMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.RequestPaymentMessage";
            };

            return RequestPaymentMessage;
        })();

        Message.RequestPhoneNumberMessage = (function() {

            /**
             * Properties of a RequestPhoneNumberMessage.
             * @memberof proto.Message
             * @interface IRequestPhoneNumberMessage
             * @property {proto.IContextInfo|null} [contextInfo] RequestPhoneNumberMessage contextInfo
             */

            /**
             * Constructs a new RequestPhoneNumberMessage.
             * @memberof proto.Message
             * @classdesc Represents a RequestPhoneNumberMessage.
             * @implements IRequestPhoneNumberMessage
             * @constructor
             * @param {proto.Message.IRequestPhoneNumberMessage=} [properties] Properties to set
             */
            function RequestPhoneNumberMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * RequestPhoneNumberMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.RequestPhoneNumberMessage
             * @instance
             */
            RequestPhoneNumberMessage.prototype.contextInfo = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(RequestPhoneNumberMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new RequestPhoneNumberMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.RequestPhoneNumberMessage
             * @static
             * @param {proto.Message.IRequestPhoneNumberMessage=} [properties] Properties to set
             * @returns {proto.Message.RequestPhoneNumberMessage} RequestPhoneNumberMessage instance
             */
            RequestPhoneNumberMessage.create = function create(properties) {
                return new RequestPhoneNumberMessage(properties);
            };

            /**
             * Encodes the specified RequestPhoneNumberMessage message. Does not implicitly {@link proto.Message.RequestPhoneNumberMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.RequestPhoneNumberMessage
             * @static
             * @param {proto.Message.IRequestPhoneNumberMessage} message RequestPhoneNumberMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RequestPhoneNumberMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified RequestPhoneNumberMessage message, length delimited. Does not implicitly {@link proto.Message.RequestPhoneNumberMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.RequestPhoneNumberMessage
             * @static
             * @param {proto.Message.IRequestPhoneNumberMessage} message RequestPhoneNumberMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RequestPhoneNumberMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a RequestPhoneNumberMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.RequestPhoneNumberMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.RequestPhoneNumberMessage} RequestPhoneNumberMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RequestPhoneNumberMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a RequestPhoneNumberMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.RequestPhoneNumberMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.RequestPhoneNumberMessage} RequestPhoneNumberMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RequestPhoneNumberMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a RequestPhoneNumberMessage message.
             * @function verify
             * @memberof proto.Message.RequestPhoneNumberMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RequestPhoneNumberMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a RequestPhoneNumberMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.RequestPhoneNumberMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.RequestPhoneNumberMessage} RequestPhoneNumberMessage
             */
            RequestPhoneNumberMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.RequestPhoneNumberMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.RequestPhoneNumberMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a RequestPhoneNumberMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.RequestPhoneNumberMessage
             * @static
             * @param {proto.Message.RequestPhoneNumberMessage} message RequestPhoneNumberMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RequestPhoneNumberMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                return object;
            };

            /**
             * Converts this RequestPhoneNumberMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.RequestPhoneNumberMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RequestPhoneNumberMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for RequestPhoneNumberMessage
             * @function getTypeUrl
             * @memberof proto.Message.RequestPhoneNumberMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            RequestPhoneNumberMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.RequestPhoneNumberMessage";
            };

            return RequestPhoneNumberMessage;
        })();

        Message.RequestWelcomeMessageMetadata = (function() {

            /**
             * Properties of a RequestWelcomeMessageMetadata.
             * @memberof proto.Message
             * @interface IRequestWelcomeMessageMetadata
             * @property {proto.Message.RequestWelcomeMessageMetadata.LocalChatState|null} [localChatState] RequestWelcomeMessageMetadata localChatState
             * @property {proto.Message.RequestWelcomeMessageMetadata.WelcomeTrigger|null} [welcomeTrigger] RequestWelcomeMessageMetadata welcomeTrigger
             * @property {proto.IBotAgentMetadata|null} [botAgentMetadata] RequestWelcomeMessageMetadata botAgentMetadata
             */

            /**
             * Constructs a new RequestWelcomeMessageMetadata.
             * @memberof proto.Message
             * @classdesc Represents a RequestWelcomeMessageMetadata.
             * @implements IRequestWelcomeMessageMetadata
             * @constructor
             * @param {proto.Message.IRequestWelcomeMessageMetadata=} [properties] Properties to set
             */
            function RequestWelcomeMessageMetadata(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * RequestWelcomeMessageMetadata localChatState.
             * @member {proto.Message.RequestWelcomeMessageMetadata.LocalChatState|null|undefined} localChatState
             * @memberof proto.Message.RequestWelcomeMessageMetadata
             * @instance
             */
            RequestWelcomeMessageMetadata.prototype.localChatState = null;

            /**
             * RequestWelcomeMessageMetadata welcomeTrigger.
             * @member {proto.Message.RequestWelcomeMessageMetadata.WelcomeTrigger|null|undefined} welcomeTrigger
             * @memberof proto.Message.RequestWelcomeMessageMetadata
             * @instance
             */
            RequestWelcomeMessageMetadata.prototype.welcomeTrigger = null;

            /**
             * RequestWelcomeMessageMetadata botAgentMetadata.
             * @member {proto.IBotAgentMetadata|null|undefined} botAgentMetadata
             * @memberof proto.Message.RequestWelcomeMessageMetadata
             * @instance
             */
            RequestWelcomeMessageMetadata.prototype.botAgentMetadata = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(RequestWelcomeMessageMetadata.prototype, "_localChatState", {
                get: $util.oneOfGetter($oneOfFields = ["localChatState"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(RequestWelcomeMessageMetadata.prototype, "_welcomeTrigger", {
                get: $util.oneOfGetter($oneOfFields = ["welcomeTrigger"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(RequestWelcomeMessageMetadata.prototype, "_botAgentMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["botAgentMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new RequestWelcomeMessageMetadata instance using the specified properties.
             * @function create
             * @memberof proto.Message.RequestWelcomeMessageMetadata
             * @static
             * @param {proto.Message.IRequestWelcomeMessageMetadata=} [properties] Properties to set
             * @returns {proto.Message.RequestWelcomeMessageMetadata} RequestWelcomeMessageMetadata instance
             */
            RequestWelcomeMessageMetadata.create = function create(properties) {
                return new RequestWelcomeMessageMetadata(properties);
            };

            /**
             * Encodes the specified RequestWelcomeMessageMetadata message. Does not implicitly {@link proto.Message.RequestWelcomeMessageMetadata.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.RequestWelcomeMessageMetadata
             * @static
             * @param {proto.Message.IRequestWelcomeMessageMetadata} message RequestWelcomeMessageMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RequestWelcomeMessageMetadata.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.localChatState != null && Object.hasOwnProperty.call(message, "localChatState"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.localChatState);
                if (message.welcomeTrigger != null && Object.hasOwnProperty.call(message, "welcomeTrigger"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.welcomeTrigger);
                if (message.botAgentMetadata != null && Object.hasOwnProperty.call(message, "botAgentMetadata"))
                    $root.proto.BotAgentMetadata.encode(message.botAgentMetadata, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified RequestWelcomeMessageMetadata message, length delimited. Does not implicitly {@link proto.Message.RequestWelcomeMessageMetadata.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.RequestWelcomeMessageMetadata
             * @static
             * @param {proto.Message.IRequestWelcomeMessageMetadata} message RequestWelcomeMessageMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RequestWelcomeMessageMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a RequestWelcomeMessageMetadata message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.RequestWelcomeMessageMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.RequestWelcomeMessageMetadata} RequestWelcomeMessageMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RequestWelcomeMessageMetadata.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.localChatState = reader.int32();
                            message._localChatState = "localChatState";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.welcomeTrigger = reader.int32();
                            message._welcomeTrigger = "welcomeTrigger";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.botAgentMetadata = $root.proto.BotAgentMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.botAgentMetadata);
                            message._botAgentMetadata = "botAgentMetadata";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a RequestWelcomeMessageMetadata message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.RequestWelcomeMessageMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.RequestWelcomeMessageMetadata} RequestWelcomeMessageMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RequestWelcomeMessageMetadata.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a RequestWelcomeMessageMetadata message.
             * @function verify
             * @memberof proto.Message.RequestWelcomeMessageMetadata
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RequestWelcomeMessageMetadata.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.localChatState != null && Object.hasOwnProperty.call(message, "localChatState")) {
                    properties._localChatState = 1;
                    if (typeof message.localChatState !== "number" || (message.localChatState | 0) !== message.localChatState)
                        return "localChatState: enum value expected";
                }
                if (message.welcomeTrigger != null && Object.hasOwnProperty.call(message, "welcomeTrigger")) {
                    properties._welcomeTrigger = 1;
                    if (typeof message.welcomeTrigger !== "number" || (message.welcomeTrigger | 0) !== message.welcomeTrigger)
                        return "welcomeTrigger: enum value expected";
                }
                if (message.botAgentMetadata != null && Object.hasOwnProperty.call(message, "botAgentMetadata")) {
                    properties._botAgentMetadata = 1;
                    {
                        var error = $root.proto.BotAgentMetadata.verify(message.botAgentMetadata, q + 1);
                        if (error)
                            return "botAgentMetadata." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a RequestWelcomeMessageMetadata message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.RequestWelcomeMessageMetadata
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.RequestWelcomeMessageMetadata} RequestWelcomeMessageMetadata
             */
            RequestWelcomeMessageMetadata.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.RequestWelcomeMessageMetadata: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.localChatState) {
                case "EMPTY":
                case 0:
                    message.localChatState = 0;
                    break;
                case "NON_EMPTY":
                case 1:
                    message.localChatState = 1;
                    break;
                default:
                    if (typeof object.localChatState === "number" && (object.localChatState | 0) === object.localChatState)
                        message.localChatState = object.localChatState;
                }
                switch (object.welcomeTrigger) {
                case "CHAT_OPEN":
                case 0:
                    message.welcomeTrigger = 0;
                    break;
                case "COMPANION_PAIRING":
                case 1:
                    message.welcomeTrigger = 1;
                    break;
                default:
                    if (typeof object.welcomeTrigger === "number" && (object.welcomeTrigger | 0) === object.welcomeTrigger)
                        message.welcomeTrigger = object.welcomeTrigger;
                }
                if (object.botAgentMetadata != null) {
                    if (!$util.isObject(object.botAgentMetadata))
                        throw TypeError(".proto.Message.RequestWelcomeMessageMetadata.botAgentMetadata: object expected");
                    message.botAgentMetadata = $root.proto.BotAgentMetadata.fromObject(object.botAgentMetadata, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a RequestWelcomeMessageMetadata message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.RequestWelcomeMessageMetadata
             * @static
             * @param {proto.Message.RequestWelcomeMessageMetadata} message RequestWelcomeMessageMetadata
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RequestWelcomeMessageMetadata.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.localChatState != null && Object.hasOwnProperty.call(message, "localChatState"))
                    object.localChatState = options.enums === String ? $root.proto.Message.RequestWelcomeMessageMetadata.LocalChatState[message.localChatState] === undefined ? message.localChatState : $root.proto.Message.RequestWelcomeMessageMetadata.LocalChatState[message.localChatState] : message.localChatState;
                if (message.welcomeTrigger != null && Object.hasOwnProperty.call(message, "welcomeTrigger"))
                    object.welcomeTrigger = options.enums === String ? $root.proto.Message.RequestWelcomeMessageMetadata.WelcomeTrigger[message.welcomeTrigger] === undefined ? message.welcomeTrigger : $root.proto.Message.RequestWelcomeMessageMetadata.WelcomeTrigger[message.welcomeTrigger] : message.welcomeTrigger;
                if (message.botAgentMetadata != null && Object.hasOwnProperty.call(message, "botAgentMetadata"))
                    object.botAgentMetadata = $root.proto.BotAgentMetadata.toObject(message.botAgentMetadata, options, q + 1);
                return object;
            };

            /**
             * Converts this RequestWelcomeMessageMetadata to JSON.
             * @function toJSON
             * @memberof proto.Message.RequestWelcomeMessageMetadata
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RequestWelcomeMessageMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for RequestWelcomeMessageMetadata
             * @function getTypeUrl
             * @memberof proto.Message.RequestWelcomeMessageMetadata
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            RequestWelcomeMessageMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.RequestWelcomeMessageMetadata";
            };

            /**
             * LocalChatState enum.
             * @name proto.Message.RequestWelcomeMessageMetadata.LocalChatState
             * @enum {number}
             * @property {number} EMPTY=0 EMPTY value
             * @property {number} NON_EMPTY=1 NON_EMPTY value
             */
            RequestWelcomeMessageMetadata.LocalChatState = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "EMPTY"] = 0;
                values[valuesById[1] = "NON_EMPTY"] = 1;
                return values;
            })();

            /**
             * WelcomeTrigger enum.
             * @name proto.Message.RequestWelcomeMessageMetadata.WelcomeTrigger
             * @enum {number}
             * @property {number} CHAT_OPEN=0 CHAT_OPEN value
             * @property {number} COMPANION_PAIRING=1 COMPANION_PAIRING value
             */
            RequestWelcomeMessageMetadata.WelcomeTrigger = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "CHAT_OPEN"] = 0;
                values[valuesById[1] = "COMPANION_PAIRING"] = 1;
                return values;
            })();

            return RequestWelcomeMessageMetadata;
        })();

        Message.RootSecretDistributeMessage = (function() {

            /**
             * Properties of a RootSecretDistributeMessage.
             * @memberof proto.Message
             * @interface IRootSecretDistributeMessage
             * @property {string|null} [chatJid] RootSecretDistributeMessage chatJid
             */

            /**
             * Constructs a new RootSecretDistributeMessage.
             * @memberof proto.Message
             * @classdesc Represents a RootSecretDistributeMessage.
             * @implements IRootSecretDistributeMessage
             * @constructor
             * @param {proto.Message.IRootSecretDistributeMessage=} [properties] Properties to set
             */
            function RootSecretDistributeMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * RootSecretDistributeMessage chatJid.
             * @member {string|null|undefined} chatJid
             * @memberof proto.Message.RootSecretDistributeMessage
             * @instance
             */
            RootSecretDistributeMessage.prototype.chatJid = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(RootSecretDistributeMessage.prototype, "_chatJid", {
                get: $util.oneOfGetter($oneOfFields = ["chatJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new RootSecretDistributeMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.RootSecretDistributeMessage
             * @static
             * @param {proto.Message.IRootSecretDistributeMessage=} [properties] Properties to set
             * @returns {proto.Message.RootSecretDistributeMessage} RootSecretDistributeMessage instance
             */
            RootSecretDistributeMessage.create = function create(properties) {
                return new RootSecretDistributeMessage(properties);
            };

            /**
             * Encodes the specified RootSecretDistributeMessage message. Does not implicitly {@link proto.Message.RootSecretDistributeMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.RootSecretDistributeMessage
             * @static
             * @param {proto.Message.IRootSecretDistributeMessage} message RootSecretDistributeMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RootSecretDistributeMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.chatJid != null && Object.hasOwnProperty.call(message, "chatJid"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.chatJid);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified RootSecretDistributeMessage message, length delimited. Does not implicitly {@link proto.Message.RootSecretDistributeMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.RootSecretDistributeMessage
             * @static
             * @param {proto.Message.IRootSecretDistributeMessage} message RootSecretDistributeMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RootSecretDistributeMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a RootSecretDistributeMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.RootSecretDistributeMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.RootSecretDistributeMessage} RootSecretDistributeMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RootSecretDistributeMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.chatJid = reader.stringVerify();
                            message._chatJid = "chatJid";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a RootSecretDistributeMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.RootSecretDistributeMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.RootSecretDistributeMessage} RootSecretDistributeMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RootSecretDistributeMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a RootSecretDistributeMessage message.
             * @function verify
             * @memberof proto.Message.RootSecretDistributeMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RootSecretDistributeMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.chatJid != null && Object.hasOwnProperty.call(message, "chatJid")) {
                    properties._chatJid = 1;
                    if (!$util.isString(message.chatJid))
                        return "chatJid: string expected";
                }
                return null;
            };

            /**
             * Creates a RootSecretDistributeMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.RootSecretDistributeMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.RootSecretDistributeMessage} RootSecretDistributeMessage
             */
            RootSecretDistributeMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.RootSecretDistributeMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.chatJid != null)
                    message.chatJid = String(object.chatJid);
                return message;
            };

            /**
             * Creates a plain object from a RootSecretDistributeMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.RootSecretDistributeMessage
             * @static
             * @param {proto.Message.RootSecretDistributeMessage} message RootSecretDistributeMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RootSecretDistributeMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.chatJid != null && Object.hasOwnProperty.call(message, "chatJid"))
                    object.chatJid = message.chatJid;
                return object;
            };

            /**
             * Converts this RootSecretDistributeMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.RootSecretDistributeMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RootSecretDistributeMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for RootSecretDistributeMessage
             * @function getTypeUrl
             * @memberof proto.Message.RootSecretDistributeMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            RootSecretDistributeMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.RootSecretDistributeMessage";
            };

            return RootSecretDistributeMessage;
        })();

        Message.ScheduledCallCreationMessage = (function() {

            /**
             * Properties of a ScheduledCallCreationMessage.
             * @memberof proto.Message
             * @interface IScheduledCallCreationMessage
             * @property {number|Long|null} [scheduledTimestampMs] ScheduledCallCreationMessage scheduledTimestampMs
             * @property {proto.Message.ScheduledCallCreationMessage.CallType|null} [callType] ScheduledCallCreationMessage callType
             * @property {string|null} [title] ScheduledCallCreationMessage title
             */

            /**
             * Constructs a new ScheduledCallCreationMessage.
             * @memberof proto.Message
             * @classdesc Represents a ScheduledCallCreationMessage.
             * @implements IScheduledCallCreationMessage
             * @constructor
             * @param {proto.Message.IScheduledCallCreationMessage=} [properties] Properties to set
             */
            function ScheduledCallCreationMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ScheduledCallCreationMessage scheduledTimestampMs.
             * @member {number|Long|null|undefined} scheduledTimestampMs
             * @memberof proto.Message.ScheduledCallCreationMessage
             * @instance
             */
            ScheduledCallCreationMessage.prototype.scheduledTimestampMs = null;

            /**
             * ScheduledCallCreationMessage callType.
             * @member {proto.Message.ScheduledCallCreationMessage.CallType|null|undefined} callType
             * @memberof proto.Message.ScheduledCallCreationMessage
             * @instance
             */
            ScheduledCallCreationMessage.prototype.callType = null;

            /**
             * ScheduledCallCreationMessage title.
             * @member {string|null|undefined} title
             * @memberof proto.Message.ScheduledCallCreationMessage
             * @instance
             */
            ScheduledCallCreationMessage.prototype.title = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ScheduledCallCreationMessage.prototype, "_scheduledTimestampMs", {
                get: $util.oneOfGetter($oneOfFields = ["scheduledTimestampMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ScheduledCallCreationMessage.prototype, "_callType", {
                get: $util.oneOfGetter($oneOfFields = ["callType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ScheduledCallCreationMessage.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ScheduledCallCreationMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.ScheduledCallCreationMessage
             * @static
             * @param {proto.Message.IScheduledCallCreationMessage=} [properties] Properties to set
             * @returns {proto.Message.ScheduledCallCreationMessage} ScheduledCallCreationMessage instance
             */
            ScheduledCallCreationMessage.create = function create(properties) {
                return new ScheduledCallCreationMessage(properties);
            };

            /**
             * Encodes the specified ScheduledCallCreationMessage message. Does not implicitly {@link proto.Message.ScheduledCallCreationMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.ScheduledCallCreationMessage
             * @static
             * @param {proto.Message.IScheduledCallCreationMessage} message ScheduledCallCreationMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ScheduledCallCreationMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.scheduledTimestampMs != null && Object.hasOwnProperty.call(message, "scheduledTimestampMs"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int64(message.scheduledTimestampMs);
                if (message.callType != null && Object.hasOwnProperty.call(message, "callType"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.callType);
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.title);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ScheduledCallCreationMessage message, length delimited. Does not implicitly {@link proto.Message.ScheduledCallCreationMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.ScheduledCallCreationMessage
             * @static
             * @param {proto.Message.IScheduledCallCreationMessage} message ScheduledCallCreationMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ScheduledCallCreationMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ScheduledCallCreationMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.ScheduledCallCreationMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.ScheduledCallCreationMessage} ScheduledCallCreationMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ScheduledCallCreationMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.scheduledTimestampMs = reader.int64();
                            message._scheduledTimestampMs = "scheduledTimestampMs";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.callType = reader.int32();
                            message._callType = "callType";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.title = reader.stringVerify();
                            message._title = "title";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ScheduledCallCreationMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.ScheduledCallCreationMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.ScheduledCallCreationMessage} ScheduledCallCreationMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ScheduledCallCreationMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ScheduledCallCreationMessage message.
             * @function verify
             * @memberof proto.Message.ScheduledCallCreationMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ScheduledCallCreationMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.scheduledTimestampMs != null && Object.hasOwnProperty.call(message, "scheduledTimestampMs")) {
                    properties._scheduledTimestampMs = 1;
                    if (!$util.isInteger(message.scheduledTimestampMs) && !(message.scheduledTimestampMs && $util.isInteger(message.scheduledTimestampMs.low) && $util.isInteger(message.scheduledTimestampMs.high)))
                        return "scheduledTimestampMs: integer|Long expected";
                }
                if (message.callType != null && Object.hasOwnProperty.call(message, "callType")) {
                    properties._callType = 1;
                    if (typeof message.callType !== "number" || (message.callType | 0) !== message.callType)
                        return "callType: enum value expected";
                }
                if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                    properties._title = 1;
                    if (!$util.isString(message.title))
                        return "title: string expected";
                }
                return null;
            };

            /**
             * Creates a ScheduledCallCreationMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.ScheduledCallCreationMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.ScheduledCallCreationMessage} ScheduledCallCreationMessage
             */
            ScheduledCallCreationMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.ScheduledCallCreationMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.scheduledTimestampMs != null)
                    if ($util.Long)
                        message.scheduledTimestampMs = $util.Long.fromValue(object.scheduledTimestampMs, false);
                    else if (typeof object.scheduledTimestampMs === "string")
                        message.scheduledTimestampMs = parseInt(object.scheduledTimestampMs, 10);
                    else if (typeof object.scheduledTimestampMs === "number")
                        message.scheduledTimestampMs = object.scheduledTimestampMs;
                    else if (typeof object.scheduledTimestampMs === "object")
                        message.scheduledTimestampMs = new $util.LongBits(object.scheduledTimestampMs.low >>> 0, object.scheduledTimestampMs.high >>> 0).toNumber();
                switch (object.callType) {
                case "UNKNOWN":
                case 0:
                    message.callType = 0;
                    break;
                case "VOICE":
                case 1:
                    message.callType = 1;
                    break;
                case "VIDEO":
                case 2:
                    message.callType = 2;
                    break;
                default:
                    if (typeof object.callType === "number" && (object.callType | 0) === object.callType)
                        message.callType = object.callType;
                }
                if (object.title != null)
                    message.title = String(object.title);
                return message;
            };

            /**
             * Creates a plain object from a ScheduledCallCreationMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.ScheduledCallCreationMessage
             * @static
             * @param {proto.Message.ScheduledCallCreationMessage} message ScheduledCallCreationMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ScheduledCallCreationMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.scheduledTimestampMs != null && Object.hasOwnProperty.call(message, "scheduledTimestampMs"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.scheduledTimestampMs = typeof message.scheduledTimestampMs === "number" ? BigInt(message.scheduledTimestampMs) : $util.Long.fromBits(message.scheduledTimestampMs.low >>> 0, message.scheduledTimestampMs.high >>> 0, false).toBigInt();
                    else if (typeof message.scheduledTimestampMs === "number")
                        object.scheduledTimestampMs = options.longs === String ? String(message.scheduledTimestampMs) : message.scheduledTimestampMs;
                    else
                        object.scheduledTimestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.scheduledTimestampMs) : options.longs === Number ? new $util.LongBits(message.scheduledTimestampMs.low >>> 0, message.scheduledTimestampMs.high >>> 0).toNumber() : message.scheduledTimestampMs;
                if (message.callType != null && Object.hasOwnProperty.call(message, "callType"))
                    object.callType = options.enums === String ? $root.proto.Message.ScheduledCallCreationMessage.CallType[message.callType] === undefined ? message.callType : $root.proto.Message.ScheduledCallCreationMessage.CallType[message.callType] : message.callType;
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    object.title = message.title;
                return object;
            };

            /**
             * Converts this ScheduledCallCreationMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.ScheduledCallCreationMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ScheduledCallCreationMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ScheduledCallCreationMessage
             * @function getTypeUrl
             * @memberof proto.Message.ScheduledCallCreationMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ScheduledCallCreationMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.ScheduledCallCreationMessage";
            };

            /**
             * CallType enum.
             * @name proto.Message.ScheduledCallCreationMessage.CallType
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} VOICE=1 VOICE value
             * @property {number} VIDEO=2 VIDEO value
             */
            ScheduledCallCreationMessage.CallType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "VOICE"] = 1;
                values[valuesById[2] = "VIDEO"] = 2;
                return values;
            })();

            return ScheduledCallCreationMessage;
        })();

        Message.ScheduledCallEditMessage = (function() {

            /**
             * Properties of a ScheduledCallEditMessage.
             * @memberof proto.Message
             * @interface IScheduledCallEditMessage
             * @property {proto.IMessageKey|null} [key] ScheduledCallEditMessage key
             * @property {proto.Message.ScheduledCallEditMessage.EditType|null} [editType] ScheduledCallEditMessage editType
             */

            /**
             * Constructs a new ScheduledCallEditMessage.
             * @memberof proto.Message
             * @classdesc Represents a ScheduledCallEditMessage.
             * @implements IScheduledCallEditMessage
             * @constructor
             * @param {proto.Message.IScheduledCallEditMessage=} [properties] Properties to set
             */
            function ScheduledCallEditMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ScheduledCallEditMessage key.
             * @member {proto.IMessageKey|null|undefined} key
             * @memberof proto.Message.ScheduledCallEditMessage
             * @instance
             */
            ScheduledCallEditMessage.prototype.key = null;

            /**
             * ScheduledCallEditMessage editType.
             * @member {proto.Message.ScheduledCallEditMessage.EditType|null|undefined} editType
             * @memberof proto.Message.ScheduledCallEditMessage
             * @instance
             */
            ScheduledCallEditMessage.prototype.editType = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ScheduledCallEditMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ScheduledCallEditMessage.prototype, "_editType", {
                get: $util.oneOfGetter($oneOfFields = ["editType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ScheduledCallEditMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.ScheduledCallEditMessage
             * @static
             * @param {proto.Message.IScheduledCallEditMessage=} [properties] Properties to set
             * @returns {proto.Message.ScheduledCallEditMessage} ScheduledCallEditMessage instance
             */
            ScheduledCallEditMessage.create = function create(properties) {
                return new ScheduledCallEditMessage(properties);
            };

            /**
             * Encodes the specified ScheduledCallEditMessage message. Does not implicitly {@link proto.Message.ScheduledCallEditMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.ScheduledCallEditMessage
             * @static
             * @param {proto.Message.IScheduledCallEditMessage} message ScheduledCallEditMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ScheduledCallEditMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    $root.proto.MessageKey.encode(message.key, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.editType != null && Object.hasOwnProperty.call(message, "editType"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.editType);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ScheduledCallEditMessage message, length delimited. Does not implicitly {@link proto.Message.ScheduledCallEditMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.ScheduledCallEditMessage
             * @static
             * @param {proto.Message.IScheduledCallEditMessage} message ScheduledCallEditMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ScheduledCallEditMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ScheduledCallEditMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.ScheduledCallEditMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.ScheduledCallEditMessage} ScheduledCallEditMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ScheduledCallEditMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.key = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.key);
                            message._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.editType = reader.int32();
                            message._editType = "editType";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ScheduledCallEditMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.ScheduledCallEditMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.ScheduledCallEditMessage} ScheduledCallEditMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ScheduledCallEditMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ScheduledCallEditMessage message.
             * @function verify
             * @memberof proto.Message.ScheduledCallEditMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ScheduledCallEditMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                    properties._key = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.key, q + 1);
                        if (error)
                            return "key." + error;
                    }
                }
                if (message.editType != null && Object.hasOwnProperty.call(message, "editType")) {
                    properties._editType = 1;
                    if (typeof message.editType !== "number" || (message.editType | 0) !== message.editType)
                        return "editType: enum value expected";
                }
                return null;
            };

            /**
             * Creates a ScheduledCallEditMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.ScheduledCallEditMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.ScheduledCallEditMessage} ScheduledCallEditMessage
             */
            ScheduledCallEditMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.ScheduledCallEditMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.key != null) {
                    if (!$util.isObject(object.key))
                        throw TypeError(".proto.Message.ScheduledCallEditMessage.key: object expected");
                    message.key = $root.proto.MessageKey.fromObject(object.key, q + 1);
                }
                switch (object.editType) {
                case "UNKNOWN":
                case 0:
                    message.editType = 0;
                    break;
                case "CANCEL":
                case 1:
                    message.editType = 1;
                    break;
                default:
                    if (typeof object.editType === "number" && (object.editType | 0) === object.editType)
                        message.editType = object.editType;
                }
                return message;
            };

            /**
             * Creates a plain object from a ScheduledCallEditMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.ScheduledCallEditMessage
             * @static
             * @param {proto.Message.ScheduledCallEditMessage} message ScheduledCallEditMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ScheduledCallEditMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    object.key = $root.proto.MessageKey.toObject(message.key, options, q + 1);
                if (message.editType != null && Object.hasOwnProperty.call(message, "editType"))
                    object.editType = options.enums === String ? $root.proto.Message.ScheduledCallEditMessage.EditType[message.editType] === undefined ? message.editType : $root.proto.Message.ScheduledCallEditMessage.EditType[message.editType] : message.editType;
                return object;
            };

            /**
             * Converts this ScheduledCallEditMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.ScheduledCallEditMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ScheduledCallEditMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ScheduledCallEditMessage
             * @function getTypeUrl
             * @memberof proto.Message.ScheduledCallEditMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ScheduledCallEditMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.ScheduledCallEditMessage";
            };

            /**
             * EditType enum.
             * @name proto.Message.ScheduledCallEditMessage.EditType
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} CANCEL=1 CANCEL value
             */
            ScheduledCallEditMessage.EditType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "CANCEL"] = 1;
                return values;
            })();

            return ScheduledCallEditMessage;
        })();

        Message.SecretEncryptedMessage = (function() {

            /**
             * Properties of a SecretEncryptedMessage.
             * @memberof proto.Message
             * @interface ISecretEncryptedMessage
             * @property {proto.IMessageKey|null} [targetMessageKey] SecretEncryptedMessage targetMessageKey
             * @property {Uint8Array|null} [encPayload] SecretEncryptedMessage encPayload
             * @property {Uint8Array|null} [encIv] SecretEncryptedMessage encIv
             * @property {proto.Message.SecretEncryptedMessage.SecretEncType|null} [secretEncType] SecretEncryptedMessage secretEncType
             * @property {string|null} [remoteKeyId] SecretEncryptedMessage remoteKeyId
             */

            /**
             * Constructs a new SecretEncryptedMessage.
             * @memberof proto.Message
             * @classdesc Represents a SecretEncryptedMessage.
             * @implements ISecretEncryptedMessage
             * @constructor
             * @param {proto.Message.ISecretEncryptedMessage=} [properties] Properties to set
             */
            function SecretEncryptedMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * SecretEncryptedMessage targetMessageKey.
             * @member {proto.IMessageKey|null|undefined} targetMessageKey
             * @memberof proto.Message.SecretEncryptedMessage
             * @instance
             */
            SecretEncryptedMessage.prototype.targetMessageKey = null;

            /**
             * SecretEncryptedMessage encPayload.
             * @member {Uint8Array|null|undefined} encPayload
             * @memberof proto.Message.SecretEncryptedMessage
             * @instance
             */
            SecretEncryptedMessage.prototype.encPayload = null;

            /**
             * SecretEncryptedMessage encIv.
             * @member {Uint8Array|null|undefined} encIv
             * @memberof proto.Message.SecretEncryptedMessage
             * @instance
             */
            SecretEncryptedMessage.prototype.encIv = null;

            /**
             * SecretEncryptedMessage secretEncType.
             * @member {proto.Message.SecretEncryptedMessage.SecretEncType|null|undefined} secretEncType
             * @memberof proto.Message.SecretEncryptedMessage
             * @instance
             */
            SecretEncryptedMessage.prototype.secretEncType = null;

            /**
             * SecretEncryptedMessage remoteKeyId.
             * @member {string|null|undefined} remoteKeyId
             * @memberof proto.Message.SecretEncryptedMessage
             * @instance
             */
            SecretEncryptedMessage.prototype.remoteKeyId = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SecretEncryptedMessage.prototype, "_targetMessageKey", {
                get: $util.oneOfGetter($oneOfFields = ["targetMessageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SecretEncryptedMessage.prototype, "_encPayload", {
                get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SecretEncryptedMessage.prototype, "_encIv", {
                get: $util.oneOfGetter($oneOfFields = ["encIv"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SecretEncryptedMessage.prototype, "_secretEncType", {
                get: $util.oneOfGetter($oneOfFields = ["secretEncType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SecretEncryptedMessage.prototype, "_remoteKeyId", {
                get: $util.oneOfGetter($oneOfFields = ["remoteKeyId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new SecretEncryptedMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.SecretEncryptedMessage
             * @static
             * @param {proto.Message.ISecretEncryptedMessage=} [properties] Properties to set
             * @returns {proto.Message.SecretEncryptedMessage} SecretEncryptedMessage instance
             */
            SecretEncryptedMessage.create = function create(properties) {
                return new SecretEncryptedMessage(properties);
            };

            /**
             * Encodes the specified SecretEncryptedMessage message. Does not implicitly {@link proto.Message.SecretEncryptedMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.SecretEncryptedMessage
             * @static
             * @param {proto.Message.ISecretEncryptedMessage} message SecretEncryptedMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SecretEncryptedMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.targetMessageKey != null && Object.hasOwnProperty.call(message, "targetMessageKey"))
                    $root.proto.MessageKey.encode(message.targetMessageKey, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encPayload);
                if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.encIv);
                if (message.secretEncType != null && Object.hasOwnProperty.call(message, "secretEncType"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int32(message.secretEncType);
                if (message.remoteKeyId != null && Object.hasOwnProperty.call(message, "remoteKeyId"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.remoteKeyId);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SecretEncryptedMessage message, length delimited. Does not implicitly {@link proto.Message.SecretEncryptedMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.SecretEncryptedMessage
             * @static
             * @param {proto.Message.ISecretEncryptedMessage} message SecretEncryptedMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SecretEncryptedMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a SecretEncryptedMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.SecretEncryptedMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.SecretEncryptedMessage} SecretEncryptedMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SecretEncryptedMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.targetMessageKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.targetMessageKey);
                            message._targetMessageKey = "targetMessageKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.encPayload = reader.bytes();
                            message._encPayload = "encPayload";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.encIv = reader.bytes();
                            message._encIv = "encIv";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.secretEncType = reader.int32();
                            message._secretEncType = "secretEncType";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.remoteKeyId = reader.stringVerify();
                            message._remoteKeyId = "remoteKeyId";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a SecretEncryptedMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.SecretEncryptedMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.SecretEncryptedMessage} SecretEncryptedMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SecretEncryptedMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SecretEncryptedMessage message.
             * @function verify
             * @memberof proto.Message.SecretEncryptedMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SecretEncryptedMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.targetMessageKey != null && Object.hasOwnProperty.call(message, "targetMessageKey")) {
                    properties._targetMessageKey = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.targetMessageKey, q + 1);
                        if (error)
                            return "targetMessageKey." + error;
                    }
                }
                if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload")) {
                    properties._encPayload = 1;
                    if (!(message.encPayload && typeof message.encPayload.length === "number" || $util.isString(message.encPayload)))
                        return "encPayload: buffer expected";
                }
                if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv")) {
                    properties._encIv = 1;
                    if (!(message.encIv && typeof message.encIv.length === "number" || $util.isString(message.encIv)))
                        return "encIv: buffer expected";
                }
                if (message.secretEncType != null && Object.hasOwnProperty.call(message, "secretEncType")) {
                    properties._secretEncType = 1;
                    if (typeof message.secretEncType !== "number" || (message.secretEncType | 0) !== message.secretEncType)
                        return "secretEncType: enum value expected";
                }
                if (message.remoteKeyId != null && Object.hasOwnProperty.call(message, "remoteKeyId")) {
                    properties._remoteKeyId = 1;
                    if (!$util.isString(message.remoteKeyId))
                        return "remoteKeyId: string expected";
                }
                return null;
            };

            /**
             * Creates a SecretEncryptedMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.SecretEncryptedMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.SecretEncryptedMessage} SecretEncryptedMessage
             */
            SecretEncryptedMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.SecretEncryptedMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.targetMessageKey != null) {
                    if (!$util.isObject(object.targetMessageKey))
                        throw TypeError(".proto.Message.SecretEncryptedMessage.targetMessageKey: object expected");
                    message.targetMessageKey = $root.proto.MessageKey.fromObject(object.targetMessageKey, q + 1);
                }
                if (object.encPayload != null)
                    if (typeof object.encPayload === "string")
                        $util.base64.decode(object.encPayload, message.encPayload = $util.newBuffer($util.base64.length(object.encPayload)), 0);
                    else if (object.encPayload.length >= 0)
                        message.encPayload = object.encPayload;
                if (object.encIv != null)
                    if (typeof object.encIv === "string")
                        $util.base64.decode(object.encIv, message.encIv = $util.newBuffer($util.base64.length(object.encIv)), 0);
                    else if (object.encIv.length >= 0)
                        message.encIv = object.encIv;
                switch (object.secretEncType) {
                case "UNKNOWN":
                case 0:
                    message.secretEncType = 0;
                    break;
                case "EVENT_EDIT":
                case 1:
                    message.secretEncType = 1;
                    break;
                case "MESSAGE_EDIT":
                case 2:
                    message.secretEncType = 2;
                    break;
                case "MESSAGE_SCHEDULE":
                case 3:
                    message.secretEncType = 3;
                    break;
                case "POLL_EDIT":
                case 4:
                    message.secretEncType = 4;
                    break;
                case "POLL_ADD_OPTION":
                case 5:
                    message.secretEncType = 5;
                    break;
                default:
                    if (typeof object.secretEncType === "number" && (object.secretEncType | 0) === object.secretEncType)
                        message.secretEncType = object.secretEncType;
                }
                if (object.remoteKeyId != null)
                    message.remoteKeyId = String(object.remoteKeyId);
                return message;
            };

            /**
             * Creates a plain object from a SecretEncryptedMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.SecretEncryptedMessage
             * @static
             * @param {proto.Message.SecretEncryptedMessage} message SecretEncryptedMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SecretEncryptedMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.targetMessageKey != null && Object.hasOwnProperty.call(message, "targetMessageKey"))
                    object.targetMessageKey = $root.proto.MessageKey.toObject(message.targetMessageKey, options, q + 1);
                if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload"))
                    object.encPayload = options.bytes === String ? $util.base64.encode(message.encPayload, 0, message.encPayload.length) : options.bytes === Array ? Array.prototype.slice.call(message.encPayload) : message.encPayload;
                if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv"))
                    object.encIv = options.bytes === String ? $util.base64.encode(message.encIv, 0, message.encIv.length) : options.bytes === Array ? Array.prototype.slice.call(message.encIv) : message.encIv;
                if (message.secretEncType != null && Object.hasOwnProperty.call(message, "secretEncType"))
                    object.secretEncType = options.enums === String ? $root.proto.Message.SecretEncryptedMessage.SecretEncType[message.secretEncType] === undefined ? message.secretEncType : $root.proto.Message.SecretEncryptedMessage.SecretEncType[message.secretEncType] : message.secretEncType;
                if (message.remoteKeyId != null && Object.hasOwnProperty.call(message, "remoteKeyId"))
                    object.remoteKeyId = message.remoteKeyId;
                return object;
            };

            /**
             * Converts this SecretEncryptedMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.SecretEncryptedMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SecretEncryptedMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for SecretEncryptedMessage
             * @function getTypeUrl
             * @memberof proto.Message.SecretEncryptedMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            SecretEncryptedMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.SecretEncryptedMessage";
            };

            /**
             * SecretEncType enum.
             * @name proto.Message.SecretEncryptedMessage.SecretEncType
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} EVENT_EDIT=1 EVENT_EDIT value
             * @property {number} MESSAGE_EDIT=2 MESSAGE_EDIT value
             * @property {number} MESSAGE_SCHEDULE=3 MESSAGE_SCHEDULE value
             * @property {number} POLL_EDIT=4 POLL_EDIT value
             * @property {number} POLL_ADD_OPTION=5 POLL_ADD_OPTION value
             */
            SecretEncryptedMessage.SecretEncType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "EVENT_EDIT"] = 1;
                values[valuesById[2] = "MESSAGE_EDIT"] = 2;
                values[valuesById[3] = "MESSAGE_SCHEDULE"] = 3;
                values[valuesById[4] = "POLL_EDIT"] = 4;
                values[valuesById[5] = "POLL_ADD_OPTION"] = 5;
                return values;
            })();

            return SecretEncryptedMessage;
        })();

        Message.SendPaymentMessage = (function() {

            /**
             * Properties of a SendPaymentMessage.
             * @memberof proto.Message
             * @interface ISendPaymentMessage
             * @property {proto.IMessage|null} [noteMessage] SendPaymentMessage noteMessage
             * @property {proto.IMessageKey|null} [requestMessageKey] SendPaymentMessage requestMessageKey
             * @property {proto.IPaymentBackground|null} [background] SendPaymentMessage background
             * @property {string|null} [transactionData] SendPaymentMessage transactionData
             */

            /**
             * Constructs a new SendPaymentMessage.
             * @memberof proto.Message
             * @classdesc Represents a SendPaymentMessage.
             * @implements ISendPaymentMessage
             * @constructor
             * @param {proto.Message.ISendPaymentMessage=} [properties] Properties to set
             */
            function SendPaymentMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * SendPaymentMessage noteMessage.
             * @member {proto.IMessage|null|undefined} noteMessage
             * @memberof proto.Message.SendPaymentMessage
             * @instance
             */
            SendPaymentMessage.prototype.noteMessage = null;

            /**
             * SendPaymentMessage requestMessageKey.
             * @member {proto.IMessageKey|null|undefined} requestMessageKey
             * @memberof proto.Message.SendPaymentMessage
             * @instance
             */
            SendPaymentMessage.prototype.requestMessageKey = null;

            /**
             * SendPaymentMessage background.
             * @member {proto.IPaymentBackground|null|undefined} background
             * @memberof proto.Message.SendPaymentMessage
             * @instance
             */
            SendPaymentMessage.prototype.background = null;

            /**
             * SendPaymentMessage transactionData.
             * @member {string|null|undefined} transactionData
             * @memberof proto.Message.SendPaymentMessage
             * @instance
             */
            SendPaymentMessage.prototype.transactionData = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SendPaymentMessage.prototype, "_noteMessage", {
                get: $util.oneOfGetter($oneOfFields = ["noteMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SendPaymentMessage.prototype, "_requestMessageKey", {
                get: $util.oneOfGetter($oneOfFields = ["requestMessageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SendPaymentMessage.prototype, "_background", {
                get: $util.oneOfGetter($oneOfFields = ["background"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SendPaymentMessage.prototype, "_transactionData", {
                get: $util.oneOfGetter($oneOfFields = ["transactionData"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new SendPaymentMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.SendPaymentMessage
             * @static
             * @param {proto.Message.ISendPaymentMessage=} [properties] Properties to set
             * @returns {proto.Message.SendPaymentMessage} SendPaymentMessage instance
             */
            SendPaymentMessage.create = function create(properties) {
                return new SendPaymentMessage(properties);
            };

            /**
             * Encodes the specified SendPaymentMessage message. Does not implicitly {@link proto.Message.SendPaymentMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.SendPaymentMessage
             * @static
             * @param {proto.Message.ISendPaymentMessage} message SendPaymentMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SendPaymentMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.noteMessage != null && Object.hasOwnProperty.call(message, "noteMessage"))
                    $root.proto.Message.encode(message.noteMessage, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.requestMessageKey != null && Object.hasOwnProperty.call(message, "requestMessageKey"))
                    $root.proto.MessageKey.encode(message.requestMessageKey, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.background != null && Object.hasOwnProperty.call(message, "background"))
                    $root.proto.PaymentBackground.encode(message.background, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.transactionData != null && Object.hasOwnProperty.call(message, "transactionData"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.transactionData);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SendPaymentMessage message, length delimited. Does not implicitly {@link proto.Message.SendPaymentMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.SendPaymentMessage
             * @static
             * @param {proto.Message.ISendPaymentMessage} message SendPaymentMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SendPaymentMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a SendPaymentMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.SendPaymentMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.SendPaymentMessage} SendPaymentMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SendPaymentMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 2: {
                            if (u !== 2)
                                break;
                            message.noteMessage = $root.proto.Message.decode(reader, reader.uint32(), undefined, q + 1, message.noteMessage);
                            message._noteMessage = "noteMessage";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.requestMessageKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.requestMessageKey);
                            message._requestMessageKey = "requestMessageKey";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.background = $root.proto.PaymentBackground.decode(reader, reader.uint32(), undefined, q + 1, message.background);
                            message._background = "background";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.transactionData = reader.stringVerify();
                            message._transactionData = "transactionData";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a SendPaymentMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.SendPaymentMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.SendPaymentMessage} SendPaymentMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SendPaymentMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SendPaymentMessage message.
             * @function verify
             * @memberof proto.Message.SendPaymentMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SendPaymentMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.noteMessage != null && Object.hasOwnProperty.call(message, "noteMessage")) {
                    properties._noteMessage = 1;
                    {
                        var error = $root.proto.Message.verify(message.noteMessage, q + 1);
                        if (error)
                            return "noteMessage." + error;
                    }
                }
                if (message.requestMessageKey != null && Object.hasOwnProperty.call(message, "requestMessageKey")) {
                    properties._requestMessageKey = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.requestMessageKey, q + 1);
                        if (error)
                            return "requestMessageKey." + error;
                    }
                }
                if (message.background != null && Object.hasOwnProperty.call(message, "background")) {
                    properties._background = 1;
                    {
                        var error = $root.proto.PaymentBackground.verify(message.background, q + 1);
                        if (error)
                            return "background." + error;
                    }
                }
                if (message.transactionData != null && Object.hasOwnProperty.call(message, "transactionData")) {
                    properties._transactionData = 1;
                    if (!$util.isString(message.transactionData))
                        return "transactionData: string expected";
                }
                return null;
            };

            /**
             * Creates a SendPaymentMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.SendPaymentMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.SendPaymentMessage} SendPaymentMessage
             */
            SendPaymentMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.SendPaymentMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.noteMessage != null) {
                    if (!$util.isObject(object.noteMessage))
                        throw TypeError(".proto.Message.SendPaymentMessage.noteMessage: object expected");
                    message.noteMessage = $root.proto.Message.fromObject(object.noteMessage, q + 1);
                }
                if (object.requestMessageKey != null) {
                    if (!$util.isObject(object.requestMessageKey))
                        throw TypeError(".proto.Message.SendPaymentMessage.requestMessageKey: object expected");
                    message.requestMessageKey = $root.proto.MessageKey.fromObject(object.requestMessageKey, q + 1);
                }
                if (object.background != null) {
                    if (!$util.isObject(object.background))
                        throw TypeError(".proto.Message.SendPaymentMessage.background: object expected");
                    message.background = $root.proto.PaymentBackground.fromObject(object.background, q + 1);
                }
                if (object.transactionData != null)
                    message.transactionData = String(object.transactionData);
                return message;
            };

            /**
             * Creates a plain object from a SendPaymentMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.SendPaymentMessage
             * @static
             * @param {proto.Message.SendPaymentMessage} message SendPaymentMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SendPaymentMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.noteMessage != null && Object.hasOwnProperty.call(message, "noteMessage"))
                    object.noteMessage = $root.proto.Message.toObject(message.noteMessage, options, q + 1);
                if (message.requestMessageKey != null && Object.hasOwnProperty.call(message, "requestMessageKey"))
                    object.requestMessageKey = $root.proto.MessageKey.toObject(message.requestMessageKey, options, q + 1);
                if (message.background != null && Object.hasOwnProperty.call(message, "background"))
                    object.background = $root.proto.PaymentBackground.toObject(message.background, options, q + 1);
                if (message.transactionData != null && Object.hasOwnProperty.call(message, "transactionData"))
                    object.transactionData = message.transactionData;
                return object;
            };

            /**
             * Converts this SendPaymentMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.SendPaymentMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SendPaymentMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for SendPaymentMessage
             * @function getTypeUrl
             * @memberof proto.Message.SendPaymentMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            SendPaymentMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.SendPaymentMessage";
            };

            return SendPaymentMessage;
        })();

        Message.SenderKeyDistributionMessage = (function() {

            /**
             * Properties of a SenderKeyDistributionMessage.
             * @memberof proto.Message
             * @interface ISenderKeyDistributionMessage
             * @property {string|null} [groupId] SenderKeyDistributionMessage groupId
             * @property {Uint8Array|null} [axolotlSenderKeyDistributionMessage] SenderKeyDistributionMessage axolotlSenderKeyDistributionMessage
             */

            /**
             * Constructs a new SenderKeyDistributionMessage.
             * @memberof proto.Message
             * @classdesc Represents a SenderKeyDistributionMessage.
             * @implements ISenderKeyDistributionMessage
             * @constructor
             * @param {proto.Message.ISenderKeyDistributionMessage=} [properties] Properties to set
             */
            function SenderKeyDistributionMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * SenderKeyDistributionMessage groupId.
             * @member {string|null|undefined} groupId
             * @memberof proto.Message.SenderKeyDistributionMessage
             * @instance
             */
            SenderKeyDistributionMessage.prototype.groupId = null;

            /**
             * SenderKeyDistributionMessage axolotlSenderKeyDistributionMessage.
             * @member {Uint8Array|null|undefined} axolotlSenderKeyDistributionMessage
             * @memberof proto.Message.SenderKeyDistributionMessage
             * @instance
             */
            SenderKeyDistributionMessage.prototype.axolotlSenderKeyDistributionMessage = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SenderKeyDistributionMessage.prototype, "_groupId", {
                get: $util.oneOfGetter($oneOfFields = ["groupId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SenderKeyDistributionMessage.prototype, "_axolotlSenderKeyDistributionMessage", {
                get: $util.oneOfGetter($oneOfFields = ["axolotlSenderKeyDistributionMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new SenderKeyDistributionMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.SenderKeyDistributionMessage
             * @static
             * @param {proto.Message.ISenderKeyDistributionMessage=} [properties] Properties to set
             * @returns {proto.Message.SenderKeyDistributionMessage} SenderKeyDistributionMessage instance
             */
            SenderKeyDistributionMessage.create = function create(properties) {
                return new SenderKeyDistributionMessage(properties);
            };

            /**
             * Encodes the specified SenderKeyDistributionMessage message. Does not implicitly {@link proto.Message.SenderKeyDistributionMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.SenderKeyDistributionMessage
             * @static
             * @param {proto.Message.ISenderKeyDistributionMessage} message SenderKeyDistributionMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SenderKeyDistributionMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.groupId != null && Object.hasOwnProperty.call(message, "groupId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.groupId);
                if (message.axolotlSenderKeyDistributionMessage != null && Object.hasOwnProperty.call(message, "axolotlSenderKeyDistributionMessage"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.axolotlSenderKeyDistributionMessage);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SenderKeyDistributionMessage message, length delimited. Does not implicitly {@link proto.Message.SenderKeyDistributionMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.SenderKeyDistributionMessage
             * @static
             * @param {proto.Message.ISenderKeyDistributionMessage} message SenderKeyDistributionMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SenderKeyDistributionMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a SenderKeyDistributionMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.SenderKeyDistributionMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.SenderKeyDistributionMessage} SenderKeyDistributionMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SenderKeyDistributionMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.groupId = reader.stringVerify();
                            message._groupId = "groupId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.axolotlSenderKeyDistributionMessage = reader.bytes();
                            message._axolotlSenderKeyDistributionMessage = "axolotlSenderKeyDistributionMessage";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a SenderKeyDistributionMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.SenderKeyDistributionMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.SenderKeyDistributionMessage} SenderKeyDistributionMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SenderKeyDistributionMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SenderKeyDistributionMessage message.
             * @function verify
             * @memberof proto.Message.SenderKeyDistributionMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SenderKeyDistributionMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.groupId != null && Object.hasOwnProperty.call(message, "groupId")) {
                    properties._groupId = 1;
                    if (!$util.isString(message.groupId))
                        return "groupId: string expected";
                }
                if (message.axolotlSenderKeyDistributionMessage != null && Object.hasOwnProperty.call(message, "axolotlSenderKeyDistributionMessage")) {
                    properties._axolotlSenderKeyDistributionMessage = 1;
                    if (!(message.axolotlSenderKeyDistributionMessage && typeof message.axolotlSenderKeyDistributionMessage.length === "number" || $util.isString(message.axolotlSenderKeyDistributionMessage)))
                        return "axolotlSenderKeyDistributionMessage: buffer expected";
                }
                return null;
            };

            /**
             * Creates a SenderKeyDistributionMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.SenderKeyDistributionMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.SenderKeyDistributionMessage} SenderKeyDistributionMessage
             */
            SenderKeyDistributionMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.SenderKeyDistributionMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.groupId != null)
                    message.groupId = String(object.groupId);
                if (object.axolotlSenderKeyDistributionMessage != null)
                    if (typeof object.axolotlSenderKeyDistributionMessage === "string")
                        $util.base64.decode(object.axolotlSenderKeyDistributionMessage, message.axolotlSenderKeyDistributionMessage = $util.newBuffer($util.base64.length(object.axolotlSenderKeyDistributionMessage)), 0);
                    else if (object.axolotlSenderKeyDistributionMessage.length >= 0)
                        message.axolotlSenderKeyDistributionMessage = object.axolotlSenderKeyDistributionMessage;
                return message;
            };

            /**
             * Creates a plain object from a SenderKeyDistributionMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.SenderKeyDistributionMessage
             * @static
             * @param {proto.Message.SenderKeyDistributionMessage} message SenderKeyDistributionMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SenderKeyDistributionMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.groupId != null && Object.hasOwnProperty.call(message, "groupId"))
                    object.groupId = message.groupId;
                if (message.axolotlSenderKeyDistributionMessage != null && Object.hasOwnProperty.call(message, "axolotlSenderKeyDistributionMessage"))
                    object.axolotlSenderKeyDistributionMessage = options.bytes === String ? $util.base64.encode(message.axolotlSenderKeyDistributionMessage, 0, message.axolotlSenderKeyDistributionMessage.length) : options.bytes === Array ? Array.prototype.slice.call(message.axolotlSenderKeyDistributionMessage) : message.axolotlSenderKeyDistributionMessage;
                return object;
            };

            /**
             * Converts this SenderKeyDistributionMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.SenderKeyDistributionMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SenderKeyDistributionMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for SenderKeyDistributionMessage
             * @function getTypeUrl
             * @memberof proto.Message.SenderKeyDistributionMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            SenderKeyDistributionMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.SenderKeyDistributionMessage";
            };

            return SenderKeyDistributionMessage;
        })();

        Message.SplitPaymentMessage = (function() {

            /**
             * Properties of a SplitPaymentMessage.
             * @memberof proto.Message
             * @interface ISplitPaymentMessage
             * @property {string|null} [splitId] SplitPaymentMessage splitId
             * @property {proto.IMoney|null} [totalAmount] SplitPaymentMessage totalAmount
             * @property {string|null} [description] SplitPaymentMessage description
             * @property {string|null} [requesterJid] SplitPaymentMessage requesterJid
             * @property {Array.<proto.Message.ISplitPaymentParticipant>|null} [participants] SplitPaymentMessage participants
             * @property {number|Long|null} [createdAtMs] SplitPaymentMessage createdAtMs
             * @property {proto.IContextInfo|null} [contextInfo] SplitPaymentMessage contextInfo
             */

            /**
             * Constructs a new SplitPaymentMessage.
             * @memberof proto.Message
             * @classdesc Represents a SplitPaymentMessage.
             * @implements ISplitPaymentMessage
             * @constructor
             * @param {proto.Message.ISplitPaymentMessage=} [properties] Properties to set
             */
            function SplitPaymentMessage(properties) {
                this.participants = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * SplitPaymentMessage splitId.
             * @member {string|null|undefined} splitId
             * @memberof proto.Message.SplitPaymentMessage
             * @instance
             */
            SplitPaymentMessage.prototype.splitId = null;

            /**
             * SplitPaymentMessage totalAmount.
             * @member {proto.IMoney|null|undefined} totalAmount
             * @memberof proto.Message.SplitPaymentMessage
             * @instance
             */
            SplitPaymentMessage.prototype.totalAmount = null;

            /**
             * SplitPaymentMessage description.
             * @member {string|null|undefined} description
             * @memberof proto.Message.SplitPaymentMessage
             * @instance
             */
            SplitPaymentMessage.prototype.description = null;

            /**
             * SplitPaymentMessage requesterJid.
             * @member {string|null|undefined} requesterJid
             * @memberof proto.Message.SplitPaymentMessage
             * @instance
             */
            SplitPaymentMessage.prototype.requesterJid = null;

            /**
             * SplitPaymentMessage participants.
             * @member {Array.<proto.Message.ISplitPaymentParticipant>} participants
             * @memberof proto.Message.SplitPaymentMessage
             * @instance
             */
            SplitPaymentMessage.prototype.participants = $util.emptyArray;

            /**
             * SplitPaymentMessage createdAtMs.
             * @member {number|Long|null|undefined} createdAtMs
             * @memberof proto.Message.SplitPaymentMessage
             * @instance
             */
            SplitPaymentMessage.prototype.createdAtMs = null;

            /**
             * SplitPaymentMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.SplitPaymentMessage
             * @instance
             */
            SplitPaymentMessage.prototype.contextInfo = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SplitPaymentMessage.prototype, "_splitId", {
                get: $util.oneOfGetter($oneOfFields = ["splitId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SplitPaymentMessage.prototype, "_totalAmount", {
                get: $util.oneOfGetter($oneOfFields = ["totalAmount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SplitPaymentMessage.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SplitPaymentMessage.prototype, "_requesterJid", {
                get: $util.oneOfGetter($oneOfFields = ["requesterJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SplitPaymentMessage.prototype, "_createdAtMs", {
                get: $util.oneOfGetter($oneOfFields = ["createdAtMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SplitPaymentMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new SplitPaymentMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.SplitPaymentMessage
             * @static
             * @param {proto.Message.ISplitPaymentMessage=} [properties] Properties to set
             * @returns {proto.Message.SplitPaymentMessage} SplitPaymentMessage instance
             */
            SplitPaymentMessage.create = function create(properties) {
                return new SplitPaymentMessage(properties);
            };

            /**
             * Encodes the specified SplitPaymentMessage message. Does not implicitly {@link proto.Message.SplitPaymentMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.SplitPaymentMessage
             * @static
             * @param {proto.Message.ISplitPaymentMessage} message SplitPaymentMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SplitPaymentMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.splitId != null && Object.hasOwnProperty.call(message, "splitId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.splitId);
                if (message.totalAmount != null && Object.hasOwnProperty.call(message, "totalAmount"))
                    $root.proto.Money.encode(message.totalAmount, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.description);
                if (message.requesterJid != null && Object.hasOwnProperty.call(message, "requesterJid"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.requesterJid);
                if (message.participants != null && message.participants.length)
                    for (var i = 0; i < message.participants.length; ++i)
                        $root.proto.Message.SplitPaymentParticipant.encode(message.participants[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                if (message.createdAtMs != null && Object.hasOwnProperty.call(message, "createdAtMs"))
                    writer.uint32(/* id 6, wireType 0 =*/48).int64(message.createdAtMs);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SplitPaymentMessage message, length delimited. Does not implicitly {@link proto.Message.SplitPaymentMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.SplitPaymentMessage
             * @static
             * @param {proto.Message.ISplitPaymentMessage} message SplitPaymentMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SplitPaymentMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a SplitPaymentMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.SplitPaymentMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.SplitPaymentMessage} SplitPaymentMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SplitPaymentMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.splitId = reader.stringVerify();
                            message._splitId = "splitId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.totalAmount = $root.proto.Money.decode(reader, reader.uint32(), undefined, q + 1, message.totalAmount);
                            message._totalAmount = "totalAmount";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.description = reader.stringVerify();
                            message._description = "description";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.requesterJid = reader.stringVerify();
                            message._requesterJid = "requesterJid";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            if (!(message.participants && message.participants.length))
                                message.participants = [];
                            message.participants.push($root.proto.Message.SplitPaymentParticipant.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.createdAtMs = reader.int64();
                            message._createdAtMs = "createdAtMs";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a SplitPaymentMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.SplitPaymentMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.SplitPaymentMessage} SplitPaymentMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SplitPaymentMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SplitPaymentMessage message.
             * @function verify
             * @memberof proto.Message.SplitPaymentMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SplitPaymentMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.splitId != null && Object.hasOwnProperty.call(message, "splitId")) {
                    properties._splitId = 1;
                    if (!$util.isString(message.splitId))
                        return "splitId: string expected";
                }
                if (message.totalAmount != null && Object.hasOwnProperty.call(message, "totalAmount")) {
                    properties._totalAmount = 1;
                    {
                        var error = $root.proto.Money.verify(message.totalAmount, q + 1);
                        if (error)
                            return "totalAmount." + error;
                    }
                }
                if (message.description != null && Object.hasOwnProperty.call(message, "description")) {
                    properties._description = 1;
                    if (!$util.isString(message.description))
                        return "description: string expected";
                }
                if (message.requesterJid != null && Object.hasOwnProperty.call(message, "requesterJid")) {
                    properties._requesterJid = 1;
                    if (!$util.isString(message.requesterJid))
                        return "requesterJid: string expected";
                }
                if (message.participants != null && Object.hasOwnProperty.call(message, "participants")) {
                    if (!Array.isArray(message.participants))
                        return "participants: array expected";
                    for (var i = 0; i < message.participants.length; ++i) {
                        var error = $root.proto.Message.SplitPaymentParticipant.verify(message.participants[i], q + 1);
                        if (error)
                            return "participants." + error;
                    }
                }
                if (message.createdAtMs != null && Object.hasOwnProperty.call(message, "createdAtMs")) {
                    properties._createdAtMs = 1;
                    if (!$util.isInteger(message.createdAtMs) && !(message.createdAtMs && $util.isInteger(message.createdAtMs.low) && $util.isInteger(message.createdAtMs.high)))
                        return "createdAtMs: integer|Long expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a SplitPaymentMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.SplitPaymentMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.SplitPaymentMessage} SplitPaymentMessage
             */
            SplitPaymentMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.SplitPaymentMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.splitId != null)
                    message.splitId = String(object.splitId);
                if (object.totalAmount != null) {
                    if (!$util.isObject(object.totalAmount))
                        throw TypeError(".proto.Message.SplitPaymentMessage.totalAmount: object expected");
                    message.totalAmount = $root.proto.Money.fromObject(object.totalAmount, q + 1);
                }
                if (object.description != null)
                    message.description = String(object.description);
                if (object.requesterJid != null)
                    message.requesterJid = String(object.requesterJid);
                if (object.participants) {
                    if (!Array.isArray(object.participants))
                        throw TypeError(".proto.Message.SplitPaymentMessage.participants: array expected");
                    message.participants = Array(object.participants.length);
                    for (var i = 0; i < object.participants.length; ++i) {
                        if (!$util.isObject(object.participants[i]))
                            throw TypeError(".proto.Message.SplitPaymentMessage.participants: object expected");
                        message.participants[i] = $root.proto.Message.SplitPaymentParticipant.fromObject(object.participants[i], q + 1);
                    }
                }
                if (object.createdAtMs != null)
                    if ($util.Long)
                        message.createdAtMs = $util.Long.fromValue(object.createdAtMs, false);
                    else if (typeof object.createdAtMs === "string")
                        message.createdAtMs = parseInt(object.createdAtMs, 10);
                    else if (typeof object.createdAtMs === "number")
                        message.createdAtMs = object.createdAtMs;
                    else if (typeof object.createdAtMs === "object")
                        message.createdAtMs = new $util.LongBits(object.createdAtMs.low >>> 0, object.createdAtMs.high >>> 0).toNumber();
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.SplitPaymentMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a SplitPaymentMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.SplitPaymentMessage
             * @static
             * @param {proto.Message.SplitPaymentMessage} message SplitPaymentMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SplitPaymentMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.participants = [];
                if (message.splitId != null && Object.hasOwnProperty.call(message, "splitId"))
                    object.splitId = message.splitId;
                if (message.totalAmount != null && Object.hasOwnProperty.call(message, "totalAmount"))
                    object.totalAmount = $root.proto.Money.toObject(message.totalAmount, options, q + 1);
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    object.description = message.description;
                if (message.requesterJid != null && Object.hasOwnProperty.call(message, "requesterJid"))
                    object.requesterJid = message.requesterJid;
                if (message.participants && message.participants.length) {
                    object.participants = Array(message.participants.length);
                    for (var j = 0; j < message.participants.length; ++j)
                        object.participants[j] = $root.proto.Message.SplitPaymentParticipant.toObject(message.participants[j], options, q + 1);
                }
                if (message.createdAtMs != null && Object.hasOwnProperty.call(message, "createdAtMs"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.createdAtMs = typeof message.createdAtMs === "number" ? BigInt(message.createdAtMs) : $util.Long.fromBits(message.createdAtMs.low >>> 0, message.createdAtMs.high >>> 0, false).toBigInt();
                    else if (typeof message.createdAtMs === "number")
                        object.createdAtMs = options.longs === String ? String(message.createdAtMs) : message.createdAtMs;
                    else
                        object.createdAtMs = options.longs === String ? $util.Long.prototype.toString.call(message.createdAtMs) : options.longs === Number ? new $util.LongBits(message.createdAtMs.low >>> 0, message.createdAtMs.high >>> 0).toNumber() : message.createdAtMs;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                return object;
            };

            /**
             * Converts this SplitPaymentMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.SplitPaymentMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SplitPaymentMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for SplitPaymentMessage
             * @function getTypeUrl
             * @memberof proto.Message.SplitPaymentMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            SplitPaymentMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.SplitPaymentMessage";
            };

            return SplitPaymentMessage;
        })();

        Message.SplitPaymentParticipant = (function() {

            /**
             * Properties of a SplitPaymentParticipant.
             * @memberof proto.Message
             * @interface ISplitPaymentParticipant
             * @property {string|null} [jid] SplitPaymentParticipant jid
             * @property {proto.IMoney|null} [amount] SplitPaymentParticipant amount
             * @property {proto.Message.SplitPaymentParticipant.SplitPaymentStatus|null} [status] SplitPaymentParticipant status
             */

            /**
             * Constructs a new SplitPaymentParticipant.
             * @memberof proto.Message
             * @classdesc Represents a SplitPaymentParticipant.
             * @implements ISplitPaymentParticipant
             * @constructor
             * @param {proto.Message.ISplitPaymentParticipant=} [properties] Properties to set
             */
            function SplitPaymentParticipant(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * SplitPaymentParticipant jid.
             * @member {string|null|undefined} jid
             * @memberof proto.Message.SplitPaymentParticipant
             * @instance
             */
            SplitPaymentParticipant.prototype.jid = null;

            /**
             * SplitPaymentParticipant amount.
             * @member {proto.IMoney|null|undefined} amount
             * @memberof proto.Message.SplitPaymentParticipant
             * @instance
             */
            SplitPaymentParticipant.prototype.amount = null;

            /**
             * SplitPaymentParticipant status.
             * @member {proto.Message.SplitPaymentParticipant.SplitPaymentStatus|null|undefined} status
             * @memberof proto.Message.SplitPaymentParticipant
             * @instance
             */
            SplitPaymentParticipant.prototype.status = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SplitPaymentParticipant.prototype, "_jid", {
                get: $util.oneOfGetter($oneOfFields = ["jid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SplitPaymentParticipant.prototype, "_amount", {
                get: $util.oneOfGetter($oneOfFields = ["amount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SplitPaymentParticipant.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new SplitPaymentParticipant instance using the specified properties.
             * @function create
             * @memberof proto.Message.SplitPaymentParticipant
             * @static
             * @param {proto.Message.ISplitPaymentParticipant=} [properties] Properties to set
             * @returns {proto.Message.SplitPaymentParticipant} SplitPaymentParticipant instance
             */
            SplitPaymentParticipant.create = function create(properties) {
                return new SplitPaymentParticipant(properties);
            };

            /**
             * Encodes the specified SplitPaymentParticipant message. Does not implicitly {@link proto.Message.SplitPaymentParticipant.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.SplitPaymentParticipant
             * @static
             * @param {proto.Message.ISplitPaymentParticipant} message SplitPaymentParticipant message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SplitPaymentParticipant.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.jid != null && Object.hasOwnProperty.call(message, "jid"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.jid);
                if (message.amount != null && Object.hasOwnProperty.call(message, "amount"))
                    $root.proto.Money.encode(message.amount, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.status);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SplitPaymentParticipant message, length delimited. Does not implicitly {@link proto.Message.SplitPaymentParticipant.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.SplitPaymentParticipant
             * @static
             * @param {proto.Message.ISplitPaymentParticipant} message SplitPaymentParticipant message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SplitPaymentParticipant.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a SplitPaymentParticipant message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.SplitPaymentParticipant
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.SplitPaymentParticipant} SplitPaymentParticipant
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SplitPaymentParticipant.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.jid = reader.stringVerify();
                            message._jid = "jid";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.amount = $root.proto.Money.decode(reader, reader.uint32(), undefined, q + 1, message.amount);
                            message._amount = "amount";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.status = reader.int32();
                            message._status = "status";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a SplitPaymentParticipant message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.SplitPaymentParticipant
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.SplitPaymentParticipant} SplitPaymentParticipant
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SplitPaymentParticipant.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SplitPaymentParticipant message.
             * @function verify
             * @memberof proto.Message.SplitPaymentParticipant
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SplitPaymentParticipant.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.jid != null && Object.hasOwnProperty.call(message, "jid")) {
                    properties._jid = 1;
                    if (!$util.isString(message.jid))
                        return "jid: string expected";
                }
                if (message.amount != null && Object.hasOwnProperty.call(message, "amount")) {
                    properties._amount = 1;
                    {
                        var error = $root.proto.Money.verify(message.amount, q + 1);
                        if (error)
                            return "amount." + error;
                    }
                }
                if (message.status != null && Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (typeof message.status !== "number" || (message.status | 0) !== message.status)
                        return "status: enum value expected";
                }
                return null;
            };

            /**
             * Creates a SplitPaymentParticipant message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.SplitPaymentParticipant
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.SplitPaymentParticipant} SplitPaymentParticipant
             */
            SplitPaymentParticipant.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.SplitPaymentParticipant: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.jid != null)
                    message.jid = String(object.jid);
                if (object.amount != null) {
                    if (!$util.isObject(object.amount))
                        throw TypeError(".proto.Message.SplitPaymentParticipant.amount: object expected");
                    message.amount = $root.proto.Money.fromObject(object.amount, q + 1);
                }
                switch (object.status) {
                case "PENDING":
                case 0:
                    message.status = 0;
                    break;
                case "PAID":
                case 1:
                    message.status = 1;
                    break;
                default:
                    if (typeof object.status === "number" && (object.status | 0) === object.status)
                        message.status = object.status;
                }
                return message;
            };

            /**
             * Creates a plain object from a SplitPaymentParticipant message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.SplitPaymentParticipant
             * @static
             * @param {proto.Message.SplitPaymentParticipant} message SplitPaymentParticipant
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SplitPaymentParticipant.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.jid != null && Object.hasOwnProperty.call(message, "jid"))
                    object.jid = message.jid;
                if (message.amount != null && Object.hasOwnProperty.call(message, "amount"))
                    object.amount = $root.proto.Money.toObject(message.amount, options, q + 1);
                if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                    object.status = options.enums === String ? $root.proto.Message.SplitPaymentParticipant.SplitPaymentStatus[message.status] === undefined ? message.status : $root.proto.Message.SplitPaymentParticipant.SplitPaymentStatus[message.status] : message.status;
                return object;
            };

            /**
             * Converts this SplitPaymentParticipant to JSON.
             * @function toJSON
             * @memberof proto.Message.SplitPaymentParticipant
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SplitPaymentParticipant.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for SplitPaymentParticipant
             * @function getTypeUrl
             * @memberof proto.Message.SplitPaymentParticipant
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            SplitPaymentParticipant.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.SplitPaymentParticipant";
            };

            /**
             * SplitPaymentStatus enum.
             * @name proto.Message.SplitPaymentParticipant.SplitPaymentStatus
             * @enum {number}
             * @property {number} PENDING=0 PENDING value
             * @property {number} PAID=1 PAID value
             */
            SplitPaymentParticipant.SplitPaymentStatus = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "PENDING"] = 0;
                values[valuesById[1] = "PAID"] = 1;
                return values;
            })();

            return SplitPaymentParticipant;
        })();

        Message.StatusNotificationMessage = (function() {

            /**
             * Properties of a StatusNotificationMessage.
             * @memberof proto.Message
             * @interface IStatusNotificationMessage
             * @property {proto.IMessageKey|null} [responseMessageKey] StatusNotificationMessage responseMessageKey
             * @property {proto.IMessageKey|null} [originalMessageKey] StatusNotificationMessage originalMessageKey
             * @property {proto.Message.StatusNotificationMessage.StatusNotificationType|null} [type] StatusNotificationMessage type
             */

            /**
             * Constructs a new StatusNotificationMessage.
             * @memberof proto.Message
             * @classdesc Represents a StatusNotificationMessage.
             * @implements IStatusNotificationMessage
             * @constructor
             * @param {proto.Message.IStatusNotificationMessage=} [properties] Properties to set
             */
            function StatusNotificationMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * StatusNotificationMessage responseMessageKey.
             * @member {proto.IMessageKey|null|undefined} responseMessageKey
             * @memberof proto.Message.StatusNotificationMessage
             * @instance
             */
            StatusNotificationMessage.prototype.responseMessageKey = null;

            /**
             * StatusNotificationMessage originalMessageKey.
             * @member {proto.IMessageKey|null|undefined} originalMessageKey
             * @memberof proto.Message.StatusNotificationMessage
             * @instance
             */
            StatusNotificationMessage.prototype.originalMessageKey = null;

            /**
             * StatusNotificationMessage type.
             * @member {proto.Message.StatusNotificationMessage.StatusNotificationType|null|undefined} type
             * @memberof proto.Message.StatusNotificationMessage
             * @instance
             */
            StatusNotificationMessage.prototype.type = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusNotificationMessage.prototype, "_responseMessageKey", {
                get: $util.oneOfGetter($oneOfFields = ["responseMessageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusNotificationMessage.prototype, "_originalMessageKey", {
                get: $util.oneOfGetter($oneOfFields = ["originalMessageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusNotificationMessage.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StatusNotificationMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.StatusNotificationMessage
             * @static
             * @param {proto.Message.IStatusNotificationMessage=} [properties] Properties to set
             * @returns {proto.Message.StatusNotificationMessage} StatusNotificationMessage instance
             */
            StatusNotificationMessage.create = function create(properties) {
                return new StatusNotificationMessage(properties);
            };

            /**
             * Encodes the specified StatusNotificationMessage message. Does not implicitly {@link proto.Message.StatusNotificationMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.StatusNotificationMessage
             * @static
             * @param {proto.Message.IStatusNotificationMessage} message StatusNotificationMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StatusNotificationMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.responseMessageKey != null && Object.hasOwnProperty.call(message, "responseMessageKey"))
                    $root.proto.MessageKey.encode(message.responseMessageKey, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.originalMessageKey != null && Object.hasOwnProperty.call(message, "originalMessageKey"))
                    $root.proto.MessageKey.encode(message.originalMessageKey, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.type);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified StatusNotificationMessage message, length delimited. Does not implicitly {@link proto.Message.StatusNotificationMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.StatusNotificationMessage
             * @static
             * @param {proto.Message.IStatusNotificationMessage} message StatusNotificationMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StatusNotificationMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a StatusNotificationMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.StatusNotificationMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.StatusNotificationMessage} StatusNotificationMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StatusNotificationMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.responseMessageKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.responseMessageKey);
                            message._responseMessageKey = "responseMessageKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.originalMessageKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.originalMessageKey);
                            message._originalMessageKey = "originalMessageKey";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.type = reader.int32();
                            message._type = "type";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a StatusNotificationMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.StatusNotificationMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.StatusNotificationMessage} StatusNotificationMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StatusNotificationMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a StatusNotificationMessage message.
             * @function verify
             * @memberof proto.Message.StatusNotificationMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StatusNotificationMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.responseMessageKey != null && Object.hasOwnProperty.call(message, "responseMessageKey")) {
                    properties._responseMessageKey = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.responseMessageKey, q + 1);
                        if (error)
                            return "responseMessageKey." + error;
                    }
                }
                if (message.originalMessageKey != null && Object.hasOwnProperty.call(message, "originalMessageKey")) {
                    properties._originalMessageKey = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.originalMessageKey, q + 1);
                        if (error)
                            return "originalMessageKey." + error;
                    }
                }
                if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                    properties._type = 1;
                    if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                        return "type: enum value expected";
                }
                return null;
            };

            /**
             * Creates a StatusNotificationMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.StatusNotificationMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.StatusNotificationMessage} StatusNotificationMessage
             */
            StatusNotificationMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.StatusNotificationMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.responseMessageKey != null) {
                    if (!$util.isObject(object.responseMessageKey))
                        throw TypeError(".proto.Message.StatusNotificationMessage.responseMessageKey: object expected");
                    message.responseMessageKey = $root.proto.MessageKey.fromObject(object.responseMessageKey, q + 1);
                }
                if (object.originalMessageKey != null) {
                    if (!$util.isObject(object.originalMessageKey))
                        throw TypeError(".proto.Message.StatusNotificationMessage.originalMessageKey: object expected");
                    message.originalMessageKey = $root.proto.MessageKey.fromObject(object.originalMessageKey, q + 1);
                }
                switch (object.type) {
                case "UNKNOWN":
                case 0:
                    message.type = 0;
                    break;
                case "STATUS_ADD_YOURS":
                case 1:
                    message.type = 1;
                    break;
                case "STATUS_RESHARE":
                case 2:
                    message.type = 2;
                    break;
                case "STATUS_QUESTION_ANSWER_RESHARE":
                case 3:
                    message.type = 3;
                    break;
                default:
                    if (typeof object.type === "number" && (object.type | 0) === object.type)
                        message.type = object.type;
                }
                return message;
            };

            /**
             * Creates a plain object from a StatusNotificationMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.StatusNotificationMessage
             * @static
             * @param {proto.Message.StatusNotificationMessage} message StatusNotificationMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StatusNotificationMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.responseMessageKey != null && Object.hasOwnProperty.call(message, "responseMessageKey"))
                    object.responseMessageKey = $root.proto.MessageKey.toObject(message.responseMessageKey, options, q + 1);
                if (message.originalMessageKey != null && Object.hasOwnProperty.call(message, "originalMessageKey"))
                    object.originalMessageKey = $root.proto.MessageKey.toObject(message.originalMessageKey, options, q + 1);
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    object.type = options.enums === String ? $root.proto.Message.StatusNotificationMessage.StatusNotificationType[message.type] === undefined ? message.type : $root.proto.Message.StatusNotificationMessage.StatusNotificationType[message.type] : message.type;
                return object;
            };

            /**
             * Converts this StatusNotificationMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.StatusNotificationMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StatusNotificationMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for StatusNotificationMessage
             * @function getTypeUrl
             * @memberof proto.Message.StatusNotificationMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            StatusNotificationMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.StatusNotificationMessage";
            };

            /**
             * StatusNotificationType enum.
             * @name proto.Message.StatusNotificationMessage.StatusNotificationType
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} STATUS_ADD_YOURS=1 STATUS_ADD_YOURS value
             * @property {number} STATUS_RESHARE=2 STATUS_RESHARE value
             * @property {number} STATUS_QUESTION_ANSWER_RESHARE=3 STATUS_QUESTION_ANSWER_RESHARE value
             */
            StatusNotificationMessage.StatusNotificationType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "STATUS_ADD_YOURS"] = 1;
                values[valuesById[2] = "STATUS_RESHARE"] = 2;
                values[valuesById[3] = "STATUS_QUESTION_ANSWER_RESHARE"] = 3;
                return values;
            })();

            return StatusNotificationMessage;
        })();

        Message.StatusQuestionAnswerMessage = (function() {

            /**
             * Properties of a StatusQuestionAnswerMessage.
             * @memberof proto.Message
             * @interface IStatusQuestionAnswerMessage
             * @property {proto.IMessageKey|null} [key] StatusQuestionAnswerMessage key
             * @property {string|null} [text] StatusQuestionAnswerMessage text
             */

            /**
             * Constructs a new StatusQuestionAnswerMessage.
             * @memberof proto.Message
             * @classdesc Represents a StatusQuestionAnswerMessage.
             * @implements IStatusQuestionAnswerMessage
             * @constructor
             * @param {proto.Message.IStatusQuestionAnswerMessage=} [properties] Properties to set
             */
            function StatusQuestionAnswerMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * StatusQuestionAnswerMessage key.
             * @member {proto.IMessageKey|null|undefined} key
             * @memberof proto.Message.StatusQuestionAnswerMessage
             * @instance
             */
            StatusQuestionAnswerMessage.prototype.key = null;

            /**
             * StatusQuestionAnswerMessage text.
             * @member {string|null|undefined} text
             * @memberof proto.Message.StatusQuestionAnswerMessage
             * @instance
             */
            StatusQuestionAnswerMessage.prototype.text = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusQuestionAnswerMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusQuestionAnswerMessage.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StatusQuestionAnswerMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.StatusQuestionAnswerMessage
             * @static
             * @param {proto.Message.IStatusQuestionAnswerMessage=} [properties] Properties to set
             * @returns {proto.Message.StatusQuestionAnswerMessage} StatusQuestionAnswerMessage instance
             */
            StatusQuestionAnswerMessage.create = function create(properties) {
                return new StatusQuestionAnswerMessage(properties);
            };

            /**
             * Encodes the specified StatusQuestionAnswerMessage message. Does not implicitly {@link proto.Message.StatusQuestionAnswerMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.StatusQuestionAnswerMessage
             * @static
             * @param {proto.Message.IStatusQuestionAnswerMessage} message StatusQuestionAnswerMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StatusQuestionAnswerMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    $root.proto.MessageKey.encode(message.key, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.text);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified StatusQuestionAnswerMessage message, length delimited. Does not implicitly {@link proto.Message.StatusQuestionAnswerMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.StatusQuestionAnswerMessage
             * @static
             * @param {proto.Message.IStatusQuestionAnswerMessage} message StatusQuestionAnswerMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StatusQuestionAnswerMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a StatusQuestionAnswerMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.StatusQuestionAnswerMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.StatusQuestionAnswerMessage} StatusQuestionAnswerMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StatusQuestionAnswerMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.key = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.key);
                            message._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.text = reader.stringVerify();
                            message._text = "text";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a StatusQuestionAnswerMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.StatusQuestionAnswerMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.StatusQuestionAnswerMessage} StatusQuestionAnswerMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StatusQuestionAnswerMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a StatusQuestionAnswerMessage message.
             * @function verify
             * @memberof proto.Message.StatusQuestionAnswerMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StatusQuestionAnswerMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                    properties._key = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.key, q + 1);
                        if (error)
                            return "key." + error;
                    }
                }
                if (message.text != null && Object.hasOwnProperty.call(message, "text")) {
                    properties._text = 1;
                    if (!$util.isString(message.text))
                        return "text: string expected";
                }
                return null;
            };

            /**
             * Creates a StatusQuestionAnswerMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.StatusQuestionAnswerMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.StatusQuestionAnswerMessage} StatusQuestionAnswerMessage
             */
            StatusQuestionAnswerMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.StatusQuestionAnswerMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.key != null) {
                    if (!$util.isObject(object.key))
                        throw TypeError(".proto.Message.StatusQuestionAnswerMessage.key: object expected");
                    message.key = $root.proto.MessageKey.fromObject(object.key, q + 1);
                }
                if (object.text != null)
                    message.text = String(object.text);
                return message;
            };

            /**
             * Creates a plain object from a StatusQuestionAnswerMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.StatusQuestionAnswerMessage
             * @static
             * @param {proto.Message.StatusQuestionAnswerMessage} message StatusQuestionAnswerMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StatusQuestionAnswerMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    object.key = $root.proto.MessageKey.toObject(message.key, options, q + 1);
                if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                    object.text = message.text;
                return object;
            };

            /**
             * Converts this StatusQuestionAnswerMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.StatusQuestionAnswerMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StatusQuestionAnswerMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for StatusQuestionAnswerMessage
             * @function getTypeUrl
             * @memberof proto.Message.StatusQuestionAnswerMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            StatusQuestionAnswerMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.StatusQuestionAnswerMessage";
            };

            return StatusQuestionAnswerMessage;
        })();

        Message.StatusQuotedMessage = (function() {

            /**
             * Properties of a StatusQuotedMessage.
             * @memberof proto.Message
             * @interface IStatusQuotedMessage
             * @property {proto.Message.StatusQuotedMessage.StatusQuotedMessageType|null} [type] StatusQuotedMessage type
             * @property {string|null} [text] StatusQuotedMessage text
             * @property {Uint8Array|null} [thumbnail] StatusQuotedMessage thumbnail
             * @property {proto.IMessageKey|null} [originalStatusId] StatusQuotedMessage originalStatusId
             */

            /**
             * Constructs a new StatusQuotedMessage.
             * @memberof proto.Message
             * @classdesc Represents a StatusQuotedMessage.
             * @implements IStatusQuotedMessage
             * @constructor
             * @param {proto.Message.IStatusQuotedMessage=} [properties] Properties to set
             */
            function StatusQuotedMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * StatusQuotedMessage type.
             * @member {proto.Message.StatusQuotedMessage.StatusQuotedMessageType|null|undefined} type
             * @memberof proto.Message.StatusQuotedMessage
             * @instance
             */
            StatusQuotedMessage.prototype.type = null;

            /**
             * StatusQuotedMessage text.
             * @member {string|null|undefined} text
             * @memberof proto.Message.StatusQuotedMessage
             * @instance
             */
            StatusQuotedMessage.prototype.text = null;

            /**
             * StatusQuotedMessage thumbnail.
             * @member {Uint8Array|null|undefined} thumbnail
             * @memberof proto.Message.StatusQuotedMessage
             * @instance
             */
            StatusQuotedMessage.prototype.thumbnail = null;

            /**
             * StatusQuotedMessage originalStatusId.
             * @member {proto.IMessageKey|null|undefined} originalStatusId
             * @memberof proto.Message.StatusQuotedMessage
             * @instance
             */
            StatusQuotedMessage.prototype.originalStatusId = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusQuotedMessage.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusQuotedMessage.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusQuotedMessage.prototype, "_thumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusQuotedMessage.prototype, "_originalStatusId", {
                get: $util.oneOfGetter($oneOfFields = ["originalStatusId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StatusQuotedMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.StatusQuotedMessage
             * @static
             * @param {proto.Message.IStatusQuotedMessage=} [properties] Properties to set
             * @returns {proto.Message.StatusQuotedMessage} StatusQuotedMessage instance
             */
            StatusQuotedMessage.create = function create(properties) {
                return new StatusQuotedMessage(properties);
            };

            /**
             * Encodes the specified StatusQuotedMessage message. Does not implicitly {@link proto.Message.StatusQuotedMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.StatusQuotedMessage
             * @static
             * @param {proto.Message.IStatusQuotedMessage} message StatusQuotedMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StatusQuotedMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
                if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.text);
                if (message.thumbnail != null && Object.hasOwnProperty.call(message, "thumbnail"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.thumbnail);
                if (message.originalStatusId != null && Object.hasOwnProperty.call(message, "originalStatusId"))
                    $root.proto.MessageKey.encode(message.originalStatusId, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified StatusQuotedMessage message, length delimited. Does not implicitly {@link proto.Message.StatusQuotedMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.StatusQuotedMessage
             * @static
             * @param {proto.Message.IStatusQuotedMessage} message StatusQuotedMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StatusQuotedMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a StatusQuotedMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.StatusQuotedMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.StatusQuotedMessage} StatusQuotedMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StatusQuotedMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.type = reader.int32();
                            message._type = "type";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.text = reader.stringVerify();
                            message._text = "text";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.thumbnail = reader.bytes();
                            message._thumbnail = "thumbnail";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.originalStatusId = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.originalStatusId);
                            message._originalStatusId = "originalStatusId";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a StatusQuotedMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.StatusQuotedMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.StatusQuotedMessage} StatusQuotedMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StatusQuotedMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a StatusQuotedMessage message.
             * @function verify
             * @memberof proto.Message.StatusQuotedMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StatusQuotedMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                    properties._type = 1;
                    if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                        return "type: enum value expected";
                }
                if (message.text != null && Object.hasOwnProperty.call(message, "text")) {
                    properties._text = 1;
                    if (!$util.isString(message.text))
                        return "text: string expected";
                }
                if (message.thumbnail != null && Object.hasOwnProperty.call(message, "thumbnail")) {
                    properties._thumbnail = 1;
                    if (!(message.thumbnail && typeof message.thumbnail.length === "number" || $util.isString(message.thumbnail)))
                        return "thumbnail: buffer expected";
                }
                if (message.originalStatusId != null && Object.hasOwnProperty.call(message, "originalStatusId")) {
                    properties._originalStatusId = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.originalStatusId, q + 1);
                        if (error)
                            return "originalStatusId." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a StatusQuotedMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.StatusQuotedMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.StatusQuotedMessage} StatusQuotedMessage
             */
            StatusQuotedMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.StatusQuotedMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.type) {
                case "QUESTION_ANSWER":
                case 1:
                    message.type = 1;
                    break;
                default:
                    if (typeof object.type === "number" && (object.type | 0) === object.type)
                        message.type = object.type;
                }
                if (object.text != null)
                    message.text = String(object.text);
                if (object.thumbnail != null)
                    if (typeof object.thumbnail === "string")
                        $util.base64.decode(object.thumbnail, message.thumbnail = $util.newBuffer($util.base64.length(object.thumbnail)), 0);
                    else if (object.thumbnail.length >= 0)
                        message.thumbnail = object.thumbnail;
                if (object.originalStatusId != null) {
                    if (!$util.isObject(object.originalStatusId))
                        throw TypeError(".proto.Message.StatusQuotedMessage.originalStatusId: object expected");
                    message.originalStatusId = $root.proto.MessageKey.fromObject(object.originalStatusId, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a StatusQuotedMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.StatusQuotedMessage
             * @static
             * @param {proto.Message.StatusQuotedMessage} message StatusQuotedMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StatusQuotedMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    object.type = options.enums === String ? $root.proto.Message.StatusQuotedMessage.StatusQuotedMessageType[message.type] === undefined ? message.type : $root.proto.Message.StatusQuotedMessage.StatusQuotedMessageType[message.type] : message.type;
                if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                    object.text = message.text;
                if (message.thumbnail != null && Object.hasOwnProperty.call(message, "thumbnail"))
                    object.thumbnail = options.bytes === String ? $util.base64.encode(message.thumbnail, 0, message.thumbnail.length) : options.bytes === Array ? Array.prototype.slice.call(message.thumbnail) : message.thumbnail;
                if (message.originalStatusId != null && Object.hasOwnProperty.call(message, "originalStatusId"))
                    object.originalStatusId = $root.proto.MessageKey.toObject(message.originalStatusId, options, q + 1);
                return object;
            };

            /**
             * Converts this StatusQuotedMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.StatusQuotedMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StatusQuotedMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for StatusQuotedMessage
             * @function getTypeUrl
             * @memberof proto.Message.StatusQuotedMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            StatusQuotedMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.StatusQuotedMessage";
            };

            /**
             * StatusQuotedMessageType enum.
             * @name proto.Message.StatusQuotedMessage.StatusQuotedMessageType
             * @enum {number}
             * @property {number} QUESTION_ANSWER=1 QUESTION_ANSWER value
             */
            StatusQuotedMessage.StatusQuotedMessageType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[1] = "QUESTION_ANSWER"] = 1;
                return values;
            })();

            return StatusQuotedMessage;
        })();

        Message.StatusStickerInteractionMessage = (function() {

            /**
             * Properties of a StatusStickerInteractionMessage.
             * @memberof proto.Message
             * @interface IStatusStickerInteractionMessage
             * @property {proto.IMessageKey|null} [key] StatusStickerInteractionMessage key
             * @property {string|null} [stickerKey] StatusStickerInteractionMessage stickerKey
             * @property {proto.Message.StatusStickerInteractionMessage.StatusStickerType|null} [type] StatusStickerInteractionMessage type
             */

            /**
             * Constructs a new StatusStickerInteractionMessage.
             * @memberof proto.Message
             * @classdesc Represents a StatusStickerInteractionMessage.
             * @implements IStatusStickerInteractionMessage
             * @constructor
             * @param {proto.Message.IStatusStickerInteractionMessage=} [properties] Properties to set
             */
            function StatusStickerInteractionMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * StatusStickerInteractionMessage key.
             * @member {proto.IMessageKey|null|undefined} key
             * @memberof proto.Message.StatusStickerInteractionMessage
             * @instance
             */
            StatusStickerInteractionMessage.prototype.key = null;

            /**
             * StatusStickerInteractionMessage stickerKey.
             * @member {string|null|undefined} stickerKey
             * @memberof proto.Message.StatusStickerInteractionMessage
             * @instance
             */
            StatusStickerInteractionMessage.prototype.stickerKey = null;

            /**
             * StatusStickerInteractionMessage type.
             * @member {proto.Message.StatusStickerInteractionMessage.StatusStickerType|null|undefined} type
             * @memberof proto.Message.StatusStickerInteractionMessage
             * @instance
             */
            StatusStickerInteractionMessage.prototype.type = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusStickerInteractionMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusStickerInteractionMessage.prototype, "_stickerKey", {
                get: $util.oneOfGetter($oneOfFields = ["stickerKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusStickerInteractionMessage.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StatusStickerInteractionMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.StatusStickerInteractionMessage
             * @static
             * @param {proto.Message.IStatusStickerInteractionMessage=} [properties] Properties to set
             * @returns {proto.Message.StatusStickerInteractionMessage} StatusStickerInteractionMessage instance
             */
            StatusStickerInteractionMessage.create = function create(properties) {
                return new StatusStickerInteractionMessage(properties);
            };

            /**
             * Encodes the specified StatusStickerInteractionMessage message. Does not implicitly {@link proto.Message.StatusStickerInteractionMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.StatusStickerInteractionMessage
             * @static
             * @param {proto.Message.IStatusStickerInteractionMessage} message StatusStickerInteractionMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StatusStickerInteractionMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    $root.proto.MessageKey.encode(message.key, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.stickerKey != null && Object.hasOwnProperty.call(message, "stickerKey"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.stickerKey);
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.type);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified StatusStickerInteractionMessage message, length delimited. Does not implicitly {@link proto.Message.StatusStickerInteractionMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.StatusStickerInteractionMessage
             * @static
             * @param {proto.Message.IStatusStickerInteractionMessage} message StatusStickerInteractionMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StatusStickerInteractionMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a StatusStickerInteractionMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.StatusStickerInteractionMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.StatusStickerInteractionMessage} StatusStickerInteractionMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StatusStickerInteractionMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.key = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.key);
                            message._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.stickerKey = reader.stringVerify();
                            message._stickerKey = "stickerKey";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.type = reader.int32();
                            message._type = "type";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a StatusStickerInteractionMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.StatusStickerInteractionMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.StatusStickerInteractionMessage} StatusStickerInteractionMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StatusStickerInteractionMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a StatusStickerInteractionMessage message.
             * @function verify
             * @memberof proto.Message.StatusStickerInteractionMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StatusStickerInteractionMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                    properties._key = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.key, q + 1);
                        if (error)
                            return "key." + error;
                    }
                }
                if (message.stickerKey != null && Object.hasOwnProperty.call(message, "stickerKey")) {
                    properties._stickerKey = 1;
                    if (!$util.isString(message.stickerKey))
                        return "stickerKey: string expected";
                }
                if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                    properties._type = 1;
                    if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                        return "type: enum value expected";
                }
                return null;
            };

            /**
             * Creates a StatusStickerInteractionMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.StatusStickerInteractionMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.StatusStickerInteractionMessage} StatusStickerInteractionMessage
             */
            StatusStickerInteractionMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.StatusStickerInteractionMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.key != null) {
                    if (!$util.isObject(object.key))
                        throw TypeError(".proto.Message.StatusStickerInteractionMessage.key: object expected");
                    message.key = $root.proto.MessageKey.fromObject(object.key, q + 1);
                }
                if (object.stickerKey != null)
                    message.stickerKey = String(object.stickerKey);
                switch (object.type) {
                case "UNKNOWN":
                case 0:
                    message.type = 0;
                    break;
                case "REACTION":
                case 1:
                    message.type = 1;
                    break;
                default:
                    if (typeof object.type === "number" && (object.type | 0) === object.type)
                        message.type = object.type;
                }
                return message;
            };

            /**
             * Creates a plain object from a StatusStickerInteractionMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.StatusStickerInteractionMessage
             * @static
             * @param {proto.Message.StatusStickerInteractionMessage} message StatusStickerInteractionMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StatusStickerInteractionMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    object.key = $root.proto.MessageKey.toObject(message.key, options, q + 1);
                if (message.stickerKey != null && Object.hasOwnProperty.call(message, "stickerKey"))
                    object.stickerKey = message.stickerKey;
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    object.type = options.enums === String ? $root.proto.Message.StatusStickerInteractionMessage.StatusStickerType[message.type] === undefined ? message.type : $root.proto.Message.StatusStickerInteractionMessage.StatusStickerType[message.type] : message.type;
                return object;
            };

            /**
             * Converts this StatusStickerInteractionMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.StatusStickerInteractionMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StatusStickerInteractionMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for StatusStickerInteractionMessage
             * @function getTypeUrl
             * @memberof proto.Message.StatusStickerInteractionMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            StatusStickerInteractionMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.StatusStickerInteractionMessage";
            };

            /**
             * StatusStickerType enum.
             * @name proto.Message.StatusStickerInteractionMessage.StatusStickerType
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} REACTION=1 REACTION value
             */
            StatusStickerInteractionMessage.StatusStickerType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "REACTION"] = 1;
                return values;
            })();

            return StatusStickerInteractionMessage;
        })();

        Message.StickerMessage = (function() {

            /**
             * Properties of a StickerMessage.
             * @memberof proto.Message
             * @interface IStickerMessage
             * @property {string|null} [url] StickerMessage url
             * @property {Uint8Array|null} [fileSha256] StickerMessage fileSha256
             * @property {Uint8Array|null} [fileEncSha256] StickerMessage fileEncSha256
             * @property {Uint8Array|null} [mediaKey] StickerMessage mediaKey
             * @property {string|null} [mimetype] StickerMessage mimetype
             * @property {number|null} [height] StickerMessage height
             * @property {number|null} [width] StickerMessage width
             * @property {string|null} [directPath] StickerMessage directPath
             * @property {number|Long|null} [fileLength] StickerMessage fileLength
             * @property {number|Long|null} [mediaKeyTimestamp] StickerMessage mediaKeyTimestamp
             * @property {number|null} [firstFrameLength] StickerMessage firstFrameLength
             * @property {Uint8Array|null} [firstFrameSidecar] StickerMessage firstFrameSidecar
             * @property {boolean|null} [isAnimated] StickerMessage isAnimated
             * @property {Uint8Array|null} [pngThumbnail] StickerMessage pngThumbnail
             * @property {proto.IContextInfo|null} [contextInfo] StickerMessage contextInfo
             * @property {number|Long|null} [stickerSentTs] StickerMessage stickerSentTs
             * @property {boolean|null} [isAvatar] StickerMessage isAvatar
             * @property {boolean|null} [isAiSticker] StickerMessage isAiSticker
             * @property {boolean|null} [isLottie] StickerMessage isLottie
             * @property {string|null} [accessibilityLabel] StickerMessage accessibilityLabel
             * @property {number|null} [premium] StickerMessage premium
             * @property {string|null} [emojis] StickerMessage emojis
             */

            /**
             * Constructs a new StickerMessage.
             * @memberof proto.Message
             * @classdesc Represents a StickerMessage.
             * @implements IStickerMessage
             * @constructor
             * @param {proto.Message.IStickerMessage=} [properties] Properties to set
             */
            function StickerMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * StickerMessage url.
             * @member {string|null|undefined} url
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.url = null;

            /**
             * StickerMessage fileSha256.
             * @member {Uint8Array|null|undefined} fileSha256
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.fileSha256 = null;

            /**
             * StickerMessage fileEncSha256.
             * @member {Uint8Array|null|undefined} fileEncSha256
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.fileEncSha256 = null;

            /**
             * StickerMessage mediaKey.
             * @member {Uint8Array|null|undefined} mediaKey
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.mediaKey = null;

            /**
             * StickerMessage mimetype.
             * @member {string|null|undefined} mimetype
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.mimetype = null;

            /**
             * StickerMessage height.
             * @member {number|null|undefined} height
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.height = null;

            /**
             * StickerMessage width.
             * @member {number|null|undefined} width
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.width = null;

            /**
             * StickerMessage directPath.
             * @member {string|null|undefined} directPath
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.directPath = null;

            /**
             * StickerMessage fileLength.
             * @member {number|Long|null|undefined} fileLength
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.fileLength = null;

            /**
             * StickerMessage mediaKeyTimestamp.
             * @member {number|Long|null|undefined} mediaKeyTimestamp
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.mediaKeyTimestamp = null;

            /**
             * StickerMessage firstFrameLength.
             * @member {number|null|undefined} firstFrameLength
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.firstFrameLength = null;

            /**
             * StickerMessage firstFrameSidecar.
             * @member {Uint8Array|null|undefined} firstFrameSidecar
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.firstFrameSidecar = null;

            /**
             * StickerMessage isAnimated.
             * @member {boolean|null|undefined} isAnimated
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.isAnimated = null;

            /**
             * StickerMessage pngThumbnail.
             * @member {Uint8Array|null|undefined} pngThumbnail
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.pngThumbnail = null;

            /**
             * StickerMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.contextInfo = null;

            /**
             * StickerMessage stickerSentTs.
             * @member {number|Long|null|undefined} stickerSentTs
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.stickerSentTs = null;

            /**
             * StickerMessage isAvatar.
             * @member {boolean|null|undefined} isAvatar
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.isAvatar = null;

            /**
             * StickerMessage isAiSticker.
             * @member {boolean|null|undefined} isAiSticker
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.isAiSticker = null;

            /**
             * StickerMessage isLottie.
             * @member {boolean|null|undefined} isLottie
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.isLottie = null;

            /**
             * StickerMessage accessibilityLabel.
             * @member {string|null|undefined} accessibilityLabel
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.accessibilityLabel = null;

            /**
             * StickerMessage premium.
             * @member {number|null|undefined} premium
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.premium = null;

            /**
             * StickerMessage emojis.
             * @member {string|null|undefined} emojis
             * @memberof proto.Message.StickerMessage
             * @instance
             */
            StickerMessage.prototype.emojis = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_fileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_mimetype", {
                get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_height", {
                get: $util.oneOfGetter($oneOfFields = ["height"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_width", {
                get: $util.oneOfGetter($oneOfFields = ["width"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_fileLength", {
                get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_firstFrameLength", {
                get: $util.oneOfGetter($oneOfFields = ["firstFrameLength"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_firstFrameSidecar", {
                get: $util.oneOfGetter($oneOfFields = ["firstFrameSidecar"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_isAnimated", {
                get: $util.oneOfGetter($oneOfFields = ["isAnimated"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_pngThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["pngThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_stickerSentTs", {
                get: $util.oneOfGetter($oneOfFields = ["stickerSentTs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_isAvatar", {
                get: $util.oneOfGetter($oneOfFields = ["isAvatar"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_isAiSticker", {
                get: $util.oneOfGetter($oneOfFields = ["isAiSticker"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_isLottie", {
                get: $util.oneOfGetter($oneOfFields = ["isLottie"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_accessibilityLabel", {
                get: $util.oneOfGetter($oneOfFields = ["accessibilityLabel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_premium", {
                get: $util.oneOfGetter($oneOfFields = ["premium"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerMessage.prototype, "_emojis", {
                get: $util.oneOfGetter($oneOfFields = ["emojis"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StickerMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.StickerMessage
             * @static
             * @param {proto.Message.IStickerMessage=} [properties] Properties to set
             * @returns {proto.Message.StickerMessage} StickerMessage instance
             */
            StickerMessage.create = function create(properties) {
                return new StickerMessage(properties);
            };

            /**
             * Encodes the specified StickerMessage message. Does not implicitly {@link proto.Message.StickerMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.StickerMessage
             * @static
             * @param {proto.Message.IStickerMessage} message StickerMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StickerMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.url);
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.fileSha256);
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.fileEncSha256);
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.mediaKey);
                if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.mimetype);
                if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                    writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.height);
                if (message.width != null && Object.hasOwnProperty.call(message, "width"))
                    writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.width);
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.directPath);
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                    writer.uint32(/* id 9, wireType 0 =*/72).uint64(message.fileLength);
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                    writer.uint32(/* id 10, wireType 0 =*/80).int64(message.mediaKeyTimestamp);
                if (message.firstFrameLength != null && Object.hasOwnProperty.call(message, "firstFrameLength"))
                    writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.firstFrameLength);
                if (message.firstFrameSidecar != null && Object.hasOwnProperty.call(message, "firstFrameSidecar"))
                    writer.uint32(/* id 12, wireType 2 =*/98).bytes(message.firstFrameSidecar);
                if (message.isAnimated != null && Object.hasOwnProperty.call(message, "isAnimated"))
                    writer.uint32(/* id 13, wireType 0 =*/104).bool(message.isAnimated);
                if (message.pngThumbnail != null && Object.hasOwnProperty.call(message, "pngThumbnail"))
                    writer.uint32(/* id 16, wireType 2 =*/130).bytes(message.pngThumbnail);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
                if (message.stickerSentTs != null && Object.hasOwnProperty.call(message, "stickerSentTs"))
                    writer.uint32(/* id 18, wireType 0 =*/144).int64(message.stickerSentTs);
                if (message.isAvatar != null && Object.hasOwnProperty.call(message, "isAvatar"))
                    writer.uint32(/* id 19, wireType 0 =*/152).bool(message.isAvatar);
                if (message.isAiSticker != null && Object.hasOwnProperty.call(message, "isAiSticker"))
                    writer.uint32(/* id 20, wireType 0 =*/160).bool(message.isAiSticker);
                if (message.isLottie != null && Object.hasOwnProperty.call(message, "isLottie"))
                    writer.uint32(/* id 21, wireType 0 =*/168).bool(message.isLottie);
                if (message.accessibilityLabel != null && Object.hasOwnProperty.call(message, "accessibilityLabel"))
                    writer.uint32(/* id 22, wireType 2 =*/178).string(message.accessibilityLabel);
                if (message.premium != null && Object.hasOwnProperty.call(message, "premium"))
                    writer.uint32(/* id 24, wireType 0 =*/192).int32(message.premium);
                if (message.emojis != null && Object.hasOwnProperty.call(message, "emojis"))
                    writer.uint32(/* id 25, wireType 2 =*/202).string(message.emojis);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified StickerMessage message, length delimited. Does not implicitly {@link proto.Message.StickerMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.StickerMessage
             * @static
             * @param {proto.Message.IStickerMessage} message StickerMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StickerMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a StickerMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.StickerMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.StickerMessage} StickerMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StickerMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.url = reader.stringVerify();
                            message._url = "url";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.fileSha256 = reader.bytes();
                            message._fileSha256 = "fileSha256";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.fileEncSha256 = reader.bytes();
                            message._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.mediaKey = reader.bytes();
                            message._mediaKey = "mediaKey";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.mimetype = reader.stringVerify();
                            message._mimetype = "mimetype";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.height = reader.uint32();
                            message._height = "height";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            message.width = reader.uint32();
                            message._width = "width";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.directPath = reader.stringVerify();
                            message._directPath = "directPath";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            message.fileLength = reader.uint64();
                            message._fileLength = "fileLength";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            message.mediaKeyTimestamp = reader.int64();
                            message._mediaKeyTimestamp = "mediaKeyTimestamp";
                            continue;
                        }
                    case 11: {
                            if (u !== 0)
                                break;
                            message.firstFrameLength = reader.uint32();
                            message._firstFrameLength = "firstFrameLength";
                            continue;
                        }
                    case 12: {
                            if (u !== 2)
                                break;
                            message.firstFrameSidecar = reader.bytes();
                            message._firstFrameSidecar = "firstFrameSidecar";
                            continue;
                        }
                    case 13: {
                            if (u !== 0)
                                break;
                            message.isAnimated = reader.bool();
                            message._isAnimated = "isAnimated";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            message.pngThumbnail = reader.bytes();
                            message._pngThumbnail = "pngThumbnail";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 18: {
                            if (u !== 0)
                                break;
                            message.stickerSentTs = reader.int64();
                            message._stickerSentTs = "stickerSentTs";
                            continue;
                        }
                    case 19: {
                            if (u !== 0)
                                break;
                            message.isAvatar = reader.bool();
                            message._isAvatar = "isAvatar";
                            continue;
                        }
                    case 20: {
                            if (u !== 0)
                                break;
                            message.isAiSticker = reader.bool();
                            message._isAiSticker = "isAiSticker";
                            continue;
                        }
                    case 21: {
                            if (u !== 0)
                                break;
                            message.isLottie = reader.bool();
                            message._isLottie = "isLottie";
                            continue;
                        }
                    case 22: {
                            if (u !== 2)
                                break;
                            message.accessibilityLabel = reader.stringVerify();
                            message._accessibilityLabel = "accessibilityLabel";
                            continue;
                        }
                    case 24: {
                            if (u !== 0)
                                break;
                            message.premium = reader.int32();
                            message._premium = "premium";
                            continue;
                        }
                    case 25: {
                            if (u !== 2)
                                break;
                            message.emojis = reader.stringVerify();
                            message._emojis = "emojis";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a StickerMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.StickerMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.StickerMessage} StickerMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StickerMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a StickerMessage message.
             * @function verify
             * @memberof proto.Message.StickerMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StickerMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.url != null && Object.hasOwnProperty.call(message, "url")) {
                    properties._url = 1;
                    if (!$util.isString(message.url))
                        return "url: string expected";
                }
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256")) {
                    properties._fileSha256 = 1;
                    if (!(message.fileSha256 && typeof message.fileSha256.length === "number" || $util.isString(message.fileSha256)))
                        return "fileSha256: buffer expected";
                }
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256")) {
                    properties._fileEncSha256 = 1;
                    if (!(message.fileEncSha256 && typeof message.fileEncSha256.length === "number" || $util.isString(message.fileEncSha256)))
                        return "fileEncSha256: buffer expected";
                }
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey")) {
                    properties._mediaKey = 1;
                    if (!(message.mediaKey && typeof message.mediaKey.length === "number" || $util.isString(message.mediaKey)))
                        return "mediaKey: buffer expected";
                }
                if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype")) {
                    properties._mimetype = 1;
                    if (!$util.isString(message.mimetype))
                        return "mimetype: string expected";
                }
                if (message.height != null && Object.hasOwnProperty.call(message, "height")) {
                    properties._height = 1;
                    if (!$util.isInteger(message.height))
                        return "height: integer expected";
                }
                if (message.width != null && Object.hasOwnProperty.call(message, "width")) {
                    properties._width = 1;
                    if (!$util.isInteger(message.width))
                        return "width: integer expected";
                }
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath")) {
                    properties._directPath = 1;
                    if (!$util.isString(message.directPath))
                        return "directPath: string expected";
                }
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength")) {
                    properties._fileLength = 1;
                    if (!$util.isInteger(message.fileLength) && !(message.fileLength && $util.isInteger(message.fileLength.low) && $util.isInteger(message.fileLength.high)))
                        return "fileLength: integer|Long expected";
                }
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp")) {
                    properties._mediaKeyTimestamp = 1;
                    if (!$util.isInteger(message.mediaKeyTimestamp) && !(message.mediaKeyTimestamp && $util.isInteger(message.mediaKeyTimestamp.low) && $util.isInteger(message.mediaKeyTimestamp.high)))
                        return "mediaKeyTimestamp: integer|Long expected";
                }
                if (message.firstFrameLength != null && Object.hasOwnProperty.call(message, "firstFrameLength")) {
                    properties._firstFrameLength = 1;
                    if (!$util.isInteger(message.firstFrameLength))
                        return "firstFrameLength: integer expected";
                }
                if (message.firstFrameSidecar != null && Object.hasOwnProperty.call(message, "firstFrameSidecar")) {
                    properties._firstFrameSidecar = 1;
                    if (!(message.firstFrameSidecar && typeof message.firstFrameSidecar.length === "number" || $util.isString(message.firstFrameSidecar)))
                        return "firstFrameSidecar: buffer expected";
                }
                if (message.isAnimated != null && Object.hasOwnProperty.call(message, "isAnimated")) {
                    properties._isAnimated = 1;
                    if (typeof message.isAnimated !== "boolean")
                        return "isAnimated: boolean expected";
                }
                if (message.pngThumbnail != null && Object.hasOwnProperty.call(message, "pngThumbnail")) {
                    properties._pngThumbnail = 1;
                    if (!(message.pngThumbnail && typeof message.pngThumbnail.length === "number" || $util.isString(message.pngThumbnail)))
                        return "pngThumbnail: buffer expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.stickerSentTs != null && Object.hasOwnProperty.call(message, "stickerSentTs")) {
                    properties._stickerSentTs = 1;
                    if (!$util.isInteger(message.stickerSentTs) && !(message.stickerSentTs && $util.isInteger(message.stickerSentTs.low) && $util.isInteger(message.stickerSentTs.high)))
                        return "stickerSentTs: integer|Long expected";
                }
                if (message.isAvatar != null && Object.hasOwnProperty.call(message, "isAvatar")) {
                    properties._isAvatar = 1;
                    if (typeof message.isAvatar !== "boolean")
                        return "isAvatar: boolean expected";
                }
                if (message.isAiSticker != null && Object.hasOwnProperty.call(message, "isAiSticker")) {
                    properties._isAiSticker = 1;
                    if (typeof message.isAiSticker !== "boolean")
                        return "isAiSticker: boolean expected";
                }
                if (message.isLottie != null && Object.hasOwnProperty.call(message, "isLottie")) {
                    properties._isLottie = 1;
                    if (typeof message.isLottie !== "boolean")
                        return "isLottie: boolean expected";
                }
                if (message.accessibilityLabel != null && Object.hasOwnProperty.call(message, "accessibilityLabel")) {
                    properties._accessibilityLabel = 1;
                    if (!$util.isString(message.accessibilityLabel))
                        return "accessibilityLabel: string expected";
                }
                if (message.premium != null && Object.hasOwnProperty.call(message, "premium")) {
                    properties._premium = 1;
                    if (!$util.isInteger(message.premium))
                        return "premium: integer expected";
                }
                if (message.emojis != null && Object.hasOwnProperty.call(message, "emojis")) {
                    properties._emojis = 1;
                    if (!$util.isString(message.emojis))
                        return "emojis: string expected";
                }
                return null;
            };

            /**
             * Creates a StickerMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.StickerMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.StickerMessage} StickerMessage
             */
            StickerMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.StickerMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.url != null)
                    message.url = String(object.url);
                if (object.fileSha256 != null)
                    if (typeof object.fileSha256 === "string")
                        $util.base64.decode(object.fileSha256, message.fileSha256 = $util.newBuffer($util.base64.length(object.fileSha256)), 0);
                    else if (object.fileSha256.length >= 0)
                        message.fileSha256 = object.fileSha256;
                if (object.fileEncSha256 != null)
                    if (typeof object.fileEncSha256 === "string")
                        $util.base64.decode(object.fileEncSha256, message.fileEncSha256 = $util.newBuffer($util.base64.length(object.fileEncSha256)), 0);
                    else if (object.fileEncSha256.length >= 0)
                        message.fileEncSha256 = object.fileEncSha256;
                if (object.mediaKey != null)
                    if (typeof object.mediaKey === "string")
                        $util.base64.decode(object.mediaKey, message.mediaKey = $util.newBuffer($util.base64.length(object.mediaKey)), 0);
                    else if (object.mediaKey.length >= 0)
                        message.mediaKey = object.mediaKey;
                if (object.mimetype != null)
                    message.mimetype = String(object.mimetype);
                if (object.height != null)
                    message.height = object.height >>> 0;
                if (object.width != null)
                    message.width = object.width >>> 0;
                if (object.directPath != null)
                    message.directPath = String(object.directPath);
                if (object.fileLength != null)
                    if ($util.Long)
                        message.fileLength = $util.Long.fromValue(object.fileLength, true);
                    else if (typeof object.fileLength === "string")
                        message.fileLength = parseInt(object.fileLength, 10);
                    else if (typeof object.fileLength === "number")
                        message.fileLength = object.fileLength;
                    else if (typeof object.fileLength === "object")
                        message.fileLength = new $util.LongBits(object.fileLength.low >>> 0, object.fileLength.high >>> 0).toNumber(true);
                if (object.mediaKeyTimestamp != null)
                    if ($util.Long)
                        message.mediaKeyTimestamp = $util.Long.fromValue(object.mediaKeyTimestamp, false);
                    else if (typeof object.mediaKeyTimestamp === "string")
                        message.mediaKeyTimestamp = parseInt(object.mediaKeyTimestamp, 10);
                    else if (typeof object.mediaKeyTimestamp === "number")
                        message.mediaKeyTimestamp = object.mediaKeyTimestamp;
                    else if (typeof object.mediaKeyTimestamp === "object")
                        message.mediaKeyTimestamp = new $util.LongBits(object.mediaKeyTimestamp.low >>> 0, object.mediaKeyTimestamp.high >>> 0).toNumber();
                if (object.firstFrameLength != null)
                    message.firstFrameLength = object.firstFrameLength >>> 0;
                if (object.firstFrameSidecar != null)
                    if (typeof object.firstFrameSidecar === "string")
                        $util.base64.decode(object.firstFrameSidecar, message.firstFrameSidecar = $util.newBuffer($util.base64.length(object.firstFrameSidecar)), 0);
                    else if (object.firstFrameSidecar.length >= 0)
                        message.firstFrameSidecar = object.firstFrameSidecar;
                if (object.isAnimated != null)
                    message.isAnimated = Boolean(object.isAnimated);
                if (object.pngThumbnail != null)
                    if (typeof object.pngThumbnail === "string")
                        $util.base64.decode(object.pngThumbnail, message.pngThumbnail = $util.newBuffer($util.base64.length(object.pngThumbnail)), 0);
                    else if (object.pngThumbnail.length >= 0)
                        message.pngThumbnail = object.pngThumbnail;
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.StickerMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                if (object.stickerSentTs != null)
                    if ($util.Long)
                        message.stickerSentTs = $util.Long.fromValue(object.stickerSentTs, false);
                    else if (typeof object.stickerSentTs === "string")
                        message.stickerSentTs = parseInt(object.stickerSentTs, 10);
                    else if (typeof object.stickerSentTs === "number")
                        message.stickerSentTs = object.stickerSentTs;
                    else if (typeof object.stickerSentTs === "object")
                        message.stickerSentTs = new $util.LongBits(object.stickerSentTs.low >>> 0, object.stickerSentTs.high >>> 0).toNumber();
                if (object.isAvatar != null)
                    message.isAvatar = Boolean(object.isAvatar);
                if (object.isAiSticker != null)
                    message.isAiSticker = Boolean(object.isAiSticker);
                if (object.isLottie != null)
                    message.isLottie = Boolean(object.isLottie);
                if (object.accessibilityLabel != null)
                    message.accessibilityLabel = String(object.accessibilityLabel);
                if (object.premium != null)
                    message.premium = object.premium | 0;
                if (object.emojis != null)
                    message.emojis = String(object.emojis);
                return message;
            };

            /**
             * Creates a plain object from a StickerMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.StickerMessage
             * @static
             * @param {proto.Message.StickerMessage} message StickerMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StickerMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    object.url = message.url;
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                    object.fileSha256 = options.bytes === String ? $util.base64.encode(message.fileSha256, 0, message.fileSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileSha256) : message.fileSha256;
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    object.fileEncSha256 = options.bytes === String ? $util.base64.encode(message.fileEncSha256, 0, message.fileEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileEncSha256) : message.fileEncSha256;
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    object.mediaKey = options.bytes === String ? $util.base64.encode(message.mediaKey, 0, message.mediaKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.mediaKey) : message.mediaKey;
                if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                    object.mimetype = message.mimetype;
                if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                    object.height = message.height;
                if (message.width != null && Object.hasOwnProperty.call(message, "width"))
                    object.width = message.width;
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    object.directPath = message.directPath;
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.fileLength = typeof message.fileLength === "number" ? BigInt(message.fileLength) : $util.Long.fromBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0, true).toBigInt();
                    else if (typeof message.fileLength === "number")
                        object.fileLength = options.longs === String ? String(message.fileLength) : message.fileLength;
                    else
                        object.fileLength = options.longs === String ? $util.Long.prototype.toString.call(message.fileLength) : options.longs === Number ? new $util.LongBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0).toNumber(true) : message.fileLength;
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.mediaKeyTimestamp = typeof message.mediaKeyTimestamp === "number" ? BigInt(message.mediaKeyTimestamp) : $util.Long.fromBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.mediaKeyTimestamp === "number")
                        object.mediaKeyTimestamp = options.longs === String ? String(message.mediaKeyTimestamp) : message.mediaKeyTimestamp;
                    else
                        object.mediaKeyTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.mediaKeyTimestamp) : options.longs === Number ? new $util.LongBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0).toNumber() : message.mediaKeyTimestamp;
                if (message.firstFrameLength != null && Object.hasOwnProperty.call(message, "firstFrameLength"))
                    object.firstFrameLength = message.firstFrameLength;
                if (message.firstFrameSidecar != null && Object.hasOwnProperty.call(message, "firstFrameSidecar"))
                    object.firstFrameSidecar = options.bytes === String ? $util.base64.encode(message.firstFrameSidecar, 0, message.firstFrameSidecar.length) : options.bytes === Array ? Array.prototype.slice.call(message.firstFrameSidecar) : message.firstFrameSidecar;
                if (message.isAnimated != null && Object.hasOwnProperty.call(message, "isAnimated"))
                    object.isAnimated = message.isAnimated;
                if (message.pngThumbnail != null && Object.hasOwnProperty.call(message, "pngThumbnail"))
                    object.pngThumbnail = options.bytes === String ? $util.base64.encode(message.pngThumbnail, 0, message.pngThumbnail.length) : options.bytes === Array ? Array.prototype.slice.call(message.pngThumbnail) : message.pngThumbnail;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.stickerSentTs != null && Object.hasOwnProperty.call(message, "stickerSentTs"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.stickerSentTs = typeof message.stickerSentTs === "number" ? BigInt(message.stickerSentTs) : $util.Long.fromBits(message.stickerSentTs.low >>> 0, message.stickerSentTs.high >>> 0, false).toBigInt();
                    else if (typeof message.stickerSentTs === "number")
                        object.stickerSentTs = options.longs === String ? String(message.stickerSentTs) : message.stickerSentTs;
                    else
                        object.stickerSentTs = options.longs === String ? $util.Long.prototype.toString.call(message.stickerSentTs) : options.longs === Number ? new $util.LongBits(message.stickerSentTs.low >>> 0, message.stickerSentTs.high >>> 0).toNumber() : message.stickerSentTs;
                if (message.isAvatar != null && Object.hasOwnProperty.call(message, "isAvatar"))
                    object.isAvatar = message.isAvatar;
                if (message.isAiSticker != null && Object.hasOwnProperty.call(message, "isAiSticker"))
                    object.isAiSticker = message.isAiSticker;
                if (message.isLottie != null && Object.hasOwnProperty.call(message, "isLottie"))
                    object.isLottie = message.isLottie;
                if (message.accessibilityLabel != null && Object.hasOwnProperty.call(message, "accessibilityLabel"))
                    object.accessibilityLabel = message.accessibilityLabel;
                if (message.premium != null && Object.hasOwnProperty.call(message, "premium"))
                    object.premium = message.premium;
                if (message.emojis != null && Object.hasOwnProperty.call(message, "emojis"))
                    object.emojis = message.emojis;
                return object;
            };

            /**
             * Converts this StickerMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.StickerMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StickerMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for StickerMessage
             * @function getTypeUrl
             * @memberof proto.Message.StickerMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            StickerMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.StickerMessage";
            };

            return StickerMessage;
        })();

        Message.StickerPackMessage = (function() {

            /**
             * Properties of a StickerPackMessage.
             * @memberof proto.Message
             * @interface IStickerPackMessage
             * @property {string|null} [stickerPackId] StickerPackMessage stickerPackId
             * @property {string|null} [name] StickerPackMessage name
             * @property {string|null} [publisher] StickerPackMessage publisher
             * @property {Array.<proto.Message.StickerPackMessage.ISticker>|null} [stickers] StickerPackMessage stickers
             * @property {number|Long|null} [fileLength] StickerPackMessage fileLength
             * @property {Uint8Array|null} [fileSha256] StickerPackMessage fileSha256
             * @property {Uint8Array|null} [fileEncSha256] StickerPackMessage fileEncSha256
             * @property {Uint8Array|null} [mediaKey] StickerPackMessage mediaKey
             * @property {string|null} [directPath] StickerPackMessage directPath
             * @property {string|null} [caption] StickerPackMessage caption
             * @property {proto.IContextInfo|null} [contextInfo] StickerPackMessage contextInfo
             * @property {string|null} [packDescription] StickerPackMessage packDescription
             * @property {number|Long|null} [mediaKeyTimestamp] StickerPackMessage mediaKeyTimestamp
             * @property {string|null} [trayIconFileName] StickerPackMessage trayIconFileName
             * @property {string|null} [thumbnailDirectPath] StickerPackMessage thumbnailDirectPath
             * @property {Uint8Array|null} [thumbnailSha256] StickerPackMessage thumbnailSha256
             * @property {Uint8Array|null} [thumbnailEncSha256] StickerPackMessage thumbnailEncSha256
             * @property {number|null} [thumbnailHeight] StickerPackMessage thumbnailHeight
             * @property {number|null} [thumbnailWidth] StickerPackMessage thumbnailWidth
             * @property {string|null} [imageDataHash] StickerPackMessage imageDataHash
             * @property {number|Long|null} [stickerPackSize] StickerPackMessage stickerPackSize
             * @property {proto.Message.StickerPackMessage.StickerPackOrigin|null} [stickerPackOrigin] StickerPackMessage stickerPackOrigin
             */

            /**
             * Constructs a new StickerPackMessage.
             * @memberof proto.Message
             * @classdesc Represents a StickerPackMessage.
             * @implements IStickerPackMessage
             * @constructor
             * @param {proto.Message.IStickerPackMessage=} [properties] Properties to set
             */
            function StickerPackMessage(properties) {
                this.stickers = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * StickerPackMessage stickerPackId.
             * @member {string|null|undefined} stickerPackId
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.stickerPackId = null;

            /**
             * StickerPackMessage name.
             * @member {string|null|undefined} name
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.name = null;

            /**
             * StickerPackMessage publisher.
             * @member {string|null|undefined} publisher
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.publisher = null;

            /**
             * StickerPackMessage stickers.
             * @member {Array.<proto.Message.StickerPackMessage.ISticker>} stickers
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.stickers = $util.emptyArray;

            /**
             * StickerPackMessage fileLength.
             * @member {number|Long|null|undefined} fileLength
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.fileLength = null;

            /**
             * StickerPackMessage fileSha256.
             * @member {Uint8Array|null|undefined} fileSha256
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.fileSha256 = null;

            /**
             * StickerPackMessage fileEncSha256.
             * @member {Uint8Array|null|undefined} fileEncSha256
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.fileEncSha256 = null;

            /**
             * StickerPackMessage mediaKey.
             * @member {Uint8Array|null|undefined} mediaKey
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.mediaKey = null;

            /**
             * StickerPackMessage directPath.
             * @member {string|null|undefined} directPath
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.directPath = null;

            /**
             * StickerPackMessage caption.
             * @member {string|null|undefined} caption
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.caption = null;

            /**
             * StickerPackMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.contextInfo = null;

            /**
             * StickerPackMessage packDescription.
             * @member {string|null|undefined} packDescription
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.packDescription = null;

            /**
             * StickerPackMessage mediaKeyTimestamp.
             * @member {number|Long|null|undefined} mediaKeyTimestamp
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.mediaKeyTimestamp = null;

            /**
             * StickerPackMessage trayIconFileName.
             * @member {string|null|undefined} trayIconFileName
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.trayIconFileName = null;

            /**
             * StickerPackMessage thumbnailDirectPath.
             * @member {string|null|undefined} thumbnailDirectPath
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.thumbnailDirectPath = null;

            /**
             * StickerPackMessage thumbnailSha256.
             * @member {Uint8Array|null|undefined} thumbnailSha256
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.thumbnailSha256 = null;

            /**
             * StickerPackMessage thumbnailEncSha256.
             * @member {Uint8Array|null|undefined} thumbnailEncSha256
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.thumbnailEncSha256 = null;

            /**
             * StickerPackMessage thumbnailHeight.
             * @member {number|null|undefined} thumbnailHeight
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.thumbnailHeight = null;

            /**
             * StickerPackMessage thumbnailWidth.
             * @member {number|null|undefined} thumbnailWidth
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.thumbnailWidth = null;

            /**
             * StickerPackMessage imageDataHash.
             * @member {string|null|undefined} imageDataHash
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.imageDataHash = null;

            /**
             * StickerPackMessage stickerPackSize.
             * @member {number|Long|null|undefined} stickerPackSize
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.stickerPackSize = null;

            /**
             * StickerPackMessage stickerPackOrigin.
             * @member {proto.Message.StickerPackMessage.StickerPackOrigin|null|undefined} stickerPackOrigin
             * @memberof proto.Message.StickerPackMessage
             * @instance
             */
            StickerPackMessage.prototype.stickerPackOrigin = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerPackMessage.prototype, "_stickerPackId", {
                get: $util.oneOfGetter($oneOfFields = ["stickerPackId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerPackMessage.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerPackMessage.prototype, "_publisher", {
                get: $util.oneOfGetter($oneOfFields = ["publisher"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerPackMessage.prototype, "_fileLength", {
                get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerPackMessage.prototype, "_fileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerPackMessage.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerPackMessage.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerPackMessage.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerPackMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerPackMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerPackMessage.prototype, "_packDescription", {
                get: $util.oneOfGetter($oneOfFields = ["packDescription"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerPackMessage.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerPackMessage.prototype, "_trayIconFileName", {
                get: $util.oneOfGetter($oneOfFields = ["trayIconFileName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerPackMessage.prototype, "_thumbnailDirectPath", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailDirectPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerPackMessage.prototype, "_thumbnailSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerPackMessage.prototype, "_thumbnailEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerPackMessage.prototype, "_thumbnailHeight", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailHeight"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerPackMessage.prototype, "_thumbnailWidth", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailWidth"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerPackMessage.prototype, "_imageDataHash", {
                get: $util.oneOfGetter($oneOfFields = ["imageDataHash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerPackMessage.prototype, "_stickerPackSize", {
                get: $util.oneOfGetter($oneOfFields = ["stickerPackSize"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerPackMessage.prototype, "_stickerPackOrigin", {
                get: $util.oneOfGetter($oneOfFields = ["stickerPackOrigin"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StickerPackMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.StickerPackMessage
             * @static
             * @param {proto.Message.IStickerPackMessage=} [properties] Properties to set
             * @returns {proto.Message.StickerPackMessage} StickerPackMessage instance
             */
            StickerPackMessage.create = function create(properties) {
                return new StickerPackMessage(properties);
            };

            /**
             * Encodes the specified StickerPackMessage message. Does not implicitly {@link proto.Message.StickerPackMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.StickerPackMessage
             * @static
             * @param {proto.Message.IStickerPackMessage} message StickerPackMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StickerPackMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.stickerPackId != null && Object.hasOwnProperty.call(message, "stickerPackId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.stickerPackId);
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
                if (message.publisher != null && Object.hasOwnProperty.call(message, "publisher"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.publisher);
                if (message.stickers != null && message.stickers.length)
                    for (var i = 0; i < message.stickers.length; ++i)
                        $root.proto.Message.StickerPackMessage.Sticker.encode(message.stickers[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                    writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.fileLength);
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                    writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.fileSha256);
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.fileEncSha256);
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.mediaKey);
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.directPath);
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    writer.uint32(/* id 10, wireType 2 =*/82).string(message.caption);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 11, wireType 2 =*/90).fork(), q + 1).ldelim();
                if (message.packDescription != null && Object.hasOwnProperty.call(message, "packDescription"))
                    writer.uint32(/* id 12, wireType 2 =*/98).string(message.packDescription);
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                    writer.uint32(/* id 13, wireType 0 =*/104).int64(message.mediaKeyTimestamp);
                if (message.trayIconFileName != null && Object.hasOwnProperty.call(message, "trayIconFileName"))
                    writer.uint32(/* id 14, wireType 2 =*/114).string(message.trayIconFileName);
                if (message.thumbnailDirectPath != null && Object.hasOwnProperty.call(message, "thumbnailDirectPath"))
                    writer.uint32(/* id 15, wireType 2 =*/122).string(message.thumbnailDirectPath);
                if (message.thumbnailSha256 != null && Object.hasOwnProperty.call(message, "thumbnailSha256"))
                    writer.uint32(/* id 16, wireType 2 =*/130).bytes(message.thumbnailSha256);
                if (message.thumbnailEncSha256 != null && Object.hasOwnProperty.call(message, "thumbnailEncSha256"))
                    writer.uint32(/* id 17, wireType 2 =*/138).bytes(message.thumbnailEncSha256);
                if (message.thumbnailHeight != null && Object.hasOwnProperty.call(message, "thumbnailHeight"))
                    writer.uint32(/* id 18, wireType 0 =*/144).uint32(message.thumbnailHeight);
                if (message.thumbnailWidth != null && Object.hasOwnProperty.call(message, "thumbnailWidth"))
                    writer.uint32(/* id 19, wireType 0 =*/152).uint32(message.thumbnailWidth);
                if (message.imageDataHash != null && Object.hasOwnProperty.call(message, "imageDataHash"))
                    writer.uint32(/* id 20, wireType 2 =*/162).string(message.imageDataHash);
                if (message.stickerPackSize != null && Object.hasOwnProperty.call(message, "stickerPackSize"))
                    writer.uint32(/* id 21, wireType 0 =*/168).uint64(message.stickerPackSize);
                if (message.stickerPackOrigin != null && Object.hasOwnProperty.call(message, "stickerPackOrigin"))
                    writer.uint32(/* id 22, wireType 0 =*/176).int32(message.stickerPackOrigin);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified StickerPackMessage message, length delimited. Does not implicitly {@link proto.Message.StickerPackMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.StickerPackMessage
             * @static
             * @param {proto.Message.IStickerPackMessage} message StickerPackMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StickerPackMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a StickerPackMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.StickerPackMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.StickerPackMessage} StickerPackMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StickerPackMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.stickerPackId = reader.stringVerify();
                            message._stickerPackId = "stickerPackId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.publisher = reader.stringVerify();
                            message._publisher = "publisher";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            if (!(message.stickers && message.stickers.length))
                                message.stickers = [];
                            message.stickers.push($root.proto.Message.StickerPackMessage.Sticker.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.fileLength = reader.uint64();
                            message._fileLength = "fileLength";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.fileSha256 = reader.bytes();
                            message._fileSha256 = "fileSha256";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.fileEncSha256 = reader.bytes();
                            message._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.mediaKey = reader.bytes();
                            message._mediaKey = "mediaKey";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            message.directPath = reader.stringVerify();
                            message._directPath = "directPath";
                            continue;
                        }
                    case 10: {
                            if (u !== 2)
                                break;
                            message.caption = reader.stringVerify();
                            message._caption = "caption";
                            continue;
                        }
                    case 11: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 12: {
                            if (u !== 2)
                                break;
                            message.packDescription = reader.stringVerify();
                            message._packDescription = "packDescription";
                            continue;
                        }
                    case 13: {
                            if (u !== 0)
                                break;
                            message.mediaKeyTimestamp = reader.int64();
                            message._mediaKeyTimestamp = "mediaKeyTimestamp";
                            continue;
                        }
                    case 14: {
                            if (u !== 2)
                                break;
                            message.trayIconFileName = reader.stringVerify();
                            message._trayIconFileName = "trayIconFileName";
                            continue;
                        }
                    case 15: {
                            if (u !== 2)
                                break;
                            message.thumbnailDirectPath = reader.stringVerify();
                            message._thumbnailDirectPath = "thumbnailDirectPath";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            message.thumbnailSha256 = reader.bytes();
                            message._thumbnailSha256 = "thumbnailSha256";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            message.thumbnailEncSha256 = reader.bytes();
                            message._thumbnailEncSha256 = "thumbnailEncSha256";
                            continue;
                        }
                    case 18: {
                            if (u !== 0)
                                break;
                            message.thumbnailHeight = reader.uint32();
                            message._thumbnailHeight = "thumbnailHeight";
                            continue;
                        }
                    case 19: {
                            if (u !== 0)
                                break;
                            message.thumbnailWidth = reader.uint32();
                            message._thumbnailWidth = "thumbnailWidth";
                            continue;
                        }
                    case 20: {
                            if (u !== 2)
                                break;
                            message.imageDataHash = reader.stringVerify();
                            message._imageDataHash = "imageDataHash";
                            continue;
                        }
                    case 21: {
                            if (u !== 0)
                                break;
                            message.stickerPackSize = reader.uint64();
                            message._stickerPackSize = "stickerPackSize";
                            continue;
                        }
                    case 22: {
                            if (u !== 0)
                                break;
                            message.stickerPackOrigin = reader.int32();
                            message._stickerPackOrigin = "stickerPackOrigin";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a StickerPackMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.StickerPackMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.StickerPackMessage} StickerPackMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StickerPackMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a StickerPackMessage message.
             * @function verify
             * @memberof proto.Message.StickerPackMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StickerPackMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.stickerPackId != null && Object.hasOwnProperty.call(message, "stickerPackId")) {
                    properties._stickerPackId = 1;
                    if (!$util.isString(message.stickerPackId))
                        return "stickerPackId: string expected";
                }
                if (message.name != null && Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                if (message.publisher != null && Object.hasOwnProperty.call(message, "publisher")) {
                    properties._publisher = 1;
                    if (!$util.isString(message.publisher))
                        return "publisher: string expected";
                }
                if (message.stickers != null && Object.hasOwnProperty.call(message, "stickers")) {
                    if (!Array.isArray(message.stickers))
                        return "stickers: array expected";
                    for (var i = 0; i < message.stickers.length; ++i) {
                        var error = $root.proto.Message.StickerPackMessage.Sticker.verify(message.stickers[i], q + 1);
                        if (error)
                            return "stickers." + error;
                    }
                }
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength")) {
                    properties._fileLength = 1;
                    if (!$util.isInteger(message.fileLength) && !(message.fileLength && $util.isInteger(message.fileLength.low) && $util.isInteger(message.fileLength.high)))
                        return "fileLength: integer|Long expected";
                }
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256")) {
                    properties._fileSha256 = 1;
                    if (!(message.fileSha256 && typeof message.fileSha256.length === "number" || $util.isString(message.fileSha256)))
                        return "fileSha256: buffer expected";
                }
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256")) {
                    properties._fileEncSha256 = 1;
                    if (!(message.fileEncSha256 && typeof message.fileEncSha256.length === "number" || $util.isString(message.fileEncSha256)))
                        return "fileEncSha256: buffer expected";
                }
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey")) {
                    properties._mediaKey = 1;
                    if (!(message.mediaKey && typeof message.mediaKey.length === "number" || $util.isString(message.mediaKey)))
                        return "mediaKey: buffer expected";
                }
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath")) {
                    properties._directPath = 1;
                    if (!$util.isString(message.directPath))
                        return "directPath: string expected";
                }
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption")) {
                    properties._caption = 1;
                    if (!$util.isString(message.caption))
                        return "caption: string expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.packDescription != null && Object.hasOwnProperty.call(message, "packDescription")) {
                    properties._packDescription = 1;
                    if (!$util.isString(message.packDescription))
                        return "packDescription: string expected";
                }
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp")) {
                    properties._mediaKeyTimestamp = 1;
                    if (!$util.isInteger(message.mediaKeyTimestamp) && !(message.mediaKeyTimestamp && $util.isInteger(message.mediaKeyTimestamp.low) && $util.isInteger(message.mediaKeyTimestamp.high)))
                        return "mediaKeyTimestamp: integer|Long expected";
                }
                if (message.trayIconFileName != null && Object.hasOwnProperty.call(message, "trayIconFileName")) {
                    properties._trayIconFileName = 1;
                    if (!$util.isString(message.trayIconFileName))
                        return "trayIconFileName: string expected";
                }
                if (message.thumbnailDirectPath != null && Object.hasOwnProperty.call(message, "thumbnailDirectPath")) {
                    properties._thumbnailDirectPath = 1;
                    if (!$util.isString(message.thumbnailDirectPath))
                        return "thumbnailDirectPath: string expected";
                }
                if (message.thumbnailSha256 != null && Object.hasOwnProperty.call(message, "thumbnailSha256")) {
                    properties._thumbnailSha256 = 1;
                    if (!(message.thumbnailSha256 && typeof message.thumbnailSha256.length === "number" || $util.isString(message.thumbnailSha256)))
                        return "thumbnailSha256: buffer expected";
                }
                if (message.thumbnailEncSha256 != null && Object.hasOwnProperty.call(message, "thumbnailEncSha256")) {
                    properties._thumbnailEncSha256 = 1;
                    if (!(message.thumbnailEncSha256 && typeof message.thumbnailEncSha256.length === "number" || $util.isString(message.thumbnailEncSha256)))
                        return "thumbnailEncSha256: buffer expected";
                }
                if (message.thumbnailHeight != null && Object.hasOwnProperty.call(message, "thumbnailHeight")) {
                    properties._thumbnailHeight = 1;
                    if (!$util.isInteger(message.thumbnailHeight))
                        return "thumbnailHeight: integer expected";
                }
                if (message.thumbnailWidth != null && Object.hasOwnProperty.call(message, "thumbnailWidth")) {
                    properties._thumbnailWidth = 1;
                    if (!$util.isInteger(message.thumbnailWidth))
                        return "thumbnailWidth: integer expected";
                }
                if (message.imageDataHash != null && Object.hasOwnProperty.call(message, "imageDataHash")) {
                    properties._imageDataHash = 1;
                    if (!$util.isString(message.imageDataHash))
                        return "imageDataHash: string expected";
                }
                if (message.stickerPackSize != null && Object.hasOwnProperty.call(message, "stickerPackSize")) {
                    properties._stickerPackSize = 1;
                    if (!$util.isInteger(message.stickerPackSize) && !(message.stickerPackSize && $util.isInteger(message.stickerPackSize.low) && $util.isInteger(message.stickerPackSize.high)))
                        return "stickerPackSize: integer|Long expected";
                }
                if (message.stickerPackOrigin != null && Object.hasOwnProperty.call(message, "stickerPackOrigin")) {
                    properties._stickerPackOrigin = 1;
                    if (typeof message.stickerPackOrigin !== "number" || (message.stickerPackOrigin | 0) !== message.stickerPackOrigin)
                        return "stickerPackOrigin: enum value expected";
                }
                return null;
            };

            /**
             * Creates a StickerPackMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.StickerPackMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.StickerPackMessage} StickerPackMessage
             */
            StickerPackMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.StickerPackMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.stickerPackId != null)
                    message.stickerPackId = String(object.stickerPackId);
                if (object.name != null)
                    message.name = String(object.name);
                if (object.publisher != null)
                    message.publisher = String(object.publisher);
                if (object.stickers) {
                    if (!Array.isArray(object.stickers))
                        throw TypeError(".proto.Message.StickerPackMessage.stickers: array expected");
                    message.stickers = Array(object.stickers.length);
                    for (var i = 0; i < object.stickers.length; ++i) {
                        if (!$util.isObject(object.stickers[i]))
                            throw TypeError(".proto.Message.StickerPackMessage.stickers: object expected");
                        message.stickers[i] = $root.proto.Message.StickerPackMessage.Sticker.fromObject(object.stickers[i], q + 1);
                    }
                }
                if (object.fileLength != null)
                    if ($util.Long)
                        message.fileLength = $util.Long.fromValue(object.fileLength, true);
                    else if (typeof object.fileLength === "string")
                        message.fileLength = parseInt(object.fileLength, 10);
                    else if (typeof object.fileLength === "number")
                        message.fileLength = object.fileLength;
                    else if (typeof object.fileLength === "object")
                        message.fileLength = new $util.LongBits(object.fileLength.low >>> 0, object.fileLength.high >>> 0).toNumber(true);
                if (object.fileSha256 != null)
                    if (typeof object.fileSha256 === "string")
                        $util.base64.decode(object.fileSha256, message.fileSha256 = $util.newBuffer($util.base64.length(object.fileSha256)), 0);
                    else if (object.fileSha256.length >= 0)
                        message.fileSha256 = object.fileSha256;
                if (object.fileEncSha256 != null)
                    if (typeof object.fileEncSha256 === "string")
                        $util.base64.decode(object.fileEncSha256, message.fileEncSha256 = $util.newBuffer($util.base64.length(object.fileEncSha256)), 0);
                    else if (object.fileEncSha256.length >= 0)
                        message.fileEncSha256 = object.fileEncSha256;
                if (object.mediaKey != null)
                    if (typeof object.mediaKey === "string")
                        $util.base64.decode(object.mediaKey, message.mediaKey = $util.newBuffer($util.base64.length(object.mediaKey)), 0);
                    else if (object.mediaKey.length >= 0)
                        message.mediaKey = object.mediaKey;
                if (object.directPath != null)
                    message.directPath = String(object.directPath);
                if (object.caption != null)
                    message.caption = String(object.caption);
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.StickerPackMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                if (object.packDescription != null)
                    message.packDescription = String(object.packDescription);
                if (object.mediaKeyTimestamp != null)
                    if ($util.Long)
                        message.mediaKeyTimestamp = $util.Long.fromValue(object.mediaKeyTimestamp, false);
                    else if (typeof object.mediaKeyTimestamp === "string")
                        message.mediaKeyTimestamp = parseInt(object.mediaKeyTimestamp, 10);
                    else if (typeof object.mediaKeyTimestamp === "number")
                        message.mediaKeyTimestamp = object.mediaKeyTimestamp;
                    else if (typeof object.mediaKeyTimestamp === "object")
                        message.mediaKeyTimestamp = new $util.LongBits(object.mediaKeyTimestamp.low >>> 0, object.mediaKeyTimestamp.high >>> 0).toNumber();
                if (object.trayIconFileName != null)
                    message.trayIconFileName = String(object.trayIconFileName);
                if (object.thumbnailDirectPath != null)
                    message.thumbnailDirectPath = String(object.thumbnailDirectPath);
                if (object.thumbnailSha256 != null)
                    if (typeof object.thumbnailSha256 === "string")
                        $util.base64.decode(object.thumbnailSha256, message.thumbnailSha256 = $util.newBuffer($util.base64.length(object.thumbnailSha256)), 0);
                    else if (object.thumbnailSha256.length >= 0)
                        message.thumbnailSha256 = object.thumbnailSha256;
                if (object.thumbnailEncSha256 != null)
                    if (typeof object.thumbnailEncSha256 === "string")
                        $util.base64.decode(object.thumbnailEncSha256, message.thumbnailEncSha256 = $util.newBuffer($util.base64.length(object.thumbnailEncSha256)), 0);
                    else if (object.thumbnailEncSha256.length >= 0)
                        message.thumbnailEncSha256 = object.thumbnailEncSha256;
                if (object.thumbnailHeight != null)
                    message.thumbnailHeight = object.thumbnailHeight >>> 0;
                if (object.thumbnailWidth != null)
                    message.thumbnailWidth = object.thumbnailWidth >>> 0;
                if (object.imageDataHash != null)
                    message.imageDataHash = String(object.imageDataHash);
                if (object.stickerPackSize != null)
                    if ($util.Long)
                        message.stickerPackSize = $util.Long.fromValue(object.stickerPackSize, true);
                    else if (typeof object.stickerPackSize === "string")
                        message.stickerPackSize = parseInt(object.stickerPackSize, 10);
                    else if (typeof object.stickerPackSize === "number")
                        message.stickerPackSize = object.stickerPackSize;
                    else if (typeof object.stickerPackSize === "object")
                        message.stickerPackSize = new $util.LongBits(object.stickerPackSize.low >>> 0, object.stickerPackSize.high >>> 0).toNumber(true);
                switch (object.stickerPackOrigin) {
                case "FIRST_PARTY":
                case 0:
                    message.stickerPackOrigin = 0;
                    break;
                case "THIRD_PARTY":
                case 1:
                    message.stickerPackOrigin = 1;
                    break;
                case "USER_CREATED":
                case 2:
                    message.stickerPackOrigin = 2;
                    break;
                default:
                    if (typeof object.stickerPackOrigin === "number" && (object.stickerPackOrigin | 0) === object.stickerPackOrigin)
                        message.stickerPackOrigin = object.stickerPackOrigin;
                }
                return message;
            };

            /**
             * Creates a plain object from a StickerPackMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.StickerPackMessage
             * @static
             * @param {proto.Message.StickerPackMessage} message StickerPackMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StickerPackMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.stickers = [];
                if (message.stickerPackId != null && Object.hasOwnProperty.call(message, "stickerPackId"))
                    object.stickerPackId = message.stickerPackId;
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.publisher != null && Object.hasOwnProperty.call(message, "publisher"))
                    object.publisher = message.publisher;
                if (message.stickers && message.stickers.length) {
                    object.stickers = Array(message.stickers.length);
                    for (var j = 0; j < message.stickers.length; ++j)
                        object.stickers[j] = $root.proto.Message.StickerPackMessage.Sticker.toObject(message.stickers[j], options, q + 1);
                }
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.fileLength = typeof message.fileLength === "number" ? BigInt(message.fileLength) : $util.Long.fromBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0, true).toBigInt();
                    else if (typeof message.fileLength === "number")
                        object.fileLength = options.longs === String ? String(message.fileLength) : message.fileLength;
                    else
                        object.fileLength = options.longs === String ? $util.Long.prototype.toString.call(message.fileLength) : options.longs === Number ? new $util.LongBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0).toNumber(true) : message.fileLength;
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                    object.fileSha256 = options.bytes === String ? $util.base64.encode(message.fileSha256, 0, message.fileSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileSha256) : message.fileSha256;
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    object.fileEncSha256 = options.bytes === String ? $util.base64.encode(message.fileEncSha256, 0, message.fileEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileEncSha256) : message.fileEncSha256;
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    object.mediaKey = options.bytes === String ? $util.base64.encode(message.mediaKey, 0, message.mediaKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.mediaKey) : message.mediaKey;
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    object.directPath = message.directPath;
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    object.caption = message.caption;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.packDescription != null && Object.hasOwnProperty.call(message, "packDescription"))
                    object.packDescription = message.packDescription;
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.mediaKeyTimestamp = typeof message.mediaKeyTimestamp === "number" ? BigInt(message.mediaKeyTimestamp) : $util.Long.fromBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.mediaKeyTimestamp === "number")
                        object.mediaKeyTimestamp = options.longs === String ? String(message.mediaKeyTimestamp) : message.mediaKeyTimestamp;
                    else
                        object.mediaKeyTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.mediaKeyTimestamp) : options.longs === Number ? new $util.LongBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0).toNumber() : message.mediaKeyTimestamp;
                if (message.trayIconFileName != null && Object.hasOwnProperty.call(message, "trayIconFileName"))
                    object.trayIconFileName = message.trayIconFileName;
                if (message.thumbnailDirectPath != null && Object.hasOwnProperty.call(message, "thumbnailDirectPath"))
                    object.thumbnailDirectPath = message.thumbnailDirectPath;
                if (message.thumbnailSha256 != null && Object.hasOwnProperty.call(message, "thumbnailSha256"))
                    object.thumbnailSha256 = options.bytes === String ? $util.base64.encode(message.thumbnailSha256, 0, message.thumbnailSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.thumbnailSha256) : message.thumbnailSha256;
                if (message.thumbnailEncSha256 != null && Object.hasOwnProperty.call(message, "thumbnailEncSha256"))
                    object.thumbnailEncSha256 = options.bytes === String ? $util.base64.encode(message.thumbnailEncSha256, 0, message.thumbnailEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.thumbnailEncSha256) : message.thumbnailEncSha256;
                if (message.thumbnailHeight != null && Object.hasOwnProperty.call(message, "thumbnailHeight"))
                    object.thumbnailHeight = message.thumbnailHeight;
                if (message.thumbnailWidth != null && Object.hasOwnProperty.call(message, "thumbnailWidth"))
                    object.thumbnailWidth = message.thumbnailWidth;
                if (message.imageDataHash != null && Object.hasOwnProperty.call(message, "imageDataHash"))
                    object.imageDataHash = message.imageDataHash;
                if (message.stickerPackSize != null && Object.hasOwnProperty.call(message, "stickerPackSize"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.stickerPackSize = typeof message.stickerPackSize === "number" ? BigInt(message.stickerPackSize) : $util.Long.fromBits(message.stickerPackSize.low >>> 0, message.stickerPackSize.high >>> 0, true).toBigInt();
                    else if (typeof message.stickerPackSize === "number")
                        object.stickerPackSize = options.longs === String ? String(message.stickerPackSize) : message.stickerPackSize;
                    else
                        object.stickerPackSize = options.longs === String ? $util.Long.prototype.toString.call(message.stickerPackSize) : options.longs === Number ? new $util.LongBits(message.stickerPackSize.low >>> 0, message.stickerPackSize.high >>> 0).toNumber(true) : message.stickerPackSize;
                if (message.stickerPackOrigin != null && Object.hasOwnProperty.call(message, "stickerPackOrigin"))
                    object.stickerPackOrigin = options.enums === String ? $root.proto.Message.StickerPackMessage.StickerPackOrigin[message.stickerPackOrigin] === undefined ? message.stickerPackOrigin : $root.proto.Message.StickerPackMessage.StickerPackOrigin[message.stickerPackOrigin] : message.stickerPackOrigin;
                return object;
            };

            /**
             * Converts this StickerPackMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.StickerPackMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StickerPackMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for StickerPackMessage
             * @function getTypeUrl
             * @memberof proto.Message.StickerPackMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            StickerPackMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.StickerPackMessage";
            };

            StickerPackMessage.Sticker = (function() {

                /**
                 * Properties of a Sticker.
                 * @memberof proto.Message.StickerPackMessage
                 * @interface ISticker
                 * @property {string|null} [fileName] Sticker fileName
                 * @property {boolean|null} [isAnimated] Sticker isAnimated
                 * @property {Array.<string>|null} [emojis] Sticker emojis
                 * @property {string|null} [accessibilityLabel] Sticker accessibilityLabel
                 * @property {boolean|null} [isLottie] Sticker isLottie
                 * @property {string|null} [mimetype] Sticker mimetype
                 * @property {number|null} [premium] Sticker premium
                 */

                /**
                 * Constructs a new Sticker.
                 * @memberof proto.Message.StickerPackMessage
                 * @classdesc Represents a Sticker.
                 * @implements ISticker
                 * @constructor
                 * @param {proto.Message.StickerPackMessage.ISticker=} [properties] Properties to set
                 */
                function Sticker(properties) {
                    this.emojis = [];
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * Sticker fileName.
                 * @member {string|null|undefined} fileName
                 * @memberof proto.Message.StickerPackMessage.Sticker
                 * @instance
                 */
                Sticker.prototype.fileName = null;

                /**
                 * Sticker isAnimated.
                 * @member {boolean|null|undefined} isAnimated
                 * @memberof proto.Message.StickerPackMessage.Sticker
                 * @instance
                 */
                Sticker.prototype.isAnimated = null;

                /**
                 * Sticker emojis.
                 * @member {Array.<string>} emojis
                 * @memberof proto.Message.StickerPackMessage.Sticker
                 * @instance
                 */
                Sticker.prototype.emojis = $util.emptyArray;

                /**
                 * Sticker accessibilityLabel.
                 * @member {string|null|undefined} accessibilityLabel
                 * @memberof proto.Message.StickerPackMessage.Sticker
                 * @instance
                 */
                Sticker.prototype.accessibilityLabel = null;

                /**
                 * Sticker isLottie.
                 * @member {boolean|null|undefined} isLottie
                 * @memberof proto.Message.StickerPackMessage.Sticker
                 * @instance
                 */
                Sticker.prototype.isLottie = null;

                /**
                 * Sticker mimetype.
                 * @member {string|null|undefined} mimetype
                 * @memberof proto.Message.StickerPackMessage.Sticker
                 * @instance
                 */
                Sticker.prototype.mimetype = null;

                /**
                 * Sticker premium.
                 * @member {number|null|undefined} premium
                 * @memberof proto.Message.StickerPackMessage.Sticker
                 * @instance
                 */
                Sticker.prototype.premium = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Sticker.prototype, "_fileName", {
                    get: $util.oneOfGetter($oneOfFields = ["fileName"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Sticker.prototype, "_isAnimated", {
                    get: $util.oneOfGetter($oneOfFields = ["isAnimated"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Sticker.prototype, "_accessibilityLabel", {
                    get: $util.oneOfGetter($oneOfFields = ["accessibilityLabel"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Sticker.prototype, "_isLottie", {
                    get: $util.oneOfGetter($oneOfFields = ["isLottie"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Sticker.prototype, "_mimetype", {
                    get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Sticker.prototype, "_premium", {
                    get: $util.oneOfGetter($oneOfFields = ["premium"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new Sticker instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.StickerPackMessage.Sticker
                 * @static
                 * @param {proto.Message.StickerPackMessage.ISticker=} [properties] Properties to set
                 * @returns {proto.Message.StickerPackMessage.Sticker} Sticker instance
                 */
                Sticker.create = function create(properties) {
                    return new Sticker(properties);
                };

                /**
                 * Encodes the specified Sticker message. Does not implicitly {@link proto.Message.StickerPackMessage.Sticker.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.StickerPackMessage.Sticker
                 * @static
                 * @param {proto.Message.StickerPackMessage.ISticker} message Sticker message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Sticker.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.fileName);
                    if (message.isAnimated != null && Object.hasOwnProperty.call(message, "isAnimated"))
                        writer.uint32(/* id 2, wireType 0 =*/16).bool(message.isAnimated);
                    if (message.emojis != null && message.emojis.length)
                        for (var i = 0; i < message.emojis.length; ++i)
                            writer.uint32(/* id 3, wireType 2 =*/26).string(message.emojis[i]);
                    if (message.accessibilityLabel != null && Object.hasOwnProperty.call(message, "accessibilityLabel"))
                        writer.uint32(/* id 4, wireType 2 =*/34).string(message.accessibilityLabel);
                    if (message.isLottie != null && Object.hasOwnProperty.call(message, "isLottie"))
                        writer.uint32(/* id 5, wireType 0 =*/40).bool(message.isLottie);
                    if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                        writer.uint32(/* id 6, wireType 2 =*/50).string(message.mimetype);
                    if (message.premium != null && Object.hasOwnProperty.call(message, "premium"))
                        writer.uint32(/* id 7, wireType 0 =*/56).int32(message.premium);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified Sticker message, length delimited. Does not implicitly {@link proto.Message.StickerPackMessage.Sticker.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.StickerPackMessage.Sticker
                 * @static
                 * @param {proto.Message.StickerPackMessage.ISticker} message Sticker message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Sticker.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a Sticker message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.StickerPackMessage.Sticker
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.StickerPackMessage.Sticker} Sticker
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Sticker.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.fileName = reader.stringVerify();
                                message._fileName = "fileName";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                message.isAnimated = reader.bool();
                                message._isAnimated = "isAnimated";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                if (!(message.emojis && message.emojis.length))
                                    message.emojis = [];
                                message.emojis.push(reader.stringVerify());
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                message.accessibilityLabel = reader.stringVerify();
                                message._accessibilityLabel = "accessibilityLabel";
                                continue;
                            }
                        case 5: {
                                if (u !== 0)
                                    break;
                                message.isLottie = reader.bool();
                                message._isLottie = "isLottie";
                                continue;
                            }
                        case 6: {
                                if (u !== 2)
                                    break;
                                message.mimetype = reader.stringVerify();
                                message._mimetype = "mimetype";
                                continue;
                            }
                        case 7: {
                                if (u !== 0)
                                    break;
                                message.premium = reader.int32();
                                message._premium = "premium";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a Sticker message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.StickerPackMessage.Sticker
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.StickerPackMessage.Sticker} Sticker
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Sticker.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a Sticker message.
                 * @function verify
                 * @memberof proto.Message.StickerPackMessage.Sticker
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                Sticker.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName")) {
                        properties._fileName = 1;
                        if (!$util.isString(message.fileName))
                            return "fileName: string expected";
                    }
                    if (message.isAnimated != null && Object.hasOwnProperty.call(message, "isAnimated")) {
                        properties._isAnimated = 1;
                        if (typeof message.isAnimated !== "boolean")
                            return "isAnimated: boolean expected";
                    }
                    if (message.emojis != null && Object.hasOwnProperty.call(message, "emojis")) {
                        if (!Array.isArray(message.emojis))
                            return "emojis: array expected";
                        for (var i = 0; i < message.emojis.length; ++i)
                            if (!$util.isString(message.emojis[i]))
                                return "emojis: string[] expected";
                    }
                    if (message.accessibilityLabel != null && Object.hasOwnProperty.call(message, "accessibilityLabel")) {
                        properties._accessibilityLabel = 1;
                        if (!$util.isString(message.accessibilityLabel))
                            return "accessibilityLabel: string expected";
                    }
                    if (message.isLottie != null && Object.hasOwnProperty.call(message, "isLottie")) {
                        properties._isLottie = 1;
                        if (typeof message.isLottie !== "boolean")
                            return "isLottie: boolean expected";
                    }
                    if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype")) {
                        properties._mimetype = 1;
                        if (!$util.isString(message.mimetype))
                            return "mimetype: string expected";
                    }
                    if (message.premium != null && Object.hasOwnProperty.call(message, "premium")) {
                        properties._premium = 1;
                        if (!$util.isInteger(message.premium))
                            return "premium: integer expected";
                    }
                    return null;
                };

                /**
                 * Creates a Sticker message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.StickerPackMessage.Sticker
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.StickerPackMessage.Sticker} Sticker
                 */
                Sticker.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.StickerPackMessage.Sticker: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.fileName != null)
                        message.fileName = String(object.fileName);
                    if (object.isAnimated != null)
                        message.isAnimated = Boolean(object.isAnimated);
                    if (object.emojis) {
                        if (!Array.isArray(object.emojis))
                            throw TypeError(".proto.Message.StickerPackMessage.Sticker.emojis: array expected");
                        message.emojis = Array(object.emojis.length);
                        for (var i = 0; i < object.emojis.length; ++i)
                            message.emojis[i] = String(object.emojis[i]);
                    }
                    if (object.accessibilityLabel != null)
                        message.accessibilityLabel = String(object.accessibilityLabel);
                    if (object.isLottie != null)
                        message.isLottie = Boolean(object.isLottie);
                    if (object.mimetype != null)
                        message.mimetype = String(object.mimetype);
                    if (object.premium != null)
                        message.premium = object.premium | 0;
                    return message;
                };

                /**
                 * Creates a plain object from a Sticker message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.StickerPackMessage.Sticker
                 * @static
                 * @param {proto.Message.StickerPackMessage.Sticker} message Sticker
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                Sticker.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (options.arrays || options.defaults)
                        object.emojis = [];
                    if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                        object.fileName = message.fileName;
                    if (message.isAnimated != null && Object.hasOwnProperty.call(message, "isAnimated"))
                        object.isAnimated = message.isAnimated;
                    if (message.emojis && message.emojis.length) {
                        object.emojis = Array(message.emojis.length);
                        for (var j = 0; j < message.emojis.length; ++j)
                            object.emojis[j] = message.emojis[j];
                    }
                    if (message.accessibilityLabel != null && Object.hasOwnProperty.call(message, "accessibilityLabel"))
                        object.accessibilityLabel = message.accessibilityLabel;
                    if (message.isLottie != null && Object.hasOwnProperty.call(message, "isLottie"))
                        object.isLottie = message.isLottie;
                    if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                        object.mimetype = message.mimetype;
                    if (message.premium != null && Object.hasOwnProperty.call(message, "premium"))
                        object.premium = message.premium;
                    return object;
                };

                /**
                 * Converts this Sticker to JSON.
                 * @function toJSON
                 * @memberof proto.Message.StickerPackMessage.Sticker
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                Sticker.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for Sticker
                 * @function getTypeUrl
                 * @memberof proto.Message.StickerPackMessage.Sticker
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                Sticker.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.StickerPackMessage.Sticker";
                };

                return Sticker;
            })();

            /**
             * StickerPackOrigin enum.
             * @name proto.Message.StickerPackMessage.StickerPackOrigin
             * @enum {number}
             * @property {number} FIRST_PARTY=0 FIRST_PARTY value
             * @property {number} THIRD_PARTY=1 THIRD_PARTY value
             * @property {number} USER_CREATED=2 USER_CREATED value
             */
            StickerPackMessage.StickerPackOrigin = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "FIRST_PARTY"] = 0;
                values[valuesById[1] = "THIRD_PARTY"] = 1;
                values[valuesById[2] = "USER_CREATED"] = 2;
                return values;
            })();

            return StickerPackMessage;
        })();

        Message.StickerSyncRMRMessage = (function() {

            /**
             * Properties of a StickerSyncRMRMessage.
             * @memberof proto.Message
             * @interface IStickerSyncRMRMessage
             * @property {Array.<string>|null} [filehash] StickerSyncRMRMessage filehash
             * @property {string|null} [rmrSource] StickerSyncRMRMessage rmrSource
             * @property {number|Long|null} [requestTimestamp] StickerSyncRMRMessage requestTimestamp
             */

            /**
             * Constructs a new StickerSyncRMRMessage.
             * @memberof proto.Message
             * @classdesc Represents a StickerSyncRMRMessage.
             * @implements IStickerSyncRMRMessage
             * @constructor
             * @param {proto.Message.IStickerSyncRMRMessage=} [properties] Properties to set
             */
            function StickerSyncRMRMessage(properties) {
                this.filehash = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * StickerSyncRMRMessage filehash.
             * @member {Array.<string>} filehash
             * @memberof proto.Message.StickerSyncRMRMessage
             * @instance
             */
            StickerSyncRMRMessage.prototype.filehash = $util.emptyArray;

            /**
             * StickerSyncRMRMessage rmrSource.
             * @member {string|null|undefined} rmrSource
             * @memberof proto.Message.StickerSyncRMRMessage
             * @instance
             */
            StickerSyncRMRMessage.prototype.rmrSource = null;

            /**
             * StickerSyncRMRMessage requestTimestamp.
             * @member {number|Long|null|undefined} requestTimestamp
             * @memberof proto.Message.StickerSyncRMRMessage
             * @instance
             */
            StickerSyncRMRMessage.prototype.requestTimestamp = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerSyncRMRMessage.prototype, "_rmrSource", {
                get: $util.oneOfGetter($oneOfFields = ["rmrSource"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerSyncRMRMessage.prototype, "_requestTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["requestTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StickerSyncRMRMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.StickerSyncRMRMessage
             * @static
             * @param {proto.Message.IStickerSyncRMRMessage=} [properties] Properties to set
             * @returns {proto.Message.StickerSyncRMRMessage} StickerSyncRMRMessage instance
             */
            StickerSyncRMRMessage.create = function create(properties) {
                return new StickerSyncRMRMessage(properties);
            };

            /**
             * Encodes the specified StickerSyncRMRMessage message. Does not implicitly {@link proto.Message.StickerSyncRMRMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.StickerSyncRMRMessage
             * @static
             * @param {proto.Message.IStickerSyncRMRMessage} message StickerSyncRMRMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StickerSyncRMRMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.filehash != null && message.filehash.length)
                    for (var i = 0; i < message.filehash.length; ++i)
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.filehash[i]);
                if (message.rmrSource != null && Object.hasOwnProperty.call(message, "rmrSource"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.rmrSource);
                if (message.requestTimestamp != null && Object.hasOwnProperty.call(message, "requestTimestamp"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.requestTimestamp);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified StickerSyncRMRMessage message, length delimited. Does not implicitly {@link proto.Message.StickerSyncRMRMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.StickerSyncRMRMessage
             * @static
             * @param {proto.Message.IStickerSyncRMRMessage} message StickerSyncRMRMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StickerSyncRMRMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a StickerSyncRMRMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.StickerSyncRMRMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.StickerSyncRMRMessage} StickerSyncRMRMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StickerSyncRMRMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(message.filehash && message.filehash.length))
                                message.filehash = [];
                            message.filehash.push(reader.stringVerify());
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.rmrSource = reader.stringVerify();
                            message._rmrSource = "rmrSource";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.requestTimestamp = reader.int64();
                            message._requestTimestamp = "requestTimestamp";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a StickerSyncRMRMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.StickerSyncRMRMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.StickerSyncRMRMessage} StickerSyncRMRMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StickerSyncRMRMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a StickerSyncRMRMessage message.
             * @function verify
             * @memberof proto.Message.StickerSyncRMRMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StickerSyncRMRMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.filehash != null && Object.hasOwnProperty.call(message, "filehash")) {
                    if (!Array.isArray(message.filehash))
                        return "filehash: array expected";
                    for (var i = 0; i < message.filehash.length; ++i)
                        if (!$util.isString(message.filehash[i]))
                            return "filehash: string[] expected";
                }
                if (message.rmrSource != null && Object.hasOwnProperty.call(message, "rmrSource")) {
                    properties._rmrSource = 1;
                    if (!$util.isString(message.rmrSource))
                        return "rmrSource: string expected";
                }
                if (message.requestTimestamp != null && Object.hasOwnProperty.call(message, "requestTimestamp")) {
                    properties._requestTimestamp = 1;
                    if (!$util.isInteger(message.requestTimestamp) && !(message.requestTimestamp && $util.isInteger(message.requestTimestamp.low) && $util.isInteger(message.requestTimestamp.high)))
                        return "requestTimestamp: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a StickerSyncRMRMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.StickerSyncRMRMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.StickerSyncRMRMessage} StickerSyncRMRMessage
             */
            StickerSyncRMRMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.StickerSyncRMRMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.filehash) {
                    if (!Array.isArray(object.filehash))
                        throw TypeError(".proto.Message.StickerSyncRMRMessage.filehash: array expected");
                    message.filehash = Array(object.filehash.length);
                    for (var i = 0; i < object.filehash.length; ++i)
                        message.filehash[i] = String(object.filehash[i]);
                }
                if (object.rmrSource != null)
                    message.rmrSource = String(object.rmrSource);
                if (object.requestTimestamp != null)
                    if ($util.Long)
                        message.requestTimestamp = $util.Long.fromValue(object.requestTimestamp, false);
                    else if (typeof object.requestTimestamp === "string")
                        message.requestTimestamp = parseInt(object.requestTimestamp, 10);
                    else if (typeof object.requestTimestamp === "number")
                        message.requestTimestamp = object.requestTimestamp;
                    else if (typeof object.requestTimestamp === "object")
                        message.requestTimestamp = new $util.LongBits(object.requestTimestamp.low >>> 0, object.requestTimestamp.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a StickerSyncRMRMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.StickerSyncRMRMessage
             * @static
             * @param {proto.Message.StickerSyncRMRMessage} message StickerSyncRMRMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StickerSyncRMRMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.filehash = [];
                if (message.filehash && message.filehash.length) {
                    object.filehash = Array(message.filehash.length);
                    for (var j = 0; j < message.filehash.length; ++j)
                        object.filehash[j] = message.filehash[j];
                }
                if (message.rmrSource != null && Object.hasOwnProperty.call(message, "rmrSource"))
                    object.rmrSource = message.rmrSource;
                if (message.requestTimestamp != null && Object.hasOwnProperty.call(message, "requestTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.requestTimestamp = typeof message.requestTimestamp === "number" ? BigInt(message.requestTimestamp) : $util.Long.fromBits(message.requestTimestamp.low >>> 0, message.requestTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.requestTimestamp === "number")
                        object.requestTimestamp = options.longs === String ? String(message.requestTimestamp) : message.requestTimestamp;
                    else
                        object.requestTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.requestTimestamp) : options.longs === Number ? new $util.LongBits(message.requestTimestamp.low >>> 0, message.requestTimestamp.high >>> 0).toNumber() : message.requestTimestamp;
                return object;
            };

            /**
             * Converts this StickerSyncRMRMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.StickerSyncRMRMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StickerSyncRMRMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for StickerSyncRMRMessage
             * @function getTypeUrl
             * @memberof proto.Message.StickerSyncRMRMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            StickerSyncRMRMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.StickerSyncRMRMessage";
            };

            return StickerSyncRMRMessage;
        })();

        Message.TemplateButtonReplyMessage = (function() {

            /**
             * Properties of a TemplateButtonReplyMessage.
             * @memberof proto.Message
             * @interface ITemplateButtonReplyMessage
             * @property {string|null} [selectedId] TemplateButtonReplyMessage selectedId
             * @property {string|null} [selectedDisplayText] TemplateButtonReplyMessage selectedDisplayText
             * @property {proto.IContextInfo|null} [contextInfo] TemplateButtonReplyMessage contextInfo
             * @property {number|null} [selectedIndex] TemplateButtonReplyMessage selectedIndex
             * @property {number|null} [selectedCarouselCardIndex] TemplateButtonReplyMessage selectedCarouselCardIndex
             */

            /**
             * Constructs a new TemplateButtonReplyMessage.
             * @memberof proto.Message
             * @classdesc Represents a TemplateButtonReplyMessage.
             * @implements ITemplateButtonReplyMessage
             * @constructor
             * @param {proto.Message.ITemplateButtonReplyMessage=} [properties] Properties to set
             */
            function TemplateButtonReplyMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * TemplateButtonReplyMessage selectedId.
             * @member {string|null|undefined} selectedId
             * @memberof proto.Message.TemplateButtonReplyMessage
             * @instance
             */
            TemplateButtonReplyMessage.prototype.selectedId = null;

            /**
             * TemplateButtonReplyMessage selectedDisplayText.
             * @member {string|null|undefined} selectedDisplayText
             * @memberof proto.Message.TemplateButtonReplyMessage
             * @instance
             */
            TemplateButtonReplyMessage.prototype.selectedDisplayText = null;

            /**
             * TemplateButtonReplyMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.TemplateButtonReplyMessage
             * @instance
             */
            TemplateButtonReplyMessage.prototype.contextInfo = null;

            /**
             * TemplateButtonReplyMessage selectedIndex.
             * @member {number|null|undefined} selectedIndex
             * @memberof proto.Message.TemplateButtonReplyMessage
             * @instance
             */
            TemplateButtonReplyMessage.prototype.selectedIndex = null;

            /**
             * TemplateButtonReplyMessage selectedCarouselCardIndex.
             * @member {number|null|undefined} selectedCarouselCardIndex
             * @memberof proto.Message.TemplateButtonReplyMessage
             * @instance
             */
            TemplateButtonReplyMessage.prototype.selectedCarouselCardIndex = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(TemplateButtonReplyMessage.prototype, "_selectedId", {
                get: $util.oneOfGetter($oneOfFields = ["selectedId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(TemplateButtonReplyMessage.prototype, "_selectedDisplayText", {
                get: $util.oneOfGetter($oneOfFields = ["selectedDisplayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(TemplateButtonReplyMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(TemplateButtonReplyMessage.prototype, "_selectedIndex", {
                get: $util.oneOfGetter($oneOfFields = ["selectedIndex"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(TemplateButtonReplyMessage.prototype, "_selectedCarouselCardIndex", {
                get: $util.oneOfGetter($oneOfFields = ["selectedCarouselCardIndex"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new TemplateButtonReplyMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.TemplateButtonReplyMessage
             * @static
             * @param {proto.Message.ITemplateButtonReplyMessage=} [properties] Properties to set
             * @returns {proto.Message.TemplateButtonReplyMessage} TemplateButtonReplyMessage instance
             */
            TemplateButtonReplyMessage.create = function create(properties) {
                return new TemplateButtonReplyMessage(properties);
            };

            /**
             * Encodes the specified TemplateButtonReplyMessage message. Does not implicitly {@link proto.Message.TemplateButtonReplyMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.TemplateButtonReplyMessage
             * @static
             * @param {proto.Message.ITemplateButtonReplyMessage} message TemplateButtonReplyMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            TemplateButtonReplyMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.selectedId != null && Object.hasOwnProperty.call(message, "selectedId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.selectedId);
                if (message.selectedDisplayText != null && Object.hasOwnProperty.call(message, "selectedDisplayText"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.selectedDisplayText);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.selectedIndex != null && Object.hasOwnProperty.call(message, "selectedIndex"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.selectedIndex);
                if (message.selectedCarouselCardIndex != null && Object.hasOwnProperty.call(message, "selectedCarouselCardIndex"))
                    writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.selectedCarouselCardIndex);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified TemplateButtonReplyMessage message, length delimited. Does not implicitly {@link proto.Message.TemplateButtonReplyMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.TemplateButtonReplyMessage
             * @static
             * @param {proto.Message.ITemplateButtonReplyMessage} message TemplateButtonReplyMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            TemplateButtonReplyMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a TemplateButtonReplyMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.TemplateButtonReplyMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.TemplateButtonReplyMessage} TemplateButtonReplyMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            TemplateButtonReplyMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.selectedId = reader.stringVerify();
                            message._selectedId = "selectedId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.selectedDisplayText = reader.stringVerify();
                            message._selectedDisplayText = "selectedDisplayText";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.selectedIndex = reader.uint32();
                            message._selectedIndex = "selectedIndex";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.selectedCarouselCardIndex = reader.uint32();
                            message._selectedCarouselCardIndex = "selectedCarouselCardIndex";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a TemplateButtonReplyMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.TemplateButtonReplyMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.TemplateButtonReplyMessage} TemplateButtonReplyMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            TemplateButtonReplyMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a TemplateButtonReplyMessage message.
             * @function verify
             * @memberof proto.Message.TemplateButtonReplyMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            TemplateButtonReplyMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.selectedId != null && Object.hasOwnProperty.call(message, "selectedId")) {
                    properties._selectedId = 1;
                    if (!$util.isString(message.selectedId))
                        return "selectedId: string expected";
                }
                if (message.selectedDisplayText != null && Object.hasOwnProperty.call(message, "selectedDisplayText")) {
                    properties._selectedDisplayText = 1;
                    if (!$util.isString(message.selectedDisplayText))
                        return "selectedDisplayText: string expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.selectedIndex != null && Object.hasOwnProperty.call(message, "selectedIndex")) {
                    properties._selectedIndex = 1;
                    if (!$util.isInteger(message.selectedIndex))
                        return "selectedIndex: integer expected";
                }
                if (message.selectedCarouselCardIndex != null && Object.hasOwnProperty.call(message, "selectedCarouselCardIndex")) {
                    properties._selectedCarouselCardIndex = 1;
                    if (!$util.isInteger(message.selectedCarouselCardIndex))
                        return "selectedCarouselCardIndex: integer expected";
                }
                return null;
            };

            /**
             * Creates a TemplateButtonReplyMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.TemplateButtonReplyMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.TemplateButtonReplyMessage} TemplateButtonReplyMessage
             */
            TemplateButtonReplyMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.TemplateButtonReplyMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.selectedId != null)
                    message.selectedId = String(object.selectedId);
                if (object.selectedDisplayText != null)
                    message.selectedDisplayText = String(object.selectedDisplayText);
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.TemplateButtonReplyMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                if (object.selectedIndex != null)
                    message.selectedIndex = object.selectedIndex >>> 0;
                if (object.selectedCarouselCardIndex != null)
                    message.selectedCarouselCardIndex = object.selectedCarouselCardIndex >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a TemplateButtonReplyMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.TemplateButtonReplyMessage
             * @static
             * @param {proto.Message.TemplateButtonReplyMessage} message TemplateButtonReplyMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            TemplateButtonReplyMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.selectedId != null && Object.hasOwnProperty.call(message, "selectedId"))
                    object.selectedId = message.selectedId;
                if (message.selectedDisplayText != null && Object.hasOwnProperty.call(message, "selectedDisplayText"))
                    object.selectedDisplayText = message.selectedDisplayText;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.selectedIndex != null && Object.hasOwnProperty.call(message, "selectedIndex"))
                    object.selectedIndex = message.selectedIndex;
                if (message.selectedCarouselCardIndex != null && Object.hasOwnProperty.call(message, "selectedCarouselCardIndex"))
                    object.selectedCarouselCardIndex = message.selectedCarouselCardIndex;
                return object;
            };

            /**
             * Converts this TemplateButtonReplyMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.TemplateButtonReplyMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            TemplateButtonReplyMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for TemplateButtonReplyMessage
             * @function getTypeUrl
             * @memberof proto.Message.TemplateButtonReplyMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            TemplateButtonReplyMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.TemplateButtonReplyMessage";
            };

            return TemplateButtonReplyMessage;
        })();

        Message.TemplateMessage = (function() {

            /**
             * Properties of a TemplateMessage.
             * @memberof proto.Message
             * @interface ITemplateMessage
             * @property {proto.IContextInfo|null} [contextInfo] TemplateMessage contextInfo
             * @property {proto.Message.TemplateMessage.IHydratedFourRowTemplate|null} [hydratedTemplate] TemplateMessage hydratedTemplate
             * @property {string|null} [templateId] TemplateMessage templateId
             * @property {proto.Message.TemplateMessage.IFourRowTemplate|null} [fourRowTemplate] TemplateMessage fourRowTemplate
             * @property {proto.Message.TemplateMessage.IHydratedFourRowTemplate|null} [hydratedFourRowTemplate] TemplateMessage hydratedFourRowTemplate
             * @property {proto.Message.IInteractiveMessage|null} [interactiveMessageTemplate] TemplateMessage interactiveMessageTemplate
             */

            /**
             * Constructs a new TemplateMessage.
             * @memberof proto.Message
             * @classdesc Represents a TemplateMessage.
             * @implements ITemplateMessage
             * @constructor
             * @param {proto.Message.ITemplateMessage=} [properties] Properties to set
             */
            function TemplateMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * TemplateMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.TemplateMessage
             * @instance
             */
            TemplateMessage.prototype.contextInfo = null;

            /**
             * TemplateMessage hydratedTemplate.
             * @member {proto.Message.TemplateMessage.IHydratedFourRowTemplate|null|undefined} hydratedTemplate
             * @memberof proto.Message.TemplateMessage
             * @instance
             */
            TemplateMessage.prototype.hydratedTemplate = null;

            /**
             * TemplateMessage templateId.
             * @member {string|null|undefined} templateId
             * @memberof proto.Message.TemplateMessage
             * @instance
             */
            TemplateMessage.prototype.templateId = null;

            /**
             * TemplateMessage fourRowTemplate.
             * @member {proto.Message.TemplateMessage.IFourRowTemplate|null|undefined} fourRowTemplate
             * @memberof proto.Message.TemplateMessage
             * @instance
             */
            TemplateMessage.prototype.fourRowTemplate = null;

            /**
             * TemplateMessage hydratedFourRowTemplate.
             * @member {proto.Message.TemplateMessage.IHydratedFourRowTemplate|null|undefined} hydratedFourRowTemplate
             * @memberof proto.Message.TemplateMessage
             * @instance
             */
            TemplateMessage.prototype.hydratedFourRowTemplate = null;

            /**
             * TemplateMessage interactiveMessageTemplate.
             * @member {proto.Message.IInteractiveMessage|null|undefined} interactiveMessageTemplate
             * @memberof proto.Message.TemplateMessage
             * @instance
             */
            TemplateMessage.prototype.interactiveMessageTemplate = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(TemplateMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(TemplateMessage.prototype, "_hydratedTemplate", {
                get: $util.oneOfGetter($oneOfFields = ["hydratedTemplate"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(TemplateMessage.prototype, "_templateId", {
                get: $util.oneOfGetter($oneOfFields = ["templateId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * TemplateMessage format.
             * @member {"fourRowTemplate"|"hydratedFourRowTemplate"|"interactiveMessageTemplate"|undefined} format
             * @memberof proto.Message.TemplateMessage
             * @instance
             */
            Object.defineProperty(TemplateMessage.prototype, "format", {
                get: $util.oneOfGetter($oneOfFields = ["fourRowTemplate", "hydratedFourRowTemplate", "interactiveMessageTemplate"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new TemplateMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.TemplateMessage
             * @static
             * @param {proto.Message.ITemplateMessage=} [properties] Properties to set
             * @returns {proto.Message.TemplateMessage} TemplateMessage instance
             */
            TemplateMessage.create = function create(properties) {
                return new TemplateMessage(properties);
            };

            /**
             * Encodes the specified TemplateMessage message. Does not implicitly {@link proto.Message.TemplateMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.TemplateMessage
             * @static
             * @param {proto.Message.ITemplateMessage} message TemplateMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            TemplateMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.fourRowTemplate != null && Object.hasOwnProperty.call(message, "fourRowTemplate"))
                    $root.proto.Message.TemplateMessage.FourRowTemplate.encode(message.fourRowTemplate, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.hydratedFourRowTemplate != null && Object.hasOwnProperty.call(message, "hydratedFourRowTemplate"))
                    $root.proto.Message.TemplateMessage.HydratedFourRowTemplate.encode(message.hydratedFourRowTemplate, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.hydratedTemplate != null && Object.hasOwnProperty.call(message, "hydratedTemplate"))
                    $root.proto.Message.TemplateMessage.HydratedFourRowTemplate.encode(message.hydratedTemplate, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.interactiveMessageTemplate != null && Object.hasOwnProperty.call(message, "interactiveMessageTemplate"))
                    $root.proto.Message.InteractiveMessage.encode(message.interactiveMessageTemplate, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                if (message.templateId != null && Object.hasOwnProperty.call(message, "templateId"))
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.templateId);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified TemplateMessage message, length delimited. Does not implicitly {@link proto.Message.TemplateMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.TemplateMessage
             * @static
             * @param {proto.Message.ITemplateMessage} message TemplateMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            TemplateMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a TemplateMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.TemplateMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.TemplateMessage} TemplateMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            TemplateMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 3: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.hydratedTemplate = $root.proto.Message.TemplateMessage.HydratedFourRowTemplate.decode(reader, reader.uint32(), undefined, q + 1, message.hydratedTemplate);
                            message._hydratedTemplate = "hydratedTemplate";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            message.templateId = reader.stringVerify();
                            message._templateId = "templateId";
                            continue;
                        }
                    case 1: {
                            if (u !== 2)
                                break;
                            message.fourRowTemplate = $root.proto.Message.TemplateMessage.FourRowTemplate.decode(reader, reader.uint32(), undefined, q + 1, message.fourRowTemplate);
                            message.format = "fourRowTemplate";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.hydratedFourRowTemplate = $root.proto.Message.TemplateMessage.HydratedFourRowTemplate.decode(reader, reader.uint32(), undefined, q + 1, message.hydratedFourRowTemplate);
                            message.format = "hydratedFourRowTemplate";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.interactiveMessageTemplate = $root.proto.Message.InteractiveMessage.decode(reader, reader.uint32(), undefined, q + 1, message.interactiveMessageTemplate);
                            message.format = "interactiveMessageTemplate";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a TemplateMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.TemplateMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.TemplateMessage} TemplateMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            TemplateMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a TemplateMessage message.
             * @function verify
             * @memberof proto.Message.TemplateMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            TemplateMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.hydratedTemplate != null && Object.hasOwnProperty.call(message, "hydratedTemplate")) {
                    properties._hydratedTemplate = 1;
                    {
                        var error = $root.proto.Message.TemplateMessage.HydratedFourRowTemplate.verify(message.hydratedTemplate, q + 1);
                        if (error)
                            return "hydratedTemplate." + error;
                    }
                }
                if (message.templateId != null && Object.hasOwnProperty.call(message, "templateId")) {
                    properties._templateId = 1;
                    if (!$util.isString(message.templateId))
                        return "templateId: string expected";
                }
                if (message.fourRowTemplate != null && Object.hasOwnProperty.call(message, "fourRowTemplate")) {
                    properties.format = 1;
                    {
                        var error = $root.proto.Message.TemplateMessage.FourRowTemplate.verify(message.fourRowTemplate, q + 1);
                        if (error)
                            return "fourRowTemplate." + error;
                    }
                }
                if (message.hydratedFourRowTemplate != null && Object.hasOwnProperty.call(message, "hydratedFourRowTemplate")) {
                    if (properties.format === 1)
                        return "format: multiple values";
                    properties.format = 1;
                    {
                        var error = $root.proto.Message.TemplateMessage.HydratedFourRowTemplate.verify(message.hydratedFourRowTemplate, q + 1);
                        if (error)
                            return "hydratedFourRowTemplate." + error;
                    }
                }
                if (message.interactiveMessageTemplate != null && Object.hasOwnProperty.call(message, "interactiveMessageTemplate")) {
                    if (properties.format === 1)
                        return "format: multiple values";
                    properties.format = 1;
                    {
                        var error = $root.proto.Message.InteractiveMessage.verify(message.interactiveMessageTemplate, q + 1);
                        if (error)
                            return "interactiveMessageTemplate." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a TemplateMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.TemplateMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.TemplateMessage} TemplateMessage
             */
            TemplateMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.TemplateMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.TemplateMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                if (object.hydratedTemplate != null) {
                    if (!$util.isObject(object.hydratedTemplate))
                        throw TypeError(".proto.Message.TemplateMessage.hydratedTemplate: object expected");
                    message.hydratedTemplate = $root.proto.Message.TemplateMessage.HydratedFourRowTemplate.fromObject(object.hydratedTemplate, q + 1);
                }
                if (object.templateId != null)
                    message.templateId = String(object.templateId);
                if (object.fourRowTemplate != null) {
                    if (!$util.isObject(object.fourRowTemplate))
                        throw TypeError(".proto.Message.TemplateMessage.fourRowTemplate: object expected");
                    message.fourRowTemplate = $root.proto.Message.TemplateMessage.FourRowTemplate.fromObject(object.fourRowTemplate, q + 1);
                }
                if (object.hydratedFourRowTemplate != null) {
                    if (!$util.isObject(object.hydratedFourRowTemplate))
                        throw TypeError(".proto.Message.TemplateMessage.hydratedFourRowTemplate: object expected");
                    message.hydratedFourRowTemplate = $root.proto.Message.TemplateMessage.HydratedFourRowTemplate.fromObject(object.hydratedFourRowTemplate, q + 1);
                }
                if (object.interactiveMessageTemplate != null) {
                    if (!$util.isObject(object.interactiveMessageTemplate))
                        throw TypeError(".proto.Message.TemplateMessage.interactiveMessageTemplate: object expected");
                    message.interactiveMessageTemplate = $root.proto.Message.InteractiveMessage.fromObject(object.interactiveMessageTemplate, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a TemplateMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.TemplateMessage
             * @static
             * @param {proto.Message.TemplateMessage} message TemplateMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            TemplateMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.fourRowTemplate != null && Object.hasOwnProperty.call(message, "fourRowTemplate")) {
                    object.fourRowTemplate = $root.proto.Message.TemplateMessage.FourRowTemplate.toObject(message.fourRowTemplate, options, q + 1);
                    if (options.oneofs)
                        object.format = "fourRowTemplate";
                }
                if (message.hydratedFourRowTemplate != null && Object.hasOwnProperty.call(message, "hydratedFourRowTemplate")) {
                    object.hydratedFourRowTemplate = $root.proto.Message.TemplateMessage.HydratedFourRowTemplate.toObject(message.hydratedFourRowTemplate, options, q + 1);
                    if (options.oneofs)
                        object.format = "hydratedFourRowTemplate";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.hydratedTemplate != null && Object.hasOwnProperty.call(message, "hydratedTemplate"))
                    object.hydratedTemplate = $root.proto.Message.TemplateMessage.HydratedFourRowTemplate.toObject(message.hydratedTemplate, options, q + 1);
                if (message.interactiveMessageTemplate != null && Object.hasOwnProperty.call(message, "interactiveMessageTemplate")) {
                    object.interactiveMessageTemplate = $root.proto.Message.InteractiveMessage.toObject(message.interactiveMessageTemplate, options, q + 1);
                    if (options.oneofs)
                        object.format = "interactiveMessageTemplate";
                }
                if (message.templateId != null && Object.hasOwnProperty.call(message, "templateId"))
                    object.templateId = message.templateId;
                return object;
            };

            /**
             * Converts this TemplateMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.TemplateMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            TemplateMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for TemplateMessage
             * @function getTypeUrl
             * @memberof proto.Message.TemplateMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            TemplateMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.TemplateMessage";
            };

            TemplateMessage.FourRowTemplate = (function() {

                /**
                 * Properties of a FourRowTemplate.
                 * @memberof proto.Message.TemplateMessage
                 * @interface IFourRowTemplate
                 * @property {proto.Message.IHighlyStructuredMessage|null} [content] FourRowTemplate content
                 * @property {proto.Message.IHighlyStructuredMessage|null} [footer] FourRowTemplate footer
                 * @property {Array.<proto.ITemplateButton>|null} [buttons] FourRowTemplate buttons
                 * @property {proto.Message.IDocumentMessage|null} [documentMessage] FourRowTemplate documentMessage
                 * @property {proto.Message.IHighlyStructuredMessage|null} [highlyStructuredMessage] FourRowTemplate highlyStructuredMessage
                 * @property {proto.Message.IImageMessage|null} [imageMessage] FourRowTemplate imageMessage
                 * @property {proto.Message.IVideoMessage|null} [videoMessage] FourRowTemplate videoMessage
                 * @property {proto.Message.ILocationMessage|null} [locationMessage] FourRowTemplate locationMessage
                 */

                /**
                 * Constructs a new FourRowTemplate.
                 * @memberof proto.Message.TemplateMessage
                 * @classdesc Represents a FourRowTemplate.
                 * @implements IFourRowTemplate
                 * @constructor
                 * @param {proto.Message.TemplateMessage.IFourRowTemplate=} [properties] Properties to set
                 */
                function FourRowTemplate(properties) {
                    this.buttons = [];
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * FourRowTemplate content.
                 * @member {proto.Message.IHighlyStructuredMessage|null|undefined} content
                 * @memberof proto.Message.TemplateMessage.FourRowTemplate
                 * @instance
                 */
                FourRowTemplate.prototype.content = null;

                /**
                 * FourRowTemplate footer.
                 * @member {proto.Message.IHighlyStructuredMessage|null|undefined} footer
                 * @memberof proto.Message.TemplateMessage.FourRowTemplate
                 * @instance
                 */
                FourRowTemplate.prototype.footer = null;

                /**
                 * FourRowTemplate buttons.
                 * @member {Array.<proto.ITemplateButton>} buttons
                 * @memberof proto.Message.TemplateMessage.FourRowTemplate
                 * @instance
                 */
                FourRowTemplate.prototype.buttons = $util.emptyArray;

                /**
                 * FourRowTemplate documentMessage.
                 * @member {proto.Message.IDocumentMessage|null|undefined} documentMessage
                 * @memberof proto.Message.TemplateMessage.FourRowTemplate
                 * @instance
                 */
                FourRowTemplate.prototype.documentMessage = null;

                /**
                 * FourRowTemplate highlyStructuredMessage.
                 * @member {proto.Message.IHighlyStructuredMessage|null|undefined} highlyStructuredMessage
                 * @memberof proto.Message.TemplateMessage.FourRowTemplate
                 * @instance
                 */
                FourRowTemplate.prototype.highlyStructuredMessage = null;

                /**
                 * FourRowTemplate imageMessage.
                 * @member {proto.Message.IImageMessage|null|undefined} imageMessage
                 * @memberof proto.Message.TemplateMessage.FourRowTemplate
                 * @instance
                 */
                FourRowTemplate.prototype.imageMessage = null;

                /**
                 * FourRowTemplate videoMessage.
                 * @member {proto.Message.IVideoMessage|null|undefined} videoMessage
                 * @memberof proto.Message.TemplateMessage.FourRowTemplate
                 * @instance
                 */
                FourRowTemplate.prototype.videoMessage = null;

                /**
                 * FourRowTemplate locationMessage.
                 * @member {proto.Message.ILocationMessage|null|undefined} locationMessage
                 * @memberof proto.Message.TemplateMessage.FourRowTemplate
                 * @instance
                 */
                FourRowTemplate.prototype.locationMessage = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(FourRowTemplate.prototype, "_content", {
                    get: $util.oneOfGetter($oneOfFields = ["content"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(FourRowTemplate.prototype, "_footer", {
                    get: $util.oneOfGetter($oneOfFields = ["footer"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * FourRowTemplate title.
                 * @member {"documentMessage"|"highlyStructuredMessage"|"imageMessage"|"videoMessage"|"locationMessage"|undefined} title
                 * @memberof proto.Message.TemplateMessage.FourRowTemplate
                 * @instance
                 */
                Object.defineProperty(FourRowTemplate.prototype, "title", {
                    get: $util.oneOfGetter($oneOfFields = ["documentMessage", "highlyStructuredMessage", "imageMessage", "videoMessage", "locationMessage"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new FourRowTemplate instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.TemplateMessage.FourRowTemplate
                 * @static
                 * @param {proto.Message.TemplateMessage.IFourRowTemplate=} [properties] Properties to set
                 * @returns {proto.Message.TemplateMessage.FourRowTemplate} FourRowTemplate instance
                 */
                FourRowTemplate.create = function create(properties) {
                    return new FourRowTemplate(properties);
                };

                /**
                 * Encodes the specified FourRowTemplate message. Does not implicitly {@link proto.Message.TemplateMessage.FourRowTemplate.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.TemplateMessage.FourRowTemplate
                 * @static
                 * @param {proto.Message.TemplateMessage.IFourRowTemplate} message FourRowTemplate message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                FourRowTemplate.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.documentMessage != null && Object.hasOwnProperty.call(message, "documentMessage"))
                        $root.proto.Message.DocumentMessage.encode(message.documentMessage, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                    if (message.highlyStructuredMessage != null && Object.hasOwnProperty.call(message, "highlyStructuredMessage"))
                        $root.proto.Message.HighlyStructuredMessage.encode(message.highlyStructuredMessage, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                    if (message.imageMessage != null && Object.hasOwnProperty.call(message, "imageMessage"))
                        $root.proto.Message.ImageMessage.encode(message.imageMessage, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                    if (message.videoMessage != null && Object.hasOwnProperty.call(message, "videoMessage"))
                        $root.proto.Message.VideoMessage.encode(message.videoMessage, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                    if (message.locationMessage != null && Object.hasOwnProperty.call(message, "locationMessage"))
                        $root.proto.Message.LocationMessage.encode(message.locationMessage, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                    if (message.content != null && Object.hasOwnProperty.call(message, "content"))
                        $root.proto.Message.HighlyStructuredMessage.encode(message.content, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
                    if (message.footer != null && Object.hasOwnProperty.call(message, "footer"))
                        $root.proto.Message.HighlyStructuredMessage.encode(message.footer, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
                    if (message.buttons != null && message.buttons.length)
                        for (var i = 0; i < message.buttons.length; ++i)
                            $root.proto.TemplateButton.encode(message.buttons[i], writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified FourRowTemplate message, length delimited. Does not implicitly {@link proto.Message.TemplateMessage.FourRowTemplate.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.TemplateMessage.FourRowTemplate
                 * @static
                 * @param {proto.Message.TemplateMessage.IFourRowTemplate} message FourRowTemplate message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                FourRowTemplate.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a FourRowTemplate message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.TemplateMessage.FourRowTemplate
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.TemplateMessage.FourRowTemplate} FourRowTemplate
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                FourRowTemplate.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 6: {
                                if (u !== 2)
                                    break;
                                message.content = $root.proto.Message.HighlyStructuredMessage.decode(reader, reader.uint32(), undefined, q + 1, message.content);
                                message._content = "content";
                                continue;
                            }
                        case 7: {
                                if (u !== 2)
                                    break;
                                message.footer = $root.proto.Message.HighlyStructuredMessage.decode(reader, reader.uint32(), undefined, q + 1, message.footer);
                                message._footer = "footer";
                                continue;
                            }
                        case 8: {
                                if (u !== 2)
                                    break;
                                if (!(message.buttons && message.buttons.length))
                                    message.buttons = [];
                                message.buttons.push($root.proto.TemplateButton.decode(reader, reader.uint32(), undefined, q + 1));
                                continue;
                            }
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.documentMessage = $root.proto.Message.DocumentMessage.decode(reader, reader.uint32(), undefined, q + 1, message.documentMessage);
                                message.title = "documentMessage";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.highlyStructuredMessage = $root.proto.Message.HighlyStructuredMessage.decode(reader, reader.uint32(), undefined, q + 1, message.highlyStructuredMessage);
                                message.title = "highlyStructuredMessage";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.imageMessage = $root.proto.Message.ImageMessage.decode(reader, reader.uint32(), undefined, q + 1, message.imageMessage);
                                message.title = "imageMessage";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                message.videoMessage = $root.proto.Message.VideoMessage.decode(reader, reader.uint32(), undefined, q + 1, message.videoMessage);
                                message.title = "videoMessage";
                                continue;
                            }
                        case 5: {
                                if (u !== 2)
                                    break;
                                message.locationMessage = $root.proto.Message.LocationMessage.decode(reader, reader.uint32(), undefined, q + 1, message.locationMessage);
                                message.title = "locationMessage";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a FourRowTemplate message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.TemplateMessage.FourRowTemplate
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.TemplateMessage.FourRowTemplate} FourRowTemplate
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                FourRowTemplate.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a FourRowTemplate message.
                 * @function verify
                 * @memberof proto.Message.TemplateMessage.FourRowTemplate
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                FourRowTemplate.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.content != null && Object.hasOwnProperty.call(message, "content")) {
                        properties._content = 1;
                        {
                            var error = $root.proto.Message.HighlyStructuredMessage.verify(message.content, q + 1);
                            if (error)
                                return "content." + error;
                        }
                    }
                    if (message.footer != null && Object.hasOwnProperty.call(message, "footer")) {
                        properties._footer = 1;
                        {
                            var error = $root.proto.Message.HighlyStructuredMessage.verify(message.footer, q + 1);
                            if (error)
                                return "footer." + error;
                        }
                    }
                    if (message.buttons != null && Object.hasOwnProperty.call(message, "buttons")) {
                        if (!Array.isArray(message.buttons))
                            return "buttons: array expected";
                        for (var i = 0; i < message.buttons.length; ++i) {
                            var error = $root.proto.TemplateButton.verify(message.buttons[i], q + 1);
                            if (error)
                                return "buttons." + error;
                        }
                    }
                    if (message.documentMessage != null && Object.hasOwnProperty.call(message, "documentMessage")) {
                        properties.title = 1;
                        {
                            var error = $root.proto.Message.DocumentMessage.verify(message.documentMessage, q + 1);
                            if (error)
                                return "documentMessage." + error;
                        }
                    }
                    if (message.highlyStructuredMessage != null && Object.hasOwnProperty.call(message, "highlyStructuredMessage")) {
                        if (properties.title === 1)
                            return "title: multiple values";
                        properties.title = 1;
                        {
                            var error = $root.proto.Message.HighlyStructuredMessage.verify(message.highlyStructuredMessage, q + 1);
                            if (error)
                                return "highlyStructuredMessage." + error;
                        }
                    }
                    if (message.imageMessage != null && Object.hasOwnProperty.call(message, "imageMessage")) {
                        if (properties.title === 1)
                            return "title: multiple values";
                        properties.title = 1;
                        {
                            var error = $root.proto.Message.ImageMessage.verify(message.imageMessage, q + 1);
                            if (error)
                                return "imageMessage." + error;
                        }
                    }
                    if (message.videoMessage != null && Object.hasOwnProperty.call(message, "videoMessage")) {
                        if (properties.title === 1)
                            return "title: multiple values";
                        properties.title = 1;
                        {
                            var error = $root.proto.Message.VideoMessage.verify(message.videoMessage, q + 1);
                            if (error)
                                return "videoMessage." + error;
                        }
                    }
                    if (message.locationMessage != null && Object.hasOwnProperty.call(message, "locationMessage")) {
                        if (properties.title === 1)
                            return "title: multiple values";
                        properties.title = 1;
                        {
                            var error = $root.proto.Message.LocationMessage.verify(message.locationMessage, q + 1);
                            if (error)
                                return "locationMessage." + error;
                        }
                    }
                    return null;
                };

                /**
                 * Creates a FourRowTemplate message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.TemplateMessage.FourRowTemplate
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.TemplateMessage.FourRowTemplate} FourRowTemplate
                 */
                FourRowTemplate.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.TemplateMessage.FourRowTemplate: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.content != null) {
                        if (!$util.isObject(object.content))
                            throw TypeError(".proto.Message.TemplateMessage.FourRowTemplate.content: object expected");
                        message.content = $root.proto.Message.HighlyStructuredMessage.fromObject(object.content, q + 1);
                    }
                    if (object.footer != null) {
                        if (!$util.isObject(object.footer))
                            throw TypeError(".proto.Message.TemplateMessage.FourRowTemplate.footer: object expected");
                        message.footer = $root.proto.Message.HighlyStructuredMessage.fromObject(object.footer, q + 1);
                    }
                    if (object.buttons) {
                        if (!Array.isArray(object.buttons))
                            throw TypeError(".proto.Message.TemplateMessage.FourRowTemplate.buttons: array expected");
                        message.buttons = Array(object.buttons.length);
                        for (var i = 0; i < object.buttons.length; ++i) {
                            if (!$util.isObject(object.buttons[i]))
                                throw TypeError(".proto.Message.TemplateMessage.FourRowTemplate.buttons: object expected");
                            message.buttons[i] = $root.proto.TemplateButton.fromObject(object.buttons[i], q + 1);
                        }
                    }
                    if (object.documentMessage != null) {
                        if (!$util.isObject(object.documentMessage))
                            throw TypeError(".proto.Message.TemplateMessage.FourRowTemplate.documentMessage: object expected");
                        message.documentMessage = $root.proto.Message.DocumentMessage.fromObject(object.documentMessage, q + 1);
                    }
                    if (object.highlyStructuredMessage != null) {
                        if (!$util.isObject(object.highlyStructuredMessage))
                            throw TypeError(".proto.Message.TemplateMessage.FourRowTemplate.highlyStructuredMessage: object expected");
                        message.highlyStructuredMessage = $root.proto.Message.HighlyStructuredMessage.fromObject(object.highlyStructuredMessage, q + 1);
                    }
                    if (object.imageMessage != null) {
                        if (!$util.isObject(object.imageMessage))
                            throw TypeError(".proto.Message.TemplateMessage.FourRowTemplate.imageMessage: object expected");
                        message.imageMessage = $root.proto.Message.ImageMessage.fromObject(object.imageMessage, q + 1);
                    }
                    if (object.videoMessage != null) {
                        if (!$util.isObject(object.videoMessage))
                            throw TypeError(".proto.Message.TemplateMessage.FourRowTemplate.videoMessage: object expected");
                        message.videoMessage = $root.proto.Message.VideoMessage.fromObject(object.videoMessage, q + 1);
                    }
                    if (object.locationMessage != null) {
                        if (!$util.isObject(object.locationMessage))
                            throw TypeError(".proto.Message.TemplateMessage.FourRowTemplate.locationMessage: object expected");
                        message.locationMessage = $root.proto.Message.LocationMessage.fromObject(object.locationMessage, q + 1);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a FourRowTemplate message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.TemplateMessage.FourRowTemplate
                 * @static
                 * @param {proto.Message.TemplateMessage.FourRowTemplate} message FourRowTemplate
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                FourRowTemplate.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (options.arrays || options.defaults)
                        object.buttons = [];
                    if (message.documentMessage != null && Object.hasOwnProperty.call(message, "documentMessage")) {
                        object.documentMessage = $root.proto.Message.DocumentMessage.toObject(message.documentMessage, options, q + 1);
                        if (options.oneofs)
                            object.title = "documentMessage";
                    }
                    if (message.highlyStructuredMessage != null && Object.hasOwnProperty.call(message, "highlyStructuredMessage")) {
                        object.highlyStructuredMessage = $root.proto.Message.HighlyStructuredMessage.toObject(message.highlyStructuredMessage, options, q + 1);
                        if (options.oneofs)
                            object.title = "highlyStructuredMessage";
                    }
                    if (message.imageMessage != null && Object.hasOwnProperty.call(message, "imageMessage")) {
                        object.imageMessage = $root.proto.Message.ImageMessage.toObject(message.imageMessage, options, q + 1);
                        if (options.oneofs)
                            object.title = "imageMessage";
                    }
                    if (message.videoMessage != null && Object.hasOwnProperty.call(message, "videoMessage")) {
                        object.videoMessage = $root.proto.Message.VideoMessage.toObject(message.videoMessage, options, q + 1);
                        if (options.oneofs)
                            object.title = "videoMessage";
                    }
                    if (message.locationMessage != null && Object.hasOwnProperty.call(message, "locationMessage")) {
                        object.locationMessage = $root.proto.Message.LocationMessage.toObject(message.locationMessage, options, q + 1);
                        if (options.oneofs)
                            object.title = "locationMessage";
                    }
                    if (message.content != null && Object.hasOwnProperty.call(message, "content"))
                        object.content = $root.proto.Message.HighlyStructuredMessage.toObject(message.content, options, q + 1);
                    if (message.footer != null && Object.hasOwnProperty.call(message, "footer"))
                        object.footer = $root.proto.Message.HighlyStructuredMessage.toObject(message.footer, options, q + 1);
                    if (message.buttons && message.buttons.length) {
                        object.buttons = Array(message.buttons.length);
                        for (var j = 0; j < message.buttons.length; ++j)
                            object.buttons[j] = $root.proto.TemplateButton.toObject(message.buttons[j], options, q + 1);
                    }
                    return object;
                };

                /**
                 * Converts this FourRowTemplate to JSON.
                 * @function toJSON
                 * @memberof proto.Message.TemplateMessage.FourRowTemplate
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                FourRowTemplate.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for FourRowTemplate
                 * @function getTypeUrl
                 * @memberof proto.Message.TemplateMessage.FourRowTemplate
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                FourRowTemplate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.TemplateMessage.FourRowTemplate";
                };

                return FourRowTemplate;
            })();

            TemplateMessage.HydratedFourRowTemplate = (function() {

                /**
                 * Properties of a HydratedFourRowTemplate.
                 * @memberof proto.Message.TemplateMessage
                 * @interface IHydratedFourRowTemplate
                 * @property {string|null} [hydratedContentText] HydratedFourRowTemplate hydratedContentText
                 * @property {string|null} [hydratedFooterText] HydratedFourRowTemplate hydratedFooterText
                 * @property {Array.<proto.IHydratedTemplateButton>|null} [hydratedButtons] HydratedFourRowTemplate hydratedButtons
                 * @property {string|null} [templateId] HydratedFourRowTemplate templateId
                 * @property {boolean|null} [maskLinkedDevices] HydratedFourRowTemplate maskLinkedDevices
                 * @property {proto.Message.IDocumentMessage|null} [documentMessage] HydratedFourRowTemplate documentMessage
                 * @property {string|null} [hydratedTitleText] HydratedFourRowTemplate hydratedTitleText
                 * @property {proto.Message.IImageMessage|null} [imageMessage] HydratedFourRowTemplate imageMessage
                 * @property {proto.Message.IVideoMessage|null} [videoMessage] HydratedFourRowTemplate videoMessage
                 * @property {proto.Message.ILocationMessage|null} [locationMessage] HydratedFourRowTemplate locationMessage
                 */

                /**
                 * Constructs a new HydratedFourRowTemplate.
                 * @memberof proto.Message.TemplateMessage
                 * @classdesc Represents a HydratedFourRowTemplate.
                 * @implements IHydratedFourRowTemplate
                 * @constructor
                 * @param {proto.Message.TemplateMessage.IHydratedFourRowTemplate=} [properties] Properties to set
                 */
                function HydratedFourRowTemplate(properties) {
                    this.hydratedButtons = [];
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * HydratedFourRowTemplate hydratedContentText.
                 * @member {string|null|undefined} hydratedContentText
                 * @memberof proto.Message.TemplateMessage.HydratedFourRowTemplate
                 * @instance
                 */
                HydratedFourRowTemplate.prototype.hydratedContentText = null;

                /**
                 * HydratedFourRowTemplate hydratedFooterText.
                 * @member {string|null|undefined} hydratedFooterText
                 * @memberof proto.Message.TemplateMessage.HydratedFourRowTemplate
                 * @instance
                 */
                HydratedFourRowTemplate.prototype.hydratedFooterText = null;

                /**
                 * HydratedFourRowTemplate hydratedButtons.
                 * @member {Array.<proto.IHydratedTemplateButton>} hydratedButtons
                 * @memberof proto.Message.TemplateMessage.HydratedFourRowTemplate
                 * @instance
                 */
                HydratedFourRowTemplate.prototype.hydratedButtons = $util.emptyArray;

                /**
                 * HydratedFourRowTemplate templateId.
                 * @member {string|null|undefined} templateId
                 * @memberof proto.Message.TemplateMessage.HydratedFourRowTemplate
                 * @instance
                 */
                HydratedFourRowTemplate.prototype.templateId = null;

                /**
                 * HydratedFourRowTemplate maskLinkedDevices.
                 * @member {boolean|null|undefined} maskLinkedDevices
                 * @memberof proto.Message.TemplateMessage.HydratedFourRowTemplate
                 * @instance
                 */
                HydratedFourRowTemplate.prototype.maskLinkedDevices = null;

                /**
                 * HydratedFourRowTemplate documentMessage.
                 * @member {proto.Message.IDocumentMessage|null|undefined} documentMessage
                 * @memberof proto.Message.TemplateMessage.HydratedFourRowTemplate
                 * @instance
                 */
                HydratedFourRowTemplate.prototype.documentMessage = null;

                /**
                 * HydratedFourRowTemplate hydratedTitleText.
                 * @member {string|null|undefined} hydratedTitleText
                 * @memberof proto.Message.TemplateMessage.HydratedFourRowTemplate
                 * @instance
                 */
                HydratedFourRowTemplate.prototype.hydratedTitleText = null;

                /**
                 * HydratedFourRowTemplate imageMessage.
                 * @member {proto.Message.IImageMessage|null|undefined} imageMessage
                 * @memberof proto.Message.TemplateMessage.HydratedFourRowTemplate
                 * @instance
                 */
                HydratedFourRowTemplate.prototype.imageMessage = null;

                /**
                 * HydratedFourRowTemplate videoMessage.
                 * @member {proto.Message.IVideoMessage|null|undefined} videoMessage
                 * @memberof proto.Message.TemplateMessage.HydratedFourRowTemplate
                 * @instance
                 */
                HydratedFourRowTemplate.prototype.videoMessage = null;

                /**
                 * HydratedFourRowTemplate locationMessage.
                 * @member {proto.Message.ILocationMessage|null|undefined} locationMessage
                 * @memberof proto.Message.TemplateMessage.HydratedFourRowTemplate
                 * @instance
                 */
                HydratedFourRowTemplate.prototype.locationMessage = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HydratedFourRowTemplate.prototype, "_hydratedContentText", {
                    get: $util.oneOfGetter($oneOfFields = ["hydratedContentText"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HydratedFourRowTemplate.prototype, "_hydratedFooterText", {
                    get: $util.oneOfGetter($oneOfFields = ["hydratedFooterText"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HydratedFourRowTemplate.prototype, "_templateId", {
                    get: $util.oneOfGetter($oneOfFields = ["templateId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HydratedFourRowTemplate.prototype, "_maskLinkedDevices", {
                    get: $util.oneOfGetter($oneOfFields = ["maskLinkedDevices"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * HydratedFourRowTemplate title.
                 * @member {"documentMessage"|"hydratedTitleText"|"imageMessage"|"videoMessage"|"locationMessage"|undefined} title
                 * @memberof proto.Message.TemplateMessage.HydratedFourRowTemplate
                 * @instance
                 */
                Object.defineProperty(HydratedFourRowTemplate.prototype, "title", {
                    get: $util.oneOfGetter($oneOfFields = ["documentMessage", "hydratedTitleText", "imageMessage", "videoMessage", "locationMessage"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new HydratedFourRowTemplate instance using the specified properties.
                 * @function create
                 * @memberof proto.Message.TemplateMessage.HydratedFourRowTemplate
                 * @static
                 * @param {proto.Message.TemplateMessage.IHydratedFourRowTemplate=} [properties] Properties to set
                 * @returns {proto.Message.TemplateMessage.HydratedFourRowTemplate} HydratedFourRowTemplate instance
                 */
                HydratedFourRowTemplate.create = function create(properties) {
                    return new HydratedFourRowTemplate(properties);
                };

                /**
                 * Encodes the specified HydratedFourRowTemplate message. Does not implicitly {@link proto.Message.TemplateMessage.HydratedFourRowTemplate.verify|verify} messages.
                 * @function encode
                 * @memberof proto.Message.TemplateMessage.HydratedFourRowTemplate
                 * @static
                 * @param {proto.Message.TemplateMessage.IHydratedFourRowTemplate} message HydratedFourRowTemplate message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                HydratedFourRowTemplate.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.documentMessage != null && Object.hasOwnProperty.call(message, "documentMessage"))
                        $root.proto.Message.DocumentMessage.encode(message.documentMessage, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                    if (message.hydratedTitleText != null && Object.hasOwnProperty.call(message, "hydratedTitleText"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.hydratedTitleText);
                    if (message.imageMessage != null && Object.hasOwnProperty.call(message, "imageMessage"))
                        $root.proto.Message.ImageMessage.encode(message.imageMessage, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                    if (message.videoMessage != null && Object.hasOwnProperty.call(message, "videoMessage"))
                        $root.proto.Message.VideoMessage.encode(message.videoMessage, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                    if (message.locationMessage != null && Object.hasOwnProperty.call(message, "locationMessage"))
                        $root.proto.Message.LocationMessage.encode(message.locationMessage, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                    if (message.hydratedContentText != null && Object.hasOwnProperty.call(message, "hydratedContentText"))
                        writer.uint32(/* id 6, wireType 2 =*/50).string(message.hydratedContentText);
                    if (message.hydratedFooterText != null && Object.hasOwnProperty.call(message, "hydratedFooterText"))
                        writer.uint32(/* id 7, wireType 2 =*/58).string(message.hydratedFooterText);
                    if (message.hydratedButtons != null && message.hydratedButtons.length)
                        for (var i = 0; i < message.hydratedButtons.length; ++i)
                            $root.proto.HydratedTemplateButton.encode(message.hydratedButtons[i], writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
                    if (message.templateId != null && Object.hasOwnProperty.call(message, "templateId"))
                        writer.uint32(/* id 9, wireType 2 =*/74).string(message.templateId);
                    if (message.maskLinkedDevices != null && Object.hasOwnProperty.call(message, "maskLinkedDevices"))
                        writer.uint32(/* id 10, wireType 0 =*/80).bool(message.maskLinkedDevices);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified HydratedFourRowTemplate message, length delimited. Does not implicitly {@link proto.Message.TemplateMessage.HydratedFourRowTemplate.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.Message.TemplateMessage.HydratedFourRowTemplate
                 * @static
                 * @param {proto.Message.TemplateMessage.IHydratedFourRowTemplate} message HydratedFourRowTemplate message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                HydratedFourRowTemplate.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a HydratedFourRowTemplate message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.Message.TemplateMessage.HydratedFourRowTemplate
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.Message.TemplateMessage.HydratedFourRowTemplate} HydratedFourRowTemplate
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                HydratedFourRowTemplate.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 6: {
                                if (u !== 2)
                                    break;
                                message.hydratedContentText = reader.stringVerify();
                                message._hydratedContentText = "hydratedContentText";
                                continue;
                            }
                        case 7: {
                                if (u !== 2)
                                    break;
                                message.hydratedFooterText = reader.stringVerify();
                                message._hydratedFooterText = "hydratedFooterText";
                                continue;
                            }
                        case 8: {
                                if (u !== 2)
                                    break;
                                if (!(message.hydratedButtons && message.hydratedButtons.length))
                                    message.hydratedButtons = [];
                                message.hydratedButtons.push($root.proto.HydratedTemplateButton.decode(reader, reader.uint32(), undefined, q + 1));
                                continue;
                            }
                        case 9: {
                                if (u !== 2)
                                    break;
                                message.templateId = reader.stringVerify();
                                message._templateId = "templateId";
                                continue;
                            }
                        case 10: {
                                if (u !== 0)
                                    break;
                                message.maskLinkedDevices = reader.bool();
                                message._maskLinkedDevices = "maskLinkedDevices";
                                continue;
                            }
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.documentMessage = $root.proto.Message.DocumentMessage.decode(reader, reader.uint32(), undefined, q + 1, message.documentMessage);
                                message.title = "documentMessage";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.hydratedTitleText = reader.stringVerify();
                                message.title = "hydratedTitleText";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.imageMessage = $root.proto.Message.ImageMessage.decode(reader, reader.uint32(), undefined, q + 1, message.imageMessage);
                                message.title = "imageMessage";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                message.videoMessage = $root.proto.Message.VideoMessage.decode(reader, reader.uint32(), undefined, q + 1, message.videoMessage);
                                message.title = "videoMessage";
                                continue;
                            }
                        case 5: {
                                if (u !== 2)
                                    break;
                                message.locationMessage = $root.proto.Message.LocationMessage.decode(reader, reader.uint32(), undefined, q + 1, message.locationMessage);
                                message.title = "locationMessage";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a HydratedFourRowTemplate message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.Message.TemplateMessage.HydratedFourRowTemplate
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.Message.TemplateMessage.HydratedFourRowTemplate} HydratedFourRowTemplate
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                HydratedFourRowTemplate.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a HydratedFourRowTemplate message.
                 * @function verify
                 * @memberof proto.Message.TemplateMessage.HydratedFourRowTemplate
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                HydratedFourRowTemplate.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.hydratedContentText != null && Object.hasOwnProperty.call(message, "hydratedContentText")) {
                        properties._hydratedContentText = 1;
                        if (!$util.isString(message.hydratedContentText))
                            return "hydratedContentText: string expected";
                    }
                    if (message.hydratedFooterText != null && Object.hasOwnProperty.call(message, "hydratedFooterText")) {
                        properties._hydratedFooterText = 1;
                        if (!$util.isString(message.hydratedFooterText))
                            return "hydratedFooterText: string expected";
                    }
                    if (message.hydratedButtons != null && Object.hasOwnProperty.call(message, "hydratedButtons")) {
                        if (!Array.isArray(message.hydratedButtons))
                            return "hydratedButtons: array expected";
                        for (var i = 0; i < message.hydratedButtons.length; ++i) {
                            var error = $root.proto.HydratedTemplateButton.verify(message.hydratedButtons[i], q + 1);
                            if (error)
                                return "hydratedButtons." + error;
                        }
                    }
                    if (message.templateId != null && Object.hasOwnProperty.call(message, "templateId")) {
                        properties._templateId = 1;
                        if (!$util.isString(message.templateId))
                            return "templateId: string expected";
                    }
                    if (message.maskLinkedDevices != null && Object.hasOwnProperty.call(message, "maskLinkedDevices")) {
                        properties._maskLinkedDevices = 1;
                        if (typeof message.maskLinkedDevices !== "boolean")
                            return "maskLinkedDevices: boolean expected";
                    }
                    if (message.documentMessage != null && Object.hasOwnProperty.call(message, "documentMessage")) {
                        properties.title = 1;
                        {
                            var error = $root.proto.Message.DocumentMessage.verify(message.documentMessage, q + 1);
                            if (error)
                                return "documentMessage." + error;
                        }
                    }
                    if (message.hydratedTitleText != null && Object.hasOwnProperty.call(message, "hydratedTitleText")) {
                        if (properties.title === 1)
                            return "title: multiple values";
                        properties.title = 1;
                        if (!$util.isString(message.hydratedTitleText))
                            return "hydratedTitleText: string expected";
                    }
                    if (message.imageMessage != null && Object.hasOwnProperty.call(message, "imageMessage")) {
                        if (properties.title === 1)
                            return "title: multiple values";
                        properties.title = 1;
                        {
                            var error = $root.proto.Message.ImageMessage.verify(message.imageMessage, q + 1);
                            if (error)
                                return "imageMessage." + error;
                        }
                    }
                    if (message.videoMessage != null && Object.hasOwnProperty.call(message, "videoMessage")) {
                        if (properties.title === 1)
                            return "title: multiple values";
                        properties.title = 1;
                        {
                            var error = $root.proto.Message.VideoMessage.verify(message.videoMessage, q + 1);
                            if (error)
                                return "videoMessage." + error;
                        }
                    }
                    if (message.locationMessage != null && Object.hasOwnProperty.call(message, "locationMessage")) {
                        if (properties.title === 1)
                            return "title: multiple values";
                        properties.title = 1;
                        {
                            var error = $root.proto.Message.LocationMessage.verify(message.locationMessage, q + 1);
                            if (error)
                                return "locationMessage." + error;
                        }
                    }
                    return null;
                };

                /**
                 * Creates a HydratedFourRowTemplate message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.Message.TemplateMessage.HydratedFourRowTemplate
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.Message.TemplateMessage.HydratedFourRowTemplate} HydratedFourRowTemplate
                 */
                HydratedFourRowTemplate.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.Message.TemplateMessage.HydratedFourRowTemplate: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.hydratedContentText != null)
                        message.hydratedContentText = String(object.hydratedContentText);
                    if (object.hydratedFooterText != null)
                        message.hydratedFooterText = String(object.hydratedFooterText);
                    if (object.hydratedButtons) {
                        if (!Array.isArray(object.hydratedButtons))
                            throw TypeError(".proto.Message.TemplateMessage.HydratedFourRowTemplate.hydratedButtons: array expected");
                        message.hydratedButtons = Array(object.hydratedButtons.length);
                        for (var i = 0; i < object.hydratedButtons.length; ++i) {
                            if (!$util.isObject(object.hydratedButtons[i]))
                                throw TypeError(".proto.Message.TemplateMessage.HydratedFourRowTemplate.hydratedButtons: object expected");
                            message.hydratedButtons[i] = $root.proto.HydratedTemplateButton.fromObject(object.hydratedButtons[i], q + 1);
                        }
                    }
                    if (object.templateId != null)
                        message.templateId = String(object.templateId);
                    if (object.maskLinkedDevices != null)
                        message.maskLinkedDevices = Boolean(object.maskLinkedDevices);
                    if (object.documentMessage != null) {
                        if (!$util.isObject(object.documentMessage))
                            throw TypeError(".proto.Message.TemplateMessage.HydratedFourRowTemplate.documentMessage: object expected");
                        message.documentMessage = $root.proto.Message.DocumentMessage.fromObject(object.documentMessage, q + 1);
                    }
                    if (object.hydratedTitleText != null)
                        message.hydratedTitleText = String(object.hydratedTitleText);
                    if (object.imageMessage != null) {
                        if (!$util.isObject(object.imageMessage))
                            throw TypeError(".proto.Message.TemplateMessage.HydratedFourRowTemplate.imageMessage: object expected");
                        message.imageMessage = $root.proto.Message.ImageMessage.fromObject(object.imageMessage, q + 1);
                    }
                    if (object.videoMessage != null) {
                        if (!$util.isObject(object.videoMessage))
                            throw TypeError(".proto.Message.TemplateMessage.HydratedFourRowTemplate.videoMessage: object expected");
                        message.videoMessage = $root.proto.Message.VideoMessage.fromObject(object.videoMessage, q + 1);
                    }
                    if (object.locationMessage != null) {
                        if (!$util.isObject(object.locationMessage))
                            throw TypeError(".proto.Message.TemplateMessage.HydratedFourRowTemplate.locationMessage: object expected");
                        message.locationMessage = $root.proto.Message.LocationMessage.fromObject(object.locationMessage, q + 1);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a HydratedFourRowTemplate message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.Message.TemplateMessage.HydratedFourRowTemplate
                 * @static
                 * @param {proto.Message.TemplateMessage.HydratedFourRowTemplate} message HydratedFourRowTemplate
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                HydratedFourRowTemplate.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (options.arrays || options.defaults)
                        object.hydratedButtons = [];
                    if (message.documentMessage != null && Object.hasOwnProperty.call(message, "documentMessage")) {
                        object.documentMessage = $root.proto.Message.DocumentMessage.toObject(message.documentMessage, options, q + 1);
                        if (options.oneofs)
                            object.title = "documentMessage";
                    }
                    if (message.hydratedTitleText != null && Object.hasOwnProperty.call(message, "hydratedTitleText")) {
                        object.hydratedTitleText = message.hydratedTitleText;
                        if (options.oneofs)
                            object.title = "hydratedTitleText";
                    }
                    if (message.imageMessage != null && Object.hasOwnProperty.call(message, "imageMessage")) {
                        object.imageMessage = $root.proto.Message.ImageMessage.toObject(message.imageMessage, options, q + 1);
                        if (options.oneofs)
                            object.title = "imageMessage";
                    }
                    if (message.videoMessage != null && Object.hasOwnProperty.call(message, "videoMessage")) {
                        object.videoMessage = $root.proto.Message.VideoMessage.toObject(message.videoMessage, options, q + 1);
                        if (options.oneofs)
                            object.title = "videoMessage";
                    }
                    if (message.locationMessage != null && Object.hasOwnProperty.call(message, "locationMessage")) {
                        object.locationMessage = $root.proto.Message.LocationMessage.toObject(message.locationMessage, options, q + 1);
                        if (options.oneofs)
                            object.title = "locationMessage";
                    }
                    if (message.hydratedContentText != null && Object.hasOwnProperty.call(message, "hydratedContentText"))
                        object.hydratedContentText = message.hydratedContentText;
                    if (message.hydratedFooterText != null && Object.hasOwnProperty.call(message, "hydratedFooterText"))
                        object.hydratedFooterText = message.hydratedFooterText;
                    if (message.hydratedButtons && message.hydratedButtons.length) {
                        object.hydratedButtons = Array(message.hydratedButtons.length);
                        for (var j = 0; j < message.hydratedButtons.length; ++j)
                            object.hydratedButtons[j] = $root.proto.HydratedTemplateButton.toObject(message.hydratedButtons[j], options, q + 1);
                    }
                    if (message.templateId != null && Object.hasOwnProperty.call(message, "templateId"))
                        object.templateId = message.templateId;
                    if (message.maskLinkedDevices != null && Object.hasOwnProperty.call(message, "maskLinkedDevices"))
                        object.maskLinkedDevices = message.maskLinkedDevices;
                    return object;
                };

                /**
                 * Converts this HydratedFourRowTemplate to JSON.
                 * @function toJSON
                 * @memberof proto.Message.TemplateMessage.HydratedFourRowTemplate
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                HydratedFourRowTemplate.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for HydratedFourRowTemplate
                 * @function getTypeUrl
                 * @memberof proto.Message.TemplateMessage.HydratedFourRowTemplate
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                HydratedFourRowTemplate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.Message.TemplateMessage.HydratedFourRowTemplate";
                };

                return HydratedFourRowTemplate;
            })();

            return TemplateMessage;
        })();

        Message.URLMetadata = (function() {

            /**
             * Properties of a URLMetadata.
             * @memberof proto.Message
             * @interface IURLMetadata
             * @property {number|null} [fbExperimentId] URLMetadata fbExperimentId
             */

            /**
             * Constructs a new URLMetadata.
             * @memberof proto.Message
             * @classdesc Represents a URLMetadata.
             * @implements IURLMetadata
             * @constructor
             * @param {proto.Message.IURLMetadata=} [properties] Properties to set
             */
            function URLMetadata(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * URLMetadata fbExperimentId.
             * @member {number|null|undefined} fbExperimentId
             * @memberof proto.Message.URLMetadata
             * @instance
             */
            URLMetadata.prototype.fbExperimentId = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(URLMetadata.prototype, "_fbExperimentId", {
                get: $util.oneOfGetter($oneOfFields = ["fbExperimentId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new URLMetadata instance using the specified properties.
             * @function create
             * @memberof proto.Message.URLMetadata
             * @static
             * @param {proto.Message.IURLMetadata=} [properties] Properties to set
             * @returns {proto.Message.URLMetadata} URLMetadata instance
             */
            URLMetadata.create = function create(properties) {
                return new URLMetadata(properties);
            };

            /**
             * Encodes the specified URLMetadata message. Does not implicitly {@link proto.Message.URLMetadata.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.URLMetadata
             * @static
             * @param {proto.Message.IURLMetadata} message URLMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            URLMetadata.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.fbExperimentId != null && Object.hasOwnProperty.call(message, "fbExperimentId"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.fbExperimentId);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified URLMetadata message, length delimited. Does not implicitly {@link proto.Message.URLMetadata.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.URLMetadata
             * @static
             * @param {proto.Message.IURLMetadata} message URLMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            URLMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a URLMetadata message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.URLMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.URLMetadata} URLMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            URLMetadata.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.fbExperimentId = reader.uint32();
                            message._fbExperimentId = "fbExperimentId";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a URLMetadata message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.URLMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.URLMetadata} URLMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            URLMetadata.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a URLMetadata message.
             * @function verify
             * @memberof proto.Message.URLMetadata
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            URLMetadata.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.fbExperimentId != null && Object.hasOwnProperty.call(message, "fbExperimentId")) {
                    properties._fbExperimentId = 1;
                    if (!$util.isInteger(message.fbExperimentId))
                        return "fbExperimentId: integer expected";
                }
                return null;
            };

            /**
             * Creates a URLMetadata message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.URLMetadata
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.URLMetadata} URLMetadata
             */
            URLMetadata.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.URLMetadata: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.fbExperimentId != null)
                    message.fbExperimentId = object.fbExperimentId >>> 0;
                return message;
            };

            /**
             * Creates a plain object from a URLMetadata message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.URLMetadata
             * @static
             * @param {proto.Message.URLMetadata} message URLMetadata
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            URLMetadata.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.fbExperimentId != null && Object.hasOwnProperty.call(message, "fbExperimentId"))
                    object.fbExperimentId = message.fbExperimentId;
                return object;
            };

            /**
             * Converts this URLMetadata to JSON.
             * @function toJSON
             * @memberof proto.Message.URLMetadata
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            URLMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for URLMetadata
             * @function getTypeUrl
             * @memberof proto.Message.URLMetadata
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            URLMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.URLMetadata";
            };

            return URLMetadata;
        })();

        Message.VideoEndCard = (function() {

            /**
             * Properties of a VideoEndCard.
             * @memberof proto.Message
             * @interface IVideoEndCard
             * @property {string|null} [username] VideoEndCard username
             * @property {string|null} [caption] VideoEndCard caption
             * @property {string|null} [thumbnailImageUrl] VideoEndCard thumbnailImageUrl
             * @property {string|null} [profilePictureUrl] VideoEndCard profilePictureUrl
             */

            /**
             * Constructs a new VideoEndCard.
             * @memberof proto.Message
             * @classdesc Represents a VideoEndCard.
             * @implements IVideoEndCard
             * @constructor
             * @param {proto.Message.IVideoEndCard=} [properties] Properties to set
             */
            function VideoEndCard(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * VideoEndCard username.
             * @member {string|null|undefined} username
             * @memberof proto.Message.VideoEndCard
             * @instance
             */
            VideoEndCard.prototype.username = null;

            /**
             * VideoEndCard caption.
             * @member {string|null|undefined} caption
             * @memberof proto.Message.VideoEndCard
             * @instance
             */
            VideoEndCard.prototype.caption = null;

            /**
             * VideoEndCard thumbnailImageUrl.
             * @member {string|null|undefined} thumbnailImageUrl
             * @memberof proto.Message.VideoEndCard
             * @instance
             */
            VideoEndCard.prototype.thumbnailImageUrl = null;

            /**
             * VideoEndCard profilePictureUrl.
             * @member {string|null|undefined} profilePictureUrl
             * @memberof proto.Message.VideoEndCard
             * @instance
             */
            VideoEndCard.prototype.profilePictureUrl = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoEndCard.prototype, "_username", {
                get: $util.oneOfGetter($oneOfFields = ["username"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoEndCard.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoEndCard.prototype, "_thumbnailImageUrl", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailImageUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoEndCard.prototype, "_profilePictureUrl", {
                get: $util.oneOfGetter($oneOfFields = ["profilePictureUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new VideoEndCard instance using the specified properties.
             * @function create
             * @memberof proto.Message.VideoEndCard
             * @static
             * @param {proto.Message.IVideoEndCard=} [properties] Properties to set
             * @returns {proto.Message.VideoEndCard} VideoEndCard instance
             */
            VideoEndCard.create = function create(properties) {
                return new VideoEndCard(properties);
            };

            /**
             * Encodes the specified VideoEndCard message. Does not implicitly {@link proto.Message.VideoEndCard.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.VideoEndCard
             * @static
             * @param {proto.Message.IVideoEndCard} message VideoEndCard message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            VideoEndCard.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.username);
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.caption);
                if (message.thumbnailImageUrl != null && Object.hasOwnProperty.call(message, "thumbnailImageUrl"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.thumbnailImageUrl);
                if (message.profilePictureUrl != null && Object.hasOwnProperty.call(message, "profilePictureUrl"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.profilePictureUrl);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified VideoEndCard message, length delimited. Does not implicitly {@link proto.Message.VideoEndCard.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.VideoEndCard
             * @static
             * @param {proto.Message.IVideoEndCard} message VideoEndCard message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            VideoEndCard.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a VideoEndCard message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.VideoEndCard
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.VideoEndCard} VideoEndCard
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            VideoEndCard.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.username = reader.stringVerify();
                            message._username = "username";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.caption = reader.stringVerify();
                            message._caption = "caption";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.thumbnailImageUrl = reader.stringVerify();
                            message._thumbnailImageUrl = "thumbnailImageUrl";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.profilePictureUrl = reader.stringVerify();
                            message._profilePictureUrl = "profilePictureUrl";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a VideoEndCard message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.VideoEndCard
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.VideoEndCard} VideoEndCard
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            VideoEndCard.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a VideoEndCard message.
             * @function verify
             * @memberof proto.Message.VideoEndCard
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            VideoEndCard.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.username != null && Object.hasOwnProperty.call(message, "username")) {
                    properties._username = 1;
                    if (!$util.isString(message.username))
                        return "username: string expected";
                }
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption")) {
                    properties._caption = 1;
                    if (!$util.isString(message.caption))
                        return "caption: string expected";
                }
                if (message.thumbnailImageUrl != null && Object.hasOwnProperty.call(message, "thumbnailImageUrl")) {
                    properties._thumbnailImageUrl = 1;
                    if (!$util.isString(message.thumbnailImageUrl))
                        return "thumbnailImageUrl: string expected";
                }
                if (message.profilePictureUrl != null && Object.hasOwnProperty.call(message, "profilePictureUrl")) {
                    properties._profilePictureUrl = 1;
                    if (!$util.isString(message.profilePictureUrl))
                        return "profilePictureUrl: string expected";
                }
                return null;
            };

            /**
             * Creates a VideoEndCard message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.VideoEndCard
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.VideoEndCard} VideoEndCard
             */
            VideoEndCard.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.VideoEndCard: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.username != null)
                    message.username = String(object.username);
                if (object.caption != null)
                    message.caption = String(object.caption);
                if (object.thumbnailImageUrl != null)
                    message.thumbnailImageUrl = String(object.thumbnailImageUrl);
                if (object.profilePictureUrl != null)
                    message.profilePictureUrl = String(object.profilePictureUrl);
                return message;
            };

            /**
             * Creates a plain object from a VideoEndCard message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.VideoEndCard
             * @static
             * @param {proto.Message.VideoEndCard} message VideoEndCard
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            VideoEndCard.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                    object.username = message.username;
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    object.caption = message.caption;
                if (message.thumbnailImageUrl != null && Object.hasOwnProperty.call(message, "thumbnailImageUrl"))
                    object.thumbnailImageUrl = message.thumbnailImageUrl;
                if (message.profilePictureUrl != null && Object.hasOwnProperty.call(message, "profilePictureUrl"))
                    object.profilePictureUrl = message.profilePictureUrl;
                return object;
            };

            /**
             * Converts this VideoEndCard to JSON.
             * @function toJSON
             * @memberof proto.Message.VideoEndCard
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            VideoEndCard.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for VideoEndCard
             * @function getTypeUrl
             * @memberof proto.Message.VideoEndCard
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            VideoEndCard.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.VideoEndCard";
            };

            return VideoEndCard;
        })();

        Message.VideoMessage = (function() {

            /**
             * Properties of a VideoMessage.
             * @memberof proto.Message
             * @interface IVideoMessage
             * @property {string|null} [url] VideoMessage url
             * @property {string|null} [mimetype] VideoMessage mimetype
             * @property {Uint8Array|null} [fileSha256] VideoMessage fileSha256
             * @property {number|Long|null} [fileLength] VideoMessage fileLength
             * @property {number|null} [seconds] VideoMessage seconds
             * @property {Uint8Array|null} [mediaKey] VideoMessage mediaKey
             * @property {string|null} [caption] VideoMessage caption
             * @property {boolean|null} [gifPlayback] VideoMessage gifPlayback
             * @property {number|null} [height] VideoMessage height
             * @property {number|null} [width] VideoMessage width
             * @property {Uint8Array|null} [fileEncSha256] VideoMessage fileEncSha256
             * @property {Array.<proto.IInteractiveAnnotation>|null} [interactiveAnnotations] VideoMessage interactiveAnnotations
             * @property {string|null} [directPath] VideoMessage directPath
             * @property {number|Long|null} [mediaKeyTimestamp] VideoMessage mediaKeyTimestamp
             * @property {Uint8Array|null} [jpegThumbnail] VideoMessage jpegThumbnail
             * @property {proto.IContextInfo|null} [contextInfo] VideoMessage contextInfo
             * @property {Uint8Array|null} [streamingSidecar] VideoMessage streamingSidecar
             * @property {proto.Message.VideoMessage.Attribution|null} [gifAttribution] VideoMessage gifAttribution
             * @property {boolean|null} [viewOnce] VideoMessage viewOnce
             * @property {string|null} [thumbnailDirectPath] VideoMessage thumbnailDirectPath
             * @property {Uint8Array|null} [thumbnailSha256] VideoMessage thumbnailSha256
             * @property {Uint8Array|null} [thumbnailEncSha256] VideoMessage thumbnailEncSha256
             * @property {string|null} [staticUrl] VideoMessage staticUrl
             * @property {Array.<proto.IInteractiveAnnotation>|null} [annotations] VideoMessage annotations
             * @property {string|null} [accessibilityLabel] VideoMessage accessibilityLabel
             * @property {Array.<proto.IProcessedVideo>|null} [processedVideos] VideoMessage processedVideos
             * @property {number|null} [externalShareFullVideoDurationInSeconds] VideoMessage externalShareFullVideoDurationInSeconds
             * @property {number|Long|null} [motionPhotoPresentationOffsetMs] VideoMessage motionPhotoPresentationOffsetMs
             * @property {string|null} [metadataUrl] VideoMessage metadataUrl
             * @property {proto.Message.VideoMessage.VideoSourceType|null} [videoSourceType] VideoMessage videoSourceType
             */

            /**
             * Constructs a new VideoMessage.
             * @memberof proto.Message
             * @classdesc Represents a VideoMessage.
             * @implements IVideoMessage
             * @constructor
             * @param {proto.Message.IVideoMessage=} [properties] Properties to set
             */
            function VideoMessage(properties) {
                this.interactiveAnnotations = [];
                this.annotations = [];
                this.processedVideos = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * VideoMessage url.
             * @member {string|null|undefined} url
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.url = null;

            /**
             * VideoMessage mimetype.
             * @member {string|null|undefined} mimetype
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.mimetype = null;

            /**
             * VideoMessage fileSha256.
             * @member {Uint8Array|null|undefined} fileSha256
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.fileSha256 = null;

            /**
             * VideoMessage fileLength.
             * @member {number|Long|null|undefined} fileLength
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.fileLength = null;

            /**
             * VideoMessage seconds.
             * @member {number|null|undefined} seconds
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.seconds = null;

            /**
             * VideoMessage mediaKey.
             * @member {Uint8Array|null|undefined} mediaKey
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.mediaKey = null;

            /**
             * VideoMessage caption.
             * @member {string|null|undefined} caption
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.caption = null;

            /**
             * VideoMessage gifPlayback.
             * @member {boolean|null|undefined} gifPlayback
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.gifPlayback = null;

            /**
             * VideoMessage height.
             * @member {number|null|undefined} height
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.height = null;

            /**
             * VideoMessage width.
             * @member {number|null|undefined} width
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.width = null;

            /**
             * VideoMessage fileEncSha256.
             * @member {Uint8Array|null|undefined} fileEncSha256
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.fileEncSha256 = null;

            /**
             * VideoMessage interactiveAnnotations.
             * @member {Array.<proto.IInteractiveAnnotation>} interactiveAnnotations
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.interactiveAnnotations = $util.emptyArray;

            /**
             * VideoMessage directPath.
             * @member {string|null|undefined} directPath
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.directPath = null;

            /**
             * VideoMessage mediaKeyTimestamp.
             * @member {number|Long|null|undefined} mediaKeyTimestamp
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.mediaKeyTimestamp = null;

            /**
             * VideoMessage jpegThumbnail.
             * @member {Uint8Array|null|undefined} jpegThumbnail
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.jpegThumbnail = null;

            /**
             * VideoMessage contextInfo.
             * @member {proto.IContextInfo|null|undefined} contextInfo
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.contextInfo = null;

            /**
             * VideoMessage streamingSidecar.
             * @member {Uint8Array|null|undefined} streamingSidecar
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.streamingSidecar = null;

            /**
             * VideoMessage gifAttribution.
             * @member {proto.Message.VideoMessage.Attribution|null|undefined} gifAttribution
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.gifAttribution = null;

            /**
             * VideoMessage viewOnce.
             * @member {boolean|null|undefined} viewOnce
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.viewOnce = null;

            /**
             * VideoMessage thumbnailDirectPath.
             * @member {string|null|undefined} thumbnailDirectPath
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.thumbnailDirectPath = null;

            /**
             * VideoMessage thumbnailSha256.
             * @member {Uint8Array|null|undefined} thumbnailSha256
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.thumbnailSha256 = null;

            /**
             * VideoMessage thumbnailEncSha256.
             * @member {Uint8Array|null|undefined} thumbnailEncSha256
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.thumbnailEncSha256 = null;

            /**
             * VideoMessage staticUrl.
             * @member {string|null|undefined} staticUrl
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.staticUrl = null;

            /**
             * VideoMessage annotations.
             * @member {Array.<proto.IInteractiveAnnotation>} annotations
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.annotations = $util.emptyArray;

            /**
             * VideoMessage accessibilityLabel.
             * @member {string|null|undefined} accessibilityLabel
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.accessibilityLabel = null;

            /**
             * VideoMessage processedVideos.
             * @member {Array.<proto.IProcessedVideo>} processedVideos
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.processedVideos = $util.emptyArray;

            /**
             * VideoMessage externalShareFullVideoDurationInSeconds.
             * @member {number|null|undefined} externalShareFullVideoDurationInSeconds
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.externalShareFullVideoDurationInSeconds = null;

            /**
             * VideoMessage motionPhotoPresentationOffsetMs.
             * @member {number|Long|null|undefined} motionPhotoPresentationOffsetMs
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.motionPhotoPresentationOffsetMs = null;

            /**
             * VideoMessage metadataUrl.
             * @member {string|null|undefined} metadataUrl
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.metadataUrl = null;

            /**
             * VideoMessage videoSourceType.
             * @member {proto.Message.VideoMessage.VideoSourceType|null|undefined} videoSourceType
             * @memberof proto.Message.VideoMessage
             * @instance
             */
            VideoMessage.prototype.videoSourceType = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_mimetype", {
                get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_fileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_fileLength", {
                get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_seconds", {
                get: $util.oneOfGetter($oneOfFields = ["seconds"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_gifPlayback", {
                get: $util.oneOfGetter($oneOfFields = ["gifPlayback"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_height", {
                get: $util.oneOfGetter($oneOfFields = ["height"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_width", {
                get: $util.oneOfGetter($oneOfFields = ["width"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_streamingSidecar", {
                get: $util.oneOfGetter($oneOfFields = ["streamingSidecar"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_gifAttribution", {
                get: $util.oneOfGetter($oneOfFields = ["gifAttribution"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_viewOnce", {
                get: $util.oneOfGetter($oneOfFields = ["viewOnce"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_thumbnailDirectPath", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailDirectPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_thumbnailSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_thumbnailEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_staticUrl", {
                get: $util.oneOfGetter($oneOfFields = ["staticUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_accessibilityLabel", {
                get: $util.oneOfGetter($oneOfFields = ["accessibilityLabel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_externalShareFullVideoDurationInSeconds", {
                get: $util.oneOfGetter($oneOfFields = ["externalShareFullVideoDurationInSeconds"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_motionPhotoPresentationOffsetMs", {
                get: $util.oneOfGetter($oneOfFields = ["motionPhotoPresentationOffsetMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_metadataUrl", {
                get: $util.oneOfGetter($oneOfFields = ["metadataUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(VideoMessage.prototype, "_videoSourceType", {
                get: $util.oneOfGetter($oneOfFields = ["videoSourceType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new VideoMessage instance using the specified properties.
             * @function create
             * @memberof proto.Message.VideoMessage
             * @static
             * @param {proto.Message.IVideoMessage=} [properties] Properties to set
             * @returns {proto.Message.VideoMessage} VideoMessage instance
             */
            VideoMessage.create = function create(properties) {
                return new VideoMessage(properties);
            };

            /**
             * Encodes the specified VideoMessage message. Does not implicitly {@link proto.Message.VideoMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.Message.VideoMessage
             * @static
             * @param {proto.Message.IVideoMessage} message VideoMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            VideoMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.url);
                if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.mimetype);
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.fileSha256);
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.fileLength);
                if (message.seconds != null && Object.hasOwnProperty.call(message, "seconds"))
                    writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.seconds);
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.mediaKey);
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.caption);
                if (message.gifPlayback != null && Object.hasOwnProperty.call(message, "gifPlayback"))
                    writer.uint32(/* id 8, wireType 0 =*/64).bool(message.gifPlayback);
                if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                    writer.uint32(/* id 9, wireType 0 =*/72).uint32(message.height);
                if (message.width != null && Object.hasOwnProperty.call(message, "width"))
                    writer.uint32(/* id 10, wireType 0 =*/80).uint32(message.width);
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    writer.uint32(/* id 11, wireType 2 =*/90).bytes(message.fileEncSha256);
                if (message.interactiveAnnotations != null && message.interactiveAnnotations.length)
                    for (var i = 0; i < message.interactiveAnnotations.length; ++i)
                        $root.proto.InteractiveAnnotation.encode(message.interactiveAnnotations[i], writer.uint32(/* id 12, wireType 2 =*/98).fork(), q + 1).ldelim();
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    writer.uint32(/* id 13, wireType 2 =*/106).string(message.directPath);
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                    writer.uint32(/* id 14, wireType 0 =*/112).int64(message.mediaKeyTimestamp);
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    writer.uint32(/* id 16, wireType 2 =*/130).bytes(message.jpegThumbnail);
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    $root.proto.ContextInfo.encode(message.contextInfo, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
                if (message.streamingSidecar != null && Object.hasOwnProperty.call(message, "streamingSidecar"))
                    writer.uint32(/* id 18, wireType 2 =*/146).bytes(message.streamingSidecar);
                if (message.gifAttribution != null && Object.hasOwnProperty.call(message, "gifAttribution"))
                    writer.uint32(/* id 19, wireType 0 =*/152).int32(message.gifAttribution);
                if (message.viewOnce != null && Object.hasOwnProperty.call(message, "viewOnce"))
                    writer.uint32(/* id 20, wireType 0 =*/160).bool(message.viewOnce);
                if (message.thumbnailDirectPath != null && Object.hasOwnProperty.call(message, "thumbnailDirectPath"))
                    writer.uint32(/* id 21, wireType 2 =*/170).string(message.thumbnailDirectPath);
                if (message.thumbnailSha256 != null && Object.hasOwnProperty.call(message, "thumbnailSha256"))
                    writer.uint32(/* id 22, wireType 2 =*/178).bytes(message.thumbnailSha256);
                if (message.thumbnailEncSha256 != null && Object.hasOwnProperty.call(message, "thumbnailEncSha256"))
                    writer.uint32(/* id 23, wireType 2 =*/186).bytes(message.thumbnailEncSha256);
                if (message.staticUrl != null && Object.hasOwnProperty.call(message, "staticUrl"))
                    writer.uint32(/* id 24, wireType 2 =*/194).string(message.staticUrl);
                if (message.annotations != null && message.annotations.length)
                    for (var i = 0; i < message.annotations.length; ++i)
                        $root.proto.InteractiveAnnotation.encode(message.annotations[i], writer.uint32(/* id 25, wireType 2 =*/202).fork(), q + 1).ldelim();
                if (message.accessibilityLabel != null && Object.hasOwnProperty.call(message, "accessibilityLabel"))
                    writer.uint32(/* id 26, wireType 2 =*/210).string(message.accessibilityLabel);
                if (message.processedVideos != null && message.processedVideos.length)
                    for (var i = 0; i < message.processedVideos.length; ++i)
                        $root.proto.ProcessedVideo.encode(message.processedVideos[i], writer.uint32(/* id 27, wireType 2 =*/218).fork(), q + 1).ldelim();
                if (message.externalShareFullVideoDurationInSeconds != null && Object.hasOwnProperty.call(message, "externalShareFullVideoDurationInSeconds"))
                    writer.uint32(/* id 28, wireType 0 =*/224).uint32(message.externalShareFullVideoDurationInSeconds);
                if (message.motionPhotoPresentationOffsetMs != null && Object.hasOwnProperty.call(message, "motionPhotoPresentationOffsetMs"))
                    writer.uint32(/* id 29, wireType 0 =*/232).uint64(message.motionPhotoPresentationOffsetMs);
                if (message.metadataUrl != null && Object.hasOwnProperty.call(message, "metadataUrl"))
                    writer.uint32(/* id 30, wireType 2 =*/242).string(message.metadataUrl);
                if (message.videoSourceType != null && Object.hasOwnProperty.call(message, "videoSourceType"))
                    writer.uint32(/* id 31, wireType 0 =*/248).int32(message.videoSourceType);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified VideoMessage message, length delimited. Does not implicitly {@link proto.Message.VideoMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.Message.VideoMessage
             * @static
             * @param {proto.Message.IVideoMessage} message VideoMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            VideoMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a VideoMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.Message.VideoMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.Message.VideoMessage} VideoMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            VideoMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.url = reader.stringVerify();
                            message._url = "url";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.mimetype = reader.stringVerify();
                            message._mimetype = "mimetype";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.fileSha256 = reader.bytes();
                            message._fileSha256 = "fileSha256";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.fileLength = reader.uint64();
                            message._fileLength = "fileLength";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.seconds = reader.uint32();
                            message._seconds = "seconds";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.mediaKey = reader.bytes();
                            message._mediaKey = "mediaKey";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.caption = reader.stringVerify();
                            message._caption = "caption";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            message.gifPlayback = reader.bool();
                            message._gifPlayback = "gifPlayback";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            message.height = reader.uint32();
                            message._height = "height";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            message.width = reader.uint32();
                            message._width = "width";
                            continue;
                        }
                    case 11: {
                            if (u !== 2)
                                break;
                            message.fileEncSha256 = reader.bytes();
                            message._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 12: {
                            if (u !== 2)
                                break;
                            if (!(message.interactiveAnnotations && message.interactiveAnnotations.length))
                                message.interactiveAnnotations = [];
                            message.interactiveAnnotations.push($root.proto.InteractiveAnnotation.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 13: {
                            if (u !== 2)
                                break;
                            message.directPath = reader.stringVerify();
                            message._directPath = "directPath";
                            continue;
                        }
                    case 14: {
                            if (u !== 0)
                                break;
                            message.mediaKeyTimestamp = reader.int64();
                            message._mediaKeyTimestamp = "mediaKeyTimestamp";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            message.jpegThumbnail = reader.bytes();
                            message._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            message.contextInfo = $root.proto.ContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.contextInfo);
                            message._contextInfo = "contextInfo";
                            continue;
                        }
                    case 18: {
                            if (u !== 2)
                                break;
                            message.streamingSidecar = reader.bytes();
                            message._streamingSidecar = "streamingSidecar";
                            continue;
                        }
                    case 19: {
                            if (u !== 0)
                                break;
                            message.gifAttribution = reader.int32();
                            message._gifAttribution = "gifAttribution";
                            continue;
                        }
                    case 20: {
                            if (u !== 0)
                                break;
                            message.viewOnce = reader.bool();
                            message._viewOnce = "viewOnce";
                            continue;
                        }
                    case 21: {
                            if (u !== 2)
                                break;
                            message.thumbnailDirectPath = reader.stringVerify();
                            message._thumbnailDirectPath = "thumbnailDirectPath";
                            continue;
                        }
                    case 22: {
                            if (u !== 2)
                                break;
                            message.thumbnailSha256 = reader.bytes();
                            message._thumbnailSha256 = "thumbnailSha256";
                            continue;
                        }
                    case 23: {
                            if (u !== 2)
                                break;
                            message.thumbnailEncSha256 = reader.bytes();
                            message._thumbnailEncSha256 = "thumbnailEncSha256";
                            continue;
                        }
                    case 24: {
                            if (u !== 2)
                                break;
                            message.staticUrl = reader.stringVerify();
                            message._staticUrl = "staticUrl";
                            continue;
                        }
                    case 25: {
                            if (u !== 2)
                                break;
                            if (!(message.annotations && message.annotations.length))
                                message.annotations = [];
                            message.annotations.push($root.proto.InteractiveAnnotation.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 26: {
                            if (u !== 2)
                                break;
                            message.accessibilityLabel = reader.stringVerify();
                            message._accessibilityLabel = "accessibilityLabel";
                            continue;
                        }
                    case 27: {
                            if (u !== 2)
                                break;
                            if (!(message.processedVideos && message.processedVideos.length))
                                message.processedVideos = [];
                            message.processedVideos.push($root.proto.ProcessedVideo.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 28: {
                            if (u !== 0)
                                break;
                            message.externalShareFullVideoDurationInSeconds = reader.uint32();
                            message._externalShareFullVideoDurationInSeconds = "externalShareFullVideoDurationInSeconds";
                            continue;
                        }
                    case 29: {
                            if (u !== 0)
                                break;
                            message.motionPhotoPresentationOffsetMs = reader.uint64();
                            message._motionPhotoPresentationOffsetMs = "motionPhotoPresentationOffsetMs";
                            continue;
                        }
                    case 30: {
                            if (u !== 2)
                                break;
                            message.metadataUrl = reader.stringVerify();
                            message._metadataUrl = "metadataUrl";
                            continue;
                        }
                    case 31: {
                            if (u !== 0)
                                break;
                            message.videoSourceType = reader.int32();
                            message._videoSourceType = "videoSourceType";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a VideoMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.Message.VideoMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.Message.VideoMessage} VideoMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            VideoMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a VideoMessage message.
             * @function verify
             * @memberof proto.Message.VideoMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            VideoMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.url != null && Object.hasOwnProperty.call(message, "url")) {
                    properties._url = 1;
                    if (!$util.isString(message.url))
                        return "url: string expected";
                }
                if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype")) {
                    properties._mimetype = 1;
                    if (!$util.isString(message.mimetype))
                        return "mimetype: string expected";
                }
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256")) {
                    properties._fileSha256 = 1;
                    if (!(message.fileSha256 && typeof message.fileSha256.length === "number" || $util.isString(message.fileSha256)))
                        return "fileSha256: buffer expected";
                }
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength")) {
                    properties._fileLength = 1;
                    if (!$util.isInteger(message.fileLength) && !(message.fileLength && $util.isInteger(message.fileLength.low) && $util.isInteger(message.fileLength.high)))
                        return "fileLength: integer|Long expected";
                }
                if (message.seconds != null && Object.hasOwnProperty.call(message, "seconds")) {
                    properties._seconds = 1;
                    if (!$util.isInteger(message.seconds))
                        return "seconds: integer expected";
                }
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey")) {
                    properties._mediaKey = 1;
                    if (!(message.mediaKey && typeof message.mediaKey.length === "number" || $util.isString(message.mediaKey)))
                        return "mediaKey: buffer expected";
                }
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption")) {
                    properties._caption = 1;
                    if (!$util.isString(message.caption))
                        return "caption: string expected";
                }
                if (message.gifPlayback != null && Object.hasOwnProperty.call(message, "gifPlayback")) {
                    properties._gifPlayback = 1;
                    if (typeof message.gifPlayback !== "boolean")
                        return "gifPlayback: boolean expected";
                }
                if (message.height != null && Object.hasOwnProperty.call(message, "height")) {
                    properties._height = 1;
                    if (!$util.isInteger(message.height))
                        return "height: integer expected";
                }
                if (message.width != null && Object.hasOwnProperty.call(message, "width")) {
                    properties._width = 1;
                    if (!$util.isInteger(message.width))
                        return "width: integer expected";
                }
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256")) {
                    properties._fileEncSha256 = 1;
                    if (!(message.fileEncSha256 && typeof message.fileEncSha256.length === "number" || $util.isString(message.fileEncSha256)))
                        return "fileEncSha256: buffer expected";
                }
                if (message.interactiveAnnotations != null && Object.hasOwnProperty.call(message, "interactiveAnnotations")) {
                    if (!Array.isArray(message.interactiveAnnotations))
                        return "interactiveAnnotations: array expected";
                    for (var i = 0; i < message.interactiveAnnotations.length; ++i) {
                        var error = $root.proto.InteractiveAnnotation.verify(message.interactiveAnnotations[i], q + 1);
                        if (error)
                            return "interactiveAnnotations." + error;
                    }
                }
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath")) {
                    properties._directPath = 1;
                    if (!$util.isString(message.directPath))
                        return "directPath: string expected";
                }
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp")) {
                    properties._mediaKeyTimestamp = 1;
                    if (!$util.isInteger(message.mediaKeyTimestamp) && !(message.mediaKeyTimestamp && $util.isInteger(message.mediaKeyTimestamp.low) && $util.isInteger(message.mediaKeyTimestamp.high)))
                        return "mediaKeyTimestamp: integer|Long expected";
                }
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail")) {
                    properties._jpegThumbnail = 1;
                    if (!(message.jpegThumbnail && typeof message.jpegThumbnail.length === "number" || $util.isString(message.jpegThumbnail)))
                        return "jpegThumbnail: buffer expected";
                }
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo")) {
                    properties._contextInfo = 1;
                    {
                        var error = $root.proto.ContextInfo.verify(message.contextInfo, q + 1);
                        if (error)
                            return "contextInfo." + error;
                    }
                }
                if (message.streamingSidecar != null && Object.hasOwnProperty.call(message, "streamingSidecar")) {
                    properties._streamingSidecar = 1;
                    if (!(message.streamingSidecar && typeof message.streamingSidecar.length === "number" || $util.isString(message.streamingSidecar)))
                        return "streamingSidecar: buffer expected";
                }
                if (message.gifAttribution != null && Object.hasOwnProperty.call(message, "gifAttribution")) {
                    properties._gifAttribution = 1;
                    if (typeof message.gifAttribution !== "number" || (message.gifAttribution | 0) !== message.gifAttribution)
                        return "gifAttribution: enum value expected";
                }
                if (message.viewOnce != null && Object.hasOwnProperty.call(message, "viewOnce")) {
                    properties._viewOnce = 1;
                    if (typeof message.viewOnce !== "boolean")
                        return "viewOnce: boolean expected";
                }
                if (message.thumbnailDirectPath != null && Object.hasOwnProperty.call(message, "thumbnailDirectPath")) {
                    properties._thumbnailDirectPath = 1;
                    if (!$util.isString(message.thumbnailDirectPath))
                        return "thumbnailDirectPath: string expected";
                }
                if (message.thumbnailSha256 != null && Object.hasOwnProperty.call(message, "thumbnailSha256")) {
                    properties._thumbnailSha256 = 1;
                    if (!(message.thumbnailSha256 && typeof message.thumbnailSha256.length === "number" || $util.isString(message.thumbnailSha256)))
                        return "thumbnailSha256: buffer expected";
                }
                if (message.thumbnailEncSha256 != null && Object.hasOwnProperty.call(message, "thumbnailEncSha256")) {
                    properties._thumbnailEncSha256 = 1;
                    if (!(message.thumbnailEncSha256 && typeof message.thumbnailEncSha256.length === "number" || $util.isString(message.thumbnailEncSha256)))
                        return "thumbnailEncSha256: buffer expected";
                }
                if (message.staticUrl != null && Object.hasOwnProperty.call(message, "staticUrl")) {
                    properties._staticUrl = 1;
                    if (!$util.isString(message.staticUrl))
                        return "staticUrl: string expected";
                }
                if (message.annotations != null && Object.hasOwnProperty.call(message, "annotations")) {
                    if (!Array.isArray(message.annotations))
                        return "annotations: array expected";
                    for (var i = 0; i < message.annotations.length; ++i) {
                        var error = $root.proto.InteractiveAnnotation.verify(message.annotations[i], q + 1);
                        if (error)
                            return "annotations." + error;
                    }
                }
                if (message.accessibilityLabel != null && Object.hasOwnProperty.call(message, "accessibilityLabel")) {
                    properties._accessibilityLabel = 1;
                    if (!$util.isString(message.accessibilityLabel))
                        return "accessibilityLabel: string expected";
                }
                if (message.processedVideos != null && Object.hasOwnProperty.call(message, "processedVideos")) {
                    if (!Array.isArray(message.processedVideos))
                        return "processedVideos: array expected";
                    for (var i = 0; i < message.processedVideos.length; ++i) {
                        var error = $root.proto.ProcessedVideo.verify(message.processedVideos[i], q + 1);
                        if (error)
                            return "processedVideos." + error;
                    }
                }
                if (message.externalShareFullVideoDurationInSeconds != null && Object.hasOwnProperty.call(message, "externalShareFullVideoDurationInSeconds")) {
                    properties._externalShareFullVideoDurationInSeconds = 1;
                    if (!$util.isInteger(message.externalShareFullVideoDurationInSeconds))
                        return "externalShareFullVideoDurationInSeconds: integer expected";
                }
                if (message.motionPhotoPresentationOffsetMs != null && Object.hasOwnProperty.call(message, "motionPhotoPresentationOffsetMs")) {
                    properties._motionPhotoPresentationOffsetMs = 1;
                    if (!$util.isInteger(message.motionPhotoPresentationOffsetMs) && !(message.motionPhotoPresentationOffsetMs && $util.isInteger(message.motionPhotoPresentationOffsetMs.low) && $util.isInteger(message.motionPhotoPresentationOffsetMs.high)))
                        return "motionPhotoPresentationOffsetMs: integer|Long expected";
                }
                if (message.metadataUrl != null && Object.hasOwnProperty.call(message, "metadataUrl")) {
                    properties._metadataUrl = 1;
                    if (!$util.isString(message.metadataUrl))
                        return "metadataUrl: string expected";
                }
                if (message.videoSourceType != null && Object.hasOwnProperty.call(message, "videoSourceType")) {
                    properties._videoSourceType = 1;
                    if (typeof message.videoSourceType !== "number" || (message.videoSourceType | 0) !== message.videoSourceType)
                        return "videoSourceType: enum value expected";
                }
                return null;
            };

            /**
             * Creates a VideoMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.Message.VideoMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.Message.VideoMessage} VideoMessage
             */
            VideoMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.Message.VideoMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.url != null)
                    message.url = String(object.url);
                if (object.mimetype != null)
                    message.mimetype = String(object.mimetype);
                if (object.fileSha256 != null)
                    if (typeof object.fileSha256 === "string")
                        $util.base64.decode(object.fileSha256, message.fileSha256 = $util.newBuffer($util.base64.length(object.fileSha256)), 0);
                    else if (object.fileSha256.length >= 0)
                        message.fileSha256 = object.fileSha256;
                if (object.fileLength != null)
                    if ($util.Long)
                        message.fileLength = $util.Long.fromValue(object.fileLength, true);
                    else if (typeof object.fileLength === "string")
                        message.fileLength = parseInt(object.fileLength, 10);
                    else if (typeof object.fileLength === "number")
                        message.fileLength = object.fileLength;
                    else if (typeof object.fileLength === "object")
                        message.fileLength = new $util.LongBits(object.fileLength.low >>> 0, object.fileLength.high >>> 0).toNumber(true);
                if (object.seconds != null)
                    message.seconds = object.seconds >>> 0;
                if (object.mediaKey != null)
                    if (typeof object.mediaKey === "string")
                        $util.base64.decode(object.mediaKey, message.mediaKey = $util.newBuffer($util.base64.length(object.mediaKey)), 0);
                    else if (object.mediaKey.length >= 0)
                        message.mediaKey = object.mediaKey;
                if (object.caption != null)
                    message.caption = String(object.caption);
                if (object.gifPlayback != null)
                    message.gifPlayback = Boolean(object.gifPlayback);
                if (object.height != null)
                    message.height = object.height >>> 0;
                if (object.width != null)
                    message.width = object.width >>> 0;
                if (object.fileEncSha256 != null)
                    if (typeof object.fileEncSha256 === "string")
                        $util.base64.decode(object.fileEncSha256, message.fileEncSha256 = $util.newBuffer($util.base64.length(object.fileEncSha256)), 0);
                    else if (object.fileEncSha256.length >= 0)
                        message.fileEncSha256 = object.fileEncSha256;
                if (object.interactiveAnnotations) {
                    if (!Array.isArray(object.interactiveAnnotations))
                        throw TypeError(".proto.Message.VideoMessage.interactiveAnnotations: array expected");
                    message.interactiveAnnotations = Array(object.interactiveAnnotations.length);
                    for (var i = 0; i < object.interactiveAnnotations.length; ++i) {
                        if (!$util.isObject(object.interactiveAnnotations[i]))
                            throw TypeError(".proto.Message.VideoMessage.interactiveAnnotations: object expected");
                        message.interactiveAnnotations[i] = $root.proto.InteractiveAnnotation.fromObject(object.interactiveAnnotations[i], q + 1);
                    }
                }
                if (object.directPath != null)
                    message.directPath = String(object.directPath);
                if (object.mediaKeyTimestamp != null)
                    if ($util.Long)
                        message.mediaKeyTimestamp = $util.Long.fromValue(object.mediaKeyTimestamp, false);
                    else if (typeof object.mediaKeyTimestamp === "string")
                        message.mediaKeyTimestamp = parseInt(object.mediaKeyTimestamp, 10);
                    else if (typeof object.mediaKeyTimestamp === "number")
                        message.mediaKeyTimestamp = object.mediaKeyTimestamp;
                    else if (typeof object.mediaKeyTimestamp === "object")
                        message.mediaKeyTimestamp = new $util.LongBits(object.mediaKeyTimestamp.low >>> 0, object.mediaKeyTimestamp.high >>> 0).toNumber();
                if (object.jpegThumbnail != null)
                    if (typeof object.jpegThumbnail === "string")
                        $util.base64.decode(object.jpegThumbnail, message.jpegThumbnail = $util.newBuffer($util.base64.length(object.jpegThumbnail)), 0);
                    else if (object.jpegThumbnail.length >= 0)
                        message.jpegThumbnail = object.jpegThumbnail;
                if (object.contextInfo != null) {
                    if (!$util.isObject(object.contextInfo))
                        throw TypeError(".proto.Message.VideoMessage.contextInfo: object expected");
                    message.contextInfo = $root.proto.ContextInfo.fromObject(object.contextInfo, q + 1);
                }
                if (object.streamingSidecar != null)
                    if (typeof object.streamingSidecar === "string")
                        $util.base64.decode(object.streamingSidecar, message.streamingSidecar = $util.newBuffer($util.base64.length(object.streamingSidecar)), 0);
                    else if (object.streamingSidecar.length >= 0)
                        message.streamingSidecar = object.streamingSidecar;
                switch (object.gifAttribution) {
                case "NONE":
                case 0:
                    message.gifAttribution = 0;
                    break;
                case "GIPHY":
                case 1:
                    message.gifAttribution = 1;
                    break;
                case "TENOR":
                case 2:
                    message.gifAttribution = 2;
                    break;
                case "KLIPY":
                case 3:
                    message.gifAttribution = 3;
                    break;
                default:
                    if (typeof object.gifAttribution === "number" && (object.gifAttribution | 0) === object.gifAttribution)
                        message.gifAttribution = object.gifAttribution;
                }
                if (object.viewOnce != null)
                    message.viewOnce = Boolean(object.viewOnce);
                if (object.thumbnailDirectPath != null)
                    message.thumbnailDirectPath = String(object.thumbnailDirectPath);
                if (object.thumbnailSha256 != null)
                    if (typeof object.thumbnailSha256 === "string")
                        $util.base64.decode(object.thumbnailSha256, message.thumbnailSha256 = $util.newBuffer($util.base64.length(object.thumbnailSha256)), 0);
                    else if (object.thumbnailSha256.length >= 0)
                        message.thumbnailSha256 = object.thumbnailSha256;
                if (object.thumbnailEncSha256 != null)
                    if (typeof object.thumbnailEncSha256 === "string")
                        $util.base64.decode(object.thumbnailEncSha256, message.thumbnailEncSha256 = $util.newBuffer($util.base64.length(object.thumbnailEncSha256)), 0);
                    else if (object.thumbnailEncSha256.length >= 0)
                        message.thumbnailEncSha256 = object.thumbnailEncSha256;
                if (object.staticUrl != null)
                    message.staticUrl = String(object.staticUrl);
                if (object.annotations) {
                    if (!Array.isArray(object.annotations))
                        throw TypeError(".proto.Message.VideoMessage.annotations: array expected");
                    message.annotations = Array(object.annotations.length);
                    for (var i = 0; i < object.annotations.length; ++i) {
                        if (!$util.isObject(object.annotations[i]))
                            throw TypeError(".proto.Message.VideoMessage.annotations: object expected");
                        message.annotations[i] = $root.proto.InteractiveAnnotation.fromObject(object.annotations[i], q + 1);
                    }
                }
                if (object.accessibilityLabel != null)
                    message.accessibilityLabel = String(object.accessibilityLabel);
                if (object.processedVideos) {
                    if (!Array.isArray(object.processedVideos))
                        throw TypeError(".proto.Message.VideoMessage.processedVideos: array expected");
                    message.processedVideos = Array(object.processedVideos.length);
                    for (var i = 0; i < object.processedVideos.length; ++i) {
                        if (!$util.isObject(object.processedVideos[i]))
                            throw TypeError(".proto.Message.VideoMessage.processedVideos: object expected");
                        message.processedVideos[i] = $root.proto.ProcessedVideo.fromObject(object.processedVideos[i], q + 1);
                    }
                }
                if (object.externalShareFullVideoDurationInSeconds != null)
                    message.externalShareFullVideoDurationInSeconds = object.externalShareFullVideoDurationInSeconds >>> 0;
                if (object.motionPhotoPresentationOffsetMs != null)
                    if ($util.Long)
                        message.motionPhotoPresentationOffsetMs = $util.Long.fromValue(object.motionPhotoPresentationOffsetMs, true);
                    else if (typeof object.motionPhotoPresentationOffsetMs === "string")
                        message.motionPhotoPresentationOffsetMs = parseInt(object.motionPhotoPresentationOffsetMs, 10);
                    else if (typeof object.motionPhotoPresentationOffsetMs === "number")
                        message.motionPhotoPresentationOffsetMs = object.motionPhotoPresentationOffsetMs;
                    else if (typeof object.motionPhotoPresentationOffsetMs === "object")
                        message.motionPhotoPresentationOffsetMs = new $util.LongBits(object.motionPhotoPresentationOffsetMs.low >>> 0, object.motionPhotoPresentationOffsetMs.high >>> 0).toNumber(true);
                if (object.metadataUrl != null)
                    message.metadataUrl = String(object.metadataUrl);
                switch (object.videoSourceType) {
                case "USER_VIDEO":
                case 0:
                    message.videoSourceType = 0;
                    break;
                case "AI_GENERATED":
                case 1:
                    message.videoSourceType = 1;
                    break;
                default:
                    if (typeof object.videoSourceType === "number" && (object.videoSourceType | 0) === object.videoSourceType)
                        message.videoSourceType = object.videoSourceType;
                }
                return message;
            };

            /**
             * Creates a plain object from a VideoMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.Message.VideoMessage
             * @static
             * @param {proto.Message.VideoMessage} message VideoMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            VideoMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.interactiveAnnotations = [];
                    object.annotations = [];
                    object.processedVideos = [];
                }
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    object.url = message.url;
                if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                    object.mimetype = message.mimetype;
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                    object.fileSha256 = options.bytes === String ? $util.base64.encode(message.fileSha256, 0, message.fileSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileSha256) : message.fileSha256;
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.fileLength = typeof message.fileLength === "number" ? BigInt(message.fileLength) : $util.Long.fromBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0, true).toBigInt();
                    else if (typeof message.fileLength === "number")
                        object.fileLength = options.longs === String ? String(message.fileLength) : message.fileLength;
                    else
                        object.fileLength = options.longs === String ? $util.Long.prototype.toString.call(message.fileLength) : options.longs === Number ? new $util.LongBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0).toNumber(true) : message.fileLength;
                if (message.seconds != null && Object.hasOwnProperty.call(message, "seconds"))
                    object.seconds = message.seconds;
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    object.mediaKey = options.bytes === String ? $util.base64.encode(message.mediaKey, 0, message.mediaKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.mediaKey) : message.mediaKey;
                if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                    object.caption = message.caption;
                if (message.gifPlayback != null && Object.hasOwnProperty.call(message, "gifPlayback"))
                    object.gifPlayback = message.gifPlayback;
                if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                    object.height = message.height;
                if (message.width != null && Object.hasOwnProperty.call(message, "width"))
                    object.width = message.width;
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    object.fileEncSha256 = options.bytes === String ? $util.base64.encode(message.fileEncSha256, 0, message.fileEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileEncSha256) : message.fileEncSha256;
                if (message.interactiveAnnotations && message.interactiveAnnotations.length) {
                    object.interactiveAnnotations = Array(message.interactiveAnnotations.length);
                    for (var j = 0; j < message.interactiveAnnotations.length; ++j)
                        object.interactiveAnnotations[j] = $root.proto.InteractiveAnnotation.toObject(message.interactiveAnnotations[j], options, q + 1);
                }
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    object.directPath = message.directPath;
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.mediaKeyTimestamp = typeof message.mediaKeyTimestamp === "number" ? BigInt(message.mediaKeyTimestamp) : $util.Long.fromBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.mediaKeyTimestamp === "number")
                        object.mediaKeyTimestamp = options.longs === String ? String(message.mediaKeyTimestamp) : message.mediaKeyTimestamp;
                    else
                        object.mediaKeyTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.mediaKeyTimestamp) : options.longs === Number ? new $util.LongBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0).toNumber() : message.mediaKeyTimestamp;
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    object.jpegThumbnail = options.bytes === String ? $util.base64.encode(message.jpegThumbnail, 0, message.jpegThumbnail.length) : options.bytes === Array ? Array.prototype.slice.call(message.jpegThumbnail) : message.jpegThumbnail;
                if (message.contextInfo != null && Object.hasOwnProperty.call(message, "contextInfo"))
                    object.contextInfo = $root.proto.ContextInfo.toObject(message.contextInfo, options, q + 1);
                if (message.streamingSidecar != null && Object.hasOwnProperty.call(message, "streamingSidecar"))
                    object.streamingSidecar = options.bytes === String ? $util.base64.encode(message.streamingSidecar, 0, message.streamingSidecar.length) : options.bytes === Array ? Array.prototype.slice.call(message.streamingSidecar) : message.streamingSidecar;
                if (message.gifAttribution != null && Object.hasOwnProperty.call(message, "gifAttribution"))
                    object.gifAttribution = options.enums === String ? $root.proto.Message.VideoMessage.Attribution[message.gifAttribution] === undefined ? message.gifAttribution : $root.proto.Message.VideoMessage.Attribution[message.gifAttribution] : message.gifAttribution;
                if (message.viewOnce != null && Object.hasOwnProperty.call(message, "viewOnce"))
                    object.viewOnce = message.viewOnce;
                if (message.thumbnailDirectPath != null && Object.hasOwnProperty.call(message, "thumbnailDirectPath"))
                    object.thumbnailDirectPath = message.thumbnailDirectPath;
                if (message.thumbnailSha256 != null && Object.hasOwnProperty.call(message, "thumbnailSha256"))
                    object.thumbnailSha256 = options.bytes === String ? $util.base64.encode(message.thumbnailSha256, 0, message.thumbnailSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.thumbnailSha256) : message.thumbnailSha256;
                if (message.thumbnailEncSha256 != null && Object.hasOwnProperty.call(message, "thumbnailEncSha256"))
                    object.thumbnailEncSha256 = options.bytes === String ? $util.base64.encode(message.thumbnailEncSha256, 0, message.thumbnailEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.thumbnailEncSha256) : message.thumbnailEncSha256;
                if (message.staticUrl != null && Object.hasOwnProperty.call(message, "staticUrl"))
                    object.staticUrl = message.staticUrl;
                if (message.annotations && message.annotations.length) {
                    object.annotations = Array(message.annotations.length);
                    for (var j = 0; j < message.annotations.length; ++j)
                        object.annotations[j] = $root.proto.InteractiveAnnotation.toObject(message.annotations[j], options, q + 1);
                }
                if (message.accessibilityLabel != null && Object.hasOwnProperty.call(message, "accessibilityLabel"))
                    object.accessibilityLabel = message.accessibilityLabel;
                if (message.processedVideos && message.processedVideos.length) {
                    object.processedVideos = Array(message.processedVideos.length);
                    for (var j = 0; j < message.processedVideos.length; ++j)
                        object.processedVideos[j] = $root.proto.ProcessedVideo.toObject(message.processedVideos[j], options, q + 1);
                }
                if (message.externalShareFullVideoDurationInSeconds != null && Object.hasOwnProperty.call(message, "externalShareFullVideoDurationInSeconds"))
                    object.externalShareFullVideoDurationInSeconds = message.externalShareFullVideoDurationInSeconds;
                if (message.motionPhotoPresentationOffsetMs != null && Object.hasOwnProperty.call(message, "motionPhotoPresentationOffsetMs"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.motionPhotoPresentationOffsetMs = typeof message.motionPhotoPresentationOffsetMs === "number" ? BigInt(message.motionPhotoPresentationOffsetMs) : $util.Long.fromBits(message.motionPhotoPresentationOffsetMs.low >>> 0, message.motionPhotoPresentationOffsetMs.high >>> 0, true).toBigInt();
                    else if (typeof message.motionPhotoPresentationOffsetMs === "number")
                        object.motionPhotoPresentationOffsetMs = options.longs === String ? String(message.motionPhotoPresentationOffsetMs) : message.motionPhotoPresentationOffsetMs;
                    else
                        object.motionPhotoPresentationOffsetMs = options.longs === String ? $util.Long.prototype.toString.call(message.motionPhotoPresentationOffsetMs) : options.longs === Number ? new $util.LongBits(message.motionPhotoPresentationOffsetMs.low >>> 0, message.motionPhotoPresentationOffsetMs.high >>> 0).toNumber(true) : message.motionPhotoPresentationOffsetMs;
                if (message.metadataUrl != null && Object.hasOwnProperty.call(message, "metadataUrl"))
                    object.metadataUrl = message.metadataUrl;
                if (message.videoSourceType != null && Object.hasOwnProperty.call(message, "videoSourceType"))
                    object.videoSourceType = options.enums === String ? $root.proto.Message.VideoMessage.VideoSourceType[message.videoSourceType] === undefined ? message.videoSourceType : $root.proto.Message.VideoMessage.VideoSourceType[message.videoSourceType] : message.videoSourceType;
                return object;
            };

            /**
             * Converts this VideoMessage to JSON.
             * @function toJSON
             * @memberof proto.Message.VideoMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            VideoMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for VideoMessage
             * @function getTypeUrl
             * @memberof proto.Message.VideoMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            VideoMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.Message.VideoMessage";
            };

            /**
             * Attribution enum.
             * @name proto.Message.VideoMessage.Attribution
             * @enum {number}
             * @property {number} NONE=0 NONE value
             * @property {number} GIPHY=1 GIPHY value
             * @property {number} TENOR=2 TENOR value
             * @property {number} KLIPY=3 KLIPY value
             */
            VideoMessage.Attribution = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "NONE"] = 0;
                values[valuesById[1] = "GIPHY"] = 1;
                values[valuesById[2] = "TENOR"] = 2;
                values[valuesById[3] = "KLIPY"] = 3;
                return values;
            })();

            /**
             * VideoSourceType enum.
             * @name proto.Message.VideoMessage.VideoSourceType
             * @enum {number}
             * @property {number} USER_VIDEO=0 USER_VIDEO value
             * @property {number} AI_GENERATED=1 AI_GENERATED value
             */
            VideoMessage.VideoSourceType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "USER_VIDEO"] = 0;
                values[valuesById[1] = "AI_GENERATED"] = 1;
                return values;
            })();

            return VideoMessage;
        })();

        return Message;
    })();

    proto.MessageAddOn = (function() {

        /**
         * Properties of a MessageAddOn.
         * @memberof proto
         * @interface IMessageAddOn
         * @property {proto.MessageAddOn.MessageAddOnType|null} [messageAddOnType] MessageAddOn messageAddOnType
         * @property {proto.IMessage|null} [messageAddOn] MessageAddOn messageAddOn
         * @property {number|Long|null} [senderTimestampMs] MessageAddOn senderTimestampMs
         * @property {number|Long|null} [serverTimestampMs] MessageAddOn serverTimestampMs
         * @property {proto.WebMessageInfo.Status|null} [status] MessageAddOn status
         * @property {proto.IMessageAddOnContextInfo|null} [addOnContextInfo] MessageAddOn addOnContextInfo
         * @property {proto.IMessageKey|null} [messageAddOnKey] MessageAddOn messageAddOnKey
         * @property {proto.ILegacyMessage|null} [legacyMessage] MessageAddOn legacyMessage
         */

        /**
         * Constructs a new MessageAddOn.
         * @memberof proto
         * @classdesc Represents a MessageAddOn.
         * @implements IMessageAddOn
         * @constructor
         * @param {proto.IMessageAddOn=} [properties] Properties to set
         */
        function MessageAddOn(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MessageAddOn messageAddOnType.
         * @member {proto.MessageAddOn.MessageAddOnType|null|undefined} messageAddOnType
         * @memberof proto.MessageAddOn
         * @instance
         */
        MessageAddOn.prototype.messageAddOnType = null;

        /**
         * MessageAddOn messageAddOn.
         * @member {proto.IMessage|null|undefined} messageAddOn
         * @memberof proto.MessageAddOn
         * @instance
         */
        MessageAddOn.prototype.messageAddOn = null;

        /**
         * MessageAddOn senderTimestampMs.
         * @member {number|Long|null|undefined} senderTimestampMs
         * @memberof proto.MessageAddOn
         * @instance
         */
        MessageAddOn.prototype.senderTimestampMs = null;

        /**
         * MessageAddOn serverTimestampMs.
         * @member {number|Long|null|undefined} serverTimestampMs
         * @memberof proto.MessageAddOn
         * @instance
         */
        MessageAddOn.prototype.serverTimestampMs = null;

        /**
         * MessageAddOn status.
         * @member {proto.WebMessageInfo.Status|null|undefined} status
         * @memberof proto.MessageAddOn
         * @instance
         */
        MessageAddOn.prototype.status = null;

        /**
         * MessageAddOn addOnContextInfo.
         * @member {proto.IMessageAddOnContextInfo|null|undefined} addOnContextInfo
         * @memberof proto.MessageAddOn
         * @instance
         */
        MessageAddOn.prototype.addOnContextInfo = null;

        /**
         * MessageAddOn messageAddOnKey.
         * @member {proto.IMessageKey|null|undefined} messageAddOnKey
         * @memberof proto.MessageAddOn
         * @instance
         */
        MessageAddOn.prototype.messageAddOnKey = null;

        /**
         * MessageAddOn legacyMessage.
         * @member {proto.ILegacyMessage|null|undefined} legacyMessage
         * @memberof proto.MessageAddOn
         * @instance
         */
        MessageAddOn.prototype.legacyMessage = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAddOn.prototype, "_messageAddOnType", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOnType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAddOn.prototype, "_messageAddOn", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOn"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAddOn.prototype, "_senderTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["senderTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAddOn.prototype, "_serverTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["serverTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAddOn.prototype, "_status", {
            get: $util.oneOfGetter($oneOfFields = ["status"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAddOn.prototype, "_addOnContextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["addOnContextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAddOn.prototype, "_messageAddOnKey", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOnKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAddOn.prototype, "_legacyMessage", {
            get: $util.oneOfGetter($oneOfFields = ["legacyMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new MessageAddOn instance using the specified properties.
         * @function create
         * @memberof proto.MessageAddOn
         * @static
         * @param {proto.IMessageAddOn=} [properties] Properties to set
         * @returns {proto.MessageAddOn} MessageAddOn instance
         */
        MessageAddOn.create = function create(properties) {
            return new MessageAddOn(properties);
        };

        /**
         * Encodes the specified MessageAddOn message. Does not implicitly {@link proto.MessageAddOn.verify|verify} messages.
         * @function encode
         * @memberof proto.MessageAddOn
         * @static
         * @param {proto.IMessageAddOn} message MessageAddOn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MessageAddOn.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.messageAddOnType != null && Object.hasOwnProperty.call(message, "messageAddOnType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.messageAddOnType);
            if (message.messageAddOn != null && Object.hasOwnProperty.call(message, "messageAddOn"))
                $root.proto.Message.encode(message.messageAddOn, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.senderTimestampMs);
            if (message.serverTimestampMs != null && Object.hasOwnProperty.call(message, "serverTimestampMs"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.serverTimestampMs);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.status);
            if (message.addOnContextInfo != null && Object.hasOwnProperty.call(message, "addOnContextInfo"))
                $root.proto.MessageAddOnContextInfo.encode(message.addOnContextInfo, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
            if (message.messageAddOnKey != null && Object.hasOwnProperty.call(message, "messageAddOnKey"))
                $root.proto.MessageKey.encode(message.messageAddOnKey, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
            if (message.legacyMessage != null && Object.hasOwnProperty.call(message, "legacyMessage"))
                $root.proto.LegacyMessage.encode(message.legacyMessage, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MessageAddOn message, length delimited. Does not implicitly {@link proto.MessageAddOn.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.MessageAddOn
         * @static
         * @param {proto.IMessageAddOn} message MessageAddOn message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MessageAddOn.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MessageAddOn message from the specified reader or buffer.
         * @function decode
         * @memberof proto.MessageAddOn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.MessageAddOn} MessageAddOn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MessageAddOn.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.messageAddOnType = reader.int32();
                        message._messageAddOnType = "messageAddOnType";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.messageAddOn = $root.proto.Message.decode(reader, reader.uint32(), undefined, q + 1, message.messageAddOn);
                        message._messageAddOn = "messageAddOn";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.senderTimestampMs = reader.int64();
                        message._senderTimestampMs = "senderTimestampMs";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.serverTimestampMs = reader.int64();
                        message._serverTimestampMs = "serverTimestampMs";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.status = reader.int32();
                        message._status = "status";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        message.addOnContextInfo = $root.proto.MessageAddOnContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.addOnContextInfo);
                        message._addOnContextInfo = "addOnContextInfo";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        message.messageAddOnKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.messageAddOnKey);
                        message._messageAddOnKey = "messageAddOnKey";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        message.legacyMessage = $root.proto.LegacyMessage.decode(reader, reader.uint32(), undefined, q + 1, message.legacyMessage);
                        message._legacyMessage = "legacyMessage";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a MessageAddOn message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.MessageAddOn
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.MessageAddOn} MessageAddOn
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MessageAddOn.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MessageAddOn message.
         * @function verify
         * @memberof proto.MessageAddOn
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MessageAddOn.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.messageAddOnType != null && Object.hasOwnProperty.call(message, "messageAddOnType")) {
                properties._messageAddOnType = 1;
                if (typeof message.messageAddOnType !== "number" || (message.messageAddOnType | 0) !== message.messageAddOnType)
                    return "messageAddOnType: enum value expected";
            }
            if (message.messageAddOn != null && Object.hasOwnProperty.call(message, "messageAddOn")) {
                properties._messageAddOn = 1;
                {
                    var error = $root.proto.Message.verify(message.messageAddOn, q + 1);
                    if (error)
                        return "messageAddOn." + error;
                }
            }
            if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs")) {
                properties._senderTimestampMs = 1;
                if (!$util.isInteger(message.senderTimestampMs) && !(message.senderTimestampMs && $util.isInteger(message.senderTimestampMs.low) && $util.isInteger(message.senderTimestampMs.high)))
                    return "senderTimestampMs: integer|Long expected";
            }
            if (message.serverTimestampMs != null && Object.hasOwnProperty.call(message, "serverTimestampMs")) {
                properties._serverTimestampMs = 1;
                if (!$util.isInteger(message.serverTimestampMs) && !(message.serverTimestampMs && $util.isInteger(message.serverTimestampMs.low) && $util.isInteger(message.serverTimestampMs.high)))
                    return "serverTimestampMs: integer|Long expected";
            }
            if (message.status != null && Object.hasOwnProperty.call(message, "status")) {
                properties._status = 1;
                if (typeof message.status !== "number" || (message.status | 0) !== message.status)
                    return "status: enum value expected";
            }
            if (message.addOnContextInfo != null && Object.hasOwnProperty.call(message, "addOnContextInfo")) {
                properties._addOnContextInfo = 1;
                {
                    var error = $root.proto.MessageAddOnContextInfo.verify(message.addOnContextInfo, q + 1);
                    if (error)
                        return "addOnContextInfo." + error;
                }
            }
            if (message.messageAddOnKey != null && Object.hasOwnProperty.call(message, "messageAddOnKey")) {
                properties._messageAddOnKey = 1;
                {
                    var error = $root.proto.MessageKey.verify(message.messageAddOnKey, q + 1);
                    if (error)
                        return "messageAddOnKey." + error;
                }
            }
            if (message.legacyMessage != null && Object.hasOwnProperty.call(message, "legacyMessage")) {
                properties._legacyMessage = 1;
                {
                    var error = $root.proto.LegacyMessage.verify(message.legacyMessage, q + 1);
                    if (error)
                        return "legacyMessage." + error;
                }
            }
            return null;
        };

        /**
         * Creates a MessageAddOn message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.MessageAddOn
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.MessageAddOn} MessageAddOn
         */
        MessageAddOn.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.MessageAddOn: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.messageAddOnType) {
            case "UNDEFINED":
            case 0:
                message.messageAddOnType = 0;
                break;
            case "REACTION":
            case 1:
                message.messageAddOnType = 1;
                break;
            case "EVENT_RESPONSE":
            case 2:
                message.messageAddOnType = 2;
                break;
            case "POLL_UPDATE":
            case 3:
                message.messageAddOnType = 3;
                break;
            case "PIN_IN_CHAT":
            case 4:
                message.messageAddOnType = 4;
                break;
            default:
                if (typeof object.messageAddOnType === "number" && (object.messageAddOnType | 0) === object.messageAddOnType)
                    message.messageAddOnType = object.messageAddOnType;
            }
            if (object.messageAddOn != null) {
                if (!$util.isObject(object.messageAddOn))
                    throw TypeError(".proto.MessageAddOn.messageAddOn: object expected");
                message.messageAddOn = $root.proto.Message.fromObject(object.messageAddOn, q + 1);
            }
            if (object.senderTimestampMs != null)
                if ($util.Long)
                    message.senderTimestampMs = $util.Long.fromValue(object.senderTimestampMs, false);
                else if (typeof object.senderTimestampMs === "string")
                    message.senderTimestampMs = parseInt(object.senderTimestampMs, 10);
                else if (typeof object.senderTimestampMs === "number")
                    message.senderTimestampMs = object.senderTimestampMs;
                else if (typeof object.senderTimestampMs === "object")
                    message.senderTimestampMs = new $util.LongBits(object.senderTimestampMs.low >>> 0, object.senderTimestampMs.high >>> 0).toNumber();
            if (object.serverTimestampMs != null)
                if ($util.Long)
                    message.serverTimestampMs = $util.Long.fromValue(object.serverTimestampMs, false);
                else if (typeof object.serverTimestampMs === "string")
                    message.serverTimestampMs = parseInt(object.serverTimestampMs, 10);
                else if (typeof object.serverTimestampMs === "number")
                    message.serverTimestampMs = object.serverTimestampMs;
                else if (typeof object.serverTimestampMs === "object")
                    message.serverTimestampMs = new $util.LongBits(object.serverTimestampMs.low >>> 0, object.serverTimestampMs.high >>> 0).toNumber();
            switch (object.status) {
            case "ERROR":
            case 0:
                message.status = 0;
                break;
            case "PENDING":
            case 1:
                message.status = 1;
                break;
            case "SERVER_ACK":
            case 2:
                message.status = 2;
                break;
            case "DELIVERY_ACK":
            case 3:
                message.status = 3;
                break;
            case "READ":
            case 4:
                message.status = 4;
                break;
            case "PLAYED":
            case 5:
                message.status = 5;
                break;
            default:
                if (typeof object.status === "number" && (object.status | 0) === object.status)
                    message.status = object.status;
            }
            if (object.addOnContextInfo != null) {
                if (!$util.isObject(object.addOnContextInfo))
                    throw TypeError(".proto.MessageAddOn.addOnContextInfo: object expected");
                message.addOnContextInfo = $root.proto.MessageAddOnContextInfo.fromObject(object.addOnContextInfo, q + 1);
            }
            if (object.messageAddOnKey != null) {
                if (!$util.isObject(object.messageAddOnKey))
                    throw TypeError(".proto.MessageAddOn.messageAddOnKey: object expected");
                message.messageAddOnKey = $root.proto.MessageKey.fromObject(object.messageAddOnKey, q + 1);
            }
            if (object.legacyMessage != null) {
                if (!$util.isObject(object.legacyMessage))
                    throw TypeError(".proto.MessageAddOn.legacyMessage: object expected");
                message.legacyMessage = $root.proto.LegacyMessage.fromObject(object.legacyMessage, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a MessageAddOn message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.MessageAddOn
         * @static
         * @param {proto.MessageAddOn} message MessageAddOn
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MessageAddOn.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.messageAddOnType != null && Object.hasOwnProperty.call(message, "messageAddOnType"))
                object.messageAddOnType = options.enums === String ? $root.proto.MessageAddOn.MessageAddOnType[message.messageAddOnType] === undefined ? message.messageAddOnType : $root.proto.MessageAddOn.MessageAddOnType[message.messageAddOnType] : message.messageAddOnType;
            if (message.messageAddOn != null && Object.hasOwnProperty.call(message, "messageAddOn"))
                object.messageAddOn = $root.proto.Message.toObject(message.messageAddOn, options, q + 1);
            if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.senderTimestampMs = typeof message.senderTimestampMs === "number" ? BigInt(message.senderTimestampMs) : $util.Long.fromBits(message.senderTimestampMs.low >>> 0, message.senderTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof message.senderTimestampMs === "number")
                    object.senderTimestampMs = options.longs === String ? String(message.senderTimestampMs) : message.senderTimestampMs;
                else
                    object.senderTimestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.senderTimestampMs) : options.longs === Number ? new $util.LongBits(message.senderTimestampMs.low >>> 0, message.senderTimestampMs.high >>> 0).toNumber() : message.senderTimestampMs;
            if (message.serverTimestampMs != null && Object.hasOwnProperty.call(message, "serverTimestampMs"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.serverTimestampMs = typeof message.serverTimestampMs === "number" ? BigInt(message.serverTimestampMs) : $util.Long.fromBits(message.serverTimestampMs.low >>> 0, message.serverTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof message.serverTimestampMs === "number")
                    object.serverTimestampMs = options.longs === String ? String(message.serverTimestampMs) : message.serverTimestampMs;
                else
                    object.serverTimestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.serverTimestampMs) : options.longs === Number ? new $util.LongBits(message.serverTimestampMs.low >>> 0, message.serverTimestampMs.high >>> 0).toNumber() : message.serverTimestampMs;
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                object.status = options.enums === String ? $root.proto.WebMessageInfo.Status[message.status] === undefined ? message.status : $root.proto.WebMessageInfo.Status[message.status] : message.status;
            if (message.addOnContextInfo != null && Object.hasOwnProperty.call(message, "addOnContextInfo"))
                object.addOnContextInfo = $root.proto.MessageAddOnContextInfo.toObject(message.addOnContextInfo, options, q + 1);
            if (message.messageAddOnKey != null && Object.hasOwnProperty.call(message, "messageAddOnKey"))
                object.messageAddOnKey = $root.proto.MessageKey.toObject(message.messageAddOnKey, options, q + 1);
            if (message.legacyMessage != null && Object.hasOwnProperty.call(message, "legacyMessage"))
                object.legacyMessage = $root.proto.LegacyMessage.toObject(message.legacyMessage, options, q + 1);
            return object;
        };

        /**
         * Converts this MessageAddOn to JSON.
         * @function toJSON
         * @memberof proto.MessageAddOn
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MessageAddOn.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MessageAddOn
         * @function getTypeUrl
         * @memberof proto.MessageAddOn
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MessageAddOn.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.MessageAddOn";
        };

        /**
         * MessageAddOnType enum.
         * @name proto.MessageAddOn.MessageAddOnType
         * @enum {number}
         * @property {number} UNDEFINED=0 UNDEFINED value
         * @property {number} REACTION=1 REACTION value
         * @property {number} EVENT_RESPONSE=2 EVENT_RESPONSE value
         * @property {number} POLL_UPDATE=3 POLL_UPDATE value
         * @property {number} PIN_IN_CHAT=4 PIN_IN_CHAT value
         */
        MessageAddOn.MessageAddOnType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNDEFINED"] = 0;
            values[valuesById[1] = "REACTION"] = 1;
            values[valuesById[2] = "EVENT_RESPONSE"] = 2;
            values[valuesById[3] = "POLL_UPDATE"] = 3;
            values[valuesById[4] = "PIN_IN_CHAT"] = 4;
            return values;
        })();

        return MessageAddOn;
    })();

    proto.MessageAddOnContextInfo = (function() {

        /**
         * Properties of a MessageAddOnContextInfo.
         * @memberof proto
         * @interface IMessageAddOnContextInfo
         * @property {number|null} [messageAddOnDurationInSecs] MessageAddOnContextInfo messageAddOnDurationInSecs
         * @property {proto.MessageContextInfo.MessageAddonExpiryType|null} [messageAddOnExpiryType] MessageAddOnContextInfo messageAddOnExpiryType
         */

        /**
         * Constructs a new MessageAddOnContextInfo.
         * @memberof proto
         * @classdesc Represents a MessageAddOnContextInfo.
         * @implements IMessageAddOnContextInfo
         * @constructor
         * @param {proto.IMessageAddOnContextInfo=} [properties] Properties to set
         */
        function MessageAddOnContextInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MessageAddOnContextInfo messageAddOnDurationInSecs.
         * @member {number|null|undefined} messageAddOnDurationInSecs
         * @memberof proto.MessageAddOnContextInfo
         * @instance
         */
        MessageAddOnContextInfo.prototype.messageAddOnDurationInSecs = null;

        /**
         * MessageAddOnContextInfo messageAddOnExpiryType.
         * @member {proto.MessageContextInfo.MessageAddonExpiryType|null|undefined} messageAddOnExpiryType
         * @memberof proto.MessageAddOnContextInfo
         * @instance
         */
        MessageAddOnContextInfo.prototype.messageAddOnExpiryType = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAddOnContextInfo.prototype, "_messageAddOnDurationInSecs", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOnDurationInSecs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAddOnContextInfo.prototype, "_messageAddOnExpiryType", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOnExpiryType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new MessageAddOnContextInfo instance using the specified properties.
         * @function create
         * @memberof proto.MessageAddOnContextInfo
         * @static
         * @param {proto.IMessageAddOnContextInfo=} [properties] Properties to set
         * @returns {proto.MessageAddOnContextInfo} MessageAddOnContextInfo instance
         */
        MessageAddOnContextInfo.create = function create(properties) {
            return new MessageAddOnContextInfo(properties);
        };

        /**
         * Encodes the specified MessageAddOnContextInfo message. Does not implicitly {@link proto.MessageAddOnContextInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.MessageAddOnContextInfo
         * @static
         * @param {proto.IMessageAddOnContextInfo} message MessageAddOnContextInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MessageAddOnContextInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.messageAddOnDurationInSecs != null && Object.hasOwnProperty.call(message, "messageAddOnDurationInSecs"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.messageAddOnDurationInSecs);
            if (message.messageAddOnExpiryType != null && Object.hasOwnProperty.call(message, "messageAddOnExpiryType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.messageAddOnExpiryType);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MessageAddOnContextInfo message, length delimited. Does not implicitly {@link proto.MessageAddOnContextInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.MessageAddOnContextInfo
         * @static
         * @param {proto.IMessageAddOnContextInfo} message MessageAddOnContextInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MessageAddOnContextInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MessageAddOnContextInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.MessageAddOnContextInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.MessageAddOnContextInfo} MessageAddOnContextInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MessageAddOnContextInfo.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.messageAddOnDurationInSecs = reader.uint32();
                        message._messageAddOnDurationInSecs = "messageAddOnDurationInSecs";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.messageAddOnExpiryType = reader.int32();
                        message._messageAddOnExpiryType = "messageAddOnExpiryType";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a MessageAddOnContextInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.MessageAddOnContextInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.MessageAddOnContextInfo} MessageAddOnContextInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MessageAddOnContextInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MessageAddOnContextInfo message.
         * @function verify
         * @memberof proto.MessageAddOnContextInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MessageAddOnContextInfo.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.messageAddOnDurationInSecs != null && Object.hasOwnProperty.call(message, "messageAddOnDurationInSecs")) {
                properties._messageAddOnDurationInSecs = 1;
                if (!$util.isInteger(message.messageAddOnDurationInSecs))
                    return "messageAddOnDurationInSecs: integer expected";
            }
            if (message.messageAddOnExpiryType != null && Object.hasOwnProperty.call(message, "messageAddOnExpiryType")) {
                properties._messageAddOnExpiryType = 1;
                if (typeof message.messageAddOnExpiryType !== "number" || (message.messageAddOnExpiryType | 0) !== message.messageAddOnExpiryType)
                    return "messageAddOnExpiryType: enum value expected";
            }
            return null;
        };

        /**
         * Creates a MessageAddOnContextInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.MessageAddOnContextInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.MessageAddOnContextInfo} MessageAddOnContextInfo
         */
        MessageAddOnContextInfo.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.MessageAddOnContextInfo: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.messageAddOnDurationInSecs != null)
                message.messageAddOnDurationInSecs = object.messageAddOnDurationInSecs >>> 0;
            switch (object.messageAddOnExpiryType) {
            case "STATIC":
            case 1:
                message.messageAddOnExpiryType = 1;
                break;
            case "DEPENDENT_ON_PARENT":
            case 2:
                message.messageAddOnExpiryType = 2;
                break;
            default:
                if (typeof object.messageAddOnExpiryType === "number" && (object.messageAddOnExpiryType | 0) === object.messageAddOnExpiryType)
                    message.messageAddOnExpiryType = object.messageAddOnExpiryType;
            }
            return message;
        };

        /**
         * Creates a plain object from a MessageAddOnContextInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.MessageAddOnContextInfo
         * @static
         * @param {proto.MessageAddOnContextInfo} message MessageAddOnContextInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MessageAddOnContextInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.messageAddOnDurationInSecs != null && Object.hasOwnProperty.call(message, "messageAddOnDurationInSecs"))
                object.messageAddOnDurationInSecs = message.messageAddOnDurationInSecs;
            if (message.messageAddOnExpiryType != null && Object.hasOwnProperty.call(message, "messageAddOnExpiryType"))
                object.messageAddOnExpiryType = options.enums === String ? $root.proto.MessageContextInfo.MessageAddonExpiryType[message.messageAddOnExpiryType] === undefined ? message.messageAddOnExpiryType : $root.proto.MessageContextInfo.MessageAddonExpiryType[message.messageAddOnExpiryType] : message.messageAddOnExpiryType;
            return object;
        };

        /**
         * Converts this MessageAddOnContextInfo to JSON.
         * @function toJSON
         * @memberof proto.MessageAddOnContextInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MessageAddOnContextInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MessageAddOnContextInfo
         * @function getTypeUrl
         * @memberof proto.MessageAddOnContextInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MessageAddOnContextInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.MessageAddOnContextInfo";
        };

        return MessageAddOnContextInfo;
    })();

    proto.MessageAssociation = (function() {

        /**
         * Properties of a MessageAssociation.
         * @memberof proto
         * @interface IMessageAssociation
         * @property {proto.MessageAssociation.AssociationType|null} [associationType] MessageAssociation associationType
         * @property {proto.IMessageKey|null} [parentMessageKey] MessageAssociation parentMessageKey
         * @property {number|null} [messageIndex] MessageAssociation messageIndex
         */

        /**
         * Constructs a new MessageAssociation.
         * @memberof proto
         * @classdesc Represents a MessageAssociation.
         * @implements IMessageAssociation
         * @constructor
         * @param {proto.IMessageAssociation=} [properties] Properties to set
         */
        function MessageAssociation(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MessageAssociation associationType.
         * @member {proto.MessageAssociation.AssociationType|null|undefined} associationType
         * @memberof proto.MessageAssociation
         * @instance
         */
        MessageAssociation.prototype.associationType = null;

        /**
         * MessageAssociation parentMessageKey.
         * @member {proto.IMessageKey|null|undefined} parentMessageKey
         * @memberof proto.MessageAssociation
         * @instance
         */
        MessageAssociation.prototype.parentMessageKey = null;

        /**
         * MessageAssociation messageIndex.
         * @member {number|null|undefined} messageIndex
         * @memberof proto.MessageAssociation
         * @instance
         */
        MessageAssociation.prototype.messageIndex = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAssociation.prototype, "_associationType", {
            get: $util.oneOfGetter($oneOfFields = ["associationType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAssociation.prototype, "_parentMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["parentMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAssociation.prototype, "_messageIndex", {
            get: $util.oneOfGetter($oneOfFields = ["messageIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new MessageAssociation instance using the specified properties.
         * @function create
         * @memberof proto.MessageAssociation
         * @static
         * @param {proto.IMessageAssociation=} [properties] Properties to set
         * @returns {proto.MessageAssociation} MessageAssociation instance
         */
        MessageAssociation.create = function create(properties) {
            return new MessageAssociation(properties);
        };

        /**
         * Encodes the specified MessageAssociation message. Does not implicitly {@link proto.MessageAssociation.verify|verify} messages.
         * @function encode
         * @memberof proto.MessageAssociation
         * @static
         * @param {proto.IMessageAssociation} message MessageAssociation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MessageAssociation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.associationType != null && Object.hasOwnProperty.call(message, "associationType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.associationType);
            if (message.parentMessageKey != null && Object.hasOwnProperty.call(message, "parentMessageKey"))
                $root.proto.MessageKey.encode(message.parentMessageKey, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.messageIndex != null && Object.hasOwnProperty.call(message, "messageIndex"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.messageIndex);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MessageAssociation message, length delimited. Does not implicitly {@link proto.MessageAssociation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.MessageAssociation
         * @static
         * @param {proto.IMessageAssociation} message MessageAssociation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MessageAssociation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MessageAssociation message from the specified reader or buffer.
         * @function decode
         * @memberof proto.MessageAssociation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.MessageAssociation} MessageAssociation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MessageAssociation.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.associationType = reader.int32();
                        message._associationType = "associationType";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.parentMessageKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.parentMessageKey);
                        message._parentMessageKey = "parentMessageKey";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.messageIndex = reader.int32();
                        message._messageIndex = "messageIndex";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a MessageAssociation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.MessageAssociation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.MessageAssociation} MessageAssociation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MessageAssociation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MessageAssociation message.
         * @function verify
         * @memberof proto.MessageAssociation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MessageAssociation.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.associationType != null && Object.hasOwnProperty.call(message, "associationType")) {
                properties._associationType = 1;
                if (typeof message.associationType !== "number" || (message.associationType | 0) !== message.associationType)
                    return "associationType: enum value expected";
            }
            if (message.parentMessageKey != null && Object.hasOwnProperty.call(message, "parentMessageKey")) {
                properties._parentMessageKey = 1;
                {
                    var error = $root.proto.MessageKey.verify(message.parentMessageKey, q + 1);
                    if (error)
                        return "parentMessageKey." + error;
                }
            }
            if (message.messageIndex != null && Object.hasOwnProperty.call(message, "messageIndex")) {
                properties._messageIndex = 1;
                if (!$util.isInteger(message.messageIndex))
                    return "messageIndex: integer expected";
            }
            return null;
        };

        /**
         * Creates a MessageAssociation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.MessageAssociation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.MessageAssociation} MessageAssociation
         */
        MessageAssociation.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.MessageAssociation: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.associationType) {
            case "UNKNOWN":
            case 0:
                message.associationType = 0;
                break;
            case "MEDIA_ALBUM":
            case 1:
                message.associationType = 1;
                break;
            case "BOT_PLUGIN":
            case 2:
                message.associationType = 2;
                break;
            case "EVENT_COVER_IMAGE":
            case 3:
                message.associationType = 3;
                break;
            case "STATUS_POLL":
            case 4:
                message.associationType = 4;
                break;
            case "HD_VIDEO_DUAL_UPLOAD":
            case 5:
                message.associationType = 5;
                break;
            case "STATUS_EXTERNAL_RESHARE":
            case 6:
                message.associationType = 6;
                break;
            case "MEDIA_POLL":
            case 7:
                message.associationType = 7;
                break;
            case "STATUS_ADD_YOURS":
            case 8:
                message.associationType = 8;
                break;
            case "STATUS_NOTIFICATION":
            case 9:
                message.associationType = 9;
                break;
            case "HD_IMAGE_DUAL_UPLOAD":
            case 10:
                message.associationType = 10;
                break;
            case "STICKER_ANNOTATION":
            case 11:
                message.associationType = 11;
                break;
            case "MOTION_PHOTO":
            case 12:
                message.associationType = 12;
                break;
            case "STATUS_LINK_ACTION":
            case 13:
                message.associationType = 13;
                break;
            case "VIEW_ALL_REPLIES":
            case 14:
                message.associationType = 14;
                break;
            case "STATUS_ADD_YOURS_AI_IMAGINE":
            case 15:
                message.associationType = 15;
                break;
            case "STATUS_QUESTION":
            case 16:
                message.associationType = 16;
                break;
            case "STATUS_ADD_YOURS_DIWALI":
            case 17:
                message.associationType = 17;
                break;
            case "STATUS_REACTION":
            case 18:
                message.associationType = 18;
                break;
            case "HEVC_VIDEO_DUAL_UPLOAD":
            case 19:
                message.associationType = 19;
                break;
            case "POLL_ADD_OPTION":
            case 20:
                message.associationType = 20;
                break;
            default:
                if (typeof object.associationType === "number" && (object.associationType | 0) === object.associationType)
                    message.associationType = object.associationType;
            }
            if (object.parentMessageKey != null) {
                if (!$util.isObject(object.parentMessageKey))
                    throw TypeError(".proto.MessageAssociation.parentMessageKey: object expected");
                message.parentMessageKey = $root.proto.MessageKey.fromObject(object.parentMessageKey, q + 1);
            }
            if (object.messageIndex != null)
                message.messageIndex = object.messageIndex | 0;
            return message;
        };

        /**
         * Creates a plain object from a MessageAssociation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.MessageAssociation
         * @static
         * @param {proto.MessageAssociation} message MessageAssociation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MessageAssociation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.associationType != null && Object.hasOwnProperty.call(message, "associationType"))
                object.associationType = options.enums === String ? $root.proto.MessageAssociation.AssociationType[message.associationType] === undefined ? message.associationType : $root.proto.MessageAssociation.AssociationType[message.associationType] : message.associationType;
            if (message.parentMessageKey != null && Object.hasOwnProperty.call(message, "parentMessageKey"))
                object.parentMessageKey = $root.proto.MessageKey.toObject(message.parentMessageKey, options, q + 1);
            if (message.messageIndex != null && Object.hasOwnProperty.call(message, "messageIndex"))
                object.messageIndex = message.messageIndex;
            return object;
        };

        /**
         * Converts this MessageAssociation to JSON.
         * @function toJSON
         * @memberof proto.MessageAssociation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MessageAssociation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MessageAssociation
         * @function getTypeUrl
         * @memberof proto.MessageAssociation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MessageAssociation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.MessageAssociation";
        };

        /**
         * AssociationType enum.
         * @name proto.MessageAssociation.AssociationType
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} MEDIA_ALBUM=1 MEDIA_ALBUM value
         * @property {number} BOT_PLUGIN=2 BOT_PLUGIN value
         * @property {number} EVENT_COVER_IMAGE=3 EVENT_COVER_IMAGE value
         * @property {number} STATUS_POLL=4 STATUS_POLL value
         * @property {number} HD_VIDEO_DUAL_UPLOAD=5 HD_VIDEO_DUAL_UPLOAD value
         * @property {number} STATUS_EXTERNAL_RESHARE=6 STATUS_EXTERNAL_RESHARE value
         * @property {number} MEDIA_POLL=7 MEDIA_POLL value
         * @property {number} STATUS_ADD_YOURS=8 STATUS_ADD_YOURS value
         * @property {number} STATUS_NOTIFICATION=9 STATUS_NOTIFICATION value
         * @property {number} HD_IMAGE_DUAL_UPLOAD=10 HD_IMAGE_DUAL_UPLOAD value
         * @property {number} STICKER_ANNOTATION=11 STICKER_ANNOTATION value
         * @property {number} MOTION_PHOTO=12 MOTION_PHOTO value
         * @property {number} STATUS_LINK_ACTION=13 STATUS_LINK_ACTION value
         * @property {number} VIEW_ALL_REPLIES=14 VIEW_ALL_REPLIES value
         * @property {number} STATUS_ADD_YOURS_AI_IMAGINE=15 STATUS_ADD_YOURS_AI_IMAGINE value
         * @property {number} STATUS_QUESTION=16 STATUS_QUESTION value
         * @property {number} STATUS_ADD_YOURS_DIWALI=17 STATUS_ADD_YOURS_DIWALI value
         * @property {number} STATUS_REACTION=18 STATUS_REACTION value
         * @property {number} HEVC_VIDEO_DUAL_UPLOAD=19 HEVC_VIDEO_DUAL_UPLOAD value
         * @property {number} POLL_ADD_OPTION=20 POLL_ADD_OPTION value
         */
        MessageAssociation.AssociationType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "MEDIA_ALBUM"] = 1;
            values[valuesById[2] = "BOT_PLUGIN"] = 2;
            values[valuesById[3] = "EVENT_COVER_IMAGE"] = 3;
            values[valuesById[4] = "STATUS_POLL"] = 4;
            values[valuesById[5] = "HD_VIDEO_DUAL_UPLOAD"] = 5;
            values[valuesById[6] = "STATUS_EXTERNAL_RESHARE"] = 6;
            values[valuesById[7] = "MEDIA_POLL"] = 7;
            values[valuesById[8] = "STATUS_ADD_YOURS"] = 8;
            values[valuesById[9] = "STATUS_NOTIFICATION"] = 9;
            values[valuesById[10] = "HD_IMAGE_DUAL_UPLOAD"] = 10;
            values[valuesById[11] = "STICKER_ANNOTATION"] = 11;
            values[valuesById[12] = "MOTION_PHOTO"] = 12;
            values[valuesById[13] = "STATUS_LINK_ACTION"] = 13;
            values[valuesById[14] = "VIEW_ALL_REPLIES"] = 14;
            values[valuesById[15] = "STATUS_ADD_YOURS_AI_IMAGINE"] = 15;
            values[valuesById[16] = "STATUS_QUESTION"] = 16;
            values[valuesById[17] = "STATUS_ADD_YOURS_DIWALI"] = 17;
            values[valuesById[18] = "STATUS_REACTION"] = 18;
            values[valuesById[19] = "HEVC_VIDEO_DUAL_UPLOAD"] = 19;
            values[valuesById[20] = "POLL_ADD_OPTION"] = 20;
            return values;
        })();

        return MessageAssociation;
    })();

    proto.MessageContextInfo = (function() {

        /**
         * Properties of a MessageContextInfo.
         * @memberof proto
         * @interface IMessageContextInfo
         * @property {proto.IDeviceListMetadata|null} [deviceListMetadata] MessageContextInfo deviceListMetadata
         * @property {number|null} [deviceListMetadataVersion] MessageContextInfo deviceListMetadataVersion
         * @property {Uint8Array|null} [messageSecret] MessageContextInfo messageSecret
         * @property {Uint8Array|null} [paddingBytes] MessageContextInfo paddingBytes
         * @property {number|null} [messageAddOnDurationInSecs] MessageContextInfo messageAddOnDurationInSecs
         * @property {Uint8Array|null} [botMessageSecret] MessageContextInfo botMessageSecret
         * @property {proto.IBotMetadata|null} [botMetadata] MessageContextInfo botMetadata
         * @property {number|null} [reportingTokenVersion] MessageContextInfo reportingTokenVersion
         * @property {proto.MessageContextInfo.MessageAddonExpiryType|null} [messageAddOnExpiryType] MessageContextInfo messageAddOnExpiryType
         * @property {proto.IMessageAssociation|null} [messageAssociation] MessageContextInfo messageAssociation
         * @property {boolean|null} [capiCreatedGroup] MessageContextInfo capiCreatedGroup
         * @property {string|null} [supportPayload] MessageContextInfo supportPayload
         * @property {proto.ILimitSharing|null} [limitSharing] MessageContextInfo limitSharing
         * @property {proto.ILimitSharing|null} [limitSharingV2] MessageContextInfo limitSharingV2
         * @property {Array.<proto.IThreadID>|null} [threadId] MessageContextInfo threadId
         * @property {proto.WebLinkRenderConfig|null} [weblinkRenderConfig] MessageContextInfo weblinkRenderConfig
         * @property {Uint8Array|null} [teeBotMetadata] MessageContextInfo teeBotMetadata
         */

        /**
         * Constructs a new MessageContextInfo.
         * @memberof proto
         * @classdesc Represents a MessageContextInfo.
         * @implements IMessageContextInfo
         * @constructor
         * @param {proto.IMessageContextInfo=} [properties] Properties to set
         */
        function MessageContextInfo(properties) {
            this.threadId = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MessageContextInfo deviceListMetadata.
         * @member {proto.IDeviceListMetadata|null|undefined} deviceListMetadata
         * @memberof proto.MessageContextInfo
         * @instance
         */
        MessageContextInfo.prototype.deviceListMetadata = null;

        /**
         * MessageContextInfo deviceListMetadataVersion.
         * @member {number|null|undefined} deviceListMetadataVersion
         * @memberof proto.MessageContextInfo
         * @instance
         */
        MessageContextInfo.prototype.deviceListMetadataVersion = null;

        /**
         * MessageContextInfo messageSecret.
         * @member {Uint8Array|null|undefined} messageSecret
         * @memberof proto.MessageContextInfo
         * @instance
         */
        MessageContextInfo.prototype.messageSecret = null;

        /**
         * MessageContextInfo paddingBytes.
         * @member {Uint8Array|null|undefined} paddingBytes
         * @memberof proto.MessageContextInfo
         * @instance
         */
        MessageContextInfo.prototype.paddingBytes = null;

        /**
         * MessageContextInfo messageAddOnDurationInSecs.
         * @member {number|null|undefined} messageAddOnDurationInSecs
         * @memberof proto.MessageContextInfo
         * @instance
         */
        MessageContextInfo.prototype.messageAddOnDurationInSecs = null;

        /**
         * MessageContextInfo botMessageSecret.
         * @member {Uint8Array|null|undefined} botMessageSecret
         * @memberof proto.MessageContextInfo
         * @instance
         */
        MessageContextInfo.prototype.botMessageSecret = null;

        /**
         * MessageContextInfo botMetadata.
         * @member {proto.IBotMetadata|null|undefined} botMetadata
         * @memberof proto.MessageContextInfo
         * @instance
         */
        MessageContextInfo.prototype.botMetadata = null;

        /**
         * MessageContextInfo reportingTokenVersion.
         * @member {number|null|undefined} reportingTokenVersion
         * @memberof proto.MessageContextInfo
         * @instance
         */
        MessageContextInfo.prototype.reportingTokenVersion = null;

        /**
         * MessageContextInfo messageAddOnExpiryType.
         * @member {proto.MessageContextInfo.MessageAddonExpiryType|null|undefined} messageAddOnExpiryType
         * @memberof proto.MessageContextInfo
         * @instance
         */
        MessageContextInfo.prototype.messageAddOnExpiryType = null;

        /**
         * MessageContextInfo messageAssociation.
         * @member {proto.IMessageAssociation|null|undefined} messageAssociation
         * @memberof proto.MessageContextInfo
         * @instance
         */
        MessageContextInfo.prototype.messageAssociation = null;

        /**
         * MessageContextInfo capiCreatedGroup.
         * @member {boolean|null|undefined} capiCreatedGroup
         * @memberof proto.MessageContextInfo
         * @instance
         */
        MessageContextInfo.prototype.capiCreatedGroup = null;

        /**
         * MessageContextInfo supportPayload.
         * @member {string|null|undefined} supportPayload
         * @memberof proto.MessageContextInfo
         * @instance
         */
        MessageContextInfo.prototype.supportPayload = null;

        /**
         * MessageContextInfo limitSharing.
         * @member {proto.ILimitSharing|null|undefined} limitSharing
         * @memberof proto.MessageContextInfo
         * @instance
         */
        MessageContextInfo.prototype.limitSharing = null;

        /**
         * MessageContextInfo limitSharingV2.
         * @member {proto.ILimitSharing|null|undefined} limitSharingV2
         * @memberof proto.MessageContextInfo
         * @instance
         */
        MessageContextInfo.prototype.limitSharingV2 = null;

        /**
         * MessageContextInfo threadId.
         * @member {Array.<proto.IThreadID>} threadId
         * @memberof proto.MessageContextInfo
         * @instance
         */
        MessageContextInfo.prototype.threadId = $util.emptyArray;

        /**
         * MessageContextInfo weblinkRenderConfig.
         * @member {proto.WebLinkRenderConfig|null|undefined} weblinkRenderConfig
         * @memberof proto.MessageContextInfo
         * @instance
         */
        MessageContextInfo.prototype.weblinkRenderConfig = null;

        /**
         * MessageContextInfo teeBotMetadata.
         * @member {Uint8Array|null|undefined} teeBotMetadata
         * @memberof proto.MessageContextInfo
         * @instance
         */
        MessageContextInfo.prototype.teeBotMetadata = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_deviceListMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["deviceListMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_deviceListMetadataVersion", {
            get: $util.oneOfGetter($oneOfFields = ["deviceListMetadataVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_messageSecret", {
            get: $util.oneOfGetter($oneOfFields = ["messageSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_paddingBytes", {
            get: $util.oneOfGetter($oneOfFields = ["paddingBytes"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_messageAddOnDurationInSecs", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOnDurationInSecs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_botMessageSecret", {
            get: $util.oneOfGetter($oneOfFields = ["botMessageSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_botMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_reportingTokenVersion", {
            get: $util.oneOfGetter($oneOfFields = ["reportingTokenVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_messageAddOnExpiryType", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOnExpiryType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_messageAssociation", {
            get: $util.oneOfGetter($oneOfFields = ["messageAssociation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_capiCreatedGroup", {
            get: $util.oneOfGetter($oneOfFields = ["capiCreatedGroup"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_supportPayload", {
            get: $util.oneOfGetter($oneOfFields = ["supportPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_limitSharing", {
            get: $util.oneOfGetter($oneOfFields = ["limitSharing"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_limitSharingV2", {
            get: $util.oneOfGetter($oneOfFields = ["limitSharingV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_weblinkRenderConfig", {
            get: $util.oneOfGetter($oneOfFields = ["weblinkRenderConfig"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_teeBotMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["teeBotMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new MessageContextInfo instance using the specified properties.
         * @function create
         * @memberof proto.MessageContextInfo
         * @static
         * @param {proto.IMessageContextInfo=} [properties] Properties to set
         * @returns {proto.MessageContextInfo} MessageContextInfo instance
         */
        MessageContextInfo.create = function create(properties) {
            return new MessageContextInfo(properties);
        };

        /**
         * Encodes the specified MessageContextInfo message. Does not implicitly {@link proto.MessageContextInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.MessageContextInfo
         * @static
         * @param {proto.IMessageContextInfo} message MessageContextInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MessageContextInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.deviceListMetadata != null && Object.hasOwnProperty.call(message, "deviceListMetadata"))
                $root.proto.DeviceListMetadata.encode(message.deviceListMetadata, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.deviceListMetadataVersion != null && Object.hasOwnProperty.call(message, "deviceListMetadataVersion"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.deviceListMetadataVersion);
            if (message.messageSecret != null && Object.hasOwnProperty.call(message, "messageSecret"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.messageSecret);
            if (message.paddingBytes != null && Object.hasOwnProperty.call(message, "paddingBytes"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.paddingBytes);
            if (message.messageAddOnDurationInSecs != null && Object.hasOwnProperty.call(message, "messageAddOnDurationInSecs"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.messageAddOnDurationInSecs);
            if (message.botMessageSecret != null && Object.hasOwnProperty.call(message, "botMessageSecret"))
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.botMessageSecret);
            if (message.botMetadata != null && Object.hasOwnProperty.call(message, "botMetadata"))
                $root.proto.BotMetadata.encode(message.botMetadata, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
            if (message.reportingTokenVersion != null && Object.hasOwnProperty.call(message, "reportingTokenVersion"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.reportingTokenVersion);
            if (message.messageAddOnExpiryType != null && Object.hasOwnProperty.call(message, "messageAddOnExpiryType"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.messageAddOnExpiryType);
            if (message.messageAssociation != null && Object.hasOwnProperty.call(message, "messageAssociation"))
                $root.proto.MessageAssociation.encode(message.messageAssociation, writer.uint32(/* id 10, wireType 2 =*/82).fork(), q + 1).ldelim();
            if (message.capiCreatedGroup != null && Object.hasOwnProperty.call(message, "capiCreatedGroup"))
                writer.uint32(/* id 11, wireType 0 =*/88).bool(message.capiCreatedGroup);
            if (message.supportPayload != null && Object.hasOwnProperty.call(message, "supportPayload"))
                writer.uint32(/* id 12, wireType 2 =*/98).string(message.supportPayload);
            if (message.limitSharing != null && Object.hasOwnProperty.call(message, "limitSharing"))
                $root.proto.LimitSharing.encode(message.limitSharing, writer.uint32(/* id 13, wireType 2 =*/106).fork(), q + 1).ldelim();
            if (message.limitSharingV2 != null && Object.hasOwnProperty.call(message, "limitSharingV2"))
                $root.proto.LimitSharing.encode(message.limitSharingV2, writer.uint32(/* id 14, wireType 2 =*/114).fork(), q + 1).ldelim();
            if (message.threadId != null && message.threadId.length)
                for (var i = 0; i < message.threadId.length; ++i)
                    $root.proto.ThreadID.encode(message.threadId[i], writer.uint32(/* id 15, wireType 2 =*/122).fork(), q + 1).ldelim();
            if (message.weblinkRenderConfig != null && Object.hasOwnProperty.call(message, "weblinkRenderConfig"))
                writer.uint32(/* id 16, wireType 0 =*/128).int32(message.weblinkRenderConfig);
            if (message.teeBotMetadata != null && Object.hasOwnProperty.call(message, "teeBotMetadata"))
                writer.uint32(/* id 17, wireType 2 =*/138).bytes(message.teeBotMetadata);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MessageContextInfo message, length delimited. Does not implicitly {@link proto.MessageContextInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.MessageContextInfo
         * @static
         * @param {proto.IMessageContextInfo} message MessageContextInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MessageContextInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MessageContextInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.MessageContextInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.MessageContextInfo} MessageContextInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MessageContextInfo.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.deviceListMetadata = $root.proto.DeviceListMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.deviceListMetadata);
                        message._deviceListMetadata = "deviceListMetadata";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.deviceListMetadataVersion = reader.int32();
                        message._deviceListMetadataVersion = "deviceListMetadataVersion";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.messageSecret = reader.bytes();
                        message._messageSecret = "messageSecret";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.paddingBytes = reader.bytes();
                        message._paddingBytes = "paddingBytes";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.messageAddOnDurationInSecs = reader.uint32();
                        message._messageAddOnDurationInSecs = "messageAddOnDurationInSecs";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        message.botMessageSecret = reader.bytes();
                        message._botMessageSecret = "botMessageSecret";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        message.botMetadata = $root.proto.BotMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.botMetadata);
                        message._botMetadata = "botMetadata";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        message.reportingTokenVersion = reader.int32();
                        message._reportingTokenVersion = "reportingTokenVersion";
                        continue;
                    }
                case 9: {
                        if (u !== 0)
                            break;
                        message.messageAddOnExpiryType = reader.int32();
                        message._messageAddOnExpiryType = "messageAddOnExpiryType";
                        continue;
                    }
                case 10: {
                        if (u !== 2)
                            break;
                        message.messageAssociation = $root.proto.MessageAssociation.decode(reader, reader.uint32(), undefined, q + 1, message.messageAssociation);
                        message._messageAssociation = "messageAssociation";
                        continue;
                    }
                case 11: {
                        if (u !== 0)
                            break;
                        message.capiCreatedGroup = reader.bool();
                        message._capiCreatedGroup = "capiCreatedGroup";
                        continue;
                    }
                case 12: {
                        if (u !== 2)
                            break;
                        message.supportPayload = reader.stringVerify();
                        message._supportPayload = "supportPayload";
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        message.limitSharing = $root.proto.LimitSharing.decode(reader, reader.uint32(), undefined, q + 1, message.limitSharing);
                        message._limitSharing = "limitSharing";
                        continue;
                    }
                case 14: {
                        if (u !== 2)
                            break;
                        message.limitSharingV2 = $root.proto.LimitSharing.decode(reader, reader.uint32(), undefined, q + 1, message.limitSharingV2);
                        message._limitSharingV2 = "limitSharingV2";
                        continue;
                    }
                case 15: {
                        if (u !== 2)
                            break;
                        if (!(message.threadId && message.threadId.length))
                            message.threadId = [];
                        message.threadId.push($root.proto.ThreadID.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 16: {
                        if (u !== 0)
                            break;
                        message.weblinkRenderConfig = reader.int32();
                        message._weblinkRenderConfig = "weblinkRenderConfig";
                        continue;
                    }
                case 17: {
                        if (u !== 2)
                            break;
                        message.teeBotMetadata = reader.bytes();
                        message._teeBotMetadata = "teeBotMetadata";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a MessageContextInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.MessageContextInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.MessageContextInfo} MessageContextInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MessageContextInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MessageContextInfo message.
         * @function verify
         * @memberof proto.MessageContextInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MessageContextInfo.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.deviceListMetadata != null && Object.hasOwnProperty.call(message, "deviceListMetadata")) {
                properties._deviceListMetadata = 1;
                {
                    var error = $root.proto.DeviceListMetadata.verify(message.deviceListMetadata, q + 1);
                    if (error)
                        return "deviceListMetadata." + error;
                }
            }
            if (message.deviceListMetadataVersion != null && Object.hasOwnProperty.call(message, "deviceListMetadataVersion")) {
                properties._deviceListMetadataVersion = 1;
                if (!$util.isInteger(message.deviceListMetadataVersion))
                    return "deviceListMetadataVersion: integer expected";
            }
            if (message.messageSecret != null && Object.hasOwnProperty.call(message, "messageSecret")) {
                properties._messageSecret = 1;
                if (!(message.messageSecret && typeof message.messageSecret.length === "number" || $util.isString(message.messageSecret)))
                    return "messageSecret: buffer expected";
            }
            if (message.paddingBytes != null && Object.hasOwnProperty.call(message, "paddingBytes")) {
                properties._paddingBytes = 1;
                if (!(message.paddingBytes && typeof message.paddingBytes.length === "number" || $util.isString(message.paddingBytes)))
                    return "paddingBytes: buffer expected";
            }
            if (message.messageAddOnDurationInSecs != null && Object.hasOwnProperty.call(message, "messageAddOnDurationInSecs")) {
                properties._messageAddOnDurationInSecs = 1;
                if (!$util.isInteger(message.messageAddOnDurationInSecs))
                    return "messageAddOnDurationInSecs: integer expected";
            }
            if (message.botMessageSecret != null && Object.hasOwnProperty.call(message, "botMessageSecret")) {
                properties._botMessageSecret = 1;
                if (!(message.botMessageSecret && typeof message.botMessageSecret.length === "number" || $util.isString(message.botMessageSecret)))
                    return "botMessageSecret: buffer expected";
            }
            if (message.botMetadata != null && Object.hasOwnProperty.call(message, "botMetadata")) {
                properties._botMetadata = 1;
                {
                    var error = $root.proto.BotMetadata.verify(message.botMetadata, q + 1);
                    if (error)
                        return "botMetadata." + error;
                }
            }
            if (message.reportingTokenVersion != null && Object.hasOwnProperty.call(message, "reportingTokenVersion")) {
                properties._reportingTokenVersion = 1;
                if (!$util.isInteger(message.reportingTokenVersion))
                    return "reportingTokenVersion: integer expected";
            }
            if (message.messageAddOnExpiryType != null && Object.hasOwnProperty.call(message, "messageAddOnExpiryType")) {
                properties._messageAddOnExpiryType = 1;
                if (typeof message.messageAddOnExpiryType !== "number" || (message.messageAddOnExpiryType | 0) !== message.messageAddOnExpiryType)
                    return "messageAddOnExpiryType: enum value expected";
            }
            if (message.messageAssociation != null && Object.hasOwnProperty.call(message, "messageAssociation")) {
                properties._messageAssociation = 1;
                {
                    var error = $root.proto.MessageAssociation.verify(message.messageAssociation, q + 1);
                    if (error)
                        return "messageAssociation." + error;
                }
            }
            if (message.capiCreatedGroup != null && Object.hasOwnProperty.call(message, "capiCreatedGroup")) {
                properties._capiCreatedGroup = 1;
                if (typeof message.capiCreatedGroup !== "boolean")
                    return "capiCreatedGroup: boolean expected";
            }
            if (message.supportPayload != null && Object.hasOwnProperty.call(message, "supportPayload")) {
                properties._supportPayload = 1;
                if (!$util.isString(message.supportPayload))
                    return "supportPayload: string expected";
            }
            if (message.limitSharing != null && Object.hasOwnProperty.call(message, "limitSharing")) {
                properties._limitSharing = 1;
                {
                    var error = $root.proto.LimitSharing.verify(message.limitSharing, q + 1);
                    if (error)
                        return "limitSharing." + error;
                }
            }
            if (message.limitSharingV2 != null && Object.hasOwnProperty.call(message, "limitSharingV2")) {
                properties._limitSharingV2 = 1;
                {
                    var error = $root.proto.LimitSharing.verify(message.limitSharingV2, q + 1);
                    if (error)
                        return "limitSharingV2." + error;
                }
            }
            if (message.threadId != null && Object.hasOwnProperty.call(message, "threadId")) {
                if (!Array.isArray(message.threadId))
                    return "threadId: array expected";
                for (var i = 0; i < message.threadId.length; ++i) {
                    var error = $root.proto.ThreadID.verify(message.threadId[i], q + 1);
                    if (error)
                        return "threadId." + error;
                }
            }
            if (message.weblinkRenderConfig != null && Object.hasOwnProperty.call(message, "weblinkRenderConfig")) {
                properties._weblinkRenderConfig = 1;
                if (typeof message.weblinkRenderConfig !== "number" || (message.weblinkRenderConfig | 0) !== message.weblinkRenderConfig)
                    return "weblinkRenderConfig: enum value expected";
            }
            if (message.teeBotMetadata != null && Object.hasOwnProperty.call(message, "teeBotMetadata")) {
                properties._teeBotMetadata = 1;
                if (!(message.teeBotMetadata && typeof message.teeBotMetadata.length === "number" || $util.isString(message.teeBotMetadata)))
                    return "teeBotMetadata: buffer expected";
            }
            return null;
        };

        /**
         * Creates a MessageContextInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.MessageContextInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.MessageContextInfo} MessageContextInfo
         */
        MessageContextInfo.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.MessageContextInfo: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.deviceListMetadata != null) {
                if (!$util.isObject(object.deviceListMetadata))
                    throw TypeError(".proto.MessageContextInfo.deviceListMetadata: object expected");
                message.deviceListMetadata = $root.proto.DeviceListMetadata.fromObject(object.deviceListMetadata, q + 1);
            }
            if (object.deviceListMetadataVersion != null)
                message.deviceListMetadataVersion = object.deviceListMetadataVersion | 0;
            if (object.messageSecret != null)
                if (typeof object.messageSecret === "string")
                    $util.base64.decode(object.messageSecret, message.messageSecret = $util.newBuffer($util.base64.length(object.messageSecret)), 0);
                else if (object.messageSecret.length >= 0)
                    message.messageSecret = object.messageSecret;
            if (object.paddingBytes != null)
                if (typeof object.paddingBytes === "string")
                    $util.base64.decode(object.paddingBytes, message.paddingBytes = $util.newBuffer($util.base64.length(object.paddingBytes)), 0);
                else if (object.paddingBytes.length >= 0)
                    message.paddingBytes = object.paddingBytes;
            if (object.messageAddOnDurationInSecs != null)
                message.messageAddOnDurationInSecs = object.messageAddOnDurationInSecs >>> 0;
            if (object.botMessageSecret != null)
                if (typeof object.botMessageSecret === "string")
                    $util.base64.decode(object.botMessageSecret, message.botMessageSecret = $util.newBuffer($util.base64.length(object.botMessageSecret)), 0);
                else if (object.botMessageSecret.length >= 0)
                    message.botMessageSecret = object.botMessageSecret;
            if (object.botMetadata != null) {
                if (!$util.isObject(object.botMetadata))
                    throw TypeError(".proto.MessageContextInfo.botMetadata: object expected");
                message.botMetadata = $root.proto.BotMetadata.fromObject(object.botMetadata, q + 1);
            }
            if (object.reportingTokenVersion != null)
                message.reportingTokenVersion = object.reportingTokenVersion | 0;
            switch (object.messageAddOnExpiryType) {
            case "STATIC":
            case 1:
                message.messageAddOnExpiryType = 1;
                break;
            case "DEPENDENT_ON_PARENT":
            case 2:
                message.messageAddOnExpiryType = 2;
                break;
            default:
                if (typeof object.messageAddOnExpiryType === "number" && (object.messageAddOnExpiryType | 0) === object.messageAddOnExpiryType)
                    message.messageAddOnExpiryType = object.messageAddOnExpiryType;
            }
            if (object.messageAssociation != null) {
                if (!$util.isObject(object.messageAssociation))
                    throw TypeError(".proto.MessageContextInfo.messageAssociation: object expected");
                message.messageAssociation = $root.proto.MessageAssociation.fromObject(object.messageAssociation, q + 1);
            }
            if (object.capiCreatedGroup != null)
                message.capiCreatedGroup = Boolean(object.capiCreatedGroup);
            if (object.supportPayload != null)
                message.supportPayload = String(object.supportPayload);
            if (object.limitSharing != null) {
                if (!$util.isObject(object.limitSharing))
                    throw TypeError(".proto.MessageContextInfo.limitSharing: object expected");
                message.limitSharing = $root.proto.LimitSharing.fromObject(object.limitSharing, q + 1);
            }
            if (object.limitSharingV2 != null) {
                if (!$util.isObject(object.limitSharingV2))
                    throw TypeError(".proto.MessageContextInfo.limitSharingV2: object expected");
                message.limitSharingV2 = $root.proto.LimitSharing.fromObject(object.limitSharingV2, q + 1);
            }
            if (object.threadId) {
                if (!Array.isArray(object.threadId))
                    throw TypeError(".proto.MessageContextInfo.threadId: array expected");
                message.threadId = Array(object.threadId.length);
                for (var i = 0; i < object.threadId.length; ++i) {
                    if (!$util.isObject(object.threadId[i]))
                        throw TypeError(".proto.MessageContextInfo.threadId: object expected");
                    message.threadId[i] = $root.proto.ThreadID.fromObject(object.threadId[i], q + 1);
                }
            }
            switch (object.weblinkRenderConfig) {
            case "WEBVIEW":
            case 0:
                message.weblinkRenderConfig = 0;
                break;
            case "SYSTEM":
            case 1:
                message.weblinkRenderConfig = 1;
                break;
            default:
                if (typeof object.weblinkRenderConfig === "number" && (object.weblinkRenderConfig | 0) === object.weblinkRenderConfig)
                    message.weblinkRenderConfig = object.weblinkRenderConfig;
            }
            if (object.teeBotMetadata != null)
                if (typeof object.teeBotMetadata === "string")
                    $util.base64.decode(object.teeBotMetadata, message.teeBotMetadata = $util.newBuffer($util.base64.length(object.teeBotMetadata)), 0);
                else if (object.teeBotMetadata.length >= 0)
                    message.teeBotMetadata = object.teeBotMetadata;
            return message;
        };

        /**
         * Creates a plain object from a MessageContextInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.MessageContextInfo
         * @static
         * @param {proto.MessageContextInfo} message MessageContextInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MessageContextInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.threadId = [];
            if (message.deviceListMetadata != null && Object.hasOwnProperty.call(message, "deviceListMetadata"))
                object.deviceListMetadata = $root.proto.DeviceListMetadata.toObject(message.deviceListMetadata, options, q + 1);
            if (message.deviceListMetadataVersion != null && Object.hasOwnProperty.call(message, "deviceListMetadataVersion"))
                object.deviceListMetadataVersion = message.deviceListMetadataVersion;
            if (message.messageSecret != null && Object.hasOwnProperty.call(message, "messageSecret"))
                object.messageSecret = options.bytes === String ? $util.base64.encode(message.messageSecret, 0, message.messageSecret.length) : options.bytes === Array ? Array.prototype.slice.call(message.messageSecret) : message.messageSecret;
            if (message.paddingBytes != null && Object.hasOwnProperty.call(message, "paddingBytes"))
                object.paddingBytes = options.bytes === String ? $util.base64.encode(message.paddingBytes, 0, message.paddingBytes.length) : options.bytes === Array ? Array.prototype.slice.call(message.paddingBytes) : message.paddingBytes;
            if (message.messageAddOnDurationInSecs != null && Object.hasOwnProperty.call(message, "messageAddOnDurationInSecs"))
                object.messageAddOnDurationInSecs = message.messageAddOnDurationInSecs;
            if (message.botMessageSecret != null && Object.hasOwnProperty.call(message, "botMessageSecret"))
                object.botMessageSecret = options.bytes === String ? $util.base64.encode(message.botMessageSecret, 0, message.botMessageSecret.length) : options.bytes === Array ? Array.prototype.slice.call(message.botMessageSecret) : message.botMessageSecret;
            if (message.botMetadata != null && Object.hasOwnProperty.call(message, "botMetadata"))
                object.botMetadata = $root.proto.BotMetadata.toObject(message.botMetadata, options, q + 1);
            if (message.reportingTokenVersion != null && Object.hasOwnProperty.call(message, "reportingTokenVersion"))
                object.reportingTokenVersion = message.reportingTokenVersion;
            if (message.messageAddOnExpiryType != null && Object.hasOwnProperty.call(message, "messageAddOnExpiryType"))
                object.messageAddOnExpiryType = options.enums === String ? $root.proto.MessageContextInfo.MessageAddonExpiryType[message.messageAddOnExpiryType] === undefined ? message.messageAddOnExpiryType : $root.proto.MessageContextInfo.MessageAddonExpiryType[message.messageAddOnExpiryType] : message.messageAddOnExpiryType;
            if (message.messageAssociation != null && Object.hasOwnProperty.call(message, "messageAssociation"))
                object.messageAssociation = $root.proto.MessageAssociation.toObject(message.messageAssociation, options, q + 1);
            if (message.capiCreatedGroup != null && Object.hasOwnProperty.call(message, "capiCreatedGroup"))
                object.capiCreatedGroup = message.capiCreatedGroup;
            if (message.supportPayload != null && Object.hasOwnProperty.call(message, "supportPayload"))
                object.supportPayload = message.supportPayload;
            if (message.limitSharing != null && Object.hasOwnProperty.call(message, "limitSharing"))
                object.limitSharing = $root.proto.LimitSharing.toObject(message.limitSharing, options, q + 1);
            if (message.limitSharingV2 != null && Object.hasOwnProperty.call(message, "limitSharingV2"))
                object.limitSharingV2 = $root.proto.LimitSharing.toObject(message.limitSharingV2, options, q + 1);
            if (message.threadId && message.threadId.length) {
                object.threadId = Array(message.threadId.length);
                for (var j = 0; j < message.threadId.length; ++j)
                    object.threadId[j] = $root.proto.ThreadID.toObject(message.threadId[j], options, q + 1);
            }
            if (message.weblinkRenderConfig != null && Object.hasOwnProperty.call(message, "weblinkRenderConfig"))
                object.weblinkRenderConfig = options.enums === String ? $root.proto.WebLinkRenderConfig[message.weblinkRenderConfig] === undefined ? message.weblinkRenderConfig : $root.proto.WebLinkRenderConfig[message.weblinkRenderConfig] : message.weblinkRenderConfig;
            if (message.teeBotMetadata != null && Object.hasOwnProperty.call(message, "teeBotMetadata"))
                object.teeBotMetadata = options.bytes === String ? $util.base64.encode(message.teeBotMetadata, 0, message.teeBotMetadata.length) : options.bytes === Array ? Array.prototype.slice.call(message.teeBotMetadata) : message.teeBotMetadata;
            return object;
        };

        /**
         * Converts this MessageContextInfo to JSON.
         * @function toJSON
         * @memberof proto.MessageContextInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MessageContextInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MessageContextInfo
         * @function getTypeUrl
         * @memberof proto.MessageContextInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MessageContextInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.MessageContextInfo";
        };

        /**
         * MessageAddonExpiryType enum.
         * @name proto.MessageContextInfo.MessageAddonExpiryType
         * @enum {number}
         * @property {number} STATIC=1 STATIC value
         * @property {number} DEPENDENT_ON_PARENT=2 DEPENDENT_ON_PARENT value
         */
        MessageContextInfo.MessageAddonExpiryType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[1] = "STATIC"] = 1;
            values[valuesById[2] = "DEPENDENT_ON_PARENT"] = 2;
            return values;
        })();

        return MessageContextInfo;
    })();

    proto.MessageKey = (function() {

        /**
         * Properties of a MessageKey.
         * @memberof proto
         * @interface IMessageKey
         * @property {string|null} [remoteJid] MessageKey remoteJid
         * @property {boolean|null} [fromMe] MessageKey fromMe
         * @property {string|null} [id] MessageKey id
         * @property {string|null} [participant] MessageKey participant
         */

        /**
         * Constructs a new MessageKey.
         * @memberof proto
         * @classdesc Represents a MessageKey.
         * @implements IMessageKey
         * @constructor
         * @param {proto.IMessageKey=} [properties] Properties to set
         */
        function MessageKey(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MessageKey remoteJid.
         * @member {string|null|undefined} remoteJid
         * @memberof proto.MessageKey
         * @instance
         */
        MessageKey.prototype.remoteJid = null;

        /**
         * MessageKey fromMe.
         * @member {boolean|null|undefined} fromMe
         * @memberof proto.MessageKey
         * @instance
         */
        MessageKey.prototype.fromMe = null;

        /**
         * MessageKey id.
         * @member {string|null|undefined} id
         * @memberof proto.MessageKey
         * @instance
         */
        MessageKey.prototype.id = null;

        /**
         * MessageKey participant.
         * @member {string|null|undefined} participant
         * @memberof proto.MessageKey
         * @instance
         */
        MessageKey.prototype.participant = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageKey.prototype, "_remoteJid", {
            get: $util.oneOfGetter($oneOfFields = ["remoteJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageKey.prototype, "_fromMe", {
            get: $util.oneOfGetter($oneOfFields = ["fromMe"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageKey.prototype, "_id", {
            get: $util.oneOfGetter($oneOfFields = ["id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageKey.prototype, "_participant", {
            get: $util.oneOfGetter($oneOfFields = ["participant"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new MessageKey instance using the specified properties.
         * @function create
         * @memberof proto.MessageKey
         * @static
         * @param {proto.IMessageKey=} [properties] Properties to set
         * @returns {proto.MessageKey} MessageKey instance
         */
        MessageKey.create = function create(properties) {
            return new MessageKey(properties);
        };

        /**
         * Encodes the specified MessageKey message. Does not implicitly {@link proto.MessageKey.verify|verify} messages.
         * @function encode
         * @memberof proto.MessageKey
         * @static
         * @param {proto.IMessageKey} message MessageKey message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MessageKey.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.remoteJid != null && Object.hasOwnProperty.call(message, "remoteJid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.remoteJid);
            if (message.fromMe != null && Object.hasOwnProperty.call(message, "fromMe"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.fromMe);
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.id);
            if (message.participant != null && Object.hasOwnProperty.call(message, "participant"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.participant);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MessageKey message, length delimited. Does not implicitly {@link proto.MessageKey.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.MessageKey
         * @static
         * @param {proto.IMessageKey} message MessageKey message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MessageKey.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MessageKey message from the specified reader or buffer.
         * @function decode
         * @memberof proto.MessageKey
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.MessageKey} MessageKey
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MessageKey.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.remoteJid = reader.stringVerify();
                        message._remoteJid = "remoteJid";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.fromMe = reader.bool();
                        message._fromMe = "fromMe";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.id = reader.stringVerify();
                        message._id = "id";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.participant = reader.stringVerify();
                        message._participant = "participant";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a MessageKey message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.MessageKey
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.MessageKey} MessageKey
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MessageKey.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MessageKey message.
         * @function verify
         * @memberof proto.MessageKey
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MessageKey.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.remoteJid != null && Object.hasOwnProperty.call(message, "remoteJid")) {
                properties._remoteJid = 1;
                if (!$util.isString(message.remoteJid))
                    return "remoteJid: string expected";
            }
            if (message.fromMe != null && Object.hasOwnProperty.call(message, "fromMe")) {
                properties._fromMe = 1;
                if (typeof message.fromMe !== "boolean")
                    return "fromMe: boolean expected";
            }
            if (message.id != null && Object.hasOwnProperty.call(message, "id")) {
                properties._id = 1;
                if (!$util.isString(message.id))
                    return "id: string expected";
            }
            if (message.participant != null && Object.hasOwnProperty.call(message, "participant")) {
                properties._participant = 1;
                if (!$util.isString(message.participant))
                    return "participant: string expected";
            }
            return null;
        };

        /**
         * Creates a MessageKey message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.MessageKey
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.MessageKey} MessageKey
         */
        MessageKey.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.MessageKey: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.remoteJid != null)
                message.remoteJid = String(object.remoteJid);
            if (object.fromMe != null)
                message.fromMe = Boolean(object.fromMe);
            if (object.id != null)
                message.id = String(object.id);
            if (object.participant != null)
                message.participant = String(object.participant);
            return message;
        };

        /**
         * Creates a plain object from a MessageKey message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.MessageKey
         * @static
         * @param {proto.MessageKey} message MessageKey
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MessageKey.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.remoteJid != null && Object.hasOwnProperty.call(message, "remoteJid"))
                object.remoteJid = message.remoteJid;
            if (message.fromMe != null && Object.hasOwnProperty.call(message, "fromMe"))
                object.fromMe = message.fromMe;
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.participant != null && Object.hasOwnProperty.call(message, "participant"))
                object.participant = message.participant;
            return object;
        };

        /**
         * Converts this MessageKey to JSON.
         * @function toJSON
         * @memberof proto.MessageKey
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MessageKey.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MessageKey
         * @function getTypeUrl
         * @memberof proto.MessageKey
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MessageKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.MessageKey";
        };

        return MessageKey;
    })();

    proto.MessageSecretMessage = (function() {

        /**
         * Properties of a MessageSecretMessage.
         * @memberof proto
         * @interface IMessageSecretMessage
         * @property {number|null} [version] MessageSecretMessage version
         * @property {Uint8Array|null} [encIv] MessageSecretMessage encIv
         * @property {Uint8Array|null} [encPayload] MessageSecretMessage encPayload
         */

        /**
         * Constructs a new MessageSecretMessage.
         * @memberof proto
         * @classdesc Represents a MessageSecretMessage.
         * @implements IMessageSecretMessage
         * @constructor
         * @param {proto.IMessageSecretMessage=} [properties] Properties to set
         */
        function MessageSecretMessage(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MessageSecretMessage version.
         * @member {number|null|undefined} version
         * @memberof proto.MessageSecretMessage
         * @instance
         */
        MessageSecretMessage.prototype.version = null;

        /**
         * MessageSecretMessage encIv.
         * @member {Uint8Array|null|undefined} encIv
         * @memberof proto.MessageSecretMessage
         * @instance
         */
        MessageSecretMessage.prototype.encIv = null;

        /**
         * MessageSecretMessage encPayload.
         * @member {Uint8Array|null|undefined} encPayload
         * @memberof proto.MessageSecretMessage
         * @instance
         */
        MessageSecretMessage.prototype.encPayload = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageSecretMessage.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageSecretMessage.prototype, "_encIv", {
            get: $util.oneOfGetter($oneOfFields = ["encIv"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageSecretMessage.prototype, "_encPayload", {
            get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new MessageSecretMessage instance using the specified properties.
         * @function create
         * @memberof proto.MessageSecretMessage
         * @static
         * @param {proto.IMessageSecretMessage=} [properties] Properties to set
         * @returns {proto.MessageSecretMessage} MessageSecretMessage instance
         */
        MessageSecretMessage.create = function create(properties) {
            return new MessageSecretMessage(properties);
        };

        /**
         * Encodes the specified MessageSecretMessage message. Does not implicitly {@link proto.MessageSecretMessage.verify|verify} messages.
         * @function encode
         * @memberof proto.MessageSecretMessage
         * @static
         * @param {proto.IMessageSecretMessage} message MessageSecretMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MessageSecretMessage.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                writer.uint32(/* id 1, wireType 5 =*/13).sfixed32(message.version);
            if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encIv);
            if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.encPayload);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MessageSecretMessage message, length delimited. Does not implicitly {@link proto.MessageSecretMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.MessageSecretMessage
         * @static
         * @param {proto.IMessageSecretMessage} message MessageSecretMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MessageSecretMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MessageSecretMessage message from the specified reader or buffer.
         * @function decode
         * @memberof proto.MessageSecretMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.MessageSecretMessage} MessageSecretMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MessageSecretMessage.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 5)
                            break;
                        message.version = reader.sfixed32();
                        message._version = "version";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.encIv = reader.bytes();
                        message._encIv = "encIv";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.encPayload = reader.bytes();
                        message._encPayload = "encPayload";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a MessageSecretMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.MessageSecretMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.MessageSecretMessage} MessageSecretMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MessageSecretMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MessageSecretMessage message.
         * @function verify
         * @memberof proto.MessageSecretMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MessageSecretMessage.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.version != null && Object.hasOwnProperty.call(message, "version")) {
                properties._version = 1;
                if (!$util.isInteger(message.version))
                    return "version: integer expected";
            }
            if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv")) {
                properties._encIv = 1;
                if (!(message.encIv && typeof message.encIv.length === "number" || $util.isString(message.encIv)))
                    return "encIv: buffer expected";
            }
            if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload")) {
                properties._encPayload = 1;
                if (!(message.encPayload && typeof message.encPayload.length === "number" || $util.isString(message.encPayload)))
                    return "encPayload: buffer expected";
            }
            return null;
        };

        /**
         * Creates a MessageSecretMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.MessageSecretMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.MessageSecretMessage} MessageSecretMessage
         */
        MessageSecretMessage.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.MessageSecretMessage: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.version != null)
                message.version = object.version | 0;
            if (object.encIv != null)
                if (typeof object.encIv === "string")
                    $util.base64.decode(object.encIv, message.encIv = $util.newBuffer($util.base64.length(object.encIv)), 0);
                else if (object.encIv.length >= 0)
                    message.encIv = object.encIv;
            if (object.encPayload != null)
                if (typeof object.encPayload === "string")
                    $util.base64.decode(object.encPayload, message.encPayload = $util.newBuffer($util.base64.length(object.encPayload)), 0);
                else if (object.encPayload.length >= 0)
                    message.encPayload = object.encPayload;
            return message;
        };

        /**
         * Creates a plain object from a MessageSecretMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.MessageSecretMessage
         * @static
         * @param {proto.MessageSecretMessage} message MessageSecretMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MessageSecretMessage.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                object.version = message.version;
            if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv"))
                object.encIv = options.bytes === String ? $util.base64.encode(message.encIv, 0, message.encIv.length) : options.bytes === Array ? Array.prototype.slice.call(message.encIv) : message.encIv;
            if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload"))
                object.encPayload = options.bytes === String ? $util.base64.encode(message.encPayload, 0, message.encPayload.length) : options.bytes === Array ? Array.prototype.slice.call(message.encPayload) : message.encPayload;
            return object;
        };

        /**
         * Converts this MessageSecretMessage to JSON.
         * @function toJSON
         * @memberof proto.MessageSecretMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MessageSecretMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MessageSecretMessage
         * @function getTypeUrl
         * @memberof proto.MessageSecretMessage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MessageSecretMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.MessageSecretMessage";
        };

        return MessageSecretMessage;
    })();

    proto.Money = (function() {

        /**
         * Properties of a Money.
         * @memberof proto
         * @interface IMoney
         * @property {number|Long|null} [value] Money value
         * @property {number|null} [offset] Money offset
         * @property {string|null} [currencyCode] Money currencyCode
         */

        /**
         * Constructs a new Money.
         * @memberof proto
         * @classdesc Represents a Money.
         * @implements IMoney
         * @constructor
         * @param {proto.IMoney=} [properties] Properties to set
         */
        function Money(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Money value.
         * @member {number|Long|null|undefined} value
         * @memberof proto.Money
         * @instance
         */
        Money.prototype.value = null;

        /**
         * Money offset.
         * @member {number|null|undefined} offset
         * @memberof proto.Money
         * @instance
         */
        Money.prototype.offset = null;

        /**
         * Money currencyCode.
         * @member {string|null|undefined} currencyCode
         * @memberof proto.Money
         * @instance
         */
        Money.prototype.currencyCode = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Money.prototype, "_value", {
            get: $util.oneOfGetter($oneOfFields = ["value"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Money.prototype, "_offset", {
            get: $util.oneOfGetter($oneOfFields = ["offset"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Money.prototype, "_currencyCode", {
            get: $util.oneOfGetter($oneOfFields = ["currencyCode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new Money instance using the specified properties.
         * @function create
         * @memberof proto.Money
         * @static
         * @param {proto.IMoney=} [properties] Properties to set
         * @returns {proto.Money} Money instance
         */
        Money.create = function create(properties) {
            return new Money(properties);
        };

        /**
         * Encodes the specified Money message. Does not implicitly {@link proto.Money.verify|verify} messages.
         * @function encode
         * @memberof proto.Money
         * @static
         * @param {proto.IMoney} message Money message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Money.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.value);
            if (message.offset != null && Object.hasOwnProperty.call(message, "offset"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.offset);
            if (message.currencyCode != null && Object.hasOwnProperty.call(message, "currencyCode"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.currencyCode);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Money message, length delimited. Does not implicitly {@link proto.Money.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Money
         * @static
         * @param {proto.IMoney} message Money message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Money.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Money message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Money
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Money} Money
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Money.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.value = reader.int64();
                        message._value = "value";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.offset = reader.uint32();
                        message._offset = "offset";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.currencyCode = reader.stringVerify();
                        message._currencyCode = "currencyCode";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a Money message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Money
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Money} Money
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Money.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Money message.
         * @function verify
         * @memberof proto.Money
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Money.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.value != null && Object.hasOwnProperty.call(message, "value")) {
                properties._value = 1;
                if (!$util.isInteger(message.value) && !(message.value && $util.isInteger(message.value.low) && $util.isInteger(message.value.high)))
                    return "value: integer|Long expected";
            }
            if (message.offset != null && Object.hasOwnProperty.call(message, "offset")) {
                properties._offset = 1;
                if (!$util.isInteger(message.offset))
                    return "offset: integer expected";
            }
            if (message.currencyCode != null && Object.hasOwnProperty.call(message, "currencyCode")) {
                properties._currencyCode = 1;
                if (!$util.isString(message.currencyCode))
                    return "currencyCode: string expected";
            }
            return null;
        };

        /**
         * Creates a Money message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Money
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Money} Money
         */
        Money.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.Money: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.value != null)
                if ($util.Long)
                    message.value = $util.Long.fromValue(object.value, false);
                else if (typeof object.value === "string")
                    message.value = parseInt(object.value, 10);
                else if (typeof object.value === "number")
                    message.value = object.value;
                else if (typeof object.value === "object")
                    message.value = new $util.LongBits(object.value.low >>> 0, object.value.high >>> 0).toNumber();
            if (object.offset != null)
                message.offset = object.offset >>> 0;
            if (object.currencyCode != null)
                message.currencyCode = String(object.currencyCode);
            return message;
        };

        /**
         * Creates a plain object from a Money message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Money
         * @static
         * @param {proto.Money} message Money
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Money.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.value = typeof message.value === "number" ? BigInt(message.value) : $util.Long.fromBits(message.value.low >>> 0, message.value.high >>> 0, false).toBigInt();
                else if (typeof message.value === "number")
                    object.value = options.longs === String ? String(message.value) : message.value;
                else
                    object.value = options.longs === String ? $util.Long.prototype.toString.call(message.value) : options.longs === Number ? new $util.LongBits(message.value.low >>> 0, message.value.high >>> 0).toNumber() : message.value;
            if (message.offset != null && Object.hasOwnProperty.call(message, "offset"))
                object.offset = message.offset;
            if (message.currencyCode != null && Object.hasOwnProperty.call(message, "currencyCode"))
                object.currencyCode = message.currencyCode;
            return object;
        };

        /**
         * Converts this Money to JSON.
         * @function toJSON
         * @memberof proto.Money
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Money.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Money
         * @function getTypeUrl
         * @memberof proto.Money
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Money.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.Money";
        };

        return Money;
    })();

    proto.MsgOpaqueData = (function() {

        /**
         * Properties of a MsgOpaqueData.
         * @memberof proto
         * @interface IMsgOpaqueData
         * @property {string|null} [body] MsgOpaqueData body
         * @property {string|null} [caption] MsgOpaqueData caption
         * @property {number|null} [lng] MsgOpaqueData lng
         * @property {boolean|null} [isLive] MsgOpaqueData isLive
         * @property {number|null} [lat] MsgOpaqueData lat
         * @property {number|null} [paymentAmount1000] MsgOpaqueData paymentAmount1000
         * @property {string|null} [paymentNoteMsgBody] MsgOpaqueData paymentNoteMsgBody
         * @property {string|null} [matchedText] MsgOpaqueData matchedText
         * @property {string|null} [title] MsgOpaqueData title
         * @property {string|null} [description] MsgOpaqueData description
         * @property {Uint8Array|null} [futureproofBuffer] MsgOpaqueData futureproofBuffer
         * @property {string|null} [clientUrl] MsgOpaqueData clientUrl
         * @property {string|null} [loc] MsgOpaqueData loc
         * @property {string|null} [pollName] MsgOpaqueData pollName
         * @property {Array.<proto.MsgOpaqueData.IPollOption>|null} [pollOptions] MsgOpaqueData pollOptions
         * @property {number|null} [pollSelectableOptionsCount] MsgOpaqueData pollSelectableOptionsCount
         * @property {Uint8Array|null} [messageSecret] MsgOpaqueData messageSecret
         * @property {string|null} [originalSelfAuthor] MsgOpaqueData originalSelfAuthor
         * @property {number|Long|null} [senderTimestampMs] MsgOpaqueData senderTimestampMs
         * @property {string|null} [pollUpdateParentKey] MsgOpaqueData pollUpdateParentKey
         * @property {proto.IPollEncValue|null} [encPollVote] MsgOpaqueData encPollVote
         * @property {boolean|null} [isSentCagPollCreation] MsgOpaqueData isSentCagPollCreation
         * @property {proto.MsgOpaqueData.PollContentType|null} [pollContentType] MsgOpaqueData pollContentType
         * @property {proto.MsgOpaqueData.PollType|null} [pollType] MsgOpaqueData pollType
         * @property {number|null} [correctOptionIndex] MsgOpaqueData correctOptionIndex
         * @property {proto.MsgOpaqueData.IPollVotesSnapshot|null} [pollVotesSnapshot] MsgOpaqueData pollVotesSnapshot
         * @property {string|null} [encReactionTargetMessageKey] MsgOpaqueData encReactionTargetMessageKey
         * @property {Uint8Array|null} [encReactionEncPayload] MsgOpaqueData encReactionEncPayload
         * @property {Uint8Array|null} [encReactionEncIv] MsgOpaqueData encReactionEncIv
         * @property {Uint8Array|null} [botMessageSecret] MsgOpaqueData botMessageSecret
         * @property {string|null} [targetMessageKey] MsgOpaqueData targetMessageKey
         * @property {Uint8Array|null} [encPayload] MsgOpaqueData encPayload
         * @property {Uint8Array|null} [encIv] MsgOpaqueData encIv
         * @property {string|null} [eventName] MsgOpaqueData eventName
         * @property {boolean|null} [isEventCanceled] MsgOpaqueData isEventCanceled
         * @property {string|null} [eventDescription] MsgOpaqueData eventDescription
         * @property {string|null} [eventJoinLink] MsgOpaqueData eventJoinLink
         * @property {number|Long|null} [eventStartTime] MsgOpaqueData eventStartTime
         * @property {proto.MsgOpaqueData.IEventLocation|null} [eventLocation] MsgOpaqueData eventLocation
         * @property {number|Long|null} [eventEndTime] MsgOpaqueData eventEndTime
         * @property {boolean|null} [eventIsScheduledCall] MsgOpaqueData eventIsScheduledCall
         * @property {boolean|null} [eventExtraGuestsAllowed] MsgOpaqueData eventExtraGuestsAllowed
         * @property {Uint8Array|null} [plainProtobufBytes] MsgOpaqueData plainProtobufBytes
         * @property {string|null} [quarantineExtractedText] MsgOpaqueData quarantineExtractedText
         * @property {number|Long|null} [pollEndTime] MsgOpaqueData pollEndTime
         * @property {boolean|null} [pollHideVoterNames] MsgOpaqueData pollHideVoterNames
         * @property {boolean|null} [pollAllowAddOption] MsgOpaqueData pollAllowAddOption
         * @property {string|null} [sharableEventInviteId] MsgOpaqueData sharableEventInviteId
         * @property {string|null} [sharableEventInviteTitle] MsgOpaqueData sharableEventInviteTitle
         * @property {number|Long|null} [sharableEventInviteStartTime] MsgOpaqueData sharableEventInviteStartTime
         * @property {number|Long|null} [sharableEventInviteEndTime] MsgOpaqueData sharableEventInviteEndTime
         * @property {string|null} [sharableEventInviteCaption] MsgOpaqueData sharableEventInviteCaption
         * @property {boolean|null} [sharableEventInviteIsCanceled] MsgOpaqueData sharableEventInviteIsCanceled
         * @property {Uint8Array|null} [sharableEventInviteJpegThumbnail] MsgOpaqueData sharableEventInviteJpegThumbnail
         * @property {string|null} [sharableEventInviteCallLink] MsgOpaqueData sharableEventInviteCallLink
         */

        /**
         * Constructs a new MsgOpaqueData.
         * @memberof proto
         * @classdesc Represents a MsgOpaqueData.
         * @implements IMsgOpaqueData
         * @constructor
         * @param {proto.IMsgOpaqueData=} [properties] Properties to set
         */
        function MsgOpaqueData(properties) {
            this.pollOptions = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MsgOpaqueData body.
         * @member {string|null|undefined} body
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.body = null;

        /**
         * MsgOpaqueData caption.
         * @member {string|null|undefined} caption
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.caption = null;

        /**
         * MsgOpaqueData lng.
         * @member {number|null|undefined} lng
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.lng = null;

        /**
         * MsgOpaqueData isLive.
         * @member {boolean|null|undefined} isLive
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.isLive = null;

        /**
         * MsgOpaqueData lat.
         * @member {number|null|undefined} lat
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.lat = null;

        /**
         * MsgOpaqueData paymentAmount1000.
         * @member {number|null|undefined} paymentAmount1000
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.paymentAmount1000 = null;

        /**
         * MsgOpaqueData paymentNoteMsgBody.
         * @member {string|null|undefined} paymentNoteMsgBody
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.paymentNoteMsgBody = null;

        /**
         * MsgOpaqueData matchedText.
         * @member {string|null|undefined} matchedText
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.matchedText = null;

        /**
         * MsgOpaqueData title.
         * @member {string|null|undefined} title
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.title = null;

        /**
         * MsgOpaqueData description.
         * @member {string|null|undefined} description
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.description = null;

        /**
         * MsgOpaqueData futureproofBuffer.
         * @member {Uint8Array|null|undefined} futureproofBuffer
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.futureproofBuffer = null;

        /**
         * MsgOpaqueData clientUrl.
         * @member {string|null|undefined} clientUrl
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.clientUrl = null;

        /**
         * MsgOpaqueData loc.
         * @member {string|null|undefined} loc
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.loc = null;

        /**
         * MsgOpaqueData pollName.
         * @member {string|null|undefined} pollName
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.pollName = null;

        /**
         * MsgOpaqueData pollOptions.
         * @member {Array.<proto.MsgOpaqueData.IPollOption>} pollOptions
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.pollOptions = $util.emptyArray;

        /**
         * MsgOpaqueData pollSelectableOptionsCount.
         * @member {number|null|undefined} pollSelectableOptionsCount
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.pollSelectableOptionsCount = null;

        /**
         * MsgOpaqueData messageSecret.
         * @member {Uint8Array|null|undefined} messageSecret
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.messageSecret = null;

        /**
         * MsgOpaqueData originalSelfAuthor.
         * @member {string|null|undefined} originalSelfAuthor
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.originalSelfAuthor = null;

        /**
         * MsgOpaqueData senderTimestampMs.
         * @member {number|Long|null|undefined} senderTimestampMs
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.senderTimestampMs = null;

        /**
         * MsgOpaqueData pollUpdateParentKey.
         * @member {string|null|undefined} pollUpdateParentKey
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.pollUpdateParentKey = null;

        /**
         * MsgOpaqueData encPollVote.
         * @member {proto.IPollEncValue|null|undefined} encPollVote
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.encPollVote = null;

        /**
         * MsgOpaqueData isSentCagPollCreation.
         * @member {boolean|null|undefined} isSentCagPollCreation
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.isSentCagPollCreation = null;

        /**
         * MsgOpaqueData pollContentType.
         * @member {proto.MsgOpaqueData.PollContentType|null|undefined} pollContentType
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.pollContentType = null;

        /**
         * MsgOpaqueData pollType.
         * @member {proto.MsgOpaqueData.PollType|null|undefined} pollType
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.pollType = null;

        /**
         * MsgOpaqueData correctOptionIndex.
         * @member {number|null|undefined} correctOptionIndex
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.correctOptionIndex = null;

        /**
         * MsgOpaqueData pollVotesSnapshot.
         * @member {proto.MsgOpaqueData.IPollVotesSnapshot|null|undefined} pollVotesSnapshot
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.pollVotesSnapshot = null;

        /**
         * MsgOpaqueData encReactionTargetMessageKey.
         * @member {string|null|undefined} encReactionTargetMessageKey
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.encReactionTargetMessageKey = null;

        /**
         * MsgOpaqueData encReactionEncPayload.
         * @member {Uint8Array|null|undefined} encReactionEncPayload
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.encReactionEncPayload = null;

        /**
         * MsgOpaqueData encReactionEncIv.
         * @member {Uint8Array|null|undefined} encReactionEncIv
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.encReactionEncIv = null;

        /**
         * MsgOpaqueData botMessageSecret.
         * @member {Uint8Array|null|undefined} botMessageSecret
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.botMessageSecret = null;

        /**
         * MsgOpaqueData targetMessageKey.
         * @member {string|null|undefined} targetMessageKey
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.targetMessageKey = null;

        /**
         * MsgOpaqueData encPayload.
         * @member {Uint8Array|null|undefined} encPayload
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.encPayload = null;

        /**
         * MsgOpaqueData encIv.
         * @member {Uint8Array|null|undefined} encIv
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.encIv = null;

        /**
         * MsgOpaqueData eventName.
         * @member {string|null|undefined} eventName
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.eventName = null;

        /**
         * MsgOpaqueData isEventCanceled.
         * @member {boolean|null|undefined} isEventCanceled
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.isEventCanceled = null;

        /**
         * MsgOpaqueData eventDescription.
         * @member {string|null|undefined} eventDescription
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.eventDescription = null;

        /**
         * MsgOpaqueData eventJoinLink.
         * @member {string|null|undefined} eventJoinLink
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.eventJoinLink = null;

        /**
         * MsgOpaqueData eventStartTime.
         * @member {number|Long|null|undefined} eventStartTime
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.eventStartTime = null;

        /**
         * MsgOpaqueData eventLocation.
         * @member {proto.MsgOpaqueData.IEventLocation|null|undefined} eventLocation
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.eventLocation = null;

        /**
         * MsgOpaqueData eventEndTime.
         * @member {number|Long|null|undefined} eventEndTime
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.eventEndTime = null;

        /**
         * MsgOpaqueData eventIsScheduledCall.
         * @member {boolean|null|undefined} eventIsScheduledCall
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.eventIsScheduledCall = null;

        /**
         * MsgOpaqueData eventExtraGuestsAllowed.
         * @member {boolean|null|undefined} eventExtraGuestsAllowed
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.eventExtraGuestsAllowed = null;

        /**
         * MsgOpaqueData plainProtobufBytes.
         * @member {Uint8Array|null|undefined} plainProtobufBytes
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.plainProtobufBytes = null;

        /**
         * MsgOpaqueData quarantineExtractedText.
         * @member {string|null|undefined} quarantineExtractedText
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.quarantineExtractedText = null;

        /**
         * MsgOpaqueData pollEndTime.
         * @member {number|Long|null|undefined} pollEndTime
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.pollEndTime = null;

        /**
         * MsgOpaqueData pollHideVoterNames.
         * @member {boolean|null|undefined} pollHideVoterNames
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.pollHideVoterNames = null;

        /**
         * MsgOpaqueData pollAllowAddOption.
         * @member {boolean|null|undefined} pollAllowAddOption
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.pollAllowAddOption = null;

        /**
         * MsgOpaqueData sharableEventInviteId.
         * @member {string|null|undefined} sharableEventInviteId
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.sharableEventInviteId = null;

        /**
         * MsgOpaqueData sharableEventInviteTitle.
         * @member {string|null|undefined} sharableEventInviteTitle
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.sharableEventInviteTitle = null;

        /**
         * MsgOpaqueData sharableEventInviteStartTime.
         * @member {number|Long|null|undefined} sharableEventInviteStartTime
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.sharableEventInviteStartTime = null;

        /**
         * MsgOpaqueData sharableEventInviteEndTime.
         * @member {number|Long|null|undefined} sharableEventInviteEndTime
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.sharableEventInviteEndTime = null;

        /**
         * MsgOpaqueData sharableEventInviteCaption.
         * @member {string|null|undefined} sharableEventInviteCaption
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.sharableEventInviteCaption = null;

        /**
         * MsgOpaqueData sharableEventInviteIsCanceled.
         * @member {boolean|null|undefined} sharableEventInviteIsCanceled
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.sharableEventInviteIsCanceled = null;

        /**
         * MsgOpaqueData sharableEventInviteJpegThumbnail.
         * @member {Uint8Array|null|undefined} sharableEventInviteJpegThumbnail
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.sharableEventInviteJpegThumbnail = null;

        /**
         * MsgOpaqueData sharableEventInviteCallLink.
         * @member {string|null|undefined} sharableEventInviteCallLink
         * @memberof proto.MsgOpaqueData
         * @instance
         */
        MsgOpaqueData.prototype.sharableEventInviteCallLink = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_body", {
            get: $util.oneOfGetter($oneOfFields = ["body"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_caption", {
            get: $util.oneOfGetter($oneOfFields = ["caption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_lng", {
            get: $util.oneOfGetter($oneOfFields = ["lng"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_isLive", {
            get: $util.oneOfGetter($oneOfFields = ["isLive"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_lat", {
            get: $util.oneOfGetter($oneOfFields = ["lat"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_paymentAmount1000", {
            get: $util.oneOfGetter($oneOfFields = ["paymentAmount1000"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_paymentNoteMsgBody", {
            get: $util.oneOfGetter($oneOfFields = ["paymentNoteMsgBody"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_matchedText", {
            get: $util.oneOfGetter($oneOfFields = ["matchedText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_title", {
            get: $util.oneOfGetter($oneOfFields = ["title"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_description", {
            get: $util.oneOfGetter($oneOfFields = ["description"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_futureproofBuffer", {
            get: $util.oneOfGetter($oneOfFields = ["futureproofBuffer"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_clientUrl", {
            get: $util.oneOfGetter($oneOfFields = ["clientUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_loc", {
            get: $util.oneOfGetter($oneOfFields = ["loc"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_pollName", {
            get: $util.oneOfGetter($oneOfFields = ["pollName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_pollSelectableOptionsCount", {
            get: $util.oneOfGetter($oneOfFields = ["pollSelectableOptionsCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_messageSecret", {
            get: $util.oneOfGetter($oneOfFields = ["messageSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_originalSelfAuthor", {
            get: $util.oneOfGetter($oneOfFields = ["originalSelfAuthor"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_senderTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["senderTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_pollUpdateParentKey", {
            get: $util.oneOfGetter($oneOfFields = ["pollUpdateParentKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_encPollVote", {
            get: $util.oneOfGetter($oneOfFields = ["encPollVote"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_isSentCagPollCreation", {
            get: $util.oneOfGetter($oneOfFields = ["isSentCagPollCreation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_pollContentType", {
            get: $util.oneOfGetter($oneOfFields = ["pollContentType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_pollType", {
            get: $util.oneOfGetter($oneOfFields = ["pollType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_correctOptionIndex", {
            get: $util.oneOfGetter($oneOfFields = ["correctOptionIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_pollVotesSnapshot", {
            get: $util.oneOfGetter($oneOfFields = ["pollVotesSnapshot"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_encReactionTargetMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["encReactionTargetMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_encReactionEncPayload", {
            get: $util.oneOfGetter($oneOfFields = ["encReactionEncPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_encReactionEncIv", {
            get: $util.oneOfGetter($oneOfFields = ["encReactionEncIv"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_botMessageSecret", {
            get: $util.oneOfGetter($oneOfFields = ["botMessageSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_targetMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["targetMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_encPayload", {
            get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_encIv", {
            get: $util.oneOfGetter($oneOfFields = ["encIv"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_eventName", {
            get: $util.oneOfGetter($oneOfFields = ["eventName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_isEventCanceled", {
            get: $util.oneOfGetter($oneOfFields = ["isEventCanceled"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_eventDescription", {
            get: $util.oneOfGetter($oneOfFields = ["eventDescription"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_eventJoinLink", {
            get: $util.oneOfGetter($oneOfFields = ["eventJoinLink"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_eventStartTime", {
            get: $util.oneOfGetter($oneOfFields = ["eventStartTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_eventLocation", {
            get: $util.oneOfGetter($oneOfFields = ["eventLocation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_eventEndTime", {
            get: $util.oneOfGetter($oneOfFields = ["eventEndTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_eventIsScheduledCall", {
            get: $util.oneOfGetter($oneOfFields = ["eventIsScheduledCall"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_eventExtraGuestsAllowed", {
            get: $util.oneOfGetter($oneOfFields = ["eventExtraGuestsAllowed"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_plainProtobufBytes", {
            get: $util.oneOfGetter($oneOfFields = ["plainProtobufBytes"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_quarantineExtractedText", {
            get: $util.oneOfGetter($oneOfFields = ["quarantineExtractedText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_pollEndTime", {
            get: $util.oneOfGetter($oneOfFields = ["pollEndTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_pollHideVoterNames", {
            get: $util.oneOfGetter($oneOfFields = ["pollHideVoterNames"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_pollAllowAddOption", {
            get: $util.oneOfGetter($oneOfFields = ["pollAllowAddOption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_sharableEventInviteId", {
            get: $util.oneOfGetter($oneOfFields = ["sharableEventInviteId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_sharableEventInviteTitle", {
            get: $util.oneOfGetter($oneOfFields = ["sharableEventInviteTitle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_sharableEventInviteStartTime", {
            get: $util.oneOfGetter($oneOfFields = ["sharableEventInviteStartTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_sharableEventInviteEndTime", {
            get: $util.oneOfGetter($oneOfFields = ["sharableEventInviteEndTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_sharableEventInviteCaption", {
            get: $util.oneOfGetter($oneOfFields = ["sharableEventInviteCaption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_sharableEventInviteIsCanceled", {
            get: $util.oneOfGetter($oneOfFields = ["sharableEventInviteIsCanceled"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_sharableEventInviteJpegThumbnail", {
            get: $util.oneOfGetter($oneOfFields = ["sharableEventInviteJpegThumbnail"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgOpaqueData.prototype, "_sharableEventInviteCallLink", {
            get: $util.oneOfGetter($oneOfFields = ["sharableEventInviteCallLink"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new MsgOpaqueData instance using the specified properties.
         * @function create
         * @memberof proto.MsgOpaqueData
         * @static
         * @param {proto.IMsgOpaqueData=} [properties] Properties to set
         * @returns {proto.MsgOpaqueData} MsgOpaqueData instance
         */
        MsgOpaqueData.create = function create(properties) {
            return new MsgOpaqueData(properties);
        };

        /**
         * Encodes the specified MsgOpaqueData message. Does not implicitly {@link proto.MsgOpaqueData.verify|verify} messages.
         * @function encode
         * @memberof proto.MsgOpaqueData
         * @static
         * @param {proto.IMsgOpaqueData} message MsgOpaqueData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MsgOpaqueData.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.body != null && Object.hasOwnProperty.call(message, "body"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.body);
            if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.caption);
            if (message.lng != null && Object.hasOwnProperty.call(message, "lng"))
                writer.uint32(/* id 5, wireType 1 =*/41).double(message.lng);
            if (message.isLive != null && Object.hasOwnProperty.call(message, "isLive"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.isLive);
            if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
                writer.uint32(/* id 7, wireType 1 =*/57).double(message.lat);
            if (message.paymentAmount1000 != null && Object.hasOwnProperty.call(message, "paymentAmount1000"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.paymentAmount1000);
            if (message.paymentNoteMsgBody != null && Object.hasOwnProperty.call(message, "paymentNoteMsgBody"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.paymentNoteMsgBody);
            if (message.matchedText != null && Object.hasOwnProperty.call(message, "matchedText"))
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.matchedText);
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                writer.uint32(/* id 12, wireType 2 =*/98).string(message.title);
            if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.description);
            if (message.futureproofBuffer != null && Object.hasOwnProperty.call(message, "futureproofBuffer"))
                writer.uint32(/* id 14, wireType 2 =*/114).bytes(message.futureproofBuffer);
            if (message.clientUrl != null && Object.hasOwnProperty.call(message, "clientUrl"))
                writer.uint32(/* id 15, wireType 2 =*/122).string(message.clientUrl);
            if (message.loc != null && Object.hasOwnProperty.call(message, "loc"))
                writer.uint32(/* id 16, wireType 2 =*/130).string(message.loc);
            if (message.pollName != null && Object.hasOwnProperty.call(message, "pollName"))
                writer.uint32(/* id 17, wireType 2 =*/138).string(message.pollName);
            if (message.pollOptions != null && message.pollOptions.length)
                for (var i = 0; i < message.pollOptions.length; ++i)
                    $root.proto.MsgOpaqueData.PollOption.encode(message.pollOptions[i], writer.uint32(/* id 18, wireType 2 =*/146).fork(), q + 1).ldelim();
            if (message.pollSelectableOptionsCount != null && Object.hasOwnProperty.call(message, "pollSelectableOptionsCount"))
                writer.uint32(/* id 20, wireType 0 =*/160).uint32(message.pollSelectableOptionsCount);
            if (message.messageSecret != null && Object.hasOwnProperty.call(message, "messageSecret"))
                writer.uint32(/* id 21, wireType 2 =*/170).bytes(message.messageSecret);
            if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs"))
                writer.uint32(/* id 22, wireType 0 =*/176).int64(message.senderTimestampMs);
            if (message.pollUpdateParentKey != null && Object.hasOwnProperty.call(message, "pollUpdateParentKey"))
                writer.uint32(/* id 23, wireType 2 =*/186).string(message.pollUpdateParentKey);
            if (message.encPollVote != null && Object.hasOwnProperty.call(message, "encPollVote"))
                $root.proto.PollEncValue.encode(message.encPollVote, writer.uint32(/* id 24, wireType 2 =*/194).fork(), q + 1).ldelim();
            if (message.encReactionTargetMessageKey != null && Object.hasOwnProperty.call(message, "encReactionTargetMessageKey"))
                writer.uint32(/* id 25, wireType 2 =*/202).string(message.encReactionTargetMessageKey);
            if (message.encReactionEncPayload != null && Object.hasOwnProperty.call(message, "encReactionEncPayload"))
                writer.uint32(/* id 26, wireType 2 =*/210).bytes(message.encReactionEncPayload);
            if (message.encReactionEncIv != null && Object.hasOwnProperty.call(message, "encReactionEncIv"))
                writer.uint32(/* id 27, wireType 2 =*/218).bytes(message.encReactionEncIv);
            if (message.isSentCagPollCreation != null && Object.hasOwnProperty.call(message, "isSentCagPollCreation"))
                writer.uint32(/* id 28, wireType 0 =*/224).bool(message.isSentCagPollCreation);
            if (message.botMessageSecret != null && Object.hasOwnProperty.call(message, "botMessageSecret"))
                writer.uint32(/* id 29, wireType 2 =*/234).bytes(message.botMessageSecret);
            if (message.targetMessageKey != null && Object.hasOwnProperty.call(message, "targetMessageKey"))
                writer.uint32(/* id 30, wireType 2 =*/242).string(message.targetMessageKey);
            if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload"))
                writer.uint32(/* id 31, wireType 2 =*/250).bytes(message.encPayload);
            if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv"))
                writer.uint32(/* id 32, wireType 2 =*/258).bytes(message.encIv);
            if (message.eventName != null && Object.hasOwnProperty.call(message, "eventName"))
                writer.uint32(/* id 33, wireType 2 =*/266).string(message.eventName);
            if (message.isEventCanceled != null && Object.hasOwnProperty.call(message, "isEventCanceled"))
                writer.uint32(/* id 34, wireType 0 =*/272).bool(message.isEventCanceled);
            if (message.eventDescription != null && Object.hasOwnProperty.call(message, "eventDescription"))
                writer.uint32(/* id 35, wireType 2 =*/282).string(message.eventDescription);
            if (message.eventJoinLink != null && Object.hasOwnProperty.call(message, "eventJoinLink"))
                writer.uint32(/* id 36, wireType 2 =*/290).string(message.eventJoinLink);
            if (message.eventStartTime != null && Object.hasOwnProperty.call(message, "eventStartTime"))
                writer.uint32(/* id 37, wireType 0 =*/296).int64(message.eventStartTime);
            if (message.eventLocation != null && Object.hasOwnProperty.call(message, "eventLocation"))
                $root.proto.MsgOpaqueData.EventLocation.encode(message.eventLocation, writer.uint32(/* id 38, wireType 2 =*/306).fork(), q + 1).ldelim();
            if (message.eventEndTime != null && Object.hasOwnProperty.call(message, "eventEndTime"))
                writer.uint32(/* id 40, wireType 0 =*/320).int64(message.eventEndTime);
            if (message.pollVotesSnapshot != null && Object.hasOwnProperty.call(message, "pollVotesSnapshot"))
                $root.proto.MsgOpaqueData.PollVotesSnapshot.encode(message.pollVotesSnapshot, writer.uint32(/* id 41, wireType 2 =*/330).fork(), q + 1).ldelim();
            if (message.pollContentType != null && Object.hasOwnProperty.call(message, "pollContentType"))
                writer.uint32(/* id 42, wireType 0 =*/336).int32(message.pollContentType);
            if (message.plainProtobufBytes != null && Object.hasOwnProperty.call(message, "plainProtobufBytes"))
                writer.uint32(/* id 43, wireType 2 =*/346).bytes(message.plainProtobufBytes);
            if (message.eventIsScheduledCall != null && Object.hasOwnProperty.call(message, "eventIsScheduledCall"))
                writer.uint32(/* id 44, wireType 0 =*/352).bool(message.eventIsScheduledCall);
            if (message.eventExtraGuestsAllowed != null && Object.hasOwnProperty.call(message, "eventExtraGuestsAllowed"))
                writer.uint32(/* id 45, wireType 0 =*/360).bool(message.eventExtraGuestsAllowed);
            if (message.pollType != null && Object.hasOwnProperty.call(message, "pollType"))
                writer.uint32(/* id 46, wireType 0 =*/368).int32(message.pollType);
            if (message.correctOptionIndex != null && Object.hasOwnProperty.call(message, "correctOptionIndex"))
                writer.uint32(/* id 47, wireType 0 =*/376).int32(message.correctOptionIndex);
            if (message.quarantineExtractedText != null && Object.hasOwnProperty.call(message, "quarantineExtractedText"))
                writer.uint32(/* id 48, wireType 2 =*/386).string(message.quarantineExtractedText);
            if (message.pollEndTime != null && Object.hasOwnProperty.call(message, "pollEndTime"))
                writer.uint32(/* id 49, wireType 0 =*/392).int64(message.pollEndTime);
            if (message.pollHideVoterNames != null && Object.hasOwnProperty.call(message, "pollHideVoterNames"))
                writer.uint32(/* id 50, wireType 0 =*/400).bool(message.pollHideVoterNames);
            if (message.originalSelfAuthor != null && Object.hasOwnProperty.call(message, "originalSelfAuthor"))
                writer.uint32(/* id 51, wireType 2 =*/410).string(message.originalSelfAuthor);
            if (message.pollAllowAddOption != null && Object.hasOwnProperty.call(message, "pollAllowAddOption"))
                writer.uint32(/* id 52, wireType 0 =*/416).bool(message.pollAllowAddOption);
            if (message.sharableEventInviteId != null && Object.hasOwnProperty.call(message, "sharableEventInviteId"))
                writer.uint32(/* id 53, wireType 2 =*/426).string(message.sharableEventInviteId);
            if (message.sharableEventInviteTitle != null && Object.hasOwnProperty.call(message, "sharableEventInviteTitle"))
                writer.uint32(/* id 54, wireType 2 =*/434).string(message.sharableEventInviteTitle);
            if (message.sharableEventInviteStartTime != null && Object.hasOwnProperty.call(message, "sharableEventInviteStartTime"))
                writer.uint32(/* id 55, wireType 0 =*/440).int64(message.sharableEventInviteStartTime);
            if (message.sharableEventInviteEndTime != null && Object.hasOwnProperty.call(message, "sharableEventInviteEndTime"))
                writer.uint32(/* id 56, wireType 0 =*/448).int64(message.sharableEventInviteEndTime);
            if (message.sharableEventInviteCaption != null && Object.hasOwnProperty.call(message, "sharableEventInviteCaption"))
                writer.uint32(/* id 57, wireType 2 =*/458).string(message.sharableEventInviteCaption);
            if (message.sharableEventInviteIsCanceled != null && Object.hasOwnProperty.call(message, "sharableEventInviteIsCanceled"))
                writer.uint32(/* id 58, wireType 0 =*/464).bool(message.sharableEventInviteIsCanceled);
            if (message.sharableEventInviteJpegThumbnail != null && Object.hasOwnProperty.call(message, "sharableEventInviteJpegThumbnail"))
                writer.uint32(/* id 59, wireType 2 =*/474).bytes(message.sharableEventInviteJpegThumbnail);
            if (message.sharableEventInviteCallLink != null && Object.hasOwnProperty.call(message, "sharableEventInviteCallLink"))
                writer.uint32(/* id 60, wireType 2 =*/482).string(message.sharableEventInviteCallLink);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MsgOpaqueData message, length delimited. Does not implicitly {@link proto.MsgOpaqueData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.MsgOpaqueData
         * @static
         * @param {proto.IMsgOpaqueData} message MsgOpaqueData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MsgOpaqueData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MsgOpaqueData message from the specified reader or buffer.
         * @function decode
         * @memberof proto.MsgOpaqueData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.MsgOpaqueData} MsgOpaqueData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MsgOpaqueData.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.body = reader.stringVerify();
                        message._body = "body";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.caption = reader.stringVerify();
                        message._caption = "caption";
                        continue;
                    }
                case 5: {
                        if (u !== 1)
                            break;
                        message.lng = reader.double();
                        message._lng = "lng";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        message.isLive = reader.bool();
                        message._isLive = "isLive";
                        continue;
                    }
                case 7: {
                        if (u !== 1)
                            break;
                        message.lat = reader.double();
                        message._lat = "lat";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        message.paymentAmount1000 = reader.int32();
                        message._paymentAmount1000 = "paymentAmount1000";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        message.paymentNoteMsgBody = reader.stringVerify();
                        message._paymentNoteMsgBody = "paymentNoteMsgBody";
                        continue;
                    }
                case 11: {
                        if (u !== 2)
                            break;
                        message.matchedText = reader.stringVerify();
                        message._matchedText = "matchedText";
                        continue;
                    }
                case 12: {
                        if (u !== 2)
                            break;
                        message.title = reader.stringVerify();
                        message._title = "title";
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        message.description = reader.stringVerify();
                        message._description = "description";
                        continue;
                    }
                case 14: {
                        if (u !== 2)
                            break;
                        message.futureproofBuffer = reader.bytes();
                        message._futureproofBuffer = "futureproofBuffer";
                        continue;
                    }
                case 15: {
                        if (u !== 2)
                            break;
                        message.clientUrl = reader.stringVerify();
                        message._clientUrl = "clientUrl";
                        continue;
                    }
                case 16: {
                        if (u !== 2)
                            break;
                        message.loc = reader.stringVerify();
                        message._loc = "loc";
                        continue;
                    }
                case 17: {
                        if (u !== 2)
                            break;
                        message.pollName = reader.stringVerify();
                        message._pollName = "pollName";
                        continue;
                    }
                case 18: {
                        if (u !== 2)
                            break;
                        if (!(message.pollOptions && message.pollOptions.length))
                            message.pollOptions = [];
                        message.pollOptions.push($root.proto.MsgOpaqueData.PollOption.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 20: {
                        if (u !== 0)
                            break;
                        message.pollSelectableOptionsCount = reader.uint32();
                        message._pollSelectableOptionsCount = "pollSelectableOptionsCount";
                        continue;
                    }
                case 21: {
                        if (u !== 2)
                            break;
                        message.messageSecret = reader.bytes();
                        message._messageSecret = "messageSecret";
                        continue;
                    }
                case 51: {
                        if (u !== 2)
                            break;
                        message.originalSelfAuthor = reader.stringVerify();
                        message._originalSelfAuthor = "originalSelfAuthor";
                        continue;
                    }
                case 22: {
                        if (u !== 0)
                            break;
                        message.senderTimestampMs = reader.int64();
                        message._senderTimestampMs = "senderTimestampMs";
                        continue;
                    }
                case 23: {
                        if (u !== 2)
                            break;
                        message.pollUpdateParentKey = reader.stringVerify();
                        message._pollUpdateParentKey = "pollUpdateParentKey";
                        continue;
                    }
                case 24: {
                        if (u !== 2)
                            break;
                        message.encPollVote = $root.proto.PollEncValue.decode(reader, reader.uint32(), undefined, q + 1, message.encPollVote);
                        message._encPollVote = "encPollVote";
                        continue;
                    }
                case 28: {
                        if (u !== 0)
                            break;
                        message.isSentCagPollCreation = reader.bool();
                        message._isSentCagPollCreation = "isSentCagPollCreation";
                        continue;
                    }
                case 42: {
                        if (u !== 0)
                            break;
                        message.pollContentType = reader.int32();
                        message._pollContentType = "pollContentType";
                        continue;
                    }
                case 46: {
                        if (u !== 0)
                            break;
                        message.pollType = reader.int32();
                        message._pollType = "pollType";
                        continue;
                    }
                case 47: {
                        if (u !== 0)
                            break;
                        message.correctOptionIndex = reader.int32();
                        message._correctOptionIndex = "correctOptionIndex";
                        continue;
                    }
                case 41: {
                        if (u !== 2)
                            break;
                        message.pollVotesSnapshot = $root.proto.MsgOpaqueData.PollVotesSnapshot.decode(reader, reader.uint32(), undefined, q + 1, message.pollVotesSnapshot);
                        message._pollVotesSnapshot = "pollVotesSnapshot";
                        continue;
                    }
                case 25: {
                        if (u !== 2)
                            break;
                        message.encReactionTargetMessageKey = reader.stringVerify();
                        message._encReactionTargetMessageKey = "encReactionTargetMessageKey";
                        continue;
                    }
                case 26: {
                        if (u !== 2)
                            break;
                        message.encReactionEncPayload = reader.bytes();
                        message._encReactionEncPayload = "encReactionEncPayload";
                        continue;
                    }
                case 27: {
                        if (u !== 2)
                            break;
                        message.encReactionEncIv = reader.bytes();
                        message._encReactionEncIv = "encReactionEncIv";
                        continue;
                    }
                case 29: {
                        if (u !== 2)
                            break;
                        message.botMessageSecret = reader.bytes();
                        message._botMessageSecret = "botMessageSecret";
                        continue;
                    }
                case 30: {
                        if (u !== 2)
                            break;
                        message.targetMessageKey = reader.stringVerify();
                        message._targetMessageKey = "targetMessageKey";
                        continue;
                    }
                case 31: {
                        if (u !== 2)
                            break;
                        message.encPayload = reader.bytes();
                        message._encPayload = "encPayload";
                        continue;
                    }
                case 32: {
                        if (u !== 2)
                            break;
                        message.encIv = reader.bytes();
                        message._encIv = "encIv";
                        continue;
                    }
                case 33: {
                        if (u !== 2)
                            break;
                        message.eventName = reader.stringVerify();
                        message._eventName = "eventName";
                        continue;
                    }
                case 34: {
                        if (u !== 0)
                            break;
                        message.isEventCanceled = reader.bool();
                        message._isEventCanceled = "isEventCanceled";
                        continue;
                    }
                case 35: {
                        if (u !== 2)
                            break;
                        message.eventDescription = reader.stringVerify();
                        message._eventDescription = "eventDescription";
                        continue;
                    }
                case 36: {
                        if (u !== 2)
                            break;
                        message.eventJoinLink = reader.stringVerify();
                        message._eventJoinLink = "eventJoinLink";
                        continue;
                    }
                case 37: {
                        if (u !== 0)
                            break;
                        message.eventStartTime = reader.int64();
                        message._eventStartTime = "eventStartTime";
                        continue;
                    }
                case 38: {
                        if (u !== 2)
                            break;
                        message.eventLocation = $root.proto.MsgOpaqueData.EventLocation.decode(reader, reader.uint32(), undefined, q + 1, message.eventLocation);
                        message._eventLocation = "eventLocation";
                        continue;
                    }
                case 40: {
                        if (u !== 0)
                            break;
                        message.eventEndTime = reader.int64();
                        message._eventEndTime = "eventEndTime";
                        continue;
                    }
                case 44: {
                        if (u !== 0)
                            break;
                        message.eventIsScheduledCall = reader.bool();
                        message._eventIsScheduledCall = "eventIsScheduledCall";
                        continue;
                    }
                case 45: {
                        if (u !== 0)
                            break;
                        message.eventExtraGuestsAllowed = reader.bool();
                        message._eventExtraGuestsAllowed = "eventExtraGuestsAllowed";
                        continue;
                    }
                case 43: {
                        if (u !== 2)
                            break;
                        message.plainProtobufBytes = reader.bytes();
                        message._plainProtobufBytes = "plainProtobufBytes";
                        continue;
                    }
                case 48: {
                        if (u !== 2)
                            break;
                        message.quarantineExtractedText = reader.stringVerify();
                        message._quarantineExtractedText = "quarantineExtractedText";
                        continue;
                    }
                case 49: {
                        if (u !== 0)
                            break;
                        message.pollEndTime = reader.int64();
                        message._pollEndTime = "pollEndTime";
                        continue;
                    }
                case 50: {
                        if (u !== 0)
                            break;
                        message.pollHideVoterNames = reader.bool();
                        message._pollHideVoterNames = "pollHideVoterNames";
                        continue;
                    }
                case 52: {
                        if (u !== 0)
                            break;
                        message.pollAllowAddOption = reader.bool();
                        message._pollAllowAddOption = "pollAllowAddOption";
                        continue;
                    }
                case 53: {
                        if (u !== 2)
                            break;
                        message.sharableEventInviteId = reader.stringVerify();
                        message._sharableEventInviteId = "sharableEventInviteId";
                        continue;
                    }
                case 54: {
                        if (u !== 2)
                            break;
                        message.sharableEventInviteTitle = reader.stringVerify();
                        message._sharableEventInviteTitle = "sharableEventInviteTitle";
                        continue;
                    }
                case 55: {
                        if (u !== 0)
                            break;
                        message.sharableEventInviteStartTime = reader.int64();
                        message._sharableEventInviteStartTime = "sharableEventInviteStartTime";
                        continue;
                    }
                case 56: {
                        if (u !== 0)
                            break;
                        message.sharableEventInviteEndTime = reader.int64();
                        message._sharableEventInviteEndTime = "sharableEventInviteEndTime";
                        continue;
                    }
                case 57: {
                        if (u !== 2)
                            break;
                        message.sharableEventInviteCaption = reader.stringVerify();
                        message._sharableEventInviteCaption = "sharableEventInviteCaption";
                        continue;
                    }
                case 58: {
                        if (u !== 0)
                            break;
                        message.sharableEventInviteIsCanceled = reader.bool();
                        message._sharableEventInviteIsCanceled = "sharableEventInviteIsCanceled";
                        continue;
                    }
                case 59: {
                        if (u !== 2)
                            break;
                        message.sharableEventInviteJpegThumbnail = reader.bytes();
                        message._sharableEventInviteJpegThumbnail = "sharableEventInviteJpegThumbnail";
                        continue;
                    }
                case 60: {
                        if (u !== 2)
                            break;
                        message.sharableEventInviteCallLink = reader.stringVerify();
                        message._sharableEventInviteCallLink = "sharableEventInviteCallLink";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a MsgOpaqueData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.MsgOpaqueData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.MsgOpaqueData} MsgOpaqueData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MsgOpaqueData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MsgOpaqueData message.
         * @function verify
         * @memberof proto.MsgOpaqueData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MsgOpaqueData.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.body != null && Object.hasOwnProperty.call(message, "body")) {
                properties._body = 1;
                if (!$util.isString(message.body))
                    return "body: string expected";
            }
            if (message.caption != null && Object.hasOwnProperty.call(message, "caption")) {
                properties._caption = 1;
                if (!$util.isString(message.caption))
                    return "caption: string expected";
            }
            if (message.lng != null && Object.hasOwnProperty.call(message, "lng")) {
                properties._lng = 1;
                if (typeof message.lng !== "number")
                    return "lng: number expected";
            }
            if (message.isLive != null && Object.hasOwnProperty.call(message, "isLive")) {
                properties._isLive = 1;
                if (typeof message.isLive !== "boolean")
                    return "isLive: boolean expected";
            }
            if (message.lat != null && Object.hasOwnProperty.call(message, "lat")) {
                properties._lat = 1;
                if (typeof message.lat !== "number")
                    return "lat: number expected";
            }
            if (message.paymentAmount1000 != null && Object.hasOwnProperty.call(message, "paymentAmount1000")) {
                properties._paymentAmount1000 = 1;
                if (!$util.isInteger(message.paymentAmount1000))
                    return "paymentAmount1000: integer expected";
            }
            if (message.paymentNoteMsgBody != null && Object.hasOwnProperty.call(message, "paymentNoteMsgBody")) {
                properties._paymentNoteMsgBody = 1;
                if (!$util.isString(message.paymentNoteMsgBody))
                    return "paymentNoteMsgBody: string expected";
            }
            if (message.matchedText != null && Object.hasOwnProperty.call(message, "matchedText")) {
                properties._matchedText = 1;
                if (!$util.isString(message.matchedText))
                    return "matchedText: string expected";
            }
            if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                properties._title = 1;
                if (!$util.isString(message.title))
                    return "title: string expected";
            }
            if (message.description != null && Object.hasOwnProperty.call(message, "description")) {
                properties._description = 1;
                if (!$util.isString(message.description))
                    return "description: string expected";
            }
            if (message.futureproofBuffer != null && Object.hasOwnProperty.call(message, "futureproofBuffer")) {
                properties._futureproofBuffer = 1;
                if (!(message.futureproofBuffer && typeof message.futureproofBuffer.length === "number" || $util.isString(message.futureproofBuffer)))
                    return "futureproofBuffer: buffer expected";
            }
            if (message.clientUrl != null && Object.hasOwnProperty.call(message, "clientUrl")) {
                properties._clientUrl = 1;
                if (!$util.isString(message.clientUrl))
                    return "clientUrl: string expected";
            }
            if (message.loc != null && Object.hasOwnProperty.call(message, "loc")) {
                properties._loc = 1;
                if (!$util.isString(message.loc))
                    return "loc: string expected";
            }
            if (message.pollName != null && Object.hasOwnProperty.call(message, "pollName")) {
                properties._pollName = 1;
                if (!$util.isString(message.pollName))
                    return "pollName: string expected";
            }
            if (message.pollOptions != null && Object.hasOwnProperty.call(message, "pollOptions")) {
                if (!Array.isArray(message.pollOptions))
                    return "pollOptions: array expected";
                for (var i = 0; i < message.pollOptions.length; ++i) {
                    var error = $root.proto.MsgOpaqueData.PollOption.verify(message.pollOptions[i], q + 1);
                    if (error)
                        return "pollOptions." + error;
                }
            }
            if (message.pollSelectableOptionsCount != null && Object.hasOwnProperty.call(message, "pollSelectableOptionsCount")) {
                properties._pollSelectableOptionsCount = 1;
                if (!$util.isInteger(message.pollSelectableOptionsCount))
                    return "pollSelectableOptionsCount: integer expected";
            }
            if (message.messageSecret != null && Object.hasOwnProperty.call(message, "messageSecret")) {
                properties._messageSecret = 1;
                if (!(message.messageSecret && typeof message.messageSecret.length === "number" || $util.isString(message.messageSecret)))
                    return "messageSecret: buffer expected";
            }
            if (message.originalSelfAuthor != null && Object.hasOwnProperty.call(message, "originalSelfAuthor")) {
                properties._originalSelfAuthor = 1;
                if (!$util.isString(message.originalSelfAuthor))
                    return "originalSelfAuthor: string expected";
            }
            if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs")) {
                properties._senderTimestampMs = 1;
                if (!$util.isInteger(message.senderTimestampMs) && !(message.senderTimestampMs && $util.isInteger(message.senderTimestampMs.low) && $util.isInteger(message.senderTimestampMs.high)))
                    return "senderTimestampMs: integer|Long expected";
            }
            if (message.pollUpdateParentKey != null && Object.hasOwnProperty.call(message, "pollUpdateParentKey")) {
                properties._pollUpdateParentKey = 1;
                if (!$util.isString(message.pollUpdateParentKey))
                    return "pollUpdateParentKey: string expected";
            }
            if (message.encPollVote != null && Object.hasOwnProperty.call(message, "encPollVote")) {
                properties._encPollVote = 1;
                {
                    var error = $root.proto.PollEncValue.verify(message.encPollVote, q + 1);
                    if (error)
                        return "encPollVote." + error;
                }
            }
            if (message.isSentCagPollCreation != null && Object.hasOwnProperty.call(message, "isSentCagPollCreation")) {
                properties._isSentCagPollCreation = 1;
                if (typeof message.isSentCagPollCreation !== "boolean")
                    return "isSentCagPollCreation: boolean expected";
            }
            if (message.pollContentType != null && Object.hasOwnProperty.call(message, "pollContentType")) {
                properties._pollContentType = 1;
                if (typeof message.pollContentType !== "number" || (message.pollContentType | 0) !== message.pollContentType)
                    return "pollContentType: enum value expected";
            }
            if (message.pollType != null && Object.hasOwnProperty.call(message, "pollType")) {
                properties._pollType = 1;
                if (typeof message.pollType !== "number" || (message.pollType | 0) !== message.pollType)
                    return "pollType: enum value expected";
            }
            if (message.correctOptionIndex != null && Object.hasOwnProperty.call(message, "correctOptionIndex")) {
                properties._correctOptionIndex = 1;
                if (!$util.isInteger(message.correctOptionIndex))
                    return "correctOptionIndex: integer expected";
            }
            if (message.pollVotesSnapshot != null && Object.hasOwnProperty.call(message, "pollVotesSnapshot")) {
                properties._pollVotesSnapshot = 1;
                {
                    var error = $root.proto.MsgOpaqueData.PollVotesSnapshot.verify(message.pollVotesSnapshot, q + 1);
                    if (error)
                        return "pollVotesSnapshot." + error;
                }
            }
            if (message.encReactionTargetMessageKey != null && Object.hasOwnProperty.call(message, "encReactionTargetMessageKey")) {
                properties._encReactionTargetMessageKey = 1;
                if (!$util.isString(message.encReactionTargetMessageKey))
                    return "encReactionTargetMessageKey: string expected";
            }
            if (message.encReactionEncPayload != null && Object.hasOwnProperty.call(message, "encReactionEncPayload")) {
                properties._encReactionEncPayload = 1;
                if (!(message.encReactionEncPayload && typeof message.encReactionEncPayload.length === "number" || $util.isString(message.encReactionEncPayload)))
                    return "encReactionEncPayload: buffer expected";
            }
            if (message.encReactionEncIv != null && Object.hasOwnProperty.call(message, "encReactionEncIv")) {
                properties._encReactionEncIv = 1;
                if (!(message.encReactionEncIv && typeof message.encReactionEncIv.length === "number" || $util.isString(message.encReactionEncIv)))
                    return "encReactionEncIv: buffer expected";
            }
            if (message.botMessageSecret != null && Object.hasOwnProperty.call(message, "botMessageSecret")) {
                properties._botMessageSecret = 1;
                if (!(message.botMessageSecret && typeof message.botMessageSecret.length === "number" || $util.isString(message.botMessageSecret)))
                    return "botMessageSecret: buffer expected";
            }
            if (message.targetMessageKey != null && Object.hasOwnProperty.call(message, "targetMessageKey")) {
                properties._targetMessageKey = 1;
                if (!$util.isString(message.targetMessageKey))
                    return "targetMessageKey: string expected";
            }
            if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload")) {
                properties._encPayload = 1;
                if (!(message.encPayload && typeof message.encPayload.length === "number" || $util.isString(message.encPayload)))
                    return "encPayload: buffer expected";
            }
            if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv")) {
                properties._encIv = 1;
                if (!(message.encIv && typeof message.encIv.length === "number" || $util.isString(message.encIv)))
                    return "encIv: buffer expected";
            }
            if (message.eventName != null && Object.hasOwnProperty.call(message, "eventName")) {
                properties._eventName = 1;
                if (!$util.isString(message.eventName))
                    return "eventName: string expected";
            }
            if (message.isEventCanceled != null && Object.hasOwnProperty.call(message, "isEventCanceled")) {
                properties._isEventCanceled = 1;
                if (typeof message.isEventCanceled !== "boolean")
                    return "isEventCanceled: boolean expected";
            }
            if (message.eventDescription != null && Object.hasOwnProperty.call(message, "eventDescription")) {
                properties._eventDescription = 1;
                if (!$util.isString(message.eventDescription))
                    return "eventDescription: string expected";
            }
            if (message.eventJoinLink != null && Object.hasOwnProperty.call(message, "eventJoinLink")) {
                properties._eventJoinLink = 1;
                if (!$util.isString(message.eventJoinLink))
                    return "eventJoinLink: string expected";
            }
            if (message.eventStartTime != null && Object.hasOwnProperty.call(message, "eventStartTime")) {
                properties._eventStartTime = 1;
                if (!$util.isInteger(message.eventStartTime) && !(message.eventStartTime && $util.isInteger(message.eventStartTime.low) && $util.isInteger(message.eventStartTime.high)))
                    return "eventStartTime: integer|Long expected";
            }
            if (message.eventLocation != null && Object.hasOwnProperty.call(message, "eventLocation")) {
                properties._eventLocation = 1;
                {
                    var error = $root.proto.MsgOpaqueData.EventLocation.verify(message.eventLocation, q + 1);
                    if (error)
                        return "eventLocation." + error;
                }
            }
            if (message.eventEndTime != null && Object.hasOwnProperty.call(message, "eventEndTime")) {
                properties._eventEndTime = 1;
                if (!$util.isInteger(message.eventEndTime) && !(message.eventEndTime && $util.isInteger(message.eventEndTime.low) && $util.isInteger(message.eventEndTime.high)))
                    return "eventEndTime: integer|Long expected";
            }
            if (message.eventIsScheduledCall != null && Object.hasOwnProperty.call(message, "eventIsScheduledCall")) {
                properties._eventIsScheduledCall = 1;
                if (typeof message.eventIsScheduledCall !== "boolean")
                    return "eventIsScheduledCall: boolean expected";
            }
            if (message.eventExtraGuestsAllowed != null && Object.hasOwnProperty.call(message, "eventExtraGuestsAllowed")) {
                properties._eventExtraGuestsAllowed = 1;
                if (typeof message.eventExtraGuestsAllowed !== "boolean")
                    return "eventExtraGuestsAllowed: boolean expected";
            }
            if (message.plainProtobufBytes != null && Object.hasOwnProperty.call(message, "plainProtobufBytes")) {
                properties._plainProtobufBytes = 1;
                if (!(message.plainProtobufBytes && typeof message.plainProtobufBytes.length === "number" || $util.isString(message.plainProtobufBytes)))
                    return "plainProtobufBytes: buffer expected";
            }
            if (message.quarantineExtractedText != null && Object.hasOwnProperty.call(message, "quarantineExtractedText")) {
                properties._quarantineExtractedText = 1;
                if (!$util.isString(message.quarantineExtractedText))
                    return "quarantineExtractedText: string expected";
            }
            if (message.pollEndTime != null && Object.hasOwnProperty.call(message, "pollEndTime")) {
                properties._pollEndTime = 1;
                if (!$util.isInteger(message.pollEndTime) && !(message.pollEndTime && $util.isInteger(message.pollEndTime.low) && $util.isInteger(message.pollEndTime.high)))
                    return "pollEndTime: integer|Long expected";
            }
            if (message.pollHideVoterNames != null && Object.hasOwnProperty.call(message, "pollHideVoterNames")) {
                properties._pollHideVoterNames = 1;
                if (typeof message.pollHideVoterNames !== "boolean")
                    return "pollHideVoterNames: boolean expected";
            }
            if (message.pollAllowAddOption != null && Object.hasOwnProperty.call(message, "pollAllowAddOption")) {
                properties._pollAllowAddOption = 1;
                if (typeof message.pollAllowAddOption !== "boolean")
                    return "pollAllowAddOption: boolean expected";
            }
            if (message.sharableEventInviteId != null && Object.hasOwnProperty.call(message, "sharableEventInviteId")) {
                properties._sharableEventInviteId = 1;
                if (!$util.isString(message.sharableEventInviteId))
                    return "sharableEventInviteId: string expected";
            }
            if (message.sharableEventInviteTitle != null && Object.hasOwnProperty.call(message, "sharableEventInviteTitle")) {
                properties._sharableEventInviteTitle = 1;
                if (!$util.isString(message.sharableEventInviteTitle))
                    return "sharableEventInviteTitle: string expected";
            }
            if (message.sharableEventInviteStartTime != null && Object.hasOwnProperty.call(message, "sharableEventInviteStartTime")) {
                properties._sharableEventInviteStartTime = 1;
                if (!$util.isInteger(message.sharableEventInviteStartTime) && !(message.sharableEventInviteStartTime && $util.isInteger(message.sharableEventInviteStartTime.low) && $util.isInteger(message.sharableEventInviteStartTime.high)))
                    return "sharableEventInviteStartTime: integer|Long expected";
            }
            if (message.sharableEventInviteEndTime != null && Object.hasOwnProperty.call(message, "sharableEventInviteEndTime")) {
                properties._sharableEventInviteEndTime = 1;
                if (!$util.isInteger(message.sharableEventInviteEndTime) && !(message.sharableEventInviteEndTime && $util.isInteger(message.sharableEventInviteEndTime.low) && $util.isInteger(message.sharableEventInviteEndTime.high)))
                    return "sharableEventInviteEndTime: integer|Long expected";
            }
            if (message.sharableEventInviteCaption != null && Object.hasOwnProperty.call(message, "sharableEventInviteCaption")) {
                properties._sharableEventInviteCaption = 1;
                if (!$util.isString(message.sharableEventInviteCaption))
                    return "sharableEventInviteCaption: string expected";
            }
            if (message.sharableEventInviteIsCanceled != null && Object.hasOwnProperty.call(message, "sharableEventInviteIsCanceled")) {
                properties._sharableEventInviteIsCanceled = 1;
                if (typeof message.sharableEventInviteIsCanceled !== "boolean")
                    return "sharableEventInviteIsCanceled: boolean expected";
            }
            if (message.sharableEventInviteJpegThumbnail != null && Object.hasOwnProperty.call(message, "sharableEventInviteJpegThumbnail")) {
                properties._sharableEventInviteJpegThumbnail = 1;
                if (!(message.sharableEventInviteJpegThumbnail && typeof message.sharableEventInviteJpegThumbnail.length === "number" || $util.isString(message.sharableEventInviteJpegThumbnail)))
                    return "sharableEventInviteJpegThumbnail: buffer expected";
            }
            if (message.sharableEventInviteCallLink != null && Object.hasOwnProperty.call(message, "sharableEventInviteCallLink")) {
                properties._sharableEventInviteCallLink = 1;
                if (!$util.isString(message.sharableEventInviteCallLink))
                    return "sharableEventInviteCallLink: string expected";
            }
            return null;
        };

        /**
         * Creates a MsgOpaqueData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.MsgOpaqueData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.MsgOpaqueData} MsgOpaqueData
         */
        MsgOpaqueData.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.MsgOpaqueData: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.body != null)
                message.body = String(object.body);
            if (object.caption != null)
                message.caption = String(object.caption);
            if (object.lng != null)
                message.lng = Number(object.lng);
            if (object.isLive != null)
                message.isLive = Boolean(object.isLive);
            if (object.lat != null)
                message.lat = Number(object.lat);
            if (object.paymentAmount1000 != null)
                message.paymentAmount1000 = object.paymentAmount1000 | 0;
            if (object.paymentNoteMsgBody != null)
                message.paymentNoteMsgBody = String(object.paymentNoteMsgBody);
            if (object.matchedText != null)
                message.matchedText = String(object.matchedText);
            if (object.title != null)
                message.title = String(object.title);
            if (object.description != null)
                message.description = String(object.description);
            if (object.futureproofBuffer != null)
                if (typeof object.futureproofBuffer === "string")
                    $util.base64.decode(object.futureproofBuffer, message.futureproofBuffer = $util.newBuffer($util.base64.length(object.futureproofBuffer)), 0);
                else if (object.futureproofBuffer.length >= 0)
                    message.futureproofBuffer = object.futureproofBuffer;
            if (object.clientUrl != null)
                message.clientUrl = String(object.clientUrl);
            if (object.loc != null)
                message.loc = String(object.loc);
            if (object.pollName != null)
                message.pollName = String(object.pollName);
            if (object.pollOptions) {
                if (!Array.isArray(object.pollOptions))
                    throw TypeError(".proto.MsgOpaqueData.pollOptions: array expected");
                message.pollOptions = Array(object.pollOptions.length);
                for (var i = 0; i < object.pollOptions.length; ++i) {
                    if (!$util.isObject(object.pollOptions[i]))
                        throw TypeError(".proto.MsgOpaqueData.pollOptions: object expected");
                    message.pollOptions[i] = $root.proto.MsgOpaqueData.PollOption.fromObject(object.pollOptions[i], q + 1);
                }
            }
            if (object.pollSelectableOptionsCount != null)
                message.pollSelectableOptionsCount = object.pollSelectableOptionsCount >>> 0;
            if (object.messageSecret != null)
                if (typeof object.messageSecret === "string")
                    $util.base64.decode(object.messageSecret, message.messageSecret = $util.newBuffer($util.base64.length(object.messageSecret)), 0);
                else if (object.messageSecret.length >= 0)
                    message.messageSecret = object.messageSecret;
            if (object.originalSelfAuthor != null)
                message.originalSelfAuthor = String(object.originalSelfAuthor);
            if (object.senderTimestampMs != null)
                if ($util.Long)
                    message.senderTimestampMs = $util.Long.fromValue(object.senderTimestampMs, false);
                else if (typeof object.senderTimestampMs === "string")
                    message.senderTimestampMs = parseInt(object.senderTimestampMs, 10);
                else if (typeof object.senderTimestampMs === "number")
                    message.senderTimestampMs = object.senderTimestampMs;
                else if (typeof object.senderTimestampMs === "object")
                    message.senderTimestampMs = new $util.LongBits(object.senderTimestampMs.low >>> 0, object.senderTimestampMs.high >>> 0).toNumber();
            if (object.pollUpdateParentKey != null)
                message.pollUpdateParentKey = String(object.pollUpdateParentKey);
            if (object.encPollVote != null) {
                if (!$util.isObject(object.encPollVote))
                    throw TypeError(".proto.MsgOpaqueData.encPollVote: object expected");
                message.encPollVote = $root.proto.PollEncValue.fromObject(object.encPollVote, q + 1);
            }
            if (object.isSentCagPollCreation != null)
                message.isSentCagPollCreation = Boolean(object.isSentCagPollCreation);
            switch (object.pollContentType) {
            case "UNKNOWN":
            case 0:
                message.pollContentType = 0;
                break;
            case "TEXT":
            case 1:
                message.pollContentType = 1;
                break;
            case "IMAGE":
            case 2:
                message.pollContentType = 2;
                break;
            default:
                if (typeof object.pollContentType === "number" && (object.pollContentType | 0) === object.pollContentType)
                    message.pollContentType = object.pollContentType;
            }
            switch (object.pollType) {
            case "POLL":
            case 0:
                message.pollType = 0;
                break;
            case "QUIZ":
            case 1:
                message.pollType = 1;
                break;
            default:
                if (typeof object.pollType === "number" && (object.pollType | 0) === object.pollType)
                    message.pollType = object.pollType;
            }
            if (object.correctOptionIndex != null)
                message.correctOptionIndex = object.correctOptionIndex | 0;
            if (object.pollVotesSnapshot != null) {
                if (!$util.isObject(object.pollVotesSnapshot))
                    throw TypeError(".proto.MsgOpaqueData.pollVotesSnapshot: object expected");
                message.pollVotesSnapshot = $root.proto.MsgOpaqueData.PollVotesSnapshot.fromObject(object.pollVotesSnapshot, q + 1);
            }
            if (object.encReactionTargetMessageKey != null)
                message.encReactionTargetMessageKey = String(object.encReactionTargetMessageKey);
            if (object.encReactionEncPayload != null)
                if (typeof object.encReactionEncPayload === "string")
                    $util.base64.decode(object.encReactionEncPayload, message.encReactionEncPayload = $util.newBuffer($util.base64.length(object.encReactionEncPayload)), 0);
                else if (object.encReactionEncPayload.length >= 0)
                    message.encReactionEncPayload = object.encReactionEncPayload;
            if (object.encReactionEncIv != null)
                if (typeof object.encReactionEncIv === "string")
                    $util.base64.decode(object.encReactionEncIv, message.encReactionEncIv = $util.newBuffer($util.base64.length(object.encReactionEncIv)), 0);
                else if (object.encReactionEncIv.length >= 0)
                    message.encReactionEncIv = object.encReactionEncIv;
            if (object.botMessageSecret != null)
                if (typeof object.botMessageSecret === "string")
                    $util.base64.decode(object.botMessageSecret, message.botMessageSecret = $util.newBuffer($util.base64.length(object.botMessageSecret)), 0);
                else if (object.botMessageSecret.length >= 0)
                    message.botMessageSecret = object.botMessageSecret;
            if (object.targetMessageKey != null)
                message.targetMessageKey = String(object.targetMessageKey);
            if (object.encPayload != null)
                if (typeof object.encPayload === "string")
                    $util.base64.decode(object.encPayload, message.encPayload = $util.newBuffer($util.base64.length(object.encPayload)), 0);
                else if (object.encPayload.length >= 0)
                    message.encPayload = object.encPayload;
            if (object.encIv != null)
                if (typeof object.encIv === "string")
                    $util.base64.decode(object.encIv, message.encIv = $util.newBuffer($util.base64.length(object.encIv)), 0);
                else if (object.encIv.length >= 0)
                    message.encIv = object.encIv;
            if (object.eventName != null)
                message.eventName = String(object.eventName);
            if (object.isEventCanceled != null)
                message.isEventCanceled = Boolean(object.isEventCanceled);
            if (object.eventDescription != null)
                message.eventDescription = String(object.eventDescription);
            if (object.eventJoinLink != null)
                message.eventJoinLink = String(object.eventJoinLink);
            if (object.eventStartTime != null)
                if ($util.Long)
                    message.eventStartTime = $util.Long.fromValue(object.eventStartTime, false);
                else if (typeof object.eventStartTime === "string")
                    message.eventStartTime = parseInt(object.eventStartTime, 10);
                else if (typeof object.eventStartTime === "number")
                    message.eventStartTime = object.eventStartTime;
                else if (typeof object.eventStartTime === "object")
                    message.eventStartTime = new $util.LongBits(object.eventStartTime.low >>> 0, object.eventStartTime.high >>> 0).toNumber();
            if (object.eventLocation != null) {
                if (!$util.isObject(object.eventLocation))
                    throw TypeError(".proto.MsgOpaqueData.eventLocation: object expected");
                message.eventLocation = $root.proto.MsgOpaqueData.EventLocation.fromObject(object.eventLocation, q + 1);
            }
            if (object.eventEndTime != null)
                if ($util.Long)
                    message.eventEndTime = $util.Long.fromValue(object.eventEndTime, false);
                else if (typeof object.eventEndTime === "string")
                    message.eventEndTime = parseInt(object.eventEndTime, 10);
                else if (typeof object.eventEndTime === "number")
                    message.eventEndTime = object.eventEndTime;
                else if (typeof object.eventEndTime === "object")
                    message.eventEndTime = new $util.LongBits(object.eventEndTime.low >>> 0, object.eventEndTime.high >>> 0).toNumber();
            if (object.eventIsScheduledCall != null)
                message.eventIsScheduledCall = Boolean(object.eventIsScheduledCall);
            if (object.eventExtraGuestsAllowed != null)
                message.eventExtraGuestsAllowed = Boolean(object.eventExtraGuestsAllowed);
            if (object.plainProtobufBytes != null)
                if (typeof object.plainProtobufBytes === "string")
                    $util.base64.decode(object.plainProtobufBytes, message.plainProtobufBytes = $util.newBuffer($util.base64.length(object.plainProtobufBytes)), 0);
                else if (object.plainProtobufBytes.length >= 0)
                    message.plainProtobufBytes = object.plainProtobufBytes;
            if (object.quarantineExtractedText != null)
                message.quarantineExtractedText = String(object.quarantineExtractedText);
            if (object.pollEndTime != null)
                if ($util.Long)
                    message.pollEndTime = $util.Long.fromValue(object.pollEndTime, false);
                else if (typeof object.pollEndTime === "string")
                    message.pollEndTime = parseInt(object.pollEndTime, 10);
                else if (typeof object.pollEndTime === "number")
                    message.pollEndTime = object.pollEndTime;
                else if (typeof object.pollEndTime === "object")
                    message.pollEndTime = new $util.LongBits(object.pollEndTime.low >>> 0, object.pollEndTime.high >>> 0).toNumber();
            if (object.pollHideVoterNames != null)
                message.pollHideVoterNames = Boolean(object.pollHideVoterNames);
            if (object.pollAllowAddOption != null)
                message.pollAllowAddOption = Boolean(object.pollAllowAddOption);
            if (object.sharableEventInviteId != null)
                message.sharableEventInviteId = String(object.sharableEventInviteId);
            if (object.sharableEventInviteTitle != null)
                message.sharableEventInviteTitle = String(object.sharableEventInviteTitle);
            if (object.sharableEventInviteStartTime != null)
                if ($util.Long)
                    message.sharableEventInviteStartTime = $util.Long.fromValue(object.sharableEventInviteStartTime, false);
                else if (typeof object.sharableEventInviteStartTime === "string")
                    message.sharableEventInviteStartTime = parseInt(object.sharableEventInviteStartTime, 10);
                else if (typeof object.sharableEventInviteStartTime === "number")
                    message.sharableEventInviteStartTime = object.sharableEventInviteStartTime;
                else if (typeof object.sharableEventInviteStartTime === "object")
                    message.sharableEventInviteStartTime = new $util.LongBits(object.sharableEventInviteStartTime.low >>> 0, object.sharableEventInviteStartTime.high >>> 0).toNumber();
            if (object.sharableEventInviteEndTime != null)
                if ($util.Long)
                    message.sharableEventInviteEndTime = $util.Long.fromValue(object.sharableEventInviteEndTime, false);
                else if (typeof object.sharableEventInviteEndTime === "string")
                    message.sharableEventInviteEndTime = parseInt(object.sharableEventInviteEndTime, 10);
                else if (typeof object.sharableEventInviteEndTime === "number")
                    message.sharableEventInviteEndTime = object.sharableEventInviteEndTime;
                else if (typeof object.sharableEventInviteEndTime === "object")
                    message.sharableEventInviteEndTime = new $util.LongBits(object.sharableEventInviteEndTime.low >>> 0, object.sharableEventInviteEndTime.high >>> 0).toNumber();
            if (object.sharableEventInviteCaption != null)
                message.sharableEventInviteCaption = String(object.sharableEventInviteCaption);
            if (object.sharableEventInviteIsCanceled != null)
                message.sharableEventInviteIsCanceled = Boolean(object.sharableEventInviteIsCanceled);
            if (object.sharableEventInviteJpegThumbnail != null)
                if (typeof object.sharableEventInviteJpegThumbnail === "string")
                    $util.base64.decode(object.sharableEventInviteJpegThumbnail, message.sharableEventInviteJpegThumbnail = $util.newBuffer($util.base64.length(object.sharableEventInviteJpegThumbnail)), 0);
                else if (object.sharableEventInviteJpegThumbnail.length >= 0)
                    message.sharableEventInviteJpegThumbnail = object.sharableEventInviteJpegThumbnail;
            if (object.sharableEventInviteCallLink != null)
                message.sharableEventInviteCallLink = String(object.sharableEventInviteCallLink);
            return message;
        };

        /**
         * Creates a plain object from a MsgOpaqueData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.MsgOpaqueData
         * @static
         * @param {proto.MsgOpaqueData} message MsgOpaqueData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MsgOpaqueData.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.pollOptions = [];
            if (message.body != null && Object.hasOwnProperty.call(message, "body"))
                object.body = message.body;
            if (message.caption != null && Object.hasOwnProperty.call(message, "caption"))
                object.caption = message.caption;
            if (message.lng != null && Object.hasOwnProperty.call(message, "lng"))
                object.lng = options.json && !isFinite(message.lng) ? String(message.lng) : message.lng;
            if (message.isLive != null && Object.hasOwnProperty.call(message, "isLive"))
                object.isLive = message.isLive;
            if (message.lat != null && Object.hasOwnProperty.call(message, "lat"))
                object.lat = options.json && !isFinite(message.lat) ? String(message.lat) : message.lat;
            if (message.paymentAmount1000 != null && Object.hasOwnProperty.call(message, "paymentAmount1000"))
                object.paymentAmount1000 = message.paymentAmount1000;
            if (message.paymentNoteMsgBody != null && Object.hasOwnProperty.call(message, "paymentNoteMsgBody"))
                object.paymentNoteMsgBody = message.paymentNoteMsgBody;
            if (message.matchedText != null && Object.hasOwnProperty.call(message, "matchedText"))
                object.matchedText = message.matchedText;
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                object.title = message.title;
            if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                object.description = message.description;
            if (message.futureproofBuffer != null && Object.hasOwnProperty.call(message, "futureproofBuffer"))
                object.futureproofBuffer = options.bytes === String ? $util.base64.encode(message.futureproofBuffer, 0, message.futureproofBuffer.length) : options.bytes === Array ? Array.prototype.slice.call(message.futureproofBuffer) : message.futureproofBuffer;
            if (message.clientUrl != null && Object.hasOwnProperty.call(message, "clientUrl"))
                object.clientUrl = message.clientUrl;
            if (message.loc != null && Object.hasOwnProperty.call(message, "loc"))
                object.loc = message.loc;
            if (message.pollName != null && Object.hasOwnProperty.call(message, "pollName"))
                object.pollName = message.pollName;
            if (message.pollOptions && message.pollOptions.length) {
                object.pollOptions = Array(message.pollOptions.length);
                for (var j = 0; j < message.pollOptions.length; ++j)
                    object.pollOptions[j] = $root.proto.MsgOpaqueData.PollOption.toObject(message.pollOptions[j], options, q + 1);
            }
            if (message.pollSelectableOptionsCount != null && Object.hasOwnProperty.call(message, "pollSelectableOptionsCount"))
                object.pollSelectableOptionsCount = message.pollSelectableOptionsCount;
            if (message.messageSecret != null && Object.hasOwnProperty.call(message, "messageSecret"))
                object.messageSecret = options.bytes === String ? $util.base64.encode(message.messageSecret, 0, message.messageSecret.length) : options.bytes === Array ? Array.prototype.slice.call(message.messageSecret) : message.messageSecret;
            if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.senderTimestampMs = typeof message.senderTimestampMs === "number" ? BigInt(message.senderTimestampMs) : $util.Long.fromBits(message.senderTimestampMs.low >>> 0, message.senderTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof message.senderTimestampMs === "number")
                    object.senderTimestampMs = options.longs === String ? String(message.senderTimestampMs) : message.senderTimestampMs;
                else
                    object.senderTimestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.senderTimestampMs) : options.longs === Number ? new $util.LongBits(message.senderTimestampMs.low >>> 0, message.senderTimestampMs.high >>> 0).toNumber() : message.senderTimestampMs;
            if (message.pollUpdateParentKey != null && Object.hasOwnProperty.call(message, "pollUpdateParentKey"))
                object.pollUpdateParentKey = message.pollUpdateParentKey;
            if (message.encPollVote != null && Object.hasOwnProperty.call(message, "encPollVote"))
                object.encPollVote = $root.proto.PollEncValue.toObject(message.encPollVote, options, q + 1);
            if (message.encReactionTargetMessageKey != null && Object.hasOwnProperty.call(message, "encReactionTargetMessageKey"))
                object.encReactionTargetMessageKey = message.encReactionTargetMessageKey;
            if (message.encReactionEncPayload != null && Object.hasOwnProperty.call(message, "encReactionEncPayload"))
                object.encReactionEncPayload = options.bytes === String ? $util.base64.encode(message.encReactionEncPayload, 0, message.encReactionEncPayload.length) : options.bytes === Array ? Array.prototype.slice.call(message.encReactionEncPayload) : message.encReactionEncPayload;
            if (message.encReactionEncIv != null && Object.hasOwnProperty.call(message, "encReactionEncIv"))
                object.encReactionEncIv = options.bytes === String ? $util.base64.encode(message.encReactionEncIv, 0, message.encReactionEncIv.length) : options.bytes === Array ? Array.prototype.slice.call(message.encReactionEncIv) : message.encReactionEncIv;
            if (message.isSentCagPollCreation != null && Object.hasOwnProperty.call(message, "isSentCagPollCreation"))
                object.isSentCagPollCreation = message.isSentCagPollCreation;
            if (message.botMessageSecret != null && Object.hasOwnProperty.call(message, "botMessageSecret"))
                object.botMessageSecret = options.bytes === String ? $util.base64.encode(message.botMessageSecret, 0, message.botMessageSecret.length) : options.bytes === Array ? Array.prototype.slice.call(message.botMessageSecret) : message.botMessageSecret;
            if (message.targetMessageKey != null && Object.hasOwnProperty.call(message, "targetMessageKey"))
                object.targetMessageKey = message.targetMessageKey;
            if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload"))
                object.encPayload = options.bytes === String ? $util.base64.encode(message.encPayload, 0, message.encPayload.length) : options.bytes === Array ? Array.prototype.slice.call(message.encPayload) : message.encPayload;
            if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv"))
                object.encIv = options.bytes === String ? $util.base64.encode(message.encIv, 0, message.encIv.length) : options.bytes === Array ? Array.prototype.slice.call(message.encIv) : message.encIv;
            if (message.eventName != null && Object.hasOwnProperty.call(message, "eventName"))
                object.eventName = message.eventName;
            if (message.isEventCanceled != null && Object.hasOwnProperty.call(message, "isEventCanceled"))
                object.isEventCanceled = message.isEventCanceled;
            if (message.eventDescription != null && Object.hasOwnProperty.call(message, "eventDescription"))
                object.eventDescription = message.eventDescription;
            if (message.eventJoinLink != null && Object.hasOwnProperty.call(message, "eventJoinLink"))
                object.eventJoinLink = message.eventJoinLink;
            if (message.eventStartTime != null && Object.hasOwnProperty.call(message, "eventStartTime"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.eventStartTime = typeof message.eventStartTime === "number" ? BigInt(message.eventStartTime) : $util.Long.fromBits(message.eventStartTime.low >>> 0, message.eventStartTime.high >>> 0, false).toBigInt();
                else if (typeof message.eventStartTime === "number")
                    object.eventStartTime = options.longs === String ? String(message.eventStartTime) : message.eventStartTime;
                else
                    object.eventStartTime = options.longs === String ? $util.Long.prototype.toString.call(message.eventStartTime) : options.longs === Number ? new $util.LongBits(message.eventStartTime.low >>> 0, message.eventStartTime.high >>> 0).toNumber() : message.eventStartTime;
            if (message.eventLocation != null && Object.hasOwnProperty.call(message, "eventLocation"))
                object.eventLocation = $root.proto.MsgOpaqueData.EventLocation.toObject(message.eventLocation, options, q + 1);
            if (message.eventEndTime != null && Object.hasOwnProperty.call(message, "eventEndTime"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.eventEndTime = typeof message.eventEndTime === "number" ? BigInt(message.eventEndTime) : $util.Long.fromBits(message.eventEndTime.low >>> 0, message.eventEndTime.high >>> 0, false).toBigInt();
                else if (typeof message.eventEndTime === "number")
                    object.eventEndTime = options.longs === String ? String(message.eventEndTime) : message.eventEndTime;
                else
                    object.eventEndTime = options.longs === String ? $util.Long.prototype.toString.call(message.eventEndTime) : options.longs === Number ? new $util.LongBits(message.eventEndTime.low >>> 0, message.eventEndTime.high >>> 0).toNumber() : message.eventEndTime;
            if (message.pollVotesSnapshot != null && Object.hasOwnProperty.call(message, "pollVotesSnapshot"))
                object.pollVotesSnapshot = $root.proto.MsgOpaqueData.PollVotesSnapshot.toObject(message.pollVotesSnapshot, options, q + 1);
            if (message.pollContentType != null && Object.hasOwnProperty.call(message, "pollContentType"))
                object.pollContentType = options.enums === String ? $root.proto.MsgOpaqueData.PollContentType[message.pollContentType] === undefined ? message.pollContentType : $root.proto.MsgOpaqueData.PollContentType[message.pollContentType] : message.pollContentType;
            if (message.plainProtobufBytes != null && Object.hasOwnProperty.call(message, "plainProtobufBytes"))
                object.plainProtobufBytes = options.bytes === String ? $util.base64.encode(message.plainProtobufBytes, 0, message.plainProtobufBytes.length) : options.bytes === Array ? Array.prototype.slice.call(message.plainProtobufBytes) : message.plainProtobufBytes;
            if (message.eventIsScheduledCall != null && Object.hasOwnProperty.call(message, "eventIsScheduledCall"))
                object.eventIsScheduledCall = message.eventIsScheduledCall;
            if (message.eventExtraGuestsAllowed != null && Object.hasOwnProperty.call(message, "eventExtraGuestsAllowed"))
                object.eventExtraGuestsAllowed = message.eventExtraGuestsAllowed;
            if (message.pollType != null && Object.hasOwnProperty.call(message, "pollType"))
                object.pollType = options.enums === String ? $root.proto.MsgOpaqueData.PollType[message.pollType] === undefined ? message.pollType : $root.proto.MsgOpaqueData.PollType[message.pollType] : message.pollType;
            if (message.correctOptionIndex != null && Object.hasOwnProperty.call(message, "correctOptionIndex"))
                object.correctOptionIndex = message.correctOptionIndex;
            if (message.quarantineExtractedText != null && Object.hasOwnProperty.call(message, "quarantineExtractedText"))
                object.quarantineExtractedText = message.quarantineExtractedText;
            if (message.pollEndTime != null && Object.hasOwnProperty.call(message, "pollEndTime"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.pollEndTime = typeof message.pollEndTime === "number" ? BigInt(message.pollEndTime) : $util.Long.fromBits(message.pollEndTime.low >>> 0, message.pollEndTime.high >>> 0, false).toBigInt();
                else if (typeof message.pollEndTime === "number")
                    object.pollEndTime = options.longs === String ? String(message.pollEndTime) : message.pollEndTime;
                else
                    object.pollEndTime = options.longs === String ? $util.Long.prototype.toString.call(message.pollEndTime) : options.longs === Number ? new $util.LongBits(message.pollEndTime.low >>> 0, message.pollEndTime.high >>> 0).toNumber() : message.pollEndTime;
            if (message.pollHideVoterNames != null && Object.hasOwnProperty.call(message, "pollHideVoterNames"))
                object.pollHideVoterNames = message.pollHideVoterNames;
            if (message.originalSelfAuthor != null && Object.hasOwnProperty.call(message, "originalSelfAuthor"))
                object.originalSelfAuthor = message.originalSelfAuthor;
            if (message.pollAllowAddOption != null && Object.hasOwnProperty.call(message, "pollAllowAddOption"))
                object.pollAllowAddOption = message.pollAllowAddOption;
            if (message.sharableEventInviteId != null && Object.hasOwnProperty.call(message, "sharableEventInviteId"))
                object.sharableEventInviteId = message.sharableEventInviteId;
            if (message.sharableEventInviteTitle != null && Object.hasOwnProperty.call(message, "sharableEventInviteTitle"))
                object.sharableEventInviteTitle = message.sharableEventInviteTitle;
            if (message.sharableEventInviteStartTime != null && Object.hasOwnProperty.call(message, "sharableEventInviteStartTime"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.sharableEventInviteStartTime = typeof message.sharableEventInviteStartTime === "number" ? BigInt(message.sharableEventInviteStartTime) : $util.Long.fromBits(message.sharableEventInviteStartTime.low >>> 0, message.sharableEventInviteStartTime.high >>> 0, false).toBigInt();
                else if (typeof message.sharableEventInviteStartTime === "number")
                    object.sharableEventInviteStartTime = options.longs === String ? String(message.sharableEventInviteStartTime) : message.sharableEventInviteStartTime;
                else
                    object.sharableEventInviteStartTime = options.longs === String ? $util.Long.prototype.toString.call(message.sharableEventInviteStartTime) : options.longs === Number ? new $util.LongBits(message.sharableEventInviteStartTime.low >>> 0, message.sharableEventInviteStartTime.high >>> 0).toNumber() : message.sharableEventInviteStartTime;
            if (message.sharableEventInviteEndTime != null && Object.hasOwnProperty.call(message, "sharableEventInviteEndTime"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.sharableEventInviteEndTime = typeof message.sharableEventInviteEndTime === "number" ? BigInt(message.sharableEventInviteEndTime) : $util.Long.fromBits(message.sharableEventInviteEndTime.low >>> 0, message.sharableEventInviteEndTime.high >>> 0, false).toBigInt();
                else if (typeof message.sharableEventInviteEndTime === "number")
                    object.sharableEventInviteEndTime = options.longs === String ? String(message.sharableEventInviteEndTime) : message.sharableEventInviteEndTime;
                else
                    object.sharableEventInviteEndTime = options.longs === String ? $util.Long.prototype.toString.call(message.sharableEventInviteEndTime) : options.longs === Number ? new $util.LongBits(message.sharableEventInviteEndTime.low >>> 0, message.sharableEventInviteEndTime.high >>> 0).toNumber() : message.sharableEventInviteEndTime;
            if (message.sharableEventInviteCaption != null && Object.hasOwnProperty.call(message, "sharableEventInviteCaption"))
                object.sharableEventInviteCaption = message.sharableEventInviteCaption;
            if (message.sharableEventInviteIsCanceled != null && Object.hasOwnProperty.call(message, "sharableEventInviteIsCanceled"))
                object.sharableEventInviteIsCanceled = message.sharableEventInviteIsCanceled;
            if (message.sharableEventInviteJpegThumbnail != null && Object.hasOwnProperty.call(message, "sharableEventInviteJpegThumbnail"))
                object.sharableEventInviteJpegThumbnail = options.bytes === String ? $util.base64.encode(message.sharableEventInviteJpegThumbnail, 0, message.sharableEventInviteJpegThumbnail.length) : options.bytes === Array ? Array.prototype.slice.call(message.sharableEventInviteJpegThumbnail) : message.sharableEventInviteJpegThumbnail;
            if (message.sharableEventInviteCallLink != null && Object.hasOwnProperty.call(message, "sharableEventInviteCallLink"))
                object.sharableEventInviteCallLink = message.sharableEventInviteCallLink;
            return object;
        };

        /**
         * Converts this MsgOpaqueData to JSON.
         * @function toJSON
         * @memberof proto.MsgOpaqueData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MsgOpaqueData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MsgOpaqueData
         * @function getTypeUrl
         * @memberof proto.MsgOpaqueData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MsgOpaqueData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.MsgOpaqueData";
        };

        MsgOpaqueData.EventLocation = (function() {

            /**
             * Properties of an EventLocation.
             * @memberof proto.MsgOpaqueData
             * @interface IEventLocation
             * @property {number|null} [degreesLatitude] EventLocation degreesLatitude
             * @property {number|null} [degreesLongitude] EventLocation degreesLongitude
             * @property {string|null} [name] EventLocation name
             * @property {string|null} [address] EventLocation address
             * @property {string|null} [url] EventLocation url
             * @property {Uint8Array|null} [jpegThumbnail] EventLocation jpegThumbnail
             */

            /**
             * Constructs a new EventLocation.
             * @memberof proto.MsgOpaqueData
             * @classdesc Represents an EventLocation.
             * @implements IEventLocation
             * @constructor
             * @param {proto.MsgOpaqueData.IEventLocation=} [properties] Properties to set
             */
            function EventLocation(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * EventLocation degreesLatitude.
             * @member {number|null|undefined} degreesLatitude
             * @memberof proto.MsgOpaqueData.EventLocation
             * @instance
             */
            EventLocation.prototype.degreesLatitude = null;

            /**
             * EventLocation degreesLongitude.
             * @member {number|null|undefined} degreesLongitude
             * @memberof proto.MsgOpaqueData.EventLocation
             * @instance
             */
            EventLocation.prototype.degreesLongitude = null;

            /**
             * EventLocation name.
             * @member {string|null|undefined} name
             * @memberof proto.MsgOpaqueData.EventLocation
             * @instance
             */
            EventLocation.prototype.name = null;

            /**
             * EventLocation address.
             * @member {string|null|undefined} address
             * @memberof proto.MsgOpaqueData.EventLocation
             * @instance
             */
            EventLocation.prototype.address = null;

            /**
             * EventLocation url.
             * @member {string|null|undefined} url
             * @memberof proto.MsgOpaqueData.EventLocation
             * @instance
             */
            EventLocation.prototype.url = null;

            /**
             * EventLocation jpegThumbnail.
             * @member {Uint8Array|null|undefined} jpegThumbnail
             * @memberof proto.MsgOpaqueData.EventLocation
             * @instance
             */
            EventLocation.prototype.jpegThumbnail = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventLocation.prototype, "_degreesLatitude", {
                get: $util.oneOfGetter($oneOfFields = ["degreesLatitude"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventLocation.prototype, "_degreesLongitude", {
                get: $util.oneOfGetter($oneOfFields = ["degreesLongitude"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventLocation.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventLocation.prototype, "_address", {
                get: $util.oneOfGetter($oneOfFields = ["address"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventLocation.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(EventLocation.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new EventLocation instance using the specified properties.
             * @function create
             * @memberof proto.MsgOpaqueData.EventLocation
             * @static
             * @param {proto.MsgOpaqueData.IEventLocation=} [properties] Properties to set
             * @returns {proto.MsgOpaqueData.EventLocation} EventLocation instance
             */
            EventLocation.create = function create(properties) {
                return new EventLocation(properties);
            };

            /**
             * Encodes the specified EventLocation message. Does not implicitly {@link proto.MsgOpaqueData.EventLocation.verify|verify} messages.
             * @function encode
             * @memberof proto.MsgOpaqueData.EventLocation
             * @static
             * @param {proto.MsgOpaqueData.IEventLocation} message EventLocation message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EventLocation.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.degreesLatitude != null && Object.hasOwnProperty.call(message, "degreesLatitude"))
                    writer.uint32(/* id 1, wireType 1 =*/9).double(message.degreesLatitude);
                if (message.degreesLongitude != null && Object.hasOwnProperty.call(message, "degreesLongitude"))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.degreesLongitude);
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.name);
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.address);
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.url);
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.jpegThumbnail);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified EventLocation message, length delimited. Does not implicitly {@link proto.MsgOpaqueData.EventLocation.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.MsgOpaqueData.EventLocation
             * @static
             * @param {proto.MsgOpaqueData.IEventLocation} message EventLocation message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            EventLocation.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an EventLocation message from the specified reader or buffer.
             * @function decode
             * @memberof proto.MsgOpaqueData.EventLocation
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.MsgOpaqueData.EventLocation} EventLocation
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EventLocation.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 1)
                                break;
                            message.degreesLatitude = reader.double();
                            message._degreesLatitude = "degreesLatitude";
                            continue;
                        }
                    case 2: {
                            if (u !== 1)
                                break;
                            message.degreesLongitude = reader.double();
                            message._degreesLongitude = "degreesLongitude";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.address = reader.stringVerify();
                            message._address = "address";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.url = reader.stringVerify();
                            message._url = "url";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.jpegThumbnail = reader.bytes();
                            message._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an EventLocation message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.MsgOpaqueData.EventLocation
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.MsgOpaqueData.EventLocation} EventLocation
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            EventLocation.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an EventLocation message.
             * @function verify
             * @memberof proto.MsgOpaqueData.EventLocation
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            EventLocation.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.degreesLatitude != null && Object.hasOwnProperty.call(message, "degreesLatitude")) {
                    properties._degreesLatitude = 1;
                    if (typeof message.degreesLatitude !== "number")
                        return "degreesLatitude: number expected";
                }
                if (message.degreesLongitude != null && Object.hasOwnProperty.call(message, "degreesLongitude")) {
                    properties._degreesLongitude = 1;
                    if (typeof message.degreesLongitude !== "number")
                        return "degreesLongitude: number expected";
                }
                if (message.name != null && Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                if (message.address != null && Object.hasOwnProperty.call(message, "address")) {
                    properties._address = 1;
                    if (!$util.isString(message.address))
                        return "address: string expected";
                }
                if (message.url != null && Object.hasOwnProperty.call(message, "url")) {
                    properties._url = 1;
                    if (!$util.isString(message.url))
                        return "url: string expected";
                }
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail")) {
                    properties._jpegThumbnail = 1;
                    if (!(message.jpegThumbnail && typeof message.jpegThumbnail.length === "number" || $util.isString(message.jpegThumbnail)))
                        return "jpegThumbnail: buffer expected";
                }
                return null;
            };

            /**
             * Creates an EventLocation message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.MsgOpaqueData.EventLocation
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.MsgOpaqueData.EventLocation} EventLocation
             */
            EventLocation.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.MsgOpaqueData.EventLocation: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.degreesLatitude != null)
                    message.degreesLatitude = Number(object.degreesLatitude);
                if (object.degreesLongitude != null)
                    message.degreesLongitude = Number(object.degreesLongitude);
                if (object.name != null)
                    message.name = String(object.name);
                if (object.address != null)
                    message.address = String(object.address);
                if (object.url != null)
                    message.url = String(object.url);
                if (object.jpegThumbnail != null)
                    if (typeof object.jpegThumbnail === "string")
                        $util.base64.decode(object.jpegThumbnail, message.jpegThumbnail = $util.newBuffer($util.base64.length(object.jpegThumbnail)), 0);
                    else if (object.jpegThumbnail.length >= 0)
                        message.jpegThumbnail = object.jpegThumbnail;
                return message;
            };

            /**
             * Creates a plain object from an EventLocation message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.MsgOpaqueData.EventLocation
             * @static
             * @param {proto.MsgOpaqueData.EventLocation} message EventLocation
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            EventLocation.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.degreesLatitude != null && Object.hasOwnProperty.call(message, "degreesLatitude"))
                    object.degreesLatitude = options.json && !isFinite(message.degreesLatitude) ? String(message.degreesLatitude) : message.degreesLatitude;
                if (message.degreesLongitude != null && Object.hasOwnProperty.call(message, "degreesLongitude"))
                    object.degreesLongitude = options.json && !isFinite(message.degreesLongitude) ? String(message.degreesLongitude) : message.degreesLongitude;
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    object.address = message.address;
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    object.url = message.url;
                if (message.jpegThumbnail != null && Object.hasOwnProperty.call(message, "jpegThumbnail"))
                    object.jpegThumbnail = options.bytes === String ? $util.base64.encode(message.jpegThumbnail, 0, message.jpegThumbnail.length) : options.bytes === Array ? Array.prototype.slice.call(message.jpegThumbnail) : message.jpegThumbnail;
                return object;
            };

            /**
             * Converts this EventLocation to JSON.
             * @function toJSON
             * @memberof proto.MsgOpaqueData.EventLocation
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            EventLocation.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for EventLocation
             * @function getTypeUrl
             * @memberof proto.MsgOpaqueData.EventLocation
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            EventLocation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.MsgOpaqueData.EventLocation";
            };

            return EventLocation;
        })();

        /**
         * PollContentType enum.
         * @name proto.MsgOpaqueData.PollContentType
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} TEXT=1 TEXT value
         * @property {number} IMAGE=2 IMAGE value
         */
        MsgOpaqueData.PollContentType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "TEXT"] = 1;
            values[valuesById[2] = "IMAGE"] = 2;
            return values;
        })();

        MsgOpaqueData.PollOption = (function() {

            /**
             * Properties of a PollOption.
             * @memberof proto.MsgOpaqueData
             * @interface IPollOption
             * @property {string|null} [name] PollOption name
             * @property {string|null} [hash] PollOption hash
             */

            /**
             * Constructs a new PollOption.
             * @memberof proto.MsgOpaqueData
             * @classdesc Represents a PollOption.
             * @implements IPollOption
             * @constructor
             * @param {proto.MsgOpaqueData.IPollOption=} [properties] Properties to set
             */
            function PollOption(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PollOption name.
             * @member {string|null|undefined} name
             * @memberof proto.MsgOpaqueData.PollOption
             * @instance
             */
            PollOption.prototype.name = null;

            /**
             * PollOption hash.
             * @member {string|null|undefined} hash
             * @memberof proto.MsgOpaqueData.PollOption
             * @instance
             */
            PollOption.prototype.hash = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollOption.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollOption.prototype, "_hash", {
                get: $util.oneOfGetter($oneOfFields = ["hash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PollOption instance using the specified properties.
             * @function create
             * @memberof proto.MsgOpaqueData.PollOption
             * @static
             * @param {proto.MsgOpaqueData.IPollOption=} [properties] Properties to set
             * @returns {proto.MsgOpaqueData.PollOption} PollOption instance
             */
            PollOption.create = function create(properties) {
                return new PollOption(properties);
            };

            /**
             * Encodes the specified PollOption message. Does not implicitly {@link proto.MsgOpaqueData.PollOption.verify|verify} messages.
             * @function encode
             * @memberof proto.MsgOpaqueData.PollOption
             * @static
             * @param {proto.MsgOpaqueData.IPollOption} message PollOption message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollOption.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.hash != null && Object.hasOwnProperty.call(message, "hash"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.hash);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PollOption message, length delimited. Does not implicitly {@link proto.MsgOpaqueData.PollOption.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.MsgOpaqueData.PollOption
             * @static
             * @param {proto.MsgOpaqueData.IPollOption} message PollOption message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollOption.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PollOption message from the specified reader or buffer.
             * @function decode
             * @memberof proto.MsgOpaqueData.PollOption
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.MsgOpaqueData.PollOption} PollOption
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollOption.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.hash = reader.stringVerify();
                            message._hash = "hash";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PollOption message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.MsgOpaqueData.PollOption
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.MsgOpaqueData.PollOption} PollOption
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollOption.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PollOption message.
             * @function verify
             * @memberof proto.MsgOpaqueData.PollOption
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PollOption.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.name != null && Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                if (message.hash != null && Object.hasOwnProperty.call(message, "hash")) {
                    properties._hash = 1;
                    if (!$util.isString(message.hash))
                        return "hash: string expected";
                }
                return null;
            };

            /**
             * Creates a PollOption message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.MsgOpaqueData.PollOption
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.MsgOpaqueData.PollOption} PollOption
             */
            PollOption.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.MsgOpaqueData.PollOption: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.name != null)
                    message.name = String(object.name);
                if (object.hash != null)
                    message.hash = String(object.hash);
                return message;
            };

            /**
             * Creates a plain object from a PollOption message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.MsgOpaqueData.PollOption
             * @static
             * @param {proto.MsgOpaqueData.PollOption} message PollOption
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PollOption.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.hash != null && Object.hasOwnProperty.call(message, "hash"))
                    object.hash = message.hash;
                return object;
            };

            /**
             * Converts this PollOption to JSON.
             * @function toJSON
             * @memberof proto.MsgOpaqueData.PollOption
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PollOption.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PollOption
             * @function getTypeUrl
             * @memberof proto.MsgOpaqueData.PollOption
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PollOption.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.MsgOpaqueData.PollOption";
            };

            return PollOption;
        })();

        /**
         * PollType enum.
         * @name proto.MsgOpaqueData.PollType
         * @enum {number}
         * @property {number} POLL=0 POLL value
         * @property {number} QUIZ=1 QUIZ value
         */
        MsgOpaqueData.PollType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "POLL"] = 0;
            values[valuesById[1] = "QUIZ"] = 1;
            return values;
        })();

        MsgOpaqueData.PollVoteSnapshot = (function() {

            /**
             * Properties of a PollVoteSnapshot.
             * @memberof proto.MsgOpaqueData
             * @interface IPollVoteSnapshot
             * @property {proto.MsgOpaqueData.IPollOption|null} [option] PollVoteSnapshot option
             * @property {number|null} [optionVoteCount] PollVoteSnapshot optionVoteCount
             */

            /**
             * Constructs a new PollVoteSnapshot.
             * @memberof proto.MsgOpaqueData
             * @classdesc Represents a PollVoteSnapshot.
             * @implements IPollVoteSnapshot
             * @constructor
             * @param {proto.MsgOpaqueData.IPollVoteSnapshot=} [properties] Properties to set
             */
            function PollVoteSnapshot(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PollVoteSnapshot option.
             * @member {proto.MsgOpaqueData.IPollOption|null|undefined} option
             * @memberof proto.MsgOpaqueData.PollVoteSnapshot
             * @instance
             */
            PollVoteSnapshot.prototype.option = null;

            /**
             * PollVoteSnapshot optionVoteCount.
             * @member {number|null|undefined} optionVoteCount
             * @memberof proto.MsgOpaqueData.PollVoteSnapshot
             * @instance
             */
            PollVoteSnapshot.prototype.optionVoteCount = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollVoteSnapshot.prototype, "_option", {
                get: $util.oneOfGetter($oneOfFields = ["option"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollVoteSnapshot.prototype, "_optionVoteCount", {
                get: $util.oneOfGetter($oneOfFields = ["optionVoteCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PollVoteSnapshot instance using the specified properties.
             * @function create
             * @memberof proto.MsgOpaqueData.PollVoteSnapshot
             * @static
             * @param {proto.MsgOpaqueData.IPollVoteSnapshot=} [properties] Properties to set
             * @returns {proto.MsgOpaqueData.PollVoteSnapshot} PollVoteSnapshot instance
             */
            PollVoteSnapshot.create = function create(properties) {
                return new PollVoteSnapshot(properties);
            };

            /**
             * Encodes the specified PollVoteSnapshot message. Does not implicitly {@link proto.MsgOpaqueData.PollVoteSnapshot.verify|verify} messages.
             * @function encode
             * @memberof proto.MsgOpaqueData.PollVoteSnapshot
             * @static
             * @param {proto.MsgOpaqueData.IPollVoteSnapshot} message PollVoteSnapshot message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollVoteSnapshot.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.option != null && Object.hasOwnProperty.call(message, "option"))
                    $root.proto.MsgOpaqueData.PollOption.encode(message.option, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.optionVoteCount != null && Object.hasOwnProperty.call(message, "optionVoteCount"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.optionVoteCount);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PollVoteSnapshot message, length delimited. Does not implicitly {@link proto.MsgOpaqueData.PollVoteSnapshot.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.MsgOpaqueData.PollVoteSnapshot
             * @static
             * @param {proto.MsgOpaqueData.IPollVoteSnapshot} message PollVoteSnapshot message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollVoteSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PollVoteSnapshot message from the specified reader or buffer.
             * @function decode
             * @memberof proto.MsgOpaqueData.PollVoteSnapshot
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.MsgOpaqueData.PollVoteSnapshot} PollVoteSnapshot
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollVoteSnapshot.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.option = $root.proto.MsgOpaqueData.PollOption.decode(reader, reader.uint32(), undefined, q + 1, message.option);
                            message._option = "option";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.optionVoteCount = reader.int32();
                            message._optionVoteCount = "optionVoteCount";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PollVoteSnapshot message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.MsgOpaqueData.PollVoteSnapshot
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.MsgOpaqueData.PollVoteSnapshot} PollVoteSnapshot
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollVoteSnapshot.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PollVoteSnapshot message.
             * @function verify
             * @memberof proto.MsgOpaqueData.PollVoteSnapshot
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PollVoteSnapshot.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.option != null && Object.hasOwnProperty.call(message, "option")) {
                    properties._option = 1;
                    {
                        var error = $root.proto.MsgOpaqueData.PollOption.verify(message.option, q + 1);
                        if (error)
                            return "option." + error;
                    }
                }
                if (message.optionVoteCount != null && Object.hasOwnProperty.call(message, "optionVoteCount")) {
                    properties._optionVoteCount = 1;
                    if (!$util.isInteger(message.optionVoteCount))
                        return "optionVoteCount: integer expected";
                }
                return null;
            };

            /**
             * Creates a PollVoteSnapshot message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.MsgOpaqueData.PollVoteSnapshot
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.MsgOpaqueData.PollVoteSnapshot} PollVoteSnapshot
             */
            PollVoteSnapshot.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.MsgOpaqueData.PollVoteSnapshot: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.option != null) {
                    if (!$util.isObject(object.option))
                        throw TypeError(".proto.MsgOpaqueData.PollVoteSnapshot.option: object expected");
                    message.option = $root.proto.MsgOpaqueData.PollOption.fromObject(object.option, q + 1);
                }
                if (object.optionVoteCount != null)
                    message.optionVoteCount = object.optionVoteCount | 0;
                return message;
            };

            /**
             * Creates a plain object from a PollVoteSnapshot message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.MsgOpaqueData.PollVoteSnapshot
             * @static
             * @param {proto.MsgOpaqueData.PollVoteSnapshot} message PollVoteSnapshot
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PollVoteSnapshot.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.option != null && Object.hasOwnProperty.call(message, "option"))
                    object.option = $root.proto.MsgOpaqueData.PollOption.toObject(message.option, options, q + 1);
                if (message.optionVoteCount != null && Object.hasOwnProperty.call(message, "optionVoteCount"))
                    object.optionVoteCount = message.optionVoteCount;
                return object;
            };

            /**
             * Converts this PollVoteSnapshot to JSON.
             * @function toJSON
             * @memberof proto.MsgOpaqueData.PollVoteSnapshot
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PollVoteSnapshot.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PollVoteSnapshot
             * @function getTypeUrl
             * @memberof proto.MsgOpaqueData.PollVoteSnapshot
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PollVoteSnapshot.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.MsgOpaqueData.PollVoteSnapshot";
            };

            return PollVoteSnapshot;
        })();

        MsgOpaqueData.PollVotesSnapshot = (function() {

            /**
             * Properties of a PollVotesSnapshot.
             * @memberof proto.MsgOpaqueData
             * @interface IPollVotesSnapshot
             * @property {Array.<proto.MsgOpaqueData.IPollVoteSnapshot>|null} [pollVotes] PollVotesSnapshot pollVotes
             */

            /**
             * Constructs a new PollVotesSnapshot.
             * @memberof proto.MsgOpaqueData
             * @classdesc Represents a PollVotesSnapshot.
             * @implements IPollVotesSnapshot
             * @constructor
             * @param {proto.MsgOpaqueData.IPollVotesSnapshot=} [properties] Properties to set
             */
            function PollVotesSnapshot(properties) {
                this.pollVotes = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PollVotesSnapshot pollVotes.
             * @member {Array.<proto.MsgOpaqueData.IPollVoteSnapshot>} pollVotes
             * @memberof proto.MsgOpaqueData.PollVotesSnapshot
             * @instance
             */
            PollVotesSnapshot.prototype.pollVotes = $util.emptyArray;

            /**
             * Creates a new PollVotesSnapshot instance using the specified properties.
             * @function create
             * @memberof proto.MsgOpaqueData.PollVotesSnapshot
             * @static
             * @param {proto.MsgOpaqueData.IPollVotesSnapshot=} [properties] Properties to set
             * @returns {proto.MsgOpaqueData.PollVotesSnapshot} PollVotesSnapshot instance
             */
            PollVotesSnapshot.create = function create(properties) {
                return new PollVotesSnapshot(properties);
            };

            /**
             * Encodes the specified PollVotesSnapshot message. Does not implicitly {@link proto.MsgOpaqueData.PollVotesSnapshot.verify|verify} messages.
             * @function encode
             * @memberof proto.MsgOpaqueData.PollVotesSnapshot
             * @static
             * @param {proto.MsgOpaqueData.IPollVotesSnapshot} message PollVotesSnapshot message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollVotesSnapshot.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.pollVotes != null && message.pollVotes.length)
                    for (var i = 0; i < message.pollVotes.length; ++i)
                        $root.proto.MsgOpaqueData.PollVoteSnapshot.encode(message.pollVotes[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PollVotesSnapshot message, length delimited. Does not implicitly {@link proto.MsgOpaqueData.PollVotesSnapshot.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.MsgOpaqueData.PollVotesSnapshot
             * @static
             * @param {proto.MsgOpaqueData.IPollVotesSnapshot} message PollVotesSnapshot message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollVotesSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PollVotesSnapshot message from the specified reader or buffer.
             * @function decode
             * @memberof proto.MsgOpaqueData.PollVotesSnapshot
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.MsgOpaqueData.PollVotesSnapshot} PollVotesSnapshot
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollVotesSnapshot.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(message.pollVotes && message.pollVotes.length))
                                message.pollVotes = [];
                            message.pollVotes.push($root.proto.MsgOpaqueData.PollVoteSnapshot.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PollVotesSnapshot message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.MsgOpaqueData.PollVotesSnapshot
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.MsgOpaqueData.PollVotesSnapshot} PollVotesSnapshot
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollVotesSnapshot.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PollVotesSnapshot message.
             * @function verify
             * @memberof proto.MsgOpaqueData.PollVotesSnapshot
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PollVotesSnapshot.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.pollVotes != null && Object.hasOwnProperty.call(message, "pollVotes")) {
                    if (!Array.isArray(message.pollVotes))
                        return "pollVotes: array expected";
                    for (var i = 0; i < message.pollVotes.length; ++i) {
                        var error = $root.proto.MsgOpaqueData.PollVoteSnapshot.verify(message.pollVotes[i], q + 1);
                        if (error)
                            return "pollVotes." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a PollVotesSnapshot message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.MsgOpaqueData.PollVotesSnapshot
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.MsgOpaqueData.PollVotesSnapshot} PollVotesSnapshot
             */
            PollVotesSnapshot.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.MsgOpaqueData.PollVotesSnapshot: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.pollVotes) {
                    if (!Array.isArray(object.pollVotes))
                        throw TypeError(".proto.MsgOpaqueData.PollVotesSnapshot.pollVotes: array expected");
                    message.pollVotes = Array(object.pollVotes.length);
                    for (var i = 0; i < object.pollVotes.length; ++i) {
                        if (!$util.isObject(object.pollVotes[i]))
                            throw TypeError(".proto.MsgOpaqueData.PollVotesSnapshot.pollVotes: object expected");
                        message.pollVotes[i] = $root.proto.MsgOpaqueData.PollVoteSnapshot.fromObject(object.pollVotes[i], q + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a PollVotesSnapshot message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.MsgOpaqueData.PollVotesSnapshot
             * @static
             * @param {proto.MsgOpaqueData.PollVotesSnapshot} message PollVotesSnapshot
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PollVotesSnapshot.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.pollVotes = [];
                if (message.pollVotes && message.pollVotes.length) {
                    object.pollVotes = Array(message.pollVotes.length);
                    for (var j = 0; j < message.pollVotes.length; ++j)
                        object.pollVotes[j] = $root.proto.MsgOpaqueData.PollVoteSnapshot.toObject(message.pollVotes[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this PollVotesSnapshot to JSON.
             * @function toJSON
             * @memberof proto.MsgOpaqueData.PollVotesSnapshot
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PollVotesSnapshot.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PollVotesSnapshot
             * @function getTypeUrl
             * @memberof proto.MsgOpaqueData.PollVotesSnapshot
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PollVotesSnapshot.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.MsgOpaqueData.PollVotesSnapshot";
            };

            return PollVotesSnapshot;
        })();

        return MsgOpaqueData;
    })();

    proto.MsgRowOpaqueData = (function() {

        /**
         * Properties of a MsgRowOpaqueData.
         * @memberof proto
         * @interface IMsgRowOpaqueData
         * @property {proto.IMsgOpaqueData|null} [currentMsg] MsgRowOpaqueData currentMsg
         * @property {proto.IMsgOpaqueData|null} [quotedMsg] MsgRowOpaqueData quotedMsg
         */

        /**
         * Constructs a new MsgRowOpaqueData.
         * @memberof proto
         * @classdesc Represents a MsgRowOpaqueData.
         * @implements IMsgRowOpaqueData
         * @constructor
         * @param {proto.IMsgRowOpaqueData=} [properties] Properties to set
         */
        function MsgRowOpaqueData(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MsgRowOpaqueData currentMsg.
         * @member {proto.IMsgOpaqueData|null|undefined} currentMsg
         * @memberof proto.MsgRowOpaqueData
         * @instance
         */
        MsgRowOpaqueData.prototype.currentMsg = null;

        /**
         * MsgRowOpaqueData quotedMsg.
         * @member {proto.IMsgOpaqueData|null|undefined} quotedMsg
         * @memberof proto.MsgRowOpaqueData
         * @instance
         */
        MsgRowOpaqueData.prototype.quotedMsg = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgRowOpaqueData.prototype, "_currentMsg", {
            get: $util.oneOfGetter($oneOfFields = ["currentMsg"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MsgRowOpaqueData.prototype, "_quotedMsg", {
            get: $util.oneOfGetter($oneOfFields = ["quotedMsg"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new MsgRowOpaqueData instance using the specified properties.
         * @function create
         * @memberof proto.MsgRowOpaqueData
         * @static
         * @param {proto.IMsgRowOpaqueData=} [properties] Properties to set
         * @returns {proto.MsgRowOpaqueData} MsgRowOpaqueData instance
         */
        MsgRowOpaqueData.create = function create(properties) {
            return new MsgRowOpaqueData(properties);
        };

        /**
         * Encodes the specified MsgRowOpaqueData message. Does not implicitly {@link proto.MsgRowOpaqueData.verify|verify} messages.
         * @function encode
         * @memberof proto.MsgRowOpaqueData
         * @static
         * @param {proto.IMsgRowOpaqueData} message MsgRowOpaqueData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MsgRowOpaqueData.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.currentMsg != null && Object.hasOwnProperty.call(message, "currentMsg"))
                $root.proto.MsgOpaqueData.encode(message.currentMsg, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.quotedMsg != null && Object.hasOwnProperty.call(message, "quotedMsg"))
                $root.proto.MsgOpaqueData.encode(message.quotedMsg, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified MsgRowOpaqueData message, length delimited. Does not implicitly {@link proto.MsgRowOpaqueData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.MsgRowOpaqueData
         * @static
         * @param {proto.IMsgRowOpaqueData} message MsgRowOpaqueData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MsgRowOpaqueData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MsgRowOpaqueData message from the specified reader or buffer.
         * @function decode
         * @memberof proto.MsgRowOpaqueData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.MsgRowOpaqueData} MsgRowOpaqueData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MsgRowOpaqueData.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.currentMsg = $root.proto.MsgOpaqueData.decode(reader, reader.uint32(), undefined, q + 1, message.currentMsg);
                        message._currentMsg = "currentMsg";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.quotedMsg = $root.proto.MsgOpaqueData.decode(reader, reader.uint32(), undefined, q + 1, message.quotedMsg);
                        message._quotedMsg = "quotedMsg";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a MsgRowOpaqueData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.MsgRowOpaqueData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.MsgRowOpaqueData} MsgRowOpaqueData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MsgRowOpaqueData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MsgRowOpaqueData message.
         * @function verify
         * @memberof proto.MsgRowOpaqueData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MsgRowOpaqueData.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.currentMsg != null && Object.hasOwnProperty.call(message, "currentMsg")) {
                properties._currentMsg = 1;
                {
                    var error = $root.proto.MsgOpaqueData.verify(message.currentMsg, q + 1);
                    if (error)
                        return "currentMsg." + error;
                }
            }
            if (message.quotedMsg != null && Object.hasOwnProperty.call(message, "quotedMsg")) {
                properties._quotedMsg = 1;
                {
                    var error = $root.proto.MsgOpaqueData.verify(message.quotedMsg, q + 1);
                    if (error)
                        return "quotedMsg." + error;
                }
            }
            return null;
        };

        /**
         * Creates a MsgRowOpaqueData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.MsgRowOpaqueData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.MsgRowOpaqueData} MsgRowOpaqueData
         */
        MsgRowOpaqueData.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.MsgRowOpaqueData: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.currentMsg != null) {
                if (!$util.isObject(object.currentMsg))
                    throw TypeError(".proto.MsgRowOpaqueData.currentMsg: object expected");
                message.currentMsg = $root.proto.MsgOpaqueData.fromObject(object.currentMsg, q + 1);
            }
            if (object.quotedMsg != null) {
                if (!$util.isObject(object.quotedMsg))
                    throw TypeError(".proto.MsgRowOpaqueData.quotedMsg: object expected");
                message.quotedMsg = $root.proto.MsgOpaqueData.fromObject(object.quotedMsg, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a MsgRowOpaqueData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.MsgRowOpaqueData
         * @static
         * @param {proto.MsgRowOpaqueData} message MsgRowOpaqueData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MsgRowOpaqueData.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.currentMsg != null && Object.hasOwnProperty.call(message, "currentMsg"))
                object.currentMsg = $root.proto.MsgOpaqueData.toObject(message.currentMsg, options, q + 1);
            if (message.quotedMsg != null && Object.hasOwnProperty.call(message, "quotedMsg"))
                object.quotedMsg = $root.proto.MsgOpaqueData.toObject(message.quotedMsg, options, q + 1);
            return object;
        };

        /**
         * Converts this MsgRowOpaqueData to JSON.
         * @function toJSON
         * @memberof proto.MsgRowOpaqueData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MsgRowOpaqueData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MsgRowOpaqueData
         * @function getTypeUrl
         * @memberof proto.MsgRowOpaqueData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MsgRowOpaqueData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.MsgRowOpaqueData";
        };

        return MsgRowOpaqueData;
    })();

    /**
     * MutationProps enum.
     * @name proto.MutationProps
     * @enum {number}
     * @property {number} STAR_ACTION=2 STAR_ACTION value
     * @property {number} CONTACT_ACTION=3 CONTACT_ACTION value
     * @property {number} MUTE_ACTION=4 MUTE_ACTION value
     * @property {number} PIN_ACTION=5 PIN_ACTION value
     * @property {number} SECURITY_NOTIFICATION_SETTING=6 SECURITY_NOTIFICATION_SETTING value
     * @property {number} PUSH_NAME_SETTING=7 PUSH_NAME_SETTING value
     * @property {number} QUICK_REPLY_ACTION=8 QUICK_REPLY_ACTION value
     * @property {number} RECENT_EMOJI_WEIGHTS_ACTION=11 RECENT_EMOJI_WEIGHTS_ACTION value
     * @property {number} LABEL_MESSAGE_ACTION=13 LABEL_MESSAGE_ACTION value
     * @property {number} LABEL_EDIT_ACTION=14 LABEL_EDIT_ACTION value
     * @property {number} LABEL_ASSOCIATION_ACTION=15 LABEL_ASSOCIATION_ACTION value
     * @property {number} LOCALE_SETTING=16 LOCALE_SETTING value
     * @property {number} ARCHIVE_CHAT_ACTION=17 ARCHIVE_CHAT_ACTION value
     * @property {number} DELETE_MESSAGE_FOR_ME_ACTION=18 DELETE_MESSAGE_FOR_ME_ACTION value
     * @property {number} KEY_EXPIRATION=19 KEY_EXPIRATION value
     * @property {number} MARK_CHAT_AS_READ_ACTION=20 MARK_CHAT_AS_READ_ACTION value
     * @property {number} CLEAR_CHAT_ACTION=21 CLEAR_CHAT_ACTION value
     * @property {number} DELETE_CHAT_ACTION=22 DELETE_CHAT_ACTION value
     * @property {number} UNARCHIVE_CHATS_SETTING=23 UNARCHIVE_CHATS_SETTING value
     * @property {number} PRIMARY_FEATURE=24 PRIMARY_FEATURE value
     * @property {number} ANDROID_UNSUPPORTED_ACTIONS=26 ANDROID_UNSUPPORTED_ACTIONS value
     * @property {number} AGENT_ACTION=27 AGENT_ACTION value
     * @property {number} SUBSCRIPTION_ACTION=28 SUBSCRIPTION_ACTION value
     * @property {number} USER_STATUS_MUTE_ACTION=29 USER_STATUS_MUTE_ACTION value
     * @property {number} TIME_FORMAT_ACTION=30 TIME_FORMAT_ACTION value
     * @property {number} NUX_ACTION=31 NUX_ACTION value
     * @property {number} PRIMARY_VERSION_ACTION=32 PRIMARY_VERSION_ACTION value
     * @property {number} STICKER_ACTION=33 STICKER_ACTION value
     * @property {number} REMOVE_RECENT_STICKER_ACTION=34 REMOVE_RECENT_STICKER_ACTION value
     * @property {number} CHAT_ASSIGNMENT=35 CHAT_ASSIGNMENT value
     * @property {number} CHAT_ASSIGNMENT_OPENED_STATUS=36 CHAT_ASSIGNMENT_OPENED_STATUS value
     * @property {number} PN_FOR_LID_CHAT_ACTION=37 PN_FOR_LID_CHAT_ACTION value
     * @property {number} MARKETING_MESSAGE_ACTION=38 MARKETING_MESSAGE_ACTION value
     * @property {number} MARKETING_MESSAGE_BROADCAST_ACTION=39 MARKETING_MESSAGE_BROADCAST_ACTION value
     * @property {number} EXTERNAL_WEB_BETA_ACTION=40 EXTERNAL_WEB_BETA_ACTION value
     * @property {number} PRIVACY_SETTING_RELAY_ALL_CALLS=41 PRIVACY_SETTING_RELAY_ALL_CALLS value
     * @property {number} CALL_LOG_ACTION=42 CALL_LOG_ACTION value
     * @property {number} UGC_BOT=43 UGC_BOT value
     * @property {number} STATUS_PRIVACY=44 STATUS_PRIVACY value
     * @property {number} BOT_WELCOME_REQUEST_ACTION=45 BOT_WELCOME_REQUEST_ACTION value
     * @property {number} DELETE_INDIVIDUAL_CALL_LOG=46 DELETE_INDIVIDUAL_CALL_LOG value
     * @property {number} LABEL_REORDERING_ACTION=47 LABEL_REORDERING_ACTION value
     * @property {number} PAYMENT_INFO_ACTION=48 PAYMENT_INFO_ACTION value
     * @property {number} CUSTOM_PAYMENT_METHODS_ACTION=49 CUSTOM_PAYMENT_METHODS_ACTION value
     * @property {number} LOCK_CHAT_ACTION=50 LOCK_CHAT_ACTION value
     * @property {number} CHAT_LOCK_SETTINGS=51 CHAT_LOCK_SETTINGS value
     * @property {number} WAMO_USER_IDENTIFIER_ACTION=52 WAMO_USER_IDENTIFIER_ACTION value
     * @property {number} PRIVACY_SETTING_DISABLE_LINK_PREVIEWS_ACTION=53 PRIVACY_SETTING_DISABLE_LINK_PREVIEWS_ACTION value
     * @property {number} DEVICE_CAPABILITIES=54 DEVICE_CAPABILITIES value
     * @property {number} NOTE_EDIT_ACTION=55 NOTE_EDIT_ACTION value
     * @property {number} FAVORITES_ACTION=56 FAVORITES_ACTION value
     * @property {number} MERCHANT_PAYMENT_PARTNER_ACTION=57 MERCHANT_PAYMENT_PARTNER_ACTION value
     * @property {number} WAFFLE_ACCOUNT_LINK_STATE_ACTION=58 WAFFLE_ACCOUNT_LINK_STATE_ACTION value
     * @property {number} USERNAME_CHAT_START_MODE=59 USERNAME_CHAT_START_MODE value
     * @property {number} NOTIFICATION_ACTIVITY_SETTING_ACTION=60 NOTIFICATION_ACTIVITY_SETTING_ACTION value
     * @property {number} LID_CONTACT_ACTION=61 LID_CONTACT_ACTION value
     * @property {number} CTWA_PER_CUSTOMER_DATA_SHARING_ACTION=62 CTWA_PER_CUSTOMER_DATA_SHARING_ACTION value
     * @property {number} PAYMENT_TOS_ACTION=63 PAYMENT_TOS_ACTION value
     * @property {number} PRIVACY_SETTING_CHANNELS_PERSONALISED_RECOMMENDATION_ACTION=64 PRIVACY_SETTING_CHANNELS_PERSONALISED_RECOMMENDATION_ACTION value
     * @property {number} BUSINESS_BROADCAST_ASSOCIATION_ACTION=65 BUSINESS_BROADCAST_ASSOCIATION_ACTION value
     * @property {number} DETECTED_OUTCOMES_STATUS_ACTION=66 DETECTED_OUTCOMES_STATUS_ACTION value
     * @property {number} MAIBA_AI_FEATURES_CONTROL_ACTION=68 MAIBA_AI_FEATURES_CONTROL_ACTION value
     * @property {number} BUSINESS_BROADCAST_LIST_ACTION=69 BUSINESS_BROADCAST_LIST_ACTION value
     * @property {number} MUSIC_USER_ID_ACTION=70 MUSIC_USER_ID_ACTION value
     * @property {number} STATUS_POST_OPT_IN_NOTIFICATION_PREFERENCES_ACTION=71 STATUS_POST_OPT_IN_NOTIFICATION_PREFERENCES_ACTION value
     * @property {number} AVATAR_UPDATED_ACTION=72 AVATAR_UPDATED_ACTION value
     * @property {number} GALAXY_FLOW_ACTION=73 GALAXY_FLOW_ACTION value
     * @property {number} PRIVATE_PROCESSING_SETTING_ACTION=74 PRIVATE_PROCESSING_SETTING_ACTION value
     * @property {number} NEWSLETTER_SAVED_INTERESTS_ACTION=75 NEWSLETTER_SAVED_INTERESTS_ACTION value
     * @property {number} AI_THREAD_RENAME_ACTION=76 AI_THREAD_RENAME_ACTION value
     * @property {number} INTERACTIVE_MESSAGE_ACTION=77 INTERACTIVE_MESSAGE_ACTION value
     * @property {number} SETTINGS_SYNC_ACTION=78 SETTINGS_SYNC_ACTION value
     * @property {number} OUT_CONTACT_ACTION=79 OUT_CONTACT_ACTION value
     * @property {number} NCT_SALT_SYNC_ACTION=80 NCT_SALT_SYNC_ACTION value
     * @property {number} BUSINESS_BROADCAST_CAMPAIGN_ACTION=81 BUSINESS_BROADCAST_CAMPAIGN_ACTION value
     * @property {number} BUSINESS_BROADCAST_INSIGHTS_ACTION=82 BUSINESS_BROADCAST_INSIGHTS_ACTION value
     * @property {number} CUSTOMER_DATA_ACTION=83 CUSTOMER_DATA_ACTION value
     * @property {number} SUBSCRIPTIONS_SYNC_V2_ACTION=84 SUBSCRIPTIONS_SYNC_V2_ACTION value
     * @property {number} THREAD_PIN_ACTION=85 THREAD_PIN_ACTION value
     * @property {number} AUTO_ORGANIZE_BUSINESS_CHAT_SETTING=86 AUTO_ORGANIZE_BUSINESS_CHAT_SETTING value
     * @property {number} BIZ_AI_SETTINGS_NUDGE_ACTION=87 BIZ_AI_SETTINGS_NUDGE_ACTION value
     * @property {number} COEX_V2_VERSION_ACTION=88 COEX_V2_VERSION_ACTION value
     * @property {number} WASA_ROOT_SECRET_ACTION=89 WASA_ROOT_SECRET_ACTION value
     * @property {number} SHARE_OWN_PN=10001 SHARE_OWN_PN value
     * @property {number} BUSINESS_BROADCAST_ACTION=10002 BUSINESS_BROADCAST_ACTION value
     * @property {number} AI_THREAD_DELETE_ACTION=10003 AI_THREAD_DELETE_ACTION value
     */
    proto.MutationProps = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[2] = "STAR_ACTION"] = 2;
        values[valuesById[3] = "CONTACT_ACTION"] = 3;
        values[valuesById[4] = "MUTE_ACTION"] = 4;
        values[valuesById[5] = "PIN_ACTION"] = 5;
        values[valuesById[6] = "SECURITY_NOTIFICATION_SETTING"] = 6;
        values[valuesById[7] = "PUSH_NAME_SETTING"] = 7;
        values[valuesById[8] = "QUICK_REPLY_ACTION"] = 8;
        values[valuesById[11] = "RECENT_EMOJI_WEIGHTS_ACTION"] = 11;
        values[valuesById[13] = "LABEL_MESSAGE_ACTION"] = 13;
        values[valuesById[14] = "LABEL_EDIT_ACTION"] = 14;
        values[valuesById[15] = "LABEL_ASSOCIATION_ACTION"] = 15;
        values[valuesById[16] = "LOCALE_SETTING"] = 16;
        values[valuesById[17] = "ARCHIVE_CHAT_ACTION"] = 17;
        values[valuesById[18] = "DELETE_MESSAGE_FOR_ME_ACTION"] = 18;
        values[valuesById[19] = "KEY_EXPIRATION"] = 19;
        values[valuesById[20] = "MARK_CHAT_AS_READ_ACTION"] = 20;
        values[valuesById[21] = "CLEAR_CHAT_ACTION"] = 21;
        values[valuesById[22] = "DELETE_CHAT_ACTION"] = 22;
        values[valuesById[23] = "UNARCHIVE_CHATS_SETTING"] = 23;
        values[valuesById[24] = "PRIMARY_FEATURE"] = 24;
        values[valuesById[26] = "ANDROID_UNSUPPORTED_ACTIONS"] = 26;
        values[valuesById[27] = "AGENT_ACTION"] = 27;
        values[valuesById[28] = "SUBSCRIPTION_ACTION"] = 28;
        values[valuesById[29] = "USER_STATUS_MUTE_ACTION"] = 29;
        values[valuesById[30] = "TIME_FORMAT_ACTION"] = 30;
        values[valuesById[31] = "NUX_ACTION"] = 31;
        values[valuesById[32] = "PRIMARY_VERSION_ACTION"] = 32;
        values[valuesById[33] = "STICKER_ACTION"] = 33;
        values[valuesById[34] = "REMOVE_RECENT_STICKER_ACTION"] = 34;
        values[valuesById[35] = "CHAT_ASSIGNMENT"] = 35;
        values[valuesById[36] = "CHAT_ASSIGNMENT_OPENED_STATUS"] = 36;
        values[valuesById[37] = "PN_FOR_LID_CHAT_ACTION"] = 37;
        values[valuesById[38] = "MARKETING_MESSAGE_ACTION"] = 38;
        values[valuesById[39] = "MARKETING_MESSAGE_BROADCAST_ACTION"] = 39;
        values[valuesById[40] = "EXTERNAL_WEB_BETA_ACTION"] = 40;
        values[valuesById[41] = "PRIVACY_SETTING_RELAY_ALL_CALLS"] = 41;
        values[valuesById[42] = "CALL_LOG_ACTION"] = 42;
        values[valuesById[43] = "UGC_BOT"] = 43;
        values[valuesById[44] = "STATUS_PRIVACY"] = 44;
        values[valuesById[45] = "BOT_WELCOME_REQUEST_ACTION"] = 45;
        values[valuesById[46] = "DELETE_INDIVIDUAL_CALL_LOG"] = 46;
        values[valuesById[47] = "LABEL_REORDERING_ACTION"] = 47;
        values[valuesById[48] = "PAYMENT_INFO_ACTION"] = 48;
        values[valuesById[49] = "CUSTOM_PAYMENT_METHODS_ACTION"] = 49;
        values[valuesById[50] = "LOCK_CHAT_ACTION"] = 50;
        values[valuesById[51] = "CHAT_LOCK_SETTINGS"] = 51;
        values[valuesById[52] = "WAMO_USER_IDENTIFIER_ACTION"] = 52;
        values[valuesById[53] = "PRIVACY_SETTING_DISABLE_LINK_PREVIEWS_ACTION"] = 53;
        values[valuesById[54] = "DEVICE_CAPABILITIES"] = 54;
        values[valuesById[55] = "NOTE_EDIT_ACTION"] = 55;
        values[valuesById[56] = "FAVORITES_ACTION"] = 56;
        values[valuesById[57] = "MERCHANT_PAYMENT_PARTNER_ACTION"] = 57;
        values[valuesById[58] = "WAFFLE_ACCOUNT_LINK_STATE_ACTION"] = 58;
        values[valuesById[59] = "USERNAME_CHAT_START_MODE"] = 59;
        values[valuesById[60] = "NOTIFICATION_ACTIVITY_SETTING_ACTION"] = 60;
        values[valuesById[61] = "LID_CONTACT_ACTION"] = 61;
        values[valuesById[62] = "CTWA_PER_CUSTOMER_DATA_SHARING_ACTION"] = 62;
        values[valuesById[63] = "PAYMENT_TOS_ACTION"] = 63;
        values[valuesById[64] = "PRIVACY_SETTING_CHANNELS_PERSONALISED_RECOMMENDATION_ACTION"] = 64;
        values[valuesById[65] = "BUSINESS_BROADCAST_ASSOCIATION_ACTION"] = 65;
        values[valuesById[66] = "DETECTED_OUTCOMES_STATUS_ACTION"] = 66;
        values[valuesById[68] = "MAIBA_AI_FEATURES_CONTROL_ACTION"] = 68;
        values[valuesById[69] = "BUSINESS_BROADCAST_LIST_ACTION"] = 69;
        values[valuesById[70] = "MUSIC_USER_ID_ACTION"] = 70;
        values[valuesById[71] = "STATUS_POST_OPT_IN_NOTIFICATION_PREFERENCES_ACTION"] = 71;
        values[valuesById[72] = "AVATAR_UPDATED_ACTION"] = 72;
        values[valuesById[73] = "GALAXY_FLOW_ACTION"] = 73;
        values[valuesById[74] = "PRIVATE_PROCESSING_SETTING_ACTION"] = 74;
        values[valuesById[75] = "NEWSLETTER_SAVED_INTERESTS_ACTION"] = 75;
        values[valuesById[76] = "AI_THREAD_RENAME_ACTION"] = 76;
        values[valuesById[77] = "INTERACTIVE_MESSAGE_ACTION"] = 77;
        values[valuesById[78] = "SETTINGS_SYNC_ACTION"] = 78;
        values[valuesById[79] = "OUT_CONTACT_ACTION"] = 79;
        values[valuesById[80] = "NCT_SALT_SYNC_ACTION"] = 80;
        values[valuesById[81] = "BUSINESS_BROADCAST_CAMPAIGN_ACTION"] = 81;
        values[valuesById[82] = "BUSINESS_BROADCAST_INSIGHTS_ACTION"] = 82;
        values[valuesById[83] = "CUSTOMER_DATA_ACTION"] = 83;
        values[valuesById[84] = "SUBSCRIPTIONS_SYNC_V2_ACTION"] = 84;
        values[valuesById[85] = "THREAD_PIN_ACTION"] = 85;
        values[valuesById[86] = "AUTO_ORGANIZE_BUSINESS_CHAT_SETTING"] = 86;
        values[valuesById[87] = "BIZ_AI_SETTINGS_NUDGE_ACTION"] = 87;
        values[valuesById[88] = "COEX_V2_VERSION_ACTION"] = 88;
        values[valuesById[89] = "WASA_ROOT_SECRET_ACTION"] = 89;
        values[valuesById[10001] = "SHARE_OWN_PN"] = 10001;
        values[valuesById[10002] = "BUSINESS_BROADCAST_ACTION"] = 10002;
        values[valuesById[10003] = "AI_THREAD_DELETE_ACTION"] = 10003;
        return values;
    })();

    proto.NoiseCertificate = (function() {

        /**
         * Properties of a NoiseCertificate.
         * @memberof proto
         * @interface INoiseCertificate
         * @property {Uint8Array|null} [details] NoiseCertificate details
         * @property {Uint8Array|null} [signature] NoiseCertificate signature
         */

        /**
         * Constructs a new NoiseCertificate.
         * @memberof proto
         * @classdesc Represents a NoiseCertificate.
         * @implements INoiseCertificate
         * @constructor
         * @param {proto.INoiseCertificate=} [properties] Properties to set
         */
        function NoiseCertificate(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * NoiseCertificate details.
         * @member {Uint8Array|null|undefined} details
         * @memberof proto.NoiseCertificate
         * @instance
         */
        NoiseCertificate.prototype.details = null;

        /**
         * NoiseCertificate signature.
         * @member {Uint8Array|null|undefined} signature
         * @memberof proto.NoiseCertificate
         * @instance
         */
        NoiseCertificate.prototype.signature = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NoiseCertificate.prototype, "_details", {
            get: $util.oneOfGetter($oneOfFields = ["details"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NoiseCertificate.prototype, "_signature", {
            get: $util.oneOfGetter($oneOfFields = ["signature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new NoiseCertificate instance using the specified properties.
         * @function create
         * @memberof proto.NoiseCertificate
         * @static
         * @param {proto.INoiseCertificate=} [properties] Properties to set
         * @returns {proto.NoiseCertificate} NoiseCertificate instance
         */
        NoiseCertificate.create = function create(properties) {
            return new NoiseCertificate(properties);
        };

        /**
         * Encodes the specified NoiseCertificate message. Does not implicitly {@link proto.NoiseCertificate.verify|verify} messages.
         * @function encode
         * @memberof proto.NoiseCertificate
         * @static
         * @param {proto.INoiseCertificate} message NoiseCertificate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NoiseCertificate.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.details != null && Object.hasOwnProperty.call(message, "details"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.details);
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.signature);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NoiseCertificate message, length delimited. Does not implicitly {@link proto.NoiseCertificate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.NoiseCertificate
         * @static
         * @param {proto.INoiseCertificate} message NoiseCertificate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NoiseCertificate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NoiseCertificate message from the specified reader or buffer.
         * @function decode
         * @memberof proto.NoiseCertificate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.NoiseCertificate} NoiseCertificate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NoiseCertificate.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.details = reader.bytes();
                        message._details = "details";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.signature = reader.bytes();
                        message._signature = "signature";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a NoiseCertificate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.NoiseCertificate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.NoiseCertificate} NoiseCertificate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NoiseCertificate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NoiseCertificate message.
         * @function verify
         * @memberof proto.NoiseCertificate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NoiseCertificate.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.details != null && Object.hasOwnProperty.call(message, "details")) {
                properties._details = 1;
                if (!(message.details && typeof message.details.length === "number" || $util.isString(message.details)))
                    return "details: buffer expected";
            }
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature")) {
                properties._signature = 1;
                if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                    return "signature: buffer expected";
            }
            return null;
        };

        /**
         * Creates a NoiseCertificate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.NoiseCertificate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.NoiseCertificate} NoiseCertificate
         */
        NoiseCertificate.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.NoiseCertificate: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.details != null)
                if (typeof object.details === "string")
                    $util.base64.decode(object.details, message.details = $util.newBuffer($util.base64.length(object.details)), 0);
                else if (object.details.length >= 0)
                    message.details = object.details;
            if (object.signature != null)
                if (typeof object.signature === "string")
                    $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                else if (object.signature.length >= 0)
                    message.signature = object.signature;
            return message;
        };

        /**
         * Creates a plain object from a NoiseCertificate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.NoiseCertificate
         * @static
         * @param {proto.NoiseCertificate} message NoiseCertificate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NoiseCertificate.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.details != null && Object.hasOwnProperty.call(message, "details"))
                object.details = options.bytes === String ? $util.base64.encode(message.details, 0, message.details.length) : options.bytes === Array ? Array.prototype.slice.call(message.details) : message.details;
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                object.signature = options.bytes === String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === Array ? Array.prototype.slice.call(message.signature) : message.signature;
            return object;
        };

        /**
         * Converts this NoiseCertificate to JSON.
         * @function toJSON
         * @memberof proto.NoiseCertificate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NoiseCertificate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NoiseCertificate
         * @function getTypeUrl
         * @memberof proto.NoiseCertificate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NoiseCertificate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.NoiseCertificate";
        };

        NoiseCertificate.Details = (function() {

            /**
             * Properties of a Details.
             * @memberof proto.NoiseCertificate
             * @interface IDetails
             * @property {number|null} [serial] Details serial
             * @property {string|null} [issuer] Details issuer
             * @property {number|Long|null} [expires] Details expires
             * @property {string|null} [subject] Details subject
             * @property {Uint8Array|null} [key] Details key
             */

            /**
             * Constructs a new Details.
             * @memberof proto.NoiseCertificate
             * @classdesc Represents a Details.
             * @implements IDetails
             * @constructor
             * @param {proto.NoiseCertificate.IDetails=} [properties] Properties to set
             */
            function Details(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Details serial.
             * @member {number|null|undefined} serial
             * @memberof proto.NoiseCertificate.Details
             * @instance
             */
            Details.prototype.serial = null;

            /**
             * Details issuer.
             * @member {string|null|undefined} issuer
             * @memberof proto.NoiseCertificate.Details
             * @instance
             */
            Details.prototype.issuer = null;

            /**
             * Details expires.
             * @member {number|Long|null|undefined} expires
             * @memberof proto.NoiseCertificate.Details
             * @instance
             */
            Details.prototype.expires = null;

            /**
             * Details subject.
             * @member {string|null|undefined} subject
             * @memberof proto.NoiseCertificate.Details
             * @instance
             */
            Details.prototype.subject = null;

            /**
             * Details key.
             * @member {Uint8Array|null|undefined} key
             * @memberof proto.NoiseCertificate.Details
             * @instance
             */
            Details.prototype.key = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Details.prototype, "_serial", {
                get: $util.oneOfGetter($oneOfFields = ["serial"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Details.prototype, "_issuer", {
                get: $util.oneOfGetter($oneOfFields = ["issuer"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Details.prototype, "_expires", {
                get: $util.oneOfGetter($oneOfFields = ["expires"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Details.prototype, "_subject", {
                get: $util.oneOfGetter($oneOfFields = ["subject"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Details.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new Details instance using the specified properties.
             * @function create
             * @memberof proto.NoiseCertificate.Details
             * @static
             * @param {proto.NoiseCertificate.IDetails=} [properties] Properties to set
             * @returns {proto.NoiseCertificate.Details} Details instance
             */
            Details.create = function create(properties) {
                return new Details(properties);
            };

            /**
             * Encodes the specified Details message. Does not implicitly {@link proto.NoiseCertificate.Details.verify|verify} messages.
             * @function encode
             * @memberof proto.NoiseCertificate.Details
             * @static
             * @param {proto.NoiseCertificate.IDetails} message Details message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Details.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.serial != null && Object.hasOwnProperty.call(message, "serial"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.serial);
                if (message.issuer != null && Object.hasOwnProperty.call(message, "issuer"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.issuer);
                if (message.expires != null && Object.hasOwnProperty.call(message, "expires"))
                    writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.expires);
                if (message.subject != null && Object.hasOwnProperty.call(message, "subject"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.subject);
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.key);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Details message, length delimited. Does not implicitly {@link proto.NoiseCertificate.Details.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.NoiseCertificate.Details
             * @static
             * @param {proto.NoiseCertificate.IDetails} message Details message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Details.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a Details message from the specified reader or buffer.
             * @function decode
             * @memberof proto.NoiseCertificate.Details
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.NoiseCertificate.Details} Details
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Details.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.serial = reader.uint32();
                            message._serial = "serial";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.issuer = reader.stringVerify();
                            message._issuer = "issuer";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.expires = reader.uint64();
                            message._expires = "expires";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.subject = reader.stringVerify();
                            message._subject = "subject";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.key = reader.bytes();
                            message._key = "key";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a Details message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.NoiseCertificate.Details
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.NoiseCertificate.Details} Details
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Details.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Details message.
             * @function verify
             * @memberof proto.NoiseCertificate.Details
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Details.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.serial != null && Object.hasOwnProperty.call(message, "serial")) {
                    properties._serial = 1;
                    if (!$util.isInteger(message.serial))
                        return "serial: integer expected";
                }
                if (message.issuer != null && Object.hasOwnProperty.call(message, "issuer")) {
                    properties._issuer = 1;
                    if (!$util.isString(message.issuer))
                        return "issuer: string expected";
                }
                if (message.expires != null && Object.hasOwnProperty.call(message, "expires")) {
                    properties._expires = 1;
                    if (!$util.isInteger(message.expires) && !(message.expires && $util.isInteger(message.expires.low) && $util.isInteger(message.expires.high)))
                        return "expires: integer|Long expected";
                }
                if (message.subject != null && Object.hasOwnProperty.call(message, "subject")) {
                    properties._subject = 1;
                    if (!$util.isString(message.subject))
                        return "subject: string expected";
                }
                if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                    properties._key = 1;
                    if (!(message.key && typeof message.key.length === "number" || $util.isString(message.key)))
                        return "key: buffer expected";
                }
                return null;
            };

            /**
             * Creates a Details message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.NoiseCertificate.Details
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.NoiseCertificate.Details} Details
             */
            Details.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.NoiseCertificate.Details: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.serial != null)
                    message.serial = object.serial >>> 0;
                if (object.issuer != null)
                    message.issuer = String(object.issuer);
                if (object.expires != null)
                    if ($util.Long)
                        message.expires = $util.Long.fromValue(object.expires, true);
                    else if (typeof object.expires === "string")
                        message.expires = parseInt(object.expires, 10);
                    else if (typeof object.expires === "number")
                        message.expires = object.expires;
                    else if (typeof object.expires === "object")
                        message.expires = new $util.LongBits(object.expires.low >>> 0, object.expires.high >>> 0).toNumber(true);
                if (object.subject != null)
                    message.subject = String(object.subject);
                if (object.key != null)
                    if (typeof object.key === "string")
                        $util.base64.decode(object.key, message.key = $util.newBuffer($util.base64.length(object.key)), 0);
                    else if (object.key.length >= 0)
                        message.key = object.key;
                return message;
            };

            /**
             * Creates a plain object from a Details message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.NoiseCertificate.Details
             * @static
             * @param {proto.NoiseCertificate.Details} message Details
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Details.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.serial != null && Object.hasOwnProperty.call(message, "serial"))
                    object.serial = message.serial;
                if (message.issuer != null && Object.hasOwnProperty.call(message, "issuer"))
                    object.issuer = message.issuer;
                if (message.expires != null && Object.hasOwnProperty.call(message, "expires"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.expires = typeof message.expires === "number" ? BigInt(message.expires) : $util.Long.fromBits(message.expires.low >>> 0, message.expires.high >>> 0, true).toBigInt();
                    else if (typeof message.expires === "number")
                        object.expires = options.longs === String ? String(message.expires) : message.expires;
                    else
                        object.expires = options.longs === String ? $util.Long.prototype.toString.call(message.expires) : options.longs === Number ? new $util.LongBits(message.expires.low >>> 0, message.expires.high >>> 0).toNumber(true) : message.expires;
                if (message.subject != null && Object.hasOwnProperty.call(message, "subject"))
                    object.subject = message.subject;
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    object.key = options.bytes === String ? $util.base64.encode(message.key, 0, message.key.length) : options.bytes === Array ? Array.prototype.slice.call(message.key) : message.key;
                return object;
            };

            /**
             * Converts this Details to JSON.
             * @function toJSON
             * @memberof proto.NoiseCertificate.Details
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Details.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Details
             * @function getTypeUrl
             * @memberof proto.NoiseCertificate.Details
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Details.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.NoiseCertificate.Details";
            };

            return Details;
        })();

        return NoiseCertificate;
    })();

    proto.NotificationMessageInfo = (function() {

        /**
         * Properties of a NotificationMessageInfo.
         * @memberof proto
         * @interface INotificationMessageInfo
         * @property {proto.IMessageKey|null} [key] NotificationMessageInfo key
         * @property {proto.IMessage|null} [message] NotificationMessageInfo message
         * @property {number|Long|null} [messageTimestamp] NotificationMessageInfo messageTimestamp
         * @property {string|null} [participant] NotificationMessageInfo participant
         */

        /**
         * Constructs a new NotificationMessageInfo.
         * @memberof proto
         * @classdesc Represents a NotificationMessageInfo.
         * @implements INotificationMessageInfo
         * @constructor
         * @param {proto.INotificationMessageInfo=} [properties] Properties to set
         */
        function NotificationMessageInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * NotificationMessageInfo key.
         * @member {proto.IMessageKey|null|undefined} key
         * @memberof proto.NotificationMessageInfo
         * @instance
         */
        NotificationMessageInfo.prototype.key = null;

        /**
         * NotificationMessageInfo message.
         * @member {proto.IMessage|null|undefined} message
         * @memberof proto.NotificationMessageInfo
         * @instance
         */
        NotificationMessageInfo.prototype.message = null;

        /**
         * NotificationMessageInfo messageTimestamp.
         * @member {number|Long|null|undefined} messageTimestamp
         * @memberof proto.NotificationMessageInfo
         * @instance
         */
        NotificationMessageInfo.prototype.messageTimestamp = null;

        /**
         * NotificationMessageInfo participant.
         * @member {string|null|undefined} participant
         * @memberof proto.NotificationMessageInfo
         * @instance
         */
        NotificationMessageInfo.prototype.participant = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NotificationMessageInfo.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NotificationMessageInfo.prototype, "_message", {
            get: $util.oneOfGetter($oneOfFields = ["message"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NotificationMessageInfo.prototype, "_messageTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["messageTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NotificationMessageInfo.prototype, "_participant", {
            get: $util.oneOfGetter($oneOfFields = ["participant"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new NotificationMessageInfo instance using the specified properties.
         * @function create
         * @memberof proto.NotificationMessageInfo
         * @static
         * @param {proto.INotificationMessageInfo=} [properties] Properties to set
         * @returns {proto.NotificationMessageInfo} NotificationMessageInfo instance
         */
        NotificationMessageInfo.create = function create(properties) {
            return new NotificationMessageInfo(properties);
        };

        /**
         * Encodes the specified NotificationMessageInfo message. Does not implicitly {@link proto.NotificationMessageInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.NotificationMessageInfo
         * @static
         * @param {proto.INotificationMessageInfo} message NotificationMessageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotificationMessageInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                $root.proto.MessageKey.encode(message.key, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                $root.proto.Message.encode(message.message, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.messageTimestamp != null && Object.hasOwnProperty.call(message, "messageTimestamp"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.messageTimestamp);
            if (message.participant != null && Object.hasOwnProperty.call(message, "participant"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.participant);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NotificationMessageInfo message, length delimited. Does not implicitly {@link proto.NotificationMessageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.NotificationMessageInfo
         * @static
         * @param {proto.INotificationMessageInfo} message NotificationMessageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotificationMessageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NotificationMessageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.NotificationMessageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.NotificationMessageInfo} NotificationMessageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotificationMessageInfo.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.key = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.key);
                        message._key = "key";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.message = $root.proto.Message.decode(reader, reader.uint32(), undefined, q + 1, message.message);
                        message._message = "message";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.messageTimestamp = reader.uint64();
                        message._messageTimestamp = "messageTimestamp";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.participant = reader.stringVerify();
                        message._participant = "participant";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a NotificationMessageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.NotificationMessageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.NotificationMessageInfo} NotificationMessageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotificationMessageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NotificationMessageInfo message.
         * @function verify
         * @memberof proto.NotificationMessageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NotificationMessageInfo.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                properties._key = 1;
                {
                    var error = $root.proto.MessageKey.verify(message.key, q + 1);
                    if (error)
                        return "key." + error;
                }
            }
            if (message.message != null && Object.hasOwnProperty.call(message, "message")) {
                properties._message = 1;
                {
                    var error = $root.proto.Message.verify(message.message, q + 1);
                    if (error)
                        return "message." + error;
                }
            }
            if (message.messageTimestamp != null && Object.hasOwnProperty.call(message, "messageTimestamp")) {
                properties._messageTimestamp = 1;
                if (!$util.isInteger(message.messageTimestamp) && !(message.messageTimestamp && $util.isInteger(message.messageTimestamp.low) && $util.isInteger(message.messageTimestamp.high)))
                    return "messageTimestamp: integer|Long expected";
            }
            if (message.participant != null && Object.hasOwnProperty.call(message, "participant")) {
                properties._participant = 1;
                if (!$util.isString(message.participant))
                    return "participant: string expected";
            }
            return null;
        };

        /**
         * Creates a NotificationMessageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.NotificationMessageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.NotificationMessageInfo} NotificationMessageInfo
         */
        NotificationMessageInfo.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.NotificationMessageInfo: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.key != null) {
                if (!$util.isObject(object.key))
                    throw TypeError(".proto.NotificationMessageInfo.key: object expected");
                message.key = $root.proto.MessageKey.fromObject(object.key, q + 1);
            }
            if (object.message != null) {
                if (!$util.isObject(object.message))
                    throw TypeError(".proto.NotificationMessageInfo.message: object expected");
                message.message = $root.proto.Message.fromObject(object.message, q + 1);
            }
            if (object.messageTimestamp != null)
                if ($util.Long)
                    message.messageTimestamp = $util.Long.fromValue(object.messageTimestamp, true);
                else if (typeof object.messageTimestamp === "string")
                    message.messageTimestamp = parseInt(object.messageTimestamp, 10);
                else if (typeof object.messageTimestamp === "number")
                    message.messageTimestamp = object.messageTimestamp;
                else if (typeof object.messageTimestamp === "object")
                    message.messageTimestamp = new $util.LongBits(object.messageTimestamp.low >>> 0, object.messageTimestamp.high >>> 0).toNumber(true);
            if (object.participant != null)
                message.participant = String(object.participant);
            return message;
        };

        /**
         * Creates a plain object from a NotificationMessageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.NotificationMessageInfo
         * @static
         * @param {proto.NotificationMessageInfo} message NotificationMessageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NotificationMessageInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                object.key = $root.proto.MessageKey.toObject(message.key, options, q + 1);
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                object.message = $root.proto.Message.toObject(message.message, options, q + 1);
            if (message.messageTimestamp != null && Object.hasOwnProperty.call(message, "messageTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.messageTimestamp = typeof message.messageTimestamp === "number" ? BigInt(message.messageTimestamp) : $util.Long.fromBits(message.messageTimestamp.low >>> 0, message.messageTimestamp.high >>> 0, true).toBigInt();
                else if (typeof message.messageTimestamp === "number")
                    object.messageTimestamp = options.longs === String ? String(message.messageTimestamp) : message.messageTimestamp;
                else
                    object.messageTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.messageTimestamp) : options.longs === Number ? new $util.LongBits(message.messageTimestamp.low >>> 0, message.messageTimestamp.high >>> 0).toNumber(true) : message.messageTimestamp;
            if (message.participant != null && Object.hasOwnProperty.call(message, "participant"))
                object.participant = message.participant;
            return object;
        };

        /**
         * Converts this NotificationMessageInfo to JSON.
         * @function toJSON
         * @memberof proto.NotificationMessageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NotificationMessageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NotificationMessageInfo
         * @function getTypeUrl
         * @memberof proto.NotificationMessageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NotificationMessageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.NotificationMessageInfo";
        };

        return NotificationMessageInfo;
    })();

    proto.NotificationSettings = (function() {

        /**
         * Properties of a NotificationSettings.
         * @memberof proto
         * @interface INotificationSettings
         * @property {string|null} [messageVibrate] NotificationSettings messageVibrate
         * @property {string|null} [messagePopup] NotificationSettings messagePopup
         * @property {string|null} [messageLight] NotificationSettings messageLight
         * @property {boolean|null} [lowPriorityNotifications] NotificationSettings lowPriorityNotifications
         * @property {boolean|null} [reactionsMuted] NotificationSettings reactionsMuted
         * @property {string|null} [callVibrate] NotificationSettings callVibrate
         */

        /**
         * Constructs a new NotificationSettings.
         * @memberof proto
         * @classdesc Represents a NotificationSettings.
         * @implements INotificationSettings
         * @constructor
         * @param {proto.INotificationSettings=} [properties] Properties to set
         */
        function NotificationSettings(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * NotificationSettings messageVibrate.
         * @member {string|null|undefined} messageVibrate
         * @memberof proto.NotificationSettings
         * @instance
         */
        NotificationSettings.prototype.messageVibrate = null;

        /**
         * NotificationSettings messagePopup.
         * @member {string|null|undefined} messagePopup
         * @memberof proto.NotificationSettings
         * @instance
         */
        NotificationSettings.prototype.messagePopup = null;

        /**
         * NotificationSettings messageLight.
         * @member {string|null|undefined} messageLight
         * @memberof proto.NotificationSettings
         * @instance
         */
        NotificationSettings.prototype.messageLight = null;

        /**
         * NotificationSettings lowPriorityNotifications.
         * @member {boolean|null|undefined} lowPriorityNotifications
         * @memberof proto.NotificationSettings
         * @instance
         */
        NotificationSettings.prototype.lowPriorityNotifications = null;

        /**
         * NotificationSettings reactionsMuted.
         * @member {boolean|null|undefined} reactionsMuted
         * @memberof proto.NotificationSettings
         * @instance
         */
        NotificationSettings.prototype.reactionsMuted = null;

        /**
         * NotificationSettings callVibrate.
         * @member {string|null|undefined} callVibrate
         * @memberof proto.NotificationSettings
         * @instance
         */
        NotificationSettings.prototype.callVibrate = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NotificationSettings.prototype, "_messageVibrate", {
            get: $util.oneOfGetter($oneOfFields = ["messageVibrate"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NotificationSettings.prototype, "_messagePopup", {
            get: $util.oneOfGetter($oneOfFields = ["messagePopup"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NotificationSettings.prototype, "_messageLight", {
            get: $util.oneOfGetter($oneOfFields = ["messageLight"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NotificationSettings.prototype, "_lowPriorityNotifications", {
            get: $util.oneOfGetter($oneOfFields = ["lowPriorityNotifications"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NotificationSettings.prototype, "_reactionsMuted", {
            get: $util.oneOfGetter($oneOfFields = ["reactionsMuted"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NotificationSettings.prototype, "_callVibrate", {
            get: $util.oneOfGetter($oneOfFields = ["callVibrate"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new NotificationSettings instance using the specified properties.
         * @function create
         * @memberof proto.NotificationSettings
         * @static
         * @param {proto.INotificationSettings=} [properties] Properties to set
         * @returns {proto.NotificationSettings} NotificationSettings instance
         */
        NotificationSettings.create = function create(properties) {
            return new NotificationSettings(properties);
        };

        /**
         * Encodes the specified NotificationSettings message. Does not implicitly {@link proto.NotificationSettings.verify|verify} messages.
         * @function encode
         * @memberof proto.NotificationSettings
         * @static
         * @param {proto.INotificationSettings} message NotificationSettings message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotificationSettings.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.messageVibrate != null && Object.hasOwnProperty.call(message, "messageVibrate"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.messageVibrate);
            if (message.messagePopup != null && Object.hasOwnProperty.call(message, "messagePopup"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.messagePopup);
            if (message.messageLight != null && Object.hasOwnProperty.call(message, "messageLight"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.messageLight);
            if (message.lowPriorityNotifications != null && Object.hasOwnProperty.call(message, "lowPriorityNotifications"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.lowPriorityNotifications);
            if (message.reactionsMuted != null && Object.hasOwnProperty.call(message, "reactionsMuted"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.reactionsMuted);
            if (message.callVibrate != null && Object.hasOwnProperty.call(message, "callVibrate"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.callVibrate);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified NotificationSettings message, length delimited. Does not implicitly {@link proto.NotificationSettings.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.NotificationSettings
         * @static
         * @param {proto.INotificationSettings} message NotificationSettings message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotificationSettings.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NotificationSettings message from the specified reader or buffer.
         * @function decode
         * @memberof proto.NotificationSettings
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.NotificationSettings} NotificationSettings
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotificationSettings.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.messageVibrate = reader.stringVerify();
                        message._messageVibrate = "messageVibrate";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.messagePopup = reader.stringVerify();
                        message._messagePopup = "messagePopup";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.messageLight = reader.stringVerify();
                        message._messageLight = "messageLight";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.lowPriorityNotifications = reader.bool();
                        message._lowPriorityNotifications = "lowPriorityNotifications";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.reactionsMuted = reader.bool();
                        message._reactionsMuted = "reactionsMuted";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        message.callVibrate = reader.stringVerify();
                        message._callVibrate = "callVibrate";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a NotificationSettings message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.NotificationSettings
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.NotificationSettings} NotificationSettings
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotificationSettings.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NotificationSettings message.
         * @function verify
         * @memberof proto.NotificationSettings
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NotificationSettings.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.messageVibrate != null && Object.hasOwnProperty.call(message, "messageVibrate")) {
                properties._messageVibrate = 1;
                if (!$util.isString(message.messageVibrate))
                    return "messageVibrate: string expected";
            }
            if (message.messagePopup != null && Object.hasOwnProperty.call(message, "messagePopup")) {
                properties._messagePopup = 1;
                if (!$util.isString(message.messagePopup))
                    return "messagePopup: string expected";
            }
            if (message.messageLight != null && Object.hasOwnProperty.call(message, "messageLight")) {
                properties._messageLight = 1;
                if (!$util.isString(message.messageLight))
                    return "messageLight: string expected";
            }
            if (message.lowPriorityNotifications != null && Object.hasOwnProperty.call(message, "lowPriorityNotifications")) {
                properties._lowPriorityNotifications = 1;
                if (typeof message.lowPriorityNotifications !== "boolean")
                    return "lowPriorityNotifications: boolean expected";
            }
            if (message.reactionsMuted != null && Object.hasOwnProperty.call(message, "reactionsMuted")) {
                properties._reactionsMuted = 1;
                if (typeof message.reactionsMuted !== "boolean")
                    return "reactionsMuted: boolean expected";
            }
            if (message.callVibrate != null && Object.hasOwnProperty.call(message, "callVibrate")) {
                properties._callVibrate = 1;
                if (!$util.isString(message.callVibrate))
                    return "callVibrate: string expected";
            }
            return null;
        };

        /**
         * Creates a NotificationSettings message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.NotificationSettings
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.NotificationSettings} NotificationSettings
         */
        NotificationSettings.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.NotificationSettings: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.messageVibrate != null)
                message.messageVibrate = String(object.messageVibrate);
            if (object.messagePopup != null)
                message.messagePopup = String(object.messagePopup);
            if (object.messageLight != null)
                message.messageLight = String(object.messageLight);
            if (object.lowPriorityNotifications != null)
                message.lowPriorityNotifications = Boolean(object.lowPriorityNotifications);
            if (object.reactionsMuted != null)
                message.reactionsMuted = Boolean(object.reactionsMuted);
            if (object.callVibrate != null)
                message.callVibrate = String(object.callVibrate);
            return message;
        };

        /**
         * Creates a plain object from a NotificationSettings message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.NotificationSettings
         * @static
         * @param {proto.NotificationSettings} message NotificationSettings
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NotificationSettings.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.messageVibrate != null && Object.hasOwnProperty.call(message, "messageVibrate"))
                object.messageVibrate = message.messageVibrate;
            if (message.messagePopup != null && Object.hasOwnProperty.call(message, "messagePopup"))
                object.messagePopup = message.messagePopup;
            if (message.messageLight != null && Object.hasOwnProperty.call(message, "messageLight"))
                object.messageLight = message.messageLight;
            if (message.lowPriorityNotifications != null && Object.hasOwnProperty.call(message, "lowPriorityNotifications"))
                object.lowPriorityNotifications = message.lowPriorityNotifications;
            if (message.reactionsMuted != null && Object.hasOwnProperty.call(message, "reactionsMuted"))
                object.reactionsMuted = message.reactionsMuted;
            if (message.callVibrate != null && Object.hasOwnProperty.call(message, "callVibrate"))
                object.callVibrate = message.callVibrate;
            return object;
        };

        /**
         * Converts this NotificationSettings to JSON.
         * @function toJSON
         * @memberof proto.NotificationSettings
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NotificationSettings.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NotificationSettings
         * @function getTypeUrl
         * @memberof proto.NotificationSettings
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NotificationSettings.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.NotificationSettings";
        };

        return NotificationSettings;
    })();

    proto.PairingRequest = (function() {

        /**
         * Properties of a PairingRequest.
         * @memberof proto
         * @interface IPairingRequest
         * @property {Uint8Array|null} [companionPublicKey] PairingRequest companionPublicKey
         * @property {Uint8Array|null} [companionIdentityKey] PairingRequest companionIdentityKey
         * @property {Uint8Array|null} [advSecret] PairingRequest advSecret
         */

        /**
         * Constructs a new PairingRequest.
         * @memberof proto
         * @classdesc Represents a PairingRequest.
         * @implements IPairingRequest
         * @constructor
         * @param {proto.IPairingRequest=} [properties] Properties to set
         */
        function PairingRequest(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PairingRequest companionPublicKey.
         * @member {Uint8Array|null|undefined} companionPublicKey
         * @memberof proto.PairingRequest
         * @instance
         */
        PairingRequest.prototype.companionPublicKey = null;

        /**
         * PairingRequest companionIdentityKey.
         * @member {Uint8Array|null|undefined} companionIdentityKey
         * @memberof proto.PairingRequest
         * @instance
         */
        PairingRequest.prototype.companionIdentityKey = null;

        /**
         * PairingRequest advSecret.
         * @member {Uint8Array|null|undefined} advSecret
         * @memberof proto.PairingRequest
         * @instance
         */
        PairingRequest.prototype.advSecret = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PairingRequest.prototype, "_companionPublicKey", {
            get: $util.oneOfGetter($oneOfFields = ["companionPublicKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PairingRequest.prototype, "_companionIdentityKey", {
            get: $util.oneOfGetter($oneOfFields = ["companionIdentityKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PairingRequest.prototype, "_advSecret", {
            get: $util.oneOfGetter($oneOfFields = ["advSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PairingRequest instance using the specified properties.
         * @function create
         * @memberof proto.PairingRequest
         * @static
         * @param {proto.IPairingRequest=} [properties] Properties to set
         * @returns {proto.PairingRequest} PairingRequest instance
         */
        PairingRequest.create = function create(properties) {
            return new PairingRequest(properties);
        };

        /**
         * Encodes the specified PairingRequest message. Does not implicitly {@link proto.PairingRequest.verify|verify} messages.
         * @function encode
         * @memberof proto.PairingRequest
         * @static
         * @param {proto.IPairingRequest} message PairingRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PairingRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.companionPublicKey != null && Object.hasOwnProperty.call(message, "companionPublicKey"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.companionPublicKey);
            if (message.companionIdentityKey != null && Object.hasOwnProperty.call(message, "companionIdentityKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.companionIdentityKey);
            if (message.advSecret != null && Object.hasOwnProperty.call(message, "advSecret"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.advSecret);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PairingRequest message, length delimited. Does not implicitly {@link proto.PairingRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.PairingRequest
         * @static
         * @param {proto.IPairingRequest} message PairingRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PairingRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PairingRequest message from the specified reader or buffer.
         * @function decode
         * @memberof proto.PairingRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.PairingRequest} PairingRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PairingRequest.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.companionPublicKey = reader.bytes();
                        message._companionPublicKey = "companionPublicKey";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.companionIdentityKey = reader.bytes();
                        message._companionIdentityKey = "companionIdentityKey";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.advSecret = reader.bytes();
                        message._advSecret = "advSecret";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a PairingRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.PairingRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.PairingRequest} PairingRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PairingRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PairingRequest message.
         * @function verify
         * @memberof proto.PairingRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PairingRequest.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.companionPublicKey != null && Object.hasOwnProperty.call(message, "companionPublicKey")) {
                properties._companionPublicKey = 1;
                if (!(message.companionPublicKey && typeof message.companionPublicKey.length === "number" || $util.isString(message.companionPublicKey)))
                    return "companionPublicKey: buffer expected";
            }
            if (message.companionIdentityKey != null && Object.hasOwnProperty.call(message, "companionIdentityKey")) {
                properties._companionIdentityKey = 1;
                if (!(message.companionIdentityKey && typeof message.companionIdentityKey.length === "number" || $util.isString(message.companionIdentityKey)))
                    return "companionIdentityKey: buffer expected";
            }
            if (message.advSecret != null && Object.hasOwnProperty.call(message, "advSecret")) {
                properties._advSecret = 1;
                if (!(message.advSecret && typeof message.advSecret.length === "number" || $util.isString(message.advSecret)))
                    return "advSecret: buffer expected";
            }
            return null;
        };

        /**
         * Creates a PairingRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.PairingRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.PairingRequest} PairingRequest
         */
        PairingRequest.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.PairingRequest: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.companionPublicKey != null)
                if (typeof object.companionPublicKey === "string")
                    $util.base64.decode(object.companionPublicKey, message.companionPublicKey = $util.newBuffer($util.base64.length(object.companionPublicKey)), 0);
                else if (object.companionPublicKey.length >= 0)
                    message.companionPublicKey = object.companionPublicKey;
            if (object.companionIdentityKey != null)
                if (typeof object.companionIdentityKey === "string")
                    $util.base64.decode(object.companionIdentityKey, message.companionIdentityKey = $util.newBuffer($util.base64.length(object.companionIdentityKey)), 0);
                else if (object.companionIdentityKey.length >= 0)
                    message.companionIdentityKey = object.companionIdentityKey;
            if (object.advSecret != null)
                if (typeof object.advSecret === "string")
                    $util.base64.decode(object.advSecret, message.advSecret = $util.newBuffer($util.base64.length(object.advSecret)), 0);
                else if (object.advSecret.length >= 0)
                    message.advSecret = object.advSecret;
            return message;
        };

        /**
         * Creates a plain object from a PairingRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.PairingRequest
         * @static
         * @param {proto.PairingRequest} message PairingRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PairingRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.companionPublicKey != null && Object.hasOwnProperty.call(message, "companionPublicKey"))
                object.companionPublicKey = options.bytes === String ? $util.base64.encode(message.companionPublicKey, 0, message.companionPublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.companionPublicKey) : message.companionPublicKey;
            if (message.companionIdentityKey != null && Object.hasOwnProperty.call(message, "companionIdentityKey"))
                object.companionIdentityKey = options.bytes === String ? $util.base64.encode(message.companionIdentityKey, 0, message.companionIdentityKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.companionIdentityKey) : message.companionIdentityKey;
            if (message.advSecret != null && Object.hasOwnProperty.call(message, "advSecret"))
                object.advSecret = options.bytes === String ? $util.base64.encode(message.advSecret, 0, message.advSecret.length) : options.bytes === Array ? Array.prototype.slice.call(message.advSecret) : message.advSecret;
            return object;
        };

        /**
         * Converts this PairingRequest to JSON.
         * @function toJSON
         * @memberof proto.PairingRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PairingRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PairingRequest
         * @function getTypeUrl
         * @memberof proto.PairingRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PairingRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.PairingRequest";
        };

        return PairingRequest;
    })();

    proto.PastParticipant = (function() {

        /**
         * Properties of a PastParticipant.
         * @memberof proto
         * @interface IPastParticipant
         * @property {string|null} [userJid] PastParticipant userJid
         * @property {proto.PastParticipant.LeaveReason|null} [leaveReason] PastParticipant leaveReason
         * @property {number|Long|null} [leaveTs] PastParticipant leaveTs
         */

        /**
         * Constructs a new PastParticipant.
         * @memberof proto
         * @classdesc Represents a PastParticipant.
         * @implements IPastParticipant
         * @constructor
         * @param {proto.IPastParticipant=} [properties] Properties to set
         */
        function PastParticipant(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PastParticipant userJid.
         * @member {string|null|undefined} userJid
         * @memberof proto.PastParticipant
         * @instance
         */
        PastParticipant.prototype.userJid = null;

        /**
         * PastParticipant leaveReason.
         * @member {proto.PastParticipant.LeaveReason|null|undefined} leaveReason
         * @memberof proto.PastParticipant
         * @instance
         */
        PastParticipant.prototype.leaveReason = null;

        /**
         * PastParticipant leaveTs.
         * @member {number|Long|null|undefined} leaveTs
         * @memberof proto.PastParticipant
         * @instance
         */
        PastParticipant.prototype.leaveTs = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PastParticipant.prototype, "_userJid", {
            get: $util.oneOfGetter($oneOfFields = ["userJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PastParticipant.prototype, "_leaveReason", {
            get: $util.oneOfGetter($oneOfFields = ["leaveReason"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PastParticipant.prototype, "_leaveTs", {
            get: $util.oneOfGetter($oneOfFields = ["leaveTs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PastParticipant instance using the specified properties.
         * @function create
         * @memberof proto.PastParticipant
         * @static
         * @param {proto.IPastParticipant=} [properties] Properties to set
         * @returns {proto.PastParticipant} PastParticipant instance
         */
        PastParticipant.create = function create(properties) {
            return new PastParticipant(properties);
        };

        /**
         * Encodes the specified PastParticipant message. Does not implicitly {@link proto.PastParticipant.verify|verify} messages.
         * @function encode
         * @memberof proto.PastParticipant
         * @static
         * @param {proto.IPastParticipant} message PastParticipant message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PastParticipant.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.userJid != null && Object.hasOwnProperty.call(message, "userJid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.userJid);
            if (message.leaveReason != null && Object.hasOwnProperty.call(message, "leaveReason"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.leaveReason);
            if (message.leaveTs != null && Object.hasOwnProperty.call(message, "leaveTs"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.leaveTs);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PastParticipant message, length delimited. Does not implicitly {@link proto.PastParticipant.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.PastParticipant
         * @static
         * @param {proto.IPastParticipant} message PastParticipant message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PastParticipant.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PastParticipant message from the specified reader or buffer.
         * @function decode
         * @memberof proto.PastParticipant
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.PastParticipant} PastParticipant
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PastParticipant.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.userJid = reader.stringVerify();
                        message._userJid = "userJid";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.leaveReason = reader.int32();
                        message._leaveReason = "leaveReason";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.leaveTs = reader.uint64();
                        message._leaveTs = "leaveTs";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a PastParticipant message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.PastParticipant
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.PastParticipant} PastParticipant
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PastParticipant.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PastParticipant message.
         * @function verify
         * @memberof proto.PastParticipant
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PastParticipant.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.userJid != null && Object.hasOwnProperty.call(message, "userJid")) {
                properties._userJid = 1;
                if (!$util.isString(message.userJid))
                    return "userJid: string expected";
            }
            if (message.leaveReason != null && Object.hasOwnProperty.call(message, "leaveReason")) {
                properties._leaveReason = 1;
                if (typeof message.leaveReason !== "number" || (message.leaveReason | 0) !== message.leaveReason)
                    return "leaveReason: enum value expected";
            }
            if (message.leaveTs != null && Object.hasOwnProperty.call(message, "leaveTs")) {
                properties._leaveTs = 1;
                if (!$util.isInteger(message.leaveTs) && !(message.leaveTs && $util.isInteger(message.leaveTs.low) && $util.isInteger(message.leaveTs.high)))
                    return "leaveTs: integer|Long expected";
            }
            return null;
        };

        /**
         * Creates a PastParticipant message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.PastParticipant
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.PastParticipant} PastParticipant
         */
        PastParticipant.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.PastParticipant: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.userJid != null)
                message.userJid = String(object.userJid);
            switch (object.leaveReason) {
            case "LEFT":
            case 0:
                message.leaveReason = 0;
                break;
            case "REMOVED":
            case 1:
                message.leaveReason = 1;
                break;
            default:
                if (typeof object.leaveReason === "number" && (object.leaveReason | 0) === object.leaveReason)
                    message.leaveReason = object.leaveReason;
            }
            if (object.leaveTs != null)
                if ($util.Long)
                    message.leaveTs = $util.Long.fromValue(object.leaveTs, true);
                else if (typeof object.leaveTs === "string")
                    message.leaveTs = parseInt(object.leaveTs, 10);
                else if (typeof object.leaveTs === "number")
                    message.leaveTs = object.leaveTs;
                else if (typeof object.leaveTs === "object")
                    message.leaveTs = new $util.LongBits(object.leaveTs.low >>> 0, object.leaveTs.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a PastParticipant message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.PastParticipant
         * @static
         * @param {proto.PastParticipant} message PastParticipant
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PastParticipant.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.userJid != null && Object.hasOwnProperty.call(message, "userJid"))
                object.userJid = message.userJid;
            if (message.leaveReason != null && Object.hasOwnProperty.call(message, "leaveReason"))
                object.leaveReason = options.enums === String ? $root.proto.PastParticipant.LeaveReason[message.leaveReason] === undefined ? message.leaveReason : $root.proto.PastParticipant.LeaveReason[message.leaveReason] : message.leaveReason;
            if (message.leaveTs != null && Object.hasOwnProperty.call(message, "leaveTs"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.leaveTs = typeof message.leaveTs === "number" ? BigInt(message.leaveTs) : $util.Long.fromBits(message.leaveTs.low >>> 0, message.leaveTs.high >>> 0, true).toBigInt();
                else if (typeof message.leaveTs === "number")
                    object.leaveTs = options.longs === String ? String(message.leaveTs) : message.leaveTs;
                else
                    object.leaveTs = options.longs === String ? $util.Long.prototype.toString.call(message.leaveTs) : options.longs === Number ? new $util.LongBits(message.leaveTs.low >>> 0, message.leaveTs.high >>> 0).toNumber(true) : message.leaveTs;
            return object;
        };

        /**
         * Converts this PastParticipant to JSON.
         * @function toJSON
         * @memberof proto.PastParticipant
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PastParticipant.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PastParticipant
         * @function getTypeUrl
         * @memberof proto.PastParticipant
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PastParticipant.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.PastParticipant";
        };

        /**
         * LeaveReason enum.
         * @name proto.PastParticipant.LeaveReason
         * @enum {number}
         * @property {number} LEFT=0 LEFT value
         * @property {number} REMOVED=1 REMOVED value
         */
        PastParticipant.LeaveReason = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "LEFT"] = 0;
            values[valuesById[1] = "REMOVED"] = 1;
            return values;
        })();

        return PastParticipant;
    })();

    proto.PastParticipants = (function() {

        /**
         * Properties of a PastParticipants.
         * @memberof proto
         * @interface IPastParticipants
         * @property {string|null} [groupJid] PastParticipants groupJid
         * @property {Array.<proto.IPastParticipant>|null} [pastParticipants] PastParticipants pastParticipants
         */

        /**
         * Constructs a new PastParticipants.
         * @memberof proto
         * @classdesc Represents a PastParticipants.
         * @implements IPastParticipants
         * @constructor
         * @param {proto.IPastParticipants=} [properties] Properties to set
         */
        function PastParticipants(properties) {
            this.pastParticipants = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PastParticipants groupJid.
         * @member {string|null|undefined} groupJid
         * @memberof proto.PastParticipants
         * @instance
         */
        PastParticipants.prototype.groupJid = null;

        /**
         * PastParticipants pastParticipants.
         * @member {Array.<proto.IPastParticipant>} pastParticipants
         * @memberof proto.PastParticipants
         * @instance
         */
        PastParticipants.prototype.pastParticipants = $util.emptyArray;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PastParticipants.prototype, "_groupJid", {
            get: $util.oneOfGetter($oneOfFields = ["groupJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PastParticipants instance using the specified properties.
         * @function create
         * @memberof proto.PastParticipants
         * @static
         * @param {proto.IPastParticipants=} [properties] Properties to set
         * @returns {proto.PastParticipants} PastParticipants instance
         */
        PastParticipants.create = function create(properties) {
            return new PastParticipants(properties);
        };

        /**
         * Encodes the specified PastParticipants message. Does not implicitly {@link proto.PastParticipants.verify|verify} messages.
         * @function encode
         * @memberof proto.PastParticipants
         * @static
         * @param {proto.IPastParticipants} message PastParticipants message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PastParticipants.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.groupJid != null && Object.hasOwnProperty.call(message, "groupJid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.groupJid);
            if (message.pastParticipants != null && message.pastParticipants.length)
                for (var i = 0; i < message.pastParticipants.length; ++i)
                    $root.proto.PastParticipant.encode(message.pastParticipants[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PastParticipants message, length delimited. Does not implicitly {@link proto.PastParticipants.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.PastParticipants
         * @static
         * @param {proto.IPastParticipants} message PastParticipants message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PastParticipants.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PastParticipants message from the specified reader or buffer.
         * @function decode
         * @memberof proto.PastParticipants
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.PastParticipants} PastParticipants
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PastParticipants.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.groupJid = reader.stringVerify();
                        message._groupJid = "groupJid";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(message.pastParticipants && message.pastParticipants.length))
                            message.pastParticipants = [];
                        message.pastParticipants.push($root.proto.PastParticipant.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a PastParticipants message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.PastParticipants
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.PastParticipants} PastParticipants
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PastParticipants.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PastParticipants message.
         * @function verify
         * @memberof proto.PastParticipants
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PastParticipants.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.groupJid != null && Object.hasOwnProperty.call(message, "groupJid")) {
                properties._groupJid = 1;
                if (!$util.isString(message.groupJid))
                    return "groupJid: string expected";
            }
            if (message.pastParticipants != null && Object.hasOwnProperty.call(message, "pastParticipants")) {
                if (!Array.isArray(message.pastParticipants))
                    return "pastParticipants: array expected";
                for (var i = 0; i < message.pastParticipants.length; ++i) {
                    var error = $root.proto.PastParticipant.verify(message.pastParticipants[i], q + 1);
                    if (error)
                        return "pastParticipants." + error;
                }
            }
            return null;
        };

        /**
         * Creates a PastParticipants message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.PastParticipants
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.PastParticipants} PastParticipants
         */
        PastParticipants.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.PastParticipants: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.groupJid != null)
                message.groupJid = String(object.groupJid);
            if (object.pastParticipants) {
                if (!Array.isArray(object.pastParticipants))
                    throw TypeError(".proto.PastParticipants.pastParticipants: array expected");
                message.pastParticipants = Array(object.pastParticipants.length);
                for (var i = 0; i < object.pastParticipants.length; ++i) {
                    if (!$util.isObject(object.pastParticipants[i]))
                        throw TypeError(".proto.PastParticipants.pastParticipants: object expected");
                    message.pastParticipants[i] = $root.proto.PastParticipant.fromObject(object.pastParticipants[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a PastParticipants message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.PastParticipants
         * @static
         * @param {proto.PastParticipants} message PastParticipants
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PastParticipants.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.pastParticipants = [];
            if (message.groupJid != null && Object.hasOwnProperty.call(message, "groupJid"))
                object.groupJid = message.groupJid;
            if (message.pastParticipants && message.pastParticipants.length) {
                object.pastParticipants = Array(message.pastParticipants.length);
                for (var j = 0; j < message.pastParticipants.length; ++j)
                    object.pastParticipants[j] = $root.proto.PastParticipant.toObject(message.pastParticipants[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this PastParticipants to JSON.
         * @function toJSON
         * @memberof proto.PastParticipants
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PastParticipants.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PastParticipants
         * @function getTypeUrl
         * @memberof proto.PastParticipants
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PastParticipants.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.PastParticipants";
        };

        return PastParticipants;
    })();

    proto.PatchDebugData = (function() {

        /**
         * Properties of a PatchDebugData.
         * @memberof proto
         * @interface IPatchDebugData
         * @property {Uint8Array|null} [currentLthash] PatchDebugData currentLthash
         * @property {Uint8Array|null} [newLthash] PatchDebugData newLthash
         * @property {Uint8Array|null} [patchVersion] PatchDebugData patchVersion
         * @property {Uint8Array|null} [collectionName] PatchDebugData collectionName
         * @property {Uint8Array|null} [firstFourBytesFromAHashOfSnapshotMacKey] PatchDebugData firstFourBytesFromAHashOfSnapshotMacKey
         * @property {Uint8Array|null} [newLthashSubtract] PatchDebugData newLthashSubtract
         * @property {number|null} [numberAdd] PatchDebugData numberAdd
         * @property {number|null} [numberRemove] PatchDebugData numberRemove
         * @property {number|null} [numberOverride] PatchDebugData numberOverride
         * @property {proto.PatchDebugData.Platform|null} [senderPlatform] PatchDebugData senderPlatform
         * @property {boolean|null} [isSenderPrimary] PatchDebugData isSenderPrimary
         */

        /**
         * Constructs a new PatchDebugData.
         * @memberof proto
         * @classdesc Represents a PatchDebugData.
         * @implements IPatchDebugData
         * @constructor
         * @param {proto.IPatchDebugData=} [properties] Properties to set
         */
        function PatchDebugData(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PatchDebugData currentLthash.
         * @member {Uint8Array|null|undefined} currentLthash
         * @memberof proto.PatchDebugData
         * @instance
         */
        PatchDebugData.prototype.currentLthash = null;

        /**
         * PatchDebugData newLthash.
         * @member {Uint8Array|null|undefined} newLthash
         * @memberof proto.PatchDebugData
         * @instance
         */
        PatchDebugData.prototype.newLthash = null;

        /**
         * PatchDebugData patchVersion.
         * @member {Uint8Array|null|undefined} patchVersion
         * @memberof proto.PatchDebugData
         * @instance
         */
        PatchDebugData.prototype.patchVersion = null;

        /**
         * PatchDebugData collectionName.
         * @member {Uint8Array|null|undefined} collectionName
         * @memberof proto.PatchDebugData
         * @instance
         */
        PatchDebugData.prototype.collectionName = null;

        /**
         * PatchDebugData firstFourBytesFromAHashOfSnapshotMacKey.
         * @member {Uint8Array|null|undefined} firstFourBytesFromAHashOfSnapshotMacKey
         * @memberof proto.PatchDebugData
         * @instance
         */
        PatchDebugData.prototype.firstFourBytesFromAHashOfSnapshotMacKey = null;

        /**
         * PatchDebugData newLthashSubtract.
         * @member {Uint8Array|null|undefined} newLthashSubtract
         * @memberof proto.PatchDebugData
         * @instance
         */
        PatchDebugData.prototype.newLthashSubtract = null;

        /**
         * PatchDebugData numberAdd.
         * @member {number|null|undefined} numberAdd
         * @memberof proto.PatchDebugData
         * @instance
         */
        PatchDebugData.prototype.numberAdd = null;

        /**
         * PatchDebugData numberRemove.
         * @member {number|null|undefined} numberRemove
         * @memberof proto.PatchDebugData
         * @instance
         */
        PatchDebugData.prototype.numberRemove = null;

        /**
         * PatchDebugData numberOverride.
         * @member {number|null|undefined} numberOverride
         * @memberof proto.PatchDebugData
         * @instance
         */
        PatchDebugData.prototype.numberOverride = null;

        /**
         * PatchDebugData senderPlatform.
         * @member {proto.PatchDebugData.Platform|null|undefined} senderPlatform
         * @memberof proto.PatchDebugData
         * @instance
         */
        PatchDebugData.prototype.senderPlatform = null;

        /**
         * PatchDebugData isSenderPrimary.
         * @member {boolean|null|undefined} isSenderPrimary
         * @memberof proto.PatchDebugData
         * @instance
         */
        PatchDebugData.prototype.isSenderPrimary = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PatchDebugData.prototype, "_currentLthash", {
            get: $util.oneOfGetter($oneOfFields = ["currentLthash"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PatchDebugData.prototype, "_newLthash", {
            get: $util.oneOfGetter($oneOfFields = ["newLthash"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PatchDebugData.prototype, "_patchVersion", {
            get: $util.oneOfGetter($oneOfFields = ["patchVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PatchDebugData.prototype, "_collectionName", {
            get: $util.oneOfGetter($oneOfFields = ["collectionName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PatchDebugData.prototype, "_firstFourBytesFromAHashOfSnapshotMacKey", {
            get: $util.oneOfGetter($oneOfFields = ["firstFourBytesFromAHashOfSnapshotMacKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PatchDebugData.prototype, "_newLthashSubtract", {
            get: $util.oneOfGetter($oneOfFields = ["newLthashSubtract"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PatchDebugData.prototype, "_numberAdd", {
            get: $util.oneOfGetter($oneOfFields = ["numberAdd"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PatchDebugData.prototype, "_numberRemove", {
            get: $util.oneOfGetter($oneOfFields = ["numberRemove"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PatchDebugData.prototype, "_numberOverride", {
            get: $util.oneOfGetter($oneOfFields = ["numberOverride"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PatchDebugData.prototype, "_senderPlatform", {
            get: $util.oneOfGetter($oneOfFields = ["senderPlatform"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PatchDebugData.prototype, "_isSenderPrimary", {
            get: $util.oneOfGetter($oneOfFields = ["isSenderPrimary"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PatchDebugData instance using the specified properties.
         * @function create
         * @memberof proto.PatchDebugData
         * @static
         * @param {proto.IPatchDebugData=} [properties] Properties to set
         * @returns {proto.PatchDebugData} PatchDebugData instance
         */
        PatchDebugData.create = function create(properties) {
            return new PatchDebugData(properties);
        };

        /**
         * Encodes the specified PatchDebugData message. Does not implicitly {@link proto.PatchDebugData.verify|verify} messages.
         * @function encode
         * @memberof proto.PatchDebugData
         * @static
         * @param {proto.IPatchDebugData} message PatchDebugData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PatchDebugData.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.currentLthash != null && Object.hasOwnProperty.call(message, "currentLthash"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.currentLthash);
            if (message.newLthash != null && Object.hasOwnProperty.call(message, "newLthash"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.newLthash);
            if (message.patchVersion != null && Object.hasOwnProperty.call(message, "patchVersion"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.patchVersion);
            if (message.collectionName != null && Object.hasOwnProperty.call(message, "collectionName"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.collectionName);
            if (message.firstFourBytesFromAHashOfSnapshotMacKey != null && Object.hasOwnProperty.call(message, "firstFourBytesFromAHashOfSnapshotMacKey"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.firstFourBytesFromAHashOfSnapshotMacKey);
            if (message.newLthashSubtract != null && Object.hasOwnProperty.call(message, "newLthashSubtract"))
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.newLthashSubtract);
            if (message.numberAdd != null && Object.hasOwnProperty.call(message, "numberAdd"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.numberAdd);
            if (message.numberRemove != null && Object.hasOwnProperty.call(message, "numberRemove"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.numberRemove);
            if (message.numberOverride != null && Object.hasOwnProperty.call(message, "numberOverride"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.numberOverride);
            if (message.senderPlatform != null && Object.hasOwnProperty.call(message, "senderPlatform"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.senderPlatform);
            if (message.isSenderPrimary != null && Object.hasOwnProperty.call(message, "isSenderPrimary"))
                writer.uint32(/* id 11, wireType 0 =*/88).bool(message.isSenderPrimary);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PatchDebugData message, length delimited. Does not implicitly {@link proto.PatchDebugData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.PatchDebugData
         * @static
         * @param {proto.IPatchDebugData} message PatchDebugData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PatchDebugData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PatchDebugData message from the specified reader or buffer.
         * @function decode
         * @memberof proto.PatchDebugData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.PatchDebugData} PatchDebugData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PatchDebugData.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.currentLthash = reader.bytes();
                        message._currentLthash = "currentLthash";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.newLthash = reader.bytes();
                        message._newLthash = "newLthash";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.patchVersion = reader.bytes();
                        message._patchVersion = "patchVersion";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.collectionName = reader.bytes();
                        message._collectionName = "collectionName";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.firstFourBytesFromAHashOfSnapshotMacKey = reader.bytes();
                        message._firstFourBytesFromAHashOfSnapshotMacKey = "firstFourBytesFromAHashOfSnapshotMacKey";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        message.newLthashSubtract = reader.bytes();
                        message._newLthashSubtract = "newLthashSubtract";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        message.numberAdd = reader.int32();
                        message._numberAdd = "numberAdd";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        message.numberRemove = reader.int32();
                        message._numberRemove = "numberRemove";
                        continue;
                    }
                case 9: {
                        if (u !== 0)
                            break;
                        message.numberOverride = reader.int32();
                        message._numberOverride = "numberOverride";
                        continue;
                    }
                case 10: {
                        if (u !== 0)
                            break;
                        message.senderPlatform = reader.int32();
                        message._senderPlatform = "senderPlatform";
                        continue;
                    }
                case 11: {
                        if (u !== 0)
                            break;
                        message.isSenderPrimary = reader.bool();
                        message._isSenderPrimary = "isSenderPrimary";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a PatchDebugData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.PatchDebugData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.PatchDebugData} PatchDebugData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PatchDebugData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PatchDebugData message.
         * @function verify
         * @memberof proto.PatchDebugData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PatchDebugData.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.currentLthash != null && Object.hasOwnProperty.call(message, "currentLthash")) {
                properties._currentLthash = 1;
                if (!(message.currentLthash && typeof message.currentLthash.length === "number" || $util.isString(message.currentLthash)))
                    return "currentLthash: buffer expected";
            }
            if (message.newLthash != null && Object.hasOwnProperty.call(message, "newLthash")) {
                properties._newLthash = 1;
                if (!(message.newLthash && typeof message.newLthash.length === "number" || $util.isString(message.newLthash)))
                    return "newLthash: buffer expected";
            }
            if (message.patchVersion != null && Object.hasOwnProperty.call(message, "patchVersion")) {
                properties._patchVersion = 1;
                if (!(message.patchVersion && typeof message.patchVersion.length === "number" || $util.isString(message.patchVersion)))
                    return "patchVersion: buffer expected";
            }
            if (message.collectionName != null && Object.hasOwnProperty.call(message, "collectionName")) {
                properties._collectionName = 1;
                if (!(message.collectionName && typeof message.collectionName.length === "number" || $util.isString(message.collectionName)))
                    return "collectionName: buffer expected";
            }
            if (message.firstFourBytesFromAHashOfSnapshotMacKey != null && Object.hasOwnProperty.call(message, "firstFourBytesFromAHashOfSnapshotMacKey")) {
                properties._firstFourBytesFromAHashOfSnapshotMacKey = 1;
                if (!(message.firstFourBytesFromAHashOfSnapshotMacKey && typeof message.firstFourBytesFromAHashOfSnapshotMacKey.length === "number" || $util.isString(message.firstFourBytesFromAHashOfSnapshotMacKey)))
                    return "firstFourBytesFromAHashOfSnapshotMacKey: buffer expected";
            }
            if (message.newLthashSubtract != null && Object.hasOwnProperty.call(message, "newLthashSubtract")) {
                properties._newLthashSubtract = 1;
                if (!(message.newLthashSubtract && typeof message.newLthashSubtract.length === "number" || $util.isString(message.newLthashSubtract)))
                    return "newLthashSubtract: buffer expected";
            }
            if (message.numberAdd != null && Object.hasOwnProperty.call(message, "numberAdd")) {
                properties._numberAdd = 1;
                if (!$util.isInteger(message.numberAdd))
                    return "numberAdd: integer expected";
            }
            if (message.numberRemove != null && Object.hasOwnProperty.call(message, "numberRemove")) {
                properties._numberRemove = 1;
                if (!$util.isInteger(message.numberRemove))
                    return "numberRemove: integer expected";
            }
            if (message.numberOverride != null && Object.hasOwnProperty.call(message, "numberOverride")) {
                properties._numberOverride = 1;
                if (!$util.isInteger(message.numberOverride))
                    return "numberOverride: integer expected";
            }
            if (message.senderPlatform != null && Object.hasOwnProperty.call(message, "senderPlatform")) {
                properties._senderPlatform = 1;
                if (typeof message.senderPlatform !== "number" || (message.senderPlatform | 0) !== message.senderPlatform)
                    return "senderPlatform: enum value expected";
            }
            if (message.isSenderPrimary != null && Object.hasOwnProperty.call(message, "isSenderPrimary")) {
                properties._isSenderPrimary = 1;
                if (typeof message.isSenderPrimary !== "boolean")
                    return "isSenderPrimary: boolean expected";
            }
            return null;
        };

        /**
         * Creates a PatchDebugData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.PatchDebugData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.PatchDebugData} PatchDebugData
         */
        PatchDebugData.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.PatchDebugData: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.currentLthash != null)
                if (typeof object.currentLthash === "string")
                    $util.base64.decode(object.currentLthash, message.currentLthash = $util.newBuffer($util.base64.length(object.currentLthash)), 0);
                else if (object.currentLthash.length >= 0)
                    message.currentLthash = object.currentLthash;
            if (object.newLthash != null)
                if (typeof object.newLthash === "string")
                    $util.base64.decode(object.newLthash, message.newLthash = $util.newBuffer($util.base64.length(object.newLthash)), 0);
                else if (object.newLthash.length >= 0)
                    message.newLthash = object.newLthash;
            if (object.patchVersion != null)
                if (typeof object.patchVersion === "string")
                    $util.base64.decode(object.patchVersion, message.patchVersion = $util.newBuffer($util.base64.length(object.patchVersion)), 0);
                else if (object.patchVersion.length >= 0)
                    message.patchVersion = object.patchVersion;
            if (object.collectionName != null)
                if (typeof object.collectionName === "string")
                    $util.base64.decode(object.collectionName, message.collectionName = $util.newBuffer($util.base64.length(object.collectionName)), 0);
                else if (object.collectionName.length >= 0)
                    message.collectionName = object.collectionName;
            if (object.firstFourBytesFromAHashOfSnapshotMacKey != null)
                if (typeof object.firstFourBytesFromAHashOfSnapshotMacKey === "string")
                    $util.base64.decode(object.firstFourBytesFromAHashOfSnapshotMacKey, message.firstFourBytesFromAHashOfSnapshotMacKey = $util.newBuffer($util.base64.length(object.firstFourBytesFromAHashOfSnapshotMacKey)), 0);
                else if (object.firstFourBytesFromAHashOfSnapshotMacKey.length >= 0)
                    message.firstFourBytesFromAHashOfSnapshotMacKey = object.firstFourBytesFromAHashOfSnapshotMacKey;
            if (object.newLthashSubtract != null)
                if (typeof object.newLthashSubtract === "string")
                    $util.base64.decode(object.newLthashSubtract, message.newLthashSubtract = $util.newBuffer($util.base64.length(object.newLthashSubtract)), 0);
                else if (object.newLthashSubtract.length >= 0)
                    message.newLthashSubtract = object.newLthashSubtract;
            if (object.numberAdd != null)
                message.numberAdd = object.numberAdd | 0;
            if (object.numberRemove != null)
                message.numberRemove = object.numberRemove | 0;
            if (object.numberOverride != null)
                message.numberOverride = object.numberOverride | 0;
            switch (object.senderPlatform) {
            case "ANDROID":
            case 0:
                message.senderPlatform = 0;
                break;
            case "SMBA":
            case 1:
                message.senderPlatform = 1;
                break;
            case "IPHONE":
            case 2:
                message.senderPlatform = 2;
                break;
            case "SMBI":
            case 3:
                message.senderPlatform = 3;
                break;
            case "WEB":
            case 4:
                message.senderPlatform = 4;
                break;
            case "UWP":
            case 5:
                message.senderPlatform = 5;
                break;
            case "DARWIN":
            case 6:
                message.senderPlatform = 6;
                break;
            case "IPAD":
            case 7:
                message.senderPlatform = 7;
                break;
            case "WEAROS":
            case 8:
                message.senderPlatform = 8;
                break;
            case "WASG":
            case 9:
                message.senderPlatform = 9;
                break;
            case "WEARM":
            case 10:
                message.senderPlatform = 10;
                break;
            case "CAPI":
            case 11:
                message.senderPlatform = 11;
                break;
            default:
                if (typeof object.senderPlatform === "number" && (object.senderPlatform | 0) === object.senderPlatform)
                    message.senderPlatform = object.senderPlatform;
            }
            if (object.isSenderPrimary != null)
                message.isSenderPrimary = Boolean(object.isSenderPrimary);
            return message;
        };

        /**
         * Creates a plain object from a PatchDebugData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.PatchDebugData
         * @static
         * @param {proto.PatchDebugData} message PatchDebugData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PatchDebugData.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.currentLthash != null && Object.hasOwnProperty.call(message, "currentLthash"))
                object.currentLthash = options.bytes === String ? $util.base64.encode(message.currentLthash, 0, message.currentLthash.length) : options.bytes === Array ? Array.prototype.slice.call(message.currentLthash) : message.currentLthash;
            if (message.newLthash != null && Object.hasOwnProperty.call(message, "newLthash"))
                object.newLthash = options.bytes === String ? $util.base64.encode(message.newLthash, 0, message.newLthash.length) : options.bytes === Array ? Array.prototype.slice.call(message.newLthash) : message.newLthash;
            if (message.patchVersion != null && Object.hasOwnProperty.call(message, "patchVersion"))
                object.patchVersion = options.bytes === String ? $util.base64.encode(message.patchVersion, 0, message.patchVersion.length) : options.bytes === Array ? Array.prototype.slice.call(message.patchVersion) : message.patchVersion;
            if (message.collectionName != null && Object.hasOwnProperty.call(message, "collectionName"))
                object.collectionName = options.bytes === String ? $util.base64.encode(message.collectionName, 0, message.collectionName.length) : options.bytes === Array ? Array.prototype.slice.call(message.collectionName) : message.collectionName;
            if (message.firstFourBytesFromAHashOfSnapshotMacKey != null && Object.hasOwnProperty.call(message, "firstFourBytesFromAHashOfSnapshotMacKey"))
                object.firstFourBytesFromAHashOfSnapshotMacKey = options.bytes === String ? $util.base64.encode(message.firstFourBytesFromAHashOfSnapshotMacKey, 0, message.firstFourBytesFromAHashOfSnapshotMacKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.firstFourBytesFromAHashOfSnapshotMacKey) : message.firstFourBytesFromAHashOfSnapshotMacKey;
            if (message.newLthashSubtract != null && Object.hasOwnProperty.call(message, "newLthashSubtract"))
                object.newLthashSubtract = options.bytes === String ? $util.base64.encode(message.newLthashSubtract, 0, message.newLthashSubtract.length) : options.bytes === Array ? Array.prototype.slice.call(message.newLthashSubtract) : message.newLthashSubtract;
            if (message.numberAdd != null && Object.hasOwnProperty.call(message, "numberAdd"))
                object.numberAdd = message.numberAdd;
            if (message.numberRemove != null && Object.hasOwnProperty.call(message, "numberRemove"))
                object.numberRemove = message.numberRemove;
            if (message.numberOverride != null && Object.hasOwnProperty.call(message, "numberOverride"))
                object.numberOverride = message.numberOverride;
            if (message.senderPlatform != null && Object.hasOwnProperty.call(message, "senderPlatform"))
                object.senderPlatform = options.enums === String ? $root.proto.PatchDebugData.Platform[message.senderPlatform] === undefined ? message.senderPlatform : $root.proto.PatchDebugData.Platform[message.senderPlatform] : message.senderPlatform;
            if (message.isSenderPrimary != null && Object.hasOwnProperty.call(message, "isSenderPrimary"))
                object.isSenderPrimary = message.isSenderPrimary;
            return object;
        };

        /**
         * Converts this PatchDebugData to JSON.
         * @function toJSON
         * @memberof proto.PatchDebugData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PatchDebugData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PatchDebugData
         * @function getTypeUrl
         * @memberof proto.PatchDebugData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PatchDebugData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.PatchDebugData";
        };

        /**
         * Platform enum.
         * @name proto.PatchDebugData.Platform
         * @enum {number}
         * @property {number} ANDROID=0 ANDROID value
         * @property {number} SMBA=1 SMBA value
         * @property {number} IPHONE=2 IPHONE value
         * @property {number} SMBI=3 SMBI value
         * @property {number} WEB=4 WEB value
         * @property {number} UWP=5 UWP value
         * @property {number} DARWIN=6 DARWIN value
         * @property {number} IPAD=7 IPAD value
         * @property {number} WEAROS=8 WEAROS value
         * @property {number} WASG=9 WASG value
         * @property {number} WEARM=10 WEARM value
         * @property {number} CAPI=11 CAPI value
         */
        PatchDebugData.Platform = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "ANDROID"] = 0;
            values[valuesById[1] = "SMBA"] = 1;
            values[valuesById[2] = "IPHONE"] = 2;
            values[valuesById[3] = "SMBI"] = 3;
            values[valuesById[4] = "WEB"] = 4;
            values[valuesById[5] = "UWP"] = 5;
            values[valuesById[6] = "DARWIN"] = 6;
            values[valuesById[7] = "IPAD"] = 7;
            values[valuesById[8] = "WEAROS"] = 8;
            values[valuesById[9] = "WASG"] = 9;
            values[valuesById[10] = "WEARM"] = 10;
            values[valuesById[11] = "CAPI"] = 11;
            return values;
        })();

        return PatchDebugData;
    })();

    proto.PaymentBackground = (function() {

        /**
         * Properties of a PaymentBackground.
         * @memberof proto
         * @interface IPaymentBackground
         * @property {string|null} [id] PaymentBackground id
         * @property {number|Long|null} [fileLength] PaymentBackground fileLength
         * @property {number|null} [width] PaymentBackground width
         * @property {number|null} [height] PaymentBackground height
         * @property {string|null} [mimetype] PaymentBackground mimetype
         * @property {number|null} [placeholderArgb] PaymentBackground placeholderArgb
         * @property {number|null} [textArgb] PaymentBackground textArgb
         * @property {number|null} [subtextArgb] PaymentBackground subtextArgb
         * @property {proto.PaymentBackground.IMediaData|null} [mediaData] PaymentBackground mediaData
         * @property {proto.PaymentBackground.Type|null} [type] PaymentBackground type
         */

        /**
         * Constructs a new PaymentBackground.
         * @memberof proto
         * @classdesc Represents a PaymentBackground.
         * @implements IPaymentBackground
         * @constructor
         * @param {proto.IPaymentBackground=} [properties] Properties to set
         */
        function PaymentBackground(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PaymentBackground id.
         * @member {string|null|undefined} id
         * @memberof proto.PaymentBackground
         * @instance
         */
        PaymentBackground.prototype.id = null;

        /**
         * PaymentBackground fileLength.
         * @member {number|Long|null|undefined} fileLength
         * @memberof proto.PaymentBackground
         * @instance
         */
        PaymentBackground.prototype.fileLength = null;

        /**
         * PaymentBackground width.
         * @member {number|null|undefined} width
         * @memberof proto.PaymentBackground
         * @instance
         */
        PaymentBackground.prototype.width = null;

        /**
         * PaymentBackground height.
         * @member {number|null|undefined} height
         * @memberof proto.PaymentBackground
         * @instance
         */
        PaymentBackground.prototype.height = null;

        /**
         * PaymentBackground mimetype.
         * @member {string|null|undefined} mimetype
         * @memberof proto.PaymentBackground
         * @instance
         */
        PaymentBackground.prototype.mimetype = null;

        /**
         * PaymentBackground placeholderArgb.
         * @member {number|null|undefined} placeholderArgb
         * @memberof proto.PaymentBackground
         * @instance
         */
        PaymentBackground.prototype.placeholderArgb = null;

        /**
         * PaymentBackground textArgb.
         * @member {number|null|undefined} textArgb
         * @memberof proto.PaymentBackground
         * @instance
         */
        PaymentBackground.prototype.textArgb = null;

        /**
         * PaymentBackground subtextArgb.
         * @member {number|null|undefined} subtextArgb
         * @memberof proto.PaymentBackground
         * @instance
         */
        PaymentBackground.prototype.subtextArgb = null;

        /**
         * PaymentBackground mediaData.
         * @member {proto.PaymentBackground.IMediaData|null|undefined} mediaData
         * @memberof proto.PaymentBackground
         * @instance
         */
        PaymentBackground.prototype.mediaData = null;

        /**
         * PaymentBackground type.
         * @member {proto.PaymentBackground.Type|null|undefined} type
         * @memberof proto.PaymentBackground
         * @instance
         */
        PaymentBackground.prototype.type = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentBackground.prototype, "_id", {
            get: $util.oneOfGetter($oneOfFields = ["id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentBackground.prototype, "_fileLength", {
            get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentBackground.prototype, "_width", {
            get: $util.oneOfGetter($oneOfFields = ["width"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentBackground.prototype, "_height", {
            get: $util.oneOfGetter($oneOfFields = ["height"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentBackground.prototype, "_mimetype", {
            get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentBackground.prototype, "_placeholderArgb", {
            get: $util.oneOfGetter($oneOfFields = ["placeholderArgb"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentBackground.prototype, "_textArgb", {
            get: $util.oneOfGetter($oneOfFields = ["textArgb"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentBackground.prototype, "_subtextArgb", {
            get: $util.oneOfGetter($oneOfFields = ["subtextArgb"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentBackground.prototype, "_mediaData", {
            get: $util.oneOfGetter($oneOfFields = ["mediaData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentBackground.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PaymentBackground instance using the specified properties.
         * @function create
         * @memberof proto.PaymentBackground
         * @static
         * @param {proto.IPaymentBackground=} [properties] Properties to set
         * @returns {proto.PaymentBackground} PaymentBackground instance
         */
        PaymentBackground.create = function create(properties) {
            return new PaymentBackground(properties);
        };

        /**
         * Encodes the specified PaymentBackground message. Does not implicitly {@link proto.PaymentBackground.verify|verify} messages.
         * @function encode
         * @memberof proto.PaymentBackground
         * @static
         * @param {proto.IPaymentBackground} message PaymentBackground message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PaymentBackground.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.fileLength);
            if (message.width != null && Object.hasOwnProperty.call(message, "width"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.width);
            if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.height);
            if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.mimetype);
            if (message.placeholderArgb != null && Object.hasOwnProperty.call(message, "placeholderArgb"))
                writer.uint32(/* id 6, wireType 5 =*/53).fixed32(message.placeholderArgb);
            if (message.textArgb != null && Object.hasOwnProperty.call(message, "textArgb"))
                writer.uint32(/* id 7, wireType 5 =*/61).fixed32(message.textArgb);
            if (message.subtextArgb != null && Object.hasOwnProperty.call(message, "subtextArgb"))
                writer.uint32(/* id 8, wireType 5 =*/69).fixed32(message.subtextArgb);
            if (message.mediaData != null && Object.hasOwnProperty.call(message, "mediaData"))
                $root.proto.PaymentBackground.MediaData.encode(message.mediaData, writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.type);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PaymentBackground message, length delimited. Does not implicitly {@link proto.PaymentBackground.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.PaymentBackground
         * @static
         * @param {proto.IPaymentBackground} message PaymentBackground message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PaymentBackground.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PaymentBackground message from the specified reader or buffer.
         * @function decode
         * @memberof proto.PaymentBackground
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.PaymentBackground} PaymentBackground
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PaymentBackground.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.id = reader.stringVerify();
                        message._id = "id";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.fileLength = reader.uint64();
                        message._fileLength = "fileLength";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.width = reader.uint32();
                        message._width = "width";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.height = reader.uint32();
                        message._height = "height";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.mimetype = reader.stringVerify();
                        message._mimetype = "mimetype";
                        continue;
                    }
                case 6: {
                        if (u !== 5)
                            break;
                        message.placeholderArgb = reader.fixed32();
                        message._placeholderArgb = "placeholderArgb";
                        continue;
                    }
                case 7: {
                        if (u !== 5)
                            break;
                        message.textArgb = reader.fixed32();
                        message._textArgb = "textArgb";
                        continue;
                    }
                case 8: {
                        if (u !== 5)
                            break;
                        message.subtextArgb = reader.fixed32();
                        message._subtextArgb = "subtextArgb";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        message.mediaData = $root.proto.PaymentBackground.MediaData.decode(reader, reader.uint32(), undefined, q + 1, message.mediaData);
                        message._mediaData = "mediaData";
                        continue;
                    }
                case 10: {
                        if (u !== 0)
                            break;
                        message.type = reader.int32();
                        message._type = "type";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a PaymentBackground message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.PaymentBackground
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.PaymentBackground} PaymentBackground
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PaymentBackground.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PaymentBackground message.
         * @function verify
         * @memberof proto.PaymentBackground
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PaymentBackground.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.id != null && Object.hasOwnProperty.call(message, "id")) {
                properties._id = 1;
                if (!$util.isString(message.id))
                    return "id: string expected";
            }
            if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength")) {
                properties._fileLength = 1;
                if (!$util.isInteger(message.fileLength) && !(message.fileLength && $util.isInteger(message.fileLength.low) && $util.isInteger(message.fileLength.high)))
                    return "fileLength: integer|Long expected";
            }
            if (message.width != null && Object.hasOwnProperty.call(message, "width")) {
                properties._width = 1;
                if (!$util.isInteger(message.width))
                    return "width: integer expected";
            }
            if (message.height != null && Object.hasOwnProperty.call(message, "height")) {
                properties._height = 1;
                if (!$util.isInteger(message.height))
                    return "height: integer expected";
            }
            if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype")) {
                properties._mimetype = 1;
                if (!$util.isString(message.mimetype))
                    return "mimetype: string expected";
            }
            if (message.placeholderArgb != null && Object.hasOwnProperty.call(message, "placeholderArgb")) {
                properties._placeholderArgb = 1;
                if (!$util.isInteger(message.placeholderArgb))
                    return "placeholderArgb: integer expected";
            }
            if (message.textArgb != null && Object.hasOwnProperty.call(message, "textArgb")) {
                properties._textArgb = 1;
                if (!$util.isInteger(message.textArgb))
                    return "textArgb: integer expected";
            }
            if (message.subtextArgb != null && Object.hasOwnProperty.call(message, "subtextArgb")) {
                properties._subtextArgb = 1;
                if (!$util.isInteger(message.subtextArgb))
                    return "subtextArgb: integer expected";
            }
            if (message.mediaData != null && Object.hasOwnProperty.call(message, "mediaData")) {
                properties._mediaData = 1;
                {
                    var error = $root.proto.PaymentBackground.MediaData.verify(message.mediaData, q + 1);
                    if (error)
                        return "mediaData." + error;
                }
            }
            if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                properties._type = 1;
                if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                    return "type: enum value expected";
            }
            return null;
        };

        /**
         * Creates a PaymentBackground message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.PaymentBackground
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.PaymentBackground} PaymentBackground
         */
        PaymentBackground.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.PaymentBackground: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.id != null)
                message.id = String(object.id);
            if (object.fileLength != null)
                if ($util.Long)
                    message.fileLength = $util.Long.fromValue(object.fileLength, true);
                else if (typeof object.fileLength === "string")
                    message.fileLength = parseInt(object.fileLength, 10);
                else if (typeof object.fileLength === "number")
                    message.fileLength = object.fileLength;
                else if (typeof object.fileLength === "object")
                    message.fileLength = new $util.LongBits(object.fileLength.low >>> 0, object.fileLength.high >>> 0).toNumber(true);
            if (object.width != null)
                message.width = object.width >>> 0;
            if (object.height != null)
                message.height = object.height >>> 0;
            if (object.mimetype != null)
                message.mimetype = String(object.mimetype);
            if (object.placeholderArgb != null)
                message.placeholderArgb = object.placeholderArgb >>> 0;
            if (object.textArgb != null)
                message.textArgb = object.textArgb >>> 0;
            if (object.subtextArgb != null)
                message.subtextArgb = object.subtextArgb >>> 0;
            if (object.mediaData != null) {
                if (!$util.isObject(object.mediaData))
                    throw TypeError(".proto.PaymentBackground.mediaData: object expected");
                message.mediaData = $root.proto.PaymentBackground.MediaData.fromObject(object.mediaData, q + 1);
            }
            switch (object.type) {
            case "UNKNOWN":
            case 0:
                message.type = 0;
                break;
            case "DEFAULT":
            case 1:
                message.type = 1;
                break;
            default:
                if (typeof object.type === "number" && (object.type | 0) === object.type)
                    message.type = object.type;
            }
            return message;
        };

        /**
         * Creates a plain object from a PaymentBackground message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.PaymentBackground
         * @static
         * @param {proto.PaymentBackground} message PaymentBackground
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PaymentBackground.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.fileLength = typeof message.fileLength === "number" ? BigInt(message.fileLength) : $util.Long.fromBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0, true).toBigInt();
                else if (typeof message.fileLength === "number")
                    object.fileLength = options.longs === String ? String(message.fileLength) : message.fileLength;
                else
                    object.fileLength = options.longs === String ? $util.Long.prototype.toString.call(message.fileLength) : options.longs === Number ? new $util.LongBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0).toNumber(true) : message.fileLength;
            if (message.width != null && Object.hasOwnProperty.call(message, "width"))
                object.width = message.width;
            if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                object.height = message.height;
            if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                object.mimetype = message.mimetype;
            if (message.placeholderArgb != null && Object.hasOwnProperty.call(message, "placeholderArgb"))
                object.placeholderArgb = message.placeholderArgb;
            if (message.textArgb != null && Object.hasOwnProperty.call(message, "textArgb"))
                object.textArgb = message.textArgb;
            if (message.subtextArgb != null && Object.hasOwnProperty.call(message, "subtextArgb"))
                object.subtextArgb = message.subtextArgb;
            if (message.mediaData != null && Object.hasOwnProperty.call(message, "mediaData"))
                object.mediaData = $root.proto.PaymentBackground.MediaData.toObject(message.mediaData, options, q + 1);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                object.type = options.enums === String ? $root.proto.PaymentBackground.Type[message.type] === undefined ? message.type : $root.proto.PaymentBackground.Type[message.type] : message.type;
            return object;
        };

        /**
         * Converts this PaymentBackground to JSON.
         * @function toJSON
         * @memberof proto.PaymentBackground
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PaymentBackground.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PaymentBackground
         * @function getTypeUrl
         * @memberof proto.PaymentBackground
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PaymentBackground.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.PaymentBackground";
        };

        PaymentBackground.MediaData = (function() {

            /**
             * Properties of a MediaData.
             * @memberof proto.PaymentBackground
             * @interface IMediaData
             * @property {Uint8Array|null} [mediaKey] MediaData mediaKey
             * @property {number|Long|null} [mediaKeyTimestamp] MediaData mediaKeyTimestamp
             * @property {Uint8Array|null} [fileSha256] MediaData fileSha256
             * @property {Uint8Array|null} [fileEncSha256] MediaData fileEncSha256
             * @property {string|null} [directPath] MediaData directPath
             */

            /**
             * Constructs a new MediaData.
             * @memberof proto.PaymentBackground
             * @classdesc Represents a MediaData.
             * @implements IMediaData
             * @constructor
             * @param {proto.PaymentBackground.IMediaData=} [properties] Properties to set
             */
            function MediaData(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MediaData mediaKey.
             * @member {Uint8Array|null|undefined} mediaKey
             * @memberof proto.PaymentBackground.MediaData
             * @instance
             */
            MediaData.prototype.mediaKey = null;

            /**
             * MediaData mediaKeyTimestamp.
             * @member {number|Long|null|undefined} mediaKeyTimestamp
             * @memberof proto.PaymentBackground.MediaData
             * @instance
             */
            MediaData.prototype.mediaKeyTimestamp = null;

            /**
             * MediaData fileSha256.
             * @member {Uint8Array|null|undefined} fileSha256
             * @memberof proto.PaymentBackground.MediaData
             * @instance
             */
            MediaData.prototype.fileSha256 = null;

            /**
             * MediaData fileEncSha256.
             * @member {Uint8Array|null|undefined} fileEncSha256
             * @memberof proto.PaymentBackground.MediaData
             * @instance
             */
            MediaData.prototype.fileEncSha256 = null;

            /**
             * MediaData directPath.
             * @member {string|null|undefined} directPath
             * @memberof proto.PaymentBackground.MediaData
             * @instance
             */
            MediaData.prototype.directPath = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MediaData.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MediaData.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MediaData.prototype, "_fileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MediaData.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MediaData.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new MediaData instance using the specified properties.
             * @function create
             * @memberof proto.PaymentBackground.MediaData
             * @static
             * @param {proto.PaymentBackground.IMediaData=} [properties] Properties to set
             * @returns {proto.PaymentBackground.MediaData} MediaData instance
             */
            MediaData.create = function create(properties) {
                return new MediaData(properties);
            };

            /**
             * Encodes the specified MediaData message. Does not implicitly {@link proto.PaymentBackground.MediaData.verify|verify} messages.
             * @function encode
             * @memberof proto.PaymentBackground.MediaData
             * @static
             * @param {proto.PaymentBackground.IMediaData} message MediaData message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MediaData.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.mediaKey);
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.mediaKeyTimestamp);
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.fileSha256);
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.fileEncSha256);
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.directPath);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MediaData message, length delimited. Does not implicitly {@link proto.PaymentBackground.MediaData.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.PaymentBackground.MediaData
             * @static
             * @param {proto.PaymentBackground.IMediaData} message MediaData message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MediaData.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a MediaData message from the specified reader or buffer.
             * @function decode
             * @memberof proto.PaymentBackground.MediaData
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.PaymentBackground.MediaData} MediaData
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MediaData.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.mediaKey = reader.bytes();
                            message._mediaKey = "mediaKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.mediaKeyTimestamp = reader.int64();
                            message._mediaKeyTimestamp = "mediaKeyTimestamp";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.fileSha256 = reader.bytes();
                            message._fileSha256 = "fileSha256";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.fileEncSha256 = reader.bytes();
                            message._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.directPath = reader.stringVerify();
                            message._directPath = "directPath";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a MediaData message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.PaymentBackground.MediaData
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.PaymentBackground.MediaData} MediaData
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MediaData.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MediaData message.
             * @function verify
             * @memberof proto.PaymentBackground.MediaData
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MediaData.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey")) {
                    properties._mediaKey = 1;
                    if (!(message.mediaKey && typeof message.mediaKey.length === "number" || $util.isString(message.mediaKey)))
                        return "mediaKey: buffer expected";
                }
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp")) {
                    properties._mediaKeyTimestamp = 1;
                    if (!$util.isInteger(message.mediaKeyTimestamp) && !(message.mediaKeyTimestamp && $util.isInteger(message.mediaKeyTimestamp.low) && $util.isInteger(message.mediaKeyTimestamp.high)))
                        return "mediaKeyTimestamp: integer|Long expected";
                }
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256")) {
                    properties._fileSha256 = 1;
                    if (!(message.fileSha256 && typeof message.fileSha256.length === "number" || $util.isString(message.fileSha256)))
                        return "fileSha256: buffer expected";
                }
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256")) {
                    properties._fileEncSha256 = 1;
                    if (!(message.fileEncSha256 && typeof message.fileEncSha256.length === "number" || $util.isString(message.fileEncSha256)))
                        return "fileEncSha256: buffer expected";
                }
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath")) {
                    properties._directPath = 1;
                    if (!$util.isString(message.directPath))
                        return "directPath: string expected";
                }
                return null;
            };

            /**
             * Creates a MediaData message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.PaymentBackground.MediaData
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.PaymentBackground.MediaData} MediaData
             */
            MediaData.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.PaymentBackground.MediaData: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.mediaKey != null)
                    if (typeof object.mediaKey === "string")
                        $util.base64.decode(object.mediaKey, message.mediaKey = $util.newBuffer($util.base64.length(object.mediaKey)), 0);
                    else if (object.mediaKey.length >= 0)
                        message.mediaKey = object.mediaKey;
                if (object.mediaKeyTimestamp != null)
                    if ($util.Long)
                        message.mediaKeyTimestamp = $util.Long.fromValue(object.mediaKeyTimestamp, false);
                    else if (typeof object.mediaKeyTimestamp === "string")
                        message.mediaKeyTimestamp = parseInt(object.mediaKeyTimestamp, 10);
                    else if (typeof object.mediaKeyTimestamp === "number")
                        message.mediaKeyTimestamp = object.mediaKeyTimestamp;
                    else if (typeof object.mediaKeyTimestamp === "object")
                        message.mediaKeyTimestamp = new $util.LongBits(object.mediaKeyTimestamp.low >>> 0, object.mediaKeyTimestamp.high >>> 0).toNumber();
                if (object.fileSha256 != null)
                    if (typeof object.fileSha256 === "string")
                        $util.base64.decode(object.fileSha256, message.fileSha256 = $util.newBuffer($util.base64.length(object.fileSha256)), 0);
                    else if (object.fileSha256.length >= 0)
                        message.fileSha256 = object.fileSha256;
                if (object.fileEncSha256 != null)
                    if (typeof object.fileEncSha256 === "string")
                        $util.base64.decode(object.fileEncSha256, message.fileEncSha256 = $util.newBuffer($util.base64.length(object.fileEncSha256)), 0);
                    else if (object.fileEncSha256.length >= 0)
                        message.fileEncSha256 = object.fileEncSha256;
                if (object.directPath != null)
                    message.directPath = String(object.directPath);
                return message;
            };

            /**
             * Creates a plain object from a MediaData message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.PaymentBackground.MediaData
             * @static
             * @param {proto.PaymentBackground.MediaData} message MediaData
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MediaData.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    object.mediaKey = options.bytes === String ? $util.base64.encode(message.mediaKey, 0, message.mediaKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.mediaKey) : message.mediaKey;
                if (message.mediaKeyTimestamp != null && Object.hasOwnProperty.call(message, "mediaKeyTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.mediaKeyTimestamp = typeof message.mediaKeyTimestamp === "number" ? BigInt(message.mediaKeyTimestamp) : $util.Long.fromBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.mediaKeyTimestamp === "number")
                        object.mediaKeyTimestamp = options.longs === String ? String(message.mediaKeyTimestamp) : message.mediaKeyTimestamp;
                    else
                        object.mediaKeyTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.mediaKeyTimestamp) : options.longs === Number ? new $util.LongBits(message.mediaKeyTimestamp.low >>> 0, message.mediaKeyTimestamp.high >>> 0).toNumber() : message.mediaKeyTimestamp;
                if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                    object.fileSha256 = options.bytes === String ? $util.base64.encode(message.fileSha256, 0, message.fileSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileSha256) : message.fileSha256;
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    object.fileEncSha256 = options.bytes === String ? $util.base64.encode(message.fileEncSha256, 0, message.fileEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileEncSha256) : message.fileEncSha256;
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    object.directPath = message.directPath;
                return object;
            };

            /**
             * Converts this MediaData to JSON.
             * @function toJSON
             * @memberof proto.PaymentBackground.MediaData
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MediaData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MediaData
             * @function getTypeUrl
             * @memberof proto.PaymentBackground.MediaData
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MediaData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.PaymentBackground.MediaData";
            };

            return MediaData;
        })();

        /**
         * Type enum.
         * @name proto.PaymentBackground.Type
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} DEFAULT=1 DEFAULT value
         */
        PaymentBackground.Type = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "DEFAULT"] = 1;
            return values;
        })();

        return PaymentBackground;
    })();

    proto.PaymentInfo = (function() {

        /**
         * Properties of a PaymentInfo.
         * @memberof proto
         * @interface IPaymentInfo
         * @property {proto.PaymentInfo.Currency|null} [currencyDeprecated] PaymentInfo currencyDeprecated
         * @property {number|Long|null} [amount1000] PaymentInfo amount1000
         * @property {string|null} [receiverJid] PaymentInfo receiverJid
         * @property {proto.PaymentInfo.Status|null} [status] PaymentInfo status
         * @property {number|Long|null} [transactionTimestamp] PaymentInfo transactionTimestamp
         * @property {proto.IMessageKey|null} [requestMessageKey] PaymentInfo requestMessageKey
         * @property {number|Long|null} [expiryTimestamp] PaymentInfo expiryTimestamp
         * @property {boolean|null} [futureproofed] PaymentInfo futureproofed
         * @property {string|null} [currency] PaymentInfo currency
         * @property {proto.PaymentInfo.TxnStatus|null} [txnStatus] PaymentInfo txnStatus
         * @property {boolean|null} [useNoviFiatFormat] PaymentInfo useNoviFiatFormat
         * @property {proto.IMoney|null} [primaryAmount] PaymentInfo primaryAmount
         * @property {proto.IMoney|null} [exchangeAmount] PaymentInfo exchangeAmount
         */

        /**
         * Constructs a new PaymentInfo.
         * @memberof proto
         * @classdesc Represents a PaymentInfo.
         * @implements IPaymentInfo
         * @constructor
         * @param {proto.IPaymentInfo=} [properties] Properties to set
         */
        function PaymentInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PaymentInfo currencyDeprecated.
         * @member {proto.PaymentInfo.Currency|null|undefined} currencyDeprecated
         * @memberof proto.PaymentInfo
         * @instance
         */
        PaymentInfo.prototype.currencyDeprecated = null;

        /**
         * PaymentInfo amount1000.
         * @member {number|Long|null|undefined} amount1000
         * @memberof proto.PaymentInfo
         * @instance
         */
        PaymentInfo.prototype.amount1000 = null;

        /**
         * PaymentInfo receiverJid.
         * @member {string|null|undefined} receiverJid
         * @memberof proto.PaymentInfo
         * @instance
         */
        PaymentInfo.prototype.receiverJid = null;

        /**
         * PaymentInfo status.
         * @member {proto.PaymentInfo.Status|null|undefined} status
         * @memberof proto.PaymentInfo
         * @instance
         */
        PaymentInfo.prototype.status = null;

        /**
         * PaymentInfo transactionTimestamp.
         * @member {number|Long|null|undefined} transactionTimestamp
         * @memberof proto.PaymentInfo
         * @instance
         */
        PaymentInfo.prototype.transactionTimestamp = null;

        /**
         * PaymentInfo requestMessageKey.
         * @member {proto.IMessageKey|null|undefined} requestMessageKey
         * @memberof proto.PaymentInfo
         * @instance
         */
        PaymentInfo.prototype.requestMessageKey = null;

        /**
         * PaymentInfo expiryTimestamp.
         * @member {number|Long|null|undefined} expiryTimestamp
         * @memberof proto.PaymentInfo
         * @instance
         */
        PaymentInfo.prototype.expiryTimestamp = null;

        /**
         * PaymentInfo futureproofed.
         * @member {boolean|null|undefined} futureproofed
         * @memberof proto.PaymentInfo
         * @instance
         */
        PaymentInfo.prototype.futureproofed = null;

        /**
         * PaymentInfo currency.
         * @member {string|null|undefined} currency
         * @memberof proto.PaymentInfo
         * @instance
         */
        PaymentInfo.prototype.currency = null;

        /**
         * PaymentInfo txnStatus.
         * @member {proto.PaymentInfo.TxnStatus|null|undefined} txnStatus
         * @memberof proto.PaymentInfo
         * @instance
         */
        PaymentInfo.prototype.txnStatus = null;

        /**
         * PaymentInfo useNoviFiatFormat.
         * @member {boolean|null|undefined} useNoviFiatFormat
         * @memberof proto.PaymentInfo
         * @instance
         */
        PaymentInfo.prototype.useNoviFiatFormat = null;

        /**
         * PaymentInfo primaryAmount.
         * @member {proto.IMoney|null|undefined} primaryAmount
         * @memberof proto.PaymentInfo
         * @instance
         */
        PaymentInfo.prototype.primaryAmount = null;

        /**
         * PaymentInfo exchangeAmount.
         * @member {proto.IMoney|null|undefined} exchangeAmount
         * @memberof proto.PaymentInfo
         * @instance
         */
        PaymentInfo.prototype.exchangeAmount = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_currencyDeprecated", {
            get: $util.oneOfGetter($oneOfFields = ["currencyDeprecated"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_amount1000", {
            get: $util.oneOfGetter($oneOfFields = ["amount1000"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_receiverJid", {
            get: $util.oneOfGetter($oneOfFields = ["receiverJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_status", {
            get: $util.oneOfGetter($oneOfFields = ["status"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_transactionTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["transactionTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_requestMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["requestMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_expiryTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["expiryTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_futureproofed", {
            get: $util.oneOfGetter($oneOfFields = ["futureproofed"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_currency", {
            get: $util.oneOfGetter($oneOfFields = ["currency"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_txnStatus", {
            get: $util.oneOfGetter($oneOfFields = ["txnStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_useNoviFiatFormat", {
            get: $util.oneOfGetter($oneOfFields = ["useNoviFiatFormat"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_primaryAmount", {
            get: $util.oneOfGetter($oneOfFields = ["primaryAmount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_exchangeAmount", {
            get: $util.oneOfGetter($oneOfFields = ["exchangeAmount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PaymentInfo instance using the specified properties.
         * @function create
         * @memberof proto.PaymentInfo
         * @static
         * @param {proto.IPaymentInfo=} [properties] Properties to set
         * @returns {proto.PaymentInfo} PaymentInfo instance
         */
        PaymentInfo.create = function create(properties) {
            return new PaymentInfo(properties);
        };

        /**
         * Encodes the specified PaymentInfo message. Does not implicitly {@link proto.PaymentInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.PaymentInfo
         * @static
         * @param {proto.IPaymentInfo} message PaymentInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PaymentInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.currencyDeprecated != null && Object.hasOwnProperty.call(message, "currencyDeprecated"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.currencyDeprecated);
            if (message.amount1000 != null && Object.hasOwnProperty.call(message, "amount1000"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.amount1000);
            if (message.receiverJid != null && Object.hasOwnProperty.call(message, "receiverJid"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.receiverJid);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.status);
            if (message.transactionTimestamp != null && Object.hasOwnProperty.call(message, "transactionTimestamp"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.transactionTimestamp);
            if (message.requestMessageKey != null && Object.hasOwnProperty.call(message, "requestMessageKey"))
                $root.proto.MessageKey.encode(message.requestMessageKey, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
            if (message.expiryTimestamp != null && Object.hasOwnProperty.call(message, "expiryTimestamp"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint64(message.expiryTimestamp);
            if (message.futureproofed != null && Object.hasOwnProperty.call(message, "futureproofed"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.futureproofed);
            if (message.currency != null && Object.hasOwnProperty.call(message, "currency"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.currency);
            if (message.txnStatus != null && Object.hasOwnProperty.call(message, "txnStatus"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.txnStatus);
            if (message.useNoviFiatFormat != null && Object.hasOwnProperty.call(message, "useNoviFiatFormat"))
                writer.uint32(/* id 11, wireType 0 =*/88).bool(message.useNoviFiatFormat);
            if (message.primaryAmount != null && Object.hasOwnProperty.call(message, "primaryAmount"))
                $root.proto.Money.encode(message.primaryAmount, writer.uint32(/* id 12, wireType 2 =*/98).fork(), q + 1).ldelim();
            if (message.exchangeAmount != null && Object.hasOwnProperty.call(message, "exchangeAmount"))
                $root.proto.Money.encode(message.exchangeAmount, writer.uint32(/* id 13, wireType 2 =*/106).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PaymentInfo message, length delimited. Does not implicitly {@link proto.PaymentInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.PaymentInfo
         * @static
         * @param {proto.IPaymentInfo} message PaymentInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PaymentInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PaymentInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.PaymentInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.PaymentInfo} PaymentInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PaymentInfo.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.currencyDeprecated = reader.int32();
                        message._currencyDeprecated = "currencyDeprecated";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.amount1000 = reader.uint64();
                        message._amount1000 = "amount1000";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.receiverJid = reader.stringVerify();
                        message._receiverJid = "receiverJid";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.status = reader.int32();
                        message._status = "status";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.transactionTimestamp = reader.uint64();
                        message._transactionTimestamp = "transactionTimestamp";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        message.requestMessageKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.requestMessageKey);
                        message._requestMessageKey = "requestMessageKey";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        message.expiryTimestamp = reader.uint64();
                        message._expiryTimestamp = "expiryTimestamp";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        message.futureproofed = reader.bool();
                        message._futureproofed = "futureproofed";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        message.currency = reader.stringVerify();
                        message._currency = "currency";
                        continue;
                    }
                case 10: {
                        if (u !== 0)
                            break;
                        message.txnStatus = reader.int32();
                        message._txnStatus = "txnStatus";
                        continue;
                    }
                case 11: {
                        if (u !== 0)
                            break;
                        message.useNoviFiatFormat = reader.bool();
                        message._useNoviFiatFormat = "useNoviFiatFormat";
                        continue;
                    }
                case 12: {
                        if (u !== 2)
                            break;
                        message.primaryAmount = $root.proto.Money.decode(reader, reader.uint32(), undefined, q + 1, message.primaryAmount);
                        message._primaryAmount = "primaryAmount";
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        message.exchangeAmount = $root.proto.Money.decode(reader, reader.uint32(), undefined, q + 1, message.exchangeAmount);
                        message._exchangeAmount = "exchangeAmount";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a PaymentInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.PaymentInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.PaymentInfo} PaymentInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PaymentInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PaymentInfo message.
         * @function verify
         * @memberof proto.PaymentInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PaymentInfo.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.currencyDeprecated != null && Object.hasOwnProperty.call(message, "currencyDeprecated")) {
                properties._currencyDeprecated = 1;
                if (typeof message.currencyDeprecated !== "number" || (message.currencyDeprecated | 0) !== message.currencyDeprecated)
                    return "currencyDeprecated: enum value expected";
            }
            if (message.amount1000 != null && Object.hasOwnProperty.call(message, "amount1000")) {
                properties._amount1000 = 1;
                if (!$util.isInteger(message.amount1000) && !(message.amount1000 && $util.isInteger(message.amount1000.low) && $util.isInteger(message.amount1000.high)))
                    return "amount1000: integer|Long expected";
            }
            if (message.receiverJid != null && Object.hasOwnProperty.call(message, "receiverJid")) {
                properties._receiverJid = 1;
                if (!$util.isString(message.receiverJid))
                    return "receiverJid: string expected";
            }
            if (message.status != null && Object.hasOwnProperty.call(message, "status")) {
                properties._status = 1;
                if (typeof message.status !== "number" || (message.status | 0) !== message.status)
                    return "status: enum value expected";
            }
            if (message.transactionTimestamp != null && Object.hasOwnProperty.call(message, "transactionTimestamp")) {
                properties._transactionTimestamp = 1;
                if (!$util.isInteger(message.transactionTimestamp) && !(message.transactionTimestamp && $util.isInteger(message.transactionTimestamp.low) && $util.isInteger(message.transactionTimestamp.high)))
                    return "transactionTimestamp: integer|Long expected";
            }
            if (message.requestMessageKey != null && Object.hasOwnProperty.call(message, "requestMessageKey")) {
                properties._requestMessageKey = 1;
                {
                    var error = $root.proto.MessageKey.verify(message.requestMessageKey, q + 1);
                    if (error)
                        return "requestMessageKey." + error;
                }
            }
            if (message.expiryTimestamp != null && Object.hasOwnProperty.call(message, "expiryTimestamp")) {
                properties._expiryTimestamp = 1;
                if (!$util.isInteger(message.expiryTimestamp) && !(message.expiryTimestamp && $util.isInteger(message.expiryTimestamp.low) && $util.isInteger(message.expiryTimestamp.high)))
                    return "expiryTimestamp: integer|Long expected";
            }
            if (message.futureproofed != null && Object.hasOwnProperty.call(message, "futureproofed")) {
                properties._futureproofed = 1;
                if (typeof message.futureproofed !== "boolean")
                    return "futureproofed: boolean expected";
            }
            if (message.currency != null && Object.hasOwnProperty.call(message, "currency")) {
                properties._currency = 1;
                if (!$util.isString(message.currency))
                    return "currency: string expected";
            }
            if (message.txnStatus != null && Object.hasOwnProperty.call(message, "txnStatus")) {
                properties._txnStatus = 1;
                if (typeof message.txnStatus !== "number" || (message.txnStatus | 0) !== message.txnStatus)
                    return "txnStatus: enum value expected";
            }
            if (message.useNoviFiatFormat != null && Object.hasOwnProperty.call(message, "useNoviFiatFormat")) {
                properties._useNoviFiatFormat = 1;
                if (typeof message.useNoviFiatFormat !== "boolean")
                    return "useNoviFiatFormat: boolean expected";
            }
            if (message.primaryAmount != null && Object.hasOwnProperty.call(message, "primaryAmount")) {
                properties._primaryAmount = 1;
                {
                    var error = $root.proto.Money.verify(message.primaryAmount, q + 1);
                    if (error)
                        return "primaryAmount." + error;
                }
            }
            if (message.exchangeAmount != null && Object.hasOwnProperty.call(message, "exchangeAmount")) {
                properties._exchangeAmount = 1;
                {
                    var error = $root.proto.Money.verify(message.exchangeAmount, q + 1);
                    if (error)
                        return "exchangeAmount." + error;
                }
            }
            return null;
        };

        /**
         * Creates a PaymentInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.PaymentInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.PaymentInfo} PaymentInfo
         */
        PaymentInfo.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.PaymentInfo: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.currencyDeprecated) {
            case "UNKNOWN_CURRENCY":
            case 0:
                message.currencyDeprecated = 0;
                break;
            case "INR":
            case 1:
                message.currencyDeprecated = 1;
                break;
            default:
                if (typeof object.currencyDeprecated === "number" && (object.currencyDeprecated | 0) === object.currencyDeprecated)
                    message.currencyDeprecated = object.currencyDeprecated;
            }
            if (object.amount1000 != null)
                if ($util.Long)
                    message.amount1000 = $util.Long.fromValue(object.amount1000, true);
                else if (typeof object.amount1000 === "string")
                    message.amount1000 = parseInt(object.amount1000, 10);
                else if (typeof object.amount1000 === "number")
                    message.amount1000 = object.amount1000;
                else if (typeof object.amount1000 === "object")
                    message.amount1000 = new $util.LongBits(object.amount1000.low >>> 0, object.amount1000.high >>> 0).toNumber(true);
            if (object.receiverJid != null)
                message.receiverJid = String(object.receiverJid);
            switch (object.status) {
            case "UNKNOWN_STATUS":
            case 0:
                message.status = 0;
                break;
            case "PROCESSING":
            case 1:
                message.status = 1;
                break;
            case "SENT":
            case 2:
                message.status = 2;
                break;
            case "NEED_TO_ACCEPT":
            case 3:
                message.status = 3;
                break;
            case "COMPLETE":
            case 4:
                message.status = 4;
                break;
            case "COULD_NOT_COMPLETE":
            case 5:
                message.status = 5;
                break;
            case "REFUNDED":
            case 6:
                message.status = 6;
                break;
            case "EXPIRED":
            case 7:
                message.status = 7;
                break;
            case "REJECTED":
            case 8:
                message.status = 8;
                break;
            case "CANCELLED":
            case 9:
                message.status = 9;
                break;
            case "WAITING_FOR_PAYER":
            case 10:
                message.status = 10;
                break;
            case "WAITING":
            case 11:
                message.status = 11;
                break;
            default:
                if (typeof object.status === "number" && (object.status | 0) === object.status)
                    message.status = object.status;
            }
            if (object.transactionTimestamp != null)
                if ($util.Long)
                    message.transactionTimestamp = $util.Long.fromValue(object.transactionTimestamp, true);
                else if (typeof object.transactionTimestamp === "string")
                    message.transactionTimestamp = parseInt(object.transactionTimestamp, 10);
                else if (typeof object.transactionTimestamp === "number")
                    message.transactionTimestamp = object.transactionTimestamp;
                else if (typeof object.transactionTimestamp === "object")
                    message.transactionTimestamp = new $util.LongBits(object.transactionTimestamp.low >>> 0, object.transactionTimestamp.high >>> 0).toNumber(true);
            if (object.requestMessageKey != null) {
                if (!$util.isObject(object.requestMessageKey))
                    throw TypeError(".proto.PaymentInfo.requestMessageKey: object expected");
                message.requestMessageKey = $root.proto.MessageKey.fromObject(object.requestMessageKey, q + 1);
            }
            if (object.expiryTimestamp != null)
                if ($util.Long)
                    message.expiryTimestamp = $util.Long.fromValue(object.expiryTimestamp, true);
                else if (typeof object.expiryTimestamp === "string")
                    message.expiryTimestamp = parseInt(object.expiryTimestamp, 10);
                else if (typeof object.expiryTimestamp === "number")
                    message.expiryTimestamp = object.expiryTimestamp;
                else if (typeof object.expiryTimestamp === "object")
                    message.expiryTimestamp = new $util.LongBits(object.expiryTimestamp.low >>> 0, object.expiryTimestamp.high >>> 0).toNumber(true);
            if (object.futureproofed != null)
                message.futureproofed = Boolean(object.futureproofed);
            if (object.currency != null)
                message.currency = String(object.currency);
            switch (object.txnStatus) {
            case "UNKNOWN":
            case 0:
                message.txnStatus = 0;
                break;
            case "PENDING_SETUP":
            case 1:
                message.txnStatus = 1;
                break;
            case "PENDING_RECEIVER_SETUP":
            case 2:
                message.txnStatus = 2;
                break;
            case "INIT":
            case 3:
                message.txnStatus = 3;
                break;
            case "SUCCESS":
            case 4:
                message.txnStatus = 4;
                break;
            case "COMPLETED":
            case 5:
                message.txnStatus = 5;
                break;
            case "FAILED":
            case 6:
                message.txnStatus = 6;
                break;
            case "FAILED_RISK":
            case 7:
                message.txnStatus = 7;
                break;
            case "FAILED_PROCESSING":
            case 8:
                message.txnStatus = 8;
                break;
            case "FAILED_RECEIVER_PROCESSING":
            case 9:
                message.txnStatus = 9;
                break;
            case "FAILED_DA":
            case 10:
                message.txnStatus = 10;
                break;
            case "FAILED_DA_FINAL":
            case 11:
                message.txnStatus = 11;
                break;
            case "REFUNDED_TXN":
            case 12:
                message.txnStatus = 12;
                break;
            case "REFUND_FAILED":
            case 13:
                message.txnStatus = 13;
                break;
            case "REFUND_FAILED_PROCESSING":
            case 14:
                message.txnStatus = 14;
                break;
            case "REFUND_FAILED_DA":
            case 15:
                message.txnStatus = 15;
                break;
            case "EXPIRED_TXN":
            case 16:
                message.txnStatus = 16;
                break;
            case "AUTH_CANCELED":
            case 17:
                message.txnStatus = 17;
                break;
            case "AUTH_CANCEL_FAILED_PROCESSING":
            case 18:
                message.txnStatus = 18;
                break;
            case "AUTH_CANCEL_FAILED":
            case 19:
                message.txnStatus = 19;
                break;
            case "COLLECT_INIT":
            case 20:
                message.txnStatus = 20;
                break;
            case "COLLECT_SUCCESS":
            case 21:
                message.txnStatus = 21;
                break;
            case "COLLECT_FAILED":
            case 22:
                message.txnStatus = 22;
                break;
            case "COLLECT_FAILED_RISK":
            case 23:
                message.txnStatus = 23;
                break;
            case "COLLECT_REJECTED":
            case 24:
                message.txnStatus = 24;
                break;
            case "COLLECT_EXPIRED":
            case 25:
                message.txnStatus = 25;
                break;
            case "COLLECT_CANCELED":
            case 26:
                message.txnStatus = 26;
                break;
            case "COLLECT_CANCELLING":
            case 27:
                message.txnStatus = 27;
                break;
            case "IN_REVIEW":
            case 28:
                message.txnStatus = 28;
                break;
            case "REVERSAL_SUCCESS":
            case 29:
                message.txnStatus = 29;
                break;
            case "REVERSAL_PENDING":
            case 30:
                message.txnStatus = 30;
                break;
            case "REFUND_PENDING":
            case 31:
                message.txnStatus = 31;
                break;
            default:
                if (typeof object.txnStatus === "number" && (object.txnStatus | 0) === object.txnStatus)
                    message.txnStatus = object.txnStatus;
            }
            if (object.useNoviFiatFormat != null)
                message.useNoviFiatFormat = Boolean(object.useNoviFiatFormat);
            if (object.primaryAmount != null) {
                if (!$util.isObject(object.primaryAmount))
                    throw TypeError(".proto.PaymentInfo.primaryAmount: object expected");
                message.primaryAmount = $root.proto.Money.fromObject(object.primaryAmount, q + 1);
            }
            if (object.exchangeAmount != null) {
                if (!$util.isObject(object.exchangeAmount))
                    throw TypeError(".proto.PaymentInfo.exchangeAmount: object expected");
                message.exchangeAmount = $root.proto.Money.fromObject(object.exchangeAmount, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a PaymentInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.PaymentInfo
         * @static
         * @param {proto.PaymentInfo} message PaymentInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PaymentInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.currencyDeprecated != null && Object.hasOwnProperty.call(message, "currencyDeprecated"))
                object.currencyDeprecated = options.enums === String ? $root.proto.PaymentInfo.Currency[message.currencyDeprecated] === undefined ? message.currencyDeprecated : $root.proto.PaymentInfo.Currency[message.currencyDeprecated] : message.currencyDeprecated;
            if (message.amount1000 != null && Object.hasOwnProperty.call(message, "amount1000"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.amount1000 = typeof message.amount1000 === "number" ? BigInt(message.amount1000) : $util.Long.fromBits(message.amount1000.low >>> 0, message.amount1000.high >>> 0, true).toBigInt();
                else if (typeof message.amount1000 === "number")
                    object.amount1000 = options.longs === String ? String(message.amount1000) : message.amount1000;
                else
                    object.amount1000 = options.longs === String ? $util.Long.prototype.toString.call(message.amount1000) : options.longs === Number ? new $util.LongBits(message.amount1000.low >>> 0, message.amount1000.high >>> 0).toNumber(true) : message.amount1000;
            if (message.receiverJid != null && Object.hasOwnProperty.call(message, "receiverJid"))
                object.receiverJid = message.receiverJid;
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                object.status = options.enums === String ? $root.proto.PaymentInfo.Status[message.status] === undefined ? message.status : $root.proto.PaymentInfo.Status[message.status] : message.status;
            if (message.transactionTimestamp != null && Object.hasOwnProperty.call(message, "transactionTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.transactionTimestamp = typeof message.transactionTimestamp === "number" ? BigInt(message.transactionTimestamp) : $util.Long.fromBits(message.transactionTimestamp.low >>> 0, message.transactionTimestamp.high >>> 0, true).toBigInt();
                else if (typeof message.transactionTimestamp === "number")
                    object.transactionTimestamp = options.longs === String ? String(message.transactionTimestamp) : message.transactionTimestamp;
                else
                    object.transactionTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.transactionTimestamp) : options.longs === Number ? new $util.LongBits(message.transactionTimestamp.low >>> 0, message.transactionTimestamp.high >>> 0).toNumber(true) : message.transactionTimestamp;
            if (message.requestMessageKey != null && Object.hasOwnProperty.call(message, "requestMessageKey"))
                object.requestMessageKey = $root.proto.MessageKey.toObject(message.requestMessageKey, options, q + 1);
            if (message.expiryTimestamp != null && Object.hasOwnProperty.call(message, "expiryTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.expiryTimestamp = typeof message.expiryTimestamp === "number" ? BigInt(message.expiryTimestamp) : $util.Long.fromBits(message.expiryTimestamp.low >>> 0, message.expiryTimestamp.high >>> 0, true).toBigInt();
                else if (typeof message.expiryTimestamp === "number")
                    object.expiryTimestamp = options.longs === String ? String(message.expiryTimestamp) : message.expiryTimestamp;
                else
                    object.expiryTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.expiryTimestamp) : options.longs === Number ? new $util.LongBits(message.expiryTimestamp.low >>> 0, message.expiryTimestamp.high >>> 0).toNumber(true) : message.expiryTimestamp;
            if (message.futureproofed != null && Object.hasOwnProperty.call(message, "futureproofed"))
                object.futureproofed = message.futureproofed;
            if (message.currency != null && Object.hasOwnProperty.call(message, "currency"))
                object.currency = message.currency;
            if (message.txnStatus != null && Object.hasOwnProperty.call(message, "txnStatus"))
                object.txnStatus = options.enums === String ? $root.proto.PaymentInfo.TxnStatus[message.txnStatus] === undefined ? message.txnStatus : $root.proto.PaymentInfo.TxnStatus[message.txnStatus] : message.txnStatus;
            if (message.useNoviFiatFormat != null && Object.hasOwnProperty.call(message, "useNoviFiatFormat"))
                object.useNoviFiatFormat = message.useNoviFiatFormat;
            if (message.primaryAmount != null && Object.hasOwnProperty.call(message, "primaryAmount"))
                object.primaryAmount = $root.proto.Money.toObject(message.primaryAmount, options, q + 1);
            if (message.exchangeAmount != null && Object.hasOwnProperty.call(message, "exchangeAmount"))
                object.exchangeAmount = $root.proto.Money.toObject(message.exchangeAmount, options, q + 1);
            return object;
        };

        /**
         * Converts this PaymentInfo to JSON.
         * @function toJSON
         * @memberof proto.PaymentInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PaymentInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PaymentInfo
         * @function getTypeUrl
         * @memberof proto.PaymentInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PaymentInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.PaymentInfo";
        };

        /**
         * Currency enum.
         * @name proto.PaymentInfo.Currency
         * @enum {number}
         * @property {number} UNKNOWN_CURRENCY=0 UNKNOWN_CURRENCY value
         * @property {number} INR=1 INR value
         */
        PaymentInfo.Currency = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_CURRENCY"] = 0;
            values[valuesById[1] = "INR"] = 1;
            return values;
        })();

        /**
         * Status enum.
         * @name proto.PaymentInfo.Status
         * @enum {number}
         * @property {number} UNKNOWN_STATUS=0 UNKNOWN_STATUS value
         * @property {number} PROCESSING=1 PROCESSING value
         * @property {number} SENT=2 SENT value
         * @property {number} NEED_TO_ACCEPT=3 NEED_TO_ACCEPT value
         * @property {number} COMPLETE=4 COMPLETE value
         * @property {number} COULD_NOT_COMPLETE=5 COULD_NOT_COMPLETE value
         * @property {number} REFUNDED=6 REFUNDED value
         * @property {number} EXPIRED=7 EXPIRED value
         * @property {number} REJECTED=8 REJECTED value
         * @property {number} CANCELLED=9 CANCELLED value
         * @property {number} WAITING_FOR_PAYER=10 WAITING_FOR_PAYER value
         * @property {number} WAITING=11 WAITING value
         */
        PaymentInfo.Status = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_STATUS"] = 0;
            values[valuesById[1] = "PROCESSING"] = 1;
            values[valuesById[2] = "SENT"] = 2;
            values[valuesById[3] = "NEED_TO_ACCEPT"] = 3;
            values[valuesById[4] = "COMPLETE"] = 4;
            values[valuesById[5] = "COULD_NOT_COMPLETE"] = 5;
            values[valuesById[6] = "REFUNDED"] = 6;
            values[valuesById[7] = "EXPIRED"] = 7;
            values[valuesById[8] = "REJECTED"] = 8;
            values[valuesById[9] = "CANCELLED"] = 9;
            values[valuesById[10] = "WAITING_FOR_PAYER"] = 10;
            values[valuesById[11] = "WAITING"] = 11;
            return values;
        })();

        /**
         * TxnStatus enum.
         * @name proto.PaymentInfo.TxnStatus
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} PENDING_SETUP=1 PENDING_SETUP value
         * @property {number} PENDING_RECEIVER_SETUP=2 PENDING_RECEIVER_SETUP value
         * @property {number} INIT=3 INIT value
         * @property {number} SUCCESS=4 SUCCESS value
         * @property {number} COMPLETED=5 COMPLETED value
         * @property {number} FAILED=6 FAILED value
         * @property {number} FAILED_RISK=7 FAILED_RISK value
         * @property {number} FAILED_PROCESSING=8 FAILED_PROCESSING value
         * @property {number} FAILED_RECEIVER_PROCESSING=9 FAILED_RECEIVER_PROCESSING value
         * @property {number} FAILED_DA=10 FAILED_DA value
         * @property {number} FAILED_DA_FINAL=11 FAILED_DA_FINAL value
         * @property {number} REFUNDED_TXN=12 REFUNDED_TXN value
         * @property {number} REFUND_FAILED=13 REFUND_FAILED value
         * @property {number} REFUND_FAILED_PROCESSING=14 REFUND_FAILED_PROCESSING value
         * @property {number} REFUND_FAILED_DA=15 REFUND_FAILED_DA value
         * @property {number} EXPIRED_TXN=16 EXPIRED_TXN value
         * @property {number} AUTH_CANCELED=17 AUTH_CANCELED value
         * @property {number} AUTH_CANCEL_FAILED_PROCESSING=18 AUTH_CANCEL_FAILED_PROCESSING value
         * @property {number} AUTH_CANCEL_FAILED=19 AUTH_CANCEL_FAILED value
         * @property {number} COLLECT_INIT=20 COLLECT_INIT value
         * @property {number} COLLECT_SUCCESS=21 COLLECT_SUCCESS value
         * @property {number} COLLECT_FAILED=22 COLLECT_FAILED value
         * @property {number} COLLECT_FAILED_RISK=23 COLLECT_FAILED_RISK value
         * @property {number} COLLECT_REJECTED=24 COLLECT_REJECTED value
         * @property {number} COLLECT_EXPIRED=25 COLLECT_EXPIRED value
         * @property {number} COLLECT_CANCELED=26 COLLECT_CANCELED value
         * @property {number} COLLECT_CANCELLING=27 COLLECT_CANCELLING value
         * @property {number} IN_REVIEW=28 IN_REVIEW value
         * @property {number} REVERSAL_SUCCESS=29 REVERSAL_SUCCESS value
         * @property {number} REVERSAL_PENDING=30 REVERSAL_PENDING value
         * @property {number} REFUND_PENDING=31 REFUND_PENDING value
         */
        PaymentInfo.TxnStatus = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "PENDING_SETUP"] = 1;
            values[valuesById[2] = "PENDING_RECEIVER_SETUP"] = 2;
            values[valuesById[3] = "INIT"] = 3;
            values[valuesById[4] = "SUCCESS"] = 4;
            values[valuesById[5] = "COMPLETED"] = 5;
            values[valuesById[6] = "FAILED"] = 6;
            values[valuesById[7] = "FAILED_RISK"] = 7;
            values[valuesById[8] = "FAILED_PROCESSING"] = 8;
            values[valuesById[9] = "FAILED_RECEIVER_PROCESSING"] = 9;
            values[valuesById[10] = "FAILED_DA"] = 10;
            values[valuesById[11] = "FAILED_DA_FINAL"] = 11;
            values[valuesById[12] = "REFUNDED_TXN"] = 12;
            values[valuesById[13] = "REFUND_FAILED"] = 13;
            values[valuesById[14] = "REFUND_FAILED_PROCESSING"] = 14;
            values[valuesById[15] = "REFUND_FAILED_DA"] = 15;
            values[valuesById[16] = "EXPIRED_TXN"] = 16;
            values[valuesById[17] = "AUTH_CANCELED"] = 17;
            values[valuesById[18] = "AUTH_CANCEL_FAILED_PROCESSING"] = 18;
            values[valuesById[19] = "AUTH_CANCEL_FAILED"] = 19;
            values[valuesById[20] = "COLLECT_INIT"] = 20;
            values[valuesById[21] = "COLLECT_SUCCESS"] = 21;
            values[valuesById[22] = "COLLECT_FAILED"] = 22;
            values[valuesById[23] = "COLLECT_FAILED_RISK"] = 23;
            values[valuesById[24] = "COLLECT_REJECTED"] = 24;
            values[valuesById[25] = "COLLECT_EXPIRED"] = 25;
            values[valuesById[26] = "COLLECT_CANCELED"] = 26;
            values[valuesById[27] = "COLLECT_CANCELLING"] = 27;
            values[valuesById[28] = "IN_REVIEW"] = 28;
            values[valuesById[29] = "REVERSAL_SUCCESS"] = 29;
            values[valuesById[30] = "REVERSAL_PENDING"] = 30;
            values[valuesById[31] = "REFUND_PENDING"] = 31;
            return values;
        })();

        return PaymentInfo;
    })();

    proto.PhoneNumberToLIDMapping = (function() {

        /**
         * Properties of a PhoneNumberToLIDMapping.
         * @memberof proto
         * @interface IPhoneNumberToLIDMapping
         * @property {string|null} [pnJid] PhoneNumberToLIDMapping pnJid
         * @property {string|null} [lidJid] PhoneNumberToLIDMapping lidJid
         */

        /**
         * Constructs a new PhoneNumberToLIDMapping.
         * @memberof proto
         * @classdesc Represents a PhoneNumberToLIDMapping.
         * @implements IPhoneNumberToLIDMapping
         * @constructor
         * @param {proto.IPhoneNumberToLIDMapping=} [properties] Properties to set
         */
        function PhoneNumberToLIDMapping(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PhoneNumberToLIDMapping pnJid.
         * @member {string|null|undefined} pnJid
         * @memberof proto.PhoneNumberToLIDMapping
         * @instance
         */
        PhoneNumberToLIDMapping.prototype.pnJid = null;

        /**
         * PhoneNumberToLIDMapping lidJid.
         * @member {string|null|undefined} lidJid
         * @memberof proto.PhoneNumberToLIDMapping
         * @instance
         */
        PhoneNumberToLIDMapping.prototype.lidJid = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PhoneNumberToLIDMapping.prototype, "_pnJid", {
            get: $util.oneOfGetter($oneOfFields = ["pnJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PhoneNumberToLIDMapping.prototype, "_lidJid", {
            get: $util.oneOfGetter($oneOfFields = ["lidJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PhoneNumberToLIDMapping instance using the specified properties.
         * @function create
         * @memberof proto.PhoneNumberToLIDMapping
         * @static
         * @param {proto.IPhoneNumberToLIDMapping=} [properties] Properties to set
         * @returns {proto.PhoneNumberToLIDMapping} PhoneNumberToLIDMapping instance
         */
        PhoneNumberToLIDMapping.create = function create(properties) {
            return new PhoneNumberToLIDMapping(properties);
        };

        /**
         * Encodes the specified PhoneNumberToLIDMapping message. Does not implicitly {@link proto.PhoneNumberToLIDMapping.verify|verify} messages.
         * @function encode
         * @memberof proto.PhoneNumberToLIDMapping
         * @static
         * @param {proto.IPhoneNumberToLIDMapping} message PhoneNumberToLIDMapping message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PhoneNumberToLIDMapping.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.pnJid != null && Object.hasOwnProperty.call(message, "pnJid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.pnJid);
            if (message.lidJid != null && Object.hasOwnProperty.call(message, "lidJid"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.lidJid);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PhoneNumberToLIDMapping message, length delimited. Does not implicitly {@link proto.PhoneNumberToLIDMapping.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.PhoneNumberToLIDMapping
         * @static
         * @param {proto.IPhoneNumberToLIDMapping} message PhoneNumberToLIDMapping message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PhoneNumberToLIDMapping.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PhoneNumberToLIDMapping message from the specified reader or buffer.
         * @function decode
         * @memberof proto.PhoneNumberToLIDMapping
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.PhoneNumberToLIDMapping} PhoneNumberToLIDMapping
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PhoneNumberToLIDMapping.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.pnJid = reader.stringVerify();
                        message._pnJid = "pnJid";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.lidJid = reader.stringVerify();
                        message._lidJid = "lidJid";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a PhoneNumberToLIDMapping message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.PhoneNumberToLIDMapping
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.PhoneNumberToLIDMapping} PhoneNumberToLIDMapping
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PhoneNumberToLIDMapping.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PhoneNumberToLIDMapping message.
         * @function verify
         * @memberof proto.PhoneNumberToLIDMapping
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PhoneNumberToLIDMapping.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.pnJid != null && Object.hasOwnProperty.call(message, "pnJid")) {
                properties._pnJid = 1;
                if (!$util.isString(message.pnJid))
                    return "pnJid: string expected";
            }
            if (message.lidJid != null && Object.hasOwnProperty.call(message, "lidJid")) {
                properties._lidJid = 1;
                if (!$util.isString(message.lidJid))
                    return "lidJid: string expected";
            }
            return null;
        };

        /**
         * Creates a PhoneNumberToLIDMapping message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.PhoneNumberToLIDMapping
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.PhoneNumberToLIDMapping} PhoneNumberToLIDMapping
         */
        PhoneNumberToLIDMapping.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.PhoneNumberToLIDMapping: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.pnJid != null)
                message.pnJid = String(object.pnJid);
            if (object.lidJid != null)
                message.lidJid = String(object.lidJid);
            return message;
        };

        /**
         * Creates a plain object from a PhoneNumberToLIDMapping message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.PhoneNumberToLIDMapping
         * @static
         * @param {proto.PhoneNumberToLIDMapping} message PhoneNumberToLIDMapping
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PhoneNumberToLIDMapping.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.pnJid != null && Object.hasOwnProperty.call(message, "pnJid"))
                object.pnJid = message.pnJid;
            if (message.lidJid != null && Object.hasOwnProperty.call(message, "lidJid"))
                object.lidJid = message.lidJid;
            return object;
        };

        /**
         * Converts this PhoneNumberToLIDMapping to JSON.
         * @function toJSON
         * @memberof proto.PhoneNumberToLIDMapping
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PhoneNumberToLIDMapping.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PhoneNumberToLIDMapping
         * @function getTypeUrl
         * @memberof proto.PhoneNumberToLIDMapping
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PhoneNumberToLIDMapping.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.PhoneNumberToLIDMapping";
        };

        return PhoneNumberToLIDMapping;
    })();

    proto.PhotoChange = (function() {

        /**
         * Properties of a PhotoChange.
         * @memberof proto
         * @interface IPhotoChange
         * @property {Uint8Array|null} [oldPhoto] PhotoChange oldPhoto
         * @property {Uint8Array|null} [newPhoto] PhotoChange newPhoto
         * @property {number|null} [newPhotoId] PhotoChange newPhotoId
         */

        /**
         * Constructs a new PhotoChange.
         * @memberof proto
         * @classdesc Represents a PhotoChange.
         * @implements IPhotoChange
         * @constructor
         * @param {proto.IPhotoChange=} [properties] Properties to set
         */
        function PhotoChange(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PhotoChange oldPhoto.
         * @member {Uint8Array|null|undefined} oldPhoto
         * @memberof proto.PhotoChange
         * @instance
         */
        PhotoChange.prototype.oldPhoto = null;

        /**
         * PhotoChange newPhoto.
         * @member {Uint8Array|null|undefined} newPhoto
         * @memberof proto.PhotoChange
         * @instance
         */
        PhotoChange.prototype.newPhoto = null;

        /**
         * PhotoChange newPhotoId.
         * @member {number|null|undefined} newPhotoId
         * @memberof proto.PhotoChange
         * @instance
         */
        PhotoChange.prototype.newPhotoId = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PhotoChange.prototype, "_oldPhoto", {
            get: $util.oneOfGetter($oneOfFields = ["oldPhoto"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PhotoChange.prototype, "_newPhoto", {
            get: $util.oneOfGetter($oneOfFields = ["newPhoto"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PhotoChange.prototype, "_newPhotoId", {
            get: $util.oneOfGetter($oneOfFields = ["newPhotoId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PhotoChange instance using the specified properties.
         * @function create
         * @memberof proto.PhotoChange
         * @static
         * @param {proto.IPhotoChange=} [properties] Properties to set
         * @returns {proto.PhotoChange} PhotoChange instance
         */
        PhotoChange.create = function create(properties) {
            return new PhotoChange(properties);
        };

        /**
         * Encodes the specified PhotoChange message. Does not implicitly {@link proto.PhotoChange.verify|verify} messages.
         * @function encode
         * @memberof proto.PhotoChange
         * @static
         * @param {proto.IPhotoChange} message PhotoChange message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PhotoChange.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.oldPhoto != null && Object.hasOwnProperty.call(message, "oldPhoto"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.oldPhoto);
            if (message.newPhoto != null && Object.hasOwnProperty.call(message, "newPhoto"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.newPhoto);
            if (message.newPhotoId != null && Object.hasOwnProperty.call(message, "newPhotoId"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.newPhotoId);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PhotoChange message, length delimited. Does not implicitly {@link proto.PhotoChange.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.PhotoChange
         * @static
         * @param {proto.IPhotoChange} message PhotoChange message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PhotoChange.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PhotoChange message from the specified reader or buffer.
         * @function decode
         * @memberof proto.PhotoChange
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.PhotoChange} PhotoChange
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PhotoChange.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.oldPhoto = reader.bytes();
                        message._oldPhoto = "oldPhoto";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.newPhoto = reader.bytes();
                        message._newPhoto = "newPhoto";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.newPhotoId = reader.uint32();
                        message._newPhotoId = "newPhotoId";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a PhotoChange message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.PhotoChange
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.PhotoChange} PhotoChange
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PhotoChange.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PhotoChange message.
         * @function verify
         * @memberof proto.PhotoChange
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PhotoChange.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.oldPhoto != null && Object.hasOwnProperty.call(message, "oldPhoto")) {
                properties._oldPhoto = 1;
                if (!(message.oldPhoto && typeof message.oldPhoto.length === "number" || $util.isString(message.oldPhoto)))
                    return "oldPhoto: buffer expected";
            }
            if (message.newPhoto != null && Object.hasOwnProperty.call(message, "newPhoto")) {
                properties._newPhoto = 1;
                if (!(message.newPhoto && typeof message.newPhoto.length === "number" || $util.isString(message.newPhoto)))
                    return "newPhoto: buffer expected";
            }
            if (message.newPhotoId != null && Object.hasOwnProperty.call(message, "newPhotoId")) {
                properties._newPhotoId = 1;
                if (!$util.isInteger(message.newPhotoId))
                    return "newPhotoId: integer expected";
            }
            return null;
        };

        /**
         * Creates a PhotoChange message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.PhotoChange
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.PhotoChange} PhotoChange
         */
        PhotoChange.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.PhotoChange: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.oldPhoto != null)
                if (typeof object.oldPhoto === "string")
                    $util.base64.decode(object.oldPhoto, message.oldPhoto = $util.newBuffer($util.base64.length(object.oldPhoto)), 0);
                else if (object.oldPhoto.length >= 0)
                    message.oldPhoto = object.oldPhoto;
            if (object.newPhoto != null)
                if (typeof object.newPhoto === "string")
                    $util.base64.decode(object.newPhoto, message.newPhoto = $util.newBuffer($util.base64.length(object.newPhoto)), 0);
                else if (object.newPhoto.length >= 0)
                    message.newPhoto = object.newPhoto;
            if (object.newPhotoId != null)
                message.newPhotoId = object.newPhotoId >>> 0;
            return message;
        };

        /**
         * Creates a plain object from a PhotoChange message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.PhotoChange
         * @static
         * @param {proto.PhotoChange} message PhotoChange
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PhotoChange.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.oldPhoto != null && Object.hasOwnProperty.call(message, "oldPhoto"))
                object.oldPhoto = options.bytes === String ? $util.base64.encode(message.oldPhoto, 0, message.oldPhoto.length) : options.bytes === Array ? Array.prototype.slice.call(message.oldPhoto) : message.oldPhoto;
            if (message.newPhoto != null && Object.hasOwnProperty.call(message, "newPhoto"))
                object.newPhoto = options.bytes === String ? $util.base64.encode(message.newPhoto, 0, message.newPhoto.length) : options.bytes === Array ? Array.prototype.slice.call(message.newPhoto) : message.newPhoto;
            if (message.newPhotoId != null && Object.hasOwnProperty.call(message, "newPhotoId"))
                object.newPhotoId = message.newPhotoId;
            return object;
        };

        /**
         * Converts this PhotoChange to JSON.
         * @function toJSON
         * @memberof proto.PhotoChange
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PhotoChange.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PhotoChange
         * @function getTypeUrl
         * @memberof proto.PhotoChange
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PhotoChange.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.PhotoChange";
        };

        return PhotoChange;
    })();

    proto.PinInChat = (function() {

        /**
         * Properties of a PinInChat.
         * @memberof proto
         * @interface IPinInChat
         * @property {proto.PinInChat.Type|null} [type] PinInChat type
         * @property {proto.IMessageKey|null} [key] PinInChat key
         * @property {number|Long|null} [senderTimestampMs] PinInChat senderTimestampMs
         * @property {number|Long|null} [serverTimestampMs] PinInChat serverTimestampMs
         * @property {proto.IMessageAddOnContextInfo|null} [messageAddOnContextInfo] PinInChat messageAddOnContextInfo
         */

        /**
         * Constructs a new PinInChat.
         * @memberof proto
         * @classdesc Represents a PinInChat.
         * @implements IPinInChat
         * @constructor
         * @param {proto.IPinInChat=} [properties] Properties to set
         */
        function PinInChat(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PinInChat type.
         * @member {proto.PinInChat.Type|null|undefined} type
         * @memberof proto.PinInChat
         * @instance
         */
        PinInChat.prototype.type = null;

        /**
         * PinInChat key.
         * @member {proto.IMessageKey|null|undefined} key
         * @memberof proto.PinInChat
         * @instance
         */
        PinInChat.prototype.key = null;

        /**
         * PinInChat senderTimestampMs.
         * @member {number|Long|null|undefined} senderTimestampMs
         * @memberof proto.PinInChat
         * @instance
         */
        PinInChat.prototype.senderTimestampMs = null;

        /**
         * PinInChat serverTimestampMs.
         * @member {number|Long|null|undefined} serverTimestampMs
         * @memberof proto.PinInChat
         * @instance
         */
        PinInChat.prototype.serverTimestampMs = null;

        /**
         * PinInChat messageAddOnContextInfo.
         * @member {proto.IMessageAddOnContextInfo|null|undefined} messageAddOnContextInfo
         * @memberof proto.PinInChat
         * @instance
         */
        PinInChat.prototype.messageAddOnContextInfo = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PinInChat.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PinInChat.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PinInChat.prototype, "_senderTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["senderTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PinInChat.prototype, "_serverTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["serverTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PinInChat.prototype, "_messageAddOnContextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOnContextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PinInChat instance using the specified properties.
         * @function create
         * @memberof proto.PinInChat
         * @static
         * @param {proto.IPinInChat=} [properties] Properties to set
         * @returns {proto.PinInChat} PinInChat instance
         */
        PinInChat.create = function create(properties) {
            return new PinInChat(properties);
        };

        /**
         * Encodes the specified PinInChat message. Does not implicitly {@link proto.PinInChat.verify|verify} messages.
         * @function encode
         * @memberof proto.PinInChat
         * @static
         * @param {proto.IPinInChat} message PinInChat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PinInChat.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                $root.proto.MessageKey.encode(message.key, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.senderTimestampMs);
            if (message.serverTimestampMs != null && Object.hasOwnProperty.call(message, "serverTimestampMs"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.serverTimestampMs);
            if (message.messageAddOnContextInfo != null && Object.hasOwnProperty.call(message, "messageAddOnContextInfo"))
                $root.proto.MessageAddOnContextInfo.encode(message.messageAddOnContextInfo, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PinInChat message, length delimited. Does not implicitly {@link proto.PinInChat.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.PinInChat
         * @static
         * @param {proto.IPinInChat} message PinInChat message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PinInChat.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PinInChat message from the specified reader or buffer.
         * @function decode
         * @memberof proto.PinInChat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.PinInChat} PinInChat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PinInChat.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.type = reader.int32();
                        message._type = "type";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.key = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.key);
                        message._key = "key";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.senderTimestampMs = reader.int64();
                        message._senderTimestampMs = "senderTimestampMs";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.serverTimestampMs = reader.int64();
                        message._serverTimestampMs = "serverTimestampMs";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.messageAddOnContextInfo = $root.proto.MessageAddOnContextInfo.decode(reader, reader.uint32(), undefined, q + 1, message.messageAddOnContextInfo);
                        message._messageAddOnContextInfo = "messageAddOnContextInfo";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a PinInChat message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.PinInChat
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.PinInChat} PinInChat
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PinInChat.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PinInChat message.
         * @function verify
         * @memberof proto.PinInChat
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PinInChat.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                properties._type = 1;
                if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                    return "type: enum value expected";
            }
            if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                properties._key = 1;
                {
                    var error = $root.proto.MessageKey.verify(message.key, q + 1);
                    if (error)
                        return "key." + error;
                }
            }
            if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs")) {
                properties._senderTimestampMs = 1;
                if (!$util.isInteger(message.senderTimestampMs) && !(message.senderTimestampMs && $util.isInteger(message.senderTimestampMs.low) && $util.isInteger(message.senderTimestampMs.high)))
                    return "senderTimestampMs: integer|Long expected";
            }
            if (message.serverTimestampMs != null && Object.hasOwnProperty.call(message, "serverTimestampMs")) {
                properties._serverTimestampMs = 1;
                if (!$util.isInteger(message.serverTimestampMs) && !(message.serverTimestampMs && $util.isInteger(message.serverTimestampMs.low) && $util.isInteger(message.serverTimestampMs.high)))
                    return "serverTimestampMs: integer|Long expected";
            }
            if (message.messageAddOnContextInfo != null && Object.hasOwnProperty.call(message, "messageAddOnContextInfo")) {
                properties._messageAddOnContextInfo = 1;
                {
                    var error = $root.proto.MessageAddOnContextInfo.verify(message.messageAddOnContextInfo, q + 1);
                    if (error)
                        return "messageAddOnContextInfo." + error;
                }
            }
            return null;
        };

        /**
         * Creates a PinInChat message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.PinInChat
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.PinInChat} PinInChat
         */
        PinInChat.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.PinInChat: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.type) {
            case "UNKNOWN_TYPE":
            case 0:
                message.type = 0;
                break;
            case "PIN_FOR_ALL":
            case 1:
                message.type = 1;
                break;
            case "UNPIN_FOR_ALL":
            case 2:
                message.type = 2;
                break;
            default:
                if (typeof object.type === "number" && (object.type | 0) === object.type)
                    message.type = object.type;
            }
            if (object.key != null) {
                if (!$util.isObject(object.key))
                    throw TypeError(".proto.PinInChat.key: object expected");
                message.key = $root.proto.MessageKey.fromObject(object.key, q + 1);
            }
            if (object.senderTimestampMs != null)
                if ($util.Long)
                    message.senderTimestampMs = $util.Long.fromValue(object.senderTimestampMs, false);
                else if (typeof object.senderTimestampMs === "string")
                    message.senderTimestampMs = parseInt(object.senderTimestampMs, 10);
                else if (typeof object.senderTimestampMs === "number")
                    message.senderTimestampMs = object.senderTimestampMs;
                else if (typeof object.senderTimestampMs === "object")
                    message.senderTimestampMs = new $util.LongBits(object.senderTimestampMs.low >>> 0, object.senderTimestampMs.high >>> 0).toNumber();
            if (object.serverTimestampMs != null)
                if ($util.Long)
                    message.serverTimestampMs = $util.Long.fromValue(object.serverTimestampMs, false);
                else if (typeof object.serverTimestampMs === "string")
                    message.serverTimestampMs = parseInt(object.serverTimestampMs, 10);
                else if (typeof object.serverTimestampMs === "number")
                    message.serverTimestampMs = object.serverTimestampMs;
                else if (typeof object.serverTimestampMs === "object")
                    message.serverTimestampMs = new $util.LongBits(object.serverTimestampMs.low >>> 0, object.serverTimestampMs.high >>> 0).toNumber();
            if (object.messageAddOnContextInfo != null) {
                if (!$util.isObject(object.messageAddOnContextInfo))
                    throw TypeError(".proto.PinInChat.messageAddOnContextInfo: object expected");
                message.messageAddOnContextInfo = $root.proto.MessageAddOnContextInfo.fromObject(object.messageAddOnContextInfo, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a PinInChat message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.PinInChat
         * @static
         * @param {proto.PinInChat} message PinInChat
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PinInChat.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                object.type = options.enums === String ? $root.proto.PinInChat.Type[message.type] === undefined ? message.type : $root.proto.PinInChat.Type[message.type] : message.type;
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                object.key = $root.proto.MessageKey.toObject(message.key, options, q + 1);
            if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.senderTimestampMs = typeof message.senderTimestampMs === "number" ? BigInt(message.senderTimestampMs) : $util.Long.fromBits(message.senderTimestampMs.low >>> 0, message.senderTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof message.senderTimestampMs === "number")
                    object.senderTimestampMs = options.longs === String ? String(message.senderTimestampMs) : message.senderTimestampMs;
                else
                    object.senderTimestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.senderTimestampMs) : options.longs === Number ? new $util.LongBits(message.senderTimestampMs.low >>> 0, message.senderTimestampMs.high >>> 0).toNumber() : message.senderTimestampMs;
            if (message.serverTimestampMs != null && Object.hasOwnProperty.call(message, "serverTimestampMs"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.serverTimestampMs = typeof message.serverTimestampMs === "number" ? BigInt(message.serverTimestampMs) : $util.Long.fromBits(message.serverTimestampMs.low >>> 0, message.serverTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof message.serverTimestampMs === "number")
                    object.serverTimestampMs = options.longs === String ? String(message.serverTimestampMs) : message.serverTimestampMs;
                else
                    object.serverTimestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.serverTimestampMs) : options.longs === Number ? new $util.LongBits(message.serverTimestampMs.low >>> 0, message.serverTimestampMs.high >>> 0).toNumber() : message.serverTimestampMs;
            if (message.messageAddOnContextInfo != null && Object.hasOwnProperty.call(message, "messageAddOnContextInfo"))
                object.messageAddOnContextInfo = $root.proto.MessageAddOnContextInfo.toObject(message.messageAddOnContextInfo, options, q + 1);
            return object;
        };

        /**
         * Converts this PinInChat to JSON.
         * @function toJSON
         * @memberof proto.PinInChat
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PinInChat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PinInChat
         * @function getTypeUrl
         * @memberof proto.PinInChat
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PinInChat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.PinInChat";
        };

        /**
         * Type enum.
         * @name proto.PinInChat.Type
         * @enum {number}
         * @property {number} UNKNOWN_TYPE=0 UNKNOWN_TYPE value
         * @property {number} PIN_FOR_ALL=1 PIN_FOR_ALL value
         * @property {number} UNPIN_FOR_ALL=2 UNPIN_FOR_ALL value
         */
        PinInChat.Type = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_TYPE"] = 0;
            values[valuesById[1] = "PIN_FOR_ALL"] = 1;
            values[valuesById[2] = "UNPIN_FOR_ALL"] = 2;
            return values;
        })();

        return PinInChat;
    })();

    proto.Point = (function() {

        /**
         * Properties of a Point.
         * @memberof proto
         * @interface IPoint
         * @property {number|null} [xDeprecated] Point xDeprecated
         * @property {number|null} [yDeprecated] Point yDeprecated
         * @property {number|null} [x] Point x
         * @property {number|null} [y] Point y
         */

        /**
         * Constructs a new Point.
         * @memberof proto
         * @classdesc Represents a Point.
         * @implements IPoint
         * @constructor
         * @param {proto.IPoint=} [properties] Properties to set
         */
        function Point(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Point xDeprecated.
         * @member {number|null|undefined} xDeprecated
         * @memberof proto.Point
         * @instance
         */
        Point.prototype.xDeprecated = null;

        /**
         * Point yDeprecated.
         * @member {number|null|undefined} yDeprecated
         * @memberof proto.Point
         * @instance
         */
        Point.prototype.yDeprecated = null;

        /**
         * Point x.
         * @member {number|null|undefined} x
         * @memberof proto.Point
         * @instance
         */
        Point.prototype.x = null;

        /**
         * Point y.
         * @member {number|null|undefined} y
         * @memberof proto.Point
         * @instance
         */
        Point.prototype.y = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Point.prototype, "_xDeprecated", {
            get: $util.oneOfGetter($oneOfFields = ["xDeprecated"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Point.prototype, "_yDeprecated", {
            get: $util.oneOfGetter($oneOfFields = ["yDeprecated"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Point.prototype, "_x", {
            get: $util.oneOfGetter($oneOfFields = ["x"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Point.prototype, "_y", {
            get: $util.oneOfGetter($oneOfFields = ["y"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new Point instance using the specified properties.
         * @function create
         * @memberof proto.Point
         * @static
         * @param {proto.IPoint=} [properties] Properties to set
         * @returns {proto.Point} Point instance
         */
        Point.create = function create(properties) {
            return new Point(properties);
        };

        /**
         * Encodes the specified Point message. Does not implicitly {@link proto.Point.verify|verify} messages.
         * @function encode
         * @memberof proto.Point
         * @static
         * @param {proto.IPoint} message Point message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Point.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.xDeprecated != null && Object.hasOwnProperty.call(message, "xDeprecated"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.xDeprecated);
            if (message.yDeprecated != null && Object.hasOwnProperty.call(message, "yDeprecated"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.yDeprecated);
            if (message.x != null && Object.hasOwnProperty.call(message, "x"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.x);
            if (message.y != null && Object.hasOwnProperty.call(message, "y"))
                writer.uint32(/* id 4, wireType 1 =*/33).double(message.y);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Point message, length delimited. Does not implicitly {@link proto.Point.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Point
         * @static
         * @param {proto.IPoint} message Point message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Point.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Point message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Point
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Point} Point
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Point.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.xDeprecated = reader.int32();
                        message._xDeprecated = "xDeprecated";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.yDeprecated = reader.int32();
                        message._yDeprecated = "yDeprecated";
                        continue;
                    }
                case 3: {
                        if (u !== 1)
                            break;
                        message.x = reader.double();
                        message._x = "x";
                        continue;
                    }
                case 4: {
                        if (u !== 1)
                            break;
                        message.y = reader.double();
                        message._y = "y";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a Point message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Point
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Point} Point
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Point.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Point message.
         * @function verify
         * @memberof proto.Point
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Point.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.xDeprecated != null && Object.hasOwnProperty.call(message, "xDeprecated")) {
                properties._xDeprecated = 1;
                if (!$util.isInteger(message.xDeprecated))
                    return "xDeprecated: integer expected";
            }
            if (message.yDeprecated != null && Object.hasOwnProperty.call(message, "yDeprecated")) {
                properties._yDeprecated = 1;
                if (!$util.isInteger(message.yDeprecated))
                    return "yDeprecated: integer expected";
            }
            if (message.x != null && Object.hasOwnProperty.call(message, "x")) {
                properties._x = 1;
                if (typeof message.x !== "number")
                    return "x: number expected";
            }
            if (message.y != null && Object.hasOwnProperty.call(message, "y")) {
                properties._y = 1;
                if (typeof message.y !== "number")
                    return "y: number expected";
            }
            return null;
        };

        /**
         * Creates a Point message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Point
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Point} Point
         */
        Point.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.Point: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.xDeprecated != null)
                message.xDeprecated = object.xDeprecated | 0;
            if (object.yDeprecated != null)
                message.yDeprecated = object.yDeprecated | 0;
            if (object.x != null)
                message.x = Number(object.x);
            if (object.y != null)
                message.y = Number(object.y);
            return message;
        };

        /**
         * Creates a plain object from a Point message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Point
         * @static
         * @param {proto.Point} message Point
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Point.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.xDeprecated != null && Object.hasOwnProperty.call(message, "xDeprecated"))
                object.xDeprecated = message.xDeprecated;
            if (message.yDeprecated != null && Object.hasOwnProperty.call(message, "yDeprecated"))
                object.yDeprecated = message.yDeprecated;
            if (message.x != null && Object.hasOwnProperty.call(message, "x"))
                object.x = options.json && !isFinite(message.x) ? String(message.x) : message.x;
            if (message.y != null && Object.hasOwnProperty.call(message, "y"))
                object.y = options.json && !isFinite(message.y) ? String(message.y) : message.y;
            return object;
        };

        /**
         * Converts this Point to JSON.
         * @function toJSON
         * @memberof proto.Point
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Point.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Point
         * @function getTypeUrl
         * @memberof proto.Point
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Point.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.Point";
        };

        return Point;
    })();

    proto.PollAdditionalMetadata = (function() {

        /**
         * Properties of a PollAdditionalMetadata.
         * @memberof proto
         * @interface IPollAdditionalMetadata
         * @property {boolean|null} [pollInvalidated] PollAdditionalMetadata pollInvalidated
         * @property {Array.<proto.PollAdditionalMetadata.IPollNameHashHistoryEntry>|null} [pollNameHashHistory] PollAdditionalMetadata pollNameHashHistory
         */

        /**
         * Constructs a new PollAdditionalMetadata.
         * @memberof proto
         * @classdesc Represents a PollAdditionalMetadata.
         * @implements IPollAdditionalMetadata
         * @constructor
         * @param {proto.IPollAdditionalMetadata=} [properties] Properties to set
         */
        function PollAdditionalMetadata(properties) {
            this.pollNameHashHistory = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PollAdditionalMetadata pollInvalidated.
         * @member {boolean|null|undefined} pollInvalidated
         * @memberof proto.PollAdditionalMetadata
         * @instance
         */
        PollAdditionalMetadata.prototype.pollInvalidated = null;

        /**
         * PollAdditionalMetadata pollNameHashHistory.
         * @member {Array.<proto.PollAdditionalMetadata.IPollNameHashHistoryEntry>} pollNameHashHistory
         * @memberof proto.PollAdditionalMetadata
         * @instance
         */
        PollAdditionalMetadata.prototype.pollNameHashHistory = $util.emptyArray;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollAdditionalMetadata.prototype, "_pollInvalidated", {
            get: $util.oneOfGetter($oneOfFields = ["pollInvalidated"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PollAdditionalMetadata instance using the specified properties.
         * @function create
         * @memberof proto.PollAdditionalMetadata
         * @static
         * @param {proto.IPollAdditionalMetadata=} [properties] Properties to set
         * @returns {proto.PollAdditionalMetadata} PollAdditionalMetadata instance
         */
        PollAdditionalMetadata.create = function create(properties) {
            return new PollAdditionalMetadata(properties);
        };

        /**
         * Encodes the specified PollAdditionalMetadata message. Does not implicitly {@link proto.PollAdditionalMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.PollAdditionalMetadata
         * @static
         * @param {proto.IPollAdditionalMetadata} message PollAdditionalMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PollAdditionalMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.pollInvalidated != null && Object.hasOwnProperty.call(message, "pollInvalidated"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.pollInvalidated);
            if (message.pollNameHashHistory != null && message.pollNameHashHistory.length)
                for (var i = 0; i < message.pollNameHashHistory.length; ++i)
                    $root.proto.PollAdditionalMetadata.PollNameHashHistoryEntry.encode(message.pollNameHashHistory[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PollAdditionalMetadata message, length delimited. Does not implicitly {@link proto.PollAdditionalMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.PollAdditionalMetadata
         * @static
         * @param {proto.IPollAdditionalMetadata} message PollAdditionalMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PollAdditionalMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PollAdditionalMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.PollAdditionalMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.PollAdditionalMetadata} PollAdditionalMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PollAdditionalMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.pollInvalidated = reader.bool();
                        message._pollInvalidated = "pollInvalidated";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(message.pollNameHashHistory && message.pollNameHashHistory.length))
                            message.pollNameHashHistory = [];
                        message.pollNameHashHistory.push($root.proto.PollAdditionalMetadata.PollNameHashHistoryEntry.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a PollAdditionalMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.PollAdditionalMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.PollAdditionalMetadata} PollAdditionalMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PollAdditionalMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PollAdditionalMetadata message.
         * @function verify
         * @memberof proto.PollAdditionalMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PollAdditionalMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.pollInvalidated != null && Object.hasOwnProperty.call(message, "pollInvalidated")) {
                properties._pollInvalidated = 1;
                if (typeof message.pollInvalidated !== "boolean")
                    return "pollInvalidated: boolean expected";
            }
            if (message.pollNameHashHistory != null && Object.hasOwnProperty.call(message, "pollNameHashHistory")) {
                if (!Array.isArray(message.pollNameHashHistory))
                    return "pollNameHashHistory: array expected";
                for (var i = 0; i < message.pollNameHashHistory.length; ++i) {
                    var error = $root.proto.PollAdditionalMetadata.PollNameHashHistoryEntry.verify(message.pollNameHashHistory[i], q + 1);
                    if (error)
                        return "pollNameHashHistory." + error;
                }
            }
            return null;
        };

        /**
         * Creates a PollAdditionalMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.PollAdditionalMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.PollAdditionalMetadata} PollAdditionalMetadata
         */
        PollAdditionalMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.PollAdditionalMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.pollInvalidated != null)
                message.pollInvalidated = Boolean(object.pollInvalidated);
            if (object.pollNameHashHistory) {
                if (!Array.isArray(object.pollNameHashHistory))
                    throw TypeError(".proto.PollAdditionalMetadata.pollNameHashHistory: array expected");
                message.pollNameHashHistory = Array(object.pollNameHashHistory.length);
                for (var i = 0; i < object.pollNameHashHistory.length; ++i) {
                    if (!$util.isObject(object.pollNameHashHistory[i]))
                        throw TypeError(".proto.PollAdditionalMetadata.pollNameHashHistory: object expected");
                    message.pollNameHashHistory[i] = $root.proto.PollAdditionalMetadata.PollNameHashHistoryEntry.fromObject(object.pollNameHashHistory[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a PollAdditionalMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.PollAdditionalMetadata
         * @static
         * @param {proto.PollAdditionalMetadata} message PollAdditionalMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PollAdditionalMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.pollNameHashHistory = [];
            if (message.pollInvalidated != null && Object.hasOwnProperty.call(message, "pollInvalidated"))
                object.pollInvalidated = message.pollInvalidated;
            if (message.pollNameHashHistory && message.pollNameHashHistory.length) {
                object.pollNameHashHistory = Array(message.pollNameHashHistory.length);
                for (var j = 0; j < message.pollNameHashHistory.length; ++j)
                    object.pollNameHashHistory[j] = $root.proto.PollAdditionalMetadata.PollNameHashHistoryEntry.toObject(message.pollNameHashHistory[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this PollAdditionalMetadata to JSON.
         * @function toJSON
         * @memberof proto.PollAdditionalMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PollAdditionalMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PollAdditionalMetadata
         * @function getTypeUrl
         * @memberof proto.PollAdditionalMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PollAdditionalMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.PollAdditionalMetadata";
        };

        PollAdditionalMetadata.PollNameHashHistoryEntry = (function() {

            /**
             * Properties of a PollNameHashHistoryEntry.
             * @memberof proto.PollAdditionalMetadata
             * @interface IPollNameHashHistoryEntry
             * @property {string|null} [editStanzaId] PollNameHashHistoryEntry editStanzaId
             * @property {Uint8Array|null} [pollNameHash] PollNameHashHistoryEntry pollNameHash
             */

            /**
             * Constructs a new PollNameHashHistoryEntry.
             * @memberof proto.PollAdditionalMetadata
             * @classdesc Represents a PollNameHashHistoryEntry.
             * @implements IPollNameHashHistoryEntry
             * @constructor
             * @param {proto.PollAdditionalMetadata.IPollNameHashHistoryEntry=} [properties] Properties to set
             */
            function PollNameHashHistoryEntry(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PollNameHashHistoryEntry editStanzaId.
             * @member {string|null|undefined} editStanzaId
             * @memberof proto.PollAdditionalMetadata.PollNameHashHistoryEntry
             * @instance
             */
            PollNameHashHistoryEntry.prototype.editStanzaId = null;

            /**
             * PollNameHashHistoryEntry pollNameHash.
             * @member {Uint8Array|null|undefined} pollNameHash
             * @memberof proto.PollAdditionalMetadata.PollNameHashHistoryEntry
             * @instance
             */
            PollNameHashHistoryEntry.prototype.pollNameHash = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollNameHashHistoryEntry.prototype, "_editStanzaId", {
                get: $util.oneOfGetter($oneOfFields = ["editStanzaId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollNameHashHistoryEntry.prototype, "_pollNameHash", {
                get: $util.oneOfGetter($oneOfFields = ["pollNameHash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PollNameHashHistoryEntry instance using the specified properties.
             * @function create
             * @memberof proto.PollAdditionalMetadata.PollNameHashHistoryEntry
             * @static
             * @param {proto.PollAdditionalMetadata.IPollNameHashHistoryEntry=} [properties] Properties to set
             * @returns {proto.PollAdditionalMetadata.PollNameHashHistoryEntry} PollNameHashHistoryEntry instance
             */
            PollNameHashHistoryEntry.create = function create(properties) {
                return new PollNameHashHistoryEntry(properties);
            };

            /**
             * Encodes the specified PollNameHashHistoryEntry message. Does not implicitly {@link proto.PollAdditionalMetadata.PollNameHashHistoryEntry.verify|verify} messages.
             * @function encode
             * @memberof proto.PollAdditionalMetadata.PollNameHashHistoryEntry
             * @static
             * @param {proto.PollAdditionalMetadata.IPollNameHashHistoryEntry} message PollNameHashHistoryEntry message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollNameHashHistoryEntry.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.editStanzaId != null && Object.hasOwnProperty.call(message, "editStanzaId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.editStanzaId);
                if (message.pollNameHash != null && Object.hasOwnProperty.call(message, "pollNameHash"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.pollNameHash);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PollNameHashHistoryEntry message, length delimited. Does not implicitly {@link proto.PollAdditionalMetadata.PollNameHashHistoryEntry.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.PollAdditionalMetadata.PollNameHashHistoryEntry
             * @static
             * @param {proto.PollAdditionalMetadata.IPollNameHashHistoryEntry} message PollNameHashHistoryEntry message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PollNameHashHistoryEntry.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PollNameHashHistoryEntry message from the specified reader or buffer.
             * @function decode
             * @memberof proto.PollAdditionalMetadata.PollNameHashHistoryEntry
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.PollAdditionalMetadata.PollNameHashHistoryEntry} PollNameHashHistoryEntry
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollNameHashHistoryEntry.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.editStanzaId = reader.stringVerify();
                            message._editStanzaId = "editStanzaId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.pollNameHash = reader.bytes();
                            message._pollNameHash = "pollNameHash";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PollNameHashHistoryEntry message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.PollAdditionalMetadata.PollNameHashHistoryEntry
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.PollAdditionalMetadata.PollNameHashHistoryEntry} PollNameHashHistoryEntry
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PollNameHashHistoryEntry.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PollNameHashHistoryEntry message.
             * @function verify
             * @memberof proto.PollAdditionalMetadata.PollNameHashHistoryEntry
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PollNameHashHistoryEntry.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.editStanzaId != null && Object.hasOwnProperty.call(message, "editStanzaId")) {
                    properties._editStanzaId = 1;
                    if (!$util.isString(message.editStanzaId))
                        return "editStanzaId: string expected";
                }
                if (message.pollNameHash != null && Object.hasOwnProperty.call(message, "pollNameHash")) {
                    properties._pollNameHash = 1;
                    if (!(message.pollNameHash && typeof message.pollNameHash.length === "number" || $util.isString(message.pollNameHash)))
                        return "pollNameHash: buffer expected";
                }
                return null;
            };

            /**
             * Creates a PollNameHashHistoryEntry message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.PollAdditionalMetadata.PollNameHashHistoryEntry
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.PollAdditionalMetadata.PollNameHashHistoryEntry} PollNameHashHistoryEntry
             */
            PollNameHashHistoryEntry.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.PollAdditionalMetadata.PollNameHashHistoryEntry: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.editStanzaId != null)
                    message.editStanzaId = String(object.editStanzaId);
                if (object.pollNameHash != null)
                    if (typeof object.pollNameHash === "string")
                        $util.base64.decode(object.pollNameHash, message.pollNameHash = $util.newBuffer($util.base64.length(object.pollNameHash)), 0);
                    else if (object.pollNameHash.length >= 0)
                        message.pollNameHash = object.pollNameHash;
                return message;
            };

            /**
             * Creates a plain object from a PollNameHashHistoryEntry message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.PollAdditionalMetadata.PollNameHashHistoryEntry
             * @static
             * @param {proto.PollAdditionalMetadata.PollNameHashHistoryEntry} message PollNameHashHistoryEntry
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PollNameHashHistoryEntry.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.editStanzaId != null && Object.hasOwnProperty.call(message, "editStanzaId"))
                    object.editStanzaId = message.editStanzaId;
                if (message.pollNameHash != null && Object.hasOwnProperty.call(message, "pollNameHash"))
                    object.pollNameHash = options.bytes === String ? $util.base64.encode(message.pollNameHash, 0, message.pollNameHash.length) : options.bytes === Array ? Array.prototype.slice.call(message.pollNameHash) : message.pollNameHash;
                return object;
            };

            /**
             * Converts this PollNameHashHistoryEntry to JSON.
             * @function toJSON
             * @memberof proto.PollAdditionalMetadata.PollNameHashHistoryEntry
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PollNameHashHistoryEntry.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PollNameHashHistoryEntry
             * @function getTypeUrl
             * @memberof proto.PollAdditionalMetadata.PollNameHashHistoryEntry
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PollNameHashHistoryEntry.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.PollAdditionalMetadata.PollNameHashHistoryEntry";
            };

            return PollNameHashHistoryEntry;
        })();

        return PollAdditionalMetadata;
    })();

    proto.PollEncValue = (function() {

        /**
         * Properties of a PollEncValue.
         * @memberof proto
         * @interface IPollEncValue
         * @property {Uint8Array|null} [encPayload] PollEncValue encPayload
         * @property {Uint8Array|null} [encIv] PollEncValue encIv
         */

        /**
         * Constructs a new PollEncValue.
         * @memberof proto
         * @classdesc Represents a PollEncValue.
         * @implements IPollEncValue
         * @constructor
         * @param {proto.IPollEncValue=} [properties] Properties to set
         */
        function PollEncValue(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PollEncValue encPayload.
         * @member {Uint8Array|null|undefined} encPayload
         * @memberof proto.PollEncValue
         * @instance
         */
        PollEncValue.prototype.encPayload = null;

        /**
         * PollEncValue encIv.
         * @member {Uint8Array|null|undefined} encIv
         * @memberof proto.PollEncValue
         * @instance
         */
        PollEncValue.prototype.encIv = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollEncValue.prototype, "_encPayload", {
            get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollEncValue.prototype, "_encIv", {
            get: $util.oneOfGetter($oneOfFields = ["encIv"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PollEncValue instance using the specified properties.
         * @function create
         * @memberof proto.PollEncValue
         * @static
         * @param {proto.IPollEncValue=} [properties] Properties to set
         * @returns {proto.PollEncValue} PollEncValue instance
         */
        PollEncValue.create = function create(properties) {
            return new PollEncValue(properties);
        };

        /**
         * Encodes the specified PollEncValue message. Does not implicitly {@link proto.PollEncValue.verify|verify} messages.
         * @function encode
         * @memberof proto.PollEncValue
         * @static
         * @param {proto.IPollEncValue} message PollEncValue message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PollEncValue.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encPayload);
            if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encIv);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PollEncValue message, length delimited. Does not implicitly {@link proto.PollEncValue.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.PollEncValue
         * @static
         * @param {proto.IPollEncValue} message PollEncValue message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PollEncValue.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PollEncValue message from the specified reader or buffer.
         * @function decode
         * @memberof proto.PollEncValue
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.PollEncValue} PollEncValue
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PollEncValue.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.encPayload = reader.bytes();
                        message._encPayload = "encPayload";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.encIv = reader.bytes();
                        message._encIv = "encIv";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a PollEncValue message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.PollEncValue
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.PollEncValue} PollEncValue
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PollEncValue.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PollEncValue message.
         * @function verify
         * @memberof proto.PollEncValue
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PollEncValue.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload")) {
                properties._encPayload = 1;
                if (!(message.encPayload && typeof message.encPayload.length === "number" || $util.isString(message.encPayload)))
                    return "encPayload: buffer expected";
            }
            if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv")) {
                properties._encIv = 1;
                if (!(message.encIv && typeof message.encIv.length === "number" || $util.isString(message.encIv)))
                    return "encIv: buffer expected";
            }
            return null;
        };

        /**
         * Creates a PollEncValue message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.PollEncValue
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.PollEncValue} PollEncValue
         */
        PollEncValue.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.PollEncValue: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.encPayload != null)
                if (typeof object.encPayload === "string")
                    $util.base64.decode(object.encPayload, message.encPayload = $util.newBuffer($util.base64.length(object.encPayload)), 0);
                else if (object.encPayload.length >= 0)
                    message.encPayload = object.encPayload;
            if (object.encIv != null)
                if (typeof object.encIv === "string")
                    $util.base64.decode(object.encIv, message.encIv = $util.newBuffer($util.base64.length(object.encIv)), 0);
                else if (object.encIv.length >= 0)
                    message.encIv = object.encIv;
            return message;
        };

        /**
         * Creates a plain object from a PollEncValue message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.PollEncValue
         * @static
         * @param {proto.PollEncValue} message PollEncValue
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PollEncValue.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.encPayload != null && Object.hasOwnProperty.call(message, "encPayload"))
                object.encPayload = options.bytes === String ? $util.base64.encode(message.encPayload, 0, message.encPayload.length) : options.bytes === Array ? Array.prototype.slice.call(message.encPayload) : message.encPayload;
            if (message.encIv != null && Object.hasOwnProperty.call(message, "encIv"))
                object.encIv = options.bytes === String ? $util.base64.encode(message.encIv, 0, message.encIv.length) : options.bytes === Array ? Array.prototype.slice.call(message.encIv) : message.encIv;
            return object;
        };

        /**
         * Converts this PollEncValue to JSON.
         * @function toJSON
         * @memberof proto.PollEncValue
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PollEncValue.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PollEncValue
         * @function getTypeUrl
         * @memberof proto.PollEncValue
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PollEncValue.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.PollEncValue";
        };

        return PollEncValue;
    })();

    proto.PollUpdate = (function() {

        /**
         * Properties of a PollUpdate.
         * @memberof proto
         * @interface IPollUpdate
         * @property {proto.IMessageKey|null} [pollUpdateMessageKey] PollUpdate pollUpdateMessageKey
         * @property {proto.Message.IPollVoteMessage|null} [vote] PollUpdate vote
         * @property {number|Long|null} [senderTimestampMs] PollUpdate senderTimestampMs
         * @property {number|Long|null} [serverTimestampMs] PollUpdate serverTimestampMs
         * @property {boolean|null} [unread] PollUpdate unread
         * @property {proto.Message.IPollUpdateMessageMetadata|null} [metadata] PollUpdate metadata
         */

        /**
         * Constructs a new PollUpdate.
         * @memberof proto
         * @classdesc Represents a PollUpdate.
         * @implements IPollUpdate
         * @constructor
         * @param {proto.IPollUpdate=} [properties] Properties to set
         */
        function PollUpdate(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PollUpdate pollUpdateMessageKey.
         * @member {proto.IMessageKey|null|undefined} pollUpdateMessageKey
         * @memberof proto.PollUpdate
         * @instance
         */
        PollUpdate.prototype.pollUpdateMessageKey = null;

        /**
         * PollUpdate vote.
         * @member {proto.Message.IPollVoteMessage|null|undefined} vote
         * @memberof proto.PollUpdate
         * @instance
         */
        PollUpdate.prototype.vote = null;

        /**
         * PollUpdate senderTimestampMs.
         * @member {number|Long|null|undefined} senderTimestampMs
         * @memberof proto.PollUpdate
         * @instance
         */
        PollUpdate.prototype.senderTimestampMs = null;

        /**
         * PollUpdate serverTimestampMs.
         * @member {number|Long|null|undefined} serverTimestampMs
         * @memberof proto.PollUpdate
         * @instance
         */
        PollUpdate.prototype.serverTimestampMs = null;

        /**
         * PollUpdate unread.
         * @member {boolean|null|undefined} unread
         * @memberof proto.PollUpdate
         * @instance
         */
        PollUpdate.prototype.unread = null;

        /**
         * PollUpdate metadata.
         * @member {proto.Message.IPollUpdateMessageMetadata|null|undefined} metadata
         * @memberof proto.PollUpdate
         * @instance
         */
        PollUpdate.prototype.metadata = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollUpdate.prototype, "_pollUpdateMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["pollUpdateMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollUpdate.prototype, "_vote", {
            get: $util.oneOfGetter($oneOfFields = ["vote"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollUpdate.prototype, "_senderTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["senderTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollUpdate.prototype, "_serverTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["serverTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollUpdate.prototype, "_unread", {
            get: $util.oneOfGetter($oneOfFields = ["unread"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollUpdate.prototype, "_metadata", {
            get: $util.oneOfGetter($oneOfFields = ["metadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PollUpdate instance using the specified properties.
         * @function create
         * @memberof proto.PollUpdate
         * @static
         * @param {proto.IPollUpdate=} [properties] Properties to set
         * @returns {proto.PollUpdate} PollUpdate instance
         */
        PollUpdate.create = function create(properties) {
            return new PollUpdate(properties);
        };

        /**
         * Encodes the specified PollUpdate message. Does not implicitly {@link proto.PollUpdate.verify|verify} messages.
         * @function encode
         * @memberof proto.PollUpdate
         * @static
         * @param {proto.IPollUpdate} message PollUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PollUpdate.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.pollUpdateMessageKey != null && Object.hasOwnProperty.call(message, "pollUpdateMessageKey"))
                $root.proto.MessageKey.encode(message.pollUpdateMessageKey, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.vote != null && Object.hasOwnProperty.call(message, "vote"))
                $root.proto.Message.PollVoteMessage.encode(message.vote, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.senderTimestampMs);
            if (message.serverTimestampMs != null && Object.hasOwnProperty.call(message, "serverTimestampMs"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.serverTimestampMs);
            if (message.unread != null && Object.hasOwnProperty.call(message, "unread"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.unread);
            if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata"))
                $root.proto.Message.PollUpdateMessageMetadata.encode(message.metadata, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PollUpdate message, length delimited. Does not implicitly {@link proto.PollUpdate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.PollUpdate
         * @static
         * @param {proto.IPollUpdate} message PollUpdate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PollUpdate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PollUpdate message from the specified reader or buffer.
         * @function decode
         * @memberof proto.PollUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.PollUpdate} PollUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PollUpdate.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.pollUpdateMessageKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.pollUpdateMessageKey);
                        message._pollUpdateMessageKey = "pollUpdateMessageKey";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.vote = $root.proto.Message.PollVoteMessage.decode(reader, reader.uint32(), undefined, q + 1, message.vote);
                        message._vote = "vote";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.senderTimestampMs = reader.int64();
                        message._senderTimestampMs = "senderTimestampMs";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.serverTimestampMs = reader.int64();
                        message._serverTimestampMs = "serverTimestampMs";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.unread = reader.bool();
                        message._unread = "unread";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        message.metadata = $root.proto.Message.PollUpdateMessageMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.metadata);
                        message._metadata = "metadata";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a PollUpdate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.PollUpdate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.PollUpdate} PollUpdate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PollUpdate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PollUpdate message.
         * @function verify
         * @memberof proto.PollUpdate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PollUpdate.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.pollUpdateMessageKey != null && Object.hasOwnProperty.call(message, "pollUpdateMessageKey")) {
                properties._pollUpdateMessageKey = 1;
                {
                    var error = $root.proto.MessageKey.verify(message.pollUpdateMessageKey, q + 1);
                    if (error)
                        return "pollUpdateMessageKey." + error;
                }
            }
            if (message.vote != null && Object.hasOwnProperty.call(message, "vote")) {
                properties._vote = 1;
                {
                    var error = $root.proto.Message.PollVoteMessage.verify(message.vote, q + 1);
                    if (error)
                        return "vote." + error;
                }
            }
            if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs")) {
                properties._senderTimestampMs = 1;
                if (!$util.isInteger(message.senderTimestampMs) && !(message.senderTimestampMs && $util.isInteger(message.senderTimestampMs.low) && $util.isInteger(message.senderTimestampMs.high)))
                    return "senderTimestampMs: integer|Long expected";
            }
            if (message.serverTimestampMs != null && Object.hasOwnProperty.call(message, "serverTimestampMs")) {
                properties._serverTimestampMs = 1;
                if (!$util.isInteger(message.serverTimestampMs) && !(message.serverTimestampMs && $util.isInteger(message.serverTimestampMs.low) && $util.isInteger(message.serverTimestampMs.high)))
                    return "serverTimestampMs: integer|Long expected";
            }
            if (message.unread != null && Object.hasOwnProperty.call(message, "unread")) {
                properties._unread = 1;
                if (typeof message.unread !== "boolean")
                    return "unread: boolean expected";
            }
            if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata")) {
                properties._metadata = 1;
                {
                    var error = $root.proto.Message.PollUpdateMessageMetadata.verify(message.metadata, q + 1);
                    if (error)
                        return "metadata." + error;
                }
            }
            return null;
        };

        /**
         * Creates a PollUpdate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.PollUpdate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.PollUpdate} PollUpdate
         */
        PollUpdate.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.PollUpdate: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.pollUpdateMessageKey != null) {
                if (!$util.isObject(object.pollUpdateMessageKey))
                    throw TypeError(".proto.PollUpdate.pollUpdateMessageKey: object expected");
                message.pollUpdateMessageKey = $root.proto.MessageKey.fromObject(object.pollUpdateMessageKey, q + 1);
            }
            if (object.vote != null) {
                if (!$util.isObject(object.vote))
                    throw TypeError(".proto.PollUpdate.vote: object expected");
                message.vote = $root.proto.Message.PollVoteMessage.fromObject(object.vote, q + 1);
            }
            if (object.senderTimestampMs != null)
                if ($util.Long)
                    message.senderTimestampMs = $util.Long.fromValue(object.senderTimestampMs, false);
                else if (typeof object.senderTimestampMs === "string")
                    message.senderTimestampMs = parseInt(object.senderTimestampMs, 10);
                else if (typeof object.senderTimestampMs === "number")
                    message.senderTimestampMs = object.senderTimestampMs;
                else if (typeof object.senderTimestampMs === "object")
                    message.senderTimestampMs = new $util.LongBits(object.senderTimestampMs.low >>> 0, object.senderTimestampMs.high >>> 0).toNumber();
            if (object.serverTimestampMs != null)
                if ($util.Long)
                    message.serverTimestampMs = $util.Long.fromValue(object.serverTimestampMs, false);
                else if (typeof object.serverTimestampMs === "string")
                    message.serverTimestampMs = parseInt(object.serverTimestampMs, 10);
                else if (typeof object.serverTimestampMs === "number")
                    message.serverTimestampMs = object.serverTimestampMs;
                else if (typeof object.serverTimestampMs === "object")
                    message.serverTimestampMs = new $util.LongBits(object.serverTimestampMs.low >>> 0, object.serverTimestampMs.high >>> 0).toNumber();
            if (object.unread != null)
                message.unread = Boolean(object.unread);
            if (object.metadata != null) {
                if (!$util.isObject(object.metadata))
                    throw TypeError(".proto.PollUpdate.metadata: object expected");
                message.metadata = $root.proto.Message.PollUpdateMessageMetadata.fromObject(object.metadata, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a PollUpdate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.PollUpdate
         * @static
         * @param {proto.PollUpdate} message PollUpdate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PollUpdate.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.pollUpdateMessageKey != null && Object.hasOwnProperty.call(message, "pollUpdateMessageKey"))
                object.pollUpdateMessageKey = $root.proto.MessageKey.toObject(message.pollUpdateMessageKey, options, q + 1);
            if (message.vote != null && Object.hasOwnProperty.call(message, "vote"))
                object.vote = $root.proto.Message.PollVoteMessage.toObject(message.vote, options, q + 1);
            if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.senderTimestampMs = typeof message.senderTimestampMs === "number" ? BigInt(message.senderTimestampMs) : $util.Long.fromBits(message.senderTimestampMs.low >>> 0, message.senderTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof message.senderTimestampMs === "number")
                    object.senderTimestampMs = options.longs === String ? String(message.senderTimestampMs) : message.senderTimestampMs;
                else
                    object.senderTimestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.senderTimestampMs) : options.longs === Number ? new $util.LongBits(message.senderTimestampMs.low >>> 0, message.senderTimestampMs.high >>> 0).toNumber() : message.senderTimestampMs;
            if (message.serverTimestampMs != null && Object.hasOwnProperty.call(message, "serverTimestampMs"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.serverTimestampMs = typeof message.serverTimestampMs === "number" ? BigInt(message.serverTimestampMs) : $util.Long.fromBits(message.serverTimestampMs.low >>> 0, message.serverTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof message.serverTimestampMs === "number")
                    object.serverTimestampMs = options.longs === String ? String(message.serverTimestampMs) : message.serverTimestampMs;
                else
                    object.serverTimestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.serverTimestampMs) : options.longs === Number ? new $util.LongBits(message.serverTimestampMs.low >>> 0, message.serverTimestampMs.high >>> 0).toNumber() : message.serverTimestampMs;
            if (message.unread != null && Object.hasOwnProperty.call(message, "unread"))
                object.unread = message.unread;
            if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata"))
                object.metadata = $root.proto.Message.PollUpdateMessageMetadata.toObject(message.metadata, options, q + 1);
            return object;
        };

        /**
         * Converts this PollUpdate to JSON.
         * @function toJSON
         * @memberof proto.PollUpdate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PollUpdate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PollUpdate
         * @function getTypeUrl
         * @memberof proto.PollUpdate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PollUpdate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.PollUpdate";
        };

        return PollUpdate;
    })();

    proto.PreKeyRecordStructure = (function() {

        /**
         * Properties of a PreKeyRecordStructure.
         * @memberof proto
         * @interface IPreKeyRecordStructure
         * @property {number|null} [id] PreKeyRecordStructure id
         * @property {Uint8Array|null} [publicKey] PreKeyRecordStructure publicKey
         * @property {Uint8Array|null} [privateKey] PreKeyRecordStructure privateKey
         */

        /**
         * Constructs a new PreKeyRecordStructure.
         * @memberof proto
         * @classdesc Represents a PreKeyRecordStructure.
         * @implements IPreKeyRecordStructure
         * @constructor
         * @param {proto.IPreKeyRecordStructure=} [properties] Properties to set
         */
        function PreKeyRecordStructure(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PreKeyRecordStructure id.
         * @member {number|null|undefined} id
         * @memberof proto.PreKeyRecordStructure
         * @instance
         */
        PreKeyRecordStructure.prototype.id = null;

        /**
         * PreKeyRecordStructure publicKey.
         * @member {Uint8Array|null|undefined} publicKey
         * @memberof proto.PreKeyRecordStructure
         * @instance
         */
        PreKeyRecordStructure.prototype.publicKey = null;

        /**
         * PreKeyRecordStructure privateKey.
         * @member {Uint8Array|null|undefined} privateKey
         * @memberof proto.PreKeyRecordStructure
         * @instance
         */
        PreKeyRecordStructure.prototype.privateKey = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PreKeyRecordStructure.prototype, "_id", {
            get: $util.oneOfGetter($oneOfFields = ["id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PreKeyRecordStructure.prototype, "_publicKey", {
            get: $util.oneOfGetter($oneOfFields = ["publicKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PreKeyRecordStructure.prototype, "_privateKey", {
            get: $util.oneOfGetter($oneOfFields = ["privateKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PreKeyRecordStructure instance using the specified properties.
         * @function create
         * @memberof proto.PreKeyRecordStructure
         * @static
         * @param {proto.IPreKeyRecordStructure=} [properties] Properties to set
         * @returns {proto.PreKeyRecordStructure} PreKeyRecordStructure instance
         */
        PreKeyRecordStructure.create = function create(properties) {
            return new PreKeyRecordStructure(properties);
        };

        /**
         * Encodes the specified PreKeyRecordStructure message. Does not implicitly {@link proto.PreKeyRecordStructure.verify|verify} messages.
         * @function encode
         * @memberof proto.PreKeyRecordStructure
         * @static
         * @param {proto.IPreKeyRecordStructure} message PreKeyRecordStructure message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PreKeyRecordStructure.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.id);
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.publicKey);
            if (message.privateKey != null && Object.hasOwnProperty.call(message, "privateKey"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.privateKey);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PreKeyRecordStructure message, length delimited. Does not implicitly {@link proto.PreKeyRecordStructure.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.PreKeyRecordStructure
         * @static
         * @param {proto.IPreKeyRecordStructure} message PreKeyRecordStructure message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PreKeyRecordStructure.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PreKeyRecordStructure message from the specified reader or buffer.
         * @function decode
         * @memberof proto.PreKeyRecordStructure
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.PreKeyRecordStructure} PreKeyRecordStructure
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PreKeyRecordStructure.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.id = reader.uint32();
                        message._id = "id";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.publicKey = reader.bytes();
                        message._publicKey = "publicKey";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.privateKey = reader.bytes();
                        message._privateKey = "privateKey";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a PreKeyRecordStructure message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.PreKeyRecordStructure
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.PreKeyRecordStructure} PreKeyRecordStructure
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PreKeyRecordStructure.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PreKeyRecordStructure message.
         * @function verify
         * @memberof proto.PreKeyRecordStructure
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PreKeyRecordStructure.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.id != null && Object.hasOwnProperty.call(message, "id")) {
                properties._id = 1;
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            }
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey")) {
                properties._publicKey = 1;
                if (!(message.publicKey && typeof message.publicKey.length === "number" || $util.isString(message.publicKey)))
                    return "publicKey: buffer expected";
            }
            if (message.privateKey != null && Object.hasOwnProperty.call(message, "privateKey")) {
                properties._privateKey = 1;
                if (!(message.privateKey && typeof message.privateKey.length === "number" || $util.isString(message.privateKey)))
                    return "privateKey: buffer expected";
            }
            return null;
        };

        /**
         * Creates a PreKeyRecordStructure message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.PreKeyRecordStructure
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.PreKeyRecordStructure} PreKeyRecordStructure
         */
        PreKeyRecordStructure.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.PreKeyRecordStructure: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.id != null)
                message.id = object.id >>> 0;
            if (object.publicKey != null)
                if (typeof object.publicKey === "string")
                    $util.base64.decode(object.publicKey, message.publicKey = $util.newBuffer($util.base64.length(object.publicKey)), 0);
                else if (object.publicKey.length >= 0)
                    message.publicKey = object.publicKey;
            if (object.privateKey != null)
                if (typeof object.privateKey === "string")
                    $util.base64.decode(object.privateKey, message.privateKey = $util.newBuffer($util.base64.length(object.privateKey)), 0);
                else if (object.privateKey.length >= 0)
                    message.privateKey = object.privateKey;
            return message;
        };

        /**
         * Creates a plain object from a PreKeyRecordStructure message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.PreKeyRecordStructure
         * @static
         * @param {proto.PreKeyRecordStructure} message PreKeyRecordStructure
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PreKeyRecordStructure.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                object.publicKey = options.bytes === String ? $util.base64.encode(message.publicKey, 0, message.publicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.publicKey) : message.publicKey;
            if (message.privateKey != null && Object.hasOwnProperty.call(message, "privateKey"))
                object.privateKey = options.bytes === String ? $util.base64.encode(message.privateKey, 0, message.privateKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.privateKey) : message.privateKey;
            return object;
        };

        /**
         * Converts this PreKeyRecordStructure to JSON.
         * @function toJSON
         * @memberof proto.PreKeyRecordStructure
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PreKeyRecordStructure.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PreKeyRecordStructure
         * @function getTypeUrl
         * @memberof proto.PreKeyRecordStructure
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PreKeyRecordStructure.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.PreKeyRecordStructure";
        };

        return PreKeyRecordStructure;
    })();

    proto.PreKeySignalMessage = (function() {

        /**
         * Properties of a PreKeySignalMessage.
         * @memberof proto
         * @interface IPreKeySignalMessage
         * @property {number|null} [registrationId] PreKeySignalMessage registrationId
         * @property {number|null} [preKeyId] PreKeySignalMessage preKeyId
         * @property {number|null} [signedPreKeyId] PreKeySignalMessage signedPreKeyId
         * @property {Uint8Array|null} [baseKey] PreKeySignalMessage baseKey
         * @property {Uint8Array|null} [identityKey] PreKeySignalMessage identityKey
         * @property {Uint8Array|null} [message] PreKeySignalMessage message
         * @property {number|null} [kyberPreKeyId] PreKeySignalMessage kyberPreKeyId
         * @property {Uint8Array|null} [kyberCiphertext] PreKeySignalMessage kyberCiphertext
         */

        /**
         * Constructs a new PreKeySignalMessage.
         * @memberof proto
         * @classdesc Represents a PreKeySignalMessage.
         * @implements IPreKeySignalMessage
         * @constructor
         * @param {proto.IPreKeySignalMessage=} [properties] Properties to set
         */
        function PreKeySignalMessage(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PreKeySignalMessage registrationId.
         * @member {number|null|undefined} registrationId
         * @memberof proto.PreKeySignalMessage
         * @instance
         */
        PreKeySignalMessage.prototype.registrationId = null;

        /**
         * PreKeySignalMessage preKeyId.
         * @member {number|null|undefined} preKeyId
         * @memberof proto.PreKeySignalMessage
         * @instance
         */
        PreKeySignalMessage.prototype.preKeyId = null;

        /**
         * PreKeySignalMessage signedPreKeyId.
         * @member {number|null|undefined} signedPreKeyId
         * @memberof proto.PreKeySignalMessage
         * @instance
         */
        PreKeySignalMessage.prototype.signedPreKeyId = null;

        /**
         * PreKeySignalMessage baseKey.
         * @member {Uint8Array|null|undefined} baseKey
         * @memberof proto.PreKeySignalMessage
         * @instance
         */
        PreKeySignalMessage.prototype.baseKey = null;

        /**
         * PreKeySignalMessage identityKey.
         * @member {Uint8Array|null|undefined} identityKey
         * @memberof proto.PreKeySignalMessage
         * @instance
         */
        PreKeySignalMessage.prototype.identityKey = null;

        /**
         * PreKeySignalMessage message.
         * @member {Uint8Array|null|undefined} message
         * @memberof proto.PreKeySignalMessage
         * @instance
         */
        PreKeySignalMessage.prototype.message = null;

        /**
         * PreKeySignalMessage kyberPreKeyId.
         * @member {number|null|undefined} kyberPreKeyId
         * @memberof proto.PreKeySignalMessage
         * @instance
         */
        PreKeySignalMessage.prototype.kyberPreKeyId = null;

        /**
         * PreKeySignalMessage kyberCiphertext.
         * @member {Uint8Array|null|undefined} kyberCiphertext
         * @memberof proto.PreKeySignalMessage
         * @instance
         */
        PreKeySignalMessage.prototype.kyberCiphertext = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PreKeySignalMessage.prototype, "_registrationId", {
            get: $util.oneOfGetter($oneOfFields = ["registrationId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PreKeySignalMessage.prototype, "_preKeyId", {
            get: $util.oneOfGetter($oneOfFields = ["preKeyId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PreKeySignalMessage.prototype, "_signedPreKeyId", {
            get: $util.oneOfGetter($oneOfFields = ["signedPreKeyId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PreKeySignalMessage.prototype, "_baseKey", {
            get: $util.oneOfGetter($oneOfFields = ["baseKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PreKeySignalMessage.prototype, "_identityKey", {
            get: $util.oneOfGetter($oneOfFields = ["identityKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PreKeySignalMessage.prototype, "_message", {
            get: $util.oneOfGetter($oneOfFields = ["message"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PreKeySignalMessage.prototype, "_kyberPreKeyId", {
            get: $util.oneOfGetter($oneOfFields = ["kyberPreKeyId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PreKeySignalMessage.prototype, "_kyberCiphertext", {
            get: $util.oneOfGetter($oneOfFields = ["kyberCiphertext"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PreKeySignalMessage instance using the specified properties.
         * @function create
         * @memberof proto.PreKeySignalMessage
         * @static
         * @param {proto.IPreKeySignalMessage=} [properties] Properties to set
         * @returns {proto.PreKeySignalMessage} PreKeySignalMessage instance
         */
        PreKeySignalMessage.create = function create(properties) {
            return new PreKeySignalMessage(properties);
        };

        /**
         * Encodes the specified PreKeySignalMessage message. Does not implicitly {@link proto.PreKeySignalMessage.verify|verify} messages.
         * @function encode
         * @memberof proto.PreKeySignalMessage
         * @static
         * @param {proto.IPreKeySignalMessage} message PreKeySignalMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PreKeySignalMessage.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.preKeyId != null && Object.hasOwnProperty.call(message, "preKeyId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.preKeyId);
            if (message.baseKey != null && Object.hasOwnProperty.call(message, "baseKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.baseKey);
            if (message.identityKey != null && Object.hasOwnProperty.call(message, "identityKey"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.identityKey);
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.message);
            if (message.registrationId != null && Object.hasOwnProperty.call(message, "registrationId"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.registrationId);
            if (message.signedPreKeyId != null && Object.hasOwnProperty.call(message, "signedPreKeyId"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.signedPreKeyId);
            if (message.kyberPreKeyId != null && Object.hasOwnProperty.call(message, "kyberPreKeyId"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.kyberPreKeyId);
            if (message.kyberCiphertext != null && Object.hasOwnProperty.call(message, "kyberCiphertext"))
                writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.kyberCiphertext);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PreKeySignalMessage message, length delimited. Does not implicitly {@link proto.PreKeySignalMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.PreKeySignalMessage
         * @static
         * @param {proto.IPreKeySignalMessage} message PreKeySignalMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PreKeySignalMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PreKeySignalMessage message from the specified reader or buffer.
         * @function decode
         * @memberof proto.PreKeySignalMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.PreKeySignalMessage} PreKeySignalMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PreKeySignalMessage.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 5: {
                        if (u !== 0)
                            break;
                        message.registrationId = reader.uint32();
                        message._registrationId = "registrationId";
                        continue;
                    }
                case 1: {
                        if (u !== 0)
                            break;
                        message.preKeyId = reader.uint32();
                        message._preKeyId = "preKeyId";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        message.signedPreKeyId = reader.uint32();
                        message._signedPreKeyId = "signedPreKeyId";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.baseKey = reader.bytes();
                        message._baseKey = "baseKey";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.identityKey = reader.bytes();
                        message._identityKey = "identityKey";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.message = reader.bytes();
                        message._message = "message";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        message.kyberPreKeyId = reader.uint32();
                        message._kyberPreKeyId = "kyberPreKeyId";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        message.kyberCiphertext = reader.bytes();
                        message._kyberCiphertext = "kyberCiphertext";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a PreKeySignalMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.PreKeySignalMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.PreKeySignalMessage} PreKeySignalMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PreKeySignalMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PreKeySignalMessage message.
         * @function verify
         * @memberof proto.PreKeySignalMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PreKeySignalMessage.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.registrationId != null && Object.hasOwnProperty.call(message, "registrationId")) {
                properties._registrationId = 1;
                if (!$util.isInteger(message.registrationId))
                    return "registrationId: integer expected";
            }
            if (message.preKeyId != null && Object.hasOwnProperty.call(message, "preKeyId")) {
                properties._preKeyId = 1;
                if (!$util.isInteger(message.preKeyId))
                    return "preKeyId: integer expected";
            }
            if (message.signedPreKeyId != null && Object.hasOwnProperty.call(message, "signedPreKeyId")) {
                properties._signedPreKeyId = 1;
                if (!$util.isInteger(message.signedPreKeyId))
                    return "signedPreKeyId: integer expected";
            }
            if (message.baseKey != null && Object.hasOwnProperty.call(message, "baseKey")) {
                properties._baseKey = 1;
                if (!(message.baseKey && typeof message.baseKey.length === "number" || $util.isString(message.baseKey)))
                    return "baseKey: buffer expected";
            }
            if (message.identityKey != null && Object.hasOwnProperty.call(message, "identityKey")) {
                properties._identityKey = 1;
                if (!(message.identityKey && typeof message.identityKey.length === "number" || $util.isString(message.identityKey)))
                    return "identityKey: buffer expected";
            }
            if (message.message != null && Object.hasOwnProperty.call(message, "message")) {
                properties._message = 1;
                if (!(message.message && typeof message.message.length === "number" || $util.isString(message.message)))
                    return "message: buffer expected";
            }
            if (message.kyberPreKeyId != null && Object.hasOwnProperty.call(message, "kyberPreKeyId")) {
                properties._kyberPreKeyId = 1;
                if (!$util.isInteger(message.kyberPreKeyId))
                    return "kyberPreKeyId: integer expected";
            }
            if (message.kyberCiphertext != null && Object.hasOwnProperty.call(message, "kyberCiphertext")) {
                properties._kyberCiphertext = 1;
                if (!(message.kyberCiphertext && typeof message.kyberCiphertext.length === "number" || $util.isString(message.kyberCiphertext)))
                    return "kyberCiphertext: buffer expected";
            }
            return null;
        };

        /**
         * Creates a PreKeySignalMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.PreKeySignalMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.PreKeySignalMessage} PreKeySignalMessage
         */
        PreKeySignalMessage.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.PreKeySignalMessage: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.registrationId != null)
                message.registrationId = object.registrationId >>> 0;
            if (object.preKeyId != null)
                message.preKeyId = object.preKeyId >>> 0;
            if (object.signedPreKeyId != null)
                message.signedPreKeyId = object.signedPreKeyId >>> 0;
            if (object.baseKey != null)
                if (typeof object.baseKey === "string")
                    $util.base64.decode(object.baseKey, message.baseKey = $util.newBuffer($util.base64.length(object.baseKey)), 0);
                else if (object.baseKey.length >= 0)
                    message.baseKey = object.baseKey;
            if (object.identityKey != null)
                if (typeof object.identityKey === "string")
                    $util.base64.decode(object.identityKey, message.identityKey = $util.newBuffer($util.base64.length(object.identityKey)), 0);
                else if (object.identityKey.length >= 0)
                    message.identityKey = object.identityKey;
            if (object.message != null)
                if (typeof object.message === "string")
                    $util.base64.decode(object.message, message.message = $util.newBuffer($util.base64.length(object.message)), 0);
                else if (object.message.length >= 0)
                    message.message = object.message;
            if (object.kyberPreKeyId != null)
                message.kyberPreKeyId = object.kyberPreKeyId >>> 0;
            if (object.kyberCiphertext != null)
                if (typeof object.kyberCiphertext === "string")
                    $util.base64.decode(object.kyberCiphertext, message.kyberCiphertext = $util.newBuffer($util.base64.length(object.kyberCiphertext)), 0);
                else if (object.kyberCiphertext.length >= 0)
                    message.kyberCiphertext = object.kyberCiphertext;
            return message;
        };

        /**
         * Creates a plain object from a PreKeySignalMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.PreKeySignalMessage
         * @static
         * @param {proto.PreKeySignalMessage} message PreKeySignalMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PreKeySignalMessage.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.preKeyId != null && Object.hasOwnProperty.call(message, "preKeyId"))
                object.preKeyId = message.preKeyId;
            if (message.baseKey != null && Object.hasOwnProperty.call(message, "baseKey"))
                object.baseKey = options.bytes === String ? $util.base64.encode(message.baseKey, 0, message.baseKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.baseKey) : message.baseKey;
            if (message.identityKey != null && Object.hasOwnProperty.call(message, "identityKey"))
                object.identityKey = options.bytes === String ? $util.base64.encode(message.identityKey, 0, message.identityKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.identityKey) : message.identityKey;
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                object.message = options.bytes === String ? $util.base64.encode(message.message, 0, message.message.length) : options.bytes === Array ? Array.prototype.slice.call(message.message) : message.message;
            if (message.registrationId != null && Object.hasOwnProperty.call(message, "registrationId"))
                object.registrationId = message.registrationId;
            if (message.signedPreKeyId != null && Object.hasOwnProperty.call(message, "signedPreKeyId"))
                object.signedPreKeyId = message.signedPreKeyId;
            if (message.kyberPreKeyId != null && Object.hasOwnProperty.call(message, "kyberPreKeyId"))
                object.kyberPreKeyId = message.kyberPreKeyId;
            if (message.kyberCiphertext != null && Object.hasOwnProperty.call(message, "kyberCiphertext"))
                object.kyberCiphertext = options.bytes === String ? $util.base64.encode(message.kyberCiphertext, 0, message.kyberCiphertext.length) : options.bytes === Array ? Array.prototype.slice.call(message.kyberCiphertext) : message.kyberCiphertext;
            return object;
        };

        /**
         * Converts this PreKeySignalMessage to JSON.
         * @function toJSON
         * @memberof proto.PreKeySignalMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PreKeySignalMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PreKeySignalMessage
         * @function getTypeUrl
         * @memberof proto.PreKeySignalMessage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PreKeySignalMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.PreKeySignalMessage";
        };

        return PreKeySignalMessage;
    })();

    proto.PremiumMessageInfo = (function() {

        /**
         * Properties of a PremiumMessageInfo.
         * @memberof proto
         * @interface IPremiumMessageInfo
         * @property {string|null} [serverCampaignId] PremiumMessageInfo serverCampaignId
         */

        /**
         * Constructs a new PremiumMessageInfo.
         * @memberof proto
         * @classdesc Represents a PremiumMessageInfo.
         * @implements IPremiumMessageInfo
         * @constructor
         * @param {proto.IPremiumMessageInfo=} [properties] Properties to set
         */
        function PremiumMessageInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PremiumMessageInfo serverCampaignId.
         * @member {string|null|undefined} serverCampaignId
         * @memberof proto.PremiumMessageInfo
         * @instance
         */
        PremiumMessageInfo.prototype.serverCampaignId = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PremiumMessageInfo.prototype, "_serverCampaignId", {
            get: $util.oneOfGetter($oneOfFields = ["serverCampaignId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PremiumMessageInfo instance using the specified properties.
         * @function create
         * @memberof proto.PremiumMessageInfo
         * @static
         * @param {proto.IPremiumMessageInfo=} [properties] Properties to set
         * @returns {proto.PremiumMessageInfo} PremiumMessageInfo instance
         */
        PremiumMessageInfo.create = function create(properties) {
            return new PremiumMessageInfo(properties);
        };

        /**
         * Encodes the specified PremiumMessageInfo message. Does not implicitly {@link proto.PremiumMessageInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.PremiumMessageInfo
         * @static
         * @param {proto.IPremiumMessageInfo} message PremiumMessageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PremiumMessageInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.serverCampaignId != null && Object.hasOwnProperty.call(message, "serverCampaignId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.serverCampaignId);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PremiumMessageInfo message, length delimited. Does not implicitly {@link proto.PremiumMessageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.PremiumMessageInfo
         * @static
         * @param {proto.IPremiumMessageInfo} message PremiumMessageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PremiumMessageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PremiumMessageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.PremiumMessageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.PremiumMessageInfo} PremiumMessageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PremiumMessageInfo.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.serverCampaignId = reader.stringVerify();
                        message._serverCampaignId = "serverCampaignId";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a PremiumMessageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.PremiumMessageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.PremiumMessageInfo} PremiumMessageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PremiumMessageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PremiumMessageInfo message.
         * @function verify
         * @memberof proto.PremiumMessageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PremiumMessageInfo.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.serverCampaignId != null && Object.hasOwnProperty.call(message, "serverCampaignId")) {
                properties._serverCampaignId = 1;
                if (!$util.isString(message.serverCampaignId))
                    return "serverCampaignId: string expected";
            }
            return null;
        };

        /**
         * Creates a PremiumMessageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.PremiumMessageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.PremiumMessageInfo} PremiumMessageInfo
         */
        PremiumMessageInfo.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.PremiumMessageInfo: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.serverCampaignId != null)
                message.serverCampaignId = String(object.serverCampaignId);
            return message;
        };

        /**
         * Creates a plain object from a PremiumMessageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.PremiumMessageInfo
         * @static
         * @param {proto.PremiumMessageInfo} message PremiumMessageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PremiumMessageInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.serverCampaignId != null && Object.hasOwnProperty.call(message, "serverCampaignId"))
                object.serverCampaignId = message.serverCampaignId;
            return object;
        };

        /**
         * Converts this PremiumMessageInfo to JSON.
         * @function toJSON
         * @memberof proto.PremiumMessageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PremiumMessageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PremiumMessageInfo
         * @function getTypeUrl
         * @memberof proto.PremiumMessageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PremiumMessageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.PremiumMessageInfo";
        };

        return PremiumMessageInfo;
    })();

    proto.PrimaryEphemeralIdentity = (function() {

        /**
         * Properties of a PrimaryEphemeralIdentity.
         * @memberof proto
         * @interface IPrimaryEphemeralIdentity
         * @property {Uint8Array|null} [publicKey] PrimaryEphemeralIdentity publicKey
         * @property {Uint8Array|null} [nonce] PrimaryEphemeralIdentity nonce
         */

        /**
         * Constructs a new PrimaryEphemeralIdentity.
         * @memberof proto
         * @classdesc Represents a PrimaryEphemeralIdentity.
         * @implements IPrimaryEphemeralIdentity
         * @constructor
         * @param {proto.IPrimaryEphemeralIdentity=} [properties] Properties to set
         */
        function PrimaryEphemeralIdentity(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PrimaryEphemeralIdentity publicKey.
         * @member {Uint8Array|null|undefined} publicKey
         * @memberof proto.PrimaryEphemeralIdentity
         * @instance
         */
        PrimaryEphemeralIdentity.prototype.publicKey = null;

        /**
         * PrimaryEphemeralIdentity nonce.
         * @member {Uint8Array|null|undefined} nonce
         * @memberof proto.PrimaryEphemeralIdentity
         * @instance
         */
        PrimaryEphemeralIdentity.prototype.nonce = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PrimaryEphemeralIdentity.prototype, "_publicKey", {
            get: $util.oneOfGetter($oneOfFields = ["publicKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PrimaryEphemeralIdentity.prototype, "_nonce", {
            get: $util.oneOfGetter($oneOfFields = ["nonce"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PrimaryEphemeralIdentity instance using the specified properties.
         * @function create
         * @memberof proto.PrimaryEphemeralIdentity
         * @static
         * @param {proto.IPrimaryEphemeralIdentity=} [properties] Properties to set
         * @returns {proto.PrimaryEphemeralIdentity} PrimaryEphemeralIdentity instance
         */
        PrimaryEphemeralIdentity.create = function create(properties) {
            return new PrimaryEphemeralIdentity(properties);
        };

        /**
         * Encodes the specified PrimaryEphemeralIdentity message. Does not implicitly {@link proto.PrimaryEphemeralIdentity.verify|verify} messages.
         * @function encode
         * @memberof proto.PrimaryEphemeralIdentity
         * @static
         * @param {proto.IPrimaryEphemeralIdentity} message PrimaryEphemeralIdentity message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PrimaryEphemeralIdentity.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.publicKey);
            if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.nonce);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified PrimaryEphemeralIdentity message, length delimited. Does not implicitly {@link proto.PrimaryEphemeralIdentity.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.PrimaryEphemeralIdentity
         * @static
         * @param {proto.IPrimaryEphemeralIdentity} message PrimaryEphemeralIdentity message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PrimaryEphemeralIdentity.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PrimaryEphemeralIdentity message from the specified reader or buffer.
         * @function decode
         * @memberof proto.PrimaryEphemeralIdentity
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.PrimaryEphemeralIdentity} PrimaryEphemeralIdentity
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PrimaryEphemeralIdentity.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.publicKey = reader.bytes();
                        message._publicKey = "publicKey";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.nonce = reader.bytes();
                        message._nonce = "nonce";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a PrimaryEphemeralIdentity message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.PrimaryEphemeralIdentity
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.PrimaryEphemeralIdentity} PrimaryEphemeralIdentity
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PrimaryEphemeralIdentity.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PrimaryEphemeralIdentity message.
         * @function verify
         * @memberof proto.PrimaryEphemeralIdentity
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PrimaryEphemeralIdentity.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey")) {
                properties._publicKey = 1;
                if (!(message.publicKey && typeof message.publicKey.length === "number" || $util.isString(message.publicKey)))
                    return "publicKey: buffer expected";
            }
            if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce")) {
                properties._nonce = 1;
                if (!(message.nonce && typeof message.nonce.length === "number" || $util.isString(message.nonce)))
                    return "nonce: buffer expected";
            }
            return null;
        };

        /**
         * Creates a PrimaryEphemeralIdentity message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.PrimaryEphemeralIdentity
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.PrimaryEphemeralIdentity} PrimaryEphemeralIdentity
         */
        PrimaryEphemeralIdentity.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.PrimaryEphemeralIdentity: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.publicKey != null)
                if (typeof object.publicKey === "string")
                    $util.base64.decode(object.publicKey, message.publicKey = $util.newBuffer($util.base64.length(object.publicKey)), 0);
                else if (object.publicKey.length >= 0)
                    message.publicKey = object.publicKey;
            if (object.nonce != null)
                if (typeof object.nonce === "string")
                    $util.base64.decode(object.nonce, message.nonce = $util.newBuffer($util.base64.length(object.nonce)), 0);
                else if (object.nonce.length >= 0)
                    message.nonce = object.nonce;
            return message;
        };

        /**
         * Creates a plain object from a PrimaryEphemeralIdentity message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.PrimaryEphemeralIdentity
         * @static
         * @param {proto.PrimaryEphemeralIdentity} message PrimaryEphemeralIdentity
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PrimaryEphemeralIdentity.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                object.publicKey = options.bytes === String ? $util.base64.encode(message.publicKey, 0, message.publicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.publicKey) : message.publicKey;
            if (message.nonce != null && Object.hasOwnProperty.call(message, "nonce"))
                object.nonce = options.bytes === String ? $util.base64.encode(message.nonce, 0, message.nonce.length) : options.bytes === Array ? Array.prototype.slice.call(message.nonce) : message.nonce;
            return object;
        };

        /**
         * Converts this PrimaryEphemeralIdentity to JSON.
         * @function toJSON
         * @memberof proto.PrimaryEphemeralIdentity
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PrimaryEphemeralIdentity.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PrimaryEphemeralIdentity
         * @function getTypeUrl
         * @memberof proto.PrimaryEphemeralIdentity
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PrimaryEphemeralIdentity.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.PrimaryEphemeralIdentity";
        };

        return PrimaryEphemeralIdentity;
    })();

    /**
     * PrivacySystemMessage enum.
     * @name proto.PrivacySystemMessage
     * @enum {number}
     * @property {number} E2EE_MSG=1 E2EE_MSG value
     * @property {number} NE2EE_SELF=2 NE2EE_SELF value
     * @property {number} NE2EE_OTHER=3 NE2EE_OTHER value
     */
    proto.PrivacySystemMessage = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[1] = "E2EE_MSG"] = 1;
        values[valuesById[2] = "NE2EE_SELF"] = 2;
        values[valuesById[3] = "NE2EE_OTHER"] = 3;
        return values;
    })();

    proto.ProcessedVideo = (function() {

        /**
         * Properties of a ProcessedVideo.
         * @memberof proto
         * @interface IProcessedVideo
         * @property {string|null} [directPath] ProcessedVideo directPath
         * @property {Uint8Array|null} [fileSha256] ProcessedVideo fileSha256
         * @property {number|null} [height] ProcessedVideo height
         * @property {number|null} [width] ProcessedVideo width
         * @property {number|Long|null} [fileLength] ProcessedVideo fileLength
         * @property {number|null} [bitrate] ProcessedVideo bitrate
         * @property {proto.ProcessedVideo.VideoQuality|null} [quality] ProcessedVideo quality
         * @property {Array.<string>|null} [capabilities] ProcessedVideo capabilities
         */

        /**
         * Constructs a new ProcessedVideo.
         * @memberof proto
         * @classdesc Represents a ProcessedVideo.
         * @implements IProcessedVideo
         * @constructor
         * @param {proto.IProcessedVideo=} [properties] Properties to set
         */
        function ProcessedVideo(properties) {
            this.capabilities = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ProcessedVideo directPath.
         * @member {string|null|undefined} directPath
         * @memberof proto.ProcessedVideo
         * @instance
         */
        ProcessedVideo.prototype.directPath = null;

        /**
         * ProcessedVideo fileSha256.
         * @member {Uint8Array|null|undefined} fileSha256
         * @memberof proto.ProcessedVideo
         * @instance
         */
        ProcessedVideo.prototype.fileSha256 = null;

        /**
         * ProcessedVideo height.
         * @member {number|null|undefined} height
         * @memberof proto.ProcessedVideo
         * @instance
         */
        ProcessedVideo.prototype.height = null;

        /**
         * ProcessedVideo width.
         * @member {number|null|undefined} width
         * @memberof proto.ProcessedVideo
         * @instance
         */
        ProcessedVideo.prototype.width = null;

        /**
         * ProcessedVideo fileLength.
         * @member {number|Long|null|undefined} fileLength
         * @memberof proto.ProcessedVideo
         * @instance
         */
        ProcessedVideo.prototype.fileLength = null;

        /**
         * ProcessedVideo bitrate.
         * @member {number|null|undefined} bitrate
         * @memberof proto.ProcessedVideo
         * @instance
         */
        ProcessedVideo.prototype.bitrate = null;

        /**
         * ProcessedVideo quality.
         * @member {proto.ProcessedVideo.VideoQuality|null|undefined} quality
         * @memberof proto.ProcessedVideo
         * @instance
         */
        ProcessedVideo.prototype.quality = null;

        /**
         * ProcessedVideo capabilities.
         * @member {Array.<string>} capabilities
         * @memberof proto.ProcessedVideo
         * @instance
         */
        ProcessedVideo.prototype.capabilities = $util.emptyArray;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProcessedVideo.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProcessedVideo.prototype, "_fileSha256", {
            get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProcessedVideo.prototype, "_height", {
            get: $util.oneOfGetter($oneOfFields = ["height"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProcessedVideo.prototype, "_width", {
            get: $util.oneOfGetter($oneOfFields = ["width"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProcessedVideo.prototype, "_fileLength", {
            get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProcessedVideo.prototype, "_bitrate", {
            get: $util.oneOfGetter($oneOfFields = ["bitrate"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProcessedVideo.prototype, "_quality", {
            get: $util.oneOfGetter($oneOfFields = ["quality"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ProcessedVideo instance using the specified properties.
         * @function create
         * @memberof proto.ProcessedVideo
         * @static
         * @param {proto.IProcessedVideo=} [properties] Properties to set
         * @returns {proto.ProcessedVideo} ProcessedVideo instance
         */
        ProcessedVideo.create = function create(properties) {
            return new ProcessedVideo(properties);
        };

        /**
         * Encodes the specified ProcessedVideo message. Does not implicitly {@link proto.ProcessedVideo.verify|verify} messages.
         * @function encode
         * @memberof proto.ProcessedVideo
         * @static
         * @param {proto.IProcessedVideo} message ProcessedVideo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProcessedVideo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.directPath);
            if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.fileSha256);
            if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.height);
            if (message.width != null && Object.hasOwnProperty.call(message, "width"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.width);
            if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.fileLength);
            if (message.bitrate != null && Object.hasOwnProperty.call(message, "bitrate"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.bitrate);
            if (message.quality != null && Object.hasOwnProperty.call(message, "quality"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.quality);
            if (message.capabilities != null && message.capabilities.length)
                for (var i = 0; i < message.capabilities.length; ++i)
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.capabilities[i]);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ProcessedVideo message, length delimited. Does not implicitly {@link proto.ProcessedVideo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ProcessedVideo
         * @static
         * @param {proto.IProcessedVideo} message ProcessedVideo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProcessedVideo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ProcessedVideo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ProcessedVideo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ProcessedVideo} ProcessedVideo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProcessedVideo.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.directPath = reader.stringVerify();
                        message._directPath = "directPath";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.fileSha256 = reader.bytes();
                        message._fileSha256 = "fileSha256";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.height = reader.uint32();
                        message._height = "height";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.width = reader.uint32();
                        message._width = "width";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.fileLength = reader.uint64();
                        message._fileLength = "fileLength";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        message.bitrate = reader.uint32();
                        message._bitrate = "bitrate";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        message.quality = reader.int32();
                        message._quality = "quality";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        if (!(message.capabilities && message.capabilities.length))
                            message.capabilities = [];
                        message.capabilities.push(reader.stringVerify());
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a ProcessedVideo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ProcessedVideo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ProcessedVideo} ProcessedVideo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProcessedVideo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ProcessedVideo message.
         * @function verify
         * @memberof proto.ProcessedVideo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ProcessedVideo.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath")) {
                properties._directPath = 1;
                if (!$util.isString(message.directPath))
                    return "directPath: string expected";
            }
            if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256")) {
                properties._fileSha256 = 1;
                if (!(message.fileSha256 && typeof message.fileSha256.length === "number" || $util.isString(message.fileSha256)))
                    return "fileSha256: buffer expected";
            }
            if (message.height != null && Object.hasOwnProperty.call(message, "height")) {
                properties._height = 1;
                if (!$util.isInteger(message.height))
                    return "height: integer expected";
            }
            if (message.width != null && Object.hasOwnProperty.call(message, "width")) {
                properties._width = 1;
                if (!$util.isInteger(message.width))
                    return "width: integer expected";
            }
            if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength")) {
                properties._fileLength = 1;
                if (!$util.isInteger(message.fileLength) && !(message.fileLength && $util.isInteger(message.fileLength.low) && $util.isInteger(message.fileLength.high)))
                    return "fileLength: integer|Long expected";
            }
            if (message.bitrate != null && Object.hasOwnProperty.call(message, "bitrate")) {
                properties._bitrate = 1;
                if (!$util.isInteger(message.bitrate))
                    return "bitrate: integer expected";
            }
            if (message.quality != null && Object.hasOwnProperty.call(message, "quality")) {
                properties._quality = 1;
                if (typeof message.quality !== "number" || (message.quality | 0) !== message.quality)
                    return "quality: enum value expected";
            }
            if (message.capabilities != null && Object.hasOwnProperty.call(message, "capabilities")) {
                if (!Array.isArray(message.capabilities))
                    return "capabilities: array expected";
                for (var i = 0; i < message.capabilities.length; ++i)
                    if (!$util.isString(message.capabilities[i]))
                        return "capabilities: string[] expected";
            }
            return null;
        };

        /**
         * Creates a ProcessedVideo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ProcessedVideo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ProcessedVideo} ProcessedVideo
         */
        ProcessedVideo.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.ProcessedVideo: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.directPath != null)
                message.directPath = String(object.directPath);
            if (object.fileSha256 != null)
                if (typeof object.fileSha256 === "string")
                    $util.base64.decode(object.fileSha256, message.fileSha256 = $util.newBuffer($util.base64.length(object.fileSha256)), 0);
                else if (object.fileSha256.length >= 0)
                    message.fileSha256 = object.fileSha256;
            if (object.height != null)
                message.height = object.height >>> 0;
            if (object.width != null)
                message.width = object.width >>> 0;
            if (object.fileLength != null)
                if ($util.Long)
                    message.fileLength = $util.Long.fromValue(object.fileLength, true);
                else if (typeof object.fileLength === "string")
                    message.fileLength = parseInt(object.fileLength, 10);
                else if (typeof object.fileLength === "number")
                    message.fileLength = object.fileLength;
                else if (typeof object.fileLength === "object")
                    message.fileLength = new $util.LongBits(object.fileLength.low >>> 0, object.fileLength.high >>> 0).toNumber(true);
            if (object.bitrate != null)
                message.bitrate = object.bitrate >>> 0;
            switch (object.quality) {
            case "UNDEFINED":
            case 0:
                message.quality = 0;
                break;
            case "LOW":
            case 1:
                message.quality = 1;
                break;
            case "MID":
            case 2:
                message.quality = 2;
                break;
            case "HIGH":
            case 3:
                message.quality = 3;
                break;
            default:
                if (typeof object.quality === "number" && (object.quality | 0) === object.quality)
                    message.quality = object.quality;
            }
            if (object.capabilities) {
                if (!Array.isArray(object.capabilities))
                    throw TypeError(".proto.ProcessedVideo.capabilities: array expected");
                message.capabilities = Array(object.capabilities.length);
                for (var i = 0; i < object.capabilities.length; ++i)
                    message.capabilities[i] = String(object.capabilities[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ProcessedVideo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ProcessedVideo
         * @static
         * @param {proto.ProcessedVideo} message ProcessedVideo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ProcessedVideo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.capabilities = [];
            if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                object.directPath = message.directPath;
            if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                object.fileSha256 = options.bytes === String ? $util.base64.encode(message.fileSha256, 0, message.fileSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileSha256) : message.fileSha256;
            if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                object.height = message.height;
            if (message.width != null && Object.hasOwnProperty.call(message, "width"))
                object.width = message.width;
            if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.fileLength = typeof message.fileLength === "number" ? BigInt(message.fileLength) : $util.Long.fromBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0, true).toBigInt();
                else if (typeof message.fileLength === "number")
                    object.fileLength = options.longs === String ? String(message.fileLength) : message.fileLength;
                else
                    object.fileLength = options.longs === String ? $util.Long.prototype.toString.call(message.fileLength) : options.longs === Number ? new $util.LongBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0).toNumber(true) : message.fileLength;
            if (message.bitrate != null && Object.hasOwnProperty.call(message, "bitrate"))
                object.bitrate = message.bitrate;
            if (message.quality != null && Object.hasOwnProperty.call(message, "quality"))
                object.quality = options.enums === String ? $root.proto.ProcessedVideo.VideoQuality[message.quality] === undefined ? message.quality : $root.proto.ProcessedVideo.VideoQuality[message.quality] : message.quality;
            if (message.capabilities && message.capabilities.length) {
                object.capabilities = Array(message.capabilities.length);
                for (var j = 0; j < message.capabilities.length; ++j)
                    object.capabilities[j] = message.capabilities[j];
            }
            return object;
        };

        /**
         * Converts this ProcessedVideo to JSON.
         * @function toJSON
         * @memberof proto.ProcessedVideo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ProcessedVideo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ProcessedVideo
         * @function getTypeUrl
         * @memberof proto.ProcessedVideo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ProcessedVideo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.ProcessedVideo";
        };

        /**
         * VideoQuality enum.
         * @name proto.ProcessedVideo.VideoQuality
         * @enum {number}
         * @property {number} UNDEFINED=0 UNDEFINED value
         * @property {number} LOW=1 LOW value
         * @property {number} MID=2 MID value
         * @property {number} HIGH=3 HIGH value
         */
        ProcessedVideo.VideoQuality = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNDEFINED"] = 0;
            values[valuesById[1] = "LOW"] = 1;
            values[valuesById[2] = "MID"] = 2;
            values[valuesById[3] = "HIGH"] = 3;
            return values;
        })();

        return ProcessedVideo;
    })();

    proto.ProloguePayload = (function() {

        /**
         * Properties of a ProloguePayload.
         * @memberof proto
         * @interface IProloguePayload
         * @property {Uint8Array|null} [companionEphemeralIdentity] ProloguePayload companionEphemeralIdentity
         * @property {proto.ICompanionCommitment|null} [commitment] ProloguePayload commitment
         */

        /**
         * Constructs a new ProloguePayload.
         * @memberof proto
         * @classdesc Represents a ProloguePayload.
         * @implements IProloguePayload
         * @constructor
         * @param {proto.IProloguePayload=} [properties] Properties to set
         */
        function ProloguePayload(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ProloguePayload companionEphemeralIdentity.
         * @member {Uint8Array|null|undefined} companionEphemeralIdentity
         * @memberof proto.ProloguePayload
         * @instance
         */
        ProloguePayload.prototype.companionEphemeralIdentity = null;

        /**
         * ProloguePayload commitment.
         * @member {proto.ICompanionCommitment|null|undefined} commitment
         * @memberof proto.ProloguePayload
         * @instance
         */
        ProloguePayload.prototype.commitment = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProloguePayload.prototype, "_companionEphemeralIdentity", {
            get: $util.oneOfGetter($oneOfFields = ["companionEphemeralIdentity"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProloguePayload.prototype, "_commitment", {
            get: $util.oneOfGetter($oneOfFields = ["commitment"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ProloguePayload instance using the specified properties.
         * @function create
         * @memberof proto.ProloguePayload
         * @static
         * @param {proto.IProloguePayload=} [properties] Properties to set
         * @returns {proto.ProloguePayload} ProloguePayload instance
         */
        ProloguePayload.create = function create(properties) {
            return new ProloguePayload(properties);
        };

        /**
         * Encodes the specified ProloguePayload message. Does not implicitly {@link proto.ProloguePayload.verify|verify} messages.
         * @function encode
         * @memberof proto.ProloguePayload
         * @static
         * @param {proto.IProloguePayload} message ProloguePayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProloguePayload.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.companionEphemeralIdentity != null && Object.hasOwnProperty.call(message, "companionEphemeralIdentity"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.companionEphemeralIdentity);
            if (message.commitment != null && Object.hasOwnProperty.call(message, "commitment"))
                $root.proto.CompanionCommitment.encode(message.commitment, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ProloguePayload message, length delimited. Does not implicitly {@link proto.ProloguePayload.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ProloguePayload
         * @static
         * @param {proto.IProloguePayload} message ProloguePayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProloguePayload.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ProloguePayload message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ProloguePayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ProloguePayload} ProloguePayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProloguePayload.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.companionEphemeralIdentity = reader.bytes();
                        message._companionEphemeralIdentity = "companionEphemeralIdentity";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.commitment = $root.proto.CompanionCommitment.decode(reader, reader.uint32(), undefined, q + 1, message.commitment);
                        message._commitment = "commitment";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a ProloguePayload message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ProloguePayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ProloguePayload} ProloguePayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProloguePayload.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ProloguePayload message.
         * @function verify
         * @memberof proto.ProloguePayload
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ProloguePayload.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.companionEphemeralIdentity != null && Object.hasOwnProperty.call(message, "companionEphemeralIdentity")) {
                properties._companionEphemeralIdentity = 1;
                if (!(message.companionEphemeralIdentity && typeof message.companionEphemeralIdentity.length === "number" || $util.isString(message.companionEphemeralIdentity)))
                    return "companionEphemeralIdentity: buffer expected";
            }
            if (message.commitment != null && Object.hasOwnProperty.call(message, "commitment")) {
                properties._commitment = 1;
                {
                    var error = $root.proto.CompanionCommitment.verify(message.commitment, q + 1);
                    if (error)
                        return "commitment." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ProloguePayload message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ProloguePayload
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ProloguePayload} ProloguePayload
         */
        ProloguePayload.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.ProloguePayload: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.companionEphemeralIdentity != null)
                if (typeof object.companionEphemeralIdentity === "string")
                    $util.base64.decode(object.companionEphemeralIdentity, message.companionEphemeralIdentity = $util.newBuffer($util.base64.length(object.companionEphemeralIdentity)), 0);
                else if (object.companionEphemeralIdentity.length >= 0)
                    message.companionEphemeralIdentity = object.companionEphemeralIdentity;
            if (object.commitment != null) {
                if (!$util.isObject(object.commitment))
                    throw TypeError(".proto.ProloguePayload.commitment: object expected");
                message.commitment = $root.proto.CompanionCommitment.fromObject(object.commitment, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ProloguePayload message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ProloguePayload
         * @static
         * @param {proto.ProloguePayload} message ProloguePayload
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ProloguePayload.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.companionEphemeralIdentity != null && Object.hasOwnProperty.call(message, "companionEphemeralIdentity"))
                object.companionEphemeralIdentity = options.bytes === String ? $util.base64.encode(message.companionEphemeralIdentity, 0, message.companionEphemeralIdentity.length) : options.bytes === Array ? Array.prototype.slice.call(message.companionEphemeralIdentity) : message.companionEphemeralIdentity;
            if (message.commitment != null && Object.hasOwnProperty.call(message, "commitment"))
                object.commitment = $root.proto.CompanionCommitment.toObject(message.commitment, options, q + 1);
            return object;
        };

        /**
         * Converts this ProloguePayload to JSON.
         * @function toJSON
         * @memberof proto.ProloguePayload
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ProloguePayload.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ProloguePayload
         * @function getTypeUrl
         * @memberof proto.ProloguePayload
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ProloguePayload.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.ProloguePayload";
        };

        return ProloguePayload;
    })();

    proto.Pushname = (function() {

        /**
         * Properties of a Pushname.
         * @memberof proto
         * @interface IPushname
         * @property {string|null} [id] Pushname id
         * @property {string|null} [pushname] Pushname pushname
         */

        /**
         * Constructs a new Pushname.
         * @memberof proto
         * @classdesc Represents a Pushname.
         * @implements IPushname
         * @constructor
         * @param {proto.IPushname=} [properties] Properties to set
         */
        function Pushname(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Pushname id.
         * @member {string|null|undefined} id
         * @memberof proto.Pushname
         * @instance
         */
        Pushname.prototype.id = null;

        /**
         * Pushname pushname.
         * @member {string|null|undefined} pushname
         * @memberof proto.Pushname
         * @instance
         */
        Pushname.prototype.pushname = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Pushname.prototype, "_id", {
            get: $util.oneOfGetter($oneOfFields = ["id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Pushname.prototype, "_pushname", {
            get: $util.oneOfGetter($oneOfFields = ["pushname"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new Pushname instance using the specified properties.
         * @function create
         * @memberof proto.Pushname
         * @static
         * @param {proto.IPushname=} [properties] Properties to set
         * @returns {proto.Pushname} Pushname instance
         */
        Pushname.create = function create(properties) {
            return new Pushname(properties);
        };

        /**
         * Encodes the specified Pushname message. Does not implicitly {@link proto.Pushname.verify|verify} messages.
         * @function encode
         * @memberof proto.Pushname
         * @static
         * @param {proto.IPushname} message Pushname message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Pushname.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.pushname != null && Object.hasOwnProperty.call(message, "pushname"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.pushname);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Pushname message, length delimited. Does not implicitly {@link proto.Pushname.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Pushname
         * @static
         * @param {proto.IPushname} message Pushname message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Pushname.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Pushname message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Pushname
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Pushname} Pushname
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Pushname.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.id = reader.stringVerify();
                        message._id = "id";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.pushname = reader.stringVerify();
                        message._pushname = "pushname";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a Pushname message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Pushname
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Pushname} Pushname
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Pushname.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Pushname message.
         * @function verify
         * @memberof proto.Pushname
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Pushname.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.id != null && Object.hasOwnProperty.call(message, "id")) {
                properties._id = 1;
                if (!$util.isString(message.id))
                    return "id: string expected";
            }
            if (message.pushname != null && Object.hasOwnProperty.call(message, "pushname")) {
                properties._pushname = 1;
                if (!$util.isString(message.pushname))
                    return "pushname: string expected";
            }
            return null;
        };

        /**
         * Creates a Pushname message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Pushname
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Pushname} Pushname
         */
        Pushname.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.Pushname: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.id != null)
                message.id = String(object.id);
            if (object.pushname != null)
                message.pushname = String(object.pushname);
            return message;
        };

        /**
         * Creates a plain object from a Pushname message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Pushname
         * @static
         * @param {proto.Pushname} message Pushname
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Pushname.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.pushname != null && Object.hasOwnProperty.call(message, "pushname"))
                object.pushname = message.pushname;
            return object;
        };

        /**
         * Converts this Pushname to JSON.
         * @function toJSON
         * @memberof proto.Pushname
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Pushname.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Pushname
         * @function getTypeUrl
         * @memberof proto.Pushname
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Pushname.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.Pushname";
        };

        return Pushname;
    })();

    proto.QuarantinedMessage = (function() {

        /**
         * Properties of a QuarantinedMessage.
         * @memberof proto
         * @interface IQuarantinedMessage
         * @property {Uint8Array|null} [originalData] QuarantinedMessage originalData
         * @property {string|null} [extractedText] QuarantinedMessage extractedText
         */

        /**
         * Constructs a new QuarantinedMessage.
         * @memberof proto
         * @classdesc Represents a QuarantinedMessage.
         * @implements IQuarantinedMessage
         * @constructor
         * @param {proto.IQuarantinedMessage=} [properties] Properties to set
         */
        function QuarantinedMessage(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * QuarantinedMessage originalData.
         * @member {Uint8Array|null|undefined} originalData
         * @memberof proto.QuarantinedMessage
         * @instance
         */
        QuarantinedMessage.prototype.originalData = null;

        /**
         * QuarantinedMessage extractedText.
         * @member {string|null|undefined} extractedText
         * @memberof proto.QuarantinedMessage
         * @instance
         */
        QuarantinedMessage.prototype.extractedText = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(QuarantinedMessage.prototype, "_originalData", {
            get: $util.oneOfGetter($oneOfFields = ["originalData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(QuarantinedMessage.prototype, "_extractedText", {
            get: $util.oneOfGetter($oneOfFields = ["extractedText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new QuarantinedMessage instance using the specified properties.
         * @function create
         * @memberof proto.QuarantinedMessage
         * @static
         * @param {proto.IQuarantinedMessage=} [properties] Properties to set
         * @returns {proto.QuarantinedMessage} QuarantinedMessage instance
         */
        QuarantinedMessage.create = function create(properties) {
            return new QuarantinedMessage(properties);
        };

        /**
         * Encodes the specified QuarantinedMessage message. Does not implicitly {@link proto.QuarantinedMessage.verify|verify} messages.
         * @function encode
         * @memberof proto.QuarantinedMessage
         * @static
         * @param {proto.IQuarantinedMessage} message QuarantinedMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QuarantinedMessage.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.originalData != null && Object.hasOwnProperty.call(message, "originalData"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.originalData);
            if (message.extractedText != null && Object.hasOwnProperty.call(message, "extractedText"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.extractedText);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified QuarantinedMessage message, length delimited. Does not implicitly {@link proto.QuarantinedMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.QuarantinedMessage
         * @static
         * @param {proto.IQuarantinedMessage} message QuarantinedMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QuarantinedMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a QuarantinedMessage message from the specified reader or buffer.
         * @function decode
         * @memberof proto.QuarantinedMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.QuarantinedMessage} QuarantinedMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QuarantinedMessage.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.originalData = reader.bytes();
                        message._originalData = "originalData";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.extractedText = reader.stringVerify();
                        message._extractedText = "extractedText";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a QuarantinedMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.QuarantinedMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.QuarantinedMessage} QuarantinedMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QuarantinedMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a QuarantinedMessage message.
         * @function verify
         * @memberof proto.QuarantinedMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        QuarantinedMessage.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.originalData != null && Object.hasOwnProperty.call(message, "originalData")) {
                properties._originalData = 1;
                if (!(message.originalData && typeof message.originalData.length === "number" || $util.isString(message.originalData)))
                    return "originalData: buffer expected";
            }
            if (message.extractedText != null && Object.hasOwnProperty.call(message, "extractedText")) {
                properties._extractedText = 1;
                if (!$util.isString(message.extractedText))
                    return "extractedText: string expected";
            }
            return null;
        };

        /**
         * Creates a QuarantinedMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.QuarantinedMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.QuarantinedMessage} QuarantinedMessage
         */
        QuarantinedMessage.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.QuarantinedMessage: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.originalData != null)
                if (typeof object.originalData === "string")
                    $util.base64.decode(object.originalData, message.originalData = $util.newBuffer($util.base64.length(object.originalData)), 0);
                else if (object.originalData.length >= 0)
                    message.originalData = object.originalData;
            if (object.extractedText != null)
                message.extractedText = String(object.extractedText);
            return message;
        };

        /**
         * Creates a plain object from a QuarantinedMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.QuarantinedMessage
         * @static
         * @param {proto.QuarantinedMessage} message QuarantinedMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        QuarantinedMessage.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.originalData != null && Object.hasOwnProperty.call(message, "originalData"))
                object.originalData = options.bytes === String ? $util.base64.encode(message.originalData, 0, message.originalData.length) : options.bytes === Array ? Array.prototype.slice.call(message.originalData) : message.originalData;
            if (message.extractedText != null && Object.hasOwnProperty.call(message, "extractedText"))
                object.extractedText = message.extractedText;
            return object;
        };

        /**
         * Converts this QuarantinedMessage to JSON.
         * @function toJSON
         * @memberof proto.QuarantinedMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        QuarantinedMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for QuarantinedMessage
         * @function getTypeUrl
         * @memberof proto.QuarantinedMessage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        QuarantinedMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.QuarantinedMessage";
        };

        return QuarantinedMessage;
    })();

    proto.Reaction = (function() {

        /**
         * Properties of a Reaction.
         * @memberof proto
         * @interface IReaction
         * @property {proto.IMessageKey|null} [key] Reaction key
         * @property {string|null} [text] Reaction text
         * @property {string|null} [groupingKey] Reaction groupingKey
         * @property {number|Long|null} [senderTimestampMs] Reaction senderTimestampMs
         * @property {boolean|null} [unread] Reaction unread
         */

        /**
         * Constructs a new Reaction.
         * @memberof proto
         * @classdesc Represents a Reaction.
         * @implements IReaction
         * @constructor
         * @param {proto.IReaction=} [properties] Properties to set
         */
        function Reaction(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Reaction key.
         * @member {proto.IMessageKey|null|undefined} key
         * @memberof proto.Reaction
         * @instance
         */
        Reaction.prototype.key = null;

        /**
         * Reaction text.
         * @member {string|null|undefined} text
         * @memberof proto.Reaction
         * @instance
         */
        Reaction.prototype.text = null;

        /**
         * Reaction groupingKey.
         * @member {string|null|undefined} groupingKey
         * @memberof proto.Reaction
         * @instance
         */
        Reaction.prototype.groupingKey = null;

        /**
         * Reaction senderTimestampMs.
         * @member {number|Long|null|undefined} senderTimestampMs
         * @memberof proto.Reaction
         * @instance
         */
        Reaction.prototype.senderTimestampMs = null;

        /**
         * Reaction unread.
         * @member {boolean|null|undefined} unread
         * @memberof proto.Reaction
         * @instance
         */
        Reaction.prototype.unread = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Reaction.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Reaction.prototype, "_text", {
            get: $util.oneOfGetter($oneOfFields = ["text"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Reaction.prototype, "_groupingKey", {
            get: $util.oneOfGetter($oneOfFields = ["groupingKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Reaction.prototype, "_senderTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["senderTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Reaction.prototype, "_unread", {
            get: $util.oneOfGetter($oneOfFields = ["unread"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new Reaction instance using the specified properties.
         * @function create
         * @memberof proto.Reaction
         * @static
         * @param {proto.IReaction=} [properties] Properties to set
         * @returns {proto.Reaction} Reaction instance
         */
        Reaction.create = function create(properties) {
            return new Reaction(properties);
        };

        /**
         * Encodes the specified Reaction message. Does not implicitly {@link proto.Reaction.verify|verify} messages.
         * @function encode
         * @memberof proto.Reaction
         * @static
         * @param {proto.IReaction} message Reaction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Reaction.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                $root.proto.MessageKey.encode(message.key, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.text);
            if (message.groupingKey != null && Object.hasOwnProperty.call(message, "groupingKey"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.groupingKey);
            if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.senderTimestampMs);
            if (message.unread != null && Object.hasOwnProperty.call(message, "unread"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.unread);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Reaction message, length delimited. Does not implicitly {@link proto.Reaction.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Reaction
         * @static
         * @param {proto.IReaction} message Reaction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Reaction.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Reaction message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Reaction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Reaction} Reaction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Reaction.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.key = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.key);
                        message._key = "key";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.text = reader.stringVerify();
                        message._text = "text";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.groupingKey = reader.stringVerify();
                        message._groupingKey = "groupingKey";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.senderTimestampMs = reader.int64();
                        message._senderTimestampMs = "senderTimestampMs";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.unread = reader.bool();
                        message._unread = "unread";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a Reaction message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Reaction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Reaction} Reaction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Reaction.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Reaction message.
         * @function verify
         * @memberof proto.Reaction
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Reaction.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                properties._key = 1;
                {
                    var error = $root.proto.MessageKey.verify(message.key, q + 1);
                    if (error)
                        return "key." + error;
                }
            }
            if (message.text != null && Object.hasOwnProperty.call(message, "text")) {
                properties._text = 1;
                if (!$util.isString(message.text))
                    return "text: string expected";
            }
            if (message.groupingKey != null && Object.hasOwnProperty.call(message, "groupingKey")) {
                properties._groupingKey = 1;
                if (!$util.isString(message.groupingKey))
                    return "groupingKey: string expected";
            }
            if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs")) {
                properties._senderTimestampMs = 1;
                if (!$util.isInteger(message.senderTimestampMs) && !(message.senderTimestampMs && $util.isInteger(message.senderTimestampMs.low) && $util.isInteger(message.senderTimestampMs.high)))
                    return "senderTimestampMs: integer|Long expected";
            }
            if (message.unread != null && Object.hasOwnProperty.call(message, "unread")) {
                properties._unread = 1;
                if (typeof message.unread !== "boolean")
                    return "unread: boolean expected";
            }
            return null;
        };

        /**
         * Creates a Reaction message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Reaction
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Reaction} Reaction
         */
        Reaction.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.Reaction: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.key != null) {
                if (!$util.isObject(object.key))
                    throw TypeError(".proto.Reaction.key: object expected");
                message.key = $root.proto.MessageKey.fromObject(object.key, q + 1);
            }
            if (object.text != null)
                message.text = String(object.text);
            if (object.groupingKey != null)
                message.groupingKey = String(object.groupingKey);
            if (object.senderTimestampMs != null)
                if ($util.Long)
                    message.senderTimestampMs = $util.Long.fromValue(object.senderTimestampMs, false);
                else if (typeof object.senderTimestampMs === "string")
                    message.senderTimestampMs = parseInt(object.senderTimestampMs, 10);
                else if (typeof object.senderTimestampMs === "number")
                    message.senderTimestampMs = object.senderTimestampMs;
                else if (typeof object.senderTimestampMs === "object")
                    message.senderTimestampMs = new $util.LongBits(object.senderTimestampMs.low >>> 0, object.senderTimestampMs.high >>> 0).toNumber();
            if (object.unread != null)
                message.unread = Boolean(object.unread);
            return message;
        };

        /**
         * Creates a plain object from a Reaction message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Reaction
         * @static
         * @param {proto.Reaction} message Reaction
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Reaction.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                object.key = $root.proto.MessageKey.toObject(message.key, options, q + 1);
            if (message.text != null && Object.hasOwnProperty.call(message, "text"))
                object.text = message.text;
            if (message.groupingKey != null && Object.hasOwnProperty.call(message, "groupingKey"))
                object.groupingKey = message.groupingKey;
            if (message.senderTimestampMs != null && Object.hasOwnProperty.call(message, "senderTimestampMs"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.senderTimestampMs = typeof message.senderTimestampMs === "number" ? BigInt(message.senderTimestampMs) : $util.Long.fromBits(message.senderTimestampMs.low >>> 0, message.senderTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof message.senderTimestampMs === "number")
                    object.senderTimestampMs = options.longs === String ? String(message.senderTimestampMs) : message.senderTimestampMs;
                else
                    object.senderTimestampMs = options.longs === String ? $util.Long.prototype.toString.call(message.senderTimestampMs) : options.longs === Number ? new $util.LongBits(message.senderTimestampMs.low >>> 0, message.senderTimestampMs.high >>> 0).toNumber() : message.senderTimestampMs;
            if (message.unread != null && Object.hasOwnProperty.call(message, "unread"))
                object.unread = message.unread;
            return object;
        };

        /**
         * Converts this Reaction to JSON.
         * @function toJSON
         * @memberof proto.Reaction
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Reaction.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Reaction
         * @function getTypeUrl
         * @memberof proto.Reaction
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Reaction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.Reaction";
        };

        return Reaction;
    })();

    proto.RecentEmojiWeight = (function() {

        /**
         * Properties of a RecentEmojiWeight.
         * @memberof proto
         * @interface IRecentEmojiWeight
         * @property {string|null} [emoji] RecentEmojiWeight emoji
         * @property {number|null} [weight] RecentEmojiWeight weight
         */

        /**
         * Constructs a new RecentEmojiWeight.
         * @memberof proto
         * @classdesc Represents a RecentEmojiWeight.
         * @implements IRecentEmojiWeight
         * @constructor
         * @param {proto.IRecentEmojiWeight=} [properties] Properties to set
         */
        function RecentEmojiWeight(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RecentEmojiWeight emoji.
         * @member {string|null|undefined} emoji
         * @memberof proto.RecentEmojiWeight
         * @instance
         */
        RecentEmojiWeight.prototype.emoji = null;

        /**
         * RecentEmojiWeight weight.
         * @member {number|null|undefined} weight
         * @memberof proto.RecentEmojiWeight
         * @instance
         */
        RecentEmojiWeight.prototype.weight = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(RecentEmojiWeight.prototype, "_emoji", {
            get: $util.oneOfGetter($oneOfFields = ["emoji"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(RecentEmojiWeight.prototype, "_weight", {
            get: $util.oneOfGetter($oneOfFields = ["weight"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new RecentEmojiWeight instance using the specified properties.
         * @function create
         * @memberof proto.RecentEmojiWeight
         * @static
         * @param {proto.IRecentEmojiWeight=} [properties] Properties to set
         * @returns {proto.RecentEmojiWeight} RecentEmojiWeight instance
         */
        RecentEmojiWeight.create = function create(properties) {
            return new RecentEmojiWeight(properties);
        };

        /**
         * Encodes the specified RecentEmojiWeight message. Does not implicitly {@link proto.RecentEmojiWeight.verify|verify} messages.
         * @function encode
         * @memberof proto.RecentEmojiWeight
         * @static
         * @param {proto.IRecentEmojiWeight} message RecentEmojiWeight message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RecentEmojiWeight.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.emoji != null && Object.hasOwnProperty.call(message, "emoji"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.emoji);
            if (message.weight != null && Object.hasOwnProperty.call(message, "weight"))
                writer.uint32(/* id 2, wireType 5 =*/21).float(message.weight);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RecentEmojiWeight message, length delimited. Does not implicitly {@link proto.RecentEmojiWeight.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.RecentEmojiWeight
         * @static
         * @param {proto.IRecentEmojiWeight} message RecentEmojiWeight message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RecentEmojiWeight.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RecentEmojiWeight message from the specified reader or buffer.
         * @function decode
         * @memberof proto.RecentEmojiWeight
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.RecentEmojiWeight} RecentEmojiWeight
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RecentEmojiWeight.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.emoji = reader.stringVerify();
                        message._emoji = "emoji";
                        continue;
                    }
                case 2: {
                        if (u !== 5)
                            break;
                        message.weight = reader.float();
                        message._weight = "weight";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a RecentEmojiWeight message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.RecentEmojiWeight
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.RecentEmojiWeight} RecentEmojiWeight
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RecentEmojiWeight.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RecentEmojiWeight message.
         * @function verify
         * @memberof proto.RecentEmojiWeight
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RecentEmojiWeight.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.emoji != null && Object.hasOwnProperty.call(message, "emoji")) {
                properties._emoji = 1;
                if (!$util.isString(message.emoji))
                    return "emoji: string expected";
            }
            if (message.weight != null && Object.hasOwnProperty.call(message, "weight")) {
                properties._weight = 1;
                if (typeof message.weight !== "number")
                    return "weight: number expected";
            }
            return null;
        };

        /**
         * Creates a RecentEmojiWeight message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.RecentEmojiWeight
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.RecentEmojiWeight} RecentEmojiWeight
         */
        RecentEmojiWeight.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.RecentEmojiWeight: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.emoji != null)
                message.emoji = String(object.emoji);
            if (object.weight != null)
                message.weight = Number(object.weight);
            return message;
        };

        /**
         * Creates a plain object from a RecentEmojiWeight message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.RecentEmojiWeight
         * @static
         * @param {proto.RecentEmojiWeight} message RecentEmojiWeight
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RecentEmojiWeight.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.emoji != null && Object.hasOwnProperty.call(message, "emoji"))
                object.emoji = message.emoji;
            if (message.weight != null && Object.hasOwnProperty.call(message, "weight"))
                object.weight = options.json && !isFinite(message.weight) ? String(message.weight) : message.weight;
            return object;
        };

        /**
         * Converts this RecentEmojiWeight to JSON.
         * @function toJSON
         * @memberof proto.RecentEmojiWeight
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RecentEmojiWeight.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RecentEmojiWeight
         * @function getTypeUrl
         * @memberof proto.RecentEmojiWeight
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RecentEmojiWeight.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.RecentEmojiWeight";
        };

        return RecentEmojiWeight;
    })();

    proto.RecordStructure = (function() {

        /**
         * Properties of a RecordStructure.
         * @memberof proto
         * @interface IRecordStructure
         * @property {proto.ISessionStructure|null} [currentSession] RecordStructure currentSession
         * @property {Array.<proto.ISessionStructure>|null} [previousSessions] RecordStructure previousSessions
         */

        /**
         * Constructs a new RecordStructure.
         * @memberof proto
         * @classdesc Represents a RecordStructure.
         * @implements IRecordStructure
         * @constructor
         * @param {proto.IRecordStructure=} [properties] Properties to set
         */
        function RecordStructure(properties) {
            this.previousSessions = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RecordStructure currentSession.
         * @member {proto.ISessionStructure|null|undefined} currentSession
         * @memberof proto.RecordStructure
         * @instance
         */
        RecordStructure.prototype.currentSession = null;

        /**
         * RecordStructure previousSessions.
         * @member {Array.<proto.ISessionStructure>} previousSessions
         * @memberof proto.RecordStructure
         * @instance
         */
        RecordStructure.prototype.previousSessions = $util.emptyArray;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(RecordStructure.prototype, "_currentSession", {
            get: $util.oneOfGetter($oneOfFields = ["currentSession"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new RecordStructure instance using the specified properties.
         * @function create
         * @memberof proto.RecordStructure
         * @static
         * @param {proto.IRecordStructure=} [properties] Properties to set
         * @returns {proto.RecordStructure} RecordStructure instance
         */
        RecordStructure.create = function create(properties) {
            return new RecordStructure(properties);
        };

        /**
         * Encodes the specified RecordStructure message. Does not implicitly {@link proto.RecordStructure.verify|verify} messages.
         * @function encode
         * @memberof proto.RecordStructure
         * @static
         * @param {proto.IRecordStructure} message RecordStructure message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RecordStructure.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.currentSession != null && Object.hasOwnProperty.call(message, "currentSession"))
                $root.proto.SessionStructure.encode(message.currentSession, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.previousSessions != null && message.previousSessions.length)
                for (var i = 0; i < message.previousSessions.length; ++i)
                    $root.proto.SessionStructure.encode(message.previousSessions[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified RecordStructure message, length delimited. Does not implicitly {@link proto.RecordStructure.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.RecordStructure
         * @static
         * @param {proto.IRecordStructure} message RecordStructure message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RecordStructure.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RecordStructure message from the specified reader or buffer.
         * @function decode
         * @memberof proto.RecordStructure
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.RecordStructure} RecordStructure
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RecordStructure.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.currentSession = $root.proto.SessionStructure.decode(reader, reader.uint32(), undefined, q + 1, message.currentSession);
                        message._currentSession = "currentSession";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(message.previousSessions && message.previousSessions.length))
                            message.previousSessions = [];
                        message.previousSessions.push($root.proto.SessionStructure.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a RecordStructure message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.RecordStructure
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.RecordStructure} RecordStructure
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RecordStructure.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RecordStructure message.
         * @function verify
         * @memberof proto.RecordStructure
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RecordStructure.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.currentSession != null && Object.hasOwnProperty.call(message, "currentSession")) {
                properties._currentSession = 1;
                {
                    var error = $root.proto.SessionStructure.verify(message.currentSession, q + 1);
                    if (error)
                        return "currentSession." + error;
                }
            }
            if (message.previousSessions != null && Object.hasOwnProperty.call(message, "previousSessions")) {
                if (!Array.isArray(message.previousSessions))
                    return "previousSessions: array expected";
                for (var i = 0; i < message.previousSessions.length; ++i) {
                    var error = $root.proto.SessionStructure.verify(message.previousSessions[i], q + 1);
                    if (error)
                        return "previousSessions." + error;
                }
            }
            return null;
        };

        /**
         * Creates a RecordStructure message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.RecordStructure
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.RecordStructure} RecordStructure
         */
        RecordStructure.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.RecordStructure: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.currentSession != null) {
                if (!$util.isObject(object.currentSession))
                    throw TypeError(".proto.RecordStructure.currentSession: object expected");
                message.currentSession = $root.proto.SessionStructure.fromObject(object.currentSession, q + 1);
            }
            if (object.previousSessions) {
                if (!Array.isArray(object.previousSessions))
                    throw TypeError(".proto.RecordStructure.previousSessions: array expected");
                message.previousSessions = Array(object.previousSessions.length);
                for (var i = 0; i < object.previousSessions.length; ++i) {
                    if (!$util.isObject(object.previousSessions[i]))
                        throw TypeError(".proto.RecordStructure.previousSessions: object expected");
                    message.previousSessions[i] = $root.proto.SessionStructure.fromObject(object.previousSessions[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RecordStructure message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.RecordStructure
         * @static
         * @param {proto.RecordStructure} message RecordStructure
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RecordStructure.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.previousSessions = [];
            if (message.currentSession != null && Object.hasOwnProperty.call(message, "currentSession"))
                object.currentSession = $root.proto.SessionStructure.toObject(message.currentSession, options, q + 1);
            if (message.previousSessions && message.previousSessions.length) {
                object.previousSessions = Array(message.previousSessions.length);
                for (var j = 0; j < message.previousSessions.length; ++j)
                    object.previousSessions[j] = $root.proto.SessionStructure.toObject(message.previousSessions[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this RecordStructure to JSON.
         * @function toJSON
         * @memberof proto.RecordStructure
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RecordStructure.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RecordStructure
         * @function getTypeUrl
         * @memberof proto.RecordStructure
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RecordStructure.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.RecordStructure";
        };

        return RecordStructure;
    })();

    proto.Reportable = (function() {

        /**
         * Properties of a Reportable.
         * @memberof proto
         * @interface IReportable
         * @property {number|null} [minVersion] Reportable minVersion
         * @property {number|null} [maxVersion] Reportable maxVersion
         * @property {number|null} [notReportableMinVersion] Reportable notReportableMinVersion
         * @property {boolean|null} [never] Reportable never
         */

        /**
         * Constructs a new Reportable.
         * @memberof proto
         * @classdesc Represents a Reportable.
         * @implements IReportable
         * @constructor
         * @param {proto.IReportable=} [properties] Properties to set
         */
        function Reportable(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Reportable minVersion.
         * @member {number|null|undefined} minVersion
         * @memberof proto.Reportable
         * @instance
         */
        Reportable.prototype.minVersion = null;

        /**
         * Reportable maxVersion.
         * @member {number|null|undefined} maxVersion
         * @memberof proto.Reportable
         * @instance
         */
        Reportable.prototype.maxVersion = null;

        /**
         * Reportable notReportableMinVersion.
         * @member {number|null|undefined} notReportableMinVersion
         * @memberof proto.Reportable
         * @instance
         */
        Reportable.prototype.notReportableMinVersion = null;

        /**
         * Reportable never.
         * @member {boolean|null|undefined} never
         * @memberof proto.Reportable
         * @instance
         */
        Reportable.prototype.never = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Reportable.prototype, "_minVersion", {
            get: $util.oneOfGetter($oneOfFields = ["minVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Reportable.prototype, "_maxVersion", {
            get: $util.oneOfGetter($oneOfFields = ["maxVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Reportable.prototype, "_notReportableMinVersion", {
            get: $util.oneOfGetter($oneOfFields = ["notReportableMinVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Reportable.prototype, "_never", {
            get: $util.oneOfGetter($oneOfFields = ["never"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new Reportable instance using the specified properties.
         * @function create
         * @memberof proto.Reportable
         * @static
         * @param {proto.IReportable=} [properties] Properties to set
         * @returns {proto.Reportable} Reportable instance
         */
        Reportable.create = function create(properties) {
            return new Reportable(properties);
        };

        /**
         * Encodes the specified Reportable message. Does not implicitly {@link proto.Reportable.verify|verify} messages.
         * @function encode
         * @memberof proto.Reportable
         * @static
         * @param {proto.IReportable} message Reportable message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Reportable.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.minVersion != null && Object.hasOwnProperty.call(message, "minVersion"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.minVersion);
            if (message.maxVersion != null && Object.hasOwnProperty.call(message, "maxVersion"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.maxVersion);
            if (message.notReportableMinVersion != null && Object.hasOwnProperty.call(message, "notReportableMinVersion"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.notReportableMinVersion);
            if (message.never != null && Object.hasOwnProperty.call(message, "never"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.never);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified Reportable message, length delimited. Does not implicitly {@link proto.Reportable.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.Reportable
         * @static
         * @param {proto.IReportable} message Reportable message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Reportable.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Reportable message from the specified reader or buffer.
         * @function decode
         * @memberof proto.Reportable
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.Reportable} Reportable
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Reportable.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.minVersion = reader.uint32();
                        message._minVersion = "minVersion";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.maxVersion = reader.uint32();
                        message._maxVersion = "maxVersion";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.notReportableMinVersion = reader.uint32();
                        message._notReportableMinVersion = "notReportableMinVersion";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.never = reader.bool();
                        message._never = "never";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a Reportable message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.Reportable
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.Reportable} Reportable
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Reportable.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Reportable message.
         * @function verify
         * @memberof proto.Reportable
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Reportable.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.minVersion != null && Object.hasOwnProperty.call(message, "minVersion")) {
                properties._minVersion = 1;
                if (!$util.isInteger(message.minVersion))
                    return "minVersion: integer expected";
            }
            if (message.maxVersion != null && Object.hasOwnProperty.call(message, "maxVersion")) {
                properties._maxVersion = 1;
                if (!$util.isInteger(message.maxVersion))
                    return "maxVersion: integer expected";
            }
            if (message.notReportableMinVersion != null && Object.hasOwnProperty.call(message, "notReportableMinVersion")) {
                properties._notReportableMinVersion = 1;
                if (!$util.isInteger(message.notReportableMinVersion))
                    return "notReportableMinVersion: integer expected";
            }
            if (message.never != null && Object.hasOwnProperty.call(message, "never")) {
                properties._never = 1;
                if (typeof message.never !== "boolean")
                    return "never: boolean expected";
            }
            return null;
        };

        /**
         * Creates a Reportable message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.Reportable
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.Reportable} Reportable
         */
        Reportable.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.Reportable: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.minVersion != null)
                message.minVersion = object.minVersion >>> 0;
            if (object.maxVersion != null)
                message.maxVersion = object.maxVersion >>> 0;
            if (object.notReportableMinVersion != null)
                message.notReportableMinVersion = object.notReportableMinVersion >>> 0;
            if (object.never != null)
                message.never = Boolean(object.never);
            return message;
        };

        /**
         * Creates a plain object from a Reportable message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.Reportable
         * @static
         * @param {proto.Reportable} message Reportable
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Reportable.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.minVersion != null && Object.hasOwnProperty.call(message, "minVersion"))
                object.minVersion = message.minVersion;
            if (message.maxVersion != null && Object.hasOwnProperty.call(message, "maxVersion"))
                object.maxVersion = message.maxVersion;
            if (message.notReportableMinVersion != null && Object.hasOwnProperty.call(message, "notReportableMinVersion"))
                object.notReportableMinVersion = message.notReportableMinVersion;
            if (message.never != null && Object.hasOwnProperty.call(message, "never"))
                object.never = message.never;
            return object;
        };

        /**
         * Converts this Reportable to JSON.
         * @function toJSON
         * @memberof proto.Reportable
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Reportable.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Reportable
         * @function getTypeUrl
         * @memberof proto.Reportable
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Reportable.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.Reportable";
        };

        return Reportable;
    })();

    proto.ReportingTokenInfo = (function() {

        /**
         * Properties of a ReportingTokenInfo.
         * @memberof proto
         * @interface IReportingTokenInfo
         * @property {Uint8Array|null} [reportingTag] ReportingTokenInfo reportingTag
         */

        /**
         * Constructs a new ReportingTokenInfo.
         * @memberof proto
         * @classdesc Represents a ReportingTokenInfo.
         * @implements IReportingTokenInfo
         * @constructor
         * @param {proto.IReportingTokenInfo=} [properties] Properties to set
         */
        function ReportingTokenInfo(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReportingTokenInfo reportingTag.
         * @member {Uint8Array|null|undefined} reportingTag
         * @memberof proto.ReportingTokenInfo
         * @instance
         */
        ReportingTokenInfo.prototype.reportingTag = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ReportingTokenInfo.prototype, "_reportingTag", {
            get: $util.oneOfGetter($oneOfFields = ["reportingTag"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ReportingTokenInfo instance using the specified properties.
         * @function create
         * @memberof proto.ReportingTokenInfo
         * @static
         * @param {proto.IReportingTokenInfo=} [properties] Properties to set
         * @returns {proto.ReportingTokenInfo} ReportingTokenInfo instance
         */
        ReportingTokenInfo.create = function create(properties) {
            return new ReportingTokenInfo(properties);
        };

        /**
         * Encodes the specified ReportingTokenInfo message. Does not implicitly {@link proto.ReportingTokenInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.ReportingTokenInfo
         * @static
         * @param {proto.IReportingTokenInfo} message ReportingTokenInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReportingTokenInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.reportingTag != null && Object.hasOwnProperty.call(message, "reportingTag"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.reportingTag);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ReportingTokenInfo message, length delimited. Does not implicitly {@link proto.ReportingTokenInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ReportingTokenInfo
         * @static
         * @param {proto.IReportingTokenInfo} message ReportingTokenInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReportingTokenInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReportingTokenInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ReportingTokenInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ReportingTokenInfo} ReportingTokenInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReportingTokenInfo.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.reportingTag = reader.bytes();
                        message._reportingTag = "reportingTag";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a ReportingTokenInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ReportingTokenInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ReportingTokenInfo} ReportingTokenInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReportingTokenInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReportingTokenInfo message.
         * @function verify
         * @memberof proto.ReportingTokenInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReportingTokenInfo.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.reportingTag != null && Object.hasOwnProperty.call(message, "reportingTag")) {
                properties._reportingTag = 1;
                if (!(message.reportingTag && typeof message.reportingTag.length === "number" || $util.isString(message.reportingTag)))
                    return "reportingTag: buffer expected";
            }
            return null;
        };

        /**
         * Creates a ReportingTokenInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ReportingTokenInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ReportingTokenInfo} ReportingTokenInfo
         */
        ReportingTokenInfo.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.ReportingTokenInfo: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.reportingTag != null)
                if (typeof object.reportingTag === "string")
                    $util.base64.decode(object.reportingTag, message.reportingTag = $util.newBuffer($util.base64.length(object.reportingTag)), 0);
                else if (object.reportingTag.length >= 0)
                    message.reportingTag = object.reportingTag;
            return message;
        };

        /**
         * Creates a plain object from a ReportingTokenInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ReportingTokenInfo
         * @static
         * @param {proto.ReportingTokenInfo} message ReportingTokenInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReportingTokenInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.reportingTag != null && Object.hasOwnProperty.call(message, "reportingTag"))
                object.reportingTag = options.bytes === String ? $util.base64.encode(message.reportingTag, 0, message.reportingTag.length) : options.bytes === Array ? Array.prototype.slice.call(message.reportingTag) : message.reportingTag;
            return object;
        };

        /**
         * Converts this ReportingTokenInfo to JSON.
         * @function toJSON
         * @memberof proto.ReportingTokenInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReportingTokenInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReportingTokenInfo
         * @function getTypeUrl
         * @memberof proto.ReportingTokenInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReportingTokenInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.ReportingTokenInfo";
        };

        return ReportingTokenInfo;
    })();

    proto.ScheduledMessageMetadata = (function() {

        /**
         * Properties of a ScheduledMessageMetadata.
         * @memberof proto
         * @interface IScheduledMessageMetadata
         * @property {string|null} [revealKeyId] ScheduledMessageMetadata revealKeyId
         * @property {Uint8Array|null} [revealKey] ScheduledMessageMetadata revealKey
         * @property {number|Long|null} [scheduledTime] ScheduledMessageMetadata scheduledTime
         */

        /**
         * Constructs a new ScheduledMessageMetadata.
         * @memberof proto
         * @classdesc Represents a ScheduledMessageMetadata.
         * @implements IScheduledMessageMetadata
         * @constructor
         * @param {proto.IScheduledMessageMetadata=} [properties] Properties to set
         */
        function ScheduledMessageMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ScheduledMessageMetadata revealKeyId.
         * @member {string|null|undefined} revealKeyId
         * @memberof proto.ScheduledMessageMetadata
         * @instance
         */
        ScheduledMessageMetadata.prototype.revealKeyId = null;

        /**
         * ScheduledMessageMetadata revealKey.
         * @member {Uint8Array|null|undefined} revealKey
         * @memberof proto.ScheduledMessageMetadata
         * @instance
         */
        ScheduledMessageMetadata.prototype.revealKey = null;

        /**
         * ScheduledMessageMetadata scheduledTime.
         * @member {number|Long|null|undefined} scheduledTime
         * @memberof proto.ScheduledMessageMetadata
         * @instance
         */
        ScheduledMessageMetadata.prototype.scheduledTime = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ScheduledMessageMetadata.prototype, "_revealKeyId", {
            get: $util.oneOfGetter($oneOfFields = ["revealKeyId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ScheduledMessageMetadata.prototype, "_revealKey", {
            get: $util.oneOfGetter($oneOfFields = ["revealKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ScheduledMessageMetadata.prototype, "_scheduledTime", {
            get: $util.oneOfGetter($oneOfFields = ["scheduledTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ScheduledMessageMetadata instance using the specified properties.
         * @function create
         * @memberof proto.ScheduledMessageMetadata
         * @static
         * @param {proto.IScheduledMessageMetadata=} [properties] Properties to set
         * @returns {proto.ScheduledMessageMetadata} ScheduledMessageMetadata instance
         */
        ScheduledMessageMetadata.create = function create(properties) {
            return new ScheduledMessageMetadata(properties);
        };

        /**
         * Encodes the specified ScheduledMessageMetadata message. Does not implicitly {@link proto.ScheduledMessageMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.ScheduledMessageMetadata
         * @static
         * @param {proto.IScheduledMessageMetadata} message ScheduledMessageMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduledMessageMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.revealKeyId != null && Object.hasOwnProperty.call(message, "revealKeyId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.revealKeyId);
            if (message.revealKey != null && Object.hasOwnProperty.call(message, "revealKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.revealKey);
            if (message.scheduledTime != null && Object.hasOwnProperty.call(message, "scheduledTime"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.scheduledTime);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ScheduledMessageMetadata message, length delimited. Does not implicitly {@link proto.ScheduledMessageMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ScheduledMessageMetadata
         * @static
         * @param {proto.IScheduledMessageMetadata} message ScheduledMessageMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ScheduledMessageMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ScheduledMessageMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ScheduledMessageMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ScheduledMessageMetadata} ScheduledMessageMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduledMessageMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.revealKeyId = reader.stringVerify();
                        message._revealKeyId = "revealKeyId";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.revealKey = reader.bytes();
                        message._revealKey = "revealKey";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.scheduledTime = reader.uint64();
                        message._scheduledTime = "scheduledTime";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a ScheduledMessageMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ScheduledMessageMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ScheduledMessageMetadata} ScheduledMessageMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ScheduledMessageMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ScheduledMessageMetadata message.
         * @function verify
         * @memberof proto.ScheduledMessageMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ScheduledMessageMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.revealKeyId != null && Object.hasOwnProperty.call(message, "revealKeyId")) {
                properties._revealKeyId = 1;
                if (!$util.isString(message.revealKeyId))
                    return "revealKeyId: string expected";
            }
            if (message.revealKey != null && Object.hasOwnProperty.call(message, "revealKey")) {
                properties._revealKey = 1;
                if (!(message.revealKey && typeof message.revealKey.length === "number" || $util.isString(message.revealKey)))
                    return "revealKey: buffer expected";
            }
            if (message.scheduledTime != null && Object.hasOwnProperty.call(message, "scheduledTime")) {
                properties._scheduledTime = 1;
                if (!$util.isInteger(message.scheduledTime) && !(message.scheduledTime && $util.isInteger(message.scheduledTime.low) && $util.isInteger(message.scheduledTime.high)))
                    return "scheduledTime: integer|Long expected";
            }
            return null;
        };

        /**
         * Creates a ScheduledMessageMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ScheduledMessageMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ScheduledMessageMetadata} ScheduledMessageMetadata
         */
        ScheduledMessageMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.ScheduledMessageMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.revealKeyId != null)
                message.revealKeyId = String(object.revealKeyId);
            if (object.revealKey != null)
                if (typeof object.revealKey === "string")
                    $util.base64.decode(object.revealKey, message.revealKey = $util.newBuffer($util.base64.length(object.revealKey)), 0);
                else if (object.revealKey.length >= 0)
                    message.revealKey = object.revealKey;
            if (object.scheduledTime != null)
                if ($util.Long)
                    message.scheduledTime = $util.Long.fromValue(object.scheduledTime, true);
                else if (typeof object.scheduledTime === "string")
                    message.scheduledTime = parseInt(object.scheduledTime, 10);
                else if (typeof object.scheduledTime === "number")
                    message.scheduledTime = object.scheduledTime;
                else if (typeof object.scheduledTime === "object")
                    message.scheduledTime = new $util.LongBits(object.scheduledTime.low >>> 0, object.scheduledTime.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a ScheduledMessageMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ScheduledMessageMetadata
         * @static
         * @param {proto.ScheduledMessageMetadata} message ScheduledMessageMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ScheduledMessageMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.revealKeyId != null && Object.hasOwnProperty.call(message, "revealKeyId"))
                object.revealKeyId = message.revealKeyId;
            if (message.revealKey != null && Object.hasOwnProperty.call(message, "revealKey"))
                object.revealKey = options.bytes === String ? $util.base64.encode(message.revealKey, 0, message.revealKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.revealKey) : message.revealKey;
            if (message.scheduledTime != null && Object.hasOwnProperty.call(message, "scheduledTime"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.scheduledTime = typeof message.scheduledTime === "number" ? BigInt(message.scheduledTime) : $util.Long.fromBits(message.scheduledTime.low >>> 0, message.scheduledTime.high >>> 0, true).toBigInt();
                else if (typeof message.scheduledTime === "number")
                    object.scheduledTime = options.longs === String ? String(message.scheduledTime) : message.scheduledTime;
                else
                    object.scheduledTime = options.longs === String ? $util.Long.prototype.toString.call(message.scheduledTime) : options.longs === Number ? new $util.LongBits(message.scheduledTime.low >>> 0, message.scheduledTime.high >>> 0).toNumber(true) : message.scheduledTime;
            return object;
        };

        /**
         * Converts this ScheduledMessageMetadata to JSON.
         * @function toJSON
         * @memberof proto.ScheduledMessageMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ScheduledMessageMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ScheduledMessageMetadata
         * @function getTypeUrl
         * @memberof proto.ScheduledMessageMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ScheduledMessageMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.ScheduledMessageMetadata";
        };

        return ScheduledMessageMetadata;
    })();

    proto.SenderKeyDistributionMessage = (function() {

        /**
         * Properties of a SenderKeyDistributionMessage.
         * @memberof proto
         * @interface ISenderKeyDistributionMessage
         * @property {number|null} [id] SenderKeyDistributionMessage id
         * @property {number|null} [iteration] SenderKeyDistributionMessage iteration
         * @property {Uint8Array|null} [chainKey] SenderKeyDistributionMessage chainKey
         * @property {Uint8Array|null} [signingKey] SenderKeyDistributionMessage signingKey
         */

        /**
         * Constructs a new SenderKeyDistributionMessage.
         * @memberof proto
         * @classdesc Represents a SenderKeyDistributionMessage.
         * @implements ISenderKeyDistributionMessage
         * @constructor
         * @param {proto.ISenderKeyDistributionMessage=} [properties] Properties to set
         */
        function SenderKeyDistributionMessage(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SenderKeyDistributionMessage id.
         * @member {number|null|undefined} id
         * @memberof proto.SenderKeyDistributionMessage
         * @instance
         */
        SenderKeyDistributionMessage.prototype.id = null;

        /**
         * SenderKeyDistributionMessage iteration.
         * @member {number|null|undefined} iteration
         * @memberof proto.SenderKeyDistributionMessage
         * @instance
         */
        SenderKeyDistributionMessage.prototype.iteration = null;

        /**
         * SenderKeyDistributionMessage chainKey.
         * @member {Uint8Array|null|undefined} chainKey
         * @memberof proto.SenderKeyDistributionMessage
         * @instance
         */
        SenderKeyDistributionMessage.prototype.chainKey = null;

        /**
         * SenderKeyDistributionMessage signingKey.
         * @member {Uint8Array|null|undefined} signingKey
         * @memberof proto.SenderKeyDistributionMessage
         * @instance
         */
        SenderKeyDistributionMessage.prototype.signingKey = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SenderKeyDistributionMessage.prototype, "_id", {
            get: $util.oneOfGetter($oneOfFields = ["id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SenderKeyDistributionMessage.prototype, "_iteration", {
            get: $util.oneOfGetter($oneOfFields = ["iteration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SenderKeyDistributionMessage.prototype, "_chainKey", {
            get: $util.oneOfGetter($oneOfFields = ["chainKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SenderKeyDistributionMessage.prototype, "_signingKey", {
            get: $util.oneOfGetter($oneOfFields = ["signingKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SenderKeyDistributionMessage instance using the specified properties.
         * @function create
         * @memberof proto.SenderKeyDistributionMessage
         * @static
         * @param {proto.ISenderKeyDistributionMessage=} [properties] Properties to set
         * @returns {proto.SenderKeyDistributionMessage} SenderKeyDistributionMessage instance
         */
        SenderKeyDistributionMessage.create = function create(properties) {
            return new SenderKeyDistributionMessage(properties);
        };

        /**
         * Encodes the specified SenderKeyDistributionMessage message. Does not implicitly {@link proto.SenderKeyDistributionMessage.verify|verify} messages.
         * @function encode
         * @memberof proto.SenderKeyDistributionMessage
         * @static
         * @param {proto.ISenderKeyDistributionMessage} message SenderKeyDistributionMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SenderKeyDistributionMessage.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.id);
            if (message.iteration != null && Object.hasOwnProperty.call(message, "iteration"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.iteration);
            if (message.chainKey != null && Object.hasOwnProperty.call(message, "chainKey"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.chainKey);
            if (message.signingKey != null && Object.hasOwnProperty.call(message, "signingKey"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.signingKey);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SenderKeyDistributionMessage message, length delimited. Does not implicitly {@link proto.SenderKeyDistributionMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SenderKeyDistributionMessage
         * @static
         * @param {proto.ISenderKeyDistributionMessage} message SenderKeyDistributionMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SenderKeyDistributionMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SenderKeyDistributionMessage message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SenderKeyDistributionMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SenderKeyDistributionMessage} SenderKeyDistributionMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SenderKeyDistributionMessage.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.id = reader.uint32();
                        message._id = "id";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.iteration = reader.uint32();
                        message._iteration = "iteration";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.chainKey = reader.bytes();
                        message._chainKey = "chainKey";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.signingKey = reader.bytes();
                        message._signingKey = "signingKey";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a SenderKeyDistributionMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SenderKeyDistributionMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SenderKeyDistributionMessage} SenderKeyDistributionMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SenderKeyDistributionMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SenderKeyDistributionMessage message.
         * @function verify
         * @memberof proto.SenderKeyDistributionMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SenderKeyDistributionMessage.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.id != null && Object.hasOwnProperty.call(message, "id")) {
                properties._id = 1;
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            }
            if (message.iteration != null && Object.hasOwnProperty.call(message, "iteration")) {
                properties._iteration = 1;
                if (!$util.isInteger(message.iteration))
                    return "iteration: integer expected";
            }
            if (message.chainKey != null && Object.hasOwnProperty.call(message, "chainKey")) {
                properties._chainKey = 1;
                if (!(message.chainKey && typeof message.chainKey.length === "number" || $util.isString(message.chainKey)))
                    return "chainKey: buffer expected";
            }
            if (message.signingKey != null && Object.hasOwnProperty.call(message, "signingKey")) {
                properties._signingKey = 1;
                if (!(message.signingKey && typeof message.signingKey.length === "number" || $util.isString(message.signingKey)))
                    return "signingKey: buffer expected";
            }
            return null;
        };

        /**
         * Creates a SenderKeyDistributionMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SenderKeyDistributionMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SenderKeyDistributionMessage} SenderKeyDistributionMessage
         */
        SenderKeyDistributionMessage.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.SenderKeyDistributionMessage: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.id != null)
                message.id = object.id >>> 0;
            if (object.iteration != null)
                message.iteration = object.iteration >>> 0;
            if (object.chainKey != null)
                if (typeof object.chainKey === "string")
                    $util.base64.decode(object.chainKey, message.chainKey = $util.newBuffer($util.base64.length(object.chainKey)), 0);
                else if (object.chainKey.length >= 0)
                    message.chainKey = object.chainKey;
            if (object.signingKey != null)
                if (typeof object.signingKey === "string")
                    $util.base64.decode(object.signingKey, message.signingKey = $util.newBuffer($util.base64.length(object.signingKey)), 0);
                else if (object.signingKey.length >= 0)
                    message.signingKey = object.signingKey;
            return message;
        };

        /**
         * Creates a plain object from a SenderKeyDistributionMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SenderKeyDistributionMessage
         * @static
         * @param {proto.SenderKeyDistributionMessage} message SenderKeyDistributionMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SenderKeyDistributionMessage.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.iteration != null && Object.hasOwnProperty.call(message, "iteration"))
                object.iteration = message.iteration;
            if (message.chainKey != null && Object.hasOwnProperty.call(message, "chainKey"))
                object.chainKey = options.bytes === String ? $util.base64.encode(message.chainKey, 0, message.chainKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.chainKey) : message.chainKey;
            if (message.signingKey != null && Object.hasOwnProperty.call(message, "signingKey"))
                object.signingKey = options.bytes === String ? $util.base64.encode(message.signingKey, 0, message.signingKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.signingKey) : message.signingKey;
            return object;
        };

        /**
         * Converts this SenderKeyDistributionMessage to JSON.
         * @function toJSON
         * @memberof proto.SenderKeyDistributionMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SenderKeyDistributionMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SenderKeyDistributionMessage
         * @function getTypeUrl
         * @memberof proto.SenderKeyDistributionMessage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SenderKeyDistributionMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.SenderKeyDistributionMessage";
        };

        return SenderKeyDistributionMessage;
    })();

    proto.SenderKeyMessage = (function() {

        /**
         * Properties of a SenderKeyMessage.
         * @memberof proto
         * @interface ISenderKeyMessage
         * @property {number|null} [id] SenderKeyMessage id
         * @property {number|null} [iteration] SenderKeyMessage iteration
         * @property {Uint8Array|null} [ciphertext] SenderKeyMessage ciphertext
         */

        /**
         * Constructs a new SenderKeyMessage.
         * @memberof proto
         * @classdesc Represents a SenderKeyMessage.
         * @implements ISenderKeyMessage
         * @constructor
         * @param {proto.ISenderKeyMessage=} [properties] Properties to set
         */
        function SenderKeyMessage(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SenderKeyMessage id.
         * @member {number|null|undefined} id
         * @memberof proto.SenderKeyMessage
         * @instance
         */
        SenderKeyMessage.prototype.id = null;

        /**
         * SenderKeyMessage iteration.
         * @member {number|null|undefined} iteration
         * @memberof proto.SenderKeyMessage
         * @instance
         */
        SenderKeyMessage.prototype.iteration = null;

        /**
         * SenderKeyMessage ciphertext.
         * @member {Uint8Array|null|undefined} ciphertext
         * @memberof proto.SenderKeyMessage
         * @instance
         */
        SenderKeyMessage.prototype.ciphertext = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SenderKeyMessage.prototype, "_id", {
            get: $util.oneOfGetter($oneOfFields = ["id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SenderKeyMessage.prototype, "_iteration", {
            get: $util.oneOfGetter($oneOfFields = ["iteration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SenderKeyMessage.prototype, "_ciphertext", {
            get: $util.oneOfGetter($oneOfFields = ["ciphertext"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SenderKeyMessage instance using the specified properties.
         * @function create
         * @memberof proto.SenderKeyMessage
         * @static
         * @param {proto.ISenderKeyMessage=} [properties] Properties to set
         * @returns {proto.SenderKeyMessage} SenderKeyMessage instance
         */
        SenderKeyMessage.create = function create(properties) {
            return new SenderKeyMessage(properties);
        };

        /**
         * Encodes the specified SenderKeyMessage message. Does not implicitly {@link proto.SenderKeyMessage.verify|verify} messages.
         * @function encode
         * @memberof proto.SenderKeyMessage
         * @static
         * @param {proto.ISenderKeyMessage} message SenderKeyMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SenderKeyMessage.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.id);
            if (message.iteration != null && Object.hasOwnProperty.call(message, "iteration"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.iteration);
            if (message.ciphertext != null && Object.hasOwnProperty.call(message, "ciphertext"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.ciphertext);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SenderKeyMessage message, length delimited. Does not implicitly {@link proto.SenderKeyMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SenderKeyMessage
         * @static
         * @param {proto.ISenderKeyMessage} message SenderKeyMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SenderKeyMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SenderKeyMessage message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SenderKeyMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SenderKeyMessage} SenderKeyMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SenderKeyMessage.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.id = reader.uint32();
                        message._id = "id";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.iteration = reader.uint32();
                        message._iteration = "iteration";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.ciphertext = reader.bytes();
                        message._ciphertext = "ciphertext";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a SenderKeyMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SenderKeyMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SenderKeyMessage} SenderKeyMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SenderKeyMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SenderKeyMessage message.
         * @function verify
         * @memberof proto.SenderKeyMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SenderKeyMessage.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.id != null && Object.hasOwnProperty.call(message, "id")) {
                properties._id = 1;
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            }
            if (message.iteration != null && Object.hasOwnProperty.call(message, "iteration")) {
                properties._iteration = 1;
                if (!$util.isInteger(message.iteration))
                    return "iteration: integer expected";
            }
            if (message.ciphertext != null && Object.hasOwnProperty.call(message, "ciphertext")) {
                properties._ciphertext = 1;
                if (!(message.ciphertext && typeof message.ciphertext.length === "number" || $util.isString(message.ciphertext)))
                    return "ciphertext: buffer expected";
            }
            return null;
        };

        /**
         * Creates a SenderKeyMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SenderKeyMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SenderKeyMessage} SenderKeyMessage
         */
        SenderKeyMessage.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.SenderKeyMessage: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.id != null)
                message.id = object.id >>> 0;
            if (object.iteration != null)
                message.iteration = object.iteration >>> 0;
            if (object.ciphertext != null)
                if (typeof object.ciphertext === "string")
                    $util.base64.decode(object.ciphertext, message.ciphertext = $util.newBuffer($util.base64.length(object.ciphertext)), 0);
                else if (object.ciphertext.length >= 0)
                    message.ciphertext = object.ciphertext;
            return message;
        };

        /**
         * Creates a plain object from a SenderKeyMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SenderKeyMessage
         * @static
         * @param {proto.SenderKeyMessage} message SenderKeyMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SenderKeyMessage.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.iteration != null && Object.hasOwnProperty.call(message, "iteration"))
                object.iteration = message.iteration;
            if (message.ciphertext != null && Object.hasOwnProperty.call(message, "ciphertext"))
                object.ciphertext = options.bytes === String ? $util.base64.encode(message.ciphertext, 0, message.ciphertext.length) : options.bytes === Array ? Array.prototype.slice.call(message.ciphertext) : message.ciphertext;
            return object;
        };

        /**
         * Converts this SenderKeyMessage to JSON.
         * @function toJSON
         * @memberof proto.SenderKeyMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SenderKeyMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SenderKeyMessage
         * @function getTypeUrl
         * @memberof proto.SenderKeyMessage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SenderKeyMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.SenderKeyMessage";
        };

        return SenderKeyMessage;
    })();

    proto.SenderKeyRecordStructure = (function() {

        /**
         * Properties of a SenderKeyRecordStructure.
         * @memberof proto
         * @interface ISenderKeyRecordStructure
         * @property {Array.<proto.ISenderKeyStateStructure>|null} [senderKeyStates] SenderKeyRecordStructure senderKeyStates
         */

        /**
         * Constructs a new SenderKeyRecordStructure.
         * @memberof proto
         * @classdesc Represents a SenderKeyRecordStructure.
         * @implements ISenderKeyRecordStructure
         * @constructor
         * @param {proto.ISenderKeyRecordStructure=} [properties] Properties to set
         */
        function SenderKeyRecordStructure(properties) {
            this.senderKeyStates = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SenderKeyRecordStructure senderKeyStates.
         * @member {Array.<proto.ISenderKeyStateStructure>} senderKeyStates
         * @memberof proto.SenderKeyRecordStructure
         * @instance
         */
        SenderKeyRecordStructure.prototype.senderKeyStates = $util.emptyArray;

        /**
         * Creates a new SenderKeyRecordStructure instance using the specified properties.
         * @function create
         * @memberof proto.SenderKeyRecordStructure
         * @static
         * @param {proto.ISenderKeyRecordStructure=} [properties] Properties to set
         * @returns {proto.SenderKeyRecordStructure} SenderKeyRecordStructure instance
         */
        SenderKeyRecordStructure.create = function create(properties) {
            return new SenderKeyRecordStructure(properties);
        };

        /**
         * Encodes the specified SenderKeyRecordStructure message. Does not implicitly {@link proto.SenderKeyRecordStructure.verify|verify} messages.
         * @function encode
         * @memberof proto.SenderKeyRecordStructure
         * @static
         * @param {proto.ISenderKeyRecordStructure} message SenderKeyRecordStructure message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SenderKeyRecordStructure.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.senderKeyStates != null && message.senderKeyStates.length)
                for (var i = 0; i < message.senderKeyStates.length; ++i)
                    $root.proto.SenderKeyStateStructure.encode(message.senderKeyStates[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SenderKeyRecordStructure message, length delimited. Does not implicitly {@link proto.SenderKeyRecordStructure.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SenderKeyRecordStructure
         * @static
         * @param {proto.ISenderKeyRecordStructure} message SenderKeyRecordStructure message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SenderKeyRecordStructure.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SenderKeyRecordStructure message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SenderKeyRecordStructure
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SenderKeyRecordStructure} SenderKeyRecordStructure
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SenderKeyRecordStructure.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(message.senderKeyStates && message.senderKeyStates.length))
                            message.senderKeyStates = [];
                        message.senderKeyStates.push($root.proto.SenderKeyStateStructure.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a SenderKeyRecordStructure message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SenderKeyRecordStructure
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SenderKeyRecordStructure} SenderKeyRecordStructure
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SenderKeyRecordStructure.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SenderKeyRecordStructure message.
         * @function verify
         * @memberof proto.SenderKeyRecordStructure
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SenderKeyRecordStructure.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            if (message.senderKeyStates != null && Object.hasOwnProperty.call(message, "senderKeyStates")) {
                if (!Array.isArray(message.senderKeyStates))
                    return "senderKeyStates: array expected";
                for (var i = 0; i < message.senderKeyStates.length; ++i) {
                    var error = $root.proto.SenderKeyStateStructure.verify(message.senderKeyStates[i], q + 1);
                    if (error)
                        return "senderKeyStates." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SenderKeyRecordStructure message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SenderKeyRecordStructure
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SenderKeyRecordStructure} SenderKeyRecordStructure
         */
        SenderKeyRecordStructure.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.SenderKeyRecordStructure: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.senderKeyStates) {
                if (!Array.isArray(object.senderKeyStates))
                    throw TypeError(".proto.SenderKeyRecordStructure.senderKeyStates: array expected");
                message.senderKeyStates = Array(object.senderKeyStates.length);
                for (var i = 0; i < object.senderKeyStates.length; ++i) {
                    if (!$util.isObject(object.senderKeyStates[i]))
                        throw TypeError(".proto.SenderKeyRecordStructure.senderKeyStates: object expected");
                    message.senderKeyStates[i] = $root.proto.SenderKeyStateStructure.fromObject(object.senderKeyStates[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SenderKeyRecordStructure message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SenderKeyRecordStructure
         * @static
         * @param {proto.SenderKeyRecordStructure} message SenderKeyRecordStructure
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SenderKeyRecordStructure.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.senderKeyStates = [];
            if (message.senderKeyStates && message.senderKeyStates.length) {
                object.senderKeyStates = Array(message.senderKeyStates.length);
                for (var j = 0; j < message.senderKeyStates.length; ++j)
                    object.senderKeyStates[j] = $root.proto.SenderKeyStateStructure.toObject(message.senderKeyStates[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this SenderKeyRecordStructure to JSON.
         * @function toJSON
         * @memberof proto.SenderKeyRecordStructure
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SenderKeyRecordStructure.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SenderKeyRecordStructure
         * @function getTypeUrl
         * @memberof proto.SenderKeyRecordStructure
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SenderKeyRecordStructure.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.SenderKeyRecordStructure";
        };

        return SenderKeyRecordStructure;
    })();

    proto.SenderKeyStateStructure = (function() {

        /**
         * Properties of a SenderKeyStateStructure.
         * @memberof proto
         * @interface ISenderKeyStateStructure
         * @property {number|null} [senderKeyId] SenderKeyStateStructure senderKeyId
         * @property {proto.SenderKeyStateStructure.ISenderChainKey|null} [senderChainKey] SenderKeyStateStructure senderChainKey
         * @property {proto.SenderKeyStateStructure.ISenderSigningKey|null} [senderSigningKey] SenderKeyStateStructure senderSigningKey
         * @property {Array.<proto.SenderKeyStateStructure.ISenderMessageKey>|null} [senderMessageKeys] SenderKeyStateStructure senderMessageKeys
         */

        /**
         * Constructs a new SenderKeyStateStructure.
         * @memberof proto
         * @classdesc Represents a SenderKeyStateStructure.
         * @implements ISenderKeyStateStructure
         * @constructor
         * @param {proto.ISenderKeyStateStructure=} [properties] Properties to set
         */
        function SenderKeyStateStructure(properties) {
            this.senderMessageKeys = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SenderKeyStateStructure senderKeyId.
         * @member {number|null|undefined} senderKeyId
         * @memberof proto.SenderKeyStateStructure
         * @instance
         */
        SenderKeyStateStructure.prototype.senderKeyId = null;

        /**
         * SenderKeyStateStructure senderChainKey.
         * @member {proto.SenderKeyStateStructure.ISenderChainKey|null|undefined} senderChainKey
         * @memberof proto.SenderKeyStateStructure
         * @instance
         */
        SenderKeyStateStructure.prototype.senderChainKey = null;

        /**
         * SenderKeyStateStructure senderSigningKey.
         * @member {proto.SenderKeyStateStructure.ISenderSigningKey|null|undefined} senderSigningKey
         * @memberof proto.SenderKeyStateStructure
         * @instance
         */
        SenderKeyStateStructure.prototype.senderSigningKey = null;

        /**
         * SenderKeyStateStructure senderMessageKeys.
         * @member {Array.<proto.SenderKeyStateStructure.ISenderMessageKey>} senderMessageKeys
         * @memberof proto.SenderKeyStateStructure
         * @instance
         */
        SenderKeyStateStructure.prototype.senderMessageKeys = $util.emptyArray;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SenderKeyStateStructure.prototype, "_senderKeyId", {
            get: $util.oneOfGetter($oneOfFields = ["senderKeyId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SenderKeyStateStructure.prototype, "_senderChainKey", {
            get: $util.oneOfGetter($oneOfFields = ["senderChainKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SenderKeyStateStructure.prototype, "_senderSigningKey", {
            get: $util.oneOfGetter($oneOfFields = ["senderSigningKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SenderKeyStateStructure instance using the specified properties.
         * @function create
         * @memberof proto.SenderKeyStateStructure
         * @static
         * @param {proto.ISenderKeyStateStructure=} [properties] Properties to set
         * @returns {proto.SenderKeyStateStructure} SenderKeyStateStructure instance
         */
        SenderKeyStateStructure.create = function create(properties) {
            return new SenderKeyStateStructure(properties);
        };

        /**
         * Encodes the specified SenderKeyStateStructure message. Does not implicitly {@link proto.SenderKeyStateStructure.verify|verify} messages.
         * @function encode
         * @memberof proto.SenderKeyStateStructure
         * @static
         * @param {proto.ISenderKeyStateStructure} message SenderKeyStateStructure message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SenderKeyStateStructure.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.senderKeyId != null && Object.hasOwnProperty.call(message, "senderKeyId"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.senderKeyId);
            if (message.senderChainKey != null && Object.hasOwnProperty.call(message, "senderChainKey"))
                $root.proto.SenderKeyStateStructure.SenderChainKey.encode(message.senderChainKey, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.senderSigningKey != null && Object.hasOwnProperty.call(message, "senderSigningKey"))
                $root.proto.SenderKeyStateStructure.SenderSigningKey.encode(message.senderSigningKey, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.senderMessageKeys != null && message.senderMessageKeys.length)
                for (var i = 0; i < message.senderMessageKeys.length; ++i)
                    $root.proto.SenderKeyStateStructure.SenderMessageKey.encode(message.senderMessageKeys[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SenderKeyStateStructure message, length delimited. Does not implicitly {@link proto.SenderKeyStateStructure.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SenderKeyStateStructure
         * @static
         * @param {proto.ISenderKeyStateStructure} message SenderKeyStateStructure message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SenderKeyStateStructure.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SenderKeyStateStructure message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SenderKeyStateStructure
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SenderKeyStateStructure} SenderKeyStateStructure
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SenderKeyStateStructure.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.senderKeyId = reader.uint32();
                        message._senderKeyId = "senderKeyId";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.senderChainKey = $root.proto.SenderKeyStateStructure.SenderChainKey.decode(reader, reader.uint32(), undefined, q + 1, message.senderChainKey);
                        message._senderChainKey = "senderChainKey";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.senderSigningKey = $root.proto.SenderKeyStateStructure.SenderSigningKey.decode(reader, reader.uint32(), undefined, q + 1, message.senderSigningKey);
                        message._senderSigningKey = "senderSigningKey";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        if (!(message.senderMessageKeys && message.senderMessageKeys.length))
                            message.senderMessageKeys = [];
                        message.senderMessageKeys.push($root.proto.SenderKeyStateStructure.SenderMessageKey.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a SenderKeyStateStructure message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SenderKeyStateStructure
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SenderKeyStateStructure} SenderKeyStateStructure
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SenderKeyStateStructure.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SenderKeyStateStructure message.
         * @function verify
         * @memberof proto.SenderKeyStateStructure
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SenderKeyStateStructure.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.senderKeyId != null && Object.hasOwnProperty.call(message, "senderKeyId")) {
                properties._senderKeyId = 1;
                if (!$util.isInteger(message.senderKeyId))
                    return "senderKeyId: integer expected";
            }
            if (message.senderChainKey != null && Object.hasOwnProperty.call(message, "senderChainKey")) {
                properties._senderChainKey = 1;
                {
                    var error = $root.proto.SenderKeyStateStructure.SenderChainKey.verify(message.senderChainKey, q + 1);
                    if (error)
                        return "senderChainKey." + error;
                }
            }
            if (message.senderSigningKey != null && Object.hasOwnProperty.call(message, "senderSigningKey")) {
                properties._senderSigningKey = 1;
                {
                    var error = $root.proto.SenderKeyStateStructure.SenderSigningKey.verify(message.senderSigningKey, q + 1);
                    if (error)
                        return "senderSigningKey." + error;
                }
            }
            if (message.senderMessageKeys != null && Object.hasOwnProperty.call(message, "senderMessageKeys")) {
                if (!Array.isArray(message.senderMessageKeys))
                    return "senderMessageKeys: array expected";
                for (var i = 0; i < message.senderMessageKeys.length; ++i) {
                    var error = $root.proto.SenderKeyStateStructure.SenderMessageKey.verify(message.senderMessageKeys[i], q + 1);
                    if (error)
                        return "senderMessageKeys." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SenderKeyStateStructure message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SenderKeyStateStructure
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SenderKeyStateStructure} SenderKeyStateStructure
         */
        SenderKeyStateStructure.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.SenderKeyStateStructure: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.senderKeyId != null)
                message.senderKeyId = object.senderKeyId >>> 0;
            if (object.senderChainKey != null) {
                if (!$util.isObject(object.senderChainKey))
                    throw TypeError(".proto.SenderKeyStateStructure.senderChainKey: object expected");
                message.senderChainKey = $root.proto.SenderKeyStateStructure.SenderChainKey.fromObject(object.senderChainKey, q + 1);
            }
            if (object.senderSigningKey != null) {
                if (!$util.isObject(object.senderSigningKey))
                    throw TypeError(".proto.SenderKeyStateStructure.senderSigningKey: object expected");
                message.senderSigningKey = $root.proto.SenderKeyStateStructure.SenderSigningKey.fromObject(object.senderSigningKey, q + 1);
            }
            if (object.senderMessageKeys) {
                if (!Array.isArray(object.senderMessageKeys))
                    throw TypeError(".proto.SenderKeyStateStructure.senderMessageKeys: array expected");
                message.senderMessageKeys = Array(object.senderMessageKeys.length);
                for (var i = 0; i < object.senderMessageKeys.length; ++i) {
                    if (!$util.isObject(object.senderMessageKeys[i]))
                        throw TypeError(".proto.SenderKeyStateStructure.senderMessageKeys: object expected");
                    message.senderMessageKeys[i] = $root.proto.SenderKeyStateStructure.SenderMessageKey.fromObject(object.senderMessageKeys[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SenderKeyStateStructure message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SenderKeyStateStructure
         * @static
         * @param {proto.SenderKeyStateStructure} message SenderKeyStateStructure
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SenderKeyStateStructure.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.senderMessageKeys = [];
            if (message.senderKeyId != null && Object.hasOwnProperty.call(message, "senderKeyId"))
                object.senderKeyId = message.senderKeyId;
            if (message.senderChainKey != null && Object.hasOwnProperty.call(message, "senderChainKey"))
                object.senderChainKey = $root.proto.SenderKeyStateStructure.SenderChainKey.toObject(message.senderChainKey, options, q + 1);
            if (message.senderSigningKey != null && Object.hasOwnProperty.call(message, "senderSigningKey"))
                object.senderSigningKey = $root.proto.SenderKeyStateStructure.SenderSigningKey.toObject(message.senderSigningKey, options, q + 1);
            if (message.senderMessageKeys && message.senderMessageKeys.length) {
                object.senderMessageKeys = Array(message.senderMessageKeys.length);
                for (var j = 0; j < message.senderMessageKeys.length; ++j)
                    object.senderMessageKeys[j] = $root.proto.SenderKeyStateStructure.SenderMessageKey.toObject(message.senderMessageKeys[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this SenderKeyStateStructure to JSON.
         * @function toJSON
         * @memberof proto.SenderKeyStateStructure
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SenderKeyStateStructure.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SenderKeyStateStructure
         * @function getTypeUrl
         * @memberof proto.SenderKeyStateStructure
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SenderKeyStateStructure.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.SenderKeyStateStructure";
        };

        SenderKeyStateStructure.SenderChainKey = (function() {

            /**
             * Properties of a SenderChainKey.
             * @memberof proto.SenderKeyStateStructure
             * @interface ISenderChainKey
             * @property {number|null} [iteration] SenderChainKey iteration
             * @property {Uint8Array|null} [seed] SenderChainKey seed
             */

            /**
             * Constructs a new SenderChainKey.
             * @memberof proto.SenderKeyStateStructure
             * @classdesc Represents a SenderChainKey.
             * @implements ISenderChainKey
             * @constructor
             * @param {proto.SenderKeyStateStructure.ISenderChainKey=} [properties] Properties to set
             */
            function SenderChainKey(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * SenderChainKey iteration.
             * @member {number|null|undefined} iteration
             * @memberof proto.SenderKeyStateStructure.SenderChainKey
             * @instance
             */
            SenderChainKey.prototype.iteration = null;

            /**
             * SenderChainKey seed.
             * @member {Uint8Array|null|undefined} seed
             * @memberof proto.SenderKeyStateStructure.SenderChainKey
             * @instance
             */
            SenderChainKey.prototype.seed = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SenderChainKey.prototype, "_iteration", {
                get: $util.oneOfGetter($oneOfFields = ["iteration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SenderChainKey.prototype, "_seed", {
                get: $util.oneOfGetter($oneOfFields = ["seed"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new SenderChainKey instance using the specified properties.
             * @function create
             * @memberof proto.SenderKeyStateStructure.SenderChainKey
             * @static
             * @param {proto.SenderKeyStateStructure.ISenderChainKey=} [properties] Properties to set
             * @returns {proto.SenderKeyStateStructure.SenderChainKey} SenderChainKey instance
             */
            SenderChainKey.create = function create(properties) {
                return new SenderChainKey(properties);
            };

            /**
             * Encodes the specified SenderChainKey message. Does not implicitly {@link proto.SenderKeyStateStructure.SenderChainKey.verify|verify} messages.
             * @function encode
             * @memberof proto.SenderKeyStateStructure.SenderChainKey
             * @static
             * @param {proto.SenderKeyStateStructure.ISenderChainKey} message SenderChainKey message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SenderChainKey.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.iteration != null && Object.hasOwnProperty.call(message, "iteration"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.iteration);
                if (message.seed != null && Object.hasOwnProperty.call(message, "seed"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.seed);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SenderChainKey message, length delimited. Does not implicitly {@link proto.SenderKeyStateStructure.SenderChainKey.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SenderKeyStateStructure.SenderChainKey
             * @static
             * @param {proto.SenderKeyStateStructure.ISenderChainKey} message SenderChainKey message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SenderChainKey.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a SenderChainKey message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SenderKeyStateStructure.SenderChainKey
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SenderKeyStateStructure.SenderChainKey} SenderChainKey
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SenderChainKey.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.iteration = reader.uint32();
                            message._iteration = "iteration";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.seed = reader.bytes();
                            message._seed = "seed";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a SenderChainKey message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SenderKeyStateStructure.SenderChainKey
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SenderKeyStateStructure.SenderChainKey} SenderChainKey
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SenderChainKey.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SenderChainKey message.
             * @function verify
             * @memberof proto.SenderKeyStateStructure.SenderChainKey
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SenderChainKey.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.iteration != null && Object.hasOwnProperty.call(message, "iteration")) {
                    properties._iteration = 1;
                    if (!$util.isInteger(message.iteration))
                        return "iteration: integer expected";
                }
                if (message.seed != null && Object.hasOwnProperty.call(message, "seed")) {
                    properties._seed = 1;
                    if (!(message.seed && typeof message.seed.length === "number" || $util.isString(message.seed)))
                        return "seed: buffer expected";
                }
                return null;
            };

            /**
             * Creates a SenderChainKey message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SenderKeyStateStructure.SenderChainKey
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SenderKeyStateStructure.SenderChainKey} SenderChainKey
             */
            SenderChainKey.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SenderKeyStateStructure.SenderChainKey: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.iteration != null)
                    message.iteration = object.iteration >>> 0;
                if (object.seed != null)
                    if (typeof object.seed === "string")
                        $util.base64.decode(object.seed, message.seed = $util.newBuffer($util.base64.length(object.seed)), 0);
                    else if (object.seed.length >= 0)
                        message.seed = object.seed;
                return message;
            };

            /**
             * Creates a plain object from a SenderChainKey message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SenderKeyStateStructure.SenderChainKey
             * @static
             * @param {proto.SenderKeyStateStructure.SenderChainKey} message SenderChainKey
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SenderChainKey.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.iteration != null && Object.hasOwnProperty.call(message, "iteration"))
                    object.iteration = message.iteration;
                if (message.seed != null && Object.hasOwnProperty.call(message, "seed"))
                    object.seed = options.bytes === String ? $util.base64.encode(message.seed, 0, message.seed.length) : options.bytes === Array ? Array.prototype.slice.call(message.seed) : message.seed;
                return object;
            };

            /**
             * Converts this SenderChainKey to JSON.
             * @function toJSON
             * @memberof proto.SenderKeyStateStructure.SenderChainKey
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SenderChainKey.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for SenderChainKey
             * @function getTypeUrl
             * @memberof proto.SenderKeyStateStructure.SenderChainKey
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            SenderChainKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SenderKeyStateStructure.SenderChainKey";
            };

            return SenderChainKey;
        })();

        SenderKeyStateStructure.SenderMessageKey = (function() {

            /**
             * Properties of a SenderMessageKey.
             * @memberof proto.SenderKeyStateStructure
             * @interface ISenderMessageKey
             * @property {number|null} [iteration] SenderMessageKey iteration
             * @property {Uint8Array|null} [seed] SenderMessageKey seed
             */

            /**
             * Constructs a new SenderMessageKey.
             * @memberof proto.SenderKeyStateStructure
             * @classdesc Represents a SenderMessageKey.
             * @implements ISenderMessageKey
             * @constructor
             * @param {proto.SenderKeyStateStructure.ISenderMessageKey=} [properties] Properties to set
             */
            function SenderMessageKey(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * SenderMessageKey iteration.
             * @member {number|null|undefined} iteration
             * @memberof proto.SenderKeyStateStructure.SenderMessageKey
             * @instance
             */
            SenderMessageKey.prototype.iteration = null;

            /**
             * SenderMessageKey seed.
             * @member {Uint8Array|null|undefined} seed
             * @memberof proto.SenderKeyStateStructure.SenderMessageKey
             * @instance
             */
            SenderMessageKey.prototype.seed = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SenderMessageKey.prototype, "_iteration", {
                get: $util.oneOfGetter($oneOfFields = ["iteration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SenderMessageKey.prototype, "_seed", {
                get: $util.oneOfGetter($oneOfFields = ["seed"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new SenderMessageKey instance using the specified properties.
             * @function create
             * @memberof proto.SenderKeyStateStructure.SenderMessageKey
             * @static
             * @param {proto.SenderKeyStateStructure.ISenderMessageKey=} [properties] Properties to set
             * @returns {proto.SenderKeyStateStructure.SenderMessageKey} SenderMessageKey instance
             */
            SenderMessageKey.create = function create(properties) {
                return new SenderMessageKey(properties);
            };

            /**
             * Encodes the specified SenderMessageKey message. Does not implicitly {@link proto.SenderKeyStateStructure.SenderMessageKey.verify|verify} messages.
             * @function encode
             * @memberof proto.SenderKeyStateStructure.SenderMessageKey
             * @static
             * @param {proto.SenderKeyStateStructure.ISenderMessageKey} message SenderMessageKey message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SenderMessageKey.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.iteration != null && Object.hasOwnProperty.call(message, "iteration"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.iteration);
                if (message.seed != null && Object.hasOwnProperty.call(message, "seed"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.seed);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SenderMessageKey message, length delimited. Does not implicitly {@link proto.SenderKeyStateStructure.SenderMessageKey.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SenderKeyStateStructure.SenderMessageKey
             * @static
             * @param {proto.SenderKeyStateStructure.ISenderMessageKey} message SenderMessageKey message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SenderMessageKey.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a SenderMessageKey message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SenderKeyStateStructure.SenderMessageKey
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SenderKeyStateStructure.SenderMessageKey} SenderMessageKey
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SenderMessageKey.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.iteration = reader.uint32();
                            message._iteration = "iteration";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.seed = reader.bytes();
                            message._seed = "seed";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a SenderMessageKey message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SenderKeyStateStructure.SenderMessageKey
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SenderKeyStateStructure.SenderMessageKey} SenderMessageKey
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SenderMessageKey.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SenderMessageKey message.
             * @function verify
             * @memberof proto.SenderKeyStateStructure.SenderMessageKey
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SenderMessageKey.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.iteration != null && Object.hasOwnProperty.call(message, "iteration")) {
                    properties._iteration = 1;
                    if (!$util.isInteger(message.iteration))
                        return "iteration: integer expected";
                }
                if (message.seed != null && Object.hasOwnProperty.call(message, "seed")) {
                    properties._seed = 1;
                    if (!(message.seed && typeof message.seed.length === "number" || $util.isString(message.seed)))
                        return "seed: buffer expected";
                }
                return null;
            };

            /**
             * Creates a SenderMessageKey message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SenderKeyStateStructure.SenderMessageKey
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SenderKeyStateStructure.SenderMessageKey} SenderMessageKey
             */
            SenderMessageKey.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SenderKeyStateStructure.SenderMessageKey: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.iteration != null)
                    message.iteration = object.iteration >>> 0;
                if (object.seed != null)
                    if (typeof object.seed === "string")
                        $util.base64.decode(object.seed, message.seed = $util.newBuffer($util.base64.length(object.seed)), 0);
                    else if (object.seed.length >= 0)
                        message.seed = object.seed;
                return message;
            };

            /**
             * Creates a plain object from a SenderMessageKey message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SenderKeyStateStructure.SenderMessageKey
             * @static
             * @param {proto.SenderKeyStateStructure.SenderMessageKey} message SenderMessageKey
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SenderMessageKey.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.iteration != null && Object.hasOwnProperty.call(message, "iteration"))
                    object.iteration = message.iteration;
                if (message.seed != null && Object.hasOwnProperty.call(message, "seed"))
                    object.seed = options.bytes === String ? $util.base64.encode(message.seed, 0, message.seed.length) : options.bytes === Array ? Array.prototype.slice.call(message.seed) : message.seed;
                return object;
            };

            /**
             * Converts this SenderMessageKey to JSON.
             * @function toJSON
             * @memberof proto.SenderKeyStateStructure.SenderMessageKey
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SenderMessageKey.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for SenderMessageKey
             * @function getTypeUrl
             * @memberof proto.SenderKeyStateStructure.SenderMessageKey
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            SenderMessageKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SenderKeyStateStructure.SenderMessageKey";
            };

            return SenderMessageKey;
        })();

        SenderKeyStateStructure.SenderSigningKey = (function() {

            /**
             * Properties of a SenderSigningKey.
             * @memberof proto.SenderKeyStateStructure
             * @interface ISenderSigningKey
             * @property {Uint8Array|null} ["public"] SenderSigningKey public
             * @property {Uint8Array|null} ["private"] SenderSigningKey private
             */

            /**
             * Constructs a new SenderSigningKey.
             * @memberof proto.SenderKeyStateStructure
             * @classdesc Represents a SenderSigningKey.
             * @implements ISenderSigningKey
             * @constructor
             * @param {proto.SenderKeyStateStructure.ISenderSigningKey=} [properties] Properties to set
             */
            function SenderSigningKey(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * SenderSigningKey public.
             * @member {Uint8Array|null|undefined} public
             * @memberof proto.SenderKeyStateStructure.SenderSigningKey
             * @instance
             */
            SenderSigningKey.prototype["public"] = null;

            /**
             * SenderSigningKey private.
             * @member {Uint8Array|null|undefined} private
             * @memberof proto.SenderKeyStateStructure.SenderSigningKey
             * @instance
             */
            SenderSigningKey.prototype["private"] = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SenderSigningKey.prototype, "_public", {
                get: $util.oneOfGetter($oneOfFields = ["public"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SenderSigningKey.prototype, "_private", {
                get: $util.oneOfGetter($oneOfFields = ["private"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new SenderSigningKey instance using the specified properties.
             * @function create
             * @memberof proto.SenderKeyStateStructure.SenderSigningKey
             * @static
             * @param {proto.SenderKeyStateStructure.ISenderSigningKey=} [properties] Properties to set
             * @returns {proto.SenderKeyStateStructure.SenderSigningKey} SenderSigningKey instance
             */
            SenderSigningKey.create = function create(properties) {
                return new SenderSigningKey(properties);
            };

            /**
             * Encodes the specified SenderSigningKey message. Does not implicitly {@link proto.SenderKeyStateStructure.SenderSigningKey.verify|verify} messages.
             * @function encode
             * @memberof proto.SenderKeyStateStructure.SenderSigningKey
             * @static
             * @param {proto.SenderKeyStateStructure.ISenderSigningKey} message SenderSigningKey message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SenderSigningKey.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message["public"] != null && Object.hasOwnProperty.call(message, "public"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message["public"]);
                if (message["private"] != null && Object.hasOwnProperty.call(message, "private"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message["private"]);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SenderSigningKey message, length delimited. Does not implicitly {@link proto.SenderKeyStateStructure.SenderSigningKey.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SenderKeyStateStructure.SenderSigningKey
             * @static
             * @param {proto.SenderKeyStateStructure.ISenderSigningKey} message SenderSigningKey message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SenderSigningKey.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a SenderSigningKey message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SenderKeyStateStructure.SenderSigningKey
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SenderKeyStateStructure.SenderSigningKey} SenderSigningKey
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SenderSigningKey.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message["public"] = reader.bytes();
                            message._public = "public";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message["private"] = reader.bytes();
                            message._private = "private";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a SenderSigningKey message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SenderKeyStateStructure.SenderSigningKey
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SenderKeyStateStructure.SenderSigningKey} SenderSigningKey
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SenderSigningKey.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SenderSigningKey message.
             * @function verify
             * @memberof proto.SenderKeyStateStructure.SenderSigningKey
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SenderSigningKey.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message["public"] != null && Object.hasOwnProperty.call(message, "public")) {
                    properties._public = 1;
                    if (!(message["public"] && typeof message["public"].length === "number" || $util.isString(message["public"])))
                        return "public: buffer expected";
                }
                if (message["private"] != null && Object.hasOwnProperty.call(message, "private")) {
                    properties._private = 1;
                    if (!(message["private"] && typeof message["private"].length === "number" || $util.isString(message["private"])))
                        return "private: buffer expected";
                }
                return null;
            };

            /**
             * Creates a SenderSigningKey message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SenderKeyStateStructure.SenderSigningKey
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SenderKeyStateStructure.SenderSigningKey} SenderSigningKey
             */
            SenderSigningKey.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SenderKeyStateStructure.SenderSigningKey: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object["public"] != null)
                    if (typeof object["public"] === "string")
                        $util.base64.decode(object["public"], message["public"] = $util.newBuffer($util.base64.length(object["public"])), 0);
                    else if (object["public"].length >= 0)
                        message["public"] = object["public"];
                if (object["private"] != null)
                    if (typeof object["private"] === "string")
                        $util.base64.decode(object["private"], message["private"] = $util.newBuffer($util.base64.length(object["private"])), 0);
                    else if (object["private"].length >= 0)
                        message["private"] = object["private"];
                return message;
            };

            /**
             * Creates a plain object from a SenderSigningKey message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SenderKeyStateStructure.SenderSigningKey
             * @static
             * @param {proto.SenderKeyStateStructure.SenderSigningKey} message SenderSigningKey
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SenderSigningKey.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message["public"] != null && Object.hasOwnProperty.call(message, "public"))
                    object["public"] = options.bytes === String ? $util.base64.encode(message["public"], 0, message["public"].length) : options.bytes === Array ? Array.prototype.slice.call(message["public"]) : message["public"];
                if (message["private"] != null && Object.hasOwnProperty.call(message, "private"))
                    object["private"] = options.bytes === String ? $util.base64.encode(message["private"], 0, message["private"].length) : options.bytes === Array ? Array.prototype.slice.call(message["private"]) : message["private"];
                return object;
            };

            /**
             * Converts this SenderSigningKey to JSON.
             * @function toJSON
             * @memberof proto.SenderKeyStateStructure.SenderSigningKey
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SenderSigningKey.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for SenderSigningKey
             * @function getTypeUrl
             * @memberof proto.SenderKeyStateStructure.SenderSigningKey
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            SenderSigningKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SenderKeyStateStructure.SenderSigningKey";
            };

            return SenderSigningKey;
        })();

        return SenderKeyStateStructure;
    })();

    proto.ServerErrorReceipt = (function() {

        /**
         * Properties of a ServerErrorReceipt.
         * @memberof proto
         * @interface IServerErrorReceipt
         * @property {string|null} [stanzaId] ServerErrorReceipt stanzaId
         */

        /**
         * Constructs a new ServerErrorReceipt.
         * @memberof proto
         * @classdesc Represents a ServerErrorReceipt.
         * @implements IServerErrorReceipt
         * @constructor
         * @param {proto.IServerErrorReceipt=} [properties] Properties to set
         */
        function ServerErrorReceipt(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ServerErrorReceipt stanzaId.
         * @member {string|null|undefined} stanzaId
         * @memberof proto.ServerErrorReceipt
         * @instance
         */
        ServerErrorReceipt.prototype.stanzaId = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ServerErrorReceipt.prototype, "_stanzaId", {
            get: $util.oneOfGetter($oneOfFields = ["stanzaId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ServerErrorReceipt instance using the specified properties.
         * @function create
         * @memberof proto.ServerErrorReceipt
         * @static
         * @param {proto.IServerErrorReceipt=} [properties] Properties to set
         * @returns {proto.ServerErrorReceipt} ServerErrorReceipt instance
         */
        ServerErrorReceipt.create = function create(properties) {
            return new ServerErrorReceipt(properties);
        };

        /**
         * Encodes the specified ServerErrorReceipt message. Does not implicitly {@link proto.ServerErrorReceipt.verify|verify} messages.
         * @function encode
         * @memberof proto.ServerErrorReceipt
         * @static
         * @param {proto.IServerErrorReceipt} message ServerErrorReceipt message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ServerErrorReceipt.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.stanzaId != null && Object.hasOwnProperty.call(message, "stanzaId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.stanzaId);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ServerErrorReceipt message, length delimited. Does not implicitly {@link proto.ServerErrorReceipt.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ServerErrorReceipt
         * @static
         * @param {proto.IServerErrorReceipt} message ServerErrorReceipt message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ServerErrorReceipt.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ServerErrorReceipt message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ServerErrorReceipt
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ServerErrorReceipt} ServerErrorReceipt
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ServerErrorReceipt.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.stanzaId = reader.stringVerify();
                        message._stanzaId = "stanzaId";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a ServerErrorReceipt message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ServerErrorReceipt
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ServerErrorReceipt} ServerErrorReceipt
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ServerErrorReceipt.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ServerErrorReceipt message.
         * @function verify
         * @memberof proto.ServerErrorReceipt
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ServerErrorReceipt.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.stanzaId != null && Object.hasOwnProperty.call(message, "stanzaId")) {
                properties._stanzaId = 1;
                if (!$util.isString(message.stanzaId))
                    return "stanzaId: string expected";
            }
            return null;
        };

        /**
         * Creates a ServerErrorReceipt message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ServerErrorReceipt
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ServerErrorReceipt} ServerErrorReceipt
         */
        ServerErrorReceipt.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.ServerErrorReceipt: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.stanzaId != null)
                message.stanzaId = String(object.stanzaId);
            return message;
        };

        /**
         * Creates a plain object from a ServerErrorReceipt message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ServerErrorReceipt
         * @static
         * @param {proto.ServerErrorReceipt} message ServerErrorReceipt
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ServerErrorReceipt.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.stanzaId != null && Object.hasOwnProperty.call(message, "stanzaId"))
                object.stanzaId = message.stanzaId;
            return object;
        };

        /**
         * Converts this ServerErrorReceipt to JSON.
         * @function toJSON
         * @memberof proto.ServerErrorReceipt
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ServerErrorReceipt.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ServerErrorReceipt
         * @function getTypeUrl
         * @memberof proto.ServerErrorReceipt
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ServerErrorReceipt.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.ServerErrorReceipt";
        };

        return ServerErrorReceipt;
    })();

    proto.SessionStructure = (function() {

        /**
         * Properties of a SessionStructure.
         * @memberof proto
         * @interface ISessionStructure
         * @property {number|null} [sessionVersion] SessionStructure sessionVersion
         * @property {Uint8Array|null} [localIdentityPublic] SessionStructure localIdentityPublic
         * @property {Uint8Array|null} [remoteIdentityPublic] SessionStructure remoteIdentityPublic
         * @property {Uint8Array|null} [rootKey] SessionStructure rootKey
         * @property {number|null} [previousCounter] SessionStructure previousCounter
         * @property {proto.SessionStructure.IChain|null} [senderChain] SessionStructure senderChain
         * @property {Array.<proto.SessionStructure.IChain>|null} [receiverChains] SessionStructure receiverChains
         * @property {proto.SessionStructure.IPendingKeyExchange|null} [pendingKeyExchange] SessionStructure pendingKeyExchange
         * @property {proto.SessionStructure.IPendingPreKey|null} [pendingPreKey] SessionStructure pendingPreKey
         * @property {number|null} [remoteRegistrationId] SessionStructure remoteRegistrationId
         * @property {number|null} [localRegistrationId] SessionStructure localRegistrationId
         * @property {boolean|null} [needsRefresh] SessionStructure needsRefresh
         * @property {Uint8Array|null} [aliceBaseKey] SessionStructure aliceBaseKey
         */

        /**
         * Constructs a new SessionStructure.
         * @memberof proto
         * @classdesc Represents a SessionStructure.
         * @implements ISessionStructure
         * @constructor
         * @param {proto.ISessionStructure=} [properties] Properties to set
         */
        function SessionStructure(properties) {
            this.receiverChains = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SessionStructure sessionVersion.
         * @member {number|null|undefined} sessionVersion
         * @memberof proto.SessionStructure
         * @instance
         */
        SessionStructure.prototype.sessionVersion = null;

        /**
         * SessionStructure localIdentityPublic.
         * @member {Uint8Array|null|undefined} localIdentityPublic
         * @memberof proto.SessionStructure
         * @instance
         */
        SessionStructure.prototype.localIdentityPublic = null;

        /**
         * SessionStructure remoteIdentityPublic.
         * @member {Uint8Array|null|undefined} remoteIdentityPublic
         * @memberof proto.SessionStructure
         * @instance
         */
        SessionStructure.prototype.remoteIdentityPublic = null;

        /**
         * SessionStructure rootKey.
         * @member {Uint8Array|null|undefined} rootKey
         * @memberof proto.SessionStructure
         * @instance
         */
        SessionStructure.prototype.rootKey = null;

        /**
         * SessionStructure previousCounter.
         * @member {number|null|undefined} previousCounter
         * @memberof proto.SessionStructure
         * @instance
         */
        SessionStructure.prototype.previousCounter = null;

        /**
         * SessionStructure senderChain.
         * @member {proto.SessionStructure.IChain|null|undefined} senderChain
         * @memberof proto.SessionStructure
         * @instance
         */
        SessionStructure.prototype.senderChain = null;

        /**
         * SessionStructure receiverChains.
         * @member {Array.<proto.SessionStructure.IChain>} receiverChains
         * @memberof proto.SessionStructure
         * @instance
         */
        SessionStructure.prototype.receiverChains = $util.emptyArray;

        /**
         * SessionStructure pendingKeyExchange.
         * @member {proto.SessionStructure.IPendingKeyExchange|null|undefined} pendingKeyExchange
         * @memberof proto.SessionStructure
         * @instance
         */
        SessionStructure.prototype.pendingKeyExchange = null;

        /**
         * SessionStructure pendingPreKey.
         * @member {proto.SessionStructure.IPendingPreKey|null|undefined} pendingPreKey
         * @memberof proto.SessionStructure
         * @instance
         */
        SessionStructure.prototype.pendingPreKey = null;

        /**
         * SessionStructure remoteRegistrationId.
         * @member {number|null|undefined} remoteRegistrationId
         * @memberof proto.SessionStructure
         * @instance
         */
        SessionStructure.prototype.remoteRegistrationId = null;

        /**
         * SessionStructure localRegistrationId.
         * @member {number|null|undefined} localRegistrationId
         * @memberof proto.SessionStructure
         * @instance
         */
        SessionStructure.prototype.localRegistrationId = null;

        /**
         * SessionStructure needsRefresh.
         * @member {boolean|null|undefined} needsRefresh
         * @memberof proto.SessionStructure
         * @instance
         */
        SessionStructure.prototype.needsRefresh = null;

        /**
         * SessionStructure aliceBaseKey.
         * @member {Uint8Array|null|undefined} aliceBaseKey
         * @memberof proto.SessionStructure
         * @instance
         */
        SessionStructure.prototype.aliceBaseKey = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SessionStructure.prototype, "_sessionVersion", {
            get: $util.oneOfGetter($oneOfFields = ["sessionVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SessionStructure.prototype, "_localIdentityPublic", {
            get: $util.oneOfGetter($oneOfFields = ["localIdentityPublic"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SessionStructure.prototype, "_remoteIdentityPublic", {
            get: $util.oneOfGetter($oneOfFields = ["remoteIdentityPublic"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SessionStructure.prototype, "_rootKey", {
            get: $util.oneOfGetter($oneOfFields = ["rootKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SessionStructure.prototype, "_previousCounter", {
            get: $util.oneOfGetter($oneOfFields = ["previousCounter"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SessionStructure.prototype, "_senderChain", {
            get: $util.oneOfGetter($oneOfFields = ["senderChain"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SessionStructure.prototype, "_pendingKeyExchange", {
            get: $util.oneOfGetter($oneOfFields = ["pendingKeyExchange"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SessionStructure.prototype, "_pendingPreKey", {
            get: $util.oneOfGetter($oneOfFields = ["pendingPreKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SessionStructure.prototype, "_remoteRegistrationId", {
            get: $util.oneOfGetter($oneOfFields = ["remoteRegistrationId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SessionStructure.prototype, "_localRegistrationId", {
            get: $util.oneOfGetter($oneOfFields = ["localRegistrationId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SessionStructure.prototype, "_needsRefresh", {
            get: $util.oneOfGetter($oneOfFields = ["needsRefresh"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SessionStructure.prototype, "_aliceBaseKey", {
            get: $util.oneOfGetter($oneOfFields = ["aliceBaseKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SessionStructure instance using the specified properties.
         * @function create
         * @memberof proto.SessionStructure
         * @static
         * @param {proto.ISessionStructure=} [properties] Properties to set
         * @returns {proto.SessionStructure} SessionStructure instance
         */
        SessionStructure.create = function create(properties) {
            return new SessionStructure(properties);
        };

        /**
         * Encodes the specified SessionStructure message. Does not implicitly {@link proto.SessionStructure.verify|verify} messages.
         * @function encode
         * @memberof proto.SessionStructure
         * @static
         * @param {proto.ISessionStructure} message SessionStructure message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SessionStructure.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.sessionVersion != null && Object.hasOwnProperty.call(message, "sessionVersion"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.sessionVersion);
            if (message.localIdentityPublic != null && Object.hasOwnProperty.call(message, "localIdentityPublic"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.localIdentityPublic);
            if (message.remoteIdentityPublic != null && Object.hasOwnProperty.call(message, "remoteIdentityPublic"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.remoteIdentityPublic);
            if (message.rootKey != null && Object.hasOwnProperty.call(message, "rootKey"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.rootKey);
            if (message.previousCounter != null && Object.hasOwnProperty.call(message, "previousCounter"))
                writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.previousCounter);
            if (message.senderChain != null && Object.hasOwnProperty.call(message, "senderChain"))
                $root.proto.SessionStructure.Chain.encode(message.senderChain, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
            if (message.receiverChains != null && message.receiverChains.length)
                for (var i = 0; i < message.receiverChains.length; ++i)
                    $root.proto.SessionStructure.Chain.encode(message.receiverChains[i], writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
            if (message.pendingKeyExchange != null && Object.hasOwnProperty.call(message, "pendingKeyExchange"))
                $root.proto.SessionStructure.PendingKeyExchange.encode(message.pendingKeyExchange, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
            if (message.pendingPreKey != null && Object.hasOwnProperty.call(message, "pendingPreKey"))
                $root.proto.SessionStructure.PendingPreKey.encode(message.pendingPreKey, writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
            if (message.remoteRegistrationId != null && Object.hasOwnProperty.call(message, "remoteRegistrationId"))
                writer.uint32(/* id 10, wireType 0 =*/80).uint32(message.remoteRegistrationId);
            if (message.localRegistrationId != null && Object.hasOwnProperty.call(message, "localRegistrationId"))
                writer.uint32(/* id 11, wireType 0 =*/88).uint32(message.localRegistrationId);
            if (message.needsRefresh != null && Object.hasOwnProperty.call(message, "needsRefresh"))
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.needsRefresh);
            if (message.aliceBaseKey != null && Object.hasOwnProperty.call(message, "aliceBaseKey"))
                writer.uint32(/* id 13, wireType 2 =*/106).bytes(message.aliceBaseKey);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SessionStructure message, length delimited. Does not implicitly {@link proto.SessionStructure.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SessionStructure
         * @static
         * @param {proto.ISessionStructure} message SessionStructure message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SessionStructure.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SessionStructure message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SessionStructure
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SessionStructure} SessionStructure
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SessionStructure.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.sessionVersion = reader.uint32();
                        message._sessionVersion = "sessionVersion";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.localIdentityPublic = reader.bytes();
                        message._localIdentityPublic = "localIdentityPublic";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.remoteIdentityPublic = reader.bytes();
                        message._remoteIdentityPublic = "remoteIdentityPublic";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.rootKey = reader.bytes();
                        message._rootKey = "rootKey";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.previousCounter = reader.uint32();
                        message._previousCounter = "previousCounter";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        message.senderChain = $root.proto.SessionStructure.Chain.decode(reader, reader.uint32(), undefined, q + 1, message.senderChain);
                        message._senderChain = "senderChain";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        if (!(message.receiverChains && message.receiverChains.length))
                            message.receiverChains = [];
                        message.receiverChains.push($root.proto.SessionStructure.Chain.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        message.pendingKeyExchange = $root.proto.SessionStructure.PendingKeyExchange.decode(reader, reader.uint32(), undefined, q + 1, message.pendingKeyExchange);
                        message._pendingKeyExchange = "pendingKeyExchange";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        message.pendingPreKey = $root.proto.SessionStructure.PendingPreKey.decode(reader, reader.uint32(), undefined, q + 1, message.pendingPreKey);
                        message._pendingPreKey = "pendingPreKey";
                        continue;
                    }
                case 10: {
                        if (u !== 0)
                            break;
                        message.remoteRegistrationId = reader.uint32();
                        message._remoteRegistrationId = "remoteRegistrationId";
                        continue;
                    }
                case 11: {
                        if (u !== 0)
                            break;
                        message.localRegistrationId = reader.uint32();
                        message._localRegistrationId = "localRegistrationId";
                        continue;
                    }
                case 12: {
                        if (u !== 0)
                            break;
                        message.needsRefresh = reader.bool();
                        message._needsRefresh = "needsRefresh";
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        message.aliceBaseKey = reader.bytes();
                        message._aliceBaseKey = "aliceBaseKey";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a SessionStructure message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SessionStructure
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SessionStructure} SessionStructure
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SessionStructure.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SessionStructure message.
         * @function verify
         * @memberof proto.SessionStructure
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SessionStructure.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.sessionVersion != null && Object.hasOwnProperty.call(message, "sessionVersion")) {
                properties._sessionVersion = 1;
                if (!$util.isInteger(message.sessionVersion))
                    return "sessionVersion: integer expected";
            }
            if (message.localIdentityPublic != null && Object.hasOwnProperty.call(message, "localIdentityPublic")) {
                properties._localIdentityPublic = 1;
                if (!(message.localIdentityPublic && typeof message.localIdentityPublic.length === "number" || $util.isString(message.localIdentityPublic)))
                    return "localIdentityPublic: buffer expected";
            }
            if (message.remoteIdentityPublic != null && Object.hasOwnProperty.call(message, "remoteIdentityPublic")) {
                properties._remoteIdentityPublic = 1;
                if (!(message.remoteIdentityPublic && typeof message.remoteIdentityPublic.length === "number" || $util.isString(message.remoteIdentityPublic)))
                    return "remoteIdentityPublic: buffer expected";
            }
            if (message.rootKey != null && Object.hasOwnProperty.call(message, "rootKey")) {
                properties._rootKey = 1;
                if (!(message.rootKey && typeof message.rootKey.length === "number" || $util.isString(message.rootKey)))
                    return "rootKey: buffer expected";
            }
            if (message.previousCounter != null && Object.hasOwnProperty.call(message, "previousCounter")) {
                properties._previousCounter = 1;
                if (!$util.isInteger(message.previousCounter))
                    return "previousCounter: integer expected";
            }
            if (message.senderChain != null && Object.hasOwnProperty.call(message, "senderChain")) {
                properties._senderChain = 1;
                {
                    var error = $root.proto.SessionStructure.Chain.verify(message.senderChain, q + 1);
                    if (error)
                        return "senderChain." + error;
                }
            }
            if (message.receiverChains != null && Object.hasOwnProperty.call(message, "receiverChains")) {
                if (!Array.isArray(message.receiverChains))
                    return "receiverChains: array expected";
                for (var i = 0; i < message.receiverChains.length; ++i) {
                    var error = $root.proto.SessionStructure.Chain.verify(message.receiverChains[i], q + 1);
                    if (error)
                        return "receiverChains." + error;
                }
            }
            if (message.pendingKeyExchange != null && Object.hasOwnProperty.call(message, "pendingKeyExchange")) {
                properties._pendingKeyExchange = 1;
                {
                    var error = $root.proto.SessionStructure.PendingKeyExchange.verify(message.pendingKeyExchange, q + 1);
                    if (error)
                        return "pendingKeyExchange." + error;
                }
            }
            if (message.pendingPreKey != null && Object.hasOwnProperty.call(message, "pendingPreKey")) {
                properties._pendingPreKey = 1;
                {
                    var error = $root.proto.SessionStructure.PendingPreKey.verify(message.pendingPreKey, q + 1);
                    if (error)
                        return "pendingPreKey." + error;
                }
            }
            if (message.remoteRegistrationId != null && Object.hasOwnProperty.call(message, "remoteRegistrationId")) {
                properties._remoteRegistrationId = 1;
                if (!$util.isInteger(message.remoteRegistrationId))
                    return "remoteRegistrationId: integer expected";
            }
            if (message.localRegistrationId != null && Object.hasOwnProperty.call(message, "localRegistrationId")) {
                properties._localRegistrationId = 1;
                if (!$util.isInteger(message.localRegistrationId))
                    return "localRegistrationId: integer expected";
            }
            if (message.needsRefresh != null && Object.hasOwnProperty.call(message, "needsRefresh")) {
                properties._needsRefresh = 1;
                if (typeof message.needsRefresh !== "boolean")
                    return "needsRefresh: boolean expected";
            }
            if (message.aliceBaseKey != null && Object.hasOwnProperty.call(message, "aliceBaseKey")) {
                properties._aliceBaseKey = 1;
                if (!(message.aliceBaseKey && typeof message.aliceBaseKey.length === "number" || $util.isString(message.aliceBaseKey)))
                    return "aliceBaseKey: buffer expected";
            }
            return null;
        };

        /**
         * Creates a SessionStructure message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SessionStructure
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SessionStructure} SessionStructure
         */
        SessionStructure.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.SessionStructure: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.sessionVersion != null)
                message.sessionVersion = object.sessionVersion >>> 0;
            if (object.localIdentityPublic != null)
                if (typeof object.localIdentityPublic === "string")
                    $util.base64.decode(object.localIdentityPublic, message.localIdentityPublic = $util.newBuffer($util.base64.length(object.localIdentityPublic)), 0);
                else if (object.localIdentityPublic.length >= 0)
                    message.localIdentityPublic = object.localIdentityPublic;
            if (object.remoteIdentityPublic != null)
                if (typeof object.remoteIdentityPublic === "string")
                    $util.base64.decode(object.remoteIdentityPublic, message.remoteIdentityPublic = $util.newBuffer($util.base64.length(object.remoteIdentityPublic)), 0);
                else if (object.remoteIdentityPublic.length >= 0)
                    message.remoteIdentityPublic = object.remoteIdentityPublic;
            if (object.rootKey != null)
                if (typeof object.rootKey === "string")
                    $util.base64.decode(object.rootKey, message.rootKey = $util.newBuffer($util.base64.length(object.rootKey)), 0);
                else if (object.rootKey.length >= 0)
                    message.rootKey = object.rootKey;
            if (object.previousCounter != null)
                message.previousCounter = object.previousCounter >>> 0;
            if (object.senderChain != null) {
                if (!$util.isObject(object.senderChain))
                    throw TypeError(".proto.SessionStructure.senderChain: object expected");
                message.senderChain = $root.proto.SessionStructure.Chain.fromObject(object.senderChain, q + 1);
            }
            if (object.receiverChains) {
                if (!Array.isArray(object.receiverChains))
                    throw TypeError(".proto.SessionStructure.receiverChains: array expected");
                message.receiverChains = Array(object.receiverChains.length);
                for (var i = 0; i < object.receiverChains.length; ++i) {
                    if (!$util.isObject(object.receiverChains[i]))
                        throw TypeError(".proto.SessionStructure.receiverChains: object expected");
                    message.receiverChains[i] = $root.proto.SessionStructure.Chain.fromObject(object.receiverChains[i], q + 1);
                }
            }
            if (object.pendingKeyExchange != null) {
                if (!$util.isObject(object.pendingKeyExchange))
                    throw TypeError(".proto.SessionStructure.pendingKeyExchange: object expected");
                message.pendingKeyExchange = $root.proto.SessionStructure.PendingKeyExchange.fromObject(object.pendingKeyExchange, q + 1);
            }
            if (object.pendingPreKey != null) {
                if (!$util.isObject(object.pendingPreKey))
                    throw TypeError(".proto.SessionStructure.pendingPreKey: object expected");
                message.pendingPreKey = $root.proto.SessionStructure.PendingPreKey.fromObject(object.pendingPreKey, q + 1);
            }
            if (object.remoteRegistrationId != null)
                message.remoteRegistrationId = object.remoteRegistrationId >>> 0;
            if (object.localRegistrationId != null)
                message.localRegistrationId = object.localRegistrationId >>> 0;
            if (object.needsRefresh != null)
                message.needsRefresh = Boolean(object.needsRefresh);
            if (object.aliceBaseKey != null)
                if (typeof object.aliceBaseKey === "string")
                    $util.base64.decode(object.aliceBaseKey, message.aliceBaseKey = $util.newBuffer($util.base64.length(object.aliceBaseKey)), 0);
                else if (object.aliceBaseKey.length >= 0)
                    message.aliceBaseKey = object.aliceBaseKey;
            return message;
        };

        /**
         * Creates a plain object from a SessionStructure message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SessionStructure
         * @static
         * @param {proto.SessionStructure} message SessionStructure
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SessionStructure.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.receiverChains = [];
            if (message.sessionVersion != null && Object.hasOwnProperty.call(message, "sessionVersion"))
                object.sessionVersion = message.sessionVersion;
            if (message.localIdentityPublic != null && Object.hasOwnProperty.call(message, "localIdentityPublic"))
                object.localIdentityPublic = options.bytes === String ? $util.base64.encode(message.localIdentityPublic, 0, message.localIdentityPublic.length) : options.bytes === Array ? Array.prototype.slice.call(message.localIdentityPublic) : message.localIdentityPublic;
            if (message.remoteIdentityPublic != null && Object.hasOwnProperty.call(message, "remoteIdentityPublic"))
                object.remoteIdentityPublic = options.bytes === String ? $util.base64.encode(message.remoteIdentityPublic, 0, message.remoteIdentityPublic.length) : options.bytes === Array ? Array.prototype.slice.call(message.remoteIdentityPublic) : message.remoteIdentityPublic;
            if (message.rootKey != null && Object.hasOwnProperty.call(message, "rootKey"))
                object.rootKey = options.bytes === String ? $util.base64.encode(message.rootKey, 0, message.rootKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.rootKey) : message.rootKey;
            if (message.previousCounter != null && Object.hasOwnProperty.call(message, "previousCounter"))
                object.previousCounter = message.previousCounter;
            if (message.senderChain != null && Object.hasOwnProperty.call(message, "senderChain"))
                object.senderChain = $root.proto.SessionStructure.Chain.toObject(message.senderChain, options, q + 1);
            if (message.receiverChains && message.receiverChains.length) {
                object.receiverChains = Array(message.receiverChains.length);
                for (var j = 0; j < message.receiverChains.length; ++j)
                    object.receiverChains[j] = $root.proto.SessionStructure.Chain.toObject(message.receiverChains[j], options, q + 1);
            }
            if (message.pendingKeyExchange != null && Object.hasOwnProperty.call(message, "pendingKeyExchange"))
                object.pendingKeyExchange = $root.proto.SessionStructure.PendingKeyExchange.toObject(message.pendingKeyExchange, options, q + 1);
            if (message.pendingPreKey != null && Object.hasOwnProperty.call(message, "pendingPreKey"))
                object.pendingPreKey = $root.proto.SessionStructure.PendingPreKey.toObject(message.pendingPreKey, options, q + 1);
            if (message.remoteRegistrationId != null && Object.hasOwnProperty.call(message, "remoteRegistrationId"))
                object.remoteRegistrationId = message.remoteRegistrationId;
            if (message.localRegistrationId != null && Object.hasOwnProperty.call(message, "localRegistrationId"))
                object.localRegistrationId = message.localRegistrationId;
            if (message.needsRefresh != null && Object.hasOwnProperty.call(message, "needsRefresh"))
                object.needsRefresh = message.needsRefresh;
            if (message.aliceBaseKey != null && Object.hasOwnProperty.call(message, "aliceBaseKey"))
                object.aliceBaseKey = options.bytes === String ? $util.base64.encode(message.aliceBaseKey, 0, message.aliceBaseKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.aliceBaseKey) : message.aliceBaseKey;
            return object;
        };

        /**
         * Converts this SessionStructure to JSON.
         * @function toJSON
         * @memberof proto.SessionStructure
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SessionStructure.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SessionStructure
         * @function getTypeUrl
         * @memberof proto.SessionStructure
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SessionStructure.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.SessionStructure";
        };

        SessionStructure.Chain = (function() {

            /**
             * Properties of a Chain.
             * @memberof proto.SessionStructure
             * @interface IChain
             * @property {Uint8Array|null} [senderRatchetKey] Chain senderRatchetKey
             * @property {Uint8Array|null} [senderRatchetKeyPrivate] Chain senderRatchetKeyPrivate
             * @property {proto.SessionStructure.Chain.IChainKey|null} [chainKey] Chain chainKey
             * @property {Array.<proto.SessionStructure.Chain.IMessageKey>|null} [messageKeys] Chain messageKeys
             */

            /**
             * Constructs a new Chain.
             * @memberof proto.SessionStructure
             * @classdesc Represents a Chain.
             * @implements IChain
             * @constructor
             * @param {proto.SessionStructure.IChain=} [properties] Properties to set
             */
            function Chain(properties) {
                this.messageKeys = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Chain senderRatchetKey.
             * @member {Uint8Array|null|undefined} senderRatchetKey
             * @memberof proto.SessionStructure.Chain
             * @instance
             */
            Chain.prototype.senderRatchetKey = null;

            /**
             * Chain senderRatchetKeyPrivate.
             * @member {Uint8Array|null|undefined} senderRatchetKeyPrivate
             * @memberof proto.SessionStructure.Chain
             * @instance
             */
            Chain.prototype.senderRatchetKeyPrivate = null;

            /**
             * Chain chainKey.
             * @member {proto.SessionStructure.Chain.IChainKey|null|undefined} chainKey
             * @memberof proto.SessionStructure.Chain
             * @instance
             */
            Chain.prototype.chainKey = null;

            /**
             * Chain messageKeys.
             * @member {Array.<proto.SessionStructure.Chain.IMessageKey>} messageKeys
             * @memberof proto.SessionStructure.Chain
             * @instance
             */
            Chain.prototype.messageKeys = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Chain.prototype, "_senderRatchetKey", {
                get: $util.oneOfGetter($oneOfFields = ["senderRatchetKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Chain.prototype, "_senderRatchetKeyPrivate", {
                get: $util.oneOfGetter($oneOfFields = ["senderRatchetKeyPrivate"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Chain.prototype, "_chainKey", {
                get: $util.oneOfGetter($oneOfFields = ["chainKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new Chain instance using the specified properties.
             * @function create
             * @memberof proto.SessionStructure.Chain
             * @static
             * @param {proto.SessionStructure.IChain=} [properties] Properties to set
             * @returns {proto.SessionStructure.Chain} Chain instance
             */
            Chain.create = function create(properties) {
                return new Chain(properties);
            };

            /**
             * Encodes the specified Chain message. Does not implicitly {@link proto.SessionStructure.Chain.verify|verify} messages.
             * @function encode
             * @memberof proto.SessionStructure.Chain
             * @static
             * @param {proto.SessionStructure.IChain} message Chain message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Chain.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.senderRatchetKey != null && Object.hasOwnProperty.call(message, "senderRatchetKey"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.senderRatchetKey);
                if (message.senderRatchetKeyPrivate != null && Object.hasOwnProperty.call(message, "senderRatchetKeyPrivate"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.senderRatchetKeyPrivate);
                if (message.chainKey != null && Object.hasOwnProperty.call(message, "chainKey"))
                    $root.proto.SessionStructure.Chain.ChainKey.encode(message.chainKey, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.messageKeys != null && message.messageKeys.length)
                    for (var i = 0; i < message.messageKeys.length; ++i)
                        $root.proto.SessionStructure.Chain.MessageKey.encode(message.messageKeys[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Chain message, length delimited. Does not implicitly {@link proto.SessionStructure.Chain.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SessionStructure.Chain
             * @static
             * @param {proto.SessionStructure.IChain} message Chain message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Chain.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a Chain message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SessionStructure.Chain
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SessionStructure.Chain} Chain
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Chain.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.senderRatchetKey = reader.bytes();
                            message._senderRatchetKey = "senderRatchetKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.senderRatchetKeyPrivate = reader.bytes();
                            message._senderRatchetKeyPrivate = "senderRatchetKeyPrivate";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.chainKey = $root.proto.SessionStructure.Chain.ChainKey.decode(reader, reader.uint32(), undefined, q + 1, message.chainKey);
                            message._chainKey = "chainKey";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            if (!(message.messageKeys && message.messageKeys.length))
                                message.messageKeys = [];
                            message.messageKeys.push($root.proto.SessionStructure.Chain.MessageKey.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a Chain message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SessionStructure.Chain
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SessionStructure.Chain} Chain
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Chain.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Chain message.
             * @function verify
             * @memberof proto.SessionStructure.Chain
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Chain.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.senderRatchetKey != null && Object.hasOwnProperty.call(message, "senderRatchetKey")) {
                    properties._senderRatchetKey = 1;
                    if (!(message.senderRatchetKey && typeof message.senderRatchetKey.length === "number" || $util.isString(message.senderRatchetKey)))
                        return "senderRatchetKey: buffer expected";
                }
                if (message.senderRatchetKeyPrivate != null && Object.hasOwnProperty.call(message, "senderRatchetKeyPrivate")) {
                    properties._senderRatchetKeyPrivate = 1;
                    if (!(message.senderRatchetKeyPrivate && typeof message.senderRatchetKeyPrivate.length === "number" || $util.isString(message.senderRatchetKeyPrivate)))
                        return "senderRatchetKeyPrivate: buffer expected";
                }
                if (message.chainKey != null && Object.hasOwnProperty.call(message, "chainKey")) {
                    properties._chainKey = 1;
                    {
                        var error = $root.proto.SessionStructure.Chain.ChainKey.verify(message.chainKey, q + 1);
                        if (error)
                            return "chainKey." + error;
                    }
                }
                if (message.messageKeys != null && Object.hasOwnProperty.call(message, "messageKeys")) {
                    if (!Array.isArray(message.messageKeys))
                        return "messageKeys: array expected";
                    for (var i = 0; i < message.messageKeys.length; ++i) {
                        var error = $root.proto.SessionStructure.Chain.MessageKey.verify(message.messageKeys[i], q + 1);
                        if (error)
                            return "messageKeys." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a Chain message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SessionStructure.Chain
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SessionStructure.Chain} Chain
             */
            Chain.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SessionStructure.Chain: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.senderRatchetKey != null)
                    if (typeof object.senderRatchetKey === "string")
                        $util.base64.decode(object.senderRatchetKey, message.senderRatchetKey = $util.newBuffer($util.base64.length(object.senderRatchetKey)), 0);
                    else if (object.senderRatchetKey.length >= 0)
                        message.senderRatchetKey = object.senderRatchetKey;
                if (object.senderRatchetKeyPrivate != null)
                    if (typeof object.senderRatchetKeyPrivate === "string")
                        $util.base64.decode(object.senderRatchetKeyPrivate, message.senderRatchetKeyPrivate = $util.newBuffer($util.base64.length(object.senderRatchetKeyPrivate)), 0);
                    else if (object.senderRatchetKeyPrivate.length >= 0)
                        message.senderRatchetKeyPrivate = object.senderRatchetKeyPrivate;
                if (object.chainKey != null) {
                    if (!$util.isObject(object.chainKey))
                        throw TypeError(".proto.SessionStructure.Chain.chainKey: object expected");
                    message.chainKey = $root.proto.SessionStructure.Chain.ChainKey.fromObject(object.chainKey, q + 1);
                }
                if (object.messageKeys) {
                    if (!Array.isArray(object.messageKeys))
                        throw TypeError(".proto.SessionStructure.Chain.messageKeys: array expected");
                    message.messageKeys = Array(object.messageKeys.length);
                    for (var i = 0; i < object.messageKeys.length; ++i) {
                        if (!$util.isObject(object.messageKeys[i]))
                            throw TypeError(".proto.SessionStructure.Chain.messageKeys: object expected");
                        message.messageKeys[i] = $root.proto.SessionStructure.Chain.MessageKey.fromObject(object.messageKeys[i], q + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a Chain message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SessionStructure.Chain
             * @static
             * @param {proto.SessionStructure.Chain} message Chain
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Chain.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.messageKeys = [];
                if (message.senderRatchetKey != null && Object.hasOwnProperty.call(message, "senderRatchetKey"))
                    object.senderRatchetKey = options.bytes === String ? $util.base64.encode(message.senderRatchetKey, 0, message.senderRatchetKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.senderRatchetKey) : message.senderRatchetKey;
                if (message.senderRatchetKeyPrivate != null && Object.hasOwnProperty.call(message, "senderRatchetKeyPrivate"))
                    object.senderRatchetKeyPrivate = options.bytes === String ? $util.base64.encode(message.senderRatchetKeyPrivate, 0, message.senderRatchetKeyPrivate.length) : options.bytes === Array ? Array.prototype.slice.call(message.senderRatchetKeyPrivate) : message.senderRatchetKeyPrivate;
                if (message.chainKey != null && Object.hasOwnProperty.call(message, "chainKey"))
                    object.chainKey = $root.proto.SessionStructure.Chain.ChainKey.toObject(message.chainKey, options, q + 1);
                if (message.messageKeys && message.messageKeys.length) {
                    object.messageKeys = Array(message.messageKeys.length);
                    for (var j = 0; j < message.messageKeys.length; ++j)
                        object.messageKeys[j] = $root.proto.SessionStructure.Chain.MessageKey.toObject(message.messageKeys[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this Chain to JSON.
             * @function toJSON
             * @memberof proto.SessionStructure.Chain
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Chain.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Chain
             * @function getTypeUrl
             * @memberof proto.SessionStructure.Chain
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Chain.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SessionStructure.Chain";
            };

            Chain.ChainKey = (function() {

                /**
                 * Properties of a ChainKey.
                 * @memberof proto.SessionStructure.Chain
                 * @interface IChainKey
                 * @property {number|null} [index] ChainKey index
                 * @property {Uint8Array|null} [key] ChainKey key
                 */

                /**
                 * Constructs a new ChainKey.
                 * @memberof proto.SessionStructure.Chain
                 * @classdesc Represents a ChainKey.
                 * @implements IChainKey
                 * @constructor
                 * @param {proto.SessionStructure.Chain.IChainKey=} [properties] Properties to set
                 */
                function ChainKey(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * ChainKey index.
                 * @member {number|null|undefined} index
                 * @memberof proto.SessionStructure.Chain.ChainKey
                 * @instance
                 */
                ChainKey.prototype.index = null;

                /**
                 * ChainKey key.
                 * @member {Uint8Array|null|undefined} key
                 * @memberof proto.SessionStructure.Chain.ChainKey
                 * @instance
                 */
                ChainKey.prototype.key = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ChainKey.prototype, "_index", {
                    get: $util.oneOfGetter($oneOfFields = ["index"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ChainKey.prototype, "_key", {
                    get: $util.oneOfGetter($oneOfFields = ["key"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new ChainKey instance using the specified properties.
                 * @function create
                 * @memberof proto.SessionStructure.Chain.ChainKey
                 * @static
                 * @param {proto.SessionStructure.Chain.IChainKey=} [properties] Properties to set
                 * @returns {proto.SessionStructure.Chain.ChainKey} ChainKey instance
                 */
                ChainKey.create = function create(properties) {
                    return new ChainKey(properties);
                };

                /**
                 * Encodes the specified ChainKey message. Does not implicitly {@link proto.SessionStructure.Chain.ChainKey.verify|verify} messages.
                 * @function encode
                 * @memberof proto.SessionStructure.Chain.ChainKey
                 * @static
                 * @param {proto.SessionStructure.Chain.IChainKey} message ChainKey message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ChainKey.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                        writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.index);
                    if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                        writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.key);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified ChainKey message, length delimited. Does not implicitly {@link proto.SessionStructure.Chain.ChainKey.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.SessionStructure.Chain.ChainKey
                 * @static
                 * @param {proto.SessionStructure.Chain.IChainKey} message ChainKey message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ChainKey.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a ChainKey message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.SessionStructure.Chain.ChainKey
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.SessionStructure.Chain.ChainKey} ChainKey
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ChainKey.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                message.index = reader.uint32();
                                message._index = "index";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.key = reader.bytes();
                                message._key = "key";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a ChainKey message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.SessionStructure.Chain.ChainKey
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.SessionStructure.Chain.ChainKey} ChainKey
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ChainKey.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a ChainKey message.
                 * @function verify
                 * @memberof proto.SessionStructure.Chain.ChainKey
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                ChainKey.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.index != null && Object.hasOwnProperty.call(message, "index")) {
                        properties._index = 1;
                        if (!$util.isInteger(message.index))
                            return "index: integer expected";
                    }
                    if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                        properties._key = 1;
                        if (!(message.key && typeof message.key.length === "number" || $util.isString(message.key)))
                            return "key: buffer expected";
                    }
                    return null;
                };

                /**
                 * Creates a ChainKey message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.SessionStructure.Chain.ChainKey
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.SessionStructure.Chain.ChainKey} ChainKey
                 */
                ChainKey.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.SessionStructure.Chain.ChainKey: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.index != null)
                        message.index = object.index >>> 0;
                    if (object.key != null)
                        if (typeof object.key === "string")
                            $util.base64.decode(object.key, message.key = $util.newBuffer($util.base64.length(object.key)), 0);
                        else if (object.key.length >= 0)
                            message.key = object.key;
                    return message;
                };

                /**
                 * Creates a plain object from a ChainKey message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.SessionStructure.Chain.ChainKey
                 * @static
                 * @param {proto.SessionStructure.Chain.ChainKey} message ChainKey
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                ChainKey.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                        object.index = message.index;
                    if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                        object.key = options.bytes === String ? $util.base64.encode(message.key, 0, message.key.length) : options.bytes === Array ? Array.prototype.slice.call(message.key) : message.key;
                    return object;
                };

                /**
                 * Converts this ChainKey to JSON.
                 * @function toJSON
                 * @memberof proto.SessionStructure.Chain.ChainKey
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                ChainKey.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for ChainKey
                 * @function getTypeUrl
                 * @memberof proto.SessionStructure.Chain.ChainKey
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                ChainKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.SessionStructure.Chain.ChainKey";
                };

                return ChainKey;
            })();

            Chain.MessageKey = (function() {

                /**
                 * Properties of a MessageKey.
                 * @memberof proto.SessionStructure.Chain
                 * @interface IMessageKey
                 * @property {number|null} [index] MessageKey index
                 * @property {Uint8Array|null} [cipherKey] MessageKey cipherKey
                 * @property {Uint8Array|null} [macKey] MessageKey macKey
                 * @property {Uint8Array|null} [iv] MessageKey iv
                 */

                /**
                 * Constructs a new MessageKey.
                 * @memberof proto.SessionStructure.Chain
                 * @classdesc Represents a MessageKey.
                 * @implements IMessageKey
                 * @constructor
                 * @param {proto.SessionStructure.Chain.IMessageKey=} [properties] Properties to set
                 */
                function MessageKey(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * MessageKey index.
                 * @member {number|null|undefined} index
                 * @memberof proto.SessionStructure.Chain.MessageKey
                 * @instance
                 */
                MessageKey.prototype.index = null;

                /**
                 * MessageKey cipherKey.
                 * @member {Uint8Array|null|undefined} cipherKey
                 * @memberof proto.SessionStructure.Chain.MessageKey
                 * @instance
                 */
                MessageKey.prototype.cipherKey = null;

                /**
                 * MessageKey macKey.
                 * @member {Uint8Array|null|undefined} macKey
                 * @memberof proto.SessionStructure.Chain.MessageKey
                 * @instance
                 */
                MessageKey.prototype.macKey = null;

                /**
                 * MessageKey iv.
                 * @member {Uint8Array|null|undefined} iv
                 * @memberof proto.SessionStructure.Chain.MessageKey
                 * @instance
                 */
                MessageKey.prototype.iv = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(MessageKey.prototype, "_index", {
                    get: $util.oneOfGetter($oneOfFields = ["index"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(MessageKey.prototype, "_cipherKey", {
                    get: $util.oneOfGetter($oneOfFields = ["cipherKey"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(MessageKey.prototype, "_macKey", {
                    get: $util.oneOfGetter($oneOfFields = ["macKey"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(MessageKey.prototype, "_iv", {
                    get: $util.oneOfGetter($oneOfFields = ["iv"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new MessageKey instance using the specified properties.
                 * @function create
                 * @memberof proto.SessionStructure.Chain.MessageKey
                 * @static
                 * @param {proto.SessionStructure.Chain.IMessageKey=} [properties] Properties to set
                 * @returns {proto.SessionStructure.Chain.MessageKey} MessageKey instance
                 */
                MessageKey.create = function create(properties) {
                    return new MessageKey(properties);
                };

                /**
                 * Encodes the specified MessageKey message. Does not implicitly {@link proto.SessionStructure.Chain.MessageKey.verify|verify} messages.
                 * @function encode
                 * @memberof proto.SessionStructure.Chain.MessageKey
                 * @static
                 * @param {proto.SessionStructure.Chain.IMessageKey} message MessageKey message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                MessageKey.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                        writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.index);
                    if (message.cipherKey != null && Object.hasOwnProperty.call(message, "cipherKey"))
                        writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.cipherKey);
                    if (message.macKey != null && Object.hasOwnProperty.call(message, "macKey"))
                        writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.macKey);
                    if (message.iv != null && Object.hasOwnProperty.call(message, "iv"))
                        writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.iv);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified MessageKey message, length delimited. Does not implicitly {@link proto.SessionStructure.Chain.MessageKey.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.SessionStructure.Chain.MessageKey
                 * @static
                 * @param {proto.SessionStructure.Chain.IMessageKey} message MessageKey message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                MessageKey.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a MessageKey message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.SessionStructure.Chain.MessageKey
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.SessionStructure.Chain.MessageKey} MessageKey
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                MessageKey.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                message.index = reader.uint32();
                                message._index = "index";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.cipherKey = reader.bytes();
                                message._cipherKey = "cipherKey";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.macKey = reader.bytes();
                                message._macKey = "macKey";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                message.iv = reader.bytes();
                                message._iv = "iv";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a MessageKey message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.SessionStructure.Chain.MessageKey
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.SessionStructure.Chain.MessageKey} MessageKey
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                MessageKey.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a MessageKey message.
                 * @function verify
                 * @memberof proto.SessionStructure.Chain.MessageKey
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                MessageKey.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.index != null && Object.hasOwnProperty.call(message, "index")) {
                        properties._index = 1;
                        if (!$util.isInteger(message.index))
                            return "index: integer expected";
                    }
                    if (message.cipherKey != null && Object.hasOwnProperty.call(message, "cipherKey")) {
                        properties._cipherKey = 1;
                        if (!(message.cipherKey && typeof message.cipherKey.length === "number" || $util.isString(message.cipherKey)))
                            return "cipherKey: buffer expected";
                    }
                    if (message.macKey != null && Object.hasOwnProperty.call(message, "macKey")) {
                        properties._macKey = 1;
                        if (!(message.macKey && typeof message.macKey.length === "number" || $util.isString(message.macKey)))
                            return "macKey: buffer expected";
                    }
                    if (message.iv != null && Object.hasOwnProperty.call(message, "iv")) {
                        properties._iv = 1;
                        if (!(message.iv && typeof message.iv.length === "number" || $util.isString(message.iv)))
                            return "iv: buffer expected";
                    }
                    return null;
                };

                /**
                 * Creates a MessageKey message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.SessionStructure.Chain.MessageKey
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.SessionStructure.Chain.MessageKey} MessageKey
                 */
                MessageKey.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.SessionStructure.Chain.MessageKey: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.index != null)
                        message.index = object.index >>> 0;
                    if (object.cipherKey != null)
                        if (typeof object.cipherKey === "string")
                            $util.base64.decode(object.cipherKey, message.cipherKey = $util.newBuffer($util.base64.length(object.cipherKey)), 0);
                        else if (object.cipherKey.length >= 0)
                            message.cipherKey = object.cipherKey;
                    if (object.macKey != null)
                        if (typeof object.macKey === "string")
                            $util.base64.decode(object.macKey, message.macKey = $util.newBuffer($util.base64.length(object.macKey)), 0);
                        else if (object.macKey.length >= 0)
                            message.macKey = object.macKey;
                    if (object.iv != null)
                        if (typeof object.iv === "string")
                            $util.base64.decode(object.iv, message.iv = $util.newBuffer($util.base64.length(object.iv)), 0);
                        else if (object.iv.length >= 0)
                            message.iv = object.iv;
                    return message;
                };

                /**
                 * Creates a plain object from a MessageKey message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.SessionStructure.Chain.MessageKey
                 * @static
                 * @param {proto.SessionStructure.Chain.MessageKey} message MessageKey
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                MessageKey.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                        object.index = message.index;
                    if (message.cipherKey != null && Object.hasOwnProperty.call(message, "cipherKey"))
                        object.cipherKey = options.bytes === String ? $util.base64.encode(message.cipherKey, 0, message.cipherKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.cipherKey) : message.cipherKey;
                    if (message.macKey != null && Object.hasOwnProperty.call(message, "macKey"))
                        object.macKey = options.bytes === String ? $util.base64.encode(message.macKey, 0, message.macKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.macKey) : message.macKey;
                    if (message.iv != null && Object.hasOwnProperty.call(message, "iv"))
                        object.iv = options.bytes === String ? $util.base64.encode(message.iv, 0, message.iv.length) : options.bytes === Array ? Array.prototype.slice.call(message.iv) : message.iv;
                    return object;
                };

                /**
                 * Converts this MessageKey to JSON.
                 * @function toJSON
                 * @memberof proto.SessionStructure.Chain.MessageKey
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                MessageKey.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for MessageKey
                 * @function getTypeUrl
                 * @memberof proto.SessionStructure.Chain.MessageKey
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                MessageKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.SessionStructure.Chain.MessageKey";
                };

                return MessageKey;
            })();

            return Chain;
        })();

        SessionStructure.PendingKeyExchange = (function() {

            /**
             * Properties of a PendingKeyExchange.
             * @memberof proto.SessionStructure
             * @interface IPendingKeyExchange
             * @property {number|null} [sequence] PendingKeyExchange sequence
             * @property {Uint8Array|null} [localBaseKey] PendingKeyExchange localBaseKey
             * @property {Uint8Array|null} [localBaseKeyPrivate] PendingKeyExchange localBaseKeyPrivate
             * @property {Uint8Array|null} [localRatchetKey] PendingKeyExchange localRatchetKey
             * @property {Uint8Array|null} [localRatchetKeyPrivate] PendingKeyExchange localRatchetKeyPrivate
             * @property {Uint8Array|null} [localIdentityKey] PendingKeyExchange localIdentityKey
             * @property {Uint8Array|null} [localIdentityKeyPrivate] PendingKeyExchange localIdentityKeyPrivate
             */

            /**
             * Constructs a new PendingKeyExchange.
             * @memberof proto.SessionStructure
             * @classdesc Represents a PendingKeyExchange.
             * @implements IPendingKeyExchange
             * @constructor
             * @param {proto.SessionStructure.IPendingKeyExchange=} [properties] Properties to set
             */
            function PendingKeyExchange(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PendingKeyExchange sequence.
             * @member {number|null|undefined} sequence
             * @memberof proto.SessionStructure.PendingKeyExchange
             * @instance
             */
            PendingKeyExchange.prototype.sequence = null;

            /**
             * PendingKeyExchange localBaseKey.
             * @member {Uint8Array|null|undefined} localBaseKey
             * @memberof proto.SessionStructure.PendingKeyExchange
             * @instance
             */
            PendingKeyExchange.prototype.localBaseKey = null;

            /**
             * PendingKeyExchange localBaseKeyPrivate.
             * @member {Uint8Array|null|undefined} localBaseKeyPrivate
             * @memberof proto.SessionStructure.PendingKeyExchange
             * @instance
             */
            PendingKeyExchange.prototype.localBaseKeyPrivate = null;

            /**
             * PendingKeyExchange localRatchetKey.
             * @member {Uint8Array|null|undefined} localRatchetKey
             * @memberof proto.SessionStructure.PendingKeyExchange
             * @instance
             */
            PendingKeyExchange.prototype.localRatchetKey = null;

            /**
             * PendingKeyExchange localRatchetKeyPrivate.
             * @member {Uint8Array|null|undefined} localRatchetKeyPrivate
             * @memberof proto.SessionStructure.PendingKeyExchange
             * @instance
             */
            PendingKeyExchange.prototype.localRatchetKeyPrivate = null;

            /**
             * PendingKeyExchange localIdentityKey.
             * @member {Uint8Array|null|undefined} localIdentityKey
             * @memberof proto.SessionStructure.PendingKeyExchange
             * @instance
             */
            PendingKeyExchange.prototype.localIdentityKey = null;

            /**
             * PendingKeyExchange localIdentityKeyPrivate.
             * @member {Uint8Array|null|undefined} localIdentityKeyPrivate
             * @memberof proto.SessionStructure.PendingKeyExchange
             * @instance
             */
            PendingKeyExchange.prototype.localIdentityKeyPrivate = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PendingKeyExchange.prototype, "_sequence", {
                get: $util.oneOfGetter($oneOfFields = ["sequence"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PendingKeyExchange.prototype, "_localBaseKey", {
                get: $util.oneOfGetter($oneOfFields = ["localBaseKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PendingKeyExchange.prototype, "_localBaseKeyPrivate", {
                get: $util.oneOfGetter($oneOfFields = ["localBaseKeyPrivate"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PendingKeyExchange.prototype, "_localRatchetKey", {
                get: $util.oneOfGetter($oneOfFields = ["localRatchetKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PendingKeyExchange.prototype, "_localRatchetKeyPrivate", {
                get: $util.oneOfGetter($oneOfFields = ["localRatchetKeyPrivate"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PendingKeyExchange.prototype, "_localIdentityKey", {
                get: $util.oneOfGetter($oneOfFields = ["localIdentityKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PendingKeyExchange.prototype, "_localIdentityKeyPrivate", {
                get: $util.oneOfGetter($oneOfFields = ["localIdentityKeyPrivate"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PendingKeyExchange instance using the specified properties.
             * @function create
             * @memberof proto.SessionStructure.PendingKeyExchange
             * @static
             * @param {proto.SessionStructure.IPendingKeyExchange=} [properties] Properties to set
             * @returns {proto.SessionStructure.PendingKeyExchange} PendingKeyExchange instance
             */
            PendingKeyExchange.create = function create(properties) {
                return new PendingKeyExchange(properties);
            };

            /**
             * Encodes the specified PendingKeyExchange message. Does not implicitly {@link proto.SessionStructure.PendingKeyExchange.verify|verify} messages.
             * @function encode
             * @memberof proto.SessionStructure.PendingKeyExchange
             * @static
             * @param {proto.SessionStructure.IPendingKeyExchange} message PendingKeyExchange message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PendingKeyExchange.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.sequence != null && Object.hasOwnProperty.call(message, "sequence"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.sequence);
                if (message.localBaseKey != null && Object.hasOwnProperty.call(message, "localBaseKey"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.localBaseKey);
                if (message.localBaseKeyPrivate != null && Object.hasOwnProperty.call(message, "localBaseKeyPrivate"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.localBaseKeyPrivate);
                if (message.localRatchetKey != null && Object.hasOwnProperty.call(message, "localRatchetKey"))
                    writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.localRatchetKey);
                if (message.localRatchetKeyPrivate != null && Object.hasOwnProperty.call(message, "localRatchetKeyPrivate"))
                    writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.localRatchetKeyPrivate);
                if (message.localIdentityKey != null && Object.hasOwnProperty.call(message, "localIdentityKey"))
                    writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.localIdentityKey);
                if (message.localIdentityKeyPrivate != null && Object.hasOwnProperty.call(message, "localIdentityKeyPrivate"))
                    writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.localIdentityKeyPrivate);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PendingKeyExchange message, length delimited. Does not implicitly {@link proto.SessionStructure.PendingKeyExchange.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SessionStructure.PendingKeyExchange
             * @static
             * @param {proto.SessionStructure.IPendingKeyExchange} message PendingKeyExchange message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PendingKeyExchange.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PendingKeyExchange message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SessionStructure.PendingKeyExchange
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SessionStructure.PendingKeyExchange} PendingKeyExchange
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PendingKeyExchange.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.sequence = reader.uint32();
                            message._sequence = "sequence";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.localBaseKey = reader.bytes();
                            message._localBaseKey = "localBaseKey";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.localBaseKeyPrivate = reader.bytes();
                            message._localBaseKeyPrivate = "localBaseKeyPrivate";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.localRatchetKey = reader.bytes();
                            message._localRatchetKey = "localRatchetKey";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.localRatchetKeyPrivate = reader.bytes();
                            message._localRatchetKeyPrivate = "localRatchetKeyPrivate";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.localIdentityKey = reader.bytes();
                            message._localIdentityKey = "localIdentityKey";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            message.localIdentityKeyPrivate = reader.bytes();
                            message._localIdentityKeyPrivate = "localIdentityKeyPrivate";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PendingKeyExchange message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SessionStructure.PendingKeyExchange
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SessionStructure.PendingKeyExchange} PendingKeyExchange
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PendingKeyExchange.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PendingKeyExchange message.
             * @function verify
             * @memberof proto.SessionStructure.PendingKeyExchange
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PendingKeyExchange.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.sequence != null && Object.hasOwnProperty.call(message, "sequence")) {
                    properties._sequence = 1;
                    if (!$util.isInteger(message.sequence))
                        return "sequence: integer expected";
                }
                if (message.localBaseKey != null && Object.hasOwnProperty.call(message, "localBaseKey")) {
                    properties._localBaseKey = 1;
                    if (!(message.localBaseKey && typeof message.localBaseKey.length === "number" || $util.isString(message.localBaseKey)))
                        return "localBaseKey: buffer expected";
                }
                if (message.localBaseKeyPrivate != null && Object.hasOwnProperty.call(message, "localBaseKeyPrivate")) {
                    properties._localBaseKeyPrivate = 1;
                    if (!(message.localBaseKeyPrivate && typeof message.localBaseKeyPrivate.length === "number" || $util.isString(message.localBaseKeyPrivate)))
                        return "localBaseKeyPrivate: buffer expected";
                }
                if (message.localRatchetKey != null && Object.hasOwnProperty.call(message, "localRatchetKey")) {
                    properties._localRatchetKey = 1;
                    if (!(message.localRatchetKey && typeof message.localRatchetKey.length === "number" || $util.isString(message.localRatchetKey)))
                        return "localRatchetKey: buffer expected";
                }
                if (message.localRatchetKeyPrivate != null && Object.hasOwnProperty.call(message, "localRatchetKeyPrivate")) {
                    properties._localRatchetKeyPrivate = 1;
                    if (!(message.localRatchetKeyPrivate && typeof message.localRatchetKeyPrivate.length === "number" || $util.isString(message.localRatchetKeyPrivate)))
                        return "localRatchetKeyPrivate: buffer expected";
                }
                if (message.localIdentityKey != null && Object.hasOwnProperty.call(message, "localIdentityKey")) {
                    properties._localIdentityKey = 1;
                    if (!(message.localIdentityKey && typeof message.localIdentityKey.length === "number" || $util.isString(message.localIdentityKey)))
                        return "localIdentityKey: buffer expected";
                }
                if (message.localIdentityKeyPrivate != null && Object.hasOwnProperty.call(message, "localIdentityKeyPrivate")) {
                    properties._localIdentityKeyPrivate = 1;
                    if (!(message.localIdentityKeyPrivate && typeof message.localIdentityKeyPrivate.length === "number" || $util.isString(message.localIdentityKeyPrivate)))
                        return "localIdentityKeyPrivate: buffer expected";
                }
                return null;
            };

            /**
             * Creates a PendingKeyExchange message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SessionStructure.PendingKeyExchange
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SessionStructure.PendingKeyExchange} PendingKeyExchange
             */
            PendingKeyExchange.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SessionStructure.PendingKeyExchange: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.sequence != null)
                    message.sequence = object.sequence >>> 0;
                if (object.localBaseKey != null)
                    if (typeof object.localBaseKey === "string")
                        $util.base64.decode(object.localBaseKey, message.localBaseKey = $util.newBuffer($util.base64.length(object.localBaseKey)), 0);
                    else if (object.localBaseKey.length >= 0)
                        message.localBaseKey = object.localBaseKey;
                if (object.localBaseKeyPrivate != null)
                    if (typeof object.localBaseKeyPrivate === "string")
                        $util.base64.decode(object.localBaseKeyPrivate, message.localBaseKeyPrivate = $util.newBuffer($util.base64.length(object.localBaseKeyPrivate)), 0);
                    else if (object.localBaseKeyPrivate.length >= 0)
                        message.localBaseKeyPrivate = object.localBaseKeyPrivate;
                if (object.localRatchetKey != null)
                    if (typeof object.localRatchetKey === "string")
                        $util.base64.decode(object.localRatchetKey, message.localRatchetKey = $util.newBuffer($util.base64.length(object.localRatchetKey)), 0);
                    else if (object.localRatchetKey.length >= 0)
                        message.localRatchetKey = object.localRatchetKey;
                if (object.localRatchetKeyPrivate != null)
                    if (typeof object.localRatchetKeyPrivate === "string")
                        $util.base64.decode(object.localRatchetKeyPrivate, message.localRatchetKeyPrivate = $util.newBuffer($util.base64.length(object.localRatchetKeyPrivate)), 0);
                    else if (object.localRatchetKeyPrivate.length >= 0)
                        message.localRatchetKeyPrivate = object.localRatchetKeyPrivate;
                if (object.localIdentityKey != null)
                    if (typeof object.localIdentityKey === "string")
                        $util.base64.decode(object.localIdentityKey, message.localIdentityKey = $util.newBuffer($util.base64.length(object.localIdentityKey)), 0);
                    else if (object.localIdentityKey.length >= 0)
                        message.localIdentityKey = object.localIdentityKey;
                if (object.localIdentityKeyPrivate != null)
                    if (typeof object.localIdentityKeyPrivate === "string")
                        $util.base64.decode(object.localIdentityKeyPrivate, message.localIdentityKeyPrivate = $util.newBuffer($util.base64.length(object.localIdentityKeyPrivate)), 0);
                    else if (object.localIdentityKeyPrivate.length >= 0)
                        message.localIdentityKeyPrivate = object.localIdentityKeyPrivate;
                return message;
            };

            /**
             * Creates a plain object from a PendingKeyExchange message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SessionStructure.PendingKeyExchange
             * @static
             * @param {proto.SessionStructure.PendingKeyExchange} message PendingKeyExchange
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PendingKeyExchange.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.sequence != null && Object.hasOwnProperty.call(message, "sequence"))
                    object.sequence = message.sequence;
                if (message.localBaseKey != null && Object.hasOwnProperty.call(message, "localBaseKey"))
                    object.localBaseKey = options.bytes === String ? $util.base64.encode(message.localBaseKey, 0, message.localBaseKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.localBaseKey) : message.localBaseKey;
                if (message.localBaseKeyPrivate != null && Object.hasOwnProperty.call(message, "localBaseKeyPrivate"))
                    object.localBaseKeyPrivate = options.bytes === String ? $util.base64.encode(message.localBaseKeyPrivate, 0, message.localBaseKeyPrivate.length) : options.bytes === Array ? Array.prototype.slice.call(message.localBaseKeyPrivate) : message.localBaseKeyPrivate;
                if (message.localRatchetKey != null && Object.hasOwnProperty.call(message, "localRatchetKey"))
                    object.localRatchetKey = options.bytes === String ? $util.base64.encode(message.localRatchetKey, 0, message.localRatchetKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.localRatchetKey) : message.localRatchetKey;
                if (message.localRatchetKeyPrivate != null && Object.hasOwnProperty.call(message, "localRatchetKeyPrivate"))
                    object.localRatchetKeyPrivate = options.bytes === String ? $util.base64.encode(message.localRatchetKeyPrivate, 0, message.localRatchetKeyPrivate.length) : options.bytes === Array ? Array.prototype.slice.call(message.localRatchetKeyPrivate) : message.localRatchetKeyPrivate;
                if (message.localIdentityKey != null && Object.hasOwnProperty.call(message, "localIdentityKey"))
                    object.localIdentityKey = options.bytes === String ? $util.base64.encode(message.localIdentityKey, 0, message.localIdentityKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.localIdentityKey) : message.localIdentityKey;
                if (message.localIdentityKeyPrivate != null && Object.hasOwnProperty.call(message, "localIdentityKeyPrivate"))
                    object.localIdentityKeyPrivate = options.bytes === String ? $util.base64.encode(message.localIdentityKeyPrivate, 0, message.localIdentityKeyPrivate.length) : options.bytes === Array ? Array.prototype.slice.call(message.localIdentityKeyPrivate) : message.localIdentityKeyPrivate;
                return object;
            };

            /**
             * Converts this PendingKeyExchange to JSON.
             * @function toJSON
             * @memberof proto.SessionStructure.PendingKeyExchange
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PendingKeyExchange.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PendingKeyExchange
             * @function getTypeUrl
             * @memberof proto.SessionStructure.PendingKeyExchange
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PendingKeyExchange.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SessionStructure.PendingKeyExchange";
            };

            return PendingKeyExchange;
        })();

        SessionStructure.PendingPreKey = (function() {

            /**
             * Properties of a PendingPreKey.
             * @memberof proto.SessionStructure
             * @interface IPendingPreKey
             * @property {number|null} [preKeyId] PendingPreKey preKeyId
             * @property {number|null} [signedPreKeyId] PendingPreKey signedPreKeyId
             * @property {Uint8Array|null} [baseKey] PendingPreKey baseKey
             * @property {number|null} [kyberPreKeyId] PendingPreKey kyberPreKeyId
             * @property {Uint8Array|null} [kyberCiphertext] PendingPreKey kyberCiphertext
             */

            /**
             * Constructs a new PendingPreKey.
             * @memberof proto.SessionStructure
             * @classdesc Represents a PendingPreKey.
             * @implements IPendingPreKey
             * @constructor
             * @param {proto.SessionStructure.IPendingPreKey=} [properties] Properties to set
             */
            function PendingPreKey(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PendingPreKey preKeyId.
             * @member {number|null|undefined} preKeyId
             * @memberof proto.SessionStructure.PendingPreKey
             * @instance
             */
            PendingPreKey.prototype.preKeyId = null;

            /**
             * PendingPreKey signedPreKeyId.
             * @member {number|null|undefined} signedPreKeyId
             * @memberof proto.SessionStructure.PendingPreKey
             * @instance
             */
            PendingPreKey.prototype.signedPreKeyId = null;

            /**
             * PendingPreKey baseKey.
             * @member {Uint8Array|null|undefined} baseKey
             * @memberof proto.SessionStructure.PendingPreKey
             * @instance
             */
            PendingPreKey.prototype.baseKey = null;

            /**
             * PendingPreKey kyberPreKeyId.
             * @member {number|null|undefined} kyberPreKeyId
             * @memberof proto.SessionStructure.PendingPreKey
             * @instance
             */
            PendingPreKey.prototype.kyberPreKeyId = null;

            /**
             * PendingPreKey kyberCiphertext.
             * @member {Uint8Array|null|undefined} kyberCiphertext
             * @memberof proto.SessionStructure.PendingPreKey
             * @instance
             */
            PendingPreKey.prototype.kyberCiphertext = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PendingPreKey.prototype, "_preKeyId", {
                get: $util.oneOfGetter($oneOfFields = ["preKeyId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PendingPreKey.prototype, "_signedPreKeyId", {
                get: $util.oneOfGetter($oneOfFields = ["signedPreKeyId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PendingPreKey.prototype, "_baseKey", {
                get: $util.oneOfGetter($oneOfFields = ["baseKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PendingPreKey.prototype, "_kyberPreKeyId", {
                get: $util.oneOfGetter($oneOfFields = ["kyberPreKeyId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PendingPreKey.prototype, "_kyberCiphertext", {
                get: $util.oneOfGetter($oneOfFields = ["kyberCiphertext"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PendingPreKey instance using the specified properties.
             * @function create
             * @memberof proto.SessionStructure.PendingPreKey
             * @static
             * @param {proto.SessionStructure.IPendingPreKey=} [properties] Properties to set
             * @returns {proto.SessionStructure.PendingPreKey} PendingPreKey instance
             */
            PendingPreKey.create = function create(properties) {
                return new PendingPreKey(properties);
            };

            /**
             * Encodes the specified PendingPreKey message. Does not implicitly {@link proto.SessionStructure.PendingPreKey.verify|verify} messages.
             * @function encode
             * @memberof proto.SessionStructure.PendingPreKey
             * @static
             * @param {proto.SessionStructure.IPendingPreKey} message PendingPreKey message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PendingPreKey.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.preKeyId != null && Object.hasOwnProperty.call(message, "preKeyId"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.preKeyId);
                if (message.baseKey != null && Object.hasOwnProperty.call(message, "baseKey"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.baseKey);
                if (message.signedPreKeyId != null && Object.hasOwnProperty.call(message, "signedPreKeyId"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.signedPreKeyId);
                if (message.kyberPreKeyId != null && Object.hasOwnProperty.call(message, "kyberPreKeyId"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.kyberPreKeyId);
                if (message.kyberCiphertext != null && Object.hasOwnProperty.call(message, "kyberCiphertext"))
                    writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.kyberCiphertext);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PendingPreKey message, length delimited. Does not implicitly {@link proto.SessionStructure.PendingPreKey.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SessionStructure.PendingPreKey
             * @static
             * @param {proto.SessionStructure.IPendingPreKey} message PendingPreKey message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PendingPreKey.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PendingPreKey message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SessionStructure.PendingPreKey
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SessionStructure.PendingPreKey} PendingPreKey
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PendingPreKey.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.preKeyId = reader.uint32();
                            message._preKeyId = "preKeyId";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.signedPreKeyId = reader.int32();
                            message._signedPreKeyId = "signedPreKeyId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.baseKey = reader.bytes();
                            message._baseKey = "baseKey";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.kyberPreKeyId = reader.uint32();
                            message._kyberPreKeyId = "kyberPreKeyId";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.kyberCiphertext = reader.bytes();
                            message._kyberCiphertext = "kyberCiphertext";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PendingPreKey message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SessionStructure.PendingPreKey
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SessionStructure.PendingPreKey} PendingPreKey
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PendingPreKey.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PendingPreKey message.
             * @function verify
             * @memberof proto.SessionStructure.PendingPreKey
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PendingPreKey.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.preKeyId != null && Object.hasOwnProperty.call(message, "preKeyId")) {
                    properties._preKeyId = 1;
                    if (!$util.isInteger(message.preKeyId))
                        return "preKeyId: integer expected";
                }
                if (message.signedPreKeyId != null && Object.hasOwnProperty.call(message, "signedPreKeyId")) {
                    properties._signedPreKeyId = 1;
                    if (!$util.isInteger(message.signedPreKeyId))
                        return "signedPreKeyId: integer expected";
                }
                if (message.baseKey != null && Object.hasOwnProperty.call(message, "baseKey")) {
                    properties._baseKey = 1;
                    if (!(message.baseKey && typeof message.baseKey.length === "number" || $util.isString(message.baseKey)))
                        return "baseKey: buffer expected";
                }
                if (message.kyberPreKeyId != null && Object.hasOwnProperty.call(message, "kyberPreKeyId")) {
                    properties._kyberPreKeyId = 1;
                    if (!$util.isInteger(message.kyberPreKeyId))
                        return "kyberPreKeyId: integer expected";
                }
                if (message.kyberCiphertext != null && Object.hasOwnProperty.call(message, "kyberCiphertext")) {
                    properties._kyberCiphertext = 1;
                    if (!(message.kyberCiphertext && typeof message.kyberCiphertext.length === "number" || $util.isString(message.kyberCiphertext)))
                        return "kyberCiphertext: buffer expected";
                }
                return null;
            };

            /**
             * Creates a PendingPreKey message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SessionStructure.PendingPreKey
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SessionStructure.PendingPreKey} PendingPreKey
             */
            PendingPreKey.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SessionStructure.PendingPreKey: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.preKeyId != null)
                    message.preKeyId = object.preKeyId >>> 0;
                if (object.signedPreKeyId != null)
                    message.signedPreKeyId = object.signedPreKeyId | 0;
                if (object.baseKey != null)
                    if (typeof object.baseKey === "string")
                        $util.base64.decode(object.baseKey, message.baseKey = $util.newBuffer($util.base64.length(object.baseKey)), 0);
                    else if (object.baseKey.length >= 0)
                        message.baseKey = object.baseKey;
                if (object.kyberPreKeyId != null)
                    message.kyberPreKeyId = object.kyberPreKeyId >>> 0;
                if (object.kyberCiphertext != null)
                    if (typeof object.kyberCiphertext === "string")
                        $util.base64.decode(object.kyberCiphertext, message.kyberCiphertext = $util.newBuffer($util.base64.length(object.kyberCiphertext)), 0);
                    else if (object.kyberCiphertext.length >= 0)
                        message.kyberCiphertext = object.kyberCiphertext;
                return message;
            };

            /**
             * Creates a plain object from a PendingPreKey message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SessionStructure.PendingPreKey
             * @static
             * @param {proto.SessionStructure.PendingPreKey} message PendingPreKey
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PendingPreKey.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.preKeyId != null && Object.hasOwnProperty.call(message, "preKeyId"))
                    object.preKeyId = message.preKeyId;
                if (message.baseKey != null && Object.hasOwnProperty.call(message, "baseKey"))
                    object.baseKey = options.bytes === String ? $util.base64.encode(message.baseKey, 0, message.baseKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.baseKey) : message.baseKey;
                if (message.signedPreKeyId != null && Object.hasOwnProperty.call(message, "signedPreKeyId"))
                    object.signedPreKeyId = message.signedPreKeyId;
                if (message.kyberPreKeyId != null && Object.hasOwnProperty.call(message, "kyberPreKeyId"))
                    object.kyberPreKeyId = message.kyberPreKeyId;
                if (message.kyberCiphertext != null && Object.hasOwnProperty.call(message, "kyberCiphertext"))
                    object.kyberCiphertext = options.bytes === String ? $util.base64.encode(message.kyberCiphertext, 0, message.kyberCiphertext.length) : options.bytes === Array ? Array.prototype.slice.call(message.kyberCiphertext) : message.kyberCiphertext;
                return object;
            };

            /**
             * Converts this PendingPreKey to JSON.
             * @function toJSON
             * @memberof proto.SessionStructure.PendingPreKey
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PendingPreKey.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PendingPreKey
             * @function getTypeUrl
             * @memberof proto.SessionStructure.PendingPreKey
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PendingPreKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SessionStructure.PendingPreKey";
            };

            return PendingPreKey;
        })();

        return SessionStructure;
    })();

    proto.SessionTransparencyMetadata = (function() {

        /**
         * Properties of a SessionTransparencyMetadata.
         * @memberof proto
         * @interface ISessionTransparencyMetadata
         * @property {string|null} [disclaimerText] SessionTransparencyMetadata disclaimerText
         * @property {string|null} [hcaId] SessionTransparencyMetadata hcaId
         * @property {proto.SessionTransparencyType|null} [sessionTransparencyType] SessionTransparencyMetadata sessionTransparencyType
         */

        /**
         * Constructs a new SessionTransparencyMetadata.
         * @memberof proto
         * @classdesc Represents a SessionTransparencyMetadata.
         * @implements ISessionTransparencyMetadata
         * @constructor
         * @param {proto.ISessionTransparencyMetadata=} [properties] Properties to set
         */
        function SessionTransparencyMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SessionTransparencyMetadata disclaimerText.
         * @member {string|null|undefined} disclaimerText
         * @memberof proto.SessionTransparencyMetadata
         * @instance
         */
        SessionTransparencyMetadata.prototype.disclaimerText = null;

        /**
         * SessionTransparencyMetadata hcaId.
         * @member {string|null|undefined} hcaId
         * @memberof proto.SessionTransparencyMetadata
         * @instance
         */
        SessionTransparencyMetadata.prototype.hcaId = null;

        /**
         * SessionTransparencyMetadata sessionTransparencyType.
         * @member {proto.SessionTransparencyType|null|undefined} sessionTransparencyType
         * @memberof proto.SessionTransparencyMetadata
         * @instance
         */
        SessionTransparencyMetadata.prototype.sessionTransparencyType = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SessionTransparencyMetadata.prototype, "_disclaimerText", {
            get: $util.oneOfGetter($oneOfFields = ["disclaimerText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SessionTransparencyMetadata.prototype, "_hcaId", {
            get: $util.oneOfGetter($oneOfFields = ["hcaId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SessionTransparencyMetadata.prototype, "_sessionTransparencyType", {
            get: $util.oneOfGetter($oneOfFields = ["sessionTransparencyType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SessionTransparencyMetadata instance using the specified properties.
         * @function create
         * @memberof proto.SessionTransparencyMetadata
         * @static
         * @param {proto.ISessionTransparencyMetadata=} [properties] Properties to set
         * @returns {proto.SessionTransparencyMetadata} SessionTransparencyMetadata instance
         */
        SessionTransparencyMetadata.create = function create(properties) {
            return new SessionTransparencyMetadata(properties);
        };

        /**
         * Encodes the specified SessionTransparencyMetadata message. Does not implicitly {@link proto.SessionTransparencyMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.SessionTransparencyMetadata
         * @static
         * @param {proto.ISessionTransparencyMetadata} message SessionTransparencyMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SessionTransparencyMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.disclaimerText != null && Object.hasOwnProperty.call(message, "disclaimerText"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.disclaimerText);
            if (message.hcaId != null && Object.hasOwnProperty.call(message, "hcaId"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.hcaId);
            if (message.sessionTransparencyType != null && Object.hasOwnProperty.call(message, "sessionTransparencyType"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.sessionTransparencyType);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SessionTransparencyMetadata message, length delimited. Does not implicitly {@link proto.SessionTransparencyMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SessionTransparencyMetadata
         * @static
         * @param {proto.ISessionTransparencyMetadata} message SessionTransparencyMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SessionTransparencyMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SessionTransparencyMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SessionTransparencyMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SessionTransparencyMetadata} SessionTransparencyMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SessionTransparencyMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.disclaimerText = reader.stringVerify();
                        message._disclaimerText = "disclaimerText";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.hcaId = reader.stringVerify();
                        message._hcaId = "hcaId";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.sessionTransparencyType = reader.int32();
                        message._sessionTransparencyType = "sessionTransparencyType";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a SessionTransparencyMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SessionTransparencyMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SessionTransparencyMetadata} SessionTransparencyMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SessionTransparencyMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SessionTransparencyMetadata message.
         * @function verify
         * @memberof proto.SessionTransparencyMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SessionTransparencyMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.disclaimerText != null && Object.hasOwnProperty.call(message, "disclaimerText")) {
                properties._disclaimerText = 1;
                if (!$util.isString(message.disclaimerText))
                    return "disclaimerText: string expected";
            }
            if (message.hcaId != null && Object.hasOwnProperty.call(message, "hcaId")) {
                properties._hcaId = 1;
                if (!$util.isString(message.hcaId))
                    return "hcaId: string expected";
            }
            if (message.sessionTransparencyType != null && Object.hasOwnProperty.call(message, "sessionTransparencyType")) {
                properties._sessionTransparencyType = 1;
                if (typeof message.sessionTransparencyType !== "number" || (message.sessionTransparencyType | 0) !== message.sessionTransparencyType)
                    return "sessionTransparencyType: enum value expected";
            }
            return null;
        };

        /**
         * Creates a SessionTransparencyMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SessionTransparencyMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SessionTransparencyMetadata} SessionTransparencyMetadata
         */
        SessionTransparencyMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.SessionTransparencyMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.disclaimerText != null)
                message.disclaimerText = String(object.disclaimerText);
            if (object.hcaId != null)
                message.hcaId = String(object.hcaId);
            switch (object.sessionTransparencyType) {
            case "UNKNOWN_TYPE":
            case 0:
                message.sessionTransparencyType = 0;
                break;
            case "NY_AI_SAFETY_DISCLAIMER":
            case 1:
                message.sessionTransparencyType = 1;
                break;
            default:
                if (typeof object.sessionTransparencyType === "number" && (object.sessionTransparencyType | 0) === object.sessionTransparencyType)
                    message.sessionTransparencyType = object.sessionTransparencyType;
            }
            return message;
        };

        /**
         * Creates a plain object from a SessionTransparencyMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SessionTransparencyMetadata
         * @static
         * @param {proto.SessionTransparencyMetadata} message SessionTransparencyMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SessionTransparencyMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.disclaimerText != null && Object.hasOwnProperty.call(message, "disclaimerText"))
                object.disclaimerText = message.disclaimerText;
            if (message.hcaId != null && Object.hasOwnProperty.call(message, "hcaId"))
                object.hcaId = message.hcaId;
            if (message.sessionTransparencyType != null && Object.hasOwnProperty.call(message, "sessionTransparencyType"))
                object.sessionTransparencyType = options.enums === String ? $root.proto.SessionTransparencyType[message.sessionTransparencyType] === undefined ? message.sessionTransparencyType : $root.proto.SessionTransparencyType[message.sessionTransparencyType] : message.sessionTransparencyType;
            return object;
        };

        /**
         * Converts this SessionTransparencyMetadata to JSON.
         * @function toJSON
         * @memberof proto.SessionTransparencyMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SessionTransparencyMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SessionTransparencyMetadata
         * @function getTypeUrl
         * @memberof proto.SessionTransparencyMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SessionTransparencyMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.SessionTransparencyMetadata";
        };

        return SessionTransparencyMetadata;
    })();

    /**
     * SessionTransparencyType enum.
     * @name proto.SessionTransparencyType
     * @enum {number}
     * @property {number} UNKNOWN_TYPE=0 UNKNOWN_TYPE value
     * @property {number} NY_AI_SAFETY_DISCLAIMER=1 NY_AI_SAFETY_DISCLAIMER value
     */
    proto.SessionTransparencyType = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "UNKNOWN_TYPE"] = 0;
        values[valuesById[1] = "NY_AI_SAFETY_DISCLAIMER"] = 1;
        return values;
    })();

    proto.SignalMessage = (function() {

        /**
         * Properties of a SignalMessage.
         * @memberof proto
         * @interface ISignalMessage
         * @property {Uint8Array|null} [ratchetKey] SignalMessage ratchetKey
         * @property {number|null} [counter] SignalMessage counter
         * @property {number|null} [previousCounter] SignalMessage previousCounter
         * @property {Uint8Array|null} [ciphertext] SignalMessage ciphertext
         */

        /**
         * Constructs a new SignalMessage.
         * @memberof proto
         * @classdesc Represents a SignalMessage.
         * @implements ISignalMessage
         * @constructor
         * @param {proto.ISignalMessage=} [properties] Properties to set
         */
        function SignalMessage(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SignalMessage ratchetKey.
         * @member {Uint8Array|null|undefined} ratchetKey
         * @memberof proto.SignalMessage
         * @instance
         */
        SignalMessage.prototype.ratchetKey = null;

        /**
         * SignalMessage counter.
         * @member {number|null|undefined} counter
         * @memberof proto.SignalMessage
         * @instance
         */
        SignalMessage.prototype.counter = null;

        /**
         * SignalMessage previousCounter.
         * @member {number|null|undefined} previousCounter
         * @memberof proto.SignalMessage
         * @instance
         */
        SignalMessage.prototype.previousCounter = null;

        /**
         * SignalMessage ciphertext.
         * @member {Uint8Array|null|undefined} ciphertext
         * @memberof proto.SignalMessage
         * @instance
         */
        SignalMessage.prototype.ciphertext = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SignalMessage.prototype, "_ratchetKey", {
            get: $util.oneOfGetter($oneOfFields = ["ratchetKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SignalMessage.prototype, "_counter", {
            get: $util.oneOfGetter($oneOfFields = ["counter"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SignalMessage.prototype, "_previousCounter", {
            get: $util.oneOfGetter($oneOfFields = ["previousCounter"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SignalMessage.prototype, "_ciphertext", {
            get: $util.oneOfGetter($oneOfFields = ["ciphertext"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SignalMessage instance using the specified properties.
         * @function create
         * @memberof proto.SignalMessage
         * @static
         * @param {proto.ISignalMessage=} [properties] Properties to set
         * @returns {proto.SignalMessage} SignalMessage instance
         */
        SignalMessage.create = function create(properties) {
            return new SignalMessage(properties);
        };

        /**
         * Encodes the specified SignalMessage message. Does not implicitly {@link proto.SignalMessage.verify|verify} messages.
         * @function encode
         * @memberof proto.SignalMessage
         * @static
         * @param {proto.ISignalMessage} message SignalMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignalMessage.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ratchetKey != null && Object.hasOwnProperty.call(message, "ratchetKey"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.ratchetKey);
            if (message.counter != null && Object.hasOwnProperty.call(message, "counter"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.counter);
            if (message.previousCounter != null && Object.hasOwnProperty.call(message, "previousCounter"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.previousCounter);
            if (message.ciphertext != null && Object.hasOwnProperty.call(message, "ciphertext"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.ciphertext);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SignalMessage message, length delimited. Does not implicitly {@link proto.SignalMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SignalMessage
         * @static
         * @param {proto.ISignalMessage} message SignalMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignalMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SignalMessage message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SignalMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SignalMessage} SignalMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignalMessage.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.ratchetKey = reader.bytes();
                        message._ratchetKey = "ratchetKey";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.counter = reader.uint32();
                        message._counter = "counter";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.previousCounter = reader.uint32();
                        message._previousCounter = "previousCounter";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.ciphertext = reader.bytes();
                        message._ciphertext = "ciphertext";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a SignalMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SignalMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SignalMessage} SignalMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignalMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SignalMessage message.
         * @function verify
         * @memberof proto.SignalMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SignalMessage.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.ratchetKey != null && Object.hasOwnProperty.call(message, "ratchetKey")) {
                properties._ratchetKey = 1;
                if (!(message.ratchetKey && typeof message.ratchetKey.length === "number" || $util.isString(message.ratchetKey)))
                    return "ratchetKey: buffer expected";
            }
            if (message.counter != null && Object.hasOwnProperty.call(message, "counter")) {
                properties._counter = 1;
                if (!$util.isInteger(message.counter))
                    return "counter: integer expected";
            }
            if (message.previousCounter != null && Object.hasOwnProperty.call(message, "previousCounter")) {
                properties._previousCounter = 1;
                if (!$util.isInteger(message.previousCounter))
                    return "previousCounter: integer expected";
            }
            if (message.ciphertext != null && Object.hasOwnProperty.call(message, "ciphertext")) {
                properties._ciphertext = 1;
                if (!(message.ciphertext && typeof message.ciphertext.length === "number" || $util.isString(message.ciphertext)))
                    return "ciphertext: buffer expected";
            }
            return null;
        };

        /**
         * Creates a SignalMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SignalMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SignalMessage} SignalMessage
         */
        SignalMessage.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.SignalMessage: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.ratchetKey != null)
                if (typeof object.ratchetKey === "string")
                    $util.base64.decode(object.ratchetKey, message.ratchetKey = $util.newBuffer($util.base64.length(object.ratchetKey)), 0);
                else if (object.ratchetKey.length >= 0)
                    message.ratchetKey = object.ratchetKey;
            if (object.counter != null)
                message.counter = object.counter >>> 0;
            if (object.previousCounter != null)
                message.previousCounter = object.previousCounter >>> 0;
            if (object.ciphertext != null)
                if (typeof object.ciphertext === "string")
                    $util.base64.decode(object.ciphertext, message.ciphertext = $util.newBuffer($util.base64.length(object.ciphertext)), 0);
                else if (object.ciphertext.length >= 0)
                    message.ciphertext = object.ciphertext;
            return message;
        };

        /**
         * Creates a plain object from a SignalMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SignalMessage
         * @static
         * @param {proto.SignalMessage} message SignalMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SignalMessage.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.ratchetKey != null && Object.hasOwnProperty.call(message, "ratchetKey"))
                object.ratchetKey = options.bytes === String ? $util.base64.encode(message.ratchetKey, 0, message.ratchetKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.ratchetKey) : message.ratchetKey;
            if (message.counter != null && Object.hasOwnProperty.call(message, "counter"))
                object.counter = message.counter;
            if (message.previousCounter != null && Object.hasOwnProperty.call(message, "previousCounter"))
                object.previousCounter = message.previousCounter;
            if (message.ciphertext != null && Object.hasOwnProperty.call(message, "ciphertext"))
                object.ciphertext = options.bytes === String ? $util.base64.encode(message.ciphertext, 0, message.ciphertext.length) : options.bytes === Array ? Array.prototype.slice.call(message.ciphertext) : message.ciphertext;
            return object;
        };

        /**
         * Converts this SignalMessage to JSON.
         * @function toJSON
         * @memberof proto.SignalMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SignalMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SignalMessage
         * @function getTypeUrl
         * @memberof proto.SignalMessage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SignalMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.SignalMessage";
        };

        return SignalMessage;
    })();

    proto.SignedPreKeyRecordStructure = (function() {

        /**
         * Properties of a SignedPreKeyRecordStructure.
         * @memberof proto
         * @interface ISignedPreKeyRecordStructure
         * @property {number|null} [id] SignedPreKeyRecordStructure id
         * @property {Uint8Array|null} [publicKey] SignedPreKeyRecordStructure publicKey
         * @property {Uint8Array|null} [privateKey] SignedPreKeyRecordStructure privateKey
         * @property {Uint8Array|null} [signature] SignedPreKeyRecordStructure signature
         * @property {number|Long|null} [timestamp] SignedPreKeyRecordStructure timestamp
         */

        /**
         * Constructs a new SignedPreKeyRecordStructure.
         * @memberof proto
         * @classdesc Represents a SignedPreKeyRecordStructure.
         * @implements ISignedPreKeyRecordStructure
         * @constructor
         * @param {proto.ISignedPreKeyRecordStructure=} [properties] Properties to set
         */
        function SignedPreKeyRecordStructure(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SignedPreKeyRecordStructure id.
         * @member {number|null|undefined} id
         * @memberof proto.SignedPreKeyRecordStructure
         * @instance
         */
        SignedPreKeyRecordStructure.prototype.id = null;

        /**
         * SignedPreKeyRecordStructure publicKey.
         * @member {Uint8Array|null|undefined} publicKey
         * @memberof proto.SignedPreKeyRecordStructure
         * @instance
         */
        SignedPreKeyRecordStructure.prototype.publicKey = null;

        /**
         * SignedPreKeyRecordStructure privateKey.
         * @member {Uint8Array|null|undefined} privateKey
         * @memberof proto.SignedPreKeyRecordStructure
         * @instance
         */
        SignedPreKeyRecordStructure.prototype.privateKey = null;

        /**
         * SignedPreKeyRecordStructure signature.
         * @member {Uint8Array|null|undefined} signature
         * @memberof proto.SignedPreKeyRecordStructure
         * @instance
         */
        SignedPreKeyRecordStructure.prototype.signature = null;

        /**
         * SignedPreKeyRecordStructure timestamp.
         * @member {number|Long|null|undefined} timestamp
         * @memberof proto.SignedPreKeyRecordStructure
         * @instance
         */
        SignedPreKeyRecordStructure.prototype.timestamp = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SignedPreKeyRecordStructure.prototype, "_id", {
            get: $util.oneOfGetter($oneOfFields = ["id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SignedPreKeyRecordStructure.prototype, "_publicKey", {
            get: $util.oneOfGetter($oneOfFields = ["publicKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SignedPreKeyRecordStructure.prototype, "_privateKey", {
            get: $util.oneOfGetter($oneOfFields = ["privateKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SignedPreKeyRecordStructure.prototype, "_signature", {
            get: $util.oneOfGetter($oneOfFields = ["signature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SignedPreKeyRecordStructure.prototype, "_timestamp", {
            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SignedPreKeyRecordStructure instance using the specified properties.
         * @function create
         * @memberof proto.SignedPreKeyRecordStructure
         * @static
         * @param {proto.ISignedPreKeyRecordStructure=} [properties] Properties to set
         * @returns {proto.SignedPreKeyRecordStructure} SignedPreKeyRecordStructure instance
         */
        SignedPreKeyRecordStructure.create = function create(properties) {
            return new SignedPreKeyRecordStructure(properties);
        };

        /**
         * Encodes the specified SignedPreKeyRecordStructure message. Does not implicitly {@link proto.SignedPreKeyRecordStructure.verify|verify} messages.
         * @function encode
         * @memberof proto.SignedPreKeyRecordStructure
         * @static
         * @param {proto.ISignedPreKeyRecordStructure} message SignedPreKeyRecordStructure message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignedPreKeyRecordStructure.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint32(message.id);
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.publicKey);
            if (message.privateKey != null && Object.hasOwnProperty.call(message, "privateKey"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.privateKey);
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.signature);
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                writer.uint32(/* id 5, wireType 1 =*/41).fixed64(message.timestamp);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SignedPreKeyRecordStructure message, length delimited. Does not implicitly {@link proto.SignedPreKeyRecordStructure.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SignedPreKeyRecordStructure
         * @static
         * @param {proto.ISignedPreKeyRecordStructure} message SignedPreKeyRecordStructure message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SignedPreKeyRecordStructure.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SignedPreKeyRecordStructure message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SignedPreKeyRecordStructure
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SignedPreKeyRecordStructure} SignedPreKeyRecordStructure
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignedPreKeyRecordStructure.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.id = reader.uint32();
                        message._id = "id";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.publicKey = reader.bytes();
                        message._publicKey = "publicKey";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.privateKey = reader.bytes();
                        message._privateKey = "privateKey";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.signature = reader.bytes();
                        message._signature = "signature";
                        continue;
                    }
                case 5: {
                        if (u !== 1)
                            break;
                        message.timestamp = reader.fixed64();
                        message._timestamp = "timestamp";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a SignedPreKeyRecordStructure message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SignedPreKeyRecordStructure
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SignedPreKeyRecordStructure} SignedPreKeyRecordStructure
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SignedPreKeyRecordStructure.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SignedPreKeyRecordStructure message.
         * @function verify
         * @memberof proto.SignedPreKeyRecordStructure
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SignedPreKeyRecordStructure.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.id != null && Object.hasOwnProperty.call(message, "id")) {
                properties._id = 1;
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            }
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey")) {
                properties._publicKey = 1;
                if (!(message.publicKey && typeof message.publicKey.length === "number" || $util.isString(message.publicKey)))
                    return "publicKey: buffer expected";
            }
            if (message.privateKey != null && Object.hasOwnProperty.call(message, "privateKey")) {
                properties._privateKey = 1;
                if (!(message.privateKey && typeof message.privateKey.length === "number" || $util.isString(message.privateKey)))
                    return "privateKey: buffer expected";
            }
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature")) {
                properties._signature = 1;
                if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                    return "signature: buffer expected";
            }
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp")) {
                properties._timestamp = 1;
                if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                    return "timestamp: integer|Long expected";
            }
            return null;
        };

        /**
         * Creates a SignedPreKeyRecordStructure message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SignedPreKeyRecordStructure
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SignedPreKeyRecordStructure} SignedPreKeyRecordStructure
         */
        SignedPreKeyRecordStructure.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.SignedPreKeyRecordStructure: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.id != null)
                message.id = object.id >>> 0;
            if (object.publicKey != null)
                if (typeof object.publicKey === "string")
                    $util.base64.decode(object.publicKey, message.publicKey = $util.newBuffer($util.base64.length(object.publicKey)), 0);
                else if (object.publicKey.length >= 0)
                    message.publicKey = object.publicKey;
            if (object.privateKey != null)
                if (typeof object.privateKey === "string")
                    $util.base64.decode(object.privateKey, message.privateKey = $util.newBuffer($util.base64.length(object.privateKey)), 0);
                else if (object.privateKey.length >= 0)
                    message.privateKey = object.privateKey;
            if (object.signature != null)
                if (typeof object.signature === "string")
                    $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                else if (object.signature.length >= 0)
                    message.signature = object.signature;
            if (object.timestamp != null)
                if ($util.Long)
                    message.timestamp = $util.Long.fromValue(object.timestamp, true);
                else if (typeof object.timestamp === "string")
                    message.timestamp = parseInt(object.timestamp, 10);
                else if (typeof object.timestamp === "number")
                    message.timestamp = object.timestamp;
                else if (typeof object.timestamp === "object")
                    message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a SignedPreKeyRecordStructure message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SignedPreKeyRecordStructure
         * @static
         * @param {proto.SignedPreKeyRecordStructure} message SignedPreKeyRecordStructure
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SignedPreKeyRecordStructure.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                object.publicKey = options.bytes === String ? $util.base64.encode(message.publicKey, 0, message.publicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.publicKey) : message.publicKey;
            if (message.privateKey != null && Object.hasOwnProperty.call(message, "privateKey"))
                object.privateKey = options.bytes === String ? $util.base64.encode(message.privateKey, 0, message.privateKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.privateKey) : message.privateKey;
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                object.signature = options.bytes === String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === Array ? Array.prototype.slice.call(message.signature) : message.signature;
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.timestamp = typeof message.timestamp === "number" ? BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, true).toBigInt();
                else if (typeof message.timestamp === "number")
                    object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                else
                    object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber(true) : message.timestamp;
            return object;
        };

        /**
         * Converts this SignedPreKeyRecordStructure to JSON.
         * @function toJSON
         * @memberof proto.SignedPreKeyRecordStructure
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SignedPreKeyRecordStructure.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SignedPreKeyRecordStructure
         * @function getTypeUrl
         * @memberof proto.SignedPreKeyRecordStructure
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SignedPreKeyRecordStructure.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.SignedPreKeyRecordStructure";
        };

        return SignedPreKeyRecordStructure;
    })();

    proto.StatusAttribution = (function() {

        /**
         * Properties of a StatusAttribution.
         * @memberof proto
         * @interface IStatusAttribution
         * @property {proto.StatusAttribution.Type|null} [type] StatusAttribution type
         * @property {string|null} [actionUrl] StatusAttribution actionUrl
         * @property {proto.StatusAttribution.IStatusReshare|null} [statusReshare] StatusAttribution statusReshare
         * @property {proto.StatusAttribution.IExternalShare|null} [externalShare] StatusAttribution externalShare
         * @property {proto.StatusAttribution.IMusic|null} [music] StatusAttribution music
         * @property {proto.StatusAttribution.IGroupStatus|null} [groupStatus] StatusAttribution groupStatus
         * @property {proto.StatusAttribution.IRLAttribution|null} [rlAttribution] StatusAttribution rlAttribution
         * @property {proto.StatusAttribution.IAiCreatedAttribution|null} [aiCreatedAttribution] StatusAttribution aiCreatedAttribution
         */

        /**
         * Constructs a new StatusAttribution.
         * @memberof proto
         * @classdesc Represents a StatusAttribution.
         * @implements IStatusAttribution
         * @constructor
         * @param {proto.IStatusAttribution=} [properties] Properties to set
         */
        function StatusAttribution(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StatusAttribution type.
         * @member {proto.StatusAttribution.Type|null|undefined} type
         * @memberof proto.StatusAttribution
         * @instance
         */
        StatusAttribution.prototype.type = null;

        /**
         * StatusAttribution actionUrl.
         * @member {string|null|undefined} actionUrl
         * @memberof proto.StatusAttribution
         * @instance
         */
        StatusAttribution.prototype.actionUrl = null;

        /**
         * StatusAttribution statusReshare.
         * @member {proto.StatusAttribution.IStatusReshare|null|undefined} statusReshare
         * @memberof proto.StatusAttribution
         * @instance
         */
        StatusAttribution.prototype.statusReshare = null;

        /**
         * StatusAttribution externalShare.
         * @member {proto.StatusAttribution.IExternalShare|null|undefined} externalShare
         * @memberof proto.StatusAttribution
         * @instance
         */
        StatusAttribution.prototype.externalShare = null;

        /**
         * StatusAttribution music.
         * @member {proto.StatusAttribution.IMusic|null|undefined} music
         * @memberof proto.StatusAttribution
         * @instance
         */
        StatusAttribution.prototype.music = null;

        /**
         * StatusAttribution groupStatus.
         * @member {proto.StatusAttribution.IGroupStatus|null|undefined} groupStatus
         * @memberof proto.StatusAttribution
         * @instance
         */
        StatusAttribution.prototype.groupStatus = null;

        /**
         * StatusAttribution rlAttribution.
         * @member {proto.StatusAttribution.IRLAttribution|null|undefined} rlAttribution
         * @memberof proto.StatusAttribution
         * @instance
         */
        StatusAttribution.prototype.rlAttribution = null;

        /**
         * StatusAttribution aiCreatedAttribution.
         * @member {proto.StatusAttribution.IAiCreatedAttribution|null|undefined} aiCreatedAttribution
         * @memberof proto.StatusAttribution
         * @instance
         */
        StatusAttribution.prototype.aiCreatedAttribution = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusAttribution.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusAttribution.prototype, "_actionUrl", {
            get: $util.oneOfGetter($oneOfFields = ["actionUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * StatusAttribution attributionData.
         * @member {"statusReshare"|"externalShare"|"music"|"groupStatus"|"rlAttribution"|"aiCreatedAttribution"|undefined} attributionData
         * @memberof proto.StatusAttribution
         * @instance
         */
        Object.defineProperty(StatusAttribution.prototype, "attributionData", {
            get: $util.oneOfGetter($oneOfFields = ["statusReshare", "externalShare", "music", "groupStatus", "rlAttribution", "aiCreatedAttribution"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new StatusAttribution instance using the specified properties.
         * @function create
         * @memberof proto.StatusAttribution
         * @static
         * @param {proto.IStatusAttribution=} [properties] Properties to set
         * @returns {proto.StatusAttribution} StatusAttribution instance
         */
        StatusAttribution.create = function create(properties) {
            return new StatusAttribution(properties);
        };

        /**
         * Encodes the specified StatusAttribution message. Does not implicitly {@link proto.StatusAttribution.verify|verify} messages.
         * @function encode
         * @memberof proto.StatusAttribution
         * @static
         * @param {proto.IStatusAttribution} message StatusAttribution message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatusAttribution.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
            if (message.actionUrl != null && Object.hasOwnProperty.call(message, "actionUrl"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.actionUrl);
            if (message.statusReshare != null && Object.hasOwnProperty.call(message, "statusReshare"))
                $root.proto.StatusAttribution.StatusReshare.encode(message.statusReshare, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.externalShare != null && Object.hasOwnProperty.call(message, "externalShare"))
                $root.proto.StatusAttribution.ExternalShare.encode(message.externalShare, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.music != null && Object.hasOwnProperty.call(message, "music"))
                $root.proto.StatusAttribution.Music.encode(message.music, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
            if (message.groupStatus != null && Object.hasOwnProperty.call(message, "groupStatus"))
                $root.proto.StatusAttribution.GroupStatus.encode(message.groupStatus, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
            if (message.rlAttribution != null && Object.hasOwnProperty.call(message, "rlAttribution"))
                $root.proto.StatusAttribution.RLAttribution.encode(message.rlAttribution, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
            if (message.aiCreatedAttribution != null && Object.hasOwnProperty.call(message, "aiCreatedAttribution"))
                $root.proto.StatusAttribution.AiCreatedAttribution.encode(message.aiCreatedAttribution, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified StatusAttribution message, length delimited. Does not implicitly {@link proto.StatusAttribution.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.StatusAttribution
         * @static
         * @param {proto.IStatusAttribution} message StatusAttribution message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatusAttribution.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a StatusAttribution message from the specified reader or buffer.
         * @function decode
         * @memberof proto.StatusAttribution
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.StatusAttribution} StatusAttribution
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatusAttribution.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.type = reader.int32();
                        message._type = "type";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.actionUrl = reader.stringVerify();
                        message._actionUrl = "actionUrl";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.statusReshare = $root.proto.StatusAttribution.StatusReshare.decode(reader, reader.uint32(), undefined, q + 1, message.statusReshare);
                        message.attributionData = "statusReshare";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.externalShare = $root.proto.StatusAttribution.ExternalShare.decode(reader, reader.uint32(), undefined, q + 1, message.externalShare);
                        message.attributionData = "externalShare";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.music = $root.proto.StatusAttribution.Music.decode(reader, reader.uint32(), undefined, q + 1, message.music);
                        message.attributionData = "music";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        message.groupStatus = $root.proto.StatusAttribution.GroupStatus.decode(reader, reader.uint32(), undefined, q + 1, message.groupStatus);
                        message.attributionData = "groupStatus";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        message.rlAttribution = $root.proto.StatusAttribution.RLAttribution.decode(reader, reader.uint32(), undefined, q + 1, message.rlAttribution);
                        message.attributionData = "rlAttribution";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        message.aiCreatedAttribution = $root.proto.StatusAttribution.AiCreatedAttribution.decode(reader, reader.uint32(), undefined, q + 1, message.aiCreatedAttribution);
                        message.attributionData = "aiCreatedAttribution";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a StatusAttribution message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.StatusAttribution
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.StatusAttribution} StatusAttribution
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatusAttribution.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StatusAttribution message.
         * @function verify
         * @memberof proto.StatusAttribution
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StatusAttribution.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                properties._type = 1;
                if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                    return "type: enum value expected";
            }
            if (message.actionUrl != null && Object.hasOwnProperty.call(message, "actionUrl")) {
                properties._actionUrl = 1;
                if (!$util.isString(message.actionUrl))
                    return "actionUrl: string expected";
            }
            if (message.statusReshare != null && Object.hasOwnProperty.call(message, "statusReshare")) {
                properties.attributionData = 1;
                {
                    var error = $root.proto.StatusAttribution.StatusReshare.verify(message.statusReshare, q + 1);
                    if (error)
                        return "statusReshare." + error;
                }
            }
            if (message.externalShare != null && Object.hasOwnProperty.call(message, "externalShare")) {
                if (properties.attributionData === 1)
                    return "attributionData: multiple values";
                properties.attributionData = 1;
                {
                    var error = $root.proto.StatusAttribution.ExternalShare.verify(message.externalShare, q + 1);
                    if (error)
                        return "externalShare." + error;
                }
            }
            if (message.music != null && Object.hasOwnProperty.call(message, "music")) {
                if (properties.attributionData === 1)
                    return "attributionData: multiple values";
                properties.attributionData = 1;
                {
                    var error = $root.proto.StatusAttribution.Music.verify(message.music, q + 1);
                    if (error)
                        return "music." + error;
                }
            }
            if (message.groupStatus != null && Object.hasOwnProperty.call(message, "groupStatus")) {
                if (properties.attributionData === 1)
                    return "attributionData: multiple values";
                properties.attributionData = 1;
                {
                    var error = $root.proto.StatusAttribution.GroupStatus.verify(message.groupStatus, q + 1);
                    if (error)
                        return "groupStatus." + error;
                }
            }
            if (message.rlAttribution != null && Object.hasOwnProperty.call(message, "rlAttribution")) {
                if (properties.attributionData === 1)
                    return "attributionData: multiple values";
                properties.attributionData = 1;
                {
                    var error = $root.proto.StatusAttribution.RLAttribution.verify(message.rlAttribution, q + 1);
                    if (error)
                        return "rlAttribution." + error;
                }
            }
            if (message.aiCreatedAttribution != null && Object.hasOwnProperty.call(message, "aiCreatedAttribution")) {
                if (properties.attributionData === 1)
                    return "attributionData: multiple values";
                properties.attributionData = 1;
                {
                    var error = $root.proto.StatusAttribution.AiCreatedAttribution.verify(message.aiCreatedAttribution, q + 1);
                    if (error)
                        return "aiCreatedAttribution." + error;
                }
            }
            return null;
        };

        /**
         * Creates a StatusAttribution message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.StatusAttribution
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.StatusAttribution} StatusAttribution
         */
        StatusAttribution.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.StatusAttribution: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.type) {
            case "UNKNOWN":
            case 0:
                message.type = 0;
                break;
            case "RESHARE":
            case 1:
                message.type = 1;
                break;
            case "EXTERNAL_SHARE":
            case 2:
                message.type = 2;
                break;
            case "MUSIC":
            case 3:
                message.type = 3;
                break;
            case "STATUS_MENTION":
            case 4:
                message.type = 4;
                break;
            case "GROUP_STATUS":
            case 5:
                message.type = 5;
                break;
            case "RL_ATTRIBUTION":
            case 6:
                message.type = 6;
                break;
            case "AI_CREATED":
            case 7:
                message.type = 7;
                break;
            case "LAYOUTS":
            case 8:
                message.type = 8;
                break;
            case "NEWSLETTER_STATUS":
            case 9:
                message.type = 9;
                break;
            case "STATUS_CLOSE_SHARING":
            case 10:
                message.type = 10;
                break;
            case "PAID_PARTNERSHIP":
            case 11:
                message.type = 11;
                break;
            default:
                if (typeof object.type === "number" && (object.type | 0) === object.type)
                    message.type = object.type;
            }
            if (object.actionUrl != null)
                message.actionUrl = String(object.actionUrl);
            if (object.statusReshare != null) {
                if (!$util.isObject(object.statusReshare))
                    throw TypeError(".proto.StatusAttribution.statusReshare: object expected");
                message.statusReshare = $root.proto.StatusAttribution.StatusReshare.fromObject(object.statusReshare, q + 1);
            }
            if (object.externalShare != null) {
                if (!$util.isObject(object.externalShare))
                    throw TypeError(".proto.StatusAttribution.externalShare: object expected");
                message.externalShare = $root.proto.StatusAttribution.ExternalShare.fromObject(object.externalShare, q + 1);
            }
            if (object.music != null) {
                if (!$util.isObject(object.music))
                    throw TypeError(".proto.StatusAttribution.music: object expected");
                message.music = $root.proto.StatusAttribution.Music.fromObject(object.music, q + 1);
            }
            if (object.groupStatus != null) {
                if (!$util.isObject(object.groupStatus))
                    throw TypeError(".proto.StatusAttribution.groupStatus: object expected");
                message.groupStatus = $root.proto.StatusAttribution.GroupStatus.fromObject(object.groupStatus, q + 1);
            }
            if (object.rlAttribution != null) {
                if (!$util.isObject(object.rlAttribution))
                    throw TypeError(".proto.StatusAttribution.rlAttribution: object expected");
                message.rlAttribution = $root.proto.StatusAttribution.RLAttribution.fromObject(object.rlAttribution, q + 1);
            }
            if (object.aiCreatedAttribution != null) {
                if (!$util.isObject(object.aiCreatedAttribution))
                    throw TypeError(".proto.StatusAttribution.aiCreatedAttribution: object expected");
                message.aiCreatedAttribution = $root.proto.StatusAttribution.AiCreatedAttribution.fromObject(object.aiCreatedAttribution, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a StatusAttribution message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.StatusAttribution
         * @static
         * @param {proto.StatusAttribution} message StatusAttribution
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StatusAttribution.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                object.type = options.enums === String ? $root.proto.StatusAttribution.Type[message.type] === undefined ? message.type : $root.proto.StatusAttribution.Type[message.type] : message.type;
            if (message.actionUrl != null && Object.hasOwnProperty.call(message, "actionUrl"))
                object.actionUrl = message.actionUrl;
            if (message.statusReshare != null && Object.hasOwnProperty.call(message, "statusReshare")) {
                object.statusReshare = $root.proto.StatusAttribution.StatusReshare.toObject(message.statusReshare, options, q + 1);
                if (options.oneofs)
                    object.attributionData = "statusReshare";
            }
            if (message.externalShare != null && Object.hasOwnProperty.call(message, "externalShare")) {
                object.externalShare = $root.proto.StatusAttribution.ExternalShare.toObject(message.externalShare, options, q + 1);
                if (options.oneofs)
                    object.attributionData = "externalShare";
            }
            if (message.music != null && Object.hasOwnProperty.call(message, "music")) {
                object.music = $root.proto.StatusAttribution.Music.toObject(message.music, options, q + 1);
                if (options.oneofs)
                    object.attributionData = "music";
            }
            if (message.groupStatus != null && Object.hasOwnProperty.call(message, "groupStatus")) {
                object.groupStatus = $root.proto.StatusAttribution.GroupStatus.toObject(message.groupStatus, options, q + 1);
                if (options.oneofs)
                    object.attributionData = "groupStatus";
            }
            if (message.rlAttribution != null && Object.hasOwnProperty.call(message, "rlAttribution")) {
                object.rlAttribution = $root.proto.StatusAttribution.RLAttribution.toObject(message.rlAttribution, options, q + 1);
                if (options.oneofs)
                    object.attributionData = "rlAttribution";
            }
            if (message.aiCreatedAttribution != null && Object.hasOwnProperty.call(message, "aiCreatedAttribution")) {
                object.aiCreatedAttribution = $root.proto.StatusAttribution.AiCreatedAttribution.toObject(message.aiCreatedAttribution, options, q + 1);
                if (options.oneofs)
                    object.attributionData = "aiCreatedAttribution";
            }
            return object;
        };

        /**
         * Converts this StatusAttribution to JSON.
         * @function toJSON
         * @memberof proto.StatusAttribution
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StatusAttribution.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StatusAttribution
         * @function getTypeUrl
         * @memberof proto.StatusAttribution
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StatusAttribution.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.StatusAttribution";
        };

        StatusAttribution.AiCreatedAttribution = (function() {

            /**
             * Properties of an AiCreatedAttribution.
             * @memberof proto.StatusAttribution
             * @interface IAiCreatedAttribution
             * @property {proto.StatusAttribution.AiCreatedAttribution.Source|null} [source] AiCreatedAttribution source
             */

            /**
             * Constructs a new AiCreatedAttribution.
             * @memberof proto.StatusAttribution
             * @classdesc Represents an AiCreatedAttribution.
             * @implements IAiCreatedAttribution
             * @constructor
             * @param {proto.StatusAttribution.IAiCreatedAttribution=} [properties] Properties to set
             */
            function AiCreatedAttribution(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AiCreatedAttribution source.
             * @member {proto.StatusAttribution.AiCreatedAttribution.Source|null|undefined} source
             * @memberof proto.StatusAttribution.AiCreatedAttribution
             * @instance
             */
            AiCreatedAttribution.prototype.source = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AiCreatedAttribution.prototype, "_source", {
                get: $util.oneOfGetter($oneOfFields = ["source"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AiCreatedAttribution instance using the specified properties.
             * @function create
             * @memberof proto.StatusAttribution.AiCreatedAttribution
             * @static
             * @param {proto.StatusAttribution.IAiCreatedAttribution=} [properties] Properties to set
             * @returns {proto.StatusAttribution.AiCreatedAttribution} AiCreatedAttribution instance
             */
            AiCreatedAttribution.create = function create(properties) {
                return new AiCreatedAttribution(properties);
            };

            /**
             * Encodes the specified AiCreatedAttribution message. Does not implicitly {@link proto.StatusAttribution.AiCreatedAttribution.verify|verify} messages.
             * @function encode
             * @memberof proto.StatusAttribution.AiCreatedAttribution
             * @static
             * @param {proto.StatusAttribution.IAiCreatedAttribution} message AiCreatedAttribution message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AiCreatedAttribution.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.source != null && Object.hasOwnProperty.call(message, "source"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.source);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AiCreatedAttribution message, length delimited. Does not implicitly {@link proto.StatusAttribution.AiCreatedAttribution.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.StatusAttribution.AiCreatedAttribution
             * @static
             * @param {proto.StatusAttribution.IAiCreatedAttribution} message AiCreatedAttribution message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AiCreatedAttribution.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an AiCreatedAttribution message from the specified reader or buffer.
             * @function decode
             * @memberof proto.StatusAttribution.AiCreatedAttribution
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.StatusAttribution.AiCreatedAttribution} AiCreatedAttribution
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AiCreatedAttribution.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.source = reader.int32();
                            message._source = "source";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an AiCreatedAttribution message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.StatusAttribution.AiCreatedAttribution
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.StatusAttribution.AiCreatedAttribution} AiCreatedAttribution
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AiCreatedAttribution.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AiCreatedAttribution message.
             * @function verify
             * @memberof proto.StatusAttribution.AiCreatedAttribution
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AiCreatedAttribution.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.source != null && Object.hasOwnProperty.call(message, "source")) {
                    properties._source = 1;
                    if (typeof message.source !== "number" || (message.source | 0) !== message.source)
                        return "source: enum value expected";
                }
                return null;
            };

            /**
             * Creates an AiCreatedAttribution message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.StatusAttribution.AiCreatedAttribution
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.StatusAttribution.AiCreatedAttribution} AiCreatedAttribution
             */
            AiCreatedAttribution.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.StatusAttribution.AiCreatedAttribution: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.source) {
                case "UNKNOWN":
                case 0:
                    message.source = 0;
                    break;
                case "STATUS_MIMICRY":
                case 1:
                    message.source = 1;
                    break;
                default:
                    if (typeof object.source === "number" && (object.source | 0) === object.source)
                        message.source = object.source;
                }
                return message;
            };

            /**
             * Creates a plain object from an AiCreatedAttribution message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.StatusAttribution.AiCreatedAttribution
             * @static
             * @param {proto.StatusAttribution.AiCreatedAttribution} message AiCreatedAttribution
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AiCreatedAttribution.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.source != null && Object.hasOwnProperty.call(message, "source"))
                    object.source = options.enums === String ? $root.proto.StatusAttribution.AiCreatedAttribution.Source[message.source] === undefined ? message.source : $root.proto.StatusAttribution.AiCreatedAttribution.Source[message.source] : message.source;
                return object;
            };

            /**
             * Converts this AiCreatedAttribution to JSON.
             * @function toJSON
             * @memberof proto.StatusAttribution.AiCreatedAttribution
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AiCreatedAttribution.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AiCreatedAttribution
             * @function getTypeUrl
             * @memberof proto.StatusAttribution.AiCreatedAttribution
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AiCreatedAttribution.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.StatusAttribution.AiCreatedAttribution";
            };

            /**
             * Source enum.
             * @name proto.StatusAttribution.AiCreatedAttribution.Source
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} STATUS_MIMICRY=1 STATUS_MIMICRY value
             */
            AiCreatedAttribution.Source = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "STATUS_MIMICRY"] = 1;
                return values;
            })();

            return AiCreatedAttribution;
        })();

        StatusAttribution.ExternalShare = (function() {

            /**
             * Properties of an ExternalShare.
             * @memberof proto.StatusAttribution
             * @interface IExternalShare
             * @property {string|null} [actionUrl] ExternalShare actionUrl
             * @property {proto.StatusAttribution.ExternalShare.Source|null} [source] ExternalShare source
             * @property {number|null} [duration] ExternalShare duration
             * @property {string|null} [actionFallbackUrl] ExternalShare actionFallbackUrl
             */

            /**
             * Constructs a new ExternalShare.
             * @memberof proto.StatusAttribution
             * @classdesc Represents an ExternalShare.
             * @implements IExternalShare
             * @constructor
             * @param {proto.StatusAttribution.IExternalShare=} [properties] Properties to set
             */
            function ExternalShare(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ExternalShare actionUrl.
             * @member {string|null|undefined} actionUrl
             * @memberof proto.StatusAttribution.ExternalShare
             * @instance
             */
            ExternalShare.prototype.actionUrl = null;

            /**
             * ExternalShare source.
             * @member {proto.StatusAttribution.ExternalShare.Source|null|undefined} source
             * @memberof proto.StatusAttribution.ExternalShare
             * @instance
             */
            ExternalShare.prototype.source = null;

            /**
             * ExternalShare duration.
             * @member {number|null|undefined} duration
             * @memberof proto.StatusAttribution.ExternalShare
             * @instance
             */
            ExternalShare.prototype.duration = null;

            /**
             * ExternalShare actionFallbackUrl.
             * @member {string|null|undefined} actionFallbackUrl
             * @memberof proto.StatusAttribution.ExternalShare
             * @instance
             */
            ExternalShare.prototype.actionFallbackUrl = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalShare.prototype, "_actionUrl", {
                get: $util.oneOfGetter($oneOfFields = ["actionUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalShare.prototype, "_source", {
                get: $util.oneOfGetter($oneOfFields = ["source"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalShare.prototype, "_duration", {
                get: $util.oneOfGetter($oneOfFields = ["duration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalShare.prototype, "_actionFallbackUrl", {
                get: $util.oneOfGetter($oneOfFields = ["actionFallbackUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ExternalShare instance using the specified properties.
             * @function create
             * @memberof proto.StatusAttribution.ExternalShare
             * @static
             * @param {proto.StatusAttribution.IExternalShare=} [properties] Properties to set
             * @returns {proto.StatusAttribution.ExternalShare} ExternalShare instance
             */
            ExternalShare.create = function create(properties) {
                return new ExternalShare(properties);
            };

            /**
             * Encodes the specified ExternalShare message. Does not implicitly {@link proto.StatusAttribution.ExternalShare.verify|verify} messages.
             * @function encode
             * @memberof proto.StatusAttribution.ExternalShare
             * @static
             * @param {proto.StatusAttribution.IExternalShare} message ExternalShare message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ExternalShare.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.actionUrl != null && Object.hasOwnProperty.call(message, "actionUrl"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.actionUrl);
                if (message.source != null && Object.hasOwnProperty.call(message, "source"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.source);
                if (message.duration != null && Object.hasOwnProperty.call(message, "duration"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.duration);
                if (message.actionFallbackUrl != null && Object.hasOwnProperty.call(message, "actionFallbackUrl"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.actionFallbackUrl);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ExternalShare message, length delimited. Does not implicitly {@link proto.StatusAttribution.ExternalShare.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.StatusAttribution.ExternalShare
             * @static
             * @param {proto.StatusAttribution.IExternalShare} message ExternalShare message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ExternalShare.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an ExternalShare message from the specified reader or buffer.
             * @function decode
             * @memberof proto.StatusAttribution.ExternalShare
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.StatusAttribution.ExternalShare} ExternalShare
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ExternalShare.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.actionUrl = reader.stringVerify();
                            message._actionUrl = "actionUrl";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.source = reader.int32();
                            message._source = "source";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.duration = reader.int32();
                            message._duration = "duration";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.actionFallbackUrl = reader.stringVerify();
                            message._actionFallbackUrl = "actionFallbackUrl";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an ExternalShare message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.StatusAttribution.ExternalShare
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.StatusAttribution.ExternalShare} ExternalShare
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ExternalShare.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an ExternalShare message.
             * @function verify
             * @memberof proto.StatusAttribution.ExternalShare
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ExternalShare.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.actionUrl != null && Object.hasOwnProperty.call(message, "actionUrl")) {
                    properties._actionUrl = 1;
                    if (!$util.isString(message.actionUrl))
                        return "actionUrl: string expected";
                }
                if (message.source != null && Object.hasOwnProperty.call(message, "source")) {
                    properties._source = 1;
                    if (typeof message.source !== "number" || (message.source | 0) !== message.source)
                        return "source: enum value expected";
                }
                if (message.duration != null && Object.hasOwnProperty.call(message, "duration")) {
                    properties._duration = 1;
                    if (!$util.isInteger(message.duration))
                        return "duration: integer expected";
                }
                if (message.actionFallbackUrl != null && Object.hasOwnProperty.call(message, "actionFallbackUrl")) {
                    properties._actionFallbackUrl = 1;
                    if (!$util.isString(message.actionFallbackUrl))
                        return "actionFallbackUrl: string expected";
                }
                return null;
            };

            /**
             * Creates an ExternalShare message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.StatusAttribution.ExternalShare
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.StatusAttribution.ExternalShare} ExternalShare
             */
            ExternalShare.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.StatusAttribution.ExternalShare: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.actionUrl != null)
                    message.actionUrl = String(object.actionUrl);
                switch (object.source) {
                case "UNKNOWN":
                case 0:
                    message.source = 0;
                    break;
                case "INSTAGRAM":
                case 1:
                    message.source = 1;
                    break;
                case "FACEBOOK":
                case 2:
                    message.source = 2;
                    break;
                case "MESSENGER":
                case 3:
                    message.source = 3;
                    break;
                case "SPOTIFY":
                case 4:
                    message.source = 4;
                    break;
                case "YOUTUBE":
                case 5:
                    message.source = 5;
                    break;
                case "PINTEREST":
                case 6:
                    message.source = 6;
                    break;
                case "THREADS":
                case 7:
                    message.source = 7;
                    break;
                case "APPLE_MUSIC":
                case 8:
                    message.source = 8;
                    break;
                case "SHARECHAT":
                case 9:
                    message.source = 9;
                    break;
                case "GOOGLE_PHOTOS":
                case 10:
                    message.source = 10;
                    break;
                case "SOUNDCLOUD":
                case 11:
                    message.source = 11;
                    break;
                case "SHAZAM":
                case 12:
                    message.source = 12;
                    break;
                default:
                    if (typeof object.source === "number" && (object.source | 0) === object.source)
                        message.source = object.source;
                }
                if (object.duration != null)
                    message.duration = object.duration | 0;
                if (object.actionFallbackUrl != null)
                    message.actionFallbackUrl = String(object.actionFallbackUrl);
                return message;
            };

            /**
             * Creates a plain object from an ExternalShare message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.StatusAttribution.ExternalShare
             * @static
             * @param {proto.StatusAttribution.ExternalShare} message ExternalShare
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ExternalShare.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.actionUrl != null && Object.hasOwnProperty.call(message, "actionUrl"))
                    object.actionUrl = message.actionUrl;
                if (message.source != null && Object.hasOwnProperty.call(message, "source"))
                    object.source = options.enums === String ? $root.proto.StatusAttribution.ExternalShare.Source[message.source] === undefined ? message.source : $root.proto.StatusAttribution.ExternalShare.Source[message.source] : message.source;
                if (message.duration != null && Object.hasOwnProperty.call(message, "duration"))
                    object.duration = message.duration;
                if (message.actionFallbackUrl != null && Object.hasOwnProperty.call(message, "actionFallbackUrl"))
                    object.actionFallbackUrl = message.actionFallbackUrl;
                return object;
            };

            /**
             * Converts this ExternalShare to JSON.
             * @function toJSON
             * @memberof proto.StatusAttribution.ExternalShare
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ExternalShare.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ExternalShare
             * @function getTypeUrl
             * @memberof proto.StatusAttribution.ExternalShare
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ExternalShare.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.StatusAttribution.ExternalShare";
            };

            /**
             * Source enum.
             * @name proto.StatusAttribution.ExternalShare.Source
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} INSTAGRAM=1 INSTAGRAM value
             * @property {number} FACEBOOK=2 FACEBOOK value
             * @property {number} MESSENGER=3 MESSENGER value
             * @property {number} SPOTIFY=4 SPOTIFY value
             * @property {number} YOUTUBE=5 YOUTUBE value
             * @property {number} PINTEREST=6 PINTEREST value
             * @property {number} THREADS=7 THREADS value
             * @property {number} APPLE_MUSIC=8 APPLE_MUSIC value
             * @property {number} SHARECHAT=9 SHARECHAT value
             * @property {number} GOOGLE_PHOTOS=10 GOOGLE_PHOTOS value
             * @property {number} SOUNDCLOUD=11 SOUNDCLOUD value
             * @property {number} SHAZAM=12 SHAZAM value
             */
            ExternalShare.Source = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "INSTAGRAM"] = 1;
                values[valuesById[2] = "FACEBOOK"] = 2;
                values[valuesById[3] = "MESSENGER"] = 3;
                values[valuesById[4] = "SPOTIFY"] = 4;
                values[valuesById[5] = "YOUTUBE"] = 5;
                values[valuesById[6] = "PINTEREST"] = 6;
                values[valuesById[7] = "THREADS"] = 7;
                values[valuesById[8] = "APPLE_MUSIC"] = 8;
                values[valuesById[9] = "SHARECHAT"] = 9;
                values[valuesById[10] = "GOOGLE_PHOTOS"] = 10;
                values[valuesById[11] = "SOUNDCLOUD"] = 11;
                values[valuesById[12] = "SHAZAM"] = 12;
                return values;
            })();

            return ExternalShare;
        })();

        StatusAttribution.GroupStatus = (function() {

            /**
             * Properties of a GroupStatus.
             * @memberof proto.StatusAttribution
             * @interface IGroupStatus
             * @property {string|null} [authorJid] GroupStatus authorJid
             */

            /**
             * Constructs a new GroupStatus.
             * @memberof proto.StatusAttribution
             * @classdesc Represents a GroupStatus.
             * @implements IGroupStatus
             * @constructor
             * @param {proto.StatusAttribution.IGroupStatus=} [properties] Properties to set
             */
            function GroupStatus(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * GroupStatus authorJid.
             * @member {string|null|undefined} authorJid
             * @memberof proto.StatusAttribution.GroupStatus
             * @instance
             */
            GroupStatus.prototype.authorJid = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(GroupStatus.prototype, "_authorJid", {
                get: $util.oneOfGetter($oneOfFields = ["authorJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new GroupStatus instance using the specified properties.
             * @function create
             * @memberof proto.StatusAttribution.GroupStatus
             * @static
             * @param {proto.StatusAttribution.IGroupStatus=} [properties] Properties to set
             * @returns {proto.StatusAttribution.GroupStatus} GroupStatus instance
             */
            GroupStatus.create = function create(properties) {
                return new GroupStatus(properties);
            };

            /**
             * Encodes the specified GroupStatus message. Does not implicitly {@link proto.StatusAttribution.GroupStatus.verify|verify} messages.
             * @function encode
             * @memberof proto.StatusAttribution.GroupStatus
             * @static
             * @param {proto.StatusAttribution.IGroupStatus} message GroupStatus message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GroupStatus.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.authorJid != null && Object.hasOwnProperty.call(message, "authorJid"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.authorJid);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified GroupStatus message, length delimited. Does not implicitly {@link proto.StatusAttribution.GroupStatus.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.StatusAttribution.GroupStatus
             * @static
             * @param {proto.StatusAttribution.IGroupStatus} message GroupStatus message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            GroupStatus.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a GroupStatus message from the specified reader or buffer.
             * @function decode
             * @memberof proto.StatusAttribution.GroupStatus
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.StatusAttribution.GroupStatus} GroupStatus
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GroupStatus.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.authorJid = reader.stringVerify();
                            message._authorJid = "authorJid";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a GroupStatus message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.StatusAttribution.GroupStatus
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.StatusAttribution.GroupStatus} GroupStatus
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            GroupStatus.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a GroupStatus message.
             * @function verify
             * @memberof proto.StatusAttribution.GroupStatus
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            GroupStatus.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.authorJid != null && Object.hasOwnProperty.call(message, "authorJid")) {
                    properties._authorJid = 1;
                    if (!$util.isString(message.authorJid))
                        return "authorJid: string expected";
                }
                return null;
            };

            /**
             * Creates a GroupStatus message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.StatusAttribution.GroupStatus
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.StatusAttribution.GroupStatus} GroupStatus
             */
            GroupStatus.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.StatusAttribution.GroupStatus: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.authorJid != null)
                    message.authorJid = String(object.authorJid);
                return message;
            };

            /**
             * Creates a plain object from a GroupStatus message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.StatusAttribution.GroupStatus
             * @static
             * @param {proto.StatusAttribution.GroupStatus} message GroupStatus
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            GroupStatus.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.authorJid != null && Object.hasOwnProperty.call(message, "authorJid"))
                    object.authorJid = message.authorJid;
                return object;
            };

            /**
             * Converts this GroupStatus to JSON.
             * @function toJSON
             * @memberof proto.StatusAttribution.GroupStatus
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            GroupStatus.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for GroupStatus
             * @function getTypeUrl
             * @memberof proto.StatusAttribution.GroupStatus
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            GroupStatus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.StatusAttribution.GroupStatus";
            };

            return GroupStatus;
        })();

        StatusAttribution.Music = (function() {

            /**
             * Properties of a Music.
             * @memberof proto.StatusAttribution
             * @interface IMusic
             * @property {string|null} [authorName] Music authorName
             * @property {string|null} [songId] Music songId
             * @property {string|null} [title] Music title
             * @property {string|null} [author] Music author
             * @property {string|null} [artistAttribution] Music artistAttribution
             * @property {boolean|null} [isExplicit] Music isExplicit
             */

            /**
             * Constructs a new Music.
             * @memberof proto.StatusAttribution
             * @classdesc Represents a Music.
             * @implements IMusic
             * @constructor
             * @param {proto.StatusAttribution.IMusic=} [properties] Properties to set
             */
            function Music(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Music authorName.
             * @member {string|null|undefined} authorName
             * @memberof proto.StatusAttribution.Music
             * @instance
             */
            Music.prototype.authorName = null;

            /**
             * Music songId.
             * @member {string|null|undefined} songId
             * @memberof proto.StatusAttribution.Music
             * @instance
             */
            Music.prototype.songId = null;

            /**
             * Music title.
             * @member {string|null|undefined} title
             * @memberof proto.StatusAttribution.Music
             * @instance
             */
            Music.prototype.title = null;

            /**
             * Music author.
             * @member {string|null|undefined} author
             * @memberof proto.StatusAttribution.Music
             * @instance
             */
            Music.prototype.author = null;

            /**
             * Music artistAttribution.
             * @member {string|null|undefined} artistAttribution
             * @memberof proto.StatusAttribution.Music
             * @instance
             */
            Music.prototype.artistAttribution = null;

            /**
             * Music isExplicit.
             * @member {boolean|null|undefined} isExplicit
             * @memberof proto.StatusAttribution.Music
             * @instance
             */
            Music.prototype.isExplicit = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Music.prototype, "_authorName", {
                get: $util.oneOfGetter($oneOfFields = ["authorName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Music.prototype, "_songId", {
                get: $util.oneOfGetter($oneOfFields = ["songId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Music.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Music.prototype, "_author", {
                get: $util.oneOfGetter($oneOfFields = ["author"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Music.prototype, "_artistAttribution", {
                get: $util.oneOfGetter($oneOfFields = ["artistAttribution"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Music.prototype, "_isExplicit", {
                get: $util.oneOfGetter($oneOfFields = ["isExplicit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new Music instance using the specified properties.
             * @function create
             * @memberof proto.StatusAttribution.Music
             * @static
             * @param {proto.StatusAttribution.IMusic=} [properties] Properties to set
             * @returns {proto.StatusAttribution.Music} Music instance
             */
            Music.create = function create(properties) {
                return new Music(properties);
            };

            /**
             * Encodes the specified Music message. Does not implicitly {@link proto.StatusAttribution.Music.verify|verify} messages.
             * @function encode
             * @memberof proto.StatusAttribution.Music
             * @static
             * @param {proto.StatusAttribution.IMusic} message Music message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Music.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.authorName != null && Object.hasOwnProperty.call(message, "authorName"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.authorName);
                if (message.songId != null && Object.hasOwnProperty.call(message, "songId"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.songId);
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.title);
                if (message.author != null && Object.hasOwnProperty.call(message, "author"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.author);
                if (message.artistAttribution != null && Object.hasOwnProperty.call(message, "artistAttribution"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.artistAttribution);
                if (message.isExplicit != null && Object.hasOwnProperty.call(message, "isExplicit"))
                    writer.uint32(/* id 6, wireType 0 =*/48).bool(message.isExplicit);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Music message, length delimited. Does not implicitly {@link proto.StatusAttribution.Music.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.StatusAttribution.Music
             * @static
             * @param {proto.StatusAttribution.IMusic} message Music message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Music.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a Music message from the specified reader or buffer.
             * @function decode
             * @memberof proto.StatusAttribution.Music
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.StatusAttribution.Music} Music
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Music.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.authorName = reader.stringVerify();
                            message._authorName = "authorName";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.songId = reader.stringVerify();
                            message._songId = "songId";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.title = reader.stringVerify();
                            message._title = "title";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.author = reader.stringVerify();
                            message._author = "author";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.artistAttribution = reader.stringVerify();
                            message._artistAttribution = "artistAttribution";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.isExplicit = reader.bool();
                            message._isExplicit = "isExplicit";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a Music message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.StatusAttribution.Music
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.StatusAttribution.Music} Music
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Music.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Music message.
             * @function verify
             * @memberof proto.StatusAttribution.Music
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Music.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.authorName != null && Object.hasOwnProperty.call(message, "authorName")) {
                    properties._authorName = 1;
                    if (!$util.isString(message.authorName))
                        return "authorName: string expected";
                }
                if (message.songId != null && Object.hasOwnProperty.call(message, "songId")) {
                    properties._songId = 1;
                    if (!$util.isString(message.songId))
                        return "songId: string expected";
                }
                if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                    properties._title = 1;
                    if (!$util.isString(message.title))
                        return "title: string expected";
                }
                if (message.author != null && Object.hasOwnProperty.call(message, "author")) {
                    properties._author = 1;
                    if (!$util.isString(message.author))
                        return "author: string expected";
                }
                if (message.artistAttribution != null && Object.hasOwnProperty.call(message, "artistAttribution")) {
                    properties._artistAttribution = 1;
                    if (!$util.isString(message.artistAttribution))
                        return "artistAttribution: string expected";
                }
                if (message.isExplicit != null && Object.hasOwnProperty.call(message, "isExplicit")) {
                    properties._isExplicit = 1;
                    if (typeof message.isExplicit !== "boolean")
                        return "isExplicit: boolean expected";
                }
                return null;
            };

            /**
             * Creates a Music message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.StatusAttribution.Music
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.StatusAttribution.Music} Music
             */
            Music.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.StatusAttribution.Music: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.authorName != null)
                    message.authorName = String(object.authorName);
                if (object.songId != null)
                    message.songId = String(object.songId);
                if (object.title != null)
                    message.title = String(object.title);
                if (object.author != null)
                    message.author = String(object.author);
                if (object.artistAttribution != null)
                    message.artistAttribution = String(object.artistAttribution);
                if (object.isExplicit != null)
                    message.isExplicit = Boolean(object.isExplicit);
                return message;
            };

            /**
             * Creates a plain object from a Music message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.StatusAttribution.Music
             * @static
             * @param {proto.StatusAttribution.Music} message Music
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Music.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.authorName != null && Object.hasOwnProperty.call(message, "authorName"))
                    object.authorName = message.authorName;
                if (message.songId != null && Object.hasOwnProperty.call(message, "songId"))
                    object.songId = message.songId;
                if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                    object.title = message.title;
                if (message.author != null && Object.hasOwnProperty.call(message, "author"))
                    object.author = message.author;
                if (message.artistAttribution != null && Object.hasOwnProperty.call(message, "artistAttribution"))
                    object.artistAttribution = message.artistAttribution;
                if (message.isExplicit != null && Object.hasOwnProperty.call(message, "isExplicit"))
                    object.isExplicit = message.isExplicit;
                return object;
            };

            /**
             * Converts this Music to JSON.
             * @function toJSON
             * @memberof proto.StatusAttribution.Music
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Music.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Music
             * @function getTypeUrl
             * @memberof proto.StatusAttribution.Music
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Music.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.StatusAttribution.Music";
            };

            return Music;
        })();

        StatusAttribution.RLAttribution = (function() {

            /**
             * Properties of a RLAttribution.
             * @memberof proto.StatusAttribution
             * @interface IRLAttribution
             * @property {proto.StatusAttribution.RLAttribution.Source|null} [source] RLAttribution source
             */

            /**
             * Constructs a new RLAttribution.
             * @memberof proto.StatusAttribution
             * @classdesc Represents a RLAttribution.
             * @implements IRLAttribution
             * @constructor
             * @param {proto.StatusAttribution.IRLAttribution=} [properties] Properties to set
             */
            function RLAttribution(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * RLAttribution source.
             * @member {proto.StatusAttribution.RLAttribution.Source|null|undefined} source
             * @memberof proto.StatusAttribution.RLAttribution
             * @instance
             */
            RLAttribution.prototype.source = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(RLAttribution.prototype, "_source", {
                get: $util.oneOfGetter($oneOfFields = ["source"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new RLAttribution instance using the specified properties.
             * @function create
             * @memberof proto.StatusAttribution.RLAttribution
             * @static
             * @param {proto.StatusAttribution.IRLAttribution=} [properties] Properties to set
             * @returns {proto.StatusAttribution.RLAttribution} RLAttribution instance
             */
            RLAttribution.create = function create(properties) {
                return new RLAttribution(properties);
            };

            /**
             * Encodes the specified RLAttribution message. Does not implicitly {@link proto.StatusAttribution.RLAttribution.verify|verify} messages.
             * @function encode
             * @memberof proto.StatusAttribution.RLAttribution
             * @static
             * @param {proto.StatusAttribution.IRLAttribution} message RLAttribution message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RLAttribution.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.source != null && Object.hasOwnProperty.call(message, "source"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.source);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified RLAttribution message, length delimited. Does not implicitly {@link proto.StatusAttribution.RLAttribution.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.StatusAttribution.RLAttribution
             * @static
             * @param {proto.StatusAttribution.IRLAttribution} message RLAttribution message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RLAttribution.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a RLAttribution message from the specified reader or buffer.
             * @function decode
             * @memberof proto.StatusAttribution.RLAttribution
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.StatusAttribution.RLAttribution} RLAttribution
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RLAttribution.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.source = reader.int32();
                            message._source = "source";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a RLAttribution message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.StatusAttribution.RLAttribution
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.StatusAttribution.RLAttribution} RLAttribution
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RLAttribution.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a RLAttribution message.
             * @function verify
             * @memberof proto.StatusAttribution.RLAttribution
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RLAttribution.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.source != null && Object.hasOwnProperty.call(message, "source")) {
                    properties._source = 1;
                    if (typeof message.source !== "number" || (message.source | 0) !== message.source)
                        return "source: enum value expected";
                }
                return null;
            };

            /**
             * Creates a RLAttribution message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.StatusAttribution.RLAttribution
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.StatusAttribution.RLAttribution} RLAttribution
             */
            RLAttribution.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.StatusAttribution.RLAttribution: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.source) {
                case "UNKNOWN":
                case 0:
                    message.source = 0;
                    break;
                case "RAY_BAN_META_GLASSES":
                case 1:
                    message.source = 1;
                    break;
                case "OAKLEY_META_GLASSES":
                case 2:
                    message.source = 2;
                    break;
                case "HYPERNOVA_GLASSES":
                case 3:
                    message.source = 3;
                    break;
                default:
                    if (typeof object.source === "number" && (object.source | 0) === object.source)
                        message.source = object.source;
                }
                return message;
            };

            /**
             * Creates a plain object from a RLAttribution message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.StatusAttribution.RLAttribution
             * @static
             * @param {proto.StatusAttribution.RLAttribution} message RLAttribution
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RLAttribution.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.source != null && Object.hasOwnProperty.call(message, "source"))
                    object.source = options.enums === String ? $root.proto.StatusAttribution.RLAttribution.Source[message.source] === undefined ? message.source : $root.proto.StatusAttribution.RLAttribution.Source[message.source] : message.source;
                return object;
            };

            /**
             * Converts this RLAttribution to JSON.
             * @function toJSON
             * @memberof proto.StatusAttribution.RLAttribution
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RLAttribution.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for RLAttribution
             * @function getTypeUrl
             * @memberof proto.StatusAttribution.RLAttribution
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            RLAttribution.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.StatusAttribution.RLAttribution";
            };

            /**
             * Source enum.
             * @name proto.StatusAttribution.RLAttribution.Source
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} RAY_BAN_META_GLASSES=1 RAY_BAN_META_GLASSES value
             * @property {number} OAKLEY_META_GLASSES=2 OAKLEY_META_GLASSES value
             * @property {number} HYPERNOVA_GLASSES=3 HYPERNOVA_GLASSES value
             */
            RLAttribution.Source = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "RAY_BAN_META_GLASSES"] = 1;
                values[valuesById[2] = "OAKLEY_META_GLASSES"] = 2;
                values[valuesById[3] = "HYPERNOVA_GLASSES"] = 3;
                return values;
            })();

            return RLAttribution;
        })();

        StatusAttribution.StatusReshare = (function() {

            /**
             * Properties of a StatusReshare.
             * @memberof proto.StatusAttribution
             * @interface IStatusReshare
             * @property {proto.StatusAttribution.StatusReshare.Source|null} [source] StatusReshare source
             * @property {proto.StatusAttribution.StatusReshare.IMetadata|null} [metadata] StatusReshare metadata
             */

            /**
             * Constructs a new StatusReshare.
             * @memberof proto.StatusAttribution
             * @classdesc Represents a StatusReshare.
             * @implements IStatusReshare
             * @constructor
             * @param {proto.StatusAttribution.IStatusReshare=} [properties] Properties to set
             */
            function StatusReshare(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * StatusReshare source.
             * @member {proto.StatusAttribution.StatusReshare.Source|null|undefined} source
             * @memberof proto.StatusAttribution.StatusReshare
             * @instance
             */
            StatusReshare.prototype.source = null;

            /**
             * StatusReshare metadata.
             * @member {proto.StatusAttribution.StatusReshare.IMetadata|null|undefined} metadata
             * @memberof proto.StatusAttribution.StatusReshare
             * @instance
             */
            StatusReshare.prototype.metadata = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusReshare.prototype, "_source", {
                get: $util.oneOfGetter($oneOfFields = ["source"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusReshare.prototype, "_metadata", {
                get: $util.oneOfGetter($oneOfFields = ["metadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StatusReshare instance using the specified properties.
             * @function create
             * @memberof proto.StatusAttribution.StatusReshare
             * @static
             * @param {proto.StatusAttribution.IStatusReshare=} [properties] Properties to set
             * @returns {proto.StatusAttribution.StatusReshare} StatusReshare instance
             */
            StatusReshare.create = function create(properties) {
                return new StatusReshare(properties);
            };

            /**
             * Encodes the specified StatusReshare message. Does not implicitly {@link proto.StatusAttribution.StatusReshare.verify|verify} messages.
             * @function encode
             * @memberof proto.StatusAttribution.StatusReshare
             * @static
             * @param {proto.StatusAttribution.IStatusReshare} message StatusReshare message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StatusReshare.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.source != null && Object.hasOwnProperty.call(message, "source"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.source);
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata"))
                    $root.proto.StatusAttribution.StatusReshare.Metadata.encode(message.metadata, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified StatusReshare message, length delimited. Does not implicitly {@link proto.StatusAttribution.StatusReshare.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.StatusAttribution.StatusReshare
             * @static
             * @param {proto.StatusAttribution.IStatusReshare} message StatusReshare message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StatusReshare.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a StatusReshare message from the specified reader or buffer.
             * @function decode
             * @memberof proto.StatusAttribution.StatusReshare
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.StatusAttribution.StatusReshare} StatusReshare
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StatusReshare.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.source = reader.int32();
                            message._source = "source";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.metadata = $root.proto.StatusAttribution.StatusReshare.Metadata.decode(reader, reader.uint32(), undefined, q + 1, message.metadata);
                            message._metadata = "metadata";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a StatusReshare message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.StatusAttribution.StatusReshare
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.StatusAttribution.StatusReshare} StatusReshare
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StatusReshare.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a StatusReshare message.
             * @function verify
             * @memberof proto.StatusAttribution.StatusReshare
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StatusReshare.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.source != null && Object.hasOwnProperty.call(message, "source")) {
                    properties._source = 1;
                    if (typeof message.source !== "number" || (message.source | 0) !== message.source)
                        return "source: enum value expected";
                }
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata")) {
                    properties._metadata = 1;
                    {
                        var error = $root.proto.StatusAttribution.StatusReshare.Metadata.verify(message.metadata, q + 1);
                        if (error)
                            return "metadata." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a StatusReshare message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.StatusAttribution.StatusReshare
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.StatusAttribution.StatusReshare} StatusReshare
             */
            StatusReshare.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.StatusAttribution.StatusReshare: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.source) {
                case "UNKNOWN":
                case 0:
                    message.source = 0;
                    break;
                case "INTERNAL_RESHARE":
                case 1:
                    message.source = 1;
                    break;
                case "MENTION_RESHARE":
                case 2:
                    message.source = 2;
                    break;
                case "CHANNEL_RESHARE":
                case 3:
                    message.source = 3;
                    break;
                case "FORWARD":
                case 4:
                    message.source = 4;
                    break;
                default:
                    if (typeof object.source === "number" && (object.source | 0) === object.source)
                        message.source = object.source;
                }
                if (object.metadata != null) {
                    if (!$util.isObject(object.metadata))
                        throw TypeError(".proto.StatusAttribution.StatusReshare.metadata: object expected");
                    message.metadata = $root.proto.StatusAttribution.StatusReshare.Metadata.fromObject(object.metadata, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a StatusReshare message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.StatusAttribution.StatusReshare
             * @static
             * @param {proto.StatusAttribution.StatusReshare} message StatusReshare
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StatusReshare.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.source != null && Object.hasOwnProperty.call(message, "source"))
                    object.source = options.enums === String ? $root.proto.StatusAttribution.StatusReshare.Source[message.source] === undefined ? message.source : $root.proto.StatusAttribution.StatusReshare.Source[message.source] : message.source;
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata"))
                    object.metadata = $root.proto.StatusAttribution.StatusReshare.Metadata.toObject(message.metadata, options, q + 1);
                return object;
            };

            /**
             * Converts this StatusReshare to JSON.
             * @function toJSON
             * @memberof proto.StatusAttribution.StatusReshare
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StatusReshare.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for StatusReshare
             * @function getTypeUrl
             * @memberof proto.StatusAttribution.StatusReshare
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            StatusReshare.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.StatusAttribution.StatusReshare";
            };

            StatusReshare.Metadata = (function() {

                /**
                 * Properties of a Metadata.
                 * @memberof proto.StatusAttribution.StatusReshare
                 * @interface IMetadata
                 * @property {number|null} [duration] Metadata duration
                 * @property {string|null} [channelJid] Metadata channelJid
                 * @property {number|null} [channelMessageId] Metadata channelMessageId
                 * @property {boolean|null} [hasMultipleReshares] Metadata hasMultipleReshares
                 */

                /**
                 * Constructs a new Metadata.
                 * @memberof proto.StatusAttribution.StatusReshare
                 * @classdesc Represents a Metadata.
                 * @implements IMetadata
                 * @constructor
                 * @param {proto.StatusAttribution.StatusReshare.IMetadata=} [properties] Properties to set
                 */
                function Metadata(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * Metadata duration.
                 * @member {number|null|undefined} duration
                 * @memberof proto.StatusAttribution.StatusReshare.Metadata
                 * @instance
                 */
                Metadata.prototype.duration = null;

                /**
                 * Metadata channelJid.
                 * @member {string|null|undefined} channelJid
                 * @memberof proto.StatusAttribution.StatusReshare.Metadata
                 * @instance
                 */
                Metadata.prototype.channelJid = null;

                /**
                 * Metadata channelMessageId.
                 * @member {number|null|undefined} channelMessageId
                 * @memberof proto.StatusAttribution.StatusReshare.Metadata
                 * @instance
                 */
                Metadata.prototype.channelMessageId = null;

                /**
                 * Metadata hasMultipleReshares.
                 * @member {boolean|null|undefined} hasMultipleReshares
                 * @memberof proto.StatusAttribution.StatusReshare.Metadata
                 * @instance
                 */
                Metadata.prototype.hasMultipleReshares = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Metadata.prototype, "_duration", {
                    get: $util.oneOfGetter($oneOfFields = ["duration"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Metadata.prototype, "_channelJid", {
                    get: $util.oneOfGetter($oneOfFields = ["channelJid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Metadata.prototype, "_channelMessageId", {
                    get: $util.oneOfGetter($oneOfFields = ["channelMessageId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Metadata.prototype, "_hasMultipleReshares", {
                    get: $util.oneOfGetter($oneOfFields = ["hasMultipleReshares"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new Metadata instance using the specified properties.
                 * @function create
                 * @memberof proto.StatusAttribution.StatusReshare.Metadata
                 * @static
                 * @param {proto.StatusAttribution.StatusReshare.IMetadata=} [properties] Properties to set
                 * @returns {proto.StatusAttribution.StatusReshare.Metadata} Metadata instance
                 */
                Metadata.create = function create(properties) {
                    return new Metadata(properties);
                };

                /**
                 * Encodes the specified Metadata message. Does not implicitly {@link proto.StatusAttribution.StatusReshare.Metadata.verify|verify} messages.
                 * @function encode
                 * @memberof proto.StatusAttribution.StatusReshare.Metadata
                 * @static
                 * @param {proto.StatusAttribution.StatusReshare.IMetadata} message Metadata message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Metadata.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.duration != null && Object.hasOwnProperty.call(message, "duration"))
                        writer.uint32(/* id 1, wireType 0 =*/8).int32(message.duration);
                    if (message.channelJid != null && Object.hasOwnProperty.call(message, "channelJid"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.channelJid);
                    if (message.channelMessageId != null && Object.hasOwnProperty.call(message, "channelMessageId"))
                        writer.uint32(/* id 3, wireType 0 =*/24).int32(message.channelMessageId);
                    if (message.hasMultipleReshares != null && Object.hasOwnProperty.call(message, "hasMultipleReshares"))
                        writer.uint32(/* id 4, wireType 0 =*/32).bool(message.hasMultipleReshares);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified Metadata message, length delimited. Does not implicitly {@link proto.StatusAttribution.StatusReshare.Metadata.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.StatusAttribution.StatusReshare.Metadata
                 * @static
                 * @param {proto.StatusAttribution.StatusReshare.IMetadata} message Metadata message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Metadata.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a Metadata message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.StatusAttribution.StatusReshare.Metadata
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.StatusAttribution.StatusReshare.Metadata} Metadata
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Metadata.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                message.duration = reader.int32();
                                message._duration = "duration";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.channelJid = reader.stringVerify();
                                message._channelJid = "channelJid";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                message.channelMessageId = reader.int32();
                                message._channelMessageId = "channelMessageId";
                                continue;
                            }
                        case 4: {
                                if (u !== 0)
                                    break;
                                message.hasMultipleReshares = reader.bool();
                                message._hasMultipleReshares = "hasMultipleReshares";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a Metadata message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.StatusAttribution.StatusReshare.Metadata
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.StatusAttribution.StatusReshare.Metadata} Metadata
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Metadata.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a Metadata message.
                 * @function verify
                 * @memberof proto.StatusAttribution.StatusReshare.Metadata
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                Metadata.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.duration != null && Object.hasOwnProperty.call(message, "duration")) {
                        properties._duration = 1;
                        if (!$util.isInteger(message.duration))
                            return "duration: integer expected";
                    }
                    if (message.channelJid != null && Object.hasOwnProperty.call(message, "channelJid")) {
                        properties._channelJid = 1;
                        if (!$util.isString(message.channelJid))
                            return "channelJid: string expected";
                    }
                    if (message.channelMessageId != null && Object.hasOwnProperty.call(message, "channelMessageId")) {
                        properties._channelMessageId = 1;
                        if (!$util.isInteger(message.channelMessageId))
                            return "channelMessageId: integer expected";
                    }
                    if (message.hasMultipleReshares != null && Object.hasOwnProperty.call(message, "hasMultipleReshares")) {
                        properties._hasMultipleReshares = 1;
                        if (typeof message.hasMultipleReshares !== "boolean")
                            return "hasMultipleReshares: boolean expected";
                    }
                    return null;
                };

                /**
                 * Creates a Metadata message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.StatusAttribution.StatusReshare.Metadata
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.StatusAttribution.StatusReshare.Metadata} Metadata
                 */
                Metadata.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.StatusAttribution.StatusReshare.Metadata: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.duration != null)
                        message.duration = object.duration | 0;
                    if (object.channelJid != null)
                        message.channelJid = String(object.channelJid);
                    if (object.channelMessageId != null)
                        message.channelMessageId = object.channelMessageId | 0;
                    if (object.hasMultipleReshares != null)
                        message.hasMultipleReshares = Boolean(object.hasMultipleReshares);
                    return message;
                };

                /**
                 * Creates a plain object from a Metadata message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.StatusAttribution.StatusReshare.Metadata
                 * @static
                 * @param {proto.StatusAttribution.StatusReshare.Metadata} message Metadata
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                Metadata.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.duration != null && Object.hasOwnProperty.call(message, "duration"))
                        object.duration = message.duration;
                    if (message.channelJid != null && Object.hasOwnProperty.call(message, "channelJid"))
                        object.channelJid = message.channelJid;
                    if (message.channelMessageId != null && Object.hasOwnProperty.call(message, "channelMessageId"))
                        object.channelMessageId = message.channelMessageId;
                    if (message.hasMultipleReshares != null && Object.hasOwnProperty.call(message, "hasMultipleReshares"))
                        object.hasMultipleReshares = message.hasMultipleReshares;
                    return object;
                };

                /**
                 * Converts this Metadata to JSON.
                 * @function toJSON
                 * @memberof proto.StatusAttribution.StatusReshare.Metadata
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                Metadata.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for Metadata
                 * @function getTypeUrl
                 * @memberof proto.StatusAttribution.StatusReshare.Metadata
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                Metadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.StatusAttribution.StatusReshare.Metadata";
                };

                return Metadata;
            })();

            /**
             * Source enum.
             * @name proto.StatusAttribution.StatusReshare.Source
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} INTERNAL_RESHARE=1 INTERNAL_RESHARE value
             * @property {number} MENTION_RESHARE=2 MENTION_RESHARE value
             * @property {number} CHANNEL_RESHARE=3 CHANNEL_RESHARE value
             * @property {number} FORWARD=4 FORWARD value
             */
            StatusReshare.Source = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "INTERNAL_RESHARE"] = 1;
                values[valuesById[2] = "MENTION_RESHARE"] = 2;
                values[valuesById[3] = "CHANNEL_RESHARE"] = 3;
                values[valuesById[4] = "FORWARD"] = 4;
                return values;
            })();

            return StatusReshare;
        })();

        /**
         * Type enum.
         * @name proto.StatusAttribution.Type
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} RESHARE=1 RESHARE value
         * @property {number} EXTERNAL_SHARE=2 EXTERNAL_SHARE value
         * @property {number} MUSIC=3 MUSIC value
         * @property {number} STATUS_MENTION=4 STATUS_MENTION value
         * @property {number} GROUP_STATUS=5 GROUP_STATUS value
         * @property {number} RL_ATTRIBUTION=6 RL_ATTRIBUTION value
         * @property {number} AI_CREATED=7 AI_CREATED value
         * @property {number} LAYOUTS=8 LAYOUTS value
         * @property {number} NEWSLETTER_STATUS=9 NEWSLETTER_STATUS value
         * @property {number} STATUS_CLOSE_SHARING=10 STATUS_CLOSE_SHARING value
         * @property {number} PAID_PARTNERSHIP=11 PAID_PARTNERSHIP value
         */
        StatusAttribution.Type = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "RESHARE"] = 1;
            values[valuesById[2] = "EXTERNAL_SHARE"] = 2;
            values[valuesById[3] = "MUSIC"] = 3;
            values[valuesById[4] = "STATUS_MENTION"] = 4;
            values[valuesById[5] = "GROUP_STATUS"] = 5;
            values[valuesById[6] = "RL_ATTRIBUTION"] = 6;
            values[valuesById[7] = "AI_CREATED"] = 7;
            values[valuesById[8] = "LAYOUTS"] = 8;
            values[valuesById[9] = "NEWSLETTER_STATUS"] = 9;
            values[valuesById[10] = "STATUS_CLOSE_SHARING"] = 10;
            values[valuesById[11] = "PAID_PARTNERSHIP"] = 11;
            return values;
        })();

        return StatusAttribution;
    })();

    proto.StatusMentionMessage = (function() {

        /**
         * Properties of a StatusMentionMessage.
         * @memberof proto
         * @interface IStatusMentionMessage
         * @property {proto.IMessage|null} [quotedStatus] StatusMentionMessage quotedStatus
         */

        /**
         * Constructs a new StatusMentionMessage.
         * @memberof proto
         * @classdesc Represents a StatusMentionMessage.
         * @implements IStatusMentionMessage
         * @constructor
         * @param {proto.IStatusMentionMessage=} [properties] Properties to set
         */
        function StatusMentionMessage(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StatusMentionMessage quotedStatus.
         * @member {proto.IMessage|null|undefined} quotedStatus
         * @memberof proto.StatusMentionMessage
         * @instance
         */
        StatusMentionMessage.prototype.quotedStatus = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusMentionMessage.prototype, "_quotedStatus", {
            get: $util.oneOfGetter($oneOfFields = ["quotedStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new StatusMentionMessage instance using the specified properties.
         * @function create
         * @memberof proto.StatusMentionMessage
         * @static
         * @param {proto.IStatusMentionMessage=} [properties] Properties to set
         * @returns {proto.StatusMentionMessage} StatusMentionMessage instance
         */
        StatusMentionMessage.create = function create(properties) {
            return new StatusMentionMessage(properties);
        };

        /**
         * Encodes the specified StatusMentionMessage message. Does not implicitly {@link proto.StatusMentionMessage.verify|verify} messages.
         * @function encode
         * @memberof proto.StatusMentionMessage
         * @static
         * @param {proto.IStatusMentionMessage} message StatusMentionMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatusMentionMessage.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.quotedStatus != null && Object.hasOwnProperty.call(message, "quotedStatus"))
                $root.proto.Message.encode(message.quotedStatus, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified StatusMentionMessage message, length delimited. Does not implicitly {@link proto.StatusMentionMessage.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.StatusMentionMessage
         * @static
         * @param {proto.IStatusMentionMessage} message StatusMentionMessage message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatusMentionMessage.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a StatusMentionMessage message from the specified reader or buffer.
         * @function decode
         * @memberof proto.StatusMentionMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.StatusMentionMessage} StatusMentionMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatusMentionMessage.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.quotedStatus = $root.proto.Message.decode(reader, reader.uint32(), undefined, q + 1, message.quotedStatus);
                        message._quotedStatus = "quotedStatus";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a StatusMentionMessage message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.StatusMentionMessage
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.StatusMentionMessage} StatusMentionMessage
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatusMentionMessage.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StatusMentionMessage message.
         * @function verify
         * @memberof proto.StatusMentionMessage
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StatusMentionMessage.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.quotedStatus != null && Object.hasOwnProperty.call(message, "quotedStatus")) {
                properties._quotedStatus = 1;
                {
                    var error = $root.proto.Message.verify(message.quotedStatus, q + 1);
                    if (error)
                        return "quotedStatus." + error;
                }
            }
            return null;
        };

        /**
         * Creates a StatusMentionMessage message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.StatusMentionMessage
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.StatusMentionMessage} StatusMentionMessage
         */
        StatusMentionMessage.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.StatusMentionMessage: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.quotedStatus != null) {
                if (!$util.isObject(object.quotedStatus))
                    throw TypeError(".proto.StatusMentionMessage.quotedStatus: object expected");
                message.quotedStatus = $root.proto.Message.fromObject(object.quotedStatus, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a StatusMentionMessage message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.StatusMentionMessage
         * @static
         * @param {proto.StatusMentionMessage} message StatusMentionMessage
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StatusMentionMessage.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.quotedStatus != null && Object.hasOwnProperty.call(message, "quotedStatus"))
                object.quotedStatus = $root.proto.Message.toObject(message.quotedStatus, options, q + 1);
            return object;
        };

        /**
         * Converts this StatusMentionMessage to JSON.
         * @function toJSON
         * @memberof proto.StatusMentionMessage
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StatusMentionMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StatusMentionMessage
         * @function getTypeUrl
         * @memberof proto.StatusMentionMessage
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StatusMentionMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.StatusMentionMessage";
        };

        return StatusMentionMessage;
    })();

    proto.StatusPSA = (function() {

        /**
         * Properties of a StatusPSA.
         * @memberof proto
         * @interface IStatusPSA
         * @property {number|Long|null} [campaignId] StatusPSA campaignId
         * @property {number|Long|null} [campaignExpirationTimestamp] StatusPSA campaignExpirationTimestamp
         */

        /**
         * Constructs a new StatusPSA.
         * @memberof proto
         * @classdesc Represents a StatusPSA.
         * @implements IStatusPSA
         * @constructor
         * @param {proto.IStatusPSA=} [properties] Properties to set
         */
        function StatusPSA(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StatusPSA campaignId.
         * @member {number|Long|null|undefined} campaignId
         * @memberof proto.StatusPSA
         * @instance
         */
        StatusPSA.prototype.campaignId = null;

        /**
         * StatusPSA campaignExpirationTimestamp.
         * @member {number|Long|null|undefined} campaignExpirationTimestamp
         * @memberof proto.StatusPSA
         * @instance
         */
        StatusPSA.prototype.campaignExpirationTimestamp = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusPSA.prototype, "_campaignId", {
            get: $util.oneOfGetter($oneOfFields = ["campaignId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusPSA.prototype, "_campaignExpirationTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["campaignExpirationTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new StatusPSA instance using the specified properties.
         * @function create
         * @memberof proto.StatusPSA
         * @static
         * @param {proto.IStatusPSA=} [properties] Properties to set
         * @returns {proto.StatusPSA} StatusPSA instance
         */
        StatusPSA.create = function create(properties) {
            return new StatusPSA(properties);
        };

        /**
         * Encodes the specified StatusPSA message. Does not implicitly {@link proto.StatusPSA.verify|verify} messages.
         * @function encode
         * @memberof proto.StatusPSA
         * @static
         * @param {proto.IStatusPSA} message StatusPSA message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatusPSA.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.campaignId != null && Object.hasOwnProperty.call(message, "campaignId"))
                writer.uint32(/* id 44, wireType 0 =*/352).uint64(message.campaignId);
            if (message.campaignExpirationTimestamp != null && Object.hasOwnProperty.call(message, "campaignExpirationTimestamp"))
                writer.uint32(/* id 45, wireType 0 =*/360).uint64(message.campaignExpirationTimestamp);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified StatusPSA message, length delimited. Does not implicitly {@link proto.StatusPSA.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.StatusPSA
         * @static
         * @param {proto.IStatusPSA} message StatusPSA message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StatusPSA.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a StatusPSA message from the specified reader or buffer.
         * @function decode
         * @memberof proto.StatusPSA
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.StatusPSA} StatusPSA
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatusPSA.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 44: {
                        if (u !== 0)
                            break;
                        message.campaignId = reader.uint64();
                        message._campaignId = "campaignId";
                        continue;
                    }
                case 45: {
                        if (u !== 0)
                            break;
                        message.campaignExpirationTimestamp = reader.uint64();
                        message._campaignExpirationTimestamp = "campaignExpirationTimestamp";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a StatusPSA message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.StatusPSA
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.StatusPSA} StatusPSA
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StatusPSA.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StatusPSA message.
         * @function verify
         * @memberof proto.StatusPSA
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StatusPSA.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.campaignId != null && Object.hasOwnProperty.call(message, "campaignId")) {
                properties._campaignId = 1;
                if (!$util.isInteger(message.campaignId) && !(message.campaignId && $util.isInteger(message.campaignId.low) && $util.isInteger(message.campaignId.high)))
                    return "campaignId: integer|Long expected";
            }
            if (message.campaignExpirationTimestamp != null && Object.hasOwnProperty.call(message, "campaignExpirationTimestamp")) {
                properties._campaignExpirationTimestamp = 1;
                if (!$util.isInteger(message.campaignExpirationTimestamp) && !(message.campaignExpirationTimestamp && $util.isInteger(message.campaignExpirationTimestamp.low) && $util.isInteger(message.campaignExpirationTimestamp.high)))
                    return "campaignExpirationTimestamp: integer|Long expected";
            }
            return null;
        };

        /**
         * Creates a StatusPSA message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.StatusPSA
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.StatusPSA} StatusPSA
         */
        StatusPSA.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.StatusPSA: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.campaignId != null)
                if ($util.Long)
                    message.campaignId = $util.Long.fromValue(object.campaignId, true);
                else if (typeof object.campaignId === "string")
                    message.campaignId = parseInt(object.campaignId, 10);
                else if (typeof object.campaignId === "number")
                    message.campaignId = object.campaignId;
                else if (typeof object.campaignId === "object")
                    message.campaignId = new $util.LongBits(object.campaignId.low >>> 0, object.campaignId.high >>> 0).toNumber(true);
            if (object.campaignExpirationTimestamp != null)
                if ($util.Long)
                    message.campaignExpirationTimestamp = $util.Long.fromValue(object.campaignExpirationTimestamp, true);
                else if (typeof object.campaignExpirationTimestamp === "string")
                    message.campaignExpirationTimestamp = parseInt(object.campaignExpirationTimestamp, 10);
                else if (typeof object.campaignExpirationTimestamp === "number")
                    message.campaignExpirationTimestamp = object.campaignExpirationTimestamp;
                else if (typeof object.campaignExpirationTimestamp === "object")
                    message.campaignExpirationTimestamp = new $util.LongBits(object.campaignExpirationTimestamp.low >>> 0, object.campaignExpirationTimestamp.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a StatusPSA message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.StatusPSA
         * @static
         * @param {proto.StatusPSA} message StatusPSA
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StatusPSA.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.campaignId != null && Object.hasOwnProperty.call(message, "campaignId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.campaignId = typeof message.campaignId === "number" ? BigInt(message.campaignId) : $util.Long.fromBits(message.campaignId.low >>> 0, message.campaignId.high >>> 0, true).toBigInt();
                else if (typeof message.campaignId === "number")
                    object.campaignId = options.longs === String ? String(message.campaignId) : message.campaignId;
                else
                    object.campaignId = options.longs === String ? $util.Long.prototype.toString.call(message.campaignId) : options.longs === Number ? new $util.LongBits(message.campaignId.low >>> 0, message.campaignId.high >>> 0).toNumber(true) : message.campaignId;
            if (message.campaignExpirationTimestamp != null && Object.hasOwnProperty.call(message, "campaignExpirationTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.campaignExpirationTimestamp = typeof message.campaignExpirationTimestamp === "number" ? BigInt(message.campaignExpirationTimestamp) : $util.Long.fromBits(message.campaignExpirationTimestamp.low >>> 0, message.campaignExpirationTimestamp.high >>> 0, true).toBigInt();
                else if (typeof message.campaignExpirationTimestamp === "number")
                    object.campaignExpirationTimestamp = options.longs === String ? String(message.campaignExpirationTimestamp) : message.campaignExpirationTimestamp;
                else
                    object.campaignExpirationTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.campaignExpirationTimestamp) : options.longs === Number ? new $util.LongBits(message.campaignExpirationTimestamp.low >>> 0, message.campaignExpirationTimestamp.high >>> 0).toNumber(true) : message.campaignExpirationTimestamp;
            return object;
        };

        /**
         * Converts this StatusPSA to JSON.
         * @function toJSON
         * @memberof proto.StatusPSA
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StatusPSA.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StatusPSA
         * @function getTypeUrl
         * @memberof proto.StatusPSA
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StatusPSA.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.StatusPSA";
        };

        return StatusPSA;
    })();

    proto.StickerMetadata = (function() {

        /**
         * Properties of a StickerMetadata.
         * @memberof proto
         * @interface IStickerMetadata
         * @property {string|null} [url] StickerMetadata url
         * @property {Uint8Array|null} [fileSha256] StickerMetadata fileSha256
         * @property {Uint8Array|null} [fileEncSha256] StickerMetadata fileEncSha256
         * @property {Uint8Array|null} [mediaKey] StickerMetadata mediaKey
         * @property {string|null} [mimetype] StickerMetadata mimetype
         * @property {number|null} [height] StickerMetadata height
         * @property {number|null} [width] StickerMetadata width
         * @property {string|null} [directPath] StickerMetadata directPath
         * @property {number|Long|null} [fileLength] StickerMetadata fileLength
         * @property {number|null} [weight] StickerMetadata weight
         * @property {number|Long|null} [lastStickerSentTs] StickerMetadata lastStickerSentTs
         * @property {boolean|null} [isLottie] StickerMetadata isLottie
         * @property {string|null} [imageHash] StickerMetadata imageHash
         * @property {boolean|null} [isAvatarSticker] StickerMetadata isAvatarSticker
         */

        /**
         * Constructs a new StickerMetadata.
         * @memberof proto
         * @classdesc Represents a StickerMetadata.
         * @implements IStickerMetadata
         * @constructor
         * @param {proto.IStickerMetadata=} [properties] Properties to set
         */
        function StickerMetadata(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StickerMetadata url.
         * @member {string|null|undefined} url
         * @memberof proto.StickerMetadata
         * @instance
         */
        StickerMetadata.prototype.url = null;

        /**
         * StickerMetadata fileSha256.
         * @member {Uint8Array|null|undefined} fileSha256
         * @memberof proto.StickerMetadata
         * @instance
         */
        StickerMetadata.prototype.fileSha256 = null;

        /**
         * StickerMetadata fileEncSha256.
         * @member {Uint8Array|null|undefined} fileEncSha256
         * @memberof proto.StickerMetadata
         * @instance
         */
        StickerMetadata.prototype.fileEncSha256 = null;

        /**
         * StickerMetadata mediaKey.
         * @member {Uint8Array|null|undefined} mediaKey
         * @memberof proto.StickerMetadata
         * @instance
         */
        StickerMetadata.prototype.mediaKey = null;

        /**
         * StickerMetadata mimetype.
         * @member {string|null|undefined} mimetype
         * @memberof proto.StickerMetadata
         * @instance
         */
        StickerMetadata.prototype.mimetype = null;

        /**
         * StickerMetadata height.
         * @member {number|null|undefined} height
         * @memberof proto.StickerMetadata
         * @instance
         */
        StickerMetadata.prototype.height = null;

        /**
         * StickerMetadata width.
         * @member {number|null|undefined} width
         * @memberof proto.StickerMetadata
         * @instance
         */
        StickerMetadata.prototype.width = null;

        /**
         * StickerMetadata directPath.
         * @member {string|null|undefined} directPath
         * @memberof proto.StickerMetadata
         * @instance
         */
        StickerMetadata.prototype.directPath = null;

        /**
         * StickerMetadata fileLength.
         * @member {number|Long|null|undefined} fileLength
         * @memberof proto.StickerMetadata
         * @instance
         */
        StickerMetadata.prototype.fileLength = null;

        /**
         * StickerMetadata weight.
         * @member {number|null|undefined} weight
         * @memberof proto.StickerMetadata
         * @instance
         */
        StickerMetadata.prototype.weight = null;

        /**
         * StickerMetadata lastStickerSentTs.
         * @member {number|Long|null|undefined} lastStickerSentTs
         * @memberof proto.StickerMetadata
         * @instance
         */
        StickerMetadata.prototype.lastStickerSentTs = null;

        /**
         * StickerMetadata isLottie.
         * @member {boolean|null|undefined} isLottie
         * @memberof proto.StickerMetadata
         * @instance
         */
        StickerMetadata.prototype.isLottie = null;

        /**
         * StickerMetadata imageHash.
         * @member {string|null|undefined} imageHash
         * @memberof proto.StickerMetadata
         * @instance
         */
        StickerMetadata.prototype.imageHash = null;

        /**
         * StickerMetadata isAvatarSticker.
         * @member {boolean|null|undefined} isAvatarSticker
         * @memberof proto.StickerMetadata
         * @instance
         */
        StickerMetadata.prototype.isAvatarSticker = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMetadata.prototype, "_url", {
            get: $util.oneOfGetter($oneOfFields = ["url"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMetadata.prototype, "_fileSha256", {
            get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMetadata.prototype, "_fileEncSha256", {
            get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMetadata.prototype, "_mediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMetadata.prototype, "_mimetype", {
            get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMetadata.prototype, "_height", {
            get: $util.oneOfGetter($oneOfFields = ["height"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMetadata.prototype, "_width", {
            get: $util.oneOfGetter($oneOfFields = ["width"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMetadata.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMetadata.prototype, "_fileLength", {
            get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMetadata.prototype, "_weight", {
            get: $util.oneOfGetter($oneOfFields = ["weight"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMetadata.prototype, "_lastStickerSentTs", {
            get: $util.oneOfGetter($oneOfFields = ["lastStickerSentTs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMetadata.prototype, "_isLottie", {
            get: $util.oneOfGetter($oneOfFields = ["isLottie"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMetadata.prototype, "_imageHash", {
            get: $util.oneOfGetter($oneOfFields = ["imageHash"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMetadata.prototype, "_isAvatarSticker", {
            get: $util.oneOfGetter($oneOfFields = ["isAvatarSticker"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new StickerMetadata instance using the specified properties.
         * @function create
         * @memberof proto.StickerMetadata
         * @static
         * @param {proto.IStickerMetadata=} [properties] Properties to set
         * @returns {proto.StickerMetadata} StickerMetadata instance
         */
        StickerMetadata.create = function create(properties) {
            return new StickerMetadata(properties);
        };

        /**
         * Encodes the specified StickerMetadata message. Does not implicitly {@link proto.StickerMetadata.verify|verify} messages.
         * @function encode
         * @memberof proto.StickerMetadata
         * @static
         * @param {proto.IStickerMetadata} message StickerMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StickerMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.url);
            if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.fileSha256);
            if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.fileEncSha256);
            if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.mediaKey);
            if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.mimetype);
            if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.height);
            if (message.width != null && Object.hasOwnProperty.call(message, "width"))
                writer.uint32(/* id 7, wireType 0 =*/56).uint32(message.width);
            if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.directPath);
            if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                writer.uint32(/* id 9, wireType 0 =*/72).uint64(message.fileLength);
            if (message.weight != null && Object.hasOwnProperty.call(message, "weight"))
                writer.uint32(/* id 10, wireType 5 =*/85).float(message.weight);
            if (message.lastStickerSentTs != null && Object.hasOwnProperty.call(message, "lastStickerSentTs"))
                writer.uint32(/* id 11, wireType 0 =*/88).int64(message.lastStickerSentTs);
            if (message.isLottie != null && Object.hasOwnProperty.call(message, "isLottie"))
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.isLottie);
            if (message.imageHash != null && Object.hasOwnProperty.call(message, "imageHash"))
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.imageHash);
            if (message.isAvatarSticker != null && Object.hasOwnProperty.call(message, "isAvatarSticker"))
                writer.uint32(/* id 14, wireType 0 =*/112).bool(message.isAvatarSticker);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified StickerMetadata message, length delimited. Does not implicitly {@link proto.StickerMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.StickerMetadata
         * @static
         * @param {proto.IStickerMetadata} message StickerMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StickerMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a StickerMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof proto.StickerMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.StickerMetadata} StickerMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StickerMetadata.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.url = reader.stringVerify();
                        message._url = "url";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.fileSha256 = reader.bytes();
                        message._fileSha256 = "fileSha256";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.fileEncSha256 = reader.bytes();
                        message._fileEncSha256 = "fileEncSha256";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.mediaKey = reader.bytes();
                        message._mediaKey = "mediaKey";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.mimetype = reader.stringVerify();
                        message._mimetype = "mimetype";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        message.height = reader.uint32();
                        message._height = "height";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        message.width = reader.uint32();
                        message._width = "width";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        message.directPath = reader.stringVerify();
                        message._directPath = "directPath";
                        continue;
                    }
                case 9: {
                        if (u !== 0)
                            break;
                        message.fileLength = reader.uint64();
                        message._fileLength = "fileLength";
                        continue;
                    }
                case 10: {
                        if (u !== 5)
                            break;
                        message.weight = reader.float();
                        message._weight = "weight";
                        continue;
                    }
                case 11: {
                        if (u !== 0)
                            break;
                        message.lastStickerSentTs = reader.int64();
                        message._lastStickerSentTs = "lastStickerSentTs";
                        continue;
                    }
                case 12: {
                        if (u !== 0)
                            break;
                        message.isLottie = reader.bool();
                        message._isLottie = "isLottie";
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        message.imageHash = reader.stringVerify();
                        message._imageHash = "imageHash";
                        continue;
                    }
                case 14: {
                        if (u !== 0)
                            break;
                        message.isAvatarSticker = reader.bool();
                        message._isAvatarSticker = "isAvatarSticker";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a StickerMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.StickerMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.StickerMetadata} StickerMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StickerMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a StickerMetadata message.
         * @function verify
         * @memberof proto.StickerMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        StickerMetadata.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.url != null && Object.hasOwnProperty.call(message, "url")) {
                properties._url = 1;
                if (!$util.isString(message.url))
                    return "url: string expected";
            }
            if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256")) {
                properties._fileSha256 = 1;
                if (!(message.fileSha256 && typeof message.fileSha256.length === "number" || $util.isString(message.fileSha256)))
                    return "fileSha256: buffer expected";
            }
            if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256")) {
                properties._fileEncSha256 = 1;
                if (!(message.fileEncSha256 && typeof message.fileEncSha256.length === "number" || $util.isString(message.fileEncSha256)))
                    return "fileEncSha256: buffer expected";
            }
            if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey")) {
                properties._mediaKey = 1;
                if (!(message.mediaKey && typeof message.mediaKey.length === "number" || $util.isString(message.mediaKey)))
                    return "mediaKey: buffer expected";
            }
            if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype")) {
                properties._mimetype = 1;
                if (!$util.isString(message.mimetype))
                    return "mimetype: string expected";
            }
            if (message.height != null && Object.hasOwnProperty.call(message, "height")) {
                properties._height = 1;
                if (!$util.isInteger(message.height))
                    return "height: integer expected";
            }
            if (message.width != null && Object.hasOwnProperty.call(message, "width")) {
                properties._width = 1;
                if (!$util.isInteger(message.width))
                    return "width: integer expected";
            }
            if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath")) {
                properties._directPath = 1;
                if (!$util.isString(message.directPath))
                    return "directPath: string expected";
            }
            if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength")) {
                properties._fileLength = 1;
                if (!$util.isInteger(message.fileLength) && !(message.fileLength && $util.isInteger(message.fileLength.low) && $util.isInteger(message.fileLength.high)))
                    return "fileLength: integer|Long expected";
            }
            if (message.weight != null && Object.hasOwnProperty.call(message, "weight")) {
                properties._weight = 1;
                if (typeof message.weight !== "number")
                    return "weight: number expected";
            }
            if (message.lastStickerSentTs != null && Object.hasOwnProperty.call(message, "lastStickerSentTs")) {
                properties._lastStickerSentTs = 1;
                if (!$util.isInteger(message.lastStickerSentTs) && !(message.lastStickerSentTs && $util.isInteger(message.lastStickerSentTs.low) && $util.isInteger(message.lastStickerSentTs.high)))
                    return "lastStickerSentTs: integer|Long expected";
            }
            if (message.isLottie != null && Object.hasOwnProperty.call(message, "isLottie")) {
                properties._isLottie = 1;
                if (typeof message.isLottie !== "boolean")
                    return "isLottie: boolean expected";
            }
            if (message.imageHash != null && Object.hasOwnProperty.call(message, "imageHash")) {
                properties._imageHash = 1;
                if (!$util.isString(message.imageHash))
                    return "imageHash: string expected";
            }
            if (message.isAvatarSticker != null && Object.hasOwnProperty.call(message, "isAvatarSticker")) {
                properties._isAvatarSticker = 1;
                if (typeof message.isAvatarSticker !== "boolean")
                    return "isAvatarSticker: boolean expected";
            }
            return null;
        };

        /**
         * Creates a StickerMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.StickerMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.StickerMetadata} StickerMetadata
         */
        StickerMetadata.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.StickerMetadata: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.url != null)
                message.url = String(object.url);
            if (object.fileSha256 != null)
                if (typeof object.fileSha256 === "string")
                    $util.base64.decode(object.fileSha256, message.fileSha256 = $util.newBuffer($util.base64.length(object.fileSha256)), 0);
                else if (object.fileSha256.length >= 0)
                    message.fileSha256 = object.fileSha256;
            if (object.fileEncSha256 != null)
                if (typeof object.fileEncSha256 === "string")
                    $util.base64.decode(object.fileEncSha256, message.fileEncSha256 = $util.newBuffer($util.base64.length(object.fileEncSha256)), 0);
                else if (object.fileEncSha256.length >= 0)
                    message.fileEncSha256 = object.fileEncSha256;
            if (object.mediaKey != null)
                if (typeof object.mediaKey === "string")
                    $util.base64.decode(object.mediaKey, message.mediaKey = $util.newBuffer($util.base64.length(object.mediaKey)), 0);
                else if (object.mediaKey.length >= 0)
                    message.mediaKey = object.mediaKey;
            if (object.mimetype != null)
                message.mimetype = String(object.mimetype);
            if (object.height != null)
                message.height = object.height >>> 0;
            if (object.width != null)
                message.width = object.width >>> 0;
            if (object.directPath != null)
                message.directPath = String(object.directPath);
            if (object.fileLength != null)
                if ($util.Long)
                    message.fileLength = $util.Long.fromValue(object.fileLength, true);
                else if (typeof object.fileLength === "string")
                    message.fileLength = parseInt(object.fileLength, 10);
                else if (typeof object.fileLength === "number")
                    message.fileLength = object.fileLength;
                else if (typeof object.fileLength === "object")
                    message.fileLength = new $util.LongBits(object.fileLength.low >>> 0, object.fileLength.high >>> 0).toNumber(true);
            if (object.weight != null)
                message.weight = Number(object.weight);
            if (object.lastStickerSentTs != null)
                if ($util.Long)
                    message.lastStickerSentTs = $util.Long.fromValue(object.lastStickerSentTs, false);
                else if (typeof object.lastStickerSentTs === "string")
                    message.lastStickerSentTs = parseInt(object.lastStickerSentTs, 10);
                else if (typeof object.lastStickerSentTs === "number")
                    message.lastStickerSentTs = object.lastStickerSentTs;
                else if (typeof object.lastStickerSentTs === "object")
                    message.lastStickerSentTs = new $util.LongBits(object.lastStickerSentTs.low >>> 0, object.lastStickerSentTs.high >>> 0).toNumber();
            if (object.isLottie != null)
                message.isLottie = Boolean(object.isLottie);
            if (object.imageHash != null)
                message.imageHash = String(object.imageHash);
            if (object.isAvatarSticker != null)
                message.isAvatarSticker = Boolean(object.isAvatarSticker);
            return message;
        };

        /**
         * Creates a plain object from a StickerMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.StickerMetadata
         * @static
         * @param {proto.StickerMetadata} message StickerMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StickerMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                object.url = message.url;
            if (message.fileSha256 != null && Object.hasOwnProperty.call(message, "fileSha256"))
                object.fileSha256 = options.bytes === String ? $util.base64.encode(message.fileSha256, 0, message.fileSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileSha256) : message.fileSha256;
            if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                object.fileEncSha256 = options.bytes === String ? $util.base64.encode(message.fileEncSha256, 0, message.fileEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileEncSha256) : message.fileEncSha256;
            if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                object.mediaKey = options.bytes === String ? $util.base64.encode(message.mediaKey, 0, message.mediaKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.mediaKey) : message.mediaKey;
            if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                object.mimetype = message.mimetype;
            if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                object.height = message.height;
            if (message.width != null && Object.hasOwnProperty.call(message, "width"))
                object.width = message.width;
            if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                object.directPath = message.directPath;
            if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.fileLength = typeof message.fileLength === "number" ? BigInt(message.fileLength) : $util.Long.fromBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0, true).toBigInt();
                else if (typeof message.fileLength === "number")
                    object.fileLength = options.longs === String ? String(message.fileLength) : message.fileLength;
                else
                    object.fileLength = options.longs === String ? $util.Long.prototype.toString.call(message.fileLength) : options.longs === Number ? new $util.LongBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0).toNumber(true) : message.fileLength;
            if (message.weight != null && Object.hasOwnProperty.call(message, "weight"))
                object.weight = options.json && !isFinite(message.weight) ? String(message.weight) : message.weight;
            if (message.lastStickerSentTs != null && Object.hasOwnProperty.call(message, "lastStickerSentTs"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastStickerSentTs = typeof message.lastStickerSentTs === "number" ? BigInt(message.lastStickerSentTs) : $util.Long.fromBits(message.lastStickerSentTs.low >>> 0, message.lastStickerSentTs.high >>> 0, false).toBigInt();
                else if (typeof message.lastStickerSentTs === "number")
                    object.lastStickerSentTs = options.longs === String ? String(message.lastStickerSentTs) : message.lastStickerSentTs;
                else
                    object.lastStickerSentTs = options.longs === String ? $util.Long.prototype.toString.call(message.lastStickerSentTs) : options.longs === Number ? new $util.LongBits(message.lastStickerSentTs.low >>> 0, message.lastStickerSentTs.high >>> 0).toNumber() : message.lastStickerSentTs;
            if (message.isLottie != null && Object.hasOwnProperty.call(message, "isLottie"))
                object.isLottie = message.isLottie;
            if (message.imageHash != null && Object.hasOwnProperty.call(message, "imageHash"))
                object.imageHash = message.imageHash;
            if (message.isAvatarSticker != null && Object.hasOwnProperty.call(message, "isAvatarSticker"))
                object.isAvatarSticker = message.isAvatarSticker;
            return object;
        };

        /**
         * Converts this StickerMetadata to JSON.
         * @function toJSON
         * @memberof proto.StickerMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StickerMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StickerMetadata
         * @function getTypeUrl
         * @memberof proto.StickerMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StickerMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.StickerMetadata";
        };

        return StickerMetadata;
    })();

    proto.SyncActionData = (function() {

        /**
         * Properties of a SyncActionData.
         * @memberof proto
         * @interface ISyncActionData
         * @property {Uint8Array|null} [index] SyncActionData index
         * @property {proto.ISyncActionValue|null} [value] SyncActionData value
         * @property {Uint8Array|null} [padding] SyncActionData padding
         * @property {number|null} [version] SyncActionData version
         */

        /**
         * Constructs a new SyncActionData.
         * @memberof proto
         * @classdesc Represents a SyncActionData.
         * @implements ISyncActionData
         * @constructor
         * @param {proto.ISyncActionData=} [properties] Properties to set
         */
        function SyncActionData(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SyncActionData index.
         * @member {Uint8Array|null|undefined} index
         * @memberof proto.SyncActionData
         * @instance
         */
        SyncActionData.prototype.index = null;

        /**
         * SyncActionData value.
         * @member {proto.ISyncActionValue|null|undefined} value
         * @memberof proto.SyncActionData
         * @instance
         */
        SyncActionData.prototype.value = null;

        /**
         * SyncActionData padding.
         * @member {Uint8Array|null|undefined} padding
         * @memberof proto.SyncActionData
         * @instance
         */
        SyncActionData.prototype.padding = null;

        /**
         * SyncActionData version.
         * @member {number|null|undefined} version
         * @memberof proto.SyncActionData
         * @instance
         */
        SyncActionData.prototype.version = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionData.prototype, "_index", {
            get: $util.oneOfGetter($oneOfFields = ["index"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionData.prototype, "_value", {
            get: $util.oneOfGetter($oneOfFields = ["value"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionData.prototype, "_padding", {
            get: $util.oneOfGetter($oneOfFields = ["padding"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionData.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SyncActionData instance using the specified properties.
         * @function create
         * @memberof proto.SyncActionData
         * @static
         * @param {proto.ISyncActionData=} [properties] Properties to set
         * @returns {proto.SyncActionData} SyncActionData instance
         */
        SyncActionData.create = function create(properties) {
            return new SyncActionData(properties);
        };

        /**
         * Encodes the specified SyncActionData message. Does not implicitly {@link proto.SyncActionData.verify|verify} messages.
         * @function encode
         * @memberof proto.SyncActionData
         * @static
         * @param {proto.ISyncActionData} message SyncActionData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SyncActionData.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.index);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                $root.proto.SyncActionValue.encode(message.value, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.padding != null && Object.hasOwnProperty.call(message, "padding"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.padding);
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.version);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SyncActionData message, length delimited. Does not implicitly {@link proto.SyncActionData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SyncActionData
         * @static
         * @param {proto.ISyncActionData} message SyncActionData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SyncActionData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SyncActionData message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SyncActionData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SyncActionData} SyncActionData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SyncActionData.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.index = reader.bytes();
                        message._index = "index";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.value = $root.proto.SyncActionValue.decode(reader, reader.uint32(), undefined, q + 1, message.value);
                        message._value = "value";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.padding = reader.bytes();
                        message._padding = "padding";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.version = reader.int32();
                        message._version = "version";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a SyncActionData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SyncActionData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SyncActionData} SyncActionData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SyncActionData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SyncActionData message.
         * @function verify
         * @memberof proto.SyncActionData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SyncActionData.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.index != null && Object.hasOwnProperty.call(message, "index")) {
                properties._index = 1;
                if (!(message.index && typeof message.index.length === "number" || $util.isString(message.index)))
                    return "index: buffer expected";
            }
            if (message.value != null && Object.hasOwnProperty.call(message, "value")) {
                properties._value = 1;
                {
                    var error = $root.proto.SyncActionValue.verify(message.value, q + 1);
                    if (error)
                        return "value." + error;
                }
            }
            if (message.padding != null && Object.hasOwnProperty.call(message, "padding")) {
                properties._padding = 1;
                if (!(message.padding && typeof message.padding.length === "number" || $util.isString(message.padding)))
                    return "padding: buffer expected";
            }
            if (message.version != null && Object.hasOwnProperty.call(message, "version")) {
                properties._version = 1;
                if (!$util.isInteger(message.version))
                    return "version: integer expected";
            }
            return null;
        };

        /**
         * Creates a SyncActionData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SyncActionData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SyncActionData} SyncActionData
         */
        SyncActionData.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.SyncActionData: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.index != null)
                if (typeof object.index === "string")
                    $util.base64.decode(object.index, message.index = $util.newBuffer($util.base64.length(object.index)), 0);
                else if (object.index.length >= 0)
                    message.index = object.index;
            if (object.value != null) {
                if (!$util.isObject(object.value))
                    throw TypeError(".proto.SyncActionData.value: object expected");
                message.value = $root.proto.SyncActionValue.fromObject(object.value, q + 1);
            }
            if (object.padding != null)
                if (typeof object.padding === "string")
                    $util.base64.decode(object.padding, message.padding = $util.newBuffer($util.base64.length(object.padding)), 0);
                else if (object.padding.length >= 0)
                    message.padding = object.padding;
            if (object.version != null)
                message.version = object.version | 0;
            return message;
        };

        /**
         * Creates a plain object from a SyncActionData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SyncActionData
         * @static
         * @param {proto.SyncActionData} message SyncActionData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SyncActionData.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                object.index = options.bytes === String ? $util.base64.encode(message.index, 0, message.index.length) : options.bytes === Array ? Array.prototype.slice.call(message.index) : message.index;
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                object.value = $root.proto.SyncActionValue.toObject(message.value, options, q + 1);
            if (message.padding != null && Object.hasOwnProperty.call(message, "padding"))
                object.padding = options.bytes === String ? $util.base64.encode(message.padding, 0, message.padding.length) : options.bytes === Array ? Array.prototype.slice.call(message.padding) : message.padding;
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                object.version = message.version;
            return object;
        };

        /**
         * Converts this SyncActionData to JSON.
         * @function toJSON
         * @memberof proto.SyncActionData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SyncActionData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SyncActionData
         * @function getTypeUrl
         * @memberof proto.SyncActionData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SyncActionData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.SyncActionData";
        };

        return SyncActionData;
    })();

    proto.SyncActionValue = (function() {

        /**
         * Properties of a SyncActionValue.
         * @memberof proto
         * @interface ISyncActionValue
         * @property {number|Long|null} [timestamp] SyncActionValue timestamp
         * @property {proto.SyncActionValue.IStarAction|null} [starAction] SyncActionValue starAction
         * @property {proto.SyncActionValue.IContactAction|null} [contactAction] SyncActionValue contactAction
         * @property {proto.SyncActionValue.IMuteAction|null} [muteAction] SyncActionValue muteAction
         * @property {proto.SyncActionValue.IPinAction|null} [pinAction] SyncActionValue pinAction
         * @property {proto.SyncActionValue.IPushNameSetting|null} [pushNameSetting] SyncActionValue pushNameSetting
         * @property {proto.SyncActionValue.IQuickReplyAction|null} [quickReplyAction] SyncActionValue quickReplyAction
         * @property {proto.SyncActionValue.IRecentEmojiWeightsAction|null} [recentEmojiWeightsAction] SyncActionValue recentEmojiWeightsAction
         * @property {proto.SyncActionValue.ILabelEditAction|null} [labelEditAction] SyncActionValue labelEditAction
         * @property {proto.SyncActionValue.ILabelAssociationAction|null} [labelAssociationAction] SyncActionValue labelAssociationAction
         * @property {proto.SyncActionValue.ILocaleSetting|null} [localeSetting] SyncActionValue localeSetting
         * @property {proto.SyncActionValue.IArchiveChatAction|null} [archiveChatAction] SyncActionValue archiveChatAction
         * @property {proto.SyncActionValue.IDeleteMessageForMeAction|null} [deleteMessageForMeAction] SyncActionValue deleteMessageForMeAction
         * @property {proto.SyncActionValue.IKeyExpiration|null} [keyExpiration] SyncActionValue keyExpiration
         * @property {proto.SyncActionValue.IMarkChatAsReadAction|null} [markChatAsReadAction] SyncActionValue markChatAsReadAction
         * @property {proto.SyncActionValue.IClearChatAction|null} [clearChatAction] SyncActionValue clearChatAction
         * @property {proto.SyncActionValue.IDeleteChatAction|null} [deleteChatAction] SyncActionValue deleteChatAction
         * @property {proto.SyncActionValue.IUnarchiveChatsSetting|null} [unarchiveChatsSetting] SyncActionValue unarchiveChatsSetting
         * @property {proto.SyncActionValue.IPrimaryFeature|null} [primaryFeature] SyncActionValue primaryFeature
         * @property {proto.SyncActionValue.IAndroidUnsupportedActions|null} [androidUnsupportedActions] SyncActionValue androidUnsupportedActions
         * @property {proto.SyncActionValue.IAgentAction|null} [agentAction] SyncActionValue agentAction
         * @property {proto.SyncActionValue.ISubscriptionAction|null} [subscriptionAction] SyncActionValue subscriptionAction
         * @property {proto.SyncActionValue.IUserStatusMuteAction|null} [userStatusMuteAction] SyncActionValue userStatusMuteAction
         * @property {proto.SyncActionValue.ITimeFormatAction|null} [timeFormatAction] SyncActionValue timeFormatAction
         * @property {proto.SyncActionValue.INuxAction|null} [nuxAction] SyncActionValue nuxAction
         * @property {proto.SyncActionValue.IPrimaryVersionAction|null} [primaryVersionAction] SyncActionValue primaryVersionAction
         * @property {proto.SyncActionValue.IStickerAction|null} [stickerAction] SyncActionValue stickerAction
         * @property {proto.SyncActionValue.IRemoveRecentStickerAction|null} [removeRecentStickerAction] SyncActionValue removeRecentStickerAction
         * @property {proto.SyncActionValue.IChatAssignmentAction|null} [chatAssignment] SyncActionValue chatAssignment
         * @property {proto.SyncActionValue.IChatAssignmentOpenedStatusAction|null} [chatAssignmentOpenedStatus] SyncActionValue chatAssignmentOpenedStatus
         * @property {proto.SyncActionValue.IPnForLidChatAction|null} [pnForLidChatAction] SyncActionValue pnForLidChatAction
         * @property {proto.SyncActionValue.IMarketingMessageAction|null} [marketingMessageAction] SyncActionValue marketingMessageAction
         * @property {proto.SyncActionValue.IMarketingMessageBroadcastAction|null} [marketingMessageBroadcastAction] SyncActionValue marketingMessageBroadcastAction
         * @property {proto.SyncActionValue.IExternalWebBetaAction|null} [externalWebBetaAction] SyncActionValue externalWebBetaAction
         * @property {proto.SyncActionValue.IPrivacySettingRelayAllCalls|null} [privacySettingRelayAllCalls] SyncActionValue privacySettingRelayAllCalls
         * @property {proto.SyncActionValue.ICallLogAction|null} [callLogAction] SyncActionValue callLogAction
         * @property {proto.SyncActionValue.IUGCBot|null} [ugcBot] SyncActionValue ugcBot
         * @property {proto.SyncActionValue.IStatusPrivacyAction|null} [statusPrivacy] SyncActionValue statusPrivacy
         * @property {proto.SyncActionValue.IBotWelcomeRequestAction|null} [botWelcomeRequestAction] SyncActionValue botWelcomeRequestAction
         * @property {proto.SyncActionValue.IDeleteIndividualCallLogAction|null} [deleteIndividualCallLog] SyncActionValue deleteIndividualCallLog
         * @property {proto.SyncActionValue.ILabelReorderingAction|null} [labelReorderingAction] SyncActionValue labelReorderingAction
         * @property {proto.SyncActionValue.IPaymentInfoAction|null} [paymentInfoAction] SyncActionValue paymentInfoAction
         * @property {proto.SyncActionValue.ICustomPaymentMethodsAction|null} [customPaymentMethodsAction] SyncActionValue customPaymentMethodsAction
         * @property {proto.SyncActionValue.ILockChatAction|null} [lockChatAction] SyncActionValue lockChatAction
         * @property {proto.IChatLockSettings|null} [chatLockSettings] SyncActionValue chatLockSettings
         * @property {proto.SyncActionValue.IWamoUserIdentifierAction|null} [wamoUserIdentifierAction] SyncActionValue wamoUserIdentifierAction
         * @property {proto.SyncActionValue.IPrivacySettingDisableLinkPreviewsAction|null} [privacySettingDisableLinkPreviewsAction] SyncActionValue privacySettingDisableLinkPreviewsAction
         * @property {proto.IDeviceCapabilities|null} [deviceCapabilities] SyncActionValue deviceCapabilities
         * @property {proto.SyncActionValue.INoteEditAction|null} [noteEditAction] SyncActionValue noteEditAction
         * @property {proto.SyncActionValue.IFavoritesAction|null} [favoritesAction] SyncActionValue favoritesAction
         * @property {proto.SyncActionValue.IMerchantPaymentPartnerAction|null} [merchantPaymentPartnerAction] SyncActionValue merchantPaymentPartnerAction
         * @property {proto.SyncActionValue.IWaffleAccountLinkStateAction|null} [waffleAccountLinkStateAction] SyncActionValue waffleAccountLinkStateAction
         * @property {proto.SyncActionValue.IUsernameChatStartModeAction|null} [usernameChatStartMode] SyncActionValue usernameChatStartMode
         * @property {proto.SyncActionValue.INotificationActivitySettingAction|null} [notificationActivitySettingAction] SyncActionValue notificationActivitySettingAction
         * @property {proto.SyncActionValue.ILidContactAction|null} [lidContactAction] SyncActionValue lidContactAction
         * @property {proto.SyncActionValue.ICtwaPerCustomerDataSharingAction|null} [ctwaPerCustomerDataSharingAction] SyncActionValue ctwaPerCustomerDataSharingAction
         * @property {proto.SyncActionValue.IPaymentTosAction|null} [paymentTosAction] SyncActionValue paymentTosAction
         * @property {proto.SyncActionValue.IPrivacySettingChannelsPersonalisedRecommendationAction|null} [privacySettingChannelsPersonalisedRecommendationAction] SyncActionValue privacySettingChannelsPersonalisedRecommendationAction
         * @property {proto.SyncActionValue.IDetectedOutcomesStatusAction|null} [detectedOutcomesStatusAction] SyncActionValue detectedOutcomesStatusAction
         * @property {proto.SyncActionValue.IMaibaAIFeaturesControlAction|null} [maibaAiFeaturesControlAction] SyncActionValue maibaAiFeaturesControlAction
         * @property {proto.SyncActionValue.IBusinessBroadcastListAction|null} [businessBroadcastListAction] SyncActionValue businessBroadcastListAction
         * @property {proto.SyncActionValue.IMusicUserIdAction|null} [musicUserIdAction] SyncActionValue musicUserIdAction
         * @property {proto.SyncActionValue.IStatusPostOptInNotificationPreferencesAction|null} [statusPostOptInNotificationPreferencesAction] SyncActionValue statusPostOptInNotificationPreferencesAction
         * @property {proto.SyncActionValue.IAvatarUpdatedAction|null} [avatarUpdatedAction] SyncActionValue avatarUpdatedAction
         * @property {proto.SyncActionValue.IPrivateProcessingSettingAction|null} [privateProcessingSettingAction] SyncActionValue privateProcessingSettingAction
         * @property {proto.SyncActionValue.INewsletterSavedInterestsAction|null} [newsletterSavedInterestsAction] SyncActionValue newsletterSavedInterestsAction
         * @property {proto.SyncActionValue.IAiThreadRenameAction|null} [aiThreadRenameAction] SyncActionValue aiThreadRenameAction
         * @property {proto.SyncActionValue.IInteractiveMessageAction|null} [interactiveMessageAction] SyncActionValue interactiveMessageAction
         * @property {proto.SyncActionValue.ISettingsSyncAction|null} [settingsSyncAction] SyncActionValue settingsSyncAction
         * @property {proto.SyncActionValue.IOutContactAction|null} [outContactAction] SyncActionValue outContactAction
         * @property {proto.SyncActionValue.INctSaltSyncAction|null} [nctSaltSyncAction] SyncActionValue nctSaltSyncAction
         * @property {proto.SyncActionValue.IBusinessBroadcastCampaignAction|null} [businessBroadcastCampaignAction] SyncActionValue businessBroadcastCampaignAction
         * @property {proto.SyncActionValue.IBusinessBroadcastInsightsAction|null} [businessBroadcastInsightsAction] SyncActionValue businessBroadcastInsightsAction
         * @property {proto.SyncActionValue.ICustomerDataAction|null} [customerDataAction] SyncActionValue customerDataAction
         * @property {proto.SyncActionValue.ISubscriptionsSyncV2Action|null} [subscriptionsSyncV2Action] SyncActionValue subscriptionsSyncV2Action
         * @property {proto.SyncActionValue.IThreadPinAction|null} [threadPinAction] SyncActionValue threadPinAction
         * @property {proto.SyncActionValue.IAutoOrganizeBusinessChatSetting|null} [autoOrganizeBusinessChatSetting] SyncActionValue autoOrganizeBusinessChatSetting
         * @property {proto.SyncActionValue.IBizAISettingsNudgeAction|null} [bizAiSettingsNudgeAction] SyncActionValue bizAiSettingsNudgeAction
         * @property {proto.SyncActionValue.ICoexV2VersionAction|null} [coexV2VersionAction] SyncActionValue coexV2VersionAction
         * @property {proto.SyncActionValue.IWASARootSecretAction|null} [wasaRootSecretAction] SyncActionValue wasaRootSecretAction
         */

        /**
         * Constructs a new SyncActionValue.
         * @memberof proto
         * @classdesc Represents a SyncActionValue.
         * @implements ISyncActionValue
         * @constructor
         * @param {proto.ISyncActionValue=} [properties] Properties to set
         */
        function SyncActionValue(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SyncActionValue timestamp.
         * @member {number|Long|null|undefined} timestamp
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.timestamp = null;

        /**
         * SyncActionValue starAction.
         * @member {proto.SyncActionValue.IStarAction|null|undefined} starAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.starAction = null;

        /**
         * SyncActionValue contactAction.
         * @member {proto.SyncActionValue.IContactAction|null|undefined} contactAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.contactAction = null;

        /**
         * SyncActionValue muteAction.
         * @member {proto.SyncActionValue.IMuteAction|null|undefined} muteAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.muteAction = null;

        /**
         * SyncActionValue pinAction.
         * @member {proto.SyncActionValue.IPinAction|null|undefined} pinAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.pinAction = null;

        /**
         * SyncActionValue pushNameSetting.
         * @member {proto.SyncActionValue.IPushNameSetting|null|undefined} pushNameSetting
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.pushNameSetting = null;

        /**
         * SyncActionValue quickReplyAction.
         * @member {proto.SyncActionValue.IQuickReplyAction|null|undefined} quickReplyAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.quickReplyAction = null;

        /**
         * SyncActionValue recentEmojiWeightsAction.
         * @member {proto.SyncActionValue.IRecentEmojiWeightsAction|null|undefined} recentEmojiWeightsAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.recentEmojiWeightsAction = null;

        /**
         * SyncActionValue labelEditAction.
         * @member {proto.SyncActionValue.ILabelEditAction|null|undefined} labelEditAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.labelEditAction = null;

        /**
         * SyncActionValue labelAssociationAction.
         * @member {proto.SyncActionValue.ILabelAssociationAction|null|undefined} labelAssociationAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.labelAssociationAction = null;

        /**
         * SyncActionValue localeSetting.
         * @member {proto.SyncActionValue.ILocaleSetting|null|undefined} localeSetting
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.localeSetting = null;

        /**
         * SyncActionValue archiveChatAction.
         * @member {proto.SyncActionValue.IArchiveChatAction|null|undefined} archiveChatAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.archiveChatAction = null;

        /**
         * SyncActionValue deleteMessageForMeAction.
         * @member {proto.SyncActionValue.IDeleteMessageForMeAction|null|undefined} deleteMessageForMeAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.deleteMessageForMeAction = null;

        /**
         * SyncActionValue keyExpiration.
         * @member {proto.SyncActionValue.IKeyExpiration|null|undefined} keyExpiration
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.keyExpiration = null;

        /**
         * SyncActionValue markChatAsReadAction.
         * @member {proto.SyncActionValue.IMarkChatAsReadAction|null|undefined} markChatAsReadAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.markChatAsReadAction = null;

        /**
         * SyncActionValue clearChatAction.
         * @member {proto.SyncActionValue.IClearChatAction|null|undefined} clearChatAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.clearChatAction = null;

        /**
         * SyncActionValue deleteChatAction.
         * @member {proto.SyncActionValue.IDeleteChatAction|null|undefined} deleteChatAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.deleteChatAction = null;

        /**
         * SyncActionValue unarchiveChatsSetting.
         * @member {proto.SyncActionValue.IUnarchiveChatsSetting|null|undefined} unarchiveChatsSetting
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.unarchiveChatsSetting = null;

        /**
         * SyncActionValue primaryFeature.
         * @member {proto.SyncActionValue.IPrimaryFeature|null|undefined} primaryFeature
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.primaryFeature = null;

        /**
         * SyncActionValue androidUnsupportedActions.
         * @member {proto.SyncActionValue.IAndroidUnsupportedActions|null|undefined} androidUnsupportedActions
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.androidUnsupportedActions = null;

        /**
         * SyncActionValue agentAction.
         * @member {proto.SyncActionValue.IAgentAction|null|undefined} agentAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.agentAction = null;

        /**
         * SyncActionValue subscriptionAction.
         * @member {proto.SyncActionValue.ISubscriptionAction|null|undefined} subscriptionAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.subscriptionAction = null;

        /**
         * SyncActionValue userStatusMuteAction.
         * @member {proto.SyncActionValue.IUserStatusMuteAction|null|undefined} userStatusMuteAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.userStatusMuteAction = null;

        /**
         * SyncActionValue timeFormatAction.
         * @member {proto.SyncActionValue.ITimeFormatAction|null|undefined} timeFormatAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.timeFormatAction = null;

        /**
         * SyncActionValue nuxAction.
         * @member {proto.SyncActionValue.INuxAction|null|undefined} nuxAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.nuxAction = null;

        /**
         * SyncActionValue primaryVersionAction.
         * @member {proto.SyncActionValue.IPrimaryVersionAction|null|undefined} primaryVersionAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.primaryVersionAction = null;

        /**
         * SyncActionValue stickerAction.
         * @member {proto.SyncActionValue.IStickerAction|null|undefined} stickerAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.stickerAction = null;

        /**
         * SyncActionValue removeRecentStickerAction.
         * @member {proto.SyncActionValue.IRemoveRecentStickerAction|null|undefined} removeRecentStickerAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.removeRecentStickerAction = null;

        /**
         * SyncActionValue chatAssignment.
         * @member {proto.SyncActionValue.IChatAssignmentAction|null|undefined} chatAssignment
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.chatAssignment = null;

        /**
         * SyncActionValue chatAssignmentOpenedStatus.
         * @member {proto.SyncActionValue.IChatAssignmentOpenedStatusAction|null|undefined} chatAssignmentOpenedStatus
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.chatAssignmentOpenedStatus = null;

        /**
         * SyncActionValue pnForLidChatAction.
         * @member {proto.SyncActionValue.IPnForLidChatAction|null|undefined} pnForLidChatAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.pnForLidChatAction = null;

        /**
         * SyncActionValue marketingMessageAction.
         * @member {proto.SyncActionValue.IMarketingMessageAction|null|undefined} marketingMessageAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.marketingMessageAction = null;

        /**
         * SyncActionValue marketingMessageBroadcastAction.
         * @member {proto.SyncActionValue.IMarketingMessageBroadcastAction|null|undefined} marketingMessageBroadcastAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.marketingMessageBroadcastAction = null;

        /**
         * SyncActionValue externalWebBetaAction.
         * @member {proto.SyncActionValue.IExternalWebBetaAction|null|undefined} externalWebBetaAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.externalWebBetaAction = null;

        /**
         * SyncActionValue privacySettingRelayAllCalls.
         * @member {proto.SyncActionValue.IPrivacySettingRelayAllCalls|null|undefined} privacySettingRelayAllCalls
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.privacySettingRelayAllCalls = null;

        /**
         * SyncActionValue callLogAction.
         * @member {proto.SyncActionValue.ICallLogAction|null|undefined} callLogAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.callLogAction = null;

        /**
         * SyncActionValue ugcBot.
         * @member {proto.SyncActionValue.IUGCBot|null|undefined} ugcBot
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.ugcBot = null;

        /**
         * SyncActionValue statusPrivacy.
         * @member {proto.SyncActionValue.IStatusPrivacyAction|null|undefined} statusPrivacy
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.statusPrivacy = null;

        /**
         * SyncActionValue botWelcomeRequestAction.
         * @member {proto.SyncActionValue.IBotWelcomeRequestAction|null|undefined} botWelcomeRequestAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.botWelcomeRequestAction = null;

        /**
         * SyncActionValue deleteIndividualCallLog.
         * @member {proto.SyncActionValue.IDeleteIndividualCallLogAction|null|undefined} deleteIndividualCallLog
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.deleteIndividualCallLog = null;

        /**
         * SyncActionValue labelReorderingAction.
         * @member {proto.SyncActionValue.ILabelReorderingAction|null|undefined} labelReorderingAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.labelReorderingAction = null;

        /**
         * SyncActionValue paymentInfoAction.
         * @member {proto.SyncActionValue.IPaymentInfoAction|null|undefined} paymentInfoAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.paymentInfoAction = null;

        /**
         * SyncActionValue customPaymentMethodsAction.
         * @member {proto.SyncActionValue.ICustomPaymentMethodsAction|null|undefined} customPaymentMethodsAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.customPaymentMethodsAction = null;

        /**
         * SyncActionValue lockChatAction.
         * @member {proto.SyncActionValue.ILockChatAction|null|undefined} lockChatAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.lockChatAction = null;

        /**
         * SyncActionValue chatLockSettings.
         * @member {proto.IChatLockSettings|null|undefined} chatLockSettings
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.chatLockSettings = null;

        /**
         * SyncActionValue wamoUserIdentifierAction.
         * @member {proto.SyncActionValue.IWamoUserIdentifierAction|null|undefined} wamoUserIdentifierAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.wamoUserIdentifierAction = null;

        /**
         * SyncActionValue privacySettingDisableLinkPreviewsAction.
         * @member {proto.SyncActionValue.IPrivacySettingDisableLinkPreviewsAction|null|undefined} privacySettingDisableLinkPreviewsAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.privacySettingDisableLinkPreviewsAction = null;

        /**
         * SyncActionValue deviceCapabilities.
         * @member {proto.IDeviceCapabilities|null|undefined} deviceCapabilities
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.deviceCapabilities = null;

        /**
         * SyncActionValue noteEditAction.
         * @member {proto.SyncActionValue.INoteEditAction|null|undefined} noteEditAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.noteEditAction = null;

        /**
         * SyncActionValue favoritesAction.
         * @member {proto.SyncActionValue.IFavoritesAction|null|undefined} favoritesAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.favoritesAction = null;

        /**
         * SyncActionValue merchantPaymentPartnerAction.
         * @member {proto.SyncActionValue.IMerchantPaymentPartnerAction|null|undefined} merchantPaymentPartnerAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.merchantPaymentPartnerAction = null;

        /**
         * SyncActionValue waffleAccountLinkStateAction.
         * @member {proto.SyncActionValue.IWaffleAccountLinkStateAction|null|undefined} waffleAccountLinkStateAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.waffleAccountLinkStateAction = null;

        /**
         * SyncActionValue usernameChatStartMode.
         * @member {proto.SyncActionValue.IUsernameChatStartModeAction|null|undefined} usernameChatStartMode
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.usernameChatStartMode = null;

        /**
         * SyncActionValue notificationActivitySettingAction.
         * @member {proto.SyncActionValue.INotificationActivitySettingAction|null|undefined} notificationActivitySettingAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.notificationActivitySettingAction = null;

        /**
         * SyncActionValue lidContactAction.
         * @member {proto.SyncActionValue.ILidContactAction|null|undefined} lidContactAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.lidContactAction = null;

        /**
         * SyncActionValue ctwaPerCustomerDataSharingAction.
         * @member {proto.SyncActionValue.ICtwaPerCustomerDataSharingAction|null|undefined} ctwaPerCustomerDataSharingAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.ctwaPerCustomerDataSharingAction = null;

        /**
         * SyncActionValue paymentTosAction.
         * @member {proto.SyncActionValue.IPaymentTosAction|null|undefined} paymentTosAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.paymentTosAction = null;

        /**
         * SyncActionValue privacySettingChannelsPersonalisedRecommendationAction.
         * @member {proto.SyncActionValue.IPrivacySettingChannelsPersonalisedRecommendationAction|null|undefined} privacySettingChannelsPersonalisedRecommendationAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.privacySettingChannelsPersonalisedRecommendationAction = null;

        /**
         * SyncActionValue detectedOutcomesStatusAction.
         * @member {proto.SyncActionValue.IDetectedOutcomesStatusAction|null|undefined} detectedOutcomesStatusAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.detectedOutcomesStatusAction = null;

        /**
         * SyncActionValue maibaAiFeaturesControlAction.
         * @member {proto.SyncActionValue.IMaibaAIFeaturesControlAction|null|undefined} maibaAiFeaturesControlAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.maibaAiFeaturesControlAction = null;

        /**
         * SyncActionValue businessBroadcastListAction.
         * @member {proto.SyncActionValue.IBusinessBroadcastListAction|null|undefined} businessBroadcastListAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.businessBroadcastListAction = null;

        /**
         * SyncActionValue musicUserIdAction.
         * @member {proto.SyncActionValue.IMusicUserIdAction|null|undefined} musicUserIdAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.musicUserIdAction = null;

        /**
         * SyncActionValue statusPostOptInNotificationPreferencesAction.
         * @member {proto.SyncActionValue.IStatusPostOptInNotificationPreferencesAction|null|undefined} statusPostOptInNotificationPreferencesAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.statusPostOptInNotificationPreferencesAction = null;

        /**
         * SyncActionValue avatarUpdatedAction.
         * @member {proto.SyncActionValue.IAvatarUpdatedAction|null|undefined} avatarUpdatedAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.avatarUpdatedAction = null;

        /**
         * SyncActionValue privateProcessingSettingAction.
         * @member {proto.SyncActionValue.IPrivateProcessingSettingAction|null|undefined} privateProcessingSettingAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.privateProcessingSettingAction = null;

        /**
         * SyncActionValue newsletterSavedInterestsAction.
         * @member {proto.SyncActionValue.INewsletterSavedInterestsAction|null|undefined} newsletterSavedInterestsAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.newsletterSavedInterestsAction = null;

        /**
         * SyncActionValue aiThreadRenameAction.
         * @member {proto.SyncActionValue.IAiThreadRenameAction|null|undefined} aiThreadRenameAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.aiThreadRenameAction = null;

        /**
         * SyncActionValue interactiveMessageAction.
         * @member {proto.SyncActionValue.IInteractiveMessageAction|null|undefined} interactiveMessageAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.interactiveMessageAction = null;

        /**
         * SyncActionValue settingsSyncAction.
         * @member {proto.SyncActionValue.ISettingsSyncAction|null|undefined} settingsSyncAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.settingsSyncAction = null;

        /**
         * SyncActionValue outContactAction.
         * @member {proto.SyncActionValue.IOutContactAction|null|undefined} outContactAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.outContactAction = null;

        /**
         * SyncActionValue nctSaltSyncAction.
         * @member {proto.SyncActionValue.INctSaltSyncAction|null|undefined} nctSaltSyncAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.nctSaltSyncAction = null;

        /**
         * SyncActionValue businessBroadcastCampaignAction.
         * @member {proto.SyncActionValue.IBusinessBroadcastCampaignAction|null|undefined} businessBroadcastCampaignAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.businessBroadcastCampaignAction = null;

        /**
         * SyncActionValue businessBroadcastInsightsAction.
         * @member {proto.SyncActionValue.IBusinessBroadcastInsightsAction|null|undefined} businessBroadcastInsightsAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.businessBroadcastInsightsAction = null;

        /**
         * SyncActionValue customerDataAction.
         * @member {proto.SyncActionValue.ICustomerDataAction|null|undefined} customerDataAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.customerDataAction = null;

        /**
         * SyncActionValue subscriptionsSyncV2Action.
         * @member {proto.SyncActionValue.ISubscriptionsSyncV2Action|null|undefined} subscriptionsSyncV2Action
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.subscriptionsSyncV2Action = null;

        /**
         * SyncActionValue threadPinAction.
         * @member {proto.SyncActionValue.IThreadPinAction|null|undefined} threadPinAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.threadPinAction = null;

        /**
         * SyncActionValue autoOrganizeBusinessChatSetting.
         * @member {proto.SyncActionValue.IAutoOrganizeBusinessChatSetting|null|undefined} autoOrganizeBusinessChatSetting
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.autoOrganizeBusinessChatSetting = null;

        /**
         * SyncActionValue bizAiSettingsNudgeAction.
         * @member {proto.SyncActionValue.IBizAISettingsNudgeAction|null|undefined} bizAiSettingsNudgeAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.bizAiSettingsNudgeAction = null;

        /**
         * SyncActionValue coexV2VersionAction.
         * @member {proto.SyncActionValue.ICoexV2VersionAction|null|undefined} coexV2VersionAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.coexV2VersionAction = null;

        /**
         * SyncActionValue wasaRootSecretAction.
         * @member {proto.SyncActionValue.IWASARootSecretAction|null|undefined} wasaRootSecretAction
         * @memberof proto.SyncActionValue
         * @instance
         */
        SyncActionValue.prototype.wasaRootSecretAction = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_timestamp", {
            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_starAction", {
            get: $util.oneOfGetter($oneOfFields = ["starAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_contactAction", {
            get: $util.oneOfGetter($oneOfFields = ["contactAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_muteAction", {
            get: $util.oneOfGetter($oneOfFields = ["muteAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_pinAction", {
            get: $util.oneOfGetter($oneOfFields = ["pinAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_pushNameSetting", {
            get: $util.oneOfGetter($oneOfFields = ["pushNameSetting"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_quickReplyAction", {
            get: $util.oneOfGetter($oneOfFields = ["quickReplyAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_recentEmojiWeightsAction", {
            get: $util.oneOfGetter($oneOfFields = ["recentEmojiWeightsAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_labelEditAction", {
            get: $util.oneOfGetter($oneOfFields = ["labelEditAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_labelAssociationAction", {
            get: $util.oneOfGetter($oneOfFields = ["labelAssociationAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_localeSetting", {
            get: $util.oneOfGetter($oneOfFields = ["localeSetting"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_archiveChatAction", {
            get: $util.oneOfGetter($oneOfFields = ["archiveChatAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_deleteMessageForMeAction", {
            get: $util.oneOfGetter($oneOfFields = ["deleteMessageForMeAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_keyExpiration", {
            get: $util.oneOfGetter($oneOfFields = ["keyExpiration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_markChatAsReadAction", {
            get: $util.oneOfGetter($oneOfFields = ["markChatAsReadAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_clearChatAction", {
            get: $util.oneOfGetter($oneOfFields = ["clearChatAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_deleteChatAction", {
            get: $util.oneOfGetter($oneOfFields = ["deleteChatAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_unarchiveChatsSetting", {
            get: $util.oneOfGetter($oneOfFields = ["unarchiveChatsSetting"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_primaryFeature", {
            get: $util.oneOfGetter($oneOfFields = ["primaryFeature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_androidUnsupportedActions", {
            get: $util.oneOfGetter($oneOfFields = ["androidUnsupportedActions"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_agentAction", {
            get: $util.oneOfGetter($oneOfFields = ["agentAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_subscriptionAction", {
            get: $util.oneOfGetter($oneOfFields = ["subscriptionAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_userStatusMuteAction", {
            get: $util.oneOfGetter($oneOfFields = ["userStatusMuteAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_timeFormatAction", {
            get: $util.oneOfGetter($oneOfFields = ["timeFormatAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_nuxAction", {
            get: $util.oneOfGetter($oneOfFields = ["nuxAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_primaryVersionAction", {
            get: $util.oneOfGetter($oneOfFields = ["primaryVersionAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_stickerAction", {
            get: $util.oneOfGetter($oneOfFields = ["stickerAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_removeRecentStickerAction", {
            get: $util.oneOfGetter($oneOfFields = ["removeRecentStickerAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_chatAssignment", {
            get: $util.oneOfGetter($oneOfFields = ["chatAssignment"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_chatAssignmentOpenedStatus", {
            get: $util.oneOfGetter($oneOfFields = ["chatAssignmentOpenedStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_pnForLidChatAction", {
            get: $util.oneOfGetter($oneOfFields = ["pnForLidChatAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_marketingMessageAction", {
            get: $util.oneOfGetter($oneOfFields = ["marketingMessageAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_marketingMessageBroadcastAction", {
            get: $util.oneOfGetter($oneOfFields = ["marketingMessageBroadcastAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_externalWebBetaAction", {
            get: $util.oneOfGetter($oneOfFields = ["externalWebBetaAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_privacySettingRelayAllCalls", {
            get: $util.oneOfGetter($oneOfFields = ["privacySettingRelayAllCalls"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_callLogAction", {
            get: $util.oneOfGetter($oneOfFields = ["callLogAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_ugcBot", {
            get: $util.oneOfGetter($oneOfFields = ["ugcBot"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_statusPrivacy", {
            get: $util.oneOfGetter($oneOfFields = ["statusPrivacy"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_botWelcomeRequestAction", {
            get: $util.oneOfGetter($oneOfFields = ["botWelcomeRequestAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_deleteIndividualCallLog", {
            get: $util.oneOfGetter($oneOfFields = ["deleteIndividualCallLog"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_labelReorderingAction", {
            get: $util.oneOfGetter($oneOfFields = ["labelReorderingAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_paymentInfoAction", {
            get: $util.oneOfGetter($oneOfFields = ["paymentInfoAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_customPaymentMethodsAction", {
            get: $util.oneOfGetter($oneOfFields = ["customPaymentMethodsAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_lockChatAction", {
            get: $util.oneOfGetter($oneOfFields = ["lockChatAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_chatLockSettings", {
            get: $util.oneOfGetter($oneOfFields = ["chatLockSettings"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_wamoUserIdentifierAction", {
            get: $util.oneOfGetter($oneOfFields = ["wamoUserIdentifierAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_privacySettingDisableLinkPreviewsAction", {
            get: $util.oneOfGetter($oneOfFields = ["privacySettingDisableLinkPreviewsAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_deviceCapabilities", {
            get: $util.oneOfGetter($oneOfFields = ["deviceCapabilities"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_noteEditAction", {
            get: $util.oneOfGetter($oneOfFields = ["noteEditAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_favoritesAction", {
            get: $util.oneOfGetter($oneOfFields = ["favoritesAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_merchantPaymentPartnerAction", {
            get: $util.oneOfGetter($oneOfFields = ["merchantPaymentPartnerAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_waffleAccountLinkStateAction", {
            get: $util.oneOfGetter($oneOfFields = ["waffleAccountLinkStateAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_usernameChatStartMode", {
            get: $util.oneOfGetter($oneOfFields = ["usernameChatStartMode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_notificationActivitySettingAction", {
            get: $util.oneOfGetter($oneOfFields = ["notificationActivitySettingAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_lidContactAction", {
            get: $util.oneOfGetter($oneOfFields = ["lidContactAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_ctwaPerCustomerDataSharingAction", {
            get: $util.oneOfGetter($oneOfFields = ["ctwaPerCustomerDataSharingAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_paymentTosAction", {
            get: $util.oneOfGetter($oneOfFields = ["paymentTosAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_privacySettingChannelsPersonalisedRecommendationAction", {
            get: $util.oneOfGetter($oneOfFields = ["privacySettingChannelsPersonalisedRecommendationAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_detectedOutcomesStatusAction", {
            get: $util.oneOfGetter($oneOfFields = ["detectedOutcomesStatusAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_maibaAiFeaturesControlAction", {
            get: $util.oneOfGetter($oneOfFields = ["maibaAiFeaturesControlAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_businessBroadcastListAction", {
            get: $util.oneOfGetter($oneOfFields = ["businessBroadcastListAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_musicUserIdAction", {
            get: $util.oneOfGetter($oneOfFields = ["musicUserIdAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_statusPostOptInNotificationPreferencesAction", {
            get: $util.oneOfGetter($oneOfFields = ["statusPostOptInNotificationPreferencesAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_avatarUpdatedAction", {
            get: $util.oneOfGetter($oneOfFields = ["avatarUpdatedAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_privateProcessingSettingAction", {
            get: $util.oneOfGetter($oneOfFields = ["privateProcessingSettingAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_newsletterSavedInterestsAction", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterSavedInterestsAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_aiThreadRenameAction", {
            get: $util.oneOfGetter($oneOfFields = ["aiThreadRenameAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_interactiveMessageAction", {
            get: $util.oneOfGetter($oneOfFields = ["interactiveMessageAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_settingsSyncAction", {
            get: $util.oneOfGetter($oneOfFields = ["settingsSyncAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_outContactAction", {
            get: $util.oneOfGetter($oneOfFields = ["outContactAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_nctSaltSyncAction", {
            get: $util.oneOfGetter($oneOfFields = ["nctSaltSyncAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_businessBroadcastCampaignAction", {
            get: $util.oneOfGetter($oneOfFields = ["businessBroadcastCampaignAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_businessBroadcastInsightsAction", {
            get: $util.oneOfGetter($oneOfFields = ["businessBroadcastInsightsAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_customerDataAction", {
            get: $util.oneOfGetter($oneOfFields = ["customerDataAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_subscriptionsSyncV2Action", {
            get: $util.oneOfGetter($oneOfFields = ["subscriptionsSyncV2Action"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_threadPinAction", {
            get: $util.oneOfGetter($oneOfFields = ["threadPinAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_autoOrganizeBusinessChatSetting", {
            get: $util.oneOfGetter($oneOfFields = ["autoOrganizeBusinessChatSetting"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_bizAiSettingsNudgeAction", {
            get: $util.oneOfGetter($oneOfFields = ["bizAiSettingsNudgeAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_coexV2VersionAction", {
            get: $util.oneOfGetter($oneOfFields = ["coexV2VersionAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncActionValue.prototype, "_wasaRootSecretAction", {
            get: $util.oneOfGetter($oneOfFields = ["wasaRootSecretAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SyncActionValue instance using the specified properties.
         * @function create
         * @memberof proto.SyncActionValue
         * @static
         * @param {proto.ISyncActionValue=} [properties] Properties to set
         * @returns {proto.SyncActionValue} SyncActionValue instance
         */
        SyncActionValue.create = function create(properties) {
            return new SyncActionValue(properties);
        };

        /**
         * Encodes the specified SyncActionValue message. Does not implicitly {@link proto.SyncActionValue.verify|verify} messages.
         * @function encode
         * @memberof proto.SyncActionValue
         * @static
         * @param {proto.ISyncActionValue} message SyncActionValue message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SyncActionValue.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.timestamp);
            if (message.starAction != null && Object.hasOwnProperty.call(message, "starAction"))
                $root.proto.SyncActionValue.StarAction.encode(message.starAction, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.contactAction != null && Object.hasOwnProperty.call(message, "contactAction"))
                $root.proto.SyncActionValue.ContactAction.encode(message.contactAction, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.muteAction != null && Object.hasOwnProperty.call(message, "muteAction"))
                $root.proto.SyncActionValue.MuteAction.encode(message.muteAction, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.pinAction != null && Object.hasOwnProperty.call(message, "pinAction"))
                $root.proto.SyncActionValue.PinAction.encode(message.pinAction, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
            if (message.pushNameSetting != null && Object.hasOwnProperty.call(message, "pushNameSetting"))
                $root.proto.SyncActionValue.PushNameSetting.encode(message.pushNameSetting, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
            if (message.quickReplyAction != null && Object.hasOwnProperty.call(message, "quickReplyAction"))
                $root.proto.SyncActionValue.QuickReplyAction.encode(message.quickReplyAction, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
            if (message.recentEmojiWeightsAction != null && Object.hasOwnProperty.call(message, "recentEmojiWeightsAction"))
                $root.proto.SyncActionValue.RecentEmojiWeightsAction.encode(message.recentEmojiWeightsAction, writer.uint32(/* id 11, wireType 2 =*/90).fork(), q + 1).ldelim();
            if (message.labelEditAction != null && Object.hasOwnProperty.call(message, "labelEditAction"))
                $root.proto.SyncActionValue.LabelEditAction.encode(message.labelEditAction, writer.uint32(/* id 14, wireType 2 =*/114).fork(), q + 1).ldelim();
            if (message.labelAssociationAction != null && Object.hasOwnProperty.call(message, "labelAssociationAction"))
                $root.proto.SyncActionValue.LabelAssociationAction.encode(message.labelAssociationAction, writer.uint32(/* id 15, wireType 2 =*/122).fork(), q + 1).ldelim();
            if (message.localeSetting != null && Object.hasOwnProperty.call(message, "localeSetting"))
                $root.proto.SyncActionValue.LocaleSetting.encode(message.localeSetting, writer.uint32(/* id 16, wireType 2 =*/130).fork(), q + 1).ldelim();
            if (message.archiveChatAction != null && Object.hasOwnProperty.call(message, "archiveChatAction"))
                $root.proto.SyncActionValue.ArchiveChatAction.encode(message.archiveChatAction, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
            if (message.deleteMessageForMeAction != null && Object.hasOwnProperty.call(message, "deleteMessageForMeAction"))
                $root.proto.SyncActionValue.DeleteMessageForMeAction.encode(message.deleteMessageForMeAction, writer.uint32(/* id 18, wireType 2 =*/146).fork(), q + 1).ldelim();
            if (message.keyExpiration != null && Object.hasOwnProperty.call(message, "keyExpiration"))
                $root.proto.SyncActionValue.KeyExpiration.encode(message.keyExpiration, writer.uint32(/* id 19, wireType 2 =*/154).fork(), q + 1).ldelim();
            if (message.markChatAsReadAction != null && Object.hasOwnProperty.call(message, "markChatAsReadAction"))
                $root.proto.SyncActionValue.MarkChatAsReadAction.encode(message.markChatAsReadAction, writer.uint32(/* id 20, wireType 2 =*/162).fork(), q + 1).ldelim();
            if (message.clearChatAction != null && Object.hasOwnProperty.call(message, "clearChatAction"))
                $root.proto.SyncActionValue.ClearChatAction.encode(message.clearChatAction, writer.uint32(/* id 21, wireType 2 =*/170).fork(), q + 1).ldelim();
            if (message.deleteChatAction != null && Object.hasOwnProperty.call(message, "deleteChatAction"))
                $root.proto.SyncActionValue.DeleteChatAction.encode(message.deleteChatAction, writer.uint32(/* id 22, wireType 2 =*/178).fork(), q + 1).ldelim();
            if (message.unarchiveChatsSetting != null && Object.hasOwnProperty.call(message, "unarchiveChatsSetting"))
                $root.proto.SyncActionValue.UnarchiveChatsSetting.encode(message.unarchiveChatsSetting, writer.uint32(/* id 23, wireType 2 =*/186).fork(), q + 1).ldelim();
            if (message.primaryFeature != null && Object.hasOwnProperty.call(message, "primaryFeature"))
                $root.proto.SyncActionValue.PrimaryFeature.encode(message.primaryFeature, writer.uint32(/* id 24, wireType 2 =*/194).fork(), q + 1).ldelim();
            if (message.androidUnsupportedActions != null && Object.hasOwnProperty.call(message, "androidUnsupportedActions"))
                $root.proto.SyncActionValue.AndroidUnsupportedActions.encode(message.androidUnsupportedActions, writer.uint32(/* id 26, wireType 2 =*/210).fork(), q + 1).ldelim();
            if (message.agentAction != null && Object.hasOwnProperty.call(message, "agentAction"))
                $root.proto.SyncActionValue.AgentAction.encode(message.agentAction, writer.uint32(/* id 27, wireType 2 =*/218).fork(), q + 1).ldelim();
            if (message.subscriptionAction != null && Object.hasOwnProperty.call(message, "subscriptionAction"))
                $root.proto.SyncActionValue.SubscriptionAction.encode(message.subscriptionAction, writer.uint32(/* id 28, wireType 2 =*/226).fork(), q + 1).ldelim();
            if (message.userStatusMuteAction != null && Object.hasOwnProperty.call(message, "userStatusMuteAction"))
                $root.proto.SyncActionValue.UserStatusMuteAction.encode(message.userStatusMuteAction, writer.uint32(/* id 29, wireType 2 =*/234).fork(), q + 1).ldelim();
            if (message.timeFormatAction != null && Object.hasOwnProperty.call(message, "timeFormatAction"))
                $root.proto.SyncActionValue.TimeFormatAction.encode(message.timeFormatAction, writer.uint32(/* id 30, wireType 2 =*/242).fork(), q + 1).ldelim();
            if (message.nuxAction != null && Object.hasOwnProperty.call(message, "nuxAction"))
                $root.proto.SyncActionValue.NuxAction.encode(message.nuxAction, writer.uint32(/* id 31, wireType 2 =*/250).fork(), q + 1).ldelim();
            if (message.primaryVersionAction != null && Object.hasOwnProperty.call(message, "primaryVersionAction"))
                $root.proto.SyncActionValue.PrimaryVersionAction.encode(message.primaryVersionAction, writer.uint32(/* id 32, wireType 2 =*/258).fork(), q + 1).ldelim();
            if (message.stickerAction != null && Object.hasOwnProperty.call(message, "stickerAction"))
                $root.proto.SyncActionValue.StickerAction.encode(message.stickerAction, writer.uint32(/* id 33, wireType 2 =*/266).fork(), q + 1).ldelim();
            if (message.removeRecentStickerAction != null && Object.hasOwnProperty.call(message, "removeRecentStickerAction"))
                $root.proto.SyncActionValue.RemoveRecentStickerAction.encode(message.removeRecentStickerAction, writer.uint32(/* id 34, wireType 2 =*/274).fork(), q + 1).ldelim();
            if (message.chatAssignment != null && Object.hasOwnProperty.call(message, "chatAssignment"))
                $root.proto.SyncActionValue.ChatAssignmentAction.encode(message.chatAssignment, writer.uint32(/* id 35, wireType 2 =*/282).fork(), q + 1).ldelim();
            if (message.chatAssignmentOpenedStatus != null && Object.hasOwnProperty.call(message, "chatAssignmentOpenedStatus"))
                $root.proto.SyncActionValue.ChatAssignmentOpenedStatusAction.encode(message.chatAssignmentOpenedStatus, writer.uint32(/* id 36, wireType 2 =*/290).fork(), q + 1).ldelim();
            if (message.pnForLidChatAction != null && Object.hasOwnProperty.call(message, "pnForLidChatAction"))
                $root.proto.SyncActionValue.PnForLidChatAction.encode(message.pnForLidChatAction, writer.uint32(/* id 37, wireType 2 =*/298).fork(), q + 1).ldelim();
            if (message.marketingMessageAction != null && Object.hasOwnProperty.call(message, "marketingMessageAction"))
                $root.proto.SyncActionValue.MarketingMessageAction.encode(message.marketingMessageAction, writer.uint32(/* id 38, wireType 2 =*/306).fork(), q + 1).ldelim();
            if (message.marketingMessageBroadcastAction != null && Object.hasOwnProperty.call(message, "marketingMessageBroadcastAction"))
                $root.proto.SyncActionValue.MarketingMessageBroadcastAction.encode(message.marketingMessageBroadcastAction, writer.uint32(/* id 39, wireType 2 =*/314).fork(), q + 1).ldelim();
            if (message.externalWebBetaAction != null && Object.hasOwnProperty.call(message, "externalWebBetaAction"))
                $root.proto.SyncActionValue.ExternalWebBetaAction.encode(message.externalWebBetaAction, writer.uint32(/* id 40, wireType 2 =*/322).fork(), q + 1).ldelim();
            if (message.privacySettingRelayAllCalls != null && Object.hasOwnProperty.call(message, "privacySettingRelayAllCalls"))
                $root.proto.SyncActionValue.PrivacySettingRelayAllCalls.encode(message.privacySettingRelayAllCalls, writer.uint32(/* id 41, wireType 2 =*/330).fork(), q + 1).ldelim();
            if (message.callLogAction != null && Object.hasOwnProperty.call(message, "callLogAction"))
                $root.proto.SyncActionValue.CallLogAction.encode(message.callLogAction, writer.uint32(/* id 42, wireType 2 =*/338).fork(), q + 1).ldelim();
            if (message.ugcBot != null && Object.hasOwnProperty.call(message, "ugcBot"))
                $root.proto.SyncActionValue.UGCBot.encode(message.ugcBot, writer.uint32(/* id 43, wireType 2 =*/346).fork(), q + 1).ldelim();
            if (message.statusPrivacy != null && Object.hasOwnProperty.call(message, "statusPrivacy"))
                $root.proto.SyncActionValue.StatusPrivacyAction.encode(message.statusPrivacy, writer.uint32(/* id 44, wireType 2 =*/354).fork(), q + 1).ldelim();
            if (message.botWelcomeRequestAction != null && Object.hasOwnProperty.call(message, "botWelcomeRequestAction"))
                $root.proto.SyncActionValue.BotWelcomeRequestAction.encode(message.botWelcomeRequestAction, writer.uint32(/* id 45, wireType 2 =*/362).fork(), q + 1).ldelim();
            if (message.deleteIndividualCallLog != null && Object.hasOwnProperty.call(message, "deleteIndividualCallLog"))
                $root.proto.SyncActionValue.DeleteIndividualCallLogAction.encode(message.deleteIndividualCallLog, writer.uint32(/* id 46, wireType 2 =*/370).fork(), q + 1).ldelim();
            if (message.labelReorderingAction != null && Object.hasOwnProperty.call(message, "labelReorderingAction"))
                $root.proto.SyncActionValue.LabelReorderingAction.encode(message.labelReorderingAction, writer.uint32(/* id 47, wireType 2 =*/378).fork(), q + 1).ldelim();
            if (message.paymentInfoAction != null && Object.hasOwnProperty.call(message, "paymentInfoAction"))
                $root.proto.SyncActionValue.PaymentInfoAction.encode(message.paymentInfoAction, writer.uint32(/* id 48, wireType 2 =*/386).fork(), q + 1).ldelim();
            if (message.customPaymentMethodsAction != null && Object.hasOwnProperty.call(message, "customPaymentMethodsAction"))
                $root.proto.SyncActionValue.CustomPaymentMethodsAction.encode(message.customPaymentMethodsAction, writer.uint32(/* id 49, wireType 2 =*/394).fork(), q + 1).ldelim();
            if (message.lockChatAction != null && Object.hasOwnProperty.call(message, "lockChatAction"))
                $root.proto.SyncActionValue.LockChatAction.encode(message.lockChatAction, writer.uint32(/* id 50, wireType 2 =*/402).fork(), q + 1).ldelim();
            if (message.chatLockSettings != null && Object.hasOwnProperty.call(message, "chatLockSettings"))
                $root.proto.ChatLockSettings.encode(message.chatLockSettings, writer.uint32(/* id 51, wireType 2 =*/410).fork(), q + 1).ldelim();
            if (message.wamoUserIdentifierAction != null && Object.hasOwnProperty.call(message, "wamoUserIdentifierAction"))
                $root.proto.SyncActionValue.WamoUserIdentifierAction.encode(message.wamoUserIdentifierAction, writer.uint32(/* id 52, wireType 2 =*/418).fork(), q + 1).ldelim();
            if (message.privacySettingDisableLinkPreviewsAction != null && Object.hasOwnProperty.call(message, "privacySettingDisableLinkPreviewsAction"))
                $root.proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction.encode(message.privacySettingDisableLinkPreviewsAction, writer.uint32(/* id 53, wireType 2 =*/426).fork(), q + 1).ldelim();
            if (message.deviceCapabilities != null && Object.hasOwnProperty.call(message, "deviceCapabilities"))
                $root.proto.DeviceCapabilities.encode(message.deviceCapabilities, writer.uint32(/* id 54, wireType 2 =*/434).fork(), q + 1).ldelim();
            if (message.noteEditAction != null && Object.hasOwnProperty.call(message, "noteEditAction"))
                $root.proto.SyncActionValue.NoteEditAction.encode(message.noteEditAction, writer.uint32(/* id 55, wireType 2 =*/442).fork(), q + 1).ldelim();
            if (message.favoritesAction != null && Object.hasOwnProperty.call(message, "favoritesAction"))
                $root.proto.SyncActionValue.FavoritesAction.encode(message.favoritesAction, writer.uint32(/* id 56, wireType 2 =*/450).fork(), q + 1).ldelim();
            if (message.merchantPaymentPartnerAction != null && Object.hasOwnProperty.call(message, "merchantPaymentPartnerAction"))
                $root.proto.SyncActionValue.MerchantPaymentPartnerAction.encode(message.merchantPaymentPartnerAction, writer.uint32(/* id 57, wireType 2 =*/458).fork(), q + 1).ldelim();
            if (message.waffleAccountLinkStateAction != null && Object.hasOwnProperty.call(message, "waffleAccountLinkStateAction"))
                $root.proto.SyncActionValue.WaffleAccountLinkStateAction.encode(message.waffleAccountLinkStateAction, writer.uint32(/* id 58, wireType 2 =*/466).fork(), q + 1).ldelim();
            if (message.usernameChatStartMode != null && Object.hasOwnProperty.call(message, "usernameChatStartMode"))
                $root.proto.SyncActionValue.UsernameChatStartModeAction.encode(message.usernameChatStartMode, writer.uint32(/* id 59, wireType 2 =*/474).fork(), q + 1).ldelim();
            if (message.notificationActivitySettingAction != null && Object.hasOwnProperty.call(message, "notificationActivitySettingAction"))
                $root.proto.SyncActionValue.NotificationActivitySettingAction.encode(message.notificationActivitySettingAction, writer.uint32(/* id 60, wireType 2 =*/482).fork(), q + 1).ldelim();
            if (message.lidContactAction != null && Object.hasOwnProperty.call(message, "lidContactAction"))
                $root.proto.SyncActionValue.LidContactAction.encode(message.lidContactAction, writer.uint32(/* id 61, wireType 2 =*/490).fork(), q + 1).ldelim();
            if (message.ctwaPerCustomerDataSharingAction != null && Object.hasOwnProperty.call(message, "ctwaPerCustomerDataSharingAction"))
                $root.proto.SyncActionValue.CtwaPerCustomerDataSharingAction.encode(message.ctwaPerCustomerDataSharingAction, writer.uint32(/* id 62, wireType 2 =*/498).fork(), q + 1).ldelim();
            if (message.paymentTosAction != null && Object.hasOwnProperty.call(message, "paymentTosAction"))
                $root.proto.SyncActionValue.PaymentTosAction.encode(message.paymentTosAction, writer.uint32(/* id 63, wireType 2 =*/506).fork(), q + 1).ldelim();
            if (message.privacySettingChannelsPersonalisedRecommendationAction != null && Object.hasOwnProperty.call(message, "privacySettingChannelsPersonalisedRecommendationAction"))
                $root.proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction.encode(message.privacySettingChannelsPersonalisedRecommendationAction, writer.uint32(/* id 64, wireType 2 =*/514).fork(), q + 1).ldelim();
            if (message.detectedOutcomesStatusAction != null && Object.hasOwnProperty.call(message, "detectedOutcomesStatusAction"))
                $root.proto.SyncActionValue.DetectedOutcomesStatusAction.encode(message.detectedOutcomesStatusAction, writer.uint32(/* id 66, wireType 2 =*/530).fork(), q + 1).ldelim();
            if (message.maibaAiFeaturesControlAction != null && Object.hasOwnProperty.call(message, "maibaAiFeaturesControlAction"))
                $root.proto.SyncActionValue.MaibaAIFeaturesControlAction.encode(message.maibaAiFeaturesControlAction, writer.uint32(/* id 68, wireType 2 =*/546).fork(), q + 1).ldelim();
            if (message.businessBroadcastListAction != null && Object.hasOwnProperty.call(message, "businessBroadcastListAction"))
                $root.proto.SyncActionValue.BusinessBroadcastListAction.encode(message.businessBroadcastListAction, writer.uint32(/* id 69, wireType 2 =*/554).fork(), q + 1).ldelim();
            if (message.musicUserIdAction != null && Object.hasOwnProperty.call(message, "musicUserIdAction"))
                $root.proto.SyncActionValue.MusicUserIdAction.encode(message.musicUserIdAction, writer.uint32(/* id 70, wireType 2 =*/562).fork(), q + 1).ldelim();
            if (message.statusPostOptInNotificationPreferencesAction != null && Object.hasOwnProperty.call(message, "statusPostOptInNotificationPreferencesAction"))
                $root.proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction.encode(message.statusPostOptInNotificationPreferencesAction, writer.uint32(/* id 71, wireType 2 =*/570).fork(), q + 1).ldelim();
            if (message.avatarUpdatedAction != null && Object.hasOwnProperty.call(message, "avatarUpdatedAction"))
                $root.proto.SyncActionValue.AvatarUpdatedAction.encode(message.avatarUpdatedAction, writer.uint32(/* id 72, wireType 2 =*/578).fork(), q + 1).ldelim();
            if (message.privateProcessingSettingAction != null && Object.hasOwnProperty.call(message, "privateProcessingSettingAction"))
                $root.proto.SyncActionValue.PrivateProcessingSettingAction.encode(message.privateProcessingSettingAction, writer.uint32(/* id 74, wireType 2 =*/594).fork(), q + 1).ldelim();
            if (message.newsletterSavedInterestsAction != null && Object.hasOwnProperty.call(message, "newsletterSavedInterestsAction"))
                $root.proto.SyncActionValue.NewsletterSavedInterestsAction.encode(message.newsletterSavedInterestsAction, writer.uint32(/* id 75, wireType 2 =*/602).fork(), q + 1).ldelim();
            if (message.aiThreadRenameAction != null && Object.hasOwnProperty.call(message, "aiThreadRenameAction"))
                $root.proto.SyncActionValue.AiThreadRenameAction.encode(message.aiThreadRenameAction, writer.uint32(/* id 76, wireType 2 =*/610).fork(), q + 1).ldelim();
            if (message.interactiveMessageAction != null && Object.hasOwnProperty.call(message, "interactiveMessageAction"))
                $root.proto.SyncActionValue.InteractiveMessageAction.encode(message.interactiveMessageAction, writer.uint32(/* id 77, wireType 2 =*/618).fork(), q + 1).ldelim();
            if (message.settingsSyncAction != null && Object.hasOwnProperty.call(message, "settingsSyncAction"))
                $root.proto.SyncActionValue.SettingsSyncAction.encode(message.settingsSyncAction, writer.uint32(/* id 78, wireType 2 =*/626).fork(), q + 1).ldelim();
            if (message.outContactAction != null && Object.hasOwnProperty.call(message, "outContactAction"))
                $root.proto.SyncActionValue.OutContactAction.encode(message.outContactAction, writer.uint32(/* id 79, wireType 2 =*/634).fork(), q + 1).ldelim();
            if (message.nctSaltSyncAction != null && Object.hasOwnProperty.call(message, "nctSaltSyncAction"))
                $root.proto.SyncActionValue.NctSaltSyncAction.encode(message.nctSaltSyncAction, writer.uint32(/* id 80, wireType 2 =*/642).fork(), q + 1).ldelim();
            if (message.businessBroadcastCampaignAction != null && Object.hasOwnProperty.call(message, "businessBroadcastCampaignAction"))
                $root.proto.SyncActionValue.BusinessBroadcastCampaignAction.encode(message.businessBroadcastCampaignAction, writer.uint32(/* id 81, wireType 2 =*/650).fork(), q + 1).ldelim();
            if (message.businessBroadcastInsightsAction != null && Object.hasOwnProperty.call(message, "businessBroadcastInsightsAction"))
                $root.proto.SyncActionValue.BusinessBroadcastInsightsAction.encode(message.businessBroadcastInsightsAction, writer.uint32(/* id 82, wireType 2 =*/658).fork(), q + 1).ldelim();
            if (message.customerDataAction != null && Object.hasOwnProperty.call(message, "customerDataAction"))
                $root.proto.SyncActionValue.CustomerDataAction.encode(message.customerDataAction, writer.uint32(/* id 83, wireType 2 =*/666).fork(), q + 1).ldelim();
            if (message.subscriptionsSyncV2Action != null && Object.hasOwnProperty.call(message, "subscriptionsSyncV2Action"))
                $root.proto.SyncActionValue.SubscriptionsSyncV2Action.encode(message.subscriptionsSyncV2Action, writer.uint32(/* id 84, wireType 2 =*/674).fork(), q + 1).ldelim();
            if (message.threadPinAction != null && Object.hasOwnProperty.call(message, "threadPinAction"))
                $root.proto.SyncActionValue.ThreadPinAction.encode(message.threadPinAction, writer.uint32(/* id 85, wireType 2 =*/682).fork(), q + 1).ldelim();
            if (message.autoOrganizeBusinessChatSetting != null && Object.hasOwnProperty.call(message, "autoOrganizeBusinessChatSetting"))
                $root.proto.SyncActionValue.AutoOrganizeBusinessChatSetting.encode(message.autoOrganizeBusinessChatSetting, writer.uint32(/* id 86, wireType 2 =*/690).fork(), q + 1).ldelim();
            if (message.bizAiSettingsNudgeAction != null && Object.hasOwnProperty.call(message, "bizAiSettingsNudgeAction"))
                $root.proto.SyncActionValue.BizAISettingsNudgeAction.encode(message.bizAiSettingsNudgeAction, writer.uint32(/* id 87, wireType 2 =*/698).fork(), q + 1).ldelim();
            if (message.coexV2VersionAction != null && Object.hasOwnProperty.call(message, "coexV2VersionAction"))
                $root.proto.SyncActionValue.CoexV2VersionAction.encode(message.coexV2VersionAction, writer.uint32(/* id 88, wireType 2 =*/706).fork(), q + 1).ldelim();
            if (message.wasaRootSecretAction != null && Object.hasOwnProperty.call(message, "wasaRootSecretAction"))
                $root.proto.SyncActionValue.WASARootSecretAction.encode(message.wasaRootSecretAction, writer.uint32(/* id 89, wireType 2 =*/714).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SyncActionValue message, length delimited. Does not implicitly {@link proto.SyncActionValue.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SyncActionValue
         * @static
         * @param {proto.ISyncActionValue} message SyncActionValue message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SyncActionValue.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SyncActionValue message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SyncActionValue
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SyncActionValue} SyncActionValue
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SyncActionValue.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.timestamp = reader.int64();
                        message._timestamp = "timestamp";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.starAction = $root.proto.SyncActionValue.StarAction.decode(reader, reader.uint32(), undefined, q + 1, message.starAction);
                        message._starAction = "starAction";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.contactAction = $root.proto.SyncActionValue.ContactAction.decode(reader, reader.uint32(), undefined, q + 1, message.contactAction);
                        message._contactAction = "contactAction";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.muteAction = $root.proto.SyncActionValue.MuteAction.decode(reader, reader.uint32(), undefined, q + 1, message.muteAction);
                        message._muteAction = "muteAction";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.pinAction = $root.proto.SyncActionValue.PinAction.decode(reader, reader.uint32(), undefined, q + 1, message.pinAction);
                        message._pinAction = "pinAction";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        message.pushNameSetting = $root.proto.SyncActionValue.PushNameSetting.decode(reader, reader.uint32(), undefined, q + 1, message.pushNameSetting);
                        message._pushNameSetting = "pushNameSetting";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        message.quickReplyAction = $root.proto.SyncActionValue.QuickReplyAction.decode(reader, reader.uint32(), undefined, q + 1, message.quickReplyAction);
                        message._quickReplyAction = "quickReplyAction";
                        continue;
                    }
                case 11: {
                        if (u !== 2)
                            break;
                        message.recentEmojiWeightsAction = $root.proto.SyncActionValue.RecentEmojiWeightsAction.decode(reader, reader.uint32(), undefined, q + 1, message.recentEmojiWeightsAction);
                        message._recentEmojiWeightsAction = "recentEmojiWeightsAction";
                        continue;
                    }
                case 14: {
                        if (u !== 2)
                            break;
                        message.labelEditAction = $root.proto.SyncActionValue.LabelEditAction.decode(reader, reader.uint32(), undefined, q + 1, message.labelEditAction);
                        message._labelEditAction = "labelEditAction";
                        continue;
                    }
                case 15: {
                        if (u !== 2)
                            break;
                        message.labelAssociationAction = $root.proto.SyncActionValue.LabelAssociationAction.decode(reader, reader.uint32(), undefined, q + 1, message.labelAssociationAction);
                        message._labelAssociationAction = "labelAssociationAction";
                        continue;
                    }
                case 16: {
                        if (u !== 2)
                            break;
                        message.localeSetting = $root.proto.SyncActionValue.LocaleSetting.decode(reader, reader.uint32(), undefined, q + 1, message.localeSetting);
                        message._localeSetting = "localeSetting";
                        continue;
                    }
                case 17: {
                        if (u !== 2)
                            break;
                        message.archiveChatAction = $root.proto.SyncActionValue.ArchiveChatAction.decode(reader, reader.uint32(), undefined, q + 1, message.archiveChatAction);
                        message._archiveChatAction = "archiveChatAction";
                        continue;
                    }
                case 18: {
                        if (u !== 2)
                            break;
                        message.deleteMessageForMeAction = $root.proto.SyncActionValue.DeleteMessageForMeAction.decode(reader, reader.uint32(), undefined, q + 1, message.deleteMessageForMeAction);
                        message._deleteMessageForMeAction = "deleteMessageForMeAction";
                        continue;
                    }
                case 19: {
                        if (u !== 2)
                            break;
                        message.keyExpiration = $root.proto.SyncActionValue.KeyExpiration.decode(reader, reader.uint32(), undefined, q + 1, message.keyExpiration);
                        message._keyExpiration = "keyExpiration";
                        continue;
                    }
                case 20: {
                        if (u !== 2)
                            break;
                        message.markChatAsReadAction = $root.proto.SyncActionValue.MarkChatAsReadAction.decode(reader, reader.uint32(), undefined, q + 1, message.markChatAsReadAction);
                        message._markChatAsReadAction = "markChatAsReadAction";
                        continue;
                    }
                case 21: {
                        if (u !== 2)
                            break;
                        message.clearChatAction = $root.proto.SyncActionValue.ClearChatAction.decode(reader, reader.uint32(), undefined, q + 1, message.clearChatAction);
                        message._clearChatAction = "clearChatAction";
                        continue;
                    }
                case 22: {
                        if (u !== 2)
                            break;
                        message.deleteChatAction = $root.proto.SyncActionValue.DeleteChatAction.decode(reader, reader.uint32(), undefined, q + 1, message.deleteChatAction);
                        message._deleteChatAction = "deleteChatAction";
                        continue;
                    }
                case 23: {
                        if (u !== 2)
                            break;
                        message.unarchiveChatsSetting = $root.proto.SyncActionValue.UnarchiveChatsSetting.decode(reader, reader.uint32(), undefined, q + 1, message.unarchiveChatsSetting);
                        message._unarchiveChatsSetting = "unarchiveChatsSetting";
                        continue;
                    }
                case 24: {
                        if (u !== 2)
                            break;
                        message.primaryFeature = $root.proto.SyncActionValue.PrimaryFeature.decode(reader, reader.uint32(), undefined, q + 1, message.primaryFeature);
                        message._primaryFeature = "primaryFeature";
                        continue;
                    }
                case 26: {
                        if (u !== 2)
                            break;
                        message.androidUnsupportedActions = $root.proto.SyncActionValue.AndroidUnsupportedActions.decode(reader, reader.uint32(), undefined, q + 1, message.androidUnsupportedActions);
                        message._androidUnsupportedActions = "androidUnsupportedActions";
                        continue;
                    }
                case 27: {
                        if (u !== 2)
                            break;
                        message.agentAction = $root.proto.SyncActionValue.AgentAction.decode(reader, reader.uint32(), undefined, q + 1, message.agentAction);
                        message._agentAction = "agentAction";
                        continue;
                    }
                case 28: {
                        if (u !== 2)
                            break;
                        message.subscriptionAction = $root.proto.SyncActionValue.SubscriptionAction.decode(reader, reader.uint32(), undefined, q + 1, message.subscriptionAction);
                        message._subscriptionAction = "subscriptionAction";
                        continue;
                    }
                case 29: {
                        if (u !== 2)
                            break;
                        message.userStatusMuteAction = $root.proto.SyncActionValue.UserStatusMuteAction.decode(reader, reader.uint32(), undefined, q + 1, message.userStatusMuteAction);
                        message._userStatusMuteAction = "userStatusMuteAction";
                        continue;
                    }
                case 30: {
                        if (u !== 2)
                            break;
                        message.timeFormatAction = $root.proto.SyncActionValue.TimeFormatAction.decode(reader, reader.uint32(), undefined, q + 1, message.timeFormatAction);
                        message._timeFormatAction = "timeFormatAction";
                        continue;
                    }
                case 31: {
                        if (u !== 2)
                            break;
                        message.nuxAction = $root.proto.SyncActionValue.NuxAction.decode(reader, reader.uint32(), undefined, q + 1, message.nuxAction);
                        message._nuxAction = "nuxAction";
                        continue;
                    }
                case 32: {
                        if (u !== 2)
                            break;
                        message.primaryVersionAction = $root.proto.SyncActionValue.PrimaryVersionAction.decode(reader, reader.uint32(), undefined, q + 1, message.primaryVersionAction);
                        message._primaryVersionAction = "primaryVersionAction";
                        continue;
                    }
                case 33: {
                        if (u !== 2)
                            break;
                        message.stickerAction = $root.proto.SyncActionValue.StickerAction.decode(reader, reader.uint32(), undefined, q + 1, message.stickerAction);
                        message._stickerAction = "stickerAction";
                        continue;
                    }
                case 34: {
                        if (u !== 2)
                            break;
                        message.removeRecentStickerAction = $root.proto.SyncActionValue.RemoveRecentStickerAction.decode(reader, reader.uint32(), undefined, q + 1, message.removeRecentStickerAction);
                        message._removeRecentStickerAction = "removeRecentStickerAction";
                        continue;
                    }
                case 35: {
                        if (u !== 2)
                            break;
                        message.chatAssignment = $root.proto.SyncActionValue.ChatAssignmentAction.decode(reader, reader.uint32(), undefined, q + 1, message.chatAssignment);
                        message._chatAssignment = "chatAssignment";
                        continue;
                    }
                case 36: {
                        if (u !== 2)
                            break;
                        message.chatAssignmentOpenedStatus = $root.proto.SyncActionValue.ChatAssignmentOpenedStatusAction.decode(reader, reader.uint32(), undefined, q + 1, message.chatAssignmentOpenedStatus);
                        message._chatAssignmentOpenedStatus = "chatAssignmentOpenedStatus";
                        continue;
                    }
                case 37: {
                        if (u !== 2)
                            break;
                        message.pnForLidChatAction = $root.proto.SyncActionValue.PnForLidChatAction.decode(reader, reader.uint32(), undefined, q + 1, message.pnForLidChatAction);
                        message._pnForLidChatAction = "pnForLidChatAction";
                        continue;
                    }
                case 38: {
                        if (u !== 2)
                            break;
                        message.marketingMessageAction = $root.proto.SyncActionValue.MarketingMessageAction.decode(reader, reader.uint32(), undefined, q + 1, message.marketingMessageAction);
                        message._marketingMessageAction = "marketingMessageAction";
                        continue;
                    }
                case 39: {
                        if (u !== 2)
                            break;
                        message.marketingMessageBroadcastAction = $root.proto.SyncActionValue.MarketingMessageBroadcastAction.decode(reader, reader.uint32(), undefined, q + 1, message.marketingMessageBroadcastAction);
                        message._marketingMessageBroadcastAction = "marketingMessageBroadcastAction";
                        continue;
                    }
                case 40: {
                        if (u !== 2)
                            break;
                        message.externalWebBetaAction = $root.proto.SyncActionValue.ExternalWebBetaAction.decode(reader, reader.uint32(), undefined, q + 1, message.externalWebBetaAction);
                        message._externalWebBetaAction = "externalWebBetaAction";
                        continue;
                    }
                case 41: {
                        if (u !== 2)
                            break;
                        message.privacySettingRelayAllCalls = $root.proto.SyncActionValue.PrivacySettingRelayAllCalls.decode(reader, reader.uint32(), undefined, q + 1, message.privacySettingRelayAllCalls);
                        message._privacySettingRelayAllCalls = "privacySettingRelayAllCalls";
                        continue;
                    }
                case 42: {
                        if (u !== 2)
                            break;
                        message.callLogAction = $root.proto.SyncActionValue.CallLogAction.decode(reader, reader.uint32(), undefined, q + 1, message.callLogAction);
                        message._callLogAction = "callLogAction";
                        continue;
                    }
                case 43: {
                        if (u !== 2)
                            break;
                        message.ugcBot = $root.proto.SyncActionValue.UGCBot.decode(reader, reader.uint32(), undefined, q + 1, message.ugcBot);
                        message._ugcBot = "ugcBot";
                        continue;
                    }
                case 44: {
                        if (u !== 2)
                            break;
                        message.statusPrivacy = $root.proto.SyncActionValue.StatusPrivacyAction.decode(reader, reader.uint32(), undefined, q + 1, message.statusPrivacy);
                        message._statusPrivacy = "statusPrivacy";
                        continue;
                    }
                case 45: {
                        if (u !== 2)
                            break;
                        message.botWelcomeRequestAction = $root.proto.SyncActionValue.BotWelcomeRequestAction.decode(reader, reader.uint32(), undefined, q + 1, message.botWelcomeRequestAction);
                        message._botWelcomeRequestAction = "botWelcomeRequestAction";
                        continue;
                    }
                case 46: {
                        if (u !== 2)
                            break;
                        message.deleteIndividualCallLog = $root.proto.SyncActionValue.DeleteIndividualCallLogAction.decode(reader, reader.uint32(), undefined, q + 1, message.deleteIndividualCallLog);
                        message._deleteIndividualCallLog = "deleteIndividualCallLog";
                        continue;
                    }
                case 47: {
                        if (u !== 2)
                            break;
                        message.labelReorderingAction = $root.proto.SyncActionValue.LabelReorderingAction.decode(reader, reader.uint32(), undefined, q + 1, message.labelReorderingAction);
                        message._labelReorderingAction = "labelReorderingAction";
                        continue;
                    }
                case 48: {
                        if (u !== 2)
                            break;
                        message.paymentInfoAction = $root.proto.SyncActionValue.PaymentInfoAction.decode(reader, reader.uint32(), undefined, q + 1, message.paymentInfoAction);
                        message._paymentInfoAction = "paymentInfoAction";
                        continue;
                    }
                case 49: {
                        if (u !== 2)
                            break;
                        message.customPaymentMethodsAction = $root.proto.SyncActionValue.CustomPaymentMethodsAction.decode(reader, reader.uint32(), undefined, q + 1, message.customPaymentMethodsAction);
                        message._customPaymentMethodsAction = "customPaymentMethodsAction";
                        continue;
                    }
                case 50: {
                        if (u !== 2)
                            break;
                        message.lockChatAction = $root.proto.SyncActionValue.LockChatAction.decode(reader, reader.uint32(), undefined, q + 1, message.lockChatAction);
                        message._lockChatAction = "lockChatAction";
                        continue;
                    }
                case 51: {
                        if (u !== 2)
                            break;
                        message.chatLockSettings = $root.proto.ChatLockSettings.decode(reader, reader.uint32(), undefined, q + 1, message.chatLockSettings);
                        message._chatLockSettings = "chatLockSettings";
                        continue;
                    }
                case 52: {
                        if (u !== 2)
                            break;
                        message.wamoUserIdentifierAction = $root.proto.SyncActionValue.WamoUserIdentifierAction.decode(reader, reader.uint32(), undefined, q + 1, message.wamoUserIdentifierAction);
                        message._wamoUserIdentifierAction = "wamoUserIdentifierAction";
                        continue;
                    }
                case 53: {
                        if (u !== 2)
                            break;
                        message.privacySettingDisableLinkPreviewsAction = $root.proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction.decode(reader, reader.uint32(), undefined, q + 1, message.privacySettingDisableLinkPreviewsAction);
                        message._privacySettingDisableLinkPreviewsAction = "privacySettingDisableLinkPreviewsAction";
                        continue;
                    }
                case 54: {
                        if (u !== 2)
                            break;
                        message.deviceCapabilities = $root.proto.DeviceCapabilities.decode(reader, reader.uint32(), undefined, q + 1, message.deviceCapabilities);
                        message._deviceCapabilities = "deviceCapabilities";
                        continue;
                    }
                case 55: {
                        if (u !== 2)
                            break;
                        message.noteEditAction = $root.proto.SyncActionValue.NoteEditAction.decode(reader, reader.uint32(), undefined, q + 1, message.noteEditAction);
                        message._noteEditAction = "noteEditAction";
                        continue;
                    }
                case 56: {
                        if (u !== 2)
                            break;
                        message.favoritesAction = $root.proto.SyncActionValue.FavoritesAction.decode(reader, reader.uint32(), undefined, q + 1, message.favoritesAction);
                        message._favoritesAction = "favoritesAction";
                        continue;
                    }
                case 57: {
                        if (u !== 2)
                            break;
                        message.merchantPaymentPartnerAction = $root.proto.SyncActionValue.MerchantPaymentPartnerAction.decode(reader, reader.uint32(), undefined, q + 1, message.merchantPaymentPartnerAction);
                        message._merchantPaymentPartnerAction = "merchantPaymentPartnerAction";
                        continue;
                    }
                case 58: {
                        if (u !== 2)
                            break;
                        message.waffleAccountLinkStateAction = $root.proto.SyncActionValue.WaffleAccountLinkStateAction.decode(reader, reader.uint32(), undefined, q + 1, message.waffleAccountLinkStateAction);
                        message._waffleAccountLinkStateAction = "waffleAccountLinkStateAction";
                        continue;
                    }
                case 59: {
                        if (u !== 2)
                            break;
                        message.usernameChatStartMode = $root.proto.SyncActionValue.UsernameChatStartModeAction.decode(reader, reader.uint32(), undefined, q + 1, message.usernameChatStartMode);
                        message._usernameChatStartMode = "usernameChatStartMode";
                        continue;
                    }
                case 60: {
                        if (u !== 2)
                            break;
                        message.notificationActivitySettingAction = $root.proto.SyncActionValue.NotificationActivitySettingAction.decode(reader, reader.uint32(), undefined, q + 1, message.notificationActivitySettingAction);
                        message._notificationActivitySettingAction = "notificationActivitySettingAction";
                        continue;
                    }
                case 61: {
                        if (u !== 2)
                            break;
                        message.lidContactAction = $root.proto.SyncActionValue.LidContactAction.decode(reader, reader.uint32(), undefined, q + 1, message.lidContactAction);
                        message._lidContactAction = "lidContactAction";
                        continue;
                    }
                case 62: {
                        if (u !== 2)
                            break;
                        message.ctwaPerCustomerDataSharingAction = $root.proto.SyncActionValue.CtwaPerCustomerDataSharingAction.decode(reader, reader.uint32(), undefined, q + 1, message.ctwaPerCustomerDataSharingAction);
                        message._ctwaPerCustomerDataSharingAction = "ctwaPerCustomerDataSharingAction";
                        continue;
                    }
                case 63: {
                        if (u !== 2)
                            break;
                        message.paymentTosAction = $root.proto.SyncActionValue.PaymentTosAction.decode(reader, reader.uint32(), undefined, q + 1, message.paymentTosAction);
                        message._paymentTosAction = "paymentTosAction";
                        continue;
                    }
                case 64: {
                        if (u !== 2)
                            break;
                        message.privacySettingChannelsPersonalisedRecommendationAction = $root.proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction.decode(reader, reader.uint32(), undefined, q + 1, message.privacySettingChannelsPersonalisedRecommendationAction);
                        message._privacySettingChannelsPersonalisedRecommendationAction = "privacySettingChannelsPersonalisedRecommendationAction";
                        continue;
                    }
                case 66: {
                        if (u !== 2)
                            break;
                        message.detectedOutcomesStatusAction = $root.proto.SyncActionValue.DetectedOutcomesStatusAction.decode(reader, reader.uint32(), undefined, q + 1, message.detectedOutcomesStatusAction);
                        message._detectedOutcomesStatusAction = "detectedOutcomesStatusAction";
                        continue;
                    }
                case 68: {
                        if (u !== 2)
                            break;
                        message.maibaAiFeaturesControlAction = $root.proto.SyncActionValue.MaibaAIFeaturesControlAction.decode(reader, reader.uint32(), undefined, q + 1, message.maibaAiFeaturesControlAction);
                        message._maibaAiFeaturesControlAction = "maibaAiFeaturesControlAction";
                        continue;
                    }
                case 69: {
                        if (u !== 2)
                            break;
                        message.businessBroadcastListAction = $root.proto.SyncActionValue.BusinessBroadcastListAction.decode(reader, reader.uint32(), undefined, q + 1, message.businessBroadcastListAction);
                        message._businessBroadcastListAction = "businessBroadcastListAction";
                        continue;
                    }
                case 70: {
                        if (u !== 2)
                            break;
                        message.musicUserIdAction = $root.proto.SyncActionValue.MusicUserIdAction.decode(reader, reader.uint32(), undefined, q + 1, message.musicUserIdAction);
                        message._musicUserIdAction = "musicUserIdAction";
                        continue;
                    }
                case 71: {
                        if (u !== 2)
                            break;
                        message.statusPostOptInNotificationPreferencesAction = $root.proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction.decode(reader, reader.uint32(), undefined, q + 1, message.statusPostOptInNotificationPreferencesAction);
                        message._statusPostOptInNotificationPreferencesAction = "statusPostOptInNotificationPreferencesAction";
                        continue;
                    }
                case 72: {
                        if (u !== 2)
                            break;
                        message.avatarUpdatedAction = $root.proto.SyncActionValue.AvatarUpdatedAction.decode(reader, reader.uint32(), undefined, q + 1, message.avatarUpdatedAction);
                        message._avatarUpdatedAction = "avatarUpdatedAction";
                        continue;
                    }
                case 74: {
                        if (u !== 2)
                            break;
                        message.privateProcessingSettingAction = $root.proto.SyncActionValue.PrivateProcessingSettingAction.decode(reader, reader.uint32(), undefined, q + 1, message.privateProcessingSettingAction);
                        message._privateProcessingSettingAction = "privateProcessingSettingAction";
                        continue;
                    }
                case 75: {
                        if (u !== 2)
                            break;
                        message.newsletterSavedInterestsAction = $root.proto.SyncActionValue.NewsletterSavedInterestsAction.decode(reader, reader.uint32(), undefined, q + 1, message.newsletterSavedInterestsAction);
                        message._newsletterSavedInterestsAction = "newsletterSavedInterestsAction";
                        continue;
                    }
                case 76: {
                        if (u !== 2)
                            break;
                        message.aiThreadRenameAction = $root.proto.SyncActionValue.AiThreadRenameAction.decode(reader, reader.uint32(), undefined, q + 1, message.aiThreadRenameAction);
                        message._aiThreadRenameAction = "aiThreadRenameAction";
                        continue;
                    }
                case 77: {
                        if (u !== 2)
                            break;
                        message.interactiveMessageAction = $root.proto.SyncActionValue.InteractiveMessageAction.decode(reader, reader.uint32(), undefined, q + 1, message.interactiveMessageAction);
                        message._interactiveMessageAction = "interactiveMessageAction";
                        continue;
                    }
                case 78: {
                        if (u !== 2)
                            break;
                        message.settingsSyncAction = $root.proto.SyncActionValue.SettingsSyncAction.decode(reader, reader.uint32(), undefined, q + 1, message.settingsSyncAction);
                        message._settingsSyncAction = "settingsSyncAction";
                        continue;
                    }
                case 79: {
                        if (u !== 2)
                            break;
                        message.outContactAction = $root.proto.SyncActionValue.OutContactAction.decode(reader, reader.uint32(), undefined, q + 1, message.outContactAction);
                        message._outContactAction = "outContactAction";
                        continue;
                    }
                case 80: {
                        if (u !== 2)
                            break;
                        message.nctSaltSyncAction = $root.proto.SyncActionValue.NctSaltSyncAction.decode(reader, reader.uint32(), undefined, q + 1, message.nctSaltSyncAction);
                        message._nctSaltSyncAction = "nctSaltSyncAction";
                        continue;
                    }
                case 81: {
                        if (u !== 2)
                            break;
                        message.businessBroadcastCampaignAction = $root.proto.SyncActionValue.BusinessBroadcastCampaignAction.decode(reader, reader.uint32(), undefined, q + 1, message.businessBroadcastCampaignAction);
                        message._businessBroadcastCampaignAction = "businessBroadcastCampaignAction";
                        continue;
                    }
                case 82: {
                        if (u !== 2)
                            break;
                        message.businessBroadcastInsightsAction = $root.proto.SyncActionValue.BusinessBroadcastInsightsAction.decode(reader, reader.uint32(), undefined, q + 1, message.businessBroadcastInsightsAction);
                        message._businessBroadcastInsightsAction = "businessBroadcastInsightsAction";
                        continue;
                    }
                case 83: {
                        if (u !== 2)
                            break;
                        message.customerDataAction = $root.proto.SyncActionValue.CustomerDataAction.decode(reader, reader.uint32(), undefined, q + 1, message.customerDataAction);
                        message._customerDataAction = "customerDataAction";
                        continue;
                    }
                case 84: {
                        if (u !== 2)
                            break;
                        message.subscriptionsSyncV2Action = $root.proto.SyncActionValue.SubscriptionsSyncV2Action.decode(reader, reader.uint32(), undefined, q + 1, message.subscriptionsSyncV2Action);
                        message._subscriptionsSyncV2Action = "subscriptionsSyncV2Action";
                        continue;
                    }
                case 85: {
                        if (u !== 2)
                            break;
                        message.threadPinAction = $root.proto.SyncActionValue.ThreadPinAction.decode(reader, reader.uint32(), undefined, q + 1, message.threadPinAction);
                        message._threadPinAction = "threadPinAction";
                        continue;
                    }
                case 86: {
                        if (u !== 2)
                            break;
                        message.autoOrganizeBusinessChatSetting = $root.proto.SyncActionValue.AutoOrganizeBusinessChatSetting.decode(reader, reader.uint32(), undefined, q + 1, message.autoOrganizeBusinessChatSetting);
                        message._autoOrganizeBusinessChatSetting = "autoOrganizeBusinessChatSetting";
                        continue;
                    }
                case 87: {
                        if (u !== 2)
                            break;
                        message.bizAiSettingsNudgeAction = $root.proto.SyncActionValue.BizAISettingsNudgeAction.decode(reader, reader.uint32(), undefined, q + 1, message.bizAiSettingsNudgeAction);
                        message._bizAiSettingsNudgeAction = "bizAiSettingsNudgeAction";
                        continue;
                    }
                case 88: {
                        if (u !== 2)
                            break;
                        message.coexV2VersionAction = $root.proto.SyncActionValue.CoexV2VersionAction.decode(reader, reader.uint32(), undefined, q + 1, message.coexV2VersionAction);
                        message._coexV2VersionAction = "coexV2VersionAction";
                        continue;
                    }
                case 89: {
                        if (u !== 2)
                            break;
                        message.wasaRootSecretAction = $root.proto.SyncActionValue.WASARootSecretAction.decode(reader, reader.uint32(), undefined, q + 1, message.wasaRootSecretAction);
                        message._wasaRootSecretAction = "wasaRootSecretAction";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a SyncActionValue message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SyncActionValue
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SyncActionValue} SyncActionValue
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SyncActionValue.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SyncActionValue message.
         * @function verify
         * @memberof proto.SyncActionValue
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SyncActionValue.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp")) {
                properties._timestamp = 1;
                if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                    return "timestamp: integer|Long expected";
            }
            if (message.starAction != null && Object.hasOwnProperty.call(message, "starAction")) {
                properties._starAction = 1;
                {
                    var error = $root.proto.SyncActionValue.StarAction.verify(message.starAction, q + 1);
                    if (error)
                        return "starAction." + error;
                }
            }
            if (message.contactAction != null && Object.hasOwnProperty.call(message, "contactAction")) {
                properties._contactAction = 1;
                {
                    var error = $root.proto.SyncActionValue.ContactAction.verify(message.contactAction, q + 1);
                    if (error)
                        return "contactAction." + error;
                }
            }
            if (message.muteAction != null && Object.hasOwnProperty.call(message, "muteAction")) {
                properties._muteAction = 1;
                {
                    var error = $root.proto.SyncActionValue.MuteAction.verify(message.muteAction, q + 1);
                    if (error)
                        return "muteAction." + error;
                }
            }
            if (message.pinAction != null && Object.hasOwnProperty.call(message, "pinAction")) {
                properties._pinAction = 1;
                {
                    var error = $root.proto.SyncActionValue.PinAction.verify(message.pinAction, q + 1);
                    if (error)
                        return "pinAction." + error;
                }
            }
            if (message.pushNameSetting != null && Object.hasOwnProperty.call(message, "pushNameSetting")) {
                properties._pushNameSetting = 1;
                {
                    var error = $root.proto.SyncActionValue.PushNameSetting.verify(message.pushNameSetting, q + 1);
                    if (error)
                        return "pushNameSetting." + error;
                }
            }
            if (message.quickReplyAction != null && Object.hasOwnProperty.call(message, "quickReplyAction")) {
                properties._quickReplyAction = 1;
                {
                    var error = $root.proto.SyncActionValue.QuickReplyAction.verify(message.quickReplyAction, q + 1);
                    if (error)
                        return "quickReplyAction." + error;
                }
            }
            if (message.recentEmojiWeightsAction != null && Object.hasOwnProperty.call(message, "recentEmojiWeightsAction")) {
                properties._recentEmojiWeightsAction = 1;
                {
                    var error = $root.proto.SyncActionValue.RecentEmojiWeightsAction.verify(message.recentEmojiWeightsAction, q + 1);
                    if (error)
                        return "recentEmojiWeightsAction." + error;
                }
            }
            if (message.labelEditAction != null && Object.hasOwnProperty.call(message, "labelEditAction")) {
                properties._labelEditAction = 1;
                {
                    var error = $root.proto.SyncActionValue.LabelEditAction.verify(message.labelEditAction, q + 1);
                    if (error)
                        return "labelEditAction." + error;
                }
            }
            if (message.labelAssociationAction != null && Object.hasOwnProperty.call(message, "labelAssociationAction")) {
                properties._labelAssociationAction = 1;
                {
                    var error = $root.proto.SyncActionValue.LabelAssociationAction.verify(message.labelAssociationAction, q + 1);
                    if (error)
                        return "labelAssociationAction." + error;
                }
            }
            if (message.localeSetting != null && Object.hasOwnProperty.call(message, "localeSetting")) {
                properties._localeSetting = 1;
                {
                    var error = $root.proto.SyncActionValue.LocaleSetting.verify(message.localeSetting, q + 1);
                    if (error)
                        return "localeSetting." + error;
                }
            }
            if (message.archiveChatAction != null && Object.hasOwnProperty.call(message, "archiveChatAction")) {
                properties._archiveChatAction = 1;
                {
                    var error = $root.proto.SyncActionValue.ArchiveChatAction.verify(message.archiveChatAction, q + 1);
                    if (error)
                        return "archiveChatAction." + error;
                }
            }
            if (message.deleteMessageForMeAction != null && Object.hasOwnProperty.call(message, "deleteMessageForMeAction")) {
                properties._deleteMessageForMeAction = 1;
                {
                    var error = $root.proto.SyncActionValue.DeleteMessageForMeAction.verify(message.deleteMessageForMeAction, q + 1);
                    if (error)
                        return "deleteMessageForMeAction." + error;
                }
            }
            if (message.keyExpiration != null && Object.hasOwnProperty.call(message, "keyExpiration")) {
                properties._keyExpiration = 1;
                {
                    var error = $root.proto.SyncActionValue.KeyExpiration.verify(message.keyExpiration, q + 1);
                    if (error)
                        return "keyExpiration." + error;
                }
            }
            if (message.markChatAsReadAction != null && Object.hasOwnProperty.call(message, "markChatAsReadAction")) {
                properties._markChatAsReadAction = 1;
                {
                    var error = $root.proto.SyncActionValue.MarkChatAsReadAction.verify(message.markChatAsReadAction, q + 1);
                    if (error)
                        return "markChatAsReadAction." + error;
                }
            }
            if (message.clearChatAction != null && Object.hasOwnProperty.call(message, "clearChatAction")) {
                properties._clearChatAction = 1;
                {
                    var error = $root.proto.SyncActionValue.ClearChatAction.verify(message.clearChatAction, q + 1);
                    if (error)
                        return "clearChatAction." + error;
                }
            }
            if (message.deleteChatAction != null && Object.hasOwnProperty.call(message, "deleteChatAction")) {
                properties._deleteChatAction = 1;
                {
                    var error = $root.proto.SyncActionValue.DeleteChatAction.verify(message.deleteChatAction, q + 1);
                    if (error)
                        return "deleteChatAction." + error;
                }
            }
            if (message.unarchiveChatsSetting != null && Object.hasOwnProperty.call(message, "unarchiveChatsSetting")) {
                properties._unarchiveChatsSetting = 1;
                {
                    var error = $root.proto.SyncActionValue.UnarchiveChatsSetting.verify(message.unarchiveChatsSetting, q + 1);
                    if (error)
                        return "unarchiveChatsSetting." + error;
                }
            }
            if (message.primaryFeature != null && Object.hasOwnProperty.call(message, "primaryFeature")) {
                properties._primaryFeature = 1;
                {
                    var error = $root.proto.SyncActionValue.PrimaryFeature.verify(message.primaryFeature, q + 1);
                    if (error)
                        return "primaryFeature." + error;
                }
            }
            if (message.androidUnsupportedActions != null && Object.hasOwnProperty.call(message, "androidUnsupportedActions")) {
                properties._androidUnsupportedActions = 1;
                {
                    var error = $root.proto.SyncActionValue.AndroidUnsupportedActions.verify(message.androidUnsupportedActions, q + 1);
                    if (error)
                        return "androidUnsupportedActions." + error;
                }
            }
            if (message.agentAction != null && Object.hasOwnProperty.call(message, "agentAction")) {
                properties._agentAction = 1;
                {
                    var error = $root.proto.SyncActionValue.AgentAction.verify(message.agentAction, q + 1);
                    if (error)
                        return "agentAction." + error;
                }
            }
            if (message.subscriptionAction != null && Object.hasOwnProperty.call(message, "subscriptionAction")) {
                properties._subscriptionAction = 1;
                {
                    var error = $root.proto.SyncActionValue.SubscriptionAction.verify(message.subscriptionAction, q + 1);
                    if (error)
                        return "subscriptionAction." + error;
                }
            }
            if (message.userStatusMuteAction != null && Object.hasOwnProperty.call(message, "userStatusMuteAction")) {
                properties._userStatusMuteAction = 1;
                {
                    var error = $root.proto.SyncActionValue.UserStatusMuteAction.verify(message.userStatusMuteAction, q + 1);
                    if (error)
                        return "userStatusMuteAction." + error;
                }
            }
            if (message.timeFormatAction != null && Object.hasOwnProperty.call(message, "timeFormatAction")) {
                properties._timeFormatAction = 1;
                {
                    var error = $root.proto.SyncActionValue.TimeFormatAction.verify(message.timeFormatAction, q + 1);
                    if (error)
                        return "timeFormatAction." + error;
                }
            }
            if (message.nuxAction != null && Object.hasOwnProperty.call(message, "nuxAction")) {
                properties._nuxAction = 1;
                {
                    var error = $root.proto.SyncActionValue.NuxAction.verify(message.nuxAction, q + 1);
                    if (error)
                        return "nuxAction." + error;
                }
            }
            if (message.primaryVersionAction != null && Object.hasOwnProperty.call(message, "primaryVersionAction")) {
                properties._primaryVersionAction = 1;
                {
                    var error = $root.proto.SyncActionValue.PrimaryVersionAction.verify(message.primaryVersionAction, q + 1);
                    if (error)
                        return "primaryVersionAction." + error;
                }
            }
            if (message.stickerAction != null && Object.hasOwnProperty.call(message, "stickerAction")) {
                properties._stickerAction = 1;
                {
                    var error = $root.proto.SyncActionValue.StickerAction.verify(message.stickerAction, q + 1);
                    if (error)
                        return "stickerAction." + error;
                }
            }
            if (message.removeRecentStickerAction != null && Object.hasOwnProperty.call(message, "removeRecentStickerAction")) {
                properties._removeRecentStickerAction = 1;
                {
                    var error = $root.proto.SyncActionValue.RemoveRecentStickerAction.verify(message.removeRecentStickerAction, q + 1);
                    if (error)
                        return "removeRecentStickerAction." + error;
                }
            }
            if (message.chatAssignment != null && Object.hasOwnProperty.call(message, "chatAssignment")) {
                properties._chatAssignment = 1;
                {
                    var error = $root.proto.SyncActionValue.ChatAssignmentAction.verify(message.chatAssignment, q + 1);
                    if (error)
                        return "chatAssignment." + error;
                }
            }
            if (message.chatAssignmentOpenedStatus != null && Object.hasOwnProperty.call(message, "chatAssignmentOpenedStatus")) {
                properties._chatAssignmentOpenedStatus = 1;
                {
                    var error = $root.proto.SyncActionValue.ChatAssignmentOpenedStatusAction.verify(message.chatAssignmentOpenedStatus, q + 1);
                    if (error)
                        return "chatAssignmentOpenedStatus." + error;
                }
            }
            if (message.pnForLidChatAction != null && Object.hasOwnProperty.call(message, "pnForLidChatAction")) {
                properties._pnForLidChatAction = 1;
                {
                    var error = $root.proto.SyncActionValue.PnForLidChatAction.verify(message.pnForLidChatAction, q + 1);
                    if (error)
                        return "pnForLidChatAction." + error;
                }
            }
            if (message.marketingMessageAction != null && Object.hasOwnProperty.call(message, "marketingMessageAction")) {
                properties._marketingMessageAction = 1;
                {
                    var error = $root.proto.SyncActionValue.MarketingMessageAction.verify(message.marketingMessageAction, q + 1);
                    if (error)
                        return "marketingMessageAction." + error;
                }
            }
            if (message.marketingMessageBroadcastAction != null && Object.hasOwnProperty.call(message, "marketingMessageBroadcastAction")) {
                properties._marketingMessageBroadcastAction = 1;
                {
                    var error = $root.proto.SyncActionValue.MarketingMessageBroadcastAction.verify(message.marketingMessageBroadcastAction, q + 1);
                    if (error)
                        return "marketingMessageBroadcastAction." + error;
                }
            }
            if (message.externalWebBetaAction != null && Object.hasOwnProperty.call(message, "externalWebBetaAction")) {
                properties._externalWebBetaAction = 1;
                {
                    var error = $root.proto.SyncActionValue.ExternalWebBetaAction.verify(message.externalWebBetaAction, q + 1);
                    if (error)
                        return "externalWebBetaAction." + error;
                }
            }
            if (message.privacySettingRelayAllCalls != null && Object.hasOwnProperty.call(message, "privacySettingRelayAllCalls")) {
                properties._privacySettingRelayAllCalls = 1;
                {
                    var error = $root.proto.SyncActionValue.PrivacySettingRelayAllCalls.verify(message.privacySettingRelayAllCalls, q + 1);
                    if (error)
                        return "privacySettingRelayAllCalls." + error;
                }
            }
            if (message.callLogAction != null && Object.hasOwnProperty.call(message, "callLogAction")) {
                properties._callLogAction = 1;
                {
                    var error = $root.proto.SyncActionValue.CallLogAction.verify(message.callLogAction, q + 1);
                    if (error)
                        return "callLogAction." + error;
                }
            }
            if (message.ugcBot != null && Object.hasOwnProperty.call(message, "ugcBot")) {
                properties._ugcBot = 1;
                {
                    var error = $root.proto.SyncActionValue.UGCBot.verify(message.ugcBot, q + 1);
                    if (error)
                        return "ugcBot." + error;
                }
            }
            if (message.statusPrivacy != null && Object.hasOwnProperty.call(message, "statusPrivacy")) {
                properties._statusPrivacy = 1;
                {
                    var error = $root.proto.SyncActionValue.StatusPrivacyAction.verify(message.statusPrivacy, q + 1);
                    if (error)
                        return "statusPrivacy." + error;
                }
            }
            if (message.botWelcomeRequestAction != null && Object.hasOwnProperty.call(message, "botWelcomeRequestAction")) {
                properties._botWelcomeRequestAction = 1;
                {
                    var error = $root.proto.SyncActionValue.BotWelcomeRequestAction.verify(message.botWelcomeRequestAction, q + 1);
                    if (error)
                        return "botWelcomeRequestAction." + error;
                }
            }
            if (message.deleteIndividualCallLog != null && Object.hasOwnProperty.call(message, "deleteIndividualCallLog")) {
                properties._deleteIndividualCallLog = 1;
                {
                    var error = $root.proto.SyncActionValue.DeleteIndividualCallLogAction.verify(message.deleteIndividualCallLog, q + 1);
                    if (error)
                        return "deleteIndividualCallLog." + error;
                }
            }
            if (message.labelReorderingAction != null && Object.hasOwnProperty.call(message, "labelReorderingAction")) {
                properties._labelReorderingAction = 1;
                {
                    var error = $root.proto.SyncActionValue.LabelReorderingAction.verify(message.labelReorderingAction, q + 1);
                    if (error)
                        return "labelReorderingAction." + error;
                }
            }
            if (message.paymentInfoAction != null && Object.hasOwnProperty.call(message, "paymentInfoAction")) {
                properties._paymentInfoAction = 1;
                {
                    var error = $root.proto.SyncActionValue.PaymentInfoAction.verify(message.paymentInfoAction, q + 1);
                    if (error)
                        return "paymentInfoAction." + error;
                }
            }
            if (message.customPaymentMethodsAction != null && Object.hasOwnProperty.call(message, "customPaymentMethodsAction")) {
                properties._customPaymentMethodsAction = 1;
                {
                    var error = $root.proto.SyncActionValue.CustomPaymentMethodsAction.verify(message.customPaymentMethodsAction, q + 1);
                    if (error)
                        return "customPaymentMethodsAction." + error;
                }
            }
            if (message.lockChatAction != null && Object.hasOwnProperty.call(message, "lockChatAction")) {
                properties._lockChatAction = 1;
                {
                    var error = $root.proto.SyncActionValue.LockChatAction.verify(message.lockChatAction, q + 1);
                    if (error)
                        return "lockChatAction." + error;
                }
            }
            if (message.chatLockSettings != null && Object.hasOwnProperty.call(message, "chatLockSettings")) {
                properties._chatLockSettings = 1;
                {
                    var error = $root.proto.ChatLockSettings.verify(message.chatLockSettings, q + 1);
                    if (error)
                        return "chatLockSettings." + error;
                }
            }
            if (message.wamoUserIdentifierAction != null && Object.hasOwnProperty.call(message, "wamoUserIdentifierAction")) {
                properties._wamoUserIdentifierAction = 1;
                {
                    var error = $root.proto.SyncActionValue.WamoUserIdentifierAction.verify(message.wamoUserIdentifierAction, q + 1);
                    if (error)
                        return "wamoUserIdentifierAction." + error;
                }
            }
            if (message.privacySettingDisableLinkPreviewsAction != null && Object.hasOwnProperty.call(message, "privacySettingDisableLinkPreviewsAction")) {
                properties._privacySettingDisableLinkPreviewsAction = 1;
                {
                    var error = $root.proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction.verify(message.privacySettingDisableLinkPreviewsAction, q + 1);
                    if (error)
                        return "privacySettingDisableLinkPreviewsAction." + error;
                }
            }
            if (message.deviceCapabilities != null && Object.hasOwnProperty.call(message, "deviceCapabilities")) {
                properties._deviceCapabilities = 1;
                {
                    var error = $root.proto.DeviceCapabilities.verify(message.deviceCapabilities, q + 1);
                    if (error)
                        return "deviceCapabilities." + error;
                }
            }
            if (message.noteEditAction != null && Object.hasOwnProperty.call(message, "noteEditAction")) {
                properties._noteEditAction = 1;
                {
                    var error = $root.proto.SyncActionValue.NoteEditAction.verify(message.noteEditAction, q + 1);
                    if (error)
                        return "noteEditAction." + error;
                }
            }
            if (message.favoritesAction != null && Object.hasOwnProperty.call(message, "favoritesAction")) {
                properties._favoritesAction = 1;
                {
                    var error = $root.proto.SyncActionValue.FavoritesAction.verify(message.favoritesAction, q + 1);
                    if (error)
                        return "favoritesAction." + error;
                }
            }
            if (message.merchantPaymentPartnerAction != null && Object.hasOwnProperty.call(message, "merchantPaymentPartnerAction")) {
                properties._merchantPaymentPartnerAction = 1;
                {
                    var error = $root.proto.SyncActionValue.MerchantPaymentPartnerAction.verify(message.merchantPaymentPartnerAction, q + 1);
                    if (error)
                        return "merchantPaymentPartnerAction." + error;
                }
            }
            if (message.waffleAccountLinkStateAction != null && Object.hasOwnProperty.call(message, "waffleAccountLinkStateAction")) {
                properties._waffleAccountLinkStateAction = 1;
                {
                    var error = $root.proto.SyncActionValue.WaffleAccountLinkStateAction.verify(message.waffleAccountLinkStateAction, q + 1);
                    if (error)
                        return "waffleAccountLinkStateAction." + error;
                }
            }
            if (message.usernameChatStartMode != null && Object.hasOwnProperty.call(message, "usernameChatStartMode")) {
                properties._usernameChatStartMode = 1;
                {
                    var error = $root.proto.SyncActionValue.UsernameChatStartModeAction.verify(message.usernameChatStartMode, q + 1);
                    if (error)
                        return "usernameChatStartMode." + error;
                }
            }
            if (message.notificationActivitySettingAction != null && Object.hasOwnProperty.call(message, "notificationActivitySettingAction")) {
                properties._notificationActivitySettingAction = 1;
                {
                    var error = $root.proto.SyncActionValue.NotificationActivitySettingAction.verify(message.notificationActivitySettingAction, q + 1);
                    if (error)
                        return "notificationActivitySettingAction." + error;
                }
            }
            if (message.lidContactAction != null && Object.hasOwnProperty.call(message, "lidContactAction")) {
                properties._lidContactAction = 1;
                {
                    var error = $root.proto.SyncActionValue.LidContactAction.verify(message.lidContactAction, q + 1);
                    if (error)
                        return "lidContactAction." + error;
                }
            }
            if (message.ctwaPerCustomerDataSharingAction != null && Object.hasOwnProperty.call(message, "ctwaPerCustomerDataSharingAction")) {
                properties._ctwaPerCustomerDataSharingAction = 1;
                {
                    var error = $root.proto.SyncActionValue.CtwaPerCustomerDataSharingAction.verify(message.ctwaPerCustomerDataSharingAction, q + 1);
                    if (error)
                        return "ctwaPerCustomerDataSharingAction." + error;
                }
            }
            if (message.paymentTosAction != null && Object.hasOwnProperty.call(message, "paymentTosAction")) {
                properties._paymentTosAction = 1;
                {
                    var error = $root.proto.SyncActionValue.PaymentTosAction.verify(message.paymentTosAction, q + 1);
                    if (error)
                        return "paymentTosAction." + error;
                }
            }
            if (message.privacySettingChannelsPersonalisedRecommendationAction != null && Object.hasOwnProperty.call(message, "privacySettingChannelsPersonalisedRecommendationAction")) {
                properties._privacySettingChannelsPersonalisedRecommendationAction = 1;
                {
                    var error = $root.proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction.verify(message.privacySettingChannelsPersonalisedRecommendationAction, q + 1);
                    if (error)
                        return "privacySettingChannelsPersonalisedRecommendationAction." + error;
                }
            }
            if (message.detectedOutcomesStatusAction != null && Object.hasOwnProperty.call(message, "detectedOutcomesStatusAction")) {
                properties._detectedOutcomesStatusAction = 1;
                {
                    var error = $root.proto.SyncActionValue.DetectedOutcomesStatusAction.verify(message.detectedOutcomesStatusAction, q + 1);
                    if (error)
                        return "detectedOutcomesStatusAction." + error;
                }
            }
            if (message.maibaAiFeaturesControlAction != null && Object.hasOwnProperty.call(message, "maibaAiFeaturesControlAction")) {
                properties._maibaAiFeaturesControlAction = 1;
                {
                    var error = $root.proto.SyncActionValue.MaibaAIFeaturesControlAction.verify(message.maibaAiFeaturesControlAction, q + 1);
                    if (error)
                        return "maibaAiFeaturesControlAction." + error;
                }
            }
            if (message.businessBroadcastListAction != null && Object.hasOwnProperty.call(message, "businessBroadcastListAction")) {
                properties._businessBroadcastListAction = 1;
                {
                    var error = $root.proto.SyncActionValue.BusinessBroadcastListAction.verify(message.businessBroadcastListAction, q + 1);
                    if (error)
                        return "businessBroadcastListAction." + error;
                }
            }
            if (message.musicUserIdAction != null && Object.hasOwnProperty.call(message, "musicUserIdAction")) {
                properties._musicUserIdAction = 1;
                {
                    var error = $root.proto.SyncActionValue.MusicUserIdAction.verify(message.musicUserIdAction, q + 1);
                    if (error)
                        return "musicUserIdAction." + error;
                }
            }
            if (message.statusPostOptInNotificationPreferencesAction != null && Object.hasOwnProperty.call(message, "statusPostOptInNotificationPreferencesAction")) {
                properties._statusPostOptInNotificationPreferencesAction = 1;
                {
                    var error = $root.proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction.verify(message.statusPostOptInNotificationPreferencesAction, q + 1);
                    if (error)
                        return "statusPostOptInNotificationPreferencesAction." + error;
                }
            }
            if (message.avatarUpdatedAction != null && Object.hasOwnProperty.call(message, "avatarUpdatedAction")) {
                properties._avatarUpdatedAction = 1;
                {
                    var error = $root.proto.SyncActionValue.AvatarUpdatedAction.verify(message.avatarUpdatedAction, q + 1);
                    if (error)
                        return "avatarUpdatedAction." + error;
                }
            }
            if (message.privateProcessingSettingAction != null && Object.hasOwnProperty.call(message, "privateProcessingSettingAction")) {
                properties._privateProcessingSettingAction = 1;
                {
                    var error = $root.proto.SyncActionValue.PrivateProcessingSettingAction.verify(message.privateProcessingSettingAction, q + 1);
                    if (error)
                        return "privateProcessingSettingAction." + error;
                }
            }
            if (message.newsletterSavedInterestsAction != null && Object.hasOwnProperty.call(message, "newsletterSavedInterestsAction")) {
                properties._newsletterSavedInterestsAction = 1;
                {
                    var error = $root.proto.SyncActionValue.NewsletterSavedInterestsAction.verify(message.newsletterSavedInterestsAction, q + 1);
                    if (error)
                        return "newsletterSavedInterestsAction." + error;
                }
            }
            if (message.aiThreadRenameAction != null && Object.hasOwnProperty.call(message, "aiThreadRenameAction")) {
                properties._aiThreadRenameAction = 1;
                {
                    var error = $root.proto.SyncActionValue.AiThreadRenameAction.verify(message.aiThreadRenameAction, q + 1);
                    if (error)
                        return "aiThreadRenameAction." + error;
                }
            }
            if (message.interactiveMessageAction != null && Object.hasOwnProperty.call(message, "interactiveMessageAction")) {
                properties._interactiveMessageAction = 1;
                {
                    var error = $root.proto.SyncActionValue.InteractiveMessageAction.verify(message.interactiveMessageAction, q + 1);
                    if (error)
                        return "interactiveMessageAction." + error;
                }
            }
            if (message.settingsSyncAction != null && Object.hasOwnProperty.call(message, "settingsSyncAction")) {
                properties._settingsSyncAction = 1;
                {
                    var error = $root.proto.SyncActionValue.SettingsSyncAction.verify(message.settingsSyncAction, q + 1);
                    if (error)
                        return "settingsSyncAction." + error;
                }
            }
            if (message.outContactAction != null && Object.hasOwnProperty.call(message, "outContactAction")) {
                properties._outContactAction = 1;
                {
                    var error = $root.proto.SyncActionValue.OutContactAction.verify(message.outContactAction, q + 1);
                    if (error)
                        return "outContactAction." + error;
                }
            }
            if (message.nctSaltSyncAction != null && Object.hasOwnProperty.call(message, "nctSaltSyncAction")) {
                properties._nctSaltSyncAction = 1;
                {
                    var error = $root.proto.SyncActionValue.NctSaltSyncAction.verify(message.nctSaltSyncAction, q + 1);
                    if (error)
                        return "nctSaltSyncAction." + error;
                }
            }
            if (message.businessBroadcastCampaignAction != null && Object.hasOwnProperty.call(message, "businessBroadcastCampaignAction")) {
                properties._businessBroadcastCampaignAction = 1;
                {
                    var error = $root.proto.SyncActionValue.BusinessBroadcastCampaignAction.verify(message.businessBroadcastCampaignAction, q + 1);
                    if (error)
                        return "businessBroadcastCampaignAction." + error;
                }
            }
            if (message.businessBroadcastInsightsAction != null && Object.hasOwnProperty.call(message, "businessBroadcastInsightsAction")) {
                properties._businessBroadcastInsightsAction = 1;
                {
                    var error = $root.proto.SyncActionValue.BusinessBroadcastInsightsAction.verify(message.businessBroadcastInsightsAction, q + 1);
                    if (error)
                        return "businessBroadcastInsightsAction." + error;
                }
            }
            if (message.customerDataAction != null && Object.hasOwnProperty.call(message, "customerDataAction")) {
                properties._customerDataAction = 1;
                {
                    var error = $root.proto.SyncActionValue.CustomerDataAction.verify(message.customerDataAction, q + 1);
                    if (error)
                        return "customerDataAction." + error;
                }
            }
            if (message.subscriptionsSyncV2Action != null && Object.hasOwnProperty.call(message, "subscriptionsSyncV2Action")) {
                properties._subscriptionsSyncV2Action = 1;
                {
                    var error = $root.proto.SyncActionValue.SubscriptionsSyncV2Action.verify(message.subscriptionsSyncV2Action, q + 1);
                    if (error)
                        return "subscriptionsSyncV2Action." + error;
                }
            }
            if (message.threadPinAction != null && Object.hasOwnProperty.call(message, "threadPinAction")) {
                properties._threadPinAction = 1;
                {
                    var error = $root.proto.SyncActionValue.ThreadPinAction.verify(message.threadPinAction, q + 1);
                    if (error)
                        return "threadPinAction." + error;
                }
            }
            if (message.autoOrganizeBusinessChatSetting != null && Object.hasOwnProperty.call(message, "autoOrganizeBusinessChatSetting")) {
                properties._autoOrganizeBusinessChatSetting = 1;
                {
                    var error = $root.proto.SyncActionValue.AutoOrganizeBusinessChatSetting.verify(message.autoOrganizeBusinessChatSetting, q + 1);
                    if (error)
                        return "autoOrganizeBusinessChatSetting." + error;
                }
            }
            if (message.bizAiSettingsNudgeAction != null && Object.hasOwnProperty.call(message, "bizAiSettingsNudgeAction")) {
                properties._bizAiSettingsNudgeAction = 1;
                {
                    var error = $root.proto.SyncActionValue.BizAISettingsNudgeAction.verify(message.bizAiSettingsNudgeAction, q + 1);
                    if (error)
                        return "bizAiSettingsNudgeAction." + error;
                }
            }
            if (message.coexV2VersionAction != null && Object.hasOwnProperty.call(message, "coexV2VersionAction")) {
                properties._coexV2VersionAction = 1;
                {
                    var error = $root.proto.SyncActionValue.CoexV2VersionAction.verify(message.coexV2VersionAction, q + 1);
                    if (error)
                        return "coexV2VersionAction." + error;
                }
            }
            if (message.wasaRootSecretAction != null && Object.hasOwnProperty.call(message, "wasaRootSecretAction")) {
                properties._wasaRootSecretAction = 1;
                {
                    var error = $root.proto.SyncActionValue.WASARootSecretAction.verify(message.wasaRootSecretAction, q + 1);
                    if (error)
                        return "wasaRootSecretAction." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SyncActionValue message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SyncActionValue
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SyncActionValue} SyncActionValue
         */
        SyncActionValue.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.SyncActionValue: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.timestamp != null)
                if ($util.Long)
                    message.timestamp = $util.Long.fromValue(object.timestamp, false);
                else if (typeof object.timestamp === "string")
                    message.timestamp = parseInt(object.timestamp, 10);
                else if (typeof object.timestamp === "number")
                    message.timestamp = object.timestamp;
                else if (typeof object.timestamp === "object")
                    message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
            if (object.starAction != null) {
                if (!$util.isObject(object.starAction))
                    throw TypeError(".proto.SyncActionValue.starAction: object expected");
                message.starAction = $root.proto.SyncActionValue.StarAction.fromObject(object.starAction, q + 1);
            }
            if (object.contactAction != null) {
                if (!$util.isObject(object.contactAction))
                    throw TypeError(".proto.SyncActionValue.contactAction: object expected");
                message.contactAction = $root.proto.SyncActionValue.ContactAction.fromObject(object.contactAction, q + 1);
            }
            if (object.muteAction != null) {
                if (!$util.isObject(object.muteAction))
                    throw TypeError(".proto.SyncActionValue.muteAction: object expected");
                message.muteAction = $root.proto.SyncActionValue.MuteAction.fromObject(object.muteAction, q + 1);
            }
            if (object.pinAction != null) {
                if (!$util.isObject(object.pinAction))
                    throw TypeError(".proto.SyncActionValue.pinAction: object expected");
                message.pinAction = $root.proto.SyncActionValue.PinAction.fromObject(object.pinAction, q + 1);
            }
            if (object.pushNameSetting != null) {
                if (!$util.isObject(object.pushNameSetting))
                    throw TypeError(".proto.SyncActionValue.pushNameSetting: object expected");
                message.pushNameSetting = $root.proto.SyncActionValue.PushNameSetting.fromObject(object.pushNameSetting, q + 1);
            }
            if (object.quickReplyAction != null) {
                if (!$util.isObject(object.quickReplyAction))
                    throw TypeError(".proto.SyncActionValue.quickReplyAction: object expected");
                message.quickReplyAction = $root.proto.SyncActionValue.QuickReplyAction.fromObject(object.quickReplyAction, q + 1);
            }
            if (object.recentEmojiWeightsAction != null) {
                if (!$util.isObject(object.recentEmojiWeightsAction))
                    throw TypeError(".proto.SyncActionValue.recentEmojiWeightsAction: object expected");
                message.recentEmojiWeightsAction = $root.proto.SyncActionValue.RecentEmojiWeightsAction.fromObject(object.recentEmojiWeightsAction, q + 1);
            }
            if (object.labelEditAction != null) {
                if (!$util.isObject(object.labelEditAction))
                    throw TypeError(".proto.SyncActionValue.labelEditAction: object expected");
                message.labelEditAction = $root.proto.SyncActionValue.LabelEditAction.fromObject(object.labelEditAction, q + 1);
            }
            if (object.labelAssociationAction != null) {
                if (!$util.isObject(object.labelAssociationAction))
                    throw TypeError(".proto.SyncActionValue.labelAssociationAction: object expected");
                message.labelAssociationAction = $root.proto.SyncActionValue.LabelAssociationAction.fromObject(object.labelAssociationAction, q + 1);
            }
            if (object.localeSetting != null) {
                if (!$util.isObject(object.localeSetting))
                    throw TypeError(".proto.SyncActionValue.localeSetting: object expected");
                message.localeSetting = $root.proto.SyncActionValue.LocaleSetting.fromObject(object.localeSetting, q + 1);
            }
            if (object.archiveChatAction != null) {
                if (!$util.isObject(object.archiveChatAction))
                    throw TypeError(".proto.SyncActionValue.archiveChatAction: object expected");
                message.archiveChatAction = $root.proto.SyncActionValue.ArchiveChatAction.fromObject(object.archiveChatAction, q + 1);
            }
            if (object.deleteMessageForMeAction != null) {
                if (!$util.isObject(object.deleteMessageForMeAction))
                    throw TypeError(".proto.SyncActionValue.deleteMessageForMeAction: object expected");
                message.deleteMessageForMeAction = $root.proto.SyncActionValue.DeleteMessageForMeAction.fromObject(object.deleteMessageForMeAction, q + 1);
            }
            if (object.keyExpiration != null) {
                if (!$util.isObject(object.keyExpiration))
                    throw TypeError(".proto.SyncActionValue.keyExpiration: object expected");
                message.keyExpiration = $root.proto.SyncActionValue.KeyExpiration.fromObject(object.keyExpiration, q + 1);
            }
            if (object.markChatAsReadAction != null) {
                if (!$util.isObject(object.markChatAsReadAction))
                    throw TypeError(".proto.SyncActionValue.markChatAsReadAction: object expected");
                message.markChatAsReadAction = $root.proto.SyncActionValue.MarkChatAsReadAction.fromObject(object.markChatAsReadAction, q + 1);
            }
            if (object.clearChatAction != null) {
                if (!$util.isObject(object.clearChatAction))
                    throw TypeError(".proto.SyncActionValue.clearChatAction: object expected");
                message.clearChatAction = $root.proto.SyncActionValue.ClearChatAction.fromObject(object.clearChatAction, q + 1);
            }
            if (object.deleteChatAction != null) {
                if (!$util.isObject(object.deleteChatAction))
                    throw TypeError(".proto.SyncActionValue.deleteChatAction: object expected");
                message.deleteChatAction = $root.proto.SyncActionValue.DeleteChatAction.fromObject(object.deleteChatAction, q + 1);
            }
            if (object.unarchiveChatsSetting != null) {
                if (!$util.isObject(object.unarchiveChatsSetting))
                    throw TypeError(".proto.SyncActionValue.unarchiveChatsSetting: object expected");
                message.unarchiveChatsSetting = $root.proto.SyncActionValue.UnarchiveChatsSetting.fromObject(object.unarchiveChatsSetting, q + 1);
            }
            if (object.primaryFeature != null) {
                if (!$util.isObject(object.primaryFeature))
                    throw TypeError(".proto.SyncActionValue.primaryFeature: object expected");
                message.primaryFeature = $root.proto.SyncActionValue.PrimaryFeature.fromObject(object.primaryFeature, q + 1);
            }
            if (object.androidUnsupportedActions != null) {
                if (!$util.isObject(object.androidUnsupportedActions))
                    throw TypeError(".proto.SyncActionValue.androidUnsupportedActions: object expected");
                message.androidUnsupportedActions = $root.proto.SyncActionValue.AndroidUnsupportedActions.fromObject(object.androidUnsupportedActions, q + 1);
            }
            if (object.agentAction != null) {
                if (!$util.isObject(object.agentAction))
                    throw TypeError(".proto.SyncActionValue.agentAction: object expected");
                message.agentAction = $root.proto.SyncActionValue.AgentAction.fromObject(object.agentAction, q + 1);
            }
            if (object.subscriptionAction != null) {
                if (!$util.isObject(object.subscriptionAction))
                    throw TypeError(".proto.SyncActionValue.subscriptionAction: object expected");
                message.subscriptionAction = $root.proto.SyncActionValue.SubscriptionAction.fromObject(object.subscriptionAction, q + 1);
            }
            if (object.userStatusMuteAction != null) {
                if (!$util.isObject(object.userStatusMuteAction))
                    throw TypeError(".proto.SyncActionValue.userStatusMuteAction: object expected");
                message.userStatusMuteAction = $root.proto.SyncActionValue.UserStatusMuteAction.fromObject(object.userStatusMuteAction, q + 1);
            }
            if (object.timeFormatAction != null) {
                if (!$util.isObject(object.timeFormatAction))
                    throw TypeError(".proto.SyncActionValue.timeFormatAction: object expected");
                message.timeFormatAction = $root.proto.SyncActionValue.TimeFormatAction.fromObject(object.timeFormatAction, q + 1);
            }
            if (object.nuxAction != null) {
                if (!$util.isObject(object.nuxAction))
                    throw TypeError(".proto.SyncActionValue.nuxAction: object expected");
                message.nuxAction = $root.proto.SyncActionValue.NuxAction.fromObject(object.nuxAction, q + 1);
            }
            if (object.primaryVersionAction != null) {
                if (!$util.isObject(object.primaryVersionAction))
                    throw TypeError(".proto.SyncActionValue.primaryVersionAction: object expected");
                message.primaryVersionAction = $root.proto.SyncActionValue.PrimaryVersionAction.fromObject(object.primaryVersionAction, q + 1);
            }
            if (object.stickerAction != null) {
                if (!$util.isObject(object.stickerAction))
                    throw TypeError(".proto.SyncActionValue.stickerAction: object expected");
                message.stickerAction = $root.proto.SyncActionValue.StickerAction.fromObject(object.stickerAction, q + 1);
            }
            if (object.removeRecentStickerAction != null) {
                if (!$util.isObject(object.removeRecentStickerAction))
                    throw TypeError(".proto.SyncActionValue.removeRecentStickerAction: object expected");
                message.removeRecentStickerAction = $root.proto.SyncActionValue.RemoveRecentStickerAction.fromObject(object.removeRecentStickerAction, q + 1);
            }
            if (object.chatAssignment != null) {
                if (!$util.isObject(object.chatAssignment))
                    throw TypeError(".proto.SyncActionValue.chatAssignment: object expected");
                message.chatAssignment = $root.proto.SyncActionValue.ChatAssignmentAction.fromObject(object.chatAssignment, q + 1);
            }
            if (object.chatAssignmentOpenedStatus != null) {
                if (!$util.isObject(object.chatAssignmentOpenedStatus))
                    throw TypeError(".proto.SyncActionValue.chatAssignmentOpenedStatus: object expected");
                message.chatAssignmentOpenedStatus = $root.proto.SyncActionValue.ChatAssignmentOpenedStatusAction.fromObject(object.chatAssignmentOpenedStatus, q + 1);
            }
            if (object.pnForLidChatAction != null) {
                if (!$util.isObject(object.pnForLidChatAction))
                    throw TypeError(".proto.SyncActionValue.pnForLidChatAction: object expected");
                message.pnForLidChatAction = $root.proto.SyncActionValue.PnForLidChatAction.fromObject(object.pnForLidChatAction, q + 1);
            }
            if (object.marketingMessageAction != null) {
                if (!$util.isObject(object.marketingMessageAction))
                    throw TypeError(".proto.SyncActionValue.marketingMessageAction: object expected");
                message.marketingMessageAction = $root.proto.SyncActionValue.MarketingMessageAction.fromObject(object.marketingMessageAction, q + 1);
            }
            if (object.marketingMessageBroadcastAction != null) {
                if (!$util.isObject(object.marketingMessageBroadcastAction))
                    throw TypeError(".proto.SyncActionValue.marketingMessageBroadcastAction: object expected");
                message.marketingMessageBroadcastAction = $root.proto.SyncActionValue.MarketingMessageBroadcastAction.fromObject(object.marketingMessageBroadcastAction, q + 1);
            }
            if (object.externalWebBetaAction != null) {
                if (!$util.isObject(object.externalWebBetaAction))
                    throw TypeError(".proto.SyncActionValue.externalWebBetaAction: object expected");
                message.externalWebBetaAction = $root.proto.SyncActionValue.ExternalWebBetaAction.fromObject(object.externalWebBetaAction, q + 1);
            }
            if (object.privacySettingRelayAllCalls != null) {
                if (!$util.isObject(object.privacySettingRelayAllCalls))
                    throw TypeError(".proto.SyncActionValue.privacySettingRelayAllCalls: object expected");
                message.privacySettingRelayAllCalls = $root.proto.SyncActionValue.PrivacySettingRelayAllCalls.fromObject(object.privacySettingRelayAllCalls, q + 1);
            }
            if (object.callLogAction != null) {
                if (!$util.isObject(object.callLogAction))
                    throw TypeError(".proto.SyncActionValue.callLogAction: object expected");
                message.callLogAction = $root.proto.SyncActionValue.CallLogAction.fromObject(object.callLogAction, q + 1);
            }
            if (object.ugcBot != null) {
                if (!$util.isObject(object.ugcBot))
                    throw TypeError(".proto.SyncActionValue.ugcBot: object expected");
                message.ugcBot = $root.proto.SyncActionValue.UGCBot.fromObject(object.ugcBot, q + 1);
            }
            if (object.statusPrivacy != null) {
                if (!$util.isObject(object.statusPrivacy))
                    throw TypeError(".proto.SyncActionValue.statusPrivacy: object expected");
                message.statusPrivacy = $root.proto.SyncActionValue.StatusPrivacyAction.fromObject(object.statusPrivacy, q + 1);
            }
            if (object.botWelcomeRequestAction != null) {
                if (!$util.isObject(object.botWelcomeRequestAction))
                    throw TypeError(".proto.SyncActionValue.botWelcomeRequestAction: object expected");
                message.botWelcomeRequestAction = $root.proto.SyncActionValue.BotWelcomeRequestAction.fromObject(object.botWelcomeRequestAction, q + 1);
            }
            if (object.deleteIndividualCallLog != null) {
                if (!$util.isObject(object.deleteIndividualCallLog))
                    throw TypeError(".proto.SyncActionValue.deleteIndividualCallLog: object expected");
                message.deleteIndividualCallLog = $root.proto.SyncActionValue.DeleteIndividualCallLogAction.fromObject(object.deleteIndividualCallLog, q + 1);
            }
            if (object.labelReorderingAction != null) {
                if (!$util.isObject(object.labelReorderingAction))
                    throw TypeError(".proto.SyncActionValue.labelReorderingAction: object expected");
                message.labelReorderingAction = $root.proto.SyncActionValue.LabelReorderingAction.fromObject(object.labelReorderingAction, q + 1);
            }
            if (object.paymentInfoAction != null) {
                if (!$util.isObject(object.paymentInfoAction))
                    throw TypeError(".proto.SyncActionValue.paymentInfoAction: object expected");
                message.paymentInfoAction = $root.proto.SyncActionValue.PaymentInfoAction.fromObject(object.paymentInfoAction, q + 1);
            }
            if (object.customPaymentMethodsAction != null) {
                if (!$util.isObject(object.customPaymentMethodsAction))
                    throw TypeError(".proto.SyncActionValue.customPaymentMethodsAction: object expected");
                message.customPaymentMethodsAction = $root.proto.SyncActionValue.CustomPaymentMethodsAction.fromObject(object.customPaymentMethodsAction, q + 1);
            }
            if (object.lockChatAction != null) {
                if (!$util.isObject(object.lockChatAction))
                    throw TypeError(".proto.SyncActionValue.lockChatAction: object expected");
                message.lockChatAction = $root.proto.SyncActionValue.LockChatAction.fromObject(object.lockChatAction, q + 1);
            }
            if (object.chatLockSettings != null) {
                if (!$util.isObject(object.chatLockSettings))
                    throw TypeError(".proto.SyncActionValue.chatLockSettings: object expected");
                message.chatLockSettings = $root.proto.ChatLockSettings.fromObject(object.chatLockSettings, q + 1);
            }
            if (object.wamoUserIdentifierAction != null) {
                if (!$util.isObject(object.wamoUserIdentifierAction))
                    throw TypeError(".proto.SyncActionValue.wamoUserIdentifierAction: object expected");
                message.wamoUserIdentifierAction = $root.proto.SyncActionValue.WamoUserIdentifierAction.fromObject(object.wamoUserIdentifierAction, q + 1);
            }
            if (object.privacySettingDisableLinkPreviewsAction != null) {
                if (!$util.isObject(object.privacySettingDisableLinkPreviewsAction))
                    throw TypeError(".proto.SyncActionValue.privacySettingDisableLinkPreviewsAction: object expected");
                message.privacySettingDisableLinkPreviewsAction = $root.proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction.fromObject(object.privacySettingDisableLinkPreviewsAction, q + 1);
            }
            if (object.deviceCapabilities != null) {
                if (!$util.isObject(object.deviceCapabilities))
                    throw TypeError(".proto.SyncActionValue.deviceCapabilities: object expected");
                message.deviceCapabilities = $root.proto.DeviceCapabilities.fromObject(object.deviceCapabilities, q + 1);
            }
            if (object.noteEditAction != null) {
                if (!$util.isObject(object.noteEditAction))
                    throw TypeError(".proto.SyncActionValue.noteEditAction: object expected");
                message.noteEditAction = $root.proto.SyncActionValue.NoteEditAction.fromObject(object.noteEditAction, q + 1);
            }
            if (object.favoritesAction != null) {
                if (!$util.isObject(object.favoritesAction))
                    throw TypeError(".proto.SyncActionValue.favoritesAction: object expected");
                message.favoritesAction = $root.proto.SyncActionValue.FavoritesAction.fromObject(object.favoritesAction, q + 1);
            }
            if (object.merchantPaymentPartnerAction != null) {
                if (!$util.isObject(object.merchantPaymentPartnerAction))
                    throw TypeError(".proto.SyncActionValue.merchantPaymentPartnerAction: object expected");
                message.merchantPaymentPartnerAction = $root.proto.SyncActionValue.MerchantPaymentPartnerAction.fromObject(object.merchantPaymentPartnerAction, q + 1);
            }
            if (object.waffleAccountLinkStateAction != null) {
                if (!$util.isObject(object.waffleAccountLinkStateAction))
                    throw TypeError(".proto.SyncActionValue.waffleAccountLinkStateAction: object expected");
                message.waffleAccountLinkStateAction = $root.proto.SyncActionValue.WaffleAccountLinkStateAction.fromObject(object.waffleAccountLinkStateAction, q + 1);
            }
            if (object.usernameChatStartMode != null) {
                if (!$util.isObject(object.usernameChatStartMode))
                    throw TypeError(".proto.SyncActionValue.usernameChatStartMode: object expected");
                message.usernameChatStartMode = $root.proto.SyncActionValue.UsernameChatStartModeAction.fromObject(object.usernameChatStartMode, q + 1);
            }
            if (object.notificationActivitySettingAction != null) {
                if (!$util.isObject(object.notificationActivitySettingAction))
                    throw TypeError(".proto.SyncActionValue.notificationActivitySettingAction: object expected");
                message.notificationActivitySettingAction = $root.proto.SyncActionValue.NotificationActivitySettingAction.fromObject(object.notificationActivitySettingAction, q + 1);
            }
            if (object.lidContactAction != null) {
                if (!$util.isObject(object.lidContactAction))
                    throw TypeError(".proto.SyncActionValue.lidContactAction: object expected");
                message.lidContactAction = $root.proto.SyncActionValue.LidContactAction.fromObject(object.lidContactAction, q + 1);
            }
            if (object.ctwaPerCustomerDataSharingAction != null) {
                if (!$util.isObject(object.ctwaPerCustomerDataSharingAction))
                    throw TypeError(".proto.SyncActionValue.ctwaPerCustomerDataSharingAction: object expected");
                message.ctwaPerCustomerDataSharingAction = $root.proto.SyncActionValue.CtwaPerCustomerDataSharingAction.fromObject(object.ctwaPerCustomerDataSharingAction, q + 1);
            }
            if (object.paymentTosAction != null) {
                if (!$util.isObject(object.paymentTosAction))
                    throw TypeError(".proto.SyncActionValue.paymentTosAction: object expected");
                message.paymentTosAction = $root.proto.SyncActionValue.PaymentTosAction.fromObject(object.paymentTosAction, q + 1);
            }
            if (object.privacySettingChannelsPersonalisedRecommendationAction != null) {
                if (!$util.isObject(object.privacySettingChannelsPersonalisedRecommendationAction))
                    throw TypeError(".proto.SyncActionValue.privacySettingChannelsPersonalisedRecommendationAction: object expected");
                message.privacySettingChannelsPersonalisedRecommendationAction = $root.proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction.fromObject(object.privacySettingChannelsPersonalisedRecommendationAction, q + 1);
            }
            if (object.detectedOutcomesStatusAction != null) {
                if (!$util.isObject(object.detectedOutcomesStatusAction))
                    throw TypeError(".proto.SyncActionValue.detectedOutcomesStatusAction: object expected");
                message.detectedOutcomesStatusAction = $root.proto.SyncActionValue.DetectedOutcomesStatusAction.fromObject(object.detectedOutcomesStatusAction, q + 1);
            }
            if (object.maibaAiFeaturesControlAction != null) {
                if (!$util.isObject(object.maibaAiFeaturesControlAction))
                    throw TypeError(".proto.SyncActionValue.maibaAiFeaturesControlAction: object expected");
                message.maibaAiFeaturesControlAction = $root.proto.SyncActionValue.MaibaAIFeaturesControlAction.fromObject(object.maibaAiFeaturesControlAction, q + 1);
            }
            if (object.businessBroadcastListAction != null) {
                if (!$util.isObject(object.businessBroadcastListAction))
                    throw TypeError(".proto.SyncActionValue.businessBroadcastListAction: object expected");
                message.businessBroadcastListAction = $root.proto.SyncActionValue.BusinessBroadcastListAction.fromObject(object.businessBroadcastListAction, q + 1);
            }
            if (object.musicUserIdAction != null) {
                if (!$util.isObject(object.musicUserIdAction))
                    throw TypeError(".proto.SyncActionValue.musicUserIdAction: object expected");
                message.musicUserIdAction = $root.proto.SyncActionValue.MusicUserIdAction.fromObject(object.musicUserIdAction, q + 1);
            }
            if (object.statusPostOptInNotificationPreferencesAction != null) {
                if (!$util.isObject(object.statusPostOptInNotificationPreferencesAction))
                    throw TypeError(".proto.SyncActionValue.statusPostOptInNotificationPreferencesAction: object expected");
                message.statusPostOptInNotificationPreferencesAction = $root.proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction.fromObject(object.statusPostOptInNotificationPreferencesAction, q + 1);
            }
            if (object.avatarUpdatedAction != null) {
                if (!$util.isObject(object.avatarUpdatedAction))
                    throw TypeError(".proto.SyncActionValue.avatarUpdatedAction: object expected");
                message.avatarUpdatedAction = $root.proto.SyncActionValue.AvatarUpdatedAction.fromObject(object.avatarUpdatedAction, q + 1);
            }
            if (object.privateProcessingSettingAction != null) {
                if (!$util.isObject(object.privateProcessingSettingAction))
                    throw TypeError(".proto.SyncActionValue.privateProcessingSettingAction: object expected");
                message.privateProcessingSettingAction = $root.proto.SyncActionValue.PrivateProcessingSettingAction.fromObject(object.privateProcessingSettingAction, q + 1);
            }
            if (object.newsletterSavedInterestsAction != null) {
                if (!$util.isObject(object.newsletterSavedInterestsAction))
                    throw TypeError(".proto.SyncActionValue.newsletterSavedInterestsAction: object expected");
                message.newsletterSavedInterestsAction = $root.proto.SyncActionValue.NewsletterSavedInterestsAction.fromObject(object.newsletterSavedInterestsAction, q + 1);
            }
            if (object.aiThreadRenameAction != null) {
                if (!$util.isObject(object.aiThreadRenameAction))
                    throw TypeError(".proto.SyncActionValue.aiThreadRenameAction: object expected");
                message.aiThreadRenameAction = $root.proto.SyncActionValue.AiThreadRenameAction.fromObject(object.aiThreadRenameAction, q + 1);
            }
            if (object.interactiveMessageAction != null) {
                if (!$util.isObject(object.interactiveMessageAction))
                    throw TypeError(".proto.SyncActionValue.interactiveMessageAction: object expected");
                message.interactiveMessageAction = $root.proto.SyncActionValue.InteractiveMessageAction.fromObject(object.interactiveMessageAction, q + 1);
            }
            if (object.settingsSyncAction != null) {
                if (!$util.isObject(object.settingsSyncAction))
                    throw TypeError(".proto.SyncActionValue.settingsSyncAction: object expected");
                message.settingsSyncAction = $root.proto.SyncActionValue.SettingsSyncAction.fromObject(object.settingsSyncAction, q + 1);
            }
            if (object.outContactAction != null) {
                if (!$util.isObject(object.outContactAction))
                    throw TypeError(".proto.SyncActionValue.outContactAction: object expected");
                message.outContactAction = $root.proto.SyncActionValue.OutContactAction.fromObject(object.outContactAction, q + 1);
            }
            if (object.nctSaltSyncAction != null) {
                if (!$util.isObject(object.nctSaltSyncAction))
                    throw TypeError(".proto.SyncActionValue.nctSaltSyncAction: object expected");
                message.nctSaltSyncAction = $root.proto.SyncActionValue.NctSaltSyncAction.fromObject(object.nctSaltSyncAction, q + 1);
            }
            if (object.businessBroadcastCampaignAction != null) {
                if (!$util.isObject(object.businessBroadcastCampaignAction))
                    throw TypeError(".proto.SyncActionValue.businessBroadcastCampaignAction: object expected");
                message.businessBroadcastCampaignAction = $root.proto.SyncActionValue.BusinessBroadcastCampaignAction.fromObject(object.businessBroadcastCampaignAction, q + 1);
            }
            if (object.businessBroadcastInsightsAction != null) {
                if (!$util.isObject(object.businessBroadcastInsightsAction))
                    throw TypeError(".proto.SyncActionValue.businessBroadcastInsightsAction: object expected");
                message.businessBroadcastInsightsAction = $root.proto.SyncActionValue.BusinessBroadcastInsightsAction.fromObject(object.businessBroadcastInsightsAction, q + 1);
            }
            if (object.customerDataAction != null) {
                if (!$util.isObject(object.customerDataAction))
                    throw TypeError(".proto.SyncActionValue.customerDataAction: object expected");
                message.customerDataAction = $root.proto.SyncActionValue.CustomerDataAction.fromObject(object.customerDataAction, q + 1);
            }
            if (object.subscriptionsSyncV2Action != null) {
                if (!$util.isObject(object.subscriptionsSyncV2Action))
                    throw TypeError(".proto.SyncActionValue.subscriptionsSyncV2Action: object expected");
                message.subscriptionsSyncV2Action = $root.proto.SyncActionValue.SubscriptionsSyncV2Action.fromObject(object.subscriptionsSyncV2Action, q + 1);
            }
            if (object.threadPinAction != null) {
                if (!$util.isObject(object.threadPinAction))
                    throw TypeError(".proto.SyncActionValue.threadPinAction: object expected");
                message.threadPinAction = $root.proto.SyncActionValue.ThreadPinAction.fromObject(object.threadPinAction, q + 1);
            }
            if (object.autoOrganizeBusinessChatSetting != null) {
                if (!$util.isObject(object.autoOrganizeBusinessChatSetting))
                    throw TypeError(".proto.SyncActionValue.autoOrganizeBusinessChatSetting: object expected");
                message.autoOrganizeBusinessChatSetting = $root.proto.SyncActionValue.AutoOrganizeBusinessChatSetting.fromObject(object.autoOrganizeBusinessChatSetting, q + 1);
            }
            if (object.bizAiSettingsNudgeAction != null) {
                if (!$util.isObject(object.bizAiSettingsNudgeAction))
                    throw TypeError(".proto.SyncActionValue.bizAiSettingsNudgeAction: object expected");
                message.bizAiSettingsNudgeAction = $root.proto.SyncActionValue.BizAISettingsNudgeAction.fromObject(object.bizAiSettingsNudgeAction, q + 1);
            }
            if (object.coexV2VersionAction != null) {
                if (!$util.isObject(object.coexV2VersionAction))
                    throw TypeError(".proto.SyncActionValue.coexV2VersionAction: object expected");
                message.coexV2VersionAction = $root.proto.SyncActionValue.CoexV2VersionAction.fromObject(object.coexV2VersionAction, q + 1);
            }
            if (object.wasaRootSecretAction != null) {
                if (!$util.isObject(object.wasaRootSecretAction))
                    throw TypeError(".proto.SyncActionValue.wasaRootSecretAction: object expected");
                message.wasaRootSecretAction = $root.proto.SyncActionValue.WASARootSecretAction.fromObject(object.wasaRootSecretAction, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a SyncActionValue message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SyncActionValue
         * @static
         * @param {proto.SyncActionValue} message SyncActionValue
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SyncActionValue.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.timestamp = typeof message.timestamp === "number" ? BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
                else if (typeof message.timestamp === "number")
                    object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                else
                    object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
            if (message.starAction != null && Object.hasOwnProperty.call(message, "starAction"))
                object.starAction = $root.proto.SyncActionValue.StarAction.toObject(message.starAction, options, q + 1);
            if (message.contactAction != null && Object.hasOwnProperty.call(message, "contactAction"))
                object.contactAction = $root.proto.SyncActionValue.ContactAction.toObject(message.contactAction, options, q + 1);
            if (message.muteAction != null && Object.hasOwnProperty.call(message, "muteAction"))
                object.muteAction = $root.proto.SyncActionValue.MuteAction.toObject(message.muteAction, options, q + 1);
            if (message.pinAction != null && Object.hasOwnProperty.call(message, "pinAction"))
                object.pinAction = $root.proto.SyncActionValue.PinAction.toObject(message.pinAction, options, q + 1);
            if (message.pushNameSetting != null && Object.hasOwnProperty.call(message, "pushNameSetting"))
                object.pushNameSetting = $root.proto.SyncActionValue.PushNameSetting.toObject(message.pushNameSetting, options, q + 1);
            if (message.quickReplyAction != null && Object.hasOwnProperty.call(message, "quickReplyAction"))
                object.quickReplyAction = $root.proto.SyncActionValue.QuickReplyAction.toObject(message.quickReplyAction, options, q + 1);
            if (message.recentEmojiWeightsAction != null && Object.hasOwnProperty.call(message, "recentEmojiWeightsAction"))
                object.recentEmojiWeightsAction = $root.proto.SyncActionValue.RecentEmojiWeightsAction.toObject(message.recentEmojiWeightsAction, options, q + 1);
            if (message.labelEditAction != null && Object.hasOwnProperty.call(message, "labelEditAction"))
                object.labelEditAction = $root.proto.SyncActionValue.LabelEditAction.toObject(message.labelEditAction, options, q + 1);
            if (message.labelAssociationAction != null && Object.hasOwnProperty.call(message, "labelAssociationAction"))
                object.labelAssociationAction = $root.proto.SyncActionValue.LabelAssociationAction.toObject(message.labelAssociationAction, options, q + 1);
            if (message.localeSetting != null && Object.hasOwnProperty.call(message, "localeSetting"))
                object.localeSetting = $root.proto.SyncActionValue.LocaleSetting.toObject(message.localeSetting, options, q + 1);
            if (message.archiveChatAction != null && Object.hasOwnProperty.call(message, "archiveChatAction"))
                object.archiveChatAction = $root.proto.SyncActionValue.ArchiveChatAction.toObject(message.archiveChatAction, options, q + 1);
            if (message.deleteMessageForMeAction != null && Object.hasOwnProperty.call(message, "deleteMessageForMeAction"))
                object.deleteMessageForMeAction = $root.proto.SyncActionValue.DeleteMessageForMeAction.toObject(message.deleteMessageForMeAction, options, q + 1);
            if (message.keyExpiration != null && Object.hasOwnProperty.call(message, "keyExpiration"))
                object.keyExpiration = $root.proto.SyncActionValue.KeyExpiration.toObject(message.keyExpiration, options, q + 1);
            if (message.markChatAsReadAction != null && Object.hasOwnProperty.call(message, "markChatAsReadAction"))
                object.markChatAsReadAction = $root.proto.SyncActionValue.MarkChatAsReadAction.toObject(message.markChatAsReadAction, options, q + 1);
            if (message.clearChatAction != null && Object.hasOwnProperty.call(message, "clearChatAction"))
                object.clearChatAction = $root.proto.SyncActionValue.ClearChatAction.toObject(message.clearChatAction, options, q + 1);
            if (message.deleteChatAction != null && Object.hasOwnProperty.call(message, "deleteChatAction"))
                object.deleteChatAction = $root.proto.SyncActionValue.DeleteChatAction.toObject(message.deleteChatAction, options, q + 1);
            if (message.unarchiveChatsSetting != null && Object.hasOwnProperty.call(message, "unarchiveChatsSetting"))
                object.unarchiveChatsSetting = $root.proto.SyncActionValue.UnarchiveChatsSetting.toObject(message.unarchiveChatsSetting, options, q + 1);
            if (message.primaryFeature != null && Object.hasOwnProperty.call(message, "primaryFeature"))
                object.primaryFeature = $root.proto.SyncActionValue.PrimaryFeature.toObject(message.primaryFeature, options, q + 1);
            if (message.androidUnsupportedActions != null && Object.hasOwnProperty.call(message, "androidUnsupportedActions"))
                object.androidUnsupportedActions = $root.proto.SyncActionValue.AndroidUnsupportedActions.toObject(message.androidUnsupportedActions, options, q + 1);
            if (message.agentAction != null && Object.hasOwnProperty.call(message, "agentAction"))
                object.agentAction = $root.proto.SyncActionValue.AgentAction.toObject(message.agentAction, options, q + 1);
            if (message.subscriptionAction != null && Object.hasOwnProperty.call(message, "subscriptionAction"))
                object.subscriptionAction = $root.proto.SyncActionValue.SubscriptionAction.toObject(message.subscriptionAction, options, q + 1);
            if (message.userStatusMuteAction != null && Object.hasOwnProperty.call(message, "userStatusMuteAction"))
                object.userStatusMuteAction = $root.proto.SyncActionValue.UserStatusMuteAction.toObject(message.userStatusMuteAction, options, q + 1);
            if (message.timeFormatAction != null && Object.hasOwnProperty.call(message, "timeFormatAction"))
                object.timeFormatAction = $root.proto.SyncActionValue.TimeFormatAction.toObject(message.timeFormatAction, options, q + 1);
            if (message.nuxAction != null && Object.hasOwnProperty.call(message, "nuxAction"))
                object.nuxAction = $root.proto.SyncActionValue.NuxAction.toObject(message.nuxAction, options, q + 1);
            if (message.primaryVersionAction != null && Object.hasOwnProperty.call(message, "primaryVersionAction"))
                object.primaryVersionAction = $root.proto.SyncActionValue.PrimaryVersionAction.toObject(message.primaryVersionAction, options, q + 1);
            if (message.stickerAction != null && Object.hasOwnProperty.call(message, "stickerAction"))
                object.stickerAction = $root.proto.SyncActionValue.StickerAction.toObject(message.stickerAction, options, q + 1);
            if (message.removeRecentStickerAction != null && Object.hasOwnProperty.call(message, "removeRecentStickerAction"))
                object.removeRecentStickerAction = $root.proto.SyncActionValue.RemoveRecentStickerAction.toObject(message.removeRecentStickerAction, options, q + 1);
            if (message.chatAssignment != null && Object.hasOwnProperty.call(message, "chatAssignment"))
                object.chatAssignment = $root.proto.SyncActionValue.ChatAssignmentAction.toObject(message.chatAssignment, options, q + 1);
            if (message.chatAssignmentOpenedStatus != null && Object.hasOwnProperty.call(message, "chatAssignmentOpenedStatus"))
                object.chatAssignmentOpenedStatus = $root.proto.SyncActionValue.ChatAssignmentOpenedStatusAction.toObject(message.chatAssignmentOpenedStatus, options, q + 1);
            if (message.pnForLidChatAction != null && Object.hasOwnProperty.call(message, "pnForLidChatAction"))
                object.pnForLidChatAction = $root.proto.SyncActionValue.PnForLidChatAction.toObject(message.pnForLidChatAction, options, q + 1);
            if (message.marketingMessageAction != null && Object.hasOwnProperty.call(message, "marketingMessageAction"))
                object.marketingMessageAction = $root.proto.SyncActionValue.MarketingMessageAction.toObject(message.marketingMessageAction, options, q + 1);
            if (message.marketingMessageBroadcastAction != null && Object.hasOwnProperty.call(message, "marketingMessageBroadcastAction"))
                object.marketingMessageBroadcastAction = $root.proto.SyncActionValue.MarketingMessageBroadcastAction.toObject(message.marketingMessageBroadcastAction, options, q + 1);
            if (message.externalWebBetaAction != null && Object.hasOwnProperty.call(message, "externalWebBetaAction"))
                object.externalWebBetaAction = $root.proto.SyncActionValue.ExternalWebBetaAction.toObject(message.externalWebBetaAction, options, q + 1);
            if (message.privacySettingRelayAllCalls != null && Object.hasOwnProperty.call(message, "privacySettingRelayAllCalls"))
                object.privacySettingRelayAllCalls = $root.proto.SyncActionValue.PrivacySettingRelayAllCalls.toObject(message.privacySettingRelayAllCalls, options, q + 1);
            if (message.callLogAction != null && Object.hasOwnProperty.call(message, "callLogAction"))
                object.callLogAction = $root.proto.SyncActionValue.CallLogAction.toObject(message.callLogAction, options, q + 1);
            if (message.ugcBot != null && Object.hasOwnProperty.call(message, "ugcBot"))
                object.ugcBot = $root.proto.SyncActionValue.UGCBot.toObject(message.ugcBot, options, q + 1);
            if (message.statusPrivacy != null && Object.hasOwnProperty.call(message, "statusPrivacy"))
                object.statusPrivacy = $root.proto.SyncActionValue.StatusPrivacyAction.toObject(message.statusPrivacy, options, q + 1);
            if (message.botWelcomeRequestAction != null && Object.hasOwnProperty.call(message, "botWelcomeRequestAction"))
                object.botWelcomeRequestAction = $root.proto.SyncActionValue.BotWelcomeRequestAction.toObject(message.botWelcomeRequestAction, options, q + 1);
            if (message.deleteIndividualCallLog != null && Object.hasOwnProperty.call(message, "deleteIndividualCallLog"))
                object.deleteIndividualCallLog = $root.proto.SyncActionValue.DeleteIndividualCallLogAction.toObject(message.deleteIndividualCallLog, options, q + 1);
            if (message.labelReorderingAction != null && Object.hasOwnProperty.call(message, "labelReorderingAction"))
                object.labelReorderingAction = $root.proto.SyncActionValue.LabelReorderingAction.toObject(message.labelReorderingAction, options, q + 1);
            if (message.paymentInfoAction != null && Object.hasOwnProperty.call(message, "paymentInfoAction"))
                object.paymentInfoAction = $root.proto.SyncActionValue.PaymentInfoAction.toObject(message.paymentInfoAction, options, q + 1);
            if (message.customPaymentMethodsAction != null && Object.hasOwnProperty.call(message, "customPaymentMethodsAction"))
                object.customPaymentMethodsAction = $root.proto.SyncActionValue.CustomPaymentMethodsAction.toObject(message.customPaymentMethodsAction, options, q + 1);
            if (message.lockChatAction != null && Object.hasOwnProperty.call(message, "lockChatAction"))
                object.lockChatAction = $root.proto.SyncActionValue.LockChatAction.toObject(message.lockChatAction, options, q + 1);
            if (message.chatLockSettings != null && Object.hasOwnProperty.call(message, "chatLockSettings"))
                object.chatLockSettings = $root.proto.ChatLockSettings.toObject(message.chatLockSettings, options, q + 1);
            if (message.wamoUserIdentifierAction != null && Object.hasOwnProperty.call(message, "wamoUserIdentifierAction"))
                object.wamoUserIdentifierAction = $root.proto.SyncActionValue.WamoUserIdentifierAction.toObject(message.wamoUserIdentifierAction, options, q + 1);
            if (message.privacySettingDisableLinkPreviewsAction != null && Object.hasOwnProperty.call(message, "privacySettingDisableLinkPreviewsAction"))
                object.privacySettingDisableLinkPreviewsAction = $root.proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction.toObject(message.privacySettingDisableLinkPreviewsAction, options, q + 1);
            if (message.deviceCapabilities != null && Object.hasOwnProperty.call(message, "deviceCapabilities"))
                object.deviceCapabilities = $root.proto.DeviceCapabilities.toObject(message.deviceCapabilities, options, q + 1);
            if (message.noteEditAction != null && Object.hasOwnProperty.call(message, "noteEditAction"))
                object.noteEditAction = $root.proto.SyncActionValue.NoteEditAction.toObject(message.noteEditAction, options, q + 1);
            if (message.favoritesAction != null && Object.hasOwnProperty.call(message, "favoritesAction"))
                object.favoritesAction = $root.proto.SyncActionValue.FavoritesAction.toObject(message.favoritesAction, options, q + 1);
            if (message.merchantPaymentPartnerAction != null && Object.hasOwnProperty.call(message, "merchantPaymentPartnerAction"))
                object.merchantPaymentPartnerAction = $root.proto.SyncActionValue.MerchantPaymentPartnerAction.toObject(message.merchantPaymentPartnerAction, options, q + 1);
            if (message.waffleAccountLinkStateAction != null && Object.hasOwnProperty.call(message, "waffleAccountLinkStateAction"))
                object.waffleAccountLinkStateAction = $root.proto.SyncActionValue.WaffleAccountLinkStateAction.toObject(message.waffleAccountLinkStateAction, options, q + 1);
            if (message.usernameChatStartMode != null && Object.hasOwnProperty.call(message, "usernameChatStartMode"))
                object.usernameChatStartMode = $root.proto.SyncActionValue.UsernameChatStartModeAction.toObject(message.usernameChatStartMode, options, q + 1);
            if (message.notificationActivitySettingAction != null && Object.hasOwnProperty.call(message, "notificationActivitySettingAction"))
                object.notificationActivitySettingAction = $root.proto.SyncActionValue.NotificationActivitySettingAction.toObject(message.notificationActivitySettingAction, options, q + 1);
            if (message.lidContactAction != null && Object.hasOwnProperty.call(message, "lidContactAction"))
                object.lidContactAction = $root.proto.SyncActionValue.LidContactAction.toObject(message.lidContactAction, options, q + 1);
            if (message.ctwaPerCustomerDataSharingAction != null && Object.hasOwnProperty.call(message, "ctwaPerCustomerDataSharingAction"))
                object.ctwaPerCustomerDataSharingAction = $root.proto.SyncActionValue.CtwaPerCustomerDataSharingAction.toObject(message.ctwaPerCustomerDataSharingAction, options, q + 1);
            if (message.paymentTosAction != null && Object.hasOwnProperty.call(message, "paymentTosAction"))
                object.paymentTosAction = $root.proto.SyncActionValue.PaymentTosAction.toObject(message.paymentTosAction, options, q + 1);
            if (message.privacySettingChannelsPersonalisedRecommendationAction != null && Object.hasOwnProperty.call(message, "privacySettingChannelsPersonalisedRecommendationAction"))
                object.privacySettingChannelsPersonalisedRecommendationAction = $root.proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction.toObject(message.privacySettingChannelsPersonalisedRecommendationAction, options, q + 1);
            if (message.detectedOutcomesStatusAction != null && Object.hasOwnProperty.call(message, "detectedOutcomesStatusAction"))
                object.detectedOutcomesStatusAction = $root.proto.SyncActionValue.DetectedOutcomesStatusAction.toObject(message.detectedOutcomesStatusAction, options, q + 1);
            if (message.maibaAiFeaturesControlAction != null && Object.hasOwnProperty.call(message, "maibaAiFeaturesControlAction"))
                object.maibaAiFeaturesControlAction = $root.proto.SyncActionValue.MaibaAIFeaturesControlAction.toObject(message.maibaAiFeaturesControlAction, options, q + 1);
            if (message.businessBroadcastListAction != null && Object.hasOwnProperty.call(message, "businessBroadcastListAction"))
                object.businessBroadcastListAction = $root.proto.SyncActionValue.BusinessBroadcastListAction.toObject(message.businessBroadcastListAction, options, q + 1);
            if (message.musicUserIdAction != null && Object.hasOwnProperty.call(message, "musicUserIdAction"))
                object.musicUserIdAction = $root.proto.SyncActionValue.MusicUserIdAction.toObject(message.musicUserIdAction, options, q + 1);
            if (message.statusPostOptInNotificationPreferencesAction != null && Object.hasOwnProperty.call(message, "statusPostOptInNotificationPreferencesAction"))
                object.statusPostOptInNotificationPreferencesAction = $root.proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction.toObject(message.statusPostOptInNotificationPreferencesAction, options, q + 1);
            if (message.avatarUpdatedAction != null && Object.hasOwnProperty.call(message, "avatarUpdatedAction"))
                object.avatarUpdatedAction = $root.proto.SyncActionValue.AvatarUpdatedAction.toObject(message.avatarUpdatedAction, options, q + 1);
            if (message.privateProcessingSettingAction != null && Object.hasOwnProperty.call(message, "privateProcessingSettingAction"))
                object.privateProcessingSettingAction = $root.proto.SyncActionValue.PrivateProcessingSettingAction.toObject(message.privateProcessingSettingAction, options, q + 1);
            if (message.newsletterSavedInterestsAction != null && Object.hasOwnProperty.call(message, "newsletterSavedInterestsAction"))
                object.newsletterSavedInterestsAction = $root.proto.SyncActionValue.NewsletterSavedInterestsAction.toObject(message.newsletterSavedInterestsAction, options, q + 1);
            if (message.aiThreadRenameAction != null && Object.hasOwnProperty.call(message, "aiThreadRenameAction"))
                object.aiThreadRenameAction = $root.proto.SyncActionValue.AiThreadRenameAction.toObject(message.aiThreadRenameAction, options, q + 1);
            if (message.interactiveMessageAction != null && Object.hasOwnProperty.call(message, "interactiveMessageAction"))
                object.interactiveMessageAction = $root.proto.SyncActionValue.InteractiveMessageAction.toObject(message.interactiveMessageAction, options, q + 1);
            if (message.settingsSyncAction != null && Object.hasOwnProperty.call(message, "settingsSyncAction"))
                object.settingsSyncAction = $root.proto.SyncActionValue.SettingsSyncAction.toObject(message.settingsSyncAction, options, q + 1);
            if (message.outContactAction != null && Object.hasOwnProperty.call(message, "outContactAction"))
                object.outContactAction = $root.proto.SyncActionValue.OutContactAction.toObject(message.outContactAction, options, q + 1);
            if (message.nctSaltSyncAction != null && Object.hasOwnProperty.call(message, "nctSaltSyncAction"))
                object.nctSaltSyncAction = $root.proto.SyncActionValue.NctSaltSyncAction.toObject(message.nctSaltSyncAction, options, q + 1);
            if (message.businessBroadcastCampaignAction != null && Object.hasOwnProperty.call(message, "businessBroadcastCampaignAction"))
                object.businessBroadcastCampaignAction = $root.proto.SyncActionValue.BusinessBroadcastCampaignAction.toObject(message.businessBroadcastCampaignAction, options, q + 1);
            if (message.businessBroadcastInsightsAction != null && Object.hasOwnProperty.call(message, "businessBroadcastInsightsAction"))
                object.businessBroadcastInsightsAction = $root.proto.SyncActionValue.BusinessBroadcastInsightsAction.toObject(message.businessBroadcastInsightsAction, options, q + 1);
            if (message.customerDataAction != null && Object.hasOwnProperty.call(message, "customerDataAction"))
                object.customerDataAction = $root.proto.SyncActionValue.CustomerDataAction.toObject(message.customerDataAction, options, q + 1);
            if (message.subscriptionsSyncV2Action != null && Object.hasOwnProperty.call(message, "subscriptionsSyncV2Action"))
                object.subscriptionsSyncV2Action = $root.proto.SyncActionValue.SubscriptionsSyncV2Action.toObject(message.subscriptionsSyncV2Action, options, q + 1);
            if (message.threadPinAction != null && Object.hasOwnProperty.call(message, "threadPinAction"))
                object.threadPinAction = $root.proto.SyncActionValue.ThreadPinAction.toObject(message.threadPinAction, options, q + 1);
            if (message.autoOrganizeBusinessChatSetting != null && Object.hasOwnProperty.call(message, "autoOrganizeBusinessChatSetting"))
                object.autoOrganizeBusinessChatSetting = $root.proto.SyncActionValue.AutoOrganizeBusinessChatSetting.toObject(message.autoOrganizeBusinessChatSetting, options, q + 1);
            if (message.bizAiSettingsNudgeAction != null && Object.hasOwnProperty.call(message, "bizAiSettingsNudgeAction"))
                object.bizAiSettingsNudgeAction = $root.proto.SyncActionValue.BizAISettingsNudgeAction.toObject(message.bizAiSettingsNudgeAction, options, q + 1);
            if (message.coexV2VersionAction != null && Object.hasOwnProperty.call(message, "coexV2VersionAction"))
                object.coexV2VersionAction = $root.proto.SyncActionValue.CoexV2VersionAction.toObject(message.coexV2VersionAction, options, q + 1);
            if (message.wasaRootSecretAction != null && Object.hasOwnProperty.call(message, "wasaRootSecretAction"))
                object.wasaRootSecretAction = $root.proto.SyncActionValue.WASARootSecretAction.toObject(message.wasaRootSecretAction, options, q + 1);
            return object;
        };

        /**
         * Converts this SyncActionValue to JSON.
         * @function toJSON
         * @memberof proto.SyncActionValue
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SyncActionValue.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SyncActionValue
         * @function getTypeUrl
         * @memberof proto.SyncActionValue
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SyncActionValue.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.SyncActionValue";
        };

        SyncActionValue.AgentAction = (function() {

            /**
             * Properties of an AgentAction.
             * @memberof proto.SyncActionValue
             * @interface IAgentAction
             * @property {string|null} [name] AgentAction name
             * @property {number|null} [deviceId] AgentAction deviceId
             * @property {boolean|null} [isDeleted] AgentAction isDeleted
             */

            /**
             * Constructs a new AgentAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents an AgentAction.
             * @implements IAgentAction
             * @constructor
             * @param {proto.SyncActionValue.IAgentAction=} [properties] Properties to set
             */
            function AgentAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AgentAction name.
             * @member {string|null|undefined} name
             * @memberof proto.SyncActionValue.AgentAction
             * @instance
             */
            AgentAction.prototype.name = null;

            /**
             * AgentAction deviceId.
             * @member {number|null|undefined} deviceId
             * @memberof proto.SyncActionValue.AgentAction
             * @instance
             */
            AgentAction.prototype.deviceId = null;

            /**
             * AgentAction isDeleted.
             * @member {boolean|null|undefined} isDeleted
             * @memberof proto.SyncActionValue.AgentAction
             * @instance
             */
            AgentAction.prototype.isDeleted = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AgentAction.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AgentAction.prototype, "_deviceId", {
                get: $util.oneOfGetter($oneOfFields = ["deviceId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AgentAction.prototype, "_isDeleted", {
                get: $util.oneOfGetter($oneOfFields = ["isDeleted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AgentAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.AgentAction
             * @static
             * @param {proto.SyncActionValue.IAgentAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.AgentAction} AgentAction instance
             */
            AgentAction.create = function create(properties) {
                return new AgentAction(properties);
            };

            /**
             * Encodes the specified AgentAction message. Does not implicitly {@link proto.SyncActionValue.AgentAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.AgentAction
             * @static
             * @param {proto.SyncActionValue.IAgentAction} message AgentAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AgentAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.deviceId);
                if (message.isDeleted != null && Object.hasOwnProperty.call(message, "isDeleted"))
                    writer.uint32(/* id 3, wireType 0 =*/24).bool(message.isDeleted);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AgentAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.AgentAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.AgentAction
             * @static
             * @param {proto.SyncActionValue.IAgentAction} message AgentAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AgentAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an AgentAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.AgentAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.AgentAction} AgentAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AgentAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.deviceId = reader.int32();
                            message._deviceId = "deviceId";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.isDeleted = reader.bool();
                            message._isDeleted = "isDeleted";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an AgentAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.AgentAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.AgentAction} AgentAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AgentAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AgentAction message.
             * @function verify
             * @memberof proto.SyncActionValue.AgentAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AgentAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.name != null && Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId")) {
                    properties._deviceId = 1;
                    if (!$util.isInteger(message.deviceId))
                        return "deviceId: integer expected";
                }
                if (message.isDeleted != null && Object.hasOwnProperty.call(message, "isDeleted")) {
                    properties._isDeleted = 1;
                    if (typeof message.isDeleted !== "boolean")
                        return "isDeleted: boolean expected";
                }
                return null;
            };

            /**
             * Creates an AgentAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.AgentAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.AgentAction} AgentAction
             */
            AgentAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.AgentAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.name != null)
                    message.name = String(object.name);
                if (object.deviceId != null)
                    message.deviceId = object.deviceId | 0;
                if (object.isDeleted != null)
                    message.isDeleted = Boolean(object.isDeleted);
                return message;
            };

            /**
             * Creates a plain object from an AgentAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.AgentAction
             * @static
             * @param {proto.SyncActionValue.AgentAction} message AgentAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AgentAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                    object.deviceId = message.deviceId;
                if (message.isDeleted != null && Object.hasOwnProperty.call(message, "isDeleted"))
                    object.isDeleted = message.isDeleted;
                return object;
            };

            /**
             * Converts this AgentAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.AgentAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AgentAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AgentAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.AgentAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AgentAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.AgentAction";
            };

            return AgentAction;
        })();

        SyncActionValue.AiThreadRenameAction = (function() {

            /**
             * Properties of an AiThreadRenameAction.
             * @memberof proto.SyncActionValue
             * @interface IAiThreadRenameAction
             * @property {string|null} [newTitle] AiThreadRenameAction newTitle
             */

            /**
             * Constructs a new AiThreadRenameAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents an AiThreadRenameAction.
             * @implements IAiThreadRenameAction
             * @constructor
             * @param {proto.SyncActionValue.IAiThreadRenameAction=} [properties] Properties to set
             */
            function AiThreadRenameAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AiThreadRenameAction newTitle.
             * @member {string|null|undefined} newTitle
             * @memberof proto.SyncActionValue.AiThreadRenameAction
             * @instance
             */
            AiThreadRenameAction.prototype.newTitle = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AiThreadRenameAction.prototype, "_newTitle", {
                get: $util.oneOfGetter($oneOfFields = ["newTitle"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AiThreadRenameAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.AiThreadRenameAction
             * @static
             * @param {proto.SyncActionValue.IAiThreadRenameAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.AiThreadRenameAction} AiThreadRenameAction instance
             */
            AiThreadRenameAction.create = function create(properties) {
                return new AiThreadRenameAction(properties);
            };

            /**
             * Encodes the specified AiThreadRenameAction message. Does not implicitly {@link proto.SyncActionValue.AiThreadRenameAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.AiThreadRenameAction
             * @static
             * @param {proto.SyncActionValue.IAiThreadRenameAction} message AiThreadRenameAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AiThreadRenameAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.newTitle != null && Object.hasOwnProperty.call(message, "newTitle"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.newTitle);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AiThreadRenameAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.AiThreadRenameAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.AiThreadRenameAction
             * @static
             * @param {proto.SyncActionValue.IAiThreadRenameAction} message AiThreadRenameAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AiThreadRenameAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an AiThreadRenameAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.AiThreadRenameAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.AiThreadRenameAction} AiThreadRenameAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AiThreadRenameAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.newTitle = reader.stringVerify();
                            message._newTitle = "newTitle";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an AiThreadRenameAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.AiThreadRenameAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.AiThreadRenameAction} AiThreadRenameAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AiThreadRenameAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AiThreadRenameAction message.
             * @function verify
             * @memberof proto.SyncActionValue.AiThreadRenameAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AiThreadRenameAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.newTitle != null && Object.hasOwnProperty.call(message, "newTitle")) {
                    properties._newTitle = 1;
                    if (!$util.isString(message.newTitle))
                        return "newTitle: string expected";
                }
                return null;
            };

            /**
             * Creates an AiThreadRenameAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.AiThreadRenameAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.AiThreadRenameAction} AiThreadRenameAction
             */
            AiThreadRenameAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.AiThreadRenameAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.newTitle != null)
                    message.newTitle = String(object.newTitle);
                return message;
            };

            /**
             * Creates a plain object from an AiThreadRenameAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.AiThreadRenameAction
             * @static
             * @param {proto.SyncActionValue.AiThreadRenameAction} message AiThreadRenameAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AiThreadRenameAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.newTitle != null && Object.hasOwnProperty.call(message, "newTitle"))
                    object.newTitle = message.newTitle;
                return object;
            };

            /**
             * Converts this AiThreadRenameAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.AiThreadRenameAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AiThreadRenameAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AiThreadRenameAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.AiThreadRenameAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AiThreadRenameAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.AiThreadRenameAction";
            };

            return AiThreadRenameAction;
        })();

        SyncActionValue.AndroidUnsupportedActions = (function() {

            /**
             * Properties of an AndroidUnsupportedActions.
             * @memberof proto.SyncActionValue
             * @interface IAndroidUnsupportedActions
             * @property {boolean|null} [allowed] AndroidUnsupportedActions allowed
             */

            /**
             * Constructs a new AndroidUnsupportedActions.
             * @memberof proto.SyncActionValue
             * @classdesc Represents an AndroidUnsupportedActions.
             * @implements IAndroidUnsupportedActions
             * @constructor
             * @param {proto.SyncActionValue.IAndroidUnsupportedActions=} [properties] Properties to set
             */
            function AndroidUnsupportedActions(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AndroidUnsupportedActions allowed.
             * @member {boolean|null|undefined} allowed
             * @memberof proto.SyncActionValue.AndroidUnsupportedActions
             * @instance
             */
            AndroidUnsupportedActions.prototype.allowed = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AndroidUnsupportedActions.prototype, "_allowed", {
                get: $util.oneOfGetter($oneOfFields = ["allowed"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AndroidUnsupportedActions instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.AndroidUnsupportedActions
             * @static
             * @param {proto.SyncActionValue.IAndroidUnsupportedActions=} [properties] Properties to set
             * @returns {proto.SyncActionValue.AndroidUnsupportedActions} AndroidUnsupportedActions instance
             */
            AndroidUnsupportedActions.create = function create(properties) {
                return new AndroidUnsupportedActions(properties);
            };

            /**
             * Encodes the specified AndroidUnsupportedActions message. Does not implicitly {@link proto.SyncActionValue.AndroidUnsupportedActions.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.AndroidUnsupportedActions
             * @static
             * @param {proto.SyncActionValue.IAndroidUnsupportedActions} message AndroidUnsupportedActions message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AndroidUnsupportedActions.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.allowed != null && Object.hasOwnProperty.call(message, "allowed"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.allowed);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AndroidUnsupportedActions message, length delimited. Does not implicitly {@link proto.SyncActionValue.AndroidUnsupportedActions.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.AndroidUnsupportedActions
             * @static
             * @param {proto.SyncActionValue.IAndroidUnsupportedActions} message AndroidUnsupportedActions message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AndroidUnsupportedActions.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an AndroidUnsupportedActions message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.AndroidUnsupportedActions
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.AndroidUnsupportedActions} AndroidUnsupportedActions
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AndroidUnsupportedActions.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.allowed = reader.bool();
                            message._allowed = "allowed";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an AndroidUnsupportedActions message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.AndroidUnsupportedActions
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.AndroidUnsupportedActions} AndroidUnsupportedActions
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AndroidUnsupportedActions.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AndroidUnsupportedActions message.
             * @function verify
             * @memberof proto.SyncActionValue.AndroidUnsupportedActions
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AndroidUnsupportedActions.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.allowed != null && Object.hasOwnProperty.call(message, "allowed")) {
                    properties._allowed = 1;
                    if (typeof message.allowed !== "boolean")
                        return "allowed: boolean expected";
                }
                return null;
            };

            /**
             * Creates an AndroidUnsupportedActions message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.AndroidUnsupportedActions
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.AndroidUnsupportedActions} AndroidUnsupportedActions
             */
            AndroidUnsupportedActions.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.AndroidUnsupportedActions: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.allowed != null)
                    message.allowed = Boolean(object.allowed);
                return message;
            };

            /**
             * Creates a plain object from an AndroidUnsupportedActions message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.AndroidUnsupportedActions
             * @static
             * @param {proto.SyncActionValue.AndroidUnsupportedActions} message AndroidUnsupportedActions
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AndroidUnsupportedActions.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.allowed != null && Object.hasOwnProperty.call(message, "allowed"))
                    object.allowed = message.allowed;
                return object;
            };

            /**
             * Converts this AndroidUnsupportedActions to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.AndroidUnsupportedActions
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AndroidUnsupportedActions.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AndroidUnsupportedActions
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.AndroidUnsupportedActions
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AndroidUnsupportedActions.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.AndroidUnsupportedActions";
            };

            return AndroidUnsupportedActions;
        })();

        SyncActionValue.ArchiveChatAction = (function() {

            /**
             * Properties of an ArchiveChatAction.
             * @memberof proto.SyncActionValue
             * @interface IArchiveChatAction
             * @property {boolean|null} [archived] ArchiveChatAction archived
             * @property {proto.SyncActionValue.ISyncActionMessageRange|null} [messageRange] ArchiveChatAction messageRange
             */

            /**
             * Constructs a new ArchiveChatAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents an ArchiveChatAction.
             * @implements IArchiveChatAction
             * @constructor
             * @param {proto.SyncActionValue.IArchiveChatAction=} [properties] Properties to set
             */
            function ArchiveChatAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ArchiveChatAction archived.
             * @member {boolean|null|undefined} archived
             * @memberof proto.SyncActionValue.ArchiveChatAction
             * @instance
             */
            ArchiveChatAction.prototype.archived = null;

            /**
             * ArchiveChatAction messageRange.
             * @member {proto.SyncActionValue.ISyncActionMessageRange|null|undefined} messageRange
             * @memberof proto.SyncActionValue.ArchiveChatAction
             * @instance
             */
            ArchiveChatAction.prototype.messageRange = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ArchiveChatAction.prototype, "_archived", {
                get: $util.oneOfGetter($oneOfFields = ["archived"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ArchiveChatAction.prototype, "_messageRange", {
                get: $util.oneOfGetter($oneOfFields = ["messageRange"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ArchiveChatAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.ArchiveChatAction
             * @static
             * @param {proto.SyncActionValue.IArchiveChatAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.ArchiveChatAction} ArchiveChatAction instance
             */
            ArchiveChatAction.create = function create(properties) {
                return new ArchiveChatAction(properties);
            };

            /**
             * Encodes the specified ArchiveChatAction message. Does not implicitly {@link proto.SyncActionValue.ArchiveChatAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.ArchiveChatAction
             * @static
             * @param {proto.SyncActionValue.IArchiveChatAction} message ArchiveChatAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ArchiveChatAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.archived != null && Object.hasOwnProperty.call(message, "archived"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.archived);
                if (message.messageRange != null && Object.hasOwnProperty.call(message, "messageRange"))
                    $root.proto.SyncActionValue.SyncActionMessageRange.encode(message.messageRange, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ArchiveChatAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.ArchiveChatAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.ArchiveChatAction
             * @static
             * @param {proto.SyncActionValue.IArchiveChatAction} message ArchiveChatAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ArchiveChatAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an ArchiveChatAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.ArchiveChatAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.ArchiveChatAction} ArchiveChatAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ArchiveChatAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.archived = reader.bool();
                            message._archived = "archived";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.decode(reader, reader.uint32(), undefined, q + 1, message.messageRange);
                            message._messageRange = "messageRange";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an ArchiveChatAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.ArchiveChatAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.ArchiveChatAction} ArchiveChatAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ArchiveChatAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an ArchiveChatAction message.
             * @function verify
             * @memberof proto.SyncActionValue.ArchiveChatAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ArchiveChatAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.archived != null && Object.hasOwnProperty.call(message, "archived")) {
                    properties._archived = 1;
                    if (typeof message.archived !== "boolean")
                        return "archived: boolean expected";
                }
                if (message.messageRange != null && Object.hasOwnProperty.call(message, "messageRange")) {
                    properties._messageRange = 1;
                    {
                        var error = $root.proto.SyncActionValue.SyncActionMessageRange.verify(message.messageRange, q + 1);
                        if (error)
                            return "messageRange." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an ArchiveChatAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.ArchiveChatAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.ArchiveChatAction} ArchiveChatAction
             */
            ArchiveChatAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.ArchiveChatAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.archived != null)
                    message.archived = Boolean(object.archived);
                if (object.messageRange != null) {
                    if (!$util.isObject(object.messageRange))
                        throw TypeError(".proto.SyncActionValue.ArchiveChatAction.messageRange: object expected");
                    message.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.fromObject(object.messageRange, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from an ArchiveChatAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.ArchiveChatAction
             * @static
             * @param {proto.SyncActionValue.ArchiveChatAction} message ArchiveChatAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ArchiveChatAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.archived != null && Object.hasOwnProperty.call(message, "archived"))
                    object.archived = message.archived;
                if (message.messageRange != null && Object.hasOwnProperty.call(message, "messageRange"))
                    object.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.toObject(message.messageRange, options, q + 1);
                return object;
            };

            /**
             * Converts this ArchiveChatAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.ArchiveChatAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ArchiveChatAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ArchiveChatAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.ArchiveChatAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ArchiveChatAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.ArchiveChatAction";
            };

            return ArchiveChatAction;
        })();

        SyncActionValue.AutoOrganizeBusinessChatSetting = (function() {

            /**
             * Properties of an AutoOrganizeBusinessChatSetting.
             * @memberof proto.SyncActionValue
             * @interface IAutoOrganizeBusinessChatSetting
             * @property {boolean|null} [autoOrganize] AutoOrganizeBusinessChatSetting autoOrganize
             */

            /**
             * Constructs a new AutoOrganizeBusinessChatSetting.
             * @memberof proto.SyncActionValue
             * @classdesc Represents an AutoOrganizeBusinessChatSetting.
             * @implements IAutoOrganizeBusinessChatSetting
             * @constructor
             * @param {proto.SyncActionValue.IAutoOrganizeBusinessChatSetting=} [properties] Properties to set
             */
            function AutoOrganizeBusinessChatSetting(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AutoOrganizeBusinessChatSetting autoOrganize.
             * @member {boolean|null|undefined} autoOrganize
             * @memberof proto.SyncActionValue.AutoOrganizeBusinessChatSetting
             * @instance
             */
            AutoOrganizeBusinessChatSetting.prototype.autoOrganize = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AutoOrganizeBusinessChatSetting.prototype, "_autoOrganize", {
                get: $util.oneOfGetter($oneOfFields = ["autoOrganize"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AutoOrganizeBusinessChatSetting instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.AutoOrganizeBusinessChatSetting
             * @static
             * @param {proto.SyncActionValue.IAutoOrganizeBusinessChatSetting=} [properties] Properties to set
             * @returns {proto.SyncActionValue.AutoOrganizeBusinessChatSetting} AutoOrganizeBusinessChatSetting instance
             */
            AutoOrganizeBusinessChatSetting.create = function create(properties) {
                return new AutoOrganizeBusinessChatSetting(properties);
            };

            /**
             * Encodes the specified AutoOrganizeBusinessChatSetting message. Does not implicitly {@link proto.SyncActionValue.AutoOrganizeBusinessChatSetting.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.AutoOrganizeBusinessChatSetting
             * @static
             * @param {proto.SyncActionValue.IAutoOrganizeBusinessChatSetting} message AutoOrganizeBusinessChatSetting message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AutoOrganizeBusinessChatSetting.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.autoOrganize != null && Object.hasOwnProperty.call(message, "autoOrganize"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.autoOrganize);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AutoOrganizeBusinessChatSetting message, length delimited. Does not implicitly {@link proto.SyncActionValue.AutoOrganizeBusinessChatSetting.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.AutoOrganizeBusinessChatSetting
             * @static
             * @param {proto.SyncActionValue.IAutoOrganizeBusinessChatSetting} message AutoOrganizeBusinessChatSetting message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AutoOrganizeBusinessChatSetting.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an AutoOrganizeBusinessChatSetting message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.AutoOrganizeBusinessChatSetting
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.AutoOrganizeBusinessChatSetting} AutoOrganizeBusinessChatSetting
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AutoOrganizeBusinessChatSetting.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.autoOrganize = reader.bool();
                            message._autoOrganize = "autoOrganize";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an AutoOrganizeBusinessChatSetting message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.AutoOrganizeBusinessChatSetting
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.AutoOrganizeBusinessChatSetting} AutoOrganizeBusinessChatSetting
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AutoOrganizeBusinessChatSetting.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AutoOrganizeBusinessChatSetting message.
             * @function verify
             * @memberof proto.SyncActionValue.AutoOrganizeBusinessChatSetting
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AutoOrganizeBusinessChatSetting.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.autoOrganize != null && Object.hasOwnProperty.call(message, "autoOrganize")) {
                    properties._autoOrganize = 1;
                    if (typeof message.autoOrganize !== "boolean")
                        return "autoOrganize: boolean expected";
                }
                return null;
            };

            /**
             * Creates an AutoOrganizeBusinessChatSetting message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.AutoOrganizeBusinessChatSetting
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.AutoOrganizeBusinessChatSetting} AutoOrganizeBusinessChatSetting
             */
            AutoOrganizeBusinessChatSetting.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.AutoOrganizeBusinessChatSetting: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.autoOrganize != null)
                    message.autoOrganize = Boolean(object.autoOrganize);
                return message;
            };

            /**
             * Creates a plain object from an AutoOrganizeBusinessChatSetting message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.AutoOrganizeBusinessChatSetting
             * @static
             * @param {proto.SyncActionValue.AutoOrganizeBusinessChatSetting} message AutoOrganizeBusinessChatSetting
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AutoOrganizeBusinessChatSetting.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.autoOrganize != null && Object.hasOwnProperty.call(message, "autoOrganize"))
                    object.autoOrganize = message.autoOrganize;
                return object;
            };

            /**
             * Converts this AutoOrganizeBusinessChatSetting to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.AutoOrganizeBusinessChatSetting
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AutoOrganizeBusinessChatSetting.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AutoOrganizeBusinessChatSetting
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.AutoOrganizeBusinessChatSetting
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AutoOrganizeBusinessChatSetting.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.AutoOrganizeBusinessChatSetting";
            };

            return AutoOrganizeBusinessChatSetting;
        })();

        SyncActionValue.AvatarUpdatedAction = (function() {

            /**
             * Properties of an AvatarUpdatedAction.
             * @memberof proto.SyncActionValue
             * @interface IAvatarUpdatedAction
             * @property {proto.SyncActionValue.AvatarUpdatedAction.AvatarEventType|null} [eventType] AvatarUpdatedAction eventType
             * @property {Array.<proto.SyncActionValue.IStickerAction>|null} [recentAvatarStickers] AvatarUpdatedAction recentAvatarStickers
             */

            /**
             * Constructs a new AvatarUpdatedAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents an AvatarUpdatedAction.
             * @implements IAvatarUpdatedAction
             * @constructor
             * @param {proto.SyncActionValue.IAvatarUpdatedAction=} [properties] Properties to set
             */
            function AvatarUpdatedAction(properties) {
                this.recentAvatarStickers = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AvatarUpdatedAction eventType.
             * @member {proto.SyncActionValue.AvatarUpdatedAction.AvatarEventType|null|undefined} eventType
             * @memberof proto.SyncActionValue.AvatarUpdatedAction
             * @instance
             */
            AvatarUpdatedAction.prototype.eventType = null;

            /**
             * AvatarUpdatedAction recentAvatarStickers.
             * @member {Array.<proto.SyncActionValue.IStickerAction>} recentAvatarStickers
             * @memberof proto.SyncActionValue.AvatarUpdatedAction
             * @instance
             */
            AvatarUpdatedAction.prototype.recentAvatarStickers = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AvatarUpdatedAction.prototype, "_eventType", {
                get: $util.oneOfGetter($oneOfFields = ["eventType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new AvatarUpdatedAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.AvatarUpdatedAction
             * @static
             * @param {proto.SyncActionValue.IAvatarUpdatedAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.AvatarUpdatedAction} AvatarUpdatedAction instance
             */
            AvatarUpdatedAction.create = function create(properties) {
                return new AvatarUpdatedAction(properties);
            };

            /**
             * Encodes the specified AvatarUpdatedAction message. Does not implicitly {@link proto.SyncActionValue.AvatarUpdatedAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.AvatarUpdatedAction
             * @static
             * @param {proto.SyncActionValue.IAvatarUpdatedAction} message AvatarUpdatedAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AvatarUpdatedAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.eventType != null && Object.hasOwnProperty.call(message, "eventType"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.eventType);
                if (message.recentAvatarStickers != null && message.recentAvatarStickers.length)
                    for (var i = 0; i < message.recentAvatarStickers.length; ++i)
                        $root.proto.SyncActionValue.StickerAction.encode(message.recentAvatarStickers[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified AvatarUpdatedAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.AvatarUpdatedAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.AvatarUpdatedAction
             * @static
             * @param {proto.SyncActionValue.IAvatarUpdatedAction} message AvatarUpdatedAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AvatarUpdatedAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an AvatarUpdatedAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.AvatarUpdatedAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.AvatarUpdatedAction} AvatarUpdatedAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AvatarUpdatedAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.eventType = reader.int32();
                            message._eventType = "eventType";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(message.recentAvatarStickers && message.recentAvatarStickers.length))
                                message.recentAvatarStickers = [];
                            message.recentAvatarStickers.push($root.proto.SyncActionValue.StickerAction.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an AvatarUpdatedAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.AvatarUpdatedAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.AvatarUpdatedAction} AvatarUpdatedAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AvatarUpdatedAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AvatarUpdatedAction message.
             * @function verify
             * @memberof proto.SyncActionValue.AvatarUpdatedAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AvatarUpdatedAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.eventType != null && Object.hasOwnProperty.call(message, "eventType")) {
                    properties._eventType = 1;
                    if (typeof message.eventType !== "number" || (message.eventType | 0) !== message.eventType)
                        return "eventType: enum value expected";
                }
                if (message.recentAvatarStickers != null && Object.hasOwnProperty.call(message, "recentAvatarStickers")) {
                    if (!Array.isArray(message.recentAvatarStickers))
                        return "recentAvatarStickers: array expected";
                    for (var i = 0; i < message.recentAvatarStickers.length; ++i) {
                        var error = $root.proto.SyncActionValue.StickerAction.verify(message.recentAvatarStickers[i], q + 1);
                        if (error)
                            return "recentAvatarStickers." + error;
                    }
                }
                return null;
            };

            /**
             * Creates an AvatarUpdatedAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.AvatarUpdatedAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.AvatarUpdatedAction} AvatarUpdatedAction
             */
            AvatarUpdatedAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.AvatarUpdatedAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.eventType) {
                case "UPDATED":
                case 0:
                    message.eventType = 0;
                    break;
                case "CREATED":
                case 1:
                    message.eventType = 1;
                    break;
                case "DELETED":
                case 2:
                    message.eventType = 2;
                    break;
                default:
                    if (typeof object.eventType === "number" && (object.eventType | 0) === object.eventType)
                        message.eventType = object.eventType;
                }
                if (object.recentAvatarStickers) {
                    if (!Array.isArray(object.recentAvatarStickers))
                        throw TypeError(".proto.SyncActionValue.AvatarUpdatedAction.recentAvatarStickers: array expected");
                    message.recentAvatarStickers = Array(object.recentAvatarStickers.length);
                    for (var i = 0; i < object.recentAvatarStickers.length; ++i) {
                        if (!$util.isObject(object.recentAvatarStickers[i]))
                            throw TypeError(".proto.SyncActionValue.AvatarUpdatedAction.recentAvatarStickers: object expected");
                        message.recentAvatarStickers[i] = $root.proto.SyncActionValue.StickerAction.fromObject(object.recentAvatarStickers[i], q + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from an AvatarUpdatedAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.AvatarUpdatedAction
             * @static
             * @param {proto.SyncActionValue.AvatarUpdatedAction} message AvatarUpdatedAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AvatarUpdatedAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.recentAvatarStickers = [];
                if (message.eventType != null && Object.hasOwnProperty.call(message, "eventType"))
                    object.eventType = options.enums === String ? $root.proto.SyncActionValue.AvatarUpdatedAction.AvatarEventType[message.eventType] === undefined ? message.eventType : $root.proto.SyncActionValue.AvatarUpdatedAction.AvatarEventType[message.eventType] : message.eventType;
                if (message.recentAvatarStickers && message.recentAvatarStickers.length) {
                    object.recentAvatarStickers = Array(message.recentAvatarStickers.length);
                    for (var j = 0; j < message.recentAvatarStickers.length; ++j)
                        object.recentAvatarStickers[j] = $root.proto.SyncActionValue.StickerAction.toObject(message.recentAvatarStickers[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this AvatarUpdatedAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.AvatarUpdatedAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AvatarUpdatedAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AvatarUpdatedAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.AvatarUpdatedAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AvatarUpdatedAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.AvatarUpdatedAction";
            };

            /**
             * AvatarEventType enum.
             * @name proto.SyncActionValue.AvatarUpdatedAction.AvatarEventType
             * @enum {number}
             * @property {number} UPDATED=0 UPDATED value
             * @property {number} CREATED=1 CREATED value
             * @property {number} DELETED=2 DELETED value
             */
            AvatarUpdatedAction.AvatarEventType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UPDATED"] = 0;
                values[valuesById[1] = "CREATED"] = 1;
                values[valuesById[2] = "DELETED"] = 2;
                return values;
            })();

            return AvatarUpdatedAction;
        })();

        SyncActionValue.BizAISettingsNudgeAction = (function() {

            /**
             * Properties of a BizAISettingsNudgeAction.
             * @memberof proto.SyncActionValue
             * @interface IBizAISettingsNudgeAction
             * @property {proto.SyncActionValue.BizAISettingsNudgeAction.BizAISettingsCategory|null} [category] BizAISettingsNudgeAction category
             * @property {number|Long|null} [version] BizAISettingsNudgeAction version
             * @property {number|Long|null} [updatedAtMs] BizAISettingsNudgeAction updatedAtMs
             */

            /**
             * Constructs a new BizAISettingsNudgeAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a BizAISettingsNudgeAction.
             * @implements IBizAISettingsNudgeAction
             * @constructor
             * @param {proto.SyncActionValue.IBizAISettingsNudgeAction=} [properties] Properties to set
             */
            function BizAISettingsNudgeAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * BizAISettingsNudgeAction category.
             * @member {proto.SyncActionValue.BizAISettingsNudgeAction.BizAISettingsCategory|null|undefined} category
             * @memberof proto.SyncActionValue.BizAISettingsNudgeAction
             * @instance
             */
            BizAISettingsNudgeAction.prototype.category = null;

            /**
             * BizAISettingsNudgeAction version.
             * @member {number|Long|null|undefined} version
             * @memberof proto.SyncActionValue.BizAISettingsNudgeAction
             * @instance
             */
            BizAISettingsNudgeAction.prototype.version = null;

            /**
             * BizAISettingsNudgeAction updatedAtMs.
             * @member {number|Long|null|undefined} updatedAtMs
             * @memberof proto.SyncActionValue.BizAISettingsNudgeAction
             * @instance
             */
            BizAISettingsNudgeAction.prototype.updatedAtMs = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BizAISettingsNudgeAction.prototype, "_category", {
                get: $util.oneOfGetter($oneOfFields = ["category"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BizAISettingsNudgeAction.prototype, "_version", {
                get: $util.oneOfGetter($oneOfFields = ["version"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BizAISettingsNudgeAction.prototype, "_updatedAtMs", {
                get: $util.oneOfGetter($oneOfFields = ["updatedAtMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BizAISettingsNudgeAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.BizAISettingsNudgeAction
             * @static
             * @param {proto.SyncActionValue.IBizAISettingsNudgeAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.BizAISettingsNudgeAction} BizAISettingsNudgeAction instance
             */
            BizAISettingsNudgeAction.create = function create(properties) {
                return new BizAISettingsNudgeAction(properties);
            };

            /**
             * Encodes the specified BizAISettingsNudgeAction message. Does not implicitly {@link proto.SyncActionValue.BizAISettingsNudgeAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.BizAISettingsNudgeAction
             * @static
             * @param {proto.SyncActionValue.IBizAISettingsNudgeAction} message BizAISettingsNudgeAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BizAISettingsNudgeAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.category != null && Object.hasOwnProperty.call(message, "category"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.category);
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.version);
                if (message.updatedAtMs != null && Object.hasOwnProperty.call(message, "updatedAtMs"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.updatedAtMs);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified BizAISettingsNudgeAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.BizAISettingsNudgeAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.BizAISettingsNudgeAction
             * @static
             * @param {proto.SyncActionValue.IBizAISettingsNudgeAction} message BizAISettingsNudgeAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BizAISettingsNudgeAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a BizAISettingsNudgeAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.BizAISettingsNudgeAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.BizAISettingsNudgeAction} BizAISettingsNudgeAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BizAISettingsNudgeAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.category = reader.int32();
                            message._category = "category";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.version = reader.int64();
                            message._version = "version";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.updatedAtMs = reader.int64();
                            message._updatedAtMs = "updatedAtMs";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a BizAISettingsNudgeAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.BizAISettingsNudgeAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.BizAISettingsNudgeAction} BizAISettingsNudgeAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BizAISettingsNudgeAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BizAISettingsNudgeAction message.
             * @function verify
             * @memberof proto.SyncActionValue.BizAISettingsNudgeAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BizAISettingsNudgeAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.category != null && Object.hasOwnProperty.call(message, "category")) {
                    properties._category = 1;
                    if (typeof message.category !== "number" || (message.category | 0) !== message.category)
                        return "category: enum value expected";
                }
                if (message.version != null && Object.hasOwnProperty.call(message, "version")) {
                    properties._version = 1;
                    if (!$util.isInteger(message.version) && !(message.version && $util.isInteger(message.version.low) && $util.isInteger(message.version.high)))
                        return "version: integer|Long expected";
                }
                if (message.updatedAtMs != null && Object.hasOwnProperty.call(message, "updatedAtMs")) {
                    properties._updatedAtMs = 1;
                    if (!$util.isInteger(message.updatedAtMs) && !(message.updatedAtMs && $util.isInteger(message.updatedAtMs.low) && $util.isInteger(message.updatedAtMs.high)))
                        return "updatedAtMs: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a BizAISettingsNudgeAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.BizAISettingsNudgeAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.BizAISettingsNudgeAction} BizAISettingsNudgeAction
             */
            BizAISettingsNudgeAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.BizAISettingsNudgeAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.category) {
                case "UNKNOWN":
                case 0:
                    message.category = 0;
                    break;
                case "INSTRUCTIONS":
                case 1:
                    message.category = 1;
                    break;
                case "RESPONSE_SETTINGS":
                case 2:
                    message.category = 2;
                    break;
                case "EXAMPLE_RESPONSES":
                case 3:
                    message.category = 3;
                    break;
                case "KNOWLEDGE":
                case 4:
                    message.category = 4;
                    break;
                case "LEAD_GEN":
                case 5:
                    message.category = 5;
                    break;
                default:
                    if (typeof object.category === "number" && (object.category | 0) === object.category)
                        message.category = object.category;
                }
                if (object.version != null)
                    if ($util.Long)
                        message.version = $util.Long.fromValue(object.version, false);
                    else if (typeof object.version === "string")
                        message.version = parseInt(object.version, 10);
                    else if (typeof object.version === "number")
                        message.version = object.version;
                    else if (typeof object.version === "object")
                        message.version = new $util.LongBits(object.version.low >>> 0, object.version.high >>> 0).toNumber();
                if (object.updatedAtMs != null)
                    if ($util.Long)
                        message.updatedAtMs = $util.Long.fromValue(object.updatedAtMs, false);
                    else if (typeof object.updatedAtMs === "string")
                        message.updatedAtMs = parseInt(object.updatedAtMs, 10);
                    else if (typeof object.updatedAtMs === "number")
                        message.updatedAtMs = object.updatedAtMs;
                    else if (typeof object.updatedAtMs === "object")
                        message.updatedAtMs = new $util.LongBits(object.updatedAtMs.low >>> 0, object.updatedAtMs.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a BizAISettingsNudgeAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.BizAISettingsNudgeAction
             * @static
             * @param {proto.SyncActionValue.BizAISettingsNudgeAction} message BizAISettingsNudgeAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BizAISettingsNudgeAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.category != null && Object.hasOwnProperty.call(message, "category"))
                    object.category = options.enums === String ? $root.proto.SyncActionValue.BizAISettingsNudgeAction.BizAISettingsCategory[message.category] === undefined ? message.category : $root.proto.SyncActionValue.BizAISettingsNudgeAction.BizAISettingsCategory[message.category] : message.category;
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.version = typeof message.version === "number" ? BigInt(message.version) : $util.Long.fromBits(message.version.low >>> 0, message.version.high >>> 0, false).toBigInt();
                    else if (typeof message.version === "number")
                        object.version = options.longs === String ? String(message.version) : message.version;
                    else
                        object.version = options.longs === String ? $util.Long.prototype.toString.call(message.version) : options.longs === Number ? new $util.LongBits(message.version.low >>> 0, message.version.high >>> 0).toNumber() : message.version;
                if (message.updatedAtMs != null && Object.hasOwnProperty.call(message, "updatedAtMs"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.updatedAtMs = typeof message.updatedAtMs === "number" ? BigInt(message.updatedAtMs) : $util.Long.fromBits(message.updatedAtMs.low >>> 0, message.updatedAtMs.high >>> 0, false).toBigInt();
                    else if (typeof message.updatedAtMs === "number")
                        object.updatedAtMs = options.longs === String ? String(message.updatedAtMs) : message.updatedAtMs;
                    else
                        object.updatedAtMs = options.longs === String ? $util.Long.prototype.toString.call(message.updatedAtMs) : options.longs === Number ? new $util.LongBits(message.updatedAtMs.low >>> 0, message.updatedAtMs.high >>> 0).toNumber() : message.updatedAtMs;
                return object;
            };

            /**
             * Converts this BizAISettingsNudgeAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.BizAISettingsNudgeAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BizAISettingsNudgeAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for BizAISettingsNudgeAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.BizAISettingsNudgeAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            BizAISettingsNudgeAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.BizAISettingsNudgeAction";
            };

            /**
             * BizAISettingsCategory enum.
             * @name proto.SyncActionValue.BizAISettingsNudgeAction.BizAISettingsCategory
             * @enum {number}
             * @property {number} UNKNOWN=0 UNKNOWN value
             * @property {number} INSTRUCTIONS=1 INSTRUCTIONS value
             * @property {number} RESPONSE_SETTINGS=2 RESPONSE_SETTINGS value
             * @property {number} EXAMPLE_RESPONSES=3 EXAMPLE_RESPONSES value
             * @property {number} KNOWLEDGE=4 KNOWLEDGE value
             * @property {number} LEAD_GEN=5 LEAD_GEN value
             */
            BizAISettingsNudgeAction.BizAISettingsCategory = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "INSTRUCTIONS"] = 1;
                values[valuesById[2] = "RESPONSE_SETTINGS"] = 2;
                values[valuesById[3] = "EXAMPLE_RESPONSES"] = 3;
                values[valuesById[4] = "KNOWLEDGE"] = 4;
                values[valuesById[5] = "LEAD_GEN"] = 5;
                return values;
            })();

            return BizAISettingsNudgeAction;
        })();

        SyncActionValue.BotWelcomeRequestAction = (function() {

            /**
             * Properties of a BotWelcomeRequestAction.
             * @memberof proto.SyncActionValue
             * @interface IBotWelcomeRequestAction
             * @property {boolean|null} [isSent] BotWelcomeRequestAction isSent
             */

            /**
             * Constructs a new BotWelcomeRequestAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a BotWelcomeRequestAction.
             * @implements IBotWelcomeRequestAction
             * @constructor
             * @param {proto.SyncActionValue.IBotWelcomeRequestAction=} [properties] Properties to set
             */
            function BotWelcomeRequestAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * BotWelcomeRequestAction isSent.
             * @member {boolean|null|undefined} isSent
             * @memberof proto.SyncActionValue.BotWelcomeRequestAction
             * @instance
             */
            BotWelcomeRequestAction.prototype.isSent = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotWelcomeRequestAction.prototype, "_isSent", {
                get: $util.oneOfGetter($oneOfFields = ["isSent"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BotWelcomeRequestAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.BotWelcomeRequestAction
             * @static
             * @param {proto.SyncActionValue.IBotWelcomeRequestAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.BotWelcomeRequestAction} BotWelcomeRequestAction instance
             */
            BotWelcomeRequestAction.create = function create(properties) {
                return new BotWelcomeRequestAction(properties);
            };

            /**
             * Encodes the specified BotWelcomeRequestAction message. Does not implicitly {@link proto.SyncActionValue.BotWelcomeRequestAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.BotWelcomeRequestAction
             * @static
             * @param {proto.SyncActionValue.IBotWelcomeRequestAction} message BotWelcomeRequestAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BotWelcomeRequestAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.isSent != null && Object.hasOwnProperty.call(message, "isSent"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.isSent);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified BotWelcomeRequestAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.BotWelcomeRequestAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.BotWelcomeRequestAction
             * @static
             * @param {proto.SyncActionValue.IBotWelcomeRequestAction} message BotWelcomeRequestAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BotWelcomeRequestAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a BotWelcomeRequestAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.BotWelcomeRequestAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.BotWelcomeRequestAction} BotWelcomeRequestAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BotWelcomeRequestAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.isSent = reader.bool();
                            message._isSent = "isSent";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a BotWelcomeRequestAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.BotWelcomeRequestAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.BotWelcomeRequestAction} BotWelcomeRequestAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BotWelcomeRequestAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BotWelcomeRequestAction message.
             * @function verify
             * @memberof proto.SyncActionValue.BotWelcomeRequestAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BotWelcomeRequestAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.isSent != null && Object.hasOwnProperty.call(message, "isSent")) {
                    properties._isSent = 1;
                    if (typeof message.isSent !== "boolean")
                        return "isSent: boolean expected";
                }
                return null;
            };

            /**
             * Creates a BotWelcomeRequestAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.BotWelcomeRequestAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.BotWelcomeRequestAction} BotWelcomeRequestAction
             */
            BotWelcomeRequestAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.BotWelcomeRequestAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.isSent != null)
                    message.isSent = Boolean(object.isSent);
                return message;
            };

            /**
             * Creates a plain object from a BotWelcomeRequestAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.BotWelcomeRequestAction
             * @static
             * @param {proto.SyncActionValue.BotWelcomeRequestAction} message BotWelcomeRequestAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BotWelcomeRequestAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.isSent != null && Object.hasOwnProperty.call(message, "isSent"))
                    object.isSent = message.isSent;
                return object;
            };

            /**
             * Converts this BotWelcomeRequestAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.BotWelcomeRequestAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BotWelcomeRequestAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for BotWelcomeRequestAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.BotWelcomeRequestAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            BotWelcomeRequestAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.BotWelcomeRequestAction";
            };

            return BotWelcomeRequestAction;
        })();

        SyncActionValue.BroadcastListParticipant = (function() {

            /**
             * Properties of a BroadcastListParticipant.
             * @memberof proto.SyncActionValue
             * @interface IBroadcastListParticipant
             * @property {string|null} [lidJid] BroadcastListParticipant lidJid
             * @property {string|null} [pnJid] BroadcastListParticipant pnJid
             */

            /**
             * Constructs a new BroadcastListParticipant.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a BroadcastListParticipant.
             * @implements IBroadcastListParticipant
             * @constructor
             * @param {proto.SyncActionValue.IBroadcastListParticipant=} [properties] Properties to set
             */
            function BroadcastListParticipant(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * BroadcastListParticipant lidJid.
             * @member {string|null|undefined} lidJid
             * @memberof proto.SyncActionValue.BroadcastListParticipant
             * @instance
             */
            BroadcastListParticipant.prototype.lidJid = null;

            /**
             * BroadcastListParticipant pnJid.
             * @member {string|null|undefined} pnJid
             * @memberof proto.SyncActionValue.BroadcastListParticipant
             * @instance
             */
            BroadcastListParticipant.prototype.pnJid = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BroadcastListParticipant.prototype, "_lidJid", {
                get: $util.oneOfGetter($oneOfFields = ["lidJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BroadcastListParticipant.prototype, "_pnJid", {
                get: $util.oneOfGetter($oneOfFields = ["pnJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BroadcastListParticipant instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.BroadcastListParticipant
             * @static
             * @param {proto.SyncActionValue.IBroadcastListParticipant=} [properties] Properties to set
             * @returns {proto.SyncActionValue.BroadcastListParticipant} BroadcastListParticipant instance
             */
            BroadcastListParticipant.create = function create(properties) {
                return new BroadcastListParticipant(properties);
            };

            /**
             * Encodes the specified BroadcastListParticipant message. Does not implicitly {@link proto.SyncActionValue.BroadcastListParticipant.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.BroadcastListParticipant
             * @static
             * @param {proto.SyncActionValue.IBroadcastListParticipant} message BroadcastListParticipant message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BroadcastListParticipant.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.lidJid != null && Object.hasOwnProperty.call(message, "lidJid"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.lidJid);
                if (message.pnJid != null && Object.hasOwnProperty.call(message, "pnJid"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.pnJid);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified BroadcastListParticipant message, length delimited. Does not implicitly {@link proto.SyncActionValue.BroadcastListParticipant.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.BroadcastListParticipant
             * @static
             * @param {proto.SyncActionValue.IBroadcastListParticipant} message BroadcastListParticipant message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BroadcastListParticipant.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a BroadcastListParticipant message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.BroadcastListParticipant
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.BroadcastListParticipant} BroadcastListParticipant
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BroadcastListParticipant.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.lidJid = reader.stringVerify();
                            message._lidJid = "lidJid";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.pnJid = reader.stringVerify();
                            message._pnJid = "pnJid";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a BroadcastListParticipant message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.BroadcastListParticipant
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.BroadcastListParticipant} BroadcastListParticipant
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BroadcastListParticipant.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BroadcastListParticipant message.
             * @function verify
             * @memberof proto.SyncActionValue.BroadcastListParticipant
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BroadcastListParticipant.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.lidJid != null && Object.hasOwnProperty.call(message, "lidJid")) {
                    properties._lidJid = 1;
                    if (!$util.isString(message.lidJid))
                        return "lidJid: string expected";
                }
                if (message.pnJid != null && Object.hasOwnProperty.call(message, "pnJid")) {
                    properties._pnJid = 1;
                    if (!$util.isString(message.pnJid))
                        return "pnJid: string expected";
                }
                return null;
            };

            /**
             * Creates a BroadcastListParticipant message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.BroadcastListParticipant
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.BroadcastListParticipant} BroadcastListParticipant
             */
            BroadcastListParticipant.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.BroadcastListParticipant: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.lidJid != null)
                    message.lidJid = String(object.lidJid);
                if (object.pnJid != null)
                    message.pnJid = String(object.pnJid);
                return message;
            };

            /**
             * Creates a plain object from a BroadcastListParticipant message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.BroadcastListParticipant
             * @static
             * @param {proto.SyncActionValue.BroadcastListParticipant} message BroadcastListParticipant
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BroadcastListParticipant.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.lidJid != null && Object.hasOwnProperty.call(message, "lidJid"))
                    object.lidJid = message.lidJid;
                if (message.pnJid != null && Object.hasOwnProperty.call(message, "pnJid"))
                    object.pnJid = message.pnJid;
                return object;
            };

            /**
             * Converts this BroadcastListParticipant to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.BroadcastListParticipant
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BroadcastListParticipant.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for BroadcastListParticipant
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.BroadcastListParticipant
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            BroadcastListParticipant.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.BroadcastListParticipant";
            };

            return BroadcastListParticipant;
        })();

        SyncActionValue.BusinessBroadcastAssociationAction = (function() {

            /**
             * Properties of a BusinessBroadcastAssociationAction.
             * @memberof proto.SyncActionValue
             * @interface IBusinessBroadcastAssociationAction
             * @property {boolean|null} [deleted] BusinessBroadcastAssociationAction deleted
             */

            /**
             * Constructs a new BusinessBroadcastAssociationAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a BusinessBroadcastAssociationAction.
             * @implements IBusinessBroadcastAssociationAction
             * @constructor
             * @param {proto.SyncActionValue.IBusinessBroadcastAssociationAction=} [properties] Properties to set
             */
            function BusinessBroadcastAssociationAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * BusinessBroadcastAssociationAction deleted.
             * @member {boolean|null|undefined} deleted
             * @memberof proto.SyncActionValue.BusinessBroadcastAssociationAction
             * @instance
             */
            BusinessBroadcastAssociationAction.prototype.deleted = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcastAssociationAction.prototype, "_deleted", {
                get: $util.oneOfGetter($oneOfFields = ["deleted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BusinessBroadcastAssociationAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.BusinessBroadcastAssociationAction
             * @static
             * @param {proto.SyncActionValue.IBusinessBroadcastAssociationAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.BusinessBroadcastAssociationAction} BusinessBroadcastAssociationAction instance
             */
            BusinessBroadcastAssociationAction.create = function create(properties) {
                return new BusinessBroadcastAssociationAction(properties);
            };

            /**
             * Encodes the specified BusinessBroadcastAssociationAction message. Does not implicitly {@link proto.SyncActionValue.BusinessBroadcastAssociationAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.BusinessBroadcastAssociationAction
             * @static
             * @param {proto.SyncActionValue.IBusinessBroadcastAssociationAction} message BusinessBroadcastAssociationAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BusinessBroadcastAssociationAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.deleted != null && Object.hasOwnProperty.call(message, "deleted"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.deleted);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified BusinessBroadcastAssociationAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.BusinessBroadcastAssociationAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.BusinessBroadcastAssociationAction
             * @static
             * @param {proto.SyncActionValue.IBusinessBroadcastAssociationAction} message BusinessBroadcastAssociationAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BusinessBroadcastAssociationAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a BusinessBroadcastAssociationAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.BusinessBroadcastAssociationAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.BusinessBroadcastAssociationAction} BusinessBroadcastAssociationAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BusinessBroadcastAssociationAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.deleted = reader.bool();
                            message._deleted = "deleted";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a BusinessBroadcastAssociationAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.BusinessBroadcastAssociationAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.BusinessBroadcastAssociationAction} BusinessBroadcastAssociationAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BusinessBroadcastAssociationAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BusinessBroadcastAssociationAction message.
             * @function verify
             * @memberof proto.SyncActionValue.BusinessBroadcastAssociationAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BusinessBroadcastAssociationAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.deleted != null && Object.hasOwnProperty.call(message, "deleted")) {
                    properties._deleted = 1;
                    if (typeof message.deleted !== "boolean")
                        return "deleted: boolean expected";
                }
                return null;
            };

            /**
             * Creates a BusinessBroadcastAssociationAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.BusinessBroadcastAssociationAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.BusinessBroadcastAssociationAction} BusinessBroadcastAssociationAction
             */
            BusinessBroadcastAssociationAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.BusinessBroadcastAssociationAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.deleted != null)
                    message.deleted = Boolean(object.deleted);
                return message;
            };

            /**
             * Creates a plain object from a BusinessBroadcastAssociationAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.BusinessBroadcastAssociationAction
             * @static
             * @param {proto.SyncActionValue.BusinessBroadcastAssociationAction} message BusinessBroadcastAssociationAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BusinessBroadcastAssociationAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.deleted != null && Object.hasOwnProperty.call(message, "deleted"))
                    object.deleted = message.deleted;
                return object;
            };

            /**
             * Converts this BusinessBroadcastAssociationAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.BusinessBroadcastAssociationAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BusinessBroadcastAssociationAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for BusinessBroadcastAssociationAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.BusinessBroadcastAssociationAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            BusinessBroadcastAssociationAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.BusinessBroadcastAssociationAction";
            };

            return BusinessBroadcastAssociationAction;
        })();

        SyncActionValue.BusinessBroadcastCampaignAction = (function() {

            /**
             * Properties of a BusinessBroadcastCampaignAction.
             * @memberof proto.SyncActionValue
             * @interface IBusinessBroadcastCampaignAction
             * @property {number|null} [deviceId] BusinessBroadcastCampaignAction deviceId
             * @property {string|null} [adId] BusinessBroadcastCampaignAction adId
             * @property {string|null} [name] BusinessBroadcastCampaignAction name
             * @property {string|null} [msgId] BusinessBroadcastCampaignAction msgId
             * @property {string|null} [broadcastJid] BusinessBroadcastCampaignAction broadcastJid
             * @property {number|null} [reservedQuota] BusinessBroadcastCampaignAction reservedQuota
             * @property {number|Long|null} [scheduledTimestamp] BusinessBroadcastCampaignAction scheduledTimestamp
             * @property {number|Long|null} [createTimestamp] BusinessBroadcastCampaignAction createTimestamp
             * @property {proto.SyncActionValue.BusinessBroadcastCampaignStatus|null} [status] BusinessBroadcastCampaignAction status
             */

            /**
             * Constructs a new BusinessBroadcastCampaignAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a BusinessBroadcastCampaignAction.
             * @implements IBusinessBroadcastCampaignAction
             * @constructor
             * @param {proto.SyncActionValue.IBusinessBroadcastCampaignAction=} [properties] Properties to set
             */
            function BusinessBroadcastCampaignAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * BusinessBroadcastCampaignAction deviceId.
             * @member {number|null|undefined} deviceId
             * @memberof proto.SyncActionValue.BusinessBroadcastCampaignAction
             * @instance
             */
            BusinessBroadcastCampaignAction.prototype.deviceId = null;

            /**
             * BusinessBroadcastCampaignAction adId.
             * @member {string|null|undefined} adId
             * @memberof proto.SyncActionValue.BusinessBroadcastCampaignAction
             * @instance
             */
            BusinessBroadcastCampaignAction.prototype.adId = null;

            /**
             * BusinessBroadcastCampaignAction name.
             * @member {string|null|undefined} name
             * @memberof proto.SyncActionValue.BusinessBroadcastCampaignAction
             * @instance
             */
            BusinessBroadcastCampaignAction.prototype.name = null;

            /**
             * BusinessBroadcastCampaignAction msgId.
             * @member {string|null|undefined} msgId
             * @memberof proto.SyncActionValue.BusinessBroadcastCampaignAction
             * @instance
             */
            BusinessBroadcastCampaignAction.prototype.msgId = null;

            /**
             * BusinessBroadcastCampaignAction broadcastJid.
             * @member {string|null|undefined} broadcastJid
             * @memberof proto.SyncActionValue.BusinessBroadcastCampaignAction
             * @instance
             */
            BusinessBroadcastCampaignAction.prototype.broadcastJid = null;

            /**
             * BusinessBroadcastCampaignAction reservedQuota.
             * @member {number|null|undefined} reservedQuota
             * @memberof proto.SyncActionValue.BusinessBroadcastCampaignAction
             * @instance
             */
            BusinessBroadcastCampaignAction.prototype.reservedQuota = null;

            /**
             * BusinessBroadcastCampaignAction scheduledTimestamp.
             * @member {number|Long|null|undefined} scheduledTimestamp
             * @memberof proto.SyncActionValue.BusinessBroadcastCampaignAction
             * @instance
             */
            BusinessBroadcastCampaignAction.prototype.scheduledTimestamp = null;

            /**
             * BusinessBroadcastCampaignAction createTimestamp.
             * @member {number|Long|null|undefined} createTimestamp
             * @memberof proto.SyncActionValue.BusinessBroadcastCampaignAction
             * @instance
             */
            BusinessBroadcastCampaignAction.prototype.createTimestamp = null;

            /**
             * BusinessBroadcastCampaignAction status.
             * @member {proto.SyncActionValue.BusinessBroadcastCampaignStatus|null|undefined} status
             * @memberof proto.SyncActionValue.BusinessBroadcastCampaignAction
             * @instance
             */
            BusinessBroadcastCampaignAction.prototype.status = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcastCampaignAction.prototype, "_deviceId", {
                get: $util.oneOfGetter($oneOfFields = ["deviceId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcastCampaignAction.prototype, "_adId", {
                get: $util.oneOfGetter($oneOfFields = ["adId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcastCampaignAction.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcastCampaignAction.prototype, "_msgId", {
                get: $util.oneOfGetter($oneOfFields = ["msgId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcastCampaignAction.prototype, "_broadcastJid", {
                get: $util.oneOfGetter($oneOfFields = ["broadcastJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcastCampaignAction.prototype, "_reservedQuota", {
                get: $util.oneOfGetter($oneOfFields = ["reservedQuota"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcastCampaignAction.prototype, "_scheduledTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["scheduledTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcastCampaignAction.prototype, "_createTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["createTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcastCampaignAction.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BusinessBroadcastCampaignAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.BusinessBroadcastCampaignAction
             * @static
             * @param {proto.SyncActionValue.IBusinessBroadcastCampaignAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.BusinessBroadcastCampaignAction} BusinessBroadcastCampaignAction instance
             */
            BusinessBroadcastCampaignAction.create = function create(properties) {
                return new BusinessBroadcastCampaignAction(properties);
            };

            /**
             * Encodes the specified BusinessBroadcastCampaignAction message. Does not implicitly {@link proto.SyncActionValue.BusinessBroadcastCampaignAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.BusinessBroadcastCampaignAction
             * @static
             * @param {proto.SyncActionValue.IBusinessBroadcastCampaignAction} message BusinessBroadcastCampaignAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BusinessBroadcastCampaignAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.deviceId);
                if (message.adId != null && Object.hasOwnProperty.call(message, "adId"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.adId);
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.name);
                if (message.msgId != null && Object.hasOwnProperty.call(message, "msgId"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.msgId);
                if (message.broadcastJid != null && Object.hasOwnProperty.call(message, "broadcastJid"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.broadcastJid);
                if (message.reservedQuota != null && Object.hasOwnProperty.call(message, "reservedQuota"))
                    writer.uint32(/* id 6, wireType 0 =*/48).int32(message.reservedQuota);
                if (message.scheduledTimestamp != null && Object.hasOwnProperty.call(message, "scheduledTimestamp"))
                    writer.uint32(/* id 7, wireType 0 =*/56).int64(message.scheduledTimestamp);
                if (message.createTimestamp != null && Object.hasOwnProperty.call(message, "createTimestamp"))
                    writer.uint32(/* id 8, wireType 0 =*/64).int64(message.createTimestamp);
                if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 9, wireType 0 =*/72).int32(message.status);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified BusinessBroadcastCampaignAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.BusinessBroadcastCampaignAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.BusinessBroadcastCampaignAction
             * @static
             * @param {proto.SyncActionValue.IBusinessBroadcastCampaignAction} message BusinessBroadcastCampaignAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BusinessBroadcastCampaignAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a BusinessBroadcastCampaignAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.BusinessBroadcastCampaignAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.BusinessBroadcastCampaignAction} BusinessBroadcastCampaignAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BusinessBroadcastCampaignAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.deviceId = reader.int32();
                            message._deviceId = "deviceId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.adId = reader.stringVerify();
                            message._adId = "adId";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.msgId = reader.stringVerify();
                            message._msgId = "msgId";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.broadcastJid = reader.stringVerify();
                            message._broadcastJid = "broadcastJid";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.reservedQuota = reader.int32();
                            message._reservedQuota = "reservedQuota";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            message.scheduledTimestamp = reader.int64();
                            message._scheduledTimestamp = "scheduledTimestamp";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            message.createTimestamp = reader.int64();
                            message._createTimestamp = "createTimestamp";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            message.status = reader.int32();
                            message._status = "status";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a BusinessBroadcastCampaignAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.BusinessBroadcastCampaignAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.BusinessBroadcastCampaignAction} BusinessBroadcastCampaignAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BusinessBroadcastCampaignAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BusinessBroadcastCampaignAction message.
             * @function verify
             * @memberof proto.SyncActionValue.BusinessBroadcastCampaignAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BusinessBroadcastCampaignAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId")) {
                    properties._deviceId = 1;
                    if (!$util.isInteger(message.deviceId))
                        return "deviceId: integer expected";
                }
                if (message.adId != null && Object.hasOwnProperty.call(message, "adId")) {
                    properties._adId = 1;
                    if (!$util.isString(message.adId))
                        return "adId: string expected";
                }
                if (message.name != null && Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                if (message.msgId != null && Object.hasOwnProperty.call(message, "msgId")) {
                    properties._msgId = 1;
                    if (!$util.isString(message.msgId))
                        return "msgId: string expected";
                }
                if (message.broadcastJid != null && Object.hasOwnProperty.call(message, "broadcastJid")) {
                    properties._broadcastJid = 1;
                    if (!$util.isString(message.broadcastJid))
                        return "broadcastJid: string expected";
                }
                if (message.reservedQuota != null && Object.hasOwnProperty.call(message, "reservedQuota")) {
                    properties._reservedQuota = 1;
                    if (!$util.isInteger(message.reservedQuota))
                        return "reservedQuota: integer expected";
                }
                if (message.scheduledTimestamp != null && Object.hasOwnProperty.call(message, "scheduledTimestamp")) {
                    properties._scheduledTimestamp = 1;
                    if (!$util.isInteger(message.scheduledTimestamp) && !(message.scheduledTimestamp && $util.isInteger(message.scheduledTimestamp.low) && $util.isInteger(message.scheduledTimestamp.high)))
                        return "scheduledTimestamp: integer|Long expected";
                }
                if (message.createTimestamp != null && Object.hasOwnProperty.call(message, "createTimestamp")) {
                    properties._createTimestamp = 1;
                    if (!$util.isInteger(message.createTimestamp) && !(message.createTimestamp && $util.isInteger(message.createTimestamp.low) && $util.isInteger(message.createTimestamp.high)))
                        return "createTimestamp: integer|Long expected";
                }
                if (message.status != null && Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (typeof message.status !== "number" || (message.status | 0) !== message.status)
                        return "status: enum value expected";
                }
                return null;
            };

            /**
             * Creates a BusinessBroadcastCampaignAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.BusinessBroadcastCampaignAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.BusinessBroadcastCampaignAction} BusinessBroadcastCampaignAction
             */
            BusinessBroadcastCampaignAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.BusinessBroadcastCampaignAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.deviceId != null)
                    message.deviceId = object.deviceId | 0;
                if (object.adId != null)
                    message.adId = String(object.adId);
                if (object.name != null)
                    message.name = String(object.name);
                if (object.msgId != null)
                    message.msgId = String(object.msgId);
                if (object.broadcastJid != null)
                    message.broadcastJid = String(object.broadcastJid);
                if (object.reservedQuota != null)
                    message.reservedQuota = object.reservedQuota | 0;
                if (object.scheduledTimestamp != null)
                    if ($util.Long)
                        message.scheduledTimestamp = $util.Long.fromValue(object.scheduledTimestamp, false);
                    else if (typeof object.scheduledTimestamp === "string")
                        message.scheduledTimestamp = parseInt(object.scheduledTimestamp, 10);
                    else if (typeof object.scheduledTimestamp === "number")
                        message.scheduledTimestamp = object.scheduledTimestamp;
                    else if (typeof object.scheduledTimestamp === "object")
                        message.scheduledTimestamp = new $util.LongBits(object.scheduledTimestamp.low >>> 0, object.scheduledTimestamp.high >>> 0).toNumber();
                if (object.createTimestamp != null)
                    if ($util.Long)
                        message.createTimestamp = $util.Long.fromValue(object.createTimestamp, false);
                    else if (typeof object.createTimestamp === "string")
                        message.createTimestamp = parseInt(object.createTimestamp, 10);
                    else if (typeof object.createTimestamp === "number")
                        message.createTimestamp = object.createTimestamp;
                    else if (typeof object.createTimestamp === "object")
                        message.createTimestamp = new $util.LongBits(object.createTimestamp.low >>> 0, object.createTimestamp.high >>> 0).toNumber();
                switch (object.status) {
                case "DRAFT":
                case 1:
                    message.status = 1;
                    break;
                case "SCHEDULED":
                case 2:
                    message.status = 2;
                    break;
                case "PROCESSING":
                case 3:
                    message.status = 3;
                    break;
                case "FAILED":
                case 4:
                    message.status = 4;
                    break;
                case "SENT":
                case 5:
                    message.status = 5;
                    break;
                default:
                    if (typeof object.status === "number" && (object.status | 0) === object.status)
                        message.status = object.status;
                }
                return message;
            };

            /**
             * Creates a plain object from a BusinessBroadcastCampaignAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.BusinessBroadcastCampaignAction
             * @static
             * @param {proto.SyncActionValue.BusinessBroadcastCampaignAction} message BusinessBroadcastCampaignAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BusinessBroadcastCampaignAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                    object.deviceId = message.deviceId;
                if (message.adId != null && Object.hasOwnProperty.call(message, "adId"))
                    object.adId = message.adId;
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.msgId != null && Object.hasOwnProperty.call(message, "msgId"))
                    object.msgId = message.msgId;
                if (message.broadcastJid != null && Object.hasOwnProperty.call(message, "broadcastJid"))
                    object.broadcastJid = message.broadcastJid;
                if (message.reservedQuota != null && Object.hasOwnProperty.call(message, "reservedQuota"))
                    object.reservedQuota = message.reservedQuota;
                if (message.scheduledTimestamp != null && Object.hasOwnProperty.call(message, "scheduledTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.scheduledTimestamp = typeof message.scheduledTimestamp === "number" ? BigInt(message.scheduledTimestamp) : $util.Long.fromBits(message.scheduledTimestamp.low >>> 0, message.scheduledTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.scheduledTimestamp === "number")
                        object.scheduledTimestamp = options.longs === String ? String(message.scheduledTimestamp) : message.scheduledTimestamp;
                    else
                        object.scheduledTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.scheduledTimestamp) : options.longs === Number ? new $util.LongBits(message.scheduledTimestamp.low >>> 0, message.scheduledTimestamp.high >>> 0).toNumber() : message.scheduledTimestamp;
                if (message.createTimestamp != null && Object.hasOwnProperty.call(message, "createTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.createTimestamp = typeof message.createTimestamp === "number" ? BigInt(message.createTimestamp) : $util.Long.fromBits(message.createTimestamp.low >>> 0, message.createTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.createTimestamp === "number")
                        object.createTimestamp = options.longs === String ? String(message.createTimestamp) : message.createTimestamp;
                    else
                        object.createTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.createTimestamp) : options.longs === Number ? new $util.LongBits(message.createTimestamp.low >>> 0, message.createTimestamp.high >>> 0).toNumber() : message.createTimestamp;
                if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                    object.status = options.enums === String ? $root.proto.SyncActionValue.BusinessBroadcastCampaignStatus[message.status] === undefined ? message.status : $root.proto.SyncActionValue.BusinessBroadcastCampaignStatus[message.status] : message.status;
                return object;
            };

            /**
             * Converts this BusinessBroadcastCampaignAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.BusinessBroadcastCampaignAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BusinessBroadcastCampaignAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for BusinessBroadcastCampaignAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.BusinessBroadcastCampaignAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            BusinessBroadcastCampaignAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.BusinessBroadcastCampaignAction";
            };

            return BusinessBroadcastCampaignAction;
        })();

        /**
         * BusinessBroadcastCampaignStatus enum.
         * @name proto.SyncActionValue.BusinessBroadcastCampaignStatus
         * @enum {number}
         * @property {number} DRAFT=1 DRAFT value
         * @property {number} SCHEDULED=2 SCHEDULED value
         * @property {number} PROCESSING=3 PROCESSING value
         * @property {number} FAILED=4 FAILED value
         * @property {number} SENT=5 SENT value
         */
        SyncActionValue.BusinessBroadcastCampaignStatus = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[1] = "DRAFT"] = 1;
            values[valuesById[2] = "SCHEDULED"] = 2;
            values[valuesById[3] = "PROCESSING"] = 3;
            values[valuesById[4] = "FAILED"] = 4;
            values[valuesById[5] = "SENT"] = 5;
            return values;
        })();

        SyncActionValue.BusinessBroadcastInsightsAction = (function() {

            /**
             * Properties of a BusinessBroadcastInsightsAction.
             * @memberof proto.SyncActionValue
             * @interface IBusinessBroadcastInsightsAction
             * @property {number|null} [recipientCount] BusinessBroadcastInsightsAction recipientCount
             * @property {number|null} [deliveredCount] BusinessBroadcastInsightsAction deliveredCount
             * @property {number|null} [readCount] BusinessBroadcastInsightsAction readCount
             * @property {number|null} [repliedCount] BusinessBroadcastInsightsAction repliedCount
             * @property {number|null} [quickReplyCount] BusinessBroadcastInsightsAction quickReplyCount
             */

            /**
             * Constructs a new BusinessBroadcastInsightsAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a BusinessBroadcastInsightsAction.
             * @implements IBusinessBroadcastInsightsAction
             * @constructor
             * @param {proto.SyncActionValue.IBusinessBroadcastInsightsAction=} [properties] Properties to set
             */
            function BusinessBroadcastInsightsAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * BusinessBroadcastInsightsAction recipientCount.
             * @member {number|null|undefined} recipientCount
             * @memberof proto.SyncActionValue.BusinessBroadcastInsightsAction
             * @instance
             */
            BusinessBroadcastInsightsAction.prototype.recipientCount = null;

            /**
             * BusinessBroadcastInsightsAction deliveredCount.
             * @member {number|null|undefined} deliveredCount
             * @memberof proto.SyncActionValue.BusinessBroadcastInsightsAction
             * @instance
             */
            BusinessBroadcastInsightsAction.prototype.deliveredCount = null;

            /**
             * BusinessBroadcastInsightsAction readCount.
             * @member {number|null|undefined} readCount
             * @memberof proto.SyncActionValue.BusinessBroadcastInsightsAction
             * @instance
             */
            BusinessBroadcastInsightsAction.prototype.readCount = null;

            /**
             * BusinessBroadcastInsightsAction repliedCount.
             * @member {number|null|undefined} repliedCount
             * @memberof proto.SyncActionValue.BusinessBroadcastInsightsAction
             * @instance
             */
            BusinessBroadcastInsightsAction.prototype.repliedCount = null;

            /**
             * BusinessBroadcastInsightsAction quickReplyCount.
             * @member {number|null|undefined} quickReplyCount
             * @memberof proto.SyncActionValue.BusinessBroadcastInsightsAction
             * @instance
             */
            BusinessBroadcastInsightsAction.prototype.quickReplyCount = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcastInsightsAction.prototype, "_recipientCount", {
                get: $util.oneOfGetter($oneOfFields = ["recipientCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcastInsightsAction.prototype, "_deliveredCount", {
                get: $util.oneOfGetter($oneOfFields = ["deliveredCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcastInsightsAction.prototype, "_readCount", {
                get: $util.oneOfGetter($oneOfFields = ["readCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcastInsightsAction.prototype, "_repliedCount", {
                get: $util.oneOfGetter($oneOfFields = ["repliedCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcastInsightsAction.prototype, "_quickReplyCount", {
                get: $util.oneOfGetter($oneOfFields = ["quickReplyCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BusinessBroadcastInsightsAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.BusinessBroadcastInsightsAction
             * @static
             * @param {proto.SyncActionValue.IBusinessBroadcastInsightsAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.BusinessBroadcastInsightsAction} BusinessBroadcastInsightsAction instance
             */
            BusinessBroadcastInsightsAction.create = function create(properties) {
                return new BusinessBroadcastInsightsAction(properties);
            };

            /**
             * Encodes the specified BusinessBroadcastInsightsAction message. Does not implicitly {@link proto.SyncActionValue.BusinessBroadcastInsightsAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.BusinessBroadcastInsightsAction
             * @static
             * @param {proto.SyncActionValue.IBusinessBroadcastInsightsAction} message BusinessBroadcastInsightsAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BusinessBroadcastInsightsAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.recipientCount != null && Object.hasOwnProperty.call(message, "recipientCount"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.recipientCount);
                if (message.deliveredCount != null && Object.hasOwnProperty.call(message, "deliveredCount"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.deliveredCount);
                if (message.readCount != null && Object.hasOwnProperty.call(message, "readCount"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.readCount);
                if (message.repliedCount != null && Object.hasOwnProperty.call(message, "repliedCount"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int32(message.repliedCount);
                if (message.quickReplyCount != null && Object.hasOwnProperty.call(message, "quickReplyCount"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int32(message.quickReplyCount);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified BusinessBroadcastInsightsAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.BusinessBroadcastInsightsAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.BusinessBroadcastInsightsAction
             * @static
             * @param {proto.SyncActionValue.IBusinessBroadcastInsightsAction} message BusinessBroadcastInsightsAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BusinessBroadcastInsightsAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a BusinessBroadcastInsightsAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.BusinessBroadcastInsightsAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.BusinessBroadcastInsightsAction} BusinessBroadcastInsightsAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BusinessBroadcastInsightsAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.recipientCount = reader.int32();
                            message._recipientCount = "recipientCount";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.deliveredCount = reader.int32();
                            message._deliveredCount = "deliveredCount";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.readCount = reader.int32();
                            message._readCount = "readCount";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.repliedCount = reader.int32();
                            message._repliedCount = "repliedCount";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.quickReplyCount = reader.int32();
                            message._quickReplyCount = "quickReplyCount";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a BusinessBroadcastInsightsAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.BusinessBroadcastInsightsAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.BusinessBroadcastInsightsAction} BusinessBroadcastInsightsAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BusinessBroadcastInsightsAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BusinessBroadcastInsightsAction message.
             * @function verify
             * @memberof proto.SyncActionValue.BusinessBroadcastInsightsAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BusinessBroadcastInsightsAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.recipientCount != null && Object.hasOwnProperty.call(message, "recipientCount")) {
                    properties._recipientCount = 1;
                    if (!$util.isInteger(message.recipientCount))
                        return "recipientCount: integer expected";
                }
                if (message.deliveredCount != null && Object.hasOwnProperty.call(message, "deliveredCount")) {
                    properties._deliveredCount = 1;
                    if (!$util.isInteger(message.deliveredCount))
                        return "deliveredCount: integer expected";
                }
                if (message.readCount != null && Object.hasOwnProperty.call(message, "readCount")) {
                    properties._readCount = 1;
                    if (!$util.isInteger(message.readCount))
                        return "readCount: integer expected";
                }
                if (message.repliedCount != null && Object.hasOwnProperty.call(message, "repliedCount")) {
                    properties._repliedCount = 1;
                    if (!$util.isInteger(message.repliedCount))
                        return "repliedCount: integer expected";
                }
                if (message.quickReplyCount != null && Object.hasOwnProperty.call(message, "quickReplyCount")) {
                    properties._quickReplyCount = 1;
                    if (!$util.isInteger(message.quickReplyCount))
                        return "quickReplyCount: integer expected";
                }
                return null;
            };

            /**
             * Creates a BusinessBroadcastInsightsAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.BusinessBroadcastInsightsAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.BusinessBroadcastInsightsAction} BusinessBroadcastInsightsAction
             */
            BusinessBroadcastInsightsAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.BusinessBroadcastInsightsAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.recipientCount != null)
                    message.recipientCount = object.recipientCount | 0;
                if (object.deliveredCount != null)
                    message.deliveredCount = object.deliveredCount | 0;
                if (object.readCount != null)
                    message.readCount = object.readCount | 0;
                if (object.repliedCount != null)
                    message.repliedCount = object.repliedCount | 0;
                if (object.quickReplyCount != null)
                    message.quickReplyCount = object.quickReplyCount | 0;
                return message;
            };

            /**
             * Creates a plain object from a BusinessBroadcastInsightsAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.BusinessBroadcastInsightsAction
             * @static
             * @param {proto.SyncActionValue.BusinessBroadcastInsightsAction} message BusinessBroadcastInsightsAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BusinessBroadcastInsightsAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.recipientCount != null && Object.hasOwnProperty.call(message, "recipientCount"))
                    object.recipientCount = message.recipientCount;
                if (message.deliveredCount != null && Object.hasOwnProperty.call(message, "deliveredCount"))
                    object.deliveredCount = message.deliveredCount;
                if (message.readCount != null && Object.hasOwnProperty.call(message, "readCount"))
                    object.readCount = message.readCount;
                if (message.repliedCount != null && Object.hasOwnProperty.call(message, "repliedCount"))
                    object.repliedCount = message.repliedCount;
                if (message.quickReplyCount != null && Object.hasOwnProperty.call(message, "quickReplyCount"))
                    object.quickReplyCount = message.quickReplyCount;
                return object;
            };

            /**
             * Converts this BusinessBroadcastInsightsAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.BusinessBroadcastInsightsAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BusinessBroadcastInsightsAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for BusinessBroadcastInsightsAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.BusinessBroadcastInsightsAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            BusinessBroadcastInsightsAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.BusinessBroadcastInsightsAction";
            };

            return BusinessBroadcastInsightsAction;
        })();

        SyncActionValue.BusinessBroadcastListAction = (function() {

            /**
             * Properties of a BusinessBroadcastListAction.
             * @memberof proto.SyncActionValue
             * @interface IBusinessBroadcastListAction
             * @property {boolean|null} [deleted] BusinessBroadcastListAction deleted
             * @property {Array.<proto.SyncActionValue.IBroadcastListParticipant>|null} [participants] BusinessBroadcastListAction participants
             * @property {string|null} [listName] BusinessBroadcastListAction listName
             * @property {Array.<string>|null} [labelIds] BusinessBroadcastListAction labelIds
             * @property {string|null} [audienceExpression] BusinessBroadcastListAction audienceExpression
             */

            /**
             * Constructs a new BusinessBroadcastListAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a BusinessBroadcastListAction.
             * @implements IBusinessBroadcastListAction
             * @constructor
             * @param {proto.SyncActionValue.IBusinessBroadcastListAction=} [properties] Properties to set
             */
            function BusinessBroadcastListAction(properties) {
                this.participants = [];
                this.labelIds = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * BusinessBroadcastListAction deleted.
             * @member {boolean|null|undefined} deleted
             * @memberof proto.SyncActionValue.BusinessBroadcastListAction
             * @instance
             */
            BusinessBroadcastListAction.prototype.deleted = null;

            /**
             * BusinessBroadcastListAction participants.
             * @member {Array.<proto.SyncActionValue.IBroadcastListParticipant>} participants
             * @memberof proto.SyncActionValue.BusinessBroadcastListAction
             * @instance
             */
            BusinessBroadcastListAction.prototype.participants = $util.emptyArray;

            /**
             * BusinessBroadcastListAction listName.
             * @member {string|null|undefined} listName
             * @memberof proto.SyncActionValue.BusinessBroadcastListAction
             * @instance
             */
            BusinessBroadcastListAction.prototype.listName = null;

            /**
             * BusinessBroadcastListAction labelIds.
             * @member {Array.<string>} labelIds
             * @memberof proto.SyncActionValue.BusinessBroadcastListAction
             * @instance
             */
            BusinessBroadcastListAction.prototype.labelIds = $util.emptyArray;

            /**
             * BusinessBroadcastListAction audienceExpression.
             * @member {string|null|undefined} audienceExpression
             * @memberof proto.SyncActionValue.BusinessBroadcastListAction
             * @instance
             */
            BusinessBroadcastListAction.prototype.audienceExpression = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcastListAction.prototype, "_deleted", {
                get: $util.oneOfGetter($oneOfFields = ["deleted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcastListAction.prototype, "_listName", {
                get: $util.oneOfGetter($oneOfFields = ["listName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessBroadcastListAction.prototype, "_audienceExpression", {
                get: $util.oneOfGetter($oneOfFields = ["audienceExpression"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new BusinessBroadcastListAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.BusinessBroadcastListAction
             * @static
             * @param {proto.SyncActionValue.IBusinessBroadcastListAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.BusinessBroadcastListAction} BusinessBroadcastListAction instance
             */
            BusinessBroadcastListAction.create = function create(properties) {
                return new BusinessBroadcastListAction(properties);
            };

            /**
             * Encodes the specified BusinessBroadcastListAction message. Does not implicitly {@link proto.SyncActionValue.BusinessBroadcastListAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.BusinessBroadcastListAction
             * @static
             * @param {proto.SyncActionValue.IBusinessBroadcastListAction} message BusinessBroadcastListAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BusinessBroadcastListAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.deleted != null && Object.hasOwnProperty.call(message, "deleted"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.deleted);
                if (message.participants != null && message.participants.length)
                    for (var i = 0; i < message.participants.length; ++i)
                        $root.proto.SyncActionValue.BroadcastListParticipant.encode(message.participants[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.listName != null && Object.hasOwnProperty.call(message, "listName"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.listName);
                if (message.labelIds != null && message.labelIds.length)
                    for (var i = 0; i < message.labelIds.length; ++i)
                        writer.uint32(/* id 4, wireType 2 =*/34).string(message.labelIds[i]);
                if (message.audienceExpression != null && Object.hasOwnProperty.call(message, "audienceExpression"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.audienceExpression);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified BusinessBroadcastListAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.BusinessBroadcastListAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.BusinessBroadcastListAction
             * @static
             * @param {proto.SyncActionValue.IBusinessBroadcastListAction} message BusinessBroadcastListAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BusinessBroadcastListAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a BusinessBroadcastListAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.BusinessBroadcastListAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.BusinessBroadcastListAction} BusinessBroadcastListAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BusinessBroadcastListAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.deleted = reader.bool();
                            message._deleted = "deleted";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(message.participants && message.participants.length))
                                message.participants = [];
                            message.participants.push($root.proto.SyncActionValue.BroadcastListParticipant.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.listName = reader.stringVerify();
                            message._listName = "listName";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            if (!(message.labelIds && message.labelIds.length))
                                message.labelIds = [];
                            message.labelIds.push(reader.stringVerify());
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.audienceExpression = reader.stringVerify();
                            message._audienceExpression = "audienceExpression";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a BusinessBroadcastListAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.BusinessBroadcastListAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.BusinessBroadcastListAction} BusinessBroadcastListAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BusinessBroadcastListAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BusinessBroadcastListAction message.
             * @function verify
             * @memberof proto.SyncActionValue.BusinessBroadcastListAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BusinessBroadcastListAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.deleted != null && Object.hasOwnProperty.call(message, "deleted")) {
                    properties._deleted = 1;
                    if (typeof message.deleted !== "boolean")
                        return "deleted: boolean expected";
                }
                if (message.participants != null && Object.hasOwnProperty.call(message, "participants")) {
                    if (!Array.isArray(message.participants))
                        return "participants: array expected";
                    for (var i = 0; i < message.participants.length; ++i) {
                        var error = $root.proto.SyncActionValue.BroadcastListParticipant.verify(message.participants[i], q + 1);
                        if (error)
                            return "participants." + error;
                    }
                }
                if (message.listName != null && Object.hasOwnProperty.call(message, "listName")) {
                    properties._listName = 1;
                    if (!$util.isString(message.listName))
                        return "listName: string expected";
                }
                if (message.labelIds != null && Object.hasOwnProperty.call(message, "labelIds")) {
                    if (!Array.isArray(message.labelIds))
                        return "labelIds: array expected";
                    for (var i = 0; i < message.labelIds.length; ++i)
                        if (!$util.isString(message.labelIds[i]))
                            return "labelIds: string[] expected";
                }
                if (message.audienceExpression != null && Object.hasOwnProperty.call(message, "audienceExpression")) {
                    properties._audienceExpression = 1;
                    if (!$util.isString(message.audienceExpression))
                        return "audienceExpression: string expected";
                }
                return null;
            };

            /**
             * Creates a BusinessBroadcastListAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.BusinessBroadcastListAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.BusinessBroadcastListAction} BusinessBroadcastListAction
             */
            BusinessBroadcastListAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.BusinessBroadcastListAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.deleted != null)
                    message.deleted = Boolean(object.deleted);
                if (object.participants) {
                    if (!Array.isArray(object.participants))
                        throw TypeError(".proto.SyncActionValue.BusinessBroadcastListAction.participants: array expected");
                    message.participants = Array(object.participants.length);
                    for (var i = 0; i < object.participants.length; ++i) {
                        if (!$util.isObject(object.participants[i]))
                            throw TypeError(".proto.SyncActionValue.BusinessBroadcastListAction.participants: object expected");
                        message.participants[i] = $root.proto.SyncActionValue.BroadcastListParticipant.fromObject(object.participants[i], q + 1);
                    }
                }
                if (object.listName != null)
                    message.listName = String(object.listName);
                if (object.labelIds) {
                    if (!Array.isArray(object.labelIds))
                        throw TypeError(".proto.SyncActionValue.BusinessBroadcastListAction.labelIds: array expected");
                    message.labelIds = Array(object.labelIds.length);
                    for (var i = 0; i < object.labelIds.length; ++i)
                        message.labelIds[i] = String(object.labelIds[i]);
                }
                if (object.audienceExpression != null)
                    message.audienceExpression = String(object.audienceExpression);
                return message;
            };

            /**
             * Creates a plain object from a BusinessBroadcastListAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.BusinessBroadcastListAction
             * @static
             * @param {proto.SyncActionValue.BusinessBroadcastListAction} message BusinessBroadcastListAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BusinessBroadcastListAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.participants = [];
                    object.labelIds = [];
                }
                if (message.deleted != null && Object.hasOwnProperty.call(message, "deleted"))
                    object.deleted = message.deleted;
                if (message.participants && message.participants.length) {
                    object.participants = Array(message.participants.length);
                    for (var j = 0; j < message.participants.length; ++j)
                        object.participants[j] = $root.proto.SyncActionValue.BroadcastListParticipant.toObject(message.participants[j], options, q + 1);
                }
                if (message.listName != null && Object.hasOwnProperty.call(message, "listName"))
                    object.listName = message.listName;
                if (message.labelIds && message.labelIds.length) {
                    object.labelIds = Array(message.labelIds.length);
                    for (var j = 0; j < message.labelIds.length; ++j)
                        object.labelIds[j] = message.labelIds[j];
                }
                if (message.audienceExpression != null && Object.hasOwnProperty.call(message, "audienceExpression"))
                    object.audienceExpression = message.audienceExpression;
                return object;
            };

            /**
             * Converts this BusinessBroadcastListAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.BusinessBroadcastListAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BusinessBroadcastListAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for BusinessBroadcastListAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.BusinessBroadcastListAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            BusinessBroadcastListAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.BusinessBroadcastListAction";
            };

            return BusinessBroadcastListAction;
        })();

        SyncActionValue.CallLogAction = (function() {

            /**
             * Properties of a CallLogAction.
             * @memberof proto.SyncActionValue
             * @interface ICallLogAction
             * @property {proto.ICallLogRecord|null} [callLogRecord] CallLogAction callLogRecord
             */

            /**
             * Constructs a new CallLogAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a CallLogAction.
             * @implements ICallLogAction
             * @constructor
             * @param {proto.SyncActionValue.ICallLogAction=} [properties] Properties to set
             */
            function CallLogAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * CallLogAction callLogRecord.
             * @member {proto.ICallLogRecord|null|undefined} callLogRecord
             * @memberof proto.SyncActionValue.CallLogAction
             * @instance
             */
            CallLogAction.prototype.callLogRecord = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CallLogAction.prototype, "_callLogRecord", {
                get: $util.oneOfGetter($oneOfFields = ["callLogRecord"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new CallLogAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.CallLogAction
             * @static
             * @param {proto.SyncActionValue.ICallLogAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.CallLogAction} CallLogAction instance
             */
            CallLogAction.create = function create(properties) {
                return new CallLogAction(properties);
            };

            /**
             * Encodes the specified CallLogAction message. Does not implicitly {@link proto.SyncActionValue.CallLogAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.CallLogAction
             * @static
             * @param {proto.SyncActionValue.ICallLogAction} message CallLogAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CallLogAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.callLogRecord != null && Object.hasOwnProperty.call(message, "callLogRecord"))
                    $root.proto.CallLogRecord.encode(message.callLogRecord, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CallLogAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.CallLogAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.CallLogAction
             * @static
             * @param {proto.SyncActionValue.ICallLogAction} message CallLogAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CallLogAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a CallLogAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.CallLogAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.CallLogAction} CallLogAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CallLogAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.callLogRecord = $root.proto.CallLogRecord.decode(reader, reader.uint32(), undefined, q + 1, message.callLogRecord);
                            message._callLogRecord = "callLogRecord";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a CallLogAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.CallLogAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.CallLogAction} CallLogAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CallLogAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CallLogAction message.
             * @function verify
             * @memberof proto.SyncActionValue.CallLogAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CallLogAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.callLogRecord != null && Object.hasOwnProperty.call(message, "callLogRecord")) {
                    properties._callLogRecord = 1;
                    {
                        var error = $root.proto.CallLogRecord.verify(message.callLogRecord, q + 1);
                        if (error)
                            return "callLogRecord." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a CallLogAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.CallLogAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.CallLogAction} CallLogAction
             */
            CallLogAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.CallLogAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.callLogRecord != null) {
                    if (!$util.isObject(object.callLogRecord))
                        throw TypeError(".proto.SyncActionValue.CallLogAction.callLogRecord: object expected");
                    message.callLogRecord = $root.proto.CallLogRecord.fromObject(object.callLogRecord, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a CallLogAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.CallLogAction
             * @static
             * @param {proto.SyncActionValue.CallLogAction} message CallLogAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CallLogAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.callLogRecord != null && Object.hasOwnProperty.call(message, "callLogRecord"))
                    object.callLogRecord = $root.proto.CallLogRecord.toObject(message.callLogRecord, options, q + 1);
                return object;
            };

            /**
             * Converts this CallLogAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.CallLogAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CallLogAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for CallLogAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.CallLogAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            CallLogAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.CallLogAction";
            };

            return CallLogAction;
        })();

        SyncActionValue.ChatAssignmentAction = (function() {

            /**
             * Properties of a ChatAssignmentAction.
             * @memberof proto.SyncActionValue
             * @interface IChatAssignmentAction
             * @property {string|null} [deviceAgentId] ChatAssignmentAction deviceAgentId
             */

            /**
             * Constructs a new ChatAssignmentAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a ChatAssignmentAction.
             * @implements IChatAssignmentAction
             * @constructor
             * @param {proto.SyncActionValue.IChatAssignmentAction=} [properties] Properties to set
             */
            function ChatAssignmentAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ChatAssignmentAction deviceAgentId.
             * @member {string|null|undefined} deviceAgentId
             * @memberof proto.SyncActionValue.ChatAssignmentAction
             * @instance
             */
            ChatAssignmentAction.prototype.deviceAgentId = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ChatAssignmentAction.prototype, "_deviceAgentId", {
                get: $util.oneOfGetter($oneOfFields = ["deviceAgentId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ChatAssignmentAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.ChatAssignmentAction
             * @static
             * @param {proto.SyncActionValue.IChatAssignmentAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.ChatAssignmentAction} ChatAssignmentAction instance
             */
            ChatAssignmentAction.create = function create(properties) {
                return new ChatAssignmentAction(properties);
            };

            /**
             * Encodes the specified ChatAssignmentAction message. Does not implicitly {@link proto.SyncActionValue.ChatAssignmentAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.ChatAssignmentAction
             * @static
             * @param {proto.SyncActionValue.IChatAssignmentAction} message ChatAssignmentAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ChatAssignmentAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.deviceAgentId != null && Object.hasOwnProperty.call(message, "deviceAgentId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.deviceAgentId);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ChatAssignmentAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.ChatAssignmentAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.ChatAssignmentAction
             * @static
             * @param {proto.SyncActionValue.IChatAssignmentAction} message ChatAssignmentAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ChatAssignmentAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ChatAssignmentAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.ChatAssignmentAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.ChatAssignmentAction} ChatAssignmentAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ChatAssignmentAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.deviceAgentId = reader.stringVerify();
                            message._deviceAgentId = "deviceAgentId";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ChatAssignmentAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.ChatAssignmentAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.ChatAssignmentAction} ChatAssignmentAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ChatAssignmentAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ChatAssignmentAction message.
             * @function verify
             * @memberof proto.SyncActionValue.ChatAssignmentAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ChatAssignmentAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.deviceAgentId != null && Object.hasOwnProperty.call(message, "deviceAgentId")) {
                    properties._deviceAgentId = 1;
                    if (!$util.isString(message.deviceAgentId))
                        return "deviceAgentId: string expected";
                }
                return null;
            };

            /**
             * Creates a ChatAssignmentAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.ChatAssignmentAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.ChatAssignmentAction} ChatAssignmentAction
             */
            ChatAssignmentAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.ChatAssignmentAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.deviceAgentId != null)
                    message.deviceAgentId = String(object.deviceAgentId);
                return message;
            };

            /**
             * Creates a plain object from a ChatAssignmentAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.ChatAssignmentAction
             * @static
             * @param {proto.SyncActionValue.ChatAssignmentAction} message ChatAssignmentAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ChatAssignmentAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.deviceAgentId != null && Object.hasOwnProperty.call(message, "deviceAgentId"))
                    object.deviceAgentId = message.deviceAgentId;
                return object;
            };

            /**
             * Converts this ChatAssignmentAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.ChatAssignmentAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ChatAssignmentAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ChatAssignmentAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.ChatAssignmentAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ChatAssignmentAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.ChatAssignmentAction";
            };

            return ChatAssignmentAction;
        })();

        SyncActionValue.ChatAssignmentOpenedStatusAction = (function() {

            /**
             * Properties of a ChatAssignmentOpenedStatusAction.
             * @memberof proto.SyncActionValue
             * @interface IChatAssignmentOpenedStatusAction
             * @property {boolean|null} [chatOpened] ChatAssignmentOpenedStatusAction chatOpened
             */

            /**
             * Constructs a new ChatAssignmentOpenedStatusAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a ChatAssignmentOpenedStatusAction.
             * @implements IChatAssignmentOpenedStatusAction
             * @constructor
             * @param {proto.SyncActionValue.IChatAssignmentOpenedStatusAction=} [properties] Properties to set
             */
            function ChatAssignmentOpenedStatusAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ChatAssignmentOpenedStatusAction chatOpened.
             * @member {boolean|null|undefined} chatOpened
             * @memberof proto.SyncActionValue.ChatAssignmentOpenedStatusAction
             * @instance
             */
            ChatAssignmentOpenedStatusAction.prototype.chatOpened = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ChatAssignmentOpenedStatusAction.prototype, "_chatOpened", {
                get: $util.oneOfGetter($oneOfFields = ["chatOpened"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ChatAssignmentOpenedStatusAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.ChatAssignmentOpenedStatusAction
             * @static
             * @param {proto.SyncActionValue.IChatAssignmentOpenedStatusAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.ChatAssignmentOpenedStatusAction} ChatAssignmentOpenedStatusAction instance
             */
            ChatAssignmentOpenedStatusAction.create = function create(properties) {
                return new ChatAssignmentOpenedStatusAction(properties);
            };

            /**
             * Encodes the specified ChatAssignmentOpenedStatusAction message. Does not implicitly {@link proto.SyncActionValue.ChatAssignmentOpenedStatusAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.ChatAssignmentOpenedStatusAction
             * @static
             * @param {proto.SyncActionValue.IChatAssignmentOpenedStatusAction} message ChatAssignmentOpenedStatusAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ChatAssignmentOpenedStatusAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.chatOpened != null && Object.hasOwnProperty.call(message, "chatOpened"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.chatOpened);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ChatAssignmentOpenedStatusAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.ChatAssignmentOpenedStatusAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.ChatAssignmentOpenedStatusAction
             * @static
             * @param {proto.SyncActionValue.IChatAssignmentOpenedStatusAction} message ChatAssignmentOpenedStatusAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ChatAssignmentOpenedStatusAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ChatAssignmentOpenedStatusAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.ChatAssignmentOpenedStatusAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.ChatAssignmentOpenedStatusAction} ChatAssignmentOpenedStatusAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ChatAssignmentOpenedStatusAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.chatOpened = reader.bool();
                            message._chatOpened = "chatOpened";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ChatAssignmentOpenedStatusAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.ChatAssignmentOpenedStatusAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.ChatAssignmentOpenedStatusAction} ChatAssignmentOpenedStatusAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ChatAssignmentOpenedStatusAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ChatAssignmentOpenedStatusAction message.
             * @function verify
             * @memberof proto.SyncActionValue.ChatAssignmentOpenedStatusAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ChatAssignmentOpenedStatusAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.chatOpened != null && Object.hasOwnProperty.call(message, "chatOpened")) {
                    properties._chatOpened = 1;
                    if (typeof message.chatOpened !== "boolean")
                        return "chatOpened: boolean expected";
                }
                return null;
            };

            /**
             * Creates a ChatAssignmentOpenedStatusAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.ChatAssignmentOpenedStatusAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.ChatAssignmentOpenedStatusAction} ChatAssignmentOpenedStatusAction
             */
            ChatAssignmentOpenedStatusAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.ChatAssignmentOpenedStatusAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.chatOpened != null)
                    message.chatOpened = Boolean(object.chatOpened);
                return message;
            };

            /**
             * Creates a plain object from a ChatAssignmentOpenedStatusAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.ChatAssignmentOpenedStatusAction
             * @static
             * @param {proto.SyncActionValue.ChatAssignmentOpenedStatusAction} message ChatAssignmentOpenedStatusAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ChatAssignmentOpenedStatusAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.chatOpened != null && Object.hasOwnProperty.call(message, "chatOpened"))
                    object.chatOpened = message.chatOpened;
                return object;
            };

            /**
             * Converts this ChatAssignmentOpenedStatusAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.ChatAssignmentOpenedStatusAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ChatAssignmentOpenedStatusAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ChatAssignmentOpenedStatusAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.ChatAssignmentOpenedStatusAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ChatAssignmentOpenedStatusAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.ChatAssignmentOpenedStatusAction";
            };

            return ChatAssignmentOpenedStatusAction;
        })();

        SyncActionValue.ClearChatAction = (function() {

            /**
             * Properties of a ClearChatAction.
             * @memberof proto.SyncActionValue
             * @interface IClearChatAction
             * @property {proto.SyncActionValue.ISyncActionMessageRange|null} [messageRange] ClearChatAction messageRange
             */

            /**
             * Constructs a new ClearChatAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a ClearChatAction.
             * @implements IClearChatAction
             * @constructor
             * @param {proto.SyncActionValue.IClearChatAction=} [properties] Properties to set
             */
            function ClearChatAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ClearChatAction messageRange.
             * @member {proto.SyncActionValue.ISyncActionMessageRange|null|undefined} messageRange
             * @memberof proto.SyncActionValue.ClearChatAction
             * @instance
             */
            ClearChatAction.prototype.messageRange = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ClearChatAction.prototype, "_messageRange", {
                get: $util.oneOfGetter($oneOfFields = ["messageRange"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ClearChatAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.ClearChatAction
             * @static
             * @param {proto.SyncActionValue.IClearChatAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.ClearChatAction} ClearChatAction instance
             */
            ClearChatAction.create = function create(properties) {
                return new ClearChatAction(properties);
            };

            /**
             * Encodes the specified ClearChatAction message. Does not implicitly {@link proto.SyncActionValue.ClearChatAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.ClearChatAction
             * @static
             * @param {proto.SyncActionValue.IClearChatAction} message ClearChatAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ClearChatAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.messageRange != null && Object.hasOwnProperty.call(message, "messageRange"))
                    $root.proto.SyncActionValue.SyncActionMessageRange.encode(message.messageRange, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ClearChatAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.ClearChatAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.ClearChatAction
             * @static
             * @param {proto.SyncActionValue.IClearChatAction} message ClearChatAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ClearChatAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ClearChatAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.ClearChatAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.ClearChatAction} ClearChatAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ClearChatAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.decode(reader, reader.uint32(), undefined, q + 1, message.messageRange);
                            message._messageRange = "messageRange";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ClearChatAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.ClearChatAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.ClearChatAction} ClearChatAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ClearChatAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ClearChatAction message.
             * @function verify
             * @memberof proto.SyncActionValue.ClearChatAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ClearChatAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.messageRange != null && Object.hasOwnProperty.call(message, "messageRange")) {
                    properties._messageRange = 1;
                    {
                        var error = $root.proto.SyncActionValue.SyncActionMessageRange.verify(message.messageRange, q + 1);
                        if (error)
                            return "messageRange." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a ClearChatAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.ClearChatAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.ClearChatAction} ClearChatAction
             */
            ClearChatAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.ClearChatAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.messageRange != null) {
                    if (!$util.isObject(object.messageRange))
                        throw TypeError(".proto.SyncActionValue.ClearChatAction.messageRange: object expected");
                    message.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.fromObject(object.messageRange, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a ClearChatAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.ClearChatAction
             * @static
             * @param {proto.SyncActionValue.ClearChatAction} message ClearChatAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ClearChatAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.messageRange != null && Object.hasOwnProperty.call(message, "messageRange"))
                    object.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.toObject(message.messageRange, options, q + 1);
                return object;
            };

            /**
             * Converts this ClearChatAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.ClearChatAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ClearChatAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ClearChatAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.ClearChatAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ClearChatAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.ClearChatAction";
            };

            return ClearChatAction;
        })();

        SyncActionValue.CoexV2VersionAction = (function() {

            /**
             * Properties of a CoexV2VersionAction.
             * @memberof proto.SyncActionValue
             * @interface ICoexV2VersionAction
             * @property {number|Long|null} [version] CoexV2VersionAction version
             */

            /**
             * Constructs a new CoexV2VersionAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a CoexV2VersionAction.
             * @implements ICoexV2VersionAction
             * @constructor
             * @param {proto.SyncActionValue.ICoexV2VersionAction=} [properties] Properties to set
             */
            function CoexV2VersionAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * CoexV2VersionAction version.
             * @member {number|Long|null|undefined} version
             * @memberof proto.SyncActionValue.CoexV2VersionAction
             * @instance
             */
            CoexV2VersionAction.prototype.version = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CoexV2VersionAction.prototype, "_version", {
                get: $util.oneOfGetter($oneOfFields = ["version"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new CoexV2VersionAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.CoexV2VersionAction
             * @static
             * @param {proto.SyncActionValue.ICoexV2VersionAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.CoexV2VersionAction} CoexV2VersionAction instance
             */
            CoexV2VersionAction.create = function create(properties) {
                return new CoexV2VersionAction(properties);
            };

            /**
             * Encodes the specified CoexV2VersionAction message. Does not implicitly {@link proto.SyncActionValue.CoexV2VersionAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.CoexV2VersionAction
             * @static
             * @param {proto.SyncActionValue.ICoexV2VersionAction} message CoexV2VersionAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CoexV2VersionAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.version);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CoexV2VersionAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.CoexV2VersionAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.CoexV2VersionAction
             * @static
             * @param {proto.SyncActionValue.ICoexV2VersionAction} message CoexV2VersionAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CoexV2VersionAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a CoexV2VersionAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.CoexV2VersionAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.CoexV2VersionAction} CoexV2VersionAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CoexV2VersionAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.version = reader.uint64();
                            message._version = "version";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a CoexV2VersionAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.CoexV2VersionAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.CoexV2VersionAction} CoexV2VersionAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CoexV2VersionAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CoexV2VersionAction message.
             * @function verify
             * @memberof proto.SyncActionValue.CoexV2VersionAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CoexV2VersionAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.version != null && Object.hasOwnProperty.call(message, "version")) {
                    properties._version = 1;
                    if (!$util.isInteger(message.version) && !(message.version && $util.isInteger(message.version.low) && $util.isInteger(message.version.high)))
                        return "version: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a CoexV2VersionAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.CoexV2VersionAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.CoexV2VersionAction} CoexV2VersionAction
             */
            CoexV2VersionAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.CoexV2VersionAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.version != null)
                    if ($util.Long)
                        message.version = $util.Long.fromValue(object.version, true);
                    else if (typeof object.version === "string")
                        message.version = parseInt(object.version, 10);
                    else if (typeof object.version === "number")
                        message.version = object.version;
                    else if (typeof object.version === "object")
                        message.version = new $util.LongBits(object.version.low >>> 0, object.version.high >>> 0).toNumber(true);
                return message;
            };

            /**
             * Creates a plain object from a CoexV2VersionAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.CoexV2VersionAction
             * @static
             * @param {proto.SyncActionValue.CoexV2VersionAction} message CoexV2VersionAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CoexV2VersionAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.version = typeof message.version === "number" ? BigInt(message.version) : $util.Long.fromBits(message.version.low >>> 0, message.version.high >>> 0, true).toBigInt();
                    else if (typeof message.version === "number")
                        object.version = options.longs === String ? String(message.version) : message.version;
                    else
                        object.version = options.longs === String ? $util.Long.prototype.toString.call(message.version) : options.longs === Number ? new $util.LongBits(message.version.low >>> 0, message.version.high >>> 0).toNumber(true) : message.version;
                return object;
            };

            /**
             * Converts this CoexV2VersionAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.CoexV2VersionAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CoexV2VersionAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for CoexV2VersionAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.CoexV2VersionAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            CoexV2VersionAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.CoexV2VersionAction";
            };

            return CoexV2VersionAction;
        })();

        SyncActionValue.ContactAction = (function() {

            /**
             * Properties of a ContactAction.
             * @memberof proto.SyncActionValue
             * @interface IContactAction
             * @property {string|null} [fullName] ContactAction fullName
             * @property {string|null} [firstName] ContactAction firstName
             * @property {string|null} [lidJid] ContactAction lidJid
             * @property {boolean|null} [saveOnPrimaryAddressbook] ContactAction saveOnPrimaryAddressbook
             * @property {string|null} [pnJid] ContactAction pnJid
             * @property {string|null} [username] ContactAction username
             */

            /**
             * Constructs a new ContactAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a ContactAction.
             * @implements IContactAction
             * @constructor
             * @param {proto.SyncActionValue.IContactAction=} [properties] Properties to set
             */
            function ContactAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ContactAction fullName.
             * @member {string|null|undefined} fullName
             * @memberof proto.SyncActionValue.ContactAction
             * @instance
             */
            ContactAction.prototype.fullName = null;

            /**
             * ContactAction firstName.
             * @member {string|null|undefined} firstName
             * @memberof proto.SyncActionValue.ContactAction
             * @instance
             */
            ContactAction.prototype.firstName = null;

            /**
             * ContactAction lidJid.
             * @member {string|null|undefined} lidJid
             * @memberof proto.SyncActionValue.ContactAction
             * @instance
             */
            ContactAction.prototype.lidJid = null;

            /**
             * ContactAction saveOnPrimaryAddressbook.
             * @member {boolean|null|undefined} saveOnPrimaryAddressbook
             * @memberof proto.SyncActionValue.ContactAction
             * @instance
             */
            ContactAction.prototype.saveOnPrimaryAddressbook = null;

            /**
             * ContactAction pnJid.
             * @member {string|null|undefined} pnJid
             * @memberof proto.SyncActionValue.ContactAction
             * @instance
             */
            ContactAction.prototype.pnJid = null;

            /**
             * ContactAction username.
             * @member {string|null|undefined} username
             * @memberof proto.SyncActionValue.ContactAction
             * @instance
             */
            ContactAction.prototype.username = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ContactAction.prototype, "_fullName", {
                get: $util.oneOfGetter($oneOfFields = ["fullName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ContactAction.prototype, "_firstName", {
                get: $util.oneOfGetter($oneOfFields = ["firstName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ContactAction.prototype, "_lidJid", {
                get: $util.oneOfGetter($oneOfFields = ["lidJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ContactAction.prototype, "_saveOnPrimaryAddressbook", {
                get: $util.oneOfGetter($oneOfFields = ["saveOnPrimaryAddressbook"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ContactAction.prototype, "_pnJid", {
                get: $util.oneOfGetter($oneOfFields = ["pnJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ContactAction.prototype, "_username", {
                get: $util.oneOfGetter($oneOfFields = ["username"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ContactAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.ContactAction
             * @static
             * @param {proto.SyncActionValue.IContactAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.ContactAction} ContactAction instance
             */
            ContactAction.create = function create(properties) {
                return new ContactAction(properties);
            };

            /**
             * Encodes the specified ContactAction message. Does not implicitly {@link proto.SyncActionValue.ContactAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.ContactAction
             * @static
             * @param {proto.SyncActionValue.IContactAction} message ContactAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ContactAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.fullName != null && Object.hasOwnProperty.call(message, "fullName"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.fullName);
                if (message.firstName != null && Object.hasOwnProperty.call(message, "firstName"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.firstName);
                if (message.lidJid != null && Object.hasOwnProperty.call(message, "lidJid"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.lidJid);
                if (message.saveOnPrimaryAddressbook != null && Object.hasOwnProperty.call(message, "saveOnPrimaryAddressbook"))
                    writer.uint32(/* id 4, wireType 0 =*/32).bool(message.saveOnPrimaryAddressbook);
                if (message.pnJid != null && Object.hasOwnProperty.call(message, "pnJid"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.pnJid);
                if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.username);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ContactAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.ContactAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.ContactAction
             * @static
             * @param {proto.SyncActionValue.IContactAction} message ContactAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ContactAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ContactAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.ContactAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.ContactAction} ContactAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ContactAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.fullName = reader.stringVerify();
                            message._fullName = "fullName";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.firstName = reader.stringVerify();
                            message._firstName = "firstName";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.lidJid = reader.stringVerify();
                            message._lidJid = "lidJid";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.saveOnPrimaryAddressbook = reader.bool();
                            message._saveOnPrimaryAddressbook = "saveOnPrimaryAddressbook";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.pnJid = reader.stringVerify();
                            message._pnJid = "pnJid";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.username = reader.stringVerify();
                            message._username = "username";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ContactAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.ContactAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.ContactAction} ContactAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ContactAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ContactAction message.
             * @function verify
             * @memberof proto.SyncActionValue.ContactAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ContactAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.fullName != null && Object.hasOwnProperty.call(message, "fullName")) {
                    properties._fullName = 1;
                    if (!$util.isString(message.fullName))
                        return "fullName: string expected";
                }
                if (message.firstName != null && Object.hasOwnProperty.call(message, "firstName")) {
                    properties._firstName = 1;
                    if (!$util.isString(message.firstName))
                        return "firstName: string expected";
                }
                if (message.lidJid != null && Object.hasOwnProperty.call(message, "lidJid")) {
                    properties._lidJid = 1;
                    if (!$util.isString(message.lidJid))
                        return "lidJid: string expected";
                }
                if (message.saveOnPrimaryAddressbook != null && Object.hasOwnProperty.call(message, "saveOnPrimaryAddressbook")) {
                    properties._saveOnPrimaryAddressbook = 1;
                    if (typeof message.saveOnPrimaryAddressbook !== "boolean")
                        return "saveOnPrimaryAddressbook: boolean expected";
                }
                if (message.pnJid != null && Object.hasOwnProperty.call(message, "pnJid")) {
                    properties._pnJid = 1;
                    if (!$util.isString(message.pnJid))
                        return "pnJid: string expected";
                }
                if (message.username != null && Object.hasOwnProperty.call(message, "username")) {
                    properties._username = 1;
                    if (!$util.isString(message.username))
                        return "username: string expected";
                }
                return null;
            };

            /**
             * Creates a ContactAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.ContactAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.ContactAction} ContactAction
             */
            ContactAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.ContactAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.fullName != null)
                    message.fullName = String(object.fullName);
                if (object.firstName != null)
                    message.firstName = String(object.firstName);
                if (object.lidJid != null)
                    message.lidJid = String(object.lidJid);
                if (object.saveOnPrimaryAddressbook != null)
                    message.saveOnPrimaryAddressbook = Boolean(object.saveOnPrimaryAddressbook);
                if (object.pnJid != null)
                    message.pnJid = String(object.pnJid);
                if (object.username != null)
                    message.username = String(object.username);
                return message;
            };

            /**
             * Creates a plain object from a ContactAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.ContactAction
             * @static
             * @param {proto.SyncActionValue.ContactAction} message ContactAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ContactAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.fullName != null && Object.hasOwnProperty.call(message, "fullName"))
                    object.fullName = message.fullName;
                if (message.firstName != null && Object.hasOwnProperty.call(message, "firstName"))
                    object.firstName = message.firstName;
                if (message.lidJid != null && Object.hasOwnProperty.call(message, "lidJid"))
                    object.lidJid = message.lidJid;
                if (message.saveOnPrimaryAddressbook != null && Object.hasOwnProperty.call(message, "saveOnPrimaryAddressbook"))
                    object.saveOnPrimaryAddressbook = message.saveOnPrimaryAddressbook;
                if (message.pnJid != null && Object.hasOwnProperty.call(message, "pnJid"))
                    object.pnJid = message.pnJid;
                if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                    object.username = message.username;
                return object;
            };

            /**
             * Converts this ContactAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.ContactAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ContactAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ContactAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.ContactAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ContactAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.ContactAction";
            };

            return ContactAction;
        })();

        SyncActionValue.CtwaPerCustomerDataSharingAction = (function() {

            /**
             * Properties of a CtwaPerCustomerDataSharingAction.
             * @memberof proto.SyncActionValue
             * @interface ICtwaPerCustomerDataSharingAction
             * @property {boolean|null} [isCtwaPerCustomerDataSharingEnabled] CtwaPerCustomerDataSharingAction isCtwaPerCustomerDataSharingEnabled
             */

            /**
             * Constructs a new CtwaPerCustomerDataSharingAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a CtwaPerCustomerDataSharingAction.
             * @implements ICtwaPerCustomerDataSharingAction
             * @constructor
             * @param {proto.SyncActionValue.ICtwaPerCustomerDataSharingAction=} [properties] Properties to set
             */
            function CtwaPerCustomerDataSharingAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * CtwaPerCustomerDataSharingAction isCtwaPerCustomerDataSharingEnabled.
             * @member {boolean|null|undefined} isCtwaPerCustomerDataSharingEnabled
             * @memberof proto.SyncActionValue.CtwaPerCustomerDataSharingAction
             * @instance
             */
            CtwaPerCustomerDataSharingAction.prototype.isCtwaPerCustomerDataSharingEnabled = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CtwaPerCustomerDataSharingAction.prototype, "_isCtwaPerCustomerDataSharingEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isCtwaPerCustomerDataSharingEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new CtwaPerCustomerDataSharingAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.CtwaPerCustomerDataSharingAction
             * @static
             * @param {proto.SyncActionValue.ICtwaPerCustomerDataSharingAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.CtwaPerCustomerDataSharingAction} CtwaPerCustomerDataSharingAction instance
             */
            CtwaPerCustomerDataSharingAction.create = function create(properties) {
                return new CtwaPerCustomerDataSharingAction(properties);
            };

            /**
             * Encodes the specified CtwaPerCustomerDataSharingAction message. Does not implicitly {@link proto.SyncActionValue.CtwaPerCustomerDataSharingAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.CtwaPerCustomerDataSharingAction
             * @static
             * @param {proto.SyncActionValue.ICtwaPerCustomerDataSharingAction} message CtwaPerCustomerDataSharingAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CtwaPerCustomerDataSharingAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.isCtwaPerCustomerDataSharingEnabled != null && Object.hasOwnProperty.call(message, "isCtwaPerCustomerDataSharingEnabled"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.isCtwaPerCustomerDataSharingEnabled);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CtwaPerCustomerDataSharingAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.CtwaPerCustomerDataSharingAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.CtwaPerCustomerDataSharingAction
             * @static
             * @param {proto.SyncActionValue.ICtwaPerCustomerDataSharingAction} message CtwaPerCustomerDataSharingAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CtwaPerCustomerDataSharingAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a CtwaPerCustomerDataSharingAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.CtwaPerCustomerDataSharingAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.CtwaPerCustomerDataSharingAction} CtwaPerCustomerDataSharingAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CtwaPerCustomerDataSharingAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.isCtwaPerCustomerDataSharingEnabled = reader.bool();
                            message._isCtwaPerCustomerDataSharingEnabled = "isCtwaPerCustomerDataSharingEnabled";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a CtwaPerCustomerDataSharingAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.CtwaPerCustomerDataSharingAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.CtwaPerCustomerDataSharingAction} CtwaPerCustomerDataSharingAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CtwaPerCustomerDataSharingAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CtwaPerCustomerDataSharingAction message.
             * @function verify
             * @memberof proto.SyncActionValue.CtwaPerCustomerDataSharingAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CtwaPerCustomerDataSharingAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.isCtwaPerCustomerDataSharingEnabled != null && Object.hasOwnProperty.call(message, "isCtwaPerCustomerDataSharingEnabled")) {
                    properties._isCtwaPerCustomerDataSharingEnabled = 1;
                    if (typeof message.isCtwaPerCustomerDataSharingEnabled !== "boolean")
                        return "isCtwaPerCustomerDataSharingEnabled: boolean expected";
                }
                return null;
            };

            /**
             * Creates a CtwaPerCustomerDataSharingAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.CtwaPerCustomerDataSharingAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.CtwaPerCustomerDataSharingAction} CtwaPerCustomerDataSharingAction
             */
            CtwaPerCustomerDataSharingAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.CtwaPerCustomerDataSharingAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.isCtwaPerCustomerDataSharingEnabled != null)
                    message.isCtwaPerCustomerDataSharingEnabled = Boolean(object.isCtwaPerCustomerDataSharingEnabled);
                return message;
            };

            /**
             * Creates a plain object from a CtwaPerCustomerDataSharingAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.CtwaPerCustomerDataSharingAction
             * @static
             * @param {proto.SyncActionValue.CtwaPerCustomerDataSharingAction} message CtwaPerCustomerDataSharingAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CtwaPerCustomerDataSharingAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.isCtwaPerCustomerDataSharingEnabled != null && Object.hasOwnProperty.call(message, "isCtwaPerCustomerDataSharingEnabled"))
                    object.isCtwaPerCustomerDataSharingEnabled = message.isCtwaPerCustomerDataSharingEnabled;
                return object;
            };

            /**
             * Converts this CtwaPerCustomerDataSharingAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.CtwaPerCustomerDataSharingAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CtwaPerCustomerDataSharingAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for CtwaPerCustomerDataSharingAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.CtwaPerCustomerDataSharingAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            CtwaPerCustomerDataSharingAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.CtwaPerCustomerDataSharingAction";
            };

            return CtwaPerCustomerDataSharingAction;
        })();

        SyncActionValue.CustomPaymentMethod = (function() {

            /**
             * Properties of a CustomPaymentMethod.
             * @memberof proto.SyncActionValue
             * @interface ICustomPaymentMethod
             * @property {string|null} [credentialId] CustomPaymentMethod credentialId
             * @property {string|null} [country] CustomPaymentMethod country
             * @property {string|null} [type] CustomPaymentMethod type
             * @property {Array.<proto.SyncActionValue.ICustomPaymentMethodMetadata>|null} [metadata] CustomPaymentMethod metadata
             */

            /**
             * Constructs a new CustomPaymentMethod.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a CustomPaymentMethod.
             * @implements ICustomPaymentMethod
             * @constructor
             * @param {proto.SyncActionValue.ICustomPaymentMethod=} [properties] Properties to set
             */
            function CustomPaymentMethod(properties) {
                this.metadata = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * CustomPaymentMethod credentialId.
             * @member {string|null|undefined} credentialId
             * @memberof proto.SyncActionValue.CustomPaymentMethod
             * @instance
             */
            CustomPaymentMethod.prototype.credentialId = null;

            /**
             * CustomPaymentMethod country.
             * @member {string|null|undefined} country
             * @memberof proto.SyncActionValue.CustomPaymentMethod
             * @instance
             */
            CustomPaymentMethod.prototype.country = null;

            /**
             * CustomPaymentMethod type.
             * @member {string|null|undefined} type
             * @memberof proto.SyncActionValue.CustomPaymentMethod
             * @instance
             */
            CustomPaymentMethod.prototype.type = null;

            /**
             * CustomPaymentMethod metadata.
             * @member {Array.<proto.SyncActionValue.ICustomPaymentMethodMetadata>} metadata
             * @memberof proto.SyncActionValue.CustomPaymentMethod
             * @instance
             */
            CustomPaymentMethod.prototype.metadata = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CustomPaymentMethod.prototype, "_credentialId", {
                get: $util.oneOfGetter($oneOfFields = ["credentialId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CustomPaymentMethod.prototype, "_country", {
                get: $util.oneOfGetter($oneOfFields = ["country"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CustomPaymentMethod.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new CustomPaymentMethod instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.CustomPaymentMethod
             * @static
             * @param {proto.SyncActionValue.ICustomPaymentMethod=} [properties] Properties to set
             * @returns {proto.SyncActionValue.CustomPaymentMethod} CustomPaymentMethod instance
             */
            CustomPaymentMethod.create = function create(properties) {
                return new CustomPaymentMethod(properties);
            };

            /**
             * Encodes the specified CustomPaymentMethod message. Does not implicitly {@link proto.SyncActionValue.CustomPaymentMethod.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.CustomPaymentMethod
             * @static
             * @param {proto.SyncActionValue.ICustomPaymentMethod} message CustomPaymentMethod message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CustomPaymentMethod.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.credentialId != null && Object.hasOwnProperty.call(message, "credentialId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.credentialId);
                if (message.country != null && Object.hasOwnProperty.call(message, "country"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.country);
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.type);
                if (message.metadata != null && message.metadata.length)
                    for (var i = 0; i < message.metadata.length; ++i)
                        $root.proto.SyncActionValue.CustomPaymentMethodMetadata.encode(message.metadata[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CustomPaymentMethod message, length delimited. Does not implicitly {@link proto.SyncActionValue.CustomPaymentMethod.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.CustomPaymentMethod
             * @static
             * @param {proto.SyncActionValue.ICustomPaymentMethod} message CustomPaymentMethod message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CustomPaymentMethod.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a CustomPaymentMethod message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.CustomPaymentMethod
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.CustomPaymentMethod} CustomPaymentMethod
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CustomPaymentMethod.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.credentialId = reader.stringVerify();
                            message._credentialId = "credentialId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.country = reader.stringVerify();
                            message._country = "country";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.type = reader.stringVerify();
                            message._type = "type";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            if (!(message.metadata && message.metadata.length))
                                message.metadata = [];
                            message.metadata.push($root.proto.SyncActionValue.CustomPaymentMethodMetadata.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a CustomPaymentMethod message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.CustomPaymentMethod
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.CustomPaymentMethod} CustomPaymentMethod
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CustomPaymentMethod.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CustomPaymentMethod message.
             * @function verify
             * @memberof proto.SyncActionValue.CustomPaymentMethod
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CustomPaymentMethod.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.credentialId != null && Object.hasOwnProperty.call(message, "credentialId")) {
                    properties._credentialId = 1;
                    if (!$util.isString(message.credentialId))
                        return "credentialId: string expected";
                }
                if (message.country != null && Object.hasOwnProperty.call(message, "country")) {
                    properties._country = 1;
                    if (!$util.isString(message.country))
                        return "country: string expected";
                }
                if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                    properties._type = 1;
                    if (!$util.isString(message.type))
                        return "type: string expected";
                }
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata")) {
                    if (!Array.isArray(message.metadata))
                        return "metadata: array expected";
                    for (var i = 0; i < message.metadata.length; ++i) {
                        var error = $root.proto.SyncActionValue.CustomPaymentMethodMetadata.verify(message.metadata[i], q + 1);
                        if (error)
                            return "metadata." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a CustomPaymentMethod message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.CustomPaymentMethod
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.CustomPaymentMethod} CustomPaymentMethod
             */
            CustomPaymentMethod.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.CustomPaymentMethod: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.credentialId != null)
                    message.credentialId = String(object.credentialId);
                if (object.country != null)
                    message.country = String(object.country);
                if (object.type != null)
                    message.type = String(object.type);
                if (object.metadata) {
                    if (!Array.isArray(object.metadata))
                        throw TypeError(".proto.SyncActionValue.CustomPaymentMethod.metadata: array expected");
                    message.metadata = Array(object.metadata.length);
                    for (var i = 0; i < object.metadata.length; ++i) {
                        if (!$util.isObject(object.metadata[i]))
                            throw TypeError(".proto.SyncActionValue.CustomPaymentMethod.metadata: object expected");
                        message.metadata[i] = $root.proto.SyncActionValue.CustomPaymentMethodMetadata.fromObject(object.metadata[i], q + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a CustomPaymentMethod message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.CustomPaymentMethod
             * @static
             * @param {proto.SyncActionValue.CustomPaymentMethod} message CustomPaymentMethod
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CustomPaymentMethod.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.metadata = [];
                if (message.credentialId != null && Object.hasOwnProperty.call(message, "credentialId"))
                    object.credentialId = message.credentialId;
                if (message.country != null && Object.hasOwnProperty.call(message, "country"))
                    object.country = message.country;
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    object.type = message.type;
                if (message.metadata && message.metadata.length) {
                    object.metadata = Array(message.metadata.length);
                    for (var j = 0; j < message.metadata.length; ++j)
                        object.metadata[j] = $root.proto.SyncActionValue.CustomPaymentMethodMetadata.toObject(message.metadata[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this CustomPaymentMethod to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.CustomPaymentMethod
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CustomPaymentMethod.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for CustomPaymentMethod
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.CustomPaymentMethod
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            CustomPaymentMethod.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.CustomPaymentMethod";
            };

            return CustomPaymentMethod;
        })();

        SyncActionValue.CustomPaymentMethodMetadata = (function() {

            /**
             * Properties of a CustomPaymentMethodMetadata.
             * @memberof proto.SyncActionValue
             * @interface ICustomPaymentMethodMetadata
             * @property {string|null} [key] CustomPaymentMethodMetadata key
             * @property {string|null} [value] CustomPaymentMethodMetadata value
             */

            /**
             * Constructs a new CustomPaymentMethodMetadata.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a CustomPaymentMethodMetadata.
             * @implements ICustomPaymentMethodMetadata
             * @constructor
             * @param {proto.SyncActionValue.ICustomPaymentMethodMetadata=} [properties] Properties to set
             */
            function CustomPaymentMethodMetadata(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * CustomPaymentMethodMetadata key.
             * @member {string|null|undefined} key
             * @memberof proto.SyncActionValue.CustomPaymentMethodMetadata
             * @instance
             */
            CustomPaymentMethodMetadata.prototype.key = null;

            /**
             * CustomPaymentMethodMetadata value.
             * @member {string|null|undefined} value
             * @memberof proto.SyncActionValue.CustomPaymentMethodMetadata
             * @instance
             */
            CustomPaymentMethodMetadata.prototype.value = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CustomPaymentMethodMetadata.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CustomPaymentMethodMetadata.prototype, "_value", {
                get: $util.oneOfGetter($oneOfFields = ["value"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new CustomPaymentMethodMetadata instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.CustomPaymentMethodMetadata
             * @static
             * @param {proto.SyncActionValue.ICustomPaymentMethodMetadata=} [properties] Properties to set
             * @returns {proto.SyncActionValue.CustomPaymentMethodMetadata} CustomPaymentMethodMetadata instance
             */
            CustomPaymentMethodMetadata.create = function create(properties) {
                return new CustomPaymentMethodMetadata(properties);
            };

            /**
             * Encodes the specified CustomPaymentMethodMetadata message. Does not implicitly {@link proto.SyncActionValue.CustomPaymentMethodMetadata.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.CustomPaymentMethodMetadata
             * @static
             * @param {proto.SyncActionValue.ICustomPaymentMethodMetadata} message CustomPaymentMethodMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CustomPaymentMethodMetadata.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
                if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.value);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CustomPaymentMethodMetadata message, length delimited. Does not implicitly {@link proto.SyncActionValue.CustomPaymentMethodMetadata.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.CustomPaymentMethodMetadata
             * @static
             * @param {proto.SyncActionValue.ICustomPaymentMethodMetadata} message CustomPaymentMethodMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CustomPaymentMethodMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a CustomPaymentMethodMetadata message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.CustomPaymentMethodMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.CustomPaymentMethodMetadata} CustomPaymentMethodMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CustomPaymentMethodMetadata.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.key = reader.stringVerify();
                            message._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.value = reader.stringVerify();
                            message._value = "value";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a CustomPaymentMethodMetadata message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.CustomPaymentMethodMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.CustomPaymentMethodMetadata} CustomPaymentMethodMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CustomPaymentMethodMetadata.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CustomPaymentMethodMetadata message.
             * @function verify
             * @memberof proto.SyncActionValue.CustomPaymentMethodMetadata
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CustomPaymentMethodMetadata.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                    properties._key = 1;
                    if (!$util.isString(message.key))
                        return "key: string expected";
                }
                if (message.value != null && Object.hasOwnProperty.call(message, "value")) {
                    properties._value = 1;
                    if (!$util.isString(message.value))
                        return "value: string expected";
                }
                return null;
            };

            /**
             * Creates a CustomPaymentMethodMetadata message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.CustomPaymentMethodMetadata
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.CustomPaymentMethodMetadata} CustomPaymentMethodMetadata
             */
            CustomPaymentMethodMetadata.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.CustomPaymentMethodMetadata: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.key != null)
                    message.key = String(object.key);
                if (object.value != null)
                    message.value = String(object.value);
                return message;
            };

            /**
             * Creates a plain object from a CustomPaymentMethodMetadata message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.CustomPaymentMethodMetadata
             * @static
             * @param {proto.SyncActionValue.CustomPaymentMethodMetadata} message CustomPaymentMethodMetadata
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CustomPaymentMethodMetadata.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    object.key = message.key;
                if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                    object.value = message.value;
                return object;
            };

            /**
             * Converts this CustomPaymentMethodMetadata to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.CustomPaymentMethodMetadata
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CustomPaymentMethodMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for CustomPaymentMethodMetadata
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.CustomPaymentMethodMetadata
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            CustomPaymentMethodMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.CustomPaymentMethodMetadata";
            };

            return CustomPaymentMethodMetadata;
        })();

        SyncActionValue.CustomPaymentMethodsAction = (function() {

            /**
             * Properties of a CustomPaymentMethodsAction.
             * @memberof proto.SyncActionValue
             * @interface ICustomPaymentMethodsAction
             * @property {Array.<proto.SyncActionValue.ICustomPaymentMethod>|null} [customPaymentMethods] CustomPaymentMethodsAction customPaymentMethods
             */

            /**
             * Constructs a new CustomPaymentMethodsAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a CustomPaymentMethodsAction.
             * @implements ICustomPaymentMethodsAction
             * @constructor
             * @param {proto.SyncActionValue.ICustomPaymentMethodsAction=} [properties] Properties to set
             */
            function CustomPaymentMethodsAction(properties) {
                this.customPaymentMethods = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * CustomPaymentMethodsAction customPaymentMethods.
             * @member {Array.<proto.SyncActionValue.ICustomPaymentMethod>} customPaymentMethods
             * @memberof proto.SyncActionValue.CustomPaymentMethodsAction
             * @instance
             */
            CustomPaymentMethodsAction.prototype.customPaymentMethods = $util.emptyArray;

            /**
             * Creates a new CustomPaymentMethodsAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.CustomPaymentMethodsAction
             * @static
             * @param {proto.SyncActionValue.ICustomPaymentMethodsAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.CustomPaymentMethodsAction} CustomPaymentMethodsAction instance
             */
            CustomPaymentMethodsAction.create = function create(properties) {
                return new CustomPaymentMethodsAction(properties);
            };

            /**
             * Encodes the specified CustomPaymentMethodsAction message. Does not implicitly {@link proto.SyncActionValue.CustomPaymentMethodsAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.CustomPaymentMethodsAction
             * @static
             * @param {proto.SyncActionValue.ICustomPaymentMethodsAction} message CustomPaymentMethodsAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CustomPaymentMethodsAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.customPaymentMethods != null && message.customPaymentMethods.length)
                    for (var i = 0; i < message.customPaymentMethods.length; ++i)
                        $root.proto.SyncActionValue.CustomPaymentMethod.encode(message.customPaymentMethods[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CustomPaymentMethodsAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.CustomPaymentMethodsAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.CustomPaymentMethodsAction
             * @static
             * @param {proto.SyncActionValue.ICustomPaymentMethodsAction} message CustomPaymentMethodsAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CustomPaymentMethodsAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a CustomPaymentMethodsAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.CustomPaymentMethodsAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.CustomPaymentMethodsAction} CustomPaymentMethodsAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CustomPaymentMethodsAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(message.customPaymentMethods && message.customPaymentMethods.length))
                                message.customPaymentMethods = [];
                            message.customPaymentMethods.push($root.proto.SyncActionValue.CustomPaymentMethod.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a CustomPaymentMethodsAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.CustomPaymentMethodsAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.CustomPaymentMethodsAction} CustomPaymentMethodsAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CustomPaymentMethodsAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CustomPaymentMethodsAction message.
             * @function verify
             * @memberof proto.SyncActionValue.CustomPaymentMethodsAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CustomPaymentMethodsAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.customPaymentMethods != null && Object.hasOwnProperty.call(message, "customPaymentMethods")) {
                    if (!Array.isArray(message.customPaymentMethods))
                        return "customPaymentMethods: array expected";
                    for (var i = 0; i < message.customPaymentMethods.length; ++i) {
                        var error = $root.proto.SyncActionValue.CustomPaymentMethod.verify(message.customPaymentMethods[i], q + 1);
                        if (error)
                            return "customPaymentMethods." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a CustomPaymentMethodsAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.CustomPaymentMethodsAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.CustomPaymentMethodsAction} CustomPaymentMethodsAction
             */
            CustomPaymentMethodsAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.CustomPaymentMethodsAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.customPaymentMethods) {
                    if (!Array.isArray(object.customPaymentMethods))
                        throw TypeError(".proto.SyncActionValue.CustomPaymentMethodsAction.customPaymentMethods: array expected");
                    message.customPaymentMethods = Array(object.customPaymentMethods.length);
                    for (var i = 0; i < object.customPaymentMethods.length; ++i) {
                        if (!$util.isObject(object.customPaymentMethods[i]))
                            throw TypeError(".proto.SyncActionValue.CustomPaymentMethodsAction.customPaymentMethods: object expected");
                        message.customPaymentMethods[i] = $root.proto.SyncActionValue.CustomPaymentMethod.fromObject(object.customPaymentMethods[i], q + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a CustomPaymentMethodsAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.CustomPaymentMethodsAction
             * @static
             * @param {proto.SyncActionValue.CustomPaymentMethodsAction} message CustomPaymentMethodsAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CustomPaymentMethodsAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.customPaymentMethods = [];
                if (message.customPaymentMethods && message.customPaymentMethods.length) {
                    object.customPaymentMethods = Array(message.customPaymentMethods.length);
                    for (var j = 0; j < message.customPaymentMethods.length; ++j)
                        object.customPaymentMethods[j] = $root.proto.SyncActionValue.CustomPaymentMethod.toObject(message.customPaymentMethods[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this CustomPaymentMethodsAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.CustomPaymentMethodsAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CustomPaymentMethodsAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for CustomPaymentMethodsAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.CustomPaymentMethodsAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            CustomPaymentMethodsAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.CustomPaymentMethodsAction";
            };

            return CustomPaymentMethodsAction;
        })();

        SyncActionValue.CustomerDataAction = (function() {

            /**
             * Properties of a CustomerDataAction.
             * @memberof proto.SyncActionValue
             * @interface ICustomerDataAction
             * @property {string|null} [chatJid] CustomerDataAction chatJid
             * @property {number|null} [contactType] CustomerDataAction contactType
             * @property {string|null} [email] CustomerDataAction email
             * @property {string|null} [altPhoneNumbers] CustomerDataAction altPhoneNumbers
             * @property {number|Long|null} [birthday] CustomerDataAction birthday
             * @property {string|null} [address] CustomerDataAction address
             * @property {number|null} [acquisitionSource] CustomerDataAction acquisitionSource
             * @property {number|null} [leadStage] CustomerDataAction leadStage
             * @property {number|Long|null} [lastOrder] CustomerDataAction lastOrder
             * @property {number|Long|null} [createdAt] CustomerDataAction createdAt
             * @property {number|Long|null} [modifiedAt] CustomerDataAction modifiedAt
             */

            /**
             * Constructs a new CustomerDataAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a CustomerDataAction.
             * @implements ICustomerDataAction
             * @constructor
             * @param {proto.SyncActionValue.ICustomerDataAction=} [properties] Properties to set
             */
            function CustomerDataAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * CustomerDataAction chatJid.
             * @member {string|null|undefined} chatJid
             * @memberof proto.SyncActionValue.CustomerDataAction
             * @instance
             */
            CustomerDataAction.prototype.chatJid = null;

            /**
             * CustomerDataAction contactType.
             * @member {number|null|undefined} contactType
             * @memberof proto.SyncActionValue.CustomerDataAction
             * @instance
             */
            CustomerDataAction.prototype.contactType = null;

            /**
             * CustomerDataAction email.
             * @member {string|null|undefined} email
             * @memberof proto.SyncActionValue.CustomerDataAction
             * @instance
             */
            CustomerDataAction.prototype.email = null;

            /**
             * CustomerDataAction altPhoneNumbers.
             * @member {string|null|undefined} altPhoneNumbers
             * @memberof proto.SyncActionValue.CustomerDataAction
             * @instance
             */
            CustomerDataAction.prototype.altPhoneNumbers = null;

            /**
             * CustomerDataAction birthday.
             * @member {number|Long|null|undefined} birthday
             * @memberof proto.SyncActionValue.CustomerDataAction
             * @instance
             */
            CustomerDataAction.prototype.birthday = null;

            /**
             * CustomerDataAction address.
             * @member {string|null|undefined} address
             * @memberof proto.SyncActionValue.CustomerDataAction
             * @instance
             */
            CustomerDataAction.prototype.address = null;

            /**
             * CustomerDataAction acquisitionSource.
             * @member {number|null|undefined} acquisitionSource
             * @memberof proto.SyncActionValue.CustomerDataAction
             * @instance
             */
            CustomerDataAction.prototype.acquisitionSource = null;

            /**
             * CustomerDataAction leadStage.
             * @member {number|null|undefined} leadStage
             * @memberof proto.SyncActionValue.CustomerDataAction
             * @instance
             */
            CustomerDataAction.prototype.leadStage = null;

            /**
             * CustomerDataAction lastOrder.
             * @member {number|Long|null|undefined} lastOrder
             * @memberof proto.SyncActionValue.CustomerDataAction
             * @instance
             */
            CustomerDataAction.prototype.lastOrder = null;

            /**
             * CustomerDataAction createdAt.
             * @member {number|Long|null|undefined} createdAt
             * @memberof proto.SyncActionValue.CustomerDataAction
             * @instance
             */
            CustomerDataAction.prototype.createdAt = null;

            /**
             * CustomerDataAction modifiedAt.
             * @member {number|Long|null|undefined} modifiedAt
             * @memberof proto.SyncActionValue.CustomerDataAction
             * @instance
             */
            CustomerDataAction.prototype.modifiedAt = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CustomerDataAction.prototype, "_chatJid", {
                get: $util.oneOfGetter($oneOfFields = ["chatJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CustomerDataAction.prototype, "_contactType", {
                get: $util.oneOfGetter($oneOfFields = ["contactType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CustomerDataAction.prototype, "_email", {
                get: $util.oneOfGetter($oneOfFields = ["email"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CustomerDataAction.prototype, "_altPhoneNumbers", {
                get: $util.oneOfGetter($oneOfFields = ["altPhoneNumbers"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CustomerDataAction.prototype, "_birthday", {
                get: $util.oneOfGetter($oneOfFields = ["birthday"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CustomerDataAction.prototype, "_address", {
                get: $util.oneOfGetter($oneOfFields = ["address"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CustomerDataAction.prototype, "_acquisitionSource", {
                get: $util.oneOfGetter($oneOfFields = ["acquisitionSource"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CustomerDataAction.prototype, "_leadStage", {
                get: $util.oneOfGetter($oneOfFields = ["leadStage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CustomerDataAction.prototype, "_lastOrder", {
                get: $util.oneOfGetter($oneOfFields = ["lastOrder"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CustomerDataAction.prototype, "_createdAt", {
                get: $util.oneOfGetter($oneOfFields = ["createdAt"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CustomerDataAction.prototype, "_modifiedAt", {
                get: $util.oneOfGetter($oneOfFields = ["modifiedAt"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new CustomerDataAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.CustomerDataAction
             * @static
             * @param {proto.SyncActionValue.ICustomerDataAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.CustomerDataAction} CustomerDataAction instance
             */
            CustomerDataAction.create = function create(properties) {
                return new CustomerDataAction(properties);
            };

            /**
             * Encodes the specified CustomerDataAction message. Does not implicitly {@link proto.SyncActionValue.CustomerDataAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.CustomerDataAction
             * @static
             * @param {proto.SyncActionValue.ICustomerDataAction} message CustomerDataAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CustomerDataAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.chatJid != null && Object.hasOwnProperty.call(message, "chatJid"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.chatJid);
                if (message.contactType != null && Object.hasOwnProperty.call(message, "contactType"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.contactType);
                if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.email);
                if (message.altPhoneNumbers != null && Object.hasOwnProperty.call(message, "altPhoneNumbers"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.altPhoneNumbers);
                if (message.birthday != null && Object.hasOwnProperty.call(message, "birthday"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.birthday);
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.address);
                if (message.acquisitionSource != null && Object.hasOwnProperty.call(message, "acquisitionSource"))
                    writer.uint32(/* id 7, wireType 0 =*/56).int32(message.acquisitionSource);
                if (message.leadStage != null && Object.hasOwnProperty.call(message, "leadStage"))
                    writer.uint32(/* id 8, wireType 0 =*/64).int32(message.leadStage);
                if (message.lastOrder != null && Object.hasOwnProperty.call(message, "lastOrder"))
                    writer.uint32(/* id 9, wireType 0 =*/72).int64(message.lastOrder);
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    writer.uint32(/* id 10, wireType 0 =*/80).int64(message.createdAt);
                if (message.modifiedAt != null && Object.hasOwnProperty.call(message, "modifiedAt"))
                    writer.uint32(/* id 11, wireType 0 =*/88).int64(message.modifiedAt);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CustomerDataAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.CustomerDataAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.CustomerDataAction
             * @static
             * @param {proto.SyncActionValue.ICustomerDataAction} message CustomerDataAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CustomerDataAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a CustomerDataAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.CustomerDataAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.CustomerDataAction} CustomerDataAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CustomerDataAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.chatJid = reader.stringVerify();
                            message._chatJid = "chatJid";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.contactType = reader.int32();
                            message._contactType = "contactType";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.email = reader.stringVerify();
                            message._email = "email";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.altPhoneNumbers = reader.stringVerify();
                            message._altPhoneNumbers = "altPhoneNumbers";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.birthday = reader.int64();
                            message._birthday = "birthday";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            message.address = reader.stringVerify();
                            message._address = "address";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            message.acquisitionSource = reader.int32();
                            message._acquisitionSource = "acquisitionSource";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            message.leadStage = reader.int32();
                            message._leadStage = "leadStage";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            message.lastOrder = reader.int64();
                            message._lastOrder = "lastOrder";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            message.createdAt = reader.int64();
                            message._createdAt = "createdAt";
                            continue;
                        }
                    case 11: {
                            if (u !== 0)
                                break;
                            message.modifiedAt = reader.int64();
                            message._modifiedAt = "modifiedAt";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a CustomerDataAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.CustomerDataAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.CustomerDataAction} CustomerDataAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CustomerDataAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CustomerDataAction message.
             * @function verify
             * @memberof proto.SyncActionValue.CustomerDataAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CustomerDataAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.chatJid != null && Object.hasOwnProperty.call(message, "chatJid")) {
                    properties._chatJid = 1;
                    if (!$util.isString(message.chatJid))
                        return "chatJid: string expected";
                }
                if (message.contactType != null && Object.hasOwnProperty.call(message, "contactType")) {
                    properties._contactType = 1;
                    if (!$util.isInteger(message.contactType))
                        return "contactType: integer expected";
                }
                if (message.email != null && Object.hasOwnProperty.call(message, "email")) {
                    properties._email = 1;
                    if (!$util.isString(message.email))
                        return "email: string expected";
                }
                if (message.altPhoneNumbers != null && Object.hasOwnProperty.call(message, "altPhoneNumbers")) {
                    properties._altPhoneNumbers = 1;
                    if (!$util.isString(message.altPhoneNumbers))
                        return "altPhoneNumbers: string expected";
                }
                if (message.birthday != null && Object.hasOwnProperty.call(message, "birthday")) {
                    properties._birthday = 1;
                    if (!$util.isInteger(message.birthday) && !(message.birthday && $util.isInteger(message.birthday.low) && $util.isInteger(message.birthday.high)))
                        return "birthday: integer|Long expected";
                }
                if (message.address != null && Object.hasOwnProperty.call(message, "address")) {
                    properties._address = 1;
                    if (!$util.isString(message.address))
                        return "address: string expected";
                }
                if (message.acquisitionSource != null && Object.hasOwnProperty.call(message, "acquisitionSource")) {
                    properties._acquisitionSource = 1;
                    if (!$util.isInteger(message.acquisitionSource))
                        return "acquisitionSource: integer expected";
                }
                if (message.leadStage != null && Object.hasOwnProperty.call(message, "leadStage")) {
                    properties._leadStage = 1;
                    if (!$util.isInteger(message.leadStage))
                        return "leadStage: integer expected";
                }
                if (message.lastOrder != null && Object.hasOwnProperty.call(message, "lastOrder")) {
                    properties._lastOrder = 1;
                    if (!$util.isInteger(message.lastOrder) && !(message.lastOrder && $util.isInteger(message.lastOrder.low) && $util.isInteger(message.lastOrder.high)))
                        return "lastOrder: integer|Long expected";
                }
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt")) {
                    properties._createdAt = 1;
                    if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
                        return "createdAt: integer|Long expected";
                }
                if (message.modifiedAt != null && Object.hasOwnProperty.call(message, "modifiedAt")) {
                    properties._modifiedAt = 1;
                    if (!$util.isInteger(message.modifiedAt) && !(message.modifiedAt && $util.isInteger(message.modifiedAt.low) && $util.isInteger(message.modifiedAt.high)))
                        return "modifiedAt: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a CustomerDataAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.CustomerDataAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.CustomerDataAction} CustomerDataAction
             */
            CustomerDataAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.CustomerDataAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.chatJid != null)
                    message.chatJid = String(object.chatJid);
                if (object.contactType != null)
                    message.contactType = object.contactType | 0;
                if (object.email != null)
                    message.email = String(object.email);
                if (object.altPhoneNumbers != null)
                    message.altPhoneNumbers = String(object.altPhoneNumbers);
                if (object.birthday != null)
                    if ($util.Long)
                        message.birthday = $util.Long.fromValue(object.birthday, false);
                    else if (typeof object.birthday === "string")
                        message.birthday = parseInt(object.birthday, 10);
                    else if (typeof object.birthday === "number")
                        message.birthday = object.birthday;
                    else if (typeof object.birthday === "object")
                        message.birthday = new $util.LongBits(object.birthday.low >>> 0, object.birthday.high >>> 0).toNumber();
                if (object.address != null)
                    message.address = String(object.address);
                if (object.acquisitionSource != null)
                    message.acquisitionSource = object.acquisitionSource | 0;
                if (object.leadStage != null)
                    message.leadStage = object.leadStage | 0;
                if (object.lastOrder != null)
                    if ($util.Long)
                        message.lastOrder = $util.Long.fromValue(object.lastOrder, false);
                    else if (typeof object.lastOrder === "string")
                        message.lastOrder = parseInt(object.lastOrder, 10);
                    else if (typeof object.lastOrder === "number")
                        message.lastOrder = object.lastOrder;
                    else if (typeof object.lastOrder === "object")
                        message.lastOrder = new $util.LongBits(object.lastOrder.low >>> 0, object.lastOrder.high >>> 0).toNumber();
                if (object.createdAt != null)
                    if ($util.Long)
                        message.createdAt = $util.Long.fromValue(object.createdAt, false);
                    else if (typeof object.createdAt === "string")
                        message.createdAt = parseInt(object.createdAt, 10);
                    else if (typeof object.createdAt === "number")
                        message.createdAt = object.createdAt;
                    else if (typeof object.createdAt === "object")
                        message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
                if (object.modifiedAt != null)
                    if ($util.Long)
                        message.modifiedAt = $util.Long.fromValue(object.modifiedAt, false);
                    else if (typeof object.modifiedAt === "string")
                        message.modifiedAt = parseInt(object.modifiedAt, 10);
                    else if (typeof object.modifiedAt === "number")
                        message.modifiedAt = object.modifiedAt;
                    else if (typeof object.modifiedAt === "object")
                        message.modifiedAt = new $util.LongBits(object.modifiedAt.low >>> 0, object.modifiedAt.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a CustomerDataAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.CustomerDataAction
             * @static
             * @param {proto.SyncActionValue.CustomerDataAction} message CustomerDataAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CustomerDataAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.chatJid != null && Object.hasOwnProperty.call(message, "chatJid"))
                    object.chatJid = message.chatJid;
                if (message.contactType != null && Object.hasOwnProperty.call(message, "contactType"))
                    object.contactType = message.contactType;
                if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                    object.email = message.email;
                if (message.altPhoneNumbers != null && Object.hasOwnProperty.call(message, "altPhoneNumbers"))
                    object.altPhoneNumbers = message.altPhoneNumbers;
                if (message.birthday != null && Object.hasOwnProperty.call(message, "birthday"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.birthday = typeof message.birthday === "number" ? BigInt(message.birthday) : $util.Long.fromBits(message.birthday.low >>> 0, message.birthday.high >>> 0, false).toBigInt();
                    else if (typeof message.birthday === "number")
                        object.birthday = options.longs === String ? String(message.birthday) : message.birthday;
                    else
                        object.birthday = options.longs === String ? $util.Long.prototype.toString.call(message.birthday) : options.longs === Number ? new $util.LongBits(message.birthday.low >>> 0, message.birthday.high >>> 0).toNumber() : message.birthday;
                if (message.address != null && Object.hasOwnProperty.call(message, "address"))
                    object.address = message.address;
                if (message.acquisitionSource != null && Object.hasOwnProperty.call(message, "acquisitionSource"))
                    object.acquisitionSource = message.acquisitionSource;
                if (message.leadStage != null && Object.hasOwnProperty.call(message, "leadStage"))
                    object.leadStage = message.leadStage;
                if (message.lastOrder != null && Object.hasOwnProperty.call(message, "lastOrder"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.lastOrder = typeof message.lastOrder === "number" ? BigInt(message.lastOrder) : $util.Long.fromBits(message.lastOrder.low >>> 0, message.lastOrder.high >>> 0, false).toBigInt();
                    else if (typeof message.lastOrder === "number")
                        object.lastOrder = options.longs === String ? String(message.lastOrder) : message.lastOrder;
                    else
                        object.lastOrder = options.longs === String ? $util.Long.prototype.toString.call(message.lastOrder) : options.longs === Number ? new $util.LongBits(message.lastOrder.low >>> 0, message.lastOrder.high >>> 0).toNumber() : message.lastOrder;
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.createdAt = typeof message.createdAt === "number" ? BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
                    else if (typeof message.createdAt === "number")
                        object.createdAt = options.longs === String ? String(message.createdAt) : message.createdAt;
                    else
                        object.createdAt = options.longs === String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
                if (message.modifiedAt != null && Object.hasOwnProperty.call(message, "modifiedAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.modifiedAt = typeof message.modifiedAt === "number" ? BigInt(message.modifiedAt) : $util.Long.fromBits(message.modifiedAt.low >>> 0, message.modifiedAt.high >>> 0, false).toBigInt();
                    else if (typeof message.modifiedAt === "number")
                        object.modifiedAt = options.longs === String ? String(message.modifiedAt) : message.modifiedAt;
                    else
                        object.modifiedAt = options.longs === String ? $util.Long.prototype.toString.call(message.modifiedAt) : options.longs === Number ? new $util.LongBits(message.modifiedAt.low >>> 0, message.modifiedAt.high >>> 0).toNumber() : message.modifiedAt;
                return object;
            };

            /**
             * Converts this CustomerDataAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.CustomerDataAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CustomerDataAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for CustomerDataAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.CustomerDataAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            CustomerDataAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.CustomerDataAction";
            };

            return CustomerDataAction;
        })();

        SyncActionValue.DeleteChatAction = (function() {

            /**
             * Properties of a DeleteChatAction.
             * @memberof proto.SyncActionValue
             * @interface IDeleteChatAction
             * @property {proto.SyncActionValue.ISyncActionMessageRange|null} [messageRange] DeleteChatAction messageRange
             */

            /**
             * Constructs a new DeleteChatAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a DeleteChatAction.
             * @implements IDeleteChatAction
             * @constructor
             * @param {proto.SyncActionValue.IDeleteChatAction=} [properties] Properties to set
             */
            function DeleteChatAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * DeleteChatAction messageRange.
             * @member {proto.SyncActionValue.ISyncActionMessageRange|null|undefined} messageRange
             * @memberof proto.SyncActionValue.DeleteChatAction
             * @instance
             */
            DeleteChatAction.prototype.messageRange = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DeleteChatAction.prototype, "_messageRange", {
                get: $util.oneOfGetter($oneOfFields = ["messageRange"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new DeleteChatAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.DeleteChatAction
             * @static
             * @param {proto.SyncActionValue.IDeleteChatAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.DeleteChatAction} DeleteChatAction instance
             */
            DeleteChatAction.create = function create(properties) {
                return new DeleteChatAction(properties);
            };

            /**
             * Encodes the specified DeleteChatAction message. Does not implicitly {@link proto.SyncActionValue.DeleteChatAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.DeleteChatAction
             * @static
             * @param {proto.SyncActionValue.IDeleteChatAction} message DeleteChatAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DeleteChatAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.messageRange != null && Object.hasOwnProperty.call(message, "messageRange"))
                    $root.proto.SyncActionValue.SyncActionMessageRange.encode(message.messageRange, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DeleteChatAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.DeleteChatAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.DeleteChatAction
             * @static
             * @param {proto.SyncActionValue.IDeleteChatAction} message DeleteChatAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DeleteChatAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a DeleteChatAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.DeleteChatAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.DeleteChatAction} DeleteChatAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DeleteChatAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.decode(reader, reader.uint32(), undefined, q + 1, message.messageRange);
                            message._messageRange = "messageRange";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a DeleteChatAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.DeleteChatAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.DeleteChatAction} DeleteChatAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DeleteChatAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DeleteChatAction message.
             * @function verify
             * @memberof proto.SyncActionValue.DeleteChatAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DeleteChatAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.messageRange != null && Object.hasOwnProperty.call(message, "messageRange")) {
                    properties._messageRange = 1;
                    {
                        var error = $root.proto.SyncActionValue.SyncActionMessageRange.verify(message.messageRange, q + 1);
                        if (error)
                            return "messageRange." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a DeleteChatAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.DeleteChatAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.DeleteChatAction} DeleteChatAction
             */
            DeleteChatAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.DeleteChatAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.messageRange != null) {
                    if (!$util.isObject(object.messageRange))
                        throw TypeError(".proto.SyncActionValue.DeleteChatAction.messageRange: object expected");
                    message.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.fromObject(object.messageRange, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a DeleteChatAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.DeleteChatAction
             * @static
             * @param {proto.SyncActionValue.DeleteChatAction} message DeleteChatAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DeleteChatAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.messageRange != null && Object.hasOwnProperty.call(message, "messageRange"))
                    object.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.toObject(message.messageRange, options, q + 1);
                return object;
            };

            /**
             * Converts this DeleteChatAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.DeleteChatAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DeleteChatAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for DeleteChatAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.DeleteChatAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            DeleteChatAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.DeleteChatAction";
            };

            return DeleteChatAction;
        })();

        SyncActionValue.DeleteIndividualCallLogAction = (function() {

            /**
             * Properties of a DeleteIndividualCallLogAction.
             * @memberof proto.SyncActionValue
             * @interface IDeleteIndividualCallLogAction
             * @property {string|null} [peerJid] DeleteIndividualCallLogAction peerJid
             * @property {boolean|null} [isIncoming] DeleteIndividualCallLogAction isIncoming
             */

            /**
             * Constructs a new DeleteIndividualCallLogAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a DeleteIndividualCallLogAction.
             * @implements IDeleteIndividualCallLogAction
             * @constructor
             * @param {proto.SyncActionValue.IDeleteIndividualCallLogAction=} [properties] Properties to set
             */
            function DeleteIndividualCallLogAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * DeleteIndividualCallLogAction peerJid.
             * @member {string|null|undefined} peerJid
             * @memberof proto.SyncActionValue.DeleteIndividualCallLogAction
             * @instance
             */
            DeleteIndividualCallLogAction.prototype.peerJid = null;

            /**
             * DeleteIndividualCallLogAction isIncoming.
             * @member {boolean|null|undefined} isIncoming
             * @memberof proto.SyncActionValue.DeleteIndividualCallLogAction
             * @instance
             */
            DeleteIndividualCallLogAction.prototype.isIncoming = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DeleteIndividualCallLogAction.prototype, "_peerJid", {
                get: $util.oneOfGetter($oneOfFields = ["peerJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DeleteIndividualCallLogAction.prototype, "_isIncoming", {
                get: $util.oneOfGetter($oneOfFields = ["isIncoming"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new DeleteIndividualCallLogAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.DeleteIndividualCallLogAction
             * @static
             * @param {proto.SyncActionValue.IDeleteIndividualCallLogAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.DeleteIndividualCallLogAction} DeleteIndividualCallLogAction instance
             */
            DeleteIndividualCallLogAction.create = function create(properties) {
                return new DeleteIndividualCallLogAction(properties);
            };

            /**
             * Encodes the specified DeleteIndividualCallLogAction message. Does not implicitly {@link proto.SyncActionValue.DeleteIndividualCallLogAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.DeleteIndividualCallLogAction
             * @static
             * @param {proto.SyncActionValue.IDeleteIndividualCallLogAction} message DeleteIndividualCallLogAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DeleteIndividualCallLogAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.peerJid != null && Object.hasOwnProperty.call(message, "peerJid"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.peerJid);
                if (message.isIncoming != null && Object.hasOwnProperty.call(message, "isIncoming"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.isIncoming);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DeleteIndividualCallLogAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.DeleteIndividualCallLogAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.DeleteIndividualCallLogAction
             * @static
             * @param {proto.SyncActionValue.IDeleteIndividualCallLogAction} message DeleteIndividualCallLogAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DeleteIndividualCallLogAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a DeleteIndividualCallLogAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.DeleteIndividualCallLogAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.DeleteIndividualCallLogAction} DeleteIndividualCallLogAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DeleteIndividualCallLogAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.peerJid = reader.stringVerify();
                            message._peerJid = "peerJid";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.isIncoming = reader.bool();
                            message._isIncoming = "isIncoming";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a DeleteIndividualCallLogAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.DeleteIndividualCallLogAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.DeleteIndividualCallLogAction} DeleteIndividualCallLogAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DeleteIndividualCallLogAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DeleteIndividualCallLogAction message.
             * @function verify
             * @memberof proto.SyncActionValue.DeleteIndividualCallLogAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DeleteIndividualCallLogAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.peerJid != null && Object.hasOwnProperty.call(message, "peerJid")) {
                    properties._peerJid = 1;
                    if (!$util.isString(message.peerJid))
                        return "peerJid: string expected";
                }
                if (message.isIncoming != null && Object.hasOwnProperty.call(message, "isIncoming")) {
                    properties._isIncoming = 1;
                    if (typeof message.isIncoming !== "boolean")
                        return "isIncoming: boolean expected";
                }
                return null;
            };

            /**
             * Creates a DeleteIndividualCallLogAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.DeleteIndividualCallLogAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.DeleteIndividualCallLogAction} DeleteIndividualCallLogAction
             */
            DeleteIndividualCallLogAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.DeleteIndividualCallLogAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.peerJid != null)
                    message.peerJid = String(object.peerJid);
                if (object.isIncoming != null)
                    message.isIncoming = Boolean(object.isIncoming);
                return message;
            };

            /**
             * Creates a plain object from a DeleteIndividualCallLogAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.DeleteIndividualCallLogAction
             * @static
             * @param {proto.SyncActionValue.DeleteIndividualCallLogAction} message DeleteIndividualCallLogAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DeleteIndividualCallLogAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.peerJid != null && Object.hasOwnProperty.call(message, "peerJid"))
                    object.peerJid = message.peerJid;
                if (message.isIncoming != null && Object.hasOwnProperty.call(message, "isIncoming"))
                    object.isIncoming = message.isIncoming;
                return object;
            };

            /**
             * Converts this DeleteIndividualCallLogAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.DeleteIndividualCallLogAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DeleteIndividualCallLogAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for DeleteIndividualCallLogAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.DeleteIndividualCallLogAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            DeleteIndividualCallLogAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.DeleteIndividualCallLogAction";
            };

            return DeleteIndividualCallLogAction;
        })();

        SyncActionValue.DeleteMessageForMeAction = (function() {

            /**
             * Properties of a DeleteMessageForMeAction.
             * @memberof proto.SyncActionValue
             * @interface IDeleteMessageForMeAction
             * @property {boolean|null} [deleteMedia] DeleteMessageForMeAction deleteMedia
             * @property {number|Long|null} [messageTimestamp] DeleteMessageForMeAction messageTimestamp
             */

            /**
             * Constructs a new DeleteMessageForMeAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a DeleteMessageForMeAction.
             * @implements IDeleteMessageForMeAction
             * @constructor
             * @param {proto.SyncActionValue.IDeleteMessageForMeAction=} [properties] Properties to set
             */
            function DeleteMessageForMeAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * DeleteMessageForMeAction deleteMedia.
             * @member {boolean|null|undefined} deleteMedia
             * @memberof proto.SyncActionValue.DeleteMessageForMeAction
             * @instance
             */
            DeleteMessageForMeAction.prototype.deleteMedia = null;

            /**
             * DeleteMessageForMeAction messageTimestamp.
             * @member {number|Long|null|undefined} messageTimestamp
             * @memberof proto.SyncActionValue.DeleteMessageForMeAction
             * @instance
             */
            DeleteMessageForMeAction.prototype.messageTimestamp = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DeleteMessageForMeAction.prototype, "_deleteMedia", {
                get: $util.oneOfGetter($oneOfFields = ["deleteMedia"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DeleteMessageForMeAction.prototype, "_messageTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["messageTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new DeleteMessageForMeAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.DeleteMessageForMeAction
             * @static
             * @param {proto.SyncActionValue.IDeleteMessageForMeAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.DeleteMessageForMeAction} DeleteMessageForMeAction instance
             */
            DeleteMessageForMeAction.create = function create(properties) {
                return new DeleteMessageForMeAction(properties);
            };

            /**
             * Encodes the specified DeleteMessageForMeAction message. Does not implicitly {@link proto.SyncActionValue.DeleteMessageForMeAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.DeleteMessageForMeAction
             * @static
             * @param {proto.SyncActionValue.IDeleteMessageForMeAction} message DeleteMessageForMeAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DeleteMessageForMeAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.deleteMedia != null && Object.hasOwnProperty.call(message, "deleteMedia"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.deleteMedia);
                if (message.messageTimestamp != null && Object.hasOwnProperty.call(message, "messageTimestamp"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.messageTimestamp);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DeleteMessageForMeAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.DeleteMessageForMeAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.DeleteMessageForMeAction
             * @static
             * @param {proto.SyncActionValue.IDeleteMessageForMeAction} message DeleteMessageForMeAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DeleteMessageForMeAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a DeleteMessageForMeAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.DeleteMessageForMeAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.DeleteMessageForMeAction} DeleteMessageForMeAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DeleteMessageForMeAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.deleteMedia = reader.bool();
                            message._deleteMedia = "deleteMedia";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.messageTimestamp = reader.int64();
                            message._messageTimestamp = "messageTimestamp";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a DeleteMessageForMeAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.DeleteMessageForMeAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.DeleteMessageForMeAction} DeleteMessageForMeAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DeleteMessageForMeAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DeleteMessageForMeAction message.
             * @function verify
             * @memberof proto.SyncActionValue.DeleteMessageForMeAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DeleteMessageForMeAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.deleteMedia != null && Object.hasOwnProperty.call(message, "deleteMedia")) {
                    properties._deleteMedia = 1;
                    if (typeof message.deleteMedia !== "boolean")
                        return "deleteMedia: boolean expected";
                }
                if (message.messageTimestamp != null && Object.hasOwnProperty.call(message, "messageTimestamp")) {
                    properties._messageTimestamp = 1;
                    if (!$util.isInteger(message.messageTimestamp) && !(message.messageTimestamp && $util.isInteger(message.messageTimestamp.low) && $util.isInteger(message.messageTimestamp.high)))
                        return "messageTimestamp: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a DeleteMessageForMeAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.DeleteMessageForMeAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.DeleteMessageForMeAction} DeleteMessageForMeAction
             */
            DeleteMessageForMeAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.DeleteMessageForMeAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.deleteMedia != null)
                    message.deleteMedia = Boolean(object.deleteMedia);
                if (object.messageTimestamp != null)
                    if ($util.Long)
                        message.messageTimestamp = $util.Long.fromValue(object.messageTimestamp, false);
                    else if (typeof object.messageTimestamp === "string")
                        message.messageTimestamp = parseInt(object.messageTimestamp, 10);
                    else if (typeof object.messageTimestamp === "number")
                        message.messageTimestamp = object.messageTimestamp;
                    else if (typeof object.messageTimestamp === "object")
                        message.messageTimestamp = new $util.LongBits(object.messageTimestamp.low >>> 0, object.messageTimestamp.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a DeleteMessageForMeAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.DeleteMessageForMeAction
             * @static
             * @param {proto.SyncActionValue.DeleteMessageForMeAction} message DeleteMessageForMeAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DeleteMessageForMeAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.deleteMedia != null && Object.hasOwnProperty.call(message, "deleteMedia"))
                    object.deleteMedia = message.deleteMedia;
                if (message.messageTimestamp != null && Object.hasOwnProperty.call(message, "messageTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.messageTimestamp = typeof message.messageTimestamp === "number" ? BigInt(message.messageTimestamp) : $util.Long.fromBits(message.messageTimestamp.low >>> 0, message.messageTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.messageTimestamp === "number")
                        object.messageTimestamp = options.longs === String ? String(message.messageTimestamp) : message.messageTimestamp;
                    else
                        object.messageTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.messageTimestamp) : options.longs === Number ? new $util.LongBits(message.messageTimestamp.low >>> 0, message.messageTimestamp.high >>> 0).toNumber() : message.messageTimestamp;
                return object;
            };

            /**
             * Converts this DeleteMessageForMeAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.DeleteMessageForMeAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DeleteMessageForMeAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for DeleteMessageForMeAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.DeleteMessageForMeAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            DeleteMessageForMeAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.DeleteMessageForMeAction";
            };

            return DeleteMessageForMeAction;
        })();

        SyncActionValue.DetectedOutcomesStatusAction = (function() {

            /**
             * Properties of a DetectedOutcomesStatusAction.
             * @memberof proto.SyncActionValue
             * @interface IDetectedOutcomesStatusAction
             * @property {boolean|null} [isEnabled] DetectedOutcomesStatusAction isEnabled
             */

            /**
             * Constructs a new DetectedOutcomesStatusAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a DetectedOutcomesStatusAction.
             * @implements IDetectedOutcomesStatusAction
             * @constructor
             * @param {proto.SyncActionValue.IDetectedOutcomesStatusAction=} [properties] Properties to set
             */
            function DetectedOutcomesStatusAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * DetectedOutcomesStatusAction isEnabled.
             * @member {boolean|null|undefined} isEnabled
             * @memberof proto.SyncActionValue.DetectedOutcomesStatusAction
             * @instance
             */
            DetectedOutcomesStatusAction.prototype.isEnabled = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DetectedOutcomesStatusAction.prototype, "_isEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new DetectedOutcomesStatusAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.DetectedOutcomesStatusAction
             * @static
             * @param {proto.SyncActionValue.IDetectedOutcomesStatusAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.DetectedOutcomesStatusAction} DetectedOutcomesStatusAction instance
             */
            DetectedOutcomesStatusAction.create = function create(properties) {
                return new DetectedOutcomesStatusAction(properties);
            };

            /**
             * Encodes the specified DetectedOutcomesStatusAction message. Does not implicitly {@link proto.SyncActionValue.DetectedOutcomesStatusAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.DetectedOutcomesStatusAction
             * @static
             * @param {proto.SyncActionValue.IDetectedOutcomesStatusAction} message DetectedOutcomesStatusAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DetectedOutcomesStatusAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.isEnabled != null && Object.hasOwnProperty.call(message, "isEnabled"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.isEnabled);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified DetectedOutcomesStatusAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.DetectedOutcomesStatusAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.DetectedOutcomesStatusAction
             * @static
             * @param {proto.SyncActionValue.IDetectedOutcomesStatusAction} message DetectedOutcomesStatusAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DetectedOutcomesStatusAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a DetectedOutcomesStatusAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.DetectedOutcomesStatusAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.DetectedOutcomesStatusAction} DetectedOutcomesStatusAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DetectedOutcomesStatusAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.isEnabled = reader.bool();
                            message._isEnabled = "isEnabled";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a DetectedOutcomesStatusAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.DetectedOutcomesStatusAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.DetectedOutcomesStatusAction} DetectedOutcomesStatusAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DetectedOutcomesStatusAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DetectedOutcomesStatusAction message.
             * @function verify
             * @memberof proto.SyncActionValue.DetectedOutcomesStatusAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DetectedOutcomesStatusAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.isEnabled != null && Object.hasOwnProperty.call(message, "isEnabled")) {
                    properties._isEnabled = 1;
                    if (typeof message.isEnabled !== "boolean")
                        return "isEnabled: boolean expected";
                }
                return null;
            };

            /**
             * Creates a DetectedOutcomesStatusAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.DetectedOutcomesStatusAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.DetectedOutcomesStatusAction} DetectedOutcomesStatusAction
             */
            DetectedOutcomesStatusAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.DetectedOutcomesStatusAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.isEnabled != null)
                    message.isEnabled = Boolean(object.isEnabled);
                return message;
            };

            /**
             * Creates a plain object from a DetectedOutcomesStatusAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.DetectedOutcomesStatusAction
             * @static
             * @param {proto.SyncActionValue.DetectedOutcomesStatusAction} message DetectedOutcomesStatusAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DetectedOutcomesStatusAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.isEnabled != null && Object.hasOwnProperty.call(message, "isEnabled"))
                    object.isEnabled = message.isEnabled;
                return object;
            };

            /**
             * Converts this DetectedOutcomesStatusAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.DetectedOutcomesStatusAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DetectedOutcomesStatusAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for DetectedOutcomesStatusAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.DetectedOutcomesStatusAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            DetectedOutcomesStatusAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.DetectedOutcomesStatusAction";
            };

            return DetectedOutcomesStatusAction;
        })();

        SyncActionValue.ExternalWebBetaAction = (function() {

            /**
             * Properties of an ExternalWebBetaAction.
             * @memberof proto.SyncActionValue
             * @interface IExternalWebBetaAction
             * @property {boolean|null} [isOptIn] ExternalWebBetaAction isOptIn
             */

            /**
             * Constructs a new ExternalWebBetaAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents an ExternalWebBetaAction.
             * @implements IExternalWebBetaAction
             * @constructor
             * @param {proto.SyncActionValue.IExternalWebBetaAction=} [properties] Properties to set
             */
            function ExternalWebBetaAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ExternalWebBetaAction isOptIn.
             * @member {boolean|null|undefined} isOptIn
             * @memberof proto.SyncActionValue.ExternalWebBetaAction
             * @instance
             */
            ExternalWebBetaAction.prototype.isOptIn = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalWebBetaAction.prototype, "_isOptIn", {
                get: $util.oneOfGetter($oneOfFields = ["isOptIn"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ExternalWebBetaAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.ExternalWebBetaAction
             * @static
             * @param {proto.SyncActionValue.IExternalWebBetaAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.ExternalWebBetaAction} ExternalWebBetaAction instance
             */
            ExternalWebBetaAction.create = function create(properties) {
                return new ExternalWebBetaAction(properties);
            };

            /**
             * Encodes the specified ExternalWebBetaAction message. Does not implicitly {@link proto.SyncActionValue.ExternalWebBetaAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.ExternalWebBetaAction
             * @static
             * @param {proto.SyncActionValue.IExternalWebBetaAction} message ExternalWebBetaAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ExternalWebBetaAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.isOptIn != null && Object.hasOwnProperty.call(message, "isOptIn"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.isOptIn);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ExternalWebBetaAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.ExternalWebBetaAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.ExternalWebBetaAction
             * @static
             * @param {proto.SyncActionValue.IExternalWebBetaAction} message ExternalWebBetaAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ExternalWebBetaAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an ExternalWebBetaAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.ExternalWebBetaAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.ExternalWebBetaAction} ExternalWebBetaAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ExternalWebBetaAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.isOptIn = reader.bool();
                            message._isOptIn = "isOptIn";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an ExternalWebBetaAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.ExternalWebBetaAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.ExternalWebBetaAction} ExternalWebBetaAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ExternalWebBetaAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an ExternalWebBetaAction message.
             * @function verify
             * @memberof proto.SyncActionValue.ExternalWebBetaAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ExternalWebBetaAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.isOptIn != null && Object.hasOwnProperty.call(message, "isOptIn")) {
                    properties._isOptIn = 1;
                    if (typeof message.isOptIn !== "boolean")
                        return "isOptIn: boolean expected";
                }
                return null;
            };

            /**
             * Creates an ExternalWebBetaAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.ExternalWebBetaAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.ExternalWebBetaAction} ExternalWebBetaAction
             */
            ExternalWebBetaAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.ExternalWebBetaAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.isOptIn != null)
                    message.isOptIn = Boolean(object.isOptIn);
                return message;
            };

            /**
             * Creates a plain object from an ExternalWebBetaAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.ExternalWebBetaAction
             * @static
             * @param {proto.SyncActionValue.ExternalWebBetaAction} message ExternalWebBetaAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ExternalWebBetaAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.isOptIn != null && Object.hasOwnProperty.call(message, "isOptIn"))
                    object.isOptIn = message.isOptIn;
                return object;
            };

            /**
             * Converts this ExternalWebBetaAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.ExternalWebBetaAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ExternalWebBetaAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ExternalWebBetaAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.ExternalWebBetaAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ExternalWebBetaAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.ExternalWebBetaAction";
            };

            return ExternalWebBetaAction;
        })();

        SyncActionValue.FavoritesAction = (function() {

            /**
             * Properties of a FavoritesAction.
             * @memberof proto.SyncActionValue
             * @interface IFavoritesAction
             * @property {Array.<proto.SyncActionValue.FavoritesAction.IFavorite>|null} [favorites] FavoritesAction favorites
             */

            /**
             * Constructs a new FavoritesAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a FavoritesAction.
             * @implements IFavoritesAction
             * @constructor
             * @param {proto.SyncActionValue.IFavoritesAction=} [properties] Properties to set
             */
            function FavoritesAction(properties) {
                this.favorites = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * FavoritesAction favorites.
             * @member {Array.<proto.SyncActionValue.FavoritesAction.IFavorite>} favorites
             * @memberof proto.SyncActionValue.FavoritesAction
             * @instance
             */
            FavoritesAction.prototype.favorites = $util.emptyArray;

            /**
             * Creates a new FavoritesAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.FavoritesAction
             * @static
             * @param {proto.SyncActionValue.IFavoritesAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.FavoritesAction} FavoritesAction instance
             */
            FavoritesAction.create = function create(properties) {
                return new FavoritesAction(properties);
            };

            /**
             * Encodes the specified FavoritesAction message. Does not implicitly {@link proto.SyncActionValue.FavoritesAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.FavoritesAction
             * @static
             * @param {proto.SyncActionValue.IFavoritesAction} message FavoritesAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FavoritesAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.favorites != null && message.favorites.length)
                    for (var i = 0; i < message.favorites.length; ++i)
                        $root.proto.SyncActionValue.FavoritesAction.Favorite.encode(message.favorites[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified FavoritesAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.FavoritesAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.FavoritesAction
             * @static
             * @param {proto.SyncActionValue.IFavoritesAction} message FavoritesAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            FavoritesAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a FavoritesAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.FavoritesAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.FavoritesAction} FavoritesAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FavoritesAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(message.favorites && message.favorites.length))
                                message.favorites = [];
                            message.favorites.push($root.proto.SyncActionValue.FavoritesAction.Favorite.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a FavoritesAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.FavoritesAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.FavoritesAction} FavoritesAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            FavoritesAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a FavoritesAction message.
             * @function verify
             * @memberof proto.SyncActionValue.FavoritesAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            FavoritesAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.favorites != null && Object.hasOwnProperty.call(message, "favorites")) {
                    if (!Array.isArray(message.favorites))
                        return "favorites: array expected";
                    for (var i = 0; i < message.favorites.length; ++i) {
                        var error = $root.proto.SyncActionValue.FavoritesAction.Favorite.verify(message.favorites[i], q + 1);
                        if (error)
                            return "favorites." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a FavoritesAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.FavoritesAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.FavoritesAction} FavoritesAction
             */
            FavoritesAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.FavoritesAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.favorites) {
                    if (!Array.isArray(object.favorites))
                        throw TypeError(".proto.SyncActionValue.FavoritesAction.favorites: array expected");
                    message.favorites = Array(object.favorites.length);
                    for (var i = 0; i < object.favorites.length; ++i) {
                        if (!$util.isObject(object.favorites[i]))
                            throw TypeError(".proto.SyncActionValue.FavoritesAction.favorites: object expected");
                        message.favorites[i] = $root.proto.SyncActionValue.FavoritesAction.Favorite.fromObject(object.favorites[i], q + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a FavoritesAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.FavoritesAction
             * @static
             * @param {proto.SyncActionValue.FavoritesAction} message FavoritesAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            FavoritesAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.favorites = [];
                if (message.favorites && message.favorites.length) {
                    object.favorites = Array(message.favorites.length);
                    for (var j = 0; j < message.favorites.length; ++j)
                        object.favorites[j] = $root.proto.SyncActionValue.FavoritesAction.Favorite.toObject(message.favorites[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this FavoritesAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.FavoritesAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            FavoritesAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for FavoritesAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.FavoritesAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            FavoritesAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.FavoritesAction";
            };

            FavoritesAction.Favorite = (function() {

                /**
                 * Properties of a Favorite.
                 * @memberof proto.SyncActionValue.FavoritesAction
                 * @interface IFavorite
                 * @property {string|null} [id] Favorite id
                 */

                /**
                 * Constructs a new Favorite.
                 * @memberof proto.SyncActionValue.FavoritesAction
                 * @classdesc Represents a Favorite.
                 * @implements IFavorite
                 * @constructor
                 * @param {proto.SyncActionValue.FavoritesAction.IFavorite=} [properties] Properties to set
                 */
                function Favorite(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * Favorite id.
                 * @member {string|null|undefined} id
                 * @memberof proto.SyncActionValue.FavoritesAction.Favorite
                 * @instance
                 */
                Favorite.prototype.id = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Favorite.prototype, "_id", {
                    get: $util.oneOfGetter($oneOfFields = ["id"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new Favorite instance using the specified properties.
                 * @function create
                 * @memberof proto.SyncActionValue.FavoritesAction.Favorite
                 * @static
                 * @param {proto.SyncActionValue.FavoritesAction.IFavorite=} [properties] Properties to set
                 * @returns {proto.SyncActionValue.FavoritesAction.Favorite} Favorite instance
                 */
                Favorite.create = function create(properties) {
                    return new Favorite(properties);
                };

                /**
                 * Encodes the specified Favorite message. Does not implicitly {@link proto.SyncActionValue.FavoritesAction.Favorite.verify|verify} messages.
                 * @function encode
                 * @memberof proto.SyncActionValue.FavoritesAction.Favorite
                 * @static
                 * @param {proto.SyncActionValue.FavoritesAction.IFavorite} message Favorite message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Favorite.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified Favorite message, length delimited. Does not implicitly {@link proto.SyncActionValue.FavoritesAction.Favorite.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.SyncActionValue.FavoritesAction.Favorite
                 * @static
                 * @param {proto.SyncActionValue.FavoritesAction.IFavorite} message Favorite message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Favorite.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a Favorite message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.SyncActionValue.FavoritesAction.Favorite
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.SyncActionValue.FavoritesAction.Favorite} Favorite
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Favorite.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.id = reader.stringVerify();
                                message._id = "id";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a Favorite message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.SyncActionValue.FavoritesAction.Favorite
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.SyncActionValue.FavoritesAction.Favorite} Favorite
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Favorite.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a Favorite message.
                 * @function verify
                 * @memberof proto.SyncActionValue.FavoritesAction.Favorite
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                Favorite.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.id != null && Object.hasOwnProperty.call(message, "id")) {
                        properties._id = 1;
                        if (!$util.isString(message.id))
                            return "id: string expected";
                    }
                    return null;
                };

                /**
                 * Creates a Favorite message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.SyncActionValue.FavoritesAction.Favorite
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.SyncActionValue.FavoritesAction.Favorite} Favorite
                 */
                Favorite.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.SyncActionValue.FavoritesAction.Favorite: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.id != null)
                        message.id = String(object.id);
                    return message;
                };

                /**
                 * Creates a plain object from a Favorite message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.SyncActionValue.FavoritesAction.Favorite
                 * @static
                 * @param {proto.SyncActionValue.FavoritesAction.Favorite} message Favorite
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                Favorite.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                        object.id = message.id;
                    return object;
                };

                /**
                 * Converts this Favorite to JSON.
                 * @function toJSON
                 * @memberof proto.SyncActionValue.FavoritesAction.Favorite
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                Favorite.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for Favorite
                 * @function getTypeUrl
                 * @memberof proto.SyncActionValue.FavoritesAction.Favorite
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                Favorite.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.SyncActionValue.FavoritesAction.Favorite";
                };

                return Favorite;
            })();

            return FavoritesAction;
        })();

        SyncActionValue.InteractiveMessageAction = (function() {

            /**
             * Properties of an InteractiveMessageAction.
             * @memberof proto.SyncActionValue
             * @interface IInteractiveMessageAction
             * @property {proto.SyncActionValue.InteractiveMessageAction.InteractiveMessageActionMode|null} [type] InteractiveMessageAction type
             * @property {string|null} [agmId] InteractiveMessageAction agmId
             */

            /**
             * Constructs a new InteractiveMessageAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents an InteractiveMessageAction.
             * @implements IInteractiveMessageAction
             * @constructor
             * @param {proto.SyncActionValue.IInteractiveMessageAction=} [properties] Properties to set
             */
            function InteractiveMessageAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * InteractiveMessageAction type.
             * @member {proto.SyncActionValue.InteractiveMessageAction.InteractiveMessageActionMode|null|undefined} type
             * @memberof proto.SyncActionValue.InteractiveMessageAction
             * @instance
             */
            InteractiveMessageAction.prototype.type = null;

            /**
             * InteractiveMessageAction agmId.
             * @member {string|null|undefined} agmId
             * @memberof proto.SyncActionValue.InteractiveMessageAction
             * @instance
             */
            InteractiveMessageAction.prototype.agmId = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InteractiveMessageAction.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InteractiveMessageAction.prototype, "_agmId", {
                get: $util.oneOfGetter($oneOfFields = ["agmId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new InteractiveMessageAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.InteractiveMessageAction
             * @static
             * @param {proto.SyncActionValue.IInteractiveMessageAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.InteractiveMessageAction} InteractiveMessageAction instance
             */
            InteractiveMessageAction.create = function create(properties) {
                return new InteractiveMessageAction(properties);
            };

            /**
             * Encodes the specified InteractiveMessageAction message. Does not implicitly {@link proto.SyncActionValue.InteractiveMessageAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.InteractiveMessageAction
             * @static
             * @param {proto.SyncActionValue.IInteractiveMessageAction} message InteractiveMessageAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InteractiveMessageAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
                if (message.agmId != null && Object.hasOwnProperty.call(message, "agmId"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.agmId);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified InteractiveMessageAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.InteractiveMessageAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.InteractiveMessageAction
             * @static
             * @param {proto.SyncActionValue.IInteractiveMessageAction} message InteractiveMessageAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            InteractiveMessageAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an InteractiveMessageAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.InteractiveMessageAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.InteractiveMessageAction} InteractiveMessageAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InteractiveMessageAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.type = reader.int32();
                            message._type = "type";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.agmId = reader.stringVerify();
                            message._agmId = "agmId";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an InteractiveMessageAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.InteractiveMessageAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.InteractiveMessageAction} InteractiveMessageAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            InteractiveMessageAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an InteractiveMessageAction message.
             * @function verify
             * @memberof proto.SyncActionValue.InteractiveMessageAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            InteractiveMessageAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                    properties._type = 1;
                    if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                        return "type: enum value expected";
                }
                if (message.agmId != null && Object.hasOwnProperty.call(message, "agmId")) {
                    properties._agmId = 1;
                    if (!$util.isString(message.agmId))
                        return "agmId: string expected";
                }
                return null;
            };

            /**
             * Creates an InteractiveMessageAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.InteractiveMessageAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.InteractiveMessageAction} InteractiveMessageAction
             */
            InteractiveMessageAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.InteractiveMessageAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.type) {
                case "DISABLE_CTA":
                case 1:
                    message.type = 1;
                    break;
                default:
                    if (typeof object.type === "number" && (object.type | 0) === object.type)
                        message.type = object.type;
                }
                if (object.agmId != null)
                    message.agmId = String(object.agmId);
                return message;
            };

            /**
             * Creates a plain object from an InteractiveMessageAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.InteractiveMessageAction
             * @static
             * @param {proto.SyncActionValue.InteractiveMessageAction} message InteractiveMessageAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            InteractiveMessageAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    object.type = options.enums === String ? $root.proto.SyncActionValue.InteractiveMessageAction.InteractiveMessageActionMode[message.type] === undefined ? message.type : $root.proto.SyncActionValue.InteractiveMessageAction.InteractiveMessageActionMode[message.type] : message.type;
                if (message.agmId != null && Object.hasOwnProperty.call(message, "agmId"))
                    object.agmId = message.agmId;
                return object;
            };

            /**
             * Converts this InteractiveMessageAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.InteractiveMessageAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            InteractiveMessageAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for InteractiveMessageAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.InteractiveMessageAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            InteractiveMessageAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.InteractiveMessageAction";
            };

            /**
             * InteractiveMessageActionMode enum.
             * @name proto.SyncActionValue.InteractiveMessageAction.InteractiveMessageActionMode
             * @enum {number}
             * @property {number} DISABLE_CTA=1 DISABLE_CTA value
             */
            InteractiveMessageAction.InteractiveMessageActionMode = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[1] = "DISABLE_CTA"] = 1;
                return values;
            })();

            return InteractiveMessageAction;
        })();

        SyncActionValue.KeyExpiration = (function() {

            /**
             * Properties of a KeyExpiration.
             * @memberof proto.SyncActionValue
             * @interface IKeyExpiration
             * @property {number|null} [expiredKeyEpoch] KeyExpiration expiredKeyEpoch
             */

            /**
             * Constructs a new KeyExpiration.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a KeyExpiration.
             * @implements IKeyExpiration
             * @constructor
             * @param {proto.SyncActionValue.IKeyExpiration=} [properties] Properties to set
             */
            function KeyExpiration(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * KeyExpiration expiredKeyEpoch.
             * @member {number|null|undefined} expiredKeyEpoch
             * @memberof proto.SyncActionValue.KeyExpiration
             * @instance
             */
            KeyExpiration.prototype.expiredKeyEpoch = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(KeyExpiration.prototype, "_expiredKeyEpoch", {
                get: $util.oneOfGetter($oneOfFields = ["expiredKeyEpoch"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new KeyExpiration instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.KeyExpiration
             * @static
             * @param {proto.SyncActionValue.IKeyExpiration=} [properties] Properties to set
             * @returns {proto.SyncActionValue.KeyExpiration} KeyExpiration instance
             */
            KeyExpiration.create = function create(properties) {
                return new KeyExpiration(properties);
            };

            /**
             * Encodes the specified KeyExpiration message. Does not implicitly {@link proto.SyncActionValue.KeyExpiration.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.KeyExpiration
             * @static
             * @param {proto.SyncActionValue.IKeyExpiration} message KeyExpiration message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            KeyExpiration.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.expiredKeyEpoch != null && Object.hasOwnProperty.call(message, "expiredKeyEpoch"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.expiredKeyEpoch);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified KeyExpiration message, length delimited. Does not implicitly {@link proto.SyncActionValue.KeyExpiration.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.KeyExpiration
             * @static
             * @param {proto.SyncActionValue.IKeyExpiration} message KeyExpiration message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            KeyExpiration.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a KeyExpiration message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.KeyExpiration
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.KeyExpiration} KeyExpiration
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            KeyExpiration.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.expiredKeyEpoch = reader.int32();
                            message._expiredKeyEpoch = "expiredKeyEpoch";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a KeyExpiration message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.KeyExpiration
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.KeyExpiration} KeyExpiration
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            KeyExpiration.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a KeyExpiration message.
             * @function verify
             * @memberof proto.SyncActionValue.KeyExpiration
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            KeyExpiration.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.expiredKeyEpoch != null && Object.hasOwnProperty.call(message, "expiredKeyEpoch")) {
                    properties._expiredKeyEpoch = 1;
                    if (!$util.isInteger(message.expiredKeyEpoch))
                        return "expiredKeyEpoch: integer expected";
                }
                return null;
            };

            /**
             * Creates a KeyExpiration message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.KeyExpiration
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.KeyExpiration} KeyExpiration
             */
            KeyExpiration.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.KeyExpiration: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.expiredKeyEpoch != null)
                    message.expiredKeyEpoch = object.expiredKeyEpoch | 0;
                return message;
            };

            /**
             * Creates a plain object from a KeyExpiration message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.KeyExpiration
             * @static
             * @param {proto.SyncActionValue.KeyExpiration} message KeyExpiration
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            KeyExpiration.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.expiredKeyEpoch != null && Object.hasOwnProperty.call(message, "expiredKeyEpoch"))
                    object.expiredKeyEpoch = message.expiredKeyEpoch;
                return object;
            };

            /**
             * Converts this KeyExpiration to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.KeyExpiration
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            KeyExpiration.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for KeyExpiration
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.KeyExpiration
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            KeyExpiration.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.KeyExpiration";
            };

            return KeyExpiration;
        })();

        SyncActionValue.LabelAssociationAction = (function() {

            /**
             * Properties of a LabelAssociationAction.
             * @memberof proto.SyncActionValue
             * @interface ILabelAssociationAction
             * @property {boolean|null} [labeled] LabelAssociationAction labeled
             * @property {string|null} [modelMetaData] LabelAssociationAction modelMetaData
             */

            /**
             * Constructs a new LabelAssociationAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a LabelAssociationAction.
             * @implements ILabelAssociationAction
             * @constructor
             * @param {proto.SyncActionValue.ILabelAssociationAction=} [properties] Properties to set
             */
            function LabelAssociationAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * LabelAssociationAction labeled.
             * @member {boolean|null|undefined} labeled
             * @memberof proto.SyncActionValue.LabelAssociationAction
             * @instance
             */
            LabelAssociationAction.prototype.labeled = null;

            /**
             * LabelAssociationAction modelMetaData.
             * @member {string|null|undefined} modelMetaData
             * @memberof proto.SyncActionValue.LabelAssociationAction
             * @instance
             */
            LabelAssociationAction.prototype.modelMetaData = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LabelAssociationAction.prototype, "_labeled", {
                get: $util.oneOfGetter($oneOfFields = ["labeled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LabelAssociationAction.prototype, "_modelMetaData", {
                get: $util.oneOfGetter($oneOfFields = ["modelMetaData"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new LabelAssociationAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.LabelAssociationAction
             * @static
             * @param {proto.SyncActionValue.ILabelAssociationAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.LabelAssociationAction} LabelAssociationAction instance
             */
            LabelAssociationAction.create = function create(properties) {
                return new LabelAssociationAction(properties);
            };

            /**
             * Encodes the specified LabelAssociationAction message. Does not implicitly {@link proto.SyncActionValue.LabelAssociationAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.LabelAssociationAction
             * @static
             * @param {proto.SyncActionValue.ILabelAssociationAction} message LabelAssociationAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LabelAssociationAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.labeled != null && Object.hasOwnProperty.call(message, "labeled"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.labeled);
                if (message.modelMetaData != null && Object.hasOwnProperty.call(message, "modelMetaData"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.modelMetaData);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LabelAssociationAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.LabelAssociationAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.LabelAssociationAction
             * @static
             * @param {proto.SyncActionValue.ILabelAssociationAction} message LabelAssociationAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LabelAssociationAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a LabelAssociationAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.LabelAssociationAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.LabelAssociationAction} LabelAssociationAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LabelAssociationAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.labeled = reader.bool();
                            message._labeled = "labeled";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.modelMetaData = reader.stringVerify();
                            message._modelMetaData = "modelMetaData";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a LabelAssociationAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.LabelAssociationAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.LabelAssociationAction} LabelAssociationAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LabelAssociationAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LabelAssociationAction message.
             * @function verify
             * @memberof proto.SyncActionValue.LabelAssociationAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LabelAssociationAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.labeled != null && Object.hasOwnProperty.call(message, "labeled")) {
                    properties._labeled = 1;
                    if (typeof message.labeled !== "boolean")
                        return "labeled: boolean expected";
                }
                if (message.modelMetaData != null && Object.hasOwnProperty.call(message, "modelMetaData")) {
                    properties._modelMetaData = 1;
                    if (!$util.isString(message.modelMetaData))
                        return "modelMetaData: string expected";
                }
                return null;
            };

            /**
             * Creates a LabelAssociationAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.LabelAssociationAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.LabelAssociationAction} LabelAssociationAction
             */
            LabelAssociationAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.LabelAssociationAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.labeled != null)
                    message.labeled = Boolean(object.labeled);
                if (object.modelMetaData != null)
                    message.modelMetaData = String(object.modelMetaData);
                return message;
            };

            /**
             * Creates a plain object from a LabelAssociationAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.LabelAssociationAction
             * @static
             * @param {proto.SyncActionValue.LabelAssociationAction} message LabelAssociationAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LabelAssociationAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.labeled != null && Object.hasOwnProperty.call(message, "labeled"))
                    object.labeled = message.labeled;
                if (message.modelMetaData != null && Object.hasOwnProperty.call(message, "modelMetaData"))
                    object.modelMetaData = message.modelMetaData;
                return object;
            };

            /**
             * Converts this LabelAssociationAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.LabelAssociationAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LabelAssociationAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for LabelAssociationAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.LabelAssociationAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            LabelAssociationAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.LabelAssociationAction";
            };

            return LabelAssociationAction;
        })();

        SyncActionValue.LabelEditAction = (function() {

            /**
             * Properties of a LabelEditAction.
             * @memberof proto.SyncActionValue
             * @interface ILabelEditAction
             * @property {string|null} [name] LabelEditAction name
             * @property {number|null} [color] LabelEditAction color
             * @property {number|null} [predefinedId] LabelEditAction predefinedId
             * @property {boolean|null} [deleted] LabelEditAction deleted
             * @property {number|null} [orderIndex] LabelEditAction orderIndex
             * @property {boolean|null} [isActive] LabelEditAction isActive
             * @property {proto.SyncActionValue.LabelEditAction.ListType|null} [type] LabelEditAction type
             * @property {boolean|null} [isImmutable] LabelEditAction isImmutable
             * @property {number|Long|null} [muteEndTimeMs] LabelEditAction muteEndTimeMs
             */

            /**
             * Constructs a new LabelEditAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a LabelEditAction.
             * @implements ILabelEditAction
             * @constructor
             * @param {proto.SyncActionValue.ILabelEditAction=} [properties] Properties to set
             */
            function LabelEditAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * LabelEditAction name.
             * @member {string|null|undefined} name
             * @memberof proto.SyncActionValue.LabelEditAction
             * @instance
             */
            LabelEditAction.prototype.name = null;

            /**
             * LabelEditAction color.
             * @member {number|null|undefined} color
             * @memberof proto.SyncActionValue.LabelEditAction
             * @instance
             */
            LabelEditAction.prototype.color = null;

            /**
             * LabelEditAction predefinedId.
             * @member {number|null|undefined} predefinedId
             * @memberof proto.SyncActionValue.LabelEditAction
             * @instance
             */
            LabelEditAction.prototype.predefinedId = null;

            /**
             * LabelEditAction deleted.
             * @member {boolean|null|undefined} deleted
             * @memberof proto.SyncActionValue.LabelEditAction
             * @instance
             */
            LabelEditAction.prototype.deleted = null;

            /**
             * LabelEditAction orderIndex.
             * @member {number|null|undefined} orderIndex
             * @memberof proto.SyncActionValue.LabelEditAction
             * @instance
             */
            LabelEditAction.prototype.orderIndex = null;

            /**
             * LabelEditAction isActive.
             * @member {boolean|null|undefined} isActive
             * @memberof proto.SyncActionValue.LabelEditAction
             * @instance
             */
            LabelEditAction.prototype.isActive = null;

            /**
             * LabelEditAction type.
             * @member {proto.SyncActionValue.LabelEditAction.ListType|null|undefined} type
             * @memberof proto.SyncActionValue.LabelEditAction
             * @instance
             */
            LabelEditAction.prototype.type = null;

            /**
             * LabelEditAction isImmutable.
             * @member {boolean|null|undefined} isImmutable
             * @memberof proto.SyncActionValue.LabelEditAction
             * @instance
             */
            LabelEditAction.prototype.isImmutable = null;

            /**
             * LabelEditAction muteEndTimeMs.
             * @member {number|Long|null|undefined} muteEndTimeMs
             * @memberof proto.SyncActionValue.LabelEditAction
             * @instance
             */
            LabelEditAction.prototype.muteEndTimeMs = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LabelEditAction.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LabelEditAction.prototype, "_color", {
                get: $util.oneOfGetter($oneOfFields = ["color"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LabelEditAction.prototype, "_predefinedId", {
                get: $util.oneOfGetter($oneOfFields = ["predefinedId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LabelEditAction.prototype, "_deleted", {
                get: $util.oneOfGetter($oneOfFields = ["deleted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LabelEditAction.prototype, "_orderIndex", {
                get: $util.oneOfGetter($oneOfFields = ["orderIndex"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LabelEditAction.prototype, "_isActive", {
                get: $util.oneOfGetter($oneOfFields = ["isActive"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LabelEditAction.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LabelEditAction.prototype, "_isImmutable", {
                get: $util.oneOfGetter($oneOfFields = ["isImmutable"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LabelEditAction.prototype, "_muteEndTimeMs", {
                get: $util.oneOfGetter($oneOfFields = ["muteEndTimeMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new LabelEditAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.LabelEditAction
             * @static
             * @param {proto.SyncActionValue.ILabelEditAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.LabelEditAction} LabelEditAction instance
             */
            LabelEditAction.create = function create(properties) {
                return new LabelEditAction(properties);
            };

            /**
             * Encodes the specified LabelEditAction message. Does not implicitly {@link proto.SyncActionValue.LabelEditAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.LabelEditAction
             * @static
             * @param {proto.SyncActionValue.ILabelEditAction} message LabelEditAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LabelEditAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.color != null && Object.hasOwnProperty.call(message, "color"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.color);
                if (message.predefinedId != null && Object.hasOwnProperty.call(message, "predefinedId"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.predefinedId);
                if (message.deleted != null && Object.hasOwnProperty.call(message, "deleted"))
                    writer.uint32(/* id 4, wireType 0 =*/32).bool(message.deleted);
                if (message.orderIndex != null && Object.hasOwnProperty.call(message, "orderIndex"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int32(message.orderIndex);
                if (message.isActive != null && Object.hasOwnProperty.call(message, "isActive"))
                    writer.uint32(/* id 6, wireType 0 =*/48).bool(message.isActive);
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(/* id 7, wireType 0 =*/56).int32(message.type);
                if (message.isImmutable != null && Object.hasOwnProperty.call(message, "isImmutable"))
                    writer.uint32(/* id 8, wireType 0 =*/64).bool(message.isImmutable);
                if (message.muteEndTimeMs != null && Object.hasOwnProperty.call(message, "muteEndTimeMs"))
                    writer.uint32(/* id 9, wireType 0 =*/72).int64(message.muteEndTimeMs);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LabelEditAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.LabelEditAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.LabelEditAction
             * @static
             * @param {proto.SyncActionValue.ILabelEditAction} message LabelEditAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LabelEditAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a LabelEditAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.LabelEditAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.LabelEditAction} LabelEditAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LabelEditAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.color = reader.int32();
                            message._color = "color";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.predefinedId = reader.int32();
                            message._predefinedId = "predefinedId";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.deleted = reader.bool();
                            message._deleted = "deleted";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.orderIndex = reader.int32();
                            message._orderIndex = "orderIndex";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.isActive = reader.bool();
                            message._isActive = "isActive";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            message.type = reader.int32();
                            message._type = "type";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            message.isImmutable = reader.bool();
                            message._isImmutable = "isImmutable";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            message.muteEndTimeMs = reader.int64();
                            message._muteEndTimeMs = "muteEndTimeMs";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a LabelEditAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.LabelEditAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.LabelEditAction} LabelEditAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LabelEditAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LabelEditAction message.
             * @function verify
             * @memberof proto.SyncActionValue.LabelEditAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LabelEditAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.name != null && Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                if (message.color != null && Object.hasOwnProperty.call(message, "color")) {
                    properties._color = 1;
                    if (!$util.isInteger(message.color))
                        return "color: integer expected";
                }
                if (message.predefinedId != null && Object.hasOwnProperty.call(message, "predefinedId")) {
                    properties._predefinedId = 1;
                    if (!$util.isInteger(message.predefinedId))
                        return "predefinedId: integer expected";
                }
                if (message.deleted != null && Object.hasOwnProperty.call(message, "deleted")) {
                    properties._deleted = 1;
                    if (typeof message.deleted !== "boolean")
                        return "deleted: boolean expected";
                }
                if (message.orderIndex != null && Object.hasOwnProperty.call(message, "orderIndex")) {
                    properties._orderIndex = 1;
                    if (!$util.isInteger(message.orderIndex))
                        return "orderIndex: integer expected";
                }
                if (message.isActive != null && Object.hasOwnProperty.call(message, "isActive")) {
                    properties._isActive = 1;
                    if (typeof message.isActive !== "boolean")
                        return "isActive: boolean expected";
                }
                if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                    properties._type = 1;
                    if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                        return "type: enum value expected";
                }
                if (message.isImmutable != null && Object.hasOwnProperty.call(message, "isImmutable")) {
                    properties._isImmutable = 1;
                    if (typeof message.isImmutable !== "boolean")
                        return "isImmutable: boolean expected";
                }
                if (message.muteEndTimeMs != null && Object.hasOwnProperty.call(message, "muteEndTimeMs")) {
                    properties._muteEndTimeMs = 1;
                    if (!$util.isInteger(message.muteEndTimeMs) && !(message.muteEndTimeMs && $util.isInteger(message.muteEndTimeMs.low) && $util.isInteger(message.muteEndTimeMs.high)))
                        return "muteEndTimeMs: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a LabelEditAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.LabelEditAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.LabelEditAction} LabelEditAction
             */
            LabelEditAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.LabelEditAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.name != null)
                    message.name = String(object.name);
                if (object.color != null)
                    message.color = object.color | 0;
                if (object.predefinedId != null)
                    message.predefinedId = object.predefinedId | 0;
                if (object.deleted != null)
                    message.deleted = Boolean(object.deleted);
                if (object.orderIndex != null)
                    message.orderIndex = object.orderIndex | 0;
                if (object.isActive != null)
                    message.isActive = Boolean(object.isActive);
                switch (object.type) {
                case "NONE":
                case 0:
                    message.type = 0;
                    break;
                case "UNREAD":
                case 1:
                    message.type = 1;
                    break;
                case "GROUPS":
                case 2:
                    message.type = 2;
                    break;
                case "FAVORITES":
                case 3:
                    message.type = 3;
                    break;
                case "PREDEFINED":
                case 4:
                    message.type = 4;
                    break;
                case "CUSTOM":
                case 5:
                    message.type = 5;
                    break;
                case "COMMUNITY":
                case 6:
                    message.type = 6;
                    break;
                case "SERVER_ASSIGNED":
                case 7:
                    message.type = 7;
                    break;
                case "DRAFTED":
                case 8:
                    message.type = 8;
                    break;
                case "AI_HANDOFF":
                case 9:
                    message.type = 9;
                    break;
                case "CHANNELS":
                case 10:
                    message.type = 10;
                    break;
                case "AI_RESPONDING":
                case 11:
                    message.type = 11;
                    break;
                case "ARCHIVED":
                case 12:
                    message.type = 12;
                    break;
                case "LOCKED":
                case 13:
                    message.type = 13;
                    break;
                case "INVITES":
                case 14:
                    message.type = 14;
                    break;
                case "THIRD_PARTY":
                case 15:
                    message.type = 15;
                    break;
                default:
                    if (typeof object.type === "number" && (object.type | 0) === object.type)
                        message.type = object.type;
                }
                if (object.isImmutable != null)
                    message.isImmutable = Boolean(object.isImmutable);
                if (object.muteEndTimeMs != null)
                    if ($util.Long)
                        message.muteEndTimeMs = $util.Long.fromValue(object.muteEndTimeMs, false);
                    else if (typeof object.muteEndTimeMs === "string")
                        message.muteEndTimeMs = parseInt(object.muteEndTimeMs, 10);
                    else if (typeof object.muteEndTimeMs === "number")
                        message.muteEndTimeMs = object.muteEndTimeMs;
                    else if (typeof object.muteEndTimeMs === "object")
                        message.muteEndTimeMs = new $util.LongBits(object.muteEndTimeMs.low >>> 0, object.muteEndTimeMs.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a LabelEditAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.LabelEditAction
             * @static
             * @param {proto.SyncActionValue.LabelEditAction} message LabelEditAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LabelEditAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.color != null && Object.hasOwnProperty.call(message, "color"))
                    object.color = message.color;
                if (message.predefinedId != null && Object.hasOwnProperty.call(message, "predefinedId"))
                    object.predefinedId = message.predefinedId;
                if (message.deleted != null && Object.hasOwnProperty.call(message, "deleted"))
                    object.deleted = message.deleted;
                if (message.orderIndex != null && Object.hasOwnProperty.call(message, "orderIndex"))
                    object.orderIndex = message.orderIndex;
                if (message.isActive != null && Object.hasOwnProperty.call(message, "isActive"))
                    object.isActive = message.isActive;
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    object.type = options.enums === String ? $root.proto.SyncActionValue.LabelEditAction.ListType[message.type] === undefined ? message.type : $root.proto.SyncActionValue.LabelEditAction.ListType[message.type] : message.type;
                if (message.isImmutable != null && Object.hasOwnProperty.call(message, "isImmutable"))
                    object.isImmutable = message.isImmutable;
                if (message.muteEndTimeMs != null && Object.hasOwnProperty.call(message, "muteEndTimeMs"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.muteEndTimeMs = typeof message.muteEndTimeMs === "number" ? BigInt(message.muteEndTimeMs) : $util.Long.fromBits(message.muteEndTimeMs.low >>> 0, message.muteEndTimeMs.high >>> 0, false).toBigInt();
                    else if (typeof message.muteEndTimeMs === "number")
                        object.muteEndTimeMs = options.longs === String ? String(message.muteEndTimeMs) : message.muteEndTimeMs;
                    else
                        object.muteEndTimeMs = options.longs === String ? $util.Long.prototype.toString.call(message.muteEndTimeMs) : options.longs === Number ? new $util.LongBits(message.muteEndTimeMs.low >>> 0, message.muteEndTimeMs.high >>> 0).toNumber() : message.muteEndTimeMs;
                return object;
            };

            /**
             * Converts this LabelEditAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.LabelEditAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LabelEditAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for LabelEditAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.LabelEditAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            LabelEditAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.LabelEditAction";
            };

            /**
             * ListType enum.
             * @name proto.SyncActionValue.LabelEditAction.ListType
             * @enum {number}
             * @property {number} NONE=0 NONE value
             * @property {number} UNREAD=1 UNREAD value
             * @property {number} GROUPS=2 GROUPS value
             * @property {number} FAVORITES=3 FAVORITES value
             * @property {number} PREDEFINED=4 PREDEFINED value
             * @property {number} CUSTOM=5 CUSTOM value
             * @property {number} COMMUNITY=6 COMMUNITY value
             * @property {number} SERVER_ASSIGNED=7 SERVER_ASSIGNED value
             * @property {number} DRAFTED=8 DRAFTED value
             * @property {number} AI_HANDOFF=9 AI_HANDOFF value
             * @property {number} CHANNELS=10 CHANNELS value
             * @property {number} AI_RESPONDING=11 AI_RESPONDING value
             * @property {number} ARCHIVED=12 ARCHIVED value
             * @property {number} LOCKED=13 LOCKED value
             * @property {number} INVITES=14 INVITES value
             * @property {number} THIRD_PARTY=15 THIRD_PARTY value
             */
            LabelEditAction.ListType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "NONE"] = 0;
                values[valuesById[1] = "UNREAD"] = 1;
                values[valuesById[2] = "GROUPS"] = 2;
                values[valuesById[3] = "FAVORITES"] = 3;
                values[valuesById[4] = "PREDEFINED"] = 4;
                values[valuesById[5] = "CUSTOM"] = 5;
                values[valuesById[6] = "COMMUNITY"] = 6;
                values[valuesById[7] = "SERVER_ASSIGNED"] = 7;
                values[valuesById[8] = "DRAFTED"] = 8;
                values[valuesById[9] = "AI_HANDOFF"] = 9;
                values[valuesById[10] = "CHANNELS"] = 10;
                values[valuesById[11] = "AI_RESPONDING"] = 11;
                values[valuesById[12] = "ARCHIVED"] = 12;
                values[valuesById[13] = "LOCKED"] = 13;
                values[valuesById[14] = "INVITES"] = 14;
                values[valuesById[15] = "THIRD_PARTY"] = 15;
                return values;
            })();

            return LabelEditAction;
        })();

        SyncActionValue.LabelReorderingAction = (function() {

            /**
             * Properties of a LabelReorderingAction.
             * @memberof proto.SyncActionValue
             * @interface ILabelReorderingAction
             * @property {Array.<number>|null} [sortedLabelIds] LabelReorderingAction sortedLabelIds
             */

            /**
             * Constructs a new LabelReorderingAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a LabelReorderingAction.
             * @implements ILabelReorderingAction
             * @constructor
             * @param {proto.SyncActionValue.ILabelReorderingAction=} [properties] Properties to set
             */
            function LabelReorderingAction(properties) {
                this.sortedLabelIds = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * LabelReorderingAction sortedLabelIds.
             * @member {Array.<number>} sortedLabelIds
             * @memberof proto.SyncActionValue.LabelReorderingAction
             * @instance
             */
            LabelReorderingAction.prototype.sortedLabelIds = $util.emptyArray;

            /**
             * Creates a new LabelReorderingAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.LabelReorderingAction
             * @static
             * @param {proto.SyncActionValue.ILabelReorderingAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.LabelReorderingAction} LabelReorderingAction instance
             */
            LabelReorderingAction.create = function create(properties) {
                return new LabelReorderingAction(properties);
            };

            /**
             * Encodes the specified LabelReorderingAction message. Does not implicitly {@link proto.SyncActionValue.LabelReorderingAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.LabelReorderingAction
             * @static
             * @param {proto.SyncActionValue.ILabelReorderingAction} message LabelReorderingAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LabelReorderingAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.sortedLabelIds != null && message.sortedLabelIds.length) {
                    writer.uint32(/* id 1, wireType 2 =*/10).fork();
                    for (var i = 0; i < message.sortedLabelIds.length; ++i)
                        writer.int32(message.sortedLabelIds[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LabelReorderingAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.LabelReorderingAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.LabelReorderingAction
             * @static
             * @param {proto.SyncActionValue.ILabelReorderingAction} message LabelReorderingAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LabelReorderingAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a LabelReorderingAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.LabelReorderingAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.LabelReorderingAction} LabelReorderingAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LabelReorderingAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u === 2) {
                                if (!(message.sortedLabelIds && message.sortedLabelIds.length))
                                    message.sortedLabelIds = [];
                                var end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.sortedLabelIds.push(reader.int32());
                                continue;
                            }
                            if (u !== 0)
                                break;
                            if (!(message.sortedLabelIds && message.sortedLabelIds.length))
                                message.sortedLabelIds = [];
                            message.sortedLabelIds.push(reader.int32());
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a LabelReorderingAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.LabelReorderingAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.LabelReorderingAction} LabelReorderingAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LabelReorderingAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LabelReorderingAction message.
             * @function verify
             * @memberof proto.SyncActionValue.LabelReorderingAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LabelReorderingAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.sortedLabelIds != null && Object.hasOwnProperty.call(message, "sortedLabelIds")) {
                    if (!Array.isArray(message.sortedLabelIds))
                        return "sortedLabelIds: array expected";
                    for (var i = 0; i < message.sortedLabelIds.length; ++i)
                        if (!$util.isInteger(message.sortedLabelIds[i]))
                            return "sortedLabelIds: integer[] expected";
                }
                return null;
            };

            /**
             * Creates a LabelReorderingAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.LabelReorderingAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.LabelReorderingAction} LabelReorderingAction
             */
            LabelReorderingAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.LabelReorderingAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.sortedLabelIds) {
                    if (!Array.isArray(object.sortedLabelIds))
                        throw TypeError(".proto.SyncActionValue.LabelReorderingAction.sortedLabelIds: array expected");
                    message.sortedLabelIds = Array(object.sortedLabelIds.length);
                    for (var i = 0; i < object.sortedLabelIds.length; ++i)
                        message.sortedLabelIds[i] = object.sortedLabelIds[i] | 0;
                }
                return message;
            };

            /**
             * Creates a plain object from a LabelReorderingAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.LabelReorderingAction
             * @static
             * @param {proto.SyncActionValue.LabelReorderingAction} message LabelReorderingAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LabelReorderingAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.sortedLabelIds = [];
                if (message.sortedLabelIds && message.sortedLabelIds.length) {
                    object.sortedLabelIds = Array(message.sortedLabelIds.length);
                    for (var j = 0; j < message.sortedLabelIds.length; ++j)
                        object.sortedLabelIds[j] = message.sortedLabelIds[j];
                }
                return object;
            };

            /**
             * Converts this LabelReorderingAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.LabelReorderingAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LabelReorderingAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for LabelReorderingAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.LabelReorderingAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            LabelReorderingAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.LabelReorderingAction";
            };

            return LabelReorderingAction;
        })();

        SyncActionValue.LidContactAction = (function() {

            /**
             * Properties of a LidContactAction.
             * @memberof proto.SyncActionValue
             * @interface ILidContactAction
             * @property {string|null} [fullName] LidContactAction fullName
             * @property {string|null} [firstName] LidContactAction firstName
             * @property {string|null} [username] LidContactAction username
             */

            /**
             * Constructs a new LidContactAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a LidContactAction.
             * @implements ILidContactAction
             * @constructor
             * @param {proto.SyncActionValue.ILidContactAction=} [properties] Properties to set
             */
            function LidContactAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * LidContactAction fullName.
             * @member {string|null|undefined} fullName
             * @memberof proto.SyncActionValue.LidContactAction
             * @instance
             */
            LidContactAction.prototype.fullName = null;

            /**
             * LidContactAction firstName.
             * @member {string|null|undefined} firstName
             * @memberof proto.SyncActionValue.LidContactAction
             * @instance
             */
            LidContactAction.prototype.firstName = null;

            /**
             * LidContactAction username.
             * @member {string|null|undefined} username
             * @memberof proto.SyncActionValue.LidContactAction
             * @instance
             */
            LidContactAction.prototype.username = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LidContactAction.prototype, "_fullName", {
                get: $util.oneOfGetter($oneOfFields = ["fullName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LidContactAction.prototype, "_firstName", {
                get: $util.oneOfGetter($oneOfFields = ["firstName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LidContactAction.prototype, "_username", {
                get: $util.oneOfGetter($oneOfFields = ["username"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new LidContactAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.LidContactAction
             * @static
             * @param {proto.SyncActionValue.ILidContactAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.LidContactAction} LidContactAction instance
             */
            LidContactAction.create = function create(properties) {
                return new LidContactAction(properties);
            };

            /**
             * Encodes the specified LidContactAction message. Does not implicitly {@link proto.SyncActionValue.LidContactAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.LidContactAction
             * @static
             * @param {proto.SyncActionValue.ILidContactAction} message LidContactAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LidContactAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.fullName != null && Object.hasOwnProperty.call(message, "fullName"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.fullName);
                if (message.firstName != null && Object.hasOwnProperty.call(message, "firstName"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.firstName);
                if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.username);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LidContactAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.LidContactAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.LidContactAction
             * @static
             * @param {proto.SyncActionValue.ILidContactAction} message LidContactAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LidContactAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a LidContactAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.LidContactAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.LidContactAction} LidContactAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LidContactAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.fullName = reader.stringVerify();
                            message._fullName = "fullName";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.firstName = reader.stringVerify();
                            message._firstName = "firstName";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.username = reader.stringVerify();
                            message._username = "username";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a LidContactAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.LidContactAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.LidContactAction} LidContactAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LidContactAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LidContactAction message.
             * @function verify
             * @memberof proto.SyncActionValue.LidContactAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LidContactAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.fullName != null && Object.hasOwnProperty.call(message, "fullName")) {
                    properties._fullName = 1;
                    if (!$util.isString(message.fullName))
                        return "fullName: string expected";
                }
                if (message.firstName != null && Object.hasOwnProperty.call(message, "firstName")) {
                    properties._firstName = 1;
                    if (!$util.isString(message.firstName))
                        return "firstName: string expected";
                }
                if (message.username != null && Object.hasOwnProperty.call(message, "username")) {
                    properties._username = 1;
                    if (!$util.isString(message.username))
                        return "username: string expected";
                }
                return null;
            };

            /**
             * Creates a LidContactAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.LidContactAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.LidContactAction} LidContactAction
             */
            LidContactAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.LidContactAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.fullName != null)
                    message.fullName = String(object.fullName);
                if (object.firstName != null)
                    message.firstName = String(object.firstName);
                if (object.username != null)
                    message.username = String(object.username);
                return message;
            };

            /**
             * Creates a plain object from a LidContactAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.LidContactAction
             * @static
             * @param {proto.SyncActionValue.LidContactAction} message LidContactAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LidContactAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.fullName != null && Object.hasOwnProperty.call(message, "fullName"))
                    object.fullName = message.fullName;
                if (message.firstName != null && Object.hasOwnProperty.call(message, "firstName"))
                    object.firstName = message.firstName;
                if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                    object.username = message.username;
                return object;
            };

            /**
             * Converts this LidContactAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.LidContactAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LidContactAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for LidContactAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.LidContactAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            LidContactAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.LidContactAction";
            };

            return LidContactAction;
        })();

        SyncActionValue.LocaleSetting = (function() {

            /**
             * Properties of a LocaleSetting.
             * @memberof proto.SyncActionValue
             * @interface ILocaleSetting
             * @property {string|null} [locale] LocaleSetting locale
             */

            /**
             * Constructs a new LocaleSetting.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a LocaleSetting.
             * @implements ILocaleSetting
             * @constructor
             * @param {proto.SyncActionValue.ILocaleSetting=} [properties] Properties to set
             */
            function LocaleSetting(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * LocaleSetting locale.
             * @member {string|null|undefined} locale
             * @memberof proto.SyncActionValue.LocaleSetting
             * @instance
             */
            LocaleSetting.prototype.locale = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LocaleSetting.prototype, "_locale", {
                get: $util.oneOfGetter($oneOfFields = ["locale"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new LocaleSetting instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.LocaleSetting
             * @static
             * @param {proto.SyncActionValue.ILocaleSetting=} [properties] Properties to set
             * @returns {proto.SyncActionValue.LocaleSetting} LocaleSetting instance
             */
            LocaleSetting.create = function create(properties) {
                return new LocaleSetting(properties);
            };

            /**
             * Encodes the specified LocaleSetting message. Does not implicitly {@link proto.SyncActionValue.LocaleSetting.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.LocaleSetting
             * @static
             * @param {proto.SyncActionValue.ILocaleSetting} message LocaleSetting message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LocaleSetting.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.locale != null && Object.hasOwnProperty.call(message, "locale"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.locale);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LocaleSetting message, length delimited. Does not implicitly {@link proto.SyncActionValue.LocaleSetting.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.LocaleSetting
             * @static
             * @param {proto.SyncActionValue.ILocaleSetting} message LocaleSetting message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LocaleSetting.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a LocaleSetting message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.LocaleSetting
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.LocaleSetting} LocaleSetting
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LocaleSetting.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.locale = reader.stringVerify();
                            message._locale = "locale";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a LocaleSetting message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.LocaleSetting
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.LocaleSetting} LocaleSetting
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LocaleSetting.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LocaleSetting message.
             * @function verify
             * @memberof proto.SyncActionValue.LocaleSetting
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LocaleSetting.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.locale != null && Object.hasOwnProperty.call(message, "locale")) {
                    properties._locale = 1;
                    if (!$util.isString(message.locale))
                        return "locale: string expected";
                }
                return null;
            };

            /**
             * Creates a LocaleSetting message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.LocaleSetting
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.LocaleSetting} LocaleSetting
             */
            LocaleSetting.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.LocaleSetting: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.locale != null)
                    message.locale = String(object.locale);
                return message;
            };

            /**
             * Creates a plain object from a LocaleSetting message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.LocaleSetting
             * @static
             * @param {proto.SyncActionValue.LocaleSetting} message LocaleSetting
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LocaleSetting.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.locale != null && Object.hasOwnProperty.call(message, "locale"))
                    object.locale = message.locale;
                return object;
            };

            /**
             * Converts this LocaleSetting to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.LocaleSetting
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LocaleSetting.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for LocaleSetting
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.LocaleSetting
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            LocaleSetting.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.LocaleSetting";
            };

            return LocaleSetting;
        })();

        SyncActionValue.LockChatAction = (function() {

            /**
             * Properties of a LockChatAction.
             * @memberof proto.SyncActionValue
             * @interface ILockChatAction
             * @property {boolean|null} [locked] LockChatAction locked
             */

            /**
             * Constructs a new LockChatAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a LockChatAction.
             * @implements ILockChatAction
             * @constructor
             * @param {proto.SyncActionValue.ILockChatAction=} [properties] Properties to set
             */
            function LockChatAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * LockChatAction locked.
             * @member {boolean|null|undefined} locked
             * @memberof proto.SyncActionValue.LockChatAction
             * @instance
             */
            LockChatAction.prototype.locked = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(LockChatAction.prototype, "_locked", {
                get: $util.oneOfGetter($oneOfFields = ["locked"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new LockChatAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.LockChatAction
             * @static
             * @param {proto.SyncActionValue.ILockChatAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.LockChatAction} LockChatAction instance
             */
            LockChatAction.create = function create(properties) {
                return new LockChatAction(properties);
            };

            /**
             * Encodes the specified LockChatAction message. Does not implicitly {@link proto.SyncActionValue.LockChatAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.LockChatAction
             * @static
             * @param {proto.SyncActionValue.ILockChatAction} message LockChatAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LockChatAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.locked != null && Object.hasOwnProperty.call(message, "locked"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.locked);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified LockChatAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.LockChatAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.LockChatAction
             * @static
             * @param {proto.SyncActionValue.ILockChatAction} message LockChatAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LockChatAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a LockChatAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.LockChatAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.LockChatAction} LockChatAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LockChatAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.locked = reader.bool();
                            message._locked = "locked";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a LockChatAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.LockChatAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.LockChatAction} LockChatAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LockChatAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LockChatAction message.
             * @function verify
             * @memberof proto.SyncActionValue.LockChatAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LockChatAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.locked != null && Object.hasOwnProperty.call(message, "locked")) {
                    properties._locked = 1;
                    if (typeof message.locked !== "boolean")
                        return "locked: boolean expected";
                }
                return null;
            };

            /**
             * Creates a LockChatAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.LockChatAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.LockChatAction} LockChatAction
             */
            LockChatAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.LockChatAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.locked != null)
                    message.locked = Boolean(object.locked);
                return message;
            };

            /**
             * Creates a plain object from a LockChatAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.LockChatAction
             * @static
             * @param {proto.SyncActionValue.LockChatAction} message LockChatAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LockChatAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.locked != null && Object.hasOwnProperty.call(message, "locked"))
                    object.locked = message.locked;
                return object;
            };

            /**
             * Converts this LockChatAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.LockChatAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LockChatAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for LockChatAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.LockChatAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            LockChatAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.LockChatAction";
            };

            return LockChatAction;
        })();

        SyncActionValue.MaibaAIFeaturesControlAction = (function() {

            /**
             * Properties of a MaibaAIFeaturesControlAction.
             * @memberof proto.SyncActionValue
             * @interface IMaibaAIFeaturesControlAction
             * @property {proto.SyncActionValue.MaibaAIFeaturesControlAction.MaibaAIFeatureStatus|null} [aiFeatureStatus] MaibaAIFeaturesControlAction aiFeatureStatus
             * @property {proto.SyncActionValue.MaibaAIFeaturesControlAction.MaibaAIReplyMode|null} [aiReplyMode] MaibaAIFeaturesControlAction aiReplyMode
             */

            /**
             * Constructs a new MaibaAIFeaturesControlAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a MaibaAIFeaturesControlAction.
             * @implements IMaibaAIFeaturesControlAction
             * @constructor
             * @param {proto.SyncActionValue.IMaibaAIFeaturesControlAction=} [properties] Properties to set
             */
            function MaibaAIFeaturesControlAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MaibaAIFeaturesControlAction aiFeatureStatus.
             * @member {proto.SyncActionValue.MaibaAIFeaturesControlAction.MaibaAIFeatureStatus|null|undefined} aiFeatureStatus
             * @memberof proto.SyncActionValue.MaibaAIFeaturesControlAction
             * @instance
             */
            MaibaAIFeaturesControlAction.prototype.aiFeatureStatus = null;

            /**
             * MaibaAIFeaturesControlAction aiReplyMode.
             * @member {proto.SyncActionValue.MaibaAIFeaturesControlAction.MaibaAIReplyMode|null|undefined} aiReplyMode
             * @memberof proto.SyncActionValue.MaibaAIFeaturesControlAction
             * @instance
             */
            MaibaAIFeaturesControlAction.prototype.aiReplyMode = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MaibaAIFeaturesControlAction.prototype, "_aiFeatureStatus", {
                get: $util.oneOfGetter($oneOfFields = ["aiFeatureStatus"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MaibaAIFeaturesControlAction.prototype, "_aiReplyMode", {
                get: $util.oneOfGetter($oneOfFields = ["aiReplyMode"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new MaibaAIFeaturesControlAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.MaibaAIFeaturesControlAction
             * @static
             * @param {proto.SyncActionValue.IMaibaAIFeaturesControlAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.MaibaAIFeaturesControlAction} MaibaAIFeaturesControlAction instance
             */
            MaibaAIFeaturesControlAction.create = function create(properties) {
                return new MaibaAIFeaturesControlAction(properties);
            };

            /**
             * Encodes the specified MaibaAIFeaturesControlAction message. Does not implicitly {@link proto.SyncActionValue.MaibaAIFeaturesControlAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.MaibaAIFeaturesControlAction
             * @static
             * @param {proto.SyncActionValue.IMaibaAIFeaturesControlAction} message MaibaAIFeaturesControlAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MaibaAIFeaturesControlAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.aiFeatureStatus != null && Object.hasOwnProperty.call(message, "aiFeatureStatus"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.aiFeatureStatus);
                if (message.aiReplyMode != null && Object.hasOwnProperty.call(message, "aiReplyMode"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.aiReplyMode);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MaibaAIFeaturesControlAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.MaibaAIFeaturesControlAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.MaibaAIFeaturesControlAction
             * @static
             * @param {proto.SyncActionValue.IMaibaAIFeaturesControlAction} message MaibaAIFeaturesControlAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MaibaAIFeaturesControlAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a MaibaAIFeaturesControlAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.MaibaAIFeaturesControlAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.MaibaAIFeaturesControlAction} MaibaAIFeaturesControlAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MaibaAIFeaturesControlAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.aiFeatureStatus = reader.int32();
                            message._aiFeatureStatus = "aiFeatureStatus";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.aiReplyMode = reader.int32();
                            message._aiReplyMode = "aiReplyMode";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a MaibaAIFeaturesControlAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.MaibaAIFeaturesControlAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.MaibaAIFeaturesControlAction} MaibaAIFeaturesControlAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MaibaAIFeaturesControlAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MaibaAIFeaturesControlAction message.
             * @function verify
             * @memberof proto.SyncActionValue.MaibaAIFeaturesControlAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MaibaAIFeaturesControlAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.aiFeatureStatus != null && Object.hasOwnProperty.call(message, "aiFeatureStatus")) {
                    properties._aiFeatureStatus = 1;
                    if (typeof message.aiFeatureStatus !== "number" || (message.aiFeatureStatus | 0) !== message.aiFeatureStatus)
                        return "aiFeatureStatus: enum value expected";
                }
                if (message.aiReplyMode != null && Object.hasOwnProperty.call(message, "aiReplyMode")) {
                    properties._aiReplyMode = 1;
                    if (typeof message.aiReplyMode !== "number" || (message.aiReplyMode | 0) !== message.aiReplyMode)
                        return "aiReplyMode: enum value expected";
                }
                return null;
            };

            /**
             * Creates a MaibaAIFeaturesControlAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.MaibaAIFeaturesControlAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.MaibaAIFeaturesControlAction} MaibaAIFeaturesControlAction
             */
            MaibaAIFeaturesControlAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.MaibaAIFeaturesControlAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.aiFeatureStatus) {
                case "ENABLED":
                case 0:
                    message.aiFeatureStatus = 0;
                    break;
                case "ENABLED_HAS_LEARNING":
                case 1:
                    message.aiFeatureStatus = 1;
                    break;
                case "DISABLED":
                case 2:
                    message.aiFeatureStatus = 2;
                    break;
                default:
                    if (typeof object.aiFeatureStatus === "number" && (object.aiFeatureStatus | 0) === object.aiFeatureStatus)
                        message.aiFeatureStatus = object.aiFeatureStatus;
                }
                switch (object.aiReplyMode) {
                case "MUTED":
                case 0:
                    message.aiReplyMode = 0;
                    break;
                case "AI_AGENT":
                case 1:
                    message.aiReplyMode = 1;
                    break;
                case "SUGGESTIONS":
                case 2:
                    message.aiReplyMode = 2;
                    break;
                default:
                    if (typeof object.aiReplyMode === "number" && (object.aiReplyMode | 0) === object.aiReplyMode)
                        message.aiReplyMode = object.aiReplyMode;
                }
                return message;
            };

            /**
             * Creates a plain object from a MaibaAIFeaturesControlAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.MaibaAIFeaturesControlAction
             * @static
             * @param {proto.SyncActionValue.MaibaAIFeaturesControlAction} message MaibaAIFeaturesControlAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MaibaAIFeaturesControlAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.aiFeatureStatus != null && Object.hasOwnProperty.call(message, "aiFeatureStatus"))
                    object.aiFeatureStatus = options.enums === String ? $root.proto.SyncActionValue.MaibaAIFeaturesControlAction.MaibaAIFeatureStatus[message.aiFeatureStatus] === undefined ? message.aiFeatureStatus : $root.proto.SyncActionValue.MaibaAIFeaturesControlAction.MaibaAIFeatureStatus[message.aiFeatureStatus] : message.aiFeatureStatus;
                if (message.aiReplyMode != null && Object.hasOwnProperty.call(message, "aiReplyMode"))
                    object.aiReplyMode = options.enums === String ? $root.proto.SyncActionValue.MaibaAIFeaturesControlAction.MaibaAIReplyMode[message.aiReplyMode] === undefined ? message.aiReplyMode : $root.proto.SyncActionValue.MaibaAIFeaturesControlAction.MaibaAIReplyMode[message.aiReplyMode] : message.aiReplyMode;
                return object;
            };

            /**
             * Converts this MaibaAIFeaturesControlAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.MaibaAIFeaturesControlAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MaibaAIFeaturesControlAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MaibaAIFeaturesControlAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.MaibaAIFeaturesControlAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MaibaAIFeaturesControlAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.MaibaAIFeaturesControlAction";
            };

            /**
             * MaibaAIFeatureStatus enum.
             * @name proto.SyncActionValue.MaibaAIFeaturesControlAction.MaibaAIFeatureStatus
             * @enum {number}
             * @property {number} ENABLED=0 ENABLED value
             * @property {number} ENABLED_HAS_LEARNING=1 ENABLED_HAS_LEARNING value
             * @property {number} DISABLED=2 DISABLED value
             */
            MaibaAIFeaturesControlAction.MaibaAIFeatureStatus = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "ENABLED"] = 0;
                values[valuesById[1] = "ENABLED_HAS_LEARNING"] = 1;
                values[valuesById[2] = "DISABLED"] = 2;
                return values;
            })();

            /**
             * MaibaAIReplyMode enum.
             * @name proto.SyncActionValue.MaibaAIFeaturesControlAction.MaibaAIReplyMode
             * @enum {number}
             * @property {number} MUTED=0 MUTED value
             * @property {number} AI_AGENT=1 AI_AGENT value
             * @property {number} SUGGESTIONS=2 SUGGESTIONS value
             */
            MaibaAIFeaturesControlAction.MaibaAIReplyMode = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "MUTED"] = 0;
                values[valuesById[1] = "AI_AGENT"] = 1;
                values[valuesById[2] = "SUGGESTIONS"] = 2;
                return values;
            })();

            return MaibaAIFeaturesControlAction;
        })();

        SyncActionValue.MarkChatAsReadAction = (function() {

            /**
             * Properties of a MarkChatAsReadAction.
             * @memberof proto.SyncActionValue
             * @interface IMarkChatAsReadAction
             * @property {boolean|null} [read] MarkChatAsReadAction read
             * @property {proto.SyncActionValue.ISyncActionMessageRange|null} [messageRange] MarkChatAsReadAction messageRange
             */

            /**
             * Constructs a new MarkChatAsReadAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a MarkChatAsReadAction.
             * @implements IMarkChatAsReadAction
             * @constructor
             * @param {proto.SyncActionValue.IMarkChatAsReadAction=} [properties] Properties to set
             */
            function MarkChatAsReadAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MarkChatAsReadAction read.
             * @member {boolean|null|undefined} read
             * @memberof proto.SyncActionValue.MarkChatAsReadAction
             * @instance
             */
            MarkChatAsReadAction.prototype.read = null;

            /**
             * MarkChatAsReadAction messageRange.
             * @member {proto.SyncActionValue.ISyncActionMessageRange|null|undefined} messageRange
             * @memberof proto.SyncActionValue.MarkChatAsReadAction
             * @instance
             */
            MarkChatAsReadAction.prototype.messageRange = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MarkChatAsReadAction.prototype, "_read", {
                get: $util.oneOfGetter($oneOfFields = ["read"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MarkChatAsReadAction.prototype, "_messageRange", {
                get: $util.oneOfGetter($oneOfFields = ["messageRange"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new MarkChatAsReadAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.MarkChatAsReadAction
             * @static
             * @param {proto.SyncActionValue.IMarkChatAsReadAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.MarkChatAsReadAction} MarkChatAsReadAction instance
             */
            MarkChatAsReadAction.create = function create(properties) {
                return new MarkChatAsReadAction(properties);
            };

            /**
             * Encodes the specified MarkChatAsReadAction message. Does not implicitly {@link proto.SyncActionValue.MarkChatAsReadAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.MarkChatAsReadAction
             * @static
             * @param {proto.SyncActionValue.IMarkChatAsReadAction} message MarkChatAsReadAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MarkChatAsReadAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.read != null && Object.hasOwnProperty.call(message, "read"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.read);
                if (message.messageRange != null && Object.hasOwnProperty.call(message, "messageRange"))
                    $root.proto.SyncActionValue.SyncActionMessageRange.encode(message.messageRange, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MarkChatAsReadAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.MarkChatAsReadAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.MarkChatAsReadAction
             * @static
             * @param {proto.SyncActionValue.IMarkChatAsReadAction} message MarkChatAsReadAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MarkChatAsReadAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a MarkChatAsReadAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.MarkChatAsReadAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.MarkChatAsReadAction} MarkChatAsReadAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MarkChatAsReadAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.read = reader.bool();
                            message._read = "read";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.decode(reader, reader.uint32(), undefined, q + 1, message.messageRange);
                            message._messageRange = "messageRange";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a MarkChatAsReadAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.MarkChatAsReadAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.MarkChatAsReadAction} MarkChatAsReadAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MarkChatAsReadAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MarkChatAsReadAction message.
             * @function verify
             * @memberof proto.SyncActionValue.MarkChatAsReadAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MarkChatAsReadAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.read != null && Object.hasOwnProperty.call(message, "read")) {
                    properties._read = 1;
                    if (typeof message.read !== "boolean")
                        return "read: boolean expected";
                }
                if (message.messageRange != null && Object.hasOwnProperty.call(message, "messageRange")) {
                    properties._messageRange = 1;
                    {
                        var error = $root.proto.SyncActionValue.SyncActionMessageRange.verify(message.messageRange, q + 1);
                        if (error)
                            return "messageRange." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a MarkChatAsReadAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.MarkChatAsReadAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.MarkChatAsReadAction} MarkChatAsReadAction
             */
            MarkChatAsReadAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.MarkChatAsReadAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.read != null)
                    message.read = Boolean(object.read);
                if (object.messageRange != null) {
                    if (!$util.isObject(object.messageRange))
                        throw TypeError(".proto.SyncActionValue.MarkChatAsReadAction.messageRange: object expected");
                    message.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.fromObject(object.messageRange, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a MarkChatAsReadAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.MarkChatAsReadAction
             * @static
             * @param {proto.SyncActionValue.MarkChatAsReadAction} message MarkChatAsReadAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MarkChatAsReadAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.read != null && Object.hasOwnProperty.call(message, "read"))
                    object.read = message.read;
                if (message.messageRange != null && Object.hasOwnProperty.call(message, "messageRange"))
                    object.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.toObject(message.messageRange, options, q + 1);
                return object;
            };

            /**
             * Converts this MarkChatAsReadAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.MarkChatAsReadAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MarkChatAsReadAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MarkChatAsReadAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.MarkChatAsReadAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MarkChatAsReadAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.MarkChatAsReadAction";
            };

            return MarkChatAsReadAction;
        })();

        SyncActionValue.MarketingMessageAction = (function() {

            /**
             * Properties of a MarketingMessageAction.
             * @memberof proto.SyncActionValue
             * @interface IMarketingMessageAction
             * @property {string|null} [name] MarketingMessageAction name
             * @property {string|null} [message] MarketingMessageAction message
             * @property {proto.SyncActionValue.MarketingMessageAction.MarketingMessagePrototypeType|null} [type] MarketingMessageAction type
             * @property {number|Long|null} [createdAt] MarketingMessageAction createdAt
             * @property {number|Long|null} [lastSentAt] MarketingMessageAction lastSentAt
             * @property {boolean|null} [isDeleted] MarketingMessageAction isDeleted
             * @property {string|null} [mediaId] MarketingMessageAction mediaId
             */

            /**
             * Constructs a new MarketingMessageAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a MarketingMessageAction.
             * @implements IMarketingMessageAction
             * @constructor
             * @param {proto.SyncActionValue.IMarketingMessageAction=} [properties] Properties to set
             */
            function MarketingMessageAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MarketingMessageAction name.
             * @member {string|null|undefined} name
             * @memberof proto.SyncActionValue.MarketingMessageAction
             * @instance
             */
            MarketingMessageAction.prototype.name = null;

            /**
             * MarketingMessageAction message.
             * @member {string|null|undefined} message
             * @memberof proto.SyncActionValue.MarketingMessageAction
             * @instance
             */
            MarketingMessageAction.prototype.message = null;

            /**
             * MarketingMessageAction type.
             * @member {proto.SyncActionValue.MarketingMessageAction.MarketingMessagePrototypeType|null|undefined} type
             * @memberof proto.SyncActionValue.MarketingMessageAction
             * @instance
             */
            MarketingMessageAction.prototype.type = null;

            /**
             * MarketingMessageAction createdAt.
             * @member {number|Long|null|undefined} createdAt
             * @memberof proto.SyncActionValue.MarketingMessageAction
             * @instance
             */
            MarketingMessageAction.prototype.createdAt = null;

            /**
             * MarketingMessageAction lastSentAt.
             * @member {number|Long|null|undefined} lastSentAt
             * @memberof proto.SyncActionValue.MarketingMessageAction
             * @instance
             */
            MarketingMessageAction.prototype.lastSentAt = null;

            /**
             * MarketingMessageAction isDeleted.
             * @member {boolean|null|undefined} isDeleted
             * @memberof proto.SyncActionValue.MarketingMessageAction
             * @instance
             */
            MarketingMessageAction.prototype.isDeleted = null;

            /**
             * MarketingMessageAction mediaId.
             * @member {string|null|undefined} mediaId
             * @memberof proto.SyncActionValue.MarketingMessageAction
             * @instance
             */
            MarketingMessageAction.prototype.mediaId = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MarketingMessageAction.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MarketingMessageAction.prototype, "_message", {
                get: $util.oneOfGetter($oneOfFields = ["message"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MarketingMessageAction.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MarketingMessageAction.prototype, "_createdAt", {
                get: $util.oneOfGetter($oneOfFields = ["createdAt"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MarketingMessageAction.prototype, "_lastSentAt", {
                get: $util.oneOfGetter($oneOfFields = ["lastSentAt"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MarketingMessageAction.prototype, "_isDeleted", {
                get: $util.oneOfGetter($oneOfFields = ["isDeleted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MarketingMessageAction.prototype, "_mediaId", {
                get: $util.oneOfGetter($oneOfFields = ["mediaId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new MarketingMessageAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.MarketingMessageAction
             * @static
             * @param {proto.SyncActionValue.IMarketingMessageAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.MarketingMessageAction} MarketingMessageAction instance
             */
            MarketingMessageAction.create = function create(properties) {
                return new MarketingMessageAction(properties);
            };

            /**
             * Encodes the specified MarketingMessageAction message. Does not implicitly {@link proto.SyncActionValue.MarketingMessageAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.MarketingMessageAction
             * @static
             * @param {proto.SyncActionValue.IMarketingMessageAction} message MarketingMessageAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MarketingMessageAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.message);
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int32(message.type);
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int64(message.createdAt);
                if (message.lastSentAt != null && Object.hasOwnProperty.call(message, "lastSentAt"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int64(message.lastSentAt);
                if (message.isDeleted != null && Object.hasOwnProperty.call(message, "isDeleted"))
                    writer.uint32(/* id 6, wireType 0 =*/48).bool(message.isDeleted);
                if (message.mediaId != null && Object.hasOwnProperty.call(message, "mediaId"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.mediaId);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MarketingMessageAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.MarketingMessageAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.MarketingMessageAction
             * @static
             * @param {proto.SyncActionValue.IMarketingMessageAction} message MarketingMessageAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MarketingMessageAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a MarketingMessageAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.MarketingMessageAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.MarketingMessageAction} MarketingMessageAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MarketingMessageAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.message = reader.stringVerify();
                            message._message = "message";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.type = reader.int32();
                            message._type = "type";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.createdAt = reader.int64();
                            message._createdAt = "createdAt";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.lastSentAt = reader.int64();
                            message._lastSentAt = "lastSentAt";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.isDeleted = reader.bool();
                            message._isDeleted = "isDeleted";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.mediaId = reader.stringVerify();
                            message._mediaId = "mediaId";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a MarketingMessageAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.MarketingMessageAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.MarketingMessageAction} MarketingMessageAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MarketingMessageAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MarketingMessageAction message.
             * @function verify
             * @memberof proto.SyncActionValue.MarketingMessageAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MarketingMessageAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.name != null && Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                if (message.message != null && Object.hasOwnProperty.call(message, "message")) {
                    properties._message = 1;
                    if (!$util.isString(message.message))
                        return "message: string expected";
                }
                if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                    properties._type = 1;
                    if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                        return "type: enum value expected";
                }
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt")) {
                    properties._createdAt = 1;
                    if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
                        return "createdAt: integer|Long expected";
                }
                if (message.lastSentAt != null && Object.hasOwnProperty.call(message, "lastSentAt")) {
                    properties._lastSentAt = 1;
                    if (!$util.isInteger(message.lastSentAt) && !(message.lastSentAt && $util.isInteger(message.lastSentAt.low) && $util.isInteger(message.lastSentAt.high)))
                        return "lastSentAt: integer|Long expected";
                }
                if (message.isDeleted != null && Object.hasOwnProperty.call(message, "isDeleted")) {
                    properties._isDeleted = 1;
                    if (typeof message.isDeleted !== "boolean")
                        return "isDeleted: boolean expected";
                }
                if (message.mediaId != null && Object.hasOwnProperty.call(message, "mediaId")) {
                    properties._mediaId = 1;
                    if (!$util.isString(message.mediaId))
                        return "mediaId: string expected";
                }
                return null;
            };

            /**
             * Creates a MarketingMessageAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.MarketingMessageAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.MarketingMessageAction} MarketingMessageAction
             */
            MarketingMessageAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.MarketingMessageAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.name != null)
                    message.name = String(object.name);
                if (object.message != null)
                    message.message = String(object.message);
                switch (object.type) {
                case "PERSONALIZED":
                case 0:
                    message.type = 0;
                    break;
                default:
                    if (typeof object.type === "number" && (object.type | 0) === object.type)
                        message.type = object.type;
                }
                if (object.createdAt != null)
                    if ($util.Long)
                        message.createdAt = $util.Long.fromValue(object.createdAt, false);
                    else if (typeof object.createdAt === "string")
                        message.createdAt = parseInt(object.createdAt, 10);
                    else if (typeof object.createdAt === "number")
                        message.createdAt = object.createdAt;
                    else if (typeof object.createdAt === "object")
                        message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
                if (object.lastSentAt != null)
                    if ($util.Long)
                        message.lastSentAt = $util.Long.fromValue(object.lastSentAt, false);
                    else if (typeof object.lastSentAt === "string")
                        message.lastSentAt = parseInt(object.lastSentAt, 10);
                    else if (typeof object.lastSentAt === "number")
                        message.lastSentAt = object.lastSentAt;
                    else if (typeof object.lastSentAt === "object")
                        message.lastSentAt = new $util.LongBits(object.lastSentAt.low >>> 0, object.lastSentAt.high >>> 0).toNumber();
                if (object.isDeleted != null)
                    message.isDeleted = Boolean(object.isDeleted);
                if (object.mediaId != null)
                    message.mediaId = String(object.mediaId);
                return message;
            };

            /**
             * Creates a plain object from a MarketingMessageAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.MarketingMessageAction
             * @static
             * @param {proto.SyncActionValue.MarketingMessageAction} message MarketingMessageAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MarketingMessageAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                    object.message = message.message;
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    object.type = options.enums === String ? $root.proto.SyncActionValue.MarketingMessageAction.MarketingMessagePrototypeType[message.type] === undefined ? message.type : $root.proto.SyncActionValue.MarketingMessageAction.MarketingMessagePrototypeType[message.type] : message.type;
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.createdAt = typeof message.createdAt === "number" ? BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
                    else if (typeof message.createdAt === "number")
                        object.createdAt = options.longs === String ? String(message.createdAt) : message.createdAt;
                    else
                        object.createdAt = options.longs === String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
                if (message.lastSentAt != null && Object.hasOwnProperty.call(message, "lastSentAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.lastSentAt = typeof message.lastSentAt === "number" ? BigInt(message.lastSentAt) : $util.Long.fromBits(message.lastSentAt.low >>> 0, message.lastSentAt.high >>> 0, false).toBigInt();
                    else if (typeof message.lastSentAt === "number")
                        object.lastSentAt = options.longs === String ? String(message.lastSentAt) : message.lastSentAt;
                    else
                        object.lastSentAt = options.longs === String ? $util.Long.prototype.toString.call(message.lastSentAt) : options.longs === Number ? new $util.LongBits(message.lastSentAt.low >>> 0, message.lastSentAt.high >>> 0).toNumber() : message.lastSentAt;
                if (message.isDeleted != null && Object.hasOwnProperty.call(message, "isDeleted"))
                    object.isDeleted = message.isDeleted;
                if (message.mediaId != null && Object.hasOwnProperty.call(message, "mediaId"))
                    object.mediaId = message.mediaId;
                return object;
            };

            /**
             * Converts this MarketingMessageAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.MarketingMessageAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MarketingMessageAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MarketingMessageAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.MarketingMessageAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MarketingMessageAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.MarketingMessageAction";
            };

            /**
             * MarketingMessagePrototypeType enum.
             * @name proto.SyncActionValue.MarketingMessageAction.MarketingMessagePrototypeType
             * @enum {number}
             * @property {number} PERSONALIZED=0 PERSONALIZED value
             */
            MarketingMessageAction.MarketingMessagePrototypeType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "PERSONALIZED"] = 0;
                return values;
            })();

            return MarketingMessageAction;
        })();

        SyncActionValue.MarketingMessageBroadcastAction = (function() {

            /**
             * Properties of a MarketingMessageBroadcastAction.
             * @memberof proto.SyncActionValue
             * @interface IMarketingMessageBroadcastAction
             * @property {number|null} [repliedCount] MarketingMessageBroadcastAction repliedCount
             */

            /**
             * Constructs a new MarketingMessageBroadcastAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a MarketingMessageBroadcastAction.
             * @implements IMarketingMessageBroadcastAction
             * @constructor
             * @param {proto.SyncActionValue.IMarketingMessageBroadcastAction=} [properties] Properties to set
             */
            function MarketingMessageBroadcastAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MarketingMessageBroadcastAction repliedCount.
             * @member {number|null|undefined} repliedCount
             * @memberof proto.SyncActionValue.MarketingMessageBroadcastAction
             * @instance
             */
            MarketingMessageBroadcastAction.prototype.repliedCount = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MarketingMessageBroadcastAction.prototype, "_repliedCount", {
                get: $util.oneOfGetter($oneOfFields = ["repliedCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new MarketingMessageBroadcastAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.MarketingMessageBroadcastAction
             * @static
             * @param {proto.SyncActionValue.IMarketingMessageBroadcastAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.MarketingMessageBroadcastAction} MarketingMessageBroadcastAction instance
             */
            MarketingMessageBroadcastAction.create = function create(properties) {
                return new MarketingMessageBroadcastAction(properties);
            };

            /**
             * Encodes the specified MarketingMessageBroadcastAction message. Does not implicitly {@link proto.SyncActionValue.MarketingMessageBroadcastAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.MarketingMessageBroadcastAction
             * @static
             * @param {proto.SyncActionValue.IMarketingMessageBroadcastAction} message MarketingMessageBroadcastAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MarketingMessageBroadcastAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.repliedCount != null && Object.hasOwnProperty.call(message, "repliedCount"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.repliedCount);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MarketingMessageBroadcastAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.MarketingMessageBroadcastAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.MarketingMessageBroadcastAction
             * @static
             * @param {proto.SyncActionValue.IMarketingMessageBroadcastAction} message MarketingMessageBroadcastAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MarketingMessageBroadcastAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a MarketingMessageBroadcastAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.MarketingMessageBroadcastAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.MarketingMessageBroadcastAction} MarketingMessageBroadcastAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MarketingMessageBroadcastAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.repliedCount = reader.int32();
                            message._repliedCount = "repliedCount";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a MarketingMessageBroadcastAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.MarketingMessageBroadcastAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.MarketingMessageBroadcastAction} MarketingMessageBroadcastAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MarketingMessageBroadcastAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MarketingMessageBroadcastAction message.
             * @function verify
             * @memberof proto.SyncActionValue.MarketingMessageBroadcastAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MarketingMessageBroadcastAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.repliedCount != null && Object.hasOwnProperty.call(message, "repliedCount")) {
                    properties._repliedCount = 1;
                    if (!$util.isInteger(message.repliedCount))
                        return "repliedCount: integer expected";
                }
                return null;
            };

            /**
             * Creates a MarketingMessageBroadcastAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.MarketingMessageBroadcastAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.MarketingMessageBroadcastAction} MarketingMessageBroadcastAction
             */
            MarketingMessageBroadcastAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.MarketingMessageBroadcastAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.repliedCount != null)
                    message.repliedCount = object.repliedCount | 0;
                return message;
            };

            /**
             * Creates a plain object from a MarketingMessageBroadcastAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.MarketingMessageBroadcastAction
             * @static
             * @param {proto.SyncActionValue.MarketingMessageBroadcastAction} message MarketingMessageBroadcastAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MarketingMessageBroadcastAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.repliedCount != null && Object.hasOwnProperty.call(message, "repliedCount"))
                    object.repliedCount = message.repliedCount;
                return object;
            };

            /**
             * Converts this MarketingMessageBroadcastAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.MarketingMessageBroadcastAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MarketingMessageBroadcastAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MarketingMessageBroadcastAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.MarketingMessageBroadcastAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MarketingMessageBroadcastAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.MarketingMessageBroadcastAction";
            };

            return MarketingMessageBroadcastAction;
        })();

        SyncActionValue.MerchantPaymentPartnerAction = (function() {

            /**
             * Properties of a MerchantPaymentPartnerAction.
             * @memberof proto.SyncActionValue
             * @interface IMerchantPaymentPartnerAction
             * @property {proto.SyncActionValue.MerchantPaymentPartnerAction.Status|null} [status] MerchantPaymentPartnerAction status
             * @property {string|null} [country] MerchantPaymentPartnerAction country
             * @property {string|null} [gatewayName] MerchantPaymentPartnerAction gatewayName
             * @property {string|null} [credentialId] MerchantPaymentPartnerAction credentialId
             */

            /**
             * Constructs a new MerchantPaymentPartnerAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a MerchantPaymentPartnerAction.
             * @implements IMerchantPaymentPartnerAction
             * @constructor
             * @param {proto.SyncActionValue.IMerchantPaymentPartnerAction=} [properties] Properties to set
             */
            function MerchantPaymentPartnerAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MerchantPaymentPartnerAction status.
             * @member {proto.SyncActionValue.MerchantPaymentPartnerAction.Status|null|undefined} status
             * @memberof proto.SyncActionValue.MerchantPaymentPartnerAction
             * @instance
             */
            MerchantPaymentPartnerAction.prototype.status = null;

            /**
             * MerchantPaymentPartnerAction country.
             * @member {string|null|undefined} country
             * @memberof proto.SyncActionValue.MerchantPaymentPartnerAction
             * @instance
             */
            MerchantPaymentPartnerAction.prototype.country = null;

            /**
             * MerchantPaymentPartnerAction gatewayName.
             * @member {string|null|undefined} gatewayName
             * @memberof proto.SyncActionValue.MerchantPaymentPartnerAction
             * @instance
             */
            MerchantPaymentPartnerAction.prototype.gatewayName = null;

            /**
             * MerchantPaymentPartnerAction credentialId.
             * @member {string|null|undefined} credentialId
             * @memberof proto.SyncActionValue.MerchantPaymentPartnerAction
             * @instance
             */
            MerchantPaymentPartnerAction.prototype.credentialId = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MerchantPaymentPartnerAction.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MerchantPaymentPartnerAction.prototype, "_country", {
                get: $util.oneOfGetter($oneOfFields = ["country"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MerchantPaymentPartnerAction.prototype, "_gatewayName", {
                get: $util.oneOfGetter($oneOfFields = ["gatewayName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MerchantPaymentPartnerAction.prototype, "_credentialId", {
                get: $util.oneOfGetter($oneOfFields = ["credentialId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new MerchantPaymentPartnerAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.MerchantPaymentPartnerAction
             * @static
             * @param {proto.SyncActionValue.IMerchantPaymentPartnerAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.MerchantPaymentPartnerAction} MerchantPaymentPartnerAction instance
             */
            MerchantPaymentPartnerAction.create = function create(properties) {
                return new MerchantPaymentPartnerAction(properties);
            };

            /**
             * Encodes the specified MerchantPaymentPartnerAction message. Does not implicitly {@link proto.SyncActionValue.MerchantPaymentPartnerAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.MerchantPaymentPartnerAction
             * @static
             * @param {proto.SyncActionValue.IMerchantPaymentPartnerAction} message MerchantPaymentPartnerAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MerchantPaymentPartnerAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.status);
                if (message.country != null && Object.hasOwnProperty.call(message, "country"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.country);
                if (message.gatewayName != null && Object.hasOwnProperty.call(message, "gatewayName"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.gatewayName);
                if (message.credentialId != null && Object.hasOwnProperty.call(message, "credentialId"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.credentialId);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MerchantPaymentPartnerAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.MerchantPaymentPartnerAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.MerchantPaymentPartnerAction
             * @static
             * @param {proto.SyncActionValue.IMerchantPaymentPartnerAction} message MerchantPaymentPartnerAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MerchantPaymentPartnerAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a MerchantPaymentPartnerAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.MerchantPaymentPartnerAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.MerchantPaymentPartnerAction} MerchantPaymentPartnerAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MerchantPaymentPartnerAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.status = reader.int32();
                            message._status = "status";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.country = reader.stringVerify();
                            message._country = "country";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.gatewayName = reader.stringVerify();
                            message._gatewayName = "gatewayName";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.credentialId = reader.stringVerify();
                            message._credentialId = "credentialId";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a MerchantPaymentPartnerAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.MerchantPaymentPartnerAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.MerchantPaymentPartnerAction} MerchantPaymentPartnerAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MerchantPaymentPartnerAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MerchantPaymentPartnerAction message.
             * @function verify
             * @memberof proto.SyncActionValue.MerchantPaymentPartnerAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MerchantPaymentPartnerAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.status != null && Object.hasOwnProperty.call(message, "status")) {
                    properties._status = 1;
                    if (typeof message.status !== "number" || (message.status | 0) !== message.status)
                        return "status: enum value expected";
                }
                if (message.country != null && Object.hasOwnProperty.call(message, "country")) {
                    properties._country = 1;
                    if (!$util.isString(message.country))
                        return "country: string expected";
                }
                if (message.gatewayName != null && Object.hasOwnProperty.call(message, "gatewayName")) {
                    properties._gatewayName = 1;
                    if (!$util.isString(message.gatewayName))
                        return "gatewayName: string expected";
                }
                if (message.credentialId != null && Object.hasOwnProperty.call(message, "credentialId")) {
                    properties._credentialId = 1;
                    if (!$util.isString(message.credentialId))
                        return "credentialId: string expected";
                }
                return null;
            };

            /**
             * Creates a MerchantPaymentPartnerAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.MerchantPaymentPartnerAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.MerchantPaymentPartnerAction} MerchantPaymentPartnerAction
             */
            MerchantPaymentPartnerAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.MerchantPaymentPartnerAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.status) {
                case "ACTIVE":
                case 0:
                    message.status = 0;
                    break;
                case "INACTIVE":
                case 1:
                    message.status = 1;
                    break;
                default:
                    if (typeof object.status === "number" && (object.status | 0) === object.status)
                        message.status = object.status;
                }
                if (object.country != null)
                    message.country = String(object.country);
                if (object.gatewayName != null)
                    message.gatewayName = String(object.gatewayName);
                if (object.credentialId != null)
                    message.credentialId = String(object.credentialId);
                return message;
            };

            /**
             * Creates a plain object from a MerchantPaymentPartnerAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.MerchantPaymentPartnerAction
             * @static
             * @param {proto.SyncActionValue.MerchantPaymentPartnerAction} message MerchantPaymentPartnerAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MerchantPaymentPartnerAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                    object.status = options.enums === String ? $root.proto.SyncActionValue.MerchantPaymentPartnerAction.Status[message.status] === undefined ? message.status : $root.proto.SyncActionValue.MerchantPaymentPartnerAction.Status[message.status] : message.status;
                if (message.country != null && Object.hasOwnProperty.call(message, "country"))
                    object.country = message.country;
                if (message.gatewayName != null && Object.hasOwnProperty.call(message, "gatewayName"))
                    object.gatewayName = message.gatewayName;
                if (message.credentialId != null && Object.hasOwnProperty.call(message, "credentialId"))
                    object.credentialId = message.credentialId;
                return object;
            };

            /**
             * Converts this MerchantPaymentPartnerAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.MerchantPaymentPartnerAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MerchantPaymentPartnerAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MerchantPaymentPartnerAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.MerchantPaymentPartnerAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MerchantPaymentPartnerAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.MerchantPaymentPartnerAction";
            };

            /**
             * Status enum.
             * @name proto.SyncActionValue.MerchantPaymentPartnerAction.Status
             * @enum {number}
             * @property {number} ACTIVE=0 ACTIVE value
             * @property {number} INACTIVE=1 INACTIVE value
             */
            MerchantPaymentPartnerAction.Status = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "ACTIVE"] = 0;
                values[valuesById[1] = "INACTIVE"] = 1;
                return values;
            })();

            return MerchantPaymentPartnerAction;
        })();

        SyncActionValue.MusicUserIdAction = (function() {

            /**
             * Properties of a MusicUserIdAction.
             * @memberof proto.SyncActionValue
             * @interface IMusicUserIdAction
             * @property {string|null} [musicUserId] MusicUserIdAction musicUserId
             * @property {Object.<string,string>|null} [musicUserIdMap] MusicUserIdAction musicUserIdMap
             */

            /**
             * Constructs a new MusicUserIdAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a MusicUserIdAction.
             * @implements IMusicUserIdAction
             * @constructor
             * @param {proto.SyncActionValue.IMusicUserIdAction=} [properties] Properties to set
             */
            function MusicUserIdAction(properties) {
                this.musicUserIdMap = {};
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MusicUserIdAction musicUserId.
             * @member {string|null|undefined} musicUserId
             * @memberof proto.SyncActionValue.MusicUserIdAction
             * @instance
             */
            MusicUserIdAction.prototype.musicUserId = null;

            /**
             * MusicUserIdAction musicUserIdMap.
             * @member {Object.<string,string>} musicUserIdMap
             * @memberof proto.SyncActionValue.MusicUserIdAction
             * @instance
             */
            MusicUserIdAction.prototype.musicUserIdMap = $util.emptyObject;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MusicUserIdAction.prototype, "_musicUserId", {
                get: $util.oneOfGetter($oneOfFields = ["musicUserId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new MusicUserIdAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.MusicUserIdAction
             * @static
             * @param {proto.SyncActionValue.IMusicUserIdAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.MusicUserIdAction} MusicUserIdAction instance
             */
            MusicUserIdAction.create = function create(properties) {
                return new MusicUserIdAction(properties);
            };

            /**
             * Encodes the specified MusicUserIdAction message. Does not implicitly {@link proto.SyncActionValue.MusicUserIdAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.MusicUserIdAction
             * @static
             * @param {proto.SyncActionValue.IMusicUserIdAction} message MusicUserIdAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MusicUserIdAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.musicUserId != null && Object.hasOwnProperty.call(message, "musicUserId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.musicUserId);
                if (message.musicUserIdMap != null && Object.hasOwnProperty.call(message, "musicUserIdMap"))
                    for (var keys = Object.keys(message.musicUserIdMap), i = 0; i < keys.length; ++i)
                        writer.uint32(/* id 2, wireType 2 =*/18).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.musicUserIdMap[keys[i]]).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MusicUserIdAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.MusicUserIdAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.MusicUserIdAction
             * @static
             * @param {proto.SyncActionValue.IMusicUserIdAction} message MusicUserIdAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MusicUserIdAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a MusicUserIdAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.MusicUserIdAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.MusicUserIdAction} MusicUserIdAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MusicUserIdAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), key, v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.musicUserId = reader.stringVerify();
                            message._musicUserId = "musicUserId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (message.musicUserIdMap === $util.emptyObject)
                                message.musicUserIdMap = {};
                            var end2 = reader.uint32() + reader.pos;
                            key = "";
                            v = "";
                            while (reader.pos < end2) {
                                var t2 = reader.tag();
                                u = t2 & 7;
                                switch (t2 >>>= 3) {
                                case 1:
                                    if (u !== 2)
                                        break;
                                    key = reader.stringVerify();
                                    continue;
                                case 2:
                                    if (u !== 2)
                                        break;
                                    v = reader.stringVerify();
                                    continue;
                                }
                                reader.skipType(u, q, t2);
                            }
                            if (key === "__proto__")
                                $util.makeProp(message.musicUserIdMap, key);
                            message.musicUserIdMap[key] = v;
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a MusicUserIdAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.MusicUserIdAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.MusicUserIdAction} MusicUserIdAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MusicUserIdAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MusicUserIdAction message.
             * @function verify
             * @memberof proto.SyncActionValue.MusicUserIdAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MusicUserIdAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.musicUserId != null && Object.hasOwnProperty.call(message, "musicUserId")) {
                    properties._musicUserId = 1;
                    if (!$util.isString(message.musicUserId))
                        return "musicUserId: string expected";
                }
                if (message.musicUserIdMap != null && Object.hasOwnProperty.call(message, "musicUserIdMap")) {
                    if (!$util.isObject(message.musicUserIdMap))
                        return "musicUserIdMap: object expected";
                    var key = Object.keys(message.musicUserIdMap);
                    for (var i = 0; i < key.length; ++i)
                        if (!$util.isString(message.musicUserIdMap[key[i]]))
                            return "musicUserIdMap: string{k:string} expected";
                }
                return null;
            };

            /**
             * Creates a MusicUserIdAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.MusicUserIdAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.MusicUserIdAction} MusicUserIdAction
             */
            MusicUserIdAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.MusicUserIdAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.musicUserId != null)
                    message.musicUserId = String(object.musicUserId);
                if (object.musicUserIdMap) {
                    if (!$util.isObject(object.musicUserIdMap))
                        throw TypeError(".proto.SyncActionValue.MusicUserIdAction.musicUserIdMap: object expected");
                    message.musicUserIdMap = {};
                    for (var keys = Object.keys(object.musicUserIdMap), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.musicUserIdMap, keys[i]);
                        message.musicUserIdMap[keys[i]] = String(object.musicUserIdMap[keys[i]]);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a MusicUserIdAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.MusicUserIdAction
             * @static
             * @param {proto.SyncActionValue.MusicUserIdAction} message MusicUserIdAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MusicUserIdAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.objects || options.defaults)
                    object.musicUserIdMap = {};
                if (message.musicUserId != null && Object.hasOwnProperty.call(message, "musicUserId"))
                    object.musicUserId = message.musicUserId;
                var keys2;
                if (message.musicUserIdMap && (keys2 = Object.keys(message.musicUserIdMap)).length) {
                    object.musicUserIdMap = {};
                    for (var j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.musicUserIdMap, keys2[j]);
                        object.musicUserIdMap[keys2[j]] = message.musicUserIdMap[keys2[j]];
                    }
                }
                return object;
            };

            /**
             * Converts this MusicUserIdAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.MusicUserIdAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MusicUserIdAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MusicUserIdAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.MusicUserIdAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MusicUserIdAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.MusicUserIdAction";
            };

            return MusicUserIdAction;
        })();

        SyncActionValue.MuteAction = (function() {

            /**
             * Properties of a MuteAction.
             * @memberof proto.SyncActionValue
             * @interface IMuteAction
             * @property {boolean|null} [muted] MuteAction muted
             * @property {number|Long|null} [muteEndTimestamp] MuteAction muteEndTimestamp
             * @property {boolean|null} [autoMuted] MuteAction autoMuted
             * @property {number|Long|null} [muteEveryoneMentionEndTimestamp] MuteAction muteEveryoneMentionEndTimestamp
             */

            /**
             * Constructs a new MuteAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a MuteAction.
             * @implements IMuteAction
             * @constructor
             * @param {proto.SyncActionValue.IMuteAction=} [properties] Properties to set
             */
            function MuteAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * MuteAction muted.
             * @member {boolean|null|undefined} muted
             * @memberof proto.SyncActionValue.MuteAction
             * @instance
             */
            MuteAction.prototype.muted = null;

            /**
             * MuteAction muteEndTimestamp.
             * @member {number|Long|null|undefined} muteEndTimestamp
             * @memberof proto.SyncActionValue.MuteAction
             * @instance
             */
            MuteAction.prototype.muteEndTimestamp = null;

            /**
             * MuteAction autoMuted.
             * @member {boolean|null|undefined} autoMuted
             * @memberof proto.SyncActionValue.MuteAction
             * @instance
             */
            MuteAction.prototype.autoMuted = null;

            /**
             * MuteAction muteEveryoneMentionEndTimestamp.
             * @member {number|Long|null|undefined} muteEveryoneMentionEndTimestamp
             * @memberof proto.SyncActionValue.MuteAction
             * @instance
             */
            MuteAction.prototype.muteEveryoneMentionEndTimestamp = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MuteAction.prototype, "_muted", {
                get: $util.oneOfGetter($oneOfFields = ["muted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MuteAction.prototype, "_muteEndTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["muteEndTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MuteAction.prototype, "_autoMuted", {
                get: $util.oneOfGetter($oneOfFields = ["autoMuted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MuteAction.prototype, "_muteEveryoneMentionEndTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["muteEveryoneMentionEndTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new MuteAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.MuteAction
             * @static
             * @param {proto.SyncActionValue.IMuteAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.MuteAction} MuteAction instance
             */
            MuteAction.create = function create(properties) {
                return new MuteAction(properties);
            };

            /**
             * Encodes the specified MuteAction message. Does not implicitly {@link proto.SyncActionValue.MuteAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.MuteAction
             * @static
             * @param {proto.SyncActionValue.IMuteAction} message MuteAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MuteAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.muted != null && Object.hasOwnProperty.call(message, "muted"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.muted);
                if (message.muteEndTimestamp != null && Object.hasOwnProperty.call(message, "muteEndTimestamp"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.muteEndTimestamp);
                if (message.autoMuted != null && Object.hasOwnProperty.call(message, "autoMuted"))
                    writer.uint32(/* id 3, wireType 0 =*/24).bool(message.autoMuted);
                if (message.muteEveryoneMentionEndTimestamp != null && Object.hasOwnProperty.call(message, "muteEveryoneMentionEndTimestamp"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int64(message.muteEveryoneMentionEndTimestamp);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified MuteAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.MuteAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.MuteAction
             * @static
             * @param {proto.SyncActionValue.IMuteAction} message MuteAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            MuteAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a MuteAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.MuteAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.MuteAction} MuteAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MuteAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.muted = reader.bool();
                            message._muted = "muted";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.muteEndTimestamp = reader.int64();
                            message._muteEndTimestamp = "muteEndTimestamp";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.autoMuted = reader.bool();
                            message._autoMuted = "autoMuted";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.muteEveryoneMentionEndTimestamp = reader.int64();
                            message._muteEveryoneMentionEndTimestamp = "muteEveryoneMentionEndTimestamp";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a MuteAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.MuteAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.MuteAction} MuteAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            MuteAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a MuteAction message.
             * @function verify
             * @memberof proto.SyncActionValue.MuteAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            MuteAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.muted != null && Object.hasOwnProperty.call(message, "muted")) {
                    properties._muted = 1;
                    if (typeof message.muted !== "boolean")
                        return "muted: boolean expected";
                }
                if (message.muteEndTimestamp != null && Object.hasOwnProperty.call(message, "muteEndTimestamp")) {
                    properties._muteEndTimestamp = 1;
                    if (!$util.isInteger(message.muteEndTimestamp) && !(message.muteEndTimestamp && $util.isInteger(message.muteEndTimestamp.low) && $util.isInteger(message.muteEndTimestamp.high)))
                        return "muteEndTimestamp: integer|Long expected";
                }
                if (message.autoMuted != null && Object.hasOwnProperty.call(message, "autoMuted")) {
                    properties._autoMuted = 1;
                    if (typeof message.autoMuted !== "boolean")
                        return "autoMuted: boolean expected";
                }
                if (message.muteEveryoneMentionEndTimestamp != null && Object.hasOwnProperty.call(message, "muteEveryoneMentionEndTimestamp")) {
                    properties._muteEveryoneMentionEndTimestamp = 1;
                    if (!$util.isInteger(message.muteEveryoneMentionEndTimestamp) && !(message.muteEveryoneMentionEndTimestamp && $util.isInteger(message.muteEveryoneMentionEndTimestamp.low) && $util.isInteger(message.muteEveryoneMentionEndTimestamp.high)))
                        return "muteEveryoneMentionEndTimestamp: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a MuteAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.MuteAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.MuteAction} MuteAction
             */
            MuteAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.MuteAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.muted != null)
                    message.muted = Boolean(object.muted);
                if (object.muteEndTimestamp != null)
                    if ($util.Long)
                        message.muteEndTimestamp = $util.Long.fromValue(object.muteEndTimestamp, false);
                    else if (typeof object.muteEndTimestamp === "string")
                        message.muteEndTimestamp = parseInt(object.muteEndTimestamp, 10);
                    else if (typeof object.muteEndTimestamp === "number")
                        message.muteEndTimestamp = object.muteEndTimestamp;
                    else if (typeof object.muteEndTimestamp === "object")
                        message.muteEndTimestamp = new $util.LongBits(object.muteEndTimestamp.low >>> 0, object.muteEndTimestamp.high >>> 0).toNumber();
                if (object.autoMuted != null)
                    message.autoMuted = Boolean(object.autoMuted);
                if (object.muteEveryoneMentionEndTimestamp != null)
                    if ($util.Long)
                        message.muteEveryoneMentionEndTimestamp = $util.Long.fromValue(object.muteEveryoneMentionEndTimestamp, false);
                    else if (typeof object.muteEveryoneMentionEndTimestamp === "string")
                        message.muteEveryoneMentionEndTimestamp = parseInt(object.muteEveryoneMentionEndTimestamp, 10);
                    else if (typeof object.muteEveryoneMentionEndTimestamp === "number")
                        message.muteEveryoneMentionEndTimestamp = object.muteEveryoneMentionEndTimestamp;
                    else if (typeof object.muteEveryoneMentionEndTimestamp === "object")
                        message.muteEveryoneMentionEndTimestamp = new $util.LongBits(object.muteEveryoneMentionEndTimestamp.low >>> 0, object.muteEveryoneMentionEndTimestamp.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a MuteAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.MuteAction
             * @static
             * @param {proto.SyncActionValue.MuteAction} message MuteAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            MuteAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.muted != null && Object.hasOwnProperty.call(message, "muted"))
                    object.muted = message.muted;
                if (message.muteEndTimestamp != null && Object.hasOwnProperty.call(message, "muteEndTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.muteEndTimestamp = typeof message.muteEndTimestamp === "number" ? BigInt(message.muteEndTimestamp) : $util.Long.fromBits(message.muteEndTimestamp.low >>> 0, message.muteEndTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.muteEndTimestamp === "number")
                        object.muteEndTimestamp = options.longs === String ? String(message.muteEndTimestamp) : message.muteEndTimestamp;
                    else
                        object.muteEndTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.muteEndTimestamp) : options.longs === Number ? new $util.LongBits(message.muteEndTimestamp.low >>> 0, message.muteEndTimestamp.high >>> 0).toNumber() : message.muteEndTimestamp;
                if (message.autoMuted != null && Object.hasOwnProperty.call(message, "autoMuted"))
                    object.autoMuted = message.autoMuted;
                if (message.muteEveryoneMentionEndTimestamp != null && Object.hasOwnProperty.call(message, "muteEveryoneMentionEndTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.muteEveryoneMentionEndTimestamp = typeof message.muteEveryoneMentionEndTimestamp === "number" ? BigInt(message.muteEveryoneMentionEndTimestamp) : $util.Long.fromBits(message.muteEveryoneMentionEndTimestamp.low >>> 0, message.muteEveryoneMentionEndTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.muteEveryoneMentionEndTimestamp === "number")
                        object.muteEveryoneMentionEndTimestamp = options.longs === String ? String(message.muteEveryoneMentionEndTimestamp) : message.muteEveryoneMentionEndTimestamp;
                    else
                        object.muteEveryoneMentionEndTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.muteEveryoneMentionEndTimestamp) : options.longs === Number ? new $util.LongBits(message.muteEveryoneMentionEndTimestamp.low >>> 0, message.muteEveryoneMentionEndTimestamp.high >>> 0).toNumber() : message.muteEveryoneMentionEndTimestamp;
                return object;
            };

            /**
             * Converts this MuteAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.MuteAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            MuteAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for MuteAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.MuteAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            MuteAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.MuteAction";
            };

            return MuteAction;
        })();

        SyncActionValue.NctSaltSyncAction = (function() {

            /**
             * Properties of a NctSaltSyncAction.
             * @memberof proto.SyncActionValue
             * @interface INctSaltSyncAction
             * @property {Uint8Array|null} [salt] NctSaltSyncAction salt
             */

            /**
             * Constructs a new NctSaltSyncAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a NctSaltSyncAction.
             * @implements INctSaltSyncAction
             * @constructor
             * @param {proto.SyncActionValue.INctSaltSyncAction=} [properties] Properties to set
             */
            function NctSaltSyncAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * NctSaltSyncAction salt.
             * @member {Uint8Array|null|undefined} salt
             * @memberof proto.SyncActionValue.NctSaltSyncAction
             * @instance
             */
            NctSaltSyncAction.prototype.salt = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NctSaltSyncAction.prototype, "_salt", {
                get: $util.oneOfGetter($oneOfFields = ["salt"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new NctSaltSyncAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.NctSaltSyncAction
             * @static
             * @param {proto.SyncActionValue.INctSaltSyncAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.NctSaltSyncAction} NctSaltSyncAction instance
             */
            NctSaltSyncAction.create = function create(properties) {
                return new NctSaltSyncAction(properties);
            };

            /**
             * Encodes the specified NctSaltSyncAction message. Does not implicitly {@link proto.SyncActionValue.NctSaltSyncAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.NctSaltSyncAction
             * @static
             * @param {proto.SyncActionValue.INctSaltSyncAction} message NctSaltSyncAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            NctSaltSyncAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.salt != null && Object.hasOwnProperty.call(message, "salt"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.salt);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified NctSaltSyncAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.NctSaltSyncAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.NctSaltSyncAction
             * @static
             * @param {proto.SyncActionValue.INctSaltSyncAction} message NctSaltSyncAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            NctSaltSyncAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a NctSaltSyncAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.NctSaltSyncAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.NctSaltSyncAction} NctSaltSyncAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            NctSaltSyncAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.salt = reader.bytes();
                            message._salt = "salt";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a NctSaltSyncAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.NctSaltSyncAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.NctSaltSyncAction} NctSaltSyncAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            NctSaltSyncAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a NctSaltSyncAction message.
             * @function verify
             * @memberof proto.SyncActionValue.NctSaltSyncAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            NctSaltSyncAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.salt != null && Object.hasOwnProperty.call(message, "salt")) {
                    properties._salt = 1;
                    if (!(message.salt && typeof message.salt.length === "number" || $util.isString(message.salt)))
                        return "salt: buffer expected";
                }
                return null;
            };

            /**
             * Creates a NctSaltSyncAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.NctSaltSyncAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.NctSaltSyncAction} NctSaltSyncAction
             */
            NctSaltSyncAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.NctSaltSyncAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.salt != null)
                    if (typeof object.salt === "string")
                        $util.base64.decode(object.salt, message.salt = $util.newBuffer($util.base64.length(object.salt)), 0);
                    else if (object.salt.length >= 0)
                        message.salt = object.salt;
                return message;
            };

            /**
             * Creates a plain object from a NctSaltSyncAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.NctSaltSyncAction
             * @static
             * @param {proto.SyncActionValue.NctSaltSyncAction} message NctSaltSyncAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            NctSaltSyncAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.salt != null && Object.hasOwnProperty.call(message, "salt"))
                    object.salt = options.bytes === String ? $util.base64.encode(message.salt, 0, message.salt.length) : options.bytes === Array ? Array.prototype.slice.call(message.salt) : message.salt;
                return object;
            };

            /**
             * Converts this NctSaltSyncAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.NctSaltSyncAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            NctSaltSyncAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for NctSaltSyncAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.NctSaltSyncAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            NctSaltSyncAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.NctSaltSyncAction";
            };

            return NctSaltSyncAction;
        })();

        SyncActionValue.NewsletterSavedInterestsAction = (function() {

            /**
             * Properties of a NewsletterSavedInterestsAction.
             * @memberof proto.SyncActionValue
             * @interface INewsletterSavedInterestsAction
             * @property {string|null} [newsletterSavedInterests] NewsletterSavedInterestsAction newsletterSavedInterests
             */

            /**
             * Constructs a new NewsletterSavedInterestsAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a NewsletterSavedInterestsAction.
             * @implements INewsletterSavedInterestsAction
             * @constructor
             * @param {proto.SyncActionValue.INewsletterSavedInterestsAction=} [properties] Properties to set
             */
            function NewsletterSavedInterestsAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * NewsletterSavedInterestsAction newsletterSavedInterests.
             * @member {string|null|undefined} newsletterSavedInterests
             * @memberof proto.SyncActionValue.NewsletterSavedInterestsAction
             * @instance
             */
            NewsletterSavedInterestsAction.prototype.newsletterSavedInterests = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NewsletterSavedInterestsAction.prototype, "_newsletterSavedInterests", {
                get: $util.oneOfGetter($oneOfFields = ["newsletterSavedInterests"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new NewsletterSavedInterestsAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.NewsletterSavedInterestsAction
             * @static
             * @param {proto.SyncActionValue.INewsletterSavedInterestsAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.NewsletterSavedInterestsAction} NewsletterSavedInterestsAction instance
             */
            NewsletterSavedInterestsAction.create = function create(properties) {
                return new NewsletterSavedInterestsAction(properties);
            };

            /**
             * Encodes the specified NewsletterSavedInterestsAction message. Does not implicitly {@link proto.SyncActionValue.NewsletterSavedInterestsAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.NewsletterSavedInterestsAction
             * @static
             * @param {proto.SyncActionValue.INewsletterSavedInterestsAction} message NewsletterSavedInterestsAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            NewsletterSavedInterestsAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.newsletterSavedInterests != null && Object.hasOwnProperty.call(message, "newsletterSavedInterests"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.newsletterSavedInterests);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified NewsletterSavedInterestsAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.NewsletterSavedInterestsAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.NewsletterSavedInterestsAction
             * @static
             * @param {proto.SyncActionValue.INewsletterSavedInterestsAction} message NewsletterSavedInterestsAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            NewsletterSavedInterestsAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a NewsletterSavedInterestsAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.NewsletterSavedInterestsAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.NewsletterSavedInterestsAction} NewsletterSavedInterestsAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            NewsletterSavedInterestsAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.newsletterSavedInterests = reader.stringVerify();
                            message._newsletterSavedInterests = "newsletterSavedInterests";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a NewsletterSavedInterestsAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.NewsletterSavedInterestsAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.NewsletterSavedInterestsAction} NewsletterSavedInterestsAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            NewsletterSavedInterestsAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a NewsletterSavedInterestsAction message.
             * @function verify
             * @memberof proto.SyncActionValue.NewsletterSavedInterestsAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            NewsletterSavedInterestsAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.newsletterSavedInterests != null && Object.hasOwnProperty.call(message, "newsletterSavedInterests")) {
                    properties._newsletterSavedInterests = 1;
                    if (!$util.isString(message.newsletterSavedInterests))
                        return "newsletterSavedInterests: string expected";
                }
                return null;
            };

            /**
             * Creates a NewsletterSavedInterestsAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.NewsletterSavedInterestsAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.NewsletterSavedInterestsAction} NewsletterSavedInterestsAction
             */
            NewsletterSavedInterestsAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.NewsletterSavedInterestsAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.newsletterSavedInterests != null)
                    message.newsletterSavedInterests = String(object.newsletterSavedInterests);
                return message;
            };

            /**
             * Creates a plain object from a NewsletterSavedInterestsAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.NewsletterSavedInterestsAction
             * @static
             * @param {proto.SyncActionValue.NewsletterSavedInterestsAction} message NewsletterSavedInterestsAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            NewsletterSavedInterestsAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.newsletterSavedInterests != null && Object.hasOwnProperty.call(message, "newsletterSavedInterests"))
                    object.newsletterSavedInterests = message.newsletterSavedInterests;
                return object;
            };

            /**
             * Converts this NewsletterSavedInterestsAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.NewsletterSavedInterestsAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            NewsletterSavedInterestsAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for NewsletterSavedInterestsAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.NewsletterSavedInterestsAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            NewsletterSavedInterestsAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.NewsletterSavedInterestsAction";
            };

            return NewsletterSavedInterestsAction;
        })();

        SyncActionValue.NoteEditAction = (function() {

            /**
             * Properties of a NoteEditAction.
             * @memberof proto.SyncActionValue
             * @interface INoteEditAction
             * @property {proto.SyncActionValue.NoteEditAction.NoteType|null} [type] NoteEditAction type
             * @property {string|null} [chatJid] NoteEditAction chatJid
             * @property {number|Long|null} [createdAt] NoteEditAction createdAt
             * @property {boolean|null} [deleted] NoteEditAction deleted
             * @property {string|null} [unstructuredContent] NoteEditAction unstructuredContent
             */

            /**
             * Constructs a new NoteEditAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a NoteEditAction.
             * @implements INoteEditAction
             * @constructor
             * @param {proto.SyncActionValue.INoteEditAction=} [properties] Properties to set
             */
            function NoteEditAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * NoteEditAction type.
             * @member {proto.SyncActionValue.NoteEditAction.NoteType|null|undefined} type
             * @memberof proto.SyncActionValue.NoteEditAction
             * @instance
             */
            NoteEditAction.prototype.type = null;

            /**
             * NoteEditAction chatJid.
             * @member {string|null|undefined} chatJid
             * @memberof proto.SyncActionValue.NoteEditAction
             * @instance
             */
            NoteEditAction.prototype.chatJid = null;

            /**
             * NoteEditAction createdAt.
             * @member {number|Long|null|undefined} createdAt
             * @memberof proto.SyncActionValue.NoteEditAction
             * @instance
             */
            NoteEditAction.prototype.createdAt = null;

            /**
             * NoteEditAction deleted.
             * @member {boolean|null|undefined} deleted
             * @memberof proto.SyncActionValue.NoteEditAction
             * @instance
             */
            NoteEditAction.prototype.deleted = null;

            /**
             * NoteEditAction unstructuredContent.
             * @member {string|null|undefined} unstructuredContent
             * @memberof proto.SyncActionValue.NoteEditAction
             * @instance
             */
            NoteEditAction.prototype.unstructuredContent = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NoteEditAction.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NoteEditAction.prototype, "_chatJid", {
                get: $util.oneOfGetter($oneOfFields = ["chatJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NoteEditAction.prototype, "_createdAt", {
                get: $util.oneOfGetter($oneOfFields = ["createdAt"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NoteEditAction.prototype, "_deleted", {
                get: $util.oneOfGetter($oneOfFields = ["deleted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NoteEditAction.prototype, "_unstructuredContent", {
                get: $util.oneOfGetter($oneOfFields = ["unstructuredContent"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new NoteEditAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.NoteEditAction
             * @static
             * @param {proto.SyncActionValue.INoteEditAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.NoteEditAction} NoteEditAction instance
             */
            NoteEditAction.create = function create(properties) {
                return new NoteEditAction(properties);
            };

            /**
             * Encodes the specified NoteEditAction message. Does not implicitly {@link proto.SyncActionValue.NoteEditAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.NoteEditAction
             * @static
             * @param {proto.SyncActionValue.INoteEditAction} message NoteEditAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            NoteEditAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
                if (message.chatJid != null && Object.hasOwnProperty.call(message, "chatJid"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.chatJid);
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.createdAt);
                if (message.deleted != null && Object.hasOwnProperty.call(message, "deleted"))
                    writer.uint32(/* id 4, wireType 0 =*/32).bool(message.deleted);
                if (message.unstructuredContent != null && Object.hasOwnProperty.call(message, "unstructuredContent"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.unstructuredContent);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified NoteEditAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.NoteEditAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.NoteEditAction
             * @static
             * @param {proto.SyncActionValue.INoteEditAction} message NoteEditAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            NoteEditAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a NoteEditAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.NoteEditAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.NoteEditAction} NoteEditAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            NoteEditAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.type = reader.int32();
                            message._type = "type";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.chatJid = reader.stringVerify();
                            message._chatJid = "chatJid";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.createdAt = reader.int64();
                            message._createdAt = "createdAt";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.deleted = reader.bool();
                            message._deleted = "deleted";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            message.unstructuredContent = reader.stringVerify();
                            message._unstructuredContent = "unstructuredContent";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a NoteEditAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.NoteEditAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.NoteEditAction} NoteEditAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            NoteEditAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a NoteEditAction message.
             * @function verify
             * @memberof proto.SyncActionValue.NoteEditAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            NoteEditAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.type != null && Object.hasOwnProperty.call(message, "type")) {
                    properties._type = 1;
                    if (typeof message.type !== "number" || (message.type | 0) !== message.type)
                        return "type: enum value expected";
                }
                if (message.chatJid != null && Object.hasOwnProperty.call(message, "chatJid")) {
                    properties._chatJid = 1;
                    if (!$util.isString(message.chatJid))
                        return "chatJid: string expected";
                }
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt")) {
                    properties._createdAt = 1;
                    if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
                        return "createdAt: integer|Long expected";
                }
                if (message.deleted != null && Object.hasOwnProperty.call(message, "deleted")) {
                    properties._deleted = 1;
                    if (typeof message.deleted !== "boolean")
                        return "deleted: boolean expected";
                }
                if (message.unstructuredContent != null && Object.hasOwnProperty.call(message, "unstructuredContent")) {
                    properties._unstructuredContent = 1;
                    if (!$util.isString(message.unstructuredContent))
                        return "unstructuredContent: string expected";
                }
                return null;
            };

            /**
             * Creates a NoteEditAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.NoteEditAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.NoteEditAction} NoteEditAction
             */
            NoteEditAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.NoteEditAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.type) {
                case "UNSTRUCTURED":
                case 1:
                    message.type = 1;
                    break;
                case "STRUCTURED":
                case 2:
                    message.type = 2;
                    break;
                default:
                    if (typeof object.type === "number" && (object.type | 0) === object.type)
                        message.type = object.type;
                }
                if (object.chatJid != null)
                    message.chatJid = String(object.chatJid);
                if (object.createdAt != null)
                    if ($util.Long)
                        message.createdAt = $util.Long.fromValue(object.createdAt, false);
                    else if (typeof object.createdAt === "string")
                        message.createdAt = parseInt(object.createdAt, 10);
                    else if (typeof object.createdAt === "number")
                        message.createdAt = object.createdAt;
                    else if (typeof object.createdAt === "object")
                        message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
                if (object.deleted != null)
                    message.deleted = Boolean(object.deleted);
                if (object.unstructuredContent != null)
                    message.unstructuredContent = String(object.unstructuredContent);
                return message;
            };

            /**
             * Creates a plain object from a NoteEditAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.NoteEditAction
             * @static
             * @param {proto.SyncActionValue.NoteEditAction} message NoteEditAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            NoteEditAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                    object.type = options.enums === String ? $root.proto.SyncActionValue.NoteEditAction.NoteType[message.type] === undefined ? message.type : $root.proto.SyncActionValue.NoteEditAction.NoteType[message.type] : message.type;
                if (message.chatJid != null && Object.hasOwnProperty.call(message, "chatJid"))
                    object.chatJid = message.chatJid;
                if (message.createdAt != null && Object.hasOwnProperty.call(message, "createdAt"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.createdAt = typeof message.createdAt === "number" ? BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
                    else if (typeof message.createdAt === "number")
                        object.createdAt = options.longs === String ? String(message.createdAt) : message.createdAt;
                    else
                        object.createdAt = options.longs === String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
                if (message.deleted != null && Object.hasOwnProperty.call(message, "deleted"))
                    object.deleted = message.deleted;
                if (message.unstructuredContent != null && Object.hasOwnProperty.call(message, "unstructuredContent"))
                    object.unstructuredContent = message.unstructuredContent;
                return object;
            };

            /**
             * Converts this NoteEditAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.NoteEditAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            NoteEditAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for NoteEditAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.NoteEditAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            NoteEditAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.NoteEditAction";
            };

            /**
             * NoteType enum.
             * @name proto.SyncActionValue.NoteEditAction.NoteType
             * @enum {number}
             * @property {number} UNSTRUCTURED=1 UNSTRUCTURED value
             * @property {number} STRUCTURED=2 STRUCTURED value
             */
            NoteEditAction.NoteType = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[1] = "UNSTRUCTURED"] = 1;
                values[valuesById[2] = "STRUCTURED"] = 2;
                return values;
            })();

            return NoteEditAction;
        })();

        SyncActionValue.NotificationActivitySettingAction = (function() {

            /**
             * Properties of a NotificationActivitySettingAction.
             * @memberof proto.SyncActionValue
             * @interface INotificationActivitySettingAction
             * @property {proto.SyncActionValue.NotificationActivitySettingAction.NotificationActivitySetting|null} [notificationActivitySetting] NotificationActivitySettingAction notificationActivitySetting
             */

            /**
             * Constructs a new NotificationActivitySettingAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a NotificationActivitySettingAction.
             * @implements INotificationActivitySettingAction
             * @constructor
             * @param {proto.SyncActionValue.INotificationActivitySettingAction=} [properties] Properties to set
             */
            function NotificationActivitySettingAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * NotificationActivitySettingAction notificationActivitySetting.
             * @member {proto.SyncActionValue.NotificationActivitySettingAction.NotificationActivitySetting|null|undefined} notificationActivitySetting
             * @memberof proto.SyncActionValue.NotificationActivitySettingAction
             * @instance
             */
            NotificationActivitySettingAction.prototype.notificationActivitySetting = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NotificationActivitySettingAction.prototype, "_notificationActivitySetting", {
                get: $util.oneOfGetter($oneOfFields = ["notificationActivitySetting"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new NotificationActivitySettingAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.NotificationActivitySettingAction
             * @static
             * @param {proto.SyncActionValue.INotificationActivitySettingAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.NotificationActivitySettingAction} NotificationActivitySettingAction instance
             */
            NotificationActivitySettingAction.create = function create(properties) {
                return new NotificationActivitySettingAction(properties);
            };

            /**
             * Encodes the specified NotificationActivitySettingAction message. Does not implicitly {@link proto.SyncActionValue.NotificationActivitySettingAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.NotificationActivitySettingAction
             * @static
             * @param {proto.SyncActionValue.INotificationActivitySettingAction} message NotificationActivitySettingAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            NotificationActivitySettingAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.notificationActivitySetting != null && Object.hasOwnProperty.call(message, "notificationActivitySetting"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.notificationActivitySetting);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified NotificationActivitySettingAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.NotificationActivitySettingAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.NotificationActivitySettingAction
             * @static
             * @param {proto.SyncActionValue.INotificationActivitySettingAction} message NotificationActivitySettingAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            NotificationActivitySettingAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a NotificationActivitySettingAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.NotificationActivitySettingAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.NotificationActivitySettingAction} NotificationActivitySettingAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            NotificationActivitySettingAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.notificationActivitySetting = reader.int32();
                            message._notificationActivitySetting = "notificationActivitySetting";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a NotificationActivitySettingAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.NotificationActivitySettingAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.NotificationActivitySettingAction} NotificationActivitySettingAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            NotificationActivitySettingAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a NotificationActivitySettingAction message.
             * @function verify
             * @memberof proto.SyncActionValue.NotificationActivitySettingAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            NotificationActivitySettingAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.notificationActivitySetting != null && Object.hasOwnProperty.call(message, "notificationActivitySetting")) {
                    properties._notificationActivitySetting = 1;
                    if (typeof message.notificationActivitySetting !== "number" || (message.notificationActivitySetting | 0) !== message.notificationActivitySetting)
                        return "notificationActivitySetting: enum value expected";
                }
                return null;
            };

            /**
             * Creates a NotificationActivitySettingAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.NotificationActivitySettingAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.NotificationActivitySettingAction} NotificationActivitySettingAction
             */
            NotificationActivitySettingAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.NotificationActivitySettingAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.notificationActivitySetting) {
                case "DEFAULT_ALL_MESSAGES":
                case 0:
                    message.notificationActivitySetting = 0;
                    break;
                case "ALL_MESSAGES":
                case 1:
                    message.notificationActivitySetting = 1;
                    break;
                case "HIGHLIGHTS":
                case 2:
                    message.notificationActivitySetting = 2;
                    break;
                case "DEFAULT_HIGHLIGHTS":
                case 3:
                    message.notificationActivitySetting = 3;
                    break;
                default:
                    if (typeof object.notificationActivitySetting === "number" && (object.notificationActivitySetting | 0) === object.notificationActivitySetting)
                        message.notificationActivitySetting = object.notificationActivitySetting;
                }
                return message;
            };

            /**
             * Creates a plain object from a NotificationActivitySettingAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.NotificationActivitySettingAction
             * @static
             * @param {proto.SyncActionValue.NotificationActivitySettingAction} message NotificationActivitySettingAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            NotificationActivitySettingAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.notificationActivitySetting != null && Object.hasOwnProperty.call(message, "notificationActivitySetting"))
                    object.notificationActivitySetting = options.enums === String ? $root.proto.SyncActionValue.NotificationActivitySettingAction.NotificationActivitySetting[message.notificationActivitySetting] === undefined ? message.notificationActivitySetting : $root.proto.SyncActionValue.NotificationActivitySettingAction.NotificationActivitySetting[message.notificationActivitySetting] : message.notificationActivitySetting;
                return object;
            };

            /**
             * Converts this NotificationActivitySettingAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.NotificationActivitySettingAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            NotificationActivitySettingAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for NotificationActivitySettingAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.NotificationActivitySettingAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            NotificationActivitySettingAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.NotificationActivitySettingAction";
            };

            /**
             * NotificationActivitySetting enum.
             * @name proto.SyncActionValue.NotificationActivitySettingAction.NotificationActivitySetting
             * @enum {number}
             * @property {number} DEFAULT_ALL_MESSAGES=0 DEFAULT_ALL_MESSAGES value
             * @property {number} ALL_MESSAGES=1 ALL_MESSAGES value
             * @property {number} HIGHLIGHTS=2 HIGHLIGHTS value
             * @property {number} DEFAULT_HIGHLIGHTS=3 DEFAULT_HIGHLIGHTS value
             */
            NotificationActivitySettingAction.NotificationActivitySetting = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "DEFAULT_ALL_MESSAGES"] = 0;
                values[valuesById[1] = "ALL_MESSAGES"] = 1;
                values[valuesById[2] = "HIGHLIGHTS"] = 2;
                values[valuesById[3] = "DEFAULT_HIGHLIGHTS"] = 3;
                return values;
            })();

            return NotificationActivitySettingAction;
        })();

        SyncActionValue.NuxAction = (function() {

            /**
             * Properties of a NuxAction.
             * @memberof proto.SyncActionValue
             * @interface INuxAction
             * @property {boolean|null} [acknowledged] NuxAction acknowledged
             */

            /**
             * Constructs a new NuxAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a NuxAction.
             * @implements INuxAction
             * @constructor
             * @param {proto.SyncActionValue.INuxAction=} [properties] Properties to set
             */
            function NuxAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * NuxAction acknowledged.
             * @member {boolean|null|undefined} acknowledged
             * @memberof proto.SyncActionValue.NuxAction
             * @instance
             */
            NuxAction.prototype.acknowledged = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NuxAction.prototype, "_acknowledged", {
                get: $util.oneOfGetter($oneOfFields = ["acknowledged"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new NuxAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.NuxAction
             * @static
             * @param {proto.SyncActionValue.INuxAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.NuxAction} NuxAction instance
             */
            NuxAction.create = function create(properties) {
                return new NuxAction(properties);
            };

            /**
             * Encodes the specified NuxAction message. Does not implicitly {@link proto.SyncActionValue.NuxAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.NuxAction
             * @static
             * @param {proto.SyncActionValue.INuxAction} message NuxAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            NuxAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.acknowledged != null && Object.hasOwnProperty.call(message, "acknowledged"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.acknowledged);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified NuxAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.NuxAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.NuxAction
             * @static
             * @param {proto.SyncActionValue.INuxAction} message NuxAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            NuxAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a NuxAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.NuxAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.NuxAction} NuxAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            NuxAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.acknowledged = reader.bool();
                            message._acknowledged = "acknowledged";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a NuxAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.NuxAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.NuxAction} NuxAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            NuxAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a NuxAction message.
             * @function verify
             * @memberof proto.SyncActionValue.NuxAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            NuxAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.acknowledged != null && Object.hasOwnProperty.call(message, "acknowledged")) {
                    properties._acknowledged = 1;
                    if (typeof message.acknowledged !== "boolean")
                        return "acknowledged: boolean expected";
                }
                return null;
            };

            /**
             * Creates a NuxAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.NuxAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.NuxAction} NuxAction
             */
            NuxAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.NuxAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.acknowledged != null)
                    message.acknowledged = Boolean(object.acknowledged);
                return message;
            };

            /**
             * Creates a plain object from a NuxAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.NuxAction
             * @static
             * @param {proto.SyncActionValue.NuxAction} message NuxAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            NuxAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.acknowledged != null && Object.hasOwnProperty.call(message, "acknowledged"))
                    object.acknowledged = message.acknowledged;
                return object;
            };

            /**
             * Converts this NuxAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.NuxAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            NuxAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for NuxAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.NuxAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            NuxAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.NuxAction";
            };

            return NuxAction;
        })();

        SyncActionValue.OutContactAction = (function() {

            /**
             * Properties of an OutContactAction.
             * @memberof proto.SyncActionValue
             * @interface IOutContactAction
             * @property {string|null} [fullName] OutContactAction fullName
             * @property {string|null} [firstName] OutContactAction firstName
             */

            /**
             * Constructs a new OutContactAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents an OutContactAction.
             * @implements IOutContactAction
             * @constructor
             * @param {proto.SyncActionValue.IOutContactAction=} [properties] Properties to set
             */
            function OutContactAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * OutContactAction fullName.
             * @member {string|null|undefined} fullName
             * @memberof proto.SyncActionValue.OutContactAction
             * @instance
             */
            OutContactAction.prototype.fullName = null;

            /**
             * OutContactAction firstName.
             * @member {string|null|undefined} firstName
             * @memberof proto.SyncActionValue.OutContactAction
             * @instance
             */
            OutContactAction.prototype.firstName = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(OutContactAction.prototype, "_fullName", {
                get: $util.oneOfGetter($oneOfFields = ["fullName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(OutContactAction.prototype, "_firstName", {
                get: $util.oneOfGetter($oneOfFields = ["firstName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new OutContactAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.OutContactAction
             * @static
             * @param {proto.SyncActionValue.IOutContactAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.OutContactAction} OutContactAction instance
             */
            OutContactAction.create = function create(properties) {
                return new OutContactAction(properties);
            };

            /**
             * Encodes the specified OutContactAction message. Does not implicitly {@link proto.SyncActionValue.OutContactAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.OutContactAction
             * @static
             * @param {proto.SyncActionValue.IOutContactAction} message OutContactAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OutContactAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.fullName != null && Object.hasOwnProperty.call(message, "fullName"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.fullName);
                if (message.firstName != null && Object.hasOwnProperty.call(message, "firstName"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.firstName);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified OutContactAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.OutContactAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.OutContactAction
             * @static
             * @param {proto.SyncActionValue.IOutContactAction} message OutContactAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OutContactAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an OutContactAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.OutContactAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.OutContactAction} OutContactAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OutContactAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.fullName = reader.stringVerify();
                            message._fullName = "fullName";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.firstName = reader.stringVerify();
                            message._firstName = "firstName";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an OutContactAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.OutContactAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.OutContactAction} OutContactAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OutContactAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an OutContactAction message.
             * @function verify
             * @memberof proto.SyncActionValue.OutContactAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            OutContactAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.fullName != null && Object.hasOwnProperty.call(message, "fullName")) {
                    properties._fullName = 1;
                    if (!$util.isString(message.fullName))
                        return "fullName: string expected";
                }
                if (message.firstName != null && Object.hasOwnProperty.call(message, "firstName")) {
                    properties._firstName = 1;
                    if (!$util.isString(message.firstName))
                        return "firstName: string expected";
                }
                return null;
            };

            /**
             * Creates an OutContactAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.OutContactAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.OutContactAction} OutContactAction
             */
            OutContactAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.OutContactAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.fullName != null)
                    message.fullName = String(object.fullName);
                if (object.firstName != null)
                    message.firstName = String(object.firstName);
                return message;
            };

            /**
             * Creates a plain object from an OutContactAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.OutContactAction
             * @static
             * @param {proto.SyncActionValue.OutContactAction} message OutContactAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            OutContactAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.fullName != null && Object.hasOwnProperty.call(message, "fullName"))
                    object.fullName = message.fullName;
                if (message.firstName != null && Object.hasOwnProperty.call(message, "firstName"))
                    object.firstName = message.firstName;
                return object;
            };

            /**
             * Converts this OutContactAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.OutContactAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            OutContactAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for OutContactAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.OutContactAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            OutContactAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.OutContactAction";
            };

            return OutContactAction;
        })();

        SyncActionValue.PaymentInfoAction = (function() {

            /**
             * Properties of a PaymentInfoAction.
             * @memberof proto.SyncActionValue
             * @interface IPaymentInfoAction
             * @property {string|null} [cpi] PaymentInfoAction cpi
             */

            /**
             * Constructs a new PaymentInfoAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a PaymentInfoAction.
             * @implements IPaymentInfoAction
             * @constructor
             * @param {proto.SyncActionValue.IPaymentInfoAction=} [properties] Properties to set
             */
            function PaymentInfoAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PaymentInfoAction cpi.
             * @member {string|null|undefined} cpi
             * @memberof proto.SyncActionValue.PaymentInfoAction
             * @instance
             */
            PaymentInfoAction.prototype.cpi = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentInfoAction.prototype, "_cpi", {
                get: $util.oneOfGetter($oneOfFields = ["cpi"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PaymentInfoAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.PaymentInfoAction
             * @static
             * @param {proto.SyncActionValue.IPaymentInfoAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.PaymentInfoAction} PaymentInfoAction instance
             */
            PaymentInfoAction.create = function create(properties) {
                return new PaymentInfoAction(properties);
            };

            /**
             * Encodes the specified PaymentInfoAction message. Does not implicitly {@link proto.SyncActionValue.PaymentInfoAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.PaymentInfoAction
             * @static
             * @param {proto.SyncActionValue.IPaymentInfoAction} message PaymentInfoAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PaymentInfoAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.cpi != null && Object.hasOwnProperty.call(message, "cpi"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.cpi);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PaymentInfoAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.PaymentInfoAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.PaymentInfoAction
             * @static
             * @param {proto.SyncActionValue.IPaymentInfoAction} message PaymentInfoAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PaymentInfoAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PaymentInfoAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.PaymentInfoAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.PaymentInfoAction} PaymentInfoAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PaymentInfoAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.cpi = reader.stringVerify();
                            message._cpi = "cpi";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PaymentInfoAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.PaymentInfoAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.PaymentInfoAction} PaymentInfoAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PaymentInfoAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PaymentInfoAction message.
             * @function verify
             * @memberof proto.SyncActionValue.PaymentInfoAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PaymentInfoAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.cpi != null && Object.hasOwnProperty.call(message, "cpi")) {
                    properties._cpi = 1;
                    if (!$util.isString(message.cpi))
                        return "cpi: string expected";
                }
                return null;
            };

            /**
             * Creates a PaymentInfoAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.PaymentInfoAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.PaymentInfoAction} PaymentInfoAction
             */
            PaymentInfoAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.PaymentInfoAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.cpi != null)
                    message.cpi = String(object.cpi);
                return message;
            };

            /**
             * Creates a plain object from a PaymentInfoAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.PaymentInfoAction
             * @static
             * @param {proto.SyncActionValue.PaymentInfoAction} message PaymentInfoAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PaymentInfoAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.cpi != null && Object.hasOwnProperty.call(message, "cpi"))
                    object.cpi = message.cpi;
                return object;
            };

            /**
             * Converts this PaymentInfoAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.PaymentInfoAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PaymentInfoAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PaymentInfoAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.PaymentInfoAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PaymentInfoAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.PaymentInfoAction";
            };

            return PaymentInfoAction;
        })();

        SyncActionValue.PaymentTosAction = (function() {

            /**
             * Properties of a PaymentTosAction.
             * @memberof proto.SyncActionValue
             * @interface IPaymentTosAction
             * @property {proto.SyncActionValue.PaymentTosAction.PaymentNotice|null} [paymentNotice] PaymentTosAction paymentNotice
             * @property {boolean|null} [accepted] PaymentTosAction accepted
             */

            /**
             * Constructs a new PaymentTosAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a PaymentTosAction.
             * @implements IPaymentTosAction
             * @constructor
             * @param {proto.SyncActionValue.IPaymentTosAction=} [properties] Properties to set
             */
            function PaymentTosAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PaymentTosAction paymentNotice.
             * @member {proto.SyncActionValue.PaymentTosAction.PaymentNotice|null|undefined} paymentNotice
             * @memberof proto.SyncActionValue.PaymentTosAction
             * @instance
             */
            PaymentTosAction.prototype.paymentNotice = null;

            /**
             * PaymentTosAction accepted.
             * @member {boolean|null|undefined} accepted
             * @memberof proto.SyncActionValue.PaymentTosAction
             * @instance
             */
            PaymentTosAction.prototype.accepted = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentTosAction.prototype, "_paymentNotice", {
                get: $util.oneOfGetter($oneOfFields = ["paymentNotice"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentTosAction.prototype, "_accepted", {
                get: $util.oneOfGetter($oneOfFields = ["accepted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PaymentTosAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.PaymentTosAction
             * @static
             * @param {proto.SyncActionValue.IPaymentTosAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.PaymentTosAction} PaymentTosAction instance
             */
            PaymentTosAction.create = function create(properties) {
                return new PaymentTosAction(properties);
            };

            /**
             * Encodes the specified PaymentTosAction message. Does not implicitly {@link proto.SyncActionValue.PaymentTosAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.PaymentTosAction
             * @static
             * @param {proto.SyncActionValue.IPaymentTosAction} message PaymentTosAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PaymentTosAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.paymentNotice != null && Object.hasOwnProperty.call(message, "paymentNotice"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.paymentNotice);
                if (message.accepted != null && Object.hasOwnProperty.call(message, "accepted"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.accepted);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PaymentTosAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.PaymentTosAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.PaymentTosAction
             * @static
             * @param {proto.SyncActionValue.IPaymentTosAction} message PaymentTosAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PaymentTosAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PaymentTosAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.PaymentTosAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.PaymentTosAction} PaymentTosAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PaymentTosAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.paymentNotice = reader.int32();
                            message._paymentNotice = "paymentNotice";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.accepted = reader.bool();
                            message._accepted = "accepted";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PaymentTosAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.PaymentTosAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.PaymentTosAction} PaymentTosAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PaymentTosAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PaymentTosAction message.
             * @function verify
             * @memberof proto.SyncActionValue.PaymentTosAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PaymentTosAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.paymentNotice != null && Object.hasOwnProperty.call(message, "paymentNotice")) {
                    properties._paymentNotice = 1;
                    if (typeof message.paymentNotice !== "number" || (message.paymentNotice | 0) !== message.paymentNotice)
                        return "paymentNotice: enum value expected";
                }
                if (message.accepted != null && Object.hasOwnProperty.call(message, "accepted")) {
                    properties._accepted = 1;
                    if (typeof message.accepted !== "boolean")
                        return "accepted: boolean expected";
                }
                return null;
            };

            /**
             * Creates a PaymentTosAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.PaymentTosAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.PaymentTosAction} PaymentTosAction
             */
            PaymentTosAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.PaymentTosAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.paymentNotice) {
                case "BR_PAY_PRIVACY_POLICY":
                case 0:
                    message.paymentNotice = 0;
                    break;
                default:
                    if (typeof object.paymentNotice === "number" && (object.paymentNotice | 0) === object.paymentNotice)
                        message.paymentNotice = object.paymentNotice;
                }
                if (object.accepted != null)
                    message.accepted = Boolean(object.accepted);
                return message;
            };

            /**
             * Creates a plain object from a PaymentTosAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.PaymentTosAction
             * @static
             * @param {proto.SyncActionValue.PaymentTosAction} message PaymentTosAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PaymentTosAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.paymentNotice != null && Object.hasOwnProperty.call(message, "paymentNotice"))
                    object.paymentNotice = options.enums === String ? $root.proto.SyncActionValue.PaymentTosAction.PaymentNotice[message.paymentNotice] === undefined ? message.paymentNotice : $root.proto.SyncActionValue.PaymentTosAction.PaymentNotice[message.paymentNotice] : message.paymentNotice;
                if (message.accepted != null && Object.hasOwnProperty.call(message, "accepted"))
                    object.accepted = message.accepted;
                return object;
            };

            /**
             * Converts this PaymentTosAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.PaymentTosAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PaymentTosAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PaymentTosAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.PaymentTosAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PaymentTosAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.PaymentTosAction";
            };

            /**
             * PaymentNotice enum.
             * @name proto.SyncActionValue.PaymentTosAction.PaymentNotice
             * @enum {number}
             * @property {number} BR_PAY_PRIVACY_POLICY=0 BR_PAY_PRIVACY_POLICY value
             */
            PaymentTosAction.PaymentNotice = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "BR_PAY_PRIVACY_POLICY"] = 0;
                return values;
            })();

            return PaymentTosAction;
        })();

        SyncActionValue.PinAction = (function() {

            /**
             * Properties of a PinAction.
             * @memberof proto.SyncActionValue
             * @interface IPinAction
             * @property {boolean|null} [pinned] PinAction pinned
             */

            /**
             * Constructs a new PinAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a PinAction.
             * @implements IPinAction
             * @constructor
             * @param {proto.SyncActionValue.IPinAction=} [properties] Properties to set
             */
            function PinAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PinAction pinned.
             * @member {boolean|null|undefined} pinned
             * @memberof proto.SyncActionValue.PinAction
             * @instance
             */
            PinAction.prototype.pinned = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PinAction.prototype, "_pinned", {
                get: $util.oneOfGetter($oneOfFields = ["pinned"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PinAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.PinAction
             * @static
             * @param {proto.SyncActionValue.IPinAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.PinAction} PinAction instance
             */
            PinAction.create = function create(properties) {
                return new PinAction(properties);
            };

            /**
             * Encodes the specified PinAction message. Does not implicitly {@link proto.SyncActionValue.PinAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.PinAction
             * @static
             * @param {proto.SyncActionValue.IPinAction} message PinAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PinAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.pinned != null && Object.hasOwnProperty.call(message, "pinned"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.pinned);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PinAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.PinAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.PinAction
             * @static
             * @param {proto.SyncActionValue.IPinAction} message PinAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PinAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PinAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.PinAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.PinAction} PinAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PinAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.pinned = reader.bool();
                            message._pinned = "pinned";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PinAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.PinAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.PinAction} PinAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PinAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PinAction message.
             * @function verify
             * @memberof proto.SyncActionValue.PinAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PinAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.pinned != null && Object.hasOwnProperty.call(message, "pinned")) {
                    properties._pinned = 1;
                    if (typeof message.pinned !== "boolean")
                        return "pinned: boolean expected";
                }
                return null;
            };

            /**
             * Creates a PinAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.PinAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.PinAction} PinAction
             */
            PinAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.PinAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.pinned != null)
                    message.pinned = Boolean(object.pinned);
                return message;
            };

            /**
             * Creates a plain object from a PinAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.PinAction
             * @static
             * @param {proto.SyncActionValue.PinAction} message PinAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PinAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.pinned != null && Object.hasOwnProperty.call(message, "pinned"))
                    object.pinned = message.pinned;
                return object;
            };

            /**
             * Converts this PinAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.PinAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PinAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PinAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.PinAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PinAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.PinAction";
            };

            return PinAction;
        })();

        SyncActionValue.PnForLidChatAction = (function() {

            /**
             * Properties of a PnForLidChatAction.
             * @memberof proto.SyncActionValue
             * @interface IPnForLidChatAction
             * @property {string|null} [pnJid] PnForLidChatAction pnJid
             */

            /**
             * Constructs a new PnForLidChatAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a PnForLidChatAction.
             * @implements IPnForLidChatAction
             * @constructor
             * @param {proto.SyncActionValue.IPnForLidChatAction=} [properties] Properties to set
             */
            function PnForLidChatAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PnForLidChatAction pnJid.
             * @member {string|null|undefined} pnJid
             * @memberof proto.SyncActionValue.PnForLidChatAction
             * @instance
             */
            PnForLidChatAction.prototype.pnJid = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PnForLidChatAction.prototype, "_pnJid", {
                get: $util.oneOfGetter($oneOfFields = ["pnJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PnForLidChatAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.PnForLidChatAction
             * @static
             * @param {proto.SyncActionValue.IPnForLidChatAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.PnForLidChatAction} PnForLidChatAction instance
             */
            PnForLidChatAction.create = function create(properties) {
                return new PnForLidChatAction(properties);
            };

            /**
             * Encodes the specified PnForLidChatAction message. Does not implicitly {@link proto.SyncActionValue.PnForLidChatAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.PnForLidChatAction
             * @static
             * @param {proto.SyncActionValue.IPnForLidChatAction} message PnForLidChatAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PnForLidChatAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.pnJid != null && Object.hasOwnProperty.call(message, "pnJid"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.pnJid);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PnForLidChatAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.PnForLidChatAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.PnForLidChatAction
             * @static
             * @param {proto.SyncActionValue.IPnForLidChatAction} message PnForLidChatAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PnForLidChatAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PnForLidChatAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.PnForLidChatAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.PnForLidChatAction} PnForLidChatAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PnForLidChatAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.pnJid = reader.stringVerify();
                            message._pnJid = "pnJid";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PnForLidChatAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.PnForLidChatAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.PnForLidChatAction} PnForLidChatAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PnForLidChatAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PnForLidChatAction message.
             * @function verify
             * @memberof proto.SyncActionValue.PnForLidChatAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PnForLidChatAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.pnJid != null && Object.hasOwnProperty.call(message, "pnJid")) {
                    properties._pnJid = 1;
                    if (!$util.isString(message.pnJid))
                        return "pnJid: string expected";
                }
                return null;
            };

            /**
             * Creates a PnForLidChatAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.PnForLidChatAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.PnForLidChatAction} PnForLidChatAction
             */
            PnForLidChatAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.PnForLidChatAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.pnJid != null)
                    message.pnJid = String(object.pnJid);
                return message;
            };

            /**
             * Creates a plain object from a PnForLidChatAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.PnForLidChatAction
             * @static
             * @param {proto.SyncActionValue.PnForLidChatAction} message PnForLidChatAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PnForLidChatAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.pnJid != null && Object.hasOwnProperty.call(message, "pnJid"))
                    object.pnJid = message.pnJid;
                return object;
            };

            /**
             * Converts this PnForLidChatAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.PnForLidChatAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PnForLidChatAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PnForLidChatAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.PnForLidChatAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PnForLidChatAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.PnForLidChatAction";
            };

            return PnForLidChatAction;
        })();

        SyncActionValue.PrimaryFeature = (function() {

            /**
             * Properties of a PrimaryFeature.
             * @memberof proto.SyncActionValue
             * @interface IPrimaryFeature
             * @property {Array.<string>|null} [flags] PrimaryFeature flags
             */

            /**
             * Constructs a new PrimaryFeature.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a PrimaryFeature.
             * @implements IPrimaryFeature
             * @constructor
             * @param {proto.SyncActionValue.IPrimaryFeature=} [properties] Properties to set
             */
            function PrimaryFeature(properties) {
                this.flags = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PrimaryFeature flags.
             * @member {Array.<string>} flags
             * @memberof proto.SyncActionValue.PrimaryFeature
             * @instance
             */
            PrimaryFeature.prototype.flags = $util.emptyArray;

            /**
             * Creates a new PrimaryFeature instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.PrimaryFeature
             * @static
             * @param {proto.SyncActionValue.IPrimaryFeature=} [properties] Properties to set
             * @returns {proto.SyncActionValue.PrimaryFeature} PrimaryFeature instance
             */
            PrimaryFeature.create = function create(properties) {
                return new PrimaryFeature(properties);
            };

            /**
             * Encodes the specified PrimaryFeature message. Does not implicitly {@link proto.SyncActionValue.PrimaryFeature.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.PrimaryFeature
             * @static
             * @param {proto.SyncActionValue.IPrimaryFeature} message PrimaryFeature message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PrimaryFeature.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.flags != null && message.flags.length)
                    for (var i = 0; i < message.flags.length; ++i)
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.flags[i]);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PrimaryFeature message, length delimited. Does not implicitly {@link proto.SyncActionValue.PrimaryFeature.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.PrimaryFeature
             * @static
             * @param {proto.SyncActionValue.IPrimaryFeature} message PrimaryFeature message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PrimaryFeature.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PrimaryFeature message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.PrimaryFeature
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.PrimaryFeature} PrimaryFeature
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PrimaryFeature.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(message.flags && message.flags.length))
                                message.flags = [];
                            message.flags.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PrimaryFeature message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.PrimaryFeature
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.PrimaryFeature} PrimaryFeature
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PrimaryFeature.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PrimaryFeature message.
             * @function verify
             * @memberof proto.SyncActionValue.PrimaryFeature
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PrimaryFeature.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.flags != null && Object.hasOwnProperty.call(message, "flags")) {
                    if (!Array.isArray(message.flags))
                        return "flags: array expected";
                    for (var i = 0; i < message.flags.length; ++i)
                        if (!$util.isString(message.flags[i]))
                            return "flags: string[] expected";
                }
                return null;
            };

            /**
             * Creates a PrimaryFeature message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.PrimaryFeature
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.PrimaryFeature} PrimaryFeature
             */
            PrimaryFeature.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.PrimaryFeature: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.flags) {
                    if (!Array.isArray(object.flags))
                        throw TypeError(".proto.SyncActionValue.PrimaryFeature.flags: array expected");
                    message.flags = Array(object.flags.length);
                    for (var i = 0; i < object.flags.length; ++i)
                        message.flags[i] = String(object.flags[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a PrimaryFeature message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.PrimaryFeature
             * @static
             * @param {proto.SyncActionValue.PrimaryFeature} message PrimaryFeature
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PrimaryFeature.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.flags = [];
                if (message.flags && message.flags.length) {
                    object.flags = Array(message.flags.length);
                    for (var j = 0; j < message.flags.length; ++j)
                        object.flags[j] = message.flags[j];
                }
                return object;
            };

            /**
             * Converts this PrimaryFeature to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.PrimaryFeature
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PrimaryFeature.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PrimaryFeature
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.PrimaryFeature
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PrimaryFeature.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.PrimaryFeature";
            };

            return PrimaryFeature;
        })();

        SyncActionValue.PrimaryVersionAction = (function() {

            /**
             * Properties of a PrimaryVersionAction.
             * @memberof proto.SyncActionValue
             * @interface IPrimaryVersionAction
             * @property {string|null} [version] PrimaryVersionAction version
             */

            /**
             * Constructs a new PrimaryVersionAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a PrimaryVersionAction.
             * @implements IPrimaryVersionAction
             * @constructor
             * @param {proto.SyncActionValue.IPrimaryVersionAction=} [properties] Properties to set
             */
            function PrimaryVersionAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PrimaryVersionAction version.
             * @member {string|null|undefined} version
             * @memberof proto.SyncActionValue.PrimaryVersionAction
             * @instance
             */
            PrimaryVersionAction.prototype.version = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PrimaryVersionAction.prototype, "_version", {
                get: $util.oneOfGetter($oneOfFields = ["version"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PrimaryVersionAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.PrimaryVersionAction
             * @static
             * @param {proto.SyncActionValue.IPrimaryVersionAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.PrimaryVersionAction} PrimaryVersionAction instance
             */
            PrimaryVersionAction.create = function create(properties) {
                return new PrimaryVersionAction(properties);
            };

            /**
             * Encodes the specified PrimaryVersionAction message. Does not implicitly {@link proto.SyncActionValue.PrimaryVersionAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.PrimaryVersionAction
             * @static
             * @param {proto.SyncActionValue.IPrimaryVersionAction} message PrimaryVersionAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PrimaryVersionAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.version);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PrimaryVersionAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.PrimaryVersionAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.PrimaryVersionAction
             * @static
             * @param {proto.SyncActionValue.IPrimaryVersionAction} message PrimaryVersionAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PrimaryVersionAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PrimaryVersionAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.PrimaryVersionAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.PrimaryVersionAction} PrimaryVersionAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PrimaryVersionAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.version = reader.stringVerify();
                            message._version = "version";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PrimaryVersionAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.PrimaryVersionAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.PrimaryVersionAction} PrimaryVersionAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PrimaryVersionAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PrimaryVersionAction message.
             * @function verify
             * @memberof proto.SyncActionValue.PrimaryVersionAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PrimaryVersionAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.version != null && Object.hasOwnProperty.call(message, "version")) {
                    properties._version = 1;
                    if (!$util.isString(message.version))
                        return "version: string expected";
                }
                return null;
            };

            /**
             * Creates a PrimaryVersionAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.PrimaryVersionAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.PrimaryVersionAction} PrimaryVersionAction
             */
            PrimaryVersionAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.PrimaryVersionAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.version != null)
                    message.version = String(object.version);
                return message;
            };

            /**
             * Creates a plain object from a PrimaryVersionAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.PrimaryVersionAction
             * @static
             * @param {proto.SyncActionValue.PrimaryVersionAction} message PrimaryVersionAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PrimaryVersionAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                    object.version = message.version;
                return object;
            };

            /**
             * Converts this PrimaryVersionAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.PrimaryVersionAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PrimaryVersionAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PrimaryVersionAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.PrimaryVersionAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PrimaryVersionAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.PrimaryVersionAction";
            };

            return PrimaryVersionAction;
        })();

        SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction = (function() {

            /**
             * Properties of a PrivacySettingChannelsPersonalisedRecommendationAction.
             * @memberof proto.SyncActionValue
             * @interface IPrivacySettingChannelsPersonalisedRecommendationAction
             * @property {boolean|null} [isUserOptedOut] PrivacySettingChannelsPersonalisedRecommendationAction isUserOptedOut
             */

            /**
             * Constructs a new PrivacySettingChannelsPersonalisedRecommendationAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a PrivacySettingChannelsPersonalisedRecommendationAction.
             * @implements IPrivacySettingChannelsPersonalisedRecommendationAction
             * @constructor
             * @param {proto.SyncActionValue.IPrivacySettingChannelsPersonalisedRecommendationAction=} [properties] Properties to set
             */
            function PrivacySettingChannelsPersonalisedRecommendationAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PrivacySettingChannelsPersonalisedRecommendationAction isUserOptedOut.
             * @member {boolean|null|undefined} isUserOptedOut
             * @memberof proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction
             * @instance
             */
            PrivacySettingChannelsPersonalisedRecommendationAction.prototype.isUserOptedOut = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PrivacySettingChannelsPersonalisedRecommendationAction.prototype, "_isUserOptedOut", {
                get: $util.oneOfGetter($oneOfFields = ["isUserOptedOut"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PrivacySettingChannelsPersonalisedRecommendationAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction
             * @static
             * @param {proto.SyncActionValue.IPrivacySettingChannelsPersonalisedRecommendationAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction} PrivacySettingChannelsPersonalisedRecommendationAction instance
             */
            PrivacySettingChannelsPersonalisedRecommendationAction.create = function create(properties) {
                return new PrivacySettingChannelsPersonalisedRecommendationAction(properties);
            };

            /**
             * Encodes the specified PrivacySettingChannelsPersonalisedRecommendationAction message. Does not implicitly {@link proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction
             * @static
             * @param {proto.SyncActionValue.IPrivacySettingChannelsPersonalisedRecommendationAction} message PrivacySettingChannelsPersonalisedRecommendationAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PrivacySettingChannelsPersonalisedRecommendationAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.isUserOptedOut != null && Object.hasOwnProperty.call(message, "isUserOptedOut"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.isUserOptedOut);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PrivacySettingChannelsPersonalisedRecommendationAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction
             * @static
             * @param {proto.SyncActionValue.IPrivacySettingChannelsPersonalisedRecommendationAction} message PrivacySettingChannelsPersonalisedRecommendationAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PrivacySettingChannelsPersonalisedRecommendationAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PrivacySettingChannelsPersonalisedRecommendationAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction} PrivacySettingChannelsPersonalisedRecommendationAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PrivacySettingChannelsPersonalisedRecommendationAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.isUserOptedOut = reader.bool();
                            message._isUserOptedOut = "isUserOptedOut";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PrivacySettingChannelsPersonalisedRecommendationAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction} PrivacySettingChannelsPersonalisedRecommendationAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PrivacySettingChannelsPersonalisedRecommendationAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PrivacySettingChannelsPersonalisedRecommendationAction message.
             * @function verify
             * @memberof proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PrivacySettingChannelsPersonalisedRecommendationAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.isUserOptedOut != null && Object.hasOwnProperty.call(message, "isUserOptedOut")) {
                    properties._isUserOptedOut = 1;
                    if (typeof message.isUserOptedOut !== "boolean")
                        return "isUserOptedOut: boolean expected";
                }
                return null;
            };

            /**
             * Creates a PrivacySettingChannelsPersonalisedRecommendationAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction} PrivacySettingChannelsPersonalisedRecommendationAction
             */
            PrivacySettingChannelsPersonalisedRecommendationAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.isUserOptedOut != null)
                    message.isUserOptedOut = Boolean(object.isUserOptedOut);
                return message;
            };

            /**
             * Creates a plain object from a PrivacySettingChannelsPersonalisedRecommendationAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction
             * @static
             * @param {proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction} message PrivacySettingChannelsPersonalisedRecommendationAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PrivacySettingChannelsPersonalisedRecommendationAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.isUserOptedOut != null && Object.hasOwnProperty.call(message, "isUserOptedOut"))
                    object.isUserOptedOut = message.isUserOptedOut;
                return object;
            };

            /**
             * Converts this PrivacySettingChannelsPersonalisedRecommendationAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PrivacySettingChannelsPersonalisedRecommendationAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PrivacySettingChannelsPersonalisedRecommendationAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PrivacySettingChannelsPersonalisedRecommendationAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction";
            };

            return PrivacySettingChannelsPersonalisedRecommendationAction;
        })();

        SyncActionValue.PrivacySettingDisableLinkPreviewsAction = (function() {

            /**
             * Properties of a PrivacySettingDisableLinkPreviewsAction.
             * @memberof proto.SyncActionValue
             * @interface IPrivacySettingDisableLinkPreviewsAction
             * @property {boolean|null} [isPreviewsDisabled] PrivacySettingDisableLinkPreviewsAction isPreviewsDisabled
             */

            /**
             * Constructs a new PrivacySettingDisableLinkPreviewsAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a PrivacySettingDisableLinkPreviewsAction.
             * @implements IPrivacySettingDisableLinkPreviewsAction
             * @constructor
             * @param {proto.SyncActionValue.IPrivacySettingDisableLinkPreviewsAction=} [properties] Properties to set
             */
            function PrivacySettingDisableLinkPreviewsAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PrivacySettingDisableLinkPreviewsAction isPreviewsDisabled.
             * @member {boolean|null|undefined} isPreviewsDisabled
             * @memberof proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction
             * @instance
             */
            PrivacySettingDisableLinkPreviewsAction.prototype.isPreviewsDisabled = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PrivacySettingDisableLinkPreviewsAction.prototype, "_isPreviewsDisabled", {
                get: $util.oneOfGetter($oneOfFields = ["isPreviewsDisabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PrivacySettingDisableLinkPreviewsAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction
             * @static
             * @param {proto.SyncActionValue.IPrivacySettingDisableLinkPreviewsAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction} PrivacySettingDisableLinkPreviewsAction instance
             */
            PrivacySettingDisableLinkPreviewsAction.create = function create(properties) {
                return new PrivacySettingDisableLinkPreviewsAction(properties);
            };

            /**
             * Encodes the specified PrivacySettingDisableLinkPreviewsAction message. Does not implicitly {@link proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction
             * @static
             * @param {proto.SyncActionValue.IPrivacySettingDisableLinkPreviewsAction} message PrivacySettingDisableLinkPreviewsAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PrivacySettingDisableLinkPreviewsAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.isPreviewsDisabled != null && Object.hasOwnProperty.call(message, "isPreviewsDisabled"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.isPreviewsDisabled);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PrivacySettingDisableLinkPreviewsAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction
             * @static
             * @param {proto.SyncActionValue.IPrivacySettingDisableLinkPreviewsAction} message PrivacySettingDisableLinkPreviewsAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PrivacySettingDisableLinkPreviewsAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PrivacySettingDisableLinkPreviewsAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction} PrivacySettingDisableLinkPreviewsAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PrivacySettingDisableLinkPreviewsAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.isPreviewsDisabled = reader.bool();
                            message._isPreviewsDisabled = "isPreviewsDisabled";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PrivacySettingDisableLinkPreviewsAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction} PrivacySettingDisableLinkPreviewsAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PrivacySettingDisableLinkPreviewsAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PrivacySettingDisableLinkPreviewsAction message.
             * @function verify
             * @memberof proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PrivacySettingDisableLinkPreviewsAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.isPreviewsDisabled != null && Object.hasOwnProperty.call(message, "isPreviewsDisabled")) {
                    properties._isPreviewsDisabled = 1;
                    if (typeof message.isPreviewsDisabled !== "boolean")
                        return "isPreviewsDisabled: boolean expected";
                }
                return null;
            };

            /**
             * Creates a PrivacySettingDisableLinkPreviewsAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction} PrivacySettingDisableLinkPreviewsAction
             */
            PrivacySettingDisableLinkPreviewsAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.isPreviewsDisabled != null)
                    message.isPreviewsDisabled = Boolean(object.isPreviewsDisabled);
                return message;
            };

            /**
             * Creates a plain object from a PrivacySettingDisableLinkPreviewsAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction
             * @static
             * @param {proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction} message PrivacySettingDisableLinkPreviewsAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PrivacySettingDisableLinkPreviewsAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.isPreviewsDisabled != null && Object.hasOwnProperty.call(message, "isPreviewsDisabled"))
                    object.isPreviewsDisabled = message.isPreviewsDisabled;
                return object;
            };

            /**
             * Converts this PrivacySettingDisableLinkPreviewsAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PrivacySettingDisableLinkPreviewsAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PrivacySettingDisableLinkPreviewsAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PrivacySettingDisableLinkPreviewsAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction";
            };

            return PrivacySettingDisableLinkPreviewsAction;
        })();

        SyncActionValue.PrivacySettingRelayAllCalls = (function() {

            /**
             * Properties of a PrivacySettingRelayAllCalls.
             * @memberof proto.SyncActionValue
             * @interface IPrivacySettingRelayAllCalls
             * @property {boolean|null} [isEnabled] PrivacySettingRelayAllCalls isEnabled
             */

            /**
             * Constructs a new PrivacySettingRelayAllCalls.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a PrivacySettingRelayAllCalls.
             * @implements IPrivacySettingRelayAllCalls
             * @constructor
             * @param {proto.SyncActionValue.IPrivacySettingRelayAllCalls=} [properties] Properties to set
             */
            function PrivacySettingRelayAllCalls(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PrivacySettingRelayAllCalls isEnabled.
             * @member {boolean|null|undefined} isEnabled
             * @memberof proto.SyncActionValue.PrivacySettingRelayAllCalls
             * @instance
             */
            PrivacySettingRelayAllCalls.prototype.isEnabled = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PrivacySettingRelayAllCalls.prototype, "_isEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PrivacySettingRelayAllCalls instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.PrivacySettingRelayAllCalls
             * @static
             * @param {proto.SyncActionValue.IPrivacySettingRelayAllCalls=} [properties] Properties to set
             * @returns {proto.SyncActionValue.PrivacySettingRelayAllCalls} PrivacySettingRelayAllCalls instance
             */
            PrivacySettingRelayAllCalls.create = function create(properties) {
                return new PrivacySettingRelayAllCalls(properties);
            };

            /**
             * Encodes the specified PrivacySettingRelayAllCalls message. Does not implicitly {@link proto.SyncActionValue.PrivacySettingRelayAllCalls.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.PrivacySettingRelayAllCalls
             * @static
             * @param {proto.SyncActionValue.IPrivacySettingRelayAllCalls} message PrivacySettingRelayAllCalls message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PrivacySettingRelayAllCalls.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.isEnabled != null && Object.hasOwnProperty.call(message, "isEnabled"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.isEnabled);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PrivacySettingRelayAllCalls message, length delimited. Does not implicitly {@link proto.SyncActionValue.PrivacySettingRelayAllCalls.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.PrivacySettingRelayAllCalls
             * @static
             * @param {proto.SyncActionValue.IPrivacySettingRelayAllCalls} message PrivacySettingRelayAllCalls message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PrivacySettingRelayAllCalls.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PrivacySettingRelayAllCalls message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.PrivacySettingRelayAllCalls
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.PrivacySettingRelayAllCalls} PrivacySettingRelayAllCalls
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PrivacySettingRelayAllCalls.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.isEnabled = reader.bool();
                            message._isEnabled = "isEnabled";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PrivacySettingRelayAllCalls message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.PrivacySettingRelayAllCalls
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.PrivacySettingRelayAllCalls} PrivacySettingRelayAllCalls
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PrivacySettingRelayAllCalls.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PrivacySettingRelayAllCalls message.
             * @function verify
             * @memberof proto.SyncActionValue.PrivacySettingRelayAllCalls
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PrivacySettingRelayAllCalls.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.isEnabled != null && Object.hasOwnProperty.call(message, "isEnabled")) {
                    properties._isEnabled = 1;
                    if (typeof message.isEnabled !== "boolean")
                        return "isEnabled: boolean expected";
                }
                return null;
            };

            /**
             * Creates a PrivacySettingRelayAllCalls message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.PrivacySettingRelayAllCalls
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.PrivacySettingRelayAllCalls} PrivacySettingRelayAllCalls
             */
            PrivacySettingRelayAllCalls.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.PrivacySettingRelayAllCalls: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.isEnabled != null)
                    message.isEnabled = Boolean(object.isEnabled);
                return message;
            };

            /**
             * Creates a plain object from a PrivacySettingRelayAllCalls message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.PrivacySettingRelayAllCalls
             * @static
             * @param {proto.SyncActionValue.PrivacySettingRelayAllCalls} message PrivacySettingRelayAllCalls
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PrivacySettingRelayAllCalls.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.isEnabled != null && Object.hasOwnProperty.call(message, "isEnabled"))
                    object.isEnabled = message.isEnabled;
                return object;
            };

            /**
             * Converts this PrivacySettingRelayAllCalls to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.PrivacySettingRelayAllCalls
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PrivacySettingRelayAllCalls.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PrivacySettingRelayAllCalls
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.PrivacySettingRelayAllCalls
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PrivacySettingRelayAllCalls.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.PrivacySettingRelayAllCalls";
            };

            return PrivacySettingRelayAllCalls;
        })();

        SyncActionValue.PrivateProcessingSettingAction = (function() {

            /**
             * Properties of a PrivateProcessingSettingAction.
             * @memberof proto.SyncActionValue
             * @interface IPrivateProcessingSettingAction
             * @property {proto.SyncActionValue.PrivateProcessingSettingAction.PrivateProcessingStatus|null} [privateProcessingStatus] PrivateProcessingSettingAction privateProcessingStatus
             */

            /**
             * Constructs a new PrivateProcessingSettingAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a PrivateProcessingSettingAction.
             * @implements IPrivateProcessingSettingAction
             * @constructor
             * @param {proto.SyncActionValue.IPrivateProcessingSettingAction=} [properties] Properties to set
             */
            function PrivateProcessingSettingAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PrivateProcessingSettingAction privateProcessingStatus.
             * @member {proto.SyncActionValue.PrivateProcessingSettingAction.PrivateProcessingStatus|null|undefined} privateProcessingStatus
             * @memberof proto.SyncActionValue.PrivateProcessingSettingAction
             * @instance
             */
            PrivateProcessingSettingAction.prototype.privateProcessingStatus = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PrivateProcessingSettingAction.prototype, "_privateProcessingStatus", {
                get: $util.oneOfGetter($oneOfFields = ["privateProcessingStatus"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PrivateProcessingSettingAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.PrivateProcessingSettingAction
             * @static
             * @param {proto.SyncActionValue.IPrivateProcessingSettingAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.PrivateProcessingSettingAction} PrivateProcessingSettingAction instance
             */
            PrivateProcessingSettingAction.create = function create(properties) {
                return new PrivateProcessingSettingAction(properties);
            };

            /**
             * Encodes the specified PrivateProcessingSettingAction message. Does not implicitly {@link proto.SyncActionValue.PrivateProcessingSettingAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.PrivateProcessingSettingAction
             * @static
             * @param {proto.SyncActionValue.IPrivateProcessingSettingAction} message PrivateProcessingSettingAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PrivateProcessingSettingAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.privateProcessingStatus != null && Object.hasOwnProperty.call(message, "privateProcessingStatus"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.privateProcessingStatus);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PrivateProcessingSettingAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.PrivateProcessingSettingAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.PrivateProcessingSettingAction
             * @static
             * @param {proto.SyncActionValue.IPrivateProcessingSettingAction} message PrivateProcessingSettingAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PrivateProcessingSettingAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PrivateProcessingSettingAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.PrivateProcessingSettingAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.PrivateProcessingSettingAction} PrivateProcessingSettingAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PrivateProcessingSettingAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.privateProcessingStatus = reader.int32();
                            message._privateProcessingStatus = "privateProcessingStatus";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PrivateProcessingSettingAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.PrivateProcessingSettingAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.PrivateProcessingSettingAction} PrivateProcessingSettingAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PrivateProcessingSettingAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PrivateProcessingSettingAction message.
             * @function verify
             * @memberof proto.SyncActionValue.PrivateProcessingSettingAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PrivateProcessingSettingAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.privateProcessingStatus != null && Object.hasOwnProperty.call(message, "privateProcessingStatus")) {
                    properties._privateProcessingStatus = 1;
                    if (typeof message.privateProcessingStatus !== "number" || (message.privateProcessingStatus | 0) !== message.privateProcessingStatus)
                        return "privateProcessingStatus: enum value expected";
                }
                return null;
            };

            /**
             * Creates a PrivateProcessingSettingAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.PrivateProcessingSettingAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.PrivateProcessingSettingAction} PrivateProcessingSettingAction
             */
            PrivateProcessingSettingAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.PrivateProcessingSettingAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.privateProcessingStatus) {
                case "UNDEFINED":
                case 0:
                    message.privateProcessingStatus = 0;
                    break;
                case "ENABLED":
                case 1:
                    message.privateProcessingStatus = 1;
                    break;
                case "DISABLED":
                case 2:
                    message.privateProcessingStatus = 2;
                    break;
                default:
                    if (typeof object.privateProcessingStatus === "number" && (object.privateProcessingStatus | 0) === object.privateProcessingStatus)
                        message.privateProcessingStatus = object.privateProcessingStatus;
                }
                return message;
            };

            /**
             * Creates a plain object from a PrivateProcessingSettingAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.PrivateProcessingSettingAction
             * @static
             * @param {proto.SyncActionValue.PrivateProcessingSettingAction} message PrivateProcessingSettingAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PrivateProcessingSettingAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.privateProcessingStatus != null && Object.hasOwnProperty.call(message, "privateProcessingStatus"))
                    object.privateProcessingStatus = options.enums === String ? $root.proto.SyncActionValue.PrivateProcessingSettingAction.PrivateProcessingStatus[message.privateProcessingStatus] === undefined ? message.privateProcessingStatus : $root.proto.SyncActionValue.PrivateProcessingSettingAction.PrivateProcessingStatus[message.privateProcessingStatus] : message.privateProcessingStatus;
                return object;
            };

            /**
             * Converts this PrivateProcessingSettingAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.PrivateProcessingSettingAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PrivateProcessingSettingAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PrivateProcessingSettingAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.PrivateProcessingSettingAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PrivateProcessingSettingAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.PrivateProcessingSettingAction";
            };

            /**
             * PrivateProcessingStatus enum.
             * @name proto.SyncActionValue.PrivateProcessingSettingAction.PrivateProcessingStatus
             * @enum {number}
             * @property {number} UNDEFINED=0 UNDEFINED value
             * @property {number} ENABLED=1 ENABLED value
             * @property {number} DISABLED=2 DISABLED value
             */
            PrivateProcessingSettingAction.PrivateProcessingStatus = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNDEFINED"] = 0;
                values[valuesById[1] = "ENABLED"] = 1;
                values[valuesById[2] = "DISABLED"] = 2;
                return values;
            })();

            return PrivateProcessingSettingAction;
        })();

        SyncActionValue.PushNameSetting = (function() {

            /**
             * Properties of a PushNameSetting.
             * @memberof proto.SyncActionValue
             * @interface IPushNameSetting
             * @property {string|null} [name] PushNameSetting name
             */

            /**
             * Constructs a new PushNameSetting.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a PushNameSetting.
             * @implements IPushNameSetting
             * @constructor
             * @param {proto.SyncActionValue.IPushNameSetting=} [properties] Properties to set
             */
            function PushNameSetting(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * PushNameSetting name.
             * @member {string|null|undefined} name
             * @memberof proto.SyncActionValue.PushNameSetting
             * @instance
             */
            PushNameSetting.prototype.name = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PushNameSetting.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new PushNameSetting instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.PushNameSetting
             * @static
             * @param {proto.SyncActionValue.IPushNameSetting=} [properties] Properties to set
             * @returns {proto.SyncActionValue.PushNameSetting} PushNameSetting instance
             */
            PushNameSetting.create = function create(properties) {
                return new PushNameSetting(properties);
            };

            /**
             * Encodes the specified PushNameSetting message. Does not implicitly {@link proto.SyncActionValue.PushNameSetting.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.PushNameSetting
             * @static
             * @param {proto.SyncActionValue.IPushNameSetting} message PushNameSetting message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PushNameSetting.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified PushNameSetting message, length delimited. Does not implicitly {@link proto.SyncActionValue.PushNameSetting.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.PushNameSetting
             * @static
             * @param {proto.SyncActionValue.IPushNameSetting} message PushNameSetting message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            PushNameSetting.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a PushNameSetting message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.PushNameSetting
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.PushNameSetting} PushNameSetting
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PushNameSetting.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.name = reader.stringVerify();
                            message._name = "name";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a PushNameSetting message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.PushNameSetting
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.PushNameSetting} PushNameSetting
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            PushNameSetting.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a PushNameSetting message.
             * @function verify
             * @memberof proto.SyncActionValue.PushNameSetting
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            PushNameSetting.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.name != null && Object.hasOwnProperty.call(message, "name")) {
                    properties._name = 1;
                    if (!$util.isString(message.name))
                        return "name: string expected";
                }
                return null;
            };

            /**
             * Creates a PushNameSetting message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.PushNameSetting
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.PushNameSetting} PushNameSetting
             */
            PushNameSetting.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.PushNameSetting: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.name != null)
                    message.name = String(object.name);
                return message;
            };

            /**
             * Creates a plain object from a PushNameSetting message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.PushNameSetting
             * @static
             * @param {proto.SyncActionValue.PushNameSetting} message PushNameSetting
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            PushNameSetting.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                return object;
            };

            /**
             * Converts this PushNameSetting to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.PushNameSetting
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            PushNameSetting.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for PushNameSetting
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.PushNameSetting
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            PushNameSetting.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.PushNameSetting";
            };

            return PushNameSetting;
        })();

        SyncActionValue.QuickReplyAction = (function() {

            /**
             * Properties of a QuickReplyAction.
             * @memberof proto.SyncActionValue
             * @interface IQuickReplyAction
             * @property {string|null} [shortcut] QuickReplyAction shortcut
             * @property {string|null} [message] QuickReplyAction message
             * @property {Array.<string>|null} [keywords] QuickReplyAction keywords
             * @property {number|null} [count] QuickReplyAction count
             * @property {boolean|null} [deleted] QuickReplyAction deleted
             * @property {Array.<string>|null} [associatedLabelIds] QuickReplyAction associatedLabelIds
             */

            /**
             * Constructs a new QuickReplyAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a QuickReplyAction.
             * @implements IQuickReplyAction
             * @constructor
             * @param {proto.SyncActionValue.IQuickReplyAction=} [properties] Properties to set
             */
            function QuickReplyAction(properties) {
                this.keywords = [];
                this.associatedLabelIds = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * QuickReplyAction shortcut.
             * @member {string|null|undefined} shortcut
             * @memberof proto.SyncActionValue.QuickReplyAction
             * @instance
             */
            QuickReplyAction.prototype.shortcut = null;

            /**
             * QuickReplyAction message.
             * @member {string|null|undefined} message
             * @memberof proto.SyncActionValue.QuickReplyAction
             * @instance
             */
            QuickReplyAction.prototype.message = null;

            /**
             * QuickReplyAction keywords.
             * @member {Array.<string>} keywords
             * @memberof proto.SyncActionValue.QuickReplyAction
             * @instance
             */
            QuickReplyAction.prototype.keywords = $util.emptyArray;

            /**
             * QuickReplyAction count.
             * @member {number|null|undefined} count
             * @memberof proto.SyncActionValue.QuickReplyAction
             * @instance
             */
            QuickReplyAction.prototype.count = null;

            /**
             * QuickReplyAction deleted.
             * @member {boolean|null|undefined} deleted
             * @memberof proto.SyncActionValue.QuickReplyAction
             * @instance
             */
            QuickReplyAction.prototype.deleted = null;

            /**
             * QuickReplyAction associatedLabelIds.
             * @member {Array.<string>} associatedLabelIds
             * @memberof proto.SyncActionValue.QuickReplyAction
             * @instance
             */
            QuickReplyAction.prototype.associatedLabelIds = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(QuickReplyAction.prototype, "_shortcut", {
                get: $util.oneOfGetter($oneOfFields = ["shortcut"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(QuickReplyAction.prototype, "_message", {
                get: $util.oneOfGetter($oneOfFields = ["message"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(QuickReplyAction.prototype, "_count", {
                get: $util.oneOfGetter($oneOfFields = ["count"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(QuickReplyAction.prototype, "_deleted", {
                get: $util.oneOfGetter($oneOfFields = ["deleted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new QuickReplyAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.QuickReplyAction
             * @static
             * @param {proto.SyncActionValue.IQuickReplyAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.QuickReplyAction} QuickReplyAction instance
             */
            QuickReplyAction.create = function create(properties) {
                return new QuickReplyAction(properties);
            };

            /**
             * Encodes the specified QuickReplyAction message. Does not implicitly {@link proto.SyncActionValue.QuickReplyAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.QuickReplyAction
             * @static
             * @param {proto.SyncActionValue.IQuickReplyAction} message QuickReplyAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            QuickReplyAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.shortcut != null && Object.hasOwnProperty.call(message, "shortcut"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.shortcut);
                if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.message);
                if (message.keywords != null && message.keywords.length)
                    for (var i = 0; i < message.keywords.length; ++i)
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.keywords[i]);
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    writer.uint32(/* id 4, wireType 0 =*/32).int32(message.count);
                if (message.deleted != null && Object.hasOwnProperty.call(message, "deleted"))
                    writer.uint32(/* id 5, wireType 0 =*/40).bool(message.deleted);
                if (message.associatedLabelIds != null && message.associatedLabelIds.length)
                    for (var i = 0; i < message.associatedLabelIds.length; ++i)
                        writer.uint32(/* id 6, wireType 2 =*/50).string(message.associatedLabelIds[i]);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified QuickReplyAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.QuickReplyAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.QuickReplyAction
             * @static
             * @param {proto.SyncActionValue.IQuickReplyAction} message QuickReplyAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            QuickReplyAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a QuickReplyAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.QuickReplyAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.QuickReplyAction} QuickReplyAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            QuickReplyAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.shortcut = reader.stringVerify();
                            message._shortcut = "shortcut";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.message = reader.stringVerify();
                            message._message = "message";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            if (!(message.keywords && message.keywords.length))
                                message.keywords = [];
                            message.keywords.push(reader.stringVerify());
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.count = reader.int32();
                            message._count = "count";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.deleted = reader.bool();
                            message._deleted = "deleted";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            if (!(message.associatedLabelIds && message.associatedLabelIds.length))
                                message.associatedLabelIds = [];
                            message.associatedLabelIds.push(reader.stringVerify());
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a QuickReplyAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.QuickReplyAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.QuickReplyAction} QuickReplyAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            QuickReplyAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a QuickReplyAction message.
             * @function verify
             * @memberof proto.SyncActionValue.QuickReplyAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            QuickReplyAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.shortcut != null && Object.hasOwnProperty.call(message, "shortcut")) {
                    properties._shortcut = 1;
                    if (!$util.isString(message.shortcut))
                        return "shortcut: string expected";
                }
                if (message.message != null && Object.hasOwnProperty.call(message, "message")) {
                    properties._message = 1;
                    if (!$util.isString(message.message))
                        return "message: string expected";
                }
                if (message.keywords != null && Object.hasOwnProperty.call(message, "keywords")) {
                    if (!Array.isArray(message.keywords))
                        return "keywords: array expected";
                    for (var i = 0; i < message.keywords.length; ++i)
                        if (!$util.isString(message.keywords[i]))
                            return "keywords: string[] expected";
                }
                if (message.count != null && Object.hasOwnProperty.call(message, "count")) {
                    properties._count = 1;
                    if (!$util.isInteger(message.count))
                        return "count: integer expected";
                }
                if (message.deleted != null && Object.hasOwnProperty.call(message, "deleted")) {
                    properties._deleted = 1;
                    if (typeof message.deleted !== "boolean")
                        return "deleted: boolean expected";
                }
                if (message.associatedLabelIds != null && Object.hasOwnProperty.call(message, "associatedLabelIds")) {
                    if (!Array.isArray(message.associatedLabelIds))
                        return "associatedLabelIds: array expected";
                    for (var i = 0; i < message.associatedLabelIds.length; ++i)
                        if (!$util.isString(message.associatedLabelIds[i]))
                            return "associatedLabelIds: string[] expected";
                }
                return null;
            };

            /**
             * Creates a QuickReplyAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.QuickReplyAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.QuickReplyAction} QuickReplyAction
             */
            QuickReplyAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.QuickReplyAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.shortcut != null)
                    message.shortcut = String(object.shortcut);
                if (object.message != null)
                    message.message = String(object.message);
                if (object.keywords) {
                    if (!Array.isArray(object.keywords))
                        throw TypeError(".proto.SyncActionValue.QuickReplyAction.keywords: array expected");
                    message.keywords = Array(object.keywords.length);
                    for (var i = 0; i < object.keywords.length; ++i)
                        message.keywords[i] = String(object.keywords[i]);
                }
                if (object.count != null)
                    message.count = object.count | 0;
                if (object.deleted != null)
                    message.deleted = Boolean(object.deleted);
                if (object.associatedLabelIds) {
                    if (!Array.isArray(object.associatedLabelIds))
                        throw TypeError(".proto.SyncActionValue.QuickReplyAction.associatedLabelIds: array expected");
                    message.associatedLabelIds = Array(object.associatedLabelIds.length);
                    for (var i = 0; i < object.associatedLabelIds.length; ++i)
                        message.associatedLabelIds[i] = String(object.associatedLabelIds[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a QuickReplyAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.QuickReplyAction
             * @static
             * @param {proto.SyncActionValue.QuickReplyAction} message QuickReplyAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            QuickReplyAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.keywords = [];
                    object.associatedLabelIds = [];
                }
                if (message.shortcut != null && Object.hasOwnProperty.call(message, "shortcut"))
                    object.shortcut = message.shortcut;
                if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                    object.message = message.message;
                if (message.keywords && message.keywords.length) {
                    object.keywords = Array(message.keywords.length);
                    for (var j = 0; j < message.keywords.length; ++j)
                        object.keywords[j] = message.keywords[j];
                }
                if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                    object.count = message.count;
                if (message.deleted != null && Object.hasOwnProperty.call(message, "deleted"))
                    object.deleted = message.deleted;
                if (message.associatedLabelIds && message.associatedLabelIds.length) {
                    object.associatedLabelIds = Array(message.associatedLabelIds.length);
                    for (var j = 0; j < message.associatedLabelIds.length; ++j)
                        object.associatedLabelIds[j] = message.associatedLabelIds[j];
                }
                return object;
            };

            /**
             * Converts this QuickReplyAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.QuickReplyAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            QuickReplyAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for QuickReplyAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.QuickReplyAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            QuickReplyAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.QuickReplyAction";
            };

            return QuickReplyAction;
        })();

        SyncActionValue.RecentEmojiWeightsAction = (function() {

            /**
             * Properties of a RecentEmojiWeightsAction.
             * @memberof proto.SyncActionValue
             * @interface IRecentEmojiWeightsAction
             * @property {Array.<proto.IRecentEmojiWeight>|null} [weights] RecentEmojiWeightsAction weights
             */

            /**
             * Constructs a new RecentEmojiWeightsAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a RecentEmojiWeightsAction.
             * @implements IRecentEmojiWeightsAction
             * @constructor
             * @param {proto.SyncActionValue.IRecentEmojiWeightsAction=} [properties] Properties to set
             */
            function RecentEmojiWeightsAction(properties) {
                this.weights = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * RecentEmojiWeightsAction weights.
             * @member {Array.<proto.IRecentEmojiWeight>} weights
             * @memberof proto.SyncActionValue.RecentEmojiWeightsAction
             * @instance
             */
            RecentEmojiWeightsAction.prototype.weights = $util.emptyArray;

            /**
             * Creates a new RecentEmojiWeightsAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.RecentEmojiWeightsAction
             * @static
             * @param {proto.SyncActionValue.IRecentEmojiWeightsAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.RecentEmojiWeightsAction} RecentEmojiWeightsAction instance
             */
            RecentEmojiWeightsAction.create = function create(properties) {
                return new RecentEmojiWeightsAction(properties);
            };

            /**
             * Encodes the specified RecentEmojiWeightsAction message. Does not implicitly {@link proto.SyncActionValue.RecentEmojiWeightsAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.RecentEmojiWeightsAction
             * @static
             * @param {proto.SyncActionValue.IRecentEmojiWeightsAction} message RecentEmojiWeightsAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RecentEmojiWeightsAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.weights != null && message.weights.length)
                    for (var i = 0; i < message.weights.length; ++i)
                        $root.proto.RecentEmojiWeight.encode(message.weights[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified RecentEmojiWeightsAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.RecentEmojiWeightsAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.RecentEmojiWeightsAction
             * @static
             * @param {proto.SyncActionValue.IRecentEmojiWeightsAction} message RecentEmojiWeightsAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RecentEmojiWeightsAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a RecentEmojiWeightsAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.RecentEmojiWeightsAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.RecentEmojiWeightsAction} RecentEmojiWeightsAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RecentEmojiWeightsAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(message.weights && message.weights.length))
                                message.weights = [];
                            message.weights.push($root.proto.RecentEmojiWeight.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a RecentEmojiWeightsAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.RecentEmojiWeightsAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.RecentEmojiWeightsAction} RecentEmojiWeightsAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RecentEmojiWeightsAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a RecentEmojiWeightsAction message.
             * @function verify
             * @memberof proto.SyncActionValue.RecentEmojiWeightsAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RecentEmojiWeightsAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.weights != null && Object.hasOwnProperty.call(message, "weights")) {
                    if (!Array.isArray(message.weights))
                        return "weights: array expected";
                    for (var i = 0; i < message.weights.length; ++i) {
                        var error = $root.proto.RecentEmojiWeight.verify(message.weights[i], q + 1);
                        if (error)
                            return "weights." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a RecentEmojiWeightsAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.RecentEmojiWeightsAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.RecentEmojiWeightsAction} RecentEmojiWeightsAction
             */
            RecentEmojiWeightsAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.RecentEmojiWeightsAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.weights) {
                    if (!Array.isArray(object.weights))
                        throw TypeError(".proto.SyncActionValue.RecentEmojiWeightsAction.weights: array expected");
                    message.weights = Array(object.weights.length);
                    for (var i = 0; i < object.weights.length; ++i) {
                        if (!$util.isObject(object.weights[i]))
                            throw TypeError(".proto.SyncActionValue.RecentEmojiWeightsAction.weights: object expected");
                        message.weights[i] = $root.proto.RecentEmojiWeight.fromObject(object.weights[i], q + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a RecentEmojiWeightsAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.RecentEmojiWeightsAction
             * @static
             * @param {proto.SyncActionValue.RecentEmojiWeightsAction} message RecentEmojiWeightsAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RecentEmojiWeightsAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.weights = [];
                if (message.weights && message.weights.length) {
                    object.weights = Array(message.weights.length);
                    for (var j = 0; j < message.weights.length; ++j)
                        object.weights[j] = $root.proto.RecentEmojiWeight.toObject(message.weights[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this RecentEmojiWeightsAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.RecentEmojiWeightsAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RecentEmojiWeightsAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for RecentEmojiWeightsAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.RecentEmojiWeightsAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            RecentEmojiWeightsAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.RecentEmojiWeightsAction";
            };

            return RecentEmojiWeightsAction;
        })();

        SyncActionValue.RemoveRecentStickerAction = (function() {

            /**
             * Properties of a RemoveRecentStickerAction.
             * @memberof proto.SyncActionValue
             * @interface IRemoveRecentStickerAction
             * @property {number|Long|null} [lastStickerSentTs] RemoveRecentStickerAction lastStickerSentTs
             */

            /**
             * Constructs a new RemoveRecentStickerAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a RemoveRecentStickerAction.
             * @implements IRemoveRecentStickerAction
             * @constructor
             * @param {proto.SyncActionValue.IRemoveRecentStickerAction=} [properties] Properties to set
             */
            function RemoveRecentStickerAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * RemoveRecentStickerAction lastStickerSentTs.
             * @member {number|Long|null|undefined} lastStickerSentTs
             * @memberof proto.SyncActionValue.RemoveRecentStickerAction
             * @instance
             */
            RemoveRecentStickerAction.prototype.lastStickerSentTs = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(RemoveRecentStickerAction.prototype, "_lastStickerSentTs", {
                get: $util.oneOfGetter($oneOfFields = ["lastStickerSentTs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new RemoveRecentStickerAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.RemoveRecentStickerAction
             * @static
             * @param {proto.SyncActionValue.IRemoveRecentStickerAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.RemoveRecentStickerAction} RemoveRecentStickerAction instance
             */
            RemoveRecentStickerAction.create = function create(properties) {
                return new RemoveRecentStickerAction(properties);
            };

            /**
             * Encodes the specified RemoveRecentStickerAction message. Does not implicitly {@link proto.SyncActionValue.RemoveRecentStickerAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.RemoveRecentStickerAction
             * @static
             * @param {proto.SyncActionValue.IRemoveRecentStickerAction} message RemoveRecentStickerAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RemoveRecentStickerAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.lastStickerSentTs != null && Object.hasOwnProperty.call(message, "lastStickerSentTs"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int64(message.lastStickerSentTs);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified RemoveRecentStickerAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.RemoveRecentStickerAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.RemoveRecentStickerAction
             * @static
             * @param {proto.SyncActionValue.IRemoveRecentStickerAction} message RemoveRecentStickerAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            RemoveRecentStickerAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a RemoveRecentStickerAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.RemoveRecentStickerAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.RemoveRecentStickerAction} RemoveRecentStickerAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RemoveRecentStickerAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.lastStickerSentTs = reader.int64();
                            message._lastStickerSentTs = "lastStickerSentTs";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a RemoveRecentStickerAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.RemoveRecentStickerAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.RemoveRecentStickerAction} RemoveRecentStickerAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            RemoveRecentStickerAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a RemoveRecentStickerAction message.
             * @function verify
             * @memberof proto.SyncActionValue.RemoveRecentStickerAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            RemoveRecentStickerAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.lastStickerSentTs != null && Object.hasOwnProperty.call(message, "lastStickerSentTs")) {
                    properties._lastStickerSentTs = 1;
                    if (!$util.isInteger(message.lastStickerSentTs) && !(message.lastStickerSentTs && $util.isInteger(message.lastStickerSentTs.low) && $util.isInteger(message.lastStickerSentTs.high)))
                        return "lastStickerSentTs: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a RemoveRecentStickerAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.RemoveRecentStickerAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.RemoveRecentStickerAction} RemoveRecentStickerAction
             */
            RemoveRecentStickerAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.RemoveRecentStickerAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.lastStickerSentTs != null)
                    if ($util.Long)
                        message.lastStickerSentTs = $util.Long.fromValue(object.lastStickerSentTs, false);
                    else if (typeof object.lastStickerSentTs === "string")
                        message.lastStickerSentTs = parseInt(object.lastStickerSentTs, 10);
                    else if (typeof object.lastStickerSentTs === "number")
                        message.lastStickerSentTs = object.lastStickerSentTs;
                    else if (typeof object.lastStickerSentTs === "object")
                        message.lastStickerSentTs = new $util.LongBits(object.lastStickerSentTs.low >>> 0, object.lastStickerSentTs.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a RemoveRecentStickerAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.RemoveRecentStickerAction
             * @static
             * @param {proto.SyncActionValue.RemoveRecentStickerAction} message RemoveRecentStickerAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            RemoveRecentStickerAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.lastStickerSentTs != null && Object.hasOwnProperty.call(message, "lastStickerSentTs"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.lastStickerSentTs = typeof message.lastStickerSentTs === "number" ? BigInt(message.lastStickerSentTs) : $util.Long.fromBits(message.lastStickerSentTs.low >>> 0, message.lastStickerSentTs.high >>> 0, false).toBigInt();
                    else if (typeof message.lastStickerSentTs === "number")
                        object.lastStickerSentTs = options.longs === String ? String(message.lastStickerSentTs) : message.lastStickerSentTs;
                    else
                        object.lastStickerSentTs = options.longs === String ? $util.Long.prototype.toString.call(message.lastStickerSentTs) : options.longs === Number ? new $util.LongBits(message.lastStickerSentTs.low >>> 0, message.lastStickerSentTs.high >>> 0).toNumber() : message.lastStickerSentTs;
                return object;
            };

            /**
             * Converts this RemoveRecentStickerAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.RemoveRecentStickerAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            RemoveRecentStickerAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for RemoveRecentStickerAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.RemoveRecentStickerAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            RemoveRecentStickerAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.RemoveRecentStickerAction";
            };

            return RemoveRecentStickerAction;
        })();

        SyncActionValue.SettingsSyncAction = (function() {

            /**
             * Properties of a SettingsSyncAction.
             * @memberof proto.SyncActionValue
             * @interface ISettingsSyncAction
             * @property {boolean|null} [startAtLogin] SettingsSyncAction startAtLogin
             * @property {boolean|null} [minimizeToTray] SettingsSyncAction minimizeToTray
             * @property {string|null} [language] SettingsSyncAction language
             * @property {boolean|null} [replaceTextWithEmoji] SettingsSyncAction replaceTextWithEmoji
             * @property {proto.SyncActionValue.SettingsSyncAction.DisplayMode|null} [bannerNotificationDisplayMode] SettingsSyncAction bannerNotificationDisplayMode
             * @property {proto.SyncActionValue.SettingsSyncAction.DisplayMode|null} [unreadCounterBadgeDisplayMode] SettingsSyncAction unreadCounterBadgeDisplayMode
             * @property {boolean|null} [isMessagesNotificationEnabled] SettingsSyncAction isMessagesNotificationEnabled
             * @property {boolean|null} [isCallsNotificationEnabled] SettingsSyncAction isCallsNotificationEnabled
             * @property {boolean|null} [isReactionsNotificationEnabled] SettingsSyncAction isReactionsNotificationEnabled
             * @property {boolean|null} [isStatusReactionsNotificationEnabled] SettingsSyncAction isStatusReactionsNotificationEnabled
             * @property {boolean|null} [isTextPreviewForNotificationEnabled] SettingsSyncAction isTextPreviewForNotificationEnabled
             * @property {number|null} [defaultNotificationToneId] SettingsSyncAction defaultNotificationToneId
             * @property {number|null} [groupDefaultNotificationToneId] SettingsSyncAction groupDefaultNotificationToneId
             * @property {number|null} [appTheme] SettingsSyncAction appTheme
             * @property {number|null} [wallpaperId] SettingsSyncAction wallpaperId
             * @property {boolean|null} [isDoodleWallpaperEnabled] SettingsSyncAction isDoodleWallpaperEnabled
             * @property {number|null} [fontSize] SettingsSyncAction fontSize
             * @property {boolean|null} [isPhotosAutodownloadEnabled] SettingsSyncAction isPhotosAutodownloadEnabled
             * @property {boolean|null} [isAudiosAutodownloadEnabled] SettingsSyncAction isAudiosAutodownloadEnabled
             * @property {boolean|null} [isVideosAutodownloadEnabled] SettingsSyncAction isVideosAutodownloadEnabled
             * @property {boolean|null} [isDocumentsAutodownloadEnabled] SettingsSyncAction isDocumentsAutodownloadEnabled
             * @property {boolean|null} [disableLinkPreviews] SettingsSyncAction disableLinkPreviews
             * @property {number|null} [notificationToneId] SettingsSyncAction notificationToneId
             * @property {proto.SyncActionValue.SettingsSyncAction.MediaQualitySetting|null} [mediaUploadQuality] SettingsSyncAction mediaUploadQuality
             * @property {boolean|null} [isSpellCheckEnabled] SettingsSyncAction isSpellCheckEnabled
             * @property {boolean|null} [isEnterToSendEnabled] SettingsSyncAction isEnterToSendEnabled
             * @property {boolean|null} [isGroupMessageNotificationEnabled] SettingsSyncAction isGroupMessageNotificationEnabled
             * @property {boolean|null} [isGroupReactionsNotificationEnabled] SettingsSyncAction isGroupReactionsNotificationEnabled
             * @property {boolean|null} [isStatusNotificationEnabled] SettingsSyncAction isStatusNotificationEnabled
             * @property {number|null} [statusNotificationToneId] SettingsSyncAction statusNotificationToneId
             * @property {boolean|null} [shouldPlaySoundForCallNotification] SettingsSyncAction shouldPlaySoundForCallNotification
             * @property {string|null} [chatThemeId] SettingsSyncAction chatThemeId
             * @property {string|null} [colorSchemeId] SettingsSyncAction colorSchemeId
             */

            /**
             * Constructs a new SettingsSyncAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a SettingsSyncAction.
             * @implements ISettingsSyncAction
             * @constructor
             * @param {proto.SyncActionValue.ISettingsSyncAction=} [properties] Properties to set
             */
            function SettingsSyncAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * SettingsSyncAction startAtLogin.
             * @member {boolean|null|undefined} startAtLogin
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.startAtLogin = null;

            /**
             * SettingsSyncAction minimizeToTray.
             * @member {boolean|null|undefined} minimizeToTray
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.minimizeToTray = null;

            /**
             * SettingsSyncAction language.
             * @member {string|null|undefined} language
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.language = null;

            /**
             * SettingsSyncAction replaceTextWithEmoji.
             * @member {boolean|null|undefined} replaceTextWithEmoji
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.replaceTextWithEmoji = null;

            /**
             * SettingsSyncAction bannerNotificationDisplayMode.
             * @member {proto.SyncActionValue.SettingsSyncAction.DisplayMode|null|undefined} bannerNotificationDisplayMode
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.bannerNotificationDisplayMode = null;

            /**
             * SettingsSyncAction unreadCounterBadgeDisplayMode.
             * @member {proto.SyncActionValue.SettingsSyncAction.DisplayMode|null|undefined} unreadCounterBadgeDisplayMode
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.unreadCounterBadgeDisplayMode = null;

            /**
             * SettingsSyncAction isMessagesNotificationEnabled.
             * @member {boolean|null|undefined} isMessagesNotificationEnabled
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.isMessagesNotificationEnabled = null;

            /**
             * SettingsSyncAction isCallsNotificationEnabled.
             * @member {boolean|null|undefined} isCallsNotificationEnabled
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.isCallsNotificationEnabled = null;

            /**
             * SettingsSyncAction isReactionsNotificationEnabled.
             * @member {boolean|null|undefined} isReactionsNotificationEnabled
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.isReactionsNotificationEnabled = null;

            /**
             * SettingsSyncAction isStatusReactionsNotificationEnabled.
             * @member {boolean|null|undefined} isStatusReactionsNotificationEnabled
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.isStatusReactionsNotificationEnabled = null;

            /**
             * SettingsSyncAction isTextPreviewForNotificationEnabled.
             * @member {boolean|null|undefined} isTextPreviewForNotificationEnabled
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.isTextPreviewForNotificationEnabled = null;

            /**
             * SettingsSyncAction defaultNotificationToneId.
             * @member {number|null|undefined} defaultNotificationToneId
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.defaultNotificationToneId = null;

            /**
             * SettingsSyncAction groupDefaultNotificationToneId.
             * @member {number|null|undefined} groupDefaultNotificationToneId
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.groupDefaultNotificationToneId = null;

            /**
             * SettingsSyncAction appTheme.
             * @member {number|null|undefined} appTheme
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.appTheme = null;

            /**
             * SettingsSyncAction wallpaperId.
             * @member {number|null|undefined} wallpaperId
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.wallpaperId = null;

            /**
             * SettingsSyncAction isDoodleWallpaperEnabled.
             * @member {boolean|null|undefined} isDoodleWallpaperEnabled
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.isDoodleWallpaperEnabled = null;

            /**
             * SettingsSyncAction fontSize.
             * @member {number|null|undefined} fontSize
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.fontSize = null;

            /**
             * SettingsSyncAction isPhotosAutodownloadEnabled.
             * @member {boolean|null|undefined} isPhotosAutodownloadEnabled
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.isPhotosAutodownloadEnabled = null;

            /**
             * SettingsSyncAction isAudiosAutodownloadEnabled.
             * @member {boolean|null|undefined} isAudiosAutodownloadEnabled
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.isAudiosAutodownloadEnabled = null;

            /**
             * SettingsSyncAction isVideosAutodownloadEnabled.
             * @member {boolean|null|undefined} isVideosAutodownloadEnabled
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.isVideosAutodownloadEnabled = null;

            /**
             * SettingsSyncAction isDocumentsAutodownloadEnabled.
             * @member {boolean|null|undefined} isDocumentsAutodownloadEnabled
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.isDocumentsAutodownloadEnabled = null;

            /**
             * SettingsSyncAction disableLinkPreviews.
             * @member {boolean|null|undefined} disableLinkPreviews
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.disableLinkPreviews = null;

            /**
             * SettingsSyncAction notificationToneId.
             * @member {number|null|undefined} notificationToneId
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.notificationToneId = null;

            /**
             * SettingsSyncAction mediaUploadQuality.
             * @member {proto.SyncActionValue.SettingsSyncAction.MediaQualitySetting|null|undefined} mediaUploadQuality
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.mediaUploadQuality = null;

            /**
             * SettingsSyncAction isSpellCheckEnabled.
             * @member {boolean|null|undefined} isSpellCheckEnabled
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.isSpellCheckEnabled = null;

            /**
             * SettingsSyncAction isEnterToSendEnabled.
             * @member {boolean|null|undefined} isEnterToSendEnabled
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.isEnterToSendEnabled = null;

            /**
             * SettingsSyncAction isGroupMessageNotificationEnabled.
             * @member {boolean|null|undefined} isGroupMessageNotificationEnabled
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.isGroupMessageNotificationEnabled = null;

            /**
             * SettingsSyncAction isGroupReactionsNotificationEnabled.
             * @member {boolean|null|undefined} isGroupReactionsNotificationEnabled
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.isGroupReactionsNotificationEnabled = null;

            /**
             * SettingsSyncAction isStatusNotificationEnabled.
             * @member {boolean|null|undefined} isStatusNotificationEnabled
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.isStatusNotificationEnabled = null;

            /**
             * SettingsSyncAction statusNotificationToneId.
             * @member {number|null|undefined} statusNotificationToneId
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.statusNotificationToneId = null;

            /**
             * SettingsSyncAction shouldPlaySoundForCallNotification.
             * @member {boolean|null|undefined} shouldPlaySoundForCallNotification
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.shouldPlaySoundForCallNotification = null;

            /**
             * SettingsSyncAction chatThemeId.
             * @member {string|null|undefined} chatThemeId
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.chatThemeId = null;

            /**
             * SettingsSyncAction colorSchemeId.
             * @member {string|null|undefined} colorSchemeId
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             */
            SettingsSyncAction.prototype.colorSchemeId = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_startAtLogin", {
                get: $util.oneOfGetter($oneOfFields = ["startAtLogin"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_minimizeToTray", {
                get: $util.oneOfGetter($oneOfFields = ["minimizeToTray"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_language", {
                get: $util.oneOfGetter($oneOfFields = ["language"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_replaceTextWithEmoji", {
                get: $util.oneOfGetter($oneOfFields = ["replaceTextWithEmoji"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_bannerNotificationDisplayMode", {
                get: $util.oneOfGetter($oneOfFields = ["bannerNotificationDisplayMode"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_unreadCounterBadgeDisplayMode", {
                get: $util.oneOfGetter($oneOfFields = ["unreadCounterBadgeDisplayMode"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_isMessagesNotificationEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isMessagesNotificationEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_isCallsNotificationEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isCallsNotificationEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_isReactionsNotificationEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isReactionsNotificationEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_isStatusReactionsNotificationEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isStatusReactionsNotificationEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_isTextPreviewForNotificationEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isTextPreviewForNotificationEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_defaultNotificationToneId", {
                get: $util.oneOfGetter($oneOfFields = ["defaultNotificationToneId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_groupDefaultNotificationToneId", {
                get: $util.oneOfGetter($oneOfFields = ["groupDefaultNotificationToneId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_appTheme", {
                get: $util.oneOfGetter($oneOfFields = ["appTheme"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_wallpaperId", {
                get: $util.oneOfGetter($oneOfFields = ["wallpaperId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_isDoodleWallpaperEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isDoodleWallpaperEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_fontSize", {
                get: $util.oneOfGetter($oneOfFields = ["fontSize"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_isPhotosAutodownloadEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isPhotosAutodownloadEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_isAudiosAutodownloadEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isAudiosAutodownloadEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_isVideosAutodownloadEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isVideosAutodownloadEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_isDocumentsAutodownloadEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isDocumentsAutodownloadEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_disableLinkPreviews", {
                get: $util.oneOfGetter($oneOfFields = ["disableLinkPreviews"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_notificationToneId", {
                get: $util.oneOfGetter($oneOfFields = ["notificationToneId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_mediaUploadQuality", {
                get: $util.oneOfGetter($oneOfFields = ["mediaUploadQuality"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_isSpellCheckEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isSpellCheckEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_isEnterToSendEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isEnterToSendEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_isGroupMessageNotificationEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isGroupMessageNotificationEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_isGroupReactionsNotificationEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isGroupReactionsNotificationEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_isStatusNotificationEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isStatusNotificationEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_statusNotificationToneId", {
                get: $util.oneOfGetter($oneOfFields = ["statusNotificationToneId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_shouldPlaySoundForCallNotification", {
                get: $util.oneOfGetter($oneOfFields = ["shouldPlaySoundForCallNotification"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_chatThemeId", {
                get: $util.oneOfGetter($oneOfFields = ["chatThemeId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SettingsSyncAction.prototype, "_colorSchemeId", {
                get: $util.oneOfGetter($oneOfFields = ["colorSchemeId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new SettingsSyncAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @static
             * @param {proto.SyncActionValue.ISettingsSyncAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.SettingsSyncAction} SettingsSyncAction instance
             */
            SettingsSyncAction.create = function create(properties) {
                return new SettingsSyncAction(properties);
            };

            /**
             * Encodes the specified SettingsSyncAction message. Does not implicitly {@link proto.SyncActionValue.SettingsSyncAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @static
             * @param {proto.SyncActionValue.ISettingsSyncAction} message SettingsSyncAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SettingsSyncAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.startAtLogin != null && Object.hasOwnProperty.call(message, "startAtLogin"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.startAtLogin);
                if (message.minimizeToTray != null && Object.hasOwnProperty.call(message, "minimizeToTray"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.minimizeToTray);
                if (message.language != null && Object.hasOwnProperty.call(message, "language"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.language);
                if (message.replaceTextWithEmoji != null && Object.hasOwnProperty.call(message, "replaceTextWithEmoji"))
                    writer.uint32(/* id 4, wireType 0 =*/32).bool(message.replaceTextWithEmoji);
                if (message.bannerNotificationDisplayMode != null && Object.hasOwnProperty.call(message, "bannerNotificationDisplayMode"))
                    writer.uint32(/* id 5, wireType 0 =*/40).int32(message.bannerNotificationDisplayMode);
                if (message.unreadCounterBadgeDisplayMode != null && Object.hasOwnProperty.call(message, "unreadCounterBadgeDisplayMode"))
                    writer.uint32(/* id 6, wireType 0 =*/48).int32(message.unreadCounterBadgeDisplayMode);
                if (message.isMessagesNotificationEnabled != null && Object.hasOwnProperty.call(message, "isMessagesNotificationEnabled"))
                    writer.uint32(/* id 7, wireType 0 =*/56).bool(message.isMessagesNotificationEnabled);
                if (message.isCallsNotificationEnabled != null && Object.hasOwnProperty.call(message, "isCallsNotificationEnabled"))
                    writer.uint32(/* id 8, wireType 0 =*/64).bool(message.isCallsNotificationEnabled);
                if (message.isReactionsNotificationEnabled != null && Object.hasOwnProperty.call(message, "isReactionsNotificationEnabled"))
                    writer.uint32(/* id 9, wireType 0 =*/72).bool(message.isReactionsNotificationEnabled);
                if (message.isStatusReactionsNotificationEnabled != null && Object.hasOwnProperty.call(message, "isStatusReactionsNotificationEnabled"))
                    writer.uint32(/* id 10, wireType 0 =*/80).bool(message.isStatusReactionsNotificationEnabled);
                if (message.isTextPreviewForNotificationEnabled != null && Object.hasOwnProperty.call(message, "isTextPreviewForNotificationEnabled"))
                    writer.uint32(/* id 11, wireType 0 =*/88).bool(message.isTextPreviewForNotificationEnabled);
                if (message.defaultNotificationToneId != null && Object.hasOwnProperty.call(message, "defaultNotificationToneId"))
                    writer.uint32(/* id 12, wireType 0 =*/96).int32(message.defaultNotificationToneId);
                if (message.groupDefaultNotificationToneId != null && Object.hasOwnProperty.call(message, "groupDefaultNotificationToneId"))
                    writer.uint32(/* id 13, wireType 0 =*/104).int32(message.groupDefaultNotificationToneId);
                if (message.appTheme != null && Object.hasOwnProperty.call(message, "appTheme"))
                    writer.uint32(/* id 14, wireType 0 =*/112).int32(message.appTheme);
                if (message.wallpaperId != null && Object.hasOwnProperty.call(message, "wallpaperId"))
                    writer.uint32(/* id 15, wireType 0 =*/120).int32(message.wallpaperId);
                if (message.isDoodleWallpaperEnabled != null && Object.hasOwnProperty.call(message, "isDoodleWallpaperEnabled"))
                    writer.uint32(/* id 16, wireType 0 =*/128).bool(message.isDoodleWallpaperEnabled);
                if (message.fontSize != null && Object.hasOwnProperty.call(message, "fontSize"))
                    writer.uint32(/* id 17, wireType 0 =*/136).int32(message.fontSize);
                if (message.isPhotosAutodownloadEnabled != null && Object.hasOwnProperty.call(message, "isPhotosAutodownloadEnabled"))
                    writer.uint32(/* id 18, wireType 0 =*/144).bool(message.isPhotosAutodownloadEnabled);
                if (message.isAudiosAutodownloadEnabled != null && Object.hasOwnProperty.call(message, "isAudiosAutodownloadEnabled"))
                    writer.uint32(/* id 19, wireType 0 =*/152).bool(message.isAudiosAutodownloadEnabled);
                if (message.isVideosAutodownloadEnabled != null && Object.hasOwnProperty.call(message, "isVideosAutodownloadEnabled"))
                    writer.uint32(/* id 20, wireType 0 =*/160).bool(message.isVideosAutodownloadEnabled);
                if (message.isDocumentsAutodownloadEnabled != null && Object.hasOwnProperty.call(message, "isDocumentsAutodownloadEnabled"))
                    writer.uint32(/* id 21, wireType 0 =*/168).bool(message.isDocumentsAutodownloadEnabled);
                if (message.disableLinkPreviews != null && Object.hasOwnProperty.call(message, "disableLinkPreviews"))
                    writer.uint32(/* id 22, wireType 0 =*/176).bool(message.disableLinkPreviews);
                if (message.notificationToneId != null && Object.hasOwnProperty.call(message, "notificationToneId"))
                    writer.uint32(/* id 23, wireType 0 =*/184).int32(message.notificationToneId);
                if (message.mediaUploadQuality != null && Object.hasOwnProperty.call(message, "mediaUploadQuality"))
                    writer.uint32(/* id 24, wireType 0 =*/192).int32(message.mediaUploadQuality);
                if (message.isSpellCheckEnabled != null && Object.hasOwnProperty.call(message, "isSpellCheckEnabled"))
                    writer.uint32(/* id 25, wireType 0 =*/200).bool(message.isSpellCheckEnabled);
                if (message.isEnterToSendEnabled != null && Object.hasOwnProperty.call(message, "isEnterToSendEnabled"))
                    writer.uint32(/* id 26, wireType 0 =*/208).bool(message.isEnterToSendEnabled);
                if (message.isGroupMessageNotificationEnabled != null && Object.hasOwnProperty.call(message, "isGroupMessageNotificationEnabled"))
                    writer.uint32(/* id 27, wireType 0 =*/216).bool(message.isGroupMessageNotificationEnabled);
                if (message.isGroupReactionsNotificationEnabled != null && Object.hasOwnProperty.call(message, "isGroupReactionsNotificationEnabled"))
                    writer.uint32(/* id 28, wireType 0 =*/224).bool(message.isGroupReactionsNotificationEnabled);
                if (message.isStatusNotificationEnabled != null && Object.hasOwnProperty.call(message, "isStatusNotificationEnabled"))
                    writer.uint32(/* id 29, wireType 0 =*/232).bool(message.isStatusNotificationEnabled);
                if (message.statusNotificationToneId != null && Object.hasOwnProperty.call(message, "statusNotificationToneId"))
                    writer.uint32(/* id 30, wireType 0 =*/240).int32(message.statusNotificationToneId);
                if (message.shouldPlaySoundForCallNotification != null && Object.hasOwnProperty.call(message, "shouldPlaySoundForCallNotification"))
                    writer.uint32(/* id 31, wireType 0 =*/248).bool(message.shouldPlaySoundForCallNotification);
                if (message.chatThemeId != null && Object.hasOwnProperty.call(message, "chatThemeId"))
                    writer.uint32(/* id 32, wireType 2 =*/258).string(message.chatThemeId);
                if (message.colorSchemeId != null && Object.hasOwnProperty.call(message, "colorSchemeId"))
                    writer.uint32(/* id 33, wireType 2 =*/266).string(message.colorSchemeId);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SettingsSyncAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.SettingsSyncAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @static
             * @param {proto.SyncActionValue.ISettingsSyncAction} message SettingsSyncAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SettingsSyncAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a SettingsSyncAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.SettingsSyncAction} SettingsSyncAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SettingsSyncAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.startAtLogin = reader.bool();
                            message._startAtLogin = "startAtLogin";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.minimizeToTray = reader.bool();
                            message._minimizeToTray = "minimizeToTray";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.language = reader.stringVerify();
                            message._language = "language";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.replaceTextWithEmoji = reader.bool();
                            message._replaceTextWithEmoji = "replaceTextWithEmoji";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.bannerNotificationDisplayMode = reader.int32();
                            message._bannerNotificationDisplayMode = "bannerNotificationDisplayMode";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.unreadCounterBadgeDisplayMode = reader.int32();
                            message._unreadCounterBadgeDisplayMode = "unreadCounterBadgeDisplayMode";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            message.isMessagesNotificationEnabled = reader.bool();
                            message._isMessagesNotificationEnabled = "isMessagesNotificationEnabled";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            message.isCallsNotificationEnabled = reader.bool();
                            message._isCallsNotificationEnabled = "isCallsNotificationEnabled";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            message.isReactionsNotificationEnabled = reader.bool();
                            message._isReactionsNotificationEnabled = "isReactionsNotificationEnabled";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            message.isStatusReactionsNotificationEnabled = reader.bool();
                            message._isStatusReactionsNotificationEnabled = "isStatusReactionsNotificationEnabled";
                            continue;
                        }
                    case 11: {
                            if (u !== 0)
                                break;
                            message.isTextPreviewForNotificationEnabled = reader.bool();
                            message._isTextPreviewForNotificationEnabled = "isTextPreviewForNotificationEnabled";
                            continue;
                        }
                    case 12: {
                            if (u !== 0)
                                break;
                            message.defaultNotificationToneId = reader.int32();
                            message._defaultNotificationToneId = "defaultNotificationToneId";
                            continue;
                        }
                    case 13: {
                            if (u !== 0)
                                break;
                            message.groupDefaultNotificationToneId = reader.int32();
                            message._groupDefaultNotificationToneId = "groupDefaultNotificationToneId";
                            continue;
                        }
                    case 14: {
                            if (u !== 0)
                                break;
                            message.appTheme = reader.int32();
                            message._appTheme = "appTheme";
                            continue;
                        }
                    case 15: {
                            if (u !== 0)
                                break;
                            message.wallpaperId = reader.int32();
                            message._wallpaperId = "wallpaperId";
                            continue;
                        }
                    case 16: {
                            if (u !== 0)
                                break;
                            message.isDoodleWallpaperEnabled = reader.bool();
                            message._isDoodleWallpaperEnabled = "isDoodleWallpaperEnabled";
                            continue;
                        }
                    case 17: {
                            if (u !== 0)
                                break;
                            message.fontSize = reader.int32();
                            message._fontSize = "fontSize";
                            continue;
                        }
                    case 18: {
                            if (u !== 0)
                                break;
                            message.isPhotosAutodownloadEnabled = reader.bool();
                            message._isPhotosAutodownloadEnabled = "isPhotosAutodownloadEnabled";
                            continue;
                        }
                    case 19: {
                            if (u !== 0)
                                break;
                            message.isAudiosAutodownloadEnabled = reader.bool();
                            message._isAudiosAutodownloadEnabled = "isAudiosAutodownloadEnabled";
                            continue;
                        }
                    case 20: {
                            if (u !== 0)
                                break;
                            message.isVideosAutodownloadEnabled = reader.bool();
                            message._isVideosAutodownloadEnabled = "isVideosAutodownloadEnabled";
                            continue;
                        }
                    case 21: {
                            if (u !== 0)
                                break;
                            message.isDocumentsAutodownloadEnabled = reader.bool();
                            message._isDocumentsAutodownloadEnabled = "isDocumentsAutodownloadEnabled";
                            continue;
                        }
                    case 22: {
                            if (u !== 0)
                                break;
                            message.disableLinkPreviews = reader.bool();
                            message._disableLinkPreviews = "disableLinkPreviews";
                            continue;
                        }
                    case 23: {
                            if (u !== 0)
                                break;
                            message.notificationToneId = reader.int32();
                            message._notificationToneId = "notificationToneId";
                            continue;
                        }
                    case 24: {
                            if (u !== 0)
                                break;
                            message.mediaUploadQuality = reader.int32();
                            message._mediaUploadQuality = "mediaUploadQuality";
                            continue;
                        }
                    case 25: {
                            if (u !== 0)
                                break;
                            message.isSpellCheckEnabled = reader.bool();
                            message._isSpellCheckEnabled = "isSpellCheckEnabled";
                            continue;
                        }
                    case 26: {
                            if (u !== 0)
                                break;
                            message.isEnterToSendEnabled = reader.bool();
                            message._isEnterToSendEnabled = "isEnterToSendEnabled";
                            continue;
                        }
                    case 27: {
                            if (u !== 0)
                                break;
                            message.isGroupMessageNotificationEnabled = reader.bool();
                            message._isGroupMessageNotificationEnabled = "isGroupMessageNotificationEnabled";
                            continue;
                        }
                    case 28: {
                            if (u !== 0)
                                break;
                            message.isGroupReactionsNotificationEnabled = reader.bool();
                            message._isGroupReactionsNotificationEnabled = "isGroupReactionsNotificationEnabled";
                            continue;
                        }
                    case 29: {
                            if (u !== 0)
                                break;
                            message.isStatusNotificationEnabled = reader.bool();
                            message._isStatusNotificationEnabled = "isStatusNotificationEnabled";
                            continue;
                        }
                    case 30: {
                            if (u !== 0)
                                break;
                            message.statusNotificationToneId = reader.int32();
                            message._statusNotificationToneId = "statusNotificationToneId";
                            continue;
                        }
                    case 31: {
                            if (u !== 0)
                                break;
                            message.shouldPlaySoundForCallNotification = reader.bool();
                            message._shouldPlaySoundForCallNotification = "shouldPlaySoundForCallNotification";
                            continue;
                        }
                    case 32: {
                            if (u !== 2)
                                break;
                            message.chatThemeId = reader.stringVerify();
                            message._chatThemeId = "chatThemeId";
                            continue;
                        }
                    case 33: {
                            if (u !== 2)
                                break;
                            message.colorSchemeId = reader.stringVerify();
                            message._colorSchemeId = "colorSchemeId";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a SettingsSyncAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.SettingsSyncAction} SettingsSyncAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SettingsSyncAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SettingsSyncAction message.
             * @function verify
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SettingsSyncAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.startAtLogin != null && Object.hasOwnProperty.call(message, "startAtLogin")) {
                    properties._startAtLogin = 1;
                    if (typeof message.startAtLogin !== "boolean")
                        return "startAtLogin: boolean expected";
                }
                if (message.minimizeToTray != null && Object.hasOwnProperty.call(message, "minimizeToTray")) {
                    properties._minimizeToTray = 1;
                    if (typeof message.minimizeToTray !== "boolean")
                        return "minimizeToTray: boolean expected";
                }
                if (message.language != null && Object.hasOwnProperty.call(message, "language")) {
                    properties._language = 1;
                    if (!$util.isString(message.language))
                        return "language: string expected";
                }
                if (message.replaceTextWithEmoji != null && Object.hasOwnProperty.call(message, "replaceTextWithEmoji")) {
                    properties._replaceTextWithEmoji = 1;
                    if (typeof message.replaceTextWithEmoji !== "boolean")
                        return "replaceTextWithEmoji: boolean expected";
                }
                if (message.bannerNotificationDisplayMode != null && Object.hasOwnProperty.call(message, "bannerNotificationDisplayMode")) {
                    properties._bannerNotificationDisplayMode = 1;
                    if (typeof message.bannerNotificationDisplayMode !== "number" || (message.bannerNotificationDisplayMode | 0) !== message.bannerNotificationDisplayMode)
                        return "bannerNotificationDisplayMode: enum value expected";
                }
                if (message.unreadCounterBadgeDisplayMode != null && Object.hasOwnProperty.call(message, "unreadCounterBadgeDisplayMode")) {
                    properties._unreadCounterBadgeDisplayMode = 1;
                    if (typeof message.unreadCounterBadgeDisplayMode !== "number" || (message.unreadCounterBadgeDisplayMode | 0) !== message.unreadCounterBadgeDisplayMode)
                        return "unreadCounterBadgeDisplayMode: enum value expected";
                }
                if (message.isMessagesNotificationEnabled != null && Object.hasOwnProperty.call(message, "isMessagesNotificationEnabled")) {
                    properties._isMessagesNotificationEnabled = 1;
                    if (typeof message.isMessagesNotificationEnabled !== "boolean")
                        return "isMessagesNotificationEnabled: boolean expected";
                }
                if (message.isCallsNotificationEnabled != null && Object.hasOwnProperty.call(message, "isCallsNotificationEnabled")) {
                    properties._isCallsNotificationEnabled = 1;
                    if (typeof message.isCallsNotificationEnabled !== "boolean")
                        return "isCallsNotificationEnabled: boolean expected";
                }
                if (message.isReactionsNotificationEnabled != null && Object.hasOwnProperty.call(message, "isReactionsNotificationEnabled")) {
                    properties._isReactionsNotificationEnabled = 1;
                    if (typeof message.isReactionsNotificationEnabled !== "boolean")
                        return "isReactionsNotificationEnabled: boolean expected";
                }
                if (message.isStatusReactionsNotificationEnabled != null && Object.hasOwnProperty.call(message, "isStatusReactionsNotificationEnabled")) {
                    properties._isStatusReactionsNotificationEnabled = 1;
                    if (typeof message.isStatusReactionsNotificationEnabled !== "boolean")
                        return "isStatusReactionsNotificationEnabled: boolean expected";
                }
                if (message.isTextPreviewForNotificationEnabled != null && Object.hasOwnProperty.call(message, "isTextPreviewForNotificationEnabled")) {
                    properties._isTextPreviewForNotificationEnabled = 1;
                    if (typeof message.isTextPreviewForNotificationEnabled !== "boolean")
                        return "isTextPreviewForNotificationEnabled: boolean expected";
                }
                if (message.defaultNotificationToneId != null && Object.hasOwnProperty.call(message, "defaultNotificationToneId")) {
                    properties._defaultNotificationToneId = 1;
                    if (!$util.isInteger(message.defaultNotificationToneId))
                        return "defaultNotificationToneId: integer expected";
                }
                if (message.groupDefaultNotificationToneId != null && Object.hasOwnProperty.call(message, "groupDefaultNotificationToneId")) {
                    properties._groupDefaultNotificationToneId = 1;
                    if (!$util.isInteger(message.groupDefaultNotificationToneId))
                        return "groupDefaultNotificationToneId: integer expected";
                }
                if (message.appTheme != null && Object.hasOwnProperty.call(message, "appTheme")) {
                    properties._appTheme = 1;
                    if (!$util.isInteger(message.appTheme))
                        return "appTheme: integer expected";
                }
                if (message.wallpaperId != null && Object.hasOwnProperty.call(message, "wallpaperId")) {
                    properties._wallpaperId = 1;
                    if (!$util.isInteger(message.wallpaperId))
                        return "wallpaperId: integer expected";
                }
                if (message.isDoodleWallpaperEnabled != null && Object.hasOwnProperty.call(message, "isDoodleWallpaperEnabled")) {
                    properties._isDoodleWallpaperEnabled = 1;
                    if (typeof message.isDoodleWallpaperEnabled !== "boolean")
                        return "isDoodleWallpaperEnabled: boolean expected";
                }
                if (message.fontSize != null && Object.hasOwnProperty.call(message, "fontSize")) {
                    properties._fontSize = 1;
                    if (!$util.isInteger(message.fontSize))
                        return "fontSize: integer expected";
                }
                if (message.isPhotosAutodownloadEnabled != null && Object.hasOwnProperty.call(message, "isPhotosAutodownloadEnabled")) {
                    properties._isPhotosAutodownloadEnabled = 1;
                    if (typeof message.isPhotosAutodownloadEnabled !== "boolean")
                        return "isPhotosAutodownloadEnabled: boolean expected";
                }
                if (message.isAudiosAutodownloadEnabled != null && Object.hasOwnProperty.call(message, "isAudiosAutodownloadEnabled")) {
                    properties._isAudiosAutodownloadEnabled = 1;
                    if (typeof message.isAudiosAutodownloadEnabled !== "boolean")
                        return "isAudiosAutodownloadEnabled: boolean expected";
                }
                if (message.isVideosAutodownloadEnabled != null && Object.hasOwnProperty.call(message, "isVideosAutodownloadEnabled")) {
                    properties._isVideosAutodownloadEnabled = 1;
                    if (typeof message.isVideosAutodownloadEnabled !== "boolean")
                        return "isVideosAutodownloadEnabled: boolean expected";
                }
                if (message.isDocumentsAutodownloadEnabled != null && Object.hasOwnProperty.call(message, "isDocumentsAutodownloadEnabled")) {
                    properties._isDocumentsAutodownloadEnabled = 1;
                    if (typeof message.isDocumentsAutodownloadEnabled !== "boolean")
                        return "isDocumentsAutodownloadEnabled: boolean expected";
                }
                if (message.disableLinkPreviews != null && Object.hasOwnProperty.call(message, "disableLinkPreviews")) {
                    properties._disableLinkPreviews = 1;
                    if (typeof message.disableLinkPreviews !== "boolean")
                        return "disableLinkPreviews: boolean expected";
                }
                if (message.notificationToneId != null && Object.hasOwnProperty.call(message, "notificationToneId")) {
                    properties._notificationToneId = 1;
                    if (!$util.isInteger(message.notificationToneId))
                        return "notificationToneId: integer expected";
                }
                if (message.mediaUploadQuality != null && Object.hasOwnProperty.call(message, "mediaUploadQuality")) {
                    properties._mediaUploadQuality = 1;
                    if (typeof message.mediaUploadQuality !== "number" || (message.mediaUploadQuality | 0) !== message.mediaUploadQuality)
                        return "mediaUploadQuality: enum value expected";
                }
                if (message.isSpellCheckEnabled != null && Object.hasOwnProperty.call(message, "isSpellCheckEnabled")) {
                    properties._isSpellCheckEnabled = 1;
                    if (typeof message.isSpellCheckEnabled !== "boolean")
                        return "isSpellCheckEnabled: boolean expected";
                }
                if (message.isEnterToSendEnabled != null && Object.hasOwnProperty.call(message, "isEnterToSendEnabled")) {
                    properties._isEnterToSendEnabled = 1;
                    if (typeof message.isEnterToSendEnabled !== "boolean")
                        return "isEnterToSendEnabled: boolean expected";
                }
                if (message.isGroupMessageNotificationEnabled != null && Object.hasOwnProperty.call(message, "isGroupMessageNotificationEnabled")) {
                    properties._isGroupMessageNotificationEnabled = 1;
                    if (typeof message.isGroupMessageNotificationEnabled !== "boolean")
                        return "isGroupMessageNotificationEnabled: boolean expected";
                }
                if (message.isGroupReactionsNotificationEnabled != null && Object.hasOwnProperty.call(message, "isGroupReactionsNotificationEnabled")) {
                    properties._isGroupReactionsNotificationEnabled = 1;
                    if (typeof message.isGroupReactionsNotificationEnabled !== "boolean")
                        return "isGroupReactionsNotificationEnabled: boolean expected";
                }
                if (message.isStatusNotificationEnabled != null && Object.hasOwnProperty.call(message, "isStatusNotificationEnabled")) {
                    properties._isStatusNotificationEnabled = 1;
                    if (typeof message.isStatusNotificationEnabled !== "boolean")
                        return "isStatusNotificationEnabled: boolean expected";
                }
                if (message.statusNotificationToneId != null && Object.hasOwnProperty.call(message, "statusNotificationToneId")) {
                    properties._statusNotificationToneId = 1;
                    if (!$util.isInteger(message.statusNotificationToneId))
                        return "statusNotificationToneId: integer expected";
                }
                if (message.shouldPlaySoundForCallNotification != null && Object.hasOwnProperty.call(message, "shouldPlaySoundForCallNotification")) {
                    properties._shouldPlaySoundForCallNotification = 1;
                    if (typeof message.shouldPlaySoundForCallNotification !== "boolean")
                        return "shouldPlaySoundForCallNotification: boolean expected";
                }
                if (message.chatThemeId != null && Object.hasOwnProperty.call(message, "chatThemeId")) {
                    properties._chatThemeId = 1;
                    if (!$util.isString(message.chatThemeId))
                        return "chatThemeId: string expected";
                }
                if (message.colorSchemeId != null && Object.hasOwnProperty.call(message, "colorSchemeId")) {
                    properties._colorSchemeId = 1;
                    if (!$util.isString(message.colorSchemeId))
                        return "colorSchemeId: string expected";
                }
                return null;
            };

            /**
             * Creates a SettingsSyncAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.SettingsSyncAction} SettingsSyncAction
             */
            SettingsSyncAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.SettingsSyncAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.startAtLogin != null)
                    message.startAtLogin = Boolean(object.startAtLogin);
                if (object.minimizeToTray != null)
                    message.minimizeToTray = Boolean(object.minimizeToTray);
                if (object.language != null)
                    message.language = String(object.language);
                if (object.replaceTextWithEmoji != null)
                    message.replaceTextWithEmoji = Boolean(object.replaceTextWithEmoji);
                switch (object.bannerNotificationDisplayMode) {
                case "DISPLAY_MODE_UNKNOWN":
                case 0:
                    message.bannerNotificationDisplayMode = 0;
                    break;
                case "ALWAYS":
                case 1:
                    message.bannerNotificationDisplayMode = 1;
                    break;
                case "NEVER":
                case 2:
                    message.bannerNotificationDisplayMode = 2;
                    break;
                case "ONLY_WHEN_APP_IS_OPEN":
                case 3:
                    message.bannerNotificationDisplayMode = 3;
                    break;
                default:
                    if (typeof object.bannerNotificationDisplayMode === "number" && (object.bannerNotificationDisplayMode | 0) === object.bannerNotificationDisplayMode)
                        message.bannerNotificationDisplayMode = object.bannerNotificationDisplayMode;
                }
                switch (object.unreadCounterBadgeDisplayMode) {
                case "DISPLAY_MODE_UNKNOWN":
                case 0:
                    message.unreadCounterBadgeDisplayMode = 0;
                    break;
                case "ALWAYS":
                case 1:
                    message.unreadCounterBadgeDisplayMode = 1;
                    break;
                case "NEVER":
                case 2:
                    message.unreadCounterBadgeDisplayMode = 2;
                    break;
                case "ONLY_WHEN_APP_IS_OPEN":
                case 3:
                    message.unreadCounterBadgeDisplayMode = 3;
                    break;
                default:
                    if (typeof object.unreadCounterBadgeDisplayMode === "number" && (object.unreadCounterBadgeDisplayMode | 0) === object.unreadCounterBadgeDisplayMode)
                        message.unreadCounterBadgeDisplayMode = object.unreadCounterBadgeDisplayMode;
                }
                if (object.isMessagesNotificationEnabled != null)
                    message.isMessagesNotificationEnabled = Boolean(object.isMessagesNotificationEnabled);
                if (object.isCallsNotificationEnabled != null)
                    message.isCallsNotificationEnabled = Boolean(object.isCallsNotificationEnabled);
                if (object.isReactionsNotificationEnabled != null)
                    message.isReactionsNotificationEnabled = Boolean(object.isReactionsNotificationEnabled);
                if (object.isStatusReactionsNotificationEnabled != null)
                    message.isStatusReactionsNotificationEnabled = Boolean(object.isStatusReactionsNotificationEnabled);
                if (object.isTextPreviewForNotificationEnabled != null)
                    message.isTextPreviewForNotificationEnabled = Boolean(object.isTextPreviewForNotificationEnabled);
                if (object.defaultNotificationToneId != null)
                    message.defaultNotificationToneId = object.defaultNotificationToneId | 0;
                if (object.groupDefaultNotificationToneId != null)
                    message.groupDefaultNotificationToneId = object.groupDefaultNotificationToneId | 0;
                if (object.appTheme != null)
                    message.appTheme = object.appTheme | 0;
                if (object.wallpaperId != null)
                    message.wallpaperId = object.wallpaperId | 0;
                if (object.isDoodleWallpaperEnabled != null)
                    message.isDoodleWallpaperEnabled = Boolean(object.isDoodleWallpaperEnabled);
                if (object.fontSize != null)
                    message.fontSize = object.fontSize | 0;
                if (object.isPhotosAutodownloadEnabled != null)
                    message.isPhotosAutodownloadEnabled = Boolean(object.isPhotosAutodownloadEnabled);
                if (object.isAudiosAutodownloadEnabled != null)
                    message.isAudiosAutodownloadEnabled = Boolean(object.isAudiosAutodownloadEnabled);
                if (object.isVideosAutodownloadEnabled != null)
                    message.isVideosAutodownloadEnabled = Boolean(object.isVideosAutodownloadEnabled);
                if (object.isDocumentsAutodownloadEnabled != null)
                    message.isDocumentsAutodownloadEnabled = Boolean(object.isDocumentsAutodownloadEnabled);
                if (object.disableLinkPreviews != null)
                    message.disableLinkPreviews = Boolean(object.disableLinkPreviews);
                if (object.notificationToneId != null)
                    message.notificationToneId = object.notificationToneId | 0;
                switch (object.mediaUploadQuality) {
                case "MEDIA_QUALITY_UNKNOWN":
                case 0:
                    message.mediaUploadQuality = 0;
                    break;
                case "STANDARD":
                case 1:
                    message.mediaUploadQuality = 1;
                    break;
                case "HD":
                case 2:
                    message.mediaUploadQuality = 2;
                    break;
                default:
                    if (typeof object.mediaUploadQuality === "number" && (object.mediaUploadQuality | 0) === object.mediaUploadQuality)
                        message.mediaUploadQuality = object.mediaUploadQuality;
                }
                if (object.isSpellCheckEnabled != null)
                    message.isSpellCheckEnabled = Boolean(object.isSpellCheckEnabled);
                if (object.isEnterToSendEnabled != null)
                    message.isEnterToSendEnabled = Boolean(object.isEnterToSendEnabled);
                if (object.isGroupMessageNotificationEnabled != null)
                    message.isGroupMessageNotificationEnabled = Boolean(object.isGroupMessageNotificationEnabled);
                if (object.isGroupReactionsNotificationEnabled != null)
                    message.isGroupReactionsNotificationEnabled = Boolean(object.isGroupReactionsNotificationEnabled);
                if (object.isStatusNotificationEnabled != null)
                    message.isStatusNotificationEnabled = Boolean(object.isStatusNotificationEnabled);
                if (object.statusNotificationToneId != null)
                    message.statusNotificationToneId = object.statusNotificationToneId | 0;
                if (object.shouldPlaySoundForCallNotification != null)
                    message.shouldPlaySoundForCallNotification = Boolean(object.shouldPlaySoundForCallNotification);
                if (object.chatThemeId != null)
                    message.chatThemeId = String(object.chatThemeId);
                if (object.colorSchemeId != null)
                    message.colorSchemeId = String(object.colorSchemeId);
                return message;
            };

            /**
             * Creates a plain object from a SettingsSyncAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @static
             * @param {proto.SyncActionValue.SettingsSyncAction} message SettingsSyncAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SettingsSyncAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.startAtLogin != null && Object.hasOwnProperty.call(message, "startAtLogin"))
                    object.startAtLogin = message.startAtLogin;
                if (message.minimizeToTray != null && Object.hasOwnProperty.call(message, "minimizeToTray"))
                    object.minimizeToTray = message.minimizeToTray;
                if (message.language != null && Object.hasOwnProperty.call(message, "language"))
                    object.language = message.language;
                if (message.replaceTextWithEmoji != null && Object.hasOwnProperty.call(message, "replaceTextWithEmoji"))
                    object.replaceTextWithEmoji = message.replaceTextWithEmoji;
                if (message.bannerNotificationDisplayMode != null && Object.hasOwnProperty.call(message, "bannerNotificationDisplayMode"))
                    object.bannerNotificationDisplayMode = options.enums === String ? $root.proto.SyncActionValue.SettingsSyncAction.DisplayMode[message.bannerNotificationDisplayMode] === undefined ? message.bannerNotificationDisplayMode : $root.proto.SyncActionValue.SettingsSyncAction.DisplayMode[message.bannerNotificationDisplayMode] : message.bannerNotificationDisplayMode;
                if (message.unreadCounterBadgeDisplayMode != null && Object.hasOwnProperty.call(message, "unreadCounterBadgeDisplayMode"))
                    object.unreadCounterBadgeDisplayMode = options.enums === String ? $root.proto.SyncActionValue.SettingsSyncAction.DisplayMode[message.unreadCounterBadgeDisplayMode] === undefined ? message.unreadCounterBadgeDisplayMode : $root.proto.SyncActionValue.SettingsSyncAction.DisplayMode[message.unreadCounterBadgeDisplayMode] : message.unreadCounterBadgeDisplayMode;
                if (message.isMessagesNotificationEnabled != null && Object.hasOwnProperty.call(message, "isMessagesNotificationEnabled"))
                    object.isMessagesNotificationEnabled = message.isMessagesNotificationEnabled;
                if (message.isCallsNotificationEnabled != null && Object.hasOwnProperty.call(message, "isCallsNotificationEnabled"))
                    object.isCallsNotificationEnabled = message.isCallsNotificationEnabled;
                if (message.isReactionsNotificationEnabled != null && Object.hasOwnProperty.call(message, "isReactionsNotificationEnabled"))
                    object.isReactionsNotificationEnabled = message.isReactionsNotificationEnabled;
                if (message.isStatusReactionsNotificationEnabled != null && Object.hasOwnProperty.call(message, "isStatusReactionsNotificationEnabled"))
                    object.isStatusReactionsNotificationEnabled = message.isStatusReactionsNotificationEnabled;
                if (message.isTextPreviewForNotificationEnabled != null && Object.hasOwnProperty.call(message, "isTextPreviewForNotificationEnabled"))
                    object.isTextPreviewForNotificationEnabled = message.isTextPreviewForNotificationEnabled;
                if (message.defaultNotificationToneId != null && Object.hasOwnProperty.call(message, "defaultNotificationToneId"))
                    object.defaultNotificationToneId = message.defaultNotificationToneId;
                if (message.groupDefaultNotificationToneId != null && Object.hasOwnProperty.call(message, "groupDefaultNotificationToneId"))
                    object.groupDefaultNotificationToneId = message.groupDefaultNotificationToneId;
                if (message.appTheme != null && Object.hasOwnProperty.call(message, "appTheme"))
                    object.appTheme = message.appTheme;
                if (message.wallpaperId != null && Object.hasOwnProperty.call(message, "wallpaperId"))
                    object.wallpaperId = message.wallpaperId;
                if (message.isDoodleWallpaperEnabled != null && Object.hasOwnProperty.call(message, "isDoodleWallpaperEnabled"))
                    object.isDoodleWallpaperEnabled = message.isDoodleWallpaperEnabled;
                if (message.fontSize != null && Object.hasOwnProperty.call(message, "fontSize"))
                    object.fontSize = message.fontSize;
                if (message.isPhotosAutodownloadEnabled != null && Object.hasOwnProperty.call(message, "isPhotosAutodownloadEnabled"))
                    object.isPhotosAutodownloadEnabled = message.isPhotosAutodownloadEnabled;
                if (message.isAudiosAutodownloadEnabled != null && Object.hasOwnProperty.call(message, "isAudiosAutodownloadEnabled"))
                    object.isAudiosAutodownloadEnabled = message.isAudiosAutodownloadEnabled;
                if (message.isVideosAutodownloadEnabled != null && Object.hasOwnProperty.call(message, "isVideosAutodownloadEnabled"))
                    object.isVideosAutodownloadEnabled = message.isVideosAutodownloadEnabled;
                if (message.isDocumentsAutodownloadEnabled != null && Object.hasOwnProperty.call(message, "isDocumentsAutodownloadEnabled"))
                    object.isDocumentsAutodownloadEnabled = message.isDocumentsAutodownloadEnabled;
                if (message.disableLinkPreviews != null && Object.hasOwnProperty.call(message, "disableLinkPreviews"))
                    object.disableLinkPreviews = message.disableLinkPreviews;
                if (message.notificationToneId != null && Object.hasOwnProperty.call(message, "notificationToneId"))
                    object.notificationToneId = message.notificationToneId;
                if (message.mediaUploadQuality != null && Object.hasOwnProperty.call(message, "mediaUploadQuality"))
                    object.mediaUploadQuality = options.enums === String ? $root.proto.SyncActionValue.SettingsSyncAction.MediaQualitySetting[message.mediaUploadQuality] === undefined ? message.mediaUploadQuality : $root.proto.SyncActionValue.SettingsSyncAction.MediaQualitySetting[message.mediaUploadQuality] : message.mediaUploadQuality;
                if (message.isSpellCheckEnabled != null && Object.hasOwnProperty.call(message, "isSpellCheckEnabled"))
                    object.isSpellCheckEnabled = message.isSpellCheckEnabled;
                if (message.isEnterToSendEnabled != null && Object.hasOwnProperty.call(message, "isEnterToSendEnabled"))
                    object.isEnterToSendEnabled = message.isEnterToSendEnabled;
                if (message.isGroupMessageNotificationEnabled != null && Object.hasOwnProperty.call(message, "isGroupMessageNotificationEnabled"))
                    object.isGroupMessageNotificationEnabled = message.isGroupMessageNotificationEnabled;
                if (message.isGroupReactionsNotificationEnabled != null && Object.hasOwnProperty.call(message, "isGroupReactionsNotificationEnabled"))
                    object.isGroupReactionsNotificationEnabled = message.isGroupReactionsNotificationEnabled;
                if (message.isStatusNotificationEnabled != null && Object.hasOwnProperty.call(message, "isStatusNotificationEnabled"))
                    object.isStatusNotificationEnabled = message.isStatusNotificationEnabled;
                if (message.statusNotificationToneId != null && Object.hasOwnProperty.call(message, "statusNotificationToneId"))
                    object.statusNotificationToneId = message.statusNotificationToneId;
                if (message.shouldPlaySoundForCallNotification != null && Object.hasOwnProperty.call(message, "shouldPlaySoundForCallNotification"))
                    object.shouldPlaySoundForCallNotification = message.shouldPlaySoundForCallNotification;
                if (message.chatThemeId != null && Object.hasOwnProperty.call(message, "chatThemeId"))
                    object.chatThemeId = message.chatThemeId;
                if (message.colorSchemeId != null && Object.hasOwnProperty.call(message, "colorSchemeId"))
                    object.colorSchemeId = message.colorSchemeId;
                return object;
            };

            /**
             * Converts this SettingsSyncAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SettingsSyncAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for SettingsSyncAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.SettingsSyncAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            SettingsSyncAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.SettingsSyncAction";
            };

            /**
             * DisplayMode enum.
             * @name proto.SyncActionValue.SettingsSyncAction.DisplayMode
             * @enum {number}
             * @property {number} DISPLAY_MODE_UNKNOWN=0 DISPLAY_MODE_UNKNOWN value
             * @property {number} ALWAYS=1 ALWAYS value
             * @property {number} NEVER=2 NEVER value
             * @property {number} ONLY_WHEN_APP_IS_OPEN=3 ONLY_WHEN_APP_IS_OPEN value
             */
            SettingsSyncAction.DisplayMode = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "DISPLAY_MODE_UNKNOWN"] = 0;
                values[valuesById[1] = "ALWAYS"] = 1;
                values[valuesById[2] = "NEVER"] = 2;
                values[valuesById[3] = "ONLY_WHEN_APP_IS_OPEN"] = 3;
                return values;
            })();

            /**
             * MediaQualitySetting enum.
             * @name proto.SyncActionValue.SettingsSyncAction.MediaQualitySetting
             * @enum {number}
             * @property {number} MEDIA_QUALITY_UNKNOWN=0 MEDIA_QUALITY_UNKNOWN value
             * @property {number} STANDARD=1 STANDARD value
             * @property {number} HD=2 HD value
             */
            SettingsSyncAction.MediaQualitySetting = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "MEDIA_QUALITY_UNKNOWN"] = 0;
                values[valuesById[1] = "STANDARD"] = 1;
                values[valuesById[2] = "HD"] = 2;
                return values;
            })();

            /**
             * SettingKey enum.
             * @name proto.SyncActionValue.SettingsSyncAction.SettingKey
             * @enum {number}
             * @property {number} SETTING_KEY_UNKNOWN=0 SETTING_KEY_UNKNOWN value
             * @property {number} START_AT_LOGIN=1 START_AT_LOGIN value
             * @property {number} MINIMIZE_TO_TRAY=2 MINIMIZE_TO_TRAY value
             * @property {number} LANGUAGE=3 LANGUAGE value
             * @property {number} REPLACE_TEXT_WITH_EMOJI=4 REPLACE_TEXT_WITH_EMOJI value
             * @property {number} BANNER_NOTIFICATION_DISPLAY_MODE=5 BANNER_NOTIFICATION_DISPLAY_MODE value
             * @property {number} UNREAD_COUNTER_BADGE_DISPLAY_MODE=6 UNREAD_COUNTER_BADGE_DISPLAY_MODE value
             * @property {number} IS_MESSAGES_NOTIFICATION_ENABLED=7 IS_MESSAGES_NOTIFICATION_ENABLED value
             * @property {number} IS_CALLS_NOTIFICATION_ENABLED=8 IS_CALLS_NOTIFICATION_ENABLED value
             * @property {number} IS_REACTIONS_NOTIFICATION_ENABLED=9 IS_REACTIONS_NOTIFICATION_ENABLED value
             * @property {number} IS_STATUS_REACTIONS_NOTIFICATION_ENABLED=10 IS_STATUS_REACTIONS_NOTIFICATION_ENABLED value
             * @property {number} IS_TEXT_PREVIEW_FOR_NOTIFICATION_ENABLED=11 IS_TEXT_PREVIEW_FOR_NOTIFICATION_ENABLED value
             * @property {number} DEFAULT_NOTIFICATION_TONE_ID=12 DEFAULT_NOTIFICATION_TONE_ID value
             * @property {number} GROUP_DEFAULT_NOTIFICATION_TONE_ID=13 GROUP_DEFAULT_NOTIFICATION_TONE_ID value
             * @property {number} APP_THEME=14 APP_THEME value
             * @property {number} WALLPAPER_ID=15 WALLPAPER_ID value
             * @property {number} IS_DOODLE_WALLPAPER_ENABLED=16 IS_DOODLE_WALLPAPER_ENABLED value
             * @property {number} FONT_SIZE=17 FONT_SIZE value
             * @property {number} IS_PHOTOS_AUTODOWNLOAD_ENABLED=18 IS_PHOTOS_AUTODOWNLOAD_ENABLED value
             * @property {number} IS_AUDIOS_AUTODOWNLOAD_ENABLED=19 IS_AUDIOS_AUTODOWNLOAD_ENABLED value
             * @property {number} IS_VIDEOS_AUTODOWNLOAD_ENABLED=20 IS_VIDEOS_AUTODOWNLOAD_ENABLED value
             * @property {number} IS_DOCUMENTS_AUTODOWNLOAD_ENABLED=21 IS_DOCUMENTS_AUTODOWNLOAD_ENABLED value
             * @property {number} DISABLE_LINK_PREVIEWS=22 DISABLE_LINK_PREVIEWS value
             * @property {number} NOTIFICATION_TONE_ID=23 NOTIFICATION_TONE_ID value
             * @property {number} MEDIA_UPLOAD_QUALITY=24 MEDIA_UPLOAD_QUALITY value
             * @property {number} IS_SPELL_CHECK_ENABLED=25 IS_SPELL_CHECK_ENABLED value
             * @property {number} IS_ENTER_TO_SEND_ENABLED=26 IS_ENTER_TO_SEND_ENABLED value
             * @property {number} IS_GROUP_MESSAGE_NOTIFICATION_ENABLED=27 IS_GROUP_MESSAGE_NOTIFICATION_ENABLED value
             * @property {number} IS_GROUP_REACTIONS_NOTIFICATION_ENABLED=28 IS_GROUP_REACTIONS_NOTIFICATION_ENABLED value
             * @property {number} IS_STATUS_NOTIFICATION_ENABLED=29 IS_STATUS_NOTIFICATION_ENABLED value
             * @property {number} STATUS_NOTIFICATION_TONE_ID=30 STATUS_NOTIFICATION_TONE_ID value
             * @property {number} SHOULD_PLAY_SOUND_FOR_CALL_NOTIFICATION=31 SHOULD_PLAY_SOUND_FOR_CALL_NOTIFICATION value
             * @property {number} CHAT_THEME_ID=32 CHAT_THEME_ID value
             * @property {number} COLOR_SCHEME_ID=33 COLOR_SCHEME_ID value
             */
            SettingsSyncAction.SettingKey = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "SETTING_KEY_UNKNOWN"] = 0;
                values[valuesById[1] = "START_AT_LOGIN"] = 1;
                values[valuesById[2] = "MINIMIZE_TO_TRAY"] = 2;
                values[valuesById[3] = "LANGUAGE"] = 3;
                values[valuesById[4] = "REPLACE_TEXT_WITH_EMOJI"] = 4;
                values[valuesById[5] = "BANNER_NOTIFICATION_DISPLAY_MODE"] = 5;
                values[valuesById[6] = "UNREAD_COUNTER_BADGE_DISPLAY_MODE"] = 6;
                values[valuesById[7] = "IS_MESSAGES_NOTIFICATION_ENABLED"] = 7;
                values[valuesById[8] = "IS_CALLS_NOTIFICATION_ENABLED"] = 8;
                values[valuesById[9] = "IS_REACTIONS_NOTIFICATION_ENABLED"] = 9;
                values[valuesById[10] = "IS_STATUS_REACTIONS_NOTIFICATION_ENABLED"] = 10;
                values[valuesById[11] = "IS_TEXT_PREVIEW_FOR_NOTIFICATION_ENABLED"] = 11;
                values[valuesById[12] = "DEFAULT_NOTIFICATION_TONE_ID"] = 12;
                values[valuesById[13] = "GROUP_DEFAULT_NOTIFICATION_TONE_ID"] = 13;
                values[valuesById[14] = "APP_THEME"] = 14;
                values[valuesById[15] = "WALLPAPER_ID"] = 15;
                values[valuesById[16] = "IS_DOODLE_WALLPAPER_ENABLED"] = 16;
                values[valuesById[17] = "FONT_SIZE"] = 17;
                values[valuesById[18] = "IS_PHOTOS_AUTODOWNLOAD_ENABLED"] = 18;
                values[valuesById[19] = "IS_AUDIOS_AUTODOWNLOAD_ENABLED"] = 19;
                values[valuesById[20] = "IS_VIDEOS_AUTODOWNLOAD_ENABLED"] = 20;
                values[valuesById[21] = "IS_DOCUMENTS_AUTODOWNLOAD_ENABLED"] = 21;
                values[valuesById[22] = "DISABLE_LINK_PREVIEWS"] = 22;
                values[valuesById[23] = "NOTIFICATION_TONE_ID"] = 23;
                values[valuesById[24] = "MEDIA_UPLOAD_QUALITY"] = 24;
                values[valuesById[25] = "IS_SPELL_CHECK_ENABLED"] = 25;
                values[valuesById[26] = "IS_ENTER_TO_SEND_ENABLED"] = 26;
                values[valuesById[27] = "IS_GROUP_MESSAGE_NOTIFICATION_ENABLED"] = 27;
                values[valuesById[28] = "IS_GROUP_REACTIONS_NOTIFICATION_ENABLED"] = 28;
                values[valuesById[29] = "IS_STATUS_NOTIFICATION_ENABLED"] = 29;
                values[valuesById[30] = "STATUS_NOTIFICATION_TONE_ID"] = 30;
                values[valuesById[31] = "SHOULD_PLAY_SOUND_FOR_CALL_NOTIFICATION"] = 31;
                values[valuesById[32] = "CHAT_THEME_ID"] = 32;
                values[valuesById[33] = "COLOR_SCHEME_ID"] = 33;
                return values;
            })();

            /**
             * SettingPlatform enum.
             * @name proto.SyncActionValue.SettingsSyncAction.SettingPlatform
             * @enum {number}
             * @property {number} PLATFORM_UNKNOWN=0 PLATFORM_UNKNOWN value
             * @property {number} WEB=1 WEB value
             * @property {number} HYBRID=2 HYBRID value
             * @property {number} WINDOWS=3 WINDOWS value
             * @property {number} MAC=4 MAC value
             */
            SettingsSyncAction.SettingPlatform = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "PLATFORM_UNKNOWN"] = 0;
                values[valuesById[1] = "WEB"] = 1;
                values[valuesById[2] = "HYBRID"] = 2;
                values[valuesById[3] = "WINDOWS"] = 3;
                values[valuesById[4] = "MAC"] = 4;
                return values;
            })();

            return SettingsSyncAction;
        })();

        SyncActionValue.StarAction = (function() {

            /**
             * Properties of a StarAction.
             * @memberof proto.SyncActionValue
             * @interface IStarAction
             * @property {boolean|null} [starred] StarAction starred
             */

            /**
             * Constructs a new StarAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a StarAction.
             * @implements IStarAction
             * @constructor
             * @param {proto.SyncActionValue.IStarAction=} [properties] Properties to set
             */
            function StarAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * StarAction starred.
             * @member {boolean|null|undefined} starred
             * @memberof proto.SyncActionValue.StarAction
             * @instance
             */
            StarAction.prototype.starred = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StarAction.prototype, "_starred", {
                get: $util.oneOfGetter($oneOfFields = ["starred"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StarAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.StarAction
             * @static
             * @param {proto.SyncActionValue.IStarAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.StarAction} StarAction instance
             */
            StarAction.create = function create(properties) {
                return new StarAction(properties);
            };

            /**
             * Encodes the specified StarAction message. Does not implicitly {@link proto.SyncActionValue.StarAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.StarAction
             * @static
             * @param {proto.SyncActionValue.IStarAction} message StarAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StarAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.starred != null && Object.hasOwnProperty.call(message, "starred"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.starred);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified StarAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.StarAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.StarAction
             * @static
             * @param {proto.SyncActionValue.IStarAction} message StarAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StarAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a StarAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.StarAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.StarAction} StarAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StarAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.starred = reader.bool();
                            message._starred = "starred";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a StarAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.StarAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.StarAction} StarAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StarAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a StarAction message.
             * @function verify
             * @memberof proto.SyncActionValue.StarAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StarAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.starred != null && Object.hasOwnProperty.call(message, "starred")) {
                    properties._starred = 1;
                    if (typeof message.starred !== "boolean")
                        return "starred: boolean expected";
                }
                return null;
            };

            /**
             * Creates a StarAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.StarAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.StarAction} StarAction
             */
            StarAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.StarAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.starred != null)
                    message.starred = Boolean(object.starred);
                return message;
            };

            /**
             * Creates a plain object from a StarAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.StarAction
             * @static
             * @param {proto.SyncActionValue.StarAction} message StarAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StarAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.starred != null && Object.hasOwnProperty.call(message, "starred"))
                    object.starred = message.starred;
                return object;
            };

            /**
             * Converts this StarAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.StarAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StarAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for StarAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.StarAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            StarAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.StarAction";
            };

            return StarAction;
        })();

        SyncActionValue.StatusPostOptInNotificationPreferencesAction = (function() {

            /**
             * Properties of a StatusPostOptInNotificationPreferencesAction.
             * @memberof proto.SyncActionValue
             * @interface IStatusPostOptInNotificationPreferencesAction
             * @property {boolean|null} [enabled] StatusPostOptInNotificationPreferencesAction enabled
             */

            /**
             * Constructs a new StatusPostOptInNotificationPreferencesAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a StatusPostOptInNotificationPreferencesAction.
             * @implements IStatusPostOptInNotificationPreferencesAction
             * @constructor
             * @param {proto.SyncActionValue.IStatusPostOptInNotificationPreferencesAction=} [properties] Properties to set
             */
            function StatusPostOptInNotificationPreferencesAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * StatusPostOptInNotificationPreferencesAction enabled.
             * @member {boolean|null|undefined} enabled
             * @memberof proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction
             * @instance
             */
            StatusPostOptInNotificationPreferencesAction.prototype.enabled = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusPostOptInNotificationPreferencesAction.prototype, "_enabled", {
                get: $util.oneOfGetter($oneOfFields = ["enabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StatusPostOptInNotificationPreferencesAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction
             * @static
             * @param {proto.SyncActionValue.IStatusPostOptInNotificationPreferencesAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction} StatusPostOptInNotificationPreferencesAction instance
             */
            StatusPostOptInNotificationPreferencesAction.create = function create(properties) {
                return new StatusPostOptInNotificationPreferencesAction(properties);
            };

            /**
             * Encodes the specified StatusPostOptInNotificationPreferencesAction message. Does not implicitly {@link proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction
             * @static
             * @param {proto.SyncActionValue.IStatusPostOptInNotificationPreferencesAction} message StatusPostOptInNotificationPreferencesAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StatusPostOptInNotificationPreferencesAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.enabled != null && Object.hasOwnProperty.call(message, "enabled"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.enabled);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified StatusPostOptInNotificationPreferencesAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction
             * @static
             * @param {proto.SyncActionValue.IStatusPostOptInNotificationPreferencesAction} message StatusPostOptInNotificationPreferencesAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StatusPostOptInNotificationPreferencesAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a StatusPostOptInNotificationPreferencesAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction} StatusPostOptInNotificationPreferencesAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StatusPostOptInNotificationPreferencesAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.enabled = reader.bool();
                            message._enabled = "enabled";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a StatusPostOptInNotificationPreferencesAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction} StatusPostOptInNotificationPreferencesAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StatusPostOptInNotificationPreferencesAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a StatusPostOptInNotificationPreferencesAction message.
             * @function verify
             * @memberof proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StatusPostOptInNotificationPreferencesAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.enabled != null && Object.hasOwnProperty.call(message, "enabled")) {
                    properties._enabled = 1;
                    if (typeof message.enabled !== "boolean")
                        return "enabled: boolean expected";
                }
                return null;
            };

            /**
             * Creates a StatusPostOptInNotificationPreferencesAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction} StatusPostOptInNotificationPreferencesAction
             */
            StatusPostOptInNotificationPreferencesAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.enabled != null)
                    message.enabled = Boolean(object.enabled);
                return message;
            };

            /**
             * Creates a plain object from a StatusPostOptInNotificationPreferencesAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction
             * @static
             * @param {proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction} message StatusPostOptInNotificationPreferencesAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StatusPostOptInNotificationPreferencesAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.enabled != null && Object.hasOwnProperty.call(message, "enabled"))
                    object.enabled = message.enabled;
                return object;
            };

            /**
             * Converts this StatusPostOptInNotificationPreferencesAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StatusPostOptInNotificationPreferencesAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for StatusPostOptInNotificationPreferencesAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            StatusPostOptInNotificationPreferencesAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction";
            };

            return StatusPostOptInNotificationPreferencesAction;
        })();

        SyncActionValue.StatusPrivacyAction = (function() {

            /**
             * Properties of a StatusPrivacyAction.
             * @memberof proto.SyncActionValue
             * @interface IStatusPrivacyAction
             * @property {proto.SyncActionValue.StatusPrivacyAction.StatusDistributionMode|null} [mode] StatusPrivacyAction mode
             * @property {Array.<string>|null} [userJid] StatusPrivacyAction userJid
             * @property {boolean|null} [shareToFb] StatusPrivacyAction shareToFb
             * @property {boolean|null} [shareToIg] StatusPrivacyAction shareToIg
             * @property {Array.<proto.SyncActionValue.StatusPrivacyAction.ICustomList>|null} [customLists] StatusPrivacyAction customLists
             * @property {Array.<proto.SyncActionValue.StatusPrivacyAction.StatusDistributionMode>|null} [modes] StatusPrivacyAction modes
             */

            /**
             * Constructs a new StatusPrivacyAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a StatusPrivacyAction.
             * @implements IStatusPrivacyAction
             * @constructor
             * @param {proto.SyncActionValue.IStatusPrivacyAction=} [properties] Properties to set
             */
            function StatusPrivacyAction(properties) {
                this.userJid = [];
                this.customLists = [];
                this.modes = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * StatusPrivacyAction mode.
             * @member {proto.SyncActionValue.StatusPrivacyAction.StatusDistributionMode|null|undefined} mode
             * @memberof proto.SyncActionValue.StatusPrivacyAction
             * @instance
             */
            StatusPrivacyAction.prototype.mode = null;

            /**
             * StatusPrivacyAction userJid.
             * @member {Array.<string>} userJid
             * @memberof proto.SyncActionValue.StatusPrivacyAction
             * @instance
             */
            StatusPrivacyAction.prototype.userJid = $util.emptyArray;

            /**
             * StatusPrivacyAction shareToFb.
             * @member {boolean|null|undefined} shareToFb
             * @memberof proto.SyncActionValue.StatusPrivacyAction
             * @instance
             */
            StatusPrivacyAction.prototype.shareToFb = null;

            /**
             * StatusPrivacyAction shareToIg.
             * @member {boolean|null|undefined} shareToIg
             * @memberof proto.SyncActionValue.StatusPrivacyAction
             * @instance
             */
            StatusPrivacyAction.prototype.shareToIg = null;

            /**
             * StatusPrivacyAction customLists.
             * @member {Array.<proto.SyncActionValue.StatusPrivacyAction.ICustomList>} customLists
             * @memberof proto.SyncActionValue.StatusPrivacyAction
             * @instance
             */
            StatusPrivacyAction.prototype.customLists = $util.emptyArray;

            /**
             * StatusPrivacyAction modes.
             * @member {Array.<proto.SyncActionValue.StatusPrivacyAction.StatusDistributionMode>} modes
             * @memberof proto.SyncActionValue.StatusPrivacyAction
             * @instance
             */
            StatusPrivacyAction.prototype.modes = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusPrivacyAction.prototype, "_mode", {
                get: $util.oneOfGetter($oneOfFields = ["mode"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusPrivacyAction.prototype, "_shareToFb", {
                get: $util.oneOfGetter($oneOfFields = ["shareToFb"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusPrivacyAction.prototype, "_shareToIg", {
                get: $util.oneOfGetter($oneOfFields = ["shareToIg"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StatusPrivacyAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.StatusPrivacyAction
             * @static
             * @param {proto.SyncActionValue.IStatusPrivacyAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.StatusPrivacyAction} StatusPrivacyAction instance
             */
            StatusPrivacyAction.create = function create(properties) {
                return new StatusPrivacyAction(properties);
            };

            /**
             * Encodes the specified StatusPrivacyAction message. Does not implicitly {@link proto.SyncActionValue.StatusPrivacyAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.StatusPrivacyAction
             * @static
             * @param {proto.SyncActionValue.IStatusPrivacyAction} message StatusPrivacyAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StatusPrivacyAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.mode);
                if (message.userJid != null && message.userJid.length)
                    for (var i = 0; i < message.userJid.length; ++i)
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.userJid[i]);
                if (message.shareToFb != null && Object.hasOwnProperty.call(message, "shareToFb"))
                    writer.uint32(/* id 3, wireType 0 =*/24).bool(message.shareToFb);
                if (message.shareToIg != null && Object.hasOwnProperty.call(message, "shareToIg"))
                    writer.uint32(/* id 4, wireType 0 =*/32).bool(message.shareToIg);
                if (message.customLists != null && message.customLists.length)
                    for (var i = 0; i < message.customLists.length; ++i)
                        $root.proto.SyncActionValue.StatusPrivacyAction.CustomList.encode(message.customLists[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                if (message.modes != null && message.modes.length) {
                    writer.uint32(/* id 6, wireType 2 =*/50).fork();
                    for (var i = 0; i < message.modes.length; ++i)
                        writer.int32(message.modes[i]);
                    writer.ldelim();
                }
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified StatusPrivacyAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.StatusPrivacyAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.StatusPrivacyAction
             * @static
             * @param {proto.SyncActionValue.IStatusPrivacyAction} message StatusPrivacyAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StatusPrivacyAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a StatusPrivacyAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.StatusPrivacyAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.StatusPrivacyAction} StatusPrivacyAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StatusPrivacyAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.mode = reader.int32();
                            message._mode = "mode";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(message.userJid && message.userJid.length))
                                message.userJid = [];
                            message.userJid.push(reader.stringVerify());
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.shareToFb = reader.bool();
                            message._shareToFb = "shareToFb";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.shareToIg = reader.bool();
                            message._shareToIg = "shareToIg";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            if (!(message.customLists && message.customLists.length))
                                message.customLists = [];
                            message.customLists.push($root.proto.SyncActionValue.StatusPrivacyAction.CustomList.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 6: {
                            if (u === 2) {
                                if (!(message.modes && message.modes.length))
                                    message.modes = [];
                                var end2 = reader.uint32() + reader.pos;
                                while (reader.pos < end2)
                                    message.modes.push(reader.int32());
                                continue;
                            }
                            if (u !== 0)
                                break;
                            if (!(message.modes && message.modes.length))
                                message.modes = [];
                            message.modes.push(reader.int32());
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a StatusPrivacyAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.StatusPrivacyAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.StatusPrivacyAction} StatusPrivacyAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StatusPrivacyAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a StatusPrivacyAction message.
             * @function verify
             * @memberof proto.SyncActionValue.StatusPrivacyAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StatusPrivacyAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.mode != null && Object.hasOwnProperty.call(message, "mode")) {
                    properties._mode = 1;
                    if (typeof message.mode !== "number" || (message.mode | 0) !== message.mode)
                        return "mode: enum value expected";
                }
                if (message.userJid != null && Object.hasOwnProperty.call(message, "userJid")) {
                    if (!Array.isArray(message.userJid))
                        return "userJid: array expected";
                    for (var i = 0; i < message.userJid.length; ++i)
                        if (!$util.isString(message.userJid[i]))
                            return "userJid: string[] expected";
                }
                if (message.shareToFb != null && Object.hasOwnProperty.call(message, "shareToFb")) {
                    properties._shareToFb = 1;
                    if (typeof message.shareToFb !== "boolean")
                        return "shareToFb: boolean expected";
                }
                if (message.shareToIg != null && Object.hasOwnProperty.call(message, "shareToIg")) {
                    properties._shareToIg = 1;
                    if (typeof message.shareToIg !== "boolean")
                        return "shareToIg: boolean expected";
                }
                if (message.customLists != null && Object.hasOwnProperty.call(message, "customLists")) {
                    if (!Array.isArray(message.customLists))
                        return "customLists: array expected";
                    for (var i = 0; i < message.customLists.length; ++i) {
                        var error = $root.proto.SyncActionValue.StatusPrivacyAction.CustomList.verify(message.customLists[i], q + 1);
                        if (error)
                            return "customLists." + error;
                    }
                }
                if (message.modes != null && Object.hasOwnProperty.call(message, "modes")) {
                    if (!Array.isArray(message.modes))
                        return "modes: array expected";
                    for (var i = 0; i < message.modes.length; ++i)
                        if (typeof message.modes[i] !== "number" || (message.modes[i] | 0) !== message.modes[i])
                            return "modes: enum value[] expected";
                }
                return null;
            };

            /**
             * Creates a StatusPrivacyAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.StatusPrivacyAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.StatusPrivacyAction} StatusPrivacyAction
             */
            StatusPrivacyAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.StatusPrivacyAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.mode) {
                case "ALLOW_LIST":
                case 0:
                    message.mode = 0;
                    break;
                case "DENY_LIST":
                case 1:
                    message.mode = 1;
                    break;
                case "CONTACTS":
                case 2:
                    message.mode = 2;
                    break;
                case "CLOSE_FRIENDS":
                case 3:
                    message.mode = 3;
                    break;
                case "CUSTOM_LIST":
                case 4:
                    message.mode = 4;
                    break;
                default:
                    if (typeof object.mode === "number" && (object.mode | 0) === object.mode)
                        message.mode = object.mode;
                }
                if (object.userJid) {
                    if (!Array.isArray(object.userJid))
                        throw TypeError(".proto.SyncActionValue.StatusPrivacyAction.userJid: array expected");
                    message.userJid = Array(object.userJid.length);
                    for (var i = 0; i < object.userJid.length; ++i)
                        message.userJid[i] = String(object.userJid[i]);
                }
                if (object.shareToFb != null)
                    message.shareToFb = Boolean(object.shareToFb);
                if (object.shareToIg != null)
                    message.shareToIg = Boolean(object.shareToIg);
                if (object.customLists) {
                    if (!Array.isArray(object.customLists))
                        throw TypeError(".proto.SyncActionValue.StatusPrivacyAction.customLists: array expected");
                    message.customLists = Array(object.customLists.length);
                    for (var i = 0; i < object.customLists.length; ++i) {
                        if (!$util.isObject(object.customLists[i]))
                            throw TypeError(".proto.SyncActionValue.StatusPrivacyAction.customLists: object expected");
                        message.customLists[i] = $root.proto.SyncActionValue.StatusPrivacyAction.CustomList.fromObject(object.customLists[i], q + 1);
                    }
                }
                if (object.modes) {
                    if (!Array.isArray(object.modes))
                        throw TypeError(".proto.SyncActionValue.StatusPrivacyAction.modes: array expected");
                    message.modes = [];
                    for (var i = 0; i < object.modes.length; ++i)
                        switch (object.modes[i]) {
                        case "ALLOW_LIST":
                        case 0:
                            message.modes[message.modes.length] = 0;
                            break;
                        case "DENY_LIST":
                        case 1:
                            message.modes[message.modes.length] = 1;
                            break;
                        case "CONTACTS":
                        case 2:
                            message.modes[message.modes.length] = 2;
                            break;
                        case "CLOSE_FRIENDS":
                        case 3:
                            message.modes[message.modes.length] = 3;
                            break;
                        case "CUSTOM_LIST":
                        case 4:
                            message.modes[message.modes.length] = 4;
                            break;
                        default:
                            if (typeof object.modes[i] === "number" && (object.modes[i] | 0) === object.modes[i])
                                message.modes[message.modes.length] = object.modes[i];
                        }
                }
                return message;
            };

            /**
             * Creates a plain object from a StatusPrivacyAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.StatusPrivacyAction
             * @static
             * @param {proto.SyncActionValue.StatusPrivacyAction} message StatusPrivacyAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StatusPrivacyAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.userJid = [];
                    object.customLists = [];
                    object.modes = [];
                }
                if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
                    object.mode = options.enums === String ? $root.proto.SyncActionValue.StatusPrivacyAction.StatusDistributionMode[message.mode] === undefined ? message.mode : $root.proto.SyncActionValue.StatusPrivacyAction.StatusDistributionMode[message.mode] : message.mode;
                if (message.userJid && message.userJid.length) {
                    object.userJid = Array(message.userJid.length);
                    for (var j = 0; j < message.userJid.length; ++j)
                        object.userJid[j] = message.userJid[j];
                }
                if (message.shareToFb != null && Object.hasOwnProperty.call(message, "shareToFb"))
                    object.shareToFb = message.shareToFb;
                if (message.shareToIg != null && Object.hasOwnProperty.call(message, "shareToIg"))
                    object.shareToIg = message.shareToIg;
                if (message.customLists && message.customLists.length) {
                    object.customLists = Array(message.customLists.length);
                    for (var j = 0; j < message.customLists.length; ++j)
                        object.customLists[j] = $root.proto.SyncActionValue.StatusPrivacyAction.CustomList.toObject(message.customLists[j], options, q + 1);
                }
                if (message.modes && message.modes.length) {
                    object.modes = Array(message.modes.length);
                    for (var j = 0; j < message.modes.length; ++j)
                        object.modes[j] = options.enums === String ? $root.proto.SyncActionValue.StatusPrivacyAction.StatusDistributionMode[message.modes[j]] === undefined ? message.modes[j] : $root.proto.SyncActionValue.StatusPrivacyAction.StatusDistributionMode[message.modes[j]] : message.modes[j];
                }
                return object;
            };

            /**
             * Converts this StatusPrivacyAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.StatusPrivacyAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StatusPrivacyAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for StatusPrivacyAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.StatusPrivacyAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            StatusPrivacyAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.StatusPrivacyAction";
            };

            StatusPrivacyAction.CustomList = (function() {

                /**
                 * Properties of a CustomList.
                 * @memberof proto.SyncActionValue.StatusPrivacyAction
                 * @interface ICustomList
                 * @property {string|null} [listId] CustomList listId
                 * @property {string|null} [name] CustomList name
                 * @property {string|null} [emoji] CustomList emoji
                 * @property {boolean|null} [isSelected] CustomList isSelected
                 * @property {Array.<string>|null} [userJid] CustomList userJid
                 */

                /**
                 * Constructs a new CustomList.
                 * @memberof proto.SyncActionValue.StatusPrivacyAction
                 * @classdesc Represents a CustomList.
                 * @implements ICustomList
                 * @constructor
                 * @param {proto.SyncActionValue.StatusPrivacyAction.ICustomList=} [properties] Properties to set
                 */
                function CustomList(properties) {
                    this.userJid = [];
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * CustomList listId.
                 * @member {string|null|undefined} listId
                 * @memberof proto.SyncActionValue.StatusPrivacyAction.CustomList
                 * @instance
                 */
                CustomList.prototype.listId = null;

                /**
                 * CustomList name.
                 * @member {string|null|undefined} name
                 * @memberof proto.SyncActionValue.StatusPrivacyAction.CustomList
                 * @instance
                 */
                CustomList.prototype.name = null;

                /**
                 * CustomList emoji.
                 * @member {string|null|undefined} emoji
                 * @memberof proto.SyncActionValue.StatusPrivacyAction.CustomList
                 * @instance
                 */
                CustomList.prototype.emoji = null;

                /**
                 * CustomList isSelected.
                 * @member {boolean|null|undefined} isSelected
                 * @memberof proto.SyncActionValue.StatusPrivacyAction.CustomList
                 * @instance
                 */
                CustomList.prototype.isSelected = null;

                /**
                 * CustomList userJid.
                 * @member {Array.<string>} userJid
                 * @memberof proto.SyncActionValue.StatusPrivacyAction.CustomList
                 * @instance
                 */
                CustomList.prototype.userJid = $util.emptyArray;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CustomList.prototype, "_listId", {
                    get: $util.oneOfGetter($oneOfFields = ["listId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CustomList.prototype, "_name", {
                    get: $util.oneOfGetter($oneOfFields = ["name"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CustomList.prototype, "_emoji", {
                    get: $util.oneOfGetter($oneOfFields = ["emoji"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CustomList.prototype, "_isSelected", {
                    get: $util.oneOfGetter($oneOfFields = ["isSelected"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new CustomList instance using the specified properties.
                 * @function create
                 * @memberof proto.SyncActionValue.StatusPrivacyAction.CustomList
                 * @static
                 * @param {proto.SyncActionValue.StatusPrivacyAction.ICustomList=} [properties] Properties to set
                 * @returns {proto.SyncActionValue.StatusPrivacyAction.CustomList} CustomList instance
                 */
                CustomList.create = function create(properties) {
                    return new CustomList(properties);
                };

                /**
                 * Encodes the specified CustomList message. Does not implicitly {@link proto.SyncActionValue.StatusPrivacyAction.CustomList.verify|verify} messages.
                 * @function encode
                 * @memberof proto.SyncActionValue.StatusPrivacyAction.CustomList
                 * @static
                 * @param {proto.SyncActionValue.StatusPrivacyAction.ICustomList} message CustomList message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                CustomList.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.listId != null && Object.hasOwnProperty.call(message, "listId"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.listId);
                    if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
                    if (message.emoji != null && Object.hasOwnProperty.call(message, "emoji"))
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.emoji);
                    if (message.isSelected != null && Object.hasOwnProperty.call(message, "isSelected"))
                        writer.uint32(/* id 4, wireType 0 =*/32).bool(message.isSelected);
                    if (message.userJid != null && message.userJid.length)
                        for (var i = 0; i < message.userJid.length; ++i)
                            writer.uint32(/* id 5, wireType 2 =*/42).string(message.userJid[i]);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified CustomList message, length delimited. Does not implicitly {@link proto.SyncActionValue.StatusPrivacyAction.CustomList.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.SyncActionValue.StatusPrivacyAction.CustomList
                 * @static
                 * @param {proto.SyncActionValue.StatusPrivacyAction.ICustomList} message CustomList message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                CustomList.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a CustomList message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.SyncActionValue.StatusPrivacyAction.CustomList
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.SyncActionValue.StatusPrivacyAction.CustomList} CustomList
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                CustomList.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.listId = reader.stringVerify();
                                message._listId = "listId";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.name = reader.stringVerify();
                                message._name = "name";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.emoji = reader.stringVerify();
                                message._emoji = "emoji";
                                continue;
                            }
                        case 4: {
                                if (u !== 0)
                                    break;
                                message.isSelected = reader.bool();
                                message._isSelected = "isSelected";
                                continue;
                            }
                        case 5: {
                                if (u !== 2)
                                    break;
                                if (!(message.userJid && message.userJid.length))
                                    message.userJid = [];
                                message.userJid.push(reader.stringVerify());
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a CustomList message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.SyncActionValue.StatusPrivacyAction.CustomList
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.SyncActionValue.StatusPrivacyAction.CustomList} CustomList
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                CustomList.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a CustomList message.
                 * @function verify
                 * @memberof proto.SyncActionValue.StatusPrivacyAction.CustomList
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                CustomList.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.listId != null && Object.hasOwnProperty.call(message, "listId")) {
                        properties._listId = 1;
                        if (!$util.isString(message.listId))
                            return "listId: string expected";
                    }
                    if (message.name != null && Object.hasOwnProperty.call(message, "name")) {
                        properties._name = 1;
                        if (!$util.isString(message.name))
                            return "name: string expected";
                    }
                    if (message.emoji != null && Object.hasOwnProperty.call(message, "emoji")) {
                        properties._emoji = 1;
                        if (!$util.isString(message.emoji))
                            return "emoji: string expected";
                    }
                    if (message.isSelected != null && Object.hasOwnProperty.call(message, "isSelected")) {
                        properties._isSelected = 1;
                        if (typeof message.isSelected !== "boolean")
                            return "isSelected: boolean expected";
                    }
                    if (message.userJid != null && Object.hasOwnProperty.call(message, "userJid")) {
                        if (!Array.isArray(message.userJid))
                            return "userJid: array expected";
                        for (var i = 0; i < message.userJid.length; ++i)
                            if (!$util.isString(message.userJid[i]))
                                return "userJid: string[] expected";
                    }
                    return null;
                };

                /**
                 * Creates a CustomList message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.SyncActionValue.StatusPrivacyAction.CustomList
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.SyncActionValue.StatusPrivacyAction.CustomList} CustomList
                 */
                CustomList.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.SyncActionValue.StatusPrivacyAction.CustomList: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.listId != null)
                        message.listId = String(object.listId);
                    if (object.name != null)
                        message.name = String(object.name);
                    if (object.emoji != null)
                        message.emoji = String(object.emoji);
                    if (object.isSelected != null)
                        message.isSelected = Boolean(object.isSelected);
                    if (object.userJid) {
                        if (!Array.isArray(object.userJid))
                            throw TypeError(".proto.SyncActionValue.StatusPrivacyAction.CustomList.userJid: array expected");
                        message.userJid = Array(object.userJid.length);
                        for (var i = 0; i < object.userJid.length; ++i)
                            message.userJid[i] = String(object.userJid[i]);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from a CustomList message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.SyncActionValue.StatusPrivacyAction.CustomList
                 * @static
                 * @param {proto.SyncActionValue.StatusPrivacyAction.CustomList} message CustomList
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                CustomList.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (options.arrays || options.defaults)
                        object.userJid = [];
                    if (message.listId != null && Object.hasOwnProperty.call(message, "listId"))
                        object.listId = message.listId;
                    if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                        object.name = message.name;
                    if (message.emoji != null && Object.hasOwnProperty.call(message, "emoji"))
                        object.emoji = message.emoji;
                    if (message.isSelected != null && Object.hasOwnProperty.call(message, "isSelected"))
                        object.isSelected = message.isSelected;
                    if (message.userJid && message.userJid.length) {
                        object.userJid = Array(message.userJid.length);
                        for (var j = 0; j < message.userJid.length; ++j)
                            object.userJid[j] = message.userJid[j];
                    }
                    return object;
                };

                /**
                 * Converts this CustomList to JSON.
                 * @function toJSON
                 * @memberof proto.SyncActionValue.StatusPrivacyAction.CustomList
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                CustomList.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for CustomList
                 * @function getTypeUrl
                 * @memberof proto.SyncActionValue.StatusPrivacyAction.CustomList
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                CustomList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.SyncActionValue.StatusPrivacyAction.CustomList";
                };

                return CustomList;
            })();

            /**
             * StatusDistributionMode enum.
             * @name proto.SyncActionValue.StatusPrivacyAction.StatusDistributionMode
             * @enum {number}
             * @property {number} ALLOW_LIST=0 ALLOW_LIST value
             * @property {number} DENY_LIST=1 DENY_LIST value
             * @property {number} CONTACTS=2 CONTACTS value
             * @property {number} CLOSE_FRIENDS=3 CLOSE_FRIENDS value
             * @property {number} CUSTOM_LIST=4 CUSTOM_LIST value
             */
            StatusPrivacyAction.StatusDistributionMode = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "ALLOW_LIST"] = 0;
                values[valuesById[1] = "DENY_LIST"] = 1;
                values[valuesById[2] = "CONTACTS"] = 2;
                values[valuesById[3] = "CLOSE_FRIENDS"] = 3;
                values[valuesById[4] = "CUSTOM_LIST"] = 4;
                return values;
            })();

            return StatusPrivacyAction;
        })();

        SyncActionValue.StickerAction = (function() {

            /**
             * Properties of a StickerAction.
             * @memberof proto.SyncActionValue
             * @interface IStickerAction
             * @property {string|null} [url] StickerAction url
             * @property {Uint8Array|null} [fileEncSha256] StickerAction fileEncSha256
             * @property {Uint8Array|null} [mediaKey] StickerAction mediaKey
             * @property {string|null} [mimetype] StickerAction mimetype
             * @property {number|null} [height] StickerAction height
             * @property {number|null} [width] StickerAction width
             * @property {string|null} [directPath] StickerAction directPath
             * @property {number|Long|null} [fileLength] StickerAction fileLength
             * @property {boolean|null} [isFavorite] StickerAction isFavorite
             * @property {number|null} [deviceIdHint] StickerAction deviceIdHint
             * @property {boolean|null} [isLottie] StickerAction isLottie
             * @property {string|null} [imageHash] StickerAction imageHash
             * @property {boolean|null} [isAvatarSticker] StickerAction isAvatarSticker
             */

            /**
             * Constructs a new StickerAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a StickerAction.
             * @implements IStickerAction
             * @constructor
             * @param {proto.SyncActionValue.IStickerAction=} [properties] Properties to set
             */
            function StickerAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * StickerAction url.
             * @member {string|null|undefined} url
             * @memberof proto.SyncActionValue.StickerAction
             * @instance
             */
            StickerAction.prototype.url = null;

            /**
             * StickerAction fileEncSha256.
             * @member {Uint8Array|null|undefined} fileEncSha256
             * @memberof proto.SyncActionValue.StickerAction
             * @instance
             */
            StickerAction.prototype.fileEncSha256 = null;

            /**
             * StickerAction mediaKey.
             * @member {Uint8Array|null|undefined} mediaKey
             * @memberof proto.SyncActionValue.StickerAction
             * @instance
             */
            StickerAction.prototype.mediaKey = null;

            /**
             * StickerAction mimetype.
             * @member {string|null|undefined} mimetype
             * @memberof proto.SyncActionValue.StickerAction
             * @instance
             */
            StickerAction.prototype.mimetype = null;

            /**
             * StickerAction height.
             * @member {number|null|undefined} height
             * @memberof proto.SyncActionValue.StickerAction
             * @instance
             */
            StickerAction.prototype.height = null;

            /**
             * StickerAction width.
             * @member {number|null|undefined} width
             * @memberof proto.SyncActionValue.StickerAction
             * @instance
             */
            StickerAction.prototype.width = null;

            /**
             * StickerAction directPath.
             * @member {string|null|undefined} directPath
             * @memberof proto.SyncActionValue.StickerAction
             * @instance
             */
            StickerAction.prototype.directPath = null;

            /**
             * StickerAction fileLength.
             * @member {number|Long|null|undefined} fileLength
             * @memberof proto.SyncActionValue.StickerAction
             * @instance
             */
            StickerAction.prototype.fileLength = null;

            /**
             * StickerAction isFavorite.
             * @member {boolean|null|undefined} isFavorite
             * @memberof proto.SyncActionValue.StickerAction
             * @instance
             */
            StickerAction.prototype.isFavorite = null;

            /**
             * StickerAction deviceIdHint.
             * @member {number|null|undefined} deviceIdHint
             * @memberof proto.SyncActionValue.StickerAction
             * @instance
             */
            StickerAction.prototype.deviceIdHint = null;

            /**
             * StickerAction isLottie.
             * @member {boolean|null|undefined} isLottie
             * @memberof proto.SyncActionValue.StickerAction
             * @instance
             */
            StickerAction.prototype.isLottie = null;

            /**
             * StickerAction imageHash.
             * @member {string|null|undefined} imageHash
             * @memberof proto.SyncActionValue.StickerAction
             * @instance
             */
            StickerAction.prototype.imageHash = null;

            /**
             * StickerAction isAvatarSticker.
             * @member {boolean|null|undefined} isAvatarSticker
             * @memberof proto.SyncActionValue.StickerAction
             * @instance
             */
            StickerAction.prototype.isAvatarSticker = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerAction.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerAction.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerAction.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerAction.prototype, "_mimetype", {
                get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerAction.prototype, "_height", {
                get: $util.oneOfGetter($oneOfFields = ["height"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerAction.prototype, "_width", {
                get: $util.oneOfGetter($oneOfFields = ["width"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerAction.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerAction.prototype, "_fileLength", {
                get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerAction.prototype, "_isFavorite", {
                get: $util.oneOfGetter($oneOfFields = ["isFavorite"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerAction.prototype, "_deviceIdHint", {
                get: $util.oneOfGetter($oneOfFields = ["deviceIdHint"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerAction.prototype, "_isLottie", {
                get: $util.oneOfGetter($oneOfFields = ["isLottie"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerAction.prototype, "_imageHash", {
                get: $util.oneOfGetter($oneOfFields = ["imageHash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StickerAction.prototype, "_isAvatarSticker", {
                get: $util.oneOfGetter($oneOfFields = ["isAvatarSticker"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new StickerAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.StickerAction
             * @static
             * @param {proto.SyncActionValue.IStickerAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.StickerAction} StickerAction instance
             */
            StickerAction.create = function create(properties) {
                return new StickerAction(properties);
            };

            /**
             * Encodes the specified StickerAction message. Does not implicitly {@link proto.SyncActionValue.StickerAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.StickerAction
             * @static
             * @param {proto.SyncActionValue.IStickerAction} message StickerAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StickerAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.url);
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.fileEncSha256);
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.mediaKey);
                if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.mimetype);
                if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                    writer.uint32(/* id 5, wireType 0 =*/40).uint32(message.height);
                if (message.width != null && Object.hasOwnProperty.call(message, "width"))
                    writer.uint32(/* id 6, wireType 0 =*/48).uint32(message.width);
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.directPath);
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                    writer.uint32(/* id 8, wireType 0 =*/64).uint64(message.fileLength);
                if (message.isFavorite != null && Object.hasOwnProperty.call(message, "isFavorite"))
                    writer.uint32(/* id 9, wireType 0 =*/72).bool(message.isFavorite);
                if (message.deviceIdHint != null && Object.hasOwnProperty.call(message, "deviceIdHint"))
                    writer.uint32(/* id 10, wireType 0 =*/80).uint32(message.deviceIdHint);
                if (message.isLottie != null && Object.hasOwnProperty.call(message, "isLottie"))
                    writer.uint32(/* id 11, wireType 0 =*/88).bool(message.isLottie);
                if (message.imageHash != null && Object.hasOwnProperty.call(message, "imageHash"))
                    writer.uint32(/* id 12, wireType 2 =*/98).string(message.imageHash);
                if (message.isAvatarSticker != null && Object.hasOwnProperty.call(message, "isAvatarSticker"))
                    writer.uint32(/* id 13, wireType 0 =*/104).bool(message.isAvatarSticker);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified StickerAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.StickerAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.StickerAction
             * @static
             * @param {proto.SyncActionValue.IStickerAction} message StickerAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            StickerAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a StickerAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.StickerAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.StickerAction} StickerAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StickerAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.url = reader.stringVerify();
                            message._url = "url";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.fileEncSha256 = reader.bytes();
                            message._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.mediaKey = reader.bytes();
                            message._mediaKey = "mediaKey";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.mimetype = reader.stringVerify();
                            message._mimetype = "mimetype";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            message.height = reader.uint32();
                            message._height = "height";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            message.width = reader.uint32();
                            message._width = "width";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            message.directPath = reader.stringVerify();
                            message._directPath = "directPath";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            message.fileLength = reader.uint64();
                            message._fileLength = "fileLength";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            message.isFavorite = reader.bool();
                            message._isFavorite = "isFavorite";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            message.deviceIdHint = reader.uint32();
                            message._deviceIdHint = "deviceIdHint";
                            continue;
                        }
                    case 11: {
                            if (u !== 0)
                                break;
                            message.isLottie = reader.bool();
                            message._isLottie = "isLottie";
                            continue;
                        }
                    case 12: {
                            if (u !== 2)
                                break;
                            message.imageHash = reader.stringVerify();
                            message._imageHash = "imageHash";
                            continue;
                        }
                    case 13: {
                            if (u !== 0)
                                break;
                            message.isAvatarSticker = reader.bool();
                            message._isAvatarSticker = "isAvatarSticker";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a StickerAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.StickerAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.StickerAction} StickerAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            StickerAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a StickerAction message.
             * @function verify
             * @memberof proto.SyncActionValue.StickerAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            StickerAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.url != null && Object.hasOwnProperty.call(message, "url")) {
                    properties._url = 1;
                    if (!$util.isString(message.url))
                        return "url: string expected";
                }
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256")) {
                    properties._fileEncSha256 = 1;
                    if (!(message.fileEncSha256 && typeof message.fileEncSha256.length === "number" || $util.isString(message.fileEncSha256)))
                        return "fileEncSha256: buffer expected";
                }
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey")) {
                    properties._mediaKey = 1;
                    if (!(message.mediaKey && typeof message.mediaKey.length === "number" || $util.isString(message.mediaKey)))
                        return "mediaKey: buffer expected";
                }
                if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype")) {
                    properties._mimetype = 1;
                    if (!$util.isString(message.mimetype))
                        return "mimetype: string expected";
                }
                if (message.height != null && Object.hasOwnProperty.call(message, "height")) {
                    properties._height = 1;
                    if (!$util.isInteger(message.height))
                        return "height: integer expected";
                }
                if (message.width != null && Object.hasOwnProperty.call(message, "width")) {
                    properties._width = 1;
                    if (!$util.isInteger(message.width))
                        return "width: integer expected";
                }
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath")) {
                    properties._directPath = 1;
                    if (!$util.isString(message.directPath))
                        return "directPath: string expected";
                }
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength")) {
                    properties._fileLength = 1;
                    if (!$util.isInteger(message.fileLength) && !(message.fileLength && $util.isInteger(message.fileLength.low) && $util.isInteger(message.fileLength.high)))
                        return "fileLength: integer|Long expected";
                }
                if (message.isFavorite != null && Object.hasOwnProperty.call(message, "isFavorite")) {
                    properties._isFavorite = 1;
                    if (typeof message.isFavorite !== "boolean")
                        return "isFavorite: boolean expected";
                }
                if (message.deviceIdHint != null && Object.hasOwnProperty.call(message, "deviceIdHint")) {
                    properties._deviceIdHint = 1;
                    if (!$util.isInteger(message.deviceIdHint))
                        return "deviceIdHint: integer expected";
                }
                if (message.isLottie != null && Object.hasOwnProperty.call(message, "isLottie")) {
                    properties._isLottie = 1;
                    if (typeof message.isLottie !== "boolean")
                        return "isLottie: boolean expected";
                }
                if (message.imageHash != null && Object.hasOwnProperty.call(message, "imageHash")) {
                    properties._imageHash = 1;
                    if (!$util.isString(message.imageHash))
                        return "imageHash: string expected";
                }
                if (message.isAvatarSticker != null && Object.hasOwnProperty.call(message, "isAvatarSticker")) {
                    properties._isAvatarSticker = 1;
                    if (typeof message.isAvatarSticker !== "boolean")
                        return "isAvatarSticker: boolean expected";
                }
                return null;
            };

            /**
             * Creates a StickerAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.StickerAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.StickerAction} StickerAction
             */
            StickerAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.StickerAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.url != null)
                    message.url = String(object.url);
                if (object.fileEncSha256 != null)
                    if (typeof object.fileEncSha256 === "string")
                        $util.base64.decode(object.fileEncSha256, message.fileEncSha256 = $util.newBuffer($util.base64.length(object.fileEncSha256)), 0);
                    else if (object.fileEncSha256.length >= 0)
                        message.fileEncSha256 = object.fileEncSha256;
                if (object.mediaKey != null)
                    if (typeof object.mediaKey === "string")
                        $util.base64.decode(object.mediaKey, message.mediaKey = $util.newBuffer($util.base64.length(object.mediaKey)), 0);
                    else if (object.mediaKey.length >= 0)
                        message.mediaKey = object.mediaKey;
                if (object.mimetype != null)
                    message.mimetype = String(object.mimetype);
                if (object.height != null)
                    message.height = object.height >>> 0;
                if (object.width != null)
                    message.width = object.width >>> 0;
                if (object.directPath != null)
                    message.directPath = String(object.directPath);
                if (object.fileLength != null)
                    if ($util.Long)
                        message.fileLength = $util.Long.fromValue(object.fileLength, true);
                    else if (typeof object.fileLength === "string")
                        message.fileLength = parseInt(object.fileLength, 10);
                    else if (typeof object.fileLength === "number")
                        message.fileLength = object.fileLength;
                    else if (typeof object.fileLength === "object")
                        message.fileLength = new $util.LongBits(object.fileLength.low >>> 0, object.fileLength.high >>> 0).toNumber(true);
                if (object.isFavorite != null)
                    message.isFavorite = Boolean(object.isFavorite);
                if (object.deviceIdHint != null)
                    message.deviceIdHint = object.deviceIdHint >>> 0;
                if (object.isLottie != null)
                    message.isLottie = Boolean(object.isLottie);
                if (object.imageHash != null)
                    message.imageHash = String(object.imageHash);
                if (object.isAvatarSticker != null)
                    message.isAvatarSticker = Boolean(object.isAvatarSticker);
                return message;
            };

            /**
             * Creates a plain object from a StickerAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.StickerAction
             * @static
             * @param {proto.SyncActionValue.StickerAction} message StickerAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            StickerAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    object.url = message.url;
                if (message.fileEncSha256 != null && Object.hasOwnProperty.call(message, "fileEncSha256"))
                    object.fileEncSha256 = options.bytes === String ? $util.base64.encode(message.fileEncSha256, 0, message.fileEncSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.fileEncSha256) : message.fileEncSha256;
                if (message.mediaKey != null && Object.hasOwnProperty.call(message, "mediaKey"))
                    object.mediaKey = options.bytes === String ? $util.base64.encode(message.mediaKey, 0, message.mediaKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.mediaKey) : message.mediaKey;
                if (message.mimetype != null && Object.hasOwnProperty.call(message, "mimetype"))
                    object.mimetype = message.mimetype;
                if (message.height != null && Object.hasOwnProperty.call(message, "height"))
                    object.height = message.height;
                if (message.width != null && Object.hasOwnProperty.call(message, "width"))
                    object.width = message.width;
                if (message.directPath != null && Object.hasOwnProperty.call(message, "directPath"))
                    object.directPath = message.directPath;
                if (message.fileLength != null && Object.hasOwnProperty.call(message, "fileLength"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.fileLength = typeof message.fileLength === "number" ? BigInt(message.fileLength) : $util.Long.fromBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0, true).toBigInt();
                    else if (typeof message.fileLength === "number")
                        object.fileLength = options.longs === String ? String(message.fileLength) : message.fileLength;
                    else
                        object.fileLength = options.longs === String ? $util.Long.prototype.toString.call(message.fileLength) : options.longs === Number ? new $util.LongBits(message.fileLength.low >>> 0, message.fileLength.high >>> 0).toNumber(true) : message.fileLength;
                if (message.isFavorite != null && Object.hasOwnProperty.call(message, "isFavorite"))
                    object.isFavorite = message.isFavorite;
                if (message.deviceIdHint != null && Object.hasOwnProperty.call(message, "deviceIdHint"))
                    object.deviceIdHint = message.deviceIdHint;
                if (message.isLottie != null && Object.hasOwnProperty.call(message, "isLottie"))
                    object.isLottie = message.isLottie;
                if (message.imageHash != null && Object.hasOwnProperty.call(message, "imageHash"))
                    object.imageHash = message.imageHash;
                if (message.isAvatarSticker != null && Object.hasOwnProperty.call(message, "isAvatarSticker"))
                    object.isAvatarSticker = message.isAvatarSticker;
                return object;
            };

            /**
             * Converts this StickerAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.StickerAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            StickerAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for StickerAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.StickerAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            StickerAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.StickerAction";
            };

            return StickerAction;
        })();

        SyncActionValue.SubscriptionAction = (function() {

            /**
             * Properties of a SubscriptionAction.
             * @memberof proto.SyncActionValue
             * @interface ISubscriptionAction
             * @property {boolean|null} [isDeactivated] SubscriptionAction isDeactivated
             * @property {boolean|null} [isAutoRenewing] SubscriptionAction isAutoRenewing
             * @property {number|Long|null} [expirationDate] SubscriptionAction expirationDate
             */

            /**
             * Constructs a new SubscriptionAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a SubscriptionAction.
             * @implements ISubscriptionAction
             * @constructor
             * @param {proto.SyncActionValue.ISubscriptionAction=} [properties] Properties to set
             */
            function SubscriptionAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * SubscriptionAction isDeactivated.
             * @member {boolean|null|undefined} isDeactivated
             * @memberof proto.SyncActionValue.SubscriptionAction
             * @instance
             */
            SubscriptionAction.prototype.isDeactivated = null;

            /**
             * SubscriptionAction isAutoRenewing.
             * @member {boolean|null|undefined} isAutoRenewing
             * @memberof proto.SyncActionValue.SubscriptionAction
             * @instance
             */
            SubscriptionAction.prototype.isAutoRenewing = null;

            /**
             * SubscriptionAction expirationDate.
             * @member {number|Long|null|undefined} expirationDate
             * @memberof proto.SyncActionValue.SubscriptionAction
             * @instance
             */
            SubscriptionAction.prototype.expirationDate = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SubscriptionAction.prototype, "_isDeactivated", {
                get: $util.oneOfGetter($oneOfFields = ["isDeactivated"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SubscriptionAction.prototype, "_isAutoRenewing", {
                get: $util.oneOfGetter($oneOfFields = ["isAutoRenewing"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SubscriptionAction.prototype, "_expirationDate", {
                get: $util.oneOfGetter($oneOfFields = ["expirationDate"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new SubscriptionAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.SubscriptionAction
             * @static
             * @param {proto.SyncActionValue.ISubscriptionAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.SubscriptionAction} SubscriptionAction instance
             */
            SubscriptionAction.create = function create(properties) {
                return new SubscriptionAction(properties);
            };

            /**
             * Encodes the specified SubscriptionAction message. Does not implicitly {@link proto.SyncActionValue.SubscriptionAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.SubscriptionAction
             * @static
             * @param {proto.SyncActionValue.ISubscriptionAction} message SubscriptionAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SubscriptionAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.isDeactivated != null && Object.hasOwnProperty.call(message, "isDeactivated"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.isDeactivated);
                if (message.isAutoRenewing != null && Object.hasOwnProperty.call(message, "isAutoRenewing"))
                    writer.uint32(/* id 2, wireType 0 =*/16).bool(message.isAutoRenewing);
                if (message.expirationDate != null && Object.hasOwnProperty.call(message, "expirationDate"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.expirationDate);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SubscriptionAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.SubscriptionAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.SubscriptionAction
             * @static
             * @param {proto.SyncActionValue.ISubscriptionAction} message SubscriptionAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SubscriptionAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a SubscriptionAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.SubscriptionAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.SubscriptionAction} SubscriptionAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SubscriptionAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.isDeactivated = reader.bool();
                            message._isDeactivated = "isDeactivated";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.isAutoRenewing = reader.bool();
                            message._isAutoRenewing = "isAutoRenewing";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            message.expirationDate = reader.int64();
                            message._expirationDate = "expirationDate";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a SubscriptionAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.SubscriptionAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.SubscriptionAction} SubscriptionAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SubscriptionAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SubscriptionAction message.
             * @function verify
             * @memberof proto.SyncActionValue.SubscriptionAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SubscriptionAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.isDeactivated != null && Object.hasOwnProperty.call(message, "isDeactivated")) {
                    properties._isDeactivated = 1;
                    if (typeof message.isDeactivated !== "boolean")
                        return "isDeactivated: boolean expected";
                }
                if (message.isAutoRenewing != null && Object.hasOwnProperty.call(message, "isAutoRenewing")) {
                    properties._isAutoRenewing = 1;
                    if (typeof message.isAutoRenewing !== "boolean")
                        return "isAutoRenewing: boolean expected";
                }
                if (message.expirationDate != null && Object.hasOwnProperty.call(message, "expirationDate")) {
                    properties._expirationDate = 1;
                    if (!$util.isInteger(message.expirationDate) && !(message.expirationDate && $util.isInteger(message.expirationDate.low) && $util.isInteger(message.expirationDate.high)))
                        return "expirationDate: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a SubscriptionAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.SubscriptionAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.SubscriptionAction} SubscriptionAction
             */
            SubscriptionAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.SubscriptionAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.isDeactivated != null)
                    message.isDeactivated = Boolean(object.isDeactivated);
                if (object.isAutoRenewing != null)
                    message.isAutoRenewing = Boolean(object.isAutoRenewing);
                if (object.expirationDate != null)
                    if ($util.Long)
                        message.expirationDate = $util.Long.fromValue(object.expirationDate, false);
                    else if (typeof object.expirationDate === "string")
                        message.expirationDate = parseInt(object.expirationDate, 10);
                    else if (typeof object.expirationDate === "number")
                        message.expirationDate = object.expirationDate;
                    else if (typeof object.expirationDate === "object")
                        message.expirationDate = new $util.LongBits(object.expirationDate.low >>> 0, object.expirationDate.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a SubscriptionAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.SubscriptionAction
             * @static
             * @param {proto.SyncActionValue.SubscriptionAction} message SubscriptionAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SubscriptionAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.isDeactivated != null && Object.hasOwnProperty.call(message, "isDeactivated"))
                    object.isDeactivated = message.isDeactivated;
                if (message.isAutoRenewing != null && Object.hasOwnProperty.call(message, "isAutoRenewing"))
                    object.isAutoRenewing = message.isAutoRenewing;
                if (message.expirationDate != null && Object.hasOwnProperty.call(message, "expirationDate"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.expirationDate = typeof message.expirationDate === "number" ? BigInt(message.expirationDate) : $util.Long.fromBits(message.expirationDate.low >>> 0, message.expirationDate.high >>> 0, false).toBigInt();
                    else if (typeof message.expirationDate === "number")
                        object.expirationDate = options.longs === String ? String(message.expirationDate) : message.expirationDate;
                    else
                        object.expirationDate = options.longs === String ? $util.Long.prototype.toString.call(message.expirationDate) : options.longs === Number ? new $util.LongBits(message.expirationDate.low >>> 0, message.expirationDate.high >>> 0).toNumber() : message.expirationDate;
                return object;
            };

            /**
             * Converts this SubscriptionAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.SubscriptionAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SubscriptionAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for SubscriptionAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.SubscriptionAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            SubscriptionAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.SubscriptionAction";
            };

            return SubscriptionAction;
        })();

        SyncActionValue.SubscriptionsSyncV2Action = (function() {

            /**
             * Properties of a SubscriptionsSyncV2Action.
             * @memberof proto.SyncActionValue
             * @interface ISubscriptionsSyncV2Action
             * @property {Array.<proto.SyncActionValue.SubscriptionsSyncV2Action.ISubscriptionInfo>|null} [subscriptions] SubscriptionsSyncV2Action subscriptions
             * @property {Array.<proto.SyncActionValue.SubscriptionsSyncV2Action.IPaidFeature>|null} [paidFeature] SubscriptionsSyncV2Action paidFeature
             */

            /**
             * Constructs a new SubscriptionsSyncV2Action.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a SubscriptionsSyncV2Action.
             * @implements ISubscriptionsSyncV2Action
             * @constructor
             * @param {proto.SyncActionValue.ISubscriptionsSyncV2Action=} [properties] Properties to set
             */
            function SubscriptionsSyncV2Action(properties) {
                this.subscriptions = [];
                this.paidFeature = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * SubscriptionsSyncV2Action subscriptions.
             * @member {Array.<proto.SyncActionValue.SubscriptionsSyncV2Action.ISubscriptionInfo>} subscriptions
             * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action
             * @instance
             */
            SubscriptionsSyncV2Action.prototype.subscriptions = $util.emptyArray;

            /**
             * SubscriptionsSyncV2Action paidFeature.
             * @member {Array.<proto.SyncActionValue.SubscriptionsSyncV2Action.IPaidFeature>} paidFeature
             * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action
             * @instance
             */
            SubscriptionsSyncV2Action.prototype.paidFeature = $util.emptyArray;

            /**
             * Creates a new SubscriptionsSyncV2Action instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action
             * @static
             * @param {proto.SyncActionValue.ISubscriptionsSyncV2Action=} [properties] Properties to set
             * @returns {proto.SyncActionValue.SubscriptionsSyncV2Action} SubscriptionsSyncV2Action instance
             */
            SubscriptionsSyncV2Action.create = function create(properties) {
                return new SubscriptionsSyncV2Action(properties);
            };

            /**
             * Encodes the specified SubscriptionsSyncV2Action message. Does not implicitly {@link proto.SyncActionValue.SubscriptionsSyncV2Action.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action
             * @static
             * @param {proto.SyncActionValue.ISubscriptionsSyncV2Action} message SubscriptionsSyncV2Action message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SubscriptionsSyncV2Action.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.subscriptions != null && message.subscriptions.length)
                    for (var i = 0; i < message.subscriptions.length; ++i)
                        $root.proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo.encode(message.subscriptions[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.paidFeature != null && message.paidFeature.length)
                    for (var i = 0; i < message.paidFeature.length; ++i)
                        $root.proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature.encode(message.paidFeature[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SubscriptionsSyncV2Action message, length delimited. Does not implicitly {@link proto.SyncActionValue.SubscriptionsSyncV2Action.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action
             * @static
             * @param {proto.SyncActionValue.ISubscriptionsSyncV2Action} message SubscriptionsSyncV2Action message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SubscriptionsSyncV2Action.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a SubscriptionsSyncV2Action message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.SubscriptionsSyncV2Action} SubscriptionsSyncV2Action
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SubscriptionsSyncV2Action.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(message.subscriptions && message.subscriptions.length))
                                message.subscriptions = [];
                            message.subscriptions.push($root.proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(message.paidFeature && message.paidFeature.length))
                                message.paidFeature = [];
                            message.paidFeature.push($root.proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a SubscriptionsSyncV2Action message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.SubscriptionsSyncV2Action} SubscriptionsSyncV2Action
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SubscriptionsSyncV2Action.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SubscriptionsSyncV2Action message.
             * @function verify
             * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SubscriptionsSyncV2Action.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.subscriptions != null && Object.hasOwnProperty.call(message, "subscriptions")) {
                    if (!Array.isArray(message.subscriptions))
                        return "subscriptions: array expected";
                    for (var i = 0; i < message.subscriptions.length; ++i) {
                        var error = $root.proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo.verify(message.subscriptions[i], q + 1);
                        if (error)
                            return "subscriptions." + error;
                    }
                }
                if (message.paidFeature != null && Object.hasOwnProperty.call(message, "paidFeature")) {
                    if (!Array.isArray(message.paidFeature))
                        return "paidFeature: array expected";
                    for (var i = 0; i < message.paidFeature.length; ++i) {
                        var error = $root.proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature.verify(message.paidFeature[i], q + 1);
                        if (error)
                            return "paidFeature." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a SubscriptionsSyncV2Action message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.SubscriptionsSyncV2Action} SubscriptionsSyncV2Action
             */
            SubscriptionsSyncV2Action.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.SubscriptionsSyncV2Action: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.subscriptions) {
                    if (!Array.isArray(object.subscriptions))
                        throw TypeError(".proto.SyncActionValue.SubscriptionsSyncV2Action.subscriptions: array expected");
                    message.subscriptions = Array(object.subscriptions.length);
                    for (var i = 0; i < object.subscriptions.length; ++i) {
                        if (!$util.isObject(object.subscriptions[i]))
                            throw TypeError(".proto.SyncActionValue.SubscriptionsSyncV2Action.subscriptions: object expected");
                        message.subscriptions[i] = $root.proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo.fromObject(object.subscriptions[i], q + 1);
                    }
                }
                if (object.paidFeature) {
                    if (!Array.isArray(object.paidFeature))
                        throw TypeError(".proto.SyncActionValue.SubscriptionsSyncV2Action.paidFeature: array expected");
                    message.paidFeature = Array(object.paidFeature.length);
                    for (var i = 0; i < object.paidFeature.length; ++i) {
                        if (!$util.isObject(object.paidFeature[i]))
                            throw TypeError(".proto.SyncActionValue.SubscriptionsSyncV2Action.paidFeature: object expected");
                        message.paidFeature[i] = $root.proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature.fromObject(object.paidFeature[i], q + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a SubscriptionsSyncV2Action message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action
             * @static
             * @param {proto.SyncActionValue.SubscriptionsSyncV2Action} message SubscriptionsSyncV2Action
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SubscriptionsSyncV2Action.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults) {
                    object.subscriptions = [];
                    object.paidFeature = [];
                }
                if (message.subscriptions && message.subscriptions.length) {
                    object.subscriptions = Array(message.subscriptions.length);
                    for (var j = 0; j < message.subscriptions.length; ++j)
                        object.subscriptions[j] = $root.proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo.toObject(message.subscriptions[j], options, q + 1);
                }
                if (message.paidFeature && message.paidFeature.length) {
                    object.paidFeature = Array(message.paidFeature.length);
                    for (var j = 0; j < message.paidFeature.length; ++j)
                        object.paidFeature[j] = $root.proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature.toObject(message.paidFeature[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this SubscriptionsSyncV2Action to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SubscriptionsSyncV2Action.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for SubscriptionsSyncV2Action
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            SubscriptionsSyncV2Action.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.SubscriptionsSyncV2Action";
            };

            SubscriptionsSyncV2Action.PaidFeature = (function() {

                /**
                 * Properties of a PaidFeature.
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action
                 * @interface IPaidFeature
                 * @property {string|null} [name] PaidFeature name
                 * @property {boolean|null} [enabled] PaidFeature enabled
                 * @property {number|null} [limit] PaidFeature limit
                 * @property {number|Long|null} [expirationTime] PaidFeature expirationTime
                 */

                /**
                 * Constructs a new PaidFeature.
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action
                 * @classdesc Represents a PaidFeature.
                 * @implements IPaidFeature
                 * @constructor
                 * @param {proto.SyncActionValue.SubscriptionsSyncV2Action.IPaidFeature=} [properties] Properties to set
                 */
                function PaidFeature(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * PaidFeature name.
                 * @member {string|null|undefined} name
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature
                 * @instance
                 */
                PaidFeature.prototype.name = null;

                /**
                 * PaidFeature enabled.
                 * @member {boolean|null|undefined} enabled
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature
                 * @instance
                 */
                PaidFeature.prototype.enabled = null;

                /**
                 * PaidFeature limit.
                 * @member {number|null|undefined} limit
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature
                 * @instance
                 */
                PaidFeature.prototype.limit = null;

                /**
                 * PaidFeature expirationTime.
                 * @member {number|Long|null|undefined} expirationTime
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature
                 * @instance
                 */
                PaidFeature.prototype.expirationTime = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PaidFeature.prototype, "_name", {
                    get: $util.oneOfGetter($oneOfFields = ["name"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PaidFeature.prototype, "_enabled", {
                    get: $util.oneOfGetter($oneOfFields = ["enabled"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PaidFeature.prototype, "_limit", {
                    get: $util.oneOfGetter($oneOfFields = ["limit"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PaidFeature.prototype, "_expirationTime", {
                    get: $util.oneOfGetter($oneOfFields = ["expirationTime"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new PaidFeature instance using the specified properties.
                 * @function create
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature
                 * @static
                 * @param {proto.SyncActionValue.SubscriptionsSyncV2Action.IPaidFeature=} [properties] Properties to set
                 * @returns {proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature} PaidFeature instance
                 */
                PaidFeature.create = function create(properties) {
                    return new PaidFeature(properties);
                };

                /**
                 * Encodes the specified PaidFeature message. Does not implicitly {@link proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature.verify|verify} messages.
                 * @function encode
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature
                 * @static
                 * @param {proto.SyncActionValue.SubscriptionsSyncV2Action.IPaidFeature} message PaidFeature message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PaidFeature.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                    if (message.enabled != null && Object.hasOwnProperty.call(message, "enabled"))
                        writer.uint32(/* id 2, wireType 0 =*/16).bool(message.enabled);
                    if (message.limit != null && Object.hasOwnProperty.call(message, "limit"))
                        writer.uint32(/* id 3, wireType 0 =*/24).int32(message.limit);
                    if (message.expirationTime != null && Object.hasOwnProperty.call(message, "expirationTime"))
                        writer.uint32(/* id 4, wireType 0 =*/32).int64(message.expirationTime);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified PaidFeature message, length delimited. Does not implicitly {@link proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature
                 * @static
                 * @param {proto.SyncActionValue.SubscriptionsSyncV2Action.IPaidFeature} message PaidFeature message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                PaidFeature.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a PaidFeature message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature} PaidFeature
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PaidFeature.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.name = reader.stringVerify();
                                message._name = "name";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                message.enabled = reader.bool();
                                message._enabled = "enabled";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                message.limit = reader.int32();
                                message._limit = "limit";
                                continue;
                            }
                        case 4: {
                                if (u !== 0)
                                    break;
                                message.expirationTime = reader.int64();
                                message._expirationTime = "expirationTime";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a PaidFeature message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature} PaidFeature
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                PaidFeature.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a PaidFeature message.
                 * @function verify
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                PaidFeature.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.name != null && Object.hasOwnProperty.call(message, "name")) {
                        properties._name = 1;
                        if (!$util.isString(message.name))
                            return "name: string expected";
                    }
                    if (message.enabled != null && Object.hasOwnProperty.call(message, "enabled")) {
                        properties._enabled = 1;
                        if (typeof message.enabled !== "boolean")
                            return "enabled: boolean expected";
                    }
                    if (message.limit != null && Object.hasOwnProperty.call(message, "limit")) {
                        properties._limit = 1;
                        if (!$util.isInteger(message.limit))
                            return "limit: integer expected";
                    }
                    if (message.expirationTime != null && Object.hasOwnProperty.call(message, "expirationTime")) {
                        properties._expirationTime = 1;
                        if (!$util.isInteger(message.expirationTime) && !(message.expirationTime && $util.isInteger(message.expirationTime.low) && $util.isInteger(message.expirationTime.high)))
                            return "expirationTime: integer|Long expected";
                    }
                    return null;
                };

                /**
                 * Creates a PaidFeature message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature} PaidFeature
                 */
                PaidFeature.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.name != null)
                        message.name = String(object.name);
                    if (object.enabled != null)
                        message.enabled = Boolean(object.enabled);
                    if (object.limit != null)
                        message.limit = object.limit | 0;
                    if (object.expirationTime != null)
                        if ($util.Long)
                            message.expirationTime = $util.Long.fromValue(object.expirationTime, false);
                        else if (typeof object.expirationTime === "string")
                            message.expirationTime = parseInt(object.expirationTime, 10);
                        else if (typeof object.expirationTime === "number")
                            message.expirationTime = object.expirationTime;
                        else if (typeof object.expirationTime === "object")
                            message.expirationTime = new $util.LongBits(object.expirationTime.low >>> 0, object.expirationTime.high >>> 0).toNumber();
                    return message;
                };

                /**
                 * Creates a plain object from a PaidFeature message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature
                 * @static
                 * @param {proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature} message PaidFeature
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                PaidFeature.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                        object.name = message.name;
                    if (message.enabled != null && Object.hasOwnProperty.call(message, "enabled"))
                        object.enabled = message.enabled;
                    if (message.limit != null && Object.hasOwnProperty.call(message, "limit"))
                        object.limit = message.limit;
                    if (message.expirationTime != null && Object.hasOwnProperty.call(message, "expirationTime"))
                        if (typeof BigInt !== "undefined" && options.longs === BigInt)
                            object.expirationTime = typeof message.expirationTime === "number" ? BigInt(message.expirationTime) : $util.Long.fromBits(message.expirationTime.low >>> 0, message.expirationTime.high >>> 0, false).toBigInt();
                        else if (typeof message.expirationTime === "number")
                            object.expirationTime = options.longs === String ? String(message.expirationTime) : message.expirationTime;
                        else
                            object.expirationTime = options.longs === String ? $util.Long.prototype.toString.call(message.expirationTime) : options.longs === Number ? new $util.LongBits(message.expirationTime.low >>> 0, message.expirationTime.high >>> 0).toNumber() : message.expirationTime;
                    return object;
                };

                /**
                 * Converts this PaidFeature to JSON.
                 * @function toJSON
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                PaidFeature.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for PaidFeature
                 * @function getTypeUrl
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                PaidFeature.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature";
                };

                return PaidFeature;
            })();

            SubscriptionsSyncV2Action.SubscriptionInfo = (function() {

                /**
                 * Properties of a SubscriptionInfo.
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action
                 * @interface ISubscriptionInfo
                 * @property {string|null} [id] SubscriptionInfo id
                 * @property {number|null} [tier] SubscriptionInfo tier
                 * @property {string|null} [status] SubscriptionInfo status
                 * @property {number|Long|null} [startTime] SubscriptionInfo startTime
                 * @property {number|Long|null} [endTime] SubscriptionInfo endTime
                 * @property {boolean|null} [isPlatformChanged] SubscriptionInfo isPlatformChanged
                 * @property {string|null} [source] SubscriptionInfo source
                 * @property {number|Long|null} [creationTime] SubscriptionInfo creationTime
                 */

                /**
                 * Constructs a new SubscriptionInfo.
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action
                 * @classdesc Represents a SubscriptionInfo.
                 * @implements ISubscriptionInfo
                 * @constructor
                 * @param {proto.SyncActionValue.SubscriptionsSyncV2Action.ISubscriptionInfo=} [properties] Properties to set
                 */
                function SubscriptionInfo(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * SubscriptionInfo id.
                 * @member {string|null|undefined} id
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo
                 * @instance
                 */
                SubscriptionInfo.prototype.id = null;

                /**
                 * SubscriptionInfo tier.
                 * @member {number|null|undefined} tier
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo
                 * @instance
                 */
                SubscriptionInfo.prototype.tier = null;

                /**
                 * SubscriptionInfo status.
                 * @member {string|null|undefined} status
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo
                 * @instance
                 */
                SubscriptionInfo.prototype.status = null;

                /**
                 * SubscriptionInfo startTime.
                 * @member {number|Long|null|undefined} startTime
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo
                 * @instance
                 */
                SubscriptionInfo.prototype.startTime = null;

                /**
                 * SubscriptionInfo endTime.
                 * @member {number|Long|null|undefined} endTime
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo
                 * @instance
                 */
                SubscriptionInfo.prototype.endTime = null;

                /**
                 * SubscriptionInfo isPlatformChanged.
                 * @member {boolean|null|undefined} isPlatformChanged
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo
                 * @instance
                 */
                SubscriptionInfo.prototype.isPlatformChanged = null;

                /**
                 * SubscriptionInfo source.
                 * @member {string|null|undefined} source
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo
                 * @instance
                 */
                SubscriptionInfo.prototype.source = null;

                /**
                 * SubscriptionInfo creationTime.
                 * @member {number|Long|null|undefined} creationTime
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo
                 * @instance
                 */
                SubscriptionInfo.prototype.creationTime = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SubscriptionInfo.prototype, "_id", {
                    get: $util.oneOfGetter($oneOfFields = ["id"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SubscriptionInfo.prototype, "_tier", {
                    get: $util.oneOfGetter($oneOfFields = ["tier"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SubscriptionInfo.prototype, "_status", {
                    get: $util.oneOfGetter($oneOfFields = ["status"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SubscriptionInfo.prototype, "_startTime", {
                    get: $util.oneOfGetter($oneOfFields = ["startTime"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SubscriptionInfo.prototype, "_endTime", {
                    get: $util.oneOfGetter($oneOfFields = ["endTime"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SubscriptionInfo.prototype, "_isPlatformChanged", {
                    get: $util.oneOfGetter($oneOfFields = ["isPlatformChanged"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SubscriptionInfo.prototype, "_source", {
                    get: $util.oneOfGetter($oneOfFields = ["source"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SubscriptionInfo.prototype, "_creationTime", {
                    get: $util.oneOfGetter($oneOfFields = ["creationTime"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new SubscriptionInfo instance using the specified properties.
                 * @function create
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo
                 * @static
                 * @param {proto.SyncActionValue.SubscriptionsSyncV2Action.ISubscriptionInfo=} [properties] Properties to set
                 * @returns {proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo} SubscriptionInfo instance
                 */
                SubscriptionInfo.create = function create(properties) {
                    return new SubscriptionInfo(properties);
                };

                /**
                 * Encodes the specified SubscriptionInfo message. Does not implicitly {@link proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo.verify|verify} messages.
                 * @function encode
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo
                 * @static
                 * @param {proto.SyncActionValue.SubscriptionsSyncV2Action.ISubscriptionInfo} message SubscriptionInfo message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                SubscriptionInfo.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
                    if (message.tier != null && Object.hasOwnProperty.call(message, "tier"))
                        writer.uint32(/* id 2, wireType 0 =*/16).int32(message.tier);
                    if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.status);
                    if (message.startTime != null && Object.hasOwnProperty.call(message, "startTime"))
                        writer.uint32(/* id 4, wireType 0 =*/32).int64(message.startTime);
                    if (message.endTime != null && Object.hasOwnProperty.call(message, "endTime"))
                        writer.uint32(/* id 5, wireType 0 =*/40).int64(message.endTime);
                    if (message.isPlatformChanged != null && Object.hasOwnProperty.call(message, "isPlatformChanged"))
                        writer.uint32(/* id 6, wireType 0 =*/48).bool(message.isPlatformChanged);
                    if (message.source != null && Object.hasOwnProperty.call(message, "source"))
                        writer.uint32(/* id 7, wireType 2 =*/58).string(message.source);
                    if (message.creationTime != null && Object.hasOwnProperty.call(message, "creationTime"))
                        writer.uint32(/* id 8, wireType 0 =*/64).int64(message.creationTime);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified SubscriptionInfo message, length delimited. Does not implicitly {@link proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo
                 * @static
                 * @param {proto.SyncActionValue.SubscriptionsSyncV2Action.ISubscriptionInfo} message SubscriptionInfo message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                SubscriptionInfo.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a SubscriptionInfo message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo} SubscriptionInfo
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                SubscriptionInfo.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.id = reader.stringVerify();
                                message._id = "id";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                message.tier = reader.int32();
                                message._tier = "tier";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                message.status = reader.stringVerify();
                                message._status = "status";
                                continue;
                            }
                        case 4: {
                                if (u !== 0)
                                    break;
                                message.startTime = reader.int64();
                                message._startTime = "startTime";
                                continue;
                            }
                        case 5: {
                                if (u !== 0)
                                    break;
                                message.endTime = reader.int64();
                                message._endTime = "endTime";
                                continue;
                            }
                        case 6: {
                                if (u !== 0)
                                    break;
                                message.isPlatformChanged = reader.bool();
                                message._isPlatformChanged = "isPlatformChanged";
                                continue;
                            }
                        case 7: {
                                if (u !== 2)
                                    break;
                                message.source = reader.stringVerify();
                                message._source = "source";
                                continue;
                            }
                        case 8: {
                                if (u !== 0)
                                    break;
                                message.creationTime = reader.int64();
                                message._creationTime = "creationTime";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a SubscriptionInfo message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo} SubscriptionInfo
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                SubscriptionInfo.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a SubscriptionInfo message.
                 * @function verify
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                SubscriptionInfo.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.id != null && Object.hasOwnProperty.call(message, "id")) {
                        properties._id = 1;
                        if (!$util.isString(message.id))
                            return "id: string expected";
                    }
                    if (message.tier != null && Object.hasOwnProperty.call(message, "tier")) {
                        properties._tier = 1;
                        if (!$util.isInteger(message.tier))
                            return "tier: integer expected";
                    }
                    if (message.status != null && Object.hasOwnProperty.call(message, "status")) {
                        properties._status = 1;
                        if (!$util.isString(message.status))
                            return "status: string expected";
                    }
                    if (message.startTime != null && Object.hasOwnProperty.call(message, "startTime")) {
                        properties._startTime = 1;
                        if (!$util.isInteger(message.startTime) && !(message.startTime && $util.isInteger(message.startTime.low) && $util.isInteger(message.startTime.high)))
                            return "startTime: integer|Long expected";
                    }
                    if (message.endTime != null && Object.hasOwnProperty.call(message, "endTime")) {
                        properties._endTime = 1;
                        if (!$util.isInteger(message.endTime) && !(message.endTime && $util.isInteger(message.endTime.low) && $util.isInteger(message.endTime.high)))
                            return "endTime: integer|Long expected";
                    }
                    if (message.isPlatformChanged != null && Object.hasOwnProperty.call(message, "isPlatformChanged")) {
                        properties._isPlatformChanged = 1;
                        if (typeof message.isPlatformChanged !== "boolean")
                            return "isPlatformChanged: boolean expected";
                    }
                    if (message.source != null && Object.hasOwnProperty.call(message, "source")) {
                        properties._source = 1;
                        if (!$util.isString(message.source))
                            return "source: string expected";
                    }
                    if (message.creationTime != null && Object.hasOwnProperty.call(message, "creationTime")) {
                        properties._creationTime = 1;
                        if (!$util.isInteger(message.creationTime) && !(message.creationTime && $util.isInteger(message.creationTime.low) && $util.isInteger(message.creationTime.high)))
                            return "creationTime: integer|Long expected";
                    }
                    return null;
                };

                /**
                 * Creates a SubscriptionInfo message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo} SubscriptionInfo
                 */
                SubscriptionInfo.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.id != null)
                        message.id = String(object.id);
                    if (object.tier != null)
                        message.tier = object.tier | 0;
                    if (object.status != null)
                        message.status = String(object.status);
                    if (object.startTime != null)
                        if ($util.Long)
                            message.startTime = $util.Long.fromValue(object.startTime, false);
                        else if (typeof object.startTime === "string")
                            message.startTime = parseInt(object.startTime, 10);
                        else if (typeof object.startTime === "number")
                            message.startTime = object.startTime;
                        else if (typeof object.startTime === "object")
                            message.startTime = new $util.LongBits(object.startTime.low >>> 0, object.startTime.high >>> 0).toNumber();
                    if (object.endTime != null)
                        if ($util.Long)
                            message.endTime = $util.Long.fromValue(object.endTime, false);
                        else if (typeof object.endTime === "string")
                            message.endTime = parseInt(object.endTime, 10);
                        else if (typeof object.endTime === "number")
                            message.endTime = object.endTime;
                        else if (typeof object.endTime === "object")
                            message.endTime = new $util.LongBits(object.endTime.low >>> 0, object.endTime.high >>> 0).toNumber();
                    if (object.isPlatformChanged != null)
                        message.isPlatformChanged = Boolean(object.isPlatformChanged);
                    if (object.source != null)
                        message.source = String(object.source);
                    if (object.creationTime != null)
                        if ($util.Long)
                            message.creationTime = $util.Long.fromValue(object.creationTime, false);
                        else if (typeof object.creationTime === "string")
                            message.creationTime = parseInt(object.creationTime, 10);
                        else if (typeof object.creationTime === "number")
                            message.creationTime = object.creationTime;
                        else if (typeof object.creationTime === "object")
                            message.creationTime = new $util.LongBits(object.creationTime.low >>> 0, object.creationTime.high >>> 0).toNumber();
                    return message;
                };

                /**
                 * Creates a plain object from a SubscriptionInfo message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo
                 * @static
                 * @param {proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo} message SubscriptionInfo
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                SubscriptionInfo.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                        object.id = message.id;
                    if (message.tier != null && Object.hasOwnProperty.call(message, "tier"))
                        object.tier = message.tier;
                    if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                        object.status = message.status;
                    if (message.startTime != null && Object.hasOwnProperty.call(message, "startTime"))
                        if (typeof BigInt !== "undefined" && options.longs === BigInt)
                            object.startTime = typeof message.startTime === "number" ? BigInt(message.startTime) : $util.Long.fromBits(message.startTime.low >>> 0, message.startTime.high >>> 0, false).toBigInt();
                        else if (typeof message.startTime === "number")
                            object.startTime = options.longs === String ? String(message.startTime) : message.startTime;
                        else
                            object.startTime = options.longs === String ? $util.Long.prototype.toString.call(message.startTime) : options.longs === Number ? new $util.LongBits(message.startTime.low >>> 0, message.startTime.high >>> 0).toNumber() : message.startTime;
                    if (message.endTime != null && Object.hasOwnProperty.call(message, "endTime"))
                        if (typeof BigInt !== "undefined" && options.longs === BigInt)
                            object.endTime = typeof message.endTime === "number" ? BigInt(message.endTime) : $util.Long.fromBits(message.endTime.low >>> 0, message.endTime.high >>> 0, false).toBigInt();
                        else if (typeof message.endTime === "number")
                            object.endTime = options.longs === String ? String(message.endTime) : message.endTime;
                        else
                            object.endTime = options.longs === String ? $util.Long.prototype.toString.call(message.endTime) : options.longs === Number ? new $util.LongBits(message.endTime.low >>> 0, message.endTime.high >>> 0).toNumber() : message.endTime;
                    if (message.isPlatformChanged != null && Object.hasOwnProperty.call(message, "isPlatformChanged"))
                        object.isPlatformChanged = message.isPlatformChanged;
                    if (message.source != null && Object.hasOwnProperty.call(message, "source"))
                        object.source = message.source;
                    if (message.creationTime != null && Object.hasOwnProperty.call(message, "creationTime"))
                        if (typeof BigInt !== "undefined" && options.longs === BigInt)
                            object.creationTime = typeof message.creationTime === "number" ? BigInt(message.creationTime) : $util.Long.fromBits(message.creationTime.low >>> 0, message.creationTime.high >>> 0, false).toBigInt();
                        else if (typeof message.creationTime === "number")
                            object.creationTime = options.longs === String ? String(message.creationTime) : message.creationTime;
                        else
                            object.creationTime = options.longs === String ? $util.Long.prototype.toString.call(message.creationTime) : options.longs === Number ? new $util.LongBits(message.creationTime.low >>> 0, message.creationTime.high >>> 0).toNumber() : message.creationTime;
                    return object;
                };

                /**
                 * Converts this SubscriptionInfo to JSON.
                 * @function toJSON
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                SubscriptionInfo.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for SubscriptionInfo
                 * @function getTypeUrl
                 * @memberof proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                SubscriptionInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo";
                };

                return SubscriptionInfo;
            })();

            return SubscriptionsSyncV2Action;
        })();

        SyncActionValue.SyncActionMessage = (function() {

            /**
             * Properties of a SyncActionMessage.
             * @memberof proto.SyncActionValue
             * @interface ISyncActionMessage
             * @property {proto.IMessageKey|null} [key] SyncActionMessage key
             * @property {number|Long|null} [timestamp] SyncActionMessage timestamp
             */

            /**
             * Constructs a new SyncActionMessage.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a SyncActionMessage.
             * @implements ISyncActionMessage
             * @constructor
             * @param {proto.SyncActionValue.ISyncActionMessage=} [properties] Properties to set
             */
            function SyncActionMessage(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * SyncActionMessage key.
             * @member {proto.IMessageKey|null|undefined} key
             * @memberof proto.SyncActionValue.SyncActionMessage
             * @instance
             */
            SyncActionMessage.prototype.key = null;

            /**
             * SyncActionMessage timestamp.
             * @member {number|Long|null|undefined} timestamp
             * @memberof proto.SyncActionValue.SyncActionMessage
             * @instance
             */
            SyncActionMessage.prototype.timestamp = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SyncActionMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SyncActionMessage.prototype, "_timestamp", {
                get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new SyncActionMessage instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.SyncActionMessage
             * @static
             * @param {proto.SyncActionValue.ISyncActionMessage=} [properties] Properties to set
             * @returns {proto.SyncActionValue.SyncActionMessage} SyncActionMessage instance
             */
            SyncActionMessage.create = function create(properties) {
                return new SyncActionMessage(properties);
            };

            /**
             * Encodes the specified SyncActionMessage message. Does not implicitly {@link proto.SyncActionValue.SyncActionMessage.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.SyncActionMessage
             * @static
             * @param {proto.SyncActionValue.ISyncActionMessage} message SyncActionMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SyncActionMessage.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    $root.proto.MessageKey.encode(message.key, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.timestamp);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SyncActionMessage message, length delimited. Does not implicitly {@link proto.SyncActionValue.SyncActionMessage.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.SyncActionMessage
             * @static
             * @param {proto.SyncActionValue.ISyncActionMessage} message SyncActionMessage message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SyncActionMessage.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a SyncActionMessage message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.SyncActionMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.SyncActionMessage} SyncActionMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SyncActionMessage.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.key = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.key);
                            message._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.timestamp = reader.int64();
                            message._timestamp = "timestamp";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a SyncActionMessage message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.SyncActionMessage
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.SyncActionMessage} SyncActionMessage
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SyncActionMessage.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SyncActionMessage message.
             * @function verify
             * @memberof proto.SyncActionValue.SyncActionMessage
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SyncActionMessage.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                    properties._key = 1;
                    {
                        var error = $root.proto.MessageKey.verify(message.key, q + 1);
                        if (error)
                            return "key." + error;
                    }
                }
                if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp")) {
                    properties._timestamp = 1;
                    if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                        return "timestamp: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a SyncActionMessage message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.SyncActionMessage
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.SyncActionMessage} SyncActionMessage
             */
            SyncActionMessage.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.SyncActionMessage: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.key != null) {
                    if (!$util.isObject(object.key))
                        throw TypeError(".proto.SyncActionValue.SyncActionMessage.key: object expected");
                    message.key = $root.proto.MessageKey.fromObject(object.key, q + 1);
                }
                if (object.timestamp != null)
                    if ($util.Long)
                        message.timestamp = $util.Long.fromValue(object.timestamp, false);
                    else if (typeof object.timestamp === "string")
                        message.timestamp = parseInt(object.timestamp, 10);
                    else if (typeof object.timestamp === "number")
                        message.timestamp = object.timestamp;
                    else if (typeof object.timestamp === "object")
                        message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a SyncActionMessage message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.SyncActionMessage
             * @static
             * @param {proto.SyncActionValue.SyncActionMessage} message SyncActionMessage
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SyncActionMessage.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    object.key = $root.proto.MessageKey.toObject(message.key, options, q + 1);
                if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.timestamp = typeof message.timestamp === "number" ? BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.timestamp === "number")
                        object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                    else
                        object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
                return object;
            };

            /**
             * Converts this SyncActionMessage to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.SyncActionMessage
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SyncActionMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for SyncActionMessage
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.SyncActionMessage
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            SyncActionMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.SyncActionMessage";
            };

            return SyncActionMessage;
        })();

        SyncActionValue.SyncActionMessageRange = (function() {

            /**
             * Properties of a SyncActionMessageRange.
             * @memberof proto.SyncActionValue
             * @interface ISyncActionMessageRange
             * @property {number|Long|null} [lastMessageTimestamp] SyncActionMessageRange lastMessageTimestamp
             * @property {number|Long|null} [lastSystemMessageTimestamp] SyncActionMessageRange lastSystemMessageTimestamp
             * @property {Array.<proto.SyncActionValue.ISyncActionMessage>|null} [messages] SyncActionMessageRange messages
             */

            /**
             * Constructs a new SyncActionMessageRange.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a SyncActionMessageRange.
             * @implements ISyncActionMessageRange
             * @constructor
             * @param {proto.SyncActionValue.ISyncActionMessageRange=} [properties] Properties to set
             */
            function SyncActionMessageRange(properties) {
                this.messages = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * SyncActionMessageRange lastMessageTimestamp.
             * @member {number|Long|null|undefined} lastMessageTimestamp
             * @memberof proto.SyncActionValue.SyncActionMessageRange
             * @instance
             */
            SyncActionMessageRange.prototype.lastMessageTimestamp = null;

            /**
             * SyncActionMessageRange lastSystemMessageTimestamp.
             * @member {number|Long|null|undefined} lastSystemMessageTimestamp
             * @memberof proto.SyncActionValue.SyncActionMessageRange
             * @instance
             */
            SyncActionMessageRange.prototype.lastSystemMessageTimestamp = null;

            /**
             * SyncActionMessageRange messages.
             * @member {Array.<proto.SyncActionValue.ISyncActionMessage>} messages
             * @memberof proto.SyncActionValue.SyncActionMessageRange
             * @instance
             */
            SyncActionMessageRange.prototype.messages = $util.emptyArray;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SyncActionMessageRange.prototype, "_lastMessageTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["lastMessageTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SyncActionMessageRange.prototype, "_lastSystemMessageTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["lastSystemMessageTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new SyncActionMessageRange instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.SyncActionMessageRange
             * @static
             * @param {proto.SyncActionValue.ISyncActionMessageRange=} [properties] Properties to set
             * @returns {proto.SyncActionValue.SyncActionMessageRange} SyncActionMessageRange instance
             */
            SyncActionMessageRange.create = function create(properties) {
                return new SyncActionMessageRange(properties);
            };

            /**
             * Encodes the specified SyncActionMessageRange message. Does not implicitly {@link proto.SyncActionValue.SyncActionMessageRange.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.SyncActionMessageRange
             * @static
             * @param {proto.SyncActionValue.ISyncActionMessageRange} message SyncActionMessageRange message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SyncActionMessageRange.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.lastMessageTimestamp != null && Object.hasOwnProperty.call(message, "lastMessageTimestamp"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int64(message.lastMessageTimestamp);
                if (message.lastSystemMessageTimestamp != null && Object.hasOwnProperty.call(message, "lastSystemMessageTimestamp"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.lastSystemMessageTimestamp);
                if (message.messages != null && message.messages.length)
                    for (var i = 0; i < message.messages.length; ++i)
                        $root.proto.SyncActionValue.SyncActionMessage.encode(message.messages[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified SyncActionMessageRange message, length delimited. Does not implicitly {@link proto.SyncActionValue.SyncActionMessageRange.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.SyncActionMessageRange
             * @static
             * @param {proto.SyncActionValue.ISyncActionMessageRange} message SyncActionMessageRange message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            SyncActionMessageRange.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a SyncActionMessageRange message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.SyncActionMessageRange
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.SyncActionMessageRange} SyncActionMessageRange
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SyncActionMessageRange.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.lastMessageTimestamp = reader.int64();
                            message._lastMessageTimestamp = "lastMessageTimestamp";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            message.lastSystemMessageTimestamp = reader.int64();
                            message._lastSystemMessageTimestamp = "lastSystemMessageTimestamp";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            if (!(message.messages && message.messages.length))
                                message.messages = [];
                            message.messages.push($root.proto.SyncActionValue.SyncActionMessage.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a SyncActionMessageRange message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.SyncActionMessageRange
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.SyncActionMessageRange} SyncActionMessageRange
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            SyncActionMessageRange.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a SyncActionMessageRange message.
             * @function verify
             * @memberof proto.SyncActionValue.SyncActionMessageRange
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            SyncActionMessageRange.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.lastMessageTimestamp != null && Object.hasOwnProperty.call(message, "lastMessageTimestamp")) {
                    properties._lastMessageTimestamp = 1;
                    if (!$util.isInteger(message.lastMessageTimestamp) && !(message.lastMessageTimestamp && $util.isInteger(message.lastMessageTimestamp.low) && $util.isInteger(message.lastMessageTimestamp.high)))
                        return "lastMessageTimestamp: integer|Long expected";
                }
                if (message.lastSystemMessageTimestamp != null && Object.hasOwnProperty.call(message, "lastSystemMessageTimestamp")) {
                    properties._lastSystemMessageTimestamp = 1;
                    if (!$util.isInteger(message.lastSystemMessageTimestamp) && !(message.lastSystemMessageTimestamp && $util.isInteger(message.lastSystemMessageTimestamp.low) && $util.isInteger(message.lastSystemMessageTimestamp.high)))
                        return "lastSystemMessageTimestamp: integer|Long expected";
                }
                if (message.messages != null && Object.hasOwnProperty.call(message, "messages")) {
                    if (!Array.isArray(message.messages))
                        return "messages: array expected";
                    for (var i = 0; i < message.messages.length; ++i) {
                        var error = $root.proto.SyncActionValue.SyncActionMessage.verify(message.messages[i], q + 1);
                        if (error)
                            return "messages." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a SyncActionMessageRange message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.SyncActionMessageRange
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.SyncActionMessageRange} SyncActionMessageRange
             */
            SyncActionMessageRange.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.SyncActionMessageRange: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.lastMessageTimestamp != null)
                    if ($util.Long)
                        message.lastMessageTimestamp = $util.Long.fromValue(object.lastMessageTimestamp, false);
                    else if (typeof object.lastMessageTimestamp === "string")
                        message.lastMessageTimestamp = parseInt(object.lastMessageTimestamp, 10);
                    else if (typeof object.lastMessageTimestamp === "number")
                        message.lastMessageTimestamp = object.lastMessageTimestamp;
                    else if (typeof object.lastMessageTimestamp === "object")
                        message.lastMessageTimestamp = new $util.LongBits(object.lastMessageTimestamp.low >>> 0, object.lastMessageTimestamp.high >>> 0).toNumber();
                if (object.lastSystemMessageTimestamp != null)
                    if ($util.Long)
                        message.lastSystemMessageTimestamp = $util.Long.fromValue(object.lastSystemMessageTimestamp, false);
                    else if (typeof object.lastSystemMessageTimestamp === "string")
                        message.lastSystemMessageTimestamp = parseInt(object.lastSystemMessageTimestamp, 10);
                    else if (typeof object.lastSystemMessageTimestamp === "number")
                        message.lastSystemMessageTimestamp = object.lastSystemMessageTimestamp;
                    else if (typeof object.lastSystemMessageTimestamp === "object")
                        message.lastSystemMessageTimestamp = new $util.LongBits(object.lastSystemMessageTimestamp.low >>> 0, object.lastSystemMessageTimestamp.high >>> 0).toNumber();
                if (object.messages) {
                    if (!Array.isArray(object.messages))
                        throw TypeError(".proto.SyncActionValue.SyncActionMessageRange.messages: array expected");
                    message.messages = Array(object.messages.length);
                    for (var i = 0; i < object.messages.length; ++i) {
                        if (!$util.isObject(object.messages[i]))
                            throw TypeError(".proto.SyncActionValue.SyncActionMessageRange.messages: object expected");
                        message.messages[i] = $root.proto.SyncActionValue.SyncActionMessage.fromObject(object.messages[i], q + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a SyncActionMessageRange message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.SyncActionMessageRange
             * @static
             * @param {proto.SyncActionValue.SyncActionMessageRange} message SyncActionMessageRange
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            SyncActionMessageRange.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.messages = [];
                if (message.lastMessageTimestamp != null && Object.hasOwnProperty.call(message, "lastMessageTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.lastMessageTimestamp = typeof message.lastMessageTimestamp === "number" ? BigInt(message.lastMessageTimestamp) : $util.Long.fromBits(message.lastMessageTimestamp.low >>> 0, message.lastMessageTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.lastMessageTimestamp === "number")
                        object.lastMessageTimestamp = options.longs === String ? String(message.lastMessageTimestamp) : message.lastMessageTimestamp;
                    else
                        object.lastMessageTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.lastMessageTimestamp) : options.longs === Number ? new $util.LongBits(message.lastMessageTimestamp.low >>> 0, message.lastMessageTimestamp.high >>> 0).toNumber() : message.lastMessageTimestamp;
                if (message.lastSystemMessageTimestamp != null && Object.hasOwnProperty.call(message, "lastSystemMessageTimestamp"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.lastSystemMessageTimestamp = typeof message.lastSystemMessageTimestamp === "number" ? BigInt(message.lastSystemMessageTimestamp) : $util.Long.fromBits(message.lastSystemMessageTimestamp.low >>> 0, message.lastSystemMessageTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof message.lastSystemMessageTimestamp === "number")
                        object.lastSystemMessageTimestamp = options.longs === String ? String(message.lastSystemMessageTimestamp) : message.lastSystemMessageTimestamp;
                    else
                        object.lastSystemMessageTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.lastSystemMessageTimestamp) : options.longs === Number ? new $util.LongBits(message.lastSystemMessageTimestamp.low >>> 0, message.lastSystemMessageTimestamp.high >>> 0).toNumber() : message.lastSystemMessageTimestamp;
                if (message.messages && message.messages.length) {
                    object.messages = Array(message.messages.length);
                    for (var j = 0; j < message.messages.length; ++j)
                        object.messages[j] = $root.proto.SyncActionValue.SyncActionMessage.toObject(message.messages[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this SyncActionMessageRange to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.SyncActionMessageRange
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            SyncActionMessageRange.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for SyncActionMessageRange
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.SyncActionMessageRange
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            SyncActionMessageRange.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.SyncActionMessageRange";
            };

            return SyncActionMessageRange;
        })();

        SyncActionValue.ThreadPinAction = (function() {

            /**
             * Properties of a ThreadPinAction.
             * @memberof proto.SyncActionValue
             * @interface IThreadPinAction
             * @property {boolean|null} [pinned] ThreadPinAction pinned
             */

            /**
             * Constructs a new ThreadPinAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a ThreadPinAction.
             * @implements IThreadPinAction
             * @constructor
             * @param {proto.SyncActionValue.IThreadPinAction=} [properties] Properties to set
             */
            function ThreadPinAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ThreadPinAction pinned.
             * @member {boolean|null|undefined} pinned
             * @memberof proto.SyncActionValue.ThreadPinAction
             * @instance
             */
            ThreadPinAction.prototype.pinned = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ThreadPinAction.prototype, "_pinned", {
                get: $util.oneOfGetter($oneOfFields = ["pinned"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new ThreadPinAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.ThreadPinAction
             * @static
             * @param {proto.SyncActionValue.IThreadPinAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.ThreadPinAction} ThreadPinAction instance
             */
            ThreadPinAction.create = function create(properties) {
                return new ThreadPinAction(properties);
            };

            /**
             * Encodes the specified ThreadPinAction message. Does not implicitly {@link proto.SyncActionValue.ThreadPinAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.ThreadPinAction
             * @static
             * @param {proto.SyncActionValue.IThreadPinAction} message ThreadPinAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ThreadPinAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.pinned != null && Object.hasOwnProperty.call(message, "pinned"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.pinned);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified ThreadPinAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.ThreadPinAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.ThreadPinAction
             * @static
             * @param {proto.SyncActionValue.IThreadPinAction} message ThreadPinAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ThreadPinAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ThreadPinAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.ThreadPinAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.ThreadPinAction} ThreadPinAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ThreadPinAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.pinned = reader.bool();
                            message._pinned = "pinned";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a ThreadPinAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.ThreadPinAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.ThreadPinAction} ThreadPinAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ThreadPinAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ThreadPinAction message.
             * @function verify
             * @memberof proto.SyncActionValue.ThreadPinAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ThreadPinAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.pinned != null && Object.hasOwnProperty.call(message, "pinned")) {
                    properties._pinned = 1;
                    if (typeof message.pinned !== "boolean")
                        return "pinned: boolean expected";
                }
                return null;
            };

            /**
             * Creates a ThreadPinAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.ThreadPinAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.ThreadPinAction} ThreadPinAction
             */
            ThreadPinAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.ThreadPinAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.pinned != null)
                    message.pinned = Boolean(object.pinned);
                return message;
            };

            /**
             * Creates a plain object from a ThreadPinAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.ThreadPinAction
             * @static
             * @param {proto.SyncActionValue.ThreadPinAction} message ThreadPinAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ThreadPinAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.pinned != null && Object.hasOwnProperty.call(message, "pinned"))
                    object.pinned = message.pinned;
                return object;
            };

            /**
             * Converts this ThreadPinAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.ThreadPinAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ThreadPinAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ThreadPinAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.ThreadPinAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ThreadPinAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.ThreadPinAction";
            };

            return ThreadPinAction;
        })();

        SyncActionValue.TimeFormatAction = (function() {

            /**
             * Properties of a TimeFormatAction.
             * @memberof proto.SyncActionValue
             * @interface ITimeFormatAction
             * @property {boolean|null} [isTwentyFourHourFormatEnabled] TimeFormatAction isTwentyFourHourFormatEnabled
             */

            /**
             * Constructs a new TimeFormatAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a TimeFormatAction.
             * @implements ITimeFormatAction
             * @constructor
             * @param {proto.SyncActionValue.ITimeFormatAction=} [properties] Properties to set
             */
            function TimeFormatAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * TimeFormatAction isTwentyFourHourFormatEnabled.
             * @member {boolean|null|undefined} isTwentyFourHourFormatEnabled
             * @memberof proto.SyncActionValue.TimeFormatAction
             * @instance
             */
            TimeFormatAction.prototype.isTwentyFourHourFormatEnabled = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(TimeFormatAction.prototype, "_isTwentyFourHourFormatEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isTwentyFourHourFormatEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new TimeFormatAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.TimeFormatAction
             * @static
             * @param {proto.SyncActionValue.ITimeFormatAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.TimeFormatAction} TimeFormatAction instance
             */
            TimeFormatAction.create = function create(properties) {
                return new TimeFormatAction(properties);
            };

            /**
             * Encodes the specified TimeFormatAction message. Does not implicitly {@link proto.SyncActionValue.TimeFormatAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.TimeFormatAction
             * @static
             * @param {proto.SyncActionValue.ITimeFormatAction} message TimeFormatAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            TimeFormatAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.isTwentyFourHourFormatEnabled != null && Object.hasOwnProperty.call(message, "isTwentyFourHourFormatEnabled"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.isTwentyFourHourFormatEnabled);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified TimeFormatAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.TimeFormatAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.TimeFormatAction
             * @static
             * @param {proto.SyncActionValue.ITimeFormatAction} message TimeFormatAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            TimeFormatAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a TimeFormatAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.TimeFormatAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.TimeFormatAction} TimeFormatAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            TimeFormatAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.isTwentyFourHourFormatEnabled = reader.bool();
                            message._isTwentyFourHourFormatEnabled = "isTwentyFourHourFormatEnabled";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a TimeFormatAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.TimeFormatAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.TimeFormatAction} TimeFormatAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            TimeFormatAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a TimeFormatAction message.
             * @function verify
             * @memberof proto.SyncActionValue.TimeFormatAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            TimeFormatAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.isTwentyFourHourFormatEnabled != null && Object.hasOwnProperty.call(message, "isTwentyFourHourFormatEnabled")) {
                    properties._isTwentyFourHourFormatEnabled = 1;
                    if (typeof message.isTwentyFourHourFormatEnabled !== "boolean")
                        return "isTwentyFourHourFormatEnabled: boolean expected";
                }
                return null;
            };

            /**
             * Creates a TimeFormatAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.TimeFormatAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.TimeFormatAction} TimeFormatAction
             */
            TimeFormatAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.TimeFormatAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.isTwentyFourHourFormatEnabled != null)
                    message.isTwentyFourHourFormatEnabled = Boolean(object.isTwentyFourHourFormatEnabled);
                return message;
            };

            /**
             * Creates a plain object from a TimeFormatAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.TimeFormatAction
             * @static
             * @param {proto.SyncActionValue.TimeFormatAction} message TimeFormatAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            TimeFormatAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.isTwentyFourHourFormatEnabled != null && Object.hasOwnProperty.call(message, "isTwentyFourHourFormatEnabled"))
                    object.isTwentyFourHourFormatEnabled = message.isTwentyFourHourFormatEnabled;
                return object;
            };

            /**
             * Converts this TimeFormatAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.TimeFormatAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            TimeFormatAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for TimeFormatAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.TimeFormatAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            TimeFormatAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.TimeFormatAction";
            };

            return TimeFormatAction;
        })();

        SyncActionValue.UGCBot = (function() {

            /**
             * Properties of a UGCBot.
             * @memberof proto.SyncActionValue
             * @interface IUGCBot
             * @property {Uint8Array|null} [definition] UGCBot definition
             */

            /**
             * Constructs a new UGCBot.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a UGCBot.
             * @implements IUGCBot
             * @constructor
             * @param {proto.SyncActionValue.IUGCBot=} [properties] Properties to set
             */
            function UGCBot(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * UGCBot definition.
             * @member {Uint8Array|null|undefined} definition
             * @memberof proto.SyncActionValue.UGCBot
             * @instance
             */
            UGCBot.prototype.definition = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UGCBot.prototype, "_definition", {
                get: $util.oneOfGetter($oneOfFields = ["definition"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new UGCBot instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.UGCBot
             * @static
             * @param {proto.SyncActionValue.IUGCBot=} [properties] Properties to set
             * @returns {proto.SyncActionValue.UGCBot} UGCBot instance
             */
            UGCBot.create = function create(properties) {
                return new UGCBot(properties);
            };

            /**
             * Encodes the specified UGCBot message. Does not implicitly {@link proto.SyncActionValue.UGCBot.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.UGCBot
             * @static
             * @param {proto.SyncActionValue.IUGCBot} message UGCBot message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UGCBot.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.definition != null && Object.hasOwnProperty.call(message, "definition"))
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.definition);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified UGCBot message, length delimited. Does not implicitly {@link proto.SyncActionValue.UGCBot.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.UGCBot
             * @static
             * @param {proto.SyncActionValue.IUGCBot} message UGCBot message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UGCBot.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a UGCBot message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.UGCBot
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.UGCBot} UGCBot
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UGCBot.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.definition = reader.bytes();
                            message._definition = "definition";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a UGCBot message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.UGCBot
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.UGCBot} UGCBot
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UGCBot.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a UGCBot message.
             * @function verify
             * @memberof proto.SyncActionValue.UGCBot
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            UGCBot.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.definition != null && Object.hasOwnProperty.call(message, "definition")) {
                    properties._definition = 1;
                    if (!(message.definition && typeof message.definition.length === "number" || $util.isString(message.definition)))
                        return "definition: buffer expected";
                }
                return null;
            };

            /**
             * Creates a UGCBot message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.UGCBot
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.UGCBot} UGCBot
             */
            UGCBot.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.UGCBot: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.definition != null)
                    if (typeof object.definition === "string")
                        $util.base64.decode(object.definition, message.definition = $util.newBuffer($util.base64.length(object.definition)), 0);
                    else if (object.definition.length >= 0)
                        message.definition = object.definition;
                return message;
            };

            /**
             * Creates a plain object from a UGCBot message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.UGCBot
             * @static
             * @param {proto.SyncActionValue.UGCBot} message UGCBot
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            UGCBot.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.definition != null && Object.hasOwnProperty.call(message, "definition"))
                    object.definition = options.bytes === String ? $util.base64.encode(message.definition, 0, message.definition.length) : options.bytes === Array ? Array.prototype.slice.call(message.definition) : message.definition;
                return object;
            };

            /**
             * Converts this UGCBot to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.UGCBot
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            UGCBot.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for UGCBot
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.UGCBot
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            UGCBot.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.UGCBot";
            };

            return UGCBot;
        })();

        SyncActionValue.UnarchiveChatsSetting = (function() {

            /**
             * Properties of an UnarchiveChatsSetting.
             * @memberof proto.SyncActionValue
             * @interface IUnarchiveChatsSetting
             * @property {boolean|null} [unarchiveChats] UnarchiveChatsSetting unarchiveChats
             */

            /**
             * Constructs a new UnarchiveChatsSetting.
             * @memberof proto.SyncActionValue
             * @classdesc Represents an UnarchiveChatsSetting.
             * @implements IUnarchiveChatsSetting
             * @constructor
             * @param {proto.SyncActionValue.IUnarchiveChatsSetting=} [properties] Properties to set
             */
            function UnarchiveChatsSetting(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * UnarchiveChatsSetting unarchiveChats.
             * @member {boolean|null|undefined} unarchiveChats
             * @memberof proto.SyncActionValue.UnarchiveChatsSetting
             * @instance
             */
            UnarchiveChatsSetting.prototype.unarchiveChats = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UnarchiveChatsSetting.prototype, "_unarchiveChats", {
                get: $util.oneOfGetter($oneOfFields = ["unarchiveChats"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new UnarchiveChatsSetting instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.UnarchiveChatsSetting
             * @static
             * @param {proto.SyncActionValue.IUnarchiveChatsSetting=} [properties] Properties to set
             * @returns {proto.SyncActionValue.UnarchiveChatsSetting} UnarchiveChatsSetting instance
             */
            UnarchiveChatsSetting.create = function create(properties) {
                return new UnarchiveChatsSetting(properties);
            };

            /**
             * Encodes the specified UnarchiveChatsSetting message. Does not implicitly {@link proto.SyncActionValue.UnarchiveChatsSetting.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.UnarchiveChatsSetting
             * @static
             * @param {proto.SyncActionValue.IUnarchiveChatsSetting} message UnarchiveChatsSetting message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UnarchiveChatsSetting.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.unarchiveChats != null && Object.hasOwnProperty.call(message, "unarchiveChats"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.unarchiveChats);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified UnarchiveChatsSetting message, length delimited. Does not implicitly {@link proto.SyncActionValue.UnarchiveChatsSetting.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.UnarchiveChatsSetting
             * @static
             * @param {proto.SyncActionValue.IUnarchiveChatsSetting} message UnarchiveChatsSetting message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UnarchiveChatsSetting.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an UnarchiveChatsSetting message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.UnarchiveChatsSetting
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.UnarchiveChatsSetting} UnarchiveChatsSetting
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UnarchiveChatsSetting.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.unarchiveChats = reader.bool();
                            message._unarchiveChats = "unarchiveChats";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an UnarchiveChatsSetting message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.UnarchiveChatsSetting
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.UnarchiveChatsSetting} UnarchiveChatsSetting
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UnarchiveChatsSetting.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an UnarchiveChatsSetting message.
             * @function verify
             * @memberof proto.SyncActionValue.UnarchiveChatsSetting
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            UnarchiveChatsSetting.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.unarchiveChats != null && Object.hasOwnProperty.call(message, "unarchiveChats")) {
                    properties._unarchiveChats = 1;
                    if (typeof message.unarchiveChats !== "boolean")
                        return "unarchiveChats: boolean expected";
                }
                return null;
            };

            /**
             * Creates an UnarchiveChatsSetting message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.UnarchiveChatsSetting
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.UnarchiveChatsSetting} UnarchiveChatsSetting
             */
            UnarchiveChatsSetting.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.UnarchiveChatsSetting: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.unarchiveChats != null)
                    message.unarchiveChats = Boolean(object.unarchiveChats);
                return message;
            };

            /**
             * Creates a plain object from an UnarchiveChatsSetting message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.UnarchiveChatsSetting
             * @static
             * @param {proto.SyncActionValue.UnarchiveChatsSetting} message UnarchiveChatsSetting
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            UnarchiveChatsSetting.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.unarchiveChats != null && Object.hasOwnProperty.call(message, "unarchiveChats"))
                    object.unarchiveChats = message.unarchiveChats;
                return object;
            };

            /**
             * Converts this UnarchiveChatsSetting to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.UnarchiveChatsSetting
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            UnarchiveChatsSetting.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for UnarchiveChatsSetting
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.UnarchiveChatsSetting
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            UnarchiveChatsSetting.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.UnarchiveChatsSetting";
            };

            return UnarchiveChatsSetting;
        })();

        SyncActionValue.UserStatusMuteAction = (function() {

            /**
             * Properties of a UserStatusMuteAction.
             * @memberof proto.SyncActionValue
             * @interface IUserStatusMuteAction
             * @property {boolean|null} [muted] UserStatusMuteAction muted
             */

            /**
             * Constructs a new UserStatusMuteAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a UserStatusMuteAction.
             * @implements IUserStatusMuteAction
             * @constructor
             * @param {proto.SyncActionValue.IUserStatusMuteAction=} [properties] Properties to set
             */
            function UserStatusMuteAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * UserStatusMuteAction muted.
             * @member {boolean|null|undefined} muted
             * @memberof proto.SyncActionValue.UserStatusMuteAction
             * @instance
             */
            UserStatusMuteAction.prototype.muted = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UserStatusMuteAction.prototype, "_muted", {
                get: $util.oneOfGetter($oneOfFields = ["muted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new UserStatusMuteAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.UserStatusMuteAction
             * @static
             * @param {proto.SyncActionValue.IUserStatusMuteAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.UserStatusMuteAction} UserStatusMuteAction instance
             */
            UserStatusMuteAction.create = function create(properties) {
                return new UserStatusMuteAction(properties);
            };

            /**
             * Encodes the specified UserStatusMuteAction message. Does not implicitly {@link proto.SyncActionValue.UserStatusMuteAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.UserStatusMuteAction
             * @static
             * @param {proto.SyncActionValue.IUserStatusMuteAction} message UserStatusMuteAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UserStatusMuteAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.muted != null && Object.hasOwnProperty.call(message, "muted"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.muted);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified UserStatusMuteAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.UserStatusMuteAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.UserStatusMuteAction
             * @static
             * @param {proto.SyncActionValue.IUserStatusMuteAction} message UserStatusMuteAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UserStatusMuteAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a UserStatusMuteAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.UserStatusMuteAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.UserStatusMuteAction} UserStatusMuteAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UserStatusMuteAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.muted = reader.bool();
                            message._muted = "muted";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a UserStatusMuteAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.UserStatusMuteAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.UserStatusMuteAction} UserStatusMuteAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UserStatusMuteAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a UserStatusMuteAction message.
             * @function verify
             * @memberof proto.SyncActionValue.UserStatusMuteAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            UserStatusMuteAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.muted != null && Object.hasOwnProperty.call(message, "muted")) {
                    properties._muted = 1;
                    if (typeof message.muted !== "boolean")
                        return "muted: boolean expected";
                }
                return null;
            };

            /**
             * Creates a UserStatusMuteAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.UserStatusMuteAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.UserStatusMuteAction} UserStatusMuteAction
             */
            UserStatusMuteAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.UserStatusMuteAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.muted != null)
                    message.muted = Boolean(object.muted);
                return message;
            };

            /**
             * Creates a plain object from a UserStatusMuteAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.UserStatusMuteAction
             * @static
             * @param {proto.SyncActionValue.UserStatusMuteAction} message UserStatusMuteAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            UserStatusMuteAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.muted != null && Object.hasOwnProperty.call(message, "muted"))
                    object.muted = message.muted;
                return object;
            };

            /**
             * Converts this UserStatusMuteAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.UserStatusMuteAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            UserStatusMuteAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for UserStatusMuteAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.UserStatusMuteAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            UserStatusMuteAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.UserStatusMuteAction";
            };

            return UserStatusMuteAction;
        })();

        SyncActionValue.UsernameChatStartModeAction = (function() {

            /**
             * Properties of a UsernameChatStartModeAction.
             * @memberof proto.SyncActionValue
             * @interface IUsernameChatStartModeAction
             * @property {proto.SyncActionValue.UsernameChatStartModeAction.ChatStartMode|null} [chatStartMode] UsernameChatStartModeAction chatStartMode
             */

            /**
             * Constructs a new UsernameChatStartModeAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a UsernameChatStartModeAction.
             * @implements IUsernameChatStartModeAction
             * @constructor
             * @param {proto.SyncActionValue.IUsernameChatStartModeAction=} [properties] Properties to set
             */
            function UsernameChatStartModeAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * UsernameChatStartModeAction chatStartMode.
             * @member {proto.SyncActionValue.UsernameChatStartModeAction.ChatStartMode|null|undefined} chatStartMode
             * @memberof proto.SyncActionValue.UsernameChatStartModeAction
             * @instance
             */
            UsernameChatStartModeAction.prototype.chatStartMode = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UsernameChatStartModeAction.prototype, "_chatStartMode", {
                get: $util.oneOfGetter($oneOfFields = ["chatStartMode"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new UsernameChatStartModeAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.UsernameChatStartModeAction
             * @static
             * @param {proto.SyncActionValue.IUsernameChatStartModeAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.UsernameChatStartModeAction} UsernameChatStartModeAction instance
             */
            UsernameChatStartModeAction.create = function create(properties) {
                return new UsernameChatStartModeAction(properties);
            };

            /**
             * Encodes the specified UsernameChatStartModeAction message. Does not implicitly {@link proto.SyncActionValue.UsernameChatStartModeAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.UsernameChatStartModeAction
             * @static
             * @param {proto.SyncActionValue.IUsernameChatStartModeAction} message UsernameChatStartModeAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UsernameChatStartModeAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.chatStartMode != null && Object.hasOwnProperty.call(message, "chatStartMode"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.chatStartMode);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified UsernameChatStartModeAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.UsernameChatStartModeAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.UsernameChatStartModeAction
             * @static
             * @param {proto.SyncActionValue.IUsernameChatStartModeAction} message UsernameChatStartModeAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UsernameChatStartModeAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a UsernameChatStartModeAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.UsernameChatStartModeAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.UsernameChatStartModeAction} UsernameChatStartModeAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UsernameChatStartModeAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.chatStartMode = reader.int32();
                            message._chatStartMode = "chatStartMode";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a UsernameChatStartModeAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.UsernameChatStartModeAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.UsernameChatStartModeAction} UsernameChatStartModeAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UsernameChatStartModeAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a UsernameChatStartModeAction message.
             * @function verify
             * @memberof proto.SyncActionValue.UsernameChatStartModeAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            UsernameChatStartModeAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.chatStartMode != null && Object.hasOwnProperty.call(message, "chatStartMode")) {
                    properties._chatStartMode = 1;
                    if (typeof message.chatStartMode !== "number" || (message.chatStartMode | 0) !== message.chatStartMode)
                        return "chatStartMode: enum value expected";
                }
                return null;
            };

            /**
             * Creates a UsernameChatStartModeAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.UsernameChatStartModeAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.UsernameChatStartModeAction} UsernameChatStartModeAction
             */
            UsernameChatStartModeAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.UsernameChatStartModeAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.chatStartMode) {
                case "LID":
                case 1:
                    message.chatStartMode = 1;
                    break;
                case "PN":
                case 2:
                    message.chatStartMode = 2;
                    break;
                default:
                    if (typeof object.chatStartMode === "number" && (object.chatStartMode | 0) === object.chatStartMode)
                        message.chatStartMode = object.chatStartMode;
                }
                return message;
            };

            /**
             * Creates a plain object from a UsernameChatStartModeAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.UsernameChatStartModeAction
             * @static
             * @param {proto.SyncActionValue.UsernameChatStartModeAction} message UsernameChatStartModeAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            UsernameChatStartModeAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.chatStartMode != null && Object.hasOwnProperty.call(message, "chatStartMode"))
                    object.chatStartMode = options.enums === String ? $root.proto.SyncActionValue.UsernameChatStartModeAction.ChatStartMode[message.chatStartMode] === undefined ? message.chatStartMode : $root.proto.SyncActionValue.UsernameChatStartModeAction.ChatStartMode[message.chatStartMode] : message.chatStartMode;
                return object;
            };

            /**
             * Converts this UsernameChatStartModeAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.UsernameChatStartModeAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            UsernameChatStartModeAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for UsernameChatStartModeAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.UsernameChatStartModeAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            UsernameChatStartModeAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.UsernameChatStartModeAction";
            };

            /**
             * ChatStartMode enum.
             * @name proto.SyncActionValue.UsernameChatStartModeAction.ChatStartMode
             * @enum {number}
             * @property {number} LID=1 LID value
             * @property {number} PN=2 PN value
             */
            UsernameChatStartModeAction.ChatStartMode = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[1] = "LID"] = 1;
                values[valuesById[2] = "PN"] = 2;
                return values;
            })();

            return UsernameChatStartModeAction;
        })();

        SyncActionValue.WASARootSecretAction = (function() {

            /**
             * Properties of a WASARootSecretAction.
             * @memberof proto.SyncActionValue
             * @interface IWASARootSecretAction
             * @property {Array.<proto.SyncActionValue.WASARootSecretAction.IRootSecretEntry>|null} [secrets] WASARootSecretAction secrets
             */

            /**
             * Constructs a new WASARootSecretAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a WASARootSecretAction.
             * @implements IWASARootSecretAction
             * @constructor
             * @param {proto.SyncActionValue.IWASARootSecretAction=} [properties] Properties to set
             */
            function WASARootSecretAction(properties) {
                this.secrets = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * WASARootSecretAction secrets.
             * @member {Array.<proto.SyncActionValue.WASARootSecretAction.IRootSecretEntry>} secrets
             * @memberof proto.SyncActionValue.WASARootSecretAction
             * @instance
             */
            WASARootSecretAction.prototype.secrets = $util.emptyArray;

            /**
             * Creates a new WASARootSecretAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.WASARootSecretAction
             * @static
             * @param {proto.SyncActionValue.IWASARootSecretAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.WASARootSecretAction} WASARootSecretAction instance
             */
            WASARootSecretAction.create = function create(properties) {
                return new WASARootSecretAction(properties);
            };

            /**
             * Encodes the specified WASARootSecretAction message. Does not implicitly {@link proto.SyncActionValue.WASARootSecretAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.WASARootSecretAction
             * @static
             * @param {proto.SyncActionValue.IWASARootSecretAction} message WASARootSecretAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            WASARootSecretAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.secrets != null && message.secrets.length)
                    for (var i = 0; i < message.secrets.length; ++i)
                        $root.proto.SyncActionValue.WASARootSecretAction.RootSecretEntry.encode(message.secrets[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified WASARootSecretAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.WASARootSecretAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.WASARootSecretAction
             * @static
             * @param {proto.SyncActionValue.IWASARootSecretAction} message WASARootSecretAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            WASARootSecretAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a WASARootSecretAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.WASARootSecretAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.WASARootSecretAction} WASARootSecretAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            WASARootSecretAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(message.secrets && message.secrets.length))
                                message.secrets = [];
                            message.secrets.push($root.proto.SyncActionValue.WASARootSecretAction.RootSecretEntry.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a WASARootSecretAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.WASARootSecretAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.WASARootSecretAction} WASARootSecretAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            WASARootSecretAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a WASARootSecretAction message.
             * @function verify
             * @memberof proto.SyncActionValue.WASARootSecretAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            WASARootSecretAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                if (message.secrets != null && Object.hasOwnProperty.call(message, "secrets")) {
                    if (!Array.isArray(message.secrets))
                        return "secrets: array expected";
                    for (var i = 0; i < message.secrets.length; ++i) {
                        var error = $root.proto.SyncActionValue.WASARootSecretAction.RootSecretEntry.verify(message.secrets[i], q + 1);
                        if (error)
                            return "secrets." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a WASARootSecretAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.WASARootSecretAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.WASARootSecretAction} WASARootSecretAction
             */
            WASARootSecretAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.WASARootSecretAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.secrets) {
                    if (!Array.isArray(object.secrets))
                        throw TypeError(".proto.SyncActionValue.WASARootSecretAction.secrets: array expected");
                    message.secrets = Array(object.secrets.length);
                    for (var i = 0; i < object.secrets.length; ++i) {
                        if (!$util.isObject(object.secrets[i]))
                            throw TypeError(".proto.SyncActionValue.WASARootSecretAction.secrets: object expected");
                        message.secrets[i] = $root.proto.SyncActionValue.WASARootSecretAction.RootSecretEntry.fromObject(object.secrets[i], q + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a WASARootSecretAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.WASARootSecretAction
             * @static
             * @param {proto.SyncActionValue.WASARootSecretAction} message WASARootSecretAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            WASARootSecretAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.secrets = [];
                if (message.secrets && message.secrets.length) {
                    object.secrets = Array(message.secrets.length);
                    for (var j = 0; j < message.secrets.length; ++j)
                        object.secrets[j] = $root.proto.SyncActionValue.WASARootSecretAction.RootSecretEntry.toObject(message.secrets[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this WASARootSecretAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.WASARootSecretAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            WASARootSecretAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for WASARootSecretAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.WASARootSecretAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            WASARootSecretAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.WASARootSecretAction";
            };

            WASARootSecretAction.RootSecretEntry = (function() {

                /**
                 * Properties of a RootSecretEntry.
                 * @memberof proto.SyncActionValue.WASARootSecretAction
                 * @interface IRootSecretEntry
                 * @property {string|null} [id] RootSecretEntry id
                 * @property {Uint8Array|null} [rootSecret] RootSecretEntry rootSecret
                 * @property {number|Long|null} [epoch] RootSecretEntry epoch
                 */

                /**
                 * Constructs a new RootSecretEntry.
                 * @memberof proto.SyncActionValue.WASARootSecretAction
                 * @classdesc Represents a RootSecretEntry.
                 * @implements IRootSecretEntry
                 * @constructor
                 * @param {proto.SyncActionValue.WASARootSecretAction.IRootSecretEntry=} [properties] Properties to set
                 */
                function RootSecretEntry(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * RootSecretEntry id.
                 * @member {string|null|undefined} id
                 * @memberof proto.SyncActionValue.WASARootSecretAction.RootSecretEntry
                 * @instance
                 */
                RootSecretEntry.prototype.id = null;

                /**
                 * RootSecretEntry rootSecret.
                 * @member {Uint8Array|null|undefined} rootSecret
                 * @memberof proto.SyncActionValue.WASARootSecretAction.RootSecretEntry
                 * @instance
                 */
                RootSecretEntry.prototype.rootSecret = null;

                /**
                 * RootSecretEntry epoch.
                 * @member {number|Long|null|undefined} epoch
                 * @memberof proto.SyncActionValue.WASARootSecretAction.RootSecretEntry
                 * @instance
                 */
                RootSecretEntry.prototype.epoch = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(RootSecretEntry.prototype, "_id", {
                    get: $util.oneOfGetter($oneOfFields = ["id"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(RootSecretEntry.prototype, "_rootSecret", {
                    get: $util.oneOfGetter($oneOfFields = ["rootSecret"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(RootSecretEntry.prototype, "_epoch", {
                    get: $util.oneOfGetter($oneOfFields = ["epoch"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new RootSecretEntry instance using the specified properties.
                 * @function create
                 * @memberof proto.SyncActionValue.WASARootSecretAction.RootSecretEntry
                 * @static
                 * @param {proto.SyncActionValue.WASARootSecretAction.IRootSecretEntry=} [properties] Properties to set
                 * @returns {proto.SyncActionValue.WASARootSecretAction.RootSecretEntry} RootSecretEntry instance
                 */
                RootSecretEntry.create = function create(properties) {
                    return new RootSecretEntry(properties);
                };

                /**
                 * Encodes the specified RootSecretEntry message. Does not implicitly {@link proto.SyncActionValue.WASARootSecretAction.RootSecretEntry.verify|verify} messages.
                 * @function encode
                 * @memberof proto.SyncActionValue.WASARootSecretAction.RootSecretEntry
                 * @static
                 * @param {proto.SyncActionValue.WASARootSecretAction.IRootSecretEntry} message RootSecretEntry message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                RootSecretEntry.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
                    if (message.rootSecret != null && Object.hasOwnProperty.call(message, "rootSecret"))
                        writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.rootSecret);
                    if (message.epoch != null && Object.hasOwnProperty.call(message, "epoch"))
                        writer.uint32(/* id 3, wireType 0 =*/24).int64(message.epoch);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified RootSecretEntry message, length delimited. Does not implicitly {@link proto.SyncActionValue.WASARootSecretAction.RootSecretEntry.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.SyncActionValue.WASARootSecretAction.RootSecretEntry
                 * @static
                 * @param {proto.SyncActionValue.WASARootSecretAction.IRootSecretEntry} message RootSecretEntry message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                RootSecretEntry.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a RootSecretEntry message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.SyncActionValue.WASARootSecretAction.RootSecretEntry
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.SyncActionValue.WASARootSecretAction.RootSecretEntry} RootSecretEntry
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                RootSecretEntry.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.id = reader.stringVerify();
                                message._id = "id";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                message.rootSecret = reader.bytes();
                                message._rootSecret = "rootSecret";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                message.epoch = reader.int64();
                                message._epoch = "epoch";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a RootSecretEntry message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.SyncActionValue.WASARootSecretAction.RootSecretEntry
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.SyncActionValue.WASARootSecretAction.RootSecretEntry} RootSecretEntry
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                RootSecretEntry.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a RootSecretEntry message.
                 * @function verify
                 * @memberof proto.SyncActionValue.WASARootSecretAction.RootSecretEntry
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                RootSecretEntry.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.id != null && Object.hasOwnProperty.call(message, "id")) {
                        properties._id = 1;
                        if (!$util.isString(message.id))
                            return "id: string expected";
                    }
                    if (message.rootSecret != null && Object.hasOwnProperty.call(message, "rootSecret")) {
                        properties._rootSecret = 1;
                        if (!(message.rootSecret && typeof message.rootSecret.length === "number" || $util.isString(message.rootSecret)))
                            return "rootSecret: buffer expected";
                    }
                    if (message.epoch != null && Object.hasOwnProperty.call(message, "epoch")) {
                        properties._epoch = 1;
                        if (!$util.isInteger(message.epoch) && !(message.epoch && $util.isInteger(message.epoch.low) && $util.isInteger(message.epoch.high)))
                            return "epoch: integer|Long expected";
                    }
                    return null;
                };

                /**
                 * Creates a RootSecretEntry message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.SyncActionValue.WASARootSecretAction.RootSecretEntry
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.SyncActionValue.WASARootSecretAction.RootSecretEntry} RootSecretEntry
                 */
                RootSecretEntry.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.SyncActionValue.WASARootSecretAction.RootSecretEntry: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.id != null)
                        message.id = String(object.id);
                    if (object.rootSecret != null)
                        if (typeof object.rootSecret === "string")
                            $util.base64.decode(object.rootSecret, message.rootSecret = $util.newBuffer($util.base64.length(object.rootSecret)), 0);
                        else if (object.rootSecret.length >= 0)
                            message.rootSecret = object.rootSecret;
                    if (object.epoch != null)
                        if ($util.Long)
                            message.epoch = $util.Long.fromValue(object.epoch, false);
                        else if (typeof object.epoch === "string")
                            message.epoch = parseInt(object.epoch, 10);
                        else if (typeof object.epoch === "number")
                            message.epoch = object.epoch;
                        else if (typeof object.epoch === "object")
                            message.epoch = new $util.LongBits(object.epoch.low >>> 0, object.epoch.high >>> 0).toNumber();
                    return message;
                };

                /**
                 * Creates a plain object from a RootSecretEntry message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.SyncActionValue.WASARootSecretAction.RootSecretEntry
                 * @static
                 * @param {proto.SyncActionValue.WASARootSecretAction.RootSecretEntry} message RootSecretEntry
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                RootSecretEntry.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                        object.id = message.id;
                    if (message.rootSecret != null && Object.hasOwnProperty.call(message, "rootSecret"))
                        object.rootSecret = options.bytes === String ? $util.base64.encode(message.rootSecret, 0, message.rootSecret.length) : options.bytes === Array ? Array.prototype.slice.call(message.rootSecret) : message.rootSecret;
                    if (message.epoch != null && Object.hasOwnProperty.call(message, "epoch"))
                        if (typeof BigInt !== "undefined" && options.longs === BigInt)
                            object.epoch = typeof message.epoch === "number" ? BigInt(message.epoch) : $util.Long.fromBits(message.epoch.low >>> 0, message.epoch.high >>> 0, false).toBigInt();
                        else if (typeof message.epoch === "number")
                            object.epoch = options.longs === String ? String(message.epoch) : message.epoch;
                        else
                            object.epoch = options.longs === String ? $util.Long.prototype.toString.call(message.epoch) : options.longs === Number ? new $util.LongBits(message.epoch.low >>> 0, message.epoch.high >>> 0).toNumber() : message.epoch;
                    return object;
                };

                /**
                 * Converts this RootSecretEntry to JSON.
                 * @function toJSON
                 * @memberof proto.SyncActionValue.WASARootSecretAction.RootSecretEntry
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                RootSecretEntry.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for RootSecretEntry
                 * @function getTypeUrl
                 * @memberof proto.SyncActionValue.WASARootSecretAction.RootSecretEntry
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                RootSecretEntry.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.SyncActionValue.WASARootSecretAction.RootSecretEntry";
                };

                return RootSecretEntry;
            })();

            return WASARootSecretAction;
        })();

        SyncActionValue.WaffleAccountLinkStateAction = (function() {

            /**
             * Properties of a WaffleAccountLinkStateAction.
             * @memberof proto.SyncActionValue
             * @interface IWaffleAccountLinkStateAction
             * @property {proto.SyncActionValue.WaffleAccountLinkStateAction.AccountLinkState|null} [linkState] WaffleAccountLinkStateAction linkState
             */

            /**
             * Constructs a new WaffleAccountLinkStateAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a WaffleAccountLinkStateAction.
             * @implements IWaffleAccountLinkStateAction
             * @constructor
             * @param {proto.SyncActionValue.IWaffleAccountLinkStateAction=} [properties] Properties to set
             */
            function WaffleAccountLinkStateAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * WaffleAccountLinkStateAction linkState.
             * @member {proto.SyncActionValue.WaffleAccountLinkStateAction.AccountLinkState|null|undefined} linkState
             * @memberof proto.SyncActionValue.WaffleAccountLinkStateAction
             * @instance
             */
            WaffleAccountLinkStateAction.prototype.linkState = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(WaffleAccountLinkStateAction.prototype, "_linkState", {
                get: $util.oneOfGetter($oneOfFields = ["linkState"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new WaffleAccountLinkStateAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.WaffleAccountLinkStateAction
             * @static
             * @param {proto.SyncActionValue.IWaffleAccountLinkStateAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.WaffleAccountLinkStateAction} WaffleAccountLinkStateAction instance
             */
            WaffleAccountLinkStateAction.create = function create(properties) {
                return new WaffleAccountLinkStateAction(properties);
            };

            /**
             * Encodes the specified WaffleAccountLinkStateAction message. Does not implicitly {@link proto.SyncActionValue.WaffleAccountLinkStateAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.WaffleAccountLinkStateAction
             * @static
             * @param {proto.SyncActionValue.IWaffleAccountLinkStateAction} message WaffleAccountLinkStateAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            WaffleAccountLinkStateAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.linkState != null && Object.hasOwnProperty.call(message, "linkState"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.linkState);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified WaffleAccountLinkStateAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.WaffleAccountLinkStateAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.WaffleAccountLinkStateAction
             * @static
             * @param {proto.SyncActionValue.IWaffleAccountLinkStateAction} message WaffleAccountLinkStateAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            WaffleAccountLinkStateAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a WaffleAccountLinkStateAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.WaffleAccountLinkStateAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.WaffleAccountLinkStateAction} WaffleAccountLinkStateAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            WaffleAccountLinkStateAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 2: {
                            if (u !== 0)
                                break;
                            message.linkState = reader.int32();
                            message._linkState = "linkState";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a WaffleAccountLinkStateAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.WaffleAccountLinkStateAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.WaffleAccountLinkStateAction} WaffleAccountLinkStateAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            WaffleAccountLinkStateAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a WaffleAccountLinkStateAction message.
             * @function verify
             * @memberof proto.SyncActionValue.WaffleAccountLinkStateAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            WaffleAccountLinkStateAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.linkState != null && Object.hasOwnProperty.call(message, "linkState")) {
                    properties._linkState = 1;
                    if (typeof message.linkState !== "number" || (message.linkState | 0) !== message.linkState)
                        return "linkState: enum value expected";
                }
                return null;
            };

            /**
             * Creates a WaffleAccountLinkStateAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.WaffleAccountLinkStateAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.WaffleAccountLinkStateAction} WaffleAccountLinkStateAction
             */
            WaffleAccountLinkStateAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.WaffleAccountLinkStateAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                switch (object.linkState) {
                case "ACTIVE":
                case 0:
                    message.linkState = 0;
                    break;
                case "PAUSED":
                case 1:
                    message.linkState = 1;
                    break;
                case "UNLINKED":
                case 2:
                    message.linkState = 2;
                    break;
                default:
                    if (typeof object.linkState === "number" && (object.linkState | 0) === object.linkState)
                        message.linkState = object.linkState;
                }
                return message;
            };

            /**
             * Creates a plain object from a WaffleAccountLinkStateAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.WaffleAccountLinkStateAction
             * @static
             * @param {proto.SyncActionValue.WaffleAccountLinkStateAction} message WaffleAccountLinkStateAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            WaffleAccountLinkStateAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.linkState != null && Object.hasOwnProperty.call(message, "linkState"))
                    object.linkState = options.enums === String ? $root.proto.SyncActionValue.WaffleAccountLinkStateAction.AccountLinkState[message.linkState] === undefined ? message.linkState : $root.proto.SyncActionValue.WaffleAccountLinkStateAction.AccountLinkState[message.linkState] : message.linkState;
                return object;
            };

            /**
             * Converts this WaffleAccountLinkStateAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.WaffleAccountLinkStateAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            WaffleAccountLinkStateAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for WaffleAccountLinkStateAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.WaffleAccountLinkStateAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            WaffleAccountLinkStateAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.WaffleAccountLinkStateAction";
            };

            /**
             * AccountLinkState enum.
             * @name proto.SyncActionValue.WaffleAccountLinkStateAction.AccountLinkState
             * @enum {number}
             * @property {number} ACTIVE=0 ACTIVE value
             * @property {number} PAUSED=1 PAUSED value
             * @property {number} UNLINKED=2 UNLINKED value
             */
            WaffleAccountLinkStateAction.AccountLinkState = (function() {
                var valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "ACTIVE"] = 0;
                values[valuesById[1] = "PAUSED"] = 1;
                values[valuesById[2] = "UNLINKED"] = 2;
                return values;
            })();

            return WaffleAccountLinkStateAction;
        })();

        SyncActionValue.WamoUserIdentifierAction = (function() {

            /**
             * Properties of a WamoUserIdentifierAction.
             * @memberof proto.SyncActionValue
             * @interface IWamoUserIdentifierAction
             * @property {string|null} [identifier] WamoUserIdentifierAction identifier
             */

            /**
             * Constructs a new WamoUserIdentifierAction.
             * @memberof proto.SyncActionValue
             * @classdesc Represents a WamoUserIdentifierAction.
             * @implements IWamoUserIdentifierAction
             * @constructor
             * @param {proto.SyncActionValue.IWamoUserIdentifierAction=} [properties] Properties to set
             */
            function WamoUserIdentifierAction(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * WamoUserIdentifierAction identifier.
             * @member {string|null|undefined} identifier
             * @memberof proto.SyncActionValue.WamoUserIdentifierAction
             * @instance
             */
            WamoUserIdentifierAction.prototype.identifier = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(WamoUserIdentifierAction.prototype, "_identifier", {
                get: $util.oneOfGetter($oneOfFields = ["identifier"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new WamoUserIdentifierAction instance using the specified properties.
             * @function create
             * @memberof proto.SyncActionValue.WamoUserIdentifierAction
             * @static
             * @param {proto.SyncActionValue.IWamoUserIdentifierAction=} [properties] Properties to set
             * @returns {proto.SyncActionValue.WamoUserIdentifierAction} WamoUserIdentifierAction instance
             */
            WamoUserIdentifierAction.create = function create(properties) {
                return new WamoUserIdentifierAction(properties);
            };

            /**
             * Encodes the specified WamoUserIdentifierAction message. Does not implicitly {@link proto.SyncActionValue.WamoUserIdentifierAction.verify|verify} messages.
             * @function encode
             * @memberof proto.SyncActionValue.WamoUserIdentifierAction
             * @static
             * @param {proto.SyncActionValue.IWamoUserIdentifierAction} message WamoUserIdentifierAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            WamoUserIdentifierAction.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.identifier != null && Object.hasOwnProperty.call(message, "identifier"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.identifier);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified WamoUserIdentifierAction message, length delimited. Does not implicitly {@link proto.SyncActionValue.WamoUserIdentifierAction.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.SyncActionValue.WamoUserIdentifierAction
             * @static
             * @param {proto.SyncActionValue.IWamoUserIdentifierAction} message WamoUserIdentifierAction message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            WamoUserIdentifierAction.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a WamoUserIdentifierAction message from the specified reader or buffer.
             * @function decode
             * @memberof proto.SyncActionValue.WamoUserIdentifierAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.SyncActionValue.WamoUserIdentifierAction} WamoUserIdentifierAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            WamoUserIdentifierAction.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.identifier = reader.stringVerify();
                            message._identifier = "identifier";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a WamoUserIdentifierAction message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.SyncActionValue.WamoUserIdentifierAction
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.SyncActionValue.WamoUserIdentifierAction} WamoUserIdentifierAction
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            WamoUserIdentifierAction.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a WamoUserIdentifierAction message.
             * @function verify
             * @memberof proto.SyncActionValue.WamoUserIdentifierAction
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            WamoUserIdentifierAction.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.identifier != null && Object.hasOwnProperty.call(message, "identifier")) {
                    properties._identifier = 1;
                    if (!$util.isString(message.identifier))
                        return "identifier: string expected";
                }
                return null;
            };

            /**
             * Creates a WamoUserIdentifierAction message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.SyncActionValue.WamoUserIdentifierAction
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.SyncActionValue.WamoUserIdentifierAction} WamoUserIdentifierAction
             */
            WamoUserIdentifierAction.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.SyncActionValue.WamoUserIdentifierAction: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.identifier != null)
                    message.identifier = String(object.identifier);
                return message;
            };

            /**
             * Creates a plain object from a WamoUserIdentifierAction message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.SyncActionValue.WamoUserIdentifierAction
             * @static
             * @param {proto.SyncActionValue.WamoUserIdentifierAction} message WamoUserIdentifierAction
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            WamoUserIdentifierAction.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.identifier != null && Object.hasOwnProperty.call(message, "identifier"))
                    object.identifier = message.identifier;
                return object;
            };

            /**
             * Converts this WamoUserIdentifierAction to JSON.
             * @function toJSON
             * @memberof proto.SyncActionValue.WamoUserIdentifierAction
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            WamoUserIdentifierAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for WamoUserIdentifierAction
             * @function getTypeUrl
             * @memberof proto.SyncActionValue.WamoUserIdentifierAction
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            WamoUserIdentifierAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.SyncActionValue.WamoUserIdentifierAction";
            };

            return WamoUserIdentifierAction;
        })();

        return SyncActionValue;
    })();

    proto.SyncdIndex = (function() {

        /**
         * Properties of a SyncdIndex.
         * @memberof proto
         * @interface ISyncdIndex
         * @property {Uint8Array|null} [blob] SyncdIndex blob
         */

        /**
         * Constructs a new SyncdIndex.
         * @memberof proto
         * @classdesc Represents a SyncdIndex.
         * @implements ISyncdIndex
         * @constructor
         * @param {proto.ISyncdIndex=} [properties] Properties to set
         */
        function SyncdIndex(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SyncdIndex blob.
         * @member {Uint8Array|null|undefined} blob
         * @memberof proto.SyncdIndex
         * @instance
         */
        SyncdIndex.prototype.blob = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncdIndex.prototype, "_blob", {
            get: $util.oneOfGetter($oneOfFields = ["blob"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SyncdIndex instance using the specified properties.
         * @function create
         * @memberof proto.SyncdIndex
         * @static
         * @param {proto.ISyncdIndex=} [properties] Properties to set
         * @returns {proto.SyncdIndex} SyncdIndex instance
         */
        SyncdIndex.create = function create(properties) {
            return new SyncdIndex(properties);
        };

        /**
         * Encodes the specified SyncdIndex message. Does not implicitly {@link proto.SyncdIndex.verify|verify} messages.
         * @function encode
         * @memberof proto.SyncdIndex
         * @static
         * @param {proto.ISyncdIndex} message SyncdIndex message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SyncdIndex.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.blob != null && Object.hasOwnProperty.call(message, "blob"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.blob);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SyncdIndex message, length delimited. Does not implicitly {@link proto.SyncdIndex.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SyncdIndex
         * @static
         * @param {proto.ISyncdIndex} message SyncdIndex message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SyncdIndex.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SyncdIndex message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SyncdIndex
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SyncdIndex} SyncdIndex
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SyncdIndex.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.blob = reader.bytes();
                        message._blob = "blob";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a SyncdIndex message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SyncdIndex
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SyncdIndex} SyncdIndex
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SyncdIndex.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SyncdIndex message.
         * @function verify
         * @memberof proto.SyncdIndex
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SyncdIndex.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.blob != null && Object.hasOwnProperty.call(message, "blob")) {
                properties._blob = 1;
                if (!(message.blob && typeof message.blob.length === "number" || $util.isString(message.blob)))
                    return "blob: buffer expected";
            }
            return null;
        };

        /**
         * Creates a SyncdIndex message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SyncdIndex
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SyncdIndex} SyncdIndex
         */
        SyncdIndex.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.SyncdIndex: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.blob != null)
                if (typeof object.blob === "string")
                    $util.base64.decode(object.blob, message.blob = $util.newBuffer($util.base64.length(object.blob)), 0);
                else if (object.blob.length >= 0)
                    message.blob = object.blob;
            return message;
        };

        /**
         * Creates a plain object from a SyncdIndex message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SyncdIndex
         * @static
         * @param {proto.SyncdIndex} message SyncdIndex
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SyncdIndex.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.blob != null && Object.hasOwnProperty.call(message, "blob"))
                object.blob = options.bytes === String ? $util.base64.encode(message.blob, 0, message.blob.length) : options.bytes === Array ? Array.prototype.slice.call(message.blob) : message.blob;
            return object;
        };

        /**
         * Converts this SyncdIndex to JSON.
         * @function toJSON
         * @memberof proto.SyncdIndex
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SyncdIndex.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SyncdIndex
         * @function getTypeUrl
         * @memberof proto.SyncdIndex
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SyncdIndex.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.SyncdIndex";
        };

        return SyncdIndex;
    })();

    proto.SyncdMutation = (function() {

        /**
         * Properties of a SyncdMutation.
         * @memberof proto
         * @interface ISyncdMutation
         * @property {proto.SyncdMutation.SyncdOperation|null} [operation] SyncdMutation operation
         * @property {proto.ISyncdRecord|null} [record] SyncdMutation record
         */

        /**
         * Constructs a new SyncdMutation.
         * @memberof proto
         * @classdesc Represents a SyncdMutation.
         * @implements ISyncdMutation
         * @constructor
         * @param {proto.ISyncdMutation=} [properties] Properties to set
         */
        function SyncdMutation(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SyncdMutation operation.
         * @member {proto.SyncdMutation.SyncdOperation|null|undefined} operation
         * @memberof proto.SyncdMutation
         * @instance
         */
        SyncdMutation.prototype.operation = null;

        /**
         * SyncdMutation record.
         * @member {proto.ISyncdRecord|null|undefined} record
         * @memberof proto.SyncdMutation
         * @instance
         */
        SyncdMutation.prototype.record = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncdMutation.prototype, "_operation", {
            get: $util.oneOfGetter($oneOfFields = ["operation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncdMutation.prototype, "_record", {
            get: $util.oneOfGetter($oneOfFields = ["record"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SyncdMutation instance using the specified properties.
         * @function create
         * @memberof proto.SyncdMutation
         * @static
         * @param {proto.ISyncdMutation=} [properties] Properties to set
         * @returns {proto.SyncdMutation} SyncdMutation instance
         */
        SyncdMutation.create = function create(properties) {
            return new SyncdMutation(properties);
        };

        /**
         * Encodes the specified SyncdMutation message. Does not implicitly {@link proto.SyncdMutation.verify|verify} messages.
         * @function encode
         * @memberof proto.SyncdMutation
         * @static
         * @param {proto.ISyncdMutation} message SyncdMutation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SyncdMutation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.operation);
            if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                $root.proto.SyncdRecord.encode(message.record, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SyncdMutation message, length delimited. Does not implicitly {@link proto.SyncdMutation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SyncdMutation
         * @static
         * @param {proto.ISyncdMutation} message SyncdMutation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SyncdMutation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SyncdMutation message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SyncdMutation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SyncdMutation} SyncdMutation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SyncdMutation.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.operation = reader.int32();
                        message._operation = "operation";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.record = $root.proto.SyncdRecord.decode(reader, reader.uint32(), undefined, q + 1, message.record);
                        message._record = "record";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a SyncdMutation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SyncdMutation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SyncdMutation} SyncdMutation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SyncdMutation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SyncdMutation message.
         * @function verify
         * @memberof proto.SyncdMutation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SyncdMutation.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation")) {
                properties._operation = 1;
                if (typeof message.operation !== "number" || (message.operation | 0) !== message.operation)
                    return "operation: enum value expected";
            }
            if (message.record != null && Object.hasOwnProperty.call(message, "record")) {
                properties._record = 1;
                {
                    var error = $root.proto.SyncdRecord.verify(message.record, q + 1);
                    if (error)
                        return "record." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SyncdMutation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SyncdMutation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SyncdMutation} SyncdMutation
         */
        SyncdMutation.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.SyncdMutation: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.operation) {
            case "SET":
            case 0:
                message.operation = 0;
                break;
            case "REMOVE":
            case 1:
                message.operation = 1;
                break;
            default:
                if (typeof object.operation === "number" && (object.operation | 0) === object.operation)
                    message.operation = object.operation;
            }
            if (object.record != null) {
                if (!$util.isObject(object.record))
                    throw TypeError(".proto.SyncdMutation.record: object expected");
                message.record = $root.proto.SyncdRecord.fromObject(object.record, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a SyncdMutation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SyncdMutation
         * @static
         * @param {proto.SyncdMutation} message SyncdMutation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SyncdMutation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                object.operation = options.enums === String ? $root.proto.SyncdMutation.SyncdOperation[message.operation] === undefined ? message.operation : $root.proto.SyncdMutation.SyncdOperation[message.operation] : message.operation;
            if (message.record != null && Object.hasOwnProperty.call(message, "record"))
                object.record = $root.proto.SyncdRecord.toObject(message.record, options, q + 1);
            return object;
        };

        /**
         * Converts this SyncdMutation to JSON.
         * @function toJSON
         * @memberof proto.SyncdMutation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SyncdMutation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SyncdMutation
         * @function getTypeUrl
         * @memberof proto.SyncdMutation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SyncdMutation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.SyncdMutation";
        };

        /**
         * SyncdOperation enum.
         * @name proto.SyncdMutation.SyncdOperation
         * @enum {number}
         * @property {number} SET=0 SET value
         * @property {number} REMOVE=1 REMOVE value
         */
        SyncdMutation.SyncdOperation = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "SET"] = 0;
            values[valuesById[1] = "REMOVE"] = 1;
            return values;
        })();

        return SyncdMutation;
    })();

    proto.SyncdMutations = (function() {

        /**
         * Properties of a SyncdMutations.
         * @memberof proto
         * @interface ISyncdMutations
         * @property {Array.<proto.ISyncdMutation>|null} [mutations] SyncdMutations mutations
         */

        /**
         * Constructs a new SyncdMutations.
         * @memberof proto
         * @classdesc Represents a SyncdMutations.
         * @implements ISyncdMutations
         * @constructor
         * @param {proto.ISyncdMutations=} [properties] Properties to set
         */
        function SyncdMutations(properties) {
            this.mutations = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SyncdMutations mutations.
         * @member {Array.<proto.ISyncdMutation>} mutations
         * @memberof proto.SyncdMutations
         * @instance
         */
        SyncdMutations.prototype.mutations = $util.emptyArray;

        /**
         * Creates a new SyncdMutations instance using the specified properties.
         * @function create
         * @memberof proto.SyncdMutations
         * @static
         * @param {proto.ISyncdMutations=} [properties] Properties to set
         * @returns {proto.SyncdMutations} SyncdMutations instance
         */
        SyncdMutations.create = function create(properties) {
            return new SyncdMutations(properties);
        };

        /**
         * Encodes the specified SyncdMutations message. Does not implicitly {@link proto.SyncdMutations.verify|verify} messages.
         * @function encode
         * @memberof proto.SyncdMutations
         * @static
         * @param {proto.ISyncdMutations} message SyncdMutations message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SyncdMutations.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.mutations != null && message.mutations.length)
                for (var i = 0; i < message.mutations.length; ++i)
                    $root.proto.SyncdMutation.encode(message.mutations[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SyncdMutations message, length delimited. Does not implicitly {@link proto.SyncdMutations.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SyncdMutations
         * @static
         * @param {proto.ISyncdMutations} message SyncdMutations message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SyncdMutations.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SyncdMutations message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SyncdMutations
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SyncdMutations} SyncdMutations
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SyncdMutations.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(message.mutations && message.mutations.length))
                            message.mutations = [];
                        message.mutations.push($root.proto.SyncdMutation.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a SyncdMutations message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SyncdMutations
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SyncdMutations} SyncdMutations
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SyncdMutations.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SyncdMutations message.
         * @function verify
         * @memberof proto.SyncdMutations
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SyncdMutations.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            if (message.mutations != null && Object.hasOwnProperty.call(message, "mutations")) {
                if (!Array.isArray(message.mutations))
                    return "mutations: array expected";
                for (var i = 0; i < message.mutations.length; ++i) {
                    var error = $root.proto.SyncdMutation.verify(message.mutations[i], q + 1);
                    if (error)
                        return "mutations." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SyncdMutations message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SyncdMutations
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SyncdMutations} SyncdMutations
         */
        SyncdMutations.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.SyncdMutations: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.mutations) {
                if (!Array.isArray(object.mutations))
                    throw TypeError(".proto.SyncdMutations.mutations: array expected");
                message.mutations = Array(object.mutations.length);
                for (var i = 0; i < object.mutations.length; ++i) {
                    if (!$util.isObject(object.mutations[i]))
                        throw TypeError(".proto.SyncdMutations.mutations: object expected");
                    message.mutations[i] = $root.proto.SyncdMutation.fromObject(object.mutations[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SyncdMutations message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SyncdMutations
         * @static
         * @param {proto.SyncdMutations} message SyncdMutations
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SyncdMutations.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.mutations = [];
            if (message.mutations && message.mutations.length) {
                object.mutations = Array(message.mutations.length);
                for (var j = 0; j < message.mutations.length; ++j)
                    object.mutations[j] = $root.proto.SyncdMutation.toObject(message.mutations[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this SyncdMutations to JSON.
         * @function toJSON
         * @memberof proto.SyncdMutations
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SyncdMutations.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SyncdMutations
         * @function getTypeUrl
         * @memberof proto.SyncdMutations
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SyncdMutations.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.SyncdMutations";
        };

        return SyncdMutations;
    })();

    proto.SyncdPatch = (function() {

        /**
         * Properties of a SyncdPatch.
         * @memberof proto
         * @interface ISyncdPatch
         * @property {proto.ISyncdVersion|null} [version] SyncdPatch version
         * @property {Array.<proto.ISyncdMutation>|null} [mutations] SyncdPatch mutations
         * @property {proto.IExternalBlobReference|null} [externalMutations] SyncdPatch externalMutations
         * @property {Uint8Array|null} [snapshotMac] SyncdPatch snapshotMac
         * @property {Uint8Array|null} [patchMac] SyncdPatch patchMac
         * @property {proto.IKeyId|null} [keyId] SyncdPatch keyId
         * @property {proto.IExitCode|null} [exitCode] SyncdPatch exitCode
         * @property {number|null} [deviceIndex] SyncdPatch deviceIndex
         * @property {Uint8Array|null} [clientDebugData] SyncdPatch clientDebugData
         */

        /**
         * Constructs a new SyncdPatch.
         * @memberof proto
         * @classdesc Represents a SyncdPatch.
         * @implements ISyncdPatch
         * @constructor
         * @param {proto.ISyncdPatch=} [properties] Properties to set
         */
        function SyncdPatch(properties) {
            this.mutations = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SyncdPatch version.
         * @member {proto.ISyncdVersion|null|undefined} version
         * @memberof proto.SyncdPatch
         * @instance
         */
        SyncdPatch.prototype.version = null;

        /**
         * SyncdPatch mutations.
         * @member {Array.<proto.ISyncdMutation>} mutations
         * @memberof proto.SyncdPatch
         * @instance
         */
        SyncdPatch.prototype.mutations = $util.emptyArray;

        /**
         * SyncdPatch externalMutations.
         * @member {proto.IExternalBlobReference|null|undefined} externalMutations
         * @memberof proto.SyncdPatch
         * @instance
         */
        SyncdPatch.prototype.externalMutations = null;

        /**
         * SyncdPatch snapshotMac.
         * @member {Uint8Array|null|undefined} snapshotMac
         * @memberof proto.SyncdPatch
         * @instance
         */
        SyncdPatch.prototype.snapshotMac = null;

        /**
         * SyncdPatch patchMac.
         * @member {Uint8Array|null|undefined} patchMac
         * @memberof proto.SyncdPatch
         * @instance
         */
        SyncdPatch.prototype.patchMac = null;

        /**
         * SyncdPatch keyId.
         * @member {proto.IKeyId|null|undefined} keyId
         * @memberof proto.SyncdPatch
         * @instance
         */
        SyncdPatch.prototype.keyId = null;

        /**
         * SyncdPatch exitCode.
         * @member {proto.IExitCode|null|undefined} exitCode
         * @memberof proto.SyncdPatch
         * @instance
         */
        SyncdPatch.prototype.exitCode = null;

        /**
         * SyncdPatch deviceIndex.
         * @member {number|null|undefined} deviceIndex
         * @memberof proto.SyncdPatch
         * @instance
         */
        SyncdPatch.prototype.deviceIndex = null;

        /**
         * SyncdPatch clientDebugData.
         * @member {Uint8Array|null|undefined} clientDebugData
         * @memberof proto.SyncdPatch
         * @instance
         */
        SyncdPatch.prototype.clientDebugData = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncdPatch.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncdPatch.prototype, "_externalMutations", {
            get: $util.oneOfGetter($oneOfFields = ["externalMutations"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncdPatch.prototype, "_snapshotMac", {
            get: $util.oneOfGetter($oneOfFields = ["snapshotMac"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncdPatch.prototype, "_patchMac", {
            get: $util.oneOfGetter($oneOfFields = ["patchMac"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncdPatch.prototype, "_keyId", {
            get: $util.oneOfGetter($oneOfFields = ["keyId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncdPatch.prototype, "_exitCode", {
            get: $util.oneOfGetter($oneOfFields = ["exitCode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncdPatch.prototype, "_deviceIndex", {
            get: $util.oneOfGetter($oneOfFields = ["deviceIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncdPatch.prototype, "_clientDebugData", {
            get: $util.oneOfGetter($oneOfFields = ["clientDebugData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SyncdPatch instance using the specified properties.
         * @function create
         * @memberof proto.SyncdPatch
         * @static
         * @param {proto.ISyncdPatch=} [properties] Properties to set
         * @returns {proto.SyncdPatch} SyncdPatch instance
         */
        SyncdPatch.create = function create(properties) {
            return new SyncdPatch(properties);
        };

        /**
         * Encodes the specified SyncdPatch message. Does not implicitly {@link proto.SyncdPatch.verify|verify} messages.
         * @function encode
         * @memberof proto.SyncdPatch
         * @static
         * @param {proto.ISyncdPatch} message SyncdPatch message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SyncdPatch.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                $root.proto.SyncdVersion.encode(message.version, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.mutations != null && message.mutations.length)
                for (var i = 0; i < message.mutations.length; ++i)
                    $root.proto.SyncdMutation.encode(message.mutations[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.externalMutations != null && Object.hasOwnProperty.call(message, "externalMutations"))
                $root.proto.ExternalBlobReference.encode(message.externalMutations, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.snapshotMac != null && Object.hasOwnProperty.call(message, "snapshotMac"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.snapshotMac);
            if (message.patchMac != null && Object.hasOwnProperty.call(message, "patchMac"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.patchMac);
            if (message.keyId != null && Object.hasOwnProperty.call(message, "keyId"))
                $root.proto.KeyId.encode(message.keyId, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
            if (message.exitCode != null && Object.hasOwnProperty.call(message, "exitCode"))
                $root.proto.ExitCode.encode(message.exitCode, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
            if (message.deviceIndex != null && Object.hasOwnProperty.call(message, "deviceIndex"))
                writer.uint32(/* id 8, wireType 0 =*/64).uint32(message.deviceIndex);
            if (message.clientDebugData != null && Object.hasOwnProperty.call(message, "clientDebugData"))
                writer.uint32(/* id 9, wireType 2 =*/74).bytes(message.clientDebugData);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SyncdPatch message, length delimited. Does not implicitly {@link proto.SyncdPatch.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SyncdPatch
         * @static
         * @param {proto.ISyncdPatch} message SyncdPatch message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SyncdPatch.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SyncdPatch message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SyncdPatch
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SyncdPatch} SyncdPatch
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SyncdPatch.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.version = $root.proto.SyncdVersion.decode(reader, reader.uint32(), undefined, q + 1, message.version);
                        message._version = "version";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(message.mutations && message.mutations.length))
                            message.mutations = [];
                        message.mutations.push($root.proto.SyncdMutation.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.externalMutations = $root.proto.ExternalBlobReference.decode(reader, reader.uint32(), undefined, q + 1, message.externalMutations);
                        message._externalMutations = "externalMutations";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.snapshotMac = reader.bytes();
                        message._snapshotMac = "snapshotMac";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.patchMac = reader.bytes();
                        message._patchMac = "patchMac";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        message.keyId = $root.proto.KeyId.decode(reader, reader.uint32(), undefined, q + 1, message.keyId);
                        message._keyId = "keyId";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        message.exitCode = $root.proto.ExitCode.decode(reader, reader.uint32(), undefined, q + 1, message.exitCode);
                        message._exitCode = "exitCode";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        message.deviceIndex = reader.uint32();
                        message._deviceIndex = "deviceIndex";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        message.clientDebugData = reader.bytes();
                        message._clientDebugData = "clientDebugData";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a SyncdPatch message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SyncdPatch
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SyncdPatch} SyncdPatch
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SyncdPatch.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SyncdPatch message.
         * @function verify
         * @memberof proto.SyncdPatch
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SyncdPatch.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.version != null && Object.hasOwnProperty.call(message, "version")) {
                properties._version = 1;
                {
                    var error = $root.proto.SyncdVersion.verify(message.version, q + 1);
                    if (error)
                        return "version." + error;
                }
            }
            if (message.mutations != null && Object.hasOwnProperty.call(message, "mutations")) {
                if (!Array.isArray(message.mutations))
                    return "mutations: array expected";
                for (var i = 0; i < message.mutations.length; ++i) {
                    var error = $root.proto.SyncdMutation.verify(message.mutations[i], q + 1);
                    if (error)
                        return "mutations." + error;
                }
            }
            if (message.externalMutations != null && Object.hasOwnProperty.call(message, "externalMutations")) {
                properties._externalMutations = 1;
                {
                    var error = $root.proto.ExternalBlobReference.verify(message.externalMutations, q + 1);
                    if (error)
                        return "externalMutations." + error;
                }
            }
            if (message.snapshotMac != null && Object.hasOwnProperty.call(message, "snapshotMac")) {
                properties._snapshotMac = 1;
                if (!(message.snapshotMac && typeof message.snapshotMac.length === "number" || $util.isString(message.snapshotMac)))
                    return "snapshotMac: buffer expected";
            }
            if (message.patchMac != null && Object.hasOwnProperty.call(message, "patchMac")) {
                properties._patchMac = 1;
                if (!(message.patchMac && typeof message.patchMac.length === "number" || $util.isString(message.patchMac)))
                    return "patchMac: buffer expected";
            }
            if (message.keyId != null && Object.hasOwnProperty.call(message, "keyId")) {
                properties._keyId = 1;
                {
                    var error = $root.proto.KeyId.verify(message.keyId, q + 1);
                    if (error)
                        return "keyId." + error;
                }
            }
            if (message.exitCode != null && Object.hasOwnProperty.call(message, "exitCode")) {
                properties._exitCode = 1;
                {
                    var error = $root.proto.ExitCode.verify(message.exitCode, q + 1);
                    if (error)
                        return "exitCode." + error;
                }
            }
            if (message.deviceIndex != null && Object.hasOwnProperty.call(message, "deviceIndex")) {
                properties._deviceIndex = 1;
                if (!$util.isInteger(message.deviceIndex))
                    return "deviceIndex: integer expected";
            }
            if (message.clientDebugData != null && Object.hasOwnProperty.call(message, "clientDebugData")) {
                properties._clientDebugData = 1;
                if (!(message.clientDebugData && typeof message.clientDebugData.length === "number" || $util.isString(message.clientDebugData)))
                    return "clientDebugData: buffer expected";
            }
            return null;
        };

        /**
         * Creates a SyncdPatch message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SyncdPatch
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SyncdPatch} SyncdPatch
         */
        SyncdPatch.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.SyncdPatch: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.version != null) {
                if (!$util.isObject(object.version))
                    throw TypeError(".proto.SyncdPatch.version: object expected");
                message.version = $root.proto.SyncdVersion.fromObject(object.version, q + 1);
            }
            if (object.mutations) {
                if (!Array.isArray(object.mutations))
                    throw TypeError(".proto.SyncdPatch.mutations: array expected");
                message.mutations = Array(object.mutations.length);
                for (var i = 0; i < object.mutations.length; ++i) {
                    if (!$util.isObject(object.mutations[i]))
                        throw TypeError(".proto.SyncdPatch.mutations: object expected");
                    message.mutations[i] = $root.proto.SyncdMutation.fromObject(object.mutations[i], q + 1);
                }
            }
            if (object.externalMutations != null) {
                if (!$util.isObject(object.externalMutations))
                    throw TypeError(".proto.SyncdPatch.externalMutations: object expected");
                message.externalMutations = $root.proto.ExternalBlobReference.fromObject(object.externalMutations, q + 1);
            }
            if (object.snapshotMac != null)
                if (typeof object.snapshotMac === "string")
                    $util.base64.decode(object.snapshotMac, message.snapshotMac = $util.newBuffer($util.base64.length(object.snapshotMac)), 0);
                else if (object.snapshotMac.length >= 0)
                    message.snapshotMac = object.snapshotMac;
            if (object.patchMac != null)
                if (typeof object.patchMac === "string")
                    $util.base64.decode(object.patchMac, message.patchMac = $util.newBuffer($util.base64.length(object.patchMac)), 0);
                else if (object.patchMac.length >= 0)
                    message.patchMac = object.patchMac;
            if (object.keyId != null) {
                if (!$util.isObject(object.keyId))
                    throw TypeError(".proto.SyncdPatch.keyId: object expected");
                message.keyId = $root.proto.KeyId.fromObject(object.keyId, q + 1);
            }
            if (object.exitCode != null) {
                if (!$util.isObject(object.exitCode))
                    throw TypeError(".proto.SyncdPatch.exitCode: object expected");
                message.exitCode = $root.proto.ExitCode.fromObject(object.exitCode, q + 1);
            }
            if (object.deviceIndex != null)
                message.deviceIndex = object.deviceIndex >>> 0;
            if (object.clientDebugData != null)
                if (typeof object.clientDebugData === "string")
                    $util.base64.decode(object.clientDebugData, message.clientDebugData = $util.newBuffer($util.base64.length(object.clientDebugData)), 0);
                else if (object.clientDebugData.length >= 0)
                    message.clientDebugData = object.clientDebugData;
            return message;
        };

        /**
         * Creates a plain object from a SyncdPatch message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SyncdPatch
         * @static
         * @param {proto.SyncdPatch} message SyncdPatch
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SyncdPatch.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.mutations = [];
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                object.version = $root.proto.SyncdVersion.toObject(message.version, options, q + 1);
            if (message.mutations && message.mutations.length) {
                object.mutations = Array(message.mutations.length);
                for (var j = 0; j < message.mutations.length; ++j)
                    object.mutations[j] = $root.proto.SyncdMutation.toObject(message.mutations[j], options, q + 1);
            }
            if (message.externalMutations != null && Object.hasOwnProperty.call(message, "externalMutations"))
                object.externalMutations = $root.proto.ExternalBlobReference.toObject(message.externalMutations, options, q + 1);
            if (message.snapshotMac != null && Object.hasOwnProperty.call(message, "snapshotMac"))
                object.snapshotMac = options.bytes === String ? $util.base64.encode(message.snapshotMac, 0, message.snapshotMac.length) : options.bytes === Array ? Array.prototype.slice.call(message.snapshotMac) : message.snapshotMac;
            if (message.patchMac != null && Object.hasOwnProperty.call(message, "patchMac"))
                object.patchMac = options.bytes === String ? $util.base64.encode(message.patchMac, 0, message.patchMac.length) : options.bytes === Array ? Array.prototype.slice.call(message.patchMac) : message.patchMac;
            if (message.keyId != null && Object.hasOwnProperty.call(message, "keyId"))
                object.keyId = $root.proto.KeyId.toObject(message.keyId, options, q + 1);
            if (message.exitCode != null && Object.hasOwnProperty.call(message, "exitCode"))
                object.exitCode = $root.proto.ExitCode.toObject(message.exitCode, options, q + 1);
            if (message.deviceIndex != null && Object.hasOwnProperty.call(message, "deviceIndex"))
                object.deviceIndex = message.deviceIndex;
            if (message.clientDebugData != null && Object.hasOwnProperty.call(message, "clientDebugData"))
                object.clientDebugData = options.bytes === String ? $util.base64.encode(message.clientDebugData, 0, message.clientDebugData.length) : options.bytes === Array ? Array.prototype.slice.call(message.clientDebugData) : message.clientDebugData;
            return object;
        };

        /**
         * Converts this SyncdPatch to JSON.
         * @function toJSON
         * @memberof proto.SyncdPatch
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SyncdPatch.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SyncdPatch
         * @function getTypeUrl
         * @memberof proto.SyncdPatch
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SyncdPatch.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.SyncdPatch";
        };

        return SyncdPatch;
    })();

    proto.SyncdRecord = (function() {

        /**
         * Properties of a SyncdRecord.
         * @memberof proto
         * @interface ISyncdRecord
         * @property {proto.ISyncdIndex|null} [index] SyncdRecord index
         * @property {proto.ISyncdValue|null} [value] SyncdRecord value
         * @property {proto.IKeyId|null} [keyId] SyncdRecord keyId
         */

        /**
         * Constructs a new SyncdRecord.
         * @memberof proto
         * @classdesc Represents a SyncdRecord.
         * @implements ISyncdRecord
         * @constructor
         * @param {proto.ISyncdRecord=} [properties] Properties to set
         */
        function SyncdRecord(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SyncdRecord index.
         * @member {proto.ISyncdIndex|null|undefined} index
         * @memberof proto.SyncdRecord
         * @instance
         */
        SyncdRecord.prototype.index = null;

        /**
         * SyncdRecord value.
         * @member {proto.ISyncdValue|null|undefined} value
         * @memberof proto.SyncdRecord
         * @instance
         */
        SyncdRecord.prototype.value = null;

        /**
         * SyncdRecord keyId.
         * @member {proto.IKeyId|null|undefined} keyId
         * @memberof proto.SyncdRecord
         * @instance
         */
        SyncdRecord.prototype.keyId = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncdRecord.prototype, "_index", {
            get: $util.oneOfGetter($oneOfFields = ["index"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncdRecord.prototype, "_value", {
            get: $util.oneOfGetter($oneOfFields = ["value"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncdRecord.prototype, "_keyId", {
            get: $util.oneOfGetter($oneOfFields = ["keyId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SyncdRecord instance using the specified properties.
         * @function create
         * @memberof proto.SyncdRecord
         * @static
         * @param {proto.ISyncdRecord=} [properties] Properties to set
         * @returns {proto.SyncdRecord} SyncdRecord instance
         */
        SyncdRecord.create = function create(properties) {
            return new SyncdRecord(properties);
        };

        /**
         * Encodes the specified SyncdRecord message. Does not implicitly {@link proto.SyncdRecord.verify|verify} messages.
         * @function encode
         * @memberof proto.SyncdRecord
         * @static
         * @param {proto.ISyncdRecord} message SyncdRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SyncdRecord.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                $root.proto.SyncdIndex.encode(message.index, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                $root.proto.SyncdValue.encode(message.value, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.keyId != null && Object.hasOwnProperty.call(message, "keyId"))
                $root.proto.KeyId.encode(message.keyId, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SyncdRecord message, length delimited. Does not implicitly {@link proto.SyncdRecord.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SyncdRecord
         * @static
         * @param {proto.ISyncdRecord} message SyncdRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SyncdRecord.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SyncdRecord message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SyncdRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SyncdRecord} SyncdRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SyncdRecord.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.index = $root.proto.SyncdIndex.decode(reader, reader.uint32(), undefined, q + 1, message.index);
                        message._index = "index";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.value = $root.proto.SyncdValue.decode(reader, reader.uint32(), undefined, q + 1, message.value);
                        message._value = "value";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.keyId = $root.proto.KeyId.decode(reader, reader.uint32(), undefined, q + 1, message.keyId);
                        message._keyId = "keyId";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a SyncdRecord message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SyncdRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SyncdRecord} SyncdRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SyncdRecord.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SyncdRecord message.
         * @function verify
         * @memberof proto.SyncdRecord
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SyncdRecord.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.index != null && Object.hasOwnProperty.call(message, "index")) {
                properties._index = 1;
                {
                    var error = $root.proto.SyncdIndex.verify(message.index, q + 1);
                    if (error)
                        return "index." + error;
                }
            }
            if (message.value != null && Object.hasOwnProperty.call(message, "value")) {
                properties._value = 1;
                {
                    var error = $root.proto.SyncdValue.verify(message.value, q + 1);
                    if (error)
                        return "value." + error;
                }
            }
            if (message.keyId != null && Object.hasOwnProperty.call(message, "keyId")) {
                properties._keyId = 1;
                {
                    var error = $root.proto.KeyId.verify(message.keyId, q + 1);
                    if (error)
                        return "keyId." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SyncdRecord message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SyncdRecord
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SyncdRecord} SyncdRecord
         */
        SyncdRecord.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.SyncdRecord: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.index != null) {
                if (!$util.isObject(object.index))
                    throw TypeError(".proto.SyncdRecord.index: object expected");
                message.index = $root.proto.SyncdIndex.fromObject(object.index, q + 1);
            }
            if (object.value != null) {
                if (!$util.isObject(object.value))
                    throw TypeError(".proto.SyncdRecord.value: object expected");
                message.value = $root.proto.SyncdValue.fromObject(object.value, q + 1);
            }
            if (object.keyId != null) {
                if (!$util.isObject(object.keyId))
                    throw TypeError(".proto.SyncdRecord.keyId: object expected");
                message.keyId = $root.proto.KeyId.fromObject(object.keyId, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a SyncdRecord message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SyncdRecord
         * @static
         * @param {proto.SyncdRecord} message SyncdRecord
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SyncdRecord.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                object.index = $root.proto.SyncdIndex.toObject(message.index, options, q + 1);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                object.value = $root.proto.SyncdValue.toObject(message.value, options, q + 1);
            if (message.keyId != null && Object.hasOwnProperty.call(message, "keyId"))
                object.keyId = $root.proto.KeyId.toObject(message.keyId, options, q + 1);
            return object;
        };

        /**
         * Converts this SyncdRecord to JSON.
         * @function toJSON
         * @memberof proto.SyncdRecord
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SyncdRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SyncdRecord
         * @function getTypeUrl
         * @memberof proto.SyncdRecord
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SyncdRecord.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.SyncdRecord";
        };

        return SyncdRecord;
    })();

    proto.SyncdSnapshot = (function() {

        /**
         * Properties of a SyncdSnapshot.
         * @memberof proto
         * @interface ISyncdSnapshot
         * @property {proto.ISyncdVersion|null} [version] SyncdSnapshot version
         * @property {Array.<proto.ISyncdRecord>|null} [records] SyncdSnapshot records
         * @property {Uint8Array|null} [mac] SyncdSnapshot mac
         * @property {proto.IKeyId|null} [keyId] SyncdSnapshot keyId
         */

        /**
         * Constructs a new SyncdSnapshot.
         * @memberof proto
         * @classdesc Represents a SyncdSnapshot.
         * @implements ISyncdSnapshot
         * @constructor
         * @param {proto.ISyncdSnapshot=} [properties] Properties to set
         */
        function SyncdSnapshot(properties) {
            this.records = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SyncdSnapshot version.
         * @member {proto.ISyncdVersion|null|undefined} version
         * @memberof proto.SyncdSnapshot
         * @instance
         */
        SyncdSnapshot.prototype.version = null;

        /**
         * SyncdSnapshot records.
         * @member {Array.<proto.ISyncdRecord>} records
         * @memberof proto.SyncdSnapshot
         * @instance
         */
        SyncdSnapshot.prototype.records = $util.emptyArray;

        /**
         * SyncdSnapshot mac.
         * @member {Uint8Array|null|undefined} mac
         * @memberof proto.SyncdSnapshot
         * @instance
         */
        SyncdSnapshot.prototype.mac = null;

        /**
         * SyncdSnapshot keyId.
         * @member {proto.IKeyId|null|undefined} keyId
         * @memberof proto.SyncdSnapshot
         * @instance
         */
        SyncdSnapshot.prototype.keyId = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncdSnapshot.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncdSnapshot.prototype, "_mac", {
            get: $util.oneOfGetter($oneOfFields = ["mac"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncdSnapshot.prototype, "_keyId", {
            get: $util.oneOfGetter($oneOfFields = ["keyId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SyncdSnapshot instance using the specified properties.
         * @function create
         * @memberof proto.SyncdSnapshot
         * @static
         * @param {proto.ISyncdSnapshot=} [properties] Properties to set
         * @returns {proto.SyncdSnapshot} SyncdSnapshot instance
         */
        SyncdSnapshot.create = function create(properties) {
            return new SyncdSnapshot(properties);
        };

        /**
         * Encodes the specified SyncdSnapshot message. Does not implicitly {@link proto.SyncdSnapshot.verify|verify} messages.
         * @function encode
         * @memberof proto.SyncdSnapshot
         * @static
         * @param {proto.ISyncdSnapshot} message SyncdSnapshot message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SyncdSnapshot.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                $root.proto.SyncdVersion.encode(message.version, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.records != null && message.records.length)
                for (var i = 0; i < message.records.length; ++i)
                    $root.proto.SyncdRecord.encode(message.records[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.mac != null && Object.hasOwnProperty.call(message, "mac"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.mac);
            if (message.keyId != null && Object.hasOwnProperty.call(message, "keyId"))
                $root.proto.KeyId.encode(message.keyId, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SyncdSnapshot message, length delimited. Does not implicitly {@link proto.SyncdSnapshot.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SyncdSnapshot
         * @static
         * @param {proto.ISyncdSnapshot} message SyncdSnapshot message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SyncdSnapshot.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SyncdSnapshot message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SyncdSnapshot
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SyncdSnapshot} SyncdSnapshot
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SyncdSnapshot.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.version = $root.proto.SyncdVersion.decode(reader, reader.uint32(), undefined, q + 1, message.version);
                        message._version = "version";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(message.records && message.records.length))
                            message.records = [];
                        message.records.push($root.proto.SyncdRecord.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.mac = reader.bytes();
                        message._mac = "mac";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.keyId = $root.proto.KeyId.decode(reader, reader.uint32(), undefined, q + 1, message.keyId);
                        message._keyId = "keyId";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a SyncdSnapshot message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SyncdSnapshot
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SyncdSnapshot} SyncdSnapshot
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SyncdSnapshot.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SyncdSnapshot message.
         * @function verify
         * @memberof proto.SyncdSnapshot
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SyncdSnapshot.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.version != null && Object.hasOwnProperty.call(message, "version")) {
                properties._version = 1;
                {
                    var error = $root.proto.SyncdVersion.verify(message.version, q + 1);
                    if (error)
                        return "version." + error;
                }
            }
            if (message.records != null && Object.hasOwnProperty.call(message, "records")) {
                if (!Array.isArray(message.records))
                    return "records: array expected";
                for (var i = 0; i < message.records.length; ++i) {
                    var error = $root.proto.SyncdRecord.verify(message.records[i], q + 1);
                    if (error)
                        return "records." + error;
                }
            }
            if (message.mac != null && Object.hasOwnProperty.call(message, "mac")) {
                properties._mac = 1;
                if (!(message.mac && typeof message.mac.length === "number" || $util.isString(message.mac)))
                    return "mac: buffer expected";
            }
            if (message.keyId != null && Object.hasOwnProperty.call(message, "keyId")) {
                properties._keyId = 1;
                {
                    var error = $root.proto.KeyId.verify(message.keyId, q + 1);
                    if (error)
                        return "keyId." + error;
                }
            }
            return null;
        };

        /**
         * Creates a SyncdSnapshot message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SyncdSnapshot
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SyncdSnapshot} SyncdSnapshot
         */
        SyncdSnapshot.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.SyncdSnapshot: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.version != null) {
                if (!$util.isObject(object.version))
                    throw TypeError(".proto.SyncdSnapshot.version: object expected");
                message.version = $root.proto.SyncdVersion.fromObject(object.version, q + 1);
            }
            if (object.records) {
                if (!Array.isArray(object.records))
                    throw TypeError(".proto.SyncdSnapshot.records: array expected");
                message.records = Array(object.records.length);
                for (var i = 0; i < object.records.length; ++i) {
                    if (!$util.isObject(object.records[i]))
                        throw TypeError(".proto.SyncdSnapshot.records: object expected");
                    message.records[i] = $root.proto.SyncdRecord.fromObject(object.records[i], q + 1);
                }
            }
            if (object.mac != null)
                if (typeof object.mac === "string")
                    $util.base64.decode(object.mac, message.mac = $util.newBuffer($util.base64.length(object.mac)), 0);
                else if (object.mac.length >= 0)
                    message.mac = object.mac;
            if (object.keyId != null) {
                if (!$util.isObject(object.keyId))
                    throw TypeError(".proto.SyncdSnapshot.keyId: object expected");
                message.keyId = $root.proto.KeyId.fromObject(object.keyId, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a SyncdSnapshot message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SyncdSnapshot
         * @static
         * @param {proto.SyncdSnapshot} message SyncdSnapshot
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SyncdSnapshot.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.records = [];
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                object.version = $root.proto.SyncdVersion.toObject(message.version, options, q + 1);
            if (message.records && message.records.length) {
                object.records = Array(message.records.length);
                for (var j = 0; j < message.records.length; ++j)
                    object.records[j] = $root.proto.SyncdRecord.toObject(message.records[j], options, q + 1);
            }
            if (message.mac != null && Object.hasOwnProperty.call(message, "mac"))
                object.mac = options.bytes === String ? $util.base64.encode(message.mac, 0, message.mac.length) : options.bytes === Array ? Array.prototype.slice.call(message.mac) : message.mac;
            if (message.keyId != null && Object.hasOwnProperty.call(message, "keyId"))
                object.keyId = $root.proto.KeyId.toObject(message.keyId, options, q + 1);
            return object;
        };

        /**
         * Converts this SyncdSnapshot to JSON.
         * @function toJSON
         * @memberof proto.SyncdSnapshot
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SyncdSnapshot.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SyncdSnapshot
         * @function getTypeUrl
         * @memberof proto.SyncdSnapshot
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SyncdSnapshot.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.SyncdSnapshot";
        };

        return SyncdSnapshot;
    })();

    proto.SyncdValue = (function() {

        /**
         * Properties of a SyncdValue.
         * @memberof proto
         * @interface ISyncdValue
         * @property {Uint8Array|null} [blob] SyncdValue blob
         */

        /**
         * Constructs a new SyncdValue.
         * @memberof proto
         * @classdesc Represents a SyncdValue.
         * @implements ISyncdValue
         * @constructor
         * @param {proto.ISyncdValue=} [properties] Properties to set
         */
        function SyncdValue(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SyncdValue blob.
         * @member {Uint8Array|null|undefined} blob
         * @memberof proto.SyncdValue
         * @instance
         */
        SyncdValue.prototype.blob = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncdValue.prototype, "_blob", {
            get: $util.oneOfGetter($oneOfFields = ["blob"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SyncdValue instance using the specified properties.
         * @function create
         * @memberof proto.SyncdValue
         * @static
         * @param {proto.ISyncdValue=} [properties] Properties to set
         * @returns {proto.SyncdValue} SyncdValue instance
         */
        SyncdValue.create = function create(properties) {
            return new SyncdValue(properties);
        };

        /**
         * Encodes the specified SyncdValue message. Does not implicitly {@link proto.SyncdValue.verify|verify} messages.
         * @function encode
         * @memberof proto.SyncdValue
         * @static
         * @param {proto.ISyncdValue} message SyncdValue message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SyncdValue.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.blob != null && Object.hasOwnProperty.call(message, "blob"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.blob);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SyncdValue message, length delimited. Does not implicitly {@link proto.SyncdValue.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SyncdValue
         * @static
         * @param {proto.ISyncdValue} message SyncdValue message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SyncdValue.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SyncdValue message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SyncdValue
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SyncdValue} SyncdValue
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SyncdValue.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.blob = reader.bytes();
                        message._blob = "blob";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a SyncdValue message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SyncdValue
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SyncdValue} SyncdValue
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SyncdValue.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SyncdValue message.
         * @function verify
         * @memberof proto.SyncdValue
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SyncdValue.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.blob != null && Object.hasOwnProperty.call(message, "blob")) {
                properties._blob = 1;
                if (!(message.blob && typeof message.blob.length === "number" || $util.isString(message.blob)))
                    return "blob: buffer expected";
            }
            return null;
        };

        /**
         * Creates a SyncdValue message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SyncdValue
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SyncdValue} SyncdValue
         */
        SyncdValue.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.SyncdValue: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.blob != null)
                if (typeof object.blob === "string")
                    $util.base64.decode(object.blob, message.blob = $util.newBuffer($util.base64.length(object.blob)), 0);
                else if (object.blob.length >= 0)
                    message.blob = object.blob;
            return message;
        };

        /**
         * Creates a plain object from a SyncdValue message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SyncdValue
         * @static
         * @param {proto.SyncdValue} message SyncdValue
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SyncdValue.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.blob != null && Object.hasOwnProperty.call(message, "blob"))
                object.blob = options.bytes === String ? $util.base64.encode(message.blob, 0, message.blob.length) : options.bytes === Array ? Array.prototype.slice.call(message.blob) : message.blob;
            return object;
        };

        /**
         * Converts this SyncdValue to JSON.
         * @function toJSON
         * @memberof proto.SyncdValue
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SyncdValue.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SyncdValue
         * @function getTypeUrl
         * @memberof proto.SyncdValue
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SyncdValue.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.SyncdValue";
        };

        return SyncdValue;
    })();

    proto.SyncdVersion = (function() {

        /**
         * Properties of a SyncdVersion.
         * @memberof proto
         * @interface ISyncdVersion
         * @property {number|Long|null} [version] SyncdVersion version
         */

        /**
         * Constructs a new SyncdVersion.
         * @memberof proto
         * @classdesc Represents a SyncdVersion.
         * @implements ISyncdVersion
         * @constructor
         * @param {proto.ISyncdVersion=} [properties] Properties to set
         */
        function SyncdVersion(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SyncdVersion version.
         * @member {number|Long|null|undefined} version
         * @memberof proto.SyncdVersion
         * @instance
         */
        SyncdVersion.prototype.version = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SyncdVersion.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new SyncdVersion instance using the specified properties.
         * @function create
         * @memberof proto.SyncdVersion
         * @static
         * @param {proto.ISyncdVersion=} [properties] Properties to set
         * @returns {proto.SyncdVersion} SyncdVersion instance
         */
        SyncdVersion.create = function create(properties) {
            return new SyncdVersion(properties);
        };

        /**
         * Encodes the specified SyncdVersion message. Does not implicitly {@link proto.SyncdVersion.verify|verify} messages.
         * @function encode
         * @memberof proto.SyncdVersion
         * @static
         * @param {proto.ISyncdVersion} message SyncdVersion message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SyncdVersion.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.version);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified SyncdVersion message, length delimited. Does not implicitly {@link proto.SyncdVersion.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.SyncdVersion
         * @static
         * @param {proto.ISyncdVersion} message SyncdVersion message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SyncdVersion.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SyncdVersion message from the specified reader or buffer.
         * @function decode
         * @memberof proto.SyncdVersion
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.SyncdVersion} SyncdVersion
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SyncdVersion.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.version = reader.uint64();
                        message._version = "version";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a SyncdVersion message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.SyncdVersion
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.SyncdVersion} SyncdVersion
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SyncdVersion.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SyncdVersion message.
         * @function verify
         * @memberof proto.SyncdVersion
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SyncdVersion.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.version != null && Object.hasOwnProperty.call(message, "version")) {
                properties._version = 1;
                if (!$util.isInteger(message.version) && !(message.version && $util.isInteger(message.version.low) && $util.isInteger(message.version.high)))
                    return "version: integer|Long expected";
            }
            return null;
        };

        /**
         * Creates a SyncdVersion message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.SyncdVersion
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.SyncdVersion} SyncdVersion
         */
        SyncdVersion.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.SyncdVersion: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.version != null)
                if ($util.Long)
                    message.version = $util.Long.fromValue(object.version, true);
                else if (typeof object.version === "string")
                    message.version = parseInt(object.version, 10);
                else if (typeof object.version === "number")
                    message.version = object.version;
                else if (typeof object.version === "object")
                    message.version = new $util.LongBits(object.version.low >>> 0, object.version.high >>> 0).toNumber(true);
            return message;
        };

        /**
         * Creates a plain object from a SyncdVersion message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.SyncdVersion
         * @static
         * @param {proto.SyncdVersion} message SyncdVersion
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SyncdVersion.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.version != null && Object.hasOwnProperty.call(message, "version"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.version = typeof message.version === "number" ? BigInt(message.version) : $util.Long.fromBits(message.version.low >>> 0, message.version.high >>> 0, true).toBigInt();
                else if (typeof message.version === "number")
                    object.version = options.longs === String ? String(message.version) : message.version;
                else
                    object.version = options.longs === String ? $util.Long.prototype.toString.call(message.version) : options.longs === Number ? new $util.LongBits(message.version.low >>> 0, message.version.high >>> 0).toNumber(true) : message.version;
            return object;
        };

        /**
         * Converts this SyncdVersion to JSON.
         * @function toJSON
         * @memberof proto.SyncdVersion
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SyncdVersion.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SyncdVersion
         * @function getTypeUrl
         * @memberof proto.SyncdVersion
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SyncdVersion.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.SyncdVersion";
        };

        return SyncdVersion;
    })();

    proto.TapLinkAction = (function() {

        /**
         * Properties of a TapLinkAction.
         * @memberof proto
         * @interface ITapLinkAction
         * @property {string|null} [title] TapLinkAction title
         * @property {string|null} [tapUrl] TapLinkAction tapUrl
         */

        /**
         * Constructs a new TapLinkAction.
         * @memberof proto
         * @classdesc Represents a TapLinkAction.
         * @implements ITapLinkAction
         * @constructor
         * @param {proto.ITapLinkAction=} [properties] Properties to set
         */
        function TapLinkAction(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TapLinkAction title.
         * @member {string|null|undefined} title
         * @memberof proto.TapLinkAction
         * @instance
         */
        TapLinkAction.prototype.title = null;

        /**
         * TapLinkAction tapUrl.
         * @member {string|null|undefined} tapUrl
         * @memberof proto.TapLinkAction
         * @instance
         */
        TapLinkAction.prototype.tapUrl = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(TapLinkAction.prototype, "_title", {
            get: $util.oneOfGetter($oneOfFields = ["title"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(TapLinkAction.prototype, "_tapUrl", {
            get: $util.oneOfGetter($oneOfFields = ["tapUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new TapLinkAction instance using the specified properties.
         * @function create
         * @memberof proto.TapLinkAction
         * @static
         * @param {proto.ITapLinkAction=} [properties] Properties to set
         * @returns {proto.TapLinkAction} TapLinkAction instance
         */
        TapLinkAction.create = function create(properties) {
            return new TapLinkAction(properties);
        };

        /**
         * Encodes the specified TapLinkAction message. Does not implicitly {@link proto.TapLinkAction.verify|verify} messages.
         * @function encode
         * @memberof proto.TapLinkAction
         * @static
         * @param {proto.ITapLinkAction} message TapLinkAction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TapLinkAction.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.title);
            if (message.tapUrl != null && Object.hasOwnProperty.call(message, "tapUrl"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.tapUrl);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TapLinkAction message, length delimited. Does not implicitly {@link proto.TapLinkAction.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TapLinkAction
         * @static
         * @param {proto.ITapLinkAction} message TapLinkAction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TapLinkAction.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TapLinkAction message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TapLinkAction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TapLinkAction} TapLinkAction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TapLinkAction.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.title = reader.stringVerify();
                        message._title = "title";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.tapUrl = reader.stringVerify();
                        message._tapUrl = "tapUrl";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a TapLinkAction message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TapLinkAction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TapLinkAction} TapLinkAction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TapLinkAction.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TapLinkAction message.
         * @function verify
         * @memberof proto.TapLinkAction
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TapLinkAction.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.title != null && Object.hasOwnProperty.call(message, "title")) {
                properties._title = 1;
                if (!$util.isString(message.title))
                    return "title: string expected";
            }
            if (message.tapUrl != null && Object.hasOwnProperty.call(message, "tapUrl")) {
                properties._tapUrl = 1;
                if (!$util.isString(message.tapUrl))
                    return "tapUrl: string expected";
            }
            return null;
        };

        /**
         * Creates a TapLinkAction message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TapLinkAction
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TapLinkAction} TapLinkAction
         */
        TapLinkAction.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.TapLinkAction: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.title != null)
                message.title = String(object.title);
            if (object.tapUrl != null)
                message.tapUrl = String(object.tapUrl);
            return message;
        };

        /**
         * Creates a plain object from a TapLinkAction message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TapLinkAction
         * @static
         * @param {proto.TapLinkAction} message TapLinkAction
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TapLinkAction.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.title != null && Object.hasOwnProperty.call(message, "title"))
                object.title = message.title;
            if (message.tapUrl != null && Object.hasOwnProperty.call(message, "tapUrl"))
                object.tapUrl = message.tapUrl;
            return object;
        };

        /**
         * Converts this TapLinkAction to JSON.
         * @function toJSON
         * @memberof proto.TapLinkAction
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TapLinkAction.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TapLinkAction
         * @function getTypeUrl
         * @memberof proto.TapLinkAction
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TapLinkAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.TapLinkAction";
        };

        return TapLinkAction;
    })();

    proto.TemplateButton = (function() {

        /**
         * Properties of a TemplateButton.
         * @memberof proto
         * @interface ITemplateButton
         * @property {number|null} [index] TemplateButton index
         * @property {proto.TemplateButton.IQuickReplyButton|null} [quickReplyButton] TemplateButton quickReplyButton
         * @property {proto.TemplateButton.IURLButton|null} [urlButton] TemplateButton urlButton
         * @property {proto.TemplateButton.ICallButton|null} [callButton] TemplateButton callButton
         */

        /**
         * Constructs a new TemplateButton.
         * @memberof proto
         * @classdesc Represents a TemplateButton.
         * @implements ITemplateButton
         * @constructor
         * @param {proto.ITemplateButton=} [properties] Properties to set
         */
        function TemplateButton(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TemplateButton index.
         * @member {number|null|undefined} index
         * @memberof proto.TemplateButton
         * @instance
         */
        TemplateButton.prototype.index = null;

        /**
         * TemplateButton quickReplyButton.
         * @member {proto.TemplateButton.IQuickReplyButton|null|undefined} quickReplyButton
         * @memberof proto.TemplateButton
         * @instance
         */
        TemplateButton.prototype.quickReplyButton = null;

        /**
         * TemplateButton urlButton.
         * @member {proto.TemplateButton.IURLButton|null|undefined} urlButton
         * @memberof proto.TemplateButton
         * @instance
         */
        TemplateButton.prototype.urlButton = null;

        /**
         * TemplateButton callButton.
         * @member {proto.TemplateButton.ICallButton|null|undefined} callButton
         * @memberof proto.TemplateButton
         * @instance
         */
        TemplateButton.prototype.callButton = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(TemplateButton.prototype, "_index", {
            get: $util.oneOfGetter($oneOfFields = ["index"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * TemplateButton button.
         * @member {"quickReplyButton"|"urlButton"|"callButton"|undefined} button
         * @memberof proto.TemplateButton
         * @instance
         */
        Object.defineProperty(TemplateButton.prototype, "button", {
            get: $util.oneOfGetter($oneOfFields = ["quickReplyButton", "urlButton", "callButton"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new TemplateButton instance using the specified properties.
         * @function create
         * @memberof proto.TemplateButton
         * @static
         * @param {proto.ITemplateButton=} [properties] Properties to set
         * @returns {proto.TemplateButton} TemplateButton instance
         */
        TemplateButton.create = function create(properties) {
            return new TemplateButton(properties);
        };

        /**
         * Encodes the specified TemplateButton message. Does not implicitly {@link proto.TemplateButton.verify|verify} messages.
         * @function encode
         * @memberof proto.TemplateButton
         * @static
         * @param {proto.ITemplateButton} message TemplateButton message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TemplateButton.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.quickReplyButton != null && Object.hasOwnProperty.call(message, "quickReplyButton"))
                $root.proto.TemplateButton.QuickReplyButton.encode(message.quickReplyButton, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.urlButton != null && Object.hasOwnProperty.call(message, "urlButton"))
                $root.proto.TemplateButton.URLButton.encode(message.urlButton, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.callButton != null && Object.hasOwnProperty.call(message, "callButton"))
                $root.proto.TemplateButton.CallButton.encode(message.callButton, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.index);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified TemplateButton message, length delimited. Does not implicitly {@link proto.TemplateButton.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.TemplateButton
         * @static
         * @param {proto.ITemplateButton} message TemplateButton message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TemplateButton.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TemplateButton message from the specified reader or buffer.
         * @function decode
         * @memberof proto.TemplateButton
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.TemplateButton} TemplateButton
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TemplateButton.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 4: {
                        if (u !== 0)
                            break;
                        message.index = reader.uint32();
                        message._index = "index";
                        continue;
                    }
                case 1: {
                        if (u !== 2)
                            break;
                        message.quickReplyButton = $root.proto.TemplateButton.QuickReplyButton.decode(reader, reader.uint32(), undefined, q + 1, message.quickReplyButton);
                        message.button = "quickReplyButton";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.urlButton = $root.proto.TemplateButton.URLButton.decode(reader, reader.uint32(), undefined, q + 1, message.urlButton);
                        message.button = "urlButton";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.callButton = $root.proto.TemplateButton.CallButton.decode(reader, reader.uint32(), undefined, q + 1, message.callButton);
                        message.button = "callButton";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a TemplateButton message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.TemplateButton
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.TemplateButton} TemplateButton
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TemplateButton.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TemplateButton message.
         * @function verify
         * @memberof proto.TemplateButton
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TemplateButton.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.index != null && Object.hasOwnProperty.call(message, "index")) {
                properties._index = 1;
                if (!$util.isInteger(message.index))
                    return "index: integer expected";
            }
            if (message.quickReplyButton != null && Object.hasOwnProperty.call(message, "quickReplyButton")) {
                properties.button = 1;
                {
                    var error = $root.proto.TemplateButton.QuickReplyButton.verify(message.quickReplyButton, q + 1);
                    if (error)
                        return "quickReplyButton." + error;
                }
            }
            if (message.urlButton != null && Object.hasOwnProperty.call(message, "urlButton")) {
                if (properties.button === 1)
                    return "button: multiple values";
                properties.button = 1;
                {
                    var error = $root.proto.TemplateButton.URLButton.verify(message.urlButton, q + 1);
                    if (error)
                        return "urlButton." + error;
                }
            }
            if (message.callButton != null && Object.hasOwnProperty.call(message, "callButton")) {
                if (properties.button === 1)
                    return "button: multiple values";
                properties.button = 1;
                {
                    var error = $root.proto.TemplateButton.CallButton.verify(message.callButton, q + 1);
                    if (error)
                        return "callButton." + error;
                }
            }
            return null;
        };

        /**
         * Creates a TemplateButton message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.TemplateButton
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.TemplateButton} TemplateButton
         */
        TemplateButton.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.TemplateButton: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.index != null)
                message.index = object.index >>> 0;
            if (object.quickReplyButton != null) {
                if (!$util.isObject(object.quickReplyButton))
                    throw TypeError(".proto.TemplateButton.quickReplyButton: object expected");
                message.quickReplyButton = $root.proto.TemplateButton.QuickReplyButton.fromObject(object.quickReplyButton, q + 1);
            }
            if (object.urlButton != null) {
                if (!$util.isObject(object.urlButton))
                    throw TypeError(".proto.TemplateButton.urlButton: object expected");
                message.urlButton = $root.proto.TemplateButton.URLButton.fromObject(object.urlButton, q + 1);
            }
            if (object.callButton != null) {
                if (!$util.isObject(object.callButton))
                    throw TypeError(".proto.TemplateButton.callButton: object expected");
                message.callButton = $root.proto.TemplateButton.CallButton.fromObject(object.callButton, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TemplateButton message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.TemplateButton
         * @static
         * @param {proto.TemplateButton} message TemplateButton
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TemplateButton.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.quickReplyButton != null && Object.hasOwnProperty.call(message, "quickReplyButton")) {
                object.quickReplyButton = $root.proto.TemplateButton.QuickReplyButton.toObject(message.quickReplyButton, options, q + 1);
                if (options.oneofs)
                    object.button = "quickReplyButton";
            }
            if (message.urlButton != null && Object.hasOwnProperty.call(message, "urlButton")) {
                object.urlButton = $root.proto.TemplateButton.URLButton.toObject(message.urlButton, options, q + 1);
                if (options.oneofs)
                    object.button = "urlButton";
            }
            if (message.callButton != null && Object.hasOwnProperty.call(message, "callButton")) {
                object.callButton = $root.proto.TemplateButton.CallButton.toObject(message.callButton, options, q + 1);
                if (options.oneofs)
                    object.button = "callButton";
            }
            if (message.index != null && Object.hasOwnProperty.call(message, "index"))
                object.index = message.index;
            return object;
        };

        /**
         * Converts this TemplateButton to JSON.
         * @function toJSON
         * @memberof proto.TemplateButton
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TemplateButton.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TemplateButton
         * @function getTypeUrl
         * @memberof proto.TemplateButton
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TemplateButton.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.TemplateButton";
        };

        TemplateButton.CallButton = (function() {

            /**
             * Properties of a CallButton.
             * @memberof proto.TemplateButton
             * @interface ICallButton
             * @property {proto.Message.IHighlyStructuredMessage|null} [displayText] CallButton displayText
             * @property {proto.Message.IHighlyStructuredMessage|null} [phoneNumber] CallButton phoneNumber
             */

            /**
             * Constructs a new CallButton.
             * @memberof proto.TemplateButton
             * @classdesc Represents a CallButton.
             * @implements ICallButton
             * @constructor
             * @param {proto.TemplateButton.ICallButton=} [properties] Properties to set
             */
            function CallButton(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * CallButton displayText.
             * @member {proto.Message.IHighlyStructuredMessage|null|undefined} displayText
             * @memberof proto.TemplateButton.CallButton
             * @instance
             */
            CallButton.prototype.displayText = null;

            /**
             * CallButton phoneNumber.
             * @member {proto.Message.IHighlyStructuredMessage|null|undefined} phoneNumber
             * @memberof proto.TemplateButton.CallButton
             * @instance
             */
            CallButton.prototype.phoneNumber = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CallButton.prototype, "_displayText", {
                get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CallButton.prototype, "_phoneNumber", {
                get: $util.oneOfGetter($oneOfFields = ["phoneNumber"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new CallButton instance using the specified properties.
             * @function create
             * @memberof proto.TemplateButton.CallButton
             * @static
             * @param {proto.TemplateButton.ICallButton=} [properties] Properties to set
             * @returns {proto.TemplateButton.CallButton} CallButton instance
             */
            CallButton.create = function create(properties) {
                return new CallButton(properties);
            };

            /**
             * Encodes the specified CallButton message. Does not implicitly {@link proto.TemplateButton.CallButton.verify|verify} messages.
             * @function encode
             * @memberof proto.TemplateButton.CallButton
             * @static
             * @param {proto.TemplateButton.ICallButton} message CallButton message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CallButton.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText"))
                    $root.proto.Message.HighlyStructuredMessage.encode(message.displayText, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.phoneNumber != null && Object.hasOwnProperty.call(message, "phoneNumber"))
                    $root.proto.Message.HighlyStructuredMessage.encode(message.phoneNumber, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified CallButton message, length delimited. Does not implicitly {@link proto.TemplateButton.CallButton.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.TemplateButton.CallButton
             * @static
             * @param {proto.TemplateButton.ICallButton} message CallButton message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            CallButton.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a CallButton message from the specified reader or buffer.
             * @function decode
             * @memberof proto.TemplateButton.CallButton
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.TemplateButton.CallButton} CallButton
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CallButton.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.displayText = $root.proto.Message.HighlyStructuredMessage.decode(reader, reader.uint32(), undefined, q + 1, message.displayText);
                            message._displayText = "displayText";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.phoneNumber = $root.proto.Message.HighlyStructuredMessage.decode(reader, reader.uint32(), undefined, q + 1, message.phoneNumber);
                            message._phoneNumber = "phoneNumber";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a CallButton message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.TemplateButton.CallButton
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.TemplateButton.CallButton} CallButton
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            CallButton.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a CallButton message.
             * @function verify
             * @memberof proto.TemplateButton.CallButton
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            CallButton.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText")) {
                    properties._displayText = 1;
                    {
                        var error = $root.proto.Message.HighlyStructuredMessage.verify(message.displayText, q + 1);
                        if (error)
                            return "displayText." + error;
                    }
                }
                if (message.phoneNumber != null && Object.hasOwnProperty.call(message, "phoneNumber")) {
                    properties._phoneNumber = 1;
                    {
                        var error = $root.proto.Message.HighlyStructuredMessage.verify(message.phoneNumber, q + 1);
                        if (error)
                            return "phoneNumber." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a CallButton message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.TemplateButton.CallButton
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.TemplateButton.CallButton} CallButton
             */
            CallButton.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.TemplateButton.CallButton: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.displayText != null) {
                    if (!$util.isObject(object.displayText))
                        throw TypeError(".proto.TemplateButton.CallButton.displayText: object expected");
                    message.displayText = $root.proto.Message.HighlyStructuredMessage.fromObject(object.displayText, q + 1);
                }
                if (object.phoneNumber != null) {
                    if (!$util.isObject(object.phoneNumber))
                        throw TypeError(".proto.TemplateButton.CallButton.phoneNumber: object expected");
                    message.phoneNumber = $root.proto.Message.HighlyStructuredMessage.fromObject(object.phoneNumber, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a CallButton message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.TemplateButton.CallButton
             * @static
             * @param {proto.TemplateButton.CallButton} message CallButton
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            CallButton.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText"))
                    object.displayText = $root.proto.Message.HighlyStructuredMessage.toObject(message.displayText, options, q + 1);
                if (message.phoneNumber != null && Object.hasOwnProperty.call(message, "phoneNumber"))
                    object.phoneNumber = $root.proto.Message.HighlyStructuredMessage.toObject(message.phoneNumber, options, q + 1);
                return object;
            };

            /**
             * Converts this CallButton to JSON.
             * @function toJSON
             * @memberof proto.TemplateButton.CallButton
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            CallButton.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for CallButton
             * @function getTypeUrl
             * @memberof proto.TemplateButton.CallButton
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            CallButton.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.TemplateButton.CallButton";
            };

            return CallButton;
        })();

        TemplateButton.QuickReplyButton = (function() {

            /**
             * Properties of a QuickReplyButton.
             * @memberof proto.TemplateButton
             * @interface IQuickReplyButton
             * @property {proto.Message.IHighlyStructuredMessage|null} [displayText] QuickReplyButton displayText
             * @property {string|null} [id] QuickReplyButton id
             */

            /**
             * Constructs a new QuickReplyButton.
             * @memberof proto.TemplateButton
             * @classdesc Represents a QuickReplyButton.
             * @implements IQuickReplyButton
             * @constructor
             * @param {proto.TemplateButton.IQuickReplyButton=} [properties] Properties to set
             */
            function QuickReplyButton(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * QuickReplyButton displayText.
             * @member {proto.Message.IHighlyStructuredMessage|null|undefined} displayText
             * @memberof proto.TemplateButton.QuickReplyButton
             * @instance
             */
            QuickReplyButton.prototype.displayText = null;

            /**
             * QuickReplyButton id.
             * @member {string|null|undefined} id
             * @memberof proto.TemplateButton.QuickReplyButton
             * @instance
             */
            QuickReplyButton.prototype.id = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(QuickReplyButton.prototype, "_displayText", {
                get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(QuickReplyButton.prototype, "_id", {
                get: $util.oneOfGetter($oneOfFields = ["id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new QuickReplyButton instance using the specified properties.
             * @function create
             * @memberof proto.TemplateButton.QuickReplyButton
             * @static
             * @param {proto.TemplateButton.IQuickReplyButton=} [properties] Properties to set
             * @returns {proto.TemplateButton.QuickReplyButton} QuickReplyButton instance
             */
            QuickReplyButton.create = function create(properties) {
                return new QuickReplyButton(properties);
            };

            /**
             * Encodes the specified QuickReplyButton message. Does not implicitly {@link proto.TemplateButton.QuickReplyButton.verify|verify} messages.
             * @function encode
             * @memberof proto.TemplateButton.QuickReplyButton
             * @static
             * @param {proto.TemplateButton.IQuickReplyButton} message QuickReplyButton message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            QuickReplyButton.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText"))
                    $root.proto.Message.HighlyStructuredMessage.encode(message.displayText, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.id);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified QuickReplyButton message, length delimited. Does not implicitly {@link proto.TemplateButton.QuickReplyButton.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.TemplateButton.QuickReplyButton
             * @static
             * @param {proto.TemplateButton.IQuickReplyButton} message QuickReplyButton message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            QuickReplyButton.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a QuickReplyButton message from the specified reader or buffer.
             * @function decode
             * @memberof proto.TemplateButton.QuickReplyButton
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.TemplateButton.QuickReplyButton} QuickReplyButton
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            QuickReplyButton.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.displayText = $root.proto.Message.HighlyStructuredMessage.decode(reader, reader.uint32(), undefined, q + 1, message.displayText);
                            message._displayText = "displayText";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.id = reader.stringVerify();
                            message._id = "id";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a QuickReplyButton message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.TemplateButton.QuickReplyButton
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.TemplateButton.QuickReplyButton} QuickReplyButton
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            QuickReplyButton.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a QuickReplyButton message.
             * @function verify
             * @memberof proto.TemplateButton.QuickReplyButton
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            QuickReplyButton.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText")) {
                    properties._displayText = 1;
                    {
                        var error = $root.proto.Message.HighlyStructuredMessage.verify(message.displayText, q + 1);
                        if (error)
                            return "displayText." + error;
                    }
                }
                if (message.id != null && Object.hasOwnProperty.call(message, "id")) {
                    properties._id = 1;
                    if (!$util.isString(message.id))
                        return "id: string expected";
                }
                return null;
            };

            /**
             * Creates a QuickReplyButton message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.TemplateButton.QuickReplyButton
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.TemplateButton.QuickReplyButton} QuickReplyButton
             */
            QuickReplyButton.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.TemplateButton.QuickReplyButton: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.displayText != null) {
                    if (!$util.isObject(object.displayText))
                        throw TypeError(".proto.TemplateButton.QuickReplyButton.displayText: object expected");
                    message.displayText = $root.proto.Message.HighlyStructuredMessage.fromObject(object.displayText, q + 1);
                }
                if (object.id != null)
                    message.id = String(object.id);
                return message;
            };

            /**
             * Creates a plain object from a QuickReplyButton message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.TemplateButton.QuickReplyButton
             * @static
             * @param {proto.TemplateButton.QuickReplyButton} message QuickReplyButton
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            QuickReplyButton.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText"))
                    object.displayText = $root.proto.Message.HighlyStructuredMessage.toObject(message.displayText, options, q + 1);
                if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                    object.id = message.id;
                return object;
            };

            /**
             * Converts this QuickReplyButton to JSON.
             * @function toJSON
             * @memberof proto.TemplateButton.QuickReplyButton
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            QuickReplyButton.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for QuickReplyButton
             * @function getTypeUrl
             * @memberof proto.TemplateButton.QuickReplyButton
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            QuickReplyButton.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.TemplateButton.QuickReplyButton";
            };

            return QuickReplyButton;
        })();

        TemplateButton.URLButton = (function() {

            /**
             * Properties of a URLButton.
             * @memberof proto.TemplateButton
             * @interface IURLButton
             * @property {proto.Message.IHighlyStructuredMessage|null} [displayText] URLButton displayText
             * @property {proto.Message.IHighlyStructuredMessage|null} [url] URLButton url
             */

            /**
             * Constructs a new URLButton.
             * @memberof proto.TemplateButton
             * @classdesc Represents a URLButton.
             * @implements IURLButton
             * @constructor
             * @param {proto.TemplateButton.IURLButton=} [properties] Properties to set
             */
            function URLButton(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * URLButton displayText.
             * @member {proto.Message.IHighlyStructuredMessage|null|undefined} displayText
             * @memberof proto.TemplateButton.URLButton
             * @instance
             */
            URLButton.prototype.displayText = null;

            /**
             * URLButton url.
             * @member {proto.Message.IHighlyStructuredMessage|null|undefined} url
             * @memberof proto.TemplateButton.URLButton
             * @instance
             */
            URLButton.prototype.url = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(URLButton.prototype, "_displayText", {
                get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(URLButton.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new URLButton instance using the specified properties.
             * @function create
             * @memberof proto.TemplateButton.URLButton
             * @static
             * @param {proto.TemplateButton.IURLButton=} [properties] Properties to set
             * @returns {proto.TemplateButton.URLButton} URLButton instance
             */
            URLButton.create = function create(properties) {
                return new URLButton(properties);
            };

            /**
             * Encodes the specified URLButton message. Does not implicitly {@link proto.TemplateButton.URLButton.verify|verify} messages.
             * @function encode
             * @memberof proto.TemplateButton.URLButton
             * @static
             * @param {proto.TemplateButton.IURLButton} message URLButton message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            URLButton.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText"))
                    $root.proto.Message.HighlyStructuredMessage.encode(message.displayText, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    $root.proto.Message.HighlyStructuredMessage.encode(message.url, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified URLButton message, length delimited. Does not implicitly {@link proto.TemplateButton.URLButton.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.TemplateButton.URLButton
             * @static
             * @param {proto.TemplateButton.IURLButton} message URLButton message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            URLButton.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a URLButton message from the specified reader or buffer.
             * @function decode
             * @memberof proto.TemplateButton.URLButton
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.TemplateButton.URLButton} URLButton
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            URLButton.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.displayText = $root.proto.Message.HighlyStructuredMessage.decode(reader, reader.uint32(), undefined, q + 1, message.displayText);
                            message._displayText = "displayText";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.url = $root.proto.Message.HighlyStructuredMessage.decode(reader, reader.uint32(), undefined, q + 1, message.url);
                            message._url = "url";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a URLButton message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.TemplateButton.URLButton
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.TemplateButton.URLButton} URLButton
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            URLButton.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a URLButton message.
             * @function verify
             * @memberof proto.TemplateButton.URLButton
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            URLButton.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText")) {
                    properties._displayText = 1;
                    {
                        var error = $root.proto.Message.HighlyStructuredMessage.verify(message.displayText, q + 1);
                        if (error)
                            return "displayText." + error;
                    }
                }
                if (message.url != null && Object.hasOwnProperty.call(message, "url")) {
                    properties._url = 1;
                    {
                        var error = $root.proto.Message.HighlyStructuredMessage.verify(message.url, q + 1);
                        if (error)
                            return "url." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a URLButton message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.TemplateButton.URLButton
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.TemplateButton.URLButton} URLButton
             */
            URLButton.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.TemplateButton.URLButton: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.displayText != null) {
                    if (!$util.isObject(object.displayText))
                        throw TypeError(".proto.TemplateButton.URLButton.displayText: object expected");
                    message.displayText = $root.proto.Message.HighlyStructuredMessage.fromObject(object.displayText, q + 1);
                }
                if (object.url != null) {
                    if (!$util.isObject(object.url))
                        throw TypeError(".proto.TemplateButton.URLButton.url: object expected");
                    message.url = $root.proto.Message.HighlyStructuredMessage.fromObject(object.url, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a URLButton message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.TemplateButton.URLButton
             * @static
             * @param {proto.TemplateButton.URLButton} message URLButton
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            URLButton.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.displayText != null && Object.hasOwnProperty.call(message, "displayText"))
                    object.displayText = $root.proto.Message.HighlyStructuredMessage.toObject(message.displayText, options, q + 1);
                if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                    object.url = $root.proto.Message.HighlyStructuredMessage.toObject(message.url, options, q + 1);
                return object;
            };

            /**
             * Converts this URLButton to JSON.
             * @function toJSON
             * @memberof proto.TemplateButton.URLButton
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            URLButton.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for URLButton
             * @function getTypeUrl
             * @memberof proto.TemplateButton.URLButton
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            URLButton.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.TemplateButton.URLButton";
            };

            return URLButton;
        })();

        return TemplateButton;
    })();

    proto.ThreadID = (function() {

        /**
         * Properties of a ThreadID.
         * @memberof proto
         * @interface IThreadID
         * @property {proto.ThreadID.ThreadType|null} [threadType] ThreadID threadType
         * @property {proto.IMessageKey|null} [threadKey] ThreadID threadKey
         */

        /**
         * Constructs a new ThreadID.
         * @memberof proto
         * @classdesc Represents a ThreadID.
         * @implements IThreadID
         * @constructor
         * @param {proto.IThreadID=} [properties] Properties to set
         */
        function ThreadID(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ThreadID threadType.
         * @member {proto.ThreadID.ThreadType|null|undefined} threadType
         * @memberof proto.ThreadID
         * @instance
         */
        ThreadID.prototype.threadType = null;

        /**
         * ThreadID threadKey.
         * @member {proto.IMessageKey|null|undefined} threadKey
         * @memberof proto.ThreadID
         * @instance
         */
        ThreadID.prototype.threadKey = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ThreadID.prototype, "_threadType", {
            get: $util.oneOfGetter($oneOfFields = ["threadType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ThreadID.prototype, "_threadKey", {
            get: $util.oneOfGetter($oneOfFields = ["threadKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ThreadID instance using the specified properties.
         * @function create
         * @memberof proto.ThreadID
         * @static
         * @param {proto.IThreadID=} [properties] Properties to set
         * @returns {proto.ThreadID} ThreadID instance
         */
        ThreadID.create = function create(properties) {
            return new ThreadID(properties);
        };

        /**
         * Encodes the specified ThreadID message. Does not implicitly {@link proto.ThreadID.verify|verify} messages.
         * @function encode
         * @memberof proto.ThreadID
         * @static
         * @param {proto.IThreadID} message ThreadID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThreadID.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.threadType != null && Object.hasOwnProperty.call(message, "threadType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.threadType);
            if (message.threadKey != null && Object.hasOwnProperty.call(message, "threadKey"))
                $root.proto.MessageKey.encode(message.threadKey, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified ThreadID message, length delimited. Does not implicitly {@link proto.ThreadID.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.ThreadID
         * @static
         * @param {proto.IThreadID} message ThreadID message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThreadID.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ThreadID message from the specified reader or buffer.
         * @function decode
         * @memberof proto.ThreadID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.ThreadID} ThreadID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThreadID.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.threadType = reader.int32();
                        message._threadType = "threadType";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.threadKey = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.threadKey);
                        message._threadKey = "threadKey";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a ThreadID message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.ThreadID
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.ThreadID} ThreadID
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThreadID.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ThreadID message.
         * @function verify
         * @memberof proto.ThreadID
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ThreadID.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.threadType != null && Object.hasOwnProperty.call(message, "threadType")) {
                properties._threadType = 1;
                if (typeof message.threadType !== "number" || (message.threadType | 0) !== message.threadType)
                    return "threadType: enum value expected";
            }
            if (message.threadKey != null && Object.hasOwnProperty.call(message, "threadKey")) {
                properties._threadKey = 1;
                {
                    var error = $root.proto.MessageKey.verify(message.threadKey, q + 1);
                    if (error)
                        return "threadKey." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ThreadID message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.ThreadID
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.ThreadID} ThreadID
         */
        ThreadID.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.ThreadID: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.threadType) {
            case "UNKNOWN":
            case 0:
                message.threadType = 0;
                break;
            case "VIEW_REPLIES":
            case 1:
                message.threadType = 1;
                break;
            case "AI_THREAD":
            case 2:
                message.threadType = 2;
                break;
            default:
                if (typeof object.threadType === "number" && (object.threadType | 0) === object.threadType)
                    message.threadType = object.threadType;
            }
            if (object.threadKey != null) {
                if (!$util.isObject(object.threadKey))
                    throw TypeError(".proto.ThreadID.threadKey: object expected");
                message.threadKey = $root.proto.MessageKey.fromObject(object.threadKey, q + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a ThreadID message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.ThreadID
         * @static
         * @param {proto.ThreadID} message ThreadID
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ThreadID.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.threadType != null && Object.hasOwnProperty.call(message, "threadType"))
                object.threadType = options.enums === String ? $root.proto.ThreadID.ThreadType[message.threadType] === undefined ? message.threadType : $root.proto.ThreadID.ThreadType[message.threadType] : message.threadType;
            if (message.threadKey != null && Object.hasOwnProperty.call(message, "threadKey"))
                object.threadKey = $root.proto.MessageKey.toObject(message.threadKey, options, q + 1);
            return object;
        };

        /**
         * Converts this ThreadID to JSON.
         * @function toJSON
         * @memberof proto.ThreadID
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ThreadID.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ThreadID
         * @function getTypeUrl
         * @memberof proto.ThreadID
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ThreadID.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.ThreadID";
        };

        /**
         * ThreadType enum.
         * @name proto.ThreadID.ThreadType
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} VIEW_REPLIES=1 VIEW_REPLIES value
         * @property {number} AI_THREAD=2 AI_THREAD value
         */
        ThreadID.ThreadType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "VIEW_REPLIES"] = 1;
            values[valuesById[2] = "AI_THREAD"] = 2;
            return values;
        })();

        return ThreadID;
    })();

    proto.UrlTrackingMap = (function() {

        /**
         * Properties of an UrlTrackingMap.
         * @memberof proto
         * @interface IUrlTrackingMap
         * @property {Array.<proto.UrlTrackingMap.IUrlTrackingMapElement>|null} [urlTrackingMapElements] UrlTrackingMap urlTrackingMapElements
         */

        /**
         * Constructs a new UrlTrackingMap.
         * @memberof proto
         * @classdesc Represents an UrlTrackingMap.
         * @implements IUrlTrackingMap
         * @constructor
         * @param {proto.IUrlTrackingMap=} [properties] Properties to set
         */
        function UrlTrackingMap(properties) {
            this.urlTrackingMapElements = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UrlTrackingMap urlTrackingMapElements.
         * @member {Array.<proto.UrlTrackingMap.IUrlTrackingMapElement>} urlTrackingMapElements
         * @memberof proto.UrlTrackingMap
         * @instance
         */
        UrlTrackingMap.prototype.urlTrackingMapElements = $util.emptyArray;

        /**
         * Creates a new UrlTrackingMap instance using the specified properties.
         * @function create
         * @memberof proto.UrlTrackingMap
         * @static
         * @param {proto.IUrlTrackingMap=} [properties] Properties to set
         * @returns {proto.UrlTrackingMap} UrlTrackingMap instance
         */
        UrlTrackingMap.create = function create(properties) {
            return new UrlTrackingMap(properties);
        };

        /**
         * Encodes the specified UrlTrackingMap message. Does not implicitly {@link proto.UrlTrackingMap.verify|verify} messages.
         * @function encode
         * @memberof proto.UrlTrackingMap
         * @static
         * @param {proto.IUrlTrackingMap} message UrlTrackingMap message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UrlTrackingMap.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.urlTrackingMapElements != null && message.urlTrackingMapElements.length)
                for (var i = 0; i < message.urlTrackingMapElements.length; ++i)
                    $root.proto.UrlTrackingMap.UrlTrackingMapElement.encode(message.urlTrackingMapElements[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified UrlTrackingMap message, length delimited. Does not implicitly {@link proto.UrlTrackingMap.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.UrlTrackingMap
         * @static
         * @param {proto.IUrlTrackingMap} message UrlTrackingMap message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UrlTrackingMap.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an UrlTrackingMap message from the specified reader or buffer.
         * @function decode
         * @memberof proto.UrlTrackingMap
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.UrlTrackingMap} UrlTrackingMap
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UrlTrackingMap.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(message.urlTrackingMapElements && message.urlTrackingMapElements.length))
                            message.urlTrackingMapElements = [];
                        message.urlTrackingMapElements.push($root.proto.UrlTrackingMap.UrlTrackingMapElement.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes an UrlTrackingMap message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.UrlTrackingMap
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.UrlTrackingMap} UrlTrackingMap
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UrlTrackingMap.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an UrlTrackingMap message.
         * @function verify
         * @memberof proto.UrlTrackingMap
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UrlTrackingMap.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            if (message.urlTrackingMapElements != null && Object.hasOwnProperty.call(message, "urlTrackingMapElements")) {
                if (!Array.isArray(message.urlTrackingMapElements))
                    return "urlTrackingMapElements: array expected";
                for (var i = 0; i < message.urlTrackingMapElements.length; ++i) {
                    var error = $root.proto.UrlTrackingMap.UrlTrackingMapElement.verify(message.urlTrackingMapElements[i], q + 1);
                    if (error)
                        return "urlTrackingMapElements." + error;
                }
            }
            return null;
        };

        /**
         * Creates an UrlTrackingMap message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.UrlTrackingMap
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.UrlTrackingMap} UrlTrackingMap
         */
        UrlTrackingMap.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.UrlTrackingMap: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.urlTrackingMapElements) {
                if (!Array.isArray(object.urlTrackingMapElements))
                    throw TypeError(".proto.UrlTrackingMap.urlTrackingMapElements: array expected");
                message.urlTrackingMapElements = Array(object.urlTrackingMapElements.length);
                for (var i = 0; i < object.urlTrackingMapElements.length; ++i) {
                    if (!$util.isObject(object.urlTrackingMapElements[i]))
                        throw TypeError(".proto.UrlTrackingMap.urlTrackingMapElements: object expected");
                    message.urlTrackingMapElements[i] = $root.proto.UrlTrackingMap.UrlTrackingMapElement.fromObject(object.urlTrackingMapElements[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an UrlTrackingMap message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.UrlTrackingMap
         * @static
         * @param {proto.UrlTrackingMap} message UrlTrackingMap
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UrlTrackingMap.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.urlTrackingMapElements = [];
            if (message.urlTrackingMapElements && message.urlTrackingMapElements.length) {
                object.urlTrackingMapElements = Array(message.urlTrackingMapElements.length);
                for (var j = 0; j < message.urlTrackingMapElements.length; ++j)
                    object.urlTrackingMapElements[j] = $root.proto.UrlTrackingMap.UrlTrackingMapElement.toObject(message.urlTrackingMapElements[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this UrlTrackingMap to JSON.
         * @function toJSON
         * @memberof proto.UrlTrackingMap
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UrlTrackingMap.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UrlTrackingMap
         * @function getTypeUrl
         * @memberof proto.UrlTrackingMap
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UrlTrackingMap.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.UrlTrackingMap";
        };

        UrlTrackingMap.UrlTrackingMapElement = (function() {

            /**
             * Properties of an UrlTrackingMapElement.
             * @memberof proto.UrlTrackingMap
             * @interface IUrlTrackingMapElement
             * @property {string|null} [originalUrl] UrlTrackingMapElement originalUrl
             * @property {string|null} [unconsentedUsersUrl] UrlTrackingMapElement unconsentedUsersUrl
             * @property {string|null} [consentedUsersUrl] UrlTrackingMapElement consentedUsersUrl
             * @property {number|null} [cardIndex] UrlTrackingMapElement cardIndex
             */

            /**
             * Constructs a new UrlTrackingMapElement.
             * @memberof proto.UrlTrackingMap
             * @classdesc Represents an UrlTrackingMapElement.
             * @implements IUrlTrackingMapElement
             * @constructor
             * @param {proto.UrlTrackingMap.IUrlTrackingMapElement=} [properties] Properties to set
             */
            function UrlTrackingMapElement(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * UrlTrackingMapElement originalUrl.
             * @member {string|null|undefined} originalUrl
             * @memberof proto.UrlTrackingMap.UrlTrackingMapElement
             * @instance
             */
            UrlTrackingMapElement.prototype.originalUrl = null;

            /**
             * UrlTrackingMapElement unconsentedUsersUrl.
             * @member {string|null|undefined} unconsentedUsersUrl
             * @memberof proto.UrlTrackingMap.UrlTrackingMapElement
             * @instance
             */
            UrlTrackingMapElement.prototype.unconsentedUsersUrl = null;

            /**
             * UrlTrackingMapElement consentedUsersUrl.
             * @member {string|null|undefined} consentedUsersUrl
             * @memberof proto.UrlTrackingMap.UrlTrackingMapElement
             * @instance
             */
            UrlTrackingMapElement.prototype.consentedUsersUrl = null;

            /**
             * UrlTrackingMapElement cardIndex.
             * @member {number|null|undefined} cardIndex
             * @memberof proto.UrlTrackingMap.UrlTrackingMapElement
             * @instance
             */
            UrlTrackingMapElement.prototype.cardIndex = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UrlTrackingMapElement.prototype, "_originalUrl", {
                get: $util.oneOfGetter($oneOfFields = ["originalUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UrlTrackingMapElement.prototype, "_unconsentedUsersUrl", {
                get: $util.oneOfGetter($oneOfFields = ["unconsentedUsersUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UrlTrackingMapElement.prototype, "_consentedUsersUrl", {
                get: $util.oneOfGetter($oneOfFields = ["consentedUsersUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UrlTrackingMapElement.prototype, "_cardIndex", {
                get: $util.oneOfGetter($oneOfFields = ["cardIndex"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new UrlTrackingMapElement instance using the specified properties.
             * @function create
             * @memberof proto.UrlTrackingMap.UrlTrackingMapElement
             * @static
             * @param {proto.UrlTrackingMap.IUrlTrackingMapElement=} [properties] Properties to set
             * @returns {proto.UrlTrackingMap.UrlTrackingMapElement} UrlTrackingMapElement instance
             */
            UrlTrackingMapElement.create = function create(properties) {
                return new UrlTrackingMapElement(properties);
            };

            /**
             * Encodes the specified UrlTrackingMapElement message. Does not implicitly {@link proto.UrlTrackingMap.UrlTrackingMapElement.verify|verify} messages.
             * @function encode
             * @memberof proto.UrlTrackingMap.UrlTrackingMapElement
             * @static
             * @param {proto.UrlTrackingMap.IUrlTrackingMapElement} message UrlTrackingMapElement message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UrlTrackingMapElement.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.originalUrl != null && Object.hasOwnProperty.call(message, "originalUrl"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.originalUrl);
                if (message.unconsentedUsersUrl != null && Object.hasOwnProperty.call(message, "unconsentedUsersUrl"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.unconsentedUsersUrl);
                if (message.consentedUsersUrl != null && Object.hasOwnProperty.call(message, "consentedUsersUrl"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.consentedUsersUrl);
                if (message.cardIndex != null && Object.hasOwnProperty.call(message, "cardIndex"))
                    writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.cardIndex);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified UrlTrackingMapElement message, length delimited. Does not implicitly {@link proto.UrlTrackingMap.UrlTrackingMapElement.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.UrlTrackingMap.UrlTrackingMapElement
             * @static
             * @param {proto.UrlTrackingMap.IUrlTrackingMapElement} message UrlTrackingMapElement message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            UrlTrackingMapElement.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an UrlTrackingMapElement message from the specified reader or buffer.
             * @function decode
             * @memberof proto.UrlTrackingMap.UrlTrackingMapElement
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.UrlTrackingMap.UrlTrackingMapElement} UrlTrackingMapElement
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UrlTrackingMapElement.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.originalUrl = reader.stringVerify();
                            message._originalUrl = "originalUrl";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.unconsentedUsersUrl = reader.stringVerify();
                            message._unconsentedUsersUrl = "unconsentedUsersUrl";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            message.consentedUsersUrl = reader.stringVerify();
                            message._consentedUsersUrl = "consentedUsersUrl";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            message.cardIndex = reader.uint32();
                            message._cardIndex = "cardIndex";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes an UrlTrackingMapElement message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.UrlTrackingMap.UrlTrackingMapElement
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.UrlTrackingMap.UrlTrackingMapElement} UrlTrackingMapElement
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            UrlTrackingMapElement.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an UrlTrackingMapElement message.
             * @function verify
             * @memberof proto.UrlTrackingMap.UrlTrackingMapElement
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            UrlTrackingMapElement.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.originalUrl != null && Object.hasOwnProperty.call(message, "originalUrl")) {
                    properties._originalUrl = 1;
                    if (!$util.isString(message.originalUrl))
                        return "originalUrl: string expected";
                }
                if (message.unconsentedUsersUrl != null && Object.hasOwnProperty.call(message, "unconsentedUsersUrl")) {
                    properties._unconsentedUsersUrl = 1;
                    if (!$util.isString(message.unconsentedUsersUrl))
                        return "unconsentedUsersUrl: string expected";
                }
                if (message.consentedUsersUrl != null && Object.hasOwnProperty.call(message, "consentedUsersUrl")) {
                    properties._consentedUsersUrl = 1;
                    if (!$util.isString(message.consentedUsersUrl))
                        return "consentedUsersUrl: string expected";
                }
                if (message.cardIndex != null && Object.hasOwnProperty.call(message, "cardIndex")) {
                    properties._cardIndex = 1;
                    if (!$util.isInteger(message.cardIndex))
                        return "cardIndex: integer expected";
                }
                return null;
            };

            /**
             * Creates an UrlTrackingMapElement message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.UrlTrackingMap.UrlTrackingMapElement
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.UrlTrackingMap.UrlTrackingMapElement} UrlTrackingMapElement
             */
            UrlTrackingMapElement.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.UrlTrackingMap.UrlTrackingMapElement: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.originalUrl != null)
                    message.originalUrl = String(object.originalUrl);
                if (object.unconsentedUsersUrl != null)
                    message.unconsentedUsersUrl = String(object.unconsentedUsersUrl);
                if (object.consentedUsersUrl != null)
                    message.consentedUsersUrl = String(object.consentedUsersUrl);
                if (object.cardIndex != null)
                    message.cardIndex = object.cardIndex >>> 0;
                return message;
            };

            /**
             * Creates a plain object from an UrlTrackingMapElement message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.UrlTrackingMap.UrlTrackingMapElement
             * @static
             * @param {proto.UrlTrackingMap.UrlTrackingMapElement} message UrlTrackingMapElement
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            UrlTrackingMapElement.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.originalUrl != null && Object.hasOwnProperty.call(message, "originalUrl"))
                    object.originalUrl = message.originalUrl;
                if (message.unconsentedUsersUrl != null && Object.hasOwnProperty.call(message, "unconsentedUsersUrl"))
                    object.unconsentedUsersUrl = message.unconsentedUsersUrl;
                if (message.consentedUsersUrl != null && Object.hasOwnProperty.call(message, "consentedUsersUrl"))
                    object.consentedUsersUrl = message.consentedUsersUrl;
                if (message.cardIndex != null && Object.hasOwnProperty.call(message, "cardIndex"))
                    object.cardIndex = message.cardIndex;
                return object;
            };

            /**
             * Converts this UrlTrackingMapElement to JSON.
             * @function toJSON
             * @memberof proto.UrlTrackingMap.UrlTrackingMapElement
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            UrlTrackingMapElement.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for UrlTrackingMapElement
             * @function getTypeUrl
             * @memberof proto.UrlTrackingMap.UrlTrackingMapElement
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            UrlTrackingMapElement.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.UrlTrackingMap.UrlTrackingMapElement";
            };

            return UrlTrackingMapElement;
        })();

        return UrlTrackingMap;
    })();

    proto.UserPassword = (function() {

        /**
         * Properties of a UserPassword.
         * @memberof proto
         * @interface IUserPassword
         * @property {proto.UserPassword.Encoding|null} [encoding] UserPassword encoding
         * @property {proto.UserPassword.Transformer|null} [transformer] UserPassword transformer
         * @property {Array.<proto.UserPassword.ITransformerArg>|null} [transformerArg] UserPassword transformerArg
         * @property {Uint8Array|null} [transformedData] UserPassword transformedData
         */

        /**
         * Constructs a new UserPassword.
         * @memberof proto
         * @classdesc Represents a UserPassword.
         * @implements IUserPassword
         * @constructor
         * @param {proto.IUserPassword=} [properties] Properties to set
         */
        function UserPassword(properties) {
            this.transformerArg = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UserPassword encoding.
         * @member {proto.UserPassword.Encoding|null|undefined} encoding
         * @memberof proto.UserPassword
         * @instance
         */
        UserPassword.prototype.encoding = null;

        /**
         * UserPassword transformer.
         * @member {proto.UserPassword.Transformer|null|undefined} transformer
         * @memberof proto.UserPassword
         * @instance
         */
        UserPassword.prototype.transformer = null;

        /**
         * UserPassword transformerArg.
         * @member {Array.<proto.UserPassword.ITransformerArg>} transformerArg
         * @memberof proto.UserPassword
         * @instance
         */
        UserPassword.prototype.transformerArg = $util.emptyArray;

        /**
         * UserPassword transformedData.
         * @member {Uint8Array|null|undefined} transformedData
         * @memberof proto.UserPassword
         * @instance
         */
        UserPassword.prototype.transformedData = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(UserPassword.prototype, "_encoding", {
            get: $util.oneOfGetter($oneOfFields = ["encoding"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(UserPassword.prototype, "_transformer", {
            get: $util.oneOfGetter($oneOfFields = ["transformer"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(UserPassword.prototype, "_transformedData", {
            get: $util.oneOfGetter($oneOfFields = ["transformedData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new UserPassword instance using the specified properties.
         * @function create
         * @memberof proto.UserPassword
         * @static
         * @param {proto.IUserPassword=} [properties] Properties to set
         * @returns {proto.UserPassword} UserPassword instance
         */
        UserPassword.create = function create(properties) {
            return new UserPassword(properties);
        };

        /**
         * Encodes the specified UserPassword message. Does not implicitly {@link proto.UserPassword.verify|verify} messages.
         * @function encode
         * @memberof proto.UserPassword
         * @static
         * @param {proto.IUserPassword} message UserPassword message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserPassword.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encoding != null && Object.hasOwnProperty.call(message, "encoding"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.encoding);
            if (message.transformer != null && Object.hasOwnProperty.call(message, "transformer"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.transformer);
            if (message.transformerArg != null && message.transformerArg.length)
                for (var i = 0; i < message.transformerArg.length; ++i)
                    $root.proto.UserPassword.TransformerArg.encode(message.transformerArg[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.transformedData != null && Object.hasOwnProperty.call(message, "transformedData"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.transformedData);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified UserPassword message, length delimited. Does not implicitly {@link proto.UserPassword.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.UserPassword
         * @static
         * @param {proto.IUserPassword} message UserPassword message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserPassword.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a UserPassword message from the specified reader or buffer.
         * @function decode
         * @memberof proto.UserPassword
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.UserPassword} UserPassword
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserPassword.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.encoding = reader.int32();
                        message._encoding = "encoding";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.transformer = reader.int32();
                        message._transformer = "transformer";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        if (!(message.transformerArg && message.transformerArg.length))
                            message.transformerArg = [];
                        message.transformerArg.push($root.proto.UserPassword.TransformerArg.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        message.transformedData = reader.bytes();
                        message._transformedData = "transformedData";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a UserPassword message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.UserPassword
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.UserPassword} UserPassword
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserPassword.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a UserPassword message.
         * @function verify
         * @memberof proto.UserPassword
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UserPassword.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.encoding != null && Object.hasOwnProperty.call(message, "encoding")) {
                properties._encoding = 1;
                if (typeof message.encoding !== "number" || (message.encoding | 0) !== message.encoding)
                    return "encoding: enum value expected";
            }
            if (message.transformer != null && Object.hasOwnProperty.call(message, "transformer")) {
                properties._transformer = 1;
                if (typeof message.transformer !== "number" || (message.transformer | 0) !== message.transformer)
                    return "transformer: enum value expected";
            }
            if (message.transformerArg != null && Object.hasOwnProperty.call(message, "transformerArg")) {
                if (!Array.isArray(message.transformerArg))
                    return "transformerArg: array expected";
                for (var i = 0; i < message.transformerArg.length; ++i) {
                    var error = $root.proto.UserPassword.TransformerArg.verify(message.transformerArg[i], q + 1);
                    if (error)
                        return "transformerArg." + error;
                }
            }
            if (message.transformedData != null && Object.hasOwnProperty.call(message, "transformedData")) {
                properties._transformedData = 1;
                if (!(message.transformedData && typeof message.transformedData.length === "number" || $util.isString(message.transformedData)))
                    return "transformedData: buffer expected";
            }
            return null;
        };

        /**
         * Creates a UserPassword message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.UserPassword
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.UserPassword} UserPassword
         */
        UserPassword.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.UserPassword: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.encoding) {
            case "UTF8":
            case 0:
                message.encoding = 0;
                break;
            case "UTF8_BROKEN":
            case 1:
                message.encoding = 1;
                break;
            default:
                if (typeof object.encoding === "number" && (object.encoding | 0) === object.encoding)
                    message.encoding = object.encoding;
            }
            switch (object.transformer) {
            case "NONE":
            case 0:
                message.transformer = 0;
                break;
            case "PBKDF2_HMAC_SHA512":
            case 1:
                message.transformer = 1;
                break;
            case "PBKDF2_HMAC_SHA384":
            case 2:
                message.transformer = 2;
                break;
            default:
                if (typeof object.transformer === "number" && (object.transformer | 0) === object.transformer)
                    message.transformer = object.transformer;
            }
            if (object.transformerArg) {
                if (!Array.isArray(object.transformerArg))
                    throw TypeError(".proto.UserPassword.transformerArg: array expected");
                message.transformerArg = Array(object.transformerArg.length);
                for (var i = 0; i < object.transformerArg.length; ++i) {
                    if (!$util.isObject(object.transformerArg[i]))
                        throw TypeError(".proto.UserPassword.transformerArg: object expected");
                    message.transformerArg[i] = $root.proto.UserPassword.TransformerArg.fromObject(object.transformerArg[i], q + 1);
                }
            }
            if (object.transformedData != null)
                if (typeof object.transformedData === "string")
                    $util.base64.decode(object.transformedData, message.transformedData = $util.newBuffer($util.base64.length(object.transformedData)), 0);
                else if (object.transformedData.length >= 0)
                    message.transformedData = object.transformedData;
            return message;
        };

        /**
         * Creates a plain object from a UserPassword message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.UserPassword
         * @static
         * @param {proto.UserPassword} message UserPassword
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UserPassword.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.transformerArg = [];
            if (message.encoding != null && Object.hasOwnProperty.call(message, "encoding"))
                object.encoding = options.enums === String ? $root.proto.UserPassword.Encoding[message.encoding] === undefined ? message.encoding : $root.proto.UserPassword.Encoding[message.encoding] : message.encoding;
            if (message.transformer != null && Object.hasOwnProperty.call(message, "transformer"))
                object.transformer = options.enums === String ? $root.proto.UserPassword.Transformer[message.transformer] === undefined ? message.transformer : $root.proto.UserPassword.Transformer[message.transformer] : message.transformer;
            if (message.transformerArg && message.transformerArg.length) {
                object.transformerArg = Array(message.transformerArg.length);
                for (var j = 0; j < message.transformerArg.length; ++j)
                    object.transformerArg[j] = $root.proto.UserPassword.TransformerArg.toObject(message.transformerArg[j], options, q + 1);
            }
            if (message.transformedData != null && Object.hasOwnProperty.call(message, "transformedData"))
                object.transformedData = options.bytes === String ? $util.base64.encode(message.transformedData, 0, message.transformedData.length) : options.bytes === Array ? Array.prototype.slice.call(message.transformedData) : message.transformedData;
            return object;
        };

        /**
         * Converts this UserPassword to JSON.
         * @function toJSON
         * @memberof proto.UserPassword
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UserPassword.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UserPassword
         * @function getTypeUrl
         * @memberof proto.UserPassword
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UserPassword.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.UserPassword";
        };

        /**
         * Encoding enum.
         * @name proto.UserPassword.Encoding
         * @enum {number}
         * @property {number} UTF8=0 UTF8 value
         * @property {number} UTF8_BROKEN=1 UTF8_BROKEN value
         */
        UserPassword.Encoding = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UTF8"] = 0;
            values[valuesById[1] = "UTF8_BROKEN"] = 1;
            return values;
        })();

        /**
         * Transformer enum.
         * @name proto.UserPassword.Transformer
         * @enum {number}
         * @property {number} NONE=0 NONE value
         * @property {number} PBKDF2_HMAC_SHA512=1 PBKDF2_HMAC_SHA512 value
         * @property {number} PBKDF2_HMAC_SHA384=2 PBKDF2_HMAC_SHA384 value
         */
        UserPassword.Transformer = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "NONE"] = 0;
            values[valuesById[1] = "PBKDF2_HMAC_SHA512"] = 1;
            values[valuesById[2] = "PBKDF2_HMAC_SHA384"] = 2;
            return values;
        })();

        UserPassword.TransformerArg = (function() {

            /**
             * Properties of a TransformerArg.
             * @memberof proto.UserPassword
             * @interface ITransformerArg
             * @property {string|null} [key] TransformerArg key
             * @property {proto.UserPassword.TransformerArg.IValue|null} [value] TransformerArg value
             */

            /**
             * Constructs a new TransformerArg.
             * @memberof proto.UserPassword
             * @classdesc Represents a TransformerArg.
             * @implements ITransformerArg
             * @constructor
             * @param {proto.UserPassword.ITransformerArg=} [properties] Properties to set
             */
            function TransformerArg(properties) {
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * TransformerArg key.
             * @member {string|null|undefined} key
             * @memberof proto.UserPassword.TransformerArg
             * @instance
             */
            TransformerArg.prototype.key = null;

            /**
             * TransformerArg value.
             * @member {proto.UserPassword.TransformerArg.IValue|null|undefined} value
             * @memberof proto.UserPassword.TransformerArg
             * @instance
             */
            TransformerArg.prototype.value = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(TransformerArg.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(TransformerArg.prototype, "_value", {
                get: $util.oneOfGetter($oneOfFields = ["value"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new TransformerArg instance using the specified properties.
             * @function create
             * @memberof proto.UserPassword.TransformerArg
             * @static
             * @param {proto.UserPassword.ITransformerArg=} [properties] Properties to set
             * @returns {proto.UserPassword.TransformerArg} TransformerArg instance
             */
            TransformerArg.create = function create(properties) {
                return new TransformerArg(properties);
            };

            /**
             * Encodes the specified TransformerArg message. Does not implicitly {@link proto.UserPassword.TransformerArg.verify|verify} messages.
             * @function encode
             * @memberof proto.UserPassword.TransformerArg
             * @static
             * @param {proto.UserPassword.ITransformerArg} message TransformerArg message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            TransformerArg.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
                if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                    $root.proto.UserPassword.TransformerArg.Value.encode(message.value, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified TransformerArg message, length delimited. Does not implicitly {@link proto.UserPassword.TransformerArg.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.UserPassword.TransformerArg
             * @static
             * @param {proto.UserPassword.ITransformerArg} message TransformerArg message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            TransformerArg.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a TransformerArg message from the specified reader or buffer.
             * @function decode
             * @memberof proto.UserPassword.TransformerArg
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.UserPassword.TransformerArg} TransformerArg
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            TransformerArg.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            message.key = reader.stringVerify();
                            message._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.value = $root.proto.UserPassword.TransformerArg.Value.decode(reader, reader.uint32(), undefined, q + 1, message.value);
                            message._value = "value";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a TransformerArg message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.UserPassword.TransformerArg
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.UserPassword.TransformerArg} TransformerArg
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            TransformerArg.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a TransformerArg message.
             * @function verify
             * @memberof proto.UserPassword.TransformerArg
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            TransformerArg.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                    properties._key = 1;
                    if (!$util.isString(message.key))
                        return "key: string expected";
                }
                if (message.value != null && Object.hasOwnProperty.call(message, "value")) {
                    properties._value = 1;
                    {
                        var error = $root.proto.UserPassword.TransformerArg.Value.verify(message.value, q + 1);
                        if (error)
                            return "value." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a TransformerArg message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.UserPassword.TransformerArg
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.UserPassword.TransformerArg} TransformerArg
             */
            TransformerArg.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.UserPassword.TransformerArg: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.key != null)
                    message.key = String(object.key);
                if (object.value != null) {
                    if (!$util.isObject(object.value))
                        throw TypeError(".proto.UserPassword.TransformerArg.value: object expected");
                    message.value = $root.proto.UserPassword.TransformerArg.Value.fromObject(object.value, q + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a TransformerArg message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.UserPassword.TransformerArg
             * @static
             * @param {proto.UserPassword.TransformerArg} message TransformerArg
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            TransformerArg.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                    object.key = message.key;
                if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                    object.value = $root.proto.UserPassword.TransformerArg.Value.toObject(message.value, options, q + 1);
                return object;
            };

            /**
             * Converts this TransformerArg to JSON.
             * @function toJSON
             * @memberof proto.UserPassword.TransformerArg
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            TransformerArg.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for TransformerArg
             * @function getTypeUrl
             * @memberof proto.UserPassword.TransformerArg
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            TransformerArg.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.UserPassword.TransformerArg";
            };

            TransformerArg.Value = (function() {

                /**
                 * Properties of a Value.
                 * @memberof proto.UserPassword.TransformerArg
                 * @interface IValue
                 * @property {Uint8Array|null} [asBlob] Value asBlob
                 * @property {number|null} [asUnsignedInteger] Value asUnsignedInteger
                 */

                /**
                 * Constructs a new Value.
                 * @memberof proto.UserPassword.TransformerArg
                 * @classdesc Represents a Value.
                 * @implements IValue
                 * @constructor
                 * @param {proto.UserPassword.TransformerArg.IValue=} [properties] Properties to set
                 */
                function Value(properties) {
                    if (properties)
                        for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * Value asBlob.
                 * @member {Uint8Array|null|undefined} asBlob
                 * @memberof proto.UserPassword.TransformerArg.Value
                 * @instance
                 */
                Value.prototype.asBlob = null;

                /**
                 * Value asUnsignedInteger.
                 * @member {number|null|undefined} asUnsignedInteger
                 * @memberof proto.UserPassword.TransformerArg.Value
                 * @instance
                 */
                Value.prototype.asUnsignedInteger = null;

                // OneOf field names bound to virtual getters and setters
                var $oneOfFields;

                /**
                 * Value value.
                 * @member {"asBlob"|"asUnsignedInteger"|undefined} value
                 * @memberof proto.UserPassword.TransformerArg.Value
                 * @instance
                 */
                Object.defineProperty(Value.prototype, "value", {
                    get: $util.oneOfGetter($oneOfFields = ["asBlob", "asUnsignedInteger"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                /**
                 * Creates a new Value instance using the specified properties.
                 * @function create
                 * @memberof proto.UserPassword.TransformerArg.Value
                 * @static
                 * @param {proto.UserPassword.TransformerArg.IValue=} [properties] Properties to set
                 * @returns {proto.UserPassword.TransformerArg.Value} Value instance
                 */
                Value.create = function create(properties) {
                    return new Value(properties);
                };

                /**
                 * Encodes the specified Value message. Does not implicitly {@link proto.UserPassword.TransformerArg.Value.verify|verify} messages.
                 * @function encode
                 * @memberof proto.UserPassword.TransformerArg.Value
                 * @static
                 * @param {proto.UserPassword.TransformerArg.IValue} message Value message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Value.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.asBlob != null && Object.hasOwnProperty.call(message, "asBlob"))
                        writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.asBlob);
                    if (message.asUnsignedInteger != null && Object.hasOwnProperty.call(message, "asUnsignedInteger"))
                        writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.asUnsignedInteger);
                    if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                        for (var i = 0; i < message.$unknowns.length; ++i)
                            writer.raw(message.$unknowns[i]);
                    return writer;
                };

                /**
                 * Encodes the specified Value message, length delimited. Does not implicitly {@link proto.UserPassword.TransformerArg.Value.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof proto.UserPassword.TransformerArg.Value
                 * @static
                 * @param {proto.UserPassword.TransformerArg.IValue} message Value message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                Value.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes a Value message from the specified reader or buffer.
                 * @function decode
                 * @memberof proto.UserPassword.TransformerArg.Value
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {proto.UserPassword.TransformerArg.Value} Value
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Value.decode = function decode(reader, length, z, q, g) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (q === undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw Error("max depth exceeded");
                    var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                    while (reader.pos < end) {
                        var s = reader.pos;
                        var tag = reader.tag();
                        if (tag === z) {
                            z = undefined;
                            break;
                        }
                        var u = tag & 7;
                        switch (tag >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                message.asBlob = reader.bytes();
                                message.value = "asBlob";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                message.asUnsignedInteger = reader.uint32();
                                message.value = "asUnsignedInteger";
                                continue;
                            }
                        }
                        reader.skipType(u, q, tag);
                        if (!reader.discardUnknown) {
                            $util.makeProp(message, "$unknowns", false);
                            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                        }
                    }
                    if (z !== undefined)
                        throw Error("missing end group");
                    return message;
                };

                /**
                 * Decodes a Value message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof proto.UserPassword.TransformerArg.Value
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {proto.UserPassword.TransformerArg.Value} Value
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                Value.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies a Value message.
                 * @function verify
                 * @memberof proto.UserPassword.TransformerArg.Value
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                Value.verify = function verify(message, q) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        return "max depth exceeded";
                    var properties = {};
                    if (message.asBlob != null && Object.hasOwnProperty.call(message, "asBlob")) {
                        properties.value = 1;
                        if (!(message.asBlob && typeof message.asBlob.length === "number" || $util.isString(message.asBlob)))
                            return "asBlob: buffer expected";
                    }
                    if (message.asUnsignedInteger != null && Object.hasOwnProperty.call(message, "asUnsignedInteger")) {
                        if (properties.value === 1)
                            return "value: multiple values";
                        properties.value = 1;
                        if (!$util.isInteger(message.asUnsignedInteger))
                            return "asUnsignedInteger: integer expected";
                    }
                    return null;
                };

                /**
                 * Creates a Value message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof proto.UserPassword.TransformerArg.Value
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {proto.UserPassword.TransformerArg.Value} Value
                 */
                Value.fromObject = function fromObject(object, q) {
                    if (object instanceof C)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".proto.UserPassword.TransformerArg.Value: object expected");
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var message = new C();
                    if (object.asBlob != null)
                        if (typeof object.asBlob === "string")
                            $util.base64.decode(object.asBlob, message.asBlob = $util.newBuffer($util.base64.length(object.asBlob)), 0);
                        else if (object.asBlob.length >= 0)
                            message.asBlob = object.asBlob;
                    if (object.asUnsignedInteger != null)
                        message.asUnsignedInteger = object.asUnsignedInteger >>> 0;
                    return message;
                };

                /**
                 * Creates a plain object from a Value message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof proto.UserPassword.TransformerArg.Value
                 * @static
                 * @param {proto.UserPassword.TransformerArg.Value} message Value
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                Value.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    var object = {};
                    if (message.asBlob != null && Object.hasOwnProperty.call(message, "asBlob")) {
                        object.asBlob = options.bytes === String ? $util.base64.encode(message.asBlob, 0, message.asBlob.length) : options.bytes === Array ? Array.prototype.slice.call(message.asBlob) : message.asBlob;
                        if (options.oneofs)
                            object.value = "asBlob";
                    }
                    if (message.asUnsignedInteger != null && Object.hasOwnProperty.call(message, "asUnsignedInteger")) {
                        object.asUnsignedInteger = message.asUnsignedInteger;
                        if (options.oneofs)
                            object.value = "asUnsignedInteger";
                    }
                    return object;
                };

                /**
                 * Converts this Value to JSON.
                 * @function toJSON
                 * @memberof proto.UserPassword.TransformerArg.Value
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                Value.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for Value
                 * @function getTypeUrl
                 * @memberof proto.UserPassword.TransformerArg.Value
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                Value.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/proto.UserPassword.TransformerArg.Value";
                };

                return Value;
            })();

            return TransformerArg;
        })();

        return UserPassword;
    })();

    proto.UserReceipt = (function() {

        /**
         * Properties of a UserReceipt.
         * @memberof proto
         * @interface IUserReceipt
         * @property {string|null} [userJid] UserReceipt userJid
         * @property {number|Long|null} [receiptTimestamp] UserReceipt receiptTimestamp
         * @property {number|Long|null} [readTimestamp] UserReceipt readTimestamp
         * @property {number|Long|null} [playedTimestamp] UserReceipt playedTimestamp
         * @property {Array.<string>|null} [pendingDeviceJid] UserReceipt pendingDeviceJid
         * @property {Array.<string>|null} [deliveredDeviceJid] UserReceipt deliveredDeviceJid
         */

        /**
         * Constructs a new UserReceipt.
         * @memberof proto
         * @classdesc Represents a UserReceipt.
         * @implements IUserReceipt
         * @constructor
         * @param {proto.IUserReceipt=} [properties] Properties to set
         */
        function UserReceipt(properties) {
            this.pendingDeviceJid = [];
            this.deliveredDeviceJid = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UserReceipt userJid.
         * @member {string|null|undefined} userJid
         * @memberof proto.UserReceipt
         * @instance
         */
        UserReceipt.prototype.userJid = null;

        /**
         * UserReceipt receiptTimestamp.
         * @member {number|Long|null|undefined} receiptTimestamp
         * @memberof proto.UserReceipt
         * @instance
         */
        UserReceipt.prototype.receiptTimestamp = null;

        /**
         * UserReceipt readTimestamp.
         * @member {number|Long|null|undefined} readTimestamp
         * @memberof proto.UserReceipt
         * @instance
         */
        UserReceipt.prototype.readTimestamp = null;

        /**
         * UserReceipt playedTimestamp.
         * @member {number|Long|null|undefined} playedTimestamp
         * @memberof proto.UserReceipt
         * @instance
         */
        UserReceipt.prototype.playedTimestamp = null;

        /**
         * UserReceipt pendingDeviceJid.
         * @member {Array.<string>} pendingDeviceJid
         * @memberof proto.UserReceipt
         * @instance
         */
        UserReceipt.prototype.pendingDeviceJid = $util.emptyArray;

        /**
         * UserReceipt deliveredDeviceJid.
         * @member {Array.<string>} deliveredDeviceJid
         * @memberof proto.UserReceipt
         * @instance
         */
        UserReceipt.prototype.deliveredDeviceJid = $util.emptyArray;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(UserReceipt.prototype, "_userJid", {
            get: $util.oneOfGetter($oneOfFields = ["userJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(UserReceipt.prototype, "_receiptTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["receiptTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(UserReceipt.prototype, "_readTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["readTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(UserReceipt.prototype, "_playedTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["playedTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new UserReceipt instance using the specified properties.
         * @function create
         * @memberof proto.UserReceipt
         * @static
         * @param {proto.IUserReceipt=} [properties] Properties to set
         * @returns {proto.UserReceipt} UserReceipt instance
         */
        UserReceipt.create = function create(properties) {
            return new UserReceipt(properties);
        };

        /**
         * Encodes the specified UserReceipt message. Does not implicitly {@link proto.UserReceipt.verify|verify} messages.
         * @function encode
         * @memberof proto.UserReceipt
         * @static
         * @param {proto.IUserReceipt} message UserReceipt message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserReceipt.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.userJid != null && Object.hasOwnProperty.call(message, "userJid"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.userJid);
            if (message.receiptTimestamp != null && Object.hasOwnProperty.call(message, "receiptTimestamp"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.receiptTimestamp);
            if (message.readTimestamp != null && Object.hasOwnProperty.call(message, "readTimestamp"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.readTimestamp);
            if (message.playedTimestamp != null && Object.hasOwnProperty.call(message, "playedTimestamp"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.playedTimestamp);
            if (message.pendingDeviceJid != null && message.pendingDeviceJid.length)
                for (var i = 0; i < message.pendingDeviceJid.length; ++i)
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.pendingDeviceJid[i]);
            if (message.deliveredDeviceJid != null && message.deliveredDeviceJid.length)
                for (var i = 0; i < message.deliveredDeviceJid.length; ++i)
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.deliveredDeviceJid[i]);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified UserReceipt message, length delimited. Does not implicitly {@link proto.UserReceipt.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.UserReceipt
         * @static
         * @param {proto.IUserReceipt} message UserReceipt message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserReceipt.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a UserReceipt message from the specified reader or buffer.
         * @function decode
         * @memberof proto.UserReceipt
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.UserReceipt} UserReceipt
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserReceipt.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.userJid = reader.stringVerify();
                        message._userJid = "userJid";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.receiptTimestamp = reader.int64();
                        message._receiptTimestamp = "receiptTimestamp";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.readTimestamp = reader.int64();
                        message._readTimestamp = "readTimestamp";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.playedTimestamp = reader.int64();
                        message._playedTimestamp = "playedTimestamp";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        if (!(message.pendingDeviceJid && message.pendingDeviceJid.length))
                            message.pendingDeviceJid = [];
                        message.pendingDeviceJid.push(reader.stringVerify());
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        if (!(message.deliveredDeviceJid && message.deliveredDeviceJid.length))
                            message.deliveredDeviceJid = [];
                        message.deliveredDeviceJid.push(reader.stringVerify());
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a UserReceipt message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.UserReceipt
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.UserReceipt} UserReceipt
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserReceipt.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a UserReceipt message.
         * @function verify
         * @memberof proto.UserReceipt
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UserReceipt.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.userJid != null && Object.hasOwnProperty.call(message, "userJid")) {
                properties._userJid = 1;
                if (!$util.isString(message.userJid))
                    return "userJid: string expected";
            }
            if (message.receiptTimestamp != null && Object.hasOwnProperty.call(message, "receiptTimestamp")) {
                properties._receiptTimestamp = 1;
                if (!$util.isInteger(message.receiptTimestamp) && !(message.receiptTimestamp && $util.isInteger(message.receiptTimestamp.low) && $util.isInteger(message.receiptTimestamp.high)))
                    return "receiptTimestamp: integer|Long expected";
            }
            if (message.readTimestamp != null && Object.hasOwnProperty.call(message, "readTimestamp")) {
                properties._readTimestamp = 1;
                if (!$util.isInteger(message.readTimestamp) && !(message.readTimestamp && $util.isInteger(message.readTimestamp.low) && $util.isInteger(message.readTimestamp.high)))
                    return "readTimestamp: integer|Long expected";
            }
            if (message.playedTimestamp != null && Object.hasOwnProperty.call(message, "playedTimestamp")) {
                properties._playedTimestamp = 1;
                if (!$util.isInteger(message.playedTimestamp) && !(message.playedTimestamp && $util.isInteger(message.playedTimestamp.low) && $util.isInteger(message.playedTimestamp.high)))
                    return "playedTimestamp: integer|Long expected";
            }
            if (message.pendingDeviceJid != null && Object.hasOwnProperty.call(message, "pendingDeviceJid")) {
                if (!Array.isArray(message.pendingDeviceJid))
                    return "pendingDeviceJid: array expected";
                for (var i = 0; i < message.pendingDeviceJid.length; ++i)
                    if (!$util.isString(message.pendingDeviceJid[i]))
                        return "pendingDeviceJid: string[] expected";
            }
            if (message.deliveredDeviceJid != null && Object.hasOwnProperty.call(message, "deliveredDeviceJid")) {
                if (!Array.isArray(message.deliveredDeviceJid))
                    return "deliveredDeviceJid: array expected";
                for (var i = 0; i < message.deliveredDeviceJid.length; ++i)
                    if (!$util.isString(message.deliveredDeviceJid[i]))
                        return "deliveredDeviceJid: string[] expected";
            }
            return null;
        };

        /**
         * Creates a UserReceipt message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.UserReceipt
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.UserReceipt} UserReceipt
         */
        UserReceipt.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.UserReceipt: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.userJid != null)
                message.userJid = String(object.userJid);
            if (object.receiptTimestamp != null)
                if ($util.Long)
                    message.receiptTimestamp = $util.Long.fromValue(object.receiptTimestamp, false);
                else if (typeof object.receiptTimestamp === "string")
                    message.receiptTimestamp = parseInt(object.receiptTimestamp, 10);
                else if (typeof object.receiptTimestamp === "number")
                    message.receiptTimestamp = object.receiptTimestamp;
                else if (typeof object.receiptTimestamp === "object")
                    message.receiptTimestamp = new $util.LongBits(object.receiptTimestamp.low >>> 0, object.receiptTimestamp.high >>> 0).toNumber();
            if (object.readTimestamp != null)
                if ($util.Long)
                    message.readTimestamp = $util.Long.fromValue(object.readTimestamp, false);
                else if (typeof object.readTimestamp === "string")
                    message.readTimestamp = parseInt(object.readTimestamp, 10);
                else if (typeof object.readTimestamp === "number")
                    message.readTimestamp = object.readTimestamp;
                else if (typeof object.readTimestamp === "object")
                    message.readTimestamp = new $util.LongBits(object.readTimestamp.low >>> 0, object.readTimestamp.high >>> 0).toNumber();
            if (object.playedTimestamp != null)
                if ($util.Long)
                    message.playedTimestamp = $util.Long.fromValue(object.playedTimestamp, false);
                else if (typeof object.playedTimestamp === "string")
                    message.playedTimestamp = parseInt(object.playedTimestamp, 10);
                else if (typeof object.playedTimestamp === "number")
                    message.playedTimestamp = object.playedTimestamp;
                else if (typeof object.playedTimestamp === "object")
                    message.playedTimestamp = new $util.LongBits(object.playedTimestamp.low >>> 0, object.playedTimestamp.high >>> 0).toNumber();
            if (object.pendingDeviceJid) {
                if (!Array.isArray(object.pendingDeviceJid))
                    throw TypeError(".proto.UserReceipt.pendingDeviceJid: array expected");
                message.pendingDeviceJid = Array(object.pendingDeviceJid.length);
                for (var i = 0; i < object.pendingDeviceJid.length; ++i)
                    message.pendingDeviceJid[i] = String(object.pendingDeviceJid[i]);
            }
            if (object.deliveredDeviceJid) {
                if (!Array.isArray(object.deliveredDeviceJid))
                    throw TypeError(".proto.UserReceipt.deliveredDeviceJid: array expected");
                message.deliveredDeviceJid = Array(object.deliveredDeviceJid.length);
                for (var i = 0; i < object.deliveredDeviceJid.length; ++i)
                    message.deliveredDeviceJid[i] = String(object.deliveredDeviceJid[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a UserReceipt message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.UserReceipt
         * @static
         * @param {proto.UserReceipt} message UserReceipt
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UserReceipt.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults) {
                object.pendingDeviceJid = [];
                object.deliveredDeviceJid = [];
            }
            if (message.userJid != null && Object.hasOwnProperty.call(message, "userJid"))
                object.userJid = message.userJid;
            if (message.receiptTimestamp != null && Object.hasOwnProperty.call(message, "receiptTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.receiptTimestamp = typeof message.receiptTimestamp === "number" ? BigInt(message.receiptTimestamp) : $util.Long.fromBits(message.receiptTimestamp.low >>> 0, message.receiptTimestamp.high >>> 0, false).toBigInt();
                else if (typeof message.receiptTimestamp === "number")
                    object.receiptTimestamp = options.longs === String ? String(message.receiptTimestamp) : message.receiptTimestamp;
                else
                    object.receiptTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.receiptTimestamp) : options.longs === Number ? new $util.LongBits(message.receiptTimestamp.low >>> 0, message.receiptTimestamp.high >>> 0).toNumber() : message.receiptTimestamp;
            if (message.readTimestamp != null && Object.hasOwnProperty.call(message, "readTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.readTimestamp = typeof message.readTimestamp === "number" ? BigInt(message.readTimestamp) : $util.Long.fromBits(message.readTimestamp.low >>> 0, message.readTimestamp.high >>> 0, false).toBigInt();
                else if (typeof message.readTimestamp === "number")
                    object.readTimestamp = options.longs === String ? String(message.readTimestamp) : message.readTimestamp;
                else
                    object.readTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.readTimestamp) : options.longs === Number ? new $util.LongBits(message.readTimestamp.low >>> 0, message.readTimestamp.high >>> 0).toNumber() : message.readTimestamp;
            if (message.playedTimestamp != null && Object.hasOwnProperty.call(message, "playedTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.playedTimestamp = typeof message.playedTimestamp === "number" ? BigInt(message.playedTimestamp) : $util.Long.fromBits(message.playedTimestamp.low >>> 0, message.playedTimestamp.high >>> 0, false).toBigInt();
                else if (typeof message.playedTimestamp === "number")
                    object.playedTimestamp = options.longs === String ? String(message.playedTimestamp) : message.playedTimestamp;
                else
                    object.playedTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.playedTimestamp) : options.longs === Number ? new $util.LongBits(message.playedTimestamp.low >>> 0, message.playedTimestamp.high >>> 0).toNumber() : message.playedTimestamp;
            if (message.pendingDeviceJid && message.pendingDeviceJid.length) {
                object.pendingDeviceJid = Array(message.pendingDeviceJid.length);
                for (var j = 0; j < message.pendingDeviceJid.length; ++j)
                    object.pendingDeviceJid[j] = message.pendingDeviceJid[j];
            }
            if (message.deliveredDeviceJid && message.deliveredDeviceJid.length) {
                object.deliveredDeviceJid = Array(message.deliveredDeviceJid.length);
                for (var j = 0; j < message.deliveredDeviceJid.length; ++j)
                    object.deliveredDeviceJid[j] = message.deliveredDeviceJid[j];
            }
            return object;
        };

        /**
         * Converts this UserReceipt to JSON.
         * @function toJSON
         * @memberof proto.UserReceipt
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UserReceipt.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UserReceipt
         * @function getTypeUrl
         * @memberof proto.UserReceipt
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UserReceipt.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.UserReceipt";
        };

        return UserReceipt;
    })();

    proto.VerifiedNameCertificate = (function() {

        /**
         * Properties of a VerifiedNameCertificate.
         * @memberof proto
         * @interface IVerifiedNameCertificate
         * @property {Uint8Array|null} [details] VerifiedNameCertificate details
         * @property {Uint8Array|null} [signature] VerifiedNameCertificate signature
         * @property {Uint8Array|null} [serverSignature] VerifiedNameCertificate serverSignature
         */

        /**
         * Constructs a new VerifiedNameCertificate.
         * @memberof proto
         * @classdesc Represents a VerifiedNameCertificate.
         * @implements IVerifiedNameCertificate
         * @constructor
         * @param {proto.IVerifiedNameCertificate=} [properties] Properties to set
         */
        function VerifiedNameCertificate(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * VerifiedNameCertificate details.
         * @member {Uint8Array|null|undefined} details
         * @memberof proto.VerifiedNameCertificate
         * @instance
         */
        VerifiedNameCertificate.prototype.details = null;

        /**
         * VerifiedNameCertificate signature.
         * @member {Uint8Array|null|undefined} signature
         * @memberof proto.VerifiedNameCertificate
         * @instance
         */
        VerifiedNameCertificate.prototype.signature = null;

        /**
         * VerifiedNameCertificate serverSignature.
         * @member {Uint8Array|null|undefined} serverSignature
         * @memberof proto.VerifiedNameCertificate
         * @instance
         */
        VerifiedNameCertificate.prototype.serverSignature = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VerifiedNameCertificate.prototype, "_details", {
            get: $util.oneOfGetter($oneOfFields = ["details"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VerifiedNameCertificate.prototype, "_signature", {
            get: $util.oneOfGetter($oneOfFields = ["signature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VerifiedNameCertificate.prototype, "_serverSignature", {
            get: $util.oneOfGetter($oneOfFields = ["serverSignature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new VerifiedNameCertificate instance using the specified properties.
         * @function create
         * @memberof proto.VerifiedNameCertificate
         * @static
         * @param {proto.IVerifiedNameCertificate=} [properties] Properties to set
         * @returns {proto.VerifiedNameCertificate} VerifiedNameCertificate instance
         */
        VerifiedNameCertificate.create = function create(properties) {
            return new VerifiedNameCertificate(properties);
        };

        /**
         * Encodes the specified VerifiedNameCertificate message. Does not implicitly {@link proto.VerifiedNameCertificate.verify|verify} messages.
         * @function encode
         * @memberof proto.VerifiedNameCertificate
         * @static
         * @param {proto.IVerifiedNameCertificate} message VerifiedNameCertificate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VerifiedNameCertificate.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.details != null && Object.hasOwnProperty.call(message, "details"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.details);
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.signature);
            if (message.serverSignature != null && Object.hasOwnProperty.call(message, "serverSignature"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.serverSignature);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified VerifiedNameCertificate message, length delimited. Does not implicitly {@link proto.VerifiedNameCertificate.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.VerifiedNameCertificate
         * @static
         * @param {proto.IVerifiedNameCertificate} message VerifiedNameCertificate message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VerifiedNameCertificate.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a VerifiedNameCertificate message from the specified reader or buffer.
         * @function decode
         * @memberof proto.VerifiedNameCertificate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.VerifiedNameCertificate} VerifiedNameCertificate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VerifiedNameCertificate.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.details = reader.bytes();
                        message._details = "details";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.signature = reader.bytes();
                        message._signature = "signature";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        message.serverSignature = reader.bytes();
                        message._serverSignature = "serverSignature";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a VerifiedNameCertificate message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.VerifiedNameCertificate
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.VerifiedNameCertificate} VerifiedNameCertificate
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VerifiedNameCertificate.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VerifiedNameCertificate message.
         * @function verify
         * @memberof proto.VerifiedNameCertificate
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VerifiedNameCertificate.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.details != null && Object.hasOwnProperty.call(message, "details")) {
                properties._details = 1;
                if (!(message.details && typeof message.details.length === "number" || $util.isString(message.details)))
                    return "details: buffer expected";
            }
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature")) {
                properties._signature = 1;
                if (!(message.signature && typeof message.signature.length === "number" || $util.isString(message.signature)))
                    return "signature: buffer expected";
            }
            if (message.serverSignature != null && Object.hasOwnProperty.call(message, "serverSignature")) {
                properties._serverSignature = 1;
                if (!(message.serverSignature && typeof message.serverSignature.length === "number" || $util.isString(message.serverSignature)))
                    return "serverSignature: buffer expected";
            }
            return null;
        };

        /**
         * Creates a VerifiedNameCertificate message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.VerifiedNameCertificate
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.VerifiedNameCertificate} VerifiedNameCertificate
         */
        VerifiedNameCertificate.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.VerifiedNameCertificate: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.details != null)
                if (typeof object.details === "string")
                    $util.base64.decode(object.details, message.details = $util.newBuffer($util.base64.length(object.details)), 0);
                else if (object.details.length >= 0)
                    message.details = object.details;
            if (object.signature != null)
                if (typeof object.signature === "string")
                    $util.base64.decode(object.signature, message.signature = $util.newBuffer($util.base64.length(object.signature)), 0);
                else if (object.signature.length >= 0)
                    message.signature = object.signature;
            if (object.serverSignature != null)
                if (typeof object.serverSignature === "string")
                    $util.base64.decode(object.serverSignature, message.serverSignature = $util.newBuffer($util.base64.length(object.serverSignature)), 0);
                else if (object.serverSignature.length >= 0)
                    message.serverSignature = object.serverSignature;
            return message;
        };

        /**
         * Creates a plain object from a VerifiedNameCertificate message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.VerifiedNameCertificate
         * @static
         * @param {proto.VerifiedNameCertificate} message VerifiedNameCertificate
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VerifiedNameCertificate.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.details != null && Object.hasOwnProperty.call(message, "details"))
                object.details = options.bytes === String ? $util.base64.encode(message.details, 0, message.details.length) : options.bytes === Array ? Array.prototype.slice.call(message.details) : message.details;
            if (message.signature != null && Object.hasOwnProperty.call(message, "signature"))
                object.signature = options.bytes === String ? $util.base64.encode(message.signature, 0, message.signature.length) : options.bytes === Array ? Array.prototype.slice.call(message.signature) : message.signature;
            if (message.serverSignature != null && Object.hasOwnProperty.call(message, "serverSignature"))
                object.serverSignature = options.bytes === String ? $util.base64.encode(message.serverSignature, 0, message.serverSignature.length) : options.bytes === Array ? Array.prototype.slice.call(message.serverSignature) : message.serverSignature;
            return object;
        };

        /**
         * Converts this VerifiedNameCertificate to JSON.
         * @function toJSON
         * @memberof proto.VerifiedNameCertificate
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VerifiedNameCertificate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for VerifiedNameCertificate
         * @function getTypeUrl
         * @memberof proto.VerifiedNameCertificate
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        VerifiedNameCertificate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.VerifiedNameCertificate";
        };

        VerifiedNameCertificate.Details = (function() {

            /**
             * Properties of a Details.
             * @memberof proto.VerifiedNameCertificate
             * @interface IDetails
             * @property {number|Long|null} [serial] Details serial
             * @property {string|null} [issuer] Details issuer
             * @property {string|null} [verifiedName] Details verifiedName
             * @property {Array.<proto.ILocalizedName>|null} [localizedNames] Details localizedNames
             * @property {number|Long|null} [issueTime] Details issueTime
             */

            /**
             * Constructs a new Details.
             * @memberof proto.VerifiedNameCertificate
             * @classdesc Represents a Details.
             * @implements IDetails
             * @constructor
             * @param {proto.VerifiedNameCertificate.IDetails=} [properties] Properties to set
             */
            function Details(properties) {
                this.localizedNames = [];
                if (properties)
                    for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Details serial.
             * @member {number|Long|null|undefined} serial
             * @memberof proto.VerifiedNameCertificate.Details
             * @instance
             */
            Details.prototype.serial = null;

            /**
             * Details issuer.
             * @member {string|null|undefined} issuer
             * @memberof proto.VerifiedNameCertificate.Details
             * @instance
             */
            Details.prototype.issuer = null;

            /**
             * Details verifiedName.
             * @member {string|null|undefined} verifiedName
             * @memberof proto.VerifiedNameCertificate.Details
             * @instance
             */
            Details.prototype.verifiedName = null;

            /**
             * Details localizedNames.
             * @member {Array.<proto.ILocalizedName>} localizedNames
             * @memberof proto.VerifiedNameCertificate.Details
             * @instance
             */
            Details.prototype.localizedNames = $util.emptyArray;

            /**
             * Details issueTime.
             * @member {number|Long|null|undefined} issueTime
             * @memberof proto.VerifiedNameCertificate.Details
             * @instance
             */
            Details.prototype.issueTime = null;

            // OneOf field names bound to virtual getters and setters
            var $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Details.prototype, "_serial", {
                get: $util.oneOfGetter($oneOfFields = ["serial"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Details.prototype, "_issuer", {
                get: $util.oneOfGetter($oneOfFields = ["issuer"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Details.prototype, "_verifiedName", {
                get: $util.oneOfGetter($oneOfFields = ["verifiedName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Details.prototype, "_issueTime", {
                get: $util.oneOfGetter($oneOfFields = ["issueTime"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new Details instance using the specified properties.
             * @function create
             * @memberof proto.VerifiedNameCertificate.Details
             * @static
             * @param {proto.VerifiedNameCertificate.IDetails=} [properties] Properties to set
             * @returns {proto.VerifiedNameCertificate.Details} Details instance
             */
            Details.create = function create(properties) {
                return new Details(properties);
            };

            /**
             * Encodes the specified Details message. Does not implicitly {@link proto.VerifiedNameCertificate.Details.verify|verify} messages.
             * @function encode
             * @memberof proto.VerifiedNameCertificate.Details
             * @static
             * @param {proto.VerifiedNameCertificate.IDetails} message Details message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Details.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.serial != null && Object.hasOwnProperty.call(message, "serial"))
                    writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.serial);
                if (message.issuer != null && Object.hasOwnProperty.call(message, "issuer"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.issuer);
                if (message.verifiedName != null && Object.hasOwnProperty.call(message, "verifiedName"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.verifiedName);
                if (message.localizedNames != null && message.localizedNames.length)
                    for (var i = 0; i < message.localizedNames.length; ++i)
                        $root.proto.LocalizedName.encode(message.localizedNames[i], writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
                if (message.issueTime != null && Object.hasOwnProperty.call(message, "issueTime"))
                    writer.uint32(/* id 10, wireType 0 =*/80).uint64(message.issueTime);
                if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                    for (var i = 0; i < message.$unknowns.length; ++i)
                        writer.raw(message.$unknowns[i]);
                return writer;
            };

            /**
             * Encodes the specified Details message, length delimited. Does not implicitly {@link proto.VerifiedNameCertificate.Details.verify|verify} messages.
             * @function encodeDelimited
             * @memberof proto.VerifiedNameCertificate.Details
             * @static
             * @param {proto.VerifiedNameCertificate.IDetails} message Details message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Details.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a Details message from the specified reader or buffer.
             * @function decode
             * @memberof proto.VerifiedNameCertificate.Details
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {proto.VerifiedNameCertificate.Details} Details
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Details.decode = function decode(reader, length, z, q, g) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (q === undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw Error("max depth exceeded");
                var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
                while (reader.pos < end) {
                    var s = reader.pos;
                    var tag = reader.tag();
                    if (tag === z) {
                        z = undefined;
                        break;
                    }
                    var u = tag & 7;
                    switch (tag >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            message.serial = reader.uint64();
                            message._serial = "serial";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            message.issuer = reader.stringVerify();
                            message._issuer = "issuer";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            message.verifiedName = reader.stringVerify();
                            message._verifiedName = "verifiedName";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            if (!(message.localizedNames && message.localizedNames.length))
                                message.localizedNames = [];
                            message.localizedNames.push($root.proto.LocalizedName.decode(reader, reader.uint32(), undefined, q + 1));
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            message.issueTime = reader.uint64();
                            message._issueTime = "issueTime";
                            continue;
                        }
                    }
                    reader.skipType(u, q, tag);
                    if (!reader.discardUnknown) {
                        $util.makeProp(message, "$unknowns", false);
                        (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                    }
                }
                if (z !== undefined)
                    throw Error("missing end group");
                return message;
            };

            /**
             * Decodes a Details message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof proto.VerifiedNameCertificate.Details
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {proto.VerifiedNameCertificate.Details} Details
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Details.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Details message.
             * @function verify
             * @memberof proto.VerifiedNameCertificate.Details
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Details.verify = function verify(message, q) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    return "max depth exceeded";
                var properties = {};
                if (message.serial != null && Object.hasOwnProperty.call(message, "serial")) {
                    properties._serial = 1;
                    if (!$util.isInteger(message.serial) && !(message.serial && $util.isInteger(message.serial.low) && $util.isInteger(message.serial.high)))
                        return "serial: integer|Long expected";
                }
                if (message.issuer != null && Object.hasOwnProperty.call(message, "issuer")) {
                    properties._issuer = 1;
                    if (!$util.isString(message.issuer))
                        return "issuer: string expected";
                }
                if (message.verifiedName != null && Object.hasOwnProperty.call(message, "verifiedName")) {
                    properties._verifiedName = 1;
                    if (!$util.isString(message.verifiedName))
                        return "verifiedName: string expected";
                }
                if (message.localizedNames != null && Object.hasOwnProperty.call(message, "localizedNames")) {
                    if (!Array.isArray(message.localizedNames))
                        return "localizedNames: array expected";
                    for (var i = 0; i < message.localizedNames.length; ++i) {
                        var error = $root.proto.LocalizedName.verify(message.localizedNames[i], q + 1);
                        if (error)
                            return "localizedNames." + error;
                    }
                }
                if (message.issueTime != null && Object.hasOwnProperty.call(message, "issueTime")) {
                    properties._issueTime = 1;
                    if (!$util.isInteger(message.issueTime) && !(message.issueTime && $util.isInteger(message.issueTime.low) && $util.isInteger(message.issueTime.high)))
                        return "issueTime: integer|Long expected";
                }
                return null;
            };

            /**
             * Creates a Details message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof proto.VerifiedNameCertificate.Details
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {proto.VerifiedNameCertificate.Details} Details
             */
            Details.fromObject = function fromObject(object, q) {
                if (object instanceof C)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".proto.VerifiedNameCertificate.Details: object expected");
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var message = new C();
                if (object.serial != null)
                    if ($util.Long)
                        message.serial = $util.Long.fromValue(object.serial, true);
                    else if (typeof object.serial === "string")
                        message.serial = parseInt(object.serial, 10);
                    else if (typeof object.serial === "number")
                        message.serial = object.serial;
                    else if (typeof object.serial === "object")
                        message.serial = new $util.LongBits(object.serial.low >>> 0, object.serial.high >>> 0).toNumber(true);
                if (object.issuer != null)
                    message.issuer = String(object.issuer);
                if (object.verifiedName != null)
                    message.verifiedName = String(object.verifiedName);
                if (object.localizedNames) {
                    if (!Array.isArray(object.localizedNames))
                        throw TypeError(".proto.VerifiedNameCertificate.Details.localizedNames: array expected");
                    message.localizedNames = Array(object.localizedNames.length);
                    for (var i = 0; i < object.localizedNames.length; ++i) {
                        if (!$util.isObject(object.localizedNames[i]))
                            throw TypeError(".proto.VerifiedNameCertificate.Details.localizedNames: object expected");
                        message.localizedNames[i] = $root.proto.LocalizedName.fromObject(object.localizedNames[i], q + 1);
                    }
                }
                if (object.issueTime != null)
                    if ($util.Long)
                        message.issueTime = $util.Long.fromValue(object.issueTime, true);
                    else if (typeof object.issueTime === "string")
                        message.issueTime = parseInt(object.issueTime, 10);
                    else if (typeof object.issueTime === "number")
                        message.issueTime = object.issueTime;
                    else if (typeof object.issueTime === "object")
                        message.issueTime = new $util.LongBits(object.issueTime.low >>> 0, object.issueTime.high >>> 0).toNumber(true);
                return message;
            };

            /**
             * Creates a plain object from a Details message. Also converts values to other types if specified.
             * @function toObject
             * @memberof proto.VerifiedNameCertificate.Details
             * @static
             * @param {proto.VerifiedNameCertificate.Details} message Details
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Details.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                var object = {};
                if (options.arrays || options.defaults)
                    object.localizedNames = [];
                if (message.serial != null && Object.hasOwnProperty.call(message, "serial"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.serial = typeof message.serial === "number" ? BigInt(message.serial) : $util.Long.fromBits(message.serial.low >>> 0, message.serial.high >>> 0, true).toBigInt();
                    else if (typeof message.serial === "number")
                        object.serial = options.longs === String ? String(message.serial) : message.serial;
                    else
                        object.serial = options.longs === String ? $util.Long.prototype.toString.call(message.serial) : options.longs === Number ? new $util.LongBits(message.serial.low >>> 0, message.serial.high >>> 0).toNumber(true) : message.serial;
                if (message.issuer != null && Object.hasOwnProperty.call(message, "issuer"))
                    object.issuer = message.issuer;
                if (message.verifiedName != null && Object.hasOwnProperty.call(message, "verifiedName"))
                    object.verifiedName = message.verifiedName;
                if (message.localizedNames && message.localizedNames.length) {
                    object.localizedNames = Array(message.localizedNames.length);
                    for (var j = 0; j < message.localizedNames.length; ++j)
                        object.localizedNames[j] = $root.proto.LocalizedName.toObject(message.localizedNames[j], options, q + 1);
                }
                if (message.issueTime != null && Object.hasOwnProperty.call(message, "issueTime"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.issueTime = typeof message.issueTime === "number" ? BigInt(message.issueTime) : $util.Long.fromBits(message.issueTime.low >>> 0, message.issueTime.high >>> 0, true).toBigInt();
                    else if (typeof message.issueTime === "number")
                        object.issueTime = options.longs === String ? String(message.issueTime) : message.issueTime;
                    else
                        object.issueTime = options.longs === String ? $util.Long.prototype.toString.call(message.issueTime) : options.longs === Number ? new $util.LongBits(message.issueTime.low >>> 0, message.issueTime.high >>> 0).toNumber(true) : message.issueTime;
                return object;
            };

            /**
             * Converts this Details to JSON.
             * @function toJSON
             * @memberof proto.VerifiedNameCertificate.Details
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Details.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Details
             * @function getTypeUrl
             * @memberof proto.VerifiedNameCertificate.Details
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Details.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/proto.VerifiedNameCertificate.Details";
            };

            return Details;
        })();

        return VerifiedNameCertificate;
    })();

    proto.WallpaperSettings = (function() {

        /**
         * Properties of a WallpaperSettings.
         * @memberof proto
         * @interface IWallpaperSettings
         * @property {string|null} [filename] WallpaperSettings filename
         * @property {number|null} [opacity] WallpaperSettings opacity
         * @property {boolean|null} [isGenAi] WallpaperSettings isGenAi
         */

        /**
         * Constructs a new WallpaperSettings.
         * @memberof proto
         * @classdesc Represents a WallpaperSettings.
         * @implements IWallpaperSettings
         * @constructor
         * @param {proto.IWallpaperSettings=} [properties] Properties to set
         */
        function WallpaperSettings(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WallpaperSettings filename.
         * @member {string|null|undefined} filename
         * @memberof proto.WallpaperSettings
         * @instance
         */
        WallpaperSettings.prototype.filename = null;

        /**
         * WallpaperSettings opacity.
         * @member {number|null|undefined} opacity
         * @memberof proto.WallpaperSettings
         * @instance
         */
        WallpaperSettings.prototype.opacity = null;

        /**
         * WallpaperSettings isGenAi.
         * @member {boolean|null|undefined} isGenAi
         * @memberof proto.WallpaperSettings
         * @instance
         */
        WallpaperSettings.prototype.isGenAi = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WallpaperSettings.prototype, "_filename", {
            get: $util.oneOfGetter($oneOfFields = ["filename"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WallpaperSettings.prototype, "_opacity", {
            get: $util.oneOfGetter($oneOfFields = ["opacity"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WallpaperSettings.prototype, "_isGenAi", {
            get: $util.oneOfGetter($oneOfFields = ["isGenAi"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new WallpaperSettings instance using the specified properties.
         * @function create
         * @memberof proto.WallpaperSettings
         * @static
         * @param {proto.IWallpaperSettings=} [properties] Properties to set
         * @returns {proto.WallpaperSettings} WallpaperSettings instance
         */
        WallpaperSettings.create = function create(properties) {
            return new WallpaperSettings(properties);
        };

        /**
         * Encodes the specified WallpaperSettings message. Does not implicitly {@link proto.WallpaperSettings.verify|verify} messages.
         * @function encode
         * @memberof proto.WallpaperSettings
         * @static
         * @param {proto.IWallpaperSettings} message WallpaperSettings message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WallpaperSettings.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.filename);
            if (message.opacity != null && Object.hasOwnProperty.call(message, "opacity"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint32(message.opacity);
            if (message.isGenAi != null && Object.hasOwnProperty.call(message, "isGenAi"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.isGenAi);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified WallpaperSettings message, length delimited. Does not implicitly {@link proto.WallpaperSettings.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.WallpaperSettings
         * @static
         * @param {proto.IWallpaperSettings} message WallpaperSettings message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WallpaperSettings.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a WallpaperSettings message from the specified reader or buffer.
         * @function decode
         * @memberof proto.WallpaperSettings
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.WallpaperSettings} WallpaperSettings
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WallpaperSettings.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.filename = reader.stringVerify();
                        message._filename = "filename";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.opacity = reader.uint32();
                        message._opacity = "opacity";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.isGenAi = reader.bool();
                        message._isGenAi = "isGenAi";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a WallpaperSettings message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.WallpaperSettings
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.WallpaperSettings} WallpaperSettings
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WallpaperSettings.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WallpaperSettings message.
         * @function verify
         * @memberof proto.WallpaperSettings
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WallpaperSettings.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.filename != null && Object.hasOwnProperty.call(message, "filename")) {
                properties._filename = 1;
                if (!$util.isString(message.filename))
                    return "filename: string expected";
            }
            if (message.opacity != null && Object.hasOwnProperty.call(message, "opacity")) {
                properties._opacity = 1;
                if (!$util.isInteger(message.opacity))
                    return "opacity: integer expected";
            }
            if (message.isGenAi != null && Object.hasOwnProperty.call(message, "isGenAi")) {
                properties._isGenAi = 1;
                if (typeof message.isGenAi !== "boolean")
                    return "isGenAi: boolean expected";
            }
            return null;
        };

        /**
         * Creates a WallpaperSettings message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.WallpaperSettings
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.WallpaperSettings} WallpaperSettings
         */
        WallpaperSettings.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.WallpaperSettings: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.filename != null)
                message.filename = String(object.filename);
            if (object.opacity != null)
                message.opacity = object.opacity >>> 0;
            if (object.isGenAi != null)
                message.isGenAi = Boolean(object.isGenAi);
            return message;
        };

        /**
         * Creates a plain object from a WallpaperSettings message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.WallpaperSettings
         * @static
         * @param {proto.WallpaperSettings} message WallpaperSettings
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WallpaperSettings.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                object.filename = message.filename;
            if (message.opacity != null && Object.hasOwnProperty.call(message, "opacity"))
                object.opacity = message.opacity;
            if (message.isGenAi != null && Object.hasOwnProperty.call(message, "isGenAi"))
                object.isGenAi = message.isGenAi;
            return object;
        };

        /**
         * Converts this WallpaperSettings to JSON.
         * @function toJSON
         * @memberof proto.WallpaperSettings
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WallpaperSettings.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WallpaperSettings
         * @function getTypeUrl
         * @memberof proto.WallpaperSettings
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WallpaperSettings.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.WallpaperSettings";
        };

        return WallpaperSettings;
    })();

    proto.WebFeatures = (function() {

        /**
         * Properties of a WebFeatures.
         * @memberof proto
         * @interface IWebFeatures
         * @property {proto.WebFeatures.Flag|null} [labelsDisplay] WebFeatures labelsDisplay
         * @property {proto.WebFeatures.Flag|null} [voipIndividualOutgoing] WebFeatures voipIndividualOutgoing
         * @property {proto.WebFeatures.Flag|null} [groupsV3] WebFeatures groupsV3
         * @property {proto.WebFeatures.Flag|null} [groupsV3Create] WebFeatures groupsV3Create
         * @property {proto.WebFeatures.Flag|null} [changeNumberV2] WebFeatures changeNumberV2
         * @property {proto.WebFeatures.Flag|null} [queryStatusV3Thumbnail] WebFeatures queryStatusV3Thumbnail
         * @property {proto.WebFeatures.Flag|null} [liveLocations] WebFeatures liveLocations
         * @property {proto.WebFeatures.Flag|null} [queryVname] WebFeatures queryVname
         * @property {proto.WebFeatures.Flag|null} [voipIndividualIncoming] WebFeatures voipIndividualIncoming
         * @property {proto.WebFeatures.Flag|null} [quickRepliesQuery] WebFeatures quickRepliesQuery
         * @property {proto.WebFeatures.Flag|null} [payments] WebFeatures payments
         * @property {proto.WebFeatures.Flag|null} [stickerPackQuery] WebFeatures stickerPackQuery
         * @property {proto.WebFeatures.Flag|null} [liveLocationsFinal] WebFeatures liveLocationsFinal
         * @property {proto.WebFeatures.Flag|null} [labelsEdit] WebFeatures labelsEdit
         * @property {proto.WebFeatures.Flag|null} [mediaUpload] WebFeatures mediaUpload
         * @property {proto.WebFeatures.Flag|null} [mediaUploadRichQuickReplies] WebFeatures mediaUploadRichQuickReplies
         * @property {proto.WebFeatures.Flag|null} [vnameV2] WebFeatures vnameV2
         * @property {proto.WebFeatures.Flag|null} [videoPlaybackUrl] WebFeatures videoPlaybackUrl
         * @property {proto.WebFeatures.Flag|null} [statusRanking] WebFeatures statusRanking
         * @property {proto.WebFeatures.Flag|null} [voipIndividualVideo] WebFeatures voipIndividualVideo
         * @property {proto.WebFeatures.Flag|null} [thirdPartyStickers] WebFeatures thirdPartyStickers
         * @property {proto.WebFeatures.Flag|null} [frequentlyForwardedSetting] WebFeatures frequentlyForwardedSetting
         * @property {proto.WebFeatures.Flag|null} [groupsV4JoinPermission] WebFeatures groupsV4JoinPermission
         * @property {proto.WebFeatures.Flag|null} [recentStickers] WebFeatures recentStickers
         * @property {proto.WebFeatures.Flag|null} [catalog] WebFeatures catalog
         * @property {proto.WebFeatures.Flag|null} [starredStickers] WebFeatures starredStickers
         * @property {proto.WebFeatures.Flag|null} [voipGroupCall] WebFeatures voipGroupCall
         * @property {proto.WebFeatures.Flag|null} [templateMessage] WebFeatures templateMessage
         * @property {proto.WebFeatures.Flag|null} [templateMessageInteractivity] WebFeatures templateMessageInteractivity
         * @property {proto.WebFeatures.Flag|null} [ephemeralMessages] WebFeatures ephemeralMessages
         * @property {proto.WebFeatures.Flag|null} [e2ENotificationSync] WebFeatures e2ENotificationSync
         * @property {proto.WebFeatures.Flag|null} [recentStickersV2] WebFeatures recentStickersV2
         * @property {proto.WebFeatures.Flag|null} [recentStickersV3] WebFeatures recentStickersV3
         * @property {proto.WebFeatures.Flag|null} [userNotice] WebFeatures userNotice
         * @property {proto.WebFeatures.Flag|null} [support] WebFeatures support
         * @property {proto.WebFeatures.Flag|null} [groupUiiCleanup] WebFeatures groupUiiCleanup
         * @property {proto.WebFeatures.Flag|null} [groupDogfoodingInternalOnly] WebFeatures groupDogfoodingInternalOnly
         * @property {proto.WebFeatures.Flag|null} [settingsSync] WebFeatures settingsSync
         * @property {proto.WebFeatures.Flag|null} [archiveV2] WebFeatures archiveV2
         * @property {proto.WebFeatures.Flag|null} [ephemeralAllowGroupMembers] WebFeatures ephemeralAllowGroupMembers
         * @property {proto.WebFeatures.Flag|null} [ephemeral24HDuration] WebFeatures ephemeral24HDuration
         * @property {proto.WebFeatures.Flag|null} [mdForceUpgrade] WebFeatures mdForceUpgrade
         * @property {proto.WebFeatures.Flag|null} [disappearingMode] WebFeatures disappearingMode
         * @property {proto.WebFeatures.Flag|null} [externalMdOptInAvailable] WebFeatures externalMdOptInAvailable
         * @property {proto.WebFeatures.Flag|null} [noDeleteMessageTimeLimit] WebFeatures noDeleteMessageTimeLimit
         */

        /**
         * Constructs a new WebFeatures.
         * @memberof proto
         * @classdesc Represents a WebFeatures.
         * @implements IWebFeatures
         * @constructor
         * @param {proto.IWebFeatures=} [properties] Properties to set
         */
        function WebFeatures(properties) {
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WebFeatures labelsDisplay.
         * @member {proto.WebFeatures.Flag|null|undefined} labelsDisplay
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.labelsDisplay = null;

        /**
         * WebFeatures voipIndividualOutgoing.
         * @member {proto.WebFeatures.Flag|null|undefined} voipIndividualOutgoing
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.voipIndividualOutgoing = null;

        /**
         * WebFeatures groupsV3.
         * @member {proto.WebFeatures.Flag|null|undefined} groupsV3
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.groupsV3 = null;

        /**
         * WebFeatures groupsV3Create.
         * @member {proto.WebFeatures.Flag|null|undefined} groupsV3Create
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.groupsV3Create = null;

        /**
         * WebFeatures changeNumberV2.
         * @member {proto.WebFeatures.Flag|null|undefined} changeNumberV2
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.changeNumberV2 = null;

        /**
         * WebFeatures queryStatusV3Thumbnail.
         * @member {proto.WebFeatures.Flag|null|undefined} queryStatusV3Thumbnail
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.queryStatusV3Thumbnail = null;

        /**
         * WebFeatures liveLocations.
         * @member {proto.WebFeatures.Flag|null|undefined} liveLocations
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.liveLocations = null;

        /**
         * WebFeatures queryVname.
         * @member {proto.WebFeatures.Flag|null|undefined} queryVname
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.queryVname = null;

        /**
         * WebFeatures voipIndividualIncoming.
         * @member {proto.WebFeatures.Flag|null|undefined} voipIndividualIncoming
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.voipIndividualIncoming = null;

        /**
         * WebFeatures quickRepliesQuery.
         * @member {proto.WebFeatures.Flag|null|undefined} quickRepliesQuery
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.quickRepliesQuery = null;

        /**
         * WebFeatures payments.
         * @member {proto.WebFeatures.Flag|null|undefined} payments
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.payments = null;

        /**
         * WebFeatures stickerPackQuery.
         * @member {proto.WebFeatures.Flag|null|undefined} stickerPackQuery
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.stickerPackQuery = null;

        /**
         * WebFeatures liveLocationsFinal.
         * @member {proto.WebFeatures.Flag|null|undefined} liveLocationsFinal
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.liveLocationsFinal = null;

        /**
         * WebFeatures labelsEdit.
         * @member {proto.WebFeatures.Flag|null|undefined} labelsEdit
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.labelsEdit = null;

        /**
         * WebFeatures mediaUpload.
         * @member {proto.WebFeatures.Flag|null|undefined} mediaUpload
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.mediaUpload = null;

        /**
         * WebFeatures mediaUploadRichQuickReplies.
         * @member {proto.WebFeatures.Flag|null|undefined} mediaUploadRichQuickReplies
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.mediaUploadRichQuickReplies = null;

        /**
         * WebFeatures vnameV2.
         * @member {proto.WebFeatures.Flag|null|undefined} vnameV2
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.vnameV2 = null;

        /**
         * WebFeatures videoPlaybackUrl.
         * @member {proto.WebFeatures.Flag|null|undefined} videoPlaybackUrl
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.videoPlaybackUrl = null;

        /**
         * WebFeatures statusRanking.
         * @member {proto.WebFeatures.Flag|null|undefined} statusRanking
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.statusRanking = null;

        /**
         * WebFeatures voipIndividualVideo.
         * @member {proto.WebFeatures.Flag|null|undefined} voipIndividualVideo
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.voipIndividualVideo = null;

        /**
         * WebFeatures thirdPartyStickers.
         * @member {proto.WebFeatures.Flag|null|undefined} thirdPartyStickers
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.thirdPartyStickers = null;

        /**
         * WebFeatures frequentlyForwardedSetting.
         * @member {proto.WebFeatures.Flag|null|undefined} frequentlyForwardedSetting
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.frequentlyForwardedSetting = null;

        /**
         * WebFeatures groupsV4JoinPermission.
         * @member {proto.WebFeatures.Flag|null|undefined} groupsV4JoinPermission
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.groupsV4JoinPermission = null;

        /**
         * WebFeatures recentStickers.
         * @member {proto.WebFeatures.Flag|null|undefined} recentStickers
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.recentStickers = null;

        /**
         * WebFeatures catalog.
         * @member {proto.WebFeatures.Flag|null|undefined} catalog
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.catalog = null;

        /**
         * WebFeatures starredStickers.
         * @member {proto.WebFeatures.Flag|null|undefined} starredStickers
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.starredStickers = null;

        /**
         * WebFeatures voipGroupCall.
         * @member {proto.WebFeatures.Flag|null|undefined} voipGroupCall
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.voipGroupCall = null;

        /**
         * WebFeatures templateMessage.
         * @member {proto.WebFeatures.Flag|null|undefined} templateMessage
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.templateMessage = null;

        /**
         * WebFeatures templateMessageInteractivity.
         * @member {proto.WebFeatures.Flag|null|undefined} templateMessageInteractivity
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.templateMessageInteractivity = null;

        /**
         * WebFeatures ephemeralMessages.
         * @member {proto.WebFeatures.Flag|null|undefined} ephemeralMessages
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.ephemeralMessages = null;

        /**
         * WebFeatures e2ENotificationSync.
         * @member {proto.WebFeatures.Flag|null|undefined} e2ENotificationSync
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.e2ENotificationSync = null;

        /**
         * WebFeatures recentStickersV2.
         * @member {proto.WebFeatures.Flag|null|undefined} recentStickersV2
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.recentStickersV2 = null;

        /**
         * WebFeatures recentStickersV3.
         * @member {proto.WebFeatures.Flag|null|undefined} recentStickersV3
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.recentStickersV3 = null;

        /**
         * WebFeatures userNotice.
         * @member {proto.WebFeatures.Flag|null|undefined} userNotice
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.userNotice = null;

        /**
         * WebFeatures support.
         * @member {proto.WebFeatures.Flag|null|undefined} support
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.support = null;

        /**
         * WebFeatures groupUiiCleanup.
         * @member {proto.WebFeatures.Flag|null|undefined} groupUiiCleanup
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.groupUiiCleanup = null;

        /**
         * WebFeatures groupDogfoodingInternalOnly.
         * @member {proto.WebFeatures.Flag|null|undefined} groupDogfoodingInternalOnly
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.groupDogfoodingInternalOnly = null;

        /**
         * WebFeatures settingsSync.
         * @member {proto.WebFeatures.Flag|null|undefined} settingsSync
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.settingsSync = null;

        /**
         * WebFeatures archiveV2.
         * @member {proto.WebFeatures.Flag|null|undefined} archiveV2
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.archiveV2 = null;

        /**
         * WebFeatures ephemeralAllowGroupMembers.
         * @member {proto.WebFeatures.Flag|null|undefined} ephemeralAllowGroupMembers
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.ephemeralAllowGroupMembers = null;

        /**
         * WebFeatures ephemeral24HDuration.
         * @member {proto.WebFeatures.Flag|null|undefined} ephemeral24HDuration
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.ephemeral24HDuration = null;

        /**
         * WebFeatures mdForceUpgrade.
         * @member {proto.WebFeatures.Flag|null|undefined} mdForceUpgrade
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.mdForceUpgrade = null;

        /**
         * WebFeatures disappearingMode.
         * @member {proto.WebFeatures.Flag|null|undefined} disappearingMode
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.disappearingMode = null;

        /**
         * WebFeatures externalMdOptInAvailable.
         * @member {proto.WebFeatures.Flag|null|undefined} externalMdOptInAvailable
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.externalMdOptInAvailable = null;

        /**
         * WebFeatures noDeleteMessageTimeLimit.
         * @member {proto.WebFeatures.Flag|null|undefined} noDeleteMessageTimeLimit
         * @memberof proto.WebFeatures
         * @instance
         */
        WebFeatures.prototype.noDeleteMessageTimeLimit = null;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_labelsDisplay", {
            get: $util.oneOfGetter($oneOfFields = ["labelsDisplay"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_voipIndividualOutgoing", {
            get: $util.oneOfGetter($oneOfFields = ["voipIndividualOutgoing"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_groupsV3", {
            get: $util.oneOfGetter($oneOfFields = ["groupsV3"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_groupsV3Create", {
            get: $util.oneOfGetter($oneOfFields = ["groupsV3Create"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_changeNumberV2", {
            get: $util.oneOfGetter($oneOfFields = ["changeNumberV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_queryStatusV3Thumbnail", {
            get: $util.oneOfGetter($oneOfFields = ["queryStatusV3Thumbnail"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_liveLocations", {
            get: $util.oneOfGetter($oneOfFields = ["liveLocations"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_queryVname", {
            get: $util.oneOfGetter($oneOfFields = ["queryVname"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_voipIndividualIncoming", {
            get: $util.oneOfGetter($oneOfFields = ["voipIndividualIncoming"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_quickRepliesQuery", {
            get: $util.oneOfGetter($oneOfFields = ["quickRepliesQuery"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_payments", {
            get: $util.oneOfGetter($oneOfFields = ["payments"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_stickerPackQuery", {
            get: $util.oneOfGetter($oneOfFields = ["stickerPackQuery"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_liveLocationsFinal", {
            get: $util.oneOfGetter($oneOfFields = ["liveLocationsFinal"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_labelsEdit", {
            get: $util.oneOfGetter($oneOfFields = ["labelsEdit"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_mediaUpload", {
            get: $util.oneOfGetter($oneOfFields = ["mediaUpload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_mediaUploadRichQuickReplies", {
            get: $util.oneOfGetter($oneOfFields = ["mediaUploadRichQuickReplies"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_vnameV2", {
            get: $util.oneOfGetter($oneOfFields = ["vnameV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_videoPlaybackUrl", {
            get: $util.oneOfGetter($oneOfFields = ["videoPlaybackUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_statusRanking", {
            get: $util.oneOfGetter($oneOfFields = ["statusRanking"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_voipIndividualVideo", {
            get: $util.oneOfGetter($oneOfFields = ["voipIndividualVideo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_thirdPartyStickers", {
            get: $util.oneOfGetter($oneOfFields = ["thirdPartyStickers"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_frequentlyForwardedSetting", {
            get: $util.oneOfGetter($oneOfFields = ["frequentlyForwardedSetting"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_groupsV4JoinPermission", {
            get: $util.oneOfGetter($oneOfFields = ["groupsV4JoinPermission"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_recentStickers", {
            get: $util.oneOfGetter($oneOfFields = ["recentStickers"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_catalog", {
            get: $util.oneOfGetter($oneOfFields = ["catalog"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_starredStickers", {
            get: $util.oneOfGetter($oneOfFields = ["starredStickers"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_voipGroupCall", {
            get: $util.oneOfGetter($oneOfFields = ["voipGroupCall"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_templateMessage", {
            get: $util.oneOfGetter($oneOfFields = ["templateMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_templateMessageInteractivity", {
            get: $util.oneOfGetter($oneOfFields = ["templateMessageInteractivity"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_ephemeralMessages", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralMessages"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_e2ENotificationSync", {
            get: $util.oneOfGetter($oneOfFields = ["e2ENotificationSync"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_recentStickersV2", {
            get: $util.oneOfGetter($oneOfFields = ["recentStickersV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_recentStickersV3", {
            get: $util.oneOfGetter($oneOfFields = ["recentStickersV3"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_userNotice", {
            get: $util.oneOfGetter($oneOfFields = ["userNotice"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_support", {
            get: $util.oneOfGetter($oneOfFields = ["support"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_groupUiiCleanup", {
            get: $util.oneOfGetter($oneOfFields = ["groupUiiCleanup"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_groupDogfoodingInternalOnly", {
            get: $util.oneOfGetter($oneOfFields = ["groupDogfoodingInternalOnly"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_settingsSync", {
            get: $util.oneOfGetter($oneOfFields = ["settingsSync"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_archiveV2", {
            get: $util.oneOfGetter($oneOfFields = ["archiveV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_ephemeralAllowGroupMembers", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralAllowGroupMembers"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_ephemeral24HDuration", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeral24HDuration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_mdForceUpgrade", {
            get: $util.oneOfGetter($oneOfFields = ["mdForceUpgrade"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_disappearingMode", {
            get: $util.oneOfGetter($oneOfFields = ["disappearingMode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_externalMdOptInAvailable", {
            get: $util.oneOfGetter($oneOfFields = ["externalMdOptInAvailable"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_noDeleteMessageTimeLimit", {
            get: $util.oneOfGetter($oneOfFields = ["noDeleteMessageTimeLimit"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new WebFeatures instance using the specified properties.
         * @function create
         * @memberof proto.WebFeatures
         * @static
         * @param {proto.IWebFeatures=} [properties] Properties to set
         * @returns {proto.WebFeatures} WebFeatures instance
         */
        WebFeatures.create = function create(properties) {
            return new WebFeatures(properties);
        };

        /**
         * Encodes the specified WebFeatures message. Does not implicitly {@link proto.WebFeatures.verify|verify} messages.
         * @function encode
         * @memberof proto.WebFeatures
         * @static
         * @param {proto.IWebFeatures} message WebFeatures message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WebFeatures.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.labelsDisplay != null && Object.hasOwnProperty.call(message, "labelsDisplay"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.labelsDisplay);
            if (message.voipIndividualOutgoing != null && Object.hasOwnProperty.call(message, "voipIndividualOutgoing"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.voipIndividualOutgoing);
            if (message.groupsV3 != null && Object.hasOwnProperty.call(message, "groupsV3"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.groupsV3);
            if (message.groupsV3Create != null && Object.hasOwnProperty.call(message, "groupsV3Create"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.groupsV3Create);
            if (message.changeNumberV2 != null && Object.hasOwnProperty.call(message, "changeNumberV2"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.changeNumberV2);
            if (message.queryStatusV3Thumbnail != null && Object.hasOwnProperty.call(message, "queryStatusV3Thumbnail"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.queryStatusV3Thumbnail);
            if (message.liveLocations != null && Object.hasOwnProperty.call(message, "liveLocations"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.liveLocations);
            if (message.queryVname != null && Object.hasOwnProperty.call(message, "queryVname"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.queryVname);
            if (message.voipIndividualIncoming != null && Object.hasOwnProperty.call(message, "voipIndividualIncoming"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.voipIndividualIncoming);
            if (message.quickRepliesQuery != null && Object.hasOwnProperty.call(message, "quickRepliesQuery"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.quickRepliesQuery);
            if (message.payments != null && Object.hasOwnProperty.call(message, "payments"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.payments);
            if (message.stickerPackQuery != null && Object.hasOwnProperty.call(message, "stickerPackQuery"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.stickerPackQuery);
            if (message.liveLocationsFinal != null && Object.hasOwnProperty.call(message, "liveLocationsFinal"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.liveLocationsFinal);
            if (message.labelsEdit != null && Object.hasOwnProperty.call(message, "labelsEdit"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.labelsEdit);
            if (message.mediaUpload != null && Object.hasOwnProperty.call(message, "mediaUpload"))
                writer.uint32(/* id 15, wireType 0 =*/120).int32(message.mediaUpload);
            if (message.mediaUploadRichQuickReplies != null && Object.hasOwnProperty.call(message, "mediaUploadRichQuickReplies"))
                writer.uint32(/* id 18, wireType 0 =*/144).int32(message.mediaUploadRichQuickReplies);
            if (message.vnameV2 != null && Object.hasOwnProperty.call(message, "vnameV2"))
                writer.uint32(/* id 19, wireType 0 =*/152).int32(message.vnameV2);
            if (message.videoPlaybackUrl != null && Object.hasOwnProperty.call(message, "videoPlaybackUrl"))
                writer.uint32(/* id 20, wireType 0 =*/160).int32(message.videoPlaybackUrl);
            if (message.statusRanking != null && Object.hasOwnProperty.call(message, "statusRanking"))
                writer.uint32(/* id 21, wireType 0 =*/168).int32(message.statusRanking);
            if (message.voipIndividualVideo != null && Object.hasOwnProperty.call(message, "voipIndividualVideo"))
                writer.uint32(/* id 22, wireType 0 =*/176).int32(message.voipIndividualVideo);
            if (message.thirdPartyStickers != null && Object.hasOwnProperty.call(message, "thirdPartyStickers"))
                writer.uint32(/* id 23, wireType 0 =*/184).int32(message.thirdPartyStickers);
            if (message.frequentlyForwardedSetting != null && Object.hasOwnProperty.call(message, "frequentlyForwardedSetting"))
                writer.uint32(/* id 24, wireType 0 =*/192).int32(message.frequentlyForwardedSetting);
            if (message.groupsV4JoinPermission != null && Object.hasOwnProperty.call(message, "groupsV4JoinPermission"))
                writer.uint32(/* id 25, wireType 0 =*/200).int32(message.groupsV4JoinPermission);
            if (message.recentStickers != null && Object.hasOwnProperty.call(message, "recentStickers"))
                writer.uint32(/* id 26, wireType 0 =*/208).int32(message.recentStickers);
            if (message.catalog != null && Object.hasOwnProperty.call(message, "catalog"))
                writer.uint32(/* id 27, wireType 0 =*/216).int32(message.catalog);
            if (message.starredStickers != null && Object.hasOwnProperty.call(message, "starredStickers"))
                writer.uint32(/* id 28, wireType 0 =*/224).int32(message.starredStickers);
            if (message.voipGroupCall != null && Object.hasOwnProperty.call(message, "voipGroupCall"))
                writer.uint32(/* id 29, wireType 0 =*/232).int32(message.voipGroupCall);
            if (message.templateMessage != null && Object.hasOwnProperty.call(message, "templateMessage"))
                writer.uint32(/* id 30, wireType 0 =*/240).int32(message.templateMessage);
            if (message.templateMessageInteractivity != null && Object.hasOwnProperty.call(message, "templateMessageInteractivity"))
                writer.uint32(/* id 31, wireType 0 =*/248).int32(message.templateMessageInteractivity);
            if (message.ephemeralMessages != null && Object.hasOwnProperty.call(message, "ephemeralMessages"))
                writer.uint32(/* id 32, wireType 0 =*/256).int32(message.ephemeralMessages);
            if (message.e2ENotificationSync != null && Object.hasOwnProperty.call(message, "e2ENotificationSync"))
                writer.uint32(/* id 33, wireType 0 =*/264).int32(message.e2ENotificationSync);
            if (message.recentStickersV2 != null && Object.hasOwnProperty.call(message, "recentStickersV2"))
                writer.uint32(/* id 34, wireType 0 =*/272).int32(message.recentStickersV2);
            if (message.recentStickersV3 != null && Object.hasOwnProperty.call(message, "recentStickersV3"))
                writer.uint32(/* id 36, wireType 0 =*/288).int32(message.recentStickersV3);
            if (message.userNotice != null && Object.hasOwnProperty.call(message, "userNotice"))
                writer.uint32(/* id 37, wireType 0 =*/296).int32(message.userNotice);
            if (message.support != null && Object.hasOwnProperty.call(message, "support"))
                writer.uint32(/* id 39, wireType 0 =*/312).int32(message.support);
            if (message.groupUiiCleanup != null && Object.hasOwnProperty.call(message, "groupUiiCleanup"))
                writer.uint32(/* id 40, wireType 0 =*/320).int32(message.groupUiiCleanup);
            if (message.groupDogfoodingInternalOnly != null && Object.hasOwnProperty.call(message, "groupDogfoodingInternalOnly"))
                writer.uint32(/* id 41, wireType 0 =*/328).int32(message.groupDogfoodingInternalOnly);
            if (message.settingsSync != null && Object.hasOwnProperty.call(message, "settingsSync"))
                writer.uint32(/* id 42, wireType 0 =*/336).int32(message.settingsSync);
            if (message.archiveV2 != null && Object.hasOwnProperty.call(message, "archiveV2"))
                writer.uint32(/* id 43, wireType 0 =*/344).int32(message.archiveV2);
            if (message.ephemeralAllowGroupMembers != null && Object.hasOwnProperty.call(message, "ephemeralAllowGroupMembers"))
                writer.uint32(/* id 44, wireType 0 =*/352).int32(message.ephemeralAllowGroupMembers);
            if (message.ephemeral24HDuration != null && Object.hasOwnProperty.call(message, "ephemeral24HDuration"))
                writer.uint32(/* id 45, wireType 0 =*/360).int32(message.ephemeral24HDuration);
            if (message.mdForceUpgrade != null && Object.hasOwnProperty.call(message, "mdForceUpgrade"))
                writer.uint32(/* id 46, wireType 0 =*/368).int32(message.mdForceUpgrade);
            if (message.disappearingMode != null && Object.hasOwnProperty.call(message, "disappearingMode"))
                writer.uint32(/* id 47, wireType 0 =*/376).int32(message.disappearingMode);
            if (message.externalMdOptInAvailable != null && Object.hasOwnProperty.call(message, "externalMdOptInAvailable"))
                writer.uint32(/* id 48, wireType 0 =*/384).int32(message.externalMdOptInAvailable);
            if (message.noDeleteMessageTimeLimit != null && Object.hasOwnProperty.call(message, "noDeleteMessageTimeLimit"))
                writer.uint32(/* id 49, wireType 0 =*/392).int32(message.noDeleteMessageTimeLimit);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified WebFeatures message, length delimited. Does not implicitly {@link proto.WebFeatures.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.WebFeatures
         * @static
         * @param {proto.IWebFeatures} message WebFeatures message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WebFeatures.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a WebFeatures message from the specified reader or buffer.
         * @function decode
         * @memberof proto.WebFeatures
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.WebFeatures} WebFeatures
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WebFeatures.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        message.labelsDisplay = reader.int32();
                        message._labelsDisplay = "labelsDisplay";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        message.voipIndividualOutgoing = reader.int32();
                        message._voipIndividualOutgoing = "voipIndividualOutgoing";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.groupsV3 = reader.int32();
                        message._groupsV3 = "groupsV3";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.groupsV3Create = reader.int32();
                        message._groupsV3Create = "groupsV3Create";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        message.changeNumberV2 = reader.int32();
                        message._changeNumberV2 = "changeNumberV2";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        message.queryStatusV3Thumbnail = reader.int32();
                        message._queryStatusV3Thumbnail = "queryStatusV3Thumbnail";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        message.liveLocations = reader.int32();
                        message._liveLocations = "liveLocations";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        message.queryVname = reader.int32();
                        message._queryVname = "queryVname";
                        continue;
                    }
                case 9: {
                        if (u !== 0)
                            break;
                        message.voipIndividualIncoming = reader.int32();
                        message._voipIndividualIncoming = "voipIndividualIncoming";
                        continue;
                    }
                case 10: {
                        if (u !== 0)
                            break;
                        message.quickRepliesQuery = reader.int32();
                        message._quickRepliesQuery = "quickRepliesQuery";
                        continue;
                    }
                case 11: {
                        if (u !== 0)
                            break;
                        message.payments = reader.int32();
                        message._payments = "payments";
                        continue;
                    }
                case 12: {
                        if (u !== 0)
                            break;
                        message.stickerPackQuery = reader.int32();
                        message._stickerPackQuery = "stickerPackQuery";
                        continue;
                    }
                case 13: {
                        if (u !== 0)
                            break;
                        message.liveLocationsFinal = reader.int32();
                        message._liveLocationsFinal = "liveLocationsFinal";
                        continue;
                    }
                case 14: {
                        if (u !== 0)
                            break;
                        message.labelsEdit = reader.int32();
                        message._labelsEdit = "labelsEdit";
                        continue;
                    }
                case 15: {
                        if (u !== 0)
                            break;
                        message.mediaUpload = reader.int32();
                        message._mediaUpload = "mediaUpload";
                        continue;
                    }
                case 18: {
                        if (u !== 0)
                            break;
                        message.mediaUploadRichQuickReplies = reader.int32();
                        message._mediaUploadRichQuickReplies = "mediaUploadRichQuickReplies";
                        continue;
                    }
                case 19: {
                        if (u !== 0)
                            break;
                        message.vnameV2 = reader.int32();
                        message._vnameV2 = "vnameV2";
                        continue;
                    }
                case 20: {
                        if (u !== 0)
                            break;
                        message.videoPlaybackUrl = reader.int32();
                        message._videoPlaybackUrl = "videoPlaybackUrl";
                        continue;
                    }
                case 21: {
                        if (u !== 0)
                            break;
                        message.statusRanking = reader.int32();
                        message._statusRanking = "statusRanking";
                        continue;
                    }
                case 22: {
                        if (u !== 0)
                            break;
                        message.voipIndividualVideo = reader.int32();
                        message._voipIndividualVideo = "voipIndividualVideo";
                        continue;
                    }
                case 23: {
                        if (u !== 0)
                            break;
                        message.thirdPartyStickers = reader.int32();
                        message._thirdPartyStickers = "thirdPartyStickers";
                        continue;
                    }
                case 24: {
                        if (u !== 0)
                            break;
                        message.frequentlyForwardedSetting = reader.int32();
                        message._frequentlyForwardedSetting = "frequentlyForwardedSetting";
                        continue;
                    }
                case 25: {
                        if (u !== 0)
                            break;
                        message.groupsV4JoinPermission = reader.int32();
                        message._groupsV4JoinPermission = "groupsV4JoinPermission";
                        continue;
                    }
                case 26: {
                        if (u !== 0)
                            break;
                        message.recentStickers = reader.int32();
                        message._recentStickers = "recentStickers";
                        continue;
                    }
                case 27: {
                        if (u !== 0)
                            break;
                        message.catalog = reader.int32();
                        message._catalog = "catalog";
                        continue;
                    }
                case 28: {
                        if (u !== 0)
                            break;
                        message.starredStickers = reader.int32();
                        message._starredStickers = "starredStickers";
                        continue;
                    }
                case 29: {
                        if (u !== 0)
                            break;
                        message.voipGroupCall = reader.int32();
                        message._voipGroupCall = "voipGroupCall";
                        continue;
                    }
                case 30: {
                        if (u !== 0)
                            break;
                        message.templateMessage = reader.int32();
                        message._templateMessage = "templateMessage";
                        continue;
                    }
                case 31: {
                        if (u !== 0)
                            break;
                        message.templateMessageInteractivity = reader.int32();
                        message._templateMessageInteractivity = "templateMessageInteractivity";
                        continue;
                    }
                case 32: {
                        if (u !== 0)
                            break;
                        message.ephemeralMessages = reader.int32();
                        message._ephemeralMessages = "ephemeralMessages";
                        continue;
                    }
                case 33: {
                        if (u !== 0)
                            break;
                        message.e2ENotificationSync = reader.int32();
                        message._e2ENotificationSync = "e2ENotificationSync";
                        continue;
                    }
                case 34: {
                        if (u !== 0)
                            break;
                        message.recentStickersV2 = reader.int32();
                        message._recentStickersV2 = "recentStickersV2";
                        continue;
                    }
                case 36: {
                        if (u !== 0)
                            break;
                        message.recentStickersV3 = reader.int32();
                        message._recentStickersV3 = "recentStickersV3";
                        continue;
                    }
                case 37: {
                        if (u !== 0)
                            break;
                        message.userNotice = reader.int32();
                        message._userNotice = "userNotice";
                        continue;
                    }
                case 39: {
                        if (u !== 0)
                            break;
                        message.support = reader.int32();
                        message._support = "support";
                        continue;
                    }
                case 40: {
                        if (u !== 0)
                            break;
                        message.groupUiiCleanup = reader.int32();
                        message._groupUiiCleanup = "groupUiiCleanup";
                        continue;
                    }
                case 41: {
                        if (u !== 0)
                            break;
                        message.groupDogfoodingInternalOnly = reader.int32();
                        message._groupDogfoodingInternalOnly = "groupDogfoodingInternalOnly";
                        continue;
                    }
                case 42: {
                        if (u !== 0)
                            break;
                        message.settingsSync = reader.int32();
                        message._settingsSync = "settingsSync";
                        continue;
                    }
                case 43: {
                        if (u !== 0)
                            break;
                        message.archiveV2 = reader.int32();
                        message._archiveV2 = "archiveV2";
                        continue;
                    }
                case 44: {
                        if (u !== 0)
                            break;
                        message.ephemeralAllowGroupMembers = reader.int32();
                        message._ephemeralAllowGroupMembers = "ephemeralAllowGroupMembers";
                        continue;
                    }
                case 45: {
                        if (u !== 0)
                            break;
                        message.ephemeral24HDuration = reader.int32();
                        message._ephemeral24HDuration = "ephemeral24HDuration";
                        continue;
                    }
                case 46: {
                        if (u !== 0)
                            break;
                        message.mdForceUpgrade = reader.int32();
                        message._mdForceUpgrade = "mdForceUpgrade";
                        continue;
                    }
                case 47: {
                        if (u !== 0)
                            break;
                        message.disappearingMode = reader.int32();
                        message._disappearingMode = "disappearingMode";
                        continue;
                    }
                case 48: {
                        if (u !== 0)
                            break;
                        message.externalMdOptInAvailable = reader.int32();
                        message._externalMdOptInAvailable = "externalMdOptInAvailable";
                        continue;
                    }
                case 49: {
                        if (u !== 0)
                            break;
                        message.noDeleteMessageTimeLimit = reader.int32();
                        message._noDeleteMessageTimeLimit = "noDeleteMessageTimeLimit";
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a WebFeatures message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.WebFeatures
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.WebFeatures} WebFeatures
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WebFeatures.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WebFeatures message.
         * @function verify
         * @memberof proto.WebFeatures
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WebFeatures.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.labelsDisplay != null && Object.hasOwnProperty.call(message, "labelsDisplay")) {
                properties._labelsDisplay = 1;
                if (typeof message.labelsDisplay !== "number" || (message.labelsDisplay | 0) !== message.labelsDisplay)
                    return "labelsDisplay: enum value expected";
            }
            if (message.voipIndividualOutgoing != null && Object.hasOwnProperty.call(message, "voipIndividualOutgoing")) {
                properties._voipIndividualOutgoing = 1;
                if (typeof message.voipIndividualOutgoing !== "number" || (message.voipIndividualOutgoing | 0) !== message.voipIndividualOutgoing)
                    return "voipIndividualOutgoing: enum value expected";
            }
            if (message.groupsV3 != null && Object.hasOwnProperty.call(message, "groupsV3")) {
                properties._groupsV3 = 1;
                if (typeof message.groupsV3 !== "number" || (message.groupsV3 | 0) !== message.groupsV3)
                    return "groupsV3: enum value expected";
            }
            if (message.groupsV3Create != null && Object.hasOwnProperty.call(message, "groupsV3Create")) {
                properties._groupsV3Create = 1;
                if (typeof message.groupsV3Create !== "number" || (message.groupsV3Create | 0) !== message.groupsV3Create)
                    return "groupsV3Create: enum value expected";
            }
            if (message.changeNumberV2 != null && Object.hasOwnProperty.call(message, "changeNumberV2")) {
                properties._changeNumberV2 = 1;
                if (typeof message.changeNumberV2 !== "number" || (message.changeNumberV2 | 0) !== message.changeNumberV2)
                    return "changeNumberV2: enum value expected";
            }
            if (message.queryStatusV3Thumbnail != null && Object.hasOwnProperty.call(message, "queryStatusV3Thumbnail")) {
                properties._queryStatusV3Thumbnail = 1;
                if (typeof message.queryStatusV3Thumbnail !== "number" || (message.queryStatusV3Thumbnail | 0) !== message.queryStatusV3Thumbnail)
                    return "queryStatusV3Thumbnail: enum value expected";
            }
            if (message.liveLocations != null && Object.hasOwnProperty.call(message, "liveLocations")) {
                properties._liveLocations = 1;
                if (typeof message.liveLocations !== "number" || (message.liveLocations | 0) !== message.liveLocations)
                    return "liveLocations: enum value expected";
            }
            if (message.queryVname != null && Object.hasOwnProperty.call(message, "queryVname")) {
                properties._queryVname = 1;
                if (typeof message.queryVname !== "number" || (message.queryVname | 0) !== message.queryVname)
                    return "queryVname: enum value expected";
            }
            if (message.voipIndividualIncoming != null && Object.hasOwnProperty.call(message, "voipIndividualIncoming")) {
                properties._voipIndividualIncoming = 1;
                if (typeof message.voipIndividualIncoming !== "number" || (message.voipIndividualIncoming | 0) !== message.voipIndividualIncoming)
                    return "voipIndividualIncoming: enum value expected";
            }
            if (message.quickRepliesQuery != null && Object.hasOwnProperty.call(message, "quickRepliesQuery")) {
                properties._quickRepliesQuery = 1;
                if (typeof message.quickRepliesQuery !== "number" || (message.quickRepliesQuery | 0) !== message.quickRepliesQuery)
                    return "quickRepliesQuery: enum value expected";
            }
            if (message.payments != null && Object.hasOwnProperty.call(message, "payments")) {
                properties._payments = 1;
                if (typeof message.payments !== "number" || (message.payments | 0) !== message.payments)
                    return "payments: enum value expected";
            }
            if (message.stickerPackQuery != null && Object.hasOwnProperty.call(message, "stickerPackQuery")) {
                properties._stickerPackQuery = 1;
                if (typeof message.stickerPackQuery !== "number" || (message.stickerPackQuery | 0) !== message.stickerPackQuery)
                    return "stickerPackQuery: enum value expected";
            }
            if (message.liveLocationsFinal != null && Object.hasOwnProperty.call(message, "liveLocationsFinal")) {
                properties._liveLocationsFinal = 1;
                if (typeof message.liveLocationsFinal !== "number" || (message.liveLocationsFinal | 0) !== message.liveLocationsFinal)
                    return "liveLocationsFinal: enum value expected";
            }
            if (message.labelsEdit != null && Object.hasOwnProperty.call(message, "labelsEdit")) {
                properties._labelsEdit = 1;
                if (typeof message.labelsEdit !== "number" || (message.labelsEdit | 0) !== message.labelsEdit)
                    return "labelsEdit: enum value expected";
            }
            if (message.mediaUpload != null && Object.hasOwnProperty.call(message, "mediaUpload")) {
                properties._mediaUpload = 1;
                if (typeof message.mediaUpload !== "number" || (message.mediaUpload | 0) !== message.mediaUpload)
                    return "mediaUpload: enum value expected";
            }
            if (message.mediaUploadRichQuickReplies != null && Object.hasOwnProperty.call(message, "mediaUploadRichQuickReplies")) {
                properties._mediaUploadRichQuickReplies = 1;
                if (typeof message.mediaUploadRichQuickReplies !== "number" || (message.mediaUploadRichQuickReplies | 0) !== message.mediaUploadRichQuickReplies)
                    return "mediaUploadRichQuickReplies: enum value expected";
            }
            if (message.vnameV2 != null && Object.hasOwnProperty.call(message, "vnameV2")) {
                properties._vnameV2 = 1;
                if (typeof message.vnameV2 !== "number" || (message.vnameV2 | 0) !== message.vnameV2)
                    return "vnameV2: enum value expected";
            }
            if (message.videoPlaybackUrl != null && Object.hasOwnProperty.call(message, "videoPlaybackUrl")) {
                properties._videoPlaybackUrl = 1;
                if (typeof message.videoPlaybackUrl !== "number" || (message.videoPlaybackUrl | 0) !== message.videoPlaybackUrl)
                    return "videoPlaybackUrl: enum value expected";
            }
            if (message.statusRanking != null && Object.hasOwnProperty.call(message, "statusRanking")) {
                properties._statusRanking = 1;
                if (typeof message.statusRanking !== "number" || (message.statusRanking | 0) !== message.statusRanking)
                    return "statusRanking: enum value expected";
            }
            if (message.voipIndividualVideo != null && Object.hasOwnProperty.call(message, "voipIndividualVideo")) {
                properties._voipIndividualVideo = 1;
                if (typeof message.voipIndividualVideo !== "number" || (message.voipIndividualVideo | 0) !== message.voipIndividualVideo)
                    return "voipIndividualVideo: enum value expected";
            }
            if (message.thirdPartyStickers != null && Object.hasOwnProperty.call(message, "thirdPartyStickers")) {
                properties._thirdPartyStickers = 1;
                if (typeof message.thirdPartyStickers !== "number" || (message.thirdPartyStickers | 0) !== message.thirdPartyStickers)
                    return "thirdPartyStickers: enum value expected";
            }
            if (message.frequentlyForwardedSetting != null && Object.hasOwnProperty.call(message, "frequentlyForwardedSetting")) {
                properties._frequentlyForwardedSetting = 1;
                if (typeof message.frequentlyForwardedSetting !== "number" || (message.frequentlyForwardedSetting | 0) !== message.frequentlyForwardedSetting)
                    return "frequentlyForwardedSetting: enum value expected";
            }
            if (message.groupsV4JoinPermission != null && Object.hasOwnProperty.call(message, "groupsV4JoinPermission")) {
                properties._groupsV4JoinPermission = 1;
                if (typeof message.groupsV4JoinPermission !== "number" || (message.groupsV4JoinPermission | 0) !== message.groupsV4JoinPermission)
                    return "groupsV4JoinPermission: enum value expected";
            }
            if (message.recentStickers != null && Object.hasOwnProperty.call(message, "recentStickers")) {
                properties._recentStickers = 1;
                if (typeof message.recentStickers !== "number" || (message.recentStickers | 0) !== message.recentStickers)
                    return "recentStickers: enum value expected";
            }
            if (message.catalog != null && Object.hasOwnProperty.call(message, "catalog")) {
                properties._catalog = 1;
                if (typeof message.catalog !== "number" || (message.catalog | 0) !== message.catalog)
                    return "catalog: enum value expected";
            }
            if (message.starredStickers != null && Object.hasOwnProperty.call(message, "starredStickers")) {
                properties._starredStickers = 1;
                if (typeof message.starredStickers !== "number" || (message.starredStickers | 0) !== message.starredStickers)
                    return "starredStickers: enum value expected";
            }
            if (message.voipGroupCall != null && Object.hasOwnProperty.call(message, "voipGroupCall")) {
                properties._voipGroupCall = 1;
                if (typeof message.voipGroupCall !== "number" || (message.voipGroupCall | 0) !== message.voipGroupCall)
                    return "voipGroupCall: enum value expected";
            }
            if (message.templateMessage != null && Object.hasOwnProperty.call(message, "templateMessage")) {
                properties._templateMessage = 1;
                if (typeof message.templateMessage !== "number" || (message.templateMessage | 0) !== message.templateMessage)
                    return "templateMessage: enum value expected";
            }
            if (message.templateMessageInteractivity != null && Object.hasOwnProperty.call(message, "templateMessageInteractivity")) {
                properties._templateMessageInteractivity = 1;
                if (typeof message.templateMessageInteractivity !== "number" || (message.templateMessageInteractivity | 0) !== message.templateMessageInteractivity)
                    return "templateMessageInteractivity: enum value expected";
            }
            if (message.ephemeralMessages != null && Object.hasOwnProperty.call(message, "ephemeralMessages")) {
                properties._ephemeralMessages = 1;
                if (typeof message.ephemeralMessages !== "number" || (message.ephemeralMessages | 0) !== message.ephemeralMessages)
                    return "ephemeralMessages: enum value expected";
            }
            if (message.e2ENotificationSync != null && Object.hasOwnProperty.call(message, "e2ENotificationSync")) {
                properties._e2ENotificationSync = 1;
                if (typeof message.e2ENotificationSync !== "number" || (message.e2ENotificationSync | 0) !== message.e2ENotificationSync)
                    return "e2ENotificationSync: enum value expected";
            }
            if (message.recentStickersV2 != null && Object.hasOwnProperty.call(message, "recentStickersV2")) {
                properties._recentStickersV2 = 1;
                if (typeof message.recentStickersV2 !== "number" || (message.recentStickersV2 | 0) !== message.recentStickersV2)
                    return "recentStickersV2: enum value expected";
            }
            if (message.recentStickersV3 != null && Object.hasOwnProperty.call(message, "recentStickersV3")) {
                properties._recentStickersV3 = 1;
                if (typeof message.recentStickersV3 !== "number" || (message.recentStickersV3 | 0) !== message.recentStickersV3)
                    return "recentStickersV3: enum value expected";
            }
            if (message.userNotice != null && Object.hasOwnProperty.call(message, "userNotice")) {
                properties._userNotice = 1;
                if (typeof message.userNotice !== "number" || (message.userNotice | 0) !== message.userNotice)
                    return "userNotice: enum value expected";
            }
            if (message.support != null && Object.hasOwnProperty.call(message, "support")) {
                properties._support = 1;
                if (typeof message.support !== "number" || (message.support | 0) !== message.support)
                    return "support: enum value expected";
            }
            if (message.groupUiiCleanup != null && Object.hasOwnProperty.call(message, "groupUiiCleanup")) {
                properties._groupUiiCleanup = 1;
                if (typeof message.groupUiiCleanup !== "number" || (message.groupUiiCleanup | 0) !== message.groupUiiCleanup)
                    return "groupUiiCleanup: enum value expected";
            }
            if (message.groupDogfoodingInternalOnly != null && Object.hasOwnProperty.call(message, "groupDogfoodingInternalOnly")) {
                properties._groupDogfoodingInternalOnly = 1;
                if (typeof message.groupDogfoodingInternalOnly !== "number" || (message.groupDogfoodingInternalOnly | 0) !== message.groupDogfoodingInternalOnly)
                    return "groupDogfoodingInternalOnly: enum value expected";
            }
            if (message.settingsSync != null && Object.hasOwnProperty.call(message, "settingsSync")) {
                properties._settingsSync = 1;
                if (typeof message.settingsSync !== "number" || (message.settingsSync | 0) !== message.settingsSync)
                    return "settingsSync: enum value expected";
            }
            if (message.archiveV2 != null && Object.hasOwnProperty.call(message, "archiveV2")) {
                properties._archiveV2 = 1;
                if (typeof message.archiveV2 !== "number" || (message.archiveV2 | 0) !== message.archiveV2)
                    return "archiveV2: enum value expected";
            }
            if (message.ephemeralAllowGroupMembers != null && Object.hasOwnProperty.call(message, "ephemeralAllowGroupMembers")) {
                properties._ephemeralAllowGroupMembers = 1;
                if (typeof message.ephemeralAllowGroupMembers !== "number" || (message.ephemeralAllowGroupMembers | 0) !== message.ephemeralAllowGroupMembers)
                    return "ephemeralAllowGroupMembers: enum value expected";
            }
            if (message.ephemeral24HDuration != null && Object.hasOwnProperty.call(message, "ephemeral24HDuration")) {
                properties._ephemeral24HDuration = 1;
                if (typeof message.ephemeral24HDuration !== "number" || (message.ephemeral24HDuration | 0) !== message.ephemeral24HDuration)
                    return "ephemeral24HDuration: enum value expected";
            }
            if (message.mdForceUpgrade != null && Object.hasOwnProperty.call(message, "mdForceUpgrade")) {
                properties._mdForceUpgrade = 1;
                if (typeof message.mdForceUpgrade !== "number" || (message.mdForceUpgrade | 0) !== message.mdForceUpgrade)
                    return "mdForceUpgrade: enum value expected";
            }
            if (message.disappearingMode != null && Object.hasOwnProperty.call(message, "disappearingMode")) {
                properties._disappearingMode = 1;
                if (typeof message.disappearingMode !== "number" || (message.disappearingMode | 0) !== message.disappearingMode)
                    return "disappearingMode: enum value expected";
            }
            if (message.externalMdOptInAvailable != null && Object.hasOwnProperty.call(message, "externalMdOptInAvailable")) {
                properties._externalMdOptInAvailable = 1;
                if (typeof message.externalMdOptInAvailable !== "number" || (message.externalMdOptInAvailable | 0) !== message.externalMdOptInAvailable)
                    return "externalMdOptInAvailable: enum value expected";
            }
            if (message.noDeleteMessageTimeLimit != null && Object.hasOwnProperty.call(message, "noDeleteMessageTimeLimit")) {
                properties._noDeleteMessageTimeLimit = 1;
                if (typeof message.noDeleteMessageTimeLimit !== "number" || (message.noDeleteMessageTimeLimit | 0) !== message.noDeleteMessageTimeLimit)
                    return "noDeleteMessageTimeLimit: enum value expected";
            }
            return null;
        };

        /**
         * Creates a WebFeatures message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.WebFeatures
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.WebFeatures} WebFeatures
         */
        WebFeatures.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.WebFeatures: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            switch (object.labelsDisplay) {
            case "NOT_STARTED":
            case 0:
                message.labelsDisplay = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.labelsDisplay = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.labelsDisplay = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.labelsDisplay = 3;
                break;
            default:
                if (typeof object.labelsDisplay === "number" && (object.labelsDisplay | 0) === object.labelsDisplay)
                    message.labelsDisplay = object.labelsDisplay;
            }
            switch (object.voipIndividualOutgoing) {
            case "NOT_STARTED":
            case 0:
                message.voipIndividualOutgoing = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.voipIndividualOutgoing = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.voipIndividualOutgoing = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.voipIndividualOutgoing = 3;
                break;
            default:
                if (typeof object.voipIndividualOutgoing === "number" && (object.voipIndividualOutgoing | 0) === object.voipIndividualOutgoing)
                    message.voipIndividualOutgoing = object.voipIndividualOutgoing;
            }
            switch (object.groupsV3) {
            case "NOT_STARTED":
            case 0:
                message.groupsV3 = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.groupsV3 = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.groupsV3 = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.groupsV3 = 3;
                break;
            default:
                if (typeof object.groupsV3 === "number" && (object.groupsV3 | 0) === object.groupsV3)
                    message.groupsV3 = object.groupsV3;
            }
            switch (object.groupsV3Create) {
            case "NOT_STARTED":
            case 0:
                message.groupsV3Create = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.groupsV3Create = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.groupsV3Create = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.groupsV3Create = 3;
                break;
            default:
                if (typeof object.groupsV3Create === "number" && (object.groupsV3Create | 0) === object.groupsV3Create)
                    message.groupsV3Create = object.groupsV3Create;
            }
            switch (object.changeNumberV2) {
            case "NOT_STARTED":
            case 0:
                message.changeNumberV2 = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.changeNumberV2 = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.changeNumberV2 = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.changeNumberV2 = 3;
                break;
            default:
                if (typeof object.changeNumberV2 === "number" && (object.changeNumberV2 | 0) === object.changeNumberV2)
                    message.changeNumberV2 = object.changeNumberV2;
            }
            switch (object.queryStatusV3Thumbnail) {
            case "NOT_STARTED":
            case 0:
                message.queryStatusV3Thumbnail = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.queryStatusV3Thumbnail = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.queryStatusV3Thumbnail = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.queryStatusV3Thumbnail = 3;
                break;
            default:
                if (typeof object.queryStatusV3Thumbnail === "number" && (object.queryStatusV3Thumbnail | 0) === object.queryStatusV3Thumbnail)
                    message.queryStatusV3Thumbnail = object.queryStatusV3Thumbnail;
            }
            switch (object.liveLocations) {
            case "NOT_STARTED":
            case 0:
                message.liveLocations = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.liveLocations = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.liveLocations = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.liveLocations = 3;
                break;
            default:
                if (typeof object.liveLocations === "number" && (object.liveLocations | 0) === object.liveLocations)
                    message.liveLocations = object.liveLocations;
            }
            switch (object.queryVname) {
            case "NOT_STARTED":
            case 0:
                message.queryVname = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.queryVname = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.queryVname = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.queryVname = 3;
                break;
            default:
                if (typeof object.queryVname === "number" && (object.queryVname | 0) === object.queryVname)
                    message.queryVname = object.queryVname;
            }
            switch (object.voipIndividualIncoming) {
            case "NOT_STARTED":
            case 0:
                message.voipIndividualIncoming = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.voipIndividualIncoming = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.voipIndividualIncoming = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.voipIndividualIncoming = 3;
                break;
            default:
                if (typeof object.voipIndividualIncoming === "number" && (object.voipIndividualIncoming | 0) === object.voipIndividualIncoming)
                    message.voipIndividualIncoming = object.voipIndividualIncoming;
            }
            switch (object.quickRepliesQuery) {
            case "NOT_STARTED":
            case 0:
                message.quickRepliesQuery = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.quickRepliesQuery = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.quickRepliesQuery = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.quickRepliesQuery = 3;
                break;
            default:
                if (typeof object.quickRepliesQuery === "number" && (object.quickRepliesQuery | 0) === object.quickRepliesQuery)
                    message.quickRepliesQuery = object.quickRepliesQuery;
            }
            switch (object.payments) {
            case "NOT_STARTED":
            case 0:
                message.payments = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.payments = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.payments = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.payments = 3;
                break;
            default:
                if (typeof object.payments === "number" && (object.payments | 0) === object.payments)
                    message.payments = object.payments;
            }
            switch (object.stickerPackQuery) {
            case "NOT_STARTED":
            case 0:
                message.stickerPackQuery = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.stickerPackQuery = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.stickerPackQuery = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.stickerPackQuery = 3;
                break;
            default:
                if (typeof object.stickerPackQuery === "number" && (object.stickerPackQuery | 0) === object.stickerPackQuery)
                    message.stickerPackQuery = object.stickerPackQuery;
            }
            switch (object.liveLocationsFinal) {
            case "NOT_STARTED":
            case 0:
                message.liveLocationsFinal = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.liveLocationsFinal = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.liveLocationsFinal = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.liveLocationsFinal = 3;
                break;
            default:
                if (typeof object.liveLocationsFinal === "number" && (object.liveLocationsFinal | 0) === object.liveLocationsFinal)
                    message.liveLocationsFinal = object.liveLocationsFinal;
            }
            switch (object.labelsEdit) {
            case "NOT_STARTED":
            case 0:
                message.labelsEdit = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.labelsEdit = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.labelsEdit = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.labelsEdit = 3;
                break;
            default:
                if (typeof object.labelsEdit === "number" && (object.labelsEdit | 0) === object.labelsEdit)
                    message.labelsEdit = object.labelsEdit;
            }
            switch (object.mediaUpload) {
            case "NOT_STARTED":
            case 0:
                message.mediaUpload = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.mediaUpload = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.mediaUpload = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.mediaUpload = 3;
                break;
            default:
                if (typeof object.mediaUpload === "number" && (object.mediaUpload | 0) === object.mediaUpload)
                    message.mediaUpload = object.mediaUpload;
            }
            switch (object.mediaUploadRichQuickReplies) {
            case "NOT_STARTED":
            case 0:
                message.mediaUploadRichQuickReplies = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.mediaUploadRichQuickReplies = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.mediaUploadRichQuickReplies = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.mediaUploadRichQuickReplies = 3;
                break;
            default:
                if (typeof object.mediaUploadRichQuickReplies === "number" && (object.mediaUploadRichQuickReplies | 0) === object.mediaUploadRichQuickReplies)
                    message.mediaUploadRichQuickReplies = object.mediaUploadRichQuickReplies;
            }
            switch (object.vnameV2) {
            case "NOT_STARTED":
            case 0:
                message.vnameV2 = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.vnameV2 = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.vnameV2 = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.vnameV2 = 3;
                break;
            default:
                if (typeof object.vnameV2 === "number" && (object.vnameV2 | 0) === object.vnameV2)
                    message.vnameV2 = object.vnameV2;
            }
            switch (object.videoPlaybackUrl) {
            case "NOT_STARTED":
            case 0:
                message.videoPlaybackUrl = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.videoPlaybackUrl = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.videoPlaybackUrl = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.videoPlaybackUrl = 3;
                break;
            default:
                if (typeof object.videoPlaybackUrl === "number" && (object.videoPlaybackUrl | 0) === object.videoPlaybackUrl)
                    message.videoPlaybackUrl = object.videoPlaybackUrl;
            }
            switch (object.statusRanking) {
            case "NOT_STARTED":
            case 0:
                message.statusRanking = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.statusRanking = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.statusRanking = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.statusRanking = 3;
                break;
            default:
                if (typeof object.statusRanking === "number" && (object.statusRanking | 0) === object.statusRanking)
                    message.statusRanking = object.statusRanking;
            }
            switch (object.voipIndividualVideo) {
            case "NOT_STARTED":
            case 0:
                message.voipIndividualVideo = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.voipIndividualVideo = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.voipIndividualVideo = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.voipIndividualVideo = 3;
                break;
            default:
                if (typeof object.voipIndividualVideo === "number" && (object.voipIndividualVideo | 0) === object.voipIndividualVideo)
                    message.voipIndividualVideo = object.voipIndividualVideo;
            }
            switch (object.thirdPartyStickers) {
            case "NOT_STARTED":
            case 0:
                message.thirdPartyStickers = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.thirdPartyStickers = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.thirdPartyStickers = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.thirdPartyStickers = 3;
                break;
            default:
                if (typeof object.thirdPartyStickers === "number" && (object.thirdPartyStickers | 0) === object.thirdPartyStickers)
                    message.thirdPartyStickers = object.thirdPartyStickers;
            }
            switch (object.frequentlyForwardedSetting) {
            case "NOT_STARTED":
            case 0:
                message.frequentlyForwardedSetting = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.frequentlyForwardedSetting = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.frequentlyForwardedSetting = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.frequentlyForwardedSetting = 3;
                break;
            default:
                if (typeof object.frequentlyForwardedSetting === "number" && (object.frequentlyForwardedSetting | 0) === object.frequentlyForwardedSetting)
                    message.frequentlyForwardedSetting = object.frequentlyForwardedSetting;
            }
            switch (object.groupsV4JoinPermission) {
            case "NOT_STARTED":
            case 0:
                message.groupsV4JoinPermission = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.groupsV4JoinPermission = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.groupsV4JoinPermission = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.groupsV4JoinPermission = 3;
                break;
            default:
                if (typeof object.groupsV4JoinPermission === "number" && (object.groupsV4JoinPermission | 0) === object.groupsV4JoinPermission)
                    message.groupsV4JoinPermission = object.groupsV4JoinPermission;
            }
            switch (object.recentStickers) {
            case "NOT_STARTED":
            case 0:
                message.recentStickers = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.recentStickers = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.recentStickers = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.recentStickers = 3;
                break;
            default:
                if (typeof object.recentStickers === "number" && (object.recentStickers | 0) === object.recentStickers)
                    message.recentStickers = object.recentStickers;
            }
            switch (object.catalog) {
            case "NOT_STARTED":
            case 0:
                message.catalog = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.catalog = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.catalog = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.catalog = 3;
                break;
            default:
                if (typeof object.catalog === "number" && (object.catalog | 0) === object.catalog)
                    message.catalog = object.catalog;
            }
            switch (object.starredStickers) {
            case "NOT_STARTED":
            case 0:
                message.starredStickers = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.starredStickers = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.starredStickers = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.starredStickers = 3;
                break;
            default:
                if (typeof object.starredStickers === "number" && (object.starredStickers | 0) === object.starredStickers)
                    message.starredStickers = object.starredStickers;
            }
            switch (object.voipGroupCall) {
            case "NOT_STARTED":
            case 0:
                message.voipGroupCall = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.voipGroupCall = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.voipGroupCall = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.voipGroupCall = 3;
                break;
            default:
                if (typeof object.voipGroupCall === "number" && (object.voipGroupCall | 0) === object.voipGroupCall)
                    message.voipGroupCall = object.voipGroupCall;
            }
            switch (object.templateMessage) {
            case "NOT_STARTED":
            case 0:
                message.templateMessage = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.templateMessage = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.templateMessage = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.templateMessage = 3;
                break;
            default:
                if (typeof object.templateMessage === "number" && (object.templateMessage | 0) === object.templateMessage)
                    message.templateMessage = object.templateMessage;
            }
            switch (object.templateMessageInteractivity) {
            case "NOT_STARTED":
            case 0:
                message.templateMessageInteractivity = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.templateMessageInteractivity = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.templateMessageInteractivity = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.templateMessageInteractivity = 3;
                break;
            default:
                if (typeof object.templateMessageInteractivity === "number" && (object.templateMessageInteractivity | 0) === object.templateMessageInteractivity)
                    message.templateMessageInteractivity = object.templateMessageInteractivity;
            }
            switch (object.ephemeralMessages) {
            case "NOT_STARTED":
            case 0:
                message.ephemeralMessages = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.ephemeralMessages = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.ephemeralMessages = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.ephemeralMessages = 3;
                break;
            default:
                if (typeof object.ephemeralMessages === "number" && (object.ephemeralMessages | 0) === object.ephemeralMessages)
                    message.ephemeralMessages = object.ephemeralMessages;
            }
            switch (object.e2ENotificationSync) {
            case "NOT_STARTED":
            case 0:
                message.e2ENotificationSync = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.e2ENotificationSync = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.e2ENotificationSync = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.e2ENotificationSync = 3;
                break;
            default:
                if (typeof object.e2ENotificationSync === "number" && (object.e2ENotificationSync | 0) === object.e2ENotificationSync)
                    message.e2ENotificationSync = object.e2ENotificationSync;
            }
            switch (object.recentStickersV2) {
            case "NOT_STARTED":
            case 0:
                message.recentStickersV2 = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.recentStickersV2 = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.recentStickersV2 = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.recentStickersV2 = 3;
                break;
            default:
                if (typeof object.recentStickersV2 === "number" && (object.recentStickersV2 | 0) === object.recentStickersV2)
                    message.recentStickersV2 = object.recentStickersV2;
            }
            switch (object.recentStickersV3) {
            case "NOT_STARTED":
            case 0:
                message.recentStickersV3 = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.recentStickersV3 = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.recentStickersV3 = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.recentStickersV3 = 3;
                break;
            default:
                if (typeof object.recentStickersV3 === "number" && (object.recentStickersV3 | 0) === object.recentStickersV3)
                    message.recentStickersV3 = object.recentStickersV3;
            }
            switch (object.userNotice) {
            case "NOT_STARTED":
            case 0:
                message.userNotice = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.userNotice = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.userNotice = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.userNotice = 3;
                break;
            default:
                if (typeof object.userNotice === "number" && (object.userNotice | 0) === object.userNotice)
                    message.userNotice = object.userNotice;
            }
            switch (object.support) {
            case "NOT_STARTED":
            case 0:
                message.support = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.support = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.support = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.support = 3;
                break;
            default:
                if (typeof object.support === "number" && (object.support | 0) === object.support)
                    message.support = object.support;
            }
            switch (object.groupUiiCleanup) {
            case "NOT_STARTED":
            case 0:
                message.groupUiiCleanup = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.groupUiiCleanup = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.groupUiiCleanup = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.groupUiiCleanup = 3;
                break;
            default:
                if (typeof object.groupUiiCleanup === "number" && (object.groupUiiCleanup | 0) === object.groupUiiCleanup)
                    message.groupUiiCleanup = object.groupUiiCleanup;
            }
            switch (object.groupDogfoodingInternalOnly) {
            case "NOT_STARTED":
            case 0:
                message.groupDogfoodingInternalOnly = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.groupDogfoodingInternalOnly = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.groupDogfoodingInternalOnly = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.groupDogfoodingInternalOnly = 3;
                break;
            default:
                if (typeof object.groupDogfoodingInternalOnly === "number" && (object.groupDogfoodingInternalOnly | 0) === object.groupDogfoodingInternalOnly)
                    message.groupDogfoodingInternalOnly = object.groupDogfoodingInternalOnly;
            }
            switch (object.settingsSync) {
            case "NOT_STARTED":
            case 0:
                message.settingsSync = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.settingsSync = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.settingsSync = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.settingsSync = 3;
                break;
            default:
                if (typeof object.settingsSync === "number" && (object.settingsSync | 0) === object.settingsSync)
                    message.settingsSync = object.settingsSync;
            }
            switch (object.archiveV2) {
            case "NOT_STARTED":
            case 0:
                message.archiveV2 = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.archiveV2 = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.archiveV2 = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.archiveV2 = 3;
                break;
            default:
                if (typeof object.archiveV2 === "number" && (object.archiveV2 | 0) === object.archiveV2)
                    message.archiveV2 = object.archiveV2;
            }
            switch (object.ephemeralAllowGroupMembers) {
            case "NOT_STARTED":
            case 0:
                message.ephemeralAllowGroupMembers = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.ephemeralAllowGroupMembers = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.ephemeralAllowGroupMembers = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.ephemeralAllowGroupMembers = 3;
                break;
            default:
                if (typeof object.ephemeralAllowGroupMembers === "number" && (object.ephemeralAllowGroupMembers | 0) === object.ephemeralAllowGroupMembers)
                    message.ephemeralAllowGroupMembers = object.ephemeralAllowGroupMembers;
            }
            switch (object.ephemeral24HDuration) {
            case "NOT_STARTED":
            case 0:
                message.ephemeral24HDuration = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.ephemeral24HDuration = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.ephemeral24HDuration = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.ephemeral24HDuration = 3;
                break;
            default:
                if (typeof object.ephemeral24HDuration === "number" && (object.ephemeral24HDuration | 0) === object.ephemeral24HDuration)
                    message.ephemeral24HDuration = object.ephemeral24HDuration;
            }
            switch (object.mdForceUpgrade) {
            case "NOT_STARTED":
            case 0:
                message.mdForceUpgrade = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.mdForceUpgrade = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.mdForceUpgrade = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.mdForceUpgrade = 3;
                break;
            default:
                if (typeof object.mdForceUpgrade === "number" && (object.mdForceUpgrade | 0) === object.mdForceUpgrade)
                    message.mdForceUpgrade = object.mdForceUpgrade;
            }
            switch (object.disappearingMode) {
            case "NOT_STARTED":
            case 0:
                message.disappearingMode = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.disappearingMode = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.disappearingMode = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.disappearingMode = 3;
                break;
            default:
                if (typeof object.disappearingMode === "number" && (object.disappearingMode | 0) === object.disappearingMode)
                    message.disappearingMode = object.disappearingMode;
            }
            switch (object.externalMdOptInAvailable) {
            case "NOT_STARTED":
            case 0:
                message.externalMdOptInAvailable = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.externalMdOptInAvailable = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.externalMdOptInAvailable = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.externalMdOptInAvailable = 3;
                break;
            default:
                if (typeof object.externalMdOptInAvailable === "number" && (object.externalMdOptInAvailable | 0) === object.externalMdOptInAvailable)
                    message.externalMdOptInAvailable = object.externalMdOptInAvailable;
            }
            switch (object.noDeleteMessageTimeLimit) {
            case "NOT_STARTED":
            case 0:
                message.noDeleteMessageTimeLimit = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                message.noDeleteMessageTimeLimit = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                message.noDeleteMessageTimeLimit = 2;
                break;
            case "PRODUCTION":
            case 3:
                message.noDeleteMessageTimeLimit = 3;
                break;
            default:
                if (typeof object.noDeleteMessageTimeLimit === "number" && (object.noDeleteMessageTimeLimit | 0) === object.noDeleteMessageTimeLimit)
                    message.noDeleteMessageTimeLimit = object.noDeleteMessageTimeLimit;
            }
            return message;
        };

        /**
         * Creates a plain object from a WebFeatures message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.WebFeatures
         * @static
         * @param {proto.WebFeatures} message WebFeatures
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WebFeatures.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (message.labelsDisplay != null && Object.hasOwnProperty.call(message, "labelsDisplay"))
                object.labelsDisplay = options.enums === String ? $root.proto.WebFeatures.Flag[message.labelsDisplay] === undefined ? message.labelsDisplay : $root.proto.WebFeatures.Flag[message.labelsDisplay] : message.labelsDisplay;
            if (message.voipIndividualOutgoing != null && Object.hasOwnProperty.call(message, "voipIndividualOutgoing"))
                object.voipIndividualOutgoing = options.enums === String ? $root.proto.WebFeatures.Flag[message.voipIndividualOutgoing] === undefined ? message.voipIndividualOutgoing : $root.proto.WebFeatures.Flag[message.voipIndividualOutgoing] : message.voipIndividualOutgoing;
            if (message.groupsV3 != null && Object.hasOwnProperty.call(message, "groupsV3"))
                object.groupsV3 = options.enums === String ? $root.proto.WebFeatures.Flag[message.groupsV3] === undefined ? message.groupsV3 : $root.proto.WebFeatures.Flag[message.groupsV3] : message.groupsV3;
            if (message.groupsV3Create != null && Object.hasOwnProperty.call(message, "groupsV3Create"))
                object.groupsV3Create = options.enums === String ? $root.proto.WebFeatures.Flag[message.groupsV3Create] === undefined ? message.groupsV3Create : $root.proto.WebFeatures.Flag[message.groupsV3Create] : message.groupsV3Create;
            if (message.changeNumberV2 != null && Object.hasOwnProperty.call(message, "changeNumberV2"))
                object.changeNumberV2 = options.enums === String ? $root.proto.WebFeatures.Flag[message.changeNumberV2] === undefined ? message.changeNumberV2 : $root.proto.WebFeatures.Flag[message.changeNumberV2] : message.changeNumberV2;
            if (message.queryStatusV3Thumbnail != null && Object.hasOwnProperty.call(message, "queryStatusV3Thumbnail"))
                object.queryStatusV3Thumbnail = options.enums === String ? $root.proto.WebFeatures.Flag[message.queryStatusV3Thumbnail] === undefined ? message.queryStatusV3Thumbnail : $root.proto.WebFeatures.Flag[message.queryStatusV3Thumbnail] : message.queryStatusV3Thumbnail;
            if (message.liveLocations != null && Object.hasOwnProperty.call(message, "liveLocations"))
                object.liveLocations = options.enums === String ? $root.proto.WebFeatures.Flag[message.liveLocations] === undefined ? message.liveLocations : $root.proto.WebFeatures.Flag[message.liveLocations] : message.liveLocations;
            if (message.queryVname != null && Object.hasOwnProperty.call(message, "queryVname"))
                object.queryVname = options.enums === String ? $root.proto.WebFeatures.Flag[message.queryVname] === undefined ? message.queryVname : $root.proto.WebFeatures.Flag[message.queryVname] : message.queryVname;
            if (message.voipIndividualIncoming != null && Object.hasOwnProperty.call(message, "voipIndividualIncoming"))
                object.voipIndividualIncoming = options.enums === String ? $root.proto.WebFeatures.Flag[message.voipIndividualIncoming] === undefined ? message.voipIndividualIncoming : $root.proto.WebFeatures.Flag[message.voipIndividualIncoming] : message.voipIndividualIncoming;
            if (message.quickRepliesQuery != null && Object.hasOwnProperty.call(message, "quickRepliesQuery"))
                object.quickRepliesQuery = options.enums === String ? $root.proto.WebFeatures.Flag[message.quickRepliesQuery] === undefined ? message.quickRepliesQuery : $root.proto.WebFeatures.Flag[message.quickRepliesQuery] : message.quickRepliesQuery;
            if (message.payments != null && Object.hasOwnProperty.call(message, "payments"))
                object.payments = options.enums === String ? $root.proto.WebFeatures.Flag[message.payments] === undefined ? message.payments : $root.proto.WebFeatures.Flag[message.payments] : message.payments;
            if (message.stickerPackQuery != null && Object.hasOwnProperty.call(message, "stickerPackQuery"))
                object.stickerPackQuery = options.enums === String ? $root.proto.WebFeatures.Flag[message.stickerPackQuery] === undefined ? message.stickerPackQuery : $root.proto.WebFeatures.Flag[message.stickerPackQuery] : message.stickerPackQuery;
            if (message.liveLocationsFinal != null && Object.hasOwnProperty.call(message, "liveLocationsFinal"))
                object.liveLocationsFinal = options.enums === String ? $root.proto.WebFeatures.Flag[message.liveLocationsFinal] === undefined ? message.liveLocationsFinal : $root.proto.WebFeatures.Flag[message.liveLocationsFinal] : message.liveLocationsFinal;
            if (message.labelsEdit != null && Object.hasOwnProperty.call(message, "labelsEdit"))
                object.labelsEdit = options.enums === String ? $root.proto.WebFeatures.Flag[message.labelsEdit] === undefined ? message.labelsEdit : $root.proto.WebFeatures.Flag[message.labelsEdit] : message.labelsEdit;
            if (message.mediaUpload != null && Object.hasOwnProperty.call(message, "mediaUpload"))
                object.mediaUpload = options.enums === String ? $root.proto.WebFeatures.Flag[message.mediaUpload] === undefined ? message.mediaUpload : $root.proto.WebFeatures.Flag[message.mediaUpload] : message.mediaUpload;
            if (message.mediaUploadRichQuickReplies != null && Object.hasOwnProperty.call(message, "mediaUploadRichQuickReplies"))
                object.mediaUploadRichQuickReplies = options.enums === String ? $root.proto.WebFeatures.Flag[message.mediaUploadRichQuickReplies] === undefined ? message.mediaUploadRichQuickReplies : $root.proto.WebFeatures.Flag[message.mediaUploadRichQuickReplies] : message.mediaUploadRichQuickReplies;
            if (message.vnameV2 != null && Object.hasOwnProperty.call(message, "vnameV2"))
                object.vnameV2 = options.enums === String ? $root.proto.WebFeatures.Flag[message.vnameV2] === undefined ? message.vnameV2 : $root.proto.WebFeatures.Flag[message.vnameV2] : message.vnameV2;
            if (message.videoPlaybackUrl != null && Object.hasOwnProperty.call(message, "videoPlaybackUrl"))
                object.videoPlaybackUrl = options.enums === String ? $root.proto.WebFeatures.Flag[message.videoPlaybackUrl] === undefined ? message.videoPlaybackUrl : $root.proto.WebFeatures.Flag[message.videoPlaybackUrl] : message.videoPlaybackUrl;
            if (message.statusRanking != null && Object.hasOwnProperty.call(message, "statusRanking"))
                object.statusRanking = options.enums === String ? $root.proto.WebFeatures.Flag[message.statusRanking] === undefined ? message.statusRanking : $root.proto.WebFeatures.Flag[message.statusRanking] : message.statusRanking;
            if (message.voipIndividualVideo != null && Object.hasOwnProperty.call(message, "voipIndividualVideo"))
                object.voipIndividualVideo = options.enums === String ? $root.proto.WebFeatures.Flag[message.voipIndividualVideo] === undefined ? message.voipIndividualVideo : $root.proto.WebFeatures.Flag[message.voipIndividualVideo] : message.voipIndividualVideo;
            if (message.thirdPartyStickers != null && Object.hasOwnProperty.call(message, "thirdPartyStickers"))
                object.thirdPartyStickers = options.enums === String ? $root.proto.WebFeatures.Flag[message.thirdPartyStickers] === undefined ? message.thirdPartyStickers : $root.proto.WebFeatures.Flag[message.thirdPartyStickers] : message.thirdPartyStickers;
            if (message.frequentlyForwardedSetting != null && Object.hasOwnProperty.call(message, "frequentlyForwardedSetting"))
                object.frequentlyForwardedSetting = options.enums === String ? $root.proto.WebFeatures.Flag[message.frequentlyForwardedSetting] === undefined ? message.frequentlyForwardedSetting : $root.proto.WebFeatures.Flag[message.frequentlyForwardedSetting] : message.frequentlyForwardedSetting;
            if (message.groupsV4JoinPermission != null && Object.hasOwnProperty.call(message, "groupsV4JoinPermission"))
                object.groupsV4JoinPermission = options.enums === String ? $root.proto.WebFeatures.Flag[message.groupsV4JoinPermission] === undefined ? message.groupsV4JoinPermission : $root.proto.WebFeatures.Flag[message.groupsV4JoinPermission] : message.groupsV4JoinPermission;
            if (message.recentStickers != null && Object.hasOwnProperty.call(message, "recentStickers"))
                object.recentStickers = options.enums === String ? $root.proto.WebFeatures.Flag[message.recentStickers] === undefined ? message.recentStickers : $root.proto.WebFeatures.Flag[message.recentStickers] : message.recentStickers;
            if (message.catalog != null && Object.hasOwnProperty.call(message, "catalog"))
                object.catalog = options.enums === String ? $root.proto.WebFeatures.Flag[message.catalog] === undefined ? message.catalog : $root.proto.WebFeatures.Flag[message.catalog] : message.catalog;
            if (message.starredStickers != null && Object.hasOwnProperty.call(message, "starredStickers"))
                object.starredStickers = options.enums === String ? $root.proto.WebFeatures.Flag[message.starredStickers] === undefined ? message.starredStickers : $root.proto.WebFeatures.Flag[message.starredStickers] : message.starredStickers;
            if (message.voipGroupCall != null && Object.hasOwnProperty.call(message, "voipGroupCall"))
                object.voipGroupCall = options.enums === String ? $root.proto.WebFeatures.Flag[message.voipGroupCall] === undefined ? message.voipGroupCall : $root.proto.WebFeatures.Flag[message.voipGroupCall] : message.voipGroupCall;
            if (message.templateMessage != null && Object.hasOwnProperty.call(message, "templateMessage"))
                object.templateMessage = options.enums === String ? $root.proto.WebFeatures.Flag[message.templateMessage] === undefined ? message.templateMessage : $root.proto.WebFeatures.Flag[message.templateMessage] : message.templateMessage;
            if (message.templateMessageInteractivity != null && Object.hasOwnProperty.call(message, "templateMessageInteractivity"))
                object.templateMessageInteractivity = options.enums === String ? $root.proto.WebFeatures.Flag[message.templateMessageInteractivity] === undefined ? message.templateMessageInteractivity : $root.proto.WebFeatures.Flag[message.templateMessageInteractivity] : message.templateMessageInteractivity;
            if (message.ephemeralMessages != null && Object.hasOwnProperty.call(message, "ephemeralMessages"))
                object.ephemeralMessages = options.enums === String ? $root.proto.WebFeatures.Flag[message.ephemeralMessages] === undefined ? message.ephemeralMessages : $root.proto.WebFeatures.Flag[message.ephemeralMessages] : message.ephemeralMessages;
            if (message.e2ENotificationSync != null && Object.hasOwnProperty.call(message, "e2ENotificationSync"))
                object.e2ENotificationSync = options.enums === String ? $root.proto.WebFeatures.Flag[message.e2ENotificationSync] === undefined ? message.e2ENotificationSync : $root.proto.WebFeatures.Flag[message.e2ENotificationSync] : message.e2ENotificationSync;
            if (message.recentStickersV2 != null && Object.hasOwnProperty.call(message, "recentStickersV2"))
                object.recentStickersV2 = options.enums === String ? $root.proto.WebFeatures.Flag[message.recentStickersV2] === undefined ? message.recentStickersV2 : $root.proto.WebFeatures.Flag[message.recentStickersV2] : message.recentStickersV2;
            if (message.recentStickersV3 != null && Object.hasOwnProperty.call(message, "recentStickersV3"))
                object.recentStickersV3 = options.enums === String ? $root.proto.WebFeatures.Flag[message.recentStickersV3] === undefined ? message.recentStickersV3 : $root.proto.WebFeatures.Flag[message.recentStickersV3] : message.recentStickersV3;
            if (message.userNotice != null && Object.hasOwnProperty.call(message, "userNotice"))
                object.userNotice = options.enums === String ? $root.proto.WebFeatures.Flag[message.userNotice] === undefined ? message.userNotice : $root.proto.WebFeatures.Flag[message.userNotice] : message.userNotice;
            if (message.support != null && Object.hasOwnProperty.call(message, "support"))
                object.support = options.enums === String ? $root.proto.WebFeatures.Flag[message.support] === undefined ? message.support : $root.proto.WebFeatures.Flag[message.support] : message.support;
            if (message.groupUiiCleanup != null && Object.hasOwnProperty.call(message, "groupUiiCleanup"))
                object.groupUiiCleanup = options.enums === String ? $root.proto.WebFeatures.Flag[message.groupUiiCleanup] === undefined ? message.groupUiiCleanup : $root.proto.WebFeatures.Flag[message.groupUiiCleanup] : message.groupUiiCleanup;
            if (message.groupDogfoodingInternalOnly != null && Object.hasOwnProperty.call(message, "groupDogfoodingInternalOnly"))
                object.groupDogfoodingInternalOnly = options.enums === String ? $root.proto.WebFeatures.Flag[message.groupDogfoodingInternalOnly] === undefined ? message.groupDogfoodingInternalOnly : $root.proto.WebFeatures.Flag[message.groupDogfoodingInternalOnly] : message.groupDogfoodingInternalOnly;
            if (message.settingsSync != null && Object.hasOwnProperty.call(message, "settingsSync"))
                object.settingsSync = options.enums === String ? $root.proto.WebFeatures.Flag[message.settingsSync] === undefined ? message.settingsSync : $root.proto.WebFeatures.Flag[message.settingsSync] : message.settingsSync;
            if (message.archiveV2 != null && Object.hasOwnProperty.call(message, "archiveV2"))
                object.archiveV2 = options.enums === String ? $root.proto.WebFeatures.Flag[message.archiveV2] === undefined ? message.archiveV2 : $root.proto.WebFeatures.Flag[message.archiveV2] : message.archiveV2;
            if (message.ephemeralAllowGroupMembers != null && Object.hasOwnProperty.call(message, "ephemeralAllowGroupMembers"))
                object.ephemeralAllowGroupMembers = options.enums === String ? $root.proto.WebFeatures.Flag[message.ephemeralAllowGroupMembers] === undefined ? message.ephemeralAllowGroupMembers : $root.proto.WebFeatures.Flag[message.ephemeralAllowGroupMembers] : message.ephemeralAllowGroupMembers;
            if (message.ephemeral24HDuration != null && Object.hasOwnProperty.call(message, "ephemeral24HDuration"))
                object.ephemeral24HDuration = options.enums === String ? $root.proto.WebFeatures.Flag[message.ephemeral24HDuration] === undefined ? message.ephemeral24HDuration : $root.proto.WebFeatures.Flag[message.ephemeral24HDuration] : message.ephemeral24HDuration;
            if (message.mdForceUpgrade != null && Object.hasOwnProperty.call(message, "mdForceUpgrade"))
                object.mdForceUpgrade = options.enums === String ? $root.proto.WebFeatures.Flag[message.mdForceUpgrade] === undefined ? message.mdForceUpgrade : $root.proto.WebFeatures.Flag[message.mdForceUpgrade] : message.mdForceUpgrade;
            if (message.disappearingMode != null && Object.hasOwnProperty.call(message, "disappearingMode"))
                object.disappearingMode = options.enums === String ? $root.proto.WebFeatures.Flag[message.disappearingMode] === undefined ? message.disappearingMode : $root.proto.WebFeatures.Flag[message.disappearingMode] : message.disappearingMode;
            if (message.externalMdOptInAvailable != null && Object.hasOwnProperty.call(message, "externalMdOptInAvailable"))
                object.externalMdOptInAvailable = options.enums === String ? $root.proto.WebFeatures.Flag[message.externalMdOptInAvailable] === undefined ? message.externalMdOptInAvailable : $root.proto.WebFeatures.Flag[message.externalMdOptInAvailable] : message.externalMdOptInAvailable;
            if (message.noDeleteMessageTimeLimit != null && Object.hasOwnProperty.call(message, "noDeleteMessageTimeLimit"))
                object.noDeleteMessageTimeLimit = options.enums === String ? $root.proto.WebFeatures.Flag[message.noDeleteMessageTimeLimit] === undefined ? message.noDeleteMessageTimeLimit : $root.proto.WebFeatures.Flag[message.noDeleteMessageTimeLimit] : message.noDeleteMessageTimeLimit;
            return object;
        };

        /**
         * Converts this WebFeatures to JSON.
         * @function toJSON
         * @memberof proto.WebFeatures
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WebFeatures.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WebFeatures
         * @function getTypeUrl
         * @memberof proto.WebFeatures
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WebFeatures.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.WebFeatures";
        };

        /**
         * Flag enum.
         * @name proto.WebFeatures.Flag
         * @enum {number}
         * @property {number} NOT_STARTED=0 NOT_STARTED value
         * @property {number} FORCE_UPGRADE=1 FORCE_UPGRADE value
         * @property {number} DEVELOPMENT=2 DEVELOPMENT value
         * @property {number} PRODUCTION=3 PRODUCTION value
         */
        WebFeatures.Flag = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "NOT_STARTED"] = 0;
            values[valuesById[1] = "FORCE_UPGRADE"] = 1;
            values[valuesById[2] = "DEVELOPMENT"] = 2;
            values[valuesById[3] = "PRODUCTION"] = 3;
            return values;
        })();

        return WebFeatures;
    })();

    /**
     * WebLinkRenderConfig enum.
     * @name proto.WebLinkRenderConfig
     * @enum {number}
     * @property {number} WEBVIEW=0 WEBVIEW value
     * @property {number} SYSTEM=1 SYSTEM value
     */
    proto.WebLinkRenderConfig = (function() {
        var valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "WEBVIEW"] = 0;
        values[valuesById[1] = "SYSTEM"] = 1;
        return values;
    })();

    proto.WebMessageInfo = (function() {

        /**
         * Properties of a WebMessageInfo.
         * @memberof proto
         * @interface IWebMessageInfo
         * @property {proto.IMessageKey|null} [key] WebMessageInfo key
         * @property {proto.IMessage|null} [message] WebMessageInfo message
         * @property {number|Long|null} [messageTimestamp] WebMessageInfo messageTimestamp
         * @property {proto.WebMessageInfo.Status|null} [status] WebMessageInfo status
         * @property {string|null} [participant] WebMessageInfo participant
         * @property {number|Long|null} [messageC2STimestamp] WebMessageInfo messageC2STimestamp
         * @property {boolean|null} [ignore] WebMessageInfo ignore
         * @property {boolean|null} [starred] WebMessageInfo starred
         * @property {boolean|null} [broadcast] WebMessageInfo broadcast
         * @property {string|null} [pushName] WebMessageInfo pushName
         * @property {Uint8Array|null} [mediaCiphertextSha256] WebMessageInfo mediaCiphertextSha256
         * @property {boolean|null} [multicast] WebMessageInfo multicast
         * @property {boolean|null} [urlText] WebMessageInfo urlText
         * @property {boolean|null} [urlNumber] WebMessageInfo urlNumber
         * @property {proto.WebMessageInfo.StubType|null} [messageStubType] WebMessageInfo messageStubType
         * @property {boolean|null} [clearMedia] WebMessageInfo clearMedia
         * @property {Array.<string>|null} [messageStubParameters] WebMessageInfo messageStubParameters
         * @property {number|null} [duration] WebMessageInfo duration
         * @property {Array.<string>|null} [labels] WebMessageInfo labels
         * @property {proto.IPaymentInfo|null} [paymentInfo] WebMessageInfo paymentInfo
         * @property {proto.Message.ILiveLocationMessage|null} [finalLiveLocation] WebMessageInfo finalLiveLocation
         * @property {proto.IPaymentInfo|null} [quotedPaymentInfo] WebMessageInfo quotedPaymentInfo
         * @property {number|Long|null} [ephemeralStartTimestamp] WebMessageInfo ephemeralStartTimestamp
         * @property {number|null} [ephemeralDuration] WebMessageInfo ephemeralDuration
         * @property {boolean|null} [ephemeralOffToOn] WebMessageInfo ephemeralOffToOn
         * @property {boolean|null} [ephemeralOutOfSync] WebMessageInfo ephemeralOutOfSync
         * @property {proto.WebMessageInfo.BizPrivacyStatus|null} [bizPrivacyStatus] WebMessageInfo bizPrivacyStatus
         * @property {string|null} [verifiedBizName] WebMessageInfo verifiedBizName
         * @property {proto.IMediaData|null} [mediaData] WebMessageInfo mediaData
         * @property {proto.IPhotoChange|null} [photoChange] WebMessageInfo photoChange
         * @property {Array.<proto.IUserReceipt>|null} [userReceipt] WebMessageInfo userReceipt
         * @property {Array.<proto.IReaction>|null} [reactions] WebMessageInfo reactions
         * @property {proto.IMediaData|null} [quotedStickerData] WebMessageInfo quotedStickerData
         * @property {Uint8Array|null} [futureproofData] WebMessageInfo futureproofData
         * @property {proto.IStatusPSA|null} [statusPsa] WebMessageInfo statusPsa
         * @property {Array.<proto.IPollUpdate>|null} [pollUpdates] WebMessageInfo pollUpdates
         * @property {proto.IPollAdditionalMetadata|null} [pollAdditionalMetadata] WebMessageInfo pollAdditionalMetadata
         * @property {string|null} [agentId] WebMessageInfo agentId
         * @property {boolean|null} [statusAlreadyViewed] WebMessageInfo statusAlreadyViewed
         * @property {Uint8Array|null} [messageSecret] WebMessageInfo messageSecret
         * @property {proto.IKeepInChat|null} [keepInChat] WebMessageInfo keepInChat
         * @property {string|null} [originalSelfAuthorUserJidString] WebMessageInfo originalSelfAuthorUserJidString
         * @property {number|Long|null} [revokeMessageTimestamp] WebMessageInfo revokeMessageTimestamp
         * @property {proto.IPinInChat|null} [pinInChat] WebMessageInfo pinInChat
         * @property {proto.IPremiumMessageInfo|null} [premiumMessageInfo] WebMessageInfo premiumMessageInfo
         * @property {boolean|null} [is1PBizBotMessage] WebMessageInfo is1PBizBotMessage
         * @property {boolean|null} [isGroupHistoryMessage] WebMessageInfo isGroupHistoryMessage
         * @property {string|null} [botMessageInvokerJid] WebMessageInfo botMessageInvokerJid
         * @property {proto.ICommentMetadata|null} [commentMetadata] WebMessageInfo commentMetadata
         * @property {Array.<proto.IEventResponse>|null} [eventResponses] WebMessageInfo eventResponses
         * @property {proto.IReportingTokenInfo|null} [reportingTokenInfo] WebMessageInfo reportingTokenInfo
         * @property {number|Long|null} [newsletterServerId] WebMessageInfo newsletterServerId
         * @property {proto.IEventAdditionalMetadata|null} [eventAdditionalMetadata] WebMessageInfo eventAdditionalMetadata
         * @property {boolean|null} [isMentionedInStatus] WebMessageInfo isMentionedInStatus
         * @property {Array.<string>|null} [statusMentions] WebMessageInfo statusMentions
         * @property {proto.IMessageKey|null} [targetMessageId] WebMessageInfo targetMessageId
         * @property {Array.<proto.IMessageAddOn>|null} [messageAddOns] WebMessageInfo messageAddOns
         * @property {proto.IStatusMentionMessage|null} [statusMentionMessageInfo] WebMessageInfo statusMentionMessageInfo
         * @property {boolean|null} [isSupportAiMessage] WebMessageInfo isSupportAiMessage
         * @property {Array.<string>|null} [statusMentionSources] WebMessageInfo statusMentionSources
         * @property {Array.<proto.ICitation>|null} [supportAiCitations] WebMessageInfo supportAiCitations
         * @property {string|null} [botTargetId] WebMessageInfo botTargetId
         * @property {proto.IGroupHistoryIndividualMessageInfo|null} [groupHistoryIndividualMessageInfo] WebMessageInfo groupHistoryIndividualMessageInfo
         * @property {proto.IGroupHistoryBundleInfo|null} [groupHistoryBundleInfo] WebMessageInfo groupHistoryBundleInfo
         * @property {proto.IInteractiveMessageAdditionalMetadata|null} [interactiveMessageAdditionalMetadata] WebMessageInfo interactiveMessageAdditionalMetadata
         * @property {proto.IQuarantinedMessage|null} [quarantinedMessage] WebMessageInfo quarantinedMessage
         * @property {number|null} [nonJidMentions] WebMessageInfo nonJidMentions
         * @property {string|null} [hsmTag] WebMessageInfo hsmTag
         * @property {number|Long|null} [ephemeralExpirationTimestamp] WebMessageInfo ephemeralExpirationTimestamp
         * @property {proto.IScheduledMessageMetadata|null} [scheduledMessageMetadata] WebMessageInfo scheduledMessageMetadata
         * @property {string|null} [decisionId] WebMessageInfo decisionId
         * @property {Array.<string>|null} [decisionSources] WebMessageInfo decisionSources
         */

        /**
         * Constructs a new WebMessageInfo.
         * @memberof proto
         * @classdesc Represents a WebMessageInfo.
         * @implements IWebMessageInfo
         * @constructor
         * @param {proto.IWebMessageInfo=} [properties] Properties to set
         */
        function WebMessageInfo(properties) {
            this.messageStubParameters = [];
            this.labels = [];
            this.userReceipt = [];
            this.reactions = [];
            this.pollUpdates = [];
            this.eventResponses = [];
            this.statusMentions = [];
            this.messageAddOns = [];
            this.statusMentionSources = [];
            this.supportAiCitations = [];
            this.decisionSources = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WebMessageInfo key.
         * @member {proto.IMessageKey|null|undefined} key
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.key = null;

        /**
         * WebMessageInfo message.
         * @member {proto.IMessage|null|undefined} message
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.message = null;

        /**
         * WebMessageInfo messageTimestamp.
         * @member {number|Long|null|undefined} messageTimestamp
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.messageTimestamp = null;

        /**
         * WebMessageInfo status.
         * @member {proto.WebMessageInfo.Status|null|undefined} status
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.status = null;

        /**
         * WebMessageInfo participant.
         * @member {string|null|undefined} participant
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.participant = null;

        /**
         * WebMessageInfo messageC2STimestamp.
         * @member {number|Long|null|undefined} messageC2STimestamp
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.messageC2STimestamp = null;

        /**
         * WebMessageInfo ignore.
         * @member {boolean|null|undefined} ignore
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.ignore = null;

        /**
         * WebMessageInfo starred.
         * @member {boolean|null|undefined} starred
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.starred = null;

        /**
         * WebMessageInfo broadcast.
         * @member {boolean|null|undefined} broadcast
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.broadcast = null;

        /**
         * WebMessageInfo pushName.
         * @member {string|null|undefined} pushName
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.pushName = null;

        /**
         * WebMessageInfo mediaCiphertextSha256.
         * @member {Uint8Array|null|undefined} mediaCiphertextSha256
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.mediaCiphertextSha256 = null;

        /**
         * WebMessageInfo multicast.
         * @member {boolean|null|undefined} multicast
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.multicast = null;

        /**
         * WebMessageInfo urlText.
         * @member {boolean|null|undefined} urlText
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.urlText = null;

        /**
         * WebMessageInfo urlNumber.
         * @member {boolean|null|undefined} urlNumber
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.urlNumber = null;

        /**
         * WebMessageInfo messageStubType.
         * @member {proto.WebMessageInfo.StubType|null|undefined} messageStubType
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.messageStubType = null;

        /**
         * WebMessageInfo clearMedia.
         * @member {boolean|null|undefined} clearMedia
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.clearMedia = null;

        /**
         * WebMessageInfo messageStubParameters.
         * @member {Array.<string>} messageStubParameters
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.messageStubParameters = $util.emptyArray;

        /**
         * WebMessageInfo duration.
         * @member {number|null|undefined} duration
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.duration = null;

        /**
         * WebMessageInfo labels.
         * @member {Array.<string>} labels
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.labels = $util.emptyArray;

        /**
         * WebMessageInfo paymentInfo.
         * @member {proto.IPaymentInfo|null|undefined} paymentInfo
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.paymentInfo = null;

        /**
         * WebMessageInfo finalLiveLocation.
         * @member {proto.Message.ILiveLocationMessage|null|undefined} finalLiveLocation
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.finalLiveLocation = null;

        /**
         * WebMessageInfo quotedPaymentInfo.
         * @member {proto.IPaymentInfo|null|undefined} quotedPaymentInfo
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.quotedPaymentInfo = null;

        /**
         * WebMessageInfo ephemeralStartTimestamp.
         * @member {number|Long|null|undefined} ephemeralStartTimestamp
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.ephemeralStartTimestamp = null;

        /**
         * WebMessageInfo ephemeralDuration.
         * @member {number|null|undefined} ephemeralDuration
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.ephemeralDuration = null;

        /**
         * WebMessageInfo ephemeralOffToOn.
         * @member {boolean|null|undefined} ephemeralOffToOn
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.ephemeralOffToOn = null;

        /**
         * WebMessageInfo ephemeralOutOfSync.
         * @member {boolean|null|undefined} ephemeralOutOfSync
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.ephemeralOutOfSync = null;

        /**
         * WebMessageInfo bizPrivacyStatus.
         * @member {proto.WebMessageInfo.BizPrivacyStatus|null|undefined} bizPrivacyStatus
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.bizPrivacyStatus = null;

        /**
         * WebMessageInfo verifiedBizName.
         * @member {string|null|undefined} verifiedBizName
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.verifiedBizName = null;

        /**
         * WebMessageInfo mediaData.
         * @member {proto.IMediaData|null|undefined} mediaData
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.mediaData = null;

        /**
         * WebMessageInfo photoChange.
         * @member {proto.IPhotoChange|null|undefined} photoChange
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.photoChange = null;

        /**
         * WebMessageInfo userReceipt.
         * @member {Array.<proto.IUserReceipt>} userReceipt
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.userReceipt = $util.emptyArray;

        /**
         * WebMessageInfo reactions.
         * @member {Array.<proto.IReaction>} reactions
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.reactions = $util.emptyArray;

        /**
         * WebMessageInfo quotedStickerData.
         * @member {proto.IMediaData|null|undefined} quotedStickerData
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.quotedStickerData = null;

        /**
         * WebMessageInfo futureproofData.
         * @member {Uint8Array|null|undefined} futureproofData
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.futureproofData = null;

        /**
         * WebMessageInfo statusPsa.
         * @member {proto.IStatusPSA|null|undefined} statusPsa
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.statusPsa = null;

        /**
         * WebMessageInfo pollUpdates.
         * @member {Array.<proto.IPollUpdate>} pollUpdates
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.pollUpdates = $util.emptyArray;

        /**
         * WebMessageInfo pollAdditionalMetadata.
         * @member {proto.IPollAdditionalMetadata|null|undefined} pollAdditionalMetadata
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.pollAdditionalMetadata = null;

        /**
         * WebMessageInfo agentId.
         * @member {string|null|undefined} agentId
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.agentId = null;

        /**
         * WebMessageInfo statusAlreadyViewed.
         * @member {boolean|null|undefined} statusAlreadyViewed
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.statusAlreadyViewed = null;

        /**
         * WebMessageInfo messageSecret.
         * @member {Uint8Array|null|undefined} messageSecret
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.messageSecret = null;

        /**
         * WebMessageInfo keepInChat.
         * @member {proto.IKeepInChat|null|undefined} keepInChat
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.keepInChat = null;

        /**
         * WebMessageInfo originalSelfAuthorUserJidString.
         * @member {string|null|undefined} originalSelfAuthorUserJidString
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.originalSelfAuthorUserJidString = null;

        /**
         * WebMessageInfo revokeMessageTimestamp.
         * @member {number|Long|null|undefined} revokeMessageTimestamp
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.revokeMessageTimestamp = null;

        /**
         * WebMessageInfo pinInChat.
         * @member {proto.IPinInChat|null|undefined} pinInChat
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.pinInChat = null;

        /**
         * WebMessageInfo premiumMessageInfo.
         * @member {proto.IPremiumMessageInfo|null|undefined} premiumMessageInfo
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.premiumMessageInfo = null;

        /**
         * WebMessageInfo is1PBizBotMessage.
         * @member {boolean|null|undefined} is1PBizBotMessage
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.is1PBizBotMessage = null;

        /**
         * WebMessageInfo isGroupHistoryMessage.
         * @member {boolean|null|undefined} isGroupHistoryMessage
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.isGroupHistoryMessage = null;

        /**
         * WebMessageInfo botMessageInvokerJid.
         * @member {string|null|undefined} botMessageInvokerJid
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.botMessageInvokerJid = null;

        /**
         * WebMessageInfo commentMetadata.
         * @member {proto.ICommentMetadata|null|undefined} commentMetadata
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.commentMetadata = null;

        /**
         * WebMessageInfo eventResponses.
         * @member {Array.<proto.IEventResponse>} eventResponses
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.eventResponses = $util.emptyArray;

        /**
         * WebMessageInfo reportingTokenInfo.
         * @member {proto.IReportingTokenInfo|null|undefined} reportingTokenInfo
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.reportingTokenInfo = null;

        /**
         * WebMessageInfo newsletterServerId.
         * @member {number|Long|null|undefined} newsletterServerId
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.newsletterServerId = null;

        /**
         * WebMessageInfo eventAdditionalMetadata.
         * @member {proto.IEventAdditionalMetadata|null|undefined} eventAdditionalMetadata
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.eventAdditionalMetadata = null;

        /**
         * WebMessageInfo isMentionedInStatus.
         * @member {boolean|null|undefined} isMentionedInStatus
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.isMentionedInStatus = null;

        /**
         * WebMessageInfo statusMentions.
         * @member {Array.<string>} statusMentions
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.statusMentions = $util.emptyArray;

        /**
         * WebMessageInfo targetMessageId.
         * @member {proto.IMessageKey|null|undefined} targetMessageId
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.targetMessageId = null;

        /**
         * WebMessageInfo messageAddOns.
         * @member {Array.<proto.IMessageAddOn>} messageAddOns
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.messageAddOns = $util.emptyArray;

        /**
         * WebMessageInfo statusMentionMessageInfo.
         * @member {proto.IStatusMentionMessage|null|undefined} statusMentionMessageInfo
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.statusMentionMessageInfo = null;

        /**
         * WebMessageInfo isSupportAiMessage.
         * @member {boolean|null|undefined} isSupportAiMessage
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.isSupportAiMessage = null;

        /**
         * WebMessageInfo statusMentionSources.
         * @member {Array.<string>} statusMentionSources
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.statusMentionSources = $util.emptyArray;

        /**
         * WebMessageInfo supportAiCitations.
         * @member {Array.<proto.ICitation>} supportAiCitations
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.supportAiCitations = $util.emptyArray;

        /**
         * WebMessageInfo botTargetId.
         * @member {string|null|undefined} botTargetId
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.botTargetId = null;

        /**
         * WebMessageInfo groupHistoryIndividualMessageInfo.
         * @member {proto.IGroupHistoryIndividualMessageInfo|null|undefined} groupHistoryIndividualMessageInfo
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.groupHistoryIndividualMessageInfo = null;

        /**
         * WebMessageInfo groupHistoryBundleInfo.
         * @member {proto.IGroupHistoryBundleInfo|null|undefined} groupHistoryBundleInfo
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.groupHistoryBundleInfo = null;

        /**
         * WebMessageInfo interactiveMessageAdditionalMetadata.
         * @member {proto.IInteractiveMessageAdditionalMetadata|null|undefined} interactiveMessageAdditionalMetadata
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.interactiveMessageAdditionalMetadata = null;

        /**
         * WebMessageInfo quarantinedMessage.
         * @member {proto.IQuarantinedMessage|null|undefined} quarantinedMessage
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.quarantinedMessage = null;

        /**
         * WebMessageInfo nonJidMentions.
         * @member {number|null|undefined} nonJidMentions
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.nonJidMentions = null;

        /**
         * WebMessageInfo hsmTag.
         * @member {string|null|undefined} hsmTag
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.hsmTag = null;

        /**
         * WebMessageInfo ephemeralExpirationTimestamp.
         * @member {number|Long|null|undefined} ephemeralExpirationTimestamp
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.ephemeralExpirationTimestamp = null;

        /**
         * WebMessageInfo scheduledMessageMetadata.
         * @member {proto.IScheduledMessageMetadata|null|undefined} scheduledMessageMetadata
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.scheduledMessageMetadata = null;

        /**
         * WebMessageInfo decisionId.
         * @member {string|null|undefined} decisionId
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.decisionId = null;

        /**
         * WebMessageInfo decisionSources.
         * @member {Array.<string>} decisionSources
         * @memberof proto.WebMessageInfo
         * @instance
         */
        WebMessageInfo.prototype.decisionSources = $util.emptyArray;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_message", {
            get: $util.oneOfGetter($oneOfFields = ["message"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_messageTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["messageTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_status", {
            get: $util.oneOfGetter($oneOfFields = ["status"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_participant", {
            get: $util.oneOfGetter($oneOfFields = ["participant"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_messageC2STimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["messageC2STimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_ignore", {
            get: $util.oneOfGetter($oneOfFields = ["ignore"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_starred", {
            get: $util.oneOfGetter($oneOfFields = ["starred"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_broadcast", {
            get: $util.oneOfGetter($oneOfFields = ["broadcast"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_pushName", {
            get: $util.oneOfGetter($oneOfFields = ["pushName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_mediaCiphertextSha256", {
            get: $util.oneOfGetter($oneOfFields = ["mediaCiphertextSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_multicast", {
            get: $util.oneOfGetter($oneOfFields = ["multicast"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_urlText", {
            get: $util.oneOfGetter($oneOfFields = ["urlText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_urlNumber", {
            get: $util.oneOfGetter($oneOfFields = ["urlNumber"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_messageStubType", {
            get: $util.oneOfGetter($oneOfFields = ["messageStubType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_clearMedia", {
            get: $util.oneOfGetter($oneOfFields = ["clearMedia"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_duration", {
            get: $util.oneOfGetter($oneOfFields = ["duration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_paymentInfo", {
            get: $util.oneOfGetter($oneOfFields = ["paymentInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_finalLiveLocation", {
            get: $util.oneOfGetter($oneOfFields = ["finalLiveLocation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_quotedPaymentInfo", {
            get: $util.oneOfGetter($oneOfFields = ["quotedPaymentInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_ephemeralStartTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralStartTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_ephemeralDuration", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralDuration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_ephemeralOffToOn", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralOffToOn"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_ephemeralOutOfSync", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralOutOfSync"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_bizPrivacyStatus", {
            get: $util.oneOfGetter($oneOfFields = ["bizPrivacyStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_verifiedBizName", {
            get: $util.oneOfGetter($oneOfFields = ["verifiedBizName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_mediaData", {
            get: $util.oneOfGetter($oneOfFields = ["mediaData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_photoChange", {
            get: $util.oneOfGetter($oneOfFields = ["photoChange"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_quotedStickerData", {
            get: $util.oneOfGetter($oneOfFields = ["quotedStickerData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_futureproofData", {
            get: $util.oneOfGetter($oneOfFields = ["futureproofData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_statusPsa", {
            get: $util.oneOfGetter($oneOfFields = ["statusPsa"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_pollAdditionalMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["pollAdditionalMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_agentId", {
            get: $util.oneOfGetter($oneOfFields = ["agentId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_statusAlreadyViewed", {
            get: $util.oneOfGetter($oneOfFields = ["statusAlreadyViewed"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_messageSecret", {
            get: $util.oneOfGetter($oneOfFields = ["messageSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_keepInChat", {
            get: $util.oneOfGetter($oneOfFields = ["keepInChat"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_originalSelfAuthorUserJidString", {
            get: $util.oneOfGetter($oneOfFields = ["originalSelfAuthorUserJidString"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_revokeMessageTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["revokeMessageTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_pinInChat", {
            get: $util.oneOfGetter($oneOfFields = ["pinInChat"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_premiumMessageInfo", {
            get: $util.oneOfGetter($oneOfFields = ["premiumMessageInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_is1PBizBotMessage", {
            get: $util.oneOfGetter($oneOfFields = ["is1PBizBotMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_isGroupHistoryMessage", {
            get: $util.oneOfGetter($oneOfFields = ["isGroupHistoryMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_botMessageInvokerJid", {
            get: $util.oneOfGetter($oneOfFields = ["botMessageInvokerJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_commentMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["commentMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_reportingTokenInfo", {
            get: $util.oneOfGetter($oneOfFields = ["reportingTokenInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_newsletterServerId", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterServerId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_eventAdditionalMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["eventAdditionalMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_isMentionedInStatus", {
            get: $util.oneOfGetter($oneOfFields = ["isMentionedInStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_targetMessageId", {
            get: $util.oneOfGetter($oneOfFields = ["targetMessageId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_statusMentionMessageInfo", {
            get: $util.oneOfGetter($oneOfFields = ["statusMentionMessageInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_isSupportAiMessage", {
            get: $util.oneOfGetter($oneOfFields = ["isSupportAiMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_botTargetId", {
            get: $util.oneOfGetter($oneOfFields = ["botTargetId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_groupHistoryIndividualMessageInfo", {
            get: $util.oneOfGetter($oneOfFields = ["groupHistoryIndividualMessageInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_groupHistoryBundleInfo", {
            get: $util.oneOfGetter($oneOfFields = ["groupHistoryBundleInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_interactiveMessageAdditionalMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["interactiveMessageAdditionalMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_quarantinedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["quarantinedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_nonJidMentions", {
            get: $util.oneOfGetter($oneOfFields = ["nonJidMentions"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_hsmTag", {
            get: $util.oneOfGetter($oneOfFields = ["hsmTag"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_ephemeralExpirationTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralExpirationTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_scheduledMessageMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["scheduledMessageMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_decisionId", {
            get: $util.oneOfGetter($oneOfFields = ["decisionId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new WebMessageInfo instance using the specified properties.
         * @function create
         * @memberof proto.WebMessageInfo
         * @static
         * @param {proto.IWebMessageInfo=} [properties] Properties to set
         * @returns {proto.WebMessageInfo} WebMessageInfo instance
         */
        WebMessageInfo.create = function create(properties) {
            return new WebMessageInfo(properties);
        };

        /**
         * Encodes the specified WebMessageInfo message. Does not implicitly {@link proto.WebMessageInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.WebMessageInfo
         * @static
         * @param {proto.IWebMessageInfo} message WebMessageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WebMessageInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                $root.proto.MessageKey.encode(message.key, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                $root.proto.Message.encode(message.message, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.messageTimestamp != null && Object.hasOwnProperty.call(message, "messageTimestamp"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.messageTimestamp);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.status);
            if (message.participant != null && Object.hasOwnProperty.call(message, "participant"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.participant);
            if (message.messageC2STimestamp != null && Object.hasOwnProperty.call(message, "messageC2STimestamp"))
                writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.messageC2STimestamp);
            if (message.ignore != null && Object.hasOwnProperty.call(message, "ignore"))
                writer.uint32(/* id 16, wireType 0 =*/128).bool(message.ignore);
            if (message.starred != null && Object.hasOwnProperty.call(message, "starred"))
                writer.uint32(/* id 17, wireType 0 =*/136).bool(message.starred);
            if (message.broadcast != null && Object.hasOwnProperty.call(message, "broadcast"))
                writer.uint32(/* id 18, wireType 0 =*/144).bool(message.broadcast);
            if (message.pushName != null && Object.hasOwnProperty.call(message, "pushName"))
                writer.uint32(/* id 19, wireType 2 =*/154).string(message.pushName);
            if (message.mediaCiphertextSha256 != null && Object.hasOwnProperty.call(message, "mediaCiphertextSha256"))
                writer.uint32(/* id 20, wireType 2 =*/162).bytes(message.mediaCiphertextSha256);
            if (message.multicast != null && Object.hasOwnProperty.call(message, "multicast"))
                writer.uint32(/* id 21, wireType 0 =*/168).bool(message.multicast);
            if (message.urlText != null && Object.hasOwnProperty.call(message, "urlText"))
                writer.uint32(/* id 22, wireType 0 =*/176).bool(message.urlText);
            if (message.urlNumber != null && Object.hasOwnProperty.call(message, "urlNumber"))
                writer.uint32(/* id 23, wireType 0 =*/184).bool(message.urlNumber);
            if (message.messageStubType != null && Object.hasOwnProperty.call(message, "messageStubType"))
                writer.uint32(/* id 24, wireType 0 =*/192).int32(message.messageStubType);
            if (message.clearMedia != null && Object.hasOwnProperty.call(message, "clearMedia"))
                writer.uint32(/* id 25, wireType 0 =*/200).bool(message.clearMedia);
            if (message.messageStubParameters != null && message.messageStubParameters.length)
                for (var i = 0; i < message.messageStubParameters.length; ++i)
                    writer.uint32(/* id 26, wireType 2 =*/210).string(message.messageStubParameters[i]);
            if (message.duration != null && Object.hasOwnProperty.call(message, "duration"))
                writer.uint32(/* id 27, wireType 0 =*/216).uint32(message.duration);
            if (message.labels != null && message.labels.length)
                for (var i = 0; i < message.labels.length; ++i)
                    writer.uint32(/* id 28, wireType 2 =*/226).string(message.labels[i]);
            if (message.paymentInfo != null && Object.hasOwnProperty.call(message, "paymentInfo"))
                $root.proto.PaymentInfo.encode(message.paymentInfo, writer.uint32(/* id 29, wireType 2 =*/234).fork(), q + 1).ldelim();
            if (message.finalLiveLocation != null && Object.hasOwnProperty.call(message, "finalLiveLocation"))
                $root.proto.Message.LiveLocationMessage.encode(message.finalLiveLocation, writer.uint32(/* id 30, wireType 2 =*/242).fork(), q + 1).ldelim();
            if (message.quotedPaymentInfo != null && Object.hasOwnProperty.call(message, "quotedPaymentInfo"))
                $root.proto.PaymentInfo.encode(message.quotedPaymentInfo, writer.uint32(/* id 31, wireType 2 =*/250).fork(), q + 1).ldelim();
            if (message.ephemeralStartTimestamp != null && Object.hasOwnProperty.call(message, "ephemeralStartTimestamp"))
                writer.uint32(/* id 32, wireType 0 =*/256).uint64(message.ephemeralStartTimestamp);
            if (message.ephemeralDuration != null && Object.hasOwnProperty.call(message, "ephemeralDuration"))
                writer.uint32(/* id 33, wireType 0 =*/264).uint32(message.ephemeralDuration);
            if (message.ephemeralOffToOn != null && Object.hasOwnProperty.call(message, "ephemeralOffToOn"))
                writer.uint32(/* id 34, wireType 0 =*/272).bool(message.ephemeralOffToOn);
            if (message.ephemeralOutOfSync != null && Object.hasOwnProperty.call(message, "ephemeralOutOfSync"))
                writer.uint32(/* id 35, wireType 0 =*/280).bool(message.ephemeralOutOfSync);
            if (message.bizPrivacyStatus != null && Object.hasOwnProperty.call(message, "bizPrivacyStatus"))
                writer.uint32(/* id 36, wireType 0 =*/288).int32(message.bizPrivacyStatus);
            if (message.verifiedBizName != null && Object.hasOwnProperty.call(message, "verifiedBizName"))
                writer.uint32(/* id 37, wireType 2 =*/298).string(message.verifiedBizName);
            if (message.mediaData != null && Object.hasOwnProperty.call(message, "mediaData"))
                $root.proto.MediaData.encode(message.mediaData, writer.uint32(/* id 38, wireType 2 =*/306).fork(), q + 1).ldelim();
            if (message.photoChange != null && Object.hasOwnProperty.call(message, "photoChange"))
                $root.proto.PhotoChange.encode(message.photoChange, writer.uint32(/* id 39, wireType 2 =*/314).fork(), q + 1).ldelim();
            if (message.userReceipt != null && message.userReceipt.length)
                for (var i = 0; i < message.userReceipt.length; ++i)
                    $root.proto.UserReceipt.encode(message.userReceipt[i], writer.uint32(/* id 40, wireType 2 =*/322).fork(), q + 1).ldelim();
            if (message.reactions != null && message.reactions.length)
                for (var i = 0; i < message.reactions.length; ++i)
                    $root.proto.Reaction.encode(message.reactions[i], writer.uint32(/* id 41, wireType 2 =*/330).fork(), q + 1).ldelim();
            if (message.quotedStickerData != null && Object.hasOwnProperty.call(message, "quotedStickerData"))
                $root.proto.MediaData.encode(message.quotedStickerData, writer.uint32(/* id 42, wireType 2 =*/338).fork(), q + 1).ldelim();
            if (message.futureproofData != null && Object.hasOwnProperty.call(message, "futureproofData"))
                writer.uint32(/* id 43, wireType 2 =*/346).bytes(message.futureproofData);
            if (message.statusPsa != null && Object.hasOwnProperty.call(message, "statusPsa"))
                $root.proto.StatusPSA.encode(message.statusPsa, writer.uint32(/* id 44, wireType 2 =*/354).fork(), q + 1).ldelim();
            if (message.pollUpdates != null && message.pollUpdates.length)
                for (var i = 0; i < message.pollUpdates.length; ++i)
                    $root.proto.PollUpdate.encode(message.pollUpdates[i], writer.uint32(/* id 45, wireType 2 =*/362).fork(), q + 1).ldelim();
            if (message.pollAdditionalMetadata != null && Object.hasOwnProperty.call(message, "pollAdditionalMetadata"))
                $root.proto.PollAdditionalMetadata.encode(message.pollAdditionalMetadata, writer.uint32(/* id 46, wireType 2 =*/370).fork(), q + 1).ldelim();
            if (message.agentId != null && Object.hasOwnProperty.call(message, "agentId"))
                writer.uint32(/* id 47, wireType 2 =*/378).string(message.agentId);
            if (message.statusAlreadyViewed != null && Object.hasOwnProperty.call(message, "statusAlreadyViewed"))
                writer.uint32(/* id 48, wireType 0 =*/384).bool(message.statusAlreadyViewed);
            if (message.messageSecret != null && Object.hasOwnProperty.call(message, "messageSecret"))
                writer.uint32(/* id 49, wireType 2 =*/394).bytes(message.messageSecret);
            if (message.keepInChat != null && Object.hasOwnProperty.call(message, "keepInChat"))
                $root.proto.KeepInChat.encode(message.keepInChat, writer.uint32(/* id 50, wireType 2 =*/402).fork(), q + 1).ldelim();
            if (message.originalSelfAuthorUserJidString != null && Object.hasOwnProperty.call(message, "originalSelfAuthorUserJidString"))
                writer.uint32(/* id 51, wireType 2 =*/410).string(message.originalSelfAuthorUserJidString);
            if (message.revokeMessageTimestamp != null && Object.hasOwnProperty.call(message, "revokeMessageTimestamp"))
                writer.uint32(/* id 52, wireType 0 =*/416).uint64(message.revokeMessageTimestamp);
            if (message.pinInChat != null && Object.hasOwnProperty.call(message, "pinInChat"))
                $root.proto.PinInChat.encode(message.pinInChat, writer.uint32(/* id 54, wireType 2 =*/434).fork(), q + 1).ldelim();
            if (message.premiumMessageInfo != null && Object.hasOwnProperty.call(message, "premiumMessageInfo"))
                $root.proto.PremiumMessageInfo.encode(message.premiumMessageInfo, writer.uint32(/* id 55, wireType 2 =*/442).fork(), q + 1).ldelim();
            if (message.is1PBizBotMessage != null && Object.hasOwnProperty.call(message, "is1PBizBotMessage"))
                writer.uint32(/* id 56, wireType 0 =*/448).bool(message.is1PBizBotMessage);
            if (message.isGroupHistoryMessage != null && Object.hasOwnProperty.call(message, "isGroupHistoryMessage"))
                writer.uint32(/* id 57, wireType 0 =*/456).bool(message.isGroupHistoryMessage);
            if (message.botMessageInvokerJid != null && Object.hasOwnProperty.call(message, "botMessageInvokerJid"))
                writer.uint32(/* id 58, wireType 2 =*/466).string(message.botMessageInvokerJid);
            if (message.commentMetadata != null && Object.hasOwnProperty.call(message, "commentMetadata"))
                $root.proto.CommentMetadata.encode(message.commentMetadata, writer.uint32(/* id 59, wireType 2 =*/474).fork(), q + 1).ldelim();
            if (message.eventResponses != null && message.eventResponses.length)
                for (var i = 0; i < message.eventResponses.length; ++i)
                    $root.proto.EventResponse.encode(message.eventResponses[i], writer.uint32(/* id 61, wireType 2 =*/490).fork(), q + 1).ldelim();
            if (message.reportingTokenInfo != null && Object.hasOwnProperty.call(message, "reportingTokenInfo"))
                $root.proto.ReportingTokenInfo.encode(message.reportingTokenInfo, writer.uint32(/* id 62, wireType 2 =*/498).fork(), q + 1).ldelim();
            if (message.newsletterServerId != null && Object.hasOwnProperty.call(message, "newsletterServerId"))
                writer.uint32(/* id 63, wireType 0 =*/504).uint64(message.newsletterServerId);
            if (message.eventAdditionalMetadata != null && Object.hasOwnProperty.call(message, "eventAdditionalMetadata"))
                $root.proto.EventAdditionalMetadata.encode(message.eventAdditionalMetadata, writer.uint32(/* id 64, wireType 2 =*/514).fork(), q + 1).ldelim();
            if (message.isMentionedInStatus != null && Object.hasOwnProperty.call(message, "isMentionedInStatus"))
                writer.uint32(/* id 65, wireType 0 =*/520).bool(message.isMentionedInStatus);
            if (message.statusMentions != null && message.statusMentions.length)
                for (var i = 0; i < message.statusMentions.length; ++i)
                    writer.uint32(/* id 66, wireType 2 =*/530).string(message.statusMentions[i]);
            if (message.targetMessageId != null && Object.hasOwnProperty.call(message, "targetMessageId"))
                $root.proto.MessageKey.encode(message.targetMessageId, writer.uint32(/* id 67, wireType 2 =*/538).fork(), q + 1).ldelim();
            if (message.messageAddOns != null && message.messageAddOns.length)
                for (var i = 0; i < message.messageAddOns.length; ++i)
                    $root.proto.MessageAddOn.encode(message.messageAddOns[i], writer.uint32(/* id 68, wireType 2 =*/546).fork(), q + 1).ldelim();
            if (message.statusMentionMessageInfo != null && Object.hasOwnProperty.call(message, "statusMentionMessageInfo"))
                $root.proto.StatusMentionMessage.encode(message.statusMentionMessageInfo, writer.uint32(/* id 69, wireType 2 =*/554).fork(), q + 1).ldelim();
            if (message.isSupportAiMessage != null && Object.hasOwnProperty.call(message, "isSupportAiMessage"))
                writer.uint32(/* id 70, wireType 0 =*/560).bool(message.isSupportAiMessage);
            if (message.statusMentionSources != null && message.statusMentionSources.length)
                for (var i = 0; i < message.statusMentionSources.length; ++i)
                    writer.uint32(/* id 71, wireType 2 =*/570).string(message.statusMentionSources[i]);
            if (message.supportAiCitations != null && message.supportAiCitations.length)
                for (var i = 0; i < message.supportAiCitations.length; ++i)
                    $root.proto.Citation.encode(message.supportAiCitations[i], writer.uint32(/* id 72, wireType 2 =*/578).fork(), q + 1).ldelim();
            if (message.botTargetId != null && Object.hasOwnProperty.call(message, "botTargetId"))
                writer.uint32(/* id 73, wireType 2 =*/586).string(message.botTargetId);
            if (message.groupHistoryIndividualMessageInfo != null && Object.hasOwnProperty.call(message, "groupHistoryIndividualMessageInfo"))
                $root.proto.GroupHistoryIndividualMessageInfo.encode(message.groupHistoryIndividualMessageInfo, writer.uint32(/* id 74, wireType 2 =*/594).fork(), q + 1).ldelim();
            if (message.groupHistoryBundleInfo != null && Object.hasOwnProperty.call(message, "groupHistoryBundleInfo"))
                $root.proto.GroupHistoryBundleInfo.encode(message.groupHistoryBundleInfo, writer.uint32(/* id 75, wireType 2 =*/602).fork(), q + 1).ldelim();
            if (message.interactiveMessageAdditionalMetadata != null && Object.hasOwnProperty.call(message, "interactiveMessageAdditionalMetadata"))
                $root.proto.InteractiveMessageAdditionalMetadata.encode(message.interactiveMessageAdditionalMetadata, writer.uint32(/* id 76, wireType 2 =*/610).fork(), q + 1).ldelim();
            if (message.quarantinedMessage != null && Object.hasOwnProperty.call(message, "quarantinedMessage"))
                $root.proto.QuarantinedMessage.encode(message.quarantinedMessage, writer.uint32(/* id 77, wireType 2 =*/618).fork(), q + 1).ldelim();
            if (message.nonJidMentions != null && Object.hasOwnProperty.call(message, "nonJidMentions"))
                writer.uint32(/* id 78, wireType 0 =*/624).uint32(message.nonJidMentions);
            if (message.hsmTag != null && Object.hasOwnProperty.call(message, "hsmTag"))
                writer.uint32(/* id 79, wireType 2 =*/634).string(message.hsmTag);
            if (message.ephemeralExpirationTimestamp != null && Object.hasOwnProperty.call(message, "ephemeralExpirationTimestamp"))
                writer.uint32(/* id 80, wireType 0 =*/640).uint64(message.ephemeralExpirationTimestamp);
            if (message.scheduledMessageMetadata != null && Object.hasOwnProperty.call(message, "scheduledMessageMetadata"))
                $root.proto.ScheduledMessageMetadata.encode(message.scheduledMessageMetadata, writer.uint32(/* id 81, wireType 2 =*/650).fork(), q + 1).ldelim();
            if (message.decisionId != null && Object.hasOwnProperty.call(message, "decisionId"))
                writer.uint32(/* id 82, wireType 2 =*/658).string(message.decisionId);
            if (message.decisionSources != null && message.decisionSources.length)
                for (var i = 0; i < message.decisionSources.length; ++i)
                    writer.uint32(/* id 83, wireType 2 =*/666).string(message.decisionSources[i]);
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified WebMessageInfo message, length delimited. Does not implicitly {@link proto.WebMessageInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.WebMessageInfo
         * @static
         * @param {proto.IWebMessageInfo} message WebMessageInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WebMessageInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a WebMessageInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.WebMessageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.WebMessageInfo} WebMessageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WebMessageInfo.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C(), v;
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        message.key = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.key);
                        message._key = "key";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        message.message = $root.proto.Message.decode(reader, reader.uint32(), undefined, q + 1, message.message);
                        message._message = "message";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.messageTimestamp = reader.uint64();
                        message._messageTimestamp = "messageTimestamp";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.status = reader.int32();
                        message._status = "status";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        message.participant = reader.stringVerify();
                        message._participant = "participant";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        message.messageC2STimestamp = reader.uint64();
                        message._messageC2STimestamp = "messageC2STimestamp";
                        continue;
                    }
                case 16: {
                        if (u !== 0)
                            break;
                        message.ignore = reader.bool();
                        message._ignore = "ignore";
                        continue;
                    }
                case 17: {
                        if (u !== 0)
                            break;
                        message.starred = reader.bool();
                        message._starred = "starred";
                        continue;
                    }
                case 18: {
                        if (u !== 0)
                            break;
                        message.broadcast = reader.bool();
                        message._broadcast = "broadcast";
                        continue;
                    }
                case 19: {
                        if (u !== 2)
                            break;
                        message.pushName = reader.stringVerify();
                        message._pushName = "pushName";
                        continue;
                    }
                case 20: {
                        if (u !== 2)
                            break;
                        message.mediaCiphertextSha256 = reader.bytes();
                        message._mediaCiphertextSha256 = "mediaCiphertextSha256";
                        continue;
                    }
                case 21: {
                        if (u !== 0)
                            break;
                        message.multicast = reader.bool();
                        message._multicast = "multicast";
                        continue;
                    }
                case 22: {
                        if (u !== 0)
                            break;
                        message.urlText = reader.bool();
                        message._urlText = "urlText";
                        continue;
                    }
                case 23: {
                        if (u !== 0)
                            break;
                        message.urlNumber = reader.bool();
                        message._urlNumber = "urlNumber";
                        continue;
                    }
                case 24: {
                        if (u !== 0)
                            break;
                        message.messageStubType = reader.int32();
                        message._messageStubType = "messageStubType";
                        continue;
                    }
                case 25: {
                        if (u !== 0)
                            break;
                        message.clearMedia = reader.bool();
                        message._clearMedia = "clearMedia";
                        continue;
                    }
                case 26: {
                        if (u !== 2)
                            break;
                        if (!(message.messageStubParameters && message.messageStubParameters.length))
                            message.messageStubParameters = [];
                        message.messageStubParameters.push(reader.stringVerify());
                        continue;
                    }
                case 27: {
                        if (u !== 0)
                            break;
                        message.duration = reader.uint32();
                        message._duration = "duration";
                        continue;
                    }
                case 28: {
                        if (u !== 2)
                            break;
                        if (!(message.labels && message.labels.length))
                            message.labels = [];
                        message.labels.push(reader.stringVerify());
                        continue;
                    }
                case 29: {
                        if (u !== 2)
                            break;
                        message.paymentInfo = $root.proto.PaymentInfo.decode(reader, reader.uint32(), undefined, q + 1, message.paymentInfo);
                        message._paymentInfo = "paymentInfo";
                        continue;
                    }
                case 30: {
                        if (u !== 2)
                            break;
                        message.finalLiveLocation = $root.proto.Message.LiveLocationMessage.decode(reader, reader.uint32(), undefined, q + 1, message.finalLiveLocation);
                        message._finalLiveLocation = "finalLiveLocation";
                        continue;
                    }
                case 31: {
                        if (u !== 2)
                            break;
                        message.quotedPaymentInfo = $root.proto.PaymentInfo.decode(reader, reader.uint32(), undefined, q + 1, message.quotedPaymentInfo);
                        message._quotedPaymentInfo = "quotedPaymentInfo";
                        continue;
                    }
                case 32: {
                        if (u !== 0)
                            break;
                        message.ephemeralStartTimestamp = reader.uint64();
                        message._ephemeralStartTimestamp = "ephemeralStartTimestamp";
                        continue;
                    }
                case 33: {
                        if (u !== 0)
                            break;
                        message.ephemeralDuration = reader.uint32();
                        message._ephemeralDuration = "ephemeralDuration";
                        continue;
                    }
                case 34: {
                        if (u !== 0)
                            break;
                        message.ephemeralOffToOn = reader.bool();
                        message._ephemeralOffToOn = "ephemeralOffToOn";
                        continue;
                    }
                case 35: {
                        if (u !== 0)
                            break;
                        message.ephemeralOutOfSync = reader.bool();
                        message._ephemeralOutOfSync = "ephemeralOutOfSync";
                        continue;
                    }
                case 36: {
                        if (u !== 0)
                            break;
                        message.bizPrivacyStatus = reader.int32();
                        message._bizPrivacyStatus = "bizPrivacyStatus";
                        continue;
                    }
                case 37: {
                        if (u !== 2)
                            break;
                        message.verifiedBizName = reader.stringVerify();
                        message._verifiedBizName = "verifiedBizName";
                        continue;
                    }
                case 38: {
                        if (u !== 2)
                            break;
                        message.mediaData = $root.proto.MediaData.decode(reader, reader.uint32(), undefined, q + 1, message.mediaData);
                        message._mediaData = "mediaData";
                        continue;
                    }
                case 39: {
                        if (u !== 2)
                            break;
                        message.photoChange = $root.proto.PhotoChange.decode(reader, reader.uint32(), undefined, q + 1, message.photoChange);
                        message._photoChange = "photoChange";
                        continue;
                    }
                case 40: {
                        if (u !== 2)
                            break;
                        if (!(message.userReceipt && message.userReceipt.length))
                            message.userReceipt = [];
                        message.userReceipt.push($root.proto.UserReceipt.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 41: {
                        if (u !== 2)
                            break;
                        if (!(message.reactions && message.reactions.length))
                            message.reactions = [];
                        message.reactions.push($root.proto.Reaction.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 42: {
                        if (u !== 2)
                            break;
                        message.quotedStickerData = $root.proto.MediaData.decode(reader, reader.uint32(), undefined, q + 1, message.quotedStickerData);
                        message._quotedStickerData = "quotedStickerData";
                        continue;
                    }
                case 43: {
                        if (u !== 2)
                            break;
                        message.futureproofData = reader.bytes();
                        message._futureproofData = "futureproofData";
                        continue;
                    }
                case 44: {
                        if (u !== 2)
                            break;
                        message.statusPsa = $root.proto.StatusPSA.decode(reader, reader.uint32(), undefined, q + 1, message.statusPsa);
                        message._statusPsa = "statusPsa";
                        continue;
                    }
                case 45: {
                        if (u !== 2)
                            break;
                        if (!(message.pollUpdates && message.pollUpdates.length))
                            message.pollUpdates = [];
                        message.pollUpdates.push($root.proto.PollUpdate.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 46: {
                        if (u !== 2)
                            break;
                        message.pollAdditionalMetadata = $root.proto.PollAdditionalMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.pollAdditionalMetadata);
                        message._pollAdditionalMetadata = "pollAdditionalMetadata";
                        continue;
                    }
                case 47: {
                        if (u !== 2)
                            break;
                        message.agentId = reader.stringVerify();
                        message._agentId = "agentId";
                        continue;
                    }
                case 48: {
                        if (u !== 0)
                            break;
                        message.statusAlreadyViewed = reader.bool();
                        message._statusAlreadyViewed = "statusAlreadyViewed";
                        continue;
                    }
                case 49: {
                        if (u !== 2)
                            break;
                        message.messageSecret = reader.bytes();
                        message._messageSecret = "messageSecret";
                        continue;
                    }
                case 50: {
                        if (u !== 2)
                            break;
                        message.keepInChat = $root.proto.KeepInChat.decode(reader, reader.uint32(), undefined, q + 1, message.keepInChat);
                        message._keepInChat = "keepInChat";
                        continue;
                    }
                case 51: {
                        if (u !== 2)
                            break;
                        message.originalSelfAuthorUserJidString = reader.stringVerify();
                        message._originalSelfAuthorUserJidString = "originalSelfAuthorUserJidString";
                        continue;
                    }
                case 52: {
                        if (u !== 0)
                            break;
                        message.revokeMessageTimestamp = reader.uint64();
                        message._revokeMessageTimestamp = "revokeMessageTimestamp";
                        continue;
                    }
                case 54: {
                        if (u !== 2)
                            break;
                        message.pinInChat = $root.proto.PinInChat.decode(reader, reader.uint32(), undefined, q + 1, message.pinInChat);
                        message._pinInChat = "pinInChat";
                        continue;
                    }
                case 55: {
                        if (u !== 2)
                            break;
                        message.premiumMessageInfo = $root.proto.PremiumMessageInfo.decode(reader, reader.uint32(), undefined, q + 1, message.premiumMessageInfo);
                        message._premiumMessageInfo = "premiumMessageInfo";
                        continue;
                    }
                case 56: {
                        if (u !== 0)
                            break;
                        message.is1PBizBotMessage = reader.bool();
                        message._is1PBizBotMessage = "is1PBizBotMessage";
                        continue;
                    }
                case 57: {
                        if (u !== 0)
                            break;
                        message.isGroupHistoryMessage = reader.bool();
                        message._isGroupHistoryMessage = "isGroupHistoryMessage";
                        continue;
                    }
                case 58: {
                        if (u !== 2)
                            break;
                        message.botMessageInvokerJid = reader.stringVerify();
                        message._botMessageInvokerJid = "botMessageInvokerJid";
                        continue;
                    }
                case 59: {
                        if (u !== 2)
                            break;
                        message.commentMetadata = $root.proto.CommentMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.commentMetadata);
                        message._commentMetadata = "commentMetadata";
                        continue;
                    }
                case 61: {
                        if (u !== 2)
                            break;
                        if (!(message.eventResponses && message.eventResponses.length))
                            message.eventResponses = [];
                        message.eventResponses.push($root.proto.EventResponse.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 62: {
                        if (u !== 2)
                            break;
                        message.reportingTokenInfo = $root.proto.ReportingTokenInfo.decode(reader, reader.uint32(), undefined, q + 1, message.reportingTokenInfo);
                        message._reportingTokenInfo = "reportingTokenInfo";
                        continue;
                    }
                case 63: {
                        if (u !== 0)
                            break;
                        message.newsletterServerId = reader.uint64();
                        message._newsletterServerId = "newsletterServerId";
                        continue;
                    }
                case 64: {
                        if (u !== 2)
                            break;
                        message.eventAdditionalMetadata = $root.proto.EventAdditionalMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.eventAdditionalMetadata);
                        message._eventAdditionalMetadata = "eventAdditionalMetadata";
                        continue;
                    }
                case 65: {
                        if (u !== 0)
                            break;
                        message.isMentionedInStatus = reader.bool();
                        message._isMentionedInStatus = "isMentionedInStatus";
                        continue;
                    }
                case 66: {
                        if (u !== 2)
                            break;
                        if (!(message.statusMentions && message.statusMentions.length))
                            message.statusMentions = [];
                        message.statusMentions.push(reader.stringVerify());
                        continue;
                    }
                case 67: {
                        if (u !== 2)
                            break;
                        message.targetMessageId = $root.proto.MessageKey.decode(reader, reader.uint32(), undefined, q + 1, message.targetMessageId);
                        message._targetMessageId = "targetMessageId";
                        continue;
                    }
                case 68: {
                        if (u !== 2)
                            break;
                        if (!(message.messageAddOns && message.messageAddOns.length))
                            message.messageAddOns = [];
                        message.messageAddOns.push($root.proto.MessageAddOn.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 69: {
                        if (u !== 2)
                            break;
                        message.statusMentionMessageInfo = $root.proto.StatusMentionMessage.decode(reader, reader.uint32(), undefined, q + 1, message.statusMentionMessageInfo);
                        message._statusMentionMessageInfo = "statusMentionMessageInfo";
                        continue;
                    }
                case 70: {
                        if (u !== 0)
                            break;
                        message.isSupportAiMessage = reader.bool();
                        message._isSupportAiMessage = "isSupportAiMessage";
                        continue;
                    }
                case 71: {
                        if (u !== 2)
                            break;
                        if (!(message.statusMentionSources && message.statusMentionSources.length))
                            message.statusMentionSources = [];
                        message.statusMentionSources.push(reader.stringVerify());
                        continue;
                    }
                case 72: {
                        if (u !== 2)
                            break;
                        if (!(message.supportAiCitations && message.supportAiCitations.length))
                            message.supportAiCitations = [];
                        message.supportAiCitations.push($root.proto.Citation.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                case 73: {
                        if (u !== 2)
                            break;
                        message.botTargetId = reader.stringVerify();
                        message._botTargetId = "botTargetId";
                        continue;
                    }
                case 74: {
                        if (u !== 2)
                            break;
                        message.groupHistoryIndividualMessageInfo = $root.proto.GroupHistoryIndividualMessageInfo.decode(reader, reader.uint32(), undefined, q + 1, message.groupHistoryIndividualMessageInfo);
                        message._groupHistoryIndividualMessageInfo = "groupHistoryIndividualMessageInfo";
                        continue;
                    }
                case 75: {
                        if (u !== 2)
                            break;
                        message.groupHistoryBundleInfo = $root.proto.GroupHistoryBundleInfo.decode(reader, reader.uint32(), undefined, q + 1, message.groupHistoryBundleInfo);
                        message._groupHistoryBundleInfo = "groupHistoryBundleInfo";
                        continue;
                    }
                case 76: {
                        if (u !== 2)
                            break;
                        message.interactiveMessageAdditionalMetadata = $root.proto.InteractiveMessageAdditionalMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.interactiveMessageAdditionalMetadata);
                        message._interactiveMessageAdditionalMetadata = "interactiveMessageAdditionalMetadata";
                        continue;
                    }
                case 77: {
                        if (u !== 2)
                            break;
                        message.quarantinedMessage = $root.proto.QuarantinedMessage.decode(reader, reader.uint32(), undefined, q + 1, message.quarantinedMessage);
                        message._quarantinedMessage = "quarantinedMessage";
                        continue;
                    }
                case 78: {
                        if (u !== 0)
                            break;
                        message.nonJidMentions = reader.uint32();
                        message._nonJidMentions = "nonJidMentions";
                        continue;
                    }
                case 79: {
                        if (u !== 2)
                            break;
                        message.hsmTag = reader.stringVerify();
                        message._hsmTag = "hsmTag";
                        continue;
                    }
                case 80: {
                        if (u !== 0)
                            break;
                        message.ephemeralExpirationTimestamp = reader.uint64();
                        message._ephemeralExpirationTimestamp = "ephemeralExpirationTimestamp";
                        continue;
                    }
                case 81: {
                        if (u !== 2)
                            break;
                        message.scheduledMessageMetadata = $root.proto.ScheduledMessageMetadata.decode(reader, reader.uint32(), undefined, q + 1, message.scheduledMessageMetadata);
                        message._scheduledMessageMetadata = "scheduledMessageMetadata";
                        continue;
                    }
                case 82: {
                        if (u !== 2)
                            break;
                        message.decisionId = reader.stringVerify();
                        message._decisionId = "decisionId";
                        continue;
                    }
                case 83: {
                        if (u !== 2)
                            break;
                        if (!(message.decisionSources && message.decisionSources.length))
                            message.decisionSources = [];
                        message.decisionSources.push(reader.stringVerify());
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a WebMessageInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.WebMessageInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.WebMessageInfo} WebMessageInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WebMessageInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WebMessageInfo message.
         * @function verify
         * @memberof proto.WebMessageInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WebMessageInfo.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.key != null && Object.hasOwnProperty.call(message, "key")) {
                properties._key = 1;
                {
                    var error = $root.proto.MessageKey.verify(message.key, q + 1);
                    if (error)
                        return "key." + error;
                }
            }
            if (message.message != null && Object.hasOwnProperty.call(message, "message")) {
                properties._message = 1;
                {
                    var error = $root.proto.Message.verify(message.message, q + 1);
                    if (error)
                        return "message." + error;
                }
            }
            if (message.messageTimestamp != null && Object.hasOwnProperty.call(message, "messageTimestamp")) {
                properties._messageTimestamp = 1;
                if (!$util.isInteger(message.messageTimestamp) && !(message.messageTimestamp && $util.isInteger(message.messageTimestamp.low) && $util.isInteger(message.messageTimestamp.high)))
                    return "messageTimestamp: integer|Long expected";
            }
            if (message.status != null && Object.hasOwnProperty.call(message, "status")) {
                properties._status = 1;
                if (typeof message.status !== "number" || (message.status | 0) !== message.status)
                    return "status: enum value expected";
            }
            if (message.participant != null && Object.hasOwnProperty.call(message, "participant")) {
                properties._participant = 1;
                if (!$util.isString(message.participant))
                    return "participant: string expected";
            }
            if (message.messageC2STimestamp != null && Object.hasOwnProperty.call(message, "messageC2STimestamp")) {
                properties._messageC2STimestamp = 1;
                if (!$util.isInteger(message.messageC2STimestamp) && !(message.messageC2STimestamp && $util.isInteger(message.messageC2STimestamp.low) && $util.isInteger(message.messageC2STimestamp.high)))
                    return "messageC2STimestamp: integer|Long expected";
            }
            if (message.ignore != null && Object.hasOwnProperty.call(message, "ignore")) {
                properties._ignore = 1;
                if (typeof message.ignore !== "boolean")
                    return "ignore: boolean expected";
            }
            if (message.starred != null && Object.hasOwnProperty.call(message, "starred")) {
                properties._starred = 1;
                if (typeof message.starred !== "boolean")
                    return "starred: boolean expected";
            }
            if (message.broadcast != null && Object.hasOwnProperty.call(message, "broadcast")) {
                properties._broadcast = 1;
                if (typeof message.broadcast !== "boolean")
                    return "broadcast: boolean expected";
            }
            if (message.pushName != null && Object.hasOwnProperty.call(message, "pushName")) {
                properties._pushName = 1;
                if (!$util.isString(message.pushName))
                    return "pushName: string expected";
            }
            if (message.mediaCiphertextSha256 != null && Object.hasOwnProperty.call(message, "mediaCiphertextSha256")) {
                properties._mediaCiphertextSha256 = 1;
                if (!(message.mediaCiphertextSha256 && typeof message.mediaCiphertextSha256.length === "number" || $util.isString(message.mediaCiphertextSha256)))
                    return "mediaCiphertextSha256: buffer expected";
            }
            if (message.multicast != null && Object.hasOwnProperty.call(message, "multicast")) {
                properties._multicast = 1;
                if (typeof message.multicast !== "boolean")
                    return "multicast: boolean expected";
            }
            if (message.urlText != null && Object.hasOwnProperty.call(message, "urlText")) {
                properties._urlText = 1;
                if (typeof message.urlText !== "boolean")
                    return "urlText: boolean expected";
            }
            if (message.urlNumber != null && Object.hasOwnProperty.call(message, "urlNumber")) {
                properties._urlNumber = 1;
                if (typeof message.urlNumber !== "boolean")
                    return "urlNumber: boolean expected";
            }
            if (message.messageStubType != null && Object.hasOwnProperty.call(message, "messageStubType")) {
                properties._messageStubType = 1;
                if (typeof message.messageStubType !== "number" || (message.messageStubType | 0) !== message.messageStubType)
                    return "messageStubType: enum value expected";
            }
            if (message.clearMedia != null && Object.hasOwnProperty.call(message, "clearMedia")) {
                properties._clearMedia = 1;
                if (typeof message.clearMedia !== "boolean")
                    return "clearMedia: boolean expected";
            }
            if (message.messageStubParameters != null && Object.hasOwnProperty.call(message, "messageStubParameters")) {
                if (!Array.isArray(message.messageStubParameters))
                    return "messageStubParameters: array expected";
                for (var i = 0; i < message.messageStubParameters.length; ++i)
                    if (!$util.isString(message.messageStubParameters[i]))
                        return "messageStubParameters: string[] expected";
            }
            if (message.duration != null && Object.hasOwnProperty.call(message, "duration")) {
                properties._duration = 1;
                if (!$util.isInteger(message.duration))
                    return "duration: integer expected";
            }
            if (message.labels != null && Object.hasOwnProperty.call(message, "labels")) {
                if (!Array.isArray(message.labels))
                    return "labels: array expected";
                for (var i = 0; i < message.labels.length; ++i)
                    if (!$util.isString(message.labels[i]))
                        return "labels: string[] expected";
            }
            if (message.paymentInfo != null && Object.hasOwnProperty.call(message, "paymentInfo")) {
                properties._paymentInfo = 1;
                {
                    var error = $root.proto.PaymentInfo.verify(message.paymentInfo, q + 1);
                    if (error)
                        return "paymentInfo." + error;
                }
            }
            if (message.finalLiveLocation != null && Object.hasOwnProperty.call(message, "finalLiveLocation")) {
                properties._finalLiveLocation = 1;
                {
                    var error = $root.proto.Message.LiveLocationMessage.verify(message.finalLiveLocation, q + 1);
                    if (error)
                        return "finalLiveLocation." + error;
                }
            }
            if (message.quotedPaymentInfo != null && Object.hasOwnProperty.call(message, "quotedPaymentInfo")) {
                properties._quotedPaymentInfo = 1;
                {
                    var error = $root.proto.PaymentInfo.verify(message.quotedPaymentInfo, q + 1);
                    if (error)
                        return "quotedPaymentInfo." + error;
                }
            }
            if (message.ephemeralStartTimestamp != null && Object.hasOwnProperty.call(message, "ephemeralStartTimestamp")) {
                properties._ephemeralStartTimestamp = 1;
                if (!$util.isInteger(message.ephemeralStartTimestamp) && !(message.ephemeralStartTimestamp && $util.isInteger(message.ephemeralStartTimestamp.low) && $util.isInteger(message.ephemeralStartTimestamp.high)))
                    return "ephemeralStartTimestamp: integer|Long expected";
            }
            if (message.ephemeralDuration != null && Object.hasOwnProperty.call(message, "ephemeralDuration")) {
                properties._ephemeralDuration = 1;
                if (!$util.isInteger(message.ephemeralDuration))
                    return "ephemeralDuration: integer expected";
            }
            if (message.ephemeralOffToOn != null && Object.hasOwnProperty.call(message, "ephemeralOffToOn")) {
                properties._ephemeralOffToOn = 1;
                if (typeof message.ephemeralOffToOn !== "boolean")
                    return "ephemeralOffToOn: boolean expected";
            }
            if (message.ephemeralOutOfSync != null && Object.hasOwnProperty.call(message, "ephemeralOutOfSync")) {
                properties._ephemeralOutOfSync = 1;
                if (typeof message.ephemeralOutOfSync !== "boolean")
                    return "ephemeralOutOfSync: boolean expected";
            }
            if (message.bizPrivacyStatus != null && Object.hasOwnProperty.call(message, "bizPrivacyStatus")) {
                properties._bizPrivacyStatus = 1;
                if (typeof message.bizPrivacyStatus !== "number" || (message.bizPrivacyStatus | 0) !== message.bizPrivacyStatus)
                    return "bizPrivacyStatus: enum value expected";
            }
            if (message.verifiedBizName != null && Object.hasOwnProperty.call(message, "verifiedBizName")) {
                properties._verifiedBizName = 1;
                if (!$util.isString(message.verifiedBizName))
                    return "verifiedBizName: string expected";
            }
            if (message.mediaData != null && Object.hasOwnProperty.call(message, "mediaData")) {
                properties._mediaData = 1;
                {
                    var error = $root.proto.MediaData.verify(message.mediaData, q + 1);
                    if (error)
                        return "mediaData." + error;
                }
            }
            if (message.photoChange != null && Object.hasOwnProperty.call(message, "photoChange")) {
                properties._photoChange = 1;
                {
                    var error = $root.proto.PhotoChange.verify(message.photoChange, q + 1);
                    if (error)
                        return "photoChange." + error;
                }
            }
            if (message.userReceipt != null && Object.hasOwnProperty.call(message, "userReceipt")) {
                if (!Array.isArray(message.userReceipt))
                    return "userReceipt: array expected";
                for (var i = 0; i < message.userReceipt.length; ++i) {
                    var error = $root.proto.UserReceipt.verify(message.userReceipt[i], q + 1);
                    if (error)
                        return "userReceipt." + error;
                }
            }
            if (message.reactions != null && Object.hasOwnProperty.call(message, "reactions")) {
                if (!Array.isArray(message.reactions))
                    return "reactions: array expected";
                for (var i = 0; i < message.reactions.length; ++i) {
                    var error = $root.proto.Reaction.verify(message.reactions[i], q + 1);
                    if (error)
                        return "reactions." + error;
                }
            }
            if (message.quotedStickerData != null && Object.hasOwnProperty.call(message, "quotedStickerData")) {
                properties._quotedStickerData = 1;
                {
                    var error = $root.proto.MediaData.verify(message.quotedStickerData, q + 1);
                    if (error)
                        return "quotedStickerData." + error;
                }
            }
            if (message.futureproofData != null && Object.hasOwnProperty.call(message, "futureproofData")) {
                properties._futureproofData = 1;
                if (!(message.futureproofData && typeof message.futureproofData.length === "number" || $util.isString(message.futureproofData)))
                    return "futureproofData: buffer expected";
            }
            if (message.statusPsa != null && Object.hasOwnProperty.call(message, "statusPsa")) {
                properties._statusPsa = 1;
                {
                    var error = $root.proto.StatusPSA.verify(message.statusPsa, q + 1);
                    if (error)
                        return "statusPsa." + error;
                }
            }
            if (message.pollUpdates != null && Object.hasOwnProperty.call(message, "pollUpdates")) {
                if (!Array.isArray(message.pollUpdates))
                    return "pollUpdates: array expected";
                for (var i = 0; i < message.pollUpdates.length; ++i) {
                    var error = $root.proto.PollUpdate.verify(message.pollUpdates[i], q + 1);
                    if (error)
                        return "pollUpdates." + error;
                }
            }
            if (message.pollAdditionalMetadata != null && Object.hasOwnProperty.call(message, "pollAdditionalMetadata")) {
                properties._pollAdditionalMetadata = 1;
                {
                    var error = $root.proto.PollAdditionalMetadata.verify(message.pollAdditionalMetadata, q + 1);
                    if (error)
                        return "pollAdditionalMetadata." + error;
                }
            }
            if (message.agentId != null && Object.hasOwnProperty.call(message, "agentId")) {
                properties._agentId = 1;
                if (!$util.isString(message.agentId))
                    return "agentId: string expected";
            }
            if (message.statusAlreadyViewed != null && Object.hasOwnProperty.call(message, "statusAlreadyViewed")) {
                properties._statusAlreadyViewed = 1;
                if (typeof message.statusAlreadyViewed !== "boolean")
                    return "statusAlreadyViewed: boolean expected";
            }
            if (message.messageSecret != null && Object.hasOwnProperty.call(message, "messageSecret")) {
                properties._messageSecret = 1;
                if (!(message.messageSecret && typeof message.messageSecret.length === "number" || $util.isString(message.messageSecret)))
                    return "messageSecret: buffer expected";
            }
            if (message.keepInChat != null && Object.hasOwnProperty.call(message, "keepInChat")) {
                properties._keepInChat = 1;
                {
                    var error = $root.proto.KeepInChat.verify(message.keepInChat, q + 1);
                    if (error)
                        return "keepInChat." + error;
                }
            }
            if (message.originalSelfAuthorUserJidString != null && Object.hasOwnProperty.call(message, "originalSelfAuthorUserJidString")) {
                properties._originalSelfAuthorUserJidString = 1;
                if (!$util.isString(message.originalSelfAuthorUserJidString))
                    return "originalSelfAuthorUserJidString: string expected";
            }
            if (message.revokeMessageTimestamp != null && Object.hasOwnProperty.call(message, "revokeMessageTimestamp")) {
                properties._revokeMessageTimestamp = 1;
                if (!$util.isInteger(message.revokeMessageTimestamp) && !(message.revokeMessageTimestamp && $util.isInteger(message.revokeMessageTimestamp.low) && $util.isInteger(message.revokeMessageTimestamp.high)))
                    return "revokeMessageTimestamp: integer|Long expected";
            }
            if (message.pinInChat != null && Object.hasOwnProperty.call(message, "pinInChat")) {
                properties._pinInChat = 1;
                {
                    var error = $root.proto.PinInChat.verify(message.pinInChat, q + 1);
                    if (error)
                        return "pinInChat." + error;
                }
            }
            if (message.premiumMessageInfo != null && Object.hasOwnProperty.call(message, "premiumMessageInfo")) {
                properties._premiumMessageInfo = 1;
                {
                    var error = $root.proto.PremiumMessageInfo.verify(message.premiumMessageInfo, q + 1);
                    if (error)
                        return "premiumMessageInfo." + error;
                }
            }
            if (message.is1PBizBotMessage != null && Object.hasOwnProperty.call(message, "is1PBizBotMessage")) {
                properties._is1PBizBotMessage = 1;
                if (typeof message.is1PBizBotMessage !== "boolean")
                    return "is1PBizBotMessage: boolean expected";
            }
            if (message.isGroupHistoryMessage != null && Object.hasOwnProperty.call(message, "isGroupHistoryMessage")) {
                properties._isGroupHistoryMessage = 1;
                if (typeof message.isGroupHistoryMessage !== "boolean")
                    return "isGroupHistoryMessage: boolean expected";
            }
            if (message.botMessageInvokerJid != null && Object.hasOwnProperty.call(message, "botMessageInvokerJid")) {
                properties._botMessageInvokerJid = 1;
                if (!$util.isString(message.botMessageInvokerJid))
                    return "botMessageInvokerJid: string expected";
            }
            if (message.commentMetadata != null && Object.hasOwnProperty.call(message, "commentMetadata")) {
                properties._commentMetadata = 1;
                {
                    var error = $root.proto.CommentMetadata.verify(message.commentMetadata, q + 1);
                    if (error)
                        return "commentMetadata." + error;
                }
            }
            if (message.eventResponses != null && Object.hasOwnProperty.call(message, "eventResponses")) {
                if (!Array.isArray(message.eventResponses))
                    return "eventResponses: array expected";
                for (var i = 0; i < message.eventResponses.length; ++i) {
                    var error = $root.proto.EventResponse.verify(message.eventResponses[i], q + 1);
                    if (error)
                        return "eventResponses." + error;
                }
            }
            if (message.reportingTokenInfo != null && Object.hasOwnProperty.call(message, "reportingTokenInfo")) {
                properties._reportingTokenInfo = 1;
                {
                    var error = $root.proto.ReportingTokenInfo.verify(message.reportingTokenInfo, q + 1);
                    if (error)
                        return "reportingTokenInfo." + error;
                }
            }
            if (message.newsletterServerId != null && Object.hasOwnProperty.call(message, "newsletterServerId")) {
                properties._newsletterServerId = 1;
                if (!$util.isInteger(message.newsletterServerId) && !(message.newsletterServerId && $util.isInteger(message.newsletterServerId.low) && $util.isInteger(message.newsletterServerId.high)))
                    return "newsletterServerId: integer|Long expected";
            }
            if (message.eventAdditionalMetadata != null && Object.hasOwnProperty.call(message, "eventAdditionalMetadata")) {
                properties._eventAdditionalMetadata = 1;
                {
                    var error = $root.proto.EventAdditionalMetadata.verify(message.eventAdditionalMetadata, q + 1);
                    if (error)
                        return "eventAdditionalMetadata." + error;
                }
            }
            if (message.isMentionedInStatus != null && Object.hasOwnProperty.call(message, "isMentionedInStatus")) {
                properties._isMentionedInStatus = 1;
                if (typeof message.isMentionedInStatus !== "boolean")
                    return "isMentionedInStatus: boolean expected";
            }
            if (message.statusMentions != null && Object.hasOwnProperty.call(message, "statusMentions")) {
                if (!Array.isArray(message.statusMentions))
                    return "statusMentions: array expected";
                for (var i = 0; i < message.statusMentions.length; ++i)
                    if (!$util.isString(message.statusMentions[i]))
                        return "statusMentions: string[] expected";
            }
            if (message.targetMessageId != null && Object.hasOwnProperty.call(message, "targetMessageId")) {
                properties._targetMessageId = 1;
                {
                    var error = $root.proto.MessageKey.verify(message.targetMessageId, q + 1);
                    if (error)
                        return "targetMessageId." + error;
                }
            }
            if (message.messageAddOns != null && Object.hasOwnProperty.call(message, "messageAddOns")) {
                if (!Array.isArray(message.messageAddOns))
                    return "messageAddOns: array expected";
                for (var i = 0; i < message.messageAddOns.length; ++i) {
                    var error = $root.proto.MessageAddOn.verify(message.messageAddOns[i], q + 1);
                    if (error)
                        return "messageAddOns." + error;
                }
            }
            if (message.statusMentionMessageInfo != null && Object.hasOwnProperty.call(message, "statusMentionMessageInfo")) {
                properties._statusMentionMessageInfo = 1;
                {
                    var error = $root.proto.StatusMentionMessage.verify(message.statusMentionMessageInfo, q + 1);
                    if (error)
                        return "statusMentionMessageInfo." + error;
                }
            }
            if (message.isSupportAiMessage != null && Object.hasOwnProperty.call(message, "isSupportAiMessage")) {
                properties._isSupportAiMessage = 1;
                if (typeof message.isSupportAiMessage !== "boolean")
                    return "isSupportAiMessage: boolean expected";
            }
            if (message.statusMentionSources != null && Object.hasOwnProperty.call(message, "statusMentionSources")) {
                if (!Array.isArray(message.statusMentionSources))
                    return "statusMentionSources: array expected";
                for (var i = 0; i < message.statusMentionSources.length; ++i)
                    if (!$util.isString(message.statusMentionSources[i]))
                        return "statusMentionSources: string[] expected";
            }
            if (message.supportAiCitations != null && Object.hasOwnProperty.call(message, "supportAiCitations")) {
                if (!Array.isArray(message.supportAiCitations))
                    return "supportAiCitations: array expected";
                for (var i = 0; i < message.supportAiCitations.length; ++i) {
                    var error = $root.proto.Citation.verify(message.supportAiCitations[i], q + 1);
                    if (error)
                        return "supportAiCitations." + error;
                }
            }
            if (message.botTargetId != null && Object.hasOwnProperty.call(message, "botTargetId")) {
                properties._botTargetId = 1;
                if (!$util.isString(message.botTargetId))
                    return "botTargetId: string expected";
            }
            if (message.groupHistoryIndividualMessageInfo != null && Object.hasOwnProperty.call(message, "groupHistoryIndividualMessageInfo")) {
                properties._groupHistoryIndividualMessageInfo = 1;
                {
                    var error = $root.proto.GroupHistoryIndividualMessageInfo.verify(message.groupHistoryIndividualMessageInfo, q + 1);
                    if (error)
                        return "groupHistoryIndividualMessageInfo." + error;
                }
            }
            if (message.groupHistoryBundleInfo != null && Object.hasOwnProperty.call(message, "groupHistoryBundleInfo")) {
                properties._groupHistoryBundleInfo = 1;
                {
                    var error = $root.proto.GroupHistoryBundleInfo.verify(message.groupHistoryBundleInfo, q + 1);
                    if (error)
                        return "groupHistoryBundleInfo." + error;
                }
            }
            if (message.interactiveMessageAdditionalMetadata != null && Object.hasOwnProperty.call(message, "interactiveMessageAdditionalMetadata")) {
                properties._interactiveMessageAdditionalMetadata = 1;
                {
                    var error = $root.proto.InteractiveMessageAdditionalMetadata.verify(message.interactiveMessageAdditionalMetadata, q + 1);
                    if (error)
                        return "interactiveMessageAdditionalMetadata." + error;
                }
            }
            if (message.quarantinedMessage != null && Object.hasOwnProperty.call(message, "quarantinedMessage")) {
                properties._quarantinedMessage = 1;
                {
                    var error = $root.proto.QuarantinedMessage.verify(message.quarantinedMessage, q + 1);
                    if (error)
                        return "quarantinedMessage." + error;
                }
            }
            if (message.nonJidMentions != null && Object.hasOwnProperty.call(message, "nonJidMentions")) {
                properties._nonJidMentions = 1;
                if (!$util.isInteger(message.nonJidMentions))
                    return "nonJidMentions: integer expected";
            }
            if (message.hsmTag != null && Object.hasOwnProperty.call(message, "hsmTag")) {
                properties._hsmTag = 1;
                if (!$util.isString(message.hsmTag))
                    return "hsmTag: string expected";
            }
            if (message.ephemeralExpirationTimestamp != null && Object.hasOwnProperty.call(message, "ephemeralExpirationTimestamp")) {
                properties._ephemeralExpirationTimestamp = 1;
                if (!$util.isInteger(message.ephemeralExpirationTimestamp) && !(message.ephemeralExpirationTimestamp && $util.isInteger(message.ephemeralExpirationTimestamp.low) && $util.isInteger(message.ephemeralExpirationTimestamp.high)))
                    return "ephemeralExpirationTimestamp: integer|Long expected";
            }
            if (message.scheduledMessageMetadata != null && Object.hasOwnProperty.call(message, "scheduledMessageMetadata")) {
                properties._scheduledMessageMetadata = 1;
                {
                    var error = $root.proto.ScheduledMessageMetadata.verify(message.scheduledMessageMetadata, q + 1);
                    if (error)
                        return "scheduledMessageMetadata." + error;
                }
            }
            if (message.decisionId != null && Object.hasOwnProperty.call(message, "decisionId")) {
                properties._decisionId = 1;
                if (!$util.isString(message.decisionId))
                    return "decisionId: string expected";
            }
            if (message.decisionSources != null && Object.hasOwnProperty.call(message, "decisionSources")) {
                if (!Array.isArray(message.decisionSources))
                    return "decisionSources: array expected";
                for (var i = 0; i < message.decisionSources.length; ++i)
                    if (!$util.isString(message.decisionSources[i]))
                        return "decisionSources: string[] expected";
            }
            return null;
        };

        /**
         * Creates a WebMessageInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.WebMessageInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.WebMessageInfo} WebMessageInfo
         */
        WebMessageInfo.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.WebMessageInfo: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.key != null) {
                if (!$util.isObject(object.key))
                    throw TypeError(".proto.WebMessageInfo.key: object expected");
                message.key = $root.proto.MessageKey.fromObject(object.key, q + 1);
            }
            if (object.message != null) {
                if (!$util.isObject(object.message))
                    throw TypeError(".proto.WebMessageInfo.message: object expected");
                message.message = $root.proto.Message.fromObject(object.message, q + 1);
            }
            if (object.messageTimestamp != null)
                if ($util.Long)
                    message.messageTimestamp = $util.Long.fromValue(object.messageTimestamp, true);
                else if (typeof object.messageTimestamp === "string")
                    message.messageTimestamp = parseInt(object.messageTimestamp, 10);
                else if (typeof object.messageTimestamp === "number")
                    message.messageTimestamp = object.messageTimestamp;
                else if (typeof object.messageTimestamp === "object")
                    message.messageTimestamp = new $util.LongBits(object.messageTimestamp.low >>> 0, object.messageTimestamp.high >>> 0).toNumber(true);
            switch (object.status) {
            case "ERROR":
            case 0:
                message.status = 0;
                break;
            case "PENDING":
            case 1:
                message.status = 1;
                break;
            case "SERVER_ACK":
            case 2:
                message.status = 2;
                break;
            case "DELIVERY_ACK":
            case 3:
                message.status = 3;
                break;
            case "READ":
            case 4:
                message.status = 4;
                break;
            case "PLAYED":
            case 5:
                message.status = 5;
                break;
            default:
                if (typeof object.status === "number" && (object.status | 0) === object.status)
                    message.status = object.status;
            }
            if (object.participant != null)
                message.participant = String(object.participant);
            if (object.messageC2STimestamp != null)
                if ($util.Long)
                    message.messageC2STimestamp = $util.Long.fromValue(object.messageC2STimestamp, true);
                else if (typeof object.messageC2STimestamp === "string")
                    message.messageC2STimestamp = parseInt(object.messageC2STimestamp, 10);
                else if (typeof object.messageC2STimestamp === "number")
                    message.messageC2STimestamp = object.messageC2STimestamp;
                else if (typeof object.messageC2STimestamp === "object")
                    message.messageC2STimestamp = new $util.LongBits(object.messageC2STimestamp.low >>> 0, object.messageC2STimestamp.high >>> 0).toNumber(true);
            if (object.ignore != null)
                message.ignore = Boolean(object.ignore);
            if (object.starred != null)
                message.starred = Boolean(object.starred);
            if (object.broadcast != null)
                message.broadcast = Boolean(object.broadcast);
            if (object.pushName != null)
                message.pushName = String(object.pushName);
            if (object.mediaCiphertextSha256 != null)
                if (typeof object.mediaCiphertextSha256 === "string")
                    $util.base64.decode(object.mediaCiphertextSha256, message.mediaCiphertextSha256 = $util.newBuffer($util.base64.length(object.mediaCiphertextSha256)), 0);
                else if (object.mediaCiphertextSha256.length >= 0)
                    message.mediaCiphertextSha256 = object.mediaCiphertextSha256;
            if (object.multicast != null)
                message.multicast = Boolean(object.multicast);
            if (object.urlText != null)
                message.urlText = Boolean(object.urlText);
            if (object.urlNumber != null)
                message.urlNumber = Boolean(object.urlNumber);
            switch (object.messageStubType) {
            case "UNKNOWN":
            case 0:
                message.messageStubType = 0;
                break;
            case "REVOKE":
            case 1:
                message.messageStubType = 1;
                break;
            case "CIPHERTEXT":
            case 2:
                message.messageStubType = 2;
                break;
            case "FUTUREPROOF":
            case 3:
                message.messageStubType = 3;
                break;
            case "NON_VERIFIED_TRANSITION":
            case 4:
                message.messageStubType = 4;
                break;
            case "UNVERIFIED_TRANSITION":
            case 5:
                message.messageStubType = 5;
                break;
            case "VERIFIED_TRANSITION":
            case 6:
                message.messageStubType = 6;
                break;
            case "VERIFIED_LOW_UNKNOWN":
            case 7:
                message.messageStubType = 7;
                break;
            case "VERIFIED_HIGH":
            case 8:
                message.messageStubType = 8;
                break;
            case "VERIFIED_INITIAL_UNKNOWN":
            case 9:
                message.messageStubType = 9;
                break;
            case "VERIFIED_INITIAL_LOW":
            case 10:
                message.messageStubType = 10;
                break;
            case "VERIFIED_INITIAL_HIGH":
            case 11:
                message.messageStubType = 11;
                break;
            case "VERIFIED_TRANSITION_ANY_TO_NONE":
            case 12:
                message.messageStubType = 12;
                break;
            case "VERIFIED_TRANSITION_ANY_TO_HIGH":
            case 13:
                message.messageStubType = 13;
                break;
            case "VERIFIED_TRANSITION_HIGH_TO_LOW":
            case 14:
                message.messageStubType = 14;
                break;
            case "VERIFIED_TRANSITION_HIGH_TO_UNKNOWN":
            case 15:
                message.messageStubType = 15;
                break;
            case "VERIFIED_TRANSITION_UNKNOWN_TO_LOW":
            case 16:
                message.messageStubType = 16;
                break;
            case "VERIFIED_TRANSITION_LOW_TO_UNKNOWN":
            case 17:
                message.messageStubType = 17;
                break;
            case "VERIFIED_TRANSITION_NONE_TO_LOW":
            case 18:
                message.messageStubType = 18;
                break;
            case "VERIFIED_TRANSITION_NONE_TO_UNKNOWN":
            case 19:
                message.messageStubType = 19;
                break;
            case "GROUP_CREATE":
            case 20:
                message.messageStubType = 20;
                break;
            case "GROUP_CHANGE_SUBJECT":
            case 21:
                message.messageStubType = 21;
                break;
            case "GROUP_CHANGE_ICON":
            case 22:
                message.messageStubType = 22;
                break;
            case "GROUP_CHANGE_INVITE_LINK":
            case 23:
                message.messageStubType = 23;
                break;
            case "GROUP_CHANGE_DESCRIPTION":
            case 24:
                message.messageStubType = 24;
                break;
            case "GROUP_CHANGE_RESTRICT":
            case 25:
                message.messageStubType = 25;
                break;
            case "GROUP_CHANGE_ANNOUNCE":
            case 26:
                message.messageStubType = 26;
                break;
            case "GROUP_PARTICIPANT_ADD":
            case 27:
                message.messageStubType = 27;
                break;
            case "GROUP_PARTICIPANT_REMOVE":
            case 28:
                message.messageStubType = 28;
                break;
            case "GROUP_PARTICIPANT_PROMOTE":
            case 29:
                message.messageStubType = 29;
                break;
            case "GROUP_PARTICIPANT_DEMOTE":
            case 30:
                message.messageStubType = 30;
                break;
            case "GROUP_PARTICIPANT_INVITE":
            case 31:
                message.messageStubType = 31;
                break;
            case "GROUP_PARTICIPANT_LEAVE":
            case 32:
                message.messageStubType = 32;
                break;
            case "GROUP_PARTICIPANT_CHANGE_NUMBER":
            case 33:
                message.messageStubType = 33;
                break;
            case "BROADCAST_CREATE":
            case 34:
                message.messageStubType = 34;
                break;
            case "BROADCAST_ADD":
            case 35:
                message.messageStubType = 35;
                break;
            case "BROADCAST_REMOVE":
            case 36:
                message.messageStubType = 36;
                break;
            case "GENERIC_NOTIFICATION":
            case 37:
                message.messageStubType = 37;
                break;
            case "E2E_IDENTITY_CHANGED":
            case 38:
                message.messageStubType = 38;
                break;
            case "E2E_ENCRYPTED":
            case 39:
                message.messageStubType = 39;
                break;
            case "CALL_MISSED_VOICE":
            case 40:
                message.messageStubType = 40;
                break;
            case "CALL_MISSED_VIDEO":
            case 41:
                message.messageStubType = 41;
                break;
            case "INDIVIDUAL_CHANGE_NUMBER":
            case 42:
                message.messageStubType = 42;
                break;
            case "GROUP_DELETE":
            case 43:
                message.messageStubType = 43;
                break;
            case "GROUP_ANNOUNCE_MODE_MESSAGE_BOUNCE":
            case 44:
                message.messageStubType = 44;
                break;
            case "CALL_MISSED_GROUP_VOICE":
            case 45:
                message.messageStubType = 45;
                break;
            case "CALL_MISSED_GROUP_VIDEO":
            case 46:
                message.messageStubType = 46;
                break;
            case "PAYMENT_CIPHERTEXT":
            case 47:
                message.messageStubType = 47;
                break;
            case "PAYMENT_FUTUREPROOF":
            case 48:
                message.messageStubType = 48;
                break;
            case "PAYMENT_TRANSACTION_STATUS_UPDATE_FAILED":
            case 49:
                message.messageStubType = 49;
                break;
            case "PAYMENT_TRANSACTION_STATUS_UPDATE_REFUNDED":
            case 50:
                message.messageStubType = 50;
                break;
            case "PAYMENT_TRANSACTION_STATUS_UPDATE_REFUND_FAILED":
            case 51:
                message.messageStubType = 51;
                break;
            case "PAYMENT_TRANSACTION_STATUS_RECEIVER_PENDING_SETUP":
            case 52:
                message.messageStubType = 52;
                break;
            case "PAYMENT_TRANSACTION_STATUS_RECEIVER_SUCCESS_AFTER_HICCUP":
            case 53:
                message.messageStubType = 53;
                break;
            case "PAYMENT_ACTION_ACCOUNT_SETUP_REMINDER":
            case 54:
                message.messageStubType = 54;
                break;
            case "PAYMENT_ACTION_SEND_PAYMENT_REMINDER":
            case 55:
                message.messageStubType = 55;
                break;
            case "PAYMENT_ACTION_SEND_PAYMENT_INVITATION":
            case 56:
                message.messageStubType = 56;
                break;
            case "PAYMENT_ACTION_REQUEST_DECLINED":
            case 57:
                message.messageStubType = 57;
                break;
            case "PAYMENT_ACTION_REQUEST_EXPIRED":
            case 58:
                message.messageStubType = 58;
                break;
            case "PAYMENT_ACTION_REQUEST_CANCELLED":
            case 59:
                message.messageStubType = 59;
                break;
            case "BIZ_VERIFIED_TRANSITION_TOP_TO_BOTTOM":
            case 60:
                message.messageStubType = 60;
                break;
            case "BIZ_VERIFIED_TRANSITION_BOTTOM_TO_TOP":
            case 61:
                message.messageStubType = 61;
                break;
            case "BIZ_INTRO_TOP":
            case 62:
                message.messageStubType = 62;
                break;
            case "BIZ_INTRO_BOTTOM":
            case 63:
                message.messageStubType = 63;
                break;
            case "BIZ_NAME_CHANGE":
            case 64:
                message.messageStubType = 64;
                break;
            case "BIZ_MOVE_TO_CONSUMER_APP":
            case 65:
                message.messageStubType = 65;
                break;
            case "BIZ_TWO_TIER_MIGRATION_TOP":
            case 66:
                message.messageStubType = 66;
                break;
            case "BIZ_TWO_TIER_MIGRATION_BOTTOM":
            case 67:
                message.messageStubType = 67;
                break;
            case "OVERSIZED":
            case 68:
                message.messageStubType = 68;
                break;
            case "GROUP_CHANGE_NO_FREQUENTLY_FORWARDED":
            case 69:
                message.messageStubType = 69;
                break;
            case "GROUP_V4_ADD_INVITE_SENT":
            case 70:
                message.messageStubType = 70;
                break;
            case "GROUP_PARTICIPANT_ADD_REQUEST_JOIN":
            case 71:
                message.messageStubType = 71;
                break;
            case "CHANGE_EPHEMERAL_SETTING":
            case 72:
                message.messageStubType = 72;
                break;
            case "E2E_DEVICE_CHANGED":
            case 73:
                message.messageStubType = 73;
                break;
            case "VIEWED_ONCE":
            case 74:
                message.messageStubType = 74;
                break;
            case "E2E_ENCRYPTED_NOW":
            case 75:
                message.messageStubType = 75;
                break;
            case "BLUE_MSG_BSP_FB_TO_BSP_PREMISE":
            case 76:
                message.messageStubType = 76;
                break;
            case "BLUE_MSG_BSP_FB_TO_SELF_FB":
            case 77:
                message.messageStubType = 77;
                break;
            case "BLUE_MSG_BSP_FB_TO_SELF_PREMISE":
            case 78:
                message.messageStubType = 78;
                break;
            case "BLUE_MSG_BSP_FB_UNVERIFIED":
            case 79:
                message.messageStubType = 79;
                break;
            case "BLUE_MSG_BSP_FB_UNVERIFIED_TO_SELF_PREMISE_VERIFIED":
            case 80:
                message.messageStubType = 80;
                break;
            case "BLUE_MSG_BSP_FB_VERIFIED":
            case 81:
                message.messageStubType = 81;
                break;
            case "BLUE_MSG_BSP_FB_VERIFIED_TO_SELF_PREMISE_UNVERIFIED":
            case 82:
                message.messageStubType = 82;
                break;
            case "BLUE_MSG_BSP_PREMISE_TO_SELF_PREMISE":
            case 83:
                message.messageStubType = 83;
                break;
            case "BLUE_MSG_BSP_PREMISE_UNVERIFIED":
            case 84:
                message.messageStubType = 84;
                break;
            case "BLUE_MSG_BSP_PREMISE_UNVERIFIED_TO_SELF_PREMISE_VERIFIED":
            case 85:
                message.messageStubType = 85;
                break;
            case "BLUE_MSG_BSP_PREMISE_VERIFIED":
            case 86:
                message.messageStubType = 86;
                break;
            case "BLUE_MSG_BSP_PREMISE_VERIFIED_TO_SELF_PREMISE_UNVERIFIED":
            case 87:
                message.messageStubType = 87;
                break;
            case "BLUE_MSG_CONSUMER_TO_BSP_FB_UNVERIFIED":
            case 88:
                message.messageStubType = 88;
                break;
            case "BLUE_MSG_CONSUMER_TO_BSP_PREMISE_UNVERIFIED":
            case 89:
                message.messageStubType = 89;
                break;
            case "BLUE_MSG_CONSUMER_TO_SELF_FB_UNVERIFIED":
            case 90:
                message.messageStubType = 90;
                break;
            case "BLUE_MSG_CONSUMER_TO_SELF_PREMISE_UNVERIFIED":
            case 91:
                message.messageStubType = 91;
                break;
            case "BLUE_MSG_SELF_FB_TO_BSP_PREMISE":
            case 92:
                message.messageStubType = 92;
                break;
            case "BLUE_MSG_SELF_FB_TO_SELF_PREMISE":
            case 93:
                message.messageStubType = 93;
                break;
            case "BLUE_MSG_SELF_FB_UNVERIFIED":
            case 94:
                message.messageStubType = 94;
                break;
            case "BLUE_MSG_SELF_FB_UNVERIFIED_TO_SELF_PREMISE_VERIFIED":
            case 95:
                message.messageStubType = 95;
                break;
            case "BLUE_MSG_SELF_FB_VERIFIED":
            case 96:
                message.messageStubType = 96;
                break;
            case "BLUE_MSG_SELF_FB_VERIFIED_TO_SELF_PREMISE_UNVERIFIED":
            case 97:
                message.messageStubType = 97;
                break;
            case "BLUE_MSG_SELF_PREMISE_TO_BSP_PREMISE":
            case 98:
                message.messageStubType = 98;
                break;
            case "BLUE_MSG_SELF_PREMISE_UNVERIFIED":
            case 99:
                message.messageStubType = 99;
                break;
            case "BLUE_MSG_SELF_PREMISE_VERIFIED":
            case 100:
                message.messageStubType = 100;
                break;
            case "BLUE_MSG_TO_BSP_FB":
            case 101:
                message.messageStubType = 101;
                break;
            case "BLUE_MSG_TO_CONSUMER":
            case 102:
                message.messageStubType = 102;
                break;
            case "BLUE_MSG_TO_SELF_FB":
            case 103:
                message.messageStubType = 103;
                break;
            case "BLUE_MSG_UNVERIFIED_TO_BSP_FB_VERIFIED":
            case 104:
                message.messageStubType = 104;
                break;
            case "BLUE_MSG_UNVERIFIED_TO_BSP_PREMISE_VERIFIED":
            case 105:
                message.messageStubType = 105;
                break;
            case "BLUE_MSG_UNVERIFIED_TO_SELF_FB_VERIFIED":
            case 106:
                message.messageStubType = 106;
                break;
            case "BLUE_MSG_UNVERIFIED_TO_VERIFIED":
            case 107:
                message.messageStubType = 107;
                break;
            case "BLUE_MSG_VERIFIED_TO_BSP_FB_UNVERIFIED":
            case 108:
                message.messageStubType = 108;
                break;
            case "BLUE_MSG_VERIFIED_TO_BSP_PREMISE_UNVERIFIED":
            case 109:
                message.messageStubType = 109;
                break;
            case "BLUE_MSG_VERIFIED_TO_SELF_FB_UNVERIFIED":
            case 110:
                message.messageStubType = 110;
                break;
            case "BLUE_MSG_VERIFIED_TO_UNVERIFIED":
            case 111:
                message.messageStubType = 111;
                break;
            case "BLUE_MSG_BSP_FB_UNVERIFIED_TO_BSP_PREMISE_VERIFIED":
            case 112:
                message.messageStubType = 112;
                break;
            case "BLUE_MSG_BSP_FB_UNVERIFIED_TO_SELF_FB_VERIFIED":
            case 113:
                message.messageStubType = 113;
                break;
            case "BLUE_MSG_BSP_FB_VERIFIED_TO_BSP_PREMISE_UNVERIFIED":
            case 114:
                message.messageStubType = 114;
                break;
            case "BLUE_MSG_BSP_FB_VERIFIED_TO_SELF_FB_UNVERIFIED":
            case 115:
                message.messageStubType = 115;
                break;
            case "BLUE_MSG_SELF_FB_UNVERIFIED_TO_BSP_PREMISE_VERIFIED":
            case 116:
                message.messageStubType = 116;
                break;
            case "BLUE_MSG_SELF_FB_VERIFIED_TO_BSP_PREMISE_UNVERIFIED":
            case 117:
                message.messageStubType = 117;
                break;
            case "E2E_IDENTITY_UNAVAILABLE":
            case 118:
                message.messageStubType = 118;
                break;
            case "GROUP_CREATING":
            case 119:
                message.messageStubType = 119;
                break;
            case "GROUP_CREATE_FAILED":
            case 120:
                message.messageStubType = 120;
                break;
            case "GROUP_BOUNCED":
            case 121:
                message.messageStubType = 121;
                break;
            case "BLOCK_CONTACT":
            case 122:
                message.messageStubType = 122;
                break;
            case "EPHEMERAL_SETTING_NOT_APPLIED":
            case 123:
                message.messageStubType = 123;
                break;
            case "SYNC_FAILED":
            case 124:
                message.messageStubType = 124;
                break;
            case "SYNCING":
            case 125:
                message.messageStubType = 125;
                break;
            case "BIZ_PRIVACY_MODE_INIT_FB":
            case 126:
                message.messageStubType = 126;
                break;
            case "BIZ_PRIVACY_MODE_INIT_BSP":
            case 127:
                message.messageStubType = 127;
                break;
            case "BIZ_PRIVACY_MODE_TO_FB":
            case 128:
                message.messageStubType = 128;
                break;
            case "BIZ_PRIVACY_MODE_TO_BSP":
            case 129:
                message.messageStubType = 129;
                break;
            case "DISAPPEARING_MODE":
            case 130:
                message.messageStubType = 130;
                break;
            case "E2E_DEVICE_FETCH_FAILED":
            case 131:
                message.messageStubType = 131;
                break;
            case "ADMIN_REVOKE":
            case 132:
                message.messageStubType = 132;
                break;
            case "GROUP_INVITE_LINK_GROWTH_LOCKED":
            case 133:
                message.messageStubType = 133;
                break;
            case "COMMUNITY_LINK_PARENT_GROUP":
            case 134:
                message.messageStubType = 134;
                break;
            case "COMMUNITY_LINK_SIBLING_GROUP":
            case 135:
                message.messageStubType = 135;
                break;
            case "COMMUNITY_LINK_SUB_GROUP":
            case 136:
                message.messageStubType = 136;
                break;
            case "COMMUNITY_UNLINK_PARENT_GROUP":
            case 137:
                message.messageStubType = 137;
                break;
            case "COMMUNITY_UNLINK_SIBLING_GROUP":
            case 138:
                message.messageStubType = 138;
                break;
            case "COMMUNITY_UNLINK_SUB_GROUP":
            case 139:
                message.messageStubType = 139;
                break;
            case "GROUP_PARTICIPANT_ACCEPT":
            case 140:
                message.messageStubType = 140;
                break;
            case "GROUP_PARTICIPANT_LINKED_GROUP_JOIN":
            case 141:
                message.messageStubType = 141;
                break;
            case "COMMUNITY_CREATE":
            case 142:
                message.messageStubType = 142;
                break;
            case "EPHEMERAL_KEEP_IN_CHAT":
            case 143:
                message.messageStubType = 143;
                break;
            case "GROUP_MEMBERSHIP_JOIN_APPROVAL_REQUEST":
            case 144:
                message.messageStubType = 144;
                break;
            case "GROUP_MEMBERSHIP_JOIN_APPROVAL_MODE":
            case 145:
                message.messageStubType = 145;
                break;
            case "INTEGRITY_UNLINK_PARENT_GROUP":
            case 146:
                message.messageStubType = 146;
                break;
            case "COMMUNITY_PARTICIPANT_PROMOTE":
            case 147:
                message.messageStubType = 147;
                break;
            case "COMMUNITY_PARTICIPANT_DEMOTE":
            case 148:
                message.messageStubType = 148;
                break;
            case "COMMUNITY_PARENT_GROUP_DELETED":
            case 149:
                message.messageStubType = 149;
                break;
            case "COMMUNITY_LINK_PARENT_GROUP_MEMBERSHIP_APPROVAL":
            case 150:
                message.messageStubType = 150;
                break;
            case "GROUP_PARTICIPANT_JOINED_GROUP_AND_PARENT_GROUP":
            case 151:
                message.messageStubType = 151;
                break;
            case "MASKED_THREAD_CREATED":
            case 152:
                message.messageStubType = 152;
                break;
            case "MASKED_THREAD_UNMASKED":
            case 153:
                message.messageStubType = 153;
                break;
            case "BIZ_CHAT_ASSIGNMENT":
            case 154:
                message.messageStubType = 154;
                break;
            case "CHAT_PSA":
            case 155:
                message.messageStubType = 155;
                break;
            case "CHAT_POLL_CREATION_MESSAGE":
            case 156:
                message.messageStubType = 156;
                break;
            case "CAG_MASKED_THREAD_CREATED":
            case 157:
                message.messageStubType = 157;
                break;
            case "COMMUNITY_PARENT_GROUP_SUBJECT_CHANGED":
            case 158:
                message.messageStubType = 158;
                break;
            case "CAG_INVITE_AUTO_ADD":
            case 159:
                message.messageStubType = 159;
                break;
            case "BIZ_CHAT_ASSIGNMENT_UNASSIGN":
            case 160:
                message.messageStubType = 160;
                break;
            case "CAG_INVITE_AUTO_JOINED":
            case 161:
                message.messageStubType = 161;
                break;
            case "SCHEDULED_CALL_START_MESSAGE":
            case 162:
                message.messageStubType = 162;
                break;
            case "COMMUNITY_INVITE_RICH":
            case 163:
                message.messageStubType = 163;
                break;
            case "COMMUNITY_INVITE_AUTO_ADD_RICH":
            case 164:
                message.messageStubType = 164;
                break;
            case "SUB_GROUP_INVITE_RICH":
            case 165:
                message.messageStubType = 165;
                break;
            case "SUB_GROUP_PARTICIPANT_ADD_RICH":
            case 166:
                message.messageStubType = 166;
                break;
            case "COMMUNITY_LINK_PARENT_GROUP_RICH":
            case 167:
                message.messageStubType = 167;
                break;
            case "COMMUNITY_PARTICIPANT_ADD_RICH":
            case 168:
                message.messageStubType = 168;
                break;
            case "SILENCED_UNKNOWN_CALLER_AUDIO":
            case 169:
                message.messageStubType = 169;
                break;
            case "SILENCED_UNKNOWN_CALLER_VIDEO":
            case 170:
                message.messageStubType = 170;
                break;
            case "GROUP_MEMBER_ADD_MODE":
            case 171:
                message.messageStubType = 171;
                break;
            case "GROUP_MEMBERSHIP_JOIN_APPROVAL_REQUEST_NON_ADMIN_ADD":
            case 172:
                message.messageStubType = 172;
                break;
            case "COMMUNITY_CHANGE_DESCRIPTION":
            case 173:
                message.messageStubType = 173;
                break;
            case "SENDER_INVITE":
            case 174:
                message.messageStubType = 174;
                break;
            case "RECEIVER_INVITE":
            case 175:
                message.messageStubType = 175;
                break;
            case "COMMUNITY_ALLOW_MEMBER_ADDED_GROUPS":
            case 176:
                message.messageStubType = 176;
                break;
            case "PINNED_MESSAGE_IN_CHAT":
            case 177:
                message.messageStubType = 177;
                break;
            case "PAYMENT_INVITE_SETUP_INVITER":
            case 178:
                message.messageStubType = 178;
                break;
            case "PAYMENT_INVITE_SETUP_INVITEE_RECEIVE_ONLY":
            case 179:
                message.messageStubType = 179;
                break;
            case "PAYMENT_INVITE_SETUP_INVITEE_SEND_AND_RECEIVE":
            case 180:
                message.messageStubType = 180;
                break;
            case "LINKED_GROUP_CALL_START":
            case 181:
                message.messageStubType = 181;
                break;
            case "REPORT_TO_ADMIN_ENABLED_STATUS":
            case 182:
                message.messageStubType = 182;
                break;
            case "EMPTY_SUBGROUP_CREATE":
            case 183:
                message.messageStubType = 183;
                break;
            case "SCHEDULED_CALL_CANCEL":
            case 184:
                message.messageStubType = 184;
                break;
            case "SUBGROUP_ADMIN_TRIGGERED_AUTO_ADD_RICH":
            case 185:
                message.messageStubType = 185;
                break;
            case "GROUP_CHANGE_RECENT_HISTORY_SHARING":
            case 186:
                message.messageStubType = 186;
                break;
            case "PAID_MESSAGE_SERVER_CAMPAIGN_ID":
            case 187:
                message.messageStubType = 187;
                break;
            case "GENERAL_CHAT_CREATE":
            case 188:
                message.messageStubType = 188;
                break;
            case "GENERAL_CHAT_ADD":
            case 189:
                message.messageStubType = 189;
                break;
            case "GENERAL_CHAT_AUTO_ADD_DISABLED":
            case 190:
                message.messageStubType = 190;
                break;
            case "SUGGESTED_SUBGROUP_ANNOUNCE":
            case 191:
                message.messageStubType = 191;
                break;
            case "BIZ_BOT_1P_MESSAGING_ENABLED":
            case 192:
                message.messageStubType = 192;
                break;
            case "CHANGE_USERNAME":
            case 193:
                message.messageStubType = 193;
                break;
            case "BIZ_COEX_PRIVACY_INIT_SELF":
            case 194:
                message.messageStubType = 194;
                break;
            case "BIZ_COEX_PRIVACY_TRANSITION_SELF":
            case 195:
                message.messageStubType = 195;
                break;
            case "SUPPORT_AI_EDUCATION":
            case 196:
                message.messageStubType = 196;
                break;
            case "BIZ_BOT_3P_MESSAGING_ENABLED":
            case 197:
                message.messageStubType = 197;
                break;
            case "REMINDER_SETUP_MESSAGE":
            case 198:
                message.messageStubType = 198;
                break;
            case "REMINDER_SENT_MESSAGE":
            case 199:
                message.messageStubType = 199;
                break;
            case "REMINDER_CANCEL_MESSAGE":
            case 200:
                message.messageStubType = 200;
                break;
            case "BIZ_COEX_PRIVACY_INIT":
            case 201:
                message.messageStubType = 201;
                break;
            case "BIZ_COEX_PRIVACY_TRANSITION":
            case 202:
                message.messageStubType = 202;
                break;
            case "GROUP_DEACTIVATED":
            case 203:
                message.messageStubType = 203;
                break;
            case "COMMUNITY_DEACTIVATE_SIBLING_GROUP":
            case 204:
                message.messageStubType = 204;
                break;
            case "EVENT_UPDATED":
            case 205:
                message.messageStubType = 205;
                break;
            case "EVENT_CANCELED":
            case 206:
                message.messageStubType = 206;
                break;
            case "COMMUNITY_OWNER_UPDATED":
            case 207:
                message.messageStubType = 207;
                break;
            case "COMMUNITY_SUB_GROUP_VISIBILITY_HIDDEN":
            case 208:
                message.messageStubType = 208;
                break;
            case "CAPI_GROUP_NE2EE_SYSTEM_MESSAGE":
            case 209:
                message.messageStubType = 209;
                break;
            case "STATUS_MENTION":
            case 210:
                message.messageStubType = 210;
                break;
            case "USER_CONTROLS_SYSTEM_MESSAGE":
            case 211:
                message.messageStubType = 211;
                break;
            case "SUPPORT_SYSTEM_MESSAGE":
            case 212:
                message.messageStubType = 212;
                break;
            case "CHANGE_LID":
            case 213:
                message.messageStubType = 213;
                break;
            case "BIZ_CUSTOMER_3PD_DATA_SHARING_OPT_IN_MESSAGE":
            case 214:
                message.messageStubType = 214;
                break;
            case "BIZ_CUSTOMER_3PD_DATA_SHARING_OPT_OUT_MESSAGE":
            case 215:
                message.messageStubType = 215;
                break;
            case "CHANGE_LIMIT_SHARING":
            case 216:
                message.messageStubType = 216;
                break;
            case "GROUP_MEMBER_LINK_MODE":
            case 217:
                message.messageStubType = 217;
                break;
            case "BIZ_AUTOMATICALLY_LABELED_CHAT_SYSTEM_MESSAGE":
            case 218:
                message.messageStubType = 218;
                break;
            case "PHONE_NUMBER_HIDING_CHAT_DEPRECATED_MESSAGE":
            case 219:
                message.messageStubType = 219;
                break;
            case "QUARANTINED_MESSAGE":
            case 220:
                message.messageStubType = 220;
                break;
            case "GROUP_MEMBER_SHARE_GROUP_HISTORY_MODE":
            case 221:
                message.messageStubType = 221;
                break;
            case "GROUP_OPEN_BOT_ADDED":
            case 222:
                message.messageStubType = 222;
                break;
            case "GROUP_TEE_BOT_ADDED":
            case 223:
                message.messageStubType = 223;
                break;
            case "CONTACT_INFO":
            case 224:
                message.messageStubType = 224;
                break;
            case "SCHEDULED_MESSAGE_CREATED":
            case 225:
                message.messageStubType = 225;
                break;
            case "IDENTITY_TRUST_MARKED":
            case 226:
                message.messageStubType = 226;
                break;
            case "IDENTITY_TRUST_UNMARKED":
            case 227:
                message.messageStubType = 227;
                break;
            case "IDENTITY_TRUST_REVOKED":
            case 228:
                message.messageStubType = 228;
                break;
            case "CTWA_CONSUMER_DISCLOSURE":
            case 230:
                message.messageStubType = 230;
                break;
            default:
                if (typeof object.messageStubType === "number" && (object.messageStubType | 0) === object.messageStubType)
                    message.messageStubType = object.messageStubType;
            }
            if (object.clearMedia != null)
                message.clearMedia = Boolean(object.clearMedia);
            if (object.messageStubParameters) {
                if (!Array.isArray(object.messageStubParameters))
                    throw TypeError(".proto.WebMessageInfo.messageStubParameters: array expected");
                message.messageStubParameters = Array(object.messageStubParameters.length);
                for (var i = 0; i < object.messageStubParameters.length; ++i)
                    message.messageStubParameters[i] = String(object.messageStubParameters[i]);
            }
            if (object.duration != null)
                message.duration = object.duration >>> 0;
            if (object.labels) {
                if (!Array.isArray(object.labels))
                    throw TypeError(".proto.WebMessageInfo.labels: array expected");
                message.labels = Array(object.labels.length);
                for (var i = 0; i < object.labels.length; ++i)
                    message.labels[i] = String(object.labels[i]);
            }
            if (object.paymentInfo != null) {
                if (!$util.isObject(object.paymentInfo))
                    throw TypeError(".proto.WebMessageInfo.paymentInfo: object expected");
                message.paymentInfo = $root.proto.PaymentInfo.fromObject(object.paymentInfo, q + 1);
            }
            if (object.finalLiveLocation != null) {
                if (!$util.isObject(object.finalLiveLocation))
                    throw TypeError(".proto.WebMessageInfo.finalLiveLocation: object expected");
                message.finalLiveLocation = $root.proto.Message.LiveLocationMessage.fromObject(object.finalLiveLocation, q + 1);
            }
            if (object.quotedPaymentInfo != null) {
                if (!$util.isObject(object.quotedPaymentInfo))
                    throw TypeError(".proto.WebMessageInfo.quotedPaymentInfo: object expected");
                message.quotedPaymentInfo = $root.proto.PaymentInfo.fromObject(object.quotedPaymentInfo, q + 1);
            }
            if (object.ephemeralStartTimestamp != null)
                if ($util.Long)
                    message.ephemeralStartTimestamp = $util.Long.fromValue(object.ephemeralStartTimestamp, true);
                else if (typeof object.ephemeralStartTimestamp === "string")
                    message.ephemeralStartTimestamp = parseInt(object.ephemeralStartTimestamp, 10);
                else if (typeof object.ephemeralStartTimestamp === "number")
                    message.ephemeralStartTimestamp = object.ephemeralStartTimestamp;
                else if (typeof object.ephemeralStartTimestamp === "object")
                    message.ephemeralStartTimestamp = new $util.LongBits(object.ephemeralStartTimestamp.low >>> 0, object.ephemeralStartTimestamp.high >>> 0).toNumber(true);
            if (object.ephemeralDuration != null)
                message.ephemeralDuration = object.ephemeralDuration >>> 0;
            if (object.ephemeralOffToOn != null)
                message.ephemeralOffToOn = Boolean(object.ephemeralOffToOn);
            if (object.ephemeralOutOfSync != null)
                message.ephemeralOutOfSync = Boolean(object.ephemeralOutOfSync);
            switch (object.bizPrivacyStatus) {
            case "E2EE":
            case 0:
                message.bizPrivacyStatus = 0;
                break;
            case "FB":
            case 2:
                message.bizPrivacyStatus = 2;
                break;
            case "BSP":
            case 1:
                message.bizPrivacyStatus = 1;
                break;
            case "BSP_AND_FB":
            case 3:
                message.bizPrivacyStatus = 3;
                break;
            default:
                if (typeof object.bizPrivacyStatus === "number" && (object.bizPrivacyStatus | 0) === object.bizPrivacyStatus)
                    message.bizPrivacyStatus = object.bizPrivacyStatus;
            }
            if (object.verifiedBizName != null)
                message.verifiedBizName = String(object.verifiedBizName);
            if (object.mediaData != null) {
                if (!$util.isObject(object.mediaData))
                    throw TypeError(".proto.WebMessageInfo.mediaData: object expected");
                message.mediaData = $root.proto.MediaData.fromObject(object.mediaData, q + 1);
            }
            if (object.photoChange != null) {
                if (!$util.isObject(object.photoChange))
                    throw TypeError(".proto.WebMessageInfo.photoChange: object expected");
                message.photoChange = $root.proto.PhotoChange.fromObject(object.photoChange, q + 1);
            }
            if (object.userReceipt) {
                if (!Array.isArray(object.userReceipt))
                    throw TypeError(".proto.WebMessageInfo.userReceipt: array expected");
                message.userReceipt = Array(object.userReceipt.length);
                for (var i = 0; i < object.userReceipt.length; ++i) {
                    if (!$util.isObject(object.userReceipt[i]))
                        throw TypeError(".proto.WebMessageInfo.userReceipt: object expected");
                    message.userReceipt[i] = $root.proto.UserReceipt.fromObject(object.userReceipt[i], q + 1);
                }
            }
            if (object.reactions) {
                if (!Array.isArray(object.reactions))
                    throw TypeError(".proto.WebMessageInfo.reactions: array expected");
                message.reactions = Array(object.reactions.length);
                for (var i = 0; i < object.reactions.length; ++i) {
                    if (!$util.isObject(object.reactions[i]))
                        throw TypeError(".proto.WebMessageInfo.reactions: object expected");
                    message.reactions[i] = $root.proto.Reaction.fromObject(object.reactions[i], q + 1);
                }
            }
            if (object.quotedStickerData != null) {
                if (!$util.isObject(object.quotedStickerData))
                    throw TypeError(".proto.WebMessageInfo.quotedStickerData: object expected");
                message.quotedStickerData = $root.proto.MediaData.fromObject(object.quotedStickerData, q + 1);
            }
            if (object.futureproofData != null)
                if (typeof object.futureproofData === "string")
                    $util.base64.decode(object.futureproofData, message.futureproofData = $util.newBuffer($util.base64.length(object.futureproofData)), 0);
                else if (object.futureproofData.length >= 0)
                    message.futureproofData = object.futureproofData;
            if (object.statusPsa != null) {
                if (!$util.isObject(object.statusPsa))
                    throw TypeError(".proto.WebMessageInfo.statusPsa: object expected");
                message.statusPsa = $root.proto.StatusPSA.fromObject(object.statusPsa, q + 1);
            }
            if (object.pollUpdates) {
                if (!Array.isArray(object.pollUpdates))
                    throw TypeError(".proto.WebMessageInfo.pollUpdates: array expected");
                message.pollUpdates = Array(object.pollUpdates.length);
                for (var i = 0; i < object.pollUpdates.length; ++i) {
                    if (!$util.isObject(object.pollUpdates[i]))
                        throw TypeError(".proto.WebMessageInfo.pollUpdates: object expected");
                    message.pollUpdates[i] = $root.proto.PollUpdate.fromObject(object.pollUpdates[i], q + 1);
                }
            }
            if (object.pollAdditionalMetadata != null) {
                if (!$util.isObject(object.pollAdditionalMetadata))
                    throw TypeError(".proto.WebMessageInfo.pollAdditionalMetadata: object expected");
                message.pollAdditionalMetadata = $root.proto.PollAdditionalMetadata.fromObject(object.pollAdditionalMetadata, q + 1);
            }
            if (object.agentId != null)
                message.agentId = String(object.agentId);
            if (object.statusAlreadyViewed != null)
                message.statusAlreadyViewed = Boolean(object.statusAlreadyViewed);
            if (object.messageSecret != null)
                if (typeof object.messageSecret === "string")
                    $util.base64.decode(object.messageSecret, message.messageSecret = $util.newBuffer($util.base64.length(object.messageSecret)), 0);
                else if (object.messageSecret.length >= 0)
                    message.messageSecret = object.messageSecret;
            if (object.keepInChat != null) {
                if (!$util.isObject(object.keepInChat))
                    throw TypeError(".proto.WebMessageInfo.keepInChat: object expected");
                message.keepInChat = $root.proto.KeepInChat.fromObject(object.keepInChat, q + 1);
            }
            if (object.originalSelfAuthorUserJidString != null)
                message.originalSelfAuthorUserJidString = String(object.originalSelfAuthorUserJidString);
            if (object.revokeMessageTimestamp != null)
                if ($util.Long)
                    message.revokeMessageTimestamp = $util.Long.fromValue(object.revokeMessageTimestamp, true);
                else if (typeof object.revokeMessageTimestamp === "string")
                    message.revokeMessageTimestamp = parseInt(object.revokeMessageTimestamp, 10);
                else if (typeof object.revokeMessageTimestamp === "number")
                    message.revokeMessageTimestamp = object.revokeMessageTimestamp;
                else if (typeof object.revokeMessageTimestamp === "object")
                    message.revokeMessageTimestamp = new $util.LongBits(object.revokeMessageTimestamp.low >>> 0, object.revokeMessageTimestamp.high >>> 0).toNumber(true);
            if (object.pinInChat != null) {
                if (!$util.isObject(object.pinInChat))
                    throw TypeError(".proto.WebMessageInfo.pinInChat: object expected");
                message.pinInChat = $root.proto.PinInChat.fromObject(object.pinInChat, q + 1);
            }
            if (object.premiumMessageInfo != null) {
                if (!$util.isObject(object.premiumMessageInfo))
                    throw TypeError(".proto.WebMessageInfo.premiumMessageInfo: object expected");
                message.premiumMessageInfo = $root.proto.PremiumMessageInfo.fromObject(object.premiumMessageInfo, q + 1);
            }
            if (object.is1PBizBotMessage != null)
                message.is1PBizBotMessage = Boolean(object.is1PBizBotMessage);
            if (object.isGroupHistoryMessage != null)
                message.isGroupHistoryMessage = Boolean(object.isGroupHistoryMessage);
            if (object.botMessageInvokerJid != null)
                message.botMessageInvokerJid = String(object.botMessageInvokerJid);
            if (object.commentMetadata != null) {
                if (!$util.isObject(object.commentMetadata))
                    throw TypeError(".proto.WebMessageInfo.commentMetadata: object expected");
                message.commentMetadata = $root.proto.CommentMetadata.fromObject(object.commentMetadata, q + 1);
            }
            if (object.eventResponses) {
                if (!Array.isArray(object.eventResponses))
                    throw TypeError(".proto.WebMessageInfo.eventResponses: array expected");
                message.eventResponses = Array(object.eventResponses.length);
                for (var i = 0; i < object.eventResponses.length; ++i) {
                    if (!$util.isObject(object.eventResponses[i]))
                        throw TypeError(".proto.WebMessageInfo.eventResponses: object expected");
                    message.eventResponses[i] = $root.proto.EventResponse.fromObject(object.eventResponses[i], q + 1);
                }
            }
            if (object.reportingTokenInfo != null) {
                if (!$util.isObject(object.reportingTokenInfo))
                    throw TypeError(".proto.WebMessageInfo.reportingTokenInfo: object expected");
                message.reportingTokenInfo = $root.proto.ReportingTokenInfo.fromObject(object.reportingTokenInfo, q + 1);
            }
            if (object.newsletterServerId != null)
                if ($util.Long)
                    message.newsletterServerId = $util.Long.fromValue(object.newsletterServerId, true);
                else if (typeof object.newsletterServerId === "string")
                    message.newsletterServerId = parseInt(object.newsletterServerId, 10);
                else if (typeof object.newsletterServerId === "number")
                    message.newsletterServerId = object.newsletterServerId;
                else if (typeof object.newsletterServerId === "object")
                    message.newsletterServerId = new $util.LongBits(object.newsletterServerId.low >>> 0, object.newsletterServerId.high >>> 0).toNumber(true);
            if (object.eventAdditionalMetadata != null) {
                if (!$util.isObject(object.eventAdditionalMetadata))
                    throw TypeError(".proto.WebMessageInfo.eventAdditionalMetadata: object expected");
                message.eventAdditionalMetadata = $root.proto.EventAdditionalMetadata.fromObject(object.eventAdditionalMetadata, q + 1);
            }
            if (object.isMentionedInStatus != null)
                message.isMentionedInStatus = Boolean(object.isMentionedInStatus);
            if (object.statusMentions) {
                if (!Array.isArray(object.statusMentions))
                    throw TypeError(".proto.WebMessageInfo.statusMentions: array expected");
                message.statusMentions = Array(object.statusMentions.length);
                for (var i = 0; i < object.statusMentions.length; ++i)
                    message.statusMentions[i] = String(object.statusMentions[i]);
            }
            if (object.targetMessageId != null) {
                if (!$util.isObject(object.targetMessageId))
                    throw TypeError(".proto.WebMessageInfo.targetMessageId: object expected");
                message.targetMessageId = $root.proto.MessageKey.fromObject(object.targetMessageId, q + 1);
            }
            if (object.messageAddOns) {
                if (!Array.isArray(object.messageAddOns))
                    throw TypeError(".proto.WebMessageInfo.messageAddOns: array expected");
                message.messageAddOns = Array(object.messageAddOns.length);
                for (var i = 0; i < object.messageAddOns.length; ++i) {
                    if (!$util.isObject(object.messageAddOns[i]))
                        throw TypeError(".proto.WebMessageInfo.messageAddOns: object expected");
                    message.messageAddOns[i] = $root.proto.MessageAddOn.fromObject(object.messageAddOns[i], q + 1);
                }
            }
            if (object.statusMentionMessageInfo != null) {
                if (!$util.isObject(object.statusMentionMessageInfo))
                    throw TypeError(".proto.WebMessageInfo.statusMentionMessageInfo: object expected");
                message.statusMentionMessageInfo = $root.proto.StatusMentionMessage.fromObject(object.statusMentionMessageInfo, q + 1);
            }
            if (object.isSupportAiMessage != null)
                message.isSupportAiMessage = Boolean(object.isSupportAiMessage);
            if (object.statusMentionSources) {
                if (!Array.isArray(object.statusMentionSources))
                    throw TypeError(".proto.WebMessageInfo.statusMentionSources: array expected");
                message.statusMentionSources = Array(object.statusMentionSources.length);
                for (var i = 0; i < object.statusMentionSources.length; ++i)
                    message.statusMentionSources[i] = String(object.statusMentionSources[i]);
            }
            if (object.supportAiCitations) {
                if (!Array.isArray(object.supportAiCitations))
                    throw TypeError(".proto.WebMessageInfo.supportAiCitations: array expected");
                message.supportAiCitations = Array(object.supportAiCitations.length);
                for (var i = 0; i < object.supportAiCitations.length; ++i) {
                    if (!$util.isObject(object.supportAiCitations[i]))
                        throw TypeError(".proto.WebMessageInfo.supportAiCitations: object expected");
                    message.supportAiCitations[i] = $root.proto.Citation.fromObject(object.supportAiCitations[i], q + 1);
                }
            }
            if (object.botTargetId != null)
                message.botTargetId = String(object.botTargetId);
            if (object.groupHistoryIndividualMessageInfo != null) {
                if (!$util.isObject(object.groupHistoryIndividualMessageInfo))
                    throw TypeError(".proto.WebMessageInfo.groupHistoryIndividualMessageInfo: object expected");
                message.groupHistoryIndividualMessageInfo = $root.proto.GroupHistoryIndividualMessageInfo.fromObject(object.groupHistoryIndividualMessageInfo, q + 1);
            }
            if (object.groupHistoryBundleInfo != null) {
                if (!$util.isObject(object.groupHistoryBundleInfo))
                    throw TypeError(".proto.WebMessageInfo.groupHistoryBundleInfo: object expected");
                message.groupHistoryBundleInfo = $root.proto.GroupHistoryBundleInfo.fromObject(object.groupHistoryBundleInfo, q + 1);
            }
            if (object.interactiveMessageAdditionalMetadata != null) {
                if (!$util.isObject(object.interactiveMessageAdditionalMetadata))
                    throw TypeError(".proto.WebMessageInfo.interactiveMessageAdditionalMetadata: object expected");
                message.interactiveMessageAdditionalMetadata = $root.proto.InteractiveMessageAdditionalMetadata.fromObject(object.interactiveMessageAdditionalMetadata, q + 1);
            }
            if (object.quarantinedMessage != null) {
                if (!$util.isObject(object.quarantinedMessage))
                    throw TypeError(".proto.WebMessageInfo.quarantinedMessage: object expected");
                message.quarantinedMessage = $root.proto.QuarantinedMessage.fromObject(object.quarantinedMessage, q + 1);
            }
            if (object.nonJidMentions != null)
                message.nonJidMentions = object.nonJidMentions >>> 0;
            if (object.hsmTag != null)
                message.hsmTag = String(object.hsmTag);
            if (object.ephemeralExpirationTimestamp != null)
                if ($util.Long)
                    message.ephemeralExpirationTimestamp = $util.Long.fromValue(object.ephemeralExpirationTimestamp, true);
                else if (typeof object.ephemeralExpirationTimestamp === "string")
                    message.ephemeralExpirationTimestamp = parseInt(object.ephemeralExpirationTimestamp, 10);
                else if (typeof object.ephemeralExpirationTimestamp === "number")
                    message.ephemeralExpirationTimestamp = object.ephemeralExpirationTimestamp;
                else if (typeof object.ephemeralExpirationTimestamp === "object")
                    message.ephemeralExpirationTimestamp = new $util.LongBits(object.ephemeralExpirationTimestamp.low >>> 0, object.ephemeralExpirationTimestamp.high >>> 0).toNumber(true);
            if (object.scheduledMessageMetadata != null) {
                if (!$util.isObject(object.scheduledMessageMetadata))
                    throw TypeError(".proto.WebMessageInfo.scheduledMessageMetadata: object expected");
                message.scheduledMessageMetadata = $root.proto.ScheduledMessageMetadata.fromObject(object.scheduledMessageMetadata, q + 1);
            }
            if (object.decisionId != null)
                message.decisionId = String(object.decisionId);
            if (object.decisionSources) {
                if (!Array.isArray(object.decisionSources))
                    throw TypeError(".proto.WebMessageInfo.decisionSources: array expected");
                message.decisionSources = Array(object.decisionSources.length);
                for (var i = 0; i < object.decisionSources.length; ++i)
                    message.decisionSources[i] = String(object.decisionSources[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a WebMessageInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.WebMessageInfo
         * @static
         * @param {proto.WebMessageInfo} message WebMessageInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WebMessageInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults) {
                object.messageStubParameters = [];
                object.labels = [];
                object.userReceipt = [];
                object.reactions = [];
                object.pollUpdates = [];
                object.eventResponses = [];
                object.statusMentions = [];
                object.messageAddOns = [];
                object.statusMentionSources = [];
                object.supportAiCitations = [];
                object.decisionSources = [];
            }
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                object.key = $root.proto.MessageKey.toObject(message.key, options, q + 1);
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                object.message = $root.proto.Message.toObject(message.message, options, q + 1);
            if (message.messageTimestamp != null && Object.hasOwnProperty.call(message, "messageTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.messageTimestamp = typeof message.messageTimestamp === "number" ? BigInt(message.messageTimestamp) : $util.Long.fromBits(message.messageTimestamp.low >>> 0, message.messageTimestamp.high >>> 0, true).toBigInt();
                else if (typeof message.messageTimestamp === "number")
                    object.messageTimestamp = options.longs === String ? String(message.messageTimestamp) : message.messageTimestamp;
                else
                    object.messageTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.messageTimestamp) : options.longs === Number ? new $util.LongBits(message.messageTimestamp.low >>> 0, message.messageTimestamp.high >>> 0).toNumber(true) : message.messageTimestamp;
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                object.status = options.enums === String ? $root.proto.WebMessageInfo.Status[message.status] === undefined ? message.status : $root.proto.WebMessageInfo.Status[message.status] : message.status;
            if (message.participant != null && Object.hasOwnProperty.call(message, "participant"))
                object.participant = message.participant;
            if (message.messageC2STimestamp != null && Object.hasOwnProperty.call(message, "messageC2STimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.messageC2STimestamp = typeof message.messageC2STimestamp === "number" ? BigInt(message.messageC2STimestamp) : $util.Long.fromBits(message.messageC2STimestamp.low >>> 0, message.messageC2STimestamp.high >>> 0, true).toBigInt();
                else if (typeof message.messageC2STimestamp === "number")
                    object.messageC2STimestamp = options.longs === String ? String(message.messageC2STimestamp) : message.messageC2STimestamp;
                else
                    object.messageC2STimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.messageC2STimestamp) : options.longs === Number ? new $util.LongBits(message.messageC2STimestamp.low >>> 0, message.messageC2STimestamp.high >>> 0).toNumber(true) : message.messageC2STimestamp;
            if (message.ignore != null && Object.hasOwnProperty.call(message, "ignore"))
                object.ignore = message.ignore;
            if (message.starred != null && Object.hasOwnProperty.call(message, "starred"))
                object.starred = message.starred;
            if (message.broadcast != null && Object.hasOwnProperty.call(message, "broadcast"))
                object.broadcast = message.broadcast;
            if (message.pushName != null && Object.hasOwnProperty.call(message, "pushName"))
                object.pushName = message.pushName;
            if (message.mediaCiphertextSha256 != null && Object.hasOwnProperty.call(message, "mediaCiphertextSha256"))
                object.mediaCiphertextSha256 = options.bytes === String ? $util.base64.encode(message.mediaCiphertextSha256, 0, message.mediaCiphertextSha256.length) : options.bytes === Array ? Array.prototype.slice.call(message.mediaCiphertextSha256) : message.mediaCiphertextSha256;
            if (message.multicast != null && Object.hasOwnProperty.call(message, "multicast"))
                object.multicast = message.multicast;
            if (message.urlText != null && Object.hasOwnProperty.call(message, "urlText"))
                object.urlText = message.urlText;
            if (message.urlNumber != null && Object.hasOwnProperty.call(message, "urlNumber"))
                object.urlNumber = message.urlNumber;
            if (message.messageStubType != null && Object.hasOwnProperty.call(message, "messageStubType"))
                object.messageStubType = options.enums === String ? $root.proto.WebMessageInfo.StubType[message.messageStubType] === undefined ? message.messageStubType : $root.proto.WebMessageInfo.StubType[message.messageStubType] : message.messageStubType;
            if (message.clearMedia != null && Object.hasOwnProperty.call(message, "clearMedia"))
                object.clearMedia = message.clearMedia;
            if (message.messageStubParameters && message.messageStubParameters.length) {
                object.messageStubParameters = Array(message.messageStubParameters.length);
                for (var j = 0; j < message.messageStubParameters.length; ++j)
                    object.messageStubParameters[j] = message.messageStubParameters[j];
            }
            if (message.duration != null && Object.hasOwnProperty.call(message, "duration"))
                object.duration = message.duration;
            if (message.labels && message.labels.length) {
                object.labels = Array(message.labels.length);
                for (var j = 0; j < message.labels.length; ++j)
                    object.labels[j] = message.labels[j];
            }
            if (message.paymentInfo != null && Object.hasOwnProperty.call(message, "paymentInfo"))
                object.paymentInfo = $root.proto.PaymentInfo.toObject(message.paymentInfo, options, q + 1);
            if (message.finalLiveLocation != null && Object.hasOwnProperty.call(message, "finalLiveLocation"))
                object.finalLiveLocation = $root.proto.Message.LiveLocationMessage.toObject(message.finalLiveLocation, options, q + 1);
            if (message.quotedPaymentInfo != null && Object.hasOwnProperty.call(message, "quotedPaymentInfo"))
                object.quotedPaymentInfo = $root.proto.PaymentInfo.toObject(message.quotedPaymentInfo, options, q + 1);
            if (message.ephemeralStartTimestamp != null && Object.hasOwnProperty.call(message, "ephemeralStartTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.ephemeralStartTimestamp = typeof message.ephemeralStartTimestamp === "number" ? BigInt(message.ephemeralStartTimestamp) : $util.Long.fromBits(message.ephemeralStartTimestamp.low >>> 0, message.ephemeralStartTimestamp.high >>> 0, true).toBigInt();
                else if (typeof message.ephemeralStartTimestamp === "number")
                    object.ephemeralStartTimestamp = options.longs === String ? String(message.ephemeralStartTimestamp) : message.ephemeralStartTimestamp;
                else
                    object.ephemeralStartTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.ephemeralStartTimestamp) : options.longs === Number ? new $util.LongBits(message.ephemeralStartTimestamp.low >>> 0, message.ephemeralStartTimestamp.high >>> 0).toNumber(true) : message.ephemeralStartTimestamp;
            if (message.ephemeralDuration != null && Object.hasOwnProperty.call(message, "ephemeralDuration"))
                object.ephemeralDuration = message.ephemeralDuration;
            if (message.ephemeralOffToOn != null && Object.hasOwnProperty.call(message, "ephemeralOffToOn"))
                object.ephemeralOffToOn = message.ephemeralOffToOn;
            if (message.ephemeralOutOfSync != null && Object.hasOwnProperty.call(message, "ephemeralOutOfSync"))
                object.ephemeralOutOfSync = message.ephemeralOutOfSync;
            if (message.bizPrivacyStatus != null && Object.hasOwnProperty.call(message, "bizPrivacyStatus"))
                object.bizPrivacyStatus = options.enums === String ? $root.proto.WebMessageInfo.BizPrivacyStatus[message.bizPrivacyStatus] === undefined ? message.bizPrivacyStatus : $root.proto.WebMessageInfo.BizPrivacyStatus[message.bizPrivacyStatus] : message.bizPrivacyStatus;
            if (message.verifiedBizName != null && Object.hasOwnProperty.call(message, "verifiedBizName"))
                object.verifiedBizName = message.verifiedBizName;
            if (message.mediaData != null && Object.hasOwnProperty.call(message, "mediaData"))
                object.mediaData = $root.proto.MediaData.toObject(message.mediaData, options, q + 1);
            if (message.photoChange != null && Object.hasOwnProperty.call(message, "photoChange"))
                object.photoChange = $root.proto.PhotoChange.toObject(message.photoChange, options, q + 1);
            if (message.userReceipt && message.userReceipt.length) {
                object.userReceipt = Array(message.userReceipt.length);
                for (var j = 0; j < message.userReceipt.length; ++j)
                    object.userReceipt[j] = $root.proto.UserReceipt.toObject(message.userReceipt[j], options, q + 1);
            }
            if (message.reactions && message.reactions.length) {
                object.reactions = Array(message.reactions.length);
                for (var j = 0; j < message.reactions.length; ++j)
                    object.reactions[j] = $root.proto.Reaction.toObject(message.reactions[j], options, q + 1);
            }
            if (message.quotedStickerData != null && Object.hasOwnProperty.call(message, "quotedStickerData"))
                object.quotedStickerData = $root.proto.MediaData.toObject(message.quotedStickerData, options, q + 1);
            if (message.futureproofData != null && Object.hasOwnProperty.call(message, "futureproofData"))
                object.futureproofData = options.bytes === String ? $util.base64.encode(message.futureproofData, 0, message.futureproofData.length) : options.bytes === Array ? Array.prototype.slice.call(message.futureproofData) : message.futureproofData;
            if (message.statusPsa != null && Object.hasOwnProperty.call(message, "statusPsa"))
                object.statusPsa = $root.proto.StatusPSA.toObject(message.statusPsa, options, q + 1);
            if (message.pollUpdates && message.pollUpdates.length) {
                object.pollUpdates = Array(message.pollUpdates.length);
                for (var j = 0; j < message.pollUpdates.length; ++j)
                    object.pollUpdates[j] = $root.proto.PollUpdate.toObject(message.pollUpdates[j], options, q + 1);
            }
            if (message.pollAdditionalMetadata != null && Object.hasOwnProperty.call(message, "pollAdditionalMetadata"))
                object.pollAdditionalMetadata = $root.proto.PollAdditionalMetadata.toObject(message.pollAdditionalMetadata, options, q + 1);
            if (message.agentId != null && Object.hasOwnProperty.call(message, "agentId"))
                object.agentId = message.agentId;
            if (message.statusAlreadyViewed != null && Object.hasOwnProperty.call(message, "statusAlreadyViewed"))
                object.statusAlreadyViewed = message.statusAlreadyViewed;
            if (message.messageSecret != null && Object.hasOwnProperty.call(message, "messageSecret"))
                object.messageSecret = options.bytes === String ? $util.base64.encode(message.messageSecret, 0, message.messageSecret.length) : options.bytes === Array ? Array.prototype.slice.call(message.messageSecret) : message.messageSecret;
            if (message.keepInChat != null && Object.hasOwnProperty.call(message, "keepInChat"))
                object.keepInChat = $root.proto.KeepInChat.toObject(message.keepInChat, options, q + 1);
            if (message.originalSelfAuthorUserJidString != null && Object.hasOwnProperty.call(message, "originalSelfAuthorUserJidString"))
                object.originalSelfAuthorUserJidString = message.originalSelfAuthorUserJidString;
            if (message.revokeMessageTimestamp != null && Object.hasOwnProperty.call(message, "revokeMessageTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.revokeMessageTimestamp = typeof message.revokeMessageTimestamp === "number" ? BigInt(message.revokeMessageTimestamp) : $util.Long.fromBits(message.revokeMessageTimestamp.low >>> 0, message.revokeMessageTimestamp.high >>> 0, true).toBigInt();
                else if (typeof message.revokeMessageTimestamp === "number")
                    object.revokeMessageTimestamp = options.longs === String ? String(message.revokeMessageTimestamp) : message.revokeMessageTimestamp;
                else
                    object.revokeMessageTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.revokeMessageTimestamp) : options.longs === Number ? new $util.LongBits(message.revokeMessageTimestamp.low >>> 0, message.revokeMessageTimestamp.high >>> 0).toNumber(true) : message.revokeMessageTimestamp;
            if (message.pinInChat != null && Object.hasOwnProperty.call(message, "pinInChat"))
                object.pinInChat = $root.proto.PinInChat.toObject(message.pinInChat, options, q + 1);
            if (message.premiumMessageInfo != null && Object.hasOwnProperty.call(message, "premiumMessageInfo"))
                object.premiumMessageInfo = $root.proto.PremiumMessageInfo.toObject(message.premiumMessageInfo, options, q + 1);
            if (message.is1PBizBotMessage != null && Object.hasOwnProperty.call(message, "is1PBizBotMessage"))
                object.is1PBizBotMessage = message.is1PBizBotMessage;
            if (message.isGroupHistoryMessage != null && Object.hasOwnProperty.call(message, "isGroupHistoryMessage"))
                object.isGroupHistoryMessage = message.isGroupHistoryMessage;
            if (message.botMessageInvokerJid != null && Object.hasOwnProperty.call(message, "botMessageInvokerJid"))
                object.botMessageInvokerJid = message.botMessageInvokerJid;
            if (message.commentMetadata != null && Object.hasOwnProperty.call(message, "commentMetadata"))
                object.commentMetadata = $root.proto.CommentMetadata.toObject(message.commentMetadata, options, q + 1);
            if (message.eventResponses && message.eventResponses.length) {
                object.eventResponses = Array(message.eventResponses.length);
                for (var j = 0; j < message.eventResponses.length; ++j)
                    object.eventResponses[j] = $root.proto.EventResponse.toObject(message.eventResponses[j], options, q + 1);
            }
            if (message.reportingTokenInfo != null && Object.hasOwnProperty.call(message, "reportingTokenInfo"))
                object.reportingTokenInfo = $root.proto.ReportingTokenInfo.toObject(message.reportingTokenInfo, options, q + 1);
            if (message.newsletterServerId != null && Object.hasOwnProperty.call(message, "newsletterServerId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.newsletterServerId = typeof message.newsletterServerId === "number" ? BigInt(message.newsletterServerId) : $util.Long.fromBits(message.newsletterServerId.low >>> 0, message.newsletterServerId.high >>> 0, true).toBigInt();
                else if (typeof message.newsletterServerId === "number")
                    object.newsletterServerId = options.longs === String ? String(message.newsletterServerId) : message.newsletterServerId;
                else
                    object.newsletterServerId = options.longs === String ? $util.Long.prototype.toString.call(message.newsletterServerId) : options.longs === Number ? new $util.LongBits(message.newsletterServerId.low >>> 0, message.newsletterServerId.high >>> 0).toNumber(true) : message.newsletterServerId;
            if (message.eventAdditionalMetadata != null && Object.hasOwnProperty.call(message, "eventAdditionalMetadata"))
                object.eventAdditionalMetadata = $root.proto.EventAdditionalMetadata.toObject(message.eventAdditionalMetadata, options, q + 1);
            if (message.isMentionedInStatus != null && Object.hasOwnProperty.call(message, "isMentionedInStatus"))
                object.isMentionedInStatus = message.isMentionedInStatus;
            if (message.statusMentions && message.statusMentions.length) {
                object.statusMentions = Array(message.statusMentions.length);
                for (var j = 0; j < message.statusMentions.length; ++j)
                    object.statusMentions[j] = message.statusMentions[j];
            }
            if (message.targetMessageId != null && Object.hasOwnProperty.call(message, "targetMessageId"))
                object.targetMessageId = $root.proto.MessageKey.toObject(message.targetMessageId, options, q + 1);
            if (message.messageAddOns && message.messageAddOns.length) {
                object.messageAddOns = Array(message.messageAddOns.length);
                for (var j = 0; j < message.messageAddOns.length; ++j)
                    object.messageAddOns[j] = $root.proto.MessageAddOn.toObject(message.messageAddOns[j], options, q + 1);
            }
            if (message.statusMentionMessageInfo != null && Object.hasOwnProperty.call(message, "statusMentionMessageInfo"))
                object.statusMentionMessageInfo = $root.proto.StatusMentionMessage.toObject(message.statusMentionMessageInfo, options, q + 1);
            if (message.isSupportAiMessage != null && Object.hasOwnProperty.call(message, "isSupportAiMessage"))
                object.isSupportAiMessage = message.isSupportAiMessage;
            if (message.statusMentionSources && message.statusMentionSources.length) {
                object.statusMentionSources = Array(message.statusMentionSources.length);
                for (var j = 0; j < message.statusMentionSources.length; ++j)
                    object.statusMentionSources[j] = message.statusMentionSources[j];
            }
            if (message.supportAiCitations && message.supportAiCitations.length) {
                object.supportAiCitations = Array(message.supportAiCitations.length);
                for (var j = 0; j < message.supportAiCitations.length; ++j)
                    object.supportAiCitations[j] = $root.proto.Citation.toObject(message.supportAiCitations[j], options, q + 1);
            }
            if (message.botTargetId != null && Object.hasOwnProperty.call(message, "botTargetId"))
                object.botTargetId = message.botTargetId;
            if (message.groupHistoryIndividualMessageInfo != null && Object.hasOwnProperty.call(message, "groupHistoryIndividualMessageInfo"))
                object.groupHistoryIndividualMessageInfo = $root.proto.GroupHistoryIndividualMessageInfo.toObject(message.groupHistoryIndividualMessageInfo, options, q + 1);
            if (message.groupHistoryBundleInfo != null && Object.hasOwnProperty.call(message, "groupHistoryBundleInfo"))
                object.groupHistoryBundleInfo = $root.proto.GroupHistoryBundleInfo.toObject(message.groupHistoryBundleInfo, options, q + 1);
            if (message.interactiveMessageAdditionalMetadata != null && Object.hasOwnProperty.call(message, "interactiveMessageAdditionalMetadata"))
                object.interactiveMessageAdditionalMetadata = $root.proto.InteractiveMessageAdditionalMetadata.toObject(message.interactiveMessageAdditionalMetadata, options, q + 1);
            if (message.quarantinedMessage != null && Object.hasOwnProperty.call(message, "quarantinedMessage"))
                object.quarantinedMessage = $root.proto.QuarantinedMessage.toObject(message.quarantinedMessage, options, q + 1);
            if (message.nonJidMentions != null && Object.hasOwnProperty.call(message, "nonJidMentions"))
                object.nonJidMentions = message.nonJidMentions;
            if (message.hsmTag != null && Object.hasOwnProperty.call(message, "hsmTag"))
                object.hsmTag = message.hsmTag;
            if (message.ephemeralExpirationTimestamp != null && Object.hasOwnProperty.call(message, "ephemeralExpirationTimestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.ephemeralExpirationTimestamp = typeof message.ephemeralExpirationTimestamp === "number" ? BigInt(message.ephemeralExpirationTimestamp) : $util.Long.fromBits(message.ephemeralExpirationTimestamp.low >>> 0, message.ephemeralExpirationTimestamp.high >>> 0, true).toBigInt();
                else if (typeof message.ephemeralExpirationTimestamp === "number")
                    object.ephemeralExpirationTimestamp = options.longs === String ? String(message.ephemeralExpirationTimestamp) : message.ephemeralExpirationTimestamp;
                else
                    object.ephemeralExpirationTimestamp = options.longs === String ? $util.Long.prototype.toString.call(message.ephemeralExpirationTimestamp) : options.longs === Number ? new $util.LongBits(message.ephemeralExpirationTimestamp.low >>> 0, message.ephemeralExpirationTimestamp.high >>> 0).toNumber(true) : message.ephemeralExpirationTimestamp;
            if (message.scheduledMessageMetadata != null && Object.hasOwnProperty.call(message, "scheduledMessageMetadata"))
                object.scheduledMessageMetadata = $root.proto.ScheduledMessageMetadata.toObject(message.scheduledMessageMetadata, options, q + 1);
            if (message.decisionId != null && Object.hasOwnProperty.call(message, "decisionId"))
                object.decisionId = message.decisionId;
            if (message.decisionSources && message.decisionSources.length) {
                object.decisionSources = Array(message.decisionSources.length);
                for (var j = 0; j < message.decisionSources.length; ++j)
                    object.decisionSources[j] = message.decisionSources[j];
            }
            return object;
        };

        /**
         * Converts this WebMessageInfo to JSON.
         * @function toJSON
         * @memberof proto.WebMessageInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WebMessageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WebMessageInfo
         * @function getTypeUrl
         * @memberof proto.WebMessageInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WebMessageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.WebMessageInfo";
        };

        /**
         * BizPrivacyStatus enum.
         * @name proto.WebMessageInfo.BizPrivacyStatus
         * @enum {number}
         * @property {number} E2EE=0 E2EE value
         * @property {number} FB=2 FB value
         * @property {number} BSP=1 BSP value
         * @property {number} BSP_AND_FB=3 BSP_AND_FB value
         */
        WebMessageInfo.BizPrivacyStatus = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "E2EE"] = 0;
            values[valuesById[2] = "FB"] = 2;
            values[valuesById[1] = "BSP"] = 1;
            values[valuesById[3] = "BSP_AND_FB"] = 3;
            return values;
        })();

        /**
         * Status enum.
         * @name proto.WebMessageInfo.Status
         * @enum {number}
         * @property {number} ERROR=0 ERROR value
         * @property {number} PENDING=1 PENDING value
         * @property {number} SERVER_ACK=2 SERVER_ACK value
         * @property {number} DELIVERY_ACK=3 DELIVERY_ACK value
         * @property {number} READ=4 READ value
         * @property {number} PLAYED=5 PLAYED value
         */
        WebMessageInfo.Status = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "ERROR"] = 0;
            values[valuesById[1] = "PENDING"] = 1;
            values[valuesById[2] = "SERVER_ACK"] = 2;
            values[valuesById[3] = "DELIVERY_ACK"] = 3;
            values[valuesById[4] = "READ"] = 4;
            values[valuesById[5] = "PLAYED"] = 5;
            return values;
        })();

        /**
         * StubType enum.
         * @name proto.WebMessageInfo.StubType
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} REVOKE=1 REVOKE value
         * @property {number} CIPHERTEXT=2 CIPHERTEXT value
         * @property {number} FUTUREPROOF=3 FUTUREPROOF value
         * @property {number} NON_VERIFIED_TRANSITION=4 NON_VERIFIED_TRANSITION value
         * @property {number} UNVERIFIED_TRANSITION=5 UNVERIFIED_TRANSITION value
         * @property {number} VERIFIED_TRANSITION=6 VERIFIED_TRANSITION value
         * @property {number} VERIFIED_LOW_UNKNOWN=7 VERIFIED_LOW_UNKNOWN value
         * @property {number} VERIFIED_HIGH=8 VERIFIED_HIGH value
         * @property {number} VERIFIED_INITIAL_UNKNOWN=9 VERIFIED_INITIAL_UNKNOWN value
         * @property {number} VERIFIED_INITIAL_LOW=10 VERIFIED_INITIAL_LOW value
         * @property {number} VERIFIED_INITIAL_HIGH=11 VERIFIED_INITIAL_HIGH value
         * @property {number} VERIFIED_TRANSITION_ANY_TO_NONE=12 VERIFIED_TRANSITION_ANY_TO_NONE value
         * @property {number} VERIFIED_TRANSITION_ANY_TO_HIGH=13 VERIFIED_TRANSITION_ANY_TO_HIGH value
         * @property {number} VERIFIED_TRANSITION_HIGH_TO_LOW=14 VERIFIED_TRANSITION_HIGH_TO_LOW value
         * @property {number} VERIFIED_TRANSITION_HIGH_TO_UNKNOWN=15 VERIFIED_TRANSITION_HIGH_TO_UNKNOWN value
         * @property {number} VERIFIED_TRANSITION_UNKNOWN_TO_LOW=16 VERIFIED_TRANSITION_UNKNOWN_TO_LOW value
         * @property {number} VERIFIED_TRANSITION_LOW_TO_UNKNOWN=17 VERIFIED_TRANSITION_LOW_TO_UNKNOWN value
         * @property {number} VERIFIED_TRANSITION_NONE_TO_LOW=18 VERIFIED_TRANSITION_NONE_TO_LOW value
         * @property {number} VERIFIED_TRANSITION_NONE_TO_UNKNOWN=19 VERIFIED_TRANSITION_NONE_TO_UNKNOWN value
         * @property {number} GROUP_CREATE=20 GROUP_CREATE value
         * @property {number} GROUP_CHANGE_SUBJECT=21 GROUP_CHANGE_SUBJECT value
         * @property {number} GROUP_CHANGE_ICON=22 GROUP_CHANGE_ICON value
         * @property {number} GROUP_CHANGE_INVITE_LINK=23 GROUP_CHANGE_INVITE_LINK value
         * @property {number} GROUP_CHANGE_DESCRIPTION=24 GROUP_CHANGE_DESCRIPTION value
         * @property {number} GROUP_CHANGE_RESTRICT=25 GROUP_CHANGE_RESTRICT value
         * @property {number} GROUP_CHANGE_ANNOUNCE=26 GROUP_CHANGE_ANNOUNCE value
         * @property {number} GROUP_PARTICIPANT_ADD=27 GROUP_PARTICIPANT_ADD value
         * @property {number} GROUP_PARTICIPANT_REMOVE=28 GROUP_PARTICIPANT_REMOVE value
         * @property {number} GROUP_PARTICIPANT_PROMOTE=29 GROUP_PARTICIPANT_PROMOTE value
         * @property {number} GROUP_PARTICIPANT_DEMOTE=30 GROUP_PARTICIPANT_DEMOTE value
         * @property {number} GROUP_PARTICIPANT_INVITE=31 GROUP_PARTICIPANT_INVITE value
         * @property {number} GROUP_PARTICIPANT_LEAVE=32 GROUP_PARTICIPANT_LEAVE value
         * @property {number} GROUP_PARTICIPANT_CHANGE_NUMBER=33 GROUP_PARTICIPANT_CHANGE_NUMBER value
         * @property {number} BROADCAST_CREATE=34 BROADCAST_CREATE value
         * @property {number} BROADCAST_ADD=35 BROADCAST_ADD value
         * @property {number} BROADCAST_REMOVE=36 BROADCAST_REMOVE value
         * @property {number} GENERIC_NOTIFICATION=37 GENERIC_NOTIFICATION value
         * @property {number} E2E_IDENTITY_CHANGED=38 E2E_IDENTITY_CHANGED value
         * @property {number} E2E_ENCRYPTED=39 E2E_ENCRYPTED value
         * @property {number} CALL_MISSED_VOICE=40 CALL_MISSED_VOICE value
         * @property {number} CALL_MISSED_VIDEO=41 CALL_MISSED_VIDEO value
         * @property {number} INDIVIDUAL_CHANGE_NUMBER=42 INDIVIDUAL_CHANGE_NUMBER value
         * @property {number} GROUP_DELETE=43 GROUP_DELETE value
         * @property {number} GROUP_ANNOUNCE_MODE_MESSAGE_BOUNCE=44 GROUP_ANNOUNCE_MODE_MESSAGE_BOUNCE value
         * @property {number} CALL_MISSED_GROUP_VOICE=45 CALL_MISSED_GROUP_VOICE value
         * @property {number} CALL_MISSED_GROUP_VIDEO=46 CALL_MISSED_GROUP_VIDEO value
         * @property {number} PAYMENT_CIPHERTEXT=47 PAYMENT_CIPHERTEXT value
         * @property {number} PAYMENT_FUTUREPROOF=48 PAYMENT_FUTUREPROOF value
         * @property {number} PAYMENT_TRANSACTION_STATUS_UPDATE_FAILED=49 PAYMENT_TRANSACTION_STATUS_UPDATE_FAILED value
         * @property {number} PAYMENT_TRANSACTION_STATUS_UPDATE_REFUNDED=50 PAYMENT_TRANSACTION_STATUS_UPDATE_REFUNDED value
         * @property {number} PAYMENT_TRANSACTION_STATUS_UPDATE_REFUND_FAILED=51 PAYMENT_TRANSACTION_STATUS_UPDATE_REFUND_FAILED value
         * @property {number} PAYMENT_TRANSACTION_STATUS_RECEIVER_PENDING_SETUP=52 PAYMENT_TRANSACTION_STATUS_RECEIVER_PENDING_SETUP value
         * @property {number} PAYMENT_TRANSACTION_STATUS_RECEIVER_SUCCESS_AFTER_HICCUP=53 PAYMENT_TRANSACTION_STATUS_RECEIVER_SUCCESS_AFTER_HICCUP value
         * @property {number} PAYMENT_ACTION_ACCOUNT_SETUP_REMINDER=54 PAYMENT_ACTION_ACCOUNT_SETUP_REMINDER value
         * @property {number} PAYMENT_ACTION_SEND_PAYMENT_REMINDER=55 PAYMENT_ACTION_SEND_PAYMENT_REMINDER value
         * @property {number} PAYMENT_ACTION_SEND_PAYMENT_INVITATION=56 PAYMENT_ACTION_SEND_PAYMENT_INVITATION value
         * @property {number} PAYMENT_ACTION_REQUEST_DECLINED=57 PAYMENT_ACTION_REQUEST_DECLINED value
         * @property {number} PAYMENT_ACTION_REQUEST_EXPIRED=58 PAYMENT_ACTION_REQUEST_EXPIRED value
         * @property {number} PAYMENT_ACTION_REQUEST_CANCELLED=59 PAYMENT_ACTION_REQUEST_CANCELLED value
         * @property {number} BIZ_VERIFIED_TRANSITION_TOP_TO_BOTTOM=60 BIZ_VERIFIED_TRANSITION_TOP_TO_BOTTOM value
         * @property {number} BIZ_VERIFIED_TRANSITION_BOTTOM_TO_TOP=61 BIZ_VERIFIED_TRANSITION_BOTTOM_TO_TOP value
         * @property {number} BIZ_INTRO_TOP=62 BIZ_INTRO_TOP value
         * @property {number} BIZ_INTRO_BOTTOM=63 BIZ_INTRO_BOTTOM value
         * @property {number} BIZ_NAME_CHANGE=64 BIZ_NAME_CHANGE value
         * @property {number} BIZ_MOVE_TO_CONSUMER_APP=65 BIZ_MOVE_TO_CONSUMER_APP value
         * @property {number} BIZ_TWO_TIER_MIGRATION_TOP=66 BIZ_TWO_TIER_MIGRATION_TOP value
         * @property {number} BIZ_TWO_TIER_MIGRATION_BOTTOM=67 BIZ_TWO_TIER_MIGRATION_BOTTOM value
         * @property {number} OVERSIZED=68 OVERSIZED value
         * @property {number} GROUP_CHANGE_NO_FREQUENTLY_FORWARDED=69 GROUP_CHANGE_NO_FREQUENTLY_FORWARDED value
         * @property {number} GROUP_V4_ADD_INVITE_SENT=70 GROUP_V4_ADD_INVITE_SENT value
         * @property {number} GROUP_PARTICIPANT_ADD_REQUEST_JOIN=71 GROUP_PARTICIPANT_ADD_REQUEST_JOIN value
         * @property {number} CHANGE_EPHEMERAL_SETTING=72 CHANGE_EPHEMERAL_SETTING value
         * @property {number} E2E_DEVICE_CHANGED=73 E2E_DEVICE_CHANGED value
         * @property {number} VIEWED_ONCE=74 VIEWED_ONCE value
         * @property {number} E2E_ENCRYPTED_NOW=75 E2E_ENCRYPTED_NOW value
         * @property {number} BLUE_MSG_BSP_FB_TO_BSP_PREMISE=76 BLUE_MSG_BSP_FB_TO_BSP_PREMISE value
         * @property {number} BLUE_MSG_BSP_FB_TO_SELF_FB=77 BLUE_MSG_BSP_FB_TO_SELF_FB value
         * @property {number} BLUE_MSG_BSP_FB_TO_SELF_PREMISE=78 BLUE_MSG_BSP_FB_TO_SELF_PREMISE value
         * @property {number} BLUE_MSG_BSP_FB_UNVERIFIED=79 BLUE_MSG_BSP_FB_UNVERIFIED value
         * @property {number} BLUE_MSG_BSP_FB_UNVERIFIED_TO_SELF_PREMISE_VERIFIED=80 BLUE_MSG_BSP_FB_UNVERIFIED_TO_SELF_PREMISE_VERIFIED value
         * @property {number} BLUE_MSG_BSP_FB_VERIFIED=81 BLUE_MSG_BSP_FB_VERIFIED value
         * @property {number} BLUE_MSG_BSP_FB_VERIFIED_TO_SELF_PREMISE_UNVERIFIED=82 BLUE_MSG_BSP_FB_VERIFIED_TO_SELF_PREMISE_UNVERIFIED value
         * @property {number} BLUE_MSG_BSP_PREMISE_TO_SELF_PREMISE=83 BLUE_MSG_BSP_PREMISE_TO_SELF_PREMISE value
         * @property {number} BLUE_MSG_BSP_PREMISE_UNVERIFIED=84 BLUE_MSG_BSP_PREMISE_UNVERIFIED value
         * @property {number} BLUE_MSG_BSP_PREMISE_UNVERIFIED_TO_SELF_PREMISE_VERIFIED=85 BLUE_MSG_BSP_PREMISE_UNVERIFIED_TO_SELF_PREMISE_VERIFIED value
         * @property {number} BLUE_MSG_BSP_PREMISE_VERIFIED=86 BLUE_MSG_BSP_PREMISE_VERIFIED value
         * @property {number} BLUE_MSG_BSP_PREMISE_VERIFIED_TO_SELF_PREMISE_UNVERIFIED=87 BLUE_MSG_BSP_PREMISE_VERIFIED_TO_SELF_PREMISE_UNVERIFIED value
         * @property {number} BLUE_MSG_CONSUMER_TO_BSP_FB_UNVERIFIED=88 BLUE_MSG_CONSUMER_TO_BSP_FB_UNVERIFIED value
         * @property {number} BLUE_MSG_CONSUMER_TO_BSP_PREMISE_UNVERIFIED=89 BLUE_MSG_CONSUMER_TO_BSP_PREMISE_UNVERIFIED value
         * @property {number} BLUE_MSG_CONSUMER_TO_SELF_FB_UNVERIFIED=90 BLUE_MSG_CONSUMER_TO_SELF_FB_UNVERIFIED value
         * @property {number} BLUE_MSG_CONSUMER_TO_SELF_PREMISE_UNVERIFIED=91 BLUE_MSG_CONSUMER_TO_SELF_PREMISE_UNVERIFIED value
         * @property {number} BLUE_MSG_SELF_FB_TO_BSP_PREMISE=92 BLUE_MSG_SELF_FB_TO_BSP_PREMISE value
         * @property {number} BLUE_MSG_SELF_FB_TO_SELF_PREMISE=93 BLUE_MSG_SELF_FB_TO_SELF_PREMISE value
         * @property {number} BLUE_MSG_SELF_FB_UNVERIFIED=94 BLUE_MSG_SELF_FB_UNVERIFIED value
         * @property {number} BLUE_MSG_SELF_FB_UNVERIFIED_TO_SELF_PREMISE_VERIFIED=95 BLUE_MSG_SELF_FB_UNVERIFIED_TO_SELF_PREMISE_VERIFIED value
         * @property {number} BLUE_MSG_SELF_FB_VERIFIED=96 BLUE_MSG_SELF_FB_VERIFIED value
         * @property {number} BLUE_MSG_SELF_FB_VERIFIED_TO_SELF_PREMISE_UNVERIFIED=97 BLUE_MSG_SELF_FB_VERIFIED_TO_SELF_PREMISE_UNVERIFIED value
         * @property {number} BLUE_MSG_SELF_PREMISE_TO_BSP_PREMISE=98 BLUE_MSG_SELF_PREMISE_TO_BSP_PREMISE value
         * @property {number} BLUE_MSG_SELF_PREMISE_UNVERIFIED=99 BLUE_MSG_SELF_PREMISE_UNVERIFIED value
         * @property {number} BLUE_MSG_SELF_PREMISE_VERIFIED=100 BLUE_MSG_SELF_PREMISE_VERIFIED value
         * @property {number} BLUE_MSG_TO_BSP_FB=101 BLUE_MSG_TO_BSP_FB value
         * @property {number} BLUE_MSG_TO_CONSUMER=102 BLUE_MSG_TO_CONSUMER value
         * @property {number} BLUE_MSG_TO_SELF_FB=103 BLUE_MSG_TO_SELF_FB value
         * @property {number} BLUE_MSG_UNVERIFIED_TO_BSP_FB_VERIFIED=104 BLUE_MSG_UNVERIFIED_TO_BSP_FB_VERIFIED value
         * @property {number} BLUE_MSG_UNVERIFIED_TO_BSP_PREMISE_VERIFIED=105 BLUE_MSG_UNVERIFIED_TO_BSP_PREMISE_VERIFIED value
         * @property {number} BLUE_MSG_UNVERIFIED_TO_SELF_FB_VERIFIED=106 BLUE_MSG_UNVERIFIED_TO_SELF_FB_VERIFIED value
         * @property {number} BLUE_MSG_UNVERIFIED_TO_VERIFIED=107 BLUE_MSG_UNVERIFIED_TO_VERIFIED value
         * @property {number} BLUE_MSG_VERIFIED_TO_BSP_FB_UNVERIFIED=108 BLUE_MSG_VERIFIED_TO_BSP_FB_UNVERIFIED value
         * @property {number} BLUE_MSG_VERIFIED_TO_BSP_PREMISE_UNVERIFIED=109 BLUE_MSG_VERIFIED_TO_BSP_PREMISE_UNVERIFIED value
         * @property {number} BLUE_MSG_VERIFIED_TO_SELF_FB_UNVERIFIED=110 BLUE_MSG_VERIFIED_TO_SELF_FB_UNVERIFIED value
         * @property {number} BLUE_MSG_VERIFIED_TO_UNVERIFIED=111 BLUE_MSG_VERIFIED_TO_UNVERIFIED value
         * @property {number} BLUE_MSG_BSP_FB_UNVERIFIED_TO_BSP_PREMISE_VERIFIED=112 BLUE_MSG_BSP_FB_UNVERIFIED_TO_BSP_PREMISE_VERIFIED value
         * @property {number} BLUE_MSG_BSP_FB_UNVERIFIED_TO_SELF_FB_VERIFIED=113 BLUE_MSG_BSP_FB_UNVERIFIED_TO_SELF_FB_VERIFIED value
         * @property {number} BLUE_MSG_BSP_FB_VERIFIED_TO_BSP_PREMISE_UNVERIFIED=114 BLUE_MSG_BSP_FB_VERIFIED_TO_BSP_PREMISE_UNVERIFIED value
         * @property {number} BLUE_MSG_BSP_FB_VERIFIED_TO_SELF_FB_UNVERIFIED=115 BLUE_MSG_BSP_FB_VERIFIED_TO_SELF_FB_UNVERIFIED value
         * @property {number} BLUE_MSG_SELF_FB_UNVERIFIED_TO_BSP_PREMISE_VERIFIED=116 BLUE_MSG_SELF_FB_UNVERIFIED_TO_BSP_PREMISE_VERIFIED value
         * @property {number} BLUE_MSG_SELF_FB_VERIFIED_TO_BSP_PREMISE_UNVERIFIED=117 BLUE_MSG_SELF_FB_VERIFIED_TO_BSP_PREMISE_UNVERIFIED value
         * @property {number} E2E_IDENTITY_UNAVAILABLE=118 E2E_IDENTITY_UNAVAILABLE value
         * @property {number} GROUP_CREATING=119 GROUP_CREATING value
         * @property {number} GROUP_CREATE_FAILED=120 GROUP_CREATE_FAILED value
         * @property {number} GROUP_BOUNCED=121 GROUP_BOUNCED value
         * @property {number} BLOCK_CONTACT=122 BLOCK_CONTACT value
         * @property {number} EPHEMERAL_SETTING_NOT_APPLIED=123 EPHEMERAL_SETTING_NOT_APPLIED value
         * @property {number} SYNC_FAILED=124 SYNC_FAILED value
         * @property {number} SYNCING=125 SYNCING value
         * @property {number} BIZ_PRIVACY_MODE_INIT_FB=126 BIZ_PRIVACY_MODE_INIT_FB value
         * @property {number} BIZ_PRIVACY_MODE_INIT_BSP=127 BIZ_PRIVACY_MODE_INIT_BSP value
         * @property {number} BIZ_PRIVACY_MODE_TO_FB=128 BIZ_PRIVACY_MODE_TO_FB value
         * @property {number} BIZ_PRIVACY_MODE_TO_BSP=129 BIZ_PRIVACY_MODE_TO_BSP value
         * @property {number} DISAPPEARING_MODE=130 DISAPPEARING_MODE value
         * @property {number} E2E_DEVICE_FETCH_FAILED=131 E2E_DEVICE_FETCH_FAILED value
         * @property {number} ADMIN_REVOKE=132 ADMIN_REVOKE value
         * @property {number} GROUP_INVITE_LINK_GROWTH_LOCKED=133 GROUP_INVITE_LINK_GROWTH_LOCKED value
         * @property {number} COMMUNITY_LINK_PARENT_GROUP=134 COMMUNITY_LINK_PARENT_GROUP value
         * @property {number} COMMUNITY_LINK_SIBLING_GROUP=135 COMMUNITY_LINK_SIBLING_GROUP value
         * @property {number} COMMUNITY_LINK_SUB_GROUP=136 COMMUNITY_LINK_SUB_GROUP value
         * @property {number} COMMUNITY_UNLINK_PARENT_GROUP=137 COMMUNITY_UNLINK_PARENT_GROUP value
         * @property {number} COMMUNITY_UNLINK_SIBLING_GROUP=138 COMMUNITY_UNLINK_SIBLING_GROUP value
         * @property {number} COMMUNITY_UNLINK_SUB_GROUP=139 COMMUNITY_UNLINK_SUB_GROUP value
         * @property {number} GROUP_PARTICIPANT_ACCEPT=140 GROUP_PARTICIPANT_ACCEPT value
         * @property {number} GROUP_PARTICIPANT_LINKED_GROUP_JOIN=141 GROUP_PARTICIPANT_LINKED_GROUP_JOIN value
         * @property {number} COMMUNITY_CREATE=142 COMMUNITY_CREATE value
         * @property {number} EPHEMERAL_KEEP_IN_CHAT=143 EPHEMERAL_KEEP_IN_CHAT value
         * @property {number} GROUP_MEMBERSHIP_JOIN_APPROVAL_REQUEST=144 GROUP_MEMBERSHIP_JOIN_APPROVAL_REQUEST value
         * @property {number} GROUP_MEMBERSHIP_JOIN_APPROVAL_MODE=145 GROUP_MEMBERSHIP_JOIN_APPROVAL_MODE value
         * @property {number} INTEGRITY_UNLINK_PARENT_GROUP=146 INTEGRITY_UNLINK_PARENT_GROUP value
         * @property {number} COMMUNITY_PARTICIPANT_PROMOTE=147 COMMUNITY_PARTICIPANT_PROMOTE value
         * @property {number} COMMUNITY_PARTICIPANT_DEMOTE=148 COMMUNITY_PARTICIPANT_DEMOTE value
         * @property {number} COMMUNITY_PARENT_GROUP_DELETED=149 COMMUNITY_PARENT_GROUP_DELETED value
         * @property {number} COMMUNITY_LINK_PARENT_GROUP_MEMBERSHIP_APPROVAL=150 COMMUNITY_LINK_PARENT_GROUP_MEMBERSHIP_APPROVAL value
         * @property {number} GROUP_PARTICIPANT_JOINED_GROUP_AND_PARENT_GROUP=151 GROUP_PARTICIPANT_JOINED_GROUP_AND_PARENT_GROUP value
         * @property {number} MASKED_THREAD_CREATED=152 MASKED_THREAD_CREATED value
         * @property {number} MASKED_THREAD_UNMASKED=153 MASKED_THREAD_UNMASKED value
         * @property {number} BIZ_CHAT_ASSIGNMENT=154 BIZ_CHAT_ASSIGNMENT value
         * @property {number} CHAT_PSA=155 CHAT_PSA value
         * @property {number} CHAT_POLL_CREATION_MESSAGE=156 CHAT_POLL_CREATION_MESSAGE value
         * @property {number} CAG_MASKED_THREAD_CREATED=157 CAG_MASKED_THREAD_CREATED value
         * @property {number} COMMUNITY_PARENT_GROUP_SUBJECT_CHANGED=158 COMMUNITY_PARENT_GROUP_SUBJECT_CHANGED value
         * @property {number} CAG_INVITE_AUTO_ADD=159 CAG_INVITE_AUTO_ADD value
         * @property {number} BIZ_CHAT_ASSIGNMENT_UNASSIGN=160 BIZ_CHAT_ASSIGNMENT_UNASSIGN value
         * @property {number} CAG_INVITE_AUTO_JOINED=161 CAG_INVITE_AUTO_JOINED value
         * @property {number} SCHEDULED_CALL_START_MESSAGE=162 SCHEDULED_CALL_START_MESSAGE value
         * @property {number} COMMUNITY_INVITE_RICH=163 COMMUNITY_INVITE_RICH value
         * @property {number} COMMUNITY_INVITE_AUTO_ADD_RICH=164 COMMUNITY_INVITE_AUTO_ADD_RICH value
         * @property {number} SUB_GROUP_INVITE_RICH=165 SUB_GROUP_INVITE_RICH value
         * @property {number} SUB_GROUP_PARTICIPANT_ADD_RICH=166 SUB_GROUP_PARTICIPANT_ADD_RICH value
         * @property {number} COMMUNITY_LINK_PARENT_GROUP_RICH=167 COMMUNITY_LINK_PARENT_GROUP_RICH value
         * @property {number} COMMUNITY_PARTICIPANT_ADD_RICH=168 COMMUNITY_PARTICIPANT_ADD_RICH value
         * @property {number} SILENCED_UNKNOWN_CALLER_AUDIO=169 SILENCED_UNKNOWN_CALLER_AUDIO value
         * @property {number} SILENCED_UNKNOWN_CALLER_VIDEO=170 SILENCED_UNKNOWN_CALLER_VIDEO value
         * @property {number} GROUP_MEMBER_ADD_MODE=171 GROUP_MEMBER_ADD_MODE value
         * @property {number} GROUP_MEMBERSHIP_JOIN_APPROVAL_REQUEST_NON_ADMIN_ADD=172 GROUP_MEMBERSHIP_JOIN_APPROVAL_REQUEST_NON_ADMIN_ADD value
         * @property {number} COMMUNITY_CHANGE_DESCRIPTION=173 COMMUNITY_CHANGE_DESCRIPTION value
         * @property {number} SENDER_INVITE=174 SENDER_INVITE value
         * @property {number} RECEIVER_INVITE=175 RECEIVER_INVITE value
         * @property {number} COMMUNITY_ALLOW_MEMBER_ADDED_GROUPS=176 COMMUNITY_ALLOW_MEMBER_ADDED_GROUPS value
         * @property {number} PINNED_MESSAGE_IN_CHAT=177 PINNED_MESSAGE_IN_CHAT value
         * @property {number} PAYMENT_INVITE_SETUP_INVITER=178 PAYMENT_INVITE_SETUP_INVITER value
         * @property {number} PAYMENT_INVITE_SETUP_INVITEE_RECEIVE_ONLY=179 PAYMENT_INVITE_SETUP_INVITEE_RECEIVE_ONLY value
         * @property {number} PAYMENT_INVITE_SETUP_INVITEE_SEND_AND_RECEIVE=180 PAYMENT_INVITE_SETUP_INVITEE_SEND_AND_RECEIVE value
         * @property {number} LINKED_GROUP_CALL_START=181 LINKED_GROUP_CALL_START value
         * @property {number} REPORT_TO_ADMIN_ENABLED_STATUS=182 REPORT_TO_ADMIN_ENABLED_STATUS value
         * @property {number} EMPTY_SUBGROUP_CREATE=183 EMPTY_SUBGROUP_CREATE value
         * @property {number} SCHEDULED_CALL_CANCEL=184 SCHEDULED_CALL_CANCEL value
         * @property {number} SUBGROUP_ADMIN_TRIGGERED_AUTO_ADD_RICH=185 SUBGROUP_ADMIN_TRIGGERED_AUTO_ADD_RICH value
         * @property {number} GROUP_CHANGE_RECENT_HISTORY_SHARING=186 GROUP_CHANGE_RECENT_HISTORY_SHARING value
         * @property {number} PAID_MESSAGE_SERVER_CAMPAIGN_ID=187 PAID_MESSAGE_SERVER_CAMPAIGN_ID value
         * @property {number} GENERAL_CHAT_CREATE=188 GENERAL_CHAT_CREATE value
         * @property {number} GENERAL_CHAT_ADD=189 GENERAL_CHAT_ADD value
         * @property {number} GENERAL_CHAT_AUTO_ADD_DISABLED=190 GENERAL_CHAT_AUTO_ADD_DISABLED value
         * @property {number} SUGGESTED_SUBGROUP_ANNOUNCE=191 SUGGESTED_SUBGROUP_ANNOUNCE value
         * @property {number} BIZ_BOT_1P_MESSAGING_ENABLED=192 BIZ_BOT_1P_MESSAGING_ENABLED value
         * @property {number} CHANGE_USERNAME=193 CHANGE_USERNAME value
         * @property {number} BIZ_COEX_PRIVACY_INIT_SELF=194 BIZ_COEX_PRIVACY_INIT_SELF value
         * @property {number} BIZ_COEX_PRIVACY_TRANSITION_SELF=195 BIZ_COEX_PRIVACY_TRANSITION_SELF value
         * @property {number} SUPPORT_AI_EDUCATION=196 SUPPORT_AI_EDUCATION value
         * @property {number} BIZ_BOT_3P_MESSAGING_ENABLED=197 BIZ_BOT_3P_MESSAGING_ENABLED value
         * @property {number} REMINDER_SETUP_MESSAGE=198 REMINDER_SETUP_MESSAGE value
         * @property {number} REMINDER_SENT_MESSAGE=199 REMINDER_SENT_MESSAGE value
         * @property {number} REMINDER_CANCEL_MESSAGE=200 REMINDER_CANCEL_MESSAGE value
         * @property {number} BIZ_COEX_PRIVACY_INIT=201 BIZ_COEX_PRIVACY_INIT value
         * @property {number} BIZ_COEX_PRIVACY_TRANSITION=202 BIZ_COEX_PRIVACY_TRANSITION value
         * @property {number} GROUP_DEACTIVATED=203 GROUP_DEACTIVATED value
         * @property {number} COMMUNITY_DEACTIVATE_SIBLING_GROUP=204 COMMUNITY_DEACTIVATE_SIBLING_GROUP value
         * @property {number} EVENT_UPDATED=205 EVENT_UPDATED value
         * @property {number} EVENT_CANCELED=206 EVENT_CANCELED value
         * @property {number} COMMUNITY_OWNER_UPDATED=207 COMMUNITY_OWNER_UPDATED value
         * @property {number} COMMUNITY_SUB_GROUP_VISIBILITY_HIDDEN=208 COMMUNITY_SUB_GROUP_VISIBILITY_HIDDEN value
         * @property {number} CAPI_GROUP_NE2EE_SYSTEM_MESSAGE=209 CAPI_GROUP_NE2EE_SYSTEM_MESSAGE value
         * @property {number} STATUS_MENTION=210 STATUS_MENTION value
         * @property {number} USER_CONTROLS_SYSTEM_MESSAGE=211 USER_CONTROLS_SYSTEM_MESSAGE value
         * @property {number} SUPPORT_SYSTEM_MESSAGE=212 SUPPORT_SYSTEM_MESSAGE value
         * @property {number} CHANGE_LID=213 CHANGE_LID value
         * @property {number} BIZ_CUSTOMER_3PD_DATA_SHARING_OPT_IN_MESSAGE=214 BIZ_CUSTOMER_3PD_DATA_SHARING_OPT_IN_MESSAGE value
         * @property {number} BIZ_CUSTOMER_3PD_DATA_SHARING_OPT_OUT_MESSAGE=215 BIZ_CUSTOMER_3PD_DATA_SHARING_OPT_OUT_MESSAGE value
         * @property {number} CHANGE_LIMIT_SHARING=216 CHANGE_LIMIT_SHARING value
         * @property {number} GROUP_MEMBER_LINK_MODE=217 GROUP_MEMBER_LINK_MODE value
         * @property {number} BIZ_AUTOMATICALLY_LABELED_CHAT_SYSTEM_MESSAGE=218 BIZ_AUTOMATICALLY_LABELED_CHAT_SYSTEM_MESSAGE value
         * @property {number} PHONE_NUMBER_HIDING_CHAT_DEPRECATED_MESSAGE=219 PHONE_NUMBER_HIDING_CHAT_DEPRECATED_MESSAGE value
         * @property {number} QUARANTINED_MESSAGE=220 QUARANTINED_MESSAGE value
         * @property {number} GROUP_MEMBER_SHARE_GROUP_HISTORY_MODE=221 GROUP_MEMBER_SHARE_GROUP_HISTORY_MODE value
         * @property {number} GROUP_OPEN_BOT_ADDED=222 GROUP_OPEN_BOT_ADDED value
         * @property {number} GROUP_TEE_BOT_ADDED=223 GROUP_TEE_BOT_ADDED value
         * @property {number} CONTACT_INFO=224 CONTACT_INFO value
         * @property {number} SCHEDULED_MESSAGE_CREATED=225 SCHEDULED_MESSAGE_CREATED value
         * @property {number} IDENTITY_TRUST_MARKED=226 IDENTITY_TRUST_MARKED value
         * @property {number} IDENTITY_TRUST_UNMARKED=227 IDENTITY_TRUST_UNMARKED value
         * @property {number} IDENTITY_TRUST_REVOKED=228 IDENTITY_TRUST_REVOKED value
         * @property {number} CTWA_CONSUMER_DISCLOSURE=230 CTWA_CONSUMER_DISCLOSURE value
         */
        WebMessageInfo.StubType = (function() {
            var valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "REVOKE"] = 1;
            values[valuesById[2] = "CIPHERTEXT"] = 2;
            values[valuesById[3] = "FUTUREPROOF"] = 3;
            values[valuesById[4] = "NON_VERIFIED_TRANSITION"] = 4;
            values[valuesById[5] = "UNVERIFIED_TRANSITION"] = 5;
            values[valuesById[6] = "VERIFIED_TRANSITION"] = 6;
            values[valuesById[7] = "VERIFIED_LOW_UNKNOWN"] = 7;
            values[valuesById[8] = "VERIFIED_HIGH"] = 8;
            values[valuesById[9] = "VERIFIED_INITIAL_UNKNOWN"] = 9;
            values[valuesById[10] = "VERIFIED_INITIAL_LOW"] = 10;
            values[valuesById[11] = "VERIFIED_INITIAL_HIGH"] = 11;
            values[valuesById[12] = "VERIFIED_TRANSITION_ANY_TO_NONE"] = 12;
            values[valuesById[13] = "VERIFIED_TRANSITION_ANY_TO_HIGH"] = 13;
            values[valuesById[14] = "VERIFIED_TRANSITION_HIGH_TO_LOW"] = 14;
            values[valuesById[15] = "VERIFIED_TRANSITION_HIGH_TO_UNKNOWN"] = 15;
            values[valuesById[16] = "VERIFIED_TRANSITION_UNKNOWN_TO_LOW"] = 16;
            values[valuesById[17] = "VERIFIED_TRANSITION_LOW_TO_UNKNOWN"] = 17;
            values[valuesById[18] = "VERIFIED_TRANSITION_NONE_TO_LOW"] = 18;
            values[valuesById[19] = "VERIFIED_TRANSITION_NONE_TO_UNKNOWN"] = 19;
            values[valuesById[20] = "GROUP_CREATE"] = 20;
            values[valuesById[21] = "GROUP_CHANGE_SUBJECT"] = 21;
            values[valuesById[22] = "GROUP_CHANGE_ICON"] = 22;
            values[valuesById[23] = "GROUP_CHANGE_INVITE_LINK"] = 23;
            values[valuesById[24] = "GROUP_CHANGE_DESCRIPTION"] = 24;
            values[valuesById[25] = "GROUP_CHANGE_RESTRICT"] = 25;
            values[valuesById[26] = "GROUP_CHANGE_ANNOUNCE"] = 26;
            values[valuesById[27] = "GROUP_PARTICIPANT_ADD"] = 27;
            values[valuesById[28] = "GROUP_PARTICIPANT_REMOVE"] = 28;
            values[valuesById[29] = "GROUP_PARTICIPANT_PROMOTE"] = 29;
            values[valuesById[30] = "GROUP_PARTICIPANT_DEMOTE"] = 30;
            values[valuesById[31] = "GROUP_PARTICIPANT_INVITE"] = 31;
            values[valuesById[32] = "GROUP_PARTICIPANT_LEAVE"] = 32;
            values[valuesById[33] = "GROUP_PARTICIPANT_CHANGE_NUMBER"] = 33;
            values[valuesById[34] = "BROADCAST_CREATE"] = 34;
            values[valuesById[35] = "BROADCAST_ADD"] = 35;
            values[valuesById[36] = "BROADCAST_REMOVE"] = 36;
            values[valuesById[37] = "GENERIC_NOTIFICATION"] = 37;
            values[valuesById[38] = "E2E_IDENTITY_CHANGED"] = 38;
            values[valuesById[39] = "E2E_ENCRYPTED"] = 39;
            values[valuesById[40] = "CALL_MISSED_VOICE"] = 40;
            values[valuesById[41] = "CALL_MISSED_VIDEO"] = 41;
            values[valuesById[42] = "INDIVIDUAL_CHANGE_NUMBER"] = 42;
            values[valuesById[43] = "GROUP_DELETE"] = 43;
            values[valuesById[44] = "GROUP_ANNOUNCE_MODE_MESSAGE_BOUNCE"] = 44;
            values[valuesById[45] = "CALL_MISSED_GROUP_VOICE"] = 45;
            values[valuesById[46] = "CALL_MISSED_GROUP_VIDEO"] = 46;
            values[valuesById[47] = "PAYMENT_CIPHERTEXT"] = 47;
            values[valuesById[48] = "PAYMENT_FUTUREPROOF"] = 48;
            values[valuesById[49] = "PAYMENT_TRANSACTION_STATUS_UPDATE_FAILED"] = 49;
            values[valuesById[50] = "PAYMENT_TRANSACTION_STATUS_UPDATE_REFUNDED"] = 50;
            values[valuesById[51] = "PAYMENT_TRANSACTION_STATUS_UPDATE_REFUND_FAILED"] = 51;
            values[valuesById[52] = "PAYMENT_TRANSACTION_STATUS_RECEIVER_PENDING_SETUP"] = 52;
            values[valuesById[53] = "PAYMENT_TRANSACTION_STATUS_RECEIVER_SUCCESS_AFTER_HICCUP"] = 53;
            values[valuesById[54] = "PAYMENT_ACTION_ACCOUNT_SETUP_REMINDER"] = 54;
            values[valuesById[55] = "PAYMENT_ACTION_SEND_PAYMENT_REMINDER"] = 55;
            values[valuesById[56] = "PAYMENT_ACTION_SEND_PAYMENT_INVITATION"] = 56;
            values[valuesById[57] = "PAYMENT_ACTION_REQUEST_DECLINED"] = 57;
            values[valuesById[58] = "PAYMENT_ACTION_REQUEST_EXPIRED"] = 58;
            values[valuesById[59] = "PAYMENT_ACTION_REQUEST_CANCELLED"] = 59;
            values[valuesById[60] = "BIZ_VERIFIED_TRANSITION_TOP_TO_BOTTOM"] = 60;
            values[valuesById[61] = "BIZ_VERIFIED_TRANSITION_BOTTOM_TO_TOP"] = 61;
            values[valuesById[62] = "BIZ_INTRO_TOP"] = 62;
            values[valuesById[63] = "BIZ_INTRO_BOTTOM"] = 63;
            values[valuesById[64] = "BIZ_NAME_CHANGE"] = 64;
            values[valuesById[65] = "BIZ_MOVE_TO_CONSUMER_APP"] = 65;
            values[valuesById[66] = "BIZ_TWO_TIER_MIGRATION_TOP"] = 66;
            values[valuesById[67] = "BIZ_TWO_TIER_MIGRATION_BOTTOM"] = 67;
            values[valuesById[68] = "OVERSIZED"] = 68;
            values[valuesById[69] = "GROUP_CHANGE_NO_FREQUENTLY_FORWARDED"] = 69;
            values[valuesById[70] = "GROUP_V4_ADD_INVITE_SENT"] = 70;
            values[valuesById[71] = "GROUP_PARTICIPANT_ADD_REQUEST_JOIN"] = 71;
            values[valuesById[72] = "CHANGE_EPHEMERAL_SETTING"] = 72;
            values[valuesById[73] = "E2E_DEVICE_CHANGED"] = 73;
            values[valuesById[74] = "VIEWED_ONCE"] = 74;
            values[valuesById[75] = "E2E_ENCRYPTED_NOW"] = 75;
            values[valuesById[76] = "BLUE_MSG_BSP_FB_TO_BSP_PREMISE"] = 76;
            values[valuesById[77] = "BLUE_MSG_BSP_FB_TO_SELF_FB"] = 77;
            values[valuesById[78] = "BLUE_MSG_BSP_FB_TO_SELF_PREMISE"] = 78;
            values[valuesById[79] = "BLUE_MSG_BSP_FB_UNVERIFIED"] = 79;
            values[valuesById[80] = "BLUE_MSG_BSP_FB_UNVERIFIED_TO_SELF_PREMISE_VERIFIED"] = 80;
            values[valuesById[81] = "BLUE_MSG_BSP_FB_VERIFIED"] = 81;
            values[valuesById[82] = "BLUE_MSG_BSP_FB_VERIFIED_TO_SELF_PREMISE_UNVERIFIED"] = 82;
            values[valuesById[83] = "BLUE_MSG_BSP_PREMISE_TO_SELF_PREMISE"] = 83;
            values[valuesById[84] = "BLUE_MSG_BSP_PREMISE_UNVERIFIED"] = 84;
            values[valuesById[85] = "BLUE_MSG_BSP_PREMISE_UNVERIFIED_TO_SELF_PREMISE_VERIFIED"] = 85;
            values[valuesById[86] = "BLUE_MSG_BSP_PREMISE_VERIFIED"] = 86;
            values[valuesById[87] = "BLUE_MSG_BSP_PREMISE_VERIFIED_TO_SELF_PREMISE_UNVERIFIED"] = 87;
            values[valuesById[88] = "BLUE_MSG_CONSUMER_TO_BSP_FB_UNVERIFIED"] = 88;
            values[valuesById[89] = "BLUE_MSG_CONSUMER_TO_BSP_PREMISE_UNVERIFIED"] = 89;
            values[valuesById[90] = "BLUE_MSG_CONSUMER_TO_SELF_FB_UNVERIFIED"] = 90;
            values[valuesById[91] = "BLUE_MSG_CONSUMER_TO_SELF_PREMISE_UNVERIFIED"] = 91;
            values[valuesById[92] = "BLUE_MSG_SELF_FB_TO_BSP_PREMISE"] = 92;
            values[valuesById[93] = "BLUE_MSG_SELF_FB_TO_SELF_PREMISE"] = 93;
            values[valuesById[94] = "BLUE_MSG_SELF_FB_UNVERIFIED"] = 94;
            values[valuesById[95] = "BLUE_MSG_SELF_FB_UNVERIFIED_TO_SELF_PREMISE_VERIFIED"] = 95;
            values[valuesById[96] = "BLUE_MSG_SELF_FB_VERIFIED"] = 96;
            values[valuesById[97] = "BLUE_MSG_SELF_FB_VERIFIED_TO_SELF_PREMISE_UNVERIFIED"] = 97;
            values[valuesById[98] = "BLUE_MSG_SELF_PREMISE_TO_BSP_PREMISE"] = 98;
            values[valuesById[99] = "BLUE_MSG_SELF_PREMISE_UNVERIFIED"] = 99;
            values[valuesById[100] = "BLUE_MSG_SELF_PREMISE_VERIFIED"] = 100;
            values[valuesById[101] = "BLUE_MSG_TO_BSP_FB"] = 101;
            values[valuesById[102] = "BLUE_MSG_TO_CONSUMER"] = 102;
            values[valuesById[103] = "BLUE_MSG_TO_SELF_FB"] = 103;
            values[valuesById[104] = "BLUE_MSG_UNVERIFIED_TO_BSP_FB_VERIFIED"] = 104;
            values[valuesById[105] = "BLUE_MSG_UNVERIFIED_TO_BSP_PREMISE_VERIFIED"] = 105;
            values[valuesById[106] = "BLUE_MSG_UNVERIFIED_TO_SELF_FB_VERIFIED"] = 106;
            values[valuesById[107] = "BLUE_MSG_UNVERIFIED_TO_VERIFIED"] = 107;
            values[valuesById[108] = "BLUE_MSG_VERIFIED_TO_BSP_FB_UNVERIFIED"] = 108;
            values[valuesById[109] = "BLUE_MSG_VERIFIED_TO_BSP_PREMISE_UNVERIFIED"] = 109;
            values[valuesById[110] = "BLUE_MSG_VERIFIED_TO_SELF_FB_UNVERIFIED"] = 110;
            values[valuesById[111] = "BLUE_MSG_VERIFIED_TO_UNVERIFIED"] = 111;
            values[valuesById[112] = "BLUE_MSG_BSP_FB_UNVERIFIED_TO_BSP_PREMISE_VERIFIED"] = 112;
            values[valuesById[113] = "BLUE_MSG_BSP_FB_UNVERIFIED_TO_SELF_FB_VERIFIED"] = 113;
            values[valuesById[114] = "BLUE_MSG_BSP_FB_VERIFIED_TO_BSP_PREMISE_UNVERIFIED"] = 114;
            values[valuesById[115] = "BLUE_MSG_BSP_FB_VERIFIED_TO_SELF_FB_UNVERIFIED"] = 115;
            values[valuesById[116] = "BLUE_MSG_SELF_FB_UNVERIFIED_TO_BSP_PREMISE_VERIFIED"] = 116;
            values[valuesById[117] = "BLUE_MSG_SELF_FB_VERIFIED_TO_BSP_PREMISE_UNVERIFIED"] = 117;
            values[valuesById[118] = "E2E_IDENTITY_UNAVAILABLE"] = 118;
            values[valuesById[119] = "GROUP_CREATING"] = 119;
            values[valuesById[120] = "GROUP_CREATE_FAILED"] = 120;
            values[valuesById[121] = "GROUP_BOUNCED"] = 121;
            values[valuesById[122] = "BLOCK_CONTACT"] = 122;
            values[valuesById[123] = "EPHEMERAL_SETTING_NOT_APPLIED"] = 123;
            values[valuesById[124] = "SYNC_FAILED"] = 124;
            values[valuesById[125] = "SYNCING"] = 125;
            values[valuesById[126] = "BIZ_PRIVACY_MODE_INIT_FB"] = 126;
            values[valuesById[127] = "BIZ_PRIVACY_MODE_INIT_BSP"] = 127;
            values[valuesById[128] = "BIZ_PRIVACY_MODE_TO_FB"] = 128;
            values[valuesById[129] = "BIZ_PRIVACY_MODE_TO_BSP"] = 129;
            values[valuesById[130] = "DISAPPEARING_MODE"] = 130;
            values[valuesById[131] = "E2E_DEVICE_FETCH_FAILED"] = 131;
            values[valuesById[132] = "ADMIN_REVOKE"] = 132;
            values[valuesById[133] = "GROUP_INVITE_LINK_GROWTH_LOCKED"] = 133;
            values[valuesById[134] = "COMMUNITY_LINK_PARENT_GROUP"] = 134;
            values[valuesById[135] = "COMMUNITY_LINK_SIBLING_GROUP"] = 135;
            values[valuesById[136] = "COMMUNITY_LINK_SUB_GROUP"] = 136;
            values[valuesById[137] = "COMMUNITY_UNLINK_PARENT_GROUP"] = 137;
            values[valuesById[138] = "COMMUNITY_UNLINK_SIBLING_GROUP"] = 138;
            values[valuesById[139] = "COMMUNITY_UNLINK_SUB_GROUP"] = 139;
            values[valuesById[140] = "GROUP_PARTICIPANT_ACCEPT"] = 140;
            values[valuesById[141] = "GROUP_PARTICIPANT_LINKED_GROUP_JOIN"] = 141;
            values[valuesById[142] = "COMMUNITY_CREATE"] = 142;
            values[valuesById[143] = "EPHEMERAL_KEEP_IN_CHAT"] = 143;
            values[valuesById[144] = "GROUP_MEMBERSHIP_JOIN_APPROVAL_REQUEST"] = 144;
            values[valuesById[145] = "GROUP_MEMBERSHIP_JOIN_APPROVAL_MODE"] = 145;
            values[valuesById[146] = "INTEGRITY_UNLINK_PARENT_GROUP"] = 146;
            values[valuesById[147] = "COMMUNITY_PARTICIPANT_PROMOTE"] = 147;
            values[valuesById[148] = "COMMUNITY_PARTICIPANT_DEMOTE"] = 148;
            values[valuesById[149] = "COMMUNITY_PARENT_GROUP_DELETED"] = 149;
            values[valuesById[150] = "COMMUNITY_LINK_PARENT_GROUP_MEMBERSHIP_APPROVAL"] = 150;
            values[valuesById[151] = "GROUP_PARTICIPANT_JOINED_GROUP_AND_PARENT_GROUP"] = 151;
            values[valuesById[152] = "MASKED_THREAD_CREATED"] = 152;
            values[valuesById[153] = "MASKED_THREAD_UNMASKED"] = 153;
            values[valuesById[154] = "BIZ_CHAT_ASSIGNMENT"] = 154;
            values[valuesById[155] = "CHAT_PSA"] = 155;
            values[valuesById[156] = "CHAT_POLL_CREATION_MESSAGE"] = 156;
            values[valuesById[157] = "CAG_MASKED_THREAD_CREATED"] = 157;
            values[valuesById[158] = "COMMUNITY_PARENT_GROUP_SUBJECT_CHANGED"] = 158;
            values[valuesById[159] = "CAG_INVITE_AUTO_ADD"] = 159;
            values[valuesById[160] = "BIZ_CHAT_ASSIGNMENT_UNASSIGN"] = 160;
            values[valuesById[161] = "CAG_INVITE_AUTO_JOINED"] = 161;
            values[valuesById[162] = "SCHEDULED_CALL_START_MESSAGE"] = 162;
            values[valuesById[163] = "COMMUNITY_INVITE_RICH"] = 163;
            values[valuesById[164] = "COMMUNITY_INVITE_AUTO_ADD_RICH"] = 164;
            values[valuesById[165] = "SUB_GROUP_INVITE_RICH"] = 165;
            values[valuesById[166] = "SUB_GROUP_PARTICIPANT_ADD_RICH"] = 166;
            values[valuesById[167] = "COMMUNITY_LINK_PARENT_GROUP_RICH"] = 167;
            values[valuesById[168] = "COMMUNITY_PARTICIPANT_ADD_RICH"] = 168;
            values[valuesById[169] = "SILENCED_UNKNOWN_CALLER_AUDIO"] = 169;
            values[valuesById[170] = "SILENCED_UNKNOWN_CALLER_VIDEO"] = 170;
            values[valuesById[171] = "GROUP_MEMBER_ADD_MODE"] = 171;
            values[valuesById[172] = "GROUP_MEMBERSHIP_JOIN_APPROVAL_REQUEST_NON_ADMIN_ADD"] = 172;
            values[valuesById[173] = "COMMUNITY_CHANGE_DESCRIPTION"] = 173;
            values[valuesById[174] = "SENDER_INVITE"] = 174;
            values[valuesById[175] = "RECEIVER_INVITE"] = 175;
            values[valuesById[176] = "COMMUNITY_ALLOW_MEMBER_ADDED_GROUPS"] = 176;
            values[valuesById[177] = "PINNED_MESSAGE_IN_CHAT"] = 177;
            values[valuesById[178] = "PAYMENT_INVITE_SETUP_INVITER"] = 178;
            values[valuesById[179] = "PAYMENT_INVITE_SETUP_INVITEE_RECEIVE_ONLY"] = 179;
            values[valuesById[180] = "PAYMENT_INVITE_SETUP_INVITEE_SEND_AND_RECEIVE"] = 180;
            values[valuesById[181] = "LINKED_GROUP_CALL_START"] = 181;
            values[valuesById[182] = "REPORT_TO_ADMIN_ENABLED_STATUS"] = 182;
            values[valuesById[183] = "EMPTY_SUBGROUP_CREATE"] = 183;
            values[valuesById[184] = "SCHEDULED_CALL_CANCEL"] = 184;
            values[valuesById[185] = "SUBGROUP_ADMIN_TRIGGERED_AUTO_ADD_RICH"] = 185;
            values[valuesById[186] = "GROUP_CHANGE_RECENT_HISTORY_SHARING"] = 186;
            values[valuesById[187] = "PAID_MESSAGE_SERVER_CAMPAIGN_ID"] = 187;
            values[valuesById[188] = "GENERAL_CHAT_CREATE"] = 188;
            values[valuesById[189] = "GENERAL_CHAT_ADD"] = 189;
            values[valuesById[190] = "GENERAL_CHAT_AUTO_ADD_DISABLED"] = 190;
            values[valuesById[191] = "SUGGESTED_SUBGROUP_ANNOUNCE"] = 191;
            values[valuesById[192] = "BIZ_BOT_1P_MESSAGING_ENABLED"] = 192;
            values[valuesById[193] = "CHANGE_USERNAME"] = 193;
            values[valuesById[194] = "BIZ_COEX_PRIVACY_INIT_SELF"] = 194;
            values[valuesById[195] = "BIZ_COEX_PRIVACY_TRANSITION_SELF"] = 195;
            values[valuesById[196] = "SUPPORT_AI_EDUCATION"] = 196;
            values[valuesById[197] = "BIZ_BOT_3P_MESSAGING_ENABLED"] = 197;
            values[valuesById[198] = "REMINDER_SETUP_MESSAGE"] = 198;
            values[valuesById[199] = "REMINDER_SENT_MESSAGE"] = 199;
            values[valuesById[200] = "REMINDER_CANCEL_MESSAGE"] = 200;
            values[valuesById[201] = "BIZ_COEX_PRIVACY_INIT"] = 201;
            values[valuesById[202] = "BIZ_COEX_PRIVACY_TRANSITION"] = 202;
            values[valuesById[203] = "GROUP_DEACTIVATED"] = 203;
            values[valuesById[204] = "COMMUNITY_DEACTIVATE_SIBLING_GROUP"] = 204;
            values[valuesById[205] = "EVENT_UPDATED"] = 205;
            values[valuesById[206] = "EVENT_CANCELED"] = 206;
            values[valuesById[207] = "COMMUNITY_OWNER_UPDATED"] = 207;
            values[valuesById[208] = "COMMUNITY_SUB_GROUP_VISIBILITY_HIDDEN"] = 208;
            values[valuesById[209] = "CAPI_GROUP_NE2EE_SYSTEM_MESSAGE"] = 209;
            values[valuesById[210] = "STATUS_MENTION"] = 210;
            values[valuesById[211] = "USER_CONTROLS_SYSTEM_MESSAGE"] = 211;
            values[valuesById[212] = "SUPPORT_SYSTEM_MESSAGE"] = 212;
            values[valuesById[213] = "CHANGE_LID"] = 213;
            values[valuesById[214] = "BIZ_CUSTOMER_3PD_DATA_SHARING_OPT_IN_MESSAGE"] = 214;
            values[valuesById[215] = "BIZ_CUSTOMER_3PD_DATA_SHARING_OPT_OUT_MESSAGE"] = 215;
            values[valuesById[216] = "CHANGE_LIMIT_SHARING"] = 216;
            values[valuesById[217] = "GROUP_MEMBER_LINK_MODE"] = 217;
            values[valuesById[218] = "BIZ_AUTOMATICALLY_LABELED_CHAT_SYSTEM_MESSAGE"] = 218;
            values[valuesById[219] = "PHONE_NUMBER_HIDING_CHAT_DEPRECATED_MESSAGE"] = 219;
            values[valuesById[220] = "QUARANTINED_MESSAGE"] = 220;
            values[valuesById[221] = "GROUP_MEMBER_SHARE_GROUP_HISTORY_MODE"] = 221;
            values[valuesById[222] = "GROUP_OPEN_BOT_ADDED"] = 222;
            values[valuesById[223] = "GROUP_TEE_BOT_ADDED"] = 223;
            values[valuesById[224] = "CONTACT_INFO"] = 224;
            values[valuesById[225] = "SCHEDULED_MESSAGE_CREATED"] = 225;
            values[valuesById[226] = "IDENTITY_TRUST_MARKED"] = 226;
            values[valuesById[227] = "IDENTITY_TRUST_UNMARKED"] = 227;
            values[valuesById[228] = "IDENTITY_TRUST_REVOKED"] = 228;
            values[valuesById[230] = "CTWA_CONSUMER_DISCLOSURE"] = 230;
            return values;
        })();

        return WebMessageInfo;
    })();

    proto.WebNotificationsInfo = (function() {

        /**
         * Properties of a WebNotificationsInfo.
         * @memberof proto
         * @interface IWebNotificationsInfo
         * @property {number|Long|null} [timestamp] WebNotificationsInfo timestamp
         * @property {number|null} [unreadChats] WebNotificationsInfo unreadChats
         * @property {number|null} [notifyMessageCount] WebNotificationsInfo notifyMessageCount
         * @property {Array.<proto.IWebMessageInfo>|null} [notifyMessages] WebNotificationsInfo notifyMessages
         */

        /**
         * Constructs a new WebNotificationsInfo.
         * @memberof proto
         * @classdesc Represents a WebNotificationsInfo.
         * @implements IWebNotificationsInfo
         * @constructor
         * @param {proto.IWebNotificationsInfo=} [properties] Properties to set
         */
        function WebNotificationsInfo(properties) {
            this.notifyMessages = [];
            if (properties)
                for (var keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WebNotificationsInfo timestamp.
         * @member {number|Long|null|undefined} timestamp
         * @memberof proto.WebNotificationsInfo
         * @instance
         */
        WebNotificationsInfo.prototype.timestamp = null;

        /**
         * WebNotificationsInfo unreadChats.
         * @member {number|null|undefined} unreadChats
         * @memberof proto.WebNotificationsInfo
         * @instance
         */
        WebNotificationsInfo.prototype.unreadChats = null;

        /**
         * WebNotificationsInfo notifyMessageCount.
         * @member {number|null|undefined} notifyMessageCount
         * @memberof proto.WebNotificationsInfo
         * @instance
         */
        WebNotificationsInfo.prototype.notifyMessageCount = null;

        /**
         * WebNotificationsInfo notifyMessages.
         * @member {Array.<proto.IWebMessageInfo>} notifyMessages
         * @memberof proto.WebNotificationsInfo
         * @instance
         */
        WebNotificationsInfo.prototype.notifyMessages = $util.emptyArray;

        // OneOf field names bound to virtual getters and setters
        var $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebNotificationsInfo.prototype, "_timestamp", {
            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebNotificationsInfo.prototype, "_unreadChats", {
            get: $util.oneOfGetter($oneOfFields = ["unreadChats"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebNotificationsInfo.prototype, "_notifyMessageCount", {
            get: $util.oneOfGetter($oneOfFields = ["notifyMessageCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new WebNotificationsInfo instance using the specified properties.
         * @function create
         * @memberof proto.WebNotificationsInfo
         * @static
         * @param {proto.IWebNotificationsInfo=} [properties] Properties to set
         * @returns {proto.WebNotificationsInfo} WebNotificationsInfo instance
         */
        WebNotificationsInfo.create = function create(properties) {
            return new WebNotificationsInfo(properties);
        };

        /**
         * Encodes the specified WebNotificationsInfo message. Does not implicitly {@link proto.WebNotificationsInfo.verify|verify} messages.
         * @function encode
         * @memberof proto.WebNotificationsInfo
         * @static
         * @param {proto.IWebNotificationsInfo} message WebNotificationsInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WebNotificationsInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.timestamp);
            if (message.unreadChats != null && Object.hasOwnProperty.call(message, "unreadChats"))
                writer.uint32(/* id 3, wireType 0 =*/24).uint32(message.unreadChats);
            if (message.notifyMessageCount != null && Object.hasOwnProperty.call(message, "notifyMessageCount"))
                writer.uint32(/* id 4, wireType 0 =*/32).uint32(message.notifyMessageCount);
            if (message.notifyMessages != null && message.notifyMessages.length)
                for (var i = 0; i < message.notifyMessages.length; ++i)
                    $root.proto.WebMessageInfo.encode(message.notifyMessages[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
            if (message.$unknowns != null && Object.hasOwnProperty.call(message, "$unknowns"))
                for (var i = 0; i < message.$unknowns.length; ++i)
                    writer.raw(message.$unknowns[i]);
            return writer;
        };

        /**
         * Encodes the specified WebNotificationsInfo message, length delimited. Does not implicitly {@link proto.WebNotificationsInfo.verify|verify} messages.
         * @function encodeDelimited
         * @memberof proto.WebNotificationsInfo
         * @static
         * @param {proto.IWebNotificationsInfo} message WebNotificationsInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WebNotificationsInfo.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a WebNotificationsInfo message from the specified reader or buffer.
         * @function decode
         * @memberof proto.WebNotificationsInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {proto.WebNotificationsInfo} WebNotificationsInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WebNotificationsInfo.decode = function decode(reader, length, z, q, g) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (q === undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw Error("max depth exceeded");
            var end = length === undefined ? reader.len : reader.pos + length, message = g || new C();
            while (reader.pos < end) {
                var s = reader.pos;
                var tag = reader.tag();
                if (tag === z) {
                    z = undefined;
                    break;
                }
                var u = tag & 7;
                switch (tag >>>= 3) {
                case 2: {
                        if (u !== 0)
                            break;
                        message.timestamp = reader.uint64();
                        message._timestamp = "timestamp";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        message.unreadChats = reader.uint32();
                        message._unreadChats = "unreadChats";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        message.notifyMessageCount = reader.uint32();
                        message._notifyMessageCount = "notifyMessageCount";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        if (!(message.notifyMessages && message.notifyMessages.length))
                            message.notifyMessages = [];
                        message.notifyMessages.push($root.proto.WebMessageInfo.decode(reader, reader.uint32(), undefined, q + 1));
                        continue;
                    }
                }
                reader.skipType(u, q, tag);
                if (!reader.discardUnknown) {
                    $util.makeProp(message, "$unknowns", false);
                    (message.$unknowns || (message.$unknowns = [])).push(reader.raw(s, reader.pos));
                }
            }
            if (z !== undefined)
                throw Error("missing end group");
            return message;
        };

        /**
         * Decodes a WebNotificationsInfo message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof proto.WebNotificationsInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {proto.WebNotificationsInfo} WebNotificationsInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WebNotificationsInfo.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WebNotificationsInfo message.
         * @function verify
         * @memberof proto.WebNotificationsInfo
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WebNotificationsInfo.verify = function verify(message, q) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                return "max depth exceeded";
            var properties = {};
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp")) {
                properties._timestamp = 1;
                if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
                    return "timestamp: integer|Long expected";
            }
            if (message.unreadChats != null && Object.hasOwnProperty.call(message, "unreadChats")) {
                properties._unreadChats = 1;
                if (!$util.isInteger(message.unreadChats))
                    return "unreadChats: integer expected";
            }
            if (message.notifyMessageCount != null && Object.hasOwnProperty.call(message, "notifyMessageCount")) {
                properties._notifyMessageCount = 1;
                if (!$util.isInteger(message.notifyMessageCount))
                    return "notifyMessageCount: integer expected";
            }
            if (message.notifyMessages != null && Object.hasOwnProperty.call(message, "notifyMessages")) {
                if (!Array.isArray(message.notifyMessages))
                    return "notifyMessages: array expected";
                for (var i = 0; i < message.notifyMessages.length; ++i) {
                    var error = $root.proto.WebMessageInfo.verify(message.notifyMessages[i], q + 1);
                    if (error)
                        return "notifyMessages." + error;
                }
            }
            return null;
        };

        /**
         * Creates a WebNotificationsInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof proto.WebNotificationsInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {proto.WebNotificationsInfo} WebNotificationsInfo
         */
        WebNotificationsInfo.fromObject = function fromObject(object, q) {
            if (object instanceof C)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".proto.WebNotificationsInfo: object expected");
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var message = new C();
            if (object.timestamp != null)
                if ($util.Long)
                    message.timestamp = $util.Long.fromValue(object.timestamp, true);
                else if (typeof object.timestamp === "string")
                    message.timestamp = parseInt(object.timestamp, 10);
                else if (typeof object.timestamp === "number")
                    message.timestamp = object.timestamp;
                else if (typeof object.timestamp === "object")
                    message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber(true);
            if (object.unreadChats != null)
                message.unreadChats = object.unreadChats >>> 0;
            if (object.notifyMessageCount != null)
                message.notifyMessageCount = object.notifyMessageCount >>> 0;
            if (object.notifyMessages) {
                if (!Array.isArray(object.notifyMessages))
                    throw TypeError(".proto.WebNotificationsInfo.notifyMessages: array expected");
                message.notifyMessages = Array(object.notifyMessages.length);
                for (var i = 0; i < object.notifyMessages.length; ++i) {
                    if (!$util.isObject(object.notifyMessages[i]))
                        throw TypeError(".proto.WebNotificationsInfo.notifyMessages: object expected");
                    message.notifyMessages[i] = $root.proto.WebMessageInfo.fromObject(object.notifyMessages[i], q + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a WebNotificationsInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof proto.WebNotificationsInfo
         * @static
         * @param {proto.WebNotificationsInfo} message WebNotificationsInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WebNotificationsInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            var object = {};
            if (options.arrays || options.defaults)
                object.notifyMessages = [];
            if (message.timestamp != null && Object.hasOwnProperty.call(message, "timestamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.timestamp = typeof message.timestamp === "number" ? BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, true).toBigInt();
                else if (typeof message.timestamp === "number")
                    object.timestamp = options.longs === String ? String(message.timestamp) : message.timestamp;
                else
                    object.timestamp = options.longs === String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber(true) : message.timestamp;
            if (message.unreadChats != null && Object.hasOwnProperty.call(message, "unreadChats"))
                object.unreadChats = message.unreadChats;
            if (message.notifyMessageCount != null && Object.hasOwnProperty.call(message, "notifyMessageCount"))
                object.notifyMessageCount = message.notifyMessageCount;
            if (message.notifyMessages && message.notifyMessages.length) {
                object.notifyMessages = Array(message.notifyMessages.length);
                for (var j = 0; j < message.notifyMessages.length; ++j)
                    object.notifyMessages[j] = $root.proto.WebMessageInfo.toObject(message.notifyMessages[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this WebNotificationsInfo to JSON.
         * @function toJSON
         * @memberof proto.WebNotificationsInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WebNotificationsInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WebNotificationsInfo
         * @function getTypeUrl
         * @memberof proto.WebNotificationsInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WebNotificationsInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/proto.WebNotificationsInfo";
        };

        return WebNotificationsInfo;
    })();

    return proto;
})();

module.exports = $root;
